# Sanaa's directives — 2026-08-28 (captured verbatim by the chief at receipt)

Three directives received in one message. Each is quoted verbatim; routing follows.

## 1. Lesson assigned

> [SANAA-DIRECT] Lesson assigned (number per rule 11 at append): A control defined in terms of the thing it controls is not a control. A planted control must travel the real production path — written by the real producer's code, read through the real reader — and prove the instrument sees a non-zero the same way reality would deliver one. A control that empties the tuple it tests, or writes a schema the producer never emits, tests nothing and certifies blindness. Companion rule canonized: rule 3's question — "was this reader ever shown able to see a non-zero through the real code path?" — is now the birth requirement for every reader/comparator: no instrument grades anything until that answer is yes, demonstrated.

Routed to: verification-supervisor (owner of `scripts/append_record.py` as of 2026-08-28) — append to `docs/LESSONS.md` with the number derived per rule 11 at append time; the companion rule lands as a dated VERIFICATION_CHARTER amendment under rule 6.

## 2. Re-grade sweep ordered

> The buried alarm: six of the seven defects produced false negatives dressed as physics or toolchain findings. That means some recent verdicts may rest on instrument blindness, not reality. Order the re-grade sweep before anything else builds on them: "Enumerate every verdict, docket row, or finding that cited any of the seven defective readers (SO1a/b/c, W3, AV1R, AV2R, D18, D6R); re-grade each from preserved artifacts through the repaired instrument; report which verdicts moved." Per the TOOL LAW, nothing was dropped — artifacts exist, so this is cheap and closes the loop.

Routed to: dafoam-supervisor (owner of all seven readers) to execute BEFORE anything else builds on them; verification-supervisor rules on each moved verdict. Re-grades are successor comparators frozen by sha (rule 2); the original frozen files are never edited.

## 3. Freeze-ahead rule amended; dafoam pull-forward

> Queue depth 0 is a rule violation with an honest cause — so fix the rule, not the teams: when the nearest candidates are blocked on findings, the finding-repairs are the queue: they're frozen, capped, schedulable work items like any case. Amendment: "Freeze-ahead counts repair-registrations; a team blocked on findings freezes the repairs and runs them — queue depth 0 with open findings is impossible by definition." And dafoam specifically: rungs SO-2/SO-3 don't depend on the blocked SO-1 graders' findings — the ladder was built deep precisely so one blocked rung can't starve the family; pull forward.

Routed to: all six supervisors (the amendment binds §2 FREEZE-AHEAD of the 2026-08-27 standing directives, file `etc/sessions/2026-08-27T1654Z_sanaa_standing_directives.md`, which is not edited — this file is the amendment); dafoam-supervisor additionally: pull SO-2/SO-3 forward.
