# SANAA-DIRECT — GUI demo-only curation + multipoint launches authorized (2026-09-01, ~02:10Z)

## Sanaa's words, verbatim

> yes everything in the GUI can be abotu todays demo cases. If you need to
> delete previous stuff that is bothering us rn go for it. About the multi
> point: since its pass on the patched one multi point can be launched on the
> box in the backrgoudn (for both compressible and incompressible). That way
> we can shoot these demos tmr as well or whenever they complete. For now the
> other runs in the box or other non demo related things dont need our
> attention otherwise. All our attention goes towards the demo rn

## Context (chief's reading, disclosed)

1. **GUI curation authorized**: the GUI may present only today's demo cases;
   previous entries/missions cluttering it may be removed. Scope discipline:
   this authorizes deleting GUI-DISPLAY-LAYER items (mission history entries,
   stale tiles, old conversation lists) — it does NOT authorize deleting run
   artifacts, grading records, pre-registrations or anything a record cites
   (rule 6, RESULT_PRIORITY). Where a display item is backed by a record,
   hide/archive from the GUI rather than destroy the record. Inspect before
   delete, per standing law.
2. **Multipoint optimization launches**: both incompressible AND compressible
   multipoint run on the box in the background, on the PATCHED toolchain
   (shipped is GATE FAIL; patched is the PASS side — SO3aR2 grade object).
   Still costed pre-registrations, still via the queue. These are demo work
   (their demos shoot "tmr as well or whenever they complete").
3. Lab-wide attention stays on the demo; nothing else gets attention.
