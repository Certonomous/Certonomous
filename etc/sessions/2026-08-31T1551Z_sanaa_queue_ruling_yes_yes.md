# Sanaa ruling — 2026-08-31, captured verbatim by the chief at receipt

> Yes / yes. Agents may file queue entries — the queue daemon is the lab's ordered launch path, not a workaround. Unregistered feasibility and physics rungs are queue-legal, tagged prereg=FEASIBILITY or prereg=PHYSICS; the runner accepts them and their outputs are never gradeable as verdicts. Heat-transfer files T22, T19b, T20 now.

Routing: heat-transfer files T22, T19b coarse pair, T20 now with the tags as ruled. All teams: feasibility/physics entries carry prereg=FEASIBILITY or prereg=PHYSICS; outputs never gradeable as verdicts. The runner-side acceptance of the tag values is a sanctioned in-flight adjustment if the validator refuses them (cfd owns queue_entry_check.py).
