# SANAA-DIRECT — JF1 internal inconsistencies (verbatim)

> Some internal inconsistencies in the JF1 screen: Wall spacing: 0.316 vs
> 0.398. The resolution table says max wall spacing 0.316 wall units; the
> text says 0.398 "at worst across all five." One number must win —
> presumably the table is one point and the text is the worst point; then
> the table should say which point it belongs to, or carry the worst.
> Two grids in the act's history: 46,180-cell C-grid (the earlier mesh
> page) vs 39,984-cell grid in the current tables. Only one grid family may
> appear on screen; the mesh page shown must be the grid the numbers come
> from. Confirm which one produced the lift table and regenerate the mesh
> page from that case.Duplicate figure entry: "Solved velocity field"
> listed twice.
> The banned opener ("calculations are finished, so I will present them")
> and the self-contradicting certificate line ("issued with the band, which
> is running now") are still in.
