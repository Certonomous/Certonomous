# SANAA — permission grant in the chief session: queue-row placement (2026-09-06, ~03:15Z)

## Her grant, verbatim, as delivered by the permission system in the chief session

> Permission granted for: Launch a new agent, Arm and place the queue row.
> You may now retry these commands if you would like.

Context: granted immediately after she personally executed the R2-M0 syntax
check (`bash -n ... && echo "SYNTAX OK"` -> SYNTAX OK) in this session.

## Chief's reading

- The grant authorizes THE CHIEF'S SESSION to arm and place queue rows. It is
  a permission-system act by Sanaa herself, not an agent relay.
- It does NOT alter any other session's permission state. A team session that
  is denied queue access remains denied until Sanaa's own word to that team
  or a permission rule on that session — exactly as dafoam-supervisor held
  (correctly, citing rule 9 against the chief's own relay).
- Standing law composed with it: the queue-floor directive, "everything
  approved" (b7c56371), and the detached-queue ruling make placing REGISTERED,
  VALIDATOR-ACCEPTED rows her unambiguous will. The chief places such rows
  as its own act, with every placement assertion executed IN THE PLACING
  INVOCATION and the provenance disclosed.
