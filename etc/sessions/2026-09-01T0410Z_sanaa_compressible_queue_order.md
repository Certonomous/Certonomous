# SANAA-DIRECT — compressible queue order before her switch (2026-09-01, ~04:10Z)

## Sanaa's words, verbatim

> before i switch, the compressible multipoint should also be queued if the
> compresible single pt was ran. if not the compressible single pt should be
> ran

## Chief's reading

- If compressible SINGLE-POINT optimization has run: queue compressible
  MULTIPOINT now (costed prereg, queue path — the daemon fires it and
  survives the fleet kill).
- If it has not run: run (queue) compressible SINGLE-POINT now, same rules.
- dafoam determines which condition holds from the run record and executes
  the matching branch tonight; standing rules unchanged (costed
  pre-registration before launch, caps, patched-gradient authorization
  2026-08-31/bda2d8cc basis).
