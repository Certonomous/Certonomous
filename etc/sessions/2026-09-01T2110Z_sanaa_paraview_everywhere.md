# SANAA-DIRECT — ParaView for all acts, canvas retired (2026-09-01, ~21:10Z)

## Sanaa's words, verbatim

> fantastic. Going forward all acts use paraview. Never that trashy canvas
> you were using before

## Chief's reading

Every act's geometry, mesh and field visuals are ParaView-rendered from the
real case files (distro 5.11.2 + xvfb recipe, verified). The in-browser
canvas renderer is RETIRED as a visual source for act content — the
viewport displays ParaView-rendered imagery (stills/sequences; progressive
reveal preserved by frame sequencing). cfd owns the viewport display pipe;
each team owns its acts' render scripts.
