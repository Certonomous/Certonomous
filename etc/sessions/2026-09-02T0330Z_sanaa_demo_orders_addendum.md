# SANAA-DIRECT — demo orders addendum on top of the 0232Z list (2026-09-02, ~03:30Z)

Captured verbatim from the chief session, her reply to the chief's
consolidated approval list. Approves the list ("JF1: yes") and adds on top.

## Sanaa's words, verbatim

> JF1: yes and the 40K is jF1 specific, so any other act should use its own
> number of unknowns. 2. On top of this as i said (and this applies for all
> acts this: Explicit solver + turbulence model + numerics stated on every
> act's methods beat (DMR states inviscid Euler rather than leaving the model
> slot blank). in a table. Also whenevr the lab is listing rules or
> conventions this can also be tables. 3. no dashes anywhere -- 4. All demo
> acts use the same convention for the core min/ wall time numbers, all
> mention the solver/physics/ turbulence model 5. all report tabs should have
> that uncertainty certificate, even if its empty. I wont click on it, but
> since i have anoher demo wiht that certificate i dont want it to look like
> some runs have certificates and some dont itll look inconsitent in the page
> im going to put the videos in. oMotor act: confirm the earlier figure
> orders are still in the lane — two peak columns (core + housing), margin
> computed on the core, "core" not "rotor core," extrapolated-headroom bars
> dropped, ≤10-word titles, no in-figure paragraphs, 0.1 °C significant
> figures until the band lands. (They predate this consolidation; don't let
> them fall between lists.)
> Battery prompt professionalized like the others: "Transient thermal
> analysis of the 8-cell battery module under a takeoff power pulse: peak
> cell temperature, cell-to-cell spread, time to settle."
> Global sig-figs rule stated once: no number on any screen carries more
> decimals than its uncertainty supports — 0.1-precision until a band exists.

## Context (chief's reading, not her words)

1. The routing-policy citation is one POLICY sentence but the unknowns count
   is per-act: JF1 cites ~40,000; every other act cites its own real system
   size. The shared constant must parameterise the count, never hard-code
   40K into other acts.
2. Solver + turbulence model + numerics on every methods beat AS A TABLE;
   and any lab listing of rules/conventions may be a table too.
3. NO DASHES anywhere on screen: em-dash, en-dash, and the house double
   hyphen all banned in rendered demo text. Her own quoted GPU sentence is
   re-punctuated without dashes (commas/colon), content unchanged.
4. One shared convention for core-minute / wall-time reporting across all
   acts; every act names solver/physics/turbulence model.
5. Every Report tab carries the uncertainty-certificate slot, even empty —
   cross-video consistency on the page she will publish the videos on.
6. Motor act figure orders re-confirmed (predate this list, must not drop):
   two peak columns (core + housing); margin computed on the core; the word
   "core", never "rotor core"; extrapolated-headroom bars dropped; titles
   ≤10 words; no in-figure paragraphs; 0.1 °C sig figs until the band lands.
7. Battery act prompt becomes: "Transient thermal analysis of the 8-cell
   battery module under a takeoff power pulse: peak cell temperature,
   cell-to-cell spread, time to settle."
8. Global significant-figures rule, stated once (shared home, demo_mode):
   no on-screen number carries more decimals than its uncertainty supports;
   0.1 precision until a band exists.
