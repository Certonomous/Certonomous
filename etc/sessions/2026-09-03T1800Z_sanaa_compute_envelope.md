# SANAA-DIRECT — $1,000 compute envelope for the industrial ladder, lab law (2026-09-03, ~18:00Z)

## Sanaa's words, verbatim

> Alright so the thing that is the most urgent is the auto-stop fix that
> way the stop that happened yesterday never happens. All teams continue
> working on their tasks, launching and queuing and making sure that the
> box is always full never idle per this lab's rules. Additionally; for
> all teams: SANAA-DIRECT] Compute envelope for the industrial ladder,
> effective now, registered by all teams, as a lab law:
> 1. The per-case dollar approval loop is abolished. I set one standing
> envelope: $1,000 for the benchmark ladder (Rungs 0–3), spendable without
> returning to me. Nothing inside it ever waits on a cost approval again.
> 2. What does not change: every run still registers its cost estimate
> before launch, still carries a hard per-run cap (set by the team at ~3×
> its own estimate, not by me), still reports predicted-vs-actual, and
> still names waste. The estimate is an instrument, not a permission slip.
> 3. Node sizing: rent the node the grid needs — CRM fine-grid class means
> 64–128 core spot instances; never crop a grid to a box. Saturation
> scheduling applies as always.
> 4. Escalation to me only for: a single run projected over $150, the
> envelope reaching 80%, or a rerun of something that already failed twice
> (the no-blind-retries rule at scale). However before escalating this to
> me check that you didn't make bugs/ errors in how you estimated this
> exceedance
> 5. Priority unchanged: M6 family now, CRM registration in parallel, HLPW
> behind CRM held. The first thing I want to see is M6 surface pressures
> against AGARD tunnel data with the family band — a case an industry
> person cannot call easy.

## Context (chief's reading, not her words)

- The envelope is a LADDER budget ($1,000, Rungs 0-3), not a lab-wide one;
  spend against it is tracked in a ledger (derived dollars at the recorded
  rates, labelled derived-not-measured per rule 12) so the 80% trigger is
  measurable. Rentals for the ladder (spot instances, node-sized to the
  grid) draw from the same envelope; per-run caps ~3x the team's own
  estimate; estimate remains an instrument.
- Escalation triggers: single run > $150 projected; envelope at 80%;
  third attempt of a twice-failed thing — and every escalation is
  preceded by an arithmetic self-check (her nod to the 100x class).
- Auto-stop fix is the single most urgent item lab-wide; the box stays
  full per the idle-compute-is-failure ruling.
- First deliverable she wants to SEE: M6 surface pressures vs AGARD
  AR-138 tunnel data with the family band.
