# SANAA-DIRECT — motor: one shared-axis monitor chart; workers 16, wall 39 min (2026-09-02, ~18:00Z)

## Sanaa's words, verbatim

> motor: 1. Replace the 16-panel monitor with one shared-axis chart. The
> per-panel layout has failed three rounds; retire it. New design, one
> panel:
>
> X axis: iteration. Y axis: temperature, °C, 20–110.
> 16 lines, one per operating point — core temperature only (housing goes
> to a second identical chart below, or a toggle; never both quantities in
> one chart).
> Colour by power level (4 colours: 80/155/230/305 W), line style or shade
> by airspeed within each colour. Legend: the 4 power colours only.
> Label at the line's end, not in a header: each line gets a small "10",
> "20", "30", "40" (m/s) tag at its right endpoint; the four power groups
> separate visually by height, so line-end tags are unambiguous.
> Hover/endpoint dot shows "305 W · 10 m/s · 107.7 °C".
> All lines advance together during the solving stage (parallel framing
> preserved).
> This layout cannot overprint: no per-panel titles exist. also, change the
> # of workers in the script and in the screen to 16 instead that way the
> wall becomes 39 min (change that on the worker count and anywhere in the
> script).

## Context (chief's reading, not her words)

- The 16-panel monitor is RETIRED; one shared-axis chart per her spec
  (core only; housing on a second identical chart below or a toggle),
  colour by power, style/shade by airspeed, 4-colour legend, line-end m/s
  tags, endpoint dot "305 W · 10 m/s · 107.7 °C", all lines advancing
  together, no per-panel titles.
- Workers become 16 on script and screen everywhere: one wave of 16, wall
  39 min (the slowest member's 39.2 at screen precision), worker census,
  compute table, wave sentence and estimate beat all re-derived to one
  coherent set consistent with her within-5% order. The measured 12-worker
  execution stays in the internal notes (screen set owner-stated per the
  demo frame).
