# SANAA-DIRECT — battery act comments (2026-09-02, ~06:50Z)

## Sanaa's words, verbatim

> battery comments: The mesh render shows one refined band — the module has
> seven channels. A correct section of this mesh is a repeating pattern:
> eight cell blocks with a thin refined band at each of the seven channels
> (wall layers both sides). One band in the middle of uniform coarse cells
> means the camera is zoomed onto a single channel while the footer claims
> the whole domain (16,608 cells), or the renderer is drawing one region
> only. Fit-to-extents so all seven bands are visible, then a zoom inset on
> one channel showing the wall layers — same pattern as the motor's mesh
> page.
> Monitors, small: x-axis should read time in seconds (not "time step"), and
> shade the 0–60 s pulse window on both panels — the story of the act is
> "the pulse ends, the slope changes," and the shading makes the refusal
> figure (two-arm overlay, still to add) instantly legible. Answer the
> request's three quantities explicitly, even in refusal. The prompt asks
> peak, spread, settle. Peak: "28.7 °C hottest cell at the end of the
> record" — if the temperature is still rising at 900 s, say that: "peak
> over the record 28.7 °C, occurring at t = 900 s — the module has not
> settled within the record." That converts the missing settle-time into a
> stated finding instead of a silent gap (and matches your next-investigation
> line). Spread: give the as-computed spread number with the same "no band"
> caveat, since the field exists — refusing certification doesn't forbid
> reporting the computed value, which the act already does for the peak.
> One figure is missing for the story: the two-arm overlay — hottest-cell
> trace at 10 sweeps vs 20 sweeps, with the 23.2 mK gap visible where the
> takeoff transient is. That's the picture that makes the refusal legible
> ("the two solver efforts disagree by more than the rule allows — that's
> why no certificate"). It's the act's money shot.
> Small: state inlet/coolant temperature in the set-by table (the rise
> number needs its reference on screen), and the figure list shows only two
> figures — add the per-cell traces panel (all 8, pulse shaded) which the
> run has. and for all cases no long sentences.
