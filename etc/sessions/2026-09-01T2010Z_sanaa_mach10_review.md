# SANAA-DIRECT — Mach-10 review + one JF1 line (verbatim)

> more jf1 comment: timed on that workstation by the engineer who runs it
> and no where as i said should it mention any human/ me/ smth we talked
> about. Now mach10: Leaked captions from the jet act: the mesh screen says
> "the grid the lift and the pressures are computed on" and the resolution
> table is titled "Wall and slot resolution" — there is no lift and no slot
> here. Case-specific captions.
> The grid render is broken: image 2 shows the 4 × 1 channel as a thumbnail
> the size of a coin in an empty panel — it should fill the frame, cell
> edges visible, the wall marked.
> Image 3 is wrong outright: a hemisphere of points sitting on a line is
> not this case's geometry (there's no STL — the domain is a channel with a
> wall and an initial shock line). The geometry stage for STL-less cases
> must draw the domain: channel outline, wall segment, initial shock
> position. Whatever produced that dome must not appear.
> Same two sentences as everywhere: the "already solved, so I present them"
> opener, and "certificate issued with the band, which is running now."
>
> Once the captions and the two renders are fixed, this act is showable —
> it's the cheapest, cleanest "verified against exact theory" act you have
> (2.4 minutes,. Let me know when the other ones are done and ill check
> them. Ill check the battery act now
