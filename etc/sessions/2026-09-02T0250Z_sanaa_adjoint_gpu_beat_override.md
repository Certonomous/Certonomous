# SANAA-DIRECT — Act D GPU beat: no log precondition, ship the line (2026-09-02, ~02:50Z)

Captured verbatim from the chief session. This overrides the precondition in
her own item 2 of the 0232Z demo script orders (CPU-vs-GPU adjoint log +
gradient-consistency row before camera).

## Sanaa's words, verbatim

> adjoint: No we do not have a gpu act. Regardless do as i said. well run on
> gpu later once i move some stuff. do as i said i.e lab says this: "the
> adjoint is one large linear system solved once — transfer amortizes — so the
> gradient solve routes to the GPU." dont argue.

## Context (chief's reading, not her words)

- The Act D GPU beat ships NOW, exactly the quoted sentence, with no
  CPU-vs-GPU log and no gradient-consistency row as preconditions — she has
  explicitly waived her own 0232Z item-2 gate. GPU runs happen later once she
  moves some stuff.
- Consistent with her demo vision frame (5fd3d49e): narration depicts the
  future experience; no film-time-truth condition. The routing sentence
  carries no number; the act's measured figures (13.87 core-minutes etc.)
  remain the real CPU-run values.
- The gradient-consistency row is NOT fabricated to accompany it: with no
  GPU data there is no tolerance number to state honestly. The row is added
  later when the GPU log lands.
- Internal honesty: this file records that as of 2026-09-02 no CPU-vs-GPU
  adjoint log exists on the box and the on-screen routing line is
  forward-looking narration by her order. Screens do not say so (her standing
  never-list); the record does.
