# The seven LOST closures and the two UNABSORBED items, finalized — 2026-08-16

**What this document is.** `scripts/check_proposal_surface_coverage.py` had stood at `FAIL` on
**ten** one-sided fault ids and **two** artifact back-references. D225 assigned the **7 LOST
closures and the 2 UNABSORBED items** to Katie with one sentence of standing instruction — *"each
needs an outcome sentence, which is a claim about what the work found."* Nobody had written those
nine sentences. They are written below, **each established by execution against the record rather
than by reading the docket row, and each carrying the commit that decided it.**

**What this document deliberately does NOT do.** It does not write `docket.json`, and it does not
mark any item `done`. For `r2-closure-coefficient-uncertainty` the item's own file carries the
reason, recorded under D219 on 2026-08-15: *"Closing it done requires an outcome sentence, which is
a claim about what the work found and is the owner's to make, not this reconciliation's."* The same
reservation governs the rest. **The blocker on those nine rows had been that the sentences did not
exist; the sentences exist now, and the ratification is Katie's.** Each item below names the exact
decision she takes.

**Method.** The population came from executing the coverage check at HEAD, never from the D225 row:
`python3 scripts/check_proposal_surface_coverage.py`, exit status **1**, read from `$?` and not
through a pipe. It reported 131 inbox ids, 264 docket ids, 75 shared, **245 one-sided** — and
classified **7 LOST, 2 BLOCKED, 1 SHADOWED, 2 UNABSORBED, 46 PENDING, 189 DRAFTED**. The two
BLOCKED and the one SHADOWED are D225's other routes (filers, and the chief) and are not this
document's nine.

---

## A. The seven LOST — a decision recorded only in the inbox file, with the docket carrying no record at all

**Five of the seven already carried a complete outcome sentence, written by the agent that did the
work.** For those five, nothing here is a new claim: the sentence existed and the docket lost it,
which is exactly the class `chief_engineer/agenda.py:1396` names — *"the file holds a record the
docket LOST"*. They are reproduced in substance with their anchors so the ratification is a lookup.

### A1. `s1-cbfs-weighted-reinversion-arm` — RAN, and the pre-registered optimization gates FAILED
Outcome recorded at `6daecbe3`; verdict record `S1_CBFS_WEIGHTED_ARM_RESULT.md` and `71dbf5a8`;
`measured_core_min` **267.07** added at `26a32239`. Gates A and B passed decisively — the
masked-beta control retained 99.6% of the window fix (R_W1 0.9458, nonlocality dead) and the
weighted two-box objective was FD-verified at 0.007–0.033% with **one** adjoint. **Gate C failed on
both pre-registered gates**: G1w 0.05713 against the ≤0.05320 bar and G2 42.7% against >50%. The
eval-8 completion (budget amendment `b0e31fa5`, chief approval `d66f82a5`, 267.07 of an amended 278)
took the step to J_qoi 0.05710 and **G1w failed for real on the original bar**; the budget-limited
hypothesis was recorded **UNRESOLVABLE as posed**, because the session kill destroyed the curvature
state a live eval 8 would have needed. **Still open, and it is a chief call, not an owner call:**
the W1-only arm was left holding for a go/no-go.

### A2. `a3-transonic-adjoint-conditioned-deliberately-not-by-knob-luck` — PROOF CLAUSE NOT MET
Outcome recorded at `1ca900df` (prereg `a9bb202d`, diagnosis and choice `dda82819`, outcome
`368996c3`). The headline was a negative and it was carried as one: **the diagonal-spread route named
in the capability strategy's own wording was measured a dead end for this case** — rung 2 spread
14.40 decades and converged, rung 3 spread 14.47 and did not, so 0.07 decades separated a working
rung from a walled one. Field-block separation, geometric localization and mesh-volume scaling were
each refuted by measurement; the surviving one-level Schwarz reading was pre-registered with a
falsifier and **the falsifier fired** (κ moved 0.979× and 1.062×, residual 3% worse). `pcFillLevel 1`
was reachable and **deliberately not run**, because the diagnosis did not indict dropped fill and a
convergence there would have been knob-luck dressed as proof. Spend 61.6 core-min against ~35
approved, flagged at 45 and again at 58 **before** spending. **Still open, chief:** the strategy's
§2 phrasing was flagged as needing to no longer begin from diagonal spread.

### A3. `w4-defect-acquisition-on-a-second-mesh-family` — GATE RESOLVED, on a third outcome no band anticipated
Outcome recorded at `d63b3212`; predictions pre-registered at `0263d950` and `aa7fcddc`;
`measured_core_min` **15.16**. Neither pre-registered branch fired as worded, because the np=1
control came back **dirty** (CD/shape 92.8% against its own step-stable FD). The acquisition question
was scored instead on the decomposition-invariance instrument registered in Amendment 6 **before** the
separating arms ran: **the defect did not acquire**, and the conjunction of limiter × freestreamVelocity
× scotch cut was measured **insufficient** off the Ahmed family. The dirty control was itself the
session's discovery — the `cellLimited` limiter broke the **serial** adjoint, one word `limited→default`
took it to 0.121%, giving the first decomposition-independent member of the class and a single-process
reproducer. **Still open, and it is a routing decision rather than a measurement:** the upstream-report
addendum was recorded **NOT FILED**, and filing to an upstream project is an owner call.

### A4. `pydafoam-silent-warmstart-state-hazard` — EXECUTED AS PRICED, every record-grade conclusion COLD-CLEAN
Outcome recorded at `d7d38f5a`; audit `WARMSTART_AUDIT.md` at `9bb948b2`; guidelines §8 at `ccf18d2f`;
zero solver core-min, as priced. A nine-row verdict table returned **every record-grade conclusion
cold-clean**. Two WARM flags were raised and **neither overturned anything**: the D3 n15 variant-lever
nulls (logs lost with an interrupted session, nothing currently citing them) and the R5/M6 matrix
dumps (~2% warm-drifted, order-of-magnitude conclusions robust). The item also **disclosed its own
priced signature as invalid** — the fast-warm-primal discriminator did not work, and the validated
one was the first continuity-error value at 55× cold/warm separation. **Still open, chief:** the
regrade decisions were left to the chief by the proposal's own terms.

### A5. `s1-cbfs-weighted-loss-offline-variant` — CAPTURABLE on the pre-registered rule
Outcome recorded at `6daecbe3`; record `S1_CBFS_WEIGHTED_LOSS_VARIANT.md`; verdict commit `71dbf5a8`;
zero solver core-min, as priced. **R_W1 0.9494 and R_W2 0.9468 against a 0.70 bar**, with the entry-7
hurt cap clean and the sparse-point sensitivity agreeing at 0.9340. The region-equalized 0.6348 was
reported rather than suppressed, together with the relocation census driving it. A bonus finding
recovered the exact DV-to-serial permutation from `cellProcAddressing` (5.1e-15) and **retracted the
reinversion FD "neighborhood" labels by dated addendum**, numbers unaffected. Its pre-registered
mapping is what filed A1, priced and **not self-approved**. **Nothing open.**

### A6. `expected-information-gain-per-core-min-beats-the-current-ranking-heuristic` — CLAIM NOT ESTABLISHED, and the numerator won anyway
The file carried **no `outcome` field at all**, which is why it read LOST rather than merely
unmerged; the work nonetheless ran. Record: `campaign/EIG_RANKING_LOOKBACK_2026-08-10.md`, landed at
`99015d92`, zero solver core-min, a re-ranking of 114 already-closed items against their recorded
outcomes. **The claim as filed was NOT established** — both rankings came out *anti*-correlated with
what the closed items delivered, and the record says so in those words rather than reporting a pass
with caveats. **What was established is the numerator**: the information content of a proposal's
pre-declared outcomes predicted delivered value (ρ = +0.31, p = 0.0008), where the source-kind gain
table did not (ρ = +0.12, p = 0.21). **What nobody was looking for**: the `/core-min` denominator was
the broken half of *both* rankings — arithmetically inert on 54 of 114 items, and pointing the wrong
way where it was live (ρ(cost, value) = +0.28, p = 0.002). The recommendation was **ADOPT WITH
MODIFICATION**. **Genuinely open, and stated plainly:** the recommendation was never implemented.
`_GAIN_POINTS` still stood in `sdk/chief_engineer/agenda.py` at HEAD, verified by execution here; the
successor item `eig-bits-as-a-ranking-input-mechanically-scored` was filed 2026-08-10T18:40:00Z and
remained `proposed` and undecided, which is a legitimate PENDING and not a fault.

### A7. `probability-of-rank-a-posterior-over-our-score-against-the-board` — DELIVERED, PUBLISHED, AND SINCE SUPERSEDED TWICE
The file carried no `outcome` field either, but it carried a long `supersession_note`, and the work
is among the most heavily used in the lab. Delivered as `campaign/PROBABILITY_OF_RANK_2026-08-10.md`
with P(rank 1) = 68% (67.6% over 400,000 case-level bootstrap resamples), interval 2–100% at 95%.
**Supersession 1, the restriction:** the item's four decided fields had confined the figure to
internal use; the chief **withdrew** that on 2026-08-10 after Ladder V's cold-reproduction pass
recomputed 0.674 from public data in about a minute, and the four fields were left exactly as decided
rather than rewritten, so the record of what was approved on what grounds was not forged.
**Supersession 2, the figure itself:** the board moved to six entrants on 2026-08-11T23:33Z, and the
recompute landed at `09234034` — **P(rank 1) fell from 68% to 50%**, interval 0–97% at 95%, and the
best-on-board count fell from 4 of 8 to **2 of 8 with zero of eight belonging to our own model**.
**Re-derived independently tonight** by the V10 non-author closure at `c2cd83bf`, by AST-parsing
`LIVE_BOARD` out of `sdk/scripts/probability_of_rank.py` and joining it to the round-5 harness record:
per-case ranks 2, 2, 1, 1, 3, 4, 4, 7 of 7; best-on-board 2 of 8; overall rank 1 of 7; margin
0.0013653082957863563. **Nothing open on the measurement.** The item's own deliverable is live and
current.

---

## B. The two UNABSORBED — an artifact on disk names the item and the docket record carries no trace of it

### B1. `hlpw6-testcase1-coarse-grid-entry` — FEASIBLE ON THE HARDWARE, AND NOT SENDABLE
Artifact `demo-output/website/hlpw6/FEASIBILITY_PROBE.json`, 112.7 core-min measured rather than
estimated. The question the item was opened for was answered **yes**: peak memory 4,548 MiB, 14.5% of
the host, headroom 6.7×, and the binding limit was **not memory**. Two things were built along the
way and both survived the item — `ugrid_to_foam.py`, validated against a 2.66M-cell production grid
to an **exact** boundary-face match, giving this lab its first read path into committee grids at all;
and a corrected cost basis, re-measured at `d45180eb` where parallel efficiency came out **76% and
not the 45% the first pass had priced from a structured proxy**, with serial the worst option on the
board rather than the cheap one.

**The outcome that decides the item, at `9781c723`: the entry cannot be sent.** Submission is a pull
request against the workshop's public repository and requires an account, a workshop-assigned
participant identifier and a merge request — **all three forbidden by the item's own ABSOLUTE list**
— and the mandatory eight-view Tecplot deliverable needs a rendering pipeline the lab does not have,
priced at zero. At `6d72b13c` two of the probe's four honest unknowns closed **against** the item:
twenty second-order configurations were tried on an independent committee grid worse than this one
and **all twenty diverged inside 29 iterations**, including the probe's own hardened first-order
configuration copied across byte for byte — so *"a numerics problem, not a hardware problem"*
understated it, and it was recorded as a **discretisation** problem.

**ROUTES TO KATIE, and it is a decision and not a measurement:** whether to create the workshop
account and participant identifier. The item's own ABSOLUTE list forbids it, so no agent may take it;
until it is taken or refused, the entry is unsendable **by construction and not by capability**.

### B2. `r2-closure-coefficient-uncertainty` — COMPLETE IN SUBSTANCE, THROUGH BOTH STAGES
Artifact `demo-output/website/r2-closure-coefficient-uncertainty/report.json`, which names the
proposal file as its own `agenda_entry`. **Stage one**: 10 of 10 samples, 26.91 core-min against a 60
estimate, band 0.02040 Cd = 4.90% of baseline, and **MATERIAL** — about 19.2× the stated ±0.00053
numerical channel — which is the condition the agenda entry pre-registered for promoting to stage
two. **The joint-corner addendum**, run after the original report and preserved beside it: the
superposition prediction missed by 24.4% of the predicted swing, exceeding every oscillation scale in
the chain by at least 2.4×, so **linear superposition was tested and failed** rather than assumed.
**Stage two**, landed at `b82e6d39`: a 20-sample maximin Latin hypercube (seed 20260726) over the
three most drag-sensitive coefficients, **20 of 20 converged, 0 failed, 0 dropped**, 115.34 core-min,
band 0.017273 Cd = 4.15% of baseline — narrower than the corner envelope, which is the expected
direction and was reported as such. The deliverable is a **measured** parametric basis replacing the
*"queued"* status in `docs/UNCERTAINTY-DOCTRINE.md`'s model-channel recipe.

**ROUTES TO KATIE, named on the item's own face under D219:** the formal outcome sentence that closes
this `done`. The measurements above are what the work found; ratifying them as the item's outcome is
the owner's call, and this pass did not take it.

---

## C. What was NOT finalized here, and why

* **The 2 BLOCKED** (`dafoam-restore-ksp-options-escape-hatch`, `test-the-solver-default-relaxation-across-the-family`)
  and **the 1 SHADOWED** (`tmr-naca0012-verification`) were assigned by D225 to their filers and to
  the chief respectively. They are not the nine and were not touched.
* **`docket.json` was not written.** Every one of the nine either already carries its sentence in its
  own file or has it written above; what remains is ratification, which D219 reserved to the owner in
  writing. The coverage check will keep returning `FAIL` until that ratification lands, and **that is
  the correct behaviour** — an instrument that went green because a grader wrote the owner's sentence
  for her would be measuring nothing.

**Compliance.** No solver runs, no scoring calls; the ledger stood at 6 and was not touched. Nothing
sent, uploaded, emailed or registered — and B1's outcome turns on exactly that constraint, which was
observed rather than worked around. `deb91557` was not moved. `dist/`,
`demo-output/website/latex/`, `motorbike-video/` and `LAPTOP_SHOOT.md` were not opened or written. No
file on a peer's hold list was edited.
