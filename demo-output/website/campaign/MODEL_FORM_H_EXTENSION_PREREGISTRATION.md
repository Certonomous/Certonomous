# Family H extension arm — SA and realizableKE at a 30,000 cap: pre-registration

Written 2026-08-08 by the Cases family supervisor, **before the extension is
launched**. Approved by the chief as the first of the two family-H
successors (entry 1's outcome block, `d76d5389`); parent pre-registration
`MODEL_FORM_H_HILLS_PREREGISTRATION.md` (`28e14422`), whose gate this arm
inherits verbatim.

## What this arm is, and is not

The 12,000-cap run guillotined two **still-descending** solves. This arm
raises only the cap — to **30,000** — for `H_re10595_SpalartAllmaras` and
`H_re10595_realizableKE`, fresh solves under the identical case, gate,
targets and numerics. It is NOT an adjusted criterion: the parent record's
measured descent shows the original 1e-6 targets are reachable, and relaxing
a reachable bar manufactures members (the endorsement quoted in entry 1's
outcome). kEpsilon is NOT in this arm — its iteration-13 FPE is a different
failure class with its own paired diagnosis filing.

## Expected-reachability basis (measured, from the parent records)

- **SA:** Ux/p initial residuals fell 2.2e-5 → 5.1e-6 → 1.1e-6 across
  iterations 4,000 / 8,000 / 12,000 — a factor ~4.5 per 4,000 iterations,
  i.e. ~one decade per ~6,100. From ~1.1e-6 at 12,000, the 1e-6
  residualControl on the remaining fields projects to attainment around
  **14,000–18,000**.
- **realizableKE:** same shape, slower — Ux 8.4e-5 → 2.6e-5 → 1.1e-5,
  p ~2.1e-5 at the cap; ~1.3 decades to go projects attainment around
  **19,000–24,000**.
- 30,000 is therefore ~1.7× the slower member's projected attainment — a
  cap sized to the measured rates, not a hope.

## The pre-declared boundary (the chief's, carried here verbatim in effect)

**If a member stalls at 30,000 — cap-stop without the sentence — we do NOT
chase caps further.** The hills band then waits for the omega-budget work,
and that member's verdict is a measured non-convergence at 2.5× the family's
fast-member attainment, recorded as such. No third cap, no adjusted
criterion, no rider.

## Admission and the containment restatement

Admission: the parent gate verbatim — sentence within the cap, exit 0, no
S1/S2, exactly-2 crossings with the separation-direction check, compliant
mesh (R12 not involved). Records supersede the 12,000-cap exclusions per the
`--redo-excluded` convention (superseded records retained).

After collection, family-H membership is n = 1 (SST) + admitted extensions:

- **n = 3** (both admitted): the entry-1 containment statement COMPLETES —
  primary test as pre-declared in the parent: does [min x_R, max x_R]
  contain the literature midpoint 4.455; per-source containment (4.21 /
  4.65 / 4.69) reported alongside.
- **n = 2** (one admitted): a band exists (≥2 rule) and is reported as an
  interval; the containment verdict is REFUSED per the n<3 membership
  clause, second live application.
- **n = 1** (both stall): no band; refusal stands; boundary clause above
  closes the cap question for this family.

## Predictions, put at risk

- **P1:** both members reach the sentence inside 30,000, near the projected
  windows (SA by 18,000, realizableKE by 24,000).
- **P2:** both admitted members carry a steady bubble (exactly 2 crossings,
  separation first) — the 0-crossings state at 12,000 was an undeveloped
  transient, not a converged attached solution. Named risk: one converges to
  a genuinely bubble-less or multi-crossing wall — then clause 3 excludes it
  with the count recorded, and that IS a physics-flavored finding worth
  having (a closure that steadies to no bubble on the hills), stated as such
  only because the sentence printed.
- **P3 (decides entry 1, same lean as the parent):** every admitted member
  lands ABOVE the literature band (x_R > 4.70), so at n = 3 the band does
  NOT contain 4.455 — the hills join the hump as a linear-eddy-viscosity
  family failure with the constitutive route already excluded by the QCR
  null. Declared alternative unchanged: realizableKE undershoots into/below
  the band and the interval stretches down to contain — scored FALSE for P3,
  and the showcase gains a working epistemic bar instead.

## Budget and mechanics

Measured basis: 10.42 (SA) and 11.92 (rKE) core-min per 12,000 iterations
serial → ~26 and ~30 at 30,000 → **~56 expected; budget cap 65 core-min**
(`--max-core-min 65`). Box shared (load ~7 of 16 at pre-registration: S1
weighted arm + A3 re-file); serial wall time may stretch under contention
and the measured core-min inflates with it — recorded, and the budget's
margin covers it. Launch setsid-detached via the runner
(`--family H --model SpalartAllmaras --model realizableKE --redo-excluded
--iteration-backstop 30000`), one cell at a time, queue gate live, records
self-ledger, band regenerated by the runner's end-of-run `write_band()`.
Watch handoff: the runner process and its `runner.log` are the state; any
session collects with `--list` / the band artifact.

Per L-40 (Verification Charter v1.5 §9), the load-bearing lever of this arm
— the raised cap — has its activity proven by the runtime log itself (the
`Time = N` lines beyond 12,000 and the stop line), and each record carries
`iteration_backstop: 30000`; the model selection lever is proven by the
solver's own "Selecting RAS turbulence model" line, per the family's QCR
activity-check template.

*Nothing below this line existed when the extension was launched.*
