# Cost calibration ledger

**Purpose.** Sanaa's directive, verbatim (2026-08-23): *"for all teams involved
once a process is completed, the estimated costs must be compared with the
actual incurred costs so we can improve the lab's estimates"*. This file is
where that comparison lands — one row per completed process (a rung graded, a
case closed, a curriculum item finished), from every team. The point is
calibration: the ratio column is the lab's estimate-quality record, and the
gap-attribution column is what turns a miss into a better next estimate.
Codified as the calibration bullet of `CLAUDE.md` rule 12; charter anchor in
the 2026-08-23 amendment at the foot of
`docs/charters/COMPUTE_BUDGET_CHARTER.md`.

**Append rules — the same discipline as the docket.**

1. **Append-only.** New rows land at the foot of the table. An existing row is
   never edited; a correction is a new row naming the row it corrects.
2. **Numbers come from committed records, never from memory.** Every row cites
   the results file or commit its figures were read from, and a figure not in
   the record is left out, stated as absent — not approximated.
3. **Units.** Actuals in the lab's measured unit: core-minutes or core-hours
   from logs (say which), GPU-hours for GPU runs. Dollars are **derived, not
   measured**, at the recorded rate ($0.0513/core-h, c7a.4xlarge,
   reported-by-owner — the box cannot read its own billing,
   `COMPUTE_BUDGET_CHARTER.md` §5), and every dollar figure is labelled
   derived.
4. **Gross and cleaned carry the charter §2 meanings** (cleaned = gross minus
   rows the 3600-s stall rule matches). Where a source record uses its own
   cleaned definition, the row says so beside the figure. **Waste stays
   separately named** (charter §6) and is never laundered into either column
   or into the ratio's explanation.
5. **Concurrent appends — this file is written by multiple teams.** Re-derive
   the table's current tail **at commit time, in the same shell invocation as
   the commit**, and land the row via the **private-index protocol of
   `CLAUDE.md` rule 10** (capture HEAD once; `read-tree`; `update-index` this
   path only; `diff-tree` assert; CAS on `refs/heads/main`; post-commit
   verify). Peers commit constantly; a row built against a stale read of this
   file is re-derived, not forced.

**Divergence-by-design — the docket precedent (`CLAUDE.md` rule 11).** The
worktree copy of this file routinely diverges from HEAD and is never "fixed",
synced, or used as a base: private-index commits land rows at HEAD without
writing the worktree, and the worktree may carry a peer's unlanded tail rows,
which `scripts/append_record.py` preserves by design. `git status` and
`git diff HEAD` read this file against the stale shared index and report
phantom whole-file deletions — that output is not evidence about content
(measured 2026-08-23: a staged-deletion reading of every line while all rows
stood intact at HEAD). Rows are built FROM
`git show HEAD:docs/COST_CALIBRATION.md`, re-derived at commit time in the
commit's own shell invocation, landed under rule 10's private-index protocol,
and every landing is asserted insertions-only post-commit with each existing
row byte-identical.

**Format amendment — 2026-08-23: row ids.** Ruled by the chief on
2026-08-23, riding the `9a17109f` close-out of `scripts/append_record.py`.
**Exactly one change:** a leading `id` column was added to the table header
and its separator, and the rows already present were given the ids `C-1` …
`C-10` in file order. **Row data cells changed by this amendment: 0**
— every pre-existing cell is byte-unchanged; only the new first cell was
added, and the change was applied by an assert that refuses on any other
difference. **Why ids:** they are what makes a concurrent append *refusable*
by `scripts/append_record.py`. Without an id its tail arithmetic has nothing
to compare, so under rule 5 above two teams could each land a row and neither
the module nor any reconciliation could **name** the collision — which is the
D369 family's third bite (closure `52e5de39`) waiting in a different file.
One flat `C-` series, not per-team: this ledger is a single chronological
record. New rows continue at the next id, re-derived from the file's own tail
at commit time (`CLAUDE.md` rule 11: the **maximum existing number**, never a
count), and land through `scripts/append_record.py --path
docs/COST_CALIBRATION.md`, which refuses (exit 6) rather than renumbering
when a peer's unlanded row already holds that id.

| id | date | team | process/rung | predicted (core-min or GPU-h) | actual gross | actual cleaned | ratio (cleaned/predicted) | gap attribution | record ref (commit/results file) |
|---|---|---|---|---|---|---|---|---|---|
| C-1 | 2026-08-23 | heat-transfer | T10a-R (three repair arms graded) | 2.40 core-h / $0.123 registered (stop threshold 10x = 24.0 core-h / $1.23) | 6.66 core-h (23,969 core-s) = $0.342 derived | not stated in record — figure taken gross, under contention | 2.77x gross/predicted (no cleaned figure in record); within the registered 10x stop threshold | contention (12 T-family solvers live at launch) plus the view-factor generator's under-predicted memory/wall at n = 18,496 (21.0 GB peak vs 5.5 GB solver-basis estimate, a missed prediction reported as such) | `docs/campaigns/T-family/T10aR_RESULTS.md` §7 |
| C-2 | 2026-08-22 | closure | R5C omega-source repair (27 cases + 2 W2 legacy, GATE FAIL; re-graded 2026-08-23) | 0.210 core-h = $0.0108 (cap 1.0 core-h) | ≤ 0.168 core-h (0.140 measured + ≤ 0.028 bounded postProcess) = ≤ $0.0086 derived | = gross (no ledger row near the 3600-s stall rule; total wall 603.7 s) | ≤ 0.80x — under the estimate | misprediction, conservative direction: 27-run arm under (0.1256 vs 0.184), W2-legacy arm over (0.0140 vs 0.00603), postProcess not separately instrumented so bounded not measured; **waste named: 0.0009 core-h** (D-1, four discarded duct builds) | `cases/RANS_LES_closure_models/R5C_omega_repair/RESULTS.md` §9, prereg frozen at `f364cf2d` |
| C-3 | 2026-08-23 | dafoam | W4 M1+M2 (hump `-9` conditioning; O2 not launched at budget floor) | 40.0 core-min registered (ceiling 60.0) | ≤ 40.12 core-min = $0.03430 derived | ≤ 20.12 core-min = $0.01720 derived (record's own CLEANED: excludes the named waste row, not a stall-rule cleaning) | 0.50x cleaned/predicted; gross/predicted 1.003x, **reported in the record as a coincidence** — 20.00 core-min of waste almost exactly replaced the unbought O2, two errors cancelling | **waste named: 20.00 core-min** (M2 attempt 1 staging fault, §5a, zero measurement) plus misprediction: both of the lane's own cost predictions MISS (C-P1, C-P2 — M1 ≤ 10.25 vs > 25.0 predicted; total stopped at the O2 launch floor, not the ceiling) | `cases/dafoam/ladder-b/W4_M1M2_RESULTS.md` §5, landed at `64479072` |
| C-4 | 2026-08-23 | cfd | DPW8_V2 L4 divergence diagnosis (Arm A relaxation, Arm B momentum-upwind; both arms graded, no gate issued) | 61 core-min projected (29.8 core-min per arm solver at the measured 2.979 core-s/iter basis, plus ~1 core-min preflight) = $0.052 derived; CAP 150 core-min total, 75 per arm — prereg §8, frozen `99f939ee` | 21.11 core-min solver **measured** (Arm A 124.26 s, Arm B 1142.22 s, both single-rank, each from its own `solver.time`; record's subtotal 21.1) plus ≈ 0.4 core-min checkMesh/preflight — **estimated, not separately instrumented**, the record states it as an approximation — total ≈ **21.5 core-min** = $0.018 derived | = gross (record §6: no row exceeded 3600 wall s, max wall 1142.22 s, so there is no stall to separate out) | **0.35x** cleaned/predicted (21.5 / 61); **0.14x of cap** (21.5 / 150) | Two named causes, **zero waste**. (a) Arm A terminated at iteration **182 of 600** by the measured phenomenon itself (SIGFPE divergence) — 2.07 vs 29.8 core-min predicted; **not waste**: the crash *is* the measurement and its partial log is the evidence the diagnosis rests on. (b) Per-iteration **misprediction in the conservative direction** on Arm B — **1.904 core-s/iter** actual (derived, 1142.22 s / 600 iter) against the **2.979 core-s/iter** basis, i.e. 0.64x. That basis was measured 2026-07-29 on a box at **load ~19.5** (`verification/campaign/DPW8_V2_joukowski.md` §3a) and applied today at **load 5.03** (prereg §8), so contention was baked into the basis and then charged again to a quiet box. **Calibration lesson: record the load average beside any per-iteration cost basis, and state the load the basis is being re-applied at.** | `verification/campaign/DPW8_V2_L4_DIVERGENCE_DIAG_RESULTS.md` §6, landed at `b8fe7eea`; pre-registration frozen at `99f939ee` |
| C-5 | 2026-08-23 | cfd | F7a R0 literature diagnostic (comparator front-definition recovery — executed, no solver) | **0 core-min**, the item's own registered price (spec §7.1 quoting prereg §5 row R0, "priced at **0** core-min"); $0 derived | **0 solver core-min** — no solver, no mesh generation, no new field (spec §7: "**Cost: 0 core-min.**"); literature retrieval plus a reader over already-written, git-tracked fields | = gross, 0 core-min (no run row exists, so the 3600-s stall rule has nothing to match) | **exact (0/0) — as predicted, zero-compute** | None — no gap to attribute, and no waste | `verification/campaign/F7a_REGATE_SPEC.md` §7, landed at `3b9bcf31` |
| C-6 | 2026-08-23 | closure | FS5/D476 clip-repair instrument amendment (A1/A2/A4 PASS, A3 GATE FAIL referred to verification; instrument not relied on until that audit returns) | 2–6 core-min registered estimate, cap 30 core-min (0.5 core-h) | ~10–12 core-min = ≤ $0.0103 derived (73.44 s build wall + 105.81 audit CPU-s measured by `/usr/bin/time`; the gate-grading/triage remainder estimated from the one measured ~3x CPU-to-wall ratio, stated not-measured in the record) | 3.0 core-min = $0.0026 derived (record's own cleaned definition: registered scope only — one regeneration + one audit; no stall-rule rows, zero solver) | 0.75x cleaned/predicted (against the 4 core-min midpoint; inside the 2–6 band). Gross/predicted 2.5–3x | misprediction of scope: the estimate priced regeneration + audit but not the gate grading, the A1 refusal proof and the A3 triage that the registration itself required; **zero waste, zero contention** | `cases/RANS_LES_closure_models/_common/features/FS5_D476_CLIP_REPAIR_RESULTS.md`, prereg frozen `bf4956bc`, implemented `7e973ba8` |
| C-7 | 2026-08-23 | heat-transfer | T9aH (T9a re-graded under `Gauss harmonic`, split grading path) | ≤ 90 core-s = 1.50 core-min = $1.28e-03 derived, registered (registered CAP 300 core-s = 5.00 core-min = $4.28e-03 derived) | 21.422 core-s = 0.357 core-min = $3.053e-04 derived | = gross. Largest single wall on the box is 5.89 s (`STATUS.F_f`); no row is within three orders of magnitude of the 3600-s stall rule, so gross and cleaned coincide | **0.238x** gross = cleaned / predicted (**0.071x** of the registered cap). No overrun | misprediction, conservative direction: the registered chain-wall line predicted 12.5 core-s for the two chains against 10.915 core-s measured, and the instrument calls came in under their allowance. **No contention penalty** despite four other lanes' `buoyantBoussinesqSimpleFoam` processes live on the box throughout — the cases are sub-second and serial (`nProcs` 1, peak occupancy 1 core of 16), and the figure is stated gross rather than absorbed. **No waste.** One bound disclosed and NOT counted into either column: the launching lane died before recording `build_t9aH.py`'s own process wall, so that item enters the record as a bound (0.004 core-s of file-write mtime span), never as a measurement | `docs/campaigns/T-family/T9aH_RESULTS.md` §9 (cost table and the registered-versus-actual block) with the bound at §8.4; grade landed at `359cccfb`; per-case markers `verification/runs/T-family/T9aH_runs/STATUS.*` (10 files, all `rc=0`, verified present) |
| C-8 | 2026-08-23 (retroactive — the runs completed earlier and the rung was graded earlier; cost was never recorded at the time, D470) | heat-transfer | K0cG (F14 cooling ladder; gate graded earlier, cost first recorded in the 2026-08-23 dated addendum) | 222.7 core-min (2.557 × (2170.42 + 3055.73) = 13 363 s = 3.71 core-h) = $0.190 derived, registered at `K0cG_PREREGISTRATION.md:93-94` | 300.59 core-min = $0.257 derived — `DONE.S_KE_x` `exec_seconds` 11 105.06 s plus `DONE.S_SST_x` `exec_seconds` 6 930.36 s, ranks = 1 (serial: `launch_all.sh:11` invokes the solver with no `mpirun`, no `decomposeParDict` in either case). The markers hold `ExecutionTime`; the wall-clock (`ClockTime`) variant is 300.62 core-min and agrees to three decimals in dollars | **not derivable retroactively.** The record carries no per-row ledger to clean against, and both rows exceed 3600 wall s (11 105 s, 6 930 s) as legitimate long serial solves rather than stalls — the stall rule cannot be applied without the ledger, so the figure is taken **gross** and said to be gross | **1.350x** gross / predicted (no cleaned figure exists). This is an overrun that was **neither reported nor stopped** at the time, contrary to rule 12; the addendum records that failure rather than excusing it | misprediction: the registered estimate extrapolated linearly in cell count (2.557× the 36 864-cell level) and under-predicted by 1.35x — the record diagnoses no cause beyond the extrapolation itself. **PLUS waste, separately named and never absorbed into the ratio: attempt-1 crash waste ≥ 202.29 core-min ≥ $0.173 derived** (`CRASHED_ATTEMPT_1/S_KE_x/log.solve.tail` last `ExecutionTime` 6 068.61 s + `S_SST_x` 6 068.69 s; D432 host stop at 03:53:48Z). That is a **BOUND, not a measurement** — the tails were truncated mid-iteration, so the last flushed value is the last that reached disk, not the last the solver reached; it is not promoted to a measured cost and D470's "attempt 1 is uncosted" stands for the exact figure. **Campaign total on that bound: ≥ 502.88 core-min ≥ $0.430 derived ≥ 2.26x registered** — the rung was paid for twice and reported neither time | dated addendum at the foot of `docs/campaigns/F14-cooling-ladder/K0cG_RESULTS.md`, landed at `878f1556` — **note: `878f1556`'s commit MESSAGE is mislabeled** (it carries a cfd lane's D481/L-255 text from a same-session scratchpad race, D482); **its TREE was verified here as this one file, +116 lines, and is the right tree**. Docket row D470. All seven cited artifacts re-read on disk for this row and found present with the values above |
| C-9 | 2026-08-23 | dafoam | A3 rung 1 patched-IDWarp np=4 (arms R1-A patched / R1-B literal-shipped / R1-C trivial baseline; all three graded, arm verdict PASS under the per-component rule) | **65.1 core-min** registered (band 37.5 – 100.3; CEILING 127.0) = $0.0557 derived — prereg §8, frozen `5d8e2f52` | **49.06 core-min measured** (wall x ranks / 60 from the run-root `ledger.txt`: R1-A 344 s, R1-B 311 s, R1-C 57 s, all np=4 = 47.466 core-min solver; + guard selftest ~1.13; + pre-flights 0.466) = **$0.0419 derived** | **= gross.** No row approaches the 3600-s stall rule (max wall 344 s), so there is nothing to clean out. Waste is named separately below and is **not** netted off either column | **0.75x** cleaned/predicted (49.06 / 65.1); **0.39x of ceiling** (49.06 / 127.0) | **Misprediction in the conservative direction on all three arms**, plus a named waste row. (a) Every arm came in under its point estimate: R1-A 22.933 vs 27.9 (0.82x), R1-B 20.733 vs 27.9 (0.74x), R1-C 3.800 vs 7.3 (0.52x — **below its registered 4.0 – 13.3 band, scored a MISS as R1-P16**). (b) **Contention inflation was over-predicted and the band held**: measured **1.40x** (R1-A, 344/246 s) and **1.26x** (R1-B) against a 1.7x point estimate on a 1.0 – 2.6x band, with 4 `buoyantBoussinesqSimpleFoam`, 1 `simpleFoam` and a peer dafoam container live throughout and load1 8.4 – 18.9. The 2.6x upper end, widened after rung 2's band missed by 5 s, was not needed — **but widening it cost nothing and is not a reason to narrow it back**. (c) The launch gate cost **0.000** core-min: it opened on **poll 1** for all three arms. **WASTE NAMED: 0.200 core-min** — the pre-registration's §12 illustrative pre-flight command hashes `<pkgdir>/../libidwarp.so`, a path that exists in neither image (the library is in-package), so the registered command returned `FileNotFoundError` and produced no measurement; it was re-run with the path resolved at 0.133 + 0.133 core-min for the two images. That defect is in §12's convenience listing, **not in a gate, threshold, band, cap or label**. Consequence for the table: the pre-flight line ran **0.466** against a registered **0.200**, over by 0.266. **Calibration lesson: a pre-registration's illustrative command block should be executed once, dry, before the freeze — a path bug there is cheap at 0.2 core-min here and would not have been cheap in a line that gated a launch.** | run-root ledger (measured artifact, outside git): `/home/ubuntu/certonomous-runs/P4-a3-rung1-patched/ledger.txt`; prereg frozen at `5d8e2f52`; driver/stager at `789ed3b6`, reader at `7a91919c`. **`cases/dafoam/ladder-a/A3/rung1_patched_idwarp_np4/RESULTS.md` §6 is a DRAFT and is NOT YET COMMITTED** — it is with the dafoam supervisor for a personal read. Per append-rule 2 this row's figures are cited to the measured ledger, not to that draft; **a correcting row naming this one is owed once RESULTS.md lands** |
| C-10 | 2026-08-23 | dafoam | A3 rung 3 patched-IDWarp np=4 (arm R3-A, `ct_cd`) — **NOT A RESULT — stopped by memory** | **52.5 core-min** registered (band 32.0 – 79.1; CEILING 176.0) = $0.0449 derived — prereg §8, frozen `97a54c07` | **6.80 core-min measured** (R3-A 85 s x 4 ranks / 60 = 5.667 from the run-root `ledger.txt`; + guard selftest ~1.13; pre-flight **0.000**, reused from the rung-1 companion's in-session measurement on the identical image) = **$0.0058 derived** | **= gross.** Wall 85 s against a 2600 s timeout, so the 3600-s stall rule matches nothing | **0.13x** cleaned/predicted (6.80 / 52.5); **0.039x of ceiling** | **NOT a misprediction — the prediction was never tested.** The arm was killed at **85 s of a 2600 s budget** by its own registered `mem_guard.sh` on the **HOST FLOOR** limb: `host MemAvailable 7.3944 GiB < floor 8.0 GiB`, sustained 3 consecutive 5-s samples. **The arm did not exceed its own budget** — peak RSS **9.202 GiB** against a 15.0 GiB guard ceiling and a 16 GiB cap. The predicted 757 s wall was never reached, so 0.13x measures an interruption, not an estimate. **WASTE NAMED: 5.667 core-min** — the full solver arm returned **no adjoint, no analytic vector and no measurement of R3-P4**, the 11-checkpoint identity the item was bought for (0 of 11 checkpoints reached; 0 `Main iteration` lines printed). It did return R3-P1, R3-P2 and R3-P3 (per-rank provenance, the 16-digit cold-start signature `1.018123970654079`, and the 1355-colour warm-cache read) as by-products, all HIT — **these are not netted off the waste**: the arm is NOT A RESULT and its spend is reported gross as an unrecovered cost, per charter §6. **THE CALIBRATION FINDING, and it is the row's real content: the registered launch gate is arithmetically incapable of protecting the registered floor for this arm.** §7 sets the gate's memory limb at `MemAvailable >= 16 GiB` and the neighbourliness floor at `>= 8 GiB`; this arm's own peak is **>= 9.2 GiB**. A launch admitted at exactly the limb leaves `16.0 - 9.2 = 6.8 GiB` — **already under the 8.0 floor before a single co-tenant grows.** The gate opened at **16.02 GiB** (poll 2, 0.02 GiB of margin) and the floor was breached 60 s later as the preconditioner assembly allocated. **Rule to carry forward: a launch gate's memory limb must be at least (the arm's predicted peak + the neighbourliness floor).** Here that is **>= 17.2 GiB** on the 9.2 GiB measured-to-kill peak, and **>= 19.65 GiB** on the shipped arm's known 11.65 GiB peak at this mesh — against a box with `MemTotal` 30.64 GiB and five live co-tenants. **Changing either registered number is not a lane's call and none was changed** (gates close after first compute, CLAUDE.md rule 2); this is reported to the dafoam supervisor and to verification, unruled | run-root ledger (measured artifact, outside git): `/home/ubuntu/certonomous-runs/P4-a3-rung3-patched/ledger.txt`; the stop record with its verbatim reason: `/home/ubuntu/certonomous-runs/P4-a3-rung3-patched/MEMORY_STOP_FIRED.patched`; sample trace `rss_patched.txt`; prereg frozen at `97a54c07`; driver/stager at `3525f1d2`. **`cases/dafoam/ladder-a/A3/rung3_patched_idwarp_np4/RESULTS.md` §6 is a DRAFT and is NOT YET COMMITTED** — with the dafoam supervisor for a personal read; **a correcting row naming this one is owed once it lands** |
| C-11 | 2026-08-23 | verification | D471 `check_comparator_freeze.py` repair (verification lane, commit `95333148`) | **none stated at dispatch** — no cost was registered for this item | **not log-measured** — the figure is **lane-close-out-reported**, not read from a timing log: git/AST work over already-committed files, **zero solver compute** — no solver, no mesh, no field written, so no run log exists to read a figure from. No wall-clock instrumentation was kept, and none is invented here | = gross (no run row exists, so the 3600-s stall rule has nothing to match) | **n/a** — no predicted figure to divide by | **no estimate made at dispatch.** The gap cannot be attributed because the estimate side is absent — and that absence *is* this row's calibration finding: an item dispatched with no registered cost cannot be calibrated afterwards, however cheap it turned out to be | `95333148`; `docs/DOCKET.md` D471 row |
| C-12 | 2026-08-23 | verification | D472 `check_verdict_cells.py` landing-control repair (verification lane, commit `8f94f170`) | **none stated at dispatch** — no cost was registered for this item | **≈ 8 core-min gross, lane-close-out-reported, NOT log-measured** — ≈ 12 selftest runs × ≈ 39.5 s wall at 1 rank (≈ 474 core-s). The lane reported the run count and the per-run wall at close-out and cited no timing log, so this is a reported figure, not a measured one. **≈ $0.007 derived** (0.132 core-h at $0.0513/core-h, reported-by-owner rate) | = gross — the longest run is ≈ 39.5 s wall, nothing near the 3600-s stall rule, so there is nothing to clean out | **n/a** — no predicted figure to divide by | **no estimate made at dispatch.** The gap cannot be attributed because the estimate side is absent — and that absence *is* this row's calibration finding: an item dispatched with no registered cost cannot be calibrated afterwards, however cheap it turned out to be | `8f94f170`; `docs/DOCKET.md` D472 row |
| C-13 | 2026-08-23 | verification | D473 `check_registered_deliverables.py` build + archive replay (verification lane, commits `b2d9e7ce`, `dc1a2085`) | **none stated at dispatch** — no cost was registered for this item | **not log-measured** — the figure is **lane-close-out-reported**, not read from a timing log: **zero solver compute** — a checker built and replayed over already-committed records; no solver, no mesh, no field written. No wall-clock figure was kept at close-out and none is approximated here | = gross (no run row exists, so the 3600-s stall rule has nothing to match) | **n/a** — no predicted figure to divide by | **no estimate made at dispatch.** The gap cannot be attributed because the estimate side is absent — and that absence *is* this row's calibration finding: an item dispatched with no registered cost cannot be calibrated afterwards, however cheap it turned out to be | `b2d9e7ce`, `dc1a2085`; `docs/DOCKET.md` D473 row |
| C-14 | 2026-08-23 | verification | `scripts/append_record.py` tail-id repair (verification lane, commit `9a17109f`) | **none stated at dispatch** — no cost was registered for this item | **not log-measured** — the figure is **lane-close-out-reported**, not read from a timing log: **zero solver compute** — a pure-Python repair plus its planted controls; no solver, no mesh, no field written. No wall-clock figure was kept at close-out and none is approximated here | = gross (no run row exists, so the 3600-s stall rule has nothing to match) | **n/a** — no predicted figure to divide by | **no estimate made at dispatch.** The gap cannot be attributed because the estimate side is absent — and that absence *is* this row's calibration finding: an item dispatched with no registered cost cannot be calibrated afterwards, however cheap it turned out to be | `9a17109f`; the `docs/DOCKET.md` D474 note |
| 2026-08-23 | dafoam | B3 decomposition peak-RSS (attempt 2; M0/M2a/M2b/M5 PASS, M3a/M3b/M4 GATE FAIL) | **46.0 core-min** registered (§7 of the prereg, frozen `d062aace`; CEILING 182.0, of which **161.0 remained** after attempt 1 per the Addendum-3 ruling `5edfe8c0`) = $0.0393 derived | **42.91 core-min measured** (wall x ranks / 60, all three rows from logs: planted-control self-test 49 s x 1 = 0.82 from `selftest/selftest_watch.log` 20:42:27Z→20:43:16Z; `D_serial` 1177 s x 1 = 19.62 and `D_simple2` 337 s x 4 = 22.47, both from the run-root `ledger.csv`) = **$0.0367 derived** | **= gross.** Longest row is 1177 wall s, so the 3600-s stall rule matches nothing and there is nothing to clean out. **Attempt 2 carries ZERO waste** — both arms produced graded measurements. Attempt-1 waste is named separately below and is **not** netted off either column | **0.93x** cleaned/predicted (42.91 / 46.0); **0.24x of the 182.0 ceiling**, **0.27x of the 161.0 remaining allowance**. No overrun | **Two-sided misprediction that nearly cancels, plus contention, plus one separately-named waste row from the prior attempt.** (a) **`D_serial` −2.08 core-min** (19.62 vs 21.7 est): **favourable load** — 1177 s against the 1266 s basis measured 2026-08-21 at load ≈ 11–15, re-applied here at load 10 at the gate. (b) **`D_simple2` +1.17 core-min** (22.47 vs 21.3 est): **contention** — 337 s against 301 s with a live peer lane on the box, the departure D-5 the prereg disclosed in advance. (c) self-test −0.18, conservative. (d) **A line NOT measured and therefore excluded rather than approximated:** §7's 2.0 core-min for "watcher + staging + grading" was never separately instrumented (staging wall 2 s and grading ≈ 1 s were observed; the two watchers are 2-s-cadence `bash` loops outside the arms' cpuset), so the measured total is 42.91 **plus a small unmeasured overhead**, stated as such. (e) **WASTE NAMED, from attempt 1 and NOT renamed: 21.00 core-min = $0.0180 derived** (Addendum 1 §A1.4 — a staging-permission defect of the lane's own making killed both arms at `prob.setup()` before any solver work; `rc=1`/7 s and `rc=137`/302 s, zero graded numbers, the frozen grader refused exit 2). **Item total charged: 63.91 core-min = $0.0546 derived, 35.1 % of the 182.0 ceiling; 118.09 core-min of the Addendum-3 allowance remain.** **Calibration lesson, and it is about the estimate's SHAPE not its size: the two solver point-estimates were both taken from a single 2026-08-21 measurement of the same arm and neither carried the load average it was measured at — the same defect the DPW8_V2 row above records. The estimate was accurate (0.93x) by two errors of opposite sign cancelling, not by either arm being predicted well.** | run-root artifacts (measured, outside git): `/home/ubuntu/certonomous-runs/B3-decomposition-peakrss/ledger.csv`, `…/peak_rss.json` (grader output, exit 3), `…/selftest/selftest_verdict.txt`; prereg frozen at `d062aace`, Addenda at `7a007d67` and `5edfe8c0`. **`cases/dafoam/ladder-b/B3/decomposition_peak_rss/RESULTS.md` is a DRAFT and is NOT YET COMMITTED** — with the dafoam supervisor for a personal read. Per append-rule 2 this row's figures are cited to the measured run-root artifacts, not to that draft; **a correcting row naming this one is owed once RESULTS.md lands** |
| C-15 | 2026-08-24 | dafoam | W4 O2 re-buy — offline exact-LU of the hump `dRdWTPC` (`analyze_dump3.py`, `--cpus=1`, 20 GiB cgroup cap) — §3 decision `PENDING`, `spilu` 4/4 singular measured, `splu` killed at the cap | **35.0 core-min** registered (band 25–45; CEILING 50.0; wired stop 45.0) = $0.02993 derived — prereg §4, frozen `8d48fd46` | **15.00 core-min MEASURED** (`logs/o2_rebuy_hump_lu_20260823T211207Z.log.start` 21:12:13Z → `.end` 21:27:13Z = 900 s × 1 cpu ÷ 60; gate poll attempt 1 refused with no container, 0.00) = **$0.01283 derived** | **= gross.** One row, 900 s wall, nothing near the 3600-s stall rule. **Waste: 0.00, named** — every core-minute produced a graded measurement (the 4-of-4 `spilu` singularity, the > 20.0 GiB lower bound on the exact LU), and the cap that ended the run was the pre-registered instrument (§7 of the prereg), so the kill is a real cost of a registered measurement | **0.43×** (15.00 / 35.0); **0.30× of ceiling** | **Misprediction — of the MEMORY band, not the duration.** The frozen band 8.0–20.0 GiB (point 13.5, scaled from CBFS's ≈3 GB of factors × a predicted 2.1–3.5× fill) was low: the requirement is > 20.0 GiB, right-censored at the cap (`memory.peak` = 21,474,836,480 B exactly). **Mechanism: truncation by the registered cap** at 900 s, inside the first `splu` threshold. Contention **0.00** (dedicated cpu at ~100 % throughout). **The 35.0 core-min duration estimate was never tested** — the run stopped for memory, not time — so 0.43× measures an interruption, not an estimate (L-265). Both spend predictions (O2R-P5, C-P1R) MISS for that reason; C-P1 is now two attempts and zero tests old. **Calibration lesson:** a factorization's memory band must be priced from a measured fill on the same operator class, and a run whose registered cap binds is recorded as a memory MISS with its duration row left untested, never as a duration under-run | `cases/dafoam/ladder-b/W4_O2_REBUY_RESULTS.md` §5/§5a/§6 (committed `5a93f6ee`, supervisor close-out §12); prereg `8d48fd46`; run-root log outside git as cited |
| C-16 | 2026-08-24 | closure | Ling2016 TBNN GPU arm (frozen `e8309b6c`, RESULTS `gpu/RESULTS.md`; the lab's first GPU run, `g6.xlarge`/L4 `gpu1`) | **12–52 GPU-h registered range, cap 60** (frozen §6); **P0 in-run projection 10.99 GPU-h** from 18.645 ms/epoch measured over 200 epochs | **10.7054 GPU-h**, `spend.json` stage wall-seconds (38,539.6 s; run window 21:21:29Z→08:03:58Z = 10.708 h wall) = **$8.62 derived** at $0.8048/GPU-h (published price list, retrieved 2026-08-23) | = gross (stages are hours-long by design; no solver-row stall rule applies) | **0.974 vs P0 projection; 0.892 vs the registered floor — BELOW the registered range** | Range floor twice too high: it assumed 5–20× GPU-over-CPU throughput on a 0.78–1.5 s/epoch CPU basis; measured full-batch 18.6 ms/epoch = 42–80×. Inside the projection two errors cancelled: mini-batch ARM-B trials ran **4.2×** the projected per-epoch cost (the F.5(5) caveat, confirmed) and the MLP ran at **0.52×**. **WASTE, separately: 7.88 GPU-h = $6.34 derived** — node idle 08:03:58Z→15:56:45Z after the overnight fleet kill left no agent to report completion (RESULTS D-2, L-268); pre-launch and post-sync idle unmeasured and stated absent | `e8309b6c`, `11f93da6`, `gpu/RESULTS.md` §5, `/home/ubuntu/closure-data/tbnn_gpu/out/spend.json` |
| C-17 | 2026-08-24 | closure | R5C omega-source repair (frozen `f364cf2d`, results commit `0ac76ec2`, D465) — back-row: closed before the ledger existed | **0.210 core-h = 12.6 core-min** registered (§6: 0.184 re-extraction + 0.006 W2 legacy + 0.020 postProcess estimate); cap 1.0 core-h | **0.1395 core-h = 8.37 core-min** solver-measured (`ExecutionTime` × 1 rank, 27 + 2 runs) **+ ≤ 0.028 core-h postProcess bounded** (not instrumented: wall minus solver time) = ≤ 0.168 core-h = ≤ $0.0086 derived at $0.0513/core-h | = gross (all rows < 3600 s) | **0.66 (solver-only) to ≤ 0.80 (bounded total)** | Under-run: the repaired operator settles 10 of 15 hills far earlier than R4's 5,000-iteration cap and the 12 R4-COMPLETE cases at identical iterations, so R4's measured 660 s basis over-priced the hills; postProcess was estimated, not measured, and stays a bound. **Waste 0.0009 core-h** (four discarded duct builds, RESULTS D-1), named separately | `0ac76ec2`, `R5C_omega_repair/RESULTS.md` §9 |
| C-18 | 2026-08-24 | closure | Kaandorp2020_TBRF a-posteriori lane — the six `CBFS13700` rows graded and the lane closed out (H0 **GATE FAIL** ⇒ **NOT A RESULT** for the whole lane, on all three registered cases) | **6.48 core-h = 388.8 core-min = $0.3324 derived**, taken from the FROZEN pre-registration §7 and from nowhere else (0.126 s/iteration on CBFS × the 30,000-iteration cap = 1.05 core-h per CBFS solve, 0.06 core-h per duct solve, in its worst-case line "6 × 1.05 + 13 × 0.06 ≈ 7.1 core-hours"); this pass's nine registered rows are 6 CBFS + 3 `AR_3_Ret_360` ML ducts, so 6 × 1.05 + 3 × 0.06. **Lane cap 15 core-h** | **219.571 core-min measured = 3.6595 core-h = $0.1877 derived** — driver end-to-end (`results.json` `lane_wall_hours` 3.659522, one detached process). Solver subtotal 219.313 core-min from the six rows' own `wall_s` plus the G0a identity pair (69.3 + 1701.6 + 3411.2 + 3192.6 + 1978.7 + 2796.0 + 9.4 s). **ranks = 1**: `nProcs : 1` in every `log.run`, `OMP_NUM_THREADS=1` set by the driver | **= gross, 219.571 core-min.** No row exceeds 3600 wall s — the largest is `MEANB` at **3411.2 s**, 188.8 s (5.2 %) under it — so the stall rule matches nothing and there is nothing to clean out. **ZERO WASTE**: nothing killed, nothing re-run, and the three BLOCKED rows consumed **0.0 s** each (the `RowBlocked` exception fires in `ml_b` before any solver starts) | **0.565x** cleaned/predicted (219.571 / 388.8); **0.581x** for the six rows that actually ran against their own 6.30 core-h. No overrun; cumulative lane spend ≈**5.16 core-h** of the 15 core-h cap | **Misprediction, in the conservative direction, from a per-iteration basis measured over too few iterations — and that is the row's calibration content.** The registered 0.126 s/it came from a **5-iteration** interface check (prereg §2.1), which amortises mesh read, field read and library load across five iterations and so over-states steady-state cost. Measured per-iteration from each row's own `log.run` `ExecutionTime`: `TRUTH` 0.056640, `ML1` 0.065936, `NULL` 0.077998, `ML2` 0.093188, `ML0` 0.106303, `MEANB` 0.112386 s/it — **mean over the five capped rows 0.086891 s/it = 0.690x the registered basis**. Second, smaller: `NULL` converged on `residualControl` at **884 of 30,000** iterations, spending 3 % of the budget the estimate charged it — **not waste, that is the row converging**, and charging every row at the cap is the conservative choice a pre-registration should make. **CONTENTION NAMED BUT NOT CLAIMED**: the five capped rows ran identical meshes for identical iteration counts and spread **1.98x** in per-iteration cost (0.0566–0.1124 s/it) with the fleet live on the box; part is contention and part is real per-configuration pressure-solve work (different `bijDelta`), and this lane **cannot separate the two from what was recorded** and does not attribute the spread to either. **CALIBRATION LESSONS, two: (i) a per-iteration basis taken from a 5-iteration check carries start-up amortisation and reads ~1.45x high — state the iteration count a per-iteration basis was measured over, beside the load average the DPW8_V2 (C-4) and B3 rows already ask for; (ii) the `MEANB` row finished 5.2 % inside the registered `timeout 3600`, so ~6 % more contention would have truncated a scored solve mid-flight — a per-solve timeout wants more margin than one measured run's headroom.** | `cases/RANS_LES_closure_models/Kaandorp2020_TBRF/aposteriori/RESULTS.md` §A.6 of the 2026-08-24 addendum; measured artifact outside git: `/home/ubuntu/closure-data/aposteriori/kaandorp/results.json` and the six `CBFS13700__*/log.run`; pre-registration frozen at `0ebc9d53`, blob `411b25f1d452330558824a52885a2950ceffa54f`, verified byte-identical to disk at grading time |
| C-19 | 2026-08-24 | closure | correction to C-16 (Ling2016 TBNN GPU arm 1): idle-window end | — (correction row; no new prediction) | C-16 stated the idle waste as 7.88 GPU-h (08:03:58Z → 15:56:45Z) and the post-sync idle as "unmeasured". Sanaa reported the instance **stopped** on 2026-08-24 without a clock (`GPU_CAPABILITY_STATE.md` §10); the waste window therefore **ends at the last verified idle read, 15:56:45Z**, and the 15:56:45Z → stop interval is **absent from the record** — neither measured nor estimated. C-16 figures unchanged | = C-16 | = C-16 | No change to C-16 attribution; this row closes the open end of its window at the last measured point and names the unmeasured tail as absent. Cause of the measured waste unchanged: overnight fleet kill; now addressed by the owner-approved self-shutdown rule (`GPU_REPRODUCTION_PLAN.md` A1) | `353925c7`, `GPU_CAPABILITY_STATE.md` §10 |
| C-20 | 2026-08-24 | verification | Cross-team gate audit pass 6 (closure FS5/D476 clip repair) — lane pass `542408f7` + the supervisor's own re-derivation (belief commit, this session) | **3–6 core-min**, the lane's own pre-stated prediction (recorded in pass 6's closing paragraph); zero solver compute registered | **0.128 core-min measured** (lane: `/usr/bin/time` 7.12 s wall / 7.68 CPU-s single core over the A2 re-derivation) **+ ≈ 0.09 core-min** (supervisor's re-read: 5.54 s wall single core, Python over 80 `.npz` + 3 JSONs) = **≈ 0.22 core-min = $0.00019 derived** at $0.0513/core-h, reported-by-owner rate | = gross (no run row; the 3600-s stall rule has nothing to match) | **≈ 0.04× of the low end** (0.22 / 3.0); ≈ 0.03× on the lane's step alone | Misprediction, conservative direction, ≈ 30×: compressed `.npz` reads priced at their uncompressed size, and JSON/git arithmetic priced as if it were compute. **Zero waste, zero contention.** Calibration lesson: an audit pass over already-written artifacts is priced in tenths of a core-minute, not units; the prediction should be built from a measured read of one artifact, not from the artifact's nominal size | `docs/CROSS_TEAM_GATE_AUDIT.md` pass 6 (`542408f7`) and the "Supervisor's own read of pass 6" section appended in this commit |
| C-21 | 2026-08-24 | heat-transfer | E4a (fanPressure BC verification, exact operating-point class; five cases built, launched, completed 5/5 and graded — **rung verdict NOT A RESULT**: I1/I2/P1 PASS, R1/G1/G2/N1/D1 voided by the registered iterative-convergence gate) | **2 285 core-s = 38.083 core-min = 0.6347 core-h = $0.0326 derived**, registered at `E4a_PREREGISTRATION.md` §4, frozen `628e29c4` (10x stop threshold 6.35 core-h) | **171 core-s = 2.850 core-min = 0.0475 core-h = $0.00244 derived** — wrapper start-to-end from the five `E4_runs/<case>/LAUNCH_LOCK/launch.log` files (13 + 24 + 45 + 45 + 44), which **includes** blockMesh + checkMesh (2 core-s of the 171); solver-only from `E4_runs/STATUS.<case>` `wall=` is 169 core-s, and ExecutionTime from the five `log.solve` files sums to 168.93 s. Serial, nProcs 1, so core-s equals wall s | **= gross.** Longest wall on the box is 45 s, nearly two decades below the 3 600-s stall rule, so the stall rule matches nothing and there is nothing to clean out | **0.0748x** gross = cleaned / predicted; **0.0075x** of the 10x stop threshold. **No overrun; no run stopped** | **Misprediction, conservative direction, from a solver-class mismatch in the cost basis.** §4 took 7.5e-6 s/cell-iteration from buoyantBoussinesqSimpleFoam T1c replicates and called it conservative for a momentum-only solver; measured here at **5.50e-7 s/cell-iteration** (F_f: 44.56 s / (4 050 x 20 000)), i.e. conservative by **13.6x**. The basis solves a coupled energy equation plus alphat/k/omega; this rung solves momentum + pressure only, laminar, 2D. **Calibration lesson: a per-cell-iteration basis is not portable across solver classes — measure it on the solver that will run.** Meshing was also over-predicted: 15 core-s registered against **2 core-s** measured across five cases. **Contention is not the explanation and pushed the other way**: three peer buoyantBoussinesqSimpleFoam solvers were live on the 16-core box throughout (load1 3.63 at launch) with this lane at `nice 15` on one core — contention can only inflate the actual, so it cannot produce a 0.075x ratio, and the figure is stated gross rather than netted off. **WASTE: none** — no run stopped, relaunched or discarded; the comparator ran once | `docs/campaigns/T-family/E4a_RESULTS.md` §7 (this table) and §1 (verdict); prereg frozen at `628e29c4`; per-case markers `verification/runs/T-family/E4_runs/STATUS.*` (5 files, all rc=0) and `DONE.*` (5 files); logs `verification/runs/T-family/E4_runs/log.build.20260824T160001Z.txt`, `.../log.mark_done.20260824T160511Z.txt`, `.../log.analyse_e4a.20260824T160518Z.txt` |
| C-22 | 2026-08-24 | dafoam | D460 sweep 1, solver family — the ADF-primal NaN discriminator (prereg frozen `538c9f51` +A1, A2 `f0448fab`, Addendum 3 `a7abde10`; results commit `2cdb7448`). Graded PASS, class CONDITIONING / DIAGNOSABILITY | **5.0 core-min** registered (§9: P-SM 1.5 + F-SM 3.5); HARD CEILING 20.0, per-arm timeout 600 s | **4.700 core-min** = wall × ranks ÷ 60 from `ledger.txt` (P-SM 230 s, F-SM 52 s, both np=1) = 0.078333 core-h = **$0.004019 derived** at $0.0513/core-h (reported-by-owner, not measured) | = gross (longest row 230 s, so the 3600-s stall rule removes nothing) | **0.940** | **The 6% aggregate agreement is a coincidence of two large opposite errors: P-SM 2.56× (3.833 vs 1.5), F-SM 0.248× (0.867 vs 3.5).** (1) *Misprediction, structural, +2.333*: §9 priced P-SM per-iteration from a 1,000-iteration neighbour's 0.95 s/iter, but the solve was never the cost — `ExecutionTime = 6.92 s` for all ten iterations of a 230 s wall; **223 s (97%) is fixed harness overhead** (container start, `loadDAFoam.sh`, IDWarp md5, the A4 whole-filesystem `find /`, OpenMDAO setup, report writes) which a per-iteration basis prices at zero. **A short arm is startup-dominated.** (2) *Misprediction, conditional, −2.633*: §9's F-SM figure was a two-branch max conditioned on the outcome under test ("if healthy … if NaN-contaminated 5.383") and resolved to the cheap branch — register the interval, not the max. (3) *Contention — inferred, direction only, NOT measured*: P-SM ran at load1 14.50, F-SM at 4.31; non-solver phase 223 s vs 34 s, a 6.6× gap on a 3.4× load difference, identical launcher, both `--cpus=1`. No per-phase instrumentation exists, so the split is not claimed. **Waste 0.017 core-min** (ARM P-SM attempt 1, rc=134 in 1 s, void by its own assertions, Addendum 3 §3e), named separately and **excluded from the ratio**; item total charged to the §9 ceiling 4.717 of 20.0 (23.6%), no overrun | `2cdb7448`, `cases/dafoam/d460_sweep1_solver_family/RESULTS.md` §7c |
| C-23 | 2026-08-24 | heat-transfer | T3 rung, **ext1 extension** of the ladder from `latestTime` (D452) — 8 serial cases, 304 000 extra iterations to registered `endTime`s of 28 000–80 000, re-graded by the frozen comparator `analyse_t3.py`; rung verdict **`NOT A RESULT` 4 of 4** at gate (1) of prereg §7.1, 0 of 4 graded | **79.55 core-h = 4 773.0 core-min** registered = **$4.08 derived** — `T3_EXT1_AMENDMENT.md` §5, written **before launch** (Charter §2b condition checked 2026-08-22T17:41:39Z: no `log.solve.ext1` anywhere, rc=2). §10.4 then **revised it upward to 108.3 core-h = 6 498 core-min = $5.56** from a 5-minute measured-rate window taken *after* launch. Registered stops: **$22.93** of ext1 spend (the rung's remaining ceiling, binding) and $40.80 (10× prediction) | **79.968 core-h = 4 798.05 core-min MEASURED** — the eight `verification/runs/T-family/T3_runs/STATUS_EXT1.<case>` wall figures summing to **287 883 wall s**, serial at `nProcs 1` so core-min = wall s ÷ 60; corroborated field-by-field against the last `^ExecutionTime` of each `log.solve.ext1` (`R_f` 160 062.45 s against 162 094 s wall). **= $4.102 derived** at $0.0513/core-h, c7a.4xlarge, **owner-stated and reported-by-owner, not measured** — the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` §5) | **= gross.** All eight rows exceed 3 600 wall s (the smallest, `D_m`, is 6 380 s) and **none is a stall**: the 3 600-s rule detects a hung or abandoned row, and every one of these terminated under OpenFOAM's own hand — `rc=0`, an `End` line, last written time == `endTime`, and the full summed `ExecutionTime` count. There is nothing to clean out. **Waste: nil — 0.00 core-min, separately named** (charter §6): no rerun, no crash, no abandoned segment, no discarded field, no launcher refusal. Every core-minute spent produced a field that was graded | **1.005×** (79.968 / 79.55) against the **pre-launch** §5 prediction; **0.738×** against the §10.4 post-launch revision. Spend reached **17.9 %** of the binding $22.93 stop and **10.1 %** of the 10× stop; neither was approached within a factor of five | **Two cancelling errors — the 1.005× is NOT the record of an accurate model, and must not be filed as one.** **(a) Misprediction of model FORM, not of throughput.** §5's model is `extra iterations × cells ÷ measured rate` with **no fixed-cost term**, so it under-predicts short runs systematically: `W_m` **1.617×**, `D_m` **1.507×**, `R_c` **1.404×**. Their `ExecutionTime`/wall ratios are **0.775 / 0.778 / 0.858** — **14–23 % of their wall was not solver time** (mesh read, field read, first write, wrapper), a cost the model has no term for. **(b) An over-extrapolated contention window.** §10.4 measured its rates over a **5-minute** window on 2026-08-22 17:54–17:59Z with 15 of 16 cores committed, and attributed a 1.36× cache- and memory-bandwidth penalty. The contention fell away as the siblings finished: the critical path `R_f` came in at **0.917× of §5 and 0.653× of §10.4**, finishing **24 h 1 min ahead** of the §10.4 ETA, and its `ExecutionTime`/wall ratio of **0.988** shows its miss is pure throughput with fixed cost negligible. The penalty was real when measured and did not survive two days. **Contention 0.00 attributable to the final total.** **Calibration lesson: add a fixed startup/IO term — order 1 400–1 900 s per case on meshes of 28 160–235 520 cells — to the cell-iteration-rate model, and never extrapolate a minutes-long contention window across a multi-day critical path.** Rung total after ext1: **120.29 core-h = $6.171 derived = 24.7 %** of the $25 ceiling, against §5's predicted $6.15 (**1.003×**) | `docs/campaigns/T-family/T3_RESULTS.md` §14.7, committed `3dd28411`; predictions scored `T3_EXT1_AMENDMENT.md` §15; estimate `T3_EXT1_AMENDMENT.md` §5 and §10.4; per-case walls `verification/runs/T-family/T3_runs/STATUS_EXT1.<case>`; graded artifact `T3_runs/gate_t3.json` sha256 `8e766cd5…4ba573` |
| C-24 | 2026-08-24 | dafoam | Curriculum D1 — A1 lift-constrained drag minimisation (arm E graded, arm O PASS, arm C BLOCKED — registered arms all executed; shipped row re-registered as mini-item D1-C′) | **23.0 core-min** registered (§7 of the prereg, frozen before first compute; contingency 46.0; HARD CEILING 120.0) = $0.01967 derived; curriculum's own prior estimate ~70 | **7.000 core-min MEASURED** (wall × ranks ÷ 60, all four rows from the run-root `ledger.txt`: armE run 1 18 s, armE run 2 27 s, armO 361 s, armC 14 s, np=1) = **$0.005985 derived** | **= gross, 7.000.** Longest wall 361 s; the 3600-s stall rule matches no row. Waste named below, never netted | **0.304×** (7.000 / 23.0); **0.058× of ceiling**; **per-major 0.421×** (0.42127 vs 1.0 core-min/major) | **Misprediction, compounding, in the conservative direction — measured, not inferred:** majors 11 vs 18 predicted (0.61×) and per-major 0.42127 vs 1.0 (0.42×), so the driver cost landed at 0.26× (4.634 vs 18.0). P5's basis priced "~1 line-search primal" into every major, and **all 11 majors took `alpha_pr = 1.00e+00` with `ls = 1` — IPOPT backtracked on none**, so that primal was never bought; the labelled A4-scaled cross-check (≈3.3 core-min/major, which set P5's band ceiling) was wrong by 7.8× and A1's own np=1 `check_totals` anchor was the better basis (itself 3.3× high). **Contention: not separately measured** (arm O launched at load1 4.24/16; no penalty claimed); it cost schedule only — arm C waited 73 s across two `NOT_OPEN` gate readings at 0.000 core-min. **WASTE NAMED 0.533 core-min = $0.000456 derived, 7.6 % of gross: 0.300** (armE run 1 VOID under §4.2(c), L-266) **+ 0.233** (armC crashed at 14 s on `KeyError: 'CD_final'`, L-273). **Arm C's registered 2.0 core-min delivered no work; the shipped-row comparison is re-bought as D1-C′ under its own row, and a correcting row naming this one is owed when it lands.** Calibration lesson: an estimate built from a whole-`check_totals` anchor prices a line search into every major; when the optimiser accepts full steps the per-major basis must be re-derived from the accepted-step composition — the `ls` column is where that is read | `cases/dafoam/ladder-a/A1/curriculum_D1/RESULTS.md` §9 (`b10260a0`); prereg frozen `f07256fb`, Addenda §16 `668ce997` / §17 this commit; run-root ledger (measured, outside git): `/home/ubuntu/certonomous-runs/CURRICULUM-D1-a1-constrained-opt/ledger.txt` |
| C-25 | 2026-08-24 | verification | Instrument build, zero solver compute: `scripts/check_stamp_vs_commit.py` + `scripts/stamp_skew.py` — the stamp-vs-committer-date check for the `bd3edfe8` defect class (an in-record stamp written AHEAD of the wall clock), with the forward tolerance chosen from a 300-commit measurement and ONE skew model shared with `check_harness.py`. Committed `b6829b62`; 9 planted controls PASS; HEAD sweep graded 342 stamps and raised 15 FIRES (13 new, 2 already disclosed). Not wired to any gate — the supervisor's call after the diff read | **≤ 3.0 core-min** stated at dispatch (git-blame/log scanning + selftests, np=1); **zero solver core-min**; lane wall ≈ 60 min | **0.153 core-min** log-measured = 9.19 core-s, `/usr/bin/time` on the four timed invocations (300-commit calibration scan 2.53+0.83 = 3.36 core-s; HEAD corpus sweep 3.77+1.09 = 4.86 core-s; two selftest runs 0.48 and 0.49 core-s), all np=1 → 0.00255 core-h = **$0.000131 derived** at $0.0513/core-h (reported-by-owner, not measured — the box cannot read its own billing) | = gross (longest invocation 4.85 s wall, so the 3600-s stall rule removes nothing). **UNTIMED REMAINDER, NAMED NOT ABSORBED:** the recon greps, the two gate runs (`check_filing.py`, `check_harness.py`), the `--show-all` sweep and the bounded ID-limb feasibility scan were NOT run under `/usr/bin/time`. They are single-ranked git/python of the same order as the timed rows, but they are **not claimed as measured** and are excluded from the figure rather than estimated into it | **0.051x** (0.153 core-min measured / 3.0 core-min predicted cap) | **Misprediction, conservative direction, and the mechanism is nameable.** The ≤3.0 core-min prediction priced a per-line `git blame` over all ~145 corpus files. The instrument as built blames ONLY the lines carrying a candidate token, batching multiple `-L n,n` ranges into one `blame` invocation (150 per call), so 400 candidate tokens across 64 stamped files were attributed and 342 graded in 4.85 s wall. **Blame cost scales with stamped lines, not with corpus size** — that is the whole 20x, and it is the transferable lesson for pricing any future blame-based sweep. No contention (no solver live on this team). **Waste 0.0 core-min:** no invocation was voided. The one control that failed on first run (C6's tolerance mutant, exit 0) was a defect in the mutation HARNESS, not in the instrument — the bare string `FORWARD_TOLERANCE_S = 60` also occurs in the module docstring, so a first-occurrence replace mutated the prose and the mutant passed; found, repaired (newline-anchored site plus a `count == 1` uniqueness assertion) and re-run inside the same build, and that run is INSIDE the measured figure, not beside it. **Lane wall:** ≥ 13.5 min measured (first scratch artifact 16:13:51Z → this row 16:29Z); the lane start was not instrumented, so ≈ 20 min total is an ESTIMATE, not a measurement — against 60 min predicted | `b6829b62`; the measured skew distribution is stated in the header of `scripts/check_stamp_vs_commit.py` and in the `scripts/stamp_skew.py` docstring |
| C-26 | 2026-08-24 | verification | Cross-team gate audit pass 8 — the Ling2016 TBNN GPU arm, the lab's first GPU run (audit committed `9573db65`, sections 35–47 of `docs/CROSS_TEAM_GATE_AUDIT.md`); verdict **SOUND WITH DISCLOSED DEVIATIONS (CANDIDATE)** with one defect found | **≤ 1.0 core-min**, this lane's own pre-stated prediction, recorded in the dispatching brief before any work; **zero solver core-minutes and zero GPU-hours registered** — the pass launches nothing and contacts no GPU | **0.0003 core-min INSTRUMENTED** — two `/usr/bin/time -v` Python steps (the gate re-derivation and the §2 table re-derivation) at 0.01 user + 0.00 sys CPU-s each = **0.02 CPU-s combined**, single core. The remainder — `git` plumbing and blob hashing, ~6 sub-second `python3 -c` reads, one `ssh` probe pair — was **NOT instrumented**, and is stated as absent rather than approximated; each call is individually sub-second, which bounds the pass **well under 0.1 core-min**. **$0.0000001 derived** at $0.0513/core-h (reported-by-owner rate; the box cannot read its own billing) | = gross (no run row exists, so the 3600-s stall rule has nothing to match) | **< 0.1×** of the prediction (< 0.1 / 1.0); **≈ 0.0003×** on the instrumented steps alone | **Misprediction, conservative direction, and the SAME direction and rough magnitude as C-20 — which is the calibration content.** C-20 recorded pass 6 at ≈ 0.04× of its 3–6 core-min prediction and drew the lesson that "an audit pass over already-written artefacts is priced in tenths of a core-minute, not units". This lane predicted ≤ 1.0 core-min — already an order below pass 6's prediction, i.e. C-20's lesson was partly absorbed — and still over-predicted by more than 10×. **The residual error is now structural, not arithmetic:** an audit pass's cost is not compute at all. It is dominated by `git cat-file`/`hash-object` on blobs of tens of kilobytes and by JSON reads of 27 kB, none of which is a core-minute-scale operation; the *wall* time of the pass (≈ 15 min) is model inference and reading, which is not core-minutes and must never be booked as such. **Zero waste, zero contention.** **Calibration lesson, offered as the successor to C-20's:** a read-only audit pass over committed artefacts should be registered at **0.1 core-min or "sub-instrument, not separately costed"**, with the honest note that its real cost is lane wall time, not compute — and a pass that instruments only some of its steps must state the uninstrumented remainder as absent, never fold it into a total. **Instrument note, raised against this team's own tooling:** `scripts/check_record_reconciliation.py` run on `docs/COST_CALIBRATION.md` reports `CONTROL KIND: BROKEN` — its planted forms are the `N-*` heading forms and none matches a pipe-table ledger row, so its zero would be unsupported on this path. It found 9 real divergences here, so this row's finding rests on hits and not on a zero; but the control is a live gap in the checker, docketed to the verification supervisor | `docs/CROSS_TEAM_GATE_AUDIT.md` pass 8 (`9573db65`); audited records `e8309b6c`, `11f93da6`, `353925c7`; ledger rows audited: **C-16 as corrected by C-19** |
| C-27 | 2026-08-24 | verification | Cross-team gate audit pass 7 — dafoam's **W4 O2 re-buy** and the **B3 decomposition peak-RSS** item (audit committed `8f5bfe51`, sections 48–62 of `docs/CROSS_TEAM_GATE_AUDIT.md`; the pass landed after pass 8 and renumbered its sections from the actual tail). Verdicts: O2 **SOUND WITH DISCLOSED DEVIATIONS with ONE DEFECT FOUND** (O2R-P2 graded `PENDING` against a frozen rule that registered MISS), B3 **SOUND WITH DISCLOSED DEVIATIONS** — both CANDIDATE | **≤ 1.0 core-min**, this lane's own pre-stated prediction, recorded in the dispatching brief before any work; **zero solver core-minutes** — the pass launches nothing, and it was read-only toward dafoam's territory throughout | **0.0005 core-min INSTRUMENTED** — one `/usr/bin/time -v` Python step (this lane's own parser over both B3 watcher logs, 738 samples, and both 1.4 MB solver logs) at **0.03 user + 0.00 sys CPU-s**, single core, 0.05 s elapsed = **$0.0000004 DERIVED** at $0.0513/core-h, reported-by-owner, not measured. The remainder — `git cat-file`/`show`, `cmp`, `md5sum`, `sha256sum`, `stat` and arithmetic — is uninstrumented sub-second plumbing and is **not** folded into the figure | **= gross.** No run row exists, so the 3600-s stall rule matches nothing and there is nothing to clean out. **Waste: 0.00, named** — every step produced a finding that reached the audit section | **≈ 0.0005×** of the prediction (0.0005 / 1.0) on the instrumented step | **NOT a misprediction — the "prediction" is a CEILING, and this is the third row in a row to report one as if it were a point.** C-20 recorded pass 6 at ≈ 0.04× and C-26 pass 8 at < 0.1×, both against the same `≤ N core-min` brief form; this pass makes three. A `≤` bound divided by an actual yields a ratio that looks like a 2000× over-estimate and is not one: nothing was estimated. **Calibration content, offered for the ledger's own form:** a zero-compute audit pass should register a **point** estimate of its instrumented compute (for this pass, ~0.001 core-min would have been right) **and** a separate ceiling, so the ratio column measures a prediction rather than a bound — otherwise the ledger accumulates spurious sub-0.01× "mispredictions" that teach nothing. Contention 0.00; waste 0.00; misprediction **not assessable** on a bound. The real cost of an audit pass is lane wall (**≈ 20 min** here against a predicted ≈ 45 min), which the core-minute unit correctly does not bill | `docs/CROSS_TEAM_GATE_AUDIT.md` pass 7 (`8f5bfe51`, §48–§62); audited records `8d48fd46`, `5a93f6ee`, `b69ac6ec` (W4 O2) and `d062aace`, `7a007d67`, `5edfe8c0`, `b5ff25d7`, `bb5088c4`, `b8ba5f8f` (B3); ledger rows audited: **C-15** and the **id-less B3 peak-RSS row** flagged at §58 |
| C-28 | 2026-08-24 | dafoam | **Correction to the id-less B3 decomposition peak-RSS row** (line 89, verified at `239321af`; committed `d45596f8`, 2026-08-23 21:14:53Z) — two record-hygiene defects: its missing id, and its stale "NOT YET COMMITTED" reference cell. Raised by verification's cross-team gate audit pass 7 (`8f5bfe51`, §58) | — (correction row; no new prediction, no new compute — **0.00 core-min**) | = the corrected row (**42.91 core-min**, unchanged) | = the corrected row (**42.91 core-min**, unchanged; gross = cleaned) | = the corrected row (**0.93×**, unchanged) | **No figure, ratio, verdict or attribution moves; this row corrects two statements about records.** **(a) Missing id.** The corrected row is the only row in this ledger whose first cell is a date rather than an id, so it cannot be cited. **This row is its citable handle: the id-less row at line 89 is hereafter cited as `C-28`.** The id is **maximum-existing + 1, re-derived from this file's own tail inside the commit's shell invocation** (`CLAUDE.md` rule 11; the 2026-08-23 format amendment's "new rows continue at the next id ... never a count"), **not its position**: positionally the row sits between `C-14` and `C-15`, but **`C-15` is taken by the W4 O2 re-buy row (`b69ac6ec`) and NO existing row is renumbered.** The id is assigned by this appended row rather than written into line 89 because **append rule 1 of this file forbids editing an existing row** — "a correction is a new row naming the row it corrects". Audit §58 offered both remedies; this is the second. **Residual, stated rather than hidden: line 89's own first cell still holds a date**, so a scan for rows whose first cell begins `C-` still finds one un-id'd row; only a chief-level format amendment of the kind that added ids to `C-1`…`C-10` could change that cell, and no lane may take it. **(b) Stale reference cell.** The corrected row states that `RESULTS.md` **"is a DRAFT and is NOT YET COMMITTED"**. That was **true when written and went stale 4 min 24 s later**: this row committed 21:14:53Z, `RESULTS.md` committed `b5ff25d7` at 21:19:17Z, the supervisor's read and the item-level verdict recorded `52c26ec1` at 21:22:42Z (L-261 plus the dafoam board block). Its companion clause is **unchanged and still correct**: per append rule 2 the row's figures are cited to the measured run-root artifacts, not to that record. The row also wrote its own remedy — "a correcting row naming this one is owed once RESULTS.md lands" — and this row is the discharge of it. **Calibration content, about this ledger's form rather than about compute:** a row whose reference cell describes a sibling record that has not landed yet dates itself in minutes. Either land the record first, or write the cell as "landing this session, correcting row owed" — and then actually owe it. Contention 0.00; waste 0.00; misprediction not assessable (no prediction was made) | this row: `docs/CROSS_TEAM_GATE_AUDIT.md` §58 (`8f5bfe51`); corrected row `d45596f8`; `cases/dafoam/ladder-b/B3/decomposition_peak_rss/RESULTS.md` at `b5ff25d7` plus its 2026-08-24 **Addendum 1**, landed in this same commit; verdict of record `52c26ec1` (L-261 at `docs/LESSONS.md:9803`, dafoam board block in `docs/LAB_STATE.md`); registration `d062aace` + Addenda `7a007d67`, `5edfe8c0` |
| C-29 | 2026-08-24 | dafoam | A3 rung 3 patched-IDWarp np=4 **ATTEMPT 2** — the identity arm graded on its terminal event: **IDENTITY CONFIRMED, 11 of 11 checkpoints bit-identical to the SHIPPED-equivalent ‡ path through iteration 1000**, deliberate stop (`identity_stop.sh` exit 5, rc=137), item verdict **GATE FAIL — adjoint, inherited**. Attempt 1 of the same rung was `NOT A RESULT — stopped by memory` at 6.80 core-min under a separate, spent-and-closed budget | **51.8 core-min** total (band 31.0–78.4), of which arm R3-A **50.5** (point 757 s wall, band 445–1157 s / 29.7–77.1 core-min), guard selftest 1.2, per-rank pre-flight 0.133, polling 0.000; **HARD CEILING 176.0** core-min = $0.1505 derived. Pre-registration frozen `606930b4`, Amendment 1 (before first compute) `8a0b440d` | **36.733 core-min = $0.0314 derived** — arm R3-A **35.467** measured from `ledger.txt` (`t0=1787589001`, `t1=1787589533`, wall 532 s × 4 ranks ÷ 60), guard selftest **1.133** measured from `guard_selftest.log`'s own stamps (68 s × 1 rank, 16:28:09Z→16:29:17Z), per-rank pre-flight **0.133 registered / ≤ 1.333 BOUNDED NOT MEASURED** (no script writes a t0/t1 for §12 step 3; bounded ≤ 20.0 s wall between `guard_selftest.log`'s terminal stamp and `preflight.log`'s mtime, at `--cpus=4`), polling **0.000** (gate opened on poll 1 of ≤360; host-side `ps`/`awk`, no container). **Conservative upper bound on the total, carrying the pre-flight bound: 37.933 core-min = $0.0324 derived** | **= gross, 36.733 core-min.** The charter §2 3600-s stall rule matches **no row**: longest wall is the arm's 532 s, then 68 s, then ≤ 20 s. Stated rather than left blank, per the item's §7.2 item 1 | **0.709** (36.733 / 51.8); **0.732** on the conservative bound (37.933 / 51.8); **0.209 against the 176.0 ceiling**. Arm line alone **0.702** core-min and **0.703** wall — inside the registered band on both, so **R3-P8 scores HIT on the band while its point estimate misses by −29.7%** | **CONTENTION, in the *under* direction, and it is essentially the whole gap.** §8 derived the point as `445 s idle basis × 1.7× contention point`, the 1.7 from a 1.0–2.6× band measured on this box at rung 2 (arm P-A: 955 s against a 452 s idle basis = 2.11×). Realised factor **532/445 = 1.1955×**, near the band's floor: the box was quiet — **13 free cores, MemAvailable 27.19 GiB** at the gate, `load1` 5.49, host MemAvailable never below **15.2871 GiB** across 87 samples. Arithmetic: `(1.7 − 1.1955) × 445 s = 224.5 s = 14.97` of the **15.033** core-min arm gap; residual 0.06 is rounding on 756.5→757. **MISPREDICTION, named and not excused by its inheritance:** the 1.7× point missed by **+42%**; §6 R3-P8 registers the 757 s point as INHERITED from `97a54c07` §8 and **UNVALIDATED** (ruling R4), and §8 requires the miss reported as a miss — it is. The band held; the point did not. The 445 s idle basis stays unvalidated (the box was not idle; 532 s bounds it from above, confirming nothing). **Structural cause worth carrying forward: Amendment 1's tightened launch gate (memory limb 25.0 GiB = 82% of MemTotal) selects for a quiet box by construction, so a point estimate carrying a *contended-box* multiplier is systematically high behind such a gate — C-4's lesson arriving from the other direction, where there a contention-laden basis was re-applied to a quiet box and here the gate itself produced the quiet box. Calibration rule candidate: record the gate's own memory limb beside any contention multiplier, and state which side of the gate the multiplier was measured on.** **WASTE: 0.000 core-min**, named separately and never folded into either column or into the ratio's explanation — one arm, one launch, no abort, no restage, no re-run, no second budget; the gate opened on poll 1 so no polling wall was consumed; the guard selftest is a bought §8 instrument (R3-P9's planted-difference control), not waste; and the deliberate iteration-1000 stop is a **registered cost decision that avoided ~69% of the arm's wall** (the shipped run's own ExecutionTime stamps, 435.44 s at iteration 1000 against 1416.00 s at 4000), not waste | `cases/dafoam/ladder-a/A3/rung3_patched_idwarp_np4_attempt2/RESULTS.md` §7 (§7.1 spend, §7.2 the registered predicted-vs-actual deliverable, §7.3 the two-attempt item total **43.533 core-min = $0.0372 derived** across two separate 176.0 budgets, no carryover); pre-registration frozen `606930b4`, Amendment 1 `8a0b440d`; run root `/home/ubuntu/certonomous-runs/P5-a3-rung3-patched-attempt2` (`ledger.txt`, `rss_patched.txt`, `guard_selftest.log`, `launch_condition.txt`, `patched.log`). Rate $0.0513/core-h, c7a.4xlarge, reported-by-owner; **every dollar figure derived, not measured** (`COMPUTE_BUDGET_CHARTER.md` §5) |
| C-30 | 2026-08-24 | verification | Cross-team gate audit **pass 9** — heat-transfer's **T3 ext1 re-grade** (auditee commit `3dd28411`, 2026-08-24T16:18:52Z; sections **63–71** of `docs/CROSS_TEAM_GATE_AUDIT.md`); verdict **SOUND WITH DISCLOSED DEVIATIONS (CANDIDATE)** with **two defects found** — an inverted Richardson-extrapolate sign in two frozen T-family comparators, and an in-record stamp inside a frozen pre-registration written ahead of its own commit. Read-only toward `verification/runs/T-family/` and `docs/campaigns/T-family/`; no solver launched, no T-family comparator re-executed | **CEILING ≤ 1.5 core-min, pre-stated in the dispatching brief before any work. POINT ESTIMATE: NOT PRE-STATED — disclosed, not retro-fitted.** The brief gave a bound and no central figure, which is exactly the shape this ledger's own rule warns about (a ceiling reported as a point was the defect in three consecutive rows), so **no ratio against a point estimate can honestly be computed for this row.** For the next pass of this class this lane registers a point estimate **now**, in advance: **0.15 core-min**, ceiling 1.5 core-min. **Zero solver core-minutes and zero GPU-hours registered** — the pass launches nothing | **0.0553 core-min INSTRUMENTED** — three `/usr/bin/time` steps, single core, summing to **3.32 wall s**: the completion-rule re-derivation over all eight T3 cases (own parser over `log.solve` + `log.solve.ext1`, 3.13 s), the Celik/ASME Roache re-derivation over `gate_t3.json` (0.03 s), and `scripts/check_stamp_vs_commit.py` over the three T3 records (0.14 s, run twice; one instance timed). **NOT instrumented:** ~20 `git` plumbing/blob-hash calls, ~6 sub-second `python3 -c` reads, three `git show \| sha256sum` prefix comparisons and two `scripts/check_record_reconciliation.py` invocations — **stated as absent rather than approximated**, each individually sub-second, which bounds the whole pass **under 0.30 core-min**. **$0.00005 derived** at $0.0513/core-h (owner-stated rate, reported-by-owner; the box cannot read its own billing, `COMPUTE_BUDGET_CHARTER.md` §5) | **= gross.** No run row exists, so rule 12's 3 600-s stall heuristic has nothing to match; nothing was rerun, abandoned or discarded. **Waste: nil, 0.00 core-min, separately named** (charter §6) | **≈ 0.04× against the CEILING** (0.0553 / 1.5 on the instrumented figure; ≤ 0.20× on the un-instrumented bound). **Against a point estimate: NOT COMPUTABLE — none was registered.** The ceiling is reported as a ceiling and is not silently promoted to a prediction | **Two causes, and the first is a process gap, not a misprediction. (a) No point estimate was registered**, so the headline number here is a bound-satisfaction, not a calibration signal; the ledger learns nothing about this lane's estimating accuracy from it, and saying so is the whole value of the row. Remedy registered above. **(b) The instrumented fraction is small.** The pass is dominated by `git` plumbing and file reads that were never wrapped in `/usr/bin/time`, so 0.0553 core-min is a **floor**, not the spend; the 0.30 core-min figure is a bound from per-call sub-second observation, **not a measurement**. **Calibration lesson for audit-class passes: wrap the whole pass in one timed shell, not the three analysis steps** — an audit's cost is its plumbing, and this lane instrumented only the parts that looked like compute. **One non-cost item raised in passing:** `scripts/check_record_reconciliation.py --path docs/COST_CALIBRATION.md` returns **CONTROL KIND: BROKEN / ZERO_IS_UNSUPPORTED** — its planted forms are `N-` series and cannot match this file's `C-` id pattern, so its zero is unsupported on this path (its non-zero finding, 14 ids in HEAD and not in the worktree, is D486 decay and is real) | `docs/CROSS_TEAM_GATE_AUDIT.md` §§63–71, **landed in the same commit as this row** (this row cannot cite that sha: the audit sections and this row are one atomic commit under the rule-10 private-index protocol, so no sha exists at writing time; `git log --oneline -1 -- docs/CROSS_TEAM_GATE_AUDIT.md` resolves it); auditee `3dd28411`; heat-transfer's own calibration row for the audited item is **C-23** and was re-derived independently in §69 (287 883 wall s → 4 798.05 core-min → 79.968 core-h → $4.102 derived; rung total 433 040 s → 120.29 core-h → $6.171 → 24.7 % of the $25 ceiling) |
| C-31 | 2026-08-24 | dafoam | Curriculum mini-item **D1-C′** — the SHIPPED-image endpoint gradient at arm O's converged design point (A1 NACA0012, np=1); the re-buy of D1 arm C after L-273, under its own registration rather than an in-place repair. One container, `rc=0`, `OOMKilled false`. **G-C1 `PASS`** (all four components ≤ 0.2551 % against their own FD, zero sign flips, all four GRADED); **G-C2 `PASS`** — D1 §3.3's toolchain deliverable discharged. All ten gates returned a verdict, no falsifier fired, and the registered point prediction **P5 = `GATE FAIL` is a MISS** — the shipped and patched analytic gradients proved indistinguishable at this design point (≤ 2.8e-06 relative) | **1.62 core-min** registered point (§8 of the pre-registration, frozen `c19e0cbc` **before the run root existed**; contingency 3.24; **HARD CEILING 10.0** = $0.00855 derived). Itemised from six named, measured D1 anchors rather than a rule of thumb; the supervisor's indicative ~2.5 core-min was disclosed and **not** adopted | **1.483 core-min MEASURED** (89 s wall × 1 rank ÷ 60; single row of the run-root `ledger.txt`) = **$0.0012680 DERIVED** at $0.0513/core-h — reported-by-owner, **not measured**: the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` §5) | **= gross, 1.483.** One row, wall 89 s; the 3600-s stall rule matches nothing, so gross = cleaned | **0.915×** (1.483 / 1.62); **0.148× of the hard ceiling** | **MISPREDICTION −0.137 core-min (−8.5 %), and the headline ratio is the least informative number in this row: it is the near-cancellation of two ~14 s errors in opposite directions, each measured from the log's own `ClockTime` stamps.** **(a) The cold primal came in at ≈ 6 s against 20 s registered** — the injected vector IS arm O's optimum, so the primal converges in ~60 iterations from `0/`; the 20 s anchor priced "the deformed, cambered design point" and a *converged* one is ~3× cheaper. **(b) The first adjoint came in at ≈ 23 s against 9 s registered, and the log states the entire difference outright: `Calculating dRdW Coloring... Completed! 14.47 s`.** The 8.58 s anchor was read from arm O's log at elapsed 273.19→281.77 s — a **mid-run** adjoint on a case whose `dRdWColoring_1.bin` was already on disk — whereas gate G-C8 forces this arm to be cold by construction, so it had to buy the colouring. **Transferable rule: an adjoint on a COLD-staged DAFoam case is priced as `mid-run anchor + colouring`, with the colouring read from the log line rather than estimated; a mid-run anchor applied to a cold arm under-prices the first adjoint by ~2.5× on this case.** Every other stage landed slightly under: container start + teardown 15.86 s vs 19 s, the 16 warm perturbed primals ≈ 42 s vs 45 s, the trivial pair ≈ 2 s vs 4 s. **CONTENTION 0.000, and not merely unmeasured:** the launch gate read `load1 = 4.83` on 16 cores with 27.50 GiB available and **opened on the first reading**, so unlike D1 arm C — which waited 73 s across two `NOT_OPEN` readings at 0.000 core-min — this arm waited zero, and the container was pinned `--cpus=1` on a quiet box. **WASTE 0.000 core-min, named separately and folded into nothing:** one staging, one gate reading, one launch, one container, `rc=0`, no abort, no re-stage, no re-run, no voided arm, no second budget. **The `s_lo` rung is a BOUGHT instrument, not waste** — registered ≈ 0.37 core-min, **measured ≈ 0.35** (21 s of `ClockTime` across the four `s_lo` pairs, 23.6 % of spend) — and it is what let the shipped row be graded on the same two-step plateau test as the patched row beside it. **CORRECTION TO `C-24`, which is why this row exists:** C-24 closed with *"a correcting row is owed if arm C later runs"* and named arm C's 0.233 core-min as waste (crashed at 14 s on `KeyError: 'CD_final'`, L-273). **Arm C's registered 2.0 core-min is hereby discharged: the shipped-row comparison was re-bought under this item's own registration for 1.483 core-min, and it delivered.** C-24's figures, ratio, waste attribution and verdict are **unchanged — no number in it moves, and no row is renumbered.** **Prediction-quality note on this ledger's own form, answering C-27:** unlike C-25/C-26/C-27, whose briefs registered a `≤ N` *bound*, this item's 1.62 is a genuine **point** estimate with a separate ceiling — so its 0.915× measures a prediction rather than a bound, which is exactly the form C-27 asked the ledger to adopt | `cases/dafoam/ladder-a/A1/curriculum_D1_Cprime/RESULTS.md` §10 (this commit); pre-registration frozen `c19e0cbc` (§8 cost, §6 gates, §7 predictions), re-verified on-disk == HEAD blob by the launching lane before staging; run root (measured, outside git): `/home/ubuntu/certonomous-runs/CURRICULUM-D1Cprime-a1-shipped-endpoint/` — `ledger.txt`, `preflight_history.txt`, `armCprime_20260824T171942Z_1479979.log`, `endpoint_shipped_20260824T171942Z_1479979.json`. **Row corrected: `C-24`.** Rate $0.0513/core-h, c7a.4xlarge, reported-by-owner; **every dollar figure derived, not measured** |
| C-32 | 2026-08-24 | verification | Instrument extension, zero solver compute: the **ids-written-ahead limb** of `scripts/check_stamp_vs_commit.py` (D488/C-15 instance class — an id cited in prose in a commit that lands before the commit appending that id's row), plus the **report-only wiring record**. Committed `02a84b18`. Selftest exit 0 — **11 named controls, 4 mutants, 15 results, 0 failed**; report-only HEAD sweep exit 0 at `86fb1b34` (limb 1: 66 files / 462 tokens / 404 graded / 58 UNMARKED / 2 PLANNED / 19 FIRES; limb 2: 90 files / 3871 id tokens / 3833 graded / 38 DANGLING / 280 FIRES). No shared audit re-run entry point exists, so none was invented — the invocation lands as §9 of `docs/FAIL_OPEN_GATE_AUDIT.md`. Never a gate, never `--strict`; `check_harness.py` and `check_filing.py` untouched | **1.0 core-min point estimate, 3.0 core-min ceiling**, BOTH registered at dispatch (blame/log scanning + selftests, np=1); **zero solver core-min**; no GPU | **≈ 2.06 core-min bound**, of which **0.729 core-min is log-measured** = 43.76 core-s at np=1 (`/usr/bin/time` on the two REQUIRED runs: selftest wall 1.00 s, report-only sweep wall 21.20 s = 22.20 core-s; plus two development runs timed with the shell `time` builtin, 20.822 s sweep + 0.740 s selftest = 21.56 core-s) → 0.01215 core-h = **$0.000623 derived** at $0.0513/core-h (reported-by-owner, **not measured** — the box cannot read its own billing). **UNTIMED REMAINDER, NAMED AND NOT ABSORBED: ≈ 1.33 core-min** (three further full sweeps at ≈ 21 core-s, two further selftests, one feasibility probe and ≈ a dozen `git show`/`grep` reads) — same order and same single rank as the timed rows, but **claimed as a BOUND, never as a measurement** | = gross. Longest single invocation is 21.20 s wall, five orders below the 3600-s stall rule, so the stall cleaning removes nothing and gross = cleaned | **0.73x measured / point estimate** (0.729 / 1.0); **0.24x measured / ceiling**. On the untimed bound: **≈ 2.1x / point estimate**, **≈ 0.69x / ceiling** — inside the registered ceiling on either reading, so no overrun and nothing stopped | **Misprediction against the point estimate, conservative against the ceiling, and the mechanism extends C-25's own lesson.** C-25 measured the one-limb sweep at 4.85 s wall and drew the lesson that *blame cost scales with stamped lines, not corpus size*. The second limb attributes ~3,871 id-token lines plus the ~900 defining rows on top of limb 1's 462 stamp tokens — **8.4x the tokens for 4.4x the wall** (4.85 s → 21.20 s), because the 150-range `-L` batching amortises the extra lines rather than paying per line. **Refined lesson for pricing any future blame-based sweep: cost scales with attributed lines, SUBLINEARLY, at roughly the square root of the token-count ratio under this batching** — the 1.0 core-min point estimate was priced from C-25's single-limb figure and did not carry the second token family. **No contention** (no solver live on this team; np=1 throughout). **Waste 0.0 core-min:** no invocation was voided. One control failed on first run — the new C6 mutant for the limb-2 comparison reported "mutation site occurs 2 times, not once" — which is a defect in the mutation HARNESS and not in the instrument, the same class C-25 records for the tolerance mutant: the bare call text also occurs inside the mutants list, so it was re-anchored on its own newline and indent and re-run **inside this build**, and that run is INSIDE the measured figure, not beside it | `02a84b18`; §9 of `docs/FAIL_OPEN_GATE_AUDIT.md` (invocation, counts, and the disclosure that 280 limb-2 fires is **not** 280 findings — 256 of 281 at the preceding tree `e25908fe` are D-family against `DOCKET.md` and 124 exceed 24 h, which is the `git blame` last-toucher limitation, stated in the instrument's own output and cutting **both** ways for limb 2); measured skew model unchanged in `scripts/stamp_skew.py` |
| C-33 | 2026-08-24 | cfd | **F4 SWBLI θ=20° SIGFPE Step 0 / Step 1 clamp-discrimination experiment** — both steps `COMPLETE` and `SIGFPE-ABSENT` and graded today: Step 0 `BASELINE-RECOVERED` (S0a 8.9832 % at `t*` inside the registered [6, 24] %; S0b 23.0842 % at the window end) / `THRESHOLD-ARTIFACT` / `INDETERMINATE`, Step 1 `DEFICIT-NOT-IMPLICATED` (S1a ratio 1.0000 against the 0.60 threshold), C2 confirmed the two logs differ so `LEVER-INERT` did not fire. Prereg §9.1 row 2 eliminates the **inlet-face** variant of the dissipation deficit as mechanism #7. Prereg frozen `0bbac521` (v1.0), amended pre-compute to v1.1; **the blob that RAN is `1673f24f4b7c6eaebde6d506af8203d6626ac17e`** at `7cdb26f4`, re-verified today by hashing the first 1388 lines of the v1.2 file at HEAD; post-compute ADDENDUM 2 (§14, the event-1 reading of a twice-included `boundE.H`) at `d457f612`; grading bodies at `4bf8138d`; results record at `5b5f5183`. Four solver-free runs are counted here: 2 graded steps + 2 control-C0 twins | **9.316 core-min** solver (2 × 4.658; §10.2 arithmetic, §10.3 table rounds to 9.3), **RUN CAP 12 core-min** (6 per step), **BUILD ALLOWANCE 3 core-min** stated separately, **TOTAL ENVELOPE ≤ 15 core-min** = **$0.0128 derived**. `cost_basis` exactly as registered at §10.3: **rate record-quoted, artifact-missing** — the 279.93 wall-s / 6.51e-05 physical-s basis comes from the parent record's prose and the log behind it is gone from disk (§2.1); **price reported-by-owner, not measured**, $0.0513/core-h, because the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` §5) | **11.2018 core-min measured** = 672.11 wall-s, every row np=1 so core-min = wall-min: Step 0 **295.55 s = 4.9258** and Step 1 **295.81 s = 4.9302** (final `ExecutionTime` of each step's own committed log), C0 twin published **40.44 s = 0.6740** and C0 twin diag **40.31 s = 0.6718** (final `ExecutionTime` of each `log.c0`, both committed gzipped in `5b5f5183`) = **$0.00958 derived**. **`wmake` build time is ABSENT, not approximated** (append rule 2, prereg §10.4 rule 2): no build timing was captured by the launch wrapper and none exists anywhere on this box — the run tree, both `*_src/` trees, `LAUNCH.txt`, `RC.txt`, `POSITIVE_CONTROL.txt` and `C0_RESULT.txt` were all searched — so the §10.3 "≈2 core-min for builds + C0" estimate and the 3 core-min BUILD ALLOWANCE **cannot be closed out and are recorded as NOT CLOSED** | **10.7550 core-min** = gross − waste. **No row is a stall**: the longest single run is 295.81 wall-s, an order of magnitude below the 3600-s rule of `COMPUTE_BUDGET_CHARTER.md` §2, so the stall cleaning removes nothing and the only difference between gross and cleaned is the separately-named waste below | **1.058×** on the solver line (9.8560 measured / 9.316 registered), per step 1.0575× and 1.0584×; **0.821× of the 12 core-min RUN CAP**; **0.717×** whole-item cleaned / the ≤15 core-min envelope (10.7550 / 15). **No overrun and no run was stopped** — each step ran under `timeout 360` and finished at ~296 wall-s, so the wrapper never fired and §7.2's `TRUNCATED-AT-CAP` clause was never reached | **Misprediction against the basis, in the adverse direction but small; contention unattributable; waste named separately and not netted off either column.** **(a) Misprediction:** the observed per-physical-time rate is **4.547e+06** wall-s per physical-s (Step 0: 295.55 / 6.5e-05) and **4.551e+06** (Step 1) against the registered **4.30e+06** basis — the basis **under-predicted by 5.7–5.8 %**. From a basis that is `record-quoted, artifact-missing` that is a good outcome and is reported as one. **(b) Contention:** both steps launched at load **7.87 / 7.65 / 6.35** on 16 cores, 15 GB available, with three `buoyantBoussinesq` solvers live (heat-transfer, untouched) — recorded in both `LAUNCH.txt` files. §10.3 records the basis's own load as **NOT RECORDED**, so **the attribution cannot be made and is not made**: the 5.8 % overshoot is *consistent with* a busy box and that is all the evidence supports. **Both ends of the comparison are now on the record**, which is exactly what §10.3 predicted this row's calibration value would be — the same defect the `DPW8_V2 L4` row measured. **(c) WASTE NAMED: 0.4468 core-min = 26.81 wall-s = $0.00038 derived.** The two control-C0 twins each ran to **299** `Time =` blocks where control C0 compares only the first **200**; measured as each twin's final `ExecutionTime` minus its `ExecutionTime` at block 200 — published `40.44 − 26.99 = 13.45 s`, diag `40.31 − 26.95 = 13.36 s`. Those 99 extra blocks per twin produced no measurement, so they are waste under charter §6 and are counted, not absorbed. **Neither step was terminated by the phenomenon under study** (both `SIGFPE-ABSENT`), so §10.4's "a crash is the measurement, not waste" clause does not apply here. **(d) Two relayed figures NOT carried, and why.** The cfd supervisor relayed waste `0.40` core-min (twins "~12 s overlong" plus a PID-capture bug) and an envelope of `12.00` gross / `11.59` cleaned. **Neither total is reproducible from disk**: the measured twin overrun is 0.4468, and measured gross is 11.2018, the ~0.80 difference being consistent with the two absent `wmake` builds. **This row carries only the measured figures**; no PID-capture cost is carried because no artifact on this box records one. **Calibration lesson, and it is about instrumentation rather than arithmetic: a launch wrapper that times the solver but not the build makes its own BUILD ALLOWANCE permanently un-closeable — the allowance was registered, the compute was spent, and the ledger can never say whether it fitted.** Second lesson, cheap: a positive control that needs the first N blocks should be stopped at N, not at whatever the operator's kill loop reaches — 99 surplus blocks per twin is 100 % of this item's entire waste | results record `verification/campaign/F4_SIGFPE_STEP01_RESULTS.md` §1 (commit `5b5f5183`); reader output `verification/runs/F4_runs/swbli_cylflare/GRADING_OUTPUT.txt`; measured logs, all committed gzipped: `.../step0_instrumented/log.rhoCentralFoamBoundedDiag.gz`, `.../step1_inletupwind/log.rhoCentralFoamInletUpwindDiag.gz`, `.../c0_twin_published/log.c0.gz`, `.../c0_twin_diag/log.c0.gz`; launch state `.../step0_instrumented/LAUNCH.txt` and `.../step1_inletupwind/LAUNCH.txt`; registration `verification/campaign/F4_SIGFPE_STEP01_PREREGISTRATION.md` §10 and §10.4, frozen `0bbac521`, blob that ran `1673f24f` at `7cdb26f4`, ADDENDUM 2 at `d457f612` |
| C-34 | 2026-08-24 | heat-transfer | E4a2 (fanPressure BC verification, successor rung; the ONE re-registered item was the iterative-convergence gate — C1 sustained floor 1e-8 over 3 intervals / C2 not-growing / C3 half-run Q stationarity 1e-7 — everything else carried over from E4a and asserted deep-equal. Five cases built, launched serially at `nice 15`, completed 5/5 rc=0, strict completion rule 5/5, graded — **rung verdict PASS**: all eight registered rows I1/I2/P1/R1/G1/G2/N1/D1 PASS, controls Z1 and X1 held, comparator exit 0) | **665 core-s = 11.083 core-min = 0.18472 core-h = $0.0095 derived**, registered at `E4a2_PREREGISTRATION.md` §5, frozen `cd1f46e1` (10x stop threshold 1.8472 core-h; per-case stop thresholds F_c 390 s, F_m 690 s, F_f 1 350 s, S_f 1 350 s, N_f 1 340 s) | **≈ 497 core-s = 8.283 core-min = 0.13806 core-h = $0.00708 derived**. Directly measured: the five solver walls from `E4a2_runs/STATUS.<case>` `wall=` (38 + 70 + 135 + 134 + 114 = **491 core-s**), with `ExecutionTime` from the five `log.solve` files summing to **489.06 s**. Serial, nProcs 1, so core-s equals wall s. **Two lines are UPPER BOUNDS, not measurements, and are labelled as such**: meshing is not separately resolvable — wrapper start-to-end from each `<case>/LAUNCH_LOCK/launch.log` equals the solver wall to the second on all five cases, so blockMesh + checkMesh + the `0.orig`->`0` copy cost **under 1 s per case, bounded at ≤ 5 core-s** against a registered 3; and the comparator (**≤ 4 core-s**, registered 120) and `mark_done_e4a2.py` (**≤ 2 core-s**) are bounded from each log's UTC filename stamp to that log's mtime, the processes not having been separately timed. The registered "extra checkpoint I/O" line of 30 core-s is inside the solver walls, not additive | **= gross.** Longest wall on the box is 135 s, more than a decade below the 3 600-s stall rule, so the stall rule matches nothing and there is nothing to clean out | **0.747x** gross = cleaned / predicted; **0.0747x** of the 10x stop threshold. **No overrun; no run stopped, relaunched or discarded.** Closest per-case approach to a stop threshold: F_m at 10.1 % | **The solver line is not a miss — it is C-21's calibration lesson paying off, and that is this row's content.** §5 registered its basis as **5.50e-7 s per cell-iteration**, measured on E4a's own F_f completion (44.56 s / (4 050 x 20 000)), explicitly REJECTING E4a's borrowed `buoyantBoussinesqSimpleFoam` basis of 7.5e-6 that over-predicted by 13.6x (C-21, L-271). E4a2's F_f measures **5.5399e-7 s/cell-iteration** — the basis reproduced to **0.7 %** at 3x the iterations — and the solver subtotal lands at **0.959x** (491 actual vs 512 registered) against E4a's 0.075x. **DATED ADDENDUM 2026-08-24, and it is the calibration content of this row: L-271 is CONFIRMED, not merely re-cited.** L-271 ruled that a per-cell-iteration basis is not portable ACROSS solver classes; E4a2 is its positive control, and the finding is now two-sided — **a basis measured on the solver that will actually run IS portable WITHIN its own solver class, reproducing to 0.7 % across a 3x change in run length.** That is why this row exists as a PASS-side calibration entry rather than a miss, and no new lesson is raised for it. Per-case ratios 0.974 / 1.014 / 1.000 / 0.993 / 0.851. **MISPREDICTION, one material line: the comparator, over-predicted by >= 30x** (120 core-s registered, <= 4 measured) — §5 costed it as 5 x 30 checkpoints x ~24 700 parsed floats with no measured parsing rate; the reader is far faster than that guess, and this is the successor's calibration item. **N_f at 0.851x is misprediction in the conservative direction by construction**: §5 registered the LARGER of two estimates per case (basis 133.7 vs 3x E4a's measured wall 129 -> 134), disclosed rather than averaged, and N_f's per-cell-iteration cost fell from E4a's 5.362e-7 at 20 000 iterations to **4.6654e-7** at 60 000 as fixed per-run overhead amortised over 3x the iterations (same effect on F_m 6.603e-7 -> 6.4370e-7 and F_c 7.925e-7 -> 7.9125e-7; slightly inverted on F_f, +0.7 %). **Contention is not the explanation and could only push the other way**: three peer `buoyantBoussinesqSimpleFoam` T1b L4 solvers (pids 442445, 450274, 488219) ran at `nice 0` and 99.9 % CPU each throughout, load1 **3.90** on the 16-core box, with this lane at `nice 15` on one core and ~12 cores idle — contention can only inflate an actual, so it cannot produce a sub-1 ratio, and the per-case ratios of 0.851-1.014 show its effect here was negligible; figures are stated gross and are NOT netted off for it. **WASTE: none** — no run stopped, relaunched or discarded; no case tree rebuilt; the builder, the marker and the comparator each ran exactly once | `docs/campaigns/T-family/E4a2_RESULTS.md` §6 (this table) and §1 (verdict), committed at `5a197a41`; prereg frozen at `cd1f46e1`; per-case markers `verification/runs/T-family/E4a2_runs/STATUS.*` (5 files, all rc=0) and `DONE.*` (5 files); logs `verification/runs/T-family/E4a2_runs/log.mark_done.20260824T173456Z.txt` and `.../log.analyse_e4a2.20260824T173504Z.txt`; manifest `.../BUILD_MANIFEST.txt`; lesson `L-281` |
| C-35 | 2026-08-24 | verification | **Cross-team gate audit PASS 10** — dafoam's A3 rung-3 patched-IDWarp np=4 **ATTEMPT 2**, graded at `8871acf3` as `GATE FAIL` (adjoint, inherited) on IDENTITY CONFIRMED 11/11. Audit verdict **SOUND WITH DISCLOSED DEVIATIONS (CANDIDATE)** with **one DEFECT named** (five `patched.log` line citations off by one in `RESULTS.md` §2.1). Read-only toward dafoam; **zero solver compute**; nothing moves a dafoam verdict | **TWO COMPONENTS, both registered before the pass ran.** (a) **executed compute: point 0.30 core-min, ceiling 1.50**; (b) **lane wall: point 12.0 core-min, ceiling 20.0** (1 lane × wall min). Registered in the dispatching brief; no solver, no container, no GPU | **(a) executed 0.193 core-min MEASURED + ≤ 0.134 BOUNDED NOT MEASURED = ≤ 0.327 core-min gross** = **$0.00028 derived**. Measured half: five heavy operations timed with `/usr/bin/time`, each executed twice (once in the audit, once to time it) — stamp check on the two dafoam records 2.66 s, stamp check on this ledger 2.25 s, `git log --follow` on `PREREGISTRATION.md` 0.87 s, this lane's own identity comparator 0.02 s, `find` over the run root 0.00 s ⇒ **11.60 core-s at 1 rank = 0.193 core-min**. Bounded half: ~40 further shell invocations (`git show`, `git rev-parse`, `grep`, `awk`, `sed`) each **timed at 0.00 s at 10 ms resolution** and therefore bounded ≤ 0.1 s ⇒ ≤ 0.067 core-min, plus two private-index commit invocations ≤ 0.067 core-min. **(b) lane wall 16.32 core-min** = **$0.01395 derived**, from `git log -1 --format=%cI ac19e210` = **2026-08-24T17:37:30Z** (the HEAD this lane read at its first action, taken as the start because it is the earlier and therefore conservative bound) to `date -u` at this commit | **= gross on both components.** `COMPUTE_BUDGET_CHARTER.md` §2's 3600-s stall rule matches **no row**: the longest single operation is 2.66 s and the longest component is the lane wall itself, which is agent latency and not a solver stall | **(a) 0.64× on the measured floor (0.193 / 0.30), ≤ 1.09× on the conservative bound (0.327 / 0.30); ≤ 0.22 against the 1.50 ceiling.** **(b) 1.360× (16.32 / 12.0); 0.816 against the 20.0 ceiling.** **Both ceilings held; neither was approached** | **(a) MISPREDICTION, small and in the *under* direction on the measured floor, and the point is inside the bound.** The 0.30 estimate was built for an audit expected to run one comparator and one instrument sweep; this pass ran the stamp check **twice** (once on the dafoam records, once on the ledger) at 2.25–2.66 s each, and those two calls alone are **4.91 of the 11.60 measured core-s — 42 % of the executed compute for what is a report-only hygiene sweep**. **Calibration finding worth carrying: `check_stamp_vs_commit.py` is by two orders of magnitude the most expensive thing an audit lane runs**, because it walks `git blame` per token; every other operation in this pass rounded to 0.00 s. **An audit brief that names N stamp-sweep targets should register ~2.5 core-s per target, not a flat allowance.** **(b) **MISPREDICTION OF SCOPE, small, and the box was NOT a factor.** The 12.0 point was set for an audit of the usual six-to-seven sections; this pass landed **nine**, because the target carried two frozen commits rather than one (a freeze plus a before-compute amendment, each needing its own byte-prefix proof), a run root whose six `.gs_*` markers each had to be tested individually against their `.patched` counterparts, and a defect whose blast radius had to be bounded row by row before it could be named. **The lane also wrote and ran its own comparator with two plants rather than re-reading the item's**, which is what rule 3 costs when the finding under audit is a zero. Not contention: every timed operation on this box ran at or below 2.66 s and no operation waited. **One further wall cost, named because it is a peer-concurrency fact and not a misestimate:** the first attempt at this very row aborted on its own id assert when a peer landed `C-34` mid-flight, and the row was re-derived and rebuilt — rule 11 working as designed, at the price of one extra invocation.** **WASTE: 0.097 core-min, NAMED SEPARATELY and never folded into either column or into the ratio's explanation** (charter §6) — that is the 5.80 core-s of **re-timing overhead**, five operations re-executed for no purpose but to convert an estimate into a measurement so this row could say MEASURED rather than ESTIMATED. It bought the calibration duty of rule 12 and is disclosed as its price rather than absorbed; on the measured floor it is **exactly half** the executed compute, which is what auditing something this cheap costs | `docs/CROSS_TEAM_GATE_AUDIT.md` **§73–§81** (pass 10, committed `d74a36c2`, sections re-derived as max+1 from the HEAD blob in the commit invocation); audited records `cases/dafoam/ladder-a/A3/rung3_patched_idwarp_np4_attempt2/{PREREGISTRATION.md,RESULTS.md}` at `606930b4`/`8a0b440d`/`8871acf3`; run root `/home/ubuntu/certonomous-runs/P5-a3-rung3-patched-attempt2/`. Rate $0.0513/core-h, c7a.4xlarge, **reported-by-owner**; **every dollar figure derived, not measured** (`COMPUTE_BUDGET_CHARTER.md` §5). **DISCLOSED (D486 class):** the worktree copy of this ledger carried **14 of the 33 committed `C-` ids** at this commit — `scripts/check_record_reconciliation.py` named 19 ids present in HEAD and absent from the worktree — so this row's id was re-derived as max+1 **over the HEAD blob** and merged onto it by `scripts/append_record.py`, never over the stale worktree tail. That check's own planted controls returned **BROKEN** for this file's id form (its plants are `N-*` series, not `C-*`), so its *zero* half is unsupported by rule 3; its *non-zero* half — the 19 divergent ids — was seen and is what this disclosure rests on |
| C-36 | 2026-08-24 | verification | **External-referent audit screen RECOVERED, controlled and committed — this team's own recorded defect closed.** `docs/EXTERNAL_REFERENT_AUDIT.md` §11 was published by `e3f3b521` (one file changed, 186 insertions, no producer) quoting five population figures whose instrument was outside git — the same defect class §11.1 had just recorded against §3, in the same document. **PATH A, RECOVERY not rebuild:** the 2026-08-23 producer survived on this box and was recovered, corroborated twice — by its own surviving per-file output (892 verification sections across 151 documents = §11.2's published 892) and by an **exact** re-derivation of §11.1's four `df4d4cbe` figures (368 tracked md / 4,844 sections / 1,008 case-sensitive / 1,567 case-insensitive, four for four). Committed as `scripts/referent_population_screen.py`, framed on a named revision's blobs rather than on `git ls-files`, with a **17-check planted control (rule 3) shown able to refuse: 7 mutants, all exit 2**. §12 appended, 166 insertions, 0 deletions, prefix asserted byte-identical to the HEAD blob. §11.2's five figures **not struck** — superseded by a revision-framed row and explained by a live index-vs-tree measurement (701 listed vs 736 in tree, all 736 on disk, 35 unlisted). §3's four bucket figures stay unreproducible and were **deliberately not rebuilt** (§11.5 item 2). Commit `bb082627`. **Zero solver compute** | **Two components, both registered at dispatch, point AND ceiling for each. EXECUTED COMPUTE: 0.5 core-min point / 2.0 core-min CEILING. LANE WALL: 10 core-min point / 20 core-min CEILING.** np=1 throughout; no solver, no GPU, so core-min = wall-min on every row | **EXECUTED ≈ 1.17 core-min as a BOUND, of which 0.617 core-min = 37.0 core-s is SHAPE-MEASURED** at np=1: four 3-revision screen runs at 4.95 s, seven 1-revision runs at 2.22 s, ~19 refusing-mutant runs at 0.03 s, three `check_filing.py` runs at 0.06 s — **each shape timed with `/usr/bin/time` and multiplied by a counted invocation census, not read from a per-run log**, and stated as such. **UNTIMED REMAINDER, NAMED AND NOT ABSORBED: ≈ 0.55 core-min** — two disk-wide `grep` sweeps under `timeout 120`, one `find`, one `git log --all`, ~20 `git show` / `ls-tree` plumbing calls; same rank, same order, **claimed as a BOUND, never as a measurement**. = **$0.0010 DERIVED** at $0.0513/core-h, c7a.4xlarge, reported-by-owner — **not measured**, the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` §5). **LANE WALL 20.4 core-min MEASURED from the filesystem**: lane scratch directory created 2026-08-24T17:35:52Z, this row landed 2026-08-24T17:56:14Z | **= gross on both components.** The longest single invocation is 4.95 s wall, three orders below the 3600-s stall rule of `COMPUTE_BUDGET_CHARTER.md` §2, so the stall cleaning removes nothing and gross = cleaned. **Waste is named separately in the next cell and is netted off neither column nor the ratio** | **EXECUTED: 1.23× shape-measured / registered point (0.617 / 0.5); 2.34× bound / point; 0.31× and 0.59× of the 2.0 core-min ceiling — inside the ceiling on either reading, so no overrun and nothing was stopped on this component. LANE WALL: 2.04× / registered point (20.4 / 10); 1.02× of the 20 core-min ceiling — **OVERRUN of the 20 core-min lane-wall ceiling, disclosed and not absorbed.** Rule 12 stops an overrun rather than re-budgeting it: the item was stopped at this row, which is its completion record, and no further work was bought** | **Misprediction on both components, one mechanism, and it is about how an instrument-recovery item should be priced.** **(a) The brief priced ONE screen; the defect was TWO instruments.** `EXTERNAL_REFERENT_AUDIT.md` carries an 08-11 bucket screen (§3) and an 08-23 population screen (§11), with different vocabularies and different fates — one unrecoverable, one recovered — and establishing which was which cost a history sweep (`git log --all --diff-filter=A`), a disk sweep and a full reading of §11 before a line of the repair could be written. That is the whole of the executed-compute overshoot and about half the wall overshoot. **(b) The planted control needed three rounds, and the first mutation battery was itself invalid.** Round 1's mutants all exited 128, not 2: the script derived its repository root from `__file__`, so mutants copied to a temp directory resolved a root that is not a git repository and never reached the classifier under test. **WASTE, NAMED AND COUNTED, NOT ABSORBED: ≈ 2.0 core-min of lane wall and ≈ 0.3 core-s of executed compute** — three mutant runs that tested nothing. It is named as waste even though it paid for itself: it is what exposed the `__file__` fragility, repaired in the committed script by asking `git rev-parse --show-toplevel`. Round 2 then caught 2 of 4 mutants and **let 2 through** — a reference-column check that compared counts rather than the matched row, and a plant whose last table row was the file's last line, where an off-by-one can never be reached; both were repaired before commit, and the strengthened control catches all 7. **(c) HEAD moved four times during the lane** (`a80d5f36` → `5a197a41` → `45995a5e` → `edee5088`), and the screen's own figures moved with it, which is why every figure in §12 is pinned to a named revision rather than to HEAD. **TRANSFERABLE RULE, and it is the reason this row is worth its space: price an instrument-recovery item by the number of INSTRUMENTS named in the defect, not by the number of documents — and budget one full mutation round for the mutation HARNESS itself, because a mutant copied outside the tree under test does not test it.** **CONTENTION: not measured and not claimed** — no solver of this team was live, np=1 throughout, and no load reading was taken, so no attribution is made | `docs/EXTERNAL_REFERENT_AUDIT.md` §12 and `scripts/referent_population_screen.py`, both at commit `bb082627`; the screen re-runs from any revision, so every figure in §12 is recomputable by `scripts/referent_population_screen.py df4d4cbe 890bfa7f <rev>`. Rate $0.0513/core-h, c7a.4xlarge, reported-by-owner; **every dollar figure derived, not measured**. **INSTRUMENT DEFECT FOUND WHILE FILING THIS ROW, recorded for the next lane and NOT repaired here:** `scripts/check_record_reconciliation.py --path docs/COST_CALIBRATION.md` was run before this row as required and **self-reports `CONTROL KIND: BROKEN` / `ZERO VERDICT: ZERO_IS_UNSUPPORTED`** on this path — its planted forms are NUMERICS-shaped (`## N-X9002. an h2 entry`) and cannot match a `C-\d+` table row, so its "no worktree-only ids" side is an unsupported zero for this file and must not be read as a clean bill. Its HEAD-side divergence report (C-15…C-35 in HEAD, absent from the worktree) is the divergence-by-design this ledger's own header describes. Reliance was placed instead on `scripts/append_record.py`'s merge form and its `--expect-first-id` max+1 assertion, re-derived from the HEAD blob in the commit's own invocation |
| C-37 | 2026-08-24 | ansys-verification | VMFL001 run 1 (Ansys VM2026R1 p. 15-16, flow between rotating and stationary concentric cylinders; three-level Roache triple 16x64 / 32x128 / 64x256, serial `simpleFoam` laminar, 3000 SIMPLE iterations each). **Rung verdict `NOT A RESULT`** on two independent grounds: the frozen comparator `grade_vmfl001.py` (`8cb29610`) REFUSED exit 2 because OpenFOAM v2606's `sets` writer emits `gateAxis_p_U.xy` (set name first, fields alphabetical, NO `#` header) where the comparator was frozen to expect `U_gateAxis.*` with a header; and, independently of any parser, L3 fails the registered iterative-convergence clause of prereg section 7 (final-iteration initial residuals Ux/Uy 1.19876e-06, p 2.89185e-06 vs the frozen < 1e-6; plateau peak-to-peak on the r = 35 mm probe over the last 600 of 3000 iterations 2.77178e-05 m/s vs the frozen < 1e-6 m/s), while L1 and L2 pass both clauses (4.69e-14 / 1.49e-12; ptp 1.00e-14 / 1.27e-11). **No lab value exists** — no comparator number, no planted-zero firing, no triple, no GCI | **3.0 core-min = 0.05 core-h = $0.00257 derived**, registered at `cases/ansys_verification/VMFL001/PREREGISTRATION.md` section 8, frozen `e0afc259` (original) / `d6ea5de9` (as amended and as it ran). **CAP 10 core-min = $0.00855 derived**, enforced by `timeout` in the run script; an overrun stops the run and gets no new budget. Basis of the estimate, stated in the prereg as an estimate and not a measurement: an order-of-magnitude cell-iteration count, 21,504 cells x 3000 iterations = 6.5e7 cell-iterations at ~1 us each = ~65 s = ~1.1 core-min, rounded UP to 3.0 for safety | **119 wall s = 1.9833 core-min = 0.033055 core-h = $0.001696 derived.** Directly measured, no bounded or estimated component: the three per-level `wall_s` in `verification/runs/ansys_verification/VMFL001/<level>/RUN_RC.txt` (2 + 13 + 104 = 119 s), totalled independently in `.../VMFL001/COST.txt` (`total_wall_s=119`, `total_core_min=1.9833`). Serial, ranks = 1, so core-s == wall s. Dollars DERIVED at the owner-stated $0.0513/core-h (c7a.4xlarge, Sanaa 2026-08-21/22), never measured -- the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` section 5) | **= gross, 1.9833 core-min.** Gross and cleaned are identical here and the record says why: no run was stopped, relaunched or discarded; no level was rebuilt; `run_vmfl001.sh` and `grade_vmfl001.py` each executed exactly once; the longest single row is 104 wall s, two orders of magnitude below the 3600 wall-s stall rule. The comparator's own execution is milliseconds of Python and is not separately instrumented -- it is inside the 119 s only insofar as it is not, and the figure is stated as the three solver rows, which is what the artifacts measure | **0.661x cleaned/predicted** (1.9833 / 3.0), and **0.198x against the 10 core-min cap** -- the cap was never approached and no `CAP_EXCEEDED.txt` exists | **Misprediction, in the CONSERVATIVE direction: the estimate was ~1.5x too pessimistic.** The prereg's own cell-iteration arithmetic gave ~1.1 core-min and was rounded up to 3.0; the box did the work in 1.9833, i.e. the raw arithmetic under-predicted by 1.8x and the safety rounding over-corrected. Actionable for this suite: for serial laminar `simpleFoam` on O(10^4) cells, ~1.8 us per cell-iteration is the calibrated figure from this row, not 1 us. **WASTE: zero** -- nothing was discarded, relaunched or rebuilt; the failed grading attempt cost milliseconds of Python and produced the `REFUSE` line that is itself the evidence for the verdict. **CONTENTION: none credited** -- the levels ran serially on one core of a loaded 16-core box, and contention can only inflate an actual, so it cannot produce a sub-1 ratio; figures are stated gross and are NOT netted for it. **A separate honest note, since this is a calibration ledger and not a scoreboard: the 1.9833 core-min bought NO lab value.** It is not waste (the runs completed correctly and their artifacts are the evidence for the verdict, two lessons and two numerics facts), but the rung must be re-run under a NEW pre-registration, and that re-run's cost is not yet predicted | `cases/ansys_verification/VMFL001/RESULTS.md` section 6 (this comparison) and section 1 (verdict); artifacts committed at `ae30f914` under `verification/runs/ansys_verification/VMFL001/` (23 files: `COST.txt`, `GRADING_VMFL001.stdout.txt`, and per level `RUN_RC.txt`, `log.blockMesh`, `log.checkMesh`, `log.simpleFoam`, `postProcessing/`); prereg frozen `d6ea5de9` (original `e0afc259`, commit `ffeed580`; Amendment 1 pre-compute, commit `a9e73c5d`); comparator `8cb29610`; run script `f2b09aa2`; HEAD at the run `cffd90e7`; register row #1 in `verification/credentials/ansys/ANSYS_VALIDATION_REGISTER.md` |
| C-38 | 2026-08-24 | dafoam | curriculum D3 (A4 Ahmed body, constrained drag minimisation) — **`BLOCKED` at Stage G; the item is NOT completed and three of its four stages never launched** | **29.8 core-min** registered (registered 100 % contingency 59.6; **HARD ceiling 70.0**) = **$0.0255 derived** — `cases/dafoam/ladder-a/A4/curriculum_D3/PREREGISTRATION.md` §8, frozen at `0cbf463c` | **0.300 core-min MEASURED** — 18 wall s × 1 rank ÷ 60, read from the run-root `ledger.txt` (Stage G, `-task geom_probe`, patched image, no flow solve; rc=1, peak RSS 0.607 GiB against a 6 GiB cap, 180 s timeout did not fire, exit 1 not 137 so no OOM) = **$0.000257 derived**. **Plus one BOUNDED row, disclosed and not promoted to a measurement: ≤ 0.300 core-min** for an unregistered read-only triage container (no volume mount, no solver, pyGeo signatures read so the crash mechanism was established rather than asserted; `RESULTS.md` §7.2). **Total ≤ 0.600 core-min ≤ $0.000514 derived** | **= gross.** The longest wall on this item is **18 s**; the 3600-s stall rule (`COMPUTE_BUDGET_CHARTER.md` §2) matches nothing, so gross and cleaned coincide. Waste is named separately below and is **not** netted off either column | **0.010× on the total — AND THIS IS NOT A CALIBRATION.** Three of four stages never launched, so **the estimate was never tested**; this is the C-10 situation in that row's own words (*"NOT a misprediction — the prediction was never tested"*), and the total is carried only so the spend is on the record. **The one row that IS testable is Stage G: 0.300 actual against 1.0 predicted = 0.30×** — and even that is **soft**, because the stage **exited on a defect at 18 s** instead of completing, so a completing Stage G would cost more than 0.300 and 0.30× understates it | **Misprediction only, conservative direction; ZERO contention; waste named separately.** (a) **No contention penalty applies and none is claimed**: the G6 launch gate read `load1 3.17` on 16 cores with `MemAvailable 27.48 GiB` at launch (`preflight_history.txt`, one stamped row). (b) **WASTE NAMED: 0.300 core-min** — the Stage G container returned **no registered measurement** (P1 **MISS**; P2 and P3 **NOT TESTED**, the FFD-Jacobian and symmetry probes sit after the `prob.setup()` that raised). It returned a **defect**, which is a finding of real value, but per `COMPUTE_BUDGET_CHARTER.md` §6 that value is **not netted against the waste** and the spend is reported gross as an unrecovered cost. (c) **The calibration content of this row, which does not depend on the unrun stages:** a **zero-flow API-exercise stage cost ≈ 1 % of the item's 29.8 core-min budget** (and ≈ 2.6 % of a single ≈11.6 core-min A4 optimisation arm) and caught a fatal `configure()` defect that would otherwise have surfaced **after a paid cold primal in Stage O** — the D1-C′/L-273 failure, not repeating. **Lesson for future estimates: price a Stage-G-style no-flow stage into every constrained-optimisation registration, and split its own estimate into `completes` and `exits-on-defect`, because the two cost very differently.** (d) **Disclosed procedural departure:** the item's frozen prereg §8.2 (adopting D1 Amendment A1.1) reserves the landing of this row to the supervisor and says *"No lane writes that file"*; this row was landed by the dafoam `lab-lane` **on the dafoam-supervisor's written direction**, given after the supervisor personally read `geom.log`, and under the four ledger asserts that direction specified (id re-derived as max+1 over the committed blob **and** the preserved worktree tail; HEAD blob's last byte verified `\n`; prefix identical; previous and new id each on its own line) — merged onto the HEAD blob via `scripts/append_record.py --rev <the same rev passed to commit-tree -p>`, **never onto the worktree copy, which at landing time lagged HEAD by 23 rows (max `C-14` vs `C-37`)** | `cases/dafoam/ladder-a/A4/curriculum_D3/RESULTS.md` §6 (§6.1 measured/bounded split, §6.2 estimate-vs-actual, §6.3 the departure) and §7.2; pre-registration frozen at `0cbf463c`; supervisor launch authorisation addendum at `8bd8d9d7`; results record at `3e4c7d81`. **Measured artifacts, outside git:** `/home/ubuntu/certonomous-runs/D3-a4-constrained/ledger.txt`, `preflight_history.txt`, `geom.log` (502 lines; traceback at `:472-491`), `grade_cost.txt`, `grade_geom.txt`, `grade_selftest.txt`. All re-read on disk for this row |
| C-39 | 2026-08-24 | verification | **Launch-gate memory-limb clause DRAFTED for Sanaa's desk** — a `VERIFICATION_CHARTER.md` §2e-sibling clause (§2f if ratified), **NOT IN FORCE, no charter edited**, evaluating dafoam's L-262 / C-10 rule with three refinements, a §2c-form boundary, an instrument spec on D473's precedent and two worked instances. Docket **D507**. **Zero solver, zero container, zero GPU** | **TWO COMPONENTS, both registered in the dispatching brief before the work.** (a) **lane wall: point 8.0 core-min, ceiling 15.0**; (b) **executed compute: registered as `< 0.2 core-min` — a CEILING WITH NO SEPARATE POINT**, and it is recorded in the form the brief gave it rather than back-filled with a point that was never registered. np = 1 throughout, so core-min = wall-min on every row | **(a) lane wall 10.72 core-min**, measured between two timestamps **neither of which this lane wrote by hand**: the mtime of the earliest artifact it created, `2026-08-24T17:56:54.228Z`, and this row's commit at `2026-08-24T18:07:37Z`. **It is a LOWER BOUND and is labelled one**: the three tool calls that read `CLAUDE.md`, `FILING_CHARTER.md` and the charter section maps precede that mtime and **are covered by no timestamp**; that prefix is **NOT MEASURED and is not silently folded in**. **(b) executed compute ≤ 0.084 core-min gross, of which 0.009 core-min is MEASURED and the remainder is a BOUND, never a measurement.** Measured half, `/usr/bin/time` shapes × a counted invocation census: `check_filing.py` **0.06 s** × 5 invocations (tree, three grep passes, `--selftest`) = 0.30 core-s; `check_record_reconciliation.py` **0.08 s** × 3 invocations = 0.24 core-s ⇒ **0.54 core-s = 0.009 core-min**. Bounded half, **named and not absorbed**: ~35 `git show` / `git log` / `grep` / `sed` / `cat` invocations at ≤ 0.1 s each, 3 `append_record.py --dry-run` runs and 2 private-index commit invocations at ≤ 0.2 s each ⇒ **≤ 4.5 core-s**. Dollars **derived, not measured**, at $0.0513/core-h, c7a.4xlarge, **reported-by-owner** — the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` §5): **(a) $0.00917, (b) ≤ $0.00007** | **= gross on both components.** The longest single invocation measured on this item is **0.08 s**, four orders below the 3600-s stall rule of `COMPUTE_BUDGET_CHARTER.md` §2, so the stall cleaning removes nothing and **gross = cleaned**. Waste is named separately in the attribution cell and is netted off neither column nor the ratio | **(a) 1.340× cleaned/predicted (10.72 / 8.0); 0.715 of the 15.0 core-min ceiling.** **(b) ≤ 0.42× of the registered 0.2 core-min ceiling on the bound (0.084 / 0.2), and 0.045× on the measured floor (0.009 / 0.2) — no separate point was registered, so no point ratio is quoted rather than one being invented. Both ceilings held** | **Attribution, and the transferable half is about where a drafting item's sources live.** **(a) The mechanism is history-reading, not contention.** Two of the three source families **are not readable from the worktree at all**: rows C-10 and C-35 exist only in the HEAD blob (the worktree ledger is 22 committed ids behind), and the three refinements exist only inside a `docs/LAB_STATE.md` **diff at a named commit**, not at HEAD. Every such citation costs a `git show <rev>:<path>` plus a location pass inside a diff — **a different and slower operation than opening a file**, and there were sixteen of them. **Transferable rule: price a drafting item by the number of sources that must be reached through git HISTORY, not by the number of documents, and add a history-reading component when a cited ledger row is known to be missing from the worktree.** **CONTENTION: not measured and not claimed** — np = 1 throughout, no solver of this team was live, and no load reading was taken, so no attribution is made to the box. **(b) Deliberate under-instrumentation, and it is a decision this row defends.** C-35 named **0.097 core-min of re-timing waste** on an audit whose whole executed compute measured 0.193 core-min — *"exactly half the executed compute, which is what auditing something this cheap costs."* **This row does not repeat that trade**: five heavy shapes were timed once, in the run that used them, and everything else is stated as a **bound** rather than re-executed to convert an estimate into a measurement. **WASTE ON BOTH COMPONENTS: 0.000 core-min, and the claim is checkable** — the one invocation that could look like waste, the `append_record.py --dry-run` carrying a deliberately wrong id `D999`, **bought rule-3 evidence**: it is the planted control that showed the tool's id assert able to REFUSE, which is what makes the following run's `ID ASSERT ok : D507 == max+1` worth anything. A zero from an assert never shown able to fire is not evidence | Draft `docs/LAUNCH_GATE_MEMORY_LIMB_CLAUSE_DRAFT.md` §9 at commit `5f8f2c1b`; docket **D507**; this row's commit. Sources cited by the draft: `docs/LESSONS.md:9807` (L-262), `docs/COST_CALIBRATION.md` C-10/C-35 **from the HEAD blob**, `docs/LAB_STATE.md` at `f536b114`/`9d1d348a`, `docs/CROSS_TEAM_GATE_AUDIT.md` §73–§81 at `d74a36c2`. **DISCLOSED (D486 class):** `scripts/append_record.py` **REFUSED this path** — *"the worktree disagrees with the committed blob inside the committed blob's own bytes, first at byte 2338 of 108993; the worktree is SHORTER than the blob, which is a truncation, not an append"* — so this row was appended **to the HEAD blob by hash-object**, its id re-derived as **max+1 over that blob (38 ⇒ C-39) in the commit invocation**, insertions-only asserted, and **the worktree copy was left untouched and never reverted** (`ESCALATION_CHARTER.md` §3). The reconciliation check's own plants are `N-*` forms and returned **BROKEN** for the `C-` id pattern, so **its zero half is unsupported under rule 3**; what this rests on is its **non-zero** half, the 22 divergent ids it did see |
| C-40 | 2026-08-24 | verification | **Six lessons dictated and appended to `docs/LESSONS.md` in one commit — `L-290` … `L-295`, landed at `edddc8a7` (247 insertions, 0 deletions, one path).** The blocks: (1) the hash-object append fallback needs a three-line assert plus a trailing-byte check, paid for by `016ac2bf`/`ec35bf9e`; (2) register a point estimate AND a ceiling, and price lane wall separately from executed compute; (3) an id in prose before its own append is a prediction, not an identifier; (4) commit-message files are per-invocation-unique and the first-line guard is mandatory; (5) `git ls-files` reads the index — an instrument frames its corpus on `git ls-tree <rev>`; (6) blame's last-toucher attribution hides a fire on the citing side and manufactures one on the defining side. **Every sha and figure cited in the six blocks was re-verified against disk in this lane before it was written** — the eight commits named all resolve; `^\| C-27 ` over `016ac2bf`'s blob returns **0** and over `ec35bf9e`'s returns **1** (the concatenation and its repair); `L-277`/`D499`/`N-D32` over `86fb1b34`'s tree return **0, 0, 0**. **Zero solver compute; np=1 throughout, so core-min = wall-min on every figure in this row** | **TWO components, and the brief registered them ASYMMETRICALLY — which is this row's own first finding. LANE WALL: point 6 core-min / ceiling 10 core-min, both registered. EXECUTED COMPUTE: registered as `< 0.2 core-min` — a BOUND, with NO point estimate**, which is exactly the ceiling-as-point defect `L-291` (appended in this same item) names in `C-25`, `C-26`, `C-27` and `C-30`. It is recorded against this lane's own brief rather than quietly ratio'd, per that lesson | **EXECUTED COMPUTE ≈ 0.015 core-min (0.9 core-s), SHAPE-MEASURED at np=1** with `/usr/bin/time` against a counted invocation census: 3 × `check_record_reconciliation.py` at 0.05 s, 2 × `append_record.py --dry-run` at 0.08 s, ≈ 60 git plumbing calls (`show` of the 711 KB LESSONS blob, `ls-tree -r`, `ls-files`, `log -1`, `hash-object`, `write-tree`, `diff-tree`, `commit-tree`) each timed at **≤ 0.01 s**. **BOUND ≤ 0.05 core-min** including a handful of untimed `grep`/`comm`/`sort` passes over 737 filenames, same order, claimed as a bound and not a measurement. **LANE WALL 7.0 core-min, MEASURED AS A LOWER BOUND from filesystem timestamps**: earliest scratch write by this lane 2026-08-24T18:01:35Z (`mtime` of the first file it wrote), this row committed 2026-08-24T18:08:36Z | **= gross on both components.** The longest single invocation is 0.08 s wall, four orders below the 3600-s stall rule of `COMPUTE_BUDGET_CHARTER.md` §2, so stall cleaning removes nothing and gross = cleaned. **The UNMEASURED PROLOGUE below is named separately and netted off neither column nor the ratio** | **LANE WALL: 1.17× the registered 6 core-min point; 0.70× the 10 core-min ceiling — INSIDE the ceiling on the measured floor, so no overrun and nothing was stopped. With the unmeasured prologue added the upper bound is ≈ 10.0 core-min, which is at or fractionally over the ceiling, so the ceiling is reported as REACHED-NOT-EXCEEDED-ON-THE-MEASURED-FLOOR and the honest statement is that this item finished at its ceiling, not comfortably inside it. EXECUTED COMPUTE: NO RATIO IS COMPUTABLE** — the brief registered a bound, not a point, so the only true statement is **0.015 against a `< 0.2` bound = inside it by ≈ 13×**, and that is a containment check, not a calibration ratio. `L-291` exists because four prior rows had to say the same thing | **(a) THE UNMEASURED PROLOGUE, NAMED AND NOT ABSORBED: ≈ 2–4 core-min.** Lane wall is measured from the lane's first scratch WRITE; the read-only prologue before it — `CLAUDE.md` in full, `MEMORY_ARCHITECTURE.md` §5, the last five lesson blocks at HEAD to match the block format, and the eight-sha verification sweep — wrote nothing and therefore left no timestamp. Estimated at 2–4 core-min from the observed 9-second spacing between adjacent tool batches; **stated as an estimate, never as a measurement**, and the reason the wall figure above is a FLOOR. **TRANSFERABLE FIX: a dispatched lane should `date -u` into a uniquely-named scratch file as its FIRST action**, which converts this whole cell into a measurement for one plumbing call. **(b) MISPREDICTION ON LANE WALL, and the mechanism is verification depth, not dictation.** The dictation itself matches `L-291`'s own registered dictation-class shape (point 2.5 / ceiling 4.0 lane-min). The overshoot is that six blocks carry **eight distinct commit shas, four file-and-line citations and two population figures**, and this lane re-derived every one against disk rather than relaying the brief's — including two that had MOVED: the brief's `736` `.md` files in tree read **737** at this lane's HEAD (gap 36, not 35), and the brief's `max L = 287` read **289** by the time the append ran, peers having landed two blocks mid-lane. **A dictation item that cites N artifacts is priced as dictation PLUS N verifications, ≈ 0.5 lane-min each.** (c) **HEAD moved at least three times during the lane** (`eb2a534b` at read, `8cb66ff4` at the dry-run, `4507fc66` at the append), which is why the id was re-derived inside the committing invocation and not before it. **CONTENTION: not measured and not claimed** — np=1 throughout, no solver of this team was live, no load reading was taken | `docs/LESSONS.md` `L-290`…`L-295` at commit `edddc8a7`; this row at commit the commit that appends it. Rate $0.0513/core-h, c7a.4xlarge, reported-by-owner — the derived dollar figure for the executed component is **$0.00001, DERIVED not measured**, the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` §5). **DEVIATION DISCLOSED, both commits:** `scripts/append_record.py` was run first and **REFUSED** on `docs/LESSONS.md` — the worktree copy is **shorter than the committed blob, first differing at byte 652390 of 707181, a truncation and not an append** — and `scripts/check_record_reconciliation.py` reports the same file 24 ids short of HEAD (`L-264`…`L-287` landed and never written back), and this ledger 23 ids short (`C-15`…`C-37`). That is D486 decay, not a rogue writer. Both commits therefore used the **disclosed hash-object fallback from the HEAD blob**, each with the four checks `L-290` requires — blob trailing byte is `\n`; new content's prefix byte-identical to the blob by `cmp -n`; each new id at the start of its own line exactly once; previous last id still on its own line — **all printed and read before `write-tree`** (`L-286`), plus an assert that the written tree differs from the parent tree, that the diff touches only the one path, and that deletions are **0** on an append-only record (`L-263`). `append_record.py`'s id assertion was still read and agreed. **The stale worktree copies were INSPECTED, NEVER REVERTED** (rule 10). **`docs/LAB_STATE.md` deliberately NOT edited.** **LIVE INSTANCE OF `L-293` FOUND IN THIS LANE'S OWN SCRATCH, disclosed:** a generic filename this lane wrote, `cc_head.md`, has inode **birth 16:08:48Z and mtime 18:01:35Z** — it already existed, written by a different lane two hours earlier, and this lane's `>` truncated it. Harmless here (written and read back inside one invocation) but it is the L-252/`L-293` mechanism caught in the act, in a shared scratchpad holding **616 files**; the commit-message and private-index files for both commits carried `date -u` stamps and `$$` and the first-line guard, and did not collide |
| C-41 | 2026-08-24 | verification | **Cross-team gate audit pass 11 (CANDIDATE) — the F4 SIGFPE step-0/1 S0a event-selection limb referred by §82 of `docs/CROSS_TEAM_GATE_AUDIT.md`: a sealed blind reading of the frozen pre-registration, the code archaeology of the event selector, and the §2d / rule-3 / rule-4 checks over the graded rung.** Zero solver. The rung's own solver spend belongs to cfd and is costed in `F4_SIGFPE_STEP01_RESULTS.md` §1, not here; for the record this lane re-read it from the logs as 295.55 s + 295.81 s single-rank = **9.86 core-min against a 12.0 core-min registered cap**. | **Two components, both registered in the lane brief before launch: (i) executed compute POINT 0.2 core-min; (ii) lane wall POINT 10.0 core-min, CEILING 18.0 core-min.** No dollar figure was registered; none is derived here. | **Lane wall MEASURED: 15.20 core-min** (box clock `date -u` read inside the lane: first call 2026-08-24T17:59:42Z → 2026-08-24T18:14:54Z, the read that built this row = 912 wall s × 1 lane ÷ 60). **Executed compute: NOT SEPARATELY INSTRUMENTED — stated as absent, not approximated** (append rule 2). Its dominant single term was measured: the two full 5.8 MB log parses that re-derived every pass-11 §87 number, **0.07 wall s = 0.0012 core-min** (`/usr/bin/time`; user 0.04 s, sys 0.02 s). The remainder is ~35 short `git show` / `grep` / `stat` reads, none observed above a few seconds. | **= gross.** One lane, 912 wall s, nothing near the 3600-s stall rule, so cleaned == gross. **Waste: 0.00, named** (`COMPUTE_BUDGET_CHARTER.md` §6) — no read was discarded; the sealed blind reading, the archaeology and every re-derived clause value appear in pass 11 §83–§88. | **Lane wall 1.52× the 10.0 point, 0.84× the 18.0 ceiling — inside the 18.0 ceiling, with 2.80 core-min of margin.** **Executed compute: the 0.2 core-min point is UNTESTED** — no total was instrumented, and the measured 0.0012 core-min dominant term is a lower bound on it, not a test of it. | **Misprediction of the lane-wall estimate, mechanism named: the blind-reading protocol serialises reads that an ordinary audit batches.** Pass 11 §83 required the frozen blob, the C1 fixture and AMENDMENT 1 to be read and the reading written out in full **before** §14, the reader blobs or any graded number could be opened — four sequential read-then-write phases where an ordinary §2d pass opens everything at once. The point appears to have been priced as an ordinary pass (pass 10, C-35, registered 12.0 core-min of lane wall for a non-blind read). Contention **0.00** — no solver ran and no core was contended. **Calibration lesson: price a blind-reading limb as its own component, at roughly twice an ordinary read of the same material, because the sealing step forbids the parallelism that makes the ordinary read cheap.** Second lesson, on the other component: registering executed compute as a point without registering **how it will be measured** leaves it untestable — C-35 instrumented it, this pass did not, and that was an omission rather than a decision. | pass 11 §83–§89 of `docs/CROSS_TEAM_GATE_AUDIT.md`; audited record `verification/campaign/F4_SIGFPE_STEP01_RESULTS.md` (`5b5f5183`); prereg `verification/campaign/F4_SIGFPE_STEP01_PREREGISTRATION.md` (`0bbac521` → `290fcff2` → `d457f612`); reader blob `46f5c5af69cfa3ef29030969a06de0e88c6d48a6` (`4bf8138d`); step logs `verification/runs/F4_runs/swbli_cylflare/step{0_instrumented,1_inletupwind}/` |
| C-42 | 2026-08-24 | verification | **`scripts/check_record_reconciliation.py` repaired: a planted control PER RECORD KIND, read back from disk, a mutation harness, and a refusal when a control does not fire — closing the defect three lanes hit on 2026-08-24 and recorded in rows `C-36`, `C-39` and `C-40` of this ledger.** The check chose its planted forms by testing the path NAME, so `docs/DOCKET.md` and `docs/COST_CALIBRATION.md`, whose ids are table ROWS, were graded under NUMERICS heading forms, read `CONTROL KIND: BROKEN` / `ZERO_IS_UNSUPPORTED` — and still returned a reconciliation exit code as though the reading stood. Delivered: a per-record control-form table keyed to the same keys as the pattern table and asserted equal at import; a disk control per kind that plants a committed-only id and a worktree-only id into a temp repo and reads both back through the production readers; a `--selftest`; a five-mutant harness; and exit 5 as a refusal. **Zero solver compute; np=1 throughout, so core-min = wall-min on every figure in this row** | **Two components, both registered in the lane brief before launch as a POINT AND A CEILING — the `L-291` shape, honoured on both, so both ratios below are computable. EXECUTED COMPUTE: point 0.3 core-min, ceiling 1.0 core-min. LANE WALL: point 8 core-min, ceiling 14 core-min.** No dollar figure was registered; the derived one is in the record-ref cell | **EXECUTED COMPUTE 0.052 core-min (3.1 core-s), MEASURED at np=1** by `/usr/bin/time` over a counted invocation census: 3 × `check_record_reconciliation.py --selftest` at **0.60 s** each (each run builds four planted git repos AND runs five mutant selftests as subprocesses, so it is the dominant term by an order), 6 × live reconciliation runs at **≤ 0.16 s** (the four-path `--json` run, timed, is 0.16 s), 1 × `append_record.py --selftest` at **0.04 s**, 3 × ad-hoc blob-comparison scripts at **≈ 0.15 s** each over the 1.87 MB docket blob. **BOUND ≤ 0.085 core-min** including ≈30 untimed git plumbing calls and ≈10 grep/sed passes over the same blobs, same order of magnitude — claimed as a bound, not a measurement. **LANE WALL 9.53 core-min, MEASURED, not a floor**: `date -u` was written to a uniquely-named scratch file as this lane's FIRST action at **2026-08-24T18:10:30Z** — the transferable fix `C-40` asked for — through the read that built this row at **2026-08-24T18:20:02Z** (572 wall s × 1 lane ÷ 60). The only unmeasured interval is this lane's own first-turn reading of its brief, which preceded the first tool call by seconds and is bounded by it, not by a multi-minute read-only prologue | **= gross on both components.** The longest single invocation is 0.60 s wall, more than three orders below the 3600-s stall rule of `COMPUTE_BUDGET_CHARTER.md` §2, so stall cleaning removes nothing and gross = cleaned. **WASTE 0.00, NAMED** (§6): one `--selftest` run of the three exited 5 rather than 0, and it is deliberately **not** counted as waste — that run is the measurement which caught the mutation harness's own stale site (the fifth mutant's target string occurred TWICE in the file, once in the control table and once in the mutant list, so a first-occurrence replace would have mutated the list and let the mutant pass). It produced a finding and a repair, not a discarded read | **EXECUTED COMPUTE: 0.17× the 0.3 core-min point and 0.05× the 1.0 core-min ceiling — inside both, by ≈ 6× on the point. LANE WALL: 1.19× the 8 core-min point and 0.68× the 14 core-min ceiling — INSIDE the ceiling with 4.47 core-min of margin, so no overrun and nothing was stopped.** Both ratios are true ratios because both components were registered as points; `L-291` exists because four earlier rows could not say that | **(a) OVERPREDICTION OF EXECUTED COMPUTE, ≈ 6×, and the mechanism is a class misprice, not contention.** The brief priced a control repair as though the planted-control machinery were expensive. It is not: four temp `git init`+commit repos, five mutant subprocess selftests and every regex in the module together run in 0.60 s. **CALIBRATION LESSON, transferable: a pure-Python INSTRUMENT repair whose controls are temp git repos and string fixtures costs ≈ 0.05 core-min of executed compute — price this class at 0.1 core-min, not 0.3, and put the whole budget in lane wall, which is where the work actually is.** **(b) LANE WALL 1.19× the point, mechanism named: the reading, not the writing.** The item required both instruments read in full at HEAD before a line was written — `check_record_reconciliation.py` (283 lines) and `append_record.py` (651 lines), plus `control_kind.py` for the classification rules and `check_stamp_vs_commit.py`'s C6 block for the mutation-harness style the brief named — followed by a four-ledger diagnostic sweep and a byte-level divergence probe on two of them. **A repair brief that names a style to imitate is priced as the repair PLUS a full read of the exemplar.** **(c) CONTENTION: not measured and not claimed** — np=1 throughout, 16 cores on the box, no solver of this team live. **(d) HEAD MOVED DURING THE LANE**: `eb2a534b` at lane start, `2153bd08` as the script commit's parent, and this row's id was therefore re-derived from the HEAD blob inside the committing invocation, not before it (rule 11) | Script commit **`2e66e45e`** (`scripts/check_record_reconciliation.py`, 423 insertions / 27 deletions, one path, post-commit verify read only that path); this row at the commit that appends it. **Measured artifacts:** `--selftest` exit 0 with 4 records × 2 controls = **8 controls, every one `CONTROL KIND: RECOGNITION`**, **5 mutants**, all five driving a fresh selftest to exit 5, 0 failures; `append_record.py --selftest` still PASS with 0 control failures and that file **unmodified**. **Reconciliation facts read at `2153bd08` HEAD vs worktree, all four ledgers now `CONTROL KIND: RECOGNITION`, all four `worktree_only = 0` and 0 duplicates — and that zero is now a supported zero, which is the whole point of the repair**: `docs/LESSONS.md` 32 ids in HEAD not in the worktree (`L-264`…`L-295`, contiguous); `docs/NUMERICS_KNOWLEDGE.md` 17 (`N-AV1`…`N-AV5`, `N-B40`…`N-B42`, `N-D27`…`N-D35`, contiguous per series); `docs/DOCKET.md` 23 (`D485`…`D507`, contiguous); this ledger 27 (`C-15`…`C-41`, contiguous). **The stale worktree copies were INSPECTED, NEVER REVERTED and NOT reconciled** — that dispatch is the chief's (rule 10). Byte-level reading of the same staleness, recorded because it decides which append form is legal: `docs/LESSONS.md` and `docs/NUMERICS_KNOWLEDGE.md` worktree copies are **exact PREFIXES** of their HEAD blobs (pure D486 decay), while `docs/DOCKET.md` and this ledger **diverge inside HEAD's own bytes** — first differing byte 1779290 of 1870932 and **2338 of 130280** respectively, both worktree copies shorter, HEAD carrying prose blocks the worktree lacks. **DEVIATION DISCLOSED:** `scripts/append_record.py` was therefore run first and **REFUSED (exit 2)** on this ledger for exactly that reason, so this row landed by the **disclosed D486 hash-object fallback from the HEAD blob**, with the four checks `L-290` requires printed and read before `write-tree` — blob trailing byte is `\n`; new content byte-identical to the blob over its first 130280 bytes by `cmp -n`; the new id at the start of its own line exactly once; the previous last id `C-41` still at the start of its own line — plus the `L-263` assert that deletions are 0 on an append-only record. `append_record.py`'s own id arithmetic was still read and agreed: maximum existing `C-` number in the committed blob **41**. **`docs/LAB_STATE.md` deliberately NOT edited.** Rate $0.0513/core-h, c7a.4xlarge, reported-by-owner — the derived dollar figure for the executed component is **$0.00004, DERIVED not measured**; the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` §5) |
| C-43 | 2026-08-24 | dafoam | curriculum item **D2** — optimizer A/B on the D1 NLP (arm A IPOPT / arm B SLSQP, one CLI token apart on a byte-identical run script; both arms graded, AB2 GATE FAIL is a graded verdict, not a failed run) | **16.733 core-min** registered (probe 0.033 + arm A 6.017 + arm B 10.683) = **$0.014307 DERIVED**; 100 % contingency band ≤ 33.466; **HARD CEILING 140.0 core-min** — prereg §8, frozen `03580b8f`, launch authorisation §18 at `641c5938` | **12.150 core-min MEASURED** (wall × ranks ÷ 60 from the run root's own `ledger.txt`: arm A 336 s × 1 = 5.600, arm B 391 s × 1 = 6.517, both `rc=0`, both `docker inspect` `0 false`; plus the phase-1 image probe 0.033, measured in phase 1) = **$0.010389 DERIVED** | **= gross.** Longest wall is 391 s against the 3600-s stall rule, so nothing is cleaned out; waste is named separately below and is **not** netted off either column | **0.726×** cleaned/predicted (12.150 / 16.733); **0.0868×** of the hard ceiling and of the curriculum row's own ~140 core-min figure. Per arm: A **0.931×**, B **0.610×** | **WASTE: 0.000 core-min, named and separate.** Both arms `rc=0`, both produced every registered artifact, nothing re-run, nothing discarded, no crash, no relaunch; the launch gate opened on the FIRST reading for both arms, so zero core-minutes were spent waiting. **CONTENTION: MEASURED at −5.1935 %, and favourable — the first contention term measured on this ladder.** AB6 made the A4 §6.1 work-marker method available (three arms doing the same primal): wall-per-evaluation 20.2769 s (arm A, this session) against 21.3877 s (D1 arm O), so this session ran 5.19 % FASTER on identical work despite a concurrent D3 lane on the box. D1's C-24 had to report contention as *not separately measured*; that gap is now closed. **MISPREDICTION: the whole of the remaining gap, and it is almost entirely arm B (0.610×), decomposing into two independent factors — iterations over-predicted 20 → 13 (0.65×) and per-iteration driver cost over-predicted 27.92 s → 24.48 s (0.877×).** **C-24 NAMED, and its lesson's transfer MEASURED — this row is the test C-24 asked for.** C-24 taught that a whole-`check_totals` anchor prices a line search into every major and over-prices a run that accepts full steps. D2's P12 applied that correction and then added ONE line-search primal back (25.28 + 2.64 = 27.92 s), reasoning that an active-set SQP with an L1 merit line search would backtrack where an interior-point filter had not. **Measured answer: the lesson transferred, and the correction on top of it was itself an over-correction.** SLSQP ran at **1.15 evaluations/iteration** (`NFUNC 15` over 13 iterations) against IPOPT's **1.09** (12 over 11) — it backtracked, but bought roughly TWO extra primals across the whole run, not one per iteration — and the measured 24.48 s/iteration came in BELOW even D1's own no-line-search 25.28 s. **Calibration lesson: price a line-search primal by the measured evaluations-per-iteration ratio, never by assuming one trial per iteration; the ratio is read after the fact from `NFUNC`/`NGRAD` (SLSQP) or the `ls` column (IPOPT).** **A second lesson only visible because §8.2 registered all three bases: the per-unit predictions were slight UNDER-estimates (per-iteration 1.078×, per-evaluation 1.143×) while the total was a 1.38× over-estimate — the whole of the total's error is in the COUNT of units, not the price of one, so on this ladder the estimating effort belongs on the iteration count, and a wide band that says plainly the width is carrying the prediction (D1's P2, D2's P5) is the right instrument.** | `cases/dafoam/ladder-a/A1/curriculum_D2/RESULTS.md` §8 (and §6.6 for the AB6 work markers); pre-registration frozen at `03580b8f`, supervisor launch authorisation appended as §18 at `641c5938`; measured artifact, outside git: `/home/ubuntu/certonomous-runs/CURRICULUM-D2-a1-optimizer-ab/ledger.txt` and `preflight_history.txt` |
| C-44 | 2026-08-24 | verification | **CORRECTION ROW. It corrects row `C-42` of this ledger (commit `6f005e19`) and nothing else** — append rule 1: an existing row is never edited, a correction is a new row naming the row it corrects. **The defect: three divergence figures in `C-42`'s record-ref cell are PYTHON CHARACTER INDICES reported as BYTES.** These files carry multibyte UTF-8 (em dashes throughout), so the two units differ by about 1%, and the L-290 prefix check that `C-42` describes ran on BYTES via `cmp -n $(wc -c)` while the number quoted beside it was a character count. **The CHECK was correct and is unaffected; the NUMBER quoted for it was in the wrong unit.** Found by this lane immediately after its own commit, by re-reading the blobs in binary | **No compute was registered for a correction row.** It is bookkeeping arising inside the `C-42` item, whose two components were registered there (executed compute point 0.3 / ceiling 1.0 core-min; lane wall point 8 / ceiling 14 core-min) | **Byte-true figures, MEASURED by reading each blob and each worktree file in binary mode at HEAD `2153bd08`** — every one replacing a character count in `C-42`: `docs/LESSONS.md` HEAD **726258** bytes, worktree **655485**, worktree an exact PREFIX (D486 decay, no divergence inside the blob's bytes); `docs/NUMERICS_KNOWLEDGE.md` HEAD **279824**, worktree **254652**, also an exact prefix; `docs/DOCKET.md` HEAD **1877722**, worktree **1809535**, first differing byte **1785217** (`C-42` said 1779290 of 1870932 — characters); `docs/COST_CALIBRATION.md` HEAD **131596** bytes (`C-42` said 130280 — characters). The L-290 prefix check on this ledger therefore ran over **131596 bytes**, not the 130280 `C-42` quotes. **Executed compute for this correction: ≈ 0.01 core-min**, three short binary reads plus one commit chain | **= gross.** Nothing here is near the 3600-s stall rule. **Waste 0.00, named**: the corrected figures came from re-reading blobs that were already on disk; nothing was re-computed and no read was discarded | **No ratio.** A correction row carries no registered prediction of its own; `C-42`'s two ratios stand unchanged, because the unit error is confined to the record-ref cell's divergence figures and touches neither component's measurement | **Mechanism, and it is transferable: `len()` on a Python `str` is CHARACTERS, `wc -c` and `cmp -n` are BYTES, and a lane that probes divergence in Python and then asserts it in shell is mixing the two silently.** Every lab record whose files carry em dashes is exposed. **Fix: read blobs in binary — `git show` captured without `text=True`, files opened `'rb'` — whenever the number will be quoted beside a `wc -c` or `cmp -n` result.** **Second disclosure, a live D486 observation rather than a defect: the worktree copy of this ledger CHANGED during this lane.** At the probe it was 23712 characters and diverged from HEAD inside HEAD's own bytes; at this reading it is **143555 bytes and an exact EXTENSION** of the 131596-byte HEAD blob — a peer ran `scripts/append_record.py`, whose merge writes the worktree back, mid-lane. **Inspected, never reverted** (rule 10); a divergence figure for these files is true only of the instant it was read, and `C-42`'s were | Corrects `C-42` at commit `6f005e19`; the repaired instrument is `scripts/check_record_reconciliation.py` at `2e66e45e`. **Nothing in `C-42` is edited or struck** — this ledger is append-only and the row stands as filed, with this row as its correction. The reconciliation facts, control counts, mutant counts and both cost components reported in `C-42` are **unaffected** and are not restated here. **`docs/LAB_STATE.md` deliberately NOT edited.** This row landed by the same disclosed D486 hash-object fallback with L-290's four checks and the L-263 deletions-are-zero assert, because `scripts/append_record.py` still refuses on this file |
| C-45 | 2026-08-24 | ansys-verification | **VMFL001-R2 GRADED `PASS` — the re-run of VM2026R1 VMFL001 (flow between rotating and stationary concentric cylinders, manual pp. 15–16) after run 1 returned `NOT A RESULT`.** Three-level Roache family (1024 / 4096 / 16384 cells), OpenFOAM v2606 `simpleFoam`, steady laminar, **serial (np = 1, so core-min = wall-min on every figure in this row)**. Register row **#2**, citing row **#1**. Zero GPU, zero container | **3.7167 core-min** (223 wall s), a **POINT estimate**, with a separately registered **CAP of 10 core-min** — both frozen in `PREREGISTRATION.md` §7 at blob `c6b4a7c4` before any solver started. **= $0.003178 derived.** The point was built from run 1's *measured* per-level walls (L1 2 s, L2 13 s, L3 104 s / 3000 iters) with L3 scaled **linearly** by N/3000 = 2 ⇒ 208 s. The freeze labelled that scaling an estimate and not a measurement, which is what makes this row a real test of it | **3.2833 core-min MEASURED** — 197 wall s × 1 rank ÷ 60, read from `COST.txt` and corroborated row by row against the three per-level `RUN_RC.txt` (L1 2 s / 0.0333, L2 12 s / 0.2000, L3 183 s / 3.0500). **= $0.002807 DERIVED, NOT MEASURED**, at $0.0513/core-h, c7a.4xlarge, **reported-by-owner** — the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` §5) | **= gross, 3.2833 core-min.** The longest single wall on this item is **183 s**, an order and a half below the 3600-s stall rule (`COMPUTE_BUDGET_CHARTER.md` §2), so stall cleaning matches nothing and gross and cleaned coincide. **Waste is named separately in the attribution cell and is netted off neither column nor the ratio** | **0.8834× (3.2833 / 3.7167) — INSIDE the estimate, in the conservative direction; 0.328× of the 10 core-min cap, which was never approached and no `CAP_EXCEEDED.txt` exists.** Per level, against the frozen basis: **L1 1.00×** (2 s predicted, 2 s actual), **L2 0.92×** (13 → 12 s), **L3 0.88×** (208 → 183 s). The whole of the gap is L3 | **Misprediction only, mechanism named and measured; contention present but directionally against the gap; waste 0.000.** **(a) THE MECHANISM: extending a converging steady SIMPLE run's `endTime` is SUB-LINEAR in wall time.** The freeze scaled run 1's 104 s / 3000 iterations to 208 s / 6000 at constant cost per outer iteration. Measured per-outer-iteration cost was **34.67 ms in run 1's first 3000** but **30.50 ms across R2's 6000** — the *second* 3000 iterations are cheaper, because as the residuals fall the inner linear solves converge in fewer sweeps. At iteration 6000 the log shows `Ux` taking **4** smoothSolver iterations and the second `p` GAMG solve taking **0**. **TRANSFERABLE RULE FOR THE NEXT ESTIMATE: price a continuation of a converging steady run at ≈ 0.88 × linear, not 1.00 ×; linear scaling of wall time by N/N₀ over-estimates, here by ≈ 12 %.** **(b) CONTENTION: present, direction stated, magnitude NOT measured and NOT claimed.** Three T-family solvers (heat-transfer team) were live on the 16-core box for the whole of this run — established with a `find verification/runs -mmin -10` sweep taken before launch, not inferred. Contention pushes *serial* wall time **up**, so a contention-free run would cost *less* and the 0.8834× **understates** the over-estimate rather than flattering it. No load figure was taken, so no numerical penalty is derived. **(c) WASTE: 0.000 core-min, and the claim is checkable** — no level failed, no level restarted, no level hit the cap, the run script's three guards all passed on the first attempt, and **the comparator returned exit 0 on its first invocation**, which is the thing run 1 could not do. **(d) SECOND CALIBRATION FINDING, on a prediction that is not a cost: the freeze's residual-decay extrapolation held to 2.6 %.** `PREREGISTRATION.md` §3.2 fitted run 1's L3 `Ux` decay at −9.6065×10⁻⁴ decades/iteration and predicted **≈ 1.57×10⁻⁹ at iteration 6000**; the measured value is **1.61027×10⁻⁹** (ratio 1.026) — a two-decade extrapolation across 3000 unrun iterations, verified after the fact. **Lesson: a log-linear residual-decay fit over the last 1000 iterations of a converged-trend steady run is a sound basis for choosing N, and choosing N for two decades of margin below the registered criterion cost 12 % less than the linear wall estimate said it would.** | `cases/ansys_verification/VMFL001/R2/RESULTS.md` §7 (estimate-vs-actual) and §2.3 (the residual extrapolation); pre-registration frozen at blob **`c6b4a7c4`**, commit `4507fc66`; comparator blob **`64b02be8`**; run script blob **`116c7c2d`**; HEAD at run **`1fb3bf74`**. **Measured artifacts, all committed at `fd2321ef`:** `verification/runs/ansys_verification/VMFL001/R2/COST.txt`, the three `<level>/RUN_RC.txt`, the three `<level>/log.simpleFoam`, and `GRADING_VMFL001_R2.json`. Run 1's cost row for comparison is **C-37** (1.9833 core-min) |
| C-46 | 2026-08-24 | dafoam | curriculum item **D3 ATTEMPT 2** — A4 Ahmed body, CONSTRAINED drag minimisation (the `nom_setConstraintSurface` two-line repair of attempt 1). **Item `BLOCKED` at Stage G**, falsifier class **`F1c`** (downstream-producer) by the pre-registration's own G11(d) crash-frame rule: the repair WORKED (attempt 1's `Need to add surface "default"` KeyError absent from the 492-line `geom.log`; setup completed; both DVCon calls returned) and the run died 47 lines further on at producer `:253` `dvg.update("aero")` → pyGeo `pyBlock.py:745` → **`KeyError: 'aero'`**. Stages η, O, T-patched, T-shipped **not launched**; no second budget taken | **29.6 core-min** registered total (G 0.8 + η 2.5 + O 21.0 + T 5.3), 100 % contingency band ≤ 59.2, **HARD CEILING 69.2 core-min** = $0.0592 DERIVED — prereg §10.1/§10.2, frozen `092e54e7`, launch authorisation ADDENDUM A1 at `60d699fe`. **Stage G alone predicted 0.8, ceiling 3.0** | **2.8667 core-min MEASURED** (172 wall s × 1 rank ÷ 60, from the run root's own `ledger.txt`; `rc=1`, `timeout` 180 s did NOT fire, no OOM — exit 1 not 137; **`docker inspect` NOT available: prereg §11 registers `--rm`, so the exit code is the launcher's `wait` value**) = **$0.002451 DERIVED**. Peak RSS **0.5973 GiB** recomputed from `rss_geom.txt` against predicted 1.4 / ceiling 2.5 — **the ledger row's own `peak_rss_GiB=11` cell is a launcher misparse of `611.6MiB` and is wrong; recorded, not fixed** | **= gross.** The single row's wall is 172 s against the 3600-s stall rule, so nothing is cleaned out; waste is named separately below and is **not** netted off either column | **Stage G: 3.583×** cleaned/predicted (2.8667 / 0.8) — **the one calibratable figure this run produced**, and it is under its own 3.0 stage ceiling (95.6 %) and under the 69.2 HARD (4.14 %), so **F12 did not fire**. **Attempt total: 0.097×** (2.8667 / 29.6) — **NOT A CALIBRATION OF THE ESTIMATE**, and labelled so at every appearance: three of the four stages never launched, so the denominator prices work never attempted. Same caveat C-24/attempt-1 carried, for the same reason | **MISPREDICTION — the whole of the Stage-G gap, and it is LOCATED.** `geom/mphys.html`, written by producer line **215** (`om.n2`), has mtime `18:33:35.815Z` against a container start of `18:31:01Z` in a 172 s wall: **producer line 215 completed ≈154.8 s in, ≈90 % of the run**, leaving ≤ ~17 s for `OptFuncs` (`:217`), the whole `geom_probe` block and teardown. **The cost is in the COMMON SETUP PATH that every stage pays on every task, not in the stage's own work.** Root cause of the miss: **Stage G's 0.8 was re-based on prereg §2.2's pre-freeze probe, which measured container + `prob.setup` at 11 s wall and stopped at line 214 — one line before the expensive one.** §10.1 named that basis explicitly, so this is a **registered and disclosed risk that materialised**, not a hidden one. **CONTENTION: NOT MEASURED and NOT CLAIMED** — §10.3 registers the contention basis as the same internal work item in two logs, and the probe log carries no marker past `setup`, so no like-for-like comparison exists; host `load1` was 3.29 at the G6 reading with the container capped `--cpus=1`, which makes contention a live candidate and nothing more. Whole-arm wall clocks are explicitly NOT an inflation figure (§10.3). **The `om.n2`≈144 s arithmetic assumes the setup path was not itself inflated; that assumption is UNMEASURED and the row does not rest on it.** **WASTE: 2.8667 core-min GROSS, unrecovered, named separately and never absorbed into either ratio** (`COMPUTE_BUDGET_CHARTER.md` §6) — Stage G was priced for P1b/P2/P3/P15 and returned **none** of them, the same accounting attempt 1 applied to its own 0.300 at a figure now **9.6× larger**. **What the spend DID buy is named separately and is NOT subtracted from the waste:** the repair proved live, G7 `PASS` read from inside the loading process, G11 limbs (a)+(b) `PASS`, all five G11(c) setup markers present in `geom.log` at their registered counts, the next defect located to a line, and the ≈90 %-of-wall setup cost measured. **CALIBRATION LESSON, and it is the row's point: price a stage from a probe only as far as the probe actually ran.** A probe that stops at `prob.setup` prices `prob.setup`; every producer line after it is unpriced, and on this producer those lines carry ~90 % of a no-flow stage's wall. **Consequence: every per-stage price in prereg §10.1 rests on a ~11 s setup cost and must be re-derived before any attempt 3 is frozen.** **Two-attempt-plus item total: ≤ 3.6500 core-min worst case = $0.003121 DERIVED** (attempt 1 Stage G 0.300 MEASURED + attempt 1 triage ≤ 0.30 **BOUND, not a measurement** + attempt 2 pre-freeze probe 0.1833 MEASURED + attempt 2 Stage G 2.8667 MEASURED), **5.21 % of the curriculum row's ~70.0 core-min / $0.06. The item's budget is not what stops it; `F1c` is.** | `cases/dafoam/ladder-a/A4/curriculum_D3_attempt2/RESULTS.md` §6 (§6.1 spend, §6.2 estimate-vs-actual, §6.3 this row and the departure that let this lane land it); pre-registration frozen at `092e54e7`, supervisor launch authorisation appended as §20 ADDENDUM A1 at `60d699fe`; measured artifacts, outside git: `/home/ubuntu/certonomous-runs/D3-a4-constrained-attempt2/{ledger.txt,preflight_history.txt,rss_geom.txt,geom.log}` and `/home/ubuntu/certonomous-runs/D3-a4-attempt2-probe/probe_ledger.txt`. Attempt 1's row is `C-38`; this row does not correct it and does not supersede it |
| C-47 | 2026-08-24 | ansys-verification | **VMFL005 GRADED `PASS` — Poiseuille flow in a pipe, VM2026R1 manual p. 25, reproduced against the Hagen–Poiseuille analytical reference.** Three-level Roache family (1 000 / 4 000 / 16 000 cells) at endTime 2000 / 3000 / 6000, OpenFOAM v2606 `simpleFoam`, steady laminar on a one-cell-thick 5° axisymmetric wedge, **serial (np = 1, so core-min = wall-min on every figure in this row)**. Register row **#3**. Zero GPU, zero container | **4.9 core-min** (295 wall s), a **POINT estimate**, with a separately registered **CAP of 13 core-min** — both frozen in `PREREGISTRATION.md` §9 at blob `43aaf6bf` (commit `2d54a629`, 18:42:07Z) before any solver started (first run artifact 18:45:32Z). **= $0.004190 derived.** The estimate had three named terms: solver **217 s** (VMFL001's measured per-cell-iteration rates applied to this mesh family), `codedFixedValue` runtime compile **45 s** (~15 s/level × 3), and margin **33 s** (~15 % of solver). The freeze labelled the scaling, the compile time and the margin ESTIMATES and not measurements, which is what makes this row a real test of all three | **4.0000 core-min MEASURED** — 240 wall s × 1 rank ÷ 60, read from `COST.txt` and corroborated row by row against the three per-level `RUN_RC.txt` (L1 11 s / 0.1833, L2 29 s / 0.4833, L3 200 s / 3.3333). **= $0.003420 DERIVED, NOT MEASURED**, at $0.0513/core-h, c7a.4xlarge, **reported-by-owner** — the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` §5) | **= gross, 4.0000 core-min.** The longest single wall on this item is **200 s**, 18× below the 3600-s stall rule (`COMPUTE_BUDGET_CHARTER.md` §2), so stall cleaning matches nothing and gross and cleaned coincide. **Waste is named separately in the attribution cell and is netted off neither column nor the ratio** | **0.8163× (4.0000 / 4.9) — INSIDE the estimate, in the conservative direction; 0.308× of the 13 core-min cap, which was never approached and no `CAP_EXCEEDED.txt` exists.** In wall seconds the same ratio is 240 / 295 = 0.8136× | **Misprediction only; mechanism named and MEASURED per term, not inferred — `log.simpleFoam` prints both `ExecutionTime` (solver CPU) and `ClockTime` (wall), so startup-and-compile is `wall_s − final ExecutionTime` and the three frozen terms separate cleanly. Solver: predicted 217 s, MEASURED 221.43 s (4.68 + 23.37 + 193.38) — ratio 1.020×, accurate to +2.0 %. Compile + startup: predicted 45 s, MEASURED 18.57 s (6.32 + 5.63 + 6.62) — ratio 0.413×. Margin: 33 s predicted, 0 s consumed. The WHOLE of the −18.6 % gap is the compile allowance plus the unused margin.** At every level the first `ExecutionTime` line reads `ClockTime = 6 s` while `ExecutionTime` itself is ≤ 0.17 s, so **an OpenFOAM `codedFixedValue` `wmake libso` build plus solver startup costs ≈ 6.2 s per fresh case directory on this box, not 15 s — price it at 6 s next time.** **The per-level structure of the solver term is the more instructive miss, and it cancels:** the frozen basis borrowed VMFL001's per-cell-iteration rates, which GREW with mesh size (6.51e-7 → 1.06e-6 → 2.12e-6 s/cell·iter); VMFL005's measured rates are FLAT — **2.34e-6 / 1.95e-6 / 2.01e-6** s/cell·iter at L1/L2/L3. So the two small levels were under-predicted (L1 1.3 → 4.68 s, 3.6×; L2 12.7 → 23.37 s, 1.84×) and the large level over-predicted (L3 203.1 → 193.38 s, 0.95×), and because L3 is 87 % of the solver time the net came out at 1.020×. **Transferable: for a 2D axisymmetric wedge `simpleFoam` case on this box use a CONSTANT ≈ 2.0e-6 s per cell-iteration, not a size-dependent rate borrowed from a different geometry.** **WASTE: 0.000 core-min**, named separately per charter §6 and netted off neither column nor the ratio — no level failed, no level restarted, no level was capped, no re-run of any kind was needed, and the comparator returned exit 0 on its first invocation. **CONTENTION: NOT CHARACTERISED and not assumed to be zero** — no load figure was taken before or during the run; peer teams were committing throughout the window (`f2b54c3b`, `10b3e97c`, `2f1d6cb7`, `22abb11d` all land 18:45–18:47Z) but a commit is not a solve and this lane did not check for live solvers. **DISCLOSED DEFECT in the frozen estimate, not edited (rule 6):** `PREREGISTRATION.md` §9 prints the per-cell-iteration basis with exponents 1000× too small (`6.51e-10 / 1.06e-9 / 2.12e-9` where VMFL001's measurement is `6.51e-7 / 1.06e-6 / 2.12e-6`); the PRODUCTS in the next row are computed from the correct values and are right, so the frozen 295 s / 4.9 core-min estimate and the 13 core-min cap are unaffected and no gate, threshold, cap or label moves. Recorded in `RESULTS.md` §7.2 and referred to the supervisor | `cases/ansys_verification/VMFL005/RESULTS.md` §7 (verdict `PASS`, this commit); artifacts `verification/runs/ansys_verification/VMFL005/` (commit `90ee8d80`), `COST.txt` and three `RUN_RC.txt`; prereg blob `43aaf6bf` (commit `2d54a629`); comparator blob `8e7410cc`; register row **#3**. **Landed by the disclosed HEAD-blob fallback** — `scripts/append_record.py` REFUSES on this file because its worktree copy is ~3,892 bytes SHORTER than the HEAD blob (a truncation, not an append); this row was built from `git show HEAD:docs/COST_CALIBRATION.md`, staged with `git hash-object -w` + `update-index --cacheinfo`, id re-derived from the HEAD tail in the commit's own shell invocation, insertions-only asserted. **The stale worktree copy was INSPECTED, NEVER REVERTED** (CLAUDE.md rule 10) |
| C-48 | 2026-08-25 | heat-transfer | **T1b L4 x-arm — PARTIAL, the two CLOSED cases only** (`R_10k_x`, `R_300k_x`). Trigger is `COST_CALIBRATION.md` append rule 1's **"a case closed"**, which stands on its own and is not an early draw on the rung's row; `R_100k_x` and `R_30k_x` are **still running** and the rung-level row is owed **at completion**, citing this id (C-37/C-45 precedent). Drafted at `verification/runs/T-family/T1_runs/L4_CALIBRATION_DRAFT.md`. | **978.6 core-min** (`R_10k_x` 16.31 core-h) **+ 3 541.8** (`R_300k_x` 59.03 core-h) = **4 520.4 core-min / 75.34 core-h / $3.8649 derived**, from `T1b_L4_AMENDMENT.md` §5 line 290, the **registered** schedule 20000/80000/80000/80000 (205.44 core-h / $10.54 for all four) — frozen blob `ad7208b5`, commit `17209b50`, 2026-08-21T18:14:22Z, **3 h 13 min before first launch** | **5 965.58 core-min** = 99.4264 core-h (wall × ranks ÷ 60, the charter unit: 103 303 + 254 632 wall s, single rank) | **5 965.58 core-min — GROSS EQUALS CLEANED, and here that is MEASURED not argued**: `ExecutionTime`/wall = **0.99888** and **0.99936**, so only **115 s and 163 s of 357 935 wall s** were not solver CPU. **Waste nil (0.00 core-min)** — checked, not assumed: no second `log.solve` segment, no crash or cap marker, no `.attempt*` sibling, no non-zero `rc`, four time dirs as `purgeWrite 2` prescribes, and the only host restart (`uptime -s`) is 2026-08-21T15:21Z, before both launches | **1.3197×** (`R_10k_x` **1.7594×**, `R_300k_x` **1.1982×**) | **MISPREDICTION ~90 %; contention real, measured, 5.5–11.3 % of each miss; waste nil.** **(a) The rate was borrowed across a 2.56× mesh-size jump** — §5's rates were measured on an **81 920-cell** mesh and applied to the **209 920-cell** x mesh. Measured early throughput is 40 643 and 41 337 cell-it/s against 71 546 and 79 020 assumed = **0.568× and 0.523×**. The rate ratio reproduces the wall ratio to four figures (0.5681 vs 1/1.7598 = 0.5682; 0.8346 vs 0.8345): **the whole of both misses is the rate.** **(b) Throughput roughly DOUBLES as a steady SIMPLE case converges, and it is NOT contention** — `R_300k_x` runs ~43 000 cell-it/s over iterations 1–23 000 and **92 000–94 000 from ~33 000 onward while sar shows host load FLAT at 3.78–3.86 busy cores across the transition** (the C-45/VMFL001 effect: falling residuals ⇒ fewer inner sweeps). `R_10k_x` stops at 20 000 and never earns it; `R_300k_x` gets 60 000 iterations of it — **which is precisely why 1.759× becomes 1.198×.** **CONTENTION, measured from `/var/log/sysstat/sa21`–`sa24` rather than reconstructed** (sar `%user` × 16; the proxy self-validates — each window's *floor* equals the live solver count): mean busy cores **6.08** over `R_10k_x`'s 28.70 h and **4.95** over `R_300k_x`'s 70.73 h; the 12–16-way regime is real but is only **6.2 %** and **2.5 %** of their walls. Measured penalty inside it: throughput fell to **0.613× and 0.615×** of each case's own preceding rate = **41.0 and 41.2 core-min** excess (41–79 over the wider window), i.e. **5.5–10.0 % and 5.9–11.3 %** of overruns of 743.4 and 702.2 core-min. **Contention was NOT the prime suspect.** Upper bounds: 10-min bins smear window edges and the excess assumes the pre-window rate would have persisted. **C-23's PATTERN REPRODUCES; C-23's MECHANISM DOES NOT** — and this is the transferable finding. The short case misses far worse than the long one (1.759× vs 1.198×), exactly C-23's shape, but C-23 located that in a **missing fixed startup/IO term** (its `ExecutionTime`/wall was 0.775–0.858). **Here it is 0.999.** C-23's recommended 1 400–1 900 s per-case fixed term would explain **1.4 % of `R_10k_x`'s 76 % overrun** — right in sign, negligible in size, and **adopting it would have HIDDEN the real cause.** C-23's fixed cost is a property of **short-WALL** cases (1.8–4.5 h), not low-iteration ones; these ran 28.7 h and 70.7 h, where startup is unmeasurable. **§5's claim that a contended measurement makes the estimate "an upper bound on cost" is FALSIFIED**: the contention margin it banked was worth 1–4 % of wall; the mesh-extrapolation error it did not know about was worth 75 %. **NO CAP AND NO STOP THRESHOLD ARE REGISTERED FOR THIS RUNG** — the words cap, ceiling, stop, overrun and budget do not occur anywhere in the frozen amendment. **Supervisor's ruling: §5 registered a PREDICTION, and a prediction is not a cap, so there is no cap to overrun and rule 12's "an overrun stops the run" is NOT engaged.** The projected rung total (~270.05 core-h / **~$13.85 derived**, ratio ~1.315×) sits inside the **$25 pre-authorisation**. **The two live solvers CONTINUE.** Stopping them would forfeit ~8 866 core-min already spent and produce nothing gradeable — `analyse_t1b_L4.py:170-173` refuses partial pools — so stopping is strictly worse on every axis. **The ABSENCE of a cap is recorded as the defect, not the overrun.** | `verification/runs/T-family/T1_runs/L4_CALIBRATION_DRAFT.md`; `T1b_L4_AMENDMENT.md` §5; `gate_t1b.json`; predecessor **C-23** |
| C-49 | 2026-08-25 | dafoam | **Curriculum item `D15` — verification-at-scale institutionalization, discharged as the adjoint family's V-column standard:** `docs/dafoam/V_STANDARD_FD_VS_ADJOINT.md` v1.0, landed at `4a6ea0b8` (2026-08-24T19:18:52Z, **one file changed, +941 insertions, 0 deletions, one path**). Size verified in this lane three ways and all three agree: **941 lines / 62,939 bytes** at that commit's own blob, at `HEAD`, and on disk. **A ZERO-COMPUTE DOCUMENT ITEM** — no solver, no mesher, no container, no GPU, no field written, no run root, no `ledger.txt`. **No verdict is issued by this row and none is owed:** D15's product is a gate TEMPLATE (§13 of the deliverable, `:748`), and the deliverable says of itself at `:19` that it *"creates no gate, no threshold and no cap"*. **This row lands a day after the item it calibrates** — the comparison rule 12 owes at completion was not written at the time. That delay is disclosed here rather than smoothed over; it is the same failure `C-8` recorded against `K0cG`, in a much cheaper instance | **TWO COMPONENTS, AND ONLY ONE OF THEM WAS EVER ESTIMATED.** **(a) SOLVER COMPUTE: `~0 standalone` core-min** — the item's own registered price, `cases/dafoam/EXPERTISE_CURRICULUM.md:126` (Tier 5 D15 row), whose falsifier column reads *"n/a (it IS a gate template)"*; quoted back verbatim inside the deliverable at `V_STANDARD_FD_VS_ADJOINT.md:28`. **= $0.00 DERIVED.** No pre-registration was frozen for D15 and **none was owed** — `CLAUDE.md` rule 2 freezes a gate before a RUN, and there was no run. **(b) AGENT EFFORT: NO ESTIMATE EXISTS, IN ANY UNIT.** No point, no ceiling, no band and no bound was registered at dispatch, and the curriculum's own cost model (§2: *"cost ≈ majors × (primal + adjoint + line-search primals), scaled by cells and ranks from the nearest anchor"*) **has no term in which a document could be expressed at all.** This is `L-291`'s defect in its most complete form: not a `≤ N` bound standing in for a point (the `C-25`/`C-26`/`C-27`/`C-30`/`C-40` shape), but **no number whatever on the side where the entire cost of this item actually sat** | **(a) SOLVER COMPUTE: 0.000 core-min MEASURED, and the zero is real rather than a missing reading.** The item invoked no solver, mesher, container or GPU, so there is no log, no `ledger.txt` and no run root to read a figure from — **and no figure is invented to fill the cell**. **= $0.00 DERIVED, NOT MEASURED**, at $0.0513/core-h, c7a.4xlarge, **reported-by-owner** (the box cannot read its own billing, `COMPUTE_BUDGET_CHARTER.md` §5). **(b) AGENT EFFORT — A DIFFERENT QUANTITY, IN A DIFFERENT UNIT, DELIBERATELY NOT CONVERTED: `lane-minutes`. It is NOT multiplied into core-minutes and NO DOLLAR FIGURE IS STATED FOR IT AT ALL.** $0.0513/core-h prices c7a.4xlarge **compute**; a thinking lane holds neither a known rank count nor the instance exclusively (this one was one of three dafoam lanes at cap, beside four other supervisors' fleets and three live `buoyantBoussinesqSimpleFoam` at 99.9 % CPU each, load 13.16 at the dafoam session's formation reading), so a per-core price applied to it would be a **fabricated allocation** — the precise thing §5 forbids. **D15 LANE: ≥ 6.83 lane-min, ≤ 18.81 lane-min — A BRACKET, NOT A MEASUREMENT, with both endpoints read from committed or logged timestamps.** **FLOOR 6.83** = the dafoam board `69d2f1ba` (2026-08-24T19:12:02Z), which already carries *"Lanes live (3 of 3, at cap) … (2) **V standard** — `docs/dafoam/V_STANDARD_FD_VS_ADJOINT.md`, zero compute, also the deliverable of curriculum **D15**"* → the D15 commit at 19:18:52Z; it is a FLOOR because the lane was **already live** when that board was written. **CEILING 18.81** = this chief session's own `session_start` at 2026-08-24T19:00:03Z → the same commit; a lane cannot predate the session that spawned it. **Corroborating and NOT used as an endpoint:** the deliverable's last write is `mtime` 2026-08-24T19:17:52.466Z, 60 s before the commit. **A narrower ceiling of ≈ 10.3 lane-min follows IF the fleet was formed after the harness model-substitution commit `7c469330` (19:08:34Z) — that ordering is an INFERENCE, not a measurement, and no figure in this row rests on it.** **Lane model: `lab-lane` on Opus** (`model: opus` in `.claude/agents/lab-lane.md` at `4a6ea0b8`; the 19:08:34Z substitution moved SUPERVISORS Fable→Opus 5 and left the lane file untouched). **(c) THIS ROW'S OWN LANDING, named separately and folded into nothing: 0.000 core-min of solver compute, and lane wall ≥ 9.2 lane-min MEASURED AS A FLOOR** (this lane's first scratch write 2026-08-25T00:12:42Z → this commit), by a second dafoam `lab-lane` on **Opus 5**. Rule 12 makes the calibration comparison part of the completion, so this cost belongs to D15 and is stated rather than hidden — but it is a different lane on a different day and is kept a **different number**. Its own read-only prologue (`CLAUDE.md` in full, `COMPUTE_BUDGET_CHARTER.md` in full, this ledger's header and five pattern rows) wrote nothing and left no timestamp, which is why that figure is a floor too — the `C-40` caveat recurring, and `C-40`'s transferable fix (*a dispatched lane should `date -u` into a uniquely-named scratch file as its FIRST action*) **was again not applied, and is again recommended** | **= gross on every component, and the reason is stated rather than asserted: the 3600-s stall rule of `COMPUTE_BUDGET_CHARTER.md` §2 cleans LEDGER ROWS, and this item produced none.** With zero solver invocations there is nothing for the rule to match, so cleaned = gross = **0.000 core-min** on the compute component. On the lane-effort component the stall rule **does not apply at all** — it is defined over solver wall seconds — and no cleaning of any kind is claimed for it. **WASTE: 0.000 on both components, named separately per §6 and netted off nothing** — one lane, one deliverable, one commit (+941 / −0, one path), nothing re-run, nothing discarded, no crash, no re-stage, no second budget. **Two disclosed limits of the PRODUCT, which this row will NOT launder into waste or into cost:** §14 of the deliverable (`:870`) lists the numbers in its commissioning brief that the lane **could not verify as stated**, and its opening (`:42-52`) records that `docs/COVERAGE_MATRIX.md` **does not exist on disk**, so the V-column definition the standard is written against is carried from the verification team's brief and **not from a committed artifact**. Both are properties of what was delivered, not of what was spent | **(a) SOLVER COMPUTE: THE RATIO IS UNDEFINED — 0.000 against `~0`, i.e. 0/0 — AND IT IS NOT REPORTED AS `1.0×`.** The only true statement is a containment check and not a calibration: **the item spent zero against a price of approximately zero, which was correct and carries no information about this lab's estimating.** `C-5` (cfd, F7a R0) recorded this same shape as *"exact (0/0) — as predicted, zero-compute"*; **this row declines that phrasing deliberately**, because *exact* reads as an estimating success and 0/0 is not one. **(b) AGENT EFFORT: NO RATIO IS COMPUTABLE — the denominator does not exist.** This is not the `C-11`/`C-13`/`C-14` shape, where both sides were absent; here the actual side is **bracketed from timestamps** and it is the **estimate** that was never made. **A ratio cell that averaged the two components, or that reported this item as `1.0×`, would be the single most misleading number this ledger could carry — and none is written** | **THE LAB HAS NO ESTIMATE-VERSUS-ACTUAL DISCIPLINE FOR ZERO-COMPUTE DOCUMENT ITEMS, AND THAT SENTENCE IS THIS ROW'S ENTIRE VALUE.** The curriculum's `~0 standalone` core-min was **TRUE AND USELESS AS A PREDICTOR OF EFFORT**: **true**, because D15 ran nothing and spent 0.000 core-min; **useless**, because **every unit of cost this item actually incurred was agent effort — reading, re-verification and drafting — and the estimating method has no column in which that could have been written down.** **This is a GAP IN THE ESTIMATING METHOD, not a good prediction, and it must not be read as one.** No conventional attribution applies and none is claimed: **contention 0.000 and NOT claimed** — no load reading was taken by the D15 lane and none is invented here; **waste 0.000**; **misprediction — none on the compute side, because zero was right, and none COMPUTABLE on the effort side, because there is nothing to have departed from.** **What the item cost is nevertheless knowable in SHAPE, and is stated so the next such estimate has an anchor:** a **941-line, 62,939-byte** standards document, every number in which the lane re-read from the artifact it cites (the deliverable asserts this of itself in its opening), cost a bracketed **6.83–18.81 lane-min on Opus** — against `L-291`'s own registered dictation-class shape of **point 2.5 / ceiling 4.0 lane-min** and `C-40`'s **7.0 core-min measured floor** for six lesson blocks. **THREE THINGS THIS ROW PROPOSES AND DOES NOT DECIDE** — moving an estimating convention is not a lane's call (`CLAUDE.md` rule 9; FIRST-ACTION reserved matters). **(1) A zero-compute document item should register a LANE-WALL POINT AND CEILING at dispatch**, per `L-291`, and its `~0 core-min` should be recorded as what it is — a statement that the item **buys no compute**, never an estimate **of the item**. **(2) A `0/0` compute ratio should be written UNDEFINED, never `1.0×` and never "exact"** — `C-5` is the row to re-read if that is adopted, and **`C-5` is NOT corrected by this row: no number in it moves.** **(3) THE UNIT FOR LANE WALL IS UNSETTLED IN THIS LEDGER, AND THE CONFLICT IS DISCLOSED HERE RATHER THAN RESOLVED:** `L-291` frames lane wall as *"what the box is actually rented for while a lane thinks"* and `C-36` and `C-40` both state it in **core-min at np = 1**; this row states it in **lane-min with no dollar figure**, because here np is **zero, not one** — no core is reserved to a thinking lane and several lanes shared the box throughout. Both readings are defensible and **they give different dollar figures for the same minutes.** Referred to the dafoam supervisor and to verification; **unruled, and this row does not act as though it were ruled** | **Deliverable:** `docs/dafoam/V_STANDARD_FD_VS_ADJOINT.md` — v1.0 amendment record §16 (`:933`), the D15 framing and the `~0 standalone` quotation at `:25-32`, the no-gate statement at `:19`, the checklist §13 at `:748`, the could-not-verify list §14 at `:870`, the coverage-matrix disclosure at `:42-52` — landed at `4a6ea0b8`. **Registered price:** `cases/dafoam/EXPERTISE_CURRICULUM.md:126` (Tier 5 D15 row) and its §2 cost model. **Timestamps:** dafoam board commit `69d2f1ba` (the lanes-live line) and the dafoam section of `docs/LAB_STATE.md`; **measured artifact outside git**, cited for the 2026-08-24T19:00:03Z session start: `/home/ubuntu/harness-state/sessions/2026-08.jsonl`; `.claude/agents/lab-lane.md` at `4a6ea0b8` for the lane model. **Precedent rows named, none corrected:** `C-5` (the 0/0 shape), `C-36` and `C-40` with `L-291` (lane wall priced separately from executed compute), `C-11`/`C-13`/`C-14` (dispatched with no estimate), `C-8` (a calibration written after the fact). **No existing row is corrected by this one and no number in any existing row moves.** **LANDING METHOD, disclosed because this file moved twice under this lane while the row was drafted:** at first read the worktree copy was a strict byte-prefix of the HEAD blob and FOUR ROWS SHORT (`C-43` against `C-47`); at the next read a peer's unlanded `C-48` sat in the worktree tail and `scripts/append_record.py --dry-run` **REFUSED with exit 6** rather than renumbering it (*"ASK, never renumber"*), printing `C-49` as the correct next id; by landing time `C-48` had committed at `8cde653f` and blob and worktree coincided. **The id was therefore re-derived from the HEAD blob's own tail INSIDE the committing shell invocation** (`CLAUDE.md` rule 11 — the maximum existing number, never a count), the row merged onto the HEAD blob by the D486 `git hash-object` fallback under `L-290`'s four asserts (the blob's trailing byte is `\n`; the prefix byte-identical over the whole blob; the new id anchored at the start of its own line; the PREVIOUS last id still anchored at the start of its own line), and landed through the `CLAUDE.md` rule 10 private index with the `diff-tree` assert, a CAS on `refs/heads/main` and the post-commit verify. **The worktree copy was deliberately NOT written** — so a concurrent peer's tail cannot be clobbered and a CAS retry needs no unwind (D486 divergence-by-design). **STILL OWED AND NOT DISCHARGED HERE: the curriculum §7 execution-state-ledger row for D15** (`cases/dafoam/EXPERTISE_CURRICULUM.md` §7), which `docs/LAB_STATE.md` names as the dafoam supervisor's own next action and which this lane's brief did not cover. Rate $0.0513/core-h, c7a.4xlarge, reported-by-owner; **every dollar figure in this row is DERIVED, NOT MEASURED, and the agent-effort component deliberately carries none** |
| C-50 | 2026-08-25 | cfd | **F12 rung 1 — RAE 2822 AGARD AR-138 Case 9, COARSE workshop rung (48/48/80 = 23,040 cells, M 0.734, alpha 2.79, Re 6.5e6), `rhoSimpleFoam` + k-omega SST, serial (ranks = 1, so core-min = wall-min on every figure in this row).** The pre-registration's designated **rate-calibration rung** (2026-08-23 COSTED ADDENDUM §5). **Outcome: admission gate A GATE FAIL and admission gate B GATE FAIL; every physics quantity NOT A RESULT.** The rung nonetheless discharged its registered calibration purpose, which is why this row exists and why the ratio column is about a RATE and not about a total | **CAP 120 core-min** frozen at prereg blob `41ec748a` (v1.2, HEAD `af2b23b0`), 2026-08-23 addendum §5. **ESTIMATES, both carried by the freeze and both labelled ESTIMATED there: Basis A 713.5 s = 11.9 core-min ($0.010) and Basis B 6,564 s = 109.4 core-min ($0.094)**, each at 6,000 iterations and 1 rank, from rates 4.7015e-6 and 4.6011e-5 s per cell-iteration. **Meshing for this case ESTIMATED 3.86 s = 0.064 core-min**, by §4's own linear-in-cells scaling of the F2 secondary record's 0.6 s at 3,584 cells | **0.4617 core-min MEASURED** — 27.7 s driver wall x 1 rank / 60, from `result.json` (`started_utc` 01:03:06.630Z, `ended_utc` 01:03:34.350Z). **= $0.000395 DERIVED, NOT MEASURED**, at $0.0513/core-h, c7a.4xlarge, **reported-by-owner** — the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` §5). Split, each read from the log files' own birth and mtime timestamps rather than inferred: **blockMesh 0.351 s, checkMesh 0.407 s, rhoSimpleFoam 20.5 s wall (19.92 s final `ExecutionTime`)**; the residual ~6.4 s is the driver's Python import, dictionary writing and traceback capture and is named rather than folded into the solver term | **= gross, 0.4617 core-min.** The longest single wall on this item is 20.5 s, **175x below** the 3600-s stall rule (`COMPUTE_BUDGET_CHARTER.md` §2), so stall cleaning matches nothing and gross and cleaned coincide. **Waste is named separately in the attribution cell and is netted off neither column nor the ratio** | **THE TOTALS ARE NOT COMPARABLE AND NO TOTAL RATIO IS WRITTEN: the run stopped at iteration 180 of the registered 6,000 (3.0 %), so 0.4617 against 11.9 is not a calibration, it is an interruption.** The comparable quantity, and the one the rung was registered to produce, is the RATE. **MEASURED 4.8301e-6 s per cell-iteration** (19.92 s / (23,040 cells x 179 timed iterations)). **Against Basis A: 1.027x — Basis A is CONFIRMED to +2.7 %. Against Basis B: 0.105x — Basis B is FALSIFIED for this solver by a factor 9.5.** Projected full rung at the measured rate: 667.7 s = **11.13 core-min, 0.936x of Basis A's 11.9** and 0.102x of Basis B's 109.4. **Meshing: measured 0.771 s against 3.86 s estimated = 0.200x — over-estimated 5x** | **WASTE: 0.4617 core-min — the WHOLE of this row's spend, named in full and netted off neither column nor the ratio (charter §6).** The mesh **fails admission gate A** (max non-orthogonality **70.64625857** against the frozen threshold **<= 70**, 892 severely non-orthogonal faces; max skewness 0.827 passes; max aspect ratio 10,606.65 on 16 cells is ADVISORY and not gated), so the compute was spent on a mesh that is **inadmissible as evidence** under the pre-registration's own §"Gates, declared now". That the rate datum was obtained anyway does not make the spend un-wasted and is not used to reduce this figure. **MISPREDICTION — and it is the freeze's basis SELECTION that is calibrated, not its arithmetic.** The 2026-08-23 addendum §3 carried two bases 9.79x apart, declined to choose, and set rung 1's cap at 120 core-min precisely so the rung would complete on either. **It completed on Basis A to within 2.7 %, and the cap was never approached — 0.4617 of 120 is 0.38 %.** Transferable, and this is the row's main value: **for a 2D transonic aerofoil `rhoSimpleFoam` + k-omega SST case on this box, price at 4.83e-6 s per cell-iteration; do NOT price a compressible aerofoil solve off a `simpleFoam` incompressible O-grid record — Basis B was wrong by 9.5x.** A second transferable: **the §4 linear-in-cells meshing term is 5x too expensive at 23k cells**; `blockMesh` + `checkMesh` on this family cost ~0.77 s, not ~3.9 s. **CONTENTION: PRESENT BUT NOT CHARACTERISED, and not assumed to be zero.** `uptime` at 01:00:35Z, 2.5 min before launch, read **load 3.62 / 8.31 / 31.34 on 16 cores** — the box was unwinding from a heavy 15-min window and peer lanes were committing throughout (`666a3c9e` inside the 10 min before launch). No load reading was taken DURING the 27.7 s window. Since the measured rate landed at 1.027x an uncontended analog, any contention penalty is bounded above by roughly 3 % and is not separately quantified. **A SECOND FINDING, reported and NOT repaired: the frozen `run_case` computes `mesh_gate` and then launches the solver anyway.** The gate result is recorded in the returned record but never enforced, so 20.5 s of solver wall was spent on a mesh already known to fail gate A. Under `CLAUDE.md` rule 4's refuse-never-degrade this should have refused before spending; it is NOT a departure from the frozen text, which gates admission of EVIDENCE and not the act of launching. Referred to the cfd supervisor; **no code was changed by this lane and no gate, threshold, cap or label moves** | `verification/runs/F12_runs/coarse_workshop_M0.734_a2.79/result.json` (this commit) and its built dictionaries; **on-disk-only artifacts, GITIGNORED and therefore NOT filed** (`.gitignore:260` for `log.*`, `:59` for time dirs, `:66` for `constant/polyMesh/`, `:67` for `postProcessing/`) — the crash log `verification/runs/F12_runs/coarse_workshop_M0.734_a2.79/log.rhoSimpleFoam`, `log.checkMesh` and `log.blockMesh`, whose numbers this row quotes; the FOAM FATAL ERROR text itself IS preserved in git inside `result.json`'s `traceback` field. Driver `verification/runs/F12_runs/run_f12_rung.py` (this commit). **Prereg blob `41ec748a06b513414101dca9780107f08a25ddec` (v1.2, HEAD `af2b23b0`); grading path blobs `a18314f7` (`sdk/workflows/rae2822_case9.py`), `404ce432` (`sdk/workflows/tmr_verification.py`), `8dee0d31` (`scripts/roache_triple.py`) — all three verified disk-identical to their HEAD blobs by this lane in the launching session, before the launch.** Predecessor rows named, none corrected: **C-47** (per-cell-iteration rate transferred across geometries), **C-48** (a borrowed rate missed across a mesh-size jump). Rate $0.0513/core-h, c7a.4xlarge, reported-by-owner; every dollar figure in this row is **DERIVED, NOT MEASURED** |
| **C-51** | 2026-08-25 | ansys-verification | **VMFL051 GRADED `NOT A RESULT` — Isentropic Expansion of Supersonic Flow Over a Convex Corner, VM2026R1 manual pp. 165–166, the ansys-verification team's FIRST compressible/supersonic case and its first `rhoCentralFoam` run.** Three-level refinement-2 Roache family (6 240 / 24 960 / 99 840 cells) at the same physical `endTime` = 7e−3 s at every level, OpenFOAM v2606 `rhoCentralFoam`, **transient with `adjustTimeStep yes`**, **serial (np = 1, so core-min = wall-min on every figure in this row)**. Register row **#4**. Zero GPU, zero container. **The verdict is `NOT A RESULT` on two independent clauses of `CLAUDE.md` rule 5 — L1 and L2 both fail the frozen plateau clause, and the triple is `OSCILLATORY` — and NONE of that is a cost event**: all three levels completed with rc = 0 inside the cap, nothing failed, nothing restarted, and **this row therefore records a full-price run that produced a refusal, which is exactly what the lab wants such a run to cost** | **11.3 core-min POINT ESTIMATE (677 wall s), with a separately registered CEILING of 28 core-min** — both frozen in `PREREGISTRATION.md` §9.1 at blob `7dad5616` (commit `22249c82`, 2026-08-25T00:20:33Z) **before any solver started** (first mesh artifact 00:25:59Z; the before-first-compute Amendment 1 at `54d34542`, 00:25:34Z, altered no cost figure). **= $0.009661 at the point estimate, $0.02394 at the ceiling, both DERIVED.** The estimate had **four named terms and declared which were measurements and which were not**: solver **579 s**, from the cfd team's **MEASURED** F3 `rhoCentralFoam` throughput on this box borrowed as setup knowledge (6.65e−7 and 7.24e−7 s per cell-step; **the higher figure was deliberately taken**); meshing `blockMesh` + `checkMesh` + `topoSet` across three levels **~40 s (an estimate)**; `MachNo` and two `volFieldValue` objects evaluated every timestep at **~10 % of solver ≈ 58 s (an estimate)**; and step counts from a CFL argument, dt = 0.4·h/(U₂+a₂), giving **1 755 / 3 510 / 7 020 (an estimate)**. `L-291`'s four numbers were all registered: §9.2 also priced **lane wall** separately at 35 point / 60 ceiling lane-min for the drafting lane and **20 point / 40 ceiling** for this run-and-grade lane | **23.3167 core-min MEASURED** — 1 399 wall s × 1 rank ÷ 60, read from `COST.txt` and corroborated level by level against the three `RUN_RC.txt` (**L1 9 s / 0.1500, L2 207 s / 3.4500, L3 1 183 s / 19.7167**). **= $0.019936 DERIVED, NOT MEASURED**, at $0.0513/core-h, c7a.4xlarge, **reported-by-owner** — the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` §5) | **= gross, 23.3167 core-min.** The longest single wall on this item is **1 183 s** (L3), **3.04× below** the 3 600-s stall rule (`COMPUTE_BUDGET_CHARTER.md` §2), so stall cleaning matches nothing and gross and cleaned coincide. **Waste is named separately in the attribution cell and is netted off neither column nor the ratio; contention likewise, and contention here is large** | **2.0634× (23.3167 / 11.3) — OUTSIDE the point estimate, and the ratio is reported at that value with NOTHING netted out of it.** **0.8327× of the 28 core-min cap: the cap was never reached and no `CAP_EXCEEDED.txt` exists**, so rule 12's "an overrun stops the run" was never engaged — the point estimate was exceeded, the registered ceiling was not | **THE HEADLINE, AND IT INVERTS THE USUAL SHAPE OF THIS LEDGER: THE FROZEN ESTIMATE'S PHYSICS WAS RIGHT AND ITS ENVIRONMENT ASSUMPTION WAS WRONG. Essentially the whole 2.06× is CONTENTION, not misprediction.** **(1) SOLVER CPU, MEASURED per level from `log.rhoCentralFoam`'s final `ExecutionTime`: 7.71 + 59.31 + 467.50 = 534.52 s**, against the freeze's combined solver-plus-function-object allowance of **637 s** — **ratio 0.839×, INSIDE the estimate and in the conservative direction.** **(2) THE BORROWED PER-CELL-STEP RATE TRANSFERRED ACROSS GEOMETRY ALMOST EXACTLY, and this is the transferable finding.** F3's cases are wedge/cone/diamond **compressions**; VMFL051 is an **expansion**. Measured s/cell-step: **7.2982e−07 (L1), 7.0615e−07 (L2), 6.9742e−07 (L3)** against the frozen **7.24e−07** — **1.008× / 0.975× / 0.963×**, i.e. the borrowed rate was right to **within 3.7 % at worst and 0.8 % at best**, and the overall figure is **6.9883e−07** across 764 880 480 cell-steps. **The rate is also FLAT in mesh size (7.30 → 7.06 → 6.97e−07, mildly DECREASING), not growing** — which is `C-47`'s recommendation vindicated on a second solver: *use a CONSTANT per-cell-step rate, not a size-dependent one borrowed from a different geometry*. **For `rhoCentralFoam` 2D on this box, price at 7.0e−07 s per cell-step.** **(3) The CFL step-count estimate was also good: 1 693 / 3 365 / 6 714 actual against 1 755 / 3 510 / 7 020 predicted — 0.965× / 0.959× / 0.956×, a consistent ~4 % over-prediction** because the frozen dt used the post-expansion `U₂+a₂` while the adaptive controller sees the local maximum. **(4) THE ONE REAL MISPREDICTION IS SMALL AND IS IN THE MESHING TERM: ~40 s allowed, ≤ 2 s MEASURED** — the three solver logs' `ClockTime` sum to 8 + 207 + 1 182 = **1 397 s** against a total run wall of **1 399 s**, so `blockMesh`, `checkMesh` and `topoSet` across all three levels together account for **at most 2 s**, and the allowance was **≥ 20× too large**. `blockMesh` and `topoSet` print no `ExecutionTime` line on v2606, which is why this is a measured BOUND and not a measured value — stated rather than rounded into a figure. **(5) CONTENTION: 864.48 s = 14.408 core-min, MEASURED, NAMED SEPARATELY, AND NETTED OFF NEITHER COLUMN NOR THE RATIO** (`COMPUTE_BUDGET_CHARTER.md` §6). It is simply wall minus solver CPU (1 399 − 534.52) and it is **62 % of the entire spend on this item**. Evidence is live and contemporaneous rather than reconstructed, in `CONTENTION.txt`: **loadavg 68.38 / 64.32 / 56.15 at 00:30:40Z rising to 75.84 / 70.80 / 60.75 at 00:34:52Z on `nproc = 16`**, with two `buoyantBoussinesq*` at 99.9 % CPU and five `tesseract` at 84.4–89.3 % — **all of them other teams' work, none of it this case's, and none of it touched** (standing rule). Measured `ClockTime`/`ExecutionTime` by level: **L1 1.17×, L2 3.49×, L3 2.53×** — and the ordering is itself informative: **L1 ran at 00:26, essentially uncontended, and is the only level whose wall is near its CPU**; the load built underneath L2 and L3. **Had the box been idle this run would have cost ≈ 8.91 core-min against an 11.3 core-min estimate, a ratio of 0.79×.** That counterfactual is arithmetic on measured CPU and is offered as an explanation, **NOT as a corrected actual — the actual is 23.3167 and the ratio is 2.0634×.** **(6) WASTE: 0.000 core-min**, named separately per §6 and netted off nothing — no level failed, none restarted, none was capped, no re-run of any kind was needed, and the comparator returned exit 0 on its first invocation. **(7) THE STANDING GAP THIS ROW DOES NOT CLOSE: the lab's estimating method has no CONTENTION TERM AT ALL.** `PREREGISTRATION.md` §9.1's margin was a solver-accuracy margin, not a load margin; `C-47` recorded contention as *"NOT CHARACTERISED and not assumed to be zero"*; `C-48` measured it at 5.5–11.3 % of its misses and found it was **not** the prime suspect. **Here it is 62 % of spend and it IS the whole story**, which is the opposite result on the same axis. **PROPOSED AND NOT DECIDED** (moving an estimating convention is not a lane's call, `CLAUDE.md` rule 9): a wall-denominated estimate on this box should carry an explicit contention factor read from `loadavg` at freeze time, or the estimate should be denominated in **solver CPU** and the cap alone left in wall. Referred to the ansys-verification supervisor and to verification; **unruled, and this row does not act as though it were ruled.** **(8) INSTRUMENT DEFECT, DISCLOSED AND NOT YET FIXED — THE CAP UNIT AND THE ENFORCEMENT UNIT ARE DIFFERENT UNITS. The cap is denominated in CORE-MINUTES and was enforced as a WALL-CLOCK `timeout` in `run_vmfl051.sh`. For THIS run they coincide EXACTLY, and only because the run is SERIAL** (at 1 rank, core-min = wall_s × 1 ÷ 60). **The coincidence is arithmetic, not design. On any PARALLEL case they would NOT coincide and the enforcement would be wrong by a factor of the rank count** — a `timeout` of `cap_core_min × 60` seconds on an 8-rank job would permit **8× the registered budget** before firing, which is a cap that does not cap. **The correct conversion is `timeout_seconds = cap_core_min * 60 / ranks`.** It changed nothing about this run's cap, verdict or numbers, and it is recorded HERE, in `CONTENTION.txt` where it was first noticed, and in `RESULTS.md` §7.1, **so it is fixed before this team runs its first parallel case.** Referred to the supervisor; changing the launcher's cap idiom is not this lane's call. **(9) LANE WALL, priced separately per `L-291` and NOT converted into core-minutes: this grading lane ≥ 14 lane-min MEASURED AS A FLOOR** (its own `date -u` read at 2026-08-25T01:06:30Z → this commit), against §9.2's registered **20 point / 40 ceiling** for the run-and-grade lane — **inside both, on a floor**. It is a floor because **`C-40`'s transferable fix — *a dispatched lane should `date -u` into a uniquely-named scratch file as its FIRST action* — was AGAIN not applied**; this lane's `date -u` came mid-task, after the manual and the freeze had already been read. **That is now four rows recommending the same one-line fix (`C-36`, `C-40`, `C-49`, this row) and four rows not applying it**, and the lane wall of the *drafting* lane (§9.2's 35/60 pair) is **not measurable at all** from committed timestamps and is left **stated as absent rather than approximated** (append rule 2). **No dollar figure is attached to any lane-minute here** — `C-49`'s unresolved unit conflict is live and is not resolved by this row | `cases/ansys_verification/VMFL051/RESULTS.md` §7 and §7.1 (verdict `NOT A RESULT`, commit `ef3499f9`); artifacts `verification/runs/ansys_verification/VMFL051/` (commit **`fc46ad2d`**, 25 files) — `COST.txt`, `CONTENTION.txt`, three `RUN_RC.txt`, three `log.rhoCentralFoam` for the `ExecutionTime` figures, `GRADING_VMFL051.json`; prereg blob `7dad5616` (commit `22249c82`), comparator blob `acad1aff`; register row **#4** (commit `393476d9`). **THIS ROW'S ID IS NOT THE ONE REGISTER ROW #4 CITES, AND THAT IS A DEFECT OF THIS LANE'S, CORRECTED AT THE FOOT OF THE REGISTER RATHER THAN BY EDITING THE APPEND-ONLY ROW.** Row #4 says *"Calibration row `C-50`"*; **`C-50` at HEAD is the cfd team's F12 rung 1** (RAE 2822, AGARD AR-138 Case 9), landed at commit `cd1ac21a`, **2026-08-25T01:09:21Z** — **four minutes and fifty-two seconds BEFORE** register row #4 landed at **01:14:13Z**, so the id was already taken and already visible at HEAD when that row was written. **Root cause, stated plainly and not softened: the id was derived BEFORE the commit and not re-derived inside the committing shell invocation**, which `CLAUDE.md` rule 11 requires in terms — *"Peers commit constantly: re-derive at commit time, in the same shell invocation."* **Contributing, and not an excuse: the worktree copy of this file is ~19.8 kB shorter than HEAD (168 635 B against 188 448 B), so an id derived from it is derived from a truncated file and is wrong before any race begins.** **This is the same defect class as `L-292`** — an id in prose before its append is a prediction, not an identifier — **which register row #4 itself cites, and which this team corrected in row #3's `D510` hours earlier. It is the team's SECOND instance.** THIS row's id was re-derived from the HEAD blob's own tail inside the shell invocation that wrote its tree, and would have been re-derived again on any CAS retry. **Precedent rows named, none corrected:** `C-47` (the constant-rate recommendation, here vindicated on a second solver; and its *contention not characterised* cell, here characterised), `C-48` (contention measured and found minor — the opposite result on the same axis), `C-49` and `C-40` and `C-36` (lane wall priced separately, and the unapplied `date -u` fix). **No existing row is corrected by this one and no number in any existing row moves.** **LANDING METHOD, disclosed because this file is measurably stale in the worktree:** the worktree copy is **~13 kB SHORTER than the HEAD blob** (168 635 vs 181 803 bytes), so the row was built from `git show HEAD:docs/COST_CALIBRATION.md`, the id **re-derived from the HEAD blob's own tail INSIDE the committing shell invocation** (`CLAUDE.md` rule 11 — the maximum existing number, never a count; append rule 5), staged with `git hash-object -w` plus `update-index --cacheinfo`, insertions-only asserted via `diff-tree`, landed through the rule-10 private index with a CAS on `refs/heads/main` and the post-commit verify. **The worktree copy was deliberately NOT written**, so a concurrent peer's tail cannot be clobbered, and **it was INSPECTED, NEVER REVERTED** (rule 10) |
| **C-52** | 2026-08-25 | verification | **The coverage matrix completed to an exhaustive pass across all five families (153 rows), plus two personal rulings (§91 F12/§2d, §92 recipe-fork), the recovery of a killed lane's attribution sweep, and the recovery of Sanaa's consolidation-week directive from the session record.** Commits `6c906f58`, `3d62bc52`, `2cd70f8d`, `06aa4985`, `84960439`, `24e2d6cb`. **Session ended by Sanaa's pause, not by exhaustion of a budget** | **0.0 core-min registered.** No pre-registration was written and none was owed: **rule 2 freezes a gate before a SOLVE, and no solve was proposed at any point.** No cap, no ceiling, no per-arm timeout | **0.0 core-min solver / 0.0 GPU-hours.** No solver, no mesher, no container, no case directory, no GPU, on this supervisor or on any of its three lanes. **The only non-zero execution anywhere in the session is the §92 lane's `scripts/recipe_audit.py` and `scripts/roache_triple.py` runs and disk walks — single-rank Python, estimated under ~5 core-min and NOT INSTRUMENTED.** It is reported as **unmetered, not as measured**: rule 12 forbids calling a cost measured without a record behind it, and no `ledger.txt` or timing harness wrapped those calls | = gross; nothing to clean, no row approaches the 3600-s stall rule | **n/a — a ratio of 0.0/0.0 is undefined and is NOT reported as 1.000** | **THE ROW'S CONTENT IS THE GAP, and it is the one `C-49` named from the other side.** Two zero-compute document items in two days (dafoam's D15, this) now show the same thing: **the lab's calibration discipline is built for solves and has nothing to say about the work that actually consumed this session.** The binding constraint here was never core-minutes — it was **the weekly token limit that killed the entire fleet at ~00:50Z**, taking this team's attribution-integrity lane and its supervisor with it. That lane's 341 lines survived on disk only by luck and were recovered at `3d62bc52`; **had they not been, the session's largest finding would have been lost and nothing in the ledger would have recorded the loss.** *Proposal, for Sanaa and not adopted here:* a zero-solver item should register **the resource that actually binds it** — lanes spawned, lanes killed mid-work, and whether their output reached HEAD — because **a cost ledger that reads 0.0 for the session that lost a fleet is measuring the wrong thing.** Stated as a gap, not filled: **adding a ledger column is a change to a shared standard and is hers.** **Waste: none chargeable in core-minutes; one lane's uncommitted work lost lab-wide to the 00:50Z kill, named here because rule 12 says waste is reported, not absorbed** |
| C-53 | 2026-08-25 | heat-transfer | **T1b L4 PLANTED-ZERO CONTROL — the external rule-3 instrument for the T1b grading chain, pre-registered FROZEN/ARMED/UNFIRED at `3ae9e504` and run to PASS (exit 0) at `48fe29c0`. NOT a rung grade: the control arms an existing gate, it does not create one.** | **under 1.0 core-min**, registered in §6 of the frozen prereg as an estimate with **no cap** (nothing to cap; this team's standing ruling that a prediction is not a cap applies, and none was claimed) | **12.63 wall s x 1 rank = 0.2105 core-min**; $0.00018 **derived, not measured** | **0.2105 core-min — gross == cleaned**, and that is a MEASUREMENT not an assumption: single-rank, single invocation, no stall (the 3600 wall s stall threshold is not approached at 12.63 s), **waste nil** | **0.21x** (0.2105 / 1.0) | **CONSERVATIVE CEILING, NOT A MISPREDICTION — and the distinction is the point of this row.** §6 registered 'under 1 core-minute' as a bound on a thing never before measured, not a forecast of it. The work is six `postProcess` calls on a 209,920-cell mesh plus ~340 MB of scratch copying, which is a **~0.2 core-min item**; the ceiling was set 5x above it because the true figure was unknown. **No contention component**: single invocation, no concurrent load attributed. **REUSABLE FIGURE: ~0.2 core-min is the calibration for the next copy-and-read control instrument**, and a successor should register ~0.3 rather than re-deriving a 5x ceiling. **Predecessor rows named, NONE corrected:** C-48 (a borrowed rate missed across a mesh-size jump) and C-47 (a per-cell-iteration rate transferred across geometries) are both **misprediction** rows on SOLVER work; this row is a **ceiling** row on INSTRUMENT work and the two classes must not be pooled — pooling a 0.21x ceiling with a 1.32x solver miss would report a lab that predicts well on average while being wrong in both directions. | Instrument `verification/runs/T-family/T1_runs/planted_zero_control_t1b.py` (`94648230`); result of record `verification/runs/T-family/T1_runs/LANE_REPORT_COMPLETION.md` foot addendum (`48fe29c0`); prereg + AMENDMENT 1 `docs/campaigns/T-family/T1b_L4_PLANTED_ZERO_CONTROL_PREREGISTRATION.md` (`3ae9e504`, sha256 `303924c1...b16f6`; amendment `764b6c20`). Grading-path blobs re-hashed AFTER the run, byte-identical to prereg commit `17209b50`. Rate $0.0513/core-h, c7a.4xlarge, **reported-by-owner**; every dollar figure here is **DERIVED, NOT MEASURED** (`COMPUTE_BUDGET_CHARTER.md` §5) |
| **C-54** | 2026-08-25 | cfd | **F11 C4 mechanism probe — two arms at Re 1000, n = 32 (1,024 cells), serial, 1 rank, run OUTSIDE the repo at `/home/ubuntu/certonomous-runs/f11_c4_probe_2026-08-25/` and outside every registered run path** (the registered run root `verification/runs/F11_runs/conversion_2026-08-25/runs` was confirmed absent before and after). Arm A applied `F11_CONVERSION_PREREGISTRATION.md` §6.1 exactly as frozen; arm B added a separately-named `centerlineSeries` sampler. **Outcome: C4 CANNOT BE SATISFIED AS FROZEN** — arm A converged at **747 iterations** and wrote fields at `747/`, but `postProcessing/centerlineProfiles/` holds **`250/` and `500/` only**, so C4's literal path is empty at the graded iteration and all six registered runs would have graded `NOT A RESULT` on a dictionary defect rather than on physics. Arm B converged at **747 iterations identically**, so the added sampler does not move the solution. **A completed process, so rule 12 applies; the six-run wave it governs is UNFIRED and carries no verdict.** **THIS ROW IS ALSO A DISCIPLINE FINDING — see the gap-attribution cell: the id this row was announced under was consumed by a different row 30 minutes later** | **NO PRE-REGISTERED ESTIMATE EXISTS FOR THE PROBE ITSELF, and this row will not invent one.** The frozen pre-registration's own AMENDMENT 1 says so in terms, at line 965 of the HEAD blob: *"The probe itself was compute spent **outside** this document's §7 cap and is not drawn against it; it is a mechanism probe, not a run in §6's matrix, and it produced no graded value."* **The `~1 core-min` estimate asserted on cfd's board at `docs/LAB_STATE.md:1734` and again at `:1981` IS NOT ON DISK.** This lane searched for it and it traces to nothing frozen; the board's own ratio of 0.25 implies a denominator of **0.9910 core-min**, and no committed text carries that figure. **It is STRUCK from this row.** What IS frozen — and what makes the probe calibratable at all — is §7's line for **exactly this configuration**, at line 560 of the HEAD blob: **`Re 1000, n = 32` at 4.0 core-s = 0.06667 core-min per run**, `cost_basis` *"**ESTIMATE, labelled.** No n = 32 measurement exists anywhere"*, built as **1,500 iterations at 0.001787 s/iter = 2.6805 s of `ExecutionTime`, rounded up to 4.0 to cover everything else.** **Two arms of that configuration = 8.0 core-s = 0.1333 core-min ESTIMATED.** The §7 **HARD CAP of 13.0 core-min is NOT this row's referent** and was never drawn against, by the freeze's own words | **0.2477506 core-min MEASURED** = 14.865038395 wall-s x 1 rank / 60, summed from the two arms' own driver stopwatches: arm A **8.077935934 s** (`re1000_n32/probe_timing.json`, `total_wall_s`, with `rc: 0` and `ranks: 1`) and arm B **6.787102461 s** (`armB_re1000_n32/probe_timing.json`, `wall_s`, `rc: 0`, `ranks: 1`). **= $0.00021183 DERIVED, NOT MEASURED**, at $0.0513/core-h, c7a.4xlarge, **reported-by-owner** — the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` §5). **Split, read from the artifacts rather than inferred: solver `ExecutionTime` 1.38 s (arm A) and 1.43 s (arm B)**, both `log.simpleFoam` files carrying `SIMPLE solution converged in 747 iterations` and an `End` line; arm A's driver `solve_wall_s` **5.237800837 s**, leaving **2.840135 s** outside the solve window for `blockMesh`, `checkMesh` and the driver itself. Arm B ran **no `checkMesh`** — no `log.checkMesh` exists in its directory — which is consistent with its 1.290833 s lower total and is named rather than folded into any rate | **= gross, 0.2477506 core-min.** The longest single wall on this item is **8.078 s, 446x below** the 3600-s stall rule (`COMPUTE_BUDGET_CHARTER.md` §2), so stall cleaning matches nothing and gross and cleaned coincide. **Waste is named separately in the gap-attribution cell and is netted off neither column nor the ratio** (charter §6) | **THE BOARD'S RATIO OF 0.25 IS WITHDRAWN — it divides by a denominator that is not on disk.** Against the only frozen referent, §7's `Re 1000, n = 32` line, and stated on **BOTH** bases because they disagree and the disagreement is this row's finding. **(a) §7 LINE-ITEM basis — 4.0 core-s read as a per-run TOTAL price: arm A alone 8.0779 / 4.0 = 2.0195x; both arms 14.8650 / 8.0 = 1.8581x. That is an OVERRUN of ~1.9x, NOT a spend in the conservative direction.** **(b) §7 ARITHMETIC basis — the 2.6805 s of `ExecutionTime` the estimate actually computes: arm A 1.38 / 2.6805 = 0.5148x; both arms 2.81 / 5.3610 = 0.5242x.** **No cap was breached on either basis**, because the probe sits outside §7's cap by the freeze's own words. **The decomposition closes exactly on basis (b), and is shown so the next estimate can be built from it: rate 0.0018474 measured / 0.001787 predicted = 1.0338x, times iterations 747 / 1,500 = 0.4980x, gives 0.5148x — which IS arm A's basis-(b) ratio to four significant figures** | **WASTE: ZERO core-min, named separately and netted off neither column nor the ratio (charter §6).** Both arms returned the finding they were run for, and **the zero is PLANTED, not assumed** (`CLAUDE.md` rule 3): this lane listed the trees itself and arm A's `centerlineProfiles/250/` and `/500/` each hold both `uAlongX05_U.xy` and `vAlongY05_U.xy` while `747/` is absent, and arm B's `centerlineProfiles/747/` holds both files — so the reader was demonstrated able to see a non-zero before the zero was believed. 0.248 core-min prevented a six-run wave from grading `NOT A RESULT` on a sampling dictionary. **MISPREDICTION — AND IT IS THE OPPOSITE SIGN FROM THE ONE THE BOARD CLAIMED.** The board attributed the gap to *"misprediction in the conservative direction"*. On the ITERATION term that holds, and it is the row's transferable win: **§7 assumed 1,500 iterations and measured 747 — over-predicted 2.008x**, exactly the direction §7 itself flagged (*"generous ... n = 32 implies ~655"*; the measured 747 sits 14 % above §7's own unrounded guess and far below the number it used). On the RATE term it is mildly false in the unsafe direction: **measured 0.0018474 s/iter against 0.001787 predicted is 3.38 % HIGH, not low.** **And on the term that actually dominates, the estimate is 5x too CHEAP: §7's round-up from 2.6805 s to 4.0 s leaves 1.3195 s for everything that is not solver iteration, and arm A measured 6.6979 s — 5.076x under-predicted, and 82.9 % of the arm's entire wall.** **THE TRANSFERABLE FINDING, and it generalises past this family: at n = 32 this case is FIXED-COST DOMINATED, not iteration dominated. §7's model is built from `ExecutionTime` rates in old logs, but the billable unit is wall-seconds x ranks, and on arm A wall was 5.85x `ExecutionTime`. Extrapolating an `ExecutionTime` rate into a billable core-minute silently drops the larger term at small n.** The effect concentrates at small n and is immaterial at n = 128, where §7's measured 176.73 s dwarfs any fixed cost — which is why this row does **not** challenge the family's 8.02 core-min total prediction or its 13.0 core-min cap. **CONTENTION: PRESENT BUT NOT CHARACTERISED, and NOT assumed to be zero.** Arm A reports `ExecutionTime = 1.38 s` against `ClockTime = 4 s`, arm B `1.43 s` against `2 s` — materially more wall than CPU. Process spawn, library load and peer-lane contention all sit inside that gap and **this lane did not separate them**; no load reading was taken during either arm's window. The 5.076x overhead figure therefore **bounds** contention from above and does not isolate it. **DISCIPLINE FINDING — AN ID ANNOUNCED IN A BOARD'S FUTURE TENSE IS NOT AN ID RESERVED, AND THE ROW THAT ACTUALLY COMMITS TAKES IT.** cfd's board states at `docs/LAB_STATE.md:1734` and again at `:1981` that this row was *"landing as **C-50**"*. **It never landed.** `C-50` was taken 30 minutes later by cfd's **OWN** F12 rung-1 row (commit `cd1ac21a`, 2026-08-25T01:09:21Z), and at HEAD this ledger contained **zero** rows mentioning this probe, the cavity case or F11 — grepped against the HEAD blob, never the worktree. **This is cfd's SECOND id incident of the day and the first that nobody was watching.** No reconciliation could have named the collision, because the announced row had no id in the file to compare against — precisely the failure this ledger's own 2026-08-23 id amendment was written to make refusable. **The rule that binds, restated because a board sentence is not a reservation: an id is derived from the HEAD blob's MAXIMUM EXISTING NUMBER inside the committing shell invocation (`CLAUDE.md` rule 11), and text written before that invocation reserves nothing.** **No gate, threshold, cap or label moves by anything in this row** | **Run artifacts are ON-DISK ONLY and OUTSIDE GIT**, at `/home/ubuntu/certonomous-runs/f11_c4_probe_2026-08-25/` — `re1000_n32/probe_timing.json` and `armB_re1000_n32/probe_timing.json` (the two wall figures, `rc`, `ranks`); `re1000_n32/log.simpleFoam` and `armB_re1000_n32/log.simpleFoam` (the `ExecutionTime`, `ClockTime`, the `SIMPLE solution converged in 747 iterations` line and the `End` line); `re1000_n32/log.blockMesh`, `re1000_n32/log.checkMesh`, `armB_re1000_n32/log.blockMesh`; and the `postProcessing/` trees carrying the planted control. **Every one of those was opened and read by this lane in this session — none is quoted from the board.** **None is in git and none is recoverable if that directory is removed**; that is stated as a risk, not repaired, since the probe deliberately ran outside every registered path. The frozen referents ARE in git: `verification/campaign/F11_CONVERSION_PREREGISTRATION.md` at HEAD — §7's cost table line 560 (the `Re 1000, n = 32` 4.0 core-s ESTIMATE) and AMENDMENT 1 line 965 (the probe sits outside the §7 cap). The probe's own findings are recorded at `docs/LAB_STATE.md:1730`-`1734`. **Predecessor row named and NOT corrected: `C-50`** (cfd, F12 rung 1, commit `cd1ac21a`) — that row is not wrong; it simply took the id this row had been announced under, which is the finding, not a defect in `C-50`. Rate $0.0513/core-h, c7a.4xlarge, **reported-by-owner**; every dollar figure in this row is **DERIVED, NOT MEASURED** |
| **C-55** | 2026-08-25 | cfd | **F12 three-level mesh audit** (commit `6b462592`, 2026-08-25T01:14:05Z) — `blockMesh` + `checkMesh` built and graded at **coarse 23,040 / medium 92,160 / fine 368,640 cells**, serial, 1 rank, mesh only, **no solver launched**. **Outcome: ADMISSION GATE A GATE FAIL at all three levels, and the breach GROWS under refinement** (max non-orthogonality 70.64625857 / 70.86145203 / 72.54215374 against the frozen threshold of <= 70 degrees; 892 / 3,598 / 14,399 faces over threshold). **AN UNCOSTED SPEND, found by the F12 grading lane and recorded at `verification/campaign/F12_RESULTS.md` §11**, which states in terms that it *"is NOT in `C-50`"* and leaves the calibration-row question to the supervisor; the supervisor dispatched it and this row discharges it. **`C-50` covers F12 rung 1 ONLY** — its 0.4617 core-min is the coarse SOLVE, and its meshing line covers the coarse level alone. **This row does not correct `C-50` and no figure in `C-50` moves** | **1.35000 core-min ESTIMATED, DERIVED FROM THE FROZEN TEXT ON A LIKE-FOR-LIKE BASIS — the reduction is shown in full so a reader can refuse it.** The frozen `F12_PREREGISTRATION.md` §4 (HEAD blob `41ec748a`, lines 321-324) carries a meshing estimate that is for **five** cases, while this audit built **three** levels, so the headline 1.9 core-min is NOT the comparable quantity. **The freeze's own model is linear in cells**: *"`blockMesh` + `checkMesh` took 0.6 s at 3,584 cells in the F2 secondary record (`timings` = 0.3 s + 0.3 s). Scaled linearly in cells across all five cases: 111.9 s = **1.9 core-min ESTIMATED**"*. **Rate = 0.6 / 3,584 = 1.674107e-4 s/cell.** **Denominator, from §4's own rung table (lines 314-318), not invented: 23,040 + 92,160 + 368,640 + 92,160 + 92,160 = 668,160 cells**, and 668,160 x 1.674107e-4 = **111.857 s, which reproduces the freeze's stated 111.9 s** — so the model and its denominator are recovered exactly, not approximated. **The three levels audited are 483,840 cells = 72.414 % of that**, giving **81.0000 s = 1.35000 core-min ESTIMATED = $0.00115425 DERIVED.** **INDEPENDENT CROSS-CHECK that this reduction is defensible: the same rate gives the coarse level 23,040 x 1.674107e-4 = 3.8571 s, and `C-50` — derived separately, by a different lane, without this arithmetic — quotes the frozen coarse meshing estimate as 3.86 s.** **CAP: NONE, and nothing was breached.** §4 line 324 states meshing *"is inside the §5 headroom and is not given its own cap"* | **0.191367 core-min MEASURED** = 11.482 wall-s x 1 rank / 60, from `verification/runs/F12_runs/mesh_audit_2026-08-25/mesh_audit.json`'s own `timings` block, **read per component rather than as a total**: coarse `dict_write_s` 1.563 + `blockMesh_s` 0.465 + `checkMesh_s` 0.465 = 2.493 s; medium 0.009 + 0.916 + 1.166 = 2.091 s; fine 0.008 + 2.819 + 4.071 = 6.898 s. **= $0.00016362 DERIVED, NOT MEASURED**, at $0.0513/core-h, c7a.4xlarge, **reported-by-owner** — the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` §5). **THE SPEND SPLITS, AND THE SPLIT MATTERS BECAUSE THE FROZEN ESTIMATE PRICES ONLY ONE HALF OF IT: `blockMesh` + `checkMesh` = 9.902 s = 0.165033 core-min, which is what §4's estimate is FOR; `dict_write` = 1.580 s = 0.026333 core-min, which §4 prices at NOTHING.** The dictionary-write term is **1.563 s of first-invocation Python import and 0.017 s of actual work** across the other two levels (0.009 s and 0.008 s), so it is a per-process startup cost misnamed by the field it is stored in, and it is **named separately below rather than folded into the ratio** | **= gross, 0.191367 core-min.** The longest single component on this item is **4.071 s (the fine `checkMesh`), 884x below** the 3600-s stall rule (`COMPUTE_BUDGET_CHARTER.md` §2), so stall cleaning matches nothing and gross and cleaned coincide. **The dict-write term is NOT netted out of either column** — it was really spent — it is only held out of the ratio's numerator, where including it would compare a measured quantity against an estimate that does not price it | **0.1222x LIKE-FOR-LIKE — the frozen model OVER-PREDICTED meshing by 8.18x.** Numerator **9.902 s = 0.165033 core-min** (`blockMesh` + `checkMesh` only, the scope §4's estimate covers); denominator **81.0000 s = 1.35000 core-min** (the three built levels' share of §4's five-case term, derived above). **Stated for completeness and NOT used as the headline: comparing the full 11.482 s gives 0.1418x, but that numerator contains 1.580 s the estimate never priced, so it is not a like-for-like ratio and this row does not report it as one.** **The per-level ratios are the calibration, and they are not constant — which is the finding: coarse 0.930 s measured vs 3.8571 s = 0.2411x (over-predicted 4.15x); medium 2.082 vs 15.4286 = 0.1349x (7.41x); fine 6.890 vs 61.7143 = 0.1116x (8.96x).** **The error grows monotonically with mesh size, so the miss is in the model's EXPONENT and not only in its constant** | **MISPREDICTION — AND IT IS STRUCTURAL, NOT A BAD CONSTANT. This is the row's transferable content.** §4 scales meshing **linearly in cells** from a single 3,584-cell reference. **The measured per-cell rate is not constant and falls monotonically: 4.0365e-5 (coarse) -> 2.2591e-5 (medium) -> 1.8690e-5 s/cell (fine), a 2.16x fall across a 16x cell range.** **Fitted across the audited range, measured meshing scales as N^0.7223, not N^1.0** (ln(6.890 / 0.930) / ln(16)). **Two errors therefore COMPOUND under extrapolation: the reference constant is 4.15x too high at 23,040 cells, and the exponent is ~0.28 too steep, so the over-prediction widens from 4.15x to 8.96x over just 16x in cells.** **The cause is named, and it is a hypothesis this row does not claim to have proved: the F2 3,584-cell reference (0.3 s + 0.3 s, quantised at 0.1 s) is small enough that fixed process cost plausibly dominates it, and a fixed-cost-dominated point extrapolated linearly over 100x in cells must over-predict. This lane did not separate fixed from marginal cost and does not assert it has.** **RECOMMENDED REPLACEMENT for the next F12 estimate, offered as a measured fit and labelled as such: t ~ 0.930 s x (N / 23,040)^0.72 for `blockMesh` + `checkMesh` on this family, plus a per-process startup term of order 1.5 s that §4 currently prices at zero.** **WASTE: ZERO core-min, named separately and netted off neither column nor the ratio (charter §6).** All three levels returned the finding they were built for, and the finding is a gate failure that **stops rungs 2-5 from being worth launching** — the audit cost 0.191 core-min and protected the 320.6 core-min of Basis-A medium-and-fine solve budget behind an inadmissible mesh. **NAMED SEPARATELY SO A READER MAY DISAGREE, because it is the one component with a duplication argument against it: the coarse level (0.930 core-s of the 9.902) re-builds a mesh `C-50` had already built.** **This row does NOT count it as waste**, on the ground that it independently reproduced `C-50`'s max non-orthogonality of **70.64625857 to eight decimal places on a separately built mesh, with the same 892-face count** — a replication of the gate-A failure, not a repeat of it. **The figure is stated so a reader who rejects that argument can net it off; this row does not net it off.** **A MEASUREMENT-SCOPE DISCREPANCY WITH `C-50`, DISCLOSED AND NOT RECONCILED AWAY: for the same two programs on the same coarse mesh, this audit's driver stopwatch reads 0.465 + 0.465 = 0.930 s while `C-50` reads 0.771 s from the log files' own birth and mtime timestamps — a 1.206x scope difference, because a stopwatch around a subprocess includes spawn and library load that a log file's own timestamps do not.** Both are measured; **neither is wrong; they measure different things and a future estimate must say which it is predicting.** (`C-50`'s own component split reads 0.351 + 0.407 = 0.758 s against its stated 0.771 s headline — a 0.013 s internal difference this row notes, does not resolve, and does not need to.) **CONTENTION: NOT CHARACTERISED, and NOT assumed to be zero.** No load reading was taken during the audit's 11.5 s window and none is recoverable now. Since the measured rates came in **below** the estimate at every level, any contention penalty is bounded above by the gap itself and is not separately quantified. **NO GATE, THRESHOLD, CAP OR LABEL MOVES BY THIS ROW** | `verification/runs/F12_runs/mesh_audit_2026-08-25/mesh_audit.json` (**in git**; committed `6b462592`) — the `timings` block this row's every actual is read from, per component, and the `levels` block carrying `cells_built` 23,040 / 92,160 / 368,640 and the three `quality` results. **Read by this lane from the artifact in this session, not quoted from any report.** `verification/campaign/F12_RESULTS.md` §11 (at HEAD) — where the 0.191 core-min spend was found and flagged as absent from `C-50`. `verification/campaign/F12_PREREGISTRATION.md`, **HEAD blob `41ec748a06b513414101dca9780107f08a25ddec` (v1.2)** — §4 lines 314-318 (the five-rung cell counts that give the 668,160-cell denominator) and lines 321-324 (the 0.6 s at 3,584 cells reference, the 111.9 s five-case term, and the statement that meshing has no cap of its own). Commit `6b462592`'s message carries the three levels' gate-A results. **Predecessor row named and NOT corrected: `C-50`** (cfd, F12 rung 1, commit `cd1ac21a`) — its coarse-meshing 0.200x finding is **extended, not overturned**, by the two levels it never measured, and the scope difference between its timing method and this one is disclosed in the attribution cell. Rate $0.0513/core-h, c7a.4xlarge, **reported-by-owner**; every dollar figure in this row is **DERIVED, NOT MEASURED** |
| **C-56** | 2026-08-25 | ansys-verification | **VMFL045 run 1 — Oblique Shock Over an Inclined Ramp (VM2026R1 pp. 153–154), the team's first shock-capturing case; `rhoCentralFoam`, serial. The run CRASHED on its first timestep** with `FOAM FATAL IO ERROR: Entry 'e' not found in dictionary system/fvSolution/solvers` (a frozen-`fvSolution` `e`/`h` defect inherited from the inviscid VMFL051; VMFL045 is viscous so the implicit energy corrector runs). Verdict `NOT A RESULT`, tier `NOT HELD`. Repaired as the VMFL045-R2 rung | **20.4 core-min** point estimate (cap 48; §9.1 of the frozen pre-registration, blob `7a7f9d52`, freeze `6a9701e1`) = $0.01745 derived | **0.0000 core-min** — crash at `wall = 0 s` before any real solve (`L1_90x76/RUN_RC.txt` `core_min=0.0000`; only `blockMesh`/`checkMesh`/`topoSet` ran, all fast, then `rhoCentralFoam` aborted on step 1). $0.00 derived | **= gross, 0.0000 core-min** — no solver work ran, so the 3600-s stall rule matches nothing and there is nothing to clean out | **UNDEFINED — the run never executed.** Deliberately NOT written as `0.0×`: an interruption is not a calibration (this team's C-10 / C-15 pattern, L-265). There is no throughput to compare the 20.4 core-min estimate against, because no solver step completed | **Not a misprediction — the prediction was never tested.** The run died on a **setup defect in the frozen inputs**, not on any cost phenomenon: `energy sensibleInternalEnergy` needs an `e` solver entry that the frozen `fvSolution` (cloned from inviscid VMFL051) lacked, and the viscous (μ=1e-8) energy corrector exercised that path. **Waste 0.000 core-min** — the crash IS the finding and it was free; its fatal log is the evidence the finding rests on (cf. cfd C-4). **Contention not applicable** — no real compute ran; box load ~2.1–2.9 on 16 cores throughout, far below VMFL051's 68–76. **Calibration lesson: a comparator `--selftest` (45/45 here) proves the GRADER, not the CASE — the R2 launcher adds a pre-flight solver smoke test in scratch so a dictionary defect fails in seconds before any graded compute** | `cases/ansys_verification/VMFL045/RESULTS.md` §4; register row #5; run tree `verification/runs/ansys_verification/VMFL045/L1_90x76/` (crashed, preserved); first lane's `LANE_REPORT.md` (`f1f58fd`); repair rung `cases/ansys_verification/VMFL045/R2/` |
| **C-57** | 2026-08-25 | ansys-verification | **VMFL003 run 1 — Pressure Drop in Turbulent Flow Through a Pipe (VM2026R1 p. 19), the team's first TURBULENT case; `simpleFoam` + `kEpsilon` + `nutkWallFunction`, serial, six meshes (three-level axial Roache triple at fixed N_r = 5, plus a four-mesh wall-treatment ladder). Verdict `NOT A RESULT` (all three levels failed the frozen residual leg; L3 on ε alone). Ran to completion, rc=0, nothing discarded** | **9.6 core-min point estimate, 24 core-min cap**, both frozen at `9195d25e` before compute; basis VMFL005's measured 2.18e−06 s/(cell·iter) × a **1.6 k-ε multiplier declared in the prereg as an ESTIMATE and as the budget's largest uncertainty**; 1.395e8 cell-iterations | **13.5 core-min** = 810 s wall × 1 rank, from the six `RUN_RC.txt` files and `COST.txt`; **$0.011543 DERIVED** at $0.0513/core-h (owner-stated, **not measured** — the box cannot read its own billing) | = gross. Longest single invocation 343 s, an order below the 3600 s stall rule, so cleaning removes nothing and **gross = cleaned**. **Waste: NONE, named explicitly** — no level re-run, nothing discarded; the aborted first launch died at the pre-flight smoke test in `/tmp` **before any level directory existed** and consumed no budget | **1.406× actual/predicted** (13.5 / 9.6); **0.56× of cap** — cap NOT exceeded. Solver-only leg **1.660×** (810 s vs 488 s) | **MISPREDICTION, essentially zero contention — and the contention leg is MEASURED, not inferred.** wall/ExecutionTime was **0.9985–1.0055 at every level** (`CONTENTION.txt`), i.e. uncontended despite concurrent VMFL045-R2 and two heat-transfer solvers; loadavg 2.77–4.06 on 16 cores across 82 live 10 s samples from an OS-level detached sampler. **The controlled comparison this settles:** the same ratio on VMFL051 — the 2.06× overrun attributed to contention on a loadavg argument — is **1.1673 / 3.4901 / 2.5305**. Against ~1.00 here, **VMFL051's overrun was contention, not misprediction**, and the evidence is now a ratio measured in both runs rather than an inference from ambient load. **The gap here is the k-ε multiplier: measured aggregate 5.806e−06 s/(cell·iter) implies 2.66, not 1.6 — under-predicted 1.66×. REPORTED AS CONFOUNDED AND NOT BANKED:** per-level rates scatter 4.1× (1.427e−05 … 3.500e−06) with no monotone trend in cell count and per-iteration cost 7.0–28.6 ms, so fixed per-iteration overhead — not cells — dominates at 1250–5000 cells. The defensible reusable figure is the **asymptotic L3 rate 5.72e−06 s/(cell·iter) at 5000 cells**, with the warning that **below ~5000 cells a per-cell-iteration rate is not a cost predictor here** | `cases/ansys_verification/VMFL003/RESULTS.md` §8; `verification/runs/ansys_verification/VMFL003/{COST.txt,CONTENTION.txt}` |
| C-58 | 2026-08-25 | ansys-verification | VMFL045-R2 (oblique shock over an inclined ramp) — three-level `rhoCentralFoam` re-run of run 1 on the `h`→`"(h|e)"` fvSolution fix; frozen comparator prints **`PASS`** (L3 M₂ 1.8747790411, dev +0.041571 % vs manual 1.874, band 1 %), but observed order **p = 3.3862** surfaced as the pre-declared suspicious condition and the **tier held for the supervisor** | **20.4 core-min** point estimate (cap 48) — `cases/ansys_verification/VMFL045/R2/PREREGISTRATION.md` §5, frozen blob `592e872b` = **$0.01744 derived** | **26.6667 core-min MEASURED** — L1 0.3667 + L2 2.6667 + L3 23.6333, total wall 1600 s, serial (RANKS=1), from each level's `RUN_RC.txt` and the run-root `COST.txt` (02:54:10Z) = **$0.02280 derived** at $0.0513/core-h (reported-by-owner, not measured) | **= gross.** Longest wall is L3 at 1418 s, under the 3600-s stall rule (`COMPUTE_BUDGET_CHARTER.md` §2), so there is no stall to separate and gross = cleaned | **1.307×** cleaned/predicted (26.6667 / 20.4); **0.556× of cap** | **Under-prediction of L3's cost; ZERO contention penalty; ZERO waste.** The point estimate under-priced the 109 440-cell fine level. A peer ansys-verification job (**VMFL003**) ran concurrently for the whole solve at box load **~3–4 on 16 cores** (LIGHT; contention is named per the rule that a calibration stating no co-tenant is not a calibration) — but a **serial** job at load well under the core count is not core-starved, so contention is **not** the cause and is not charged; the gap is misprediction alone. **Waste 0**: no re-run and no crash on R2 (run 1's crash is costed in its own record, not here) | `cases/ansys_verification/VMFL045/R2/RESULTS.md` §7 (blob `77f0a271`, commit `913ff5ce`); `COST.txt` in the run tree; pre-registration frozen `592e872b` |
| C-59 | 2026-08-25 | cfd | **F6a / C-15 attempt 2 — NASA wall-mounted hump vs Greenblatt AIAA-2004-2220 Table 2; `simpleFoam` + stock `kOmegaSST`, `mpirun -np 4`, shipped 51,626-cell mesh. Converged on `residualControl` at 1813 iterations; row `GATE FAIL` as forecast.** | **13.74 core-min** point estimate registered in §8.2 of the frozen pre-registration = **6.87 uncontended subtotal + 6.87 explicit contention allowance (×1.0)**; **cap 30 core-min** | **6.1333 core-min** — solver ClockTime 85 s × 4 ranks = **5.6667**, pre/post phases (checkMesh, case copy, smoke test, `decomposePar`, `reconstructPar`) 28 wall s × 1 rank = **0.4667** | **6.1333 core-min — gross == cleaned.** No stall: 85 s is nowhere near the 3600 wall s threshold. **Waste is named SEPARATELY and is NOT absorbed into this ratio:** attempt 1 spent **≤ 0.017 core-min (BOUNDED, not measured)** on a `checkMesh` + case copy + aborted smoke test and produced **no graded quantity** | **0.45×** (6.1333 / 13.74) | **THE HEADLINE RATIO IS ALMOST ENTIRELY THE CONTENTION ALLOWANCE THAT WAS BUDGETED AND NOT SPENT, and pooling it with a misprediction would be wrong.** Against the **uncontended subtotal** the prediction was **0.89×** — good to 11 %. The ×1.0 contention allowance was drawn from C-51, which measured contention at **62 % of an entire item's spend at loadavg 68.38 on `nproc` 16**; this run launched at **loadavg 2.01** with one other team's solver live, and contention **did not materialise at all**. **THE REUSABLE FINDING: the contention term is REAL but BIMODAL — near-zero on a quiet box, dominant on a loaded one — so a flat ×1.0 allowance mis-predicts in BOTH directions and a future estimate should read `loadavg` at launch and say which regime it is pricing.** **Two component figures, both better than the headline suggests:** (1) the **identical-configuration analog was excellent** — C-45's SST leg measured 83.68 wall s and this run measured **84.81 s ExecutionTime on the same case, mesh, solver and rank count, +1.35 %**; (2) but the **×1.15 uplift registered in §8.2 term 3 for the (P-c) functional-sampling `functionObject` was a 10× OVER-ESTIMATE** — 36 extra wall-sample writes cost **~1.4 %, not 15 %**. **REUSABLE: price a surface-sampling `functionObject` on a 51,626-cell 2-D case at ~1.5 %, not 15 %.** **No predecessor row is corrected by this one.** | `verification/runs/F6a_GREENBLATT_runs/attempt2_Re936k/result.json` and `grading.json`; frozen pre-registration §8, `5b6b1ece` |
| C-60 | 2026-08-25 | cfd | **F6a / C-15 attempt 3 — re-run of the same hump case SOLELY TO MEASURE THE `rc` LIMB the supervisor refused to let attempt 2 infer. The rc repair WORKED (all five rule-4 limbs measured), but the run REACHED ITS 2000-ITERATION CAP without `residualControl` tripping, so (P-a) refuses and the row is `NOT A RESULT`.** | **13.74 core-min** point estimate (§8.2), **cap 30 core-min CUMULATIVE across attempts** | **7.2462 core-min** — solver 101.5931 wall s × 4 ranks = **6.7729**, pre/post phases 28.4 wall s × 1 rank = **0.4733** | **7.2462 core-min — gross == cleaned.** No stall. **Waste named separately and NOT absorbed:** attempt 1 **≤ 0.017 core-min (bounded)**, attempt 2 **6.1333 core-min** — attempt 2 produced a complete solve whose row was voided on an unmeasured limb, so **its whole spend is waste attributable to a launcher defect, not to the case.** **CAMPAIGN CUMULATIVE 13.3965 core-min of the 30 cap; 16.6035 remain.** | **0.53×** (7.2462 / 13.74) | **THE COST STORY IS UNCHANGED FROM C-59 AND THE CONTENTION FINDING REPRODUCED: loadavg 2.02 at launch, contention again did not materialise, and against the uncontended subtotal of 6.87 the prediction was 1.05× — good to 5 %.** Two rows now agree that the ×1.0 contention allowance is the whole of the headline miss, which strengthens C-59's REUSABLE FINDING: **the contention term is real but BIMODAL and must be conditioned on `loadavg` read at launch, not applied blind.** **AND THE ROW'S REAL CONTENT IS A REPRODUCIBILITY FINDING, NOT A COST ONE:** attempt 3 ran **101.59 wall s to 2000 iterations** against attempt 2's **85 s to 1813** — +10.3 % iterations for +19.5 % wall — because **`scotch` decomposition is NOT DETERMINISTIC between invocations.** On a **byte-identical** mesh, `0/` and `controlDict`, attempt 2 partitioned to 12777/12906/12965 and attempt 3 to 12974/12870/12865; the different summation order changes round-off, and **convergence on this case is marginal on the `omega` 1e-10 criterion alone** — attempt 2 met it at **9.94318e-11, a 0.6 % margin**, while attempt 3 stood at **4.63816e-10** at the cap with every other channel comfortably under 5e-7. **REUSABLE: a parallel OpenFOAM case whose `residualControl` margin is a fraction of a percent is NOT reproducibly convergent under `scotch`, and any campaign gating on "residualControl tripped" must either fix the decomposition or register a criterion with real margin.** **THE PHYSICS REPRODUCED REGARDLESS:** reattachment 1.2534333 → 1.2534550, a difference of **2.2e-5 in x/c**. | `verification/runs/F6a_GREENBLATT_runs/attempt3_Re936k/result.json` and `grading.json`; frozen pre-registration §8 and ADDENDUM 4 |
| C-61 | 2026-08-25 | heat-transfer | **T1b L4 x-arm — RUNG-LEVEL ROW AT COMPLETION.** All four x cases (`R_10k_x`, `R_30k_x`, `R_100k_x`, `R_300k_x`) closed and the pool graded 2026-08-25: **0 graded rows, 4 NOT A RESULT, 0 PASS, 0 GATE FAIL** (`gate_t1b_L4.json`, blob `8506c3c0`, verified byte-identical on disk and at HEAD). **Discharges the deferral registered in C-48**, which priced only the two cases closed at that time and recorded the rung row as owed at completion *"citing this id"*. **Supersedes nothing:** C-48 stands as the partial row it declared itself to be, and this row does not re-cost its two cases — it cites its measured figure. | **12 326.67 core-min = 205.444 core-h**, the **registered** schedule 20000 / 80000 / 80000 / 80000 at `T1b_L4_AMENDMENT.md` §5 **line 290**. The §5 table's own printed totals read **205.4 core-h / $10.54** (the sum of four *rounded* per-case core-hour cells); its predicted-**wall** column sums to **739 600 s** = 205.4444 core-h = **12 326.67 core-min**, and that is the figure divided into below. *Recorded disagreement, not a correction:* C-48 and this row's brief carry **12 326.4 core-min** (205.44 x 60) — a **0.27 core-min / 0.002 %** difference that moves no ratio digit. Frozen blob `ad7208b5`, commit `17209b50`, 2026-08-21T18:14:22Z; **re-verified today — the blob at HEAD is still `ad7208b5`, byte-identical to the freeze** — and frozen **3 h 12 min 48 s before first launch** (`R_300k_x/0/T`, 2026-08-21T21:27:10Z), which is C-48's "3 h 13 min" re-derived. | **16 192.45 core-min = 269.874 core-h = $13.8445 DERIVED, NOT MEASURED** ($0.0513/core-h, c7a.4xlarge, **reported-by-owner** — the box cannot read its own billing, `COMPUTE_BUDGET_CHARTER.md` §5). Wall x ranks / 60, **single rank on all four** (`nProcs = 1`, §5): `R_10k_x` 103 303 s + `R_300k_x` 254 632 s = **5 965.58 core-min** (**C-48's measured figure, cited not re-derived**; both `STATUS` walls re-read today and they agree to the second) + `R_100k_x` **294 910 s = 4 915.17 core-min** + `R_30k_x` **318 702 s = 5 311.70 core-min**, from `STATUS.R_100k_x` and `STATUS.R_30k_x`, `rc=0` on both. | **16 192.45 core-min — GROSS EQUALS CLEANED, and for the two new cases that is MEASURED, not argued**, as it was for the two old. Last `ExecutionTime` / wall = **294 721.36 / 294 910 = 0.99936** and **318 504.13 / 318 702 = 0.99938**: only **189 s and 198 s out of 613 612 wall s** fell outside solver CPU. No stall-rule row applies — charter §2's 3600-s rule matches a *row*, and these are whole-case walls of 81.9 and 88.5 core-h, not ledger rows. **Waste nil (0.00 core-min) — CHECKED, NOT ASSUMED:** exactly one `log.solve` per case, each with one `Create mesh`, one `Starting time loop`, one `End` and `Time = 1` ... `Time = 80000` across **80 000** `Time` lines — **one contiguous time loop, no restart, no second segment**; **no `.attempt*` sibling** (the only `.attempt` name in `T1_runs/` is `P_10k.attempt1_stale`, a different case); **no crash marker** — the single `FOAM FATAL`/`caught signal`/`Floating point` grep hit in each log is the `trapFpe: Floating point exception trapping enabled (FOAM_SIGFPE)` banner at **line 18**, inspected and not a fault; **`rc=0`**; two time dirs (`78000`, `80000`) as `purgeWrite 2` prescribes; **age guard holds** — `80000/T` (2026-08-25) is newer than `0/T` (2026-08-21) on both. | **1.3136x** (16 192.45 / 12 326.67; 1.3136x also against the brief's 12 326.4). Per case against §5's own predicted walls: `R_10k_x` **1.7594x**, `R_300k_x` **1.1982x** (both C-48), `R_100k_x` **1.2980x** (294 910 / 227 200), `R_30k_x` **1.3213x** (318 702 / 241 200). **C-48's PARTIAL ratio was 1.3197x — the whole-rung figure is consistent with it**, marginally lower because the two long cases miss less than the two-case partial did. | **MISPREDICTION dominant on both new cases. C-48's MECHANISM EXTENDS; ITS MAGNITUDE DOES NOT — and that was measured here, not inferred from the shape.** **(a) Same mesh, same borrowed rate.** Both new cases run the same **209 920-cell** x mesh priced on rates §5 measured on an **81 920-cell** mesh (a 2.56x jump). Whole-run mean throughput is **56 945 cell-it/s** (`R_100k_x`) and **52 694** (`R_30k_x`) against **73 925** and **69 621** assumed = **0.770x and 0.757x**, and the rate ratio reproduces the wall ratio to four figures (1/1.2980 = 0.7704; 1/1.3213 = 0.7569): **as in C-48, the whole of both misses is the rate.** **(b) But both miss far LESS than `R_10k_x` did, and the reason is measured on these cases' own logs.** They START where C-48's cases started — segment throughput from each case's own `ExecutionTime` ladder over 0-10k / 10-20k / 20-40k / 40-60k / 60-80k iterations is **43 055 / 39 891 / 56 448 / 68 818 / 72 438** cell-it/s (`R_100k_x`) and **42 354 / 39 347 / 53 376 / 59 743 / 62 984** (`R_30k_x`), against C-48's early 40 643 / 41 337 — and then they **earn C-48's mechanism (b), the roughly 2x speed-up as a steady SIMPLE case converges, over 60 000 of their 80 000 iterations.** `R_10k_x` stopped at 20 000 and never earned it (1.759x); these two ran the full schedule and land at 1.298x and 1.321x. **The rung's per-case ratios order by ITERATION COUNT, not by Reynolds number: 1.198x (80k) / 1.298x (80k) / 1.321x (80k) / 1.759x (20k).** **(c) Explicitly NOT carried across by shape.** C-48 records that transplanting C-23's mechanism on a matching *pattern* would have HIDDEN the real cause; the same discipline is applied here. `ExecutionTime`/wall was **re-measured** on the two new cases (0.99936 / 0.99938 — so C-23's fixed startup/IO term does not exist here either, exactly as C-48 found), the contiguity and waste checks were **re-run** rather than inherited, and the segment ladders above are **these cases' own numbers**. **(d) One honest gap: contention was NOT separately re-measured for these two walls.** C-48's `/var/log/sysstat/` finding — contention real and measured at **5.5-11.3 % of each miss**, not the prime suspect — is quoted **as C-48's finding on C-48's cases** and is **not extended** to `R_100k_x` and `R_30k_x`. Stated absent, not approximated. **(e) The overnight PROJECTION landed, and it is worth much less than it looks.** ~**270.05 core-h** projected against **269.874 core-h** measured = a **0.065 %** miss. But that projection was made from a throughput rate **measured late in the run, on the very cases it predicted**. It calibrates arithmetic, not forecasting. **The thing this ledger exists to score — the estimate registered before any x case existed — missed by 31.4 %.** **(f) No cap; the absence is the defect, not the overrun.** C-48's finding stands: the words *cap*, *ceiling*, *stop*, *overrun* and *budget* do not occur in the frozen amendment, so §5 registered a **prediction**, a prediction is not a cap, and rule 12's *"an overrun stops the run"* is not engaged. **$13.84 derived sits inside the $25 pre-authorisation.** **(g) The grading run is deliberately NOT pooled into this row — see C-62.** | `verification/runs/T-family/T1_runs/STATUS.R_100k_x`, `STATUS.R_30k_x`, `R_100k_x/log.solve`, `R_30k_x/log.solve`, `gate_t1b_L4.json` (blob `8506c3c0`), `L4_GRADE_REPORT_2026-08-25.md`; `docs/campaigns/T-family/T1b_L4_AMENDMENT.md` §5 line 290 (blob `ad7208b5`, commit `17209b50`); predecessors **C-48** (partial, deferring this row) and **C-23** |
| C-62 | 2026-08-25 | heat-transfer | **T1b L4 POOL GRADING — the INSTRUMENT run, recorded as its OWN row and deliberately NOT pooled with C-61's solver cost.** `analyse_t1b_L4.py` over the 16-case pool, writing `gate_t1b_L4.json` (0 graded rows, 4 NOT A RESULT). Separated on the **C-53** precedent and this team's standing ruling that **an instrument's ceiling and a solver's misprediction are different calibration subjects**: averaging a 1.46 core-min reader into a 16 192 core-min solver arm destroys both signals — the reader's cost vanishes into rounding (0.009 % of the rung) and the solver's ratio does not move a digit. | **Absent — stated absent, not approximated.** §5 of the frozen amendment prices **solver wall only**; the grading step carries **no registered estimate and no cap** anywhere in the amendment. | **1.459 core-min** = 87.565 wall s x 1 rank / 60 = **$0.00125 DERIVED, NOT MEASURED** (same rate and same reported-by-owner basis as C-61) | = gross. One reader process, one invocation, no stall-rule row, **zero waste**. | **No ratio — there is no registered prediction to divide by**, and none is invented. Recorded so the next T-family grading step HAS a basis: **~1.5 core-min to grade a 16-case, four-Reynolds pool.** | **The ABSENT estimate is itself the calibration finding**, and it is a gap in what §5 was asked to price rather than a defect of the run: the rung registered a cost for the solvers and none for the reader that turns them into a verdict. No overrun is possible against an unregistered step, which is the same structural hole C-61 (f) records for the solver arm at rung level. | `verification/runs/T-family/T1_runs/L4_GRADE_REPORT_2026-08-25.md` lines 377/379 (*"Wall seconds 87.565"*, *"87.565 x 1 / 60 = 1.459"*); `gate_t1b_L4.json`; precedent **C-53**; companion solver row **C-61** |
| C-63 | 2026-08-25 | cfd | **F12 attempt 2 MESH PHASE — the replacement RAE 2822 ladder built and admission gate A graded at all three levels. MESH ONLY; no solver was launched and the builder contains no call to one.** Gate A **`PASS` / `PASS` / `PASS`** at **51.1237 / 51.5250 / 51.9261 deg** against the frozen `<= 70`, **zero faces over 70 at every level**, skewness 0.9572 against the frozen `<= 4`; cells exactly 23,040 / 92,160 / 368,640. Attempt 1, same reader, same day: 70.646 / 70.861 / 72.542 deg with 892 / 3,598 / 14,399 faces over. | **<= 5 core-min** at ranks = 1, registered in `ATTEMPT2_MESH_REGISTRATION.md` section 6 BEFORE the build, inside the frozen section 4 meshing allowance; **no new cap created and none requested** | **0.4015 core-min** — 24.092 wall s x 1 rank / 60, from the builder's own monotonic clock, recorded in `gate_a_attempt2.json` | **0.4015 core-min — gross == cleaned.** No stall (24 s against the 3600-s rule). **Waste named separately and NOT absorbed: 0.00 core-min in the recorded build** — no rebuild, no stall, no re-run. **Exploratory compute named separately and deliberately NOT folded into the ratio: approximately 1.6 core-min** of scratchpad design search and mechanism diagnosis (~20 `blockMesh`/`checkMesh` pairs plus two runs of an exact non-orthogonality calculator). It bought the mechanism, two predictors validated against known non-zeros, and the five-point wake-anchor sweep, all of which are recorded — it is disclosed, not hidden inside the 0.4015. | **0.080x** (0.4015 / 5) | **THE ROW'S REAL CONTENT IS A THIRD INDEPENDENT CONFIRMATION OF THIS TEAM'S N^0.72 MESHING FIT, AND IT IS NOW CONFIRMED ACROSS A 16x CELL-COUNT RANGE ON A DIFFERENT TOPOLOGY.** `blockMesh` + `checkMesh` predicted by `t = 0.930 s x (N/23,040)^0.72` against measured: coarse 0.930 / 0.930 s (1.000x), medium 2.523 / 2.132 s (0.845x), fine 6.846 / 7.191 s (1.050x) — the fit holds to **-15 % / +5 %**. The frozen section 4's LINEAR model would have said 0.93 / 3.72 / 14.88 s, **over-predicting the fine level by 2.07x**, and its miss again worsens monotonically with level. **The headline 0.080x ratio is NOT a misprediction finding**: the <= 5 core-min was a registered ceiling for a phase whose per-utility costs were separately predicted to within 15 %, so the gap is ceiling headroom, not estimate error, and it is attributed as such rather than being allowed to flatter the ledger. **loadavg at launch 5.33 / 4.79 / 2.87** and contention did not materialise — a third row consistent with C-59's finding that the contention term is real but BIMODAL and must be conditioned on `loadavg` read at launch. **Dollars $0.000343, DERIVED at $0.0513/core-h and reported-by-owner, NEVER measured** (`COMPUTE_BUDGET_CHARTER.md` section 5). | `verification/runs/F12_runs/mesh_ladder_attempt2_2026-08-25/RESULTS_GATE_A.md` section 10, `gate_a_attempt2.json`, `birth_certificates.json`; builder committed at `117d8adc` BEFORE the build and hashed against its own HEAD blob at run time |
| C-64 | 2026-08-25 | cfd | **F11 lid-driven cavity CONVERSION — six `simpleFoam` laminar solves (Re 100 / Re 1000 x n=32/64/128) re-run under the frozen conversion pre-registration `53298a45`. All six rc=0 MEASURED (integer on disk via the `setsid bash -c 'CMD; echo $? > RC.txt; sync'` carry-forward — NO PROXY). Grader `grade_f11.py` blob `01aa54d7` == its HEAD blob, selftest 98/98 with 32 mutation controls, 54 planted-zero controls all PASSED. VERDICT: all six gate rows `NOT A RESULT` — every band verdict was PASS, and the Roache/plateau gate converted them, which is rule 5 working in the only direction it may.** | **8.0200 core-min** point estimate registered in §7 of the frozen prereg; **HARD CAP 13.0** | **6.0835 core-min** — launcher ledger `F11_CONVERSION_RUN_LEDGER.json`, `cap_respected: true`, `stopped_by: null`. The grader separately reports **5.8227 core-min** as the six-solve subtotal; the 0.2608 difference is launcher overhead outside the timed levels. Gross figure used here is the ledger total. | **6.0835 core-min — cleaned == gross.** No row approaches the 3600-s stall rule: the longest single solve was `re100/fine` at ~270 core-s against a 674-s per-run cap | **0.759** (cleaned/predicted). Against the cap: **0.468** | **Misprediction, over-prediction, dominant and essentially the whole gap** — the §7 estimate exceeded actual on 5 of 6 levels (`re1000/fine` 150.0 core-s vs 176.73 predicted; `re100/fine` the largest single contributor). **Contention: BOUNDED, NOT MEASURED** — loadavg was 1.29 at launch and 6.37 at the terminal sample, rising because F5b and F3 ran concurrently on the same 16-core box; no per-level contention decomposition exists, so contention cannot be separated from misprediction here and is not claimed to be. **WASTE: ZERO, and named separately rather than absorbed** — no run was killed, no per-run wall cap fired, no relaunch, no misconfiguration, `stopped_by: null`. **Dollars: 6.0835 core-min = 0.101392 core-h x $0.0513/core-h = $0.0052 — DERIVED, NOT MEASURED** (the box cannot read its own billing, COMPUTE_BUDGET §5) | `verification/runs/F11_runs/conversion_2026-08-25/F11_CONVERSION_GRADE.json`, `F11_CONVERSION_RUN_LEDGER.json`; prereg frozen at `53298a45` |
| C-65 | 2026-08-25 | cfd | **F5b Physics rung `physics_p1` — pitching NACA 0012 dynamic stall; `pimpleFoam` + `kOmegaSST` + `oscillatingRotatingMotion`, 3,584-cell coarse mesh, SERIAL (1 rank), one full pitching period to `endTime` 21.9440. Frozen prereg `c1ba1845`/`a80d5f36`; reader blob `6c6d34d0` hashed against its committed blob BEFORE grading and run UNMODIFIED. VERDICT: `NOT A RESULT` — completion clauses 4 (fields present) and 6 (age guard) FAIL on ONE frozen literal: the reader looks for `case/21.9440/` and OpenFOAM wrote `case/21.944/`, stripping the trailing zero. The gate was NOT evaluated and no `A_L` is quoted.** | **35.0 core-min** point estimate (§8); band **28.8–48.0**; **cap 72.0** | **39.426 core-min** — MEASURED: `ExecutionTime` 2365.55 s × 1 rank ÷ 60, read from `log.pimpleFoam`. Wall 2,372 s; `record.json wall_seconds` 2367.3. **54.8 pct of cap, NOT breached** | **39.426 core-min — cleaned == gross.** The single solver row is 2,367 s, **UNDER** the 3600-s stall rule, so the rule does not match and this is a legitimate long serial solve — stated explicitly rather than letting the rule apply itself (§8 close-out clause 3) | **1.127** cleaned/predicted — **INSIDE the registered band 28.8–48.0**. Against the cap: **0.548** | **MISPREDICTION: small, and in the direction §8 itself predicted** — it labelled basis A a lower bound because Δt is Courant-limited and falls once the flow separates, which is what happened. **CONTENTION: SELF-INFLICTED, NAMED SEPARATELY, AND IT MOVES THE RATIO IN NEITHER DIRECTION** — F5b launched sole-job at loadavg 0.24 and, at 64 pct complete, THIS LANE launched F11 and F3 onto the same 16-core box; terminal loadavg 9.69. Load-conditioned basis **RECONSTRUCTED, NOT MEASURED**, from this run's own `WATCH_LOG.txt` (127 samples at 15 s; `ExecutionTime` differenced between consecutive samples; each interval attributed to the load reading that opened it; boundaries ±15 s): quiet <2.0 **739.3 core-s (38.8 pct)**, mid 2.0–6.0 **561.1 core-s (29.5 pct)**, contended ≥6.0 **603.6 core-s (31.7 pct)**; ET-weighted mean load **4.13**. Samples cover **1,904.0 of 2,365.55 core-s (80.5 pct)**; the uncovered 461.55 core-s is the pre-sampler window, bracketed by two measured readings (0.24 at launch, 1.04 at first sample), **bounded as quiet but NOT sampled**, and NOT added to the quiet segment. **§8 ITEM A-4's DELIVERABLE — A CLEAN MEASURED BASIS FOR THIS FAMILY — IS NOT DELIVERED**; a future lane must not re-use 39.426 core-min as an uncontended basis. **WASTE: ZERO** — no kill, no timeout, no relaunch, no misconfiguration; one run, one arm. **Dollars: 0.65710 core-h × $0.0513/core-h = $0.0337 — DERIVED, NOT MEASURED** | `verification/runs/F5b_runs/physics_p1/RESULTS.md`, `record.json`, `WATCH_TERMINAL.txt`, `WATCH_LOG.txt`; prereg `c1ba1845`/`a80d5f36` |
| C-66 | 2026-08-25 | cfd | **F3 supersonic exact-theory suite CONVERSION — 12 registered `rhoCentralFoam`-family solves (wedge / cone / diamond at M 2.0–3.0) re-run under the conversion pre-registration frozen at `2bf4915a`, amended `e37d7610` (pre-compute). All launched solves `rc = 0` MEASURED from `RC.txt` via the `setsid bash -c 'CMD; echo $? > RC.txt; sync'` carry-forward — NO PROXY. **VERDICT: `NOT A RESULT`** — the frozen comparator `grade_f3.py` (blob `996a6c2c`, verified == its HEAD blob BEFORE running, run unmodified) **CRASHED** at line 602, `KeyError: 'core_s'`, before reaching any gate. `report["runs"]` is built at two sites that disagree on keys: the `PENDING` branch (line 482, run directory absent) omits `core_s`, and line 602 reads it across all entries. **The rows that triggered it are §7 enforcement point 2 working exactly as registered** — `stopped_by: "budget check before wave 6"`, so `wedge/M2.5_th10/fine` and `diamond/M2.5_eps5/fine` were never launched. **The comparator cannot grade the one state its own pre-registration guarantees when the budget binds.** NOT repaired: F3 has FIRED, and a grading-path change after first compute is §2d/§2d.1 and the supervisor's call.** | **35.23 core-min** point estimate (§7); **HARD CAP 39.5** | **33.4177 core-min** — MEASURED, launcher ledger `F3_CONVERSION_RUN_LEDGER.json`, wall × ranks ÷ 60, `cap_respected: true`. **84.6 pct of cap** | **33.4177 core-min — cleaned == gross.** No row approaches the 3600-s stall rule; longest single solve `cone/M2.35_th10/fine` at 1,155 core-s | **0.949** cleaned/predicted. Against the cap: **0.846** | **THE RATIO UNDERSTATES THE MISPREDICTION AND THIS ROW SAYS SO.** 0.949 falls below 1 **only because two registered runs were never launched**. On the ten that did run, **every single one came in ABOVE its prediction** — ratios **1.01 to 1.64** — largest absolute gap `cone/M2.35_th10/fine` **1,155 vs 1,072.7 core-s (+82.3)**, largest relative gaps on the cheap `wedge/M2.0_th15` levels (1.60 / 1.64). **This is SYSTEMATIC UNDER-PREDICTION, sign named and not absorbed:** the suite is more expensive per launched run than registered, and the headline ratio hides it because the budget check truncated the work rather than the estimate. **WASTE: ZERO** — no kill, no per-run wall-cap fire, no relaunch, no misconfiguration. **The two unlaunched runs are NOT waste**: they are the pre-wave budget check refusing work that did not fit with 20 pct headroom, i.e. the cap doing its job. **CONTENTION: BOUNDED, NOT MEASURED** — launched at loadavg 1.29, terminal 9.28, sharing the box with F5b (to 16:53:56Z) and F11 (to 16:40:17Z); this launcher's ledger carries no per-run load decomposition, so contention cannot be separated from the systematic under-prediction and is not claimed to be. **Dollars: 0.556962 core-h × $0.0513/core-h = $0.0286 — DERIVED, NOT MEASURED** | `verification/runs/F3_runs/conversion_2026-08-24/RESULTS.md`, `F3_CONVERSION_RUN_LEDGER.json`, `RC.txt`, `WATCH_TERMINAL.txt`; prereg `2bf4915a` / `e37d7610`. **No `F3_CONVERSION_GRADED.json` exists — the comparator crashed before writing one** |
| C-67 | 2026-08-25 | cfd | **F12 attempt 2 RUNG 1 — coarse, workshop condition, the frozen section 5 RATE-CALIBRATION rung; `rhoSimpleFoam`, serial, on the replacement gate-A-PASSING mesh.** Admission gate A **`PASS`** (51.12365363 deg, skewness 0.9572), admission gate B **`GATE FAIL`** (no convergence statement; the run aborted), strict completion rule **fails all six limbs**, `rc = 134` (SIGABRT) READ FROM DISK and never inferred. Overall **`NOT A RESULT`**; Gates 1-4 `PENDING` and no value quoted. | **120 core-min** frozen cap (section 5, verbatim); frozen section 4 point estimates 11.9 core-min (Basis A) and 109.4 (Basis B) for the full 6,000 iterations | **0.3007 core-min** — 18.0 wall s x 1 rank / 60; solver `ExecutionTime` 13.29 s over 148 iterations, mesh phase 1.35 s, from `SAMPLES.jsonl` and the run's own log | **0.3007 core-min — gross == cleaned.** No stall (18 s against the 3600-s rule). **Cap NOT breached** (0.0025 of it). **Waste named separately: 0.00 core-min** — the run aborted at iteration 148 but the spend bought BOTH a measured rate AND a falsification, and nothing was re-run. | **0.0025x** against the cap — **and this ratio is NOT a misprediction finding and must not be read as one**: the cap is a ceiling for 6,000 iterations and this run stopped at 148. | **THE ROW'S REAL CONTENT IS THE MEASURED RATE, WHICH IS WHAT THIS RUNG EXISTS FOR, AND IT SETTLES THE FROZEN SECTION 5'S OWN FORK.** Measured **3.8975e-6 s per cell-iteration** (13.29 s / 148 it / 23,040 cells) against **Basis A 4.7015e-6 (1.21x, right to 21 %)** and **Basis B 4.6010e-5 (11.80x, over-predicting by an order of magnitude)**. Section 5 declared in advance that the medium rung would blow its cap if the true rate were Basis B's; **it is not**, so rungs 2-5 re-cost off the measurement at 8.98 / 35.9 / 143.6 core-min against caps of 120 / 160 / 700 — headroom 13.4x / 4.5x / 4.9x. **HONEST LIMIT ON THE RATE, STATED NOT BURIED: it is measured over 148 of 6,000 iterations on a run that DID NOT COMPLETE, at loadavg 9.1-9.9 with three other teams' solvers on the box; the extrapolation to 6,000 assumes a constant cost per iteration and THAT IS NOT MEASURED.** loadavg recorded in EVERY sample per this team's standing requirement, not only at launch: 9.10/8.52/7.29 at launch, 9.93/8.72/7.38 at the last sample — a contended box, and the rate above is therefore a CONTENDED rate, which makes it conservative. **Dollars $0.000257, DERIVED at $0.0513/core-h and reported-by-owner, NEVER measured** (`COMPUTE_BUDGET_CHARTER.md` section 5). | `verification/runs/F12_runs/attempt2_coarse_workshop_M0.734_a2.79/RESULTS_RUNG1.md` sections 4 and 6, plus `grade.json`, `RESUME.json`, `SAMPLES.jsonl` and `RC.txt` beside it; launcher committed UNFIRED at `f723fac6` and hashed against its own HEAD blob at launch |
| C-68 | 2026-08-25 | cfd | **CORRECTION ROW — corrects `C-66` (ratio basis) and annotates BOTH `C-65` and `C-66` (contention under-count). Neither original row is edited; this row names what it corrects, per this file's append rules.** **(a) `C-66`'s headline ratio 0.949 was computed over a set INCLUDING TWO UNLAUNCHED RUNS and is therefore an AGGREGATE, not a calibration ratio.** Recomputed over **launched runs only (10 of 12)**: actual 2,005.0 core-s vs predicted 1,775.9 core-s = **RATIO 1.1290**. **Every one of the ten launched runs exceeded its prediction** — per-run ratios 1.23 / 1.01 / 1.08 / 1.64 / 1.60 / 1.26 / 1.35 / 1.13 / 1.19 / 1.28. The filed 0.949 reads as "under budget"; the truth is **systematic UNDER-prediction of ~13 pct**. The two unlaunched runs (`wedge/M2.5_th10/fine`, `diamond/M2.5_eps5/fine`) are named separately with their reason — §7's pre-wave budget check at wave 6 — and are **NOT waste**. **(b) CONTENTION IN BOTH `C-65` AND `C-66` IS NOW KNOWN TO BE UNDER-COUNTED, not merely uncharacterised**: a foreign `simpleFoam` (pid 2272474, cwd `verification/runs/ansys_verification/VMFL003_M2/A_kEpsilon/D_500x6`, another team's) was live on the same 16-core box, and neither row accounted for it. Both rows' contention labelling stands as **BOUNDED, NOT MEASURED**, and is strengthened, not weakened, by naming the omission. | corrects `C-66`: predicted **29.5983 core-min** over launched runs only (was 35.23 over all 12) | corrects `C-66`: **33.4167 core-min** over launched runs only (aggregate as filed: 33.4177 over the run tree) | **33.4167 core-min — cleaned == gross**, unchanged | **1.1290 — THE CALIBRATION RATIO** (launched runs only). The previously filed **0.949 is retained as an AGGREGATE and explicitly labelled as one**, never as a calibration figure | **STANDING RULE ESTABLISHED BY THIS CORRECTION, for cfd from 2026-08-25: a ratio computed over a set that includes UNLAUNCHED members is NOT a calibration ratio. The ratio is computed over LAUNCHED runs only; unlaunched members are named separately with their reason. Both figures may appear, but the ratio is the launched-only one and the aggregate is labelled an aggregate.** This is the same disease as absorbing waste into a ratio, in a new currency — **a favourable number assembled from a set that does not match the claim**. Gap attribution unchanged in kind and sharpened in degree: **misprediction, systematic and UNDER, ~13 pct across every launched run**; **waste ZERO**; **contention BOUNDED, NOT MEASURED, and now known to be UNDER-COUNTED** by at least one foreign solver. **Dollars unchanged and still DERIVED, NOT MEASURED** | `verification/runs/F3_runs/conversion_2026-08-24/RESULTS.md`, `F3_CONVERSION_RUN_LEDGER.json`; corrects rows `C-65` and `C-66` in this file |
| **C-69** | 2026-08-25 | dafoam | **The three curriculum CAPABILITY PROBES D10, D11, D12 (`EXPERTISE_CURRICULUM.md` Tiers 4–5, all three `PROBE FIRST`), fired in one session as EIGHT pre-registered probes — five originals-plus-re-buys and one envelope second point.** Final verdicts: **D10 `NOT A RESULT` → D10-P′ `GATE REACHED`**; **D11 `NOT A RESULT` → D11-D′ `NOT A RESULT` → D11-O′ `NOT A RESULT` → D11-F′ `GATE REACHED`**; **D12 `GATE REACHED` first time**; **D12-E′ `NOT A RESULT` on a registered branch.** All np=1 (`numberOfSubdomains 1`, so core-min = wall-min on every figure in this row), stock image `sha256:9d45679d55fd…90f07fc`, zero GPU. **All three capabilities are REACHED: a `wallHeatFlux` objective reaches the adjoint (`d(HFX)/d(patchV)` max `1.9771502962e+02`); the MRF cell-zone adjoint is non-zero and agrees with central FD to `1.704895e-07` relative; the `DAPimpleFoam` `timeAccurate` unsteady adjoint returns `max |d(obj)/d(shape)| = 1.1622935280e-01`.** Every probe carried a planted control read back off disk, a clean-copy discrimination control on the same quantity that reaches the verdict, and a non-emptiness assertion before every loop; all eight graders ship `--selftest` proving the control can refuse AND pass | **14.25 core-min**, the sum of eight separately frozen predictions (D10 2.5, D10-P′ 0.45, D11 3.5, D11-D′ 1.2, D11-O′ 1.0, D11-F′ 0.85, D12 4.0, D12-E′ 0.75), each committed **before its own first container** and each verified at launch by `git cat-file -e HEAD:<prereg>` plus an md5 match of the launcher against its HEAD blob. Registered caps totalled 39.0 core-min, each enforced by a **cumulative `CAP_CORE_MIN` guard that refuses to launch the next stage** plus a per-stage wall `timeout`. **= $0.012184 at prediction, DERIVED** | **5.3836 core-min MEASURED**, summed from the eight run-root `ledger.txt` `TOTAL_SPENT_CORE_MIN` lines: D10 0.4167, D10-P′ 0.4168, D11 0.7166, D11-D′ 0.8334, D11-O′ 0.7167, D11-F′ 0.7501, D12 0.8500, D12-E′ 0.6833. **= $0.004603 DERIVED, NOT MEASURED**, at $0.0513/core-h, c7a.4xlarge, **reported-by-owner** — the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` §5) | **= gross, 5.3836 core-min.** The longest single container wall in the whole item is **39 s** (D12-E′), **92× below** the 3600-s stall rule (`COMPUTE_BUDGET_CHARTER.md` §2), so stall cleaning matches nothing and gross and cleaned coincide. **Waste is named separately below and is netted off neither column nor the ratio** | **0.3778× (5.3836 / 14.25) — every one of the eight rows came in UNDER its own prediction**, per-row ratios 0.167 / 0.926 / 0.205 / 0.694 / 0.717 / 0.883 / 0.213 / 0.911. **0.138× of the 39.0 core-min of registered caps; no `CAP_CORE_MIN` guard fired on any probe**, so rule 12's "an overrun stops the run" was never engaged | **(1) THE HEADLINE IS NOT THE RATIO, IT IS THE WASTE: 2.6834 core-min — 49.8 % OF THE ENTIRE SPEND — PRODUCED NO GRADED QUANTITY, AND ALL OF IT WAS THIS LANE'S OWN INSTRUMENT DEFECTS.** Named per `COMPUTE_BUDGET_CHARTER.md` §6 and netted off nothing: **D10 0.4167** (the plant sat on the INITIAL internal field of `0/T`, and a converged steady solve is independent of its initial guess **by construction**, so the refusal was guaranteed before the container started — `HFX` moved by **exactly `0.000000e+00`** and base/plant/clean agreed to the last digit at `2846.286928273276`); **D11 0.7166** (`MRFProperties` written in OpenFOAM's named-zone layout when DAFoam's `IOMRFZoneListDF` wants a single top-level `MRF` sub-dictionary — `FOAM FATAL IO ERROR: Entry 'MRF' not found`); **D11-D′ 0.8334** (a 300 rad/s zone STALLS the steady SIMPLE primal, continuity plateauing at ~2.5e-4, never reaching `primalMinResTol 1e-10`, so `AnalysisError: Primal solution failed!`); **D11-O′ 0.7167** (`argparse` treats `-1.0e-3` as an option flag — its negative-number matcher accepts `-1` and `-0.001` but **not exponent notation** — so the negative FD half-step was never passed). **(2) THE MOST EXPENSIVE SINGLE MISTAKE WAS NOT A DEFECT BUT A TRIAGE FAILURE, and it is the transferable lesson: `fdm` exited `2` while its four siblings exited `1`, in ALL THREE D11 attempts. `rc = 1` is DAFoam's `AnalysisError`; `rc = 2` is `argparse`. That distinct exit code sat unexamined beside a cluster of others and was read as part of the same failure — so it survived two further re-buys, costing 1.5501 core-min of the 2.6834. A DISTINCT EXIT CODE BESIDE A CLUSTER OF OTHERS IS A DISTINCT FINDING.** **(3) THE VERDICT THIS ITEM REFUSED TO REPORT, and it is the finding with the longest reach. D11's frozen §4 mapped "`MRFProperties` rejected → `BLOCKED` (capability absent from this build)". The antecedent fired — for a reason the mapping did not contemplate. Reporting `BLOCKED` would have been FALSE and would have told D11's costing that this box cannot do MRF adjoints, when the capability was never reached and therefore never shown absent. `BLOCKED` requires reaching a capability and finding it gone. A GATE WHOSE ANTECEDENT CAN BE TRIPPED BY THE INSTRUMENT THAT TESTS IT IS NOT MEASURING WHAT ITS LABEL SAYS.** Every D11 successor carries this as a gate clause: *a fatal originating in this lane's own files is `NOT A RESULT`, never `BLOCKED`*. **(4) MISPREDICTION, where the 0.378× actually comes from, and it is dominated by two rows: D10 (0.167×) and D12 (0.213×).** D10's 2.5 core-min allowed for a 720-cell primal+adjoint that cost 0.1833. **D12's 4.0 allowed ~1.0 core-min for the pyHyp + `plot3dToFoam` + `autoPatch` + `createPatch` + `renumberMesh` mesh chain — the very term whose omission from the curriculum justified this probe's cap sitting 1.0 core-min ABOVE the curriculum's `≤5` estimate. The mesh chain MEASURED ≈ 0.037 core-min. The disclosed cap increase was not needed, and the disclosure that justified it was wrong on its facts.** The four re-buy predictions, by contrast, were **measured rather than guessed** — each scaled from the identical stages of the attempt before it — and ratios 0.926 / 0.694 / 0.717 / 0.883 / 0.911 show the method works: **a prediction anchored on the same stages of the same case on the same box lands within ~30 %, and usually within 12 %.** **(5) CONTENTION: NOT ISOLATED, AND NOT ASSUMED ZERO. Peer lanes were live throughout** (D8's A6 opt container plus D13's arms; loadavg 9.19 on 16 cores at launch). **A peer measured and committed the mechanism mid-session at `ab89210f`: `mpirun -np 1` inside a container binds EVERY rank to CPU 0, because OpenMPI binds by default and `docker --cpus=1` is a CFS QUOTA, not a PLACEMENT — four live containers each at 0.2504 of one core on a 61 %-idle box, 0.9994 after correction, 3.99× for one token.** All eight probes ran with `--bind-to none` applied by a **before-first-compute amendment** (`c2ceab24`, condition stated and checked: the run root did not exist), so the 3.99× collision was avoided — **but no uncontended control was run, so the residual contention in these figures is UNMEASURED and is not claimed to be zero.** **(6) INSTRUMENT DEFECT, DISCLOSED: D12-E′'s registered cap is `3.0` core-min and its launcher enforced `6.0`** (`d12e_stage_and_run.sh:33`, inherited unchanged from D12's launcher; the substitution list that rewrote every path and the `endTime` assertion did not include the cap, and no assertion checked it). The run cost 0.6833, `0.228×` of the registered cap, so **nothing about any verdict, number or gate is affected** — but a cap that is not the cap you registered is not a cap. **The other seven launchers were audited and their caps DO match their pre-registrations.** Same defect class as `C-51` item (8), reached by a different route: theirs a unit conversion, this a copy-forward no assertion caught. **Fix PROPOSED, not adopted** (a launcher idiom across a family is not a lane's call, rule 9): a probe launcher should assert its own `CAP_CORE_MIN` against the value its prereg names, exactly as these launchers already assert the plant landing and `endTime`. **(7) THE COSTINGS THESE PROBES WERE BLOCKING. D12 `NEEDS COSTING` is ANSWERED: from a TWO-POINT measured wall fit `wall(n) = 19.0 + 2.000·n` seconds per `compute_totals` invocation (29 s at 5 steps, 39 s at 10, same case, byte-identical script), a 300-step window costs 10.32 core-min per major, ~11.3 with one line-search primal, so a 10–20-major D12 with spin-up, the FD verification and δ_repeat lands at **127.6–240.5 core-min = $0.109–$0.206 DERIVED** — against the curriculum's `~1,000–3,000 core-min, $0.9–2.6`, i.e. **the curriculum estimate is 4–23× HIGH for this mesh and window.** Per-step anchors, the first this lab holds for an unsteady adjoint: primal **0.195 s/step**, unsteady adjoint **2.158 s/step** (≈ **11× the primal**), `dRdWTPC` assembly **≈ 3.92 s** at 317 colours, python/IDWarp startup **≈ 7.7 s**, on 2,450 cells at np=1. **D11 stays `UNPRICED`, and the reason is registered rather than glossed:** its case is not selected (the curriculum defers selection to after the probe), and the probe's substrate — a 720-cell 2D channel with an MRF zone in a through-flow — is **a different case class** from the ducted-fan and swirl-passage candidates, so a price would have to be invented across classes, which `COMPUTE_BUDGET_CHARTER.md:375-395` and the curriculum's own §2 estimation rule forbid. What the probe DID deliver toward that costing is **the lab's first anchor in the MRF case class**: at 720 cells, np=1, MRF-active `DASimpleFoam`, primal **0.1167 core-min**, primal+adjoint **0.2000**, adjoint increment **≈ 0.083** — plus a **named blocker on case selection**, that a 300 rad/s zone stalls a steady MRF-active primal, so D11's case must be physically appropriate to a rotating frame or must be unsteady. **(8) THE ENVELOPE QUESTION IS OPEN AND AN ALARM WAS RETRACTED BEFORE PUBLICATION. D12's single-point envelope extrapolated to `0.1065 GiB/step` and hence ~32 GiB at a 300-step window — ABOVE THIS 30 GiB BOX.** D12-E′ was pre-registered to buy the second point with the fit frozen in advance, and returned `per_step_GiB = −0.000262`: **negative, so the registered `NOT A RESULT` branch fired.** ΔR is `0.532536 GiB` at 5 steps and `0.531223` at 10 — **doubling the window did not measurably change resident memory**, so the 0.53 GiB is dominated by the fixed `dRdWTPC` term and **the 32 GiB alarm is NOT SUPPORTED.** Equally, **"the envelope is flat" is NOT established**: the 1.3 MiB difference is **below the run-to-run RSS noise scale** (the two primal figures alone differ by 4.9 MiB) and with one run per point that floor is **unmeasured**. A non-positive slope means either `reduceIO: True` is not holding per-step state in RAM or the allocator is not returning it to `ru_maxrss`, and **this probe cannot tell those apart and does not pretend to.** Disk is settled and tiny: the adjoint adds **177,009 B ≈ 0.169 MiB** over 5 steps. **D12's memory question must be closed by its own prereg before a 300-step window is committed to**; the arm named is two window lengths an order apart with repeats, plus a `reduceIO: False` arm — **proposed, not run, not costed as authorised.** **(9) LANE WALL, priced separately per `L-291` and NOT converted into core-minutes: this lane ≥ 27 lane-min MEASURED AS A FLOOR** (its first `date -u`, 2026-08-25T16:54:40Z, to this commit), and it is **a floor, not a value, because `C-40`'s transferable fix — a dispatched lane should `date -u` into a uniquely-named scratch file as its FIRST action — was AGAIN not applied**: this lane's first clock read came after the reconnaissance and the case authoring. **No dollar figure is attached to any lane-minute**; the family's lane-wall unit conflict noted at `C-49` is live and is not resolved by this row | `cases/dafoam/ladder-a/A5/curriculum_D10_probe/RESULTS.md`, `…/curriculum_D10_probe_Pprime/RESULTS.md`, `cases/dafoam/probes/curriculum_D11_mrf_probe{,_Dprime,_Oprime,_Fprime}/RESULTS.md`, `cases/dafoam/probes/curriculum_D12_unsteady_probe{,_Eprime}/RESULTS.md`; pre-registrations frozen at `c2913dcd` (D10, D11, D12) + before-first-compute Amendment 1 `c2ceab24`, `25c735ff` (D10-P′, D11-D′), `a35ca242` (D11-O′), `57c4a516` (D11-F′), `612c3705` (D12-E′); run artifacts and the eight `ledger.txt` at `/home/ubuntu/certonomous-runs/CURRICULUM-PROBES-D10-D11-D12/` (outside git, per `docs/LOCATIONS.md`); peer defect note `cases/dafoam/ladder-a/A6/curriculum_D8/CPU_BINDING_DEFECT_PROPOSED_NOTE.md` at `ab89210f`, **NOT FILED**. **LANDING METHOD, disclosed because this file is written by multiple teams and the worktree copy is presumed stale:** the row was built from `git show HEAD:docs/COST_CALIBRATION.md`, the id **re-derived from the HEAD blob's own tail INSIDE the committing shell invocation** (`CLAUDE.md` rule 11 — the maximum existing number, never a count), staged via `git hash-object -w` + `update-index --cacheinfo`, insertions-only asserted with `diff-tree`, landed through the rule-10 private index with a CAS on `refs/heads/main` and the non-optional post-commit verify. **No existing row is corrected by this one and no number in any existing row moves.** |
| **STRUCK — was C-69** | 2026-08-25 | cfd | **STRUCK DUPLICATE — RE-ISSUED AS C-204**, see that row; collision with `dafoam`'s **C-69** immediately above, ruled by LANDING ORDER. **Re-formed 2026-08-31 out of the `~~**C-69**~~` register form**, which matches D549's candidate shape while parsing no id and so refused every append to this ledger at exit 7 lab-wide. Content unchanged. | **F1 (ONERA M6) rung R0 — build L1/L2/L3, three real `checkMesh` logs, and the §5 admission grade. `GATE FAIL` at all three levels, so NO SOLVER STARTED.** Cells exactly as amended (108,216 / 865,728 / 6,925,824); `r = 2.000000` on both pairs; **node nesting L1⊂L2⊂L3 = 0.000e+00 m** read from the BUILT `polyMesh` under a LIVE PLANTED CONTROL (1e-9 m perturbation seen at 1.000000083e-09 m; the comparator refuses exit 2 if it is not); skewness 1.443 ≤ 4 PASS; y⁺ estimate 39.7 ≤ 300 PASS; L3 S2 cell 0.00625 c ≤ 0.00650 c PASS. **FAILING: max non-orthogonality 84.64 / 86.02 / 86.78 ° against ≤ 70 ° — and it WORSENS with refinement (severe faces 36 → 216 → 1,440), all of it on AMENDMENT 2's tip-fill collapsed lines; the measured no-fill control gives 51.2554 ° with ZERO severe faces. And `checkMesh` prints no `Mesh OK` at any level, on high aspect ratio at the WAKE CUT (5,934 / 6,469 / 6,749) — IDENTICAL in the control, so that one is §5's own frozen recipe, not the amendment.** `rc = 0` MEASURED from `RC_*.txt` at every step, never proxied. SERIAL, 1 rank — §6's partition pair does not enter this rung, stated so an absent decomposition is not later read as an omission. | **19.3 core-min** pre-registered estimate, **90 core-min HARD CAP** (§8 R0 row); **cap NOT breached**. | **2.18 core-min ACTUAL** — 131 s wall × 1 rank ÷ 60, from `verification/runs/F13_ONERA_M6_runs/log.r0`. **$0.0019 DERIVED, NEVER MEASURED** (0.0364 core-h × $0.0513/core-h, reported-by-owner). | **0.113** | **~15 of the 17.1 core-min underspend is §6's gate D DELIBERATELY WITHHELD** — §5 already refuses to grade G on a level that fails admission, so certifying determinism on an unusable ladder is spend, not diligence; gate D is `PENDING`. The remainder is a **coincidence, not a transfer**: §8 priced meshing at the team's measured `blockMesh` fit (74.9 s for all three levels) and the Python `polyMesh` writer that actually ran took **74 s** — within one per cent, on a different tool. **No stall** (longest step 66 s, far under 3600 s). **Waste: none identified.** **Separately named, never folded into the ratio:** about 0.5 core-min of serial scratch dry-runs during instrument development, run outside every registered path, disclosed on the face of AMENDMENT 2 §A2.2(4). | `verification/runs/F13_ONERA_M6_runs/R0_TERMINAL.md`, `R0_ADMISSION.json`, `verification/campaign/F13_RESULTS.md` |
| C-70 | 2026-08-25 | cfd | **F12 GATE-B PROBE — does `rhoSimpleFoam` print its own convergence statement on this box AT ALL? Run OUTSIDE the repository and outside every registered run path, on the F11-C4 precedent.** **IT DOES: `SIMPLE solution converged in 14 iterations`, rc = 0**, on a 400-cell box using F12's OWN thermophysicalProperties, turbulenceProperties, fvSchemes and fvSolution with ONLY the three `residualControl` numbers loosened to 1e-1. Gate B is SATISFIABLE as frozen; the hypothesis that it is not is ELIMINATED. | **<= 2 core-min**, registered as seconds-long before the probe ran | **0.00995 core-min** — arm 1 0.3224 wall s + arm 2 0.2748 wall s, ranks 1, from each arm's own WALL_S.txt | **0.00995 core-min — gross == cleaned.** No stall. **Waste named separately: 0.2748 core-s (arm 2)** — a `simpleFoam` control that exited rc = 1 because this lane reused F12's COMPRESSIBLE fvSchemes for an INCOMPRESSIBLE solver. It is reported broken rather than dropped, and it was not needed: it existed to prove the search string if arm 1 came back negative, and arm 1 came back positive. | **0.005x** (0.00995 / 2) — a ceiling set deliberately loose for a seconds-long probe, not a misprediction | **THE ROW'S REAL CONTENT IS WHAT THE PROBE ELIMINATED FOR A HUNDREDTH OF A CORE-MINUTE, AND WHAT IT DID NOT.** ELIMINATED: that gate B is unsatisfiable by this solver on this box; that the `residualControl` keys or the `"(k|omega|e)"` regex mismatch the solved fields; that the SIMPLE sub-dictionary is structurally deficient; that `hConstThermo`/`sensibleInternalEnergy` with energy field `e` breaks the check. **A generalisation from the seven existing rhoSimpleFoam logs — none had ever printed it — did NOT support 'it cannot'; this is the eighth and it prints.** NOT ELIMINATED, and now the real risk: **`p` is solved MORE THAN ONCE PER ITERATION and `simpleControl` reads the FIRST solve — a tail-read of the log returns the last corrector instead.** Re-read correctly, F2's `p` sat at **4.2657e-04 against its own 1e-4 threshold** (the tail-read said 3.2756e-06), and **ZERO of 2,000 iterations had every channel below threshold** — F2 never met its criterion and ran to endTime, so there is no anomaly and no mechanism defect. **F2's `p` bottomed at 1.812e-04 around iteration 1400-1500 and ROSE to 3.839e-04 by 2000 — it is not descending, and more iterations would not have reached 1e-4.** F12 asks **1e-6** on `p`, about **180x** below where the closest analogue bottomed out. **Dollars negligible, DERIVED at $0.0513/core-h and reported-by-owner, NEVER measured.** | `verification/runs/F12_runs/gateB_probe_2026-08-25/RESULTS_GATE_B_PROBE.md` and the copied arm-1 log, fvSolution and RC.txt beside it; the executing copy at `/home/ubuntu/certonomous-runs/F12_gateB_probe_2026-08-25/` |
| C-71 | 2026-08-25 | dafoam | **Curriculum item D13 — basin/restart robustness on the D1 problem: five seeded, cold-started perturbed optimisations of A1 NACA0012 (`DASimpleFoam`, 4,032 cells, np=1, IPOPT), PATCHED toolchain only, graded against an optimum-equivalence band frozen before any start ran. Per-start `PASS` on all five (`EXIT: Optimal Solution Found.`, 9–11 majors, 23/23 constraint rows in bound, endpoint FD 4/4 graded at worst 0.2591 % with zero sign flips). CROSS-START BASIN VERDICT `GATE FAIL`: 15 of 15 pairs `DIFFERENT` on the design vector, 0 of 15 `DIFFERENT` on drag.** | **40.0 core-min** point, **≤ 80.0** band, **100.0 HARD CEILING**, **25.0 per start** — `PREREGISTRATION.md` §8, frozen blob at commit `90f5527c` BEFORE any container started. **The curriculum's own ~350 core-min / $0.30 was STRUCK in that same frozen §8 and restated on D1's measured 7.000 core-min anchor** — the strike is pre-registered, not retrospective. | **GROSS 50.033 core-min = $0.042778 DERIVED, NOT MEASURED**, from the run-root `ledger.txt` (wall × ranks ÷ 60, ranks=1 on every arm). **Graded-arm subtotal 28.050 core-min = $0.023983 DERIVED** — s1 6.383, s2 5.217, s3 5.617, s4 5.150, s5 5.683. | **Graded arms: 28.050 core-min, gross == cleaned** — longest arm 383 wall s, nowhere near the 3600-s stall rule (`COMPUTE_BUDGET_CHARTER.md` §2). **NAMED WASTE 21.983 core-min, SEPARATELY NAMED AND NEVER NETTED OFF** (§6): attempt 1 ran 3 arms concurrently, was aborted on a MEASURED execution defect, and its three arms (7.683 / 7.333 / 6.967) are VOID. Precedent: D1's own arm E run 1, VOID and named waste. | **0.7013×** (28.050 / 40.0) on graded arms; **1.2508×** (50.033 / 40.0) on gross — **both inside the ≤ 80.0 band**; **0.500× of the 100.0 ceiling.** Worst single arm 6.383 against the 25.0 per-start ceiling = **0.26×**. **No overrun; no run was stopped by a ceiling.** | **THE HEADLINE GAP IS 100 % NAMED WASTE FROM ONE MEASURED EXECUTION DEFECT, AND THE UNDERLYING PER-ARM ESTIMATE WAS GOOD — the two must not be pooled.** Against the graded arms the prediction was 0.70×; the per-arm anchor was **excellent**: D1's arm O measured **6.017 core-min** and D13's five arms measured **5.150–6.383**, bracketing it. **The whole of the graded-side miss is the MAJOR COUNT, and its cause is identified: §8 predicted 15 majors per start on the reasoning that a perturbed start needs more majors than D1's 11 to restore feasibility. It needed FEWER — 9.8 on average — because `findFeasibleDesign` restores the `CL` equality by angle of attack BEFORE `run_driver` starts, so the optimiser never sees the infeasibility the estimate was pricing. REUSABLE: on this producer, a perturbed start costs the SAME as an unperturbed one, ~5.6 core-min per arm; do not add a restoration premium.** **THE WASTE HAS A REUSABLE MECHANISM, AND IT IS THE ROW'S REAL CONTENT: `mpirun -np 1` inside a `--cpus=1` container binds rank 0 to the FIRST core of the HOST topology the container sees, so EVERY concurrent container binds to the SAME core and per-arm throughput falls as 1/N. `--cpus=1` is honoured as a QUOTA and is NOT the limiter — the BINDING is.** Measured: `affinity=0` on all three concurrent arms; **0.250 cores** used per container against a cgroup `cpu.max` of 1.0 core; only **16 of 3578 periods throttled (0.45 %)**; host **61 % IDLE**; duty 0.29 against D1's 0.91; **167 s/major against D1's 25.3**. **The control that makes this a mechanism and not a hypothesis: affinity was IDENTICAL in both attempts (`0`), only the sibling count changed, and throughput moved 4× (0.250 → 1.000 cores; 167 → 27 s/major).** The peer container `d8_opt` read `affinity=8-15` and was NOT the competitor. **CONTENTION IS THEREFORE NAMED, MEASURED AND ATTRIBUTED — it is not a vague allowance:** it was 100 % of attempt 1's loss and **exactly 0 % of attempt 2's**, because attempt 2 ran serially and each arm owned the core. **This sharpens C-59/C-60's finding that the contention term is BIMODAL: on this box the bimodality is not `loadavg` at all — the box was 61 % idle while contention was total. The conditioning variable for CONTAINERISED MPI work is the number of CONCURRENT CONTAINERS, not `loadavg`, and a future estimate must read that instead.** Repair used: serial execution — **no frozen file edited, no gate, threshold, cap or label changed.** **No predecessor row is corrected by this one.** | `cases/dafoam/ladder-a/A1/curriculum_D13/RESULTS.md` §8 (and §4 for the waste mechanism), commit `15767999`; pre-registration frozen `90f5527c` §8; run-root `ledger.txt`, `core_pinning_finding.json`, `void_arms_waste.json`, `D13_GRADE.json` in `/home/ubuntu/certonomous-runs/CURRICULUM-D13-a1-basin-restart/`. Rate $0.0513/core-h, c7a.4xlarge, **reported-by-owner**; every dollar figure **DERIVED, NOT MEASURED** (`COMPUTE_BUDGET_CHARTER.md` §5) |
| C-72 | 2026-08-25 | ansys-verification | **VMFL007 run 1 — Non-Newtonian Flow in a Pipe (VM2026R1 p. 29), CASE CLOSED at `NOT A RESULT` / tier `NOT HELD`.** Steady laminar `simpleFoam` + `viscosityModels::powerLaw` on a 1° axisymmetric wedge; planned r = 2 triple 625 / 2 500 / 10 000 cells at endTime 10 000, serial. **The case DIVERGED**: L1 completed rc = 0 with areaAverage(p)_inlet at 9.449536950130e+144 m²/s²; L2 died at iteration 9 065 with SIGFPE (rc 136) in `GAMGSolver::scale`; L3 never launched. | **15** core-min (whole triple), frozen at `48f7a9bf` §10 | **6.2666** core-min MEASURED from `RUN_RC.txt` (L1 3.8833 + L2 2.3833), serial, 1 rank; no row over 3600 wall s so gross = cleaned by the §2 stall rule | **6.2666** | **0.4178 naive — AND THE NAIVE RATIO IS MISLEADING, LABELLED SO RATHER THAN QUOTED BARE: only 1.91 of 3 levels ran.** The like-for-like figures are **L1: 3.8833 actual vs a 0.7143 predicted share = ratio 5.4366**, and **L2 (to iteration 9 065): 2.3833 vs 2.5900 = ratio 0.9202**. Projected whole triple at the measured L1 rate is **81.55 core-min**, so the **frozen estimate was low by 5.44×**. | **MISPREDICTION, and the rate MODEL is itself invalid on this case.** The freeze used 2.115e−6 core-s per cell-iteration (from VMFL001) × 4; **measured L1 is 3.728e−5 — 4.4066× the assumed figure.** But the rate is **not scale-invariant here**: L2 measured **6.309983e−6 core-s per cell-iteration, 5.9081× cheaper than L1 at 4× the cells**, which is exactly why the two like-for-like ratios (5.44 and 0.92) disagree. Cost per outer iteration is the count of **inner** linear-solver sweeps, and on a diverging solve that count wanders. **A per-cell-iteration rate is not a valid basis for this case and VMFL007-R2 does not use one** — it prices A1 off L1's direct 3.8833 core-min measurement instead. **CONTENTION, named and NOT subtracted:** loadavg 9.32 on 16 cores at launch with five co-resident solvers, so 6.2666 is GROSS and the clean figure is unknown — no measurement of the contention's size was taken. **WASTE, named separately per §6 and NOT absorbed into the ratio: 2.3833 core-min** (L2's crashed run produced no usable field). Cap 60 core-min **never bit** — spend was 10.4 % of it. Derived **$0.00536** at $0.0513/core-h, **derived, not measured** (the box cannot read its own billing). | `cases/ansys_verification/VMFL007/RESULTS.md` §6; `verification/runs/ansys_verification/VMFL007/{L1_25x25,L2_50x50}/RUN_RC.txt`; prereg `48f7a9bf` §10; R2 machinery `1387b89f` |
| C-73 | 2026-08-25 | cfd | **F12 PRESSURE PROBE — two arms on the REAL coarse mesh, outside the repository and outside every registered run root, to decide attempt 3's scope BEFORE spending a registered attempt.** **Arm A (attempt-3 relaxation only) ran 1,803 iterations against rung 1's 148 — a 12.2x survival improvement — and STILL aborted on `Negative initial temperature`, rc = 134. Arm B (arm A + `transonic yes`) exited rc = 136 SIGFPE after ONE iteration with ZERO p solves, inside the first pressure solve.** | **6 core-min per arm, 12 total**, registered before the probe and enforced by `timeout` | **4.0 core-min** — arm A 220 wall s + arm B 20 wall s, ranks 1, from each arm's own SAMPLES.jsonl | **4.0 core-min — gross == cleaned.** No stall. **Cap NOT breached** (arm A used 3.67 of its 6). **Waste named separately: 0.00 core-min** — both arms answered their question, including the one that crashed. | **0.33x** (4.0 / 12) — a ceiling deliberately set at ~1.5x the measured-rate prediction, not a misprediction | **THE ROW'S REAL CONTENT IS THAT 4.0 CORE-MIN SAVED A REGISTERED ATTEMPT AND THE 120 CORE-MIN CAP BEHIND IT — the third probe today to pay for itself, after F11-C4 and the gate-B probe.** Against the supervisor's decision rule stated IN ADVANCE: 'arm A survives and p bottoms near 1e-6 -> fire attempt 3' is **NO**; 'arm B reaches it -> transonic demonstrated by counterfactual' is **NO**; the outcome is the third branch and worse than it — **neither arm is stable at all**. **FIRST-SOLVE p (never a tail read) bottomed at 8.4010e-03 at iteration 6 and never returned**: medians 5.74e-2 / 5.14e-2 / 1.07e-1 / 1.33e-1 / 5.93e-2 across the run, **ZERO of 1,803 iterations with every channel below the frozen 1e-6, and a floor 8,400x above it — 46x worse than F2's 1.812e-04 on the same condition and a better mesh.** **Attempt 3 as scoped would not merely fail gate B; it would ABORT, at ~1,800 instead of 148.** Relaxation IS a real lever and the severity reading is supported; it is not sufficient. `transonic yes` as a NAKED switch produces a non-finite pressure matrix on the first solve — what that does NOT establish is that transonic is wrong for this case, which is crash triage and is the supervisor's. **loadavg in every sample per standing requirement: 5.01 -> 6.01. Dollars $0.0034, DERIVED at $0.0513/core-h and reported-by-owner, NEVER measured.** | `verification/runs/F12_runs/pressure_probe_2026-08-25/RESULTS_PRESSURE_PROBE.md`, with both arms' logs, fvSolution and RC.txt copied beside it; executing copy at `/home/ubuntu/certonomous-runs/F12_pressure_probe_2026-08-25/` |
| C-74 | 2026-08-25 | cfd | **F12 pressure probe ARM B′ — arm B with `p` moved from GAMG/GaussSeidel to PBiCGStab/DILU, one change, outside the repository and outside every registered run root.** The solver swap WORKED (6 `p` solves against arm B's 0) **and the counterfactual came back NEGATIVE FOR `transonic`: 3 iterations and `Negative initial temperature T0: -259152.9888`, against arm A's 1,803 iterations and -14.4053.** | **6 core-min**, the same per-arm cap as arms A and B, enforced by `timeout` | **0.333 core-min** — 20 wall s x 1 rank / 60, from the arm's own SAMPLES.jsonl | **0.333 core-min — gross == cleaned.** No stall, cap NOT breached. **Waste 0.00 named separately** — the arm answered its question. **Probe total across all three arms: 4.33 core-min of 18 registered.** | **0.056x** (0.333 / 6) — a per-arm ceiling sized for a 2,000-iteration arm that instead died at 3; not a misprediction | **THE ROW SETTLES A CAUSE AND STRIKES TWO MECHANISMS, ONE OF THEM THIS TEAM'S SUPERVISOR'S AND ONE THIS LANE'S.** **REFUTED: the missing `transonic` flag as the cause of F12's pathology.** With the pressure equation actually solving, `transonic yes` gives **600x fewer iterations and an 18,000x larger temperature excursion** than arm A. **FALSIFIED FROM SOURCE: 'GAMG is a symmetric-matrix solver'** — `GAMGSolver.C:40,43` registers BOTH sym and asym tables in OpenFOAM 2606, `GaussSeidelSmoother.C:38,41` likewise, and an unregistered solver raises a FatalIOError at SELECTION time, not a SIGFPE inside `solveCoarsestLevel`. **ALSO STRUCK: this lane's own 'the first pressure matrix is already non-finite', which was never demonstrated.** What arm B established is narrower than either reading: GAMG failed with an FPE in its coarsest-level solve on the asymmetric matrix the transonic branch produces for this case, and why is NOT established. **The prescription was right on a wrong rationale** — swapping the linear solver DID get past the first `p` solve. **Confirmed relevant: `rho.relax()` fires only in the NON-transonic branch (`pEqn.H:107`), so enabling `transonic` silently retires the very lever that bought arm A its 12.2x survival — the two are NOT independent.** STILL OPEN: where first-solve `p` floors on a STABLE run, because no arm has been stable. **Dollars $0.00037, DERIVED at $0.0513/core-h and reported-by-owner, NEVER measured.** | `verification/runs/F12_runs/pressure_probe_2026-08-25/RESULTS_PRESSURE_PROBE.md` section 6, with arm B′'s first-solve residual CSV, gzipped raw log, fvSolution and RC.txt beside it |
| C-75 | 2026-08-25 | dafoam | **D11-C' — the COMPONENT-1 FD STEP SWEEP the D11-F' probe did not buy. CASE CLOSED at `GATE REACHED`, with a PLATEAU DEMONSTRATED for component 1** (h in [1.0e-5, 1.0e-1], five consecutive usable steps; adjoint `-1.3660119098e-04` vs plateau FD `-1.3657515496e-04` at the reference step h=1.0e-3, relative error `1.906351e-04` inside the pre-registered 5.0e-2 band). Prereg frozen at `a02de9fa`; 25 registered containers (1 mesh + 2 compute_totals + 22 run_model), all rc=0, all `measured_affinity=[13]`, all preceded by a pre-launch COLDSTART_PROVED line. np=1 throughout | **3.2 core-min** (registered in PREREGISTRATION.md section 6 from D11-F's per-stage ledger figures; CAP 8.0) | **3.0337 core-min**, from `.../D11C/ledger.txt` `TOTAL_SPENT_CORE_MIN` | **3.0337 core-min — IDENTICAL to gross.** No row approaches the 3600-s stall rule: the longest single stage wall was **10 s** | **0.948** | **WASTE: 0.0 core-min, NAMED SEPARATELY AND NOT ABSORBED INTO THE RATIO** — no re-run, no stall, no abandoned container, no stage skipped. The 5.2 % underspend is **misprediction concentrated in ONE stage**: the mesh build was predicted 0.22 core-min (carried from D11-F') and **measured 0.0167**, a 0.203 core-min overprediction that is essentially the whole gap. The rest of the prediction was accurate: the 22 `run_model` containers were predicted at 0.12 core-min each and **measured a mean of 0.12198 — a 1.6 % error**; the two `compute_totals` stages were predicted 0.37 combined and measured 0.3334. **CONTENTION: nil measurable, and none is claimed** — stage walls held at 7–8 s against an 8 s expectation and affinity `[13]` was READ BACK from all 25 processes, so no contention channel is needed to explain the gap. **LESSON FOR THE NEXT ESTIMATE: a mesh-build figure carried across probes is the least transferable line in a DAFoam per-stage prediction; the per-container solver figures transferred to 1.6 %.** **The D11 chain's PRIOR spend — 0.7166 + 0.8334 + 0.7167 + 0.7501 = 3.0168 core-min — is named here separately and is NOT absorbed into this row's ratio**, as section 6 of the frozen document registered. **$0.0026 DERIVED, NOT MEASURED**, at $0.0513/core-h, c7a.4xlarge, REPORTED-BY-OWNER — the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` section 5) | `cases/dafoam/probes/curriculum_D11_mrf_probe_Cprime/RESULTS.md` section 8; commit `dd053b35`; prereg `a02de9fa` |
| C-76 | 2026-08-25 | dafoam | **D8 — A6 CRM wing-alone, N=16, twist-only constrained drag minimisation. WHOLE RUNG, both arms, GRADED AND CLOSED at `GATE REACHED`** (G1 stopped at the registered 3-major IPOPT cap, never `PASS`; every other gate PASSES — G4 endpoint FD-vs-adjoint aggregate **0.6852 %** against a 5.0 % band, worst component 2.6497 %, zero sign flips, 8 of 9 components by construction with `twist` idx6 `NOT A RESULT` named in advance; plateau MEASURED at two steps per component, worst 6.78 % against 10 %; min clearance 16.09x against 5x). Patched-toolchain row bought, shipped row `PENDING` and named unbought. Prereg frozen at `70d1a3d4` before either arm launched. | **147.3 core-min** = **$0.126 derived** (registered stop threshold 215.0 core-min = $0.184 derived) | **131.101 core-min** = **$0.1121 derived** — 0.217 pre-registration image read (measured, 13 s, no solver) + 87.517 arm `opt` (5,251 wall s x 1 rank) + 43.367 arm `fd` (2,602 wall s x 1 rank), all from `certonomous-runs/CURRICULUM-D8-a6-twist-opt/ledger.txt` | **NO USABLE CLEANED FIGURE FOR THIS ROW, and it is reported rather than approximated** (append rule 2). The mechanical charter §2 rule — a ledger row over 3600 wall s is an infrastructure stall — matches arm `opt` at 5,251 wall s, so a mechanically-cleaned total would be **0.000 core-min**, which is demonstrably false: the container's own solver clock reaches **5,020.30 s**, so **95.6 %** of that wall is solver-accounted. Arm `fd` at 2,602 wall s is under the threshold and is clean either way (**99.56 %** solver-accounted, 2,590.64 s of `ExecutionTime`). **The row therefore carries GROSS with the mis-classification disclosed.** A DAFoam optimisation arm is one long ledger row by construction; the rule was calibrated on short OpenFOAM rows. Flagged upward, not worked around. | **0.890 gross/predicted** (no cleaned figure; see previous column). Well inside the registered 215.0 core-min stop threshold, which was never approached. Per arm: `opt` **0.980**, `fd` **0.750**. | **Misprediction, in the direction the pre-registration named in advance, and it is ONE identified mechanism rather than a residual.** Arm `opt` at 0.980 is essentially on prediction and contributes nothing. The entire 16.2 core-min gap is arm `fd`: §8 priced all 33 primals at the **cold** 105 s, and they ran **warm** from the converged endpoint field at a measured mean of **78.50 s/primal** (2,590.64 s of solver clock over 33 primals). **78.50/105 = 0.748 reproduces the arm ratio 0.750 to three figures.** The prereg predicted exactly this error — *"it prices every primal at the cold 105 s when majors restart warm"* — and cited D1's 0.304x as the precedent. **WASTE: 0.000 core-min, named separately and NOT absorbed into the ratio** (charter §6): no arm relaunched, none crashed, none killed, no perturbation recomputed; both arms rc=0 on their first and only launch and the run root holds exactly two solver logs. **CONTENTION: none affecting these arms** — the `fd` solver held 99.4 % of its CPU with peer containers pinned elsewhere, and 99.56 % of its wall is solver-accounted. | `cases/dafoam/ladder-a/A6/curriculum_D8/RESULTS.md` §10 (grading commit `9c241fe2`); pre-registration frozen at `70d1a3d4`; arm-completion and instrument verification in `cases/dafoam/ladder-a/A6/curriculum_D8/LANE_REPORT.md` §§14-19 (commit `aaa06b73`) |
| **C-77** | 2026-08-25 | ansys-verification | **VMFL003-M2 arm A — `kEpsilon` + `nutkWallFunction`, the "try other models" converged-baseline control (VM2026R1 p. 19); `simpleFoam` serial, six meshes (three-level axial Roache triple at fixed N_r = 5 plus the wall-treatment ladder `D_500x3/4/6`, L2 reused). Verdict `NOT A RESULT` (all three Roache levels failed the frozen residual leg; L3 on ε alone at 2.494e−8). Ran to completion, rc=0 every level, nothing discarded** | **28.1 core-min** per-model point estimate (per-arm cap **40**, slate cap 160), frozen at `c5fdcad4` (prereg blob `cdbf2659`) before compute; basis run-1's MEASURED aggregate **5.806e−6 s/(cell·iter)** over 2.9075e8 cell-iterations, replacing run-1's abandoned 1.6 k-ε multiplier | **29.962 core-min** measured, ExecutionTime-derived from the six `log.simpleFoam` finals (L1 274.83 + L2 333.93 + L3 674.50 + D3 153.33 + D4 130.82 + D6 230.30 = 1797.71 s × 1 rank ÷ 60); **$0.025617 DERIVED** at $0.0513/core-h (owner-stated, **not measured** — the box cannot read its own billing) | **= gross, 29.962 core-min.** Longest single invocation 674.50 s (L3), an order below the 3600 s stall rule → gross = cleaned. **Waste: NONE, named explicitly** — no level re-run, nothing discarded, the aborted arm-C smoke had its own tree | **1.066× actual/predicted** (29.962 / 28.1); **74.9 % of the 40 per-arm cap** — cap NOT exceeded | **MISPREDICTION, and contention is MEASURED-FALSE not merely assumed.** `ExecutionTime`/`ClockTime` = 0.998–1.001 on every level (e.g. L2 333.93/334), so despite peer solvers co-resident (arms C/D of this slate, plus heat-transfer `buoyantBoussinesqSimpleFoam`) the run was never core-starved: **the spend is real CPU, and no contention component is charged.** The 6.6 % gap is the endTime bump the prereg §12 named as the residual risk; the corrected 5.806e−6 basis landed within 6.6 % of actual, vindicating the abandonment of run-1's 1.6 multiplier (which mispredicted 1.406×). **Waste 0.** **NOT pooled with arms C/D's cap-hitting overruns — those are a separate per-iteration cost anomaly (see the N-AV candidate), not this row's story** | Register row #9; `verification/runs/ansys_verification/VMFL003_M2/A_kEpsilon/` (six `RUN_RC.txt` rc=0, `GRADING_A_kEpsilon.json`); comparator `grade_vmfl003_m2.py` `6dcc9994`; `cases/ansys_verification/VMFL003_M2/GRADING_LANE_REPORT.md` |
| **C-78** | 2026-08-25 | ansys-verification | **VMFL003-M2 arm B — `realizableKE` + `nutkWallFunction` (VM2026R1 p. 19); `simpleFoam` serial, same six meshes as arm A, the only change being the `RASModel` keyword. Verdict `NOT A RESULT` (all three Roache levels failed the frozen residual leg; L3 on k and ε). Ran to completion, rc=0 every level, nothing discarded** | **28.1 core-min** per-model point estimate (per-arm cap **40**, slate cap 160), same frozen basis as arm A (`c5fdcad4`, blob `cdbf2659`; 5.806e−6 s/(cell·iter)) | **31.152 core-min** measured, ExecutionTime-derived from the six log finals (L1 297.73 + L2 322.91 + L3 665.36 + D3 153.32 + D4 153.63 + D6 276.16 = 1869.11 s × 1 rank ÷ 60); **$0.026635 DERIVED** at $0.0513/core-h (owner-stated, **not measured**) | **= gross, 31.152 core-min.** Longest single invocation 665.36 s (L3), an order below the 3600 s stall rule → gross = cleaned. **Waste: NONE** — no level re-run, nothing discarded | **1.109× actual/predicted** (31.152 / 28.1); **77.9 % of the 40 per-arm cap** — cap NOT exceeded | **MISPREDICTION, contention MEASURED-FALSE.** `ExecutionTime`/`ClockTime` ≈ 1.000 on every level (wall_s sum 1868 vs ExecutionTime sum 1869.11 → 1.0006), so the spend is real CPU and no contention is charged. The 10.9 % gap is the endTime-bump residual risk (prereg §12); realizableKE costs essentially the same per iteration as standard k-ε (L2 322.91 s vs arm A 333.93 s, 0.97×), so the model change is NOT the cost driver. **Waste 0. NOT pooled with arms C/D's cap-hitting overruns** — those cap-limited runs are the per-iteration cost anomaly recorded as the N-AV candidate, a different phenomenon from arm B's near-estimate spend | Register row #10; `verification/runs/ansys_verification/VMFL003_M2/B_realizableKE/` (six `RUN_RC.txt` rc=0, `GRADING_B_realizableKE.json`); comparator `grade_vmfl003_m2.py` `6dcc9994`; `cases/ansys_verification/VMFL003_M2/GRADING_LANE_REPORT.md` |
| **C-79** | 2026-08-25 | dafoam | **D9 — U-bend pressure-loss minimisation, ladder A5** (run `20260825T181838Z_2370464`; verdict **NOT A RESULT**) — the registered chain `cal` + `rep1` + `rep2` + `opt` + four endpoint `check_totals` steps, np=1, patched image `dafoam-idwarp-rot:v1`. **Three of the four FD stages exited rc=1 on `AnalysisError: Mesh quality error!` (face-pyramid inversion at the optimised endpoint), so the chain underspent because it FAILED, not because the estimate was high** | **43.0 core-min** registered (`PREREGISTRATION.md` §6 and line 9; the §6 table itself sums to 42.5). Registered cap **110.0 core-min**. Derived /bin/bash.0368 | **27.7833 core-min** for the registered chain, from the launcher's own per-stage `core_min` lines in `ledger.txt`; **46.7667 core-min across EVERY D9 container** once the post-hoc crash-triage replication is included = **42.5 % of cap, NO OVERRUN**. Derived /bin/bash.0238 chain, /bin/bash.0400 all containers | **27.7833 core-min** — gross and cleaned are the same figure here: no stage exceeded 3600 wall s (longest 454 s) so there is no stall to clean, and no waste is netted out of it | **0.65 headline (27.7833 / 43.0) AND IT IS NOT A CALIBRATION OF THE ESTIMATE — flagged, not quoted.** The defensible like-for-like per-stage ratios are: `cal` **0.51** (2.80 predicted vs 1.4333); `rep1`+`rep2` **0.11** (4.20 vs 0.4666); `opt` **0.67** (10.60 vs 7.0500); `fd` **1.22** (6.225 per step vs 7.5667 for the ONE step that ran to completion) | **MISPREDICTION dominates, and the largest single miss is the repeat stages: the measured anchor assumed ~125 s per `run_model` and the measured cost was 14 s, a 9.0x overprediction** — the anchor charged a full one-off startup to a task that does not pay one. `cal` and `opt` are overpredicted 2.0x and 1.5x on the same cause. **The FD stage is underpredicted 1.22x**, so the anchor is not uniformly conservative. **WASTE, NAMED SEPARATELY AND NEVER ABSORBED INTO ANY RATIO: 11.2667 core-min** spent by the three crashed FD stages, which produced no table and no answer record (they did buy the mesh-quality finding, which is reported as a finding, not netted against the prediction). **DIAGNOSTIC SPEND, ALSO HELD OUTSIDE THE RATIO: 18.9834 core-min** of post-hoc crash-triage replication, unpredicted because it is post-hoc. **CONTENTION, DISCLOSED AND NOT ABSORBED: −4.29 % to +5.59 %** measured replicate-vs-original wall across four stages, with the longest stage identical at 454 s both times — inside the 5-11 % the lab records as acceptable | `cases/dafoam/ladder-a/A5/curriculum_D9/RESULTS.md` §8a; `LANE_REPORT.md`; ledger `/home/ubuntu/certonomous-runs/CURRICULUM-D9-a5-ubend-opt/ledger.txt`. cost_basis: c7a.4xlarge at $0.0513/core-h, REPORTED-BY-OWNER, NOT MEASURED; all dollars DERIVED |
| **C-80** | 2026-08-25 | ansys-verification | **VMFL019 — Transient Flow Near a Wall Set in Motion (Stokes' first problem), VM2026R1 p. 77; `icoFoam` serial, three-level family with space AND time refined together at ratio 2 (Ny 30/60/120, dt 0.05/0.025/0.0125 s). Verdict `PASS` — both probes inside a 1 % band against the exact analytic erfc profile, both triples `CONVERGING`. Ran to completion, rc=0 every level, nothing discarded** | **0.15 core-min** point estimate, frozen in `PREREGISTRATION.md` §7 (blob `e303c4dea3d82644e487a11ba173306cabf8d0ec`) before compute; cap **9.00** core-min = est x ~60 slack | **0.0167 core-min** measured (`RUN_RC.txt` per level: 0.0000 + 0.0000 + 0.0167; wall 0 + 0 + 1 s x 1 rank / 60) | **0.0167 core-min — gross == cleaned.** No stall (longest level 1 wall-s against the 3600 wall-s stall threshold), cap NOT breached (**0.19 % of cap**). **Waste 0.00, named separately** — no level was discarded or re-run | **0.111** | **MISPREDICTION, in the conservative direction — NOT contention and NOT waste.** The freeze deliberately took ~60x slack because the box carried 8–10 CPU-bound neighbours and the regime was new to this team, and §7 said in advance that the rate would be MEASURED from this case's own iterations rather than assumed from a whole-box loadavg. The slack was never needed. **The lesson for the next estimate is narrower than 'estimate lower': a 1-D transient on 120–480 cells is BELOW the launcher's 1-second wall-clock resolution, so the instrument, not the solver, sets the floor** — two of three levels measured 0 wall-s. Estimates for cases of this size should be quoted as bounds, not point values | `cases/ansys_verification/VMFL019/RESULTS.md`; run root `verification/runs/ansys_verification/VMFL019/`; grading `GRADING_VMFL019.json` |
| **C-81** | 2026-08-25 | ansys-verification | **VMFL010 — Laminar Flow in a 90° Tee-Junction, VM2026R1 p. 39; `simpleFoam` serial, three-level refinement-2 family (3 600 / 14 400 / 57 600 cells). Verdict `NOT A RESULT` — rule 5 step 2, grid triple `OSCILLATORY`. Ran to completion, rc=0 every level, every level converged on SIMPLE's own criteria, nothing discarded** | **10.29 core-min** point estimate and cap **38.57** core-min — **both as corrected by a dated amendment BEFORE first compute**, which struck the original 8 / 30 and scaled them by 9/7 when the mesh came in at 3 600 rather than the frozen §8's 2 800 cells (prereg blob `4d30fb4c80e6dc9c4cdedf893020d3092b632baa`) | **3.5833 core-min** measured (`RUN_RC.txt` per level: 0.2167 + 0.5167 + 2.8500; wall 13 + 31 + 171 s x 1 rank / 60) | **3.5833 core-min — gross == cleaned.** No stall (longest level 171 wall-s against the 3600 wall-s stall threshold), cap NOT breached (**9.29 % of cap**). **Waste 0.00, named separately** — the `NOT A RESULT` verdict is a finding about the gate quantity, not discarded compute; all three levels are sound and are cited by the row | **0.348** | **MISPREDICTION, in the conservative direction.** The estimate was scaled arithmetically from the corrected cell count and still over-predicted by ~3x. **The attributable cause is that the estimate assumed a fixed iteration count while SIMPLE's `residualControl` stopped each level on its own** (729 / 1125 / 1575 iterations, not a frozen endTime), so the true cost scales with convergence rate and not with endTime x cells. **A calibration lesson with teeth for this team: for a `residualControl`-terminated steady run, the honest predictor is cells x EXPECTED iterations, and the expected iteration count itself grows sub-linearly with refinement here (729 -> 1125 -> 1575, i.e. ~1.5x per level against 4x cells)** | `cases/ansys_verification/VMFL010/RESULTS.md`; run root `verification/runs/ansys_verification/VMFL010/`; `COST.txt`; captured verdict `GRADING_VMFL010.stdout.txt` |
| **C-82** | 2026-08-25 | ansys-verification | **VMFL050 — Transient Heat Conduction in a Semi-Infinite Slab, VM2026R1 p. 163; `laplacianFoam` serial, three-level family with space and time refined together at ratio 2. Verdict `PASS`, tier `GATE REACHED` (ruled by the supervisor 2026-08-25). Ran to completion, rc=0 and `DONE.flag` every level, nothing discarded. **Row landed late — the case had run, been graded and been ruled while `CASE_MAP.md` still carried it as `NEVER RUN`**| **0.2 core-min** point estimate, frozen in `PREREGISTRATION.md` §7 (blob `c816fc5506ee6b3b20412688b89c878eb16a6291`); cap **8** core-min = est x ~40 slack, taken because the box carried ~11 D-state tasks and a clean-box cap would false-kill | **≤ 0.05 core-min** measured — `RUN_RC.txt` records `wall_s=0` at ALL THREE levels, so the measurement is a **bound, not a point value** | **≤ 0.05 core-min — gross == cleaned.** No stall, cap NOT breached (**≤ 0.6 % of cap**). **Waste 0.00, named separately** | **≤ 0.25** | **INSTRUMENT FLOOR, not merely misprediction — and this row is the clearest instance the ledger has.** The case is faster than the launcher's 1-second wall-clock resolution at every level, so the actual cannot be measured at all, only bounded. **The ratio is therefore an upper bound and must not be read as an estimate quality score.** The corrective action is an instrument change, not an estimate change: **a launcher that timed in milliseconds (or counted `ExecutionTime` from the solver log, which this rung's own logs carry) would turn three unmeasurable rows into three measured ones.** Recorded as the second sighting of this floor today, with C-80 (VMFL019) the first | `cases/ansys_verification/VMFL050/SUPERVISOR_RULING_VERDICT.md`; run root `verification/runs/ansys_verification/VMFL050/`; grading `GRADING_VMFL050.json` |
| **C-83** | 2026-08-25 | ansys-verification | **VMFL059 — Conduction in a Composite Solid Block, VM2026R1 p. 185; `laplacianFoam` serial, three levels. Verdict `NOT A RESULT` — rule 5 step 2, the `rightWall` triple is `EXACT`. **The solve was clean and every control passed; the cause is a mis-specified gate quantity, not compute.** Ran to completion, rc=0 every level, nothing discarded** | **3 core-min** point estimate, frozen in `PREREGISTRATION.md` §12 (blob `f853d8cad11b3347d409b4d54760cf6e31263983`); cap **15** core-min | **0.4167 core-min** measured (`COST.txt`: 25 wall-s total x 1 rank / 60; per level 8 + 8 + 9 wall-s) | **0.4167 core-min — gross == cleaned.** No stall, cap NOT breached (**2.78 % of cap**). **Waste 0.00, named separately** — a `NOT A RESULT` on the gate specification is not wasted compute; the run is sound and its numbers are cited in register row #16 | **0.139** | **MISPREDICTION, in the conservative direction.** 2-D conduction on 280–4480 cells cost ~7x less than the frozen point estimate. **The specific over-prediction is that the estimate priced these as three independent solves at full transient length, when `laplacianFoam` on a linear conduction problem converges in a handful of steps and the per-level cost barely moved with refinement (8 / 8 / 9 wall-s across a 16x cell range) — the cost here is dominated by process start-up and I/O, NOT by cell count.** That is the reusable fact: **below roughly 5 000 cells this team's conduction cases are start-up-bound, so cell-count scaling is the wrong predictor** | `cases/ansys_verification/VMFL059/SUPERVISOR_RULING_VERDICT.md`; run root `verification/runs/ansys_verification/VMFL059/`; `COST.txt` |
| **C-84** | 2026-08-25 | dafoam | **Curriculum D4 arm F — THE ENDPOINT FD, the DAFoam bright line — RAN AND CRASHED, and the rung stays `BLOCKED`.** MACH wing constrained CD minimisation at fixed CL, A2 case, np = 4 `scotch`, PATCHED IDWarp `sha256:2927768a…f6d35`, frozen launcher `d4_run_arm.sh` md5 `399957c6…` unmodified and no override passed. Arm F was staged (`d4_stage_F.sh`, rc 0) and launched at 21:13:39Z; it failed **15 wall seconds in** with `openmdao.core.analysis_error.AnalysisError: Mesh quality error!` on the **first primal**. **This is a completed process under rule 12 — the arm reached a terminal state, was triaged, and was graded — so it is costed here; but it produced NO FD table and NO graded number, and the rung verdict is `BLOCKED`.** **The three earlier arms P1, P2 and O are ALREADY COSTED at `C-53`-class treatment in `RESULTS.md` §7 and NO FIGURE OF THEIRS MOVES**; this row adds arm F and restates the item total | **53.0 core-min for arm F**, frozen in `PREREGISTRATION.md` §8 **before any container started** (basis, quoted: *"22 primals × 0.90 + one cold `compute_totals`"*), against a registered **arm cap of 120.0 core-min** and an **item ceiling of 800.0**. Item prediction across all four arms **598.0 core-min = $0.511 DERIVED**. **Sanaa lifted cost constraints on 2026-08-25 ("no run stops to save compute"), so the 120.0 cap acted as a RUNAWAY GUARD, not a budget — and rule 12's costing and calibration continue unchanged, which is why this row exists at all.** The cap was never approached and nothing was trimmed to fit it | **Arm F: 1.0 core-min MEASURED** (15 wall s × 4 ranks ÷ 60), read from the launcher's own `ledger.txt` arm-`F` row, corroborated by `docker inspect .State.ExitCode/.State.OOMKilled` = **`1 false`** — the kernel's record, not the harness's `$?`. **Item, all four arms: 548.866 core-min MEASURED** = 0.333 (P1) + 36.4 (P2) + 511.133 (O) + 1.0 (F), the same figure the grader independently computed into `d4_grade_ARMF_20260825T212030Z.json` (`G10_caps.total_core_min`). **= $0.4693 DERIVED, NOT MEASURED**, at $0.0513/core-h, c7a.4xlarge, **reported-by-owner** — the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` §5) | **547.866 core-min cleaned** — the item's gross **minus arm F's 1.0**, because **arm F delivered no work**. The 3600-s stall rule matches nothing new here (arm F ran 15 s; arm O's 7667 s was already disclosed in `RESULTS.md` §7 as **not** a stall — a registered optimisation that terminated on IPOPT's own convergence statement 1633 s inside its timeout). **Waste is named separately below and is netted off neither column nor the ratio** (`COMPUTE_BUDGET_CHARTER.md` §6) | **1.0053× (547.866 / 545.0)** — cleaned actual against the prediction **for the work actually delivered** (P1 + P2 + O = 545.0). **ARM F'S OWN RATIO OF 1.0 / 53.0 = 0.019 IS NOT REPORTED AS A CALIBRATION SIGNAL AND MUST NOT BE READ AS ONE.** A ratio compares the cost of work done against the cost predicted for it; arm F crashed before a single primal converged, so **there is no work to compare and the ratio is meaningless**. Reporting 0.019 would record a 53-core-min estimate as a 53× over-prediction when the estimate was never tested. **The item-level 548.866 / 598.0 = 0.9178 is likewise NOT a calibration signal** — it is 0.918 only because a whole arm failed, and a ledger read at item level would score that as a *good* estimate. **The prediction of 53.0 core-min for arm F STANDS and remains the right estimate for when the arm can be run** | **ATTRIBUTION OF ARM F'S GAP: 100 % INSTRUMENT DEFECT. Not misprediction, not contention, not stall.** The cause is **D4-DEF-4**, a units defect in a **FROZEN** instrument: `d4_extract_endpoint.py` reads the endpoint from `OptView.hst`, which holds **driver-scaled** values (OpenMDAO applies `scaler` *before* pyOptSparse sees the problem — measured: pyOptSparse's own `getDVInfo()` scale is **1.0** for every DV and `getValues(scale=True)` and `getValues(scale=False)` return **identical** values, so the extractor's `scale=False` argument is **inert**), while `d4_fd_endpoint.py` applies them as **physical** via `prob.set_val`. `shape`'s registered scaler is **10.0**, so arm F set the wing shape to **ten times** its optimised deformation → 2989 non-orthogonality errors, 6090 mis-oriented face pyramids → mesh death. The **unforgeable control**: `patchV[0]` is **pinned**, `lower = upper = U0 = 100.0`, so its physical value is definitionally 100.0 and no optimiser can move it — the extractor returns **10.0**, exactly `100.0 × 0.1`, the registered scaler. **WASTE: 1.000 core-min = $0.00085 DERIVED**, named separately per charter §6, netted off neither column nor the ratio — one failed arm, no restart, no re-run (and **none was attempted**: re-running arm F unchanged reproduces the same crash, and the frozen launcher asserts the extractor's md5 before every launch and aborts with code 4 if it differs, so an edited extractor cannot run under the registered launcher at all — **the freeze is working as designed**). **CONTENTION: none measurable** — `siblings_pre=[]` and `siblings_post=[]` in the arm-`F` ledger row; no container was live at launch or exit, and `MemAvailable` was 27.34 GiB against a 12 g cap. **TRIAGE SPEND, NAMED AND `NOT MEASURED`, AND THIS IS THIS ROW'S TRANSFERABLE GAP:** two single-rank diagnostic containers (`d4_triage_scaling.py`, `d4_triage_majors.py`) were run to attribute the crash, and they ran **OUTSIDE the frozen launcher and therefore outside its `ledger.txt`**. Their cost is **`NOT MEASURED`** — bounded above by the enclosing wall time at 1 rank, **≤ ~1 core-min each** — and rule 12 forbids calling a cost measured without a record behind it, so it is reported as unmetered, **not** folded into either column. **The lab has a ledger discipline for registered arms and NONE for the diagnostic containers that triage them**, and triage is exactly the work a crash makes mandatory (`SUPERVISION_CHARTER.md` §3). *Proposed and NOT decided, because changing a launcher idiom is not a lane's call (`CLAUDE.md` rule 9): a diagnostic container should be routed through the same ledger as a registered arm, under a `DIAG` arm label with its own cap, so triage spend is measured rather than bounded.* Referred to the dafoam supervisor; **unruled, and this row does not act as though it were ruled.** **A SECOND DEFECT, D4-DEF-5, was found during the same triage and is NOT a cost event but bears on a gate:** `d4_major_history.json`'s **125 rows are function calls, not the 80 IPOPT majors**, and include the `findFeasibleDesign` AoA sweep that runs *before* `run_driver` — proved, not inferred, by the contradiction that its worst row has `|CL − 0.5| = 2.8292e-02` while the maximum `inf_pr` over all 81 IPOPT rows is `1.08e-02` and `inf_pr` bounds `|CL − 0.5|` at every major. **Graded over that file, G2 band A would return `GATE FAIL` on 37 of 125 rows AND THAT WOULD BE WRONG**; band A stays **NOT ESTABLISHED** and P3 stays **UNSCORED**. **No gate, threshold, cap or label moves by anything in this row, and `PREREGISTRATION.md` is untouched** | `cases/dafoam/ladder-a/A2/curriculum_D4/RESULTS.md` **§10.8** (verdict `BLOCKED`, commit `9233acd8`); defect note `cases/dafoam/ladder-a/A2/curriculum_D4/D4_DEF4_DEF5_ENDPOINT_SCALING.md` (commit `2ecf6ec9`); stager filed to the case dir at commit `3ce489f4` — **it had existed only UNTRACKED in the run root and `RESULTS.md` §9 cited it while it was in no git tree at all**. Run root **outside git**, `/home/ubuntu/certonomous-runs/CURRICULUM-D4-a2-wing-cdmin/` — `ledger.txt` (the arm-`F` row, every cost and kernel figure in this row), `F_driver.out`, `F_20260825T211339Z_2574215.log` (the traceback), `F_STAGING_EVIDENCE.txt` and `F_STAGING_GUARD_CONTROL.txt` (the stager's positive control: re-run with `F/` present it exits **5** and leaves `F/` intact), `F/d4_endpoint_dvs.json` and `F/d4_major_history.json` (the two defects' evidence), `d4_triage_scaling.out`, `d4_triage_majors.out`, and the grader verdict `d4_grade_ARMF_20260825T212030Z.json`. **None of the run-root artifacts is in git and none is recoverable if that directory is removed — stated as a risk, not repaired.** Frozen referents that ARE in git: `PREREGISTRATION.md` §8 (the 53.0 and the 120.0 cap) and §9a (the instrument md5s). **`d4_grade.py` md5 re-verified after all work at `f162ef69a7385e5d0586ef5f27657cbb`, unchanged; grading was done with `d4_grade_SUPPLEMENT.py` (selftest 29/29) and NEVER by invoking the frozen grader directly.** Rate $0.0513/core-h, c7a.4xlarge, **reported-by-owner**; every dollar figure in this row is **DERIVED, NOT MEASURED**. **LANDING METHOD:** the id was re-derived from the **HEAD blob's own tail** (`git show HEAD:docs/COST_CALIBRATION.md`), tolerantly (`grep -oE 'C-[0-9]+'` then numeric sort — the **maximum existing number, never a count**, `CLAUDE.md` rule 11), **inside the shell invocation that wrote the tree**, and would have been re-derived again on any CAS retry. The worktree copy was byte-identical to the HEAD blob at derivation time (317,022 B both), and **was deliberately NOT written**, so a concurrent peer's tail cannot be clobbered. Landed through the rule-10 private index with insertions-only asserted via `diff-tree` and the post-commit verify |
| C-85 | 2026-08-25 | dafoam | **D10-F′ — the FD pair for the D10-P′ adjoint gradient, graded `PASS`.** 720-cell 2D heated channel, `DASimpleFoam`, `patchVelocity` DV, **np=1 (`numberOfSubdomains 1`), so core-min = wall-min on every figure in this row**. 17 containers: 1 mesh + `base` `compute_totals` + 2 δ_repeat + 1 plant + 12 FD half-steps. Zero GPU | **1.9339 core-min = $0.001654 derived**, frozen in `PREREGISTRATION.md` §7 at commit `120dddd2` **before any container started**. **Built from MEASURED D10-P′ stages of the SAME case on THIS box** (mesh 0.0167, `compute_totals` 0.1667, `run_model` 0.1167 × 15), not guessed. Separately registered **runaway guard 8.0 core-min** — a guard reported upward, NOT a budget, cost constraints being LIFTED (Sanaa 2026-08-25) | **1.7501 core-min MEASURED** — read from `ledger.txt`: 16 stage `core_min` fields summing **1.7334** plus mesh **0.0167**. **= $0.001496 DERIVED, NOT MEASURED**, at $0.0513/core-h, c7a.4xlarge, **reported-by-owner** — the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` §5) | **= gross, 1.7501 core-min.** Longest single wall is **13 s**, more than two orders below the 3600-s stall rule, so stall cleaning matches nothing and gross and cleaned coincide. **Waste is named separately and is netted off neither column nor the ratio** | **0.905× (1.7501 / 1.9339) — INSIDE the estimate, conservative direction; 0.219× of the 8.0 core-min guard, which never fired** | **MISPREDICTION, small and conservative. NO CONTENTION PENALTY APPEARED** despite three `buoyantBoussinesqSimpleFoam` ranks at 99.9 % CPU and load ~6 at launch. The `run_model` anchor was carried at D10-P′'s 0.1167 core-min (7 s) and 14 of 16 stages measured **6 s**. **WASTE: 0.000 core-min** — all 16 stages `rc=0`, `OOMKilled false`, every one produced a graded artifact; nothing re-bought. **REUSABLE FACT: this is the THIRD confirmation that a prediction anchored on the same stages of the same case on the same box lands within ~30 %, and here within 10 % — while RESIDUAL CONTENTION REMAINS UNMEASURED (no uncontended control was bought), so the 0.905× must not be read as a contention measurement** | `cases/dafoam/ladder-a/A5/curriculum_D10_probe_Fprime/RESULTS.md`; run root `/home/ubuntu/certonomous-runs/CURRICULUM-PROBES-D10-D11-D12/D10F/`; `ledger.txt`, `GRADE_OUTPUT.txt` |
| C-86 | 2026-08-25 | dafoam | **D12-F′ — the FD pairs for the D12 probe's UNSTEADY adjoint, graded `NOT A RESULT` (no plateau on either component).** 2,450-cell 2D cylinder, `DAPimpleFoam` `unsteadyAdjoint timeAccurate`, FFD shape DV, 5-step cold-start window, **np=1 (`numberOfSubdomains 1`), core-min = wall-min**. 25 containers: 1 mesh + `base` `compute_totals` + 2 δ_repeat + 1 plant + **20 FD half-steps across TWO components** (idx 3 and idx 0). Zero GPU | **4.3544 core-min = $0.003723 derived**, frozen in `PREREGISTRATION.md` §8 at commit `120dddd2` **before any container started**, from MEASURED D12-probe stages (mesh 0.037, `compute_totals` 0.4833, `run_model` 0.1667 × 23). Separately registered **runaway guard 15.0 core-min** — a guard reported upward, not a budget | **3.2504 core-min MEASURED** — from `ledger.txt`: 24 stage `core_min` fields summing **3.2004** plus mesh **0.0500**. **= $0.002779 DERIVED, NOT MEASURED**, $0.0513/core-h, c7a.4xlarge, **reported-by-owner** | **= gross, 3.2504 core-min.** Longest single wall well under the 3600-s stall rule; gross and cleaned coincide. **Waste named separately, netted off neither column nor the ratio** | **0.747× (3.2504 / 4.3544) — INSIDE the estimate, conservative direction; 0.217× of the 15.0 core-min guard, which never fired** | **MISPREDICTION. The `run_model` anchor was carried at the probe's `plant` figure of 0.1667 core-min (10 s) and the stages measured mostly 7–8 s. No contention penalty appeared. WASTE: 0.000 core-min — all 24 stages `rc=0`, `OOMKilled false`, every one produced a graded artifact. A `NOT A RESULT` FROM A SWEEP THAT RAN CORRECTLY IS A PURCHASE, NOT WASTE**: the sweep is what established the finding below, and classifying it as waste would make the ledger punish the lab's most useful outcome. **REUSABLE FACT, and it is the point of the row: δ_repeat on this time-average is EXACTLY 0.000000e+00, yet the objective carries a PERTURBATION-RESPONSE floor ≈ 1.65e-06 absolute (1.8e-05 relative) on component 3 — three-plus orders above δ_repeat, component-dependent, and shown MODEL-FREE by the signal at h=1e-5 being SMALLER than at h=1e-6. A step sized on δ_repeat alone is a step sized against nothing.** RESIDUAL CONTENTION UNMEASURED; `MemAvailable` 27.32 GiB at launch against the 12 GiB hold | `cases/dafoam/probes/curriculum_D12_unsteady_probe_Fprime/RESULTS.md`; run root `/home/ubuntu/certonomous-runs/CURRICULUM-PROBES-D10-D11-D12/D12F/`; `ledger.txt`, `GRADE_OUTPUT.txt` |
| C-87 | 2026-08-25 | cfd | **F1 / ONERA M6 TOPOLOGY STUDY — nine mesh variants built and diagnosed; `GATE FAIL` on `MESH_STANDARD.md` §3.1's standing 70° hard gate at every one of them, best 81.5834°.** MESH_STANDARD §8.1 *build-before-you-freeze* trial: `blockMesh` + `checkMesh` at m=1 only, no solver, no GPU, np=1 throughout so **core-min = wall-min on every figure in this row**. The process completed in rule 12's sense — the trial reached a terminal state, was diagnosed face-by-face and was graded — which is why this row exists | **NONE. NO PRE-REGISTERED ESTIMATE EXISTS FOR THIS TRIAL AND ONE IS NOT INVENTED HERE.** An unregistered §8.1 build trial carries no frozen cost line, and rule 12 forbids calling a cost measured without a record; it equally forbids back-filling a prediction after the fact, which would make the ratio column read as calibration when nothing was predicted. **Recorded `N/A`, not estimated.** For the successor ladder the transferable anchor this row establishes is below | **6.367 core-min MEASURED = $0.0054 DERIVED, NOT MEASURED**, at $0.0513/core-h, c7a.4xlarge, **reported-by-owner** — the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` §5). Two components, both read from clocks on disk: **builds 13 core-s = 0.217 core-min**, summed from the nine variants' own `WALL_S.txt` written by `run_topo.sh`; **diagnostics 369 core-s = 6.150 core-min** for `outer_face_geom.py` over 13 built meshes (9 topology-study + 4 `te_study` re-reads), from the two batch clocks (120 s + 249 s). Artifact: `verification/runs/F1_MESH_TRIALS_2026-08-25/topology_study/COST_MEASURED.txt` | **= gross, 6.367 core-min.** The longest single wall in the whole study is **2 s** for a build and **~28 s** for one mesh's diagnostic, three orders below the 3600-s stall rule, so stall cleaning matches nothing and gross and cleaned coincide. **Waste is named separately below and is netted off neither column nor the ratio** (`COMPUTE_BUDGET_CHARTER.md` §6) | **N/A — no predicted figure exists to divide by.** A ratio is reported here as absent rather than as a number, because any ratio computed against a retrospectively-written estimate would be a self-comparison and would enter the lab's calibration record as though an estimate had been tested. **It had not.** Cost was never the binding constraint on this item — the whole study is $0.0054 derived, four orders under the $25 pre-authorisation | **NO GAP TO ATTRIBUTE (nothing was predicted), so what this column carries instead is the WASTE and the anchor.** **WASTE: ≈ 0.13 core-min ≈ 2 % of the total, named separately and netted off neither column:** (i) two refused first cuts of the shell topology, `blockMesh` rc=1 on **a patch-assignment defect of mine** — the shell block was handed the inner block's patch list so `wing` was claimed on an internal face — ≈ 4 core-s; (ii) three rebuilds of `t0_REPRO` to re-check byte-identity against `te_study/b1_CTRL` after each generator edit, ≈ 4 core-s. **The (ii) spend is deliberate and I would spend it again**: it is the generator's planted control and it caught nothing only because it was run every time. **NO mesh was diagnosed twice** — the first diagnostic batch was killed by a 120-s tool timeout after completing 4 of 13 meshes and those 4 output files were **kept**, so the 120 s are counted in full as productive. **CONTENTION: not separately measured, and that is stated rather than assumed away.** Three `buoyantBoussinesqSimpleFoam` ranks and dafoam work were live throughout; box load was ~3.05/16 at start with 13 idle cores, this lane held ≤ 3 concurrent processes against a 5-core ceiling, and **no uncontended control was bought**, so no contention figure is claimed. **REUSABLE ANCHOR FOR THE SUCCESSOR LADDER, and it is the point of this row:** at m=1 (111,872 cells) an ONERA M6 `blockMesh` + `checkMesh` build costs **≈ 1.4 core-s**, and a full face-by-face non-orthogonality diagnosis of the built mesh costs **≈ 28 core-s — twenty times the build.** **Any successor pre-registration that budgets mesh builds but not their diagnosis will under-predict the meshing item by more than an order of magnitude**, and this study's own split (0.217 vs 6.150 core-min) is the measurement of that. Note the diagnostic is Python over 327,168 internal faces and scales with cell count, so at L2/L3 it is the dominant meshing cost, not the solver's neighbour | `verification/runs/F1_MESH_TRIALS_2026-08-25/topology_study/LANE_REPORT.md` §8 and `COST_MEASURED.txt`, landed at commit **`c813b20d`** (106 files, insertions-only). Per-variant clocks in each `t*/WALL_S.txt`; the graded maxima in each `t*/log.checkMesh` and in `evidence/NONORTHO_*.txt` (both planted controls printed in every one). **`constant/polyMesh` and `system/` are NOT committed** — 170 MB, exactly regenerable from `gen_topo.py` plus each variant's `DIALS.txt`. Frozen referent read from HEAD, never the worktree: `verification/campaign/F13_ONERA_M6_PREREGISTRATION.md` blob **`7456a7b3dc62`**, disk hash-matched. **No gate, threshold, cap or label moves by anything in this row, and no pre-registration is touched.** **LANDING METHOD:** the id was re-derived from the **HEAD blob's own tail** tolerantly (`grep -oE '\*{0,2}C-[0-9]+\*{0,2}'` then numeric sort — the **maximum existing number, never a count**, rule 11; the bold form is matched because a peer found real rows the plain regex missed), **inside the shell invocation that wrote the tree**, and would have been re-derived on any CAS retry. The new content was built by appending to the **HEAD blob**, hashed with `git hash-object -w` and staged by `--cacheinfo`; **the worktree copy was deliberately NOT written**, so a concurrent peer's tail cannot be clobbered. Landed through the rule-10 private index with insertions-only asserted via `diff-tree` and the post-commit verify |
| C-88 | 2026-08-25 | cfd | **F12 energy-bound discriminator — three arms (0 control / 1 scheme / 2 relaxation) against the flow's own stagnation-enthalpy ceiling.** A completed process under rule 12: all three arms reached a terminal state, were read and were classified under a rule frozen before any of them ran. **IT GRADES NOTHING AND ISSUES NO VERDICT** — rung 1 stays `NOT A RESULT`, rungs 2–5 stay `BLOCKED`, no gate/threshold/cap/label moves — **and it is costed here anyway, because rule 12 costs processes, not verdicts.** 23,040-cell RAE 2822 coarse mesh, `rhoSimpleFoam`, **np = 1 on every arm, so core-min = wall-min throughout**, run serially to keep contention out of the between-arm comparison. Zero GPU | **1.31 core-min = $0.00112 DERIVED**, frozen in `PREREGISTRATION.md` §8 at commit `54acad46` **before any arm was composed**. **Built from a MEASUREMENT OF THE IDENTICAL WORKLOAD, not from a predecessor's estimate**: the terminal-departure probe's own measured **0.422230 core-min** for 148 iterations, 147 ascii field writes, 23,040 cells, 1 rank (`terminal_departure_2026-08-25/evidence/WALL_S.txt`) × 3 arms, plus 0.030 reader and 0.010 planted control. **This is C-83's lesson applied rather than re-learned** — *"a reader's cost is a property of the reader you write, not of the last one someone wrote"* — and the 0.93× below is what applying it buys. Separately registered **runaway guard 10.0 core-min**, a guard reported upward and NOT a budget (Sanaa lifted cost constraints 2026-08-25); it never fired and nothing was trimmed to fit it | **1.212238 core-min MEASURED** = arm 0 **0.464354** (27.861256 wall s) + arm 1 **0.498729** (29.923741 s) + arm 2 **0.187050** (11.222970 s) + readers **0.062105** (0.660297 + 1.157762 + 1.908252 s over three passes), each read back from its own `WALL_S` file on disk, never inferred. **= $0.001036 DERIVED, NOT MEASURED**, at $0.0513/core-h, c7a.4xlarge, **reported-by-owner** — the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` §5) | **1.201233 core-min cleaned** = gross **minus the 0.011005 core-min of named waste below**. The 3600-s stall rule matches nothing: the longest single wall is 29.9 s, more than two orders below it, so no stall cleaning applies and the only difference between gross and cleaned is the waste line. **Waste is named separately and is netted off neither the ratio's explanation nor the gross column** (`COMPUTE_BUDGET_CHARTER.md` §6) | **0.9170× (1.201233 / 1.31)** — cleaned against predicted, per this table's own header. **RECONCILIATION, STATED SO THE TWO RECORDS DO NOT LOOK INCONSISTENT:** `RESULTS.md` §7 quotes **0.9254×**, which is **gross/predicted** (1.212238 / 1.31); this column is **cleaned/predicted** by the header's definition. Both figures are correct for their own definition and neither is a correction of the other. **12.1 % of the 10.0 core-min runaway guard, which never fired** | **MISPREDICTION, small and TWO-SIDED — and the reason it is small is the transferable fact.** Arms 0 and 1 ran **10–18 % SLOWER** than the measured anchor (27.9 s and 29.9 s against 25.3 s) under live contention — three heat-transfer `buoyantBoussinesqSimpleFoam` ranks and two peer cfd lanes, load **3.67–4.01** at launch and exit (`evidence/arm*/CONTENTION_at_launch.txt`, `..._at_end.txt`) — while **arm 2 came in 56 % UNDER** the anchor because it **aborted at iteration 53 and never ran the other 95 iterations**. Those two errors are of opposite sign and largely cancelled; **the 0.92× is therefore NOT a clean measurement of prediction quality and must not be read as one.** The reader was priced at 0.030 and cost **0.062 — a 2.1× under-price**, caused by three passes where one was assumed. **RESIDUAL CONTENTION REMAINS UNMEASURED**: no uncontended control was bought, so the 10–18 % slowdown is *attributed* to contention, not *measured* as it. **WASTE: 0.011005 core-min = $0.0000094 DERIVED** — reader pass 1 (0.660297 s), which returned `P0 FAIL` on a check that **could not have passed**: `PREREGISTRATION.md` §2.3 transcribed a ten-significant-digit control artifact at six decimals and the reader tested exact float equality against the transcription. **Same defect class as the terminal-departure probe's `P4` and ansys-verification's VMFL059 (`6a9afa0a`) — a registered quantity with one unreachable branch — and this one is mine.** Disclosed in `ADDENDUM 1` (`e46d844c`), failing pass preserved, original reader preserved so the repair reads as a diff. **Reader pass 2 (1.157762 s) is NOT waste and is not counted as such**: it verified `P0` on arm 0 **before arms 1 and 2 were composed**, which is the freeze's control-first discipline and is required work. **A `NOT A RESULT`-shaped outcome from arms that ran correctly is a purchase, not waste** (the C-86 principle): the three arms are what established that neither lever removes the excursion, and that finding is the thing the 1.2 core-min bought | `verification/runs/F12_runs/energy_bound_discriminator_2026-08-25/RESULTS.md` §7 (commit `79997b54`); pre-registration `verification/campaign/F12_ENERGY_BOUND_DISCRIMINATOR_PREREGISTRATION.md`, frozen body `54acad46`, `ADDENDUM 1` `e46d844c`; per-arm wall/rc/contention at `energy_bound_discriminator_2026-08-25/evidence/arm{0,1,2}/{WALL_S,RC,CONTENTION_at_launch,CONTENTION_at_end}.txt`; reader walls at `evidence/WALL_S_reader_{P0pass,pass2,final}.txt`; all readings at `evidence/discriminator.json`; the preserved failing pass at `evidence/discriminator_P0FAIL_FIRSTPASS_PRESERVED.json`. Predicted-basis artifact: `verification/runs/F12_runs/terminal_departure_2026-08-25/evidence/WALL_S.txt`. Transient time directories are **outside git** at `/home/ubuntu/certonomous-runs/f12_energy_bound_discriminator_2026-08-25/` (1.6 GB) and are **not recoverable if that directory is removed — stated as a risk, not repaired**; every figure in this row is read from a committed artifact and none depends on it. **LANDING METHOD:** the id was re-derived from the **HEAD blob's own tail**, tolerantly (`grep -oiE 'C-[0-9]{1,4}'`, numeric sort, **maximum existing number, never a count**, `CLAUDE.md` rule 11), **inside the shell invocation that wrote the tree**. **THAT RE-DERIVATION CAUGHT A COLLISION**: an earlier sweep during this lane's work returned max **C-86**, and a peer cfd lane landed **C-87** (`2067d167`) while the arms were running. The worktree copy of this ledger was **stale, not dirty** — byte-identical to `2067d167^`, holding no content absent from any commit, which was **verified before it was refreshed** rather than assumed. The row was therefore built **on the HEAD blob**, not on the worktree copy |
| **C-89** | 2026-08-25 | dafoam | **D7 — ONERA M6 lift-constrained transonic drag minimisation, OPTIMISATION ARMS `P1`+`P2` ONLY. `P1` PASS; `P2` GATE REACHED at its cap with the adjoint three orders down and still converging (KSP 1.839e-01 → 4.841e-04 over 800 iterations), NOT OOM-killed (`inspect(exit,oomkilled)=[1 false]`) and NOT core-starved (3.991 of 4 delivered). Arm `O` BLOCKED by `D7-LAUNCHER-DEF-1` and never launched; `F-S`/`F-P` BLOCKED by `D7-DEF-4`. NO DRAG NUMBER PRODUCED.** Prereg frozen `337d4d84`, instruments+addenda committed `0e229a0a` before the first container; np=4 `scotch`, cpuset 2,3,4,6, SHIPPED row only | **31.5 core-min** for the two arms run (`PREREGISTRATION.md` §8: P1 1.5, P2 30.0; item prediction 791.5 of which 760.0 was never launched) | **60.334 core-min**, from `/home/ubuntu/certonomous-runs/CURRICULUM-D7-a3-m6-cdmin/ledger.txt` (P1 0.267 + P2 60.067) | **60.334 core-min — identical to gross.** No row approaches the 3600-s stall rule; longest wall 901 s | **1.915** | **WASTE: ≈14.6 core-min, NAMED SEPARATELY AND NOT ABSORBED INTO THE RATIO** — the incomplete adjoint killed mid-convergence, producing nothing that enters a record. The other ≈45.5 core-min bought `P2/dRdWColoring_4.bin` (3.08 MB) and `P2/d7_baseline.json` (CD 0.03311805865399452, CL 0.2876130251655752), both on disk and usable, and is NOT waste. **MISPREDICTION, and it is localised: P2's registered basis reads "13.60 (adjoint) + 2 × 3.38 (primals) + coloring build" — THE COLOURING BUILD CARRIES NO NUMBER and is the largest single cost in the arm.** Setup+primals+colouring consumed 682 s of the 901 s wall (≈45.5 core-min), leaving the adjoint 219 s (≈14.6) against its predicted 13.60 — **the adjoint prediction was approximately right and the unpriced colouring term is essentially the entire 2× overrun. A cost basis with an unpriced term cannot be calibrated.** P1 overpredicted 5.6× (1.5 vs 0.267), a 4-s arm. **CONTENTION: excluded by measurement**, 3.991 of 4 cores delivered, `max_nr_throttled=3467`. Dollars ≈$0.052 **DERIVED at $0.0513/core-h, NOT MEASURED** | `cases/dafoam/ladder-a/A3/curriculum_D7/RESULTS_optimisation_arms.md`; blockers `D7_DEF4_SCALER_BLOCKER.md`, `D7_LAUNCHER_DEF1_G8_TOKEN.md` |
| **C-90** | 2026-08-25 | ansys-verification | **VMFL022 — Nurick sharp-edged orifice, Case B, three-level cavitating Roache family. Process COMPLETE in rule 12's sense: all three levels reached `endTime`, were graded by the frozen comparator and received a verdict (`NOT A RESULT`, Roache triple `OSCILLATORY`, R = −0.104878).** A `NOT A RESULT` is a completed process and is costed like any other — the three levels are what established that cavitation onset is mesh-dependent, and that finding is what the spend bought. `interPhaseChangeFoam`/SchnerrSauer, np = 1 throughout, so **core-min = wall-min on every figure**. Zero GPU. Register row #17 | **22 core-min**, frozen in the pre-registration at commit `605d5593` **before any compute** (freeze 21:31:35Z, first run artifact 21:32:35Z — verified by the supervisor, 60 s apart) | **8.43 core-min MEASURED**, read from the run's own clocks = **$0.007208 DERIVED, NOT MEASURED** at $0.0513/core-h, c7a.4xlarge, **reported-by-owner** (`COMPUTE_BUDGET_CHARTER.md` §5) | **= gross, 8.43 core-min.** No level approaches the 3600-s stall rule, so gross and cleaned coincide. Waste is named separately below and netted off neither column nor the ratio (§6) | **0.383× (8.43 / 22)** — a **3× OVER-estimate**, i.e. the run was far cheaper than predicted | **MISPREDICTION, single-sided and localised to L3, and the cause is worth carrying: the estimate priced the finest level as if it would need the full transient to settle, and it did not.** The gate is a time-averaged Cd over a settled window (t ∈ [0.042, 0.06]), and the settled window was reached early at every level. **The transferable fact: for a transient case gated on a time-AVERAGE over a settled window, the cost driver is the SETTLING TIME, not `endTime`** — a budget built from `endTime` alone over-predicts, and by threefold here. **CONTENTION: NOT separately measured and not assumed away.** Three heat-transfer `buoyantBoussinesqSimpleFoam` ranks plus a peer ansys lane were live throughout; no uncontended control was bought, so no contention figure is claimed and the 0.383× is not a clean measurement of prediction quality in that respect. **WASTE: NONE ATTRIBUTABLE TO THIS ROW.** The duplicate-dispatch collision (a rival lane building and running the same case) **was the supervisor's error and its compute is NOT charged here** — this row costs only the committed build `605d5593` that the verdict rests on. The rival's spend is real and is disclosed in the ruling rather than folded into this ratio, because charging one lane's cost to another's verdict would corrupt both figures | `verification/runs/ansys_verification/VMFL022/GRADING_VMFL022.json`; ruling `docs/ansys_verification/VMFL021_022_COLLISION_RULING.md`; lane record commit `2e83a89b`. **LANDING METHOD:** the id was re-derived from this file tolerantly (`grep -oE '\*{0,2}C-[0-9]+'`, numeric sort, **maximum existing number, never a count**, rule 11 — the bold form is matched because plain regexes have missed real rows here) **inside the shell invocation that wrote the tree**. The derivation returned **C-89**, not the C-83 this supervisor was briefed with — **the brief was stale by six rows, which is exactly why ids are allocated at append time against HEAD and never carried in a brief** |
| **C-91** | 2026-08-25 | dafoam | **D7 — `D7-LAUNCHER-DEF-1` repaired under §2d.1 (`d7_g8_token.py`, zero bytes of any frozen file edited; 5 controls, C1/C2/C5 all DEMONSTRATED FIRING; selftest 4/4) and arm `O` FIRED. The repair worked — `O` cleared the cap assertion, the 16.0 GiB memory floor at 26.66, all three instrument md5s, the image digest and the G8 token gate, and derived `CL_target=0.2876130251655752` from disk — then ABORTED at exit 5 on the colouring cache. `BLOCKED`. The second obstruction is a GUARD WORKING, not a defect: the publish at `:428` is gated on `rc=0` and `P2` returned `rc=124`. Arm `O` is STRUCTURALLY UNREACHABLE under this freeze; the path is a NEW pre-registration** | **0.0 core-min** for arm `O` — Addendum 1 §A1.4 registers the launch gate as refusing *before any rank is claimed*, so a refused arm is predicted to cost nothing | **0.000 core-min** — no container was created (`docker ps` count 0); repair + selftest <0.1 core-min serial np=1 on artifacts already on disk | **0.000 core-min — identical to gross** | **1.000 — prediction and actual agree exactly** | **WASTE: 0.0 core-min, NAMED SEPARATELY AND NOT ABSORBED** — nothing was re-run, nothing abandoned, no container created. **This row is a CONTROL ON THE LAUNCH GATE ITSELF, not a compute estimate**: it confirms by measurement that a pre-launch refusal costs zero, which is the property the gate was registered to have. **NO CONTENTION, none claimed.** Dollars $0.00 **derived, not measured** | `cases/dafoam/ladder-a/A3/curriculum_D7/D7_LAUNCHER_REPAIR_AND_ARM_O.md`; ruling `f340e4d2` |
| C-92 | 2026-08-25 | ansys-verification | **VMFL036 — Laminar Flow Past Sphere, BOTH ARMS graded and the case closed (Arm A `GATE REACHED`, Arm B `NOT A RESULT` by registration).** A completed process under rule 12: both arms reached a terminal state and were graded under a comparator frozen before compute. | **70.0 core-min** (35.0 per arm), from the FROZEN pre-registration line 12 and nowhere else: a MEASURED throughput of **3.07e5 cell-iterations/s** × the three levels' cell-iteration counts (3.07e7 / 1.23e8 / 4.92e8) → 100 / 400 / 1 601 s per arm. **Cap 120 core-min PER ARM (240 total)**, ~3.4x the estimate, enforced in the executable path by `timeout_s = remaining_core_min × 60 / RANKS` with running drawdown. | **82.40 core-min measured** = 1.3733 core-h = **$0.0705 derived**. Arm A 41.43 (L1 1.37 + L2 6.67 + L3 33.40), Arm B 40.97 (1.35 + 6.50 + 33.12), each from that level's own `RUN_RC.txt` `wall_s`. **`ranks = 1`** on every row (serial `simpleFoam`, no decomposition), so core-min = wall-min. | **= gross, 82.40 core-min.** The largest row is **2 004 wall s**, 1 596 s (44%) under the 3 600 s stall rule, so the rule matches nothing and there is nothing to clean out. **ZERO WASTE**: no level killed, none re-run, none discarded; the launcher's pre-flight smoke tests are **explicitly refunded** from the budget in the executable path, so machinery never eats the graded run's allowance. | **1.18x** gross/predicted (82.40 / 70.0). No overrun — each arm used 41.4 of its own 120 core-min cap, leaving ~79 core-min unused. | **MISPREDICTION, and specifically a CACHE-RESIDENCY one — that is this row's calibration content.** The registered 3.07e5 cell-iter/s basis was measured on the **3 072-cell L1**, which is cache-resident. Against it: **L1 0.80x, L2 0.98x — the basis is good where it was measured** — but **L3, at 49 152 cells, came in at 1.25x**, and a mid-run sample under five concurrent solvers read as low as **1.49e5 cell-iter/s (2.1x slower)** before recovering to 2.8e5 as the box drained. **CONTENTION NAMED BUT NOT CLAIMED**: the L1→L3 degradation is present in the clean-window numbers too, so it is not purely contention, and this row does not attribute the split between cache residency and contention because the runs as recorded cannot separate them. **CALIBRATION LESSON: a throughput basis taken on the COARSEST level under-predicts the FINEST by ~25% because the coarse level fits in cache and the fine one does not — state the CELL COUNT a throughput basis was measured at, beside the basis, exactly as C-18 asks for the ITERATION COUNT.** A second, softer lesson: a mid-run throughput sample taken during a level's start-up transient read all of VMFL023's levels as cap-crossing when a clean 90 s re-measurement showed all three fitting — **do not cost from a transient sample.** | `cases/ansys_verification/VMFL036/RESULTS.md` §8; pre-registration frozen at `ff9e28da` (line 12), comparator `8a1aea3bd6f0dcd1d720c54a92f76b780924d028`; per-level figures from `verification/runs/ansys_verification/VMFL036/{A,B}/*/RUN_RC.txt` |
| C-93 | 2026-08-25 | dafoam | **Curriculum D12-proper PHASE 1 — `NOT A RESULT`, ARRESTED BY THE LANE after 19 of 23 stages returned `rc=1`.** 2,450-cell 2D cylinder, `DAPimpleFoam` unsteady adjoint, **np=1 (`numberOfSubdomains 1`), core-min = wall-min**, shipped image `sha256:9d45679d…f07fc` asserted by ID before any spend. Zero GPU, zero patched row | **~52 core-min** for the S0–S7 phase-1 portion of the 250.4 core-min whole-item figure frozen in `PREREGISTRATION.md` §6 at `8395faff` **before any container started**. Runaway guard **600.0** total / **350.0** on S8, asserted by the launcher against the value the pre-registration names | **12.0332 core-min MEASURED**, summed from `ledger.txt`'s own per-stage `core_min` fields over 23 stages. **= $0.010288 DERIVED, NOT MEASURED**, $0.0513/core-h, c7a.4xlarge, **reported-by-owner** — the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` §5) | **= gross, 12.0332 core-min.** Longest single wall 447 s, an order below the 3600-s stall rule, so stall cleaning matches nothing and gross and cleaned coincide | **NO RATIO IS QUOTED, AND THAT IS DELIBERATE: the process did NOT COMPLETE, it was ARRESTED, and a ratio against an arrested phase would be a fabrication wearing a decimal point.** The guard never fired — spend was **0.020×** of 600.0 | **FOUR PRODUCER DEFECTS, ALL IN THIS LANE'S OWN LAUNCHER, PLUS ONE MIS-SPECIFICATION IN THE FROZEN PRE-REGISTRATION. WASTE: 2.7165 core-min = 22.6 % of the spend**, the 19 `rc=1` stages, **named separately and netted off neither column nor any ratio** (§6). (1) **`FIELD_B` CANNOT START A SOLVE** — S2's per-step time dirs hold four fields (`U nuTilda p phi`) and a solve needs `nut`; the launcher froze a per-file md5 manifest over it and **re-asserted that manifest faithfully before all 23 stages**, so it was **internally perfect and externally false**. (2) **the age guard cannot arm**: `writeCompression on` makes every field `U.gz`, so the datum `0/U` never exists and the guard returned `None` on every stage after S1a — **it failed SAFE, because `None` was deliberately made a refusal, but a guard that cannot see its own datum is a guard in name only**. (3) **`obj_from_log` reads `CL`, not `CD`**: the solver prints `average:` on both lines and §5 freezes *"the LAST `average:` value"*, so the clause **could never have passed** — measured on S1b the correct `CD:` reading equals the JSON `obj` **to the last digit** while the frozen reading is off by **93.9 %** against a `1e-12` tolerance (the **VMFL059** mis-specified-gate class). **The comparator is NOT affected**: its anchored `^CD:` reader returned 2,400 samples with **zero negatives**, a *positive* demonstration of zero `CL` contamination. (4) `Time =` count is not a valid step proxy for the steady `PYDAFOAM` stage (6 lines vs 500); the limb is exact on the unsteady stages (200/200, 2400/2400). **THE REUSABLE FACT IS NOT A DEFECT — A PRE-COMPUTE PREDICTION WAS REFUTED AND THE FROZEN CODE CAUGHT IT**: a registered ≈50-timestep period (St≈0.2) measured at **18.9955 steps, St 0.5264, 0.38× the prediction**, giving `W/period = 15.7932`, so **`δ_window` is LIVE at `1.274958e-03` and NOT degenerate** — the degeneracy branch, decided from the **measured** period rather than the predicted one, **declined to fire**. Contention **UNMEASURED** (load 13–16 of 16 throughout, no uncontended control); `OOMKilled false` on all 23 stages; `MemAvailable` never below 16.9 GiB against a registered 14.0 floor | `cases/dafoam/curriculum_D12/PHASE1_ARREST_AND_DEFECTS.md`; run root `/home/ubuntu/certonomous-runs/CURRICULUM-D12-cylinder-unsteady/` **preserved byte-for-byte**; `ledger.txt`, `manifest.jsonl` |
| C-94 | 2026-08-25 | dafoam | **D4 (A2) arm ACC — the D4-DEF-4 acceptance primal, the precondition of the repair freeze.** One primal at the corrected (÷scaler) design point, np=4, PATCHED IDWarp. `PASS` on the pre-registered gate ACC-1: `CD = 0.021130918911049287` vs arm O's IPOPT objective `2.1125978108239574e-02`, `rel_CD = 2.33873328108105e-04` against a `1.0e-3` band frozen at `5ed02071` **before the primal ran**. | **20.0** core-min, frozen in `D4_DEF4_REPAIR_PREREGISTRATION.md` §7a, cap 80.0 | **3.0** core-min MEASURED from `acc_ledger.txt` (wall 45 s × 4 ranks); no row over 3600 wall s so gross = cleaned | **3.0** | **0.150** | **MISPREDICTION BY THE LANE, and the lane's estimate was WORSE than the supervisor's.** I registered 20.0 and wrote into the frozen pre-registration that the brief's ~0.9 was *"low by more than an order of magnitude"*. **I was high by 6.7×; the brief was low by 3.3×; the brief was closer.** Mechanism, named so the next estimate is better: **I priced a COLD setup, but the ACC tree was staged `cp -a` from `O/` and was therefore ALREADY DECOMPOSED** — `decomposePar` refused, the setup was warm, and the primal itself was 24.3 s of the 45 s wall. **A tree staged from a finished arm is not a cold arm and must not be priced as one.** No contention (`siblings_pre=[]`); no waste. **$0.0026 DERIVED.** | `cases/dafoam/ladder-a/A2/curriculum_D4/RESULTS.md` §11.2, §11.8; `D4_DEF4_REPAIR_PREREGISTRATION.md` §7a; `acc_ledger.txt` |
| C-95 | 2026-08-25 | dafoam | **D4 (A2) arm F2 — arm F re-run attempt 1. FAILED, `rc=1`, and the failure was NOT the units repair.** Both baseline primals converged and reproduced the acceptance primal's `CD` to all 17 digits; CONTROL P and CONTROL B both passed. It then died in `prob.compute_totals` on **D4-DEF-6** — `renameSolution` collided with arm O's 84 pseudo-time directories that `cp -a` carried into the staged tree. | 53.0 core-min (`PREREGISTRATION.md` §8 arm-F row, unchanged), cap 120.0 | **3.733** core-min MEASURED from `f2_ledger.txt` (wall 56 s × 4 ranks) | **0.000 delivered** | **— RATIO WITHHELD** | **NO RATIO, AND THE OMISSION IS THE POINT.** Arm F2 **delivered no FD table**, so 3.733/53.0 would compare a cost against a prediction for work that was never done. Precedent `c21ada18`, where arm F's 1.0-vs-53.0 was withheld for the same reason. **The 3.733 is WASTE, named separately and NOT absorbed into any ratio** (`COMPUTE_BUDGET_CHARTER.md` §6). **Attribution: 100 % INSTRUMENT DEFECT** — a staging defect inherited from `d4_stage_F.sh`, which means **arm F could never have completed even with correct units**; the earlier units crash MASKED it. Not OOM (`OOMKilled false`), not a cap-stop (56 s of an enforced 1800 s), no siblings. **$0.0032 DERIVED.** **What it bought: the triage that found D4-DEF-6.** | `RESULTS.md` §11.8; `D4_DEF4_REPAIR_PREREGISTRATION.md` ADDENDUM 2; `f2_ledger.txt` |
| C-96 | 2026-08-25 | dafoam | **D4 (A2) arm F3 — the endpoint FD table, COMPLETED.** `rc=0`, np=4 `scotch`, PATCHED IDWarp, five registered components graded, aggregate vector-relative error **0.1634 %** against a 5.0 % band, zero sign flips. **G5, the bright line, `PASS`.** | **53.0** core-min (`PREREGISTRATION.md` §8 arm-F row, carried over UNCHANGED, not re-derived), cap 120.0 | **47.267** core-min MEASURED from `f3_ledger.txt` (wall 709 s × 4 ranks); no row over 3600 wall s so gross = cleaned | **47.267** | **0.892** | **GOOD PREDICTION — and good for a stated reason, not by luck.** The 53.0 was priced on an arm that KEEPS its adjoint colouring, and the D4-DEF-6 staging repair was deliberately written to remove **outputs only**, leaving `dRdWColoring_4.bin` in place **so the arm was not silently repriced**. `compute_totals` took 112.6 s of the 709 s. **Contention present but not limiting:** a D12 sibling container appeared mid-run and `max_nr_throttled` reached 2070, yet **delivered cores measured 3.9752 of the 4-core quota** — read from the container's own cgroup, not inferred from the quota flag. No waste, no overrun, no cap moved. **$0.0404 DERIVED.** | `RESULTS.md` §11.3, §11.8; `f3_ledger.txt`; `ARMF3_d4_fd_endpoint.json` |
| C-97 | 2026-08-25 | dafoam | **D4 (A2) — CURRICULUM ITEM CLOSED at `GATE REACHED`.** MACH wing constrained CD minimisation at fixed CL: `EXIT: Optimal Solution Found.`, 80 majors, **28.675762195558196 %** drag reduction, endpoint FD table graded, bright line crossed. Seven arms: P1, P2, O, F (crashed — D4-DEF-4), ACC, F2 (crashed — D4-DEF-6), F3. | **598.0** core-min for the work actually delivered (P1 1.0 + P2 33.0 + O 511.0 + ACC 20.0 + F3 53.0), item ceiling **800.0** | **602.866** core-min TRUE item total = 548.866 (P1+P2+O+F, `ledger.txt`) + 3.0 (ACC) + 3.733 (F2) + 47.267 (F3) | **598.133** cleaned = true total minus 4.733 core-min of waste | **1.0002** | **ESSENTIALLY EXACT ON DELIVERED WORK, and two disclosures keep it honest.** (1) **WASTE = 4.733 core-min**, named separately and never absorbed: arm F 1.0 (bought D4-DEF-4) + arm F2 3.733 (bought D4-DEF-6). **$0.0040 DERIVED.** (2) **G10 GRADES 548.866, NOT 602.866, AND THE DIFFERENCE IS THE LANE'S TO DISCLOSE:** `d4_grade_SUPPLEMENT.py:630` refuses G10 with `unregistered_arm_in_ledger` on any arm outside the four registered ones, so ACC/F2/F3 were ledgered in **separate files** to avoid MOVING A GATE — with the consequence that **G10's figure is a SUBTOTAL** and the true item figure is the one in this row. No overrun; no cap moved. Item **$0.5155 DERIVED**. | `RESULTS.md` §11.8, §11.10; `ledger.txt`, `acc_ledger.txt`, `f2_ledger.txt`, `f3_ledger.txt` |
| **C-98** | 2026-08-25 | ansys-verification | **VMFL033 — Viscous Heating in an Annulus, three-level radial Roache family GRADED and the case CLOSED as `NOT A RESULT`.** A completed process under rule 12: all three levels reached `endTime` with `rc = 0`, were graded by a comparator frozen before compute, and received a verdict from the fixed vocabulary. A `NOT A RESULT` is a completed process and is costed like any other. | **8.4 core-min** registered on the FROZEN pre-registration's line 12 (5.6 raw + a ×1.5 contention allowance), from a throughput of 1.07e5 cell-iter/s measured on a 128-cell smoke; source `cases/ansys_verification/VMFL033/PREREGISTRATION.md` @ `9b0b573c` | **2.0667 core-min MEASURED** from each level's own `RUN_RC.txt` (L1 0.4167 + L2 0.6333 + L3 1.0167); `ranks = 1` so core-min = wall-min; gross = cleaned at 2.0667 (no row near the 3600-s stall rule) | **0.246×** (0.37× against the raw 5.6 basis) | **MISPREDICTION — a SHORT-RUN BASIS, and it is the OPPOSITE direction to C-92's.** Measured throughput RISES with mesh size: 2.05e5 / 2.69e5 / 3.36e5 cell-iter/s at 256 / 512 / 1024 cells, against a 1.07e5 basis taken on a **50-iteration** smoke. **A throughput basis from a very short run is dominated by fixed startup and I/O cost, not steady-state throughput, and UNDER-predicts a long run — here by 3×. State the ITERATION COUNT beside the cell count C-92 already requires.** **C-92's cache-residency effect did NOT repeat, exactly as predicted in advance** on line 12 (no level leaves cache at 1024 cells), and the upward throughput trend confirms it. **CONTENTION named separately and NOT the driver**: box load ~12.7 of 16 throughout, yet every level beat its estimate. | **WASTE 1.0334 core-min, NAMED SEPARATELY AND NEVER ABSORBED INTO THE RATIO** (COMPUTE_BUDGET §6): abandoned attempt 1 (L1 0.4167 + L2 0.6167 + an L3 fragment of ZERO solver iterations), stopped when the supervisor's mutation test showed the frozen comparator's selftest could not fail; the whole attempt was abandoned so the graded family ran entirely under the repaired comparator, and it is **PRESERVED, not deleted**, at `verification/runs/ansys_verification/VMFL033_attempt1_ABANDONED_PARTIAL/`. Gross including waste 3.1001 core-min. | **$0.00265 DERIVED** at $0.0513/core-h — derived, not measured (`COMPUTE_BUDGET_CHARTER.md` §5) | `cases/ansys_verification/VMFL033/RESULTS.md` §7 |
| **C-99** | 2026-08-25 | heat-transfer | **K0d L1 pair `M1_c` + `M2_c`** (kOmegaSST and RNGkEpsilon, L1, 25 600 cells, `endTime` 40 000, serial) — the ONLY K0d compute that has ever run | **87.00 core-min** (2 × the registered POINT line of 43.50; per-case cap 435.00 = 10× POINT, re-registration §8.1) | **105.10 core-min** (`M1_c` 3 215 s + `M2_c` 3 091 s ClockTime, ranks = 1) | **105.10 core-min** — gross == cleaned, **no stall**: both wall times are under the 3 600 s stall threshold | **1.208** | **CONTENTION, measured not guessed.** Measured solve rate was **3.140e-06** (`M1_c`) and **3.019e-06** (`M2_c`) s/cell-iteration against the registered POINT rate **2.549e-06** — ratios **1.232** and **1.184**, reproducing the 1.208 overall almost exactly, so the gap is RATE, not iteration count. The box carried 5–9 foreign solvers throughout (`pimpleFoam`, `simpleFoam`, `interPhaseChangeFoam`, a 4-core DAFoam container); measured occupancy at launch 8.10 of 16 cores. **NOT misprediction of the work — cell count and iteration count were exactly as registered.** **WASTE, named separately per `COMPUTE_BUDGET_CHARTER` §6 and NOT folded into the ratio: ~0.17 core-min** of meshing across two failed builds (missing `FoamFile` headers; `0/U` missing `uniform`) — **zero solver iterations wasted, both died before any solve.** Derived **$0.0899** at $0.0513/core-h, **DERIVED NOT MEASURED**; **3.82 %** of the rung's 2 748.64 core-min ceiling (§A2.2b). **The rung earns NOTHING from this spend: verdict `BLOCKED`, every graded row `NOT A RESULT`** on three independent grounds, and both cases are **NOT DONE** under the strict completion rule because no `STATUS` file was written at launch. | `docs/campaigns/F14-cooling-ladder/K0d_FORENSICS_2026-08-25.md` Addendum 4; `verification/runs/F14-cooling-ladder/K0d_runs/{M1_c,M2_c}/log.solve` |
| **C-100** | 2026-08-26 | dafoam | **D12R phase 1 — THE COMPUTE COMPLETED IN FULL, THE GRADE REFUSED, AND THE TWO FACTS ARE INDEPENDENT.** All 33 stages in `ledger.txt` returned `rc=0`, `docker_exit=0`, `oomkilled=false`, with `last_time == endTime` on 32/32 manifest rows and `time_line_count == expected_steps` on 31/31 unsteady rows. The frozen comparator (`d12x_grade.py`, blob `b1602722`, verified identical to `f9c8b9c8` before running; selftest `rc=0`, 72/72 units, 0 failures) then exited **2** on `REFUSAL: G12R-0 S1a: JSON status=None`. **Phase 1 is `NOT A RESULT`; no gate after `G12R-0` ran and no number was produced.** Cause is an instrument-pair defect, not a solve failure: the launcher writes `status = None` for a `shell`-task stage by design and the comparator refuses any status that is not `COMPLETE`, both frozen at the same commit | **~70.0** core-min (`PREREGISTRATION.md` §8), against the registered total guard `CAP_CORE_MIN = 600.0`. **Note for anyone re-reading §8: its `$0.2308` is the WHOLE ITEM at ~270 core-min, NOT phase 1** | **63.95** core-min MEASURED from `ledger.txt`'s `PHASE1_COMPLETE spent=63.95 core-min`. Cross-checked against the manifest's own `core_min` column: 32 rows sum to 63.8833, plus `S0` at 0.0667 (`ledger.txt:12`) = **63.95 exactly** | **63.95 — identical to gross.** Longest row is `S5` at 635 wall s, so **no row matches the charter §2 3600-s stall rule** and there is nothing to clean | **0.914** | **A GOOD PREDICTION, AND THREE DISCLOSURES KEEP IT HONEST.** (1) **DO NOT OVER-CLAIM THE 0.914.** The ~70 was priced from the SUPERSEDED item's measured stages, and this item deliberately CHANGED the stage graph (`S2a` was added so `FIELD_B` comes from a final write). Landing 9 % under a prediction built on a different graph is closer to coincidence than to estimating skill, and is recorded as such. (2) **A PRIOR CONTENTION READING IS RETIRED HERE, NOT CARRIED:** an earlier reading inferred ~2.3x contention from a belief that `S2b` was mid-flight in a ~2 h run. `S2b` finished at 22:59:08 after 633 s and phase 1 finished 50 min later at 23:49:35 — **the estimate overshot by about 2x and must not be propagated.** Contention was present but never limiting; no stage stalled, none was re-run, nothing OOMed, no cap was moved and no overrun occurred. (3) **THE SPEND CURRENTLY BUYS ZERO GRADED OUTPUT** — and the ratio is nevertheless STATED, not withheld, which is a departure from the `C-95` precedent and is deliberate: `C-95` withheld because the *work* was never done, whereas here **all 33 registered stages ran to completion and their artifacts are preserved byte-for-byte**; the ~70 priced the COMPUTE, the compute is what was delivered, and the grading that failed costs no compute. **WASTE: 0.000 core-min, NAMED SEPARATELY AND NOT ABSORBED INTO THE RATIO** (`COMPUTE_BUDGET_CHARTER.md` §6) — nothing abandoned, nothing re-run. **CONTINGENT AND FLAGGED, NOT BOOKED:** should the supervisor's ruling require phase 1 to be re-run, the whole 63.95 converts to waste and a NEW row will say so — it is **not** pre-absorbed here. Grading and the selftest cost **no compute**. **$0.0547 DERIVED, NOT MEASURED**, at $0.0513/core-h, c7a.4xlarge, reported-by-owner (`COMPUTE_BUDGET_CHARTER.md` §5 — the box cannot read its own billing) | `cases/dafoam/curriculum_D12R/PHASE1_GRADE_REFUSAL.md` (commits `9d26801e`, addendum `60e838aa`); `ledger.txt`; `manifest.jsonl`; pre-registration `f9c8b9c8` |
| **C-101** | 2026-08-26 | dafoam | **D7R (A3) — ONERA M6 lift-constrained transonic drag minimisation. Arm `O` COMPLETED (`rc=0`, 30 majors, `EXIT: Maximum Number of Iterations Exceeded.`) and the REGISTERED GRADER REFUSED on it, so the rung is `NOT A RESULT`.** A completed process under rule 12: three arms fired (`P1`, `P2`, `O`), `F-S`/`F-P` `BLOCKED` by `PREREGISTRATION.md` §6. | **630.4** core-min, arms fired (`PREREGISTRATION.md` §4: `P1` 0.30 + `P2` 60.1 + `O` 570.0). Arm `O` alone **570.0**, basis *"19.0 core-min/major × 30 majors"*; CAP 900.0, CEILING 3600.0 | **995.999** core-min MEASURED from `ledger.txt` (`P1` 0.733 + `P2` 62.733 + `O` 932.533), and computed independently by the frozen grader's own `g10_caps` as `total_actual_core_min: 995.999`. Arm `O` alone **932.533** | **995.999** — **cleaned == gross: 0 core-min of waste identified.** DISCLOSED so a reader may disagree: arm `O`'s `wall_s = 13988` exceeds charter §2's 3600-s stall trigger, but that trigger is a **stall detector** and arm `O` is a registered long optimisation (`enforced_wall_s 13500`) that terminated on its own `max_iter 30` with `rc=0`, 49 `End` lines and a written success marker. **This row does not clean it, and says so rather than choosing silently** | **1.5799** item (995.999/630.4); **1.6360** arm `O` (932.533/570.0) | **MISPREDICTION, essentially the whole gap, and it is arm `O`'s alone.** The 570.0 basis assumed one primal + one adjoint per major. `O/opt_IPOPT.txt`'s own summary block records **46 objective evaluations and 31 gradient evaluations for 30 majors = 1.53 primals/major**, from IPOPT line-search backtracking. Measured **31.084 core-min/major** against 19.0 predicted = **1.636×**. `P2` came in at **1.0438** (62.733 vs 60.1) and `P1` at +0.43 core-min absolute. **CONTENTION — NOT ATTRIBUTED: NO UNCONTENDED CONTROL WAS BOUGHT**, and none is inferred without one. What IS measured: `delivered_cores_mean=[3.9919 n=925]` of 4 requested = **99.80 % delivered**, `siblings_pre=[]` and `siblings_post=[]` for the whole run — bounding the *sibling-contention* share at **≤ 0.20 %, i.e. ≤ ~1.9 of the 362.533 core-min overshoot**. `max_nr_throttled=52435` is the container hitting **its own** cgroup quota, which four ranks on a four-core quota do as a matter of course and which is **not convertible to lost work without a control** — two different claims, only the first supported. **WASTE — 0 core-min identified, named separately and never absorbed** (`COMPUTE_BUDGET_CHARTER.md` §6): the `H1` preflight refusals (`H1_control_test.log`, `H1_testA/B/C.log`) aborted **before any container ran**. **ONE MEASUREMENT GAP, STATED NOT ESTIMATED:** the `D7-DEF-4` witness probe did run a container (`D7R_DEF4_WITNESS.txt`, `rc=0`) and `d7r_def4_witness_test.sh` records **no core-minutes at all** — that spend is **UNMEASURED**. **CAP OVERRUN, NAMED SEPARATELY AND NOT FOLDED INTO EITHER COLUMN OR THE RATIO:** arm `O` 932.533 against its registered CAP 900.0 = **32.533 core-min, 3.615 % of the cap, $0.0278 DERIVED**; `ceiling_hit=no` (3600.0, 25.9 % used); the run stopped on `max_iter`, not on the cap. **CALIBRATION WIN TO CARRY FORWARD:** §R2 forced the previously unpriced colouring build to carry a number, **24.43 core-min**; D7R's own `P2` log measured it at **24.17** (34.74 s → `Calculating dRdW Coloring... Completed! 397.31 s`, ×4 ranks ÷60) — **ratio 0.99, accurate to 1.1 %**. **CORRECTION LANDED IN THIS ROW:** an earlier roll-up added the colouring build to `P2` as a separate term; it is **inside** `P2`'s 62.733 and doing so double-counts by 24.43 core-min — the item total is **995.999**, not 1,020.429. **Dollars DERIVED, not measured** (`cost_basis`: c7a.4xlarge at $0.0513/core-h, REPORTED-BY-OWNER; the box cannot read its own billing, `COMPUTE_BUDGET_CHARTER.md` §5): item **$0.8516**, arm `O` **$0.7973**. **The frozen grader's `ITEM_CEILING_CORE_MIN = 928.0` is D7's number, not D7R's** (`D7R-GRADER-DEF-6`); D7R registers per-arm ceilings only (32.0 / 480.0 / 3600.0), **none hit**, so 995.999 is not a budget breach | `cases/dafoam/ladder-a/A3/curriculum_D7R/RESULTS.md` §3, §4 (landed `38485e16`); ledger `/home/ubuntu/certonomous-runs/CURRICULUM-D7R-a3-m6-cdmin/ledger.txt` |
| **C-104** | 2026-08-26 | ansys-verification | **VMFL023 — Oscillating Laminar Flow Around a Circular Cylinder, three-level Roache family GRADED, `GATE REACHED`.** St = 0.165993 vs 0.165 (0.6019 %, band 3 %), triple CONVERGING, p = 1.9140, GCI 0.3791 %. A completed process under rule 12. | **210.0 core-min**, frozen in `cases/ansys_verification/VMFL023/PREREGISTRATION.md` lines 164–177; cap **350.0** | **228.667 core-min MEASURED** from each level's `ClockTime` (L1 487 s + L2 1 945 s + L3 11 288 s), `ranks = 1` (launcher `run_vmfl023.sh:41` declares `RANKS=1`, confirmed by `nProcs : 1` in every log) so core-min = wall-min | **228.667** | **1.089** | **GOOD PREDICTION — 8.9 % over, and the cap was never in play** (65.3 % of 350). **THE 3600-s STALL FLAG FIRED ON L3 AND I AM CLEARING IT WITH EVIDENCE, NOT WAVING IT** (rule 12: *a row over 3600 wall s is a stall*): L3 ran **11 288 s**, but `ExecutionTime` **11 258.78 s** against `ClockTime` **11 288 s** is **99.74 % CPU-bound**, so the process was **computing, not stalling** — the flag is a screen and this row clears it. The same ratio **falsifies contention**: 60 000 timesteps at deltaT 0.005 on a 384 × 128 mesh is simply a large serial transient. **$0.195 DERIVED** at $0.0513/core-h — derived, not measured (`COMPUTE_BUDGET_CHARTER` §5). **No waste: zero abandoned levels, zero re-runs.** | `cases/ansys_verification/VMFL023/RESULTS.md`; `verification/runs/ansys_verification/VMFL023/{L1_96x32,L2_192x64,L3_384x128}/log.pimpleFoam` |
| **C-103** | 2026-08-26 | ansys-verification | **VMFL021-R2 — Cavitation Over a Sharp-Edged Orifice, three-level Roache family GRADED, `GATE REACHED`.** Cd = 0.634868 vs Nurick 0.620 (2.398 %, band 5 %), triple CONVERGING, p = 1.7405, GCI 0.5524 %, all levels plateaued and cavitating. | **~40.0 core-min**, frozen in `cases/ansys_verification/VMFL021/R2/PREREGISTRATION.md` lines 104–112; cap **180.0** | **32.933 core-min MEASURED** from `ClockTime` (L1 33 s + L2 206 s + L3 1 737 s), `ranks = 1` serial (declared line 91, confirmed `nProcs : 1`) | **32.933** | **0.823** | **GOOD PREDICTION, 17.7 % UNDER, and the direction is the informative part.** No row approached the 3 600-s stall threshold (worst 1 737 s), and `ExecutionTime`/`ClockTime` is **99.4 %** at L3 — **not contended**. **The R2 estimate was carried from attempt 1, whose defects were in the MACHINERY (a `controlDict` write-control fault and a missing `RUN_RC.txt`), not in the physics or the mesh** — so the work priced was the work done, and the under-run is the ordinary margin of a serial cavitating transient rather than a mispriced case. **This is the calibration value of an R2: a re-registration that changes only the instrument inherits a TESTED cost basis.** **$0.028 DERIVED** at $0.0513/core-h. **No waste in R2 itself**; attempt 1's spend is charged to its own earlier row, not folded in. | `cases/ansys_verification/VMFL021/R2/RESULTS.md`; `verification/runs/ansys_verification/VMFL021/R2/{L1,L2,L3}/log.interPhaseChangeFoam`; `GRADING_VMFL021_R2.json` |
| ~~**C-104**~~ **STRUCK — DUPLICATE ID; RE-ISSUED AS C-114**, see that row and C-111 | 2026-08-26 | ansys-verification | **GPU IDLE WASTE — the lab's `g6.xlarge` has been RUNNING AND DOING NOTHING for 10.90 hours, and this row exists because a supervisor's own measurement error hid it.** Not a run, not a case, no verdict: a **pure waste row**, filed under `COMPUTE_BUDGET_CHARTER` §6 where waste is named separately and never absorbed. | **0.0 GPU-hours** — no GPU work was pre-registered, estimated or authorised for this window. There is no estimate to compare against, and that absence IS the finding | **10.90 GPU-hours MEASURED** — booted **2026-08-25 16:15:42 UTC** (`uptime -s` on the host), read at **2026-08-26 03:09:58 UTC**. **The card did NO work whatsoever**: `nvidia-smi` reports **memory.used = 0 MiB** and **utilization.gpu = 0 %**, and load average is **0.00 / 0.00 / 0.00** | **0.0 delivered** | **— RATIO WITHHELD** | **NO RATIO, AND THE OMISSION IS THE POINT — nothing was predicted and nothing was delivered, so any ratio would be fiction.** **ATTRIBUTION: 100 % LAB FAILURE, and a share of it is mine by name.** A lane of mine measured the instance with `ping`, got 100 % packet loss on all three addresses, and concluded **STOPPED**; I carried that into a written ruling. **`ping` is not a liveness test — ICMP is filtered on that host while `tcp/22` was OPEN the entire time**, which I found only by testing the service port. **The error's direction is the damaging one: "stopped" made the billing invisible.** Standing correction adopted: **host liveness is measured on the SERVICE PORT, never with `ping`.** **$8.77 DERIVED** at the recorded $0.8048/GPU-h — **derived, not measured**, and the rate is the published list price, NOT a console read (`COMPUTE_BUDGET_CHARTER` §5). **This is the SECOND idle-GPU row in three days** — C-16/C-19 recorded 7.88 GPU-h ($6.34) on 2026-08-24 from the same root cause, a fleet kill leaving no agent to notice. **Running total of idle GPU waste: 18.78 GPU-h, $15.11 derived.** **STILL ACCRUING at filing time**: the build that would end it was **DENIED BY THE PERMISSION SYSTEM**, and a live denial is not overridable by any agent instruction (CLAUDE.md rule 9). | `docs/ansys_verification/gpu/RULING_second_instance_measured.md` Amendment 1; `docs/GPU_CAPABILITY_STATE.md` §8; prior rows `C-16`, `C-19` |
| **C-105** | 2026-08-26 | cfd | **F6d Option A — 16-member random-matrix UQ ensemble continuation (+12,000 iterations), `simpleFoam`, serial, 1 core per member. RETROSPECTIVE ROW: the process completed 2026-08-11, twelve days BEFORE Sanaa's 2026-08-23 calibration directive, so no row was owed and this is NOT a late discharge of a duty** — it is filed because the measurement is unusually good and was unrecorded. **The campaign's own gate fired `VOID`** (pre-registration §6.2, the control `d0.2_s000` moving −0.831 x/h against a 0.25 threshold; chief's ruling A at `ce6a66b4`, §6.3, committed before the last five cases were read, with Option C expressly NOT funded). **A cost row is not a verdict and this row does not disturb that `VOID`.** **BOUNDARY NAMED, NOT ANNEXED:** the pre-registration sits in `verification/campaign/` (cfd's folder scope) while the entire run tree sits under `cases/dafoam/` (dafoam's). Filed by cfd because the campaign document is cfd's and an unowned calibration row is how a measurement is lost; routing is the chief's if dafoam claims it | **178.0 core-min** — 152 approved plus **26 for three controls** (`null`, `d0.2_s000`, `d0.2_s027`), frozen in `F6D_OPTION_A_PREREGISTRATION.md` at **`08822fb2`**, 2026-08-11, **before any compute**. The freeze sha was re-derived with `git log --follow`; **`git log -1` without `--follow` returns `a1fbe127` instead, which is not a freeze but MOVE_MAP batch 7, a reorg that `git mv`'d 2,367 tracked files** — see the attribution cell. **= $0.1522 DERIVED, NOT MEASURED**, at $0.0513/core-h, c7a.4xlarge, **reported-by-owner**; the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` §5). The 26 core-min over-ask **was spent: all three controls ran** | **178.600 core-min MEASURED** = **10,716 s of `ClockTime` × 1 rank ÷ 60**, summed over all 16 `log.simpleFoam`. **THIS IS RULE 12's UNIT — wall × ranks ÷ 60 — AND IT IS NOT THE FIGURE FIRST REPORTED.** The lane that established firing summed the same 16 logs' final **`ExecutionTime`**: **10,662.85 s = 177.714 core-min**, and reported ratio **0.998**. **`ExecutionTime` is NOT wall time** — it excludes startup, meshing and sampling — which is the substance of this supervisor's own **F4C-Q1 ruling at `6d996408`**. **= $0.15270 DERIVED, NOT MEASURED** | **= gross, 178.600 core-min.** The longest single member's `ClockTime` is far below the 3,600-s stall rule (`COMPUTE_BUDGET_CHARTER.md` §2), so stall cleaning matches nothing and gross and cleaned coincide. **Waste is named separately in the attribution cell and is netted off neither column nor the ratio** (§6) | **1.0034× (178.600 / 178.0)** — on rule 12's unit. **The previously reported 0.9984× is WITHDRAWN: it divides an `ExecutionTime` sum by a wall-denominated prediction.** **The correction moves the ratio ACROSS 1.0**, so the item changes from a slight under-spend to a slight over-spend, and **the error was in the flattering direction** — the shape this ledger exists to catch | **THE HEADLINE IS THE UNIT, NOT THE MISS. The prediction is the most accurate on this ledger and remains so on either basis; what was wrong was the quantity it was scored against.** **(1) THE CORRECTION, 0.886 core-min**, is exactly the `ClockTime` − `ExecutionTime` gap over 16 members. It is small **because this ensemble ran essentially uncontended**: `ClockTime`/`ExecutionTime` = **1.0050** across the item, against **1.17× / 3.49× / 2.53×** measured on `C-51` under load and **5.85×** on `C-54` at small n. **A 1.005 ratio is the signature of an idle box, and it is why the ExecutionTime error stayed small enough to look like a rounding difference here — on a contended item the same substitution would have understated the actual by a factor, not by half a percent.** That is the transferable point: **the `ExecutionTime`-for-wall substitution is SAFE EXACTLY WHEN IT DOES NOT MATTER, and silently wrong when it does.** **(2) WHY THE RATE PREDICTION WAS THIS GOOD, recorded so it can be copied:** §5's basis was the ensemble's **own measured `cost.json` figures from the same case at the same mesh** — not a rate borrowed across geometry (`C-51`), not an `ExecutionTime` rate extrapolated into a billable core-minute at small n (`C-54`), and not a linear-in-cells meshing model. **A same-case, same-mesh, previously-measured rate is the strongest basis this ledger has recorded.** **(3) MEASUREMENT LIMITS, STATED RATHER THAN ROUNDED AWAY.** OpenFOAM prints `ClockTime` at **integer-second resolution** (the first line of `null/log.simpleFoam` reads `ExecutionTime = 0.81 s  ClockTime = 1 s`), so the sum carries **±16 s = ±0.267 core-min** of quantisation — the ratio is **1.0034 ± 0.0015**. Separately, `ClockTime` is the **solver process's** wall and excludes launcher and queue overhead outside it, so **178.600 is itself a tight LOWER BOUND on true wall × ranks**, not an exact value. Both are stated because a figure quoted without its resolution invites a later reader to over-read it. **(4) WASTE: 0.000 core-min**, named separately per §6 and netted off nothing. All 16 members reached `endTime 16000` with one `End` line each and 26 time directories each; every member's `endTime` fields post-date its own `0/`, so the strict completion rule (rule 4) holds in full. **Nothing failed, nothing restarted, nothing was re-run.** **The gate returning `VOID` is not waste** — a registered gate refusing is the gate working, and the compute that bought the refusal was fully delivered. **(5) THE INSTRUMENT DEFECT THIS ROW SURFACES, AND IT IS LAB-WIDE, NOT cfd's ALONE: `git log -1 -- <path>` WITHOUT `--follow` REPORTS THE MOVE_MAP REORG `a1fbe127` AS THE LAST COMMIT FOR 30 OF cfd's 50 PRE-REGISTRATIONS.** A "frozen at" sha read that way is **a rename, not a freeze**, and any rule-2 freeze check built on it would compare against the wrong blob **with no error and no warning** — the same silent-corruption shape as `git ls-files` reading the decayed index. **Enumerate freezes with `git log --follow -- <path> | tail -1`.** **PROPOSED AND NOT DECIDED** (`CLAUDE.md` rule 9 — an enumeration convention is not a lane's or a supervisor's to impose lab-wide): this belongs beside the `ls-files` finding as a standing enumeration rule. Referred; **this row does not act as though it were ruled.** **(6) A SECOND DEFECT, NOT A COST EVENT BUT IT WOULD HAVE BEEN ONE:** the campaign's launcher `cases/dafoam/f6d_random_matrix_uq/run_option_a_queue.sh` has a stale `ROOT` pointing at `demo-output/website/dafoam/f6d_random_matrix_uq/f6d_option_a`, **which no longer exists** — the same `a1fbe127` reorg moved the tree to `cases/`. Had this campaign been unfired and launched as-is, `mkdir -p` would have built a claims tree under a dead path and every `cd` would have failed. **The same commit that fakes the freeze sha also broke the launcher**, and roughly **140 tracked scripts** still cite that dead tree, including `scripts/launch_solve.sh`, `scripts/case_preflight.sh` and `scripts/dispatch_queue.py`. **Reported, not repaired: most of those paths are other teams' territory and `scripts/` is nobody's.** **(7) CONTENTION: 0.886 core-min, bounded not isolated** — it is `ClockTime` minus `ExecutionTime` and no load reading was taken during the 2026-08-11 window, so process spawn, I/O and any peer load all sit inside that figure undifferentiated. **Named separately, netted off neither column nor the ratio. NO GATE, THRESHOLD, CAP OR LABEL MOVES BY ANYTHING IN THIS ROW, and no frozen file was edited** | Run tree `cases/dafoam/f6d_random_matrix_uq/f6d_option_a/` — 16 × `log.simpleFoam` (every `ClockTime` and `ExecutionTime` figure in this row was read from these files by this supervisor, not relayed). Verdict and campaign closure: `verification/campaign/F6D_OPTION_A_RESULT.md`; `verification/campaign/F6D_OPTION_A_PREREGISTRATION.md` §6.1 (`0a504f63`), §6.2 (`7fedb88b`, the `VOID`), §6.3 (`ce6a66b4`, the chief's ruling and Option C defunded); collision check `F6D_COLLISION_INDEPENDENCE_CHECK.md` (**COLLISION EXCLUDED**, by per-snapshot writer identification — and **three of the five links in §6.1's originally offered chain are false**, recorded there). Freeze **`08822fb2`**, re-derived with `--follow`. **Predecessor rows named, NONE corrected and no figure in any of them moves:** `C-51` (rate borrowed across geometry; contention 62 % of spend), `C-54` (`ExecutionTime` extrapolated into a billable core-minute at small n — **the same substitution as this row's, there in the unsafe direction**), `C-47` (constant per-cell-step rate). **LANDING METHOD:** built from `git show HEAD:docs/COST_CALIBRATION.md`, **never the worktree copy**; the id re-derived from the HEAD blob's own tail **inside the shell invocation that wrote the tree** (rule 11 — the maximum existing number, never a count; it had moved **C-97 → C-101** while this row was drafted) and would have been re-derived again on any CAS retry; staged with `hash-object -w` + `update-index --cacheinfo`; insertions-only asserted by `diff-tree`; landed through the rule-10 private index with a CAS on `refs/heads/main` and the post-commit verify. **The worktree copy was deliberately NOT written**, so a concurrent peer's tail cannot be clobbered, and it was **INSPECTED, NEVER REVERTED** |
| **C-106** | 2026-08-26 | heat-transfer | **T8 entry rung (MTT pure-plume self-similarity) — CASE CLOSED at `NOT A RESULT`.** A completed process under rule 12: three levels fired, all three reached a registered stopping condition, and the rung received a verdict from the fixed vocabulary. **`NOT A RESULT` IS A VERDICT AND IS COSTED LIKE ANY OTHER — a calibration ledger that records only rungs which produced answers is biased toward optimism by construction, and a dead rung's spend is precisely the spend most likely to vanish.** | **335.34 core-min** total on the FROZEN pre-registration §8 (`c` **7.13** + `m` **42.81** + `f` **285.40**), from a borrowed rate of `1.196e5 cell·steps/(core·s)`; CAPS 15 / 80 / 500 = **595** total | **224.667 core-min MEASURED** from each level's own `STATUS.T8_MTT_*` file written by the runner (`c` 3.817 + `m` 2.500 + `f` 218.350); `ranks = 1` throughout, so core-min = wall-min | **224.667 — cleaned == gross, and the decision is DISCLOSED rather than taken silently.** `f`'s `wall_s = 13101` exceeds `COMPUTE_BUDGET_CHARTER` §2's 3600-s stall trigger, but that trigger is a **stall detector** and `f` terminated on its own registered `endTime = 20000` with `rc = 0`, an `End` line and `ExecutionTime = 13065.88 s`. **This row does not clean it, and says so rather than choosing silently** (the `C-101` precedent). | **0.670** total (`c` **0.535**, `f` **0.765**) | **THE HAZARD WAS REGISTERED IN ADVANCE AND ITS DIRECTION IS NOW MEASURED.** Ruling **R6** of the frozen pre-registration named the defect before compute: the `1.196e5` rate was borrowed from `K2bU3_L025`, a **transient PIMPLE** case, while T8 is a **steady SIMPLE-family** run, and §8 explicitly **refused to predict the direction** rather than assert one. **Direction, measured: SIMPLE is CHEAPER per cell-step than the borrowed PIMPLE rate, by 1.31–1.87×** — `c` **2.2356e5**, `m` **1.8534e5**, `f` **1.5632e5** cell·steps/(core·s). **THE DATUM TO CARRY FORWARD: do not price a steady SIMPLE run from a transient PIMPLE rate; on this box the borrow over-predicts cost by roughly 1.6×.** **SECOND DATUM, and it runs OPPOSITE to `C-98`'s:** throughput **FALLS** with cell count here (2.24e5 → 1.85e5 → 1.56e5 across 6.4k → 25.6k → 102.4k cells), so a rate taken on a small case **over-predicts throughput on a large one**; T8 still landed under only because the mode borrow dominated. **`m`'s 0.058 IS NOT A PREDICTION WIN AND MUST NOT BE READ AS ONE:** `m` **crashed (SIGFPE, `rc = 136`) at iteration 1086 of a registered 12,000 — 9.05 % of the work — so its actual is not comparable to its prediction.** Scaling `m` to its full registered iteration count gives ~27.6 core-min against 42.81 predicted; **that figure is an EXTRAPOLATION, is labelled as one, and is deliberately NOT used in the ratio column.** **CONTENTION — NOT ATTRIBUTED: no uncontended control was bought, and none is inferred without one.** **WASTE: 0.000 core-min identified, named separately and never absorbed into the ratio** (`COMPUTE_BUDGET_CHARTER.md` §6) — nothing was abandoned and nothing re-run; every level ran the registered configuration to a registered stopping condition. **CONTINGENT AND FLAGGED, NOT PRE-ABSORBED (the `C-100` precedent):** should a successor require these levels re-run, the whole 224.667 converts to waste and a NEW row will say so. **NO CAP WAS OVERRUN** (25 % / 3 % / 44 % of 15 / 80 / 500). **THE RUNG EARNS NO GRADED OUTPUT FROM THIS SPEND — verdict `NOT A RESULT` on two independent grounds — and that is stated separately here rather than folded into waste.** **$0.1921 DERIVED, NOT MEASURED**, at $0.0513/core-h, c7a.4xlarge, reported-by-owner (the box cannot read its own billing, `COMPUTE_BUDGET_CHARTER.md` §5). | `docs/campaigns/T-family/T8_VERDICT_2026-08-26.md` §6 (commit `0686c7b2`); `T8_STEADINESS_MEASUREMENT_2026-08-26.md` §5; prediction at `T8_PREREGISTRATION.md` §8; actuals in `verification/runs/T-family/T8_runs/STATUS.T8_MTT_{c,m,f}` |
| **C-107** | 2026-08-26 | dafoam | **D12R phase 1 — THE 63.95 core-min IS NOW WASTE, AND THIS ROW BOOKS THE CONVERSION `C-100` REGISTERED AS CONTINGENT.** The dafoam-supervisor ruled on 2026-08-26 **"RE-REGISTER. NOT REPAIR, NOT PATCH."** — D12R phase 1 stands `NOT A RESULT`, the item is re-registered as **D12R2** (pre-registration and instruments committed `e6580910`), and phase 1 is re-run from S0. **`C-100` said the conversion was contingent on this ruling; the ruling came, so it is booked here as a NEW row rather than by editing `C-100`**, per this file's append-only rule 1. **`C-100`'s 0.914 ratio is NOT withdrawn and NOT restated** — it remains the correct estimate-vs-actual comparison for compute that did complete | **—** (no new prediction; this row books a conversion, it does not price work) | **—** | **—** | **— NO RATIO, AND THE OMISSION IS THE POINT** | **WASTE = 63.95 core-min, NAMED SEPARATELY AND NOT ABSORBED INTO ANY RATIO** (`COMPUTE_BUDGET_CHARTER.md` §6). **$0.0547 DERIVED, NOT MEASURED** at $0.0513/core-h, c7a.4xlarge, reported-by-owner (§5 — the box cannot read its own billing). **ATTRIBUTION: 100 % INSTRUMENT-PAIR DEFECT, 0 % SOLVE AND 0 % CONTENTION.** All 33 stages returned `rc=0`, nothing stalled, nothing OOMed, no cap was moved and the run finished 9 % under its prediction. What made the spend unusable is that the frozen launcher and the frozen comparator — **committed together in `f9c8b9c8`** — disagree by design: `d12x_stage_and_run.sh:413` writes `status = None` for a task with no per-stage JSON and `d12x_grade.py:204` refuses anything that is not `"COMPLETE"`. **WHY IT IS WASTE RATHER THAN A RECOVERABLE ASSET:** the artifacts are intact and could have been re-graded by a repaired comparator, and that route was **considered and REFUSED on §2d.1's own terms** — its conditions (3) "quantifies what moved" and (4) "pre-repair values beside the published ones" are satisfiable here **only vacuously, because no number was ever produced**, and *a condition met by an empty set is not met*. **The re-run costs $0.055 and removes a permanent fitting objection**, since any repair authored now is authored knowing exactly which row fails. **WHAT THE 63.95 BOUGHT, and it is not nothing:** three defects — `D12R2-DEF-1` (a stage-kind repair applied to one limb of `G12R-0` and not to its sibling thirty-six lines above), **`D12R2-DEF-2` (33 ledger `STAGE=` lines against 32 manifest rows — S0 did not fail a check, IT VANISHED, and NOTHING REFUSED)**, and `D12R2-DEF-3`. **DEF-2 was found by reconciling this ledger's own cost arithmetic against the manifest**, which is the calibration discipline of `CLAUDE.md` rule 12 catching a verification defect rather than a budget one | `cases/dafoam/curriculum_D12R/PHASE1_GRADE_REFUSAL.md` (`9d26801e`, `60e838aa`); supervisor ruling 2026-08-26; D12R2 `e6580910`; corrects the contingency in `C-100` |
| **C-108** | 2026-08-26 | ansys-verification | **VMFL002 GRADED `GATE REACHED` — Laminar Flow Through a Pipe with Uniform Heat Flux, VM2026R1 p.17. Register row #24.** Axisymmetric 5° wedge, `simpleFoam` + `scalarTransport(T)`, three-level r=2 family (900 / 3600 / 14400 cells), endTime 5000 SIMPLE iters, SERIAL (np=1, so core-min = wall-min on every figure). A full-price run that produced a real GATE-REACHED result. | **11.4 core-min POINT ESTIMATE** (prereg §12, measured-basis pre-flight at loadavg ~9), cap 40 core-min, both frozen in `PREREGISTRATION.md` blob `bda4c9a083184335afa2b921fa6311cdf5d8ff4b` before any solver. **= $0.00975 DERIVED.** | **5.2 core-min MEASURED** (312 wall s × 1 rank ÷ 60), read from `COST.txt` and the three `RUN_RC.txt` (L1 18 s / 0.300, L2 49 s / 0.817, L3 245 s / 4.083). **= $0.00445 DERIVED, NOT MEASURED**, $0.0513/core-h c7a.4xlarge reported-by-owner (the box cannot read its own billing, `COMPUTE_BUDGET_CHARTER.md` §5). | **= gross, 5.2 core-min.** Longest single wall 245 s (L3), 14.7× below the 3600-s stall rule, so gross = cleaned. | **0.456× (5.2 / 11.4) — UNDER the estimate.** 0.13× of the 40 core-min cap; no overrun. | **THE MISS IS A CONSERVATIVE MISPREDICTION, NOT CONTENTION. CONTENTION MEASURED AND NAMED SEPARATELY:** `ExecutionTime`/`ClockTime` per level = **L1 0.617, L2 0.858, L3 0.970** — the dominant graded level L3 ran essentially clean (3% wall inflation), so the 5.2 figure is trustworthy and is NOT materially inflated despite a box loadavg that spiked to ~15-21 during the session; only L1 (0.3 core-min) saw real contention. The estimate was taken from a pre-flight at loadavg ~9 and over-predicted the per-cell-step rate; the actual ran faster/less-contended. **WASTE: 0.000 core-min** — no level failed, none restarted, none capped, comparator returned exit 0 first invocation. **CONTENTION is named here and netted off NEITHER column NOR the ratio** (`COMPUTE_BUDGET_CHARTER.md` §6). | `cases/ansys_verification/VMFL002/RESULTS.md`; artifacts `verification/runs/ansys_verification/VMFL002/` (`COST.txt`, three `RUN_RC.txt`, three `log.simpleFoam`, `GRADING_VMFL002.json`); prereg blob `bda4c9a083184335afa2b921fa6311cdf5d8ff4b`, comparator `027bcbf72eba2177f8b76eec0136f13b6bb75db8` (blob-verified == HEAD before grading); register row **#24**. Number re-derived from the HEAD blob tail in the committing shell (rule 11). |
| **C-109** | 2026-08-26 | ansys-verification | **VMFL004 GRADED `NOT A RESULT` — Plain Couette Flow with Pressure Gradient, VM2026R1 p.21. Register row #25. A FROZEN-INSTRUMENT block, not a physics failure** (the `volAverage(U)_x` gate is a textbook PASS: CONVERGING p=2.0000, GCI_fine 3.9e-5, extrapolated 2.4999999993; the inherited iterative-convergence check gated on Uy/p residuals that are normalization noise for a 1-D fully-developed flow — LESSON L-338). `simpleFoam`, streamwise cyclic, body-force pressure gradient, three-level r=2 (600 / 2400 / 9600 cells), endTime 20000 SIMPLE iters, SERIAL. **A full-price run that produced a refusal is exactly what the lab wants it to cost** (the C-51 / C-106 precedent). | **CLEAN POINT ~2–3 core-min** (prereg §7), slack ×~10 → cap 30 core-min, frozen in blob `0e61889534d0e7180a105190f7ead06e3c00b432`. **= $0.00214 at the clean point, DERIVED.** | **11.4 core-min MEASURED** (685 wall s × 1 rank ÷ 60): L1 21 s / 0.350, L2 100 s / 1.667, L3 564 s / 9.400, from `COST.txt` and three `RUN_RC.txt`. **= $0.00975 DERIVED, NOT MEASURED**, $0.0513/core-h reported-by-owner (§5). | **= gross, 11.4 core-min.** Longest wall 564 s (L3), 6.4× below the 3600-s stall rule, gross = cleaned. | **≈ 4.6× the clean point estimate (11.4 / 2.5); 0.38× of the 30 core-min cap — UNDER cap, OVER the clean estimate.** | **THE MISS IS MISPREDICTION OF THE ITERATION COST, AND IT IS NOT CONTENTION.** `ExecutionTime`/`ClockTime` per level = **L1 1.018, L2 1.001, L3 0.998** — the run was essentially uncontended (the load spikes fell on other levels/teams), so 11.4 is a clean measurement. The clean estimate under-priced 20000 SIMPLE iters: L3 measured 28 ms/iter × 20000 = 564 s. **AND A SECOND, DESIGN CAUSE, NAMED:** the degenerate Uy/p residuals never converge (LESSON L-338), so the run had no early-stop and used the FULL 20000-iter budget every level — the same instrument defect that produced the NOT A RESULT also inflated the cost. **WASTE: 0.000 core-min** (rc=0 throughout, nothing re-run) — though the entire 11.4 bought a NOT A RESULT; a corrected re-run gating on the driven Ux channel is a NEW row (charter §6) and would re-price this. **CONTENTION named, netted off nothing** (§6). | `cases/ansys_verification/VMFL004/RESULTS.md`; artifacts `verification/runs/ansys_verification/VMFL004/` (`COST.txt`, three `RUN_RC.txt`, three `log.simpleFoam`, `GRADING_VMFL004.json`); prereg blob `0e61889534d0e7180a105190f7ead06e3c00b432`, comparator `ddea9d473b6d6092427d29c5997c25af956a7fb6` (blob-verified == HEAD before grading); register row **#25**; LESSON **L-338**. Number re-derived from the HEAD blob tail in the committing shell (rule 11). |
| **C-110** | 2026-08-26 | ansys-verification | **VMFL011 — Laminar Flow in a Triangular Cavity, VM2026R1 p.41. Register row 26. Comparator REFUSED (exit 2) on a mis-calibrated planted-zero control; verdict `NOT A RESULT`.** `simpleFoam`, collapsed-hex triangular cavity, three-level r=2 (800 / 3200 / 12800 cells), endTime 20000 SIMPLE iters, SERIAL. A full-price run that produced a frozen-instrument refusal — priced like any run (C-51 / C-106 precedent). | **~39.3 core-min POINT ESTIMATE** (prereg §7, from an L3 pre-flight smoke at 118 ms/iter measured under box contention), cap 50 core-min, frozen in blob `4bd8c4285e379e93e1ad4e6c2b9967604d042523`. **= $0.0336 DERIVED.** | **8.2 core-min MEASURED** (492 wall s × 1 rank ÷ 60): L1 15 s / 0.250, L2 58 s / 0.967, L3 420 s / 7.000, from `COST.txt` and three `RUN_RC.txt`. **= $0.0070 DERIVED, NOT MEASURED**, $0.0513/core-h reported-by-owner (§5). | **= gross, 8.2 core-min.** Longest wall 420 s (L3), 8.6× below the 3600-s stall rule; gross = cleaned. | **0.209× (8.2 / 39.3) — well UNDER; 0.16× of the 50 core-min cap.** | **THE MISS IS A CONTENTION-CONTAMINATED ESTIMATE, and the actual is CLEAN.** The prereg rate (118 ms/iter) was measured on an L3 smoke while the box loadavg was high; the graded run then ran essentially uncontended — `ExecutionTime`/`ClockTime` = **L1 0.991, L2 1.000, L3 1.000** — so the actual 8.2 is trustworthy and the 4.8× over-prediction is the smoke's contention baked into the estimate, NOT a modelling error. **TRANSFERABLE: do not set a cost estimate from a smoke taken under load; the C-51 finding (a wall-denominated estimate needs an explicit contention term) recurs here in the estimate rather than the actual.** **WASTE: 0.000 core-min** — rc=0 all levels, nothing re-run; the comparator refusal is not a re-run. **CONTENTION on the actual: ~0** (ET/CT≈1.00), named and netted off nothing (§6). | `cases/ansys_verification/VMFL011/RESULTS.md`; artifacts `verification/runs/ansys_verification/VMFL011/` (`COST.txt`, three `RUN_RC.txt`, three `log.simpleFoam`); prereg blob `4bd8c4285e379e93e1ad4e6c2b9967604d042523`, comparator `e369496bf2e28ccb7145756e1c2442eb11e8e3f7` (blob-verified == HEAD); register row **26**. Numbers re-derived from the HEAD blob tails in the committing shell (rule 11). |
| **C-111** | 2026-08-26 | ansys-verification | **CORRECTION ROW — THIS ROW CORRECTS THE ID OF THE `VMFL023` ROW, WHICH LANDED WITH A DUPLICATE NUMBER. It corrects an id, not a figure: every measured number in the rows named below stands unchanged.** Commit `c487e3c7` appended three ansys-verification rows and, through a defect in my own renumbering code, issued them as **C-104, C-103, C-104** — **the id `C-104` twice, and `C-102` never issued at all.** **THE CORRECTION: the `VMFL023` row (the FIRST of the two printed `C-104`) IS TO BE CITED AS `C-102`.** The **GPU IDLE WASTE** row (the second printed `C-104`) **keeps `C-104`**. The `VMFL021-R2` row's `C-103` was always correct. **Neither existing row is edited** — append rule 1 of this file: *an existing row is never edited; a correction is a new row naming the row it corrects.* `C-102` is otherwise unissued, so this creates no second collision. | **n/a — this row records no estimate** | **n/a — this row records no spend** | **n/a** | **— NO RATIO: no compute was consumed by this row and none is attributed to it** | **THE DEFECT, NAMED SO IT IS NOT REPEATED: SEQUENTIAL IN-PLACE RENUMBERING ALIASES WHEN THE NEW IDS OVERLAP THE OLD IDS' NAMESPACE.** My code re-derived the tail maximum correctly (`C-101`) and then applied three ordered string replacements — `C-100 -> C-102`, `C-101 -> C-103`, `C-102 -> C-104`. **Step 1 rewrote row 1 as `C-102`. Step 3 then matched BOTH row 3 (genuinely `C-102`) AND row 1 (which step 1 had just made `C-102`), and renamed both to `C-104`.** The rename collided with its own output. **The rule: never renumber by ordered in-place substitution over a shared id namespace — build a single-pass mapping keyed by position, or allocate the id at write time per row.** Rule 11's tail re-derivation was applied and was CORRECT; **the failure was downstream of it, in the mechanism that applied the derived numbers**, which is why re-deriving the maximum is necessary and not sufficient. **This is the third instrument in one session in this territory whose OUTPUT was wrong while its INPUT reasoning was right** (a `ppid` detachment test, a `RUN_RC` reference-count proxy, and now this) — and the only one of the three that reached a committed record. | `docs/COST_CALIBRATION.md` rows printed `C-104` (twice) and `C-103`, landed at commit `c487e3c7`; register rows #22 and #23 |
| **C-112** | 2026-08-26 | cfd | **F3 SUCCESSOR (grid triples) — PROCESS COMPLETE.** Eight single-rank runs (two full grid triples + a two-armed bit-identity control), three gate rows graded through `scripts/roache_triple.py::grade_ladder` with `iterative_states` and `plateau_states` supplied. **Verdict: 0 PASS · 0 GATE FAIL · 3 NOT A RESULT.** All three rows carried a **band verdict of PASS** and all three were turned to `NOT A RESULT` by rule 5 — two on an `OSCILLATORY` triple, one on limb (1) (β not plateaued at coarse or medium). | **14.7117** core-min expected; **HARD CAP 17.6541**, frozen in `F3_SUCCESSOR_TRIPLE_PREREGISTRATION.md` Annex B at `5891db27` before compute | **15.8298** core-min MEASURED — launcher envelope, wall s × ranks ÷ 60, from `RUN_LEDGER.json`, `cap_respected: true`. ClockTime sum is **15.5667** core-min with a **±0.067 core-min quantisation bound** (ClockTime is integer-second over 8 runs); the envelope exceeds it by 15.79 core-s (1.66 %) for case build, blockMesh, checkMesh and postProcess — all of which `ExecutionTime` would also have excluded. **No row over 3600 wall s, so gross = cleaned.** | **15.8298** | **1.0760** | **MISPREDICTION, NAMED: the instrumentation uplift.** The frozen cap table registered **1.15**; the measured value is **1.2948** (arm B ÷ arm A, the identical case). That drove the single per-run overrun, `wedge/M2.5_th10/fine` at 236.29 core-s against a 230.74 claim (+2.4 %). Mechanism: the wedge series samples a patch **and** six 800-point line sets every 20 steps, and cost scales with step count, so the longer-running M2.5 case (endTime 3.12) overran a figure calibrated on M3.0 (endTime 2.6) — **an uplift calibrated on one case does not transfer to a case with a different endTime.** **CONTENTION, MEASURED SEPARATELY AND NEVER NETTED — and on a provably bit-identical computation, which is the cleanest form available:** arm A re-ran F3's `wedge/M3.0_th15/fine` and produced **bitwise-identical** `p_wall_mean` and `beta`; it took **164.09 core-s tonight against 190.01 on 2026-08-24 — 13.6 % FASTER**. With the arithmetic proven identical that difference is environment, not work, and it offsets part of the uplift misprediction in the ratio. Launcher-measured box load at launch **33.57 % busy** (/proc/stat delta, not loadavg), measured by the launcher in its own invocation. **WASTE: ZERO** — all 8 runs rc 0, none killed, none re-run. **$0.013535 DERIVED** at $0.0513/core-h (expected $0.012578, cap $0.015094) — **derived, never measured**; this box cannot read its own billing. | `verification/runs/F3_runs/successor_triple_2026-08-26/RESULTS.md`; `verification/runs/F3_runs/successor_triple_2026-08-26/F3S_GRADED.json`; `verification/runs/F3_runs/successor_triple_2026-08-26/RUN_LEDGER.json`; `verification/campaign/F3_SUCCESSOR_TRIPLE_PREREGISTRATION.md` Annex B + ADDENDA 1–2 |
| **C-113** | 2026-08-26 | ansys-verification | **VMFL076 (Forced Convection Over a Flat Plate, VM2026R1 p.219) — CASE CLOSED at `NOT A RESULT`. Three levels of `simpleFoam` + the `scalarTransport` FO, laminar, serial, 1 core per level, launched detached and graded against a comparator frozen at `ab168c0e` BEFORE any compute.** A completed process under rule 12: all three levels reached the registered `endTime`, all three passed strict completion, and the case received a verdict from the fixed vocabulary. **`NOT A RESULT` is costed like any other verdict — and this one is the expensive kind to lose, because BOTH frozen gates were MET at the finest level (Gate A 0.900557 % against a 3.00 % band, Gate B 5.396919e-03 against 1.00e-02) and rule 5's `OSCILLATORY` triple took the row anyway.** | **19.82 core-min**, frozen in `PREREGISTRATION.md` line 12 at `ab168c0e` before any solver started: L1 0.42 + L2 2.53 + L3 16.87. **CAPS 10 / 30 / 150 = 190 core-min total**, enforced in the executable path by `timeout_s = cap*60/RANKS`. **= $0.0169 DERIVED, NOT MEASURED** at $0.0513/core-h, c7a.4xlarge, reported-by-owner (`COMPUTE_BUDGET_CHARTER.md` §5). **The basis was itself MEASURED on this box, not borrowed**: a pre-freeze probe ran 2500 SIMPLE iterations on the L1 mesh (9 600 cells, 1 rank) in 31.63 s wall = **1.318e-06 s per cell per iteration**. That probe computed **no gate scalar** — only wall-clock, rc, residual decay and mesh statistics — so it is a cost basis, not a pre-freeze observation of the answer | **48.900 core-min MEASURED** = `wall_s × ranks ÷ 60` from each level's own `RUN_RC.txt` written by the launcher: **L1 0.4500** (27 s), **L2 3.3833** (203 s), **L3 45.0667** (2704 s); `ranks = 1` throughout. **= $0.0418 DERIVED, NOT MEASURED** | **48.900 — cleaned == gross.** No level's `wall_s` reaches the 3600-s stall trigger (`COMPUTE_BUDGET_CHARTER.md` §2); the longest, L3 at 2704 s, terminated on its own registered `endTime = 5000` with `rc = 0`, an `End` line and `ExecutionTime` count == `endTime`. Stall cleaning matches nothing, so the two columns coincide and this row says so rather than choosing silently | **2.467×** total (**L1 1.071×**, **L2 1.337×**, **L3 2.672×**) | **THE DATUM TO CARRY FORWARD: A PER-CELL-PER-ITERATION RATE CALIBRATED ON A SMALL MESH DOES NOT EXTRAPOLATE UP — IT UNDER-PREDICTS, AND THE MISS GROWS WITH THE MESH.** The basis was measured on this box, on this case, at this mesh — the strongest basis class this ledger records (`C-105`) — and **on its own mesh it was excellent at 1.071×**. Realised rates: **1.41e-06 (L1), 1.76e-06 (L2), 3.52e-06 s/cell/it (L3)** — **2.67× the calibration rate at 16× the cells.** *`C-106` recorded the same direction on a different family (throughput falling 2.24e5 → 1.56e5 cell·steps/core·s across 6.4k → 102.4k cells); this row is a second independent instance and the two agree, which is worth more than either alone. It runs OPPOSITE to `C-98`.* **CONTENTION — MEASURED AND PARTLY EXCLUDED, NOT ASSUMED CLEAN AND NOT ASSUMED GUILTY.** Box load ran **13–21 on 16 cores** during L3 (this team's three levels plus three heat-transfer solvers). `ClockTime`/`ExecutionTime` = **0.9840 / 0.9987 / 1.0024** and `wall_s`/`ClockTime` = **1.0000** at all three levels, so **the solver process was NEVER DESCHEDULED and the launcher's wall equals the solver's own clock exactly.** *That excludes CPU starvation ONLY.* **Memory-bandwidth contention moves neither ratio**, so it and GAMG-per-cell growth remain the two live candidates and **are NOT separated here; no uncontended control was bought and none is inferred.** The direction of the evidence favours mesh-size scaling, because L1 ran under the same load at 1.071×. **WASTE: 0.000 core-min**, named separately per §6 and netted off neither column nor the ratio — nothing stalled, nothing was killed, nothing re-run, every level ran the registered configuration to its registered stopping condition. **NO CAP WAS CROSSED** (4.5 % / 11.3 % / 30.0 % of 10 / 30 / 150). **CONTINGENT AND FLAGGED, NOT PRE-ABSORBED (the `C-100`/`C-106` precedent): the record proposes a COARSER triple — the grid family here was refined PAST its own asymptotic range, L2→L3 moving the gate scalar by only 5.78e-05 relative — and if a successor pre-registration re-runs these levels, the whole 48.900 converts to waste and a NEW row will say so.** **THE CASE EARNS NO CREDENTIAL FROM THIS SPEND — not a V and not a P — and that is stated separately here rather than folded into waste.** | `cases/ansys_verification/VMFL076/RESULTS.md` §5 and Addendum 1 (commit `9c86962e`); actuals in `verification/runs/ansys_verification/VMFL076/{L1,L2,L3}/RUN_RC.txt`; clocks in each level's `log.simpleFoam`; prediction and cap frozen at `PREREGISTRATION.md` line 12, commit `ab168c0e`; grading `GRADING_OUTPUT.txt` |
| **C-114** | 2026-08-26 | ansys-verification | **GPU IDLE WASTE — the lab's `g6.xlarge` has been RUNNING AND DOING NOTHING for 10.90 hours, and this row exists because a supervisor's own measurement error hid it.** Not a run, not a case, no verdict: a **pure waste row**, filed under `COMPUTE_BUDGET_CHARTER` §6 where waste is named separately and never absorbed. | **0.0 GPU-hours** — no GPU work was pre-registered, estimated or authorised for this window. There is no estimate to compare against, and that absence IS the finding | **10.90 GPU-hours MEASURED** — booted **2026-08-25 16:15:42 UTC** (`uptime -s` on the host), read at **2026-08-26 03:09:58 UTC**. **The card did NO work whatsoever**: `nvidia-smi` reports **memory.used = 0 MiB** and **utilization.gpu = 0 %**, and load average is **0.00 / 0.00 / 0.00** | **0.0 delivered** | **— RATIO WITHHELD** | **NO RATIO, AND THE OMISSION IS THE POINT — nothing was predicted and nothing was delivered, so any ratio would be fiction.** **ATTRIBUTION: 100 % LAB FAILURE, and a share of it is mine by name.** A lane of mine measured the instance with `ping`, got 100 % packet loss on all three addresses, and concluded **STOPPED**; I carried that into a written ruling. **`ping` is not a liveness test — ICMP is filtered on that host while `tcp/22` was OPEN the entire time**, which I found only by testing the service port. **The error's direction is the damaging one: "stopped" made the billing invisible.** Standing correction adopted: **host liveness is measured on the SERVICE PORT, never with `ping`.** **$8.77 DERIVED** at the recorded $0.8048/GPU-h — **derived, not measured**, and the rate is the published list price, NOT a console read (`COMPUTE_BUDGET_CHARTER` §5). **This is the SECOND idle-GPU row in three days** — C-16/C-19 recorded 7.88 GPU-h ($6.34) on 2026-08-24 from the same root cause, a fleet kill leaving no agent to notice. **Running total of idle GPU waste: 18.78 GPU-h, $15.11 derived.** **STILL ACCRUING at filing time**: the build that would end it was **DENIED BY THE PERMISSION SYSTEM**, and a live denial is not overridable by any agent instruction (CLAUDE.md rule 9). | `docs/ansys_verification/gpu/RULING_second_instance_measured.md` Amendment 1; `docs/GPU_CAPABILITY_STATE.md` §8; prior rows `C-16`, `C-19` |
| **C-115** | 2026-08-26 | dafoam | **D12R2 phase 1 — GRADED, and the FD line CLOSED at `G12R-4` = `NOT A RESULT`.** Re-registered item (`e6580910`) after the supervisor's 2026-08-26 "RE-REGISTER. NOT REPAIR, NOT PATCH." ruling. 33 stages, all `rc=0`; **all nine gates returned a number — the first time this case has produced graded output at all** (D12 phase 1: none; D12R phase 1: none, refused at stage 1 of 32). Headline: **`h_min = 1.742838e-01` exceeds the registered `h_max = 5.000e-02` by 3.49×, so NO ADMISSIBLE FD STEP EXISTS at `W = 300`; `admissible: false`, `steps: []`.** Phase 2 ran the registered branch and launched **nothing** | **70.0** core-min (`PREREGISTRATION.md` §7), guard `CAP_CORE_MIN = 600.0` | **55.5167** core-min MEASURED from `ledger.txt` (`PHASE1_COMPLETE spent=55.5167 core-min`), **independently reproduced** by summing the 33 manifest rows' own `core_min` to 55.5167 | **55.5167 — identical to gross.** Longest row `S5` at **654 wall s**, nowhere near the charter §2 3600-s stall rule | **0.793** | **THE PREDICTION WAS HONEST AND STILL MISSED BY 21 %, AND THE REASON IS WORTH MORE THAN THE RATIO.** The 70.0 was priced **like-for-like** — from D12R's own measured 63.95 core-min on the **identical stage graph** — which was itself the correction to `C-100`, where D12R's 0.914 was flagged as *closer to coincidence than skill* because its ~70 came from a graph it then changed. **So this is the first D12 prediction with a defensible basis, and it came in 13 % under even that basis (55.52 vs 63.95 for the same 33 stages).** **ATTRIBUTION: CONTENTION, and it is the honest reading rather than improved estimating.** D12R ran at 22:45–23:49Z against a busy fleet; D12R2 ran 03:30–04:26Z with only three heat-transfer solvers live and `MemAvailable` at 27.4 GiB throughout. **Identical work, 8.4 core-min cheaper, because the box was quieter — a fact about the night, not about the model.** The lab should NOT read 0.793 as a better estimator; **the like-for-like basis is the improvement, and it predicts 63.95, not 70.0.** **WASTE: 0.000 core-min, NAMED SEPARATELY** (`COMPUTE_BUDGET_CHARTER.md` §6) — nothing stalled, nothing OOMed, nothing re-run, no cap moved, and **phase 2 spent 0.0 by firing its registered no-admissible-step branch instead of a sweep**. **WHAT IT BOUGHT:** a `NOT A RESULT` that is a **RESULT** — the FD bright line is unreachable for this method–case pair at this window, registered in advance in two frozen documents so it cannot be read as an instrument artifact — plus live confirmation of all three D12R2 repairs and one new disclosed finding (**the `W2 = 900` contingency has no execution path in any frozen instrument**). **$0.0475 DERIVED, NOT MEASURED** at $0.0513/core-h, c7a.4xlarge, reported-by-owner (§5) | `cases/dafoam/curriculum_D12R2/RESULTS.md` (`37779471`); `PREREGISTRATION.md` §7 at `e6580910`; `ledger.txt`, `manifest.jsonl`, `step_plan.json`; predecessor rows `C-100`, `C-107` |
| **C-116** | 2026-08-26 | ansys-verification | **VMFL004-R2 — Plane Couette Flow with Pressure Gradient, three-level Roache family GRADED `PASS`.** The re-registration of row #25, which was `NOT A RESULT` on an inherited degenerate-residual convergence check. A completed process under rule 12. | **11.4 core-min**, carried over from VMFL004's measured actual rather than re-derived — the case, mesh family and solver are byte-identical, so the work priced IS the work done | **13.033 core-min MEASURED** from each level's RUN_RC.txt (L1 0.467 + L2 2.133 + L3 10.433), `ranks = 1` serial so core-min = wall-min; no row near the 3600-s stall threshold, gross = cleaned | **13.033** | **1.14** | **GOOD PREDICTION, and the 14 % gap is CONTENTION, named not absorbed.** The box carried loadavg ~13-21 of 16 across this run (heat-transfer, dafoam and a sibling ansys lane), against VMFL004's quieter window. **The cost basis itself is the calibration lesson: an R2 that changes ONLY the instrument inherits a TESTED basis** — VMFL004's own measured 11.4, not a fresh estimate — and it landed within 14 %. **Zero waste; no cap moved; cap 30 never approached at 43 % .** **$0.01114 DERIVED** at $0.0513/core-h — derived, not measured (`COMPUTE_BUDGET_CHARTER` §5). **What it bought: the lab's sixth credential**, at 3.13e-5 relative error against an exact closed form. | `cases/ansys_verification/VMFL004-R2/RESULTS.md`; `verification/runs/ansys_verification/VMFL004-R2/{L1,L2,L3}/RUN_RC.txt` |
| **C-117** | 2026-08-26 | dafoam | **D4-SHIPPED CLOSED `NOT A RESULT` ON ITS GRADER PATH — and 731.667 core-min of arm O is WASTE, named as its own figure.** Arm O (`d4_O_20260826T040414Z_3177545`, SHIPPED digest `9d45679d…`, np=4, cpuset 5,6,7,9, 12g) ran 04:04:14Z→07:07:09Z, inspect exit 0 / OOM false, 100 IPOPT majors, `EXIT: Maximum Number of Iterations Exceeded`; the launching shell died with the fleet at ~05:00Z so no ledger row was written (the exposure ADDENDUM 1 §A1.3 registered OPEN). The frozen grader refuses on the absent row (`LEDGER_RE` cannot parse an honest `memavail_post_GiB=NOT_MEASURED`), and G9 would `GATE FAIL` on the `D4S_`/`D4_` string mismatch (D4S-GRADER-DEF-1, instrument defect). §2d.1 refused by the supervisor (route iii); re-registered as D4-SHIPPED-R. Record: `cases/dafoam/ladder-a/A2/curriculum_D4_SHIPPED/RESULTS.md`. | Predicted 598.133 core-min (prereg §4; O = 511.133 registered as a LOWER BOUND, §4.2), caps P1 5 / P2 55 / O 620 / ACC 80 / F3 120, ceiling 880.0 | **Actual gross 773.467 core-min** (P1 0.667 + P2 41.133 + O 731.667, wall×4 from `docker inspect`/ledger; ACC, F3 not fired) — of which **WASTE 731.667** (arm O: complete, intact, ungradeable); cleaned = gross (no row over 3600 wall s is a stall — O's 10,975 s is the run itself). $ DERIVED at $0.0513/core-h, not measured: gross $0.6613, waste $0.6256 | ratio arms-that-ran 773.467/548.200 = **1.411**; arm O 731.667/511.133 = **1.431**; **cap crossing O +18.0 % (731.667 > 620.0)** named separately | +25 % majors (100 vs 80: §4.2's caveat fired — the shipped gradient is wrong on 7/96 components) × +14.6 % wall/major (peers' d12y containers live throughout; contention NAMED, no uncontended control bought). Waste is the shell-death exposure, not the estimate: the estimate was 1.43× low on O and the row was lost for a reason unrelated to it. Lesson forward into D4-SHIPPED-R: O priced at 731.667 measured-derived; deadline inside the container; bookkeeping in a detached driver. |
| **C-118** | 2026-08-26 | heat-transfer | **T11 EXACT tier — transient conduction in a plane wall (SOLID-ONLY, NOT conjugate), three-level Roache family GRADED `PASS` on all three rows (G1 theta_mean, G2 centreplane, G3 convective face; p = 2.000 / 2.000 / 2.005, GCI 2.9e-07 / 1.4e-06 / 3.4e-06, planted-zero control PASS with a DEMONSTRATED detection floor of 1e-07). A completed process under rule 12. Graded under AMENDMENT 1 (P_MIN = 0.5, committed pre-first-compute at `352aef0d` together with the amended prereg); the amendment did not fire on any row. Temporal-bias control C-T (prereg §7, REPORTED not gated) NOT run in this pass — its case is not a registered level of `launch_t11.sh` and is left PENDING, stated as absent. | **0.123 core-min POINT** (prereg §10: rate 5.255e-07 core-s/cell-step MEASURED on this rung's own coarse case, scaled linearly in cell-steps: c 0.0175 / m 0.0350 / f 0.0701), **cap 14 core-min** (2 / 4 / 8) | **0.050 core-min** from the three `STATUS.T11_PW_*` files (wall_s = 1 each, ranks = 1, 1-s integer resolution so each is a FLOOR; solver-reported `ExecutionTime` in `log.solve` is 0.93 / 1.12 / 1.34 s = **0.0565 core-min**, the truer figure); rc = 0, capped = no, all three DONE under the age-guarded completion rule | **0.050** (0.0565 by ExecutionTime); no row near the 3600-s stall rule, gross = cleaned | **0.41** (0.46 by ExecutionTime) | **MISPREDICTION, over-estimate by 2.2-2.4x, and the lesson is about the cost MODEL not the rate.** The POINT scaled the coarse-case rate linearly in cell-steps, but at N = 100-400 the per-timestep cost is overhead-dominated (20 000 steps, four field writes, tridiagonal solve): the fine level with 4x the cell-steps took 1.34 s against the coarse's 0.93 s, not 4x. Linear cell-step scaling is right for the large-N rungs it was borrowed from and wrong below ~1e3 cells. **Not contention** (loadavg 9.2 of 16 at launch and every level finished inside 2 s). **Waste 0.** Cap 14 never approached (0.36 %); the cap was sized as a hang guard, as §10 says, and did its job by not mattering. **$4.3e-05 DERIVED** at $0.0513/core-h — derived, not measured (`COMPUTE_BUDGET_CHARTER` §5). The absolute spend is a twentieth of a core-minute; the calibration value of this row is the scaling lesson, not the dollars. | `verification/runs/T-family/T11_runs/{STATUS.T11_PW_c,STATUS.T11_PW_m,STATUS.T11_PW_f,gate_t11.json,grade_t11.log,LAUNCH_RECORD}`; prereg + comparator commit `352aef0d` |
| **C-119** | 2026-08-26 | heat-transfer | **T4 — impinging round jet, H/D = 2, Re = 23 000, three-level Roache family (5 184 / 20 736 / 82 944 cells, r = 2, kOmegaSST, `buoyantBoussinesqSimpleFoam`, serial) — RUNG GRADED at `NOT A RESULT` × 3 (G1 DIVERGENT, G2 OSCILLATORY, G3 OSCILLATORY; levels c and f fail the directional C2 not-growing test at gate (1) with residual floors of 5.5e-09 and 9.0e-10 met). Control C1 (plate y+_max < 1.0) FIRED on `c` at y+_max 1.248, the risk AMENDMENT 2 §A2.4 registered; the frozen comparator carries no y+ reader, so C1 is measured outside it (finding). A completed process under rule 12; `[lab-attributed]` lane grading on the supervisor's brief.** | **1 044.58 core-min POINT** (c 26.78 + m 160.70 + f 857.09), CEILING 1 482.62, CAP 1 950 (50 / 300 / 1 600), frozen in `T4_PREREGISTRATION.md` §12 at `88db2d8b` before any solver started; basis 1.55e-05 s per cell-iteration measured on T1b L4's 209 920-cell mesh, same solver, same box | **442.200 core-min MEASURED** = `wall_s × ranks ÷ 60` from `STATUS.T4_IJ_{c,m,f}`: c 10.150 (609 s), m 63.983 (3 839 s), f 368.067 (22 084 s); ranks = 1; `capped=no` on all three; 22.7 % of cap. **$0.3781 DERIVED, NOT MEASURED** at $0.0513/core-h, c7a.4xlarge, reported-by-owner (`COMPUTE_BUDGET_CHARTER.md` §5) | **442.200 — stated equal to gross, departure named:** the §2 3600-s rule matches `m` (3 839 s) and `f` (22 084 s) mechanically, but both ended on their registered `endTime` with rc=0, an `End` line and `ExecutionTime` count == `endTime` (`mark_done_t4.py` DONE ×3), so no stall exists to clean | **0.4233** (c 0.379, m 0.398, f 0.429); 0.298 of CEILING | **MISPREDICTION OF THE RATE BASIS, DIRECTION OPPOSITE TO `C-113`: a per-cell-per-iteration rate calibrated on a LARGE mesh (209 920 cells) OVER-predicts small meshes by 2.3–2.6×.** Realised 5.87e-06 / 6.17e-06 / 6.66e-06 s per cell-iteration against POINT 1.55e-05 — and the realised rate RISES with cell count, the same slope `C-113` and `C-106` measured going the other way; the two directions together say the rate must be calibrated at the target mesh size, not transferred across it. **CONTENTION NAMED, NOT QUANTIFIED:** all three arms ran beside K0f `M1_f` and the box's other solvers (13.17 of 16 cores busy at 04:20Z, `SOLVER_WATCH_STATE.md` §2); no uncontended control was bought, so the 0.42 is a contended figure and the true over-prediction is at least that large. **WASTE: 0.034 core-min, named separately and netted off nothing** — three pre-AMENDMENT-2 crashes at zero iterations (`STATUS.T4_IJ_*.CRASH_pre_amendment2`), triaged. **NO CAP WAS CROSSED** (20.3 / 21.3 / 23.0 %). What it bought: a `NOT A RESULT` that scores P3 and P4 WRONG as registered, one confirmed registered risk (C1 on `c`), and three instrument findings (no y+ / C2 / C3 reader on the grading path; generic `postProcess -func yPlus` returns a reader zero; pipe wall at y+ 13–30 uncovered by C1) for the successor pre-registration | `docs/campaigns/T-family/T4_RESULTS_2026-08-26.md`; `verification/runs/T-family/T4_runs/gate_t4.json`, `STATUS.T4_IJ_{c,m,f}`; `T4_PREREGISTRATION.md` §12 at `88db2d8b` |
| **C-120** | 2026-08-26 | heat-transfer | **T1b L4 extension (EXT2) — `R_10k_x`, `R_100k_x`, `R_300k_x` continued to endTime 32 000 / 94 000 / 110 000 (209 920 cells, serial, `buoyantBoussinesqSimpleFoam`) and RE-GRADED through the registered path `mark_done_t1b_L4.py` → `analyse_t1b_L4.py` → `gate_t1b_L4.json`: `NOT A RESULT` × 4 STANDS.** Two x levels moved from NOT_CONVERGED to CONVERGED and now fall at step (2) on STAGNANT (m,f,x) triples (p +0.435 / +0.422 / +0.412); `R_10k_x` still NOT_CONVERGED at 32 000. Zero graded rows, no GCI quotable. Planted-zero control PASSED; `check_comparator_freeze.py` still reports `analyse_t1b_L4.py` UNFROZEN (standing finding, not worked around). A completed process under rule 12; `[lab-attributed]` lane grading on the supervisor's brief | **2 865.8 core-min POINT** (`R_10k_x` 1 073.6 + `R_100k_x` 671.0 + `R_300k_x` 1 121.2), CEILING 5 071.5, CAPS 1 100 / 1 300 / 2 750 = 5 150, frozen in `T1b_L4_EXT2_PREREGISTRATION.md` §9–§10 at `9d4beec4`; basis each case's OWN `ClockTime` over its own last 10 000 iterations on this mesh, ceiling the slowest sustained segment measured on this mesh (5.4338 s/it) | **2 819.700 core-min MEASURED** = `wall × 1 rank ÷ 60` from `STATUS_ext1.<case>`: `R_10k_x` 874.483 (52 469 s), `R_100k_x` 772.083 (46 325 s), `R_300k_x` 1 173.133 (70 388 s); rc = 0 on all three; cap utilisation 79.5 / 59.4 / 42.7 %. **$2.4108 DERIVED, NOT MEASURED** at $0.0513/core-h, c7a.4xlarge, reported-by-owner (`COMPUTE_BUDGET_CHARTER.md` §5) | **2 819.700 — stated equal to gross, departure named:** every wall exceeds the §2 3600-s trigger mechanically, but each ended on its registered `endTime` with rc=0, `End` in both logs and ExecutionTime counts summing to `endTime` (`mark_done_t1b_L4.py` 4/4 PASS), so no stall exists to clean | **0.9839** (`R_10k_x` 0.815, `R_100k_x` 1.151, `R_300k_x` 1.046); 0.556 of CEILING | **THE BEST-PREDICTED ROW THIS RUNG HAS PRODUCED, AND THE BASIS IS WHY: a case's OWN late-segment rate on the SAME mesh predicts its continuation to within 2 % in aggregate.** The per-case spread (0.82–1.15) is contention, not the model: `R_10k_x` ran faster than its basis (4.372 vs 5.368 s/it) as the box emptied through the night, `R_100k_x` slower (3.309 vs 2.876) — the arms ran beside UP TO NINE foreign solvers, and the 04:14–04:35Z memory incident (15.5 GB python, 7.8 GB swap) slowed each by ~1.19–1.22× for twenty minutes (`T4_runs/MEMORY_INCIDENT_2026-08-26.md`; the supervisor's brief quotes 1.24–1.30×) — **named, not netted**. The CEILING at 1.77× actual was sized to bound full 19-case contention that did not recur; a successor ceiling can use the same basis with a smaller contention multiplier. **WASTE: 0.000 core-min, named separately** — nothing re-run, no cap crossed, no stall. What it bought: every §3 prediction of `T1b_L4_AMENDMENT.md` HELD (Nu_x within 0.16–0.44 % of the predicted number at all four Re; four NOT A RESULT as predicted), and the demonstration that convergence at x does not rescue a STAGNANT triple — the rung stays NOT HELD on grid state, which is the answer this extension was registered to produce or refute | `docs/campaigns/T-family/T1b_RESULTS.md` §12; `verification/runs/T-family/T1_runs/gate_t1b_L4.json`, `STATUS_ext1.R_{10k,100k,300k}_x`; `T1b_L4_EXT2_PREREGISTRATION.md` §9 at `9d4beec4`; predecessor row C-119 (T4) same night |
| **C-121** | 2026-08-26 | heat-transfer | **T11 control C-T (temporal-bias, prereg §7 / P3, REPORTED not gated; excluded from C-118 as absent).** `T11_PW_f_CT` = level f with `deltaT` 1e-4 -> 5e-5 (40 000 steps), fired through the new wrapper `run_one_t11_ct.sh` under AMENDMENT 2 (disclosure only, `f5f67de7`). Result: G1 theta_mean 0.851595967066 (C-T) vs 0.851596281983 (f), dG1 = -3.149e-07, |dG1| = 0.37 % of the 1e-4 band; **P3 scored CORRECT** (registered < 10 %). rc = 0, End line, 40 000 ExecutionTime lines = endTime/deltaT, age guard holds, capped = no. | **0.140 core-min POINT** (2x level f's C-118 POINT 0.0701, linear in steps), **cap 8 core-min** (level f's cap, 1 rank, timeout 480 s) | **0.050 core-min** (`STATUS.T11_PW_f_CT`: wall_s = 3, ranks = 1, 1-s floor); solver `ExecutionTime` 2.7 s = **0.045 core-min** | **0.050** (0.045 by ExecutionTime); gross = cleaned, no stall | **0.36** (0.32 by ExecutionTime) | **MISPREDICTION, same direction and cause as C-118**: overhead-dominated below ~1e3 cells, so doubling the steps cost 2.0x level f's ExecutionTime (2.7 vs 1.34 s) — the step scaling itself held; the over-estimate is inherited from C-118's linear POINT. Not contention (loadavg 12.2 of 16 at launch, finished in 3 s). Waste 0. Cap 8 never approached (0.6 %). **$4.3e-05 DERIVED** at $0.0513/core-h — derived, not measured (`COMPUTE_BUDGET_CHARTER` §5). | `verification/runs/T-family/T11_runs/{STATUS.T11_PW_f_CT,read_ct_t11.log,T11_PW_f_CT/log.solve}`; AMENDMENT 2 commit `f5f67de7` |
| **C-122** | 2026-08-26 | dafoam | **D7FR (A3) arm `ACC` — `LIMIT 1`, one primal at the corrected physical design point, SHIPPED. `rc=0` (docker inspect), `ACC-1` = `PASS`: `CD = 0.02303298929962691` vs arm `O`'s `2.3048932443550496e-02`, `rel_CD = 6.917e-4` against the 1e-3 band frozen at `b424b44e`; planted-zero control PASS. Prediction `C4` confirmed.** | **25.0** core-min (`PREREGISTRATION.md` §7), cap 60.0, ceiling 240.0 | **2.067** core-min MEASURED from `ledger.txt` (wall 31 s × 4 ranks); no row over 3600 s so gross = cleaned | **2.067** | **0.083** | **MISPREDICTION, and the pre-registration named its own mechanism.** §7 priced a primal at 20.27 core-min from arm `O`'s 932.5/46 evaluations and said in the same paragraph that this *"necessarily OVERSTATES a primal"* because it carries adjoint solves; the measured primal was 18.6 s of a 31 s wall. Same shape as `C-94` (D4 ACC, ratio 0.15). Next estimate: price `ACC` from a measured primal, not from an optimisation's per-evaluation mean. Contention present, not limiting: `delivered_cores_mean` 3.7267 of 4, `max_nr_throttled` 45, sibling `d12y_S2b` on cpu 12 — read from the cgroup. No waste, no overrun, no cap moved. **$0.0018 DERIVED**, `cost_basis` REPORTED-BY-OWNER. Host rebooted 15:19Z (kernel 1011-aws); image digest unchanged. | `cases/dafoam/ladder-a/A3/curriculum_D7FR/RESULTS_ACC.md`; run root `ledger.txt`, `ACC/d7fr_accept_verdict.json`, `STATUS.ACC` |
| **C-123** | 2026-08-26 | cfd | **F16-SL2 (Stokes' second problem, three-level serial icoFoam ladder) — PROCESS COMPLETE ON COMPUTE, GRADED `PENDING` ON BOTH GATES BY THE FROZEN COMPARATOR.** All three levels rc 0, `End`, last Time 40 = endTime, `Time =` count = endTime/dt (16000/32000/64000), age guard holds at every level. The comparator `grade_f16.py` (frozen `2aea29d9`) refused on its own instrument: `TIME_RE` is `^`-anchored and applied by `finditer` to the whole log without `re.MULTILINE`, so it matches nothing on a real log (the same regex line-by-line matches 16,000 lines). Not edited (rule 2); verdict stands as printed. Attempt 1 died at zero compute (`set -u` on `openfoam2606/etc/bashrc`, `STATUS.F16.attempt1`). | Central **0.56 / 1.12 / 1.87** core-min at 0.3 / 0.6 / 1.0 ms/step, `cost_basis` ESTIMATE WITH NO MEASURED HISTORY (prereg §8, no icoFoam log of this shape on the box); **REGISTERED CAP 20** core-min, frozen at `2aea29d9` before compute; $0.017 at cap DERIVED | **0.416667 core-min MEASURED** = ClockTime × 1 rank ÷ 60 from each `log.icoFoam`: coarse 3 s + medium 7 s + fine 15 s = 25 s; ±0.05 core-min integer-second quantisation; launcher tally agrees exactly (0.4166666666666667). **$0.000356 DERIVED, NOT MEASURED** at $0.0513/core-h | **0.416667 = gross** (longest level 15 s wall; nothing near the 3600-s stall rule) | **0.223** vs the 1.0 ms/step central; 0.372 vs 0.6; 0.744 vs 0.3 ms/step; **0.021 of cap** | **MISPREDICTION IN THE CONSERVATIVE DIRECTION, and a datum the ledger lacked: serial icoFoam on 56–224-cell 1-D meshes runs at 0.223 ms/step (25,000 ms / 112,000 steps), under the lowest tabled assumption.** Per-step overhead at this size is cheaper than the prereg guessed; the ~11× cap headroom was declared as an admission of no history and is now replaced by a measured rate for icoFoam tiny-mesh ladders. **CONTENTION: none material** — launcher read free cores 3.09 / 3.0 / 3.0 of 16 at the three launches (load ~11 of 16 from peer teams), yet the run came in 4.5× under central; the direction excludes contention as the explanation and no uncontended control is claimed. **WASTE: 0.000 core-min** on compute — attempt 1 consumed no solver time; nothing killed, nothing re-run. **What it bought: NOT a verdict on the physics** — both gates `PENDING` on an instrument defect, the 54 MB of field data stays on disk for a zero-compute re-grade if the supervisor rules a §2d.1 repair or re-registration. | `verification/runs/F16_runs/RESULTS.md`; `verification/runs/F16_runs/GRADE_F16.out`; `verification/runs/F16_runs/F16_GRADED.json`; `verification/runs/F16_runs/{coarse,medium,fine}/log.icoFoam`; `verification/campaign/F16_SL2_PREREGISTRATION.md` §8 at `2aea29d9` |
| **C-124** | 2026-08-26 | cfd | **F4-S shock-locus successor — GRADED.** Nine `rhoCentralFoam` cases (M 6/7/8 × coarse/medium/fine, np = 1, serial), comparator `grade_f4s.py` frozen at `d98868fb` (blob `9585c906`). **G-F4S-1 (RH mid-density crossing): PASS × 3** — CONVERGING at every Mach, GCI 0.4516 / 0.7328 / 0.0725 %, deviation from Billig +1.59 / +1.07 / +0.50 % inside ± one local cell. **G-F4S-1B (predecessor argmax): NOT A RESULT at M6.0 (DIVERGENT) and M8.0 (OSCILLATORY, coarse level not plateaued), PASS at M7.0.** Prediction 1 (≥ 2 of 3 CONVERGING) MET at 3 of 3; prediction 2 (NOT A RESULT × 3) PARTIALLY MET, refuted at M7.0. Planted-zero passed on all six rows. | **26.30 core-min** frozen in `F4S_SHOCK_LOCUS_PREREGISTRATION.md` §9.2 at `d98868fb`; hard cap 36.0 (§9.3), per-case guard 1200 s; solver basis 24.1108 MEASURED (C-91, at load 11.14→15.05 of 16) + estimated mesh/sampling/grading limbs | **16.1755 core-min MEASURED** = Σ `wall_s_total × ranks ÷ 60` over nine cases from `runs/RUN_LEDGER.json` (blockMesh + checkMesh + solver + both sampling passes; ranks = 1). Solver-only cross-check from the logs' `ClockTime`: 954 s = 15.900 core-min. **= $0.01383 DERIVED, NOT MEASURED** at $0.0513/core-h, c7a.4xlarge, reported-by-owner (`COMPUTE_BUDGET_CHARTER.md` §5) | **16.1755 — cleaned == gross.** Longest case 324.2 wall s (M8.0 fine), under the 1200-s guard and nowhere near the 3600-s stall rule; `CAP_HALT.json` does not exist; 44.9 % of cap | **0.615** | **UNDER-RUN BY A QUIET BOX, NOT BY ESTIMATOR SKILL — the C-115 reading.** **Load at launch, measured by the launcher in its own invocation (`runs/LAUNCH_LOAD.json`): 0.19 / 0.25 / 0.10 on 16 CPUs** (1.2 %), against a solver basis measured under a load of 11.14→15.05. **Contention: NONE — named, and it runs the other way:** the same nine cases ran the solver limb 1.51× faster than C-91's basis (15.90 vs 24.11 core-min) with `nWrites` doubled, so this run is the uncontended control C-91 never had, and it puts C-91's contention penalty at ≈ 1.5× on this family. **Carry forward: the solver basis for this family on a quiet box is 15.90 core-min; on a saturated box 24.11.** The +5 % `nWrites` uplift, the 1.5× mesh/sampling factor and the ≤ 0.5 grading allowance (§17 items 1–3) could not be scored separately: mesh + sampling came to 0.2755 core-min against 0.4806 predicted. **WASTE: 0.000 core-min, named separately** — nothing stalled, killed or re-run, no cap moved. **What it bought:** a defensible standoff PASS at all three Mach numbers on the same solver and meshes that returned eight NOT A RESULTs in F4, plus the measured finding that the argmax detector settles at M7.0 — the §15 'high confidence' prediction scored as a miss there | `verification/runs/F4_runs/successor_2026-08-26/RESULTS.md`; `GRADE_F4S.out`, `GRADE_F4S.json`; actuals `runs/RUN_LEDGER.json`, `runs/LAUNCH_LOAD.json`; prereg §9 at `d98868fb`; predecessor row `C-91` |
| **C-125** | 2026-08-26 | dafoam | **D7FR (A3) arm `F-S` — endpoint FD table, SHIPPED row, `rc=0` (docker inspect). `G5` bright line on the SHIPPED row `PASS`: 5 of 5 components, aggregate worst rel err 1.959 %, zero sign flips; G6/G6b/G7 PASS. Item verdict PENDING `F-P` and a supervisor ruling on the ported grader's `O`-arm clauses (RESULTS_FS.md §3).** | **485.0** core-min (`PREREGISTRATION.md` §7: 22 × 20.27 + 24.2 + 14.6), cap 750.0, ceiling 3000.0 | **72.667** core-min MEASURED from `ledger.txt` (wall 1090 s × 4 ranks); no row over 3600 s so gross = cleaned | **72.667** | **0.150** | **MISPREDICTION, same mechanism as C-122 (ACC, 0.083)**: the per-primal price 20.27 came from arm `O`'s 932.5/46 evaluations and carries adjoint solves; measured, 22 primals + colouring ran in 1090 s, about 3.3 core-min per primal. Next estimate for an endpoint-FD arm on this mesh: about 3.5 core-min per primal plus colouring. Contention present, not limiting: `delivered_cores_mean` 3.9894 of 4 (cgroup), `memavail_min_during` 16.315 GiB against the 16.0 floor — the floor was approached, not crossed, with `d12y` siblings on cpu 12. No waste, no overrun, no cap moved. **$0.0621 DERIVED**, `cost_basis` REPORTED-BY-OWNER. | `cases/dafoam/ladder-a/A3/curriculum_D7FR/RESULTS_FS.md`; run root `ledger.txt`, `STATUS.F-S`, `d7fr_grade_FS_interim.json` |
| **C-126** | 2026-08-26 | cfd | **F17-KV40 (Kovasznay flow, Re = 40, steady exact Navier–Stokes, three-level serial `simpleFoam` ladder 1,536 / 6,144 / 24,576 cells, 4,000 fixed iterations) — GRADED `PASS` × 2 by the frozen comparator `grade_f17.py` (blob `a863a673`, prereg `4ad083fb`, 14 of 14 frozen paths disk == blob).** G-F17-1 E2 = 1.423137e−04 in [3.690e−05, 3.321e−04], CONVERGING p = 2.099, GCI 1.671e−04 abs; G-F17-2 u(probe) = 0.382441158 in [0.382202, 0.382544], CONVERGING p = 2.070, GCI 9.009e−05 abs. Rule 4 at all three levels (rc 0, `End`, 4000 `Time` lines, age guard); iterative census 0 of 1,200 above tolerance per level; planted-zero PASS on both readers. Registered prediction (p ≈ 2, PASS × 2) MET. Instrument-check, counts to no column. | **7.6 core-min** frozen in `F17_KV40_PREREGISTRATION.md` §8 at `4ad083fb` (0.36 / 1.46 / 5.82); cap 40; basis 3.55 µs per cell-iteration read from ONE record of a different, turbulent case (`FPE_DIAG_runs/BL1`) — derived, not measured on this case | **1.2000 core-min MEASURED** = ClockTime 3 + 11 + 58 s × 1 rank ÷ 60 (`verification/runs/F17_runs/{coarse,medium,fine}/log.simpleFoam`); ± 0.0083 per level integer-second quantisation. **= $0.00103 DERIVED, NOT MEASURED** at $0.0513/core-h, c7a.4xlarge, reported-by-owner (`COMPUTE_BUDGET_CHARTER.md` §5) | **1.2000 — cleaned == gross.** Longest level 58 wall s; nothing matches the 3600-s stall rule; 3.0 % of cap | **0.158** | **MISPREDICTION OF THE RATE BASIS — not contention, not waste.** Measured 0.488 / 0.448 / 0.590 µs per cell-iteration (coarse / medium / fine) against the 3.55 µs basis: 6.0–7.9× cheaper, because the basis came from a turbulent RANS `simpleFoam` record (turbulence transport + upwind turbulence advection + `residualControl`) and this case is laminar. Contention NONE: ExecutionTime 57.41 s vs ClockTime 58 s at fine (1 %), launcher probe 1.95–2.38 free cores of 16. **WASTE: 0.000 core-min, named separately** — no stall, kill, re-run or cap movement. **Carry forward:** laminar 2-D `simpleFoam`, serial, on this box ≈ 0.5–0.6 µs per cell-iteration; do not reuse a turbulent-RANS rate for a laminar case | `verification/campaign/F17_KV40_RESULTS.md`; `verification/runs/F17_runs/F17_GRADED.json`, `F17_GRADED.out`; prereg §8 at `4ad083fb`; runner tally `cases/F17_kovasznay/launcher.queue.out` |
| **C-127** | 2026-08-26 | cfd | **F16b-SL2 (Stokes' second problem with ±x CYCLIC, successor to F16's C-123, three-level serial `icoFoam` ladder 56 / 112 / 224 cells, 16,000 / 32,000 / 64,000 steps) — GRADED `PASS` × 2 by the frozen comparator `grade_f16b.py` (blob `a7d14ed9`, prereg `cadb4887`, 11 of 11 frozen paths disk == blob).** G-F16-1 E2 = 1.319909e−04 in [4.101e−05, 3.691e−04], CONVERGING p = 1.825, GCI 1.811e−04 abs; G-F16-2 u(δ) = −0.309394169 in [−0.310278, −0.308841], CONVERGING p = 1.993, GCI 2.082e−04 abs. Rule 4 at all three levels (rc 0, `End`, `Time` count == endTime/Δt, age guard); per-step p-residual census 0 above 1e−09; `Solving for Ux` at every step (F16: none); planted-zero PASS on both readers. Registered prediction (p ≈ 2) MET. One L-342 BOOKKEEPING DEFECT: `STATUS.F16b` absent under the run root (runner wrote `STATUS.F16b_SL2` in the case dir, launch-argv status); grade proceeded, cost read from the logs. | **~0.5 core-min** in `F16b_SL2_PREREGISTRATION.md` §8 at `cadb4887` = F16's measured 0.4167 × ~1.2; cap 5 (frozen text carries a `$0` shell-expansion artefact in its dollar figures — disclosed in RESULTS.md §5, no core-minute figure affected) | **1.2500 core-min MEASURED** = ClockTime 7 + 15 + 53 s × 1 rank ÷ 60 (`verification/runs/F16b_runs/{coarse,medium,fine}/log.icoFoam`); ± 0.0083 per level integer-second quantisation. **= $0.00107 DERIVED, NOT MEASURED** at $0.0513/core-h, c7a.4xlarge, reported-by-owner (`COMPUTE_BUDGET_CHARTER.md` §5) | **1.2500 — cleaned == gross.** Longest level 53 wall s; nothing matches the 3600-s stall rule; 25.0 % of cap | **2.50** | **MISPREDICTION OF THE BASIS — a basis measured on a degenerate run, not contention, not waste.** F16's 3 / 7 / 15 s were measured on identically-zero fields: all 128,000 fine-level p solves took `No Iterations 0` and no Ux equation ran. F16b's fine level does the work: 128,000 p solves at 19–21 iterations (76,490 at 21 / 51,431 at 20 / 79 at 19) plus 64,000 Ux solves. The ×1.2 uplift priced one more component, not the linear-solver work. Contention NEGLIGIBLE: ExecutionTime 50.87 s vs ClockTime 53 s at fine (4 %), 6.07 vs 7 s at coarse; launcher probe 2.21–2.41 free cores of 16. **WASTE: 0.000 core-min, named separately** (the prereg §9 smoke arm < 0.017 core-min was named there and is charged nowhere else). **Carry forward:** a basis from a run later closed NOT A RESULT on physics is not a basis; serial `icoFoam` here ≈ 3.7 µs per cell-step at 224 cells | `verification/runs/F16b_runs/RESULTS.md`; `verification/runs/F16b_runs/F16b_GRADED.json`, `F16b_GRADED.out`; prereg §8 at `cadb4887`; predecessor row `C-123`; runner tally `cases/F16b_stokes_second_problem/launcher.queue.out` |
| **C-128** | 2026-08-26 | cfd | **F18-TG2D (2-D decaying Taylor–Green vortex, `icoFoam` BDF2/PISO on a doubly-periodic box, three-level serial ladder 4,096 / 16,384 / 65,536 cells, 100 / 200 / 400 steps at Co 0.2) — GRADED `PASS` × 2 by the frozen comparator `grade_f18.py` (blob `6ff39cb8`, prereg `c4f72b27`, 13 of 13 frozen paths disk == blob).** G-F18-1 E2(T) = 1.101354e−05 in [3.153e−06, 2.838e−05], CONVERGING, GCI 2.439e−05 abs; G-F18-2 box-mean KE(T) = 0.112337460 in [0.112318792, 0.112345690] (exact 0.112332241), CONVERGING, GCI 1.749e−05 abs. **Registered prediction p ≈ 2 NOT MET on the order: grader's three-level orders 1.289 (E2) and 0.964 (KE)**; CONVERGING and in-band predictions met; the model's positive KE-error sign confirmed at all levels; medium/fine track the model to 4–16 %, the coarse level is pre-asymptotic (E2 0.72× model). Plateau ABSENT by construction. Rule 4 at all levels; p-residual census 0 above 1e−09; planted-zero PASS on both readers. Instrument-check, counts to no column. | **2.5 core-min** frozen in `F18_TG2D_PREREGISTRATION.md` §8 at `c4f72b27` (0.03 / 0.27 / 2.18) from a flat 5 µs per cell-step borrowed from `VMFL019/L1_30` (480 cells); cap 30; the prereg named "PCG on 65k cells at 1e−9 with relTol 0" as the unknown | **4.4167 core-min MEASURED** = ClockTime 1 + 18 + 246 s × 1 rank ÷ 60 (`verification/runs/F18_runs/{coarse,medium,fine}/log.icoFoam`); ± 0.0083 per level integer-second quantisation. **= $0.00378 DERIVED, NOT MEASURED** at $0.0513/core-h, c7a.4xlarge, reported-by-owner (`COMPUTE_BUDGET_CHARTER.md` §5) | **4.4167 — cleaned == gross.** Longest level 246 wall s; nothing matches the 3600-s stall rule; 14.7 % of cap. The runner's `CAP_OVERRUN.txt` (elapsed 190 s > 1.10 × the 150-s ESTIMATE, "reported, not enforced") keyed on the estimate, not the cap — no cap crossing occurred | **1.767** | **MISPREDICTION OF THE RATE'S SCALING WITH N — the named unknown — not contention, not waste.** Measured 2.44 / 5.49 / 9.38 µs per cell-step (coarse / medium / fine) against the flat 5 µs basis: PCG/DIC at 1e−9, relTol 0 took a mean 76.7 / 144.0 / 257.4 iterations per p solve (max 87 / 167 / 321), ≈ 1.8× per level, so cost per cell-step doubles per level; the flat rate matched medium (1.10×) and under-predicted fine by 1.88×. Contention NONE: ExecutionTime 246.17 s vs ClockTime 246 s at fine, launcher probe 6.9–7.2 free cores of 16. **WASTE: 0.000 core-min, named separately.** **Carry forward:** price `icoFoam` PCG/DIC pressure solves on uniform 2-D grids as ∝ N^1.5 (≈ 10 µs per cell-step at 65k cells on this box); a flat rate from a 480-cell record does not extrapolate; a runner watchdog keyed on the estimate rather than the cap reports "overruns" that are not (supervisor's desk) | `verification/campaign/F18_TG2D_RESULTS.md`; `verification/runs/F18_runs/F18_GRADED.json`, `F18_GRADED.out`; prereg §8 at `c4f72b27`; runner tally `cases/F18_taylor_green/launcher.queue.out`, `CAP_OVERRUN.txt` |
| **C-129** | 2026-08-26 | dafoam | **D7FR (A3) arm `F-P` — endpoint FD table, PATCHED row (`dafoam-idwarp-rot:v1`, digest `2927768a…`, `libidwarp.so` md5 `85f59e87…`), `rc=0` (docker inspect via the launcher's ledger row and `F-P_chain_inspect.txt`; the container object was removed by the launcher after bookkeeping, `d7fr_run_arm.sh:593`, so it is not re-readable at grading time). `G5` bright line on the PATCHED row `PASS`: 5 of 5 components, aggregate worst rel err 1.959 %, zero sign flips; G6/G6b/G7 PASS; G13 in-item adjoint reason 2 (968 it, 687.02 s).** | **485.0** core-min (`PREREGISTRATION.md` §7: 22 × 20.27 + 24.2 + 14.6), cap 750.0, ceiling 3000.0 | **73.4** core-min MEASURED from `ledger.txt` (wall 1101 s × 4 ranks, the launcher's own inspect-derived wall); no row over 3600 s so gross = cleaned | **73.4** | **0.151** | **MISPREDICTION, same mechanism as C-122/C-125**: the per-primal price 20.27 carries adjoint solves; measured ≈ 3.3 core-min per primal on the patched image too (F-S 1090 s vs F-P 1101 s — the two images cost the same to 1 %). Contention present, not limiting: `delivered_cores_mean` 3.9887 of 4 (cgroup). **`memavail_min_during` 15.725 GiB — 2 of 73 samples BELOW the 16.0 floor by 0.275 GiB during the run** (H5 launch window had passed; floor is a launch gate; no OOM, `inspect [0 false]`) — named, not absorbed; sibling `d12y_S3b_*` (8 g) on cpu 12. No waste, no overrun, no cap moved. **$0.0628 DERIVED**, `cost_basis` REPORTED-BY-OWNER. | `cases/dafoam/ladder-a/A3/curriculum_D7FR/RESULTS.md` §1, §3; run root `ledger.txt`, `STATUS.F-P`, `F-P_chain_inspect.txt`, `d7fr_grade_ITEM_20260826T170644Z.json` |
| **C-130** | 2026-08-26 | dafoam | **D7FR (A3) ITEM COMPLETE — verdict `PASS`, two rows: SHIPPED `PASS` (5/5, worst 1.959 %) and PATCHED `PASS` (5/5, worst 1.959 %); G9 distinct `.so` md5s PASS; shipped-vs-patched divergence 0.000 % on every component (FD artefacts byte-identical, md5 `e284d925…`, computed independently — the registered finding, reported with its number). Graded under Addendum 4's blob `1b810913…` (`faeda019`); pre-repair D7-port composition `NOT A RESULT` written beside. Predictions 5 HIT / 1 MISS (C3 refuted).** | **997.75** core-min (§7: P1 0.75 + X 2.0 + ACC 25.0 + F-S 485.0 + F-P 485.0), $0.8531 DERIVED; ceiling sum 6332.0 | **149.534** core-min MEASURED (P1 0.667 + X 0.733 + ACC 2.067 + F-S 72.667 + F-P 73.4, each wall × 4 from `ledger.txt`); gross = cleaned (no row over 3600 s) | **149.534** | **0.150** (per arm: P1 0.889, X 0.367, ACC 0.083, F-S 0.150, F-P 0.151) | **MISPREDICTION on the FD and ACC arms (the 20.27 per-primal basis carries adjoints — §7 registered the overstatement in advance), on-trend for P1 (0.889), and a labelled guess on X (0.367).** Contention present, not limiting on any MPI arm (≥ 3.7267 of 4 delivered). **WASTE: 0.0 core-min** (no re-fire, no refused-after-staging arm; D7F's `P1` 0.733 stays RECORDED-NOT-IMPORTED per §7, not this item's). Cap crossings: none (G10 limb 2 `n_overruns 0`). Near-floor memory named: F-S min 16.315 (approached), F-P min 15.725 (crossed by 0.275 GiB, 2 of 73 samples, no OOM). Next estimate for an endpoint-FD row on this mesh: ≈ 150 core-min, not 485. **$0.1279 DERIVED**, `cost_basis` REPORTED-BY-OWNER. Host kernel `7.0.0-1011-aws` (15:19Z reboot); image digests unchanged. | `cases/dafoam/ladder-a/A3/curriculum_D7FR/RESULTS.md`; run root `ledger.txt`, `d7fr_grade_ITEM_20260826T170644Z.json`; rows C-122, C-125, C-129 |
| **C-131** | 2026-08-26 | ansys-verification | **VMFL064 — backward-facing step (VM2026R1 p. 195), three-level laminar `simpleFoam` ladder, GRADED `NOT A RESULT`** — the frozen comparator refused (exit 2) at L3, *"wall shear never changes sign -- no reattachment found"*. Validation register **row #29** | **~10–15 core-min total** registered (`cases/ansys_verification/VMFL064/PREREGISTRATION.md` item 10, frozen `1dc0e4d5`; CAP **90 core-min**, RANKS = 1, so the wall timeout IS the core-minute budget) = **$0.0086–$0.0128 derived** at the owner-stated $0.0513/core-h ($0.0107 at the 12.5 midpoint) | **5.383 core-min MEASURED** from the run logs — L1 0.0333 + L2 0.300 + L3 5.05, total wall **323 s** × 1 rank ÷ 60, read from `verification/runs/ansys_verification/VMFL064/COST.txt` and the per-level `RUN_RC.txt` = **$0.0046 derived, NOT MEASURED** (the box cannot read its own billing, `COMPUTE_BUDGET_CHARTER.md` §5) | **= gross.** The longest row is **303 wall s** (L3), two orders below the 3600-s stall rule, so there is nothing to clean out and gross and cleaned coincide | **0.43×** cleaned/predicted against the 12.5 core-min midpoint (**0.54×** against 10, **0.36×** against 15); **0.060× of the 90 core-min cap**. No overrun | **Misprediction in the conservative direction, and — unlike this ledger's C-10 and C-15 — THE DURATION PREDICTION WAS ACTUALLY TESTED.** All three levels ran to completion (`rc = 0`, strict completion rule 4 met at all three); nothing truncated the run, so 0.43× is a real comparison and not a measurement of an interruption. **Cause of the gap:** the estimate priced *"a 2-D BFS whose convergence can be stubborn"* and bought headroom for it; the case was not stubborn — `residualControl` converged at 424 / 907 / 2 152 iterations against a 20 000 ceiling, i.e. the finest level stopped on its own at **11 %** of the iteration budget. **CONTENTION: none claimed** — the runs are serial and sub-6-minute and the figure is stated gross rather than absorbed (`COMPUTE_BUDGET_CHARTER.md` §6). **WASTE OF THE STAGING/CRASH KIND: 0.000 core-min** — there was no staging fault, no crash and no re-run; every level produced its fields and its log. **BUT THE SPEND IS UNRECOVERED AND IS NAMED AS SUCH, NOT NETTED OFF EITHER COLUMN: the item was bought for a graded LR/s with a `CONVERGING` triple and it returned no verdict value at all.** What the 5.383 core-min did buy is a finding — the finest mesh resolves a secondary corner vortex the coarser two do not, so the frozen reader's requirement that the wall-shear profile *start* negative fails on exactly the level the gate is read at, and the cross-instrument control confirmed `ORIENT = -1` on real data to 2.4e-07 m. **CALIBRATION LESSON, and it is not about the size of the estimate: a cost ratio of 0.43× says the money was predicted well and says NOTHING about whether the item delivered. This row's estimate was good and its item is `NOT A RESULT`; the two are independent and must be read as such.** **SECOND LESSON, for the successor registration: price the READER's assumptions at the FINEST level, not the coarsest** — a reader validated on L1 was priced as if it would work at L3, and the flow structure at L3 is precisely what a refinement ladder exists to reveal. A successor is a **new freeze with a coarser family or a reader that does not assume a monotone start**, never an amendment (rule 2). | `cases/ansys_verification/VMFL064/RESULTS.md` (verdict, per-level table, cost block) and `verification/runs/ansys_verification/VMFL064/COST.txt` (the measured figure); graded at **`b8b5e2bf`**; pre-registration frozen at **`1dc0e4d5`** with PRE-COMPUTE AMENDMENT 1 at **`a7c42398`**; comparator blob `0be9126cd6a3e3c860b98854a4e21ba1102edfdc`; validation register **row #29** |
| **C-132** | 2026-08-26 | dafoam | **D4-SHIPPED (A2) arm `ACC` — WASTE ROW, its own figure, never absorbed. The arm as launched (`d4s_run_arm.sh:323`, `-task compute_totals` on a pristine `base/` copy) was a byte-for-byte duplicate of P2's baseline `compute_totals`, while its registered cost and memory (3.000 core-min, 8 g) were D4's PATCHED acceptance primal (`CURRICULUM-D4-a2-wing-cdmin/acc_ledger.txt`, `d4_accept_primal.py`, 45 s at 8 g) — `D4S-LAUNCHER-DEF-2` as measured (PREREGISTRATION.md Addendum 2c). Container `d4_ACC_20260826T164743Z_200655` (cpuset 5,6,7,9, 8 g, in-container deadline 1200 s) ran 16:47:43Z → 16:56:28Z and was OOM-KILLED by its own cgroup at the total-derivative step after a fresh 480 s colouring and a converged adjoint (`PetscConvergedReason: 2`, 94 it): ledger `rc=137 wall_s=525 ranks=4 core_min=35.0 inspect(exit,oomkilled)=[137 true] memavail_pre_GiB=26.57 memavail_post_GiB=26.72 delivered_cores_mean=[3.9883 n=34]`; dmesg `CONSTRAINT_MEMCG`, one rank ~2.5 GB. Not the deadline, not the host (26.4 GiB free), not any agent. No gate reads ACC (arm list `P1,P2,O,F3`), so nothing gradeable was lost; the arm is NOT re-bought at any memory cap under 2c; Addendum 2d registers ACC as D4's acceptance primal at O's PHYSICAL endpoint for the re-fire.** | **3.000** core-min (`PREREGISTRATION.md` §4, A2.4; cap 80.0, ceiling 320.0, memory 8 g) — a prediction for a DIFFERENT program than the one the launcher ran | **35.0** core-min MEASURED (525 s × 4, launcher ledger row from `docker inspect`; the container object removed by the launcher's `:465` after the row) | **35.0** — and every core-minute of it is **WASTE** (rc=137, artefacts partial, arm mis-specified); gross = cleaned (no row over 3600 s) | **11.67** against the 3.000 registered for the acceptance primal — a ratio between two different programs, stated for the record and NOT used for calibration; against P2's 41.133 (the program that actually ran) the partial run reached 0.85 before the kill | **LAUNCHER MIS-SPECIFICATION, not misprediction and not contention**: the branch duplicated P2 (12 g) and inherited the acceptance primal's 8 g; the OOM is the consequence. Contention present, not limiting (3.9883 of 4 delivered). Lesson forward: an arm's cost row must name the PROGRAM it prices (D4's `ACC` = `d4_accept_primal.py`), and a launcher branch copied from another arm is a registration defect the cap assertion cannot see. **$0.0299 DERIVED**, `cost_basis` REPORTED-BY-OWNER. The 17:24Z F3-only fire that the launcher's own guard refused at `:281` cost 0.0 (no container). | `cases/dafoam/ladder-a/A2/curriculum_D4_SHIPPED/PREREGISTRATION.md` Addenda 2c, 2d (`0308cf30`, `08039792`); run root `ledger.txt`, `STATUS.ACC.oom_20260826T165628Z`, `ACC_20260826T164743Z_200655.log`, `ACC_launch.out`, `ACC_oom_compute_totals_20260826T164743Z/` |
| **C-133** | 2026-08-26 | cfd | **F15-OSR29 (oblique shock reflection M∞ = 2.9, `rhoCentralFoam` inviscid, three-level serial ladder 10,000 / 40,000 / 160,000 cells, adaptive Δt to t = 10) — PROCESS COMPLETE ON COMPUTE; GRADED `PENDING` × 2 BY THE FROZEN COMPARATOR; F15-R2 COMPARATOR REFUSED.** All three levels rc 0, `End`, last Time 10, `latest + dt_final > endTime`, T U p at 10/, age guard holds (fine: 37,851 steps, ClockTime 4,299 s). Frozen `grade_f15.py` (`2aea29d9`, 13/13 paths disk == blob) rc 0 → PENDING on both gates ("no `Time =` lines in the log", the non-MULTILINE `TIME_RE`, R2 prereg defect (i)). `grade_f15_r2.py` (`893cdeaf`, 2/2 SAME) rc 2 REFUSED verbatim: *"coarse/postProcessing/lineWall/0.05/lineWall_p.xy: the wall pressure never crosses the half-rise value 1.824133; min 0.714286 max 0.714286"* — the crossing detector is applied to pre-arrival samples (first crossing at t = 0.75); a third reader defect, not repaired here; F15-R3 is the supervisor's call. Prediction p ≈ 1 untested. | **122.2 core-min** frozen in `F15_OSR29_PREREGISTRATION.md` §8 at `2aea29d9` (1.7 / 13.4 / 107.1), cap 200; basis 1.03 µs per cell-step MEASURED on `VMFL045/R2` (viscous, `mu 1e-8`) | **81.5167 core-min MEASURED** = ClockTime 68 + 524 + 4,299 s × 1 rank ÷ 60 (`verification/runs/F15_runs/{coarse,medium,fine}/log.rhoCentralFoam`; = the launcher tally in `launcher.out`); ± 0.0083 per level. **= $0.0697 DERIVED, NOT MEASURED** at $0.0513/core-h, c7a.4xlarge, reported-by-owner (`COMPUTE_BUDGET_CHARTER.md` §5) | **81.5167 gross; rule-cleaned 9.8667** — the fine level (4,299 wall s) mechanically matches the §2 3600-s stall rule and IS NOT A STALL (monotone to t = 10, ExecutionTime 4,135 s = 96 % of ClockTime, pre-registered at 6,427 s); both figures stated, gross used for the ratio; 40.8 % of cap | **0.667** (gross / predicted) | **MISPREDICTION OF THE RATE BASIS WITH A PHYSICAL REASON — not contention, not waste.** Measured 0.736 / 0.698 / 0.710 µs per cell-step (flat over 16× in cells) vs 1.03 from a VISCOUS basis case: `mu 0` here skips the `tauMC`/Laplacian assembly, 0.70/1.03 = 0.68 ≈ the ratio. Contention small: 96 % at fine, 99.8 % at medium; probe 4.0–4.5 free cores of 16. **WASTE: 0.000 core-min, named separately** (attempt 1, `STATUS.F15.attempt1` rc=1 16:12:33Z, died at the L-339 bashrc face before any solver ran). **Carry forward:** inviscid serial `rhoCentralFoam` ≈ 0.70 µs per cell-step on this box, viscous ≈ 1.03 — do not price one from the other | `verification/runs/F15_runs/RESULTS_R2.md`; `GRADE_F15.out`, `F15_GRADED.json` (PENDING), `GRADE_F15_R2.err` (refusal), `launcher.out`, `STATUS.F15`; prereg §8 at `2aea29d9`; R2 prereg at `893cdeaf` |
| **C-134** | 2026-08-26 | cfd | **F19-SOD (Sod shock tube, `rhoCentralFoam` Kurganov/vanLeer/Euler, 1-D, three-level serial ladder 400 / 800 / 1,600 cells, 1,000 / 2,000 / 4,000 fixed steps to t = 0.2) — GRADED by the frozen comparator `grade_f19.py` (blob `144d1ab0`, prereg `3053d9ec`, 15 of 15 frozen paths disk == blob): G-F19-1 L1 density error `PASS` (4.400280e−04 in [1.467e−04, 1.320e−03], CONVERGING p = 1.018 inside the registered honest range [0.6, 1.2], GCI 3.895e−04 abs); G-F19-2 shock position `NOT A RESULT` (fine 0.8505395 inside the band, error +1.08e−04 = 0.17 cells, but the triple is DEGENERATE: increments 1.0716e−04 / 1.0956e−04 equal, |p| = 0.032 < P_MIN 0.05 — arithmetic, not geometric, decrease of the shock-position error; the registered model's own x_s triple carries the same equal increments, so the outcome was latent in the registration).** Rule 5 limb (1) handled as registered: no linear solve, so a Courant stability census (1,000 / 2,000 / 4,000 readings == steps, 0 above 0.25, 0 non-finite, worst 0.0900) is the basis of `CONVERGED`; plateau ABSENT by construction. Model reproduces the solver to the printed digits at every level (E1 to 7 s.f.). Rule 4 at all levels; planted-zero PASS on both readers. Instrument-check, counts to no column. | **≈ 0.2 core-min** in `F19_SOD_PREREGISTRATION.md` §8 at `3053d9ec` (0.75 µs per cell-step + 0.4 ms per step from 2-D records; ≈ 10 s incl. mesh/sampling); cap 5 | **0.1000 core-min MEASURED** = ClockTime 1 + 1 + 4 s × 1 rank ÷ 60 (`verification/runs/F19_SOD_runs/{coarse,medium,fine}/log.rhoCentralFoam`; ExecutionTime 4.54 s); **± 0.025 (25 %) integer-second quantisation**. **= $0.0000855 DERIVED, NOT MEASURED** at $0.0513/core-h, c7a.4xlarge, reported-by-owner (`COMPUTE_BUDGET_CHARTER.md` §5) | **0.1000 — cleaned == gross.** Longest level 4 wall s; 2.0 % of cap | **0.50** (± 0.13 from quantisation) | **A 2-D RATE BASIS ON A 1-D ONE-CELL-WIDE MESH, INSIDE QUANTISATION NOISE — not contention, not waste.** All-in 0.54 µs per cell-step by ExecutionTime (0.71 by ClockTime) vs 0.75 + 0.4 ms/step: two internal faces per cell instead of four halves the flux work, and the per-step overhead term (2.8 s of the ≈ 9 s projected) did not materialise. Contention: probe 0.5 free cores of 16 (saturated), ClockTime − ExecutionTime = 1.5 s over three levels — below the instrument's resolution. **WASTE: 0.000 core-min, named separately.** **Carry forward:** sub-second levels give quantisation-bound ratios; price 1-D `rhoCentralFoam` at ≈ 0.5 µs per cell-step; pass a registered model's x_s triple through `roache_triple.py` AT registration so a DEGENERATE prediction is named before the run | `verification/campaign/F19_SOD_RESULTS.md`; `verification/runs/F19_SOD_runs/F19_GRADED.json`, `F19_GRADED.out`; prereg §8 at `3053d9ec`; runner tally `cases/F19_SOD/launcher.queue.out` |
| **C-135** | 2026-08-26 | dafoam | **D14-M — pyHyp mesh regeneration of D4's MACH-wing mesh, GRADED `PASS`, mesh BIT-IDENTICAL to the baseline (`points.gz` sha256 `260e9db7…` equal), P1–P6 6/6 HIT.** Frozen `dd6c808e`; launched by the queue runner 17:40:32Z (no agent); container `d14m_MESH_20260826T174136Z_307430`, rc 0 from `docker inspect`, OOM false; G14-0 contaminant gate PASS with its planted control fired. Record `cases/dafoam/ladder-a/A2/curriculum_D14/RESULTS.md`; grade JSON in the run root. | **0.67** core-min point, band [0.3, 2.0] (`PREREGISTRATION.md` §8, anchors: baseline `logMeshGeneration.txt` pyHyp 4.2 s CPU + D12R §8 S0 0.05 core-min + container start), cap 15.0, ceiling 30.0 | **0.1833** core-min MEASURED (`ledger.txt` `ARM=MESH … wall_s=11 core_min=0.1833`, inspect `StartedAt` 17:41:36.997Z → `FinishedAt` 17:41:45.787Z) | **0.1833** — identical to gross (one 11 s row) | **0.274** | **MISPREDICTION OF OVERHEAD, not of work:** the point carried ~20 s of container start + `loadDAFoam.sh` priced from D12R2's S0 stage; on a warm image it was ~2 s. The WORK was predicted right — pyHyp 4.2 s CPU (P5 HIT at the baseline's own value), OpenFOAM utilities 3 s. **Like-for-like anchor for any later pyHyp regeneration of this wing: ≈ 9 s in-container, 11 s container wall at np=1.** **WASTE 0.000 core-min, named separately** (`COMPUTE_BUDGET_CHARTER.md` §6). **$0.000157 DERIVED, NOT MEASURED** at $0.0513/core-h, c7a.4xlarge, reported-by-owner (§5). What it bought: the first measured pyHyp timing anchor for this wing and the retirement of the "cannot regenerate on demand" premise behind D4's single-mesh caveat. |
| **C-136** | 2026-08-26 | cfd | **F20-ISENTROPIC_VORTEX (2-D isentropic vortex, `rhoCentralFoam` Kurganov/vanLeer/Euler, three-level serial ladder 128² / 256² / 512², 4,000 / 8,000 / 16,000 fixed steps to t = 10, one period) — GRADED by the frozen comparator `grade_f20.py` (blob `68057e2e`, prereg `548fc02e`, 15 of 15 frozen paths disk == blob): G-F20-1 L2 density error `PASS` (1.159874e−03 in [1.391e−04, 1.252e−03], CONVERGING p = 2.779, GCI 5.10 % = 5.92e−05 abs) — **the registered order range [1.0, 1.6] MISSED** (value band, verdict unaffected; Richardson extrapolate 1.11e−03 ≠ 0 against an exact reference, raised to the supervisor); G-F20-2 box-mean KE `PASS` (1.005538797 vs exact 1.005633549, in band, CONVERGING p = 1.276, GCI 0.0020 %) — **registered as NOT A RESULT (non-monotone triple) and came out monotone: missed in the conservative direction.** Coarse-level model values reproduce the solver to 7 s.f. (E2) and 4 s.f. (KE). Rule 4 at all levels; rule 5 limb (1) Courant census; plateau ABSENT by registration; planted-zero PASS on both readers. Runner-launched 17:51:23Z, no agent attached. | **60.0 core-min** in `F20_ISENTROPIC_VORTEX_PREREGISTRATION.md` §8 at `548fc02e` (0.85 / 6.6 / 52.5 per level: 0.75 µs per cell-step + 0.4 ms per step, DERIVED from F15 and F4-M6.0 `rhoCentralFoam` records, not measured on this case); cap 240 | **68.667 core-min MEASURED** = ClockTime 44 + 377 + 3,699 s × 1 rank ÷ 60 (`verification/runs/F20_ISENTROPIC_VORTEX_runs/{coarse,medium,fine}/log.rhoCentralFoam`; ExecutionTime 44.36 / 376.08 / 3,697.95 s); ± 0.025 integer-second quantisation. **= $0.0587 DERIVED, NOT MEASURED** at $0.0513/core-h, c7a.4xlarge, reported-by-owner (`COMPUTE_BUDGET_CHARTER.md` §5) | **68.667 — cleaned == gross.** The fine level's 3,699 wall s exceeds the 3,600-s figure of the stall rule by the letter, but it is the level's registered work run to `End` (ExecutionTime 3,697.95 s = ClockTime), not a stall — carried gross as C-133 ruled for F15; 28.6 % of cap | **1.144** (per level 0.86 / 0.95 / **1.17**) | **THE FINE LEVEL'S PER-CELL-STEP TERM: 0.88 µs measured at 262k cells against the 0.75 µs basis (0.67 µs at 16k, 0.72 µs at 65k — the rate rises with cell count, cache-shaped), not the per-step overhead and not contention** (ClockTime − ExecutionTime = 0 / 1 / 1 s; the launcher's probe read 5.0–5.4 free cores at every level). Coarse and medium under-ran the basis (0.86, 0.95). **WASTE: 0.000 core-min, named separately.** Infrastructure note: the runner's `CAP_OVERRUN.txt` fired at 18:57:38Z at elapsed 3,975 s > 1.10 × 3,600 s — **1.10 × the ESTIMATE, not the 240 core-min CAP; an infrastructure record, not a cap breach** (the run reached 28.6 % of cap); runner fixed under the 17:46Z order. **Carry forward:** price serial `rhoCentralFoam` at ≈ 0.9 µs per cell-step above ~200k cells (0.7 µs below 100k); a registered order range and a registered triple-state are predictions that get scored, and both were missed here while both verdicts held | `verification/runs/F20_ISENTROPIC_VORTEX_runs/RESULTS.md`; `verification/runs/F20_ISENTROPIC_VORTEX_runs/{F20_GRADED.json,GRADE_F20.out,GRADE_F20.err}`; prereg §8 at `548fc02e`; runner tally `cases/F20_ISENTROPIC_VORTEX/launcher.queue.out`, `cases/F20_ISENTROPIC_VORTEX/CAP_OVERRUN.txt` |
| **C-137** | 2026-08-26 | heat-transfer | **K0f (F14 cooling ladder, ten arms after AMENDMENT 1) — GRADED by the frozen `analyse_k0f.py` (blob `764dedc6…`, `--expect-sha` armed): rung verdict `GATE REACHED`, tally 0 of 10 — every graded row `NOT A RESULT` at rule-5 limb (1), no level iteratively CONVERGED under §7.1 (max |ΔT| between the 36000/40000 checkpoints 0.14–13.7 K vs 2e-5 K); seed guard fired (1.756 K at G1@0.95); equivalence gate AGREE on M1_f; planted zero seen on T and U; reference NOT OBTAINED. Strict rule 10/10 (`mark_done_k0f.py`).** | **925.90** core-min POINT (A1.2: S 912.40 + I 13.50), CEILING 2 750.70, carried guard 2 748.64; A1.2c expectation ≈ 1 111; **$0.7916 DERIVED** | **1 378.383 core-min MEASURED** = 82 703 wall s × 1 rank ÷ 60 from ten `STATUS.*` (walls 3 162–17 913 s; ExecutionTime 97.0–99.9 % of ClockTime) | **1 378.383 — gross = cleaned**: all ten rows are over 3 600 wall s and none is a stall (each monotone to Time 40000 == endTime) | **1.489** (0.501 of CEILING; per case 1.21–1.78, growing with cell count: L1 1.21–1.29, L2 1.29–1.50, L3 1.74–1.78) | **RATE MISPREDICTION growing with mesh size, not contention, not waste.** Measured 3.07–3.26e-06 s/cell-it at L1, 3.18–3.76e-06 at L2, 4.41–4.54e-06 at L3 vs the registered POINT rate 2.549e-06 (K0cS × 1.6); C-99's L1 figure (1.21) reproduced, the L3 lines were priced at the L1 rate. Contention present, not limiting: ClockTime − ExecutionTime ≤ 3.1 % (worst M2_m 201 s of 6 576). **WASTE 0.000 core-min**, named separately; no cap crossing (largest wall/timeout 0.178). `C_lam` 0.75 laminar factor measured 0.836. A1.2c's ≈ 1.2× expectation MISSED (1.489). **Carry forward:** price L3 (≈ 100k cells) `buoyantBoussinesqSimpleFoam` serial at ≈ 4.5e-06 s/cell-it on this box, L1 at ≈ 3.2e-06. **$1.1785 DERIVED, NOT MEASURED**, `cost_basis` REPORTED-BY-OWNER. | `docs/campaigns/F14-cooling-ladder/K0f_RESULTS.md`; `verification/runs/F14-cooling-ladder/K0f_runs/log.analyse_k0f.20260826T205552Z.txt`, `STATUS.*`, `DONE.*`; prereg `K0f_PREREGISTRATION.md` §7.7 / A1.2 |
| **C-138** | 2026-08-26 | heat-transfer | **T10a-R2 (2LI c/m/f ladder on T10a's B1 ceiling) — GRADED by the frozen `analyse_t10aR2.py` (blob `a252463b`, selftest 17/17): 2 PASS (RR1 identity 0.0 W/m² over 7 056 faces, RR3 dev_f 0.0785653 %), 5 GATE FAIL (RR4a 0.943, RR4b 1.112, RR6a 7.44 W/m², RR6b 3.24, RR7 1.541), 2 NOT A RESULT (RR2, RR5: B1 triple −3268.2226 / −3268.3840 / −3268.0978 OSCILLATORY). Strict rule 3/3; R2_c's 1 s wall is a complete 20-iteration solve (ExecutionTime 1.62 s, registered basis 1.63 s), stated, not smoothed.** | **11.32** core-min POINT (§4: 0.03 / 0.22 / 11.07, R2_f priced from R_q's contended 664 s CPU), CEILING 44.63, cap 160; **$0.0097 DERIVED** | **9.067 core-min MEASURED** = 1 + 12 + 531 wall s × 1 ÷ 60 (`STATUS.R2_c`, `STATUS.R2_m`, `STATUS.R2_f` core_min 0.017 + 0.200 + 8.850) | **9.067 — gross = cleaned** (longest row 531 s) | **0.801** (per level 0.56 / 0.91 / 0.80; 0.203 of CEILING) | **CONSERVATIVE MISPREDICTION OF CONTENTION, not of work:** R2_f ran uncontended (ExecutionTime 529.5 s = 99.9 % of ClockTime 530) where the POINT carried R_q's 664 s contended CPU; R2_c is integer-second quantised (±1 s). Contention none limiting. **WASTE 0.000 core-min** named separately — the three 17:24–17:26Z guard refusals (AMENDMENT 1) ran no solver. Generation 3.43 core-min spent pre-freeze, named beside the ratio, not in it. **Carry forward:** an f-level 2LI `buoyantSimpleFoam` viewFactor case at n = 7 056 costs ≈ 530 s serial uncontended on this box. **$0.00775 DERIVED, NOT MEASURED**, `cost_basis` REPORTED-BY-OWNER. | `docs/campaigns/T-family/T10aR2_RESULTS.md`; `verification/runs/T-family/T10aR2_runs/gate_t10aR2.json`, `log.analyse_t10aR2.20260826T205552Z.txt`, `STATUS.R2_*`, `DONE.R2_*`; prereg `T10aR2_PREREGISTRATION.md` §2 / §4 at `9fa66065`; beside C-1 |
| **C-139** | 2026-08-26 | dafoam | **D5 (A2, curriculum) arm `ACC48`, chain r2 — WASTE ROW, its own figure, never absorbed. `D5-PREREG-DEF-1` (the `D4S-LAUNCHER-DEF-2` class): §4 priced ACC48/ACC192 at 3.000 core-min, cap 10.0, on "D4 ACC, C-94" — a 45 s acceptance primal — while the registered program is `compute_totals` on a cold staged copy at np=4, which must first colour the Jacobian. Container `d5_ACC48_20260826T204123Z_387321` (PATCHED `2927768a…`, cpuset 8,10,11,13, 12g, in-container deadline 150 s = cap × 60 ÷ 4) ran 20:41:23Z → 20:44:05Z with no agent alive and was cut by that deadline at 162 s: ledger `D4S_CAP_CROSSED arm=ACC48 core_min=10.133 cap=10.0 … action=REPORTED_RUN_CONTINUES` then `rc=124 wall_s=162 ranks=4 core_min=10.8 inspect(exit,oomkilled)=[124 false] delivered_cores_mean=[3.9792 n=9]`; log tail `Global ColorSweep: 0 135.64 s / Number of Uncolored: 98959 4` — the total-derivative step never started. Not the toolchain, not the host, not an OOM, not any agent (none alive 17:50–20:44Z); the launcher enforced exactly the cap it was given. Triaged by the supervisor from the kernel record (FOURTEENTH block §2); repaired pre-compute for the re-fire by D5 Addendum 3 (`b5b428bc`): ACC caps 60.0, prediction 40.0, ceiling 1960.0, every other cap/gate/band/label unmoved; re-fire entry `D5_chain_r3.json`.** | **3.000** core-min (`PREREGISTRATION.md` §4 at `a893355d`; cap 10.0, deadline 150 s) — a prediction for a DIFFERENT program than the one registered in §2 | **10.8** core-min MEASURED (162 s × 4, launcher ledger row from `docker inspect`; the container object removed by the launcher's post-bookkeeping after the row) | **10.8** — every core-minute of it is **WASTE** (rc=124 at the deadline, colouring only, no artefact); gross = cleaned (no row over 3600 s) | **3.60** against the 3.000 registered — a ratio between two different programs, stated for the record and NOT used for calibration; against the re-stated 40.0 the partial run reached 0.27 before the cut | **PRE-REGISTRATION MIS-ANCHORING, not misprediction of the registered program and not contention** (3.9792 of 4 cores delivered): the cost row inherited an anchor priced on another program (C-132's lesson repeated one item later, on the cost side rather than the launcher side). Lesson forward, now written into both items: a cost row names the PROGRAM it prices and the mesh it was measured on; a cap under one colouring's wall is a cap that will fire. **$0.0092 DERIVED**, `cost_basis` REPORTED-BY-OWNER. The sibling arm O48 in the same chain landed `rc=0` at 684.533 core-min against 638.9 (ratio 1.071, inside P4's [450, 700]) and is costed at grading, not here. | `cases/dafoam/ladder-a/A2/curriculum_D5/PREREGISTRATION.md` Addendum 3 (`b5b428bc`); run root `ledger.txt`, `STATUS.ACC48.deadline_20260826T204405Z`, `STATUS.chain.acc48_stop_20260826T204405Z`, `ACC48_20260826T204123Z_387321.log`, `ACC48_launch.out`; `docs/dafoam/QUEUE_RUNNER_CERTIFICATE.md` §1.2 |
| **C-140** | 2026-08-26 | heat-transfer | **T5 partial — `T5_CUBE_c` and `H_c` DONE under the strict rule (rc=0, End, Time 5000 == endTime, fields, age guard; `mark_done_t5.py` a74ce20d); frozen `analyse_t5.py` (9c2c1d44, selftest 16/16) wrote NO verdict — ladder incomplete, verbatim "No case has run: no rows are graded and no verdict is written"; rung `PENDING`. FINDING: the `H_c` interface-scheme control is blind by construction — its only registered difference (epoxy `laplacian(alpha,e\|h)` harmonic vs linear) coincides for a uniform single-material alpha; `air/T`, `air/U`, `epoxy/T`, `air/p_rgh` at 5000 byte-identical to `T5_CUBE_c` (md5 c2cac072…, 9981db36…, 0b035892…, b9dcf614…). Registration defect recorded, not amended.** | **45.6 core-min each** (AMENDMENT 2 table, Model B; timeout 8 208 s), /bin/bash.0390 each DERIVED | **16.083 + 16.567 = 32.650 core-min MEASURED** (`STATUS.T5_CUBE_c` wall 965 s, `STATUS.H_c` 994 s, ranks 1) | **32.650 — gross = cleaned** (longest row 994 s) | **0.353 / 0.363** | **RATE MISPREDICTION, conservative:** Model B carried T9a-D's per-cell-iteration rate onto a 5.4e4-cell conjugate case that runs ~2.8× faster; contention present, not limiting (ExecutionTime 96.3 / 96.4 % of ClockTime at 14–16 busy cores). **WASTE 0.000 core-min** on these two; the 17:41Z T5_C crash was 0 core-min (AMENDMENT 8). The `H_c` spend bought a measurement of the control's blindness, named, not netted. **/bin/bash.0279 DERIVED, NOT MEASURED**, `cost_basis` REPORTED-BY-OWNER. Rung row follows at completion. | `docs/campaigns/T-family/T5_RESULTS.md` (partial); `verification/runs/T-family/T5_runs/log.analyse_t5.20260826T211657Z.txt`, `STATUS.T5_CUBE_c`, `STATUS.H_c`, `DONE.T5_CUBE_c`, `DONE.H_c`; prereg §5.6 / §16.5 / AMENDMENT 2 at `503a9a13` |
| **C-141** | 2026-08-26 | ansys-verification | **VMFL064-R2 (backward-facing step, Armaly `LR/s`, `simpleFoam` laminar Re_D = 200, three-level r = 2 ladder 3 072 / 12 288 / 49 152 cells, RANKS = 1) — GRADED `GATE REACHED` by the frozen comparator `grade_vmfl064_r2.py` (blob `e04fdf93`, prereg `abe17c3b` frozen at `3e7c792c`, `freeze OK` printed against both blobs before every level).** `LR/s` = 4.853056 at L3, 2.9389 % from the experimental 5.0 inside a 10 % band, triple CONVERGING, `p_obs` 0.7224, `GCI_fine` 2.0733 %. Register row #30; it cites row #29 (`NOT A RESULT`) and does not overwrite it. | **5.4** core-min (`PREREGISTRATION.md` line 12 at `3e7c792c`; cap 90 core-min RUNNING TOTAL across the three levels) — and the basis was a **measurement of the same byte-identical inputs**, attempt 1's 5.383 core-min at register row #29 / C-131, with only the reader changed | **5.3** core-min MEASURED = 1 + 17 + 300 wall s × 1 ÷ 60 (`COST.txt`; `RUN_RC.L1/.L2/.L3` core_min 0.0167 + 0.2833 + 5.0) | **5.3 — gross = cleaned** (longest row 300 s, far below the 3600 s stall trigger); **WASTE 0.000 core-min named separately** — the 17:49:13Z first launch was refused by the launcher's own argv guard **before OpenFOAM was sourced** and spent nothing | **0.98** (5.9 % of the 90 core-min cap, never approached) | **NEARLY NO GAP, AND THE REASON IS THE BASIS, NOT SKILL: a prediction anchored on a MEASUREMENT of the same inputs is the cheapest accurate prediction available.** Attempt 1 ran this exact mesh family and these exact schemes to `residualControl` convergence at 424 / 907 / 2 152 iterations for 5.383 core-min; the R2 changed only the comparator's reader, so the solver work was known rather than estimated. **Carry forward, and it is the transferable part: an R2 that changes only the READER should be priced from its parent's MEASURED cost, not re-derived from a rate — and a rate re-derivation here would have been worse, since VMFL076 measured that a per-cell-per-iteration rate calibrated on a small mesh under-predicts by 2.67x at 16x the cells (C-1xx / VMFL076 §5).** Contention not limiting and not separated: no uncontended control was bought, so none is inferred. **INFRASTRUCTURE, voiding nothing (L-342): the runner's `CAP_OVERRUN.txt` is stamped 20:45:38Z, FIVE SECONDS BEFORE the graded launch began at 20:45:43Z, and its "elapsed 10585 s > 1.10 x registered 324 s" is keyed on the ATTEMPT-1 launch epoch — it prices three hours in which nothing was running. It says `REPORTED, NOT ENFORCED`, nothing was killed, and NO CAP WAS CROSSED. The cost row above is measured from the launcher's own per-level records, not from that file.** **$0.0045 DERIVED, NOT MEASURED**, `cost_basis` REPORTED-BY-OWNER at $0.0513/core-h (c7a.4xlarge) — the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` §5). | `cases/ansys_verification/VMFL064-R2/RESULTS.md`; `verification/runs/ansys_verification/VMFL064-R2/GRADING_VMFL064_R2.json`, `COST.txt`, `RUN_RC.{L1,L2,L3}`, `LAUNCH_RECORD.txt`, `CAP_OVERRUN.txt`, `STATUS.VMFL064-R2`; prereg `3e7c792c`; register row #30; beside C-131 (attempt 1). **ID NOTE (rule 11): this row was drafted under the previous free id and re-issued here — the maximum id was re-derived from the HEAD blob at commit time and a heat-transfer peer had taken that id in `af6df4fe` between this lane read and its commit. The collision was caught by the re-derivation, not by a reader afterwards.** |
| **C-142** | 2026-08-26 | cfd | **F17b-KV40-EXT (Kovasznay flow, Re = 40, LADDER EXTENSION of F17 on the byte-identical case definition: three-level serial `simpleFoam` ladder 24,576 / 98,304 / 393,216 cells, 4,000 fixed iterations) — GRADED `NOT A RESULT` × 2 by the frozen comparator `grade_f17.py` (blob `cfebd8a4`, prereg `61b47973`, 15 of 15 frozen paths disk == blob).** Rule 5 limb 1: the registered Class C plateau fails on medium and fine for E2 (relative drift 1.46e−02 / 1.35e−01 against 2e−04) and on medium for u(probe) (variance ratio 0.097 outside [0.2, 5]); residual census clean at every level (0 of 1,200 above tolerance). Fine values inside both bands (E2 6.155850e−06 in [2.300e−06, 2.070e−05]; u(probe) 0.382383179 in [0.382373500, 0.382394846]) and raw triples CONVERGING p 1.858 / 3.471 — printed beside, not a result. Registered prediction (p ≈ 2, PASS × 2) NOT MET: the iterative floor inherited from F17 is not deep enough at the fine level's error scale. Rule 4 at all three levels; planted-zero PASS on both readers; 11/11 controls. Instrument-check, counts to no column. | **32.1 core-min** frozen in `F17b_KV40_EXT_PREREGISTRATION.md` §8 at `61b47973` (0.97 / 5.03 / 26.1); cap 150; basis F17's MEASURED 0.590 µs per cell-iteration on the same case definition, scaled by cells and +30 % per doubling — derived from the parent's measurement, not measured on these levels | **26.8667 core-min MEASURED** = ClockTime 67 + 371 + 1,174 s × 1 rank ÷ 60 (`verification/runs/F17b_runs/{coarse,medium,fine}/log.simpleFoam`); the runner's own tally in `cases/F17b_kovasznay_ext/launcher.queue.out` reads 26.866666666666667 — agrees to every digit; ± 0.0083 per level integer-second quantisation. **= $0.02297 DERIVED, NOT MEASURED** at $0.0513/core-h, c7a.4xlarge, reported-by-owner (`COMPUTE_BUDGET_CHARTER.md` §5) | **26.8667 — cleaned == gross.** Longest level 1,174 wall s; nothing matches the 3600-s stall rule; 17.9 % of cap | **0.837** | **THE F17 MEASURED BASIS HELD — a rate measured on the same case definition predicted the ×16 extension to within a factor 1.2; the residual 16 % is the +30 %-per-doubling GAMG-growth term, pessimistic at fine and optimistic below it.** Measured 0.682 / 0.944 / 0.746 µs per cell-iteration (coarse / medium / fine) against the registered 0.590 / 0.767 / 0.997: coarse (= F17's fine grid re-run) +16 % under a full box (launcher probe 0.5 free cores of 16, load 19.6), medium +23 %, fine −25 %. Contention: in the box load but NOT in the wall-versus-CPU reading — ExecutionTime/ClockTime 0.992 / 0.992 / 1.000, the serial rank was never descheduled; the slower rates under load are consistent with shared-cache/bandwidth pressure and are not separable into a contention figure. **WASTE: 0.000 core-min, named separately** — no stall, kill, re-run or cap movement; the compute bought a NOT A RESULT that is itself the finding (the fixed-iteration floor does not scale with the fine level's error). **Carry forward:** laminar 2-D `simpleFoam`, serial, on this box 0.68–0.94 µs per cell-iteration under a full box (0.45–0.59 idle-ish, C-126); reuse the parent's measured rate for an extension ladder, and register the iterative floor against the FINE level's predicted error, not the parent's | `verification/campaign/F17b_KV40_EXT_RESULTS.md` at `f2943b0b`; `verification/runs/F17b_runs/F17b_GRADED.json`, `F17b_GRADED.out`; prereg §8 at `61b47973`; runner tally `cases/F17b_kovasznay_ext/launcher.queue.out` |
| **C-143** | 2026-08-26 | heat-transfer | **T5 `T5_CUBE_m` — DONE under the strict rule (`mark_done_t5.py` blob `a74ce20d`, disk == registered: rc=0, `End`, `Time` 5000 == `endTime` 5000, 5 000 `ExecutionTime` lines, `air/{T,U,alphat,k,nut,omega,p,p_rgh,phi,rho}` + `epoxy/{T,p}` at 5000, age guard `5000/air/T` 22:13:02Z newer than `0/air/T` 20:57:21Z). The frozen `analyse_t5.py` (blob `9c2c1d44`, disk == registered, run exactly as registered `--root .`, rc 0) wrote NO verdict on the partial C/M/F triple — verbatim: `T5_CUBE_c: done=True`, `T5_CUBE_m: done=True`, `T5_CUBE_f: done=False -- no STATUS file`, then "No case has run: no rows are graded and no verdict is written." Rung verdict `PENDING`; no number from this rung is quoted as graded and the planted-zero control was not reached. Recorded as printed, not argued with (the closing sentence is the frozen file's fixed wording for an incomplete ladder and is now false of two levels, as its own first two lines say; the claim that governs — no rows graded — is correct).** Also recorded this session: `X_2d` stands UNGRADED BY REGISTRATION (AMENDMENT 1) — the frozen comparator exposes NO precursor arm (0 grep matches for `x_2d|re_theta|station|precursor|inflow`; `LEVELS = ("c","m","f")`), so no grade was invented; its strict-rule numbers are 5.217 core-min against POINT 26.4, ratio 0.198. `S_m` remains un-enqueued and adds 0.0 core-min: `build_t5.py` l.624's fluid-only meshing branch is NOT IMPLEMENTED, and `queue_entry_check.py` REFUSED the drafted entry (rc 2, cwd does not exist). | **530.4 core-min** (`T5_PREREGISTRATION.md` AMENDMENT 2 table at `299296a2`, Model B §11.1 row `T5_CUBE_m` 8.84 core-h × 60 at ranks 1; cap/timeout 95 472 s); **$0.4535 DERIVED** | **75.650 core-min MEASURED** = 4 539 wall s × 1 rank ÷ 60 (`verification/runs/T-family/T5_runs/STATUS.T5_CUBE_m`, written in-wrapper by the frozen `run_one_t5.sh` `313df45c`; ExecutionTime 4 490.88 s = 98.94 % of ClockTime 4 539 s) | **75.650 — gross = cleaned.** The 4 539 s row exceeds the 3 600 s stall figure by the letter and is **not a stall**: `Time =` monotone to 5000 == `endTime`, 5 000 `ExecutionTime` lines, one `End`, cap reached to only 4.8 % (`capped=0`) | **0.1426** | **MISPREDICTION OF THE MESH SCALING — not contention, not waste.** Model B priced c → m at **×11.63** (45.6 → 530.4 core-min) where the mesh grows **×4.04** (53 553 → 216 214 cells air+epoxy, `log.checkMesh`) and the box delivered **×4.70** (16.083 → 75.650). Measured per cell-iteration: **3.47e-06 s at 53 553 cells → 4.15e-06 s at 216 214 cells** — a 20 % cache-shaped rise, the same direction K0f measured at C-137 and an order of magnitude short of Model B's assumed super-linearity. Contention present, not limiting: 1.06 % ClockTime excess at ~14 of 16 busy cores. **WASTE: 0.000 core-min**, named separately (`COMPUTE_BUDGET_CHARTER.md` §6). **Carry forward:** price serial `chtMultiRegionSimpleFoam` on this box at **≈ 3.5e-06 s per cell-iteration below ~60 k cells and ≈ 4.2e-06 at ~220 k**; on that basis `T5_CUBE_f` (registered POINT 2 308.2 core-min) should land near **300–350 core-min** — a prediction this rung will score. **$0.0647 DERIVED, NOT MEASURED** at $0.0513/core-h, c7a.4xlarge, `cost_basis` REPORTED-BY-OWNER (`COMPUTE_BUDGET_CHARTER.md` §5 — the box cannot read its own billing). | `docs/campaigns/T-family/T5_RESULTS.md` §6–§12 (appended 22:14Z); `verification/runs/T-family/T5_runs/log.analyse_t5.20260826T221331Z.txt`, `STATUS.T5_CUBE_m`, `DONE.T5_CUBE_m`; prereg `299296a2` §11.1 / AMENDMENT 2 / AMENDMENT 6(b); beside C-140 (`T5_CUBE_c`, `H_c`). **DEFECT IN THE NEIGHBOURING ROW, reported not edited: C-140's predicted and derived USD cells read `/bin/bash.0390` and `/bin/bash.0279` — an unquoted-heredoc `$0` expansion at its authoring; the intended figures are $0.0390 each and $0.0279, which `T5_RESULTS.md` §4 carries correctly.** |
| **C-144** | 2026-08-26 | ansys-verification | **VMFL011-R2 (laminar triangular cavity, Jyotsna & Vanka `u_min/U_wall` benchmark, `simpleFoam` steady laminar SIMPLEC Re = 400, three-level r = 2 ladder 800 / 3 200 / 12 800 cells, RANKS = 1, `endTime` 20 000 SIMPLE iterations) — the frozen comparator `grade_vmfl011_r2.py` (blob `45aa4613`, prereg `a8c9b6f3` frozen at `9f9d6925`, both blob-verified equal on disk, at the freeze and at HEAD) REFUSED (exit 2) on its planted-zero control for `u_min_norm`; verdict `NOT A RESULT`, register row #31, which cites row #26 and does not overwrite it.** A completed process under rule 12: three levels ran to `endTime` with `rc = 0` and strict completion (rule 4) holding at all three, and the grading was performed and recorded. **No lab value, no triple, no `p_obs`, no GCI — the refusal preceded them.** The L-340 plant-sizing repair the R2 was registered to make DID work on the real attempt-1 bytes (sized plant delta 1.185698e-01 > 3.221137e-02, inside the derived bounds); the refusal came from the OTHER channel, whose fixed point plant lands on the apex row where `u ≡ 0` and therefore cannot move a `min()` reader. | **8.3** core-min (`PREREGISTRATION.md` line 12 at `9f9d6925`; cap **50** core-min RUNNING TOTAL across the three levels) — and the basis was a **measurement of the same byte-identical inputs**, attempt 1's **8.2167** core-min at register row #26, rounded up; nothing in the R2 changed a mesh, a scheme or an iteration count | **9.0833** core-min MEASURED = (17 + 63 + 465) wall s × RANKS 1 ÷ 60 (`COST.txt` `total_core_min = 9.0833`; `RUN_RC.L1/.L2/.L3` `core_min` 0.2833 + 1.0500 + 7.7500) | **9.0833 — gross = cleaned** (longest level 465 wall s, far below the 3600 s stall trigger); **WASTE 0.000 core-min named separately** — every level reached `endTime` with `rc = 0`, and a comparator refusal consumes no solver time and is not a re-run | **1.094** (18.2 % of the 50 core-min cap, never approached) | **A 9.4 % OVERRUN OF THE ESTIMATE, AND IT IS CONTENTION, NOT MISPREDICTION — and the distinction is measured, not asserted.** The prediction was anchored on attempt 1's **measured** 8.2167 core-min for byte-identical inputs, so the solver work was known rather than estimated; what changed between the two runs is the box. `CONTENTION.txt` sampled at launch (2026-08-26T21:11:27Z) records **load average 14.79 on 16 cores** with one `rhoCentralFoam`, one `icoFoam` and three `python3` live, against attempt 1's uncontended run. A ~9 % wall inflation on an unchanged serial workload under that load is the expected size of the effect. **Carry forward, and it is the transferable part: the C-141 rule — an R2 that changes only the READER is priced from its parent's MEASURED cost — held again here to within 9 %, and the residual is a CONTENTION term, so the next refinement is to price contention explicitly rather than to widen the estimate.** A per-run uncontended control was not bought, so none is inferred. **CALIBRATION NOTE THAT IS NOT A COST: this is the second consecutive VMFL011 process to spend its full solver budget and produce NO GRADED NUMBER. The core-minutes were not wasted in the charter §6 sense — the run completed as registered — but the lab has now spent 8.2167 + 9.0833 = 17.30 core-min on this case for zero graded values, and the cause both times was the GRADING INSTRUMENT, not the solver. Named here so the pattern is priced rather than rediscovered.** **$0.0078 DERIVED, NOT MEASURED**, `cost_basis` REPORTED-BY-OWNER at $0.0513/core-h (c7a.4xlarge, Sanaa 2026-08-21/22) — the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` §5). | `cases/ansys_verification/VMFL011-R2/RESULTS.md`; `verification/runs/ansys_verification/VMFL011-R2/GRADING.txt`, `COST.txt`, `RUN_RC.{L1,L2,L3}`, `STATUS.VMFL011-R2`, `LAUNCH_RECORD.txt`, `CONTENTION.txt`; prereg `9f9d6925`; register row #31; beside the attempt-1 figure carried in register row #26 |
| **C-145** | 2026-08-26 | dafoam | **D4-SHIPPED (A2) arm `O` — SPEND, and the quote-and-strike of C-117's WASTE label.** C-117 read: ~~"of which **WASTE 731.667** (arm O: complete, intact, ungradeable)"~~ — STRUCK by this row, never rewritten: under the L-342 grader (Addendum 2/2b/2d, `d4s_grade.py` md5 `f825c2cd…` == its `08039792` blob) **arm O GRADES on its preserved `O/` artefacts plus the kernel record** (`d4_O_20260826T040414Z_3177545`, inspect [0 false], 04:04:14.84Z → 07:07:09.64Z): G1 rc and terminal limbs PASS with `field_sources` per field; G2 read (band A 50 of 164 rows outside, band B final \|CL − 0.5\| = 6.59e-05 > 1e-5 → fails); G3 `converged=False` (100 majors, `EXIT: Maximum Number of Iterations Exceeded`); G4 28.694 % in band [25, 45]; G8/G9/G12 PASS; G10 `GATE FAIL` (731.667 > 620.0, registered at A2.3). Grade file `cases/dafoam/ladder-a/A2/curriculum_D4_SHIPPED/d4s_grade_ITEM_F3r2_20260826T220904Z.json` — REFUSED at G1-age on F3's absent artefact, not on O. | 511.133 core-min, registered as a LOWER BOUND (prereg §4.2), cap 620.0 | **731.667 core-min** measured-derived (kernel StartedAt/FinishedAt × 4 ranks) | 731.667 (no stall: the 10,975 wall s is the run) | **1.431** | +25 % majors (100 vs 80 — §4.2's caveat fired) × +14.6 % wall/major (peer `d12y` containers live throughout; contention NAMED, no uncontended control bought); **cap crossing +18.0 %** named, never absorbed; $0.6256 DERIVED at $0.0513/core-h, not measured. Waste on this arm: none (C-117's label struck here). | `curriculum_D4_SHIPPED/RESULTS.md` §7.3, §7.7; `PREREGISTRATION.md` Addendum 2e; the commit carrying this row |
| **C-146** | 2026-08-26 | dafoam | **D4-SHIPPED (A2) arm `F3` r2 — SPEND on a registered acceptance decision, `rc=1`; the SHIPPED FD table is `NOT A RESULT`.** Runner-launched 20:50:06Z (`verification/queue/LAUNCH_LOG.tsv:34`, prereg `08039792`, `bc0e687e`); container `d4_F3_20260826T205120Z_411184`, cpuset 5,6,7,9, 12g, deadline 1,800 s in-container, inspect [1 false], wall 41 s. The endpoint primal plateaued from Time ≈ 400 at `nuTilda` initRes **1.115891818e-05** (flat to 0.004 % over 600 iterations) above the producer's acceptance 1e-8 × 1e3 = **1e-5** (`d4_opt_runScript.py:36-37`); D4 PATCHED's endpoint floor **9.780100659e-06** was accepted on the same rule (floors differ 1.141×). Triage: INDEPENDENT of staging (the `decomposePar` block is DAFoam's own and identical on the PATCHED log `:76-112`; the decomposition is byte-identical across both rows and equals P1's `decomposePar -force`); **`D4S-PREREG-DEF-1`** (acceptance threshold at the instrument's residual floor) found, not repaired; **no re-fire, r3 NOT filed**; ACC (chain position 2) never fired, 0. | 47.267 core-min (D4's measured PATCHED F3), cap 120.0 | **2.733 core-min** (ledger row, 41 s × 4) | 2.733 | **0.058** | the arm ended at its first primal's acceptance decision; the prediction priced a full FD sweep that never began — a misprediction of OUTCOME, not of rate; $0.0023 DERIVED, not measured. Labelled SPEND (the registered program ran on its registered input to its registered decision); the supervisor may relabel WASTE — either way its own figure, never absorbed. | `curriculum_D4_SHIPPED/RESULTS.md` §7.2, §7.7; `PREREGISTRATION.md` Addendum 2e A2e.2–A2e.4 |
| **C-147** | 2026-08-26 | dafoam | **D12R2-W2R phase 1 (2D · unsteady · incompressible, cylinder, `W = 900`) — GRADED by the frozen comparator `d12y_grade.py` (blob `aecceb4e`, md5 `02a9ab62` asserted; `--plan` mode, the registered grading invocation) — `G12R-4` = `NOT A RESULT`: `h_min 1.575533e-01` > `h_max 5.000e-02`, `admissible: false`, phase 2 NOT LAUNCHED on the registered branch; ITEM `NOT A RESULT`.** 33 of 33 registered stages `rc=0`, `G12R-0b` binding PASS (33/33/33), P1 HIT, P2 HIT (`\|g\|` 1.1398 in [0.7213, 1.3396]), **P3 HIT (primary)**, P4 MISS on the frozen path (0.1576 outside [0.0738, 0.137]) — `W2R-GRADER-DEF-1` disclosed post-compute: the comparator evaluates `δ_window` at `W_PRIMARY = 300` in both modes (`:2013`, `:2511`; the manifest carries no `W`), the corrected reading with the frozen functions at `W = 900` is `δ_window 9.879556064e-04` → `h_min 8.667530e-02`, still > `h_max`; verdict invariant, nothing re-scored, frozen file untouched. Phases 2–4 wrappers registered to close `BLOCKED` at their bounds (last 2026-08-28T05:30:47Z), 0 core-min. Record `cases/dafoam/curriculum_D12R2/RESULTS_W2R.md`. | 121.35 core-min (W2R §6, carried from W2 §6), cap 600.0 | **105.2334 core-min** (ledger `SPENT_CORE_MIN`; Σ of 33 `STAGE=` rows) | 105.2334 (no row over 3600 wall s; longest S5 1,570 s) | **0.867** | the estimate scaled the window stages ×3 with `W` (32.9168 → 98.75); measured they scale ×2.4–2.6 (S3_r1 1.0667 → 2.8167; S5 10.9 → 26.1667) — a `W`-independent fixed cost the ×(W/300) law omits; the `W`-independent groups came in at or under D12R2's (S4 envelope 10.17 vs 13.68). Next-window estimates: `fixed + slope·W` from these two points. Waste: none on this item (W2's 1.2 core-min is W2's own figure, not absorbed). $0.0900 DERIVED at $0.0513/core-h, not measured. | `RESULTS_W2R.md` §2, §6; `W2R_PREREGISTRATION.md` @ `b168779c`/`5d1f89cd`; grade file `<run root>/W2R_phase1_grade_V2_plan_20260826T221226Z.json` |
| **C-148** | 2026-08-26 | heat-transfer | **CORRECTION ROW — it corrects `C-140` (T5 `T5_CUBE_c` + `H_c`, landed `af6df4fe`). `C-140` is NOT edited (this file's append rule 1: "An existing row is never edited; a correction is a new row naming the row it corrects"); its wording is quoted and struck here and left standing there.** **What `C-140` says, quoted verbatim from the committed blob:** in its predicted column, `` **45.6 core-min each** (AMENDMENT 2 table, Model B; timeout 8 208 s), /bin/bash.0390 each DERIVED ``; and in its gap-attribution column, `` **/bin/bash.0279 DERIVED, NOT MEASURED**, `cost_basis` REPORTED-BY-OWNER ``. **STRUCK.** **CAUSE, named rather than guessed:** an unquoted heredoc at that row's authoring — the literal `$0.0390` and `$0.0279` were written inside a shell heredoc whose delimiter was not quoted, so the shell expanded `$0` (the shell's own name, `/bin/bash`) and left the trailing digits, yielding `/bin/bash.0390` and `/bin/bash.0279`. It is a text-rendering defect at authoring only: **no core-minute figure, ratio, band, cap or verdict in `C-140` is affected**, and the same three dollar figures are carried CORRECTLY in `docs/campaigns/T-family/T5_RESULTS.md` §4, which was written from the same numbers without a heredoc. | **`$0.0390` DERIVED for `T5_CUBE_c` and `$0.0390` DERIVED for `H_c`** — each = 45.6 core-min ÷ 60 = 0.7600 core-h × $0.0513/core-h = $0.038988, rounded to 4 d.p. (`T5_PREREGISTRATION.md` AMENDMENT 2 table at `299296a2`, Model B §11.1, 0.76 core-h per coarse case) | 32.650 core-min MEASURED, unchanged from `C-140` (`STATUS.T5_CUBE_c` wall 965 s + `STATUS.H_c` wall 994 s, × 1 rank ÷ 60) | 32.650 — gross = cleaned, unchanged from `C-140` (no row over 3 600 wall s) | 0.353 / 0.363, unchanged from `C-140` | **`$0.0279` DERIVED, NOT MEASURED** for the pair — = 32.650 core-min ÷ 60 = 0.544167 core-h × $0.0513/core-h = $0.0279157, rounded to 4 d.p. All three figures are **DERIVED, NOT MEASURED**, at $0.0513/core-h, c7a.4xlarge, **reported-by-owner** — the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` §5). **`C-140`'s gap attribution itself (rate misprediction, conservative; contention present not limiting; WASTE 0.000 core-min) is correct as written and is NOT corrected here.** **Carry forward, and it is the transferable part: write a dollar figure into a shell heredoc only with a QUOTED delimiter (`<<'EOF'`), or the shell silently rewrites `$0`, `$1` … into positional-parameter expansions inside what looks like plain prose.** | `docs/COST_CALIBRATION.md` row `C-140` (the row corrected, landed `af6df4fe`); `docs/campaigns/T-family/T5_RESULTS.md` §4 (carries the three figures correctly); `verification/runs/T-family/T5_runs/STATUS.T5_CUBE_c`, `STATUS.H_c`; prereg `299296a2` §11.1 / AMENDMENT 2. Raised by the T5 grading lane, ruled by the heat-transfer supervisor `[lab-attributed]` (quote-and-strike, never rewrite). |
| **C-149** | 2026-08-26 | ansys-verification | **VMFL017-R2 (transonic RAE 2822, AGARD AR-138 `Cd` 0.0168 / `Cl` 0.803, `rhoCentralFoam` EXPLICIT TRANSIENT kOmegaSST, M = 0.73, Re = 6.5e6, 2-D C-mesh L1 = 23 040 cells, RANKS = 1, `endTime` 0.05 s of physical time at `maxCo` 0.2) — L1 stopped at its own registered per-level cap with `rc = 124` after 18 000 wall s, the launcher stopped WITHOUT launching L2 or L3 (its line 279), and the frozen comparator `grade_vmfl017_r2.py` (blob `97c556f4`, prereg `9a58eed3` frozen at `45328f8a`, both blob-verified equal on disk, at the freeze and at HEAD, and printed `freeze OK` by the launcher before compute) REFUSED (exit 2) on strict completion — no `End` line. Verdict `NOT A RESULT`, register row #32, which cites row #19 (`PENDING`) and does not overwrite it.** A completed process under rule 12: the cap fired as registered and the grading was performed and recorded. **No lab value, no `Cd`, no `Cl`, no plateau, no triple, no GCI.** | **300.0** core-min — L1's per-level cap frozen at `PREREGISTRATION.md` line 12 (L1 300 / L2 600 / L3 1 500, PER LEVEL, not a shared drawdown; family ceiling 2 400 if all three ran) and carried as the queue entry's `cost_core_min_estimate` **because PRE-COMPUTE AMENDMENT 2 §E had already registered that L1 would stop AT that cap.** The prereg's family figure "~800–1600 core-min TOTAL" is declared UNCERTAIN in the freeze and a **runaway-guard basis, not a confident point estimate**, and is NOT used as the denominator here | **300.0** core-min MEASURED = 18 000 wall s × RANKS 1 ÷ 60 (`RUN_RC.L1` `core_min = 300.0`; `LAUNCH_RECORD.txt` `total_core_min = 300.0`, which agree; **there is NO `COST.txt` — the launcher stopped on the cap before writing one**) | **300.0 — gross = cleaned.** The 3600-s stall rule matches this row on wall time (18 000 s) and **I am clearing it with evidence, not waving it**: `ExecutionTime` **17 761.7 s** against `ClockTime` **17 999 s** is **98.68 % CPU-bound**, so the process was **computing, not stalling**, on a box at load average 13.73 of 16 cores. **WASTE 0.000 core-min named separately** — nothing abandoned, nothing re-run, the cap stopped the level exactly as registered | **1.000** (100.0 % of L1's cap, which fired to the second) | **THE RATIO IS 1.000 AND IT IS NOT A CALIBRATION WIN — I AM SAYING SO RATHER THAN BANKING IT.** The spend was a **cap**, not a forecast: a cap that fires is a budget statement, and a ledger that scores it as a perfect prediction learns nothing. **THE REAL CALIBRATION CONTENT IS AMENDMENT 2 §E's PRE-COMPUTE PROJECTION, and all three of its numbers held on the graded run:** physical time reached inside the cap **8.89114e-04 s (1.778 % of `endTime`)** against a projected ~9.0e-04 s (~1.8 %) → **0.988**; integration rate **27.04 steps per wall-second** (486 748 steps in 17 999 wall s) against a projected ~27.7 → **0.976**; **core-minutes for L1 ALONE to reach `endTime` 16 871** (300.0 × 0.05 ÷ 8.89114e-04) against a projected ~16 700 → **1.010**, i.e. **about 56× L1's own cap**. Realised adaptive step at the stop **1.852520264e-09 s** against §E's registered 1.806e-9 s. **CARRY FORWARD, AND IT IS THE TRANSFERABLE PART: for an EXPLICIT transient solver the cost driver is the CFL-limited timestep integrated to a settled state, NOT an iteration count, and a 110-second pre-compute smoke measuring the realised Δt and the steps-per-wall-second priced a 5-hour run to within 1–2 %. That smoke cost ~2 core-min and would have saved 300 had the lab been willing to act on it; the lab registered it, launched anyway to convert the projection into a graded-path measurement, and says so here.** L2 and L3 are worse by ~8× and ~64× (four/sixteen times the cells at half/quarter the step), so the family ceiling of 2 400 core-min is **not within an order of magnitude** of what this instrument needs — **the honest next step is a DIFFERENT registration (implicit / dual-time, or a wall function raising the near-wall cell by three decades), never a bigger budget for this one.** **NAMED SEPARATELY AND NOT LAUNDERED INTO THE RATIO: 300.0 core-min bought NO GRADED NUMBER.** It is not waste in the charter §6 sense — nothing was abandoned or re-run — and calling it waste would hide that the lab chose to buy the measurement with the projection already on the record. **$0.2565 DERIVED, NOT MEASURED**, `cost_basis` REPORTED-BY-OWNER at $0.0513/core-h (c7a.4xlarge, Sanaa 2026-08-21/22) — the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` §5); inside the 2026-08-21 under-$25 blanket and still costed per item. | `cases/ansys_verification/VMFL017/R2/RESULTS.md`; `verification/runs/ansys_verification/VMFL017/R2/GRADING.txt`, `RUN_RC.L1`, `STATUS.VMFL017-R2`, `LAUNCH_RECORD.txt`, `CONTENTION.txt`, `launcher.queue.out`; prereg `45328f8a` (AMENDMENT 2 §E is the projection this row calibrates); register row #32; beside row #19 (attempt 1, `PENDING`) |
| **C-150** | 2026-08-26 | heat-transfer | **T5 `P_m` (the §8 `DP` `Pr_t` arm, medium level, `Pr_t = 0.50` against the registered 0.85) — DONE under the strict rule (`mark_done_t5.py` `a74ce20d`, disk == registered: rc=0, `End`, `Time` 5000 == `endTime`, 5 000 `ExecutionTime` lines, fields at 5000, age guard `5000/air/T` 22:32:37Z newer than `0/air/T` 21:23:42Z). The arm's registered difference was READ, not assumed: `P_m/0.orig/air/alphat` carries `Prt 0.5;` against `T5_CUBE_m`'s `Prt 0.85;`. The frozen `analyse_t5.py` (`9c2c1d44`, disk == registered, `--root .`, rc 0) again wrote NO verdict — "No case has run: no rows are graded and no verdict is written" — and **`P_m` does not appear in its listing at all, by registration and not by omission**: `LEVELS = ("c","m","f")` (l.26) and the `--root` loop builds `"T5_CUBE_" + lv`, so the comparator never looks at `P_m`, `L_m`, `H_c` or `S_m`. **The `DP` criterion (`SEPARATED` if `P_m` moves any of G1a–G3a by more than that row's ≈ 10.2 % band) is computed from graded rows that do not yet exist, so NO separation claim is made and none is available.** `T5_CUBE_f: done=False` in the same listing is correct and is not a failure — `T5_F` launched 22:22:31Z and was at `Time = 121` at 22:33Z; the frozen launcher writes `STATUS` only on solver exit, so an in-flight case reads the same as an unlaunched one. Rung verdict `PENDING`.** | **530.4 core-min** (`T5_PREREGISTRATION.md` AMENDMENT 2 at `299296a2`, Model B §11.1 row `P_m` 8.84 core-h × 60 at ranks 1; timeout 95 472 s); **$0.4535 DERIVED** | **68.883 core-min MEASURED** = 4 133 wall s × 1 rank ÷ 60 (`verification/runs/T-family/T5_runs/STATUS.P_m`; ExecutionTime 4 102.99 s = 99.27 % of ClockTime 4 133 s) | **68.883 — gross = cleaned.** Over the 3 600 s stall figure by the letter and **not a stall**: `Time =` monotone to 5000 == `endTime`, 5 000 `ExecutionTime` lines, one `End`, cap reached to 4.3 % (`capped=0`) | **0.1299** | **THE SAME MESH-SCALING MISPREDICTION AS C-143, PLUS A CLEANER BOX — and NOT the `Pr_t` change.** `P_m` and `T5_CUBE_m` are a like-for-like pair (identical mesh, 212 942 air + 3 272 epoxy cells; the only registered difference is a boundary coefficient): **3.795e-06 s per cell-iteration here against `T5_CUBE_m`'s 4.154e-06**, an 8.6 % improvement fully accounted for by contention — 99.27 % ExecutionTime/ClockTime against `T5_CUBE_m`'s 98.94 % and the coarse pair's 96.3 %/96.4 % as the box drained. **`Pr_t` is a wall-function coefficient, not a work term; it should not have moved the cost and it did not** — stated because a 8.6 % gap between two cases that differ only in `Prt` would otherwise invite the wrong inference. Model B's c → m scaling error (×11.63 registered against ×4.04 in cells) is the whole of the 0.13 ratio, as at C-143. **WASTE: 0.000 core-min**, named separately (`COMPUTE_BUDGET_CHARTER.md` §6). **$0.0589 DERIVED, NOT MEASURED** at $0.0513/core-h, c7a.4xlarge, `cost_basis` REPORTED-BY-OWNER (`COMPUTE_BUDGET_CHARTER.md` §5). **Rung total so far 177.183 core-min over four graded cases** (`T5_CUBE_c` 16.083 + `H_c` 16.567 + `T5_CUBE_m` 75.650 + `P_m` 68.883), excluding the ungraded precursor `X_2d` (5.217). | `docs/campaigns/T-family/T5_RESULTS.md` §13–§16 (appended 22:33Z); `verification/runs/T-family/T5_runs/log.analyse_t5.20260826T223248Z.txt`, `STATUS.P_m`, `DONE.P_m`; prereg `299296a2` §8 `DP` / §11.1 / AMENDMENT 2 / AMENDMENT 6(b); beside C-143 (`T5_CUBE_m`) and C-140 as corrected by C-148. |
| **C-151** | 2026-08-27 | heat-transfer | **K0f `ext1` — the ONE registered §7.1 extension (+20 000 iterations, 40 000 → 60 000) on ALL TEN arms, graded by the frozen comparator. `RUNG VERDICT: GATE REACHED`, TALLY 0 of 10 — unchanged, and unchanged for the registered reason: column `P` is UNREACHED because Blay, Mergui and Niculae (1992) is NOT OBTAINED.** Freeze set hashed before grading: seven of nine MATCH, including every instrument on this grading path (`analyse_k0f.py` `764dedc6`, `mark_done_k0f.py` `f01e3fce`, `launch_k0f_ext1.sh` `49ad67c0`). The two §7.7 launcher rows read `25071702`/`c54b7f9a` against `519e8361`/`c6e60dbd` on disk — **NOT drift**: AMENDMENT 2 (pre-first-compute) struck both rows, both files are byte-identical to their state at that amendment's own commit `b70b49c6`, and nothing has touched either since. **Bookkeeping defect REPORTED, not repaired: A2.3b strikes the rows but never transcribes the replacement shas, so two grading-path freeze values are recoverable only by resolving a commit** (L-342 — bookkeeping, not physics; neither file is on the extension's path). Completion: `mark_done_k0f.py` re-evaluated on all ten under the EXTENDED clauses (first `Time =` 40 001, 60 000 `ExecutionTime` lines over both logs, age datums `0/T` AND `STATUS.<case>`) → **10/10 `1/1 cases meet the strict completion rule`**. Controls before any graded value: §V.4 equivalence gate on `M1_f` at 60 000 **PASSED** (worst \|diff\| `T` 7.87e-08 K vs criterion 2.00e-05 K; `U.x` 3.57e-09 vs 5.70e-07 m/s); **planted-zero (rule 3): `P1` scalar reader SAW the 1.234e-03 K plant, `P2` vector reader SAW the 1.234e-03 m/s plant, `P3` negative control not distinguishable from background, as registered**. **Ten of ten arms still `converged=False`**: max \|dT\| over the last 4 000 iterations 2.36e-02 K (`M2_f`, best) to 1.45e+01 K (`C_lam`, worst) against a §7.1 criterion of ≈1.97e-05 K — **1 193× to 727 875× the criterion**. `P-A3.1` **HIT** (all ten unconverged); `P-A3.2` **HIT on substance** (`M2_f` again closest by two orders of magnitude; improved 5.85× over the extension, 0.1379 → 0.02358 K, still 1 193× its criterion). All ten graded rows `NOT A RESULT` at rule 5 step (1); two `CONVERGING` triples print a GCI that **cannot promote the row** (`G8` M1 GCI 125.7054 %, M2 67.9134 %; `G5a` M1 6.9092 %), and no GCI is quoted on a non-monotone triple. **`SEED` guard FIRES — seed dependence survived the extension** (G5a `M1_m` vs `M1_m_seed` worst 0.235272 at G2@0.9, outside its band); `DC` UNMEASURED; `M0` model-form floor REPORTED, NEVER SUBTRACTED. | **689.19 core-min** (A3.5 POINT = 0.5 × each arm's MEASURED segment-1 rate from C-137; cap sum 1 315; per-arm caps 51–284), **$0.5893 DERIVED** | **745.667 core-min MEASURED** = 44 740 wall s × 1 rank ÷ 60, summed from the ten `STATUS.*.ext1` `wall=` fields | **745.667 — gross = cleaned.** Six rows exceed the 3 600 wall-s stall figure and **not one is a stall**: every arm restarted at `Time = 40001` and ran monotonically to 60 000 with the full 20 000 `ExecutionTime` lines. **WASTE 0.000 core-min** named separately (`COMPUTE_BUDGET_CHARTER.md` §6) — no re-fire, no refused-after-arming arm, **no arm reached its cap** (worst `B_hi` at 61.8 % of 110) | **1.082** | **`P-A3.3` is a MISS on the form it was written in, and is scored as written.** It predicted *"ratio actual/POINT ≈ 1.0 ± 0.1 **per arm**"*: **7 of 10 arms inside ±0.1, 3 outside** — `B_hi` 1.174, `M1_f` 1.149, `C_lam` 1.103 (then `I_hi` 1.085, `M2_m` 1.084, `M2_f` 1.078, `M1_m_seed` 1.044, `M1_m` 0.986, `M1_c` 0.946, `M2_c` 0.938). The **aggregate** 1.082 is inside the band, and scoring the aggregate instead of the per-arm claim would turn a miss into a hit — so it is not. **Attribution: mild rate misprediction — the SECOND half of each run is dearer than the first — not contention and not waste.** POINT assumed iterations 40 001–60 000 cost exactly what iterations 1–40 000 did (`0.5 ×` segment 1); measured they cost **8.2 % more**, and the three arms outside the band are the three whose segment-1 rate was measured earliest, on a fuller box. **CARRY FORWARD, the transferable part: price a restarted SIMPLE continuation at ≈1.08 × the first segment's per-iteration rate, never 1.00 × — a restart reloads fields and rebuilds the matrix without the first segment's cheap early iterations.** **Rung total 2 124.050 core-min** (1 378.383 segment 1 + 745.667 extension) **against the registered CEILING 2 750.70 — INSIDE, at 77.2 %.** **$0.6375 DERIVED, NOT MEASURED** at $0.0513/core-h, c7a.4xlarge, `cost_basis` REPORTED-BY-OWNER — the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` §5). **§8.2 item 4 now applies: no second extension exists** and these arms' rows stand as reported with the measured spread. | `docs/campaigns/F14-cooling-ladder/K0f_RESULTS.md` §E1–§E7 (appended by this commit); `verification/runs/F14-cooling-ladder/K0f_runs/log.analyse_k0f.ext1.20260827T163448Z.txt` (comparator stdout, retained); the ten `STATUS.*.ext1` and `DONE.*`; prereg AMENDMENT 3 (§A3.1 predictions, §A3.5 cost lines) and AMENDMENT 2 §A2.3b; beside C-137 (the segment-1 rate this POINT was built from) |
| C-152 | 2026-08-27 | cfd | **F21_WOMERSLEY — rung GRADED, both gates `NOT A RESULT`.** Womersley pulsatile channel, `pimpleFoam`, three-level ladder 128²/256²/512² with h and Δt refined together (Co a ladder invariant). Coarse and medium PLATEAUED and second order; the FINE level completed rule 4 in full (rc 0, `End`, 4704/4704 `Time` lines, age guard NEWER) and every linear solve CONVERGED, yet its solution DIVERGED — Co_max 0.0342 → 68.41, max\|Ux\| 102.29 against an exact peak ≈ 1.05, periodicity change 38.53 against the 1e−5 tolerance. Rule 5 limb 1 fired first; the underlying band readings (GATE FAIL ×2) and triples (OSCILLATORY, DIVERGENT) are recorded beside the verdict, never as it. | **713.0** core-min registered (basis: 10.71 µs/cell-step MEASURED on this case's own coarse grid in a 24-step scratch probe, grown +72 %/+75 % per doubling from F18); CAP **1500.0**; $0.61 derived | **944.533** core-min from the three `log.pimpleFoam` ClockTimes × 1 rank ÷ 60 (186 + 3,125 + 53,361 s = 56,672 s) = **63.0 % of cap**; **$0.808 DERIVED, NOT MEASURED** at $0.0513/core-h, reported-by-owner | **944.533** — gross == cleaned: no row matches the 3,600-s stall rule (the solver worked continuously at every level) | **1.325** (cleaned/predicted) | **Misprediction, essentially all of it, and it is in the GROWTH EXPONENT not the base rate:** measured µs/cell-step 9.65 → 20.28 → 43.27, i.e. **+110 % then +113 % per doubling**, against the registered +72 %/+75 % borrowed from F18's `icoFoam`. The DIC-PCG count for `pimpleFoam`'s p-solve grows ≈ ∝ N on this case (≈ 900 iterations per corrector at 512² against ≈ 238 at 128²). The coarse level came in **10 % UNDER** its registered rate (9.65 vs 10.71 measured), so the base rate was right and the borrowed growth law was not — **the lesson for the next cfd estimate is to stop importing another solver's PCG growth exponent.** **Contention: NOT separable and NOT claimed** — all three levels serial on 1 rank on an 85–100 %-busy box, no per-level free-core reading was taken beside the ClockTime, so no share is attributable from the record. **WASTE, NAMED SEPARATELY AND NOT ABSORBED INTO THE RATIO (charter §6): 889.350 core-min — the ENTIRE fine level — bought no result.** The honest split of this rung is **55.183 core-min useful / 889.350 core-min unbought**; a future estimate must price F21's fine level as not delivered. | `verification/campaign/F21_WOMERSLEY_RESULTS.md` §5; `verification/runs/F21_runs/F21_GRADED.json` (`cost_claim`, `defects` empty); prereg frozen `448756a540b86dc4dc94ed094cc93db3284e5928` |
| C-153 | 2026-08-27 | cfd | **F22_LAMB_OSEEN — rung GRADED, `PASS` × 2.** 2-D decaying Lamb–Oseen vortex, `icoFoam`, three-level ladder 192²/384²/768² with h and Δt refined together. Both triples CONVERGING and monotone at dim 2, observed orders **1.9463** (E2) and **2.1188** (peak vorticity), both fine values inside their pre-registered bands. Rule 4 complete at all three levels; rule 5 limb-1 census 0/800, 0/1600, 0/3200 p readings above 1e−9; no plateau gate registered (transient value at fixed t = T) and recorded ABSENT. | **532.3** core-min registered (basis: 10.00 µs/cell-step MEASURED on this case's own coarse grid in a 24-step probe, grown +75 %/+80 % per doubling; the file's own asymptotic bound ≈ 666); CAP **1100.0**; $0.46 derived | **813.033** core-min from the three `log.icoFoam` ClockTimes × 1 rank ÷ 60 (137 + 2,583 + 46,062 s = 48,782 s) = **73.9 % of cap**, and **1.22× the pre-registration's own asymptotic bound**; **$0.695 DERIVED, NOT MEASURED** at $0.0513/core-h, reported-by-owner | **813.033** — gross == cleaned; no row matches the 3,600-s stall rule (the solver worked continuously at every level) | **1.528** (cleaned/predicted) | **Misprediction, in the GROWTH EXPONENT and not the base rate:** measured µs/cell-step 9.29 → 21.90 → 48.81, i.e. **+136 % then +123 % per doubling**, against the registered +75 %/+80 % — and above even the file's own stated asymptotic +100 % bound. The coarse level came in **7 % UNDER** its registered 10.00 µs basis, so the base measurement was sound and the imported growth law was not: `icoFoam` with DIC-PCG at 1e−9 / relTol 0, serial, grows FASTER than ∝ N per side on this box. **SECOND cfd rung on the same day to miss in exactly this way** — F21 (row C-152) measured +110 %/+113 % against a registered +72 %/+75 %; two independent cases now say the lab's per-doubling growth factor is too small, and that is the calibration lesson worth carrying, not either individual ratio. **Contention: NOT separable and NOT claimed** — all three levels serial on 1 rank on an 85–100 %-busy box, no per-level free-core reading beside the ClockTime; the pre-registration's own remark that its base rate was taken "at load 26.6" so contention "can only inflate it" is borne out in DIRECTION only. **WASTE: NONE** — no level failed, nothing was repeated, every core-minute bought a graded result; waste is named here as zero rather than omitted (charter §6). | `verification/campaign/F22_LAMB_OSEEN_RESULTS.md` §5; `verification/runs/F22_LAMB_OSEEN_runs/F22_GRADED.json` (`cost_claim`, `defects` empty); prereg frozen `29a47d994c708fddd856506fdaeea6025b44a166` |
| **C-154** | 2026-08-27 | dafoam | **D15 (A1) — NACA0012 subsonic compressible M 0.288, `DARhoSimpleFoam`, 4,032 cells, five-arm chain MESH/X-S/F-S/X-P/F-P at 2 ranks — GRADED by the frozen comparator `d15_grade.py` (md5 `b429ec89e7a738647081783b8b755711`, disk == its committed blob at the freeze `8fc2bdeb`; eight of eight frozen paths hash-MATCH), registered invocation `--root <run root> --out <...>`, rc 0. Item verdict `GATE FAIL`: SHIPPED row `GATE FAIL` (CD 2 PASS / 3 GATE FAIL, worst `shape[6]` 44.874 %, aggregate 34.681 %), PATCHED row `PASS` (CD 5/5, aggregate 0.047 %). P1-P4, P5, P6b HIT; **P6 MISS** (cost band). Re-run independently by the grading lane: json byte-identical.** Launched unattended by the queue-runner daemon 2026-08-27T11:32:30Z, pid 730849, no agent alive. | **28.3 core-min** (`PREREGISTRATION.md` §4 point; band [12, 80], ceiling 165.0 = sum of caps); **$0.0242 DERIVED** | **10.100 core-min MEASURED** (`ledger.txt`, five `ARM=` rows: MESH 0.167 + X-S 1.367 + F-S 3.433 + X-P 1.733 + F-P 3.400) | 10.100 — gross = cleaned, no row near the 3,600 wall-s stall figure (longest arm 103 s) | **0.357** | **MISPREDICTION OF THE COMPRESSIBLE PRIMAL RATE, NOT CONTENTION.** §4 adopted a x2 compressible factor over C-71's incompressible warm primal (0.14 -> 0.28 core-min) and priced each F arm at 32 primals x 0.28 + setup = 10.0; measured the F arms cost **3.433 and 3.400**, so a warm `DARhoSimpleFoam` primal on 4,032 cells at np = 2 is **~0.10 core-min** — the x2 factor was over-conservative by **~2.9x** and the true compressible-over-incompressible factor at this size is **below 1**, not 2 (np = 2 buys real speed on a mesh this small despite the communication argument). The X arms agree: 1.367/1.733 against a 4.0 point priced from C-31's np = 1 reading. **Contention present and NOT limiting**, named rather than blamed: `d16_X-S`, `d17_X-S`, `d16_F-S` and `d15_F-P` were live across these arms (launcher aggregate records) and delivered cores held at 1.573-1.982 of 2. **WASTE 0.000 core-min** — every arm reached its registered end and every artefact was graded. **$0.0086 DERIVED, NOT MEASURED** at $0.0513/core-h, c7a.4xlarge, `cost_basis` REPORTED-BY-OWNER (`COMPUTE_BUDGET_CHARTER.md` §5). **CARRY FORWARD:** for `DARhoSimpleFoam` at ~4,000 cells on 2 ranks price a warm perturbed primal at **0.10 core-min** and a cold primal + two adjoints + colouring at **1.4-1.8 core-min**, never from C-31's np = 1 incompressible figure scaled by 2. | `cases/dafoam/ladder-a/A1/curriculum_D15/RESULTS.md` §7; `PREREGISTRATION.md` @ `8fc2bdeb` §4/§6/§9; `/home/ubuntu/certonomous-runs/CURRICULUM-D15-a1-naca0012-subsonic/{D15_grade_20260827T114315Z.json,ledger.txt,STATUS.chain}`; `verification/queue/LAUNCH_LOG.tsv` |
| **C-155** | 2026-08-27 | heat-transfer | **T13 (natural convection in a vertical slot, conduction regime, Batchelor 1954 parallel flow, EXACT tier, Ra_L = 100) — GRADED by the frozen comparator at three levels. RUNG VERDICT `PASS`: four of four graded rows PASS.** Freeze set: all six §10 files MATCH on **both** channels, git blob AND sha256₁₆ (`exact_t13.py` `e730a915`/`bdc0d2c5faae445a`, `build_t13.py` `e1e8894b`, `analyse_t13.py` `127ae6d3`, `mark_done_t13.py` `02f43ea7`, `run_one_t13.sh` `cc88b1fe`, `T13_registered.json` `aecd9682`) — no drift, no frozen file edited, `__pycache__` cleared first so no stale bytecode could stand in for a frozen source. Completion (rule 4): `mark_done_t13.py` → **DONE on all three**, rc 0, `capped=no` on each (`capped` is an INFRASTRUCTURE field and never a completion conjunct, L-342). Operand identity **C_RA (L-331): `Ra_L` RECOMPUTED FROM EACH CASE'S OWN FILES reads 100.00000000004 against the registered 100**, inside the 1e-9 floor — the rung graded the case it actually ran. Planted-zero (rule 3): **five readers, five PASS**, each shown able to see a non-zero before its zero was believed, with an **ALL-ROW plant (80 cells) for the RMS reader G2** rather than a point plant that √80 would have attenuated. **G1 `v(ξ*)/u_ref` CONVERGING p = 2.0000, GCI 8.78e-02 %, rel deviation +7.031e-04 — landing on the DERIVED +7.03e-04 — PASS; G1b `ξ_max` CONVERGING p = 1.9975, GCI 4.01e-02 %, deviation −6.765e-05 — again the derived value — PASS.** G2 (RMS 6.26e-11 vs floor 1e-6) and G3 (`\|Nu−1\|` 1.99e-10 vs floor 2e-4) are **registered EXACT-class ABSOLUTE-FLOOR rows** (`exact_class: true`): their triples print OSCILLATORY and DIVERGENT and **no GCI is quoted on either**, and the registration states the ground pre-compute — a linear T is in the null space of the scheme's truncation error, so rule 5 (2) would return NOT A RESULT for a row that cannot be wrong by discretisation. Flagged to the verification supervisor as a rule-5 boundary case; the values sit four to six orders inside their floors. **Predictions: P1, P2 (both clauses), P2b, P3, P5, P6 HIT; P4 MISS** — it predicted witness `W1 < 1e-7` on every level from an a priori ~1e-9 bound, measured **6.738e-07 / 6.203e-07 / 6.014e-07**, about 6× the prediction and ~600× the a priori bound; the control still passes on its registered 1e-6 floor but **the margin is 1.5×, not 1 000×**, it is almost level-independent, and **the whole of it is the ±4L stations** (±1L/±2L agree to 1e-11–1e-9), so it is a genuine end effect, not discretisation error. | **139.308 core-min** — the registered POINT the ledger is told to compare against (`T13_registered.json` `cost.scratch_point_total_core_min`, from a scratch rate 1.7891e-06 core-s/cell-it MEASURED on 400 iterations of the 6 400-cell coarse case); the ceiling-rate figure is 215.892 and the cap total 572. **$0.1191 DERIVED** | **442.167 core-min MEASURED** = (91 + 1 233 + 25 206) wall s × 1 rank ÷ 60 from the three `STATUS.*` `wall_s` fields | **442.167 — gross = cleaned.** `T13_VS_f` exceeds the 3 600 wall-s stall figure at 25 206 s and **is not a stall**: rc 0, `capped=no`, C_CONV and C_PLAT met, last time == `endTime` 40 000. **WASTE 0.000 core-min** named separately (`COMPUTE_BUDGET_CHARTER.md` §6) — no re-fire, no abandoned case, **no level reached its cap** (worst `VS_f` at 84.0 % of 500; total 77.3 % of 572) | **3.174** (2.048 against the ceiling-rate figure) | **RATE-VERSUS-MESH MISPREDICTION — and the fine level BROKE THROUGH THE CEILING RATE THAT WAS SUPPOSED TO BOUND IT.** Measured core-s per cell-iteration: **c 1.4219e-06, m 2.4082e-06, f 6.1538e-06** — a **4.33× rise across a 16× cell increase**. Against the three registered rates: c is **0.795×** the scratch rate (cheaper than predicted), m **1.346×**, and **f is 3.440× the scratch rate, 2.220× the K0f point rate, and 1.048× the T4 CEILING rate. A ceiling that is exceeded is not a ceiling.** Not contention, not waste — the estimate applied a rate measured on 400 iterations of a 6 400-cell case to a 102 400-cell level. **CARRY FORWARD, and this is the THIRD independent sighting of the same direction in this lab** (C-143 and C-150 for T5's Model B c→m scaling; K0f §6, where the L3 rate ran 1.73–1.78× the basis against L1's 1.21–1.28×): **per-cell-iteration cost is NOT mesh-independent for this solver — it grows roughly as cells^0.5 over this range, so a POINT rate measured at the coarse level must be scaled with cell count before it is applied to a fine level, or it will under-predict by 2–3× at the top of a three-level ladder.** **$0.3781 DERIVED, NOT MEASURED** at $0.0513/core-h, c7a.4xlarge, `cost_basis` REPORTED-BY-OWNER — the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` §5). | `docs/campaigns/T-family/T13_RESULTS.md` §1–§9 (written by this commit); `verification/runs/T-family/T13_runs/log.analyse_t13.20260827T163830Z.txt` (comparator stdout) and `gate_t13.json` (machine record); the three `STATUS.T13_VS_*` and `DONE.T13_VS_*`; prereg `docs/campaigns/T-family/T13_PREREGISTRATION.md` §10 freeze set; beside C-143, C-150 and K0f C-137 on the shared mesh-scaling finding |
| **C-156** | 2026-08-27 | dafoam | **D16 (A1) — NACA0012 transonic M 0.685, `DARhoSimpleCFoam` (`transonicPCOption 1`), 4,032 cells, five-arm chain at 2 ranks — GRADED by the frozen comparator `d16_grade.py` (md5 `0b8338b3e483548da3515e79da28762a`, disk == its committed blob at the freeze `3ccb0c81`; eight of eight frozen paths hash-MATCH), rc 0, json byte-identical on an independent re-run. Item verdict `GATE FAIL`: SHIPPED `GATE FAIL` (CD `shape[0]` 5.151 % against a 5.0 % band; CL `shape[6]` 41.802 %), PATCHED `PASS` (CD aggregate 0.146 %, CL 0.176 %). P1-P4, P6, P6b HIT; **P5 MISS** — the registered `shape[6]` component reads 1.950 % on CD and PASSES, so the row failed on a DIFFERENT component from the one predicted.** Launched unattended 2026-08-27T11:33:35Z, pid 731626, no agent alive. | **40.3 core-min** (`PREREGISTRATION.md` §4 point; band [15, 120], ceiling 245.0); **$0.0345 DERIVED** | **15.935 core-min MEASURED** (`ledger.txt`: MESH 0.167 + X-S 2.067 + F-S 5.467 + X-P 2.067 + F-P 6.167) | 15.935 — gross = cleaned, longest arm 185 wall s | **0.395** | **MISPREDICTION OF THE TRANSONIC PRIMAL RATE; THE REGISTERED EXPOSURE DID NOT MATERIALISE.** §4 applied D7FR's cell-scaled figure DIRECTLY — 0.32 core-min per primal at 4,032 cells — pricing each F arm at 32 x 0.32 + 3.8 = 14.0; measured **5.467 and 6.167**, so a warm `DARhoSimpleCFoam` primal here is **~0.13-0.15 core-min**, about **2.3x cheaper** than linear cell-scaling from a 42,120-cell np = 4 anchor predicts. The frozen §4 exposure — *"a transonic primal on this coarse mesh may converge more slowly to `primalMinResTol 1e-8` … the F caps carry 6.4x the point for that reason"* — **did not fire**: 6.167 against a 90.0 cap. Contention present and not limiting (`d15_F-S`, `d17_X-S`, `d15_F-P`, `d17_F-S` live across these arms; delivered cores 1.971-1.989 of 2, i.e. under 1.5 %), named, not blamed. **WASTE 0.000 core-min.** **$0.0136 DERIVED, NOT MEASURED** at $0.0513/core-h, `cost_basis` REPORTED-BY-OWNER (`COMPUTE_BUDGET_CHARTER.md` §5). **CARRY FORWARD, and it is the transferable part: LINEAR CELL-SCALING FROM A LARGER, WIDER-DECOMPOSED ANCHOR OVERPRICES A SMALL 2-RANK COMPRESSIBLE CASE BY ~2.3x** — fixed per-iteration overhead amortises differently at 2,016 cells per rank. The honest transonic-over-subsonic factor at this size, measured arm for arm against the D15 row of this same date, is **1.4-1.8x**, not the 2x-plus the anchors implied. | `cases/dafoam/ladder-a/A1/curriculum_D16/RESULTS.md` §7; `PREREGISTRATION.md` @ `3ccb0c81` §4/§6/§9; `/home/ubuntu/certonomous-runs/CURRICULUM-D16-a1-naca0012-transonic/{D16_grade_20260827T114714Z.json,ledger.txt,STATUS.chain}`; beside the D15 row of this same date (the subsonic sibling on the same mesh) |
| **C-157** | 2026-08-27 | dafoam | **D17 — `Cone_Supersonic` established as a 2D PLANAR WEDGE (half-angle 11.31 deg), `DAHisaFoam` (HiSA density-based, JST flux, dual-time GMRES/LUSGS), M 1.958, INVISCID, 40,000 cells DERIVED, five-arm chain at 2 ranks — GRADED by the frozen comparator `d17_grade.py` (md5 `a940b0ee51c9102bb27dd6aa4516f366`, disk == its committed blob at the freeze `2d8796e3`; NINE of nine frozen paths hash-MATCH), rc 0, json byte-identical on an independent re-run. Item verdict `PASS`: BOTH rows `PASS` on CD (SHIPPED 5/5, aggregate 0.021 %; PATCHED 5/5, aggregate 0.018 %), CL a symmetry reading never composed. P1-P4, P6, P6b HIT; **P5 MISS** (SHIPPED `shape[0]` 2.642 %, inside the 5.0 % band, no flip). THE LAB'S FIRST `DAHisaFoam` RESULT: registered exposure (a) — "if its primal does not satisfy DAFoam's convergence guard, X-S exits non-zero and the item is `NOT A RESULT`" — DID NOT FIRE. The DERIVED 40,000-cell count was CONFIRMED by the run (G-M2 PASS). `CD_baseline` 0.26988560705696185 against the pre-registered oblique-shock estimate 0.27 (band [0.08, 0.45]) — 0.04 % of the point.** Launched unattended 2026-08-27T11:34:40Z, pid 733039, no agent alive. | **180.3 core-min** (`PREREGISTRATION.md` §4 point; band [60, 450], `cap_core_min_registered` 785.0); **$0.1542 DERIVED**. §4 also pre-billed **3.05 core-min / $0.0026 DERIVED** of image-identity probes | **172.833 core-min MEASURED** on the arms (`ledger.txt`: MESH 0.167 + X-S 7.833 + F-S 75.733 + X-P 7.100 + F-P 82.000); **175.883 core-min** including §4's pre-billed probes | 172.833 — gross = cleaned. **The two F arms exceed the 3,600 wall-s stall figure by the letter (2,272 s and 2,460 s) and are NOT stalls, cleared with evidence rather than waved:** each is a 32-primal FD sweep at ~71-77 wall s per primal, delivered cores held at **1.996-1.997 of 2** throughout, both reached their registered end with rc 0 and a complete artefact | **0.959** | **THE ESTIMATE WAS RIGHT AND THE REASON IS THE CALIBRATION CONTENT, NOT THE RATIO.** §4 priced a cold HiSA primal at **3.1 core-min** (D7FR's 3.3 at 42,120 cells x 40,000/42,120) on one explicit assumption: *"HiSA's implicit pseudo-time GMRES step is heavier than a SIMPLE iteration and there are 500 of them against ~1,000 — taken as a wash, stated as the assumption."* **THE WASH HELD**: measured, the F arms ran 32 primals in 75.733 and 82.000 core-min = **2.37 and 2.56 core-min per primal** against a modelled 2 x 3.1 + 30 x 3.1 x 0.748 (C-76's warm factor) = 75.9. The X arms came in at **0.59-0.65 of point**: the colouring exposure the 7.5x caps were sized against (D5 Addendum 3 A3.3: 9.0-27.7 core-min on a 38,304-cell 3D mesh at 4 ranks) **does not transfer to a structured 2D 5-state mesh**, where colouring is cheap. Caps were runaway guards, never targets, and were never approached (82.0 against 300.0). **The one arm above its point is F-P at 1.051, and it is not contention:** F-P ran with `live: []` in its aggregate record while F-S ran with two live siblings, and F-P still cost **8.3 % more** — that is the patched IDWarp's own warp cost, stated as the honest per-arm figure rather than absorbed as noise. **WASTE 0.000 core-min** on the arms; the 3.05 core-min of probes is disclosed in §4 and re-stated here, never hidden. **$0.1478 DERIVED (arms) / $0.1504 DERIVED (with probes), NOT MEASURED** at $0.0513/core-h, `cost_basis` REPORTED-BY-OWNER (`COMPUTE_BUDGET_CHARTER.md` §5). **CARRY FORWARD:** for `DAHisaFoam` on a structured 2D mesh at ~40,000 cells on 2 ranks price a cold primal at **2.4-2.6 core-min**, colouring at **~1-2** (NOT D5's 9-28), a cold adjoint pair at **~4-5**; and the HiSA-versus-SIMPLE wash assumption is **CONFIRMED at this size** and may be reused citing this row. | `cases/dafoam/curriculum_D17_cone_supersonic/RESULTS.md` §8; `PREREGISTRATION.md` @ `2d8796e3` §0/§4/§7/§9; `/home/ubuntu/certonomous-runs/CURRICULUM-D17-cone-supersonic/{D17_grade_20260827T130641Z.json,CHAIN_DONE,ledger.txt,STATUS.chain}`; anchors C-76, C-125, C-135, `curriculum_D7FR/RESULTS.md:36`, D5 Addendum 3 A3.3 |
| **C-158** | 2026-08-27 | dafoam | **AV-1 (A1) — np-invariance of the DAFoam adjoint (the adjoint graded against ITSELF across rank counts, never against FD), seven-arm chain MESH/X1-S/X2-S/X4-S/X1-P/X2-P/X4-P on A1's 4,032-cell NACA0012 — SEVEN OF SEVEN ARMS `rc = 0`, `OOMKilled false`, `chain=COMPLETE`, twelve physics artefacts intact — and the frozen comparator `av1_grade.py` (md5 `87f15e05130cfdb3cbf195d1daba6154`, disk == its committed blob at the freeze `0b3ebaa4`; NINE of nine frozen paths hash-MATCH) **REFUSED (exit 2)** at G1: `{"REFUSE":"G1","detail":{"age_reference_absent":"<root>/X1-S/0/U"}}`. Item `NOT A RESULT` by the registered composition ("any refusal -> NOT A RESULT"). Refusal reproduced byte-identically on an independent re-run. **All seven predictions P1-P7 are `NOT_MEASURED`** — no G-NP spread, no sign count, no divergence is quoted.** Launched unattended 2026-08-27T13:38:13Z, pid 777576, no agent alive. | **19.3 core-min** (`PREREGISTRATION.md` §4 point; band [10, 50], `cap_core_min_registered` 95.0); **$0.0165 DERIVED** | **9.102 core-min MEASURED** (`ledger.txt`, seven `ARM=` rows: MESH 0.167 + X1-S 1.017 + X2-S 1.367 + X4-S 2.067 + X1-P 1.017 + X2-P 1.400 + X4-P 2.067) | 9.102 — gross = cleaned, longest arm 61 wall s | **0.472** | **WASTE: 9.102 core-min = $0.0078 DERIVED — THE ENTIRE SPEND, NAMED SEPARATELY AND NEVER ABSORBED INTO THE RATIO (`COMPUTE_BUDGET_CHARTER.md` §6). Seven arms ran correctly and bought NO GRADED NUMBER.** Cause, established from disk and NOT guessed: the grader fixes `DATUM_REF = "0/U"` for every solver arm (`av1_grade.py:73`), and the frozen §3 gate text names `0/U` too, so the defect is in the REGISTERED DESIGN and not only the code; the NACA0012 incompressible tutorial runs `writeCompression on` (`base/system/controlDict:27`), so on a **SERIAL (np = 1)** arm the solver rewrites time-0 back compressed and `0/U` becomes `0/U.gz`, while a parallel arm writes under `processor*/` and never touches `0/U`. **The age guard's SUBSTANCE is satisfied on every refused arm** — X1-S datum 1787838035 vs `0/U.gz` 1787838086 (51 s newer), X1-P 1787838366 vs 1787838420 (54 s newer) — only the reference PATH vanished. Not contention, not the solver, not OOM, not a cap. Repair is `VERIFICATION_CHARTER.md` §2d.1's four-condition exception and is the supervisor's ruling; the frozen file is UNTOUCHED. **Gap attribution on the ratio itself — misprediction of the np = 1 ANCHOR, in the lab's favour, while the SCALING MODEL was good:** §4 assumed parallel efficiency 0.8 at np = 2 and 0.6 at np = 4 over an np = 1 point of 2.5 (C-31's 1.483 + ~0.7 for a second CL adjoint); measured np = 1 cost **1.017** (0.41 of point — the second adjoint is far cheaper than +0.7), np = 2 **1.367** and np = 4 **2.067**, i.e. realised efficiency **0.74 and 0.49** against the assumed 0.80 and 0.60. **CARRY FORWARD: price a cold primal + two adjoints on A1's 4,032-cell mesh at np = 1 at 1.0 core-min, not 2.5; keep the 0.8/0.6 np-scaling model.** $0.0078 DERIVED, NOT MEASURED at $0.0513/core-h, `cost_basis` REPORTED-BY-OWNER (`COMPUTE_BUDGET_CHARTER.md` §5). | `cases/dafoam/ladder-a/A1/curriculum_AV1/RESULTS.md` §3/§7; `PREREGISTRATION.md` @ `0b3ebaa4` §3/§4/§6; `/home/ubuntu/certonomous-runs/CURRICULUM-AV1-a1-naca0012-npinv/{AV1_grade_20260827T135029Z.json,AV1_grade_20260827T135029Z.out,ledger.txt,STATUS.chain}`; ONE CAUSE WITH the AV-2 row of this same date (AV-2) |
| **C-159** | 2026-08-27 | dafoam | **AV-2 (A1) — forward-AD vs reverse-AD duality at the total level, five-arm chain MESH/X-S/FAD-S/X-P/FAD-P, ALL AT np = 1 BY REGISTRATION — FIVE OF FIVE ARMS `rc = 0`, `OOMKilled false`, `chain=COMPLETE`, six physics artefacts intact — and the frozen comparator `av2_grade.py` (md5 `4bde0ad7dbdd3e460dcef1fe6d063979`, disk == its committed blob at the freeze `3e2cbf74`; seven of seven frozen paths hash-MATCH) **REFUSED (exit 2)** at G1: `{"REFUSE":"G1","detail":{"age_reference_absent":"<root>/X-S/0/U"}}`. Item `NOT A RESULT` — the registered composition puts "any refusal" AHEAD of "either row BLOCKED". Refusal reproduced byte-identically. **All seven predictions `NOT_MEASURED`; no `ε_k`, no G-DP reading is quoted.** THE FORWARD-AD ARMS FIRED THE REGISTERED `BLOCKED` BRANCH VIA ITS THIRD CONDITION ("forward mode raises on this case"), on BOTH toolchain images: `blocked_any true`, **`control_fail FALSE`**, 5/5 rows `status BLOCKED`, every row `AnalysisError("'scenario1.coupling.solver' <class DAFoamSolver>: Error calling solve_nonlinear(), Primal solution failed!")`, `n_add_dvgeo` 1 per row (the seed WAS placed), `AV2_FAD_BLOCKED` and `AV2_PLANTED_CONTROL_SEEN` in both logs. **The registered silent-no-op hazard did NOT materialise** — none of the three echo refusals (exact 0.0 / exact 1.0 / function echo within 1e-6) fired, because no forward value was produced at all. Seeding forward mode makes the PRIMAL fail.** Launched unattended 2026-08-26T23:19:58Z, pid 622367, no agent alive. | **15.3 core-min** (`PREREGISTRATION.md` §4 point; band [8, 40], `cap_core_min_registered` 75.0); **$0.0131 DERIVED** | **19.085 core-min MEASURED** (`ledger.txt`: MESH 0.167 + X-S 1.017 + FAD-S 8.267 + X-P 1.017 + FAD-P 8.617) | 19.085 — gross = cleaned, longest arm 517 wall s | **1.247** | **WASTE: 19.085 core-min = $0.0163 DERIVED — THE ENTIRE SPEND, NAMED SEPARATELY AND NEVER ABSORBED INTO THE RATIO (`COMPUTE_BUDGET_CHARTER.md` §6). Five arms ran correctly and bought NO GRADED VERDICT.** Split honestly rather than netted: **16.884 core-min (the two FAD arms) DID buy the instrument-level `BLOCKED` reading above**, which is on the record and reusable; **2.201 core-min (MESH + the two X arms) bought nothing at all.** Cause of the refusal, identical to the AV-1 row of this same date and established from disk: `DATUM_REF = "0/U"` (`av2_grade.py:63`) against a tutorial running `writeCompression on`, so on a serial arm `0/U` becomes `0/U.gz`. **Because AV-2 is np = 1 on EVERY arm by registration, this item COULD NOT HAVE REACHED A VERDICT ON ANY ARM — the refusal was structurally certain before the first container started.** The guard's substance was satisfied everywhere (X-S datum 1787786539 vs 1787786594; FAD-P 1787787353 vs 1787787770, 417 s newer); only the path vanished. Frozen file UNTOUCHED; repair is §2d.1 and the supervisor's ruling. **Gap attribution on the ratio — the ONE overrunning pair is the forward-AD pair, and the overrun IS the finding paying for itself:** §4 priced a FAD arm as one ordinary primal + five forward-mode primals "adopted at 3x a plain cold primal" = 0.3 + 5 x 0.9 = 5.0; measured **8.267 and 8.617**, **1.653x and 1.723x the point**, because each of the five forward-seeded components ran a **FULL primal attempt before failing** at `wall_s` 95.9-99.9 each (479.4 s summed on FAD-S, 496.1 s on FAD-P). **A forward-mode (CoDiPack tangent) primal on this mesh is ~1.6 core-min = ~5.3x a plain cold primal, NOT 3x, AND IT IS PAID IN FULL EVEN WHEN THE PRIMAL THEN FAILS — a `checkPrimalFailure` exit is not a cheap exit.** Contention nil (`live: []` on every arm). The X arms independently confirm the AV-1 row of this same date: a cold primal + two adjoints at np = 1 on this mesh is **1.017 core-min**, not the 2.5 that C-31-plus-a-CL-adjoint predicted. $0.0163 DERIVED, NOT MEASURED at $0.0513/core-h, `cost_basis` REPORTED-BY-OWNER (`COMPUTE_BUDGET_CHARTER.md` §5). | `cases/dafoam/ladder-a/A1/curriculum_AV2/RESULTS.md` §2/§3/§7; `PREREGISTRATION.md` @ `3e2cbf74` §2/§3/§4/§6; `/home/ubuntu/certonomous-runs/CURRICULUM-AV2-a1-naca0012-duality/{AV2_grade_20260826T234431Z.json,AV2_grade_20260826T234431Z.out,ledger.txt,FAD-S/av2_FAD.json,FAD-P/av2_FAD.json}`; ONE CAUSE WITH the AV-1 row of this same date (AV-1) |
| **C-160** | 2026-08-27 | heat-transfer | **T14 (2-D transient conduction in a square, T11b, Robin faces, EXACT tier, `laplacianFoam`) — GRADED by the frozen comparator at three levels plus a temporal-bias control. RUNG VERDICT `PASS`: three of three graded rows PASS, all four registered predictions HIT.** Freeze set: all six §9 files MATCH on **both** channels, git blob AND sha256₁₆ (`exact_t14.py` `4f72de8d`, `build_t14.py` `677363da`, `analyse_t14.py` `386181db`, `mark_done_t14.py` `680eddd3`, `run_one_t14.sh` `8b2b7b52`, `T14_registered.json` `149bbf0b`); no drift, no frozen file edited, `__pycache__` cleared first. Completion (rule 4): `DONE` on all four (`SQ_c`, `SQ_m`, `SQ_f`, `SQ_f_CT`), rc 0, `capped=no` on each. **Referent verified by Route B AND cross-checked against a DIFFERENT rung's registered values** — T11 at Bi = 1, Fo = 0.2, PASS (PDE residual 7.60e-06, symmetry gradients 0.0e+00, Robin 9.5e-08, IC 3.6e-05, Simpson to 3.7e-12); a referent checked only against itself is checked against nothing. Gate (1): every level "reached endTime, every step converged"; **`W0` transposition witness 3.01e-14 / 3.01e-14 / 6.01e-14 against a 1e-9 floor**; `C_VF` (L-341) `valueFraction` read from `CASE.txt` matched `Bi/(Bi+2N)`. Planted-zero (rule 3, L-340 sizing): three readers, three PASS — **ALL-CELL plant (40 000 cells) for the averaging reader G1, recovered EXACTLY (1.234e-03 for 1.234e-03)**, one-cell plants for the point readers G2/G3 recovering 2.7765e-03. **G1 `theta_mean` CONVERGING p = 1.9999 GCI 2.28e-04 %, rel dev +6.13e-06 in a ±5e-5 band; G2 `theta(0,0)` CONVERGING p = 2.0000 GCI 1.11e-03 %, rel dev +7.18e-06 in ±1e-4; G3 `theta(1,0)` CONVERGING p = 2.0066 GCI 1.92e-03 %, rel dev +3.03e-05 in ±1e-4.** All three monotone, all three graded under rule 5 (3), no NOT-A-RESULT path needed. **C-T temporal-bias control (`SQ_f_CT`, `deltaT` halved): moves G1 by −1.310e-06 relative = 0.026 of the band — REPORTED, NEVER GATED, and never subtracted from a graded value.** Predictions **P1, P2, P3, P4 all HIT** (bands cleared by 8.2×/13.9×/3.3×; p 1.9999–2.0066; C-T at 2.6 % of the allowed 10 %; W0 four orders inside). | **7.244 core-min** — the registered POINT the ledger is told to compare against (`T14_registered.json` `cost.scratch_point_total_core_min`, from a scratch rate 1.64e-07 core-s/cell-step MEASURED on 2 000 steps of a 2 500-cell scratch copy); the T11-rate figure is 23.210 and the cap total 100. **$0.0062 DERIVED** | **6.884 core-min MEASURED** = (7 + 30 + 141 + 235) wall s × 1 rank ÷ 60 from the four `STATUS.*` `wall_s` fields | **6.884 — gross = cleaned.** No row is anywhere near the 3 600 wall-s stall figure (longest 235 s). **WASTE 0.000 core-min** named separately (`COMPUTE_BUDGET_CHARTER.md` §6) — no re-fire, no abandoned case. **No case approached its cap** (c 5.9 %, m 6.3 %, f 7.8 %, f_CT 6.5 %, total 6.9 % of 100); the registration is explicit that these caps are **hang guards, not forecasts**, so the 6.9 % is not scored as a miss | **0.950** | **A GOOD ESTIMATE, AND RECORDING THAT IS AS MUCH THIS LEDGER'S JOB AS RECORDING A MISS.** Per case 0.854 / 0.914 / 1.075 / 0.896. Measured core-s per cell-step **c 1.400e-07, m 1.500e-07, f 1.7625e-07, f_CT 1.469e-07** against the registered 1.64e-07 — **the rate is very nearly mesh-independent, rising only 1.26× across a 16× increase in cells.** **AND THAT IS THE FINDING, BECAUSE IT DIRECTLY CONTRADICTS T13, GRADED THE SAME DAY BY THE SAME LANE WITH THE SAME METHODOLOGY (row C-155).** T13 (`buoyantBoussinesqSimpleFoam`, coupled SIMPLE) saw its rate rise **4.33×** across the same 16× cell increase, missed its POINT by **3.17×**, and its fine level broke through the T4 ceiling rate; T14 (`laplacianFoam`, linear transient conduction) rises **1.26×** and lands at **0.950**. **THE MESH-SCALING PENALTY IS A PROPERTY OF THE SOLVER, NOT OF THE ESTIMATING METHOD:** a coupled pressure–velocity–temperature SIMPLE loop degrades with mesh size because its inner iteration count and matrix conditioning do; a single linear Laplacian solve at a fixed step count does not. **CARRY FORWARD: the C-155 correction — scale a coarse-level POINT rate with cell count — MUST BE APPLIED PER SOLVER CLASS AND NOT LAB-WIDE, or it will over-predict linear transient rungs by 2–3× in the opposite direction.** **$0.0059 DERIVED, NOT MEASURED** at $0.0513/core-h, c7a.4xlarge, `cost_basis` REPORTED-BY-OWNER — the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` §5). | `docs/campaigns/T-family/T14_RESULTS.md` §1–§10 (written by this commit); `verification/runs/T-family/T14_runs/log.analyse_t14.20260827T164421Z.txt` (comparator stdout) and `verification/runs/T-family/T14_runs/gate_t14.json`; the four `STATUS.T14_*` and `DONE.T14_*`; prereg `docs/campaigns/T-family/T14_PREREGISTRATION.md` §9 freeze set; **read beside C-155 (T13) — the two rows are a matched pair on solver-dependent mesh scaling** |
| C-161 | 2026-08-27 | cfd | **F24_PRANDTL_MEYER — PARTIAL LADDER, both gates `PENDING`. THE FIRST cfd row where the PROJECTOR, NOT THE PHYSICS, ENDED THE RUN.** `rhoCentralFoam` supersonic expansion corner, 4 ranks. Coarse and medium COMPLETE and rule-4 clean (rc 0, `End`, 15,000/30,000 `Time` lines == `ExecutionTime` lines, 4 processor dirs, age guard NEWER). The fine level was **never launched**: `launcher_rc=3` is the launcher's own pre-spend cap guard — `HALT BEFORE SPENDING … The cap is NOT raised. Levels not launched stay PENDING`. The FROZEN grader returned **PENDING**, not NOT A RESULT, because it refuses at the completion check before rule 5 is reached — no triple was formed, no order computed, no band consulted. L-342 fired in the refusing direction on a live partial ladder: four BOOKKEEPING DEFECT lines against the fine level, `cost_claim.core_min_claim` **null**, physics verdicts explicitly unaffected. | **737.1** core-min registered for the whole rung (basis: F15's `rhoCentralFoam` carried forward at a FLAT 0.72 / 0.90 µs per cell-step); the two levels that ran were registered at **89.1**; CAP **1450.0**; $0.63 derived | **179.000** core-min MEASURED from the two completed `log.rhoCentralFoam` ClockTimes × 4 ranks ÷ 60 (425 + 2,260 s) = **12.3 % of cap**; **$0.153 DERIVED, NOT MEASURED** at $0.0513/core-h. The grader REFUSED the rung-level cost claim (correctly — the rung has not finished); `partial_sum_core_min` 179.0 | **179.000** — gross == cleaned; no stall row; nothing repeated | **2.009** against the 89.1 registered for the two levels that ran. **Against the whole-rung 737.1 the figure is 0.243, and that means only that the rung is a third finished — it is NOT a favourable calibration reading and must not be read as one.** | **Misprediction, and NOT in the same direction at both levels — the model SHAPE is wrong, not just its constant.** Measured core-µs/cell-step **2.519 at coarse (3.50× the registered 0.72) and 1.674 at medium (1.86× the registered 0.90)**: the rate got FASTER per cell-step as the problem grew 8×, because fixed per-run overheads (mesh, decomposition, MPI startup, I/O) amortise. **A flat µs/cell-step is the wrong model shape for a 4-rank explicit solver** — too optimistic at small sizes, converging toward right at large ones. **Contention: NOT separable and NOT claimed** — both levels ran with the launcher's own probe reading `free cores 0.5` (saturated), but no per-level free-core reading sits beside either ClockTime. **WASTE: NONE, named as zero rather than omitted (charter §6).** The rung produced no verdict, but the spend was not wasted — it was STOPPED, and it bought two complete rule-4-clean levels whose physics is intact on disk. That distinction is the whole difference between this row and C-152 (F21), where 889 core-min bought nothing. **WHY THIS ROW IS UNUSUAL AND WORTH KEEPING:** the run ended on a PRE-SPEND PROJECTION, not on physics and not on measured spend. The cfd supervisor's own triage (check 3, personally, 2026-08-27) attributes the halt to `run_f24.sh:200`, whose contention multiplier `ranks / max(free, 0.5)` pins at **8.0×** for a 4-rank entry on a saturated box; F24's own two levels bound the real effect at **1.86×**, so the formula overstated it about **4.3×**. **The halt is NOT recorded here as a false positive:** from F24's own measured rates the fine level lands at **1,205–1,440 core-min, cumulative 1,384–1,619 against the 1,450 cap = 95.5 %–111.7 %**, so it genuinely sits on the cap's edge. What is defective is the REASONING, not necessarily the decision. Fine level stays **PENDING and unbought**; F24's gates are CLOSED (rule 2, 179 core-min spent) and no cap, threshold, band or label is touched. | `verification/campaign/F24_PRANDTL_MEYER_RESULTS.md` §5–§6; `verification/runs/F24_PRANDTL_MEYER_runs/F24_GRADED.json` (`cost_claim.core_min_claim` null, 4 defects); prereg frozen `260ff3c13e6877183a508c34ecd8e12886ed9a45` |
| **C-162** | 2026-08-27 | heat-transfer | **T9a-R1b (`W1b`) — layered 1-D conduction, HARMONIC interface scheme, interface temperature `T_i1`, three levels (35 / 56 / 90 cells). GRADED by the frozen comparator. RUNG VERDICT `PASS`: ONE of ONE GRADED row PASS.** **Census is over GRADED rows only (D534)** — `R0` (`q_hot`) is a **`REPORTED` row class, not a verdict**, named and EXCLUDED from the tally, not absorbed into it. Freeze set: all five §7 files MATCH on **both** channels (`build_t9aR1b.py` `ef589866`, `analyse_t9aR1b.py` `fd6c43a0`, `mark_done_t9aR1b.py` `68c78731`, `run_one_t9aR1b.sh` `d7990722`, `T9aR1b_registered.json` `cfe867c6`); no drift, no frozen file edited. Completion: `DONE` on all three, rc 0 — and **`wall_s=0` on every arm is real, not a broken record**: `ExecutionTime` reads 0.05/0.05/0.06 s, below the launcher's one-second resolution, while each log carries **1 000 `ExecutionTime` lines against `endTime` 1000**, an `End` line and a last written time of 1000. **LATENT INSTRUMENT DEFECT REPORTED, NOT REPAIRED: `mark_done_t9aR1b.py` is derived from T14's and inherits its L-342 class declaration verbatim (`:14-15`), putting the `ExecutionTime` count on the PHYSICS-CRITICAL side — the `CONFLATED — MISCLASSIFIED` pattern of `docs/L342_GRADER_AUDIT.md`. It DID NOT FIRE (count 1 000 == expected 1 000 on every arm, so the clause passed on its merits) and no verdict here rests on the classification.** Not repaired: the rung has fired, the file is frozen, §2d.1 is not this lane's route; and the tension is genuine — `CLAUDE.md` rule 4 itself lists the `ExecutionTime` count among the strict completion conjuncts while the audit calls it infrastructure. **Referred to the verification supervisor.** Controls: `C_CONV` checkpoint form (900 vs 1000) **move 0.000e+00 K on every level**; `C_MAP`, `C_SCHEME` (requires the HARMONIC line on disk and refuses the parent's linear line) and `C_REF` all passed; referent `q = 19.502682 W/m²`, `T_i1 = 348.781082399 K` **cross-checked against T9a's registered values**; **planted-zero (rule 3) PASS — 1.234e-03 K planted into cell 25, THE CELL ADJACENT TO INTERFACE 1 AND THE POINT READER'S OWN STATION, recovered 1.17632e-03 K**, floor 1e-07. **R1: c = m = f = 348.781082399 K, deviation from exact 2.899e-12 K against an absolute floor of 1.000e-06 K — TRIPLE `EXACT` — PASS.** **Standing rule 5 (2) names `EXACT` among the states that make a row `NOT A RESULT`; this row is graded `PASS` under a rule registered BEFORE compute whose ground is a TIGHTENING, not a loosening**: the prereg §2 grades an EXACT triple by the absolute floor 1e-06 K, ~1 000× the measured round-off and **900× TIGHTER than T9a's R1 band of 0.92 mK**, and states "No band is widened"; any other non-CONVERGING state still routes to `NOT A RESULT`. The triple is exact because with the harmonic interface conductivity the discrete interface temperature in layered 1-D conduction is **mesh-independent by construction**. **FLAGGED to verification as the SECOND such row this lane graded today** (T13's `G2`/`G3` `exact_class` rows share the structural pattern) — the lane reports the frozen instrument's verdict and does not override it. `R0` REPORTED: `q_hot` rel dev c +5.920e-14, m +3.417e-12, f +3.044e-12 — **the parent T9a's 1.8 % linear-scheme excess has vanished.** Predictions **P1, P2, P3 HIT**; **P4 NOT TRIGGERED and not scored either way** (it was the conditional "if instead the triple is CONVERGING…" — a conditional whose antecedent is false is not a hit). | **0.0045 core-min** (`T9aR1b_registered.json` `cost.point_total_core_min`, 0.0015 per level, from parent T9a's MEASURED wall 0.09 s per level); cap total 3. **$0.0000038 DERIVED** | **0.000 core-min ON THE REGISTERED BASIS** — `core_min = wall_s × ranks ÷ 60` and all three `wall_s` are 0. The only usable actual is the solver's own `ExecutionTime`, **0.05 + 0.05 + 0.06 = 0.16 s → 0.002667 core-min**, stated as a **DIFFERENT BASIS** and a lower bound (it excludes start-up and I/O that wall time includes) | **0.000 registered-basis / 0.002667 `ExecutionTime`-basis — gross = cleaned.** Nothing near the 3 600 wall-s stall figure. **WASTE 0.000 core-min** named separately; no arm approached its cap (3 core-min registered total) | **UNDEFINED on the registered basis — NO RATIO IS STATED, and that refusal is the row's content.** (0.593 on the `ExecutionTime` basis, recorded as a different basis and not as the registered one) | **THE LAB'S MEASURED UNIT HAS A FLOOR, AND THIS ROW IS WHERE IT WAS HIT.** 0.000/0.0045 = 0.000 is an artefact of measurement granularity, not a calibration result: reporting it as a ratio of 0.000 would enter a false perfect-underspend into this ledger, so **no ratio is entered**. Below roughly one wall-second per case the registered core-minute basis cannot resolve the spend and returns zero, and **a zero here means "too small to measure", NOT "free"**. **CARRY FORWARD: a rung whose per-case runtime is sub-second must either register `ExecutionTime` as its cost basis up front, or accept that its calibration row carries no ratio — it cannot have both a wall-clock basis and a meaningful ratio.** Attribution of the gap is therefore not available and is not guessed. **$0.0000023 DERIVED, NOT MEASURED** at $0.0513/core-h on the `ExecutionTime` basis, c7a.4xlarge, `cost_basis` REPORTED-BY-OWNER — effectively zero, recorded for completeness and not as a budget statement. | `docs/campaigns/T-family/T9aR1b_RESULTS.md` §1–§9 (written by this commit); `verification/runs/T-family/T9aR1b_runs/log.analyse_t9aR1b.20260827T164837Z.txt` and `verification/runs/T-family/T9aR1b_runs/gate_t9aR1b.json`; the three `STATUS.W1b_*` and `DONE.W1b_*`; prereg `docs/campaigns/T-family/T9aR1b_PREREGISTRATION.md` §2 (the EXACT-triple floor) and §7 (freeze set); `docs/L342_GRADER_AUDIT.md` (the CONFLATED pattern inherited from T14's checker) |
| C-163 | 2026-08-27 | dafoam | **D5 arm ACC48 (r3) — BOUGHT, and the Addendum-3 cap repair VALIDATED BY MEASUREMENT.** `compute_totals` on the 48-DV FFD density, `dafoam-idwarp-rot:v1` PATCHED row, np=4 on the registered cpuset 8,10,11,13, 12g. This is the re-fire of the arm `D5-PREREG-DEF-1` killed at rc=124 under a 10.0 core-min cap while still colouring the Jacobian. | **40.0** core-min, band **[35.0, 60.0]**, cap **60.0**, in-container deadline 900 s — all re-registered by D5 ADDENDUM 3 (`b5b428bc`) **BEFORE the re-fire, from the measured colouring anchor**, with no answer in hand; $0.034 derived | **35.8** core-min MEASURED from the launcher's own ledger row (`wall_s=537` x 4 ranks / 60), rc=0, `inspect [0 false]`, `delivered_cores_mean 3.9889` of 4, `max_nr_throttled 2568`, MemAvailable 27.48 -> 27.30 GiB; **$0.031 DERIVED, NOT MEASURED** at $0.0513/core-h, reported-by-owner | **35.8** — gross == cleaned; 537 wall s is far under the 3,600-s stall rule | **0.895** | **Misprediction, and a favourable one that is INSIDE THE REGISTERED BAND at its lower edge — a pre-registered prediction HIT on a repair.** The band's lower bound of 35.0 was itself set from the D4-SHIPPED partial run killed before finishing, so landing just above it is the band behaving exactly as its own stated derivation said. **Contention: measured and effectively absent** — `delivered_cores_mean` 3.9889 of a 4-core quota on a dedicated cpuset, so the 10.5 percent shortfall against prediction is NOT contention and is not claimed as such; it is the adjoint-and-products term after colouring coming in under the 100-200 s the addendum allowed. **The calibration lesson: the Addendum-3 method — re-price a rung from the measured phase that actually ran (135.64 s of local colouring at 4 ranks), not from a differently-shaped sibling — produced a band the re-fire landed inside on the first attempt.** That is the method to repeat. **WASTE: NONE on this row**; the 10.8 core-min of the killed r2 fire belongs to `D5-PREREG-DEF-1` and is not absorbed here (charter section 6). | `cases/dafoam/ladder-a/A2/curriculum_D5/PREREGISTRATION.md` sections A4.2 and A4.8 @ `5d02ee0b`; ledger row `ARM=ACC48 … rc=0 … core_min=35.8` and `STATUS.ACC48` in `/home/ubuntu/certonomous-runs/CURRICULUM-D5-a2-wing-ffd-density`; prereg frozen `c5c25189`, Addendum 3 `b5b428bc` |
| C-164 | 2026-08-27 | dafoam | **D5 arm F48 (r3) — WASTE, nothing bought. `D5-LAUNCHER-DEF-1`.** The endpoint FD arm exited **rc=127 in 11 wall-seconds**: the container's working directory was spelled `$ARM` where the launcher's own `$WORK` — and `PREREGISTRATION.md` section 2 — put the F arm in `O48/`. Docker created `/mnt/F48` empty and root-owned through the `-v $BASE:/mnt` bind, and `bash /mnt/F48/d5_cmd.sh` found nothing; the command file was on disk in `O48/` all along, with the md5 the launcher had just printed. | **47.267** core-min (D5 section 4, from D4's F3 anchor C-96), cap **120.0**, deadline 1,800 s | **0.733** core-min MEASURED (`wall_s=11` x 4 / 60), rc=127, `inspect [127 false]`, no OOM, `delivered_cores_mean [NOT_MEASURED]` (the sampler's 15-s poll never took a second sample in 11 s — an INFRASTRUCTURE field under L-342, reported and NOT treated as a placement failure); **$0.0006 DERIVED, NOT MEASURED** | **0.733** — gross == cleaned; no stall row | **NO RATIO IS QUOTED.** A ratio of 0.0155 would read as a spectacular underspend and would be a lie: nothing was bought. Per charter section 6 this is named as waste and kept out of every ratio column. | **WASTE: 0.733 core-min, the whole row, and it is an INSTRUMENT defect — not contention, not a stall (11 s against the 3,600-s rule), not physics, and not the toolchain** (`inspect [127 false]`, no OOM, no host event, and the idwarp `.so` md5 was read and matched INSIDE the container before the failure). **The estimate was not wrong** — 47.267 core-min remains the price of an F arm and is carried unchanged into the r4 re-file. What this row calibrates is not the number but the MECHANISM: **a launcher that spells a path two ways will spend real core-minutes discovering it, and the only cheap defence is a guard that refuses a container working directory nothing on the host created.** That guard is now in the launcher and is driven by a planted control. **The sibling was ruled out on evidence, not assumed innocent**: `av1_X2-P_20260827T134811Z_788560` was live across all 11 seconds and there was no deletion for it to have performed. | `cases/dafoam/ladder-a/A2/curriculum_D5/PREREGISTRATION.md` sections A4.3 and A4.8 @ `5d02ee0b`; ledger row `ARM=F48 … rc=127 … core_min=0.733`; container log `F48_20260827T134814Z_789173.log`; controls `d5_addendum4_control_evidence.txt` pass=12 fail=0 |
| **C-165** | 2026-08-27 | heat-transfer | **T4b (impinging jet on a heated plate, near-wall family with the first cell HALVED from T4, `buoyantBoussinesqSimpleFoam` kOmegaSST, three levels 6 048 / 24 192 / 96 768 cells) — GRADED by the frozen comparator. RUNG VERDICT `NOT A RESULT`: three of three graded rows `NOT A RESULT`, ALL AT GATE (1). NO GRADED VALUE EXISTS FOR THIS RUNG.** **The rung did NOT fail on grid convergence** — all three triples came out `CONVERGING` (G1 p 1.414, G2 p 0.672, G3 p 1.008), exactly as prediction P8 registered, and **P8 was registered precisely "so a gate-(2) NOT A RESULT is a scored miss"; the refusal came from gate (1), not gate (2)**, so the mesh family is behaving and the RUN SCHEDULE is not. Standing rule 5 order (1) applied as written: a level not iteratively converged makes the row NOT A RESULT before the triple is consulted, and **no GCI is quoted on any row**. Freeze set: five of five MATCH on both channels, **with `launch_t4b.sh` checked against the AMENDED value `8bd93c23`/`9c035b147bd1b4ef` and NOT §11's struck `1f12792d`** — checking the struck value would have manufactured a false drift; the amendment transcribes its replacement sha properly, in contrast with K0f AMENDMENT 2 (see C-151). Completion: `DONE` on all three, rc 0, `capped=no`. **The earlier refusal log (`…refused.20260826T205552Z.txt`, "no DONE.T4b_IJ_f -- the whole rung is graded or none of it is") was CORRECT and is now STALE, not a defect**: `IJ_f` did not finish until 04:23:57Z on 2026-08-27, seven and a half hours later; it was a PENDING correctly expressed as a refusal to grade a partial rung, and it is retained rather than deleted. **Four planted controls PASS (rule 3)** — G reader recovered 3.20409e-05 from a 1.234e-03 plant; exit-line reader 4.60364e-05; flux reader inlet `phi` ×1.01 → imbalance 0.00990094775 against a PREDICTED 0.00990094775; and **the y+ reader runs a BLIND PATH returning 0 on every patch AND confirms `U` ×4 → y+ ×2, so a zero from it is evidence because the reader was shown able to produce a non-zero and to scale correctly.** **Gate-(1) failures, five across three levels:** level c `C2` U_c/U_bulk **1.1746** outside ±3 % of 1.2245 (pipe flow not fully developed) and `C6.1` `p_rgh` plateau **2.121e-06** > 1e-06; level m `C6.2` growth ratio **1.1366** > 1.05 and `C6.3` field change **2.848e-04** > 2e-04; level f `C6.3` **8.170e-04** > 2e-04. **THE FINDING: the registered endTimes are too short and get MORE too short as the mesh refines** — C6.3 reads c 1.186e-09, m 2.848e-04, f 8.170e-04, growing by nearly three orders of magnitude, while the endTime schedule rises only 1.5× then 1.33× against a 4× cell increase per step. Fine values recorded as INFORMATION AND NOT VERDICTS (a verdict on these rows does not exist): G1 1.0847 in band [1.0690,1.1090], G2 0.8120 0.0032 above its upper edge, G3 0.5371 0.0539 above. Predictions: **P1, P1b, P3, P8 HIT; P5/P6/P7 fine VALUES all HIT** (1.0847, 0.8120, 0.5371 inside their intervals, G3 essentially on its 0.538 point); **P2 MISS on level c only** (1.1746, the same fact as the C2 gate failure, not double-counted); **P4 MISS** — `p_rgh` plateau at c measured 2.121e-06 against a predicted [2e-07, 1e-06], and **the registration had already disclosed this as the thin one ("the C6.1 margin on the coarse level is 1.43x and is disclosed"), so a disclosed thin margin that then fails is the disclosure working, not a surprise**; P5's `verdict_predicted: GATE FAIL` is a MISS (the row is NOT A RESULT and on band position would have been PASS). | **515.90 core-min POINT** (c 11.84 / m 74.65 / f 429.41, from C-119's per-level MEASURED T4 rates); **CEILING 773.85 = 1.5 × POINT**; cap total 1035 (25 / 150 / 860). **$0.4411 POINT / $0.6616 CEILING DERIVED** | **729.667 core-min MEASURED** = (846 + 5 178 + 37 756) wall s × 1 rank ÷ 60 from the three `STATUS.*` | **729.667 — gross = cleaned.** `IJ_m` and `IJ_f` exceed the 3 600 wall-s stall figure and NEITHER is a stall (rc 0, `capped=no`, last time == `endTime` on both). **WASTE 0.000 core-min** named separately — nothing abandoned, nothing re-run, **no level reached its cap** (56.4 % / 57.5 % / 73.2 %, total 70.5 % of 1035) | **1.4144 against POINT; 0.943 against CEILING — INSIDE the ceiling** | **THE CEILING RULE WAS RIGHT WHERE THE POINT WAS NOT, AND THAT IS THE ROW'S LESSON.** Measured core-s per cell-iteration **c 6.9940e-06, m 7.1346e-06, f 9.7543e-06** against the registered per-level rates 5.873843e-06 / 6.171232e-06 / 6.656298e-06 — **1.191× / 1.156× / 1.465×**, a 1.395× rise across a 16× cell increase. **The registered CEILING named the MECHANISM in advance** — "the halved first cell (aspect ratio 1627–1645) and a box at 12–13 of 16 cores busy can raise the `p_rgh` GAMG iteration count" — and set 1.5 × POINT; **the realised 1.414 landed inside it. A ceiling that states a mechanism beats a point estimate that states only a number.** Consistent with C-155 (T13) and C-137 (K0f), all three `buoyantBoussinesqSimpleFoam`: the per-cell-iteration rate rises with mesh size for this coupled solver. **It is much milder here — 1.395× against T13's 4.33× — BECAUSE T4b's POINT was built from PER-LEVEL measured T4 rates rather than one coarse-level rate, which is exactly the correction C-155 recommends, applied in advance; it cut the miss from 3.17× to 1.41×.** **NAMED SEPARATELY AND NOT LAUNDERED INTO THE RATIO: 729.667 core-min BOUGHT NO GRADED VALUE.** Not waste in the charter §6 sense — every case ran its registered program to its registered `endTime` and the controls did their job — but letting a 1.41 ratio stand as the whole story would hide that the rung produced nothing gradeable (the C-149 precedent). **$0.6239 DERIVED, NOT MEASURED** at $0.0513/core-h, `cost_basis` REPORTED-BY-OWNER — the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` §5). **The recoverable next step is a NEW REGISTRATION with longer endTimes on m and f sized so C6.3 clears 2e-04, plus a longer inlet pipe or more radial cells for C2 at c — not a bigger budget for this one, and not this lane's call.** | `docs/campaigns/T-family/T4b_RESULTS.md` §1–§8 (written by this commit); `verification/runs/T-family/T4b_runs/log.analyse_t4b.20260827T165337Z.txt` and `verification/runs/T-family/T4b_runs/gate_t4b.json`; the retained `log.analyse_t4b.refused.20260826T205552Z.txt`; the three `STATUS.T4b_IJ_*` and `DONE.T4b_IJ_*`; prereg §11 freeze set + the launcher amendment; beside C-119 (the T4 rates this POINT was built from), C-155 (T13) and C-137 (K0f) |
| ~~C-165~~ STRUCK — SUPERSEDED BY C-169 | 2026-08-27 | ansys-verification | **VMFLGPU001 — GPU solver path, Taylor–Couette Couette (manual p.225), `NOT A RESULT` (frozen plateau clause I5 refused on a perfectly converged L1 channel).** First VMFLGPU (GPU-path) completion; verdict landed today after the Amendment-4 withholding was ruled by the supervisor (frozen clause STANDS). Cost is a GPU-hour figure (GPU run), CPU-arm core-minutes stated as work on the same billed instance, not a separate charge. | **0.40 GPU-h** registered (prereg §12; cap 2.0 GPU-h, CPU-arm cap 40 core-min) | **0.441667 GPU-h** measured = 1590 wall-s the instance was up (`COST.txt`, `total_gpu_h`); CPU arm 9.15 core-min | = gross (single run to endTime at all six solves; no stall, no re-run — a comparator refusal costs no solver time) | **ratio 1.10** (cleaned/predicted) | **misprediction, modest and in the honest direction** (0.4417 vs 0.40 predicted, +10 %); the estimate was a pre-compute wall-hour guess for a first GPU case; **no contention noted, WASTE 0.000**. **$0.3555 derived** at the published-list $0.8048/GPU-h (g6.xlarge us-east-2), **NOT measured** — the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` §5); **console figure still owed**. | `cases/ansys_verification/VMFLGPU001/RESULTS.md`; register row #33; prereg frozen at `0429091e`, comparator re-grade blob `21fa2387` |
| C-166 | 2026-08-27 | cfd | **F18b_TG2D_EXT — rung GRADED, `NOT A RESULT` × 2 on a triple that was otherwise a double PASS.** 2-D Taylor–Green ladder extension 256²/512²/1024², `icoFoam`, serial, h and Δt refined together. **Both triples CONVERGING and monotone, observed orders 1.9245 and 1.9244 against the model's 1.9955, and BOTH fine values INSIDE their pre-registered bands (band verdicts PASS, PASS).** Rule 5 limb (1) fires first and the gate can only turn a PASS INTO NOT A RESULT, never the reverse. Cause: **ONE p solve out of 3,200 above tolerance — the FIRST time step of the fine level** (`log.icoFoam:50`, `Time = 0.00125`, final residual **1.334e−07** against 1e−9, **No Iterations 1000**). It did not diverge; **it hit OpenFOAM's default `maxIter` of 1000.** Across that level the p solve averages **992.97** iterations (commonest counts 993/994/992) — the whole 1,600-step level ran within seven iterations of the ceiling, and the step with the worst initial guess went over. Rule 4 complete at all three levels; no plateau gate registered (transient value at fixed t = T) and recorded ABSENT; `cost_claim.defects` empty. | **889.0** core-min registered (basis: F18's MEASURED 9.39 µs/cell-step at 256², grown +75 %/+80 % per doubling; the file's own asymptotic bound 1,121); CAP **1500.0**; $0.76 derived | **1,392.767** core-min from the three `log.icoFoam` ClockTimes × 1 rank ÷ 60 (246 + 4,668 + 78,652 s = 83,566 s) = **92.85 % of cap — THE CAP WAS NOT CROSSED** (the pre-grading projection of ~1,407 was over by ~1 %; the measurement came in 107.2 core-min under, and since fine is the last level a crossing would only have been findable at completion). **$1.191 DERIVED, NOT MEASURED** at $0.0513/core-h | **1,392.767** — gross == cleaned; no stall row, no level repeated | **1.566** (cleaned/predicted) | **Misprediction in the GROWTH EXPONENT with a base rate that was EXACTLY right — this row has no room left to blame the base.** F18b's coarse level IS F18's fine level on the same grid and Δt and reproduced its registered 9.39 µs/cell-step at **ratio 1.000**. Measured growth **9.39 → 22.26 → 46.88 µs/cell-step = +137 % then +111 % per doubling** against a registered +75 %/+80 %, exceeding the file's own +100 % asymptotic bound at the first step and falling back toward it at the second — **because the p solve had run out of headroom and was capping at `maxIter`, so the rate could not keep growing. The cost miss and the verdict failure are the same physical fact seen twice.** **THIRD INDEPENDENT CONFIRMATION TODAY:** F21 (C-152) +110 %/+113 % vs +72 %/+75 %; F22 (C-153) +136 %/+123 % vs +75 %/+80 %; F18b +137 %/+111 % vs +75 %/+80 %. Three cases, one conclusion — **the lab's practice of IMPORTING a per-doubling growth exponent from another case is the defect; measure the growth.** **Contention: NOT separable and NOT claimed** (serial on 1 rank, box 85–100 % busy, no per-level free-core reading beside the ClockTimes). **WASTE, NAMED SEPARATELY AND NOT ABSORBED INTO THE RATIO (charter §6): 1,392.767 core-min — THE ENTIRE RUNG — produced no verdict. Split: 0 useful / 1,392.767 unbought.** With C-152's 889.350 that is **2,282.117 core-min spent by cfd today on two rungs returning NOT A RESULT**, and a successor for either is a supervisor decision, not a detail. Not repairable inside F18b: raising `maxIter` changes `fvSolution`, i.e. the byte-identical F18 case definition, and the gates are CLOSED (rule 2). | `verification/campaign/F18b_TG2D_EXT_RESULTS.md` §3, §6; `verification/runs/F18b_runs/F18b_GRADED.json`; prereg frozen `fcf31542947920de394f27420bfc35ccbcedc98d` |
| **C-167** | 2026-08-27 | heat-transfer | **T1c-U (unheated upstream arm: parabolic inlet at x = −10 D, wall adiabatic for −10 D < x < 0 and 310 K for x > 0; ladders `D_Ts_Re25_U` and `L_Ts_U`, six cases, `buoyantBoussinesqSimpleFoam`). COMPLETION AND THE REGISTERED CONVERGENCE CRITERION READ. NO VERDICT FROM THE FIXED VOCABULARY APPLIES, BY THE RUNG'S OWN REGISTRATION** — `DTS_U_registered.json` `.status`: *"DIAGNOSTIC, NOT GRADED. No band, cannot PASS or GATE FAIL, and no T1c verdict moves on it."* A rung with no band cannot earn one, and none is invented. **Freeze verified across TWO commits, both preceding first compute:** predictions and falsifiers at `6a0d7f45` (2026-08-21 16:40:34Z); build/run instruments at `994daa49` (2026-08-26 22:52:47Z); **first compute 2026-08-27 09:01:55Z — 10 h 09 m after the instrument freeze.** The split is declared in the registration ("ALTERS NO REGISTERED PREDICTION, BAND, THRESHOLD OR FALSIFIER"). All five instruments byte-identical to their freeze, and **the reading instrument `analyse_t1c.py` `3d566802` is byte-identical to its state at the 2026-08-21 commit** — six days and one build amendment later, the function that judges convergence has not moved. Completion (rule 4): **`DONE` on all six**, rc 0, `capped=no`; `capped` classed INFRASTRUCTURE and never a completion conjunct (L-342). **Registered convergence criterion (last two checkpoints 38000→40000, max\|a−b\|/(max(b)−min(b)) ≤ 1e-6): TWELVE of TWELVE (case, field) pairs CONVERGED — every temperature field BIT-IDENTICAL (max\|Δ\| exactly 0.000000e+00) and every velocity field at 1e-14 to 1e-16 relative.** **THE ZEROS ARE EVIDENCE, and that was established rather than assumed (rule 3):** the frozen reader was shown able to report the other outcome by planting **BY LINE INDEX** into scratch copies of two different ladders — `D_Ts_Re25_U_c/40000/T` and `L_Ts_U_f/40000/T`, line 23, +1.234 K — and it returned `NOT_CONVERGED` with `max_change` **1.234000 exactly** on both; the live cases were never written to. A first plant attempted by **string substitution** produced the corrupt token `301.234.0000000000005`, which the reader correctly refused to parse, and was redone by line index — disclosed rather than dropped. **THE DIAGNOSTIC QUESTION THE ARM WAS BUILT FOR IS NOT ANSWERED AND THAT IS DELIBERATE:** no comparator is registered (`DTS_U_registered.json` carries no `comparator`, `graded_rows`, `bands`, `grading` or `falsifier` key, and `analyse_dts.py`'s `LADDERS` at `:82` covers only the parent ladders), so the Péclet reading is **NOT AVAILABLE** — writing a reader now, with the six converged fields already on disk, would be building the measurement instrument after seeing the data, which is the one thing prediction-first pre-registration exists to prevent, and being labelled "diagnostic" does not make it acceptable. Answering it needs a NEW registration naming its reader and stations before it runs. | **234.122 core-min POINT**, built from **per-level PARENT MEASURED rates** so "the level-to-level rate drift is carried rather than averaged away"; cap total 735 (sized at **3.2 × POINT**). **$0.2002 POINT / $0.6284 cap DERIVED** | **277.300 core-min MEASURED** = (585 + 1 741 + 6 543 + 654 + 1 776 + 5 339) wall s × 1 rank ÷ 60 from the six `STATUS_u.*` | **277.300 — gross = cleaned.** `D_Ts_Re25_U_f` (6 543 s) and `L_Ts_U_f` (5 339 s) exceed the 3 600 wall-s stall figure and **neither is a stall** — rc 0, `capped=no`, and both bit-identical in T across their last 2 000 iterations. **WASTE 0.000 core-min** named separately; **no case exceeded 43.6 % of its cap**, total 37.7 % of 735 | **1.1844** | **THE REGISTERED SCRATCH PROBE, NOW SCORED — AND THE REGISTRATION ASKED FOR THIS COMPARISON IN ADVANCE.** The probe measured **6.494e-06 core-s per cell-iteration, 2.266× the parent's own c-level rate**, at 2026-08-26 22:49Z, attributed it to box contention, and said in terms: *"Recorded here rather than folded into the POINT, so the completion calibration can attribute the gap (rule 12) instead of absorbing it."* **The realised c-level rate is 3.0469e-06 — just 1.063× the parent rate. THE PROBE OVER-PREDICTED CONTENTION BY 2.13×**, because the arm ran 09:01–11:31Z on a far emptier box than the one it was probed on. **The CAP reasoning was nevertheless sound: caps were set at 3.2 × POINT explicitly to absorb the probe's 2.27×, "because rule 12 stops the run at the cap and an overrun gets no new budget", and nothing came within 56 % of a cap.** Per case 1.442 / 1.393 / 1.256 / 1.163 / 1.063 / 1.055. **Attribution: contention alone, and the ordering proves it — the two cases that ran FIRST are the dearest (`L_Ts_U_c` 1.442) and the LAST fine case the cheapest (`L_Ts_U_f` 1.055), the opposite of the mesh-scaling signature recorded at C-155 (T13) and C-165 (T4b).** **CARRY FORWARD: a contention probe is a CAP-SIZING instrument with a shelf life of hours, not a rate estimate. Either re-measure it at launch, or size the cap from it and price the POINT from uncontended parent rates — which is exactly what this registration did, and it is why a 2.13× probe error cost nothing.** **$0.2371 DERIVED, NOT MEASURED** at $0.0513/core-h, c7a.4xlarge, `cost_basis` REPORTED-BY-OWNER — the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` §5). | `docs/campaigns/T-family/T1cU_RESULTS.md` §1–§7 (written by this commit); `verification/runs/T-family/T1_runs/log.convergence_dts_u.20260827T170453Z.txt` (readings + planted control); the six `STATUS_u.*` and `DONE.*_U_*`; `DTS_U_registered.json` (freeze `994daa49`) and `DIAGNOSTIC_PREDICTION.md:786` (freeze `6a0d7f45`); read beside C-155 and C-165 as the CONTRAST case — contention-limited, not mesh-limited |
| **C-168** | 2026-08-27 | ansys-verification | **VMFLGPU002 — GPU solver path, laminar tee-junction flow split (manual p.227), `NOT A RESULT` (GPU grid triple OSCILLATORY, R = −0.197406).** Fully graded after a post-compute §2d.1 limb-A reader repair + L-342 C7 reclassification (comparator 8172a0d3 -> 92a82426, committed before the re-grade, moved no band). Cost is a GPU-hour figure; the CPU control arm's core-minutes are work on the same billed instance. | **30.4 core-min** registered (CPU-arm basis; caps 1.0 GPU-h and 60 core-min) | **0.896944 GPU-h** measured (3229 wall-s; `COST.txt`); **CPU arm 30.567 core-min** | = gross (single run to endTime at all six solves; no stall, no re-run — a routed NOT A RESULT verdict costs no extra solver time) | **CPU-arm ratio 1.005** (30.567 / 30.4); the GPU-hour figure is the first VMFLGPU002 GPU-h measurement (no separate GPU-h estimate was registered) | **near-exact on the CPU arm; the GPU run came within 10.3 % of its 1.0 GPU-h cap on a healthy run (recorded — do not repeat a tight cap).** WASTE for the run = 0.000; **named separately: ~16 h 40 m post-run GPU idle ≈ $13.41 derived**, waste not absorbed into the ratio. **$0.7219 derived** at the published-list $0.8048/GPU-h (g6.xlarge us-east-2), **NOT measured** (`COMPUTE_BUDGET_CHARTER.md` §5); console figure owed. | `cases/ansys_verification/VMFLGPU002/RESULTS.md`; register row #34; prereg frozen at `eca61c2d`, comparator amended blob `92a82426` at `51e7b54e` |
| **C-169** | 2026-08-27 | ansys-verification | **CORRECTION ROW — RE-NUMBERS the ansys-verification VMFLGPU001 calibration OFF the duplicate id C-165 (rule 6 quote-and-strike; nothing above edited).** C-165 was assigned CONCURRENTLY to two rows — heat-transfer's `T4b` and this team's `VMFLGPU001` — both deriving `max+1 = 165` in the same window before either landed (the rule-11 hazard). Per rule 6 the originals are STRUCK, never rewritten. **Heat-transfer's C-165 (T4b) is NOT touched and KEEPS id C-165** (heat-transfer is corrected separately; not this team's row to edit). **THIS ROW re-numbers the ansys VMFLGPU001 calibration to C-169:** ~~C-165 · ansys-verification · VMFLGPU001~~ → **C-169**, content verbatim and unchanged. The now-superseded string "calibration C-165" in register row #33 and `cases/ansys_verification/VMFLGPU001/RESULTS.md` reads through to THIS row; those committed records are not edited (rule 6). | **0.40 GPU-h** registered (prereg §12) | **0.441667 GPU-h** measured (1590 wall-s, `COST.txt`; CPU arm 9.15 core-min) | = gross (no stall, no re-run — a comparator refusal costs no solver time) | **ratio 1.10** | **records fix, ZERO compute** — corrects a concurrent duplicate-id assignment, not a cost figure; no physics, core-minute or dollar figure moves. **$0.3555 derived** at the published-list $0.8048/GPU-h (g6.xlarge us-east-2), NOT measured, console owed. | `cases/ansys_verification/VMFLGPU001/RESULTS.md`; register row #33; supersedes the ansys instance of C-165; prereg frozen at `0429091e`, comparator `21fa2387` |
| **C-170** | 2026-08-27 | heat-transfer | **T16 level `c` (`T16_MC_c`) — laminar mixed convection, developing vertical channel, `buoyantBoussinesqSimpleFoam`, serial. Run COMPLETE on all six limbs; case verdict `BLOCKED` on an instrument defect (`D541`), rung `PENDING` on an incomplete triple.** | **16.611** core-min POINT; ceiling 25.062; cap **40** (`T16_registered.json`, `cases.T16_MC_c`); $0.0142 derived | **12.400** core-min MEASURED — `wall_s=744` x `ranks=1` / 60 from `STATUS.T16_MC_c` (`started_utc` 17:30:06Z, `ended_utc` 17:42:30Z); **$0.0106 DERIVED, NOT MEASURED** at $0.0513/core-h, reported-by-owner (`COMPUTE_BUDGET_CHARTER` §5 — the box cannot read its own billing) | **12.400** — gross == cleaned; 744 wall s, far under the 3,600-s stall convention; one continuous solve, `ExecutionTime` count 10000 == `endTime` | **0.747** | **Misprediction, favourable, and a small one — with the strongest possible basis already in place.** The registered POINT rate `3.8932e-06` core-s/cell-iteration was **measured on a scratch copy of this exact case on this box** (`T16_PREREGISTRATION.md` §8), not imported from a sibling, and it still over-predicted by 34 %. That is the useful calibration signal here: even a same-case scratch measurement carries ~30 % over-prediction at the coarse level, so a POINT taken this way should not be treated as tight. Registered cap not approached (31.0 % of 40). **Contention: NOT separable and NOT claimed** — no per-level free-core reading was taken beside the wall time, and it is not offered as an explanation. **WASTE: NONE, and the distinction is load-bearing.** The 12.400 core-min bought a complete converged coarse-level solution, on disk at `T16_MC_c/10000/`; the marker reads artifacts, so the `D541` repair implies **no re-run** and the spend is not lost. Had the repair required re-running the case this row would have become waste in full (charter §6); it did not, and it is not named as waste. | `docs/campaigns/T-family/T16_RESULTS.md` §2, §4, §6; prereg frozen `ae20d137`, all 7 grading-path blobs hashed MATCH in the grading invocation |
| **C-171** | 2026-08-27 | heat-transfer | **T5 level `f` (`T5_CUBE_f`) — conjugate cube, `chtMultiRegionSimpleFoam`, serial (AMENDMENT 2), 9.03e5 cells, `endTime` 5000. `DONE` under the strict rule; NO ROW GRADED (the frozen comparator has no grading driver).** | **2 308.2** core-min (Model B, §11.1's 38.47 core-h); Model A 6 338.4; $1.974 derived | **319.167** core-min MEASURED — `wall_s=19150` x `ranks=1` / 60 from `STATUS.T5_CUBE_f`; **$0.273 DERIVED, NOT MEASURED** at $0.0513/core-h (`COMPUTE_BUDGET_CHARTER` §5) | **319.167** — gross == cleaned; one continuous solve, `ExecutionTime` count 5000 == `endTime`, so the 3,600-s stall convention does not apply to its 19,150 wall s | **0.138** (vs Model B); 0.050 vs Model A | **Misprediction in Model B's per-level GROWTH, localised to ONE step — and this row scores two standing predictions, one HIT and one MISS.** **C-143's carry-forward of 300-350 core-min: HIT** (measured 319.167, near the middle). **`T5_RESULTS.md` §16's own 22:33Z indication of "near 430 core-min": MISS, high by 35 %** — it extrapolated from `Time = 121` reached in ~630 s *including the mesh build*, loading start-up onto a per-iteration rate; it was labelled an indication, and it is scored here as promised rather than dropped. **THE MECHANISM, MEASURED ACROSS THE LADDER:** cost is very nearly linear in cells — `c->m` x4.704 on x4.074 cells (**O(N^1.10)**), `m->f` x4.219 on x4.105 cells (**O(N^1.02)**). Model B assumed **x11.63 for `c->m`** (O(N^1.75)) but **x4.35 for `m->f`** (O(N^1.04)) — **internally inconsistent: right at the top of the ladder, wrong by 2.5x at the bottom, and that single step is the ENTIRE over-prediction.** It propagates cleanly: actual/Model B is 0.353 (`c`), 0.363 (`H_c`), 0.143 (`T5_CUBE_m`), 0.130 (`P_m`), 0.148 (`L_m`), 0.138 (`f`) — a sharp break between the coarse level and everything above it. **DIRECTION DISCLOSED BECAUSE IT CUTS AGAINST THE LAB'S RECENT FINDING:** C-152, C-153 and C-166 each found an *imported* exponent UNDER-predicting; here an *a priori* model OVER-predicts by 7x. Same lesson from opposite sides — **measure the growth on this solver and this mesh family; do not carry an exponent in from anywhere, a cost model's own coarse level included.** **Contention: NOT separable, NOT claimed** (many T-family solvers live across the window; no per-level free-core reading). **WASTE: none.** | `docs/campaigns/T-family/T5_RESULTS.md` §18, §21; `DONE.T5_CUBE_f`; freeze set 6/6 MATCH in the grading invocation; prereg `0fcbb92e`, A9 `ad663118` |
| **C-172** | 2026-08-27 | heat-transfer | **T5 `L_m` (the §DC laminar baseline, turbulence model OFF) — landed complete on five of six limbs; verdict `BLOCKED` on an instrument defect (`DD544`): the marker's field tuple is turbulent and this arm is registered laminar.** | **371.4** core-min (Model B, 6.19 core-h); $0.317 derived | **54.933** core-min MEASURED — `wall_s=3296` x 1 / 60 from `STATUS.L_m`; **$0.047 DERIVED, NOT MEASURED** at $0.0513/core-h | **54.933** — gross == cleaned; one continuous solve, `ExecutionTime` count 5000 == `endTime` | **0.148** | **Misprediction, same mechanism and same magnitude as C-171 — which is the point of carrying this row separately.** `L_m` is a *medium*-level case and lands at 0.148 of Model B, inside the tight band the other medium cases occupy (`T5_CUBE_m` 0.143, `P_m` 0.130). **The over-prediction is therefore a property of the LEVEL, not of the case or its physics:** turning the turbulence model off changed the price by far less than Model B's `c->m` growth error did, so the laminar arm is not separately mispriced and needs no separate rate. **Contention: NOT separable, NOT claimed.** **WASTE: NONE, and the distinction is load-bearing.** The 54.933 core-min bought a complete laminar solution, on disk at `L_m/5000/`, gradeable the moment `DD544`'s tuple is made per-arm — the marker reads artifacts, so the repair implies **no re-run**. Had it required one, this row would have become waste in full (charter §6); it did not, and it is not named as waste. | `docs/campaigns/T-family/T5_RESULTS.md` §20, §21; `STATUS.L_m`; prereg `0fcbb92e` §DC |
| **C-173** | 2026-08-27 | heat-transfer | **T5 `S_m` (the §8 `DS` constant-`T` arm) — `NOT A RESULT`. WASTE, nothing bought: the registered solver cannot start a fluid-only case (`DD543`).** | **489.0** core-min (AMENDMENT 9(h)); cap **1 467.0**; $0.418 derived | **0.000** core-min recorded (`STATUS.S_m` `wall_s=0`, `rc=1`, `note=SOLVER_NONZERO_EXIT`); the solver's own life was **under one second** (`log.solve` 17:27:56.888 -> `launcher.queue.out` 17:27:56.918) and the launch including its `checkMesh` step spans **~4 wall s** from 17:27:53. **<= 0.07 core-min, BOUNDED NOT MEASURED** — the wrapper records whole seconds and recorded zero. $0.00006 derived | **<= 0.07** — gross == cleaned; no stall (4 s against the 3,600-s rule) | **NO RATIO IS QUOTED.** A ratio of ~0.0001 would read as a spectacular underspend and would be a lie: **nothing was bought** — zero iterations, no time directory beyond `0`. Per charter §6 this is named as waste and kept out of every ratio column. | **WASTE: <= 0.07 core-min, the whole row, and it is a BUILD/REGISTRATION defect** — not contention, not a stall, not physics, not the toolchain (the mesh was built cleanly on 2026-08-26 and `checkMesh` ran `rc=0`). **THE ESTIMATE WAS NOT WRONG:** 489.0 core-min remains the price of the `S_m` arm and is carried unchanged into any re-fire. **What this row calibrates is a MECHANISM, not a rate** (the C-164 precedent): AMENDMENT 9's six read-back refusals all interrogate FILE CONTENT, and its two controls covered the conjugate path and the mesh — **so nothing in the amendment ever asked whether the case it emits can be STARTED by the solver it is registered to use.** The cheap defence is a builder that starts its registered solver once, for three seconds, before the case is armed. **Cost of the missing control: 489.0 core-min of queue commitment, of which <= 0.07 was actually burned — the near-zero burn is luck, not design; the same defect behind a solver that starts and then fails late would have burned the cap.** | `docs/campaigns/T-family/T5_RESULTS.md` §19; `S_m/log.solve`, `STATUS.S_m`, `S_m/CASE.txt`; AMENDMENT 9 @ `ad663118` |

| **C-174** | 2026-08-27 | ansys-verification | **VMFL076-R2 — Forced Convection over a Flat Plate (VM2026R1 p.219), coarser three-level `simpleFoam` triple, GRADED `GATE REACHED`** — both frozen gates met at the finest level on a `CONVERGING` triple (R1's finer triple was non-`CONVERGING`). Validation register **row #35** | **0.6 core-min TOTAL** registered (`cases/ansys_verification/VMFL076-R2/PREREGISTRATION.md` item 12, frozen `8667be72`; basis a MEASUREMENT of R1's per-level rates, RANKS = 1, per-level cap 10 core-min) = **$0.00051 derived** at $0.0513/core-h | **0.6666 core-min MEASURED** from the per-level `RUN_RC.txt` (L1 0.0333 + L2 0.1000 + L3 0.5333, total wall 40 s × 1 rank ÷ 60) = **$0.00057 derived, NOT MEASURED** (the box cannot read its own billing, `COMPUTE_BUDGET_CHARTER.md` §5) | **= gross.** The longest level is 32 wall s (L3), two orders below the 3600-s stall rule, so gross and cleaned coincide | **1.111×** measured/predicted; **0.067× of the 10 core-min per-level cap** at the costliest level. No overrun | **Misprediction, small and in the costly direction.** All three levels ran to endTime (`rc = 0`, strict completion rule 4 met at all three); the estimate priced R1's per-level `ExecutionTime` rate and the coarser R2 family cost marginally more per cell-iteration than that extrapolation. **CONTENTION: none claimed** — serial, sub-minute runs; `ExecutionTime` tracks `ClockTime`. **WASTE: 0.000** — nothing truncated, nothing re-run. |

| **C-175** | 2026-08-27 | ansys-verification | **VMFL011-R3 — Laminar Flow in a Triangular Cavity (VM2026R1 p.41/42), three-level `simpleFoam` triple, GRADED `GATE FAIL`** — `rms_vs_benchmark = 0.034088 > 0.030` at L3 on a `CONVERGING` triple; the prereg predicted `GATE FAIL` before compute. First live R-RC application, lands on **R-RC-1** (rc record present, rc = 0, measured). Validation register **row #36** | **9.1 core-min TOTAL** registered (`cases/ansys_verification/VMFL011-R3/PREREGISTRATION.md` item 12, frozen `45c3e8a4`; basis a MEASUREMENT of the R2 run's per-level rates, RANKS = 1, running-total cap 50 core-min) = **$0.0078 derived** at $0.0513/core-h | **9.5667 core-min MEASURED** from `RUN_RC.L1/.L2/.L3` (L1 0.2667 + L2 1.0667 + L3 8.2333, total wall 574 s × 1 rank ÷ 60) and `COST.txt` = **$0.00818 derived, NOT MEASURED** (the box cannot read its own billing, `COMPUTE_BUDGET_CHARTER.md` §5) | **= gross.** The longest level is 494 wall s (L3), below the 3600-s stall rule, so gross and cleaned coincide | **1.051×** measured/predicted; **0.191× of the 50 core-min running-total cap**. No overrun | **Misprediction, small and in the costly direction.** All three levels ran to endTime 20000 (`rc = 0`, strict completion rule 4 met at all three); the estimate priced the R2 per-level rates and the R3 run cost 5 % more, within run-to-run scatter. **CONTENTION: none claimed** — serial runs, `ExecutionTime` tracks `ClockTime` (L3 493.76 s CPU / 494 s wall). **WASTE: 0.000** — nothing truncated, nothing re-run. |

| **C-176** | 2026-08-27 | ansys-verification | **VMFL007-R2 — Non-Newtonian pipe, linear-solver/preconditioner SLATE (VM2026R1 p.29), single-grid 6-arm screen, GRADED `NOT A RESULT`** — the frozen comparator refused (exit 2) at the planted-zero control on arm A1 (a diverged `pInlet` series, v = 7.12e+73, plant invisible). 4 of 6 arms diverged; only the DIC-preconditioned A3/A5 survived. Validation register **row #37** | **30 core-min POINT ESTIMATE** registered (`cases/ansys_verification/VMFL007_R2/PREREGISTRATION.md` §7, frozen `141185ad`; A1 MEASURED from run-1's log, A2–A6 estimated and labelled WEAK, RANKS = 1; slate cap 90, per-arm cap 20) = **$0.0257 derived** at $0.0513/core-h | **9.2167 core-min MEASURED** from `COST.txt` (total wall 553 s × 1 rank ÷ 60) = **$0.00788 derived, NOT MEASURED** (the box cannot read its own billing, `COMPUTE_BUDGET_CHARTER.md` §5) | **= gross.** No arm ran over 3600 wall s, so gross and cleaned coincide | **0.307×** measured/predicted (against the 30 core-min point estimate); **0.102× of the 90 core-min slate cap**. No overrun | **Misprediction in the conservative direction, from a WEAKLY-BASED estimate the prereg labelled weak.** The A2–A6 per-arm figures were resolution-limited at the pre-flight (iteration-1 `ExecutionTime` 0.03–0.04 s could not discriminate), so the 30 core-min point estimate carried headroom the run did not use; A1 alone was 3.88 core-min measured and the whole slate came in at 9.22. **CONTENTION: none claimed** — serial arms, `ExecutionTime` tracks `ClockTime`. **WASTE: 0.000** — every arm ran to its own endTime; nothing was truncated or re-run (four diverged, which is a physics finding, not wasted compute). |

| **C-177** | 2026-08-27 | ansys-verification | **VMFLGPU003 — GPU solver path on the triangular cavity (VM2026R1 p.229, CPU parent VMFL011), three-level GPU/forced-CPU ladder, GRADED `GATE FAIL`** — limb C (rms vs the digitised benchmark) missed at L3 (0.034088 > 0.030); limbs A (GPU execution) and B (GPU ≡ CPU, 4.607e-12) both HELD on a `CONVERGING` triple. The GPU path is validated and CPU-equivalent; the miss mirrors the CPU parent VMFL011-R3 (row #36). Validation register **row #38** | **0.70 GPU-h** registered (`cases/ansys_verification/VMFLGPU003/PREREGISTRATION.md`, frozen `fc8bef51`; CAP 1.5 GPU-h, CPU-arm cap 90 core-min, RANKS = 1) = **$0.5634 derived** at published-list $0.8048/GPU-h | **0.856944 GPU-h MEASURED** from `COST.txt` and `RUN_RC.*.gpu` (total wall 3085 s; CPU control arm 14.7333 core-min on the same billed instance) = **$0.6897 derived, NOT MEASURED** — GPU dollars are DERIVED at the AWS published list, the CONSOLE figure is STILL OWED and supersedes (`COMPUTE_BUDGET_CHARTER.md` §5, `GPU_CAPABILITY_STATE.md` §9) | **= gross.** No solve ran over its cap (`cap_fired = 0` on all six); the longest is L3 gpu 1796 wall s, so gross and cleaned coincide | **1.224×** measured/predicted; **0.571× of the 1.5 GPU-h cap**. No overrun | **Misprediction in the costly direction, and the duration prediction WAS TESTED** — all six solves ran to endTime (`rc = 0`, strict completion met). The estimate under-priced the L3 GPU solve (0.4989 GPU-h alone). **CONTENTION: none claimed** — serial arms. **WASTE, NAMED SEPARATELY AND NEVER ABSORBED INTO THE RATIO: 46.15 min GPU idle** between VMFLGPU003 finishing (18:08:43Z) and VMFLGPU001-R2 starting (18:55:33Z) = **0.7692 GPU-h, $0.6190 derived** (idle window measured by the supervisor). This is an **infrastructure defect — a launcher freeze check bound to the wrong tree** — not compute for this rung; it is reported here and excluded from the 1.224 ratio. |

| **C-178** | 2026-08-27 | dafoam | **D12R phase 1 — THE 63.95 core-min COMES BACK OUT OF WASTE, AND THE DEFECT'S REAL PRICE IS A DIFFERENT NUMBER: THE LAB BOUGHT ONE MEASUREMENT TWICE.** The chief ruled D12R phase 1 the lab's only re-gradeable infrastructure-only row (`docs/L342_GRADER_AUDIT.md` **Addendum 2**, `7547e8f8`: `rc==0` 32/32, `end_line_present` 32/32, `age_guard_ok` 32/32, `oomkilled` 0, and **one** launcher-metadata field — `status: null` on the single `task=shell` row — voiding all 32). Under **`L-342` and Sanaa's `R-RC`, deliberately NOT `VERIFICATION_CHARTER.md` §2d.1** (this family has refused §2d.1 four times; an exception used where the ordinary path is open is an exception being widened), `d12x_grade.py`'s status limb was made **task-aware** at one named site — 17 `raise Refusal` sites became 19, **one replaced by three, sixteen untouched, two of the three strictly stricter** — and the preserved artefacts were re-graded at **zero solver compute**. **All nine gate readings landed; `G12R-4` = `NOT A RESULT`, `h_min = 1.742838e-01` vs `h_max = 5.000e-02`, `admissible: false`.** **`C-107`'s `WASTE = 63.95 core-min` IS QUOTED AND STRUCK HERE**, exactly as `C-117` was struck at `C-145`: the compute did not produce nothing — it produced the measurement, and a grader defect hid it for 30 hours. **`C-107` IS NOT EDITED AND ITS REASONING IS NOT WITHDRAWN**; it was correct on the authority available on 2026-08-26, when `R-RC` did not exist. **`C-100`'s 0.914 ratio is neither withdrawn nor restated** | **—** for the flip (no new prediction; this row books a reversal and a duplication). **Re-grade itself: 0.00497 core-min**, the rule-12 estimate-versus-actual line — no estimate was ever registered for a re-grade, so **there is no ratio to state and the omission is the point** | **63.95** core-min — the original phase-1 spend, unchanged from `C-100`, `ledger.txt` `PHASE1_COMPLETE spent=63.95 core-min`; **plus 0.00497 core-min** for the re-grade, 0.298 wall s at 1 rank, measured | **63.95 + 0.00497** — identical to gross; nothing stalled, no row near the charter §2 3600-s rule | **0.914** for the phase-1 solve (carried from `C-100`, NOT restated as new skill); **— for the re-grade** | **THE STRIKE IS NOT THE COST, AND MUST NOT READ AS A TIDY RECOVERY. THE COST IS THE DUPLICATE PURCHASE: the lab bought this one measurement TWICE — D12R's 63.95 core-min produced it and D12R2's 55.5167 core-min (`C-115`) re-bought it BIT-FOR-BIT IDENTICALLY** (2400-sample `CD` series byte-equal, 0 differing samples; same `h_min`, same `δ_eff`). **The grader defect's true price is therefore 55.5167 core-min, $0.04747 DERIVED, NOT MEASURED**, a different figure from the 63.95 being struck. **D12R2's 55.5167 IS NOT RE-BOOKED AS WASTE** — it was correctly bought under the then-standing *"RE-REGISTER. NOT REPAIR, NOT PATCH."* ruling, and a spend legitimately incurred under a then-correct ruling is not retroactively waste. **ATTRIBUTION: 100 % GRADER DEFECT, 0 % SOLVE, 0 % CONTENTION** — the launcher writing `status = None` for a stage that produces no per-stage JSON is **correct behaviour**; the reading side was the one out of compliance with a standing rule. **RECOVERED: 63.95 core-min, $0.05468 derived.** **WASTE AFTER THIS ROW: 0.000 core-min on D12R** (`COMPUTE_BUDGET_CHARTER.md` §6). All dollars **DERIVED, NOT MEASURED** at $0.0513/core-h, c7a.4xlarge, **reported-by-owner** (§5 — the box cannot read its own billing). **WHAT THE RE-GRADE BOUGHT BEYOND THE RECOVERY:** the FD line closed as a registered §2.2 finding about the method–case pair, and a bit-level reproducibility datum defended from disk against a copied-output hypothesis (**`np = 1`, one case, one host, one image — NOTHING at `np > 1`**). **WHAT IT MAY NEVER BE QUOTED AS:** an independent measurement of the FD bright line — D12R2 published these gates first | `cases/dafoam/curriculum_D12R/RESULTS.md` §1, §7 (`5eff58ea`); `PREREGISTRATION.md` §10 v1.1 and `d12x_grade.py` (`9517a39f`); `PHASE1_REGRADE_GATES.json`, `D12R_D12R2_REPRODUCIBILITY_EVIDENCE.txt` (`76846c1e`); **strikes `C-107`**; cites `C-100`, `C-115`, `C-145` |
| C-179 | 2026-08-27 | heat-transfer | **T16_MC_c — the COARSE level of the T16 mixed-convection rung, run to `endTime` and marked `DONE`. THE RUNG IS NOT GRADED and stays `PENDING`:** `T16_MC_m` was still running and `T16_MC_f` still queued, so the Roache triple is incomplete and the frozen comparator refused the partial rung (exit 2), correctly. A second row for the graded rung is owed at grading and is not pre-empted here. `buoyantBoussinesqSimpleFoam`, laminar 2-D, 25 600 cells, 10 000 iterations, serial (1 rank), `nice 15`. | **16.611 core-min** POINT (ceiling 25.062; registered CAP 40; `timeout_s` 2 400) — `T16_PREREGISTRATION.md` §8, from probe-4's measured rate 3.8932e-06 core-s per cell-iteration. **USD $0.014202 — DERIVED, NOT MEASURED** at $0.0513/core-h. | **12.400 core-min** = 744 wall s × 1 rank ÷ 60, read from `STATUS.T16_MC_c` (`wall_s=744`, `ranks=1`, `core_min=12.400`, `capped=no`, `note=clean`, started 17:30:06Z ended 17:42:30Z). Completion independently corroborated: `End` line present, last time 10 000 == `endTime` 10 000, `ExecutionTime` count 10 000, fields `T U p_rgh phi` present at 10 000 and all newer than `0/T`. **USD $0.010602 — DERIVED, NOT MEASURED.** | **12.400 core-min — gross == cleaned.** The single row is 744 wall s, well under the charter §2 3 600-s stall threshold, so the stall rule matches nothing and there is nothing to subtract. | **0.7465** | **MISPREDICTION IN THE SAFE DIRECTION, CAUSE NAMED AND ALREADY DISCLOSED AT FREEZE.** The registered rate was measured on probe 4 — the registered configuration exactly — but on a box at load average ≈ 17 with three peer solvers live, and §8 stated at freeze that contention "can only inflate it, so it is conservative in the safe direction." It was: the actual rate is **2.9062e-06 core-s per cell-iteration against the registered 3.8932e-06, a ratio of 0.7465** — identical to the core-minute ratio, as it must be since both are linear in wall time, which is itself a consistency check on the two figures. So the whole 25.35 % underspend is attributable to a **contention-inflated rate basis**, not to a modelling error in cells × iterations: the geometry term was exact. **CALIBRATION LESSON: a rate probed under heavy contention over-predicts by ≈ 34 % when the graded run meets a lighter box; the conservatism is deliberate and should be kept, but the ratio it produces is not evidence of a bad model.** The run used 31.0 % of its registered cap, so no overrun and no cap decision arose. **WASTE: 0 core-min, named separately and NOT absorbed into the ratio** (charter §6) — no re-launch, no stall, no capped row, no discarded attempt. **The D541 marker defect that blocked this level cost ZERO core-minutes:** it was a false refusal by the completion marker, not a solver failure, so the 12.400 core-min were already on disk and the repair recovered them rather than re-spending them; had it been repaired by re-running instead, this row would have carried 12.400 core-min of pure waste. | commits `5bcbaf8c` (D541 repair applied), `d036fc4f` (`T16_PREREGISTRATION.md` AMENDMENT A1, freeze-set update); predicted from `docs/campaigns/T-family/T16_PREREGISTRATION.md` §8 at `ae20d137`; actual from `verification/runs/T-family/T16_runs/STATUS.T16_MC_c` and `DONE.T16_MC_c` |
| C-180 | 2026-08-27 | heat-transfer | **CORRECTION ROW, naming the row it corrects: `C-179` IS A DUPLICATE OF `C-170` AND ITS GAP ATTRIBUTION IS WITHDRAWN.** Both rows cover the same process — the T16 coarse level `T16_MC_c` — with the same figures from the same artifacts. `C-179` should not have been written; `C-170` already stood. Under this file's append rule 1 nothing is edited, so `C-179` stays on the record and this row withdraws its distinct claims. | **not a new prediction** — the predicted figure is `C-170`'s: 16.611 core-min POINT, ceiling 25.062, cap 40. | **not a new actual** — 12.400 core-min, the same `STATUS.T16_MC_c` reading `C-170` already carried. **ZERO additional core-minutes were spent** between `C-170` and this row: the D541 repair touched only the completion marker and launched no solver. | **not a new figure** — 12.400, gross == cleaned, as `C-170`. | **not a new ratio** — 0.747 as `C-170` states it. | **WHAT IS WITHDRAWN, AND WHY `C-170` WAS RIGHT AND `C-179` WAS WRONG.** `C-179` attributed the 25.35 % underspend to a "contention-inflated rate basis", reasoning from §8's freeze-time note that probe 4 ran at load average ≈ 17. **That attribution is not supported and is withdrawn.** `C-170` had already refused to make it, in terms: *"Contention: NOT separable and NOT claimed — no per-level free-core reading was taken beside the wall time, and it is not offered as an explanation."* That refusal is correct. The probe's contention is recorded; **the registered run's contention was never measured**, so the difference between the two cannot be assigned to it. `C-170`'s reading stands as this process's attribution: a same-case scratch-measured POINT still over-predicted by 34 %, and the useful signal is that a POINT taken that way should not be treated as tight. **WHAT IS GENUINELY NEW AND BELONGS ON THE RECORD:** `C-170` recorded the case verdict as `BLOCKED` on `D541` and reasoned that the marker reads artifacts, so a repair would imply no re-run and the spend would not be lost. **That is now CONFIRMED rather than anticipated:** the repair landed at `5bcbaf8c`, `T16_MC_c` was marked `DONE` at `61378c13` off the artifacts already on disk, and the recovery cost 0 core-min. `C-170`'s **WASTE: NONE** therefore holds as measured, not as forecast. **WASTE: 0 core-min, unchanged, and never folded into any ratio** (charter §6). **THE MECHANISM OF THE DUPLICATION, NAMED SO IT IS CHECKABLE:** the lane re-derived the next id correctly from the tail — `append_record.py` refused a first attempt at `C-167` (exit 6) because a naive `^\| C-[0-9]+` grep misses the `\| **C-nnn**` openers used from C-167 on, and the id was re-derived to C-179 rather than forced. **Re-deriving the id is necessary and is NOT sufficient.** It answers "is this number free?" and never asks "is this PROCESS already on the ledger?" A correct id derivation produced a duplicate process row anyway. **TWO CHECKS ARE OWED BEFORE ANY APPEND, NOT ONE: re-derive the id from the tail by the tool's own pattern, AND grep the ledger for the process key — here `T16_MC_c` — and read every row that matches before writing.** The second check costs one grep and would have caught this. | corrects `C-180` → `C-179`; the standing row for this process is `C-170`. Repair commit `5bcbaf8c`; marking commit `61378c13`; amendment `d036fc4f`. Actual from `verification/runs/T-family/T16_runs/STATUS.T16_MC_c`; `docs/campaigns/T-family/T16_RESULTS.md` §2, §4, §6 |
| **C-181** | 2026-08-27 | ansys-verification | **VMFLGPU001-R2 — GPU solver path on the concentric-cylinder (Taylor–Couette) case (VM2026R1 p.225, CPU parent VMFL001-R2), three-level GPU/forced-CPU ladder, GRADED `GATE REACHED`** — all three limbs HELD (limb A GPU execution; limb B GPU ≡ CPU, worst 3.74e-11; limb C GPU vs the exact closed form, worst 5.29e-4) on a `CONVERGING` triple (R = 0.24827, p = 2.0100, GCI_fine = 0.05635 %). Successor to VMFLGPU001 R1 (row #33, `NOT A RESULT`); R2's repaired liveness-plateau and re-based limb-A controls both graded cleanly. Validation register **row #39** | **0.44 GPU-h** registered (`cases/ansys_verification/VMFLGPU001-R2/PREREGISTRATION.md` §7, frozen `0bc37653`; drawn from R1's measured 0.441667 GPU-h at register row #33; CAP 2.0 GPU-h, CPU-arm cap 40 core-min, RANKS = 1) = **$0.3541 derived** at published-list $0.8048/GPU-h | **0.441111 GPU-h MEASURED** from `COST.txt` (total wall 1588 s; CPU control arm **9.0500 core-min** on the same billed instance, vs a ~9.2 core-min estimate) = **$0.3550 derived, NOT MEASURED** — GPU dollars are DERIVED at the AWS published list, the CONSOLE figure is STILL OWED and supersedes (`COMPUTE_BUDGET_CHARTER.md` §5, `GPU_CAPABILITY_STATE.md` §9) | **= gross.** No solve ran over its cap (`cap_fired = 0` on all six); the longest is L3 gpu 880 wall s, below the 3600-s stall rule, so gross and cleaned coincide | **1.003×** measured/predicted on GPU-h (**0.984×** on the CPU-arm core-min); **0.221× of the 2.0 GPU-h cap**. No overrun | **Near-zero misprediction — the estimate was the RIGHT KIND: a MEASUREMENT of the R1 run (row #33), and R2 reproduced it to 0.25 % on GPU-h and 1.6 % on CPU-arm core-min.** All six solves ran to endTime (`rc = 0`, strict completion met; cap_fired 0). **CONTENTION: none claimed** — serial arms on a dedicated GPU instance. **WASTE: 0.000** — nothing truncated, nothing re-run; a clean grade to the tier ceiling. **PERFORMANCE NOTE (not a cost defect, recorded for honesty):** at 1k–16k cells the GPU arm is SLOWER than the CPU arm (L3 880 s GPU vs 513 s CPU), transfer overhead dominating — expected and registered; the GPU path here is a CORRECTNESS vehicle, not a speedup, and the lab claims no speedup it did not measure. |
| **C-182** | 2026-08-27 | dafoam | **D4S-F3S (A2, `curriculum_D4_SHIPPED_F3S`) — THE TWO-ROW ENDPOINT FD TABLE, GRADED ON ITS FROZEN PATH. ITEM VERDICT `NOT A RESULT`, AND NOT ON PHYSICS.** Both arms fired and completed: `F-S` (SHIPPED `dafoam/opt-packages:latest` `9d45679d…`) 13:49:11→13:58:48Z and `F-P` (PATCHED `dafoam-idwarp-rot:v1` `2927768a…`) 13:59:59→14:09:21Z, np=4 on the registered cpuset 5,6,7,9, 12 g, in-container deadline 1,800 s. Freeze `8dfb4598` re-derived by `git log --diff-filter=A`, never from a subject line; all seven frozen files (prereg, grader, acceptance module, FD instrument, launcher, driver, stager) hash **byte-identical to their committed blobs**, and the two instruments staged into the run root hash to the same blobs. Grader md5 `2ed0651c786cb5bd8832f8660ba6c670` gated before the run and re-gated here. **Nineteen of twenty gate readings PASS** — `G5` endpoint FD **5 of 5 components in band on BOTH rows** (aggregate vector-relative error `F-P` **0.1634 %**, `F-S` **0.3501 %** against a 5 % band; no sign flips; plateau 10 % met on every component), `G-ACC` stationarity **22 of 22 primals accepted and re-graded on both rows**, `G6` planted control **1.234e-03 seen in all three channels on both rows with the source md5 unchanged**, `G6b`/`G7` negative and count-refusal controls all refused as designed, `G9` toolchain identity exact on digest and `libidwarp.so` md5 with the two rows distinct, `G10`/`G11`/`G12` PASS. **The single failing limb is `G1`'s AGE CLAUSE, and it fails on ONE artefact on each row: `OptView.hst`** (`F-S` mtime 1787728010 vs datum 1787838479; `F-P` 1787689205 vs 1787839128), `n_stale = 1` on both. **THE MECHANISM, AND IT IS AN INSTRUMENT DEFECT, NOT A RUN DEFECT: `OptView.hst` is a STAGED INPUT here, never a product.** `d4s_f3s_stage_arm.sh:61-63` copies the source endpoint directory with `cp -a` and says so in its own log line — *"mtime semantics = cp -a, PRESERVE"* — and this item re-runs no optimiser, so `OptView.hst` keeps its source mtime by construction and **the age clause on it can never pass for an FD-only item.** The grader's `ARTEFACTS` list (`d4s_f3s_grade.py:61`) was inherited from the optimiser-arm shape, where `OptView.hst` IS a product (D5's `SOLVER_ARTEFACTS` uses it correctly). The 26/26 selftest could not catch it: its fixture **creates** `OptView.hst` fresh (`:599-601`) against a datum fixed at epoch 1000000000, so the real staging semantics were never exercised. **NOT REPAIRED AND NOT ARGUED AWAY: the frozen comparator's word stands.** The five artefacts that actually date this run — `d4_endpoint_dvs_PHYSICAL.json`, `d4_major_history.json`, `d4s_f3s_fd_endpoint.json`, `.jsonl`, `d4s_f3s_accept.jsonl` — are all NEWER than the datum on both rows; rc=0 and `oomkilled false` from `docker inspect`; the POSITIONAL terminal clause holds (`Finalising parallel run` is the last non-empty line, 5703 of 5703, on both logs, with `substring_anywhere` recorded as NOT the test). Re-graded independently at zero solver compute, verdicts, `G5` and predictions **bit-identical** to the driver's run. **Predictions: P1 HIT, P3 HIT, P2 MISS, P4 MISS.** P2's MISS IS THE ITEM'S SCIENTIFIC CONTENT and was named as such before compute: the prereg predicted SHIPPED would `GATE FAIL` on `shape[18]` and registered the falsifier *"5/5 in band = the design-point-dependence finding"* — SHIPPED came in 5/5 in band (`shape[18]` rel err **0.108 %**), so **the falsifier fired as written**. P3 HIT to the printed digits: PATCHED reproduces D4's F3 table at worst relative difference **0.0** against a 1e-12 tolerance. | **94.534 core-min POINT** for the item (47.267 per arm), caps **120.0** each, ceiling **240.0** (`PREREGISTRATION.md` §7 line 78 @ `8dfb4598`; basis = 2 × D4's measured PATCHED F3 arm, `f3_ledger.txt`, 709 wall s × 4 ranks). **$0.080827 DERIVED, NOT MEASURED** at $0.0513/core-h, reported-by-owner (`COMPUTE_BUDGET_CHARTER.md` §5) | **75.867 core-min MEASURED** from the run root's own `ledger.txt`: `F-S` **38.467** (`wall_s=577` × 4 ÷ 60) and `F-P` **37.400** (`wall_s=561` × 4 ÷ 60), both rc=0, `inspect [0 false]`, `delivered_cores_mean` 3.9802 and 3.9561 of 4. **$0.064866 DERIVED, NOT MEASURED** | **75.867 — gross == cleaned.** The `scripts/self_audit.py::check_ledger_stalls` 3,600-wall-s rule (`COMPUTE_BUDGET_CHARTER.md` §2) matches no row here: the longest is 577 s | **0.8025** item (per arm: `F-S` **0.8138**, `F-P` **0.7912**) | **MISPREDICTION, FAVOURABLE — AND THE REGISTERED NUMBER AND THE REGISTERED BASIS ARE TWO DIFFERENT COMPUTATIONS. THE BASIS WAS RIGHT; THE NUMBER WAS 29 % HIGH.** §7 line 78 derives 47.267 from D4's whole F3 **arm total** (709 s), while line 80 states the basis it was actually reasoned from — *"22 primals converge at the PATCHED rate (~25 s each, measured on r2's single primal: 25 s)"*. That basis predicts **36.667 core-min per arm** (22 × 25 s × 4 ÷ 60), and the measured arms land at **ratio 1.020 (`F-P`) and 1.049 (`F-S`)** against it — 2 % and 5 %, a near-exact per-primal rate (measured **25.5 s and 26.2 s per primal**, 22 primals each). The 709 s anchor carried D4's F3 per-arm overhead at **32.2 s/primal**, which this item did not incur. **The calibration lesson: when a registration states a rate basis in prose and a total from a sibling's ledger, price the rate — and if the two disagree by 29 % the disagreement is the finding, at freeze time, not after.** **Contention: MEASURED AND PRESENT BUT IN THE OPPOSITE DIRECTION, so it explains none of the underspend and is not claimed** — `delivered_cores_mean` 3.9802 / 3.9561 of a 4-core dedicated cpuset (0.5 % and 1.1 % short) with `max_nr_throttled` 2477 / 1597; H5 floors and the memory aggregate cleared before both arms (min 25.63 / 27.21 GiB against a 16.0 floor). **WASTE: 0.000 core-min, named rather than omitted** (`COMPUTE_BUDGET_CHARTER.md` §6). Nothing was re-fired and nothing abandoned: both arms produced their complete FD tables, every physics gate reads PASS, and the artefacts are on disk. **The `NOT A RESULT` label costs the ITEM its verdict, not the lab its compute** — and the distinction is exactly why the age-list defect is worth repairing before D4S-F3S is re-filed, since a re-fire would buy the same 75.867 core-min and earn the same label. **Referred to the dafoam supervisor: not repaired by this lane** (rule 6, and repairing an instrument to get a verdict is the thing the rule forbids). | `cases/dafoam/ladder-a/A2/curriculum_D4_SHIPPED_F3S/PREREGISTRATION.md` §7 (frozen `8dfb4598`, blob `b5218d6f`); grader blob `fdffb01f`; run root `/home/ubuntu/certonomous-runs/CURRICULUM-D4S-F3S-a2-wing-cdmin` — `ledger.txt`, `STATUS.chain`, `CHAIN_DONE`, driver grade `d4s_f3s_grade_20260827T140921Z.json`, independent re-grade `d4s_f3s_grade_20260827T194848Z.json` |
| **C-183** | 2026-08-27 | dafoam | **D5 arm `O48` (chain r2) — BOUGHT, COMPLETE, AND COSTED HERE BECAUSE GRADING CANNOT REACH IT. `C-139` deferred this row with the words "is costed at grading, not here"; grading is BLOCKED and the deferral would otherwise leave 684.533 bought core-minutes uncosted indefinitely.** The 48-DV FFD-density optimiser arm, `dafoam-idwarp-rot:v1` PATCHED row `2927768a…`, np=4 on the registered cpuset 8,10,11,13, 12 g, in-container deadline 12,000 s, ran 17:49:11→20:40:20Z on 2026-08-26. **Rule 4 re-derived independently by this lane, every limb, and every limb HOLDS for this arm:** rc=0 with kernel `inspect [0 false]` (never the harness `$?`); the POSITIONAL terminal clause — `Finalising parallel run` is the last non-empty line, 50473 of 50473; both registered age artefacts NEWER than the arm's own `.d4_age_datum` 1787766551 (`opt_IPOPT.txt` and `OptView.hst`, both 1787776793 — and here `OptView.hst` IS a product, which is precisely the contrast that exposes the D4S-F3S age-list defect in the row above); IPOPT reached its registered `max_iter`, **`Number of Iterations....: 100`, `EXIT: Maximum Number of Iterations Exceeded`**, as designed. **WHY NO ITEM VERDICT EXISTS AND NONE IS INVENTED: the frozen comparator `d5_grade.py` REFUSED, correctly, exit 2** — `REFUSE G-D5-1` on `absent: O192/opt_IPOPT.txt`. The cross-density gate needs the 192-DV arm and the 192 arms were never bought: the r3 chain stopped at `F48` (`D5-LAUNCHER-DEF-1`, `C-164`) before `O192` fired. **The comparator refused rather than degrade and was not repaired to yield a partial verdict.** `CD_f(48) = 2.1085869973159383e-02` is on disk in `O48/opt_IPOPT.txt` for the r4 grading and **is deliberately NOT differenced against `CD_f(96)` here**: the frozen reader that carries the planted control (rule 3) is the one that refused, and a difference computed by hand around it would be a number from a reader never shown able to see a non-zero. The item stays `PENDING`. | **638.9 core-min**, cap **800.0** (`cases/dafoam/ladder-a/A2/curriculum_D5/PREREGISTRATION.md` §4 line 153 @ `c5c25189`; basis 6.389 core-min per major from D4 PATCHED, 511.133 ÷ 80 majors, × 100 majors), P4 band **[450, 700]**. **$0.546259 DERIVED, NOT MEASURED** at $0.0513/core-h, reported-by-owner | **684.533 core-min MEASURED** from the run root `ledger.txt` (`wall_s=10268` × 4 ranks ÷ 60), `delivered_cores_mean 3.9954` of 4 over n=680 samples, `max_nr_throttled 42539`, MemAvailable 28.38 → 27.86 GiB, no OOM. **$0.585276 DERIVED, NOT MEASURED** | **684.533 — gross == cleaned, AND THE 3,600-SECOND FIGURE IS ADDRESSED RATHER THAN PASSED OVER.** This arm's 10,268 wall s is above that number, so the row says why it is not a stall: the rule is `scripts/self_audit.py::check_ledger_stalls` (`STALL_SECONDS = 3600.0`, `COMPUTE_BUDGET_CHARTER.md` §2), it runs over the demo-output mission ledger and identifies stalls by cross-family wall-time CLUSTERING that no per-solver cost mechanism explains — not over a DAFoam run-root `ledger.txt`, and not by threshold alone. Here the wall time is one container running 100 IPOPT majors with `Total CPU secs in NLP function evaluations = 9501.852` inside it, a measured per-major rate, and nothing shared with another family. **Naming the rule and showing it does not match is the point; a cleaned figure with an unnamed cleaning rule is a judgement wearing a number's clothes** | **1.0714** | **MISPREDICTION, MODEST AND IN THE HONEST DIRECTION, AND THE ANCHOR'S REGISTERED EXPOSURE WAS STATED ON THE WRONG SIDE.** Measured **6.845 core-min per major** against the registered **6.389** — the same 7.1 %, since both are 100-major runs, so the miss is entirely in the per-major rate and none of it in the major count. §4 line 154 registered the exposure as *"the anchor may under-price 192 DVs"*; it under-priced **48** DVs, on a DV count LOWER than the 96 the anchor was measured at. The FFD density is therefore not the whole rate driver, and the r4 registration should re-anchor from a measured 48-DV major rather than carry 6.389 forward for `O192`. Inside P4's registered band [450, 700] at 684.533, but at **97.8 % of the band's upper edge** — recorded now, because a 192-DV arm priced from the same anchor has ~2 % of headroom left before P4 misses. **Contention: measured and effectively absent** — 3.9954 of a 4-core dedicated quota, a 0.1 % shortfall, which cannot account for 7.1 % and is not claimed. **WASTE: 0.000 core-min ON THIS ROW, named separately** (`COMPUTE_BUDGET_CHARTER.md` §6). The 10.8 core-min of the r2 `ACC48` kill belongs to `C-139` and the 0.733 of `F48` to `C-164`; neither is absorbed here, and the 684.533 is not waste — it bought a completed 100-major optimisation whose artefacts are intact and re-gradeable the moment `O192` is bought | `cases/dafoam/ladder-a/A2/curriculum_D5/PREREGISTRATION.md` §4 (frozen `c5c25189`, Addendum 3 `b5b428bc`, Addendum 4 in the worktree at HEAD); comparator `d5_grade.py` md5 `c87c7a64657107dec4f5b7d1e634ffe6` — the Addendum-3 value at §A3 line 421, NOT §7 line 260's superseded `d50b55f4`; run root `/home/ubuntu/certonomous-runs/CURRICULUM-D5-a2-wing-ffd-density` — `ledger.txt`, `STATUS.chain`, `O48/opt_IPOPT.txt`, `O48/.d4_age_datum`; refusal reproduced by this lane at exit 2 |
| **C-184** | 2026-08-27 | heat-transfer | **T16 level `m` (`T16_MC_m`) — laminar developing MIXED CONVECTION in a vertical parallel-plate channel, `buoyantBoussinesqSimpleFoam`, serial, 102 400 cells, `endTime` 20 000. RAN TO COMPLETION AND IS `DONE` UNDER THE STRICT RULE; THE RUNG IS STILL `PENDING` AND NO ROW OF IT IS GRADED HERE.** Started 2026-08-27T17:42:55Z, ended 2026-08-27T21:26:20Z (`STATUS.T16_MC_m`, `note=clean`). Every limb of `CLAUDE.md` rule 4 re-derived by this lane from the artifacts, not relayed: `rc=0`; an `End` line; **20 000 `Time` lines == `endTime` 20 000**; the registered fields `T U p_rgh phi` present at `20000/`; `ExecutionTime` count == `endTime`; and every field at `endTime` NEWER than the case's own `0/T` (the age guard). The verdict was **not hand-judged**: the frozen marker `mark_done_t16.py` was run, and its worktree `git hash-object` was checked **in the same shell invocation** against `git rev-parse HEAD:<path>` — both `efcf78524dc4f853202cf24dd08c733d7742ac67`, so the D541 repair is demonstrably the code that ran and not the 350-line `PRE_D541` matcher (blob `2ae1605c`) that a stale-index `git restore` would have put back. It printed `DONE T16_MC_m` and wrote `DONE.T16_MC_m` at 21:28Z. **THE COMPARATOR THEN REFUSED, AND THE REFUSAL IS CORRECT BEHAVIOUR, NOT A DEFECT:** `analyse_t16.py` (worktree blob `9054d452b43ddda60c2bb187edc762d698a595ec` == its HEAD blob, hashed in the same invocation that ran it) printed *"REFUSE: no `DONE.T16_MC_f` — the whole rung is graded or none of it is."* `T16_MC_f` has never been launched; it sits pending in `verification/queue/heat-transfer/`. **No partial grade was forced and none may be**, so `T16` stays `PENDING` and this row costs a CASE CLOSED, not a rung graded. **PROCESS-GREPPED BEFORE WRITING, NOT MERELY ID-CHECKED** (the `C-179`/`C-170` duplicate this team minted today, withdrawn by `C-180`, was a correctly re-derived id with no process grep behind it): `MC_m` appears in **zero** rows of `git show HEAD:docs/COST_CALIBRATION.md`; `C-170` and `C-179` both cover level **`c`** and neither covers level `m`. | **132.888 core-min POINT**, registered **CEILING 200.499 core-min**, **CAP 300 core-min**, `timeout_s` 18 000, RANKS = 1 — `verification/runs/T-family/T16_runs/T16_registered.json`, `cases.T16_MC_m`, frozen with the pre-registration before compute. The POINT's basis is `cost.rate_point_core_s_per_cell_it` = **3.8932e-06 core-s per cell-iteration**, MEASURED on a scratch copy of this exact case outside the repository (`T16_PREREGISTRATION.md` §8); the CEILING's basis is **5.874e-06**, carried from **T4 level `c`** (kOmegaSST, same solver, `STATUS.T4_IJ_c`, `C-119`). **$0.113619 DERIVED, NOT MEASURED** at $0.0513/core-h, owner-stated (`COMPUTE_BUDGET_CHARTER.md` §5: the box cannot read its own billing); CAP = $0.256500 derived | **223.417 core-min MEASURED** from `STATUS.T16_MC_m` (`wall_s=13405` × `ranks=1` ÷ 60), corroborated by the solver's own `log.solve` tail: `ClockTime = 13405 s`, `ExecutionTime = 13288 s`. **$0.191022 DERIVED, NOT MEASURED** | **223.417 — gross == cleaned, AND THE 3 600-SECOND FIGURE IS ADDRESSED RATHER THAN PASSED OVER.** The wall 13 405 s is above that number, so the row names the rule and shows it does not match: `scripts/self_audit.py::check_ledger_stalls` (`STALL_SECONDS = 3600.0`, `COMPUTE_BUDGET_CHARTER.md` §2) identifies stalls by cross-family wall-time CLUSTERING that no per-solver cost mechanism explains — not by threshold alone. Here **13 288 s of the 13 405 s wall (99.13 %) is solver-resident `ExecutionTime`**, 20 000 SIMPLE iterations on 102 400 cells at a measured, near-constant per-iteration rate, with 117 s of start-up and field I/O. There is nothing to clean | **1.681×** cleaned/predicted (223.417 / 132.888). Against the registered CEILING: **1.114×** (223.417 / 200.499) — **the ceiling rate borrowed from T4 was itself exceeded.** Against the CAP: **0.745×** (223.417 / 300). **NO OVERRUN: `capped=no`, wall 13 405 s of the registered `timeout_s` 18 000; the run stopped at `endTime`, not at its cap, and no new budget was sought or taken** | **MISPREDICTION, UNFAVOURABLE DIRECTION, AND THE MECHANISM IS MEASURED RATHER THAN GUESSED: THE REGISTRATION PRICED ALL THREE LEVELS FROM ONE PER-CELL-ITERATION RATE, AND THAT RATE IS NOT MESH-INDEPENDENT.** Measured from the two levels' own logs, on the `ExecutionTime` basis: level `c` **2.8890e-06 core-s per cell-iteration** (739.58 s ÷ (25 600 × 10 000)), level `m` **6.4883e-06** (13 288 s ÷ (102 400 × 20 000)). The rate **rose 2.246×** for a 4× cell count. Against the single registered POINT rate 3.8932e-06 that both levels were priced from, `c` came in at **0.742×** (which is why `C-170` reads 0.747× and looked like a conservative estimate) and `m` at **1.667×** — the same registration, the same rate, opposite errors, because the rate itself scales. Two points fit **rate ∝ cells^0.584**. **CONTENTION: MEASURED AS PRESENT AND DELIBERATELY NOT CLAIMED, because it is common-mode and cannot explain the difference.** `T3_R_ff` (8 ranks, running since 2026-08-26T20:51:28Z) and `T15_UP_f` (1 rank, since 2026-08-27T08:57:38Z) were BOTH live across the whole of level `c` (17:30:06–17:42:30Z) and the whole of level `m` (17:42:55–21:26:20Z) — the two levels ran back to back under an identical 10-of-16-core load, so contention is charged to neither and explains none of the 2.246×. **WASTE: 0.000 core-min, named rather than omitted** (`COMPUTE_BUDGET_CHARTER.md` §6). Nothing was re-fired, nothing abandoned, nothing truncated: the run met every limb of the strict rule and its fields are on disk and gradeable the moment `MC_f` lands. **THE CALIBRATION LESSON, AND IT IS ACTIONABLE BEFORE THE NEXT SPEND RATHER THAN AFTER IT: a per-cell-iteration rate measured at ONE mesh level is not a rate, it is a rate AT THAT LEVEL, and a three-level Roache ladder priced from one such measurement will under-price its refined levels systematically.** Registering the rate at the COARSE level and the CAP from a single flat multiple of it is what put `m` above its own ceiling. **FORWARD PROJECTION FOR `T16_MC_f`, LABELLED AN ESTIMATE FROM A TWO-POINT FIT AND NOT A MEASUREMENT — it is recorded here because `MC_f` is queued and unlaunched, so the number can still change a decision:** `MC_f` is registered at POINT **1063.103 core-min**, CEILING **1603.994**, CAP **2400**, `timeout_s` 144 000, from the same 3.8932e-06 rate on 409 600 cells × 40 000 iterations. If the rate stops growing and holds at `m`'s measured 6.4883e-06, `MC_f` costs **≈1 772 core-min** — inside the 2 400 CAP but **above its 1 604 ceiling**. If the measured `c`→`m` scaling continues one more 4× step (rate ≈1.457e-05), `MC_f` costs **≈3 979 core-min**, the CAP and the 144 000-s timeout stop it at roughly **60 % of `endTime`**, and under rule 5 clause (1) the level is `NOT A RESULT` on a registered cap — which would leave `T16` ungradeable, since its comparator grades the whole rung or none of it. **Neither figure is a measurement and neither authorises a cap change: a cap is not re-opened to fit a projection.** Referred to the heat-transfer supervisor as a pre-compute finding | `verification/runs/T-family/T16_runs/STATUS.T16_MC_m`, `T16_MC_m/log.solve`, `DONE.T16_MC_m`, `T16_registered.json` (`cases.T16_MC_m`, `cost`); marker `mark_done_t16.py` blob `efcf7852` (hash-checked against HEAD in the invocation that ran it); comparator `analyse_t16.py` blob `9054d452`, refusal reproduced by this lane; level-`c` comparison from `T16_MC_c/log.solve` and `STATUS.T16_MC_c` (`C-170`). **`docs/campaigns/T-family/T16_RESULTS.md` does NOT yet carry this level** — that file was last written 18:44Z, before `MC_m` ended, and the rung is `PENDING`; this row cites the run artifacts directly and says so rather than citing a results file that does not contain the figures |
| **C-203** | 2026-08-27 | ansys-verification | ~~formerly C-182~~ **STRUCK AND RENUMBERED to C-203** (verification ruled by LANDING ORDER; dafoam's C-182 landed first at `405c5775`, this row at `5d458096`). No cost figure is disputed and none moves. **VMFLGPU007** — GPU solver path, turbulent flow with heat transfer over a backward-facing step (VM2026R1 p. 243); six solves (L1/L2/L3 x GPU arm + forced-CPU control), completed 20:52:03Z, graded and registered as row #40 `NOT A RESULT` (comparator refused at the plateau clause `IG3`). **A refusal still spends compute, so it still owes a calibration row** (CLAUDE.md rule 12). | **0.70 GPU-h** and **55 core-min**, frozen in PREREGISTRATION §9 before compute. Basis stated there and not a guess: VMFLGPU002's measured per-outer-iteration model `t = 0.0945 + 5.99e-6·cells` s on the GPU arm with the forced-CPU arm assumed at **1.31x** the GPU time, applied to this case's cell counts (3 648 / 7 776 / 16 128) and endTimes (1200 / 1800 / 3000). Caps 1.5 GPU-h and 90 core-min. | **0.306944 GPU-h**; total wall **1105 s**; per-arm wall 90 / 20 / 187 / 68 / 449 / 286 s (gpu-L1, cpu-L1, gpu-L2, cpu-L2, gpu-L3, cpu-L3). **CPU arm 6.2333 core-min** at ranks = 1. | **Same as gross — cleaned == gross.** No stall (no arm approached the 3600 wall-s stall threshold; the longest is 449 s), no re-run, no truncation, **no cap fired on any arm by either mechanism** (1.5 GPU-h cap 20.5 % used; 90 core-min cap 6.9 % used). | **0.4385x** on GPU-h (0.306944 / 0.70) and **0.1133x** on CPU core-min (6.2333 / 55) — **both large OVER-estimates**, i.e. the case cost far less than registered. | **MISPREDICTION, and specifically an INVALID TRANSFER OF A COST MODEL BETWEEN CASES — not contention and not waste.** The estimate took VMFLGPU002's per-iteration model, measured on `simpleFoam` solving an isothermal tee-junction, and applied it to `buoyantSimpleFoam` solving a coupled velocity-pressure-temperature system on a different mesh topology. Two independent errors compounded: **(i) magnitude** — the per-iteration cost was over-estimated across the board; **(ii) DIRECTION OF THE GPU/CPU RATIO, which was wrong, not merely inaccurate.** The model assumed the forced-CPU arm would take **1.31x** the GPU arm's time; measured, **the CPU arms were FASTER than the GPU arms at every level** (20 vs 90, 68 vs 187, 286 vs 449 s), i.e. a ratio near **0.6x**, in the opposite direction. That single inverted assumption accounts for most of the 0.11x CPU ratio. This corroborates row #39's finding that **at these mesh sizes the GPU path is a correctness vehicle, not a speedup** — transfer overhead dominates. **Lesson for the next estimate: a per-iteration cost model is specific to a solver AND a mesh family and may not be carried across either boundary; and the GPU/CPU ratio must be measured per case, never assumed, and must not be assumed to favour the GPU at small mesh sizes.** **WASTE for this run is 0.000 and is named separately per `COMPUTE_BUDGET_CHARTER.md` §6:** GPU idle time after the run completed is an infrastructure figure boarded on its own and is **NOT** folded into either ratio above. **Dollars are DERIVED, NOT MEASURED** — $0.2470 GPU (0.306944 x $0.8048/GPU-h, g6.xlarge us-east-2 published list) + $0.0053 CPU (6.2333 core-min x $0.0513/core-h) = **$0.2523**, against a registered estimate of $0.6104. The box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` §5) and **the console figure is still owed and supersedes.** | `cases/ansys_verification/VMFLGPU007/RESULTS.md` §7; register row **#40**; artifacts `verification/runs/ansys_verification/VMFLGPU007/COST.txt` and `RUN_RC.{L1,L2,L3}.{cpu,gpu}` |
| **C-185** | 2026-08-27 | heat-transfer | **CORRECTION ROW, naming the row it corrects: `C-184` (T16 level `m`, `T16_MC_m`). `C-184` IS NOT WITHDRAWN — its figures, its ratio and its rate finding all stand and are re-derived unchanged here. TWO THINGS ARE ADDED, AND ONE PHRASE IN IT IS TOO BARE TO STAND ALONE.** (1) `C-184`'s ratio column reads **"NO OVERRUN"** without a qualifier. That is TRUE OF THE CAP and FALSE OF THE ESTIMATE: an `ESTIMATE_OVERRUN.txt` was written for this case at **2026-08-27T20:09:05Z** — `verification/runs/T-family/T16_runs/T16_MC_m/ESTIMATE_OVERRUN.txt`, 457 bytes — recording elapsed **8 774 s against 1.10 × the estimate 7 973 s** (7 973 s = the 132.888 core-min POINT at 1 rank). `C-184` did not cite it. The corrected reading: **the ESTIMATE was overrun and the monitor said so 1 h 17 min before the run ended; the CAP was not.** The run continued a further **4 631 s (77.2 core-min)** after the monitor fired and stopped at `endTime`, not at a cap. **THAT IS CHARTER-CORRECT, NOT A VIOLATION, AND IT IS NOT WASTE:** the artifact's own words are *"ESTIMATE OVERRUN REPORTED, NOT ENFORCED — THIS IS AN ESTIMATE OVERRUN, NOT A CAP"*, and rule 12's *"an overrun stops the run"* binds the CAP. An estimate exceeded is **misprediction — the very quantity this ledger exists to capture** — and it is already carried, in full, by `C-184`'s **1.681×** ratio and its measured rate mechanism. It is NOT re-attributed as waste here, and **`C-184`'s WASTE: 0.000 core-min STANDS UNCHANGED.** Naming a legitimate misprediction "waste" would corrupt both columns at once. (2) **A BOOKKEEPING DEFECT `C-184` MISSED, AND IT IS THE REASON THE MONITOR COULD ONLY SEE AN ESTIMATE: the queue entry carries the cap in PROSE BUT NOT AS A FIELD.** `verification/queue/heat-transfer/launched/T16_MC_m.json` carries `cost_core_min_estimate: 132.888` and **no `cap_core_min_registered` key at all** (grepped: 0 occurrences), while its own `cost_basis` string states *"cap 300 core-min = timeout 18000 s, the wrapper's stop (rule 12, an overrun stops the run and does not get a new budget)"*. So the runner's monitor, reading fields, had only an estimate to compare against and correctly emitted the estimate class; it was not blind through fault, it was blind through a missing key. **THE CAP WAS NEVERTHELESS LIVE AND ENFORCEABLE IN THE OTHER FRAME:** the launcher argv is `--timeout 18000 --ranks 1`, and **18 000 s × 1 rank ÷ 60 = 300.000 core-min = EXACTLY the registered `cap_core_min` of `T16_registered.json` `cases.T16_MC_m`**. The wrapper would have stopped the run at the cap to the second. Two frames, one of them field-blind, the enforcing one intact — and the run finished at 13 405 s, 74.5 % of the way to that stop, so nothing was ever near it. **CLASSIFIED UNDER THE ENTRY'S OWN REGISTERED RULE, NOT UNDER A JUDGEMENT MADE AFTER THE FACT:** `_field_classes` in that same entry lists `CAP_OVERRUN.txt`, `ESTIMATE_OVERRUN.txt` and `cost_core_min_estimate` as **INFRASTRUCTURE**, with the rule *"a missing or inconsistent INFRASTRUCTURE field is a BOOKKEEPING DEFECT reported beside the verdict and voids only the cost claim; only a PHYSICS_CRITICAL field may produce NOT A RESULT (L-342)"* — Sanaa's universal rule of 2026-08-26, bookkeeping never voids physics. **`T16_MC_m`'s completion is untouched by this row: rc=0, `End`, 20 000 `Time` lines == `endTime`, fields at `20000/`, age guard held. `DONE` stands.** The cost claim is not voided either — it is `MEASURED` from `STATUS.T16_MC_m` and the solver's own log, neither of which depends on the missing key. **REPAIR IS NOT ATTEMPTED HERE AND THE MISSING KEY IS NOT BACK-FILLED:** the entry is a launched artifact of a completed run, editing it after compute would rewrite the record of what actually ran, and the enqueue path belongs to whoever writes queue entries. Referred, not repaired | **UNCHANGED FROM `C-184`:** 132.888 core-min POINT / 200.499 CEILING / 300 CAP, ranks 1, `T16_registered.json` `cases.T16_MC_m`. **$0.113619 DERIVED, NOT MEASURED** at $0.0513/core-h, owner-stated (`COMPUTE_BUDGET_CHARTER.md` §5) | **UNCHANGED: 223.417 core-min MEASURED** (`STATUS.T16_MC_m` `wall_s=13405` × 1 ÷ 60; `log.solve` `ClockTime 13405 s`, `ExecutionTime 13288 s`). **$0.191022 DERIVED, NOT MEASURED.** The monitor's 8 774 s is a MID-RUN SAMPLE at 20:09:05Z, not a completion figure, and is not a competing actual | **UNCHANGED: 223.417 — gross == cleaned.** The 20:09Z monitor record adds nothing to clean: it reports on a run that then completed normally, and no wall-time was abandoned, re-fired or truncated | **UNCHANGED: 1.681×** of POINT, **1.114×** of CEILING, **0.745×** of CAP. **The phrase `C-184` should have used, and the one that governs: NO CAP OVERRUN — `capped=no`, wall 13 405 s of the enforcing `--timeout 18000`. THE ESTIMATE WAS OVERRUN BY 1.681× AND THE MONITOR RECORDED IT AT 20:09:05Z** | **UNCHANGED IN SUBSTANCE — misprediction, mechanism measured (rate 2.8890e-06 core-s/cell-it at `c` vs 6.4883e-06 at `m`, a 2.246× rise for a 4× cell count against one flat registered rate of 3.8932e-06). CONTENTION still measured-as-present and still NOT CLAIMED (common-mode: `T3_R_ff` and `T15_UP_f` spanned both levels, which ran back to back). WASTE STILL 0.000 core-min, NAMED** (`COMPUTE_BUDGET_CHARTER.md` §6). **ADDED BY THIS ROW: a BOOKKEEPING DEFECT, named separately from all three and absorbed into none of them** — the queue entry's missing `cap_core_min_registered` key. It changes no figure in this row, no verdict, and no core-minute; it explains only why the monitor emitted the estimate class rather than the cap class, and it is recorded so the next reader does not mistake the monitor's silence on the cap for evidence that no cap was registered. **A CAUTION THIS ROW REGISTERS FOR EVERY READER OF THIS LEDGER, BECAUSE IT COST THIS LANE A CHECK TODAY: A MONITOR ARTIFACT'S LABEL IS NOT AN AUTHORITY ON WHAT IT COMPARED AGAINST — THE PRE-REGISTRATION IS.** For `T16_MC_m` the two agree. Elsewhere in the T-family they do not, and a row built on the label rather than the registration would assert a cap breach that never happened. Reported to the heat-transfer supervisor with the per-case arithmetic; no row of another rung is corrected here, because they are not this lane's rows | `verification/runs/T-family/T16_runs/T16_MC_m/ESTIMATE_OVERRUN.txt` (20:09:05Z); `verification/queue/heat-transfer/launched/T16_MC_m.json` (`cost_core_min_estimate`, `cost_basis`, `_field_classes`, `launch_cmd` `--timeout 18000 --ranks 1`); `T16_registered.json` `cases.T16_MC_m` (`cap_core_min` 300); `STATUS.T16_MC_m`; `DONE.T16_MC_m`; artifacts landed at `1bb24c51`, corrected row `C-184` landed at `87013378` |
| **C-186** | 2026-08-27 | cfd | **CORRECTION ROW, naming the six rows it corrects: `C-126` (F17_KV40), `C-127` (F16b_SL2), `C-128` (F18_TG2D), `C-134` (F19_SOD), `C-136` (F20_ISENTROPIC_VORTEX), `C-142` (F17b_KV40_EXT). NOT ONE OF THE SIX IS WITHDRAWN, AND THIS ROW CORRECTS NO ARITHMETIC IN ANY OF THEM** — all six solver-only actuals were INDEPENDENTLY RE-DERIVED here from the last `ExecutionTime ... ClockTime` line of every level log x 1 rank / 60 and reproduce their landed figures EXACTLY (1.2500 / 1.2000 / 26.8667 / 4.4167 / 0.1000 / 68.6667 core-min). **WHAT THIS ROW ADDS IS A SECOND COST BASIS FOR THE SAME SIX RUNS, AND THE READING THAT ONLY THE SECOND BASIS MAKES VISIBLE.** The six landed rows charge **solver ClockTime only**; this row reports the same six on a **launch-to-STATUS wall** basis, which additionally captures mesh generation, build and post-processing overhead (0.033-1.967 core-min per case). **BOTH ARE TRUE ABOUT DIFFERENT QUANTITIES** — neither basis supersedes the other, and a reader must take the basis with the number. **DIRECTION FLIP, FLAGGED ON ITS FACE: `F19_SOD` reads 0.50 solver-only (`C-134`, under-ran) and 1.500 gross (over-ran).** A bare 1.500 standing beside `C-134` without its basis named would make this ledger self-contradictory; that is precisely why this is ONE row naming all six and not six fresh rows. **WHY THE ROW EXISTS AT ALL — A FALSE ABSENCE, THE THIRD IN THIS TEAM IN ONE DAY.** This correction was commissioned as six NEW rows on a lane's confident, artifact-cited report that all six were ABSENT from this ledger at HEAD. They were not: all six stand at HEAD and are findable under BOTH spellings (rows title cases with hyphens, `F17-KV40`, while every record-ref cell carries the underscore path, `F19_SOD_RESULTS.md`), so no reasonable grep misses them. Six duplicate cost records in an APPEND-ONLY ledger are irreversible. **A SEARCH THAT RETURNS NOTHING HAS TOLD YOU NOTHING UNTIL YOU HAVE SHOWN THE SEARCH COULD HAVE FOUND SOMETHING** — this is standing rule 3's planted-zero principle, which the lab enforces rigorously on comparators and had not been applying to its own greps. **READER'S NOTE ON `C-127` (F16b):** its cost is corroborated by the grader's own independent computation, but that figure lives under the key `infrastructure_census.total_core_min` (1.25, broken out 0.11667 / 0.25 / 0.88333), **NOT** under the `cost_claim` key the other five use. It was nearly recorded here as uncorroborated because the check looked for the usual KEY rather than for the THING. Look for the quantity, not the key. | **Unchanged from the six rows; re-read here from each launched queue entry's `cost_core_min_estimate` and agreeing to the digit with each frozen prereg section 8:** 0.5 (F16b, `cadb4887`) / 7.6 (F17, `4ad083fb`) / 32.1 (F17b, `61b47973`) / 2.5 (F18, `c4f72b27`) / 0.2 (F19, `3053d9ec`) / 60.0 (F20, `548fc02e`) core-min. **AGGREGATE 102.9 core-min — labelled an AGGREGATE per `C-68`; the calibration figures of this row are the SIX PER-RUN ratios, never this sum.** | **THE BASIS THIS ROW ADDS — launch-to-STATUS wall x 1 rank / 60, MEASURED:** 1.283 (F16b) / 2.617 (F17) / 28.833 (F17b) / 5.933 (F18) / 0.300 (F19) / 70.000 (F20) core-min. Each is the elapsed between the case's queue-entry `_launch.utc` and the `end=` stamp of its own `STATUS.<case>` file (77 / 157 / 1,730 / 356 / 18 / 4,200 wall s), both artifacts on disk and cited in the ref column. **AGGREGATE 108.967 core-min = $0.09317 DERIVED, NOT MEASURED** at $0.0513/core-h, c7a.4xlarge, reported-by-owner — the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` section 5). For comparison the six landed rows' solver-only basis totals 102.5000 core-min = $0.08764 derived; the whole difference, 6.467 core-min, is non-solver overhead. **`_launch.status_seen_utc` reads 2026-08-26T22:17:01Z for all six and is a queue-sweep POLL time, NOT a completion time — it must never be used to compute elapsed.** | **cleaned == gross on this basis in all six, and WASTE = 0.000 core-min in all six — VERIFIED, NOT ASSUMED** (no kill, timeout, relaunch, abandoned level or `FOAM FATAL` under any of the six run roots; every level carries `End` and rc 0; the only keyword hit was prose inside F20's own `RESULTS.md`). Waste is named separately here per `COMPUTE_BUDGET_CHARTER` section 6 and is charged into NO ratio. F20's 4,200 launch-to-STATUS wall s exceeds the 3,600-s stall figure by the letter, but it is three levels of registered work run to `End` (ExecutionTime 44.36 / 376.08 / 3,697.95 s), not a stall — carried gross exactly as `C-136` ruled on the solver basis and `C-133` before it. **CONTENTION: `NOT MEASURED`.** No `WATCH_LOG`, sampler or load artifact exists under any of the six run roots. The launcher's free-core probe is a single instantaneous reading at launch, not a sample over the run, so it cannot bound contention. **We do not bound what we cannot sample**; an estimated figure here would be the fabricated measurement `COMPUTE_BUDGET_CHARTER` section 5 exists to forbid. | **PER-RUN gross/predicted — THESE SIX ARE THE CALIBRATION FIGURES:** F16b **2.567** / F17 **0.344** / F17b **0.898** / F18 **2.373** / F19 **1.500** / F20 **1.167**. (Solver-only, as landed: 2.50 / 0.158 / 0.837 / 1.767 / 0.50 / 1.144.) **AGGREGATE 1.059 gross/predicted — EXPLICITLY LABELLED AN AGGREGATE per `C-68` AND NOT A CALIBRATION FIGURE:** it averages a 2.9x over-prediction against a 2.4x under-prediction and lands near 1.0, which would misreport a demonstrably mis-calibrated estimator as accurate. **NO CAP WAS BREACHED BY ANY OF THE SIX, on either basis** — gross against registered cap: 25.7 % of 5.0 (F16b) / 6.5 % of 40.0 (F17) / 19.2 % of 150.0 (F17b) / 19.8 % of 30.0 (F18) / 6.0 % of 5.0 (F19) / 29.2 % of 240.0 (F20). The largest consumption of any cap by any of the six is **29.2 %**. | **THE FINDING: cfd's cost estimator is CALIBRATED ABOVE ~25 core-min AND UNCALIBRATED BELOW IT.** The two large runs are the accurate ones — F17b **0.898** and F20 **1.167**, both within 17 % — while EVERY gross miss is on a run under 6 core-min. The mechanism is fixed, size-independent overhead: gross minus solver is 0.033-1.967 core-min per case (mesh generation, build, post-processing), which is a rounding error against a 32- or 60-core-min estimate and a LARGE FRACTION of a 0.2- or 0.5-core-min one. F19 is the extreme: 0.200 of its 0.300 gross core-min — two thirds — is not solver at all. **THE MISPREDICTION IS BIDIRECTIONAL, WHICH RULES OUT A SIMPLE SCALE ERROR IN THE ESTIMATOR:** F17 OVER-predicted by 2.9x (a turbulent-RANS rate basis applied to a laminar case) while F16b and F18 UNDER-predicted by ~2.4x (a basis measured on a degenerate zero-field run; and PCG iteration growth with N). A single correction factor cannot fix both directions; the fault is in the RATE BASIS CHOSEN PER CASE, not in a global scale. **CARRY FORWARD: (1) price a run under ~10 core-min with an explicit fixed-overhead term (~0.03-2 core-min on this box) added to the solver estimate, because below that scale the overhead dominates the ratio; (2) state the physics regime and the field state of the record a rate basis is taken from — three of these six missed because the basis came from a case unlike the one being priced; (3) apply the planted-zero principle to SEARCHES, not only to comparators.** **INFRASTRUCTURE, POINTED AT AND NOT RE-LITIGATED:** the `CAP_OVERRUN.txt` files under F16b, F18 and F20 are **ESTIMATE flags at the old 1.10 x ESTIMATE trigger, not cap breaches**, as `C-128` and `C-136` already record. They are now **FOSSILS**: the field-selection defect that named an estimate a cap is FIXED at HEAD by `117bf190` (cap watch keys on `cap_core_min_registered`, `CAP_OVERRUN.txt` only at 1.00 x cap, `ESTIMATE_OVERRUN.txt` at 1.10 x estimate when no cap is registered) and `257d1116` (a launched record keyed on case id alone judged a re-armed cwd). Both verified ancestors of HEAD and verified against the current `scripts/queue_runner.py`. **The three files on disk predate the fix and will not regenerate.** | Six corrected rows `C-126`, `C-127`, `C-128`, `C-134`, `C-136`, `C-142`, landed at `f018c8bf`, `49c95cc7`, `3f87e759`, `08aa454c`, `ca6a3164`, `f8d2d60c`. Solver logs re-derived here: `verification/runs/{F16b_runs,F17_runs,F17b_runs,F18_runs,F19_SOD_runs,F20_ISENTROPIC_VORTEX_runs}/{coarse,medium,fine}/log.*`. Gross basis: `verification/queue/cfd/launched/{F16b_SL2,F17_KV40,F17b_KV40_EXT,F18_TG2D,F19_SOD,F20_ISENTROPIC_VORTEX}.json` field `_launch.utc`, against `cases/<case>/STATUS.<case>` field `end=`. Grader cost corroboration: `F17_GRADED.json`, `F17b_GRADED.json`, `F18_GRADED.json`, `F19_GRADED.json`, `F20_GRADED.json` key `cost_claim.core_min_claim`; `F16b_GRADED.json` key `infrastructure_census.total_core_min`. Runner tallies: `cases/<case>/launcher.queue.out`. Estimate-flag fossils: `cases/{F16b_stokes_second_problem,F18_taylor_green,F20_ISENTROPIC_VORTEX}/CAP_OVERRUN.txt`; fix at `117bf190`, `257d1116`. |
| **C-187** | 2026-08-27 | heat-transfer | **CORRECTION ROW — it corrects the COMMIT MESSAGE of `6cce8a64`, not its content** (this file's append rule 1: an existing row is never edited and a correction is a new row naming what it corrects; the same discipline is applied here to a commit message, which cannot be edited at all). **`6cce8a64` landed 8 budget-overrun records and its message says, STRUCK: "three of them record CAP overruns that were reported and NOT enforced."** **THAT CHARACTERISATION IS FALSE. NONE of the three breached a registered cap.** **THE 8 RECORD FILES THEMSELVES ARE CORRECT, ARE NOT EDITED, AND REMAIN WORTH HAVING AT HEAD** — they are the runner's own contemporaneous text and their whole evidentiary value is that they are unaltered. **THE MECHANISM, stated so a reader can check rather than trust:** the `CAP_OVERRUN.txt` strings quote "1.10 x registered 710 s (11.84 core-min / 1 ranks)" and "4479 s (74.65 core-min / 1 ranks)", but **11.84 and 74.65 are the `point_core_min` ESTIMATES**, while the **registered caps in `T4b_registered.json` are 25 and 150**. The monitor **read the estimate field and labelled it a cap, manufacturing a cap breach out of an estimate overrun.** **AND ALL THREE FILES ARE FOSSILS OF ALREADY-FIXED DEFECTS:** `117bf190` (2026-08-26 20:52:53Z) made the cap watch key on `cap_core_min_registered` else emit `ESTIMATE_OVERRUN.txt` at 1.10x estimate named as an estimate, and `257d1116` (2026-08-26 22:15:22Z) fixed the re-armed-cwd artifact, naming our case explicitly. **The on-disk files were written by code that no longer exists. `117bf190` and `257d1116` MUST NOT BE RE-FIXED.** **WHOSE ERROR: the heat-transfer supervisor's, stated plainly at the supervisor's own direction.** The current code and the historical artifacts were read and **treated as one thing** — the "two things checked as one" class this family has been finding in others all day, committed by this family. **THE GRADING LANE REFUSED an instruction to write three waste rows and WAS RIGHT TO REFUSE:** had it obeyed, this ledger would now carry **a false governance failure plus three legitimate mispredictions rebranded as waste**, and waste is separately named under charter §6 precisely so it cannot absorb a misprediction. | **`T4b_IJ_c` CAP 25 core-min, POINT 11.84; `T4b_IJ_m` CAP 150 core-min, POINT 74.65** (`verification/runs/T-family/T4b_runs/T4b_registered.json`, read at this row's authoring). **`T5_CUBE_c` 45.6 core-min ESTIMATE** (`T5_PREREGISTRATION.md` AMENDMENT 2, Model B §11.1, as carried by C-140 and corrected by C-148) — **an estimate, never a registered cap** | **`T4b_IJ_c` 14.100 core-min** (846 wall s x 1 rank, `T4b_runs/STATUS.T4b_IJ_c`, rc=0 `capped=no` note=clean); **`T4b_IJ_m` 86.300 core-min** (5,178 wall s x 1 rank, `T4b_runs/STATUS.T4b_IJ_m`, rc=0 `capped=no` note=clean); **`T5_CUBE_c` 16.083 core-min** (965 wall s x 1 rank, `T5_runs/STATUS.T5_CUBE_c`, rc=0 `capped=0` note=clean) | **gross = cleaned for all three; no row is over the 3600 wall-s stall figure.** The **11,433 s** in `T5_CUBE_c`'s flag was **wall-clock spanning a CRASHED earlier attempt**, not solver time — `verification/runs/T-family/T5_runs/T5_CUBE_c/crash_1741Z/STATUS.T5_CUBE_c` is on disk carrying rc=1, wall_s=0, `note=SOLVER_NONZERO_EXIT`. **WASTE: named separately per charter §6 and NOT laundered into any column here** | **AGAINST THE REGISTERED CAP, which is the figure the struck wording claimed was breached: `T4b_IJ_c` 14.100 / 25 = 0.564x; `T4b_IJ_m` 86.300 / 150 = 0.575x. BOTH ABOUT HALF THEIR CAP.** Against POINT: 1.191x and 1.156x — **ESTIMATE overruns, the class the runner now names correctly.** **`T5_CUBE_c` 16.083 / 45.6 = 0.353x — AN UNDERSPEND, not an overrun of any kind** | **Rate misprediction on two coarse/medium T4b levels, conservative in the safe direction, plus one underspend.** No governance failure occurred in these three cases and none is claimed. **THE LIVE DEFECT NAMED IN THE BRIEF WAS STRUCK WHILE THIS ROW WAS BEING WRITTEN, and this row reports that rather than the brief:** cfd landed **`d8d347c3` at 2026-08-27T21:45:11Z**, "MISCITATION STRUCK in the shared queue runner", and the string "The run was NOT killed (caps report; COMPUTE_BUDGET_CHARTER)" **is no longer present in `scripts/queue_runner.py`** (verified by content grep at 2026-08-27T21:46:48Z, zero matches). **What is FIXED is the false attribution; what REMAINS is the BEHAVIOUR** — the runner still does not kill on a cap — but it now says so honestly, calling non-enforcement "A PROPERTY OF THIS RUNNER AS BUILT, NOT A PERMISSION ANY CHARTER GRANTS" and citing `COMPUTE_BUDGET_CHARTER.md:197` for the opposite, with enforcement pre-registered at `docs/standards/RUNNER_CAP_ENFORCEMENT_CLAUSE.md` (D539). **`queue_runner.py` is cfd's and is untouched by heat-transfer.** **UNSETTLED AND NOT TO BE ASSERTED EITHER WAY: whether ANY T-family case has ever exceeded a registered cap.** The census is running; **neither the claim nor its denial is made here**, and this row settles only these three cases | `6cce8a64` (the commit whose MESSAGE this row corrects; its 8 files stand unaltered); `verification/runs/T-family/T4b_runs/T4b_registered.json`, `STATUS.T4b_IJ_c`, `STATUS.T4b_IJ_m`, `T4b_IJ_c/CAP_OVERRUN.txt`, `T4b_IJ_m/CAP_OVERRUN.txt`; `verification/runs/T-family/T5_runs/STATUS.T5_CUBE_c`, `T5_CUBE_c/CAP_OVERRUN.txt`, `T5_CUBE_c/crash_1741Z/STATUS.T5_CUBE_c`; fix commits `117bf190`, `257d1116`, and the strike `d8d347c3`; beside C-140 as corrected by C-148 (the same `T5_CUBE_c` 45.6 estimate and 0.353 ratio). Figures re-verified at source by the drafting lane against the committed records, not taken from the brief; the `d8d347c3` strike is the lane's own finding and departs from the brief, which described the miscitation as live. Ruled by the heat-transfer supervisor `[lab-attributed]`, who directed that the correction run against itself |
| C-188 | 2026-08-27 | dafoam | **D6 `O_mp`** (ladder-a A2, `curriculum_D6`) — 3D multipoint wing shape optimisation, three scenarios cl04/cl05/cl06 at np=4 in a 20g cgroup, PATCHED row (`dafoam-idwarp-rot:v1`). **The arm was stopped by its own registered deadline** (`timeout -k 60 30000` inside the container, `d6_run_arm.sh:347`) at 2026-08-27T22:29:33Z after 8 h 20 m, `rc=124`, `OOMKilled=false`. **The chain stopped at the first non-zero rc** (`d6_chain_driver.sh:128`), so `ACC_mp`, `F_mp` and `REF_off` never ran and spent **0 core-min**. **The registered grading path REFUSED and produced no verdict** — `d6_grade.py` (md5 `a76a7d5e298a6ba719143f14e10c89bf`, verified equal to its committed blob at the freeze commit `3090e858` in the grading invocation) exits **rc=2** with `REFUSE G1 arm_absent_from_ledger ACC_mp, kernel_record_candidates []`. **A refusal still spends compute, so it still owes this row** (rule 12; the `C-182` precedent). | **1,533.4 core-min** for `O_mp`, frozen in `PREREGISTRATION.md:95` §4 before compute and re-stated in the grader's own `PREDICTED_CORE_MIN` at `d6_grade.py:52`. Basis, not a guess: D4-PATCHED's measured anchor **6.389 core-min/major × 3 scenarios × 80 majors** (the P5 point; the 100-major reading is 1,916.7). Cap 2,000.0; item ceiling 2,230.0; P5 band **[1,200, 2,000]** core-min. | **2,000.533 core-min**, basis **gross**, read from `ledger.txt` (`ARM=O_mp … wall_s=30008 ranks=4 core_min=2000.533`). Wall **30,008 s** at **4** declared ranks. Item total is the same 2,000.533 — the other three arms bought nothing. | **2,000.533 — gross == cleaned, AND THE 3,600-SECOND FIGURE IS ADDRESSED RATHER THAN PASSED OVER.** The 30,008 s wall is far above that number, so the row names the rule and shows it does not match: `scripts/self_audit.py::check_ledger_stalls` (`STALL_SECONDS = 3600.0`, `COMPUTE_BUDGET_CHARTER.md` §2) identifies a stall by cross-family wall-time **clustering** that no per-solver cost mechanism explains — its own docstring says *"a stall is identified, not assumed"* — never by the threshold alone. Here **the solver's own in-container clock runs 0.07 s → 29,810.33 s, i.e. 29,810 s of the 30,008 s wall (99.34 %) is solver-resident**, 64 IPOPT majors of three primals plus three adjoints each at a near-constant per-major rate. There is nothing to clean. **No compute is named as waste under §6:** the spend bought P7's answer and the per-major rate below, and it bought no graded gate. | **1.3046×** gross/predicted (2,000.533 / 1,533.4). **THAT RATIO UNDERSTATES THE MISPREDICTION AND MUST NOT BE READ ALONE, because the run was TRUNCATED BY ITS CAP at 64 majors, not run to the predicted 80.** The calibrating figure is the **per-major rate: 31.258 core-min/major measured** (2,000.533 / 64 majors, majors 1–64 after the initial point; 7 of them restoration) **against 19.167 registered** (6.389 × 3) — **1.6308×**. At the measured rate the registered 80-major point would have cost **2,500.7 core-min**, which is **1.63× the prediction and 25 % ABOVE the 2,000.0 cap**: the cap could never have been met at this rate, and that is the fact the estimate needed. Against the cap: **1.000267×** — see the gap column, this 0.533 core-min is a measurement-frame artefact, not an overrun of the run. | **MISPREDICTION OF THE ×3 MULTIPOINT SCALING, essentially in full.** The registration assumed three independent adjoints per major with no shared-primal saving and priced them at 3 × D4-PATCHED's single-point 6.389 core-min/major; the measured multipoint major costs **31.258**, i.e. the ×3 model is short by **63 %**. Its own stated exposure — D4-SHIPPED's contended 7.317 core-min/major, which would have put `O_mp` at 1,756 — was still below what happened. **CONTENTION IS NAMED SEPARATELY AND IS ABSORBED INTO NO RATIO, and it is NOT the explanation:** the cgroup sampler measured `delivered_cores_mean` **3.9768 of 4 requested (99.42 %)** over **n = 1,984** samples with `max_nr_throttled` **98,176**, and `siblings_pre` and `siblings_post` are both empty — the arm got very nearly the whole cpuset it asked for, so at most ~0.6 % of the gap is contention. **A SECOND, SEPARATE ATTRIBUTION THAT IS NOT COST MISPREDICTION AT ALL:** the optimiser **stalled** rather than ran slow — no `EXIT` line, **545** `Warning: Cutting back alpha due to evaluation error`, 7 restoration majors, and dual infeasibility **worsening** from 5.78e-04 at major 58 to 1.14e-03 at major 64 against `tol` 1e-5. A rate calibrated on a converging optimiser does not price one that is backtracking, and the successor's estimate must carry a stall branch. **FRAME ARTEFACT, DISCLOSED AND NOT REPAIRED (`D6-CAP-FRAME-1`):** the cap is enforced in the CONTAINER frame (`timeout` at `d6_run_arm.sh:347`, `enforced_wall_s=30000`, `enforced_core_min=2000.000000`) and graded in the HOST frame (`T0` at `:336` **before** `docker run` at `:340`; the container preamble at `:343-346` runs OUTSIDE the `timeout`; `T1` at `:376` follows a `sleep 10` poll loop, a `docker logs` at `:372` and a `docker inspect` at `:374`; `WALL` `:377` and `CORE_MIN` `:399` feed `d6_grade.py:502`'s `within_cap = core_min <= cap`). **Measured frame gap: 30,008 − 30,000 = 8 s = 0.5333 core-min at 4 ranks, and the recorded cap excess is 2,000.533 − 2,000.000 = 0.533 core-min — THE SAME NUMBER.** So an arm that obeyed its registered deadline exactly records **0.027 % above its own cap**, which would read `G10 GATE FAIL` and `P5 MISS` on the frozen formulas. **That limb is a measurement-frame artefact; every other limb below is real.** Gates are closed post-compute (rule 2), so **nothing is repaired here** — it is a registration defect that re-registers in the successor with a grader-wall-against-enforced-wall check across frames. The assert meant to catch it, `D4_CAP_ASSERT` at `d6_run_arm.sh:194-202`, derives `TMO` from the cap and re-checks it by **inverting the same arithmetic inside one frame**, so it passes on every run and never could. Recorded as **L-371**. | `ledger.txt` and `STATUS.chain` under `/home/ubuntu/certonomous-runs/CURRICULUM-D6-a2-wing-multipoint/` (outside git by size, `docs/LOCATIONS.md`); `CHAIN_DONE` stamp `20260827T222933Z`, md5 `f042617f0580baa03dfd6e0eb26526ae`, 174 bytes. Registration: `cases/dafoam/ladder-a/A2/curriculum_D6/PREREGISTRATION.md` §4 `:90-101` (the cost table), §5 `:117-134` (P1–P7), §7 `:172` (the grader md5), frozen at commit `3090e858`. Instruments: `d6_run_arm.sh` md5 `98472772c9cdfd9007fcf6367b0a2a37` (the §7 value, unmoved); `d6_chain_driver.sh` md5 `b4ddca654d8d5165a13447df4d0c1469` — **the ADDENDUM 3 value at `:313`, which supersedes §7's `860b9842…` through Addendum 2 at `:299`; a check stopping at §7 reports a false GATE FAIL here** (L-370). **Dollars are DERIVED, never measured** (the box cannot read its own billing, `COMPUTE_BUDGET_CHARTER.md` §5) at the owner-stated c7a.4xlarge rate **$0.0513/core-h**, reported-by-owner: actual **$1.7105 derived** (33.342 core-h), predicted **$1.3111 derived** (25.557 core-h), and the 80-major-at-measured-rate figure **$2.1381 derived**. **All three are far inside the $25 pre-authorisation.** **P7 SURVIVES THIS ROW AND IS THE ANSWER D6 EARNED: `OOMKilled=false` after 8 h 20 m at 20g, so multipoint at np=4 in 20 g IS MEMORY-FEASIBLE ON THIS BOX** — the exposure §4 called *"the one this item may fail on"* did not fire. Scored by the ledger's `inspect(exit,oomkilled)=[124 false]`, not by the grader, which refused. |
| C-189 | 2026-08-27 | cfd | **MDS-1 mesh-dimension survey** (`verification/runs/MDS1_runs/`) — the survey `MESH_STANDARD.md` §11.5 deferred aspect-ratio and cell-volume-ratio thresholds to, standing it up and discharging §11's live anti-deferral clause. **No gate, no threshold, no verdict word** — collecting a distribution and setting a gate are different acts and only the first was authorised. | **ZERO new compute**, frozen in `MESH_STANDARD.md` §11.5 before the survey existed. Basis, not a guess: both §11 quantities (`Max aspect ratio`, and the ratio derived from `Min volume`/`Max volume`) are already present in retained `log.checkMesh` files, so the survey re-reads artifacts rather than re-running `checkMesh`. No cap was registered because no compute was predicted. | **0 core-min of solver or mesher compute** — no solver, no mesher, no `checkMesh` invocation, verified by the absence of any new log or time directory. Reader runtime single-rank: 7.82 s cold, 3.85/3.90 s warm; four recorded survey runs ≈ **0.33 core-min**. Verification sweeps (per-file greps over ~1,005 files, twice) untimed, bounded **≤ 3 core-min**. Total **≤ ~3.3 core-min**, 1 rank. | **Same as gross.** No stall (longest single invocation 7.82 s against the 3,600 wall-s threshold), no re-run of a failed arm, no truncation. | **1.000x on the registered quantity.** The prediction was *zero new compute* and the actual is *zero new compute* — the ratio is exact and the prediction held. **No ratio is quoted on the ≤3.3 core-min**, because nothing was predicted for it: infrastructure runtime was outside the registered frame and quoting a ratio against an absent denominator would manufacture a calibration figure. | **NO MISPREDICTION, NO CONTENTION, NO WASTE — and the useful calibration finding is about the FRAME, not the number.** §11.5 predicted the *compute* and said nothing about the *instrument's own* runtime, so a completion that spent ~3.3 core-min reads as unpredicted spend under a naive comparison. It is not: it is a category the registration never priced. **Lesson for the next zero-compute registration: predict the instrument's runtime as well as the solver's, or state explicitly that instrument cost is out of frame** — otherwise every zero-compute survey completes "over" an estimate of zero. **WASTE is 0.000 and is named separately per `COMPUTE_BUDGET_CHARTER.md` §6.** **Dollars DERIVED, NOT MEASURED** at the owner-stated c7a.4xlarge rate $0.0513/core-h, reported-by-owner — the box cannot read its own billing (§5): **≈ $0.0028**. Far inside the $25 pre-authorisation. **One control fired for real during the build and is recorded because it is the row's most transferable content:** the reader's F5b quarantine guard reported `kept=1 pruned=0` — **the guard against false zeros was itself producing one**, stopping at the quarantine's top directory instead of counting recursively. Fixed before filing; prose review would not have found it. | `docs/standards/MESH_STANDARD.md` §12 (v1.6 → v1.7, `9377002d`); survey artifacts `verification/runs/MDS1_runs/{README.md,mds1_survey.py,MDS1_SURVEY.json,MDS1_SURVEY_STDOUT.txt,MDS1_CONTROLS.txt}` (`65af8a7f`) |
| C-190 | 2026-08-28 | closure | **G1_grid_triple** — periodic-hill grid-convergence triple (L1/L2/L3, 3,840 / 15,360 / 61,440 cells), chain COMPLETE 2026-08-27T23:38:44Z rc 0, graded 2026-08-28T16:00Z **`NOT A RESULT`** (frozen comparator refused rc 2 at the `P3 fatal` clause on all three levels). **A refusal that consumed 127 core-min still owes this row, and the refusal was an INSTRUMENT defect, not a solver failure — the physics ran to completion on every clause the comparator can still read.** | **320.0 core-min registered estimate**, cap 600.0 (`cases/RANS_LES_closure_models/G1_grid_triple/PREREGISTRATION.md`, frozen `03be2015`, amended pre-compute `a90077df` with no cost or gate change); $0.2736 derived at $0.0513/core-h | **127.08 core-min MEASURED** = 2.118 core-h = **$0.1086 derived, not measured** (the box cannot read its own billing, `COMPUTE_BUDGET_CHARTER` §5). Summed from three per-level readings written by the driver itself into `CHAIN.log`: L1 116 wall s / 1.93 core-min, L2 580 / 9.67, L3 6,929 / 115.48, all at ranks=1 | = gross. **No row matches the 3600-s stall rule as a stall**: L3's 6,929 wall s exceeds 3,600, but it is a single continuous 60,000-iteration solve at ranks=1 whose `ExecutionTime` count equals its `endTime` at hard equality and whose final residuals are 4.0e-11 (`Ux`) — it is a long run, not a stalled one, and it is named here rather than silently excluded | **0.397x actual/predicted** (127.08 / 320.0); **0.212x of cap** (127.08 / 600.0). The run came in at **40 % of estimate**. No cap approached, nothing stopped, no gate moved | **Contention variance, and it is DECIDED rather than chosen.** Zero waste. The estimate was built on an uncontended serial rate; the run then executed with **13 of 16 cores held by two other families** for its first hours and on a progressively emptier box afterwards. Three candidate causes were named ON THE BOARD AT 22:12Z 2026-08-27 **before the final number existed** — box contention, memory-bandwidth saturation at 61,440 cells, or worse linear-system conditioning at the fine level — precisely so the explanation could not be selected to fit the answer. **Contention dominates, established by the run ACCELERATING when the box emptied**: L3's instantaneous rate under contention was 2.94 it/s, and the remaining ~55,000 iterations completed nearly four hours ahead of the projection built from that rate. **The calibration lesson, and it is the reusable half: A RATE SAMPLED UNDER CONTENTION IS NOT A RATE.** Recorded beside it, against myself: the supervisor published **three consecutive wrong ETAs for this one chain** (~167, then ~90, then ~352 core-min; actual 127.08) — the last wrong by a factor of 2.8 — every one of them extrapolated from a partial-run rate instead of measured. Sibling to `C-4`'s finding (record the load average beside any per-iteration cost basis); this row extends it: a rate extrapolated MID-RUN under changing load is not a basis at all | `cases/RANS_LES_closure_models/G1_grid_triple/RESULTS.md` §8, landed at `1bd6d750`; pre-registration frozen `03be2015`, comparator sha `253d594252a20e534f1b8191a966307662c101f9db7f38d6044c51b2379c6bb0` verified identical disk == HEAD == freeze |
| C-191 | 2026-08-28 | dafoam | **D8R** (ladder-a A6, `curriculum_D8R`) — A6 CRM wing-alone N=16, `twist`-only constrained drag minimisation **run to convergence on BOTH toolchain rows**, each followed by an endpoint FD table; four SOLVER arms `O-P`, `F-P`, `O-S`, `F-S` at 4 ranks on cpuset `0,1,12,15`. All four `rc=0`, no `OOMKilled`, `chain=COMPLETE 20260828T021329Z`, frozen grader `d8r_grade.py` (md5 `3f6eafac2ad4897417521d00c1cc5f3e`) `grader_rc=0`. **ITEM VERDICT `PASS`; rows SHIPPED `PASS` / PATCHED `PASS`**; `G10_caps` `PASS`, no arm crossed its own cap. | **1,038.0 core-min** point, band **[500, 1,800]**, item ceiling **2,240.0 = Σ caps**, frozen at `PREREGISTRATION.md:66` before the run root existed (freeze commit `357a2648`). Per arm: O arms **458.0** each (cap 1,000.0), F arms **61.0** each (cap 120.0). Basis, stated as an assumption in the registration: D8's measured np=1 per-major model at **75 %** parallel efficiency for the point, 50 % for the cap; no np=4 anchor existed for this case. Dollars **DERIVED** at the registered point: **$0.8875** (`PREREGISTRATION.md:68`); ceiling $1.9152. | **876.867 core-min**, basis **gross**, MEASURED from `ledger.txt`'s four `ARM=` rows: `O-P` **316.867** (wall 4,753 s), `F-P` **63.600** (954 s), `O-S` **429.467** (6,442 s), `F-S` **66.933** (1,004 s), all at `ranks=4`. Dollars **DERIVED, NOT MEASURED**: 14.6145 core-h × $0.0513/core-h = **$0.7497**. **`cost_basis`: c7a.4xlarge at $0.0513/core-h, REPORTED-BY-OWNER, NOT MEASURED** — the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` §5). | **876.867 — gross == cleaned, AND THE 3,600-SECOND FIGURE IS ADDRESSED RATHER THAN PASSED OVER.** Two arms sit above it: `O-P` at 4,753 s and `O-S` at 6,442 s. The cgroup sampler measured `delivered_cores_mean` **3.9501** (`O-P`, n=312), **3.9843** (`F-P`, n=62), **3.9969** (`O-S`, n=425), **3.9826** (`F-S`, n=65) **of the 4 ranks requested** — 98.8 % to 99.9 % — with `siblings_pre` and `siblings_post` empty on every arm. A stall is a wall with no delivered cores behind it; these walls have very nearly four cores behind every second. **There is nothing to clean. WASTE: 0.000 core-min, named separately per `COMPUTE_BUDGET_CHARTER.md` §6 and folded into no ratio below.** | **0.8447×** (876.867 / 1,038.0) — **15.5 % UNDER the registered point, inside the [500, 1,800] band (P6 HIT).** Per arm, against each arm's own registered point: `O-P` **0.6918**, `F-P` **1.0426**, `O-S` **0.9377**, `F-S` **1.0973** (the grader's own figures, `gates.G10_caps.per_arm`). Against the 2,240.0 ceiling: **0.3915×** — the item bought 39.1 % of its authorised envelope. | **THE WHOLE UNDER-RUN IS IN THE O ARMS AND IT IS A MAJOR-COUNT EFFECT, NOT A RATE EFFECT.** The registration priced each O arm at **20 majors** (P7's point, band [8, 30]); IPOPT printed `EXIT: Optimal Solution Found.` at **8 majors** (`O-P`) and **12** (`O-S`) — both rows converged on the optimiser's own statement, neither reached `max_iter` 30. `O-P` at 8 majors therefore lands at 0.6918 of its point and `O-S` at 12 at 0.9377; the 20-major point was the open variable the band was written to carry, and it carried it. **THE F ARMS WENT OVER, and that is the half of the estimate that wants revisiting:** `F-P` **1.0426×** and `F-S` **1.0973×** against 61.0 core-min each, so the FD arms' primal-count model (32 primals × 78.50 s warm + 231 s, ÷ 0.75) is mildly optimistic at np=4 — the successor prices F arms from these two measured figures, not from D8's warm np=1 anchor. **CONTENTION IS NAMED AND IS NOT THE EXPLANATION:** 98.8–99.9 % of the requested cores were delivered on every arm and no arm waited (`aggregate_waited_s=0`, `cpuset_waited_s=0` on all four, `STATUS.<arm>`). **NOT A MISPREDICTION OF RATE:** the O-arm walls track the registered per-major model at the major counts actually taken. **Infrastructure fact recorded beside the figures, not folded into them:** the agent fleet died at ≈2026-08-27T23:15Z on the account's weekly usage limit; `F-P`, `O-S`, `F-S` and the frozen grader all ran afterwards with no agent alive, carried by the detached chain driver `pid=1587601` — Sanaa's 2026-08-26 detached-queue-runner ruling. It cost nothing and bought the item. | `cases/dafoam/ladder-a/A6/curriculum_D8R/RESULTS.md` §10 (introduced by the same commit that lands this row, so no earlier sha exists to cite — disclosed, not approximated); grading artefacts `D8R_grade_20260828T021329Z.json` / `.out`, `ledger.txt`, `STATUS.chain`, `STATUS.<arm>` under `/home/ubuntu/certonomous-runs/CURRICULUM-D8R-a6-twist-opt-conv`; prediction frozen at `PREREGISTRATION.md:66,68` @ `357a2648`. |
| C-192 | 2026-08-28 | closure | **G1b_grid_triple_regrade** — the successor rung that repaired G1's fatal clause and RE-GRADED THE SAME COMPLETED RUN ROOT with zero new solver compute. Graded 2026-08-28T16:30Z **`NOT A RESULT`** (triple `DIVERGENT`, `R = 8.5436`). **This row exists to price the SUCCESSOR ROUTE against the alternative it was chosen over**, which was editing a frozen comparator post-compute under the VERIFICATION §2d.1 repair exception. | **1.0 core-min** registered estimate for the grading pass, cap **10.0** (`cases/RANS_LES_closure_models/G1b_grid_triple_regrade/PREREGISTRATION.md` §6, frozen `dc61e9b5`); solver core-min registered **0.000**; $0.00086 estimate / $0.00855 cap derived at $0.0513/core-h | **0.188 core-min MEASURED** (11.26 s wall × 1 rank ÷ 60; `/usr/bin/time -v`: user 10.83 s, sys 0.26 s, MaxRSS 197,292 kB — artefact `artefacts/g1b_time_v.txt`) = **$0.00016 derived, not measured**. **Solver core-min: 0.000, and the 127.08 core-min of physics it re-read is charged to `C-190` and is NOT double-counted here** — the same completed run root graded a second time by a second instrument is one physics cost, not two | = gross. No stall-rule row exists: the whole pass is 11.26 wall s | **0.188x actual/predicted; 0.019x of cap.** No cap approached | **Misprediction in the conservative direction, zero waste, zero contention.** The 1.0 core-min estimate was built from a synthetic-data basis (selftest 0.08 s; `read_scalar_internal` over 61,440 values 0.100 s) and the real pass was five times cheaper, because the dominant cost turned out to be reading three logs of 22.6 / 33.8 / 67.8 MB rather than the field arithmetic the basis priced. **Calibration lesson: for a read-only grading pass, price the LOG BYTES, not the cell count.** | `cases/RANS_LES_closure_models/G1b_grid_triple_regrade/RESULTS.md` §5, landed at `e8ef5008`; pre-registration and comparator frozen `dc61e9b5`, comparator sha `614e52064b8ade5dbe109c632ddea23274157a263eaf6755b822d1fc3d02535b` verified identical disk == committed blob at grading time |
| C-193 | 2026-08-28 | ansys-verification | **VMFLGPU005** — GPU solver path, turbulent natural convection in a tall differentially-heated cavity (VM2026R1 p. 235, CPU parent VMFL052); six solves (L1/L2/L3 × GPU arm + forced-CPU control), **GRADED by the frozen comparator at exit 0** and registered as row **#42 `NOT A RESULT`** (the gate produced readings; rule 5 disqualified four of the seven channels). **A completed-and-graded run owes this row** (CLAUDE.md rule 12). | **"GPU arm ~1–4 GPU-h across the six solves (L3 dominant); forced-CPU arm ~60–200 core-min"**, frozen in `PREREGISTRATION.md:247` (prereg blob `34a3570e`, freeze commit `05555370`) before compute. Registered caps **per-solve**: 6.0 GPU-h/solve and 240 core-min/CPU-arm. **THE ESTIMATE IS AMBIGUOUS ON ITS DENOMINATOR** — it says "GPU arm" but "across the six solves", while the GPU arm is only three of the six solves — and is scored **both** ways below rather than in the flattering direction. | **GPU arms 1.6833 GPU-h** (per-arm wall 403 / 1439 / 4218 s = 6 060 s, L3 dominant); **CPU arms 151.75 core-min** (210 / 1379 / 7516 s = 9 105 s at ranks = 1); **instance occupancy 4.215278 GPU-h** (total wall **15 175 s** ÷ 3600, the launcher's own authoritative figure). Read from `COST.txt` and the six `RUN_RC.*`. The six solve wall_s sum to 15 165; the 10 s gap is meshing/bookkeeping outside the solves, named not hidden. | **Same as gross — cleaned == gross.** No stall (the longest single solve is 7 516 wall s at ranks = 1, a genuine 25 000-iteration forced-CPU solve whose `Time =` count equals its endTime — a long run, not a stalled one; the 3600-s stall rule identifies a stall by cross-family wall-time clustering that no per-solver cost explains, not by the threshold alone, and is named here rather than silently excluded). No re-run, no truncation, **`cap_fired = 0` on all six arms — no registered cap fired by either mechanism** (largest GPU solve 19.53 % of the 6.0 GPU-h/solve cap; largest CPU arm 52.19 % of the 240 core-min/arm cap). | **GPU: TWO figures, because the estimate's denominator is ambiguous.** GPU-arm-only **0.673×** (1.6833 / 2.5 band midpoint) — INSIDE the 1–4 band; instance-occupancy **1.685×** (4.2153 / 2.5) — **ABOVE the band's top of 4.0 by 0.2153 GPU-h**. **CPU: 1.167×** (151.75 / 130 midpoint) — INSIDE the 60–200 band. | **The CPU side is a GOOD prediction (1.167×, inside band) and needs no explanation. The GPU side CANNOT be scored without resolving the denominator, and THAT IS THE FINDING — a REGISTRATION DEFECT, not contention and not waste.** Ruling [lab-attributed]: the **billing-relevant** figure is **instance occupancy** (the GPU card bills wall-clock regardless of which arm runs, so even the forced-CPU control arm's 9 105 s of wall is billed as GPU time), and by that figure the run is **above the band top**; the **physics-relevant** figure is the **GPU-arm total 1.6833 GPU-h**, and by that figure it is inside. Both are reported; neither is suppressed. **The reusable calibration lesson: a GPU cost estimate MUST name its denominator — solver (arm) time vs instance occupancy — because a two-arm-per-level design (GPU arm + forced-CPU control) makes occupancy ~2.5× the GPU-arm total, and an estimate silent on which it means can be read either inside or above its own band.** Sibling to `C-182`'s finding that the GPU/CPU ratio must be measured per case, not assumed: here the CPU control arms (210/1379/7516 s) are again NOT negligible against the GPU arms (403/1439/4218 s) — at L3 the forced-CPU arm is ~1.78× the GPU arm's wall — so occupancy is dominated by whichever arm is slower, not by the GPU. **WASTE 0.000, named separately per `COMPUTE_BUDGET_CHARTER.md` §6** — all six solves completed and were graded, nothing thrown away; GPU idle before/after the run is infrastructure and is NOT folded into any ratio here. **A SEPARATE DEFECT, NAMED AND REFERRED, that is NOT a cost misprediction:** a launch-level `CAP_OVERRUN.txt` (2026-08-28T03:07:44Z, "elapsed 14400") REPORTED a 4-hour whole-case overrun and did NOT stop the run, where rule 12 says an overrun stops the run — but that 4 h cap is NOT in the pre-registration (registered caps are per-solve, neither exceeded, so nothing is voided); 14400 s = 240 × 60, the per-solve CPU cap mis-applied as a whole-case budget; the defect is a runner that reports rather than enforces; the file was ABSENT from the bundled run root and its full bytes are **UNREAD by this lane (VERIFY)**. **Referred to `verification-supervisor` for ruling, not decided here.** | `cases/ansys_verification/VMFLGPU005/RESULTS.md` §10–§11; register row **#42**; committed evidence `verification/runs/ansys_verification/VMFLGPU005/GRADE_OUTPUT_2026-08-28.txt` (blob `ec36c8e4`). Dollars **DERIVED, NOT MEASURED** at the published-list $0.8048/GPU-h and $0.0513/core-h: occupancy **$3.3925 derived**, GPU arms only **$1.3547 derived** — the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` §5) and the console figure is owed and supersedes. Prereg frozen `05555370`; comparator `f80254fd712b1cda5a86908de2ad92f4cc85f062` verified identical disk == HEAD == freeze == instance. |
| C-194 | 2026-08-28 | cfd | **F17c-KV40-FLOOR** (Kovasznay Re = 40, `simpleFoam`, 192×128 / 384×256 / 768×512, the DERIVED iterative floor under L-346) — three levels run serially on **1 rank**, 2026-08-27T22:31:42Z → 2026-08-28T02:38:25Z, all three **rule-4 COMPLETE** (`RC.txt` 0, one `End`, `Time` count == `ExecutionTime` count == endTime 4,000 / 8,000 / 48,000, fields present, age guard holds at every level), graded by the frozen `grade_f17c.py` at prereg commit `3c01061e`. **Rung verdict: `PASS` × 1 (G-F17-2 `u_at_probe`, p 2.140696, GCI 4.381227e−06 abs at Fs = 1.25) and `NOT A RESULT` × 1 (G-F17-1 `E2_velocity_L2`, rule 5 step (1): medium level `NOT_PLATEAUED_TREND`).** | **247.62 core-min** = 4.127 core-h, registered cap **370.0** (= 1.4942 × the estimate), frozen at `F17c_KV40_FLOOR_PREREGISTRATION.md` §8.3 @ `3c01061e` before the run root existed (§9 records the absence three ways). Per level: coarse **1.106**, medium **12.251**, fine **234.262**. Basis: per-cell-iteration rates **measured on these exact three meshes** from F17b's own logs (0.6751 / 0.9347 / 0.7447 core-µs), marginal (first-to-last `ExecutionTime` differenced over 3,999 iterations, startup excluded), contention multiplier **1.0**. Dollars **DERIVED** at the estimate: **$0.212** (§8.3); at the cap $0.316. **`cost_basis`: c7a.4xlarge at $0.0513/core-h, REPORTED-BY-OWNER, NOT MEASURED** — the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` §5). | **246.300 core-min**, basis **gross**, MEASURED as `ClockTime × ranks ÷ 60` from each level's `log.simpleFoam`: coarse **1.250** (75 s), medium **11.200** (672 s), fine **233.850** (14,031 s), all at `ranks=1`. Corroborated to every digit by two independent records: the launcher's own tally (`cases/F17c_kovasznay_floor/launcher.queue.out`, *"Cumulative spend: 246.29999999999998 core-min of 370"*) and the frozen grader's `cost_claim.core_min_claim` / `partial_sum_core_min` in `F17c_GRADED.json` (`defects: []`). ExecutionTime basis stated beside it, **244.393 core-min**. `ClockTime` is integer-second: ± 0.008333 core-min per level. Dollars **DERIVED, NOT MEASURED**: 4.105 core-h × $0.0513/core-h = **$0.2106**. | **246.300 — gross == cleaned, AND THE 3,600-SECOND ROW IS ADDRESSED RATHER THAN PASSED OVER.** One level sits above it: fine at **14,031 wall s**. §8.4 of the pre-registration registered **BEFORE COMPUTE** that this heuristic *"will fire on the fine level and it will not be a stall"*, and the artefacts bear that out independently of the wall figure: 48,000 `Time =` and 48,000 `ExecutionTime` lines, 480 written checkpoints, 18.87 billion cell-iterations, and `ExecutionTime / ClockTime` = 13,985.02 / 14,031 = **0.9967** — the rank delivered CPU for 99.67 % of that wall. A stall is a wall with no work behind it. **There is nothing to clean. WASTE: 0.000 core-min**, named separately per `COMPUTE_BUDGET_CHARTER.md` §6 and folded into no ratio in this row — no stall, no kill, no re-run, no cap movement; the ladder ran once, completed, and was graded once. Cap use **66.57 %** (246.300 / 370.0); no overrun and the cap was never raised (fine used 14,031 s of its 21,453 s in-level `timeout` allowance, ×1.529 headroom). | **0.9947×** (246.300 / 247.62) — **0.53 % UNDER the registered estimate.** Per level against its own §8.3 projection: coarse **1.130** (13.0 % over, 9 wall s), medium **0.914** (8.6 % under, 63 wall s), **fine 0.9982 (0.18 % under, 25 wall s)** — and fine carries **94.9 % of the ladder's spend**. | **MISPREDICTION, 0.53 % — and it is concentrated in the two cheap levels, which very nearly cancel.** **CONTENTION: NONE DETECTABLE, and it is named rather than assumed absent.** Ladder `ExecutionTime / ClockTime` = 14,663.60 / 14,778 = **0.9922**; wall exceeds CPU by **0.78 %**, inside the *"bounded below 1 %"* the registration set at §8.2 from F17b's own wall/CPU ratios. The box was **not** idle — `box_before.txt` / `box_after.txt` record load1 **15.84 → 23.08 → 24.69 of 16 cores** with MemAvailable falling 27.5 → 20.2 GB across the three launches — so a busy box produced a 0.78 % wall/CPU gap on this work and the registered contention multiplier of 1.0 **held**. **WASTE 0.000, named separately and not laundered into the ratio above.** **THE SUBSTANTIVE CALIBRATION LESSON — §8.2's DECISION TO REGISTER FROZEN PER-LEVEL RATIOS RATHER THAN A FITTED GROWTH EXPONENT IS VINDICATED BY MEASUREMENT.** §8.2 registered **no per-doubling exponent**, on the stated ground that the measured per-cell-iteration rate is not monotone in problem size (it rises 38.5 % coarse→medium and falls 20.3 % medium→fine). What `proj_f17c.py` carries instead is frozen per-level ratios from one measurement, with **`GROWTH["fine"] = 0.7967`, WHICH IS LESS THAN ONE** (= 0.7447 / 0.9347). That produced a **0.18 %-accurate projection of the level carrying 94.9 % of the spend.** The counterfactual, as arithmetic on the registered figures and labelled as such: the coarse→medium rate ratio is 0.9347 / 0.6751 = **1.3845**; an exponent fitted to that step and applied a second time gives a fine rate of **1.2941 core-µs/cell-iteration** → 18,874,368,000 × 1.2941e−06 = **24,426 wall s = 407.10 core-min**, **1.74× the actual 233.850 and 1.10× the registered cap of 370**. A projector that could only carry a growth factor above 1 would have projected this ladder **over its own cap.** This is the standing F21 / F22 / F18b defect — base rates right to 7–10 %, imported growth exponent wrong by +110 to +137 % per doubling — **not repeated.** **One measured caveat recorded against the launcher's own live re-basing:** `run_f17c.sh` re-bases the rate from THIS run's previous level and its live pre-spend projections were medium 13.8454 (actual/live 0.809) and fine 214.1600 (1.092), against the frozen §8.3 12.251 (0.914) and 234.262 (**0.9982**) — at the level carrying the spend the **frozen projection was 48× more accurate than the live re-based one** (0.18 % vs 8.42 % error against the actual). The live re-basing is a cap-safety device, not a calibration instrument, and this row is built from the §8.3 registered figures, which are what rule 12 compares against. | `verification/campaign/F17c_KV40_FLOOR_RESULTS.md` §10 and §11 (introduced by the same commit that lands this row, so no earlier sha exists to cite — disclosed, not approximated); grading artefact `verification/runs/F17c_runs/F17c_GRADED.json` (25,206 bytes, 2026-08-28T16:14:12Z, `cost_claim` block); per-level `log.simpleFoam`, `RC.txt`, `CAP_ALLOWANCE.txt`, `MESH_LINE.txt`, `box_before.txt` / `box_after.txt` under `verification/runs/F17c_runs/`; launcher record `cases/F17c_kovasznay_floor/launcher.queue.out`; prediction frozen at `verification/campaign/F17c_KV40_FLOOR_PREREGISTRATION.md` §8.2–§8.4 @ `3c01061e`. |
| C-195 | 2026-08-28 | cfd | **F25-DUCT3D** (fully developed laminar flow in a square duct, `simpleFoam`/SIMPLEC, streamwise cyclic, fixed body force G = 0.2845, nu = 0.01, Re_Dh = 99.985) — the lab's first GENUINE 3-D ladder, `dim = 3`, **r = 2.000 exactly in x, y AND z**, 32,768 / 262,144 / 2,097,152 cells, **4 ranks at every level, graded from `processor{0,1,2,3}/4000/U` and NEVER reconstructed**. All three levels **rule-4 COMPLETE** (`RC.txt` 0, one `End`, 4,000 `Time` == 4,000 `ExecutionTime` == endTime, U and p present in all four processor dirs and newer than `0/U`). Graded 2026-08-28T16:15Z by the frozen `grade_f25.py` at prereg commit `12def6b5`. **Rung verdict: `PASS` x 2** — G-F25-1 `E2_normalised_profile` (p 1.9528825654732214, GCI_abs 9.598087474857314e-04 at Fs = 1.25) and G-F25-2 `f_Re` (p 1.9697674377136016, GCI 0.12039783335840937 %, Richardson 56.90949857023062 against the exact series 56.90830753912461, agreement 1.19e-03). **A completed-and-graded ladder owes this row** (CLAUDE.md rule 12). | **1,342.8 core-min** = 22.38 core-h, registered cap **2,000** (= 1.49 x the estimate), frozen at `F25_DUCT3D_PREREGISTRATION.md` AMENDMENT 1 line 470 (cap line 471), **pre-first-compute**, run root `test -e` ABSENT at 2026-08-26T22:56:52Z. Per level: coarse **4.1**, medium **72.1**, fine **1,266.6**. **The comparison basis is the AMENDED 1,342.8, NOT the original 706.0 of section 7, which Amendment 1 STRUCK in its own words ("is superseded by this row").** Basis: 2.44 core-us/cell-iteration MEASURED on this case's own 65,536-cell instrument arm, scaled by **+30 % per doubling of cell count — MODELLED, not measured**, the fine level FIVE doublings above the anchor; the registration labelled it `cost_basis: measured at 65k cells on this case; growth per level modelled; derived beyond the anchor`. Dollars **DERIVED**: **$1.1481** at the estimate, $1.71 at the cap. **c7a.4xlarge at $0.0513/core-h, REPORTED-BY-OWNER, NOT MEASURED** — the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` section 5). | **499.1333 core-min**, basis **gross**, MEASURED as `ClockTime x ranks / 60` at **ranks = 4** from each level's `log.simpleFoam`: coarse **5.7333** (86 s), medium **53.0667** (796 s), fine **440.3333** (6,605 s). Corroborated to every digit by two independent records: the launcher's own tally (`cases/F25_DUCT3D/launcher.queue.out`, "Cumulative spend: 499.1333333333333 core-min of 2000") and the frozen grader's `cost_claim.core_min_claim` / `partial_sum_core_min` in `F25_GRADED.json` (`defects: []`). ExecutionTime basis stated beside it, **498.9680 core-min**. `ClockTime` is integer-second: +/- 0.0333 core-min per level at 4 ranks. Dollars **DERIVED, NOT MEASURED**: 8.3189 core-h x $0.0513/core-h = **$0.4268**. | **499.1333 — gross == cleaned, AND THE 3,600-SECOND ROW IS ADDRESSED RATHER THAN PASSED OVER.** One level sits above it: fine at **6,605 wall s**. It is not a stall and the artefacts say so independently of the wall figure: 4,000 `Time =` lines, 4,000 `ExecutionTime` lines, 40 written checkpoints, 8.389e9 cell-iterations, and `ExecutionTime / ClockTime` = 6,603.39 / 6,605 = **0.99976** — the four ranks delivered CPU for 99.98 % of that wall. A stall is a wall with no work behind it. **There is nothing to clean. WASTE: 0.000 core-min**, named separately per `COMPUTE_BUDGET_CHARTER.md` section 6 and folded into no ratio in this row — no stall, no kill, no re-run, no cap movement; the ladder ran once, completed, and was graded once. Cap use **25.0 %** (499.1333 / 2,000); no overrun and the cap was never raised. | **0.372x** (499.1333 / 1,342.8) — **62.8 % UNDER the registered estimate.** Per level against its own AMENDMENT 1 projection: coarse **1.398** (39.8 % OVER), medium **0.736** (26.4 % under), fine **0.348** (65.2 % under) — and fine carries **88.2 % of the ladder's spend**. **The direction is not uniform and the row does not average it away.** | **MISPREDICTION of the GROWTH MODEL — not contention, not waste, and not the base rate.** The anchor was measured on this very case (2.44 core-us/cell-iteration at 65,536 cells) and the amendment scaled it by **+30 % per doubling**, self-labelled MODELLED and "derived beyond the anchor", with the fine level five doublings out. Each ladder step multiplies cells by 8 (three doublings), so the model applied **2.197x per level**. **MEASURED rates: 2.6245 / 3.0365 / 3.1495 core-us per cell-iteration** (coarse / medium / fine) against the registered 1.877 / 4.124 / 9.060 — i.e. **the rate is very nearly FLAT across a 64x range of problem size**, growing **1.157x** then **1.037x** per level = **+5.0 % and +1.2 % per DOUBLING measured, against +30 % modelled**. The model over-predicted the ladder by **2.69x** overall; at the coarse level, one halving BELOW the anchor, it UNDER-predicted by 40 % because fixed startup does not amortise downward. **CONTENTION: present in the box load, absent from the wall-versus-CPU reading, and named rather than assumed absent.** `box_before.txt` / `box_after.txt` record load1 **10.75 -> 13.22 -> 14.00 -> 14.02 of 16 cores** with free cores falling 5.25 -> 1.98, yet `ExecutionTime / ClockTime` = **0.9953 / 0.9994 / 0.9998** — the four ranks were never descheduled, the measured rates already carry the load they ran under, and no separable contention figure can be extracted from them, so none is invented. **WASTE 0.000, named separately and not laundered into the ratio above.** **CARRY FORWARD — this is the same standing defect priced at F21, F22, F18b and F17c C-194: base rate measured right, growth exponent imported and wrong.** Here it was wrong in the SAFE direction and cost only headroom; the identical methodology can miss the other way and trip a cap. **AMENDMENT 2's L-349 replacement projector, which re-bases from THIS case's own completed levels, is what actually tracked the run:** live projections 4.1 -> 100.8 -> 491.2 core-min against actuals 5.7 -> 53.1 -> 440.3, so the fine projection — the one that decides whether the expensive level runs at all — was **1.115x** the actual against the frozen model's **2.876x**. | `verification/campaign/F25_DUCT3D_RESULTS.md` sections 6 and 7 (introduced by the same commit that lands this row, so no earlier sha exists to cite — disclosed, not approximated); grading artefact `verification/runs/F25_DUCT3D_runs/F25_GRADED.json` (33,040 bytes, 2026-08-28T16:15Z, `cost_claim` block; this grader writes no `.out` file); per-level `log.simpleFoam`, `RC.txt`, `MESH_LINE.txt`, `box_before.txt` / `box_after.txt` under `verification/runs/F25_DUCT3D_runs/`; launcher record `cases/F25_DUCT3D/launcher.queue.out`; prediction frozen at `verification/campaign/F25_DUCT3D_PREREGISTRATION.md` AMENDMENT 1 (lines 455-471) and AMENDMENT 2 @ `12def6b59d624816fd25f8cef68ff96a4982e8a8`. |
| C-196 | 2026-08-28 | cfd | **F27-WOMERSLEY-PIPE-3D** (pulsatile Womersley laminar flow in a circular pipe, alpha = 4, `pimpleFoam`, butterfly mesh, cosine body force f = 1) — three levels, `dim = 3`, **r = 2 in BOTH h and dt**, 3,840 / 30,720 / 245,760 cells at 672 / 1,344 / 2,688 steps, **4 ranks, graded from `processor*/5.25/U`**. All three levels **rule-4 COMPLETE** (`RC.txt` 0, one `End`, `Time` count == endTime/dt, U and p at 5.25 in all four processor dirs newer than `0/U`, the `4.25` periodicity field present, `0/C` and `0/V` present). Graded 2026-08-28T16:13Z by the frozen `grade_f27.py` at prereg commit `05ec72c4`. **Rung verdict: `NOT A RESULT` x 2** — both triples **OSCILLATORY**, monotone FALSE, **order null and NO GCI QUOTED** (rule 5: never quote a GCI when the three values are not monotone). The grader's `band_verdict` reads GATE FAIL on both rows and **rule 5 turned it INTO NOT A RESULT, the only permitted direction**. **A completed-and-graded ladder owes this row whatever its verdict** (CLAUDE.md rule 12); refusal is a reading, not an absence of one. | **456.74 core-min** = 7.612 core-h, the registered `cost_core_min_estimate` at `F27_WOMERSLEY_PIPE_PREREGISTRATION.md` line 378 (per-level table lines 371-376: coarse **0.563**, medium **12.028**, fine **444.145**; 704,471,040 cell-steps). Registered cap **680** (line 619 and line 378) = **1.489x** the estimate. **A single registered estimate scalar DOES exist and this row cites it rather than reconstructing one.** Basis: rates **MEASURED on this case**, not imported — three scratch arms on this box at 4 ranks under load 19.66 of 16, marginal cost per step obtained by differencing first and last `ExecutionTime`/`ClockTime` so startup is excluded, registered rate = the larger of the two readings x 4 ranks / cells = **13.09 / 17.48 / 40.34 core-us per cell-step**. **No contention multiplier is applied and the registration says why** (the rate was itself measured under real contention). Dollars **DERIVED**: **$0.39** at the estimate, $0.58 at the cap. **c7a.4xlarge at $0.0513/core-h, REPORTED-BY-OWNER, NOT MEASURED** (`COMPUTE_BUDGET_CHARTER.md` section 5). | **265.4667 core-min**, basis **gross**, MEASURED as `ClockTime x ranks / 60` at **ranks = 4** from each level's `log.pimpleFoam`: coarse **0.4000** (6 s), medium **8.0000** (120 s), fine **257.0667** (3,856 s). Corroborated to every digit by two independent records: the launcher's own tally (`cases/F27_WOMERSLEY_PIPE/launcher.queue.out`, "Cumulative spend: 265.46666666666664 core-min of 680") and the frozen grader's `cost_claim.core_min_claim` / `partial_sum_core_min` in `F27_GRADED.json` (`defects: []`). ExecutionTime basis stated beside it, **265.4267 core-min**. `ClockTime` is integer-second: +/- 0.0333 core-min per level at 4 ranks — which at the 6-second coarse level is 8 % of that level's own figure and is why its ExecutionTime (6.40 s) EXCEEDS its ClockTime (6 s). Dollars **DERIVED, NOT MEASURED**: 4.4244 core-h x $0.0513/core-h = **$0.2270**. | **265.4667 — gross == cleaned, AND THE 3,600-SECOND ROW IS ADDRESSED RATHER THAN PASSED OVER.** One level sits above it: fine at **3,856 wall s**. It is not a stall: 2,688 fixed time steps written, 660,602,880 cell-steps, and `ExecutionTime / ClockTime` = 3,855.13 / 3,856 = **0.99977** — the four ranks delivered CPU for 99.98 % of that wall. **There is nothing to clean. WASTE: 0.000 core-min**, named separately per `COMPUTE_BUDGET_CHARTER.md` section 6 and folded into no ratio in this row — no stall, no kill, no re-run, no cap movement; the ladder ran once and was graded once. **THE `NOT A RESULT` VERDICT IS NOT WASTE**: the compute bought a registered reading and the reading was refusal. Cap use **39.0 %** (265.4667 / 680); no overrun and the cap was never raised. **Disclosed separately and OUTSIDE the cap:** the registration's own scratch spend, **6.733 core-min MEASURED** (rate arms 0.267 / 1.333 / 3.933 plus the coarse physics pilot 1.200) plus **~1.57 core-min ESTIMATED** (an overwritten 30-step probe and serial mesh work), grand total **~8.3 core-min of which 6.733 is measured** — charged against no cap and a result of nothing. | **0.581x** (265.4667 / 456.74) — **41.9 % UNDER the registered estimate.** Per level against its own line 371-376 projection: coarse **0.710**, medium **0.665**, fine **0.579** — the shortfall WIDENS with level, and fine carries **96.8 % of the ladder's spend**. | **MISPREDICTION — AND THE REGISTRATION PREDICTED ITS OWN DIRECTION IN ADVANCE, WHICH IS THE POINT OF THIS ROW.** Section 8 caveat (a), lines 387-390, written BEFORE the run: the fine rate arm ran only **24 steps, from rest**, during the phase where the pressure field changes fastest, so "its iteration count is therefore likely an **over**-estimate, making 456.74 conservative." That is exactly what happened. **MEASURED rates: 9.301 / 11.626 / 23.348 core-us per cell-step** against the registered 13.09 / 17.48 / 40.34 — **every level under, and the shortfall widening (0.711 / 0.665 / 0.579)**, consistent with the caveat's own mechanism since a from-rest transient is a larger fraction of a 24-step arm than of a 2,688-step run and the DIC-PCG pressure iteration count settles as the field does. **The SHAPE of the growth was read correctly and only the LEVEL of the rate was high:** measured per-level growth **1.250x** and **2.008x** against the registered **1.3354x** and **2.3078x**. **CONTENTION: present in the box load, absent from the wall-versus-CPU reading, and named rather than assumed absent.** `box_before.txt` / `box_after.txt` record load1 **11.75 -> 11.87 -> 13.71 -> 13.00 of 16 cores**, and are explicitly labelled "INFRASTRUCTURE observation only: the pre-spend projector does NOT read it"; `ExecutionTime / ClockTime` = **1.067 / 0.9989 / 0.99977**, the coarse figure above 1 being integer-second quantisation on a 6-second run rather than a measurement. **Section 8's decision to apply NO contention multiplier HELD.** **WASTE 0.000, named separately and not laundered into the ratio above.** **CARRY FORWARD:** a rate arm run **from rest** over a handful of steps over-estimates a pulsatile `pimpleFoam` run's steady-state cost by roughly **1.72x**, and the error GROWS with level. Marginal differencing (first-to-last `ExecutionTime` over the arm) removes **startup**; it does NOT remove the **transient**. Those are two different things and this rung separates them. | `verification/campaign/F27_WOMERSLEY_PIPE_RESULTS.md` sections 6 and 7 (introduced by the same commit that lands this row, so no earlier sha exists to cite — disclosed, not approximated); grading artefact `verification/runs/F27_WOMERSLEY_PIPE_runs/F27_GRADED.json` (37,323 bytes, 2026-08-28T16:13Z, `cost_claim` block; this grader writes no `.out` file); per-level `log.pimpleFoam`, `RC.txt`, `MESH_LINE.txt`, `box_before.txt` / `box_after.txt` under `verification/runs/F27_WOMERSLEY_PIPE_runs/`; launcher record `cases/F27_WOMERSLEY_PIPE/launcher.queue.out`; prediction frozen at `verification/campaign/F27_WOMERSLEY_PIPE_PREREGISTRATION.md` section 8 (lines 352-391, estimate scalar at line 378, cap re-asserted at line 619) @ `05ec72c41ee762b49c3dc1d25a6780689b9b9044`. |
| C-197 | 2026-08-28 | cfd | **F26D RINGLEB DISCRIMINATING ARM** (`rhoSimpleFoam`, **1 rank**, three arms A0 / AV / AM x four levels 96 / 384 / 1,536 / 6,144 cells) — a **DIAGNOSTIC, NOT A LADDER**: the observable `N*` is ORDINAL (the coarsest level at which an arm FAILS), so **no triple, no observed order and no GCI exists**, and the grader proves by AST census (control G7: 10 imports, 177 identifiers, none naming roache/gci/richardson) that it structurally cannot produce one. Graded by the frozen `grade_f26d.py` at prereg commit `ed09c9ef`, **rc 0**, with a REAL freeze check printed (`FREEZE VERIFIED ... blob 3e6a6a38c393`). Measured **N*(A0) = L3, N*(AV) = NONE, N*(AM) = NONE**; A0 L3 and L4 died `rc=136` (SIGFPE) at 103 and 166 iterations of 4,000, **which is the registered MEASUREMENT (prereg lines 97-99 register SIGFPE in advance as a FAILURE reading), not a broken run**. **Verdict: `GATE REACHED` — reading `AMBIGUOUS`**, one of four rows fixed before compute (prereg line 137): both probes are sufficient repairs, neither is shown necessary, **no candidate is eliminated**. **F26_RINGLEB REMAINS BLOCKED and the rescope cost is NOT established.** | **6.8 core-min** (prereg line 182); registered cap **10.2** = 1.5 x the estimate (line 183). `cost_basis` as registered: **derived, NOT MEASURED** — 97.92 M cell-iterations at **3.55 us/cell-iteration, a rate read from ONE lab record of a DIFFERENT case** (`verification/runs/FPE_DIAG_runs/BL1/log.simpleFoam`, 3,520 cells, 2,000 iterations, serial, turbulent RANS `simpleFoam`) plus ~1.0 core-min for 12 mesh builds. **The registration itself labelled this "an upper estimate for the completing case: an arm that aborts costs less"** — the exact caveat this row turns on. Dollars **DERIVED**: **$0.0058** at the estimate, $0.0087 at the cap. **c7a.4xlarge at $0.0513/core-h, REPORTED-BY-OWNER, NOT MEASURED** (`COMPUTE_BUDGET_CHARTER.md` section 5). | **3.2500 core-min**, basis **gross**, MEASURED as `ClockTime x ranks / 60` at **ranks = 1** from each of the twelve `log.solve` files: A0 **0.1500** (3+4+0+2 s), AV **1.5667** (3+5+16+70 s), AM **1.5333** (2+3+11+76 s). Corroborated to every digit by two run-root records: `verification/runs/F26D_runs/SPENT_CORE_MIN.txt` reads **3.25** and `PROGRESS.txt`'s twelve cumulative rows close at **3.25**. ExecutionTime basis stated beside it, **3.2603 core-min** (195.62 s). `ClockTime` is integer-second: +/- 0.0167 core-min per run at 1 rank — at A0 L3 it rounds a 0.48-s ExecutionTime abort to **0 s**. Dollars **DERIVED, NOT MEASURED**: 0.05417 core-h x $0.0513/core-h = **$0.002779**. | **3.2500 — gross == cleaned.** No row approaches the 3,600-second stall rule (longest run 76 wall s), so nothing is cleaned and the heuristic is not engaged. **WASTE: 0.000 core-min**, named separately per `COMPUTE_BUDGET_CHARTER.md` section 6 and folded into no ratio in this row. **THE TWO SIGFPE ABORTS ARE NOT WASTE**: they are the registered measurement of the observable, bought deliberately, and calling them waste would misprice the arm's entire purpose. No stall, no kill, no re-run, no cap movement; **no cap-halt marker exists in the run root** (`cap_halt()` returns None), which is also what makes the grader's UNDETERMINED refusal branch inapplicable rather than merely unfired. Cap use **31.9 %** (3.2500 / 10.2); no overrun and the cap was never raised. | **NOT COMPARABLE — and this cell states that rather than reporting a number.** The arithmetic quotient is 3.2500 / 6.8 = **0.478**, and **it is NOT a like-for-like estimate-versus-actual**: the estimate priced **twelve COMPLETE runs**; the actual is **ten complete runs plus two that died at 103 and 166 iterations of 4,000**. A0 L3 and L4 together consumed **2 s of ClockTime** (3.21 s ExecutionTime) where the completing runs at those two mesh sizes cost **11-16 s** and **70-76 s**. Reporting 0.478 as a calibration ratio would credit the estimate for work that was never done. **No like-for-like figure is constructed here**, because doing so would require pricing the two aborts at assumed rates, which is arithmetic on an assumption rather than a measurement. | **NOT COMPARABLE (see the ratio cell) — the gap is dominated by two runs that did not finish, which is the registered outcome and not a cost miss.** What IS measurable and is carried forward is the **imported-rate error the registration flagged against itself**: the estimate used **3.55 us/cell-iteration borrowed from a turbulent RANS `simpleFoam` case**, while the ten completing **`rhoSimpleFoam`** runs measured **1.79 to 3.26 us/cell-iteration at L2, L3 and L4** (L2 1.95 / 2.60 / 3.26, L3 1.79 / 2.60, L4 2.85 / 3.09), with **L1 reading 5.21-7.81 us** where fixed startup on a 96-cell mesh and integer-second quantisation dominate. **The registered 3.55 therefore sits ABOVE every measured L2-L4 rate** — an over-estimate of the per-cell-iteration cost in its own right, on top of the two aborts. **This is the SAME import-a-rate-from-another-case failure the lab has now priced at F21, F22, F18b, F17 and F25** (F17's row measured 0.488-0.590 us/cell-iteration against the SAME 3.55 basis from the SAME `FPE_DIAG_runs/BL1` record, 6.0-7.9x cheaper). **CONTENTION: not separable and not claimed** — no F26D run is long enough for an `ExecutionTime / ClockTime` ratio to mean anything at integer-second resolution (the longest is 76 s), so no contention figure is invented. **WASTE 0.000, named separately and not laundered into the ratio cell.** **A SECOND, NON-COST FINDING RECORDED HERE BECAUSE IT BEARS ON WHAT THIS SPEND BOUGHT:** `grade_f26d.py:441-442` returns from `selftest()` before the graded path, so **all 13 controls (G1-G12 plus the freeze check) are UNREACHABLE from a `--prereg-commit` run** — the graded invocation ran **zero planted-zero controls**. The `--selftest` invocation exits 0 with "13 controls fired, each shown able to fail", including G3/G4's both-ways plant driving a COMPLETING case to FAILED and a FAILING case to COMPLETED through the real on-disk reader; those are evidence about the CODE, not about this graded run's own inputs. **Repair belongs in a successor, not here** (rule 2: post-compute gates are closed; rule 6: the frozen comparator is not edited). | `verification/campaign/F26D_RINGLEB_RESULTS.md` sections 6 and 8 (introduced by the same commit that lands this row, so no earlier sha exists to cite — disclosed, not approximated); **the grader writes NO artefact — its output is stdout only unless `--json` is passed, and it was not**, so the figures here are re-derived by this lane from the twelve `verification/runs/F26D_runs/<arm>/<level>/log.solve` and `RC.txt` files and from the run-root records `SPENT_CORE_MIN.txt`, `PROGRESS.txt`, `LAUNCH_UTC.txt` (2026-08-27T23:51:21Z) and `PREREG_COMMIT.txt`; prediction frozen at `cases/F26_RINGLEB/PREREGISTRATION_F26D_2026-08-27.md` section 6 (lines 179-196) @ `ed09c9ef5f9ca20571105185e5210a1d60f8a231`. |
| C-198 | 2026-08-28 | dafoam | **CURRICULUM-D18** — `DAHisaFoam` on the `Cone_Supersonic` tutorial re-staged as a planar 2-D wedge (11.3099° half-angle, 40,000 cells) at **M 5.0002**; five arms MESH / X-S / F-S / X-P / F-P, two rows (SHIPPED and PATCHED IDWarp), chain launched 2026-08-28T02:18:44Z and `chain=COMPLETE` 03:28:52Z, all five `rc=0`, graded by the frozen comparator at `grader_rc=0`. **Item verdict `PASS`, both rows `PASS`.** A completed-and-graded curriculum item owes this row (CLAUDE.md rule 12). | **182.2 core-min** point, registered band **[60, 450]**, ceiling **785.0 = Σ per-arm caps** (MESH 5.0; X-S/X-P 90.0 each; F-S/F-P 300.0 each), frozen at `cases/dafoam/curriculum_D18_cone_hypersonic/PREREGISTRATION.md:63` @ `dae3dc9d` **before the run root existed**. Per-arm point: MESH 0.2, X 9.0 each, F **82.0 each**. Basis: D17's MEASURED 172.833 core-min on the same mesh, same solver and the same fixed 500 pseudo-steps per primal, **priced UP** on the registered expectation that *"a stiffer shock may need more GMRES work per pseudo-step"* (`:65`). Dollars **DERIVED** at the estimate: **$0.1558**; at the ceiling $0.6712. **`cost_basis`: c7a.4xlarge at $0.0513/core-h, REPORTED-BY-OWNER, NOT MEASURED** — the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` §5). | **129.016 core-min**, basis **gross**, MEASURED as `wall_s × ranks ÷ 60` per arm from `/home/ubuntu/certonomous-runs/CURRICULUM-D18-cone-hypersonic/ledger.txt`: MESH **0.183** (11 s × 1), X-S **6.833** (205 s × 2), F-S **63.933** (1,918 s × 2), X-P **6.100** (183 s × 2), F-P **51.967** (1,559 s × 2). Corroborated independently by the frozen grader's own tally, `gates.G10_caps.total_core_min` = **129.016** with `not_measured: []`, in `D18_grade_20260828T032852Z.json`. Dollars **DERIVED, NOT MEASURED**: 2.1503 core-h × $0.0513/core-h = **$0.1103**. | **129.016 — gross == cleaned.** The 3,600-second stall rule matches **no row**: the longest arm is F-S at **1,918 wall s**, 53 % of the threshold, and every arm's delivered-core mean is ≥ 1.9867 of its 2 registered ranks (MESH excepted, `NOT_MEASURED`, an 11-second arm below the sampler's first tick — named, not estimated). There is nothing to clean. | **0.708×** (129.016 / 182.2) — **29.2 % UNDER the registered point**, and inside the registered [60, 450] band (P6 `HIT`). Cap use **16.44 %** of the 785.0 ceiling; **no per-arm cap was crossed** (`crossed: false` on all five; worst arm F-S at 21.3 % of its 300.0). | **MISPREDICTION IN THE CONSERVATIVE DIRECTION, CONCENTRATED IN THE TWO F ARMS, AND THE REGISTERED ASSUMPTION THAT CAUSED IT IS NAMEABLE.** `PREREGISTRATION.md:65` priced each F arm at **82.0 core-min — deliberately ABOVE D17's measured 75.733 / 82.000 — on the stated expectation that a stiffer Mach-5 shock would need more GMRES work per pseudo-step.** **It did not.** F-S came in at 63.933 (**0.780×**) and F-P at 51.967 (**0.634×**); together they account for **48.13 of the 53.18 core-min shortfall, 90.5 %** of it. X-S 0.759×, X-P 0.678×, MESH 0.915×. **Calibration lesson, and it is the reusable one: on a dual-time solver with a FIXED pseudo-step count, the step count bounds the wall and Mach does NOT buy extra per-step work — do not price a Mach increase upward on a fixed-step primal.** D17's measured figure was the right anchor and the upward adjustment on top of it was the whole error. **CONTENTION: PRESENT, SECOND-ORDER, MEASURED AND NAMED SEPARATELY rather than folded into the ratio.** The ledger's `siblings_pre`/`siblings_post` show MESH, X-S and F-S each ran beside live `so1a` and `av2r` containers while **X-P and F-P ran with no siblings at all**, and the two unshared PATCHED arms are the two cheapest against prediction (0.678× / 0.634× vs their SHIPPED twins' 0.759× / 0.780×); the delivered-core means corroborate directly — 1.9867 (X-S) and 1.9880 (F-S) against 1.9910 (X-P) and 1.9966 (F-P) of 2.0. So roughly a 12–15 % S-vs-P spread is contention on the SHIPPED half; the first-order cause of the 0.708× remains the F-arm misprediction above. **WASTE: 0.000 core-min, named separately per `COMPUTE_BUDGET_CHARTER.md` §6 and folded into NEITHER column NOR the ratio** — no arm was killed, re-run, truncated or discarded, all five completed `rc=0` and all five were graded once. **A COST CREDIT REGISTERED IN ADVANCE AND HONOURED: 3.05 core-min of pre-compute probe cost was NOT billed** here (`:65`), because D17 had already established both images carry `hisa` and the DAFoam wrapper and D18 re-used that reading rather than re-buying it — named, not silently absorbed. **AND ONE NON-COST DEFECT, NAMED AND REFERRED, THAT THIS ROW DOES NOT PRICE:** the item's P7 discrimination prediction scored `MISS` at S/N 19.8691, but `d18_grade.py:585-589` draws the numerator over all five registered components with **no verdict filter** while the denominator excludes ungraded ones, so the entire signal is the 64.3953 % divergence at **`shape[3]` — the component both rows graded `NOT A RESULT` (`NO_PLATEAU`)**; over the four graded components the same formula gives 0.1383. **The frozen comparator is NOT edited (rules 2 and 6); P7 stays `MISS`; a grader-composition repair is owed to a successor item and is referred to `dafoam-supervisor`, not decided here.** | `cases/dafoam/curriculum_D18_cone_hypersonic/RESULTS.md` §9 (introduced by the same commit that lands this row, so no earlier sha exists to cite — disclosed, not approximated); ledger `/home/ubuntu/certonomous-runs/CURRICULUM-D18-cone-hypersonic/ledger.txt`; grading artefact `D18_grade_20260828T032852Z.json` (14,046 B, 2026-08-28T03:28:52Z, `gates.G10_caps` block); chain records `STATUS.chain` and `CHAIN_DONE` in the same run root. Pre-registration frozen `dae3dc9d17dab4ecda7d44e30c9a3016637f9987` (addendum A `ed4983bd`, pre-compute, moves no cost); comparator `d18_grade.py` md5 `e4ade11ed9e3db18d2c4988b30e929b4` verified **identical disk == HEAD blob** at this write. |
| C-199 | 2026-08-28 | ansys-verification | **VMFLGPU007-R2** — GPU solver path, turbulent flow with heat transfer over a backward-facing step (VM2026R1 p. 243, CPU parent VMFL013); a re-registration of VMFLGPU007 (R1) under `ANSYS_VERIFICATION_CHARTER` §6 that moved ONLY `endTime`; six solves (L1/L2/L3 × GPU arm + forced-CPU control), **GRADED by the frozen comparator at exit 0** and registered as row **#43 `GATE REACHED`** (limb B holds, limb C inside its band). **A completed-and-graded run owes this row** (CLAUDE.md rule 12). | **0.6780 GPU-h and 13.5104 core-min**, frozen in `PREREGISTRATION.md` §9 at freeze `ed980d33` before compute and **carried BYTE-IDENTICAL from R1**. Caps 1.5 GPU-h and 90 core-min, also carried byte-identical from R1. | **GPU arms 0.467222 GPU-h** (`total_wall_s` 1682; per-arm GPU wall 151 / 291 / 646 s); **CPU arms 9.7833 core-min** (per-arm 36 / 114 / 437 s at ranks = 1). Read from `COST.txt` and the six `RUN_RC.*`. | **Same as gross — cleaned == gross.** No stall (the longest single solve is 646 wall s, far under the 3600-wall-s stall threshold, which in any case identifies a stall by cross-family wall-time clustering that no per-solver cost explains, not by the threshold alone), no re-run, no truncation, **`cap_fired = 0` on all six arms — no cap fired by either mechanism** (0.467222 GPU-h = 31.15 % of the 1.5 GPU-h cap; 9.7833 core-min = 10.87 % of the 90 core-min cap). | **GPU 0.6892×** (0.467222 / 0.6780) and **CPU 0.7241×** (9.7833 / 13.5104) — **both UNDER, both within 31 %, BY FAR THIS FAMILY'S BEST CALIBRATION.** | **MISPREDICTION, FAVOURABLE, AND THE CALIBRATION LESSON THIS ROW EARNS IS ABOUT COST-MODEL LOCALITY — not contention and not waste.** The estimate landed within 31 % on both arms because **it was built from R1's OWN MEASURED run of the SAME case on the SAME meshes** — the R1 `endTime` change is a pure horizon extension, so R1's per-iteration cost carried forward almost exactly. Contrast `C-182` (the R1 row itself), which missed by 0.4385× / 0.1133× — 2–9× — because it TRANSFERRED VMFLGPU002's per-iteration model from a **different solver (`simpleFoam` vs `buoyantSimpleFoam`) and a different mesh family**, and even inverted the GPU/CPU direction. **The reusable rule: a per-iteration cost model is ACCURATE WITHIN A CASE and UNRELIABLE ACROSS ONE** — price the next horizon of a case from that case's own measured run, never carry a model across a solver or mesh-family boundary. Sibling to `C-182`'s finding that the GPU/CPU ratio must be measured per case: here again a GPU arm SLOWER than the CPU arm (151/291/646 vs 36/114/437 s) passed every limb — the GPU path is a correctness vehicle, not a speedup, at these mesh sizes (corroborates C-182, row #39). **WASTE 0.000, named separately per `COMPUTE_BUDGET_CHARTER.md` §6** — all six solves completed and were graded, nothing thrown away; GPU idle before/after the run (767.18 min ≈ 12.786 GPU-h before; idle again from 16:35:52Z) is infrastructure, boarded on its own, and is **NOT folded into either ratio here**. **No `CAP_OVERRUN`/`CAP_EXCEEDED` file exists for this case** — proven by `find` over the run root and case directory (contrast `C-193`/VMFLGPU005, which carried a mis-scoped launch-level overrun file). | `cases/ansys_verification/VMFLGPU007-R2/RESULTS.md`; register row **#43**; artifacts `verification/runs/ansys_verification/VMFLGPU007-R2/COST.txt` and `RUN_RC.{L1,L2,L3}.{cpu,gpu}`, and the committed `GRADE_OUTPUT_2026-08-28.txt`. **Dollars DERIVED, NOT MEASURED** at the published-list **$0.8048/GPU-h** (g6.xlarge us-east-2): **$0.3760 derived** — the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` §5) and the console figure is owed and supersedes. Prereg frozen `ed980d33`; comparator `aae498976139cd8489c9b3de48427f1f425f875c` verified identical disk == HEAD == freeze == instance. |
| C-200 | 2026-08-28 | heat-transfer | **T17_CY (axisymmetric transient conduction in a finite cylinder, T11d, Robin surfaces, EXACT tier; `laplacianFoam`, Euler, Gauss linear corrected, PCG/DIC 1e-12, **1 rank**, a 1-degree wedge in (r,z)) -- COMPLETE FOUR-LEVEL RUNG, GRADED.** Levels 2,500 / 10,000 / 40,000 cells at 20,000 steps plus a 40,000-cell temporal-refinement arm `f_CT` at 40,000 steps (deltaT halved 1.0e-4 -> 5.0e-5). All four rule-4 COMPLETE: `rc=0`, one `End`, `Time` count == `ExecutionTime` count == 20,000 / 20,000 / 20,000 / 40,000 == endTime/deltaT, fields at `2` present and `capped=no` on every level. Graded by the frozen `analyse_t17.py`: **three rows `PASS`, all three triples `CONVERGING`** -- G1 p 1.999864835665163 GCI 2.958251486713521e-04 %, G2 p 2.2636 GCI 4.9703e-04 %, G3 p 2.0091 GCI 1.6281e-03 % -- with three planted-zero controls firing (`PLANT` 0.001234 recovered at 0.001234 / 0.00323925 / 0.00277573 against a 1e-07 floor), so the zero in the deviation channel is a measurement. **Rung ceiling is registered as `GATE REACHED` at best** (`T17_registered.json.ceiling`: the reference is an EXACT series, so under the upheld V/P ruling this rung scores V and can never reach HOLDS) -- stated here so the row is not read as a capability claim. **A completed-and-graded rung owes this row** (`CLAUDE.md` rule 12). | **7.244 core-min** POINT (`T17_registered.json` `cost.point_total_core_min`, and the sum of the four `cost_core_min_estimate` fields in the landed queue entries `verification/queue/heat-transfer/launched/T17_CY_{c,m,f,f_CT}.json`: **0.137 / 0.547 / 2.187 / 4.373**). Registered cap **100 core-min** (`cost.cap_total_core_min`) = **13.8x** the point; per-level caps **2 / 8 / 30 / 60** (`cases.<level>.cap_core_min`), enforced as the wrapper timeouts 120 / 480 / 1800 / 3600 s at 1 rank. Frozen at prereg commit `b8ebddfef2f0d1c64450b8b93cf42709d485511f`, `docs/campaigns/T-family/T17_PREREGISTRATION.md`. **Basis: the rate 1.64e-07 core-s per cell-step is BORROWED, NOT MEASURED ON THIS RUNG**, and the registration says so in `cost.rate_provenance` -- it is T14's rate, MEASURED 2026-08-26T21:03Z on a **scratch copy of `T14_SQ_c`** (2,500 cells, 2,000 steps, `ExecutionTime` 0.82 s -> 0.82 / 5.0e6 = 1.64e-07). Each point is that rate x cell-steps (5.0e7 / 2.0e8 / 8.0e8 / 1.6e9), so **`ratio` in this row IS `measured rate / registered rate`** and nothing else. The registration pre-registered its own expected miss: *"within a factor of 1.5 either way"*. Dollars **DERIVED, NOT MEASURED**: **$0.006194** at the point, **$0.0855** at the cap. **c7a.4xlarge at $0.0513/core-h, REPORTED-BY-OWNER** -- the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` section 5). | **14.683 core-min**, basis **gross**, MEASURED as `wall_s x ranks / 60` at **ranks = 1** from the four wrapper-written run-root STATUS files: `T17_CY_c` **0.233** (14 s), `T17_CY_m` **0.917** (55 s), `T17_CY_f` **4.683** (281 s), `T17_CY_f_CT` **8.850** (531 s). Every figure re-derived by this lane from the STATUS files themselves, not transcribed. **An `ExecutionTime` basis is stated beside it, from the four `log.solve` files: 13.73 / 54.79 / 278.86 / 486.74 core-s = 13.7213 core-min**, i.e. **6.6 % below the gross figure**, and the two bases are NOT interchangeable on this rung -- see the contention reading in the attribution cell. `wall_s` is integer: +/- 0.5 s per level at 1 rank, which is +/- 0.0083 core-min and is 3.6 % of the coarse level's own figure. Dollars **DERIVED, NOT MEASURED**: 0.244717 core-h x $0.0513/core-h = **$0.012554** (per level $0.000199 / $0.000784 / $0.004004 / $0.007567). | **14.683 -- gross == cleaned, and the 3,600-second stall rule is addressed rather than passed over.** **No level approaches it**: the longest wall is `f_CT` at **531 s**, 14.8 % of the threshold, so the rule matches no row and nothing is removed. **WASTE: 0.000 core-min**, named separately per `COMPUTE_BUDGET_CHARTER.md` section 6 and folded into neither column nor the ratio -- no level was capped (`capped=no` on all four), none was killed, re-run, truncated or discarded; the ladder ran once and was graded once. Cap use **14.68 %** of the 100 core-min rung cap; worst per-level cap use **15.61 %** (`f`, 4.683 of 30). No overrun, so rule 12's stop clause was never in play and no cap was raised. | **2.027x** (14.683 / 7.244) -- **102.7 % OVER the registered point**, and **outside the registration's own pre-registered "within a factor of 1.5 either way" band.** Per level: **c 1.701** (0.233/0.137), **m 1.676** (0.917/0.547), **f 2.141** (4.683/2.187), **f_CT 2.024** (8.850/4.373). | **MISPREDICTION OF A BORROWED RATE -- AND THE CAUSE IS NOW MEASURED, WHICH REQUIRED KILLING TWO EARLIER HYPOTHESES OF THIS TEAM'S OWN. BOTH DEAD ONES ARE RECORDED HERE BECAUSE A LEDGER THAT ONLY KEEPS THE SURVIVING STORY CANNOT BE AUDITED.** **(1) DEAD -- "the ratio grows with problem size".** FALSIFIED AT ITS OWN LARGEST POINT: `f_CT` is exactly 2x `f` in cell-steps (1.6e9 vs 8.0e8) and its ratio is LOWER (2.024 vs 2.141). Spearman rho between cell-step rank and ratio rank is **0.60 on n = 4, two-sided p ~ 0.42** -- no trend is establishable from four points even had the ordering cooperated. **(2) DEAD AS STATED -- "a two-group split by mesh, 10k-and-below ~1.69x against 40k ~2.08x, driven by PCG iteration count".** The split is a true DESCRIPTION of the four numbers, and the iteration counts are real and were re-measured by this lane from the `Solving for T` lines of each `log.solve`: **3.0026 / 4.0114 / 6.0412 / 4.1562** mean PCG iterations per timestep, with halving deltaT making each linear system easier. But the MECHANISM does not survive the control below, and a ratio explained by the wrong mechanism is worth less than an unexplained one. **(3) THE CONTROL THAT SETTLES IT, AND IT IS A LIKE-FOR-LIKE COMPARISON THIS LEDGER DID NOT PREVIOUSLY HAVE.** The borrowed rate's own case, `T14_SQ_c`, ALSO RAN ITS FULL LADDER LEVEL: 2,500 cells, 20,000 steps, mean PCG iterations **3.0023**, `ExecutionTime` **6.89 s** (`verification/runs/T-family/T14_runs/T14_SQ_c/log.solve`). `T17_CY_c` is **2,500 cells** (`T17_registered.json` `cases.T17_CY_c.cells`, corroborated on disk by `log.checkMesh`: `cells: 2500`), 20,000 steps, mean PCG iterations **3.0026**, `ExecutionTime` **13.73 s**. **IDENTICAL cell count, IDENTICAL step count, IDENTICAL mean iteration count to four significant figures -- and T17 costs 1.993x per cell-step.** The I/O confound points the WRONG way and is therefore excluded rather than assumed away: both write the same six fields (`T`, `DT`, `flux`, `gradTx/y/z`), neither has a `functions` block, and **T14 writes MORE often** (`writeInterval` 0.5 against T17's 1.0). So the excess is not iterations, not cell count, not step count and not output volume; it is **the wedge mesh topology itself** -- which is precisely and only the difference `T17_registered.json` `cost.misprediction_risk` named in advance (*"a wedge whose cells have two zero-flux wedge faces and whose innermost cells are degenerate"*). The registration named the right variable and then under-priced it by a factor of four: it expected at most 1.5x and topology alone delivered **1.993x**. **(4) A SECOND, OPPOSITE-SIGNED ERROR IS HIDING INSIDE THE SAME NUMBER, AND IT CORRECTS A CLAIM THIS TEAM MADE EARLIER TODAY.** It was argued upward that `T17_CY_c` is *"EXACTLY the mesh the T14 rate was measured on"*, so its 1.701x must be a fixed offset present at zero mesh jump. **THAT IS HALF RIGHT AND THE WRONG HALF IS LOAD-BEARING.** `T17_CY_c` has the same CELL COUNT as the measurement point, not the same MESH: T14 is `dim = 2`, a plane square (`T14_registered.json` `dimensionality: 2`); T17 is axisymmetric on a 1-degree wedge (`dimensionality: "axisymmetric (2-D in (r,z) on a 1-degree wedge)"`). The rate was measured on `T14_SQ_c`, so **the true zero-jump evaluation point is `T14_SQ_c`'s own full run, and it reads 0.840x** (6.89 core-s against the registered 8.20), **not 1.0x** -- because the rate arm was a **2,000-step scratch run** and its unamortised startup inflated the rate by **1.190x**. **THE 1.7x THEREFORE DECOMPOSES EXACTLY, AND THE ARITHMETIC CLOSES:** 1.9927 (wedge-versus-square at identical cells, steps and iterations) x 0.8402 (the rate arm's over-read of its own case) = **1.6744**, which IS `T17_CY_c`'s ratio on the `ExecutionTime` basis (13.73 / 8.20). The published 1.701 is the same quantity on the integer `wall_s` basis. **So there is no unexplained offset. There are two named errors of opposite sign, one 1.99x expensive and one 1.19x conservative, and they were being read as a single mystery constant.** **CONTENTION: PRESENT, MEASURED, AND CONCENTRATED IN ONE LEVEL.** `ExecutionTime / wall_s` = **0.9807 / 0.9962 / 0.9924 / 0.9166**. The first three levels delivered CPU for 98-100 % of their wall; **`f_CT` delivered 91.7 %**, and the STATUS timestamps say why -- `f` ran 16:17:01Z-16:21:42Z and `f_CT` ran 16:18:06Z-16:26:57Z, **overlapping for 3 min 36 s**, beside the two live foreign solvers `T3_R_ff` and `T16_MC_f`. On the `ExecutionTime` basis `f_CT`'s ratio falls from 2.024 to **1.855**, which **shrinks the f-versus-f_CT gap the dead hypothesis (2) was built to explain from 5.8 % to 12.6 % in the other direction** -- another reason that mechanism is not carried forward. Contention is named here and is **NOT** folded into the 2.027x, which stays on the gross basis the charter's unit specifies. **WASTE 0.000, named separately and laundered into nothing.** **CARRY FORWARD, and it is the transferable finding: a rate measured on a 2,000-step scratch arm over-read its OWN case's 20,000-step run by 1.19x, and a rate borrowed across a change of MESH TOPOLOGY at CONSTANT cell count, step count and iteration count under-read by 1.99x. Startup and topology are two different errors with two different signs; a single scalar correction factor cannot carry both, and this row quotes none.** | Actuals: `verification/runs/T-family/T17_runs/STATUS.T17_CY_{c,m,f,f_CT}` and `DONE.T17_CY_{c,m,f,f_CT}`; per-level `log.solve` and `log.checkMesh` under `verification/runs/T-family/T17_runs/T17_CY_{c,m,f,f_CT}/`. Points and caps: `verification/queue/heat-transfer/launched/T17_CY_{c,m,f,f_CT}.json` `cost_core_min_estimate` / `cap_core_min_registered` (**these five queue entries are UNTRACKED on disk -- disclosed, and referred as its own docket row rather than fixed here**). Registration `verification/runs/T-family/T17_runs/T17_registered.json` frozen at `b8ebddfef2f0d1c64450b8b93cf42709d485511f`, prose `docs/campaigns/T-family/T17_PREREGISTRATION.md`. Grading `verification/runs/T-family/T17_runs/gate_t17.json` and `T17_GRADE_OUTPUT.txt`. Control case `verification/runs/T-family/T14_runs/T14_SQ_c/log.solve` and `T14_registered.json` `cost.rate_scratch_measured_core_s_per_cell_step`; rate provenance also at `docs/campaigns/T-family/T14_PREREGISTRATION.md:163`. Drafts reconciled, not concatenated: `verification/runs/T-family/T17_runs/T17_COST_CALIBRATION_ROW_DRAFT.md`. Docket claim `D562`. |
| C-201 | 2026-08-28 | heat-transfer | **T18_CU (3-D transient conduction in a cube, T11c, Robin faces on all three axes, EXACT tier; `laplacianFoam`, same schemes and linear solver as T17, **1 rank**) -- PARTIAL: THE COARSE LEVEL ONLY, AND NO GATE HAS BEEN EVALUATED.** `T18_CU_c` (8,000 cells, 20,000 steps) is rule-4 COMPLETE: `rc=0`, one `End`, 20,000 `Time` == 20,000 `ExecutionTime` == endTime/deltaT, `capped=no`, `checkmesh_rc=0`, `note=clean`, `log.checkMesh` reads `cells: 8000`. **`T18_CU_m`, `T18_CU_f` and `T18_CU_f_CT` HAVE NOT RUN**; the frozen `analyse_t18.py` **REFUSED** rather than degrading -- `T18_GRADE_OUTPUT.txt` reads *"REFUSE: no DONE.T18_CU_m -- the whole rung is graded or none of it is"*, exit status 2 captured from `$?` and never through a pipe. **This row therefore carries NO verdict from the fixed vocabulary**, because none has been earned: rule 5 needs three grid levels and there is one. It is filed because **a LEVEL completed**, and rule 12's calibration bullet is owed at every process completion, not only at a rung's close. | **0.437 core-min** POINT (`T18_registered.json` `cases.T18_CU_c.point_core_min`, and `cost_core_min_estimate` in the landed queue entry). Registered cap **3 core-min** = **6.9x** the point, enforced as a 180-s wrapper timeout at 1 rank. Rung-level point **87.902**, rung cap **383** -- **neither is used as this row's basis**, because three of the four levels have not run and comparing a one-level actual against a four-level point would be arithmetic on absent work. Frozen at prereg commit `add2c78870625c75c2a48bdff018d7a034a2ff84`, `docs/campaigns/T-family/T18_PREREGISTRATION.md`. **Basis: the SAME BORROWED 1.64e-07 core-s per cell-step as T17** -- T14's scratch-arm rate, and `T18_registered.json` `cost.rate_provenance` says "BORROWED, NOT MEASURED ON THIS RUNG". The point is that rate x 1.6e8 cell-steps = 26.24 core-s. **`cost.misprediction_risk` pre-registered a ONE-SIDED expectation in advance: an UNDER-prediction "by a factor of up to about 3"**, for a 2-D -> 3-D dimension change (4-neighbour to 6-neighbour stencil) and a mesh jump. Dollars **DERIVED, NOT MEASURED**: **$0.000374** at the point, $0.002565 at the cap, at $0.0513/core-h c7a.4xlarge, **REPORTED-BY-OWNER** (`COMPUTE_BUDGET_CHARTER.md` section 5). | **0.533 core-min**, basis **gross**, MEASURED as `wall_s x ranks / 60` = 32 s x 1 / 60 from `verification/runs/T-family/T18_runs/STATUS.T18_CU_c`, re-derived by this lane from that file. **`ExecutionTime` basis stated beside it: 30.31 core-s = 0.5052 core-min**, 5.3 % below gross. `wall_s` is integer: +/- 0.5 s = +/- 0.0083 core-min, **1.6 % of this level's own figure**. Dollars **DERIVED, NOT MEASURED**: 0.008883 core-h x $0.0513/core-h = **$0.000456**. | **0.533 -- gross == cleaned.** The 3,600-second stall rule **cannot match a 32-second run**, so nothing is removed and the heuristic is not engaged. **WASTE: 0.000 core-min**, named separately per `COMPUTE_BUDGET_CHARTER.md` section 6 and folded into neither column nor the ratio -- `capped=no`, `rc=0`, `note=clean`, no stall, no kill, no re-run, no cap movement. Cap use **17.77 %** (0.533 / 3). No overrun; rule 12's stop clause was never in play. | **1.220x** (0.533 / 0.437) -- an under-prediction, **and the registration's pre-registered SIGN was right while its MAGNITUDE was over-reserved by more than a factor of two** (it named "up to about 3"; the level came in at 1.220). On the `ExecutionTime` basis the same quantity is **1.155x**. | **THE SIGN WAS PRE-REGISTERED AND HELD; THE MAGNITUDE WAS OVER-RESERVED; AND THE INTERESTING FINDING IS THAT THIS LEVEL DISAGREES WITH T17 BY 39 % ON A SHARED BORROWED RATE.** **(1) THE LEVEL, NOT THE SLOPE, IS THE DISAGREEMENT.** 1.220x sits **BELOW T17's entire range [1.676, 2.141]**, on the same solver, the same schemes, the same linear solver and the SAME 1.64e-07 rate. Whatever T17's ratios measure does not transfer to T18 as a level. **(2) CONTENTION DEEPENS THE DISAGREEMENT INSTEAD OF EXPLAINING IT AWAY, AND THE FIGURE IS NOW MEASURED RATHER THAN ASSERTED.** `ExecutionTime / wall_s` = 30.31 / 32 = **0.9472**: 5.3 % of this run's wall was not this run's CPU, against the two live foreign solvers `T3_R_ff` and `T16_MC_f` (the run occupied 16:29:56Z-16:30:28Z). Contention inflates wall, so the **true uncontended ratio is 1.220x or LOWER** -- 1.155x on the CPU basis -- **further below T17's floor, not nearer it.** **(3) A MECHANISM FOR THE DISAGREEMENT, MEASURED, THAT WAS NOT AVAILABLE WHEN THE 39 % WAS FIRST REPORTED.** Mean PCG iterations per timestep, re-measured by this lane from the `Solving for T` lines of `T18_CU_c/log.solve`: **2.0143**, against `T17_CY_c`'s **3.0026** on the same solver. Per cell-step against the control case `T14_SQ_c` (2,500 cells, 20,000 steps, 3.0023 iterations, `ExecutionTime` 6.89 s -> 1.378e-07): T18_CU_c measures **1.894e-07 = 1.375x**, where `T17_CY_c` measures **2.746e-07 = 1.993x**. **So the 3-D cube is 1.4x its 2-D square reference per cell-step while the axisymmetric wedge is 2.0x it -- the dimension change the registration priced at "up to 3" cost 1.4x, and the wedge topology the T17 registration priced at "1.5 either way" cost 2.0x. The rungs disagree because the two mesh types are genuinely different costs, not because the borrowed rate is unstable.** This is a two-point reading and is offered as a mechanism, not a law. **(4) THE FALSIFIABLE PREDICTION, CARRIED FORWARD FROM THE DRAFT AND RESTATED HERE SO IT CANNOT BE CHOSEN TO FIT THE ANSWER.** A lane registered it BEFORE `T18_CU_m` runs: **if growth with size is real and T18 merely carries a smaller offset than T17, `T18_CU_m` (64,000 cells, 8x the coarse level) comes in ABOVE 1.220x; at or below 1.220x, the T17 pattern is a property of THAT WEDGE and not of the borrowed rate.** `T18_CU_m` is armed in the drop path and HELD by the runner's 85 % ceiling; the ceiling is not raised to settle this sooner. Either outcome is a calibration finding and neither is a defect. **NOTE that the T17 row landed beside this one weakens the growth arm of that prediction in advance and does not withdraw it:** T17's growth hypothesis is already falsified at its own largest point, and its coarse-level excess is now decomposed into a startup error and a topology error rather than a size effect. **WASTE 0.000, named separately and laundered into nothing.** **A GAP REPORTED RATHER THAN FILLED: `T14_SQ`, the rung this row's control case belongs to and whose scratch arm supplied the borrowed rate to BOTH T17 and T18, has NO row in this ledger either** (measured: zero `T14_SQ` matches over `git show HEAD:docs/COST_CALIBRATION.md`). That is a third overdue rule-12 row, outside this lane's claim `D562`, and it is reported for routing rather than drafted here -- drafting a shared-ledger row for an unclaimed item is the exact duplicate risk `D562` exists to prevent. | Actual: `verification/runs/T-family/T18_runs/STATUS.T18_CU_c` (`rc=0`, `wall_s=32`, `ranks=1`, `core_min=0.533`, `capped=no`, `checkmesh_rc=0`, `note=clean`, `solver=laplacianFoam`) and `DONE.T18_CU_c`; `verification/runs/T-family/T18_runs/T18_CU_c/log.solve` and `log.checkMesh`. Point and cap: `verification/runs/T-family/T18_runs/T18_registered.json` `cases.T18_CU_c.point_core_min` / `cap_core_min` and `cost.misprediction_risk`, frozen at `add2c78870625c75c2a48bdff018d7a034a2ff84`; queue entry `verification/queue/heat-transfer/launched/T18_CU_c.json` (**UNTRACKED on disk -- disclosed, referred as its own docket row, not fixed here**). Refusal: `verification/runs/T-family/T18_runs/T18_GRADE_OUTPUT.txt`. Control case `verification/runs/T-family/T14_runs/T14_SQ_c/log.solve`. Draft reconciled, not concatenated: `verification/runs/T-family/T18_runs/COST_CALIBRATION_ROW_DRAFT.txt` (its docket reference `D552` predates this lane; this row is claimed under `D562`). |
| C-202 | 2026-08-28 | cfd | **F23_HP_WEDGE** — Hagen–Poiseuille pipe on an axisymmetric WEDGE (`simpleFoam`, streamwise cyclic, fixed body force G = 0.32, Re_D = 100, 4 ranks), three-level ladder 64×2048 / 128×4096 / 256×8192 at 4000 fixed iterations. **Rung verdict `NOT A RESULT`** on two independent grounds: the ladder is incomplete (fine never reached the solver) AND the two levels that did run were neither iteratively converged nor plateaued. A completed process owes this row whether or not it produced a verdict worth having (CLAUDE.md rule 12). | **75.4 core-min** — the TWO-LEVEL basis: coarse **9.7** + medium **65.7** from the prereg §7 table, frozen at `058e77719c57332aff245e58d536a343cbde49e5` before the run root existed. The fine level's registered **444.0** is **EXCLUDED**: no compute was ever spent against it, so it has no actual to compare with. Full registered ladder 519.4 core-min; registered **CAP 1,100**. Rate basis **DERIVED, NOT MEASURED on this case**: F17's measured 0.59 µs/cell-iteration on a *different* case, grown +30 %/doubling. Dollars at the estimate $0.44, at the cap $0.94 — **DERIVED**, `cost_basis: c7a.4xlarge at $0.0513/core-h, REPORTED-BY-OWNER, NOT MEASURED` (`COMPUTE_BUDGET_CHARTER.md` §5). | **194.7333 core-min**, MEASURED as `ClockTime × ranks ÷ 60` at ranks = 4: coarse 564 s → **37.6000**, medium 2,357 s → **157.1333**, fine **0** (never launched). Read from each level's own `log.simpleFoam` and corroborated digit-for-digit by the launcher's running tally in `cases/F23_HP_WEDGE/launcher.queue.out`. **ExecutionTime basis stated beside it: 194.3933** ((559.51 + 2,356.39) × 4 ÷ 60). ClockTime is integer-second: ± 0.0333 core-min per level. Dollars: 3.24556 core-h × $0.0513 = **$0.1665 — DERIVED, NOT MEASURED**. | **194.7333 — cleaned == gross.** No level exceeded 3,600 wall s (max 2,357 s at medium), so charter §2's stall rule matches no row and there is nothing to clean. ExecutionTime/ClockTime = 0.99204 coarse, 0.99974 medium — the four ranks delivered CPU for essentially all of their wall. **Share of cap 17.70 %; the cap was not crossed, not approached and never raised** (the launcher's own pre-fine projection read `cumulative would be 851.405792 of 1100`, HALT = 0; the halt path is `exit 3` at `run_f23.sh:215,251` and did not fire — the observed `launcher_rc=1` is the build-failure path at `run_f23.sh:219-220`). | **2.583** (194.7333 / 75.4); per level **coarse 3.876** (37.600 / 9.7), **medium 2.392** (157.133 / 65.7) | **WASTE, NAMED SEPARATELY AND KEPT OUT OF THE RATIO (charter §6): 194.7333 core-min — the ENTIRE spend.** This rung spent 194.73 core-min and produced **no result**: two levels of unconverged startup transient that no gate can read. Distinguish from C-4, where a crash at iteration 182 was *not* waste because the crash **was** the measurement; here nothing the registration set out to measure was measured, and what the results record's §3.4 establishes about the SIMPLE contraction rate was extracted from the wreckage after the fact. **TWO GAP CAUSES, DIFFERENT IN KIND, and the second is not a cost error at all.** **(a) MISPREDICTION of the rate** — the registration priced F23 from **F17's** measured rate on a *different* case (0.59 µs/cell-iteration, Cartesian) grown +30 %/doubling; measured on F23's own levels **4.303 and 4.496 µs/cell-iteration** against registered 1.112 and 1.879. The base rate ran low on an axisymmetric wedge. Note the growth term erred the *other* way: measured level drift **1.0448 = +2.2 %/doubling against +30 % modelled** — the same over-modelled exponent priced at C-4/C-194, but here sitting on a base rate ~4× too low, so the two errors did **not** cancel. The registration itself named the better anchor and did not use it: prereg §7 discloses VMFL005's **measured 2.1 µs/cell-iteration** on the lab's other `simpleFoam` wedge and claims the growth term *'places every F23 level at or above that reading'* — **false on its own table**, since registered coarse 1.112 and medium 1.879 both sit BELOW 2.1, while both measured rates sit above it. **(b) REGISTRATION DEFECT — NOT a cost misprediction and must not be filed as one.** (i) The **iterative floor was 3–4 orders too loose for the iteration count bought**: 4,000 iterations was sized against a 1,024-cell instrument run, while the registered levels need ~2×10⁴ / ~9×10⁴ / ~4×10⁵ (measured SIMPLE decay rates 8.1883e-04 and 2.0706e-04 per iteration, ratio 3.955 ≈ h²; worst in-census `Ux` initial residual 7.33e-05 and 2.65e-04 against a registered ≤ 1e-08 floor that **no iteration of either run ever reached**; Class C relative drift 5.5988e-02 and 2.0552e-01 against a 2e-04 trend tolerance). **A perfectly-predicted 519.4 core-min ladder would have produced three non-results at three times the price.** (ii) The **mesh guard was refinement-fragile by construction**: a *fixed absolute* 1e-6 tolerance (`build_f23.py:129-130`) on a `checkMesh` wedge angle that grows ~3× per level (0.28× / **0.80×** / 2.72× of budget), with `checkMesh` itself reporting `Mesh OK.` at all three levels; medium already sat at 80 % of budget and nobody computed the third value at registration or at run time. **A row reporting 2.583 without these would say the estimate was merely optimistic; it was not — the rung was unrunnable as registered.** **CONTENTION: present, not separable.** load1 12.92 → 15.06 → 14.00 of 16 cores, free cores 3.08 → 0.94 → 2.00 (`box_before/after.txt`); the measured rates already carry that load and no separable figure can be extracted, so none is invented. **CAP ARITHMETIC CORRECTED — this row supersedes an earlier supervisor reading of ≈ 1,265 core-min that put the ladder OVER its 1,100 cap.** That figure is **withdrawn as a DOUBLE COUNT**: it multiplied the *registered* fine estimate by the 2.583× over-run, re-applying to that estimate the very rate misprediction it already contained. Rebuilt from the **measured** medium rate, the fine level is **656.67 core-min** and the ladder **851.41 = 77.4 % of cap** at the case's own measured drift 1.0448 (the launcher's own printed projection), or **817.09 / 1,011.83 = 92.0 % of cap** at a conservative +30 %/level allowance. **Under every honest construction the ladder lands UNDER its cap — the cap was never F23's binding constraint.** | `verification/campaign/F23_HP_WEDGE_RESULTS.md` §6 (this row and that record land together; the record was not committed when this row was drafted, and the figures above were read by this lane directly from the artefacts it cites, not from the record). Primary artefacts: `verification/runs/F23_HP_WEDGE_runs/{coarse,medium}/log.simpleFoam` (ClockTime, ExecutionTime, all 4,000 `Ux` initial residuals), `.../{coarse,medium,fine}/log.checkMesh` (wedge angles, `Mesh OK.`), `.../fine/log.build` (the guard's abort line), `.../{coarse,medium}/processor{0,1,2,3}/{2900..4000}/U` and `.../0/V` (Ubar, f·Re, Class C drift), `cases/F23_HP_WEDGE/launcher.queue.out` (the cumulative tally and the pre-fine projection), `cases/F23_HP_WEDGE/STATUS.F23_HP_WEDGE` (`launcher_rc=1`). Pre-registration frozen at `058e77719c57332aff245e58d536a343cbde49e5`; freeze re-checked by this lane, 19 of 20 paths blob-identical to disk (the 20th is the queue entry, re-issued against the freeze it names — structural, INFRASTRUCTURE, read by no verdict). **NOT AMENDED: F23 is post-compute and its gates, thresholds, cap and labels are closed; the successor F23b is separately registered.** |
| **C-204** | 2026-08-25 | cfd | **CORRECTION ROW — RE-NUMBERS the cfd F1/ONERA M6 R0 calibration OFF the duplicate id C-69 (rule 6 quote-and-strike; nothing above edited).** C-69 was assigned CONCURRENTLY to two rows — dafoam's curriculum probes D10/D11/D12 and this team's F1 (ONERA M6) R0 — both deriving `max+1 = 69` in the same window before either landed (the rule-11 hazard). Per rule 6 the original is STRUCK, never rewritten. **Dafoam's C-69 is NOT touched and KEEPS id C-69** — verification ruled by LANDING ORDER and dafoam's row landed first; not this team's row to edit. **THIS ROW re-numbers the cfd calibration to C-204:** ~~C-69 · cfd · F1 (ONERA M6) R0~~ → **C-204**, content verbatim and unchanged. Every figure below is carried byte-identical from the struck row and reads from the same committed artifacts (append rule 2); no cost figure is disputed and none moves. Committed records citing "C-69" for this cfd row read through to THIS row; they are not edited (rule 6). —— ORIGINAL CONTENT, VERBATIM: **F1 (ONERA M6) rung R0 — build L1/L2/L3, three real `checkMesh` logs, and the §5 admission grade. `GATE FAIL` at all three levels, so NO SOLVER STARTED.** Cells exactly as amended (108,216 / 865,728 / 6,925,824); `r = 2.000000` on both pairs; **node nesting L1⊂L2⊂L3 = 0.000e+00 m** read from the BUILT `polyMesh` under a LIVE PLANTED CONTROL (1e-9 m perturbation seen at 1.000000083e-09 m; the comparator refuses exit 2 if it is not); skewness 1.443 ≤ 4 PASS; y⁺ estimate 39.7 ≤ 300 PASS; L3 S2 cell 0.00625 c ≤ 0.00650 c PASS. **FAILING: max non-orthogonality 84.64 / 86.02 / 86.78 ° against ≤ 70 ° — and it WORSENS with refinement (severe faces 36 → 216 → 1,440), all of it on AMENDMENT 2's tip-fill collapsed lines; the measured no-fill control gives 51.2554 ° with ZERO severe faces. And `checkMesh` prints no `Mesh OK` at any level, on high aspect ratio at the WAKE CUT (5,934 / 6,469 / 6,749) — IDENTICAL in the control, so that one is §5's own frozen recipe, not the amendment.** `rc = 0` MEASURED from `RC_*.txt` at every step, never proxied. SERIAL, 1 rank — §6's partition pair does not enter this rung, stated so an absent decomposition is not later read as an omission. | **19.3 core-min** pre-registered estimate, **90 core-min HARD CAP** (§8 R0 row); **cap NOT breached**. | **2.18 core-min ACTUAL** — 131 s wall × 1 rank ÷ 60, from `verification/runs/F13_ONERA_M6_runs/log.r0`. **$0.0019 DERIVED, NEVER MEASURED** (0.0364 core-h × $0.0513/core-h, reported-by-owner). | **0.113** | **~15 of the 17.1 core-min underspend is §6's gate D DELIBERATELY WITHHELD** — §5 already refuses to grade G on a level that fails admission, so certifying determinism on an unusable ladder is spend, not diligence; gate D is `PENDING`. The remainder is a **coincidence, not a transfer**: §8 priced meshing at the team's measured `blockMesh` fit (74.9 s for all three levels) and the Python `polyMesh` writer that actually ran took **74 s** — within one per cent, on a different tool. **No stall** (longest step 66 s, far under 3600 s). **Waste: none identified.** **Separately named, never folded into the ratio:** about 0.5 core-min of serial scratch dry-runs during instrument development, run outside every registered path, disclosed on the face of AMENDMENT 2 §A2.2(4). | `verification/runs/F13_ONERA_M6_runs/R0_TERMINAL.md`, `R0_ADMISSION.json`, `verification/campaign/F13_RESULTS.md` |
| **C-205** | 2026-08-28 | dafoam | **SO-1a — NACA0012 drag-min-at-fixed-lift, FD-VERIFIED GRADIENT RUNG, five arms (MESH, X-S, F-S, X-P, F-P), np=1 throughout. SOLVER COMPUTE.** **WHY THIS ROW IS LATE, STATED PLAINLY:** SO-1a's own frozen grader REFUSED on a false positive (`so1a_grade.py:122-125` held the bare substring `Floating point exception`; `:420` appended `checkMesh.log` to the C5 haystack; line 18 of that artefact is OpenFOAM's `trapFpe:` ENABLEMENT BANNER), returning `NOT A RESULT` with ZERO gates evaluated. **Rule 12's comparison could not be made at SO-1a's own completion because SO-1a had no completion in rule 12's sense** — there was no graded verdict to compare a cost against. It is closed here, at SO-1aR's completion, which is the first moment those 9.416 core-min carry a verdict. **The defect cost this lab a verdict AND its bookkeeping, and that is why the row is dated three days after the spend.** | **7.367 core-min** = 0.122783 core-h = **$0.006299 derived**. Per arm, from the item's own frozen `PREDICTED_CORE_MIN` (`so1a_grade.py:135`): MESH 0.167 · X-S 1.100 · F-S 2.500 · X-P 1.100 · F-P 2.500 | **9.416 core-min** = 0.156933 core-h = **$0.008051 derived**. Per arm from `ledger.txt` `core_min=` fields: MESH 0.183 · X-S 1.017 · F-S 3.583 · X-P 1.200 · F-P 3.433 | **9.416 core-min — CLEANED == GROSS.** The charter §2 3600-s stall rule matches **no row**: the longest arm is F-S at **215 wall s**, an order of magnitude inside the threshold. No row is excluded and no figure is adjusted | **1.2781x** (9.416 / 7.367). Per arm: MESH 1.096 · X-S **0.925** · F-S **1.433** · X-P 1.091 · F-P **1.373** | **CONTENTION — measured, not inferred, and NAMED SEPARATELY FROM WASTE (`COMPUTE_BUDGET_CHARTER.md` §6).** The two overrunning arms are exactly the two that ran beside siblings: F-S beside `av2r_FAD-S`; F-P beside `d18_F-S` and `av2r_FAD-S` (`ledger.txt` `siblings_pre`/`siblings_post`). Their cgroup throttle counts are an order of magnitude above the quiet arm's — **F-S `max_nr_throttled=68`, F-P `42`, X-P `13`, against X-S's `5`** — and **X-S, the LEAST contended arm, came in UNDER estimate at 0.925x**, which is the control that makes the contention reading falsifiable rather than a story fitted to an overrun. **NO WASTE WAS OBSERVED AND NONE IS FOLDED INTO THIS RATIO**: no stalled row, no row over 3600 wall s, no re-run. Misprediction is a secondary contributor at most — the FD arms buy 6 primals each and were estimated at 2.5 core-min, which the quiet-arm evidence does not let us separate further, and that limit is stated rather than resolved by assertion. **`P8` (total core-min in 4.0–22.0), registered before the compute, HIT** | `cases/dafoam/ladder-a/A1/curriculum_SO1aR/RESULTS.md` §8 Row A @ **b79ccd16**; figures read by SO-1aR's own `read_ledger` from `/home/ubuntu/certonomous-runs/CURRICULUM-SO1a-a1-naca0012-dragmin-gradient/ledger.txt` (birth leg B1, `BORN`); SO-1a's pre-registration @ `curriculum_SO1a/PREREGISTRATION.md`. `cost_basis`: **REPORTED-BY-OWNER, NOT MEASURED** — rate $0.0513/core-h c7a.4xlarge, owner-stated; the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` §5). Dollars **DERIVED** |
| **C-206** | 2026-08-28 | dafoam | **SO-1aR — the RE-GRADE of SO-1a's preserved artefacts. INSTRUMENT COMPUTE ONLY; ZERO SOLVER COMPUTE.** A separate registration whose subject is SO-1a's existing on-disk artefacts (SO-1a's gates are closed; rule 6 forbids editing its frozen files and none was edited). **SO-1a's 9.416 core-min is NOT re-charged to this row** — it is C-205's, and double-counting it would corrupt the ladder's accounting. **This row and C-205 share a TRIGGER (SO-1aR's completion) and nothing else**: rule 12 compares a process against ITS OWN estimate, and dividing SO-1a's solver spend by SO-1aR's instrument cap would produce a number that means nothing. | **1.0 core-min cap** = 0.016667 core-h = **$0.000855 derived**, registered at `curriculum_SO1aR/PREREGISTRATION.md` §9 before the graded run. Composed as: the graded run ≤0.167 + repeats/re-verify headroom ≤0.30, rounded up to a 1.0 cap | **0.036 core-min** = 0.000600 core-h = **$0.000031 derived**. Composed of MEASURED wall times at 1 rank: selftest `python3` 0.489 s + selftest `python3 -O` 0.585 s + `--birth` 0.122 s + development selftests 0.770 s = 0.0328 to the freeze, plus **the graded run itself at 0.160 s = 0.0027** | **0.036 core-min — CLEANED == GROSS.** Nothing to clean: the longest single invocation is 0.585 wall s and the 3600-s stall rule matches no row | **0.036x** (0.036 / 1.0) — **the estimate OVERSHOT by ~28x**, in the safe direction | **A DELIBERATELY CONSERVATIVE CAP ON A READ-ONLY INSTRUMENT — misprediction, not contention and not waste.** The cap was sized for a graded run plus repeats before anyone knew how fast grading 18.8 kB of JSON would be; the graded run cost **0.0027 core-min**, ~62x under its own ≤0.167 line. **Nothing was wasted and nothing was contended** — the run is a single-rank read of files already on disk. **CALIBRATION LESSON FOR THE NEXT RE-GRADE: an instrument-only item that launches no container and reads <100 kB should be capped near 0.1 core-min, not 1.0.** A 28x-overshot cap is not free — it reserves budget headroom that cannot be spent elsewhere and it weakens the cap's meaning as a stop condition. **`R3` (≤1.0 core-min), registered before the run, HIT** | `cases/dafoam/ladder-a/A1/curriculum_SO1aR/RESULTS.md` §7/§8 Row B @ **b79ccd16**; wall times verbatim in `curriculum_SO1aR/so1ar_grade_selftest_evidence.txt` @ **bf5aec13**; verdict artefact `/home/ubuntu/certonomous-runs/CURRICULUM-SO1a-a1-naca0012-dragmin-gradient/SO1aR_grade_20260828T171830Z.json`. `cost_basis`: **REPORTED-BY-OWNER, NOT MEASURED** (`COMPUTE_BUDGET_CHARTER.md` §5). Dollars **DERIVED** |
| C-207 | 2026-08-28 | dafoam | **D18R-P7** (`cases/dafoam/curriculum_D18R_P7`) — successor RE-GRADE of ONE registered prediction (P7) of curriculum item D18, from D18's **preserved grade JSON**, through a successor comparator frozen **before** execution. Phase 2 item 1 of Sanaa's ordered re-grade sweep. **P7 moved `MISS` → `HIT`** (S/N 19.8691 → 0.1383); **D18's item verdict `PASS` did NOT move** and the successor proves it by recomposing `d18_grade.py:596-601` from the preserved artefact. **ZERO SOLVER COMPUTE — this row exists because the INSTRUMENT cost is not zero and rule 12 does not exempt a zero-solver item.** | **0.60 core-min registered estimate**, cap 3.00 (`cases/dafoam/curriculum_D18R_P7/PREREGISTRATION.md` §9, frozen `9ef4b5ed`); **$0.000513 derived** at $0.0513/core-h, `cost_basis` REPORTED-BY-OWNER NOT MEASURED | **≈ 0.230 core-min** = ≈ 13.8 wall s at ranks 1 — the registered execution PLUS all pre-freeze instrument development and validation runs, none of them hidden | **0.0578 core-min MEASURED** = 3.47 wall s at ranks 1, the registered execution alone: selftest `python3` 0.09 s + `python3 -O` 0.22 s + birth control 3.12 s + re-grade 0.04 s, each read from its own `/usr/bin/time` line. = 0.000964 core-h = **$0.0000494 derived, not measured** (the box cannot read its own billing, `COMPUTE_BUDGET_CHARTER` §5); gross $0.000197 derived | **0.096x cleaned/predicted** (0.0578 / 0.60); 0.38x on gross; **1.9 % of cap** — no cap approached, nothing stopped, and **no row is anywhere near the 3600-s stall rule** (longest single reading 3.12 s) | **MISPREDICTION, AND IT IS THIS LANE'S.** 0.60 core-min was a deliberately loose estimate for an instrument-only item with no prior of this shape in the family. The useful part is WHY it was loose: **the dominant real cost is I/O, not CPU** — three `cp -a` copies of a **243 MB** preserved run root, so the estimate was pricing the wrong resource. **Calibration for the family: a future instrument-only re-grade of this shape should be registered at ≈ 0.10 core-min WITH THE COPY COUNT NAMED.** **WASTE, NAMED SEPARATELY AND NOT ABSORBED INTO THE RATIO (§6): 0.052 core-min** — one full birth-control re-run forced by a defect in this lane's own evidence printer (a two-word verdict emitted bare into a shell `set --`, shifting every column after it). Rework, not contention. Zero solver waste because zero solver compute | `cases/dafoam/curriculum_D18R_P7/RESULTS.md` §7; pre-registration frozen `9ef4b5ed` |
| C-208 | 2026-08-28 | heat-transfer | **T15_UP_f (unsteady axisymmetric turbulent free plume, MTT similarity, `buoyantBoussinesqPimpleFoam`, kEpsilon URANS, 1-degree wedge, 102,400 cells, 24,000 steps, **1 rank**) -- SINGLE-CASE RUNG, RULE-4 COMPLETE, GRADE `BLOCKED`.** `verification/runs/T-family/T15_runs/STATUS.T15_UP_f` reads `rc=0`, `capped=no`, `checkmesh_rc=0`, `note=clean`, `started_utc=2026-08-27T08:57:37Z`, `ended_utc=2026-08-28T04:53:26Z`; `log.solve` carries exactly **one `End` line**. **THE ROW IS OWED REGARDLESS OF WHETHER THE GRADE EVER LANDS** -- the compute was spent, and rule 12 is owed at process completion, not at verdict. **The grade is `BLOCKED` and the reason is the instrument refusing rather than degrading, which is the system working**: `T15_GRADE_OUTPUT.txt` ends *"REFUSE: planted-zero control S1(FLUCTUATION): a CONSTANT offset of one mean moved sigma/mean by 9.95e-05 -- a working fluctuation reader must be nearly blind to a constant offset; this one is not"*. Three of the four plants PASSED (`w_axis` and `T_axis` POINT, `b_th` INTEGRATING); the fourth refused, so **no verdict from the fixed vocabulary is claimed here and none is implied.** The rung's registered ceiling is `CAN DO, CAVEATS -- ONE MESH` (`T15_registered.json.capability_ceiling`): no Roache triple exists and none may be computed, so no GCI or discretisation uncertainty appears in this row either. | **1,307 core-min** POINT (`T15_registered.json` `cases.T15_UP_f.point_core_min`, and the prereg table at `docs/campaigns/T-family/T15_PREREGISTRATION.md:303-320`). Registered **CAP 2,600 core-min** = **1.99x** the point, enforced as `timeout_s=156000` at 1 rank (`STATUS` corroborates the wired value). Frozen at prereg. **Basis is TWO-PART and both parts are MEASURED, which is why this rung's estimate is the best-founded of the three T-family rows in this ledger.** (a) Parent rate 6.397e-06 core-s per cell-iteration, MEASURED from `verification/runs/T-family/T8_runs/STATUS.T8_MTT_f` (`wall_s 13101`, ranks 1, 218.350 core-min over 20,000 iterations on 102,400 cells). (b) **This rung's own rate, MEASURED on a scratch copy of the registered case** with the registered `fvSolution`: mean `ExecutionTime` delta over steps 6-20 = **3.267 core-s per step**, giving a **MEASURED PIMPLE/SIMPLE ratio of 4.99x** -- which the prereg notes is what 4 pressure solves per step against 1 per SIMPLE iteration predicts, *measured, not assumed*. Dollars **DERIVED, NOT MEASURED**: **$1.117** at the point, **$2.223** at the cap, at c7a.4xlarge **$0.0513/core-h, REPORTED-BY-OWNER** -- the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` section 5). | **1,195.817 core-min**, basis **gross**, MEASURED as `wall_s x ranks / 60` = **71,749 s x 1 / 60**, re-derived by this lane from `STATUS.T15_UP_f` itself (71749/60 = 1195.8167; the file's own `core_min=1195.817` agrees). The UTC stamps corroborate the wall independently: 2026-08-27T08:57:37Z to 2026-08-28T04:53:26Z = 19 h 55 m 49 s = **71,749 s exactly**, so `wall_s` is not a lone unchecked field. **`ExecutionTime` basis stated beside it: 70,751.29 core-s = 1,179.188 core-min**, **1.39 % below gross**. Dollars **DERIVED, NOT MEASURED**: 19.930283 core-h x $0.0513/core-h = **$1.022424**. | **1,195.817 -- gross == cleaned, and the 3,600-second stall rule is ADDRESSED, not passed over.** This is the one row in this ledger where the rule needs an explicit ruling rather than a dismissal: the single run is **71,749 wall s, 19.9x the 3,600-s threshold**. It is **NOT a stall and is not cleaned out** -- charter section 2's heuristic targets a row that *hangs*, and this row's own log disproves a hang: 24,000 timesteps completed, one `End` line, `rc=0`, `capped=no`, and `ExecutionTime` delivered **98.61 %** of the wall. A 24,000-step URANS solve is long **because it is long**, and removing it would leave the rung with no actual at all. The threshold is a screen, not a verdict, and it is recorded here as screened-and-cleared. **WASTE: 0.000 core-min**, named separately per `COMPUTE_BUDGET_CHARTER.md` section 6 and folded into neither column nor the ratio -- not capped, not killed, not re-run, not truncated, not discarded. Cap use **45.99 %** (1,195.817 / 2,600). No overrun, so rule 12's stop clause was never in play and no cap was raised. | **0.915x** (1,195.817 / 1,307) -- **8.5 % UNDER the registered point**, the conservative direction. On the `ExecutionTime` basis the same quantity is **0.902x**. | **THE BEST-CALIBRATED T-FAMILY ESTIMATE IN THIS LEDGER TO DATE, AND THE REASON IS METHOD, NOT LUCK -- WHICH IS THE ONLY TRANSFERABLE PART.** **(1) WHAT SEPARATES IT FROM C-200 AND C-201.** Those two rungs both scored against a rate **BORROWED** across a change of mesh topology (T14's 2-D square scratch arm applied to a wedge and to a 3-D cube) and missed at 2.027x and 1.220x. This rung measured its own rate **on its own registered case with its own `fvSolution`**, and landed at 0.915x. **A rate measured on the case is worth roughly a factor of two in estimate quality against a rate borrowed across a topology change**, on the three-point evidence now in this ledger. **(2) THE 8.5 % IS ATTRIBUTED, AND THE ATTRIBUTION IS ARITHMETIC RATHER THAN A STORY.** 24,000 steps x 3.267 core-s/step = 78,408 core-s = 1,306.8 core-min, which is the registered 1,307 to four figures -- so the POINT is exactly the scratch rate extrapolated linearly in steps, with **no contention allowance and no startup amortisation term**. The delivered rate is 71,749 / 24,000 = **2.98954 core-s per step gross**, or 70,751.29 / 24,000 = **2.94797 core-s per step of CPU**. The scratch arm therefore **over-read its own case's full run by 1.093x on the gross basis and 1.108x on the CPU basis**, measured over steps 6-20 of a 20-step probe. **THIS IS THE SAME SIGN AND NEARLY THE SAME SIZE AS THE 1.190x STARTUP OVER-READ MEASURED AT C-200** for T14's 2,000-step scratch arm against its 20,000-step run. **Two independent rungs now measure a short-probe rate over-reading its own long run by 1.09-1.19x**, and in both cases the mechanism is unamortised startup inside a probe window too short to dilute it. That is now a two-point regularity in this ledger and it is offered as such -- a regularity, not a law, and NOT a correction factor to apply blind. **(3) CONTENTION: PRESENT, MEASURED, SMALL, AND NOT NETTED OFF.** `ExecutionTime / wall_s` = 70,751.29 / 71,749 = **0.98609**: **1.39 % of this run's wall was not this run's CPU**, over a 19.9-hour window that overlapped foreign solvers on a shared 16-core box. The figure is named here and is **NOT** folded into the 0.915x, which stays on the gross basis the charter's unit specifies. Contention pushes the gross ratio **UP**, so the true uncontended ratio is **0.902x or lower** -- further under, not nearer, the point. **(4) THE HONEST LIMIT OF THIS ROW.** One case, one mesh, one level. It calibrates a *rate method*; it establishes nothing about grid convergence, and the registration forbids a triple being computed from it. And the run it prices produced **no grade** -- so the lab bought 1,195.817 core-min and holds, so far, a refusal. **That is a real cost of a real run and it is reported here rather than deferred to whenever the S1 reader is repaired.** | Actual: `verification/runs/T-family/T15_runs/STATUS.T15_UP_f` and `DONE.T15_UP_f`; `verification/runs/T-family/T15_runs/T15_UP_f/log.solve` (`ExecutionTime`, the single `End`). Point, cap and both rate bases: `verification/runs/T-family/T15_runs/T15_registered.json` (`cases.T15_UP_f.point_core_min` / `cap_core_min`, `cost.parent_rate_core_s_per_cell_iteration`, `cost.rate_scratch_measured_core_s_per_step`, `capability_ceiling`) and `docs/campaigns/T-family/T15_PREREGISTRATION.md:303-329`. Parent rate's own case: `verification/runs/T-family/T8_runs/STATUS.T8_MTT_f`. Refusal: `verification/runs/T-family/T15_runs/T15_GRADE_OUTPUT.txt` (last line). Cross-rung comparison: C-200 and C-201 of this ledger. Docket claim `D567`. Every figure above re-derived by this lane from the artefacts named, not transcribed from any report. |
| C-209 | 2026-08-28 | heat-transfer | **T5b rung (`T5_CUBE_c` + `T5_CUBE_m` + `T5_CUBE_f`) -- surface-mounted cube CHT, `chtMultiRegionSimpleFoam`, 5,000 iterations, **1 rank** each, 52,684 / 212,942 / 882,024 cells -- COMPLETE THREE-LEVEL LADDER, GRADED, RUNG VERDICT `NOT A RESULT`.** All three levels rule-4 COMPLETE: `rc=0`, `capped=0`, `checkMesh_rc=0`, `note=clean`, exactly one `End` line and exactly 5,000 `ExecutionTime` lines per level. The frozen `analyse_t5b.py` graded the ladder and returned **`NOT A RESULT` on all 6 graded rows** (G1a, G2a, G3a, G5a, G5b, G5c), every one at the registered section 7 clause 1: the y+ ladder tolerance fired on the FINE level, `cube_front` y+max **2.3100** against tolerance **2.00**. The 7 REPORTED rows are a row class, not a verdict, and are excluded by ruling D534. **This row therefore carries NO verdict of its own beyond the one the comparator issued**, and it is filed because the rung COMPLETED and rule 12 is owed at completion. | **419.118 core-min** POINT as the unrounded product, **419.2 as the frozen registration's own printed total** -- and **BOTH are stated because the ratio differs in the fourth digit between them and this ledger does not get to pick.** Basis is the FROZEN `verification/runs/T-family/T5b_runs/T5B_CAPS.txt` and prereg section 9: POINT = **measured x 1.02** over T5's own triple (`verification/runs/T-family/T5_runs/STATUS.T5_CUBE_{c,m,f}`, all `rc=0` `capped=0`): **16.083 + 75.650 + 319.167 = 410.900**, x1.02 = **419.118**; the registration's section 9 table prints the per-level POINTs rounded to **16.4 / 77.2 / 325.6** and totals them as **419.2**. **THIS IS THE STRONGEST BASIS ANY ESTIMATE IN THIS LEDGER HAS EVER HAD** and the registration says so on its face: *"the same mesh, the same solver, the same `endTime`, the same ranks, on this box"*, with the expected actual/predicted ratio declared as **1.00**. The **2 % is an ALLOWANCE, NOT A MEASUREMENT**, and the registration refuses to dress it up: it covers the repaired function objects' five extra field writes per case per object, and *"at the 20-iteration probe scale the effect is smaller than the contention noise on an 88 %-busy box"*. Registered **CAP = 2.0 x POINT = 838.4 core-min** (32.8 / 154.4 / 651.2), declared as **contention headroom on a saturated box, not model uncertainty**, and enforced -- `run_one_t5b.sh` REFUSES unless `--cap-core-min` equals the row in `T5B_CAPS.txt`, so an argv cannot widen a cap. Dollars **DERIVED, NOT MEASURED**: **$0.3584** at the point, **$0.7168** at the cap, at c7a.4xlarge **$0.0513/core-h, REPORTED-BY-OWNER** (`COMPUTE_BUDGET_CHARTER.md` section 5). | **451.833 core-min**, basis **gross**, MEASURED as `wall_s x ranks / 60` at ranks = 1 and re-derived by this lane from the three `STATUS` files: `T5_CUBE_c` **16.783** (1,007 s), `T5_CUBE_m` **87.533** (5,252 s), `T5_CUBE_f` **347.517** (20,851 s). **`ExecutionTime` basis stated beside it, from the three `log.solve` files: 930.66 + 5,247.99 + 20,833.54 = 27,012.19 core-s = 450.203 core-min**, **0.36 % below gross** -- and on this rung the two bases are **NOT interchangeable at the LEVEL**, which is the row's main finding. Dollars **DERIVED, NOT MEASURED**: 7.53055 core-h x $0.0513/core-h = **$0.386317**. | **451.833 -- gross == cleaned.** The **3,600-second stall rule matches the FINE level's 20,851 wall s on its face and is ruled NOT a stall**, on the same ground as the T15 row above and with the same evidence: 5,000 iterations completed, one `End`, `rc=0`, `capped=0`, and `ExecutionTime` delivered **99.92 %** of that level's wall -- a stalled row does not spend its wall on CPU. The coarse and medium levels are 1,007 s and 5,252 s, both under the threshold. Nothing is removed. **WASTE: 0.000 core-min in the charter section 6 sense**, named separately and folded into neither column nor the ratio -- no level was capped, killed, truncated, re-run or discarded, and all three were read end-to-end by the frozen comparator. **A DISTINCTION THIS ROW DECLINES TO COLLAPSE, AND IT IS A REAL OPEN QUESTION AGAINST C-202 OF THIS LEDGER:** C-202 named an entire 194.73 core-min spend as waste because its rung produced `NOT A RESULT`. T5b also produced `NOT A RESULT` on every graded row. **The two are not the same case and this lane will not silently file them alike.** F23's levels were *not converged* -- there was nothing for a gate to read. T5b's three levels each produced a **complete, converged, comparator-read y+ field**, and the `NOT A RESULT` is the *admission gate* refusing the ladder at clause 1 on a **statistic**, which is itself the measurement that produced docket rows D557 and D566 and the successor `T5c`. **Zero is entered here, and the question of whether an admission-gate refusal on a fully-converged ladder is charter-section-6 waste is REPORTED to the supervisor and to verification, unruled by this lane.** Cap use **53.89 %** (451.833 / 838.4); worst per level **56.69 %** (`m`, 87.533 of 154.4). No overrun; no cap raised. | **1.0781x** against the unrounded POINT 419.118, **1.0778x** against the registration's own printed 419.2 -- **7.8 % OVER an estimate whose registration declared its expected ratio to be 1.00.** Per level against the x1.02 POINT: **c 1.023**, **m 1.134**, **f 1.067**. On the `ExecutionTime` basis the rung ratio is **1.0742x** and the per-level figures become **c 0.946**, **m 1.134**, **f 1.067**. | **THE OVERSHOOT IS 7.8 % ON A SAME-CASE MEASURED BASIS, AND THE CONTENTION SHARE IS THE FIRST THING A READER WILL REACH FOR. IT IS MEASURED, IT IS BOUNDED, AND IT IS SMALL -- SO THE 7.8 % IS MOSTLY REAL AND MOSTLY UNEXPLAINED.** **(1) THE LOAD RECORD DOES NOT EXIST, AND THE ABSENCE IS MEASURED RATHER THAN ASSUMED.** The rung ran **2026-08-27T22:31:09Z** (`T5_CUBE_c/log.checkMesh` mtime) to **2026-08-28T08:02:01Z** (`STATUS.T5_CUBE_f` mtime), 9 h 31 m. **No per-run load record covers any part of that window**: a search for `*load*`, `*box*`, `*ledger*`, `*uptime*` at every depth under `verification/runs/T-family/T5b_runs` returns **zero**, and **the same pattern was driven as a planted control against `verification/runs/F23_HP_WEDGE_runs`, where it returns five files** (`{coarse,medium,fine}/box_before.txt`, `{coarse,medium}/box_after.txt`). **The reader that returned the zero was shown able to see a non-zero, so the absence is a measurement.** The same search returns zero under `T5_runs`, so **the PARENT MEASUREMENT that forms this rung's POINT also carries no load record** -- the basis was measured under unknown load and re-applied under unknown load. **The LOAD-AVERAGE share of the gap is therefore UNATTRIBUTED and is stated as unattributed, not guessed.** **(2) WHAT CAN BE ATTRIBUTED IS ATTRIBUTED, FROM THE LOGS THEMSELVES, AND IT CHANGES THE ANSWER.** `ExecutionTime / wall_s` per level = **0.92419 / 0.99924 / 0.99916**. The medium and fine levels delivered CPU for **99.9 %** of their wall, so **contention and preflight together account for at most 0.08 % of those two levels' cost** -- which is 92.5 % of the rung's spend. Across the rung, wall minus solver CPU is **76.34 + 4.01 + 17.46 = 97.81 s = 1.630 core-min**, i.e. **at most 0.36 of the 7.8 percentage points** can be non-solver wall of any kind. **AT LEAST 7.4 OF THE 7.8 POINTS IS SOLVER CPU GENUINELY EXCEEDING A SAME-CASE MEASUREMENT INFLATED BY 2 %.** **(3) AND THE PER-LEVEL PATTERN KILLS THE OBVIOUS EXPLANATION.** Against the raw parent walls the CPU ratios are **c 930.66/965 = 0.964 (UNDER its own parent), m 5,247.99/4,539 = 1.156, f 20,833.54/19,150 = 1.088** -- **non-monotone in level and spanning 0.96 to 1.16.** The registered 2 % allowance was for the repaired function objects' extra field writes, which are **five per case per object** and therefore roughly **constant per level**, not size-scaling and certainly not non-monotone. **A constant-per-case write cost cannot produce a level pattern of 0.96 / 1.16 / 1.09, so the allowance is not the mechanism, and no mechanism in the record explains it.** It is left unexplained rather than fitted. **(4) THE COARSE LEVEL IS THE ONE ANOMALY WITH A NAMED CANDIDATE AND IT IS NOT SEPARABLE.** `c` alone shows a 76.34 s gap between wall and solver CPU -- **larger in ABSOLUTE seconds than the fine level's 17.46 s on a case 17x smaller.** Its wall window spans `log.checkMesh` to `log.solve`, so **preflight (`checkMesh` on the coarse mesh) and contention are inside the same 76 s and cannot be separated without the load record that does not exist.** Both candidates are named; neither is asserted. On the CPU basis `c` is 0.946x, i.e. **UNDER** its POINT, so the whole of that level's 1.023x gross overshoot lives in those 76 seconds. **(5) THE TRANSFERABLE FINDING, AND IT IS THE THIRD INDEPENDENT TIME THIS LEDGER HAS REACHED IT.** C-4 (cfd/DPW8_V2) and the B3 row both concluded: *record the load average beside any per-iteration cost basis, and state the load the basis is being re-applied at.* **T5b is the strongest possible statement of that lesson, because it removes every other variable.** Same mesh, same solver, same `endTime`, same ranks, same box, same cell counts -- an estimate that the registration itself declared should land at 1.00 -- **and it missed by 7.8 % anyway, with at most 0.36 points of it attributable to anything measured.** **A same-case measured basis is not self-calibrating if neither the basis run nor the priced run records the load it ran under.** The lesson has now been filed three times without the field being added; this row records that the repetition is the finding. | Actuals: `verification/runs/T-family/T5b_runs/STATUS.T5_CUBE_{c,m,f}` (`rc`, `wall_s`, `ranks`, `core_min`, `cap_core_min`, `capped`, `checkMesh_rc`, `note`, `solver=chtMultiRegionSimpleFoam`), each level's `log.solve` (`ExecutionTime` last value and line count, the single `End`), and the `log.checkMesh` / `STATUS` mtimes that fix the 22:31:09Z-08:02:01Z window. Point, cap and basis: **frozen** `verification/runs/T-family/T5b_runs/T5B_CAPS.txt` and `docs/campaigns/T-family/T5b_PREREGISTRATION.md` section 9 (POINT table) and section 10 (cap enforcement). Parent basis: `verification/runs/T-family/T5_runs/STATUS.T5_CUBE_{c,m,f}`. Grading: `verification/runs/T-family/T5b_runs/T5B_GRADE_OUTPUT.txt`. Planted control for the load-record zero: `verification/runs/F23_HP_WEDGE_runs/{coarse,medium,fine}/box_before.txt` and `{coarse,medium}/box_after.txt`. Cross-rung comparison: C-202 of this ledger (the waste question, left open above), C-4 and the B3 row (the load-average lesson). Docket rows D534, D557, D566; docket claim `D567`. **NO FROZEN FILE WAS EDITED to write this row** -- `analyse_t5b.py`, `T5B_CAPS.txt` and both registrations were read only. Every figure re-derived by this lane from the artefacts named. |
| C-210 | 2026-08-28 | closure | **G2_grid_triple_duct** — square-duct grid-convergence triple (1,024 / 4,096 / 16,384 cells), chain COMPLETE 2026-08-28T17:33:57Z rc 0, graded **`PASS`** (primary `gradP` CONVERGING, observed order p = 1.2029 in the registered band [1.0, 3.0], GCI_fine 0.7693 % against a 5.0 % ceiling). **Closure's first converged grid triple and its first PASS.** | **61.0 core-min** registered estimate, cap **120.0** (`PREREGISTRATION.md` §7 lines 825 and 830, frozen `71654cec`, amended PRE-COMPUTE at `04366870` with **no cost, cap, gate, threshold, band, label or timeout moved** — that amendment's entire deletion set is four lines of a fatal-detection clause); $0.05215 estimate / $0.10260 cap, derived at $0.0513/core-h | **67.28 core-min MEASURED**, summed from the driver's own per-level readings in `CHAIN.log` at ranks 1: **L1 125 wall s = 2.083**, **L2 642 s = 10.700**, **L3 3,270 s = 54.500**. = 1.1213 core-h = **$0.05753 derived, not measured** (`COMPUTE_BUDGET_CHARTER` §5 — the box cannot read its own billing). The grading pass cost a further **0.0518 core-min** (3.11 s wall, MaxRSS 122,884 kB), reported beside the solve rather than folded into it | = gross. **No row matches the 3600-s stall rule** — the longest level is L3 at 3,270 wall s, below the threshold, and it is a single continuous 40,000-iteration solve whose `ExecutionTime` count equals its `endTime` at hard equality | **1.1030x actual/predicted**; **56.1 % of cap**. Rule 12's overrun clause binds on the CAP, which was never approached — nothing stopped, no gate moved | **Misprediction of the fine level, in the direction the registration was least sure of, and ZERO waste.** The estimate's own basis was a **uniform per-cell-iteration rate applied across all three levels** (the entry's `cost_basis` reads `7.9872e8 cell-iterations at 4.5e-06 s per cell-iteration serial = 3594.2 s`). Measured, that rate is **not** uniform: L1 is 1,024 cells x 20,000 it in 125 s = **6.10e-06 s/cell-it**, L2 4,096 x 30,000 in 642 s = **5.22e-06**, L3 16,384 x 40,000 in 3,270 s = **4.99e-06**. The rate **improves with problem size** — cache and loop-overhead amortisation — so one blended constant **under-predicts the small levels and over-predicts the large one**, and the residual 10.3 % is the net of those cancelling errors. **Calibration lesson: a per-cell-iteration cost basis is NOT scale-invariant — register it per level, or state the level it was measured at.** Sibling to `C-190`, where this same family's error ran the OTHER way (a rate sampled under contention, over-predicting by 2.8x); together they say a rate carries the conditions it was measured under — **load AND problem size**. **The timing reservation this rung inherited is also settled by measurement:** L3's registered work left only **1.83x headroom** against its 5,400 s per-level timeout and contention was expected to eat it; **L3 ran 3,270 s = 60.6 % of the guard**, so the margin held and no `rc 124` was bought | `cases/RANS_LES_closure_models/G2_grid_triple_duct/RESULTS.md` §7, landed at `088a9ee6`; pre-registration `71654cec` amended `04366870`, comparator sha `6839aad1d91db7c2a413e61c53b8e3c904a1651d948a9e3795f34391f472bbd9` verified identical disk == HEAD == amendment blob at grading time |

| C-211 | 2026-08-30 | ansys-verification | **VMFL063 — Separated Laminar Flow Over a Blunt Plate (VM2026R1 p.193), the complete four-solve family: L1 (5 760 cells), L1D (5 760, determinism twin), L2 (23 040), L3 (92 160), OpenFOAM v2606 `simpleFoam`, serial `RANKS = 1`, all four `residualControl`-terminated strictly before `endTime = 30000`.** Ran 2026-08-28T17:41:41Z–17:55:34Z; **graded 2026-08-30** after the run sat ungraded across a ~32 h box outage. Register row **#44**, verdict **`GATE FAIL`** (limb A `GATE FAIL` at 40.0059 % vs a 10 % band on a `CONVERGING` triple; limb B `PASS`) | **16 core-min** registered (`PREREGISTRATION.md` §7, frozen before compute); the §7 build table totalled **15.4** and was rounded up. Per level: L1 **0.17**, L1D **0.17**, L2 **1.50**, L3 **13.55**. **$0.013680 DERIVED.** Model: throughput **2.837e-6 s/cell/iteration** measured from register row #30 (VMFL064-R2 L3) × iterations from `iterations ∝ N^0.586` fitted to row #30's 424/907/2 152. Cap **90 core-min** (running total across all four solves), **$0.076950 DERIVED** | **13.8333 core-min MEASURED** — `COST.txt` `total_core_min`, independently reproduced as Σ(`wall_s` × `ranks`)/60 from the four `RUN_RC.<level>` files: **9 + 8 + 74 + 739 = 830 wall s**, `ranks = 1`. Per level **0.15 / 0.1333 / 1.2333 / 12.3167**. **$0.011827 DERIVED, NOT MEASURED** at $0.0513/core-h, c7a.4xlarge, **owner-stated** 2026-08-21/22 — the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER` §5) | **13.8333 — gross == cleaned.** No failed solve, no re-run, no crash, no cap fire, no stall: solver `rc = 0` at all four levels captured **inside** the detached subshell, longest solve **739 wall s** against the 3 600 s stall convention, no `CAP_OVERRUN.txt`, `CAP_EXCEEDED` or `ESTIMATE_OVERRUN.txt` anywhere under the run root. **Waste = 0 core-min, named separately and never absorbed into the ratio** (`COMPUTE_BUDGET_CHARTER` §6) | **0.8646** (13.8333 / 16 registered). **0.8983** against the §7 build table's own 15.4. Actual/cap **0.1537**, **76.17 core-min unspent** | **MISPREDICTION IN BOTH COMPONENTS, OF OPPOSITE SIGN, LARGELY CANCELLING — AND THE AGGREGATE 0.8646 FLATTERS A MODEL THAT WAS WRONG TWICE.** The headline ratio would let a reader conclude the model was good to 14 %; **it was not**, and the components are reported separately so the next estimate is built on the right correction. **(1) ITERATION COUNT — UNDER-PREDICTED AT EVERY LEVEL, by 1.34×–1.64×:** 613→**1 004** (L1, ×1.638), 613→**1 004** (L1D, ×1.638), 1 381→**1 944** (L2, ×1.408), 3 110→**4 177** (L3, ×1.343). Measured exponent on cells **0.477** (L1→L2) and **0.552** (L2→L3) against the model's 0.586 — **the exponent was close; the PREFACTOR carried over from a channel-expansion bubble was low.** The registration named this as the weak link in advance (§7: *"the iteration model is extrapolated from a different geometry … the extrapolation is the weak link"*) and it was right about which half would fail. **(2) THROUGHPUT RATE — OVER-PREDICTED, ×0.663:** measured **1.881e-6 s/cell/iteration** aggregate over **441 308 160** cell-iterations and 830 wall s, against the model's 2.837e-6 carried from row #30's L3. Per level 1.556e-6 / 1.383e-6 / 1.652e-6 / 1.920e-6 — **the rate rises with mesh size**, so a rate lifted from one level's L3 mispredicts another family's smaller levels most. **(3) THE CANCELLATION:** ≈1.40 (mean iteration miss) × 0.663 (rate miss) ≈ 0.93, against the observed 0.86–0.90. **Neither component was accurate and the agreement is arithmetic luck.** **(4) CONTENTION — NAMED, NOT WASTE, NOT ABSORBED:** `CONTENTION.txt` records load average **13.94 on 16 cores** and 3 other `python3` processes at launch, which inflates `wall_s`; **13.8333 core-min is therefore an UPPER BOUND** on this family's cost on a quiet box, and the true rate miss is at least as large as measured. **(5) CARRY-FORWARD for the next `simpleFoam` laminar-separation family on this box:** use **1.9e-6 s/cell/iteration** at ~1e5 cells (not 2.837e-6), and treat a row-#30-derived iteration prefactor as a **lower bound**, multiplying by ~1.4 for a leading-edge bubble on a long plate | `cases/ansys_verification/VMFL063/RESULTS.md` §6; `verification/runs/ansys_verification/VMFL063/{COST.txt,LAUNCH_RECORD.txt,CONTENTION.txt,RUN_RC.L1,RUN_RC.L1D,RUN_RC.L2,RUN_RC.L3}`; grading record `verification/runs/ansys_verification/VMFL063/GRADING_RECORD_VMFL063.json` (sha256 `e83502f74e8314f547cd8d4a3d97472a946c2e61156d438b342853821f942155`); pre-registration `cases/ansys_verification/VMFL063/PREREGISTRATION.md` §7, blob `efdb2a8abb788c5f55bac60f10fb09816af50a30`, frozen `2df23798` / amended `3602cbf3` |
| C-212 | 2026-08-30 | dafoam | **AVWC** — the `writeCompression` cluster (AV1, AV2, AV1R) re-graded from preserved run roots through readers repaired for one root cause. **ZERO SOLVER COMPUTE** — nothing meshed, solved or containerised; the cost below is comparator time only. Outcomes: **AV1 `PASS`**, **AV1R `PASS`**, **AV2 `NOT A RESULT`** with its refusal MOVED to `G-DP` (`gmresRelTol_in_artefact: null` against the registered `1e-06`), which is the branch `PREREGISTRATION.md` §8 registered in advance. **What this row measures is the REPRODUCTION, not the original execution:** the 2026-08-28 run's wall time was never instrumented and is not recoverable — the session died at ~18:05Z before it was recorded and the working directory was destroyed by the 2026-08-30T22:33Z reboot — so this lane re-ran the same registered work from a clean state (`__pycache__` cleared, `python3` and `python3 -O`) and timed it. The re-run reproduces the recorded JSON with **0 differences** on every leaf once the transient working-directory prefix is normalised, and **20/20 selftest units against the frozen `EXPECTED_UNITS = 20`** under both interpreters | **3.00 core-min** registered estimate, cap **10.00**, ranks **1** throughout (`PREREGISTRATION.md` §9, frozen `c85eb4df` 2026-08-28T17:47:02Z, 21.7 s before the first output artefact); $0.002565 estimate, derived at $0.0513/core-h | **1.33353 core-min MEASURED**, registered scope, summed from three `/usr/bin/time -v` invocations at ranks 1: **selftest `python3` 75.2358 wall s = 1.25393**, **selftest `python3 -O` 3.7372 s = 0.06229**, **re-grade of all three items 1.0389 s = 0.01731**. = 0.0222255 core-h = **$0.0011402 derived, not measured** (`COMPUTE_BUDGET_CHARTER` §5 — the box cannot read its own billing); `cost_basis` **REPORTED-BY-OWNER**. A lane-added independent must-flag driver cost a further **0.02177 core-min** (1.3059 wall s), reported beside the registered scope rather than folded into it | = gross. **No invocation is within two orders of magnitude of the 3600-s stall rule** — the longest is 75.2 wall s. Bounded total including the lane-added control and one untimed invocation: **≤ 1.37706 core-min, ≤ $0.0011774 derived** | **0.4445x actual/predicted**; **13.3 % of cap**. Rule 12's overrun clause binds on the CAP, which was never approached — nothing stopped, no gate moved | **Misprediction, and the dominant term is PAGE-CACHE STATE rather than the work.** The identical selftest cost **75.24 s cold and 3.74 s warm — a 20.1x spread on byte-identical work** — because this re-run is the first read of the three preserved roots (~1,887 files) after a 32-hour box shutdown and a reboot. The registered estimate's basis is an **operation count** ("≈12 `cp -a` copies, ≈24 md5 manifests, ≈12 frozen `grade()` calls, two selftests") with **no cache-state term at all**, and cache state moves this workload by 20x. **Contention was present but is NOT the dominant term** and is not folded into the ratio's explanation: load average 7.22 on 16 cores with peers committing concurrently, yet the warm/cold spread on identical work under that same load is 20x while the whole miss is only 2.25x under. **WASTE, named separately per charter §6 and never laundered into the ratio: 0.02177 core-min = $0.0000186 derived** — the must-flag driver was run twice, the first untimed by this lane's own oversight and the second existing only to put a measured wall figure on it; it bought a number, not a verdict, and the first run's cost is stated NOT MEASURED and bounded by the second. **Calibration lesson: a manifest-heavy re-grade estimate built from an operation count is incomplete without a cache-state term.** Price a cold first pass at roughly 20x the warm figure for this shape, or state which of the two the estimate assumes — an estimate that does not say is unfalsifiable by a factor of twenty. Sibling to `C-158` and `C-210`, which both record rate-and-condition mispredictions: together they say a rate carries the conditions it was measured under — **load, problem size, AND cache state** | `cases/dafoam/ladder-a/A1/curriculum_AVWC/RESULTS.md` §7, landed with this row's commit; pre-registration frozen `c85eb4df`, comparator md5 `6e390f8f3c0df5d4229dd3640590ca44` and reader md5 `1a7f3f211f44c7b67b4f8f2d4c65bf4a` verified identical disk == freeze blob == HEAD blob at re-grade time |
| C-213 | 2026-08-30 | ansys-verification | **VMFL069 — Two Phase Poiseuille Flow (VM2026R1 printed p.205 = PDF p.219), OpenFOAM v2606 `interFoam`, serial `RANKS = 1`. THE RUN DIED: `SIGFPE` at time step 69 of a registered `endTime = 2000`, at L1, the coarsest of three registered levels.** `rc = 136 = 128 + 8`, captured inside the detached subshell. **L2 and L3 were never launched** (the launcher stops at the first non-zero rc) and the frozen comparator **refused, exit 2, writing no grading JSON**. Register row **#45**, verdict **`NOT A RESULT`** — registered outcome **#8** of `PREREGISTRATION.md` §10, named in writing before compute, every clause of which fired in the order written. **This is a completion under rule 12's calibration directive because the process ENDED, not because it succeeded** | **8 core-min** registered (`PREREGISTRATION.md` §7 / :401, frozen at `4e4819ab` before any compute), `RANKS = 1`, for **all three levels together**: 2 000 time steps at each of 256, 1 024 and 4 096 cells. **$0.006840 DERIVED.** The registration stated its own basis honestly at :409-413 — the figure is *"a projection from the cell counts and the step count"*, **explicitly NOT measured**, because *"the interFoam PIMPLE loop on this box has not been measured for this case"*; the cap was set at **45 core-min** (running total, ~5.6× the estimate) for that reason, **$0.038475 DERIVED**. The pre-flight smoke of 2026-08-28 could not improve it: it was *"too short to measure a per-step cost"* | **0.05 core-min MEASURED** — `COST.txt` `total_core_min`, independently reproduced as `wall_s` × `ranks` / 60 from `RUN_RC.L1`: **3 wall s × 1 rank / 60**. Single level, single solve; no other level exists. **$0.00004275 DERIVED, NOT MEASURED** at $0.0513/core-h, c7a.4xlarge, **owner-stated** 2026-08-21/22 — the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER` §5) | **0.05 — gross == cleaned, and every core-minute of it is waste.** No stall (3 wall s against the 3 600 s convention), no cap fire (`timeout` was the full 2 700 s and 44.95 core-min went unspent, 0.1111 % of cap used), no re-run, no retry — **a non-zero rc is a finding, not a retry**, and none was attempted. **WASTE = 0.05 core-min, THE ENTIRE SPEND, named separately and never absorbed into the ratio** (`COMPUTE_BUDGET_CHARTER` §6): it bought no graded number, no limb, no triple, no GCI and no field on disk. It is small and it is named at its true size rather than rounded away | **0.00625** (0.05 / 8 registered). Actual/cap **0.001111**. **THIS RATIO IS NOT A CALIBRATION OF THE COST MODEL AND MUST NOT BE ENTERED INTO ANY AVERAGE AS IF IT WERE ONE** | **THE RATIO MEASURES WHEN THE RUN STOPPED, NOT WHETHER THE ESTIMATE WAS RIGHT — AND THE ROW IS FILED SAYING SO RATHER THAN LETTING 0.00625 SIT IN THE LEDGER AS A 160× OVER-ESTIMATE.** **WHAT IT DOES TELL US, and it is only this:** the run terminated after 69 of a registered 6 000 total time steps — **3.45 % of L1's own step count and 1.15 % of the three-level total** — so **98.85 % of the estimated work was never attempted.** A cost model is not tested by a run that died in the first 3 % of its first level. **WHAT IT DOES NOT TELL US:** whether 8 core-min was the right projection for three complete solves; whether the per-step cost of the `interFoam` PIMPLE loop on this box matches the projection (still unmeasured, exactly as the registration said at :413 — **this completion did not become the first real reading, and that is stated rather than implied**); whether the 5.6× cap ratio was well chosen. **The single sound arithmetic datum recoverable is a per-step figure, and it is offered as an upper bound only:** 3 wall s / 69 steps ≈ **0.0435 wall s per step at 256 cells**, which is at or below the 1 s resolution of the wall-clock timer over the run as a whole and includes solver start-up, mesh read and field initialisation amortised over only 69 steps — **so it over-states the steady per-step cost by an unknown amount and no L2/L3 projection is built on it here.** **GAP ATTRIBUTION IS NEITHER CONTENTION NOR MISPREDICTION: it is a solver death.** Contention is named and excluded on measurement — `CONTENTION.txt` records load average **1.20 on 16 cores** with 29 681 MB available, a quiet box — so contention explains nothing about this outcome and is recorded here so it cannot later be offered as an explanation. **The cause is numerical, is documented in the register row and in `RESULTS.md` §1, and is a PHYSICS finding rather than a cost finding:** the frozen `controlDict.template` fixed `deltaT 1` under `adjustTimeStep no` — its lines 13-15 stating the choice was made so that *"the number of time steps EQUALS the numeric endTime"* and rule 4's *"ExecutionTime count == endTime"* clause would *"hold LITERALLY"*, while its lines 48-52 acknowledged the `maxCo`/`maxAlphaCo`/`maxDeltaT` values *"LIMIT NOTHING"* — so the convective Courant number was 1.99978 at step 1, 73.64 by step 64, and 2.87e9 by step 68, and MULES lost `alpha.fluid1` out of [0,1] entirely. **A bookkeeping clause of the completion rule dictated a discretisation choice that destroyed the physics**, and the cost consequence is that the registered spend was never incurred | `cases/ansys_verification/VMFL069/RESULTS.md` §5; register row **#45** in `verification/credentials/ansys/ANSYS_VALIDATION_REGISTER.md`; prereg frozen at `4e4819ab` (`PREREGISTRATION.md` §7); run artefacts committed `951ec636` |
| C-214 | 2026-08-30 | dafoam | **D6RG** — D6R re-graded from its preserved run root (`/home/ubuntu/certonomous-runs/CURRICULUM-D6R-a2-wing-multipoint`, 14,546 files) through **one** repaired reader, `read_ipopt`, rebound in D6R's own frozen comparator under a fingerprint audit. **ZERO SOLVER COMPUTE** — nothing meshed, solved or containerised; the cost below is comparator time only. **The defect repaired, `D6R-GRADER-DEF-2`:** `d6r_grade.py:615-624` requires BOTH an `Objective...............:` summary line AND an `EXIT:` line and refuses at `:620-621` if either is missing; IPOPT dying on `Eval_Error` substitutes the exception text for the summary block. **Measured with a control:** D6R's log has 0 `Objective` lines and 1 `EXIT` line, D4's has exactly one of each — the reader is demonstrably able to see a non-zero. **The frozen grader was DRIVEN, not predicted:** unmodified, on a `cp -a` copy, it exited **2** on `REFUSE G-D6R-OPT no_final_objective_or_exit`, wrote **no** verdict JSON and printed **zero of eleven** gate readings. **After the repair the verdict is `NOT A RESULT` composed from eleven gate readings** — the same word, a different object. **27/27 selftest units against the frozen `EXPECTED_UNITS = 27`** under `python3` and `python3 -O`, ten of them must-FLAG plants driven on the real frozen `grade()` | **1.50 core-min** registered estimate, cap **5.00**, ranks **1** throughout (`PREREGISTRATION.md` §11, frozen `5563f78533391e8ec5842db915ff70ebd7663685`, **before** the recorded execution); **$0.001283 estimate / $0.004275 cap DERIVED**, at $0.0513/core-h | **0.320 core-min MEASURED** by the instrument's own wall clock, registered scope, at ranks 1: **`python3` 9.7 wall s = 0.162**, **`python3 -O` 9.5 wall s = 0.158**. = 0.005333 core-h = **$0.000274 derived, not measured** (`COMPUTE_BUDGET_CHARTER` §5 — the box cannot read its own billing); `cost_basis` **REPORTED-BY-OWNER**. Lane-level compute **outside** the registered scope, reported beside it and never folded into it: the §1a frozen-grader drive (14.34 s cold manifest + 1.06 s `cp -a` + a sub-second refusal + a second manifest ≈ 30 s) and four development runs (≈ 45 s) ≈ **1.25 core-min DERIVED** from those measured components — **not a single measured total, and labelled so** | = gross. **No invocation is within two orders of magnitude of the 3600-s stall rule** — the longest is 9.7 wall s. Bounded lane total including the pre-freeze drive and development: **≈ 1.6 core-min, ≈ $0.001368 derived**, inside the 5.00 cap | **0.213x actual/predicted**; **6.4 % of cap**. Rule 12's overrun clause binds on the CAP, which was never approached — nothing stopped, no gate moved | **Misprediction, and the term is PAGE-CACHE STATE again — this row is `C-212`'s sibling and it confirms `C-212`'s lesson rather than merely repeating it.** The estimate priced four md5 manifests totalling ≈ 3 GB. A **cold** manifest of the 14,546-file D6R root measured **14.34 s**; a **warm** one ≈ 2 s — **a ~7x spread on byte-identical work**. The cache was already warm from the pre-freeze drive of the frozen grader, so the manifests cost about a seventh of what was priced, and the whole 4.7x over-estimate is that one term. **`C-212` recorded a 20.1x cold/warm spread on the same shape two days earlier and drew the lesson that a manifest-heavy re-grade estimate is incomplete without a cache-state term; this estimate STILL DID NOT CARRY ONE, and missed by 4.7x in the same direction.** That is rule 14's shape at the estimate level: a lesson whose remedy is "remember the cache" has no call site and cannot be applied — **the remedy is a cache-state term written into the estimate table itself**, stating which of cold or warm the figure assumes. **CONTENTION: named and excluded on measurement, not assumed away.** The two recorded executions differ by 0.2 wall s (9.7 vs 9.5) on identical work, so no contention term is detectable at this scale; it explains nothing here and is recorded so it cannot later be offered as an explanation. **WASTE = 0 core-min**, named separately per charter §6 and never laundered into the ratio: every invocation bought either the measured refusal, a birth-requirement control or the recorded verdict; nothing was re-run to recover a number this lane had failed to capture. **CARRY-FORWARD for the next preserved-root re-grade on this box:** price a manifest at **≈ 1.0 s per 1,000 files cold** and **≈ 0.15 s per 1,000 files warm** (14.34 s / 14,546 files cold; ≈ 2 s warm), and **say which state the estimate assumes** — an estimate that does not say is unfalsifiable by roughly an order of magnitude | `cases/dafoam/ladder-a/A2/curriculum_D6RG/RESULTS.md` §7, landed with this row's commit; pre-registration frozen `5563f78533391e8ec5842db915ff70ebd7663685`, comparator md5 `14356162837faece73acf40b323927c9` and reader md5 `61bb9dbe1adab6e9b2cdb3a13f53cff1` verified identical disk == freeze blob at execution; the frozen predecessor `d6r_grade.py` `bc8e9fec48b3f58ce7a96f4b9549590b` and `curriculum_D6R/PREREGISTRATION.md` `e525084daac50c82738170925894fbe1` each verified disk == `git cat-file blob HEAD:<path>` by the comparator itself |
| **C-215** | 2026-08-30 | dafoam | **SO-2a instrument work** — writing and driving `so2a_groot5_selftest.sh`, the C5 trapFpe confirmation on a real log, and the frozen grader's own selftest: the work that discharged `PREREGISTRATION.md:227` and cleared SO-2a for the queue. **This row is INSTRUMENT TIME ONLY. SO-2a's solve is ENQUEUED AND NOT RUN**; its actual-versus-predicted row against the registered 9.19 core-min estimate and the 79.0 core-min cap is **OWED AT COMPLETION** and is deliberately not claimed here. | **NOT REGISTERED — and that is the honest cell, not a missing one.** §7.3 registered the *requirement* ("the G-ROOT.5 container legs must be driven") and named **no cost** for it. There is therefore no predicted figure to compare against, and none is invented. | **13.85 wall s at ranks = 1 = 0.2308 core-min**, MEASURED with `/usr/bin/time` across five drives: `so2a_groot5_selftest.sh` **5.77 s** then **5.60 s** (two drives — the first caught this selftest's own backtick meta-check counting *its own pattern*), `so2a_c5_trapfpe_confirmation.py` **0.14 s**, `so2a_grade.py --selftest` **1.10 s** under `python3` and **1.24 s** under `python3 -O`. **ZERO SOLVER COMPUTE: the launcher started no arm container at any point**, asserted by selftest legs (a2) and (e4). One sacrificial `sleep` container per drive — 0.1 core, 64 MB, cpuset 9, explicit name, removed by that name, `sudo -n docker ps` empty before and after. | **Same as gross — cleaned == gross.** No stall; no drive came within three orders of magnitude of the 3600 wall-s stall threshold. **STATED AS A LOWER BOUND:** two earlier **untimed** drives of the same scripts (the C5 probe recorded in the evidence file, and the first grader-selftest pair) are **not** included, so the evening's true instrument time exceeds this figure by roughly 2.5 wall s. | **NOT COMPUTABLE — there is no predicted figure** (see the predicted column). A ratio quoted against an estimate nobody registered would manufacture a calibration datum, and this ledger is worth nothing if it carries invented denominators. | **Not a gap — there is no estimate to have missed.** What this row contributes to future estimates is the **anchor**: a G-ROOT.5 container selftest of this family's shape costs **≈ 0.1 core-min and ≈ 6 wall s per drive**, so declining to drive one has never been a *cost* decision. The lane that froze SO-2a declined on **CONTENTION** (D6R live at 4 ranks), which was correct at the time and is a different thing from expense — recording the anchor is what stops the two being confused next time. | `cases/dafoam/ladder-a/A1/curriculum_SO2a/so2a_groot5_selftest_evidence.txt` (48 pass / 0 fail, rc = 0) and `cases/dafoam/ladder-a/A1/curriculum_SO2a/so2a_c5_trapfpe_confirmation_evidence.txt` (12 pass / 0 fail, rc = 0), both landed at commit `74c2d6278e35ea3422e40c8fdeef97a90c264c74`; queue entry `verification/queue/dafoam/SO2a_chain.json`, `scripts/queue_entry_check.py` **ACCEPTED, rc = 0**. Dollars **DERIVED, NOT MEASURED** at $0.0513/core-h, c7a.4xlarge, **reported-by-owner** (`COMPUTE_BUDGET_CHARTER.md` §5): **$0.000197**. |
| **C-216** | 2026-08-30 | dafoam | **AV2RG** — AV2R **and AV2** re-graded from their preserved run roots (378 files / 25.7 MB each) through one recovered field, `identity.gmresRelTol`, with **AV2R rebinding NOTHING** and **AV2 rebinding only `arm_datum`** (AVWC's own resolver, imported and md5-verified). **ZERO SOLVER COMPUTE** — nothing meshed, solved or containerised; the cost below is comparator time only. **The defect repaired:** `av2r_xf.py:56-63` / `av2_xf.py:56-63` `idwarp_identity()` — **byte-identical between the two items** — returns only `{"idwarp_file", "libidwarp_so_md5"}`, so the artefact identity never carried `gmresRelTol`, while `:152-156` emits it to the JSONL sidecar **one statement earlier**. The frozen graders (`av2r_grade.py:321,:511-513`; `av2_grade.py:252,:442-443`) were **NOT defective** and were not modified. **Both items move from `NOT A RESULT` (a refusal, no gate reached) to `BLOCKED` (a composed verdict from gates that all returned):** `G-M2 PASS` 4032 cells, `G9 PASS`, `G10 PASS`, `G12 PASS`; `G-DP BLOCKED` because 5/5 components are `blocked` on `CD` and `CL` on both rows in both items — twenty readings, zero graded, one reason: the forward-mode AD primal fails. **28/28 selftest units** against the frozen `EXPECTED_UNITS = 28` under `python3` and `python3 -O` from a proven-clean `__pycache__` state | **6.00 core-min** registered estimate, cap **15.00**, ranks **1** throughout (`PREREGISTRATION.md` §A1.6, Amendment 1, frozen `42a3c988` **before** the recorded execution); **$0.005130 estimate / $0.012825 cap DERIVED** at $0.0513/core-h | **≈ 0.20 core-min MEASURED** at ranks 1, by wall clock: failing v1.1 battery **5 wall s** (2 s `python3` + 3 s `python3 -O`), repaired v1.2 battery **5 wall s** (2 + 3), the real two-item re-grade **< 1 s**, the evidence dump **≈ 1 s**. = 0.00333 core-h = **$0.000171 derived, not measured** (`COMPUTE_BUDGET_CHARTER` §5 — the box cannot read its own billing); `cost_basis` **REPORTED-BY-OWNER**. The wall figures are at 1 s timer resolution, so this total is stated to two significant figures and no further | = gross. **No invocation is within three orders of magnitude of the 3600 wall-s stall rule** — the longest is 3 wall s. **WASTE = 0 core-min**, named separately per charter §6 and never absorbed into the ratio: **the failing v1.1 battery is NOT waste** — it bought the finding that two of its own controls were mis-specified, which is the birth requirement performing its function, and it is the reason the item did not publish a verdict on an unfit instrument | **≈ 0.033** (0.20 / 6.00 registered); **1.3 % of cap**. Rule 12's overrun clause binds on the CAP, which was never approached — nothing was stopped and no gate moved | **MISPREDICTION, AND IT IS THE CACHE-STATE TERM FOR THE THIRD ROW RUNNING — WHICH MAKES THIS A RULE-14 FINDING RATHER THAN A THIRD COINCIDENCE.** `C-212` measured a **20.1×** cold/warm spread on this exact shape and drew the lesson that a manifest-heavy re-grade estimate is incomplete without a cache-state term; `C-214` then missed by **4.7×** in the same direction and said so; **this estimate STILL carried no cache-state term and missed by ≈ 30×.** Three rows, one uncorrected term, each time in the same direction. **A lesson whose remedy is "remember the cache" has no call site and cannot be applied — the remedy is a cache-state row in the estimate table itself, and until one exists this ledger will keep recording the same miss.** **The mechanism here:** both preserved roots were **warm** from the earlier passes, and an instrument-only re-grade of preserved artefacts is dominated by **I/O, not CPU**; the 6.00 figure priced ~2–3 s per re-grade for a 25.7 MB `cp -a` plus two 378-file md5 manifests, and the measured cost is ≈ 0.2 s. **CONTENTION: named and excluded on measurement, not assumed away** — the v1.1 and v1.2 batteries cost 5 wall s each on byte-identical scale of work, so no contention term is detectable at this resolution and it explains nothing here. **CARRY-FORWARD:** price a **warm** manifest of a ~380-file / 26 MB root at **≈ 0.15 s** and a cold one at roughly an order of magnitude more, and **state which state the estimate assumes** — an estimate that does not say is unfalsifiable by that factor | `cases/dafoam/ladder-a/A1/curriculum_AV2RG/RESULTS.md` §11, landed with this row's commit; verdicts of record `AV2RG_regrade.json`, gate detail `AV2RG_full_grades.json`, selftest evidence `av2rg_selftest_evidence_v1_2_clean.txt` (28/28) and the preserved failing predecessor `av2rg_selftest_evidence.txt` (24/26); pre-registration frozen `2f827746`, amended pre-compute `42a3c988` and post-compute `cb9adb7f` under `VERIFICATION_CHARTER.md` §2d.1 with **`VERDICTS MOVED: 0, BANDS MOVED: 0, THRESHOLDS MOVED: 0`**; comparator md5 `2dc1cc87eada389da28118e2c3318fd0` and reader md5 `51f65b1d67885b8960201acfc1d2a44d`; the six frozen predecessor files each verified disk == `HEAD` blob by the comparator itself at execution |
| ~~**C-215**~~ **STRUCK — DUPLICATE ID. THE REPLACEMENT ROW IS *NOT YET LANDED* AND THIS SENTENCE REPLACES ONE THAT SAID IT LANDED IN THE SAME COMMIT, WHICH WAS FALSE FOR ELEVEN MINUTES.** The strike stands and is correct: this is the SECOND C-215 landing (`84c0a769`, 2026-08-30 23:36:22) and the first (`16a39340`, 23:14:33) keeps the id, ruled by LANDING ORDER at `docs/DEAD_LEVER_AUDIT.md` §23.4. **What failed is the OTHER half.** The replacement was to be appended in the same invocation through `scripts/append_record.py` per §22.5, and that tool REFUSED THE WHOLE RECORD at exit 7 — its D549 shape audit found two lines matching an id-bearing CANDIDATE SHAPE that yield no id, and **neither is this row**: they are the strikes at `:304` and `:306`, whose form `~~C-216 → C-225~~` is not among the strike forms the tool's register knows. This row's own form IS registered and passes the audit. **My commit proceeded on a failed assert because the guard printed its refusal and the shell did not abort — my defect, disclosed here rather than tidied.** **NO ID IS RESERVED FOR THE REPLACEMENT AND NONE IS NAMED HERE** — §23.1 rules pre-assignment dead, so the id will be whatever `append_record.py` derives inside the invocation that finally commits it. Until then this row is STRUCK AND UNREPLACED, which is a visible, honest, temporary state and not a dangling reference. Data cells below are byte-unchanged through both edits; no cost figure is disputed and none moves | 2026-08-30 | dafoam | **SO-2a instrument repair — SO2a-DRIVER-DEF-1.** `so2a_chain_driver.sh:143` called `$HERE/so2a_aggregate_memory.py`, which **did not exist on disk and had never existed in any commit on any branch**. `AGG` was the empty string every poll, the `json.load` raised, the `ok` test could never be true, and the driver was **guaranteed** to poll to the 14400 s bound and write `chain=BLOCKED_AGGREGATE` at the FIRST arm. The queue runner launched it 2026-08-30T23:13:26Z; the `dafoam-supervisor` stopped it personally at **23:18:49Z**. **THIS ROW IS NOT THE SO-2a ITEM.** SO-2a has bought nothing and its label remains `PENDING`; the process completed here is the **repair**: adopt the absent script byte-for-byte from SO-1a, prove the gate can refuse in both directions on the driver's own bytes, amend §7's instrument table by dated addendum, and sweep the family for the same shape | **NONE REGISTERED — stated as absent rather than approximated (append rule 2).** No pre-registration prices an instrument repair, so this row has **no predicted figure and therefore no ratio**. The item's own registered **9.19 core-min at ranks 1, cap 79.0** is *not* the predicted column here and is **not** compared: comparing an item estimate against a repair's instrument time would be a category error and would corrupt the ledger's ratio series | **≥ 0.462 core-min at ranks 1**, and the inequality is deliberate. Timed drives: refusal selftest **19.83 wall s** (`cpu 1.23 u + 0.77 s`), sibling sweep on the live tree **0.31 wall s**, sweep on the known-positive exported tree **1.89 wall s**, and the standalone L4 diagnostic **5.708 wall s** → 27.74 wall s = 0.462 core-min. **One drive is NOT TIMED and is therefore not in the figure**: the first, failing run of the refusal selftest, the one that exposed the harness's own `PERMISSION` defect. It is named rather than estimated | **0.367 core-min** = the three drives that produced the committed evidence (19.83 + 0.31 + 1.89 = 22.03 wall s at ranks 1). **No invocation is within two orders of magnitude of the 3600 wall-s stall rule** — the longest is 19.83 wall s. **DERIVED, NOT MEASURED: $0.00031** at $0.0513/core-h, c7a.4xlarge, reported-by-owner (`COMPUTE_BUDGET_CHARTER.md` §5 — the box cannot read its own billing) | **NO RATIO. Nothing was predicted, so nothing is divided.** A ratio computed against a figure this row does not carry would be the ledger's own version of the defect it records | **WASTE, NAMED SEPARATELY AND NEVER FOLDED INTO ANY RATIO (charter §6): the stopped launch. SOLVER COMPUTE = ZERO core-min** — no container was ever created, in any state, so no solver process existed to spend anything. What it did consume is **323 wall s** (23:13:26Z → 23:18:49Z) of a driver that was sleeping 30 s between nine polls, and an **8 GiB memory reservation** it would have held for **four hours** had it not been stopped. **The honest statement is near-zero compute waste with the wall time and the reservation named** — and the thing actually saved is the item's registered 9.19 core-min, which would have been spent to produce `BLOCKED_AGGREGATE` and no verdict. **GAP ATTRIBUTION FOR THE DEFECT ITSELF, which is the calibration lesson: this was not misprediction, not contention and not solver behaviour — it was a COPY-FORWARD THAT TOOK THE DRIVER AND NOT ITS DEPENDENCY, invisible to a freeze whose instrument table enumerates INSTRUMENTS BUT NOT THEIR DEPENDENCIES.** §7's md5-agreement control ran honestly and read *eight of eight AGREE* while the ninth file was absent from the box. **AN INSTRUMENT TABLE CAN BE COMPLETE AND WRONG AT THE SAME TIME.** **CARRY-FORWARD, with a call site so it is applied rather than remembered (rule 14): `scripts/sweep_dafoam_script_references.py` now walks every `.sh` under `cases/dafoam` with `os.walk` — never a recursive grep, because ugrep honours ignore files and races on multi-file output — and resolves every `$HERE/`, `$VAR/` and bare-invocation reference against the filesystem. Live tree: 83 files, 437 references, 417 PRESENT, 0 ABSENT, 20 RUNTIME (each traced to a staging `cp` and an existing source). Validated against a KNOWN POSITIVE FIRST — pointed at the tree exported from `HEAD` before this repair it reports the absent script ABSENT — so its zero is a claim about the family and not about the reader.** A second, smaller carry-forward: the sweep's first version reported a FALSE ABSENT (`curriculum_D18R_P7`'s `./d18_grade.py`, which is present at `$D18/`, reached via a preceding `cd "$D18"`); the resolver was fixed and the known positive re-driven, because **a false ABSENT is the noise a real ABSENT hides in** | `cases/dafoam/ladder-a/A1/curriculum_SO2a/SUPERVISOR_STOP.txt`; `PREREGISTRATION.md` **ADDENDUM A** (version 1.1, appended via `scripts/append_block.py`, landed == source byte-for-byte, provenance `sha256:d88fb453…`) with its prefix proof `so2a_addendum_prefix_proof.txt` (`lines whose number changed above this section: 0`, PROVED by byte comparison, with a one-byte mutation shown making the same comparator report 1); refusal evidence `so2a_aggregate_memory_REFUSAL_EVIDENCE.txt` (**15 of 15 checks as registered, rc 0**: known positive, MUST-WAIT at aggregate 4010.12 GiB vs ceiling 30.6, MUST-PROCEED at 14.14 vs 30.6, and `BLOCKED_AGGREGATE` shown REACHABLE); zero delta `so2a_aggregate_memory_DELTAS_from_so1a.diff`; sweep `scripts/sweep_dafoam_script_references.py`; re-filed queue entry `verification/queue/dafoam/SO2a_chain.json` (validator **ACCEPTED** on the explicit path); all landed with this row's commit |
| **C-217** | 2026-08-31 | dafoam | **D6R — THE ITEM'S OWN CHAIN SPEND, `2,282.133` core-min, WHICH UNTIL THIS ROW HAD NO CALIBRATION ROW ANYWHERE.** 3-D multipoint wing shape optimisation (ladder-a A2, `curriculum_D6R`), four registered arms `O_mp` / `ACC_mp` / `F_mp` / `REF_off`, ranks **4**, 20g cgroup, PATCHED row (`dafoam-idwarp-rot:v1`, digest `sha256:2927768a…`). **The chain STOPPED AT THE FIRST NON-ZERO rc**, closing verbatim `chain=STOPPED_AT_FIRST_NONZERO arm=ACC_mp rc=124 order=[O_mp ACC_mp F_mp REF_off] not_run=[F_mp REF_off]` at **2026-08-29T02:00:26Z** (MEASURED, `/home/ubuntu/certonomous-runs/CURRICULUM-D6R-a2-wing-multipoint/STATUS.chain` and `CHAIN_DONE`). Item verdict **`NOT A RESULT`**, ruled 2026-08-30. **THIS ROW IS A DIFFERENT OBJECT FROM THE TWO ROWS A READER FINDS NEARBY, and that confusion is exactly why the spend went unrowed for two days:** `C-188` prices the **PREDECESSOR** `D6`; `C-214` prices `D6RG`'s **0.320 core-min of ZERO-SOLVER instrument time**. Neither is D6R's chain. **PROVENANCE TAGS SIT WITH EACH NUMBER, not in a header** (dafoam §18.6, adopted 2026-08-30), and every MEASURED tag names its artefact. | **2,763.7 core-min REGISTERED for FOUR arms**, ceiling **3,270.0 REGISTERED** (`PREREGISTRATION.md` §4 cost table, `:267-271`, frozen before compute). Per arm REGISTERED, prediction then cap: `O_mp` **2,500.6** / cap **2,900.0**; `ACC_mp` **14.7** / cap **30.0**; `F_mp` **231.2** / cap **300.0**; `REF_off` **17.1** / cap **40.0**. **A CORRECTION TO A FIGURE IN CIRCULATION: `ACC_mp`'s registered PREDICTION is `14.7`, not `30.0` — `30.0` is its CAP**, and a ratio taken against the cap would flatter the estimate by 2.04x. `O_mp`'s point is `80 majors x 31.258 core-min/major`, the anchor **MEASURED on D6** (`2,000.533 / 64 majors`) and imported by REGISTRATION at `PREREGISTRATION.md:256` citing `C-188`. Dollars **$2.3630 estimate / $2.7958 ceiling, DERIVED NOT MEASURED** at $0.0513/core-h; `cost_basis` **REPORTED-BY-OWNER, NOT MEASURED** — the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` §5). | **2,282.133 core-min MEASURED**, basis **gross**, read by this lane directly from `/home/ubuntu/certonomous-runs/CURRICULUM-D6R-a2-wing-multipoint/ledger.txt`: `ARM=O_mp … rc=0 wall_s=33869 ranks=4 core_min=2257.933` and `ARM=ACC_mp … rc=124 wall_s=363 ranks=4 core_min=24.2`. **`F_mp` and `REF_off` have NO `ARM=` row in that file at all — they never ran and spent 0 core-min MEASURED (by absence, corroborated by `STATUS.chain`'s explicit `not_run=[F_mp REF_off]`).** Neither arm that ran was OOM-killed: `inspect(exit,oomkilled)=[0 false]` and `[124 false]` MEASURED. **$1.951 DERIVED, NOT MEASURED** (`2,282.133 ÷ 60 × $0.0513`, this row's own arithmetic, copied from no artefact); `cost_basis` **REPORTED-BY-OWNER**. | **2,282.133 — gross == cleaned, and the 3,600-second figure is ADDRESSED rather than passed over** (the `C-188` precedent, same case, same shape). `O_mp`'s **33,869 s** wall MEASURED is far above that threshold, so the row names the rule and shows it does not match: `scripts/self_audit.py` (`STALL_SECONDS = 3600.0` at `:175`) identifies a stall by cross-family wall-time **clustering** no per-solver cost mechanism explains — its own docstring at `:476` reads *"A stall is identified, not assumed"* — never by the threshold alone. Here the wall is **one deliberate optimisation running against a REGISTERED in-container deadline of 43,410 s**, carrying **73 IPOPT majors MEASURED** in its own log, on a cpuset that delivered **`delivered_cores_mean` 3.9791 of 4 over n = 2,240 samples MEASURED** with `siblings_pre` and `siblings_post` both EMPTY. There is nothing to clean, and no row is excluded. | **NOT COMPUTABLE.** The registered 2,763.7 prices **FOUR** arms and the measured 2,282.133 bought **TWO**, so their quotient would read as a modest, well-estimated under-spend while in fact describing **a chain that stopped** — arithmetically correct, evidentially false, and corrupting to the very series this column exists to build. **No approximation, no bracketed figure and no smoothed variant of it is recorded here.** `ACC_mp`'s own ratio is **ALSO NOT COMPUTABLE**: `24.2` MEASURED against `14.7` REGISTERED compares an arm **killed by `timeout` at 363 s having completed NONE of its registered program** against a price for completing it. **The comparison that IS legitimate is PER ARM at the level where the programs match, and it is in the gap column.** | **THE ONE FIGURE THAT CALIBRATES, AND IT IS EXCELLENT — `O_mp`'s PER-MAJOR RATE AT `0.9895`.** `O_mp` cost **30.9306 core-min/major DERIVED**, arithmetic on two MEASURED figures: `2,257.933` core-min from `ledger.txt`, and **73** majors from `O_mp/opt_IPOPT.txt:806`, `Number of Iterations....: 73`. Against the **31.258 core-min/major** anchor (MEASURED on D6, REGISTERED at `PREREGISTRATION.md:256`) that is **0.9895 DERIVED — the estimate was 1.05 % low over 73 majors of an identical configuration.** **This is the single most useful estimating fact the item produced**, and it is `C-188`'s lesson landing: D6 missed by **1.6308x** because it priced a multipoint major as `3 x` a single-point one; D6R re-anchored on the MEASURED multipoint major and came in inside **1.1 %**. **THE ANCHOR IS THE PREDICTOR, NOT THE FACTOR, AND RE-ANCHORING WORKED.** Prediction **`P5`** (band `[1,900, 2,900]`, point `2,500.6`) **HIT**. **AND THE RATE HELD THROUGH HEAVY BACKTRACKING, which `C-188` warned it might not:** `opt_IPOPT.txt` carries **673** `Cutting back alpha` warnings over **73** majors, both integers MEASURED, of which `9.219178082191782` per major is arithmetic (the value `RESULTS.md:147` prints). **NO CROSS-ITEM PER-MAJOR CUTBACK COMPARISON IS MADE HERE, DELIBERATELY:** D6's count is on record as **545** in `C-188`, a figure of **548** is in circulation, the two divide to different per-major values, and **8.5625 is a division no artefact of this lab prints** — an unresolved discrepancy is reported, not averaged. **THE ARM-LEVEL `O_mp` RATIO IS `0.9029` DERIVED (`2,257.933 / 2,500.6`) AND MUST NOT BE READ AS ESTIMATE QUALITY**, because it carries a program-size term the estimate did not miss: the registered point priced **80** majors and IPOPT stopped at **73**, on `EXIT: Invalid number in NLP function or derivative detected.` MEASURED at `opt_IPOPT.txt:822` — **not** at `max_iter`. Rate right, program short. **`ACC_mp` IS NOT A COST MISS AT ALL BUT `D6R-PREREG-DEF-1`: THE CAP PRICED A PROGRAM THE ARM DOES NOT RUN.** The registered program (`compute_totals` on a cold staged copy at np = 4, which must colour the Jacobian three times, once per scenario) needed **1,685.98 s = 112.40 core-min — EXTRAPOLATED, NOT MEASURED ON THIS ARM, WHICH NEVER COMPLETED IT**: six of its seven segments are timings lifted from `O_mp`'s log plus 75.10 s from `ACC_mp`'s own log, the seventh is INTERPOLATED between two measurements differing by 3.2 %, and an independent anchor from D5's `ARM=ACC48 … core_min=35.8` gives **107.40 core-min**, agreeing within **4.7 %** (`curriculum_D6RACC2/PREREGISTRATION.md` §2a, §2b). **The shortfall is 4.68x the 360 s deadline and 3.75x the 30.0 core-min cap**, and the arm died with **53,124 of 98,959 cells still uncoloured at 335.62 s — 46.3 % through the FIRST of three colourings.** Its cap traced to a `9.0 x 1.6308` cell whose multiplier is attributed and whose **base is attributed to nothing**: `grep -c 'C-94'` over the frozen registration returns **0**. **`F_mp` AND `REF_off`: REGISTERED at 231.2 and 17.1 core-min, NEVER RAN, spend 0 — AND AN ARM THAT NEVER RAN IS NOT A ZERO-COST SUCCESS.** `F_mp` runs the FD primals, so `G-D6R-1` … `G-D6R-4` and predictions `P2`, `P3`, `P4`, `P6` all read `NOT A RESULT` for want of inputs the item paid to stop before producing. **CONTENTION: NAMED, MEASURED, AND NEAR-ZERO — IT EXPLAINS NOTHING HERE, and is recorded so it cannot later be offered as an explanation.** `delivered_cores_mean` **3.9791 of 4 (99.48 %, n = 2,240, `max_nr_throttled` 139,171)** on `O_mp` and **3.9282 of 4 (98.21 %, n = 24, 1,595)** on `ACC_mp`, `siblings_pre` and `siblings_post` EMPTY on both — all MEASURED in `ledger.txt`. **WASTE, NAMED SEPARATELY PER CHARTER §6 AND FOLDED INTO NO RATIO: the whole 2,282.133 core-min BOUGHT NO GRADED GATE.** The honest split rather than a single word: **0 core-min bought a gate reading**; **2,257.933 bought the per-major calibration above, the `P5` HIT, and the `NOT A RESULT` of record**; **24.2 bought the 75.10 s start-up-plus-three-primals segment MEASURED in `ACC_mp`'s own log and the 335.62 s colouring observation — the evidence `D6R-PREREG-DEF-1` and the successor `D6RACC2`'s 112.40 core-min anchor now rest on.** Compute that buys a defect finding is not free and is not laundered here as useful. **CARRY-FORWARD, three items:** **(1)** re-anchoring on a MEASURED major of the same configuration held to **1.1 %** over 73 majors — the family's estimating error has been the **anchor choice**, never the rate (`C-182`, `C-188`), and this row is the first confirmation in the other direction. **(2) A per-major anchor prices the RATE and says NOTHING about the PROGRAM SIZE.** D6R bounded majors with `max_iter = 80` precisely so the optimiser's cap would bind first, and the optimiser still stopped early on an evaluation error — **a chain estimate needs an early-exit branch beside its rate**, or its arm-level ratio is unreadable. **(3) An arm whose cap is a bare multiplier over an uncited base is UNPRICED, not cheaply priced** — and `PREREGISTRATION.md:298` recording that a row was owed is what made this gap findable at all. | `cases/dafoam/ladder-a/A2/curriculum_D6R/RESULTS.md` §2 (per-arm census), §4a (`D6R-PREREG-DEF-1`) and §7, which flags this row as owed and deliberately declines to fill it; `cases/dafoam/ladder-a/A2/curriculum_D6R/PREREGISTRATION.md` §4 `:256`, `:267-271` and `:298` ("A calibration row is owed at completion"). Every MEASURED figure above was read by this lane from the run root itself — `/home/ubuntu/certonomous-runs/CURRICULUM-D6R-a2-wing-multipoint/{ledger.txt, STATUS.chain, CHAIN_DONE, O_mp/opt_IPOPT.txt}` — and not copied from a relay. EXTRAPOLATED figures from `cases/dafoam/ladder-a/A2/curriculum_D6RACC2/PREREGISTRATION.md` §2a/§2b; anchor and predecessor `C-188`; successor instrument time `C-214`; verdict of record `cases/dafoam/ladder-a/A2/curriculum_D6RG/D6RG_regrade.json`. |
| **C-218** | 2026-08-31 | dafoam | **AV-1R (A1) — THE ORIGINAL SOLVER CHAIN'S OWN SPEND, `9.069` core-min, WHICH UNTIL THIS ROW HAD NO CALIBRATION ROW ANYWHERE.** This row prices **one object and names it**: the seven-arm np-invariance chain `MESH / X1-S / X2-S / X4-S / X1-P / X2-P / X4-P` executed 2026-08-28T07:02:44Z to 07:13:53Z on A1's 4,032-cell NACA0012, **seven of seven arms `rc = 0`**, `OOMKilled false` (**MEASURED** — the seven `ARM=` rows of `/home/ubuntu/certonomous-runs/CURRICULUM-AV1R-a1-naca0012-npinv/ledger.txt`). **It does NOT price the `AVWC` re-grade that later moved this item's verdict** — that instrument time is `C-212`'s **1.33353** core-min, a different object — **and it does not price `AV-1`**, whose own chain is `C-158`. The item's verdict travelled `NOT A RESULT` to **`PASS`** via `AVWC` (**REPORTED** — `C-212`; `cases/dafoam/ladder-a/A1/curriculum_AV1R/RESULTS.md` §1.2); the verdict move is stated for orientation and is **not** what this row calibrates. **The absence was checked, not assumed:** at `HEAD` the token `AV1R` occurred in exactly **two** rows, `C-212` and `C-216`, and **both are ZERO-SOLVER-COMPUTE re-grades** (**MEASURED** — token sweep over `git show HEAD:docs/COST_CALIBRATION.md`) | **19.3 core-min REGISTERED point estimate**, band **[10, 50]**, ceiling **95.0 = sum of per-arm caps** (**REGISTERED** — `cases/dafoam/ladder-a/A1/curriculum_AV1R/PREREGISTRATION.md` §4 table at `:82`); per-arm points MESH **0.3**, X1-S and X1-P **2.5** each, X2-S and X2-P **3.0** each, X4-S and X4-P **4.0** each. **THE POINT IS THE PREDICTION AND THE CEILING IS NOT.** A ratio taken against `95.0` would flatter this estimate by **4.92x**; the ceiling is quoted here only as a cap. $**0.0165** point and $**0.0812** ceiling, **DERIVED NOT MEASURED** at $0.0513/core-h, `cost_basis` **REPORTED-BY-OWNER** (`COMPUTE_BUDGET_CHARTER.md` §5 — the box cannot read its own billing). **A SECOND registered comparator is named by the pre-registration itself** at `:84` — *"must compare against both the 19.3 point and AV-1's measured 9.102"* — namely AV-1's seven-arm chain at **9.102 core-min** (**MEASURED** by this lane at `/home/ubuntu/certonomous-runs/CURRICULUM-AV1-a1-naca0012-npinv/ledger.txt`, seven `ARM=` rows summed; read at the artefact rather than inherited from the pre-registration's quotation of it) | **9.069 core-min MEASURED**, summed from the seven `ARM=` rows of `/home/ubuntu/certonomous-runs/CURRICULUM-AV1R-a1-naca0012-npinv/ledger.txt` — MESH **0.167** at 1 rank, X1-S **1.017** at 1, X2-S **1.367** at 2, X4-S **2.067** at 4, X1-P **1.017** at 1, X2-P **1.367** at 2, X4-P **2.067** at 4 — and independently confirmed at **9.069** by the frozen grader's own `gates.G10_caps.total_core_min` with `not_measured: []` (**MEASURED** — `cases/dafoam/ladder-a/A1/curriculum_AVWC/AVWC_full_grades.json` at `AV1R.grade`). = **0.15115 core-h** = **$0.0077540 DERIVED, not measured**, `cost_basis` **REPORTED-BY-OWNER** | **= gross, 9.069 core-min.** The longest arm is **61 wall s** (**MEASURED** — same ledger), **two orders of magnitude below the 3,600 wall-s stall rule** (`COMPUTE_BUDGET_CHARTER.md` §2), so no row is cleanable and the cleaning rule is named rather than assumed. **WASTE = 0.000 core-min**, named separately per charter §6 and never absorbed into the ratio: the run root holds **exactly seven container logs, one per registered arm and no eighth** (**MEASURED** — log enumeration of that root), every arm `rc = 0`, and no arm was re-run or abandoned | **0.4699x actual/predicted** (9.069 MEASURED / 19.3 REGISTERED point); **9.5 % of the 95.0 ceiling**, quoted as a cap and not as a denominator. **THE RATIO IS COMPUTED AND NOT REFUSED, BECAUSE THE PROGRAM THAT RAN IS THE PROGRAM THE ESTIMATE PRICED** — the registered `launch_cmd` names the seven arms `MESH X1-S X2-S X4-S X1-P X2-P X4-P` (`PREREGISTRATION.md:144`) and the ledger carries those seven and only those, all `rc = 0`, none truncated, skipped or refused. **Against the pre-registration's OWN second comparator the estimate is near-exact: 0.9964x** (9.069 / 9.102, both **MEASURED** at the ledgers cited). Rule 12's overrun clause binds on the cap, which was never approached — nothing was stopped and no gate moved; `G10` reads **`PASS`** | **MISPREDICTION, DELIBERATE, AND IT IS THE MOST USEFUL THING IN THIS ROW — not contention, not waste, not truncation.** The **2.13x** under-spend (19.3 / 9.069) is **entirely** the pre-registration's own recorded decision to leave the point at AV-1's original 19.3 rather than move it to AV-1's measured 9.102: *"the estimate is not tuned to a measurement taken after the freeze it belongs to; the gap is a calibration finding, not a band, and it will be reported as one"* (`PREREGISTRATION.md:73`). **This row is that report.** The finding is sharp because **the same document held both numbers**: the untuned point missed by **2.13x** while the measured anchor sitting two sentences away was right to **0.36 %**. **What that costs and what it buys, stated so the lab can decide rather than infer:** refusing to retune preserved freeze integrity (rule 2 — a price chosen after the answer is not a prediction) and cost the ratio series a 2.13x miss; **the resolution is not to retune a frozen point but to register BOTH figures, which this item did, and to SCORE both, which is what this row does.** The registered prediction **`P5_total_core_min_band` is a `MISS`** — 9.069 falls **below** the registered `[10, 50]` band (**MEASURED** — `AVWC_full_grades.json` at `AV1R.grade.predictions`; printed as a MISS in `curriculum_AV1R/RESULTS.md` §3 rather than omitted). **CONTENTION IS NOT THE TERM:** `delivered_cores_mean` reads **0.9934 of 1**, **1.9564 of 2** and **3.6403 of 4** across the arms that measured it (**MEASURED** — same ledger), so the box delivered what was reserved. **§18.2 does not bind this row** — a solver item, not an instrument-only one, so no cache-state term is owed | `cases/dafoam/ladder-a/A1/curriculum_AV1R/RESULTS.md` §7, which states this spend and then says *"Whether that spend has a calibration row of its own is not established by this record"* — **this row settles it, and the answer was NO**; ledger `/home/ubuntu/certonomous-runs/CURRICULUM-AV1R-a1-naca0012-npinv/ledger.txt`; gate detail `cases/dafoam/ladder-a/A1/curriculum_AVWC/AVWC_full_grades.json` at `AV1R.grade.gates.G10_caps`; pre-registration `cases/dafoam/ladder-a/A1/curriculum_AV1R/PREREGISTRATION.md` §4; anchor ledger `/home/ubuntu/certonomous-runs/CURRICULUM-AV1-a1-naca0012-npinv/ledger.txt`. **ID DERIVATION DISCLOSED (`CLAUDE.md` rule 11):** `C-218` is maximum-plus-one over HEAD's ids with **MAX = `C-217`**, re-derived in this commit's own shell invocation with a pattern that matches the **BOLD** form as well as the plain one — a plain-only pattern reads MAX as `C-215` and would mint a third duplicate. **OBSERVED AND DELIBERATELY NOT REPAIRED: `C-215` occurs TWICE at HEAD**, on two distinct dafoam processes (*SO-2a instrument work* and *SO-2a instrument repair, SO2a-DRIVER-DEF-1*); renumbering a shared ledger is not this lane's call and is reported to the supervisor instead of being fixed here |
| **C-219** | 2026-08-31 | dafoam | **AV-2R (A1) — THE ORIGINAL SOLVER CHAIN'S OWN SPEND, `20.766` core-min, WHICH UNTIL THIS ROW HAD NO CALIBRATION ROW ANYWHERE.** This row prices **one object and names it**: the five-arm forward-AD-versus-reverse-AD duality chain `MESH / X-S / FAD-S / X-P / FAD-P`, **all at ranks 1 by registration**, executed 2026-08-28T02:18:44Z to 02:44:00Z on A1's 4,032-cell NACA0012, **five of five arms `rc = 0`**, `OOMKilled false` (**MEASURED** — the five `ARM=` rows of `/home/ubuntu/certonomous-runs/CURRICULUM-AV2R-a1-naca0012-duality/ledger.txt`). **It does NOT price the `AV2RG` re-grade that later moved this item's verdict** — that instrument time is `C-216`'s **≈ 0.20** core-min, a different object — **and it does not price `AV-2`**, whose own chain is `C-159`. The item's verdict travelled `NOT A RESULT` to **`BLOCKED`** via `AV2RG` (**REPORTED** — `C-216`; `cases/dafoam/ladder-a/A1/curriculum_AV2R/RESULTS.md` §1.2); the verdict move is stated for orientation and is **not** what this row calibrates. **The absence was checked, not assumed:** at `HEAD` the token `AV2R` occurred in exactly **two** rows, `C-212` and `C-216`, and **both are ZERO-SOLVER-COMPUTE re-grades** (**MEASURED** — token sweep over `git show HEAD:docs/COST_CALIBRATION.md`) | **15.3 core-min REGISTERED point estimate**, band **[8, 40]**, ceiling **75.0 = sum of per-arm caps** (**REGISTERED** — `cases/dafoam/ladder-a/A1/curriculum_AV2R/PREREGISTRATION.md` §4 table at `:104`); per-arm points MESH **0.3**, X-S and X-P **2.5** each, FAD-S and FAD-P **5.0** each, the FAD derivation reading *"1 primal + 5 forward-mode primals at 3x"*. **THE POINT IS THE PREDICTION AND THE CEILING IS NOT.** A ratio against `75.0` would flatter this estimate by **4.90x** and would turn a **1.36x over-spend into a 0.28x under-spend** — the sign of the finding, not merely its size; the ceiling is quoted only as a cap. $**0.0131** point and $**0.0641** ceiling, **DERIVED NOT MEASURED** at $0.0513/core-h, `cost_basis` **REPORTED-BY-OWNER** (`COMPUTE_BUDGET_CHARTER.md` §5). **A SECOND registered comparator is named by the pre-registration itself** at `:106` — *"must compare against both the 15.3 point and AV-2's measured 19.085"* — namely AV-2's five-arm chain at **19.085 core-min** (**MEASURED** by this lane at `/home/ubuntu/certonomous-runs/CURRICULUM-AV2-a1-naca0012-duality/ledger.txt`, five `ARM=` rows summed) | **20.766 core-min MEASURED**, summed from the five `ARM=` rows of `/home/ubuntu/certonomous-runs/CURRICULUM-AV2R-a1-naca0012-duality/ledger.txt` — MESH **0.183**, X-S **1.017**, FAD-S **9.4**, X-P **1.033**, FAD-P **9.133**, all at ranks 1 — and independently confirmed at **20.766** by the frozen grader's own `gates.G10_caps.total_core_min` with `not_measured: []` (**MEASURED** — `cases/dafoam/ladder-a/A1/curriculum_AV2RG/AV2RG_full_grades.json` at `AV2R.grade`). = **0.34610 core-h** = **$0.0177549 DERIVED, not measured**, `cost_basis` **REPORTED-BY-OWNER** | **= gross, 20.766 core-min.** The longest arm is **564 wall s** against the **3,600 wall-s** stall rule (`COMPUTE_BUDGET_CHARTER.md` §2), so no row is cleanable and the cleaning rule is named. **WASTE = 0.000 core-min, named separately per charter §6, NOT absorbed into the ratio, and ARGUED rather than asserted** — because the two FAD arms burned **18.533 core-min** and produced **zero graded components**, twenty component-readings all `blocked`, which is exactly the shape that a careless row would launder as waste. It is not waste, on the **`C-4` precedent** (*"the crash IS the measurement"*): AV-2R's frozen text **PREDICTED `BLOCKED` IN ADVANCE** — *"Predicted item verdict, written here so it cannot be written afterwards: `BLOCKED`"* (`PREREGISTRATION.md:135`) — so the blocked readings **are the item's registered product**, not a failed attempt at something else. Five container logs, one per registered arm and no sixth; every arm `rc = 0` (**MEASURED** — log enumeration of that root) | **1.3573x actual/predicted** (20.766 MEASURED / 15.3 REGISTERED point); **27.7 % of the 75.0 ceiling**, quoted as a cap and not as a denominator. **THE RATIO IS COMPUTED, NOT REFUSED, AND THAT JUDGEMENT IS THE WHOLE POINT OF THIS ROW.** A `BLOCKED` verdict is not by itself a licence to divide: the question is whether the run **executed the program the estimate priced**, and here it did — checked three ways rather than assumed. **(i)** The registered `launch_cmd` names five arms `MESH X-S FAD-S X-P FAD-P` (`PREREGISTRATION.md:164`) and the ledger carries those five and only those, all `rc = 0`, `OOMKilled false`. **(ii) Nothing was truncated by a deadline or a cap:** FAD-S ran **564 wall s** against its registered **1,500 s** in-container deadline and **9.4** core-min against its **25.0** cap; FAD-P **548 s** and **9.133** (**MEASURED** — same ledger). **(iii) All five forward-mode primals the estimate priced actually ran, and each ran its full quantum:** `FAD-S`'s container log carries **five** `Primal solution failed!` lines, at lines **1285 / 1958 / 2631 / 3304 / 3982** of **3,989**, spaced **673 / 673 / 673 / 678** lines apart — even spacing means each forward primal did the same full amount of work and failed at its **acceptance test**, not by aborting early (**MEASURED** — `/home/ubuntu/certonomous-runs/CURRICULUM-AV2R-a1-naca0012-duality/FAD-S_20260828T022210Z_1718283.log`). **So the `BLOCKED` is a PHYSICS OUTCOME, NOT A TRUNCATION, and the FAD arms came in ABOVE their price (1.88x and 1.8266x) rather than below — a truncation cheapens an arm and this one did the opposite.** **Against the pre-registration's own second comparator: 1.0881x** (20.766 / 19.085, both **MEASURED**) — the same program run twice agreeing to **8.8 %**, which is the strongest evidence in this row that the denominator prices what ran. **SCOPE, so the ratio is not over-read:** it calibrates this chain **as run, with a non-converging forward channel**; what a *converging* forward-mode primal would cost on this case is **UNMEASURED** and this row does not bound it. Rule 12's overrun clause binds on the cap, never approached; `G10` reads **`PASS`** | **MISPREDICTION, LOCALISED TO ONE TERM — not contention, not waste, not truncation.** The whole 1.36x lives in the two FAD arms: the three non-FAD arms came in **UNDER** at **0.61x**, **0.4068x** and **0.4132x** while FAD-S and FAD-P came in **OVER** at **1.88x** and **1.8266x** (**MEASURED** — the frozen grader's own per-arm `ratio_actual_over_predicted` at `AV2RG_full_grades.json` → `AV2R.grade.gates.G10_caps.per_arm`). **The defective term is the FAD derivation's `3x` multiplier:** it priced a forward-mode primal at 3x a reverse-mode arm and the measured figure is **9.24x** — FAD-S / X-S = 9.4 / 1.017 — which is precisely the registered prediction **`P5_fad_over_x_cost_ratio_shipped`, scored `MISS`** against its registered `[1.0, 4.0]` band at point 2.0, while **`P5_total_core_min_band` is a `HIT`** (20.766 inside `[8, 40]`) (**MEASURED** — same JSON, `AV2R.grade.predictions`). **TWO REGISTERED PREDICTIONS ON THE SAME SPEND, ONE HIT AND ONE MISS, AND THE MISS IS THE INFORMATIVE ONE:** the total landed inside its band because two errors of opposite sign nearly cancelled, so a row quoting only the total would have recorded a good estimate resting on a per-arm model wrong by **3.1x**. **CARRY-FORWARD, this family's own and cited as such:** on this case a forward-mode primal costs **≈ 9.2x** a reverse-mode adjoint arm at ranks 1, **not 3x** — measured twice, AV-2 at **8.13x** (8.267 / 1.017) and AV-2R at **9.24x** (**MEASURED** at the two ledgers cited) — **on ONE 4,032-cell mesh, at ONE `endTime`, with the forward primal NOT converging; it is this family's measurement on this case, is cited as `C-219`, and is NOT a constant and NOT a lab-wide rate.** **CONTENTION: present, named, and not the dominant term** — `delivered_cores_mean` reads **0.9944**, **0.9969**, **0.9931** and **0.9991 of 1** on the four timed arms (**MEASURED** — same ledger), yet the same ledger records concurrent siblings (`so1a_F-S`, `d18_X-S`, `d18_F-S`) on the box, so contention is reported as present-but-not-dominant rather than as absent. **§18.2 does not bind this row** — a solver item, not an instrument-only one, so no cache-state term is owed | `cases/dafoam/ladder-a/A1/curriculum_AV2R/RESULTS.md` §7, which states this spend and then says *"Whether that solver spend has a calibration row of its own is not established by this record"* — **this row settles it, and the answer was NO**; ledger `/home/ubuntu/certonomous-runs/CURRICULUM-AV2R-a1-naca0012-duality/ledger.txt`; forward-primal evidence `/home/ubuntu/certonomous-runs/CURRICULUM-AV2R-a1-naca0012-duality/FAD-S_20260828T022210Z_1718283.log`; gate and prediction detail `cases/dafoam/ladder-a/A1/curriculum_AV2RG/AV2RG_full_grades.json` at `AV2R.grade`; pre-registration `cases/dafoam/ladder-a/A1/curriculum_AV2R/PREREGISTRATION.md` §4; anchor ledger `/home/ubuntu/certonomous-runs/CURRICULUM-AV2-a1-naca0012-duality/ledger.txt`. **ZERO SOLVER CORE-MINUTES WERE SPENT TO PRODUCE THIS ROW OR `C-218`** — both are reads of preserved ledgers, logs and grade JSONs; no comparator was run, no run root was written, and no queue entry was touched |
| **STRUCK — was C-216** | 2026-08-31 | cfd | **STRUCK DUPLICATE — the R1_F3S_SELECTOR_REPAIR row, RE-ISSUED UNCHANGED AS C-225** (cost, ratio and text identical there). `dafoam`'s **C-216** landed FIRST at `3918abeb`; this cfd row landed at `1ee6d791` and yields the id under verification's LANDING-ORDER ruling. Struck in place under §23.2's sanctioned exception. **The id is written so it neither parses as an id NOR matches D549's candidate shape** — the `~~**C-nnn**~~` register form does BOTH, which is why it refused every append lab-wide at exit 7. Root cause of the collision was my own blind enumeration: `^\| C-[0-9]+` saw 114 of 227 ids, missing every bold row (L-312 unapplied, rule 14). | Cost carried at C-225; nothing is claimed twice. |
| C-217 | 2026-08-31 | closure | **M1 multimodel sweep — RUN CHAIN COMPLETE (76 filed arms), GRADING BARRED.** The 76 entries filed at `c1ad428b` were all consumed by the detached `queue_runner` daemon overnight and the last arm finished 2026-08-31T00:51:46Z. **This row costs the COMPUTE only. It is NOT a result and implies no verdict:** `grade_m1.py` has no fatal or crash channel at all in 988 lines (0 `FOAM FATAL`, 0 `Floating point exception`, 0 `sigFpe`, recognition-controlled by the same reader finding `def ` 31x / `PASS` 11x / `ExecutionTime` 4x), so nothing from this set has been graded and a successor comparator is being registered. Prereg frozen `73cd5ac5`; sweep artefact `/home/ubuntu/closure-data/m1_completion_sweep_2026-08-31.json` | 1,293.198 core-min = 21.553 core-h = **$1.106 derived** (cap 1,885.694 core-min = 31.428 core-h = $1.612) | **774.267 core-min = 12.905 core-h = $0.662 derived** (76 arms, each case's own `STATUS` `wall_s`, ranks=1). Solver-only `ExecutionTime` gives 766.160 core-min, 1.05 % lower, the difference being `checkMesh` plus the un-logged tail of the capped arms; `wall_s` is the honest incurred figure | **648.333 core-min over the 72 arms that produced field output**, against those same arms' 1,206.868 predicted. Cleaning removes the 4 capped arms from BOTH sides, never from the actual alone | **0.5987 gross/predicted; 0.5372 cleaned/predicted** — over-estimated in both readings | **Misprediction, and in BOTH directions from one blended rate.** The registered basis was 3.30 us per cell-iteration; the 72 clean arms ran nearer 1.77 us (~1.9x pessimistic), while the duct `AR_*` and `PH_Breuer` family ran ~1.46x OPTIMISTIC and hit their caps. 16 of 76 arms exceeded their own estimate; median per-arm ratio 0.403, best 0.331, worst 1.459 (`kOmega AR_7_Ret_180`). The aggregate 0.60 therefore hides the useful correction: the estimator is systematically optimistic on the duct family and pessimistic everywhere else. **THIS IS THE THIRD CONSECUTIVE FAILURE OF A SINGLE BLENDED RATE FOR THIS TEAM, AND EACH FAILED IN A DIFFERENT DIMENSION: C-190 a rate sampled under CONTENTION, C-210 a rate not invariant across GRID SCALE, C-217 a rate not invariant across CASE FAMILY.** A rate carries the conditions it was measured under. **Waste named separately per COMPUTE_BUDGET_CHARTER section 6 and NOT absorbed into either ratio: 125.933 core-min (16.3 % of the 76-set spend) bought no field output at all** — 4 arms wrote no numeric time directory. **No arm overran its budget**: each stopped AT its own registered cap (rc=124, `CAP_EXPIRED`), exceeding it only by ~1 s of timeout latency, so rule 12 worked as written and what failed was the prediction, not the enforcement. Gross, not cleaned for stalls: no arm exceeded 3600 wall s, so no row meets the stall rule. **The reboot at 2026-08-31T14:35:01Z destroyed no compute** — 0 of 4,981 paths under the run root carry an mtime or ctime at or after boot, controlled by the same reader seeing 3 post-reboot mtimes elsewhere. Dollars **derived, not measured**, at the owner-stated $0.0513/core-h: the box cannot read its own billing (COMPUTE_BUDGET_CHARTER section 5) | prereg `73cd5ac5`, entries filed `c1ad428b` (76 files, nothing else in the commit, verified by name); per-arm evidence `/home/ubuntu/closure-data/m1_completion_sweep_2026-08-31.json` |
| **STRUCK — was C-218** | 2026-08-31 | cfd | **STRUCK DUPLICATE — the F23b_HP_WEDGE row, RE-ISSUED UNCHANGED AS C-226** (cost, ratio and text identical there). `dafoam`'s **C-218** landed FIRST at `a7583d23`; this cfd row landed at `d910023e` and yields the id under verification's LANDING-ORDER ruling. Struck in place under §23.2's sanctioned exception. **The id is written so it neither parses as an id NOR matches D549's candidate shape** — the `~~**C-nnn**~~` register form does BOTH, which is why it refused every append lab-wide at exit 7. Root cause of the collision was my own blind enumeration: `^\| C-[0-9]+` saw 114 of 227 ids, missing every bold row (L-312 unapplied, rule 14). | Cost carried at C-226; nothing is claimed twice. |
| **C-220** | 2026-08-31 | dafoam | **D6R-ACC2 — THE RE-ANCHORED `ACC_mp` ARM, CHAIN `COMPLETE`, AND THE FAMILY'S BEST CALIBRATION RESULT TO DATE: A RE-REGISTERED ANCHOR LANDED 6.70 % LOW ON A PROGRAM WHOSE PREDECESSOR HAD BEEN MISPRICED BY 3.75x.** Single-arm item (ladder-a A2, `curriculum_D6RACC2`): D6R's `ACC_mp` re-run at a cap derived from two independent measurements of the program it actually runs. `compute_totals` on a cold staged copy at ranks **4**, 20g cgroup, PATCHED row (`dafoam-idwarp-rot:v1`, digest `sha256:2927768a…`). **`chain=COMPLETE`, `arm=ACC_mp rc=0`** at **2026-08-31T00:23:31Z** [MEASURED, `/home/ubuntu/certonomous-runs/CURRICULUM-D6RACC2-a2-wing-multipoint/{CHAIN_DONE, STATUS.chain, STATUS.ACC_mp}`]. **The registered program RAN, and that is checked rather than inferred from `rc=0`:** the arm's own log carries **three** `Calculating dRdW Coloring… Completed!` lines — one Jacobian colouring per scenario, which is the whole reason the predecessor's cap was wrong — and the three scenarios `mp04`/`mp05`/`mp06` all have output trees [MEASURED, `ACC_mp_20260830T235332Z_1606819.log:2852,:2963,:3074` and `ACC_mp/`]. **This row repairs `D6R-PREREG-DEF-1`, the defect `C-217` registered and could not price**; it is a DIFFERENT OBJECT from `C-217` (which prices D6R's stopped chain) and from `C-214` (which prices D6RG's zero-solver instrument time). **Every number carries its tag per dafoam §18.6 and every MEASURED tag names its artefact.** | **112.40 core-min REGISTERED**, ranks **4**, cap **240.00 core-min REGISTERED**, in-container deadline **3,510 s REGISTERED** [`cases/dafoam/ladder-a/A2/curriculum_D6RACC2/PREREGISTRATION.md` §7, frozen before compute at `4e4adf66`; the rule-2 condition checked three times at §8 — `ls -d /home/ubuntu/certonomous-runs/*D6RACC2*` returned **0 entries**]. The 112.40 is **EXTRAPOLATED, not measured on this arm**: `1,685.98 s x 4 ranks / 60 = 112.399` from a seven-segment model in which **six segments are MEASURED** (one from `ACC_mp`'s own aborted D6R log, five from `O_mp`'s) and **the seventh — adjoint 3 + assembly, 113.45 s — is INTERPOLATED** between two measurements differing by 3.2 % (§2a). Corroborated by an independent anchor from D5's `ARM=ACC48 … core_min=35.8` giving **107.40 core-min**, agreeing within **4.7 %** (§2b). Dollars **$0.09610 estimate / $0.20520 cap, DERIVED, NOT MEASURED** at $0.0513/core-h; `cost_basis` **REPORTED-BY-OWNER, NOT MEASURED** — the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` §5). | **119.933 core-min MEASURED**, basis **gross**, read by this lane directly from the run root's `ledger.txt`: `ARM=ACC_mp … rc=0 wall_s=1799 ranks=4 core_min=119.933 cap_core_min=240.0 enforced_wall_s=3510 enforced_core_min=240.000000 memory=20g inspect(exit,oomkilled)=[0 false] container_wall_s=1793`. **Not OOM-killed and not capped: `oomkilled false` MEASURED, and the container's own 1,793 s wall is 51.1 % of the 3,510 s REGISTERED deadline** — the arm finished on its own, it was not stopped. **$0.10254 DERIVED, NOT MEASURED** (`119.933 / 60 x $0.0513`, this row's arithmetic, copied from no artefact); `cost_basis` **REPORTED-BY-OWNER**. | **119.933 — gross == cleaned, and the 3,600-second stall convention is ADDRESSED rather than passed over** (the `C-188`/`C-217` precedent on this same case). The single row's wall is **1,799 s MEASURED**, less than half the threshold, so the rule does not match on its face and nothing is excluded. **`scripts/self_audit.py` (`STALL_SECONDS = 3600.0` at `:175`) identifies a stall by cross-family wall-time clustering no per-solver cost mechanism explains, never by a threshold alone** — and here the mechanism is fully explained segment by segment in the gap column. | **1.0670** MEASURED/REGISTERED (`119.933 / 112.40`), against the **112.40 point** and not against the cap — **a ratio taken against the 240.0 cap would read 0.4997 and would flatter the estimate by 2.14x**. **0.4997x of the registered cap; NO OVERRUN**, and the cap never came near binding. | **THE 6.70 % IS HONEST MISPREDICTION, NOT CONTENTION, AND THE WHOLE GAP CLOSES SEGMENT BY SEGMENT TO THE SECOND.** The gap is **113.02 wall s = 7.534 core-min DERIVED** (`1,799 − 1,685.98`; `119.933 − 112.399`), and it decomposes into four terms that sum to **113.02 exactly**, every measured term read from `ACC_mp_20260830T235332Z_1606819.log`'s own clock: **(1) +57.51 s, 50.9 % of the gap — THE ONE INTERPOLATED SEGMENT.** Adjoint 3 + assembly was REGISTERED at **113.45 s INTERPOLATED** and MEASURED at **170.96 s** (`1,773.60 − 1,602.64`) — **the single term the model did not measure is the single term that broke it, missing by 50.7 % of its own value while the six measured terms missed by 1.9 % in aggregate.** **(2) +30.11 s, 26.6 % — the six MEASURED segments, net +1.915 % over their 1,572.53 s REGISTERED total** (`1,602.64` MEASURED at the end of colouring 3 against `1,572.53` REGISTERED; the difference is exact). Inside it the three colourings each ran long by a consistent **+2.71 / +2.32 / +2.75 %** and start-up plus the three primals ran **6.95 s FAST**. **(3) +19.40 s, 17.2 % — a container-boundary term the model priced at ZERO.** The log's last timestamp is 1,773.60 s and `container_wall_s` is 1,793 [both MEASURED]; the final assembly write-out, finalisation and exit are real and were never in the seven-segment table. **(4) +6.00 s, 5.3 % — image start and stop**, the launcher's `wall_s=1799` less docker's `container_wall_s=1793` [MEASURED]. **CONTENTION: MEASURED, BOUNDED, AND IT EXPLAINS AT MOST A SIXTH OF THE GAP — recorded so it can never later be offered as the explanation.** `delivered_cores_mean` **3.9819 of 4 = 99.55 %, n = 118, `max_nr_throttled` 6276** [MEASURED, `ledger.txt`], so **core starvation can account for at most 0.45 % against a 6.70 % gap.** **AND UNLIKE `C-217`'s `O_mp`, THE SIBLING LISTS ARE NOT EMPTY** — `siblings_pre=[d12y_w3_S3b_c0_am_…]`, `siblings_post=[so2a_G-P_…]` [MEASURED] — so this arm ran co-tenant with W3 and SO-2a throughout, on the disjoint cpuset `2,3,4,14` against W3's `1` and SO-2a's `9`. **A SECOND, INDEPENDENT CONTENTION CONTROL, because delivered-cores only bounds core starvation and says nothing about memory bandwidth:** the three Jacobian colourings are the same computation run three times under a CHANGING sibling population, and they took **430.44 / 435.73 / 437.33 s MEASURED** — a spread of **1.60 %**, monotone. **That bounds any bandwidth or LLC effect on the dominant work at ≈ 1.6 %, which is the same order as the +2.3–2.8 % colouring overrun, so the colouring term is NOT SPLIT between cold cache and co-tenancy here and no split is guessed at.** **WASTE, NAMED SEPARATELY PER CHARTER §6 AND FOLDED INTO NO RATIO: 0.000 core-min.** No re-launch, no stall, no capped row, no discarded attempt; the arm ran once and finished. **WHAT THE 119.933 BOUGHT: the acceptance gradient the whole `D6R-PREREG-DEF-1` repair existed to buy, at a cap that fits.** **CARRY-FORWARD, three items, and the first is the one that matters. (1) RE-ANCHORING WORKS, TWICE NOW, AND THE ERROR IS ALWAYS THE ANCHOR AND NEVER THE RATE.** `C-188` missed by 1.6308x by pricing a multipoint major as 3x a single-point one; `C-217` re-anchored `O_mp` on a MEASURED multipoint major and came in at **0.9895**; this row re-anchored `ACC_mp` on a MEASURED colouring program and came in at **1.0670**. Three consecutive confirmations that **the family's estimating error has been anchor CHOICE, not rate.** **(2) AN INTERPOLATED SEGMENT IS THE WHOLE ERROR BUDGET, AND SHOULD BE PRICED AS SUCH.** One seventh of the model by count was 50.9 % of the miss. A cost table that labels a term INTERPOLATED — as §2a honestly did — should carry that term's own uncertainty into the point, not only into the cap. **(3) A SEGMENT MODEL BUILT FROM A SOLVER LOG'S OWN CLOCK PRICES NO CONTAINER BOUNDARY, AND THE BOUNDARY IS NOT NEGLIGIBLE:** 25.40 s = 1.41 % of the wall here, and it is a fixed cost that will dominate the ratio on any arm short enough to matter. **PREDICTIONS: none of §7's cost predictions is scored by this row** — the item's own gate reading is the supervisor's, and this row books cost only. | `cases/dafoam/ladder-a/A2/curriculum_D6RACC2/PREREGISTRATION.md` §2a (seven-segment model), §2b (D5 corroboration), §2d (the 240.0 cap derivation), §7 (the cost table, and its own sentence that *"A calibration row is owed in `docs/COST_CALIBRATION.md` on completion … It will state the ratio against 112.40 and will name whether the miss is contention or modelling"* — that instruction is discharged here), §8 (the rule-2 condition), frozen at `4e4adf66`. Every MEASURED figure above was read by this lane from the run root itself — `/home/ubuntu/certonomous-runs/CURRICULUM-D6RACC2-a2-wing-multipoint/{ledger.txt, CHAIN_DONE, STATUS.chain, STATUS.ACC_mp, ACC_mp_20260830T235332Z_1606819.log, ACC_mp/}` — and **not copied from a relay: the figures reaching this lane by relay were re-verified from the artefact and one framing was corrected** (see the contention paragraph: `siblings_pre`/`siblings_post` are NOT empty). Predecessor and defect of record `C-217` (`D6R-PREREG-DEF-1`); anchor lineage `C-188`, `C-139`, `C-94`; sibling instrument row `C-214`. |
| **C-221** | 2026-08-31 | dafoam | **SO-2a — NACA0012 GEOMETRIC-CONSTRAINT GRADIENT, FIVE ARMS, CHAIN `COMPLETE` AND GRADED `PASS` ON BOTH ROWS. THIS IS THE SOLVE ROW `C-215` SAID WAS OWED AT COMPLETION AND DELIBERATELY DID NOT CLAIM.** Ladder-a A1, `curriculum_SO2a`, arms `MESH`/`X-S`/`G-S`/`X-P`/`G-P`, ranks **1**, 4g cgroup, cpuset **9**, two rows: SHIPPED (`dafoam/opt-packages:latest`, `sha256:9d45679d…`) and PATCHED (`dafoam-idwarp-rot:v1`, `sha256:2927768a…`). **All five arms `rc=0`, none OOM-killed, none capped** [MEASURED, `/home/ubuntu/certonomous-runs/CURRICULUM-SO2a-a1-naca0012-geometric-constraint-gradient/ledger.txt`, five `ARM=` rows each carrying `inspect(exit,oomkilled)=[0 false]`]. Chain closed **2026-08-31T00:25:29Z** with `VERDICT PASS rows={'SHIPPED': 'PASS', 'PATCHED': 'PASS'}` [MEASURED, `SO2a_grade_20260831T002529Z.out`]. **`C-215` IS NOT SUPERSEDED, EDITED OR WITHDRAWN** — it priced instrument time at zero solver compute and said so; this row prices the solve. **NOTE FOR THE READER AND NOT THIS FAMILY'S TO REPAIR: `C-215` IS A DUPLICATE ID — two rows in this table both carry `C-215`** (an SO-2a instrument row and an SO2a-DRIVER-DEF-1 repair row), and separately **`C-102` is absent entirely**, so the table's distinct ids number 218 while its maximum is 219. **This row's id was derived from the MAXIMUM, never from a count** (`CLAUDE.md` rule 11), re-derived in the commit's own shell invocation. **The duplicate is reported upward and is NOT touched here**: the ledger is not this family's alone and an append-only file is not repaired by a lane. | **9.19 core-min REGISTERED point**, band **[5.5, 28.0] REGISTERED**, ceiling **79.0 = the sum of the five per-arm caps, asserted in the comparator's `main()`** [`cases/dafoam/ladder-a/A1/curriculum_SO2a/PREREGISTRATION.md` §4, frozen before compute; §1 (`:51`) records the rule-2 condition — *"This item has burned 0 core-min of solver compute, started no container and created no run directory"*]. Per arm REGISTERED, point then cap: `MESH` **0.19** / 5.0; `X-S` and `X-P` **1.5** each / 12.0 each; `G-S` and `G-P` **3.0** each / 25.0 each. **The point is built entirely from MEASURED core-minutes of SO-1a's arms on this same 4,032-cell mesh at this same rank count** — `MESH` 0.183, `X-S`/`X-P` 1.017/1.200, `F-S`/`F-P` 3.583/3.433, item total 9.416 [§4, read by that lane from `CURRICULUM-SO1a-…/ledger.txt`] — so **BOTH SIDES OF THIS ROW'S RATIO ARE CONTAINER-WALL FIGURES AND NEITHER PRICES ANY QUEUE OR GATE WAIT.** Dollars **$0.00786 point / $0.06754 ceiling, DERIVED, NOT MEASURED** at $0.0513/core-h; `cost_basis` **REPORTED-BY-OWNER, NOT MEASURED** (`COMPUTE_BUDGET_CHARTER.md` §5). | **5.217 core-min MEASURED**, basis **gross**, the sum of the five `ARM=` rows read by this lane from `ledger.txt`: `MESH` **0.167** (10 wall s), `X-S` **0.333** (20 s), `G-S` **2.367** (142 s), `X-P` **0.333** (20 s), `G-P` **2.017** (121 s), all at `ranks=1`. The sum is independently corroborated by the frozen comparator, which computed the same figure from the same rows: `G10_caps … "total_core_min": 5.217, "verdict": "PASS"` [MEASURED, `SO2a_grade_20260831T002529Z.json`, gate `G10_caps`]. **$0.00446 DERIVED, NOT MEASURED**; `cost_basis` **REPORTED-BY-OWNER**. | **5.217 — gross == cleaned.** The longest arm is **142 wall s**, so the charter §2 3,600-s stall rule matches nothing, no row is excluded and there is nothing to subtract. | **0.5677** MEASURED/REGISTERED (`5.217 / 9.19`) against the **9.19 point**; **0.0660x of the 79.0 ceiling; NO OVERRUN.** **THE DENOMINATOR IS STATED BECAUSE THE ITEM'S ONE PIECE OF WASTE IS EXCLUDED FROM BOTH SIDES OF IT (dafoam §18.6 refusal ground 4).** SO-2a's `MESH` arm **waited 540 s** on its aggregate-memory gate before launching [MEASURED, `STATUS.MESH`: `aggregate_waited_s=540`, plus 18 `AGGREGATE_WAIT` lines from `waited=0` at 00:06:12Z to `waited=510` at 00:15:17Z]. **THAT WAIT IS OUTSIDE THE 5.217, AND IT IS OUTSIDE THE 9.19 TOO — checked on both sides, not assumed on either.** Outside the actual: `MESH`'s `ARM=` row reads `wall_s=10` against a container stamped `20260831T001549Z`, i.e. the launcher's core-minute figure is the container's own wall and the gate loop had already exited when the container started. Outside the prediction: the 9.19 is summed from SO-1a's container walls (above), and no term in §4 prices a queue or gate wait. **So 0.5677 is like-for-like, and a reader who assumed the prediction had priced the wait would have drawn the wrong conclusion — which is why it is said here rather than left to be inferred.** | **THE UNDERSPEND IS NOT A MISPREDICTION. IT IS THE CHEAP BRANCH OF A REGISTERED TWO-BRANCH EXPOSURE, AND THE PRE-REGISTRATION NAMED BOTH BRANCHES BEFORE THE RUN.** §4 disclosed that SO-1a's X arm reverse-swept **2** flow-dependent rows while this item sweeps **23** geometric ones, and registered the fork explicitly: if OpenMDAO's relevance reduction recognises that `geometry.thickcon` does not depend on the flow state, each row costs a pyGeo transpose and the arm lands near 1.017–1.200; **if it does not, each row drives a flow adjoint and the arm scales toward ≈ 7.7 core-min.** Prediction **`P10`** was registered to score which branch occurred, and **`P10` HIT** [MEASURED, `SO2a_grade_20260831T002529Z.json`: `P10_X_arm_cheap_no_flow_adjoint_per_constraint_row = "HIT"`]. **The benign branch is worth 2.334 core-min of the 3.973 core-min total underspend — 58.7 % of it — from the two X arms alone**, which came in at **0.333 against 1.5 REGISTERED each, a ratio of 0.222** [MEASURED, ledger; the comparator prints the same per-arm ratios at `G10_caps`]. The other three arms are ordinary conservatism: `MESH` **0.879**, `G-S` **0.789**, `G-P` **0.672**. **AND THE TWO REGISTERED COST PREDICTIONS ARE COUPLED, WHICH IS THIS ROW'S REAL CALIBRATION LESSON: `P10` COULD NOT HIT WITHOUT `P8` MISSING.** **`P8` — total graded core-min inside the band `[5.5, 28.0]` — is a `MISS`** [MEASURED, same grade JSON: `P8_total_core_min_band = "MISS"`], by **0.283 core-min below a lower edge of 5.5.** That is not two independent errors; it is one branch resolving. The band's lower edge was set for the middle course while `P10` was registered precisely to detect the cheap course, so **whenever `P10` HITs benignly, `P8` is arithmetically forced low.** **A band whose lower edge is not CONDITIONED on a registered branch prediction will mis-score itself every time that branch resolves cheaply, and a `MISS` booked that way is not evidence of a bad model** — it is a band that priced the wrong branch. This is offered as the carry-forward for SO-2b and SO-3, which inherit both the fork and the band form. **CONTENTION: MEASURED AND SMALL WHERE IT IS MEASURED AT ALL, AND NOT CLAIMED WHERE IT IS NOT.** `delivered_cores_mean` is **0.9955** on `G-S` (n = 8) and **0.9956** on `G-P` (n = 7); on `X-P` it is **0.7948 on n = 1 sample**, a figure too thin to carry weight and quoted with its n so it cannot be misread; and on `MESH` and `X-S` it reads **`NOT_MEASURED`**, stated as absent rather than composed to anything (L-342 field classes, §3 at `:86`). **All five arms ran with `d6ra2_ACC_mp` live as a sibling** [MEASURED, `siblings_pre`/`siblings_post` in the ledger], on the disjoint cpuset 9 against D6R-ACC2's `2,3,4,14`. **WASTE, NAMED SEPARATELY PER CHARTER §6 AND FOLDED INTO NO RATIO: 0.000 core-min of compute, and 540 wall s of SCHEDULE LATENCY that is NOT waste and is not called waste.** The gate refused because the live aggregate stood at **34.35–35.05 GiB against a 30.6 GiB ceiling** with D6R-ACC2's 20 GiB container and W3's 8 GiB both live [MEASURED, the `AGGREGATE_WAIT` JSON payloads in `STATUS.MESH`]. **A registered resource gate declining to oversubscribe the box is the gate working, not compute being wasted; it burned zero cores and it protected two other items.** The other four arms waited **0 s** [MEASURED, `aggregate_waited_s=0` in `STATUS.X-S`, `STATUS.G-S`, `STATUS.X-P`, `STATUS.G-P`]. **AN INFRASTRUCTURE FACT ABOUT THE NIGHT, REPORTED BECAUSE IT COST TWO ITEMS DIFFERENTLY AND SHOULD NOT BE ABSORBED INTO EITHER'S RATIO: THE BOX WAS OVERSUBSCRIBED AT THREE CONCURRENT ITEMS AND THE TWO MEMORY GATES TRADED.** SO-2a's wait ended at 00:15:49Z, and the last thing its gate saw live was `d12y_w3_S3b_c1_ap` — **W3's last stage ever to launch.** From that moment W3 blocked **20 consecutive stages** on its own registered 14.0 GiB floor (see the W3 row of this table). **SO-2a paid 540 s of latency and lost nothing; W3 lost 61 % of its program. Same box, same hour, two registered gates, two entirely different prices — and neither price is visible in either item's core-minute figure.** | `cases/dafoam/ladder-a/A1/curriculum_SO2a/PREREGISTRATION.md` §4 (the cost table, the SO-1a anchors, the two-branch X-arm exposure and its own sentence that *"A calibration row in `docs/COST_CALIBRATION.md` is owed at completion … and it must state the ratio actual/predicted, attribute the gap, and name contention and waste separately"* — discharged here), §1 at `:51` (rule-2 condition), §4 at `:156` (contention named separately in advance), §6 at `:178`/`:181` (`P8`, `P10`). Every MEASURED figure above was read by this lane from the run root itself — `/home/ubuntu/certonomous-runs/CURRICULUM-SO2a-a1-naca0012-geometric-constraint-gradient/{ledger.txt, STATUS.MESH, STATUS.X-S, STATUS.G-S, STATUS.X-P, STATUS.G-P, MESH_launch.out, SO2a_grade_20260831T002529Z.json, SO2a_grade_20260831T002529Z.out}` — **and not copied from a relay.** Instrument-time row for this item `C-215` (the SO-2a instrument row of the duplicated pair), which stated this row was OWED AT COMPLETION; anchor item `C-205`/`C-206` (SO-1a); concurrent items this row's waste paragraph names, `C-220` (D6R-ACC2) and `C-222` (W3). |
| **C-222** | 2026-08-31 | dafoam | **W3 — CYLINDER UNSTEADY FD-VERIFIED GRADIENT, `curriculum_D12R2`, THE 2026-08-30T23:10:12Z FIRE. GRADED `NOT A RESULT`: the frozen comparator `d12y_grade_w3.py` REFUSED at `G12R-0b` in `--plan` mode and no gate was read. THE ITEM SPENT 63.2332 core-min AND EXECUTED 13 OF THE 33 STAGES IT REGISTERED — 20 stages were BLOCKED on a registered memory floor and NEVER LAUNCHED.** Ranks **1**, 8g cgroup, cpuset **1**, SHIPPED row (`dafoam/opt-packages:latest`, `sha256:9d45679d…`), `W_STEPS=2000` [all MEASURED, `/home/ubuntu/certonomous-runs/CURRICULUM-D12R2W3-cylinder-unsteady/ledger.txt` header]. **THE CENSUS, MEASURED BY THIS LANE FROM THAT LEDGER: 33 `STAGE=` lines; 20 of them match `BLOCKED`; the same 20 match `NOT LAUNCHED`; 13 carry `rc=0` and a core-minute figure.** The blocked twenty are `S3b_c1_am` `S3b_c1_bp` `S3b_c1_bm` `S3b_c2_ap` `S3b_c2_am` `S3b_c2_bp` `S3b_c2_bm` `S3b_c3_ap` `S3b_c3_am` `S3b_c3_bp` `S3b_c3_bm` `S4_n20_r1` `S4_n20_r2` `S4_n40_r1` `S4_n40_r2` `S4_n80_r1` `S4_n80_r2` `S5` `S7_plant` `S7_clean`, each on the line `BLOCKED memavail_GiB=13.5xxx below registered floor 14.0 -- NOT LAUNCHED`. **`MEMAVAIL_FLOOR_GIB = 14.0` is REGISTERED and was not lowered** [`W3_PREREGISTRATION.md` §0 at `:12`; echoed in the run's own ledger header]. **THE STAGES THAT DID NOT RUN INCLUDE `S5`, THE ADJOINT** — the item's whole objective. **THIS ROW FOLLOWS `C-217`'s FORM DELIBERATELY AND FOR THE SAME REASON.** | **563.3 core-min REGISTERED point**, upper **≈ 593**, cap **`CAP_CORE_MIN = 900.0` REGISTERED** (`CAP_S8 = 400.0`), ranks **1** [`cases/dafoam/curriculum_D12R2/W3_PREREGISTRATION.md` §4 (`:67-68`), and re-asserted at §12 (`:131`), §13 (`:243`) and §14 (`:595`), unmoved through four amendments and addenda, each of which asserts it in terms: *"the 563.3 core-min estimate … unchanged"*]. **THE REGISTRATION PRICES PHASES, NOT STAGES, AND THAT FACT DECIDES THIS ROW'S RATIO COLUMN.** Its four terms: **phase 1, all 33 stages together, 196.4 core-min** with band ±15 % `[167, 226]`, from a two-anchor line `cost(W) = F + V·(W/900)` fitted on D12R2's 55.5167 (`C-115`) and W2R's 105.2334 giving `V = 74.575`, `F = 30.658`; **phase 2 (the sweep) 37.6**; phase 3; and **phase 4 (S8, one stage) ≤ 266.7, a BOUND and not a prediction** — §4 says so itself: *"no S8 has ever run in this family, so this is a BOUND, not a prediction from a record."* **There is NO registered price for any individual stage of phase 1.** The per-stage figures §4 does carry — `S3_r1` 1.0667/2.8167, `S2b` 7.08/7.02, `S5` 10.9/26.17 — are **MEASURED anchors from the PREDECESSOR items D12R2 and W2R at `W = 300` and `W = 900`**, imported to fit the phase-level line; they are not a registered price list for W3's stages at `W = 2000`, and eleven of the thirteen stages that ran have no per-stage figure of any kind. Dollars **$0.48162 point / $0.76950 cap, DERIVED, NOT MEASURED** at $0.0513/core-h; `cost_basis` **REPORTED-BY-OWNER, NOT MEASURED** (`COMPUTE_BUDGET_CHARTER.md` §5). | **63.2332 core-min MEASURED**, basis **gross**, from the ledger's own closing line `PHASE1_COMPLETE spent=63.2332 core-min` and **independently re-derived by this lane by summing the 13 executed `STAGE=` rows** — `S0` 0.0833, `S1a` 0.3333, `S1b` 0.6, `S2a` 0.9667, `S2b` 7.4833, `S3_r1` 7.9833, `S3_r2` 8.9667, `S3_r3` 5.8, `S3b_c0_ap` 5.9333, `S3b_c0_am` 6.05, `S3b_c0_bp` 6.0333, `S3b_c0_bm` 6.3333, `S3b_c1_ap` 6.6667 — **which totals 63.2332 exactly.** All 13 carry `rc=0` and `inspect=[0|false]`. **$0.05406 DERIVED, NOT MEASURED**; `cost_basis` **REPORTED-BY-OWNER**. **THE LEDGER'S `PHASE1_COMPLETE` TOKEN IS FALSE OF THIS RUN and the figure beside it is nevertheless correct** — phase 1 registered 33 stages and ran 13; the line is reported verbatim because it is the artefact, and its token is flagged in `DEFECT_NOTE_W3_GRADER_DEF_3.md` §6 as a launcher matter awaiting an id. | **63.2332 — gross == cleaned.** The longest executed stage is `S3_r2` at **538 wall s**, so the charter §2 3,600-s stall rule matches no row and nothing is subtracted. **AND THE `CAP_OVERRUN.txt` IN THE CASE DIRECTORY IS NOT ABOUT THIS RUN**: it reports `W3_chain_r4` at 54,013 s against a 54,000 s cap and is an artefact of a stale `launched/` record for a pid that is dead, already disclosed at `W3_PREREGISTRATION.md` §14, `:601`. **No cap fired on this fire and no row is excluded.** | **NOT COMPUTABLE — AND NO NUMBER IS RECORDED IN THIS COLUMN, IN ANY FORM.** The registered **563.3 prices a program of 33 phase-1 stages plus phases 2, 3 and 4**; the measured **63.2332 bought 13 phase-1 stages and nothing else.** Their quotient is **0.112**, which would read as an **89 % UNDERSPEND — an outstandingly frugal run — while in fact describing a program that never executed 61 % of itself and never reached its own objective.** Arithmetically correct, evidentially false, and corrupting to the very series this column exists to build. **This is `C-217`'s refusal exactly, on the same ground:** there the registered figure priced four arms and the measured bought two. **NO APPROXIMATION, NO BRACKETED FIGURE, NO SMOOTHED OR "INDICATIVE" VARIANT IS RECORDED HERE.** **AND THE PARTIAL RATIO IS ALSO NOT COMPUTABLE, WHICH WAS CHECKED RATHER THAN ASSUMED.** A partial ratio over only the 13 stages that ran would need the registration to **price stages individually**, so that the denominator covers those 13 and no others. **It does not: §4's cost table prices PHASES** (196.4 for all 33 phase-1 stages together, then 37.6, then a ≤ 266.7 bound), and its only per-stage numbers are MEASURED anchors from D12R2 and W2R at other window lengths, covering 2 of the 13 stages that ran. **There is no honest denominator to build, so no partial ratio is given and no number stands in its place.** **THE PHASE-1 RATIO IS REFUSED ON THE SAME GROUND AND IS NAMED SO NOBODY COMPUTES IT LATER: `63.2332 / 196.4 = 0.322` IS THE IDENTICAL DEFECT ONE LEVEL DOWN** — 13 stages of spend over a denominator that prices 33. **`P4` IS THEREFORE UNSCORED AND MUST NOT BE BOOKED A `MISS`.** `P4` predicts phase-1 cost inside `196.4 ± 15 %` = `[167, 226]`, to be read *"from the ledger's `PHASE1_COMPLETE spent=`"* — and that line exists, and reads 63.2332, and is measuring a different program from the one `P4` prices. **The comparator refused before any prediction was scored, so nothing is scored; a `MISS` booked off that line would credit the estimate with an error it did not make.** | **THE GAP IS NOT AN ESTIMATING GAP AND THIS ROW CALIBRATES NOTHING ABOUT THE 563.3. IT IS A RESOURCE BLOCK, AND THE ONE HONEST NUMBER IT PRODUCES IS THE PRICE OF THE BLOCK.** **MECHANISM, MEASURED:** the run's own `AGGREGATE_MEMORY_CAP` lines read `peers=20.0 GiB + mine=8 GiB = 28.0 GiB vs physical 30.6440 GiB`, and every one of the 20 blocked stages recorded `memavail_GiB` between **13.5007 and 13.5239 against the registered floor of 14.0** — **a shortfall of under half a GiB, twenty times in a row.** The `peers=20.0 GiB` is D6R-ACC2's container (row `C-220` of this table, 119.933 core-min, live 23:53:32Z–00:23:31Z). **W3 WAS BLOCKED BY A SIBLING ITEM, ON A BOX THAT WAS OVERSUBSCRIBED AT THREE CONCURRENT ITEMS, BY A MARGIN OF 0.48 GiB.** **CONTENTION: THIS IS CONTENTION IN ITS PUREST FORM AND IT IS NOT FOLDED INTO ANY RATIO — because there is no ratio to fold it into, which is the point of refusing the ratio.** It did not slow this item down; **it stopped it.** No `delivered_cores_mean` is recorded on any W3 stage — the field is absent from this launcher's row format — so **core delivery on the 13 stages that did run is `NOT_MEASURED`, stated as absent and composed to nothing** (L-342 field classes). **WASTE, NAMED SEPARATELY PER CHARTER §6, AND THE NAMING MATTERS MORE HERE THAN ANYWHERE ELSE IN THIS TABLE: the whole 63.2332 core-min BOUGHT NO GRADED GATE.** The honest split rather than a single word: **0 core-min bought a gate reading** — the comparator refused at `G12R-0b` before reaching one; **63.2332 bought thirteen stages of primal and perturbation output that are ON DISK, INTACT AND UNGRADED**, including the full `S2b` 2,400-sample series and three `S3_r*` objective evaluations. **THAT SPEND IS NOT WRITTEN OFF HERE.** Whether it is recoverable by a re-fire that completes the remaining twenty stages against the same staged tree, or must be re-bought, is **the supervisor's call and is not pre-empted by this row** — the `C-178` precedent, where 63.95 core-min came back out of waste on exactly such a ruling, is why it is left open rather than booked. **PRIOR WASTE ON THIS ITEM, SEPARATE FROM THIS ROW AND NOT ADDED TO IT: the 2026-08-28T16:28:48Z fire, supervisor-stopped at 16:33:52Z.** `W3_PREREGISTRATION.md` §14 at `:599` registers it at **5.067 core-min** and books it as waste; **the artefact on disk attests only part of that** — `…_prior_fires/20260828T162848Z_supervisor_stopped/ledger.txt` carries a single completed row, `STAGE=S0 … core_min=0.7833` [MEASURED], the ledger recording only completed stages while the 5.067 counts the killed `S1a`'s wall too. **The discrepancy is reported, not reconciled and not averaged**, and neither figure is added to this row's 63.2332, which is a different fire in a different root. **Two earlier fires spent 0 core-min** and are already on record (`W3-LAUNCHER-DEF-1` at 03:43:17Z, `rc=4 reason=grader_md5_drifted` at 02:15:29Z). **A DEFECT REGISTERED ALONGSIDE THIS ROW, CHANGING NO GATE AND NO VERDICT: `W3-GRADER-DEF-3`.** The `G12R-0b` refusal that produced this `NOT A RESULT` fires on the right condition and **explains itself with the wrong mechanism** — it closes *"A stage that ran and left no row is UNGRADED AND SILENT"* about twenty stages that **did not run**, because its ledger reader (`d12y_grade_w3.py:578-580`, `re.match(r"^STAGE=(\\S+)\\s", line)`) matches a `BLOCKED … NOT LAUNCHED` line as readily as an executed one. **The refusal STANDS; W3 is `NOT A RESULT` with the defect and without it**, and a second limb of the same gate would refuse the same pair on the registered-count check. The cost of the defect is reader time, not compute. Written up at `cases/dafoam/curriculum_D12R2/DEFECT_NOTE_W3_GRADER_DEF_3.md`; **NOT REPAIRED — a repair to a post-compute frozen comparator is the supervisor's call.** **CARRY-FORWARD, two items. (1) A REGISTERED MEMORY FLOOR IS A SILENT PROGRAM-SIZE TERM IN EVERY COST ESTIMATE THAT DOES NOT NAME IT.** The 563.3 prices 33 stages and assumes 33 stages get to run; nothing in §4 carries a term for the probability that a peer holds 20 GiB for half an hour. **An item whose stages are individually gated on a host-wide resource needs its estimate expressed per stage, or its ratio is unreadable the first time the gate fires** — which is also precisely why this row's partial ratio is not computable. **(2) THE `C-217` REFUSAL IS NOW A PRECEDENT WITH TWO INSTANCES AND SHOULD BE READ AS THE FAMILY'S STANDING RULE:** where the registered figure prices a program the measured spend did not execute, the ratio cell carries the words and no number. **Two rows in this table now do it, on two different mechanisms — a chain stopping at a non-zero rc, and stages blocked on a resource floor — which is the evidence that the rule generalises past the case that produced it.** | `cases/dafoam/curriculum_D12R2/W3_PREREGISTRATION.md` §4 (the four-term phase cost table, the two-anchor line, the S8 bound), §0 at `:12` and §14 at `:595` (`MEMAVAIL_FLOOR_GIB = 14.0`, `CAP_CORE_MIN = 900.0`, both unmoved), §6 at `:91` (`P4`), §14 at `:442` (first compute), §14 at `:599` (the 5.067 prior-fire waste and its own sentence that *"The estimate-versus-actual calibration row in `docs/COST_CALIBRATION.md` is OWED AT COMPLETION and is NOT written now"* — owed then, discharged here), §14 at `:601` (`CAP_OVERRUN.txt` disclosed as a stale record). Defect registered with this row: `cases/dafoam/curriculum_D12R2/DEFECT_NOTE_W3_GRADER_DEF_3.md` (`W3-GRADER-DEF-3`). Refusal of record: `cases/dafoam/curriculum_D12R2/W3_plan.out`. Every MEASURED figure above was read by this lane from the run root itself — `/home/ubuntu/certonomous-runs/CURRICULUM-D12R2W3-cylinder-unsteady/ledger.txt` and `…_prior_fires/20260828T162848Z_supervisor_stopped/ledger.txt` — **and not copied from a relay.** Precedent for the refused ratio `C-217`; precedent for a spend recovered rather than written off `C-178`; predecessor anchors `C-115` (D12R2, 55.5167) and W2R's 105.2334; concurrent items `C-220` (D6R-ACC2, whose 20 GiB container is the `peers=20.0 GiB` term) and `C-221` (SO-2a). |
| **C-223** | 2026-08-31 | ansys-verification | **VMFL069-R2 — Two Phase Poiseuille Flow (VM2026R1 printed p.205 = PDF p.219), the complete three-level family L1/L2/L3 = 256 / 1 024 / 4 096 cells, OpenFOAM v2606 `interFoam`, serial `RANKS = 1`, `adjustTimeStep yes` at registered `maxCo = maxAlphaCo = 1.0`, all three levels run to `endTime = 1000 s`.** Launched 2026-08-30T23:51:23Z, **finished UNATTENDED 2026-08-31T09:00:42Z** after a session limit killed the fleet at ~00:55Z; graded 2026-08-31T14:53:28Z, **5 h 53 m later**, through the frozen comparator `8e0b4c3f…` at `PREREGISTRATION` §12's exact invocation, exit 0. **Rung verdict `PASS`** — register row **#46**; the three qualifications (limb C's GCI at 145.9103 % of the value it qualifies with a negative Richardson extrapolation, limb B's deviation-plus-GCI at 1.021639 % against a 1.000000 % band, and the planted-zero control exercised at L1 only so the readers producing the gated L3 numbers were never shown able to see a non-zero on L3's own bytes) are carried on that row's face, not here. **PREDICTED 822 core-min** (`PREREGISTRATION` §A1.1 point estimate, bracketed **76–890** with 890 the conservative end; the superseded sec.8 drafting figure was ~2 160). **ACTUAL 549.2501 core-min MEASURED** (`COST.txt` `total_core_min` = Σ `RUN_RC.<level>` `core_min`; L1 **10.7667** at 646 wall s, L2 **62.6167** at 3 757 s, L3 **475.8667** at 28 552 s). **RATIO actual/predicted = 0.6682** — the run cost **two-thirds** of its point estimate, the registered bracket **CONTAINED** the answer, and the point estimate was **49.66 % high**. **Per level: 0.9528 / 0.6950 / 0.6599.** **ATTRIBUTION — one cause carries essentially the whole 272.75 core-min gap, and the pre-registration NAMED IT IN WRITING BEFORE COMPUTE.** §A1.1 registered that L2/L3 per-step costs were **not measured**, that linear-in-cells scaling was an **assumption**, and that because "an appreciable share of the L1 step is fixed per-step overhead … linear extrapolation OVER-estimates L3". **CONFIRMED BY MEASUREMENT:** per-cell-per-step cost **falls** with refinement — **29.770 → 21.532 → 20.392 µs** — which is only possible if a fixed per-step term dominates the small case; a two-term fit `per-step = a + b·cells` on L1 and L3 gives **a = 2.5606 ms fixed per step, b = 0.019767 ms per cell**, the fixed term being **33.60 % of L1's step and only 3.07 % of L3's**, and that fit — built from L1 and L3 alone — predicts L2 at 22.8023 ms/step against a measured 22.0488, **3.42 % high**, so the mechanism is real and not a curve fitted to two points. **Linear-in-cells from L1's measured per-step cost over-predicts L2 by 38.26 % and L3 by 45.98 %; scaling misprediction therefore accounts for 272.71 of the 272.75 core-min gap (L2 27.48 + L3 245.23).** **SECOND, MUCH SMALLER CAUSE — the L1 anchor, two errors of similar size in opposite directions (0.53 core-min):** the probe's **wall** figure of 7.97 ms/step over-predicted the realised 7.6211 by **4.38 %**, while its **CPU** figure of 7.28 ms/step under-predicted the realised 7.5987 by **4.38 %**. **THIRD — step-count extrapolation, ≈ 0 error:** probe-anchored **84 817 / 169 634 / 339 268** against measured **84 765 / 170 395 / 341 828**; L1 landed within **52 steps (+0.061 %)** and inside §A1.4's honest 82 182–91 825 bracket, while sec.8's formula figure of ≈ 88 900 was **+4.878 % high**. **A REGISTERED ASSUMPTION IS FALSIFIED AT THE MARGIN AND IS RECORDED BECAUSE IT WAS REGISTERED:** §A1.1 assumed steps double **exactly** per level; measured ratios are **L2/L1 = 2.01020** and **L3/L2 = 2.00609**, contributing ≈ **+0.75 %** to L3 — a rounding error against the 46 % scaling misprediction, but not silently dropped. **CONTENTION, NAMED SEPARATELY AND NEVER ABSORBED INTO THE RATIO (`COMPUTE_BUDGET_CHARTER` §6), IS EFFECTIVELY NIL — AND THIS CORRECTS THE REGISTERED EXPECTATION.** `CONTENTION.txt` sampled at launch reads `loadavg 7.56 8.54 13.02` on `nproc 16` with **6 `simpleFoam` processes live**, which predicts inflated wall time; the logs measure the truth directly, final `ExecutionTime` (CPU) against final `ClockTime` (wall) giving **wall/CPU = 1.00295 (L1) / 1.00060 (L2) / 1.00008 (L3)** — at most **+0.295 %**, on the shortest level, falling to **+0.008 %** on the level that dominates the bill. Contrast the pre-freeze probe at `loadavg 36.88` with wall/CPU **1.0948**: the amendment deliberately carried that **9.5 %** inflation into its projection as conservatism, so it is an **identified component of the L1 over-estimate** rather than an unexplained residual. **THE RULE-12 STALL HEURISTIC IS CLEARED BY MEASUREMENT, NOT BY ASSERTION:** L2 (3 757 wall s) and L3 (28 552 wall s) both exceed the 3 600 wall-s stall flag; neither is a stall, because a stalled process shows wall ≫ CPU and these show **1.00060** and **1.00008** — the processes were computing for essentially every second they were alive. **WASTE: 0 core-min, NAMED SEPARATELY AND NEVER ABSORBED INTO THE RATIO.** Nothing was computed and discarded: three levels launched, three completed, `rc = 0` at each (captured **inside** the detached subshell), no restart, no re-mesh, no abandoned partial; `overall_rc = 0`, `smoke_mode = no`. **Two adjacent items named because they are real and neither is R2 solver spend:** (1) the **pre-freeze scoping probe** (L1, 2 005 steps, scratch) that bought §A1.1's measured cost basis — **its cost is NOT MEASURABLE: the scratch directory is gone**, looked for and not on disk, recorded as **NOT MEASURED** rather than estimated, which is L-186 behaving exactly as the lesson warns, and no repository document depended on that path; (2) **5 h 53 m of grading latency, which is NOT compute waste** — the solver finished **5 h 34 m before the 14:35Z reboot**, was **not killed**, and every field, log and `RUN_RC` survived intact, so no compute was lost; it is a scheduling finding and is folded into no ratio. **CAP: 5 000 core-min running total, 4 450.7499 unspent, 10.985 % used, 9.10× margin; the cap never came near firing and `endTime` was never reduced to fit it** (`PREREGISTRATION` §A1.2 records at length why shortening `endTime` would have moved the plateau sample into the transient and cost the entire verdict — the plateau clause samples at `endTime/2`, so at `endTime = 600` the sample would land **138×** outside the frozen 1e-6 tolerance and the comparator would have refused after the full L3 spend). **cost_basis: $0.0513/core-h, c7a.4xlarge, REPORTED-BY-OWNER (Sanaa 2026-08-21/22) — the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER` §5), so dollars are DERIVED, not measured: actual $0.46961, estimate $0.70281, cap $4.27500. CPU only, no GPU.** **EVIDENCE SPLIT, STATED SO NO AUDITOR HAS TO INFER IT:** the cost figures above cite `COST.txt` and `RUN_RC.L{1,2,3}`, which are **committed**; the contention and step-count figures were streamed from the three `log.interFoam` files (**236 684 554 / 476 354 970 / 956 180 768 bytes**), which are **on disk only and deliberately not in git** under Sanaa's 2026-08-27 §1 directive, at `/home/ubuntu/Certonomous/verification/runs/ansys_verification/VMFL069-R2/L{1,2,3}/log.interFoam`. **THE LESSON THIS ROW BUYS FOR FUTURE `interFoam` ESTIMATES ON THIS BOX: at these cell counts, per-step cost is `2.56 ms + 0.0198 ms/cell`, so a cost model anchored on a small level and scaled linearly in cells over-predicts by ~38 % at 4× and ~46 % at 16× — and the honest response is the one §A1.1 already took, which is to state the linear figure as a point estimate, name the linear assumption as unmeasured, and register a bracket whose floor assumes wholly fixed per-step cost. That bracket contained the answer; the point estimate did not come close.** | **549.2501 core-min MEASURED** | **$0.46961 DERIVED** |
| **C-224** | 2026-08-31 | dafoam | **CORRECTION ROW, NAMING THE ROW IT CORRECTS: `C-222` CARRIES A LITERAL VERTICAL BAR INSIDE A CELL AND THEREFORE RENDERS WITH ELEVEN CELLS AGAINST THIS TABLE'S TEN-COLUMN HEADER. NO FIGURE IN IT IS WRONG; EVERY COLUMN AFTER THE SIXTH IS DISPLACED BY ONE, AND THE TENTH IS DROPPED.** The defect is in `C-222`'s **actual gross** cell, where the launcher's `inspect` field is quoted verbatim from the run ledger — its value is the container exit code and the OOM flag **separated by a vertical bar** — and the bar was **not escaped**. Markdown splits a table row on every unescaped bar, so that one cell becomes two. **THE ROW IS NOT EDITED** — this file's append rule 1 is unconditional (*"An existing row is never edited; a correction is a new row naming the row it corrects"*), it has no freshness exemption, and a lane does not widen a rule on its own judgment merely because the row is fifteen minutes old and the defect is typographical. **This is the `C-180` form: the standing row for the process remains `C-222`, and this row corrects one thing about it.** | **not a new prediction** — `C-222`'s stands: **563.3 core-min REGISTERED point**, cap **`CAP_CORE_MIN = 900.0` REGISTERED**, `ranks 1`, unchanged. | **not a new actual** — `C-222`'s stands: **63.2332 core-min MEASURED**, basis gross, summed independently by that row from the 13 executed `STAGE=` rows of `/home/ubuntu/certonomous-runs/CURRICULUM-D12R2W3-cylinder-unsteady/ledger.txt`. **ZERO additional core-minutes were spent** between `C-222` and this row: no container, no solver, and nothing read but two committed blobs. | **not a new figure** — 63.2332, gross == cleaned, as `C-222` states it. | **NOT COMPUTABLE, AND THIS ROW EXISTS SO THAT SENTENCE SURVIVES THE RENDERER.** In a ten-column render of `C-222` this text appears under **gap attribution**, one column right of where it belongs, so it is restated here in its own ratio cell: **the registered 563.3 prices 33 phase-1 stages plus phases 2, 3 and 4; the measured 63.2332 bought 13 phase-1 stages and nothing else. `63.2332 / 563.3 = 0.112` would read as an 89 % UNDERSPEND while describing a program that never executed 61 % of itself and never reached its adjoint. NO APPROXIMATION, NO BRACKETED FIGURE AND NO SMOOTHED VARIANT IS RECORDED. THE PARTIAL RATIO OVER THE 13 STAGES THAT RAN IS ALSO NOT COMPUTABLE — checked, not assumed: the registration prices PHASES, not stages, so no honest denominator exists. THE PHASE-1 RATIO `63.2332 / 196.4 = 0.322` IS THE IDENTICAL DEFECT ONE LEVEL DOWN AND IS REFUSED TOO. `P4` IS UNSCORED AND MUST NOT BE BOOKED A `MISS`.** | **WHAT IS AND IS NOT WRONG WITH `C-222`, STATED PRECISELY SO NOBODY RE-VERIFIES MORE THAN THEY NEED TO.** **Nothing in its content is withdrawn.** Every figure, tag, artefact path and refusal in it stands; the row is unambiguous in the raw file, where the cell boundaries are plain, and this defect is **entirely a rendered-table defect.** **WHAT A READER OF THE RENDERED TABLE LOSES, ENUMERATED:** the *actual gross* column truncates mid-sentence inside the quoted `inspect` value; the remainder of that cell appears under *actual cleaned*; the real cleaned cell appears under *ratio*; the ratio refusal quoted above appears under *gap attribution*; the gap attribution appears under *record ref*; and **the record-ref cell — the whole provenance column — is the eleventh cell and is DROPPED by a ten-column renderer.** That last consequence is why this correction is not cosmetic: **a row whose provenance column does not render is a number without a visible artefact, which is the one thing `DAFOAM_CHARTER.md` §18.6 exists to prevent.** **THE PROVENANCE, RESTORED VERBATIM SO IT IS VISIBLE SOMEWHERE:** `cases/dafoam/curriculum_D12R2/W3_PREREGISTRATION.md` §4 (the four-term phase cost table, the two-anchor line, the S8 bound), §0 at `:12` and §14 at `:595` (`MEMAVAIL_FLOOR_GIB = 14.0` and `CAP_CORE_MIN = 900.0`, both unmoved), §6 at `:91` (`P4`), §14 at `:442` (first compute), §14 at `:599` (the 5.067 core-min prior-fire waste), §14 at `:601` (`CAP_OVERRUN.txt` disclosed as a stale record); defect registered with that row `cases/dafoam/curriculum_D12R2/DEFECT_NOTE_W3_GRADER_DEF_3.md` (`W3-GRADER-DEF-3`); refusal of record `cases/dafoam/curriculum_D12R2/W3_plan.out`; every MEASURED figure read by that lane from `/home/ubuntu/certonomous-runs/CURRICULUM-D12R2W3-cylinder-unsteady/ledger.txt` and from `…_prior_fires/20260828T162848Z_supervisor_stopped/ledger.txt`; precedents `C-217` (the refused ratio) and `C-178` (a spend recovered rather than written off); predecessor anchors `C-115` and W2R's 105.2334. **WASTE: 0.000 core-min** — this row is bookkeeping and burned no compute. **THE MECHANISM, NAMED SO IT IS CHECKABLE, BECAUSE IT WILL RECUR: A CELL THAT QUOTES A LAUNCHER FIELD VERBATIM IS EXACTLY WHERE A BAR ENTERS THIS TABLE.** The `inspect` value's syntax is the launcher's, and quoting the artefact verbatim is what every row here is *supposed* to do — **so the safe habit and the defect share a root.** Backticks do **not** protect a bar in a Markdown table; only a backslash-escaped bar does. **AND THE FIRST DRAFT OF THIS VERY ROW REPEATED THE DEFECT — measured, not confessed for effect: a pre-flight arity check on the draft returned SIXTEEN cells**, from the quoted `inspect` value again plus two field-separator flags in the shell commands cited below. **The draft was rewritten to contain no literal bar at all rather than to escape them one by one**, because an escaped bar still splits an arity checker that is not itself escape-aware, and a check you cannot trust is worse than none. **THE CHEAP CHECK THAT WOULD HAVE CAUGHT THE ORIGINAL, AND WHICH THIS LANE RAN ONLY AFTER COMMITTING IT: COUNT THE CELLS OF AN APPENDED ROW AGAINST THE HEADER'S TEN, SPLITTING ON UNESCAPED BARS ONLY.** It is mechanical and it costs one command. **Nothing in `scripts/append_record.py` performs it** — that tool asserts the id, the prefix and the merge, and **is blind to column arity by construction.** **AND THE SWEEP THAT CHECK WOULD BE, RUN ONCE OVER THE WHOLE LEDGER AT HEAD, SAYS THIS IS NOT ONE LANE'S LAPSE BUT A LEDGER-WIDE, CROSS-TEAM CONDITION: 23 OF THE 226 ID-BEARING ROWS DO NOT CARRY TEN CELLS.** MEASURED at HEAD by splitting on unescaped bars: `C-52`, `C-58`, `C-69`, `C-70`, `C-84`, `C-98`, `C-105`, `C-117`, `C-121`, `C-134`, `C-135`, `C-137`, `C-143`, `C-174`, `C-175`, `C-176`, `C-177`, `C-181`, `C-204`, `C-216` (the cfd one), `C-218` (the cfd one), `C-222`, `C-223` — spanning **all five teams that write here — ansys-verification 8, cfd 6, dafoam 5, heat-transfer 3, verification 1** — and running back to `C-52`. **THEY ARE TWO DIFFERENT DEFECTS AND SHOULD NOT BE CONFLATED.** The rows over ten cells (`C-58`, `C-69`, `C-70`, `C-84`, `C-98`, `C-105`, `C-121`, `C-134`, `C-137`, `C-143`, `C-222`) carry a stray bar and **displace** their columns; the rows under ten (`C-52`, `C-117`, `C-135`, `C-174`, `C-175`, `C-176`, `C-177`, `C-181`, `C-204`, cfd's `C-216` at five cells, cfd's `C-218` at five, `C-223` at six) are **short of columns outright** — for instance `C-176` has no record-ref cell at all, which is a missing provenance column rather than a displaced one. **NONE OF THE OTHER 22 IS TOUCHED, CORRECTED OR RENUMBERED HERE.** This ledger is written by every team, correcting another team's row is not this lane's call, and an append-only file is not repaired by a lane; the sweep is **reported upward with its command and its output**, exactly as the duplicate-id condition below is. Offered in `§18.4`'s voice as a buildable check that does not exist: *a clause nobody can fail is a preference.* **A SECOND LEDGER-WIDE CONDITION FOUND BY THE SAME SWEEP AND ALSO NOT REPAIRED HERE: FOUR IDS ARE DUPLICATED, NOT ONE.** `C-215`, `C-216`, `C-217` and `C-218` each name **two** rows — in every case a dafoam row and, below it, a cfd or closure row filed within hours of it on 2026-08-31 — and `C-102` is absent entirely. That is why 226 id-bearing rows at HEAD resolve to 222 distinct ids, and why before this evening's appends the figures were **218 distinct against a maximum of 219**: 219 minus the absent `C-102` is 218, and 218 plus the four duplicates is the 222 rows then standing. **THE ARITHMETIC IN CIRCULATION WAS RIGHT AND ITS EXPLANATION WAS INCOMPLETE** — the 218-versus-219 figures hold exactly, but they were attributed to a single duplicated `C-215`. **The ids landed by this lane were derived from the MAXIMUM at the commit's own parent** (rule 11), asserted by `append_record.py` against that blob, and are unique. | corrects `C-224` → `C-222`; the standing row for the W3 2026-08-30T23:10:12Z fire remains `C-222`, whose content is unchanged and unwithdrawn. Both rows landed 2026-08-31 by the same lane under the rule-10 private-index protocol; `C-222` at commit `7b6d4a1b`. Column arity MEASURED at HEAD by splitting each row on unescaped bars: header **10**, `C-220` **10**, `C-221` **10**, `C-222` **11**, this row **10**. |
| C-225 | 2026-08-31 | cfd | **R1_F3S_SELECTOR_REPAIR — `PASS`, all eight gates `G-R1-1`…`G-R1-8` met.** Instrument repair, not physics: the `select_one` unique-selector fix in both shipped modules `grade_f3s.py` and `run_f3s.py`, verdict decided by `cfd-supervisor` personally against `verification/runs/F3_runs/successor_selector_2026-08-28/R1_CONTROL_RECORD.json`, prereg frozen at `f7da1a24`, spec `docs/standards/UNIQUE_SELECTOR_RULE.md` at `ae2c8c49`. Full record `verification/campaign/R1_F3S_SELECTOR_REPAIR_RESULTS.md`. **The verdict was NOT relayed from the driver's summary:** the control record stores each control as a 60-character truncated line, so `G-R1-3`'s required substrings (`UNIQUE-SELECTOR`, `matched   : 2`, both basenames) and `G-R1-5`'s `matched   : 0` **are not in the record at all** — they were established by reading `selector_controls.py` as code (check 1), where `_expect_refusal` raises on any missing substring, on any exit code but 2, and — the clause that matters — on a selector that **returns instead of refusing**. `G-R1-8`'s set was re-derived on disk rather than believed: `find … successor_triple_2026-08-26/ -type f` gives **8542**, plus the three named singles = **8545**, matching `n_hashed` exactly, 0 changed. Three clauses of the strict completion rule (last time == `endTime`, `ExecutionTime` count, age guard) **have NO REFERENT** here — `postProcess -latestTime` is not time-marching — and are recorded as having none rather than marked satisfied. | **Predicted 0.200 core-min; ACTUAL 0.132375 core-min** (7.9425 wall s × 1 rank ÷ 60, measured by the driver and captured in `cost.wall_s`). **Ratio actual/predicted = 0.662.** Registered cap **0.300** core-min, **not breached** (44% consumed); no `CAP_OVERRUN.txt` or `ESTIMATE_OVERRUN.txt` exists anywhere in the run tree. **Attribution: MISPREDICTION, on the conservative side, and no waste** — the cost basis carried a 5.0 s allowance for two scratch case copies and a 3.0 s allowance for ~12 python invocations, and both came in under; there was no contention (the box ran nothing else) and every artifact the record cites is on disk and re-readable, so nothing was re-spent. **Dollars $0.000113 — DERIVED, NOT MEASURED**, at the owner-stated $0.0513/core-h for c7a.4xlarge; the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` §5) and `cost_basis` in the record says reported-by-owner. |
| C-226 | 2026-08-31 | cfd | **F23b_HP_WEDGE — `BLOCKED` at phase A0, and this row exists because rule 12 owes a calibration at EVERY process completion, including the ones that end badly.** Launched 2026-08-31T00:23:32Z, 145 wall s, launcher **rc 1** (two independent inside-the-wrapper captures; `simpleFoam` itself exited 0), prereg frozen `57d31dde`. The registered §A1.5 refusal fired on `\|1 − Ubar\| = 9.692465e-04` against a frozen `1.0e-10`. **Cause is NOT an underperforming solve:** `\|1 − Ubar\|` is a discretisation error gated at an iterative tolerance, and inverting §12's own predicted `f·Re` through the case's own `f·Re = 64/Ubar` shows the registration predicting **1,215,525×** its own threshold at the very level §5.5 gates. Unsatisfiable at every level. Full record `verification/campaign/F23b_HP_WEDGE_RESULTS.md`; **L-409**; **D584**. | **PREDICTED for item A0: 0.350 core-min** (build 0.050 + solve 0.294 at §9.1's measured coarse rate, no `S` factor). **ACTUAL charged solve: 1.86667 core-min** (28.0 wall s × 4 ranks ÷ 60, from the launcher's own EXIT-trap record `RUN_STATUS.F23b_HP_WEDGE.txt`). **RATIO actual/predicted = 6.35× on the solve line, 5.33× on the whole A0 item.** **ATTRIBUTION: MISPREDICTION, single cause, identified — §9.1's coarse rate was measured on a 131,072-cell case and DOES NOT TRANSFER to A0's 1,024-cell case**, where fixed per-iteration overhead dominates and the per-cell rate is meaningless; §A1.6 assumed transferability and that assumption is what failed. **NOT contention** (the box ran nothing else) and **NOT waste in the misprediction sense**. **WASTE, NAMED SEPARATELY AND NEVER ABSORBED INTO THE RATIO ABOVE: the entire ≈3.82 core-min gross spend is waste**, because the rung produced **no retained artifact** — `run_f23b.sh:87` wrote the pre-ladder to `${TMPDIR:-/tmp}/f23b_preladder_$$` and copied nothing back, and the 14:35Z reboot destroyed `log.simpleFoam`, `log.build`, `log.decomposePar`, `RC.txt` and all four `processor*/` trees, so the deciding number is **uncitable** and would have to be re-spent in full. **SECOND BOOKKEEPING DEFECT (L-342 — infrastructure defects void a cost claim, never physics): the launcher charges ONLY the `mpirun`.** The build and `decomposePar` are uncharged although A0's registered 0.350 explicitly budgets 0.050 for the build, so **≈1.95 core-min of real spend (DERIVED, not measured: 145 total wall s minus the 28 s mpirun at an assumed 1 rank) sits OUTSIDE every running total in this rung.** Gross ≈ **3.82 core-min**. **No cap breached** — pre-ladder cap 20.0 was 9.3% consumed, ladder cap 293.0 untouched. **Dollars: $0.00160 charged, ≈$0.00327 gross — DERIVED, NOT MEASURED** at the owner-stated $0.0513/core-h for c7a.4xlarge; the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` §5). **Forward action for the successor's cost model: charge the build and `decomposePar`, and do not reuse a rate measured at 131k cells for a 1k-cell case.** |
| C-227 | 2026-08-31 | cfd | **JF1_L1_UNBLOWN_A0 — feasibility, NO GATE, and the row exists because rule 12 owes a calibration at every process completion including the ones that score nothing.** Unblown NACA 0012, alpha 0, `Re_c` 1e6, `simpleFoam`/`kOmegaSST` low-Re on a 39,984-cell O-mesh, 1 rank, launched by the queue runner 2026-08-31T15:40:37Z pid 67215, finished 16:04:23Z, launcher rc 0, 8000 iterations, `End` written, last time == `endTime`. Figures read from `verification/runs/JF1_jet_flap/JF1_L1_UNBLOWN_A0/RUN_STATUS.JF1_L1_UNBLOWN_A0.txt` and `RESULTS_FEASIBILITY.md`, not from memory. | **12.71 core-min** (queue entry `verification/queue/cfd/launched/JF1_L1_UNBLOWN_A0.json`, `cost_basis`: 4.2378e-03 core-min/iteration × 3000 iterations) | **23.7667 core-min MEASURED** from the launcher's own inside-the-wrapper capture (wall 1426 s × 1 rank ÷ 60) | **1.870** | gross 25.75 core-min | cleaned 23.77 core-min (no row exceeds the 3600 s stall rule; the two extra items are named separately as waste and as an instrument, never folded into the ratio) | **THE RATIO DECOMPOSES CLEANLY AND THE TWO FACTORS PULL OPPOSITE WAYS, WHICH A SINGLE RATIO HIDES.** (i) The per-iteration RATE was over-predicted by 1.427×: predicted 4.2378e-03 core-min/iter, measured 1426/8000/60 = 2.9708e-03. The prediction came from a pilot run under load average 22.81 on 16 cores; the real run had the box at ~5, so the pilot's contention was not representative and the estimate was conservative in the right direction. (ii) The ITERATION COUNT was under-predicted by 2.667×: predicted 3000, actual 8000. 3000 was read off the pilot's residual trajectory on the assumption that the frozen 1e-6 all-channel criterion would be met. **IT NEVER WAS AND COULD NOT BE**: `p` and `k` plateau flat at ~4e-06 from iteration 2000 to 8000 while `Ux` sits at 2.3e-08, so the run went to the controlDict cap instead of converging out. 2.667 ÷ 1.427 = 1.869. THE LESSON FOR THE NEXT ESTIMATE: an iteration count inferred from a residual trajectory is a prediction that the criterion is REACHABLE, and on a configuration whose residuals plateau that prediction is not conservative — it is simply wrong. Estimate against the controlDict cap, not against hoped-for convergence, until a configuration has been shown to converge out once. | **WASTE, SEPARATELY NAMED AND NOT IN THE RATIO (charter §6): 0.0167 core-min**, attempt 1 of this same case, 2026-08-31T15:38:32Z, launcher rc 1 at 1 wall second — the wrapper ran under `set -u` and a non-interactive shell EXITS on the unset expansions inside the OpenFOAM `bashrc` it sources. Retained at `verification/runs/JF1_jet_flap/JF1_L1_UNBLOWN_A0.attempt1_FAILED_2026-08-31T1538Z`. **SEPARATELY AGAIN, AND NOT WASTE: 1.9833 core-min** for the cost-calibration pilot (`_pilot_2026-08-31T1535Z`, ClockTime 119 s × 1 rank), which is the instrument that produced the rate in the estimate column and is therefore an input to this row, not an overrun against it. | DOLLARS **DERIVED, NOT MEASURED**, at the owner-stated $0.0513/core-h, reported-by-owner — the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` §5): **$0.0203** actual, $0.0109 estimated, $0.0220 gross, $0.0385 at the 45.0 core-min registered cap. Under cap; the cap was not raised and never is (rule 12). |
| C-20260831T163653.411934Z-257629d0 | 2026-08-31 | dafoam | **THE REPLACEMENT ROW THE STRUCK `C-215` AT `:300` SAYS IS OWED — LANDED THROUGH `scripts/append_record.py`, WHICH IS WHY THIS ROW'S ID IS A TOOL-ALLOCATED TIMESTAMP+HASH AND NOT A NUMBER ANYONE CHOSE.** The struck row reserves no id and names none (*"NO ID IS RESERVED FOR THE REPLACEMENT AND NONE IS NAMED HERE"*, §23.1 rules pre-assignment dead); the two sequential ids this family previously considered are both now taken by landed rows — **`C-224` by dafoam** at `:311` and **`C-225` by cfd** at `:312`, the latter being cfd's re-issue of its own struck `was C-216` row — so a hand-chosen number would have collided a third time. **The tool's own audit now clears where it refused at exit 7: measured exit 0, VERDICT OK**, the two strikes at `:304`/`:306` having been restruck by cfd into a form the id pattern parses. The struck row's data cells are carried BYTE-FOR-BYTE into this one (columns 5–10 below are `cmp`-identical to `:300`'s); no figure is restated, recomputed or rounded, and this row disputes nothing in it. **The original process, unchanged:** **SO-2a instrument repair — SO2a-DRIVER-DEF-1.** `so2a_chain_driver.sh:143` called `$HERE/so2a_aggregate_memory.py`, which **did not exist on disk and had never existed in any commit on any branch**. `AGG` was the empty string every poll, the `json.load` raised, the `ok` test could never be true, and the driver was **guaranteed** to poll to the 14400 s bound and write `chain=BLOCKED_AGGREGATE` at the FIRST arm. The queue runner launched it 2026-08-30T23:13:26Z; the `dafoam-supervisor` stopped it personally at **23:18:49Z**. **THIS ROW IS NOT THE SO-2a ITEM.** SO-2a has bought nothing and its label remains `PENDING`; the process completed here is the **repair**: adopt the absent script byte-for-byte from SO-1a, prove the gate can refuse in both directions on the driver's own bytes, amend §7's instrument table by dated addendum, and sweep the family for the same shape | **NONE REGISTERED — stated as absent rather than approximated (append rule 2).** No pre-registration prices an instrument repair, so this row has **no predicted figure and therefore no ratio**. The item's own registered **9.19 core-min at ranks 1, cap 79.0** is *not* the predicted column here and is **not** compared: comparing an item estimate against a repair's instrument time would be a category error and would corrupt the ledger's ratio series | **≥ 0.462 core-min at ranks 1**, and the inequality is deliberate. Timed drives: refusal selftest **19.83 wall s** (`cpu 1.23 u + 0.77 s`), sibling sweep on the live tree **0.31 wall s**, sweep on the known-positive exported tree **1.89 wall s**, and the standalone L4 diagnostic **5.708 wall s** → 27.74 wall s = 0.462 core-min. **One drive is NOT TIMED and is therefore not in the figure**: the first, failing run of the refusal selftest, the one that exposed the harness's own `PERMISSION` defect. It is named rather than estimated | **0.367 core-min** = the three drives that produced the committed evidence (19.83 + 0.31 + 1.89 = 22.03 wall s at ranks 1). **No invocation is within two orders of magnitude of the 3600 wall-s stall rule** — the longest is 19.83 wall s. **DERIVED, NOT MEASURED: $0.00031** at $0.0513/core-h, c7a.4xlarge, reported-by-owner (`COMPUTE_BUDGET_CHARTER.md` §5 — the box cannot read its own billing) | **NO RATIO. Nothing was predicted, so nothing is divided.** A ratio computed against a figure this row does not carry would be the ledger's own version of the defect it records | **WASTE, NAMED SEPARATELY AND NEVER FOLDED INTO ANY RATIO (charter §6): the stopped launch. SOLVER COMPUTE = ZERO core-min** — no container was ever created, in any state, so no solver process existed to spend anything. What it did consume is **323 wall s** (23:13:26Z → 23:18:49Z) of a driver that was sleeping 30 s between nine polls, and an **8 GiB memory reservation** it would have held for **four hours** had it not been stopped. **The honest statement is near-zero compute waste with the wall time and the reservation named** — and the thing actually saved is the item's registered 9.19 core-min, which would have been spent to produce `BLOCKED_AGGREGATE` and no verdict. **GAP ATTRIBUTION FOR THE DEFECT ITSELF, which is the calibration lesson: this was not misprediction, not contention and not solver behaviour — it was a COPY-FORWARD THAT TOOK THE DRIVER AND NOT ITS DEPENDENCY, invisible to a freeze whose instrument table enumerates INSTRUMENTS BUT NOT THEIR DEPENDENCIES.** §7's md5-agreement control ran honestly and read *eight of eight AGREE* while the ninth file was absent from the box. **AN INSTRUMENT TABLE CAN BE COMPLETE AND WRONG AT THE SAME TIME.** **CARRY-FORWARD, with a call site so it is applied rather than remembered (rule 14): `scripts/sweep_dafoam_script_references.py` now walks every `.sh` under `cases/dafoam` with `os.walk` — never a recursive grep, because ugrep honours ignore files and races on multi-file output — and resolves every `$HERE/`, `$VAR/` and bare-invocation reference against the filesystem. Live tree: 83 files, 437 references, 417 PRESENT, 0 ABSENT, 20 RUNTIME (each traced to a staging `cp` and an existing source). Validated against a KNOWN POSITIVE FIRST — pointed at the tree exported from `HEAD` before this repair it reports the absent script ABSENT — so its zero is a claim about the family and not about the reader.** A second, smaller carry-forward: the sweep's first version reported a FALSE ABSENT (`curriculum_D18R_P7`'s `./d18_grade.py`, which is present at `$D18/`, reached via a preceding `cd "$D18"`); the resolver was fixed and the known positive re-driven, because **a false ABSENT is the noise a real ABSENT hides in** | `cases/dafoam/ladder-a/A1/curriculum_SO2a/SUPERVISOR_STOP.txt`; `PREREGISTRATION.md` **ADDENDUM A** (version 1.1, appended via `scripts/append_block.py`, landed == source byte-for-byte, provenance `sha256:d88fb453…`) with its prefix proof `so2a_addendum_prefix_proof.txt` (`lines whose number changed above this section: 0`, PROVED by byte comparison, with a one-byte mutation shown making the same comparator report 1); refusal evidence `so2a_aggregate_memory_REFUSAL_EVIDENCE.txt` (**15 of 15 checks as registered, rc 0**: known positive, MUST-WAIT at aggregate 4010.12 GiB vs ceiling 30.6, MUST-PROCEED at 14.14 vs 30.6, and `BLOCKED_AGGREGATE` shown REACHABLE); zero delta `so2a_aggregate_memory_DELTAS_from_so1a.diff`; sweep `scripts/sweep_dafoam_script_references.py`; re-filed queue entry `verification/queue/dafoam/SO2a_chain.json` (validator **ACCEPTED** on the explicit path); all landed with this row's commit |
| C-20260831T164216.179599Z-19ea8c16 | 2026-08-31 | dafoam | **D4S-F3SR (A2, `curriculum_D4_SHIPPED_F3SR`) — the two-arm endpoint FD table re-run at a repaired cap frame. ITEM VERDICT `GATE FAIL`, and the failing gate is a DELIVERY gate, not the physics.** 19 of 20 gates `PASS`; the one non-`PASS` is `G12_cpu_placement_F-P`, whose `delivered_cores_mean` limb measures what the kernel actually gave the container — the registered cpuset, the affinity mask and one-rank-per-core all hold, and `G12_cpu_placement_F-S` passed the identical gate on the identical cpuset minutes earlier. **The FD bright line PASSED on both arms** (`G5_endpoint_fd_F-S`, `G5_endpoint_fd_F-P`), as did both `G6_planted_zero` gates and both `G6b_negative_control` gates — the reader was shown able to see a non-zero before its zero was believed. Item verdict composed by the `dafoam-supervisor` from the 19/1 per-gate split; the grader composes no item verdict. **THIS ROW DISCHARGES `RESULTS.md` §7 item 1**, which recorded the row as OWED AND NOT FILED because `scripts/append_record.py` refused the whole record at exit 7 on its D549 shape audit — measured **exit 0, VERDICT OK** at filing, cfd's restrike of the two offending strike-form lines having cleared it | **75.862 core-min** = 2 × **37.931** per arm [REGISTERED, `PREREGISTRATION.md:144-145`, `:156`, `:161`], from the rate model `(115.0 + 22 × 20.635) × 4 ÷ 60`. Frozen `ed9cda9080080b1f5f450ee3202a6d64ddb76d49`, 2026-08-27T21:58:16Z, **NEVER REVISED** — four days before the run. Caps **120.0** per arm, ceiling **240.0** | **77.934 core-min**, basis **GROSS** = **30.267** (F-S, SHIPPED, 454 wall s, ranks 4) + **47.667** (F-P, PATCHED, 715 wall s, ranks 4) [MEASURED, `ledger.txt` `ARM=` rows, both `rc=0` by `docker inspect`, both `inspect(exit,oomkilled)=[0 false]`]. **A reader must not take these from the grader's report object, which returns `core_min=120.000000` for BOTH arms — that is `enforced_core_min`, the CAP, spliced onto the wrong key** (`RESULTS.md` §2); the figures above are read from the launcher's own ledger | **= GROSS, 77.934 core-min.** The charter §2 3,600-s stall rule matches NO row — the longest arm is **715 wall s**, so nothing is excluded and cleaned and gross coincide. Against the ceiling: **0.3247×** of 240.0; **no overrun and no cap fired**, the longest arm's host wall being 41.8 % of its 1,710 s enforced deadline. **DERIVED, NOT MEASURED: $0.06663** measured-side and **$0.06486** registered-side at $0.0513/core-h, c7a.4xlarge, `cost_basis` **REPORTED-BY-OWNER** — the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` §5) | **1.0273** cleaned/registered — a **2.73 % miss** on a registration frozen four days earlier and never touched, and the tightest estimate this family produced that day | **THE AGGREGATE 1.0273 IS TWO OFFSETTING ARM-LEVEL MISSES, NOT TWO ACCURATE ONES, AND THAT IS THE CALIBRATION LESSON.** F-S came in at **0.798×** its registered 37.931 and F-P at **1.257×**. The item-level rate model held; the **per-arm** model did not, and a reader who takes 1.0273 as evidence the per-arm figure is tight will mis-size the next single-arm rung. **THE PATCHED ARM COST 57.5 % MORE THAN SHIPPED (47.667 ÷ 30.267 = 1.5749) AND THAT IS REPORTED, NOT ATTRIBUTED:** the two arms ran DIFFERENT IMAGES (`dafoam/opt-packages:latest` against `dafoam-idwarp-rot:v1`), so the delta carries an image term and a patch term **this chain contains no control to separate**; a same-image control was not run and is not implied by anything on disk. One arithmetic observation, offered as arithmetic and not as an attribution: F-P's wall exceeds F-S's by **261 s** and F-P's sub-1.0-core startup phase lasted **271 s** — the same size to within 4 %, consistent with the delta living in startup rather than in compute, and **equally consistent with a larger image taking longer to load**, which is not a statement about the patch either. **NOT CONTENTION:** the contention hypothesis was tested and argues against itself on the per-sample trace — the arm that PASSED was throttled MORE (`max_nr_throttled` 1450 on F-S against 1215 on F-P), and no sibling was on the registered cpuset in the window (`siblings_pre` and `siblings_post` both empty on both arms). **WASTE: 0.000 core-min**, named separately per `COMPUTE_BUDGET_CHARTER.md` §6 and never absorbed into the ratio — both arms `rc=0`, both rows present, nothing discarded and nothing re-run | `cases/dafoam/ladder-a/A2/curriculum_D4_SHIPPED_F3SR/RESULTS.md` §6 and §6.1 (graded and landed `96b5c67b`); registration frozen `ed9cda9080080b1f5f450ee3202a6d64ddb76d49`; measured figures from `/home/ubuntu/certonomous-runs/CURRICULUM-D4S-F3SR-a2-wing-cdmin/ledger.txt` (`ARM=F-S` and `ARM=F-P` rows) with `STATUS.F-S` / `STATUS.F-P` and `CHAIN_DONE` beside them |
| C-20260831T164216.179654Z-ec45d874 | 2026-08-31 | dafoam | **SO-3aF — the alpha-multipoint FEASIBILITY sweep for SO-3a: three cold primals at np=1 in one container on the A1 NACA0012. `rc=0`, `launcher_rc=0`, three of three declared alphas EXECUTED.** A feasibility probe, not a graded rung: it carries no verdict and moves no gate. **AND IT IS FILED WITH ITS OWN ADVERSE READING RATHER THAN WITHOUT IT:** the run's `converged_alphas=0` is **the instrument's error, not the physics'** — the reader failed to recognise convergence, a defect found by reading the output rather than trusting it and repaired at `fd2f06b9` / `1d3f5b18`. The compute below was spent and is costed whatever the reader did with it. **THIS ROW DISCHARGES `cases/dafoam/ladder-a/A1/feasibility_SO3a_alpha/COST_ROW_OWED.md`**, which recorded the row as OWED AND UNFILEABLE | **1.5 core-min** [REGISTERED, the queue entry's `cost_core_min_estimate`], against a registered cap of **8.0** core-min | **0.5833 core-min**, basis **GROSS** = **35 wall s at ranks 1** ÷ 60 [MEASURED, `/home/ubuntu/certonomous-runs/CURRICULUM-SO3aF-a1-naca0012-alpha-feasibility/STATUS.SO3aF`: `wall_s=35 ranks=1 core_min=0.5833 cap_core_min=8.0`, `rc=0 source=docker_inspect_ExitCode oom_killed=false`] | **= GROSS, 0.5833 core-min.** The charter §2 3,600-s stall rule matches no row — the whole sweep is 35 wall s, two orders of magnitude short of it. **7.29 %** of the 8.0 cap; no overrun and no cap fired. **DERIVED, NOT MEASURED: $0.0005** at $0.0513/core-h, c7a.4xlarge, `cost_basis` **REPORTED-BY-OWNER** (`COMPUTE_BUDGET_CHARTER.md` §5) | **0.3889** cleaned/registered — a **2.6× OVER-prediction** | **THE MISS IS ENTIRELY IN THE COLD-START MODEL, NOT IN THE WORK.** The 1.5 core-min estimate assumed roughly 15 s per cold primal plus roughly 15 s of container start; the whole three-point sweep took **35 s of container wall**. **LIKE-FOR-LIKE ANCHOR FOR THE NEXT ALPHA SWEEP ON THIS MESH AT np=1**, which is the point of filing this row: **~11.7 s per cold primal INCLUDING amortised container start**, against the **3.755 s** warm anchor — so **cold is ~3× warm on this mesh, not the 4× the estimate assumed**. Over-prediction of this shape is cheap and under-prediction is not, so the correction is offered as a better anchor rather than as a reason to trim caps. **WASTE: 0.000 core-min**, named separately per `COMPUTE_BUDGET_CHARTER.md` §6 and never absorbed into the ratio — the container exited `rc=0` and every declared alpha executed; the reader defect consumed no solver compute | `cases/dafoam/ladder-a/A1/feasibility_SO3a_alpha/COST_ROW_OWED.md` (the owed-row record this discharges); `cases/dafoam/ladder-a/A1/feasibility_SO3a_alpha/STATUS.SO3AF`; measured figures from `/home/ubuntu/certonomous-runs/CURRICULUM-SO3aF-a1-naca0012-alpha-feasibility/STATUS.SO3aF`; instrument repairs landed `fd2f06b9` and `1d3f5b18` |
| C-20260831T164745.937132Z-3fc3bd39 | 2026-08-31 | heat-transfer | **T19b STAGE 1 — the coarse pair, and the row `T19b_PREREGISTRATION.md` §8.3 REGISTERED AND NOBODY LANDED.** This ledger carried **zero** `T19` rows and **zero** `T19b` rows at HEAD when this row was written [MEASURED, `git show HEAD:docs/COST_CALIBRATION.md`], so the stage-1 comparison was owed and unfiled; this row discharges it late rather than letting the stage-3 row quietly absorb it. `P_q_c` and `P_Ts_c`, ny 20, 2 400 cells, `buoyantBoussinesqSimpleFoam` with `simulationType laminar`, 1 rank, both reaching `Time = 30000` == `endTime` with `28000/` and `30000/` written. **THE STAGING GATE EVALUATED: the frozen comparator printed `gate (1): C_PLATEAU ok`**, so §5's release of the m/f four was legal. **AND §8.3's REQUIRED SENTENCE IS CARRIED: T19's 59.0x and 89.5x were a TRUNCATION DETECTOR THAT FIRED, NOT AN ESTIMATOR MISS** — the registration priced 30 000 iterations and those two cases stopped at 828 and 541, iteration ratios 36.2x and 55.5x, same direction and same order as the cost ratios; filing them as estimator error would have buried the finding. | **5.904 core-min POINT** (2.952 each), **24 core-min CAP** (12 each), `timeout` 720 s, ranks 1 [REGISTERED, `T19_registered.json` carried verbatim at `T19b_PREREGISTRATION.md` §8.1]. Underlying rate **2.46e-06 core-s per cell-iteration at 2 400 cells** [BORROWED, from T1c's four completed runs]. Registered prediction **P6**: each coarse case lands between **1.3 and 2.6 core-min** [REGISTERED, §6]. | **2.100 core-min MEASURED** — `P_q_c` **1.050** and `P_Ts_c` **1.050**, both `wall_s=63`, `ranks=1`, `rc=0`, `capped=no`, `checkmesh_rc=0`, `note=clean` [MEASURED, `verification/runs/T-family/T19b_runs/STATUS.P_q_c` and `STATUS.P_Ts_c`]. **$0.0018 DERIVED** at $0.0513/core-h [REPORTED-BY-OWNER — the box cannot read its own billing, `COMPUTE_BUDGET_CHARTER.md` §5]. | **2.100 core-min = gross.** 63 wall s is nowhere near charter §2's 3600-s stall rule; neither case capped (8.75 % of its 12 core-min cap) and neither was re-run. | **0.3557 — a 2.81x OVER-prediction** (2.100 / 5.904) [DERIVED], identical on both arms (`P_q_c` 0.3557, `P_Ts_c` 0.3557). Measured cost **8.75e-07 core-s per cell-iteration** against the borrowed **2.46e-06** [DERIVED]. | **BORROW FAILURE — not contention, not waste, and a LARGER miss than §8.2's own hint.** §8.2 divided the truncation out of T19's figures and obtained a residual over-prediction of **≈1.63x per iteration**, labelling it [EXTRAPOLATED] and explicitly **weak** because a 2-3 s `wall_s` at 1-s granularity carries ±17-25 % from rounding alone. Measured over **63 s** (±0.8 % granularity) the true residual is **2.81x**, so §8.2's hint **under-stated the over-prediction by 1.72x** — precisely the figure §8.2 said stage-1 actuals would settle. Cause: the **[BORROWED]** T1c core-s-per-cell-iteration rate does not transfer, T1c being a pipe rung and this a 2-D plane channel. **PREDICTION P6 LOSES AND IS REPORTED AS WRONG:** it registered 1.3-2.6 core-min per coarse case against an actual **1.050**, below its band and in the conservative direction. **Waste, separately named and NOT absorbed into the ratio: 0.000 core-min in T19b stage 1** — two of two clean, none capped, none re-run. Named beside it and charged to T19 rather than here: T19's dead coarse pair cost **0.083 core-min** (0.050 + 0.033) [MEASURED, `T19_runs/STATUS.P_q_c`, `STATUS.P_Ts_c`], ruled `NOT A RESULT` by `docs/DEAD_LEVER_AUDIT.md` §21.2. | `verification/runs/T-family/T19b_runs/STATUS.P_q_c`, `STATUS.P_Ts_c`, `DONE.P_q_c`, `DONE.P_Ts_c`, and `gate_t19b.json` keys `gate1.plateau_and_residuals.q_c` / `.Ts_c`; registration `docs/campaigns/T-family/T19b_PREREGISTRATION.md` §6 P6 and §8.1-§8.3; ruling `docs/DEAD_LEVER_AUDIT.md` §21.2. Frozen instruments: `analyse_t19b.py` at `b52ed93b` (blob `32c309e1`, re-hashed identical before it ran) and `verification/runs/T-family/T19_runs/mark_done_t19.py` (`--selftest` 11 arms, 0 failures, rc 0). |
| C-20260831T164745.937182Z-82edc690 | 2026-08-31 | heat-transfer | **T19b STAGE 3 — THE FULL SIX GRADED AND THE RUNG CLOSED: G1, G2 and G3 all `PASS`, all three triples `CONVERGING`, and C_PLATEAU's FIRST REAL EVALUATION RETURNED `ok`, SO VERIFICATION'S §21.2 DIAGNOSIS HELD.** Fully developed laminar forced convection between parallel plates, Re_Dh 100, Pr 0.71, ny 20/40/80 (2 400 / 9 600 / 38 400 cells), two wall arms, 1 rank each, all six at `Time = 30000` == `endTime`. Six of six `DONE` under the strict completion rule. **The rung's registered ceiling stands: the referent is EXACT/derived, so T19b scores V, reaches `GATE REACHED` at best and can never reach `HOLDS`** [REGISTERED, `gate_t19b.json` key `ceiling`]. The row exists because rule 12 owes a calibration at every process completion — **and it carries the supervisor's own estimate, because a calibration ledger that omits the supervisor's miss is not a calibration ledger.** | **187.056 core-min POINT** total (2.952 x2 coarse, 12.624 x2 medium, 77.952 x2 fine), **624 core-min CAP** (12 / 50 / 250 per case), `timeout` 720 / 3 000 / 15 000 s, ranks 1 [REGISTERED, §8.1]. **AND THE SECOND PREDICTION, WHICH IS THE SUPERVISOR'S OWN AND IS NOT IN THE FROZEN DOCUMENT: the commissioning brief projected ≈148 core-min per FINE case, ≈60 % of the 250 core-min cap, finishing ≈18:45Z** [TRANSCRIBED, heat-transfer-supervisor's commissioning brief to this lane]. | **57.500 core-min MEASURED** — `P_q_c` 1.050, `P_Ts_c` 1.050, `P_q_m` 3.850, `P_Ts_m` 3.817, `P_q_f` 24.183, `P_Ts_f` 23.550; all six `rc=0`, `ranks=1`, `capped=no`, `checkmesh_rc=0`, `note=clean` [MEASURED, the six `STATUS.<case>` files]. The two fines ended **16:36:34Z** and **16:37:01Z** [MEASURED, `ended_utc`]. **$0.0492 DERIVED** against **$0.1599 DERIVED** for the registered point, at $0.0513/core-h [REPORTED-BY-OWNER]. | **57.500 core-min = gross.** The longest single case ran **1451 wall s**, so charter §2's 3600-s stall rule matches no row; peak cap usage **9.67 %**; no case re-run and no case capped. | **0.3074 overall — a 3.25x OVER-prediction, in the CONSERVATIVE direction, so no cap was ever at risk** [DERIVED]. By level: coarse 2.100/5.904 = **0.3557** (2.81x); medium 7.667/25.248 = **0.3037** (3.29x); fine 47.733/155.904 = **0.3062** (3.27x). By case: 0.3557, 0.3557, 0.3050, 0.3024, 0.3102, 0.3021 — a band of 0.302 to 0.356 across a 16x mesh range, so the miss is a **systematic scale error, not scatter**. **AGAINST THE SUPERVISOR'S OWN BRIEF THE MISS IS TWICE AS LARGE: 24.183/148 = 0.163 and 23.550/148 = 0.159, a 6.12x and 6.28x OVER-prediction; ≈9.7 % and ≈9.4 % of cap against a projected ≈60 %; and both fines finished ≈2 h 08 m earlier than the projected ≈18:45Z** [DERIVED from MEASURED actuals against a TRANSCRIBED projection]. | **One cause, systematic: a BORROWED per-cell-iteration rate that does not transfer. No contention and no waste.** The registered POINT rests on T1c's measured 2.46e-06 / 2.63e-06 / 4.06e-06 core-s per cell-iteration [BORROWED]; T19b's measured rates are **8.75e-07 / 8.02e-07 / 1.26e-06** [DERIVED from `wall_s`, cells and 30 000 iterations] — 0.30 to 0.36 of the borrow at every level. T1c is a **pipe** rung; this is a 2-D plane channel, and the borrow was already flagged as a misprediction risk in T19's registration, though in the *opposite* direction (it predicted under-prediction from the mesh jump). **The supervisor's separate 6.1x-6.3x miss is a DIFFERENT error and is not folded into the 3.25x**: it compounds the borrow with a cap-fraction assumption (≈60 %) that the borrow itself never supported. **Lesson for the next estimate: a rate borrowed across a geometry change is worth roughly a factor of three here, and an independent hand estimate layered on top of it tripled the error again rather than checking it.** **Waste, separately named and NOT absorbed into the ratio: 0.000 core-min** — six of six clean, none capped, none re-run, no stall. **Avoided, not incurred, and recorded so it is not miscounted as a saving:** §21.2(b) held the m/f four back at 181.152 core-min of registered POINT that the frozen instrument was guaranteed to refuse; that spend was never made. Physics predictions P0-P5 are scored against `gate_t19b.json` in this lane's report to `heat-transfer-supervisor`; **P0 HELD** and **P5 LOST**. | `verification/runs/T-family/T19b_runs/gate_t19b.json` (rows G1/G2/G3, `triple_state`, `observed_order`, `gci_pct`, `band_verdict`, `verdict`, `gate1`, `planted_zero_controls`), the six `STATUS.<case>` and six `DONE.<case>` files under `verification/runs/T-family/T19b_runs/`; registration `docs/campaigns/T-family/T19b_PREREGISTRATION.md` §6 and §8.1-§8.3; ruling `docs/DEAD_LEVER_AUDIT.md` §21.2. Frozen instruments hashed before use: `analyse_t19b.py` blob `32c309e112152bca019cb774bb8e310f663f58f7` identical to the blob at `b52ed93b` (`--selftest` 39 arms, 0 failures), and `mark_done_t19.py` blob `13117be1af47e0153877f98e1b93feb339f217b2` identical to HEAD (`--selftest` 11 arms, 0 failures). |
| C-20260831T171048.731943Z-28d7ec77 | 2026-08-31 | ansys-verification | **VMFL038 GRADED `NOT A RESULT` AND CLOSED — Falling Film Over an Inclined Plane, VM2026R1 pp. 131–132; register row #47.** OpenFOAM v2606 `simpleFoam`, steady incompressible laminar SIMPLE, 2-D planar, **serial (RANKS = 1, so core-min = wall-min on every figure in this row)**. Three-level film-normal family, r = 2, **Ny = 40/80/160 with Nx FIXED at 180** (7 200 / 14 400 / 28 800 cells, `log.checkMesh`). Terminated on `residualControl { p 1e-10 }` at iterations 2 093 / 5 340 / 13 330, all three levels rc = 0. **The `NOT A RESULT` is a physics/method refusal, NOT a cost event**: nothing failed, nothing was capped, nothing restarted — all three levels completed inside the cap and the refusal comes from an anisotropic (y-only) refinement sequence that violates the Richardson/Roache premise, giving a `DIVERGENT` triple (R = 9.051). **This row records a full-price run that bought a defensible refusal, which is what the lab wants such a run to cost.** | **6.00 core-min REGISTERED POINT ESTIMATE**, frozen a-priori at `cases/ansys_verification/VMFL038/PREREGISTRATION.md` §7 (heading :292, table :297–:301) in blob `a1ac010431a3e65f8a4ee9af8b0edadf81d1f248`, commit `c5ae8427`, **before any VMFL038 field existed**. Separately registered **CAP 15 core-min** as a running total across L1+L2+L3. The estimate's own stated basis (:304–:306) was a 13.4 wall-s L1 probe scaled by cells to "L3 ~4 core-min → ~5–6 core-min" | **7.75 core-min MEASURED** — Σ of the per-level `RUN_RC.L{1,2,3}` `core_min` fields written by the launcher at RANKS = 1: **L1 0.2333 (14 wall-s) + L2 1.3167 (79 wall-s) + L3 6.2000 (372 wall-s)**; independently agrees with `COST.txt` `total_core_min = 7.75`. **= $0.006626 DERIVED, NOT MEASURED** (0.129167 core-h × $0.0513/core-h, c7a.4xlarge, owner-stated 2026-08-21/22, **reported-by-owner** — the box cannot read its own billing, `COMPUTE_BUDGET_CHARTER.md` §5) | **7.75 core-min — gross and cleaned are IDENTICAL here.** The charter §2 stall rule (a row over 3600 wall s) matches **no** level: the longest is L3 at 372 wall s, 9.7× under the threshold. Nothing is subtracted, so the cleaned figure is stated as equal rather than left unstated | **1.292** (7.75 cleaned ÷ 6.00 registered). Against the registered **CAP** the run used **51.7 %** (7.75 / 15), leaving 7.25 core-min unspent; **no overrun, so the CLAUDE.md rule-12 stop never armed** | **MISPREDICTION OF ITERATION COUNT, essentially all of it — and it is a specific, reusable lesson, not a shrug.** The §7 basis scaled the L1 probe by **cells** (×2 per level) and so implicitly assumed iteration count roughly constant; the solve actually took **2 093 → 5 340 → 13 330** iterations, growing **×2.55 then ×2.50 per level**, i.e. FASTER than cells, because a `residualControl { p 1e-10 }` termination on an increasingly anisotropic mesh (near-wall aspect ratio degrading 4 → 8 → 16) needs progressively more SIMPLE sweeps to drive the pressure residual to a FIXED floor. The predicted-vs-actual split lands almost entirely at L3 (predicted ~4, actual 6.2). **Actionable for the next estimate: when a level terminates on a fixed residual floor rather than a fixed iteration/endTime budget, scale the estimate by expected ITERATIONS, not by cells — and add a factor for anisotropic families, where the iteration count carries the aspect-ratio penalty too.** **CONTENTION: NEGLIGIBLE, AND NAMED SEPARATELY — it is not folded into the 1.292.** Solver `ClockTime/ExecutionTime` = **1.0393 / 1.0003 / 1.0004** (last `ExecutionTime` line of each `log.simpleFoam`); the L1 figure is an artefact of OpenFOAM printing `ClockTime` as an integer (13.47 s → 14 s), not measured contention. `CONTENTION.txt` records load average 2.35 on nproc 16 at launch. **WASTE: ZERO, AND NAMED SEPARATELY PER `COMPUTE_BUDGET_CHARTER.md` §6 — NEVER ABSORBED INTO THE RATIO.** Three levels were launched and three completed; there was **no restart, no re-mesh, no failed or abandoned level, no discarded solve, and no smoke/scoping probe** (`PREREGISTRATION.md` :308 declined one on the record). Every core-minute of the 7.75 bought a level that appears in the graded triple, so the whole 1.292 is misprediction and none of it is waste. **A cost-calibration reader must not read the `NOT A RESULT` verdict as waste**: the refusal is the product | `verification/runs/ansys_verification/VMFL038/RESULTS.md` (run root, this team's standing filing ruling); figures from `RUN_RC.L{1,2,3}` and `COST.txt` in that same run root; verdict read from the machine-readable grading record `verification/runs/ansys_verification/VMFL038/GRADING_RECORD_2026-08-31T1710Z.json`, not from stdout; register row **#47** in `verification/credentials/ansys/ANSYS_VALIDATION_REGISTER.md`; freeze `c5ae8427` |
| C-20260831T172238.285090Z-f3cfac37 | 2026-08-31 | ansys-verification | **VMFL033-R2 GRADED `PASS` AND CLOSED — Viscous Heating in an Annulus, VM2026R1 p. 119; register row #48.** A **NEW registration succeeding R1** (row #21, `NOT A RESULT`), **not** an edit of it — R1's frozen files are untouched and its row stands; R2's ONE substantive change is `endTime` 20 000 → 100 000, because R1 died of too few iterations. OpenFOAM v2606 `buoyantSimpleFoam`, `laminar`, **serial (RANKS = 1, so core-min = wall-min on every figure in this row)**, radial family r = 2 with θ held FIXED at 8 cells (256 / 512 / 1024 cells), fixed-iteration mode so every level runs the full 100 000 iterations. Freeze `7c5158df`. **The run finished at 16:39:52Z and sat UNGRADED for ~35 min before this lane graded it at 17:15Z — recorded because this team has now let a finished run sit ungraded three times, and an idle graded-ready run is a process defect even when the verdict is a `PASS`.** | **~10.3 core-min EXTRAPOLATED** — explicitly labelled EXTRAPOLATED, not MEASURED, in the frozen `cases/ansys_verification/VMFL033-R2/PREREGISTRATION.md` §2 line 12 and again in §9: R1's MEASURED per-level actuals at 20 000 iterations (0.4167 / 0.6333 / 1.0167 core-min) scaled ×5 for the 5× `endTime` → **2.08 / 3.17 / 5.08**. An IN-FAMILY extrapolation (C-199 precedent); no cross-family transfer. **PER-LEVEL CAPS, not a shared drawdown: 8 / 12 / 20 core-min** | **7.9167 core-min MEASURED** — the launcher's own `core_min` field in the three `RUN_RC.txt`, corroborated line-for-line by `launcher.queue.out`: **L1_nr32 1.6000 (96 wall s) + L2_nr64 2.4167 (145 s) + L3_nr128 3.9000 (234 s)**. **= $0.006769 DERIVED, NOT MEASURED** (0.131945 core-h × $0.0513/core-h, c7a.4xlarge, owner-stated 2026-08-21/22, **REPORTED-BY-OWNER** — the box cannot read its own billing, `COMPUTE_BUDGET_CHARTER.md` §5). **R1's spend is NOT counted here**: R1 is its own closed row, and this row grades R2's cost against R2's own registered estimate | **7.9167 core-min — gross and cleaned are IDENTICAL.** The charter §2 stall rule (a row over 3600 wall s) matches **no** level; the longest is L3 at 234 wall s, **15.4× under** the threshold. Nothing is subtracted, so the cleaned figure is stated as equal rather than left unstated | **0.769** (7.9167 cleaned ÷ 10.3 extrapolated) — **an OVER-prediction, and unusually clean: the three levels agree independently at 0.7692 / 0.7624 / 0.7677.** Cap usage **20.0 % / 20.1 % / 19.5 %** of the per-level 8 / 12 / 20; no cap crossed, `CAP CROSSED` never printed, the rule-12 stop never armed | **THE ESTIMATE SCALED THE WRONG PART OF THE MEASUREMENT, AND THE MECHANISM IS EXACT ENOUGH TO REUSE.** §2 line 12 took R1's measured per-level **whole level time** and multiplied by 5. But a level costs `F + k·iterations`, where `F` is fixed setup — `blockMesh`, `checkMesh`, `writeCellCentres`, the field write — which does **not** scale with `endTime`. Fitting the two-point model from R1 (20 000 iter) and R2 (100 000 iter) per level: L1 25 s → 96 s gives k = 8.875e-04 s/iter and **F = 7.25 s = 29.0 %** of R1's level time; L2 38 s → 145 s gives F = 11.25 s = **29.6 %**; L3 61 s → 234 s gives F = 17.75 s = **29.1 %**. **The fixed fraction is ~29 % at every level, and that one number explains the entire miss**: the correct multiplier is `0.29 + 5 × 0.71 = 3.84`, not 5.0, and **3.84 / 5.0 = 0.768**, which IS the measured 0.769 to three figures, reproduced independently at all three levels. The extrapolation was not noisy — it was systematically wrong by exactly the fixed-overhead fraction. **ACTIONABLE: when extrapolating a measured run to a longer `endTime`, split the baseline into fixed setup and iteration-proportional parts and scale ONLY the latter; scaling the whole level time over-predicts by the fixed fraction (~29 % on this family).** This is the mirror image of the same day's VMFL038 miss (ratio 1.292), which UNDER-predicted by scaling with **cells** a solve that terminated on a fixed residual floor — **both are the same error, extrapolating along the wrong variable**, and the pair is more instructive than either row alone. **CONTENTION: NAMED SEPARATELY, NEGLIGIBLE — not folded into the 0.769.** `ExecutionTime/ClockTime` = 0.9779 / 0.9668 / 0.9679, so 3–4 % of wall sat outside the solver's own accounting, consistent with I/O and startup rather than load. **WASTE: ZERO, NAMED SEPARATELY PER `COMPUTE_BUDGET_CHARTER.md` §6 AND NEVER ABSORBED INTO THE RATIO.** Three levels launched, three completed `rc = 0`; **no restart, no re-mesh, no abandoned or repeated level, no smoke run in the graded path, no cap crossing, no failed grading pass.** Every core-minute bought a level that appears in the graded triple. **A CAVEAT A CALIBRATION READER IS OWED: an over-prediction is not automatically good news** — the headroom was deliberately sized at ~3.8× on a rationale that included a FALSE premise about runner-side cap enforcement (corrected in ADDENDUM 1 to the frozen file; the caps themselves stand and are not reopened), so the 0.769 should be read as "the fixed-overhead model was missing", not as "the caps were well chosen" | `verification/runs/ansys_verification/VMFL033-R2/RESULTS.md` §C (run root, this team's standing filing ruling); figures from `L{1_nr32,2_nr64,3_nr128}/RUN_RC.txt` and `launcher.queue.out` in that same run root; verdict from the frozen comparator's own output `verification/runs/ansys_verification/VMFL033-R2/GRADING_STDOUT_2026-08-31T1715Z.txt` (**the frozen comparator has NO JSON mode — see the row's disclosure**); register row **#48** in `verification/credentials/ansys/ANSYS_VALIDATION_REGISTER.md`; freeze `7c5158df`; R1's predecessor row #21 |
| C-20260831T172527.907664Z-b006f781 | 2026-08-31 | heat-transfer | **T22_CHTb_L1 FEASIBILITY COMPLETED — THE FAMILY'S FIRST MEASURED `chtMultiRegionSimpleFoam` ANCHOR ON A THREE-REGION WEDGE, AND THE ANCHOR THAT WAS BORROWED FROM CARTESIAN SINGLE-SOLID CASES TURNS OUT TO TRANSFER.** **NO VERDICT IS ASSERTED IN THIS ROW.** T22 is an UNREGISTERED FEASIBILITY rung under Sanaa's 2026-08-31T15:51Z queue ruling; its own note (`verification/runs/T-family/T22_runs/T22_FEASIBILITY_NOTE.md`) states it produces no graded value, reaches no band and needs no comparator, and none of PASS / GATE REACHED / GATE FAIL / NOT A RESULT applies to it. What it did is answer ONE question — does `chtMultiRegionSimpleFoam` advance this three-region 5-degree wedge (fluid + housing + core, TWO conjugate `mappedWall` interfaces, sector-scaled volumetric source) without dying, and do residuals fall? **ANSWERED, AFFIRMATIVELY, ON THE SECOND LAUNCH.** OpenFOAM v2606, 39,680 cells (35,200 fluid + 3,360 core + 1,120 housing), `endTime` 5000, **serial (RANKS = 1, so core-min = wall-min on every figure in this row)**. All six conjuncts of standing rule 4 hold [MEASURED]: `End` line count 1; last time directory 5000 == registered `endTime` 5000; `ExecutionTime` count 5000; fields present (`fluid` T U alphat k nut omega p p_rgh phi rho, `housing` T p, `core` T p); age guard PASSES with every `5000/` field 844 s NEWER than the case's own `0/housing/T`. **rc = 0 is DERIVED, NOT READ, and the reason is a defect reported in the next sentence.** | **15.4 core-min [DERIVED, NOT MEASURED]** — registered in the queue entry before launch from two MEASURED Cartesian single-solid anchors (`T5_CUBE_m` 4.1541e-06, `T5_CUBE_f` 4.6476e-06 s per cell-iteration), the conservative `_f` end of that bracket, with the wedge / two-solid / source / fluid-region mismatches DISCLOSED rather than absorbed. Registered CAP 50.0 core-min, enacted as `timeout 3000s` inside the wrapper. | **13.9872 core-min [MEASURED]** — `ExecutionTime = 839.23 s` read from the case's own `log.solve`, x 1 rank / 60. Launch-to-end wall basis gives 14.0833 core-min (845 s, 17:07:37Z -> 17:21:42Z), which includes the `0.orig` copy and the three-region mesh read; the ExecutionTime basis is used for the ratio because it is the solver's own figure. USD **$0.011959, DERIVED NOT MEASURED** at the owner-stated c7a.4xlarge $0.0513/core-h (REPORTED-BY-OWNER 2026-08-21/22); the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` section 5). | **13.9872 core-min — GROSS AND CLEANED ARE IDENTICAL HERE, and that is stated rather than left to be assumed.** The charter section 2 stall rule matches a row over 3600 wall seconds; this run is 839.23 s, so the rule matches nothing and no judgement enters the figure. Cap utilisation **28.0%** of the registered 50.0. | **0.908** | **MISPREDICTION, AND CONSERVATIVE BY DESIGN — the entire 9.2% gap, with contention and waste both measured to zero and therefore not available as excuses.** CONTENTION: the queue runner measured the box at **busy = 1.7% (~0.3/16 cores), MemAvailable 29.1 GB** at launch [MEASURED, `verification/queue/runner.log` 2026-08-31T17:07:37Z], so this reading is admissible as calibration and is not a contended number. WASTE: **0.0 core-min in this launch**; launch 1 (2026-08-31T16:35:11Z, pid 140701) spent approximately **0.0 core-min** because its shell aborted inside `etc/bashrc` BEFORE the solver line, so the failed attempt bought nothing and also cost nothing — waste is named here separately and is NOT folded into the ratio (charter section 6). MISPREDICTION, and this is the transferable finding: the ACTUAL rate is **4.22999e-06 s per cell-iteration**, which lands **INSIDE the measured Cartesian bracket [4.1541e-06, 4.6476e-06]** and near its `_m` end. **So the four DISCLOSED mismatches — a 5-degree wedge instead of Cartesian, TWO solid regions instead of one, a sector-scaled volumetric source, and a fluid region running `kOmegaSST` that neither anchor's cost included — did NOT inflate the per-cell rate at all.** The whole gap is that the registration deliberately took the conservative end of a bracket it had honestly measured. **A borrowed anchor was expected to be a LOWER BOUND and turned out to be a BRACKET; that is the single most useful number this row contributes, and it is offered as a measurement and NOT as a rule** (Sanaa's 14-day freeze). | Case `verification/runs/T-family/T22_runs/T22_CHTb_L1/` — `log.solve` (839.23 s, `End`, 5000 iterations), `5000/{fluid,housing,core}/`, `STATUS.T22_CHTb_L1`. Queue entry `verification/queue/heat-transfer/launched/T22_CHTb_L1_R2.json`; launch row in `verification/queue/LAUNCH_LOG.tsv` at 2026-08-31T17:07:37Z pid 176322. Repair diff `verification/runs/T-family/T22_runs/T22_LAUNCHER_REPAIR.diff`; launch-1 evidence preserved unmodified at `T22_CHTb_L1_FAILED_LAUNCH1_20260831T163511Z/` and `T22_LAUNCH_1_TRIAGE.md`. **TWO BOOKKEEPING DEFECTS REPORTED BESIDE THIS ROW, NEITHER VOIDING THE PHYSICS (L-342; Sanaa's universal rule 2026-08-26, bookkeeping never voids physics).** (1) **THE QUEUE RUNNER CLOBBERED THE WRAPPER'S OWN `STATUS` FILE.** Both write `STATUS.<case_id>`; the runner's `echo ... > STATUS.<case_id>` lands last, so **0 of the wrapper's fields (`rc`, `wall_s`, `core_min`, `cap_core_min`, `capped`, `solver`) survive** [MEASURED] and the file the entry's own `_field_classes` calls PHYSICS_CRITICAL was destroyed. rc = 0 is recovered by a two-step derivation, not read: the surviving `launcher_rc=0` is the exit status of `bash run_t22.sh`, and that wrapper's final line is `exit "$rc"` where `$rc` was captured on the line immediately after the solver call — **so for THIS wrapper `launcher_rc` IS the solver rc, and the runner's own conservative note that it is not is correct in general and superseded by the wrapper's propagation here.** The `core_min` figure in this row is therefore taken from `log.solve`'s `ExecutionTime` rather than from the lost `STATUS`, which is why the basis is stated. (2) `launcher.queue.out` is **0 bytes** — benign here, because the wrapper redirects the solver to `log.solve` and nothing reaches stdout; **it is recorded because 0 bytes was launch 1's CRASH SIGNATURE, and the same reading must not be made twice with opposite meanings.** **A THIRD FINDING, PHYSICS AND NOT BOOKKEEPING, RECORDED HERE BECAUSE IT WILL MISLEAD THE NEXT READER: the final `Ux` initial residual is 0.1353 and DID NOT FALL, while `Uy` reached 6.25e-10, `Uz` 4.22e-12, `h` 9.59e-10 and `p_rgh` 8.64e-09.** `Ux` is the **identically-zero circumferential component of a wedge**: max|Ux| = **3.84e-15** in the final field against max|Uz| = **20.90 m/s** [MEASURED, `5000/fluid/U`, 35,312 vectors]. Its normalised residual is a ratio whose denominator is machine zero, and it sits flat in 0.1332-0.1386 across 4,500 iterations — **an artefact of normalisation, not a convergence failure. A reader that greps the last `Ux` residual would report every run of this family as unconverged**, which is the same "a match is not a meaning" class this lab has been bitten by before. Recorded as a FINDING; **it spawns no rule and no tool** (Sanaa's plumbing freeze, `etc/sessions/2026-08-31T1544Z_sanaa_plumbing_freeze.md`, read at source). |
| C-20260831T172757.247519Z-34cbf5b1 | 2026-08-31 | heat-transfer | **CORRECTION AGAINST THE COMMIT MESSAGE OF `25c36d4b`, MINUTES OLD, WHICH APPENDED THE ROW HEADED "T22_CHTb_L1 FEASIBILITY COMPLETED". THAT ROW'S CALIBRATION FIGURES ARE UNAFFECTED AND UNCHANGED; WHAT IS FALSE IS A PROVENANCE SENTENCE IN THE COMMIT MESSAGE, AND IT IS STRUCK HERE BECAUSE A COMMIT MESSAGE CANNOT BE EDITED.** That message asserts, under the heading FOREIGN ROWS, that the commit *"carries TWO ansys-verification rows I did not write ... VMFL038 and VMFL033-R2, 5,714 bytes beyond the committed blob"*. **BOTH FIGURES ARE WRONG.** `git diff 25c36d4b~1 25c36d4b` inserts exactly **TWO LINES**: **ONE** foreign row, the ansys-verification **VMFL033-R2** row, and my own. The ansys-verification **VMFL038** row was **ALREADY COMMITTED in my own parent `ac12ca5a`** and my commit did not carry it [MEASURED, `git show ac12ca5a:docs/COST_CALIBRATION.md`]. **THE MECHANISM OF THE ERROR IS THE EXACT FAILURE I GUARDED AGAINST TWICE IN THE SAME SESSION AND THEN COMMITTED ANYWAY: the "two rows / 5,714 bytes" figures were read from the `--dry-run` taken against HEAD `fe666fd5`, and by the time the real append ran a peer had moved HEAD to `ac12ca5a` and one of those rows had landed.** A figure taken minutes earlier is a fact about a different tree — which is precisely why the T23 registration re-verified its absent run directories and re-derived its T-number **inside** the committing invocation, and I then copied a stale number straight into the prose of that same commit. **The discipline was applied to the evidence and not to the sentence about the evidence.** **WHAT IS TRUE AND IS NOT WITHDRAWN, each measured:** no row is duplicated — the VMFL038, VMFL033-R2 and T22 rows each occur **exactly once** at HEAD and in the worktree; `scripts/append_record.py` merged rather than overwrote and dropped nothing; ansys-verification's rows need no second landing (the operational advice in the struck sentence happened to be correct even though its count was not); and the decision to **preserve** a foreign tail rather than drop it stands, because dropping it would delete a peer's unfiled work, which is the worse failure by a wide margin. **A SECOND GUARD FIRED ON THE WAY TO THIS ROW AND IS RECORDED RATHER THAN QUIETLY WORKED AROUND:** the first draft of this correction quoted the three rows by their literal tool-allocated ids, and `append_record.py` **REFUSED it** on the ground that hand-writing that id form is the very mechanism a tool-allocated id exists to prevent. The refusal was correct, nothing was written, and this row references the corrected rows **by subject instead of by id**. **NO VERDICT IS ASSERTED IN THIS ROW OR IN THE ROW IT CORRECTS.** | not applicable — this row corrects a provenance statement, not a cost | not applicable | not applicable | not applicable | Corrects the COMMIT MESSAGE of `25c36d4b`, **not** the T22 calibration row it landed, whose predicted 15.4 core-min / actual 13.9872 core-min / ratio 0.908 / gross-equals-cleaned / zero contention / zero waste all stand unchanged. Evidence: `git diff 25c36d4b~1 25c36d4b --stat` (1 file, 2 insertions), `git show ac12ca5a:docs/COST_CALIBRATION.md`, and per-row occurrence counts taken at HEAD and in the worktree. Appended under this file's append rule 1 — an existing row is never edited and a correction is a new row naming what it corrects. |
| C-20260831T173836.895527Z-273c297a | 2026-08-31 | ansys-verification | **VMFL006 GRADED `NOT A RESULT` AND CLOSED — Multicomponent Species Transport in Pipe Flow (VM2026R1 pp.27–28).** The three-level run completed physically (rc 0, End, last time == endTime 3000 at each level); the frozen comparator then refused at the L1 convergence control (NULL RANGE — the T residual is exactly flat at the machine floor over the last 200 iters), so the verdict is `NOT A RESULT`. The SOLVE ran and its cost is MEASURED; a grading refusal does not un-run the solver (L-342). | EXTRAPOLATED bracket **0.8–8.4 core-min** (midpoint 4.6 filed as the queue estimate; no measured anchor — the pre-freeze smoke's COST.txt read 0 core-min), cap 18 core-min. `PREREGISTRATION.md` §15. | **1.6333 core-min MEASURED** (L1 5 s / 0.0833 + L2 18 s / 0.30 + L3 75 s / 1.25; RANKS 1; read from per-level `RUN_RC.txt` and `COST.txt`). **$0.001396 DERIVED** at $0.0513/core-h (REPORTED-BY-OWNER — the box cannot read its own billing, COMPUTE_BUDGET_CHARTER §5). | = gross (no level exceeded 3600 wall s; max wall 75 s, so the stall rule matches nothing). | **0.355× actual/midpoint** (1.6333 / 4.6). Landed INSIDE the registered bracket, near the fast end — 1.94× the fast-end 0.84, 0.194× the slow-end 8.4. | Misprediction in the conservative (cheaper) direction: realised throughput **2.571×10⁶ cell-steps/s** (2.520×10⁸ cell-steps / 98 wall s) sat in the fast half of the assumed 5×10⁵–5×10⁶ window, so the bracket midpoint over-estimated wall time ≈2.8×. Box quiet — no contention evident. **Zero waste.** | `verification/runs/ansys_verification/VMFL006/RESULTS.md` §6; prereg frozen `e28a6a29`; verdict `NOT A RESULT` via the convergence-clause refusal (RESULTS.md §4). |
| C-20260831T183346.079343Z-d971eca8 | 2026-08-31 | heat-transfer | **T23 GRADED — THE FOUR REGISTERED CASES OF THE RESCALED CASE 3 MAP, `PASS` x4 ON THE PHYSICALITY TIER, AND THE FIRST SAME-GEOMETRY COST THIS FAMILY HAS EVER PREDICTED WELL.** Gates FROZEN at `docs/campaigns/T-family/T23_PREREGISTRATION.md`, commit `fe666fd5d3cf148b1266bf693d1199cec3c7d607`; the grading path was verified to BE the file that ran — worktree blob `c341476f3680c14ec593c52d12e49214cf83ebb3` identical to `HEAD:` that path, sha256 prefix `b46d23c9cbbc945d` on both [MEASURED]. Four cases `T23_P305_U{10,20,30,40}`, `chtMultiRegionSimpleFoam`, OpenFOAM v2606, 39,680 cells (35,200 fluid + 3,360 core + 1,120 housing), 5-degree wedge, two conjugate `mappedWall` interfaces, `endTime` 10000, **serial (RANKS = 1, so core-min = wall-min on every figure in this row)**. **VERDICTS: `PASS`, `PASS`, `PASS`, `PASS`** on B1/B2/B3 of section 1 line 4 — Q1 `T_max` on the housing 103.6078 / 69.0098 / 55.4389 / 47.8448 degC against the registered 200.0 degC bound, margins +96.39 / +130.99 / +144.56 / +152.16 K; Q1 − Q2 = +2.158008 / +1.825226 / +1.635613 / +1.499971 K, so the anti-degeneracy control B3 holds on all four [MEASURED]. **ZERO FLAGS, and section 1 line 4 registered before compute that zero flags was NEVER the requirement of this tier and must not be engineered for** — nothing was engineered for it. **(305 W, 20 m/s), registered UNDECIDED at +2.70 K under the bound by section 2.7, DID NOT FLAG** and landed 130.99 K clear: a registered-in-advance outcome landing on one of its two registered sides. **NO Roache classification, GCI or observed order is quoted from this rung and none may be: section 1 line 5 REGISTERS NO LADDER, every point ran at L1 only, and A SINGLE MESH LEVEL ADMITS NO TRIPLE.** All six conjuncts of standing rule 4 hold [MEASURED]; **two of them were UNEVALUABLE from their registered evidence and are named in the attribution column rather than treated as passing.** | **123.2 core-min SUBSET POINT [REGISTERED, NOT MEASURED]** — `T23_PREREGISTRATION.md` section 5.2, frozen at `fe666fd5` BEFORE the run root existed: 39,680 cells x 10,000 iterations = 3.968e08 cell-iterations at 4.6476e-06 s per cell-iteration = 30.8 core-min per case x 4. That rate is the CONSERVATIVE end of a MEASURED Cartesian bracket (`T5_CUBE_m` 216,214 cells / 4,490.88 s = 4.1541e-06; `T5_CUBE_f` 896,531 cells / 20,833.54 s = 4.6476e-06, both `chtMultiRegionSimpleFoam` at 1 rank), with the wedge / two-solid / sector-scaled-source / `kOmegaSST`-fluid mismatches DISCLOSED at section 5.1 rather than absorbed. Registered SUBSET CAP **400.0 core-min hard**, per case **100.0**, ENACTED as `timeout 6000s` inside each `run_t23.sh` (`cap_core_min = TIMEOUT_S * RANKS / 60`); an overrun STOPS the run and does not get a new budget. **T22's same-geometry 4.22999e-06 was NAMED BUT NOT SUBSTITUTED** into the registered figure, because section 5.2 froze before T22 finished and a frozen cost may not be re-fitted to a later measurement. | **120.1285 core-min [MEASURED]** — the four `ExecutionTime` lines of the cases' own `log.solve`, x 1 rank / 60: **1784.02 s = 29.7337; 1813.14 s = 30.2190; 1829.39 s = 30.4898; 1781.16 s = 29.6860 core-min.** USD **$0.102710, DERIVED NOT MEASURED** at the owner-stated c7a.4xlarge $0.0513/core-h (REPORTED-BY-OWNER 2026-08-21/22); the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` section 5). Launch-to-end wall basis gives 17:33:02Z -> 18:02:47Z etc. and includes the `0.orig` copy and the three-region mesh read; **the ExecutionTime basis is used for the ratio because it is the solver's own figure**, the same basis as this ledger's T22 row of 2026-08-31T17:25:27Z (heat-transfer, `T22_CHTb_L1`), cited by its date and subject rather than by its id because this module is the sole producer of that id form and refuses rows that hand-write one. | **120.1285 core-min — GROSS AND CLEANED ARE IDENTICAL HERE, and that is stated rather than left to be assumed.** The charter section 2 stall rule matches a row over 3600 wall seconds; the longest of the four ran **1829.39 s**, so the rule matches nothing and no judgement enters the figure. Cap utilisation **30.03 %** of the registered 400.0; worst single-case utilisation **30.49 %** of the per-case 100.0. | **0.9751** | **MISPREDICTION, AND CONSERVATIVE BY DESIGN — the entire 2.5 % gap. CONTENTION measured and small; WASTE measured to ZERO and therefore not available as an excuse.** **CONTENTION:** the four launches were recorded by the queue runner at box busy **11.9 % / 12.7 % / 22.3 % / 25.7 %** (~1.9 to ~4.1 of 16 cores), MemAvailable 29.0 / 28.9 / 28.6 / 28.5 GB [MEASURED, `verification/queue/runner.log` at 17:33:02Z, 17:34:07Z, 17:35:12Z, 17:36:17Z], so this reading is admissible as calibration and is not a contended number; the four ExecutionTimes span only **1781.16–1829.39 s, a 2.7 % spread**, which is what a low-contention concurrent set looks like. **WASTE: 0.0 core-min.** All four cases launched clean on the FIRST attempt; there is no failed T23 launch. Named separately and likewise NOT in the ratio (charter section 6): the y+ measurement passes cost **2 wall s total at 1 rank = 0.0333 core-min**, which is analysis cost, not waste and not part of the registered solve budget. **MISPREDICTION, and this is the transferable finding: the ACTUAL rate is 4.541148e-06 s per cell-iteration, which lands INSIDE the measured Cartesian bracket [4.1541e-06, 4.6476e-06], near its `_f` end.** So a 5-degree wedge with TWO solid regions, TWO conjugate `mappedWall` interfaces, a sector-scaled volumetric source and a `kOmegaSST` fluid region costs, per cell-iteration, what a Cartesian single-solid sourceless case costs. **Section 5.1 registered the anchors as "a LOWER BOUND on the per-cell rate, not a bracket around it"; the measurement says they are a BRACKET**, and T22 said the same at 4.22999e-06 on the same geometry at half the iteration count — **two independent same-geometry measurements now agree.** **THE CONTRAST WORTH RECORDING, because it is the calibration lesson: T19b's ladder missed by 3.25x while T22 and T23 landed at 0.908 and 0.9751. The difference is not luck and not effort — T19b BORROWED ITS RATE ACROSS A GEOMETRY CHANGE (T1c, a PIPE, lending to a PLANE CHANNEL), while T22 and T23 borrowed ACROSS DIMENSIONALITY AT SIMILAR GEOMETRY (a Cartesian single-solid CHT cube lending to a CHT wedge). A borrowed per-cell-iteration rate survives a change of dimensionality and does not survive a change of geometry family.** Offered as a MEASUREMENT and NOT as a rule (Sanaa's 14-day plumbing freeze, `etc/sessions/2026-08-31T1544Z_sanaa_plumbing_freeze.md`, read at source). | Results record `docs/campaigns/T-family/T23_RESULTS.md`. Cases `verification/runs/T-family/T23_runs/T23_P305_U{10,20,30,40}/` — each `log.solve` (End x1, FOAM FATAL x0, 10000 `ExecutionTime` lines), `10000/{fluid,housing,core}/`, `log.yPlus.fluid`. Marker `mark_done_t23.py` (exit 0, four `DONE.*` markers, selftest PASS 0 failed under `python3` and `python3 -O`); grader `analyse_t23.py` (selftest PASS 0 failed under both); output `T23_GRADE.txt` / `T23_GRADE.json`. Queue entries `verification/queue/heat-transfer/launched/T23_P305_U*.json`; launch rows in `verification/queue/LAUNCH_LOG.tsv` at 17:33:02Z / 17:34:07Z / 17:35:12Z / 17:36:17Z, pids 196456 / 196910 / 197744 / 198194. **FOUR FINDINGS REPORTED BESIDE THIS ROW; TWO ARE BOOKKEEPING AND NEITHER VOIDS THE PHYSICS (L-342; Sanaa's universal rule 2026-08-26, bookkeeping never voids physics).** **(1) THE QUEUE RUNNER CLOBBERED THE WRAPPER'S OWN `STATUS` FILE AGAIN — SECOND OCCURRENCE, NOW ACROSS FIVE CASES.** `run_t23.sh` writes `rc` INSIDE the wrapper on the line immediately after the solver call, with `wall_s`, `ranks`, `core_min`, `cap_core_min`, `timeout_s`, `capped` and `solver`; the runner's own `echo ... > STATUS.<case_id>` lands last, so **0 of those 7 fields survive** [MEASURED] and what remains is `launcher_rc=0 end=<utc> note=exit-status-of-the-launch-argv-NOT-the-solver-rc`. **`rc = 0` IS THEREFORE DERIVED FROM `log.solve` AND NEVER READ**: exactly one `End` line, ZERO `FOAM FATAL`, last time == `endTime` 10000. `launcher_rc` was NOT accepted as `rc` even at 0, because a zero there is the shape of the `setsid` trap the wrapper exists to avoid; the marker prints `UNEVALUABLE FROM STATUS` and `rc_source=DERIVED-FROM-LOG` on every line, and its selftest drives three NEGATIVE arms (`launcher_rc=0` with a `FOAM FATAL`, with no `End`, with last time 9000 — all NOT DONE), because a derivation that cannot return NOT DONE is not a derivation. **Corroboration, offered as corroboration and not as the basis:** the entry's `launch_cmd` is `["bash", ".../run_t23.sh"]` and that wrapper ends `exit "$rc"`, so `launcher_rc` DOES propagate the solver status through this argv — the same reading the T22 row made — and two independent paths agree on `rc = 0`. **`queue_runner.py` is CFD'S INSTRUMENT; the clobber is ESCALATED, NOT REPAIRED by this team.** **(2) NO `START.<case>` FILE EXISTS FOR ANY OF THE FOUR, AND `run_t23.sh` CONTAINS NO CODE TO WRITE ONE** [MEASURED] — a divergence between section 5.4's registered text (*"Each launcher writes a `START.<case>` file BEFORE the solver starts, carrying `start_utc`, all three `/proc/loadavg` windows and `nproc`"*) and the launcher actually used. **Section 5.4's refusal condition — a saturated box produces a COST but NOT a calibration row — was therefore evaluated from a NAMED SUBSTITUTE, the queue runner's own `runner.log`, whose four readings are in the attribution column; the box was not saturated (worst 25.7 %), so this row is admissible.** The frozen document is not edited (rule 6) and the launcher is not retro-fitted. **(3) PHYSICS, NOT BOOKKEEPING — THE REGISTERED CONTINGENCY OF SECTION 6.2 HAS FIRED ON ALL FOUR POINTS.** Taken on the temperature RISE above `T_inf` = 288 K (common to model and solve, so it carries no information about either), the section 2 lumped series-resistance model overpredicts by **DB 3.541 / 3.369 / 3.280 / 3.235** and **FP 2.031 / 1.947 / 1.911 / 1.896**, BOTH beyond the registered 1.763x spread between the two closures on every point [MEASURED] — so the miss is not an artefact of choosing between them. **Section 6.2 registered this outcome one-way and before compute: the LUMPED MODEL is the thing falsified, and it CHANGES B1, B2 AND B3 NOT AT ALL.** Consequence recorded rather than acted on: section 2.5's flag arithmetic (2 of 16 DB, 0 of 16 FP) was DERIVED from that same model, so on the solved physics the rescaled levels would flag ZERO of 16 — **a later registration's figure to choose, not this row's, and section 2.5's committed numbers are not rewritten.** **(4) PHYSICS — MAX y+ ON THE HOUSING EXCEEDS 1 AT U >= 30 m/s**: 0.4037 / 0.7515 / **1.079** / **1.397** on `fluid_to_housing` [MEASURED, `chtMultiRegionSimpleFoam -postProcess -func yPlus`, run in scratch copies so no graded artefact was written]. Section 4.4 registered exactly this: y+ is MEASURED AND REPORTED and NOT a gate; it BLOCKS the correlation tier and the map's triple — neither of which this rung claims — and **does not void the physicality rows**. **A LIVE FALSE ZERO WAS CAUGHT AND REFUSED ON THE WAY (`CLAUDE.md` rule 3): the generic `postProcess -func yPlus -region fluid` exits rc 0 and prints "Unable to find turbulence model in the database: yPlus will not be calculated" and then `y+ : min = 0, max = 0, average = 0` on every patch. A reader parsing only the second line reports y+ = 0 on the housing wall of every case in this family. The zeros were REFUSED, not read; the grader carries that guard with BOTH limbs driven in its selftest.** **A FIFTH FINDING, AGAINST THIS LANE'S OWN FIRST SWEEP AND RECORDED BECAUSE IT WOULD HAVE MISLED THE BOX-REFILL SURVEY: a sweep for built-but-unsolved cases keyed on the absence of `log.solve` returned 30 candidates, and 26 of them were FALSE — the older F14 roots (`K0c`, `K0cT`, `K2e`) name their solver log `log.buoyantBoussinesqSimpleFoam` and carry solved time directories. Only the four HELD T19 cases survive the correction. A single-filename heuristic is a census defect, and it is disclosed rather than quietly fixed.** All findings **spawn no rule and no tool** (Sanaa's plumbing freeze). |
| C-20260832 | 2026-08-31 | cfd | **JF1 F1-F3 C-TOPOLOGY MESH LADDER — mesh only, no solver.** Registered §12 F1–F3 **4.50** core-min; measured **0.310** core-min (3 builds + 3 `checkMesh`, 1 rank, 18.61 wall s). **Ratio actual/predicted 0.069.** Attribution **misprediction**, not contention and not waste; **waste 0.000**. Tuning sweeps a further **≈1.5 core-min ESTIMATED** and labelled so, not measured. Dollars **DERIVED** at $0.0513/core-h: $0.00027 registered item, $0.0016 lane total — derived, never measured; this box cannot read its own billing. Status `PENDING` — meshes are not results. |
| C-20260833 | 2026-08-31 | cfd | **F28 L1 FEASIBILITY PAIR (`dp0`/`dp1000`, `_A2`) — the SIGFPE is gone and the runs still do not converge.** Predicted **20.0** core-min (2 × §10's 10.0); actual **82.56** core-min measured from each `RUN_STATUS` `wall_s` × 4 ranks; **ratio 4.11**; **waste 0.000**. **The misprediction and the non-convergence are ONE fact seen twice:** §10 assumed convergence well before the 15,000-iteration rigor cap, and both arms instead spent the full cap without converging — `p`'s gating initial residual flat at ~0.5 with zero decay, and the registered absolute criterion `\|dT\| < 0.1 %` failing at **18.11 %** peak-to-peak. First measured rate for this case: **24.8 iterations/wall-s at 4 ranks**, so an L1 arm costs ~41 core-min, not 10. Dollars **DERIVED** $0.0706. **FEASIBILITY — scores nothing, no verdict; not drawn against the 400 core-min gated cap, which stays unspent.** |
| C-20260831T184937.930165Z-4c5c5707 | 2026-08-31 | cfd | **CORRECTION ROW, NAMING THE ROWS IT CORRECTS: `C-20260832` AND `C-20260833` CARRY MALFORMED IDS.** Their CONTENT stands and is not re-stated here; only their ids are defective. Cause, stated so it is not repeated: the supervisor minted them `max+1` under CLAUDE.md rule 11's counter, using a regex `C-(\d+)` which read the newest id `C-20260831T183346` as `20260831` and added one — producing two ids that look sequential, sort wrongly against the timestamp series, and belong to no scheme. **Sanaa retired counters on 2026-08-31** (*"no more counters … tool-allocated at append time from timestamp+hash — no sequential numbers anywhere, so collision and stale-tail are impossible, not policed"*), and `scripts/append_record.py --allocate-id` is the allocator. **Rule 11 is superseded for id minting and the supervisor applied the retired rule.** Not struck in place: an in-place strike breaks the append-only prefix invariant that `append_record.py` enforces — it refused the attempt, fail-closed and correctly, which is how this correction came to be an append instead. |
| C-20260831T185058.680499Z-3a2ae922 | 2026-08-31 | ansys-verification | **VMFL006-R2 GRADED `PASS` AND CLOSED — Multicomponent Species Transport in Pipe Flow (VM2026R1 pp.27–28).** Re-run succeeding R1's row #49 `NOT A RESULT`; R2's one substantive change (a convergence clause rebuilt SATISFIABLE, endTime 3000 → 8000, RES_FLOOR unchanged at 1e-9) graded all three levels CONVERGING and the case `PASS`. Verdict read from `GRADING_VMFL006.json`, never stdout. | EXTRAPOLATED bracket **4.1–5.7 core-min** (point 4.4 filed as the queue estimate), IN-FAMILY from R1's MEASURED per-level cost scaled ~linearly in iterations (same case, same mesh, same solver, only endTime 3000 → 8000; C-199-style in-family, no cross-family transfer claimed); cap 18 core-min. `PREREGISTRATION.md` §15. | **4.8833 core-min MEASURED** (L1 12 s / 0.200 + L2 45 s / 0.750 + L3 236 s / 3.933; RANKS 1; read from per-level `RUN_RC.txt` and `COST.txt` total_core_min 4.8833). **$0.004176 DERIVED** at $0.0513/core-h (REPORTED-BY-OWNER — the box cannot read its own billing, `COMPUTE_BUDGET_CHARTER.md` §5). | = gross (no level exceeded 3600 wall s; max wall 236 s, so the stall rule matches nothing). Cap utilisation **27.1 %** of the registered 18. | **1.110 actual/point** (4.8833 / 4.4). Landed INSIDE the registered bracket 4.1–5.7, about 0.855 of the way up it. | **MISPREDICTION, upward, and named not laundered.** The whole 11.0 % gap is that the linear-in-iterations scaling slightly under-estimated L3: R1's L3 was 75 s at 3000 iters, so a pure linear 8000 gives 200 s, but the actual was 236 s (per-level ratio 1.18) — the later iterations were NOT cheaper than the earlier ones as strict linearity would imply. L1 (12 s vs 13.3 s estimate) and L2 (45 s vs 48 s) came in slightly UNDER. **WASTE: 0.0 core-min** — all three levels ran to completion and graded on the FIRST launch, no re-run, no stall; named separately and NOT folded into the ratio (charter §6). CONTENTION not separately measured this run (CONTENTION.txt written at launch); the miss is small enough that no contention term is needed to explain it. **The transferable finding: an in-family iteration-count extrapolation from a MEASURED same-case anchor (R1's own run) landed within 11 % and inside its own bracket — the FIRST time this case had a measured anchor to predict from (R1's row #49 had none; its 0.8–8.4 bracket was cell-step-based and hit 0.355×).** | `verification/runs/ansys_verification/VMFL006-R2/RESULTS.md` §6; per-level `RUN_RC.txt`; `COST.txt` (total_wall_s 293, total_core_min 4.8833); `GRADING_VMFL006.json` (verdict `PASS`, blob 90535fae). Prereg frozen `c067c56f`; comparator `3c8f4bbc`. A fresh row for a fresh run; R1's calibration row (2026-08-31, ansys-verification, VMFL006 1.6333 core-min `NOT A RESULT`, cited by date and subject not id) stands unchanged. |
| C-20260831T195247.871530Z-5dbe03c5 | 2026-08-31 | dafoam | SO-1cR np-invariance at the optimum (scotch vs simple at np=4), full 5-arm chain, item verdict PASS both rows | 40.6 core-min registered at freeze 100da1e7 (band [22.0, 80.0], cap 125.0) | 16.435 core-min MEASURED (HOST frame, chosen by the grade object as the LARGER of the two frames; MESH 0.167 + Ns-P/Ni-P/Ns-S/Ni-S 4.067 each) = $0.01405 DERIVED at $0.0513/core-h, NOT MEASURED | 16.435 core-min — cleaned equals gross; waste 0.000, and SO-1c's 0.633 core-min is NOT folded in here (named separately as waste on its own row's item) | 0.4048 | MISPREDICTION, and it is INHERITED rather than this item's: the registered MESH point had been raised 0.167 -> 0.633 on SO-1c's dead-run datum, and the real MESH cost 0.167 again, so the dead run's figure was the outlier and not the model. Every solver arm landed at 13.6 % of its 30.0 cap. NO CONTENTION TERM IS CLAIMED: the box carried no sibling dafoam item during the chain. Registered prediction P_I_total_core_min_band is recorded as a MISS — we came in UNDER the band, booked as a miss rather than quietly enjoyed. | 3610f4ac (verdict, docs/LAB_STATE.md S-22h); grade object CURRICULUM-SO1cR-a1-naca0012-dragmin-npinv/SO1cR_grade_20260831T175758Z.json |
| C-20260831T195247.871598Z-a44a49da | 2026-08-31 | dafoam | SO-3a alpha-multipoint gradient rung, chain STOPPED at arm 2 of 5, item verdict NOT A RESULT | 22.0 core-min registered at freeze 1a06a7d6 (caps summing to 115.0) | 0.334 core-min MEASURED (MESH 0.167 rc=0 + X-S 0.167 rc=2) = $0.0003 DERIVED at $0.0513/core-h, NOT MEASURED | 0.334 core-min — cleaned equals gross; the whole 0.334 is WASTE, since the chain produced no gradeable output | REFUSED — NO RATIO IS COMPUTED AND THE REFUSAL IS THE HONEST ENTRY. The registered 22.0 prices FIVE arms; the measured 0.334 bought ONE completed arm and one refusal. 0.334/22.0 = 0.0152 would read as a 98.5 % underspend while describing a chain that never ran, so it is named here PRE-EMPTIVELY so nobody computes it later. The honest per-arm figures are MESH 0.167 against a 5.0 cap and X-S 0.167 against a 15.0 cap. | NOT A MISPREDICTION AND NOT CONTENTION — INSTRUMENT. so3a_xf.py:111 carried its own producer pin at the fail-closed sentinel UNSET-PRODUCER-PIN-SENTINEL-FAILS-CLOSED, never filled, so the arm refused rather than running against an unverified producer. THE PIN WORKED; the md5 it rejected was in fact correct. The pin census reported 13 of 13 because it counts md5-SHAPED constants and this sentinel is deliberately not md5-shaped — the property that makes it safe against false matching made it invisible to the census. Successor SO-3aR carries a census that sweeps BY ROLE rather than by shape. | e64e6e0d (verdict, docs/LAB_STATE.md S-22l); CURRICULUM-SO3a-a1-naca0012-alpha-multipoint-gradient/STATUS.chain and X-S_20260831T184310Z_277867.log |
| C-20260831T195952.923534Z-ca60f6bd | 2026-08-31 | heat-transfer | **T20 lumped-capacitance transient control — RUNG GRADED `NOT A RESULT`.** Comparator frozen at `e4877567` and run against the live root: exit 3, no row graded, no gate json written. Six of the seven registered cases ran and hold all six conjuncts of rule 4; `T20_LC_P10`, V5's only case, was never built, and section 12 registers that a refusal there makes the WHOLE RUNG `NOT A RESULT`. Rule 12's comparison is owed at process completion whatever the verdict, and is made over the six cases that actually consumed compute | **1.7975 core-min REGISTERED** for the six built cases (section 11.2 per-case POINT: c 0.0771, m 0.1042, f 0.1584, Sc 0.1558, Sf 0.1684, D 1.1336). Whole-rung POINT 1.9559 core-min including the unbuilt P10's 0.1584; hard CAP 16.0 core-min. $0.001537 six-case and $0.01368 at cap, **DERIVED NOT MEASURED** at $0.0513/core-h (c7a.4xlarge, owner-stated; the box cannot read its own billing, COMPUTE_BUDGET_CHARTER section 5) | **0.235 core-min MEASURED** from the seven STATUS files committed at `b04ac809`, as wall_s x ranks / 60 at ranks = 1: c 0.017, m 0.017, f 0.017, Sc 0.017, Sf 0.050, D 0.117. = **$0.000201 DERIVED**. `T20_LC_P10` consumed 0.000 core-min because it was never built | **= gross.** No row is `capped`, every row's note is `clean`, and the longest is 7 wall s, so no row is near the charter section 2 3600-s stall rule and there is nothing to clean | **0.131x cleaned/predicted over the six built cases** (0.235 / 1.7975). Per case: c **0.220x**, m **0.163x**, f **0.107x**, Sc **0.109x**, Sf **0.297x**, D **0.103x**. Against the whole-rung POINT 0.120x, and 1.5 % of the hard cap. Under the estimate in every single case | **MISPREDICTION, conservative direction, concentrated in the exact term section 11.1 named as the registration's weakest figure.** Registered per-step cost at 240 cells = `t_overhead` 2.1e-03 **ASSUMED** + `rate` 2.80e-07 **BORROWED** x 240 = 2.167e-03 core-s/step. MEASURED from solver ExecutionTime on the two 240-cell endpoints (T20_LC_c 0.27 s over 750 steps, T20_LC_D 7.54 s over 30000 steps): slope **2.49e-04 core-s/step**, intercept 0.084 core-s, so the per-step term is **OVER-predicted 8.7x**. The cell rate misses the OTHER way: T20_LC_f 0.99 s against T20_LC_Sf 3.34 s at equal steps gives **1.09e-06 core-s per cell-step**, so the BORROWED 2.80e-07 **UNDER-predicts 3.9x**. Subtracting, the measured per-step overhead is indistinguishable from zero against 2.1e-03 ASSUMED — the assumed overhead is essentially the whole miss, which is what the rung existed to calibrate. **Section 11.3's promise that `T20_LC_c` would be the clean reading for `t_overhead` IS NOT DELIVERABLE:** that case's entire run is 1 wall s against 1-s STATUS granularity, so the decomposition above rests on `log.solve` ExecutionTime (on disk, NOT committed — the six logs total 20 MB) rather than on a committed record, and that resolution limit is itself the calibration finding. No contention: the six ran sequentially 16:14 to 16:45 UTC, each note=clean. **WASTE NAMED SEPARATELY AND NEVER FOLDED INTO THE RATIO: <= 0.017 core-min** — 0.000 core-min MEASURED for the failed first `T20_LC_c` (rc 1, 0 wall s, died on the missing `constant/g` before doing any work; its build steps are not instrumented in that STATUS file and are stated ABSENT, not approximated) plus ~0.017 core-min DERIVED for the un-STATUS-ed feasibility tree `T20_LC_FEAS_20260831T151828Z`, whose `log.solve` carries an End line, 750 ExecutionTime lines and ClockTime 1 s | prereg frozen `7b93b2c8` sections 11.2 and 11.3; comparator freeze `e4877567`; STATUS artifacts `b04ac809`; registration transcription `c3b911d1`; `verification/runs/T-family/T20_runs/STATUS.*` |
| C-20260831T200523.489392Z-fada02a7 | 2026-08-31 | cfd | **F28 FARFIELD BC-VARIATION PROBE (`FEAS_L1_dp1000_U20_A2_BCPROBE`) — READING C, hypothesis FALSIFIED.** Predicted **42.0** core-min; actual **40.2** (603 wall s × 4 ranks ÷ 60); **ratio 0.9571**. Attribution **conservative misprediction** — the estimate took the top of the measured 23.93–24.83 it/wall-s band and the run made 24.88 *including* `decomposePar` despite three concurrent jobs, so contention was negligible. **Waste, named separately: 0 core-min** — the aborted first attempt died at `phase=guards` before `decomposePar` and spent no solver time. Dollars **DERIVED** at $0.0513/core-h: $0.034 actual vs $0.036 predicted, never measured. Enforced ceiling 100 core-min, 59.8 unused. **FEASIBILITY — scores nothing, no verdict; F28's 400 core-min gated cap untouched.** Result: thrust stationarity **20.2256 %** vs baseline 18.1149 % against a registered 0.1 % — the probe is marginally **worse**, so the free outer mass flux is **not** the cause of the stall. |
| C-20260831T200805.169708Z-f0b54f1b | 2026-08-31 | ansys-verification | **VMFL038-R2 — Falling Film Over an Inclined Plane, GRADED `PASS` on both limbs, ladder closed.** Three isotropically refined levels of `simpleFoam`, 1 800 / 7 200 / 28 800 cells at `endTime` 8 000 / 16 000 / 32 000, RANKS 1, all three `rc = 0` at `Time == endTime` under the strict completion rule. The row exists because rule 12 owes a calibration at every process completion — **and it carries an OVER-prediction, which this ledger has fewer of than over-runs and which is worth exactly as much.** | **BRACKET 17.0 – 33.8 core-min**, CAP 90 core-min running total, `timeout` 5400 / 5389 / 5314 s, RANKS 1 [REGISTERED, `PREREGISTRATION.md` §7.2, frozen at `9a6aea9b` before compute]. Basis: R1's MEASURED per-cell-per-iteration cost from its own `RUN_RC.*` — 9.29e-7 / 1.028e-6 / **9.69e-7 s** — applied to R2's 1.051e9 cell-iterations gives the LOWER end; the UPPER end prices **1.928e-6 s**, the rate this lane measured end-to-end at 28 800 cells during the pre-freeze probe while two peer 4-rank jobs held 8 of 16 cores. Labelled **EXTRAPOLATED, NOT MEASURED** in the frozen bytes: the rates are measured, the R2 wall time was not. | **12.2333 core-min MEASURED** — L1 0.1833 (11 wall-s), L2 1.2500 (75 wall-s), L3 10.8000 (648 wall-s); all three `rc=0`, `ranks=1`, no `timeout` fired, no `CAP_OVERRUN.txt` written. $0.010459 DERIVED at $0.0513/core-h (REPORTED-BY-OWNER, not measured — the box cannot read its own billing, `COMPUTE_BUDGET_CHARTER` §5). Read from `COST.txt` (`total_core_min = 12.2333`) and the three `RUN_RC.<level>` files. | **12.2333 — gross and cleaned are the SAME figure.** WASTE IS ZERO and is named rather than assumed: no level re-run, no non-zero `rc`, no cap or timeout event, no stall (the longest level ran 648 wall-s against the 3 600-s stall threshold). Cap 90 core-min, **13.6 % used**. | **0.720 against the LOWER bracket end (12.2333 / 17.0); 0.362 against the upper.** The run finished **BELOW** the registered bracket. | **MISPREDICTION, in the conservative direction — NOT waste and NOT contention.** Two causes, both measured, neither absorbed into the ratio. (1) **The lower bracket priced R1's own measured 9.69e-7 s per cell-iteration; R2 ran at 7.03e-7 s at the same 28 800 cells — 1.38x faster on the same box, same solver, same rank count.** The isotropic mesh is better conditioned per cell than R1's AR-16 mesh, which is the same property that made R2's triple gradeable. (2) **The upper bracket priced peer contention that had cleared by launch** — the two 4-rank jobs live during the 19:00Z probe had finished by 19:51Z. **THE LEDGER LESSON, which is this row's whole point: a bracket built from a PRIOR MEASUREMENT on a DIFFERENT MESH SHAPE inherits that shape's conditioning, and R1's anisotropic mesh was the slow one. A basis naming a prior measurement is still the best predictor this ledger has — but 'prior measurement' should mean prior measurement OF THE SAME MESH FAMILY.** Separately named and NOT folded into any figure above (`COMPUTE_BUDGET_CHARTER` §6): the pre-freeze diagnostic probe cost **≈60 core-min of SCRATCH compute**, disclosed in the frozen §0.1, and it bought the conservation proof that saved the case. Total compute attributable to VMFL038-R2 is ≈72 core-min, ≈$0.062 derived. | freeze `9a6aea9b`; queue `04c8349a`; `verification/runs/ansys_verification/VMFL038-R2/RESULTS.md` §5; `verification/runs/ansys_verification/VMFL038-R2/COST.txt`; `GRADING_RECORD_2026-08-31T2003Z.json` |
| C-20260831T204530.092810Z-e03cf43f | 2026-08-31 | dafoam | **SO-3aR — A1 NACA0012 alpha multipoint gradient. Chain DEAD at arm 2 of 5; item verdict `NOT A RESULT`.** The row exists because rule 12 owes a calibration at every process completion, and a DEAD item is a completed process: the question a calibration ledger has to answer honestly is not only "did we price the work right" but "is a small ratio even *about* pricing". Here it is not. Arms `MESH` rc=0 and `X-S` rc=1 ran; `F-S`, `X-P`, `F-P` never started a container. | **22.0 core-min** point, band **[14.0, 60.0]**, ceiling **115.0 = Σ per-arm caps** (MESH 5.0, X-S 15.0, X-P 15.0, F-S 40.0, F-P 40.0) [REGISTERED, `PREREGISTRATION.md` §9 and §4, frozen at `977f3d9b` BEFORE any compute]. Registered dollars $0.01881 point — **DERIVED there too, never measured**. | **1.800 core-min MEASURED** — `MESH` 0.267 (16 wall-s × 1 rank ÷ 60) + `X-S` 1.533 (92 wall-s × 1 rank ÷ 60), read from the run root's `ledger.txt`, both arms `ranks=1`, `IMG=dafoam/opt-packages:latest` digest `sha256:9d45679d…f07fc`. $0.001539 **DERIVED** at $0.0513/core-h (c7a.4xlarge, **REPORTED-BY-OWNER, NOT MEASURED** — the box cannot read its own billing, `COMPUTE_BUDGET_CHARTER` §5). | **1.800 — gross and cleaned are the SAME figure.** The 3600-s stall rule matches nothing: the longest arm ran **92 wall-s**. No cap breached (MESH 5.3 % of 5.0; X-S 10.2 % of 15.0), no in-container `timeout` fired (deadlines 300 s / 900 s), no arm re-run. Ceiling 115.0, **1.6 % used**. | **0.082** (1.800 / 22.0) | **TRUNCATION AT ARM 2 OF 5 — NOT MISPREDICTION, and that distinction is this row's whole point.** The per-arm rates were fine and are the evidence for it: both arms that ran finished far inside their own registered caps, so nothing here says the lab priced the arms badly. The ratio is 0.082 because **three of five arms never started**, and reading that as an over-prediction would launder a crash into an estimating success — the ledger would then record this lab as *better* at estimating on the day an item died. Cause of the truncation, verbatim from `X-S_20260831T202041Z_376689.log:1868`: `pyDAFoam Error: /mnt/X-S/0.0001 already exists, moving failed!`, raised by `'point1.coupling.solver'` at `pyDAFoam.py:1543` in `renameSolution`. **ROOT CAUSE IS A LAB REGRESSION, NOT AN UPSTREAM DEFECT**: `so3ar_runScript.py` gives each of point0/point1/point2 its own `DAFoamBuilder` (line 245) but **no per-point `run_directory`**, so three `DASolver`s rename into one `/mnt/X-S/0.0001`; A2's D6R had already solved exactly this at `d6r_opt_runScript.py:59` (`RUN_DIRS = {"cl04": "mp04", …}`, used at line 138) and documented it in its own docstring lines 6-8, and A1 did not carry it forward. **WASTE, NAMED SEPARATELY AND NOT FOLDED INTO THE RATIO** (`COMPUTE_BUDGET_CHARTER` §6): the entire **1.800 core-min is waste on a failed attempt** — it bought three converged primals (`J = 0.02180598162892116`) and one completed adjoint, but no row, no gate and no verdict. **Separately again and excluded entirely**, as the frozen §9 requires: SO-3a's prior **0.334 core-min** is never absorbed into SO-3aR's ratio. | freeze `977f3d9b`; addendum `fc9cdf0f`; `cases/dafoam/ladder-a/A1/curriculum_SO3aR/RESULTS.md` §3 and §7; run root `/home/ubuntu/certonomous-runs/CURRICULUM-SO3aR-a1-naca0012-alpha-multipoint-gradient/` (`ledger.txt`, `STATUS.chain`, `SO3aR_STOP_MARKER.json`, `SO3aR_grade_20260831T202215Z.out`) |
| C-20260831T210913.811885Z-2663ea60 | 2026-08-31 | heat-transfer | **T24 — Case 3 motor-in-duct CHT, THE TWELVE POINTS T23 DEFERRED: GRADED `PASS` x12, RUNG CLOSED.** Twelve `chtMultiRegionSimpleFoam` cases `T24_P{080,155,230}_U{10,20,30,40}`, OpenFOAM v2606, 39,680 cells each (35,200 fluid + 3,360 core + 1,120 housing), 5-degree wedge, two conjugate `mappedWall` interfaces, **serial RANKS = 1 on every case, so core-min = wall-min throughout this row**. All twelve reached `Time = 10000` == the registered `endTime`, one `End` line each, `ExecutionTime` count 10000 each, `launcher_rc = 0` on all twelve. **ZERO CASES CAPPED** — the per-case cap is ENACTED as `timeout 2700s`, the longest case ran 2345.12 s, and no `CAP_OVERRUN` artefact exists anywhere under the run root. Registration frozen `b9057489`, comparator frozen `d9082bfb`, verdict artefacts `cc5f1af4`. **THE PURPOSE OF THIS ROW, REGISTERED BEFORE COMPUTE AT §5.5: the figure T24 exists to calibrate is not the per-cell rate — T23 measured that — but whether that rate SURVIVES A THREEFOLD INCREASE IN CONCURRENCY (four-concurrent to twelve-concurrent on one 16-core box). IT DOES NOT, AND THE AMOUNT BY WHICH IT DOES NOT WAS PREDICTED CORRECTLY BEFORE ANY CASE LANDED.** | **SUBSET POINT 360.4 core-min** (DERIVED, 12 x 30.0321 = 360.3855) and **per-case POINT 30.0321 core-min** (MEASURED, the mean of the four `T23_P305_U*` `ExecutionTime` figures); **SUBSET CAP 540.0 core-min hard** and **per-case CAP 45.0 core-min hard**, ENACTED as `timeout 2700s` at 1 rank inside each case's own launcher. Quoted from `docs/campaigns/T-family/T24_PREREGISTRATION.md` **§8** (*"SUBSET POINT 360.4 core-min, SUBSET CAP 540.0 core-min hard, per-case CAP 45.0 core-min hard, ENACTED as `timeout 2700s`"*, `:268-269`) and the **§5.2** POINT/CAP table (`:789-792`). USD at POINT **$0.3082**, at CAP **$0.4617**, both registered **DERIVED, NOT MEASURED**. | **457.0753 core-min MEASURED** = 27,424.52 `ExecutionTime` seconds summed over the twelve, **read from each case's own `verification/runs/T-family/T24_runs/T24_P*/log.solve` `ExecutionTime` line at 1 rank**, per §5.5's explicit instruction and §3.5a's explicit prohibition on `STATUS` — **and the prohibition earned its keep: the runner clobbered every `STATUS.<case>` to a single `launcher_rc` key and the `core_min` field is DESTROYED in all twelve, so the registered source was the only surviving one.** Independently cross-checked against the frozen comparator's own per-row `core_min` / `execution_time_s` in `gate_t24.json`: **the two paths agree to 457.075333 core-min on the sum and case-by-case on all twelve**. Per case 37.2587–39.0853 core-min (mean 38.0896, spread 4.9 %). Dollars **$0.3908 — DERIVED, NOT MEASURED**, at the owner-stated c7a.4xlarge $0.0513/core-h (REPORTED-BY-OWNER 2026-08-21/22): **the box cannot read its own billing** (`COMPUTE_BUDGET_CHARTER.md` §5). | **457.0753 core-min — cleaned == gross, and that is a measurement, not an omission.** The charter §2 stall rule matches a row over **3600 wall s**; the longest T24 case is **2345.12 s**, so **zero of twelve rows match it** and nothing is subtracted. **WASTE, NAMED SEPARATELY PER CHARTER §6 AND NEVER FOLDED INTO THE RATIO: 0.000 core-min.** No restart, no aborted attempt, no capped case, exactly one `log.solve` per case directory and no orphan solver log anywhere under the run root [MEASURED]. **ONE FIGURE IS STATED AS ABSENT RATHER THAN APPROXIMATED (append rule 2): the mesh and post-processing utility compute** — `blockMesh`, `splitMeshRegions`, three `checkMesh`, `yPlus` — **is NOT QUANTIFIED, because those logs carry ZERO `ExecutionTime` lines** (grep count 0 in every one) **and no other timing artefact records them.** It is outside the registered POINT as well, which prices `39,680 cells x 10,000 iterations` of solve and nothing else, so the comparison is like-for-like; but the run's true total spend is this 457.0753 **plus an unmeasured utility increment**, and saying so is cheaper than a number nobody can cite. | **1.2683** (457.0753 / 360.4 registered subset POINT; 1.26828 against the unrounded 360.3855). Identically **1.2683** per case (38.0896 / 30.0321), as it must be — twelve cases of one geometry. **SUBSET CAP UTILISATION 84.64 %** of the registered 540.0, leaving 82.92 core-min unused. **Worst single case `T24_P155_U30` at 39.0853 core-min = 86.86 % of the 45.0 per-case cap**, its closest approach; the cap was never reached and never extended. **THE RATIO IS NOT AN ESTIMATOR MISS AND IS NOT FILED AS ONE** — see the attribution column, which is where the 1.2683 is spent in full. | **ATTRIBUTION: CONCURRENCY CONTENTION. NAMED SEPARATELY, NEVER FOLDED INTO THE RATIO** (`COMPUTE_BUDGET_CHARTER.md` §6; T24 §5.3 instructs the same in advance). **Misprediction of the rate: none — the per-cell rate is T23's own MEASURED mean and is unchanged. Waste: 0.000 core-min. The entire 1.2683 is the concurrency penalty §5.3 registered as this cap's named risk before compute.** Measured: mean `ExecutionTime` **2285.3767 s** at twelve-concurrent against T23's four-concurrent mean **1801.9275 s** = **+26.83 %**; longest against longest, 2345.12 s vs 1829.39 s = **+28.19 %**. **AND THIS IS THE LINE THE ROW EXISTS TO CARRY. The pre-compute note `verification/runs/T-family/T24_runs/T24_CONCURRENCY_CONTENTION_NOTE.md` (committed `d8262317`) BRACKETED THE PENALTY AT 26.7 % – 29.9 % BEFORE ANY CASE HAD LANDED, from two independent probes. THE OUTCOME IS +26.83 %, INSIDE ITS OWN BRACKET AND 0.13 POINTS OFF ITS LOWER EDGE. THAT IS THE BEST COST PREDICTION THIS TEAM HAS EVER FILED**, against a standing record in this ledger of a **3.25x** over-prediction (T19b stage 1) and a **6.12x** one in a supervisor's own brief — and it is filed loudly because a calibration ledger that only records misses teaches nothing about what made this one land. **THE NOTE ALSO FALSIFIED THE FRAMING IT WAS COMMISSIONED TO STATE, and the falsification is why the ~27 % is charged in full rather than discounted:** it was put to that lane that core-minutes as `wall_s x ranks / 60` *"penalises concurrency more than actual CPU consumption does"* — i.e. that the excess is a bookkeeping artefact of the unit. It is not. `cpu_s / wall_s` measured from `/proc/<pid>/stat` at 20:22Z is **0.9963 – 0.9974 per solver and 0.9950 aggregate**; wall-seconds and CPU-seconds agree to better than 0.5 %, so there is no gap for the unit to exaggerate. **The stall is real CPU time on a resident core — memory-bandwidth and shared-cache contention, with 0 processes in D-state and 27.3 GB MemAvailable ruling out I/O and memory pressure — so the same physics GENUINELY COSTS ~27 % MORE CORE-MINUTES at twelve-concurrent.** §5.3 registered, before compute, that *"a twelve-concurrent contention penalty above ~48 % caps a case"*; the measured 26.83 % sits **55.9 % of the way to that trigger**, which is why zero cases capped and why the 1.4984x headroom held. **THE §5.5 SATURATION EXCLUSION DOES NOT BITE AND ALL TWELVE ROWS ARE CALIBRATION-ELIGIBLE.** Its threshold is registered as a number — a 1-minute load average above `nproc` (16) at a case's own launch instant — and the twelve `START.<case>` files record **3.42 / 3.81 / 3.91 / 5.40 / 5.69 / 5.82 / 6.02 / 6.67 / 7.63 / 8.66 / 9.76 / 11.15** against `nproc` **16** [MEASURED, `T24_P*/START.T24_P*`]. **Maximum 11.15 < 16, so no case crossed it and none is excluded** — §5.5 expected *"to exclude some of the twelve by construction"* and, measured, it excluded none. **TWO CORRECTIONS TO THE FIGURES THIS LANE WAS HANDED, RECORDED BECAUSE RE-DERIVATION IS THE POINT OF RE-DERIVATION: (1) the launch loadavg sequence is NOT monotonic and its floor is NOT 5.40** — it dips to 3.42 at `T24_P080_U40` and 3.81 at `T24_P155_U10` before climbing to 11.15; the eligibility conclusion is unaffected because it turns only on the maximum. **(2) the `cpu_s/wall_s` band is 0.9950–0.9974 as the note itself reports, not 0.9912–0.9974**; the conclusion is likewise unaffected and is if anything slightly stronger. **A THIRD READING FROM THAT NOTE IS CARRIED FORWARD AS A WARNING TO THE NEXT ROW THAT TRIES THIS: `loadavg` OVERSTATES this contention and must not be used to size it** — it read **24.47** on a 16-core box at the instant only **11.9 cores** were genuinely consumed, roughly a factor of two. | Registration `docs/campaigns/T-family/T24_PREREGISTRATION.md` frozen at commit **`b9057489`** (§8 `:268-269`, §5.2 `:789-792`, §5.3, §5.5); comparator frozen at **`d9082bfb`**; verdict artefacts (twelve `DONE.T24_P*`, twelve `log.yPlus.fluid`, `gate_t24.json`) committed at **`cc5f1af4`**, 25 files, 2,624 insertions. Actuals re-derived for this row from `verification/runs/T-family/T24_runs/T24_P*/log.solve` and cross-checked against `verification/runs/T-family/T24_runs/gate_t24.json`. Contention evidence `verification/runs/T-family/T24_runs/T24_CONCURRENCY_CONTENTION_NOTE.md` at **`d8262317`**. Launch-instant loads `verification/runs/T-family/T24_runs/T24_P*/START.T24_P*`. T23 comparison basis: this ledger's own **T23 row** (the heat-transfer row headed *"T23 GRADED — THE FOUR REGISTERED CASES OF THE RESCALED CASE 3 MAP"*, cited by subject and not by id, because `scripts/append_record.py` REFUSED an earlier draft of this row for hand-writing a tool-allocated id and the refusal was correct — that form is the module's alone to mint) and `verification/runs/T-family/T23_runs/T23_P305_U*/log.solve`. **NO T24 ROW EXISTED IN THIS LEDGER BEFORE THIS ONE, MEASURED UNDER A LIVE CONTROL RATHER THAN ASSERTED: `T24` occurs 0 times in `git show HEAD:docs/COST_CALIBRATION.md` while the same reader on the same bytes returns `T23` 12 times and `T20 ` once** — a bare rung-substring search on this file is otherwise a known trap, since ISO row ids contain digit runs that a looser pattern matches. |
| C-20260831T213312.667302Z-6449b688 | 2026-08-31 | heat-transfer | **CORRECTION ROW. It corrects THIS LEDGER'S T24 ROW** -- the heat-transfer row headed *"T24 -- Case 3 motor-in-duct CHT, THE TWELVE POINTS T23 DEFERRED: GRADED `PASS` x12, RUNG CLOSED."*, **landed at commit `c667c10f`** -- **and every cost figure in that row stands.** **CITED BY SUBJECT AND LANDING COMMIT, NOT BY ID, AND THE REASON IS A TOOL CONSTRAINT WORTH RECORDING:** `scripts/append_record.py` REFUSES (exit 9) any rows text containing a tool-form id at all -- the smuggle guard at `:820-829` is a whole-text `re.findall`, with no exemption for a row CITING one -- **so a correction row structurally cannot name its target by id under append rule 1, and must name it by subject and commit instead.** That is the same route the corrected row itself took when citing the T23 row, and the refusal was correct then and is correct now: minting that id form is the module's alone. **REFERRED, NOT WORKED AROUND: the guard's lack of a citation exemption is reported to the module's declared owner (`verification-supervisor`, per its own OWNER annotation) as a gap between append rule 1 and the tool that enforces the file.** Filed under append rule 1 (*"an existing row is never edited; a correction is a new row naming the row it corrects"*). It corrects **two false statements and two false characterisations, all of them this team's own**, in the corrected row's *gap attribution* column. **Zero compute; no solver, no re-grade.** **NO VERDICT, GATE, BAND, THRESHOLD, CAP OR LABEL MOVES**, and the T24 rung remains `PASS` x12 with the rung closed. | **UNCHANGED, restated so this row is readable alone:** SUBSET POINT **360.4 core-min** (DERIVED, 12 x 30.0321) and per-case POINT **30.0321 core-min** (MEASURED, the mean of the four `T23_P305_U*` `ExecutionTime` figures); SUBSET CAP **540.0 core-min** hard, per-case CAP **45.0 core-min** hard ENACTED as `timeout 2700s`. Registration frozen `b9057489`. **This row moves no predicted figure.** | **UNCHANGED: 457.0753 core-min MEASURED.** RE-VERIFIED INDEPENDENTLY BY THIS LANE ON TWO PATHS, because a correction row that leaves the corrected row's headline unchecked is asserting, not verifying: the sum of `execution_time_s` over the twelve rows of `gate_t24.json` is **27,424.52 s = 457.075333 core-min** at 1 rank, and the sum of that same file's own per-row `core_min` field is **457.075333 core-min** -- the two agree to every printed digit. Dollars **$0.3908 DERIVED, NOT MEASURED** ($0.0513/core-h, REPORTED-BY-OWNER; the box cannot read its own billing, `COMPUTE_BUDGET_CHARTER.md` §5). | **UNCHANGED: 457.0753 core-min, cleaned == gross.** Longest case **2345.12 s** re-measured this lane against the charter §2 stall rule's **3600 wall s**, so zero of twelve rows match and nothing is subtracted. **WASTE, NAMED SEPARATELY PER CHARTER §6 AND NEVER FOLDED INTO THE RATIO: 0.000 core-min** -- unchanged. The utility compute (`blockMesh`, `splitMeshRegions`, `checkMesh` x3, `yPlus`) remains **STATED AS ABSENT, NOT APPROXIMATED** per append rule 2. | **UNCHANGED: 1.2683.** Subset cap utilisation **84.64 %**, worst single case `T24_P155_U30` at **86.86 %** of the 45.0 per-case cap -- all unchanged. **ONE THIRD-ORDER OBSERVATION, RECORDED RATHER THAN LEFT BECAUSE THIS ROW IS ALREADY OPEN:** the corrected row prints the headline **1.2683** and attributes it parenthetically to *"457.0753 / 360.4"*; that quotient is **1.26824** and rounds to **1.2682**. **1.2683 is correct against the UNROUNDED registered POINT 360.3855** (= 12 x 30.0321), which the same parenthetical also gives as 1.26828. **The headline ratio is right and does not move**; only its parenthetical attribution to the rounded 360.4 is off in the fifth significant figure. | **FOUR CORRECTIONS, ALL OF THEM THIS TEAM'S OWN ERRORS, EACH RE-MEASURED AT SOURCE BY A SECOND READER RATHER THAN REASONED FROM THE FIGURE BEING CORRECTED.** **(A) THE LAUNCH LOADAVG SEQUENCE -- AND THIS IS THE THIRD SUCCESSIVE ERROR ON ONE TWELVE-NUMBER SEQUENCE, WHICH IS THE POINT OF THIS ROW.** The corrected row says the sequence *"dips to 3.42 at `T24_P080_U40` and 3.81 at `T24_P155_U10`"*. **3.81 is a RISE from 3.42, not a dip.** [MEASURED, all twelve `T24_P*/START.T24_P*` files read in launch order by their own `start_utc`:] **5.40, 5.69, 3.91, 3.42, 3.81, 5.82, 6.02, 6.67, 7.63, 8.66, 9.76, 11.15.** There are **exactly TWO descending steps, both early**: 5.69 -> 3.91 into `T24_P080_U30`, and 3.91 -> 3.42 into `T24_P080_U40`, which is the **global minimum 3.42**. From `T24_P155_U10` (3.81) onward the sequence **rises monotonically** to 11.15. **THE THREE-ERROR HISTORY, STATED PLAINLY:** (1) it was first reported as *"rose monotonically 5.40 -> 11.15"* -- not monotonic, and 5.40 is merely the first value, not the floor; (2) that was *"corrected"* to *"dips at `T24_P080_U40` and `T24_P155_U10`"* -- the second of those is a rise; (3) **that second error is the one that got filed.** **THE CAUSE, RECORDED AS A CAUSE AND NOT AS AN APOLOGY: each correction was reasoned from a number already handled instead of re-derived from the twelve source files, so each pass carried forward the previous pass's frame and introduced a fresh error inside it.** **THE PROCEDURAL FIX THIS ROW OFFERS THE NEXT LANE: a claim about the SHAPE of a sequence is re-derived from ALL of its source artefacts in ONE pass, in a stated ordering, and is then stated as the FULL sequence followed by the derived shape -- never as a shape alone, because a shape cannot be checked without the numbers and a wrong shape looks exactly like a right one.** **(B) THE `cpu_s/wall_s` BAND IS A SPLICE OF TWO DIFFERENT QUANTITIES, AND THE EARLIER CORRECTION WAS A DIFFERENT SPLICE.** `T24_CONCURRENCY_CONTENTION_NOTE.md:72-73` reports **two distinct measurements** from `/proc/<pid>/stat` at 2026-08-31T20:22Z over the twelve concurrent solvers: **`cpu_s/wall_s` PER SOLVER = 0.9963 - 0.9974, a RANGE over twelve solvers**, and **AGGREGATE OVER ALL TWELVE = 0.9950, a SINGLE SCALAR** corresponding to ~11.9 of 16 cores genuinely consumed. **"0.9950-0.9974" fuses the aggregate scalar to the upper edge of the per-solver range and is a range of nothing that was measured.** **A FINDING NOT IN THE BRIEF THIS LANE WAS HANDED, AND IT PARTLY EXCULPATES THE FILED ROW: the splice ORIGINATES IN THE NOTE ITSELF at `:93`** (*"For a 1-rank job, `cpu_s / wall_s = 0.9950`-`0.9974`"*), **so the filed row's *"0.9950-0.9974 as the note itself reports"* is a literally accurate quotation of an already-spliced line, not an invention.** The note's §3 table is correct and its §4(b) prose spliced it; **and the filed row's own attribution column states it CORRECTLY elsewhere** (*"0.9963 - 0.9974 per solver and 0.9950 aggregate"*) -- **the row is right in one cell and spliced in another, which is how the error survived reading.** **THE EARLIER "0.9912-0.9974" WAS A THIRD SPLICE AND IS WITHDRAWN, NOT RESTATED:** the 0.9912/0.9966 half of it comes from an ad-hoc twelve-case probe with **no committed artefact** [MEASURED: neither figure occurs anywhere in this repository's records], and **append rule 2 requires a figure not in the record to be stated as absent rather than approximated**, so it is named as withdrawn and uncitable and is not reproduced here as a measurement. **CORRECT STATEMENT: per-solver 0.9963-0.9974 (range, twelve solvers, 20:22Z); aggregate 0.9950 (scalar, same instant).** **The conclusion is unchanged and is if anything cleaner: wall-seconds and CPU-seconds agree to better than 0.5 % on every reading, so there is no gap between the two for `core-minutes` to exaggerate, and the ~27 % is a real cost increase.** **(C) THE 26.7 % - 29.9 % IS A MEASUREMENT, NOT A PRE-COMPUTE BRACKET AND NOT A PREDICTION.** It was repeatedly described by this team as a *"pre-compute bracket"* that *"BRACKETED THE PENALTY"*, which reads as a registered gate. **It is not one.** The note titles itself *"a MEASUREMENT, for the calibration row to cite"* at `:1`, states at `:3` that it *"proposes no new rule, no new tool and no new unit, and it asks for none"*, states at `:13-14` that it *"grades nothing, marks nothing and assigns no verdict"*, and states the figure at `:60` as **THE MEASUREMENT**. **It was never pre-registered, and calling a measurement a prediction is how a lab accidentally credits itself with a forecast it never froze.** **WHAT SURVIVES, AND IT IS STILL WORTH SOMETHING: the measurement was taken before any case landed**, so the **+26.83 %** outcome falling inside it is a genuine agreement of **two independent measurement paths on the same box**, which is a cross-check -- not a scored prediction, and not a calibration hit. **THE `VERIFY` TAG ON THAT ORDERING IS DISCHARGED BY THIS LANE, NOT CARRIED [MEASURED]:** PROBE A ran at **2026-08-31T20:19:31Z** and the `/proc` probe at **20:22Z**; the **earliest of the twelve landings** is `T24_P080_U10` at **2026-08-31T20:38:50Z** (its own `start_utc` 20:01:19Z plus `execution_time_s` 2251.46, corroborated by that case's `log.solve` mtime 20:38:58Z) and the latest is `T24_P230_U40` at **20:53:49Z**. **Both probes precede every one of the twelve landings by at least 16.8 minutes.** **(D) THE PLANTED-ZERO CONTROL WAS DESCRIBED IN A WAY THAT READS AS BIT-IDENTITY, AND IT IS NOT.** It was stated that the plant was *"seen at 1.234e-03 on both readers x12"*. [MEASURED over all **24** reads in `gate_t24.json` -- `control_Q1_at_plant` and `control_Q2_at_plant` across the twelve cases:] there are **TWO distinct IEEE-754 bit patterns**, `00002c69c537543f` = 0.0012340000000108375 (n = 19) and `00002869c537543f` = 0.0012339999999539941 (n = 5), **and NEITHER is the exact double for the literal `1.234e-03`** (`c53c2b69c537543f`) -- so three patterns are in play but only **two across the reads**. **Maximum relative deviation from the literal 3.73e-11: all 24 reads agree with 1.234e-03 to 10 significant figures.** **The control is live on both readers x12 either way, and the comparator was built for exactly this:** its admission predicate is RELATIVE -- `got >= PLANT*(1-1e-9)` at `analyse_t24.py:517`, with `PLANT` imported and never redefined at `:110` -- **which is what these deviations require and what an absolute predicate would have mishandled.** **WHAT DOES NOT MOVE, AND THIS ROW EXISTS PARTLY TO SAY SO:** **§5.5's saturation ruling is unchanged** -- it turns only on the **maximum**, and **11.15 < `nproc` 16**, so **no case crossed the threshold and all twelve rows remain calibration-eligible**; the ratio **1.2683**, the total **457.0753 core-min**, cleaned == gross, waste **0.000 core-min**, the cap utilisations, and the attribution to **CONCURRENCY CONTENTION** are every one of them **unaffected by all four corrections**. **THE +26.83 % AND +28.19 % REMAIN AS MEASURED.** | Corrects this ledger's T24 row, headed *"T24 -- Case 3 motor-in-duct CHT, THE TWELVE POINTS T23 DEFERRED"*, landed at `c667c10f` (that heading occurs exactly once in `git show HEAD:docs/COST_CALIBRATION.md` [MEASURED], so it names the row unambiguously). Sources re-read at source for this row, all from `git show HEAD:` bytes or from the artefacts themselves: launch loads `verification/runs/T-family/T24_runs/T24_P*/START.T24_P*` (twelve files); `cpu_s/wall_s` and the 26.7-29.9 % measurement `verification/runs/T-family/T24_runs/T24_CONCURRENCY_CONTENTION_NOTE.md` `:1`, `:3`, `:13-14`, `:38-63`, `:60`, `:68`, `:72-73`, `:93` (committed `d8262317`); totals, per-row `core_min`, `execution_time_s`, `start.start_utc` and the 24 `control_Q*_at_plant` reads `verification/runs/T-family/T24_runs/gate_t24.json` (committed `cc5f1af4`); plant predicate `verification/runs/T-family/T24_runs/analyse_t24.py:110`, `:517` (frozen `d9082bfb`); earliest landing corroborated by `verification/runs/T-family/T24_runs/T24_P080_U10/log.solve`. Registration `docs/campaigns/T-family/T24_PREREGISTRATION.md` frozen `b9057489` -- **read, not amended: this row touches no pre-registration.** |
| C-20260831T214227.534102Z-aaf53128 | 2026-08-31 | dafoam | **CORRECTION ROW, NAMING THE TWO ROWS IT CORRECTS: BOTH SO-2a INSTRUMENT-REPAIR ROWS CARRY, BYTE-FOR-BYTE, THE SENTENCE *"SO-2a has bought nothing and its label remains `PENDING`."* — AND SO-2a HAS SINCE BEEN GRADED `PASS`.** The two rows are (a) the STRUCK row headed `~~**C-215**~~ **STRUCK — DUPLICATE ID…**`, landed at `84c0a769`, 2026-08-30T23:36:22Z, currently at `docs/COST_CALIBRATION.md:300`; and (b) its replacement, headed **"THE REPLACEMENT ROW THE STRUCK `C-215` AT `:300` SAYS IS OWED — LANDED THROUGH `scripts/append_record.py`…"**, landed at `3253d4a5`, 2026-08-31T16:36:54Z, currently at `:315`. **THE REPLACEMENT IS NAMED BY ITS HEADING AND LANDING COMMIT RATHER THAN BY ITS ID, AND THAT IS NOT AN OVERSIGHT:** `scripts/append_record.py` refuses at exit 9 any rows text containing a hand-written tool-form id ANYWHERE in it — `check_allocation` runs `re.findall(ANY_TOOL_ID, rows_text)` over the whole submission, not over the id cell — so a correction row landed through the sanctioned tool **structurally cannot quote the id of a tool-allocated row it corrects.** That is a live tension with this file's own append rule 1 (*"a correction is a new row naming the row it corrects"*), it is **REPORTED HERE, NOT WORKED AROUND**, and it is the same reason the two existing correction rows of 2026-08-31 name their targets by heading text and commit. Line numbers are given as a finding aid only and will move; the heading and the commit are the durable address. **WHAT IS FALSE:** SO-2a's item verdict is **`PASS`** — top-level `verdict` `PASS`, and `PASS` on **both** rows, `G5g_SHIPPED.row_verdict` and `G5g_PATCHED.row_verdict` — graded **2026-08-31T00:25:29Z**. And *"bought nothing"* is falsified by the item's own measured spend: **5.217 core-min** total across five arms (MESH 0.167, X-S 0.333, X-P 0.333, G-S 2.367, G-P 2.017), `G10_caps` `PASS`. **`PENDING` is a display or queue state (`CLAUDE.md` rule 1, `VERIFICATION_CHARTER.md` §9) and must never stand where an item verdict exists**, which is why a sentence that is merely stale about a queue label is worth a row: a reader meeting it cold takes away the wrong verdict for the item. **THE TIMING, MEASURED, AND IT SEPARATES THE TWO ROWS SHARPLY.** Row (a) landed **49 min 07 s BEFORE** the grading and **was TRUE when written** — nothing about it was careless and it is not criticised here. Row (b) landed **16 h 11 min 25 s AFTER** the grading and carried the sentence past its own falsification. **AND THE BYTE-FOR-BYTE CARRY DOES NOT EXPLAIN THAT, WHICH IS THE PART WORTH RECORDING.** Row (b)'s integrity assertion is explicitly *"columns 5–10 below are `cmp`-identical"* — **MEASURED BY THIS LANE: both rows split into exactly ten data fields, and the sentence sits in FIELD 4 in each**, outside the range that assertion covers. Column 4 was RESTATED by hand under row (b)'s own words *"The original process, unchanged:"*, and what was restated was a fact that had stopped being true sixteen hours earlier. **The carry-integrity property is real and is not impeached; it simply does not reach the cell that went stale, and a restatement is not a carry.** | **not a new prediction.** Both rows' predicted cells stand exactly as written: **NONE REGISTERED**, stated as absent rather than approximated per append rule 2, and therefore no ratio. Nothing in this row supplies a prediction either of them lacked. **For the ITEM — which is a different process and is NOT this row's subject, and is stated only so the "bought nothing" correction is checkable — SO-2a registered 9.19 core-min at ranks 1, cap 79.0.** | **not a new actual.** Both rows' actual-gross cells stand: **≥ 0.462 core-min at ranks 1**, the inequality deliberate, with one untimed drive named rather than estimated. **ZERO additional core-minutes were spent producing this correction:** no container, no solver, no mesh; one JSON grade artefact and one status file read, and two committed blobs compared. **The ITEM's own actual, for the checkability of the correction only: 5.217 core-min MEASURED, from the graded arms above.** | **not a new figure.** Both rows' cleaned cells stand: **0.367 core-min**, the three drives that produced the committed evidence, longest invocation 19.83 wall s and so nowhere near the 3600 wall-s stall rule. Gross equals cleaned for this correction row itself at **0.000 core-min**. | **NO RATIO, and for the reason both corrected rows already give: nothing was predicted for an instrument repair, so nothing is divided.** That is unchanged and is not disturbed here. **This row adds no ratio of its own** — a correction to a sentence has no denominator — and it is deliberately NOT the place to book the ITEM's ratio: SO-2a's own calibration against its registered 9.19 core-min belongs to the SO-2a item row, wherever that is landed, and folding it in here would repeat precisely the category error both corrected rows refuse in writing (*"comparing an item estimate against a repair's instrument time would be a category error and would corrupt the ledger's ratio series"*). **For the record and NOT as a ledger ratio: the item's own grade json books `predictions.P8_total_core_min_band` as `MISS`.** | **THE TWO ROWS REMAIN CORRECT AS SCOPED AND NEITHER IS WITHDRAWN.** Each states in terms **"THIS ROW IS NOT THE SO-2a ITEM"**, and each prices the *repair* — adopting the absent `so2a_aggregate_memory.py` byte-for-byte from SO-1a, proving the gate refuses in both directions, amending §7's instrument table by dated addendum, sweeping the family. **Every figure, tag, artefact path and refusal in both rows stands; no cost figure is disputed and none moves.** The scoping sentence does its job and a reader who weighs it is not misled about WHAT is being priced — but scoping is not currency: **"this row is not the SO-2a item" tells a reader the row is about the repair, and the very next clause then tells them SO-2a is `PENDING`, which is a claim about the item and is false.** The two sentences are adjacent, in the same cell, and only one of them is now true. **NOTHING IS REWRITTEN.** Append rule 1 is unconditional — *"An existing row is never edited; a correction is a new row naming the row it corrects"* — and the byte-for-byte identity of the sentence across the struck and replacement rows is itself an integrity property of this ledger, so editing either row would destroy evidence in order to tidy prose. **ATTRIBUTION, which is the calibration content: this is not misprediction, contention or waste. It is a TRUE SENTENCE OUTLIVING ITS CONDITION** — a status assertion about a NEIGHBOURING process, written correctly, then copied forward into a row that landed after the status changed. **The general shape: an append-only record forbids editing, so any status claim written into a row is frozen at its writing while the world moves on. A row may therefore state a cost that will always be true and a STATUS that was only ever true at 23:36:22Z on one night. The two are not the same kind of claim and the second does not belong in an append-only cell without its timestamp attached.** That is offered as experience, not as a rule: **recorded under Sanaa's 2026-08-31 PLUMBING FREEZE, and it SPAWNS NO RULE, NO TOOL AND NO PROCEDURE.** | **Grading artefact: `/home/ubuntu/certonomous-runs/CURRICULUM-SO2a-a1-naca0012-geometric-constraint-gradient/SO2a_grade_20260831T002529Z.json`, md5 `a6386f07ed658b6eaa92cc3c07bcd0d5`**, carrying `verdict` `PASS`, `gates.G5g_SHIPPED.row_verdict` `PASS`, `gates.G5g_PATCHED.row_verdict` `PASS`, `gates.G10_caps.total_core_min` 5.217, `predictions.P8_total_core_min_band` `MISS`; its `.out` sibling beside it. **Corroborating timestamp in the repository:** `cases/dafoam/ladder-a/A1/curriculum_SO2a/STATUS.SO2a_chain`, reading `launcher_rc=0 end=2026-08-31T00:25:29Z`. **Corrected rows:** `84c0a769` (struck, `:300`) and `3253d4a5` (replacement, `:315`). **Field-position and field-count measurement** taken by this lane by splitting each row on unescaped bars: ten data fields each, stale sentence in field 4 of both. **Tool refusal cited above:** `scripts/append_record.py`, `check_allocation`, `EXIT_REFUSED_ALLOCATION`. |
| C-20260831T223116.831784Z-f2a2dd3d | 2026-08-31 | dafoam | D19 phase 1 (curriculum_D19, four arms MESH/X2/S2/S1, PATCHED row, plateau sweep; phase 2 correctly not launched) | 8.7 core-min registered pre-launch (phase-1 cap 140.0; item ceiling 280.0), basis D15's frozen ledger at ~0.113 core-min per perturbed primal at np=2 | 8.683 core-min = $0.00742 derived (MESH 0.467 + X2 3.333 + S2 4.033 + S1 0.850, each wall_s x ranks / 60 read from the run root's own `ledger.txt`) | = gross (max arm wall_s = 121; no row anywhere near the 3600-s stall rule) | **0.998x** | **THE HEADLINE RATIO IS PARTLY CANCELLATION AND THE LAB SHOULD NOT LEARN 0.998 FROM IT.** Per arm: MESH 0.467 vs 0.2 = **2.34x OVER**, X2 3.333 vs 1.8 = **1.85x OVER**, S2 4.033 vs 5.7 = **0.71x UNDER**, S1 0.850 vs 1.0 = **0.85x UNDER**. Two overs and two unders nearly cancel. Attribution, measured not guessed: (a) **contention, measured** — X2's ledger row records `delivered_cores_mean=[0.5792 n=5 max_nr_throttled=33]` against `ranks=2`, i.e. it received ~29 % of the cores it was billed for, which is the whole of its 1.85x; S2 got 1.9396 and S1 0.9930, and both came in UNDER. (b) **misprediction, structural** — the 0.113 core-min/primal figure carried over from D15's `F-P` OVER-predicts a long sweep (S2's 52 primals measured **0.0776**/primal at np=2, a 31 % over-estimate) and UNDER-predicts fixed startup (MESH, X2), because the per-primal basis has no startup term at all. **Corrected anchors now registered in `curriculum_D19R/PREREGISTRATION.md` §7: 0.0776 core-min/primal at np=2, 0.0708 at np=1, with MESH and X2 priced from their own measured 0.467 and 3.333 rather than from a per-primal rate.** (c) **Waste: NONE in `COMPUTE_BUDGET_CHARTER.md` §6's sense** — nothing was discarded or re-run. But stated plainly rather than absorbed: **this 8.683 core-min bought NO graded verdict**, because the grader refused on `G1` (`age_datum_moved`, an INFRASTRUCTURE clause — the `patchV` DV rewrites `0/U` mid-run, so rule 4's "0/T is touched last at launch" premise is false for DAFoam). Under Sanaa's 2026-08-26 rule that bookkeeping never voids physics, the FD fields are read forward by D19R as evidence, so the compute is not lost — but the item carries no verdict and that is the honest label. | run root `/home/ubuntu/certonomous-runs/CURRICULUM-D19-a1-naca0012-subsonic-opt` (`ledger.txt`, `PHASE1_COST_ESTIMATE.txt`, `STATUS.D19_chain`, `D19_phase1_grade_20260831T215400Z.out`); pre-registration frozen at `f032d94e`, phase-1 instrument at `32bd000f`; successor freeze `cases/dafoam/ladder-a/A1/curriculum_D19R/PREREGISTRATION.md` §7. Dollars DERIVED at $0.0513/core-h c7a.4xlarge, **reported-by-owner, not measured** (`COMPUTE_BUDGET_CHARTER.md` §5). |
| C-20260831T224621.871750Z-5fa38f63 | 2026-08-31 | dafoam | **SO-2MR — the A1 NACA0012 moment-cap gradient rung on `CMZ`, COMPLETED: five arms, np = 1, two rows, `chain=COMPLETE arms_bought=5 of 5`, every arm `rc=0`, ITEM VERDICT `GATE FAIL` — which is the outcome the frozen document REGISTERED IN ADVANCE** (`curriculum_SO2MR/PREREGISTRATION.md` §8, frozen `c0eff9ca`: *"P1–P7 HIT → PATCHED row `PASS`, SHIPPED row `GATE FAIL`, ITEM VERDICT `GATE FAIL`"*). SHIPPED `G5m` `GATE FAIL` on `shape[6]` at **20.5497 %** against band D 5.0 %; PATCHED `PASS` on all five pairs, aggregate **0.018810621362311878 %**. **The direction-B control that killed SO-2M DEMONSTRATED on both rows** — `plant_is_sufficient: true`, `plant_margin_ratio: 2.0`, against the SAME `d_ref = -0.04976246220706002` and the SAME `plant_needed_to_cross_band_D = 0.002488123110353001` SO-2M's absolute plant could not cross. | **11.0 core-min** point REGISTERED, ceiling 79.0 (`PREREGISTRATION.md` §9) | **10.649 core-min** MEASURED, summed from the five `ARM=` rows of the item's own `ledger.txt`: MESH 0.783, X-S 3.383, G-S 2.983, X-P 1.317, G-P 2.183 | **10.649 core-min** — gross equals cleaned: **no arm is a stall**, the longest wall time being 203 s against the 3600 s stall threshold, and no arm crossed its cap (G10 `PASS`) | **0.968** cleaned/predicted | **CONTENTION, and the total ratio HIDES a per-arm scatter that is reported here rather than absorbed.** Per-arm actual/point: MESH **3.92**, X-S **2.26**, G-S **0.83**, X-P **0.88**, G-P **0.62**. The two over-runs are CONTENTION and the box MEASURED it: MESH recorded `delivered_cores_mean=[0.0512 n=1 max_nr_throttled=0]`, about 5 % of one core, against SO-2M's uncontended 0.183 core-min anchor — the arm did the same work over 47 wall s instead of 11. The two G arms came in UNDER their points because §9 sized them on SO-1a's F-arm upper anchors (3.583 / 3.433) which this case did not need. **The near-perfect 0.968 is two opposite errors cancelling at the total, not five accurate predictions**, and a future estimate that trusts the aggregate will misprice the arms. | grade artefact `/home/ubuntu/certonomous-runs/CURRICULUM-SO2MR-a1-naca0012-moment-gradient/SO2MR_grade_20260831T224317Z.json`; ledger `…/ledger.txt`; freeze `c0eff9ca9330dd310fcfbe7a77f13e2ec1ec8cca` |
| C-20260831T224621.871813Z-e6f444f5 | 2026-08-31 | dafoam | **SO-2M — THE WASTE ROW THAT WAS OWED AND THAT NOBODY HAD FILED.** `curriculum_SO2M/PREREGISTRATION.md` §9 and `CLAUDE.md` rule 12 owed this row at that item's completion; it is landed here, beside its successor's, so the pair can be read together. **SO-2M is `NOT A RESULT` and this row does not reopen, re-grade or soften that verdict.** Its solve WORKED: `chain=COMPLETE arms_bought=5 of 5`, every arm `rc=0`, and `CMZ_baseline = 0.0078407548669119` finite on both images — `d(CMZ)/dx` existed in this lab for the first time on that run. What refused was its **CONTROL**. | 11.0 core-min point REGISTERED (SO-2M's own §9, the same figure its successor carried unchanged) | **6.250 core-min** — this figure is ARITHMETIC ON FIVE ON-RECORD LEDGER ROWS (MESH 0.183, X-S 1.017, G-S 2.017, X-P 0.85, G-P 2.183, `curriculum_SO2M/RESULTS.md:185-189`); **NO ARTEFACT PRINTS THE TOTAL**, because the comparator refused before writing a `G10_caps` block, and that is stated rather than papered over | **NOT APPLICABLE — the whole 6.250 core-min is WASTE** | **NOT A RATIO, DELIBERATELY.** `COMPUTE_BUDGET_CHARTER.md` §6 and `CLAUDE.md` rule 12 require waste to be named separately and **NEVER absorbed into a calibration ratio**; computing one here would launder a control failure into an estimating error. It is also excluded from SO-2MR's 0.968 in the row above. | **A CONTROL FAILURE — a GATE-DESIGN DEFECT IN THE REGISTERED PLANT SIZE — AND EXPLICITLY NOT A SOLVER FAILURE.** The registered direction-B plant was a bare absolute `1.234e-03` inherited from a `CD`-scale item and never sized against `CMZ`, whose reference is about 14× larger: it is **2.479781 %** of `|d_ref| = 0.049762462207060022` where crossing band D needs **0.0024881231103530011**, short by a factor of **2.016307**, and so could not cross the band **by construction**. The comparator exited `grader_rc=2` refusing to report a `PASS` from a gate it had not shown able to `GATE FAIL` — **the correct behaviour, and the reason this waste bought a transferable lesson rather than nothing**: an absolute plant magnitude does not port across functionals. The repair is the relative rule now MEASURED working in the row above at `plant_margin_ratio 2.0`. | `curriculum_SO2M/RESULTS.md:50-65` (the refusal) and `:185-189` (the five ledger figures); successor freeze `c0eff9ca9330dd310fcfbe7a77f13e2ec1ec8cca` |
| C-20260831T230441.783350Z-acc4a57f | 2026-08-31 | heat-transfer | **CORRECTION ROW, NAMING THE ROW IT CORRECTS BY SUBJECT: the heat-transfer row headed *"T23 GRADED --- THE FOUR REGISTERED CASES OF THE RESCALED CASE 3 MAP, `PASS` x4 ON THE PHYSICALITY TIER"*, landed 2026-08-31T18:33:46Z. THAT ROW'S ARITHMETIC IS CORRECT AND ITS BASIS CHOICE WAS DISCLOSED ON ITS OWN FACE; THE INSTRUMENT IS NOT THE ONE THE UNIT REQUIRES.** That row took its actual from the four `log.solve` **`ExecutionTime`** lines and said so: *"the ExecutionTime basis is used for the ratio because it is the solver's own figure"*. **`ExecutionTime` is CPU time. `CLAUDE.md` rule 12 defines the unit as WALL seconds x ranks / 60.** The row framed the choice as *the solver's own figure* versus *launch-to-end wall* and missed the third option, which is both and sits on the same printed line: **`ClockTime` is the solver's own figure AND it is wall.** **NOTHING IN THE CORRECTED ROW IS CONTRADICTED** --- its four figures stand, correctly, as ExecutionTime figures; this row restates them on the wall instrument. **SECOND AND SEPARATE: both this row and the one it corrects are RE-DERIVED FROM LOGS AFTER RECORD LOSS.** The queue runner's pre-repair `echo ... > STATUS.<case_id>` truncated the nine cost fields `run_t23.sh:74-87` had written to that same path on all four cases, and `launcher.queue.out` is 0 bytes, so no stdout copy exists. **The destroyed `wall_s` is NOT reconstructed and is NOT claimed**; what is claimed is a different, surviving wall instrument. Repair landed at `9e356f09` (2026-08-31 20:21:02 +0000), **two hours forty-eight minutes AFTER `U10` launched**. | **123.2 core-min SUBSET POINT, REGISTERED NOT MEASURED, UNCHANGED** --- `docs/campaigns/T-family/T23_PREREGISTRATION.md` section 5.2, frozen at `fe666fd5d3cf148b1266bf693d1199cec3c7d607` before the run root existed; 30.8 core-min per case x 4. Registered CAP **400.0 core-min hard**, per case **100.0**, ENACTED as `TIMEOUT_S=6000` at `run_t23.sh:22` with `RANKS=1` --- **and that enacting artifact SURVIVED the record loss**, which is why the cap question is answerable at all. The frozen file is the file that ran: worktree blob `c341476f3680c14ec593c52d12e49214cf83ebb3` byte-identical to the blob at `fe666fd5` and at `HEAD` for that path [MEASURED]. | **120.1833 core-min [MEASURED, WALL, `ClockTime` x 1 rank / 60] --- RE-DERIVED FROM LOGS AFTER RECORD LOSS, AND A LOWER BOUND.** Per case, from the final `ClockTime` on each `log.solve`: **1785 s = 29.7500; 1814 s = 30.2333; 1831 s = 30.5167; 1781 s = 29.6833 core-min.** The corrected row's ExecutionTime figures were 29.7337 / 30.2190 / 30.4898 / 29.6860 = 120.1285; **the delta is +0.0548 core-min, +0.046 %.** `ClockTime` prints as an INTEGER second, so each per-case figure carries **+/- ~1 s = +/- 0.0167 core-min** of quantization --- visible at `U40`, where `ClockTime 1781` reads BELOW `ExecutionTime 1781.16`. **`ranks = 1` is ESTABLISHED, NOT ASSUMED**, from three independent artifacts that agree: `log.solve` header `nProcs : 1`, `run_t23.sh:21`, and the launched queue entry's `ranks`. USD **$0.102757, DERIVED NOT MEASURED**, at the owner-stated c7a.4xlarge $0.0513/core-h (REPORTED-BY-OWNER 2026-08-21/22; the box cannot read its own billing, `COMPUTE_BUDGET_CHARTER.md` section 5). | **120.1833 core-min --- GROSS AND CLEANED IDENTICAL.** The charter section 2 stall rule matches a row over **3600 wall s**; the longest of the four ran **1831 s, 50.9 % of that threshold**, so the rule matches nothing and no judgement enters the figure. Cap utilisation **30.05 %** of the registered 400.0 (was 30.03 % on the ExecutionTime basis); worst single case **30.52 %** of the per-case 100.0. **NO CASE EXCEEDED 3600 WALL S.** | **0.9755** | **MISPREDICTION, THE ENTIRE 2.45 % GAP, unchanged in kind from the corrected row and moved by 0.0004 in size.** CONTENTION measured and small; WASTE measured to ZERO and therefore not available as an excuse. **THE LOWER-BOUND CAVEAT IS MEASURED RATHER THAN LEFT AS PROSE, and this is what this row adds that no T23 row carried before.** `ClockTime` covers the SOLVER ONLY; the destroyed `wall_s` bracketed the `timeout` line, and neither covers what `run_t23.sh` does before it. Three further surviving clocks bound that gap: **(a)** `0/housing/T` mtime to `log.solve` mtime --- the closest analogue of the destroyed `wall_s`, since `0/housing/T` is touched six lines before `T0` --- **7210.47 s total**; **(b)** the launcher record's `_launch.started_epoch` to the runner's own `end=` stamp, **7210.14 s total**; **(c)** the launcher overhead itself, `started_epoch` to `0/housing/T` mtime, **0.172 / 0.211 / 0.215 / 0.219 s = 0.82 s = 0.0136 core-min** [ALL MEASURED, `st_mtime` and the surviving `_launch` block]. **So this figure is a lower bound by a MEASURED 0.011 %, not by an unknown amount.** All three clocks agree with `ClockTime` to within 1.3 s per case, inside `ClockTime`'s own quantization. **WASTE: 0.0 core-min** --- all four launched clean on the first attempt. Named separately and NOT in the ratio (charter section 6): the y+ measurement passes cost 2 wall s at 1 rank = 0.0333 core-min, analysis cost, not waste. **CONTENTION:** box busy **11.9 / 12.7 / 22.3 / 25.7 %** (~1.9 to ~4.1 of 16 cores) at the four launches [MEASURED, `verification/queue/runner.log` lines 8476-8483], independently re-read for this row; the four wall times span 1781-1831 s, a **2.8 % spread**. **THE TRANSFERABLE FINDING SURVIVES THE CORRECTION UNCHANGED IN SUBSTANCE:** the wall per-cell-iteration rate is **4.543221e-06 s** (was 4.541148e-06 on CPU time), still INSIDE the measured Cartesian bracket [4.1541e-06, 4.6476e-06] near its `_f` end. **AND THE FINDING THIS ROW MOST WANTS ON THE RECORD: the re-derivation was possible only because `ranks`, `solver`, `timeout_s` and four independent wall clocks happened to survive in OTHER artifacts. That is luck, not design. At more than one rank with a decomposition the log did not print, `core-min` would have been STILL VOID and no honest figure could have been written at all.** | Reconstruction record `verification/runs/T-family/T23_runs/T23_COST_RECORD_RECONSTRUCTION.md`, landed at commit `57cf9c43c17321f1946a9d8ff4bd3cfce90cb835`. Graded record `docs/campaigns/T-family/T23_RESULTS.md`. Cases `verification/runs/T-family/T23_runs/T23_P305_U{10,20,30,40}/` --- each `log.solve` (final `ClockTime` line, one `End`, zero `FOAM FATAL`, 10000 `ExecutionTime` lines), `run_t23.sh` lines 21 / 22 / 24 / 58 / 64-69 / 74-87, `STATUS.T23_P305_U*` (the runner's three surviving keys), `0/housing/T` and `10000/fluid/T` mtimes. Queue entries `verification/queue/heat-transfer/launched/T23_P305_U*.json` (`ranks`, `cap_core_min_registered`, `_launch.started_epoch`, `_field_classes`); `verification/queue/LAUNCH_LOG.tsv` lines 253-256; `verification/queue/runner.log` lines 8476-8483. Destruction mechanism `scripts/queue_runner.py` lines 530-532 as it stood at launch, repair at line 509 landed `9e356f09`. **PHYSICS UNAFFECTED UNDER L-342 AND THE REASON IS SPECIFIC: `rc` DERIVES FROM THE LOG AND NOT FROM THE DESTROYED FIELDS** --- one `End`, zero `FOAM FATAL`, last time 10000 == `endTime`, 10000 `ExecutionTime` lines, and every field at 10000 newer than that case's own `0/housing/T` [MEASURED]. `launcher_rc=0` was NOT accepted as `rc`; a zero there is the shape of the `setsid` trap the wrapper exists to avoid, and the runner's own `_field_classes` block classes every destroyed item as INFRASTRUCTURE. **The `PASS` x4 verdicts of `T23_GRADE.json` are untouched by this row.** **A SECOND GAP, REPORTED RATHER THAN SMOOTHED AND NOT PART OF THE RUNNER LOSS:** prereg section 5.4 registers a `START.<case>` load-average file written before the solver starts, and **no such file exists for any of the four because `run_t23.sh` contains no code to write one** [MEASURED]; the graded record's named substitute, `runner.log`, is what the contention reading above rests on. Frozen document not edited (standing rule 6); launcher not retro-fitted; `queue_runner.py` is cfd's instrument and is not touched by this team. |
| C-20260831T230621.613076Z-d360c175 | 2026-08-31 | dafoam | **SO-3aR2 --- THE FD-VERIFIED MULTIPOINT ALPHA GRADIENT RUNG, 5 OF 5 ARMS BOUGHT, AND THE FIRST TIME THIS FAMILY'S MULTIPOINT CHAIN RAN TO THE END.** Registered 22.1 core-min point, band [14.0, 60.0], ceiling 115.0 = the sum of the five per-arm caps, frozen at `181fd627924c9108000f5259dd30fd87cc0daf3f` before any container started. **This row reports cost only. It grades nothing:** the item verdict is the `dafoam-supervisor`'s and the comparator's exit status is not a verdict. | 22.1 core-min registered point / $0.01890 DERIVED | **14.318 core-min MEASURED** from the five `ARM=` rows of the run root's own `ledger.txt` (MESH 0.167 + X-S 2.517 + F-S 4.367 + X-P 2.517 + F-P 4.750) = **$0.01224 DERIVED, NOT MEASURED** | **14.318 core-min --- GROSS AND CLEANED ARE THE SAME FIGURE HERE AND THAT IS A READING, NOT AN ASSUMPTION**: the 3600-s stall rule (`COMPUTE_BUDGET_CHARTER.md` §2) matches no row, the longest arm being F-P at **285 wall s**, so nothing is removed and the two columns coincide | **0.648x** (14.318 / 22.1). Inside the registered band [14.0, 60.0], so **P-COST HIT** --- though it lands **0.318 core-min above the band floor**, which is a narrower margin than the ratio alone suggests and is stated rather than left to be inferred | **MISPREDICTION, IN THE OVER-ESTIMATING DIRECTION, ON EVERY ONE OF THE FIVE ARMS --- and NOT contention.** Per arm, predicted -> measured: MESH 0.30 -> 0.167 (0.557x); X-S 3.1 -> 2.517 (0.812x); X-P 3.7 -> 2.517 (0.680x); F-S 7.5 -> 4.367 (0.582x); F-P 7.5 -> 4.750 (0.633x). **CONTENTION IS EXCLUDED ON MEASUREMENT, NOT ON BELIEF**: every arm's H5 window read `samples_below_floor=0 waited_s=0` with `min_GiB` between 28.58 and 28.68 against the 16.0 floor; every AGGREGATE guard read `waited=0` with `aggregate_GiB` 13.94-14.05 against the 30.6 ceiling; `siblings_pre=[]` on all five arms; and `delivered_cores_mean` read 0.9849-0.9979 at `ranks=1`, so the cores asked for were the cores delivered. One sibling (`d19r_N2`) appears in F-P's `siblings_post` only, after that arm's work was done. **THE F ARMS CARRY THE BULK OF THE MISS AND THE CAUSE IS NAMED:** the point estimate priced 102 primals per F arm at anchor A4's **upper bound** 0.062585 core-min/primal and then added a whole X arm for setup --- an upper bound used as a point estimate predicts high by construction, and it did, by 1.7x. **AND THE ONE ANCHOR THE PRE-REGISTRATION DELIBERATELY MOVED OVERSHOT:** §7 raised MESH from 0.19 to 0.30 on SO-3aR's measured 0.267 (ratio 1.40), and MESH then measured **0.167** --- below both the old point and the new one. A correction made honestly on one measurement is still a sample of one, and this is that correction being scored against itself rather than quietly kept. **WASTE, CARRIED IN AS SPENT AND NAMED SEPARATELY, NEVER FOLDED INTO THE 0.648x ABOVE** (`COMPUTE_BUDGET_CHARTER.md` §6): SO-3a **0.334 core-min** (died on an unset producer pin) and SO-3aR **1.800 core-min** (died at arm 2 of 5 on the `renameSolution` directory collision this item repairs), **total 2.134 core-min MEASURED**. Had they been folded in the ratio would read 0.744x, and that number would be a claim about SO-3aR2's estimate quality made out of two other items' deaths --- which is exactly the laundering §6 forbids. | Freeze `181fd627924c9108000f5259dd30fd87cc0daf3f` (`cases/dafoam/ladder-a/A1/curriculum_SO3aR2/PREREGISTRATION.md` §7, §9); arming `ffae7724c8f7e10ea351f467521162ae6717ece9`. Figures read from the run root's `ledger.txt` `ARM=` rows and `SO3aR2_grade_20260831T230221Z.json` under `/home/ubuntu/certonomous-runs/CURRICULUM-SO3aR2-a1-naca0012-alpha-multipoint-gradient/` (outside git by size, `docs/LOCATIONS.md`). **A `RESULTS.md` for this item does not exist yet** --- the grading write-up is the supervisor's --- and this row cites the artefacts rather than a document that is not on disk. |
| C-20260831T231122.189443Z-12449925 | 2026-08-31 | cfd | **JF1 P1 — the registered jet-flap physics/diagnostic rung on the C-topology L1 mesh, RUN COMPLETE AND `NOT A RESULT`.** Strictly complete on every clause — `solver_rc 0`, one `End` line, last `Time` 20000 == `endTime`, `ExecutionTime` count 20000 == `endTime`, fields `U k nut omega p phi yPlus` present at 20000, and the age guard holds (`20000/U` newer than `0/U`). **It did not converge:** zero `SIMPLE solution converged` lines, and the final initial-residuals leave `k` at 4.833e-05 against the 1e-06 criterion. Sanaa's standing doctrine for this family fixes the label on hitting the iteration cap — *"Iteration cap 20,000; hit cap -> NOT A RESULT, never 'close enough'"* — so the label is **`NOT A RESULT`**, and completeness and convergence are two different things, both reported. **The plateau is the rung's registered purpose, not its failure:** the run matrix registers P1 as *"physics/diagnostic, fixes relaxation"*, and it has diagnosed that the current relaxation settings plateau (p initial residual flat and oscillating in 1.5–1.9e-04 from iteration 3,000 to 9,000). | **90.5 core-min** staged upfront (ranks 4, assumed parallel efficiency 0.75 — labelled an assumption at staging because no measured JF1 scaling row existed on this box) | **98.359 core-min** gross = `ExecutionTime` 1475.38 s × 4 ranks / 60, read from the run's own `log.simpleFoam` tail | **= gross.** Single solver invocation, wall 1478 s ClockTime — under the charter §2 3600-s stall threshold, so no row is removed and cleaned equals gross | **1.087×** (98.359 / 90.5) — an 8.7 % overrun on the upfront estimate | **Misprediction, small and in the conservative direction; waste 0.000.** The single input that moved is parallel efficiency: staged at an assumed 0.75, **measured 0.690** (ideal rank-1 basis 3.392219e-03 core-min/iter × 20,000 = 67.84 core-min, divided by 98.359 actual). The staging assumption was very nearly right and this row is the first **measured** JF1 parallel-scaling figure on this box, so the next JF1 estimate uses 0.690 rather than an assumption. **A supervisor mid-run projection of 153.5 core-min is NOT used here and is recorded as wrong:** it extrapolated an early-phase rate sample of 0.1151 s/iter, where the full-run rate is **0.07377 s/iter** — the run accelerated after its startup transient, so the mid-run figure was 56 % high. The estimate was good; the mid-run correction to it was not. **Dollars: $0.0841 DERIVED, not measured**, at the owner-stated $0.0513/core-h (reported-by-owner; this box cannot read its own billing, `COMPUTE_BUDGET_CHARTER.md` §5). | `verification/runs/JF1_jet_flap/JF1_P1_L1_CMESH_PHYSICS/` — `log.simpleFoam`, `SOLVER_RC.txt`; queue entry `cases/JF1_JET_FLAP/queue_drafts/STAGED_2026-08-31T2230Z/JF1_P1_L1_CMESH_PHYSICS.json` landed at `2f8e37c5`; registration `verification/campaign/JF1_PREREGISTRATION.md` frozen at `12b1bd84` |
| C-20260831T232958.953610Z-89c9b24d | 2026-08-31 | cfd | **JF1 L1 C_mu SWEEP — the five completed feasibility rows (unblown + C_mu 0.05/0.10/0.20/0.40), the point set ACT B's lift and pressure charts are drawn from.** Written because the corrected demo figure now publishes this comparison to a customer, and a number on a customer slide with no ledger row behind it is exactly backwards. All five are rule-4 complete at 8000 iterations, second order, rank 1, on the 39,984-cell O-topology L1 mesh. **None converged** — zero `SIMPLE solution converged` lines, and at the final iteration `k` runs 3.457e-06 → 1.472e-04 and first-pass `p` runs 4.557e-06 → 4.482e-05, both above the 1e-06 criterion on every row, the degradation monotone in blowing. These rows score nothing by registration (feasibility stage) and no verdict of the fixed vocabulary attaches. | **56.79 core-min REGISTERED** = §12's per-solve L1 figure 11.36 × 5 (`JF1_PREREGISTRATION.md:1972`). **Stated as the registered analogue, not as a line written for these runs:** §12 carries no row for the C_mu map run *as feasibility* — its 56.79 is the G1–G5 gated line for the same five L1 second-order points, and 11.36 is the registered per-solve cost of an L1 second-order solve, which is what these were. | **117.4833 core-min** gross = the sum of the five `RUN_STATUS` `core_min_MEASURED` fields (23.7667 / 24.4333 / 21.7500 / 22.3333 / 25.2000) | **= gross.** Per-row wall at rank 1 spans 1305–1512 s; **no row matches the charter §2 3600-s stall rule**, so nothing is removed | **2.068×** (117.4833 / 56.79) — **the registered estimate was low by a factor of two** | **Misprediction, and the mechanism is now identified for the whole family rather than for one row. THE REGISTERED PER-SOLVE FIGURE ASSUMES CONVERGENCE BEFORE THE ITERATION CAP, AND NO JF1 ROW HAS EVER CONVERGED** — these five ran their full 8000 iterations, and P1 ran its full 20,000, all stopping on the cap rather than on a criterion. That is the same defect behind P1's registered 11.36 sitting 5.97× below its measured cost, so it is a family-wide estimate-basis error, not a row-level miss. **A SECOND, BETTER ESTIMATE EXISTED AND IS RECORDED HERE BECAUSE IT IS THE ACTUAL CALIBRATION CONTENT:** the launch-time queue drafts staged the four blown rows at **33.90 core-min each** from a measured timing pilot; those four actually used 93.7166 against 135.60 staged, **ratio 0.691 — an OVER-prediction**. So the registered figure is 2.07× low, the pilot-based figure is 1.45× high, and the truth is between them at a measured **23.497 core-min per L1 row**. **The next JF1 L1 estimate uses 23.5 core-min per row, measured, rather than either.** **Waste: 0.0167 core-min**, separately named and not laundered into the ratio — one failed launch attempt (`JF1_L1_UNBLOWN_A0.attempt1_FAILED_2026-08-31T1538Z`, its own `core_min_MEASURED`). **Dollars: $0.1004 DERIVED, not measured**, at the owner-stated $0.0513/core-h; this box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` §5). | `verification/runs/JF1_jet_flap/JF1_L1_{UNBLOWN_A0,BLOWN_CMU005_A0,BLOWN_CMU010_A0,BLOWN_CMU020_A0,BLOWN_CMU040_A0}/RUN_STATUS.*.txt`; registered basis `verification/campaign/JF1_PREREGISTRATION.md:1972` frozen at `12b1bd84`; staged pilot basis in `cases/JF1_JET_FLAP/queue_drafts/JF1_L1_BLOWN_CMU*.json` |
| C-20260831T233302.719783Z-a463a495 | 2026-08-31 | heat-transfer | **T18 — 3-D transient conduction in a cube (T11c), Robin faces on all three axes, EXACT tier; `laplacianFoam`, `Euler` + `Gauss linear corrected`, PCG/DIC 1e-12, **1 rank** — THE RUNG-LEVEL ROW AT COMPLETION. All four registered cases closed and the rung declared: rows G1, G2 and G3 `PASS`, all three triples `CONVERGING`, observed orders 1.9996 / 2.0006 / 2.0130, GCI at Fs = 1.25 of 0.00214 / 0.01041 / 0.01543 percent.** **THIS ROW OVERLAPS `C-201` AND THE TWO MUST NOT BE SUMMED.** `C-201` (2026-08-28) was the PARTIAL row for the coarse level alone, filed when `T18_CU_c` completed and carrying no verdict because rule 5 needs three levels and there was one; it explicitly declined the rung-level point as its basis. Its 0.533 core-min actual against 0.437 predicted is **included again** in this row's four-case totals, because a rung-level ratio cannot be computed from three of four cases. `C-201` is not corrected, superseded or re-graded — its content stands for its own scope | **87.902 core-min** rung POINT, rung CAP **383 core-min**, frozen before any compute at `add2c78870625c75c2a48bdff018d7a034a2ff84` (`T18_PREREGISTRATION.md:270-276`); per-case POINTs 0.437 / 3.499 / 27.989 / 55.977 and caps 3 / 20 / 120 / 240, enforced as wrapper timeouts of 180 / 1200 / 7200 / 14400 s at 1 rank. **Basis: the BORROWED 1.64e-07 core-s per cell-step**, T14's scratch-arm rate; `T18_registered.json` `cost.rate_provenance` reads "BORROWED, NOT MEASURED ON THIS RUNG". **`cost.misprediction_risk` pre-registered a ONE-SIDED expectation IN ADVANCE: an UNDER-prediction "by a factor of up to about 3"** (`T18_PREREGISTRATION.md:259-268`), for a 2-D to 3-D dimension change (4-neighbour to 6-neighbour stencil) and a 2 500 to 512 000 cell jump. Dollars **DERIVED, NOT MEASURED**: **$0.0752** at the point, $0.327 at the cap, at $0.0513/core-h c7a.4xlarge, **REPORTED-BY-OWNER** (`COMPUTE_BUDGET_CHARTER.md` §5) | **125.516 core-min**, basis **gross**, MEASURED as `wall_s × ranks / 60` and re-derived by this lane from the four wrapper-written `verification/runs/T-family/T18_runs/STATUS.T18_CU_{c,m,f,f_CT}`: 0.533 + 5.483 + 45.417 + 74.083, from `wall_s` 32 / 329 / 2725 / 4445 at 1 rank throughout. **Each of those four wall times is independently corroborated by a second instrument**: the rule-4 age-guard margins computed from filesystem mtimes (`0/T` to `2/T`) are +31.544 / +328.577 / +2724.947 / +4444.602 s, agreeing with the launcher's own clock to better than one second in every case. Dollars **DERIVED, NOT MEASURED**: **$0.1073** | **125.516 core-min — CLEANED EQUALS GROSS, under this record's own cleaned definition, and the departure from the mechanical test is named rather than absorbed.** Append rule 4's 3600-s stall test **does match one row**: `T18_CU_f_CT` at `wall_s` 4445. It is **NOT a stall** and is not removed: it is the registered temporal-bias control at `deltaT` 5e-05, a **40 000-step** run (twice the other levels' 20 000) under a **registered 14 400-s** timeout, with `rc=0`, `capped=no`, `note=clean`, one `End` line, `ExecutionTime` count 40 000 == endTime/deltaT, and an age-guard margin reproducing its wall time to 0.4 s. **For a reader who prefers the mechanical rule the figure is 51.433 core-min** (125.516 − 74.083), and the ratio on that basis is 0.585x — stated so the choice is visible rather than hidden | **1.428x** cleaned/predicted (125.516 / 87.902); **0.328x of cap** (125.516 / 383). Per case: 1.220x / 1.567x / 1.623x / 1.323x — the miss is in the same direction on all four levels | **MISPREDICTION, one-sided and PRE-DISCLOSED. Zero waste, zero contention.** No case stalled, no case was `capped`, all four ran to `endTime` and completed on every clause of rule 4. There is no discarded, re-run or abandoned work to name under charter §6. The entire gap is the borrowed T14 rate under-pricing a 2-D to 3-D stencil change and a 205x cell-count jump — **exactly the failure shape the registration named in advance**, and the same shape that made T1b L4 miss by 31.4 %. **The realised miss is 1.428x against a pre-registered expectation of up to about 3x, so the named risk materialised at UNDER HALF its predicted size**, and the caps set at 4.3x to 6.9x the point were never approached. **Calibration lesson: the pre-registered one-sided risk statement was the useful artifact — it bounded the miss correctly where the point estimate did not, and a borrowed rate carried across a dimension change should continue to be registered with an explicit one-sided bound rather than a point alone.** **One spend named separately and NOT folded into any figure above**: the verdict-readiness audit's own `analyse_t18.py --selftest` execution, **0.777 core-min** (46.60 s × 1 rank, $0.0007 derived) — an instrument check, not a solve, carrying no pre-registered cap of its own | `docs/campaigns/T-family/T18_RESULTS.md` §10, landed at `b448e64547ad632bea1b5965d14aca20fc9f2bc1`; pre-registration frozen at `add2c78870625c75c2a48bdff018d7a034a2ff84`; gate artifact `verification/runs/T-family/T18_runs/gate_t18.json` landed at `b6a56f20`; verdict-readiness audit `verification/runs/T-family/T18_runs/T18_VERDICT_READINESS_AUDIT.md` landed at `a368b35b` |
| C-20260831T234136.682040Z-89696a12 | 2026-08-31 | heat-transfer | **T25_MOD_L1 — the battery-module 8-block multi-patch conduction feasibility run** (`chtMultiRegionFoam`, solid region `module`, 960 cells, `deltaT` 0.5, 1800 steps, `endTime` 900, 1 rank). **UNGATED FEASIBILITY — `tag=FEASIBILITY`, `gated=no`, `prereg_status=NONE` in the queue entry (Sanaa's feasibility-first ruling, no freeze required). This run carries NO VERDICT: it is not graded, nothing it produced may be cited as a result, and THIS IS A COST ROW, NOT A RESULT ROW.** | **POINT 1.0 core-min; CAP 10.0 core-min** hard (= 10x POINT, wired as `timeout` 600 wall s at 1 rank), registered in the queue entry's `cost_core_min_estimate` / `cap_core_min_registered` / `cost_basis` before launch = **$0.000855 POINT and $0.008550 CAP, DERIVED NOT MEASURED** at $0.0513/core-h (c7a.4xlarge, **reported-by-owner** — Sanaa 2026-08-21/22; the box cannot read its own billing, `COMPUTE_BUDGET_CHARTER.md` §5). **What the estimate was actually based on, quoted from its own `cost_basis`:** a measured anchor of 0.017 core-min (1.02 core-s) for T20's 240-cell 3000-step solid-only `chtMultiRegionFoam` case, i.e. 0.34 ms per step at 240 cells; scaled to 960 cells (4.0x) and 1800 steps (0.6x) that is **2.4 core-s = 0.040 core-min of solve**. The POINT was then set to 1.0 core-min, **exactly 25.0x that cell-scaled figure** (verified here by arithmetic), on the stated expectation that "per-step cost is NOT the dominant term here" because the case writes 180 field sets and runs a probes function object every one of 1800 steps — citing T20's own feasibility probe, which measured **90.7 % of wall as startup and I/O rather than solving** (T20_LC_FEAS, 3 s wall against 0.28 s `ExecutionTime`). The basis labels itself **a GUESS** and says so in terms: first 8-block multi-patch `chtMultiRegionFoam` case in the lab, **no measured I/O rate existed for it**, and the 10x cap headroom was "deliberate and reflects that the POINT rests on an unmeasured I/O term" | **0.016667 core-min on rule 12's own basis** (wall s x ranks / 60 = **ClockTime 1 s** x 1 rank / 60); `STATUS.T25_MOD_L1` records the rounded **`core_min=0.017`**, `wall_s=1`, `ranks=1`, `rc=0`, `capped=no`, `note=clean`, `started_utc` 23:31:23Z → `ended_utc` 23:31:24Z. = **$1.43e-05 DERIVED, NOT MEASURED**. **THIS FIGURE IS QUANTISATION-LIMITED AND IS NOT A PRECISE MEASUREMENT — the uncertainty is of the same order as the value itself.** `ClockTime` prints as an INTEGER second and the STATUS timestamps are also 1-s resolution, so a 1-second run carries a **±1 s quantisation = ±100 %** on this number. Two DIFFERENT time bases exist and this row deliberately does not fuse them: **ClockTime 1 s is the rule-12 basis** (rule 12 says wall s x ranks / 60, and ClockTime is wall); **`ExecutionTime` 0.77 s is the BETTER-RESOLVED figure** (0.01 s resolution, final log line `ExecutionTime = 0.77 s  ClockTime = 1 s`) **but it is CPU time, not wall, and is therefore NOT the rule-12 basis** — it is admissible only as a **lower bound** on wall for this single-rank serial run. Combining the two: **true wall ∈ [0.77, 2.0) s, so the actual ∈ [0.012833, 0.033333] core-min = $1.10e-05 to $2.85e-05 derived.** The point figure is quoted as 0.0167 core-min and the band is quoted with it, never the point alone | **= gross, 0.016667 core-min.** A single run row at ≈1 wall s against the 3600-s stall rule — the rule matches nothing and there is nothing to clean out. **WASTE: 0.000 core-min, named and nil.** Nothing stalled, nothing was capped (`capped=no`, 1 s against a 600 s timeout = 0.17 % of it), nothing was killed and nothing was re-run; the run completed with `rc=0`, an `End` line, the `endTime` 900 directory present, and all 1800 `Time =` steps in `log.solve`. **No contention penalty is claimed or deducted:** `loadavg_at_launch=0.27/2.82/5.72` on `nproc=16`, i.e. a quiet box at 1-minute load 0.27, and the figure is reported gross regardless | **0.017 actual/predicted — an OVER-PREDICTION of roughly 59x**, and the caveat travels with the number: **58.8x on STATUS's rounded 0.017, 60.0x on the exact 1 s / 60, and the ±1 s quantisation puts the honest band at 30x to 78x over-predicted** (wall 2.0 s → 30.0x; wall 0.77 s → 77.9x). Against the registered cap the run spent **0.0017x of 10.0 core-min**. **No overrun; the cap was never approached and never bound.** The over-prediction is real and large at every point in the band — the quantisation changes the figure's precision, not its sign or its order | **MISPREDICTION — not waste and not contention** (`COMPUTE_BUDGET_CHARTER.md` §6: waste stays separately named and is nil here; the run completed clean on a quiet box). **The specific cause, stated honestly because the estimate documented its own reasoning: the ~25.0x deliberate inflation above the cell-scaled 0.040 core-min was bought entirely on the expectation that I/O would dominate, and THAT EXPECTATION WAS WRONG AT THIS SIZE.** The expectation was not wrong because the I/O failed to happen — **it was checked here and it did happen**: 181 time directories written, 1802 lines in `postProcessing/module/moduleMinMax/0/fieldMinMax.dat` (1800 per-step rows plus header), the `cellProbes` output present, all 1800 steps executed. **The registered I/O load was fully performed and the whole run still finished inside one second.** Second, smaller, and in the same direction: **even the un-inflated cell-scaled figure over-predicted**, 0.040 core-min against ≈0.0167 measured (2.4x), so linear-in-cells x linear-in-steps from the T20 anchor was itself conservative. One observation offered as consistent-with rather than proven, because ±1 s cannot resolve it: the measured 0.017 core-min **equals T20's 240-cell anchor figure to the printed precision** despite 4.0x the cells and 0.6x the steps — both runs sit on the ≈1-second measurement floor, which is what fixed startup dominating at both sizes would look like, but this row does **not** claim to have measured that. **CALIBRATION LESSON, and it is the row's content: the I/O-dominance inflation does not apply to a 960-cell case whose entire solve is sub-second — T20_LC_FEAS's 90.7 %-non-solving finding describes a run whose FIXED startup cost dwarfs a tiny solve, and re-using it as a multiplier on a per-step basis charges that fixed cost 25 times over. A startup/I/O overhead is an ADDITIVE constant of order one second, not a multiplicative factor on per-step cost; price it as `(cells x steps x per-step basis) + (a fixed ≈1-2 core-s startup and I/O term)` and the 1.0 core-min POINT becomes ≈0.05 core-min.** Second lesson, on instrumentation rather than estimation: **at this scale the lab's cost instrument is at its resolution floor — a sub-second run cannot be measured to better than ±100 % from integer-second `ClockTime`, so any future row at this size should either record a higher-resolution wall clock at launch or state the band, as this row does, rather than print a point figure that looks precise** | `verification/runs/T-family/T25_MODULE_runs/STATUS.T25_MOD_L1` (the actual: `wall_s`, `ranks`, `core_min`, `rc`, `capped`, `note`, `tag`, `gated`, timestamps, `loadavg_at_launch`, `nproc`); `verification/runs/T-family/T25_MODULE_runs/T25_MOD_L1/log.solve` (final `ExecutionTime = 0.77 s  ClockTime = 1 s`, the `End` line, 1800 `Time =` steps); `verification/queue/heat-transfer/refused/T25_MOD_L1.json` (the predicted POINT 1.0, CAP 10.0, the `cost_basis` quoted above, and its own `calibration_owed` clause requiring this row); `verification/runs/T-family/T25_MODULE_runs/T25_MOD_L1/CASE.txt` (case provenance and SIMPLIFICATIONS). All figures re-measured at source for this row; no figure copied from a dispatch brief. **No prior T25 row existed** — established over both `git show HEAD:docs/COST_CALIBRATION.md` and the worktree copy with a word-boundary search under a two-armed control (positive arms T18 → 2 lines and T23 → 5 lines both non-zero, proving the reader can see a hit; negative arm T77QQ → 0; T25 → 0 in both copies), the boundary being required because bare-token `T25`/`T23` collide with the ISO timestamps inside this file's own allocated row ids |
| C-20260901T010947.592980Z-d48a2811 | 2026-09-01 | heat-transfer | **T25_MOD_L1_DT025 — the `deltaT` = 0.25 s STEP-INDEPENDENCE ARM of the battery-module 8-block multi-patch conduction feasibility run** (`chtMultiRegionFoam`, solid region `module`, 960 cells, **3600 steps**, `endTime` 900, 1 rank), the second of the two arms behind `verification/runs/T-family/T25_MODULE_runs/T25_STEP_INDEPENDENCE.md` (`f5b43403`). **UNGATED FEASIBILITY — `tag=FEASIBILITY`, `gated=no`, `prereg_status=NONE` in the queue entry (Sanaa's feasibility-first ruling, no freeze required). This run carries NO VERDICT: it is not graded, nothing it produced may be cited as a result, and THIS IS A COST ROW, NOT A RESULT ROW.** | **POINT 2.0 core-min; CAP 10.0 core-min**, read at source from the queue entry's machine fields `cost_core_min_estimate = 2.0` and `cap_core_min_registered = 10.0` in `verification/queue/heat-transfer/launched/T25_MOD_L1_DT025.json` = **$0.001710 POINT and $0.008550 CAP, DERIVED NOT MEASURED** at $0.0513/core-h (c7a.4xlarge, **reported-by-owner** — Sanaa 2026-08-21/22; the box cannot read its own billing, `COMPUTE_BUDGET_CHARTER.md` §5). **THIS IS NOT THE BASELINE ARM'S 1.0, AND GETTING THAT RIGHT WAS THE FIRST THING THIS ROW OWED:** the entry's `enqueued_by` states the basis in its own words — *"dt=0.25 s step-independence arm; est 2.0 core-min DERIVED (~2x the dt=0.5 run)"*. **BOOKKEEPING DEFECT IN THE ENTRY, DISCLOSED RATHER THAN ABSORBED:** the entry was cloned from `T25_MOD_L1` and three of its fields did not move with the clone — `deltaT = 0.5` and `n_steps = 1800` are **the BASELINE's values** (the case actually run carries `deltaT 0.25` in its own `system/controlDict` and 3600 `Time =` lines in `log.solve`, both re-read for this row), and the long `cost_basis` prose still opens *"POINT 1.0 core-min"* and still scales its arithmetic to 1800 steps. **The registered POINT is nonetheless unambiguous — the machine field reads 2.0 and `enqueued_by` reads 2.0, two independent statements that agree** — and under the entry's own `_field_classes` block `cost_core_min_estimate` is INFRASTRUCTURE, so a defect here **voids only a cost claim and never physics (L-342)**; it does not void this one | **0.016667 core-min on rule 12's own basis** (wall s × ranks / 60 = **`ClockTime` 1 s** × 1 rank / 60); `STATUS.T25_MOD_L1_DT025` records the rounded **`core_min=0.017`**, `wall_s=1`, `ranks=1`, `rc=0`, `capped=no`, `note=clean`, `started_utc` 2026-09-01T00:32:33Z → `ended_utc` 2026-09-01T00:32:34Z. = **$1.425e-05 DERIVED, NOT MEASURED**. **THIS FIGURE IS QUANTISATION-LIMITED AND IS NOT A PRECISE MEASUREMENT.** Two DIFFERENT time bases exist and this row does not fuse them: **`ClockTime` 1 s is the rule-12 basis** (rule 12 says *wall* s × ranks / 60, and `ClockTime` is wall); **`ExecutionTime` 1.38 s is the BETTER-RESOLVED figure** (0.01 s resolution, final log line `ExecutionTime = 1.38 s  ClockTime = 1 s`) **but it is CPU time, not wall, and is therefore NOT the rule-12 basis** — admissible only as a **lower bound** on wall for this serial single-rank run with no threaded numerics. **AND HERE THE TWO BASES OPENLY CONTRADICT EACH OTHER, WHICH THEY DID NOT ON THE BASELINE ARM: CPU 1.38 s EXCEEDS the integer wall print of 1 s, so the rule-12 point figure is PROVABLY AN UNDERSTATEMENT of true wall rather than merely imprecise.** Combining the two: **true wall ∈ [1.38, 2.0) s, so the actual ∈ [0.023000, 0.033333] core-min = $1.966e-05 to $2.850e-05 derived.** The point figure is quoted as 0.016667 core-min and **the band is quoted with it, never the point alone** | **= gross, 0.016667 core-min** (band [0.023000, 0.033333] core-min). A single run row at ≈1–2 wall s against the 3600-s stall rule — the rule matches nothing and there is nothing to clean out. **WASTE: 0.000 core-min, named and nil.** Nothing stalled, nothing was capped (`capped=no`; ≈1 s against a registered `timeout_s` of 1200 = 0.08 % of it, and neither `CAP_OVERRUN.txt` nor `ESTIMATE_OVERRUN.txt` exists in the case directory), nothing was killed and nothing was re-run. **Completion re-checked here rather than inherited:** `rc=0`, exactly one `End` line, 3600 `Time =` steps and 3600 `ExecutionTime` lines, the `900/module/` directory present holding `T` and `p` — the field list `CASE.txt` registers for a fluid-less solid case, **not** the thermal-family default — 181 written time directories, 3602 lines in `postProcessing/module/moduleMinMax/0/fieldMinMax.dat` (3600 per-step rows plus header), and **the rule-4 age guard passes**: `900/module/T` at 00:32:34.836Z is newer than the case's own `0/module/T` at 00:32:33.411Z. **No contention penalty is claimed or deducted:** `loadavg_at_launch=1.47/2.87/2.74` on `nproc=16` — busier at 1-minute load than the baseline arm's 0.27 but still ≈9 % of the box — and the figure is reported gross regardless | **0.008333 actual/predicted on the rule-12 point — an OVER-PREDICTION of 120.0x** (117.6x on STATUS's rounded 0.017). **But the rule-12 point demonstrably UNDER-READS on this arm, so the defensible figure is the band and the band is what this row carries: on wall ∈ [1.38, 2.0) s the ratio is 0.011500 to 0.016667, i.e. 60.0x to 87.0x over-predicted.** Against the registered cap the run spent **0.0017x of 10.0 core-min** (0.0033x at the band's top). **No overrun; the cap was never approached and never bound.** The over-prediction is large and one-signed at every point in the band — the quantisation changes the figure's precision, not its sign or its order | **MISPREDICTION — not waste and not contention** (`COMPUTE_BUDGET_CHARTER.md` §6: waste stays separately named and is nil here; the run completed clean with `rc=0` on a box at 1-minute load 1.47 of 16). **The specific cause is nameable and it is NOT the baseline arm's cause: THE ESTIMATE WAS SCALED OFF THE SUPERSEDED GUESS INSTEAD OF OFF THE MEASUREMENT THAT HAD ALREADY LANDED.** The entry's own basis reads *"~2x the dt=0.5 run"* — and 2x the dt=0.5 run's **POINT** (1.0) is 2.0, while 2x the dt=0.5 run's **MEASURED** cost (0.016667 core-min) is 0.033 core-min. **Doubling a prediction already known to be ≈59x high reproduces the entire error and doubles it.** **The correction was on the board and unused, and the timing is the whole point: the baseline arm's calibration row — the `T25_MOD_L1` row of this ledger, landed at `8f1addea` (cited by commit rather than by id, because `scripts/append_record.py` refuses any row that hand-writes a tool-allocated id and it is right to), 2026-08-31T23:41:36Z, carrying the repaired estimator `(cells × steps × per-step basis) + (a fixed ≈1–2 core-s startup and I/O term)`; DT025 was enqueued and launched at 2026-09-01T00:32:32Z — FIFTY-ONE MINUTES LATER — and was priced by doubling the very figure that row had just retired.** Applied to this arm that repaired estimator gives ≈0.16 s + 3600 × 0.34 ms ≈ 1.4 core-s ≈ **0.023 core-min**, which is **exactly the measured band's lower bound**: the lab's corrected estimator would have predicted this run to within its own measurement resolution, and the lab did not use it. **CALIBRATION LESSON, AND IT IS THIS ROW'S CONTENT: WHEN ONE ARM OF A PAIRED STUDY HAS ALREADY BEEN MEASURED, THE SECOND ARM IS PRICED FROM THAT MEASUREMENT, NEVER BY APPLYING A RATIO TO THE FIRST ARM'S ESTIMATE — a multiplier on a superseded guess carries the guess's error through untouched, and the second arm is precisely the moment the lab has its best evidence in hand.** Second lesson, on the instrument rather than the estimate: **at this scale the two time bases now DISAGREE in a direction that matters — integer `ClockTime` reports 1 s while CPU alone is 1.38 s — so a sub-second-to-second run must record a higher-resolution wall clock at launch if its cost is ever to be more than a band.** One observation offered as **CONSISTENT-WITH, NOT PROVEN**, both because ±1 s cannot resolve it and because a two-point fit of a two-parameter model is exact by construction rather than evidence: the two arms' `ExecutionTime` figures are **0.77 s → 1.38 s for exactly 2.000x the steps, i.e. 1.792x and not 2.000x**, and the additive model that pair implies is a **fixed 0.1600 s of startup plus 3.389e-04 s (0.339 ms) per step at 960 cells** — arithmetic re-derived independently by this lane, not taken from a brief. Two further consistent-with remarks, neither claimed as measured: **0.339 ms/step at 960 cells sits essentially on T20's 0.34 ms/step anchor at 240 cells**, so per-step cost did not scale with a 4.0x cell count at this size, which is what fixed per-step overheads dominating would look like; and **the implied 0.16 s fixed term is roughly one-tenth of the ≈1–2 core-s the baseline row proposed**, so that row's repaired formula is itself conservative in the same direction as everything else here | `verification/runs/T-family/T25_MODULE_runs/STATUS.T25_MOD_L1_DT025` (the actual: `wall_s`, `ranks`, `core_min`, `rc`, `capped`, `note`, `tag`, `gated`, timestamps, `loadavg_at_launch`, `nproc`); `verification/runs/T-family/T25_MODULE_runs/T25_MOD_L1_DT025/log.solve` (final `ExecutionTime = 1.38 s  ClockTime = 1 s`, the `End` line, 3600 `Time =` steps); `.../T25_MOD_L1_DT025/system/controlDict` (`deltaT 0.25`, `endTime 900.0`, `writeInterval 5.0`); `.../T25_MOD_L1_DT025/CASE.txt` (case provenance, the rule-4 field list {`T`, `p`}, and the SIMPLIFICATIONS); `verification/queue/heat-transfer/launched/T25_MOD_L1_DT025.json` (the predicted POINT 2.0 and CAP 10.0, the `calibration_owed` clause requiring this row, and the three stale cloned fields disclosed above); the baseline arm's `T25_MOD_L1` row of this ledger, landed at `8f1addea`; the step-independence record `T25_STEP_INDEPENDENCE.md` at `f5b43403`. **All figures re-measured at source for this row; no figure was copied from a dispatch brief, and the brief's own reading of the predicted POINT was checked against the queue entry before use.** **No prior DT025 row existed** — established over both `git show HEAD:docs/COST_CALIBRATION.md` (147 `C-` rows) and the worktree copy (147 rows) with a **word-boundary** search driven under a **two-armed control**: positive arms `\bT25_MOD_L1\b` → 1 line and `\bheat-transfer\b` → 65 lines, both non-zero, proving the reader can see a hit; negative arm, a **FRESH** sentinel `WQ7ZBX4M9` → 0 at HEAD, 0 in the worktree and 0 repo-wide; target `\bDT025\b` → 0 in both copies. **The sentinel had to be fresh, and that is L-420: a negative control is SPENT the moment a landed row names it — the prior lane's `T77QQ` now returns 1 occurrence at HEAD and would have read as a false positive had it been reused.** The word boundary is not decoration either: bare `T25` returns **12** occurrences against **3** for `\bT25\b`, the surplus being ISO timestamps inside this file's own tool-allocated row ids |
| C-20260901T011549.334202Z-a4e8e8fa | 2026-09-01 | dafoam | **A2 drag decomposition — attributing the 28.3 % drag reduction on the 38,304-cell wing between twist, section shape and angle-of-attack retrim** (`DARhoSimpleFoam`, compressible SA RANS, 4 ranks, 7 registered design-variable rows evaluated in one container session; `runScript_AeroOnly.py` plus one added task block). **PROCESS COMPLETE, GRADED.** Six of seven rows landed; gates G1, G2, G3, G6 **PASS** with both planted controls clean; **B2 NOT A RESULT** (lift-trim search stalled from a 0.222 lift excursion, then a diverged primal) and therefore **G5, the registered falsifier, is UNGRADED**. Registered share predictions both held: twist **-0.92 %** against a registered band of -5 % to +8 %, section shape **+100.92 %** against 92 % to 105 % | **POINT 32 core-min; CAP 60 core-min** hard, frozen before any compute at `899352ae5c9f81a90b47d75d6642d80c8364b3bf` (`A2_DRAG_DECOMPOSITION_PREREGISTRATION.md` §8) and wired as `timeout 900s` at 4 ranks inside the container. **Basis, stated in the registration: 16 primals at 24.7 s wall x 4 ranks**, the 24.7 s taken from the published optimisation's own 3606 s / 146 evaluations (`A2_mach_tutorial_wing.md` §5-§6), plus 5.3 core-min for mesh build and OpenMDAO setup from the W4 rebuild's measured 2.67 core-min (`PROOF.md:2526`). = **$0.027 POINT, $0.051 CAP, DERIVED NOT MEASURED** at $0.0513/core-h c7a.4xlarge, **reported-by-owner** (Sanaa 2026-08-21/22; the box cannot read its own billing, `COMPUTE_BUDGET_CHARTER.md` §5) | **13.87 core-min**, basis **gross**, MEASURED as `wall_s x ranks / 60` from the launcher's own `t0`/`t1` ledger (208 s x 4 ranks) and recorded by the launcher itself in `cost.txt`. Corroborated by a second instrument: the log's own cumulative `ExecutionTime` across all seven rows totals **193.01 solver-seconds = 12.87 core-min**, the 1.00 core-min difference being mesh generation, container start and OpenMDAO setup, which `ExecutionTime` does not count. = **$0.0119 DERIVED, NOT MEASURED** | **11.11 core-min cleaned.** Append rule 4's 3600-s stall test **matches nothing** (208 s total). The cleaned figure here removes **WASTE, which is named separately and never folded into the ratio or its explanation** (`COMPUTE_BUDGET_CHARTER.md` §6): **2.76 core-min — row B2's 3 primal solves, which produced no value.** Per-row solver cost, re-derived from the log for this row: A0 1.03, A1 0.91, A2 0.92, A3 0.90, A4 0.91, B1 5.44, B2 2.76 core-min. **No contention** (box idle at launch, zero other containers, verified before launch); **nothing capped** — 13.87 against 60 is 23 % of cap, and the 900-s `timeout` never fired | **0.43x gross/predicted** (13.87 / 32); **0.35x on the cleaned figure** (11.11 / 32); **0.23x of cap**. **An OVER-prediction of 2.3x** | **MISPREDICTION — not waste-driven and not contention.** (The 2.76 core-min of real waste is named in the cleaned column and is deliberately NOT used to explain the ratio; the run would have been 0.43x with or without it.) **The primal COUNT was predicted well — 16 registered, 14 executed. The entire gap is per-primal TIME, over-priced by 1.8x.** The registration took 24.7 s/evaluation from the optimisation log's 3606 s / 146 evaluations, **but that wall time also absorbed 47 gradient/adjoint computations**, which no row of this run performs. Measured here: **13.6-15.4 solver-seconds per primal** (A0 15.38, A1 13.61, A2 13.73, A3 13.56, A4 13.71). **CALIBRATION LESSON, and it is this row's content: a per-evaluation rate read off an OPTIMISATION log prices primal-plus-gradient work and must never be reused unadjusted for a primal-only run — either divide the gradient passes out of it, or measure one primal before costing the rest.** The same arithmetic applied here in advance (3606 s less the adjoint work, or simply one timed primal) would have given ~19 core-min against 13.87 measured. Second, smaller observation, offered as **consistent-with rather than measured**: B1's trim needed 6 primals against the 6 budgeted while B2's needed more than 3 and never converged, so the registration's per-row trim budget was sound and the B2 loss is a robustness failure of the lift-trim search from a far starting point, not a costing error | `cases/dafoam/A2_DRAG_DECOMPOSITION_PREREGISTRATION.md` RESULTS addendum §R6 (and §R1-§R5 for the graded rows), landed this commit; pre-registration frozen at `899352ae5c9f81a90b47d75d6642d80c8364b3bf`, committed `e23145f7` with pre-compute amendment 1 at `c8c470b8`; run artifacts `/home/ubuntu/certonomous-runs/ACTD-a2-decomposition/` (`decomp.log`, `cost.txt`, `t0`, `t1`, `RUN_RC.txt`, `instrument_sha256.txt`, `driver_vs_pristine.diff`). **All figures re-measured at source for this row from the run's own log and ledger; no figure copied from a dispatch brief.** **No prior A2-decomposition row existed** — checked over `git show HEAD:docs/COST_CALIBRATION.md` under a two-armed control: positive arms `dafoam` and `A2` both return non-zero, proving the reader can see a hit; a fresh negative sentinel `ZQ4XKV8P2` returns 0 at HEAD, 0 in the worktree and 0 repo-wide; the target `A2_DRAG_DECOMPOSITION` returns 0 in both copies |
| C-20260901T033347.472427Z-678c82d9 | 2026-09-01 | dafoam | **Act D live-meshing viability — the mesh-time measurement, the ONE run in the Act D demo-mode prep.** pyHyp hyperbolic extrusion from a CGNS surface mesh plus `plot3dToFoam` / `autoPatch` / `createPatch` / `renumberMesh`, in `dafoam/opt-packages:latest`, `--cpus=4`, **pipeline serial (1 rank)**. Identity assert holds: `renumberMesh` reports `Mesh region0 size: 38304`, so this timed **Act D's own grid** and not another. Verdict `PASS` against the pre-registered 60 s rule. The other three deliverables of that prep (replay-series extraction, geometry identity, language sweep) booked **zero solver compute** and are not costed here | **No point estimate was registered — a BAND was.** `ACTD_MESH_TIME_PREREGISTRATION.md` registered total mesh wall time in **20–300 s**, which at the 4-core reserved quota is **1.333–20.0 core-min** (0.333–5.0 core-min at the 1 rank actually executing). Registered **CAP 40 core-min** (4-core basis) / 10 core-min (1-rank basis), enforced as a 600 s `timeout` inside the container wrapper with rc captured inside it. Frozen before compute at `6e911354` | **0.585 core-min** (4-core reserved-quota basis) = 8.774 s container wall × 4 ÷ 60. At the **1 rank actually executing: 0.146 core-min**. Both bases are reported because the pipeline is serial inside a 4-core reservation and one number alone would misstate it. Dollars **DERIVED, NOT MEASURED**: **$0.0005** at $0.0513/core-h c7a.4xlarge, **REPORTED-BY-OWNER** (`COMPUTE_BUDGET_CHARTER.md` §5) | **0.585 core-min — gross and cleaned are the same figure.** No row approaches the 3600 s stall rule (the whole invocation is 8.774 s), and **no waste was incurred**: one invocation, rc=0, no retry, no stall | **0.44 against the band's LOWER end** (0.585 / 1.333) and **0.029 against its upper end** (0.585 / 20.0). Against the cap: **0.015** (0.585 / 40). **The registered band was FALSIFIED — the run fell BELOW it**, and the record says so in its own predictions block rather than presenting 8.021 s as the expected answer | **MISPREDICTION, over-prediction, and mine.** I registered 20–300 s expecting a three-dimensional hyperbolic extrusion at 38,304 cells to cost tens of seconds. It cost **8.021 s** for the mesh stages. **The attributable cause is measured, not guessed:** the extrusion marches only **39 layers off a 1,008-face surface**, so the work is small however large the resulting volume mesh is, and pyHyp took **5.547 s = 69.2 %** of the mesh time — the one prediction that HELD. **No contention** (single invocation on an otherwise-quiet box) and **no waste** are being folded into this ratio; the whole gap is my estimate | `cases/dafoam/ladder-a/A2_mesh_time.json`, committed `c5d983c2`; pre-registration `cases/dafoam/ACTD_MESH_TIME_PREREGISTRATION.md`, committed `6e911354` **before** the run; runner `cases/dafoam/run_actd_meshtime.sh` |
| **C-20260901T041827.958337Z-26909c1d** | 2026-09-01 | dafoam | **`CURRICULUM-SO3` — A1 NACA0012 alpha-multipoint SHAPE OPTIMISATION, both rows, item `PASS`.** The completion this row calibrates is the graded item, not a stage: chain `COMPLETE declared=7 executed=7`, all seven arms `rc=0`, graded `20260901T040709Z`. | **228.59 core-min REGISTERED point**, summed by this row from the ten-column `G10_caps.per_arm.predicted` fields of the grade JSON (MESH 0.19 + O-S 103.0 + O-P 103.0 + XE-S 3.7 + XE-P 3.7 + FE-S 7.5 + FE-P 7.5). Registered ceiling **595.0 core-min**; per-arm caps MESH 5.0, O 240.0 each, XE 15.0 each, FE 40.0 each. `ranks 1` on every arm. | **28.900 core-min MEASURED**, basis gross, the grader's own `G10_caps.total_core_min`, cross-checked by this row against the seven `arm_census` rows (0.167 + 7.917 + 6.950 + 2.350 + 2.533 + 4.450 + 4.533 = 28.900). **$0.02471 DERIVED, NOT MEASURED** at c7a.4xlarge $0.0513/core-h — the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` §5). | **= gross. 28.900 core-min.** No arm approaches the 3600-wall-s stall rule: the longest arm is O-S at 7.917 core-min which at `ranks 1` is **475 wall s**, and the whole seven-arm chain ran 03:30:48Z to 04:07:09Z. Nothing is cleaned out because nothing stalled. | **0.126x — the estimate over-predicted by very nearly 8x.** No cap was crossed on any arm (`crossed: false`, 7 of 7) and the ceiling was used to **4.9 %**. | **MISPREDICTION, conservative direction, and it is concentrated in ONE registered quantity.** The two IPOPT arms carry the whole gap: predicted 103.0 core-min each, actual **7.917** (O-S, ratio 0.077) and **6.950** (O-P, ratio 0.067). The registration priced the optimiser at its registered `max_iter` of **50 majors**; IPOPT converged on its own tolerance in **10 majors** — a 5x over-count that the other 4.2 core-min of gap does not come close to explaining. The non-optimiser arms were predicted well and all UNDER: MESH 0.879x, XE-S 0.635x, XE-P 0.685x, FE-S 0.593x, FE-P 0.604x — a consistent ~0.6-0.9x that is ordinary conservatism, not a modelling error. **The lesson for the next optimisation registration in this family is to price the optimiser at an EXPECTED major count with `max_iter` as the CAP, not to price `max_iter` itself as the point estimate.** The item's own registered prediction `P_COST_total_core_min_in_band` is recorded **MISS** in the grade JSON's `predictions` block, so the miss was declared by the instrument and is not being discovered here. **WASTE, NAMED SEPARATELY AND NOT ABSORBED INTO THE RATIO (`COMPUTE_BUDGET_CHARTER.md` §6): 0.167 core-min**, the single MESH arm of the accidental self-test launch at 02:41:37Z, quarantined at `/home/ubuntu/certonomous-runs/QUARANTINE-SO3-selftest-accidental-launch-20260901T0242Z` with `chain=NOT A RESULT declared=7 executed=1`. Its immediate re-launch at 02:42:59Z spent **zero** — the driver refused it `chain=REFUSED_ALREADY_BOUGHT arm=MESH`, which is the already-bought guard working. Waste is **0.58 %** of the item's spend. | `/home/ubuntu/certonomous-runs/CURRICULUM-SO3-a1-naca0012-alpha-multipoint-optimisation/SO3_grade_20260901T040709Z.json` (`G10_caps`, `arm_census`, `predictions`); pre-registration `cases/dafoam/ladder-a/A1/curriculum_SO3/PREREGISTRATION.md` at `ab27dff7` (2026-09-01T03:21:46Z, **before** the 03:30:48Z first arm); grading path `cases/dafoam/ladder-a/A1/curriculum_SO3/so3_grade.py` md5 `0ac111ef144a62111e36f676e8114af1`, verified by this supervisor as worktree == `HEAD` blob == the Amendment 2 pin. |
| C-20260901T043545.604474Z-65baf296 | 2026-09-01 | cfd | **DMR (Woodward–Colella double Mach reflection) — THE ITEM AT COMPLETION, both rungs: `res120` (h = 1/120, 480 × 120 = 57,600 cells, 2,111 steps) and `res60` (h = 1/60, 240 × 60 = 14,400 cells, 1,008 steps), native openfoam2606 `rhoCentralFoam`, Kurganov flux, inviscid, **4 MPI ranks** each. Graded outcome: **Gate V `PASS` both rungs** (0.146 % and 0.173 % of travelled distance against a pre-registered ±1.0 %); **Gate P1 `GATE FAIL`** as registered (detector tested the wrong locus — the canonical second triple point is on the reflected shock, not the leading front); **Gate P2 split** — rung-to-rung clause `PASS` (0.87° ≤ 1.5°), within-rung intercept clause `GATE FAIL` (21.6 / 21.2 cells against a 2-cell tolerance, diagnosed as a threshold-bias instrument artifact constant in cells). **THIS ROW IS LANDED 2026-09-01 FOR A PROCESS COMPLETED 2026-08-07T22:45Z: the calibration obligation (`CLAUDE.md` rule 12, Sanaa 2026-08-23) POST-DATES the run by sixteen days, which is why no row existed. Stated so its absence is not read as an obligation missed at the time** | **20.0** core-min, `DMR_PREREGISTRATION.md` §6 (*"Budget 20 core-min, 4 MPI ranks, setsid, native"*), frozen `74797a57` 2026-08-07T22:40:28Z — **3 min 08 s before the earliest artifact of either case** | **2.4** core-min. **Re-derived in this lane from the run markers rather than relayed:** `res120/.t0` 20260807T224408Z → `.t1` 20260807T224433Z = 25 s; `res60/.t0` 20260807T224510Z → `.t1` 20260807T224513Z = 3 s; 28 s × 4 ranks = **1.87 core-min of solve**, plus mesh, `setExprFields`, `decomposePar`, `reconstructPar`, `writeCellCentres` and the locator at **< 0.5**. `res120`'s own `/usr/bin/time` block records 25.09 s elapsed, `Exit status: 0`. Agrees with `DMR_RESULTS.md` cost section | **2.4 — identical to gross.** Longest step is 25.09 s wall, so **no row matches the `COMPUTE_BUDGET_CHARTER.md` §2 3600-s stall rule** and there is nothing to clean | **0.12** | **MISPREDICTION, ~8× on the cheap side — outside the factor-3 rule, and the record grades its own estimate rather than being told to.** Decomposition: the F4-anchored per-rung guess was roughly right in cells, but the acoustic-dt margin (*"plus margin for the acoustic time step at Mach 10"*) was priced as if dt-limiting dominated, while `rhoCentralFoam` at `maxCo 0.2` took only **2,111 steps on 57,600 cells** and 1,008 on 14,400. **NOT contention:** both solves ran to completion, no stall, no re-run, no cap moved, no overrun. **WASTE: 0.000 core-min, NAMED SEPARATELY AND NOT ABSORBED INTO THE RATIO** (`COMPUTE_BUDGET_CHARTER.md` §6) — nothing abandoned, nothing re-run, and the grading and locator cost no solver compute. **FORWARD CALIBRATION, the point of the row:** a 1/240 successor rung prices at **~14.0 core-min** from this measurement (8.36× `res120`: 4× cells and 2.09× steps), now registered as the estimate in `verification/campaign/DMR_R3_TRIPLE_PREREGISTRATION.md`. **$0.0021 DERIVED, NOT MEASURED** — 2.4 core-min = 0.040 core-h at $0.0513/core-h, c7a.4xlarge, reported-by-owner (`COMPUTE_BUDGET_CHARTER.md` §5: the box cannot read its own billing) | `verification/campaign/DMR_RESULTS.md` (cost section, and the three gate sections); run markers `verification/runs/DMR_runs/res{120,60}/.{rc,t0,t1}`; registry `demo-output/website/solve_registry/dmr_res{120,60}_20260807T22*.done`; pre-registration `verification/campaign/DMR_PREREGISTRATION.md` frozen `74797a57`, blob `a08ee245` identical at HEAD |
| C-20260901T045449.555459Z-dbe00002 | 2026-09-01 | cfd | **DMR rung R3 (h = 1/240, 960 × 240 = 230,400 cells, `rhoCentralFoam`, Kurganov + vanLeer, 4 MPI ranks) — THE RUNG AT TERMINATION, AND IT TERMINATED ON A CRASH.** `rhoCentralFoam` returned **rc = 136 = 128 + 8 (SIGFPE)** at **t = 0.10863175** of an `endTime` of 0.2 — 54 % of the way — after 120 s on 4 ranks; every prior step returned rc = 0 and `checkMesh` read **max non-orthogonality 0, `Mesh OK`**, so the mesh is not implicated. **Verdicts: Gate V3 `NOT A RESULT`** (no reading exists at t = 0.2 — not a `GATE FAIL`, because nothing was measured against the tolerance); **Gate T `BLOCKED`** (a Roache triple needs three levels and only two exist; no classification, no observed order and no GCI is computed or quotable); **the rung as a whole `NOT A RESULT`**. **The two existing rungs are untouched — Gate V stays `PASS` at 1/60 and 1/120.** Triage: the deepest named stack frame is `Foam::sqrt(Field<double>&, UList<double> const&)` caught by `Foam::sigFpe::sigHandler` — a **locally negative temperature** in the sound-speed evaluation; it is a **trapped** exception (`trapFpe … enabled (FOAM_SIGFPE)`), **not a silent NaN**; and it is **not a time-step runaway** — the final three steps hold `deltaT` constant at 4.3225304e-05 with max Courant **0.1998 against the registered `maxCo` 0.2**. The last written field at t = 0.10 is **completely healthy across all 230,400 cells — T min 1.000000 exactly the pre-shock value, rho min 1.4, p min 1, ZERO negatives** — so the failure is sudden and local, ~200 steps from a clean state | **16.0** core-min, `DMR_R3_TRIPLE_PREREGISTRATION.md` §6, frozen `68742cec` **before** `res240` existed. **HARD CAP 35.0** | **8.48** core-min MEASURED: **8.43** from the wrapper's per-step accumulator (`R3_PROGRESS.txt`, each step counted with its OWN rank count — `blockMesh` 2 s, `checkMesh` 2 s, `setExprFields` 19 s, `decomposePar` 3 s all at 1 rank; `rhoCentralFoam` 120 s × 4 = 480 core-s; total 506 core-s) plus **0.05** for a post-crash `reconstructPar -time 0.1` diagnostic | **8.48 — identical to gross.** Longest step is 120 s wall, so **no row matches the `COMPUTE_BUDGET_CHARTER.md` §2 3600-s stall rule** and there is nothing to clean | **0.53 — AND THIS RATIO MUST NOT BE READ AS ESTIMATE QUALITY** | **THE PROCESS DID NOT COMPLETE, SO THERE IS NO ACTUAL-FOR-THE-PREDICTED-WORK TO COMPARE.** The 16.0 priced a run reaching t = 0.2; the run reached 0.1086. **0.53 is SPEND AT THE POINT OF FAILURE, not a calibration signal**, and it is stated rather than withheld — a departure from the `C-95` precedent (which withheld because the work was never done) and closer to `C-100` (where the registered compute ran and the grading failed). **The estimate itself is therefore NEITHER VINDICATED NOR FAULTED by this row**, and the one forward-usable figure is the throughput it does establish: the 1/240 rung burns **~4.0 core-min per 0.05 t**, so a completing run would have landed near **15.5 core-min — within 3 % of the 16.0 filed**, which is the only calibration content here and is offered as such. **CAP NOT BREACHED** — `R3_CAP_BREACH.txt` absent; the wrapper's own accumulator stopped nothing because nothing approached 35.0. **NO OVERRUN, NO RE-RUN, NO RETUNE:** the filing names any change of scheme, constants, boundary conditions, `maxCo`, write times or rank count as a disqualifier because the triple requires ONE numerics family, so retuning to make it run would produce a rung that runs and a triple that means nothing. **WASTE: the full 8.48 core-min bought NO GRADED OUTPUT and is NAMED IN FULL rather than absorbed into the ratio** (`COMPUTE_BUDGET_CHARTER.md` §6). **Whether it books as waste or as the price of a discovered robustness ceiling is the supervisor's ruling; this row does not book it as a result.** **What it did buy, and it is not nothing: a negative result with a mechanism — the DMR numerics family has a resolution ceiling between 1/120 and 1/240, failing by negative temperature under a trapped FPE rather than degrading quietly.** **$0.0073 DERIVED, NOT MEASURED** — 8.48 core-min = 0.1413 core-h at $0.0513/core-h, c7a.4xlarge, reported-by-owner (`COMPUTE_BUDGET_CHARTER.md` §5: the box cannot read its own billing) | `verification/campaign/DMR_R3_RESULTS.md` (commit `1e575d9f`); `verification/runs/DMR_runs/R3_PROGRESS.txt`; `res240/log.rhoCentralFoam`, `res240/log.checkMesh`; pre-registration `verification/campaign/DMR_R3_TRIPLE_PREREGISTRATION.md` frozen `68742cec` |
| C-20260901T050857.406235Z-c96792fa | 2026-09-01 | cfd | **CORRECTION ROW — it corrects **the DMR R3 row dated 2026-09-01 whose record ref is `verification/campaign/DMR_R3_RESULTS.md` (`1e575d9f`)** — cited descriptively, not by id, because `append_record.py` REFUSES any row that hand-writes a tool-allocated id, and this ledger's append rule 1 nonetheless requires a correction to name its target; that conflict is reported to the register's owner (DMR rung R3, h = 1/240, terminated on `rc = 136` SIGFPE at t = 0.10863175 of `endTime` 0.2). THE CORRECTED ROW IS NOT EDITED AND ITS FIGURES ARE UNCHANGED; this row supplies only the disposition that row explicitly left open.** The corrected row closed with *"whether it books as waste or as the price of a discovered robustness ceiling is the supervisor's ruling; this row does not book it as a result."* **THE RULING IS NOW MADE, BY THE cfd SUPERVISOR, 2026-09-01, AND IT IS ADOPTED HERE IN ITS OWN TERMS: the 8.48 core-min is the NAMED PRICE OF A DISCOVERED ROBUSTNESS CEILING — REPORTED, NOT ABSORBED, AND EXPLICITLY NOT WASTE. It bought a real finding about the scheme.** | **16.0** core-min, `DMR_R3_TRIPLE_PREREGISTRATION.md` §6, frozen `68742cec` before `res240` existed; **HARD CAP 35.0, NOT BREACHED** (`R3_CAP_BREACH.txt` absent) | **8.48** core-min, unchanged from the corrected row — 8.43 from the wrapper's per-step accumulator (each step at its own rank count) plus 0.05 post-crash diagnostic | **8.48 — identical to gross**; longest step 120 s wall, so no row matches the `COMPUTE_BUDGET_CHARTER.md` §2 3600-s stall rule | **0.53** | **THE DISPOSITION, WHICH IS THE WHOLE CONTENT OF THIS ROW: THIS SPEND IS NOT WASTE AND IS NOT TO BE COUNTED AS WASTE IN ANY LATER SWEEP OF THIS LEDGER.** `COMPUTE_BUDGET_CHARTER.md` §6 requires waste to be separately named and never absorbed; the ruling is that this is **not** waste to name, because the compute **delivered a result** — a negative one with a mechanism. **What it bought:** the DMR numerics family (Kurganov flux, vanLeer reconstruction, no positivity-preserving limiter) has a **resolution ceiling between h = 1/120 and h = 1/240**, and it fails there by **locally negative temperature under a TRAPPED FPE** (`Foam::sqrt` in the sound-speed evaluation, caught by `Foam::sigFpe::sigHandler`, with `trapFpe … enabled` in the log header) rather than degrading quietly into a plausible wrong answer. **That is a durable, reusable fact about the solver stack**, it is the reason no finer DMR rung should be proposed on this family without a limiter, and it could not have been obtained except by running into it. **The 0.53 ratio remains NOT a calibration signal** and the corrected row's reasoning stands verbatim: the process did not complete, so 0.53 is spend at the point of failure, not an actual for the predicted work; the only forward-usable figure is the measured throughput of ~4.0 core-min per 0.05 t, which projects a completing run at ~15.5 core-min, **within 3 % of the 16.0 filed**. **NO overrun, NO cap breach, NO re-run and NO retune** — the filing makes any change of scheme, constants, boundary conditions, `maxCo`, write times or rank count a disqualifier, because a Roache triple requires ONE numerics family, so retuning to make the rung run would have produced a rung that runs and a triple that means nothing. **$0.0073 DERIVED, NOT MEASURED** — 8.48 core-min = 0.1413 core-h at $0.0513/core-h, c7a.4xlarge, reported-by-owner (`COMPUTE_BUDGET_CHARTER.md` §5: the box cannot read its own billing) | corrects **the DMR R3 row dated 2026-09-01 whose record ref is `verification/campaign/DMR_R3_RESULTS.md` (`1e575d9f`)** — cited descriptively, not by id, because `append_record.py` REFUSES any row that hand-writes a tool-allocated id, and this ledger's append rule 1 nonetheless requires a correction to name its target; that conflict is reported to the register's owner; ruling by the cfd supervisor 2026-09-01 [lab-attributed]; evidence `verification/campaign/DMR_R3_RESULTS.md` (`1e575d9f`), `verification/runs/DMR_runs/R3_PROGRESS.txt`, `res240/log.rhoCentralFoam` |
| C-20260901T051611.671333Z-99dc7165 | 2026-09-01 | cfd | **CLOSING ROW for the DMR rung R3 accounting (h = 1/240, `rhoCentralFoam`, 4 ranks; the run terminated on `rc = 136` SIGFPE at t = 0.10863175 of `endTime` 0.2). It supersedes nothing and adds the ONE thing the two earlier rows left ambiguous: the CALIBRATION VERDICT.** The first row stated the facts and left the disposition open; the second booked the supervisor's ruling that the spend is the named price of a discovered robustness ceiling and NOT waste; **this row is the third and last for this process — no fourth is owed.** | **16.0** core-min, `DMR_R3_TRIPLE_PREREGISTRATION.md` §6, frozen `68742cec` before `res240` existed; **HARD CAP 35.0, NOT BREACHED** | **8.48** core-min, unchanged | **8.48 — identical to gross**, unchanged | **0.53 raw — and the raw ratio is NOT the calibration datum; the sentence below is** | **THE ESTIMATE WAS GOOD AND THE RUN WAS NOT, AND THOSE ARE SEPARATE QUESTIONS.** Stated plainly because the raw ratio says the opposite of the truth: 0.53 reads like a 2× overestimate, and it is nothing of the kind — it is **spend at the point of failure**, because the 16.0 priced a run that reached t = 0.2 and the run reached t = 0.1086. **The estimate is vindicated by the measured throughput, not by the ratio.** The rung burned **~4.0 core-min per 0.05 t** of simulated time; extrapolated over the full `endTime` that is **~15.5 core-min against the 16.0 filed — an error of about 3 %, comfortably inside the factor-3 rule and among the better predictions in this ledger.** **THE FORWARD LESSON FOR ESTIMATING, which is what this ledger is for: a completion-conditional estimate can be RIGHT while its process FAILS, and a ledger that reads only the actual/predicted column will mis-score it in the flattering direction on an overrun and in the punishing direction on a crash.** Where a process terminates early, the honest calibration figure is the **rate**, extrapolated to the registered stopping point, and the raw ratio must be labelled rather than quoted. **NO overrun, NO cap breach, NO re-run, NO retune** — the filing makes any change of scheme, constants, boundary conditions, `maxCo`, write times or rank count a disqualifier, because a Roache triple requires ONE numerics family. **WASTE: NONE — ruled and booked in the preceding correction row; the compute delivered a negative result with a mechanism, now recorded as `N-C8` in `docs/NUMERICS_KNOWLEDGE.md`.** **$0.0073 DERIVED, NOT MEASURED** — 8.48 core-min = 0.1413 core-h at $0.0513/core-h, c7a.4xlarge, reported-by-owner (`COMPUTE_BUDGET_CHARTER.md` §5: the box cannot read its own billing) | closing row of three for the DMR R3 process (the other two are dated 2026-09-01 and cite the same record); `verification/campaign/DMR_R3_RESULTS.md` (`1e575d9f`); `verification/runs/DMR_runs/R3_PROGRESS.txt`; numerics finding `N-C8` (`36095112`) |
| C-20260901T052336.779675Z-eec05762 | 2026-09-01 | cfd | **F13 successor — ONERA M6 tip-topology admission probe: ITEM CLOSED AT ZERO SPEND, WITHDRAWN BEFORE FIRST COMPUTE.** The filing (`F13_TIP_TOPOLOGY_PROBE_PREREGISTRATION.md`, frozen `40ca3c35`) registered three candidate tip closures to be built at L1 and graded against the mesh standard's unchanged ≤ 70° non-orthogonality gate. **AMENDMENT 1 (2026-09-01, `579eaa1f`) STRIKES its prediction 1 and its candidate ordering and WITHDRAWS the rung — no candidate was built, no `checkMesh` was run, Gate M was never evaluated.** Cause: **the answer was already measured on this box three days before the filing was written.** `N-C6` in `docs/NUMERICS_KNOWLEDGE.md`, landed 2026-08-25 by this same team on this exact geometry, measures structured butterfly tip caps at **81.5834° → 82.0645°** rising monotonically to an asymptote, with the severe-face fraction rising **10.7×** while cell count rises only **1.31×**. The filing's prediction 1 — *"at least one of C1/C2 clears, 50–68°"* — was **refuted before it was registered**, and the filing's author did not consult the register. **Independently reproduced here by the arc-length route `N-C6` prescribes**, which costs no compute: the TE strip joins a surface arc `(1−U2)·c` to a core edge `(1−CORE_S)·t2(U2)·c`, giving **R = 15.49:1** at the registered `U2 = 0.90`, `CORE_S = 0.50` — matching `N-C6`'s recorded ~16:1 by a separate route — and **R is nearly flat in break location (18.28:1 at U2 = 0.70 → 14.37:1 at 0.95)** because the section half-thickness goes to **zero** at a sharp trailing edge, which makes it a property of the SECTION rather than the block structure and answers candidate C2 without building it | **4.0** core-min, filing §8; **HARD CAP 20.0** | **0.000** core-min — **no mesh generated, no `checkMesh` invoked, no solver started.** Verified in the amending invocation: `find -type f` over `verification/runs/F13_ONERA_M6_runs/SUCCESSOR_tip_topology_trial/` returns **0 files** | **0.000 — identical to gross.** Nothing ran, so no row can match the `COMPUTE_BUDGET_CHARTER.md` §2 3600-s stall rule | **0.00** | **A RATIO OF ZERO IS NOT AN ESTIMATING TRIUMPH AND IS NOT RECORDED AS ONE — the 4.0 core-min estimate was never tested, because the item was withdrawn rather than run.** This row exists to record a **DIFFERENT AND MORE VALUABLE KIND OF SAVING** than a good prediction: **4.0 core-min and a multi-hour authoring task were avoided outright by reading the lab's own numerics register before spending, and the register answered the question for free.** **THE FORWARD LESSON, which is the whole point of the row: the cheapest core-minute is the one a prior measurement makes unnecessary, and the check that finds it — searching `docs/NUMERICS_KNOWLEDGE.md` for the case's own geometry and mechanism — costs zero and belongs BEFORE a pre-registration is frozen, not after.** The inverse also stands and is the honest half: **had the register been consulted at filing time, a knowably-false prediction would never have been committed at all.** **WASTE: NONE — no compute was spent to be wasted.** The cost actually incurred by the error was **authoring time and one commit that had to be amended**, which this ledger does not measure and which is recorded here in words rather than laundered into a core-minute figure. **HONEST LIMIT ON THE WITHDRAWAL, carried so this row is not read as stronger than it is:** the arc-length ratio is escapable in principle — `R < 10` requires `CORE_S < 0.2256` at `U2 = 0.90` — so the premise is refuted across the **registered and natural design region** (minimum 10.26:1 over `U2` 0.70–0.95, `CORE_S` 0.30–0.70) and **not** across the entire admissible space; a sub-0.23 core scale is **untested and named as an open escape**. **$0.0000 DERIVED, NOT MEASURED** — 0.000 core-min at $0.0513/core-h, c7a.4xlarge, reported-by-owner (`COMPUTE_BUDGET_CHARTER.md` §5) | `verification/campaign/F13_TIP_TOPOLOGY_PROBE_PREREGISTRATION.md` AMENDMENT 1 (`579eaa1f`); `docs/NUMERICS_KNOWLEDGE.md` `N-C6`; `verification/runs/F1_MESH_TRIALS_2026-08-25/topology_study/SUMMARY.txt`; standing outcome `verification/campaign/F13_RESULTS.md` (R0 `GATE FAIL`, case `BLOCKED`) |
| C-20260901T054637.462483Z-6c843833 | 2026-09-01 | dafoam | **CURRICULUM-D19O — the compressible single-point shape optimisation, NACA0012 `DARhoSimpleFoam` M 0.288, both toolchain rows, np = 1, SEVEN ARMS OF SEVEN, `chain_rc=0`. ITEM VERDICT `GATE REACHED` — both rows composed raw `PASS` and were CAPPED by the registered verdict ceiling, because the compressible gradient this item spends has NO GRADED VERDICT (D19R's grader refused `rc=2`; D19R2's grading attempt 1 returned `NOT A RESULT`) and its plateau never closed. The first compressible optimisation ever run in this lab, and the first FD table this lab holds at an OPTIMUM rather than at iteration 0.** | **24.10** core-min, `PREREGISTRATION.md` §12.1, frozen `bb0c5b08` before the run root existed; band `[15.0, 65.0]` from eight named scenarios; **HARD CEILING 145.0 = Σ(caps), NOT APPROACHED** | **16.184** core-min, from the item's own seven ledger rows (wall_s × ranks ÷ 60), read from `D19O_grade_20260901T054304Z.json` `gates.G10_caps` | **16.184 — identical to gross**; longest arm 324 s wall, so no row matches the `COMPUTE_BUDGET_CHARTER.md` §2 3600-s stall rule | **0.6715** | **THE MISS IS ONE TERM, AND THE PRE-REGISTRATION NAMED IT AS RESIDUAL `R5` BEFORE THE RUN.** The optimiser arms were priced at **0.63122 core-min/major `[EXTRAPOLATED]`** = 0.3924 `[MEASURED, SO-1bR O-P/O-S incompressible single-point IPOPT on this A1 case at np=1, agreeing to 0.36 %]` × 1.6086 `[MEASURED compressible/incompressible PRIMAL ratio on this same 4,032-cell mesh at np=1]`. **R5 stated the risk in these words: "the ratio is measured on the PRIMAL and carried onto a major that is primal + 2 adjoints + mesh warp". THE ANSWER IS THAT IT DOES NOT TRANSFER.** MEASURED HERE, TWICE: `O-S` 5.400/13 = **0.4154** and `O-P` 4.217/10 = **0.4217** core-min/major, agreeing to 1.5 %. **So the compressible penalty ON A MAJOR is 1.059× (0.4154/0.3924), not the 1.6086× the primal shows** — because a major is dominated by the two adjoint solves and the mesh warp, whose cost is far less sensitive to the extra density and energy states than a bare primal is. **FORWARD RULE FOR THIS FAMILY: a compressible optimiser major on A1 at np = 1 prices at ≈ 0.42 core-min/major, measured on two rows.** A primal-derived penalty must not be carried onto a major without saying so; this row is the measurement that removes the need to. **ATTRIBUTION: MISPREDICTION, NOT CONTENTION** — `delivered_cores_mean` 0.9926 on `O-S` and `MemAvailable` never below 27.80 GiB against a registered 16.0 floor, so no arm was starved. The five non-optimiser arms came in at **0.607–0.835** of prediction, the same band SO-3's non-optimiser arms landed in (0.59–0.88), so that half of the model is stable and is not re-derived. **THE SO-3 DEFECT WAS AVOIDED AND THE AVOIDANCE IS MEASURED:** SO-3 registered 228.59 core-min and spent 28.900 (ratio 0.126×) because its IPOPT arms were priced at their `max_iter` of 50 and converged in 10; D19O priced its O arms at an EXPECTED 12 major rows (measured: SO-1bR O-P, SO-1bR O-S and D1 armO each returned exactly 11 iterations / 12 rows on this case) with `max_iter = 40` as the CAP and nowhere else. **The rows came in at 13 and 10 — the expectation was good, and 0.6715 is a rate error, not a major-count error.** **WASTE: NONE.** No arm crossed its cap, no arm was re-run, nothing was discarded, and the item did not approach its ceiling. **NO overrun, NO cap breach.** **$0.013837 DERIVED, NOT MEASURED** — 16.184 core-min = 0.26973 core-h at $0.0513/core-h, c7a.4xlarge, reported-by-owner (`COMPUTE_BUDGET_CHARTER.md` §5: the box cannot read its own billing). **GPU: 0 GPU-h.** | `cases/dafoam/ladder-a/A1/curriculum_D19O/RESULTS.md`; pre-registration `bb0c5b08` (Amendment 1, grading path `c55a0151dc8eab4740ee1169cb9202ae`); arming `239fd2ef`; grade artefact `/home/ubuntu/certonomous-runs/CURRICULUM-D19O-a1-naca0012-subsonic-optimisation/D19O_grade_20260901T054304Z.json`; ledger `.../ledger.txt` |
| C-20260901T054955.936783Z-5f227022 | 2026-09-01 | heat-transfer | T25R2_L1 (resolved-channel battery module, 1800 steps, nOuterCorrectors 10) — **RUN COMPLETE, `mark_done_t25R2.py` says DONE on all six conjuncts; NOTHING GRADED, the §3.5 outer-loop gate has not been evaluated and no physics number is a result** | **8.30 core-min** POINT (§8.2, ×4 margin → HARD CAP **34**, timeout_s 2040); $0.0071 derived | **7.150 core-min** wall (429 wall s × 1 rank ÷ 60) | **7.148 core-min** solver (`log.solve` `ExecutionTime` 428.87 s × 1 ÷ 60); launch overhead **0.002** named separately — `0` staged from `0.orig`, the age touch, the launcher's 5 s poll; not waste, not in the ratio | **0.861** | **MISPREDICTION, CONSERVATIVE DIRECTION, CAUSE MEASURED.** §8.2's POINT extended the probe's **takeoff-branch** settled rate 0.268 core-s/step across all 1800 steps; the probe never priced cruise (§8.1 caveat 2). Measured split at the t=60 s step-down: takeoff steps 1–120 **0.3751**, cruise steps 121–1800 **0.2285** core-s/step. 93.3 % of steps are cruise, 14.7 % cheaper per step than assumed — that is the whole of the 0.861. **THE PROBE REPRODUCED:** same first 60 steps, same numerics/mesh/loads, different day — probe A2T 31.02 core-s vs **31.79** here, ratio **1.025**; settled 21–60 0.268 vs **0.2715**, ratio **1.013**. **WASTE NAMED: 0 core-min** — 36,000 `p_rgh` GAMG solves, 65,949 iterations, mean 1.83, max 548, **ZERO at `maxIter` 1000**, confirming §3.4's `1e-8` tolerance over the full 900 s where the probe (608 of 1200 stalling at `1e-9`) could only test 30 s. **CONTENTION bounded, NOT measured, NOT subtracted:** load 2.22/16 at launch with `T23G_F` and dafoam `D19O` O-P live; the 1.025 reproduction bounds it at ~2.5 % on the comparable segment. Cap unused: 26.85 core-min of 34; `capped=no`, `rc=0` recorded inside the detached wrapper. Dollars **$0.0061 DERIVED, NOT MEASURED**, $0.0513/core-h, `cost_basis = REPORTED-BY-OWNER` (`COMPUTE_BUDGET_CHARTER` §5) | `docs/campaigns/T-family/T25R2_PREREGISTRATION.md` Addendum B1 §B1.2; prereg frozen at `bb6e5761`, Amendment A1 at `227240b5`; `verification/runs/T-family/T25R2_MODULE_runs/T25R2_L1/COMPLETION.T25R2_L1.txt` |
| C-20260901T061002.916833Z-1b754185 | 2026-09-01 | heat-transfer | T25R2_L1_OC20 (the §3.5 outer-loop arm, L1 mesh at nOuterCorrectors 20, 1800 steps) — **RUN COMPLETE and `DONE` on all six conjuncts; the GATE IT SERVES is `GATE FAIL` and every row of the rung is `NOT A RESULT`** | **18.09 core-min** POINT (§8.2, ×4 → HARD CAP **73**, timeout_s 4380); $0.0155 derived | **12.633 core-min** wall (758 wall s × 1 rank ÷ 60) | **12.615 core-min** solver (`ExecutionTime` 756.91 s × 1 ÷ 60); launch overhead **0.018** named separately, not waste, not in the ratio | **0.697** | **MISPREDICTION, CONSERVATIVE DIRECTION, CAUSE MEASURED.** §8.2 priced the sweep doubling at **×2.18**, taken from the probe's A1→A2 pair which **still carried the `p_rgh` stall** the registered 1e-8 tolerance removes. Measured here: **12.615 / 7.148 = ×1.765** whole-run, and **0.4044 / 0.2285 = ×1.77** on the like-for-like cruise branch. **Doubling the outer sweeps costs 1.77×, not 2.18×, once the stall is gone** — the sub-linear part is per-step work that is not per-sweep. **WASTE NAMED: 0 core-min**; no cap stop, `capped=no`, `rc=0` recorded inside the detached wrapper; 60.4 of 73 core-min unspent. **CONTENTION bounded, NOT measured, NOT subtracted** — run sequentially at the supervisor's instruction with `T23G_F` live. Campaign total so far **19.780 core-min of the 390 hard cap, 370.2 unspent**. Dollars **$0.0108 DERIVED, NOT MEASURED**, $0.0513/core-h, `cost_basis = REPORTED-BY-OWNER` | `docs/campaigns/T-family/T25R2_RESULTS.md` §4; prereg `bb6e5761`, Addendum B3; `verification/runs/T-family/T25R2_MODULE_runs/T25R2_L1_OC20/COMPLETION.T25R2_L1_OC20.txt` |
| C-20260901T063315.501655Z-94451286 | 2026-09-01 | heat-transfer | **T23G — the grid triple at (305 W, 20 m/s), all three levels solved and the rung GRADED. RUNG VERDICT `NOT A RESULT`**, and the reason is the rate, not the run: every level `rc=0`, every level `CONVERGED` on all six asserted residuals (≤ 1e-8 against 1e-6), every level `PLATEAUED` at spread 0.000000 K, all nine planted-zero controls `PASSED` at PLANT 1.234e-03 K, the ladder exactly as registered (9,920 → 39,680 → 158,720 cells, every region ×4, r21 = r32 = 2.000000) and all 30 invariant files byte-identical across levels. **The triple is `STAGNANT` on all three quantities at observed order p = 0.3796 / 0.3766 / 0.3744**, below `roache_triple.py:170`'s `STAGNANT_FLOOR = 0.5`, so `CLAUDE.md` rule 5 makes it `NOT A RESULT` whatever the value says. **The band verdict was computed first and unconditionally and was `PASS` on all three** — the gate turned `PASS` into `NOT A RESULT`, the only direction rule 5 permits. **NO GCI IS QUOTED ANYWHERE** and `G-ORDER` / `G-GCI-DISPLAY` / `G-GCI-LEGACY` are `NOT EVALUATED` on all three quantities, recorded as not reached rather than as passed. `G-REPRO` **PASS at exactly 0.000e+00 K** — `T23G_M` reproduces `T23_P305_U20` bit-identically — on a reference corrected hours earlier under the `§2d.1` grant at `DEAD_LEVER_AUDIT` §27.2 and **re-read from `T23_P305_U20`, never from the case being graded**. **The registered prediction p ≈ 1.0 (§6.2) is FALSIFIED in the unexpected direction**, and the three quantities agree to within 0.005 across two readers and three regions, so it is a finding about the discretisation and not scatter in one column | **158.65 core-min** REGISTERED as TOTAL POINT (`T23G_PREREGISTRATION.md` §8.2, per level: C **7.56**, M **30.22**, F **120.88**), TOTAL HARD CAP **525.0** enacted as `timeout` | **179.6334 core-min**, basis **GROSS**, MEASURED from each level's own `STATUS.<case>` at `ranks = 1`, `core-min = wall_s × ranks / 60`: C **6.0667** (364 wall s), M **27.8167** (1,669 wall s), F **145.7500** (8,745 wall s). All three `rc=0`, all three `capped=0` | **= GROSS, 179.6334 core-min.** The `COMPUTE_BUDGET_CHARTER.md` §2 3,600 wall-s stall rule excludes nothing here at the level of a run — no level was capped, no level overran, and F's 8,745 wall s ran against a 24,000 s timeout, using **36.4 %** of its 400.0 cap; campaign **34.2 %** of the 525.0 ceiling. **DERIVED, NOT MEASURED: $0.1536** actual against **$0.1357** at point, at $0.0513/core-h c7a.4xlarge, `cost_basis` **REPORTED-BY-OWNER** — the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` §5) | **1.132** cleaned/predicted | **MISPREDICTION — SPECIFICALLY A LINEAR COST MODEL ANCHORED MID-LADDER AGAINST A SUPERLINEAR REALITY. NOT CONTENTION AND NOT WASTE — AND THE AGGREGATE 1.132 IS THE DISHONEST HEADLINE, WHICH IS WHY THE PER-LEVEL DRIFT IS GIVEN INSTEAD.** Per-level actual/point: C **0.803**, M **0.921**, F **1.206** — a monotone drift, not scatter. §8.2 derived wall time from cell-iterations, so the registered points scale by exactly the cell ratio (predicted M/C **3.997**, predicted F/M **4.000**); **the measured scalings are both larger and they grow: actual M/C 4.585, actual F/M 5.240.** Cost per cell rises with mesh size as linear-solver iteration counts and cache behaviour degrade, so a linear model under-predicts upward and over-predicts downward, and the ratio passes through ≈1 at the **medium** level — precisely where §8.3 anchored it on a measured 1,813 s prior run. **CONTENTION IS RULED OUT BY THE SHAPE RATHER THAN ASSERTED: contention inflates every level, and the two smaller levels came in UNDER their points.** **CARRY-FORWARD for the next ladder on this geometry: size a fine level from a superlinear fit or from the fine level's own anchor, never by multiplying a mid-ladder anchor by the cell ratio — at r = 2 in 2D that under-prices the fine level by ~21 %.** **WASTE, NAMED SEPARATELY AND NEVER FOLDED INTO THE RATIO (charter §6): SOLVER COMPUTE WASTE = ZERO core-min.** One comparator run was wasted at 06:01Z when `analyse_t23g.py` was invoked with a `--selftest` flag **it does not implement**, and `main()` silently ignored the unrecognised flag and fell through to a full `grade()` against the live tree; it consumed no solver compute (it refused at `require_done`, the fine level then being unfinished) but wrote `DONE.T23G_C` and `DONE.T23G_M` early. The markers' content is not false — rule 4's six clauses were genuinely evaluated. **The silent-fallthrough flag handling is a real instrument defect and is recorded rather than absorbed.** Comparator cost for the graded run is **under one minute** single-rank Python, bounded from artifact timestamps (`DONE.T23G_F` 06:27:34.16Z → `T23G_GRADED.json` 06:27:35.89Z); it is not solver compute and enters no ratio above | `docs/campaigns/T-family/T23G_RESULTS.md`; machine record `verification/runs/T-family/T23G_runs/T23G_GRADED.json`; registration `docs/campaigns/T-family/T23G_PREREGISTRATION.md` §8.2; per-level `STATUS.T23G_C` / `STATUS.T23G_M` / `STATUS.T23G_F`; the `§2d.1` grant that unblocked grading `DEAD_LEVER_AUDIT` §27 commit `af6af856`; the pre-flight that found the two blocking defects `docs/campaigns/T-family/T23G_PREFLIGHT_FINDINGS.md` |
| C-20260901T080312.667856Z-f6def2d8 | 2026-09-01 | dafoam | **D12RLX — the L-426 `Final`-relaxation-key paired run, two full 33-stage arms (C = upstream dictionary, R = same file + two `Final` keys). ITEM VERDICT `GATE REACHED`; `G-RLX-0` reproduction PASS bit-exact incl. the adjoint gradient** | **111.03 core-min** POINT (2 arms x the measured 55.5167 D12R2 anchor; `CAP_ITEM` 150.0, `CAP_ARM` 70.0 each); $0.0949 derived | **92.9672 core-min** (arm C 46.3836 + arm R 46.5836, from each root's `ledger.txt` `PHASE1_COMPLETE spent=`) | **92.9672 core-min** (cleaned == gross: longest stage 397 s, nothing near the 3600-s stall rule; **no waste to name**; 0 BLOCKED stages in either arm) | **0.837** | **MISPREDICTION IN THE SAFE DIRECTION, from an anchor measured under heavier contention.** The 55.5167 anchor was bought when the box was busier; both arms ran ~16 % faster than it **despite being unpinned** while D19-family peers hold `cpuset=11`. **No contention penalty materialised and none is attributed** — the naive expectation ran the opposite way, which is exactly what this ledger exists to record. No cap approached. | `cases/dafoam/D12RLX_RESULTS.md`; prereg `cases/dafoam/D12RLX_RELAXATION_PAIRED_PREREGISTRATION.md` v1.1 (`b1b411b6`, `a75353b2`); roots `CURRICULUM-D12RLX-armC-repro`, `CURRICULUM-D12RLX-armR-repaired` |
| C-20260901T153620.973786Z-491bdd16 | 2026-09-01 | dafoam | **`CURRICULUM-D19M` — A1 NACA0012 COMPRESSIBLE ALPHA-MULTIPOINT SHAPE OPTIMISATION, `DARhoSimpleFoam` M 0.288, 4,032 cells, np = 1, three operating points α = 2.787333582 / 4.787333582 / 6.787333582 deg at equal weights 1/3, BOTH TOOLCHAIN ROWS, SEVEN ARMS OF SEVEN.** The completion this row calibrates is the **graded item**, not a stage: `STATUS.D19M_chain` reads `chain=COMPLETE stamp=20260901T083035Z chain_rc=0 declared=7 executed=7`, every arm `rc=0`, graded `20260901T083034Z`. **Item verdict `GATE REACHED`, both rows, both `verdict_before_ceiling: PASS` and `capped_by_ceiling: true`** — every gate returned `PASS` and the item was **CAPPED, NOT FAILED**, by a ceiling registered before the run (the compressible gradient it spends has no graded verdict and D19R's plateau never closed). **The first FD table any compressible multipoint objective in this lab has ever had.** | **34.10 core-min REGISTERED point**, `PREREGISTRATION.md` §10 / §12.1 (`| **ITEM** | | **34.10** | **149.0** |`), frozen at `c7d7bf10` **66 s before first compute** (freeze 06:30:47Z, chain stamp 06:31:53Z) and confirmed by this row against the grade JSON's own `G10_caps.item_point_prediction_core_min`. Per-arm predictions MESH 0.20, O-S 8.64, O-P 8.64, XE-S 2.60, XE-P 2.60, FE-S 5.71, FE-P 5.71. Registered **band [25.0, 85.0]** from seven named major-count scenarios; **HARD CEILING 149.0 = Σ(caps), NOT APPROACHED** (23.6 % of it spent). `ranks 1` on every arm. Dollars registered at point **$0.029156 DERIVED**. **GPU 0.** | **35.166 core-min MEASURED**, basis gross, the grader's own `G10_caps.total_core_min`, cross-checked by this row against the seven `ledger.txt` rows (wall_s × ranks ÷ 60: 10 + 486 + 182 + 383 + 485 + 181 + 383 = 2,110 s at ranks 1 = 35.167). **$0.030067 DERIVED, NOT MEASURED** at c7a.4xlarge $0.0513/core-h, reported-by-owner 2026-08-21/22 — **the box cannot read its own billing** (`COMPUTE_BUDGET_CHARTER.md` §5). **0 GPU-h.** | **= gross. 35.166 core-min.** No arm comes near the `COMPUTE_BUDGET_CHARTER.md` §2 3,600-wall-s stall rule: the longest arm is `O-S` at 486 wall s, and the whole seven-arm chain is 2,110 wall s. **WASTE: NONE IDENTIFIED, and named as such rather than omitted** — no arm stalled, no arm was re-run, no arm crossed its cap (`arms_over_cap: []`), nothing was discarded, and no BLOCKED arm exists. **No waste figure is laundered into either column or into the ratio.** | **1.0313** | **MISPREDICTION, MILD, AND IT IS TWO UNDER-PRICED TERMS NEARLY CANCELLED BY ONE FEWER MAJOR. NO CONTENTION IS ATTRIBUTED AND NONE WAS OBSERVED.** (i) **The `O` arms came in at 0.936/0.937 — and the RATE was right, not the arm.** The registered 0.72255 core-min/major `[EXTRAPOLATED]` × an expected **12** major rows gave 8.64; IPOPT ran **11** rows (10 iterations) on both, and the measured rate is **8.100/11 = 0.7364** and **8.083/11 = 0.7348**, i.e. the extrapolated rate was accurate to **1.9 %**. The shortfall is one fewer major, not a bad price. (ii) **`XE` under-priced by 16–17 %**: registered 2.4415 (SO-3's incompressible 3-point mean) × **1.0667**; measured 3.033/3.017, so the compressible penalty on an `XE` adjoint-totals arm is **≈1.24×, not 1.07×**. (iii) **`FE` under-priced by 12 %**: registered 102 primals × 3.3571 s, that per-primal rate taken from D19O's **single-point** compressible endpoint; measured 383 s / 102 = **3.755 s/primal** on both arms. **(ii) and (iii) are the same error — an anchor measured on one configuration carried onto another without being measured there**, which is exactly what D19O's own §5 warned about. **The mechanism behind the 12 % `FE` gap is NOT MEASURED and is NOT asserted**; three `DASolver` instances in one process is a candidate this run does not separate from ordinary cache and memory-traffic effects. **THE HEADLINE CALIBRATION FINDING: the expected-major pricing correction holds for a THIRD consecutive item.** SO-3 priced its `O` arms at `max_iter` and registered 228.59 against 28.900 spent (7.9× over); D19O switched to expected majors and landed 0.6715; D19M landed **1.0313**. `max_iter = 40` is priced into the 40.0 per-arm CAP and nowhere else — **a cap is not an estimate.** **FORWARD RULES MEASURED HERE for A1 compressible np = 1:** a **multipoint(3) optimiser major ≈ 0.735 core-min/row** (twice, agreeing to 0.2 %); a **multipoint(3) endpoint primal ≈ 3.755 s** (twice, identical at 383/102); a **3-point compressible `XE` arm ≈ 3.02 core-min**. | `cases/dafoam/ladder-a/A1/curriculum_D19M/RESULTS.md` §1 and §9; grade artefact `/home/ubuntu/certonomous-runs/CURRICULUM-D19M-a1-naca0012-subsonic-multipoint/D19M_grade_20260901T083034Z.json` (`G10_caps`) and that root's `ledger.txt`; prereg `cases/dafoam/ladder-a/A1/curriculum_D19M/PREREGISTRATION.md` frozen `c7d7bf10` |
| C-20260901T163549.130024Z-ef3eb979 | 2026-09-01 | dafoam | **`CURRICULUM-AOAI` -- A1 NACA0012 INCOMPRESSIBLE ANGLE-OF-ATTACK POLAR, `DASimpleFoam`, Re 6.67e5, 4,032 cells, np = 1, 19 points at alpha = 0..18 deg inclusive continued upward from a cold alpha = 0, plus 3 cold controls at alpha = 4/14/17 re-run from freestream. SANAA-DIRECT `48b33813`. TAG `FEASIBILITY`, NOT GATED** -- a 19-point polar on one grid has no grid triple, so under Sanaa's own convergence-prerequisite doctrine of the same afternoon it cannot be a graded result; **the item therefore carries NO VERDICT and produces readings only.** Outcome: **9 CONVERGED (alpha 0-8), 10 NOT CONVERGED (alpha 9-18)**, every failure at the registered 1000-iteration cap; all 19 declared points present and read, none dropped, retried, relaxed or replaced by a transient. Reader **11/11 planted controls PASS** and **G-STALL PASS** (no stall angle is reported and none may be derived: the reader refuses at exit 2 on any output binding a stall word to an angle) | **3.2 core-min** REGISTERED point (`AOAI_PREREGISTRATION.md` §7.3), band **[2.0, 12.0]**, per-arm **CAP 20.0** enacted as the 1200 s in-container `timeout -k 60`, item ceiling **40.0** across both arms. Basis: **13,090 predicted SIMPLE iterations x 0.011024 s/iteration** [the rate MEASURED from SO3aF's three cold primals] + ~40 s process overhead. ranks 1. Dollars at point **$0.0027 DERIVED**. **GPU 0** | **3.1000 core-min MEASURED**, basis GROSS, from `STATUS.AOAI` (`wall_s=186 ranks=1`). Solver wall **152.87 s** over **15,855** actual SIMPLE iterations, summed from the item's own per-point ledger. **$0.00265 DERIVED, NOT MEASURED** at $0.0513/core-h c7a.4xlarge, reported-by-owner 2026-08-21/22 -- **the box cannot read its own billing** (`COMPUTE_BUDGET_CHARTER.md` §5). **0 GPU-h** | **3.1000 core-min MEASURED**, basis GROSS, from `STATUS.AOAI` (`wall_s=186 ranks=1`). Solver wall **152.87 s** over **15,855** actual SIMPLE iterations, summed from the item's own per-point ledger. **$0.00265 DERIVED, NOT MEASURED** at $0.0513/core-h c7a.4xlarge, reported-by-owner 2026-08-21/22 -- **the box cannot read its own billing** (`COMPUTE_BUDGET_CHARTER.md` §5). **0 GPU-h** | **0.969** | **THE RATIO IS TWO OFFSETTING ERRORS AND IS NOT EVIDENCE THE ESTIMATE WAS GOOD -- which is why the two components are given rather than the aggregate alone.** (i) **ITERATIONS UNDER-PREDICTED BY 21-24 % IN BOTH ARMS.** Continued points were priced at **350 iterations** from D19M's FD-sweep mean of 337.9 over its 90 non-trivial primals; the converged points actually ran **314-516**. A 1 deg step in alpha is a materially larger perturbation than an FD step, which the pre-registration labelled EXTRAPOLATED and explicitly not a measurement of a 1 deg step -- it was optimistic. (ii) **THE PER-ITERATION RATE, WHICH IS THE UNIT THIS ITEM INTRODUCED, SPLIT SHARPLY BY SAMPLE SIZE.** The compressible anchor **0.011500 s/iteration**, measured over 30,622 iterations of D19M FE-S, came in at **0.011370** -- accurate to **1.1 %**. The incompressible anchor **0.011024 s/iteration**, measured over only **three** SO3aF primals, came in at **0.009642** -- **12.5 % conservative**. **A three-primal anchor is what a 12 % miss looks like; a 30,000-iteration anchor held to 1 %.** (iii) The two errors ran in OPPOSITE directions and nearly cancelled. **NO CONTENTION IS ATTRIBUTED AND NONE WAS OBSERVED** -- the two arms ran concurrently on disjoint cores 14 and 15 with 28 GiB free and neither approached its 4g reservation. **WASTE: NONE IDENTIFIED, named as such rather than omitted** -- no arm stalled (longest single point 11.6 wall s against the charter's 3,600 s stall rule), no solve was re-run, no arm crossed its cap. The one refused launch (AOAC's first attempt, G-PHYS exit 9) consumed **zero solver compute**: it aborted before the container started. **CARRY-FORWARD, MEASURED HERE for A1 4,032 cells at np=1:** incompressible **0.009642 s/SIMPLE-iteration** [MEASURED, 15,855 iterations] and compressible **0.011370 s/SIMPLE-iteration** [MEASURED, 16,452 iterations], both large-sample and both superseding the three-primal incompressible figure. **And the sizing rule this item earned: price an alpha-continuation point at ~450 iterations, not at an FD-sweep mean -- the FD mean is contaminated by perturbations so small the solver converges in under 10 iterations (6 of D19M FE-S's 105 primals did).** | `cases/dafoam/ladder-a/A1/feasibility_aoa_polar/AOA_RESULTS.md` §6; preregs `AOAI_PREREGISTRATION.md` @ `ba8b501c` and `AOAC_PREREGISTRATION.md` @ `338b539b` (pre-compute amendment `c989b956`); per-point ledgers `<root>/out/LEDGER.tsv`; machine readings `<root>/AOA_POINTS.json`; `<root>/STATUS.AOAI` and `<root>/STATUS.AOAC` |
| C-20260901T163549.130099Z-0028d6e2 | 2026-09-01 | dafoam | **`CURRICULUM-AOAC` -- A1 NACA0012 COMPRESSIBLE ANGLE-OF-ATTACK POLAR, `DARhoSimpleFoam` M 0.288, Re 6.54e6, 4,032 cells, np = 1, same 19-point continued sweep and same 3 cold controls, D19M's physics block carried across BYTE-IDENTICAL (md5 `c66504acc57bd9ef009599e883d2ef3b`, asserted at extraction, by the producer's own import-time self-assert, and by `G-PHYS` before launch). TAG `FEASIBILITY`, NOT GATED, NO VERDICT.** Outcome: **9 CONVERGED (alpha 0-8), 10 NOT CONVERGED (alpha 9-18)** -- **the same boundary as the incompressible arm, to the degree, across two solvers and a tenfold Reynolds difference.** Reader **11/11 controls PASS**, **G-STALL PASS**. Cold controls at alpha 14 and 17 failed identically to the continued points, so **the high-alpha failure is a property of the operating point and not of the continuation path**; at alpha 4 continued and cold agree to **4.1e-9 relative in CL** and continuation saved **10.6 %** of iterations (449 vs 502), so the instrument is live | **3.4 core-min** REGISTERED point (`AOAC_PREREGISTRATION.md` §7.3), band **[2.0, 12.0]**, per-arm **CAP 20.0** = the 1200 s in-container timeout, item ceiling **40.0**. Basis: **13,300 predicted SIMPLE iterations x 0.011500 s/iteration** [the rate MEASURED over 30,622 iterations of D19M FE-S] + ~40 s overhead. ranks 1. Dollars at point **$0.0029 DERIVED**. **GPU 0** | **3.5333 core-min MEASURED**, basis GROSS, from `STATUS.AOAC` (`wall_s=212 ranks=1`). Solver wall **187.05 s** over **16,452** actual SIMPLE iterations from the per-point ledger. **$0.00302 DERIVED, NOT MEASURED** at $0.0513/core-h, reported-by-owner -- the box cannot read its own billing. **0 GPU-h** | **3.5333 core-min MEASURED**, basis GROSS, from `STATUS.AOAC` (`wall_s=212 ranks=1`). Solver wall **187.05 s** over **16,452** actual SIMPLE iterations from the per-point ledger. **$0.00302 DERIVED, NOT MEASURED** at $0.0513/core-h, reported-by-owner -- the box cannot read its own billing. **0 GPU-h** | **1.039** | **THE RATIO IS TWO OFFSETTING ERRORS AND IS NOT EVIDENCE THE ESTIMATE WAS GOOD -- which is why the two components are given rather than the aggregate alone.** (i) **ITERATIONS UNDER-PREDICTED BY 21-24 % IN BOTH ARMS.** Continued points were priced at **350 iterations** from D19M's FD-sweep mean of 337.9 over its 90 non-trivial primals; the converged points actually ran **314-516**. A 1 deg step in alpha is a materially larger perturbation than an FD step, which the pre-registration labelled EXTRAPOLATED and explicitly not a measurement of a 1 deg step -- it was optimistic. (ii) **THE PER-ITERATION RATE, WHICH IS THE UNIT THIS ITEM INTRODUCED, SPLIT SHARPLY BY SAMPLE SIZE.** The compressible anchor **0.011500 s/iteration**, measured over 30,622 iterations of D19M FE-S, came in at **0.011370** -- accurate to **1.1 %**. The incompressible anchor **0.011024 s/iteration**, measured over only **three** SO3aF primals, came in at **0.009642** -- **12.5 % conservative**. **A three-primal anchor is what a 12 % miss looks like; a 30,000-iteration anchor held to 1 %.** (iii) The two errors ran in OPPOSITE directions and nearly cancelled. **NO CONTENTION IS ATTRIBUTED AND NONE WAS OBSERVED** -- the two arms ran concurrently on disjoint cores 14 and 15 with 28 GiB free and neither approached its 4g reservation. **WASTE: NONE IDENTIFIED, named as such rather than omitted** -- no arm stalled (longest single point 11.6 wall s against the charter's 3,600 s stall rule), no solve was re-run, no arm crossed its cap. The one refused launch (AOAC's first attempt, G-PHYS exit 9) consumed **zero solver compute**: it aborted before the container started. **CARRY-FORWARD, MEASURED HERE for A1 4,032 cells at np=1:** incompressible **0.009642 s/SIMPLE-iteration** [MEASURED, 15,855 iterations] and compressible **0.011370 s/SIMPLE-iteration** [MEASURED, 16,452 iterations], both large-sample and both superseding the three-primal incompressible figure. **And the sizing rule this item earned: price an alpha-continuation point at ~450 iterations, not at an FD-sweep mean -- the FD mean is contaminated by perturbations so small the solver converges in under 10 iterations (6 of D19M FE-S's 105 primals did).** | `cases/dafoam/ladder-a/A1/feasibility_aoa_polar/AOA_RESULTS.md` §6; preregs `AOAI_PREREGISTRATION.md` @ `ba8b501c` and `AOAC_PREREGISTRATION.md` @ `338b539b` (pre-compute amendment `c989b956`); per-point ledgers `<root>/out/LEDGER.tsv`; machine readings `<root>/AOA_POINTS.json`; `<root>/STATUS.AOAI` and `<root>/STATUS.AOAC` |
| C-20260901T163701.774550Z-1b8633ba | 2026-09-01 | cfd | JF1G Pass-0 level C1 (grid-convergence coarse level, `C_mu` 0.1, 39,984 cells, 8,000 iterations) — **LABEL diagnostic, scores nothing; this row calibrates its COST, not a verdict** | 21.8 core-min = 0.3633 core-h / **$0.01864 derived** (registered cap 40.0 core-min) | 21.7500 core-min (1,305 wall s × 1 rank ÷ 60) = 0.3625 core-h = **$0.01860 derived** | = gross (wall 1,305 s, nowhere near the charter §2 3600-s stall rule; single rank, no contention adjustment applied) | **0.9977** | **Essentially no gap — and the hit is the least useful thing in this row.** The estimate was built from `JF1_L1_BLOWN_CMU010_A0`, the SAME case on the SAME mesh, so 0.998 measures reproducibility, not predictive skill. **THE REFUTATION IS THE USEFUL PART: per-cell-per-iteration cost is NOT constant, so a cap built by scaling a coarse-level rate linearly UNDER-CAPS the fine level.** C1 completed at **4.0798e-06 core-s/cell/iteration**; C2 (89,964 cells, same box, same concurrent load) runs at **7.7867e-06** mid-run — **1.909× worse per cell at 2.25× the cells**, so cost grows as roughly the **4.3 power of `r`** on this 2-D family, not `r²`. Consequence already realised: JF1G's C2 Pass-0 estimate of 48.9 core-min projects to **93.4** (ratio 1.91) and **will strike its frozen 80 core-min cap at roughly 6,850 of 8,000 iterations**. The cap is NOT raised (rule 12); the lawful route to a larger C2 is a new pre-registration carrying this measured basis. Cause is not attributed to contention: both figures were taken with the same three solvers live. **Waste, named separately: 0 core-min.** Separately corrected here: the "23.497 core-min per L1 row" basis in circulation is the **mean** of five rows of DIFFERING blowing (23.7667 / 24.4333 / 21.7500 / 22.3333 / 25.2000) and was never the right basis for a single-row estimate; per-row figures supersede it. | `verification/runs/JF1_jet_flap/JF1G_P0_C1_CMU010_A0/RUN_STATUS.JF1G_P0_C1_CMU010_A0.txt`; prereg frozen `038f4bca` (§8 cost table); launcher `e8e18df5`; comparator `74c4f5fb`. C1's mesh and result are BIT-IDENTICAL to `JF1_L1_BLOWN_CMU010_A0` (all five polyMesh files; CL equal on all 8,000 iterations), which is what makes the reproducibility reading above defensible. |
| C-20260901T164255.339795Z-4e1e63e3 | 2026-09-01 | cfd | **CORRECTION to the JF1G Pass-0 C1 row landed at commit `5d53328c` (id begins `C-20260901T163701`).** **Its cost figures stand; its SCALING CLAIM and its ATTRIBUTION do not, and I got both wrong in the confident direction.** | *(no new prediction — this row corrects an attribution)* | *(unchanged: C1 21.7500 core-min, ratio 0.9977)* | *(unchanged)* | *(unchanged)* | **THREE CORRECTIONS, ALL MINE.** **(1) The per-cell super-linearity was OVERSTATED 1.30×.** That row compared a whole-run AVERAGE for C1 (4.0798e-06 core-s/cell/it) against a SNAPSHOT for C2 taken at iteration 896 (7.7867e-06) — and that snapshot sits inside C2's first 250-iteration window, which ran at **0.848 s/iter against a steady-state median of 0.5052**, because four solvers were live. Measured properly on **steady-state MARGINAL rates** (250-iteration windows, median, first window dropped, n = 29 and n = 11): C1 **3.8115e-06**, C2 **5.6151e-06**, **ratio 1.473, not 1.909.** **(2) "Contention is NOT the attribution: both figures were taken with the same load live" is FALSE, and I had not tested it.** They were not taken alike: C1's number spans a whole run across varying load, C2's was one early reading at peak contention. Contention is a substantial part of a gap I attributed entirely to mesh size. **(3) The exponent was wrong.** Corrected: total cost ratio **3.315** at a cell ratio of 2.2500, so cost grows as **N^1.478 ≈ r^2.96** — **about r^3.0, not the "roughly 4.3 power of r" that row claims.** **THE PREDICTION IT CARRIED IS ALSO FAILING:** it states C2 Pass-0 "WILL strike its frozen 80 core-min cap at roughly 6,850 of 8,000 iterations"; at the steady marginal rate C2 projects to **4,215 wall s against the 4,800 s cap and COMPLETES.** The rate improved because C1 finished and freed a core — the very contention effect that row declared absent. **WHAT SURVIVES, and it was the useful part: the super-linearity is REAL (1.473 ≠ 1.0), so a cap built by scaling a coarse-level rate linearly does still under-cap a finer level.** The magnitude, the attribution and the exponent do not survive. **Waste: 0 core-min.** C2's COMPLETED figures will be added as a further row and supersede the mid-run marginal rate used here. | Marginal-rate measurement from `verification/runs/JF1_jet_flap/JF1G_P0_C1_CMU010_A0/log.simpleFoam` (completed) and `.../JF1G_P0_C2_CMU010_A0/log.simpleFoam` (mid-run, iteration 3,171); the corrected row is at commit `5d53328c`. |
| C-20260901T171818.070164Z-9e9a6068 | 2026-09-01 | cfd | **WASTE ROW, named separately under `COMPUTE_BUDGET_CHARTER` §6 — this is not a graded process, it has no pre-registration, and it enters NO ratio.** The DMR / demo-sequencer lane attempted to attribute cost from a full-suite test run on this box while other agents' jobs were live. That attribution is one this team had already measured to be unavailable here: `docs/RESOURCE_DEPENDENT_TESTS.md` records the 1-minute load ranging **10.25 to 1.42 while its own suite measurements ran — a 7x swing, and a 3x swing INSIDE a single suite run** (that document's opening, and its §2 load table). A wall-clock figure taken from a full-suite run under those conditions is reading the neighbours, which is the L-62 defect the document exists to generalise. | *(none — waste has no estimate to calibrate against; this spend was never pre-registered)* | **~40 core-min** (single-core, so core-min = wall-min), **~$0.034 derived** at $0.0513/core-h, c7a.4xlarge, reported-by-owner — the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER` §5). **PROVENANCE OF THE FIGURE, STATED RATHER THAN DRESSED: it is REPORTED BY THE LANE THAT SPENT IT and relayed through the cfd supervisor's dispatch. It is NOT read from a committed record.** Searched for one before writing this row: no artifact under `docs/`, `verification/campaign/` or any commit message in the last ten hours carries a core-minute figure for this spend. This ledger's rule 2 requires a figure absent from the record to be stated as absent, and it is stated here rather than omitted, because a waste row with no magnitude carries no calibration signal at all. Treat the ~40 as the spending lane's own account, good to one significant figure. | *(same — no 3600-s stall rows to remove, so gross = cleaned)* | **n/a — waste is never absorbed into a ratio** (`COMPUTE_BUDGET_CHARTER` §6: a total that silently includes a failed attempt understates cost per result and overstates efficiency) | **WASTE / RE-DERIVING A SETTLED NEGATIVE.** The spend bought nothing: the answer already existed on disk, in a document written 2026-08-11 for exactly this question. The cost is not the compute, which is small; it is that a lane re-opened a measurement this team had closed, and the ~40 core-min is the price of that record not having been read first. The lesson this row exists to carry is a READING duty, not a compute one. Zero solver core-minutes; nothing was left running. | `docs/RESOURCE_DEPENDENT_TESTS.md` (the established finding, and the load figures quoted above); the DMR act this lane was building landed at commit `6f02808a`, which itself booked **zero new compute**. |
| C-20260901T173329.123480Z-d483e031 | 2026-09-01 | cfd | **M6I R0 — ONERA M6 by import: build the registered 3-level ladder (983,040 / 122,880 / 15,360 cells) and grade Gate A (mesh admission).** Graded **`GATE FAIL`**; this row calibrates its COST, not its verdict. | **60 core-min** = 1.00 core-h / **$0.0513 derived** (registered cap **180 core-min**) | **0.3833 core-min** (23 wall s × 1 rank ÷ 60) = 0.00639 core-h = **$0.00033 derived** | = gross (longest single stage 9 wall s, nowhere near the charter §2 3600-s stall rule; single rank throughout, `nice -n 15`) | **0.0064** — the estimate was **157× HIGH** | **MISPREDICTION, not contention, and the direction is the useful part: this lab has never before costed an ANALYTIC mesh generator and priced one as if it were a snapping/refinement mesher.** R0 was estimated by analogy with `F13`'s 19.3 core-min in-house `blockMesh` ladder and rounded up for a level 8× larger. The published `hcf_wing` generator writes a structured grid **analytically in one pass**: 983,040 cells in **1 wall s**, the coarsener's four levels in **9 s**, `plot3dToFoam` and `checkMesh` **5 s each** at ~1M cells. **The transferable estimate for the next imported structured family is ~0.4 core-min for three levels to ~1M cells, not tens of core-minutes** — an analytic generator and an iterative mesher are two different cost classes and this lab had only ever measured the second. Contention is explicitly NOT the attribution: load average was **5.77 on 16 cores**, and being 157× under cannot be explained by a quiet box. **Waste, named separately: 10 core-s.** An earlier partial run of the build script exited at the OpenFOAM `source` line under `set -u` after the generator and coarsener had already produced their grids; it is excluded from both columns above and named here rather than absorbed (`COMPUTE_BUDGET_CHARTER` §6). **Separately recorded: the SOLVE estimate this row does NOT calibrate.** §5's 14,622 core-min ladder estimate rests on a `DARhoSimpleCFoam` basis of 1.3427 core-min/iteration at 399,360 cells and is **untouched by this row** — R0 is meshing only. That basis was checked this session against the concurrent-`mpirun` core-0 finding and is **unaffected**: `ExecutionTime/ClockTime` over 61 samples reads median **0.9896** (min 0.9851, max 1.0194), so the master rank held a full dedicated core and no pile-up occurred; a pile-up of 4 ranks on one core would read ≈ 0.25. | `verification/runs/M6I_runs/R0_RESULTS.md` §8; per-stage timings in `verification/runs/M6I_runs/COST.tsv` written by `build_m6i_ladder.sh` itself; prereg frozen `73148a9c`, AMENDMENT 1 `cb0bfd25`, AMENDMENT 2 `1d5832b1`, ADDENDUM A (the mpirun basis check) landed with this row. |
| C-20260901T182005.938252Z-575f8f66 | 2026-09-01 | cfd | **JF1E rung E1 — continuation seeding, four blown rows at `C_mu` 0.05/0.10/0.20/0.40, `endTime` 8,000, graded `GATE FAIL` on all four.** This row calibrates its COST; the verdict is in `verification/campaign/JF1E_E1_GRADING_RECORD.md`. | **94.0 core-min** = 1.5667 core-h / **$0.0804 derived** (registered cap **150 core-min**; the four per-row wrapper caps were 40.0 each). **THE REGISTERED BASIS WAS A FIVE-ROW MEAN and this row supersedes it with PER-ROW BASES**, which is the calibration this entry exists to record — see the attribution column. Registration §6, frozen `6e83157c`. | **87.5500 core-min** (5,253 wall s × 1 rank ÷ 60) = 1.45917 core-h = **$0.07486 derived**. Per row, each read from that row's own `RUN_STATUS.<case_id>.txt`: CMU005 **23.8000** (1,428 wall s), CMU010 **25.3167** (1,519 s), CMU020 **19.6333** (1,178 s), CMU040 **18.8000** (1,128 s). Serial throughout, so core-min = wall-min. | **= gross.** Longest single row 1,519 wall s, nowhere near the `COMPUTE_BUDGET_CHARTER` §2 3,600-s stall rule; four links, four completed solves, no re-run, no cap struck (highest row reached 63 % of its own cap; the rung reached 58 % of its registered 150). **Waste: 0 core-min**, named separately per §6 and there is none to name. | **0.931** — the estimate was **7 % high** at the rung level | **THE RUNG-LEVEL RATIO OF 0.93 IS FLATTERING AND THIS ROW EXISTS TO SAY SO.** The registered 94.0 was built as `4 × 23.497`, the MEAN of the five E0 rows. Rebuilt on **PER-ROW BASES** — each E1 row predicted from its OWN `C_mu` predecessor in E0 (CMU005 24.4333, CMU010 21.7500, CMU020 22.3333, CMU040 25.2000, total 93.7166) — the rung total barely moves (ratio **0.934** against 0.931, a 0.3 % difference) **while the per-row ratios spread from 0.746 to 1.164**: CMU005 0.974, CMU010 **1.164**, CMU020 0.879, CMU040 **0.746**. Mean absolute per-row error **14.1 %**, range **−25.4 % to +16.4 %**, a **1.56× spread**. **The mean basis looked accurate at the rung level only because four per-row errors of both signs cancelled.** A rung-level ratio near 1.0 is therefore NOT evidence that the per-solve model is good, and this lab should stop reading it as such. **ATTRIBUTION OF THE RESIDUAL, AND IT IS NOT MISPREDICTION.** The five E0 rows are the SAME mesh (39,984 cells), the SAME `endTime` (8,000) and the same rank count, and they measured **21.7500 to 25.2000 core-min — a 15.9 % spread with no physical cause**. E1's own four rows spread **18.8000 to 25.3167, 34.7 %**, again at identical cell count and iteration count. **The per-row residual is dominated by box-level throughput variation between concurrent tenants on this instance, not by the cost model**, and the measured floor on this box is therefore about **±16 %** per row: no per-row basis built from a single prior row can do better than the noise in the row it is built from. **The actionable estimate for a JF1 L1 blown row at 8,000 iterations is 22 core-min ± 4**, and a four-row rung is **88 core-min ± 8** (errors partially cancelling). Contention is named rather than absorbed, per `COMPUTE_BUDGET_CHARTER` §6. **A SECOND FINDING, RECORDED HERE BECAUSE IT IS A COST FINDING AND IT IS UNPLEASANT.** The per-link wrapper cap (40.0 core-min) × four links = **160**, which is **ABOVE the rung's registered cap of 150** — so the per-link caps alone could not have held rung E1 to its own registration. E1 came in at 87.55 and never tested it. The chain driver now accumulates each link's measured figure and stops the chain at 150 (commit `3d8125e9`); **this is a guard added, not a cap changed**, and no registered number moved. | Verdict and per-row numbers: `verification/campaign/JF1E_E1_GRADING_RECORD.md`. Cost figures read from `verification/runs/JF1_jet_flap/JF1E_E1_CMU{005,010,020,040}_A0/RUN_STATUS.*.txt`; E0 bases from `verification/runs/JF1_jet_flap/JF1_L1_{UNBLOWN_A0,BLOWN_CMU*_A0}/RUN_STATUS.*.txt`. Prereg frozen `6e83157c`, blob `800944bc`, §6. Cap guard `3d8125e9`. |
| C-20260901T182306.952642Z-1721090d | 2026-09-01 | cfd | **JF1G Pass 0 level C2 — 89,964-cell C-mesh level, `endTime` 8,000, COMPLETED.** This row discharges the promise made by the mid-run C2 row appended earlier on 2026-09-01 (landed at commit `5d53328c`; it is the row in this ledger whose id timestamp reads 17:18:18Z), which used a MID-RUN C2 rate and said in terms that C2's completed figures would supersede it. **It supersedes that row's C2 rate, its exponent and its C2 projection.** Pass 0 scores nothing (`038f4bca` §6), so this calibrates cost only. | **48.9 core-min** = 0.815 core-h / **$0.0418 derived** (frozen per-level cap **80.0**; Pass-0 subtotal cap **250**), from `038f4bca` §8's single-case linear rate of `4.0798e-06` core-s per cell per iteration. The superseded row's mid-run REPROJECTION of the same level was **93.4**. | **70.8833 core-min** (4,253 wall s × 1 rank ÷ 60) = 1.18139 core-h = **$0.06060 derived**. Completed: `rc 0`, one `End` line, last `Time = 8000 == endTime`, `stage_at_exit done`, 89 % of its 80.0 cap. Completed per-cell rate **5.8908e-06** core-s/cell/it from `ExecutionTime` 4,239.7 s. C1 alongside it: **21.7500** core-min measured against 21.8 registered. | **= gross.** One solve, no re-run, no cap struck, single rank. The 4,253 s wall figure is above the `COMPUTE_BUDGET_CHARTER` §2 3,600-s stall threshold **and is NOT a stall** — it is one continuous 8,000-iteration solve on a 90k-cell mesh with a monotone `ExecutionTime` trace and an `End` line, so it is not removed; the §2 rule flags a row for inspection and this row was inspected. **Waste: 0 core-min.** | **1.449** — the estimate was **45 % LOW** | **MISPREDICTION FROM A LINEAR MODEL, AND THE CORRECTION CUTS BOTH WAYS.** `038f4bca` priced every level from ONE 40k-cell case at a fixed core-s per cell per iteration. Measured on two completed levels, the per-cell rate rises **1.4548×** for a **2.2501×** cell count, so cost grows as **N^1.4623** — as **r^2.925**, not `r^2`. The linear model stays refuted and it under-prices every finer level. **BUT THE SUPERSEDED ROW OVERCORRECTED, AND THAT IS THE MORE USEFUL LESSON.** Its C2 rate was read MID-RUN at iteration 3,171 and came out **7.7867e-06** against the completed **5.8908e-06** — **32 % high** — which inflated the exponent to *"roughly the 4.3 power of r"* against the true 2.925, and produced a C2 projection of 93.4 against an outturn of 70.9. **A mid-run `ExecutionTime` slope on this box is not a completed-run rate**: the early iterations carry mesh load, field I/O and a GAMG agglomeration that amortises, and reading the slope before it flattens biases it HIGH. That row also called its projection *"realised consequence, not a forecast"*; it was a forecast, and it was wrong by 32 %. **The rule this lab should take from the pair: never supersede a registered estimate with a mid-run rate — wait for the `End` line, and if a number is needed sooner, label it a forecast and say which direction its bias runs.** **THE CONSEQUENCE FOR C3, WHICH IS THE REASON THIS ROW EXISTS RATHER THAN A TIDINESS ONE.** C3 (202,180 cells) forecasts to **221–231 core-min / 3.7–3.9 wall-h** on the two completed levels — two independent bases (total `ExecutionTime` and each level's final-4,000-iteration marginal rate) agreeing to 4.7 %, against `038f4bca`'s registered **110.0**. C1 and C2 have already spent **92.63** of the Pass-0 subtotal cap of **250**, so the remaining **157.37** buys about **5,450–5,710 of C3's 8,000 iterations, roughly 68–71 %**. **Run as registered, C3 is stopped by its own cap before `endTime`, and rule 12 forbids raising it; the cap-struck level would fail the strict completion rule and void the triple as `NOT A RESULT` under rule 5 clause 1.** The forecast is a TWO-POINT power law with zero degrees of freedom and is labelled extrapolation, not measurement. **C3 remains UNRUN and is not launched**: the Gate G1 aspect-ratio question is reserved to the chief and unruled, and this row adds a second, purely arithmetic reason that a favourable ruling alone would not be enough. | `verification/campaign/JF1G_R2_COST_BASIS_DRAFT.md` ADDENDUM A (this session), which supersedes that draft's §1 and §2 without editing them. Figures from `verification/runs/JF1_jet_flap/JF1G_P0_C{1,2}_CMU010_A0/RUN_STATUS.*.txt` and `log.simpleFoam` (both completed). Registration `038f4bca` §8 and §2's level table. The row superseded here is the 2026-09-01 mid-run C2 row landed at commit `5d53328c` — cited by its COMMIT, never by its ledger id, because `scripts/append_record.py` is the only producer of that form and hand-writing one is how a duplicate gets reintroduced by an edit. |
| C-20260901T184528.143750Z-0edc356c | 2026-09-01 | heat-transfer | **T25R3 `S1` — 8-cell battery module, L1 (16,608 cells), two chained legs to 900 s, 11,800 steps, 2 ranks.** ⛔ **`S1` IS NOT A GRADED PHYSICS ROW** (`verification/runs/T-family/T25R3_MODULE_runs/S1/LIMITATION.S1.txt`, registered BEFORE it completed) and its marker verdict is **`NOT DONE`** on a reader defect, not a run defect. **This row calibrates its COST and nothing else.** | **76.2 core-min** = 1.270 core-h / **$0.0651 derived** (registered CAP **305**, `timeout_s` 9,150). Basis: T25R2's own measured L1 rate, fitted per-step as `0.056033 + 0.018223·n` seconds and evaluated at `n = 15` sweeps × 11,800 steps. | **71.091 core-min** (leg A `ExecutionTime` 643.78 s + leg B 1,488.94 s = 2,132.72 s × 2 ranks ÷ 60) = 1.18485 core-h = **$0.0608 derived** | **= gross.** Two legs, 643.78 s and 1,488.94 s, both under the `COMPUTE_BUDGET_CHARTER` §2 3,600-s stall threshold; `rc = 0` on both; `capped = no`, 23.3 % of the 305 cap used; no re-run of either leg. | **0.933** — the estimate was **7 % high** | **THE 0.933 IS A COINCIDENCE AND THIS ROW EXISTS TO SAY SO. DO NOT READ IT AS A GOOD ESTIMATE.** The POINT extrapolated T25R2's L1 rate linearly in cell count, and it landed on L1 only because **L1 is where the model's hidden assumption happened to hold**. The same model was **19–33× LOW on every finer level** (S2 ×18.7, S3 ×28.7, T2 ×25.3, T4 ×33.4, W30 ×19.8), and the cause is that per-step cost is dominated by a pressure solve whose iteration count runs **0.31 → 41.64 → 382.20** across L1/L2/L3 under an **ABSOLUTE** `p_rgh tolerance 1e-08`. **Median reduction actually ACHIEVED: L1 ×1.00, L2 ×1.51, L3 ×100.94 — on L1 the pressure solve did nothing at all.** So the model was not merely optimistic; **at L1 it was measuring a solver that was not running**, and a ratio of 0.933 obtained that way carries no predictive skill for any other level. **THE TRANSFERABLE RULE: a cost model calibrated on one mesh level is invalid across a ladder unless the linear-solver convergence criterion is MESH-INDEPENDENT — otherwise the model silently measures a different amount of work on every level.** Confirmed by the T25R4 probe, where a fixed **relative** criterion collapsed the iteration spread from **1,233× to 1.16×** and the per-step cost ratios to **2.32 / 2.06** against cell ratios 2.25 / 2.25 — near-linear, i.e. the scaling the linear model had assumed all along but could not have obtained under the old criterion. **CONTENTION IS NOT THE ATTRIBUTION and this was tested rather than asserted:** `S1` ran in the same window, on the same 16-core box, against the same five concurrent siblings that were 19–33× slow, and matched its own prediction to **×1.02** on the mid-run rate. **`S1` IS THE CONTROL THAT PROVES THE SLOWDOWN WAS NUMERICS AND NOT THE BOX.** **WASTE, NAMED SEPARATELY (§6) AND NOT ABSORBED: ~10 core-min** across three aborted launch attempts before the surviving fleet — a `set -u` death inside the OpenFOAM bashrc whose own redirect swallowed the message, a `SIGKILL`-on-caller-exit from a missing `</dev/null`, and a fleet in which **all twelve ranks were pinned to CPUs 0 and 1** because every independent `mpirun` numbers cores from zero (`L-431`). All three are this lane's, none is in either column above, and none is in the ratio. | Cost figures: `verification/runs/T-family/T25R3_MODULE_runs/S1/COMPLETION_REPORT.S1.txt` and that case's `log.solve.legA` / `log.solve.legB`. Registration frozen `8cef4791` §12.3 (POINT and CAP); A1 `e46aa244`; D1 `6b1f3968`. Rung closure and the 19–33× figures: `docs/campaigns/T-family/T25R3_RESULTS.md` (`2908ec2f`). The mesh-independent criterion and the 1.16× probe result: `docs/campaigns/T-family/T25R4_PREREGISTRATION.md` §1 and `verification/runs/T-family/T25R4_MODULE_runs/GP_VERDICT.json`. Lessons `L-431`, `L-432`. |
| C-20260901T202711.040168Z-2594ceb9 | 2026-09-01 | cfd | **JF1E rung E2a — turbulence-equation relaxation `k` and `omega` 0.7 -> 0.5, four blown rows at `C_mu` 0.05/0.10/0.20/0.40, `endTime` 8,000, graded `GATE FAIL` on all four.** This row calibrates its COST; the verdict is in `verification/campaign/JF1E_E2a_GRADING_RECORD.md`. **It is scored against PER-ROW bases, not a mean** — the correction the JF1E rung E1 row (landed `ef465160`) asked its successor to make. | **94.0 core-min** = 1.5667 core-h / **$0.08037 derived** (registered cap **150**; four per-link wrapper caps of 40.0 each). Registration §6, frozen `6e83157c`, blob `800944bc`, verified at grading to be the file that ran. **The transferable per-row estimator carried forward from E1 is 22 core-min ± 4 per row, 88 ± 8 per four-row rung**, and E2a is scored against BOTH that and the registered 94.0. | **79.4500 core-min** (4,767 wall s × 1 rank ÷ 60) = 1.324167 core-h = **$0.067930 derived**. Per row, each read from that row's own `RUN_STATUS.<case_id>.txt`: CMU005 **21.1167** (1,267 wall s), CMU010 **19.9333** (1,196 s), CMU020 **16.8333** (1,010 s), CMU040 **21.5667** (1,294 s). Serial throughout, so core-min = wall-min. | **= gross.** Longest single row 1,294 wall s, far below the `COMPUTE_BUDGET_CHARTER` §2 3,600-s stall rule; four links, four completed solves, no re-run, no cap struck (highest row 54 % of its own 40.0 cap; the rung 53 % of its registered 150). Chain elapsed wall 18:15:07Z→19:34:34Z is 79.45 min, **exactly** the sum of the four link walls, so **zero inter-link dead time**. **Waste: 0 core-min**, named separately per §6 and there is none to name. | **0.845** against the registered 94.0 — the estimate was **15.5 % high**. **0.903** against the transferable 88 ± 8. | **ALL FOUR PER-ROW ERRORS CARRY THE SAME SIGN, SO THIS RUNG IS THE OPPOSITE CASE TO E1 AND THE RUNG RATIO DOES NOT FLATTER.** Scored per row against the 22 core-min estimator: CMU005 **0.960** (−4.0 %), CMU010 **0.906** (−9.4 %), CMU020 **0.765** (−23.5 %), CMU040 **0.980** (−2.0 %). Mean absolute per-row error **9.7 %**, every row LOW, so nothing cancels and the rung ratio 0.903 is simply the honest mean — where E1's 0.93 was four errors of both signs cancelling. **Against the ±4 band: three of four rows inside (18–26), CMU020 outside by 1.17 core-min. Against the ±16 % measured floor: three inside, CMU020 outside at −23.5 %.** At the rung level 79.45 sits **0.55 core-min below the 80–96 band, a 0.7 % miss on the low side** — the estimator is very nearly right and errs cheap. **ATTRIBUTION: CONTENTION, AND IT IS ATTESTED RATHER THAN ASSERTED THIS TIME.** Per-iteration cost at identical mesh (39,984 cells), identical `endTime` and identical rank count — E1 **0.178500 / 0.189875 / 0.147250 / 0.141000** core-s/iter against E2a **0.158375 / 0.149500 / 0.126250 / 0.161750**. Within-rung spread is **1.347x for E1 and 1.281x for E2a at fixed configuration**, both larger than the 1.10x between-rung difference. **Decisively, the per-row ordering INVERTS between rungs**: E1's cheapest row (CMU040) is E2a's dearest, and E1's dearest (CMU010) is among E2a's cheapest. A systematic per-iteration cost of the one change (relaxation 0.7→0.5) would shift the rows together and preserve their order; it does not. **So the −9.7 % is box throughput, not the relaxation change, and no per-solve model can beat it.** Concurrent tenancy during the E2a window (18:15:07Z–19:34:34Z) is measured, not inferred: T-family compute was live throughout — `verification/runs/T-family/T23G2_runs/` writing `polyMesh` across levels L1/L2/L3 and `verification/runs/T-family/T25R4_MODULE_runs/P3/` writing decomposed `processor*/` trees. **The estimator 22 core-min ± 4 per row and 88 ± 8 per rung is CONFIRMED for three rows of four and is left unchanged**; one rung is not enough to move it, and widening it on a single low outlier would be fitting noise. **A METHOD DEFECT FOUND WHILE BUILDING THIS ROW, RECORDED BECAUSE IT PRODUCES SILENT FALSE ZEROS IN COST AND CONTENTION WORK.** `find` in this environment is a shell function shimming to `bfs`, which **REJECTS the GNU-find timestamp form `-newermt '2026-09-01 18:15:07 UTC'` as "Invalid timestamp" and the shell function nonetheless RETURNS rc=0**, so a contention sweep written that way prints an empty result and exits clean. The first sweep for this row did exactly that and read as "no concurrent tenants". **It was caught only because the sweep carried a planted control** (CLAUDE.md rule 3) requiring it to see E2a's own 521 files in its own window; the control returned 0 and the reader was rejected. The accepted forms are ISO-8601 (`'2026-09-01T18:15:07Z'`), under which the control returns 521 and the sweep returns the T-family tenancy above. **A contention or cost figure derived from `find -newermt` with a `UTC`-suffixed timestamp on this box is void unless it carried a live control.** | Verdict and per-row numbers: `verification/campaign/JF1E_E2a_GRADING_RECORD.md`. Cost figures read from `verification/runs/JF1_jet_flap/JF1E_E2a_CMU{005,010,020,040}_A0/RUN_STATUS.*.txt`; chain window from `verification/runs/JF1_jet_flap/JF1E_E2a_LAUNCH_RC.txt`. E1 comparison figures from `verification/runs/JF1_jet_flap/JF1E_E1_CMU*_A0/RUN_STATUS.*.txt` and the E1 calibration row landed at `ef465160`. Prereg frozen `6e83157c`, blob `800944bc`, §6. Grading path `verification/runs/JF1_jet_flap/analyse_jf1_ladders.py` at `74c4f5fb`, blob `ef4c0b0c`, unmodified. |
| C-20260902T052712.204002Z-ca984e29 | 2026-09-02 | cfd | **JF1R-QB4 — quiet-box 4-rank parallel rerun of the frozen JF1 L1 five-point sweep (unblown + `C_mu` 0.05/0.10/0.20/0.40), `endTime` 8,000, feasibility/timing, NO GATE.** All five cases COMPLETE under CLAUDE.md rule 4 (rc 0, one `End` line, last `Time = 8000`, 8,000 `ExecutionTime` lines, all fields at `8000/` reconstructed and per-processor, age guard holds on every case); registered physics cross-check: all four blown rows within 0.006% of the landed serial `Cl`, against a 1% expectation. Per Sanaa's 2026-09-02 ~09:30Z ruling the JF1 screens keep her interim compute set permanently; these measured figures stay internal. | 170 core-min, derived basis (frozen note §3: serial baseline 117.5 core-min on a shared box, assumed 4-rank efficiency 0.55–0.85); cap 400 core-min STRUCTURAL (per-case `timeout 1200 s`), frozen at `4778bf75` before enqueue | 131.73 core-min (sum of the five `core_min_MEASURED` cells, wall s × 4 ranks ÷ 60; per-case walls 344–468 s; sweep span 893 s) ≈ $0.11 derived at $0.0513/core-h | 131.73 core-min — cleaned equals gross: no timeout fired, no case failed, max wall 468 s, nothing near the 3600 s stall line | **0.77** (cleaned/predicted) | misprediction, conservative side, two named causes: (1) the assumed 0.55–0.85 efficiency band was beaten — apparent per-case serial-core-s over parallel-core-s realised 0.76–1.07; (2) the serial baseline itself was measured on a shared box with no load record, so the estimate inherited its unquantified contention inflation. No contention this run (queue-daemon busy-ceiling hold is the quiet-box mechanism); waste separately named: 0 | `verification/runs/JF1_jet_flap/JF1R_QB4_COMPLETION_GRADING.md` §5; per-case `RUN_STATUS` files under `verification/runs/JF1_jet_flap/JF1R_QB4_<TAG>/`; note frozen at `4778bf75`, blob `c05afbf6` |
| C-20260902T215932.385336Z-ef920ed2 | 2026-09-02 | heat-transfer | **T23G2 — the wall-resolved, similarity-repaired grid triple at (305 W, 20 m/s), three levels solved and GRADED `NOT A RESULT`.** This row calibrates its COST; the verdict is in `docs/campaigns/T-family/T23G2_RESULTS.md`. **The rung is stated COMPLETE for rule 12's purposes by this team**, answering the question `VERIFICATION_CHARTER.md` §2d.8 left to heat-transfer: the budgeted process (build, solve three levels, grade) finished and produced a verdict from the fixed vocabulary, and `NOT A RESULT` is a verdict, not the absence of one. | **579.2 core-min POINT** (per-level 17.0 / 89.6 / 472.6), CAP **1,365** (per-level 45 / 220 / 1,100, each cap enforced as the wrapper `timeout`), frozen pre-compute at `T23G2_PREREGISTRATION.md:949-954` (A1.7 budget), commit `658b3ba4`. Basis DERIVED from measurement then extrapolated: `T23G_F`'s measured 145.75 core-min / 10,000 iter at 158,720 cells at `ranks = 1` = 0.0145750 core-min/iter, scaled by T23G's measured superlinear exponent 1.1957 on cell count. | **536.77 core-min** = 8.9461 core-h ≈ **$0.4589 derived** at $0.0513/core-h (`cost_basis = REPORTED-BY-OWNER`; the box cannot read its own billing, `COMPUTE_BUDGET_CHARTER.md` §5). Sum of the three `core_min` cells in each level's own `STATUS.T23G2_L<n>`: L1 17.4500 (1,047 wall s), L2 85.3000 (5,118 wall s), L3 434.0167 (26,041 wall s). **`ranks` MEASURED as 1 at every level** (`ranks=1` in each `STATUS`, `decomposition=NONE` recorded as an absence in each `START`). `capped=0` at all three; no timeout fired. | **GROSS is the published figure, and the stall rule is NAMED rather than applied.** §2's one cleaning rule — *"a ledger row over 3600 wall seconds is an infrastructure stall"* — is matched by **two of the three levels** (L2 5,118 s, L3 26,041 s), but **they are not stalls**: each ran continuously to its registered `endTime` with rc 0, exactly one `End` line and `ExecutionTime` count == `endTime`, under registered timeouts of 13,200 s and 66,000 s set for precisely these durations. Applying the rule mechanically would strip **519.32 of the 536.77 core-min** as "infrastructure" and yield a plainly false cleaned figure. **No cleaned figure is published for this row**; the gap in §2 (its object is a `self_audit.py` ledger row, not a registered multi-hour solve) is **referred to `verification`, not decided by heat-transfer**. | **0.9268** (gross/predicted; no cleaned figure exists for this row — see the previous column). Against cap: **0.393** (536.77 / 1,365). **Registered prediction P6′ HELD**: actual inside [400, 950] core-min, ratio inside [0.69, 1.64] (`:992-995`). | **Misprediction in the conservative direction, on the per-iteration basis. WASTE SEPARATELY NAMED AND IT IS ZERO** — no level restarted, none capped, no timeout fired, none abandoned and re-solved; every core-minute bought a level `DONE` under rule 4, and nothing is laundered into the ratio. Realised per-iteration cost came in **below** the basis at the two levels that dominate the spend and slightly **above** at the cheapest: L1 **0.0029083** vs 0.0028300 predicted (**+2.8 %**), L2 **0.0071083** vs 0.0074663 (**−4.8 %**), L3 **0.0180840** vs 0.0196915 (**−8.2 %**) — all three derived from wall s ÷ `endTime`. **Likely cause STATED AND NOT MEASURED:** the basis was taken from `T23G_F` on a shared box whose load average is **not recorded**, and re-applied here at a launch load of **3.51 3.02 4.86 on 16 cores** (`T23G2_L1/START.T23G2_L1`). This is the SECOND row to hit the same defect — **row C-4 already filed the lesson: record the load average beside any per-iteration cost basis, and state the load the basis is being re-applied at.** It was not done for this rung's basis, and the successor should not inherit the same blind spot. **No contention effect is claimed as measured.** | `docs/campaigns/T-family/T23G2_RESULTS.md` §10, landed at `d6570792`; per-level `STATUS.T23G2_L<n>` and `START.T23G2_L<n>` under `verification/runs/T-family/T23G2_runs/T23G2_L<n>/`; budget frozen at `658b3ba4` (`T23G2_PREREGISTRATION.md:949-954`); comparator frozen at `976776f4`; `§2d.1` repairs `768203a9`, `23d9d9b2`, `c2ce64a5`, `ab5e753c`, `91bb04f8` under the grant at `3dad5bae`. **⚠ Sidecar provenance caveat: the launcher that wrote every `STATUS`/`START` figure above (`run_t23g2.sh`, `preflight_gate_t23g2.py`) is UNTRACKED and has no committed sha**, so the wall-second and `ranks` figures are read from files whose writer cannot be pinned; the solve itself is evidenced by `log.solve`. |
| C-20260902T220017.984139Z-f803ed3b | 2026-09-02 | ansys-verification | **VMFL054-R2 — trapezoidal driven cavity, r=2 Roache triple graded; rung verdict `GATE FAIL` on the order limb, exactly as pre-registered before the run** | **≤ 3 core-min registered** (frozen `PREREGISTRATION.md` §7; running-total cap 40 core-min in the driver) | **1.1 core-min** (L1 0.0167 / L2 0.1333 / L3 1.1 running total; wall 1 / 7 / 58 s at RANKS 1) = **$0.00094 DERIVED** at $0.0513/core-h, owner-stated, NOT measured — this box cannot read its own billing | **1.1 core-min — gross and cleaned are the SAME figure.** No stall (no level over 3600 wall s), no contention correction, and **ZERO waste on this run** | **0.37x (cleaned/predicted)** — under-spend, well inside the cap | **MISPREDICTION, in the conservative direction, and the lesson is actionable: THE SCRATCH FEASIBILITY SMOKE WAS THE ACCURATE PREDICTOR AND THE REGISTERED ESTIMATE WAS PADDED.** The pre-freeze smoke measured the same triple at **1.07 core-min**; the actual graded run came in at **1.1** — a 2.8 % error. The registered `≤ 3` carried ~3x headroom instead. **For a case whose smoke runs the full triple, the smoke's measured cost is the estimator; a padded figure discards a measurement the team already owns.** NOT contention (box load 3-5 of 16 cores, dafoam chains only) and NOT waste. **Named separately per `COMPUTE_BUDGET_CHARTER` §6 and NOT folded into the ratio: the superseded run VMFL054 R1 (register row #52) consumed 0.000 core-min** — its driver aborted at `blockMesh` rc 127 before any solver started, so the infrastructure defect cost compute time of exactly zero and the two-attempt total is still 1.1 core-min | freeze `489aef16`; verdict rows `ANSYS_VALIDATION_REGISTER.md` **#53** (this run) and **#52** (the zero-compute R1 abort); run root `verification/runs/ansys_verification/VMFL054-R2/` |
| C-20260902T221439.942229Z-6ec6c321 | 2026-09-02 | dafoam | **MAAOA — fixed-lift (Ma, AoA) sweep on the A1WR L3 wall-resolved mesh (130,304 cells), PATCHED image only, seven points at np = 1, concluded 2026-09-02T21:58:19Z. ⚠ THE ITEM HAS NO VERDICT OF RECORD: the frozen reader `maaoa_read.py` REFUSED at `reader_rc=2` (`SELFTEST REFUSED -- M1: 5 controls.`), and a refusal is not a verdict; none is supplied and the grading path is fixed at the pre-registration commit so none may be substituted. THIS ROW CALIBRATES COST ONLY and asserts no verdict.** | **315 core-min registered** (prereg §5 frozen at `586b72ca`, hash-checked: disk blob `c399da2a` == the blob at that commit). Basis: anchors **MEASURED** (A1WR §13.2 — 0.37163 s/iter compressible, 0.35625 incompressible at 130,304 cells, np = 1); trim primal count **EXTRAPOLATED** and named at the freeze as the term most likely to be wrong (~7,900 iter/point ⇒ ~45 core-min/point × 7). The registered 45 core-min/point **implies 0.34177 s/iter**, a shade below both stated anchors. Caps: **120 core-min per point** (7,200 s in-container deadline), **900 core-min item ceiling**. **$0.269325 DERIVED, NOT MEASURED** at c7a.4xlarge $0.0513/core-h, **owner-stated, reported-by-owner — the box cannot read its own billing** (`COMPUTE_BUDGET_CHARTER.md` §5). GPU: 0 GPU-h. | **664.0 core-min MEASURED** from `/home/ubuntu/certonomous-runs/MAAOA/CHAIN_LEDGER.tsv` (seven rows, `wall_s` × 1 rank ÷ 60: 92.5000 + 91.4667 + 92.4833 + 92.4833 + 92.4667 + 92.4500 + 110.1500), agreeing with `STATUS.MAAOA_chain` `spend_total=664.0` and `/home/ubuntu/certonomous-runs/MAAOA/COST_MAAOA.txt`. **$0.56772 DERIVED, NOT MEASURED** at the rate above, same owner-stated caveat. **No cap breached**: largest point 110.15 (`INCOMP`) against the 120 per-point cap; 664.0 against the 900 ceiling. The two aborted pre-compute fires (prereg Amendments 1 and 2) spent **0.0 core-min** and are in no figure here. | **NONE PUBLISHED — and the reason is named rather than left to a judgement.** `COMPUTE_BUDGET_CHARTER.md` §2's single cleaning rule (a ledger row over 3,600 wall s is an infrastructure stall) applied literally would match **all seven rows** (5,488–6,609 wall s) and clean 664.0 to **0.0**, which is false: every row's log shows a live time loop advancing (41 `Time =` prints to `Time = 4000` on each compressible point, 141 across `INCOMP`'s five primals) with `ExecutionTime` within a few seconds of the ledger wall (`MA288`: `ExecutionTime = 5518.61` vs 5,550 wall s). The 3,600 s threshold was calibrated on a ledger whose solver rows cluster far below it; a 130,304-cell wall-resolved np = 1 solve is outside that population, and a cleaned figure whose rule mis-fires is the charter's own *"judgement wearing a number's clothes"*. **Basis reported is GROSS, labelled gross.** | **2.108 gross/predicted** (664.0 / 315; no cleaned figure exists, so the ratio column is gross-based and says so). **0.738 of the 900 core-min ceiling.** | **THE MISS IS IN THE RATE, AND THE RATE'S CAUSE IS CONTENTION — with one narrowing and one honest limit.** Decomposition against the registered implied 0.34177 s/iter, exact in both arms: **six compressible points** — iterations **4,000 / 7,900 = 0.506** (OVER-predicted 2×) × rate **1.3846 / 0.34177 = 4.051** = **2.051**, matching 92.31 / 45 = 2.051; **`INCOMP`** — iterations **13,422 / 7,900 = 1.699** × rate **0.4924 / 0.34177 = 1.441** = **2.448**, matching 110.15 / 45 = 2.448. Compressible rate measured **1.3846 s/iter** (container wall, mean 5,538.5 s ÷ 4,000) and **1.3778 s/iter** (solver, mean last `ExecutionTime` 5,511.34 s ÷ 4,000) = **3.73× and 3.71×** the 0.37163 anchor. **CONTENTION: the anchor was measured at low concurrency and spent at high concurrency** — six one-core points on cpusets {2–7} while A1WR held eight more units on {8–15} of the same 16-core box (prereg §5); `launcher.queue.out` load averages 4.60 → 5.03 at the six launches and **8.01** at `INCOMP`'s, and `INCOMP` (run 1-up) came in at **1.38×** its own 0.35625 anchor against the compressible arm's 3.71×. **HONEST LIMIT: the two arms differ in concurrency AND in solver, so this item does not isolate contention from a compressible-solver cost on the wall-resolved mesh, and does not claim to.** **NARROWING of the rate-only attribution: it holds exactly for the six compressible points but only half for `INCOMP`, which misses on BOTH terms** — 1.699× the iterations and 1.441× the rate, because five primals ran over three Newton trim steps and two of them went the full 4,000 to `endTime` instead of stopping at 1e-8. **Forward rules measured here (A1WR L3, np = 1): a compressible `DARhoSimpleFoam` iteration run 6-up beside A1WR's 8 units prices at ≈1.38 s/iter (six measurements, 1.3720–1.3875 container basis); an incompressible `DASimpleFoam` iteration run 1-up at ≈0.49 s/iter; a converging fixed-lift Newton trim costs ~13,400 primal iterations, not ~7,900.** ⚠ **NAMED SEPARATELY AND NEVER FOLDED INTO THE RATIO (`COMPUTE_BUDGET_CHARTER.md` §6): 553.85 core-min — 83.4 % of the item's entire spend, $0.47354 derived not measured — RETURNED NO GRADABLE DATA**, the six compressible points each writing the sentinel `CL = 1.0 / CD = 1.0` with `Primal solution failed!` and no trim angle and no drag. It is not subtracted from the gross, not laundered into a cleaning, and the 2.108 ratio is not explained by it. **It was not valueless, which is a different claim from not being waste**: standing beside `INCOMP`'s convergence on the same mesh in the same item it is one half of the controlled pair that closed the `S-27` setup-failure triage (`docs/LAB_STATE.md`, dafoam section) — but a diagnosis is not what this item was costed to produce. **No other waste: nothing was retried, relaxed, re-tuned, re-solvered or discarded, and no point was cut by its deadline.** | `cases/dafoam/ladder-a/A1/fixed_lift_mach_sweep/RESULTS.md` §5; pre-registration `cases/dafoam/ladder-a/A1/fixed_lift_mach_sweep/MAAOA_PREREGISTRATION.md` §5 frozen at `586b72caf39dad1f79b028cf590818da585688a2`; figures from `/home/ubuntu/certonomous-runs/MAAOA/CHAIN_LEDGER.tsv`, `/home/ubuntu/certonomous-runs/MAAOA/COST_MAAOA.txt`, `cases/dafoam/ladder-a/A1/fixed_lift_mach_sweep/STATUS.MAAOA_chain` and `.../launcher.queue.out`; queue entry `verification/queue/dafoam/launched/MAAOA_chain.json` |
| C-20260902T224321.920752Z-1e90052f | 2026-09-02 | heat-transfer | **CORRECTION ROW, under this ledger's append rule 1. It corrects the T23G2 row — dated 2026-09-02, team heat-transfer, the wall-resolved similarity-repaired grid triple at (305 W, 20 m/s), the row whose tool-allocated id ends `-ef920ed2` — and REPLACES NOTHING IN IT; that row stands exactly as committed.** Exactly one cell is corrected: the **ratio**. No predicted figure, no actual, no cap, no cleaning decision, no gap attribution and no verdict changes. **T23G2's rung verdict remains `NOT A RESULT` and is untouched by a ledger arithmetic correction.** ⚠ **WHY THE CORRECTED ROW IS NAMED BY ITS SUFFIX AND NOT BY ITS FULL ID — A RULE COLLISION, DISCLOSED RATHER THAN WORKED AROUND SILENTLY.** This ledger's append rule 1 requires a correction to be *"a new row naming the row it corrects"*. `scripts/append_record.py` — the only sanctioned producer of tool-allocated ids since Sanaa's 2026-08-31 PLUMBING FREEZE — **REFUSES (exit 9) any rows file containing a tool-form id anywhere in its text**, deliberately and *"for ANY record's prefix rather than only this record's"* (`check_allocation`, `:844-852`). Its purpose is to stop a duplicate id being hand-written into an id cell; its scan cannot tell an id cell from a prose citation, **so a correction row for a tool-allocated row cannot be written through the sanctioned tool at all.** The refusal was reproduced on this exact row. **Nothing was bypassed:** the full id is not written here, the row is named by date, team, subject and its unique 8-hex suffix, and **the full id is written out in `docs/campaigns/T-family/T23G2_RESULTS.md` §16**, which is not appended by that tool. The collision is docketed. | **Unchanged: 579.2 core-min POINT** (per-level 17.0 / 89.6 / 472.6), cap **1,365**, frozen pre-compute at `T23G2_PREREGISTRATION.md:949-954`, commit `658b3ba4`. Restated only so this row reads on its own; the corrected row is the source. | **Unchanged: 536.7667 core-min** = sum of the three `core_min` cells (L1 17.4500, L2 85.3000, L3 434.0167) = 8.9461 core-h ≈ **$0.4589 DERIVED, NOT MEASURED** at $0.0513/core-h, `cost_basis = REPORTED-BY-OWNER` (the box cannot read its own billing, `COMPUTE_BUDGET_CHARTER.md` §5). `ranks` MEASURED as 1 at every level; `capped=0`; no timeout fired. | **Unchanged: NONE PUBLISHED**, for the reason the corrected row gives — §2's single cleaning rule matches two of the three levels and neither is a stall. The referral of that §2 gap to `verification` is unchanged and still open. | **CORRECTED: 0.9267, NOT 0.9268.** 536.7667 / 579.2 = **0.926738087…**, which is **0.9267** to four decimals; the corrected row rounded the last digit up. Against cap unchanged at **0.393** (536.77 / 1,365). **Registered prediction `P6′` HELD BEFORE THE CORRECTION AND HOLDS AFTER IT** — the registered band [0.69, 1.64] contains both figures — so **no prediction, threshold, gate or verdict moves in either direction** (`T23G2_PREREGISTRATION.md:992-995`). | **The corrected row's gap attribution is unchanged in every particular**: misprediction in the conservative direction on the per-iteration basis; waste separately named and zero; the unrecorded load average filed as the same defect row `C-4` already lodged. **This correction is arithmetic in the ratio cell alone** — the summed core-minutes, the predicted point and the cap were correctly stated and are not touched. **Raised by the heat-transfer supervisor against its own figure**, which had been reported upward more than once, and landed as a correction ROW rather than an edit because **append rule 1 forbids editing a committed row** — the ledger's conventions outrank any writer's preference for a tidier file. **Named rather than absorbed: a rounding error in the lab's calibration record is exactly the kind of thing that should not sit unremarked, precisely because nothing depended on it.** | Corrects the T23G2 row described in column 4 (suffix `-ef920ed2`). The same figure is corrected in `docs/campaigns/T-family/T23G2_RESULTS.md` **§16** — three places: the `P6′` row of §9, §10's ratio sentence, and §10's closing `P6′` bullet — landed there as a dated amendment rather than a silent edit, and **that section carries the corrected row's full id.** Underlying figures unchanged: per-level `STATUS.T23G2_L<n>` under `verification/runs/T-family/T23G2_runs/T23G2_L<n>/`; budget frozen at `658b3ba4`. |
| C-20260902T224635.706381Z-ce00729d | 2026-09-02 | cfd | **JF1E rung E2b — `nNonOrthogonalCorrectors 1 -> 2`, four blown rows at `C_mu` 0.05/0.10/0.20/0.40, `endTime` 8,000, graded `GATE FAIL` on all four.** This row calibrates its COST; the verdict is in `verification/campaign/JF1E_E2b_GRADING_RECORD.md`. | **141.0 core-min** = 2.35 core-h / **$0.12056 derived** (registered cap **220**; four per-link wrapper caps of 55.0 each, set at 220/4 for this rung — E1 and E2a keep the 40.0 they ran at, unchanged). Registration §6, frozen `6e83157c`, blob `800944bc`, verified before the first solve and again at grading to be the file that ran. **The 141.0 is a PRODUCT OF TWO FACTORS and this row scores each separately**: a base of 94.0 (the E0 five-row mean, `4 × 23.497`) times a multiplier of **1.5**, registered as *"2 non-orth correctors ≈ +50 % on `p`"*. | **101.5500 core-min** (6,093 wall s × 1 rank ÷ 60) = 1.6925 core-h = **$0.086825 derived**. Per row, each read from that row's own `RUN_STATUS.<case_id>.txt`: CMU005 **25.3500** (1,521 wall s), CMU010 **25.0000** (1,500 s), CMU020 **26.8667** (1,612 s), CMU040 **24.3333** (1,460 s). Serial throughout, so core-min = wall-min. | **= gross.** Longest single row 1,612 wall s, far below the `COMPUTE_BUDGET_CHARTER` §2 3,600-s stall rule; four links, four completed solves, no re-run of a solve, no cap struck (dearest row 49 % of its own 55.0 cap; the rung 46 % of its registered 220). **Waste, named separately per §6 and NOT absorbed into the ratio: 0.0167 core-min (1 wall s)** — a first launch refused at the staging stage by a defective assert in the launcher (a `grep -q` inside a pipeline under `set -o pipefail`, which SIGPIPEs its upstream `grep` on first match and so returns non-zero on SUCCESS); it died before the mesh build, before `checkMesh` and before the solver, produced no time directory and no solver log, and its stub is preserved at `verification/runs/JF1_jet_flap/JF1E_E2b_CMU005_A0.stage_refused_2026-09-02T2055Z_PRESERVED` rather than deleted. | **0.720** against the registered 141.0 — the estimate was **38.9 % high** | **THE 0.72 DECOMPOSES CLEANLY INTO TWO ERRORS OF ALMOST EQUAL SIZE, AND ONLY ONE OF THEM IS NEW.** Registered 141.0 = base **94.0** × multiplier **1.5**. Measured: E2a's actual four-row rung was **79.4500**, so the base was **94.0/79.45 = 1.183× high (18.3 %)** — that is the *same* stale E0-mean base the E1 calibration row (landed `ef465160`) already flagged and the E2a row (landed at the id timestamped 20:27:11Z) already re-scored, and nothing new is claimed for it. The multiplier is the new finding: measured rung-level cost ratio E2b/E2a is **101.55/79.45 = 1.278** in wall time and **6,068.0/4,725.2 = 1.284** in pure `ExecutionTime` CPU seconds, against a registered **1.5** — **1.5/1.278 = 1.174× high (17.4 %)**. The two compound exactly: 1.183 × 1.174 = **1.388**, and 141.0/101.55 = **1.3885**. **WHY THE ×1.5 WAS WRONG, STATED SO IT IS TRANSFERABLE.** Going from `nNonOrthogonalCorrectors` 1 to 2 takes `p` from two solves per SIMPLE iteration to three. A multiplier of 1.5 for that step is only correct if the pressure solves are **100 % of the iteration** — i.e. if momentum and the turbulence transport pair cost nothing. Solving `(3P+Q)/(2P+Q) = 1.284` for the per-`p`-solve share `P` of an E2a iteration gives **P ≈ 0.28, non-`p` work ≈ 0.43**; on the same arithmetic excluding the one E2a row whose CPU time is anomalously fast against its own siblings (CMU020, 987.0 s against 1,262.9 / 1,192.4 / 1,282.9) the ratio is 1.193 and **P ≈ 0.19**. **So one additional non-orthogonal corrector on this case costs 19–28 % of an iteration, not 50 %, and the transferable rule is: price an added corrector by the pressure solve's SHARE of the iteration, never as if the pressure solve were the iteration.** For E2c, which inherits E2b's corrector, this puts a four-row rung nearer **102 core-min** than the registered 94.0 — inside the registered cap of 150 with 32 % margin rather than the 60 % the registration implies. **No cap, threshold, gate or label is altered by that observation.** **CONTENTION IS NOT THE ATTRIBUTION HERE, AND IT WAS TESTED RATHER THAN ASSUMED.** The box was genuinely busy in the E2b window (load average 16.02 on 16 cores; concurrent tenants measured by a `find -newermt` sweep in ISO-8601 form carrying a live planted control that required it to see E2b's own 132 files and did: `verification/runs/T-family/T25R5_LINSOLVER_runs` 1,010 files, `verification/runs/ansys_verification/VMFL007-R3-DIAG` 127, `VMFL054-R2` 80). Yet `ExecutionTime/ClockTime` reads median **0.9951 / 0.9973 / 1.0001 / 0.9993** across the four rows, so **every row held a full dedicated core throughout and none was time-sliced** — and the CPU-time ratio (1.284) agrees with the wall ratio (1.278) to 0.5 %, so the multiplier finding is not a contention artefact. **Honest limit of that control: `ExecutionTime/ClockTime ≈ 1` rules out core-sharing, NOT memory-bandwidth contention, which would slow CPU and wall together and is invisible to this ratio.** **A SECOND OBSERVATION, RECORDED BECAUSE IT CUTS AGAINST THE LAB'S OWN ±16 % PER-ROW FLOOR.** E1 and E2a both concluded that per-row cost on this box carries an irreducible ~±16 % spread at identical mesh, `endTime` and rank count. E2b's four rows spread **1,456.7–1,609.9 s of CPU time, a factor of 1.105** — the tightest of the three rungs (E1 1.35×, E2a 1.30×) and this on the busiest box of the three. **One rung is not enough to move the estimator and it is left unchanged at 22 core-min ± 4 per row; but the spread is not obviously a fixed property of the box, and a successor should not treat ±16 % as a floor that cannot be beaten.** Scored per row against the registration's own logic applied per-row (22 × 1.5 = 33 core-min): CMU005 **0.768**, CMU010 **0.758**, CMU020 **0.814**, CMU040 **0.737** — all four LOW, same sign, nothing cancels, mean **0.769**, so the rung ratio does not flatter. | Verdict and per-row numbers: `verification/campaign/JF1E_E2b_GRADING_RECORD.md`. Cost figures read from `verification/runs/JF1_jet_flap/JF1E_E2b_CMU{005,010,020,040}_A0/RUN_STATUS.*.txt`; chain window and driver rc from `verification/runs/JF1_jet_flap/JF1E_E2b_LAUNCH_RC.txt` (20:57:42Z → 22:39:15Z, `chain_rc 0`) and `JF1E_E2b_CHAIN.log`. `ExecutionTime` figures from each row's `log.simpleFoam`. E2a comparison figures from `verification/runs/JF1_jet_flap/JF1E_E2a_CMU*_A0/`. Prereg frozen `6e83157c`, blob `800944bc`, §6. Grading path `verification/runs/JF1_jet_flap/analyse_jf1_ladders.py` at `74c4f5fb`, blob `ef4c0b0c`, unmodified and verified byte-identical to HEAD at grading. |
| C-20260902T231638.052356Z-1908ed66 | 2026-09-02 | cfd | **M6S-P pyHyp wall-resolved feasibility probe, ONERA M6 — pyHyp ALONE at the coarse ladder level (1,560 wing faces, N 93, requested s0 1.319e-06, marchDist 12.0), run once and completed.** This row calibrates its COST ONLY. **THERE IS NO VERDICT TO CALIBRATE AGAINST: the probe is ungradeable by construction** — no gate, no threshold and no word of the fixed vocabulary attaches to any number it produced, and per its registration section 1.1 it can KILL route option 3 but CANNOT clear it. No plot3dToFoam, no checkMesh and no solver were run, so no non-orthogonality number exists and none may be attributed to it. | **0.11 core-min** (registration section 9.2) = 0.00183 core-h ≈ **$0.0001 DERIVED**. Registered **CAP 15.0 core-min** = **$0.013 derived**, enforced STRUCTURALLY rather than by intention: pyHyp invoked serially under `timeout 900s`, so 1 rank x 900 s / 60 = 15.0 is the worst the launcher can spend. Basis, from registration section 9.1: pyHyp's own last-level serial CPU time in five run directories under /home/ubuntu/certonomous-runs/ all at 1,560 wing faces — 1.1 / 6.4 / 5.6 / 5.4 / 5.3 s at N of 8 / 15 / 28 / 52 / 65 — cost being nearly INDEPENDENT of layer count and driven by SURFACE density instead. Frozen at `ad7305fc`, amendment 1 at `87a6364f`, blob `8fad9c6019174c235c79ac9948796c9c60b381b8`, verified byte-identical to the amended commit in the launching shell. | **0.167 core-min** (10 driver wall s x 1 rank / 60) = 0.00278 core-h ≈ **$0.000143 DERIVED, NOT MEASURED** at the owner-stated $0.0513/core-h, `cost_basis` REPORTED-BY-OWNER — the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` section 5). Serial throughout, ranks MEASURED as 1, so core-min = wall-min. **Decomposed, because the decomposition is the whole finding of this row:** pyHyp's own invocation 9 wall s = 0.150 core-min, of which pyHyp's own last-level CPU (registered reading 4, table column 2) is **5.9 s = 0.0983 core-min**; the remaining ~4 s is container start plus the section 5 control gate, the read-only input copies and the reader. | **= gross.** Total wall 10 s, three orders of magnitude below the `COMPUTE_BUDGET_CHARTER` section 2 3,600-s stall rule; one launch, one solve of the tool, no re-run, no refused stage, no timeout, and the cap never approached (**1.1 % of the registered 15.0**). | **1.52 gross against the registered 0.11** — the estimate was 34 % low. **Against the CAP: 0.011.** **On the quantity the estimate actually predicted — pyHyp's own serial CPU — the ratio is 0.89** (0.0983 / 0.11), i.e. 11 % conservative. | **THE 1.52 IS SCOPE, NOT MISPREDICTION, AND THE TWO ARE SEPARATED HERE RATHER THAN AVERAGED.** The section 9.1 basis priced **the tool**: pyHyp's own last-level serial CPU, 5.3–6.4 s across five runs at this surface density. Measured: **5.9 s — inside that band** — so the physics estimate was right and is not the source of the gap. The actual figure prices **the driver**: container cold start, the three planted controls, the read-only input copies and the reader, together about as much again as pyHyp itself. **Transferable rule, which is why this row is worth its space: a registered estimate for a containerised single-shot tool must say whether it prices the TOOL or the DRIVER, and on this box the harness around a ~6 s tool costs roughly another 4 s of fixed overhead — add it, do not scale the tool figure.** **THE REGISTRATION'S OWN DISCLOSED UNCERTAINTY DID NOT BITE, and it is scored rather than quietly dropped:** section 9.1 disclosed that all five basis figures were recorded at s0 = 1.0e-4 and NOT at this probe's 1.319e-06, so the basis constrained the order and not the digit. In the event, a 76x tighter requested first-cell spacing and 92 marching layers against the basis's 64 cost **5.9 s against a 5.3 s same-surface N-65 reference — 1.11x**, so the sub-iteration and KSP counts the disclosure worried about moved the figure by about a tenth, not by an order. **CONTENTION: NOT MATERIAL, MEASURED NOT ASSUMED** — the runner logged the box at **26.3 % busy (~4.2 of 16 cores) with MemAvailable 24.4 GB** at the launch tick, and the probe is a single serial rank. **WASTE, NAMED SEPARATELY PER CHARTER SECTION 6 AND NEVER ABSORBED INTO THE RATIO: ZERO core-min.** No launch was refused, nothing was re-run, no build was discarded and the 900-s timeout did not fire. | Figures read from `verification/runs/M6S_runs/P_pyhyp_wall_resolved_probe/RESULT.json` (cost block) and `READINGS.json` (reading 4, `last_level_cpu_s`), both landed at commit `b31064f3`; the pyHyp march table itself at `.../logMeshGeneration.txt`; the launch record and its rc at `verification/runs/STATUS.queue.M6S_P_PYHYP_WALL_RESOLVED_PROBE` (`launcher_rc=0`, contents READ, not merely existence) and `verification/queue/cfd/launched/M6S_P_PYHYP_WALL_RESOLVED_PROBE.json`; box-occupancy figures from the runner's own `verification/queue/runner.log` line of 2026-09-02T23:12:14Z. Pre-registration `verification/campaign/M6S_P_PYHYP_WALL_RESOLVED_PROBE_PREREGISTRATION.md` sections 9.1 / 9.2 / 9.3, frozen `ad7305fc`, amended `87a6364f`. |
| C-20260903T162543.364120Z-e486a4a1 | 2026-09-03 | ansys-verification | **VMFL046 — Supersonic Flow with a Normal Shock in a Converging–Diverging Nozzle (VM2026R1 printed p. 155), the complete three-level family L1/L2/L3 = 3 200 / 12 800 / 51 200 cells, OpenFOAM v2606 `rhoSimpleFoam`, serial `RANKS = 1`, `stopAt endTime` with `endTime 20000` and `residualControl` REMOVED before the freeze (charter §22.5).** The completion this row calibrates is the **graded case**: run 2026-09-02T23:24:19Z–23:52:22Z, graded 2026-09-03, register row **#54**, verdict **`GATE FAIL`** on the primary shock-location limb at 7.7939 % against a frozen 5 % band. The verdict is not what this row is about and enters no figure in it. **CORRECTION ROW IN SUBSTANCE, AND IT NAMES WHAT IT CORRECTS:** `L-443` and `ANSYS_VERIFICATION_CHARTER` v1.21 §26.2 both state that the filed 12.00 core-min was *"1.66× below its own stated method"*. **That is wrong and this row replaces it** — see the gap-attribution cell. The supervisor is correcting both records separately; this ledger row carries the corrected arithmetic | **12.00 core-min**, frozen in `PREREGISTRATION.md` §8 at freeze commit `28a4a375` (prereg blob `a01b70c4d44c3bf274c99f3901e1cdea871467a8`), method stated there as *"scaled a-priori from L1's uncontended rate and the L1/L2/L3 cell counts"*. Derived **$0.010260**, not measured. **Driver running-total cap 30.00 core-min**, derived $0.025650 | **28.05 core-min MEASURED** — `wall_s` × `RANKS` ÷ 60 from `launcher.queue.out`, serial, one level at a time. **PER LEVEL: L1 0.95** (57 s), **L2 5.16667** (310 s), **L3 21.93333** (1 316 s); total wall **1 683 s**. **THESE ARE PER-LEVEL FIGURES AND THEY ARE NOT WHAT THE LAUNCHER PRINTS:** `launcher.queue.out` reports `core_min_used` as a **running total** (0.95 → 6.11667 → 28.05), and reading those three as per-level costs double-counts. The launch-time board note did exactly that. Derived **$0.023983** at $0.0513/core-h, c7a.4xlarge, owner-stated 2026-08-21/22 — **DERIVED, NOT MEASURED**, the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` §5) | **= gross.** The 3 600-s stall rule removes nothing: the longest level ran 1 316 s. No failed solve, no re-run, no discarded build, no killed level. **Cap NOT crossed: 93.5 % of 30.00 consumed, 1.95 core-min headroom.** `ESTIMATE_OVERRUN.txt` in the run root is an **ESTIMATE** overrun stamped at elapsed 842 s against 1.10 × 720 s and says `REPORTED, NOT ENFORCED` on its face; the queue entry carried no `cap_core_min_registered`, so no cap existed there to cross and nothing was killed | **2.3375** (28.05 / 12.00). Against the cap, **0.935** | **100 % MISPREDICTION, 0 % WASTE — AND THE WASTE FIGURE IS MEASURED, NOT ASSUMED.** **WASTE = 0.000 core-min, named separately (charter §6) and never folded into the ratio.** Measured as `ExecutionTime/ClockTime` on the final line of each `log.rhoSimpleFoam`: **0.98754** (56.29/57), **0.98913** (306.63/310), **0.99801** (1313.38/1316) — idle-wait 1.25 %, 1.09 %, 0.20 %. **The job held a full core at every level, so the launch-time framing that "two sweeps ran beside it" is REFUTED by measurement, and the pre-registration's own instruction that "any excess at grading is contention WASTE, reported separately, not misprediction" pre-attributed the excess to the wrong bucket.** The 2.3375 is misprediction end to end. **(1) ROOT CAUSE — AN ITERATION-COUNT REGIME CHANGE THE ESTIMATOR NEVER PRICED.** The L1 datum the estimate scaled was the smoke's **0.18 core-min, and that solve CONVERGED at 2 777 iterations under `residualControl`** (`PREREGISTRATION.md` §9, :159-160). The frozen run had **`residualControl` REMOVED** (§22.5) and every level ran the full **20 000** iterations — **the same frozen document says so at §10**. **The work per level changed by 20 000 / 2 777 = 7.202× and the estimate did not carry it.** Correcting only this: 0.18 × 7.202 = **1.29636** core-min for L1, × (1 + 4 + 16) = **27.2236** against the actual 28.05 — **ratio 1.0304, within 3.0 %.** One a-priori-knowable mechanism closes 97 % of the miss. **(2) SECONDARY — SUPERLINEAR PER-CELL COST, MEASURED FROM THIS TRIPLE.** Linear-in-cells from the graded L1 predicts 228 s at L2 and 912 s at L3; actual 310 s and 1 316 s, so **actual/linear = 1.3596 at L2 and 1.4430 at L3, 1.4060 over the triple.** It is **not a constant — it grows with level**, which is what superlinear means; ~1.41× is the triple average and not a law. **(3) THE PROCESS ERROR, AND IT IS THE ONE WORTH THE ROW: THE NEAR-EXACT PREDICTOR WAS COMPUTED, PRINTED IN THE FROZEN DOCUMENT, AND THEN DISCARDED.** `PREREGISTRATION.md` §9 records the disclosed smoke's **total as 28.2 core-min** and §8 rejects it as *"contention-inflated, not representative"*, substituting the a-priori 12.00. **The discarded number predicts the actual 28.05 to 0.53 %.** The contention premise is refuted on the one level that is apples-to-apples: the smoke's L2 and the graded L2 ran the **same 12 800-cell mesh to the same 20 000 iterations**, in **321 s** and **310 s** — **3.5 % apart, not "inflated"**. (Honest qualification: the smoke's L1 *was* slower per iteration — 3.89 ms/iter against the graded 2.85, a real 1.36× — so contention was not zero during the smoke; it was simply far too small to justify discarding a 0.53 %-accurate total.) **(4) THE SUPERSEDED ARITHMETIC, AND WHY IT WAS WRONG — this is what `L-443` and charter §26.2 must be corrected to.** Those records reconstruct the stated method as 57 s × (1 + 4 + 16) = 1 197 s = **19.95** core-min, hence *"the filed 12.00 was 1.66× below its own method"*. **The 57 s is the GRADED L1's wall — a datum that did not exist at filing.** Reconstructing what a method *would have given* from a figure produced by the run being graded is structurally the same error as gate-fitting. **Executed with the L1 datum actually available at filing — the smoke's 0.18 core-min — the stated method gives 0.18 × 21 = 3.78 core-min, so the filed 12.00 was 3.17× ABOVE its own arithmetic, not 1.66× below it.** **And the "1.66 × 1.41 = 2.34" closure is a TAUTOLOGY, not corroboration:** (19.95/12.00) × (1 683/1 197) ≡ 1 683/720 identically, for **any** intermediate anchor, so the product closing to the ratio is arithmetic bookkeeping and evidences nothing. **CARRY-FORWARD ESTIMATOR RULES, FOUR:** **(a)** reconcile a filed estimate against its own stated method **before** freezing, and print both numbers; **(b)** an estimator datum is **INVALID ACROSS A CHANGE OF STOPPING CRITERION** — before reusing a smoke cost, check whether the frozen run stops the same way, because here the iteration count moved 7.2× between smoke and freeze and the change was already written in the same document; **(c)** linear-in-cells scaling **understates an r = 2 refinement step by ~1.36–1.44×** on `rhoSimpleFoam`, growing with level; **(d)** a smoke total may be discarded as contention-inflated only on a **MEASURED** contention figure (`ExecutionTime/ClockTime`), never on an observed load average — the discard here threw away the best estimate available | Run root `verification/runs/ansys_verification/VMFL046/` — `launcher.queue.out` (per-level `rc`, `wall`, running `core_min_used` against cap 30), `STATUS.queue.VMFL046` (`launcher_rc=0`), `ESTIMATE_OVERRUN.txt`, per-level `RUN_RC` (all `0`) and `log.rhoSimpleFoam` (final `ExecutionTime`/`ClockTime` lines, the contention measurement), plus `GRADE_VMFL046.out`, `SELFTEST_VMFL046.out`, `DIAGNOSTIC_shock_roache.out` — the four graded outputs committed at **`4a4b0dbd`**. Estimate, method, smoke and cap frozen at **`28a4a375`** in `cases/ansys_verification/VMFL046/PREREGISTRATION.md` §8–§9. Verdict row **#54** of `verification/credentials/ansys/ANSYS_VALIDATION_REGISTER.md`; results record `cases/ansys_verification/VMFL046/RESULTS.md` §6, landed at **`dcdabfd5`** |
| C-20260903T162650.914713Z-0b5c3b25 | 2026-09-03 | dafoam | **A1WR Stage 0 — the wall-resolved L1/L2/L3 mesh family built, all three levels, `plot3dToFoam` + `autoPatch` + `createPatch` + `renumberMesh` + `checkMesh`.** Completed and graded; `G-MESHFAM` cleared (generator selfcheck rc 0, growth ratios 1.25483 / 1.11957 / 1.05800 all under the registered 1.35 ceiling). This row calibrates its COST. | **≤ 40 core-min** — the registered *cap* of `A1WR_PREREGISTRATION.md` §7.4 is the only Stage-0 figure on the record; §7.3's extrapolation prices the sweeps, not the build, so there is no point estimate to compare against and the cap is used, stated as such rather than approximated. 40 core-min = 0.6667 core-h = **$0.0342 DERIVED**. | **0.4000 core-min** = 24 s wall x 1 rank / 60, MEASURED from the build log; = 0.006667 core-h ≈ **$0.000342 DERIVED, NOT MEASURED** at the owner-stated $0.0513/core-h, `cost_basis` REPORTED-BY-OWNER (the box cannot read its own billing, `COMPUTE_BUDGET_CHARTER.md` §5). np = 1. | **= gross.** No row approaches the `COMPUTE_BUDGET_CHARTER` §2 3,600-s stall threshold — the entire stage is 24 s. One build per level, no re-run, no refused stage, no timeout. | **0.0100 against the registered cap of 40** — the build was over-provisioned by ~100x. | **MISPREDICTION, NOT CONTENTION, and the direction is named.** pyHyp's hyperbolic march is near-linear in surface density and far cheaper than the `plot3dToFoam` / `renumberMesh` overhead the cap was sized for; that overhead did not materialise at 8,064 / 32,576 / 130,304 cells. **Contention: not material** — the stage is 24 s serial. **WASTE, NAMED SEPARATELY PER CHARTER §6 AND NEVER ABSORBED INTO THE RATIO: ZERO core-min.** Nothing was re-run, no build discarded, no timeout fired. **Transferable: a mesh-build cap on this box should be priced from pyHyp's own surface-density cost, not from a fear of the plot3d/renumber chain.** | Cost and ratio read from `cases/dafoam/ladder-a/A1/wall_resolved_aoa_polar/A1WR_STAGE0_RESULTS.md:11-16`, landed at `c6aedca9`; build artifacts `/home/ubuntu/certonomous-runs/A1WR/{STAGE0.log,L1,L2,L3}`. Cap from `A1WR_PREREGISTRATION.md` §7.4. |
| C-20260903T162650.914777Z-27ea141c | 2026-09-03 | dafoam | **A1WR Stage 1 — the y+ probe, one point per arm at alpha = 18 on L3, fixed 1,500 iterations regardless of convergence, PATCHED image `dafoam-idwarp-rot:v1`.** Completed; the in-chain gate `a1wr_stage1_gate.py` returned rc 0 and released Stage 2. This row calibrates its COST; the y+ readings are in `A1WR_STAGE12_RESULTS.md` §7. | **~65 core-min** (§5 Stage 1: "a mesh which fails its own premise costs ~65 core-minutes instead of ~1,200"), under a registered **cap of 120** (§7.4), realized by Addendum C §15.5 as two in-container 3,600-s deadlines = exactly the 120. 65 core-min = 1.0833 core-h = **$0.0556 DERIVED**. | **31.7500 core-min** on the driver's own `CHAIN_LEDGER.tsv` basis (probe_I 923 s + probe_C 982 s, x 1 rank / 60); **31.5594 core-min** on the container basis (`docker inspect` 920.487 s + 973.075 s) — the ~0.19 core-min gap is docker-run overhead the driver counts and the container does not, and both bases are given rather than one being picked. = 0.52917 core-h ≈ **$0.027146 DERIVED, NOT MEASURED** at $0.0513/core-h, `cost_basis` REPORTED-BY-OWNER. np = 1, `OMP_NUM_THREADS=1`, cpusets 8 and 9, one core each. | **= gross.** Neither probe row (923 s, 982 s) approaches the 3,600-s stall threshold, and neither hit its 3,600-s in-container deadline (26 % and 27 % of it). No re-run inside this stage. | **0.4885 against the ~65 estimate; 0.2646 against the 120 cap.** | **MISPREDICTION, CONSERVATIVE DIRECTION, AND THE MECHANISM IS NAMED.** The ~65 was sized on 1,500 iterations at the §7.3 EXTRAPOLATED 0.353 / 0.368 s/iteration plus startup; the probes instead reached 1,500 iterations in 920 / 973 s = **0.61 / 0.65 s/iteration including container start** — but the estimate had assumed the probe would run its full deadline, and it did not. **CONTENTION: PRESENT AND SEPARATED, NOT ABSORBED** — two cfd `simpleFoam` jobs were live at freeze (§7.4's own disclosure) and the two probes ran concurrently with each other; loadavg at Stage-2 launch was 1.18/1.73/1.51, so the box was near-idle and contention is NOT material to this stage's ratio. **WASTE, NAMED SEPARATELY PER CHARTER §6 AND NEVER ABSORBED INTO THE RATIO: 1.02 core-min** — the second fire (Addendum E, 2026-09-02T18:13:25Z) ran both probes to immediate failure on two instrument defects, 41 s and 20 s of container startup, **ZERO solver iterations**, and the gate REFUSED at exit 2 exactly as built. **Plus 0.0 core-min** for the first fire (Addendum D), aborted inside `stage_unit()` before any container started. Neither is folded into the 0.4885. | Costs read from `/home/ubuntu/certonomous-runs/A1WR/STAGE12/CHAIN_LEDGER.tsv` (probe rows) and `docker inspect` on `a1wr_probe_{I,C}_20260902T181935Z`; gate outcome `.../STAGE12/stage1_gate.json` and `.../wall_resolved_aoa_polar/STATUS.A1WR_chain.log`. Estimate and cap from `A1WR_PREREGISTRATION.md` §5, §7.4, Addendum C §15.5. Fire costs from Addenda D and E. Results record `cases/dafoam/ladder-a/A1/wall_resolved_aoa_polar/A1WR_STAGE12_RESULTS.md`. |
| C-20260903T162650.914800Z-25d21982 | 2026-09-03 | dafoam | **⚠ A1WR Stage 2 — THE TWO CONTINUED alpha-SWEEPS, AND THIS IS A *TRUNCATED* ITEM: IT IS NOT A COMPLETED PROCESS AND ITS RATIO IS NOT AN ESTIMATE-QUALITY FIGURE.** 19 points per arm were declared; the 2026-09-03T02:25:05Z `shutdown -h now` killed both containers (exit 143, SIGTERM) with arm I at **13** delivered points (alpha 0-12) and arm C at **9** (alpha 0-8, all `Primal solution failed!`). Item verdict `NOT A RESULT` (`A1WR_STAGE12_RESULTS.md` §0). **The truncation does not void the completed physics (Sanaa's universal rule, 2026-08-26) and does not refund the core-minutes either — which is why this row exists.** | **Per arm: 424.5 (I) / 442.9 (C) core-min** — Addendum A §13.2's re-cost at the MEASURED L3 cell count of 130,304 and at the np = 1 ruled in §13.5, using 0.35625 / 0.37163 s/iteration. Superseded §7.3's 560 / 585, both given. Registered **cap 800 core-min PER ARM**, item ceiling 1,800 (§7.4); Addendum C §15.5 allocates 785 of the 800 as sweep 37,200 s + 3 colds x 3,300 s. Both-arms estimate 867.4 core-min = 14.457 core-h = **$0.7416 DERIVED**. | **BOTH ARMS 1,269.8951 core-min** = 21.165 core-h ≈ **$1.08576 DERIVED, NOT MEASURED** at $0.0513/core-h, `cost_basis` REPORTED-BY-OWNER. **Arm I 634.9663** = sweep container 28,146.980 s (`docker inspect`, 469.1163 core-min) + three cold controls 9,951 s (165.8500). **Arm C 634.9288** = sweep 28,146.728 s (469.1121) + colds 9,949 s (165.8167). Container-measured cold walls give 634.1446 / 634.1359; both bases stated. np = 1 throughout, `OMP_NUM_THREADS=1`, cpusets 8-15 one core each, all disjoint from MAAOA's 2-7 (VERIFIED by `docker inspect`, not assumed). **Cap consumed: 79.37 % on each arm; 165.0337 / 165.0712 core-min remain and can never now be spent on this item.** | **1,269.8951 = gross.** **Cleaned, on this record's own stated definition, = 1,112.2352** — the `COMPUTE_BUDGET_CHARTER` §2 3,600-s stall rule mechanically matches exactly two point-rows, arm I alpha = 0 at **4,610.976 s** (76.8496 core-min) and arm C alpha = 0 at **4,848.617 s** (80.8103) — **but on the evidence NEITHER IS A STALL**: both completed their full 4,000 iterations and arm I's produced a good ledger row. They are long because 14 containers were live. **The GROSS figure is the one to use here and the cleaned column is a mechanical application of the rule, stated per this ledger's append rule 4 rather than silently applied.** | **Arm I 1.4958 / arm C 1.4336 against Addendum A §13.2; 1.1339 / 1.0853 against §7.3; 0.7937 of the 800 cap on each arm.** **⚠ EVERY ONE OF THOSE IS AGAINST A TRUNCATED DELIVERY AND MUST NOT BE READ AS ESTIMATE QUALITY** — the estimate priced 19 points + 3 colds per arm; the actual bought 13 points + 0 colds (I) and 9 non-results + 0 colds (C). **The honest estimate-quality figure is on the transferable unit, s/SIMPLE-iteration, and it is 1.177 (arm I) and 1.828 (arm C).** | **THREE SEPARATE MECHANISMS, AND THEY ARE NOT AVERAGED TOGETHER.** **(1) MISPREDICTION of the per-iteration anchor.** Addendum A §13.2 predicted 0.35625 (I) / 0.37163 (C) s/iteration at 130,304 cells. MEASURED on a quiet box (after the last MAAOA container ended 21:58Z): arm I alpha 9-11 **0.41930** (ratio **1.177**, 18 % low); arm C alpha 7-8 **0.67930** (ratio **1.828**, 83 % low). **AND §7.2's finding that the two solver classes agree to 4.3 % per iteration — MEASURED at 4,032 cells — DOES NOT TRANSFER TO THE WALL-RESOLVED MESH: in directly comparable quiet-box windows the compressible arm costs 1.543x the incompressible per iteration at 130,304 cells.** That is this item's most transferable cost result. **(2) CONTENTION, MEASURED NOT ASSUMED, AND SEPARATED FROM THE RATIO.** Per-core throughput of a 130,304-cell SIMPLE solve degraded **2.75x** (arm I alpha = 0) to **4.77x** (`cold_C_4`) at 14-way concurrency — 8 A1WR + 6 MAAOA containers — **with every cpuset disjoint and one core each.** **A one-core cpuset isolates a core, it does not isolate memory bandwidth**, and no gate in this lab reads that. **(3) WASTE, NAMED SEPARATELY PER CHARTER §6 AND NEVER ABSORBED INTO ANY RATIO ABOVE — 369.37 core-min in two named lines.** **(3a) 331.6667 core-min: all six cold controls, `rc=97` TIMEOUT at the 3,300 s deadline, ZERO completed points and ZERO coefficients** — `cold_I_*` reached 1,800 of 4,000 iterations, `cold_C_*` 1,600. **Cause is the deadline, not the physics**: at its MEASURED 1.781 s/iteration `cold_I_4` needed **7,126 s** against a 3,300 s deadline sized from a quiet-box rate, and the three incompressible colds were HEALTHY (bounding census `nuTilda` only, 19 occurrences, zero `p`/`rho`/`e`/`U`) and were killed while succeeding. `G-COLD` is consequently **NOT EVALUABLE** and `G-CONCURRENCY-BITS` is **NOT A RESULT**. **(3b) 37.7012 core-min: INFRASTRUCTURE LOSS, ITS OWN LINE, NOT MISPREDICTION AND NOT CONTENTION** — the poweroff discarded in-flight solver work on arm I alpha = 13 (1,190.14 s, 2,500 of 4,000 iterations) and arm C alpha = 9 (1,071.93 s, 1,500 of 4,000), neither of which produced a ledger row. **The registered in-container deadline was 37,200 s and only 28,147 s had elapsed: no cap stopped this run, and `G-CAPS` did not fire.** **Transferable rule this row buys: an in-container deadline must be sized at the box's BUSY per-iteration rate, not its quiet one, whenever the same item's own launch plan fills the box — A1WR filled it 8-deep by design (§13.6) and then paid 331.7 core-min for zero physics.** | Container walls from `docker inspect` on `a1wr_sweep_{I,C}` and `a1wr_cold_{I,C}_{4,14,17}_20260902T181935Z`; unit walls and rc from `/home/ubuntu/certonomous-runs/A1WR/STAGE12/CHAIN_LEDGER.tsv`; per-point walls from `.../sweep_{I,C}/out/LEDGER.tsv`; per-iteration rates from the `ExecutionTime` series in `.../sweep_{I,C}/out/sweep.log` and `.../cold_*/out/sweep.log`; kill instant from `journalctl -b -1` (`shutdown -h now` at 02:25:05.244Z, `poweroff.target` 02:25:14Z) and `last -x`. Estimates and caps from `A1WR_PREREGISTRATION.md` §7.2, §7.3, §7.4, Addendum A §13.2, Addendum C §15.5. Full record `cases/dafoam/ladder-a/A1/wall_resolved_aoa_polar/A1WR_STAGE12_RESULTS.md`. |
| C-20260903T163034.569926Z-e53bb917 | 2026-09-03 | heat-transfer | **T25R5 — the linear-solver tuning probe, CLOSED at `f5e3de10` (Addendum D3). Eleven runs: stage 0 (`D0_L2`, `B0_L2`), stage 1 (`C1`–`C5` at L2, one at a time), stage 2 (`B0`/`C4` at L1 and L3).** This row calibrates its COST; the verdicts are in the registration's Addendum D3 (`G-T5` **PASS** on `C4` at 60.11×; `P-2` **LOSES**; `P-3` **LOSES** at 6.7452). **THE ROW IS FILED LATE AND SAYS SO:** the rung closed 2026-09-02 and rule 12 requires the comparison at process completion; the debt was recorded open on the board and is discharged here. **Every figure below was re-read from the artifacts before it was written; none was carried from a summary.** | **206.84 core-min POINT**, the registered estimate for exactly these eleven runs, frozen at `docs/campaigns/T-family/T25R5_PREREGISTRATION.md:636` before any arm ran. **This is the denominator of the ratio column and it is the estimate, not the guard.** Two other registered figures, named so they are not confused with it: **CAP total 309.34** (`:636`) and the **registered ceiling 320** (`:638`). Priced from T25R4's MEASURED `s_per_step` (L1 4.78375 / L2 12.808049 / L3 26.577674 s/step at 2 ranks, `--bind-to none`, under the criterion that actually ran) via `POINT = 40 steps × r(L) × 2 ranks / 60`. **320 core-min = $0.274 derived**; inside the $25 pre-authorisation by ×91. | **207.523 core-min GROSS** = 3.4587 core-h ≈ **$0.1774 DERIVED, NOT MEASURED** at the owner-stated $0.0513/core-h, `cost_basis` **REPORTED-BY-OWNER** — the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` §5). Re-summed here from the four components and it reproduces exactly: stage 0 **18.600** + stage 1 contaminated batch **70.533** + stage 1 clean **74.157** + stage 2 **44.233** = **207.523**, matching `P3_SCORE.json`'s `cost_core_min.cumulative`. **65 % of the 320 ceiling; no overrun, and the ceiling was never approached.** `ranks` = 2 throughout. ⚠ **BASIS MIXTURE, DISCLOSED RATHER THAN SMOOTHED:** stage 0 and stage 2 are computed from `clock_s` (18.6 from 516 + 42; 44.233 from 131 + 14 + 993 + 189) while stage 1 is computed from `exec_s` (74.157 from 670.61 + 729.28 + 741.35 + 45.40 + 38.07, which reproduces to the digit). An all-`exec_s` basis would give stage 2 **44.106** and a gross of **207.396**. **The difference is 0.127 core-min and moves the ratio by 0.0006 — it changes no decision, and it is stated because a figure whose basis is unstated is not a measurement.** | **136.990 core-min** = 2.2832 core-h ≈ **$0.1171 DERIVED**. ⚠ **THIS IS THE SOURCE RECORD'S OWN CLEANED DEFINITION — gross minus the named waste — AND NOT `COMPUTE_BUDGET_CHARTER` §2's, as this ledger's append rule 4 requires be said.** **Under §2's stall-rule definition, cleaned == gross == 207.523, because NO run in this probe exceeded the 3,600-s stall rule:** the longest single run in the whole rung is `B0_L3` at **993 clock s** (`P3_SCORE.json.rows`), the L2 timeout of 770 s caps every stage-1 attempt including the contaminated batch, and the one measured contended time is `C1` at **727.56 s**. **Both figures are given so a reader can use either without re-deriving it, and the ratio column uses the waste-excluding one because rule 12 forbids folding waste into a ratio.** | **0.6623** = 136.990 / 206.84. **The estimate was 51 % HIGH against clean actual.** Reported beside it and never substituted for it: gross/POINT **1.0033**, clean/CAP **0.4428**, clean/ceiling **0.4281**. **The 70.533 core-min of waste is NOT in this ratio, in either direction.** | **THE GAP IS A SYSTEMATIC, NAMEABLE MISPREDICTION, NOT NOISE — AND IT IS THE MOST TRANSFERABLE THING IN THIS ROW. (1) THE ESTIMATE PRICED EVERY ARM AT THE BASELINE RATE.** `POINT` used `r(L)` — `B0`'s measured seconds per step — for **all six** stage-1 arms and both stage-2 arms. The arms the probe existed to find ran **5–13× faster than that**: `C4_L2` **45.40 s** and `C5_L2` **38.07 s** against `B0_L2`'s **516.15 s**. **A probe hunting a speed-up whose budget prices every arm at the un-sped-up rate will always come in low, by roughly the speed-up it finds.** Transferable rule: *price the arms of a speed-up probe in two registered brackets — at the baseline rate and at the hypothesised rate — and say which one the ratio's denominator is.* T25R6a (`2b9bd20b`) adopts exactly that and fixes its denominator in advance. **(2) STAGE 0 RAN CONCURRENT AND WAS CHEAP FOR THAT REASON TOO:** `STAGE0_RESULT.json` records `"concurrent": true` and its own `actual_over_predicted` **0.545** (18.6 against `cost_point` 34.15). That is a scheduling effect, not solver efficiency, and it must not be read as one. **(3) A PARTIAL OFFSET IN THE OTHER DIRECTION:** `C2` and `C3` cap-stopped at `rc 124` (39 and 38 of 40 steps), so those two ran toward their CAP rather than their POINT and *raised* the actual — the ratio would be lower still without them. **(4) CONTENTION WAS MEASURED AND IS NOT THE EXPLANATION:** the lane attributed the cap-stops to contention it had introduced; the required re-run **falsified that** — `C1` contended/alone is **1.085×** and `C2`/`C3` **capped again running ALONE**. Contention is therefore excluded from this attribution on evidence, not by assumption. **WASTE, NAMED SEPARATELY PER CHARTER §6 AND FOLDED INTO NO RATIO AND NO EXPLANATION: 70.533 core-min ≈ $0.0603 DERIVED — 34 % of gross.** It is the contaminated concurrent stage-1 batch (2,116 wall s across three arms), **caused by this lane's own staging error**, and its three `*_L2.CONTENDED_DISQUALIFIED` directories are retained undeleted on disk as the evidence. | Cost cells re-read at this write from `verification/runs/T-family/T25R5_LINSOLVER_runs/P3_SCORE.json` (`cost_core_min`: `cumulative` 207.523, `stage0` 18.6, `stage1_clean` 74.157, `stage1_waste_contaminated` 70.533, `stage2` 44.233, `ceiling` 320, `usd_derived` 0.1774; `rows.*.clock_s`/`exec_s` for the basis check), `.../GT5_VERDICT.json` (`cost_core_min.cumulative` 163.29 at stage-1 close, `stage1_contaminated_WASTE` 70.533, `contention_correction`, `arms.*.exec_s`) and `.../STAGE0_RESULT.json` (`clock_time_s`, `execution_time_s`, `cost_point` 34.15, `cost_cap` 51.33, `concurrent`, `actual_over_predicted` 0.545). Registered estimate and ceiling: `docs/campaigns/T-family/T25R5_PREREGISTRATION.md:636`, `:638`; final cost table Addendum D3.4 `:1335-1348`. Rung closed at `f5e3de10`. Successor registration `docs/campaigns/T-family/T25R6a_PREREGISTRATION.md` at `2b9bd20b`. |
| C-20260903T165317.145853Z-98d2e6ac | 2026-09-03 | cfd | **R1-M0 — the ONERA M6 admission probe (`RUNG1_M6_PREREGISTRATION.md` §5, frozen `c7f99bb1`). NO VERDICT OF THE FIXED VOCABULARY ATTACHES — it is an admissibility measurement that gates a freeze and it grades nothing.** Measured **max non-orthogonality 88.88926674°**, max skewness 2.849165477, on a 143,520-cell pyHyp wall-resolved M6. Selects the registration's SECOND pre-stated outcome: branches (a1)/(a2) are dead | **2.0 core-min** registered (§5), **hard cap 6.0 core-min = $0.0051 DERIVED**, structural enforcement `timeout 360` at 1 rank | **0.1833 core-min MEASURED** (attempt 2, driver wall 11 s × 1 rank ÷ 60; per-step pyHyp 8 s, plot3dToFoam 1 s, checkMesh < 1 s, all serial — `nProcs : 1` in the checkMesh banner) = **$0.000157 DERIVED, NOT MEASURED** at $0.0513/core-h, reported-by-owner, the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` §5) | = gross, 0.1833 core-min. Longest row is 11 wall s, three orders of magnitude inside the 3,600-s stall rule, so gross and cleaned coincide. **Attempt-1 waste is named separately below and is NOT netted off either column** | **0.092×** cleaned/predicted (0.1833 / 2.0); **0.031× of cap**. Item total including waste **1.1000 core-min = 0.183× of cap**. No overrun; the cap was never approached | **Misprediction, conservative direction, ~11×, PLUS a separately-named waste row of this lane's own making.** (a) **The estimate's shape was right and its unmeasured remainder was ~100× too large.** §5's 2.0 core-min was built from the one measured artifact in hand — the byte-identical pyHyp deck at 9 wall s (`M6S_runs/P_pyhyp_wall_resolved_probe/RESULT.json`) — plus headroom for `plot3dToFoam` and `checkMesh` on 143,520 cells, which had never been measured. Those two steps came in at **1 s and under 1 s**, against the ~111 s the headroom implicitly priced. **Calibration lesson: a serial format converter and a mesh check on a 10⁵-cell grid are sub-second-to-second operations and should be priced from one measured conversion, not from a solver-shaped allowance.** (b) **WASTE NAMED, 0.9167 core-min = $0.000784 DERIVED, and it is this lane's defect not the mesh's:** attempt 1 (16:46:31–16:47:26Z) generated a valid grid — pyHyp rc 0, plot3dToFoam rc 0, 143,520 cells, points merged 169,911 → 148,335 — and then `checkMesh` aborted with `FOAM FATAL ERROR: cannot find file system/fvSchemes`, because the driver wrote only `controlDict` and `checkMesh` in OpenFOAM v2606 constructs an `fvMesh`. It produced **no geometry block, no non-orthogonality number, and none was read or attributed**. Preserved in full at `verification/runs/RUNG1_M6_runs/M0_ATTEMPT1_ABORTED_missing_fvSchemes/`. **Calibration lesson: a driver that invokes an OpenFOAM utility must assert its full dictionary set before spending the mesh, not discover it from the utility's own abort** — the repaired driver now refuses at zero compute if any of `controlDict`/`fvSchemes`/`fvSolution` is missing, and the fix was proven on an unrelated 56-cell mesh (`F16_runs/coarse`) before relaunch so that no M6 answer was read outside the registered run. (c) **An unregistered observation worth a gate next time, stated as inference with its evidence:** the byte-identical pyHyp deck ran **52 s in attempt 1 and 8 s in attempt 2**, a 6.5× spread eleven minutes apart, against 9 s in M6S-P. Attempt 1 was the first `docker run` of `dafoam-idwarp-rot:v1` since the box rebooted at 15:34:57Z. **Cold container start is the plausible cause and it is NOT measured** — nothing instrumented it. **Lesson: a per-run estimate for a containerised tool should state whether the container is cold, beside the load average that C-4, B3 and C-18 already ask for.** (d) **Contention: not separated out; the figure is GROSS.** | `verification/runs/RUNG1_M6_runs/M0_pyhyp_admission/RESULT.json` (cost block and completion clauses), `.../CHECKMESH_READING.json`, `.../foam/log.checkMesh` (the numeric line, and the `Non-orthogonality check OK.` line it prints beside it, which was captured and discarded); attempt 1 at `.../M0_ATTEMPT1_ABORTED_missing_fvSchemes/RESULT.json`; pre-registration frozen at `c7f99bb1`, blob sha256 `2751ddbe7b0610b9d58e281196d01491c493f0bc17784a356ae5f306579ebd6e` verified byte-identical to disk by the driver before any compute (`.../FREEZE_CHECK.txt`); queue record `verification/queue/cfd/launched/RUNG1_M6_M0.json` |
| C-20260903T165527.742942Z-e95fb815 | 2026-09-03 | ansys-verification | **DIGITIZER instrument calibration — the digitized-reference reader (charter §25/§28), calibrated as its own pre-registered costed task; frozen instrument blob `2092c55d…`, freeze commit `0fab170f`; command `python3 cases/ansys_verification/DIGITIZER/digitize_calibrate.py --calibrate --n 24`.** PARTIAL-EXECUTION COST: the run REFUSED (exit 2) at POSITION PLANT-NULL, so this figure is the cost of a run that REACHED ITS REFUSAL, not a full calibration — POSITION PLANT-DETECT and the two shared axis negatives did NOT run. Task verdict **NOT A RESULT** (charter §29.1). | **0.1 core-min filed** (`PREREGISTRATION.md` §7); cap **0.3 core-min** (~3× per §26.2/§28.7). Derived **$0.0000855** filed / **$0.0002565** cap at $0.0513/core-h — DERIVED, NOT MEASURED (box cannot read its own billing, `COMPUTE_BUDGET_CHARTER.md` §5). | **0.1128 core-min MEASURED** — 6.770 s wall × 1 core ÷ 60, the AUTHORISED run, single core (`GRADE_DIGITIZER.out`/`.err`, `RESULTS.md`). Derived **$0.0000964**, not measured. Supervisor's independent reproduction **6.009 s = 0.1002 core-min** — corroboration, not the actual. | **= gross.** No stall (whole run 6.77 s, longest op ≪ 3600 s); no failed solve, re-run or discarded build. **Waste = 0.000 core-min, named separately (charter §6)** — the refuse is the instrument's designed planted-null control firing (rule 3 / §16.4), and the compute that reached it is evidence, not waste. | **1.128** (0.1128 / 0.1) actual/filed; **0.376 of cap** (0.1128 / 0.3). Under cap, NO overrun. | The ~1.13× actual/filed gap is **Python interpreter + numpy/PIL/scipy import STARTUP (~1.5 s)**, excluded from the WARM per-op basis — attributed to **fixed interpreter/import startup, explicitly NOT per-op misprediction**. The per-op method figure (32 renders × 0.1087 s + 34 digitizes × 0.0088 s = 3.78 s = **0.0629 core-min**) is CONFIRMED sound (the §27.3 filing-time reconciliation), and the supervisor's 6.009 s reproduction brackets the same startup component. PARTIAL-EXECUTION: 0.1128 is the cost of a run that stopped at its refusal, not of a full calibration. | Results `cases/ansys_verification/DIGITIZER/RESULTS.md` (verdict NOT A RESULT) + evidence `GRADE_DIGITIZER.out`/`.err`/`.diagnostic.json`, committed **`caa6b096`**; charter §29 (v1.24) **`d95af6be`**; lesson **L-461** **`236f982e`**; filed estimate/cap `PREREGISTRATION.md` §7 at freeze **`0fab170f`** (instrument blob `2092c55d…`) |
| C-20260903T171716.824448Z-f5cbf4eb | 2026-09-03 | ansys-verification | **DIGITIZER re-file R2 calibration — POSITION re-registered per charter §29.3 (term A = MAX per-plate error, RMS-admissibility enforced by refuse), VALUE separately registered; R2 instrument blob `c1d275a2`, freeze commit `5714f2c7`, imports frozen R1 `2092c55d` read-only; command `python3 cases/ansys_verification/DIGITIZER/R2/digitize_calibrate_r2.py --calibrate --n 24` (default locator).** FULL-calibration run (unlike R1's partial): both quantities graded — **R2-VALUE PASS** (u_read 0.00505 y-data, pixel floor binds) and **R2-POSITION PASS** (u_read 0.00470 x-data, MAX term binds; the null that refused in R1 now passes by construction). | **0.1 core-min filed** (`R2/PREREGISTRATION.md` §8); cap **0.3 core-min** (~3× per §26.2/§28.7). Derived **$0.0000855** filed / **$0.0002565** cap at $0.0513/core-h — DERIVED, NOT MEASURED (`COMPUTE_BUDGET_CHARTER.md` §5). | **0.0672 core-min MEASURED** — 4.032 s wall × 1 core ÷ 60, the AUTHORISED full-calibration run, single core (`GRADE_R2.out`/`.err`). Derived **$0.0000574**, not measured. Supervisor's reproduction corroborates. **The run exited rc 1 on a `json.dumps` circular-reference crash — a DISCLOSED post-grading reporting-layer defect (both verdicts + full breakdown printed BEFORE the crash); it moves NO number, is NOT a budget kill and is NOT waste.** | **= gross.** No stall (whole run 4.03 s ≪ 3600 s); no failed solve, re-run or discarded build. **Waste = 0.000 core-min, named separately (charter §6).** | **0.672** (0.0672 / 0.1) actual/filed — an **UNDER-run**; **0.224 of cap** (0.0672 / 0.3), no overrun. | **UNDER-run attributed to load variance between runs** (R1's PARTIAL run measured 0.1128 core-min, this R2 FULL run 0.0672 — both bracket the ~0.063 core-min per-op method figure (32 renders × 0.1087 s + 34 digitizes × 0.0088 s) plus import startup; R2 doing more work yet clocking lower is load variance, not a method change). The **rc 1 `json.dumps` crash** is a reporting-layer defect (the encoder `default` returns `np.bool_` unchanged → circular reference), disclosed in `RESULTS` and flagged for a one-line re-file; it fired AFTER grading and **affects no cost figure**. **Waste = 0.000 core-min** (named, charter §6). | Results `cases/ansys_verification/DIGITIZER/R2/RESULTS.md` (verdicts R2-VALUE PASS, R2-POSITION PASS), committed **`b657074e`**; run evidence `GRADE_R2.out`/`.err`/`.json` (the `.json` reconstructed via `calibrate_r2(verbose=False)` because the CLI's own json.dumps crashed — labelled in its `_note`); freeze `5714f2c7` (R2 blob `c1d275a2`); filed estimate/cap `R2/PREREGISTRATION.md` §8 |
| C-20260903T171831.307001Z-6cf70ff7 | 2026-09-03 | heat-transfer | **T25R6a — `C5` at the outer levels L1 and L3, gated on wall cost. Four arms (`B0_L1`, `C5_L1`, `B0_L3`, `C5_L3`), all four rule-4 COMPLETE with the age guard, all four `DONE.*` markers on disk. Registration `docs/campaigns/T-family/T25R6a_PREREGISTRATION.md` v1.1, frozen `2b9bd20b`, amended pre-compute at `5d17ec03`.** ⛔ **THE RUNG'S VERDICT IS `NOT A RESULT`, AND THE GROUND IS AN INSTRUMENT DEFECT, NOT THE PHYSICS.** The frozen grader `grade_t25R6a.py` (disk blob `8540ecf5`, byte-identical to `2b9bd20b`'s — verified before grading) returned `NOT A RESULT` (exit 4) with `ground = "the equivalence control FIRED (prereg §6.4)"`, **while its own printed limbs all passed and `equivalence.C5_L1.fired` and `equivalence.C5_L3.fired` are both `[]`.** Mechanism, verified at source: `grade_t25R6a.py:434-438` calls the delegated `compare_arms_t25R5.compare()` and tests `if r is not True and r != 0`; `compare()` has exactly one return, `return res`, **a dict**, so the predicate is `True` for every possible return and `equiv_ok` is set `False` unconditionally. **The equivalence gate is unsatisfiable by construction — a gate that CANNOT PASS.** This row therefore calibrates a rung whose compute is spent and whose result is **not yet collected**. Repair is not this lane's to make (rules 2 and 6; the grader is frozen and post-compute) and is referred to the supervisor and to verification under `VERIFICATION_CHARTER.md` §2d.1. | **40.111 core-min POINT**, frozen at `T25R6a_PREREGISTRATION.md` §8.1 before any arm ran, and **fixed as this ratio's denominator by §8.5 in advance** — *"not the ceiling, not the CAP total, and not the cap-setting estimate of §8.2. A successor cannot choose the flattering denominator."* Per arm: `B0_L1` **4.376** and `B0_L3` **32.980** (both **measured**, from `P3_SCORE.json.rows.exec_s` 131.28 / 989.39 s at 2 ranks); `C5_L1` **0.323** and `C5_L3` **2.433** (both **labelled extrapolations** at every appearance, built on H1's no-decay arithmetic `× 38.07/516.15`). Named so they are not confused with the denominator: **sum of per-run hard caps 224.40**, **registered rung ceiling 230 core-min = $0.1966 derived**, per-run caps **13.20 / 13.20 / 99.00 / 99.00** set at ~3× the **no-speed-up** cap-setting estimate per §8.2, precisely so that a cap could not censor a `C5` slower than hoped. | **41.633 core-min GROSS** = 0.6939 core-h ≈ **$0.0356 DERIVED, NOT MEASURED** at the owner-stated $0.0513/core-h, `cost_basis` **REPORTED-BY-OWNER** — the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` §5). **WALL basis**, from each arm's own `DONE.*` `started_utc`/`finished_utc` stamps × 2 ranks ÷ 60: `B0_L1` 135 s = 4.500, `C5_L1` 13 s = 0.433, `B0_L3` 961 s = 32.033, `C5_L3` 140 s = 4.667. ⚠ **BASIS DISCLOSED RATHER THAN SMOOTHED, per append rule 3:** on the solver's own `exec_s` (131.87 / 12.38 / 956.97 / 135.56 s) the gross is **41.226 core-min = $0.0352 derived**. **The difference is 0.407 core-min and moves the ratio by 0.010; it changes no decision.** The 16 s unaccounted between the two bases at whole-rung level is inter-arm sequencing (2 + 6 + 8 s of gaps), and the arithmetic closes exactly: 1,249 s of run wall + 16 s of gaps = the 1,265 s span 16:42:28Z → 17:03:33Z. **`ranks` = 2 throughout, `--bind-to none`.** | **= gross, 41.633 core-min.** Longest single run is `B0_L3` at **961 wall s**, a factor of 3.7 inside `COMPUTE_BUDGET_CHARTER` §2's 3,600-s stall rule, so cleaned and gross coincide **under the charter's own definition** (not a source-record definition). **No arm cap-stopped:** the verdict record's `censored` block reads `False` on all four, and the closest approach to any cap is `B0_L3` at 32.033 of 99.00 core-min = 32 %. | **1.0379** = 41.633 / 40.111 on the wall basis. Reported beside it and never substituted for it: **1.0278** on the `exec_s` basis; **0.1855** of the sum of per-run caps; **0.1810** of the 230 core-min rung ceiling. **No overrun, on any arm or in total.** **Waste is 0.000 core-min and is in this ratio in neither direction.** | **THE 3.8 % GAP IS NOT NOISE AND IT IS NOT A BUDGETING MISS — IT IS THE RUNG'S OWN MEASUREMENT SHOWING UP IN ITS COST COLUMN, AND THE REGISTRATION PREDICTED THIS IN ADVANCE, AGAINST ITS OWN INTEREST.** §8.1 wrote of the two `C5` figures: *"⚠ The two `C5` figures are the very quantity this rung exists to measure… if `P-1`/`P-2` go against this lane they will be too small."* **They were too small, and by exactly the amount the physics moved.** Per-arm actual/predicted: `B0_L1` **×1.004**, `B0_L3` **×0.967** — **the two measured baselines reproduce their T25R5 rates to within 0.4 % and −3.3 %, so the estimate's measured half is sound and the gap is not in the baseline**; against that, `C5_L1` **×1.278** and `C5_L3` **×1.857** — **the two extrapolated arms, both under-priced, and worst at L3.** The mechanism is the decay of `C5`'s advantage with refinement: `f_C5` measures **10.65 at L1** and **7.06 at L3** against the frozen L2 value **13.56** on which both extrapolations were built. **Transferable calibration rule, and it is the direct successor to C-20260903T163034's rule that produced this design:** *pricing a speed-up arm by carrying one level's measured factor to the other levels is the same error as pricing it at the baseline rate, mirrored — it under-prices instead of over-pricing, and it under-prices most at the level that dominates ladder cost.* An estimate for an arm at an unmeasured level should carry **two registered brackets — the arm's best measured factor and no speed-up at all — and name which one the ratio's denominator is**, exactly as §8.2 already does for the **cap**. Here §8.2's cap-setting estimate did use the no-speed-up bracket and was **right**: `C5_L3`'s actual 4.667 sits inside a cap of 99.00 with the falsifier uncensored. **WASTE, NAMED SEPARATELY PER CHARTER §6 AND FOLDED INTO NO RATIO AND NO EXPLANATION: 0.000 core-min.** No cap stop, no re-run, no discarded or disqualified arm, no failed stage. ⚠ **AND ONE THING THAT IS EXPLICITLY *NOT* CALLED WASTE, because the distinction matters:** the 41.633 core-min bought four complete, reproducible, rule-4-clean runs whose `P-4` integer reproduction is **EXACT** on both baselines (931/1200 feasible, sum 28821, pinned 275 at L1; 1111/1200, sum 366954, pinned 96 at L3) and whose `ExecutionTime` sits **in band** on both. **The compute is sound and re-gradable the moment a lawful grading path exists. It is a result awaiting an instrument, not compute thrown away** — and calling it waste would misattribute an instrument defect to the runs. | Verdict record `verification/runs/T-family/T25R6a_C5_OUTER_runs/T25R6a_VERDICT.json` (`verdict`, `ground`, `equivalence.*.fired`, `censored`, `repro`, `point_estimate_core_min` 40.111, `rung_cap_core_min` 230.0); wall stamps from `DONE.B0_L1`, `DONE.C5_L1`, `DONE.B0_L3`, `DONE.C5_L3`; `exec_s` from each arm's own `log.solve.legA` via the frozen `read_log()`; `STATUS.queue.T25R6a` (`launcher_rc=0`, end 17:03:33Z). Grader freeze verified before grading: disk blob `8540ecf5aad6bf7d0c2c90c81ce2405fa85c1bf4` == `2b9bd20b:verification/runs/T-family/T25R6a_C5_OUTER_runs/grade_t25R6a.py`, disk sha256 `046f98462dc5c43ad0da3e678bc2d4bcf61a48f5f189357ecbe720dfe16b06b4`; `__pycache__` cleared before both the selftest and the grade (stale bytecode inverts control and mutation outcomes). Frozen grader `--selftest` **PASS, 0 failed**, run immediately before grading. Registered estimate/caps `T25R6a_PREREGISTRATION.md` §8.1, §8.2, §8.5; predecessor cost row C-20260903T163034.569926Z-e53bb917. |
| C-20260903T173557.828437Z-71cdb068 | 2026-09-03 | heat-transfer | **T5c — the y+ ladder-clause re-grade, T5's FIRST graded rows in the campaign's history. Registration `docs/campaigns/T-family/T5c_PREREGISTRATION.md` frozen `e0c5fee8`; grader `verification/runs/T-family/T5c_runs/analyse_t5c.py` frozen `c9865347` BEFORE it had ever been run; authority `VERIFICATION_CHARTER.md` §2d.11.2 (`ad9eda53`), which discharged T5c's own stop line at `:287`.** **NO SOLVER COMPUTE** — it re-grades the three completed T5b levels on disk. Verdicts: `G2a` **GATE FAIL** (fine 39.4023 vs reference 55.224, band ±5.66404, GCI 4.3550 %); `G1a`/`G3a` **NOT A RESULT** (triple OSCILLATORY); `G5a`/`G5b`/`G5c` **NOT A RESULT** (triple DIVERGENT); **0 of 6 PASS**. All five §6 birth arms printed both limbs before any row graded. | **`< 1 core-min`**, `T5c_PREREGISTRATION.md` §8, frozen before compute. ⚠ **THIS IS A BOUND, NOT A POINT ESTIMATE, AND THAT IS THE MOST IMPORTANT THING IN THIS ROW.** The registration wrote an inequality — *"< 1 core-min, comparator time only"* — so there is no predicted value to divide by. The ratio column below uses **1.0** as the denominator because that is the only number the registration supplies, which makes the ratio an **UPPER BOUND on the ratio**, not the ratio. Named so it is not confused with the above: the same §8 registers the **fresh three-level ladder fallback at 451.833 core-min MEASURED** (`STATUS.T5_CUBE_{c,m,f}`), which was **NOT incurred** — verification ruled FOR the re-grade, so §7's fallback condition never triggered. | **0.132 core-min MEASURED** = 7.92 wall s × 1 rank ÷ 60, the graded run, single rank. ≈ **$0.000113 DERIVED, NOT MEASURED** at the owner-stated $0.0513/core-h, `cost_basis` **REPORTED-BY-OWNER** — the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` §5). Named separately and **not** added into the graded figure: the `--selftest` run immediately before it, **0.03 wall s = 0.0005 core-min**, which grades nothing and is a control. `__pycache__` was cleared before both invocations (stale bytecode inverts control and mutation outcomes). | **= gross, 0.132 core-min.** The whole run is **7.92 wall s**, a factor of **454 inside** `COMPUTE_BUDGET_CHARTER` §2's 3,600-s stall rule, so cleaned and gross coincide **under the charter's own definition**, not a source-record definition. No failed run, no re-run, no discarded output. | **≤ 0.132** (0.132 / 1.0). ⚠ **An upper bound on the ratio, because the denominator is a bound** — see the predicted column. Stated plainly and not as a success: **the estimate was roughly an ORDER OF MAGNITUDE conservative.** No overrun; no cap was approached. | **AN ESTIMATE 10× CONSERVATIVE IS CALIBRATION DATA, NOT A SUCCESS — and the deeper defect here is the SHAPE of the estimate, not its size.** (1) **THE REGISTRATION WROTE AN INEQUALITY WHERE A POINT ESTIMATE WAS AVAILABLE AND CHEAP.** `< 1 core-min` cannot be calibrated: any actual under 1 satisfies it, so the row can never say the estimate was wrong, only that it was not exceeded. That is an unfalsifiable cost prediction, and it is the cost-column analogue of the gate discipline this lab applies everywhere else. **Transferable rule: a comparator-only step carries a POINT estimate derived from one measured comparator run — of which this team has many — and the bound goes in a separate cap column.** Had T5c priced from any prior T-family comparator it would have registered ~0.1–0.2 core-min and this ratio would have read near 1.0 and meant something. (2) **THE 10× IS THEREFORE NOT A MISPREDICTION THAT CAN BE ATTRIBUTED** — there is no predicted value that missed. It is the distance between an actual and a ceiling, which is a different quantity and is labelled as one here rather than reported as estimate quality. (3) **NO CONTENTION EFFECT IS CLAIMED:** at 7.92 s single-rank on a shared box, contention is unresolvable at this scale and nothing instrumented it; it is left unmeasured rather than asserted absent. **WASTE, NAMED SEPARATELY PER CHARTER §6 AND FOLDED INTO NO RATIO AND NO EXPLANATION: 0.000 core-min.** ⚠ **AND ONE THING EXPLICITLY NOT CALLED WASTE:** the 451.833 core-min of T5b solver compute this re-grade consumed was spent under T5b and is calibrated in its own row — T5c inherited it at zero marginal cost, which is precisely why re-grading was the cheapest real verdict available to this team. | Results `docs/campaigns/T-family/T5c_RESULTS.md` (verdict table §0, y+ block §1, the R_area monotonicity measurement §2, the disclosed sha256-witness gap §5a), landed `150a5e3e`; unedited comparator output `verification/runs/T-family/T5c_runs/T5C_GRADE_OUTPUT.txt`. Grader disk sha256 `50b484f0541cea52baea12e1058204f35b7922b40f7e75adf60bb71e13749f0b`, byte-identical to `HEAD:verification/runs/T-family/T5c_runs/analyse_t5c.py`, frozen `c9865347`. Registration frozen `e0c5fee8` (§8 for both cost figures, §7:288-289 for the fallback condition that never triggered). Authority `ad9eda53`. Predecessor T5b's six `NOT A RESULT` verdicts stand as published and are undisturbed. |
| C-20260903T180449.568691Z-0097660a | 2026-09-03 | heat-transfer | **T19 — the four arms (`P_Ts_m`, `P_q_m`, `P_Ts_f`, `P_q_f`) the queue daemon relaunched on 2026-09-03 under `prereg_commit 4d8943d8` (`docs/campaigns/T-family/T19_PREREGISTRATION.md`). ALL FOUR ARE `NOT A RESULT`, and no graded quantity exists.** Completion driven, not cited: `python3 mark_done_t19.py --root . <arm>` returns **rc = 1 on every one of the four**, rc captured directly from `$?`. Rule 4 clause by clause — **rc = 0 HELD** (`STATUS.P_*`, `launcher_rc=0`); **`End` line HELD** (each `log.solve` ends `End`); **fields present HELD** on the rung's REGISTERED four-field tuple `{U, p_rgh, T, alphat}` (T19 §9; this is a LAMINAR rung and `nut`/`k`/`omega` are not registered for it); **age guard HELD where evaluable** — at `P_q_f` the last write `12437/T` is 949 s newer than the case's own `0/T`, at `P_Ts_f` `7238/T` is 722 s newer; **last time == `endTime` FAILED** — 1929 / 3203 / 7238 / 12437 against a registered `endTime` of **30 000**; **`ExecutionTime` count == `endTime` FAILED** — the same four counts against 30 000. **Two physics-critical clauses failed, so this is not a bookkeeping loss and the 2026-08-26 rule does not reach it: the record is not missing, the SOLVE is short.** The frozen grader `analyse_t19.py` then **REFUSED at exit 2** — *"no DONE.P_Ts_c -- the whole rung is graded or none of it is"* — so **no value, no triple, no observed order and no GCI is quotable, and none is quoted.** `C_PLATEAU`, the rung's own load-bearing gate, is **UNEVALUABLE, not failed**: it compares the writes at 28 000 and 30 000 and **neither write exists on any of the four**. Freeze integrity is intact and was re-verified in this lane: all six T19 §9 instruments hash to their registered blobs and sha256 prefixes on both channels (`analyse_t19.py` `0bafecd0…`/`c2d8120ff510b1c4`, `mark_done_t19.py` `13117be1…`/`a4dee8b3eb2e1af2`, and the other four likewise), line counts unchanged, **no frozen file edited.** **CAUSE, ESTABLISHED FROM THE RECORDS, NOT INFERRED FROM MEMORY:** `build_t19.py:211` emits `residualControl { p_rgh 1e-9; U 1e-9; T 1e-9; }`, it reappears at each arm's `system/fvSolution:32`, and each `log.solve` ends *"SIMPLE solution converged in N iterations"*. **This is the identical defect `T19b_PREREGISTRATION.md` §0.1 recorded on 2026-08-31, and T19 was superseded by T19b that same day** (T19b graded and closed at `74a9141d`; `gate_t19b.json` carries `"supersedes": "T19"`). **THE ONE THING THE SPEND BOUGHT, STATED SO IT IS NOT WRITTEN OFF AS TOTAL LOSS:** T19b §0.1 could cite the defect on the **two** coarse arms only and had to declare it *"BORROWED from T1c and never measured"* on the rest; the defect is now **MEASURED on all six T19 cases**. | **181.152 core-min** — `T19_PREREGISTRATION.md` §7 POINT, per arm: `P_Ts_f` 77.952, `P_q_f` 77.952, `P_Ts_m` 12.624, `P_q_m` 12.624; caps 250/250/50/50 core-min; transcribed identically in the four queue entries' `cost_core_min_estimate`. **$0.1549 DERIVED, NOT MEASURED** at $0.0513/core-h, c7a.4xlarge, owner-stated — the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER` §5) | **29.050 core-min** on the registered basis `wall_s × ranks ÷ 60`, ranks = 1: `P_Ts_f` 722 s → 12.033, `P_q_f` 949 s → 15.817, `P_Ts_m` 31 s → 0.517, `P_q_m` 41 s → 0.683 (`STATUS.P_*`, `wall_s` field). **$0.0248 DERIVED, NOT MEASURED** | **= gross, and the stall rule is reported as NOT MATCHING rather than silently omitted.** No arm reached the 3 600 wall-s stall marker; the longest is `P_q_f` at **949 s**. **The registration had PREDICTED the two fine arms at ~4 680 wall s each, ABOVE that marker, and declared them non-stalls in advance** (§7, *"A LONG RUN, DECLARED IN ADVANCE"*); that prediction did not materialise, and the reason is the truncation, not a faster box. No cleaning applied | **0.1604 (29.050 / 181.152) — AND THIS RATIO IS NOT A RATE CALIBRATION. It is published with that warning attached, because entering it bare would put a false 6x-over-conservative estimate into this ledger.** The registered price bought **30 000 iterations per arm**; the arms delivered **24 807 of 120 000 registered iterations = 20.67 %**. The estimate was not too high — **the experiment was short** | **NOT CONTENTION, NOT WASTE-IN-THE-RATIO, AND NOT A MISPREDICTED RATE: THE ITERATION COUNT WAS TRUNCATED BY THE `residualControl` DEFECT.** Normalising to the iterations actually executed isolates the rate, and it is the ONLY calibration figure this row is entitled to publish: measured core-s per cell-iteration `P_Ts_m` **1.685e-06**, `P_q_m` **1.337e-06** against the registered medium rate 2.63e-06 (**0.641x / 0.508x**); `P_Ts_f` **2.595e-06**, `P_q_f` **1.983e-06** against the registered fine rate 4.06e-06 (**0.639x / 0.488x**); rung total **0.544x** (29.009 measured `ExecutionTime` core-min against 53.283 the registered rates price the delivered iterations at). **THE REGISTERED RATE OVER-PREDICTS BY ROUGHLY 2x, AND THAT IS A SCORED PREDICTION MISS:** T19 §7 named the misprediction risk explicitly and named its DIRECTION — *"the expected direction is under-prediction"*, on a 1.47x mesh jump *"in the direction where the rate has been observed to rise"*. **The measured direction is the OPPOSITE. The prediction is reported as WRONG, per the registration's own instruction that "If a prediction loses it is reported as wrong."** Transferable finding: T1c's fine-level rate of 4.06e-06 core-s/cell-iteration does not transfer to a 38 400-cell planar case of the same solver and closure — the true rate there is **1.98e-06 to 2.60e-06**, and the spread BETWEEN the two arms at one mesh level (1.31x) is itself larger than the whole c→f rate rise this registration modelled. **WASTE, SEPARATELY NAMED AND NEVER ABSORBED INTO THE RATIO ABOVE: 29.050 core-min, $0.0248 DERIVED — the ENTIRE spend.** A rung superseded and closed three days earlier was re-run in full. **MECHANISM, FROM THE RECORDS:** the four pending entries `verification/queue/heat-transfer/{P_Ts_m,P_q_m,P_q_f}.json` (and `P_Ts_f`) were **never retired when T19 was superseded on 2026-08-31**, and the queue daemon consumed them on 2026-09-03 at 17:27:52Z / 17:31:07Z / 17:32:12Z / 17:34:22Z (`_launch.utc`, pids 221616 / 228867 / 230879 / 233439), moving each into `launched/`. **NO FURTHER T19 ENTRY REMAINS PENDING** — `verification/queue/heat-transfer/` now holds only `README.md`, `held/`, `launched/`, `refused/` — so this cannot recur from the queue as it now stands. **CARRY FORWARD: superseding a rung must retire its pending queue entries in the same commit; a supersession recorded only in prose leaves the daemon reading a dead rung as live work.** | four `STATUS.P_*` and four `.../T19_runs/<arm>/log.solve` under `verification/runs/T-family/T19_runs/`; `verification/runs/T-family/T19_runs/<arm>/system/fvSolution:32` and `build_t19.py:211`; queue entries `verification/queue/heat-transfer/launched/{P_Ts_m,P_q_m,P_Ts_f,P_q_f}.json`; pre-registration `docs/campaigns/T-family/T19_PREREGISTRATION.md` §3, §7, §9, frozen at `4d8943d8`; the supersession `docs/campaigns/T-family/T19b_PREREGISTRATION.md` §0.1 and `verification/runs/T-family/T19b_runs/gate_t19b.json` (`"supersedes": "T19"`), closed at `74a9141d` |
| C-20260903T180449.568767Z-c7e2af11 | 2026-09-03 | heat-transfer | **T9a-R1c (`W1c`) — FLOOR DEMONSTRATION, three levels `W1c_c`/`W1c_m`/`W1c_f` (35 / 56 / 90 cells), `laplacianFoam`, 1 rank, 1 000 steady iterations each. RUNG VERDICT `PASS`: the one GRADED row `F1` `PASS`.** **THE ROW OWED SINCE 2026-08-28 AND NEVER LANDED — the delay is disclosed here, not smoothed over.** A draft was written at `verification/runs/T-family/T9aR1c_runs/COST_CALIBRATION_ROW_DRAFT.txt` and its figures are adopted here **only after this lane re-measured every one of them** from the named artifacts. Completion driven, not cited: `mark_done_t9aR1c.py --root . <level>` returns **rc = 0 on all three**, `DONE.W1c_{c,m,f}` present, `wall_s = 0` with **1 000 `ExecutionTime` lines against `endTime` 1000** in each `log.solve`. Freeze verified in this lane on both channels: `analyse_t9aR1c.py` on disk is sha256 `ecd58da928dfc523` and the blob at the freeze commit `8268ffe2` hashes to **the same value**. **F1 value: |T_i1 − T_i1_exact| = 5.684e-14 K at the graded level `W1c_c`, against a registered floor of 1.000e-04 K — nine orders inside it.** **NO ROACHE TRIPLE EXISTS AND NONE IS QUOTED — BY DESIGN, NOT BY OMISSION.** `rule5_status` in the gate file reads *"limb (2) UNREACHABLE, NOT WAIVED (charter 2f.2); limb (1) applies IN FULL as gate (1), BINDING"*, and the `C_NOTRIPLE` control **PASSES** (12 forbidden names checked, none present; the only import from `roache_triple` is `PLANT`). **So there is no observed order and no GCI, and quoting either would be the violation.** `PASS` is available to this limb under `VERIFICATION_CHARTER` §2h (v1.17), whose five §2h.4 conditions were declared before compute at prereg §3a. **THIS RUNG IS THE REMEDY §2g.3 ORDERED FOR ITS PREDECESSOR:** W1b's R1 measured an **`EXACT`** triple and its frozen comparator recorded `PASS`; §2g (v1.16) **REFUSED that floor exception on rule 5's own one-way sentence**, ruling the Roache limb `NOT A RESULT` with the measurement reported beside it. W1c registers no triple at all, so the collision cannot arise. Planted-zero controls, four arms, **all PASS**: value plant 1.234e-03 K at the reader's own stencil cell 9 → 1.175e-03 K recovered; permutation plant moved −1.158 K; far plant at cell 34, outside the stencil, moved **exactly 0.000 K**; negative arm 0.000 K; demonstrated detection floor **1e-07 K**. REPORTED rows: `N1` deviation per level 5.684e-14 / 3.183e-12 / 2.899e-12 K; `N2` mesh-family spread **3.240e-12 K**, explicitly a BOUND over the meshes actually read and **not** a GCI. **FILING DEFECT REPORTED, NOT CONCEALED: at the time this row was written the rung's entire evidence set — `gate_t9aR1c.json`, `W1c_GRADE_OUTPUT.txt`, the three `STATUS.W1c_*`, the three `DONE.W1c_*` and the three case trees — was UNTRACKED in git**, i.e. a `PASS` resting on files no commit held. Landed by the companion commit to this row. | **0.002667 core-min** on the ExecutionTime basis, `T9aR1c_registered.json` `cost.point_total_core_min_executiontime_basis` (per level: `W1c_c` 0.000833, `W1c_m` 0.000833, `W1c_f` 0.001); cap 1 core-min per level, 3 total. **$0.0000023 DERIVED, NOT MEASURED.** The rung **registered `ExecutionTime` as its PRIMARY cost basis BEFORE compute**, on W1b's transferable finding that below ~1 wall-second the core-minute basis returns 0 and a 0 there means *too small to measure*, not *free* | **0.002833 core-min MEASURED** from each named `log.solve`'s final `ExecutionTime` at `endTime` — `W1c_c` **0.05 s**, `W1c_m` **0.06 s**, `W1c_f` **0.06 s**, 1 rank, re-read in this lane from the three logs. **$0.0000024 DERIVED, NOT MEASURED** | **= gross.** The 3 600 wall-s stall rule matches no level: every level ran in under one wall-second. No cleaning applied | **1.063x (0.002833 / 0.002667) on the registered ExecutionTime basis.** **The wall basis is reported UNRESOLVED, not as a ratio**: all three `STATUS.W1c_*` read `wall_s=0`, `core_min=0.000`, exactly as the registration predicted, and 0.000/0.0045 would enter a false perfect-underspend into this ledger. **Registering the ExecutionTime basis up front is what let this rung carry a ratio at all — W1b, one week earlier and byte-identical in size, could not** | **MISPREDICTION OF ONE LEVEL BY EXACTLY ONE CLOCK QUANTUM. NO CONTENTION. ZERO WASTE.** Two of three levels landed on their point (`c` 0.05 predicted / 0.05 measured; `f` 0.06 / 0.06). `W1c_m` came in at 0.06 s against 0.05 s carried from the predecessor's measured `W1b_m` — **+20 % on that level, +6.3 % on the rung.** OpenFOAM prints `ExecutionTime` to 0.01 s, so **on a 0.05 s measurement one quantum IS 20 %**: the gap is below the instrument's resolution and **no rate should be revised from it.** **Transferable calibration finding: at 35–90 cells the `ExecutionTime` clock quantum is 20 % of the measurement, so any per-cell-iteration rate fitted at this case size carries a ±20 % floor that no care removes.** The registration's own scaling exponent (`total_time_vs_cells` 0.193, the rate falling as N^-0.807 because process start-up dominates the linear algebra at this size) is **consistent with the measurement and is neither confirmed nor refuted by it** — 0.05/0.06/0.06 s is one quantum of spread across a 2.57x cell range. Cap headroom used: **0.094 %** of the 3 core-min total. **WASTE: 0.000 core-min, separately named** | `verification/runs/T-family/T9aR1c_runs/{STATUS.W1c_c,STATUS.W1c_m,STATUS.W1c_f}` and each level's `log.solve`; `verification/runs/T-family/T9aR1c_runs/gate_t9aR1c.json` and `W1c_GRADE_OUTPUT.txt`; `T9aR1c_registered.json` `cost` block; pre-registration `docs/campaigns/T-family/T9aR1c_PREREGISTRATION.md` §3a, §9, frozen at `8268ffe2`; queue entry `verification/queue/heat-transfer/launched/W1c_c.json`; the predecessor ruling `docs/charters/VERIFICATION_CHARTER.md` §2g (v1.16) and the licensing ruling §2h (v1.17); W1b's own row `C-162` |
| C-20260903T180819.145643Z-49087129 | 2026-09-03 | cfd | **RUNG 0 `RUNG0_MESH_IMPORT` — the committee-grid import lane, ATTEMPT 3, run to completion 2026-09-03T17:46:32Z → 17:49:05Z, `rc=0`.** Four grids (DPW5 L1.T hex 638,976 / prism 1,277,952 / hybrid 2,981,888 cells, HLPW6 `h6c1_rans_3a_1` 2,661,338 = **7.560154 Mcell**) imported by `cases/committee-grids/ugrid_to_foam.py`, `checkMesh`'d, and graded by `verification/runs/RUNG0_MESH_IMPORT_runs/analyse_rung0.py`. **Rung verdict on disk: `PENDING`** — `R0-G1`, `R0-G2a`, `R0-G3`, `R0-G4` all **`PASS` on all four grids**, `R0-G2b` **`PENDING` on all four** because `cases/committee-grids/foam_to_ugrid.py` does not exist, and §4's `PASS` is the conjunction over all four grids of all of `R0-G1`/`R0-G2a`/`R0-G2b`. **`PENDING` is used here in rule 1's display sense (NOT YET RUN) and softens no failure**: no gate failed. Eleven planted controls, ten FIRED before any conversion, **C9 (the `foam_to_ugrid.py` round-trip plant of registration §7) NOT RUN — UNBUILT, neither passed nor failed.** | **7.5 core-min = $0.006412 DERIVED** — registered at `verification/campaign/RUNG0_MESH_IMPORT_PREREGISTRATION.md` §5.1 (6.139 raw × the +22 % contention band = 7.49, stated 7.5), frozen at `d127d83d`. **HARD CAP 23.0 core-min = $0.019665 DERIVED**, §13.1 (Sanaa 2026-09-03 ~18:00Z, ≈3× the team's own estimate); §5.2's earlier 200 core-min cap was struck by Amendment 1 and is not used here. **THE DENOMINATOR IS FIXED AT THE REGISTERED 7.5 AND IS NOT ADJUSTED DOWNWARD TO FLATTER THE RATIO.** | **2.5500 core-min MEASURED = $0.002180 DERIVED** — 153 wall s × 1 rank ÷ 60, from `verification/runs/RUNG0_MESH_IMPORT_runs/STATUS.RUNG0_MESH_IMPORT` (`rc=0 total_wall_s=153 core_min=2.55 cap_core_min=23`). **Serial throughout (ranks = 1, so core-min = wall-min on every figure in this row).** GROSS: contention on the shared box is not separated out. Phase split MEASURED from artifact mtimes and `TIMING.json`, not apportioned: planted controls **12.43 s** (`RUN_ROOT_CREATED_EPOCH` 17:46:32.751 → `log.controls` 17:46:45.180); convert **81 s** and `checkMesh` **30 s** (the eight per-grid rows of `TIMING.json`, summing to 111 s, which matches the 111.22 s between `log.controls` and `TIMING.json` to 0.2 s); grading pass **29.18 s** (`TIMING.json` 17:48:36.402 → `log.analyse` 17:49:05.585); driver residual **0.26 s**. | **= gross, 2.5500 core-min.** The 3600-s stall rule matches nothing: the **largest single wall on this item is 30 s** (HLPW6 convert, `TIMING.json`), two orders of magnitude inside the rule. No cleaning was applied and none was available to apply. **WASTE IS NOT IN THIS COLUMN** — see the gap cell, per `COMPUTE_BUDGET_CHARTER.md` §6. | **TWO RATIOS ARE REPORTED AND WHICH IS WHICH IS STATED. (1) HEADLINE, against the registered denominator: 0.340** (2.5500 / 7.5) — this is the rule-12 figure. **(2) Against the runnable subset, 0.491** (2.5500 / 5.198) — a *diagnostic* ratio, not the headline, over the three of §5.1's five line items that could physically execute. **5.198 = 7.5 − 2.302 is the PRE-DECLARED runnable denominator** (queue entry `cost_note_the_denominator_is_fixed_in_advance`, written BEFORE the run, predicting "near 5.2 core-min, roughly 0.69x"). **Disclosed rather than smoothed: 5.198 subtracts the two dead items at their RAW values while 7.5 carries the +22 % band, so the pre-declared 5.198 is slightly generous; the band-consistent runnable denominator is 4.692 (= 7.5 − 2.302 × 1.22) and would give 0.544.** The pre-declared 5.198 / 0.491 pair is the one reported because it is the one registered in advance. **Cap usage: 11.09 % by attempt 3 alone (2.5500 / 23); 18.70 % by everything charged to the item (4.3000 / 23 — attempt 3 plus both waste attempts).** | **ATTRIBUTED, NOT ABSORBED. The 4.95 core-min gap decomposes into three named parts that sum to it.** (a) **STRUCTURAL NON-RUN — 2.302 core-min, 46.5 % of the gap, PREDECLARED BEFORE THE RUN AND NOT AN EFFICIENCY.** Two of §5.1's five line items cannot execute without `foam_to_ugrid.py`: `foam_to_ugrid.py` export of four meshes **1.535** and the independent reader on the exported side **0.767**. Registration §9 registers that writer as to-be-written; it was not written; the two items are 0.000 measured. **This was stated in advance in the queue entry precisely so it could not be reported afterwards as a win.** (b) **MISPREDICTION — 1.290 core-min, 26.0 % of the gap, and it is CONCENTRATED IN ONE LINE ITEM.** Per item, predicted raw vs measured: import 4 grids **1.535 → 1.350, ratio 0.879** (measured converter rate **0.1786 core-min/Mcell** against the registered 0.203, i.e. 12 % under and comfortably inside the registered ±22 % band — **the converter rate is well calibrated**); `checkMesh` on 4 meshes **1.535 → 0.500, ratio 0.326**; independent reader source side **0.767 → 0.694** (controls 0.207 + grading pass 0.486), **ratio 0.904 — the best-calibrated item in the registration**. **`checkMesh` alone is 1.035 core-min = 80.3 % of ALL the misprediction.** Root cause, named for the next estimate: §5.1 priced `checkMesh` at **1× the converter rate** and it measured at **0.33×** (0.0661 core-min/Mcell). **THE LESSON FOR THE LAB'S ESTIMATES IS SPECIFIC: `checkMesh` is not a converter-rate job on committee grids; price it at ~1/3 of the converter rate, or 0.066 core-min/Mcell, until a second measurement says otherwise.** (c) **UNUSED CONTENTION BAND — 1.361 core-min, 27.5 % of the gap.** §5.1's +22 % allowance over the 6.139 raw subtotal; **the box carried no measured competing load during the 153 s window and the allowance was simply not drawn.** This is neither waste nor misprediction of the work — it is an insurance line that did not pay out, and it is named separately so it does not flatter the converter's calibration. (a)+(b)+(c) = 2.302 + 1.290 + 1.361 = **4.953 ≈ the 4.95 gap** (0.003 rounding). **WASTE — SEPARATELY NAMED, NEVER LAUNDERED INTO EITHER ACTUAL COLUMN OR INTO EITHER RATIO (`COMPUTE_BUDGET_CHARTER.md` §6): 1.7500 core-min = $0.001496 DERIVED, charged to this item and belonging to ATTEMPT 2.** Attempt 2 launched 17:33:17Z (pid 232473), `rc=0`, 105 wall s × 1 rank ÷ 60 = 1.7500 core-min (`ATTEMPT2_PRESERVED/TIMING.attempt2.json`, eight rows summing to 105 s), **converted and `checkMesh`'d all four grids cleanly and its verdict was VOID** — the comparator reported `GATE FAIL` on all four while the meshes were entirely sound, on **two defects in this team's own reader, not in the compute**: (i) the counts were taken only from a QUOTED FoamFile `note "..."` key, while `ugrid_to_foam.py` emits them as a `// comment`, so the reader found "no note" on every mesh this rung imports — the numbers were on disk the whole time, in the other label form; (ii) the repair's first derivation used `max(owner)+1`, a LOWER BOUND, which under-counted HLPW6 by exactly 2 cells while agreeing to the cell on all three DPW5 grids — **a reader validated on three of the four grids would have shipped it.** New control C10 now plants a contradicting header note and requires a REFUSAL, on BOTH label forms. **ATTEMPT 1: ~0.0 core-min (sub-second)**, launched 17:30:02Z (pid 227284), died before the planted controls and before any conversion — the driver ran under `set -u` and `/usr/lib/openfoam/openfoam2606/etc/bashrc:184` reads `WM_PROJECT_DIR` before setting it. **No mesh, no number and no gate was produced or read by either wasted attempt, so nothing about the rung is in question — but 1.7500 core-min was spent and is stated, not absorbed.** Attempt 2's artifacts are PRESERVED (moved, never deleted) under `ATTEMPT2_PRESERVED/`. **TOTAL CHARGED TO THE ITEM: 4.3000 core-min = $0.003676 DERIVED = 18.70 % of the 23.0 cap. The cap was NOT reset by either retry (rule 12: a cap is never raised).** **NO CONTENTION IS CLAIMED AS A CAUSE**: this row does not attribute one core-minute to contention, because none was measured. | **Registration** `verification/campaign/RUNG0_MESH_IMPORT_PREREGISTRATION.md` §5.1 (estimate), §5.2/§13.1 (cap), §5.4 (this row is owed by name), frozen `d127d83d`. **Queue entry** `verification/queue/cfd/launched/RUNG0_MESH_IMPORT.json` — `cost_core_min_estimate` 7.5, `cap_core_min_registered` 23.0, `cost_note_the_denominator_is_fixed_in_advance`, `cost_note_attempt_3`, `attempt_1_disclosure`, `attempt_2_disclosure`. **Actuals** `verification/runs/RUNG0_MESH_IMPORT_runs/STATUS.RUNG0_MESH_IMPORT` (153 wall s, rc=0, 2.55 core-min), `TIMING.json` (per-grid convert/checkMesh walls, ranks=1), `ATTEMPT2_PRESERVED/TIMING.attempt2.json` (105 wall s), `RUN_ROOT_CREATED_EPOCH` (1788457592.751697907), `log.controls`, `log.analyse`. **Verdict record** `RESULTS.json` (`per_grid` all `PENDING`, `prereg_commit d127d83d4ebf4caaaf0c12c39a063c5f624a03a4`, eleven control rows). **Dollars throughout are DERIVED, NOT MEASURED**, at the owner-stated c7a.4xlarge $0.0513/core-h (reported-by-owner 2026-08-21/22) — the box cannot read its own billing, `COMPUTE_BUDGET_CHARTER.md` §5. **A SECOND ROW IS OWED** when `foam_to_ugrid.py` exists and `R0-G2b` and control C9 are actually evaluated: that run draws on the same 23.0 cap, of which **18.70 % is already spent**, and it must cite this id. |

| C-20260903T181757.218115Z-be1c8e62 | 2026-09-03 | ansys-verification | **VMFL046-INVISCID — the Euler arm of the CD-nozzle shock (VM2026R1 p. 155); three-level family L1/L2/L3 = 3 200 / 12 800 / 51 200 cells, OpenFOAM v2606 `rhoSimpleFoam` with mu=0 and slip walls, serial `RANKS = 1`, `stopAt endTime`, `endTime 20000`.** The completion this row calibrates is the graded case: ran 2026-09-03T17:43:27Z to 18:11:08Z, register row **#55**, verdict **`NOT A RESULT`** (the frozen comparator printed `PASS`; demoted by the supervisor under CLAUDE.md rule 5 step (1) because L3's shock had not converged). **The verdict is not what this row is about and enters no figure in it.** | **28.00 core-min**, frozen in `PREREGISTRATION.md` section 7 at freeze commit `6345bf84`; cap **84.00** (~3x per §26.2). Derived $0.023940, not measured | **27.6667 core-min MEASURED** — `wall_s x RANKS / 60` from `launcher.queue.out`, serial, one level at a time. **PER LEVEL: L1 0.95** (57 s), **L2 3.8333** (230 s), **L3 22.8833** (1 373 s); total wall **1 660 s**. *(The launcher prints `core_min_used` as a RUNNING TOTAL — 0.95, 4.78333, 27.6666 — and reading those three as per-level costs double-counts; that is the error row `C-20260903T162543.364120Z-e486a4a1` had to correct on VMFL046.)* Derived **$0.023655** at $0.0513/core-h, c7a.4xlarge, owner-stated — **DERIVED, NOT MEASURED**, the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` §5) | **= gross.** No failed solve, no re-run, no killed level; longest level 1 373 s against the 3 600 s stall convention. **Cap NOT crossed: 32.9 percent of 84.00 consumed** | **0.9881** (27.6667 / 28.00). Against the cap, **0.329** | **MISPREDICTION 1.19 PERCENT — AND THIS IS THE FIRST TEST OF THE CORRECTED ESTIMATOR RULES, WHICH PASSED.** Row `C-20260903T162543.364120Z-e486a4a1` closed VMFL046 with four carry-forward rules after a **2.3375** overrun. This registration applied them and landed at **0.9881 — a 1.19 percent miss against a 133.75 percent miss on its predecessor.** **Rule (b) is the one that did the work:** *an estimator datum is INVALID ACROSS A CHANGE OF STOPPING CRITERION.* Both this run and its VMFL046 anchor stop on `endTime 20000` with `residualControl` removed, so the 20 000-iteration regime carried across unchanged and the anchor was valid. That is exactly the check that was missing when VMFL046's smoke datum (2 777 iterations under `residualControl`) was scaled into a 20 000-iteration run and produced the 7.202x miss. **WASTE: MEASURED, NOT ASSUMED.** `ExecutionTime/ClockTime` on the final line of each `log.rhoSimpleFoam`: **0.9737** (55.5/57), **0.9904** (227.8/230), **0.9928** (1363.1/1373) — idle-wait 2.63, 0.96 and 0.72 percent. **WASTE = 0.000 core-min, named separately (§6) and never folded into the ratio.** **A CALIBRATION DATUM WORTH CARRYING, AND IT INVERTS THE OBVIOUS EXPECTATION:** removing viscosity changed L1's cost **not at all** (57 s viscous, 57 s inviscid, 1.00x), cut L2 by **26 percent** (310 to 230 s, 0.74x), and made L3 **SLOWER by 4.3 percent** (1 316 to 1 373 s, 1.043x). **The inviscid solve is not uniformly cheaper, and at the finest level it was dearer** — consistent with its shock still hunting at iteration 20 000 while the viscous solve's was better damped. **Do not carry "inviscid is cheaper" forward as a rule: it is level-dependent and here it inverted at the level that matters most** | Run root `verification/runs/ansys_verification/VMFL046_INVISCID/` — `launcher.queue.out` (per-level `rc`, `wall`, running `core_min_used` against cap 84), `STATUS.queue.VMFL046_INVISCID` (`launcher_rc=0`), per-level `RUN_RC` (all `0`) and `log.rhoSimpleFoam` (the final `ExecutionTime`/`ClockTime` lines, which are the contention measurement). Estimate, method and cap frozen at **`6345bf84`** in `cases/ansys_verification/VMFL046/INVISCID/PREREGISTRATION.md` section 7. Verdict row **#55** of `verification/credentials/ansys/ANSYS_VALIDATION_REGISTER.md` |
| C-20260903T181835.498614Z-e6fb4b87 | 2026-09-03 | heat-transfer | **T25R6c `W440_C4_L1` — graded `NOT A RESULT`; the COST prediction registered before the run WINS.** Predicted **48.136** core-min — the registration's own §6 denominator, fixed in advance so a successor could not pick a flattering one. Actual **5.5717** core-min gross, from `ExecutionTime` 14.36 s (leg A, 40 steps) + 152.79 s (leg B, 400 steps) × 2 ranks ÷ 60; the launcher's own wall span (172 s × 2 ÷ 60 = 5.7333) is reported beside it and is not the figure used. **Cleaned = gross** — no leg approached the 3600 wall-s stall rule. **Ratio 0.1157.** **Waste 0.000**, named separately and folded into no ratio. **GAP ATTRIBUTION: MISPREDICTION OF THE ARM, and it was PREDICTED BEFORE THE RUN, not diagnosed after** — amendment A1.6 registered `P-C1` under Sanaa's 2026-09-03 21:00Z launch rule, stating that the frozen denominator is priced from `B0_L1`'s GAMG rate while the case is `C4_L1`'s PCG arm (T25R5 measured 13.56× faster at L2), and giving a **POINT estimate, never an inequality (L-463): 4.976 core-min, ratio 0.1034**. Outcome 0.1157 — **P-C1 WINS**, inside ±50 %; the over-estimate was 8.6×, predicted at ~10×. **Not contention and not waste.** The **cap** 210.485 core-min was priced at the FALSIFIER (`ρ = 1` at the conservative `r_C`) so it could never censor the outcome it exists to detect, and only **2.65 %** of it was spent. Dollars **$0.004764 DERIVED, NOT MEASURED** at $0.0513/core-h, `cost_basis` REPORTED-BY-OWNER — this box cannot read its own billing (`COMPUTE_BUDGET_CHARTER` §5). **The verdict is `NOT A RESULT` on two independent grounds and this row prices the compute, not a result:** rule 4 failed (no field directory at `t = 40.8`; `writeControl timeStep` counts on the GLOBAL time index and does not reset across a `latestTime` restart — this lane's staging defect, named in addendum A2.1) **and** G-T6c-1 PLATEAU failed independently at 13.939 % against 5 %, so the defect cost no verdict that would otherwise have existed. `verification/runs/T-family/T25R6c_LEGAB_runs/T25R6c_VERDICT.json`; registration `docs/campaigns/T-family/T25R6c_PREREGISTRATION.md` frozen `5e51676e`, amended `351a3fd7`, addendum `9e7bc119`. |
| C-20260903T184704.252672Z-9590cb63 | 2026-09-03 | heat-transfer | **T24 — Case 3 motor-in-duct CHT, the twelve points T23 deferred.** Twelve `T24_P{080,155,230}_U{10,20,30,40}` cases, `chtMultiRegionSimpleFoam`, mesh level L1 (39,680 cells: 35,200 fluid + 3,360 core + 1,120 housing), **1 rank each**, `endTime` 10000, launched twelve-concurrent on a 16-core c7a.4xlarge 2026-08-31T20:01:19Z–20:16:15Z. All twelve carry `DONE.T24_*`. **Process completion for rule 12 = the twelve runs completed and `gate_t24.json` was written; this row records COST.** | **360.4 core-min** SUBSET POINT (`T24_PREREGISTRATION.md` §5.2, DERIVED as 12 × the per-case POINT **30.0321** core-min, itself MEASURED as the mean of the four `T23_P305_U*` `ExecutionTime` figures 29.7337 / 30.2190 / 30.4898 / 29.6860). SUBSET CAP **540.0** core-min hard, per-case CAP **45.0** hard, enacted as `timeout 2700 s`. **$0.3081 DERIVED, not measured** at $0.0513/core-h. | **457.0753 core-min MEASURED**, SOLVER BASIS: the sum of the twelve `execution_time_s` figures in `gate_t24.json` divided by 60 at 1 rank, re-summed by this lane (2251.46 + 2258.31 + 2300.38 + 2235.52 + 2309.34 + 2326.49 + 2345.12 + 2265.75 + 2283.71 + 2291.68 + 2301.87 + 2254.89 = 27,424.52 s). Per case 37.2587 to 39.0853 core-min, mean **38.0896**. **$0.39080 DERIVED, not measured**; `cost_basis` **reported-by-owner** — the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER` §5). **NO WALL-BASIS FIGURE EXISTS AND NONE IS INVENTED:** T24's `STATUS.T24_*` files record only `launcher_rc`, `end` and a note, no `wall_s`, so the solver basis is the only measured basis available here. | **Cleaned = gross = 457.0753 core-min.** The 3600-wall-s stall rule matches nothing: the longest case is **2345.12 s**, 65 % of the rule's threshold. **Waste 0.000 core-min, NAMED SEPARATELY and folded into no ratio** — twelve launches, twelve completions, no failed run, no re-run, no capped arm; the worst case reached **86.9 %** of its 45.0 per-case CAP and none stopped. | **1.268×** (457.0753 / 360.4; against the unrounded 360.3855 it is 1.2683). Per case **1.2683×** (38.0896 / 30.0321). **82.925 core-min UNDER the 540.0 subset CAP — 84.6 % of it consumed. No overrun.** | **UNIFORM RATE MISPREDICTION. THE WORD "CONTENTION" IS NOT USED AS THE ATTRIBUTION, AND THE TWO READINGS THAT WOULD HAVE SUPPORTED IT ARE REJECTED HERE BY NAME.** The miss is uniform, not per-case: the spread across the twelve is **4.90 %** (2235.52 to 2345.12 s) while every one of them missed its POINT by essentially the same 1.27×. **NOT OFFERED AS EVIDENCE (1): `loadavg`.** `T24_CONCURRENCY_CONTENTION_NOTE.md` §4(a) measures it overstating by roughly a factor of two on this box — 24.47 read on 16 cores at the instant 11.9 cores were genuinely consumed — so a gap sized from `loadavg` is not sized from anything. **NOT OFFERED AS EVIDENCE (2): the 4.90 % within-batch spread.** All twelve ran at the SAME concurrency, so a within-batch spread cannot size a between-concurrency effect; a uniform shift is exactly what it would fail to show. **WHAT THE MISPREDICTION ACTUALLY IS, MEASURED AT MATCHED ITERATION BANDS:** the registered per-case rate was measured on T23 at **four**-concurrent and these twelve executed at **twelve**-concurrent. Two independent matched-band probes in `T24_CONCURRENCY_CONTENTION_NOTE.md` §2 size that difference at **26.7 % to 29.9 %** more wall time per iteration on identical geometry, mesh, solver and iteration count — Probe A 4.215 iter/s over five simultaneous cases against T23's 6.012 and 5.753 in the matched bands, Probe B 4.421 against 6.272 and 4.874 / 4.879 against 6.012 from each case's own `ExecutionTime` trace. **The observed overrun is +26.8 %, inside that measured bracket.** The mechanism is memory-bandwidth and shared-cache stall, not core starvation: `cpu_s / wall_s` per solver 0.9963–0.9974, zero processes in D-state, 27.3 GB `MemAvailable` (note §3). **TRANSFERABLE CALIBRATION FINDING: a per-case rate measured at one concurrency does not transfer to another, and a cap calibrated at low concurrency under-provisions at high concurrency. Price the POINT at the concurrency the run will actually use, or state the exposure.** `T23_RESULTS.md` §6 had already measured that a borrowed per-cell-iteration rate survives a change of dimensionality and not a change of geometry family; this adds a third axis, and the answer is again negative. | registration `docs/campaigns/T-family/T24_PREREGISTRATION.md` §5.2 (`b9057489`); per-case actuals `verification/runs/T-family/T24_runs/gate_t24.json` (`rows.*.execution_time_s`, `rows.*.core_min`); solver logs `verification/runs/T-family/T24_runs/T24_P*/log.solve`; completion markers `verification/runs/T-family/T24_runs/DONE.T24_*`; the matched-band measurement `verification/runs/T-family/T24_runs/T24_CONCURRENCY_CONTENTION_NOTE.md`; four-concurrent reference `verification/runs/T-family/T23_runs/T23_P305_U10/log.solve` |
| C-20260903T184704.252760Z-954dd6db | 2026-09-03 | heat-transfer | **T15 — UNSTEADY axisymmetric turbulent plume, `T15_UP_f`.** `buoyantBoussinesqPimpleFoam`, RAS kEpsilon URANS on T8's fine mesh (102,400 cells), **1 rank**, 24,000 fixed timesteps to `endTime` 240 s, 2026-08-27T08:57:37Z to 2026-08-28T04:53:26Z. Run COMPLETE by all six clauses of standing rule 4 (`DONE.T15_UP_f` written by `mark_done_t15.py`). **THIS ROW RECORDS COST ONLY. T15 CARRIES NO ROW VERDICT:** the frozen comparator `analyse_t15.py` REFUSED at exit 2 in the S1 planted-zero constant-offset arm before `apply_gate` was reached, so S1, V1, V2 and V3 are UNGRADED. The compute was spent and is costed here; the science is ungraded and must not be reported as graded. **Supersedes nothing — this is the first landing of the row drafted at `T15_runs/COST_CALIBRATION_ROW_DRAFT.txt` on 2026-08-28, whose figures this lane re-measured from the artifacts rather than transcribing.** | **1307 core-min** POINT (`T15_registered.json` `cost.point_core_min`), CAP **2600** core-min hard, `timeout` **156,000 s**. **$1.1175 DERIVED, not measured** at $0.0513/core-h. | **1195.8167 core-min MEASURED**, WALL BASIS: `STATUS.T15_UP_f` `wall_s=71749`, `ranks=1`, 71749 × 1 ÷ 60. **Cross-checked independently by this lane against the same file's own clock fields:** `started_utc` 2026-08-27T08:57:37Z to `ended_utc` 2026-08-28T04:53:26Z is 71,749 s exactly, so `wall_s` is not a transcription. SOLVER BASIS reported beside it and not substituted for it: the log's own last `ExecutionTime = 70751.29 s` gives **1179.1882 core-min**. **$1.02242 DERIVED, not measured**; `cost_basis` **reported-by-owner** (`COMPUTE_BUDGET_CHARTER` §5). | **GROSS IS THE PUBLISHED FIGURE AND NO CLEANED FIGURE IS PUBLISHED. The reason is NAMED, not judged.** The stall rule (`COMPUTE_BUDGET_CHARTER` §2; the single rule in `scripts/self_audit.py`) matches any ledger row over 3600 wall s. Read literally against this one-row STATUS it would clean a **registered** 71,749 s transient — capped at 156,000 s BEFORE compute, and 46.0 % of that cap — down to zero. Its operand is a per-arm run-root ledger row, not a single registered long transient. **REFERRED to the supervisor and to verification as a rule-scope question; this lane does not reinterpret a standard.** **Waste 0.000 core-min, NAMED SEPARATELY:** no failed run, no re-run, no capped arm. | **0.9149×** (1195.8167 / 1307) on the wall basis; **0.9022×** on the solver-only `ExecutionTime` basis. **46.0 % of the 2600 core-min CAP and 46.0 % of the 156,000 s timeout. No overrun; the miss is an UNDER-spend.** | **MISPREDICTION IN THE CONSERVATIVE DIRECTION, TRACED TO THE PROBE'S SAMPLING WINDOW. NO CONTENTION IS CLAIMED AND NONE IS MEASURED HERE.** The POINT came from a scratch probe measuring **3.267 core-s per step over steps 6 to 20** (`T15_registered.json` `cost.rate_scratch_source`); 24,000 × 3.267 ÷ 60 = 1306.8, which is the registered 1307. Measured over the whole run: **2.98954 core-s per step** wall basis (71749 / 24000), **2.94797** solver basis (70751.29 / 24000). The probe therefore over-priced the settled per-step rate by **9.28 %** on the wall basis and **10.82 %** on the solver basis. **ATTRIBUTION: STEPS 6 TO 20 ARE START-UP.** The registration's own P5 records the scratch Courant at 1.053 at start-up decaying through 0.72 by t = 0.12 s, so the probe sampled precisely the regime where PIMPLE does the most corrector work, and the settled regime is cheaper per step. **TRANSFERABLE CALIBRATION FINDING: a per-step rate for a fixed-step transient must be probed AFTER the start-up transient has decayed, or it will over-price the run by roughly 10 percent; a 20-step probe beginning at step 6 cannot see the settled regime at all.** **SECOND TRANSFERABLE FIGURE, confirming ONE cause rather than two:** measured **2.91947e-05 core-s per cell-step** against T8's MEASURED SIMPLE rate of **6.397e-06** core-s per cell-iteration gives a PIMPLE/SIMPLE ratio of **4.5638**, against the registered `cost.pimple_over_simple_ratio_measured` of **4.99** — the registered ratio was **9.34 % high**, the same size and sign as the per-step miss, which is consistent with a single start-up bias and not with two independent errors. **NAMED SEPARATELY AND LAUNDERED INTO NEITHER THE RATIO NOR THE WASTE COLUMN: 16.6285 core-min, 1.391 % of gross, is wall minus `ExecutionTime`** — mesh and field I/O, `writeCellCentres` / `writeCellVolumes`, solver start-up. This is NOT waste (it bought the `0/Cx`, `0/Cz`, `0/V` that the C_GEOM control reads, and the five written time directories) and NOT a stall; it is the wall-basis / solver-basis gap, stated so a reader can choose a basis. | registration `docs/campaigns/T-family/T15_PREREGISTRATION.md` §5.2 and `verification/runs/T-family/T15_runs/T15_registered.json` (`cost.*`); actual `verification/runs/T-family/T15_runs/STATUS.T15_UP_f`; solver log `verification/runs/T-family/T15_runs/T15_UP_f/log.solve`; completion marker `verification/runs/T-family/T15_runs/DONE.T15_UP_f`; the grader's refusal captured at `verification/runs/T-family/T15_runs/T15_GRADE_OUTPUT.txt`; the refusing predicate read and MEASURED at `docs/campaigns/T-family/GATE_PREDICATE_SATISFIABILITY_READ_2026-09-03.md` §A.2 (`78e9c311`); parent rate `verification/runs/T-family/T8_runs/STATUS.T8_MTT_f`; prior unlanded draft `verification/runs/T-family/T15_runs/COST_CALIBRATION_ROW_DRAFT.txt` |
| C-20260903T185415.611316Z-2329f606 | 2026-09-03 | cfd | **RUNG 0 — THE WRITER LIMB: `cases/committee-grids/foam_to_ugrid.py` BUILT AND RUN, closing the ONE unbuilt item of the predecessor's §5.1.** **SUCCESSOR TO THIS LEDGER'S ATTEMPT-3 ROW — the `cfd` / `RUNG0_MESH_IMPORT` row of 2026-09-03 carrying predicted 7.5 and actual 2.5500 core-min — WHICH IT DOES NOT CORRECT AND DOES NOT SUPERSEDE** — that row costed attempt 3, which ran three of the registration's five line items; this row costs the other two, so that between them the whole of §5.1 finally has a measured actual. Work: `foam_to_ugrid.py` built (commit `d1c5aa5d`, 21 planted controls, all firing, exit 0 under `python3 -O` as well), then `--roundtrip` run on all four committee grids, then the frozen predecessor comparator re-run to establish — measured, not inferred — that it still emits `PENDING`. **R0-G2b measured `PASS` 4/4 with its C9 plant firing 4/4. THAT IS A MEASUREMENT AND NOT A VERDICT**: the instrument sits outside any frozen grading path, and it becomes a verdict only when `RUNG0b`'s own frozen comparator emits it. **The rung verdict on disk is UNCHANGED and remains `PENDING`.** | **2.302 core-min raw = $0.001968 DERIVED** — NOT a fresh estimate but the predecessor's own §5.1 figure for exactly these two line items, `foam_to_ugrid.py` export of four meshes **1.535** + independent reader on the exported side **0.767**, frozen at `d127d83d` and **pre-declared as structurally unrunnable BEFORE attempt 3 ran** (queue entry `cost_note_the_denominator_is_fixed_in_advance`). Using the predecessor's own frozen number as this row's denominator is deliberate: a fresh estimate written after the work would be unfalsifiable. **The 23.0 core-min HARD CAP of the item is UNCHANGED, NOT RESET BY THIS WORK AND NOT RAISED** (rule 12). | **1.1347 core-min MEASURED = $0.000970 DERIVED** for the registered scope — 68.08 wall s × 1 rank ÷ 60, the four `--roundtrip` invocations, from the four `/usr/bin/time -v` artifacts at `/home/ubuntu/certonomous-runs/rung0-r0g2b-export/*.time.txt` (10.87 / 16.61 / 25.94 / 14.66 s). **Serial throughout (ranks = 1, so core-min = wall-min on every figure in this row).** GROSS: contention is not separated out; none was measured. **PLUS 0.4924 core-min MEASURED that is NOT registered scope and is NOT in the ratio** — the frozen-comparator re-run 0.4734 (28.405 s, bracketed on the wall clock) and the writer's selftests 0.019 (0.19 s per run measured by `/usr/bin/time -f`, **the run count is this lane's own count and is not a logged figure**) — both charged to §5.2's **named DEVELOPMENT allowance**, which the registration states in terms is *"a DEVELOPMENT allowance and is named as one, not disguised as solve cost."* **Total productive 1.627 core-min.** | **= gross, 1.1347 core-min.** The 3,600-s stall rule matches nothing: the **largest single wall in this row is 25.94 s** (the DPW5 hybrid round trip), two orders of magnitude inside it. No cleaning was applied and none was available. **WASTE IS NOT IN THIS COLUMN** — see the gap cell. | **0.493** (1.1347 / 2.302), registered scope against the predecessor's own frozen figure for it. **AND THE ROW THAT MATTERS IS THE ONE THIS FINALLY MAKES POSSIBLE: §5.1 CAN NOW BE SCORED LINE BY LINE, ALL FIVE ITEMS, FOR THE FIRST TIME.** Registered raw → measured actual → ratio: **import 4 grids 1.535 → 1.350 → 0.879**; **`checkMesh` × 4 1.535 → 0.500 → 0.326**; **reader source side 0.767 → 0.694 → 0.904**; **export + reader exported side 2.302 → 1.1347 → 0.493**. Raw subtotal **6.139 → 3.679 → 0.599**; the +22 % contention band **1.361 → 0.000**, an insurance line that did not pay out. **AGAINST THE FULL REGISTERED 7.5 THE WHOLE OF §5.1 CAME IN AT 0.491.** **THAT SUPERSEDES NOTHING IN `C-…49087129` AND CORRECTS NOTHING IN IT** — that row's headline 0.340 was the honest figure for a run in which two of five items could not execute, and it said so and pre-declared the shortfall. **0.491 is the number for the registered scope once all of it actually ran, and the two figures are answers to two different questions.** | **ATTRIBUTED, AND THE ATTRIBUTION IS THE POINT OF THE ROW. THE PATTERN IS IN THE *BASIS OF THE ESTIMATE*, NOT IN THE WORK.** §5.1 priced **one** line from a **MEASUREMENT** — the converter at **0.203 core-min/Mcell**, from `cases/committee-grids/measurements.jsonl` — and priced the other **three BY ANALOGY** to that same rate (`checkMesh` "allowed at 1× the converter rate", export "1× the converter rate", both readers "0.5×"). **THE MEASURED LINE CAME IN AT 0.879 — ACCURATE TO 12 %, WELL INSIDE ITS OWN REGISTERED ±22 % BAND. EVERY LINE PRICED BY ANALOGY CAME IN BETWEEN 0.33 AND 0.63.** `checkMesh` 0.326 (measured **0.0661 core-min/Mcell**, one third of the converter rate, not one times it); the export + exported-reader limb 0.493. **⚠ THE SOURCE-SIDE READER'S APPARENT 0.904 IS AN ARTEFACT OF ATTRIBUTION AND IS DISCLOSED RATHER THAN BANKED:** its 0.694 actual includes the **eleven planted controls (0.207 core-min)**, which are not part of that line item at all; **on the line item alone — the grading pass, 0.486 — it is 0.634**, and all three analogies then sit in a band of 0.33–0.63. **THE LESSON FOR THE LAB'S ESTIMATES, AND IT IS GENERAL RATHER THAN ABOUT MESHES: pricing a step from a MEASUREMENT of that same step on that same population was accurate to 12 %; pricing a step BY ANALOGY to another step's measured rate over-predicted by 1.6× to 3.1×. An analogy is not a measurement, and a registration that mixes them should say which lines are which.** The successor registration `verification/campaign/RUNG0b_MESH_IMPORT_PREREGISTRATION.md` §5.1 (frozen `ace20cb1`) acts on this directly: **every one of its four lines is priced from a measurement taken on this same population on this same day**, and its ratio at completion is a direct test of whether that method beats the analogies. Specific replacement rate for the next mesh registration: **`checkMesh` on committee grids is ~0.066 core-min/Mcell, roughly ⅓ of the converter rate, not 1×.** **WASTE — SEPARATELY NAMED, NEVER LAUNDERED INTO EITHER ACTUAL COLUMN OR INTO THE RATIO (`COMPUTE_BUDGET_CHARTER.md` §6): 0.4918 core-min = $0.000420 DERIVED**, the FIRST round-trip pass over all four grids (29.51 s: 4.40 / 6.74 / 10.77 / 7.60 s). **It was voided by a defect in THIS LANE'S OWN WRITER, not by anything about the meshes**: the writer's first version copied `ugrid_to_foam.py`'s `PYR_T`/`PYR_Q`, which are **dead code** in that file (defined at its `:150-151`, never passed to `add()`; the live pyramid path is `:203-214`, apex at local 2, base wound `[0,3,4,1]`). `verify_elements` **REFUSED on 962,824 regenerated triangle rows** and stopped the export rather than writing a different mesh wearing the right counts. **IT REFUSED ON HLPW6 ALONE, BECAUSE HLPW6 IS THE ONLY ONE OF THE FOUR GRIDS THAT CONTAINS A SINGLE PYRAMID** — measured element census: DPW5 hex 638,976 hexes / prism 1,277,952 prisms / hybrid 2,555,904 tets + 425,984 prisms, **not one pyramid among them**; HLPW6 341,341. A writer validated on the three DPW5 grids would have shipped it, and that is the **third** time this rung has been bitten by a population that does not contain the failure mode. **NO CONTENTION IS CLAIMED AS A CAUSE ANYWHERE IN THIS ROW: none was measured.** **CUMULATIVE AGAINST THE ITEM'S 23.0 CAP, so the accounting is complete rather than flattering: 10.492 core-min, 45.6 %** — attempt 1 ~0.000, attempt 2 1.7500 (waste), attempt 3 2.5500, this row 2.119, and a further **4.073 core-min** for the `VERIFICATION_CHARTER.md` §2d.1 condition-(2) control (2.073 productive + 2.000 waste, a first invocation killed by this lane's own 120 s command timeout — an operator misjudgement, not a defect in the control), which is **governance work and belongs to none of §5.1's five line items**, recorded at `verification/runs/RUNG0_MESH_IMPORT_runs/CONTROL_2d1_CONDITION2_NOT_A_GRADED_RUN.json`. | **Writer** `cases/committee-grids/foam_to_ugrid.py` (`d1c5aa5d`). **Round-trip readings and the C9 plant, per grid** `verification/runs/RUNG0_MESH_IMPORT_runs/R0G2B_DIAGNOSTIC_NOT_A_GRADED_RUN.json` (`589bacd6`). **Timing artifacts** `/home/ubuntu/certonomous-runs/rung0-r0g2b-export/{DPW5_L1T_hex,DPW5_L1T_prism,DPW5_L1T_hybrid,HLPW6_h6c1_rans_3a_1}.time.txt` (`/usr/bin/time -v`, wall and peak RSS per grid). **The predecessor's registered figures** `verification/campaign/RUNG0_MESH_IMPORT_PREREGISTRATION.md` §5.1/§5.2, frozen `d127d83d`, and `verification/queue/cfd/launched/RUNG0_MESH_IMPORT.json`. **Predecessor calibration row**: this ledger's `cfd` / `RUNG0_MESH_IMPORT` row of 2026-09-03 (predicted 7.5, actual 2.5500, ratio 0.340) — **cited, not corrected**. ⚠ **ITS TOOL-ALLOCATED ID IS DELIBERATELY NOT WRITTEN OUT HERE AND THAT IS NOT AN OVERSIGHT:** `scripts/append_record.py` REFUSED this row (exit 9) while it did, because hand-writing a tool-allocated id anywhere but a structured `corrects:` field is the `C-217`/`L-404` duplicate mechanism in new clothes, and a structured `corrects:` would have been a lie — **this row corrects nothing.** The guard is right and was obeyed rather than routed around. The id is written out in full, resolvably, at `verification/campaign/RUNG0b_MESH_IMPORT_PREREGISTRATION.md` §11, which is not governed by this ledger's plumbing freeze. **The §2d.1 control** `verification/runs/RUNG0_MESH_IMPORT_runs/CONTROL_2d1_CONDITION2_NOT_A_GRADED_RUN.json` (`89de4710`). **The framing correction this lane owed** `verification/runs/RUNG0_MESH_IMPORT_runs/CORRECTION_no_pass_branch_framing.json` (`51b0efac`). **Verification's ruling** `f5b8deec`, charter v1.52. **Successor registration, priced from these measured rates** `verification/campaign/RUNG0b_MESH_IMPORT_PREREGISTRATION.md`, frozen `ace20cb1`. **Envelope row for the same spend** in `docs/campaigns/IBL-industrial-benchmark-ladder/IBL_COMPUTE_ENVELOPE_LEDGER.md`. **Dollars throughout are DERIVED, NOT MEASURED**, at the owner-stated c7a.4xlarge $0.0513/core-h — the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` §5). **A THIRD ROW IS OWED** at `RUNG0b`'s completion, against ITS registered 4.4 core-min and 13.2 cap, and it must cite this row and the attempt-3 row above it. |
| C-20260903T185443.680924Z-b22cc77f | 2026-09-03 | heat-transfer | **T25RF — the numerics feasibility probe for the resolved-channel conjugate case, CLOSED 2026-09-03 by `verification/runs/T-family/T25RF_runs/T25RF_PROBE_CLOSURE.md`. Four arms `A0 A1 A2 A2T`; `A3` (`frozenFlow`) NOT REACHED and NOT RUN.** **NO VERDICT IS ASSERTED IN THIS ROW AND NONE EXISTS.** T25RF is an UNGATED FEASIBILITY rung under `VERIFICATION_CHARTER.md` §2m — no gate, no threshold, no band, no label — and none of PASS / GATE REACHED / GATE FAIL / NOT A RESULT applies to it; each arm's own `STATUS.<ARM>` carries `gated=no` and `verdict=NONE_THIS_RUNG_IS_UNGATED`, written at run time. **THE ROW IS FILED LATE AND SAYS SO:** the arms ran 2026-09-01 04:33–04:50Z and rule 12 requires the comparison at process completion; the probe's own estimate-versus-actual analysis was written the same day into `docs/campaigns/T-family/T25RF_FEASIBILITY_NOTE.md` Addendum A2 §5, but **no ledger row was ever filed**, and this discharges that debt. `chtMultiRegionFoam`, OpenFOAM v2606, the already-built `T25R_L1` two-region mesh (16 608 cells) copied rather than rebuilt, `deltaT` 0.5, `endTime` 30 s = 60 steps, **serial (RANKS = 1, so core-min = wall-min on every figure in this row)**. Rule 4 re-measured today on the three surviving arms: `A1`/`A2`/`A2T` each `rc=0` (captured INSIDE the detached wrapper, never around the `setsid` line, and cross-read from `.rc.<ARM>`), `End` present, last `Time = 30` == `endTime`, 60 `ExecutionTime` lines, fields at `30/`, **age guard PASS against `0/module/T`** — the region-scoped dating file this case's wrapper touches last, not a bare `0/T`. `A0` is `rc=134`, no `End`, dead at `Time = 1.5` after 3 of 60 steps — **that arm's failure IS its observation and is not a defect in the row.** | **5.9 core-min TOTAL [DERIVED, NOT MEASURED]**, as two separately-frozen predictions summed: **1.9** for `A0+A1+A2` from `T25RF_FEASIBILITY_NOTE.md` §5 (A0 "≤ 0.5", A1 ~0.5, A2 ~0.9), priced off `T25R`'s registered L1 rate of 0.00786 core-min/step; plus **~4** for `A2T` from Addendum A1, registered before that arm ran. Registered probe **CAP 30.0 core-min for all arms together**, with per-arm `timeout` 480–600 s and the running total checked against the cap before each launch. **ONE DEFECT IN THE PREDICTION ITSELF, NAMED: §5 priced `A0` as an INEQUALITY ("≤ 0.5"), which L-463 forbids — a cost prediction is a POINT.** The 1.9 denominator takes it as 0.5, the reading most favourable to the predictor, and the ratio is reported against that. | **12.217 core-min GROSS [MEASURED]** = A0 0.083 + A1 3.650 + A2 7.967 + A2T 0.517, re-summed today from `verification/runs/T-family/T25RF_runs/COST_LEDGER.txt` and cross-read from each arm's `STATUS.<ARM>` `core_min` field — **the two sources agree exactly**. = 0.20362 core-h ≈ **$0.010446 DERIVED, NOT MEASURED** at the owner-stated c7a.4xlarge $0.0513/core-h, `cost_basis` **REPORTED-BY-OWNER** — the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` §5). | **12.217 core-min — GROSS AND CLEANED ARE IDENTICAL, stated rather than assumed.** The charter §2 stall rule matches a row over 3 600 wall s; the longest arm is `A2` at 478 wall s, so the rule matches nothing and no judgement enters the figure. **Cap utilisation 40.7 % of the registered 30.0; no arm was cap-stopped and the cap stopped nothing.** | **2.07 overall.** Reported split, because the two halves calibrate opposite ways and averaging them would hide both: **`A0+A1+A2` 11.700 / 1.9 = 6.16**; **`A2T` 0.517 / ~4 = 0.129**. | **MISPREDICTION, AND THE WHOLE OF IT IS ONE MECHANISM — the coolant `p_rgh` GAMG STALL — WHICH IS NAMED HERE AS WASTE AND IS NOT ABSORBED INTO THE RATIO** (rule 12, `COMPUTE_BUDGET_CHARTER.md` §6). The `T25R` cost model priced "5 SIMPLE-equivalent outer iterations per step" and did not represent the stall. MEASURED: GAMG stalls at ~4.4e-9 against the registered absolute `tolerance` of 1e-9, so **266 of 600 `p_rgh` solves in `A1` and 608 of 1200 in `A2` terminated at `maxIter` 1000 without converging**, and the waste GROWS as the field settles — `A2`'s cost per step rises 1.60 s (steps 1–20) → 11.09 s (steps 21–60). Relaxing the tolerance one digit to 1e-8 (`A2T`, registered in Addendum A1 BEFORE it ran, with its acceptance criterion "`Min/max T` at `Time = 30` must agree with `A2`'s to within 1e-4 K" fixed in advance): **`maxIter` terminations 608 → 0; total GAMG iterations 627 533 → 25 770, a 24.4× reduction; and `Min/max T` 292.985283351 / 294.125534239 K in BOTH arms, agreeing to 0.000e+00 K.** **Those 601 763 discarded iterations changed no digit of the answer.** **REMOVABLE WASTE, MEASURED: 7.450 core-min** = `A2` 7.967 − `A2T` 0.517, the same physics with the stall removed. **`A1`'s stall fraction is NOT separately measured and is stated as ABSENT rather than estimated** — no `A1T` arm was run. **CONTENTION IS NOT AVAILABLE AS AN EXCUSE AND IS NOT OFFERED AS ONE:** the GAMG iteration counts are read from the logs and are independent of machine load, and they alone account for the gap. **The transferable finding, offered as a measurement and NOT as a rule: the per-step cost model was SOUND and was missing only the stall** — `A2T`, the same physics stall-free, came in at **0.86× the §5 per-step prediction**. A registered solver tolerance that the linear solver cannot reach is a cost bug that a cost model priced off a different case will never see. | `verification/runs/T-family/T25RF_runs/T25RF_PROBE_CLOSURE.md` (this closure, 2026-09-03); `docs/campaigns/T-family/T25RF_FEASIBILITY_NOTE.md` §5, Addendum A1 and Addendum A2 §§4–6 (verified today byte-identical to its committed blob); `verification/runs/T-family/T25RF_runs/COST_LEDGER.txt`; per-arm `verification/runs/T-family/T25RF_runs/{A0,A1,A2,A2T}/{log.solve,STATUS.<ARM>,.rc.<ARM>}`; reader `verification/runs/T-family/T25RF_runs/read_arm_t25RF.py`. **NOT CLAIMED: that any of this grades anything.** T25RF closes no gate, starts no freeze clock, consumes no registration's first-compute event, and a future `T25R2` that wants a verdict freezes its own pre-registration and RUNS AGAIN. |
| C-20260903T191002.936505Z-8cb93821 | 2026-09-03 | heat-transfer | **T25R6c-R2 — the leg-A/leg-B wall-cost ratio rung, GRADED `GATE FAIL` (rho = 1.103859 >= 1.0).** Case `W1150_C4_L1`, 40 leg-A steps at deltaT 0.02 plus 1110 leg-B steps at deltaT 0.1, 2 ranks, C4 arm, L1 mesh. | **14.707 core-min** — a POINT with a stated basis, never an inequality (L-463). Cap **45.0 core-min** (3.06x, the team's own, enforced by per-leg `timeout` 60 s / 1290 s with `(60+1290)*2/60 = 45.0` asserted at pre-flight). | **15.0993 core-min** gross = 452.98 cpu s x 2 ranks / 60, RE-MEASURED from `W1150_C4_L1/log.solve.{legA,legB}` for this row rather than transcribed (legA 14.35 + legB 438.63 cpu s). $0.012910 **DERIVED, NOT MEASURED**, at the owner-stated $0.0513/core-h; `cost_basis` **REPORTED-BY-OWNER** — this box cannot read its own billing (`COMPUTE_BUDGET_CHARTER` §5). | **15.0993 core-min — gross and cleaned are THE SAME FIGURE, and that is a measurement, not an omission.** No row exceeded 3600 wall s (leg B ran 443 ClockTime s under a 1290 s timeout), no leg was restarted, no attempt was discarded, and **no waste was incurred: there is none to name separately** (`COMPUTE_BUDGET_CHARTER` §6). | **1.0267** | **MISPREDICTION, AND A SMALL ONE — NOT contention and NOT waste, which stay separately named and are nil here.** The point was derived from T25R6c's OWN measured per-step rates on the SAME case, arm dictionary, mesh, rank count and box, so it carried no arm mismatch — unlike its predecessor T25R6c, whose point was priced from B0_L1's GAMG rate while the case ran C4_L1's PCG arm and which therefore came in at ratio ~0.10. **THE CORROBORATION THAT LICENSED THE METHOD IS LEG A: 14.35 cpu s here against T25R6c's 14.36 s on the byte-identical 40-step window — a 0.07 % reproduction across two runs three hours apart.** The residual 2.67 % is the box's own throughput drift, which this rung MEASURED at 2.4297 % on a 40-step window (against T25R6c's 2.915 %) **at constant solver work** — 92..94 linear-solver iterations/step over all 1070 graded steps, a 2.150 % integer spread. Identical work, different cost: the miss is the machine, and it is the same size as the machine. **LESSON FOR THE LAB'S ESTIMATES, and it is the actionable half of this row: pricing a rung from a MEASUREMENT OF THAT SAME CASE landed at 1.027x, while pricing the predecessor BY ANALOGY to another arm's rate landed at ~0.10x — a 10x error in the other direction. This is L-468's finding reproduced on a second, independent pair, and it says the lab should prefer a same-case measured rate over any analogy whenever one exists.** | `docs/campaigns/T-family/T25R6cR2_PREREGISTRATION.md` §6 (frozen `f67ade8d`); verdict `verification/runs/T-family/T25R6cR2_LEGAB_runs/T25R6cR2_GRADE_STDOUT.txt` (`286276a1`); completion `DONE.W1150_C4_L1` (`23eef25d`); cost derivation `T25R6cR2_LEGAB_runs/DESIGN_DERIVATION.json`. **CAVEAT CARRIED, NOT BURIED: `core-min = ExecutionTime x 2 / 60` assumes rank 1 burns rank 0's cpu. `ExecutionTime` is rank 0's CPU time only (OpenFOAM-v2606 `TimeIO.C:631` -> `cpuTimePosix` -> `times(2)`, which reports the calling process alone) and CANNOT SEE rank 1. T25R6c used the identical basis, so the two rungs stay comparable, but the multiplier is an ASSUMPTION and is named as one.** |
| C-20260903T193337.073791Z-eb712367 | 2026-09-03 | dafoam | **CORRECTION ROW — it corrects the VERDICT CELL of corrects:[C-20260831T164216.179599Z-19ea8c16] and nothing else.** Append rule 1: an existing row is never edited; a correction is a new row naming the row it corrects. **THE DEFECT: that row records `D4S-F3SR`'s ITEM VERDICT as `GATE FAIL` and states that "the grader composes no item verdict". Both are wrong.** The frozen grader DOES compose one, at `d4s_f3sr_grade.py:796-806` under its own comment *item composition (PREREGISTRATION.md section 5)*, and it emitted **`verdicts.ITEM_two_row_endpoint_fd` = `PASS`** [MEASURED, `/home/ubuntu/certonomous-runs/CURRICULUM-D4S-F3SR-a2-wing-cdmin/d4s_f3sr_grade_20260831T154117Z.json`, `grader_md5` `9596c7bf711a934313a9b4d5801481c6`]. **THE CORRECTED VERDICT IS `PASS`.** Section 5's registered composition, frozen `ed9cda9080080b1f5f450ee3202a6d64ddb76d49` four days before the run, reads: *`NOT A RESULT` if any arm fails G1 or G-ACC, any control fails, any OOM, or any row's G5 is `NOT A RESULT`; else `PASS` iff both rows' G5 `PASS`; else `GATE FAIL`* — **`G12` appears nowhere in it**, and the grader implements it exactly. `G1`, `G_ACC`, `G6`, `G6b` and `G7` are all true on both arms, `arms_oomkilled` is empty, and both `G5` are `PASS` (aggregate 0.350109 % and 0.163445 % of a 5 % band). **THE FALSE PREMISE, AND WHY IT IS FALSE:** the measurement behind it is TRUE — there is no *top-level* `verdict`, `item_verdict` or `rows` key — but the item verdict lives INSIDE `verdicts`. A search looked for three key names, did not find them, and published the absence as proof of non-existence rather than as *"I did not look where it lives"* — rule 3's planted zero with a `[MEASURED]` tag on it, which is what made it credible. **`G12_cpu_placement_F-P` REMAINS `GATE FAIL`** and is not diminished by this correction; pre-registration simply excludes it from composition | **n/a — a correction row records no estimate of its own.** The corrected row's **75.862 core-min REGISTERED** stands unchanged | **n/a — no compute was consumed by this row and none is attributed to it.** The corrected row's **77.934 core-min GROSS** stands unchanged | **n/a.** The corrected row's **cleaned = gross = 77.934 core-min** stands unchanged | **NO RATIO — and this is the load-bearing statement of this row: NOTHING IN THIS CORRECTION TOUCHES A CORE-MINUTE.** The corrected row's **1.0273** cleaned/registered stands unchanged, as does its finding that the aggregate is **two offsetting arm-level misses** (`F-S` 0.798×, `F-P` 1.257×) and not two accurate ones, and its **waste 0.000**. The defect was confined to the verdict cell and to the sentence about the grader | **MECHANISM, AND IT IS TRANSFERABLE: A VERDICT COMPOSED BY HAND FROM GATES THE REGISTRATION EXCLUDES IS NOT A STRICTER READING OF THE RULE — IT IS A DIFFERENT RULE.** The grading path is fixed at the pre-registration commit (`CLAUDE.md` rule 2), so no hand may be substituted for the frozen composer. **And the one override direction the lab permits is the other one:** rule 5 lets a gate turn a `PASS` or `GATE FAIL` **into** `NOT A RESULT`, never the reverse — nothing anywhere authorises a hand-composed `PASS` → `GATE FAIL`, so the hand-composition was unsanctioned twice over. **STATED BECAUSE IT IS UNCOMFORTABLE: this correction moves a verdict from harsher to softer and was ruled by the same role that composed the harsher one — which is exactly why the authority is the grader's own emitted value and not anyone's judgement. It removes a discretion that was never authorised; it does not exercise a new one. A hand-composed verdict is the defect whichever way it leans, and a false `GATE FAIL` is not the safe error: it discards a result the lab paid 77.934 core-min for and makes the record wrong in the direction readers trust least to be checked.** Honesty is carried by the value, not by severity | Corrects the verdict cell of corrects:[C-20260831T164216.179599Z-19ea8c16] in this file; **that row is not edited and stands as filed**, with this row as its correction. Authority: `verdicts.ITEM_two_row_endpoint_fd` in `/home/ubuntu/certonomous-runs/CURRICULUM-D4S-F3SR-a2-wing-cdmin/d4s_f3sr_grade_20260831T154117Z.json`; registered composition at `cases/dafoam/ladder-a/A2/curriculum_D4_SHIPPED_F3SR/PREREGISTRATION.md` §5, frozen `ed9cda9080080b1f5f450ee3202a6d64ddb76d49`. The item's own record was corrected in place at commit `2b3bfd5b852109ed5aa3924d8f6dea68cb5838ba` — `RESULTS.md` **CORRECTION 1**, where the original heading is **struck and preserved verbatim, never rewritten**, and **CORRECTION 2**, which discloses for the first time that the item ran with `primalMinResTolDiff` DISARMED to `1.0e12`. The predecessor `D4S-F3S`'s own results record was landed at `b73f964d` |
| C-20260903T194908.449347Z-c79014f7 | 2026-09-03 | cfd | **RUNG 0b `RUNG0b_MESH_IMPORT` — THE SUCCESSOR RUN, GRADED `PASS`.** Section 4's conjunction holds on **all four** committee grids across **all five** gates, **R0-G2b included and graded from this registration's own frozen path** rather than inherited from the 2026-09-03 out-of-band measurement. 15/15 planted controls fired — phase A 8/8 before any conversion, phase B 3/3, and **B9 4/4**, the round-trip plant the predecessor recorded as UNBUILT. Registration frozen `ace20cb1`, Amendment 1 `0b6cb24f`, notes `75ad7ef9` / `86457521`; record `33b77af5`. Strict completion verified clause by clause, all-or-nothing. | **4.4 core-min = $0.003762 DERIVED**, registered at §5.1, frozen `ace20cb1` **before first compute**. HARD CAP **13.2**; fleet ceiling **39.6** (§5.3, second term explicitly UNAVAILABLE and non-binding). `decomposition seed: none (identity)`. **THE FIRST REGISTRATION IN THIS FAMILY PRICED ENTIRELY FROM MEASURED RATES** — every one of its four lines from a measurement of that same step on that same population, taken the same day. | **4.4333 core-min MEASURED = $0.003791 DERIVED** — 266 wall s × 1 rank ÷ 60 from `verification/runs/RUNG0b_MESH_IMPORT_runs/STATUS.RUNG0b_MESH_IMPORT`. Serial throughout (ranks = 1). Phase split from `TIMING.json` and the driver trace: phase-A controls **1 s**, convert **97 s**, `checkMesh` **30 s**, grading incl. the four round trips **138 s**. | **= gross.** Longest single wall is 36 s (HLPW6 convert), two orders inside the 3,600-s stall rule. **NO WASTE. NO FAILED ATTEMPT. NO RETRY.** The item ran once, cleanly, first time — the first in this rung's history to do so. | **1.008** (4.4333 / 4.4). **THE CENTRAL RESULT OF THIS ROW IS THAT `L-468`'s THESIS IS NOW TESTED RATHER THAN ASSERTED.** The predecessor's analogy-priced estimate scored **0.491** on this same population; this measured-basis estimate scored **1.008** — inside **0.8 %**. Line by line, registered → actual → ratio: phase-A controls 0.009 → 0.0167 → 1.85 (0.008 core-min absolute, noise); convert 1.350 → 1.6167 → **1.198**; `checkMesh` 0.500 → 0.5000 → **1.000, EXACT**; grading 1.680 → 2.300 → **1.369**. | **ATTRIBUTED, AND THE HEADLINE 1.008 HIDES SOMETHING THE SPLIT SHOWS: THE RAW SUBTOTAL OVER-RAN BY 25.3 % AND THE CONTENTION BAND ABSORBED IT.** Raw registered 3.539 against 4.4333 actual = **1.253**; the +22 % band (0.779) is what carries it to 1.008. **In the predecessor that band was an insurance line that did not pay out (1.361 core-min unused); here it paid out in full and was needed.** Naming that is the difference between a calibrated estimate and a lucky one. **CONTENTION IS THE ATTRIBUTED CAUSE AND IT IS MEASURED, NOT ASSUMED**: the converter ran at **0.2138 core-min/Mcell** against the **0.1786** measured pre-freeze on the identical four grids — **+19.7 % on the same work, the same code and the same population**, with peer lanes committing throughout the window. That is exactly the ±22 % contention figure §5.1 registered, arrived at independently. **`checkMesh` AT 1.000 IS THE SINGLE CLEANEST DATUM HERE**: `L-468`'s replacement rate — ~0.066 core-min/Mcell, roughly ⅓ of the converter rate, against the predecessor's 1× analogy which scored 0.326 — predicted **0.500** and measured **0.5000**. A rate derived from one measurement and applied to the next run was exact. **Grading at 1.369** is the one genuine under-estimate: §5.1 priced it from a 68.08 s round-trip figure plus a 29.18 s grading pass, and the successor's grading does more than the sum (phase-B plants, the export sha256, the fifth gate). **FORWARD CORRECTION: price a grading pass that runs the round trip inline at ~2.3 core-min for this population, not 1.68.** | **Registration** `verification/campaign/RUNG0b_MESH_IMPORT_PREREGISTRATION.md` §5.1/§5.2/§5.3/§5.5, frozen `ace20cb1`. **Record** `verification/runs/RUNG0b_MESH_IMPORT_runs/RESULTS.json`, `STATUS.RUNG0b_MESH_IMPORT`, `TIMING.json`, `log.controls`, `log.analyse`, four `birth_certificate.json`, landed `33b77af5`. **Exports (232 MB, outside git)** `/home/ubuntu/certonomous-runs/RUNG0b_exports/`, sha256 of each recorded in its birth certificate. **Predecessor** `d127d83d`, NOT amended, its `PENDING` standing as committed. **Envelope row for the same spend** in `docs/campaigns/IBL-industrial-benchmark-ladder/IBL_COMPUTE_ENVELOPE_LEDGER.md`. **⚠ WHAT THIS `PASS` DOES NOT LICENSE:** §8's ten non-claims stand in full, and **non-claim 10 binds every citation of it** — it certifies the converter **on this population only** (three Fortran-unformatted grids, one LARGE raw C stream) and makes **no claim about small raw-C-stream files**, the class the committed 13-header audit (`b78e8858`) measured the pinned defect mis-detecting. Dollars DERIVED, NOT MEASURED, at $0.0513/core-h. |
| C-20260903T195818.434309Z-b5b45ac7 | 2026-09-03 | dafoam | **`D6RF` (A2, `curriculum_D6RF`) — `F_mp` and `REF_off`, the two arms nobody in the D6 lineage had ever bought. THE ITEM COMPLETED AS `BLOCKED`, AND THAT IS WHY THIS ROW REPORTS NO RATIO.** Rule 12 requires estimate-versus-actual at every process completion; `D6RF` completed, so the row is owed — but it completed **without either arm executing any physics**. `F_mp` aborted `rc=5` at staging (`spent_before_arm=0.000`, **no `ARM=` row**) and the chain closed `STOPPED_AT_FIRST_NONZERO arm=F_mp rc=5 order=[F_mp REF_off] not_run=[REF_off]` at 20260903T192119Z. **`D6RF-BLOCKING-1`: `d6r_opt_runScript.py` carries the sentinel `# OpenMDAO setup` TWICE — at `:230`, the real anchor, and at `:21` INSIDE THE MODULE DOCSTRING, in the sentence that EXPLAINS the anchor. Both consumers refuse on a count other than one (`d6r_fd_endpoint.py:67`, `d6r_ref_off.py:53`), so BOTH ARMS WERE UNRUNNABLE — and so were `D6R`'s own.** MEASURED: `split(ANCHOR)[0]` returns **1,396 characters of a 12,861-character file** where the correct header is **10,275**, and the truncated header carries `daOptions` — the docstring mentions it — but NOT `class Top`, `POINTS`, `U0`, `CL_TARGETS` or `WEIGHTS` | **215.77 core-min REGISTERED** = `F_mp` **155.70** + `REF_off` **60.07**, frozen `a1d8da497183dcb7f589dcbc8177462448d9346d` and never revised. Both anchored on measurements of the programs they actually run: `F_mp` = D4's arm F3 (**47.267** MEASURED, the only FD-endpoint arm that has ever succeeded in this lineage) × the MEASURED multipoint factor **3.2949** (`D6RACC2` `ACC_mp` 119.933 ÷ D4 `P2` 36.4); `REF_off` = the multipoint `findFeasibleDesign` trim MEASURED at **833 s** in `D6R`'s own `O_mp` log × 4/60 = 55.53, plus one closing multipoint primal 4.54. Caps 480.0 / 190.0, ceiling **670.0** | **0.800 core-min GROSS** — 12 wall s at 4 ranks — and **none of it is attributable to a registered arm.** [MEASURED, `/home/ubuntu/certonomous-runs/CURRICULUM-D6RF-a2-wing-multipoint-fd_partial_20260903T193723Z/ledger.txt`, the sole `ARM=` row.] **THAT ROW IS A GUARD-SELF-TEST ARTEFACT, NOT AN ARM ATTEMPT**, and the ledger carries an appended `ANNOTATION` line saying so — the row is **annotated, never edited**, this file's own append discipline applied to a run ledger. `d6rf_guard_selftest.py` handed the real launcher the real run root after the daemon had created it, so the launcher staged `REF_off/` and started a container; no chain requested that arm, `STATUS.chain` having closed five minutes earlier | **= gross, 0.800 core-min.** The 3,600-s stall rule matches no row — the only container ran **12 wall s** — so cleaned and gross coincide | **NO RATIO. NOT MEANINGFUL, AND THE REASON IS THE POINT.** 0.800 ÷ 215.77 = 0.0037 is arithmetic, not calibration: **it would masquerade as prediction quality when the gap is not misprediction at all.** The registered estimate was never tested — **neither arm executed any physics** — so there is no evidence here about whether 155.70 or 60.07 is a good number, and a 0.0037 in the ratio column would read as a spectacular under-spend to anyone scanning it. **The gap is a BLOCKING DEFECT, and rule 12's attribution clause is exactly what stops it being reported as anything else.** The estimate remains UNTESTED and its successor `D6RF2` inherits it unchanged | **ATTRIBUTION: `D6RF-BLOCKING-1`, a blocking defect — NOT contention, NOT misprediction, NOT waste-as-overrun. WASTE: 0.800 core-min, ALL OF IT**, named separately per `COMPUTE_BUDGET_CHARTER.md` §6 and never absorbed into any ratio, because there is no ratio to absorb it into. **$0.0007 DERIVED** at $0.0513/core-h, `cost_basis` REPORTED-BY-OWNER — the box cannot read its own billing (§5). **THE LESSON THIS ROW EXISTS TO CARRY: the guards refused honestly on `count != 1`; what was never checked is whether the producer could SATISFY them.** A run-time refusal turns an unrunnable registration into a wasted launch; a registration-time assertion turns it into a defect found before compute — which is why `D6RF2` registers **`G-ANCHOR`** as a PRE-COMPUTE gate. **AND THE ACCIDENT IS WHAT FOUND IT: without the self-test's unintended container, `D6RF` would have been re-enqueued after its `.gz` repair and spent `F_mp`'s registered 155.70 core-min reaching a guaranteed refusal.** That does not make the accidental launch acceptable; it makes the disclosure valuable. **A SECOND FIGURE CORRECTED BEFORE IT TRAVELLED, recorded because it is the same class:** a first reading of this total returned **380.8** core-min, from a regex that matched `cap_core_min=380.0` as well as `core_min=`. It was caught before it left the reader's hands. **A sum is only as good as its word boundary** | Run root **ARCHIVED BY `mv`, NEVER DELETED**, at `/home/ubuntu/certonomous-runs/CURRICULUM-D6RF-a2-wing-multipoint-fd_partial_20260903T193723Z`, **with preservation asserted by count: paths 617 → 617, top-level entries 25 → 25, bytes 68,393,914 → 68,393,914**, and the `ANNOTATION` line and the `ARM=REF_off` row both verified present afterwards. Registration and its three addenda: `cases/dafoam/ladder-a/A2/curriculum_D6RF/PREREGISTRATION.md`, frozen `a1d8da497183dcb7f589dcbc8177462448d9346d` (689 lines), ADDENDUM 1 `8c08f9b8` (launcher, driver, units gate), ADDENDUM 2 `1182f4eb` (the S4 vacuous-zero repair), **ADDENDUM 3 `8fc1d764` (POST-COMPUTE — the compressed-field guard, the self-test incident and `D6RF-BLOCKING-1`)**. Successor `D6RF2` frozen `3af45c05f0740124de2646dde93df240d471285b`, launcher addendum `d1c7cf2e`, queued `2f8e21a2`. `verification/queue/dafoam/launched/D6RF_chain.json` is left exactly as it is — it carries pre-ADDENDUM-2 pins and is the historical record of what the daemon actually launched; rewriting a launched row falsifies history |
| C-20260903T205738.749257Z-a478fc79 | 2026-09-03 | heat-transfer | **T25R6c-R2 (`W1150_C4_L1`, leg A / leg B within one run) — THE RUNG GRADED, `GATE FAIL`, `rho = 1.103859`.** Rule 12's completion trigger is "a rung graded"; this rung was graded and the row is owed. The verdict artifact itself carried `calibration_row_owed = "docs/COST_CALIBRATION.md -- owed at completion under rule 12; NOT discharged by this verdict"`, and this row discharges it. | **14.707 core-min**, the registered point at `docs/campaigns/T-family/T25R6cR2_PREREGISTRATION.md`, frozen at `f67ade8d` before any case directory held compute. Per-run cap 45.0 core-min (3.06x) — **not breached**. | **15.0993 core-min.** Basis, stated because it is not a pure measurement: `ExecutionTime` (CPU s, rank 0) x 2 ranks / 60. **THE RANK MULTIPLIER IS AN ASSUMPTION** — `ExecutionTime` is `times(2)` on the calling process and cannot see rank 1 (OpenFOAM-v2606 `TimeIO.C:631` -> `cpuTimePosix.C:40-47`). T25R6c used the same basis and R2 keeps it unchanged so the two rungs stay comparable. Re-derived independently by this lane from `DONE.W1150_C4_L1`'s own `exec_s_legA=14.35` and `exec_s_legB=438.63`: (14.35 + 438.63) x 2 / 60 = 15.099333 core-min, agreeing with the grader's figure to 1e-9. | **15.0993 core-min — gross equals cleaned.** Charter section 2's stall rule matches a row over 3600 wall s; leg A ran 14.35 CPU s and leg B 438.63 CPU s, so **no row is a stall and nothing is subtracted**. Stated rather than left silent. | **1.0267** (15.0993 / 14.707). | **CONTENTION, and it is measured rather than assumed — NOT waste, and NOT arm misprediction.** The 2.67 % overshoot sits inside the shared box's own per-core throughput floor, which this rung measured for itself as **R-R2-3 = 2.150 % settled-work spread at constant solver work**, and which the registration had declared in advance as an irreducible ~3 % floor. It is not arm misprediction: the point estimate was derived from T25R6c's own measured per-step rates on the **same** case, arm, mesh, ranks and box, so the two estimates differ in no modelled quantity. **WASTE: none identifiable** — no stalled row, no aborted leg, no re-run charged here; per `COMPUTE_BUDGET_CHARTER.md` section 6 waste is named separately and is never laundered into the ratio, so it is named as absent rather than omitted. **Dollars: predicted $0.01257, actual $0.01291, DERIVED at $0.0513/core-h and NEVER MEASURED** — reported-by-owner; this box cannot read its own billing (section 5). | `docs/campaigns/T-family/T25R6cR2_RESULTS.md` (commit `05364832`), rule 12 section; figures cited from `verification/runs/T-family/T25R6cR2_LEGAB_runs/T25R6cR2_VERDICT.json` and cross-derived from `DONE.W1150_C4_L1`. **ONE PROVENANCE CAVEAT A READER MUST NOT HAVE TO INFER: the cost figures in that artifact were emitted by the REPAIRED grader** (blob `947df9df`, landed at `001fc082` under `VERIFICATION_CHARTER.md` section 2ah), **whereas the VERDICT OF RECORD — `GATE FAIL`, `rho = 1.103859` — was produced by the FROZEN bytes** (blob `eb363769`) **before the repair and is landed as `T25R6cR2_GRADE_STDOUT.txt` at `286276a1` with its traceback intact. They are not the same invocation.** They agree because all 43 printed gate lines are byte-identical across the repair — measured, not asserted — and because this row's actual re-derives from the completion marker independently of either grader. |
| C-20260903T210054.497332Z-753b0aa2 | 2026-09-03 | heat-transfer | **`T16c` — the re-grade of `T16_MC_c/m/f` under the repaired comparator. RUNG GRADED; verdict `NOT A RESULT` on the registered unreachable branch of frozen §4.** Zero new solver compute: the three solves were already complete on disk, so this rung's entire cost is instrument time. | 0.50 core-min POINT for the grading pass (frozen `T16c_PREREGISTRATION.md:284-295`, tagged ESTIMATED not measured), `ranks` 1, CAP **2.0 core-min hard**; solver 0.00 core-min registered and MEASURED (no case run) | **0.0884 core-min** grading pass (5.305 wall s x 1 rank, one invocation) — the registered item. **2.3503 core-min** total for this lane including instrument validation (141.02 wall s across five timed invocations) | 0.0884 core-min — cleaned equals gross: no invocation came near the 3600 s stall rule (longest 70.4 s) and nothing was discarded | **0.177x** (grading pass, cleaned/predicted) = **4.4 % of the 2.0 CAP**. Solver 0.00/0.00 as registered | **Misprediction, in the conservative direction.** The 0.5 estimate was sized "on three cases of 20/40/80 station rows"; §4's unreachable branch fired, NO station was selected, and the four graded rows (`G1`/`G1b`/`G2`/`G3`) were never evaluated on any level — a `NOT A RESULT` on gate (1) is structurally cheaper than the graded run that was budgeted for. No contention (single-rank pass). **NO WASTE** — the live-tree fingerprint control shows 161 entries under `T16_runs` identical before and after, and nothing was spent on a run that had to be discarded. **SEPARATELY NAMED per charter §6 and NOT absorbed into the ratio: 2.2619 core-min of instrument validation** (transcription checker 0.0018, `--selftest` 0.5385, `-O --selftest` 0.5476, one mutation control 1.1741) which frozen §8 **budgets nowhere** — it caps "the grading pass" only. That silence is disclosed, not filled: under the strict whole-rung reading this lane's total would breach the 2.0 cap, and it is also why the full 41-mutant sweep (~22 core-min, an 11x breach) was **STOPPED rather than run** (rule 12: an overrun stops the run). **A successor registration should budget instrument validation explicitly.** USD derived not measured at $0.0513/core-h: grading $0.000076, total $0.00201 | `docs/campaigns/T-family/T16c_RESULTS.md` §7; artifacts `verification/runs/T-family/T16c_runs/T16C_GRADE_OUTPUT_20260903T205559Z.txt` and `gate_t16c.json` |
| C-20260903T210144.295494Z-75297022 | 2026-09-03 | dafoam | **SO-3D Stage 1 (`curriculum_SO3D`, log-replay root-cause diagnostic) — FIRST COMPUTE, VERDICT `NOT A RESULT`.** Rule 12's completion trigger is a completed process; this invocation completed (rc=2, `STATUS.SO3D`) and the row is owed whether or not the process produced a gate reading. **It did not: the plant control G-SO3D-P refused and, by the pre-registration's section 6 ordering rule 1, every other gate is `NOT A RESULT` and none was scored.** | **6.0 core-min** point estimate, bracket **3–10**, registered at `cases/dafoam/ladder-a/A2/curriculum_SO3D/PREREGISTRATION.md` section 11, frozen 2026-08-31 before the reader existed and carried unchanged through AMENDMENT 1. Cap **12.0 core-min**; **solver core-min capped at 0.0**. | **0.0392 core-min.** Basis: wall **2.350 s** x **1 rank** / 60, captured **inside** the detached wrapper immediately after the reader returned — never around the `setsid` line, which exits 0 for every outcome of what it wraps. Recorded in `STATUS.SO3D`. **Solver core-min actually spent: 0.000** — no container, no MPI job, no OpenFOAM process was started, so the 0.0 solver cap was honoured by construction and not merely by intention. | **0.0392 core-min — gross equals cleaned.** `COMPUTE_BUDGET_CHARTER.md` section 2's stall rule matches a row over 3600 wall s; this run was 2.350 s, so no row is a stall and nothing is subtracted. Stated rather than left silent. | **0.0065** (0.0392 / 6.0). **READ THE ATTRIBUTION BEFORE THIS NUMBER.** It is a floor, not a calibration of the pipeline the estimate costed. | **MISPREDICTION, of a specific and correctable kind — NOT contention, and the waste is named separately rather than absorbed.** The registration costed *"four logs totalling approx 0.9 M lines, three full passes each"* as I/O-bound line counting and over-predicted by roughly **153x**; the measured rate is **0.49 s for a full read + census + record-parse + coupling + sha256 pass over the 222,223-line D6 log** (probe run on D6 precisely because D6 carries no G-SO3D-1/2/4 threshold, so no gate quantity was computed before the run). **NOT CONTENTION:** the box was at approx 95 % occupancy and this run took 2.350 s of it; contention cannot explain an under-spend. **WASTE: 0.0392 core-min, ALL of it, named in full and never laundered into the ratio** (`COMPUTE_BUDGET_CHARTER.md` section 6) — the invocation produced no gate reading, so every core-minute is waste attributable to a defect in the lane's own PLANT-B acceptance predicate, which demanded that the plant's two content-addressed anchors fall in **two distinct scenarios** when the pre-registration's section 7 nowhere requires that and both anchors land deterministically in `cl04`. It was **unsatisfiable by construction**: no invocation of this reader could ever have passed PLANT-B. **THE RATIO IS NOT A CALIBRATION OF THE FULL PIPELINE** — the run aborted at the plant control and never reached the four gates, the two single-point controls or the JSON writes, so a successor that runs to completion will produce the honest ratio and this row must not be read as one. **Dollars: predicted $0.00513, actual $0.000033, DERIVED at $0.0513/core-h and NEVER MEASURED** — reported-by-owner; the box cannot read its own billing (section 5). | `cases/dafoam/ladder-a/A2/curriculum_SO3D/RESULTS.md` and `STATUS.SO3D`, landed in this commit. Grading path verified before invocation: `so3d_replay.py` blob `4ebd9b2f17a824b49470f32d685bbb05cba61f09` at HEAD equalled the blob on disk, md5 `60af48e4e3debb536141eb3f5c66409b` — the frozen file **is** the file that ran (`CLAUDE.md` rule 2). Pre-registration frozen 2026-08-31 (v1.0), AMENDMENT 1 at `b54575f7`. First compute authorised by `dafoam-supervisor` after checks 1 and 4 discharged personally. |
| C-20260903T210542.973647Z-152c5fa0 | 2026-09-03 | cfd | **JF1G pass-1 C1 `JF1G_P1_C1_CMU010_A0` -- the grid-convergence ladder's L1 level, RUN COMPLETE, and the ONLY one of the four JF1G rows launched 2026-09-03 that finished on its own.** Ran 2026-09-03T19:10:49Z to 20:46:34Z at 39,984 cells, `endTime` 30000, ranks 1. **THE STRICT COMPLETION RULE (`CLAUDE.md` rule 4) WAS CHECKED CLAUSE BY CLAUSE BY THIS LANE ON THE ARTIFACTS, NOT RELAYED FROM `RUN_STATUS`, AND ALL SIX HOLD:** (1) `rc = 0` -- `SOLVER_RC.txt` reads `solver_rc 0`; (2) exactly **one `End` line** in `log.simpleFoam`; (3) last `Time = 30000` == `endTime` 30000 of `system/controlDict`; (4) **fields present at `30000/`: `U k nut omega p phi yPlus`** -- the JF1 family's set, this being an incompressible family with no `T` and no `p_rgh`, so rule 4's thermal-family field list is read in its family-appropriate form; (5) **`ExecutionTime` line count 30000 == `endTime`**; (6) **THE AGE GUARD HOLDS** -- every one of the seven fields in `30000/` is stamped 2026-09-03T20:46:34.5Z, and the NEWEST file anywhere in the case's own `0/` is 19:10:51.2Z, so all seven endTime fields are newer than the whole of `0/` (the case has no `0/T`; `0/` holds `U k nut omega p`, and the guard is applied against the newest of them, which is the strictest available reading). **NO VERDICT OF THE FIXED VOCABULARY IS ASSERTED HERE. This row is the rule-12 cost comparison only; grading against gates G1..G7 of the frozen registration is a separate record and is not made, implied or anticipated by this row.** | **81.6 core-min = $0.06977 DERIVED, NOT MEASURED** -- registered in the FROZEN pre-registration `verification/campaign/JF1G_GRID_CONVERGENCE_PREREGISTRATION.md` section 8, table row *Pass 1 C1*, and carried identically in the launched queue row as `cost_core_min_estimate` 81.6. **Its derivation, from the queue row's own `cost_basis`: 21.7500 core-min at `endTime` 8000 times (30000/8000) = 81.5625, rounded to 81.6** -- a MEASURED sibling (`JF1G_P0_C1`, identical mesh, identical rank count) scaled linearly in iteration count. That linear ladder-scaling assumption is the thing this row puts under test. **REGISTERED CAP 110.0 core-min = $0.09405 DERIVED, AND IT IS ENFORCED IN METAL, NOT ON TRUST:** `cases/JF1_JET_FLAP/run_jf1g.sh` wraps `simpleFoam` in `timeout` at cap times 60 s at ranks 1, and a timeout kill is a non-zero rc that STOPS the run rather than buying it a new budget. **THE DENOMINATOR IS THE REGISTERED 81.6 AND IS NOT ADJUSTED DOWNWARD OR UPWARD TO FLATTER THE RATIO.** | **95.7500 core-min MEASURED = $0.08187 DERIVED** -- 5745 wall s times 1 rank divided by 60, from `RUN_STATUS.JF1G_P1_C1_CMU010_A0.txt` (`wall_s 5745`, `ranks 1`, `core_min_MEASURED 95.7500`). **RE-DERIVED INDEPENDENTLY BY THIS LANE RATHER THAN ACCEPTED FROM THE STATUS FILE: `utc_end` 20:46:34Z minus `utc_start` 19:10:49Z = 5745 s exactly, so the launcher's own wall figure is corroborated by its own timestamps and is not a single unchecked number.** The solver's own counters are reported beside it and are NOT the figure used: final `ExecutionTime = 5659.86 s  ClockTime = 5743 s` gives 94.3310 and 95.7167 core-min respectively; the launcher wall is used because rule 12 defines the unit as wall s times ranks divided by 60. Serial throughout (ranks = 1, so core-min equals wall-min on every figure in this row). **GROSS: contention is NOT netted out of this column** -- it is measured and attributed in the gap cell instead. | **GROSS IS THE PUBLISHED FIGURE AND NO CLEANED FIGURE IS PUBLISHED. THE REASON IS NAMED, NOT JUDGED.** The stall rule (`COMPUTE_BUDGET_CHARTER.md` section 2, the single rule in `scripts/self_audit.py`) matches any ledger row over 3600 wall s. This run's wall is **5745 s**. Read literally against a one-row-per-run reading, the rule would clean a **registered, capped, iteratively complete** solve -- one that finished at 87.05 % of a 110.0 core-min cap fixed before compute -- down to zero. The rule's operand is a per-run ledger row standing for an INFRASTRUCTURE STALL, not a single long registered solve. **REFERRED to the supervisor and to verification as a rule-scope question, exactly as the heat-transfer row of 2026-09-03 covering a registered 71,749 s transient referred it; this lane does not reinterpret a standard.** **WASTE IS NOT IN THIS COLUMN** (`COMPUTE_BUDGET_CHARTER.md` section 6) -- it is named in the gap cell. | **1.1734** (95.7500 / 81.6) -- a **17.34 % overrun** on the registered estimate. Against the unrounded derivation 81.5625 the figure is **1.1739**. **CAP USAGE 87.05 %** (95.7500 / 110.0): the run finished inside its own enforced ceiling with 14.25 core-min unspent, so no overrun-stop was triggered and rule 12's *an overrun stops the run* never bound on this row. | **ATTRIBUTED BY MEASUREMENT, AND THE HEADLINE IS THAT THE LADDER-SCALING LAW UNDER TEST IS VINDICATED, NOT CONVICTED.** The estimate's structural assumption was *cost is linear in iteration count at fixed mesh*. Tested directly: had the per-iteration wall cost stayed at the reference's **0.163000 wall s per iteration** (`JF1G_P0_C1`, `ClockTime` 1304 s over 8000 iterations), 30000 iterations would have cost 4890 s = **81.500 core-min against the registered 81.5625, an error of 0.08 %.** **THE SCALING LAW IS NOT THE DEFECT; THE PER-ITERATION RATE IS.** **THE WHOLE 1.1734 IS PER-ITERATION COST INFLATION ON A BYTE-IDENTICAL CASE, AND IT SPLITS INTO TWO MEASURED PARTS THAT MULTIPLY BACK TO IT.** **(a) SCHEDULER CONTENTION -- 1.0078, i.e. 0.78 percentage points, and it is SMALL.** Wall-over-CPU is 5743 / 5659.86 = **1.01469** on this run against 1304 / 1295.16 = **1.00683** on the reference; the ratio is 1.00781. The process was essentially never denied a core -- 16 vCPU box, every JF1G solve single-rank. **(b) CPU-TIME INFLATION -- 1.1653, i.e. 16.53 percentage points, and this is the whole story.** Mean CPU cost per iteration rose from **0.161895 core-s per iteration** on the reference to **0.188662** on this run. (a) times (b) = 1.0078 times 1.1653 = **1.1744**, which is the measured wall ratio 0.191433 / 0.163000 = **1.1744**. **THE TWO CASES ARE IDENTICAL AND THAT WAS CHECKED, NOT ASSUMED: `diff -r` over `0/`, `constant/` and `system/` between `JF1G_P0_C1_CMU010_A0` and `JF1G_P1_C1_CMU010_A0` returns EXACTLY TWO differences -- `endTime` 8000 to 30000, and `residualControl` 1e-06 to 1e-08 on p, U, k and omega. Neither changes the work done inside an iteration** (`residualControl` is a stopping test over residuals already computed, and neither run ever met either threshold), **so the 16.53 pp cannot come from the case setup.** **STEADY STATE IS WORSE THAN THE AVERAGE, AND THE AVERAGE FLATTERS THIS RUN: excluding each run's startup block, CPU cost per iteration is 0.15350 core-s per iteration on the reference (iterations 2001-8000) against 0.18668 on this run (iterations 3001-30000), a ratio of 1.2161.** The run-average 1.1653 is lower only because the startup transient amortises over 30000 iterations instead of 8000 -- an artefact of the longer run, not an efficiency. **CO-TENANCY, READ FROM THE LAUNCH RECORD RATHER THAN INFERRED FROM MEMORY:** during this run's 19:10:49Z-20:46:34Z window `verification/queue/LAUNCH_LOG.tsv` records these concurrently live queue launches -- `D6RF_chain` (dafoam, 4 ranks, 19:02:14Z), `A1ZE_chain` (dafoam, 1 rank, 19:05:24Z), `JF1G_P1_C2` (cfd, 1 rank, 19:06:29Z), `JF1G_P0_C4` (cfd, 1 rank, 19:07:34Z-20:46:14Z), `JF1G_P1_C4` (cfd, 1 rank, 19:09:44Z-20:46:14Z), `K0e_FP_T10` (heat-transfer, 2 ranks, 19:23:00Z) and `K0eR2_FP_T10` (heat-transfer, 2 ranks, 19:46:50Z) -- **up to about 12 ranks on 16 vCPUs, two of them 455,456-cell solves.** The reference ran 2026-09-01T16:10:49Z-16:32:34Z with only its own pass-0 siblings on the queue. **HONEST LIMIT ON THAT EVIDENCE: `LAUNCH_LOG.tsv` sees only queue-launched work, so agent-launched foreground work in either window is invisible to it and no claim is made about it.** **THE MECHANISM IS INFERRED, NOT INSTRUMENTED, AND THIS ROW SAYS SO:** shared last-level cache and memory bandwidth is the natural reading of *CPU seconds per iteration rose 16.5 % while the process was never denied a core*, but **NO PMU COUNTER WAS COLLECTED AND NONE IS CLAIMED.** **HOW MUCH IS CONTENTION AND HOW MUCH IS RESIDUAL MISPREDICTION CANNOT BE SPLIT FURTHER FROM THESE LOGS, AND IT IS NOT SPLIT ON A GUESS:** there exists no quiet-box measurement of this mesh at this date, so the 16.53 pp is attributed to BOX LOAD as the only variable measured to have changed, with the explicit caveat that any unmeasured drift sits inside that figure. **THE LESSON FOR THE LAB'S ESTIMATES, STATED SPECIFICALLY: a JF1 per-iteration rate measured on a lightly loaded box runs about 1.17x optimistic when the same mesh is re-run on a box carrying roughly 12 concurrent ranks; carry an explicit load factor rather than a bare sibling rate.** **WASTE -- SEPARATELY NAMED, NEVER LAUNDERED INTO EITHER ACTUAL COLUMN OR INTO THE RATIO (`COMPUTE_BUDGET_CHARTER.md` section 6): 0.0000 core-min.** An attempt 1 of this same row (queue launch 19:04:19Z, pid 487478) died at `rc=9`, `stage_at_exit dicts`, `wall_s 0` -- before `build_jf1.py`, before `checkMesh`, before `simpleFoam` -- on an unescaped alternation character used as the substitution delimiter in the launcher's `sed`, which the launcher's own verification loop CAUGHT and REFUSED rather than silently solving pass 1 at the pass-0 tolerance and calling it graded. **It spent zero solver compute, and its artifacts are PRESERVED, never deleted**, at `JF1G_P1_C1_CMU010_A0.attempt1_FAILED_2026-09-03T190419Z_PRESERVED/`. | **Frozen pre-registration** `verification/campaign/JF1G_GRID_CONVERGENCE_PREREGISTRATION.md` section 8 (estimate 81.6, cap 110.0, unit rate 4.0798e-06 core-s per cell per iteration) whose closing line owes this row by name; frozen `038f4bca160af8bc201f93805cb841a30236a28c`, blob `0b9476598ef4467dae3f9bcc0e4cf113254bd8e0`, and the run's own `RUN_STATUS` records `prereg_check VERIFIED` against both. **Queue row** `verification/queue/cfd/launched/JF1G_P1_C1_CMU010_A0.json` (`cost_core_min_estimate` 81.6, `cap_core_min_registered` 110.0, `cost_basis`, `predicted_outcome`, `attempt_1_disclosure`). **Actuals** `verification/runs/JF1_jet_flap/JF1G_P1_C1_CMU010_A0/RUN_STATUS.JF1G_P1_C1_CMU010_A0.txt`, `SOLVER_RC.txt`, and `log.simpleFoam` (30000 `Time` lines, 30000 `ExecutionTime` lines, one `End`, final `ExecutionTime = 5659.86 s  ClockTime = 5743 s`), plus the field mtimes under `0/` and `30000/` that the age guard was read from. **Reference** `verification/runs/JF1_jet_flap/JF1G_P0_C1_CMU010_A0/RUN_STATUS.JF1G_P0_C1_CMU010_A0.txt` and its `log.simpleFoam` (8000 iterations, final `ExecutionTime = 1295.16 s  ClockTime = 1304 s`). **Launcher** `cases/JF1_JET_FLAP/run_jf1g.sh`. **Co-tenancy** `verification/queue/LAUNCH_LOG.tsv`. **Dollars throughout are DERIVED, NOT MEASURED**, at the owner-stated c7a.4xlarge $0.0513 per core-h (reported-by-owner 2026-08-21/22) -- the box cannot read its own billing, `COMPUTE_BUDGET_CHARTER.md` section 5. **A FURTHER ROW IS OWED BY `JF1G_P1_C2_CMU010_A0`**, still running toward its own pre-registered cap at this row's writing and deliberately untouched by this lane; **its queue row carries an anomaly that row must address -- `cost_core_min_estimate` 265.8 EXCEEDS `cap_core_min_registered` 240.0**, so the registered estimate is above the registered ceiling. |
| C-20260903T210542.973742Z-668490f7 | 2026-09-03 | cfd | **JF1G C4 pair `JF1G_P0_C4_CMU010_A0` and `JF1G_P1_C4_CMU010_A0` -- the 455,456-cell contingency level, BOTH SUPERVISOR-STOPPED MID-SOLVE at 2026-09-03T20:46:02Z, BOTH `NOT A RESULT`.** One termination decision on one cause, so one row; both cases' figures are stated separately inside it and neither is averaged away. Each was stopped on arithmetic recorded BEFORE the signal in its own `STOP_RECORD.txt`: at the observed mean rate (0.09213 it/s for P0_C4, 0.09288 it/s for P1_C4) the iterations still required needed **4.8x** the `timeout` allowance still remaining -- on both. **`NOT A RESULT` UNDER RULE 4, VERIFIED BY THIS LANE ON THE LOGS AND NOT RELAYED: `log.simpleFoam` contains ZERO `End` lines in both cases, and the last `Time` line is 537 of `endTime` 8000 (P0_C4) and 529 of `endTime` 30000 (P1_C4)** -- two clauses failed, so the label was certain before the stop and was not created by it. Both exited `rc 7`, `stage_at_exit simpleFoam` (`cases/JF1_JET_FLAP/run_jf1g.sh:407` maps a non-zero solver rc to 7). TERM was sent, never KILL; every log, time directory and `postProcessing/` is intact, and the only time directories on disk are `0`, `250` and `500`, written by the yPlus function object. **Each `STOP_RECORD.txt` captured `Time = 536` and `528` one iteration before the logs' final lines; the difference is one iteration written between capture and exit and is stated rather than smoothed.** | **HEADLINE DENOMINATOR -- 0.15 core-min EACH, 0.30 core-min FOR THE PAIR = $0.000257 DERIVED**, the `cost_core_min_estimate` of the two launched queue rows, which is the prediction of record for these two launches. **IT WAS NOT A SOLVE ESTIMATE AT ALL:** both `predicted_outcome` fields predict REFUSAL at `checkMesh` before `simpleFoam` ever starts, priced as mesh build plus `checkMesh` scaled up from `JF1G_P0_C3`'s measured 0.0667 core-min. **A SECOND REGISTERED FIGURE EXISTS AND IS REPORTED RATHER THAN QUIETLY CHOSEN BETWEEN: the FROZEN pre-registration section 8 registers a *C4 contingency* of 929.1 core-min at `endTime` 30000 against a 1,200 cap.** That line covers pass-1 C4 only; section 8's table carries NO pass-0 C4 line at all, so P0_C4's only registered prediction is its queue row's 0.15. **REGISTERED CAPS 380.0 core-min (P0_C4) and 1,200.0 core-min (P1_C4)**, both enforced in metal as `timeout` allowances of 22800 s and 72000 s, read in each stop record from the wrapper's own argv rather than from the registration. **NEITHER DENOMINATOR IS ADJUSTED TO FLATTER THE RATIO.** | **195.1667 core-min MEASURED FOR THE PAIR = $0.16687 DERIVED**, made of **P0_C4 98.6667 core-min** (5920 wall s times 1 rank divided by 60 = $0.08436 DERIVED) and **P1_C4 96.5000 core-min** (5790 wall s = $0.08251 DERIVED), from the two `RUN_STATUS` files. **RE-DERIVED INDEPENDENTLY BY THIS LANE: P0_C4 `utc_end` 20:46:14Z minus `utc_start` 19:07:34Z = 5920 s exactly; P1_C4 20:46:14Z minus 19:09:44Z = 5790 s exactly.** Solver counters are reported beside and are not the figures used: P0_C4 final `ExecutionTime = 5829.26 s  ClockTime = 5906 s`, P1_C4 final `ExecutionTime = 5696.45 s  ClockTime = 5771 s`. Serial throughout (ranks = 1 on both, so core-min equals wall-min). **GROSS: no contention is netted out of this column.** | **GROSS IS PUBLISHED AND NO CLEANED FIGURE IS PUBLISHED -- THE SAME NAMED REASON AS THE PASS-1 C1 ROW, AND IT IS A SCOPE QUESTION, NOT A JUDGEMENT.** Both walls (5920 s and 5790 s) exceed the 3600-wall-s stall rule of `COMPUTE_BUDGET_CHARTER.md` section 2, yet neither is an infrastructure stall: both are registered, capped solves that ran continuously and were then deliberately terminated, at **25.96 %** (P0_C4) and **8.04 %** (P1_C4) of their own pre-registered caps. Cleaning them would zero a real, chosen, capped spend and would understate what this study cost. **REFERRED as the same rule-scope question; this lane does not reinterpret a standard.** **WASTE IS NOT IN THIS COLUMN** -- it is named in the gap cell, per section 6. | **650.6 FOR THE PAIR** (195.1667 / 0.30) against the headline registered denominator; **per case, 657.8** (98.6667 / 0.15) and **643.3** (96.5000 / 0.15). **A FIGURE OF ORDER 1,500x CIRCULATING FOR THIS PAIR DOES NOT REPRODUCE AND IS NOT USED HERE: dividing the PAIR's 195.1667 by a SINGLE row's 0.15 gives 1301, which double-counts the numerator against half the denominator. The pair-consistent figure is 650.6.** **DIAGNOSTIC RATIO, LABELLED DIAGNOSTIC AND NOT THE HEADLINE: against the frozen section 8 C4 contingency of 929.1, P1_C4's 96.5000 gives 0.1039** -- an apparent underspend that calibrates NOTHING, because the run was stopped at 529 of 30000 iterations and never bought the thing 929.1 was priced for. **CAP USAGE 25.96 % and 8.04 %.** | **THE PREDICTION FAILED ON A PREMISE, THE PREMISE IS QUOTED, AND MEASUREMENT FALSIFIES IT.** The P0_C4 queue row's `predicted_outcome` reads, verbatim: *C3 (202,180 cells) already failed it on max aspect ratio 1012.24 over TWO cells, and C4 is the next refinement of the same family, so the aspect ratio is predicted HIGHER still and the refusal is predicted to REPEAT at rc=5.* **Measured at C4: max aspect ratio 796.8931, and `checkMesh` printed `Mesh OK`.** The gate passed, `simpleFoam` started, and 195 core-min followed a prediction of 0.30. The ladder's four `log.checkMesh` files give **max aspect ratio 859.9542 / 634.4439 / 1012.2428 / 796.8931 across C1/C2/C3/C4 -- NON-MONOTONE -- while max non-orthogonality rises MONOTONICALLY 57.5270 / 67.6420 / 75.1437 / 81.3537 and is not gated at all.** **A MECHANISM OFFERED FOR THIS IS WRONG, AND THIS ROW RECORDS THAT SO A SUCCESSOR CANNOT RE-IMPORT IT: IT IS NOT THE CASE that `n_rad` is held fixed at 98 while only `SCALE` moves, and this ladder does NOT refine tangentially only.** The four `RUN_STATUS` files do each print `n_rad 98 (EXPLICIT, never defaulted)` -- **but that line records the COMMAND-LINE ARGUMENT, not the emitted radial count, and a reader who takes it for the count gets the ladder backwards.** `cases/JF1_JET_FLAP/build_jf1.py:394-395` reads `if args.n_rad > 0:` then `n_rad = int(round(args.n_rad * s))`, so `--n-rad 98` is a BASE that scales with the level. The runs' own `log.build_jf1` files record the EMITTED counts **`N_rad SOLVED from the cap = 98 / 147 / 220 / 331`** with near-wall spacing `y1` **5.000e-06 / 3.333e-06 / 2.222e-06 / 1.481e-06 m** -- refinement in BOTH directions, which is precisely what the freeze commit `038f4bca` bought when it rejected the generator's default path. Cell counts corroborate it independently: 39,984 / 89,964 / 202,180 / 455,456 scale as SCALE SQUARED, not as SCALE. **WHAT IS ESTABLISHED IS THEREFORE NARROWER, AND IS STATED AS SUCH: the registered premise -- that the next refinement of the same family must have a higher max aspect ratio -- was asserted with NO MECHANISM BEHIND IT and is false on this ladder; the quantity is an EXTREMUM over as few as two cells (C3 flagged exactly 2 of 202,180). THE MECHANISM OF THE NON-MONOTONICITY IS NOT ESTABLISHED BY THIS ROW AND IS NOT GUESSED AT.** The gate defect itself -- the launcher discarding `checkMesh`'s exit code and gating on the verdict STRING -- is escalated separately at `verification/campaign/JF1G_MESH_GATE_FINDING.md` and is not this row's to rule on. **WHAT THE 195.1667 CORE-MIN BOUGHT AND DID NOT BUY -- BOTH HALVES, NEITHER LAUNDERED.** **(i) IT PRODUCED ZERO GRADEABLE ROWS.** Both cases are `NOT A RESULT`; no `endTime` time directory exists in either and none ever will. **(ii) IT DID NOT BUY THE MESH-GATE FINDING, AND CREDITING IT WITH THAT WOULD BE LAUNDERING.** The aspect-ratio non-monotonicity was demonstrated by `checkMesh` inside a build-plus-check stage that `JF1G_P0_C3` measured at **0.0667 core-min** -- essentially free, and already on disk before either C4 solve began. **(iii) IT DID BUY EXACTLY ONE CALIBRATION DATUM, AND THIS ROW IS WHERE IT LANDS: the 455,456-cell per-iteration solve rate on this box -- 10.855 core-s per iteration (P0_C4, `ExecutionTime` 5829.26 s over 537 iterations) and 10.769 (P1_C4, 5696.45 s over 529).** Against the FROZEN section 8 unit rate of **4.0798e-06 core-s per cell per iteration**, derived from the 39,984-cell L1 mesh, the predicted rate at 455,456 cells is 1.8583 core-s per iteration -- **so the frozen unit rate is 5.84x OPTIMISTIC at 455k cells.** The same rate is **1.448x optimistic at 89,964 cells** (`JF1G_P0_C2` measured 0.53163 core-s per iteration against 0.36704 predicted) and exact at 39,984 by construction, that mesh being its basis. **THE LESSON FOR THE LAB'S ESTIMATES, STATED SPECIFICALLY: the JF1 per-cell-per-iteration rate measured on the L1 mesh degrades SUPERLINEARLY with cell count on this box -- about 1.0x at 40k cells, 1.45x at 90k, 5.8x at 455k -- so a bare per-cell rate carried up a grid ladder is exactly the factor wearing a basis's clothes that `COMPUTE_BUDGET_CHARTER.md` names in its own heading, and any JF1 estimate above roughly 100k cells must carry a measured size factor rather than that rate. MECHANISM INFERRED (working set outgrowing cache), NOT INSTRUMENTED -- no PMU counter was collected and none is claimed.** **THE STOP IS VINDICATED BY THAT SAME DATUM: at the measured rate P1_C4's 30000 iterations would have cost 5,427 core-min -- 5.84x the frozen contingency of 929.1 and 4.52x its own 1,200 cap -- and P0_C4's 8000 iterations 1,447 core-min, 3.81x its 380 cap. Both were certain to be TERM-killed mid-solve by their own wrappers; continuing would have spent further core-minutes to buy an outcome already known, and rule 12 forbids buying it a new budget.** **WASTE, NAMED SEPARATELY AND FOLDED INTO NO RATIO AND INTO NEITHER ACTUAL COLUMN (`COMPUTE_BUDGET_CHARTER.md` section 6): 195.1667 core-min = $0.16687 DERIVED PRODUCED NO GRADEABLE ROW.** **It is NOT written off as pure waste** -- item (iii) is what it purchased, and that datum is what makes the next JF1 estimate honest -- **and it is NOT dressed up as research value either: no gate was reached and no verdict was issued. 195.1667 core-min is what the datum cost, and this row states that plainly rather than pricing a cheaper counterfactual probe it did not run.** | **Frozen pre-registration** `verification/campaign/JF1G_GRID_CONVERGENCE_PREREGISTRATION.md` section 8 (C4 contingency 929.1, cap 1,200, unit rate 4.0798e-06 core-s per cell per iteration) and section 7 (the contingency trigger); frozen `038f4bca160af8bc201f93805cb841a30236a28c`, blob `0b9476598ef4467dae3f9bcc0e4cf113254bd8e0`, and both runs' `RUN_STATUS` record `prereg_check VERIFIED`. **Queue rows** `verification/queue/cfd/launched/JF1G_P0_C4_CMU010_A0.json` and `verification/queue/cfd/launched/JF1G_P1_C4_CMU010_A0.json` (`cost_core_min_estimate` 0.15 each, `cap_core_min_registered` 380.0 and 1200.0, `cost_basis`, `predicted_outcome` quoted above). **Actuals and stop arithmetic** `verification/runs/JF1_jet_flap/JF1G_P0_C4_CMU010_A0/` and `verification/runs/JF1_jet_flap/JF1G_P1_C4_CMU010_A0/`, each carrying `RUN_STATUS.<case>.txt`, `SOLVER_RC.txt`, `STOP_RECORD.txt`, `log.simpleFoam`, `log.checkMesh` and `log.build_jf1`. **Ladder mesh evidence** the `log.checkMesh` and `log.build_jf1` files under `JF1G_P1_C1_CMU010_A0/`, `JF1G_P0_C2_CMU010_A0/`, `JF1G_P1_C3_CMU010_A0/` and `JF1G_P1_C4_CMU010_A0/`. **Generator** `cases/JF1_JET_FLAP/build_jf1.py:394-395`. **Launcher** `cases/JF1_JET_FLAP/run_jf1g.sh:377` (the `Mesh OK` string gate) and `:407` (rc 7). **Gate defect** `verification/campaign/JF1G_MESH_GATE_FINDING.md`, escalated separately and NOT ruled on here. **Dollars throughout are DERIVED, NOT MEASURED**, at the owner-stated $0.0513 per core-h, reported-by-owner -- `COMPUTE_BUDGET_CHARTER.md` section 5. |
| C-20260903T212448.664406Z-5123949f | 2026-09-03 | dafoam | **SO-3D-R Stage 1 (`curriculum_SO3DR`, log-replay root-cause diagnostic) — RUNG COMPLETE, ALL FIVE GATES SCORED: `G-SO3D-P` PASS, `G-SO3D-1` PASS, `G-SO3D-2` GATE FAIL, `G-SO3D-3` GATE FAIL, `G-SO3D-4` PASS.** **THIS ROW CONTINUES THE SO-3D ROW OF THE SAME DATE** — the `dafoam` / *SO-3D Stage 1* row landed at commit `ef4bd22c`, verdict `NOT A RESULT` (**its allocated id is deliberately NOT hand-written here: `scripts/append_record.py` is the sole producer of that form and refuses a hand-written one, which is the guard against reintroducing a duplicate id by edit — the row is identified by team, item and commit instead**), which could report only a FLOOR because that invocation aborted at the plant control before reaching the four gates, the two single-point controls and the JSON writes. **This row is the honest one for the same registered estimate**, because the pipeline the estimate costed ran end to end. | **6.0 core-min** point, bracket **3–10**, cap **12.0**, `solver_core_min` capped at **0.0**. **CARRIED FORWARD FROM SO-3D UNREVISED AND KNOWN-HIGH, DELIBERATELY.** A successor that quietly re-costs its predecessor's registration destroys the comparison this ledger exists to make; the ~150x class of misprediction stays visible until enough passes are measured to correct it honestly rather than retrofitting the prediction to one measurement. Registered at `cases/dafoam/ladder-a/A2/curriculum_SO3DR/PREREGISTRATION.md` section 7. | **0.0713 core-min.** Basis: wall **4.277 s** x **1 rank** / 60, captured **inside** the detached wrapper immediately after the reader returned — never around the `setsid` line, which exits 0 for every outcome of what it wraps. Recorded in `STATUS.SO3DR`. **The reader's own internal clock recorded 0.0703 core-min; the LARGER figure is the one reported**, the difference being the launcher's `git` hashing of the grading path and interpreter start. **Solver core-min actually spent: 0.000** — no container, no MPI job, no OpenFOAM process, so the 0.0 solver cap was honoured by construction rather than by intention. | **0.0713 core-min — gross equals cleaned.** `COMPUTE_BUDGET_CHARTER.md` section 2's stall rule matches a row over 3600 wall s; this run was 4.277 s, so no row is a stall and nothing is subtracted. Stated rather than left silent. | **0.0119** (0.0713 / 6.0). | **MISPREDICTION, MEASURED ON A COMPLETE PASS — not contention, and the waste is named separately.** The full pipeline (four logs totalling ~578k lines, the non-finite census, per-scenario attribution over 977 primal records, cutback coupling over 673 cutbacks, THREE plant passes over the 264,607-line D6R log, and both JSON writes) completed in **4.277 s**. The registration costed the work as I/O-bound line counting and over-predicted by **~84x** against the point estimate and ~42x against the bracket floor of 3.0. **NOT CONTENTION:** the box was heavily loaded and this run took four seconds of it; contention cannot explain an under-spend. **WASTE, NAMED AND NOT LAUNDERED INTO THE RATIO** (`COMPUTE_BUDGET_CHARTER.md` section 6): one aborted launch attempt at 2026-09-03T21:18:12Z, **~0 core-min**, which refused at the launcher's own G1 grading-path guard (`rc 6`, *"UNMEASURED, not assumed"*) before creating a run root or a container, caused by the lane carrying the reader's repo-relative path as a second literal that a `sed` double-applied to. Disclosed in that item's ADDENDUM 1 and repaired structurally by deriving the path. **CALIBRATION LESSON FOR THE NEXT LOG-REPLAY ESTIMATE:** this lab's host-side log-replay work runs at roughly **0.5 s per 220k-line pass** at ranks 1, so a four-log multi-pass reader is a **sub-0.1 core-min** item, not a 6 core-min one. Two independent measurements now agree on that order (SO-3D partial 0.0392, SO-3D-R complete 0.0713). **Dollars: predicted $0.00513, actual $0.000061, DERIVED at $0.0513/core-h and NEVER MEASURED** — reported-by-owner; the box cannot read its own billing (section 5). | `cases/dafoam/ladder-a/A2/curriculum_SO3DR/RESULTS.md` and `STATUS.SO3DR`, landed in this commit; gate values from `so3dr_replay.json` and `so3dr_plant_report.json` under `/home/ubuntu/certonomous-runs/CURRICULUM-SO3DR-a2-wing-multipoint-rootcause/`. Grading path verified before invocation: blob `55b918edf4f8246c79c7e631a3bd6864eb86ec52` at HEAD equalled the blob on disk, md5 `007328fe6777b23d6d81a1a7e8fedcd0` — the frozen file **is** the file that ran (`CLAUDE.md` rule 2). Pre-registration frozen 2026-09-03 at `3e6607ea`, ADDENDUM 1 at `826283ed`. Checks 1 and 4 discharged personally by `dafoam-supervisor` before first compute. |
| C-20260903T215302.846798Z-9d30b9a8 | 2026-09-03 | heat-transfer | **CORRECTION ROW -- RECONCILIATION OF A DUPLICATE: ONE COMPLETED PROCESS, TWO LEDGER ROWS.** The rung T25R6c-R2 (`W1150_C4_L1`, leg A / leg B within one run, graded `GATE FAIL`, rho = 1.103859) was appended to this ledger TWICE. The OPERATIVE row is corrects:[C-20260903T205738.749257Z-a478fc79], landed at commit `1aee5c65`, 2026-09-03T20:57:39Z. The SUPERSEDED DUPLICATE is corrects:[C-20260903T191002.936505Z-8cb93821], landed at commit `a7760bf1`, 2026-09-03T19:10:03Z -- one hour and forty-seven minutes EARLIER. **THE FIGURES AGREE AND THE ATTRIBUTIONS DO NOT.** Both rows carry POINT 14.707 core-min, actual 15.0993 core-min gross = cleaned, ratio 1.0267 and waste nil; but the earlier row's gap attribution opens "MISPREDICTION, AND A SMALL ONE -- NOT contention and NOT waste, which stay separately named and are nil here." while the later row's opens "CONTENTION, and it is measured rather than assumed -- NOT waste, and NOT arm misprediction." `CLAUDE.md` rule 12 requires the gap to be ATTRIBUTED; two rows attributing one gap OPPOSITELY leave the ledger unreadable on the single question it exists to answer. **THE CAUSE IS THE SUPERVISOR'S AND IS RECORDED AS THE SUPERVISOR'S.** The heat-transfer supervisor dispatched the second append on a RELAYED CLAIM that the row was "carried in the results record but NOT appended to `docs/COST_CALIBRATION.md`", and DID NOT REQUIRE THE LANE TO DEMONSTRATE THAT ABSENCE BEFORE WRITING. The earlier row had stood at HEAD since 19:10:03Z, before that supervisor session began. This is the lab's own rule-3 failure -- **A ZERO ACCEPTED RATHER THAN DEMONSTRATED** -- committed by the supervisor who spent the same evening requiring planted controls from everyone else. The lane did what it was told and no part of this is attributed to it. | **14.707 core-min -- UNCHANGED, AND NO FIGURE IN EITHER ROW IS ALTERED BY THIS RECONCILIATION.** The registered point at `docs/campaigns/T-family/T25R6cR2_PREREGISTRATION.md` section 6, frozen `f67ade8d` before any case directory held compute; per-run cap 45.0 core-min (3.06x), not breached. Dollars predicted $0.01257, **DERIVED at $0.0513/core-h, REPORTED-BY-OWNER, NEVER MEASURED** -- this box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` section 5). | **15.0993 core-min gross -- UNCHANGED.** Basis `ExecutionTime` (CPU s, rank 0) x 2 ranks / 60, from legA 14.35 + legB 438.63 cpu s: (14.35 + 438.63) x 2 / 60 = 15.099333 core-min, re-derived arithmetically by this lane and agreeing with both rows. Actual $0.01291, **DERIVED, NOT MEASURED**, same rate and same reported-by-owner basis. **THE CAVEAT BOTH ROWS CARRY AND WHICH SURVIVES THIS RECONCILIATION INTACT, because it is exactly the kind of thing a tidy-up destroys: `core-min = ExecutionTime x 2 / 60` IS AN ASSUMPTION, NOT A MEASUREMENT.** `ExecutionTime` is rank 0's CPU time alone (OpenFOAM-v2606 `TimeIO.C:631` -> `cpuTimePosix`, `times(2)`, which reports the CALLING PROCESS ONLY) and CANNOT SEE RANK 1. T25R6c used the identical basis, so the two rungs stay comparable, but the multiplier is named as an assumption and is not upgraded to a measurement here. | **15.0993 core-min -- gross EQUALS cleaned, UNCHANGED, and that equality is a measurement rather than an omission.** No row exceeded 3600 wall s (leg A 14.35 CPU s, leg B 438.63 CPU s under a 1290 s timeout), no leg was restarted and no attempt was discarded. **WASTE: NONE IDENTIFIABLE, NAMED AS ABSENT RATHER THAN OMITTED** (`COMPUTE_BUDGET_CHARTER.md` section 6: waste is named separately and is never laundered into the ratio or its explanation). | **1.0267 -- UNCHANGED. AND IT IS COUNTED ONCE.** **THE CENSUS RULE, which is what a duplicate actually breaks: for any census, count, per-rung lookup or summation over this ledger, corrects:[C-20260903T205738.749257Z-a478fc79] IS THE ROW for T25R6c-R2, and corrects:[C-20260903T191002.936505Z-8cb93821] is a SUPERSEDED DUPLICATE THAT MUST NOT BE COUNTED A SECOND TIME.** A future reader summing rows must not double-count this rung's 15.0993 core-min, and a reader tabulating ratios must not enter 1.0267 twice. | **THE RULING, IN THREE LIMBS, BY THE HEAT-TRANSFER SUPERVISOR, 2026-09-03. (1) NOTHING IS DELETED AND NOTHING IS REWRITTEN.** Both rows stand where they are, byte-unchanged; this ledger is append-only (append rule 1) and `CLAUDE.md` rule 6 governs -- originals are struck, never rewritten. **(2) THE LATER ROW'S ATTRIBUTION IS OPERATIVE: the gap is CONTENTION, measured. THE EARLIER ROW'S ATTRIBUTION LABEL -- "MISPREDICTION, AND A SMALL ONE" -- IS STRUCK. ITS FIGURES ARE NOT STRUCK and remain a correct record of the same run.** The four grounds, and note that limb (2) is decided ON THE EARLIER ROW'S OWN EVIDENCE rather than on preferring the newer row: **(a)** the point was derived from T25R6c's OWN measured per-step rates on the SAME case, arm dictionary, mesh, rank count and box, so the two estimates differ in NO MODELLED QUANTITY -- a misprediction requires something mispredicted, and there is nothing; **(b)** LEG A REPRODUCED TO 0.07 % -- 14.35 cpu s here against T25R6c's 14.36 s on the byte-identical 40-step window, two runs three hours apart (0.0696 % re-derived by this lane): the model is right; **(c)** the rung MEASURED ITS OWN THROUGHPUT FLOOR -- R-R2-3, a **2.150 %** settled-work spread at CONSTANT solver work (92..94 linear-solver iterations/step over all 1070 graded steps) -- against the **~3 %** irreducible floor the registration had declared IN ADVANCE, and the 2.67 % residual sits inside it; **(d)** and the decisive ground, **THE EARLIER ROW'S OWN BODY ALREADY SAYS THIS AND CONTRADICTS ITS OWN HEADER.** Quoted verbatim from that row, its own emphasis preserved: "The residual 2.67 % is the box's own throughput drift, which this rung MEASURED at 2.4297 % on a 40-step window (against T25R6c's 2.915 %) **at constant solver work** — 92..94 linear-solver iterations/step over all 1070 graded steps, a 2.150 % integer spread. Identical work, different cost: the miss is the machine, and it is the same size as the machine." The later row is therefore NOT overruling the earlier one: the ruling is that the earlier row's LABEL is inconsistent with the earlier row's OWN MEASUREMENT, and what that row MEASURED is adopted over what it CALLED it. **(3) THE CENSUS RULE of the ratio cell above binds any reader counting rows.** The earlier row's SURVIVING and unstruck content is its figures, its basis caveat and its estimate lesson -- that pricing a rung from a measurement of that same case landed at 1.027x while pricing its predecessor by analogy to another arm's rate landed at ~0.10x -- none of which this reconciliation disturbs. | `docs/campaigns/T-family/T25R6cR2_PREREGISTRATION.md` section 6 (frozen `f67ade8d`); `docs/campaigns/T-family/T25R6cR2_RESULTS.md` (`05364832`); verdict `verification/runs/T-family/T25R6cR2_LEGAB_runs/T25R6cR2_GRADE_STDOUT.txt` (`286276a1`); completion marker `DONE.W1150_C4_L1` (`23eef25d`); the two ledger rows at lines 400 and 404 of this file as of `e5715c06`, read from `git show <rev>:docs/COST_CALIBRATION.md` and never from the worktree copy. **THE PROVENANCE SPLIT A READER MUST NOT HAVE TO INFER, carried forward from the operative row because a reconciliation is exactly where such a caveat gets lost: THE COST FIGURES came from the REPAIRED grader (blob `947df9df`, landed at `001fc082`), WHEREAS THE VERDICT OF RECORD -- `GATE FAIL`, `rho = 1.103859` -- came from the FROZEN bytes (blob `eb363769`) BEFORE that repair, landed as `T25R6cR2_GRADE_STDOUT.txt` at `286276a1` with its traceback intact. THEY ARE NOT THE SAME INVOCATION.** They agree because all 43 printed gate lines are byte-identical across the repair -- measured, not asserted -- and because the actual re-derives from `DONE.W1150_C4_L1` independently of either grader. **NO RE-GRADE, NO RE-RUN AND NO RECOMPUTED GRADED NUMBER WAS PERFORMED FOR THIS ROW; this defect moved no verdict.** |
| C-20260903T221017.737046Z-a995ad3a | 2026-09-03 | heat-transfer | **K0eR2 -- the `beta`/`g` neutralisation rung of the F14 cooling ladder, GRADED `NOT A RESULT`.** Rule 12's completion trigger is "a rung graded"; this rung was graded by one pinned invocation of `scripts/analyse_k0e.py` (git blob `25ecaa6bca8cd99f4ca1e0a84dba998cde923aaa`, `--expect-sha` armed and matched) and the row is owed. **M4b -- the ONLY gated row, threshold 0 ULP -- is `NOT A RESULT`: NOT DONE under the strict completion rule on the control arm `FP_T00`.** Two arms on one 52,224-cell mesh, `buoyantBoussinesqSimpleFoam`, `endTime` 9000, 2 ranks each. **`FP_T10` COMPLETE** (all six rule-4 conjuncts, 19:46:53Z-20:34:17Z); **`FP_T00` KILLED BY ITS REGISTERED CAP at iteration 350 of 9000** (`rc=124`, 20:46:29Z-21:39:00Z). **NO VERDICT ARITHMETIC IS RESTATED HERE BEYOND THE LABEL; this row is the rule-12 cost comparison only.** | **RUNG POINT 70.10 core-min** = 35.00 (`FP_T10`) + 35.00 (`FP_T00`) + 0.10 (grader), registered at `docs/campaigns/F14-cooling-ladder/K0eR2_PREREGISTRATION.md` section 7.2, frozen at `prereg_commit f306ce66` **before any K0eR2 case directory held compute**. **CEILING 210.30 core-min**; per-arm cap 105.00 (3x POINT), **enforced in metal** as `--timeout 3150` s at 2 ranks by `scripts/launch_k0e.sh` (`:39`: *cap_core_min x 60 / ranks*). **$0.05994 predicted, DERIVED at the owner-stated c7a.4xlarge $0.0513/core-h, `cost_basis` REPORTED-BY-OWNER, NEVER MEASURED** -- this box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` section 5). **THE DENOMINATOR IS THE REGISTERED 70.10 AND IS NOT ADJUSTED IN EITHER DIRECTION TO FLATTER THE RATIO.** | **199.80 core-min MEASURED = $0.17083 DERIVED.** `FP_T10` 2843 wall s x 2 ranks / 60 = **94.767**; `FP_T00` 3151 x 2 / 60 = **105.033**; sum **199.80** exactly. Wall figures from each arm's own `STATUS.<arm>` and **re-derived independently by this lane from those files' own timestamps** rather than accepted as single unchecked numbers: `FP_T10` 19:46:53Z->20:34:17Z = 2844 s and `FP_T00` 20:46:29Z->21:39:00Z = 3151 s, corroborating `wall=2843`/`wall=3151` to within the 1 s rounding of the stamps. **GROSS: contention is NOT netted out of this column** -- it is measured and attributed in the gap cell. **The grader's own cost is NOT separately instrumented**: its registered POINT is 0.10 core-min against a 0.30 cap and the single invocation returned in ~1 s wall at 1 rank, so it is bounded far below its point, **but that is a BOUND, NOT A MEASUREMENT, and it is not entered as one.** **CEILING NOT BREACHED: 199.80 of 210.30, leaving 10.50 core-min -- 315 wall s at 2 ranks.** | **199.80 core-min -- GROSS EQUALS CLEANED, and that equality is a measurement rather than an omission.** `COMPUTE_BUDGET_CHARTER.md` section 2's stall rule matches a ledger row over 3600 wall s; **neither arm reaches it** (2843 s and 3151 s), no arm was restarted and no attempt was discarded, **so nothing is subtracted.** **CRITICALLY: THE WASTE IS *NOT* SUBTRACTED HERE EITHER.** `FP_T00`'s 105.03 core-min bought nothing, but section 6 requires waste to be **named separately and never folded into a column or a ratio** -- so it stays inside this 199.80 and is named in the gap cell. **A reader must not compute a 'cleaned' figure of 94.77 from this row: that would launder the waste out of the ledger, which is the exact manoeuvre section 6 forbids.** | **2.8502** (199.80 / 70.10) -- a **185.0 % overrun** on the registered POINT. Per arm: `FP_T10` **2.7076** (94.767 / 35.00), `FP_T00` **3.0009** (105.033 / 35.00). **CAP USAGE: `FP_T10` 90.3 % of its 105.00 cap; `FP_T00` 100.03 %.** **THE 0.03 core-min BY WHICH `FP_T00` EXCEEDS ITS CAP IS THE ONE SECOND OF `TERM` HANDLING** (`wall=3151` against `timeout_s=3150`) **AND MUST NOT BE READ AS AN OVERRUN THAT WAS ALLOWED TO RUN: the cap FIRED and STOPPED the run.** **THE RATIO MIXES TWO UNRELATED CAUSES AND IS NOT A SINGLE-CAUSE NUMBER** -- see the gap cell, which refuses to average them. | **ATTRIBUTED IN TWO PARTS THAT ARE DELIBERATELY *NOT* MERGED, BECAUSE COLLAPSING THEM WOULD TEACH THE LAB'S ESTIMATOR THE WRONG LESSON.** **(a) `FP_T10`, 2.7076 -- A LOW BASE ESTIMATE, WITH A MEASURED CONTENTION CONFOUND THAT CANNOT BE SPLIT OUT FROM THESE LOGS.** The registered `cost_basis` (section 7.1) applied a **1.25x** solver factor to a **28.00 core-min** `simpleFoam` reference on this exact mesh -- a factor the registration itself flagged as *"an ESTIMATE, not a measurement"* (segregated solves 6 -> 7 for `+T`, i.e. 1.167, rounded up for the `alphaEff` assembly and `alphat` update). **THE MEASURED MULTIPLIER IS 3.38x** (94.767 / 28.00): the added `T` equation cost **2.7 times what the registration allowed for it**. **The contention confound is named rather than used as an excuse:** the same binary on the same mesh has been measured at **1.440 s/it under load and 0.112 s/it free, a factor of 12.9**, and `FP_T10`'s own run-average 0.3126 s/it sits between those bounds -- **so an unknown part of the 3.38x is box load and an unknown part is a genuinely low base factor, and THESE LOGS CANNOT SPLIT THEM. No split is asserted on a guess.** **(b) `FP_T00`, 3.0009 -- NEITHER CONTENTION NOR A LOW BASE ESTIMATE. IT IS A DESIGN DEGENERACY IN THE CONTROL ARM.** The zero-dT control sets `internalField`, inlet and plate all to `uniform 300`, so **`T = 300` is simultaneously the initial condition and the exact solution**; the T equation is satisfied to machine precision before the first sweep, the residual normaliser degenerates to round-off, and **all 349 T solves hit `No Iterations 1000`** (the smoothSolver default; no `maxIter` registered) against **zero of `FP_T10`'s 9000**. The arm performs ~333x the linear-solver work per outer iteration **on a system that is already satisfied**: measured **8.99 s/it against 0.3126, a factor of 28.8, flat and not improving.** **NO ESTIMATING IMPROVEMENT WOULD HAVE PREDICTED THIS FIGURE, BECAUSE IT IS NOT A COST OF THE PHYSICS -- IT IS THE COST OF A SOLVER ITERATING ON ROUND-OFF.** **WASTE -- SEPARATELY NAMED, NEVER LAUNDERED INTO EITHER ACTUAL COLUMN OR INTO THE RATIO (section 6): 105.03 core-min, i.e. 52.6 % OF THE ENTIRE RUNG SPEND BOUGHT NOTHING.** A total loss with no restart point: `writeInterval 9000` means nothing is written before the end, so the killed arm holds `0/` only and **no field, no partial result and no restartable state exists.** Productive spend was **94.767 core-min** (`FP_T10`), and both figures are stated so a reader sees the whole picture. **THE CAP IS RECORDED AS HAVING WORKED, NOT FAILED:** extrapolated at the measured rate `FP_T00` needed **~2,696 core-min -- 25.7x its own 105.00 ceiling and 12.8x the rung's 210.30** -- so the cap stopped a run that **could never have completed**, rather than buying it a new budget; and under `CLAUDE.md` rule 2 it could not have been widened anyway, first compute having closed this rung's caps. **PREDECESSOR SPEND IS *NOT* ABSORBED (section 7.3 of the registration): K0e attempt 1 = 0.00 core-min solver time, class WASTE, it bought nothing; K0eR2 preflight = 0.10 core-min, class PREFLIGHT, NOT WASTE (it answered the function-object question and found two instrument defects). NEITHER ENTERS THE POINT AND NEITHER ENTERS THIS RATIO.** **THE LESSON FOR THE LAB'S ESTIMATES, STATED SPECIFICALLY: a control arm defined by NULLING a driving difference must be costed as a DIFFERENT SOLVE, never as a sibling of the arm it controls.** K0eR2 priced both arms at an identical 35.00 core-min POINT because they share a binary, a mesh, a rank count and every dictionary -- and they then differed in cost by a factor of 28.8. **Sharing every dictionary is not sufficient grounds for sharing a cost estimate when one arm nulls the very gradient the solver iterates on.** **Dollars: predicted $0.05994, actual $0.17083, DERIVED at $0.0513/core-h and NEVER MEASURED** -- reported-by-owner; the box cannot read its own billing (section 5). | `docs/campaigns/F14-cooling-ladder/K0eR2_RESULTS.md` section 10, landed at commit `1b6b710c`; frozen registration `docs/campaigns/F14-cooling-ladder/K0eR2_PREREGISTRATION.md` sections 7.1/7.2/7.3, `prereg_commit f306ce66`. **Verdict of record** `verification/runs/F14-cooling-ladder/K0eR2_runs/K0eR2_GRADE_STDOUT.txt` (`1b6b710c`) -- **the grader writes NO verdict file of its own** (no `write_text` and no `json.dump` exists anywhere in `analyse_k0e.py`), so that artifact is this lane's captured stdout, landed byte-identical under the run directory. **Actuals** `K0eR2_runs/STATUS.FP_T10` and `STATUS.FP_T00`, plus both arms' `log.solve`. **Reference** the 28.00 core-min `simpleFoam` solve on this mesh, cited at registration section 7.1. **Queue rows** `verification/queue/heat-transfer/launched/K0eR2_FP_T10.json`, `K0eR2_FP_T00.json` (`cost_core_min_estimate` 35.0, `cap_core_min_registered` 105.0). **TWO PROVENANCE CAVEATS A READER MUST NOT HAVE TO INFER. (1) THE GRADER EXITED `rc=0` ON THIS `NOT A RESULT`** -- the verdict is taken from stdout and the landed artifact and **never** from the exit code; a reader who scripts against `$?` on this rung reads success. **(2) The grader's stdout banner at `analyse_k0e.py:735` prints the PREDECESSOR's registration path** (`K0e_PREREGISTRATION.md`) while this rung ran under **`K0eR2_PREREGISTRATION.md`**; it is a hard-coded banner string on which nothing in the verdict depends, it follows from the registration's own deliberate decision to keep the `k0e` stem on the script filenames, and **the frozen grader was NOT edited to correct it** (`CLAUDE.md` rule 6; any edit would break the registered pin). **THE THREE RULE-3 PLANTED CONTROLS WERE NOT EXERCISED AND THIS ROW DOES NOT IMPLY THEY WERE:** they live at `:771`, after the both-arms early return at `:757-761`, so they are structurally unreachable on a NOT DONE arm. **NO RE-GRADE AND NO RE-RUN WAS PERFORMED FOR THIS ROW; the grader was invoked exactly once.** |
| C-20260903T223309.077366Z-bc699e66 | 2026-09-03 | dafoam | **⚠ UNIT-LEVEL ROW, NOT THE ITEM ROW — AND IT NAMES THE ITEM ROW IT ANTICIPATES SO NO CENSUS CAN DOUBLE-COUNT.** A1WRT unit **U1 `alpha12_symmetry`** completed (container ran 21:17:19Z–22:11:39Z, `rc=0`, `End` present, `Time = 4000 == endTime`). **THE ITEM IS NOT GRADED:** the frozen grader `a1wrt_read.py` returned, verbatim, `UNIT LOG(S) ABSENT: tail_empty` / `PENDING: the item has not produced every unit's log.` — it grades the ITEM and cannot speak until U2 runs. **THE CENSUS RULE FOR THIS ROW: this is U1's spend ALONE. When A1WRT is graded a SECOND, ITEM-LEVEL row is owed, and a reader summing A1WRT's cost must take EITHER this row OR the item row's U1 component, NEVER BOTH.** Landed now rather than at item completion on the dafoam-supervisor's explicit ruling, because a measured cost not written down is lost if the session dies; landed as unit-level precisely to avoid the duplicate-attribution defect that `C-20260903T215302.846798Z-9d30b9a8` corrects one row above. **NO VERDICT IS RESTATED OR IMPLIED HERE; this row is the rule-12 cost comparison only.** | **32.33 core-min**, registered at `PREREGISTRATION_DRAFT.md` §4.3's U1 line = 30.77 (1 point × 4,000 it × **0.46155 s/it**) + 1.56 (container start + import, 93.85 s MEASURED), frozen at `prereg_commit a62d8d75` **before any A1WRT case directory held compute**. **Cap 361.0 core-min** (11.2× the point), item ceiling 3304.0. Predicted **$0.02764**, **DERIVED at the owner-stated c7a.4xlarge $0.0513/core-h, `cost_basis` REPORTED-BY-OWNER, NEVER MEASURED** — this box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` §5). **THE DENOMINATOR IS THE REGISTERED 32.33 AND IS NOT ADJUSTED IN EITHER DIRECTION.** | **55.317 core-min MEASURED = $0.04730 DERIVED.** Basis: **the launcher's OWN ledger row**, `certonomous-runs/A1WRT/ledger.txt`, `wall_s=3319 ranks=1 core_min=55.317` — host-bracketed wall × ranks ÷ 60, which is the item's own instrumented record and is preferred over any figure this lane derives. **THREE BASES EXIST AND THEY DISAGREE BY ~1.8 %; ALL THREE ARE STATED SO NOBODY HAS TO GUESS WHICH WAS USED.** (i) **host wall 3319 s → 55.317 core-min — THE FIGURE OF RECORD**, and the widest, because it includes container create/start/teardown; (ii) `container_wall_s=3299` → 54.983; (iii) `ExecutionTime = 3259.93 s` (final print in `alpha12_symmetry/out/sweep.log`) → 54.3322, the narrowest, being solver time only. **np = 1, so unlike the multi-rank rows in this ledger there is NO rank-multiplier assumption anywhere here: rank 0 is the only rank.** Per-iteration, from basis (iii) because that is the one comparable to §4.2's anchor: **0.814982 s/it** (3259.93 / 4000). **Cap usage 15.32 % of 361.0 — not breached, not approached.** | **55.317 core-min — GROSS EQUALS CLEANED, and that equality is a measurement rather than an omission.** `COMPUTE_BUDGET_CHARTER.md` §2's stall rule matches a row over 3600 wall s; this run's 3319 host wall s does not reach it, the unit was not restarted and no attempt was discarded, **so nothing is subtracted.** **WASTE: NONE IDENTIFIABLE, NAMED AS ABSENT RATHER THAN OMITTED** (§6). The supervisor's judgement, recorded as his: the core-minutes bought a measured, registered, correctly-refused non-convergence at a tolerance the registration predicted would not be met — an answer, not a loss. **This lane's caveat is recorded beside it so a reader can disagree with him: the run produced NO `CL` and NO `CD`, and whether that makes any of it waste is a judgement, not a measurement.** | **1.7110** (55.317 / 32.33) — a **71.1 % overrun on the ESTIMATE, and 15.32 % of the CAP.** **On the narrowest basis (iii) it would read 1.6805; the ratio of record uses the widest, because an estimate that omitted container start would flatter itself.** The **per-iteration** ratio is **1.7658** (0.814982 / 0.46155); the two differ because the 1.56 core-min container-start term does not scale with occupancy, and both are stated so neither is mistaken for the other. | **`NOT SEPARABLE` — TWO CANDIDATES, BOTH NAMED, NEITHER ASSIGNED. This is a fact about the evidence, not a limit of effort.** **(a) CONTENTION — the leading candidate, and quantitatively consistent rather than merely plausible.** The box was **MEASURED across U1's own window** (21:17:19Z–22:11:39Z, 50 `verification/queue/runner.log` ticks): **min 63.1 %, mean 76.6 %, max 90.5 %** busy, with `W3_chain_r2` alongside. The registration's **own** §4.2 table prices contention **on this exact case, mesh, image and solver**: α = 0 at **1.15400 s/it with 14 containers live = 2.50× the anchor**, and `cold_I_4/14/17` at **1.781–1.810 = 3.86–3.92×**. **U1's measured 1.7658× sits INSIDE that envelope and at its low end**, consistent with the ~1 sibling container actually present rather than 14. **(b) A COLD-versus-CONTINUED MISMATCH IN THE ANCHOR ITSELF, which cannot be ruled out and is the reason no split is asserted.** The 0.46155 s/it anchor is **α = 12 from `sweep_I`, which ran `CONTINUED`** (`a1wr_chain_driver.sh:239`, `launch_unit sweep_I I CONTINUED`). **A1WRT U1 is `COLD`.** The **only** COLD rows on this case anywhere in §4.2 are `cold_I_4/14/17`, and **all three are contaminated by the same 14-container window** — so **there exists NO quiet-box COLD measurement of this case, and the two candidates therefore cannot be separated from these logs.** **NO SPLIT IS ASSERTED ON A GUESS.** **THE LESSON FOR THE LAB'S ESTIMATES, STATED SPECIFICALLY: an anchor carries its START MODE as well as its box state, and a cold unit priced from a continued point inherits an unquantified confound even when the mesh, image, solver, rank count and iteration count are all identical.** §4.2 was unusually careful — it labelled every row's box state as quiet or contended, which is why (a) could be checked at all — **and it still did not label start mode, which is why (b) cannot be.** A future §4.2-shaped table should carry a start-mode column beside its occupancy column. **Dollars: predicted $0.02764, actual $0.04730, DERIVED at $0.0513/core-h and NEVER MEASURED**, reported-by-owner. | Registration `cases/dafoam/ladder-a/A1/wall_resolved_alpha_tail/PREREGISTRATION_DRAFT.md` §4.2 (the anchor and its quiet/contended labels) and §4.3 (the 32.33 U1 line), frozen `a62d8d75`, whole-file md5 at that commit `e58643efd7ab15cfa571daf31749704e`. **Actual** `/home/ubuntu/certonomous-runs/A1WRT/ledger.txt` (the launcher's own `UNIT=alpha12_symmetry ... rc=0 wall_s=3319 ranks=1 core_min=55.317` row, which also records `container_wall_s=3299`, `cpuset=10`, `memavail_pre_GiB=26.11` and `siblings_pre=[d12y_w3_S5_20260903T172242Z_179692]` — the one sibling container named, corroborating the contention limb) and `/home/ubuntu/certonomous-runs/A1WRT/alpha12_symmetry/out/sweep.log` (final `ExecutionTime = 3259.93 s`) and `/home/ubuntu/certonomous-runs/A1WRT/alpha12_symmetry_STAGING_EVIDENCE.txt`. **Box occupancy** `verification/queue/LAUNCH_LOG.tsv`'s sibling `verification/queue/runner.log`, 50 ticks inside the run window, aggregated by this lane. **Queue row** `verification/queue/dafoam/launched/A1WRT_U1_alpha12_symmetry.json` (`cost_core_min_estimate` 32.33, `cap_core_min_registered` 361.0, `ranks` 1), launched by the daemon at 21:16:49Z. **Grader output** from one invocation of `a1wrt_read.py`, blob `705db5f7e972f6c033cbe303b7a6038f`, **hashed against its committed blob in the invoking shell immediately before invocation** — committed blob == disk == registered pin, all three identical. **THREE PROVENANCE CAVEATS A READER MUST NOT HAVE TO INFER. (1) THE GRADER EXITED `rc=0` ON A `PENDING`** — the state is taken from stdout, never from the exit code. **(2) NO VERDICT EXISTS FOR A1WRT AND THIS ROW DOES NOT SUPPLY ONE**; the item is `PENDING` until U2 has run. **(3) THE ITEM'S U2 IS PREDICTED TO ABORT** at `DACheckGeometry.C:278` on a 2-direction `empty` mesh, registered in advance at ADDENDUM 7 (commit `d1665410`) **before U2 was dropped**; if that prediction holds, the item row owed after this one will carry a U2 actual of order 1.5 core-min against a 255.39 estimate, and **that asymmetry will be a fact about the case and not an estimating failure.** **NO RE-RUN AND NO RE-GRADE WAS PERFORMED FOR THIS ROW; the grader was invoked once, and once more only because this lane's first invocation used a wrong argv form and was REFUSED by the reader's own guard without reading anything.** |
| C-20260903T223927.980747Z-4bed86f0 | 2026-09-03 | dafoam | **SO-3aF2 MESH arm (`curriculum_SO3a_multipoint`, feasibility probe) — ARM COMPLETE, `rc=0`, on the FOURTH launch.** Rule 12's completion trigger is a completed process; this arm completed. **THE ARM IS COMPLETE AND IS NOT A RESULT**: the registration carries no aspect-ratio, non-orthogonality, skewness or `checkMesh` threshold — MESH is a SCRIPT arm whose product is the mesh, and predictions F1–F5 are all the XM arm's. The item stays `PENDING`; `checkMesh`'s `Mesh OK.` is its judgement against its own defaults, not this lab's against a pre-registered threshold. | **0.30 core-min**, registered at `cases/dafoam/ladder-a/A1/feasibility_SO3a_multipoint/FEASIBILITY_PREREGISTRATION.md` section 7, frozen `3b85e47f` 2026-08-31. Per-arm cap **3.0**, item ceiling **9.0**. THE ESTIMATE IS STRONGER THAN AN ESTIMATE: section 7 anchors it on SO-3aR's own MESH arm at **0.267 core-min MEASURED** off this box's ledger (`ARM=MESH rc=0 wall_s=16 core_min=0.267`), so 0.30 is a measured anchor with headroom rather than a bracket or a guess. | **0.0500 core-min.** Basis: wall **3 s** x **1 rank** / 60, captured inside the detached launcher and written to the item's own `ledger.txt` (`STAMP=2026-09-03T223426Z rc=0 wall_s=3 ranks=1 core_min=0.0500 cap_exceeded=0`). Cap 3.0 never approached — 1.7 % of it. | **0.0500 core-min — gross equals cleaned.** `COMPUTE_BUDGET_CHARTER.md` section 2's stall rule matches a row over 3600 wall s; this arm ran 3 s, so no row is a stall and nothing is subtracted. | **0.167** (0.0500 / 0.30). **READ THE ATTRIBUTION BEFORE THIS NUMBER — it calibrates ONE SCRIPT ARM, NOT THIS ITEM.** | **THE RATIO IS HONEST AND ITS SCOPE IS NARROW, AND THE ROW SAYS SO RATHER THAN LEAVING A READER TO ASSUME OTHERWISE.** A 0.167 on a **three-second mesh script** is a calibration of a `preProcessing.sh` + `checkMesh` invocation and NOTHING ELSE. It says nothing about the XM arm, which has not run, carries the item's entire predictive content (F1–F5), and is estimated separately at 1.20 core-min from a bracket of two MEASURED anchors. **The same discipline `D6RF`'s row used when it declined to print a ratio it could not support**: state the scope in the cell, not in a footnote. **GAP ATTRIBUTION: MISPREDICTION, MILD AND EXPLAINED.** The 0.267 core-min anchor came from SO-3aR's MESH arm at **16 wall s**; this ran in **3 s** on the same 4,032-cell 2-D case, same image digest, same 1 rank. The anchor was not wrong — it measured a real prior run — and the difference is host contention at the two moments, not a modelling error: the box read **56.7 % busy** at this launch. **NOT CONTENTION IN THE OVERSPEND SENSE and NOT arm misprediction.** **WASTE, NAMED SEPARATELY AND NEVER ABSORBED INTO THE RATIO** (`COMPUTE_BUDGET_CHARTER.md` section 6): **0.0167 core-min across THREE prior launches of this same arm, all `rc != 0`, none of which produced a number.** (1) 21:48:15Z `rc=127`, the arm had **no staging step at all**; (2) 22:12:06Z `rc=127`, the arm command **never sourced the DAFoam environment**; (3) 22:24:16Z `rc=1`, `set -u` **aborted inside the foreign init script** and the instrument emitted neither a refusal nor its OK line. Each was a real defect in the delivery path, each was found for essentially nothing, and all three are closed — inputs, environment, and the shell option that hid both. **Total spend on this arm across all four launches: 0.0667 core-min.** **Dollars: predicted $0.000257, actual $0.0000428, waste $0.0000143, all DERIVED at $0.0513/core-h and NEVER MEASURED** — reported-by-owner; the box cannot read its own billing (section 5). | `cases/dafoam/ladder-a/A1/feasibility_SO3a_multipoint/FEASIBILITY_PREREGISTRATION.md` ADDENDUM 5 and `so3af2_stage2_evidence.txt`, landed at commit `c0a3d3e9`; cost figures cited from `/home/ubuntu/certonomous-runs/CURRICULUM-SO3aF2-a1-naca0012-multipoint-feasibility/ledger.txt`. Queue row `verification/queue/dafoam/launched/SO3aF2_MESH_r4.json`. The three failed launches' run roots are ARCHIVED, never deleted, each with preservation asserted by count. Checks 1 and 4 discharged personally by `dafoam-supervisor` before each launch. **THE XM ARM'S CALIBRATION ROW IS OWED SEPARATELY AND IS NOT COVERED BY THIS ONE.** |
| C-20260903T224408.032809Z-bdbf23b5 | 2026-09-03 | dafoam | **⚠ UNIT-LEVEL ROW FOR A1WRT unit U2 `tail_empty`, THE SECOND AND LAST OF THIS ITEM'S UNITS — companion to `C-20260903T223309.077366Z-bc699e66` (U1). THE CENSUS RULE: these two unit rows together are A1WRT's whole spend. If an ITEM-level row is ever written it must NOT be added to these; a reader takes EITHER the two unit rows OR the item row, NEVER BOTH.** U2 launched 22:33:21Z and the container exited **1 second later**: `rc=97`, **zero time steps**, aborted inside the container at the OpenFOAM mesh check — `Mesh has 2 solution (non-empty) directions (1 1 0)` then `Mesh geometric directions is less than 3 and not supported!` then `Signal: Aborted (6)`. **NO VERDICT IS RESTATED OR IMPLIED; this row is the rule-12 cost comparison only, and no item verdict exists on record — the frozen grader is a feasibility reader and emits none.** | **255.39 core-min**, registered at `PREREGISTRATION_DRAFT.md` §4.3's U2+U3 line = 215.37 (7 points × 4,000 it × 0.46155 s/it) + 1.56 (container start) + 38.46 (tail-stiffening allowance, **EXTRAPOLATED**, and named in §4.3 as the term most likely to carry the error), frozen at `prereg_commit a62d8d75`. **Cap 2943.0 core-min**; in-container deadline 176,280 s. Predicted **$0.21836 DERIVED at $0.0513/core-h, REPORTED-BY-OWNER, NEVER MEASURED** (`COMPUTE_BUDGET_CHARTER.md` §5). | **0.683 core-min MEASURED = $0.00058 DERIVED.** Basis: the launcher's own ledger row, `certonomous-runs/A1WRT/ledger.txt`, `UNIT=tail_empty ... rc=97 wall_s=41 ranks=1 core_min=0.683`, with `container_wall_s=38`. **There is no `ExecutionTime` basis for this unit and none is invented: the solver never printed one, because it never took a time step.** Cap usage **0.023 %** of 2943.0. | **0.683 core-min — gross EQUALS cleaned.** No stall (41 s against the 3600 s rule), no restart, nothing discarded. **WASTE: THE WHOLE 0.683 core-min BOUGHT NO CL, NO CD AND NO FIELD — AND IT IS NAMED HERE RATHER THAN NETTED OUT** (`COMPUTE_BUDGET_CHARTER.md` §6: waste is named separately and never laundered into a column or a ratio). **BUT IT IS NOT A LOSS, AND THE DISTINCTION IS THE POINT: it bought a REGISTERED PREDICTION'S ANSWER.** The prediction that U2 would abort at `DACheckGeometry` with zero time steps was landed at commit `d1665410` **BEFORE the row was dropped**, and 0.683 core-min is what it cost to test it. **A measurement that refutes a registered arm is a result, not waste — and the arm it refuted was worth 255.39 core-min.** | **0.0027** (0.683 / 255.39) — a 99.7 % under-spend. **⚠ THIS RATIO IS NOT A CALIBRATION OF ANYTHING AND MUST NOT BE ENTERED INTO ANY ESTIMATE-QUALITY AVERAGE.** The estimate priced **seven converged points**; the arm **could never have run a single iteration**, because DAFoam refuses a 2-direction mesh in its own source before the first time step. **An under-spend ratio on an arm that was impossible is a statement about the arm's validity, not about the estimator's accuracy** — the 255.39 was a correct price for a solve that was never available to be bought. **A reader averaging this 0.0027 with U1's 1.7110 would produce a number describing nothing.** | **NOT AN ESTIMATING GAP AT ALL. THE ARM WAS UNSATISFIABLE BY CONSTRUCTION, AND IT WAS INVALIDATED AFTER IT WAS REGISTERED — WHICH IS A DIFFERENT ANIMAL FROM A DRAFTING DEFECT.** `DACheckGeometry.C:278` in the registered patched image refuses `nGeometricD < 3` unconditionally, and an `empty` bounding plane on this one-cell-thick 2-D mesh gives **2**. **MEASURED TWICE, IN TWO INDEPENDENT ITEMS, ON THE SAME IMAGE:** A1ZE's `Ec` arm (`rc=97`, `wall_s=88`, **1.4667 core-min**) at 19:05Z, and this arm at 22:33Z. **So `empty` is categorically unusable for this case family in this image and should never again be described as a single-item result.** **AND A1WRT COULD NOT HAVE KNOWN: it froze at `a62d8d75` BEFORE A1ZE ran** — measured on the frozen document, `DACheckGeometry` 0 hits, "geometric direction" 0, `nGeometricD` 0, `A1ZE` 0. **This is the ladder learning something that retroactively kills a registered arm, which is what a ladder is for; it is NOT a drafting defect and must not be counted alongside them.** **THE LESSON FOR THE LAB'S ESTIMATES, AND IT IS NOT ABOUT ARITHMETIC: an estimate prices a solve, and it silently assumes the solve is AVAILABLE. Nothing in a cost model can detect an arm the toolchain refuses in its own source.** What catches this is a **pre-compute feasibility probe against the registered image** — which is exactly what A1ZE was, at 1.4667 core-min, and it would have saved this arm's registration had it existed first. **Dollars: predicted $0.21836, actual $0.00058, DERIVED and NEVER MEASURED.** **One caveat on the prediction's own accuracy, recorded because it was registered in advance: it said "order of 1.5 core-min" (anchored on A1ZE's 1.4667) and measured 0.683 — right order, LOW BY A FACTOR OF 2.1. Shape, exit code, mechanism and zero-time-steps were exact; the magnitude was not.** | Registration `cases/dafoam/ladder-a/A1/wall_resolved_alpha_tail/PREREGISTRATION_DRAFT.md` §4.3 (the 255.39 U2+U3 line), frozen `a62d8d75`, whole-file md5 at that commit `e58643efd7ab15cfa571daf31749704e`. **The prediction** ADDENDUM 7, commit `d1665410`, landed **before** the drop. **Actual** `/home/ubuntu/certonomous-runs/A1WRT/ledger.txt` (`UNIT=tail_empty ... rc=97 wall_s=41 core_min=0.683 container_wall_s=38 patch_identity=empty declared=7 point_end_markers=0 cpuset=10`). **The abort** `/home/ubuntu/certonomous-runs/A1WRT/tail_empty/out/sweep.log` and `out/rc` (=97). **The corroborating item** `/home/ubuntu/certonomous-runs/A1ZE/Ec/out/sweep.log` and its ledger (`rc=97 wall_s=88 core_min=1.4667`), with its sibling `A1ZE/Sc/out/sweep.log` showing `3 solution (non-empty) directions (1 1 1)` and **no abort** — the two arms differ in the treatment and in nothing else. **Queue row** `verification/queue/dafoam/launched/A1WRT_U2_tail_empty.json`. **TWO PROVENANCE CAVEATS. (1) THE IMAGE COMPARISON IS NOT DIGEST-TO-DIGEST:** A1WRT's `S7` asserted the digest `sha256:2927768a16ac…`; **A1ZE's record names only the TAG `dafoam-idwarp-rot:v1`**, which resolves to that digest on this box today with no record of a rebuild between the two runs — **strong, but `DAFOAM_CHARTER.md` §6 is explicit that a version string is not an identity.** **(2) THE STAGING REPAIRS ARE NOT IMPLICATED AND THE RECORD SHOULD NOT LET ANYONE THINK THEY ARE:** `S0b`, `S4` (converted to `empty` AND the opposite identity refused on the same bytes), `S5b`, `S5c` and `S6` all passed for this unit; the abort is downstream of all of them, inside the container, and is a fact about DAFoam. |
| C-20260903T225326.206492Z-068ffbe9 | 2026-09-03 | dafoam | **SO-3aF2 XM arm (`curriculum_SO3a_multipoint`, feasibility probe) — ARM RAN AND REFUSED AT `rc=7`, `RESIDUAL_HISTORY_UNAVAILABLE`.** Rule 12's completion trigger is a completed process; this arm completed. **THE REFUSAL IS THE REGISTERED RISK LANDING, NOT A DEFECT AND NOT A CRASH** — section A2.3 named this outcome and its reason **before any container ran**, so that if it fired it would read as the risk landing rather than as a discovery. **No artefact was written, which is the correct outcome:** a short `residual_histories` list would have reached the reader as a **CONVERGENCE DISAGREEMENT** — which section 5 F1 defines as *a finding about the multipoint assembly* — when its true cause was the producer failing an attribute lookup. An infrastructure failure wearing a physics finding's clothes is worse than not running. **The item stays `PENDING` and F1–F5 are UNSCORED**; the reader was never invoked because no artefact exists for it to read. | **1.20 core-min**, registered at `FEASIBILITY_PREREGISTRATION.md` section 7, frozen `3b85e47f` 2026-08-31. Per-arm cap **6.0**, item ceiling **9.0**. **THE POINT IS A BRACKET, NOT A SINGLE ANCHOR, AND SECTION 7 LABELS IT AS ONE:** bounded below by SO3aF's **0.5833 core-min MEASURED** for three cold primals in one container, and above by SO-3aR's `X-S` arm at **1.533 MEASURED**, which bought three primals PLUS a baseline PLUS a partial adjoint before dying — so a primal-only multipoint sits strictly below it. 1.20 is **[DERIVED from two MEASURED figures], NOT MEASURED**. | **0.3500 core-min.** Basis: wall **21 s** x **1 rank** / 60, captured inside the detached launcher and written to the item's own `ledger.txt` (`ARM=XM STAMP=2026-09-03T224931Z rc=7 wall_s=21 ranks=1 core_min=0.3500 cap_exceeded=0`). Cap 6.0 never approached — **5.8 %** of it. | **0.3500 core-min — gross equals cleaned.** `COMPUTE_BUDGET_CHARTER.md` section 2's stall rule matches a row over 3600 wall s; this arm ran 21 s, so no row is a stall and nothing is subtracted. | **0.292** (0.3500 / 1.20). **READ THE SCOPE BEFORE THIS NUMBER — IT PRICES THE ARM UP TO THE REFUSAL POINT, NOT A COMPLETED XM PASS.** | **THE RATIO'S SCOPE IS NARROW AND THE CELL SAYS SO RATHER THAN LEAVING A READER TO ASSUME OTHERWISE.** The 1.20 bracket priced a **full multipoint XM pass**. What ran was **three primals to convergence plus a registered refusal** — `Running Primal Solver` x3 and `satisfied the prescribed tolerance` x3, then `rc=7` before the artefact was written. **So 0.292 calibrates the arm UP TO THE REFUSAL POINT and NOT the pass**, and a completed XM will cost more than this. The same discipline the MESH row used when it stated that a 0.167 on a three-second script calibrates one SCRIPT arm and not the item: **state the scope where the number is, not beneath it.** **GAP ATTRIBUTION: NOT A MISPREDICTION AND NOT CONTENTION — THE PASS DID NOT COMPLETE.** The bracket was not tested, because the work it priced was not all performed. The bracket's own anchors stand untouched and are carried forward for the successor. **WASTE: NONE — RULED.** Every core-minute of the 0.3500 ran **three converged primals** and produced a **registered refusal that prevented a manufactured finding**. **THAT IS AN ANSWER, NOT A LOSS**, and it is the same ruling made on A1WRT U1 for the same reason. **ITEM TOTAL ACROSS FIVE LAUNCHES: 0.4167 core-min** — 0.0667 for the MESH arm across four launches (three of which were delivery-path failures, named as waste in that arm's own row) and 0.3500 here. **Dollars: predicted $0.001026, actual $0.000299, all DERIVED at $0.0513/core-h and NEVER MEASURED** — reported-by-owner; the box cannot read its own billing (section 5). | `cases/dafoam/ladder-a/A1/feasibility_SO3a_multipoint/FEASIBILITY_PREREGISTRATION.md` ADDENDUM 7 and `so3af2_stage2_evidence.txt`, landed at commit `a58843d6`; figures cited from `/home/ubuntu/certonomous-runs/CURRICULUM-SO3aF2-a1-naca0012-multipoint-feasibility/ledger.txt` and `XM/XM.log:1799`. Queue row `verification/queue/dafoam/launched/SO3aF2_XM.json`. Checks 1 and 4 discharged personally by `dafoam-supervisor` before the launch. **A BOUNDED DIAGNOSTIC IS ORDERED AND NOT YET BUILT** — an attribute census on the live solver object, scoring nothing and writing no artefact — and its own calibration row is owed separately. **THE MESH ARM'S ROW IS FILED SEPARATELY AND DOES NOT COVER THIS ARM.** |
| C-20260903T231027.144976Z-4a016daa | 2026-09-03 | cfd | **F28G L1 (`dp1000`, `U_inf 20`) — GRADED, `NOT A RESULT`.** Predicted **144.0** core-min (registration §8 L1 row: 35,544 cells x 4.05e-6 s anchored `t_cell` x 15,000 iters = 2,159 wall s x 4 ranks), frozen `95974b79`. Actual **56.21** core-min gross = **cleaned** (843.21 wall s x 4 ranks; `_launch.started_epoch` 1788472680.795 -> `RUN_STATUS` `end=22:12:04Z`; the single 843 s row is far under the 3,600 s stall rule, so gross and cleaned coincide). Solver-only cross-checks: `ExecutionTime` 831.9 s = 55.46, `ClockTime` 841 s = 56.07 core-min. **Ratio 0.390** cleaned/predicted. **Waste 0.000** — no discarded or re-launched arm; the moved preflight artifact consumed zero compute. Dollars **DERIVED, NOT MEASURED**: **$0.0481** actual against $0.1231 predicted, at $0.0513/core-h c7a.4xlarge, reported-by-owner (`COMPUTE_BUDGET_CHARTER` §5). No cap breached — 39 % of the row's 144 core-min share, against a 900 core-min triple cap. **GAP ATTRIBUTION — TWO TERMS IN OPPOSITE DIRECTIONS, and the second is named separately rather than absorbed into the ratio (§6).** **(a) Misprediction, over-predicting, dominant:** §8 anchored `t_cell` at 4.05e-6 s/cell/iter from the *jet-flap lane's* fitted `N^0.55` model; actual **1.5815e-6** (anchor 2.56x high), and this case's OWN feasibility arm `FEAS_L1_dp1000_U20_A2` had already measured **1.3123e-6** (anchor 3.09x high) — a figure standing in this ledger as **`C-20260833`** three days before the launch, in its own words "an L1 arm costs ~41 core-min, not 10". **The registration priced this rung from a foreign lane's model while a direct measurement of the same case, solver, rank count and box sat in this file.** **(b) Contention, slowing, present and masked by (a):** F28G ran **1.205x slower per cell per outer iteration** than that feasibility arm (cell count divided out; 1.1195x of the 1.349x wall ratio), and it was **not** doing more work — **fewer** GAMG `p` linear iterations per outer (14.617 vs 18.136), so normalised to solver work it is **1.49x slower** (0.1082e-6 vs 0.0724e-6 s/cell/`p`-iter). Decomposition imbalance excluded (max/min 1.019). **Net of contention the misprediction alone is 0.32x**; reporting 0.390 as pure misprediction would understate it. **NOT VERIFIED AND SAID SO:** no contemporaneous box-load record for 21:58–22:12Z exists on disk, so the "56–58 % busy" prior reading is neither confirmed nor refuted; the 1.205x is **measured**, its attribution to contention specifically is an **inference** (corroborated but not contemporaneously: load average 34.68 on 16 cores at 23:06Z). **The verdict is `NOT A RESULT` and this spend bought a finding, not a graded value** — the run hit its registered 15,000-iteration cap (parent §8: HIT CAP -> NOT A RESULT) and is 49.8x off its plateau criterion. | `verification/campaign/F28G_L1_RESULTS.md`; `verification/runs/F28_runs/F28G_L1_dp1000_U20/GRADING_OUTPUT.txt`; prereg frozen `95974b79` |
| C-20260903T231235.965191Z-70cb5490 | 2026-09-03 | dafoam | **SO-3aF2 ATTRCENSUS arm (`curriculum_SO3a_multipoint`, bounded attribute-census diagnostic) — OVERRAN ITS CAP AND WAS STOPPED BY THE LANE. `rc=9`, `cap_exceeded=1`. NO ENUMERATION WAS PRODUCED.** Rule 12's completion trigger is a completed process; this one completed **by intervention**, and its cost is measured, so the duty is live. **THE STOP IS AN INTERVENTION, NOT A MEASUREMENT** (`DAFOAM_CHARTER.md` section 7): it is recorded in the item's own ledger by name, `stopped_by=dafoam-lab-lane`, with an explicit note that it **claims nothing about why the run was slow**, and no diagnosis of the slowness is offered anywhere. **The arm's only product was `so3af2_attr_census.json` and it was never written**, so the producer's attribute question is exactly where it was. | **0.40 core-min**, registered at `FEASIBILITY_PREREGISTRATION.md` ADDENDUM 8 section A8.3. Cap **2.0 core-min**, deliberately set well under the 6.0 the XM arm did not approach. The point was **[DERIVED from a MEASURED figure on this box]** — the XM arm ran the same three primals to its refusal for **0.3500 core-min MEASURED** — and **not measured**. | **3.6833 core-min.** Basis: wall **221 s** x **1 rank** / 60, from the launcher's own ledger row (`ARM=ATTRCENSUS STAMP=2026-09-03T230242Z rc=9 wall_s=221 cap_exceeded=1`). **TWO LEDGER ROWS EXIST FOR THIS ONE EVENT** — the launcher's (221 s, to container exit) and the lane's intervention note (222 s, to the stop command returning). **THEY ARE THE SAME EVENT AND MUST NOT BE DOUBLE-COUNTED; the launcher's is the figure of record.** | **3.6833 core-min — gross equals cleaned.** `COMPUTE_BUDGET_CHARTER.md` section 2's stall rule matches a row over 3600 wall s; this ran 221 s, so it is not a stall by that rule and nothing is subtracted. **That the run was nevertheless stopped for exceeding its own registered cap is recorded here rather than absorbed into a cleaning adjustment.** | **NO RATIO. DELIBERATELY DECLINED — the reason is in this cell and not in a footnote.** | **THE RATIO IS REFUSED BECAUSE AN ACTUAL/PREDICTED ON AN ARM THAT PRODUCED NO PRODUCT IS NOT A CALIBRATION OF ANYTHING.** 3.6833 / 0.40 = 9.21 is **arithmetic, not calibration**: it would read as a catastrophic estimating failure, and the estimate was never tested. **The run did not do the work the estimate priced** — it produced 208 bytes of log, static for roughly two minutes at 1.55 % CPU, and was then stopped. **An estimate is calibrated by a run that completes the work, and this one was stopped rather than mispriced.** Same discipline `D6RF`'s row used when it declined a ratio it could not support: **put the reason where the number would have been.** The 0.40 point estimate and its MEASURED 0.3500 anchor are **carried forward UNTESTED** and are not adjusted against a stopped run. **WASTE: 3.6833 core-min, ALL OF IT, NAMED AS A LOSS BECAUSE IT WAS ONE** (`COMPUTE_BUDGET_CHARTER.md` section 6), and never absorbed into any ratio. **This is the opposite ruling to the XM arm's**, and the distinction is the point: XM's 0.3500 bought **three converged primals and a registered refusal that prevented a manufactured finding** and was ruled **an answer, not a loss**; this arm bought **nothing**. **ITEM TOTAL ACROSS SIX LAUNCHES: 4.1000 core-min** — 0.0667 MESH (four launches), 0.3500 XM, 3.6833 here. **AND THE STRUCTURAL FINDING THIS SPEND PAID FOR, WHICH IS WHY THE LOSS IS RECORDED RATHER THAN REGRETTED: the registered cap could not stop the run.** The launcher's cap check runs after `docker wait` returns, so it reported the overrun only because a lane had already stopped the container; left alone, `docker wait` blocks indefinitely and the stop rule never fires. Repaired at ADDENDUM 10 by an in-container `timeout -k` deadline derived from `a1wr_chain_driver.sh`'s own bytes, which fires with no agent alive — this arm's deadline is **105 s**, against the **199 s** it had reached when stopped by hand. **Dollars: predicted $0.000342, actual $0.00315, all DERIVED at $0.0513/core-h and NEVER MEASURED** — reported-by-owner; the box cannot read its own billing (section 5). | `cases/dafoam/ladder-a/A1/feasibility_SO3a_multipoint/FEASIBILITY_PREREGISTRATION.md` ADDENDUM 9 (the overrun and the stop) and ADDENDUM 10 (the deadline repair), with `so3af2_stage2_evidence.txt` carrying the ledger rows, the whole 208-byte arm log, the pre-stop container state and the deadline drive. Queue row `verification/queue/dafoam/launched/SO3aF2_ATTRCENSUS.json`. Checks 1 and 4 discharged personally by `dafoam-supervisor` before the launch. **THE MESH AND XM ROWS ARE FILED SEPARATELY AND NEITHER COVERS THIS ARM.** |
| C-20260903T232103.275964Z-1898aae5 | 2026-09-03 | dafoam | **⚠ UNIT-LEVEL ROW FOR D6RF2 ARM `F_mp` — THE FIRST SOLVER COMPUTE EVER SPENT IN THIS LINEAGE, AND IT NAMES THE ITEM ROW IT ANTICIPATES.** **THE CENSUS RULE: this is `F_mp`'s spend ALONE. `REF_off` HAS NOT RUN. If an item-level row is ever written it must NOT be added to this one — a reader takes EITHER this row (plus a future `REF_off` row) OR the item row, NEVER BOTH.** A container ran — `d6rf2_F_mp_20260903T230951Z_937264`, 23:10:12.066Z to 23:10:59.274Z — and exited **`rc=2`, which is the KERNEL's exit code** (`source=launcher_exit=docker_inspect_ExitCode`), not a launcher refusal; `inspect(exit,oomkilled)=[2 false]`, so not an OOM. **Every guard passed before it, for the first time in four fires:** `G-DELIVERY OK 8`, `G-ANCHOR OK readers=2`, `G-COLD OK`, `CMDFILE units_gate=present`, `RUNAWAY_GUARD` armed. **NO VERDICT IS RESTATED OR IMPLIED; this row is the rule-12 cost comparison only.** | **⚠ 155.70 core-min — AND ITS PROVENANCE IS A FINDING THIS ROW MAY NOT SMOOTH OVER.** The per-arm figures **155.70 (`F_mp`) and 60.07 (`REF_off`)**, and their sum **215.77**, appear **ONLY IN THE QUEUE ROWS** — `verification/queue/dafoam/launched/D6RF2_chain.json`'s `calibration_row_owed` field and `cost_core_min_estimate`. **DEMONSTRATED, NOT ASSUMED: grep for 155.70, 60.07 and 215.77 against the frozen `PREREGISTRATION.md` returns 0, 0 and 0 hits; against the queue row, 2, 2 and 1.** **`CLAUDE.md` rule 12 requires every run to be costed IN ITS PRE-REGISTRATION, and this item's per-arm cost figures are not in that document.** They are in a committed record (the queue row) and are used here on that basis, cited as such, **but the discrepancy is named rather than papered over** — this ledger's append rule 2 requires a figure to come from a committed record and to be stated as absent where it is not, and the pre-registration side is absent. **Registered CAPS, by contrast, ARE in the frozen document: `F_mp` 480.0, `REF_off` 190.0, ceiling 670.0.** Predicted **$0.13312 DERIVED at $0.0513/core-h, REPORTED-BY-OWNER, NEVER MEASURED** (`COMPUTE_BUDGET_CHARTER.md` section 5). | **4.6 core-min MEASURED = $0.00393 DERIVED.** Basis: the launcher's **own** ledger row, `ARM=F_mp … rc=2 wall_s=69 ranks=4 core_min=4.6`, host-bracketed wall × ranks ÷ 60, preferred over any figure this lane derives. The same row records `container_wall_s=47`, so the two bases differ by the 22 s of container create/start/teardown; **the wider figure is the one of record**, as in the A1WRT rows. **Cap usage 0.96 % of 480.0** — the cap was never approached and never fired. `memavail` 27.29 GiB before, 27.31 after; `cpuset=2,3,4,14`; `memory=20g`; image `dafoam-idwarp-rot:v1` at the registered digest. | **4.6 core-min — gross EQUALS cleaned.** No stall (69 s against the 3600 s rule), no restart, nothing discarded. **WASTE: NONE. AND THIS IS A RULING, NOT AN OMISSION** (`COMPUTE_BUDGET_CHARTER.md` section 6 requires waste to be named separately, so its absence is named too). The dafoam-supervisor's judgement, recorded as his: the 4.6 core-min **bought a measured, registered refusal that establishes a registration defect reaching back through three items and could not have been established any other way.** **THE THREE PRIOR FIRES OF THIS ITEM ARE SEPARATELY ACCOUNTED AND ARE NOT IN THIS FIGURE: each aborted before any container at `spent_before_arm=0.000`, so the item's cumulative solver compute is 4.6 core-min in total.** | **0.02954** (4.6 / 155.70) — a **97.0 % under-spend.** **⚠ THIS RATIO IS NOT A CALIBRATION OF THE ESTIMATOR AND MUST NOT ENTER ANY ESTIMATE-QUALITY AVERAGE.** The 155.70 priced an arm **running to completion**; the arm **could not complete as registered**, because the extractor it must pass demands a per-major CD the history it reads does not contain and never contained. **An under-spend ratio on an arm that could not have finished is a statement about the REGISTRATION, not about the estimator's accuracy** — the 155.70 was a defensible price for a run that was never available to be bought. **A reader averaging this 0.02954 with any completed arm's ratio would produce a number describing nothing.** It is the same shape as the A1WRT `tail_empty` row's 0.0027 and must be treated the same way. | **NOT AN ESTIMATING GAP. A REGISTRATION DEFECT, AND IT REACHES BACKWARD THROUGH THREE ITEMS.** `d6r_extract_endpoint.py:70` refused — *"per-major CD for cl04 absent from history"* — with **both of its own frozen-instrument controls passing immediately after** (`C1` 1743dd42…, `C2` 137539e0…, each matching its pin): **the instrument is the file it was frozen as, and the artefact is short.** The staged `OptView.hst` is byte-identical to D6R's own (md5 70fafa07bdee618fef13039433c01114), **so staging did not damage it**; the history carries exactly three `aero_post.functionals.*` keys, all CL, and **no .CD key for any scenario.** **THE MESSAGE'S SCOPE IS MISLEADING AND THE ROW SAYS SO: it names cl04 only because the loop is `for pt in POINTS: for q in ("CL","CD")` and cl04/CD is the first missing pair — it never reaches cl05. The quantity a VERDICT depends on is cl05's CD: `d6rf2_grade.py:792` reads `hist["CD_cl05"][-1]`, computes `price = cd_mp - cd_f`, and a negative price sets verdict NOT A RESULT at :795-797. A refusal names the first missing thing, never the load-bearing one — and a successor repairing cl04 alone would get a clean extraction and still not grade.** **CD IS NOT LOAD-BEARING FOR THE FD TABLE'S ENDPOINT:** `d6r_extract_endpoint.py:53-55` builds the endpoint design vector from KEYS at :25 as `arr[-1,:]`, the CD block at :65-77 runs after it, and `_final_CD_*` is read by **nothing** in the container. **AND D6R's AND D6RF's OWN `F_mp` WOULD HAVE FAILED IDENTICALLY**, reading the same history through the same extractor — **no container had ever run `F_mp` in any lineage, so nobody could have discovered it.** **THE LESSON FOR THE LAB'S ESTIMATES: an estimate prices a solve and silently assumes the solve can COMPLETE. Nothing in a cost model detects an arm whose own frozen extractor demands a field its input has never carried — that is caught only by running it, which is what this 4.6 core-min bought.** **Dollars: predicted $0.13312, actual $0.00393, DERIVED and NEVER MEASURED.** | Frozen registration `cases/dafoam/ladder-a/A2/curriculum_D6RF2/PREREGISTRATION.md`, prereg_commit 3af45c05f0740124de2646dde93df240d471285b, whole-file md5 at that commit c9b8df447aaa484ba49b534caaf5fd68 — **which does NOT contain the estimate figures, per the estimate cell.** **Estimate** `verification/queue/dafoam/launched/D6RF2_chain.json`. **Actual** `/home/ubuntu/certonomous-runs/CURRICULUM-D6RF2-a2-wing-multipoint-fd_ARM_F_mp_EXTRACT_REFUSE_20260903T231107Z/ledger.txt`. **The refusal** the same archive's `F_mp_20260903T230951Z_937264.log` (the container's own saved log; the container was removed after `docker inspect`, so the kernel's exit code was taken before the rm and `docker logs` no longer serves it) and `F_mp_launch.out`. **Queue row** `verification/queue/dafoam/launched/D6RF2_chain_r3.json`. **ARCHIVE PRESERVATION ASSERTED BY COUNT ACROSS THE mv: files 378 → 378, dirs 85 → 85, bytes 44,797,355 → 44,797,355. No rm -rf has ever been executed against this item.** **TWO CAVEATS A READER MUST NOT HAVE TO INFER. (1) THE ITEM HAS NO VERDICT AND THIS ROW SUPPLIES NONE**; `REF_off` has not run and the registration decision on `F_mp` is deferred, deliberately and on the record, by the dafoam-supervisor. **(2) THE THREE EARLIER FIRES PRODUCED NO CONTAINER AND NO COMPUTE** — 22:13:11Z (rc=4, the anchor gate never staged), 22:54:37Z (rc=3, G-ANCHOR on d6rf2_ref_off.py), and one at rc=4 on a launcher pin the repair itself had moved — all at `spent_before_arm=0.000`, all archived by mv, and **none of them enters this row.** |
| C-20260903T234227.693660Z-ef853022 | 2026-09-03 | dafoam | **SO-3aF2 ITEM CLOSE-OUT — THE ITEM STOOD DOWN `PENDING` AND THIS ROW PRICES THE STAND-DOWN, NOT A RESULT.** Registered in §7 before any compute: **point 1.50 core-min**, item **ceiling 9.0**, per-arm caps `MESH` 3.0 / `XM` 6.0. Actual **6.8000 core-min gross across EIGHT launches**, computed from the ledger at stand-down, not recalled: five rows that reached a container (0.0500 + 0.3500 + 3.6833 + 2.3000 + 0.4000 = 6.7833) plus three archived `MESH` aborts (0.0167). **Cleaned = gross**; no row approached the 3600 wall-s stall rule, the longest being 221 s. **Ratio actual/predicted = 4.533.** The **ceiling held**: 75.6 % of 9.0 spent, and it was never raised — the one arm that hit its own stop was stopped by an in-container `timeout -k` deadline at 105 s unattended (`rc=124`), not by a lane watching a clock. **GAP ATTRIBUTION — MISPREDICTION, AND OF A KIND THE POINT ESTIMATE COULD NOT HAVE CONTAINED: the 1.50 priced the PHYSICS ARM, and the physics arm never ran.** Every core-minute above the first 0.40 was spent in the DELIVERY PATH — three closed defect classes (inputs, environment, and `set -u` propagating across a foreign init script) plus the 6.0 core-min census stall whose cause is **UNMEASURED and parked with its evidence**. **Contention: none. Waste: 0.0167 core-min**, the three `rc=127`/`rc=1` aborts, **named separately and folded into no ratio**; the census stall is deliberately NOT called waste, because a spend whose cause is unmeasured cannot be classified and calling it waste would be a guess wearing a ledger's clothes. **WHAT THE 6.8000 BOUGHT, STATED HONESTLY:** one **MEASURED** negative — no residual-history *attribute* exists on either object (all four registered candidates absent from `DASolver`'s 87 attributes and the wrapper's 298, 0 unreadable) — and a delivery path that now refuses in four legible ways instead of failing silently. **F1–F5 are unscored and the item is `PENDING`.** Whether a residual history is *obtainable* remains **UNMEASURED**. Dollars **$0.005814 DERIVED, NOT MEASURED** at $0.0513/core-h; `cost_basis` **REPORTED-BY-OWNER, NOT MEASURED** — wall seconds and rank counts are read from ledger rows the launcher wrote, the rate is owner-stated, and this box cannot read its own billing (`COMPUTE_BUDGET_CHARTER` §5). **The calibration lesson is not the 4.5×; it is that the estimate and the spend were about different things, and only a pre-registered ceiling priced independently of the point estimate kept that from becoming an open-ended bill.** |
| C-20260903T234735.974786Z-cd16ae47 | 2026-09-03 | dafoam | **⚠ CORRECTING ROW — SUPERSEDES THE WASTE FIGURE IN `C-20260903T234227.693660Z-ef853022` (SO-3aF2 close-out). THAT ROW IS STRUCK ON THIS FIELD ONLY AND IS NOT EDITED.** The original recorded **waste 0.0167** and declined to count the 6.0 core-min census stall, arguing that a spend whose cause is UNMEASURED cannot be classified. **Overruled by the dafoam supervisor, and the overrule is right: waste names an OUTCOME — core-minutes that bought nothing — not a culpability, and cause has its own field.** The decisive argument is structural: **if a spend that bought nothing escapes the waste column BECAUSE its cause is unmeasured, every unexplained spend escapes it, and unexplained spends are exactly what the column is for** — such a figure counts only the losses we can explain and is always wrong in the same direction. **`COMPUTE_BUDGET_CHARTER` §5 lines 107–110 settle it independently of the argument**, defining the split as *"solver cost that bought nothing"* — an outcome test with no culpability clause; I read it rather than take the ruling on authority. **The factual premise was verified by execution before accepting**: both census container logs end at `SO3AF2_ENV_OK`, the line before the Python invocation, and the only `so3af2_attr_census.py` on disk is the SCRIPT, never an output — the two attempts produced no enumeration. **CORRECTED FIGURE: waste 6.0167 core-min, 88.5 % of the item's 6.8000 total.** Components, each named with its condition: **6.0000** the two ATTRCENSUS attempts (first hand-stopped and archived `rc=9`, second reached `ENV_OK` and produced nothing), **cause UNMEASURED and parked with its evidence intact**; **0.0167** the three `rc=127`/`rc=1` `MESH` aborts, **cause measured and closed**. **Nothing else in the superseded row changes**: actual 6.8000, registered point 1.50, ceiling 9.0 held at 75.6 %, ratio 4.533, contention none, dollars **$0.005814 DERIVED, NOT MEASURED** at $0.0513/core-h, `cost_basis` **REPORTED-BY-OWNER, NOT MEASURED**. **The corrected split is the more useful record**: the item's one measured finding — no residual-history attribute exists on either object — cost **0.4 core-min**, and delivering it cost **6.4**. |
| C-20260904T012015.732738Z-f662522a | 2026-09-04 | cfd | **JF1G grid-convergence study at `C_mu = 0.1` — GRADED `NOT A RESULT`, and this is the STUDY-LEVEL row covering ALL NINE launched stages. NO PER-STAGE ROW EXISTS, so there is no double-count hazard: a reader takes THIS row, never this row plus a stage row.** Registration frozen `038f4bca`, blob `0b947659…`, never amended. **Predicted (frozen §8): 180.7 core-min Pass 0 + 677.5 Pass 1 + 929.1 C4 contingency + ~5 mesh = 1792.3, study cap 2320.** **Actual 623.7333 core-min gross, ranks=1 throughout**, re-derived from each run's own `utc_end − utc_start` rather than from any recorded field: P0_C1 21.7500 / P0_C2 70.8833 / P0_C3 0.0667 / P0_C4 98.6667 / P1_C1-attempt1 0.0167 / P1_C1 95.7500 / P1_C2 240.0333 / P1_C3 0.0833 / P1_C4 96.5000. **Ratio actual/predicted = 0.348** against the §8 total, **0.269 of the study cap**. **NO CLEANED FIGURE IS PUBLISHED AND THAT IS DELIBERATE:** five rows exceed 3600 wall s (P0_C2 4253, P0_C4 5920, P1_C1 5745, P1_C2 14402, P1_C4 5790) but every one is a REGISTERED LONG TRANSIENT advancing at a steady measured rate (P0_C4 0.09213 it/s, P1_C4 0.09288 it/s from their own `STOP_RECORD.txt`), not an infrastructure stall. Applying the 3600-s rule literally would clean away 96 % of a study that never stalled; declining to apply it would be a lane reinterpreting a standing rule. **GROSS ONLY, and the scope question is REFERRED to `verification` per the grading record §5.2.** **GAP ATTRIBUTION, three causes kept apart.** (1) **Outcome-class misprediction, the dominant term:** four of nine stages did not do what their row predicted. P0_C3 and P1_C3 were predicted as solves in §8 (110.0 and 412.4) and were mesh-gate refusals costing 0.0667 and 0.0833. **P0_C4 and P1_C4 are the reverse and the more instructive: their queue rows predicted a `checkMesh` refusal at 0.15 core-min on the stated reasoning that "C4 is the next refinement of the same family, so the aspect ratio is predicted HIGHER still" — MONOTONICITY IN REFINEMENT WAS ASSUMED FOR A METRIC THAT DOES NOT HAVE IT.** Measured max aspect ratio 859.95 → 634.44 → **1012.24** → **796.89**: it falls at C4, `checkMesh` printed `Mesh OK`, and both rows solved for 98.6667 and 96.5000 core-min instead of 0.15 — ratios **657.8×** and **643.3×**. The prediction was recorded before the run and was falsified, which is the mechanism working. (2) **Solver-rate misprediction, modest and one-directional:** P0_C1 landed at **0.998×** (the §8 unit rate was derived from this case's own sibling and held to 0.2 %), but P0_C2 came in at **1.450×** because §8 scaled linearly in cells and the 2.25× cell step cost 3.26× the time, and P1_C1 at **1.173×** because the 8000→30000 iteration scaling was likewise assumed linear. Nothing else occupied the box in either window, so this is scaling, **not contention**. (3) **P1_C2 is the row to learn from and it is a PREREGISTRATION MISMATCH, disclosed in its own queue row: registered estimate 265.8 core-min EXCEEDS its own registered cap of 240.0 by 25.8 (1.11×)** — an estimate above its ceiling is a prediction that the run will be killed, i.e. a prediction of `NOT A RESULT`, and it should have been raised as a pre-registration question rather than filed as a cost note. It launched under Sanaa's 2026-09-03 ~21:00Z launch rule (`825285bb`). **Its `predicted_outcome` was then correct to four significant figures:** it predicted the `timeout … 14400` would fire at 240.0 core-min before endTime 30000, giving a non-zero rc; measured **240.0333 core-min, `solver_rc 124`, stopped at iteration 24210 of 30000**, ratio vs its own estimate **0.903**. The cap bound, the run stopped, no new budget was issued (rule 12). **WASTE, NAMED SEPARATELY AND NEVER ABSORBED INTO ANY RATIO ABOVE (`COMPUTE_BUDGET_CHARTER.md` §6): 0.0167 core-min** — the P1_C1 attempt-1 launcher refusal (`rc=9`, stage `dicts`, 0 s solver compute), where a broken `sed` delimiter meant the Pass-1 `residualControl` substitution never ran and the launcher's value check REFUSED rather than solve at Pass-0 numerics. That 0.0167 bought the catch of a silent wrong-numerics run and is the cheapest spend in the table, but it is waste by outcome and is named as such. **Nothing else here is waste:** the C4 rows bought the non-monotonicity evidence and the mesh refusals bought a confirmed gate finding; a spend that buys a finding is not waste. **NO RUN EXCEEDED ITS REGISTERED CAP.** **DOLLARS DERIVED, NOT MEASURED**, at the owner-stated `c7a.4xlarge` rate $0.0513/core-h (reported-by-owner; the box cannot read its own billing, `COMPUTE_BUDGET_CHARTER.md` §5): actual **$0.5333**, study cap **$1.9836**. **VERDICT CONTEXT:** the study is `NOT A RESULT` on two independent frozen gates — G1 (`checkMesh` fails C3 on 2 of 202,180 cells) and G3 (no level in either pass meets its `residualControl` at `endTime`; `p` finishes 3 orders above the Pass-1 control while `k` is clipped in 99 % of P1_C1's final 500 iterations). No observed order and no GCI is quoted. | 1792.3 core-min (frozen §8; study cap 2320) | **623.7333 core-min** | **not computed — gross only, stall-rule scope referred (see cell)** | **0.348** vs §8 total; **0.269** of study cap | outcome-class misprediction (dominant; monotone-aspect-ratio assumption falsified) + solver-rate misprediction (1.17–1.45×, scaling not contention) + one preregistration mismatch (estimate 265.8 > cap 240.0); waste 0.0167 named separately | `verification/campaign/JF1G_GRADING_RECORD.md`; registration `038f4bca`; comparator `74c4f5fb`; run roots `verification/runs/JF1_jet_flap/JF1G_P{0,1}_C{1..4}_CMU010_A0/RUN_STATUS.*.txt` + `STOP_RECORD.txt`; queue rows `verification/queue/cfd/launched/JF1G_*.json` |
| C-20260904T012050.711056Z-be4243a0 | 2026-09-04 | dafoam | **W3 — CYLINDER UNSTEADY FD-VERIFIED GRADIENT, `curriculum_D12R2`, THE `2026-09-03T17:22:42Z` FIRE — THE FIRST OF FIVE TO COMPLETE PHASE 1, AND THE FIRST TO REACH A PHYSICS VERDICT. GRADED `NOT A RESULT` ON THE REGISTERED NO-LAUNCH BRANCH.** `G12R-4` step sizing returns **`h_min 9.119302e-02` against the registered `h_max 5.000e-02`**, `admissible: false`, `steps: []`; phase 2 LAUNCHED NOTHING; the driver stopped at `--plan2`'s `rc=2` Refusal, which `W3_PREREGISTRATION.md:80` registers as **the registered terminus, never a driver failure**. **33 of 33 registered stages ran, every one `rc=0`, `oomkilled false`, `end_line_present`, `age_guard_ok` and `coldstart_ok` on 33/33** — no `BLOCKED` stage, no re-launch, no failed stage. `ranks 1`, cpuset 1, 8g, SHIPPED row `dafoam/opt-packages:latest` `sha256:9d45679d…`, `W_STEPS=2000`. **GRADING PATH VERIFIED FIVE WAYS (rule 2): `d12y_grade_w3.py` md5 `3950d30fd09c9b56213a02f5e9864e20` on disk == the committed HEAD blob == the launcher's `INSTRUMENT_VERIFIED` line in `W3_phase1.out` and `W3_phase2.out` == the driver's `MD5_GRADER` pin == the queue entry.** The `f3c1252c…` string in `W3_PREREGISTRATION.md` §8 and §11 is the **v1.0/v1.1** hash, superseded by Amendment 2 (`3b0a7507…`) and Amendment 3 (`3950d30f…`), both pre-compute; Addendum 1 left it unchanged. **THIS ROW IS FOR THE `09-03` FIRE ONLY AND NAMES THE ROWS THAT PRICE THE OTHERS SO NO CENSUS DOUBLE-COUNTS THE ITEM:** `C-222` prices the `2026-08-30T23:10:12Z` fire at 63.2332 core-min (corrected on a rendering defect by `C-224`), and carries the `2026-08-28T16:28:48Z` supervisor-stopped fire's 5.067 core-min as waste; the `2026-08-27T03:43:17Z` and `2026-08-28T02:15:29Z` fires spent **0** core-min each (a cap-agreement abort and `rc=4 grader_md5_drifted` — both guards working). **ZERO additional core-minutes were spent producing this row: no container, no solver, only reads and arithmetic.** | **PHASE 1: 196.4 core-min, band ±15 % `[167, 226]`**, from the two-anchor line `cost(W) = F + V·(W/900)` with `V = 74.575`, `F = 30.658` fitted on D12R2's 55.5167 (`C-115`) and W2R's 105.2334 — `W3_PREREGISTRATION.md` §4 (`:63`), re-asserted unmoved at `:131`, `:243`, `:595`. **This is prediction `P4`'s own registered reading and its own registered artefact (the ledger's `PHASE1_COMPLETE spent=` line), which is what makes phase 1 the honest denominator here.** Item point 563.3 core-min (phase 1 196.4 + phase 2 37.6 + phase 3 62.6 + phase 4 **≤ 266.7, a BOUND and not a prediction** — §4: *"no S8 has ever run in this family"*), cap `CAP_CORE_MIN = 900.0`, `CAP_S8 = 400.0`, `ranks 1`. Dollars **$0.4816 point, DERIVED NOT MEASURED**. | **271.2501 core-min GROSS**, **MEASURED, two independent readings agreeing**: the ledger's `PHASE1_COMPLETE spent=271.2501` and the sum of the 33 manifest rows' own `core_min` fields. Per-stage: setup 1.7500 (0.6 %), `S2b` 6.6833, `S3 ×3` 19.1000, **`S3b ×16` 149.2168 (55.0 %)**, `S4 ×6` 14.7001, **`S5` adjoint 67.8833 (25.0 %)**, `S7 ×2` 11.9166. **30.1 % of the 900.0 cap; NO CAP BREACHED, NO OVERRUN.** Dollars **$0.2319 DERIVED, NOT MEASURED**, at $0.0513/core-h c7a.4xlarge, reported-by-owner — the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` §5). | **203.3668 core-min** by the charter §2 rule, named: gross minus the one row over 3,600 wall seconds, `S5` at **4,073 s = 67.8833** core-min. **AND THE CLEANING RULE MISFIRES HERE, REPORTED RATHER THAN APPLIED.** The rule's own justification is *"cleaning removes what was never solver cost"* (`COMPUTE_BUDGET_CHARTER.md:108-110`); **`S5` at `W = 2,000` WAS genuine solver cost** — the unsteady adjoint, the item's whole objective, sitting on the family's own scaling line (654 s at `W = 300`, 1,570 s at `W = 900`, 4,073 s at `W = 2,000`). Cleaning it would launder real solver cost as an infrastructure stall, which `:110` forbids in the other direction. **The ratio cell therefore takes GROSS, and says so.** | **1.381 — GROSS / predicted, on the PHASE-1 denominator: `271.2501 / 196.4`. THE BASIS IS NAMED BECAUSE IT IS NOT THIS COLUMN'S DEFAULT.** Cleaned is not used, for a reason that is a like-for-like argument and not a preference: **the 196.4 prediction was fitted on D12R2's 55.5167 and W2R's 105.2334, and BOTH of those are gross == cleaned** (their longest rows were 654 s and 1,570 s, under the 3,600-s threshold). Comparing a cleaned actual against a gross-anchored prediction would be the mismatch, not the correction. **`P4` IS THEREFORE SCORED, AND IT IS A `MISS`** — 271.2501 is outside `[167, 226]`, high by 20.0 % of the band's upper edge. **THIS IS THE OPPOSITE OF `C-224`'s SITUATION AND THE DIFFERENCE IS STATED SO THE TWO ROWS DO NOT LOOK INCONSISTENT: `C-224` REFUSED to score `P4` because that fire executed 13 of 33 stages and no honest denominator existed. THIS FIRE EXECUTED 33 OF 33 — exactly the population the 196.4 prices — so the denominator is honest and the prediction is scoreable.** **THE ITEM-LEVEL RATIO `271.2501 / 563.3 = 0.482` IS COMPUTED HERE AND IS EXPLICITLY NOT THE CALIBRATION FIGURE.** It would read as a 52 % underspend while describing a program whose phases 2, 3 and 4 (≈ 367 core-min) were **eliminated by a registered no-launch branch working as designed**, not left unspent by failure. Structural elimination is not underspend and is not folded into any ratio. | **THE GAP IS +74.85 CORE-MIN ON PHASE 1, AND IT IS MISPREDICTION OF ONE STAGE, MEASURED RATHER THAN ASSERTED.** **(a) The dominant term — the two-anchor line under-prices the `W`-proportional work at 2.22× extrapolation.** The line was fitted on `W = 300` and `W = 900` and asked to predict `W = 2,000`; the 22 `W`-proportional stages (`S3 ×3`, `S3b ×16`, `S5`, `S7 ×2`) came in at **248.1167** core-min = **5.6390e-03 core-min per stage-step**, while the `W`-independent stages (setup + `S4 ×6`) came in at **16.4501**. **`S5` ALONE ACCOUNTS FOR MOST OF IT:** §4 imported W2R's measured `S5` anchor of 26.17 core-min and the family's own wall record is 654 s / 1,570 s / 4,073 s at `W = 300 / 900 / 2,000` — **superlinear, ratio 2.59 for a window ratio of 2.22** — so `S5` landed at **67.8833** core-min against roughly 58 on a linear-in-`W` reading of the same anchors. **The term that overran is the term the two-anchor phase-level line could not see, because it prices PHASES and `S5`'s superlinearity lives in a STAGE.** A three-anchor `S5` wall model `≈ −81 + 2.0438·W` s now fits the family's three points to −18.6 % / +12.0 % / −1.6 % and is registered as the successor's cost basis. **(b) Contention: NOT VERIFIED AND SAID SO.** `memavail_GiB` sat at 25.1–28.5 on every one of the 33 rows and the aggregate-memory control recorded `peers=0.0 GiB` for 31 of 33 stages and `8.0 GiB` for the last two, so the box was not memory-contended; **no contemporaneous CPU-load record for 17:22–21:57Z exists on disk**, so a CPU-contention contribution is **neither confirmed nor refuted** and none is claimed. **(c) WASTE: `0.000` core-min, AND THE DECISION IS ARGUED IN BOTH DIRECTIONS BEFORE IT IS TAKEN (`COMPUTE_BUDGET_CHARTER.md` §6; the test at `:109-110` is *"solver cost that bought nothing"*, an OUTCOME test on whether evidence was produced, NOT on whether it was favourable).** *For waste:* the item returned `NOT A RESULT` and no verified gradient. *Against, and this is the reading the charter's own words carry:* the spend produced four artefacts nothing cheaper could have produced — **`\|g(2000)\| = 0.48835975139977306`**, the third point in the family's `\|g(W)\|` record and the one that falsifies `P2` and `P3`; **the measurement that `W·\|g(W)\|` FELL 4.79 % from `W = 900` to `W = 2,000`** (309.12 → 1025.85 → 976.72 against a requirement of 1781.4), which **falsifies the registration's own `W = 2,400` fallback** — `h_min,env(2400) = 0.075994`, still 1.52× over `h_max` — and with it the whole lengthen-the-window strategy; **`delta_window_exact(2000) = 4.3857102184485797e-04`**, confirming the `1/W` envelope law on this run's own series (`P1` **HIT** to the printed digits, `exact/envelope = 0.98478`); and a complete 33/33 phase-1 record after three consecutive fires that reached no physics at all. **DECISION: NOT WASTE. It is priced spend that bought a measured falsification, and it is what sizes the successor.** The registered no-launch branch additionally prevented ≈ 367 core-min of phases 2–4 from being spent on a gradient that could not be verified — the branch is the item's designed economy and it worked. **(d) PREDICTION SCORECARD: `P1` HIT, `P2` MISS (`\|g\|` 0.48836 outside `[1.01, 1.37]`, 2.07× below the lower edge), `P3` MISS — the primary, and it fired on its registered condition `\|g\| < 0.8907`, `P4` MISS (this row), `P5` NOT SCORED by its own terms (conditional on a `P3` HIT that never came).** **(e) WHAT THE ESTIMATE SHOULD LEARN, WHICH IS THIS ROW'S POINT: a phase-level two-anchor line extrapolated 2.22× hides a superlinear stage. The successor prices PER STAGE and PER ARM in its frozen document, with `S5` on its own three-anchor wall model.** | `cases/dafoam/curriculum_D12R2/RESULTS_W3.md`; `W3_PREREGISTRATION.md` v1.4 (`:63-68` cost, `:88-92` predictions, `:101` verdict mapping, `:408` the governing comparator md5); run root `/home/ubuntu/certonomous-runs/CURRICULUM-D12R2W3-cylinder-unsteady/` (`ledger.txt`, `manifest.jsonl` 33 rows, `step_plan.json`); `cases/dafoam/curriculum_D12R2/{W3_phase1.out,W3_plan.out,W3_phase2.out,W3_plan2.out,STATUS.W3_chain}`; queue `verification/queue/dafoam/launched/W3_chain.json` `prereg_commit 5ce4566260f2048e445e4f368373d4bbd37c4f5d`; successor draft (**UNFROZEN**) `cases/dafoam/curriculum_D12R2/W3S_PREREGISTRATION_DRAFT.md`; prior fires priced by `C-222` / `C-224` |
| C-20260904T014338.264068Z-a9b68e9b | 2026-09-04 | dafoam | **SO-3aF2 `XM` arm, ADDENDUM 15 repair (`feasibility_SO3a_multipoint`) — RAN CLEAN, `rc=0`, F1–F5 all HIT.** The producer's residual history is now read from the solver log, which is what §5 F1 registers (*"counted from the log, both ways"*); the frozen reader was not touched and still hashes `d5f4149d43abe3a165ffe7e653b78bee`. **A COMPLETED PROCESS, so rule 12's duty is live.** | **0.40 core-min**, registered at `FEASIBILITY_PREREGISTRATION.md` §A15.9, cap **6.0**. Anchored on `ARM=XM STAMP=2026-09-03T233357Z rc=7 wall_s=24 ranks=1 core_min=0.4000` with a **§18.1 four-term program statement asserted and CORRECT**: ranks 1 = 1, adjoint NO = NO, colouring NO = NO, tree COLD = COLD. **[DERIVED from a MEASURED figure on this box], not measured.** The item's ORIGINAL §7 registration for this arm was **1.20**, bracket `[0.5833, 1.533]` from two measured figures. | **2.4833 core-min.** Basis: wall **149 s** × **1 rank** / 60, from the launcher's own ledger row `ARM=XM STAMP=2026-09-04T013622Z rc=0 wall_s=149 ranks=1 core_min=2.4833 cap_core_min=6.0 cap_exceeded=0`. Container duration from `docker inspect`: **137.1 s**. | **2.4833 — gross equals cleaned.** `COMPUTE_BUDGET_CHARTER` §2's stall rule matches a row over 3600 wall s; this ran 149 s, so nothing is subtracted. **The arm produced its artefact and its scoring, so NONE of it is waste** — the item's standing waste stays **6.0167** from ADDENDUM 14 §A14.3, untouched by this row and never absorbed into the ratio below (§6). | **6.21× (cleaned/predicted).** Against the item's ORIGINAL §7 point of 1.20 the ratio is **2.07×**, and **2.4833 sits ABOVE the upper bound of that two-anchor bracket (1.533)** — the original bracket failed in the same direction. **Inside the arm cap 6.0 (`cap_exceeded=0`) — and NOT inside the item's registered CEILING of 9.0: the item total is now 9.2833, OVER BY 0.2833 core-min (3.15 %), disclosed at ADDENDUM 16 §A16.4.** | **MISPREDICTION, and the entire miss is MEASURED to lie OUTSIDE the solve.** Cumulative solver `ExecutionTime` at the third primal was **14.13 s** this run against **14.11 s** in the anchor, on identical iteration counts (443/436/424) — **the physics did not slow down**; in-container non-solver time went **≈10 s → ≈123 s**. **EXCLUDED BY MEASUREMENT:** the ADDENDUM 15 repair (one ≈97 KB read plus one scratch copy; solver time unchanged) and contention (**0** other containers at launch, dedicated cpuset, `MemAvailable 27.62 GiB` vs a 6.0 floor). **MEASURED environmental difference:** the box ran **no container for 2 h 02 m 40 s** before this arm, whereas the two anchors sat **13–15 min apart inside a burst of six**. **ATTRIBUTED AND EXPLICITLY NOT MEASURED: page-cache state** — the gap's LOCATION is measured, its CAUSE is not, and cache residency at 01:36 cannot be measured retroactively. **A hypothesis that fits is not a measurement.** ⚠ **AND THE CHARTER DID NOT REACH THIS:** `DAFOAM_CHARTER` §18.1's four terms **all matched** and do not include cache state, while §18.2's cache-state duty is scoped to *"an instrument-only item … [that] runs no solver"*. **`XM2` runs a solver, so no clause required a cache-state token — and cache state is where the whole 6.21× lives.** §18.2 was paid for twice by instrument-only items (`C-212` 20.1×, `C-214` 4.7×); **this is the same mechanism on a solver arm, where the clause does not reach.** Recorded, NOT amended — a charter clause is not a lane's to write. | `cases/dafoam/ladder-a/A1/feasibility_SO3a_multipoint/FEASIBILITY_PREREGISTRATION.md` ADDENDUM 16 (§A16.4 ceiling breach, §A16.6 attribution, §A16.7 the charter gap); ledger `certonomous-runs/CURRICULUM-SO3aF2-a1-naca0012-multipoint-feasibility/ledger.txt` |
| C-20260904T014959.404876Z-f4719b5f | 2026-09-04 | heat-transfer | **K0eR3 `D0`, the determinism twin -- the REFUSAL GATE armed BEFORE the main arms, RUN AND PASSED AT 0 ULP.** Rule 12's completion trigger is a completed process; `D0` is a complete, self-contained registered process and the row is owed now, not deferred to the rung. Two invocations of a BYTE-IDENTICAL case (`build_k0e.py` `ARMS`: `D0_A == D0_B == FP_T10 == ('300','310')`, enforced by the rule-14 guard `arms_intact_or_refuse()` at both call sites), 52,224 cells, `buoyantBoussinesqSimpleFoam`, `endTime` 200, 2 ranks each, launched by the queue daemon at 01:43:15Z and 01:44:20Z. **BOTH TWINS DONE ON ALL SIX CLAUSES OF STANDING RULE 4**, clause 6 reporting `compared 7 field(s), every one newer than 0/T`. **`D0` = 0 ULP: 52,224 cells x 3 components, ZERO components at non-zero ULP.** Graded by ONE invocation of the pinned grader `scripts/analyse_k0er3.py` (git blob `6d7b4d2c0825771a87edfda0cc9ba0279569ed1d`, `--expect-sha` armed and re-read from HEAD in the grader's own shell invocation). **NO RUNG VERDICT IS ASSERTED HERE: `D0` is a refusal gate, not the rung. The main arms `FP_T10` and `FP_T290` REMAIN BARRED and were not built.** This row is the rule-12 cost comparison only. | **POINT 7.00 core-min** = 3.50 + 3.50, registered at `docs/campaigns/F14-cooling-ladder/K0eR3_PREREGISTRATION.md` section 7.2, frozen at `prereg_commit 3f33d815` **before any K0eR3 case directory existed** (the absence was checked under a planted control that proved the reader non-blind). **CAP 21.00 core-min** = 10.50 per invocation = 3x POINT, **enforced in metal** as `timeout 315` s at 2 ranks by `scripts/launch_k0e.sh`. **$0.005985 predicted, DERIVED at the owner-stated c7a.4xlarge $0.0513/core-h, `cost_basis` REPORTED-BY-OWNER, NEVER MEASURED** -- the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` section 5). Basis: 200 iterations x **0.3126 wall s/it** (K0eR2 `FP_T10`'s run-average, `STATUS.FP_T10` `wall=2843` over 9000 iterations) x 2 ranks / 60 = 2.084 core-min of solve, **plus ~1.4 core-min per invocation for build and solver startup**, the latter justified from K0eR2's 5-iteration preflight running at 6x the steady rate. **THE DENOMINATOR IS THE REGISTERED 7.00 AND IS NOT ADJUSTED IN EITHER DIRECTION TO FLATTER THE RATIO** -- and here that discipline cuts AGAINST the estimator, because the miss is an OVER-estimate. | **1.4333 core-min MEASURED = $0.001225 DERIVED.** `D0_A` 22 wall s x 2 ranks / 60 = **0.7333**; `D0_B` 21 x 2 / 60 = **0.7000**. Wall figures from each twin's own `STATUS.<arm>` and **re-derived independently from those files' own timestamps**: `D0_A` 01:43:16Z->01:43:39Z = 23 s and `D0_B` 01:44:21Z->01:44:43Z = 22 s, each corroborating its `wall=` figure to within the 1 s rounding of the stamps and the ~1 s of `reconstructPar` + `writeCellCentres` that runs after the wall clock stops. **GROSS: contention is NOT netted out** -- it is measured and attributed in the gap cell. **WHAT `wall` STRUCTURALLY EXCLUDES, STATED SO A READER DOES NOT HAVE TO INFER IT:** `launch_k0e.sh` stamps `T0` AFTER `build_k0e.py` returns and immediately before `mpirun`, so **the case build is outside this column by construction**. It was measured separately at **~1 s** (runner `LAUNCHED` 01:43:15Z to solver start 01:43:16Z) and is not material, but the omission is structural and is named rather than discovered later. **The grader's own cost is NOT separately instrumented**: registered POINT 0.30 core-min against a 0.90 cap, one invocation returning in a few seconds at 1 rank, so it is bounded far below its point -- **that is a BOUND, NOT A MEASUREMENT, and it is not entered as one.** | **1.4333 core-min -- GROSS EQUALS CLEANED, and that equality is a measurement rather than an omission.** `COMPUTE_BUDGET_CHARTER.md` section 2's stall rule matches a ledger row over 3600 wall s; **neither twin reaches 1 % of it** (22 s and 21 s). No twin was restarted, no attempt was discarded, and **WASTE IS 0.0000 core-min**: both invocations completed rc=0 on all six rule-4 clauses and both are usable, so nothing is subtracted and nothing needs separate naming under section 6. | **0.2048** (1.4333 / 7.00) -- **the estimate was 4.88x TOO HIGH**, the opposite direction to K0eR2's 2.8502. **CAP USAGE 6.83 %** (6.98 % on `D0_A`, 6.67 % on `D0_B`): each twin finished with over 93 % of its enforced ceiling unspent, so rule 12's *an overrun stops the run* never bound. **The 5x cap multiple registered at section 7.2 was sized for the worst measured contention factor and is vindicated as a GUARD rather than convicted as slack -- a cap is not a prediction and this ratio is not evidence that the cap was wrong.** | **ATTRIBUTED BY MEASUREMENT, IN TWO PARTS, AND THE HEADLINE IS THAT THE MISS IS AN OVER-ESTIMATE WITH TWO DISTINCT CAUSES THAT MUST NOT BE MERGED.** **(a) THE RATE BASIS WAS TOO PESSIMISTIC -- roughly 2.9x of the 4.88x.** The registered basis used K0eR2 `FP_T10`'s run-average **0.3126 s/it**, which was itself measured UNDER LOAD. The twins measured **0.1100 s/it** (`D0_A`) and **0.1050 s/it** (`D0_B`) -- **the FREE-box rate**, against the 0.112 s/it free figure this lab has on record for this binary on this mesh. **CONTENTION DID NOT BITE, AND THAT IS A MEASUREMENT, NOT AN EXPECTATION:** `T3d` held 8 of 16 vCPUs at ~100 % throughout (pid 342276, untouched), and the queue runner's own box reading at launch was **busy 50.5 % (~8.1/16 cores), MemAvailable 27.5 GB** -- eight cores were free, so two ranks were never denied one. **THE LESSON IS SPECIFIC: a per-iteration rate lifted from a run measured under load is PESSIMISTIC by ~2.8x when the successor runs on a box that is loaded but not saturated. Box load is not a scalar; what matters is whether FREE CORES REMAIN AT THE REQUESTED RANK COUNT, and neither loadavg nor a busy percentage alone answers that.** **(b) THE STARTUP ALLOWANCE WAS BOTH TOO LARGE AND PARTLY AIMED AT A QUANTITY THIS INSTRUMENT CANNOT SEE -- roughly 1.7x of the 4.88x.** ~1.4 core-min per invocation was added for *build and solver startup*, justified from K0eR2's 5-iteration preflight running at 6x the steady rate. **That 6x is SOLVER STARTUP amortising over 5 iterations; at 200 iterations it amortises away almost entirely**, and the measured per-iteration rates above already sit at the free steady rate with no visible startup premium. **And the BUILD half of the allowance was costed into a column that structurally excludes it**, since `T0` is stamped after the build (measured ~1 s regardless). **THE LESSON: a startup premium measured on a 5-iteration preflight may not be scaled to a 200-iteration run, and any allowance for work that happens before the launcher's clock starts belongs in a separate figure, never inside a `wall`-derived POINT.** **NEITHER CAUSE IS CONTENTION IN THE USUAL DIRECTION and neither is a solver defect.** **A BOOKKEEPING DEFECT IS DISCLOSED HERE RATHER THAN LEFT IN A LOG (L-342, INFRASTRUCTURE CLASS -- it voids no physics and no verdict):** both queue entries declared **no `grading_freeze` field**, and `scripts/queue_runner.py` logged `GRADER-FREEZE K0eR3_D0_A: UNPINNED` and the same for `D0_B`. **The grade was nonetheless pinned** -- `--expect-sha` was armed with the blob re-read from HEAD in the grader's own shell invocation and the grader refuses on mismatch -- **but the QUEUE ROW did not carry the pin, so the runner could not verify it, and a successor's entries must declare `grading_freeze`.** | `verification/runs/F14-cooling-ladder/K0eR3_runs/K0eR3_D0_GRADE_STDOUT.txt` -- the verdict artifact, this lane's captured stdout from ONE pinned invocation, landed byte-identical under the run directory. **THE GRADER EXITED `rc=2` ON THIS RUN AND THAT IS NOT A `D0` FAILURE:** `D0` PASSED at 0 ULP and the grader then REFUSED at the `P7` premise row because `FP_T10` does not exist -- **the registered bar on the main arms working exactly as designed.** The verdict is taken from stdout and the landed artifact, **never from the exit code**. Frozen registration `docs/campaigns/F14-cooling-ladder/K0eR3_PREREGISTRATION.md` sections 5.5 and 7.2, `prereg_commit 3f33d815`; grading path pinned by ADDENDUM 1 at `52928b1d` (grader `6d7b4d2c...`, builder `cbd0a5f2...`, launcher `6eb38ed5...`), scripts landed at `106b7f06`. **Actuals** `K0eR3_runs/STATUS.D0_A`, `STATUS.D0_B`, both twins' `log.solve` and `log.build`, and the `0/T` and `200/` field mtimes the age guard was read from. **Queue rows** `verification/queue/heat-transfer/launched/K0eR3_D0_A.json`, `K0eR3_D0_B.json` (`cost_core_min_estimate` 3.5, `cap_core_min_registered` 10.5). **Box reading at launch** `verification/queue/runner.log`. **Dollars throughout are DERIVED, NOT MEASURED**, at $0.0513/core-h reported-by-owner. **SUPERVISION section 3 check 1 (all three grading-path files, read as diffs) and check 4 were both discharged PERSONALLY by the heat-transfer supervisor before this launch; this lane performed neither and claims neither.** |
| C-20260904T023833.148263Z-dfbdde5f | 2026-09-04 | heat-transfer | **K0eR3, the F14 forced-convection flat plate, GRADED `PASS` -- the rung that resolved K0eR2's degenerate-control conflict by DESIGN.** Rule 12's completion trigger is a rung graded. Four runs on one 52,224-cell mesh, `buoyantBoussinesqSimpleFoam`, 2 ranks each: the `D0_A`/`D0_B` determinism twins at `endTime` 200 (already carried at **this ledger's K0eR3 `D0` row of 2026-09-04, heat-transfer** and **re-stated inside this rung total rather than double-counted as a new spend**), and the two main arms `FP_T10` (plate 310 K) and `FP_T290` (plate 290 K) at `endTime` 9000, run concurrently 01:54:27Z-02:30:57Z. **ALL FOUR RUNS `rc=0`, `note=clean`, and DONE on all six clauses of standing rule 4.** Graded by ONE pinned invocation of `scripts/analyse_k0er3.py` (blob `6d7b4d2c...`, `--expect-sha` armed and re-read from HEAD in the grader's own invocation, `--prereg` supplied and printed as given). **`M4b` 0 ULP and `Z1` 0 flux on 208 faces -- the two gated rows -- so the rung is `PASS`.** This row is the rule-12 cost comparison only; the verdict lives at `K0eR3_GRADE_STDOUT.txt` and commit `da03ec13`. | **RUNG POINT 197.30 core-min** = `D0` 7.00 (3.50 x 2) + `FP_T10` 95.00 + `FP_T290` 95.00 + grader 0.30, registered at `docs/campaigns/F14-cooling-ladder/K0eR3_PREREGISTRATION.md` section 7.2, frozen at `3f33d815` **before any K0eR3 case directory existed** and pinned by a dated pre-compute addendum at `52928b1d`. **CEILING 971.90 core-min**; per-arm CAP 475.00 **enforced in metal** as `timeout 14250` s at 2 ranks. **$0.16869 predicted, DERIVED at $0.0513/core-h, `cost_basis` REPORTED-BY-OWNER, NEVER MEASURED** (`COMPUTE_BUDGET_CHARTER.md` section 5). **THE ARM POINT WAS THE MEASUREMENT ITSELF, NOT A FACTOR ON A PROXY:** K0eR2's `FP_T10` ran this exact mesh at `endTime` 9000 and 2 ranks for **94.7667 core-min** (`STATUS.FP_T10` `wall=2843`), rounded up to 95.00 -- **deliberately NOT repeating K0eR2's 35.00, which was 28.00 x an ESTIMATED 1.25 factor and missed by x2.7076.** **THE DENOMINATOR IS THE REGISTERED 197.30 AND IS NOT ADJUSTED IN EITHER DIRECTION.** | **143.2000 core-min MEASURED = $0.12244 DERIVED.** `FP_T10` 2129 wall s x 2 ranks / 60 = **70.9667**; `FP_T290` 2124 x 2 / 60 = **70.8000**; `D0_A` **0.7333** + `D0_B` **0.7000** = 1.4333. Wall figures from each run's own `STATUS.<arm>` and **re-derived independently from those files' own timestamps**: `FP_T10` 01:54:27Z->02:29:57Z = 2130 s and `FP_T290` 01:55:32Z->02:30:57Z = 2125 s, each corroborating its `wall=` figure to within the 1 s rounding of the stamps plus the `reconstructPar`/`writeCellCentres` that runs after the wall clock stops. **GROSS: contention is NOT netted out** -- it is measured and attributed in the gap cell. **`wall` STRUCTURALLY EXCLUDES THE CASE BUILD** (`launch_k0e.sh` stamps `T0` after `build_k0e.py` returns), measured at ~1 s per case and named rather than left to inference. **The grader's own cost is NOT separately instrumented:** registered POINT 0.30 core-min against a 0.90 cap, one invocation at 1 rank returning in seconds, so it is bounded far below its point -- **a BOUND, NOT A MEASUREMENT, and not entered as one.** | **143.2000 core-min -- GROSS EQUALS CLEANED, and that equality is a measurement rather than an omission.** `COMPUTE_BUDGET_CHARTER.md` section 2's stall rule matches a ledger row over 3600 wall s; **no run reaches it** (2129, 2124, 22 and 21 s). No run was restarted, no attempt was discarded, and **WASTE IS 0.0000 core-min** -- all four runs completed `rc=0` on all six rule-4 clauses and all four are usable, so nothing is subtracted and nothing needs separate naming under section 6. **THE CONTRAST WITH THE PREDECESSOR IS THE POINT OF THE REDESIGN: K0eR2 spent 105.03 core-min, 52.6 percent of its rung total, on an arm that bought NOTHING. K0eR3 spent zero on waste, and the arm that replaced the degenerate one completed in 70.80 core-min.** | **0.7258** (143.2000 / 197.30) -- **the estimate was 1.38x TOO HIGH, i.e. the rung came in 27.4 percent UNDER its registered POINT.** Per run: `FP_T10` **0.7470** (70.9667/95.00), `FP_T290` **0.7453** (70.8000/95.00), `D0` **0.2048**. **CEILING USAGE 14.73 percent** (143.2000 of 971.90); per-arm **CAP USAGE 14.94 and 14.91 percent** of 475.00, so over 85 percent of each enforced ceiling was unspent and rule 12's *an overrun stops the run* never bound. **A REGISTERED FALSIFIER FIRED AND IS RECORDED AS FIRED, NOT ROUNDED AWAY: Pred-2 registered `FP_T290` in [71.1, 118.5] core-min; the measured 70.8000 is OUTSIDE THE FLOOR by 0.3000 core-min (0.42 percent).** It is a miss in the conservative direction and it touches no gate, but **a registered interval missed by 0.42 percent is missed, and reporting it as 'essentially inside' would be widening an interval after the fact.** | **ATTRIBUTED BY MEASUREMENT, AND THE HEADLINE IS THAT A LOAD-MEASURED BASIS ERRED CONSERVATIVELY WHERE K0eR2's FACTOR-ON-A-PROXY BASIS ERRED OPTIMISTICALLY -- THE TWO ARE DIFFERENT KINDS OF ESTIMATE AND MUST NOT BE AVERAGED INTO ONE 'ACCURACY'.** **(a) THE ARMS, 0.746 -- THE BASIS WAS SOUND AND MILDLY PESSIMISTIC, AND THE RESIDUAL IS LOAD COMPOSITION.** Per-iteration: `FP_T10` here ran **0.2366 wall s/it** (2129 s / 9000) against the **0.3126 s/it** of the K0eR2 arm the POINT was built from -- **24.3 percent faster on a byte-identical case, same mesh, same rank count, same binary.** The only variable measured to have changed is what else was on the box: K0eR2's arm ran 19:46-20:34Z against up to ~12 concurrent ranks including two 455,456-cell solves, while these arms ran against `T3d`'s single 8-rank job (pid 342276, untouched) plus each other -- **12 of 16 vCPUs in both cases, but a different MIX.** **THE LESSON, AND IT COMPLETES THE ONE `D0` STARTED: rank count alone does not predict per-iteration cost; the SIZE and MEMORY FOOTPRINT of the co-tenants does. `D0` found that a load-measured rate is PESSIMISTIC by ~2.8x when the successor runs with free cores; this row finds it pessimistic by ~1.34x when the successor runs equally crowded but against smaller co-tenants. In both directions the error is BOUNDED AND SAFE, whereas K0eR2's 1.25x factor applied to a `simpleFoam` proxy was optimistic by x2.7076 and had no bound at all.** **NO SPLIT BETWEEN CONTENTION AND RESIDUAL MISPREDICTION IS ASSERTED ON A GUESS: there exists no quiet-box measurement of this mesh at this date, so the 24.3 percent is attributed to co-tenancy as the only variable measured to have changed, with the explicit caveat that any unmeasured drift sits inside that figure.** **THE TWO ARMS AGREED WITH EACH OTHER TO 0.24 PERCENT (70.9667 vs 70.8000) despite staggered starts -- so the cooled arm costs what the heated arm costs, and the K0eR2 lesson that a control arm must be costed as a DIFFERENT solve applies to an arm that NULLS a gradient, NOT to one that reverses its sign.** **(b) `D0`, 0.2048 -- carried from **this ledger's K0eR3 `D0` row of 2026-09-04, heat-transfer** and NOT re-attributed here.** Its two causes (a rate basis measured under load applied to a run that got free cores, ~2.9x; and a 5-iteration startup premium scaled to a 200-iteration run plus an allowance for build time that `wall` structurally excludes, ~1.7x) are stated there and are **not merged into (a)**, because they are different failures of estimation. **NO BOOKKEEPING DEFECT ON THIS LAUNCH: the `grading_freeze` gap disclosed against myself on the `D0` rows was CLOSED here -- both main-arm queue rows declared `grading_freeze` naming all three pinned paths, and `scripts/queue_runner.py` logged `GRADER-FREEZE K0eR3_FP_T10: PINNED -- 3 comparator(s) ... hash EXACTLY as commit 52928b1d froze them` and the same for `FP_T290`.** **THAT REQUIRED CHANGING `prereg_commit` FROM `3f33d815` TO `52928b1d` AND THE CHANGE IS DISCLOSED, NOT BURIED: `scripts/analyse_k0er3.py` DID NOT EXIST at the registration's freeze commit, because section 0.1 deliberately left the pins uncut there and a dated pre-compute addendum cut them at `52928b1d`. `grader_freeze_gate.py` resolves a declared comparator against the blob AT `prereg_commit`, so a `grading_freeze` declared against `3f33d815` reads `ABSENT-AT-FREEZE` for the grader and `MISMATCH` for the builder and launcher -- MEASURED by a dry read before the entries were written, and WORSE than the `UNPINNED` the `D0` rows carried. This is a REAL STRUCTURAL FINDING about the freeze-first/pin-later pattern and it is recorded for a successor: IF A REGISTRATION DEFERS ITS PINS TO AN ADDENDUM, THE QUEUE ROW'S `prereg_commit` MUST BE THE ADDENDUM COMMIT, OR THE FREEZE GATE CANNOT SEE THE COMPARATOR.** | `verification/runs/F14-cooling-ladder/K0eR3_runs/K0eR3_GRADE_STDOUT.txt` -- the verdict artifact, one pinned invocation's captured stdout, landed byte-identical at commit `da03ec13`. **Actuals** `K0eR3_runs/STATUS.FP_T10`, `STATUS.FP_T290` (commit `da03ec13`), `STATUS.D0_A`, `STATUS.D0_B` (commit `bae21aa7`), all four `log.solve`, and the `0/T` and `9000/`,`200/` field mtimes the age guard was read from. **Frozen registration** `docs/campaigns/F14-cooling-ladder/K0eR3_PREREGISTRATION.md` sections 5.5, 6 and 7.2, frozen `3f33d815`, pins cut by ADDENDUM 1 at `52928b1d` (grader `6d7b4d2c...`, builder `cbd0a5f2...`, launcher `6eb38ed5...`), scripts landed `106b7f06`. **Queue rows** `verification/queue/heat-transfer/launched/K0eR3_FP_T10.json`, `K0eR3_FP_T290.json` (`cost_core_min_estimate` 95.0, `cap_core_min_registered` 475.0, `grading_freeze` declared). **Freeze-gate verdicts and box readings** `verification/queue/runner.log`. **Reference** K0eR2's measured 94.7667 core-min arm, `K0eR2_runs/STATUS.FP_T10`, and **this ledger's K0eR2 rung row of 2026-09-03, heat-transfer**. **Dollars throughout DERIVED, NOT MEASURED.** **SUPERVISION section 3 checks 1 and 4 were BOTH discharged PERSONALLY by the heat-transfer supervisor before the launch; this lane performed neither and claims neither.** **A `K0eR3_RESULTS.md` IS OWED AND IS NOT YET WRITTEN, and its absence is stated here rather than left as a silence.** |
| C-20260904T150910.320385Z-a636135b | 2026-09-04 | heat-transfer | **T3d `R_fx`, the finest T3d level — THE RUN COMPLETED; THE RUNG IS NOT GRADED, AND THIS ROW CARRIES NO VERDICT.** Rule 12's trigger here is a completed *run*, not a graded rung: the solve is finished and its cost is measured, so the comparison is owed now. What is NOT claimed: `DONE.R_fx` does not exist, `analyse_t3d.py` has not been run, no gate has been evaluated, and the §2d.1 petition for a marker producer (`docs/campaigns/T-family/T3d_DONE_MARKER_2D1_PETITION.md`, filed `b0ac3ac6`) is UNRULED. One run, `buoyantBoussinesqSimpleFoam`, 602 128 cells seeded from `R_ff/118000`, `endTime` 24 000, 8 ranks, `simple (8 1 1)`, launched by the queue daemon and detached. **All six clauses of CLAUDE.md rule 4 were MEASURED to hold on 2026-09-04 by a read-only measurement that wrote nothing** — rc=0; one `End` line; last time 24 000 == `endTime` 24 000 read from the case's own `system/controlDict`; `T U p_rgh alphat nut k omega` all present at `24000/` plus `phi` which `mark_done_t3.py:34` also requires, `turbulenceProperties` `simulationType RAS`, `RASModel kOmegaSST`; 24 000 `ExecutionTime` lines == 24 000; and the age guard clean, every one of the eight fields newer than the case's own `0/T` by ≈ +35 486 s, where `0/T` (mtime 2026-09-03T18:04:05Z) and not `0.orig/T` (17:23:41Z) was established on disk as the file that dates the run, consistent with `started_utc` 18:04:57Z. `reconstructpar_rc=0` also holds, the stronger clause `mark_done_t3_rff.py:56-60` added because a failed reconstruction is not a result. **A measured completion is not a marker, and this row does not stand in for one.** | **5 442.1 core-min POINT**, frozen before first compute in `docs/campaigns/T-family/T3d_PREREGISTRATION.md` §5 (lines 135-146), blob `37c4dc92582e23514b3a71a77409280b82c93c4d`, verified identical to the worktree copy at the time of this row. Rate basis **MEASURED** and quoted there: `STATUS.R_ff` 26 757.067 core-min for 118 000 iterations on this exact mesh at these exact ranks = 0.226755 core-min per iteration, × 24 000. HARD CAP **16 326 core-min** (3×), registered `timeout_s` 122 445 s. **$4.6530 at POINT — DERIVED, NOT MEASURED** at $0.0513/core-h, reported-by-owner; the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` §5). | **4 723.200 core-min — MEASURED**, read from `verification/runs/T-family/T3_runs/STATUS.R_fx`, not derived from a wall clock: `wall_s` 35 424 × `ranks` 8 ÷ 60 = 4 723.200 exactly, `capped=no`, `note=clean`. One solve, 2026-09-03T18:04:57Z → 2026-09-04T03:55:34Z (`started_utc`/`ended_utc` in that file). **$4.0383 — DERIVED, NOT MEASURED.** | **4 723.200 core-min — cleaned equals gross**, and the reason is disclosed rather than assumed. Nothing was discarded and there was no retry, so no spend is removed. Disclosure attached instead of silence: this is a SINGLE row of 35 424 wall s, which a literal reading of rule 12's "a row over 3600 wall s is a stall" would match. It is not a stall — `log.solve` carries 24 000 of 24 000 `ExecutionTime` lines, the run's own `endTime` was reached and `End` written, so the wall seconds bought iterations throughout. The stall rule targets a ledger row that stopped progressing, not a healthy long solve, and the figure is left at gross with that reading stated. | **0.8679x** (4 723.200 ÷ 5 442.1) — **13.21 % UNDER the POINT**, and **28.93 % of the 16 326 core-min CAP**. Per-iteration: 0.196800 core-min/iter actual against the 0.226755 core-min/iter basis, the same 0.8679 factor. | **CONTENTION, in the favourable direction — and this is MEASURED, not inferred.** The basis was a whole-run average from `R_ff`, which ran 2026-08-26T20:51:28Z → 2026-08-29T04:36:19Z on a CPU-saturated box: sysstat daily means %user 86.47 / %idle 10.96 on 08/27 and %user 73.46 / %idle 24.20 on 08/28. `R_fx` met a materially lighter box: %user 42.10 / %idle 38.68 on 09/03 and %user 26.57 / %idle 62.79 on 09/04. Fewer competitors for the same 8 ranks is fewer wall seconds per iteration, so the run came in under a basis measured under heavier load. **The misprediction is therefore in the BASIS, not in the arithmetic**: `R_ff`'s per-iteration figure is measured in the charter §2 sense, but it bundles one contention regime into a constant and was then projected onto another; the lesson for the next T-family estimate is that a whole-run average core-min/iteration carries its own load conditions with it and should be quoted with them. **Honest limits on this attribution:** the sar figures are whole-day means, not clipped to the run windows, and `/var/log/sysstat/sa26` is no longer retained, so the first ≈3.2 h of `R_ff` is uncovered — the direction is measured, the exact split is not. `R_fx` was itself NOT run on an idle box (23 other solver logs were written inside its window); it was simply less contended than the basis. **NO WASTE.** One launch, `launcher_rc=0`, `checkmesh_rc=0`, `decomposepar_rc=0`, `reconstructpar_rc=0`, `note=clean`, no failed attempt and no discarded run, so nothing is separately named under `COMPUTE_BUDGET_CHARTER.md` §6 and nothing has been laundered into the ratio. **Disclosed and NOT netted against this spend:** an independent read-only probe of the same level (see the record ref) found the velocity-magnitude field NOT_CONVERGED at the graded checkpoint pair, so whether these 4 723.200 core-min end in a graded rung is open. That is a physics finding for the rung record, not a cost adjustment, and it is named here so the row cannot be read as a completed result. | `verification/runs/T-family/T3_runs/STATUS.R_fx` (all cost figures); `docs/campaigns/T-family/T3d_PREREGISTRATION.md` §5 lines 135-146, blob `37c4dc92582e23514b3a71a77409280b82c93c4d` (prediction and cap); `verification/runs/T-family/T3_runs/STATUS.R_ff` (rate basis); `verification/runs/T-family/T3_runs/R_fx/log.solve`, `log.launch`, `log.reconstructPar` (completion clauses); `verification/runs/T-family/T3_runs/probe_djone_denominator.py` at commit `59bdf2db` (the D-J1 denominator probe whose run produced the velocity-magnitude finding disclosed above). |
| C-20260904T182400.408626Z-8f279096 | 2026-09-04 | ansys-verification | **VMFL046-R2 — the `§37.4` re-run, graded `NOT A RESULT` (register row #57). The process COMPLETED — all three levels rc=0 with one `End` and `Time = 60000` — so rule 12's completion trigger fired; the verdict's label does not change what the compute cost.** | **92.0 core-min**, frozen at `PREREGISTRATION.md §8`, commit `f014449a`, BEFORE the re-run. Method: R1's OWN MEASURED per-level actuals for the SAME configuration (L1 0.94 / L2 5.11 / L3 21.89 = 27.94 core-min, read from each level's final `ExecutionTime` and verified by the supervisor against the run artifacts), scaled ×3 for the endTime factor (60000 vs 20000) = 83.82, plus a **stated 10 % instrumentation margin** for the refining sampler (nPoints 321/641/1281 against R1's flat 400) = 92.20; filed **92**, a round-down of −0.2 %. Per-level caps 9.3 / 50.6 / 216.7, total cap 276. | **84.4167 core-min MEASURED** — L1 154 s, L2 930 s, L3 3 981 s, serial (RANKS=1, so core-minutes == wall minutes), each written by the driver into `launcher.queue.out` as `core_min_used` at the level's completion and cross-read by the supervisor from the per-level logs. | **84.4167 gross == 84.4167 cleaned.** No level exceeded 3 600 s except L3 at 3 981 s, which is **not a stall** — it ran to `endTime = 60000` with rc=0 and one `End`, so the row is reported GROSS with that single long level named rather than silently cleaned. | **ratio actual/predicted = 0.9176** | **THE GAP IS MISPREDICTION IN THE CONSERVATIVE DIRECTION, NOT CONTENTION AND NOT WASTE.** **Waste 0.000**, named separately and netted off nothing: no level failed, none restarted, none was re-run, and the cap was never approached (30.6 % of 276 used). Contention was negligible — the box carried this run essentially alone (load ≈ 0.2–1.1 outside it). The 8.3 % underspend is the **10 % instrumentation margin not being consumed**: the refining sampler cost less than allowed for. **THE CALIBRATION LESSON, AND IT IS THE POINT OF THIS ROW: R1 MISSED BY 2.33× AND R2 LANDED AT 0.917×.** `§27.3` diagnosed R1's miss as pricing a run under `residualControl` and then REMOVING `residualControl` in the same document — the estimate described a different workload than the one that ran. R2's basis is the measured actual **of the configuration being re-run**, with the single workload change applied EXPLICITLY. **An estimator built from a measured actual of a DIFFERENT configuration is not a measurement; one built from the same configuration is.** | **$0.072178 DERIVED** at $0.0513/core-h (c7a.4xlarge, owner-stated 2026-08-21/22) — **NOT MEASURED**; the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER` §5). Filed $0.078660. |
| C-20260904T192344.367708Z-5d6d6696 | 2026-09-04 | ansys-verification | **VMFL072-R2 — graded `NOT A RESULT` (register row #58). The process COMPLETED as a process — five levels launched, four finished rc=0 with `End`, one died SIGFPE — so rule 12's completion trigger fired. ⚠ THIS ROW IS A CALIBRATION OF A CAMPAIGN WITH A FAILED LEVEL AND IT REFUSES TO PRESENT ITSELF AS A CLEAN RATIO.** | **92 core-min** filed at `PREREGISTRATION.md §8`, commit `f5c81c2f`, before the run. Method **60.66** from per-step wall rates MEASURED on this box at the registered configuration (measured/model 1.246 / 0.887 / 0.965 at L1/L2/L3 — accurate to 3.5% at the level that dominates cost), filed at 1.52× the method deliberately, `§26.2`'s direction. Per-level caps 1.5 / 9 / 65 / 100 / 9. | **49.3232 core-min GROSS, MEASURED** — L1 0.3627, L2 2.9222, **L3 14.3138 (INCOMPLETE — SIGFPE at `Time = 6.605` of 10.0, 66% of endTime)**, B2 28.9933, C1 2.7312; each from the level's final `ExecutionTime`, serial, RANKS=1 so core-minutes == wall minutes. | **49.3232 gross; there is no meaningful 'cleaned' figure and inventing one would be dishonest.** No row exceeded 3 600 s. **14.3138 core-min — 29% of everything spent — is WASTE in `COMPUTE_BUDGET_CHARTER §6`'s sense: compute that produced no usable artifact.** It is named here and **netted off nothing**. | **ratio 0.536 — AND THIS NUMBER MAY NOT BE READ AS AN ESTIMATOR SUCCESS.** One of five levels ran to 66% of its clock and stopped; a completed L3 would have added roughly a third again, putting the campaign near 0.7×. **A ratio below 1 that is bought by a level dying early is not an accurate estimate, it is an incomplete one**, and this ledger will not bank it as accuracy. | **The gap is NOT misprediction and NOT contention — it is a FAILED LEVEL, and the per-step rate was actually GOOD.** The rate model was measured pre-freeze at 0.965 accuracy at L3 and nothing contradicts it: B2, the same 256×64 mesh, ran to completion at 28.9933 core-min against a 33.71 method estimate (**0.860×**), which is the cleanest per-level calibration in this campaign **because it is the only fine-mesh level that finished**. Caps were never approached — L3 22% of 65, B2 29% of 100 — so `§27`'s strangling trap did not fire and the generous filing cost nothing. **THE LESSON IS ABOUT WHAT A CALIBRATION ROW CANNOT DO:** a cost model predicts the cost of a run that COMPLETES. It says nothing about whether the run will complete, and a campaign whose finest level dewets at 66% of its clock produces a ratio that looks better than the campaign was. **Estimator accuracy and campaign success are independent axes, and a single ratio conflates them.** | **$0.042171 DERIVED** at $0.0513/core-h (c7a.4xlarge, owner-stated) — **NOT MEASURED**; the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER §5`). Filed $0.078660. |
| C-20260904T193311.748581Z-7c7cc497 | 2026-09-04 | dafoam | **`A1ZE` `z_direction_empty_control` — ITEM-LEVEL ROW, THE ITEM'S WHOLE SPEND ACROSS BOTH FIRES. Graded `NOT A RESULT` by its frozen path** (`/home/ubuntu/certonomous-runs/A1ZE/A1ZE_grade_20260903T190524Z.out:24`), re-driven at ZERO compute in this pass and **`diff`-identical, all 24 lines, `rc=0`**. Grader hashed against the committed blob per rule 2: `8c04f2b747a9f86707b57a8f97218179` == `HEAD` == pin file, unchanged since `864339ab` which landed **66 s before** the graded chain began. **A COMPLETED PROCESS**: it answered its verification question (`d3f47bfa` reverted forward on measurement) and its coefficient question is `BLOCKED` by a MEASURED toolchain refusal — DAFoam's own `DACheckGeometry.C:276` aborts unconditionally on `nGeometricD < 3`, and `empty` bounding planes give 2, corroborated independently at `A1WRT U2`. **NO unit-level A1ZE row exists; a reader takes THIS row as the item's entire spend and adds nothing to it.** | **281.15 core-min**, registered at `A1ZE_PREREGISTRATION.md` §6.2 as *"predicted @ n=14, central — the registered figure"*, frozen at commit `03120e22` **before any compute**. Item ceiling **370.0**, cap sum **532.0**. Sized from two MEASURED rate points (`1.649566 it/s` at n=2, `0.561358 it/s` at n=6), extrapolated to n=14 on the **pessimistic** end of a declared band with a **stated-judgement** 1.5 allowance. Dollars **$0.2404 DERIVED**. | **5.6 core-min GROSS, MEASURED from logs.** Fire 2 (the graded run) **4.9** — `ledger.txt` `SPENT_CORE_MIN=4.9 of ceiling 532.0`, from `STAGE=Sc rc=0 wall_s=206 ranks=1 core_min=3.4333` and `STAGE=Ec rc=97 wall_s=88 ranks=1 core_min=1.4667`. Fire 1 (died 42 s in on a setup mismatch, archived to `A1ZE_partial_20260903T190245Z`) **0.7**, from `A1ZE_PREREGISTRATION.md` §E.1/E.4. **Only 2 of 4 registered arms ever launched**; `S3`/`E3` never staged — arm census driven both ways on a planted fixture before any absence was reported. | **5.6 core-min — gross EQUALS cleaned.** `COMPUTE_BUDGET_CHARTER` §2's stall rule matches a row over 3600 wall s; the longest row in this item is **206 s**, so nothing is subtracted. **The 0.7 core-min of named waste is NOT subtracted here** (§6: waste is separately named and never laundered into either column or into the ratio's explanation). | **0.020× (5.6 / 281.15).** **⚠ THIS IS NOT AN ESTIMATOR SUCCESS AND MUST NEVER BE READ AS ONE.** The one like-for-like point the item produced is `Sc`, the only arm meeting every completion clause: **3.4333 actual vs its registered `est @ n=6 (MEASURED)` 1.8374 = 1.87×**, vs `est @ n=14 central` 4.2193 = **0.81×**, vs cap 9.0 = 0.38× (`cap_exceeded=0`). **`Ec`'s 1.4667 is NOT a rate point** — it aborted at 88 s of a 480 s `TMO` — and no rate may be extracted from it. | **TRUNCATION, not misprediction and not contention.** The chain stopped at arm 2 of 4 on a toolchain refusal, so `S3`+`E3` — **514.0 of the 532.0 cap sum and 272.7 of the 281.15 central prediction** — never staged; **the rate model was not tested at L3 scale at all**, which is why the 0.020× says nothing about estimate quality. **What the model WAS tested on says the opposite and is the useful output:** at `Sc`'s launch the measured contention was `loadavg 33.64 35.20 40.05` on 16 cores with `containers=1` of A1ZE's own — far above the n=6 anchor's conditions — and the MEASURED n=6 anchor **UNDER-predicted by 1.87×** while the contention-sized n=14 central figure landed at 0.81×. **§6.1a's choice to size on the PESSIMISTIC end of the `rate(14)` band, and §6.2's choice to take the MAX of the `3.0×` and `1.25×` legs rather than their product, are vindicated by measurement.** **WASTE, separately named and argued both ways before the call (`A1ZE_PREREGISTRATION.md` §H.7): fire 1's 0.7 core-min IS waste** — a lane's own staging mismatch (a compressible skeleton lacking `constant/transportProperties`) that a file listing catches for free, which is why `G-SOLVERMATCH` now exists — **and fire 2's 4.9 in full is NOT waste, `Ec`'s 1.4667 least of all**: under the charter's OUTCOME test that arm bought the measurement that closed the question, confirmed Addendum B.3's registered prior, supplied the fact on which `d3f47bfa` was reverted, and **made `S3`/`E3`'s 514 core-min unnecessary**. The registered coarse-pair-first ordering has now saved the L3 budget three times. | `cases/dafoam/ladder-a/A1/z_direction_empty_control/A1ZE_PREREGISTRATION.md` **ADDENDUM H (v1.8) §H.7**, and Addenda E.4 / F.5 for the two fires. Run artefacts: `/home/ubuntu/certonomous-runs/A1ZE/{ledger.txt, A1ZE_grade_20260903T190524Z.out}` and `cases/dafoam/ladder-a/A1/z_direction_empty_control/STATUS.A1ZE_chain.log:26,31,33`. **Dollars: actual $0.004788, predicted $0.2404 — both DERIVED at the owner-stated $0.0513/core-h (c7a.4xlarge) and NEVER MEASURED; the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` §5).** |
| C-20260904T214919.397859Z-a5e0172b | 2026-09-04 | heat-transfer | **T3e — `R_fx` continuation `R_fy`, 8,000 additional iterations, `purgeWrite 0`** | **1,574.4** core-min (`T3e_PREREGISTRATION.md` §9 POINT = `0.196800 × 8,000`, on T3d's OWN measured rate on this identical mesh) | **1,382.933** core-min GROSS = cleaned (`STATUS.R_fy`: 10,372 wall s × 8 ranks ÷ 60; single row, `capped=no`, no stall) | **0.8784** | **Contention, favourable direction.** The basis was the predecessor's own measured `0.196800` core-min/it on the identical mesh; `R_fy` ran at `0.172867` on an idle box where `R_fx`'s was not. Using the predecessor's own rate cut the miss from T3d's **13.21 %** (which used `R_ff`'s saturated-box whole-run average) to **12.2 %** — an improvement, and a small one, **because the residual is load and load is not in the basis**. **WASTE 0.000 core-min**, named separately and not folded in: one launch, `rc=0`, all rcs zero, nothing discarded, no retry. | `verification/runs/T-family/T3_runs/STATUS.R_fy`, `DONE.R_fy`, `T3e_runs/gate_t3e.json`, `T3e_runs/T3e_GRADE_STDOUT.txt` | **$1.1824** actual vs **$1.3461** point, **DERIVED at $0.0513/core-h, c7a.4xlarge, reported-by-owner, NOT MEASURED** (`COMPUTE_BUDGET_CHARTER` §5: the box cannot read its own billing) | Rung **`GATE REACHED`**. 29.3 % of the 4,723 core-min cap. Predecessor T3d spent **4,723.200** and produced no graded row; T3e spent **29 %** of that and answered the trend question. |
| C-20260904T220239.662101Z-b467175e | 2026-09-04 | cfd | **F28G H5 PILOT — both limbs (`H5P_C_L1_dp1000_U20`, `H5P_T_L1_dp1000_U20`), 72-iteration restart from the parent rung's plateau at iteration 15000, 4 ranks** | **0.970** core-min (`F28G_H5_RESIDUAL_FIELD_PREREGISTRATION.md` §6.3: C 0.329 + T 0.641, derived from the parent's own measured `1.581523e-06` s/cell/it; registered CAP **4.600** core-min) | **0.479** core-min GROSS = C 0.2407 + T 0.2385, each = solver ClockTime 3 s × 4 ranks + single-core case assembly (5.44 s and 5.31 s total launcher wall, minus the 3 s solver), read from each limb's `log.simpleFoam` and the timed launcher invocations | **0.479** cleaned = gross; neither limb approaches the 3600-s stall rule (both under 6 s total) | **0.494** | **Rate misprediction, dominant and in the favourable direction.** The estimate used the parent's WHOLE-RUN rate `0.056214` s/it, which is inflated by its `decomposePar`, its startup and its final field write — none of which the restart repeats. Measured restart rates by OLS on the `ExecutionTime` series: **0.043111** s/it (C) and **0.042206** s/it (T), 23–25 % faster. **Write cost over-provisioned ~124×:** the registered one-snapshot bound was 4.349 s (§6.2, deliberately crude in the safe direction, attributing the parent's whole 9.1 s non-CPU tail to its single field write); measured **~0.035 s** from the difference of the two limbs' `ExecutionTime` tails. **The per-iteration surcharge of `writeResidualFields true` is NOT RESOLVABLE:** T measured 2.1 % FASTER per iteration than C, i.e. the effect is below run-to-run noise on a shared box, so the pilot yields an UPPER BOUND and not a value — recorded as the honest outcome, not as a measurement of zero. **`decomposePar` structurally removed as designed** — 0.000 s, correctly predicted. **A COST ABSENT FROM THE PRE-REGISTRATION, named rather than absorbed: case assembly** — the 23 MB copy of the parent's `processor*`/`constant`/`system`/`0` plus TWO 157-file parent-immutability fingerprints, ≈2.4 s single-core per limb, **17 % of the actual spend**; §6.2 named it as "shell" and never costed it, and the next estimate must carry it. **Byte prediction was 31 % LOW:** §6.2 predicted 2,750,682 B per snapshot from the parent's `p` field as a per-field proxy; measured **3,595,819 B** across `processor*` (ratio 1.307), because dimensionless residuals format wider than `p`'s values. **WASTE: 0.000 core-min** — no run was killed, none overran its cap, none was re-run; the two limbs spent **10.4 % of the registered cap**. **Both limbs are `NOT A RESULT`** on standing rule 4 clause 4 (solution fields absent at `endTime`, a launcher defect the pilot itself exposed and which is repaired by amendment 3); the spend is real and calibrates regardless of the verdict. Dollars **DERIVED, NOT MEASURED**: 0.479 core-min = 0.00798 core-h × $0.0513/core-h = **$0.00041 derived** (`COMPUTE_BUDGET_CHARTER` §5 — the box cannot read its own billing). | pre-registration `verification/campaign/F28G_H5_RESIDUAL_FIELD_PREREGISTRATION.md` §6.3 at commits `ecc2dec9`/`915ab504`; run roots `verification/runs/F28_runs/H5P_C_L1_dp1000_U20/` and `.../H5P_T_L1_dp1000_U20/` with their `RUN_STATUS.F28.*.txt` and `log.simpleFoam` |
| C-20260904T221340.568168Z-85090b4f | 2026-09-04 | cfd | **F28G H5 VERIFICATION LIMB (`H5P_V_L1_dp1000_U20`) — 72-iteration restart, `pilot-verify`; SUPPLEMENTS the F28G H5 PILOT row dated 2026-09-04 (both spent limbs) by MEASURING the contention that row could only bound. It corrects nothing in that row: its "not detected and not excluded" reading remains true OF THOSE LIMBS, which ran on an idle box** | **0.24** core-min (my own recommendation at §15A.7, ≈ one treatment limb at the then-measured rate) | **0.245** core-min GROSS = solver ClockTime 3 s × 4 ranks + single-core assembly (5.50 s total launcher wall minus the 3 s solver), from `log.simpleFoam` and the timed launcher invocation | **0.245** cleaned = gross; nowhere near the 3600-s stall rule | **1.02** | **CONTENTION IS NOW MEASURED RATHER THAN BOUNDED, and this is the row's value.** The two limbs of the parent row ran 21:57–21:58Z on an idle box (load 1.49, no foreign solver in `pgrep`) and their contention could only be recorded as **not detected and not excluded** — `pgrep` cannot see fleet agents (L-41). **This limb ran deliberately at 22:10Z ALONGSIDE one to two foreign `rhoPimpleFoam` ranks at ~100 %** (ansys-verification's L2/L3 probes under `timeout 3000`), load 2.83 on 16 cores. Measured per-iteration slopes: **0.043111** (C, idle), **0.042206** (T, idle), **0.042399** (V, contended) — **V sits BETWEEN the two idle-box limbs, so contention at 4 ranks against 2 foreign ranks on a 16-core box is NOT DETECTABLE.** That is a measurement, not an absence of concern, and it retires the open contention question on the parent row. **Estimate quality: ratio 1.02 — the prediction was made from the parent row's own measured rate and landed within 2 %**, which is what a calibrated estimate looks like and is recorded as the contrast with the parent row's 0.494. **WASTE: 0.000 core-min** — the limb was authorised, ran once, and returned the fact it was launched for. **What it bought, stated because a cost row that omits the return is half a record:** it PROVED amendment 3 by running (solution fields now land at `endTime`; the stray `15048` write is gone; the comparator cleared strict completion for the first time), and it EXPOSED a defect in the frozen gate — the residual ranking is dominated by cell volume, Spearman ρ = **+0.8034**, top-355 median cell size **176,034×** the median, 248 of 355 shared with the 355 largest cells — **for 0.245 core-min, before the arm spent its 2.67 on an answer that would have been an artifact of the mesh.** Dollars **DERIVED, NOT MEASURED**: 0.245 core-min = 0.00408 core-h × $0.0513/core-h = **$0.00021 derived** (`COMPUTE_BUDGET_CHARTER` §5). | `verification/campaign/F28G_H5_RESIDUAL_FIELD_PREREGISTRATION.md` §15C at commit `bbb2ae0f`-pending; run root `verification/runs/F28_runs/H5P_V_L1_dp1000_U20/` with its `RUN_STATUS.F28.H5P_V_L1_dp1000_U20.txt` and `log.simpleFoam` |
| C-20260905T211815.073245Z-b686541b | 2026-09-05 | ansys-verification | **VMFL034-R2 — graded `NOT A RESULT` (row #59). All three levels died SIGFPE in the drag correlation; the process completed as a process, so rule 12's trigger fired.** | **2 634 core-min** (S1 126 / S2 504 / S3 2 004), frozen at `PREREGISTRATION.md`, commit `3d4047e5`. N²-anchored on the medium from the STRUCK case's measured 0.24 s/step — a real datum. | **4.0356 core-min MEASURED** — S1 0.5995, S2 1.1713, S3 2.2648, from each level's final `ExecutionTime`, serial. | **4.0356 gross; ALL OF IT WASTE** — no time directory was written at any level, so not one core-minute produced a usable artifact. Netted off nothing. | **ratio 0.0015 — AND IT IS MEANINGLESS AS AN ESTIMATOR READING.** The campaign died at 0.15 % of its estimate because the solver trapped, not because the estimate was generous. **A ratio computed against a run that crashed measures nothing about the cost model.** | **THE COST MODEL WAS NEVER TESTED, and that is the honest entry.** The N² basis may be right or wrong; this run carries no evidence either way, because it stopped at 0.33 % of one residence time. **THE TRANSFERABLE LESSON IS ABOUT THE SMOKE, NOT THE ESTIMATE: the pre-freeze smoke ran 15 steps and the crash arrives at ≈27 000.** A smoke sized to prove a case STARTS proves nothing about whether it SURVIVES, and this one did exactly what it was designed to do while the case was already doomed. **A smoke's duration is itself a registered quantity and should be defended against the failure modes it is meant to exclude.** | **$0.003450 DERIVED** at $0.0513/core-h, owner-stated — NOT measured. |
| C-20260905T211815.073284Z-cbfe9e6c | 2026-09-05 | ansys-verification | **VMFL046-R3 — graded `NOT A RESULT` (row #60). L1 completed rc=0; L2 and L3 were STRANGLED BY THEIR CAPS, L3 at 99.3 % of its own endTime.** | **180 core-min** filed (L1 9.52 / L2 15.59 / L3 136.97, method 162.08), caps 30 / 50 / 420, running cap 540. Frozen at commit `91f62d73`. Basis: per-cell-step rates **measured on this box at this configuration**. | **477.7505 core-min MEASURED** — L1 **8.0452 (rc 0, complete)**, L2 **49.8660 (rc 124, cap 50)**, L3 **419.8393 (rc 124, cap 420)**. | **477.7505 gross. 469.7053 — 98.3 % — is WASTE**: L2 and L3 bought no gradeable artifact, and L3 alone is 419.84 core-min discarded **0.7 % from the finish**. Named, netted off nothing. | **ratio 2.65 overall — but the per-level ratios are the finding: L1 0.845, L2 ≥3.20, L3 ≥3.07** (the last two are lower bounds; both were killed, so their true cost is unknown and unknowable from this run). | **⚠ `§27`'s NAMED TRAP LANDED VERBATIM: *a cap at ~3× an UNDER-filed estimate does not overspend, it strangles its own run at the end, after all the compute is spent.* L3 died with 0.7 % remaining after seven hours.** **THE CAUSE IS DIAGNOSED AND IT IS NOT CARELESSNESS: the pre-freeze probes covered 8 ms of the graded 80 ms — 10 % — and A RATE EXTRAPOLATED FROM THE FIRST 10 % OF AN ADAPTIVE-TIMESTEP TRANSIENT IS NOT A RATE, because `deltaT` is not stationary.** The control is inside the same campaign: **L1's probe covered 25 % of its clock and L1 came in at 0.845×.** The estimate was good exactly where the probe was long enough. **THE REMEDY IS TO MAKE THE ESTIMATE GOOD, NOT TO RAISE THE CAP** — `§27` is not a licence to loosen a cap around a bad number. | **$0.408476 DERIVED** at $0.0513/core-h, owner-stated — NOT measured. Filed $0.153900. |
| C-20260905T220806.497202Z-2217ba50 | 2026-09-05 | dafoam | **`S1FDP` — the S1/W4 CBFS field-inversion FD PLATEAU item, Arms P and F, 8 perturbed primals at `primalMinResTol 1e-8`. Graded `NOT A RESULT` by its frozen path** (`/home/ubuntu/certonomous-runs/S1-fd-plateau/S1FDP_GRADE.out`, grader md5 `00617ba89ea5e64861b3d0f78b0d1e81` committed at `5f55c742` BEFORE compute and byte-unchanged at grading). **A COMPLETED PROCESS: all 8 declared primals `rc=0` and rule-4 complete, `declared 8 == executed 8 + blocked 0`**, so rule 12's completion trigger fired; the verdict's label does not change what the compute cost. **The token is `NOT A RESULT` because the REGISTERED FALSIFIER PASSED THE GATE IT WAS BUILT TO FAIL** — Arm F's `h = 0.5` probe missed `P1`'s 10 % plateau bar by measuring 7.9018 %, and §2.5's pre-registered consequence withdraws `P1` for every component. **The plateau itself measured 0.0186 / 0.1067 / 0.0264 % against that 10 % bar and is withheld anyway.** | **60.711 core-min**, frozen at `S1_FD_PLATEAU_PREREGISTRATION.md` §2.8, commit `a1727bd01c4012e1350cd7138460606a3d103338` (blob md5 `4c40181966d39acc39489b094e4b824a`), **before any compute**. Basis **MEASURED, not modelled**: the six existing `fd8_*` rows in the archive's own `ledger.csv` total 1366 wall s at `--cpus=2` = 45.533 core-min, i.e. **7.5889 core-min per perturbed primal on this exact case, mesh, image and tolerance**. Arm P 6 primals = 45.533 + Arm F 2 primals = 15.178. **HARD CAP 75.0**; derived $0.0519. | **62.330 core-min GROSS, MEASURED** from this item's own `ledger.csv`, all `ranks=2` (`--cpus=2`, the basis's own unit): Arm F 8.43 + 8.40; Arm P 7.70 + 7.60 + 7.70 + 7.53 + 7.50 + 7.47. Every row `rc=0`. | **62.330 cleaned == gross.** `COMPUTE_BUDGET_CHARTER` §2's stall rule matches a row over 3600 wall s; **the longest row here is 253 wall s**, a factor 14 below it, so nothing is excluded and the two columns coincide. **83.1 % of the 75.0 cap; the cap was respected and never approached.** **DERIVED, NOT MEASURED: $0.0533** at $0.0513/core-h, c7a.4xlarge, owner-stated 2026-08-21/22 and **reported-by-owner** — the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` §5). | **1.0267** — a **2.67 % miss** on an estimate frozen before the run. | **THE GAP IS CONTENTION, IT IS MEASURED RATHER THAN INFERRED, AND IT SITS ENTIRELY ON TWO OF EIGHT PRIMALS.** Arm F ran 21:31–21:40Z **while eight foreign `buoyantBoussinesq` ranks held half this 16-core box at ~100 % CPU each** (heat-transfer's T-family, 808 s elapsed at the reading); its two primals cost **8.43 and 8.40** against the 7.5889 basis, **+11.1 %**. Arm P ran 21:40–22:02Z as that load eased and cost **7.70 / 7.60 / 7.70 / 7.53 / 7.50 / 7.47, mean 7.583 — within 0.08 % of the basis.** **THE ESTIMATOR WAS ESSENTIALLY EXACT AND THE WHOLE 2.67 % MISS IS FOREIGN LOAD ON THE FIRST TWO ROWS.** The calibration lesson is a positive one and worth banking: **an estimate built from the SAME configuration's own measured actual — same case, same mesh, same image, same tolerance, same rank count — landed at 1.0003 on the uncontended half of its own program.** That is the shape `VMFL046-R2`'s row identified and this row independently confirms it on a different family. **What the basis does NOT carry is load**, exactly as `T3e`'s row records; contention remains the residual in every estimate this lab files, and 11 % on a half-occupied box is a useful magnitude to keep. **WASTE: 0.000 core-min, named separately per charter §6, netted off nothing, AND ARGUED BOTH WAYS RATHER THAN ASSERTED.** *For calling Arm F waste:* its 16.83 core-min — 27 % of the spend — bought a step nobody intends to use, and it is the sole reason an otherwise clean plateau is `NOT A RESULT`. *Against, and this is the reading that holds:* Arm F is a **registered arm of the declared program**, not a failure; it `rc=0` on both legs; it **MET its own primary prediction** (rel. err 7.9313 % against a registered `> 2 %`); and it returned the fact it was launched for. **Compute that buys a refusal you would otherwise have missed is the opposite of waste** — without it this item would have reported a plateau whose gate it had never shown able to discriminate. No primal was killed, capped, restarted, re-run or discarded. | Result `cases/dafoam/ladder-b/S1_FD_PLATEAU_RESULT.md` at commit `63f65a26`; registration frozen `a1727bd01c4012e1350cd7138460606a3d103338`; grading path `cases/dafoam/ladder-b/S1_fd_plateau/analyse_s1_fd_plateau.py` committed `5f55c742` before compute; run artefacts `/home/ubuntu/certonomous-runs/S1-fd-plateau/{ledger.csv, S1FDP_GRADE.out, S1FDP_GRADE.json, log.*, fields_*/}`; queue entry `verification/queue/dafoam/launched/S1FDP.json` with `LAUNCH_LOG.tsv` row `2026-09-05T21:31:32Z dafoam S1FDP 16687`. |
| C-20260905T221213.249206Z-20d5af6c | 2026-09-05 | cfd | **M6SR `B1`+`B2`+`B3` — the L1 mesh build, WHOLE PROCESS, all three steps `inner rc 0`.** `cgns_utils coarsen` → pyHyp march 24,960 × 64 → `plot3dToFoam`+`autoPatch 60`+`createPatch`+`renumberMesh` on 1.59744 Mcell. Registration `verification/campaign/M6SR_PREREGISTRATION.md` §2.4; run root `verification/runs/M6SR_runs/L1`. ⚠ The driver then aborted at **exit 3** on a host-side `mv` (item 51/`M5`) — **that is a transport failure AFTER all compute; every core-min below was spent and produced the mesh, which is intact and graded.** | **7.38 core-min** — §2.4's registered table, frozen pre-compute: `B1` 0.05, `B2` **7.04**, `B3` 0.29. `B2`'s figure uses §2.4's **log-interpolated** pyHyp rate 2.644e-4 s/face-layer. | **24.7500 core-min GROSS, MEASURED** from the driver's own wall clock at **1 rank** (§2.4 registers `B1`/`B2`/`B3` serial): `B1` **0.0167** (1 s), `B2` **23.1833** (1391 s), `B3` **1.5500** (93 s). | **24.7500 cleaned == gross.** No row exceeds `COMPUTE_BUDGET_CHARTER` §2's 3600 wall s stall threshold — the longest is `B2` at 1391 s. **No waste is hidden in the ratio and none is claimed.** Caps: `B1` 1.7 % of 1.0, `B2` **33.1 % of 70.0**, `B3` **51.7 % of 3.0**; **NO CAP WAS TRIPPED**, and after item 50's repair these were DERIVED bounds rather than independent literals for the first time. | **3.3537** — a **3.35× miss** on the whole process; `B2` alone **3.29×**, `B3` **5.34×**. | 🔴 **THE GAP IS MISPREDICTION, NOT CONTENTION, AND §2.4 CARRIED THE RIGHT NUMBER ALL ALONG IN A DIFFERENT COLUMN.** §2.4 lists FIVE pyHyp bases and uses the **log-interpolated** one for the estimate (7.04) while building the **cap** on the **L1 MARGINAL** rate 8.676e-4 s/face-layer → **23.10 core-min**. **MEASURED 23.18. THE MARGINAL BASIS IS ACCURATE TO 0.3 % WHILE THE REGISTERED ESTIMATE MISSED BY 3.29×.** §2.4's own prose warned pyHyp is *"strongly superlinear in face count and rises within a single march"* — **the warning was right and the estimate built beside it was not.** ⚠ **CONTENTION IS ATTRIBUTED SEPARATELY AND IS NOT ALLOWED TO ABSORB THE MISS:** a foreign `buoyantBoussinesqSimpleFoam -parallel` held 5+ of 16 cores throughout at load 11.7–13.0. **But the marginal basis was accurate to 0.3 % DESPITE that contention, which BOUNDS how much of the 3.29× contention can explain — very little of it.** 🔴 **TRANSFERABLE LESSON: WHERE A DOCUMENT CARRIES TWO BASES AND USES THE OPTIMISTIC ONE FOR ITS ESTIMATE, THE ESTIMATE IS THE THING TO DISTRUST. The cap survived only because it was built on the pessimistic basis.** Dollars derived at $0.0513/core-h are **reported-by-owner, never measured** (`COMPUTE_BUDGET_CHARTER` §5). | `verification/campaign/M6SR_PREREGISTRATION.md` **Addendum 2 §32.1 and §32.9**, commit `024955d5`; estimate frozen in §2.4 pre-compute; run artifacts under `verification/runs/M6SR_runs/L1/`. |
| C-20260905T224535.876439Z-bf3fd3be | 2026-09-05 | dafoam | **`D6RF3` arm `F_mp` — A2 wing multipoint FD at the `D6R` endpoint, ONE-ROW `PATCHED` (`dafoam-idwarp-rot:v1`). Graded `NOT A RESULT`** at §3g ladder rung 2 — the registered arm `REF_off` never ran (`STATUS.chain`: `chain=STOPPED_AT_FIRST_NONZERO … not_run=[REF_off]`). **A COMPLETED PROCESS for rule 12's purposes: the chain terminated, the item is closed at this attempt, and the verdict's label does not change what the compute cost.** ⚠ **The arm did NOT complete its own scope** — see the ratio caveat. The `cl04` baseline primal ran to its `endTime` of 1000, printed `End` at log `:2102`, and was refused at `:2105-2107` by DAFoam's **post-`End` acceptance bar** (`N-D42`): measured `1.316217833e-05` against an **effective accept floor of `primalMinResTol 1e-08 × primalMinResTolDiff 1000 = 1.0e-05`**, i.e. **exceeded by 1.316× (failed by 32 %)**, NOT by the nominal factor of 1316. Not memory: `inspect(exit,oomkilled)=[1 false]`, host `MemAvailable` 27.71 → 27.70 GiB. | **155.70 core-min** for `F_mp` (**215.77** for the item; caps 480.0 / 190.0, ceiling 670.0) — `PREREGISTRATION.md` §4a, **FROZEN at `9c079a84`, blob md5 `01079356d794186d21d52e86e1e67907`** (verified by `git show`), i.e. registered before compute. Basis: A4 × (A2/A3) = 47.267 × 3.2949 = 155.74, registered at 155.70. Derived $0.185 for the item at $0.0513/core-h. | **2.067 core-min GROSS, MEASURED** from the item's own `ledger.txt` — `wall_s=31 ranks=4` → `31 × 4 ÷ 60 = 2.0667`, recomputed here. Of which `container_wall_s=28` → **1.867 core-min is container frame** and the solver's own last `ExecutionTime` is 19.71 s. `delivered_cores_mean=[3.5202 n=1]` of 4 requested (88 %). **$0.0018 DERIVED, not measured** (box cannot read its own billing, `COMPUTE_BUDGET_CHARTER.md` §5). | **= gross, 2.067 core-min.** The 3600-s stall rule matches nothing: whole-arm wall 31 s, container wall 28 s. | **0.0133x** on `F_mp` (2.067 / 155.70); **0.0096x** on the item (2.067 / 215.77); **0.43 % of the 480.0 cap**. ⚠ **NOT AN EFFICIENCY. THE ARM DID NOT COMPLETE.** | **TRUNCATION, in full — and the estimate is therefore UNTESTED, not validated.** §4b prices `F_mp` at **22 primals**; this arm evaluated **one** (`cl04` baseline, 1000 iterations) before the acceptance bar aborted the model, i.e. **≈1/22 of the estimate's scope was exercised**. **No misprediction is demonstrated in either direction and none is claimed** — a ratio of 0.0133 read as over-estimation would be exactly the error this column exists to prevent. **Contention: not attributed** — one delivery sample (88 %) is not a contention measurement. **WASTE: 0.000 core-min**, named separately per `COMPUTE_BUDGET_CHARTER.md` §6 and never absorbed into the ratio: the 2.067 core-min bought the `N-D42` corroboration on a second case, the 1.316×-not-1316× arithmetic, a 0.0356 % stdout-to-stdout agreement between the endpoint primal's `CD` and the producing optimiser's recorded `CD`, and the structural finding that falsifier `F1`'s bar (`eta_raw`, from `baseline` vs `baseline_repeat`) cannot exist when a run stops at its first primal. **THE FORWARD-USEFUL CALIBRATION FACT, which IS measurable: an `F_mp` arm that dies at its first primal costs ≈ 2.1 core-min at ranks 4, of which ≈ 1.9 is container frame** — that, not 155.70, is what a successor should price a first-primal probe at. **Lineage:** `D6RF2` spent 4.6 core-min on the refusal that established `D6RF3-DEF-4`/`-5`; lineage total 6.667 core-min, $0.0057 derived. | `cases/dafoam/ladder-a/A2/curriculum_D6RF3/RESULTS.md` §7; ledger `/home/ubuntu/certonomous-runs/CURRICULUM-D6RF3-a2-wing-multipoint-fd/ledger.txt`; log `F_mp_20260905T222250Z_43793.log:2085-2107`; registration frozen at `9c079a84` (post-compute §13 addendum at `2201b382`, +42 lines, insertions only, no gate/threshold/cap/label moved) |
| C-20260905T224535.876521Z-57cfb7a4 | 2026-09-05 | dafoam | **`A1WRT2` arm `SEAM` — the wall-resolved α-tail restart-seam probe, ONE-ROW `PATCHED` (`dafoam-idwarp-rot:v1`). Graded `NOT A RESULT`.** The producer died at **module import**: `runScript.py:51`, `aoa0 = float(os.environ["AOA_ALPHA0"])` → `KeyError: 'AOA_ALPHA0'`, because the arm body at `a1wrt2_run_arm.sh:851-853` passes `-e OMP_NUM_THREADS=1` **and nothing else**. **OpenFOAM never started; zero `Time =` lines, zero `End`** — asserted behind a watcher whose planted control (`first=4001 last=4200 count=2`) PASSED first, so the zero is evidence (`CLAUDE.md` rule 3). Registered prediction **`P-SEAMTIME` is UNRESOLVED and its quantity UNMEASURED** — both registered branches (`Time = 4001` or `Time = 1`) required a solve that never began. Root cause `A1WRT2-DEF-ENVSEAM`: the successor transcribed the predecessor's **innermost** container command and deleted the `cmd.sh` indirection layer (`A1WRT/cmd.sh:57-64`) that supplied the `AOA_*` environment channel — the same deletion that dropped the `loadDAFoam.sh` source repaired 2026-09-05, plus two of `cmd.sh`'s own guards still missing. | **3.10344 core-min**, from the queue row citing the registration (`verification/queue/dafoam/launched/A1WRT2_SEAM.json`, `cost_core_min_estimate = 3.10344`, `cap_core_min_registered = 10.0`); the draft's SEAM solver term is **1.539 core-min = 200 it × 0.46178 s/it** at the measured `A-CONT` rate, plus staging. Registered at freeze `4e39c370`, before compute. Derived $0.0027 at $0.0513/core-h. | **0.067 core-min GROSS, MEASURED** from the item's own `ledger.txt` — `wall_s=4 ranks=1` → `4 × 1 ÷ 60 = 0.0667`. **$0.00006 DERIVED, not measured.** **0.67 % of the 10.0 cap; no overrun.** | **= gross, 0.067 core-min.** Whole-arm wall 4 s; the 3600-s stall rule matches nothing. | ⚠ **NO RATIO IS FILED, AND THE REFUSAL IS THE ROW.** `0.067 / 3.10344 = 0.0216` is computable and is **deliberately not entered as a calibration ratio**: the estimate's model prices **200 solver iterations at 0.46178 s/it** and **ZERO iterations ran**. A 0.0216 would enter this ledger as evidence that the family over-estimates by 46× when it is evidence only that a python interpreter exits quickly. **The estimate is UNTESTED.** | **CRASH AT MODULE IMPORT — the estimate's model was never exercised, so there is no gap to attribute and none is invented.** Not contention, not misprediction, not efficiency. **WASTE: 0.000 core-min**, named separately per `COMPUTE_BUDGET_CHARTER.md` §6: the four seconds bought the `A1WRT2-DEF-ENVSEAM` root cause, the finding that §8's shell unbound-variable control **cannot see across the container boundary** (a missing `-e` feeding a python `os.environ` is not a shell defect), a reporting-surface defect (`STATUS.queue.A1WRT2_SEAM` reads `launcher_rc=0` over a crash, honestly labelled and still misreadable), and a family-wide sweep of all **137** `docker run` files in `cases/dafoam/` — **47 entry-script references, 37 resolved, 2 items with fatal read-but-not-passed (`A1WRT2` 4 vars, `D13` 1), 1 with a conditional forward (`D2`), 31 of 37 genuinely reading nothing from `os.environ`, 10 unresolvable (`genMesh.py`, inside the image) and said to be so**, behind a planted control that refuses unless it can see `A1WRT2`'s known non-zero. **TREATED DIFFERENTLY FROM THE `D6RF3` ROW ABOVE ON PURPOSE:** that arm ran a primal to its `endTime`, so its ratio has a truncated referent; this one has none. One rule applied to both would have been wrong in one. **The forward-useful calibration fact: an `A1WRT2` container failing at python import costs ≈ 0.067 core-min at ranks 1, essentially all container frame.** | `cases/dafoam/ladder-a/A1/wall_resolved_alpha_tail/A1WRT2_SEAM_RESULTS.md` §6; ledger `/home/ubuntu/certonomous-runs/A1WRT2/ledger.txt`; `SEAM/out/sweep.log`; `SEAM/out/rc`; queue row `verification/queue/dafoam/launched/A1WRT2_SEAM.json` landed at `a4f71837`; registration frozen at `4e39c370`, enqueue §13 at `6d52f0af` |
