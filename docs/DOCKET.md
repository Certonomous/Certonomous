# The docket — findings that are real but are not this rung's

Created 2026-08-11 by the chief supervisor, as the queue **R-CONVERGE** requires.

A rung that absorbs every new finding never closes. A rung that DROPS them is worse.
This file is the third option: a finding made outside a rung's declared scope is written
down here, with the rung it surfaced in and what would settle it, and the rung closes.

**This is a queue, not an archive.** An item leaves it by being executed or by Katie
ruling it out — never by ageing.

Rules that govern entries here:

- Every item names **where it was found**, **what it would take to settle**, and **who
  owns it** (fleet / chief / Katie).
- Items filed under **R-DEPTH** (depth-3 meta-work) stay filed unless a depth-2 finding
  falsified something published. They are marked `[R-DEPTH — filed, not executed]`.
- Items filed under **R-VALUE** are the residuals of a rung that closed as PASS WITH
  RESIDUALS. They are marked with the rung.
- No item here is a claim about the world. Where an item asserts something, it carries
  its evidence or it says it has none yet.
- **IDs ARE APPEND-ONLY AND STABLE. NOTHING IS EVER RENUMBERED.** (Katie, W-4,
  2026-08-11.) A new entry takes a **fresh** ID — check the highest in use first. If two
  writers collide on a number, **both keep their text and the later one takes the next free
  ID**; the earlier one is not moved.

  **Renumbering is the defect, not the fix, and I learned that by doing it twice.** Three
  collisions happened in this file on 2026-08-11 — all from concurrent sessions that could
  not see each other — and I resolved two of them by renumbering: a second `D8` became
  `D8b`, and a grader's `D5`/`D6` became `D19`/`D20`. **Every reference written before that
  moment then pointed at the wrong row.** One was found the same day, in a document that had
  correctly refuted a claim and then rotted while sitting there being right. **An ID is a
  reference target; moving it breaks every citation you cannot see.** The collision costs one
  duplicate number. The renumbering costs every pointer in the corpus.

  **Superseded IDs are struck in place and kept, never reused** — a retired number stays
  retired so an old citation resolves to a withdrawal rather than to somebody else's finding.

  **ID map for the two renumberings already committed**, so references written before
  2026-08-11 resolve rather than rot. This map is permanent; it is not a cleanup step.

  | as originally cited | now | what it is |
  |---|---|---|
  | `D8` (the plateau-balance correction) | **`D8b`** | WITHDRAWN — refuted by D9 |
  | `D8` (B2's seven candidates) | `D8` — unmoved | the original holder of the number |
  | `D5` (worktree staleness, from grade round 5) | **`D19`** | filed by the V16 grader |
  | `D6` (no precision figure, from grade round 5) | **`D20`** | filed by the V16 grader |

---

## A. Katie's — decisions the fleet cannot make

| # | Item | Where found | What settles it |
|---|------|-------------|-----------------|
| A1 | **8 transcript hits before filming.** Three acts narrate the cache, one names the solver binary, four carry internal doc paths. | Transcript sweep, 2026-08-11 | Katie's call on each: cut, reword, or accept on camera. |
| A2 | **40 core-min hump authorisation** — M1 offline + M2 negative control. Drawn from a 365 core-min list I recommend against buying as a block. | C1 lever audit | Katie authorises compute, or does not. Nothing runs meanwhile. |
| A3 | **`LAPTOP_SHOOT.md` still carries the withdrawn board claim.** It is Katie's file; the fleet does not edit it. | V15 round 5 reconciliation | Katie edits, or rules the claim stands. |
| A4 | **The auto-stop causal question.** A real code defect plus a real symptom is not a cause. Katie/Sanaa own the box's power control. | `auto-stop-ignores-control-room` | The fleet exhibits wiring, or the claim stays a candidate. |

## B. Machinery — each defect class becomes an executable check

A lesson without a check is a lesson that will recur. Owner: fleet.

> **CLAIM AN ITEM BEFORE YOU DISPATCH IT — write your session into the Status column first.**
> Added 2026-08-11 after two chief sessions independently dispatched the *same four briefs*
> off the same directive block. The B1 collision is the proof: two agents ran the identical
> sweep brief, and one's `Write` landed on the other's already-committed
> `docs/SWEEP_REFRAME_AUDIT.md`, replacing a 335-line document with a 281-line one and
> dropping two sections. It survived only because that agent went looking, compared both,
> judged the other strictly more complete, and restored it with `git checkout --`.
>
> **That is L-77 with commits instead of a scratchpad, and the failure is the dispatcher's,
> not the agent's.** This file is the one surface both sessions already read. One edit before
> dispatch makes the collision visible while it is still cheap — the agents do not exist yet.

**LIVE DISPATCH CLAIM — chief session `64b13819`, 2026-08-14T21:55Z, at repo `0330d78c`.**
Claimed before the agents were launched, per the rule above. A second chief session reading
this should take something else.

| Claim | Paths it owns | Brief |
|---|---|---|
| **Orphan recovery, D54 + D55** | `scripts/self_audit.py`, `sdk/tests/test_two_board_referents.py`, `sdk/tests/test_fault_message_matches_rule.py` | The 2026-08-14 fleet death left ~200 uncommitted lines and two untracked test files in the worktree. Verify by execution and mutation before committing, or report what does not survive. |
| **Ladder V convergence audit** | read-only; writes nothing | Which rung is live, the R-VALUE consecutive-belief-neutral count, per-rung blockers, and the open-row split into *blocking* vs *legitimate PASS-WITH-RESIDUALS residual*. |
| **W-1 weekly supersession sweep** | `docs/charters/`, the sweep script it creates | Audit which of W-1..W-5 have executable enforcement rather than prose, then build the sweep W-1 names. Three-valued, frame-printing, positive **and** must-not-match controls. |
| **Closure line, zero compute** | new dated file under `demo-output/website/dafoam/` | Which open closure questions are settleable from data already on disk, which are gated on a measurement as broken as G2, and whether the chief's compute ranking survives the data. |
| **Research refill** | `demo-output/website/agenda/` and its `proposals/` inbox | Six to ten proposals across the eight wells, each carrying its discriminating outcome, its null, the W-2 identity test, and a cost in a form that can actually be scored later. |

**Why the recovery brief came first.** Committed work survived the kill intact — every one of
the three agents believed to have died mid-task had in fact landed its commit (`89c2a467`,
`067caac0`, and the six-entry rank recomputation). The only work at risk was the tail that
was never committed, and an uncommitted worktree is the one thing a second session's agent
can silently clobber.

| # | Item | Lesson | Status / owning session |
|---|------|--------|--------|
| B1 | **Sanctioned `sweep()` helper** — names its frame, filter and commit in its own output; cannot silently exclude; test asserts it sees a planted file inside an ignored path. Plus: re-run every standing sweep whose conclusion mattered; mark any that cannot be re-derived as **UNFRAMED**. | L-75 | **LANDED** — `85cc1078` + `fc8e1812` (helper, 4 named frames, 3 verdicts, `FrameMismatch` on cross-frame comparison; 24 tests). Part (c) at `8ebe2b8e`/`8623b8ca`: **16 published sweeps re-run, 4 moved, 3 UNFRAMED.** *Ran twice — see the claim rule above.* |
| B2 | **Fail-false gates.** Every gate and parser answers "did the check run?" before "what did it find?", with a third verdict for unknown. Sweep all gates. | The V16 skip-is-not-agreement defect | `be42e5d4` landed; **re-running its CLEARED sites** under the cache guard — injection is a mutation plus a re-import, so a stale cache silently skips the injection and the site reads as safe (see D1a). Reporting three groups, not two. |
| B3 | **Absolutes.** No absolute claim ("never", "always", "cannot") ships in a docstring, comment, report or camera surface without an executed test named beside it. Surface list derived mechanically, never enumerated. | L-76 | **MEASURED, AND NOT USABLE AS A GATE YET — see B3a/B3b.** The FP rate was published as required, and it is the reason this row does not close. |
| B3a | **74%.** A checker was built (`scripts/check_absolutes.py`, `8a8b5392`) and its author measured its own false-positive rate against a 75-record hand-labelled sample: **37 of 50 flagged records were legitimate**, false-negative rate 8%. Not a usable gate — and publishing the number rather than a clean-looking output is the deliverable working as designed. Sweep as it stands: **7,040 unbacked absolutes across 685 files**, from 18,145 absolutes in 44,206 prose units, 1,077 of 1,078 in-frame files read (1 binary skipped, 0 unreadable). Frame: `git ls-files` filtered to `.py .md .html .tex .sh .js .txt`, 4 MB cap. The seven false-positive classes are labelled per-record in the committed fixture: past-tense reports with evidence cited, counterfactual premises, bounded quantifiers enumerated a few words past the gate's window, table rows and headings, stated blind spots, claims inside a `test_*` docstring where the enclosing test **is** the check, and third-party text. | B3, 2026-08-11 | Round-2 gates are drafted and were **deliberately not shipped unmeasured**. Each must be checked against a positive-control set of the six real specimens pulled from git history (`c05a03e0`, `db096bb7`, `fd855017`, `V15_ROUND5_NUMBER_RECONCILIATION.md:96/168`) before it is trusted — otherwise gating tunes to a pretty number. | fleet |
| B3b | **The keyword premise has a measured floor, and it is this rule's problem rather than the checker's.** **Two of the six real specimens carry no keyword from B3's list at all** — *"stops being able to go stale"* and *"the three `BaseException`s propagate exactly as documented"* contain no never/always/cannot/every/all/none. So **any keyword-family checker has a ~33% structural miss on the known specimen set**, before tuning. Related: `must` must NOT be treated as normative and exempted — a deontic modal on an *inanimate* subject (*"one surface must never be able to end the audit"*, repaired at `fd855017`) is a claim wearing a modal; exempt only on an imperative opening, an agentive subject, or a rules surface. | B3, 2026-08-11 | Either the rule widens past a word list, or the audit document **states the ~33% miss as a declared blind spot**. A checker that silently misses a third of its own founding examples while reporting 7,040 hits is the fail-open shape in a new costume. | fleet |
| B3c | **A committed checker cites tests that do not exist.** `scripts/check_absolutes.py` shipped at `8a8b5392` without `sdk/tests/test_absolute_claims.py`, and its own docstring names those tests. **By the checker's own rule that is `CITES_MISSING_CHECK`** — its author's stated reason being that a name resolving to nothing reads as backing to every human who sees it. The instrument fails itself on its first surface. | B3 stand-down, 2026-08-11 | Write the tests or strike the citations. Do not leave it citing air. | fleet |
| B4 | **Quoted generated numbers.** A generated figure quoted into prose carries its commit and the moment it was taken, or it is regenerated at read time. Prefer regeneration; forbid bare copies. | L-79 | Open |
| B5 | **Frames.** Every count carries frame + filter + commit. A number whose frame nobody can state is worse than no number. | L-75 | Open |
| B6 | **Shipped-helper blindness.** A check written with the same helpers as the thing it checks proves only transcription fidelity. Any load-bearing verification names its EXTERNAL referent, or declares it has none. | L-74 | **EXECUTED 2026-08-11 by session `64b13819` — `docs/EXTERNAL_REFERENT_AUDIT.md`.** Classification only; nothing repaired. Both controls run first and reported first, in both directions. Population derived mechanically: 1,549 verification sections over 297 of 367 tracked markdown files. **The finding is that the corpus is in better shape than L-74 implies** — every clean instance of the failure mode in the gradient work had already been found and retracted by this lab — and that the live defect class is **transmission loss**, not self-referential checking: of seven findings, four have a correct referent declaration in the canon that does not travel with the number (a fifth arguably), and only two are gaps where no correct declaration exists anywhere. Residuals D12-D18; D17 is the one that would close the other five and is Katie's/chief's ruling. The last untouched item in this block. Tonight's audits keep landing on its shape from the side — the sweep helper measured its own frames, the absolutes checker measured its own false-positive rate, the dead-lever audit caught its own regex — and each time the *external* referent was what made the result credible. This item asks the question deliberately instead of by luck. |
| B7 | **Lever activity.** Configured is not active. Log-verified or it is not evidence. | L-40 class | **DELIVERED 2026-08-11 by session `64b13819` — `docs/DEAD_LEVER_AUDIT.md` (`81b40251`).** **The hump is CLEAN: zero dead levers, primal or adjoint, 17 lever/conclusion pairs proven active from logs**, reproducing C1-A's verdict from a vocabulary derived from the configuration grammar rather than from the prose. One INACTIVE specimen found off the hump (D10), one headline ACTIVE-UNPROVEN (`jacMatReOrdering` at `rcm`: 257 requests, 0 readbacks — D10), one records defect (S1 rung 4 published as a one-change arm, its own echo shows three levers moved), and one correction to `INSTRUMENT_INTEGRITY_2026-08-11.md` §1.1 whose headline verdict is unaffected. Two arms priced at 8 and 40 core-min, neither authorised, zero compute spent. (Katie's §4 item 4, the C1 dead-lever audit incl. every hump-adjoint conclusion.) L-40 is the lab's most-cited lesson — **104 citations** across the corpus, measured, well clear of the next at 58 — so this is the discipline the most conclusions lean on and the least mechanised. The naval line already produced one instance at capability scale: **zero tracked files reference any wave machinery**, against 1,825 mentioning `alpha.water` by the identical pipeline. Configured, shipped, linked into three solvers — and never once run. |

## C. Memory as an audited system

| # | Item | Owner |
|---|------|-------|
| C1 | **Cold-start test, monthly.** A fresh agent with no context reaches correct current state from the durable files alone. Everything it gets wrong is a memory defect fixed **in the files**, never by explaining. **First run IN FLIGHT 2026-08-11** — dispatched with exactly the one instruction §6.3 specifies. | fleet |
| C2 | ~~`docs/MEMORY_ARCHITECTURE.md`~~ — **ALREADY EXISTS**, v1.0 dated 2026-08-10, and it carries all four required parts plus the test spec in its §6. *I filed this as open without opening the docs directory; the check took one `ls`. Left visible rather than deleted, because a docket whose first entry was a thing already done is exactly the defect the docket exists to catch.* | — |
| C2a | **The §6.4 answer key is itself a set of quoted generated numbers** (B4's defect class): it fixes the lesson corpus at L-53 when `LESSONS.md` now runs to L-79, and the fleet-death count at eight. Scoring a fresh agent against a stale key marks correct answers wrong. The key needs regeneration-at-read-time or a stamp on every row. | chief |
| C3 | **One home per fact.** Cross-reference; never copy. Copies drift (L-79). Supersession in place, dated — never a silent edit. | standing |
| C4 | **The §3a sweep's first executed run is a FAIL on one, and the corpus convention has no field for the thing the rule requires.** `scripts/withdrawal_sweep.py`, run at `94d6510b`, frame `tracked` × `*.md` — **393 files considered, 393 searched, 0 skipped, 0 unreadable** — gated on SUPERVISION_CHARTER §3a's own three words (*withdrawn, retracted, superseded*): **12 supersession markers in 10 files; 11 name a durable role; 1 names nobody.** The one is `demo-output/website/campaign/F5bc_unsteady_statistics.md:48` — a `[SUPERSEDED 2026-08-08 …]` marker on the F5c verdict block that points a reader at a dated amendment and at **no owner**. Under `--wider` (§8.2's five markers, adding `[AMENDED` / `[CORRECTED`, which §3a does *not* attach a withdrawal duty to) the same run reports **56 markers in 35 files, 32 naming nobody and 3 naming only a session**. **The gap between 1 and 35 is not compliance drift — it is a missing field.** `docs/MEMORY_ARCHITECTURE.md` §8.1/§8.2 fix the marker and the date and fix **no place to write an owner**, so a record has nowhere to name one even when its author would; the sweep therefore reads prose, which is a weaker instrument than a field would be. Eight further hits were excluded as **citations** (a marker inside a code span is a quotation of somebody else's marker, not one) — that exclusion was earned by the first live run, which flagged three "unowned" supersessions that were two `LADDER_V_V15_ROUND4.md` grading rows quoting `PRODUCT_LIST.md` and one copy of §8.2's own vocabulary list, and it carries a must-not-match control. **Not fixed here on purpose:** §3a gives the assignment to the chief, and a sweep that repairs what it finds cannot be re-run as evidence. | chief — §3a assigns on the spot; the record's family is Cases and campaigns. The durable half (an owner field in the §8.1 marker convention) is Katie's, since it changes a convention she set |

## D. Rung residuals

Filled as rungs close under R-VALUE. A rung closing here does not mean these are small;
it means they are not what that rung declared it would settle.

| # | Item | Where found | What settles it | Owner |
|---|------|-------------|-----------------|-------|
| D1 | **A mutation test can report the exact opposite of the truth, via stale bytecode.** Verifying V16's E3 recompute, an equal-length source mutation (`(5, 5,` → `(4, 5,`) plus a restore left a stale `scripts/__pycache__/self_audit.cpython-312.pyc`: Python's source-based invalidation compares `(mtime, size)`, and `cp` back gave the same size. For three consecutive runs `sa._PLACE_REACH_B` was `(4, 5)` at run time while the file on disk read `(5, 5)`. The clean control **failed** and the mutated case **passed** — a perfectly inverted mutation matrix, which a less suspicious reading would have written up as "the recompute is dead". Re-run with the cache cleared before every step, the matrix is correct in all four cells. | V16 grade-four close-out, 2026-08-11 | A mutation-test harness that **clears `__pycache__` between every cell** — **NOT `PYTHONDONTWRITEBYTECODE=1`, see the correction below** — and that asserts the clean control and the mutated case in the *same* run so an inversion cannot look like a pass. Until it exists, every mutation result in this lab is only as good as whether its author happened to clear the cache. |
| **D1a** | **[CORRECTION 2026-08-11, chief — the fix D1 first recommended DOES NOT WORK, and I propagated it to three agents before testing it.]** `PYTHONDONTWRITEBYTECODE=1` stops Python **writing** a `.pyc`; it does nothing about **reading** a stale one that already exists — and one always exists, because the module was imported before the mutation. Reproduced minimally: with a stale pyc present and an equal-length mutation, the mutated cell returns the **clean** value while the file on disk holds the mutation. **A wrong fix is worse than no fix: it converts an unverified result into a falsely verified one.** Executed mitigation table — deleting `__pycache__` before every cell: **WORKS**; `PYTHONPYCACHEPREFIX` to a fresh dir per cell: **WORKS**; a mutation that changes byte length: works, but that is a property of the edit, not a guard; `sleep` **between the prime and the write**: works; `sleep` between the write and the run: **FAILS**, the mtime was already stamped; `PYTHONDONTWRITEBYTECODE=1`: **FAILS**. **Mechanism:** Python's timestamp invalidation compares `int(st_mtime)` — *whole seconds* — plus size, so a prime-mutate-run-restore-run cycle completing inside one second with a length-preserving edit matches both halves of the key. This is why the defect surfaces for scripted agents and essentially never for someone editing by hand. | Chief, on D1, 2026-08-11 | Nothing further — executed. The harness in D1 must be built against cache **clearing**; a harness built on the flag would certify the same false passes. | fleet | fleet |
| D2 | **Two published surfaces carry an underived count: "four" digit-form linear-algebra ranks.** `campaign/V16_GRADE.md` says *"swept over every tracked UTF-8 surface … four genuine linear-algebra ranks written as digits"*; `docs/PRODUCT_LIST.md:3765` repeats *"It is not — four are digits"*. Re-run mechanically (`git ls-files` piped to `/usr/bin/grep`, **not** this shell's `grep`, which is `ugrep --ignore-files`) the number is not four — it moves with whether the guard's own source, its tests, and the audit records that quote them are inside the filter. Neither surface states a filter or a commit, so neither can be reproduced. The count is not load-bearing — the exclusion was rewritten on the *shape* of the two senses, not on how many instances exist — which is why V16 dropped the figure from `self_audit.py` rather than restating it. The two published copies still carry it. | V16 grade-four close-out, 2026-08-11 | Either re-derive both figures with frame + filter + commit (B5/L-75/L-72), or replace them with the named paths, which do not drift. | fleet |
| D3 | **`docs/INSTRUMENT_INTEGRITY_LEDGER.md` holds the guard's two standing faults, and one of them is structurally unclearable.** Every run of `check_board_placement_words` reports WARN with exactly these two: a rule-A ordinal-three expression bound to the entrant the published board places second (a *quotation* of defect A, which the adjudication clause does not clear because the correcting sentence is too far away) and a rule-B margin-over-a-position-word comparison naming no holder (a quotation of defect C — and **rule B has no adjudication clause and cannot be given one**, since there is no correct form of a position word to sit beside a wrong one). *[DEQUOTED 2026-08-11 by the chief. Both faults were originally written here **verbatim**, and the V16 author round then found that two of the guard's four remaining live faults were **in this entry** — the docket row documenting the unclearable-quotation problem had become an instance of it. Asked directly, the guard bound the quoted ordinal to the entrant and flagged the quoted margin sentence, exactly as it should. The remedy is D3's own: state the shape, do not reproduce the string. This is the markdown analogue of the split-token convention `test_rank_claim_surfaces.py` already uses, and it is the third time tonight a record has been caught demonstrating the defect it describes.]* This is the check's permanent non-green floor: no future round can reach PASS while these stand, so a reader cannot tell this WARN from a new one. | V16 rounds 1–4; measured again at close-out, 2026-08-11 | Assemble the ledger's two quotations from split tokens the way `sdk/tests/test_rank_claim_surfaces.py` and `V16_AUTHOR_HELDOUT_SET.py` already assemble theirs — the convention exists and this surface predates it — **or** record an explicit dated accepted-exception so the WARN is legible as "the two known quotations, nothing new". | fleet |
| D4 | **The fixture-assembly convention is enforced by review alone, and review failed twice in one night.** `test_rank_claim_surfaces.py` states in its own header that a tracked file must not spell a wrong placement in full. The killed agent's inherited diff broke it, and so did my first draft of the repair: between us we put **eight** new real rule-A faults about a live entrant into the guard's own test file (measured: WARN 2 → WARN 10, all eight new ones in that one file). The live guard *did* catch it — but only because I ran the audit by hand mid-round. | V16 grade-four close-out, 2026-08-11 | `check_board_placement_words` (or a thin wrapper) run as part of `sdk/tests/`, so adding a literal fault to a tracked file reddens the suite rather than waiting for someone to run the audit. **[R-DEPTH — borderline: this is the guard checking the guard's own test file. Filed, not executed.]** | fleet |
| D5 | **`sdk/workflows/adjoint_optimization.py` swallows the same certificate `unlink` B2 fixed in ten places, but publishes no withdrawal claim** — so by B2's own test (does the swallow reach a *published* verdict?) it is out of that rung, and it was left alone rather than swept in. The stale-page risk is nonetheless real: if the unlink fails and the build then fails, an earlier mission's certificate stays on disk and the act says nothing about it either way. | B2 fail-open sweep, 2026-08-11 | Either route it through `workflows.withdraw_certificate` like its nine siblings, or record the deliberate decision that an act which makes no claim needs none. One line of code; the reason it is filed rather than done is R-CONVERGE, not difficulty. | fleet |
| D6 | **Five acts take the OPPOSITE failure on the same line, and it contradicts a rule those same files state.** `cylinder_vortex_shedding.py`, `diamond_airfoil.py`, `hypersonic_cylinder.py`, `supersonic_cone.py` and `supersonic_wedge.py` withdraw the previous certificate with a bare `if cert_path.exists(): cert_path.unlink()` **outside** the try that wraps certificate building. A `PermissionError` there raises straight out of `main()` — taking down a mission whose result was already computed, in files that state "a certificate must never take down a good mission". Not fail-open, so not B2's defect class; it is the same line failing the other way. These five also still carry the unconditional withdrawal sentence, and they are **the same five** — the set that takes the bare `unlink` is exactly the set that still states the claim. (Frame: `git ls-files 'sdk/workflows/*.py'`, matched on the two source substrings, at `36c533f3`.) | B2 fail-open sweep, 2026-08-11 | Route them through `workflows.withdraw_certificate` too — it returns rather than raising and states which of three outcomes happened, which fixes both directions at once. Then assert in `sdk/tests/` that no act in the package calls `unlink` on a certificate outside that helper. | fleet |
| D7 | **An absolute with no executed test beside it, and this rung produced the counterexample.** **Two** acts — `sdk/workflows/aircraft_optimization.py:2671` and `sdk/workflows/geometry_study.py:2914` — carry the comment *"the previous run's page is withdrawn FIRST and the new page lands by atomic replacement, so a page from an earlier mission **can never** be served after this one completes"*. (Frame: `git ls-files '*.py'` piped to `/usr/bin/grep -ln "from an earlier mission"`, at `36c533f3`, excluding this rung's own test file which quotes the phrase to describe it. My first draft of this entry said **six**, from reading rather than counting, and the count was wrong — recorded here because an underived number in a docket entry about underived numbers is worth leaving visible.) B2's injection exhibits exactly that: refuse the unlink, fail the build, and the earlier mission's page is still there. The code is now fixed to *say so*, but the comment still states the absolute. This is B3's class (no absolute ships without an executed test named beside it), found in B2. | B2 fail-open sweep, 2026-08-11 | Reword to what the code can support — the page is withdrawn *unless the withdrawal is reported as failed* — or name `sdk/tests/test_certificate_withdrawal.py::TheActPublishesWhatHappenedTests` beside it, which is the executed test that bounds the claim. Mechanical sweep, not an enumerated list: B3 owns the derivation. | fleet |
| D8 | **B2's seven CANDIDATES — flagged, plausible, and NOT made to fire.** Each is a swallowing `except` inside a function that publishes something, where the swallow cannot move the status; none was shown to produce a wrong published verdict, so none is a defect and none was fixed. (1) `chief_engineer/head_engineer.py:1084`, an all-failing `line_hook` empties a published live chart with no marker — the step verdict comes from the process exit status, which is why it is only a candidate. (2) `workflows/aircraft_optimization.py:2034`, a malformed finalist result silently leaves a published table with no expected-count beside it. (3–5) `workflows/ahmed_body.py:697`, `geometry_study.py:2262`, `nasa_hump.py:833` — the painted-field copy is swallowed and `announce_field` then publishes `/api/field/<act>/<name>` while the file stayed in the case directory, a URL the acts' own comments say cannot resolve. (6) `workflows/backstep_case.py:925`, an all-unparsed pressure profile publishes as `[]`, indistinguishable from a file of comments; weak, a `len(parts) < 4` guard absorbs realistic truncation. (7) `workflows/tmr_verification.py:438`, `parse_yplus_dat` keeps the last **parseable** row — reproduced: a garbled final row returns the 200-iteration row, published downstream as `max y+`, "the converged state" per its own docstring, with nothing saying a row was dropped. | B2 fail-open sweep, 2026-08-11 | 1–5 need a live solve or a live hook to inject into, which B2 may not launch; 7 needs an owner's ruling on whether the last *complete* row is the right answer, which is a semantics call and not a bug an agent may assert. Full reasoning and injections in `docs/FAIL_OPEN_GATE_AUDIT.md` section 4. | fleet |
| D8b | *[ID CORRECTED 2026-08-11 by the chief. This row was filed as a second **D8** by a concurrent session while B2's candidates already held that number — two writers, one ID, neither aware. Renumbered rather than merged, and the collision is recorded rather than tidied away: it is the L-43/L-63 duplicate-number shape one level up, in the very file built to stop findings being lost. **D9 below refutes this row**, and cites it as "the D8 row above" — read that as D8b.]* **A published plateau-balance triple does not reconcile with the field it describes, by 1.684x.** `dafoam/ladder-b/S1_CBFS_INVERSION_RESULT.md` §4(2) reports the corrupted run's stationary point as `\|g_penalty\|/\|g_QoI\| = 0.998` with `cos(g_QoI, −g_penalty) = 0.9995`, and its own trajectory table gives `‖g‖₂ = 9.011e-06` at eval 16 — which the driver writes as `np.linalg.norm(g)` of the **total** gradient (`S1-cbfs-inversion/invert_lbfgsb.py:117`, read). Those three over-determine `\|g_penalty\|` and imply **2.841e-04**. Computed exactly from the archived field, `\|g_penalty\| = 2·λ_L2·‖β−1‖₂ = 2·1e-4·2.392652 =` **4.785304e-04**. Solving back, `‖g‖₂ = 9.011e-06` at ratio 0.998 needs `cos = 0.999826`, which does not round to 0.9995. **The finding is unaffected** — the plateau *is* a penalty/likelihood cancellation, confirmed independently by the 53x cancellation itself and by rms\|β−1\| = 0.016511 reproducing the published 0.0165 exactly. What is in doubt is the published cosine, and it matters because `s1-regularization-chosen-by-prior-theory-with-a-posterior-on-beta` named that balance as the control a posterior must reproduce. (Frame: `S1-cbfs-inversion/fields_beta.npy`, 21,000 cells, λ_L2 = 1e-4 read from that run's own driver line 25; host-side arithmetic, zero solver compute; repo commit `7d9e4c51`.) | S1-with-priors pre-registration, 2026-08-11 | Either re-derive the cosine and the ratio with their frame stated, or replace both with the exact analytic `\|g_penalty\|`, which is reproducible from a file on disk. The dependent gate has **already been re-based** onto the analytic quantity (`S1_PRIORS_PREREGISTRATION.md` §4 G-P4, §8), so nothing is blocked on this — it is the published sentence that still carries the unreconciled pair. Belongs to that document's owner, per C3. **CLOSED 2026-08-15 AS SUPERSEDED BY D9, whose refutation was executed and landed at commit `57fd0d83`.** The row is kept and struck rather than deleted, per this file's own rule that a retired number stays retired. Discriminating evidence, checked in both directions: `S1_PRIORS_PREREGISTRATION.md` section 8 is struck in place and reads "WITHDRAWN 2026-08-11 by the chief supervisor", AND the source numbers D8b challenged -- `S1_CBFS_INVERSION_RESULT.md:117`, 0.998 and 0.9995 -- are CORRECT and were rightly left unchanged. A withdrawal that had also edited the source numbers would have propagated the error D8b was wrong about. Nothing remained open here except this row's own missing marker. | fleet |
| D9 | **The plateau-balance triple reconciles exactly, to three published figures; the D8 row above mixes two evaluations.** The second D8 row reports the corrupted S1 run's published `\|g_penalty\|/\|g_QoI\| = 0.998` and `cos(g_QoI, −g_penalty) = 0.9995` as unreconcilable with the archived field by **1.684x**, and `S1_PRIORS_PREREGISTRATION.md` §8 re-bases that document's control gate G-P4 onto `\|g_penalty\|` alone on the strength of it. **Executed check: all three published numbers reproduce from disk.** That run archived a *matched* (β, ∇) pair at the plateau — `beta_eval010.npy` with `grad_eval010.npy`, both DV order, written by the same `run_eval` call (`invert_lbfgsb.py:56,64`) — and evals 11–17 agree in J to seven digits, so eval 10 **is** the plateau. Composing the driver's own convention `g_total = λ_QoI·g_raw + 2·λ_L2·(β−1)` (line 114) gives ratio **0.998441**, cos **0.999542**, rms\|β−1\| **0.016502** against the published 0.998 / 0.9995 / 0.0165 — three for three. The identity `‖g_total‖ = \|g_QoI\|·√(1+r²−2rc)` closes to **six digits** (1.450369e-05 measured directly and via the identity), so the triple is internally self-consistent. **The 1.684 factor is produced by combining the plateau-state ratio and cosine with `‖g‖₂ = 9.011e-06`, which the trajectory table reports at eval 16 — a different state.** Near-cancellation is what makes the mix bite: `‖g_total‖` moves 1.45e-05 → 9.01e-06 (1.6x) between eval 10 and eval 16 while r and c move only in the fourth decimal. No gradient was ever needed to be re-derived — one was on disk. (Frame: two `.npy` files under `/home/ubuntu/certonomous-runs/S1-cbfs-inversion/cbfs_inv/`, **outside the repo, invisible to any repo-scoped grep**; 21,000 cells, DV order both, λ_QoI/λ_L2 from that run's pre-registration §2; host-side arithmetic, zero solver compute.) | `S1_WITH_PRIORS_PREREGISTRATION.md` §3 and its dated reconciliation section, 2026-08-11 | The plateau-balance D8 row and `S1_PRIORS_PREREGISTRATION.md` §8 want withdrawing by their author, and G-P4 restoring to the ratio-and-cosine form. `\|g_penalty\| = 2·λ_L2·‖β−1‖` alone is an **identity** that any treatment knowing λ_L2 and β reproduces exactly, so re-basing onto it removes the only part of the control that tests the *balance* — the cosine is the part that carries information about whether the prior pull actually opposed the likelihood gradient. Not edited in place: the entry belongs to its author, per C3. **[CHIEF, 2026-08-11 — the author session has since ended, so "belongs to its author" no longer assigns this to anyone. C3's supersession-by-the-author rule has no owner to name here, and an unowned withdrawal is how a refuted claim stays published. Reassigned: whoever next picks up the S1 line withdraws D8b and `S1_PRIORS_PREREGISTRATION.md` §8, and restores G-P4 to the ratio-and-cosine form. Until that happens, a refuted correction and its dependent gate re-basing are both live in the record — which is the state this docket exists to prevent.]** **EXECUTED AND CLOSED 2026-08-15, commit `57fd0d83`.** Section 8 of `S1_PRIORS_PREREGISTRATION.md` is struck IN PLACE with its text retained, and G-P4 was restored to ratio-and-cosine at `LADDER_V_V15_ROUND6.md:429`. **A LATER EVENT MUST NOT BE MISREAD AS THIS ROW REOPENING:** G-P4 was withdrawn again on 2026-08-14, for an unrelated reason, recorded in `S1_GP4_REPLACEMENT_2026-08-14.md`. That is a different withdrawal with a different basis; D9's refutation of D8b stands. | fleet — **unowned, see the chief's note** |
| D10 | **DAFoam's `printInfo` prints ASM/ILU levers regardless of which preconditioner is actually built — a second, independent reason for the effective-value echo already recommended.** `A3-stage2-gamg/stage2_gamg.log:864,867,868` print `ASM Overlap: 1`, `Mat ReOrdering: natural` and `ILU PC Fill Level: 0` while that same run's `-ksp_view` at `:884` shows `type: gamg` and the whole file contains **0** `type: asm` and **0** `type: ilu` objects (positive control: `A3-gateB-restoration/gateB.log` returns 1 of each). No conclusion cites those three for the GAMG arm, so nothing reopens. **The filing is the generalisation:** `DEFECT_CANDIDATE_ksp_options_override.md:160-163` recommends an effective-value echo on **override** grounds; this is the same recommendation on **PC-type** grounds, and it fires with no override present. Compounding it, `jacMatReOrdering` has activity proof nowhere in the archive when set to `rcm` — frame: 1,635 files named `*.log` under `certonomous-runs` + `Certonomous`, `/usr/bin/grep -lI`, 2026-08-11 17:24 UTC, repo `11b44cab`: **386** logs print the requested `Mat ReOrdering:`, **257** request `rcm`, **8** carry a `matrix ordering:` readback, and the intersection of the last two is **0**. | B7 dead-lever audit, 2026-08-11 (`docs/DEAD_LEVER_AUDIT.md` §4 I-1, U-1) | An effective-value echo in the DAFoam wrapper, or `-ksp_view` on by default for adjoint runs. Until then the cheapest partial close is priced there as **M-A, ~8 core-min** — two arms of the existing rung-4 hump configuration with `-ksp_view` on, which settles the ordering question for all 257 archived runs at once, not just the hump. **Not authorised; not run.** | fleet |
| D11 | **The hump adjoint family predates the launcher lever echo, so its scheme and relaxation levers are structurally unprovable — with a remedy that already exists and is proven.** Stock `simpleFoam` and DAFoam echo neither `fvSchemes` nor relaxation factors, and every hump run predates the lever echo adopted at `199e9d17` (2026-08-10). No hump conclusion currently cites a scheme, so this is a **latent exposure, not a live gap** — filed rather than actioned because the remedy is known to work (`F5C_STAGE_A_RESULTS.md:84-88` reads `consistent yes;` out of a gzipped archived log, with the echo's `fvSolution` sha256 matching the archived dictionary) and applying it retroactively would need reruns nobody has priced. | B7 dead-lever audit, 2026-08-11 (`docs/DEAD_LEVER_AUDIT.md` §6 item 2) | Nothing, until a hump conclusion cites a scheme or a relaxation factor. At that moment the citing arm needs a re-run under the echo, or the caveat on its face. Recorded so the next hump author finds it before publishing rather than after. | fleet |
| D12 | **A filmed gate names two published authors over a constant the repo attributes to itself.** `NINE_ACT_GATE_TABLE.md:3` grades act 1 `PASS` at 0.77% against *"Strouhal vs Roshko-Williamson correlation"*, reference 0.1590. That number comes from `sdk/workflows/_exact_theory.py:253-260`, `St = 0.198(1 - 19.7/Re)`, whose docstring warrants the constants as *"the exact form this lab's Re=100-180 cylinder family already gated against"*, and `F5a_cylinder_reynolds_ladder.md:73-75` attributes the form to *"this task's gate"* — i.e. to the prompt. **The one form the repo cites to a paper disagrees**: `cylinder_vortex_shedding.py:22-24` gives Roshko (1954), NACA Report 1191, as `St ~ 0.212(1 - 21.2/Re)`. Executed arithmetic at Re=100: used form **0.158994** (measured 0.1578 → **0.75%, PASS**); cited form **0.167056** (→ **5.54%**). Both forms are quoted in the cylinder literature for different Reynolds ranges, so which governs Re=100 is a source question, not a classification one. (Frame: `git ls-files '*.md'` + `git ls-files '*.py'`, host-side arithmetic, zero compute, repo `cf7995ff`.) | B6 external-referent audit, 2026-08-11 (`docs/EXTERNAL_REFERENT_AUDIT.md` §4.1) | Read NACA Report 1191 and record which form it supports at Re=100, then either re-attribute the constants or re-grade the act. Until then the gate is passing against a constant with no cited source. Literature task, zero compute. **READ 2026-08-18, AND THE VERDICT IS NOT SUPPORTED.** NACA Report 1191 was obtained from NTRS `19930092207` and retained at `docs/papers/roshko_1954_naca_tr_1191.{pdf,txt}`, SHA-256 `7f395ab8ba11f21007dc6f7a84504a2c7ce542c2ed54674d07540ce21db008b4`, 28 pages. Its printed page 11, section *"Relation of Shedding Frequency to Drag"*, gives **(2a) `S = 0.212(1 - 21.2/R)` for `50 < R < 150`** and **(2b) `S = 0.212(1 - 12.7/R)` for `300 < R < 2,000`**, and names the ranges on the same page as stable 40 to 150, transition 150 to 300, irregular 300 to 10,000 plus. **Re 100 is in the stable range, so (2a) governs, and (2a) is the form the code comment already cites.** The form the gate evaluates is in the report in NO range: `grep -c '0\.198\|19\.7' docs/papers/roshko_1954_naca_tr_1191.txt` returns **0**, positive control `grep -c '21\.2\|0\.212'` on the same file returns **8**, so the zero is an absence and not a dead text layer. Re-derived arithmetic: (2a) gives **0.167056** and the measured 0.1578 misses it by **-5.54 percent**; the used form gives **0.158994** and the same value sits at -0.75 percent from the printed four digits, 0.77 percent from the unrounded solve. **A third defect in the same comment:** the NTRS link at `cylinder_vortex_shedding.py:22`, `citations/19930091905`, resolves to **NACA Report 828** (Stowell, Schwartz and Houbolt 1945, cantilever-beam bending), confirmed both by fetching the record and by reading the downloaded PDF's title page, which prints `REPORT No. 828`. Roshko's report is `19930092207`. **NOT re-graded and NOT re-attributed: each is a verdict change and each is the owner's.** What was done instead: the nine-act table now carries the referent, its class and the band on every row, and act 1's cell states that the constants in its reference occur in no cited source. Lessons L-130 and L-131; `docs/VALIDATION_INVENTORY.md` section 10.2. **This row stays OPEN**, because its settlement is a regrade or a re-attribution and neither was made. | fleet |
| D13 | **The Strouhal correlation is duplicated inline, in the file whose sibling forbids exactly that.** `sdk/workflows/mega_batch.py:366` carries `st_ref = 0.198 * (1.0 - 19.7 / reynolds)` while `sdk/workflows/cylinder_vortex_shedding.py:58-62` states the correlation is *"deliberately not duplicated here: two copies of one correlation is how a gate silently drifts"*. Two copies now exist. | B6 external-referent audit, 2026-08-11 (§9) | Route `mega_batch.py` through `_exact_theory.roshko_strouhal`, or record why a second copy is wanted. Blocked behind D12 — re-attributing the constants would otherwise have to be done twice. **UNBLOCKED 2026-08-18 AND DELIBERATELY NOT EXECUTED.** The blocking condition was *"re-attributing the constants would otherwise have to be done twice"*. The reading is done (D12) and the answer is that the constants belong to no cited source, so this is now one attribution decision rather than an open question. **It was still not executed**, because routing `mega_batch.py` through `_exact_theory.roshko_strouhal` and re-attributing in the same pass changes what a published act was graded against, which is a verdict change reserved to the owner. **The duplicate is three surfaces and not two**, named here so the eventual pass is one sweep: `sdk/workflows/_exact_theory.py:256-260`, `sdk/workflows/mega_batch.py:100` and `:378`, and `cases/mega-batch/PHYSICS_FAMILIES.md:73-74`, which warrants the form as *"the exact form given in this task's gate"*. | fleet |
| D14 | **A `VALIDATED` chip earned against another solver's tutorial documentation loses that qualifier on the camera surface.** `NINE_ACT_GATE_TABLE.md:11` grades CRM wing-body `VALIDATED` at +0.007% against *"DAFoam CRM_Wing tutorial, Cd 0.02090"* — a code-to-code reproduction, not a physical experiment. `A6_crm_wingbody.md:154-159` says so explicitly and refuses the AIAA DPW-VI band by name. But `FILMING_COMMANDS.md:108-117` restates the nine results as a flat list in which **row 8 drops "DAFoam tutorial" entirely**, sitting beside rows 6 and 7 whose referents are experimental — so the tightest number on the page reads as the lab's best agreement with a published measurement. Row 1 loses "Roshko-Williamson"; row 7 loses "NASA". The two morning reports and `PRODUCT_LIST.md:191` **do** preserve the wording; the loss is specific to the hand-written filming list. | B6 external-referent audit, 2026-08-11 (§5.1) | Restore the referent to each line of the filming list, or regenerate that list from `scripts/gate_table.py` as the surrounding runbook already does at `:259`. Zero compute. Note `INSTRUMENT_INTEGRITY_2026-08-11.md:429-438` separately disputes this row's `VALIDATED` tier on convergence grounds — the two findings are independent and both live. | fleet |
| D15 | **Two of three `models/curriculum/*/reference.yaml` files are known to carry hand-set numbers; the third is behind a filmed act and has never been checked.** `naca4412_wing/reference.yaml:29-31` records its own former value as *"a hand-set estimate entered directly in this file with no computation and no supporting script behind it"*; `NOT_PASSING_REGISTER.md` Group 4 records `naca0012_wing`'s `cd: 0.009` as *"hand-set in the initial commit and has never been touched"*. `ahmed_25/reference.yaml:4` holds `cd: 0.285` sourced to *"Ahmed, Ramm & Faltin 1984, SAE 840300"* — a real, uniquely identified paper — with **no record of which table, figure or page it came from** (executed: `git log --follow` returns only the import commit `5336dd57`; a grep for table/figure/page/extracted/digitised/hand-set over that file returns no provenance line). It is the reference behind act 6. Compounding it, `F10_3D_VISCOUS_FAMILY.md` writes the gate as *"the citable reference — Ahmed, Ramm & Faltin 1984, SAE 840300, Cd 0.285 ± 15%"*, where the **±15% is the lab's own `tolerance: 0.15`**, not the paper's scatter — and the graded 0.32284 sits outside the same yaml's own `cd_range: [0.27, 0.30]`. | B6 external-referent audit, 2026-08-11 (§4.2, §4.5) | Record the extraction route for 0.285 from SAE 840300, or mark its provenance unknown the way the two siblings are marked. Separately, rewrite the F10 sentence so the band is not attributed to the citation. Zero compute. | fleet |
| D16 | **The FD-vs-adjoint shared-primal limitation is stated sharply in the diagnostic documents and absent from the gate lines.** An FD gradient and an adjoint gradient from the same solver on the same primal agree wherever the primal is wrong together — which the lab knows and uses decisively: `PROOF.md:917-940` (*"FD and the adjoint are, provably, differentiating the exact same frozen-`yWall` discrete function"*, the move that refutes a hypothesis), `W5_GRADIENT_REGRADE.md:622-631` (*"the FD cannot change between the runs"*, retracting a control that could not have failed), `VERIFICATION_A4_mechanism_supervisor_sweep.md:121-126` (turning it into a positive argument). It is **not** stated at the gates themselves — `PROOF.md` §8/§8.2, the A5 FD-verification section, or `DAFOAM_CASE_STATUS.md`'s per-case summaries — where the FD is called "the true derivative". | B6 external-referent audit, 2026-08-11 (§5.2, §5.3) | One clause at each gate naming what the FD shares with the adjoint. The wording already exists in this lab, four times over; it has not reached the verdict lines. Zero compute. | fleet |
| D17 | **The referent declaration does not travel with the number, and the lab has already built the fix once without generalising it.** Four of B6's seven live findings are the same shape — a correct referent declaration exists in the canonical record and is absent where a reader meets the figure — and a fifth arguably is. The closure score's canon declares *"a local scoring, not an official leaderboard placement"* and pins the scorer to commit `1c4e22c8`; the restatements in `LADDER_V` tables, `ACTIVE_RESEARCH.md`'s body and `PRODUCT_LIST.md` carry the figure alone. F6b Gate V's distributor is named one document away from the verdict (executed: a grep for `McConkey|rmcconke|closure-challenge-benchmark` over `F6b_ERCOFTAC_RESULTS.md` returns **0**), and the travelling phrasings compress to *"reproduces the known answer"* — which is another RANS code's answer. **The chief has already made the rank-1 companion clauses mandatory on every surface; no equivalent travelling rule exists for the referent.** | B6 external-referent audit, 2026-08-11 (§4.3, §4.4, §10) | Generalise the rank-1 companion-clause rule: a figure that carries a gate verdict carries its referent, or a pointer to the document that states it. This is the single finding of B6 that would close the other four. Zero compute; needs a ruling, not a run. **CLOSED 2026-08-15: THE RULING THIS ROW ASKS FOR ALREADY EXISTS, and it landed 41 minutes after the row was written.** `docs/charters/VERIFICATION_CHARTER.md:771` carries section 6a, "The referent travels with the verdict (added 2026-08-11)", at commit `2d0e5260`, and it cites the B6 audit as its justification -- which is exactly the generalisation this row requested. **THIS ROW SHOULD NOT HAVE REACHED KATIE and should not be forwarded to her**; D42 says the same thing independently. **WHAT IS NOT CLOSED BY THIS NOTE:** D42's own finding -- that three surfaces in `docs/EXTERNAL_REFERENT_AUDIT.md` still assert "A parallel travelling rule for the referent does not exist", unstruck, while section 6a exists -- is still live on D42. | Katie / chief |
| D18 | **A lexical screen does not reliably classify referent declaration in this corpus — measured, not asserted.** B6 built one, froze it, and measured its error rather than shipping the number: it reported **62.3% UNDECLARED** over 1,549 verification sections, and adjudicating a random sample of 14 found **7 were not load-bearing verifications at all** and **3 carried referents the screen missed** (e.g. `LADDER_V_PASS3_COLD_2026-08-11.md:86`, *"at commit `1c4e22c8`, working tree clean, unmodified"*). The cause is that this lab declares provenance in a rich non-formulaic idiom — *"the benchmark's own unmodified scorer"*, *"our figure, not one the benchmark publishes"*, *"(own run)"*, *"fetched live 2026-07-28, on disk"* — which is itself evidence of a corpus that does the right thing in prose. Only **12 tables** in the tracked markdown corpus carry the Verification Charter §2 `reference` column header (frame: `git ls-files '*.md'`, 367 files). | B6 external-referent audit, 2026-08-11 (§3, §9) | If referent declaration is ever to be machine-checkable it needs a **field**, not prose — the charter's `reference` column made mandatory for gate rows. Filed as a design question, not a defect: the prose is currently better than the field would be. | fleet |

### D-follow-on — the two published results D1 puts in doubt

Filed by the chief, 2026-08-11, on the back of D1. **D1 says the instrument can lie. This
entry asks whether it already did, in something we published.**

A sweep of every commit in this tree since 00:00 today for mutation-proved claims returns
three hits, one spurious:

| Commit | Time | Claim | Why it carries D1's signature |
|---|---|---|---|
| `717d7e7a` | 06:19 | *"mutating `_PLACE_REACH` from 24 to 99 moves…"* | `24` → `99` is an **equal-length** edit — the size half of `(mtime, size)` is preserved by construction. |
| `6b37866a` | 07:14 | *"the recompute moves when I mutate my own sentences"* (V16 fourth grade) | Its own body states the file was **"restored byte-for-byte"**, which reproduces both halves of the invalidation key. |
| `d4509368` | 01:36 | *"the solves did not mutate their own inputs"* | **Not a mutation test** — a different sense of the word. Discarded, and recorded so the next sweep does not re-flag it. |

**Both live ones are POSITIVE results, and that is the dangerous polarity.** Each concluded
that a recompute is alive. Under the inversion D1 demonstrated, the module can hold the
mutated value at run time while the file on disk reads the original — so a test that appeared
to redden on mutation may have been reading stale mutated bytecode throughout, and the
recompute it certified as live may not be. That is a **false pass in a published claim**, not
merely an unverified one.

**What settles it:** re-run those two with `__pycache__` **cleared between every cell**
(NOT `PYTHONDONTWRITEBYTECODE=1` — see D1a; the flag does not stop a stale pyc being read),
asserting all cells **in the same run** so an inversion cannot present as a pass. Routed to
the V16 agent with an explicit instruction not to re-open the rung for it.

**SETTLED 2026-08-11, V16 close-out — both hold, neither was a false pass.** Re-run in a
detached worktree at each claim's own commit, with `__pycache__` deleted before every cell,
each cell a fresh subprocess, and every cell reporting the value seen AT RUNTIME beside the
value ON DISK so an inversion would be visible rather than inferred. Both mutations were kept
**equal-length**, so both sat squarely in the vulnerable class rather than dodging it.

`717d7e7a`, `_PLACE_REACH` 24 → 99 (equal length), harness at `scripts/self_audit.py`:

| cell | expect | runtime | disk | verdict |
|---|---|---|---|---|
| A clean | 24 | 24 | 24 | OK |
| B mutated 24→99 | 99 | 99 | 99 | OK |
| C restored byte-for-byte | 24 | 24 | 24 | OK |

The generated verdict line carried `24 of 45` in A and C and `99 of 45` in B. Claim holds.

`6b37866a`, one sentence in `V16_AUTHOR_HELDOUT_SET.py` mutated `Wu and Zhang` →
`Xu and Zhung` (equal length, one occurrence), test
`test_every_published_reach_figure_recomputes_from_its_sentences`:

| cell | expect | got | runtime has mutant | disk has mutant | verdict |
|---|---|---|---|---|---|
| A clean control | GREEN | GREEN | False | False | OK |
| B one sentence mutated | RED | RED | True | True | OK |
| C restored byte-for-byte | GREEN | GREEN | False | False | OK |

Byte-for-byte restore verified by SHA-256. Runtime matched disk in every cell of both
matrices — no inversion. **D1 says the instrument can lie; these two say it did not lie
here.** D1 itself stays open: the finding is real and the harness it asks for is still not
part of `sdk/tests/`.

**The cheap answer that would settle it for free:** if either mutation was reverted with
`git checkout` rather than a copy, the mtime changes and the stale cache is defeated. One line
of evidence closes it.

**The general form, worth more than the two instances:** every mutation-proved result this lab
has produced is only as good as whether its author happened to clear the cache — and mutation
testing is the main way both live sessions have been proving that recomputes are live. Until
the harness in D1 exists, treat any mutation result produced without the guard as **unproven
rather than merely unverified.**
| D19-D20 note | *[RENUMBERED 2026-08-11 by the chief while landing the grade commit. These two were filed as **D5** and **D6** on a worktree branch cut when the docket ended at D4; `main` meanwhile grew D5-D18, so both numbers were taken. Kept and renumbered rather than merged or dropped -- **the third ID collision in this file today**, all three from concurrent writers who could not see each other. The docket needs allocated IDs, not sequential ones; filed as its own item.]* | | | |
| D19 | **A worktree can isolate a grader from the commit it was sent to grade, and R-ISOLATE's stated caveat does not cover it.** Grade round 5 was dispatched into a worktree whose branch was cut **18 commits behind** the subject: `HEAD` was `2c2c6a9a` (V15 round 5), and the V16 close-out it was sent to execute — `c05a03e0`, `236465a5`, `9c5b7b9d` — was not an ancestor of it (`git merge-base --is-ancestor 9c5b7b9d HEAD` → 1). The V16 work was simply absent from the checkout. R-ISOLATE part 1 already carries a caveat and it is about **gitignored** files; this is a second, independent way the same mechanism fails open, and it produces the identical shape — an honest, confident report about a tree that does not contain the subject. A grader that had gone straight to the files would have returned five UNEXECUTABLEs, or worse, five confident verdicts on the wrong revision. Detected only because `git log --oneline -3` disagreed with the dispatch brief's own quoted commit subjects; recovered with `git merge --ff-only 1393b8b4` inside the worktree. | V16 grade round 5, 2026-08-11 | Have the dispatcher **state the subject SHA in the brief**, and have the agent assert `git merge-base --is-ancestor <subject> HEAD` before it executes anything — the same "print what you can see before you report what you found in it" discipline the gitignore caveat already prescribes, applied to revision rather than to file count. **EXECUTED AND CLOSED 2026-08-15, commit `aebac241`.** Both obligations are now written into `LADDER_V_TRIPLE_VERIFICATION.md:311-315` verbatim: "The dispatch names the subject SHA" and "The agent asserts `git merge-base --is-ancestor <subject> HEAD` before executing anything". The evidence that discriminates a written rule from a followed one is that it is in PRACTICE, not only in doctrine: `V16_GRADE_ROUND6.md:12` and `LADDER_V_V5_GRADE_2026-08-15.md:137` both carry the executed ancestry assertion. **NOT DONE, and it is D21(2):** nothing yet says what a dispatcher should do when the subject is NOT an ancestor -- merge rather than cherry-pick, or re-quote the new SHA. | fleet |
| D20 | **The placement guard publishes four recall figures and no precision figure, and both of grade round 5's findings are precision defects.** `_PLACE_REACH` (three rows) and `_PLACE_REACH_B` all measure MISSES — how much placement language the patterns fail to reach — and the verdict line spends a long paragraph on them. Nothing measures the opposite direction: how often the guard binds an ordinal to an entrant in a sentence that is not a placement at all. The frame's blind-spot list names one over-binding mechanism (item 6, a surname that is also an ordinary English word) and it is not the mechanism either E2 or E4 fails by. A reader is given a well-quantified account of what the guard misses and no account of what it invents — and the guard's own comment argues the invented one is the worse of the two, "because the false positive is loud and the false negative is silent". | V16 grade round 5, 2026-08-11 | A held-out set of sentences that are NOT placements — linear-algebra ranks, dated history, quotations, cross-sentence adjacencies — measured the way the three recall sets are, recomputed from committed inputs, and interpolated into the verdict beside them. `campaign/V16_GRADE_ROUND5_PROBES.py` is a 20-sentence starting point with its controls already separated. **EXECUTED AND CLOSED 2026-08-15, commits `64281b6b` and (extended) `5e4a0cc5`.** `_PLACE_PRECISION` in `scripts/self_audit.py` is a TWO-ROW table -- the author's 41/28/20 and the grader's blind 43/25/19 -- recomputed from committed sentences rather than typed, each row naming its builder AND whether that builder was blind to the current patterns, and interpolated into the verdict through `_place_precision_sentence()` so it reaches the frame line rather than sitting in a comment. That is the precision figure this row said did not exist. See D44, which asked for this row to be marked and whose other half -- the struck "worst case on hard sentences" at `LADDER_V_TRIPLE_VERIFICATION.md:428`, done at `be36e22f` -- is separate. | fleet |
| D21 | **The staleness assertion is unsatisfiable for any subject landed by cherry-pick, and it failed on the first attempt to clear it.** D19's remedy — assert `git merge-base --is-ancestor <subject> HEAD` before executing — was adopted, and the very next use of it was a false STALE. The V16 round-2 author asserted against subject `0a51269d`, got STALE, and stopped; the chief landed the commit as cherry-pick `df53488e` and dispatched "re-run the assertion; it will pass now". **It did not pass and it never will.** A cherry-pick replays the diff under a new SHA (`0a51269d`'s parent is `1393b8b4`; `df53488e`'s is `0049463f`), so the original commit is not an ancestor of `main` and cannot become one. The content is present and byte-identical — every file the two commits touch, plus the two files under repair, matched by blob hash, and the committed probes reproduced the recorded 12-disagree/8-control split with zero drift — while the ancestry proxy says absent. This is the same defect as the E2 finding one rung down: a proxy standing in for the property actually wanted, confidently wrong in both directions. Note the standing cost: **every future agent dispatched to subject `0a51269d` will halt**, because the identifier in the brief now names a commit no ancestry query on `main` can reach. | V16 fix round 2, 2026-08-11 | Two parts. (1) The assertion must be **content-addressed, not commit-addressed**: pair or replace ancestry with `git rev-parse HEAD:<path>` blob identity on the files under repair, plus execution of the subject's committed probes with a drift check — that is what established the position here when ancestry could not. (2) A dispatcher landing a grader's branch should **merge rather than cherry-pick** when the SHA has already been quoted in a brief, or re-quote the new SHA in the dispatch; otherwise the brief's own identifier is stale on arrival. **PROGRESS NOTE 2026-08-15 (PART 1 DONE, ROW NOT CLOSED).** Part (1) is adopted at commit `1bfd3adf`: `LADDER_V_TRIPLE_VERIFICATION.md:329-334` now reads "So pair the ancestry check with a content check". **PART (2) IS ABSENT** -- `git grep cherry-pick` over that file returns **0**, and nothing anywhere in the corpus says what a dispatcher should do when the subject SHA is not an ancestor: merge rather than cherry-pick, or re-quote the new SHA. D19 is closed on the obligation to CHECK ancestry; this row holds the question of what to do when the check says no. | fleet |
| D22 | **A repair's comment states a cause that executing the build it describes contradicts, and the correction it makes to a previous grader is the wrong way round.** V16's round-6 fallback is justified by a comment saying the two false negatives round 5 filed *"stay silent with the linear-algebra discriminator answering correctly"* and that *"distance was not what muted them"* — a correction of round 5's stated cause. Executed against `1393b8b4`, the build round 5 actually graded, `_place_linalg_subject` returns **True** for both shapes, which is the `continue` that discards the placement before the bind loop is ever reached. The discriminator was what muted them; round 5 was right. The correction is true only of the repaired build, where the discriminator now answers correctly and the distance test takes over — which is a statement about the fix, presented as a statement about the defect. The repair itself is needed either way, confirmed by the same execution, and the two figures quoted in the correction (69 and 73) reproduce exactly under the frame that governs, name-start to ordinal. The pattern is the one round 4 already met: a comment explaining why the code is correct, where the explanation is what is wrong. | V16 grade round 6, 2026-08-11 | A convention that any comment asserting **why a previous verdict was wrong** cites the SHA it was executed against and the value it observed, in the same sentence — the numbers here carried a frame and the causal claim carried none. Cheap to satisfy: both facts above came from one script that imports the old and new module side by side and prints the intermediate predicate, not just the verdict. | fleet |
| D23 | **Whitespace collapse makes markdown structure invisible to every clause-boundary rule in the placement guard, so "cannot cross a sentence" means only "cannot cross `[.!?;:]`".** `_placements` flattens the surface with `re.sub(r"\s+", " ", text)` before any boundary test runs, and every boundary rule downstream — `_PLACE_CLAUSE` for the subject-NP cut, `_PLACE_SENTENCE` for the bind probe — is a punctuation rule. A markdown heading, a list item and a table cell all end a unit of meaning **without terminal punctuation**, so after flattening they are not boundaries at all and the guard reads across them as if the text were one sentence. This corpus is predominantly markdown, which makes the unpunctuated boundary the ordinary case here rather than the exotic one. Executed at `72609a04`: three shapes bind an ordinal to an entrant named in the previous structural unit at gaps of 65, 69 and 54 characters against a `_PLACE_BIND` of 40, and the identical text with a full stop inserted is correctly silent. | V16 grade round 6, 2026-08-11 | Either flatten to a **boundary-preserving** representation — substitute a sentinel for a blank line, a heading, a list marker and a cell separator before collapsing the rest, and add that sentinel to `_PLACE_CLAUSE` and `_PLACE_SENTENCE` — or state in the frame that the guard's unit is the punctuation-delimited clause and not the sentence, so the written claim matches what runs. The first is a few lines and testable; the second is free and honest. **PROGRESS NOTE 2026-08-15 (THREE SHAPES FIXED at commit `64281b6b`, ROW NOT CLOSED).** `_place_flatten` now preserves structural newlines and `_PLACE_CLAUSE` is `[.!?;:\|\n]`; executed, the three shapes at gaps 65, 69 and 54 no longer reproduce -- a heading followed by a placement at gap > 40 returns `[]`. **THE ROW STAYS OPEN ON ITS OWN REMEDY, which named TWO regexes and only one got the sentinel:** `_place_sentence_break("foo\nA")` is still **False**, so a heading boundary INSIDE 40 characters still binds -- executed, `"## Wu and Zhang ran the duct case\n\nThe rank 4 entry..."` still produces 1 fault. That residual overlaps D25, where the same two rules are shown to disagree across a semicolon. | fleet |
| D24 | **The bind's boundary probe cannot tell an abbreviation-final period from a sentence-final one, and both the old and the new form get one of the two cases wrong.** `_PLACE_SENTENCE` fires on `[.!?]` + optional closers + whitespace + an uppercase-or-opening character. Until `72609a04` the probe appended the real next character, so a period followed by a digit or a lowercase token was not treated as a boundary — the defect grade round 5 filed, six shapes binding straight across a full stop. The repair appends a constant that is unconditionally uppercase, which fixes all six and, by the same stroke, makes **every** period-plus-space a boundary, including the one closing an abbreviation. Executed old-vs-new in one invocation with `__pycache__` cleared first: an abbreviation followed directly by an ordinal, and by an ordinal-plus-noun, faulted at `1393b8b4` and are silent at `72609a04`. Both are real wrong placements, now missed. The direction is the cheap one, but the comment making the trade says the appended character's identity *"was never doing any work"*, and on this class it was doing exactly one job. | V16 grade round 6, 2026-08-11 | A boundary test with a short abbreviation exception — the closed set that actually occurs in this corpus (`et al.`, `Fig.`, `Eq.`, `Sec.`, `No.`, `cf.`, `e.g.`, `i.e.`, `vs.`) checked against the token immediately before the period — plus a two-direction control set beside the existing over-fix controls: shapes that must still bind after an abbreviation, and shapes that must stop binding after a sentence. The current controls only test one of the two directions, which is why the trade landed unnoticed. **EXECUTED AND CLOSED 2026-08-15, commit `64281b6b`.** Verified BY EXECUTION IN BOTH DIRECTIONS with `__pycache__` purged, which is the only reading that distinguishes a fixed matcher from a matcher that stopped matching: "...by Wu and Zhang et al. rank 4 is the one we mean." FAULTS (the abbreviation period is not a sentence end), "...et al. fourth place was theirs." FAULTS, and "Wu and Zhang did the duct case. Rank 4 is Montoya's." is SILENT (a real sentence end still breaks the bind). The mechanism is `_PLACE_ABBREV` plus `_place_sentence_break`, and the two-direction controls are pinned in `sdk/tests/test_rank_claim_surfaces.py` under `TheAbbreviationPeriodIsNotASentenceEndTests`. | fleet |
| D25 | **Two boundary rules run inside one function and they disagree about what a boundary is, so a semicolon stops the fallback and does not stop the ordinary bind.** `_place_subject_np` cuts the subject phrase at `_PLACE_CLAUSE`, which after this round is `[.!?;:\|\n]`. The `_PLACE_BIND` loop twenty lines above it tests `_PLACE_SENTENCE`, which reads only `[.!?]`. So the SAME surface, with the SAME two words at the SAME distance, is a boundary for one rule and not the other, purely by which of the two paths the ordinal happens to take — and which path it takes is decided by whether the entrant sits inside 40 characters. Found by the precision set built for this round, not by a probe aimed at it: an ordinal with a position word, an entrant named before a semicolon and inside the bind, binds across the semicolon and faults. It is one of the twenty false FAULTs the published figure now counts, in class `cross-sentence`. **Not fixed here, and the reason is scope, not merit** — R-CONVERGE bounded this round to four deliverables and widening the bind's boundary test is a fifth behaviour change, which is precisely the "one more discriminator" move the ruling that governs this round exists to stop. | V16 round-6 close-out, 2026-08-11 | Give both rules the same boundary predicate — most cheaply by having the bind loop consult `_PLACE_CLAUSE` for the clause-level marks and keep `_PLACE_SENTENCE` for the sentence-level ones — and measure the result on the precision set AND all four recall sets in the same invocation, since a wider boundary buys precision with recall and this rung has already traded one for the other twice without measuring both ends. | fleet |
| D26 | **A test whose whole subject is "the set must be derived, not listed" enumerated its own evidence files by hand, and the sixth file added was the one that needed it.** `test_the_evidence_files_are_not_themselves_corpora_of_faults` guards the committed held-out sets against becoming corpora of the very defects they describe (docket D4). It named five files in a literal tuple. The file added this round was written with one position word unsplit, and the guard reported that source as carrying a real fault — caught, but caught by the pre-commit sweep the brief mandates rather than by the test, and a seventh file added by an agent who does not edit the tuple would be unguarded with the suite still green. This is the identical shape as the defect the whole test file was created for, quoted from its own docstring: *the list was the defect.* Fixed in the same commit by globbing the evidence directory with a floor on the count, so a set that vanishes fails rather than passes vacuously. Filed anyway, because the finding is that **a lab can restate a principle in a docstring and violate it eleven hundred lines below**, and the general form is worth more than the instance. | V16 round-6 close-out, 2026-08-11 | Executed for this instance. The general form is a sweep for *literal collections of paths inside tests whose stated property is derivation* — there is no reason to think this file is the only one, and the two places already found (the original `check_rank_claim_surfaces` surface list, and this) were both found by accident rather than by looking. **PROGRESS NOTE 2026-08-15 (INSTANCE FIXED, GENERAL REMEDY ABSENT, ROW NOT CLOSED).** The named instance is repaired: `sdk/tests/test_rank_claim_surfaces.py:2184-2187` globs `campaign/V16_*.py` and asserts `assertGreaterEqual(len(evidence), 5)`, so the set is derived with a floor rather than enumerated. **THE ROW ASKS FOR A SWEEP, and the sweep does not exist:** nothing in `scripts/` or `sdk/tests/` looks for literal path collections inside tests that CLAIM to derive their set. Closing on the instance would close the row against the one defect it was filed to generalise beyond. | fleet |
| D27 | **The precision denominator that closes V16 is single-sourced, and its "WORST CASE" framing is falsified by the first independent set built against it.** The rung's closing condition is a published precision figure; the figure is 20 of 41 (49%) on a non-placement set built by the guard's own author *with the pattern list in hand*, and the generated verdict paragraph calls it "a WORST CASE on hard sentences". Grade round 7 built a disjoint 43-sentence set blind -- author's set and `_place*` patterns both unread until after the sentences were written -- and applied the author's own admission rule unchanged (only sentences a rule-A pattern actually matches enter the denominator; 18 of 43 excluded). Result: **19 of 25 (76%)**, Fisher exact two-sided **p = 0.040**, Wilson 95% intervals 57-89% against 34-64% -- the independent LOWER bound sits above the author's point estimate. Split by provenance: the four classes the ruling itself names went 9 of 15 (60%); the seven classes the grader invented went **10 of 10 (100%)**. So "worst case" is not a bound, it is an author's-set case, 27 points optimistic -- a false written claim of exactly the kind L-76 exists for, and the fourth such absolute this rung has produced. The lab already solved this one screen higher: `_PLACE_REACH` publishes THREE reach rows, each naming who built it and whether they were blind, under a note that "two honest sets disagree by twenty points". `_PLACE_PRECISION` publishes one row from the least independent possible source. | V16 grade round 7, 2026-08-11, subject `64281b6b` | Make `_PLACE_PRECISION` a multi-row table shaped like `_PLACE_REACH`, carrying the grader's row with `blind=True` beside the author's `blind=False`; delete "a WORST CASE" and let two disagreeing rows say what one row cannot. The set is committed at `campaign/V16_GRADE_ROUND7_PRECISION_SET.py` and recomputes, so it cannot go stale (L-79). **General form, worth more than the instance:** any figure this lab publishes about its own cost should carry the provenance of the sample it was measured on, because the sample's author is a variable in the result. **EXECUTED 2026-08-11 by V16 round 8, `5e4a0cc5`.** `_PLACE_PRECISION` is a two-row table shaped like `_PLACE_REACH`; both rows name their builder and their blindness and both recompute from committed sentences; the verdict computes the 27-point spread and which row is higher rather than asserting either; and "a WORST CASE on hard sentences" is withdrawn from both places it stood, quoted beside its falsification (L-76) and added to `_FALSIFIED` so reinstating it as a claim reddens the suite. | fleet |
| D28 | **The test that enforces the "knowingly accepted" proviso is scoped to the author's own set, so it cannot see a shape the author never thought of.** The V16 ruling permits a false-FAULT shape to stand unfixed PROVIDED it is counted in the precision figure and named in the blind-spot enumeration, and `test_every_false_fault_class_is_counted_and_named` enforces that well: dict equality in both directions plus a sum, so a shape cannot be counted in the figure and left out of the list. Verified binding. But both sides of that equality are computed over `campaign/V16_PRECISION_SET.py`, so the proviso is self-certifying one level up -- it is the same L-74 circularity as the figure itself, moved from the number to the test that guards the number. Executed: an independent set found two rule-A false-FAULT shapes outside the nine-class enumeration -- **negation/interrogative** (an ordinal denied or questioned, never asserted) and **a present-tense placement on a different, explicitly named ranking** (which is not `dated-history`; nothing about it is dated, and it disclaims the closure board in the same sentence). Neither is counted; neither is named; the suite is green. A rule-A fault on a travelling surface is FAIL severity, and a citation ranking is a thing a travelling submission document plausibly contains. | V16 grade round 7, 2026-08-11, subject `64281b6b` | Either enumerate the two shapes with their counts, or state explicitly that the enumeration is scoped to the author's set rather than to the guard. **General form:** a proviso of the form "accepted IF counted and named" is only as wide as the corpus the counting runs over, and that corpus should be named wherever the proviso is claimed. **EXECUTED 2026-08-11 by V16 round 8, `5e4a0cc5`** — and the remedy taken was the first option, not the second: the test now binds against the grader's blind set as well as the author's, so a false FAULT in a shape nobody enumerated reddens it. Mutation-proved with the cache cleared and control and mutants in one invocation. Two classes (`other-named-board`, `negated-or-questioned`) entered the enumeration because of that binding and are filed as D46. **The residual circularity — the label→class assignment is still the author's — is filed separately as D47 rather than treated as closed.** **EXECUTED AND CLOSED 2026-08-15, commit `5e4a0cc5`.** `test_every_false_fault_class_is_counted_and_named` now binds against the GRADER'S BLIND SET as well as the author's, and its docstring names this row and the two classes -- `other-named-board` and `negated-or-questioned` -- that entered the enumeration BECAUSE of the second binding. That is the discriminating evidence: a test bound to one set can only enumerate the classes that set happens to contain, and two new classes appearing on the second binding is what a real widening looks like. The residual was correctly filed as D47 rather than folded in here, and D47 remains open. | fleet |
| D29 | **A gate can be non-monotone in the property it names, and G2 is: it awards its worst possible score to a perfectly localised correction.** `np.quantile(dev, 0.9)` returns **0.0** when fewer than 10% of cells deviate, so `dev >= thr` selects the whole domain and the top-decile share collapses to the window's base rate. Executed on the real mesh (21,000 cells, window 1,773 = 8.44%): pure noise confined **entirely inside the window** scores **8.4% and FAILS**, while the same noise smeared over the 14.14% W2 support scores **59.5% and PASSES**. Negative control passed -- noise over the whole domain returns 8.3%, so the instrument can return the null. **This did not cause the recorded 26.9%** (both real beta fields deviate in nearly every cell and sit far from the collapse); it is independent proof that the metric is invalid, not the explanation of the value. **General form:** any gate of the shape "fraction of the top q-quantile falling inside region R" silently inverts when the support of the deviation is smaller than the quantile, which is exactly the regime a *localisation* gate is supposed to reward. | Closure task 2, 2026-08-11, subject `64281b6b`, evidence `demo-output/website/campaign/CLOSURE_STAGE1_AND_C2_STATUS.md` §1.5 at `e43084c8` | A quantile-based spatial gate declares its behaviour when the deviating set is smaller than the quantile, or uses a base-rate-free enrichment statistic instead. The S1-with-priors pre-registration is the next place this choice gets made, and it must be made **before** that line's first compute (§2b). | fleet |
| D30 | **The lab's "four official scoring calls, ever" is self-imposed discipline, not compliance -- the benchmark ships the test ground truth and instructs previewing against it.** The eval package contains `src/closure_challenge/data/ground_truth_test.npz` and the benchmark README tells submitters "You can preview what your score will be using the benchmark dataset's python package." There is **no limit on scoring calls and there cannot be one**; the only published rule is the strict prohibition on *training or validating* on test cases. `CLOSURE_CHALLENGE_SUBMISSION_DRAFT.md:169-172` already states this correctly, but the constraint is recited elsewhere in a register that reads as an organiser requirement. **Naming it as a choice is worth more than obeying it silently:** a stricter-than-required protocol is a credibility asset only if the record says it was elective. | Closure task 2 submission-policy research, 2026-08-11, subject `64281b6b` | Every surface that recites the scoring-call count labels it as lab discipline rather than benchmark rule, per the §6a principle that a verdict carries what it was checked against. **PROGRESS NOTE 2026-08-15 (CANNOT DETERMINE THAT THIS ROW'S OWN SURFACES WERE FIXED -- deliberately NOT closed).** Every surface that recites the count AS A CONSTRAINT is labelled today: `ACTIVE_RESEARCH.md:837`, `CLOSURE_CHALLENGE_STATUS.md:426,606,897`, `apply_closure_ph_gate.py:51-52`, `export_closure_submission_csvs.py:27-36`, all three `MANIFEST.json`, `latex/closure_challenge_report.tex:2302`, and `CLOSURE_STAGE1_AND_C2_STATUS.md:499`. A sweep of every tracked file containing "official scoring call" leaves 7 unlabelled, and each of those uses the phrase DESCRIPTIVELY ("zero official scoring calls", dated LADDER_V history) rather than as a rule. **WHY THIS IS NOT A CLOSURE:** several of the labels predate this row by ten days (commit `fe121af2`, 2026-07-31), and the row never named the surfaces it meant. So it cannot be shown that the offending surfaces were REPAIRED rather than never identified, and a closure note asserting a repair would be asserting something no evidence here supports. | fleet |
| D31 | **Five of the eight submission-policy questions have no answer in any open source, and four of them are the ones that bind a first send.** Verified from fetched primaries with positive controls (the same grep found `strictly forbidden`, `anytime` and `authors` in the same documents, so the zeros are real absences): **no published rule exists** on (a) entries per team -- no limit stated *and* no statement that multiple are allowed, (b) whether a corrected resubmission is permitted after scoring, (c) whether hyperparameter tuning must be disclosed, (d) what licence submitted predictions and code fall under -- the GitHub API returns `license: null` for both repos that would host them, (e) whether a company rather than named individuals may occupy the "Authors" row. Practice evidence exists for (b) -- an open PR was scored, and the steward re-scored all entries in place at `44540c8f` -- but practice is not a rule. **All five are steward questions, not document questions**, and the README invites them. **Addendum 2026-08-12:** audited against the challenge README as pasted by Katie from the live page on 2026-08-12 (not fetched by the fleet; operative wording identical to the pinned clone at `deb91557`, README lines 92–100, so the procedure did not change when upstream HEAD moved to `d572d40c`): the paste re-confirms the three already-answered questions — registration (none), attribution (test subdirectory + authors + references to rmcconke@mit.edu), deadline (none, "Submissions are accepted anytime!") — and the strict rule verbatim, and **answers NONE of (a)–(e); all five remain open and remain Katie's steward questions.** The relaxed-rules sentence ("free to use your own input features, base turbulence model, data assimilation technique") broadens what is permitted and says nothing about disclosure, so (c) does not close. Full audit with the measured readiness checklist and per-file conformance verification: `demo-output/website/CLOSURE_SUBMISSION_REQUIREMENTS.md`, written 2026-08-12 at `5c9c63fb`. | Closure task 2 submission-policy research, 2026-08-11, subject `64281b6b`; addendum 2026-08-12, Katie's paste of the live README | **Katie's, and only Katie's.** Contacting the steward is an external interaction reserved to her in two charters. This entry records that the questions exist and are unanswerable from documents -- it does not authorise asking them. | Katie |
| D32 | **The L-76 enforcer cites thirteen tests, none of which exists, and its own missing-check class is structurally unable to fire on it.** `scripts/check_absolutes.py` backticks 13 distinct `test_*` names under the literal word *"Evidence:"*. **0 of the 13 resolve to a `def` in any `.py` on disk** (frame: `find` under the repo root, `.git` and `.claude/worktrees/` excluded; 1,459 real test defs found in the same sweep, so the finder works). Worse, the module's stated rule -- *"a cited test that does not exist is reported as its own class, `CITES_MISSING_CHECK`, and is counted as unbacked"* -- **does not hold in its own code**: `classify_unit` tests `elif resolved:` **before** `elif dangling:`, so a single runnable command anywhere in the same prose unit launders every dangling name into `BACKED`. Executed on the module itself: **BACKED 15, CITES_MISSING_CHECK 0**. Injection control, both directions: the unit *"This never fails. Evidence: `test_does_not_exist_anywhere_at_all`."* classifies `CITES_MISSING_CHECK`; the same unit with `python3 scripts/check_absolutes.py --frame tracked` appended classifies `BACKED`. The module's `resolved` list for its own docstring is `['<command>', 'path.py:function']` -- and `path.py:function` is the metasyntactic placeholder from its own prose. | V15 round 6, 2026-08-11, subject `2266c4e3` | Either write the 13 tests or delete the citations; and reorder the verdict cascade so `dangling` is consulted before `resolved`, with a test that plants a dangling name beside a runnable command. Until then every `BACKED` this checker reports is unsafe, and B3a's published 74% false-positive rate was measured on `UNBACKED` only -- `CITES_MISSING_CHECK` has no measured rate at all. **EXECUTED AND CLOSED 2026-08-11, commit `89c2a467`, subject `f6b4eec0`.** Both halves done, and the second half was done per citation rather than wholesale. **(1) The cascade is reordered** -- `dangling` is asked before `resolved` -- and the mixed unit is DECIDED rather than left implicit: **any dangling name makes the unit `CITES_MISSING_CHECK` even when a sibling citation resolves.** The argument is written at the branch and it is about the reader, not the arithmetic: someone who spot-checks one of five citations and finds it good does not check the other four, so one real name beside four fictional ones misleads him more than no citation at all. The rejected alternative (report the dangling ones as a note) is named there too, with its reason -- a note is not a verdict and nothing downstream counts it. The cost is stated: this is strictly the more severe rule, so the reason string now says `mixed unit: N other citation(s) here do resolve, and that does not rescue it`. **(2) Of the 13 citations, 11 were WRITTEN and 2 STRUCK.** Written into `sdk/tests/test_absolute_claims.py` (23 tests, 8 classes) because the claim beside each is load-bearing: the frame is derived from `git ls-files` rather than enumerated, the verdict is three-valued with skips counted per reason, backing is unit-level, the four gates each reject with a positive control surviving, the measured rate is recomputed from the fixture at call time and replays through the same `classify_unit` the corpus sweep uses. Struck where the name was aspirational, with the reason left in place at each site: the one asserting the audit's published rate matched the recomputed one (it does not -- **D50**), and the one asserting the sample was drawn from the checker's own output (a past sampling act at `8ebe2b8e` that no execution today repeats). **Neither struck name is repeated in the module even as a corpse**, since by its own rule a name resolving to nothing reads as backing. Also fixed: `FILE_FUNC_RE` was reading the metasyntactic placeholder `path.py:function` out of the module's own prose as a citation -- now written `<path>.py:<function>` (see **D51**). **NONE OF THE 13 WAS A DELETED TEST.** `git log --all -S"def <name>" -- '*.py'` returns zero commits for all thirteen and `sdk/tests/test_absolute_claims.py` had never existed in history, so they were fictional rather than decayed -- a different finding from a citation that rotted. **SELF-CLASSIFICATION, `--paths scripts/check_absolutes.py`, corpus as it stood at each cell:** BEFORE (code and prose as committed at `8a8b5392`) **BACKED 15, CITES_MISSING_CHECK 0, UNBACKED 2**, with **13 distinct dangling names silently discarded**; **ORDERING FIX ALONE**, same prose, same corpus: **BACKED 0, CITES_MISSING_CHECK 15, UNBACKED 2** -- the instrument indicting itself, which is the proof the fix reaches; AFTER (fix + citations settled) **BACKED 15, CITES_MISSING_CHECK 0, UNBACKED 2, 0 dangling**. **MUST-NOT-MATCH CONTROLS (L-84) AT TWO SCALES.** Unit: a unit whose every citation resolves is `BACKED`, and a unit citing only a runnable command is `BACKED` -- so the fix is not "flag everything". File: `UNBACKED` stays 2 and the verdict on itself stays `FLAGGED`, asserted by a test -- so the fix is not "switch it off". The two survivors are real and left standing (*"Every word the docket names has a family here"*, *"never enumerated by path"*). **AND THE CONTROL THAT DECIDES WHAT THE EVIDENCE IS.** Once the citations are real, the OLD order gives the SAME file-level counts as the new one, so **the file count no longer discriminates between fixed and unfixed** and cannot be the proof. The discriminating evidence is the unit-level mutation replay -- control and mutant loaded in ONE invocation with `__pycache__` purged -- where **exactly 2 of the 23 tests fail on the pre-fix order and 0 on the fix**. The two are the ones planting a dangling name beside a runnable command and beside a resolving test; the other 21 pass on both, which is what a test that pins prose rather than mechanism should do. **Not touched, on purpose:** no matching retuned, no gate added, no corpus sweep re-run, and the 74% figure not chased -- that is B3a/B3b, and two things found in passing are filed to D50 and D51 rather than fixed here. | fleet |
| D33 | **"Not one log file is cited by path anywhere in the committed markdown corpus" is false, and the recommendation built on it would break a published citation.** `docs/TRACKED_ARTIFACT_SCOPING.md:69` publishes **0** for logs cited by exact path, and `§5` recommends *"Move solver **logs** first -- 778 files, no path citations"*. Re-executed with a broader citation vocabulary -- every path-like token in all 372 tracked `.md` files rather than only `*_runs/`-shaped tokens -- finds **1**: `demo-output/website/tmr/runs/naca-a10-medium/log.checkMesh`, tracked, cited by exact backticked path at `demo-output/website/tmr/C4_naca0012_closure.md:80`. The published harvester misses it because the path is under `tmr/runs/`, which carries no underscore and so matches no `*_runs/` token. **The sibling claim holds**: 0 of the 8,582 solver-output files are cited by exact path under the broad vocabulary too, and `scripts/corpus_figures.py` reproduces 8,582 exactly at `2266c4e3`. The defect is the restricted vocabulary, which the frame does not state. | V15 round 6, 2026-08-11, subject `2266c4e3` | Re-run the citation harvest with no `*_runs/` restriction, publish the corrected log figure, and state the vocabulary in the frame. `§4` lists three ways the measurement could be wrong and this is not one of them. **EXECUTED AND CLOSED 2026-08-15, commit `6a78acac`.** `docs/TRACKED_ARTIFACT_SCOPING.md:70` now reads `~~0~~ -> **1**`, the absolute sentence is struck, and -- this is the part that matters -- the BROAD re-derivation is published at `:81-83` with its frame stated: 204 log-shaped tokens swept, exactly 1 resolves. The solver-output row was re-checked under the same broad vocabulary and SURVIVES at 0 of 8,582, so the widening was applied to the claim that held as well as to the claim that broke, which is what stops a correction becoming a selective one. Section 5's recommendation is rewritten at `:149`. **NOT DONE:** the counterexample citation is still live at `demo-output/website/tmr/C4_naca0012_closure.md:80`. | fleet |
| D34 | **[WITHDRAWN 2026-08-11 by its own author, subject `0be3f1d0`. E1 was right; this entry was wrong in all three of its parts, and the defect was in MY frame.** The disagreement is not a moving tree: both frames run over the identical 34-check population (`set(check_* attrs) - set(CHECKS)` is empty), and the replay at `e551f68f` gives the same answer as HEAD. **The whole difference is one check.** I widened the blind-spot keyword to `blind|cannot see|does not see|unable to see|not detected|out of reach|no visibility`; the chief matched `blind` alone. My extra hit is `check_record_writers_name_their_drops`, and its `cannot see` is the check **reporting a defect it found** -- *"`mesh_validity()` builds a record from `wall` ... so **this function** cannot see what its caller put in it"* -- where the subject is the audited function, not the instrument. E1's question is what the checks say **they** cannot see, so my match is a false positive and the count is **3 of 34, 31 silent**, exactly as published. **(2)** Not "wrong when written" -- nothing was wrong. **(3)** No contradiction with the ledger: `INSTRUMENT_INTEGRITY_LEDGER.md:219` counts checks whose `BLIND TO:` line the terminal printer emits at `self_audit.py:3713` (gated on `GENERATOR`/`TRANSCRIBED`/shared-symbol), which is a different measurement from what a `Result` says; **0 of 34 checks reprint their own declared BASIS text**, so 3 and 4 were never the same quantity. **The lesson, and it is D20's defect committed by the agent auditing D20:** I broadened a keyword list for recall and ran no precision control over the broadening, so I bought one extra hit and it was noise. A paraphrase list needs a set of sentences that must NOT match, measured, before the widened figure is published -- which is exactly what B3b already records about keyword-family checkers. The one part worth keeping is minor and stands: **E1 carries no commit anchor (W-5).** The original entry follows unedited.]** ~~Docket E1's headline correction is itself the error: it is 30 of 34, not 31.~~ E1 reads *"MEASURED 2026-08-11: it is 31 of 34, not 30 ... Three state one; thirty-one state none"*. Executed all 34 callables in `scripts.self_audit.CHECKS`, searching `summary` and `detail` for a blind-spot marker: **4 state one, 30 state none, 0 raised** -- `check_rank_claim_surfaces`, `check_board_placement_words`, `check_record_writers_name_their_drops`, `check_every_check_states_its_basis`. This is not rot: `scripts/self_audit.py` **as of `e551f68f`, the commit that filed E1**, replayed against the current tree gives the same 4 / 30, and all four emitters already existed there. So E1 corrected a right number to a wrong one, in the direction that makes its own finding sound worse, and it disagrees with `docs/INSTRUMENT_INTEGRITY_LEDGER.md:25` and `docs/LEDGER_HEADLINE_AUDIT.md:437` -- both of which say 30 / 4 and both of which are right. E1 also carries no commit anchor, which is W-5. | V15 round 6, 2026-08-11, subject `2266c4e3` | Correct E1 to 4 / 30 with its anchor, and note that its own positive-control paragraph (*"my first run returned 0 of 34"*) did not catch the residual off-by-one. | fleet |
| D35 | **`docs/MISSION_TOKEN_COSTS.md` declares two mission classes at a token boundary that is the eighth-largest of eleven gaps in its own data.** The document states *"There are two mission classes here, not a spectrum"* and gives them as **88k-160k** and **164k-309k**. Sorted, the twelve token totals are 88, 103, 157, 160, 164, 168, 185, 196, 197, 228, 249, 309. The claimed split is the 160 -> 164 gap: **4k, which is 1.8% of the 221k range and ranks 8th of the 11 gaps** (the largest is 249 -> 309 at 60k, inside the class the document calls homogeneous). The token distribution is continuous across the declared boundary. **The split is real in the other two columns** and the document half-notices it -- tool calls jump 5 -> 33, a 28-call gap that is the largest of 11 and 40% of that range. So the finding is sound and the axis is wrong: the discriminator is tool calls, not tokens, and the class ranges are stated in tokens first. Secondarily, class 2 is described as *"15 to 22 minutes"* while containing its own 27-minute and 62-minute rows. | V15 round 6, 2026-08-11, subject `2266c4e3` | Restate the classes on the tool-call axis with the token range as a consequence rather than a definition, and either drop the wall-clock range or widen it to cover the rows it contains. | fleet |
| D36 | **R-ISOLATE's own leftover worktree silently doubles two of the four frames in `scripts/sweep.py`, the instrument built the same evening to stop exactly this.** `.claude/worktrees/agent-ad85b7e8a54a4a57b/` is a full second copy of the repo -- **20,579 files**, checked out at `0a51269d`, ~3.5 hours behind `main` -- and it is excluded via `.git/info/exclude`, not `.gitignore`. Consequence, measured: `everything` selects **80,493** files against the module docstring's 59,560, and `worktree` **78,414** against 58,083, both ~35% inflated, entirely by that one directory. A sweep for `R-ISOLATE` over `*.md` returns **8 files under `worktree` and 4 under `tracked`** -- exactly 2x, because it is the same four files twice. `tracked` and `ignore-honouring` are unaffected. The `worktree` frame's own words are *"drops .bzr/.git/.hg/.jj/.sl/.svn directories"*: it prunes `.git` **directories**, and a nested worktree's metadata is a `.git` **file**, so the working copy walks straight in. The docstring's four numbers are anchored to `83ed6dab` and so are visibly dated rather than rotted -- but any `everything` or `worktree` sweep run today is silently double-counting, which is L-75 reintroduced by the rule written to prevent the class. | V15 round 6, 2026-08-11, subject `2266c4e3` | Add a fifth frame, or prune nested worktrees from `worktree`/`everything` and name the prune in the frame's own words; plus a test that plants a nested worktree and requires the frames to disagree. Separately: R-ISOLATE's dispatcher obligation should include tearing the worktree down, since the ladder doc already makes merging the output branch the dispatcher's job. | fleet |
| D37 | **`docs/MEMORY_ARCHITECTURE.md:852` misquotes the verification charter's version line by two bumps, inside the lab's own register of stale date literals.** It reads `` `VERIFICATION_CHARTER.md:3` (*"Version 1.6, dated 2026-08-11"*) ``; the actual line 3 reads *"Version 1.8, dated 2026-08-11."* (`2d0e5260` took it to 1.7, `be1b955c` to 1.8, both today). The register rotted in precisely the manner it documents. Adjacent, same class: `VERIFICATION_CHARTER.md`'s own version-history block covers 1.1-1.6 only -- **sections 2a, 2b and 6a appear in no changelog entry**, breaking a convention every prior bump kept; and three surfaces still cite `SUPERVISION_CHARTER.md` as **v1.0** after today's 1.1 bump (`docs/charters/PROPOSALS_OPEN.md:37`, `docs/standards/INFRA_FAMILY_SUPERVISION_GUIDELINES.md:113`, `docs/PRODUCT_LIST.md:512`). | V15 round 6, 2026-08-11, subject `2266c4e3` | One sweep over every tracked file quoting a charter version literal, then the changelog entries for 1.7 and 1.8. This is the cross-file propagation gap `MEMORY_ARCHITECTURE.md:861` already names, recurring on the file that names it. **SCOPE EXTENDED 2026-08-12 at `3f2f9fe6`: a fourth surface, worse in kind than the three above and outside this row's original list.** `demo-output/website/AGENT_MODEL_DISTRIBUTION.md:7` does not merely cite a stale version number — it **quotes the now-struck SUPERVISION_CHARTER §5 sentence in full and presents it as a live standing rule**, after that sentence was struck in place at v1.2. The three surfaces already named here are stale *pointers*; this one is a stale *rule*, which a reader can act on. It was found by the charter amendment's own sweep and **deliberately left untouched**, on the ground that propagating a rule Katie has not yet ruled on would spread a decision nobody has made — so this row now carries it as a known-stale surface awaiting her ruling, not as an open repair. **SWEEP EXECUTED 2026-08-17, re-derived at `3af826ed` after a box poweroff killed the first pass, and the two derivations agree citation for citation. This row's stated remedy -- *"one sweep over every tracked file quoting a charter version literal"* -- is discharged, it repaired nothing, and that is the finding rather than a shortfall.** Frame, stated first because the frame is most of the answer, and stated twice because there are two of them. **Tracked corpus, which is the frame this row's remedy names, taken in a clean detached worktree at `3af826ed`: 13,178 text files read, 1,113 binary skipped, and 0 of those binaries mention a charter at all**, so no PDF or archive surface is passed over in silence -- **89 attached citations across 58 files**. **Whole live working tree except `.git`, the wider frame, read by the same instrument: 61,503 text files read, 2,699 binary skipped -- 99 attached citations across 62 files.** The ten-citation gap is decomposed rather than smoothed. Five are the untracked vendored copy under `dist/certonomous-demo/sdk/`, byte-identical duplicates of five tracked `sdk/chief_engineer/` citations, which is the surface D61 already records as invisible to `git grep -- sdk`. **The other five are this append's own reflection, measured rather than assumed: `docs/DOCKET.md:230` carried 2 attached citations before it and carries 7 after, two of them naming a current version.** Charters were matched by filename token and by prose name, the nine current versions were read from line 3 of the nine charters rather than from any brief, and a hit counts as a citation only where a version literal is syntactically attached to the charter name. Three completeness checks rather than an assertion: 68 further sites named a charter with a version only NEARBY, within plus-or-minus six lines, and every one was a mention whose number belonged to something else; 53 matched the bare *"charter N.M"* form with no `v` or `version` prefix, and every one was a section number (9.6, 2.1, 3.2) or a proposal id (P-6.1); and counting every match on a line rather than the first per charter moved the tracked figure by 0, so no tracked line hides a second citation of the same charter behind the first. **NOT ONE OF THE 89 NAMES THE VERSION ITS CHARTER NOW CARRIES.** Classified before anything was touched. **88 are dated records** -- they name a version in order to date a statement, they are correct as written, and they were left byte-identical. **1 means "the current charter"**, and it is this row's own scope extension, `demo-output/website/AGENT_MODEL_DISTRIBUTION.md:7`. **8 quote charter text** and were checked against the current text instead of being renumbered, found by screening every citing line for quotation markers rather than by eye: **3 quotations still hold** (`scripts/withdrawal_sweep.py:6` on SUPERVISION 3a, `docs/standards/MESH_STANDARD.md:11` and `demo-output/website/campaign/MESH_BIRTH_CERTIFICATE_AUDIT_2026-08-08.md:4` on VERIFICATION 9's birth-certificate clause, all three compared byte-for-byte against `101079fd` and against `VERIFICATION_CHARTER.md:1009-1010` and unmoved by the style amendment) and **5 are stale in substance** (`AGENT_MODEL_DISTRIBUTION.md:7`, `MEMORY_ARCHITECTURE.md:672` and `:879`, this row itself, and `LADDER_V_V15_ROUND6.md:328`), so renumbering those would have hidden a wrong quotation under a right number, which is less detectable than an obviously old one. **NOTHING WAS RENUMBERED, and the second class is not empty by accident: its single member is the one SUPERVISION_CHARTER 5a leaves open.** Repairing `AGENT_MODEL_DISTRIBUTION.md:7` means propagating 5's model-designation rule while 5a(d) states in terms that the standing rule is Katie's and still open, so it stays exactly as this row's 2026-08-12 extension left it, and for the same reason. **THREE CORRECTIONS TO THIS ROW, appended rather than edited in, because the row's figures describe the frame it names.** (i) `docs/PRODUCT_LIST.md:512` is the wrong line, and was already wrong when the scope extension was written: at `3af826ed` line 512 is about a DAFoam in-process primal restart. The supervision citation is at **`:571`**, inside the dated `### 2026-08-07 (night)` changelog entry, which makes it a snapshot rather than a pointer. (ii) `docs/MEMORY_ARCHITECTURE.md:852` has drifted to **`:879`**, and `VERIFICATION_CHARTER.md:3` now reads *"Version 1.9, dated 2026-08-17"*, so this row's own *"Version 1.8, dated 2026-08-11"* is stale by one further bump. That is the point rather than an embarrassment: the register and the row that catches the register rot at the same rate, and neither is repaired by renumbering. (iii) **The three surfaces this row calls stale pointers are all dated records.** `PROPOSALS_OPEN.md:37` sits inside its **What changed in 2.4** block; `INFRA_FAMILY_SUPERVISION_GUIDELINES.md:113` is that file's own *"Version 1.0, dated 2026-08-07"* changelog entry, recording which charter version it was issued under, and it is the only such statement in the file; `PRODUCT_LIST.md:571` is the 2026-08-07 snapshot named above. Renumbering any of the three to v1.3 would assert that supervision charter v1.3 existed on 2026-08-07. **The largest single class is not stale at all, and repairing it would have introduced the error.** 67 of the 89 cite `Verification Charter v1.5` section 9, and the charter's own live text still carries the same marker: `levers_verified_active` *(v1.5, L-40)* at `VERIFICATION_CHARTER.md:981` and *"Mesh birth certificate (v1.5)"* at `:1007`. Those citations name the version at which the clause ENTERED, they agree with the charter, and renumbering them to 1.9 would contradict it and date section 9's clauses to a style amendment that did not write them. `docs/MEMORY_ARCHITECTURE.md:664` had already said so in one parenthesis -- *"the v1.5 at body line 177 is not a version claim at all: it cites Verification Charter v1.5"* -- and that reading is now measured rather than asserted. **The `git ls-files` trap that produced a fourth false "untracked" report of `scripts/check_verdict_cells.py` in one day is recorded at `docs/USING_THIS_LAB.md` section 8.7**, with the two commands that settle it, the file's `HEAD` mode and blob, and the 160-file measurement from `902b72fc`. | fleet |
| D38 | **The four standing rules written on the evening of 2026-08-11 contradict the charters they sit beside, in six places, and none is enforced by any code.** Frame: `git ls-files` + `/usr/bin/grep` for `R-ISOLATE|R-CONVERGE|R-DEPTH|R-VALUE` -- **15 files match, all markdown, two test fixtures and one docstring mention; no script, hook, test or dispatch template enforces any part.** Against `SUPERVISION_CHARTER.md:136`, written the same evening -- *"A rule without a sweep is a preference"* -- all four are currently preferences, and `LADDER_V_TRIPLE_VERIFICATION.md:213`'s *"R-ISOLATE, four parts, all mechanical"* is an absolute with no executed test beside it (L-76). The six conflicts: **(1)** R-VALUE's `PASS WITH RESIDUALS` is a sixth gate verdict against `VERIFICATION_CHARTER.md:95` *"The verdict vocabulary is fixed"*, and §16's negative-verdict-review sweep enumerates the fixed vocabulary, so residuals are invisible to it by construction. **(2)** R-VALUE's closing condition -- two rounds returning only findings that would not change an external reader's belief -- is satisfied by R-ISOLATE's own documented fail-open shape, *"an honest, confident, empty result"*; two rules in one document, neither citing the other. **(3)** R-CONVERGE requires closure declared *before* the first grade; the E2 ruling in the same file sets E2's closing condition after six rounds, which `VERIFICATION_CHARTER.md:156-164` (§2b) forbids explicitly. **(4)** R-DEPTH caps meta-work at depth 2 with no exemption, while `SUPERVISION_CHARTER.md` §3 checks 1 and 3 mandate depth-3 supervisor work unconditionally, and A15 mandates it too -- V16 rounds 5-7 ran it. **(5)** Two live documents are both *"the docket"*, definite article: `LADDER_V:189` says `docs/DOCKET.md`, `ESCALATION_CHARTER.md:85` says `demo-output/website/agenda/docket.json`, they overlap on substance, and neither names the collision, against `MEMORY_ARCHITECTURE.md:613` *"A fact has one home"*. **(6)** None of the four carries the archive replay and fire rate `VERIFICATION_CHARTER.md:622` and §16 rule 2 require before a rule is adopted. | V15 round 6, 2026-08-11, subject `2266c4e3` | Katie's on (1) and (3) -- they are charter amendments, not fleet edits. (2), (4) and (5) are drafting fixes the chief can make. (6) is a measurement nobody has run. **The rung is not green while a rule set written in one evening conflicts with the charters in six places.** | Katie |
| D39 | **37 archived case directories carry a finished result and no log of any kind, so nothing about them can be verified — including whether they ran.** `act9-crm_wingbody-*` under `/home/ubuntu/certonomous-runs/` (37 directories, frame: `find /home/ubuntu/certonomous-runs -maxdepth 1 -type d -name 'act9-crm_wingbody-*'`) each hold a complete `1000/` field set with `1000/uniform/time` reading `value 1000; index 1000;`, and a `runScript.py` **byte-identical** to A6's (`diff` → empty). A sweep of all 37 for `*.log`, `*.txt` and `*stdout*` returns **zero files**. So there is no way to check convergence, CD/CL, exit status, or whether any of them crashed the way A6's own `run_model_run2` did. They can neither corroborate nor contradict the A6 CRM findings, and were excluded from them for that reason. This is the same family as the four zero-byte completion records already filed (E2) — **absence reads as nothing happened**, except here the fields prove something did. | A1/A5/A6 diagnosis, 2026-08-11, commit anchor `1393b8b4` | Either locate the driver that produced them and confirm whether it wrote logs elsewhere, or mark the 37 directories as unevidenced in whatever index cites them, so a later sweep does not read 37 finished-looking cases as 37 results. Not a compute request. | fleet |
| D40 | **A published root-cause document answers a standing question with a check that a dead lever would answer the same way, and reports it as passed.** `demo-output/website/dafoam/ROOTCAUSE_getRotationMatrix3d.md` §4.7 tests pre-registered prediction P6 — *"adjoint matrix reordering (`rcm` vs `natural`) must not move the result at all"* — reports 207.04% vs 207.05% at idx8, and concludes *"Reordering is not a factor, exactly as a purely geometric defect requires."* The only activity evidence in those runs is `W5-rotation-branch/D5a_pl_real_rotON_RCM.log:825` `Mat ReOrdering: rcm`, which `docs/DEAD_LEVER_AUDIT.md` §4 U-1 identifies as the **requested-value** print, not a readback. Checked: `/usr/bin/grep -c "matrix ordering"` returns **0** on `D5a_pl_real_rotON_RCM.log`, `D5b_pl_real_rotOFF_RCM.log` and `D1a_pl_real_rot_ON.log`, and neither D5 log holds a `KSP Object` block. **Positive control:** the identical grep on `W4-adjoint-pc-unblock/control_rcm.log` returns `12:    matrix ordering: rcm`, so the method finds a readback when one exists. P6's null is therefore exactly what an inert `rcm` would produce and the check does not discriminate. **This does not threaten the root cause** — it is established upstream of any linear solver by the `\|\|dXs\|\|` fingerprint being bit-identical rotations-on/off at the baseline and differing once pre-deformed — but P6 is one of seven pre-registered predictions in a document headed for upstream filing, and it should be relabelled as unresolved rather than cited as confirmed. | A1/A5/A6 diagnosis, 2026-08-11, commit anchor `1393b8b4` | `DEAD_LEVER_AUDIT.md`'s M-A (two arms with `-ksp_view`, ~8 core-min) settles it at the class level and 257 archived `rcm` runs inherit the answer, this one among them. Until then: strike P6's verdict in place, per the family's quote-and-strike rule, and do not carry it into any upstream draft. **CLOSED 2026-08-15 AS SETTLED BY REFUTATION -- ITS CHARGE IS FALSIFIED AND ITS REMEDY WAS DELIBERATELY REFUSED**, with reasons, in a dated re-test at `ROOTCAUSE_getRotationMatrix3d.md:471-500` (`DEAD_LEVER_AUDIT_ROUND5_2026-08-14.md`, repo `94307129`). BOTH halves were reproduced independently here rather than read off the refutation, and they point opposite ways, which is the whole finding: D40's EVIDENCE is right -- `grep -c "matrix ordering"` returns 0 in `D5a`, `D5b` and `D1a` -- and D40's CONCLUSION is wrong, because the GMRES totals are **86** for the natural-ordering pair (`D1a`/`D1b`) against **79** for the rcm pair (`D5a`/`D5b`), and an inert lever cannot move an iteration count. A silent log is not a dead lever. This row leaves the queue by being ruled out, not by being executed. | fleet |
| D41 | **A live, actionable proposal still gates on a control that was withdrawn three and a half hours ago precisely because it could not fail.** `demo-output/website/agenda/proposals/s1-regularization-chosen-by-prior-theory-with-a-posterior-on-beta.json:21` carries the gate definition verbatim: G-P4 requires the posterior to reproduce the plateau balance *"re-based onto the exact analytic \|g_penalty\| = 4.785304e-04 computed from the archived beta field rather than onto the published ratio-and-cosine pair"*. That re-basing was **withdrawn at `57fd0d83` (20:30:59)**, whose own title is *"Withdraw the plateau-balance correction and restore G-P4 to ratio-and-cosine: the control it replaced could not fail"*, and `S1_PRIORS_PREREGISTRATION.md:379` now reads *"G-P4 is restored to the ratio-and-cosine form in §4."* The proposal was last touched at `b63b568e` (17:08:07) and nothing swept it. **An agent that picks this proposal up executes against a control that cannot fail** — the exact defect the withdrawal exists to remove — and a proposal JSON is a dispatch surface, not a narrative record. Doubly rotted: the same line cites `docket D8`, which now resolves to B2's seven CANDIDATES. | V15 round 6 rot hunt, 2026-08-11, subject `cb7122ba` | Sweep the proposal corpus for every gate definition whose source preregistration has been amended since the proposal was last touched -- `t_preregistration > t_proposal` is the two-timestamp detector `MEMORY_ARCHITECTURE.md` §8.4 already specifies and nobody has run over `agenda/proposals/`. Highest priority of the seven rot findings because it is the only one on a surface an agent acts from. | fleet |
| D42 | **D17 asks Katie for a ruling that the charter made 41 minutes after D17 was written, and the stale ask is repeated on four surfaces.** D17 (`docs/DOCKET.md:123`, written `0049463f` 18:17:54) says *"The chief has already made the rank-1 companion clauses mandatory on every surface; no equivalent travelling rule exists for the referent"*, owner **Katie / chief**, settled by *"a ruling, not a run"*. `docs/charters/VERIFICATION_CHARTER.md:772` -- **§6a, "The referent travels with the verdict (added 2026-08-11)"**, landed at `2d0e5260` (18:58:54) -- is exactly that generalised rule, and its own text cites this audit's figures as its justification. Three further copies are live: `docs/EXTERNAL_REFERENT_AUDIT.md:368`, `:744` (*"A parallel travelling rule for the referent does not exist"*) and `:811`. `docs/DOCKET.md:86` compounds it -- *"D17 is the one that would close the other five and is Katie's/chief's ruling"* -- so five docket items are parked behind a question already answered. The docket header says an item leaves *"by being executed or by Katie ruling it out -- never by ageing"*; this one was executed and stayed. | V15 round 6 rot hunt, 2026-08-11, subject `cb7122ba` | Close D17 against `2d0e5260`, unblock the five items behind it, and strike the three sentences in `EXTERNAL_REFERENT_AUDIT.md` in place. **This is chief work, not Katie's** -- the ask should never reach her. | chief |
| D43 | **Two docket pointers in a grade record no longer dangle -- they resolve, silently, to unrelated live defects.** `demo-output/website/campaign/V16_GRADE_ROUND5.md:142-143` files two findings as *"**docket D5**"* (a worktree cut behind the subject commit) and *"**docket D6**"* (the guard publishes four recall figures and no precision figure), repeated at `:149`. Those rows are now **D19** and **D20** -- `docs/DOCKET.md:46-47` says so in its own ID map. D5 and D6 today are the `adjoint_optimization.py` certificate-`unlink` swallow and the five acts taking the opposite failure, **both still live and unfixed** (verified: `unlink` at `adjoint_optimization.py:1427`; the five acts still carry `if cert_path.exists(): cert_path.unlink()` outside the try). `df53488e` (18:20:45) landed the round-5 file and renumbered the rows **in the same commit**, without rewriting the file's own prose; rounds 6 and 7 cite D20 correctly, round 5 was never swept. Same class, same evening: `S1_WITH_PRIORS_PREREGISTRATION.md:16` and `:571` still say **D8** where `ad02a331` -- the commit titled *"The refutation's own pointer had rotted twice while it sat there being right"* -- corrected only the occurrence at `:547`. | V15 round 6 rot hunt, 2026-08-11, subject `cb7122ba` | A resolving-but-wrong pointer is worse than a dangling one and no checker looks for it. `docs/DOCKET.md:32` already names the hazard -- *"An ID is a reference target; moving it breaks every citation you cannot see"* -- so the sweep is owed: every `\bD[0-9]+\b` in the corpus, resolved against the ID map, with the renumber commits `df53488e`/`ad02a331` as the positive-control set. **This is the strongest argument yet for W-4** and should be cited in it. | fleet |
| D44 | **D20 is E2's declared closing condition, it was met two hours forty-five minutes after it was filed, and the queue still shows it open.** D20 (`docs/DOCKET.md:194`, filed `df53488e` 18:20:45) states in the present tense that *"The placement guard publishes four recall figures and no precision figure … Nothing measures the opposite direction"*. `scripts/self_audit.py` now defines `_PLACE_PRECISION` and generates it into the verdict paragraph; `git log -S'_PLACE_PRECISION'` returns a single hit, **`64281b6b` (21:05:17)**. `LADDER_V_TRIPLE_VERIFICATION.md:325` says *"This is docket D20, and it is now E2's closing condition."* The row carries no closure marker while its siblings do (D1a *"Nothing further — executed"*, D26 *"Executed for this instance"*), so a reader working the queue rebuilds a figure that already exists. **Related and unswept:** `LADDER_V_TRIPLE_VERIFICATION.md:364-365` still tells a reader *"20 of 41 is a worst case on hard sentences"*; grade round 7 falsified that at `db18553b` with an independent 19-of-25 (76%) against the published 49%, filed as D27 -- but round 7's "WHAT WAS FILED" list names neither the ladder record nor this human copy, so **the ladder's own closing narrative still carries the refuted bound**. | V15 round 6 rot hunt, 2026-08-11, subject `cb7122ba` | Mark D20 executed against `64281b6b`; strike the worst-case sentence in the ladder record against `db18553b`. Structurally: a docket item promoted to a rung's closing condition needs a back-pointer from the commit that closes it, or the queue and the rung disagree about whether the rung can close. | fleet |
| D45 | **A filed 340 core-min item's enabling premise is measured false, and the gate it is filed to cross has a bar above its own ceiling.** R1 (`RESEARCH_DIRECTIONS_2026-08.md`, `6773c36f`) was filed as R5's prerequisite because R5's §2a says *"whitening ... pushes correction away from high-sensitivity cells; **if those cells happen to sit outside the window**, G2 rises for a reason with nothing to do with turbulence."* R1 executed at zero compute (`S1_SENSITIVITY_VS_ERROR.md`): the top decile of `\|g(β=1)\|` from `S1-cbfs-reinversion/cbfs_inv/grad_eval001.npy` is **35.4% in-window against an 8.44% base rate — a ×4.19 enrichment, the largest of any region**, and the bottom decile of `\|g\|` is **90.0% above the shear layer, 1.8% in-window**. R5's "if" is false in the direction that reverses its prediction: whitening moves G2 down from 26.86%, not up toward 50%. **Independently of any whitening argument, the >50% bar cannot be met by any loss-following field**: the top decile of the *per-cell baseline loss* (`0/U` vs `0/UData`, the best case a physical correction could produce) lies **32.71%** in the window, so 26.86% is 82% of the achievable ceiling. This sharpens `CLOSURE_STAGE1_AND_C2_STATUS.md` §1.2, which put the ceiling at the window's 41.2% total loss share; the operative ceiling is the loss's own top-decile geography, **32.71%**. Same arithmetic closes R8's condition (a): the correction's geography is reproduced by a field containing no inversion result, so there is no physical geography to build a transferable prior on. **Reported with its own null stated:** the headline correlation (Spearman +0.9742) does **not** clear its null (+1.0000, an arithmetic identity), the TOO-CLOSE abort fires, and none of the numbers in this row depend on it. | R1 execution, 2026-08-11, `S1_SENSITIVITY_VS_ERROR.md` | **Do not buy R5 at 340 core-min in its current form**, and do not promote R8 into the ambition slot; both are premised on an answer now measured the other way. R5's G-W1 reproduction control is already discharged — both archived G2 values (29.0476%, 26.8571%) reproduce at zero compute. The one thing worth buying is PR-1, a second-QoI sensitivity map at β=1, **25 core-min** on the measured `anchor8` basis (599 s wall × 2 cores = 19.97 core-min): it is the only measurement in reach that separates "the adjoint is loud there" from "the closure is wrong there". | Katie / chief |
| D46 | **The placement guard reads WHETHER an ordinal is claimed and never WHICH board it is claimed on, and it reads no polarity and no mood.** Two false-FAULT shapes, both found by a held-out set built *outside* the lab (grade round 7, `campaign/V16_GRADE_ROUND7_PRECISION_SET.py`), both rule-A faults, both **FAIL severity on a travelling surface**. (a) *Other named board:* a genuine placement on a different and explicitly named ranking — another benchmark, a citation ranking, an internal timing table — is indistinguishable from a placement on this one, and naming the other ranking in the same sentence does not clear it. A submission document is a likely place to meet one. **2 of the 2 admitted sentences of this shape fault.** (b) *Negation, interrogative, counterfactual:* rule A tests neither polarity nor mood, so a sentence saying the *opposite* of a placement reads as the placement. **2 of 2 admitted fault.** Both are now counted in `_PLACE_FALSE_FAULT` with their per-sample counts and named as BLIND TO items 10 and 11, so they are **knowingly accepted** under the V16 ruling rather than unnamed — which is the whole difference between a residual and a defect. | V16 round 8, 2026-08-11, subject `db18553b`, landed `5e4a0cc5` | Neither closes with a word list; both need to read structure a regex does not carry (which ranking a sentence is about; whether an ordinal is asserted). **Nothing here is worth a fifth discriminator** — the V16 ruling says so explicitly, and four discriminators over six rounds each broke on a sentence their author had not tried. This is filed so the cost stays visible, not so it gets paid. It becomes worth paying if a travelling submission document is ever found to contain either shape — which is checkable today with the live sweep and has not been checked. | fleet |
| D47 | **The proviso test's independence is now exactly one author-written map deep, and that map is the remaining circularity.** Round 8 fixed D28: `test_every_false_fault_class_is_counted_and_named` no longer computes both sides over the author's own set, and it reddens when the outside set faults in a shape nobody enumerated (mutation-proved). But the assignment of each of that set's false FAULTs to an enumerated class — `_PLACE_FF_OUTSIDE` in `scripts/self_audit.py` — **is still written by the party being measured.** So the test can see a shape the author never *thought of*; it cannot see a shape the author **mis-filed into an existing class**, because nothing external checks the assignment. The assignments made in round 8 were checked by execution rather than read off the grader's own labels (two `LINALG` faults have different mechanisms — one is silenced by restoring the deleted relativizer, one is not — and they were filed to different classes accordingly), so the instance is sound; the *class* of defect is not closed. **This is L-74 one level further up again, and the pattern is worth more than this instance: each fix moves the circularity up a level rather than removing it, because the last step of any self-measurement is written by the self.** | V16 round 8, 2026-08-11, `5e4a0cc5` | A grader re-deriving the label→class assignment independently, from the sentences alone, and the two derivations compared — cheap, since the set and its labels are committed and recompute. Or a third precision set from a third builder, which would test the enumeration's coverage again without testing the assignment. **The general form for the chief:** where a self-measurement's last step is an author's judgment, name that step in the record so a grader knows where to aim, rather than reporting the measurement as fully external. **CONFIRMED AND SHARPENED BY EXECUTION — V16 grade round 9, 2026-08-11, subject `e16d7329`** (`__pycache__` purged before every cell; control and mutants in one invocation): a **single** mis-file reddens the test, because the per-class totals move — so the row's own wording is stronger than the defect. What is invisible is a **count-preserving swap**: exchanging the class of one `other-named-board` label with one `quotation` label leaves both class totals unchanged and the suite **green**. That is narrower than "cannot see a mis-file" and exactly as bad, because a totals-preserving swap is the mis-file an author would actually make. **The remedy this row proposes was also executed and the instance is discharged:** all nineteen assignments were re-derived from the sentences alone, using restoration of the deleted relativizer as the discriminator between `reduced-relative` and `linalg-head-unlisted` (a head on `_PLACE_LINALG_NEAR` goes silent when the relativizer is restored; one off the list faults either way), and **all nineteen agree with `_PLACE_FF_OUTSIDE`.** The *class* of defect stays open; this instance does not. | fleet |
| D48 | **The placement guard's board is a pinned snapshot, the world has moved under it, and the guard's own eleven-item BLIND TO list does not say so.** `scripts/self_audit.py` reads its board from the frozen benchmark clone's README at `deb9155` (dated 2026-05-04) and validates every placement sentence against the four entrants that table lists. `demo-output/website/campaign/BOARD_MOVED_2026-08-11.md` (`9cb2a20a`, fetched and verified twice by two routes) records the live board as **six** entrants with a new leader. **Executed at `be36e22f`:** six probe sentences that are TRUE of the live board all FAULT, and the two that are true only of the pin stay silent — so the guard is presently able to fault a correct sentence at FAIL severity on a travelling surface, and to clear a false one. **Latent, not live:** the corpus sweep is WARN on the same two lab-record quotations of D3, no tracked surface currently states a live-board placement, and submission is PARKED. **The pin itself is correct and must not be moved** — V1 needs a frozen scoring reference and `BOARD_MOVED` §4 says so explicitly. What is missing is disclosure: the verdict names its board by commit, annotated *"at the commit this lab's claims are dated to"*, which reads as alignment rather than as age; and none of BLIND TO items 1–11 names the referent's currency, though item 9 names the symmetric hazard one level down, for the SENTENCE (*"whether a placement is dated history rather than a live claim"*). **[ADDENDUM 2026-08-12, P1.2 — THE LATENCY PREMISE HAS EXPIRED, AND WITH IT THIS ROW'S REASON FOR BEING FILED RATHER THAN FIXED.]** *"No tracked surface currently states a live-board placement"* was true when this row was filed and is false as of `5c9c63fb`, which moved the lab's surfaces onto the live six-entry board (Yang 1 at 0.0580, Reissmann now 2). `CLOSURE_CHALLENGE_SUBMISSION_DRAFT.md` then read *"Reissmann is now rank 2"* — **correct of the live board** — and the guard faulted it against the pin: the both-ways error BLIND TO item 12 describes, observed rather than predicted. **So this row went live without anything in it changing.** **What was done at the commit carrying this addendum, and what was NOT:** the pin was not moved and the guard was not changed by one character. The sentence was cleared through the guard's OWN existing adjudication clause (`_PLACE_ADJUDICATED`, 400 characters) by ALSO stating Reissmann's placement **on the pin**, so the row now names both boards — item 12's disclosure carried down from the verdict to the sentence. Executed: with the addition `board_placement_faults` returns `([], [])`; without it, the rule-A fault. **This is a prose repair, not a guard repair, and it RESTORES the latency rather than removing it** — the next correct live-board placement anyone writes will fault again and will need the same hand-written pin-side clause. That treadmill is the cost of not fixing it, and it is stated here so the next reader does not have to rediscover it. The structural question is unchanged and is now costed in **D55**. | V16 grade round 9, 2026-08-11, subject `e16d7329` (ancestry OK; four-path content diff against HEAD `be36e22f` empty) | **Ruled OUTSIDE V16 and filed rather than fixed.** E2's closing condition governs how the guard READS a sentence, measured over held-out sets that are themselves defined against whatever board `_published_board()` returns; board currency is orthogonal to that frame, and no written claim the guard makes is falsified by it — it says "the board", defines it by commit in the same verdict, and never asserts currency. Cheapest settlement: a twelfth BLIND TO item naming the pin's age and the direction of the error, **generated from the pin rather than typed**, so it cannot go stale the way the figures did (L-79). The separate and larger decision — whether a rank check should fetch the live board rather than the scoring pin — is `BOARD_MOVED` §4's and is not this row's. **DISCLOSURE SETTLED 2026-08-11 by V16 round 10, subject `067caac0`, carried alongside D49 because it is one line.** BLIND TO item **(12) WHETHER ITS OWN BOARD IS STILL CURRENT** now stands in the frame, naming itself as item 9's hazard one level up — for the REFERENT rather than the sentence — and stating that if the board has moved the error runs **both** ways: a sentence true of the live board FAULTS, one true only of the pin stays SILENT. **Generated from the pin, not typed:** the entrant count comes from the parsed board, the commit from `_published_board`, and the date from a new `_board_pin_date()` that reads the clone's own HEAD (an unreadable clone yields "date unreadable" rather than a guess). `test_the_verdict_admits_it_cannot_tell_its_board_is_stale` asserts the item carries what those functions actually return, so moving the clone moves the item or reddens the suite — the one thing an item about staleness must not do is go stale. The item says explicitly that the pin is deliberate and **not** a defect to repair there, so it cannot be misread as licence to move the scoring pin. A prose parallel was added to `BASIS` carrying no pin values. **The row stays open**: `BOARD_MOVED` §4's larger question is untouched, and the nine surfaces carrying the stale `P(rank 1) = 68%` are not this row's either. | fleet |
| D49 | **The two precision rows are built by two different admission rules, and the 27-point spread the verdict publishes is mostly that difference rather than the builder's.** `_place_precision_sentence` asserts, in the generated verdict and in BASIS, that *"a set's builder is a variable in the result, **the two here differ in that variable and in nothing else**"* and that *"**the same admission rule applies to both**, so neither denominator can be padded with sentences the guard never looks at."* Two different predicates implement that one rule. The author's set admits on a **raw pattern match** (`_place_pattern(upto).search(_place_flatten(s))`, asserted at import for all 41). The grader's set admits on **`len(_placements(...)) > 0`**, which applies `_RANK_HOMONYM`, `_PLACE_PROB`, `_PLACE_OFN` and the linear-algebra subject-head discriminator *after* the match — strictly narrower. **Executed at `be36e22f`:** the author's 41 drop to **28** under the grader's predicate, losing 13 sentences the guard examines and correctly clears (8 `linalg-head-listed`, 1 `coordination`, 4 `homonym`); the grader's 26 raw matches drop to **25** under it. All 13 are true negatives, so the author's numerator stays 20. **Score both samples under ONE rule and the headline moves: raw-match everywhere gives 49% vs 73%, a 24-point spread (Fisher two-sided p = 0.075); `_placements` everywhere gives 71% vs 76%, a 5-point spread (p = 0.763).** The published pairing — the *looser* rule on the author's set, the *stricter* one on the grader's — is the only one of the four combinations that is not internally consistent, and it is the one that maximises the gap and yields the p = 0.040 the ruling at `02e941c4` cited. **What this does NOT touch:** both rows recompute correctly from their own sentences; all four recall figures are unchanged; the false-FAULT enumeration and its per-class counts are unaffected; and the struck *"worst case on hard sentences"* stays struck, because 76% exceeds the author's rate under **every** common rule. What is falsified is the comparison narrative and the absolute carrying it. | V16 grade round 9, 2026-08-11, subject `e16d7329` (ancestry OK; four-path content diff against HEAD `be36e22f` empty); executed at `be36e22f`, suite 111 passed | **Material — this is the rung's fifth L-76 absolute and it sits in the generated verdict, the surface an outside reader reads.** Settlement is narrow and needs no fifth discriminator: pick ONE admission predicate, apply it to both samples, recompute both rows from it, and **replace "differ in that variable and in nothing else" with the measured sensitivity** — the spread is 5 to 24 points depending on the rule, and that range is itself the honest statement. If the raw-match rule is kept, say why a sentence the discriminator correctly clears belongs in a precision denominator; if `_placements` is kept, say why a correct silencing should not be counted as a precision success. Either is defensible; asserting the choice does not exist is not. **EXECUTED AND CLOSED 2026-08-11 by V16 round 10, subject `067caac0` (ancestry OK; four-path content diff against HEAD empty).** `_placements` was chosen and applied to both rows, and the reason is structural rather than a preference: `board_placement_faults`, whose output **is** the numerator, is built on `_placements`, so under the raw rule the numerator and denominator came from two different functions and a sentence the discriminator correctly cleared sat in a denominator it could never enter. **It is the less flattering choice and the row it worsens is the author's own — 49% → 71%, numerator unmoved at 20** — and the code says so. Published now: author **20 of 28** of 41 held; grader **19 of 25** of 43 held; spread **5 points**, and both set sizes reach the reader for the first time. The absolute is struck and kept: the verdict states the admission rule moves the spread across a **5-to-24 point range** and that the builder effect is **confounded** with it, all recomputed from a new `_PLACE_ADMISSION` table rather than typed (L-79), with both falsified clauses added to `_FALSIFIED`. The strike on "worst case" survives and no longer rests on prose: `test_the_precision_spread_is_measured_under_both_rules` asserts the grader's rate exceeds the author's under **every** rule in the table, and pins that the numerator does not move with the rule (all 13 dropped sentences being true negatives). Recall figures, class enumeration and per-class counts unchanged; no fifth discriminator; no parsing touched. Mutation-proved with `__pycache__` purged before every cell and control and mutants in one invocation. **The transferable shape, and the reason three rounds missed it: every row recomputed correctly from its own sentences, and a per-row check cannot see a defect that lives in a PAIRING.** The new test asserts the property that was absent — that both rows were admitted by one rule — rather than the property each row already satisfied. | fleet |
| D50 | **The false-positive rate the docket publishes for the L-76 checker and the rate the checker recomputes are measured on two different samples, and neither reproduces the other.** `scripts/check_absolutes.py` states in its own docstring that *"the rate is recomputed from that file by `measure_against_sample()`"*, where *that file* is `SAMPLE_PATH` = `sdk/tests/fixtures/absolute_claims_labelled.json` -- **100 records, `drawn_at_commit` `8ebe2b8e`**. B3a's published **74% (37 of 50 flagged)** was measured on the *other* fixture, `absolute_claims_labelled_second_instance.json` -- **75 records, `drawn_at_commit` `ecbdc288`, `checker_commit` `8a8b5392`**, a different labeller with a different `label_question`. **Executed at `f6b4eec0`, both files through the same `measure_against_sample()`:** SAMPLE_PATH gives **fp 42 of 70 flagged = 60.0%**, fn 5 of 33 = 15.2%; the second-instance file gives **fp 41 of 54 flagged = 75.9%**, fn 2 of 15 = 13.3%. So the second-instance file does not reproduce 37-of-50 either -- the flagged count has moved from 50 to 54 because the checker has changed since `8a8b5392`. Three numbers, one docket row, and the module's docstring points at the sample that is NOT the published one. | found while settling B3c/D32, 2026-08-11, executed at `f6b4eec0` | **B3a's territory, not this rung's -- filed rather than fixed.** Settlement is a choice, not a merge: name which sample is the lab's rate and say so in B3a with its record count and `drawn_at_commit`, or publish both with their two `label_question`s side by side, since a rate is a rate over a labelling rule as much as over a checker. Either way the module's docstring and B3a must name the SAME file. A citation asserting the two matched was struck from the module rather than written, because it would have asserted something false. | fleet |
| D51 | **The L-76 checker's `<command>` citation -- the exact token that laundered thirteen fictional test names -- can be conjured by ordinary English.** `COMMAND_RE` includes the alternative `make\s+\w+`, so the sentence *"Skips that make the verdict UNKNOWN rather than merely narrower"* (the `Skips.blinding` docstring, in the checker's own source) resolves to a runnable command and backs every absolute in its unit. **Executed at `f6b4eec0`** over `scripts/check_absolutes.py`: that unit reports `resolved=['<command>']` with no command in it. The unit happens to contain no absolute, so nothing is mis-verdicted **in this file today** -- the finding is that the token is reachable from prose, and D32's laundering mechanism needed exactly one such token. Related and unmeasured: `FILE_FUNC_RE` matched the metasyntactic placeholder `path.py:function` out of the module's own prose (fixed in this rung by writing it `<path>.py:<function>`), which is the same defect in the other direction -- a citation regex reading illustrative prose as a citation. | found while mapping the checker's own citations, 2026-08-11, executed at `f6b4eec0` | **B3a/B3b's territory (matching precision) -- filed rather than fixed, because retuning `COMMAND_RE` without replaying the labelled sample is how a checker gets tuned to a pretty number.** Candidate narrowing: require a command to sit inside backticks, a fenced block, or at the start of a line. **It must be measured against the hand-labelled sample in BOTH directions before it ships** -- the rate over `<command>`-backed units has never been measured at all, which is the same gap D32 records for `CITES_MISSING_CHECK`. | fleet |
| D52 | **The repair that fixed E2's fail-open `.done` records moved the corpus denominator, and five published figures still cite the old one.** E2 diagnosed four zero-byte `.done` records — a zero-byte record being indistinguishable from a completed one to anything testing existence rather than content — and instructed that the four empties be *"marked, not deleted"*. That instruction was followed correctly: the four are present as `f6b2_{coarse,medium,fine,veryfine}_20260805T171046Z.done.INTERRUPTED`. **Nobody re-checked what the rename did to every consumer that counts.** `scripts/dispatch_queue.py:114` enumerates the corpus as `REGISTRY.glob("*.done")`, and `*.done` does not match `*.done.INTERRUPTED`, so the live count is **142**. `docs/PRODUCT_LIST.md` cites a **146**-record corpus in five places, including a measured statistic — *"Of 146 completion records, 3 carry a rank declaration"* — and *"all 146 records intact and unmodified"*, which is now false in the letter: four were renamed. **Measured 2026-08-12 at `d1585e3b`:** 142 `*.done`, 4 `*.done.INTERRUPTED`, **0 zero-byte, 0 `.partial`** — so the atomic-collector repair itself held, and this row is about the accounting, not the fix. **The registry is gitignored, so no repo-scoped sweep can see any of this** (L-75); it was found only by listing the directory directly. | state reconciliation after the 2026-08-12 outage, executed at `d1585e3b` | **This is the D50 shape a second time: a published denominator and a recomputed one that are two different samples, with no check that they agree.** Settlement is a choice, not a merge — decide whether the corpus is 146 records of which 4 are interrupted, or 142 usable records, then make every surface and every glob say the same thing. Whichever is chosen, the *interrupted* four must stay countable, because they are evidence of a real interruption and a corpus that silently drops them cannot answer how often collection fails. The general form worth more than the instance: **a fix that renames or re-scopes an artifact changes every consumer that counted it, and consumers are the half nobody re-runs.** **CLEARED 2026-08-14 by [OPUS-B], pinned at `8d977d23`, surfaces annotated at `975f3a8b` and `1c455160`. Three of this row's own claims were falsified by execution and are struck-and-kept here rather than corrected in place (L-76).** **~~"five published figures"~~ — the count was wrong by a factor of four.** A two-pass sweep (`git grep` for tracked files, plus a gitignore-blind `find`+`grep -lIE` pass reaching `solve_registry/`, `wall/`, `dist/certonomous-demo/`, `mission-output/` and `chief-engineer-runs/`, plus per-member `unzip -p` over the bundle and `pdftotext` over all four PDFs) found **~20 instances across 10 tracked files**, not five in one: `docs/PRODUCT_LIST.md` (5), `demo-output/website/campaign/INSTRUMENT_INTEGRITY_2026-08-11.md` (4), `docs/INSTRUMENT_INTEGRITY_LEDGER.md` (3), `scripts/case_preflight.sh` (2), `docs/standards/INFRA_FAMILY_SUPERVISION_GUIDELINES.md` (1), `docs/LEDGER_HEADLINE_AUDIT.md` (1), `scripts/launch_solve.sh` (1), the live proposal `agenda/proposals/cross-family-lesson-propagation-sweep-within-one-week.json` (1), `sdk/tests/fixtures/absolute_claims_labelled.json` (1), and this docket's own E2 row (1). **~~"146 records of which 4 are interrupted, or 142 usable"~~ — neither is the number of completion records.** A **fifth** record, `f5a_re2000_20260729T021614Z.done.FAILED_ATTEMPT_cwd_bug`, was renamed out of the glob at 2026-07-29 02:19:53 (ctime, read off the file), before any published figure was written. Executed at `8d977d23`: **142 usable `*.done`, 5 renamed out (4 INTERRUPTED + 1 FAILED_ATTEMPT), 147 in all**, 0 zero-byte `*.done`, 0 `.partial`. So the historical 146 had ALREADY silently omitted a failed run, and **no surface in the repo has ever cited 147**. **~~"five published figures still cite the old one"~~, read as five errors — they were TRUE WHEN WRITTEN.** The four `*.done.INTERRUPTED` renames happened at **2026-08-11 17:31:31** (ctime); the two rank-retrospective entries are dated 2026-08-10 and the launcher-test entry 2026-08-11. At the moment each was written `*.done` really did return 146. The defect was therefore never a wrong digit but **the absence of any note recording the supersession**, which is why all five carry dated supersession notes and not one had its number overwritten — rewriting 146 to 142 would have destroyed a true record of what the registry held on 2026-08-10. **RE-DERIVED, NOT PATCHED:** the measured statistic was recomputed through the live code path — **3 records carry a rank declaration, and it is 3 over 142 AND 3 over 147**, so the "measured zero" finding survives either denominator; the companion 143 becomes 139 usable / 144 of all. **GENERATOR PINNED AT THE HEAD OF THE CHAIN, and the chain was shorter than expected: no generator held the literal at all.** 146 existed only as frozen prose; the only thing that ever produced the denominator was `scripts/dispatch_queue.py:114` itself. That glob now enumerates sibling classes positively via `*.done.*`, reports `records` / `excluded_from_records` / `excluded_by_class` / `all_completion_records` **separately and never summed**, and names a class it has never seen rather than dropping it — control at `8d977d23` over a synthetic registry containing `*.done.NEW_CLASS_NEVER_SEEN` returned 3 usable / 2 excluded / 5 total and an absent registry returned zeroes without raising (that control proves the enumeration logic ONLY, not anything about the live registry — L-84). The counts are deliberately not merged: a spend audit wants the 142, and *"how often does collection fail?"* is answered only by the 5 excluded, which is the exact reason E2 required them kept. **THE LIVE DEFECT THIS ROW DID NOT NAME, and the one that mattered most:** `scripts/launch_solve.sh` recorded that **proposal P6 names a 146-record corpus replay as a prerequisite, whose stated point was that four of the records it would read are empty**. Those four are the renamed four. A P6 replay written against `*.done` would read 142, **silently skip all four, and return clean having never touched the evidence it was commissioned to examine** — a prerequisite inverted into a blind spot by a correct repair, with no symptom. Annotated in the script at `1c455160` with the requirement that any replay glob `*.done*` and report classes separately. **LEFT OPEN, with reasons:** four surfaces were NOT edited — `docs/standards/INFRA_FAMILY_SUPERVISION_GUIDELINES.md`, `docs/INSTRUMENT_INTEGRITY_LEDGER.md` and `docs/LEDGER_HEADLINE_AUDIT.md` were held back under the standing-records constraint (H6) and need their owner; and `sdk/tests/fixtures/absolute_claims_labelled.json:1264` must NOT be edited at all — it is frozen labelled ground truth with a `drawn_at_commit`, and silently editing a labelled sample to match a corrected world is the D50 defect exactly. The live proposal `cross-family-lesson-propagation-sweep-within-one-week.json` rests its cost basis on the 146 denominator and is also open. **REACH THIS CLEARANCE CANNOT HAVE:** the registry is gitignored (L-75), so every figure above came from listing the directory directly and none of it is reproducible from a clean clone; `registry_attribution()` returns zeroes there rather than a wrong number. **PROGRESS NOTE 2026-08-15 (NOT CLOSED).** The structural half is executed and mutation-pinned at commit `8d977d23`: `dispatch_queue.registry_attribution()` returns `{records: 142, excluded_from_records: 5, all_completion_records: 147, excluded_by_class: {INTERRUPTED: 4, FAILED_ATTEMPT_cwd_bug: 1}}` -- separate counts, never summed, exactly the pinned shape. **THE ROW STAYS OPEN because the four surfaces it itself lists as LEFT OPEN still cite 146**, verified at HEAD: `docs/INSTRUMENT_INTEGRITY_LEDGER.md:289` and `:316`, `docs/LEDGER_HEADLINE_AUDIT.md:336`, `docs/standards/INFRA_FAMILY_SUPERVISION_GUIDELINES.md:1127`, and the live proposal JSON. A number that is right in the function and wrong on every surface that quotes it is the defect this row was filed about. | fleet |
| D53 | **A repair for the gate that powers this box off was written, committed to the tree, and never installed; the machine ran the broken file for thirteen days.** `/usr/local/bin/auto-stop.sh` (root cron, every 5 min) decided *"is anyone working?"* by matching command-line **strings**, one being the literal `Certonomous/sdk`. A verification suite launched from the repo root as `python -m pytest sdk/tests/...` spells that path *relatively*, so it never matched and read as an idle box. **Executed 2026-08-12:** the gate powered the machine off at 02:45 and again at 15:35, the second time mid-suite, killing the tmux server and two in-flight verification runs. The same predicate failed the other way too — it matched *any* command line containing the pattern, including a `grep Certonomous/sdk` typed to investigate this very bug, which silently reprieved the box. **A test that misses the real thing and fires on a mention of itself is not a test.** The class was already known: memory recorded the predecessor incident of 2026-07-30, a backup `auto-stop.sh.orig-20260730` was taken, and a repaired version was staged at `scripts/auto-stop.sh.proposed` dated Jul 30 15:01. **`diff` proves the live script on 2026-08-12 was byte-identical to the backup**, so nothing was ever applied. | found while answering Katie's *"make sure the instance stops dying"*, executed and repaired at `025d59a6` | **Repaired at the gate, not worked around.** The three predicates now used are ones a spelling cannot change: process NAMES via `pgrep -x`; any worker whose CWD is inside the repo **and** which is burning CPU (the CPU sample is load-bearing — `chief_engineer.server` is a permanent daemon with a repo CWD, and counting mere presence would pin the box forever and delete the cost control the script exists for); and a Claude session transcript written inside the window. Cost discipline is unchanged; only the definition of *idle* moved. `sdk/tests/test_autostop_gate.py` asserts the installed gate has not drifted from the tracked one and exercises **both** directions — still stops a genuinely idle box, does not stop one whose transcript is seconds old — all five mutation-proved individually with `__pycache__` purged before every cell. **The transferable finding is not the regex.** It is that a fix living only in the repo was never checked against the artifact actually running, and nothing in the lab was watching that gap. Every piece of machinery this lab depends on that lives OUTSIDE the tree — cron entries, installed scripts, systemd units — has the same unguarded gap today. **EXECUTED AND CLOSED 2026-08-15, commit `025d59a6`.** The repair is on the INSTALLED copy, which is the only copy that runs, and the evidence that discriminates a fixed box from an unfixed one is a diff against the pre-repair backup rather than a reading of the tracked source: `/usr/local/bin/auto-stop.sh` (root, 2026-08-12 15:50) DIFFERS from `/usr/local/bin/auto-stop.sh.orig-20260730`, and carries the repaired predicate `pgrep -x` at its clause (1) plus the D53 narrative header. The staged `scripts/auto-stop.sh.proposed` is gone. `sdk/tests/test_autostop_gate.py` and `test_autostop_session_glob.py`: 17 passed. **WHAT WAS NOT DONE, and it is the reason D65 is still open beside this row: the installed copy is still NOT the tracked copy.** It differs by 72 diff lines and still hardcodes the single-directory referent `SESSIONS=${SESSIONS:-/home/ubuntu/.claude-sanaa/projects/...}` where the tracked script has the glob. D53's predicate defect is fixed; D65's install is not, and closing this row must not be read as installing anything. **Also not done:** the row's wider observation -- that every other hand-installed surface (cron entries, systemd units) has the same unguarded gap -- is untouched, and `scripts/installed_registry.py` reports the nightly cron and the pre-push hook both ABSENT. | fleet |
| D54 | **Rule B's fault message says "without naming who holds it"; the rule actually tests only whether a name sits INSIDE the phrase, and has no adjudication clause at any distance.** `_place_unnamed` (`scripts/self_audit.py`) is a bare regex over whitespace-flattened text. Rule A has `_PLACE_ADJUDICATED` = 400 characters and a clause that clears a wrong ordinal adjudicated nearby; **rule B has neither, and its own docstring says so** (*"rule B has no adjudication clause"*) — a deliberate and defensible choice about WIDENING the match, which nonetheless leaves the fault MESSAGE claiming more than the rule tests. **Executed 2026-08-12:** `"Yang leads at 0.0580. Our margin over the leader is 0.0027."` faults; `"Yang: our margin over the leader is 0.0027."` — the holder's name one character away — faults; only moving the name inside the phrase (`"our margin over Yang"`) clears it. **Consequence for how this corpus was measured:** of the 13 live occurrences at `5c9c63fb`, 9 named Yang within ±300 characters and 4 did not (`benchmarks.json`, `wall/wall.json`, `latex/closure_challenge_report.tex` second occurrence, and `sdk/scripts/build_benchmarks.py`) — but **that split is a property of the corpus, not of the guard**, which faults all 13 identically. A ±300 window measured over the same corpus put the anonymous count at 3 and missed the generator, which is the one that writes two of the others. | P1.2, 2026-08-12, executed against `5c9c63fb` | **NOT fixed here, deliberately: retuning a detector so that its author's own prose passes is how a guard gets tuned to a pretty number.** All 13 were repaired in the prose instead. What is open is whether the MESSAGE should be narrowed to what the rule tests, or the RULE widened with a proximity-clearing clause. **The second is not free and must be costed before it is built:** rule B's reach, as measured at `5c9c63fb`, rests on a five-sentence smoke test — the check's own verdict calls that "not a reach measurement" — and both precision rows published at `5c9c63fb` are rule-A sets, so there is no sample in this lab against which a rule-B widening could be shown not to lose the anonymous cases it exists to catch. The message is the cheaper and more honest of the two. **EXECUTED AND CLOSED 2026-08-14, commit `d2d6bd6c`** -- recovered uncommitted from the weekly-usage-limit kill that ended the fleet earlier that day, and verified by execution before it was taken. **The message was narrowed; the rule was not widened**, which is the choice this row recommended. The rule half is now **D77**, not the D63 the work cited: that ID was reserved by CITATION while the work sat uncommitted, and the fleet allocated it to an unrelated auto-stop defect hours before the work was recovered -- a pointer into an append-only register is sound only once the row exists. **RE-EXECUTED RATHER THAN BELIEVED, and it moved a number in this row.** Against a worktree of `5c9c63fb` itself, rule B has **14** occurrences there, not 13. The numerator reproduces exactly -- **9** name Yang within +/-300 characters -- and all four non-Yang surfaces named above are confirmed, but `docs/INSTRUMENT_INTEGRITY_LEDGER.md`'s *"Our margin over the runner-up"* is an uncounted fifth: the only `runner-up` phrase where the other thirteen are `leader` phrases, which is the likely reason it was missed. **This row's 13 stands as the dated measurement it is** and is not rewritten; the corrected denominator lives in `sdk/tests/test_fault_message_matches_rule.py` and in D77. **WHAT WAS NOT DONE:** rule B gained no proximity clause at any radius; no reach or precision figure moved; no held-out set was touched; the 13 prose repairs this row records were not revisited. **Mutation-proved with `__pycache__` purged before every cell and control and mutants in one invocation -- and the proof found the guard weaker than its own docstring claimed.** A window keyed on any capitalised proper noun reddens `test_the_rule_still_has_no_window_at_any_distance` at all five distances. A window keyed on `_board_names(board)` -- the way rule A's own adjudication is keyed, and so the likelier way it would be written -- left **all 18 tests green** while silencing a real fault, because every probe in the file names **Yang**, who leads the LIVE board and is not on the four-entry scoring pin those probes pass. `test_a_window_keyed_on_the_BOARD_is_caught_too` was added and reddens at distances 1, 50 and 300. **THE TRANSFERABLE SHAPE: a guard test whose probe names a subject its own referent does not carry is blind to every mutation keyed on that referent, and it stays green for exactly the reason it is blind.** The old guard was not weak by oversight in the assertion -- it was weak in the FIXTURE, which no assertion inspects. | fleet |
| D55 | **A check that RANKS is reading a referent that SCORES, and the repair is a second referent, not a moved pin.** `BOARD_MOVED_2026-08-11.md` §4's question, costed here because D48 went live (see its 2026-08-12 addendum) and the standing workaround is now a treadmill. **Options, priced.** **(1) Move the pin — REJECTED**, and not on cost: rung V1 needs a frozen scoring reference so case scores recompute identically, and `BOARD_MOVED` §4 says so explicitly. **(2) Fetch the live board at check time — REJECTED:** a self-audit that needs the network is OFF whenever the network is, and its verdict stops being reproducible from the tree. **(3) A committed, dated live-board record as a separate RANKING referent, the scoring pin untouched — RECOMMENDED, and not cheap.** Price: a second parse path and a second OFF condition; the staleness problem moves up one level rather than away, so the record needs its own date in the verdict (`_board_pin_date`, added at `067caac0`, is the pattern to copy); the fault message must name WHICH of the two referents it faulted against; and every reach and precision figure is recomputed by the suite from held-out sets, so BASIS text and the verdict frame both move. **(4) Status quo, which is what P1.2 shipped:** every future live-board placement faults and needs a hand-written pin-side clause beside it. **GATE, and this is the load-bearing part:** option 3 must not be built until someone builds a rule-A held-out set of LIVE-BOARD sentences. Without one the change is unmeasurable — it would be graded against sets whose sentences are all defined against whatever `_published_board` returns, which is the very thing being changed, and the lab would be reading transcription fidelity as if it were reach (L-74). | P1.2, 2026-08-12, on D48's expired premise | Not this rung's, and outside P1.2's declared scope under R-CONVERGE — filed, not appended. Settles when option 3 is built behind its gate, or when the fleet rules the treadmill in option 4 acceptable and says so in writing. **EXECUTED AND CLOSED 2026-08-14, commit `d2d6bd6c`** -- recovered uncommitted from the usage-limit kill, verified by execution. **Option 3 was built**: `_ranking_board` reads a committed, dated record (`demo-output/website/campaign/BOARD_MOVED_2026-08-11.md`, tracked, committed `9cb2a20a`, dated 2026-08-11) through the same `_table_blocks` / `_read_board_table` / `_first_author_surname` machinery as the pin, so no second parser can drift from the first; it answers **only** the existence question, returns three-valued UNKNOWN on any failure, and the verdict names which referent answered. Fail-open was specifically checked (defect class B1): an unreadable record and a two-table record both return `(None, reason)` and the check degrades to the pin **loudly**, never to an empty board that would pass vacuously. **THE GATE, STATED PLAINLY RATHER THAN QUIETLY SATISFIED.** This row's gate is written against *option 3* as a whole, and option 3 is what was built, with no live-board held-out set in existence. The gate's stated REASON does not bite -- it is that reach and precision are graded against sets defined by whatever `_published_board` returns, and this work does not touch `_published_board`, the binding, the held-out sets, or any published reach or precision figure. Its WORDING does bite, and the fleet should rule rather than inherit the reading: **it is recorded here, not resolved by silence.** **RE-EXECUTED RATHER THAN BELIEVED.** Across the tracked corpus (1455 opened surfaces): swapping the binding to the live board takes rule-A faults **from 2 to 68 over DISJOINT sets** -- the work's own docstrings called this "66 additions", which is wrong twice. Both current faults CLEAR (they sit in `docs/DOCKET.md` and `docs/INSTRUMENT_INTEGRITY_LEDGER.md` and are faults only because those sentences are correct of the LIVE board) and 68 wholly new ones appear, neither original file among them. Nor are the 68 one kind: **50 are dated records** and **18 are the check's own measuring apparatus** (14 in `sdk/tests/test_rank_claim_surfaces.py`, 3 in `scripts/self_audit.py`, 1 in `campaign/V16_AUTHOR_HELDOUT_SET.py`) -- this row's gate reasoning showing up as a measurement rather than an argument. **AND THE REPAIR CHANGES NO VERDICT TODAY:** the sweep faults the same surfaces with and without a ranking referent (2 rule-A, 3 rule-B either way) because **not one fault in the corpus reaches the existence clause**. What moved is the frame text and what happens the next time a live-board placement is written; nobody should read this as a fault the guard was making today. **WHAT WAS NOT DONE:** the pin was not moved (rung V1 intact, `BOARD_MOVED` section 4 honoured); the name-to-rank binding was not re-pointed; nothing fetches the network; BASIS text did not move, correctly, because no reach or precision figure changed. **Mutation-proved, `__pycache__` purged before every cell, control and mutants in one invocation -- and one guard was missing.** Re-pointing the binding INSIDE `board_placement_faults` reddens `test_the_binding_still_reads_the_scoring_pin` (2 failures). Re-pointing it at the CALL SITE in `check_board_placement_words` -- where a maintainer taking the gated move would most naturally take it -- left **all 18 tests green**, because that test hands the pin to the function itself and so can only see re-points made inside it. `test_the_check_passes_the_scoring_pin_as_the_binding` was added, watches what the check actually passes, and reddens on that mutation. **THE TRANSFERABLE SHAPE: a test that supplies the referent it is guarding cannot guard the choice of referent.** It proves the callee behaves given a board; the defect being guarded lives in the CALLER. **[CHIEF RULING 2026-08-14, at repo `ccedccdd`, on the question this row explicitly declined to settle by silence.] THE GATE IS SATISFIED FOR THE EXISTENCE CLAUSE AND REMAINS BINDING, UNWEAKENED, FOR THE NAME-TO-RANK BINDING.** The gate's wording says *option 3 as a whole*; option 3 as priced bundles two separable changes, and the reason the gate gives distinguishes them. That reason is confounding: a held-out set built against whatever `_published_board` returns cannot measure a change to `_published_board`, so the lab would read transcription fidelity as reach (L-74). **It bites on the binding and it does not bite on the existence clause**, because the existence clause does not touch `_published_board`, the binding, the held-out sets, or any published reach or precision figure. That is not an argument I am accepting on its face — it was measured: the sweep faults the same surfaces with and without the ranking referent (2 rule-A, 3 rule-B either way), and **not one fault in the corpus reaches the existence clause**. A change that moves no verdict cannot have its measurement confounded by the sets, because there is nothing to measure yet. **So: the existence clause stands as built. The binding stays gated behind a rule-A held-out set of live-board sentences, exactly as written, and this ruling gives no licence to take it** — the recovery pass showed why that matters, since re-pointing the binding at the CALL SITE left all 18 tests green until a guard was added for it. **WHAT WOULD REVERSE THIS RULING**, stated so it is falsifiable rather than final: a single fault that reaches the existence clause and whose correctness depends on a live-board held-out set. If one is produced, the existence clause is back behind the gate and this note is struck. **AND THE RULING IS ITSELF SUBJECT TO GRADING.** Under §2 the chief's own record is never the presumed-correct side of a conflict, and this ruling lets an agent's work stand — the direction in which a supervisor's judgment is least trustworthy. A non-author should grade it by execution, not by reading this paragraph. **THE STALENESS DID NOT GO AWAY, IT MOVED UP ONE LEVEL,** which this row priced in advance: the existence clause now reads a committed record that can itself go stale. **D57 is therefore the follow-on to this ruling rather than an alternative to it** — a scheduled fetch, outside the audit and outside the suite, writing a dated retrieval record into the tree and alarming on any difference, leaving both the audit and the suite reading committed artifacts and reproducible offline. D57's own row asks that it be decided together with this one; this ruling decides the half that was ready and hands D57 the half that was not. | fleet |
| D56 | **The one tracked artifact built to be handed to somebody carries four falsified figures, and the guard's own declared blind spot is exactly why nothing saw it.** `dist/certonomous-demo.zip` is **tracked** (`git ls-files dist/` returns it, and it alone), is dated 2026-08-11, and is the bundle the lab ships. **Executed 2026-08-12 at `dcde4dc2`,** reading the archive members directly: **8 stale instances across 3 files** — `certonomous-demo/site/closure.html` carries `RANK 1 OF 5`, `P(rank 1) = 68%`, `2–100% at 95%` and `67.6%`; `site/benchmarks.html` carries `P(rank 1) = 68%` and `2–100% at 95%`; `snapshot/lab_stats.json` carries `P(rank 1) = 68%` and `2-100% at 95%`. Every one of them was falsified on 2026-08-11 by the six-entry recomputation at `5c9c63fb` — P(rank 1) is 50% (50.2%), the interval is 0–97% at 95%, and the board has six entrants, not five. **The mechanism is not neglect, it is a declared blind spot being occupied:** the placement guard's BLIND TO item 8 states in its own generated verdict that it does not open *"files over 4 MB, non-UTF-8 surfaces, untracked files and **archive members**"*. The corpus sweep that turned the whole tree green therefore could not, by construction, read the one file most likely to leave the building. The uncompressed copies under `dist/certonomous-demo/` are untracked and stale from well before 2026-08-12 — also unreadable to the sweep, by the same item, for the other reason it lists. | found while adjudicating a P1.2 side-finding, executed at `dcde4dc2` | **This is the highest-consequence stale-claim surface the lab currently has, because it is the only one built for handing to a third party**, and it is worse than a website page: a bundle is copied, renamed and opened offline, so it cannot be corrected after it travels. Settlement has two halves and the second matters more. (i) Rebuild the bundle from the corrected surfaces and re-verify by reading the archive members, not the build inputs. (ii) **Make archive members reachable by the sweep, or stop shipping a tracked archive.** A blind spot that is honestly declared is still a blind spot, and item 8 has been quietly correct about this one for the whole campaign — L-84's exact shape: *a positive control proves an instrument can fire; it does not prove its reach.* Until (ii), every future corpus-green verdict is silent about the same file. **Do not rebuild and re-commit the zip without deciding (ii) first**, or the next recomputation reintroduces the identical defect with no check able to see it. **~~MECHANISM CORRECTED 2026-08-12 at `1449557a` — the row's finding stands, its explanation was WRONG, and the author was the chief.~~** The struck sentences above attribute the bundle's staleness to a blind spot, citing the guard's BLIND TO item 8 (*"untracked files and archive members, none of which it opens"*). **Executed:** `scripts/self_audit.py` imports `zipfile` at line 89 and opens `dist/*.zip` at lines 834 and 918; item 8's text at line 2581 belongs to **`check_board_placement_words`, a different check**. The rank-claim sweep **does** read archive members — and those three bundle members are **the sole reason that check reports FAIL rather than WARN**. So the bundle was never invisible: **it was seen, reported at FAIL severity, and not acted on.** That is a worse finding than the one originally filed, and a different kind: not an instrument that could not reach, but a fault the instrument raised that nobody cleared. Settlement half (ii) as written is largely moot for this check — the reach already exists. What remains is (i) rebuilding the bundle from corrected surfaces, and the real question this row should have asked: **why a standing FAIL on a travelling artifact went uncleared long enough to be rediscovered from the other direction.** The original wording is struck and kept rather than rewritten, per L-76, because a docket row that quietly corrects its own reasoning destroys the record of how the lab was thinking when it filed it. **CLEARED 2026-08-14 by [OPUS-B] at `e9144bcb` and `bd3da3a1`. Half (i) done, half (ii) confirmed already satisfied by measurement rather than by argument, and the row's corrected mechanism verified independently.** **BEFORE:** 1,500,512 bytes, sha256 `cc77bb0a`, mtime 2026-08-11 02:06:18 — byte-identical to its HEAD blob, so no concurrent agent had touched it. **AFTER:** rebuilt twice, ending 1,476,024 bytes, sha256 `34b8feed`, mtime 2026-08-14 21:16:50. **THE FIGURES CAME FROM THE SOURCES, NOT THE PACKAGING**, established by tracing rather than assumed: `scripts/build_laptop_bundle.py` copies `site/{closure,benchmarks}.html` verbatim from `demo-output/website/` and writes `snapshot/lab_stats.json` from a live call to `chief_engineer.lab_stats.lifetime_counters()`, whose closure text comes from `sdk/scripts/build_benchmarks.py::_CLOSURE`. All three sources had been corrected on 2026-08-11 **after** this bundle was built, and nothing ever rebuilt it. No file inside the archive was hand-edited; the zip was regenerated by `python3 scripts/build_laptop_bundle.py --zip`. Verified by reading the archive members, not the build inputs: the three members carry `RANK 1 OF 7`, `P(rank 1) = 50%` and `0–97% at 95%`, the superseded `68%` / `2–100%` surviving only inside their L-76 tombstones. **Note the board moved again since this row was filed — seven entrants, not the six it cites.** **THE ROW'S CORRECTED MECHANISM HOLDS, re-verified at `e9144bcb`:** `check_rank_claim_surfaces` returns FAIL only when `shipped_faults` is non-empty, and the three bundle members were the whole of `shipped_faults`; every other fault it lists is a lab record and WARN by construction. Executed before and after: **FAIL → WARN**. **Half (ii) needed no build:** the check opens `dist/*.zip` and reads its members, and it found the real faults in the real archive — reach demonstrated on the live surface, not inferred from a planted positive control (L-84). **A SECOND, UNASKED FAIL WAS CLEARED AND THEN IMMEDIATELY RE-OPENED, and that is the more useful finding.** `check_bundle_drift` had been FAILing on four differing members; the first rebuild took it to one, `sdk/chief_engineer/exec_bits.py` — which was **not stale**. It was built correctly at 21:03:55 and the tree moved under it: commit `9806ab1d` (another agent, D53) edited that file at 21:10:56. **The bundle was accurate for seven minutes.** A second rebuild at `bd3da3a1` took `check_bundle_drift` FAIL → **PASS** (empty detail). The transferable point: **nothing rebuilds this artifact.** It is a snapshot of sources with no trigger, no dependency edge and no owner, so its correctness decays from the instant it is written, at whatever rate the lab commits — with ten agents live, minutes. The decay IS guarded (`check_bundle_drift` caught it unprompted within minutes), so what is missing is not detection but a rebuild trigger. **Until one exists, every clearance of this row is point-in-time with a named expiry: any commit touching a bundled source re-opens it.** The falsified-figures half is content rather than file identity and is not affected by that drift. **THE AGED-FAIL CLASS SWEEP — the answer to *"why a standing FAIL went uncleared"*.** Executed over all 34 registered checks at `bd3da3a1`: **9 FAIL, 9 WARN**. A concurrent sweep by another agent (`088c1c65`, `AUDIT_AGED_FAIL_SWEEP_2026-08-14.md`) published *"Docket rows covering FAILs: **0**"* — **that is false, and self-refutingly so**: its own table lists the bundle rank-claim FAIL as uncovered while **D56 is the docket row covering exactly those three files**. It reached only `docs/DOCKET.md`, never opening `demo-output/website/agenda/docket.json` (264 proposals) or `git log`, which is where the lab's real record of these faults lives — a reach failure in a sweep about reach failures. Against those sources, **two other FAILs match D56's shape exactly** — raised at FAIL severity, written down by a human, seen, left uncleared, with no acceptance note: **(1) `check_nonconclusive_band_readers`** (`sdk/scripts/run_uq_studies.py:518`), recorded in `docket.json` on 2026-08-02 as *"immediately failed… precisely the defect the check was written for"* with the FAIL-count delta logged (*"5 to 6"*) — arguably a purer instance than D56 itself, uncleared 12 days, the line merely drifted 632→518; and **(2) `check_ladder_rungs_share_one_recipe`** (cube, motorBike, naca0015_sail), recorded at FAIL in its commissioning item's own outcome on 2026-08-02, re-confirmed by an independent sweep 2026-08-10, and **explicitly excluded** from the *"reported, not escalated"* carve-out sitting three lines away in the same check — its only mitigation still sits at `proposed` and covers 2 of the 3. **Two more are D56-adjacent but fail the *"recorded"* limb** — instrument-raised for 12 days and never written down anywhere: `check_gate_table_vs_transcripts` (the Ahmed 25° transcript stopped emitting `Verdict:` lines) and `check_studies_carry_what_the_fit_records` (b52 lost its `asymptotic` field at `db89139c`, whose own commit message enumerates the moved fields and omits that one). **Three are NOT the class:** `check_wall_credentials_vs_results` was deferred in an explicit sentence in `289611fb`'s commit message on 2026-08-05 — deliberate and documented, but *"a commit message is not a docket row"*, so the deferral had no owner and the promised *"next wall rebuild"* never happened; `check_fd_grades_current_standard` was self-raised and self-superseded in the faulting file itself; and `check_declared_fleet_vs_work` plus `check_evidence_paths_exist` are largely **check-reach artifacts** (a `MESH_STAGE_WORKERS = 1` constant the check's literal-`0` exemption cannot read; a deliberately planted fake path serving as someone's positive control) — the inverse of D56, an instrument reaching where it should not. **The class finding, which is worth more than this instance: the lab has no owner for a FAIL.** Faults are recorded where they are found — commit messages, `docket.json` outcomes, campaign sweeps — and `docs/DOCKET.md`, the one place anybody looks, was reaching none of it, which is how a FAIL on a travelling artifact stood long enough to be rediscovered from the other direction. **NOT DONE:** none of the other eight FAILs was fixed here — they are outside this row and are reported, not cleared. **EXECUTED AND CLOSED 2026-08-15 on its own half, commits `e9144bcb` and `bd3da3a1`.** Read from the ARCHIVE MEMBERS and not from the build inputs, which is the only reading that discriminates here: `dist/certonomous-demo.zip` (sha256 `34b8feed...`, the value this row states as its AFTER) carries `RANK 1 OF 7` and `P(rank 1) = 50%` in `site/closure.html`, and every surviving `P(rank 1) = 68%` sits inside an `<s>...</s>` L-76 tombstone. `check_rank_claim_surfaces` is WARN, not FAIL. **THE ROW'S OWN EXPIRY HAS ALREADY FIRED AND THAT IS NOT A RE-OPENING OF THIS ROW.** The row says any commit touching a bundled source re-opens the bundle question, and `check_bundle_drift` is FAIL today on five members (`agenda.py`, `exec_bits.py`, `closure.html`, `benchmarks.html`, `wall/wall.html`). That is D80(c) and D112(i) -- a STALENESS question about the artifact -- not D56's falsified-figures question, which is discharged. Closing this row does not clear the bundle. | fleet |
| D57 | **The board-freshness check that `BOARD_MOVED_2026-08-11.md` §5 recommended and priced at one fetch was never built, and the nearest thing the lab owns compares two committed artifacts to each other rather than either to the live board.** `sdk/tests/test_rank_claim_surfaces.py::test_the_committed_record_matches_the_board_on_disk` was written as *"the control that will redden the DAY the board moves again"*, but it compared `sdk/scripts/probability_of_rank_record.json` against the board **on disk** — both committed, both moving only when a human moves them. **A sweep at `45922a65`** over `*.py`, `*.sh` and `*.md` for any live-board fetch or freshness predicate returned two files, and both were prose: `BOARD_MOVED_2026-08-11.md` and the docstring of `sdk/scripts/probability_of_rank.py`. **Nothing scheduled, and nothing in any test, had compared the lab's board to the live one since 2026-08-11T23:33Z.** B-1 closed that gap once by hand — a read-only GET of the benchmark README **retrieved 2026-08-14T21:01Z** found the live board **unchanged**: six entries, the same ranks and author lists, the same six overall scores and the same forty-eight per-case values across the same eight case columns — but **a one-off fetch by one agent on one day is evidence, not a check**, and recording it as though it were a control would be the exact defect L-39 names. | B-1 live-board re-score, 2026-08-14, swept and fetched at `45922a65` | **Filed, not fixed, because the cheap version is the wrong one and D55 already priced why.** D55 rejected *"fetch the live board at check time"* for `self_audit.py` on the ground that an audit needing the network is off whenever the network is, and its verdict stops being reproducible from the tree — that reasoning transfers unchanged to putting a fetch inside the test suite, so **this must not be settled by adding a network call to either.** What it plausibly wants is the opposite shape: a **scheduled** fetch, outside the audit and outside the suite, whose only job is to write a dated retrieval record into the tree and alarm on any difference from the last one — leaving the audit and the suite reading committed artifacts exactly as they do now, and reproducible offline. That is close to D55's recommended option 3 and should be **decided together with it rather than separately**, since both turn on the same choice of what the lab's ranking referent is. Whoever takes it should note that the two questions have different owners today and that is part of why neither moved. | fleet |
| D58 | **A board state is asserted at a date no retrieval in this lab supports, in the paragraph that exists to complain about undated leaderboard rows.** `demo-output/website/campaign/CLOSURE_STAGE1_AND_C2_STATUS.md:443` reads *"the live leaderboard **at 2026-08-05** has six rows, not four, with a new rank 1 (Yang, 0.0580) and a new rank 4 (Tian, Buchanan, Hickel, Dwight, 0.0641)"*, and closes *"the leaderboard rows are undated in the table itself"* — a W-5 complaint made in a sentence with an unsourced date and a present-tense verb. **Executed at `45922a65`:** a sweep of every `…T..:..Z` literal in the tree, over tracked files and gitignored ones alike (GNU `grep` at `/usr/bin/grep`, because the PATH `grep` is ugrep and honours `.gitignore`, L-75), returned **exactly two board-fetch timestamps in existence anywhere** — `2026-08-11T23:33Z` and `2026-08-14T21:01Z`. **There was never a 2026-08-05 retrieval**, and the earliest evidence for the six-row board is the 08-11 fetch, six days later. | B-1 live-board re-score, 2026-08-14, swept at `45922a65` | **NOT struck, and the reason is the substance of the row.** L-76 strikes *falsified* claims; this one is **unsourced, which is a different defect and a lesser one** — the board plausibly did have six rows on 2026-08-05, and striking a sentence that merely lost its provenance would assert it false, which is not known and not measured. What is defective is the date and the tense, not the row count. Settlement is one of: attach the retrieval that supports 2026-08-05 if one exists outside the tree; or re-anchor the sentence to `2026-08-11T23:33Z`, the earliest date the lab can actually evidence, with a dated note recording that the original date had no source. **The general form is worth more than the instance:** this lab's W-5 discipline is enforced on *figures* and has no equivalent enforcement on *dates*, and a date is the half of a state claim that decides whether the figure was ever true. | fleet |
| D59 | **The cross-surface sweep excluded two `docs/` files by name and left three others unclassified in either direction.** `docs/P33_CROSS_SURFACE_SWEEP.md` ruled `docs/PRODUCT_LIST.md` and `docs/DOCKET.md` out of scope as append-only chronological logs whose historical figures are quotations rather than claims — **a principled exclusion this row does not dispute.** `docs/HANDSHAKE.md`, `docs/H4_ALLOCATION_AUDIT.md` and `docs/CAPABILITY_STRATEGY.md` appear in that document **neither as swept nor as excluded**, and each states P(rank 1) with no interval and no `not statistically decided` on the same line. **Measured at `45922a65`:** `HANDSHAKE.md` L39, L42, L111, L272; `H4_ALLOCATION_AUDIT.md` L75, L349; `CAPABILITY_STRATEGY.md` L82, L85 — nine instances, NSD count 0 in all three files. **Most of the nine read as the same chronological-log class already excluded** (commit-subject recaps; descriptions of *other* surfaces' staleness — L272 is literally a report that the bundle copies still read `68%`). The one that does not is **`docs/HANDSHAKE.md:111`, *"(P(rank 1) now 50%, board now …"*, which states the figure as current with no interval.** | B-1 live-board re-score, 2026-08-14, measured at `45922a65` | **The finding filed is the CLASSIFICATION GAP, not a count of nine violations, and the distinction is the whole row.** Claiming nine breaches would assert more than was measured; re-drawing the boundary between a claim and a quotation so that a number comes out is the failure D54 already names in the other direction. What is owed is cheap: P33's scope section should say, for each `docs/` file that mentions the figure, whether it is swept or excluded and on what rule — the same treatment `PRODUCT_LIST.md` and `DOCKET.md` already got. **An exclusion stated by filename does not generalise to a class**, and until the rule is written down rather than applied, the next file of the same kind will also be neither. `HANDSHAKE.md:111` should be settled on its own merits whichever way the rule lands, since "now" is not a quotation. | fleet |
| D60 | **A lever found dead is a finding with a half-life, and this lab has been filing the finding without filing the sweep of what leaned on it.** Dead-lever audit **Round 5** (2026-08-14, `demo-output/website/campaign/DEAD_LEVER_AUDIT_ROUND5_2026-08-14.md`, repo `94307129`, **zero core-minutes**) re-opened every prior found-dead verdict from rounds R1 (`946e4a26`), R2, R3 and R4 (`81b40251`) and found the class defect is not in the verdicts — it is that nothing sweeps their downstream. **Four instances in one round.** **(1) D40's prescribed remedy was never executed for three days, and it pointed the wrong way.** D40 charged that `ROOTCAUSE_getRotationMatrix3d.md` §4.7 reports prediction P6 as passed on a check an inert `rcm` would answer identically, and prescribed *"strike P6's verdict in place … and do not carry it into any upstream draft"*. D40's evidence re-verifies exactly (**0** `-ksp_view` readbacks in `D5a`/`D5b`/`D1a`, **1** in the `control_rcm.log` positive control). **Its conclusion is nevertheless FALSIFIED by the same four logs**: the `natural` arms converge in **86** GMRES iterations to `1.084852511275e-04`, the `rcm` arms in **79** to `1.242231735482e-04`, from a **bit-identical** iteration-0 residual `1.243721539281e+01` — so the runs share a primal state and an RHS, and an inert lever cannot move the count by 8.1%. A full pre-solve diff of `D1a` against `D5a` returns **86 differing lines of which exactly one is substantive**, line 417, the ordering token; the rest is hostname, wall clock and MPI buffer-attach ordering. Nondeterminism is excluded because the two runs sharing each ordering agree to all 13 digits. **`jacMatReOrdering` is ACTIVE, proven by behaviour in a matched single-change arm (L-50's standard, a measurement not a second source read); P6 HELD and is stronger than claimed; and `docs/DEAD_LEVER_AUDIT.md` §4 U-1's broad form — *"no activity proof anywhere in this archive when set to `rcm`"* — is falsified, the proof having been inside its own frame L the whole time.** U-1's narrow form survives untouched: this proves the key reaches and changes the preconditioner, **not** that the permutation is specifically reverse-Cuthill-McKee, which still needs the readback absent from all 257 archived `rcm` runs. **D40 is thereby the second false found-dead in this corpus after FD-2/SIMPLEC, and it came within three days of writing a false correction onto a document headed for upstream filing** (`latex/dafoam_defect_report.tex:3249-3250` already records *"P5 and P6 both held"*). **This row corrects D40; D40's own row is left unedited, per the one-agent-one-row rule.** **(2) S8 was left behind when its twin was repaired.** R3's D1 found S6 and S8 both unreachable in production. S6 was wired (`head_engineer.py:1129` `arm_residual_gate`, called from staging at `:1127`, setting the target at `:1193`). S8 was not: `courant_limit` is accepted at `:221`, gates the rule at `:619`, and `git grep` returns it in exactly three places — the definition, the gate, and `sdk/tests/test_head_engineer.py:148,163`. **Mutation-proven** (`__pycache__` cleared first): a Courant number of **49.48 against a case limit of 1.5**, climbing monotonically over 60 fixed time steps, raises **0** anomalies in the shape production builds at `head_engineer.py:1030`, and **2** the moment `courant_limit=1.5` is supplied. `MONITOR_STANDARD.md`'s reach table at `:1028` is honest and current; the defect is one cell in a different table of the same document — the validation ladder at `:694` grades S8 bare **`validated`**, the **only** row of twelve carrying no date, while `:1168-1169` of that same file states *"S8 is unchanged: still no replay, still needs one"* and *"No replay exists at all"*. `sdk/scripts/replay_monitor_rules.py` covers **S1 to S6 only** (`:2`, `:123-128`), consistent with the latter. Whichever sentence is true, a reader scanning the ladder sees S8 graded validated beside S9/S10/S12 with no reach qualifier for a rule that has **never fired on a production run**. L-55's exact shape. **(3) N-1's downstream table was never annotated.** R2's near-miss re-measured unchanged: `AHMED_REFINEMENT_HIGH_RE = 3` is live wired code with a reachable condition — **not dead** — but of 52 `simplefoam-ahmed-3d-viscous` ledger rows, **0** carry `metrics.mesh_refinement`, **0** carry 79,439 cells, all cells are `{45760, 45813}`, and the last row (2026-07-29T11:16:13Z) predates the fix `a75e1fa1` by seven hours. Positive control: the token occurs **69,288** times in the same 208,194-row ledger. `F10_YPLUS_FIX.md`'s AFTER table publishes four measurements — including the one it names load-bearing, y+ 463.89 at refinement 3 — from indices with no surviving ledger row, case directory or log. **(4) Two found-dead levers were never named.** R3 §1.4 publishes **6** fleet-wide found-dead and evidences four (D1, D4, D2, FD-1); ranks 5–6 read *"D3 and D5"* with *"detail pending consolidation"*, and R3 §5 item 10 carries the same gap forward. `git grep` finds no record naming them three days later. **The headline 6 has been quoted since 2026-08-11 and one third of it is unfalsifiable** — nothing downstream can be checked because there is nothing to check against. | dead-lever audit Round 5, 2026-08-14, `demo-output/website/campaign/DEAD_LEVER_AUDIT_ROUND5_2026-08-14.md`, repo `94307129` | **The class fix is a rule, not four repairs: a found-dead verdict does not close until the sweep of what leaned on it is filed beside it.** Three of the four instances above were already actioned by Round 5 on the records' own faces (strike on `A1_A5_A6_DIAGNOSIS.md` §1.4, upheld-note on `ROOTCAUSE_getRotationMatrix3d.md` §4.7, caveat on `F10_YPLUS_FIX.md`). Four items remain and none is this round's to take: **(i)** `MONITOR_STANDARD.md:694`'s undated `validated` cell and its contradiction with `:1169` — a standing record, owner's call, and note that a Courant replay is priced at **~1 pass, 0 core-min** at `:1181`; **(ii)** wire S8 the way S6 was wired, from the case's own `controlDict` `maxCo`, or grade it unreachable on the ladder; **(iii)** R3's owner to name D3 and D5 or withdraw the count of 6; **(iv)** `docs/DEAD_LEVER_AUDIT.md` §4 U-1 owes a face correction narrowing its broad claim, and its priced **M-A (~8 core-min)** is now **half-superseded for free** — the archive already answers whether the lever acts, and only the RCM-specific readback still needs buying. **No compute is requested by this round.** | fleet |
| D61 | **A knob wired to nothing whose value happens to agree with the constant that actually acts - three instances, all latent, all one edit away from a silent divergence.** SDK-wide mechanical census by AST over **253 tracked Python files / 4,034 functions** in `sdk/` + `scripts/`, five categories (unconsumed parameters, config keys with no reader, env vars, CLI flags, always-true/false gates), dead-lever audit Round 5 SS10. **Two categories came back CLEAN and are recorded as absences, not unswept surfaces: 0 of 182 `add_argument` dests unreferenced after parsing, and 0 module-level literal-constant gates.** **(1) A block that has never executed, and the gate that lived inside it.** `sdk/chief_engineer/ask_the_lab.py:252-277` is `try:` ... `except Exception: pass`, and its first statement at `:253` is `from .compiler import load_physics_rules`. **Runtime-proven: `ModuleNotFoundError: No module named 'chief_engineer.compiler'`** - `compiler.py` is absent from disk and returns **0** from `git ls-files`, and `def load_physics_rules` exists nowhere in the repo. Dead twice over: `:256` reads `rules.get("regime")`, and `regime` occurs **0** times in `docs/physics_rules.yaml`, whose only top-level keys are `womersley:` (`:7`), `mesh_quality:` (`:31`) and `sobol:` (`:58`) - the block was written against a schema that is not there. Two `Source` entries (vortex-shedding onset by geometry; mesh-quality acceptance gate) therefore never enter the *ask the lab* retrieval corpus, silently. **The downstream lever:** `docs/physics_rules.yaml:50` `max_non_orthogonality_deg: 70.0  # hard gate; checkMesh acceptance threshold` has its **only** `.py` reader at `ask_the_lab.py:273`, inside the dead block - **zero live readers**. **Stated at its real strength: the gate is NOT unenforced.** It is enforced from a hardcoded `MAX_NON_ORTHO = 70.0` at `sdk/scripts/model_form_batch.py:88`, applied at `:636` and `:664`. The two agree at 70.0 today, so nothing is mis-gated; the defect is that a key labelled *hard gate* in a governed rules file moves nothing when edited. Four further `mesh_quality` keys (`:51,53,54,55,56`) have zero readers of any kind, partly self-declared by the file's own `:48-49` comment that they *await enforcement wiring*. **(2) The confidence level that changes the numbers and cannot be read back.** `docs/physics_rules.yaml:99` `confidence_level: 0.95` is live - `sobol_sensitivity.py:87` -> `:285` -> `sensitivity.py:231` `tail = (1.0 - ci_level)/2.0` - and really moves the bootstrap interval. `SobolIndices.ci_level` (`sensitivity.py:87`, set `:247`) is the only record of which level ran and has **zero readers**, while every label is hardcoded 95 percent: `sobol_sensitivity.py:95-96`, `:203`, `:625` (`"confidence": "95%"`), `:661`, `:695`. **Set 0.90 and every interval narrows while every surface still reads 95%.** **(3) Five verification acts encode a judgement into a parameter that cannot act.** `lab.trust(..., tight_threshold=0.02, ...)` at `sdk/chief_engineer/lab.py:205` is never referenced in its body; `cylinder_vortex_shedding.py:688` (0.007), `hypersonic_cylinder.py:354` (0.007), `diamond_airfoil.py:283` (0.02), `supersonic_wedge.py:348` (0.02) and `supersonic_cone.py:315` (0.03) each pass a distinct physically-chosen value. **No chip is wrong** - every branch returns `SOLVER_BACKED` and `:224-225` explains why the parameter went inert - but a reader of `:688` reasonably believes 0.007 does something. **Reach finding: `dist/certonomous-demo/sdk/` is a vendored duplicate carrying the same dead import at its own `:253`, invisible to `git grep -- sdk`, so (1) exists in two places** - D56's travelling-bundle surface reached from the opposite direction. | dead-lever audit Round 5 SS10, 2026-08-14, `demo-output/website/campaign/DEAD_LEVER_AUDIT_ROUND5_2026-08-14.md`, repo `94307129`; every finding re-verified firsthand before filing | **All three are latent, not live - no published number is currently wrong, and the fix is cheap precisely because nothing has broken yet.** Ranked: **(i)** delete the dead block at `ask_the_lab.py:252-277` or restore `compiler.py` - a bare `except Exception: pass` around an import is why this survived, so the `except` is the real defect; **(ii)** make `model_form_batch.MAX_NON_ORTHO` read `physics_rules.yaml:50`, or strike the `# hard gate` comment so the key stops claiming to gate; **(iii)** thread `ci_level` into the Sobol labels, or hardcode the config to 0.95 and delete the knob - one or the other, not both as now; **(iv)** drop `tight_threshold` from `lab.trust` and its five call sites. **A SUSPECTED list of ~10 further unconsumed parameters was returned and deliberately NOT promoted** (`certificate.py:876`, `exec_bits.py:416`, `external_aero.py:358`, `closure_generalization_criterion.py:258`, `mega_batch.py:402,742`, `tmr_verification.py:2588` and its `init_potential` which no call site ever passes `True`, `transonic_airfoil.py:457`, `models.py:66,68`), because only the four above were verified firsthand and L-43's corollary says a found-dead verdict is a claim about implementation. `morning_report.py --date` is carried separately: 6 builders take it, only `_build_spend` consumes it (`:375-378`), so past-date queries return today's sections 2-6 under that date's heading - a **partial**-effect flag, a different class, owed its own measurement. | fleet |
| D62 | **A prerequisite replay was commissioned BECAUSE four records were empty, and a later correct repair moved those four outside the glob the replay would use — so the replay would now return clean having never opened the evidence it exists to examine.** Filed out of D52's clearance at the coordinator's instruction; it is a distinct defect from that row's accounting and more serious. **The commission, from two independent surfaces read firsthand:** `scripts/launch_solve.sh` records that *"146 `.done` records are load-bearing evidence elsewhere in this repo, and proposal P6 names a 146-record corpus replay as a prerequisite; four of the records that replay would read are empty"* — the emptiness IS the reason for the replay. `docs/INSTRUMENT_INTEGRITY_LEDGER.md:316` states the same independently (*"proposal P6 names a 146-record `.done` corpus replay as the prerequisite"*), and `:487` makes remediation item **V3** — giving `case_preflight.sh` a third state and a count — explicitly conditional on it: *"**do not touch the pattern before that replay runs**"*. **The break, executed at `8d977d23`:** E2's repair renamed those four to `f6b2_{coarse,medium,fine,veryfine}_20260805T171046Z.done.INTERRUPTED` at **2026-08-11 17:31:31** (ctime, read off the files) — correctly, and on the instruction that they be *"marked, not deleted"*. `*.done` does not match `*.done.INTERRUPTED`. A replay written today against the `*.done` pattern (the pattern `scripts/dispatch_queue.py:114` used, and the one the commissioning text names) enumerates **142** records, **silently skips all four**, and reports success. **All four are 0 bytes; all four are still on disk; nothing errors.** A fifth record, `f5a_re2000_20260729T021614Z.done.FAILED_ATTEMPT_cwd_bug`, was renamed out the same way on 2026-07-29 02:19:53 and is excluded by the identical mechanism, so **147 completion records exist and the replay would see 142**. **Why this is fail-open and not staleness:** a stale figure is wrong and can be read as wrong. Here the instrument returns the verdict it was built to return when nothing is amiss, on a corpus from which the only anomalous members have been removed by the very repair that marked them anomalous. There is **no symptom** — no error, no count mismatch surfaced to the caller, no missing file. The stronger the marking, the more completely it hides. **Not yet executed, and deliberately so: the replay has not been run in either form, so this row asserts what the pattern would enumerate — measured directly off the registry — and NOT that a replay was run and came back falsely clean.** | filed 2026-08-14 by [OPUS-B] out of the D52 clearance (`8d977d23`, `1c455160`), at the coordinator's instruction that it not live only in a report; P6's prerequisite verified against `scripts/launch_solve.sh` and `docs/INSTRUMENT_INTEGRITY_LEDGER.md:316,487` independently | **Cheap to fix and worth fixing before P6 runs, not after.** Minimum: any replay must enumerate `*.done*` and report the classes SEPARATELY — never summed — so the four interrupted are counted as interrupted rather than absent; `scripts/dispatch_queue.py::registry_attribution` was pinned to that shape at `8d977d23` (`records` / `excluded_from_records` / `excluded_by_class` / `all_completion_records`) and is the reference implementation. A replay whose denominator is 142 should be refused as under-scoped. **Two things this row does NOT decide, both needing an owner.** (i) Whether P6's *"146-record"* specification should be re-stated as 147 or as 142+5 — a specification quoting a denominator that no longer exists cannot be satisfied as written, and the number it quotes (146) was never the count of completion records anyway. (ii) Whether V3 stays blocked: it is gated on a replay that, as specified, would not examine the records the gate is about. **The transferable class, which is the reason to file this separately from D52: a repair that MARKS an anomaly by renaming it removes it from every glob that selected the population the anomaly belongs to — and the more faithfully the marking is done, the more completely the evidence leaves the frame of anything commissioned to inspect it.** D52 is the accounting half of that mechanism; this is the operational half. **No compute requested; the replay itself was already scoped NO COMPUTE by the prior pass.** | fleet |
| D63 | **A second W-2 identity survived — in the document that diagnosed W-2 — and it survived because the paragraph defending it misnames the leg it kept.** `demo-output/website/dafoam/ladder-b/S1_WITH_PRIORS_PREREGISTRATION.md` §7 gate **(c) CONTROL**, lines 343–349, bars *"both quantities within **2% of 0.998441 and 0.016502**"*. The same document's §3 table, lines 172–174, says what those numbers are: **0.998441 is the ratio `&#124;g_penalty&#124;/&#124;g_QoI&#124;`; 0.016502 is `rms‖β−1‖`; the cosine is 0.999542 and is not barred.** So gate (c) gates on the ratio and on `rms‖β−1‖` — and `&#124;g_penalty&#124; = 2·λ_L2·‖β−1‖₂`, so `rms‖β−1‖` **is that identity divided by λ_L2 and √N, both constants fixed in this same file**. VERIFICATION_CHARTER §2a names that exact quantity as the identity: *"any treatment that knows λ_L2 and β reproduces it exactly — including one whose posterior is wrong."* **The tell is the defence, not the gate.** The addendum at line 570 — written 2026-08-11, the same day the chief restored the sibling gate G-P4 onto the ratio-and-cosine form at `57fd0d83` — argues correctly against the identity for three paragraphs and then closes: *"**Gate (c) of §7 stands as written**, on the ratio and the **cosine**, with the 2% bars against 0.998441 and 0.016502."* It names the two numbers and calls the second one the cosine when it is the rms. A reader checking the defence against the numbers agrees with it; a reader checking the numbers against §3's table does not. The sibling `S1_PRIORS_PREREGISTRATION.md` G-P4 carries all three legs and is correct, so **the defect is confined to this file's gate (c)**. Verified at `94d6510b` by reading §3, §7 and the addendum directly, not from a summary. **Separately and much milder, same file:** gate **(a) DERIVATION** self-declares *"Bar met in advance by construction"* — λ_L2 is derived from σ_pr = 0.35 by algebra, so once σ_pr is stated the gate's answer is fixed before anything runs. §2a permits reporting that derivation and forbids gating on it. | W-1/W-2 charter sweep, 2026-08-14, at `94d6510b`; the already-known instance (the S1 sensitivity correlation whose null is pinned at Spearman **+1.0000** and whose first accepted iterate satisfies the null to **1.110e-16**, `S1_SENSITIVITY_VS_ERROR.md:23–36`) is correctly handled — reported INCONCLUSIVE, not gated — and is **not** this row | **Three steps, in order, and none of them is mine.** (i) The chief rules which of the two parallel pre-registrations is canonical — the addendum says that is the chief's call and it is still open, and if `S1_PRIORS_PREREGISTRATION.md` is canonical this row is moot for the gate and survives only as a lesson about self-defending prose. (ii) If this file is canonical, §2b decides the instrument: an **amendment** restoring the cosine leg is legal only while no compute has run, **checked against the run directories and stated on the amendment's face** rather than asserted; if compute has run, the only legal move is a **dated addendum** recording gate (c) as defective and shipping the failed gate as a documented failure under §8. (iii) Either way the addendum's sentence at line 570 is wrong on its face and needs striking in place, retained. **Reported, not re-gated: `VERIFICATION_CHARTER` §2a says an identity may be reported and never gated on, and it does not say an auditor may quietly repair somebody's frozen pre-registration.** | chief |
| D64 | **Four of Katie's five W-rules have no executable enforcement, and — the larger finding — nothing in this lab is scheduled to run any check at all.** Audit at `0330d78c` using real GNU grep and `git grep` over both the tracked and the full-worktree frames, never the shell `grep` function. **W-1** was PROSE-ONLY until `94d6510b`; C4 above records its first executed run. **W-2** (identity test): no code reads a pre-registration — `scripts/self_audit.py`'s 34 registered checks contain **zero** occurrences of `prereg`, and the single mention across all 64 files in `sdk/tests/` is a docstring citation. `scripts/gate_table.py` scrapes act transcripts and its `main()` returns 0 unconditionally, so it cannot fail; `scripts/fail_open_scan.py` is a real, tested, exit-non-zero control but for a **different** class (a swallow that cannot move a verdict) and has no notion of derivability — the `&#124;g_penalty&#124;` identity that earned W-2 is invisible to it. VERIFICATION_CHARTER §12 "Enforcement" lists what is mechanised and **does not list §2a or §2b**, so the charter does not claim otherwise. **W-3** (amendment legality): nothing stats a run directory; `S1_PRIORS_PREREGISTRATION.md`'s *"there is no `S1-priors` run directory"* — the charter's own worked example of the rule — is asserted in prose and verified by nothing. **W-4** (docket ID stability): **no code reads `docs/DOCKET.md` at all**; its only appearance in executable code is one alternative in `check_absolutes.py`'s blast-radius regex, which reads the file's absolutes and never its IDs. D43 already said *"no checker looks for it"*. **W-5** (present tense / commit anchor): `check_absolutes.py` + `test_absolute_claims.py` enforce **L-76** (an absolute needs a named executed check) with no tense analysis and no commit-hash requirement, and B3a records them as not gate-usable at a 74% false-positive rate; `scripts/corpus_figures.py` is a printer whose `main()` always returns 0 and which no test covers. **The frame finding is worse than the split.** There is no CI (no `.github/`, no Makefile, no pytest config), no installed git hook, and no Claude hook. The only cron on this box is `auto-stop.sh` every five minutes and a `@reboot` server start. **So every check that does exist — `self_audit.py`'s 34 and all 64 `sdk/tests/` files — reddens only when a human or an agent happens to run it.** | The W-1 brief's enforcement audit, 2026-08-14, at `0330d78c` | Katie's call, on two separable questions. (a) Which of W-2..W-5 is worth an executable check — W-4 is the cheapest by a wide margin (a duplicate-and-renumber detector over `docs/DOCKET.md` against git history, with the two committed renumber commits `df53488e`/`ad02a331` as its positive-control set, which D43 already scoped) and W-2 is the most valuable and the hardest, since derivability is a semantic property no regex reaches. (b) Whether anything should be **scheduled**. That half changes no rule, needs no new check, and is one crontab line; it is also the half that decides whether any of the answers to (a) would ever run. **[NOTE 2026-08-14, at `6e54a444`.]** Half (b) is built and **deliberately not installed**. `scripts/lab_check.py` is one entry point that ENUMERATES the check set mechanically -- `git ls-files` plus `os.walk` over `scripts/` and `sdk/tests/`, tracked and worktree frames both, never the shell's `grep` (`ugrep --ignore-files` here) -- and admits a candidate only on predicates read out of its own AST: an entry point, an exit that is not the constant 0 (**this row's own criterion for `gate_table.py`**), no required positional, no solver in a subprocess head, no write to the tree. It returns PASS / FAIL / UNKNOWN, cannot PASS from an empty set, reads a usage error or a traceback as UNKNOWN rather than as a finding, never discards stderr, and prints its frame every run: **140 candidates, 11 script gates and 69 test files admitted, 60 skipped with the reason attached to each**. **First aggregate on the live tree: FAIL** -- and the finding is what it was made of, not that it fired. `self_audit.py`, which nothing schedules, is red RIGHT NOW on five of its 34 checks (`cited evidence paths` 6 of 1106 unresolved, `gate table vs transcripts`, `wall credentials vs results`, `FD grades vs current standard`, `non-conclusive band readers`) with four WARNs beside them. Those checks are correct, they were correct yesterday, and running them was nobody's job. **Cost, measured on this box rather than estimated: 14m37s wall / 983 s CPU for the 1,609-test suite; ~3 min wall / 176 s CPU for the eleven script gates (`self_audit.py` is 102 s of it); ~18 min for the lot.** Nightly is what that supports; hourly is about 7 CPU-hours a day for inputs that move a few times an hour. **Scheduling delivered as TRACKED FILES AND NOT INSTALLED, both registered in `scripts/installed_registry.py` and both reading ABSENT with a reason today:** `scripts/installed/certonomous-lab-check.cron` (`sudo install -m 644 -o root -g root scripts/installed/certonomous-lab-check.cron /etc/cron.d/certonomous-lab-check` -- root, so A4, so Katie or Sanaa) and `scripts/installed/pre-push` (`install -m 755 scripts/installed/pre-push .git/hooks/pre-push` -- **no root, which is why it exists**). **Until one of those two commands is run, nothing about this row has changed.** Adopting the hook today would block every push, because the fast tier is FAIL on the tree as it stands; that is a statement about the tree rather than about the hook, and the honest order is cron first, hook once those five are cleared or waived. | Katie |
| D65 | **The auto-stop gate's control-room clause named ONE session directory, the session config directory was migrated out from under it, and the clause went on firing off a copy nobody writes to.** `scripts/auto-stop.sh` clause (3) is what stops this box powering off under an occupied control room -- the failure of 2026-07-30 and of 15:35 on 2026-08-12, which killed the tmux server and two in-flight suites. The 2026-08-12 rewrite gave it `SESSIONS=${SESSIONS:-/home/ubuntu/.claude-sanaa/projects/-home-ubuntu-Certonomous}`. **Re-verified firsthand with GNU find (`/usr/bin/find`, findutils 4.9.0) under `env -i`, never the interactive `find`, which is `bfs` here and REJECTS `-newermt "-30 min"` as an invalid timestamp -- measuring with it produces a false negative on the very clause under test.** At 22:00:42Z on 2026-08-14 the live transcript `/home/ubuntu/.claude/projects/-home-ubuntu-Certonomous/64b13819-....jsonl` was advancing every few seconds (22,755,821 B at 21:59:15Z, 22,849,320 B at 21:59:54Z) while `.claude-sanaa/`'s copy of the same session id had not been written since **21:46:43Z**. The clause matched at `-30 min` and **not** at `-5 min`, and it matched **the quiescent copy** -- so it was holding the box by accident and was due to go silent at **22:16:43Z** with five agents at work. Demonstrated rather than argued: at `IDLE_MINUTES=5` (a proxy for that moment) the HEAD script printed `idle 90min, shutting down` against the real directories while the repaired one printed `ALIVE ... (/home/ubuntu/.claude/projects/...)`. **One correction to the record it was filed on, since a chief's own note is not the presumed-correct side of a conflict here: `.claude-sanaa/` is NOT a frozen artifact of the one-time `cp -rn`.** Every other file there carries the copy's 2026-08-12 15:22 mtime, but that one transcript is a genuine DIVERGENT FORK -- byte-identical to the live file for 8,818 lines and then different, 10,474 lines against the live 9,050, with its own real session records running to 21:46:43.475Z. A second, now quiescent control room, not a stale copy. It does not change the defect and it strengthens the fix: **both** directories are real control rooms and the gate must cover both. **The transferable class, which is the reason this is filed rather than just fixed: this is a REFERENT that went stale, not a test that was wrong.** The predicate was correct on 2026-08-12 and is correct now; what moved was the thing it names. A check whose subject is named by a path inherits every migration of that path -- and this one fails SILENTLY, because a directory that stopped receiving writes is indistinguishable from a directory nobody is working in. Same shape as **D48** (a guard reading a pinned board after the world moved) and **D55**. Re-typing the new path would rebuild the defect one migration later. | Re-verified 2026-08-14 22:00-22:04Z off a chief-supervisor measurement, with GNU find under `env -i`; repair, controls and mutation proofs at `b39df811` | **Repaired in the tree at `b39df811` and NOT INSTALLED -- that half is row A4's and is what remains open here.** The fix: `SESSIONS` is a GLOB (`/home/ubuntu/.claude*/projects/-home-ubuntu-Certonomous`), still overridable so the negative path stays testable; a pattern matching nothing is announced as **BLIND** rather than evaluating to false; `find`'s stderr is surfaced rather than `2>/dev/null`'d, which is what would have turned the `bfs` rejection above into a clean "nobody is working"; and the matched transcript is named in the journal line, which would have made this stale referent visible for free every five minutes. **Four controls, DRY_RUN, real script, fixture directories:** fresh in `.claude` + frozen in `.claude-sanaa` -> HOLD (naming the `.claude` file); frozen in `.claude` + fresh in `.claude-sanaa` -> HOLD; **frozen in BOTH -> `idle 90min, shutting down`**; pattern matching nothing -> WARNING + shutdown. `sdk/tests/test_autostop_session_glob.py` (8 tests) carries all four plus the anti-hardcode invariant, **mutation-proven three ways with `__pycache__` purged before each cell**: hardcoding one dir reddens 2 tests, silencing the empty-glob case reddens 1, and holding unconditionally reddens the negative control -- so the negative control is not vacuous. **The waiver, because tracked and installed now diverge on purpose:** `installed_registry.py` gained `PendingInstall` (reason, owner, expiry **2026-08-21**, and the sha256 of what is expected to still be installed). Inside the window the pair reads PENDING and prints on every run; **expired, or an installed copy that is not the pinned one, FAILS** -- both proven against the LIVE entry, not only planted controls, so this is a declared divergence and not a silenced test. **What settles this row: Katie or Sanaa running `sudo install -m 755 scripts/auto-stop.sh /usr/local/bin/auto-stop.sh`, then deleting the `pending=` block.** Until then the box is running the defective gate and **the only thing holding it is `.autostop-hold`, which lapses after HOLD_MAX_HOURS=24** (refreshed 21:58:10Z on 2026-08-14). | Katie / Sanaa (A4) to install; fleet owns the tracked side |
| D66 | **Two more W-2 identities survive in frozen pre-registrations, and one of them decides a scientific verdict.** Both re-derived here independently rather than taken on report. **(1) `B52_RUNG8_PREREGISTRATION.md:39`, Verdict 1 — the most consequential, because its output is a mechanism verdict and not an admission bar.** The rule is *"Decision rule on the valid family (135,779 to 441,057 cells, the five same-recipe rungs): fit the exponent b in n_f proportional to N^b"*, branching **b < 1/3 = mechanism REAL** against **b in [0.55, 0.80] = REFUTED**. Every input to that fit is printed **in the same document, seventeen lines above the branch**: lines 22-24 give the `body` face counts 931 / 1825 / **6098 / 7533 / 9395 / 11621 / 13688** at 40,656 / 107,489 / **135,779 / 193,880 / 255,358 / 330,950 / 441,057** cells. Recomputed here on the five valid-family rungs by log-log OLS: **b = 0.7075**, endpoint slope **0.6863** — **inside the pre-registered REFUTED band**, obtainable with a calculator at the moment the bands were written, and the document itself calls the verdict zero-compute. The author's disclosure is real and is the right instinct (*"That exposure is declared here rather than hidden"*, and *"What has NOT been computed at the time of writing: … any scaling fit of faces against cells"*) — but **not having run the fit is not the same as the answer being unavailable**, and §2b protects exactly one property, that the gate could not have been chosen to fit the answer. That property is not intact when the answer sits four lines up the page. **(2) `S6_WIRING_PREREGISTRATION.md:62` — the sentinel exclusion.** The prediction *"Sentinel exclusion: 0 gated runs whose target is at or below its own solver tolerance"* is forced by the definition of gating. `sdk/chief_engineer/head_engineer.py:1008` partitions a field into `excluded` exactly when `target <= tolerance`, and `:1192` draws the arming target as `max(reachable.values())` — from the complementary bucket only — so a gated run with an unreachable target is **unconstructible**, not rare. `sdk/scripts/score_s6_partition.py:36,83` then imports and calls **the same `_field_reachability`**, so the predicted and the measured sides are one function. A wrong treatment passes unchanged: the real 2026-08-11 defect, a tolerance parser mis-resolving regex-group keys, mis-resolves identically on both sides. The prereg is candid at line 28 (*"excluded **BY CONSTRUCTION**, not by a threshold"*) and then scores it **"met"** at line 151 in a table whose other rows are genuinely informative fire rates — which is how arithmetic gets read as evidence. **A family pattern worth naming separately, three instances:** `R4_PREREGISTRATION.md:136` G1, `W3_WING_VALID_FAMILY_PREREGISTRATION.md:178` G1 and `B52_RUNG7_PREREGISTRATION.md:93` G1 are all mesh h-ratio bars **already met at filing** (0.90%, 2.88%/1.57%, 0.94%) where the same bar was **the mesh-selection rule** — meshes at 2.74%, 2.71%, 5.0% and 8.0% were discarded until the survivors sat inside 3%. So the surviving family passes by construction of the selection. These are ADMISSION gates on the input, they are disclosed on their face, and R4/B52-R7 are nonetheless later reported **MET** / **HOLDS** — a PASS badge on a number fixed before any solver ran. Not the same defect as (1) and (2); worth a convention (report as *fixed*, not as *met*) rather than a repair. **One non-identity with a worse consequence, recorded so it is not lost:** `F6a_DIFFUSION_PREREGISTRATION.md:41`'s T1 control arm CAN fail, but the likeliest OpenFOAM template defect — a `kOmegaSSTCoeffs` block under a key the solver ignores — **reproduces the 1.2534 baseline exactly and PASSES**, leaving the a1 lever inert; an inert lever gives swing ~ 0, which line 63 scores as **"T1 falsifies H"**. Its blind spot points at a **false REFUTED**. F5c's `fvSolution` sha256 leg is the device that closes it and F6a mandates no equivalent. **Cleared, so nobody re-litigates it:** `sdk/workflows/sobol_sensitivity.py:394`'s self-named "identity gate" is a genuine CONTROL despite the name — the Saltelli main-effect estimator is a sample covariance that really does leave [0,1], and executing the shipped code at the approved opening budget failed the check on **14/30 seeds (valve)** and **16/30 (airliner)**, driving the sample-doubling ladder. It tests the estimator and never the physics, and its 0.05 slack is calibrated on the two models it grades — a weak sibling of §2a's calibrated-band failure, closable against the closed-form Ishigami case already in `sensitivity.py:255`. | The W-1/W-2 charter sweep's breadth pass over campaign and workflow gates, 2026-08-14, at `348dd7c4`; (1) and (2) re-derived directly from the meshes' printed counts and from the two source files, not accepted on report | **Katie's, not the fleet's, for (1).** Withdrawing a REFUTED verdict that a pre-registration's own arithmetic already fixed is a scoring call, and R9 keeps those with the owner. The mechanical part is smaller: under §2b, (1) and (2) can only take **dated addenda** if compute has run against them, and (2)'s scorecard row should be restated from **met** to **by construction, not a prediction** in place, struck, with the original retained. For (1) the honest move is to report b = 0.7075 as a **measurement** of the recipe's surface-refinement behaviour — which is a real and useful number — and to withdraw the REAL/REFUTED branch as a gate it was never able to be. **Reported, not re-gated: §2a permits reporting an identity and forbids gating on it, and it does not license an auditor to edit somebody's frozen pre-registration.** | Katie (the verdict) / chief (the two in-place restatements) |
| D67 | **The restored G-P4 is the same could-not-fail control the withdrawal removed, in three parts, and it gates the most expensive unrun item on the S1 board.** `S1_PRIORS_PREREGISTRATION.md` §4 was restored 2026-08-11 to *"the ratio AND the cosine … A posterior treatment that cannot recover all three to 1% fails its own control"*, on the stated ground that *"the **cosine** is the informative leg: it is the only number here that carries whether the prior pull actually opposed the likelihood gradient."* **Executed on the archived pair** (`beta_eval010.npy` + `grad_eval010.npy`, S1-cbfs-inversion, λ_QoI and λ_L2 from that run's own driver lines 24-25): the three legs are all functions of (β, g_QoI, λ_L2). **rms‖β−1‖ is a function of β alone, and β is an INPUT to the gate.** **The ratio** is `2·λ_L2·‖β−1‖₂ / ‖g_QoI‖` — the numerator is the very identity the withdrawal removed, and the only new content is the scalar `‖g_QoI‖`. **The cosine is fixed by the other two** through `ε² = 1 + r² − 2rc` with `ε = ‖g_total‖/‖g_QoI‖`: solved for c it returns **0.999542138470174** against the directly measured **0.999542138470174**, agreement **2.2e-16** — the identity D9 already verified closes to six digits, read for what it implies about the gate rather than about the arithmetic. **Exhibited rather than argued:** a treatment reporting the PRIOR as the posterior (covariance = prior covariance, credible intervals straddling β=1 in 100% of cells, rank 0 — precisely the shape falsifier **F1** exists to catch) reproduces ratio 0.998441, cos 0.999542, rms 0.016502 — **all three to 0.0e+00 relative error. G-P4: PASS.** No leg is a function of any covariance, credible interval or Hessian. **And the cosine is near 1 for a reason unrelated to the prior being right:** at any interior stationary point `g_QoI + g_pen = 0` forces r = c = 1, and at the measured ε the bound `c ≥ 1 − ε²/(2r) = 0.999540922` is forced by convergence alone against the measured 0.999542138 — the published 0.9995 is *"the run converged"*, restated as an angle. **What G-P4 does test, so a replacement can keep it:** exactly two scalars, `‖g_QoI‖` at the plateau state and the angle between `g_QoI` and (β−1) — a sound reproduction control on the gradient that would have caught the mismatched-evaluation error behind the 1.684× confusion, and not a control on a posterior. Sibling instances of the same W-2 class are at **D66**; this one differs in that its item is unrun and repriceable. (Frame: two `.npy` files under `/home/ubuntu/certonomous-runs/S1-cbfs-inversion/cbfs_inv/`, **outside the repo and invisible to any repo-scoped grep**; 21,000 cells, DV order both; host-side arithmetic, zero solver compute; repo `c966bf7e`.) | S1 zero-compute triage, 2026-08-14, `dafoam/S1_ZEROCOMPUTE_TRIAGE_2026-08-14.md` §2.2 | Replace G-P4 before S1-priors buys its 260 core-min, and say in the replacement which quantity of the POSTERIOR it grades — F1 already names one (credible-interval coverage in the window) and G-P3 names another (the leading prior-preconditioned Hessian spectrum). Keep the present three legs, relabelled as the **gradient reproduction control** they are, which is worth keeping under its own name. Note **D41** separately: the proposal JSON an agent would dispatch from still carries the *withdrawn* form, so the live dispatch surface is a third variant again. | fleet |
| D68 | **PR-1 asks 25 core-min for a second-QoI sensitivity map that was already on disk when the request was written, and it returns the outcome its own author pre-registered as closing the question.** `S1_SENSITIVITY_VS_ERROR.md` §7 files PR-1 as *"a second-QoI sensitivity map on CBFS at β = 1 … Priced at 25 core-min"*, with the rule *"If the second map's geography matches the first, reading 2 is confirmed outright and R5/R8 are closed."* `S1-cbfs-weighted-arm/cbfs_inv/grad_anchorw.npy` is `dJw/dβ` at **β = 1** against `Jw = 5319·varUwin + 3591·varUrec`, two disjoint `boxToCell` variances over **2,970** cells (`runScript_w.py:79-105`) against varianceU's 21,000 — a different functional, same case, same mesh, same repaired inlet. **Provenance three ways, because this is the load-bearing claim:** `runScript_w.py:144` sets `b0 = np.ones(NCELLS)` unless `-betafile` is given and `log.anchorw` carries none; `driver_w.py:24` comments `LQOI = 1.0 / 27.465931825190644  # 1 / Jw_raw(beta=1), anchorw` against `log.anchorw:18720` printing `OBJ Jw: 2.7465931825190644e+01`; `log.anchorw:18721` prints `GRAD norm=7.3228773523e+00` against the measured 7.3228773523e+00. `ledger.csv` bills it **16.83 core-min, 2026-08-08** — four days before PR-1 was filed. **Measured against the first map** (frame: 21,000 cells, DV order, top decile = 2,100 by magnitude, window 0≤x/h≤6, 0≤y/h≤2): **Spearman +0.9894**, top-decile set overlap **1,975 of 2,100 = 94.05%** against a 10% chance level, upstream x<0 shares **45.76% vs 45.57%** — agreeing to **0.19 percentage points**. For scale, R1's own *"second independent baseline gradient"* (same objective, different inlet) agrees far less well: Spearman +0.9450 reproduced, overlap **61.14%**. **Changing the objective's spatial support by 7× moves the geography less than repairing the inlet did.** **Identity hazard stated and not gated on:** Jw's in-window share is partly derivable from its own mask, so the gated leg is the share OFF the support — 57.67% vs 54.67% — and the upstream share, which no box touches. Spearman carries the claim; Pearson (+0.9992) is tail-dominated (max/median 2.05e+06) and is reported, not relied on. | S1 zero-compute triage, 2026-08-14, `dafoam/S1_ZEROCOMPUTE_TRIAGE_2026-08-14.md` §2.1 | **Do not buy PR-1 as filed** — the support-change version is answered, and a third velocity-variance map would leave belief unmoved whatever it returned. **Respecified it is worth 25 core-min:** the second QoI must be a structurally different QUANTITY (a force, an integrated wall shear over the step face, a reattachment location), not another velocity-matching functional on another region, and the pre-registration must argue why, since the support-change variant is now measured not to qualify. R1 §7's own decision rule applies to what is reported here and should be applied by R1's owner, not by this row. | Katie / chief |
| D69 | **G-P2 predicts an unpinning from a comparison between two priors, and the quantity that decides unpinning is a third — measured, at the only archived state where it can be.** `S1_PRIORS_PREREGISTRATION.md` §4 G-P2 reads *"the restoring pull at β = 0.2 is `2λ_LN·log(0.2)/0.2` = 1.309e-4 against the Gaussian's `2λ_L2·(0.2−1)` = 1.600e-5 — **8.2× stronger**. Prediction: the count of cells at the lower bound falls from 223 to fewer than 60."* Both pulls reproduce (**1.3090e-04**, **1.6000e-05**, ratio **8.18×**). But whether a bounded cell lifts off its bound is decided by prior pull against the **likelihood** pull at that cell, not by one prior against another. **Frame, stated rather than glossed:** the FINAL state has 223 low + 1 high and its gradient is deleted by `invert_lbfgsb.py:96-97`, so evaluation 10 is the only archived state where β and its gradient can both be read, and it is early — **5** cells sit at the lower bound there. At those cells `\|g_QoI\| = λ_QoI·\|gv₁₀\|` runs min 4.6498e-04 / median 6.8796e-04 / max 1.8206e-03: **100% (5 of 5) exceed the lognormal restoring pull**, 100% exceed the Gaussian pull actually in force, and the median likelihood pull is **5.3×** the stronger prior's. **This is a caution on the mechanism, not a measurement of the gate** — n = 5, and the state is not the one G-P2 grades. What it establishes is narrower and firm: the 8.2× ratio does not by itself imply unpinning, so the argument printed for the *"<60"* bar does not support it. | S1 zero-compute triage, 2026-08-14, `dafoam/S1_ZEROCOMPUTE_TRIAGE_2026-08-14.md` §2.5 | Re-derive G-P2's bar against the likelihood gradient rather than against the Gaussian prior, before S1-priors runs — §2b protects the freeze only while there is no answer to tune to, and there is none. Cheapest honest form: state the prediction as a comparison of the lognormal pull to the measured `\|g_QoI\|` distribution at the bound, and accept that the archived distribution is at evaluation 10 and not at the final state. | fleet |
| D70 | **"Nine gradient arrays survive in the entire archive" is frame-scoped, 14 survive, and the pricing premise built on it — that curvature must be bought fresh — is false: two secant pairs are reconstructible at zero compute.** `S1_SENSITIVITY_VS_ERROR.md` §1 and `RESEARCH_DIRECTIONS_2026-08.md` R1 state *"Nine gradient arrays survive in the whole archive … nine, confirmed"* and, on that basis, *"No L-BFGS curvature pair (`y_k = g_{k+1} − g_k`) is reconstructible from any completed run … Buying curvature as fresh gradients is **FORCED, not chosen**, which is what prices the whole Bayesian line."* The enumeration is exact for the frame it ran over (`S1-cbfs-{inversion,reinversion,weighted-arm}/cbfs_inv/`) and the sentence generalises it to the archive. **Frame: `find /home/ubuntu/certonomous-runs -name '*grad*.npy'`, executed 2026-08-14 — 14 arrays**, the five unenumerated being `S1-fiml/ramp_kw/s1_e{1_kw,2_sst,2_tight}_grad.npy` (n=5,000, a different case), `W4-adjoint-pc-unblock/cbfs_beta/cbfs_beta_grad.npy` (**bitwise identical**, md5 `06fe8c50`, to `S1-cbfs-inversion/cbfs_inv/grad_eval001.npy`) and `W4-defect-reach/cbfs_simple411/cbfs_beta_grad.npy`. **The consequential half is the premise, not the count.** The claim is exact for CONSECUTIVE-ITERATE pairs and does not hold for SECANT pairs, which is all a curvature probe needs: `invert_lbfgsb.py:96-97,120-121` keeps evaluation 1 **and every tenth evaluation**, both β and gradient, so S1-cbfs-inversion and S1-cbfs-reinversion each archived a matched pair at two states. With `s = β₁₀ − 1` and `y_QoI = λ_QoI·(gv₁₀ − gv₁)`, `sᵀy = sᵀH̄s` **exactly** by the mean-value form of the gradient map. Measured: reinversion `sᵀy_QoI` **+7.313133e-01**, `R_qoi` **+3.933e-02** against `R_prior = 2λ_L2 = 2.0e-05`, **prior-preconditioned Rayleigh quotient +1.966e+03** (data-dominated); inversion **+6.086e-01** (prior-dominated, the quantitative form of that run's plateau being a penalty/likelihood cancellation). **Identity separated from measurement as §2a requires:** `sᵀy_penalty = 2λ_L2‖s‖²` is derivable from (β, λ_L2) and is reported, never gated on — it is 0.05% of `sᵀy_total` on the repaired run and **62.17%** on the corrupted one. **Nulls and controls:** sign-randomised `y_QoI` over 200 draws puts the measurement **6.6 sd** (reinversion) and **10.0 sd** (inversion) from chance; cross-pairing s from one run with y from the other is wrong by two orders of magnitude in both directions; and **out-of-sample against `J_history_main.csv`, which shares no array with the gradients**, a quadratic built from the secant predicts the corrupted run's objective change to **0.06%** where the linear term alone is **99.98%** wrong (the repaired run, a 1.8× longer step, leaves a 30% cubic remainder — reported both ways). **What is NOT established:** this measures the curvature SCALE in one direction per run, not the DECAY of a spectrum, so falsifier **F2** is untouched and the Hv budget is not eliminated. | S1 zero-compute triage, 2026-08-14, `dafoam/S1_ZEROCOMPUTE_TRIAGE_2026-08-14.md` §2.3 and §S0 | Two things, both cheap. (a) Restate R1's count with its frame, per B5 — the number is not load-bearing but the premise built on it is. (b) **Adopt the free first-Hv correctness control before S1-priors buys 120 core-min of Hessian-vector products:** the first Hv along `s` must reproduce `sᵀy_QoI = +7.313133e-01` to FD accuracy. The prereg's planned symmetry check (`v₁ᵀHv₂` vs `v₂ᵀHv₁`) is self-consistency and passes for a consistently-wrong operator; this does not, and it costs nothing. | fleet |
| D71 | **A correction applied to one surface and not travelled to the copies is not a correction — and the proof is that the SAME sentence has now been repaired twice, the second time because the first repair was overtaken within hours.** Re-verified independently before acting, not taken on report: running `scripts/self_audit.py`'s own `board_placement_faults` over the tracked corpus (`git ls-files`, ≤4 MB, UTF-8-decodable, `__pycache__` cleared) gives, at `7009c745`, **rule-A 2 with 0 on travelling surfaces under the four-entry scoring pin, and rule-A 68 with 3 on travelling surfaces under the live six-entry board** — the pin run reproducing the standing audit exactly, so the instrument is sound and the referent was three days stale. Repaired here, strike-and-keep with the board and its retrieval date on the face of every corrected claim: **(1)** `closure_challenge_submission_round5/DESCRIPTION_DOCUMENT.md` — Wu & Zhang's ordinal, *the second repair of this one sentence*: it was repaired on 2026-08-11 to the board frozen at `deb91557` (2026-05-04) and the live board gained two entrants at **2026-08-11T23:33Z that same night**, re-falsifying it within hours. **(2)** the same file's §8, **the serious one, where the section's conclusion reverses rather than its numbers changing**: it read *"Our margin over Reissmann, Fang & Sandberg — rank 1 on the published board — is 0.0028863. The seed bound covers 84% of the margin."* Reissmann, Fang & Sandberg are **rank 2** on the live board and no longer the leader; Yang leads at 0.058013, our locally scored 0.056647 sits 0.001365 below it, and the truth-free seed bound of 0.002419 is **177% of that margin, not 84% of it**. Loaded adversely the bound puts our overall at 0.059047 — above Yang — so **the seed uncertainty spans the boundary between the top two positions on the board, and the lead ahead of Yang is not distinguishable from single-seed noise**. The section was rewritten to that conclusion rather than having the name and the number swapped, because swapping alone would have carried an argument across a reversal in its own direction. **(3)** `demo-output/website/closure.html`, the same defect on a live public page. **(4)** `demo-output/website/benchmarks.html`, not a rank fault but the V10 failure condition — one page giving two answers, §1 saying *"our margin over Yang more than halved, 0.0029 → 0.0014"* and a later paragraph still calling the 0.0024 seed bound *"of comparable size"* to it; verified not inside any struck or dated block before repair. **After the repairs, measured the same way: travelling rule-A faults under the live board 3 → 0, corpus rule-A 68 → 65, and the four-entry-pin count is UNCHANGED at 2 rule-A / 0 travelling — so no sentence written here is the both-ways error that the check's own BLIND TO item 12 predicts.** Two findings about the repair itself, both measured rather than reasoned: the guard binds an ordinal to a name only within **40 characters** and adjudicates a struck ordinal against its replacement only within **400**, so a strike-and-keep whose corrected ordinal sits too far from the surname reads as an unadjudicated fault under BOTH boards — the first draft of repairs (1) and (3) did exactly that and had to be restructured; and **prose written to explain a rank defect creates rank defects** — two of mine did, each caught only by re-running the sweep after writing rather than after editing: a repair note saying an entrant had moved down by one position matched rule A's `<ordinal> place` family, and a repair sentence describing the boundary between the top two positions with an `<ordinal> place` phrase bound that ordinal to the entrant named a few words later — who is the leader, not the runner-up. Both were reworded to carry no ordinal at all. **Neither is quoted verbatim in this row, deliberately: the guard cannot tell using a placement phrase from quoting one, so a docket row that quotes its own false positive files a fresh fault.** **The final counts are therefore identical to the pre-repair internal counts on both boards — live rule-A 68 → 65 is exactly the three travelling faults removed and nothing else, and this row itself contributes zero.** **[VERIFIED AND PARTLY REFUTED 2026-08-14 by a non-author who re-ran the measurement rather than reading it, at `63453645`. The endpoints reproduce exactly; the accounting between them does not.]** Same frame, same instrument, `board_placement_faults` imported read-only from an unmodified `scripts/self_audit.py`, `__pycache__` purged before every run — 20,669 tracked, 277 over 4 MB, 35 non-UTF-8, 1,474 decoded and swept. **CONFIRMED, each independently:** at `7009c745` the corpus gives rule-A 2 with 0 travelling under the four-entry pin and rule-A 68 with 3 travelling under the live six-entry board, digit for digit; travelling rule-A goes to **0**; the four-entry pin count is **unchanged at 2**, and the two faults are the SAME two, in the same two files, with byte-identical messages — which is the load-bearing check and it holds, so nothing written in the repair is the both-ways error BLIND TO item 12 predicts; rule B is 3 with 0 travelling throughout. **REFUTED:** the sentence above. The tree the repair was actually applied to stood at **rule-A 70**, not 68, because this row's own first draft landed in `docs/DOCKET.md` before the repair commit and carried two faults of its own. The prose repair took **70 → 67** (exactly the three travelling faults, so that half of the claim is structurally right), and the rewrite of this row took **67 → 65** by removing the two faults it had itself introduced. So `68 → 65` is not "the three travelling faults and nothing else", and this row did not "contribute zero" at any point — it contributed **+2 and then −2**, and the two cancel only when read across the whole span. The arithmetic survives; the attribution does not. **The 0.001365 / 177% correction the repairing agent made to the brief it was given is CORRECT and was re-derived from source, not from the prose:** `closure_challenge_seed_sensitivity.json` → `overall_equivalent_S_bound` = **0.002419121853891026**, and `campaign/BOARD_RESCORE_2026-08-14.md` §3.1 gives 0.058013 against our 0.056647 for a margin of **0.001365**, so the ratio is **1.772 → 177%**. The chief's 0.001353 / 179% is the board's published 4-dp 0.0580 as its basis; §8 uses the full-precision re-score throughout and uses it **consistently** — margin, ratio and adverse leg all on the same basis — and it is also the **more conservative** of the two, since the 4-dp basis gives a LARGER fraction. §3.4 of the same document independently records the adverse leg at **0.059047** against 0.058013, P(rank 1) 34.2%, so §8's reversal is what the evidence supports and is **not** an overcorrection: every figure it states — 50.2%, 0–97%, t = −0.189, dispersion fifteen times the margin, 4 of 8 cases, 65.4% favourable against 34.2% adverse — is present in the committed re-score, and no claim in it exceeds them. | Travelling-surface repair, 2026-08-14; measured against `7009c745` and applied at `cc906ec9`, with `board_placement_faults` imported from `scripts/self_audit.py` unmodified and re-run before and after. The scoring pin `deb91557` was not moved and `scripts/self_audit.py` was not touched | **NOT fixed here, and deliberately.** (a) `demo-output/website/latex/closure_challenge_report.tex:1137` still reads *"published rank-2 entry"* — `latex/*.tex` has a designated owner and was not touched; it needs the same strike-and-keep. (b) `demo-output/website/closure_challenge_round5_qcr.json` §`qcr_vs_rank2_duct_consistency` carries the same stale ordinal twice inside a **machine record** that other documents anchor to; correcting a machine record's narrative field is not a prose edit and needs its owner's call. (c) `demo-output/website/closure.html:316` reads *"the campaign's central arithmetic (match rank 2's duct scores ≈ 0.0563)"* — dated campaign history, and it does not fault because no entrant is named near it, but it is a stale ordinal on a live public page. (d) The remaining **65** internal rule-A faults under the live board are overwhelmingly dated history, held-out test sets and the guard's own source — the check has no discriminator for dated history (its own BLIND TO item 9) and re-pointing the name-to-rank binding wholesale would fault them, which is why the binding stays on the pin. **The generalisable action: a rank correction is not done when one surface is fixed — the corpus is swept for the same claim in the same pass, and the freshness check that would have caught the board move costs one fetch.** **[(b) AND (c) EXECUTED 2026-08-14 at `63453645` by a non-author; (a) CONFIRMED STILL OPEN.]** **(b) done.** `closure_challenge_round5_qcr.json` → `qcr_vs_rank2_duct_consistency` sits inside `/leaderboard_comparison_dated_2026_08_07/` and its ordinal is TRUE of the board that block is dated to — the four-entry leaderboard frozen at `deb91557` (2026-05-04) — so overwriting it would have made a dated record false of its own date, a worse defect than the one being repaired. It was therefore superseded by a dated sibling, `qcr_vs_rank2_duct_consistency_2026_08_11_six_entry_board`, in the record's own idiom (the same one `rank_companion_2026_08_11_six_entry_board` and `best_on_board_count_2026_08_11_six_entry_board` already use), with the superseded value left **byte-identical**, verified by sha256 of the parsed string against HEAD. **The key was NOT renamed, and that was checked rather than assumed:** `campaign/LADDER_V_V15_ROUND4.md` Q3 names it in the list of keys a structural diff proved unchanged, and `campaign/LADDER_V_V8_REVERIFICATION_2026-08-11.md` F1 cites it as the **source of truth** for the ordinal at `DESCRIPTION_DOCUMENT.md:54` — a rename falsifies the first and orphans the second. **Nothing consumes it programmatically:** no generator writes this file (`git grep` over `*.py` finds readers only), and the one test that opens it, `sdk/tests/test_rank_claim_surfaces.py`, walks it for the shipped interval and never for this key. **What would have inherited the error — and already did:** F1 corrected the travelling `DESCRIPTION_DOCUMENT.md` **to this record's ordinal** on 2026-08-11, and the live board falsified that correction the same night. The sibling is placed **above** the key it supersedes so nothing walking the block in order meets the dated ordinal without the live one first; measured rather than intended, that also puts the corrected ordinal inside the guard's 400-character adjudication window, which the house ordering could not have done across a 631-character value — so the live-board corpus count falls **65 → 64** and the pin list is unchanged line for line. **(c) done.** `demo-output/website/closure.html:316` struck and dated on its face with the four-entry pin it was written against and the six-entry board retrieved 2026-08-11T23:33Z, re-verified by read-only fetch 2026-08-14T21:01Z. **No entrant is named at that location, deliberately:** re-pointing a historical arithmetic at a live board would stop it being a record of what was done, and naming one there would manufacture a binding that then has to be adjudicated; the reader is pointed at the dated box lower on the same page instead. **(a) still open, for its owner:** `demo-output/website/latex/closure_challenge_report.tex:1137` was read at `63453645` and still says the same thing about the entry that reached 0.0455 and 0.0399 on `AR_1` and `AR_3`. It needs the same strike-and-keep from whoever owns `latex/*.tex`; it was not touched. **(d) unchanged in kind, and now 64 rather than 65.** | fleet |
| D72 | **A defect class was "fixed" by writing the function that makes it visible and then wiring that function to nothing — so the class recurred, and the recurrence is the finding rather than the three records it dropped.** On 2026-08-07 the family-supervision pass found nineteen of fifty-six inbox files being silently dropped (finding A-3), and built `agenda.refused_inbox()` against its own stated principle, *"a filter nobody can see is a filter nobody can question"*. On 2026-08-07 a further single record was dropped for a file-extension field and was fixed **for that record**. Re-verified by execution here, not taken on report: at `48d3f05a` `agenda.read_inbox()` admits **128 of 131** files and refuses **3** — `dafoam-restore-ksp-options-escape-hatch` (15 core-min, 5 violations, all `cost_basis`/`citation`: an internal file reference), `f5c-unsteady-probe-run` (`rationale`: an internal file reference) and `test-the-solver-default-relaxation-across-the-family` (26 core-min, 7 citation violations plus `hard_criterion: 'paired-solve'` outside the closed list). **Two of the three are solver/numerics work.** The refusals were never invisible in the sense the 2026-08-07 fix imagined — they were *recorded*, into a ledger with **no caller anywhere outside `sdk/tests/`** (`git grep refused_inbox` returns the definition, two test call sites, and three prose mentions). **That is the transferable shape: a fix that relocates a silence rather than ending it. The record was written; nothing read it.** A third per-record patch would have relocated it again. **What was done instead, at `db165e4e`:** `scripts/calibration_scorecard.py` — the instrument whose output the chief quotes into dispatch briefs — becomes the ledger's first real caller and enforces an ARITHMETIC INVARIANT, `files on disk == admitted + refused`, printing every refusal with its violations and its core-min and **exiting 2** when the identity fails. It deliberately does not re-admit anything; admission stays `agenda`'s judgement. The next dropped record announces itself as a broken sum instead of waiting for an audit. Mutation-proven with `__pycache__` purged before each cell: unwiring the call reddens 3 tests, hardcoding `reconciles = True` reddens 1. **That second mutant caught a vacuous first draft of the test — it recomputed the invariant instead of driving the code, and passed against an intake hardcoded to reconcile.** MUST-NOT-MATCH control (L-84), against a temp corpus of 1 well-formed and 5 genuinely malformed records: the good one is admitted, all 5 stay refused, 6 == 1 + 5 balances — the reporting fix did not re-open the rails. | Re-verified by execution 2026-08-14 at `48d3f05a` against a research-audit report whose own figures were stale (it said 121 files / 118 loaded; ten proposals had since been filed). Repair, seven mutants and both controls at `db165e4e` | **The three refused records are NOT repaired and are not the fleet's to rewrite.** Each is a real proposal refused on style rails (internal file references in prose fields) and one also on a `hard_criterion` outside the charter's closed list. Settling it means one of: rewriting the three files' prose to cite by title rather than by path and choosing a legal `hard_criterion` for the paired-solve case; or amending the closed list if `paired-solve` is a category the charter should have. **Do not settle it by loosening the rails** — they are working, as the must-not-match control shows. The structural half is closed. | Katie (the three records' fate; `paired-solve`) / fleet (the invariant, done) |
| D73 | **`queued` is not a status this lab recognises, so 807 core-min of TMR work reads back as merely `proposed` — and the rewrite is silent, which is D72's shape one field down.** `agenda.read_inbox()` line 1101 reads `status if status in STATUSES else "proposed"`. `agenda.STATUSES` is `("proposed", "approved", "approved-queued", "dismissed", "done")`. Measured over all 131 proposal files at `48d3f05a`: raw statuses on disk are `proposed` 100, `done` 19, `approved` 9, `queued` **2**, `dismissed` 1. The two are `tmr-flatplate-finest-grids` (**327 core-min**) and `tmr-naca0012-complete-ladders` (**480 core-min**), **807 core-min** total. The file says one thing and every reader downstream is handed another, with nothing anywhere saying so — the same failure as a refused file, except that here the record does not vanish, it quietly changes its mind. **Surfaced but deliberately not decided at `db165e4e`:** the scorecard now prints a `STATUS COERCED` block naming each file, its on-disk status, what it reads back as, and its core-min. **Not fixed, and the reason is the reason it is filed:** the two available fixes are both somebody else's call. Widening `STATUSES` to admit `queued` changes a vocabulary that `server.py:549,601`, `morning_report.py:284-285` and `set_status` all read, and `OPEN_STATUSES` with it. Rewriting the two records to the canonical `approved-queued` would move **807 core-min of work into an approved queue** — a work-state change dressed up as a spelling correction, which is not an instrument's decision to make. | Executed census over all 131 files in `demo-output/website/agenda/proposals/` (real `/usr/bin/find` and direct JSON parse, never the shell `grep`, which is `ugrep --ignore-files` here), 2026-08-14 at `48d3f05a`; surfaced at `db165e4e` | Katie or the chief choosing ONE: (a) `queued` is a typo for `approved-queued` and the two records are corrected, accepting that this queues 807 core-min; or (b) `queued` is a real state the vocabulary lacks, and `STATUSES` gains it along with a stated relationship to `OPEN_STATUSES`; or (c) the two records are re-filed as `proposed` on purpose, which is what they currently read as anyway — in which case the coercion was right by accident and should be made explicit. **What must not survive any of the three: a status that is silently rewritten.** | Katie / chief |
| D74 | **The lab's calibration hit rate is the wrong headline: more completed work recorded no cost at all than there is graded evidence to be wrong about, and the "three most recent forecasts" the rule scores are not the three most recent.** Measured at `48d3f05a`, frame `demo-output/website/agenda/proposals/*.json`, every file, no filter (131 files): **19** records are `done`; **11** carry both `est_core_min` and `measured_core_min`; **8 (42% of completed work) carry no measured cost at all**. Two of the eleven are exact-zero pairs that test no tolerance band, so **10 of 19 (53%) yield no graded evidence**, and CAPABILITY_STRATEGY §4's rule is judged on **9 samples where 19 were available**. Against that, the forecasting itself is 3 hits of 9 graded (33%). **The auditor's judgement that "the record problem is bigger than the forecasting problem" is SUPPORTED, but not by the raw rates** — 42% missing against a 67% miss rate would say the opposite. It is supported by what the gap does to evaluability: the rule needs three *consecutive* pairs, and consecutive means consecutive among all completed work, not among the subset that happened to get written down. **The sharp instance, found while fixing the ordering defect and not visible before it:** `a3-transonic-adjoint-conditioned-deliberately-not-by-knob-luck`, `created_at` 2026-08-10, is **newer than every one of the eleven pairs** and carries no measured cost. So the window the instrument scores (`n-a10` / `s1-cbfs-weighted-reinversion-arm` / `kfamily-fpe`, all 2026-08-08) is the last three **recorded**, and there is more recent completed work absent from it. Ordering by date fixes which recorded pairs are scored; it cannot conjure pairs that were never recorded. The scorecard now names any such record under `AND THE SCORED WINDOW IS NOT THE TRUE TAIL`. **Stated so no repair here can be read as flattery: the rule remains UNSATISFIED.** Longest all-hit run in time order is **2** against 3 required, and it is 2 under every treatment tested — exact-zero excluded, counted as hits, counted as misses, and under the old filename order. No scoring choice made here manufactured the verdict. | The calibration-instrument repair, 2026-08-14, at `48d3f05a`; the displacement finding and the 8/19 and 10/19 fractions are emitted by `scripts/calibration_scorecard.py` at `db165e4e` and are regenerated on every run rather than quoted (L-79) | Writing the outturn back to the eight records. Several of the costs are already known and written down elsewhere — the pattern D-section rows keep re-finding — so this is transcription, not measurement, for some fraction of the eight. **Until it is done, quote the 42%/53% and not the hit rate:** a 3-of-9 within-20% figure computed on half the completed work invites more confidence than the corpus can carry, and it was being quoted into dispatch briefs. The chief's stale quotation of an earlier figure is what surfaced this. | fleet (transcription) / chief (what gets quoted) |
| D75 | **The restored G-P4 has been withdrawn a second time, on a measurement, and replaced by a gate whose negative control fires.** `S1_PRIORS_PREREGISTRATION.md` §4's restored ratio-and-cosine gate is a W-2 identity: with `ε = ‖g_total‖/‖g_QoI‖`, solving `ε² = 1 + r² − 2rc` for the cosine returns **0.999542138470174** against the directly measured **0.999542138470174**, agreeing to **2.220e-16**, so the third leg is the first two rearranged; and `c ≥ 1 − ε²/(2r) = 0.999540922` is forced by convergence alone before anything about the prior is known. **Re-verified independently and both headline numbers reproduce**, the reader first controlled by recomposing `‖g_total‖` from the two archived arrays and reproducing the reinversion's own history lines entry for entry (1.986435e-01 vs archived 1.986e-01 at eval 1; 1.592554e-02 vs 1.593e-02 at eval 10). **The specimen is D67's, not this row's** (`d659ca61` §2.2), taken over deliberately because a control written by a gate's own author tends to be the control that gate catches: the prior-as-posterior treatment scores **0.0e+00 relative error against the exactly recomputed targets** (≤1.2e-05 against the published rounded ones, into a 1% bar) and **PASSES**. **Correction to D67 on one digit only:** the 0.0e+00 is exact against unrounded targets; the 1.2e-05 residual is the rounding of the published target, not a disagreement, and nothing in the conclusion moves. **W-3 legality checked FIRST and stated in the amendment**, seven ways against the archive outside the repo — no `S1-priors` directory among 444, exactly three run ledgers (all S1-cbfs), zero files written anywhere in the archive since the pre-registration was authored, no container, no repo-side result record. **No compute has run; the amendment is legal.** **The replacement, G-P4b, three legs:** λ₁ of the prior-preconditioned misfit Hessian ≥ 10; the first Hessian-vector product taken along the direction `s = β₁₀ − β₁` of the reinversion, fixed in advance, returning `sᵀHs` within a factor 3 of the archived secant **+7.313133e-01** (D70); and the total variance the reported intervals remove bounded by `s²Σλᵢ/(1+λᵢ)`, which at rank 6 can never exceed **3.375 against a total prior variance of 11,812.5**. **Six treatments scored by both gates: the old gate returns PASS for four of them and cannot tell the prior from a posterior; the new one passes exactly one.** W and Z fail all three legs; H — correct spectrum, Hessian-vector product in the wrong frame — fails leg B alone at +2.397e-03; Q — a 98% tightening in every cell — fails leg C alone, removing 11,807.8 against an entitlement of 3.2757. **The controls corrected the gate before it was published:** leg C's first draft passed a treatment that tightened 2,000 cells by 97%, which the rank bound then showed is not a rank-6 posterior at all. **G-P4a retains the three withdrawn legs under their honest name**, the gradient reproduction control, which does fail a real thing — the plateau β paired with the eval-1 gradient returns ratio 0.501974 and fails — and which must never again be cited as evidence about a posterior. **Bar A is 10 and not the measured Rayleigh quotient because that quotient moves 40× with the state the lognormal prior Hessian is evaluated at (6.1638e+01 at the final field, 2.4175e+03 at β ≡ 1), driven by the 223 cells pinned at 0.2** — a bar at the measured value would be a bar on where the MAP lands. **Named hole, per W-2 question 2: a treatment with the right λ₁ and the wrong eigenvectors still passes, and this stage has no second referent to close it.** **Cost consequence: none — 260 core-min unchanged, six Hv remain six, and nothing here authorises the purchase.** (Frame: four `.npy` files under `/home/ubuntu/certonomous-runs/`, outside the repo and invisible to any repo-scoped grep; DV order throughout and **no permutation applied**; zero solver compute; repo `48d3f05a`.) | G-P4 replacement, 2026-08-14, `dafoam/S1_GP4_REPLACEMENT_2026-08-14.md` and `ladder-b/S1_PRIORS_PREREGISTRATION.md` §4/§4a | The 260 core-min purchase is now gated on something that can fail, so the hold is the owner's to lift on the science rather than on the gate. Before it is lifted, close G-P4b's named hole if it is worth closing: it needs one external referent on a *direction* rather than a magnitude, and the archive does not hold one — price that separately rather than pretending leg C covers it. | fleet |
| D76 | **A second W-2 identity survives in the same pre-registration, and its twin says so in its own words — reported, deliberately not withdrawn.** `S1_PRIORS_PREREGISTRATION.md` §4's **G-P1** gates on *"the λ is derived, not matched"*, and its quantity is a closed form in four numbers the document supplies itself: `λ_LN = λ_QoI·σ_d²/(3N·s²)` with λ_QoI from the reinversion driver, σ_d from §3c, 3N = 63,000 and s = 0.75 from §3d. Executed 2026-08-14 it returns **8.133557e-06** against the **8.1335e-06** §3d already publishes, and the band floor **2.033389e-06** against the published 2.0334e-06. So the gate's quantity is not merely derivable from its inputs — **it is already computed and printed in the same document, before any run**, and the stated failure mode *"a value chosen to match a previous run fails"* cannot occur because the value is not chosen. **The same shape is self-declared in the parallel `S1_WITH_PRIORS_PREREGISTRATION.md` §7(a), whose own text reads "Bar met in advance by construction"** — two instances, one ruling. **The rest of the board is clean on this test**, checked rather than assumed: G-P2 bars on a *count* of pinned cells and is sound (its 8.2× restoring-pull ratio *is* an identity, reproduced at 8.18×, and is correctly reported rather than gated on); G-P3 is a deliverable check that a submission omitting the spectrum fails; §5's Hv symmetry check can fail because a finite difference of a truncated adjoint is not symmetric by construction; F1/F2/F3 and G1/G2/G1w all bar on measurements, and G2 has failed three times empirically (29.0%, 26.9%, 42.7%). **Filed and NOT acted on: G-P1 is outside the G-P4 amendment's scope and the call is the owner's**, as it is for D66. Cross-references, not re-filings: `S1_WITH_PRIORS_PREREGISTRATION.md` §7(c) bars on the identity its own §2a forbids by name, and `B52_RUNG8_PREREGISTRATION.md:39` is **D66** and remains the owner's verdict. **Separately, three proposal records are being silently discarded by the inbox reader right now** — `dafoam-restore-ksp-options-escape-hatch` (file references in `cost_basis` and four citations), `f5c-unsteady-probe-run` (one in `rationale`), and `test-the-solver-default-relaxation-across-the-family` (six citation violations plus `hard_criterion: paired-solve`, outside the closed list) — measured by executing `read_inbox` at 128 loaded records, and reported rather than repaired because another agent owns that reader. | G-P4 replacement sibling sweep, 2026-08-14, `dafoam/S1_GP4_REPLACEMENT_2026-08-14.md` §6 | Rule on G-P1 and on the parallel document's §7(a) together, since the fix is one ruling: either both are demoted to reported-not-gated under W-2, or W-2 is narrowed and the narrowing is written down. Neither is urgent in compute terms — both items are unrun — but G-P1 is one of four gates on a 260 core-min purchase, so the ruling should land before the purchase does. | fleet |
| D77 | **Rule B's other half: the message was narrowed to the rule, and whether the RULE should gain the proximity clause that would have earned the old message is still open.** D54 settled only the cheaper half. `_place_unnamed` (`scripts/self_audit.py`) faults a phrase whose holder is named one character outside the match exactly as it faults one naming nobody anywhere, and as of `48d3f05a` it still does, deliberately. **Re-executed at `48d3f05a` against a worktree of `5c9c63fb`:** rule B has **14** occurrences there, not the 13 D54's row records -- the numerator reproduces exactly (**9** name Yang within +/-300 characters) and so do D54's four named non-Yang surfaces, but `docs/INSTRUMENT_INTEGRITY_LEDGER.md`'s *"Our margin over the runner-up"* is a fifth, uncounted, and it is the only `runner-up` phrase where the other thirteen are `leader` phrases, which is the likely reason. Of the 14, **3 carry no board surname at all within +/-300 characters** (`benchmarks.json`, `wall/wall.json`, `sdk/scripts/build_benchmarks.py` -- the generator that writes one of the others), so the anonymous cases the rule exists to catch are a **fifth** of the sample, not a rounding error. | D54's open half, costed at `5c9c63fb` 2026-08-12; re-executed 2026-08-14 at `48d3f05a` during the recovery of D54/D55's orphaned work | **Still not free, and the cost is now measured in one place D54 did not look: a proximity clause is not one change but at least two, with different blast radii.** Mutation-proved at `48d3f05a`: a window keyed on **any capitalised proper noun** and a window keyed on **`_board_names(board)`** -- the way rule A's own adjudication is keyed, and so the likelier way it would be written -- are different detectors, and the board-keyed one is invisible to any probe naming an entrant the passed board does not carry. **The gate is unchanged and is D54's:** rule B's reach still rests on a five-sentence smoke test the check's own verdict calls *"not a reach measurement"*, and both published precision rows are rule-A sets, so there is still no sample in this lab against which a widening could be shown not to lose those 3 anonymous cases. What would settle it is a rule-B held-out set with the anonymous shapes enumerated, built by someone other than the widening's author. Until then `test_the_rule_still_has_no_window_at_any_distance` and `test_a_window_keyed_on_the_BOARD_is_caught_too` (`sdk/tests/test_fault_message_matches_rule.py`) hold the rule where it is, and both are mutation-proved against the clause they name. | fleet |
| D78 | **Five things a machine now knows about how checkable this lab is, none of them visible until something tried to run everything at once.** Measured at `6e54a444` by `scripts/lab_check.py`, which enumerates rather than reads a list. **(1) A HEAD worktree is NOT a gradeable tree here, so the tidy way to schedule a checker is the wrong one.** Grading a `git worktree` of HEAD is the safer thing to schedule -- write-capable checks cannot collide with ten agents in the live tree, and the verdict is anchored to a commit rather than to whatever was half-saved at 03:17. Run against the live tree at the same commit it manufactures failures: `self_audit.py` reports `ledger integrity: ledger not found or empty` and **67 unresolved citations against 6**, and `check_convergence_sweep.py` goes red with `RED: this sweep matched no logs at all ... solve_registry DOES NOT EXIST`. The ledger, the solve registry and the case archives are **gitignored**, so a worktree of HEAD is missing most of the evidence these checks are about. Nothing about the lab is wrong in those runs; the frame is. A nightly snapshot run would cry wolf every night and be switched off inside a week, which is worse than not scheduling it. **(2) A detector that goes OFF cannot redden anything -- and this lab already ruled, for a different artifact, that it must.** `self_audit.py`'s two board-referent guards degrade to **WARN** when their referent cannot be read: `rank claims carry their probability` returns WARN saying `THIS DETECTOR IS PARTLY OFF` when the P(rank 1) interval could not be confirmed current, and `placement words agree with the published board` returns WARN saying `this detector is OFF, not reporting nothing to find` when the board or `git` is unreadable. `self_audit.main()` returns non-zero **only on FAIL**. So the exact condition behind today's stale-board defects reddens no gate anywhere, and today it is live: both guards are sitting at WARN. The opposite was decided for the shipped bundle on 2026-08-10 -- `sdk/tests/test_bundle_drift_gate.py` pins **absent -> FAIL, not WARN**, because `an absent detector is not good news`. Two doctrines, one repository. NOT repaired here: `scripts/self_audit.py` is another family's path today. **(3) The census, and it is smaller than the directory listing: of 53 tracked files under `scripts/`, ELEVEN are gates.** Ten have an entry point whose every exit is the constant 0 -- printers, not gates, which is D64's own criterion for `gate_table.py` and now names nine more; six have no entry point at all; fifteen are shell **actuators** (`auto-stop.sh` powers the box off, `kill_worker.sh` kills solvers, `launch_solve.sh` spends core-hours) with no exit contract a runner can read; six write to the tree; two need arguments. **(4) Three checks exit 2 with `usage:` when called with no arguments** -- `sweep.py` (no default frame, on purpose), `detect_overwrite_signature.py`, `morning_report.py`. A scheduled caller that reads any non-zero as a finding manufactures red as readily as a `2>/dev/null` manufactures green. **(5) The commit ritual this lab mandates discards `git add --chmod=+x`.** `core.fileMode=false` here and `git commit -- <pathspec>` builds its tree from the WORKING TREE, so an index mode of 100755 is dropped and the filesystem bit is not read either: two shebang-bearing files went in 100644 at `6e54a444` despite being staged 755. `sdk/tests/test_exec_bits.py` caught it within a minute; repaired at `e772027c` with `git -c core.fileMode=true`, which changes no repository configuration. D53's family is exactly this defect and the fix D53 prescribes does not survive the commit form this lab prescribes. | Found while closing D64 with `scripts/lab_check.py`, 2026-08-14, at `6e54a444`. (1) is two runs of the same checks at the same commit in two frames, not an argument; (2) is read out of the three `Result(...)` sites and the `main()` return, and is reproducible with `python scripts/self_audit.py &#124; grep 'DETECTOR'`; (3) and (4) are the runner's own printed skip census. | **(2) is the one worth someone's time**: either the two board guards' OFF branches become FAIL as the bundle gate's did, or the lab writes down why the bundle is different -- one of those two, not silence. (5) is a one-line addition to whatever documents the commit ritual. (1) needs nothing unless somebody wants a snapshot-gradeable tree, which would mean tracking or synthesising the evidence a check reads, and that belongs with the Phase-2 structure question rather than here. (3) and (4) are for whoever next writes a check: a printer is not a gate, and a gate is a program whose exit code is a function of its finding. | fleet / chief |
| D79 | **A before/after delta is not a delta if the "before" was measured on a different tree — and in a lab where ten agents commit concurrently, the tree moves between reading it and changing it.** Found while independently re-executing D71's own measurement rather than reading its report. D71 recorded rule-A 68 with 3 travelling faults "before" and 65 with 0 "after", and concluded that the difference *is* the three travelling repairs and that the docket row itself contributed nothing. Re-run at each commit with one fixed instrument, the corpus actually stood at **70** on the tree the repair was applied to: the row's own first draft had landed in the file in an intervening commit carrying two faults of its own. The repair took 70 → 67; the row's rewrite took 67 → 65. **Both published endpoints are exactly right and the attribution between them is wrong**, because the "before" was five commits older than the "after" and the row's own +2 and −2 cancelled invisibly across the gap. This is not a slip in arithmetic — the arithmetic is correct — it is a **frame** defect of the kind L-75 names, and it is the one a self-measuring surface is most exposed to: **a document that is inside its own corpus changes the number it is reporting, and the act of reporting the change is itself a change.** The failure is invisible to any amount of re-reading and shows up on the first re-execution. **It is also not hypothetical about direction:** had the intervening commits added faults that were never removed, the row would have published a delta that understated its own damage while every individual figure in it verified. | Verifying D71 by execution, 2026-08-14 — `board_placement_faults` imported read-only from an unmodified `scripts/self_audit.py`, one instrument held fixed while the corpus was varied by extracting each commit with `git archive` (no worktree, no checkout, nothing written to the live tree): `7009c745` 68/3, `cc906ec9^` 70/3, `cc906ec9` 67/0, `2018f7e8` 65/0, working tree 65/0, and 64/0 after `63453645`. The four-entry pin reads 2 with 0 travelling at every one of those points. | **Cheap and mechanical:** a before/after claim states the commit its "before" was measured at, and the two anchors must be **adjacent** — measure the tree you are about to change, immediately before changing it, not the tree you started reading. Where the measuring surface is itself inside the measured corpus, the honest form is three numbers and not two: before, after the change, and after writing the record of the change. Settled by a checker that refuses a before/after pair whose anchors differ, or by the convention alone if nobody wants another gate. | fleet |
| D201 | **A6's published convergence gate is an IDENTITY-GATE: `finalRes` is the linear solver's own stopping rule, so the verdict it licenses is forced by its own inputs.** `A6-crm-wing/system/fvSolution` sets `relTol 0.1; tolerance 0;` on **every** solver (`GAMG` for `p`, `smoothSolver` for `U\|T\|e\|h\|nuTilda\|k\|omega\|epsilon`), so `finalRes <= 0.1 x initRes` holds by construction at every outer iteration of every run. **Executed over all 66 `initRes/finalRes` lines in `run_model_run1.log` (6 fields x 11 write points; evidence count printed before the verdict): violations of the bound = 0; max ratio 0.099617 (`p`, line 796 -- within 0.4% of the bound); min 0.008440.** `dafoam/ladder-a/A6_crm_wingbody.md:121-123` gates convergence on *"All six field `finalRes` values at t=1000 are below 1e-8 ... this satisfies `primalMinResTol=1e-8` on every field"*. At t=1000 **all six `finalRes` are below 1e-8 and all six `initRes` are above it** (`nuTilda` 1.181681e-07 -> 7.485783e-09). The gate returns PASS whenever `initRes < 1e-7`: **it is a 1e-7 gate wearing a 1e-8 label**, its sole input is the other number printed on the same line, and it could not have returned anything else -- it would read identically on a run four times further from convergence. It passes loudest on `nuTilda`, the one field that is provably never going to converge (D203). Independent of what quantity `primalMinResTol` actually gates, the operative fact stands: DAFoam's own `satisfied the prescribed tolerance` line appears **0** times in the log (positive controls: **21** in `a1_unpatched_stock.log`, **17** in each of three `W4-defect-robustness` A1 arms). | A1/A5/A6 zero-compute status settlement at HEAD, 2026-08-15 (`demo-output/website/dafoam/S1_A1_A5_A6_HEAD_SETTLEMENT_2026-08-15.md`) | **Under W-2 the quantity may be reported and no verdict may rest on it.** Withdraw the 1e-8 convergence claim from `finalRes` and re-derive it from `initRes`, or from the presence of the `satisfied the prescribed tolerance` line -- which is absent, so the honest verdict is **force-stationary, not converged**. Live sites needing the same edit: `docs/PRODUCT_LIST.md:250`; `dafoam/DAFOAM_CASE_STATUS.md:111`; `dafoam/ladder-a/A6_crm_wingbody.md:1, 121-123, 126, 206-207`; `.json:165` (`"all_below_primalMinResTol_1e-8": true`) and `:216`; `campaign/NOT_PASSING_REGISTER.md:331`; `campaign/DPW8_AEPW4_SCOPING.md:248` / `.json:244`. **Transferable form: a solver's `relTol` is the ceiling on every `finalRes` it prints, so no gate on `finalRes` can be tighter than `relTol x initRes` -- any convergence check reading the final column is measuring the stopping rule it configured.** | the A6 record's owner (the verdict); fleet (a guard refusing any convergence gate on a residual column bounded by a configured `relTol`) |
| D202 | **A second identity-gate on the same line: the geometric-constraint and `patchV` `check_totals` rows CANNOT move under the IDWarp patch, and their non-movement is cited as evidence the patch did no harm.** `W5_GRADIENT_REGRADE.md` §1 and `dafoam/A1_A5_A6_DIAGNOSIS.md` §1.2 row 4(a) kill the "summation over patches" mechanism partly on the observation that `volcon`/`thickcon`/`rcon` *"read 4.37e-12%, 1.27e-11% and 1.36e-08% and **do not move** under the patch."* **Executed over every archived log carrying those rows -- frame: `/usr/bin/find /home/ubuntu/certonomous-runs -name '*.log' -print0 \| xargs -0 /usr/bin/grep -l "geometry.rcon' wrt 'dvs.shape'"` -> 14 files.** The three rows take exactly **3 distinct value-triples across the 14**, partitioned by **case geometry and FD step size only**: `1.363491e-10 / 1.265004e-13 / 4.366597e-14` on **11 logs** spanning **stock and patched IDWarp, np=1 and np=4-scotch, and limiter-on / limiter-off / limiter-default** -- every lever this line tests; `3.917950e-07 / 1.972430e-12 / 7.459069e-14` on 3 sail logs, **stock and patched alike**; `1.227131e-09 / 2.149984e-14 / 1.004041e-14` on the single A1 arm at FD step `h=3e-3`. **Discriminating control from the same table: `CD wrt shape` takes 13 distinct values across the same 14 logs.** These rows are pure FFD/DVGeo surface functions with no volume warp and no CFD in their chain, while the four-line patch lives entirely inside `warpDeriv`; the same identity holds for `CD`/`CL` wrt `patchV`, bit-identical stock<->patched in both case families (`2.316770e-03`, `2.315520e-03`; sail `1.722189e-04`). They would read identically if the patch had done nothing, and identically again if it had destroyed `warpDeriv` outright. | A1/A5/A6 zero-compute status settlement at HEAD, 2026-08-15 (`demo-output/website/dafoam/S1_A1_A5_A6_HEAD_SETTLEMENT_2026-08-15.md`) | **Split the citation, do not delete it.** The *values* are informative -- 1e-12 to 1e-14 agreement genuinely validates the shared FFD/DVGeo Jacobian, and that half stands. **Their invariance under the patch carries no information and must not be gated on (W-2).** The mechanism kill does not fall: leg (b) of the same row -- IDWarp's `sum(dXs)` bit-identical with the rotation term present and absent on all five of upstream's own regression meshes -- measures the quantity actually at issue and carries the row alone. **Transferable form: before citing "X did not move under the change" as evidence the change was safe, check whether X is downstream of the change at all; if it is not, the null is manufactured.** | the `W5_GRADIENT_REGRADE.md` and diagnosis owners (the split); fleet (an invariance-claim check asking whether the invariant quantity is even in the changed chain) |
| D203 | **A6's primal does NOT reach its own `primalMinResTol=1e-8`, the record's trace read the wrong field, and this KILLS a priced experiment at zero compute.** `A1_A5_A6_DIAGNOSIS.md` §2.5 traces `he` alone, finds it strictly decreasing over 7.33 decades and still falling 3.34x per 100 iterations, and concludes *"nothing in the log suggests it would not have continued closing"*; §5 then prices **P-2** (restart to `endTime 1250`) at **~7 core-min** on that rate. **Executed here across all six fields -- 66 lines parsed, evidence count printed before the verdict -- `nuTilda`, the field with the LARGEST `initRes` and therefore the binding one, stopped decaying at t~600.** Its `initRes` at t=600,700,800,900,1000 reads `1.318e-7, 1.265e-7, 1.218e-7, 1.201e-7, 1.182e-7`: a factor of **1.115 across 400 iterations** (1.01-1.02x per 100) against `he`'s factor of **165** over the same span. At the measured rate `nuTilda` needs **15,573 further iterations** to cross 1e-8 = **~419 core-min** on the run's own basis (`ExecutionTime = 403.99 s` for 1000 iterations at np=4 = 26.93 core-min/1000 iters) -- **60x the quoted price**, and only if a 400-iteration plateau breaks. At `endTime 1250` `nuTilda` lands at ~1.13e-7, still ~11x above tolerance: **P-2 cannot produce the outcome it was bought for.** Its second stated purpose -- re-verifying CD to rescue the "0.0067%" claim -- is refuted by the same document's §5: the reference `0.02090` is four significant figures, resolving agreement to **+/-0.023923%**, while the run's own CD moves **0.007323%** peak-to-peak over its last four write points against the **0.006862%** being claimed. **Every outcome of P-2 as scoped leaves belief unmoved.** | A1/A5/A6 zero-compute status settlement at HEAD, 2026-08-15 (`demo-output/website/dafoam/S1_A1_A5_A6_HEAD_SETTLEMENT_2026-08-15.md`) | **Do not buy P-2.** The question it was bought to answer is already answered on disk in the opposite direction: **A6's primal is force-stationary with a turbulence-variable plateau and does not converge to its own criterion on this configuration** -- a stronger statement than "not converged", and free. Amend `A1_A5_A6_DIAGNOSIS.md` §2.5 and §5 **beside** the original per L-76 rather than rewriting: the `he` trace is correct; the inference from it to the run is not. **Transferable form, and it is the general one: a convergence extrapolation must be taken on the argmax field, not on the field the open question happened to name -- the register asked about temperature, so temperature is what got traced, and the binding field was never looked at.** | the diagnosis's author (the amendment); chief (the P-2 line item); fleet (an argmax-field rule for any convergence extrapolation) |
| D204 | **A6's "memory wall" is not merely unmeasured -- it is misnamed at its source, and `docs/PRODUCT_LIST.md` contradicts itself about it seventy lines apart.** `:251` reads *"Adjoint not attempted (memory wall)"*, inherited from A3 *"for the same structural reason"* (`DAFOAM_CASE_STATUS.md:275`). **`docs/PRODUCT_LIST.md:176-179`, the A3 entry in the same section, says the opposite in its own words: `DIVERGED_BREAKDOWN` at every mesh size incl. 21,840 cells with >20 GB free -- "conditioning, not memory".** Executed with a validated positive control: on `A3-onera-m6-adjoint-probe80k/run_opt4_probe80k.log` (`Global Cells: 79560`, `Global Adjoint States: 724609`) the adjoint **ran** -- `dRdWT Jacobian Free created!` at line 589, then `Main iteration 0/100/200 KSP Residual ...` **twice**, each ending `**Completed**! Total iterations: 200. PetscConvergedReason: -5`, then `Finalising parallel run`. **No OOM, no kill, no allocation failure.** On A6 itself, `run_model_run1.log` registers **`Global Adjoint States: 5244840`** over 579,072 cells and **creates the matrix-free transposed-Jacobian operator** (`dRdWT Jacobian Free created!`, line 666) on this host at np=4, then completes 1000 SIMPLE iterations and exits `End` cleanly. `Main iteration` and `KSP Residual` appear **0** times in all four A6 logs (controls: 16 / 18 / 17 in three logs that do solve adjoints), so **no adjoint solve was ever attempted on A6** -- the attribution has no A6 measurement behind it AND names a cause its own source case rules out. **Stated against myself: successful `dRdWT` creation does not prove an A6 adjoint would fit** -- the matrix-free operator is the cheap half and A3's documented death point is preconditioner/Jacobian assembly. What is established is narrower and still decisive: the label is projected from a case whose record names a different cause, past a step that demonstrably did not fail. | A1/A5/A6 zero-compute status settlement at HEAD, 2026-08-15 (`demo-output/website/dafoam/S1_A1_A5_A6_HEAD_SETTLEMENT_2026-08-15.md`) | **Free, documentary, and the instrument already exists in this lab**: apply *"NOT EVALUABLE ON THIS HOST"* -- the phrasing adopted here for exactly this state -- in place of "memory wall" at `docs/PRODUCT_LIST.md:251`, `dafoam/DAFOAM_CASE_STATUS.md:112`, `campaign/NOT_PASSING_REGISTER.md:329, 332, 754`, `dafoam/ladder-a/A6_crm_wingbody.md:4-6, ~186` / `.json:6`, `ACTIVE_RESEARCH.md:146`, and **`sdk/workflows/crm_wingbody.py:386` (shipped SDK code)**. **An A6 adjoint attempt (the 760-1,520 core-min class) must NOT be proposed to settle this** -- a zero-cost analysis decides the same question, which is the standing reason that class is recommended against. Separable and related: `campaign/DPW8_AEPW4_SCOPING.md:250` states the envelope *"works at 63,920 cells; dies in Jacobian assembly at 156,089 cells"*, and the 79,560-cell probe above assembled and ran 200 KSP iterations twice -- so the lower bound is at least 79,560, not 63,920. | the A6 record's owner; the SDK owner (`crm_wingbody.py:386`); Katie (whether the DPW8 envelope figure is re-based) |
| D205 | **The `demo-output/website/latex/` sibling is a stale artifact of the known class: its `.tex` was repaired and its PDF was not.** `closure_challenge_report.tex` is dated **2026-08-12 17:54:12**; `closure_challenge_report.pdf` is dated **2026-08-11 03:26:36** -- **the source is a day and a half newer than the rendered document that travels.** Found while establishing build currency for `dafoam_defect_report`, whose ordering is the healthy one (`.tex` 03:24:39, `.pdf` 03:26:38, and every string checked in the text layer reproduces the current `.tex`). **Grading method stated, because a text extract cannot settle this class:** `\sout{}` is strike-and-KEEP, so a withdrawn line sits in a `pdftotext` extract of a correctly repaired report exactly as in a stale one. Pages 1 and 20 of `dafoam_defect_report.pdf` were therefore rendered with `pdftoppm -png -r 130` and **read visually**; no strikethrough rules appear on either, consistent with `/usr/bin/grep -c "sout" dafoam_defect_report.tex` -> **0**, i.e. that report holds no withdrawn content at all. **Scope, stated so this is not read as more than it is:** the stale sibling carries **zero** A6/CRM claims (`/usr/bin/grep -c "0.0067\|CRM\|crm_wing"` -> 0), so its staleness carries none of the four wrong A6 statements; it is a build-hygiene defect on a document intended to travel, not a live false claim. | A1/A5/A6 zero-compute status settlement at HEAD, 2026-08-15 (`demo-output/website/dafoam/S1_A1_A5_A6_HEAD_SETTLEMENT_2026-08-15.md`) | Rebuild `closure_challenge_report.pdf` from its current `.tex`, then diff the text layers to confirm the repair landed. **The durable half is a check, not a rebuild:** any tracked `.pdf` whose sibling `.tex` has a newer mtime is stale by construction, and that is a one-line predicate over `git ls-files '*.tex'`. It would have caught this without anyone looking, and it is the same class as the 8 claim sites that needed **2 outward-facing PDFs rebuilt** on 2026-08-11, where the rebuild happened only because someone thought to check. **This also governs `dafoam_defect_report.pdf` the moment its owner applies the three A1 currency edits filed in the settlement's §8.3: the edit is not complete until the PDF is rebuilt.** | the `latex/` owner (both rebuilds); fleet (the tex-newer-than-pdf predicate) |

## E. Standing task-list items not yet executed

| # | Item |
|---|------|
| E1 | **MEASURED 2026-08-11: it is 31 of 34, not 30.** Executed every `check_*` in `scripts/self_audit.py` and searched both `summary` and `detail` for a stated blind spot. **Three state one; thirty-one state none; zero raised.** So the audit's own checks are almost entirely silent about what they cannot see — and a green check that does not say what it is blind to reads as coverage. The three that do are the recently-built ones, which suggests the practice is arriving rather than eroding. *Positive control, and it was needed: my **first** run of this measurement returned **0 of 34** — a clean, confident zero produced by searching only `summary`, when the blind-spot text lives in `detail`. `check_board_placement_words` is the known-positive that a working method must find, and the first method did not. The corrected method finds it. This is the third instrument tonight whose first answer was a false zero.* | Print them. The measurement is the easy half; the thirty-one texts are the work, and each is a claim about a check's reach that its author has to be able to defend. |
| E2 | **DIAGNOSED 2026-08-11. Four of 146 `.done` records are zero bytes**, and all four carry the *same* timestamp — `f6b2_{coarse,medium,fine,veryfine}_20260805T171046Z` — so one event killed four collectors at once. **Mechanism:** `scripts/launch_solve.sh:427` ends its collector with `} > "$REC"`, and the shell **creates and truncates `$REC` at redirection time, before the block runs**. If the disowned subshell dies first — box power-off, session kill — the file survives at zero bytes. **Why it matters:** a zero-byte `.done` is indistinguishable from a completed record to anything testing existence rather than content, which is the fail-open shape; and **146 `.done` records are load-bearing evidence** cited across `INSTRUMENT_INTEGRITY_LEDGER.md`, `LEDGER_HEADLINE_AUDIT.md` and `PRODUCT_LIST.md`, with proposal P6 naming a 146-record corpus replay as a prerequisite. Four of the records that replay would read are empty. The class is already known — `campaign/DPW8_V2_joukowski.md:214` says *"Check the `.done` body, not merely its existence"* — but was never made structural. | **Write the record atomically**: build it at `$REC.partial`, then `mv` into place, so a `.done` either exists complete or does not exist. A rename within one directory is atomic; a redirection is not. Then the existence test becomes true again and every consumer is correct without changing. The four existing empties are evidence of a real interruption and should be marked, not deleted. *(Registry is gitignored, so this is invisible to any repo-scoped sweep — L-75.)* |

## F. Repo professionalization — Katie's §7, 2026-08-11. **BLOCKED BY DESIGN.**

**The block is Katie's own sequencing and it is not a delay to be worked around:** *"after
Ladder V converges — not during"*, because path moves under active worktrees create merge
chaos and moves during an open verification poison the ladder's paths. It sits here, in the
queue, rather than in flight.

**Release conditions, all four:**

1. Ladder V converged (the fixed point, not a clean sweep — see the ladder's termination rule).
2. Every family drained or checkpointed. No agent holding open work in the tree.
3. One dedicated agent, one branch `chore/repo-structure`, one quiet window.
4. A written `MOVE_MAP` (old → new, **every** file) committed **first**, before any move.

**Mechanics that bind that agent:** `git mv` only, history preserved; imports, launchers,
cron and systemd paths updated **in the same commits** as the moves; full test suite green
after **every batch**; the sanctioned `sweep()` helper re-pointed (it is B1's deliverable, so
B1 lands first); no history rewrite; nothing load-bearing deleted without a docket entry.

**Pointers that will dangle when `scripts/` moves under `ops/`** — a running list, because
each is a live cross-file dependency and G3 requires them updated *in the same commit as the
move*, not afterwards. Add to it as they are created; this is cheaper to maintain now than to
discover during the window:

- `scripts/corpus_figures.py` — `docs/MEMORY_ARCHITECTURE.md` §5 points at it by path, having
  replaced its hardcoded corpus counts with a call to it. If it moves and the pointer does
  not, the map's central section instructs the reader to run a command that does not exist.
  *(Raised by peer session `certonomous-3b`, whose agent holds the other half of that repair.)*
- `scripts/sweep.py` — B1's sanctioned helper, named by G3 explicitly.
- `scripts/self_audit.py` — invoked by name from the test suite and from several campaign
  records.
- `scripts/fail_open_scan.py` and `sdk/tests/test_fail_open_scan.py` — the scanner hardcodes a
  path to `scripts/self_audit.py` **and pins a historical commit for its positive control**. So
  a move breaks **the control rather than the scan**, which is the quieter and worse failure: a
  positive control that silently stops working is how a scan starts reporting confident
  negatives. *(Raised by the peer session; reasoning kept as they put it, because the reason is
  sharper than the fact.)*
- `demo-output/website/campaign/F7_runs/old_spec_readings.py` — reads tracked field files by
  relative path.

**Target shape** (proposed, then executed): product source under `certonomous/` or a retained
`sdk/`, with `workflows/`, `agents/`, `kernel/` beneath it; `ops/` for launchers, auto-stop and
preflight; `docs/` as the **only** documentation home, with `charters/`, `standards/`,
`research/`, `architecture/`; `cases/` for configs and birth certificates, **never results**;
`evidence/` gitignored with a README explaining what lives there and why it is untracked;
`web/` for site-bound artifacts. Root reduced to README, the LICENSE decision, top-level
configs, and directories. **Nothing loose at root.**

**READMEs:** root README = what Certonomous is in three sentences, a 30-second architecture
sketch, how to run one mission, a repo map at one line per directory, and a pointer to
`docs/`. Every top-level directory gets its own short README naming purpose and entry points.
The qg-closure Q2 standard applies — goals and structure, professional register, no stale
claims — and **every number quoted obeys L-79: regenerate it, or carry its provenance.**

**Acceptance is a test, not an opinion — the cold-visitor test.** An agent with no context
clones the branch and must answer, within one screen per level: what is this, how do I run a
mission, where are the charters, where is the evidence for claim X. **Every question it has to
dig for is a defect.** Then Sanaa walks the tree herself before merge. That second gate is a
**taste gate and taste is hers** — it is not delegable to a checklist.

**One finding already, made while filing this and worth landing before the move window opens:**
G5 asks that tracked generated artifacts move to a gitignored `evidence/` or leave tracking.
They exist in quantity **today** — `demo-output/website/campaign/F7_runs/F7a_R1/` alone tracks
OpenFOAM time directories (`0.025/U`, `alpha.water`, `phi`, `uniform/functionObjects/…`) as
version-controlled files. That is solver output in git. **Scoping it is zero-compute and can
be done now**; the moves themselves wait for the window with everything else.

| # | Item | Filed | State |
|---|------|-------|-------|
| F1 | **Phase-2 prep delivered: proposed target tree + full MOVE_MAP + breakage census — AWAITING SANAA'S VETO; execution stays gated on Ladder V's confirmed green, the drained fleet, the quiet window, and her walk.** Filed 2026-08-14 at `1503fa4d`/`7c7296ff`. The tree instantiates this section's target shape (sdk/ retained — it already carried the §7 substructure; ops/ ← scripts/; docs/ + filming/; cases/ ← models/ + demo-surfaces/; web/site/ ← demo-output/website with the served root moving whole so URL paths below it survive; web/filming/; evidence/ gitignored, mirroring old paths). `docs/PHASE2_MOVE_MAP.tsv` enumerated **20,202 rows at anchor `1503fa4d`, no wildcards** (12,791 untrack-to-evidence / 263 low-fanout mv / 7,148 big-rename); generated by `scripts/phase2_move_map.py` with controls (zero unmapped, zero collisions, every output case root explained) — **the generator is authoritative and the last regeneration before execution is the binding map**, because the tree moved twice while this was drafted. Breakage measured, not guessed: worst offender `demo-output/website` → web/site at ~2,114 live refs (~680 in the guard layer; concentrated in exec_bits.py's waiver registry, two sdk fixtures, self_audit's `WEB` constant); scripts→ops ~459 live refs under the prefix incl. fail_open_scan's negative control, `_PY_ROOTS`, the BASIS producer pins and a **live crontab line no repo sweep can see**; §4.2's owed non-markdown sweep executed and it **refuted the markdown-only zero — eight runtime consumers hard-break** (old_spec_readings.py, score_our_baseline.py, build_ref.py, 3× f6d builders, check_convergence_validate.py, score_s6_partition.py, drag_split.py, W3 setup.sh) plus two archive-sweep instruments rooted at demo-output; the published-URL surface came back **clean — 0 of 79 links at risk**. The H3b `.gitignore` rebuild lands as the FIRST act of the quiet window, two back-to-back commits (rule, then untrack+move), window verified three ways before the edit — **it was not touched here**. Nine veto items (V1–V9) put the judgment calls to Sanaa explicitly, incl. LESSONS.md and uq_batch root exceptions, the D56-gated dist zip, and models/curriculum/results entering cases/ against its "never results" charter. Full text: `docs/PHASE2_STRUCTURE_PROPOSAL.md`. | 2026-08-14, `1503fa4d` + `7c7296ff` | **AWAITING SANAA'S VETO — no file moved, nothing executed** |

## G. Naval campaign — Katie's §8, 2026-08-11. Executing the zero-compute half now.

| # | Item | Compute? | State |
|---|------|----------|-------|
| G1 | **H1 F7a re-gate.** Pin the measurement definition **contractually** in the gate spec — probe row, front criterion, time origin: the exact ambiguity that produced the false FAIL. Then re-run and take the verdict either way. | Spec: **no**. Re-run: **yes** | **DONE, and the re-run turned out not to be needed for the verdict.** `campaign/F7a_REGATE_SPEC.md` §2 pins all four definitions; §1 exhibits the old ambiguity as measured fact (+13.4% vs −16.1% on the same solve); §3 takes the verdict **FAIL, max +11.03%**, from already-tracked fields at **zero compute**. Compute request re-scoped and its expensive half recommended against — see G1a |
| G2 | **H2 Wigley hull.** Wave-making resistance vs published data at 2–3 Froude numbers; free-surface mesh discipline written into a new marine section of `MESH_STANDARD`; birth certificates as everywhere. | Yes | Mesh section and pre-registration can precede it |
| G3 | **H3 DTMB 5415.** Resistance vs open workshop data — the credibility case naval people recognise on sight. Staged per doctrine: feasibility → physics (wave pattern qualitatively right) → gate rung. | Yes, staged | Staging plan is zero-compute |
| G4 | **H4 extensions, filed as COSTED PROPOSALS and not auto-run:** KCS container ship; propeller open-water curve vs workshop data ~~(reuses F8's MRF machinery — the naval turbine)~~ **— see the amendment below, the reuse premise does not survive**; seakeeping / added resistance, which needs wave BCs we may not have, so **the deliverable is the capability-gap map, honest about what is missing.** | Filing: **no** | Filing now |
| G5 | **H5** Every naval gate lands on the credentials wall with regime metadata like everything else, and **Naval becomes a wall category.** Katie's GTM list already touches marine engineers, so these wall entries are sales artifacts the day they exist. | No | Follows each gate |

**State after the 2026-08-11 zero-compute pass.** G1 done (above). G2 mesh
section done — `docs/standards/MESH_STANDARD.md` §7, with §7.0 stating why it
landed there rather than in `docs/MESH_STANDARD.md`, and the Wigley comparison
stations pinned in `campaign/NAVAL_CAPABILITY_GAP_MAP.md` §6.2a. G3 staging plan
done — `campaign/F7c_DTMB5415_STAGING_PLAN.md`, now carrying a **Stage 0** it was
missing (see G3a). G4 done — three proposals filed, and the gap map delivered.

| # | Amendment | Owner |
|---|---|---|
| **G1a** | **The F7a compute request, re-scoped.** The re-gate needs no compute to produce a verdict, so the request is now: **buy the zero-core-min comparison-basis item (R0)**; **do not buy** the a/512 rung (R1a, **760–1,520 core-min**, 2–4× the entire R1 campaign) to settle the shape of a convergence sequence. Reason, measured: `res16_papermodel` — comparator physics, mesh and domain all matched — sits **+23.3% ahead of the comparator's own published curve**, an offset larger than the +8.2% residual the campaign has been chasing. Refining the mesh explores a direction already shown to be small. Priced in `campaign/F7a_REGATE_PREREGISTRATION.md` §5. | Katie |
| **G1b** | **The compressed version of the D-12 story is wrong, and D-12 names six files that carry it.** "The F7a FAIL was a measurement artifact" is false: the ambiguity is worth ~20 points of the *originally reported* number and **essentially none of the current one** — at dy ≤ a/32 every reading of even the old definition lands +7.8% to +11.9%, all failing 5%. The six files are not individually wrong; the summary that circulates is. Not edited here: six near-verbatim copies is exactly the amendment hazard D-12 exists to flag, and C3 gives the case record the home. | fleet |
| **G3a** | **DTMB 5415 has no verified open geometry route** — ITTC-cited navy host **DNS-dead**, `simman2014` **registration-walled**, `simman2008` **TLS cert mismatch**. Its reference *data* is the best of the four (ITTC 27th Resistance Committee: CT, sinkage, trim at Fr 0.1/0.28/0.41 across **eleven** tanks), which is what makes it look fundable until someone tries to mesh it. **Registration is an external interaction — PARKED and reserved to Katie.** Filed as Stage 0 of the staging plan. | Katie |
| **G3b** | **The naval ordering docket §8 implies is inverted by the evidence.** By blockedness: propeller open-water (**not blocked by the ladder rule at all**, 900 core-min) → wave-capability step (135) → KCS (3,000; data open and tabulated, geometry verified) → **DTMB 5415, the most blocked of the four**. Recommendation only; §8's sequencing was written before this evidence existed. | Katie |
| **G4a** | **`grade_f7a.py` averages spurious post-wall crossings into its reported mean** — it prints "mean −0.8%" for `res16_base` and "mean −6.7%" for `res16_papermodel`, both arithmetic over ≈ −87% artifacts produced when the front passes the far wall and the furthest-crossing search picks up a crossing back in the collapsing column. Fixed **by contract** in `F7a_REGATE_SPEC.md` §2.2 (monotonicity guard); **not fixed in the script**. B2's class. Any number taken from that script's unrestricted mean line is unframed until re-derived. | fleet |
| **G1c** | **G1's "DONE" was half true: §2 pinned the definition in prose, and the code had already drifted from it in five ways.** `F7a_REGATE_SPEC.md` §2 declared itself normative and lived only in markdown; `front_metrics.py`, which §3 named as its extractor, landed at `4aad8298` **twelve days before** the spec at `1393b8b4` and implemented none of §2's assertions, guards, station set, tolerance or verdict — nothing could go red when they disagreed. Executed at `86704ddf`: `F7_runs/f7a_contract.py` became the definition's single executable home and `sdk/tests/test_f7a_contract.py` re-parses every frozen constant out of §2's own prose on each run, so prose and code cannot diverge in either direction. **42 tests; 0 passed against the prior tree (2 failed, 40 errored); 42 passed after**; mutation-checked on six constants. Found: 9-dp column grouping where §2.1 pins 8 (on the a/16 family 37×263 apparent against 4,800 real cells); the length scale `a` taken from argv where §2.1 pins it exactly; `gate_compare.py`'s reference table reading T = 6.74 where every other surface reads 6.70 (**struck and kept**, L-76 — it records what the 2026-07-28 gate executed). **Two conclusions struck, spec §6.1–6.2:** (i) the six-file "deviation roughly halved, +13.6% → +8.2%" compared readings under **two different definitions on two different meshes** — holding the case fixed and changing only the definition moves the mean +12.7% → +13.1%, so the halving was **refinement, not the metric**; and the case behind +13.6% cannot carry a verdict under §2 at all (ΔT = 0.655 against §2.3.4's 0.35 cap, mesh a/20 against §2.5's a/128 floor). (ii) `res32y256_base`, the **finest** tracked mesh, is UNGRADEABLE: R1's "unexplained early-time outlier" at T = 3.90 is the metric's own threshold spread at **14.97% of Z**, fifteen times the §2.2 trigger — its +10.8% mean was unframed. **Re-gate, zero compute, tracked fields only: three cases satisfy §2.5's verdict floor and all three FAIL** — `res32y128_base` 11.03%, `res64y128_base` 12.23%, `res32y128_slip` 19.12%, against 5%. §3's numbers reproduced to 0.01 pt at every graded station. **Open, and the only thing this row asks of anyone:** §2.1's pinned **8 dp is defective** — it does not close `res8_base` (211 apparent columns against 120) or `damBreak_MM_a2p25in_medium_closedbox` (329×72 against 300×40; its `C` was written at 6 significant figures), on which the contract FAILS LOUD rather than integrating over columns that do not exist. **6 dp closes every tracked case and moves no verdict and no number.** Re-pinning is a **v1.1 under §2.6** and was deliberately **not taken** — an unauthorised edit to a frozen normative clause is not justified by a change that alters nothing. | fleet |

**The honest note about H4's third item:** "needs wave BCs we may not have" is a claim about
our capability, and the deliverable Katie asked for is the **gap map**, not a workaround. An
agent that discovers we lack the boundary conditions and quietly substitutes something else
has destroyed the deliverable. The map is the product.

> **[AMENDED 2026-08-11 — G4's propeller premise did not survive checking. Original text
> retained above rather than edited, per the supersession rule; the claim is Katie's and she
> is entitled to see it challenged rather than quietly reinterpreted.]**
>
> G4 describes the propeller open-water case as *"reuses F8's MRF machinery — the naval
> turbine"*. That phrasing is Katie's, from §8 H4, and I transcribed it here. **An evidence
> audit of the F8 case files contradicts it on four points**, and pricing the proposal on the
> reuse premise would understate it badly:
>
> - **F8 is a WIND turbine in AIR** — `simpleFoam`, steady, incompressible (`nu 1.4805e-05`,
>   kinematic `p`, no thermophysical model), single-phase, Spalart-Allmaras.
> - Its MRF **is genuine** (a real `constant/MRFProperties`, `omega 7.5398`, zone actually
>   built by `topoSetDict` — not SRF, not AMI, not `rotatingWallVelocity`) — **but the rotating
>   zone is the entire domain**, which is the one thing a propeller case must not copy.
> - **F8's steady-MRF branch is closed on evidence three ways, with ZERO gated CFD numbers.**
>   Reuse would inherit a branch that never produced a gated result.
> - The geometry is a fixed 68 MB / 330,950-triangle STL with **no generator anywhere in the
>   repo**, so a propeller blade is **100% new work**, not a parameter change. And **the sign
>   convention is inverted** — F8 extracts power, a propeller absorbs torque — which is
>   precisely the error class that cost F8 three riders.
>
> **Status: routed to the naval agent to confirm or contradict against the case files
> itself**, because the audit reached me second-hand and this lab's rule is that a grader
> executes rather than relays. Not treated as settled here.
>
> **Calibration datum from the same audit, and it is load-bearing for every H4 price:** F8's
> own proposal carried `est_core_min: 6` against **47.7 core-min actual — an 8× overrun**,
> at a measured 0.54–0.60 core-s/iteration for 230k cells. That is the house's demonstrated
> optimism factor on a rotating case. *(A "0.145 core-s/iteration" figure in F8 §5 contradicts
> every other number in its own record and is not to be used.)*

> **[AMENDED 2026-08-11 — the F8 reuse premise in G4 does not survive checking, and the
> premise was mine to check.]** Katie's §8 describes the propeller item as reusing F8's MRF
> machinery, *"the naval version of the turbine"*. I transcribed that into G4 without opening
> F8. An agent sent to price the reuse found **both halves wrong**:
>
> - **F8 is a WIND turbine in AIR** — `simpleFoam`, steady, incompressible, single-phase,
>   Spalart–Allmaras — not a naval one. And its steady-MRF branch is **closed on evidence
>   three ways** with **zero gated CFD numbers**, so there is no validated result to inherit.
> - **The geometry does not transfer at all.** `blade.stl` is a fixed 68 MB, 330,950-triangle
>   S809 wind blade with **no generator anywhere in the repo**. A propeller blade is **100%
>   new work**.
> - **The sign convention is INVERTED** for a propeller, which absorbs torque rather than
>   producing it — and that is precisely the error class that cost F8 three riders.
> - What genuinely transfers is **dictionaries and process discipline**, which is real but is
>   not what "reuses the MRF machinery" prices at.
>
> One more thing found while pricing it, and it is a calibration fact about the whole proposal
> system rather than about F8: **F8 cost 47.7 core-min against an `est_core_min` of 6 in its
> own proposal — an 8× overrun.** Any naval estimate built by analogy to F8's *estimate*
> inherits that error; build them against its *measured* 0.54–0.60 core-s/iteration at 230k
> cells instead. (F8's own record also carries a "0.145 core-s/iteration" figure in its §5
> that is inconsistent with every other number in the same document; do not use it.)
>
> **And the thing not to copy:** F8's MRF cellZone is **the entire domain** — 230,135 of
> 230,135 cells. It ran, and it is not what an MRF zone is for.
>
> This is Katie's text, so the correction goes to her rather than being applied silently. The
> proposal is not withdrawn — a propeller open-water gate against PPTC/VP1304 is still worth
> filing — it is **repriced from scratch rather than by analogy**, and the report to Katie
> says the analogy was the thing that failed.
| D80 | **Three self_audit FAILs cannot be cleared by the agent clearing self_audit, and each is blocked by a different thing.** First run of `scripts/lab_check.py --only self_audit` on a purged tree returned **9 FAIL and 9 WARN**, not the 5 and 6 the dispatch carried — the dispatch was written against an earlier reading and four reds were never named. Six of the nine were cleared at no compute (`13b965dd`, `b14356b0`, `5120d79c`). The three that remain are each blocked, and none of them is blocked by not knowing the answer. **(a) `cited evidence paths`, 1 of 1112.** `demo-output/website/dafoam/ladder-b/S1_FIML_FIELD_INVERSION.md:229` cites `scripts/analyze_fd.py`; the analyzer is at `demo-output/website/dafoam/ladder-b/S1_work/scripts/analyze_fd.py`, i.e. the citation is relative-to-the-work-dir written as repo-rooted. The edit is one path and the evidence is on disk; `demo-output/website/dafoam/ladder-b/` is a barred directory for this agent. **(b) `declared fleet vs work`, 2 sites.** `sdk/workflows/ahmed_body.py:424` and `sdk/workflows/geometry_study.py:1934` declare a worker fleet on a warm branch that dispatches nothing. This is not an unexamined defect: it is exactly the last two rows of **C-3** in `docs/charters/PROPOSALS_OPEN.md`, which measured all five sites on 2026-08-01, ruled option B for the hump only, and left these two *deliberately* reported-not-edited because their warm branch spends a REAL replay interval and the numeral is half of a two-part misstatement — the other half being that the interval is spent to the ledger as `simpleFoam (14s)`. C-3 says whoever decides should decide once for all six sites. A red check does not convert that into a code edit. **(c) `bundle drift vs tree`, 4 files.** `sdk/chief_engineer/exec_bits.py`, `closure.html`, `benchmarks.html` and now `wall.html` differ from the shipped copy in `dist/certonomous-demo`. The remedy is to rebuild the bundle, which writes into `dist/`, a barred directory. **The fourth file is mine**: rebuilding the wall to clear `wall credentials vs results` moved `wall.html` and so widened this fault by one. That trade was taken knowingly — the wall was carrying four stale literals — but it is recorded here rather than left for the next reader to discover, because a fix that reddens another check and does not say so is how a green run gets built. | Clearing D64's red checks, 2026-08-14, from `scripts/self_audit.py` at `b47cb6bc` and re-verified at `5120d79c`. Frame: full `self_audit.py` run, whole tracked tree plus archive members, `__pycache__` purged before each cell. | **(a)** one path edit by whoever owns `dafoam/ladder-b/`. **(b)** the C-3 ruling, Katie's or the chief's, made once for all six sites and covering what a warm replay may SPEND as well as what it may DISPLAY — not a code edit taken off the back of a red check. **(c)** `python3 scripts/build_laptop_bundle.py --zip` by whoever owns `dist/`, then re-run the check; note that this check FAILs on an absent bundle by the 2026-08-10 ruling, so deleting the bundle is not a way to clear it. | Katie / dafoam owner / dist owner |
| D81 | **A test file full of deliberately wrong placements faults the guard it tests, the moment it is committed — and the verdict moved under me while I worked, from another agent's commit.** `placement words agree with the published board` read **6** lab record placements at `b47cb6bc` and **18** at `5120d79c`. None of the twelve are mine and none are in a file I touched: `d2d6bd6c` committed `sdk/tests/test_two_board_referents.py` and `sdk/tests/test_fault_message_matches_rule.py`, which were UNTRACKED at the first reading, and the sweep's declared blind spot (8) is that it does not open untracked files. Committing them moved 2 surfaces into the swept set and 12 faults onto the verdict. The fixtures are doing their job — the probes pin a deliberately wrong ordinal on a named board entrant, one of them a position the pinned board does not even have, and the guard is right that each disagrees with the board. **They are described here and not quoted, and that is itself part of the finding:** the first draft of this row quoted one probe verbatim and thereby added a nineteenth fault to the very count the row is about. A defect class you cannot write down without committing an instance of it is one the corpus will keep re-committing, and the check's blind-spot list names quotation without drawing that consequence. What the guard cannot do is tell a FIXTURE from a CLAIM, and the check's own blind-spot list does not name that class: it names quotation (a wrong placement quoted to correct it) but not a probe string in a test that exists to make the detector fire. **The consequence is worse than the noise.** A guard whose own test suite reddens it teaches its readers to expect a non-zero count, and a non-zero count is where a real fault hides. It also makes the count a function of what happens to be tracked, so `git add` on a test file changes a corpus measurement. | Re-running `self_audit.py` after each write batch while clearing D64's reds, 2026-08-14. Delta traced to `d2d6bd6c` by `git log b47cb6bc..HEAD` after the count moved, per the method note that a failure inside the run window is checked against the log before it is called flaky. | A decision by the owner of `scripts/self_audit.py`, who is working in it now. The cheap options, in the order I would try them: exclude `sdk/tests/` fixtures the way `work`/`processor0` are already excluded and say so in the frame; or require a fixture to carry a marker the sweep reads, as the citation check's `retracted` window already does for quoted paths; or accept the faults and state on the verdict line how many are fixtures, so the number a reader compares against zero is the number that means something. Not settled by editing the test files — they are correct. | self_audit owner |
| D82 | **The Ahmed act stopped writing its own tier into its transcript, and the gate table published one anyway for an unknown length of time.** `demo-output/website/campaign/NINE_ACT_GATE_TABLE.md` published `SOLVER-BACKED` in the Ahmed 25 deg verdict cell while `scripts/gate_table.py` re-derives `see record` from the artifact the row cites. The tier itself is NOT in doubt and did not move: `lab.displayed_credential` re-derives SOLVER-BACKED from `models/curriculum/results/ahmed_25.json`, `wall.json` carries it, and the act's own `mission-output/ahmed-body/certificate.pdf` prints *Fidelity: SOLVER-BACKED*. What is untrue is that the cited artifact states it. `mission-output/ahmed-body/transcript.txt` carries no tier line at all: its conclusion phase writes pipe-delimited rows (`Quantity ... | Verdict Pass`), while `gate_table._verdict_line` looks for the act's own one-word tier as `Verdict: <tier>.` with a colon, which appears **zero** times in that file — the NASA hump transcript still carries the colon form, which is why only one of nine rows faulted. The cell now publishes `see record`, what the citation supports, with a strike-and-keep note. **The generator was deliberately NOT taught to read the tier from somewhere else**, because this check is declared blind to defects inside `gate_table.py` and re-pointing the generator at the credential file would move that blind spot rather than close it, and would make the row agree with itself by construction. **Neither the transcript nor the act is tracked** — `git ls-files mission-output/ahmed-body/` returns nothing — so there is no commit history to say when the act's output format changed, which is the second finding here. | `gate table vs transcripts` FAIL while clearing D64's reds, 2026-08-14; diagnosed by running `gate_table.rows()` directly against the transcript and grepping the transcript for the colon form. | Either the act writes its tier into its transcript again in the form the generator reads, or the gate table's verdict column is redefined to be sourced from the credential record and `gate_table.py` is changed to match — a decision about what that column MEANS, not a patch. Separately: decide whether act transcripts under `mission-output/` should be tracked, since a published table cites them as evidence and nothing can currently say when one changed. **PROGRESS NOTE 2026-08-15 (NOT CLOSED).** The reported FAIL is repaired at commit `b14356b0`: the Ahmed cell of `NINE_ACT_GATE_TABLE.md` now reads `see record` with a strike-and-keep note, and `check_gate_table_vs_transcripts` PASSes -- all nine rows re-derive from their cited transcript. **THE ROW STAYS OPEN on the two facts underneath it, both still true at HEAD:** `mission-output/ahmed-body/transcript.txt` contains ZERO `Verdict:` lines, and `git ls-files mission-output/ahmed-body/` returns nothing -- the transcript a published table cites as its evidence is UNTRACKED, so no reader can re-derive the cell and no check in `scripts/self_audit.py` can read it. What the verdict column should MEAN, and whether `mission-output/` becomes tracked, are the open questions. | fleet |
| D83 | **The docket has no safe single-author commit, and pathspec granularity is the whole cause — measured at 2 undisclosed sweeps in 36 filing commits, with a fourth instance observed live while measuring.** A pathspec commit carries whatever is in the WORKTREE for that path and cannot carry half a file, so when two agents have `docs/DOCKET.md` open the first to commit sweeps the other's in-flight rows into a commit whose message describes only its own work. Nothing is lost, append-only holds, and **attribution breaks permanently** because commit messages are immutable. Frame: `git log --follow -- docs/DOCKET.md` at `9daef4c9` — 64 commits touch the file, **36** file a new row, **80** rows filed, **14 rows across 7 commits** were filed without their message naming them. Those 14 are NOT 14 instances, and adjudicating rather than reporting them is the point: **9** predate the convention of citing IDs in messages at all (first is `281a6dc9`), **1** (D60 at `f5f7126e`) is described in full in prose, **1** was swept and correctly DISCLOSED (`c2ee8764`, the model to copy), and **2 are genuine undisclosed sweeps** — `a8d6a33a`, whose message says *"Seven docket entries"* while its diff files **nine** (D32–D40), and `d6fafcf3`, which names D77 and files D75–D77. Two prior measurements of mine were wrong and are recorded because the errors are reusable: passing the SHA AFTER `--` makes git read it as a pathspec and diff HEAD every time (that sweep reported 91%, reading one commit 64 times), and a docket row is ONE markdown line so EDITING a row emits a `+` line exactly like FILING one, inflating the total roughly threefold. | Docket contention audit, 2026-08-14, commissioned off three independent agent reports the same day | `docs/DOCKET_SHARDING_PROPOSAL.md` at `177b9d3c` — one file per row, table generated, so two agents filing at once touch two files and the contention stops existing rather than being managed. **GATED, not executed**, on the same "after, not during" reasoning as section F: its release condition 1 is `git diff --quiet -- docs/DOCKET.md`, which was FALSE for the entire time it was written. Rejected there with reasons: an allocator (does not touch the sweep problem, must perform the contended write to claim an ID, races with itself, skipped by `lab_check` as write-capable, and unnecessary once the guard reads the worktree) and a commit-time attribution gate (permanently red on 14 immutable historical rows — D3's unclearable-floor shape — and a workaround for a shape sharding deletes). Interim discipline, costing nothing, in §8 of that document: diff against HEAD before committing the docket, and if you are carrying another agent's rows, **say so by ID**. | fleet |
| D84 | **Three surfaces disclose the D63 mis-citation and all three misdescribe what D63 actually is — a correction that is itself wrong, in the record built to stop exactly that.** The known incident is real and already REPAIRED: the orphaned rule-B work cited `D63` while it sat uncommitted, the fleet allocated D63 elsewhere, and `scripts/self_audit.py:2547` and `sdk/tests/test_fault_message_matches_rule.py:33-34` now correctly cite **D77**. But the surviving disclosure sentences all say the number went to *"an unrelated **auto-stop** defect"* — at `scripts/self_audit.py:2549-2552`, `sdk/tests/test_fault_message_matches_rule.py:36-38`, and `docs/DOCKET.md` in D54's own closing note. **D63 is not an auto-stop defect.** Verified by `git log -S` on the row: D63 was filed once, at `6536dc0b`, and is *"A second W-2 identity survived — in the document that diagnosed W-2"*. The auto-stop rows are **D53** and **D65**. So the *"unrelated"* half is right and the *"auto-stop"* half is wrong, on two executable source files and one docket row. Found by an independent citation sweep of the tracked tree, which also measured the wider class: **0 dangling** citations and **7 live MIS-RESOLVING** ones, of which two are not listed in D43 — `S1_PRIORS_PREREGISTRATION.md:608` carries the same rotted `D8`-should-be-`D8b` pointer its sibling corrected, and `PROBABILITY_OF_RANK_SIX_ENTRY_2026-08-11.md:203` cites `D51` for a finding **no row was ever filed for**. | Docket citation sweep, 2026-08-14, `git grep` over 20,671 tracked files (never the shell's grep, which is `ugrep --ignore-files`) | Three one-word corrections, none of them mine to make: `self_audit.py` and `test_fault_message_matches_rule.py` are being edited by the agent triaging red checks, and D54's note belongs to its author under C3. **The generalisable half is that a mis-resolving citation is invisible to the guard built this round** — `scripts/docket_citation_guard.py` proves an ID EXISTS, not that it is the RIGHT one, and no mechanical check can close that gap, which is why D43's remedy stays allocation-and-citation discipline. | fleet |
| D85 | **The travelling ordinal repaired twice this week is defended by no instrument at all, and the marker that makes it honest to a reader is what blinds the guard to it.** `DESCRIPTION_DOCUMENT.md:54` states an entrant's placement with the board's entrant count and two retrieval timestamps on the sentence's face — a genuine improvement, and it is the third state of this one sentence in four days. But it is a **typed literal**: `git grep -n "DESCRIPTION_DOCUMENT" -- '*.py'` returns three hits, all comments in `scripts/self_audit.py` and probes in `sdk/tests/test_rank_claim_surfaces.py`, so **no generator writes that file** and the note's claim that the placement *"is parsed from"* the ranking record describes an aspiration, not a mechanism. Measured by mutation at HEAD `839f6355`, `__pycache__` purged, scoring pin as the name-to-rank referent and `campaign/BOARD_MOVED_2026-08-11.md` as the ranking referent: **M0** current text → rule-A **0**; **M1** delete the struck parenthetical, keep the correct ordinal → rule-A **1** (the guard faults the *correct* sentence, because the pin binds that entrant to its four-entry position); **M2** falsify the ordinal to a position the entrant does not hold, leave the struck parenthetical → rule-A **0** — **the guard cannot see it**; **M3** falsify *and* delete the marker → rule-A **1** (positive control). The cause is in the source and is not a bug: `board_placement_faults` skips a placement when another placement within `_PLACE_ADJUDICATED` (400 characters) binds the same entrant to the board's own rank, and a strike-and-keep marker **is** such a placement. **So strike-and-keep, which this lab requires for every correction, permanently exempts the corrected sentence from the one check that exists for it.** The second referent does not close the gap: `_ranking_board()`'s return is consumed in exactly one expression, `positions = len(ranking)`, so **no code path anywhere reads any entrant's live-board rank** — the guard's own frame says so (*"NOT re-pointed: the name-to-rank binding, deliberately"*). The next board move falsifies that sentence silently, exactly as the 2026-08-11T23:33Z move falsified its predecessor within hours of repair. | Ladder V rung V8 re-verification, 2026-08-14 (`campaign/LADDER_V_V8_REVERIFICATION_2026-08-14.md` §3), grading F1 for durability rather than correctness | Not a widening of the adjudication clause — that clause is correct and removing it would fault every strike-and-keep in the corpus. The two candidates worth pricing: **(a)** an *exemption ledger* — count adjudicated-and-skipped placements and print the count on the verdict line, so a reader can see how many sentences the guard declined to judge rather than reading 0 as coverage; **(b)** a rule that a placement adjudicated by a **struck** sibling must still agree with the **ranking** referent, which requires the live-board held-out set D55's gate says does not yet exist. Building that set is the prerequisite for either. **[CHIEF CORRECTION 2026-08-15, at repo `6c729fa2`. THE SHORTHAND I PROPAGATED FOR THIS ROW IS WRONG, AND IT IS WRONG IN THE DIRECTION THAT OVERSTATES THE BLIND SPOT.]** I briefed at least six agents, and reported to the owner, that `board_placement_faults` *"skips any sentence carrying a strike marker."* **It does not.** There is **no strike stripper** in the function or its caller — `git grep` for `~~`, `<del>`, or any strike handling in `scripts/self_audit.py` returns nothing, and the only mechanism is `_PLACE_ADJUDICATED = 400`, a **proximity window**. Executed by the docket-triage pass against a positive control: a wrong placement wrapped in `~~…~~` **and** one wrapped in `<del>…</del>` **both still fault**. **The true mechanism, and it is narrower:** a placement is skipped when another placement within 400 characters binds the same entrant to the board's rank. A strike-and-keep repair usually *produces* such a sibling — the corrected ordinal sits beside the struck one — so struck sentences are **often** exempted, but by proximity to a correct binding, not by their markup. A struck wrong placement with **no** corrected sibling nearby still faults. **This reconciles the two measurements rather than choosing between them:** the V8 grader's mutation (falsify the ordinal, keep the marker → 0 faults; delete the marker → 1 fault) is correct and consistent, because *its* marker text contained the correct placement — deleting the marker deleted the adjudicating sibling, not the strike. **Why the error matters beyond precision:** my version implied every struck sentence is unguarded, which would have justified treating a whole class of repairs as unverifiable; the true version says the exemption is created by the repair's own shape and is therefore avoidable by writing the corrected placement outside the window, or measurable by counting adjudications. Remedy (a) above — the exemption ledger — is unaffected and is still the cheap fix. **Declaring a blind spot the code does not have is the same defect as missing one, in the other direction**, and it is worse coming from the supervisor, because it travels in every brief. | self_audit owner / V16 |
| D86 | **The 2026-08-11 V8 round names a machine-record key as the source of truth for the travelling ordinal, and that key has since been superseded without the citation learning of it.** `campaign/LADDER_V_V8_REVERIFICATION_2026-08-11.md` §1, finding F1, cites `closure_challenge_round5_qcr.json` → `qcr_vs_rank2_duct_consistency` as *"the lab's own source record for this exact sentence"* and quotes its ordinal as the correct answer. That was true of the four-entry board the block is dated to, and it is **why the first repair of the travelling sentence was wrong within hours**: the repair was made *from* this record. At `63453645` the record gained a dated sibling placed above it, correctly leaving the superseded value byte-identical — but the citing report carries no pointer, so a reader following F1 today lands on the superseded value with nothing on the page to say so. The commit that created the sibling **anticipated exactly this** (it names both citing surfaces in its message and checks that the key was not renamed), which is why the key still resolves; what it could not do is edit another round's report. Corrections to the ladder's own reports do not reopen the ladder under the termination rule, so this is a record-hygiene row and not a rung failure. | Ladder V rung V8 re-verification, 2026-08-14, §4, following the citation chain the dispatch brief flagged | A one-line dated addendum at F1 in the 2026-08-11 document pointing at the dated sibling — **appended, not overwritten**, and by that document's owner or the chief, since a grader may not edit the round it is grading. The generalisable half: a citation to a JSON **key** is a pointer into an append-only record with no mechanism to notice a supersession, and `scripts/docket_citation_guard.py`'s limitation (it proves an ID exists, not that it is the right one — D84) is the same shape one level down. | chief / owner of the 2026-08-11 round |
| D87 | **A source artifact's stated range still disagrees with its own JSON, one full ladder round after it was reported.** `demo-output/website/closure_challenge_stability_physicality_audit.md:218–219` states the operator floor as *"0.1–0.5% of the gradient scale on PH"*; the per-case rows of its own `closure_challenge_divergence_audit.json` span **0.078%–0.74%** — wider at both ends. Reported as observation 3 of `campaign/LADDER_V_V8_REVERIFICATION_2026-08-11.md` §6 and unchanged at HEAD `839f6355`. **The graded text is not at fault and this is not a V8 failure**: the submission draft and the description document both quote the artifact faithfully, which is why the corresponding claim passed in both rounds. But the quotation is only as good as the artifact, and this is the one link in that chain nobody owns — a *correct* quotation of a *wrong* range travels into the package unmarked. Carried forward as a docket row rather than a third consecutive observation, because an observation that survives two rounds is not an observation. | Ladder V rung V8 re-verification, 2026-08-14, §2, re-executing the previous round's observations | Widen the range in the audit's §2 to what its JSON supports, or state the two figures as different measurements (whole-field vs per-case) if that is what they are — a decision by the audit's owner about what the sentence means, not a number swap. Then the quoting surfaces inherit the fix for free, since they quote rather than transcribe. | owner of `closure_challenge_stability_physicality_audit.md` |
| D88 | **The check that exists for the rank claim cannot see the arithmetic claim at all, so the blocking finding repaired today was never guard-detectable — and re-introducing it verbatim still returns a clean sweep.** The V8 blocking finding G1 was an inverted ratio: `overall_equivalent_S_bound` 0.002419121853891026 stated as *"covers 84% of the margin"* when against the live six-entry board's 0.0013658082957863568 it is **177%** (1.7712), a bound that **exceeds** the margin and, loaded adversely, puts our overall at 0.059047 against Yang's 0.058013. Measured by mutation on the repaired cover-email sentence at HEAD `26fca317`, `__pycache__` purged, `board_placement_faults` imported read-only, both referents (`_published_board()` = the four-entry scoring pin, `_ranking_board()` = the live six-entry record): **M0** as written -> rule A **0**, rule B **0**; **M1** falsify the margin by 10x (0.001365 -> 0.013650) -> **0/0**; **M2** append the withdrawn sentence verbatim, *"Our 0.002419 seed bound covers 84% of that margin"* -> **0/0** under both referents. **The guard has no arithmetic predicate whatsoever**: it binds ordinals to entrant names and nothing else, so a margin, a ratio and a percentage are all outside it by construction. This is not a defect in `board_placement_faults`, which never claimed the ground; it is a gap in what the corpus is checked for, and it is the reason a figure that flatters us lived in the cover email for four days after the travelling document withdrew it at `cc906ec9`. Repaired by re-derivation at three sites in `demo-output/website/CLOSURE_CHALLENGE_SUBMISSION_DRAFT.md` (§5.2, §5.3 item 9, §5.4) and one in `closure_challenge_submission_round5/DESCRIPTION_DOCUMENT.md`. | Repair of V8 finding G1, 2026-08-15, measured on the tree changed by that repair rather than on an earlier commit (D79) | A derived-figure check is a different instrument from a placement check and should be priced as one: the three inputs here are all machine records already on disk (`closure_challenge_seed_sensitivity.json` -> `overall_equivalent_S_bound`, `closure_challenge_round5_qcr.json` -> `round5_overall_full`, and the board scores that `sdk/scripts/probability_of_rank.py` parses), so the cheap form is a check that every surface quoting the bound-as-a-fraction-of-margin recomputes to the ratio those three imply, faulting a transcribed percentage that no longer divides. Note that this instrument would ALSO be blinded by a strike-and-keep marker unless it is written to read only the unstruck text, which D85's measurement says the placement check is not. | self_audit owner / V16 |
| D89 | **In the configuration that actually ships, no sentence about the entrant who holds first place can ever fault, because that entrant is not a name the check knows.** `board_placement_faults` takes its name-to-rank binding from `_published_board()`, the frozen four-entry scoring clone at `deb91557` (Reissmann, Wu, Liu, Montoya); D55's second referent `_ranking_board()` is consumed only as `positions = len(ranking)`. **Yang is not in the four-entry pin at all** — Yang joined at the 2026-08-11T23:33Z board move — and Yang is simultaneously the entry holding rank 1 on the live six-entry board and the entrant every current margin in the package is measured against. Measured by mutation at HEAD `26fca317`, `__pycache__` purged, both referents: the probe *"Yang is rank 4 on that board"* returns rule A **1** against the live referent (*'rank 4' is bound to Yang, whom the published board puts at rank 1*) and rule A **0** against the pin — **and the pin is what the shipped check passes as `board`.** So the guard's whole discriminating power on the live leader is available only in a configuration nothing runs. This is D48's pinned-snapshot finding and D55's deliberate non-re-pointing, met at the one entrant where the gap costs the most; it is recorded separately because the earlier rows price the general staleness and neither states that the leader is outside the name set entirely. Adding the marker reproduces D85 on this same probe: *"Yang is rank 4 on that board. (~~Yang, rank 1~~ struck.)"* returns **0** even against the live referent. | Mutation control run while repairing V8 finding G1, 2026-08-15 (`board_placement_faults` imported read-only; nothing in `scripts/self_audit.py` was modified) | Not a swap of `board` to the live record — that move was measured at `48d3f05a` and raises rule A from 2 to 68 on a disjoint set, 50 of them correct dated records, and D55's gate stands. The narrow candidate this row supports: extend the name set to the UNION of pin and live entrants while keeping each name's rank from whichever board the sentence's own stated retrieval date selects, which requires the dated-placement discriminator the check's BLIND-TO item 9 says it does not have. Until then, sentences about Yang are defended by re-derivation and review only, and any surface claiming guard coverage of them is overclaiming. | self_audit owner / V16 |
| D90 | **The placement guard's own frame line ships a fault count that was already false in the commit that wrote it, and a frame number that reproduces at no commit.** `scripts/self_audit.py:2728` (generated into the live verdict of `check_board_placement_words`) and its docstring twin at `:2461-2463` read *"re-measured at `48d3f05a`, and takes rule-A faults from **2 to 68** over DISJOINT sets: **both current faults clear**"* and *"over the same **1455** opened surfaces"*. Re-executed in three detached worktrees, `__pycache__` purged before each: at `48d3f05a` rule-A is **2** (the published figure reproduces exactly, which is this probe's positive control) but **1465** surfaces are opened, not 1455; at **`d2d6bd6c`, the commit that WROTE the sentence**, rule-A is **5** and 1476 opened; at `26fca317` it is **5** and 1479 opened. The three extra faults are `'sits at 6'`, `'rank 6'` and `'ranked 9th'` bound to Montoya in `sdk/tests/test_two_board_referents.py` — **a file added by `d2d6bd6c` itself**. So the measurement and the file that falsifies it were committed together. This is D79's shape at one commit's remove instead of five, and no rule catches it because the claim carries a perfectly good anchor pointing at a tree where it was true. It lands on the one function whose whole recorded history is literals going stale inside generated text: `_place_reach_sentence()` and `_place_precision_sentence()` are generated; this sentence is an f-string with 2, 68, 50, 18 and 1455 typed into it. **Consequence: the standing justification for NOT re-pointing the name-to-rank binding to the live six-entry board rests on a count that was wrong when published.** | Ladder V, V15 round 7, 2026-08-15, executed at `48d3f05a`, `d2d6bd6c` and `26fca317` | Generate the sentence the way the reach and precision sentences are generated, so it recomputes rather than being typed; or re-measure and re-anchor it, stating that the 5 includes the check's own held-out fixtures. Either way the "before" must be taken on the tree that ships the claim, not on the tree before the claim's own commit. | self_audit owner / V16 |
| D91 | **A repaired `.tex` ships beside an unrepaired `.pdf`, no check in this lab opens a PDF, and the only thing that ever checked this one is a rung report that now certifies a false state.** `demo-output/website/latex/closure_challenge_report.pdf` is tracked (`100644 ed88b507`). Extracted with `pdftotext`: *"P(rank 1) = 68%"* and *"2-100% at 95%"* at L17-18, *"rank 1 of 5, scored locally, 0.002878 below Reissmann, Fang and Sandberg's"* at L27, *"rank 1 of 5, scored locally; P 68% (2-100% at 95%)"* at L1520. `68%` appears 14 times; `Yang` **0**, `0.0580` **0**, `six-entry` **0**, `0-97` **0**; two not-decided pairs, not four. It is a complete four-entry-board artifact stating the exact form the V8 recomputation note of 2026-08-12 bans. Its source is fully repaired — `0--97\%` and `\textbf{six}-entry` appear 16 times and every probability `68\%` is inside `\sout{}` — and **the divergence was created inside this scope**: the `.tex` was rewritten at `2cec44ee` (2026-08-12 18:05Z), the PDF blob last committed at `98a39662` (2026-08-11 03:29Z), mtimes `2026-08-12 17:54` against `2026-08-11 03:26`. **Why nothing caught it:** `/usr/bin/grep -c '\.pdf' scripts/self_audit.py` returns **0**. The only instrument that ever opened it is `campaign/LADDER_V_V14_SURFACE_DISCOVERY.md:150-154`, which extracted all 68 tracked PDFs and wrote *"It is **CURRENT** ... both files share mtime `2026-08-10 16:22` ... **In sync.**"* A one-shot measurement written into a report cannot notice that its subject moved. There is also no build path: nothing in `git ls-files` rebuilds this artifact, though `pdflatex` is installed. **The transferable shape: A14 demanded a mechanical surface discovery and got one, and what it produced was a certificate rather than an instrument, so the surface class it discovered went dark the day the sweep ended and its green is still on the record.** | Ladder V, V15 round 7, 2026-08-15, at `26fca317`; frame `git ls-files` plus `pdftotext`, which cannot see figure text, chart labels or glyph runs | Two separable things. (a) Rebuild the PDF from the repaired source, or mark it superseded on its face, or untrack it — the owner's call, and it is not the auditor's to make on a compiled deliverable. (b) The general half, which is the point of this row: a live check that extracts every tracked PDF and applies the same rank-claim rules the `.md`/`.tex` surfaces get, so V14's finding stops being a dated sentence. | chief / V14 owner |
| D92 | **The 2026-08-14 rank repair fixed one page and left three four-entry sentences live on its sibling, one of them in the banned bare form.** `demo-output/website/closure.html`, unstruck prose at `26fca317`: `:389` *"0.0566 is the best overall number on the board — 0.0029 below the published leader"*; `:434` *"comparable to the 0.0029 margin over the published leader"*; `:525` *"holds no official rank — rank 1 of 5 is our local scoring at a pinned benchmark commit"*. **0.0029 is the four-entry margin over Reissmann**; the live margin over Yang is **0.001365**, which the same page states correctly and struck at `:145`. **`rank 1 of 5` is the four-entry ordinal**; the same page says `RANK 1 OF 7` twice. And `:525` states a placement with **no P, no interval and no board**, which the V8 amendment forbids outright. The identical *"comparable"* sentence on the sibling page was struck in the same repair pass: `benchmarks.html:156` reads `<s>...comparable size (0.0024)</s> — <b>struck 2026-08-14.</b>`. **This is D71's own shape recurring on the day D71 was filed.** It is not caught by `check_rank_claim_surfaces` because that check's companion test is per file, and two compliant paragraphs elsewhere in `closure.html` clear every claim in it. | Ladder V, V15 round 7, 2026-08-15, `/usr/bin/grep` over the tracked surface at `26fca317` | Strike-and-keep the three sentences with the board and its retrieval date on the face of each, as the sibling page already does. Separately, and larger: the per-file companion rule means one compliant paragraph launders a whole document, which is why this survived a green check — that rule needs to be per claim or the check needs to say plainly that green means "somewhere in this file". **EXECUTED AND CLOSED 2026-08-15, commits `5bec65f0` (the first two sentences) and `3010e12f` (the `rank 1 of 5` one, which is the same sentence D117(a) repaired).** All three are now strike-and-keep, carrying the six-entry board and its retrieval date on the face, at `demo-output/website/closure.html:395`, `:456` and `:554`. **THE ROW'S LARGER HALF WAS NOT ADOPTED and must not be read as adopted:** the guard is still per-FILE, not per-CLAIM, so one compliant paragraph still clears every claim in a file. What the row asked for downstream of that -- that the green say plainly it means "somewhere in this file" -- already existed before this row was filed, in `scripts/self_audit.py`'s companion note and in its BASIS entry, landed at `1611011b`. The row is closed on its three named instances and the per-claim redesign is not this row's. | closure surfaces owner / V10 |
| D93 | **`docs/PRODUCT_LIST.md`: the repair rewrote the paragraph and stopped one line short, so four superseded figures survive — including the lab's own copy of the rule, which now names the interval the rule forbids. And the cross-surface sweep's principled exclusion of this file is falsified by the repair having edited it.** The 08-14 repair rewrote `:59-68` to the six-entry figures. Immediately beneath, `:69-71` is the surviving tail of the sentence it replaced: *"Eight cases cannot resolve a rank probability better than **2-100% at 95%** (double bootstrap). The standing is **TWO CASES WIDE** ... **drop the hump and P = 91%**."* All three re-derived by running `sdk/scripts/probability_of_rank.py` (pure arithmetic over committed scores, no scoring call, ledger unchanged at 6): the interval is **0.2%-96.9%**; **three** deletions move us off point-rank 1, not two (`alpha_15_13929_4048` to rank 2, `alpha_15_13929_2024` to rank 3, `alpha_05_4071_4048` to rank 2); dropping `NASA_2DWMH` gives **P(rank 1) = 78.5%**, not 91%. The same file states *"a standing three deletions wide"* correctly at `:91`, and `:53` still reads *"ROUND 5, 0.056647, RANK 1 OF 5"* over a five-row board at `:55` carrying no Yang and no Tian. **The worst line is the rule itself**, `:75-76`: *"the figure ... may never appear without its interval **(2-100% at 95%)** and the not-decided pairs"* — the prohibition stating the forbidden value, in the copy other passes read. **And the frame that hides it was endorsed twice:** `docs/P33_CROSS_SURFACE_SWEEP.md` ruled this file out of scope as an *"append-only chronological log whose historical figures are quotations rather than claims"*, and D59 recorded that as *"a principled exclusion this row does not dispute"*. Executed, the exclusion is false of this region: `:53-76` is one live bullet that the 08-14 repair pass itself edited. A principled exclusion that the repair pass then writes into is not an exclusion. | Ladder V, V15 round 7, 2026-08-15, at `26fca317`, figures re-derived by executing `probability_of_rank.py` rather than transcribed | Finish the paragraph the repair started, strike-and-keep as elsewhere. Then re-scope P33: an append-only log is excludable only for the regions nobody edits, and the exclusion must be measured (does any commit in the window touch the excluded region?) rather than asserted from the file's genre. **HALF EXECUTED AND CLOSED 2026-08-15, commit `7a0419f7`** — the repair half, by a thread that is not this row's filer and not the 08-14 repair pass. Every figure the row names is now struck-and-kept with a dated correction: `:53`'s *"RANK 1 OF 5"* and its five-row board (which omitted the leader and one entrant between us and the entrants it listed) → rank 1 of 7 with all six entrants printed; the surviving tail's `2-100%` → **0–97% at 95%**, *"TWO CASES WIDE"* → **three deletions wide**, *"drop the hump and P = 91%"* → **78.5%**; and the rule itself, which had been mandating the value it forbids. Re-derived rather than transcribed from this row, out of `sdk/scripts/probability_of_rank_record.json` (`frame.entries` = 6, `frame.fetched` = 2026-08-11T23:33Z, `double95` = 0.0025–0.9690) — no solve, no scoring call, ledger unchanged at 6 — and every corrected claim now names its board by **entrant count and retrieval date** (six entries, 2026-08-11T23:33Z, re-verified 2026-08-14T21:01Z) rather than by a commit. **The one instance beyond this row's enumeration is repaired with it**: `:79`, one bullet below the region listed here, read *"AR_14 is now 3 of 5 and best-on-board is 4 of 8"* — both four-entry figures, live values **4 of 7** and **2 of 8** with **zero of 8** earned by our own model, both surviving rows being the organisers' own unmodified RANS field the decline gate passed through untouched. That is this row's own shape recurring one line further down, which is the finding worth more than the fix. **The P33 re-scope half is NOT done and this row's second sentence stays open under it** — see D141. **Not marked green:** the repairing thread is now an author of these surfaces, and under R-ISOLATE a non-author must measure them. *(Anchor note, and it is an instance of the hazard rather than a nuisance: this closure sentence is carried by commit `09234034`, whose subject is another thread's D133 repair, because `docs/DOCKET.md` was staged by that thread while this text sat uncommitted in the shared working tree — a pathspec isolates by file, not by author. The repair it certifies is at `7a0419f7`, which is this thread's and is where the surfaces actually changed; `a20a720b` carries D141–D142. Recorded rather than reverted, per the rule that a foreign row landing in a commit gets named and not undone.)* | PRODUCT_LIST owner / V10 |
| D94 | **The git-history purge decision rests on a solver-log figure wrong by a factor of about 23, produced by a suffix frame that misses the OpenFOAM naming convention.** `docs/MOVE_MAP_HISTORY_PURGE.md` concludes at `:14` that reaching 150 MB is *"structurally impossible"*, and its decomposition ranks solver logs **third at 33.6 MB across 379 blobs** (`:91`, `:140`, `:163`) with a KEEP recommendation. Measured at `26fca317` with `git ls-tree -r -l HEAD`: files matching the OpenFOAM convention `.../log.<solver>` number **778** and total **761.9 MiB** — larger than mesh files (374 unique blobs, 365.8 MiB) and therefore **the largest category in the repository**. The document's frame is the `*.log` **suffix**, which sees 441 files and 50.2 MiB; `log.pimpleFoam` has no `.log` suffix. The document also contradicts itself twice on its own page — its Category-3 table gives one row as *"~38 MB"* inside a category it totals at 33.6 MB, and `:17` reuses the same 33.6 MB figure for build artifacts and wheels — and it contradicts its sibling, `docs/PHASE2_STRUCTURE_PROPOSAL.md:133`, which says **778 logs**. Two documents in `docs/` disagree by a factor of two about the same objects, and the one with the wrong number is the one carrying the recommendation. **W-5:** the file carries **zero** git-resolvable commit anchors against present-tense state claims and a *"Tracked at HEAD: YES"* column. | Ladder V, V15 round 7, 2026-08-15, `git ls-tree -r -l HEAD`, both raw and blob-deduped | Recompute the decomposition on a frame that is stated (path convention, not suffix), re-rank the categories, and re-decide — the KEEP recommendation may still be right, but it is not currently supported by the numbers under it. Anchor the file. | infra / repo hygiene |
| D95 | **Three gates written after D78 exit 0 on their own findings, and one of them is held there by a test.** Executed at `26fca317`, true exit codes captured without a pipe: `demo-output/website/campaign/F7_runs/f7a_contract.py F7a_R1/res32y128_base` prints *"VERDICT: FAIL"* (max deviation 11.03% against a 5% tolerance) and exits **0**; `scripts/calibration_scorecard.py` prints *"satisfied : False"* and *"hit rate 3 of 9 (33%)"* and exits **0**; `scripts/installed_registry.py` reports 1 PENDING and 2 ABSENT and exits **0**. Two are worse than a missing check. (1) **`sdk/tests/test_f7a_contract.py:576` asserts `out.returncode == 0`** on the exact case whose verdict is FAIL, under a docstring reading *"the verdict is meant to be re-derivable by anyone with the repo"* — so repairing the exit code reddens the suite, and the fail-open is guarded in place rather than merely unguarded. (2) **`calibration_scorecard.py`'s only non-zero path is a construction identity**: `:481` returns 2 only when `reconciles` is false and `:315` defines `reconciles = len(on_disk) == len(admitted) + len(refused)`, while every iteration of `chief_engineer.agenda.read_inbox()`'s loop terminates in exactly one of those two lists — so the sum IS the file count and the live run prints `reconciles: 131 == 128 admitted + 3 refused` while reporting every finding it has. The instrument that grades this lab's forecasting cannot exit non-zero on a forecasting defect. **Counter-example, recorded because a row that names only failures is not a measurement:** `scripts/withdrawal_sweep.py` returns EXIT=1 on today's tree (2 unowned supersessions), downgrades a broken instrument to UNKNOWN(3) rather than swallowing it, and has no state in which it prints a defect and exits 0. | Ladder V, V15 round 7, 2026-08-15, true exit codes at `26fca317` | Each gate's exit code becomes a function of its finding, per D78(3). For `f7a_contract.py` the test must be corrected in the same change or the repair will read as a regression. For `calibration_scorecard.py` the exit condition must be the finding (`satisfied`), not the intake identity — and the intake check should stay, as a separate non-zero code, because it is a real check of a different thing. **EXECUTED AND CLOSED 2026-08-15, commit `f13729e0`, AND THE REPAIR DEVIATES FROM WHAT THIS ROW ASKED -- read the deviation before citing this closure.** Executed: `f7a_contract.py F7a_R1/res32y128_base` prints `VERDICT: FAIL` and exits **1**, against `EXIT_BY_VERDICT = {PASS: 0, FAIL: 1, UNGRADEABLE: 3}`, and `sdk/tests/test_f7a_contract.py` now asserts `returncode == 1` rather than 0. `calibration_scorecard.py` exits **2** on a real defect through a new `defects()` function. `installed_registry.py` exits 0 today, correctly -- only ABSENT and PENDING records exist -- and its `failures()` now covers DRIFT, DANGLING, GHOST and STALE. **THE DEVIATION:** this row asked that `satisfied` drive the scorecard's exit. It does not. The repair deliberately excludes the hit rate, argued in place as an L-84 alarm-versus-gate distinction, and exits on RECORD DEFECTS instead. A closure note saying "settled as filed" would be wrong; what is settled is that three instruments that could only print a complaint can now fail a runner. | gate owners / D78 |
| D96 | **The auto-stop test suite is satisfied by a comment, and the comment's text is the denial that a comment can satisfy it.** `sdk/tests/test_autostop_gate.py:40` guards the three activity clauses of `scripts/auto-stop.sh` with substring assertions over the raw file text, using `assertIn("pgrep -x", text)` for clause (1) — the solver and mesher protection that stops this box powering off mid-OpenFOAM-run. `scripts/auto-stop.sh:80-81` is a COMMENT reading *"(1) Solvers and meshers, matched on PROCESS NAME. pgrep -x on the name cannot be triggered by a path, a comment, or a grep that mentions the name."* Executed on a scratch `git archive HEAD` copy, control run first (`12 passed, 1 skipped`, identical to the repo): deleting lines 82-84, the entire `if pgrep -x '...simpleFoam...snappyHexMesh...'; then keep; fi` block, so that `/usr/bin/grep -c simpleFoam scripts/auto-stop.sh` returns **0**, gives **`12 passed, 1 skipped`** — unchanged. **The clause can be deleted wholesale with the suite green.** Clause (2) has no behavioural test at all: mutating its CPU threshold to `-gt 999999999`, so no worker can ever hold the box, also leaves 12 passed. **The shape, which is not the identity-as-control shape already on the record:** this is not a quantity derivable from its own inputs, it is a **test corpus that contains its own answer key** — the fixture is the documentation of the thing being detected. | Ladder V, V15 round 7, 2026-08-15, mutation on a scratch `git archive` copy; the repository file was never modified | Behavioural tests for clauses (1) and (2): plant a process and assert the gate holds, the way `test_autostop_session_glob.py` already plants transcript directories for clause (3) and does it well. Until then, delete the substring assertions rather than keep them — an assertion that cannot fail is worse than none, because it is counted. **EXECUTED AND CLOSED 2026-08-15, commit `fe36584e`.** The discriminating evidence is that the assertions now run over EXECUTABLE lines rather than file text: `sdk/tests/test_autostop_gate.py` carries a comment stripper whose docstring is "The lines of a shell script that EXECUTE -- comments dropped", and the substring assertions read `code`, not `text`. A comment claiming the clause would no longer satisfy them. Four behavioural tests exist and they test both directions, which is what this row asked for: a running solver holds the box, a command line that merely MENTIONS a solver does not, a worker burning CPU in the repo holds it, a parked process in the repo does not. **NOT DONE:** the tests exercise the TRACKED script; the installed copy is still divergent under D65, so a green suite here is not a statement about the box. | auto-stop owner / Katie, Sanaa per A4 |
| D97 | **`sdk/tests/test_exec_bits.py` is RED at HEAD, and the commit that declared it green is 47 commits back.** `0330d78c` (2026-08-14 21:30Z), in scope, is titled *"Register `f7a_contract.py` and `phase2_move_map.py` as waived; **HEAD green on test_exec_bits again**"*. Executed at `26fca317`: `1 failed, 14 passed`, `ThisRepositoryTests::test_no_shebang_script_is_both_unexecutable_and_unregistered`, naming *"new shebang-bearing tracked file(s) with no exec bit and no entry in `exec_bits.WAIVED_NO_EXEC_BIT` ... `scripts/docket_citation_guard.py`"*. **The offending file is the lab's own new docket citation guard**, landed at `e6057707` (23:32Z), two hours after the commit that declared the register green. Confirmed by a full-suite run at `26fca317`: **3 failed, 1669 passed, 230 subtests passed, 1007 s** — one failure is this, and the two `test_calibration_scorecard::IntakeIsReconciled` failures do not reproduce in isolation (18 passed) and are the concurrent-write artifact `d2d6bd6c` diagnosed, which this row confirms rather than disputes. | Ladder V, V15 round 7, 2026-08-15, full suite at `26fca317` | Give `scripts/docket_citation_guard.py` the exec bit its shebang implies, or register it. The larger question this row does not settle: the register has gone stale six times in three hours, so the ratchet is working and the cadence of new shebang files is faster than the cadence of registering them. | exec_bits owner |
| D98 | **A headline residual and its verdict word are literals in the print statement that publishes them.** `demo-output/website/dafoam/S1_zerocompute_2026-08-14/scripts/s1_zerocompute.py:295-296` reads `print("    ratio %.6f  cos %.6f  rms %.6f  -> agreement with the targets %.1e. G-P4: PASS." % (r_, c_, rms, 0.0))`. `r_`, `c_` and `rms` are computed from the arrays; **the `0.0` that becomes the published `0.0e+00` is a literal, and `G-P4: PASS.` is inside the format string**. That `0.0e+00` reached `docs/DOCKET.md` D67 and the `gate` field of a live proposal JSON. **The number is not wrong** — D67's own amendment defends it (*"the 0.0e+00 is exact against unrounded targets; the 1.2e-05 residual is the rounding of the published target"*) and the sibling script computes the true residual as 1.2e-05. **The defect is that the artifact offered as its reproduction cannot disconfirm it**: a gate whose PASS is spelled in the print statement is an identity presented as a control wearing a run's clothes, and the whole point of shipping the script beside the write-up was that a reader could re-derive rather than re-read. | Ladder V, V15 round 7, 2026-08-15, read at `26fca317`; the value itself was not disputed and reproduces via the sibling script | Compute the residual and the PASS/FAIL word from the same arrays the other three numbers come from, and re-run. If the printed value then differs from 0.0e+00, D67's amendment already says which is which and nothing in the conclusion moves — which is exactly why this is cheap to fix and worth fixing. | S1 zerocompute author |
| D99 | **The round's own correction mechanism produced a wrong correction, and the guard it built cannot detect the class it was built for, states the wrong number of its own controls, and evidences its central design choice with two string literals.** Four things, one root, all in the last ninety minutes of 2026-08-14. **(a)** D84's headline reads *"**Three** surfaces disclose the D63 mis-citation and **all three** misdescribe what D63 actually is"*. `git grep -n D63` over the tracked tree finds **four**, and the fourth is right: `scripts/self_audit.py:2549-2552`, `sdk/tests/test_fault_message_matches_rule.py:36-38` and D54's own closing note all say *"an unrelated **auto-stop** defect"* and are wrong; `scripts/docket_citation_guard.py:18` says *"an unrelated **S1 pre-registration** finding"* and is **correct** — and it landed at `e6057707` (23:32:20Z), **eight minutes before D84's own commit `839f6355`** (23:40:10Z), by the same round, in the file D84's settlement column names by path. The row filed to catch a wrong correction is wrong about how many corrections exist. **(b)** The guard's own first section (`:11`) says *"the defect is a citation that RESOLVES, not one that dangles"*, and it detects only the dangling kind; D84 says so, and D84's two live examples sit inside the guard's 164 counted citations and pass. Disclosed, not a defect — the three that follow are. **(c)** `:84` states *"**Four** cells run before the live sweep"*; `/usr/bin/grep -c 'cell("'` returns **6** and the live run prints **6** `[ok]` lines. NEG4 was added at `9daef4c9` and NEG5 at `9ddf0eeb` without updating the census — and `839f6355`'s commit message says *"six controls green"*, so the message got it right while the file it describes still says four, which is D83's class one level down. **(d)** `:238-240` prints `[info] NEG2  the HEAD-only frame would have flagged it: True -- which is why the frame is the worktree`. `strict = bool(unresolved(citations_in(neg2), alloc_c))` where `alloc_c = allocated_ids(_COMMITTED)`, and `:195-196` are `_COMMITTED = "...D1...D43...D44..."` and `_WORKTREE = _COMMITTED + "...D80..."`. `neg2` cites D80, so `strict` is True by construction of two adjacent string literals; it is appended with `lines.append`, not `cell()`, so it is never asserted. And the guard's only git calls are `ls-files` and `rev-parse --short HEAD` — **it never reads `git show HEAD:docs/DOCKET.md`** — so the load-bearing claim *"the frame is the working tree, not HEAD"* is evidenced by variable naming. **(e)** `sweep()` at `:294-297` is `except (OSError, ValueError): continue`: an unreadable tracked file is skipped and **still counted in the published `files swept`**, so the reach number overcounts by exactly the files it failed to open, with no note. | Ladder V, V15 round 7, 2026-08-15, `git grep` over the tracked tree and direct execution at `26fca317` | (a) is a one-word census correction in D84, its author's under C3. (c) is a two-word fix and a test that the census recomputes from `grep -c 'cell("'` rather than being typed — the same fix D90 needs. (d) either drop the `[info]` line or make the committed frame actually come from `git show HEAD:docs/DOCKET.md`, which would also make the claim testable. (e) count skipped files separately and name them, as `check_board_placement_words` already does for its own raising surfaces. | docket guard author / fleet |
| D100 | **Three leads this round could not pin, recorded so they are not lost and not counted.** (1) **An audit gap of 21 commits.** V15 round 6's declared subject ended at `2266c4e3` (2026-08-11 ~21:30Z); this round's base is `88e915e4` (23:48Z), the last commit of that day. The 21 commits between them — including `f6b4eec0`, `89c2a467`, `88e915e4` and the V16 round 8/9/10 work — are inside no V15 round's declared scope. (2) **A citation that rotted in that gap.** `demo-output/website/campaign/V16_PRECISION_SET.py:86` cites `test_the_published_precision_figure_recomputes_from_its_sentences` as the test that reddens if its figure drifts. No such `def` exists; the test was renamed to `test_both_published_precision_figures_recompute_from_their_sentences` at `5e4a0cc5` (2026-08-11 22:06Z), and the citation was written at `64281b6b`. The sentence asserting that a test protects the figure names a test that does not exist. (3) **Two probe findings the grader could not verify to its own standard and therefore excluded from this round's count of twenty**: that `scripts/withdrawal_sweep.py`'s published `SESSION_ONLY: 0` and `DIFFUSE: 0` are false zeros because `:229-237` searches `ROLE_RE` before `SESSION_RE` over the whole block, so one incidental role word launders a session-attributed withdrawal into OWNED; and a claimed miscount in D59's *"nine instances"*, which the grader measured at 6 under a strict `P(rank 1)` reading and could not disambiguate against the union reading, so **D59 is not disputed here**. **Separately and verified:** `scripts/withdrawal_sweep.py:38` documents the WIDER set as *"REPORTED only"* while `:485` feeds `WIDER_MARKERS` into the verdict and `--wider` returns EXIT=1; the file's own usage comment at `:146` says *"gate on all five"*, which is what the code does. | Ladder V, V15 round 7, 2026-08-15 | (1) is a scope question for whoever owns the V15 cadence: either a round covers the gap or the record states that it is uncovered. (2) is a one-name correction. (3) needs someone other than this grader to execute the two withdrawal_sweep zeros with a plant that carries both word classes in one block. The `--wider` contradiction is a docstring fix. | fleet |
| D101 | **A check can switch off the runner's verdict by writing one line to its own stderr, and the shipped hook maps that outcome to a passing push. The exit contract binding the two is untested.** `scripts/lab_check.py:690` sets `crashed = "Traceback (most recent call last)" in err` and `:710` downgrades the verdict to UNKNOWN when `crashed and code != 0` — where `err` is **the check's own stderr**, which the check fully controls. `scripts/installed/pre-push:61-68`, the runner's only shipped consumer and added in this same scope, maps UNKNOWN to `rc=0` (*"NOT blocking, and that is on purpose"*) and every out-of-contract code to `rc=0` (*"Not blocking; nothing was checked"*), so **the only blocking exit code is 1**. Demonstrated end to end in a scratch git repo, never in this tree, with one gate whose exit code is a genuine function of its finding, run twice with the same finding on stdout and the same exit code 1, differing only by one stderr line: plain gives `[FAIL]`, `VERDICT: FAIL`, true exit **1**, push blocked; with `sys.stderr.write("Traceback (most recent call last)\n")` it gives `[UNKNOWN]`, `VERDICT: UNKNOWN`, true exit **3**, push not blocked. The finding text is still on stdout in the second run: the runner read it, printed it, and did not act on it. The same escape exists through `usage_error` at `:703` (`code == 2 and not out.strip() and err.strip()`) even though `EXIT_CONTRACT` maps 2 to FAIL. **And the numeric contract is untested:** copying `scripts/lab_check.py` and `sdk/tests/test_lab_check.py` into a scratch tree and mutating one line, `EXIT = {PASS: 0, FAIL: 1, UNKNOWN: 3}` to `{PASS: 0, FAIL: 77, UNKNOWN: 99}`, gives **25 passed for control and mutant alike** with `__pycache__` purged before both — every whole-runner test asserts `rc == lc.EXIT[lc.FAIL]`, comparing the subprocess's exit code against the module-under-test's own dictionary, while `pre-push` hardcodes `case "$rc" in 0) 1) 3) *)` and would route every FAIL into `*)`. **This is one level above D78**: not a detector that switches itself off, but a runner whose "did this count?" predicate is a substring of the graded party's own output. **Secondary, same file:** `CHECK_DIRS = ("scripts", "sdk/tests")` at `:245` is exactly the typed list the module's own docstring condemns, and `sdk/scripts/` holds 73 tracked `.py` including real gates (`citation_tier_audit.py`, `closure_in_sample_gate.py`, `dow_2011_table42_check.py`, `pope_1975_basis_check.py`, `closure_round5_points_order_check.py`) that are not run, not skipped, not in the 60 not-admitted, and in no coverage statement. And `[PASS] scripts/installed_registry.py` prints in the live run directly above three bullets saying the lab's own apparatus is PENDING and ABSENT, because sub-verdicts are scraped for display and never reach `aggregate()`. | Ladder V, V15 round 7, 2026-08-15, demonstrated in a scratch git repo at `26fca317`; the repository was never modified | The downgrade must not be decided by the graded party: distinguish a crash by the process's own signal or by an exception the RUNNER raised, not by a substring of the check's stderr, or keep the label and stop letting it change the exit code. `pre-push` must block on UNKNOWN or say in the same breath that it does not gate. The exit contract needs one test that pins 0/1/3 against the hook's `case` arms rather than against `EXIT`. `CHECK_DIRS` needs either to be derived or to be named in the frame as a typed list with what it omits. **PROGRESS NOTE 2026-08-15 (NOT CLOSED).** The primary defect is fixed at commit `8184dbc5`: `scripts/lab_check.py`'s `crashed` predicate is now `"Traceback..." in err and not reported`, a new `EXIT_UNSOUND = 4` separates UNKNOWN-about-output from UNKNOWN-about-reach, and `scripts/installed/pre-push` blocks on 4 and on `*`, so the escape leads nowhere better. `sdk/tests/test_lab_check.py` runs THE REAL HOOK against a stub runner, which is the test this row asked for. **THE ROW STAYS OPEN on its own secondary:** `CHECK_DIRS = ("scripts", "sdk/tests")` is unchanged and `git grep 'sdk/scripts' -- scripts/lab_check.py` returns nothing, so the 69 `sdk/scripts/*.py` are still outside the frame AND unnamed in it. Closing this row now would delete that residual. | lab_check author / D64 |
| D102 | **Two of the five 2026-08-14 audit documents publish headline counts that reproduce at no commit, one of them presenting hand-composed text as command output — and the underlying mechanism is that `git log --since` is not stable on this repository.** (a) `campaign/WEEKLY_METRICS_2026-08-14.md:9-19` is a fenced block captioned `git log --since='2026-08-07' --until='2026-08-15' --pretty=format:'%cd' --date=format:'%m-%d' &#124; sort &#124; uniq -c` containing the rows `0 08-09` and `0 08-13`. **`uniq -c` cannot emit a zero count**: a bucket with no occurrences produces no line. The block is hand-composed and captioned as machine output. Its headlines `672 commits` (`:22`, `:37`, `:96`) and `5,678 unique files` (`:47`, `:49`, `:96`) reproduce at no commit in the window; measured at `650c1e04`, `git log --since=… &#124; wc -l` gives **785** and an explicit committer-date filter over a full `git log` gives **783**. An independent probe observed **638** and **709** for the *identical pinned invocation inside one session*, git 2.43's `--since` traversal cutoff silently dropping an entire 71-commit day; `638 + 34 = 672`, and `34` is the hand-inserted `08-07` row whose true value is 71. **Any recurring metric in this lab built on `--since` is measuring the traversal cutoff as much as the corpus.** (b) `campaign/AUDIT_AGED_FAIL_SWEEP_2026-08-14.md:17,20` states *"**10 FAILs** and **8 WARNs**"* with **no commit anchor anywhere in the file**. Re-executed with the unmodified `self_audit.py` from three trees: `45922a65` gives **FAIL 10 / WARN 9** with the ten names reproducing exactly; **`2c74a891`, the document's own correction commit 24 minutes later**, gives **FAIL 8 / WARN 10**, because the two D56 bundle rebuilds cleared exactly the two checks the document's own correction section discusses; `26fca317` gives **FAIL 3**. D56 records *"9 FAIL, 9 WARN"* for the same sweep. **Three counts for one 24-minute window, on three surfaces, none anchored.** | Ladder V, V15 round 7, 2026-08-15, re-executed at `45922a65`, `2c74a891` and `26fca317` | Replace every `--since` window in a recurring metric with an explicit committer-date filter over a full `git log`, which reproduces; re-derive the two headlines and anchor them; anchor `AUDIT_AGED_FAIL_SWEEP` and reconcile it against D56, choosing one number rather than merging three. The `uniq -c` block should either be re-run and pasted, or written as prose that does not claim to be output. | metrics author / fleet |
| D103 | **A dead-lever round's load-bearing negative claim is falsified by one file, its published zero is an identity, and a stale board literal survives beneath the comment warning about that exact regression.** (a) `campaign/DEAD_LEVER_AUDIT_ROUND5_2026-08-14.md:364` states *"`git grep` over the tracked frame finds no record naming a fleet-wide found-dead D3 or D5"*, and `:370` builds *"one third of it is unfalsifiable"* on it. `docs/PRODUCT_LIST.md:1822-1826` is titled **"D3–D5, three more reopened"** and describes all three in enough detail to re-verify (the motorBike mesh at skewness 8.94 against a hard 4.0; the FPE-banner rule false for 5 of 149 DAFoam logs; one cited source location that is `NotImplemented` dead code), landed at `093a4b37` on 2026-08-10, **a day before** the record §6 calls pending. **Wrong when written**, and D60's remedy is built on the absence. What is actually true is narrower: the consolidation was never folded back into `INSTRUMENT_INTEGRITY_2026-08-11.md`. (b) The same document's `:237` publishes *"rows carrying `metrics.mesh_refinement`: 0 of 52"*. Parsed over all 208,194 ledger rows, `mesh_refinement` occurs **69,288 times and every occurrence is under `design`, never under `metrics`, for any solver** — so the zero is derivable by construction and would be zero for any selection. Its stated positive control at `:240-244` demonstrates that `design.mesh_refinement` is findable, **a different key from the one being counted**: an identity presented as a control, with the control attached to the wrong noun. The §4 verdict may still be right on other grounds; the stated evidence is vacuous. **Recorded in fairness, because this row names only the failures of a document whose central measurement is the best-verified thing in this round:** §3.3's four archived logs reproduce bit-for-bit (iteration-0 KSP residual `1.243721539281e+01` identical across all four, 86/86/79/79 iterations, exactly one substantive line in an 830-line diff), and the refutation of D40 stands. (c) Separately, `sdk/scripts/build_benchmarks.py:99-100` still hardcodes `"target_rank": 4, "target_overall": 0.0779` — Montoya's overall and Montoya's rank on the **four-entry** board, where the live six-entry board puts them at **6**. It propagates into `demo-output/website/benchmarks.json` and `demo-output/website/wall/wall.json`, both machine-read travelling surfaces, and `sdk/chief_engineer/lab_stats.py:351` uses `4` as its fallback default. `docs/P33_CROSS_SURFACE_SWEEP.md:48` flagged it on 2026-08-12 and it is unrepaired at `650c1e04`. **The comment immediately above it reads *"That regression happened once: the round-5 session hand-updated benchmarks.json and wall.json but left this literal at round 3; Ladder V rung V7 caught and fixed it on 2026-08-08."*** The same literal, in the same generator, has now gone stale twice — once on a round change and once on a board move — under the comment warning that it went stale on a round change. | Ladder V, V15 round 7, 2026-08-15, at `650c1e04`; ledger parsed as JSON over all 208,194 rows, not by substring | (a) is a two-sentence correction in §6 and a re-look at D60, whose remedy rests on the absence. (b) either count the key that exists (`design.mesh_refinement`) or state that the counted key exists nowhere in the ledger and say what that means; the control must exercise the same key as the numerator. (c) derive `target_rank` and `target_overall` from the live board record the way `_derive_closure_facts` already derives the interval, rather than typing them — a literal that has gone stale twice under a comment about going stale is not going to be fixed by a third correction. | dead-lever author / benchmarks generator owner |
| D104 | **The submission draft prints a seventeen-digit margin whose basis supports six, in the sentence that calls it re-derived at full precision — and it is the same defect the same file indicts a hundred lines earlier.** `demo-output/website/CLOSURE_CHALLENGE_SUBMISSION_DRAFT.md:738` and `:951` both state our margin over Yang as **0.0013658082957863568**. Measured, not reasoned: that digit string is *exactly* `0.058013 - 0.056647191704213645` in IEEE double, and **0.058013 is the SIX-DECIMAL printing of Yang** that `campaign/BOARD_RESCORE_2026-08-14.md` §3.1 publishes — a table that says so on its own face (*"shown to 6 dp; the board publishes them to 4 dp"*). The machine record holds a different number: `sdk/scripts/probability_of_rank_record.json` -> `pairwise.Yang.margin` = **-0.0013653082957863563**, taken from Yang's full mean of eight published per-case values, **0.0580125**, which `sdk/scripts/probability_of_rank.py`'s `LIVE_BOARD` carries. So there are **three margins in circulation on three bases**, all defensible: 0.0013528082957863577 against the published 4-dp 0.0580, 0.0013653082957863563 against the full mean, and 0.0013658082957863568 against the 6-dp print. **The conclusion is untouched — all three give 177%** — and every rounded quotation of `0.001365` in the package is correct on the full-mean basis; the defect is narrow and is exactly this: **the eleven digits after the sixth are not measurements of anything**, and the sentence carrying them reads *"Re-derived at full precision from the lab's own committed sources"*. It is the same class the draft itself indicts at `:613-618` about `0.0595338` — *"a transcription slip in the seventh decimal: it does not subtract to the margin printed beside it"* — 5e-7 here against 3e-7 there, in the same document. **The figure has also propagated into dispatch briefs**, which is how it reached this row's author as a given. | `scripts/check_derived_figures.py` R1, first run over the tracked corpus at HEAD `edbaa0fe`; the three bases derived from `LIVE_BOARD` by AST literal and from the two JSON records, `__pycache__` purged | **NOT repaired here, deliberately.** Which basis the sentence means is a decision about what it says, not a number swap — D87's precedent exactly — the package is PARKED and reserved to Katie, and a V15 round-7 grader was working the tree at the time. The narrow repair is to quote the margin at the precision its basis supports (`0.001365`, or `0.0013653082957863563` if the full-mean basis is meant) and to name the basis on the sentence's face, since three exist and no document states which it used. **EXECUTED AND CLOSED 2026-08-15, commit `5bec65f0`.** `demo-output/website/CLOSURE_CHALLENGE_SUBMISSION_DRAFT.md:752` and `:987` now read `sits **0.001365** below it. ~~sits **0.0013658082957863568**~~`, strike-and-keep, and the basis is named on the face at `:766-776` -- both the record's `pairwise.Yang.margin` (0.0013653082957863563) and the six-decimal basis, with "177% on every basis". The discriminating check is a sweep for an UNSTRUCK 17-digit instance: none remains in the draft. **WHAT WAS NOT DONE:** the row's "PARKED and reserved to Katie" caveat has been overtaken rather than answered -- the narrow precision repair the row itself proposed was executed without touching what the package CLAIMS, so no scoring call was made and none is implied. See D120, which is refuted, and D127, whose successor question measured the interval as [172%, 184%]. | owner of `CLOSURE_CHALLENGE_SUBMISSION_DRAFT.md` |
| D105 | **The V13 closeout instructs a future agent to hand over the withdrawn 84% figure, and it is the fifth copy of a sentence four separate repair passes have now missed.** `demo-output/website/campaign/LADDER_V_V13_CLOSEOUT.md:216` reads *"The seed bound covers **84%** of the margin"*, unstruck, with no denominator on the sentence and no date on the bullet. It is **not a dated record of a measurement**: it sits in the bullet list under §W2, whose instruction to the reader is *"Agree, and hand over the number that proves you already knew"* — so it is a **standing instruction about what to say to a challenger**, the same shape as the §5.3 item 9 rule D88 records as having *"mandated that every document quote a figure whose direction of argument had already reversed"*. Against the live six-entry board the bound is **177%** of the margin and the argument reverses: a bound covering most of a margin qualifies a lead that survives it, and this one does not. The section's own heading still carries the four-entry framing unstruck (*"Your margin is 0.0029 ... your own measured seed uncertainty ... is 0.0024"*). **D71 repaired this sentence at `DESCRIPTION_DOCUMENT.md` §8, `closure.html` and `benchmarks.html`; D88 repaired three further sites in the submission draft. Neither pass reached this one**, and neither could have: the placement guard has no arithmetic predicate (D88) and no sweep for this sentence existed until now. | `scripts/check_derived_figures.py` R1 and R3 agreeing on one sentence, first run over the tracked corpus at HEAD `edbaa0fe` | Strike and keep, in the corpus's own idiom, with the six-entry board and its `2026-08-11T23:33Z` retrieval on the face of the replacement — and check the heading of §W2 in the same edit, since it carries the same four-entry framing one line up. The generalisable half is already this check's job: the sweep now exists, so the next repair of this sentence is a re-run and not a re-search. | owner of `LADDER_V_V13_CLOSEOUT.md` |
| D106 | **P(rank 1) was measured, priced and deliberately NOT shipped in the new derived-figure check, because nothing in the lab can tell a stale assertion from a correct dated record.** With `p_rank1` in the registry, `scripts/check_derived_figures.py` returns **13 further disagreements** over the tracked corpus. The record already holds **twelve legitimate values** — as scored **50.1522500%**, the three seed legs (adverse 34.2295%, as-scored 50.1523%, favourable 65.401%) and the eight leave-one-out drops (31.699% to 78.511%), all in `sdk/scripts/probability_of_rank_record.json` — and the corpus carries a **thirteenth, the four-entry 68%**, which was true until the board move of 2026-08-11T23:33Z. Two guard idioms cut the 13 down but do not close it: a **reporting verb** before the figure (*"all four now carry P(rank 1) = 68%"*) and the **correct value quoted beside it**; both are shipped and both are mutation-proved. What remains is a genuine split — most of the residue sits in dated ladder rounds and closeouts where 68% is true of the board they are dated to, but a minority states it as a **standing rule** (`CLOSURE_FAMILY_SUPERVISION_GUIDELINES.md:104` and `:127`, *"carries P(rank 1) = 68% with its interval"*), which is a live defect. **The missing instrument is the dated-context discriminator** that `scripts/self_audit.py`'s own BLIND-TO item 9 says it does not have, that D85's remedy (b) needs, and that D89's narrow candidate needs. It is the same prerequisite three times over. Not shipped rather than shipped noisy: this lab already carries one prose instrument at a measured **74%** false-positive rate, and the cost of that one is that nobody acts on it. The omission is printed on every run of the check under `MEASURED, PRICED, AND NOT SHIPPED`, because an omission a reader cannot see is a silent skip. | Building `scripts/check_derived_figures.py`, 2026-08-15, at HEAD `edbaa0fe`; the 13 counted with the quantity enabled and the corpus swept, `__pycache__` purged | Build the dated-context discriminator once and three rows collect: a claim is a DATED RECORD when its own block states the referent it was measured against (a retrieval timestamp, a board identity, a commit) and a LIVE ASSERTION otherwise. The corpus already has the convention — 33 `fetched YYYY-MM-DD`, 16 `retrieved`, and the two board retrievals `2026-08-11T23:33Z` / `2026-08-14T21:01Z` — so the discriminator is a reader of an existing idiom, not a new one to impose. Until it exists, P(rank 1) claims are defended by re-derivation and review only. | self_audit owner / V16 |
| D107 | **The re-score publishes a seven-way rank distribution of which six figures have no machine record at all, in a document whose own frame says no stored output was cited.** `demo-output/website/campaign/BOARD_RESCORE_2026-08-14.md:115` publishes the share table **50.15% / 19.00% / 14.65% / 10.56% / 4.68% / 0.77% / 0.18%** over ranks 1 to 7. `sdk/scripts/probability_of_rank_record.json` — the committed `--json` output of `sdk/scripts/probability_of_rank.py`, which is what `§2` of that document names as the authoritative recomputation — holds exactly two of those: `p_rank1` = 0.5015225 and `point_rank` = 1. Its ten top-level keys are `double68`, `double95`, `frame`, `leader`, `loo`, `mc_wilson95`, `p_rank1`, `pairwise`, `point_rank`, `seed`; **the script prints the distribution and does not persist it.** So six published figures are typed prose with nothing behind them, and the only way to check them is to re-run a 400,000-draw bootstrap — which is why the derived-figure check grades `p_rank1` against a record and cannot reach the other six in either direction. This is not an accusation that they are wrong; it is that they are **unfalsifiable from the tree**, which is the property D55's gate exists to prevent. | Enumerating the source records for `scripts/check_derived_figures.py`, 2026-08-15, at HEAD `edbaa0fe`; keys read with `json.load`, not by substring | One line in `probability_of_rank.py`'s `--json` writer: persist the full rank-share vector it already computes, under a key beside `p_rank1`. The figures then become checkable by the instrument that already reads that file, at no new compute — the bootstrap is already run, only its output is discarded. | owner of `sdk/scripts/probability_of_rank.py` |
| D108 | **Two gates are ADMITTED by the runner and cannot be invoked by it, and under the repaired exit contract that is now a blocking state rather than a harmless one.** `scripts/sweep.py` and `scripts/detect_overwrite_signature.py` both pass `lab_check.py`'s three admission predicates — their argparse positionals are `nargs="?"` and `nargs="*"`, so the static `requires-arguments` predicate is CORRECT to admit them — and both then refuse at runtime with exit 2, empty stdout and a sentence on stderr, because each requires its frame to be named (`sweep.py` a pattern and `--frame`; `detect_overwrite_signature.py` a root or `--self-test`). Measured at `edbaa0fe`, `--no-tests`: 13 checks ran, and these are 2 of them, both `[UNKNOWN]`. Until 2026-08-15 that read as a harmless "statement about the runner, not about the lab" and exited 3, which the pre-push hook did not block on. **It is now blocking (exit 4), and the reason is that the diagnosis is written entirely by the graded check** — `code`, `out` and `err` are all its own — so a check that would have exited 1 could instead exit 2 with an empty stdout and buy a non-blocking code. There is no signal in this path the check does not control, so the hole cannot be closed by a better predicate, only by making the escape lead nowhere better. **The consequence, stated rather than discovered later: while these two remain admitted-and-unrunnable, `lab_check.py` cannot return a non-blocking verdict on this tree even if every FAIL is repaired.** Today that is invisible because the aggregate is FAIL on four other checks; it becomes the whole story the day those clear. | V15 round 7 repair pass, 2026-08-15, at `edbaa0fe`; admission predicates read from the AST, exit codes and stdout/stderr observed by running each script with no arguments | Three options, and the choice is a coverage decision rather than a code one: (a) give each a schedulable default frame, so the runner gets a real verdict; (b) make the argument genuinely REQUIRED in argparse, so the static predicate skips them before launch and they land in the coverage statement where an unrunnable check belongs; (c) have them exit 3 deliberately, which is honest but still blocks and therefore only relabels. (b) is the cheapest and is what the coverage statement is for. **REPAIRED 2026-08-15 at `6ff9fb43`, by (b), with one correction to this row's premise.** Neither script is argument-free even today: each has a legitimate positional-free MODE (`sweep.py --list-frames`, `detect_overwrite_signature.py --self-test`), and `nargs` was carrying that mode rather than describing the sweep, so making the positional required outright would have deleted a working capability. The mode is now answered ahead of the parser by a small reader of the same command line argparse reads (option scanning stops at `--`, prefix abbreviations honoured), and the positional is required in argparse's own terms: `pattern` with no `nargs`, `roots` with `nargs='+'`. The runner's exit contract is untouched; what moved is which category these two are in, and they moved by becoming honestly un-runnable without arguments. Caller sweep before the signature change, `git grep` over the tracked tree: `scripts/check_absolutes.py:163` loads `sweep.py` AS A MODULE by path (unaffected); `sdk/tests/test_sweep_frames.py` runs the CLI in three subprocesses, each passing a positional first; `sweep.py`'s own docstring documents three invocations, all still working, including the flag-before-positional form and `python3 -m scripts.sweep --list-frames`; `scripts/lab_check.py` is the no-argument caller this row is about; `detect_overwrite_signature.py` has NO executable caller besides `lab_check.py`; and no cron entry or shell script invokes either (`scripts/installed/*.cron`, `crontab.root`, `crontab.ubuntu`, `lab.sh` checked). Measured after at `6ff9fb43`, the `--only` runs re-confirmed at `9cbaeec6`: `--list --only sweep.py` and `--list --only detect_overwrite` both report **SKIPPED `requires-arguments`**; `--no-tests --only sweep.py` returns **UNKNOWN, exit 3, NOT blocking**, the skip printed in the coverage statement where an un-runnable check belongs; and the whole-tree `--no-tests` run at `6ff9fb43` returns **FAIL, exit 1**, 12 checks run, 62 not admitted, with **NO blocking UNKNOWN of any kind** and both of these two sitting in the coverage statement under `requires-arguments` alongside `check_convergence.py` and `memwatch.py`. **This row's stated consequence is discharged**: the blockers left in that run are exactly the five genuine FAILs the row said this defect was hiding behind (`calibration_scorecard.py`, `check_absolutes.py`, `contention_audit.py`, `docket_citation_guard.py`, `self_audit.py`), so the day those clear the verdict is UNKNOWN-about-reach and exit 3 rather than a blocking 4. Follow-up at `9cbaeec6`: the comment written here to explain the repair itself carried two unbacked absolutes, `check_absolutes.py` counted them (19 -> 21 on that file), and they were reworded back to 19. **EXECUTED AND CLOSED 2026-08-15, commit `6ff9fb43`.** Verified from the argument parsers themselves rather than from the runner's output, because the runner's output is what the row said was misleading: `scripts/sweep.py` declares `p.add_argument("pattern", ...)` with NO `nargs`, and `scripts/detect_overwrite_signature.py` declares `ap.add_argument('roots', nargs='+', ...)`. Both therefore genuinely require arguments. Live confirmation: `python3 scripts/lab_check.py --list --only sweep.py` prints `SKIPPED, BY REASON ... 1 requires-arguments / scripts/sweep.py`, so the skip is now reported with its reason rather than counted as a pass. | lab_check author / D64 |
| D109 | **A substring assertion against the source of a heavily commented file is a general trap, and `test_autostop_gate.py` was not the only place it appears.** V15 round 7 F7 recorded that `assertIn("pgrep -x", text)` was satisfied by the comment at `scripts/auto-stop.sh:80` whose own words are *"pgrep -x on the name cannot be triggered by a path, a comment, or a grep that mentions the name"* — a detector whose fixture is the documentation of the thing it detects. That instance is repaired (live-code-only text assertions plus four behavioural tests, mutation-proved). **Swept for siblings, and the frame matters:** `git grep` over the 85 tracked files under `sdk/tests/` (never the shell's `grep`, which is `ugrep --ignore-files`), for tests that read a SCRIPT'S OWN SOURCE and assert a substring against it. Most `assertIn(..., text)` hits in that directory are against GENERATED OUTPUT — a certificate, a report, a log — and are not this shape. The residue is small and is named here rather than left to the next round: (a) `sdk/tests/test_autostop_session_glob.py:279`, `assertIn("clause (3)", SCRIPT.read_text())` guarding *"the control-room clause is not where this file thinks it is"* — satisfied today by live `say` strings at `auto-stop.sh:166` and `:176`, so it is not currently false, but nothing in the assertion requires that and the file it reads is 60% comment; (b) `sdk/tests/test_fail_open_scan.py:104`, `assertIn("could not be swept", source)` over `self_audit.py`, used as the precondition of a control. **Deliberately NOT counted as instances:** `test_rank_claim_surfaces.py:683` and `:2115` assert on comment text ON PURPOSE — L-76 requires a falsified claim to be RECORDED rather than deleted, and those tests strip `#` themselves and check for a falsification marker beside each claim. A rule that flagged them would be wrong. | V15 round 7 repair pass, 2026-08-15, at `edbaa0fe`; frame is `git grep` over `git ls-files sdk/tests` (85 files), classified by hand rather than by pattern because "output" and "source" are not lexically distinguishable | The repair for (a) is the one already applied next door and it is cheap: strip whole-line comments before the substring assertion, and put the load on the behavioural tests that file already has. The generalisable half is a rule rather than a patch — **a text assertion over a source file is a tripwire, never the guard; the guard is running the thing** — and it is worth stating in the test-writing conventions rather than discovered a third time. | test authors / fleet |
| D110 | **Correction to V15 round 7's F6: `scripts/installed_registry.py` was listed among three gates that "exit 0 on their own findings", and re-executed that part does not hold as stated — the real hole was narrower and is repaired.** F6 cites the registry's live run as *"1 PENDING, 2 ABSENT"* with exit 0. Executed at `edbaa0fe`: `main()` ended `return 1 if any(f.is_failure for f in findings) else 0` and `Finding.is_failure` is `state in (DRIFT, DANGLING)`, so **the exit code IS a function of the finding for the two states that are failures**, and both are exercised in both directions by `test_installed_matches_tracked.py` (`test_planted_drift_is_refused`, `test_a_dangling_tracked_side_is_a_failure_not_a_skip`). ABSENT and PENDING are DECLARED non-failure states, each argued at length in the module docstring — absence because a laptop with no root crontab has nothing to drift, PENDING because the box's power control belongs to Katie and Sanaa (row A4) and the waiver carries an owner, an expiry and the sha256 of what is expected to be installed, failing the moment either lapses. Calling those a fail-open would make the registry red on every machine that is not this one. **What DID hold, and is the part worth having:** `staging_ghosts()` and `stale_waivers()` were computed, PRINTED, and dropped — so the module could print `GHOST scripts/auto-stop.sh.proposed is a tracked staging copy`, which is the 2026-07-30 incident's own artifact, and exit 0. A finding that reaches stdout and not the exit code is invisible to every caller that composes the module, `lab_check.py` among them. Repaired 2026-08-15: `failures()` is now a named function covering DRIFT, DANGLING, GHOST and STALE, `main()` returns 1 on any of them, and the classes that must NOT fail (ABSENT, PENDING) are asserted not to, in the same test class, so widening the rule later has to argue with a test. Both halves mutation-proved. **This row exists because a round document is a durable record and a correction to it should be findable from the docket rather than only from a commit message.** | V15 round 7 repair pass, 2026-08-15, at `edbaa0fe`; `report()`, `staging_ghosts()` and `stale_waivers()` driven directly, `__pycache__` purged | Nothing further on the registry. The transferable note is for the next round: **"prints a finding and exits 0" and "has no non-zero path for that finding class" are different claims**, and the second is the one that needs the exit paths enumerated before it is made. F6's other two members (`f7a_contract.py`, `calibration_scorecard.py`) were the second kind and both are repaired. **EXECUTED AND CLOSED 2026-08-15, commit `f13729e0`.** This row is itself a correction, and what closes it is that its own repair is now load-bearing: `scripts/installed_registry.py`'s `failures()` covers DRIFT and DANGLING and also GHOST and STALE, and `main()` returns `1 if bad else 0`. The evidence that discriminates a real gate from a printed complaint is that BOTH directions are tested -- `test_a_tracked_ghost_reaches_the_exit_code`, `test_a_stale_waiver_reaches_the_exit_code`, `test_absence_and_a_declared_pending_install_do_NOT_fail`, and `test_main_ACTUALLY_consumes_the_rule_and_not_just_prints_it`. **The live run exits 0 today and that is the correct answer, not a vacuous one:** only ABSENT and PENDING records exist right now, which is D64's and D65's territory, not a failure of this gate. | V15 round 7 grader / fleet |
| D111 | **The two suite failures diagnosed as a concurrent-write artifact are not one: they are deterministic in-process contamination, and the diagnosis has now been confirmed twice by two parties who both only tried isolation.** `d2d6bd6c`'s commit message attributed `test_calibration_scorecard.py::IntakeIsReconciled::test_a_record_dropped_with_no_refusal_recorded_is_caught` (`AssertionError: -3 != 1`) and `::test_files_on_disk_equal_admitted_plus_refused` to another session writing proposal records mid-run. V15 round 7 §1.2 re-ran the file alone, got 18 passed, and recorded *"The chief's diagnosis is confirmed by execution and this line records that I tried to contradict it and could not."* **Both tested isolation; neither tested ORDER.** Executed 2026-08-15 at `29beb7cf`, `__pycache__` purged: `pytest sdk/tests/test_agenda.py sdk/tests/test_calibration_scorecard.py` -> **2 failed, 76 passed**, every time; the same two files in the reverse order -> **78 passed**, every time; the file alone -> **26 passed**. The mechanism is exact. `sdk/chief_engineer/agenda.py:1055` is `_REFUSED_INBOX: dict[str, dict] = {}`, a module-level ledger keyed by FILENAME that `read_inbox()` writes into and **nothing ever clears**. `test_agenda.py` points the reader at a temporary agenda root via `CERTONOMOUS_AGENDA_DIR`, writes fixtures that are refused by design, and calls `read_inbox()`. The env var is restored; the ledger is not. Probed directly: after `test_agenda.py`, `agenda.refused_inbox()` holds **7** entries -- the 3 real refusals plus **`bad.json`, `closed.json`, `dupe.json`, `without-replay.json`**, none of which exist in the live corpus -- so `intake()`'s reconcile compares 131 files on disk against 128 admitted + 7 refused and the residual list is EMPTY, because the extra three are ghosts of a directory that no longer exists. **`pytest sdk/tests` collects `test_agenda.py` before `test_calibration_scorecard.py` alphabetically, so the full suite is deterministically red on this and always has been.** The reason it looked like concurrency is that the only two experiments anyone ran -- the file alone, and the file again alone -- are both the passing case. | V15 round 7 repair pass, 2026-08-15, at `29beb7cf`; both orderings run three times each, the ledger's contents printed from a throwaway probe module collected after `test_agenda.py` and then deleted; `__pycache__` purged before every cell | The ledger needs the same lifecycle as the read that fills it: clear `_REFUSED_INBOX` at the top of `read_inbox()` so it describes the LAST read rather than every read this process has ever done -- which is what `refused_inbox()`'s own docstring already claims (*"Every inbox file the last reads refused"*, and the plural is doing the damage). `_REFUSED_COST_BASES` at `:530` is the same shape and should be looked at in the same edit. Not repaired here: `agenda.py` is outside this pass's scope, the fix changes a public-ish contract, and it deserves its own control pair rather than a passenger seat. **Whoever takes it should also strike round 7 §1.2 and `d2d6bd6c`'s message**, because a confirmed wrong diagnosis is more durable than an unconfirmed one. **REPAIRED 2026-08-15 at `297b82a0`, at the cause and not at the collection order.** `read_inbox()` collects refusals into a LOCAL dict and publishes it wholesale at every exit, including the early return when the inbox directory is absent, so the ledger's lifetime is ONE READ rather than one process; a companion `_REFUSED_INBOX_ROOT` anchors the filenames to the directory they were read from, `refused_inbox()` declines to answer for a directory it has not read, and `refused_inbox_root()` is exported so that empty answer is inspectable rather than a filter nobody can see. `scripts/check_derived_figures.py`'s `build_registry()` clears `P_RANK1_NOT_SHIPPED` on the same principle at the same point; this is that pattern applied where it was missing. Measured after, `__pycache__` purged before each cell: both orderings **78 passed**, the file alone **26 passed**, and the live reconcile balances **131 == 128 admitted + 3 refused** -- the seven were three real refusals and four ghosts. Guard: `sdk/tests/test_refused_inbox_lifecycle.py`, 8 tests, each of which POLLUTES FIRST and then asserts, which is the experiment neither earlier diagnosis ran; it carries the whole D111 failure inside ONE test function rather than across two files and a collection order, with an L-84 positive control on the polluting step itself. Mutation-proved with the control and three mutants in ONE interpreter, the mutants compiled from source strings so no `.pyc` can exist for either side: control 0/8 failed, `no-clear` (the leak restored) 7/8, `no-root-guard` 2/8, the exact pre-fix shape 7/8, control again last 0/8. Full suite before **10 failed / 1731 passed** at `0b0041b1`, after **1767 passed, 0 failed** at `6ff9fb43` (the `9cbaeec6` comment reword landed mid-run, in a file no test in the suite reads). **`_REFUSED_COST_BASES` and `_UNSCORED_KINDS` were looked at in the same edit as this row asks, and deliberately left alone -- see D125** -- because neither is filled by a read there is a boundary to hang a lifetime on, and neither has a caller that would redden today; changing them is a contract decision rather than a repair. Round 7 §1.2 and `d2d6bd6c`'s message are still uncorrected: that striking is what this row still asks for. **PROGRESS NOTE 2026-08-15 (NOT CLOSED).** The code defect is fixed at commit `297b82a0` and verified by execution with `__pycache__` purged: `pytest sdk/tests/test_agenda.py sdk/tests/test_calibration_scorecard.py` gives **78 passed** where it gave 2 failed, `sdk/chief_engineer/agenda.py` now clears and republishes `_REFUSED_INBOX` per read against `_REFUSED_INBOX_ROOT`, and `sdk/tests/test_refused_inbox_lifecycle.py` is tracked. **THE ROW STAYS OPEN on the sentence it exists to get struck:** `LADDER_V_V15_ROUND7.md` section 1.2 still reads "The chief's diagnosis is confirmed by execution and this line records that I tried to contradict it and could not", unstruck, and `d2d6bd6c`'s message is likewise uncorrected. A record that says it tried to falsify a claim and failed, when the claim was wrong, is worse than one that never tried. | agenda owner / V15 round 7 grader |
| D112 | **D56's clearance was verified member-by-member against the row's own list of three members, so a fourth stale claim standing beside them rode the rebuild into the tracked shipping bundle.** `dist/certonomous-demo.zip` was rebuilt 2026-08-14 21:16 (1,476,024 bytes, sha256 `34b8feed8301d91a8aa36d506326c89d74cf4b8ac42c2eb19a2948af1c00ba33`) to clear D56, and the rebuild is genuinely good on everything D56 enumerated: the three named members now read `RANK 1 OF 7`, `P(rank 1) = 50%` and `0-97% at 95%`, with `68%` and `2-100%` surviving only inside dated `<s>` tombstones. **Executed here by opening the archive (90 members, `unzip` to scratch, `/usr/bin/grep -rIn`, positive control `Closure` present in 7 members): six four-entry hits, five of them correct tombstones, and one that is not.** `certonomous-demo/site/closure.html:502` reads *"holds no official rank -- rank 1 of 5 is our local scoring at a pinned benchmark commit, with a seed-uncertainty bound comparable to its margin"*, unstruck. Both clauses are false against the board of record (six entries, retrieved 2026-08-11T23:33Z, re-verified 2026-08-14T21:01Z): the local standing is rank 1 of 7, and the truth-free seed bound `0.002419` is **177% of** the `0.001365` margin over Yang -- it exceeds the margin rather than being comparable to it, and the identical sentence was struck on the sibling page `benchmarks.html:155-156` on 2026-08-14 and left standing here. Its source is `demo-output/website/closure.html:550-551`, whose own `:139` says rank 1 of 7 on the live board, so the page contradicts itself 400 lines apart. **The source-page instance is NOT a new finding** -- `docs/P33_CROSS_SURFACE_SWEEP.md` item 3 named it on 2026-08-12 at `5a15844f`, and a concurrent pass had that file open when this row was written. **What is new is that it is now inside the tracked artifact**, which P33 could not have seen because it read the archive as it stood before the D56 rebuild. **The transferable shape is D56's own shape one level up:** D56's lesson was that a FAIL nobody cleared went uncleared; this is a clearance that was checked against an enumeration instead of against a search, and an enumeration cannot see the item beside the ones it lists. | Ladder V, V14 re-sweep, 2026-08-15, at `29beb7cf`. Frame: `/usr/bin/grep` (GNU 3.11) and `/usr/bin/find` (GNU findutils 4.9.0), verified with `type grep`/`type find` because the shell's own are ugrep and bfs; archive opened rather than grepped in place. Cannot see: rendered text, PDF figure layers, 955 PNGs, binary members `grep -I` skips. | Two things, and the second is the one that lasts. **(i)** Repair `demo-output/website/closure.html:550-551` -- one sentence, both clauses -- and rebuild the bundle from it; verify by reading the archive member, not the build input. **(ii)** **Stop clearing a stale-surface row by re-checking the members it names.** A clearance of this class must re-run the search that found the row, over the whole artifact, with its positive control -- otherwise every clearance is exactly as complete as the enumeration that opened it. Until (ii), D56 and every row like it clear point-in-time against their own list. | fleet (i); chief (ii) |
| D113 | **V13's two PENDING slots were answered six minutes after the close-out committed, and neither can ever be marked answered, because the document holding them is signed and nobody edits it.** `campaign/LADDER_V_V13_CLOSEOUT.md` committed at `46a64042`, 2026-08-11 01:24:34Z; `campaign/LADDER_V_V15_ROUND4.md`, which answers both, committed at `65e79de4`, **01:30:23Z**. PENDING-1 (*"is the ladder at the fixed point?"*) is answered **NO** in round 4's own display type, on seven ranked new failures, and all **fourteen** commits PENDING-1 names were verified by `git rev-list` to sit inside round 4's declared corpus and inside round 6's range `656c09c9..2266c4e3`. PENDING-2 (*"three commits unread by anyone but their author"*) is answered for the reading: round 4's owner states it wrote none of the audited text and never wrote to the closure line, and it read `656c09c9` (2c), `63009dd3` (2h, returning FAIL R5) and `472f9f92` (2j, nine rows, **eight exact PASS re-derived at the frame rather than inherited**), plus V10's `2b251689`/`7cd558b1` (2i, sha256 identity of the two absorbed members, an independently planted wrap-proof control, and FAIL R6). **What is genuinely outstanding is two rows, not three commits, and they have no owner:** **Q69**, the untrained-duct `*_LES*` positive control, inherited because **the run tree is outside the repository**; and **Q60**, V10's *"56 of 56 drift check"* and *"served and read over HTTP"*, inherited because **rebuilding and serving are not read-only acts**. Both were honestly marked inherited rather than passed on the prose, which is R-ISOLATE part 3 working. **The record-keeping defect is structural and general:** a PENDING slot filed in a signed, never-edited document has no closing mechanism, so it reports open forever and the ledger that reads from it inherits the staleness -- which is what put *"one commit unread by anyone but its author"* against V6 and *"SELF-GRADED at the last step"* against V10 in the Ladder V ledger for four days after both were false. | Ladder V records repair, 2026-08-15, at `29beb7cf`; verified by `git log`, `git rev-list` and by reading round 4's sections rather than its summary | **(i)** Dispatch Q69 to an agent **in the main checkout with an exclusive scratch path** -- NOT worktree-isolated, because R-ISOLATE's own caveat is that a fresh worktree does not carry the gitignored run tree and such an agent returns an honest, confident, empty result -- that wrote none of `472f9f92`, and require it to print the count of run-tree evidence files it can see **before** reporting what it found in them. **(ii)** Dispatch Q60 to an agent with build and serve authority that wrote neither `2b251689` nor `7cd558b1`. **(iii)** The general fix: **a PENDING slot must live where it can be closed.** Either the close-out's slots are mirrored into `docs/DOCKET.md` when they are opened, or a signed report states its slots' closure location on its face. **PROGRESS NOTE 2026-08-15 (PARTIALLY DISCHARGED, NOT CLOSED).** Items (i) and (ii) are measured by a NON-AUTHOR at commit `dde6e1cf`: `LADDER_V_V13_PENDING2_MEASURED_2026-08-15.md` records Q69 MEASURED-PASS, Q60 MEASURED-PASS on the claim with a live FAIL at HEAD, and "V13 section 8 PENDING-2 has no inherited rows left." **ITEM (iii) IS KATIE'S AND IS NOT DISCHARGED** -- see the owner column: whether a signed close-out's PENDING slots must be mirrored into `docs/DOCKET.md` when opened, or whether a signed report must state on its face where its slots will be closed. Nothing in the tree can settle a records convention. | chief (i, ii); Katie (iii, it is a records convention) |
| D114 | **The 84% claim had two more copies, and the clause that kept generating them is an instruction telling every surface to write it.** Four repair passes each fixed the copies they were told about (D71 `cc906ec9`; D88 `d80649f9`; D105) and none was a sweep. A corpus-wide sweep — `git grep` over all 20,677 tracked files of every extension plus untracked/ignored ones and a byte pass over non-UTF-8 artifacts, with strike classification reusing `check_derived_figures.mask_exempt`'s X1–X3 idioms rather than a new line-by-line masker — found **two more live copies and their source**. **(a)** `demo-output/website/closure.html` §5 lede: *"0.0029 below the published leader ... the margin carries a seed-uncertainty bound of comparable size"*. **The sibling page `benchmarks.html` carried the identical sentence and had it repaired at `cc906ec9`; that same commit touched `closure.html` and did not repair this one.** The page was giving two answers at once — its own §0 banner already said *"our margin over Yang is 0.001365, not the 0.002878 over Reissmann quoted below"*, and this was the sentence it meant. **(b)** The same file's stability note: *"comparable to the 0.0029 margin over the published leader. The rank-1 reading above must carry that uncertainty."* **(c) The generator: `CLOSURE_FAMILY_SUPERVISION_GUIDELINES.md` §4 clause (b)**, a MANDATORY rule instructing every claim-bearing surface to state the bound as *comparable to* the margin, which names `closure.html`'s stability note — copy (b) — as *"the reference wording"*. That file's own §6 already recorded clause (b) as *"now an understatement"*, and the clause it corrects was **left standing and binding**. Neither the string `84%` nor any percentage appears in (a) or (b): the claim travels as the **prose form** (*comparable size*) attached to the **rounded 0.0029** four-entry margin, which is why four string-anchored passes missed it. | Corpus-wide sweep of the claim family, 2026-08-15, at `29beb7cf`; frame stated in commit `5bec65f0`. Not the shell's `grep` (`ugrep --ignore-files`, ~23% of the tree) and not `find` (`bfs`) | **Repaired at `5bec65f0`**, strike-and-keep, together with D105 and D104. Verified by **re-derivation** from `probability_of_rank.py` `LIVE_BOARD` (six entrants; leader Yang, eight-case mean 0.0580125), `closure_challenge_round5_qcr.json`, `closure_challenge_seed_sensitivity.json` and `probability_of_rank_record.json` — **not** by a clean sweep, because D85 (a strike-and-keep marker IS a placement), D88 (no arithmetic predicate) and D89 (no Yang sentence can fault as shipped) mean the placement guard cannot confirm this work. The durable half is (c): **a repair pass that fixes copies without striking the instruction that mandates them leaves the generator running**, and no check in this lab reads a document as a set of instructions rather than a set of assertions. **EXECUTED AND CLOSED 2026-08-15, commit `5bec65f0`.** The repair reached THE GENERATOR and not only the copies, which is the discrimination this row exists to force: clause (b) of `CLOSURE_FAMILY_SUPERVISION_GUIDELINES.md:96-99` is now inside `~~...~~` and carries "STRUCK 2026-08-15 -- AND THIS CLAUSE IS WHY THE FIGURE KEPT TRAVELLING", with the corrected instruction (the bound is 177% OF THE MARGIN) beside it. Both downstream copies verified struck independently at `closure.html:395` and `:456`. A repair that had struck only the two copies would have left the clause that regenerates them, and this row would have reopened on the next build. | fleet / closure family |
| D115 | **`demo-output/website/latex/closure_challenge_report.pdf` is a complete four-entry-board artifact, its own `.tex` is fully repaired, and no check in this lab opens a PDF at all.** The 41-page PDF (mtime 2026-08-11 03:26Z, last touched `98a39662`) predates the board move of 2026-08-11T23:33Z by twenty hours. Enumerated with `pdftotext -layout`: the withdrawn **`P(rank 1) = 68%` twelve times** (plus two legitimate *"68% confidence level"* uses), **`rank 1 of 5` twice**, the four-entry margin **`0.002878` four times**, and a five-row Table 1 in which **Yang does not appear at all**. Page 5 carries **the origin of the 84% figure**: *"the seed-spread bound ... is 0.0024 against a rank-1 margin of 0.002878, and loading it adversely moves P(rank 1) from 68% to 52%"* — and 0.002419121853891026 / 0.0028863365997926355 = 0.8381 is exactly where `84%` and `0.838` came from. Its source `closure_challenge_report.tex` was repaired at `2cec44ee` (2026-08-12): every `68%` `\\sout{}`-struck to 50%, `rank 1 of 5` struck to *"rank 1 of 7 entrants"*, a new `sec:boardmoved` stating the six-entry board and the 0.001365 Yang margin, and the seed-bound sentence now reading *"the bound is now 1.8 times the margin ... puts us at rank 2 on the point score"*. **The PDF is stale against its own repaired source by 1.5 days and nothing in the lab can see it**: a sweep for `pdftotext|pypdf|PyPDF2|pdfminer` outside `docs/papers/` finds only a comment, and the only test touching PDF bytes is `sdk/tests/test_certificate.py:100,106`, which asserts the file *starts with* `%PDF`. No test, script or audit names either report PDF. The other six tracked non-literature PDFs (`dafoam_defect_report.pdf`, three round-3 certificates, two B-52 certificates) were extracted and are **positively confirmed clean** — zero hits on any of the 22 claim patterns, a clean finding measured rather than assumed. | PDF enumeration during the D114 sweep, 2026-08-15; `pdftotext -layout` over all seven non-literature PDFs, each compared against its `.tex` sibling | **A rebuild from the current `.tex` (`2cec44ee`) resolves every item — not an edit to the PDF.** Not done here: `latex/*` has a designated owner and no PDF was modified or opened for writing. The generalisable half is the second sentence: **this lab's green is computed over text surfaces only**, so a binary artifact can carry a withdrawn conclusion indefinitely while every check passes. Either a check opens the shipped PDFs, or the build is made reproducible and the PDF rebuilt-and-diffed in CI. A magic-number assertion is not coverage. | `latex/*` owner / fleet |
| D116 | **Two live surfaces still assert the superseded comparison in the present tense, and they are legal under every rule this lab has.** `demo-output/website/ACTIVE_RESEARCH.md:591-594` (*"margin 0.002878, which carries the G1 seed qualifier: the truth-free seed-spread bound ... is 0.0024 overall-equivalent, **comparable to the margin itself** ... carried forward 2026-08-07"*) and `demo-output/website/CLOSURE_CHALLENGE_STATUS.md:584` (*"**0.0024 is comparable to the 0.002878 rank-1 margin**, and the rank-1 reading carries that uncertainty"*, labelled *"added 2026-08-07 ... carried forward because the three seed-dependent predictions ship in round 5"*). Both **name their own four-entry denominator**, so both are arithmetically true as written and both are correctly exempted by `check_derived_figures.py`'s NEG-5 control — an exemption that is deliberate and right, since faulting them would punish the dated-record convention that makes corrections honest. **But neither is struck and both say *carried forward*, i.e. both assert present operative force for a comparison whose referent died at 2026-08-11T23:33Z.** This is exactly `check_derived_figures.py`'s own BLIND-TO item 1 (*a figure correct to the digits written but derived from a stale referent that rounds the same way*), and the dated-context discriminator D85 and D89 both name as the prerequisite still does not exist. Classified AMBIGUOUS and left untouched in the D114 sweep, recorded rather than repaired, because rewriting a true dated sentence is the failure mode strike-and-keep exists to prevent. | D114 corpus-wide sweep, 2026-08-15 | A one-clause board stamp on each (*"against the four-entry board; on the live six-entry board the same bound is 177% of the margin"*) by each file's owner — an addendum, not a rewrite. The generalisable half is the honest answer to *what would still let another copy exist*: **the sweep's frame is text and its arithmetic predicate is anchor-phrase-bound.** `check_derived_figures.py` reads only tracked `*.md`/`*.html` under 2 MB, so `.json`, `.py`, `.tex` and every binary are outside it (D115 is that gap realised); a figure whose anchor phrase is absent from its registry is unchecked by construction; and **a claim carrying no number at all — *comparable size*, *sits inside the margin* — offers an arithmetic predicate nothing to grade**, which is precisely how D114(a) and (b) survived four passes. | closure family / fleet |
| D117 | **[CORRECTION TO D114, by its own author, 2026-08-15 — MY FRAME SENTENCE OVERSTATED WHAT I SEARCHED, AND AN EIGHTH COPY WAS INSIDE THE GAP.]** D114 says its frame was *"all 20,677 tracked files of every extension **plus untracked/ignored ones**"*. **The second half is false.** Measured: `git grep -l -E 'comparable' -- .` returns **157** files and `git grep -l --untracked -E 'comparable' -- .` returns **157** — identical, and **neither reaches a single gitignored file**. `--untracked` adds untracked-but-not-ignored files (this tree has exactly one, `.autostop-hold`) and still applies the exclude rules; it is also **not a superset**, because it switches to a worktree walk that drops tracked files living under ignored directories. A complete sweep needs the third arm: `git ls-files --others --ignored --exclude-standard`. My own memory note said *grep skips ignored files* and I assumed `--untracked` was the fix. **The gap had a live copy in it, and a second, independent one in a pattern hole. (a) `demo-output/website/closure.html:551`, the page's closing "honest bottom line": _"holds no official rank — rank 1 of 5 is our local scoring at a pinned benchmark commit, with a seed-uncertainty bound comparable to its margin"_ — unstruck, both clauses false, in the same file whose §5 and stability note I had just repaired.** D114's prose patterns matched `comparable size|scale|in magnitude` and **not the possessive `comparable to its margin`**, and the sentence carries **no numeral at all**, so `check_derived_figures.py`'s arithmetic predicate had nothing to grade and stayed green over it. It was already a known-open finding — `docs/P33_CROSS_SURFACE_SWEEP.md` item 3 named it on 2026-08-12 and **D112 asked for exactly this repair as its settlement item (i)** — so this is the third correction to one paragraph. **(b)** The gitignored build `dist/certonomous-demo/site/{closure,benchmarks}.html` carries three live copies of the same family; the tracked `dist/certonomous-demo.zip` carries them too and is invisible to a byte sweep because its text is DEFLATE-compressed, which is D112's finding reached by a second route. | Adversarial re-check of the D114 sweep's own frame, 2026-08-15, run against the sweep rather than with it; the `--untracked` counts and the ignored-file arm executed directly | **(a) repaired here**, strike-and-keep, with the pattern hole named on the face of the correction; the repair also removes a `rank 1 of 5` that my first draft of the correction note had itself re-introduced into live prose, which is D71's *"prose written to explain a rank defect creates rank defects"* recurring inside this row's own fix. **(b) NOT repaired: `dist/` is outside this pass's scope and stays D112(i)'s to clear.** The durable half is the method, and it is the honest answer to *what would still let another copy exist*: **a sweep is only as wide as the weakest of its three arms, and a claim with no number in it is invisible to every arithmetic check this lab owns.** Any future sweep of this family states all three arms explicitly and greps the possessive forms, or it will report clean over the same gap again. **PROGRESS NOTE 2026-08-15 (HALF (a) DISCHARGED, ROW NOT CLOSED).** (a) is repaired at commit `3010e12f`: `demo-output/website/closure.html:554` is strike-and-kept with the pattern hole named on its own face, and no unstruck `rank 1 of 5` remains in any tracked `.html`. **(b) IS STILL LIVE AND IS D112(i):** verified by extracting the HEAD blob of `dist/certonomous-demo.zip` -- `site/closure.html:502` still carries the live copy, as does the gitignored `dist/certonomous-demo/site/closure.html`. So the source tree and the artifact that ships now DISAGREE, which is a different and worse state than both being wrong together. Do not close this row with (a). | fleet |
| D118 | **The lab's bundle-drift detector never opens the artifact that ships.** `self_audit.check_bundle_drift()` compares tracked sources against `dist/certonomous-demo/` — a directory **gitignored at `.gitignore:72`**, whose own words are *"the .zip is committed; the extracted tree is not"*. **The only file that leaves this box is `dist/certonomous-demo.zip`, and no check in this tree reads it.** The two agree today only because one build at 2026-08-14 21:16 wrote both; nothing enforces that, and regenerating the directory without rebuilding the zip returns the check to green over a stale shipped artifact — the silent-zero shape in the one place it costs most. Three further gaps in the same enumeration, measured rather than asserted: **(i)** the zip holds **90 files** and the check grades **56** (49 control-room modules + 4 launcher + 3 pages), so the 34 ungraded members — including `snapshot/lab_stats.json`, which carried defect 4 of the 2026-08-10 rebuild — **cannot appear in any numerator the check prints**; **(ii)** because the reference directory is untracked, **no later agent can ever reconstruct what the check saw on 2026-08-11**, so V10's `56/56` was verifiable in 2026-08-15 only by substituting the tracked zip — **a verification whose reference is gitignored expires the moment it is made**; **(iii)** measured at HEAD `297b82a0` the check returns **FAIL at 51/56 against both references**, five members stale (`agenda.py` 64,270→61,008 B, `exec_bits.py` 24,989→24,597 B, `closure.html` 46,874→41,370 B, `benchmarks.html` 20,362→19,057 B, `wall.html` 10,302→10,304 B), and the shipped `site/closure.html:502` carries **D112's unstruck *"rank 1 of 5 … comparable to its margin"* as live prose** while its source at `:554` carries the corrected clause, the 177% bound and an `<s>` tombstone struck 2026-08-15. **This is D112's shape one level up:** D112 found a clearance checked against an enumeration instead of a search; this is a **detector** whose reference is a build scratch copy instead of the artifact. **`BUNDLE_REBUILD_2026-08-10.md` §4's ruling to KEEP the directory is sound** — it is the check's eyes — and this row is the gap that ruling leaves open, not a reversal of it. | V13 PENDING-2 measurement pass, 2026-08-15, at `dde6e1cf`; full record `LADDER_V_V13_PENDING2_MEASURED_2026-08-15.md`. Frame: main checkout never a worktree, `/usr/bin/grep` (GNU 3.11) and `/usr/bin/find` (GNU findutils 4.9.0) because this shell's own are ugrep `--ignore-files` and bfs; the zip extracted from its **tracked blob** into scratch rather than read in place; `__pycache__` purged before every cell; drift re-derived in Python against `_bundle_pairs()` rather than read from a prior report. Cannot see: whether the 34 ungraded members are themselves correct — this row measures only that they are **ungraded**; nor any drift in `dist/certonomous-demo/` before 2026-08-14, which is unrecoverable by construction. | **Nothing repaired here — `dist/` has a designated owner and was not rebuilt or edited, and the five stale members and the `:502` sentence stay D112(i)'s to clear.** The durable half is the method: **a drift detector must compare against the artifact that ships, not against the directory the build happened to leave beside it**, and where the shipped artifact is a compressed archive the comparison must open it. Two concrete forms, neither applied: teach `check_bundle_drift()` to extract `dist/certonomous-demo.zip` and compare against **that**, reporting the directory only as a convenience; and make the graded set the zip's **whole member list** with derived members named as explicit, listed exclusions, so the denominator states what it declines to grade instead of silently omitting it. Until then every green this check prints is a statement about a gitignored scratch directory. **EXECUTED AND CLOSED 2026-08-15, commit `e01dfdf6`.** `scripts/self_audit.py` now sets `_BUNDLE_ZIP = Path("dist") / "certonomous-demo.zip"` and `check_bundle_drift()` opens that TRACKED archive with `zipfile.ZipFile`, prints `FRAME: <n> member(s) in dist/certonomous-demo.zip`, and names `_BUNDLE_DIR` as a convenience copy it "no longer grades against (D118)". Every ungraded member is classified `derived-at-build` / `untracked-source` / `unclassified`, so the denominator states what it declines to grade instead of silently omitting it -- which is the specific thing this row asked for. **THE CHECK IS FAIL TODAY AND THAT IS THE POINT:** it reports five stale members. Those are D112(i)'s and D80(c)'s, not this row's; what was broken here was that the verification's reference was gitignored and therefore unre-derivable by anyone, and that is fixed. **NOT DONE:** the bundle itself is not rebuilt, and rebuilding it is `dist/`'s owner's call. | chief (routing); `dist/` owner (the stale members) |
| D119 | **No check in this lab reads a document as a set of INSTRUCTIONS, and that is why the 84% claim came back five times.** Every existing check grades an ASSERTION (*is this sentence true?*). None grades a DIRECTIVE (*does this rule, obeyed, write a true sentence?*). A normative clause is a claim generator: `CLOSURE_FAMILY_SUPERVISION_GUIDELINES.md` §4 clause (b) was a MANDATORY rule telling every claim-bearing surface to state the seed bound as *comparable to* the margin AND naming one specific existing sentence as *"the reference wording"*, so it manufactured copies faster than four sweeps removed them (D71, D88, D105, D114). Its own §4(e) had recorded it as *"now an understatement"* on 2026-08-11 at `5c9c63fb` and **left it standing and binding** — a correction filed as an OBSERVATION beside a MANDATE loses to the mandate, because the mandate is what the next author obeys. **A second instance in the same section, found by the census and not by any instrument: clause (d)**, *"Every surface, internal or public — including anything shipping in `dist/` — carries **P(rank 1) = 68%**"*, a live MANDATE pinning a four-entry figure whose only superseding banner sits eighty lines up **inside clause (c)**, while (d)'s own header announces the supersession of a *different* thing (the internal/external split). **A third, outside this file and out of repair scope: `campaign/LADDER_V_V13_CLOSEOUT.md:203`**, *"Agree, and hand over the number that proves you already knew"* — an order to write the claim, with the four-entry bullets still live beneath it. | Normative-clause census, 2026-08-15, over the 404 tracked prose files `git ls-files -- '*.md' '*.html'` opens (NOT the shell's `grep`, which execs `ugrep --ignore-files`; NOT `find`, which is `bfs`), struck text masked by `check_derived_figures.mask_exempt` reused unmodified. 839 mandate-bearing clauses examined; 34 governance documents hand-classified | **(1) Clause (b) repaired**: the struck clause is kept visible with its falsifying date and its strike anchor `6138d452`, the orphaned §4(e) observation is now linked from it by name, and a **replacement clause (b-1) fixes a FORM and no figure**: no surface may quote the margin except as the triple (margin; entrant **by name**; board by **entrant count and retrieval date**), with a commit anchor **not** admissible because `deb91557` scores but does not rank, and **no reference sentence to copy, naming one prohibited**. Verified by **re-derivation** from `closure_challenge_round5_qcr.json` -> `round5_overall_full`, `closure_challenge_seed_sensitivity.json` -> `overall_equivalent_S_bound` and `campaign/BOARD_RESCORE_2026-08-14.md` §3.1, **not** by a clean sweep: margin 0.001365 over Yang on the six-entry board of 2026-08-11T23:33Z, bound 0.002419, ratio **177%**, adverse overall 0.059047 above Yang's 0.058013. **(2) Clause (d)** carries a dated note **on the mandate itself**, not beside it; not rewritten, per that section's own rule. **(3) The instrument**: `scripts/check_normative_clauses.py`, three-valued, admitted and run by `scripts/lab_check.py` (verified: `gate: main() can exit non-zero`, no required args, no write primitive). Three rules — W1 mandated WORDING, W2 mandated FIGURE, W3 mandated QUALITATIVE relation (always UNDECIDABLE: *comparable to its margin* gives an arithmetic predicate nothing to grade, which is the claim's main vector after five passes removed the literal `84%`). Twelve controls both ways on every invocation, including clause (b) at its pre-repair state fetched from `5bec65f0^`; zero clauses matched is UNKNOWN, never PASS. Eight mutants, all killed. **An unanchored version was measured first and REJECTED: 561 hits in 95 files, essentially all co-occurrence** — that measurement is in the docstring instead of shipped, because this lab already has one 74%-false-positive instrument and the cost of it is that readers ignore it. **EXECUTED AND CLOSED 2026-08-15, commits `65659488` (the clause) and `1c4a643f` (the instrument), WITH ONE NAMED RESIDUAL THAT IS NOT CLOSED.** Executed: `python3 scripts/check_normative_clauses.py` returns `VERDICT: PASS`, exit 0, over **853 clauses examined**, and `python3 scripts/lab_check.py --list` finds it by directory enumeration rather than by a name literal -- so a checker that stopped running would be a finding rather than a silence. Clause (b) is struck with anchor `6138d452` and (b-1) is in force; clause (d) carries its dated note on the mandate. **RESIDUAL, STILL LIVE and deliberately not closed:** instance 3, `LADDER_V_V13_CLOSEOUT.md:203` and its four-entry bullets (`P(rank 1) = 0.674`, `2-100%`, `t = -0.50/-0.95`), carries no supersession banner. The instrument now reports it as the single UNDECIDABLE W1 hit, so it is visible rather than lost, but it is outside this row's repair scope and needs its own owner. | closure family / fleet |
| D119a | **[DISCLOSURE, by D119's author, 2026-08-15 — MY FILING COMMIT ABSORBED TWO ROWS THAT ARE NOT MINE.]** `769a83f0` is pathspec-scoped to `docs/DOCKET.md` and its message names the concurrent uncommitted work it did not touch, but a pathspec is a FILE and two rows landed inside that file between my `git diff --stat` (which showed 2 insertions, both mine) and my `git add` seconds later. **`D121` (the Spalart citation hand-fixed on the page and never reaching the generator) and `D122` (V5's closing condition with three readings) are another session's work, authored by another session, and are committed under my message.** Not reverted: another session may already be building on that commit, and reverting a row I did not write to fix a commit I did write trades one defect for a worse one. **The generalisable half, and it is D83 measured a third time: a docket commit has no safe granularity.** `git add docs/DOCKET.md` is the only pathspec available and it stages whatever the file holds at that instant, so the disclosure in a filing commit's message is a statement about a moment that has already passed by the time the commit runs — and `git status` in this tree reads stale under concurrency, so the pre-flight check that showed 2 insertions was not wrong so much as expired. | Post-commit `git show --stat HEAD -- docs/DOCKET.md`, which reported 4 insertions against the 2 I wrote | The disclosure IS the remedy at row granularity. Structurally: a docket commit needs either a per-row file (append-only, one file per ID, concatenated on read) or a pre-commit hook that diffs the staged rows against the author's declared IDs and refuses the commit on a mismatch. Until one exists, **every docket filing commit in this repository should be read as possibly carrying another session's rows**, and this row is the standing evidence for that. | chief (routing); D83's owner |
| D120 | **The margin is written `0.001365` everywhere and the arithmetic gives `0.0013658`, so the corpus-wide form is a TRUNCATION where every other figure in the family is a ROUND.** `0.058013 - 0.056647191704213645 = 0.001365808295786355`, which to four significant figures is **0.001366**. The form actually carried by the submission draft's binding rule, `campaign/BOARD_RESCORE_2026-08-14.md` §3.1, `DESCRIPTION_DOCUMENT.md`, `closure_challenge_round5_qcr.json` and now clause (b-1) is **0.001365**. Nothing downstream changes — the ratio to the seed bound is 177% at either (177.1% on 0.001366, 177.2% on 0.001365) and the conclusion does not depend on it — and `check_derived_figures.py` passes both, because `0.001365` written to four significant digits agrees with the 4-dp basis `0.0013528...` within its own interval. **But the lab has no stated rounding convention**, so a reader cannot tell whether `0.001365` is a rounding decision or a transcription error, and D104 has already spent one repair on precision in this exact quantity. Not repaired here: changing it would touch six surfaces including the frozen submission package, and the convention has to be chosen before the digits are. | Re-derivation of the clause (b-1) figures from the primary records, 2026-08-15, during the D119 repair | State the convention once, in the submission draft's replacement binding rule, and make the other surfaces cite it: either *truncate at four significant figures* (keeps every existing surface correct, and is defensible as never overstating a margin in our own favour) or *round* (changes six surfaces to 0.001366). **Truncation is the better rule here and should be written down as one**, since the margin is a quantity we benefit from overstating. Naming it costs one sentence; leaving it unnamed leaves every future author guessing, which is how D104 happened. **CLOSED 2026-08-15 AS REFUTED, NOT AS IMPLEMENTED -- and the ruling's own author accepted the refutation in full at commit `0b8fb5d6`.** Re-derived independently rather than read off the refutation: `LIVE_BOARD`'s Yang column in `sdk/scripts/probability_of_rank.py` has mean exactly **0.0580125**, and `0.0580125 - 0.056647191704213645 = 0.0013653082957863563`, which is **0.001365** at six decimals. `probability_of_rank_record.json`'s `pairwise.Yang.margin` carries the same value. D120's `0.001366` is obtained by subtracting the PRINTED `0.058013` -- a printing of the basis mistaken for the basis, which is the error the author's own acceptance note names. **ZERO INSTANCES SHOULD CHANGE**, and the falsification is on the ruling's own table, not merely unproven: see `b4604fc1`, which records that acting on D120 would have broken two other figures it never mentions. The successor question -- what interval the 4-dp board actually supports -- was measured separately and is D127's. | closure family |
| D121 | **The Spalart citation was hand-fixed on the page and never reached the generator that writes the same sentence into three shipped JSON surfaces.** `sdk/scripts/build_benchmarks.py:106` emits an `our_entry` string reading *"…round-5 **untrained** QCR2000 forward solve on the ducts…"* — the untrained claim about our own model, with no attribution — and writes it to `demo-output/website/benchmarks.json` at `:346`; `lab_stats.research_programs()` reads the same string (`build_benchmarks.py:88`) and `build_wall.py:333` writes it into `demo-output/website/wall/wall.json`, and it appears a fourth time in the **gitignored** `dist/certonomous-demo/snapshot/lab_stats.json`. Zero occurrences of "Spalart" in any of the four. **`e071075d` closed V5's leg-3 gap on `benchmarks.html` and did the right generator check** — it established that no script writes that page — **but the check was scoped to the file being edited, not to the sentence being corrected**, and the machine-readable twin of the fixed page was never opened. The next `build_benchmarks.py` run rewrites `benchmarks.json` with the unattributed claim no matter what the page says: a correction to an output a generator will rewrite is not a correction, which is `e071075d`'s own stated principle applied one file to its left. | V5 closure grade, 2026-08-15, `LADDER_V_V5_GRADE_2026-08-15.md` §4 (U1–U4). Frame: three arms swept — tracked via `git grep`, untracked via `git ls-files --others --exclude-standard`, and the **37,242 gitignored files** via `git ls-files --others --ignored --exclude-standard` piped to `/usr/bin/grep`, because this shell's `grep` is `ugrep --ignore-files` and its `find` is `bfs`. Control executed: `git grep -lI untrained -- dist/` and `git grep -lI --untracked untrained -- dist/` both return **nothing** while `/usr/bin/grep -rlI` returns three files, so the fourth copy is in the arm no git instrument reaches. Cannot see: whether the `our_entry` enumeration counts as a load-bearing claim or as a model name — that is the frame question D122 records as unruled. | **Not repaired — the grader is a non-author and stays one.** The durable half is the rule: **when a claim is corrected, the unit of correction is the sentence across every surface that emits it, not the file it was found in.** A generator check that asks *"does anything write this file?"* answers a narrower question than *"does anything write this sentence?"*, and the second is the one that keeps a fix from being undone by the next build. Concretely: fix the string at `build_benchmarks.py:106`, regenerate `benchmarks.json` and `wall.json`, and leave `dist/` to its owner under D112(i)/D118. **EXECUTED AND CLOSED 2026-08-15, commit `e639796e`.** Verified on all three TRACKED surfaces independently at HEAD, by counting the attribution rather than reading the commit message: `sdk/scripts/build_benchmarks.py` (the generator), `demo-output/website/benchmarks.json` and `demo-output/website/wall/wall.json` each now carry exactly one `Spalart` attribution, the generator's at line 106 reading "...whose one coefficient Ccr1 = 0.3 is Spalart (2000)'s published constant, adopted untrained...". THE GENERATOR WAS FIXED FIRST AND THE TWO JSON SURFACES REGENERATED, which is the ordering this row demanded: a fix applied to the copies alone would be undone by the next build. **NOT DONE, and deliberately out of scope:** the fourth copy, `dist/certonomous-demo/snapshot/lab_stats.json`, is gitignored and still unattributed; this row's own remedy column routes `dist/` to its owner under D112(i)/D118. | chief (frame ruling first — under a C3 frame these may be deliberately out of scope); `build_benchmarks.py` owner |
| D122 | **V5's closing condition has three different readings, the chief was formally asked to choose, and never did — so the rung has been graded against a criterion that narrowed by transcription.** The ladder pre-registers leg 3 as *"Spalart (2000) cited **wherever QCR is named**"* (`LADDER_V_TRIPLE_VERIFICATION.md:32-33`). `LADDER_V_PASS1_2026-08-11.md` §A5 found that reading fails at HEAD, adopted a narrower one — surfaces carrying the **load-bearing untrained claim about our model** — named **five** gaps under it, and closed with *"**Chief ruling requested** on (a) the frame"* (`:486-487`). The chief's addendum `e59ae644` issues Ruling 1 (the pre-registration freeze) and Ruling 2 (the stale §4.1 citations) and **never answers (a)**. `LADDER_V_V13_CLOSEOUT.md` §7 item 9 then listed **two** of the five; the ledger's GREEN REQUIRES copied the close-out; the 2026-08-15 records repair verified those two and reported V5 *"looks closeable"*. Each step is individually defensible and the composition dropped three exceptions and the chief's express refusal to upgrade the rung. **Two second-order defects fall out of the same chain:** the repaired ledger paragraph calls the refused surfaces *"never gaps"* when Pass 1's own table is headed *"five load-bearing gaps"* and the chief held V5 at PASS WITH EXCEPTIONS precisely because they are gaps — a refusal is not a non-gap; and the refused count is **four, not three**, because the 08-08 pass refused `campaign/LADDER_V_RUNGS_V2_V7_V10_2026-08-08.md:121` (*"duct gains from the **untrained** QCR2000 term (nothing fitted)"*, zero Spalart) on the identical signed-report ground and the 08-11 table never carried it forward. **And one refusal's ground is stale:** `campaign/QCR_ACTIVITY_CHECK_2026-08-08.md` was refused as *"another verification agent's signed report, left to its owner"*, but it has **two** commits — `1a14e90b` signed it 2026-08-08T02:18:29Z and `fe612d03` amended it 20 hours later with exactly the class of change V5 declined to make, a dated additive below-the-line note, three days **before** the refusal was written. | V5 closure grade, 2026-08-15, `LADDER_V_V5_GRADE_2026-08-15.md` §§2, 5, 6. Executed: all four refused surfaces re-measured by commit history and by `sha256sum` of worktree against `git show HEAD:` — both frozen artifacts are single-commit and byte-identical to HEAD, so **those two refusals hold**. A repository-wide search for a ruling settling V5's frame returns nothing. Cannot see: whether a frame ruling was issued outside the repository, in a dispatch message no artifact records — which is the same unverifiability D-row this grade raises about independence itself. | **The rung cannot be closed by editing.** Under the literal criterion two of V5's five exceptions sit on **frozen artifacts** that may take a dated addendum but may never take a revision (L-44), and a `"trained": false` JSON field cannot carry a citation without being revised — so under that reading V5 can **never** reach a plain PASS. What settles it is the ruling that was requested in 2026-08-11 and is still owed: fix the frame at one of the three readings **by name**, and say explicitly whether the frozen artifacts, the two signed reports, `docs/PRODUCT_LIST.md` and the four surfaces of D119 are in it or out of it. The durable half, which outlives V5: **a rung whose criterion narrows by transcription through three documents has not been closed, it has been re-scoped** — and the place to catch that is the moment a close-out lists a subset of a rung's own findings without saying it is doing so. | chief (the frame ruling is his and was requested of him) |
| D123 | **A verification whose reference is gitignored expires the moment it is made, and D118 is not the only one — the population is measured here, and one member of it returns a live green from an empty set.** Sweep frame stated before the count: every `*.py` and `*.sh` under `scripts/` (**52** on disk, **51** tracked) and every file under `sdk/tests/` (**94** on disk, **86** tracked), plus the `.cron`, `crontab.*` and `pre-push` files — **129 files opened**, **279** distinct path literals and **59** composed `REPO / "..."` paths extracted, each classified by `git check-ignore -v` **and** `git ls-files --error-unmatch` **and** existence on disk. Of the 279: **103 TRACKED, 16 GITIGNORED, 4 UNTRACKED-NOT-IGNORED, 27 OUTSIDE-THE-REPO**, 88 non-existent, 36 system. **The live defect, verified by execution rather than by reading:** `check_ledger_stalls` (`scripts/self_audit.py:246`) iterates `_iter_ledger()`, which at `:137` returns immediately when `demo-output/website/mega-batch/ledger.jsonl` is absent — **gitignored at `.gitignore:25`**. `stalls` stays empty and the check returns **`PASS` "no row exceeds the stall threshold"** having read **zero rows**. Measured both ways in one process: with the ledger present it returns `WARN` (6 stall rows, 11.3% of published core-hours); with `LEDGER` repointed at an absent path it returns **`PASS`**. Its sibling `check_ledger_integrity`, reading the same absent file, returns **FAIL** — **the two checks disagree about what an absent ledger means, and the one that fails open is the one nobody would re-read.** Four more that survive their reference vanishing: `check_convergence_validate.py:124` expects `CANNOT_TELL` from a gitignored log while `check_convergence.py:266` returns `CANNOT_TELL` for a *missing file*, so **8 of that suite's 13 known-answer cases** rest on evidence no clone can obtain; `sdk/tests/test_log_signatures.py:816` guards three assertions behind `if external.exists()` on `/home/ubuntu/certonomous-runs`; the same file's `:606-700` carries four `@skipUnless(...exists())` tests on paths gitignored at `.gitignore:61`; and `sdk/tests/test_installed_matches_tracked.py:162` skips on `/usr/local/bin/auto-stop.sh` under its own docstring's warning that skipping "would be the original defect wearing a green suite". `scripts/lab_check.py:253-259` already states the class in aggregate — *"Most of what these checks examine ... is GITIGNORED"* — but carries no instance list and does not note that `check_ledger_stalls` converts that absence into a PASS. | D118 repair pass, 2026-08-15, at `3af7bd2b`. Frame: `/usr/bin/grep` (GNU 3.11) and `/usr/bin/find` (GNU findutils 4.9.0) throughout, because this shell's own are `ugrep --ignore-files` (which cannot see the gitignored paths this sweep is *about*) and `bfs`; `git ls-files` and `/usr/bin/find` run as two independent enumerations and their difference reported. Cannot see: **paths built at run time** — 11 base-variable-relative paths (`<RUN_ROOT>/baseline` in `coefficient_uq_plate.py:257`, `<BUNDLE>/...` in `laptop_bundle/replay_console.py:51-153`), any f-string segment containing `{` (`RESULTS / f"{name}.json"`, `self_audit.py:3315`), the **9 environment overrides** that repoint references at run time (`CLOSURE_BENCHMARK_DIR`, `R2_PLATE_RUN_ROOT`, `CAMERA_AUDIT_ROOT`, ...) — defaults only were classified — paths scraped out of JSON or markdown and then opened (`mint_retrospective_certificates.py:57-77` regexes its evidence paths out of a `.md` and resolves them against two out-of-repo roots), and everything read by a subprocess (`crontab -l`, `git show` inside `~/closure-challenge-benchmark`, `lab_check`'s pytest). `sdk/chief_engineer/`, `sdk/workflows/` and `sdk/scripts/` were **not swept**. | **`check_ledger_stalls` first, because it is the only one measured returning a false green:** give it the same absent-evidence branch its sibling already has — zero rows read is UNKNOWN or FAIL, never PASS — and the `UNKNOWN` status and exit-3 contract added to `self_audit.py` at `e01dfdf6` is the mechanism to do it with. Then the general rule, which is the durable half and is **not** "stop using gitignored evidence" (much of this lab's primary evidence is legitimately untracked and large): **a check whose reference is not reproducible from a clone must print its evidence count before its verdict and must not be able to return PASS from zero** — the count is what converts an absence into a measurement, and D118's repair prints exactly that frame every run as the worked example. Third, and cheapest: a test that skips is not a test that passed, so the four `@skipUnless` sites and the two `if ...exists()` guards should report SKIPPED-FOR-MISSING-EVIDENCE in the suite summary rather than dissolving into the green count. **EXECUTED AND CLOSED 2026-08-15, anchored at `9af7e2d3`, WHICH IS NOT THIS GRADER'S COMMIT AND WAS NOT THIS ROW'S AUTHOR'S EITHER -- the code was in history before anybody graded it, and the record should show that.** The repair was left uncommitted in the worktree by an agent killed mid-flight; a third agent's pathspec commit `9af7e2d3` ("Thirty blind spots the audit had written down and never once shown anybody") then carried `scripts/self_audit.py` and the untracked `sdk/tests/test_empty_set_is_not_agreement.py` into history beside its own unrelated work, because **a pathspec isolates by file and not by author**. That agent named the carried work in its message, did not claim it, and did not close this row. This note is the grading, done afterwards, and it would have been the same verdict either way. **NON-AUTHORSHIP is attested by the dispatch record and NOT by the repository, exactly as D124 and D132 say it must be:** every commit on this box carries the one synthesised `Ubuntu <ubuntu@ip-172-31-43-247...>` identity, so the author field cannot discriminate; what is available is that this grading session was dispatched to verify code it found already written, and it wrote none of the guards it graded. **VERIFIED BY EXECUTION, not by reading the docstring, which is the instruction that produced most of what follows.** **21 of `self_audit.py`'s 34 checks changed verdict-on-absence**, measured by running all 34 old-against-new in ONE process in a stripped tree (only `scripts/self_audit.py` plus a symlinked `sdk` so `_uq_module()` imports): **19 PASS -> UNKNOWN** (`ledger_stalls`, `campaign_json_citations`, `evidence_paths_exist`, `ungated_completed_runs`, `statistical_labels`, `withdrawn_numbers`, `rung_estimates_state_their_iterations`, the five sharing `_py_sources`, and the six sharing `models/curriculum/uq-studies/`); **1 FAIL -> UNKNOWN** (`check_ledger_integrity`, the sibling half); and **1 that returned no verdict at all** -- `check_fd_grades_current_standard` RAISED `FileNotFoundError` on an absent `ACTIVE_RESEARCH.md`, a crash rather than a false green, repaired in the same pass and NOT mentioned by the work that repaired it. **THIS ROW'S OWN NUMBERS RE-DERIVED RATHER THAN TAKEN ON REPORT:** ledger present -> WARN, 6 stall rows, 26.98 of 239.259 published core-hours, the share recomputed straight off the 208,194-row file at **11.2754%** (the check prints 11.3%, and the six rows are the two 2026-07-23 and one 2026-07-24 clusters across `vspaero-wing` and `openfoam-cylinder`); ledger absent -> the PASS this row names, with `check_ledger_integrity` FAIL on the same nothing. Both reproduced in one process against HEAD's code and the old code side by side. **THE SETTLED VERDICT IS UNKNOWN IN BOTH AND THE ARGUMENT IS WRITTEN AT THE CODE RATHER THAN ASSUMED:** the ledger is gitignored and batch-written, so its absence is a fact about the box and not about the record, and neither PASS nor FAIL follows. The loud case the FAIL stood in for -- a published counter with no ledger under it -- was measured surviving in `check_wall_counters_vs_ledger` (`missions_run: published 208102, ledger says 0`), and that check now separates SUBJECT from EVIDENCE inside one body: `wall.json` absent stays FAIL, ledger absent is UNKNOWN, because three counters "disagreeing" with a zero the function invented diagnoses the wall for a file that is not there. `check_gate_table_vs_transcripts` and `check_register_group_counts` keep FAIL-on-absence on the same distinction and are named in the code as the two deliberate exceptions. **THE MUST-NOT-MATCH HALF, WHICH IS THE ONE THAT MATTERED HERE (L-84):** all 34 checks were re-run on a FULLY POPULATED mirror of this tree, old against new -- **ZERO status flips**. A present ledger carrying a genuine stall still WARNs and NAMES it (solver, 7200 s, timestamp); a present clean ledger still PASSes, and that PASS now carries its denominator (`no row of N`) as a second, independent barrier so a reader meets the zero even where a status lies; a present torn ledger still WARNs on the torn line. The thirteen checks whose coverage was added were each shown non-UNKNOWN against the live tree in the same process (**PASS 6, WARN 4, INFO 2, FAIL 1**). So the fix did not replace a false green with an instrument that says UNKNOWN everywhere. **MUTATION: 14 mutants, one per guard site, ALL KILLED**, control and mutant in ONE invocation with every `__pycache__` purged before each cell, control green at both ends. **AND THE HALF THE WORK DID NOT HAVE WHEN IT WAS FOUND, which is the same defect one level up and is why the test in `9af7e2d3` is 448 lines rather than 341:** as recovered it proved **6 of the 14**. **Eight guard sites survived deletion with the whole suite still green** -- the seven single-source checks and the one guard **six** checks share, whose test was NAMED `test_an_empty_study_directory_is_UNKNOWN_and_not_all_zero_studies` and asserted only the POPULATED arm. The `self_audit.py` repair was correct on all thirteen when executed directly; it was the EVIDENCE for eight of them that did not exist. `TestEveryGuardedCheckOnAnAbsentSource` closes it (19 -> 21 tests, 8 -> 34 subtests) and the failure is written into the test's own docstring rather than quietly fixed. **The recovered file was also headed D124** -- the R-ISOLATE authorship row, nothing to do with empty sweeps -- **and is corrected to D123**; D118 stays cited as the precedent for the UNKNOWN status. **FULL SUITE at the graded tree: 1,839 passed, 273 subtests passed, 1 failed, 17m49s, every `__pycache__` purged first.** The one failure is `test_exec_bits.py::test_no_shebang_script_is_both_unexecutable_and_unregistered` on `scripts/check_normative_clauses.py`, which is PRE-EXISTING, belongs to another family, landed at `1c4a643f`, is unmodified in this pass, and is the exact failure D134 predicts; it is unrelated to this row. **COST, STATED:** on a fresh clone or in the laptop bundle many of these now return UNKNOWN, which exits 3 and BLOCKS under `lab_check.py`'s EXIT_CONTRACT -- intended, since a clone that cannot read the evidence cannot self-audit, and it previously reported that as a pass. On this box, tree populated, nothing changed. **NOT DONE, and three of this row's four items stay OPEN:** `check_convergence_validate.py`'s 8-of-13 known-answer cases resting on a gitignored log; `test_log_signatures.py`'s two `if external.exists()` guards and its four `@skipUnless` sites; and reporting SKIPPED-FOR-MISSING-EVIDENCE separately in the suite summary. No check was retuned, no threshold moved, and the six real stall rows report exactly as before. **CANNOT SEE:** whether any PAST self_audit run reported a PASS that was in fact an empty sweep -- the runs left no per-check evidence counts behind, which is precisely the disclosure this row asked for and the reason the repair prints denominators now. | infra family (`check_ledger_stalls`); fleet (the population) |
| D124 | **R-ISOLATE's central requirement — a rung closes when a NON-AUTHOR measures it — cannot be verified from this repository at all, because git's author field does not discriminate between agents on this box.** Measured over the full history: **1,837 commits, 4 distinct author identities**, and the distribution is not a spread but a floor. **Since 2026-08-01: 1,165 of 1,165 commits — 100% — carry the single shared `Ubuntu <ubuntu@ip-172-31-43-247.us-east-2.compute.internal>` identity.** Today alone, **102 of 102**. The only other identities are `Sanaa Mouzahir` (a human, last commit **2026-07-26**, before the agent era) and `Claude <noreply@anthropic.com>` (**11 commits, 2026-07-30 to 2026-07-31 only**). **The one per-commit field that does vary is uninformative for this purpose:** the `Co-Authored-By` trailer names a MODEL, not an agent — since 2026-08-01 it takes five values across 1,165 commits (`Claude Fable 5` 393, `Claude Opus 5 (1M context)` 343, `Claude Opus 5` 137, `Claude Sonnet 5` 3, `Claude Haiku 4.5` 1), so any two of the *hundreds* of distinct agent sessions running the same model produce byte-identical authorship metadata. **Consequence, and it is structural rather than incidental: every non-authorship claim in this lab rests on dispatch records that live OUTSIDE the repository** — session logs, briefs, and the timestamp reasoning agents do by hand. The V13 PENDING-2 pass demonstrated the only available substitute and named it correctly as provenance rather than authorship: it proved it wrote none of `472f9f92`, `2b251689`, `7cd558b1` by showing all three were authored **2026-08-11 00:18-00:31 UTC** while its session opened **2026-08-15 01:56 UTC** (`LADDER_V_V13_PENDING2_MEASURED_2026-08-15.md` §0, at `dde6e1cf`). **That argument works only across days.** Two agents dispatched the same hour — which is this box's normal operating mode, with three to five concurrent — leave no repository-internal evidence that separates them, so a same-session R-ISOLATE claim is currently unfalsifiable. **This is a fixable convention, not a constraint:** the 11 commits of 2026-07-30/31 prove the author field is configurable on this box. | D118 repair pass, 2026-08-15, at `3af7bd2b`; the shared-identity observation originates with the V13 PENDING-2 pass and is confirmed and quantified here rather than taken on report. Frame: `git log --format` over the full history and over three date windows; author, committer and body trailers all counted separately. Cannot see: whether any dispatch record outside the repository *does* establish non-authorship for a given rung — this row measures only that **git cannot**, and says nothing about whether past R-ISOLATE closures were in fact independent. | Two options, and the choice is Katie's or the chief's because it changes how every future rung is closed. **(a) Make the metadata carry it:** set a per-agent `GIT_AUTHOR_EMAIL` (or a mandatory `Agent-Id:` trailer) at dispatch, after which non-authorship becomes one `git log --author` away and is verifiable by anyone forever. **(b) Keep the shared identity and stop claiming otherwise:** state plainly in R-ISOLATE that non-authorship is attested by the dispatch record and NOT by the repository, and require every rung closure to cite the dispatch artefact it rests on. What is not available is the status quo, in which closures read as though the repository backed them. Neither option needs compute; (a) is one line in the dispatch path. **CLOSED 2026-08-15 AS SUPERSEDED BY D132**, which is the row that carries the executed half. Option (a) is built and anchored (`e933e31b`, `44ac957a`): `python3 scripts/check_rung_attribution.py --emit-trailer` emits a `Lab-Agent:` trailer and the checker runs clean. **THE RULING HALF OF THIS ROW IS NOT SETTLED AND IS NOT CLOSED BY THIS NOTE** -- it is carried forward verbatim in D132's owner column, where it is the chief's (whether the rule binds, and whether briefs carry the line) and Katie's (whether it must be machine-checkable). Adoption is measured, not assumed: the checker reports 4 of 11 post-anchor commits carrying a trailer and 7 UNKNOWN. Cite D132, not this row. | chief (the rule); Katie (whether rung independence must be machine-checkable) |
| D125 | **A module-level cache with no invalidation is a shape, not an instance, and the sweep after D111 found five more -- two of them in the module D111 repaired, and not one of them red today.** The frame decides this answer, so it is stated first: every one of the **267 tracked `.py` files under `sdk/` and `scripts/`** (`git ls-files`, never the shell's `grep`, which is `ugrep --ignore-files` and would have hidden roughly a quarter of this tree), parsed with `ast` and searched for module-level names bound to a mutable object and then mutated from inside a function, plus every `functools.lru_cache`/`@cache` and every import-time file read in the same frame. **Eight mutated globals, five caches, two import-time snapshots.** Repaired: `agenda._REFUSED_INBOX` (D111, at `297b82a0`). **Leaks between tests, none red today:** (a) `sdk/chief_engineer/agenda.py:557` `_REFUSED_COST_BASES`, appended by `cost_basis_violations()` and cleared by nothing -- probed live, three IDENTICAL calls leave three entries, which is the unbounded growth already noted as A-5 in `docs/standards/INFRA_FAMILY_SUPERVISION_GUIDELINES.md`, and `sdk/tests/test_agenda.py:285` writes an entry that outlives its test; (b) `agenda.py:639` `_UNSCORED_KINDS`, added to by `gain_points()` for any unknown `source_kind` and cleared by nothing -- probed live, `test_agenda.py:483` plants `somekind-nobody-scored` in it for the life of the process; (c) `sdk/chief_engineer/server.py:231` `_missions`, filled from the state directory by `_rehydrate_missions()`, which opens `if _missions: return`, so it reads ONCE per process and a second state directory is never read -- `test_agenda.py:683` onward already works around it by asserting DELTAS rather than absolutes, and the workaround is what keeps it green; (d) `sdk/scripts/pope_1975_basis_check.py:112` `CHECKS`, appended by `record()` and never cleared by `main()`, so two `main()` calls in one process double the list -- and `sdk/tests/test_pope_basis.py:37` clears it in the test's own `setUp`, which is the anti-pattern exactly: the same bug with a longer fuse, held off by the one caller that remembers. **The same shape one layer up:** `scripts/self_audit.py:427` `_closure_facts()` is `@lru_cache(maxsize=1)` on a function taking NO arguments that reads `LIVE_BOARD` and `CASES` off disk -- a whole-process snapshot of the board with nothing that can invalidate it; `cache_clear` appears nowhere in the tree, and `sdk/tests/test_rank_claim_surfaces.py:406` substitutes the function object instead, which is the workaround this shape forces. **Deliberately NOT counted, and why:** `scripts/check_derived_figures.py:436` `P_RANK1_NOT_SHIPPED` is the counter-example and the model -- `build_registry()` clears it at the top, so its lifetime is the build that fills it, which is the shape D111's repair adopted; `sdk/chief_engineer/log_signatures.py:362` `_ENVELOPE_MEMO` is keyed by `(path, mtime_ns, size)`, so a rewritten ledger is re-read, and it is named here as how to key a disk cache; `sdk/tests/test_register.py:341` `_ACTS` is a documented run-once corpus shared across TestCases in one file, in-process and not from disk; the three other `self_audit.py` caches and `head_engineer.py:907` `_pattern` are keyed by their arguments and pure; the two import-time reads (`test_sweep_frames.py:387`, `test_rank_claim_surfaces.py:115`) are read-only snapshots rather than accumulators. | swept 2026-08-15 at `297b82a0`; frame is the AST sweep above over `git ls-files -- sdk scripts` (267 `.py` files), and (a) and (b) were confirmed by importing `agenda` and probing the accessors directly rather than by reading the code | Bind each ledger's lifetime to the operation that fills it, or key it by what it was read from -- the two repairs D111 used. (a) and (b) have no read boundary to hang a lifetime on: they are filled per-proposal during validation and ranking, not per-read, so the fix is first a decision about what *"every basis this process refused"* should mean, and it deserves its own control pair rather than a passenger seat on someone else's edit. (c) is mechanical: key the rehydrate on the state directory rather than on emptiness. (d) is cheaper still: clear `CHECKS` at the top of `main()`, then DELETE `test_pope_basis.py:37` -- whether the suite stays green without the test-side clear is the test of whether the repair worked. `self_audit._closure_facts` is the one that matters most and is out of scope here (that file belongs to another pass); the fix is `_ENVELOPE_MEMO`'s -- key it on what it read. | agenda owner / self_audit owner |
| D126 | **Eight tests in the auto-stop suite go red whenever any solver is running on this box, and the whole of it is a harness that fabricates an idle world and then lets the gate look straight past it.** `sdk/tests/test_autostop_gate.py`'s `_idle_env()` overrides `MARKER`, `REPO` and `SESSIONS` and hands the gate a temporary tree, and `test_autostop_session_glob.py` steers `SESSIONS` the same way -- but clause (1) of the gate, `scripts/auto-stop.sh:82`, is a `pgrep -x` over the HOST's process table against an alternation of eleven solver and mesher names (`simpleFoam` through `vspaero_opt`), with no override and nothing in the environment that could give it one. So the first clause answers about the real machine while the other two answer about the fixture, and the first clause wins, because it `keep`s and exits before the others run. **Measured 2026-08-15.** The full suite at `0b0041b1` returned **10 failed, 1731 passed** in 17m10s; 8 of the 10 were these, and every one of them failed on the identical string `'ALIVE: solver or mesher running'` -- `pgrep -x` against that exact alternation, while the run was in flight, listed **nine live `vspaero` processes** from another session. The same suite re-run at `6ff9fb43`, after those processes exited and with nothing in the tree touching these files, returned **1767 passed, 0 failed**, all eight of them green. **This is D111's trap with a different mechanism**: run one of the eight alone on a quiet box and it passes, which reads as flakiness and is in fact an unrepaired dependency on host state -- and this lab has now twice recorded "passes in isolation" as evidence of transience when it was evidence of contamination. | measured 2026-08-15: full suite at `0b0041b1` (10 failed / 1731 passed) and at `6ff9fb43`; cause confirmed by running `pgrep -x` against the alternation in `auto-stop.sh:82` during the first run, and by the eight going green once the box was quiet with no change to any file they read | Give clause (1) the same override the other two clauses have -- a `SOLVER_PGREP`-style hook the harness can point at a fabricated table -- and then assert that the override is what decides the planted cases, which is the control `TheHarnessIsShownToSteerTests` in `test_autostop_session_glob.py` already applies to `SESSIONS`. Until that exists, a red in these eight is un-attributable without `pgrep` output from the moment of the run, and the correct reading of a lone green re-run is *"the box was quiet"* rather than *"transient"*. | auto-stop gate owner / D96, D109 |
| D127 | **[CORRECTION TO D120's RULING, by a non-author of it, executed 2026-08-15 at `e639796e`. `0.001365` IS CORRECT, IT IS A ROUND AND NOT A TRUNCATION, AND NOTHING SHOULD BE CHANGED.]** The ruling directed that the margin be written **0.001366**, on the ground that `0.058013 - 0.056647191704213645 = 0.001365808295786355` and *"the basis here is `0.058013`, a six-decimal printing"*. **That subtraction is exact and I reproduce it; the premise about the basis is what fails.** `0.058013` is not the basis. It is the six-decimal PRINTING OF the basis. The basis is the board's eight published per-case values for Yang, held at `sdk/scripts/probability_of_rank.py:82` -- `[0.0806, 0.1229, 0.0645, 0.0748, 0.0291, 0.0311, 0.0250, 0.0361]` -- whose mean is **exactly 0.0580125**, and `0.058013` is that mean rounded half-away-from-zero at the trailing 5. Against our `0.056647191704213645` the margin is therefore **0.001365308295786355**, whose six-decimal half-away-from-zero rounding is **0.001365**. **The lab's own committed record already holds this number**: `sdk/scripts/probability_of_rank_record.json` -> `pairwise.Yang.margin = -0.0013653082957863563`, and `pairwise.Yang.overall = 0.0580125`. So every `0.001365` in this corpus is the correctly-rounded six-decimal value of the record's own margin, and `0.001366` is what you get by subtracting a PRE-ROUNDED input -- which is the exact defect class **D104** was filed to repair, in this exact quantity, three rows ago: *"the subtraction was carried out at full precision, the input was not."* Implementing the ruling would have pushed a 5e-7 error into every surface that currently reads correctly, under the name of a correction. **INSTANCES CHANGED: ZERO. INSTANCES DELIBERATELY LEFT: ALL OF THEM.** The convention clause was also NOT written, and deliberately: it was dispatched with this figure attached, and a charter clause is the last place a false premise should be parked. **The form rule is still worth having and is untouched by this** -- a lab that rounds in one place and truncates in another is unreadable either way -- and it should be re-issued as form alone, naming no figure, exactly as `CLOSURE_FAMILY_SUPERVISION_GUIDELINES.md` §4 (b-1) does. **One further question the re-issue has to answer, at lower severity:** Yang's per-case values are themselves the board's FOUR-decimal printings, +/-5e-5 apiece, so the mean `0.0580125` carries an interval far wider than its sixth decimal and the margin's honestly-supported precision is nearer **0.0014** than either six-decimal form. `check_derived_figures.py` already reads it that way and passes both. So the convention must say whether this lab quotes to the digits of the LAST rounding it performed or to the digits its ULTIMATE inputs support; those are different rules and they give different answers here. **[ADDENDUM, same author, same day, at `85090c53` -- THE TRUNCATION DIAGNOSIS IS FALSIFIED OUTRIGHT, NOT MERELY UNPROVEN.]** The ruling calls `0.001365` *"a truncation, where the family otherwise rounds"*. The family it sits in is the six-row margin table at `campaign/BOARD_RESCORE_2026-08-14.md` §3.1, whose header states its own basis -- *"Board scores are the means of the published per-case values (shown to 6 dp; the board publishes them to 4 dp)"*. Two tests on all six rows. **(1) Is the table truncated? No, and three rows prove it:** truncating `printed overall - ours` gives `0.002877`, `0.005764` and `0.017039` against the table's `0.002878`, `0.005765` and `0.017040`. A truncation convention cannot produce those. **(2) Which basis is the table on? The MEANS, on all six, and three rows discriminate:** Yang, Tian/Buchanan/Hickel/Dwight and Montoya/Oulghelou/Cinnella come out `0.001365`, `0.007490`, `0.021215` from the full means and `0.001366`, `0.007491`, `0.021216` from the printed overalls, and the table carries the mean-based value in all three. **So the lab already rounds, already rounds half-away-from-zero, and already rounds from the means rather than from the printings -- consistently, across a whole table.** `0.001365` is not the odd one out; it is one of three figures the ruling would have broken, and the other two are not mentioned anywhere in it. **[RULING AUTHOR'S RESPONSE, 2026-08-15, at repo `85090c53`. THE REFUTATION IS ACCEPTED IN FULL. My ruling was wrong, `0.001365` stands, and zero instances should change.]** The error was mine and it is worth naming precisely rather than generally: I took **a printing of the basis for the basis**, which is the identical mistake D104 was filed to repair *in this exact quantity* — and I made it while citing D104. That the refuting agent declined to write my convention into a charter because *"a charter is the last place a false premise should be parked"* is the correct call and I would not have made it as cleanly. **THE CONVENTION, RE-ISSUED AS FORM ALONE AND NAMING NO FIGURE, per this row's own request.** (1) **A derived quantity is computed from the most precise available inputs, never from a printing of them.** Where an unrounded value exists in a committed record, subtracting a rounded display of it is a defect regardless of which direction it moves the result. (2) **Figures are rounded, never truncated**, half-away-from-zero — this half of the original ruling survives, because it was never the part in dispute and the lab already does it consistently. (3) **A quoted figure carries its basis**, so a reader can tell which of (1)'s two paths produced it. **AND THE QUESTION THIS ROW HANDS BACK IS ANSWERED WITH A MEASUREMENT RATHER THAN A RULE, because I have just demonstrated what my unmeasured arithmetic is worth.** The row asks whether the lab quotes to the digits of the LAST rounding it performed or to the digits its ULTIMATE inputs support — noting that Yang's per-case values are themselves four-decimal printings, so this margin's honestly-supported precision may be nearer `0.0014` than either six-decimal form. **I am not ruling that surfaces be rewritten to a coarser figure.** The rewrite I ordered on unmeasured reasoning would have injected an error into three correct figures; ordering a second one on the same kind of reasoning would be the same mistake with the sign flipped. What is wanted instead: **compute the interval the four-decimal per-case printings actually imply, then report whether any LIVE claim's direction depends on digits outside it.** If none does, present quoting is harmless and needs only a note naming its basis. If one does, that claim needs an interval rather than more digits — and the claim to check first is the one where this matters, the seed bound against the margin, since a ratio near unity is exactly where a hidden interval changes a conclusion. Filed as the successor question; **nothing changes until it is measured.** **[ANSWERED BY MEASUREMENT, 2026-08-15, by a third party to both D120 and its refutation — `demo-output/website/campaign/MARGIN_PRECISION_INTERVAL_2026-08-15.md`. NO LIVE CLAIM'S DIRECTION DEPENDS ON DIGITS OUTSIDE THE INTERVAL, AND NOTHING SHOULD BE REWRITTEN.]** The interval was propagated by interval arithmetic (no Monte Carlo) from the board's 4-dp per-case printings, half-ulp +/-5e-5 apiece, and tightened by intersecting each entrant mean with the half-ulp interval of the board's own published 4-dp overall -- a second printing of the same quantity that `probability_of_rank.py`'s reproduction check already ties to the first, and which narrows five of six rows below 1e-4. Every step is affine with each input appearing once, so the enclosure is the exact image of the box, not an over-enclosure. **The ratio checked first and hardest, the seed bound against the margin: [172.45%, 183.92%], point 177.19%. It does NOT span 100% and does not come within 66 points of it** -- reaching unity would need the printings wrong by twenty-one half-ulps -- and under the opposite assumption that the board truncates it is [166.51%, 177.19%], which also does not. **Eleven live claims were enumerated and every direction survives**: the adverse leg (0.059047 clears Yang's whole interval by 9.97e-4 and still lands at rank 2, below Reissmann's whole interval), lowest-on-board (26 half-ulps of slack at the narrowest), both decided pairs (Liu P in [98.40%, 98.92%] against the 0.98 threshold, |t| in [2.1915, 2.2152] against 2.0), all four undecided pairs (nearest is 11.7 points below threshold), P(rank 1) (+/-1.76pp against a shipped interval 96.7 points wide), and all eight leave-one-out ranks, each certain. **This row's own guess is also refuted, in the other direction:** the margin's supported precision is not `0.0014` either -- |margin| lies in [0.0013153, 0.0014028], which straddles 0.00135, so even the FOUR-decimal rounding is undetermined and only one significant figure is stable. A coarser single figure is not more honest than a finer one; it is a different unsupported claim, which is why the answer is an interval and not a digit count. **The convention the measurement recommends is neither of the two the question offered:** print at the precision of the derivation, and state the input uncertainty as an interval where the quantity is defined -- digits record what was computed, an interval records what is known, and deleting digits conveys neither. One figure in the corpus IS unsupported by the interval and it is not a direction: **D133**. **EXECUTED AND CLOSED 2026-08-15; every open item discharged.** The successor question was measured at commit `eadcd112`: `campaign/MARGIN_PRECISION_INTERVAL_2026-08-15.md` gives a seed-bound over margin interval of **[172.45%, 183.92%]** with point 177.19%, states "spans 100%: NO", and carries a truncation-model robustness row beside it -- so the interval flips no live claim. The convention this row proposed, which until then existed ONLY inside this docket row, was adopted as FORM at commit `638b9af2` into `docs/charters/REPORTING_CHARTER.md` section 10, **naming no figure**, which is what stops it going stale with the number. **WHAT WAS NOT DONE:** the row's own residual was D133, which is repaired separately at `09234034` and closed on its own row rather than folded in here. | D120's ruling author / D104, D120 |
| D128 | **A THIRD generator writes the unattributed untrained-QCR claim, found by the V5 sweep at `e639796e` and not repaired there.** `sdk/scripts/closure_eval_battery/build_master_table.py:246` and `:371` both emit *"round 5 (untrained QCR2000 duct forward solve, the sixth pre-registered scoring call)"*, into two TRACKED surfaces -- `demo-output/website/closure_eval/closure_eval_master_table.md:20` and `closure_eval_master_table.json:84` -- and the file contains **zero occurrences of "Spalart"**. Under the chief ruling of 2026-08-15 (`3af7bd2b`), whose frame is *every surface a future build or edit can change plus the generators that write them*, this is in scope and it is the same defect `e639796e` closed one file to its left. **Not repaired in that commit, and the reason is the reason it needs an owner:** its two outputs can only be regenerated by re-running the eval battery, which re-parses the live leaderboard README at `~/closure-challenge-benchmark/README.md` and would move figures across blocks B and C that this dispatch had no mandate over; and hand-editing the two outputs instead would recreate exactly the generator-vs-output split that keeps V5 open. So it is one edit plus one battery re-run, by whoever owns the battery. **How it was found, because the finding method is the transferable part:** the near-miss `e071075d` asked what writes the FILE; this sweep asked what writes the SENTENCE, across all three arms -- 20,682 tracked via `git grep`, 2 untracked, and 37,244 gitignored via `git ls-files --others --ignored --exclude-standard`. Fifteen tracked source files of any language name `QCR` case-sensitively and every one was opened for a write path. The gitignored arm holds no generator at all, only `dist/` copies. Also checked and CLEAR: `scripts/add_proposals_supervisor_review_2026_08_07.py:129` writes the claim into `agenda/proposals/f6b-qcr2000-on-the-hills.json`, and its own citations list carries Spalart in the same record, so the surface it writes is attributed. | closure eval battery owner / Ladder V rung V5, `3af7bd2b`, `e639796e` |
| D129 | **The guard that would have caught both of V10's live failures is bounded to one line and wired to one surface, and the proof is a whitespace mutation.** `scripts/self_audit.py::_best_on_board_faults` is a good instrument: it holds no literal count, derives it from `LIVE_BOARD` in `sdk/scripts/probability_of_rank.py`, and faults a wrong count, a missing baseline disclosure and an unstated model-earned zero. Driven directly at `99c66254` with its paths bound to the repository, it returns **3 faults** on `docs/PRODUCT_LIST.md:79` verbatim and **3 faults** on `demo-output/website/ACTIVE_RESEARCH.md:670-682` verbatim — both of them V10's own named surfaces, and both stale at HEAD. In production it returns **zero** on both, for two independent reasons and neither is about the text. (1) **Reach**: its only caller feeds it the credentials wall's `our_entry` string, so it never opens either file. (2) **Line-boundedness**: `_BEST_COUNT` is `\bbest\b[^.\n]{0,80}?…`, and `[^.\n]` cannot cross a newline. Mutation-proved on `demo-output/website/closure.html:352-354`: the bytes as written return **0 faults**; the identical bytes with whitespace collapsed return **3**, the newline falling between the phrase naming the count and the figures. That is L-61 and V16's own defect class in the sibling guard V16 did not cover, and V16's ruling `8bdda313` already requires whole-text-with-whitespace-collapsed matching — `check_board_placement_words` complies, this one does not. Positive controls for every negative above: the real wall text is clean, a count mutation and a disclosure-strip mutation both fire. | Ladder V, V6/V10 closure grade, 2026-08-15, `LADDER_V_V6_V10_GRADE_2026-08-15.md` §5d. Executed against `git show HEAD:scripts/self_audit.py` in a scratch copy with `_HERE`, `REPO` and the three path globals rebound to the repository, because the working copy is dirty from a concurrent V5 repair; `__pycache__` purged before the cell | Collapse whitespace before matching, as the placement guard already does, and give the check a caller that reads the surfaces V10 names rather than only the wall. The reach half is worth more than the pattern half: a guard whose predicate is right and whose caller sees one file reports a clean corpus it never opened, which is the silent-zero shape rather than a miss. Do not widen the pattern without a false-positive measurement — V16's own criterion is that a guard which cries wolf gets switched off. | self_audit owner |
| D130 | **Independence — the rule this ladder is built on — is not verifiable from the repository, by anyone, ever.** R-ISOLATE's premise is *"no agent verifies work it produced"*, and every grade in this campaign asserts its own independence. None of those assertions can be checked from the tree. Git metadata cannot discriminate: all commits on this box carry one identity, `Ubuntu <ubuntu@ip-172-31-43-247…>`, which two graders established independently on 2026-08-14. Session start time cannot either: this grade's session container opens **2026-08-04T14:54:48Z**, eleven days before the work it clears itself of, and a prior grader hit the same shape at five hours. What actually settles it is the **dispatch instant** in the agent-thread transcript — for this grade, an `Agent` tool call `toolu_01CPYmdHQvvpMVKePoGzVkpi` at **2026-08-15T02:36:58.876Z**, against six commits the latest of which is 31m55s older. **That transcript lives at `~/.claude/projects/…jsonl`: untracked, per-machine, outside every git frame, and not reachable by `git check-ignore` because it is not under the tree at all.** A reader who clones this repository can read any grade's independence section and verify none of it. The consequence is not hypothetical: the ledger has already carried a rung as *"self-graded"* for four days after seven independent grades existed, because the only evidence that they were independent was outside the record. | Ladder V, V6/V10 closure grade, 2026-08-15, `LADDER_V_V6_V10_GRADE_2026-08-15.md` §1. Measured: the transcript parsed for the first appearance of this task; all six commit timestamps read with `git log -1 --format=%aI` | Decide whether independence is a **claim** or a **record**. If a record, the durable half is cheap and is not a tool: a grade states its dispatch instant and its thread identifier in the document, so a later reader has something dated to compare against commit times even though the underlying transcript is gone. If a claim, then say so in R-ISOLATE, and stop writing ledger columns headed *"independently confirmed?"* as though the answer were checkable. What must not continue is the present state, where the strongest rule in the campaign is enforced by evidence that leaves no trace. This row was filed by a grader recording how its own independence was established, because the next reader cannot re-derive it. | Katie / chief — R-ISOLATE is hers |
| D131 | **V10's closing condition is indexed to no board referent, and since the two-referent ruling that makes it undecidable.** The rung reads *"closure.html, the Active Research board, the wall, PRODUCT_LIST, and the submission package must all carry round-5 numbers with the same caveats"* (`LADDER_V_TRIPLE_VERIFICATION.md:133-136`). It was written before the board moved at 2026-08-11T23:33Z, and it names no board. Since `d2d6bd6c` this lab holds **two** legitimate referents — the frozen scoring commit, which by that ruling's own words *scores but does not rank*, and the live six-entry board — so a surface pinned to either satisfies the criterion as written while disagreeing with a sibling surface on **every ordinal**: the placement, the entrant count, the per-case standings and the best-on-board count all differ between them. Two of V10's five surfaces are currently pinned one way and two the other, and the criterion cannot say which is wrong. **This is D122's shape on a different rung**: a closing condition that a later ruling silently re-scoped, being graded by whoever transcribes it next. The 2026-08-15 grade declined to resolve it by choosing a referent — choosing one would be the bar moving under the measurement, which `3af7bd2b` forbids — and graded V10 on the half the ambiguity does not touch, the three figures withdrawn lab-wide on 2026-08-12 that have no surviving referent under either board. | Ladder V, V6/V10 closure grade, 2026-08-15, `LADDER_V_V6_V10_GRADE_2026-08-15.md` §5e | One ruling, and the same ruling settles D122: say by name which referent a cross-surface rung is graded against, and whether a surface may pin itself to the other provided it says so. Until then every future V10 round re-argues it. The durable half: **a rung criterion written before a referent split does not survive the split, and it fails silently — it goes on passing and failing surfaces on a question it no longer asks.** | chief (the same frame ruling D122 is waiting on) |
| D132 | **D124's option (a) is built and anchored: commit authorship now travels in the artifact, and the honest headline is that it reaches forward only — every independence claim about the 1,849 commits before `e933e31b` still rests on records outside this repository, and always will.** A `Lab-Agent: <host>/<session-uuid>/<tag>` trailer is emitted from `$CLAUDE_CODE_SESSION_ID` by `scripts/check_rung_attribution.py --emit-trailer` and lands in the commit message, which is part of the commit object and therefore travels with `clone`, `fetch`, `format-patch` and `archive`. Rejected, with reasons: **`GIT_AUTHOR_EMAIL`** — decisive, this harness does not persist shell state between tool calls, so an `export` dies after one command; it is also destructive rather than additive, and author and committer diverge under a cherry-pick so the two fields disagree about who did what. **`git notes`** — `refs/notes/*` is not cloned, not pushed, and not copied by a cherry-pick: evidence stored *beside* the artifact instead of *inside* it, which is the same defect as `check_bundle_drift()`'s gitignored reference. **Per-agent GPG** — unforgeable, but key material cannot be compressed into one line of a dispatch brief, and an unadopted strong mechanism is weaker than an adopted weak one. **Overloading `Co-Authored-By`** — two meanings in one key makes the 1,217 existing values ambiguous. **The granularity is a session, not an agent, and this is the finding inside the repair:** every subagent of a chief inherits the same `CLAUDE_CODE_SESSION_ID` and the same `CLAUDE_PID`, and the per-call shell pid changes every tool call, so **no per-subagent handle exists in the environment at all**. Two agents dispatched by one chief read as the same agent. The price is explicit: **a rung's grader must be dispatched from a different chief session, or its independence is not machine-checkable and rests on dispatch records exactly as it does today.** The self-declared `<tag>` is recorded, printed, and **never decides a verdict** — it is the one field an agent types, therefore the one a copied brief duplicates by accident. **Threat model, stated in the code and the doc:** an honesty instrument, not a security boundary — it resists accident and nothing done on purpose; a `cherry-pick` replays the *original* agent's trailer onto a *new* sha (pinned by a test, not asserted); NON-AUTHOR means "no evidence of the same session", never "provably different agents". **It degrades to UNKNOWN and never to independent:** absent, malformed, duplicated and pre-anchor trailers all yield no identity and a reason naming the commits; the one deliberate exception runs the safe way, since a part-unattributed graded set containing one same-session commit returns AUTHOR, and AUTHOR can only ever *deny* a closure. | Built and measured 2026-08-15 against D124; mechanism at `e933e31b`, anchored and documented at the follow-up commit. Frame for the re-measurement: `git log HEAD` with `--pretty='%an <%ae>'` at `e639796e`, 02:51Z — **1,849 commits / 4 author identities; 1,217 of 1,217 since 2026-08-01T00:00Z and 36 of 36 that day carry the single synthesised `Ubuntu <ubuntu@ip-172-31-43-247…>` identity**, confirming D124's ratio on a moved denominator (D124 read 1,837 / 1,165 / 102 a few hours earlier). The "convention, not constraint" claim is measured, not inferred: there is no `user.name` or `user.email` in `.git/config` and `~/.gitconfig` does not exist, so git synthesises the identity from the unix user and the hostname. `scripts/check_rung_attribution.py` answers AUTHOR / NON-AUTHOR / UNKNOWN for a closing commit against the commits it grades, cannot PASS from an empty set (class B1), prints its frame and its two disclaimers on every run, and is admitted by `scripts/lab_check.py` by AST as `gate: main() can exit non-zero (return codes[verdict])` — an admission asserted by its own test rather than checked once by hand. Controls both ways per L-84 in `sdk/tests/test_rung_attribution.py`, every case a real git repository in a tempdir: a genuine non-author verifies (NON-AUTHOR, exit 0), a same-agent pair is caught (AUTHOR, exit 1), a commit predating the mechanism returns UNKNOWN and specifically neither verdict, an empty graded set is UNKNOWN naming B1. 24 tests, 12 mutants, all killed, control and mutant in ONE invocation with every `__pycache__` purged before each cell. **Cannot see:** whether any grader EXECUTED what it graded — R-ISOLATE's other half — because a grader that read a summary and a grader that ran the code commit identical bytes; and whether any `Lab-Agent` value names a session that existed, since the transcript that would show it lives in a per-machine `~/.claude/projects/…jsonl` no clone contains. The mechanism makes *difference* verifiable from the repository and leaves *existence* unverifiable. | Two things, and neither needs compute. **(1) Put the one line in every dispatch brief** — ~~`python3 scripts/check_rung_attribution.py --emit-trailer >> <msgfile>`~~ **[SUPERSEDED at `62d5757e`, and the published wording corrected at `9416db99`: the bare `--emit-trailer` form is SESSION granularity and returns AUTHOR for every pairing inside one chief session, which is this box's normal mode. The line a brief quotes is `python3 scripts/check_rung_attribution.py --emit-trailer --probe <a-typed-token> >> <msgfile>`. Re-executed at `d91b101a` in D230's staleness sweep.]** before `git commit -F`, no per-agent configuration, no hook, no exported variable. Until briefs carry it, adoption stays near zero and the integrity check's PASS keeps meaning only "nobody lied", which is why the adoption ratio is printed loudly on every run. **(2) Rule on what R-ISOLATE now says.** Either rung closures must cite an attribution query and dispatch graders from a different chief session, or R-ISOLATE states plainly that non-authorship is attested by the dispatch record and NOT by the repository. What is still not available is the status quo, in which closures read as though the repository backed them. Adjacent and untouched: `check_bundle_drift()`'s 2026-08-11 result is permanently unrecoverable because its reference was gitignored — **a verification whose reference is gitignored expires the moment it is made**, and this row and that one are one finding wearing two hats. | chief (the rule, and whether briefs carry the line); Katie (whether rung independence must be machine-checkable, per D124) |
| D133 | **The one per-case comparison on the whole board that sits inside the board's own printing precision is published as a win, and a second live surface already says it is not.** `campaign/BOARD_RESCORE_2026-08-14.md` section 3.3 counts **5/8 cases won** against Wu & Zhang. One of those five is `AR_1_Ret_360`: our `0.04547044480564218` against Wu's printed `0.0455`, a gap of **2.96e-5**, which is **below the 5e-5 half-ulp of a 4-dp printing** -- Wu's true value lies in [0.045450, 0.045550] and ours lies inside it, so **whether we won that case is not determined by anything published**. It is the only such comparison in the 48-cell board: every other per-case gap is at least 26 half-ulps. **The corpus already contains the correct reading and the two surfaces disagree** -- `demo-output/website/CLOSURE_CHALLENGE_STATUS.md:571-572` states *"`AR_1_Ret_360` and `AR_3_Ret_360` are ties below published precision (0.00003 and 0.00008) and are not per-case wins or losses"*, and the measurement says STATUS is right on `AR_1`. (On `AR_3` the gap is 8.2e-5, above the half-ulp, so that one IS determined under a rounding model and STATUS's call there is the more cautious full-ulp reading rather than an error.) **Nothing turns on it and that is why it is filed rather than fixed:** the decided threshold is 7 of 8, the published verdict is *not statistically decided* at either 4/8 or 5/8, and the pairwise P and t are unmoved beyond 0.35pp and 0.02. | Interval propagation of the board's 4-dp printings, 2026-08-15, `campaign/MARGIN_PRECISION_INTERVAL_2026-08-15.md` section 3.1; `LIVE_BOARD` read by AST from `sdk/scripts/probability_of_rank.py`, our per-case values from `closure_challenge_round5_qcr.json` -> `round5_per_case_full`; every `__pycache__` purged before the cell | **NOT repaired, deliberately.** The cell is in a submission-facing table and editing it is a material change to a standing claim, which is the chief's to direct -- and D127 exists precisely because a sweep ordered on unmeasured reasoning injected error into three correct figures. The narrow repair, if directed, is to make the two surfaces agree: `4-5/8`, or `5/8 (AR_1_Ret_360 is a sub-precision tie)`, matching the wording `CLOSURE_CHALLENGE_STATUS.md` already uses. Do **not** change the verdict column: it is identical at 4 and at 5. **[DIRECTED AND REPAIRED 2026-08-15 by a third party to both this row and the measurement, after re-deriving all 48 cells rather than taking either on report. Two surfaces repaired, strike-and-keep, verdicts untouched.]** `campaign/BOARD_RESCORE_2026-08-14.md` §3.3 and `campaign/PROBABILITY_OF_RANK_SIX_ENTRY_2026-08-11.md` §3 now read **`4 or 5 of 8`** against Wu & Zhang with the struck `5` left visible, `AR_1_Ret_360` named as the undetermined cell and the half-ulp reasoning given. A bare `4/8` was deliberately NOT written: it asserts a resolution the published data does not support in the other direction, which is the same error mirrored. The face of each repair states that the direction is unaffected -- the decided threshold is 7 of 8 -- so it cannot be read as a retreat. The measurement's parked R3 basis note was placed **once**, at `BOARD_RESCORE` §3.1 where the margin is defined, giving the derived value, the supported interval `[0.00132, 0.00140]` and the ratio range 172%-184%; it is not scattered. **[AND THIS ROW'S OWN SUPPORTING ARITHMETIC IS CORRECTED, BY MEASUREMENT.]** The sentence *"every other per-case gap is at least 26 half-ulps"* is **FALSE** and was believed by at least two later readers. Measured over all 48 cells: the three smallest gaps on the board are ALL in Wu & Zhang's own column -- `AR_1_Ret_360` at **0.59** half-ulps, `AR_3_Ret_360` at **1.64** (8.216e-5) and `AR_14_Ret_180` at **6.77** (3.386e-4) -- and only OUTSIDE that column is the smallest gap 26.2 half-ulps (Tian, `alpha_15_13929_2024`, 1.312e-3). `26 half-ulps` is the slack on the **Yang OVERALL margin** from the same document's §2.3/C3, transplanted onto per-case gaps; the two are different quantities. **The row's headline claim survives intact and is independently confirmed**: exactly one cell of the 48 lies below the half-ulp, and it is this one. Also confirmed: no other cases-won cell moves -- Tian's `5/8` is exact (all eight of its cells determined), as are Yang's and Reissmann's `4/8` and both `7/8` rows. **EXECUTED AND CLOSED 2026-08-15, commit `09234034`.** The defect reproduced at `44ac957a` -- `BOARD_RESCORE_2026-08-14.md` carried Wu & Zhang at `5/8` while `CLOSURE_CHALLENGE_STATUS.md` called `AR_1_Ret_360` a sub-precision tie, so two published surfaces disagreed about the same cell. At HEAD line 153 reads `~~5/8~~ **4 or 5 of 8** ` with a dated repair note citing the +/-5e-5 half-ulp and the 48-cell re-derivation, and the two surfaces now agree. **THE DISCRIMINATING EVIDENCE IS THE RE-DERIVATION, NOT THE STRIKE:** exactly one cell of the 48 lies below the half-ulp and it is this one, so no other cases-won cell moves -- Tian's `5/8` is exact, as are Yang's and Reissmann's `4/8` and both `7/8` rows. A repair that had struck the cell without re-deriving the other 47 would have left the same class of defect everywhere else in the table. **Note the row's own arithmetic was wrong in passing:** "26 half-ulps" is not the figure, and the repair commit says so. | closure family / D127 |
| D134 | **This lab's mandated commit form structurally cannot set a file mode, so `sdk/tests/test_exec_bits.py` offers new files a repair no protocol-conforming agent can perform — and its own failure message names that impossible repair first.** `.git/config` carries `core.filemode = false`, which is correct for this box, and under it a PARTIAL commit (`git commit -F <msg> -- <paths>` — the only form every dispatch brief in this lab permits, precisely so that another agent's staged work cannot be swept in) **discards the index's mode and re-records `100644`**. Measured 2026-08-15 in a scratch repository with `core.filemode false`, four variants on one shebang-bearing file, and only the last works: **(A)** `git update-index --chmod=+x` then a pathspec commit → git reports *"nothing to commit"* and the mode is unchanged; **(B)** `--chmod=+x` plus a content change, pathspec commit → new blob, mode still `100644`; **(C)** `git add`, then `--chmod=+x`, then pathspec commit → the index reads `100755`, `git ls-tree HEAD` reads `100644`; **(D)** `git add`, `--chmod=+x`, then an index commit with **no pathspec** → `100755`. This is not theoretical: it happened in this pass. `scripts/check_rung_attribution.py` was `--chmod=+x`'d before `44ac957a` and that commit recorded `100644` anyway, and a second attempt to fix it produced *"nothing to commit, working tree clean"*. `test_exec_bits.py` was right both times — it reads `git ls-tree -r HEAD` on purpose, because its own first draft read `git ls-files -s` and passed on a mode that was staged and never committed (`0462b45b`). **The consequence for the register: under this protocol `WAIVED_NO_EXEC_BIT` can only ever grow.** Every new shebang-bearing file is offered two repairs, one of which the protocol forbids and the other of which is the waiver, so the waiver is not a grandfathering boundary that shrinks — it is the only exit. That is a different object from what the module docstring describes ("the register makes the gap countable and stops it growing"): it still stops the gap growing *silently*, but it cannot stop it growing. | Found 2026-08-15 while landing the attribution mechanism (`e933e31b`, anchored `44ac957a`); the four-variant measurement is a scratch repository under `/tmp`, `git init` with `core.filemode false`, one file, `git ls-tree HEAD` read after each commit. Not inferred from documentation. **Cannot see:** whether any of the 234 files already in the register got there this way rather than by never having been chmod'd — this row measures the mechanism, not its history; and whether `core.filemode = false` is still needed on this box, which is the question that would dissolve the problem rather than route around it. | Three options, and the choice is the chief's because it is a protocol rule. **(a) Amend the commit protocol** to permit exactly one extra step for a mode change — `git update-index --chmod=+x <path>` followed by an index commit with the same single path staged — and say when it is allowed, since the reason for the pathspec rule (never sweep another agent's staged work) is real and a bare index commit defeats it. **(b) Set `core.filemode = true`** on this box, after which `git add` carries the mode and the ordinary pathspec commit works; needs a check that no tracked file's mode flips as a side effect. **(c) Accept the register as permanent** and say so in `exec_bits.py`'s docstring, replacing "stops it growing" with what is true. Until one is chosen, `test_exec_bits.py`'s failure message should stop leading with `git update-index --chmod=+x`, because following it produces a green index and a red test. Immediate and already done: `scripts/check_rung_attribution.py` is registered with the reason, and `scripts/check_normative_clauses.py` (landed today at `1c4a643f`, a different family, not touched here) is still red on the same test for the same reason. | chief (the protocol rule); the infra family (`exec_bits.py`'s docstring and message); the owner of `check_normative_clauses.py` |
| D135 | **A certificate serial identifies a BUILD, not a result: two renders of the same mission one second apart carry two different certificate numbers, and the tracked shipping bundle has already shipped seven of them for one airliner result.** `sdk/chief_engineer/certificate.py:863` `_human_number()` returns `C-{year}-{int(seal[:6],16) % 10000:04d}`. That IS deterministic from the seal, exactly as the module docstring and `demo-output/website/certificates/README.md:14` say. The defect is one level up: the seal covers `issued_utc` (`_seal_payload`, `certificate.py:551`), and **all 16 of its call sites in the acts, across 14 workflow modules, pass `issued_utc=time.strftime('%Y-%m-%dT%H:%M:%SZ', time.gmtime())`** -- the wall clock at render time, which is not a fact of the run. So the serial is a pure function of the clock, and the number is a 4-digit lossy projection of a hash that carries no information the seal does not already carry. MEASURED, not inferred. The formula re-derives all five serials in the shipped bundle from their own printed seals (`aeccc6` -> 5686, `4979bf` -> 5295, `fac49a` -> 4330, `6dd22c` -> 7228, `d88946` -> 0918). Executed in a scratch path with identical facts and only the clock moving, nine timestamps of the same result produced **nine distinct serials**. The 702 `certificate.ready` events in `sdk/chief-engineer-runs/mission-state/` carry **670 distinct serials**, i.e. very nearly one identity per build: `aircraft-optimization` alone has issued **96**, `geometry-study` 91, `adjoint-optimization` 90. The tracked zip's own history is the cleanest exhibit -- across nine commits of `dist/certonomous-demo.zip` the SAME airliner result shipped as C-2026-2843, 1557, 1218, 4533, 8701, 2027 and 5686, and the working tree now holds a further one (9575). **Second, independent defect in the same line: the namespace is only 10,000 wide and the draw is effectively random, so it collides.** 24 serials in the run logs have been issued to **two different missions** -- the birthday model for 702 draws from 10,000 predicts 24.6, and the observed count is 24, which is the signature of random assignment rather than identity. One collision is live right now: **C-2026-0105 is currently printed on `mission-output/supersonic-wedge/certificate.pdf` and was also issued to `adjoint-optimization-1785529216`.** Consequently **the question 'which serial is correct' is not well posed** -- there is no correct serial, because nothing anywhere binds a serial to a result. NOT REPAIRED, deliberately: every available repair changes an identity, and a regeneration that changes an identity is a second identity, not a fix. **STILL THE OWNER'S, AND UNTOUCHED — 2026-08-15. The three options are not narrowed and no certificate was regenerated; what was repaired is everything around the identity that does not require choosing one (`3314d746`, `67e472c6`, `a0f89c4b`).** The mechanism was re-verified live rather than inherited, and the cleanest exhibit is incidental: **the airliner certificate moved twice inside one four-hour session** — `mission-output/aircraft-optimization/certificate.pdf` printed C-2026-9575 when this work started and C-2026-4583 by the time the bundle builder ran, with no decision, no re-scoring and nobody asking for a re-issue. **One correction to this row's own framing, load-bearing for D137's repair:** the row's remedy discussion treats the SEAL as the stable alternative to the serial, and it is not. The seal covers `issued_utc` (`_seal_payload`) and the serial is a four-digit projection of the seal, so **the seal moves for exactly the same reason the serial moves**. It is collision-free; it is not stable. Any repair that swaps a cited serial for a cited seal buys a 64-hex-digit dangling citation in place of a 4-digit one, which is why `render_mc_convergence.py` was repaired to cite the PATH and the NUMBERS instead. Under option (a) or (c) that distinction has to be stated wherever the lab tells people what to cite. | D118's repaired bundle-drift check (`e01dfdf6`) flagged `mission-output/aircraft-optimization/certificate.pdf` as diverging from the tracked zip; investigated 2026-08-15 by decompressing the zip (`unzip -d`) and reading the PDFs' uncompressed content streams, since the streams are plain text but the zip is DEFLATE, so a byte sweep over the archive finds nothing. Byte-diff of the two aircraft certificates: 77 bytes differ, at exactly three sites -- the issuance timestamp, the two halves of the SHA-256 seal, and the two occurrences of the serial. **Every reported number, every channel, every word is identical**, so the two files record the same result and disagree only about who they say they are. Generator executed in `/tmp` scratch with `__pycache__` purged first; no solver run, no scoring call. | **The owner's decision, and it is a design decision, not a repair.** Three options. **(a) Drop the serial**: the seal already is the identity, it is collision-free, and it is already what the control room displays -- the number adds only collisions. **(b) Make the serial a function of the RESULT**: seal over the run's facts with `issued_utc` excluded, so re-issuing the same result reproduces the same number and a genuine change moves it; costs a widened namespace to survive collisions and re-issues every certificate once, at a known moment, on purpose. **(c) Keep the clock and add a register**: a tracked append-only serial -> (mission, seal, issued) map, which makes a cited serial resolvable forever and makes collisions detectable at issue time. (b) is the only option under which the word 'certificate' means what a reader takes it to mean. Whichever is chosen, D137 has to be answered in the same breath, because the published claim is the reader-facing half of this one. | chief (design decision); the certificate family; the `dist/` owner |
| D136 | **The shipped bundle pairs a FROZEN recording with a MUTABLE artifact path, so the control room displays one evidence seal on camera and the certificate it links to carries a different one -- live in every bundle shipped since 2026-08-10, on Act 1.** `scripts/build_laptop_bundle.py:167-193` picks a recorded mission (a fixed historical `events.jsonl` out of `sdk/chief-engineer-runs/mission-state/`), then for each artifact that recording references copies **the file living at `mission-output/<dir>/certificate.pdf` right now**. The recording is pinned to one past run; the destination is a single fixed path that **every later run of that act overwrites** -- and overwrites after `withdraw_certificate()` has deleted the previous page (`aircraft_optimization.py:2678` and the same call in every act). So the pair holds only while nobody re-runs the act, and silently breaks the first time anyone does. MEASURED in the tracked zip at `44ac957a`: three acts still agree, and **`aircraft-optimization` does not**. Its shipped `mission-state/m-9c67e2638ff1.events.jsonl` carries `hash d889461d168b47c2...` / `certificate_no C-2026-0918`, while the `mission-output/aircraft-optimization/certificate.pdf` shipped beside it in the same archive prints seal `aeccc6bf2eb7e68a...` / **C-2026-5686**. **This is camera-facing.** `sdk/chief_engineer/control_room.html:838,1664` handles `certificate.ready` by rendering `EVIDENCE SEAL <first 16 hex of the EVENT's hash>` directly above a 'Sealed certificate (PDF)' link to `/api/certificate/<dir>` -- so on the laptop the console shows `EVIDENCE SEAL d889461d168b47c2...` and the PDF one click away opens `aeccc6bf...`. **The demo's own tamper-evidence claim visibly fails, in the airliner act, inside the artifact that leaves this box.** The event serials have been frozen at 0918/4330/5295/7228 since `892f11f7` (2026-08-10) while the airliner PDF's serial moved six times underneath them, so **five consecutive shipped bundles carry an internally contradictory credential**; before that, at `c83c7240` (2026-08-01) and earlier, the event serials were a different set again. NOT REPAIRED: the repair is a `dist/` rebuild and `dist/` has a designated owner, and rebuilding now would ship a NINTH airliner identity rather than restoring either of the two already in the archive. **PAIRING REPAIRED IN CODE 2026-08-15 at `67e472c6`; the shipped `dist/` zip is NOT rebuilt and still carries the contradiction, which remains its owner's call.** The row's option (b) was taken and option (a)'s intent was reached without waiting on D135, by making SELECTION a function of what is on disk rather than of recency. `mission_candidates()` ranks every complete recording newest-first and `pick_missions()` takes **the first whose certificate events agree with the files on disk** — "newest complete mission of this intent" was never the same question as "the run that wrote this file", because several intents share one output directory. `check_bundle_pairing()` then re-checks the **shipped bytes** inside the built bundle, runs inside `main()` before the zip with no skip flag, and returns exit 2 naming the disagreement. Unverifiable counts as failure: a seal that cannot be read out of the page is an unguaranteed pairing, and an unguaranteed pairing is what shipped for five builds. **MUTATION-PROVEN**, `__pycache__` purged before each cell, control and mutant in one invocation: 11 unit tests in `sdk/tests/test_bundle_certificate_pairing.py` build REAL certificates and read the seal back out of the rendered PDF (matched passes; the planted `d889461d...` airliner shape fails; mismatched serial fails; unreadable page fails rather than passing; must-not-match control — an absent certificate is NOT a fault, so the gate is a pairing check and not a blanket alarm); and end-to-end, a bundle built into scratch passed clean, then one shipped nasa-hump certificate re-rendered at a different issuance clock (C-2026-4330 to C-2026-8920) fired the gate on both seal and serial. **A HOLE IN THE FIRST VERSION OF THIS FIX, found by RUNNING it and recorded because reading would not have caught it:** selection initially accepted any recording with no pairing faults, and a recording carrying no `certificate.ready` event has none *vacuously* — so the first end-to-end build shipped Act 1 with **no certificate at all**, reported only as "3 of 4 acts". That trades a contradictory certificate for a missing one, and the sealed page is the beat every act ends on. Selection now requires the recording to state a certificate, that page to exist, and the two to match. **MEASURED ON THE LIVE TREE, and it is the substantive finding:** the repaired builder walked back through **seven consecutive complete airliner recordings, every one disagreeing with the certificate now on disk**, and found no complete `aircraft-optimization` recording that the current page describes. The build now says so loudly and omits the act (exit 1) instead of shipping the contradiction; the other three acts pair cleanly and the gate confirms it on the shipped bytes. | Investigated 2026-08-15 out of the D118 divergence (`e01dfdf6`). Bundle members extracted with `unzip`; seals read out of the PDFs' uncompressed content streams and out of the bundled `events.jsonl`, then compared per act. Historical bundles read with `git show <sha>:dist/certonomous-demo.zip` piped to `unzip`, over the nine commits that touch the tracked zip. | **The pairing rule, not the instance.** The bundle must either (a) copy the certificate the RECORDING references rather than whatever occupies the path today -- which requires the certificate to be archived per run instead of overwritten at one fixed path, i.e. it requires D135 to be settled first -- or (b) **verify the pair at build time and refuse to build when the event's `hash` does not equal the seal in the PDF it is about to ship**. (b) is cheap, is a strict improvement whatever is decided about D135, and would have caught this on 2026-08-10. Note that D118's own reasoning already names this class: the certificate is one of the 32 members whose SOURCE is gitignored, so the drift check can compare it against the tree but cannot grade it, and NOTHING checks it against the recording that ships in the same archive. | the `dist/` owner; the bundle-builder owner; chief |
| D137 | **Four tracked, published certificates print 'Reproducible from the sealed evidence bundle' on the sealed page, a camera beat is scripted to say it out loud, and every serial cited on a tracked surface -- 5 of 5 -- no longer resolves to anything.** The sentence 'Reproducible from the sealed evidence bundle; any change to the recorded run invalidates the seal.' is rendered on `demo-output/acts/round3/airliner_certificate.pdf`, `naca_certificate.pdf`, `valve_certificate.pdf` and `demo-output/website/certificates/b52-certificate-redesign.pdf` (all tracked), is described as the provenance footer in `demo-output/website/certificates/README.md:22` and `sdk/scripts/build_certificate_compare.py:90`, and is **beat 5 of a shoot**: `demo-output/website/motorbike-video/shotlist.md:47` reads '- Reproducible from the sealed evidence bundle.' under trigger `certificate.ready`. **The sentence has two readings and the page does not say which.** As VERIFICATION -- recompute the hash from the recorded facts and check it -- it is true, but only because the issuance timestamp it covers is printed on the certificate itself. As RE-ISSUANCE, which is what 'reproducible' means to a reader holding a credential, **it is false**: by D135 the evidence bundle does not determine the seal, because `issued_utc` is the wall clock and not a fact of the run, so re-issuing the same result reproduces neither the seal nor the number. On a credential shown to a buyer, the second reading is the one that lands. **The dangling half is measured across all three arms of the tree and is unanimous.** Of 20,688 TRACKED files, 33 hits name a serial under `git grep -I` **and that arm is itself defective, exactly as D139 says**: `-I` honours `.gitattributes` `binary`, which hid **precisely the four tracked published certificate PDFs** named above -- `git grep -a` reaches them and adds their own serials (7069, 7080, 8945 and the b52 redesign's 4658). They are self-identification, not citation, so no conclusion below moves; the four were found by a separate route. Re-run with `-a`, every distinct serial CITED by a tracked surface is still dead: **C-2026-9704** (`demo-output/plots/monte_carlo/panel_stats.md:15,65,74` and `sdk/scripts/render_mc_convergence.py:19,316,331,592,638`, cited as the race-study certificate publishing peak L/D 18.14 +-0.06 -- the live race-study certificate is **C-2026-7228**), **C-2026-7080** (airliner) and **C-2026-7069** (NACA 4412) and **C-2026-8945** (valve), all in `demo-output/acts/round3/VERIFICATION.md`, `summary.json`, the `*_certificate.txt` pages and `docs/HANDOFF-BACKEND3.md:86-88`, and **C-2026-4461** (`demo-output/plots/submarine_sail/events.jsonl:237`). Of 2 UNTRACKED-not-ignored files, none cite a serial. Of 37,253 GITIGNORED files (`git ls-files --others --ignored --exclude-standard`), 720 cite one, essentially all of them per-run event logs. Against the 15 serials that resolve to a certificate PDF existing anywhere on this box right now, **the tracked score is 0 of 5**. **This is structural, not an accident of staleness**: there is no register -- nothing in the tree maps a serial to a mission, a seal or a file -- and `withdraw_certificate()` deliberately DELETES the previous page before each run, so the lab's own withdrawal doctrine ('nothing out of date is served', pinned by `sdk/tests/test_certificate_withdrawal.py`) guarantees that every previously cited serial becomes unresolvable. A citation defect this lab has repaired twice is here regenerated by design. **What does NOT depend on these, checked and reported as a negative so the blast radius is not overstated:** the credentials wall carries **no serial at all** -- the shipped `snapshot/credentials.json` keys its four bodies on geometry name, measured Cd and tier, so the sales artifact is untouched by D135 and D136. **The competition submission carries no certificate either** -- `git grep -i certificate` over `closure_challenge_submission_round5/`, `_round4/` and `closure_challenge_submission/` returns zero hits, so none of this is submission-facing and submissions stay PARKED regardless. The exposure is camera-facing only, which is also where it is worst. **The suite already half-knows**: `sdk/tests/test_certificate.py:259-263` BANS the string 'reproducible evidence' from ever rendering on a sealed page -- and the shipped footer reads 'Reproducible from the sealed evidence **bundle**', so the guard is defeated by one word of separation and the banned idea ships anyway. And **nothing in the repo ASSERTS certificate stability**: `sdk/tests/test_certificate.py:130` asserts only the FORMAT (`assertRegex(out['certificate_no'], r'^C-\d{4}-\d{4}$')`) and never that two builds agree, so no test fails today and none would fail under any of D135's options. The false statement is on the published page, not in the suite. NOT REPAIRED: the four PDFs are published artifacts, the shotlist is a shoot document, and rewording either before D135 is decided would be guessing at what the sentence is going to be allowed to mean. **TWO CORRECTIONS TO THIS ROW AND A PARTIAL REPAIR, 2026-08-15 at `3314d746` and `a0f89c4b`. Both corrections were found by re-verifying rather than inheriting, and each makes the row's own claim smaller.** **(1) "0 of 5" is wrong; it is 1 of 5, and the denominator was the error.** The live-serial set was built from `mission-output/*/certificate.pdf` only, which **excludes the archived act certificates**. Re-resolved by extracting text from EVERY pdf on the box: **C-2026-7069, C-2026-7080 and C-2026-8945 RESOLVE**, each to a tracked, present file in `demo-output/acts/round3/`, and every citing surface (`VERIFICATION.md`, `summary.json`, the `*_certificate.txt` pages, `docs/HANDOFF-BACKEND3.md`) names that directory alongside the serial — a frozen, self-consistent archive, not a dangling citation. **C-2026-4461** does not resolve and cannot: it is a RECORDING, on a Windows path from another machine, self-identifying what happened; editing it would falsify a record, so it is left alone deliberately. **(2) The one real defect is C-2026-9704, and it is worse than the row says.** It is not merely dangling: it named `mission-output/race-study/certificate.pdf`, **that file exists**, and it prints C-2026-7228 — the citation was **contradicted by the very artifact it pointed at**, hardcoded in a live generator that would have reprinted it. **REPAIRED** at all five sites in `sdk/scripts/render_mc_convergence.py` and all three in the derived `demo-output/plots/monte_carlo/panel_stats.md`: the certificate is now cited by its PATH and by the NUMBERS it publishes, which survive re-issuance, with the choice and its rejected alternatives defended in the code. **This row's own remedy was among the rejected**: citing the SEAL does not survive the mechanism, because the seal covers `issued_utc` and moves exactly as the serial does — see the correction added to D135. A new `observed_certificate_identity()` reports what the page carried AT RENDER TIME as a dated observation rather than a name, so the panel dates itself. **(3) The reproducibility wording: the generator had ALREADY stopped saying it, which inverts this row's remedy.** Both live builders were rendered in a scratch path with `__pycache__` purged and **neither prints "reproducible" anywhere**: `build_certificate` reads "Sealed by the hash above; any change to the documented run invalidates it", and `build_certificate_v2`'s provenance footer carries issuance, seal and mission/certificate line with **no prose claim at all**. The false sentence survives only in the four already-published PDFs (not reworded — that needs regeneration, i.e. a second identity) and in three places that DESCRIBE the footer and were never updated when it changed. `demo-output/website/certificates/README.md` and `sdk/scripts/build_certificate_compare.py` are corrected, each noting that the tracked `b52-certificate-redesign.pdf` still prints the old sentence and that re-running the compare script yields a DIFFERENT number and seal rather than a fix. `docs/HANDOFF-BG2.md:113` is left as a historical handoff record, on the same reasoning as the submarine-sail event log. **(4) THE GUARD IS WIDENED AND THE WIDENING IS MUTATION-PROVEN.** `test_certificate.py` now bans a case-insensitive "reproduc" — the claim as an idea, not as a phrase — with a must-not-match control pinning that "Verifiable from the sealed evidence bundle" survives, so it bans the false claim and not the topic. Proof against a scratch copy of the generator with the shipped sentence planted back into the v2 footer, `__pycache__` purged, control and mutant in ONE invocation: **CONTROL old guard False / widened False; MUTANT old guard still False / widened TRUE.** The old guard is silent on the exact sentence that shipped on four published certificates — measured, not argued. 51 tests in `test_certificate` pass. **NOT DONE, and left to Katie:** `demo-output/website/motorbike-video/shotlist.md:47` is camera-facing and was not edited. Beat 5 — Certificate (`05-certificate.png`), trigger `certificate.ready` — reads exactly three lines: "A sealed certificate closes the mission." / "Result, uncertainty channels, and provenance seal, all in one page." / "Reproducible from the sealed evidence bundle." Only the third is at issue, and "Verifiable" is honest under all three of D135's options. **ADDENDUM 2026-08-16 (`c90588c6`): the GENERATOR half of this row is CLOSED, and what remains is a REBUILD rather than a generator defect.** `sdk/chief_engineer/certificate.py` now contains **zero** occurrences of the sentence, and `sdk/tests/test_certificate.py` no longer bans a phrase but the IDEA -- `assertNotIn("reproduc", text.lower())` -- carrying its own mutation proof, `test_the_reproducibility_guard_catches_the_footer_that_defeated_it`, which replays the exact shipped footer and asserts the widened guard catches what the narrow one missed. **The half of this row reading that the guard is defeated by one word of separation is therefore discharged**; the false sentence survives on the published artifact alone. **That artifact is the SECOND known member of D243's class -- a tracked build product that outlived its input** -- so it routes to the ARTIFACT's owner rather than to the generator's, and it stays blocked behind D135 rather than merely undone, because re-issuing gives the same result a second identity. **`scripts/check_pdf_surfaces.py` cannot see it, for two INDEPENDENT mechanisms either of which is sufficient alone**: its input is a Python generator rather than an `X.tex` beside an `X.pdf`, so the commit-recency test has nothing to compare; and its stale sentence is a reproducibility claim while every pattern in that check is a four-entry-board claim. **Fixing either mechanism alone would still not find it.** That check now prints **MEASURED RECALL ON THIS CHECK'S OWN CLASS: 1/2 (50%)** on every run, naming this file as the miss with both mechanisms, so a PASS from it is not read as a PASS over the class. | Investigated 2026-08-15 from the D118 divergence (`e01dfdf6`). Serial sweep run separately over all three arms with the frame stated for each, because the shell's `grep` is `ugrep --ignore-files` and cannot see the 37,253 gitignored files at all; PDFs read by decompressing the zip and reading uncompressed content streams. Live-serial set built by extracting every `C-2026-NNNN` from every `certificate.pdf` present on the box. | **The claim has to be settled with D135 and in the same decision, because it is the reader-facing half of it** -- under D135(b) the sentence becomes true as written and needs no change; under D135(a) or (c) it must be narrowed to what is actually true, e.g. 'Verifiable from the sealed evidence bundle', which is one word and is honest under all three options. Katie's call on the shotlist line, since it is scripted narration. Separately and regardless: the eight tracked citation sites should either cite the SEAL (stable, collision-free, and already what the console displays) instead of the serial, or wait for a register. The `C-2026-9704` citations are the urgent ones -- they are a published uncertainty result attributing a band to a certificate that no longer exists, and the serial is **hardcoded as a string literal in a live generator** (`render_mc_convergence.py:19,316,331,592,638`), so the next re-render of that panel will reprint the dead citation rather than notice it. That is the one place in the tree where the stability assumption is load-bearing in CODE rather than in prose. | chief; Katie (the camera line); the certificate family |
| D138 | **Sub-finding of D135, and the phrase that will mislead a reader fastest: `certificate.py:867` calls the serial 'the certificate's own **registered identity**' -- and there is no register.** `git grep 'registered identity'` returns exactly one hit, that docstring. Nothing in the tree maps a serial to a mission, a seal, or a file, so a serial can never be looked up; the word 'registered' is doing work nothing backs. The suite cannot catch this because `sdk/tests/test_certificate.py:36` pins `issued_utc='2026-07-20T00:00:00Z'` as a **literal**, so **no test ever exercises the wall-clock path** that makes the number move, and `test_hash_is_deterministic` (:40-53) only compares `evidence_hash(payload)` to itself within one process. The lab's own machinery already says the opposite of 'registered', loudly and in capitals: `scripts/self_audit.py:5077` reads 'A VERIFICATION WHOSE REFERENCE IS GITIGNORED EXPIRES THE MOMENT IT IS MADE', `self_audit.py:5146` and `sdk/tests/test_bundle_drift_gate.py:297-301` **refuse to grade `certificate.pdf` at all** because its source is 'a reference no clone can reproduce', and `.gitignore:13` makes `mission-output/` untracked (`git ls-files mission-output` returns 0). The infrastructure correctly treats the certificate as unreproducible while the certificate calls itself registered. **REPAIRED 2026-08-15 at `a0f89c4b`, and repaired WITHOUT settling D135, which is why it could be done now.** `_human_number`'s docstring no longer says "registered identity" — the phrase had exactly one hit in the tree and nothing backed it. It now states what is true and can be checked: there is no register, so a number printed here can be **READ but never LOOKED UP**; it identifies an **ISSUANCE, not a result**, because it is deterministic from a seal that covers `issued_utc`, so re-issuing the same result one second later prints a different number; and the namespace is 10,000 wide with an effectively random draw, so **a number is not unique across missions**. The docstring explicitly leaves whether that is the right design to D135's owner and only stops the code from claiming otherwise, so it needs no rewrite under any of the three options — under (c) it gains a sentence, under (a) or (b) it is already correct. | Reported by the certificate-surface sweep, 2026-08-15, alongside D135. | Falls out of whichever D135 option is chosen: under (c) the word becomes true; under (a) or (b) it should be deleted or narrowed. It should not survive unchanged under any of them. | chief; the certificate family |
| D139 | **`.gitattributes` marks 981 tracked files `binary`, and `git grep -I` -- the first arm of every sweep in this lab -- therefore skips 12 files that contain no NUL byte at all, five of them sealed product certificates whose claim text is plain uncompressed ASCII.** git's binary decision comes from the attribute, not from the bytes: `*.pdf`, `*.png`, `*.jpg`, `*.stl`, `*.obj` are all marked `binary` in `.gitattributes`. Measured at `44ac957a`, 2026-08-15T18:58Z, over `git ls-files` with `git check-attr binary` and a NUL test on the first 8,000 bytes: **981 marked, 12 of them actually text** -- `demo-output/website/certificates/b52-certificate-{current,redesign}.pdf`, `demo-output/acts/round3/{airliner,naca,valve}_certificate.pdf`, `docs/papers/Paper3.pdf`, four `.stl` and two `.obj`. Controls both ways per L-84: **positive** -- `command grep -c '0\.056647' demo-surfaces/motorBike.obj` returns 3 while `git grep -lI` on the same path exits 1 and `git grep -lI --text` recovers it; **must-not-match** -- the same literal in `docs/PRODUCT_LIST.md` is found by `git grep -lI` at exit 0, so the miss is specific to the attribute and not a broken pattern; **boundary** -- `demo-output/Gui_issue.png` carries a NUL in its first 200 bytes and must stay skipped, which scopes the finding to exactly the 12 and no wider. Materiality is the certificates: `command grep -aoE '\(([^)]{3,60})\) *Tj'` on `b52-certificate-current.pdf` returns `(CERTONOMOUS)`, `(CERTIFICATE OF AUTONOMOUS SOLVE)`, `(Mission geometry-study-b52)` -- the lab's most claim-bearing artefact class is plain-text greppable by GNU grep and invisible to every `git grep -I` sweep in this repository. This is a **second, independent** blind spot from L-75's: L-75 is about `.gitignore` and the shell's ugrep; this one survives `command grep` and bites the `git grep` arm that L-75's repair recommends. | Found 2026-08-15 while writing `docs/USING_THIS_LAB.md`; frame is tracked files only at `44ac957a`, live working tree, `git check-attr -z binary` over `git ls-files -z`, text defined as no NUL in the first 8,000 bytes. Filed at D139 and not D135: D135 was free when the highest ID was checked at 19:03Z and taken by a concurrent session before the row could be written, so this row takes the next free ID per the docket's collision rule rather than moving anyone. **Cannot see:** whether any past sweep actually missed a live claim in those 12 -- this measures the mechanism, not its history; whether compressed PDFs elsewhere carry claims that `--text` would still miss, since compression defeats grep regardless of the mark; and the same class of mark in the gitignored and out-of-repo arms, which `git grep` never reaches at all. | Zero compute. Two candidate repairs, and the choice is a method question rather than a code one. **(a)** Make `--text` the house form for the tracked arm of any completeness sweep, and say so wherever the three-arm sweep is written down -- cheapest, and it costs only speed. **(b)** Narrow `.gitattributes` so that only formats that are actually binary carry the mark, e.g. drop `*.stl` and `*.obj` (both are ASCII here) and keep it for `*.png`/`*.jpg`; this needs a check that no diff or EOL conversion breaks as a side effect, which is the reason the marks were added. Neither repairs the PDFs, which are marked correctly and are text only by accident of how they were generated. | fleet (the sweep form); chief (whether `.gitattributes` changes, since it affects diffs lab-wide) |
| D140 | **A third live surface carries the `5 of 8` against Wu & Zhang that D133 falsified, and it is a `LADDER_V_*` document that this dispatch is forbidden to touch -- so the repair is complete on two arms and stranded on the third.** `campaign/LADDER_V_PASS3_COLD_2026-08-11.md:421` reads *"Wu and Zhang (84.7%, -0.953, 5 of 8)"* inside the blockquoted corrected-claims briefing that rung V-PASS3 hands forward. It is the identical defect repaired at `campaign/BOARD_RESCORE_2026-08-14.md` §3.3 and `campaign/PROBABILITY_OF_RANK_SIX_ENTRY_2026-08-11.md` §3 in the same pass: `AR_1_Ret_360` is 2.96e-5 from Wu & Zhang's printed `0.0455`, below the 5e-5 half-ulp, so the count is `4 or 5`. **Nothing turns on it** -- the decided threshold is 7 of 8 and that document's own verdict, *"four pairwise comparisons are not statistically decided, not two"*, is identical at 4 and at 5. The reason it is filed rather than fixed is a **standing-order collision, not a judgement**: the dispatch that measured and repaired the other two was barred from `LADDER_V_*` documents by its own brief, and a repair that stops at the boundary of an ownership rule leaves the corpus in exactly the state D71 names -- one sentence repaired, its copy not travelled. **This is also the fourth surface of the family and the second time this week a rung briefing has been the copy a sweep could not reach.** Adjacent and NOT a defect: `campaign/LADDER_V_V15_LADDER_TEXT_CLAIMS.md:169` records `AR_1`/`AR_3` as ties at 0.00003/0.00008, which is the correct reading, and the `latex/` report and `dist/` package carry only *best-on-board* counts, a different claim, correctly. | Measured 2026-08-15 at HEAD `44ac957a`: `LIVE_BOARD` read by `ast` from `sdk/scripts/probability_of_rank.py` (never imported), ours from `closure_challenge_round5_qcr.json` -> `round5_per_case_full`, all 48 cells compared, every `__pycache__` purged first. Three-arm sweep, 20,688 tracked + 2 untracked + 37,245 gitignored; the gitignored arm was reached with `git ls-files --others --ignored --exclude-standard -z` piped to `/usr/bin/grep -F` on the entrant surnames (8s over 11.9 GB) and returned five real files, ALL of them under `latex/` or `dist/` -- both owned arms -- plus two VTK base64 false positives. **Cannot see:** whether any reader has already copied this line onward into an untracked brief. | One line, by whoever owns `LADDER_V_*`: strike the `5 of 8` and write `4 or 5 of 8`, naming `AR_1_Ret_360` and the half-ulp, exactly as the two repaired surfaces now do. Do **not** change the verdict. The durable half is larger than the line: **an ownership boundary that a sweep must not cross is a place where copies survive corrections, and the sweep that stops there has to file the remainder or the corpus silently keeps the old figure in the one document rungs are graded from.** **[REPAIRED 2026-08-15 at `be0a0c5d` by a dispatch holding explicit permission for this one file, which answers the chief question this row raised: a cross-boundary repair WAS dispatched as one unit, and the unit that crossed was a named single-file exemption, not a lifting of the zone.** `campaign/LADDER_V_PASS3_COLD_2026-08-11.md:421` now reads `Wu and Zhang (84.7%, -0.953, ~~5 of 8~~ **4 or 5 of 8** ‡)` with a dated `‡` block inside the same blockquoted briefing carrying the half-ulp reasoning, the falsifying anchor `eadcd112` (D133), the sibling repairs at `09234034`, and a paragraph headed *"This is an accuracy repair and not a retreat"*. A bare `4 of 8` was deliberately not written. No other `LADDER_V_*` document was touched. **Verified after the edit, tracked frame, `git grep -a` over `*.md` `*.tex` `*.html` `*.json` on the discriminator ENTRANT-SURNAME-NEAR-COUNT rather than the numeral: the tracked arm now carries no unstruck entrant-attributed `5 of 8` against Wu & Zhang anywhere.** The one remaining occurrence is `sdk/tests/fixtures/absolute_claims_labelled_second_instance.json:634`, which is quoted historical text inside a labelled test fixture and is correct as history.] **The durable half is filed separately as D145**, which enumerates every ownership zone, whether anything sweeps it, and the live wrong claims inside each. | owner of `LADDER_V_*` (the line) -- **DONE**; chief (the durable half, now D143) |
| D141 | **A summary block can go stale against the section it summarises with neither one being edited, and this lab has no instrument that compares the two.** V6's blocker was exactly this: the currency block's §4.8 row in `CLOSURE_CHALLENGE_SUBMISSION_DRAFT.md` said the leaderboard finding *"HOLDS ... re-verifying it needs a network read this pass did not perform"*, while §4.8 itself, **212 lines below in the same file**, had been headed `~~VERIFIED~~ FALSIFIED 2026-08-11` since `5c9c63fb`. Neither text was wrong when written and neither was touched afterwards; the world moved between them. **The row was byte-identical to what `472f9f92` wrote** (per-line sha256, HEAD against `git show 472f9f92:`) and so was every other row in the block bar §4.7 — which is precisely why a diff-shaped check cannot find this class: **the defect is the absence of an edit.** Repairing it, all eleven rows were compared against their sections by execution; **exactly one was stale**, so the class is real but its base rate here is 1 in 11 rather than epidemic. Every existing instrument is per-sentence or per-figure (`check_derived_figures` grades a figure against its record, `_best_on_board_faults` a count against the board, `board_placement_faults` an ordinal against a board) and none takes two regions of one document as its unit. **Also filed here because it is cheap and true:** the grade at `377d6afb` §4b describes the block as *"Twelve rows"*; it is **eleven** (`4.1`, `4.1` citations, `4.2`–`4.6`, `4.7`, `4.7` asymmetry, `4.8`, `4.9`). Not correctable by this thread — `LADDER_V_*` documents are outside its write scope. | Repairing V6's blocker, 2026-08-15, at `87324012`; row-vs-section comparison executed over all eleven rows and the per-line hash comparison against `472f9f92` | A check whose unit is a **claim and its referent inside one document**: for every table row or summary bullet that names a section, compare its verdict word against that section's heading. It needs no arithmetic and no board — `FALSIFIED` in a heading against `HOLDS` in the row that points at it is a string comparison. Whether that generalises beyond this one document's `§N.M` convention is the open question, and is what should be measured before anything is built. **BUILT AND MEASURED 2026-08-15, commits `840c0c27` (check + tests) and `7370b3d3` (its exec bit).** `scripts/check_summary_consistency.py`, wired into `lab_check.py` by AST with no registration edit (`[PASS] scripts/check_summary_consistency.py exit 0`), `sdk/tests/test_summary_consistency.py` 34 tests green, twelve mutants. **It does generalise, and the measurement says how far.** Pairs are derived from three structural rules and never from a list, each counted on every run: a table row whose LEADING CELL is a section id (**34**), a table row whose leading cell is the section's HEADING STRING (**23** — this is what reaches the `S3.` / `S7.` sections in `MONITOR_STANDARD.md` that carry no `§N.M` at all, so the convention is NOT the limit), and a bullet naming a section in its first 60 characters (**37**). **94 pairs over 412 tracked documents; 95 over 484 across all four arms** (tracked, untracked, gitignored, run tree — the run tree needed a structural minified-payload exclusion, 559 OpenMDAO `mphys.html`/`n2.html` payloads at ~1.1 s of regex each, counted in the frame rather than dropped silently). **Both control halves run in-process on every invocation, over two immutable blobs named by commit** (`87324012` and its parent): P1 the pre-repair 4.8 row FIRES; **N1 the other TEN rows of that same block DO NOT** — the discrimination the check exists to make, and the control a check tuned to fire would not have; N2 the repaired row does not fire and grades AGREE; P2 a synthetic `HOLDS`-over-`FALSIFIED` fires; N3 a correctly struck historical row does not fire and is reported EXEMPT. A control misfire makes the run UNKNOWN. `mask_exempt` is IMPORTED from `check_derived_figures`, never reimplemented; the section side re-asks `_STRUCK_HEAD` over the MASKED heading with one `#` restored, because the raw line matches a heading corrected the OTHER way (`~~FALSIFIED~~ **VERIFIED**`) and would read a standing section as withdrawn — mutant M11. **Sensitivity and specificity measured over 47 historical snapshots spanning 2026-08-10 to HEAD** (`git archive` per commit, 3,253 pairs, 64 graded): **14 hits, exactly ONE distinct, and it is this row's own defect** — present at every snapshot from `5c9c63fb` (which headed §4.8 FALSIFIED) to `e933e31b`, absent before and after. **Zero other hits, so the measured false-positive rate is 0 of 14.** **On the live corpus at HEAD, all four arms: ZERO hits**, so nothing was repaired and nothing needed to be — a checker's author repairing his own corpus is how a checker gets tuned. **The honest limit, and it is filed as D163:** only **2 of 94** pairs are gradeable at all. *(Anchor note: this closure sentence and D163 are carried by commit `bfe9b812`, whose subject is another thread's D158-D162 filing, because `docs/DOCKET.md` was staged by that thread while this text sat uncommitted in the shared working tree — a pathspec isolates by file, not by author. The instrument it certifies is at `840c0c27` and `7370b3d3`, which are this thread's. Named rather than undone, as at D93.)* | unassigned; the instrument exists, D163 carries what it cannot reach |
| D142 | **D93's second half is not done and has no owner: the P33 exclusion that hid `PRODUCT_LIST.md` was never re-scoped, only overtaken.** D93 made two demands. The repair half is closed at `7a0419f7`. The other half — *"re-scope P33: an append-only log is excludable only for the regions nobody edits, and the exclusion must be measured rather than asserted from the file's genre"* — is untouched. `docs/P33_CROSS_SURFACE_SWEEP.md` still rules `docs/PRODUCT_LIST.md` out of scope as an *"append-only chronological log whose historical figures are quotations rather than claims"*, and D59 still records that as *"a principled exclusion this row does not dispute"*. Measured against the region actually at issue, the premise is false: `:53–98` is **one live bullet**, the 08-14 repair pass wrote into it, and the 08-15 repair at `7a0419f7` wrote into it again — twice in two days, in a region declared to be nobody's. **So the exclusion has now been falsified by two separate repair passes and still stands in the sweep document.** Closing D93's repair half without saying so would have retired the row that carries this, which is why it is a row and not a remark. | Closing D93's repair half, 2026-08-15, at `7a0419f7`; exclusion premise re-tested against the commits that touch the excluded region | Re-scope P33 by measurement, not by genre: for each excluded file, ask whether any commit in the sweep's own window touched the excluded region, and drop the exclusion where the answer is yes. Then re-run the sweep at its stated width and publish the delta, because an exclusion silently withdrawn changes a coverage number nobody re-derived. | P33 owner / cross-surface sweep |
| D143 | **The repaired public page has a second, older copy in the shipping build that no dispatch is allowed to touch, and the sweep that finds it is not the sweep that graded it.** `dist/certonomous-demo/site/closure.html` is a **different build** from the tracked `demo-output/website/closure.html` (byte-diff: DIFFERS; it has no `4th of 7` and no 2026-08-15 strike) and carries the same five stale per-case table tags the tracked page does: two gold `OUR MODEL LEADS` badges on `alpha_15_13929_4048` and `alpha_15_13929_2024`, which we **lose** to Tian, Buchanan, Hickel & Dwight on the live six-entry board (0.0501 vs 0.0432, 0.1011 vs 0.0998), and three `Nth of 5` duct placements whose live values are 3rd, 4th and 4th **of 7**. It carries `RANK 1 OF 7` at `:95`, so it is *partly* current — which is worse than uniformly stale, because a reader who spot-checks the headline concludes the page was repaired. **Why no instrument reaches it:** it is in the gitignored arm (37,244 files), invisible to every `git grep`, and `dist/` is on the do-not-touch list of this dispatch and of the V6/V10 repair dispatch before it. So the surface is discoverable in 11.6 s by one command and repairable by nobody currently dispatched. **The shape worth more than the file:** a do-not-touch list and a surface-discovery rung can be individually correct and jointly guarantee a permanent stale copy — V14 finds it, V10 grades a sibling, and neither is allowed to fix it. | Ladder V, V6/V10 re-grade, 2026-08-15, HEAD `be0a0c5d`; gitignored arm via `git ls-files --others --ignored --exclude-standard -z \| xargs -0 /usr/bin/grep -aIlF -f <patterns>` (11.6 s, 1 hit); board figures re-derived from `LIVE_BOARD` by AST and `round5_per_case_full`, not transcribed | Either rebuild `dist/` from the repaired tracked page, or untrack/mark-superseded the build, or put `dist/` explicitly on some agent's dispatch — the owner's call. The general half, which is the row: every do-not-touch list needs a named owner for the surfaces it excludes, or the exclusion is a guarantee of staleness rather than a protection. | dist/ build owner / V14 |
| D144 | **A repair's own self-measurement was falsified by the same commit that wrote it, and the sentence names `HEAD` as its anchor — D79 at ZERO commits' remove.** `demo-output/website/CLOSURE_CHALLENGE_SUBMISSION_DRAFT.md:299-301` reads *"eleven rows, one stale — §4.8, repaired above. **Ten of the eleven are byte-identical** to what `472f9f92` wrote (sha256 of each line, **HEAD against `git show 472f9f92:`**), the eleventh being §4.7"*. Measured by extracting the eleven rows and hashing each, `__pycache__` purged, on one working copy across five trees: `472f9f92` 11/11 identical; `377d6afb` (pre-repair) **10** identical, §4.7 differing — **the sentence is true here**; `87324012`, `bd8280de` and HEAD **9** identical, §4.7 **and §4.8** differing. The repair edited §4.8 and then reported the row count as if it had not, under an explicit HEAD anchor, so the partition it asserts ({10 identical} ∪ {§4.7} = 11) has no slot for the row the block exists to announce as repaired. D79 is a "before" taken at an older *commit*; this is a "before" taken on the tree the same edit was about to change, which no commit-anchor discipline can catch. **Second instance in the same row, and it is the inheritance failure:** the same repaired row says §4.8 is *"212 lines below in this same file"*. Measured, `(line of '### 4.8 ') − (line of the '\| **4.8** \|' row)` is **240** at `472f9f92` and **256** at `377d6afb`, `87324012` and HEAD — 212 at no commit. It was transcribed from `campaign/LADDER_V_V6_V10_GRADE_2026-08-15.md:148,193` (*"`:519+`, 212 lines below"*), and at `377d6afb` line 519 is **§4.7's** heading. A repair dispatched to verify rather than inherit inherited the one figure in the grade that was already wrong. **Both are machine-checkable in three lines and both survived the repair's own post-write sweep**, because that sweep runs guards that count faults, and no instrument in this lab compares a summary block's *arithmetic about itself* against the tree it names. | Ladder V, V6/V10 re-grade, 2026-08-15, executed at HEAD `be0a0c5d` and at `472f9f92`, `377d6afb`, `87324012`, `bd8280de` from one working copy; row extraction + per-row sha256, and line-distance arithmetic | (a) Correct the two figures to nine and 256, strike-and-keep. (b) The row: a check that recomputes any *self-referential* count or locator a document states about its own file — row counts, byte-identity tallies, `N lines below` — and fails when the stated value disagrees with the file it is in. This is the class D141 names from the other side: D141 is a summary row going stale against its section, this is a summary row going stale against **itself**. (c) A repair may not restate a measurement of the tree it is editing without retaking it after the edit. | V6 owner / self_audit owner |
| D145 | **An ownership boundary that a sweep must not cross is a place where wrong copies survive corrections -- and measured, the boundaries did NOT fail for lack of a sweep. Four of the five zones carrying live wrong claims had ALREADY been detected, in writing, by name, before this pass began.** D140 named the class; this row enumerates it. **27 live wrong passages sit in 7 files across 5 owned zones**: 15 in `latex/closure_challenge_report.pdf` (the withdrawn `68%` twelve times, `rank 1 of 5` twice, Yang absent from Table 1, two undecided pairs where there are four), 4 in the `dist/certonomous-demo.zip` bundle (the withdrawn *"comparable to its margin"*, one copy carrying `rank 1 of 5` in the same sentence), 4 in `LAPTOP_SHOOT.md` (a round-3-era ready-to-film script: `0.0676` as the entry of record, *"we lead the board on five of eight cases"*, *"third of five"*), 3 in `demo-output/website/closure.html` (per-case badges reading `2nd of 5` / `3rd of 5`), and the 1 in `LADDER_V_PASS3_COLD_2026-08-11.md` that this dispatch held an exemption for and **repaired at `be0a0c5d`**. **THE WORST FINDING IS NOT A BLIND SPOT BUT A FALSE CLEAN**: `self_audit.py::check_rank_claim_surfaces` DOES open `dist/*.zip` (`:1060-1079`), DOES read line 502 of `certonomous-demo/site/closure.html` inside it, DOES recognise it as a rank claim -- and reports **"every travelling surface complies"**, because it grades claim FORM (does the sentence carry P(rank 1), its interval, the token) and never claim VALUE (is the denominator `5` or `7`; is `comparable` still true). **A form check on a stale artifact is worse than no check, because it is quoted as reassurance.** The staleness itself IS caught -- `check_bundle_drift` returns **FAIL right now** naming those same pages -- and three earlier `LADDER_V_*` passes each found the four bad copies and each wrote down why they stopped, one verbatim: *"Reported, not repaired -- `dist/` has a designated owner."* **Nobody looked up who that was, because there is nobody: `dist/`, `latex/`, `LADDER_V_*`, `dafoam/ladder-b/`, `self_audit.py` and nine of twelve charters are barred to a role with no occupant** -- *"the designated owner"* appears six times across six docket rows and never once resolves to a name. **Every anonymous zone is one this lab invented for itself; the only named ones (`LAPTOP_SHOOT.md`, `/usr/local/bin/`, submissions) are Katie's.** And a name alone is not enough: `LAPTOP_SHOOT.md` has the clearest owner statement in the corpus (A3, *"It is Katie's file"*) and is two weeks stale in four places. **A boundary needs a name AND a cadence AND a list.** | Swept 2026-08-15 at HEAD `1360a5a9`, four arms, each stated per count. **tracked 20,688** by `git grep -a` -- `git grep -I` skips **1,476** of them as binary, **50** PDFs among those, so `-a` is mandatory and the shell's `grep` function passes `-I` too. **untracked 4.** **gitignored 37,242 / 11.9 GB** by `git ls-files --others --ignored --exclude-standard -z \| xargs -0 /usr/bin/grep -aIlF -f <patterns>` in **12.3s** (a regex alternation over the same set times out past 2 minutes); it reduces to three files, all mirrors of the zip. **Outside the repo: `/home/ubuntu/certonomous-runs/`, 66 GB**, invisible to every git route -- zero hits over every file under 1 MB. **Containers decoded, never byte-grepped:** all 50 tracked PDFs by `pdftotext -layout` in **8.2s** (exactly one carries a withdrawn figure; the other three hits are the entrants' own published papers, and the four published certificate PDFs are clean), 18 gitignored PDFs in **0.5s** (zero), 454 tracked `.gz` in **3.4s** (six match `0.002878` and all six are solver residuals), the one tracked `.zip` by `zipfile`. `check_rank_claim_surfaces` and `check_bundle_drift` were RUN, not read. Every `__pycache__` purged first; `/usr/bin/find` throughout, since `find` here is `bfs`. **Discriminators, not wider patterns:** for the per-case count, attribution to a single named entrant, never the numeral -- the sweep that filed D140 got 292 lines in 124 files from the numeral, and this pass reproduced the trap exactly, 14 `68%` strings in one PDF of which **2 are the 1-sigma confidence convention and not the claim**. For the seed bound, the word `comparable` beside a margin figure, never `0.0024`. **Cannot see:** ligature-bearing words (`fi`/`fl` drop in extraction; no digit affected), binary members inside the zip (`_surface_text` drops non-UTF-8), `.gz` above the size cap, and act transcripts under `mission-output/`, which published tables cite as evidence. **Two false positives recorded** (`CLOSURE_CHALLENGE_STATUS.md:405-407, :667`): both are correct dated history under supersession banners in their section headings, and both would have been filed as live on a line-scoped read -- **the unit of classification is the line plus the nearest banner above it.** | **The owner's worklist is `campaign/OWNERSHIP_BOUNDARY_SWEEP_2026-08-15.md` section 3**, W1-W8, each with file, line or member, what it says, what it should say and who must do it. **The two largest are rebuilds, not edits, because both zones' sources are already repaired**: `latex/` is `pdflatex closure_challenge_report.tex` twice (`pdflatex` is installed, `latexmk` is not) and `dist/` is one run of `scripts/build_laptop_bundle.py`, which is a `shutil.copy2` of already-correct pages. **Proposed fix, in order of cost. (a) A boundary must carry a named occupant AND a review cadence, or it is advisory and a sweep may repair inside it with strike-and-keep, saying so on the face of the edit.** D140 was resolved today by the cheapest cross-boundary unit and it should be the standard one: the zone was not lifted, one named file inside it was exempted for one measured repair. **(b) Sweeps report across boundaries and edit only inside them -- nearly free, because the machinery already exists and is already unrestricted:** `scripts/sweep.py`'s `everything` frame states *"no ignore rules, no directory exclusions"* and returns the `dist/` and `latex/` hits today with no change at all. What is missing is container decoding and a route from report to owner; **measured cost of decoding every container in the corpus is under 25 seconds on tools already installed.** **(c) A per-zone owner registry** -- do not invent it: `MEMORY_ARCHITECTURE.md` section 9 already carries "record custody" as an open item and `sdk/chief_engineer/exec_bits.py:346-363` already has an `OWNERS` prefix table (families, not people) to extend. **(c) is worth nothing without (a): a registry of anonymous roles is what we have now.** **(d) Every check that cannot see something must print what it cannot see** -- already the practice at `self_audit.py:5948`, whose generated output reads *"nothing in this file opens a PDF, so a claim that exists only in a compiled report is invisible while its `.tex` source is not."* **That sentence described `latex/closure_challenge_report.pdf` exactly, for three days, and nobody acted, so (d) is necessary and demonstrably not sufficient: a printed blind spot that does not feed a worklist is decoration.** **REJECTED:** lifting the boundaries (both are build outputs -- editing the output instead of the source is the defect, and `charters/README.md:57` bars the lab presenting its own invention as the owner's policy); auto-rebuilding on source change (a material act on an outward-facing artifact, and it would destroy the live `check_bundle_drift` FAIL, which is information); deleting or gitignoring stale artifacts (destroys dated history, against strike-and-keep); **widening the pattern** (292 lines in 124 files is what that buys; the answer to a noisy sweep is a discriminator); line-scoped classification (see the two false positives); and a new scheduled runner, since D64 already establishes nothing is scheduled and a runner without (a) is just an unattended red light. No scoring call, ledger stands at 6; nothing fetched, sent or filed; the pin `deb91557` was not moved. | chief (name an occupant and a cadence for each zone in section 1 -- this is the whole fix); `latex/*` owner (W1); `dist/` owner (W2, W3); `scripts/self_audit.py` owner (W4, the false clean); Katie (W5 `LAPTOP_SHOOT.md`, and the custody ruling W7) |
| D146 | **Every agent in a chief's session shares ONE scratchpad directory, and a commit-message file staged there can be silently replaced by another agent between writing it and `git commit -F`.** Observed twice in one pass on 2026-08-15 while grading D123: `<scratchpad>/msg1.txt` and `<scratchpad>/msg2.txt`, written as the message files for two pending commits, were each **overwritten in place by a different agent's commit message** before either was used -- one describing D140's Wu & Zhang `5 of 8` repair, one a reporting-charter precision clause -- and two probe scripts in the same directory (`probe1.py`, `probe2.py`) were replaced mid-analysis by a third agent's `check_board_placement_words` probe. The path is keyed on `CLAUDE_CODE_SESSION_ID`, and **D132 measured that every subagent of a chief inherits the same one**, so this is not a collision between sessions: within one session it is the guaranteed behaviour, and this row and D132 are one finding wearing two hats. **The failure mode is silent and it is the dangerous kind.** `git commit -F <msgfile> -- <paths>` reads whatever is at that path at exec time, so a clobber between write and commit lands ANOTHER AGENT'S message on YOUR files: the commit succeeds, `git log --oneline -1` looks right, and **nothing in this lab checks that a commit message describes its own diff**. It is the backtick hazard's mirror image -- `-m` with backticks silently never commits, this silently commits the wrong thing -- and the second is worse, because the first leaves the work uncommitted where the next `git status` finds it. **Nothing was miscommitted here:** the clobber was noticed before use and both messages were rewritten under a private subdirectory. | Observed 2026-08-15 during the D123 grading, session `64b13819`. Frame, read off the directory at 19:20Z with `ls`: **452 files**, of which **18 are `msg*.txt`** at generic names (`msg.txt`, `msg1.txt`...`msg5.txt`, `msg_docket.txt`, `msg_exec.txt`, ...) and **11 are `probe*.py`**, spanning two days and many agents. One agent had already met this and solved it privately: a `MYSCRATCH` file in the same directory pins that agent to a per-agent subdirectory -- a convention invented locally and written down nowhere. **Cannot see:** how many past commits carry a message written for a different diff, because detecting it needs a message-versus-diff comparison this lab has no instrument for and a clobber leaves no trace in the repository at all; and whether any of the 18 `msg*.txt` files was ever the victim rather than the aggressor. | Cheap, mechanical, needs no new tool: **write every commit-message file to a per-agent subdirectory of the scratchpad, never to a fixed name in its root**, and put that line in the dispatch brief beside the existing `-F`-not-`-m` rule, which exists precisely because commit messages are where this lab's silent failures live. Stronger variant if the chief wants one: require the message file be created with `mktemp` so the name cannot collide even by accident. The `MYSCRATCH` convention already sitting in the directory is evidence both that the fix works and that leaving each agent to rediscover it does not. | fleet (the convention); chief (whether the dispatch brief mandates it) |
| D147 | **`git -c core.fileMode=true` is a fourth repair for D134 that needs neither a protocol amendment nor a repo-wide config flip -- and it works on the commit, not on the `git add`.** D134 measured four variants under `core.filemode=false` and offered three options, all of which change something durable: amend the commit protocol (a), set `core.filemode = true` repo-wide (b), or accept the register as permanent (c). A fourth exists and changes nothing durable: pass the config per invocation. Measured 2026-08-15T19:31Z in four controlled scratch repositories under `/tmp`, `git init` with `core.filemode false`, one shebang-bearing file, `git ls-tree HEAD` read after every commit -- the same instrument D134 used. **(1) The mechanism is sharper than "re-derives the mode from the worktree":** with `core.filemode=false` a pathspec commit takes the mode from **HEAD**. HEAD `100644` + index `100755` (via `update-index --chmod=+x`) commits `100644`; HEAD `100755` + index `100644` (via `--chmod=-x`) commits **`100755`**. `git update-index --chmod` is inert in *both* directions, which is why D134's variants A-C all failed and why the register can only grow. **(2) The repair is asymmetric, and the tempting half is the wrong one:** with the worktree bit set, `git -c core.fileMode=true commit -F <msg> -- <path>` records **`100755`** even when the `git add` was plain; `git -c core.fileMode=true add <path>` followed by a plain pathspec commit records **`100644`** despite the index visibly reading `100755`. The failing form is the one an agent would pick, because it is the one where you can watch the index change. Controls both ways per L-84: **positive** -- D134's variant (D), a bare index commit, still records `100755` in the same scratch repo, so the instrument can see a 755; **must-not-match** -- plain `add` plus plain pathspec commit records `100644` on the same file in the same repo, so the difference is flag placement and not the scratch setup; **boundary** -- `-c core.fileMode=true` with the worktree bit **absent** is not a bypass, it honestly re-derives `100644`, which scopes the finding to flag placement and not to the flag being a mode-forcing switch. | Found 2026-08-15T19:28-19:32Z by the cold-read acceptance pass on `docs/USING_THIS_LAB.md` (a non-author of that document; dispatch record, not git, establishes that -- every commit on this box carries one `Ubuntu` identity). Repo at `9af7e2d3`; the measurement itself is four scratch repositories under `/tmp`, not this tree, and not inferred from documentation. Section 9.2 of the guide now carries both tables. **Cannot see:** whether any of the files in `WAIVED_NO_EXEC_BIT` would clear under this form -- that needs each file's worktree bit set and one commit per file, which is tree-writing work this pass did not do; whether a per-invocation `-c` is protocol-conforming at all, which is D134's question and not a measurement; and whether `core.filemode = false` is still needed on this box, which D134 already records as the question that would dissolve the problem rather than route around it. | Zero compute. Either fold the per-invocation form into D134's option list so the chief chooses among four rather than three, or rule it out explicitly. It is strictly cheaper than (a) and (b) -- no protocol text changes and no tracked file's mode flips as a side effect -- and its only standing cost is that every exec-bit commit then needs a flag no reader will know to look for, which argues for it being named in `test_exec_bits.py`'s failure message rather than left to the guide. If adopted, that message should name **this** repair instead of `git update-index --chmod=+x`, which D134 already established produces a green index and a red test. | chief (whether a per-invocation config override is protocol-conforming under ESCALATION_CHARTER 9.6); the infra family (`exec_bits.py`'s message) |
| D148 | **`scripts/lab_check.py` produces zero output until the instant it finishes, so any interruption of the ~20-minute full tier destroys the entire result rather than truncating it -- and `python3 -u` cannot help.** The report is accumulated into a `lines` list and emitted by a single terminal `print("\n".join(lines))` -- `scripts/lab_check.py:1261` for `--list`, `:1357` for a real run. There is no intermediate write and no progress channel. Measured 2026-08-15: `/usr/bin/time -v python3 -u scripts/lab_check.py > full.out` left `full.out` at **0 bytes for the whole run**, checked at 2m12s, 5m29s, 7m08s, 10m08s and 11m56s into it; the `--no-tests` tier (3m19s wall, 182s user + 9s system, exit 1) behaved identically. `-u` unbuffers a stream; it cannot flush a string the program has not built yet, so the remedy is not merely weak, it is inapplicable. **Why this is a lab-level finding rather than a UX nit:** this box powers itself off on an idle timer (`IDLE_MINUTES=30` in both the tracked and installed `auto-stop.sh`), a usage limit terminates every agent at once, and the full tier is the lab's only single-entry-point health check at ~20 minutes wall carrying a 1,830-test suite. An interruption at minute 19 of 20 yields **nothing at all** -- no FRAME block, no per-gate verdicts, no SUITE FRAME, no skip list -- and the whole suite is re-paid from zero. The failure mode is also invisible in the shape this lab cares about: a zero-byte output file is indistinguishable from a run that has not started, from one that died, and from one that is nineteen minutes healthy. **REPAIRED 2026-08-16, `d6e9e82b`, and the mechanism was re-measured rather than taken from this row.** Reproduction stands: `timeout 45 python3 scripts/lab_check.py --no-tests` returned exit 124 with **0 bytes on stdout AND 0 on stderr**. The repair streams every line flushed as it is produced and marks the run: `LAB-CHECK-BEGIN` before enumeration, `LAB-CHECK-PLAN expected=N` with the names before the first check starts (so the DENOMINATOR survives truncation), a `>>` line per check as it starts, and `LAB-CHECK-END COMPLETE|INCOMPLETE` as the only line that certifies a report. Demonstrated on the LIVE tree in both directions: `timeout 400` gave exit 124 with **7,614 bytes**, 17 of 18 units reported and `LAB-CHECK-END INCOMPLETE ran=17/18 in-flight=scripts/self_audit.py`; the unconstrained run gave `LAB-CHECK-END COMPLETE verdict=FAIL exit=1 ran=19/19 elapsed=518.2s`, 11,374 bytes. **The row's two open questions are answered.** (i) *Does any caller depend on the single-print behaviour?* No. The consumers are `scripts/installed/pre-push` (exit code only), the cron line (appends the stream to a log), and `sdk/tests/test_lab_check.py` (substrings on stdout); `--json` still emits ONE parseable document on stdout, with its narration moved to stderr where the cron `2>&1` puts it anyway. Nothing broke. (ii) *Whether `--junitxml` advances during the run* remains UNTESTED -- it was not needed and is not claimed. **The row's suggested repair was deliberately not taken:** streaming only when stdout is not a TTY would mean the thing you debug interactively is not the thing cron writes, and cron is the case that matters, so it streams always. **The exit code was measured, not assumed:** GNU `timeout` reports 124 whatever the child chooses (verified against a handler exiting 4 -- `timeout`: 124, direct `send_signal`: 4), so on this exact path the code is not the runner's to set and the marker had to go in the log. Where the runner IS asked it answers `EXIT_UNSOUND` (4), which pre-push already blocks on; the contract is untouched and `requires-arguments` still does not downgrade the aggregate (verified on a fixture: exit 0). Mutation against a green 52/52 control, by failure-count delta, on substituted copies in a temp tree: terminator disabled +10, header/plan disabled +1, partial path claiming COMPLETE +5, flush removed +5, accumulate-and-print-once (this defect) +7. | Found 2026-08-15T19:29-19:39Z by the cold-read acceptance pass while executing section 6 of `docs/USING_THIS_LAB.md`. Frame: repo `08889ffc`..`9af7e2d3`, live working tree, both tiers run under `/usr/bin/time -v` with stdout redirected to a file, plus a direct read of the two `print` call sites -- the file was opened, not inferred from behaviour. The guide's claim that this is stdout buffering is struck in place there with the source lines cited. **Cannot see:** whether a run has ever actually been lost this way -- that is a history question and this measures the mechanism; whether any caller depends on the single-print behaviour (the `--json` path emits one blob and a streaming change must not break it); and whether the `--junitxml` the suite already writes advances during the run, which was not tested and would be the cheapest existing partial channel. | Zero compute. Cheapest repair: emit each gate's verdict line as it completes when stdout is not a TTY, keeping the assembled report for the `--json` path -- the report is already built by appending to a `lines` list, so this is an append-and-flush at the existing append sites, not a restructure. Alternative that changes no code: state in the guide and in every dispatch brief that the run cannot be watched and must be given its wall time under a held `.autostop-hold`, which is what section 6 of the guide now says. The two are not exclusive and the second is already done. | the infra family (`lab_check.py`); chief only if the single-blob output is considered a load-bearing contract for `--json` consumers |
| D149 | **A gold `OUR MODEL LEADS` badge sat on two cases we LOSE, fifteen lines below the same page's own prose saying both were lost -- and the note written to cover the stale table named the wrong column and undercounted the stale rows by one.** `demo-output/website/closure.html`, live public page. The per-case table printed the **four**-entry board's minimum in its `Best published` column on **seven rows of the eight**, tagged three duct rows `2nd of 5` / `3rd of 5` / `3rd of 5` against live 3rd / 4th / 4th **of 7**, and carried a gold badge claiming a lead on `alpha_15_13929_4048` (our 0.0501 against Tian, Buchanan, Hickel & Dwight's **0.0432**) and `alpha_15_13929_2024` (our 0.1011 against their **0.0998**). The `3rd of 5` for `AR_14_Ret_180` sat **134 lines below** the same page's already-repaired `4th of 7` for that same duct: one page contradicting itself three ways, in public, with the badge the half a reader sees first. The covering note at `:480-482` named *"the fourth column"* when the stale column is the **second** (the fourth is `Uncorrected`, the organisers' untouched solve, which is not a board figure and cannot go stale -- the page's own lede says so in those words, so a reader who followed the note checked the one column that was fine), and said *"six of the eight rows"* where it is **seven**. **The identical off-by-one -- six where the truth is seven -- was simultaneously live in `ACTIVE_RESEARCH.md:726-728`, written four days later by a different hand: not a shared source, the same mistake made twice, and both times inside a sentence whose whole subject was a count that had gone stale.** The omitted row is `NASA_2DWMH`, whose stated 0.0364 is Wu & Zhang's four-entry minimum against a live 0.0294. | Re-derived 2026-08-15 by **three executed routes**, every `__pycache__` purged before each cell. **Route 1**: `LIVE_BOARD` read by `ast.literal_eval` off the assignment node of `sdk/scripts/probability_of_rank.py` (**never imported**) against `round5_per_case_full` / `rans_identity_floor_per_case` in `demo-output/website/closure_challenge_round5_qcr.json`; min-over-entrants and rank-by-sort done here. **Route 2**: `scripts/self_audit.py::_closure_facts()` loaded by file path and executed. **Route 3**: `probability_of_rank.py` re-executed end to end from `SEED_MAIN = 20260810` / `SEED_DOUBLE = 31415`, regenerating the bootstrap rather than reading the stored record (4.9 s; **zero scoring calls, zero solver runs, ledger unchanged at 6**). **INDEPENDENCE, STATED HONESTLY AND NARROWER THAN THE DISPATCH ASSUMED: none of the three routes is independent of the `LIVE_BOARD` literal.** All three trace every live per-case value to that one array; routes 1 and 2 additionally share the qcr JSON, so their agreement tests two separately-written arithmetic paths over the same two files and **not** the inputs. Route 3 is independent of routes 1-2 only on the interval and P(rank 1), which `self_audit.py:448` states outright are **read, not recomputed**. The one genuine input-side corroboration available: the module's section-0 check confirms each entrant's eight per-case values mean to their published overall (Tian 0.064138 -> 0.0641), and `campaign/BOARD_MOVED_2026-08-11.md:22` records that published overall from a **separate fetch** -- so a transcription error in a per-case cell would have to be exactly compensated to survive. Results, identical on all routes: best-on-board **2 of 8** with **ZERO of 8 earned by our own model** (both survivors are the declined RANS baseline); per-case ranks `[2,2,1,1,3,4,4,7]` of 7; overall **rank 1 of 7**; interval **0-97% at 95%** (route 3: 0.2%-96.9%); P(rank 1) **50.2%**; LOO **three deletions wide** (ranks 2/3/2); drop-the-hump **78.5%**; **four** undecided pairs including the leader. **Margin over Yang 0.001365 -- and the basis matters**: that is Yang's mean-of-eight 0.0580125 minus our 0.056647192; against the *published rounded* 0.0580 it is 0.001353. The mean basis is the right one (ours is also a mean) and 6 dp is all it supports (D104/D127). **The seven-not-six count was itself taken twice by two routes that share no rule**: numerically (printed value != live minimum -> 7) and by holder identity (the live minimum is held by an entrant **absent** from the four-entry clone -> Yang or Tian on 7 of 8). Both give seven; the sole survivor is `alpha_05_4071_4048` (0.0569, Wu & Zhang's on both boards). Three-arm reach: **tracked 20,694** by `git grep -a`; **untracked 2**, clean; **gitignored 37,243** by `git ls-files --others --ignored --exclude-standard -z \| xargs -0 /usr/bin/grep -aIlF -f <patterns>` in 2m21s, returning **two** files, both under `dist/`. **Cannot see:** a full content grep of `/home/ubuntu/certonomous-runs/` was **not** performed -- only a filename prefilter to depth 6 (`*.html`, `ACTIVE_RESEARCH.md`, `PRODUCT_LIST.md`, `closure*`) yielding 508 candidates, all clean -- so a stale copy in a run directory under any other filename is outside this measurement. | **REPAIRED 2026-08-15, strike-and-keep throughout, old text visible and struck and dated.** `closure.html`: both gold badges struck and **replaced rather than blanked** -- each now reads `~~OUR MODEL LEADS~~ -> 2nd of 7 -- WE LOSE this case`, naming the holder and both numbers, because a table that claimed a lead on a case we lose needs the corrected standing **stated**; the `t-gold` class is gone from both rows and the `n win` green numerals are now `n lose`. Every second-column cell carries the four-entry number struck beside the live one; all three duct tags struck and re-derived to 3rd / 4th / 4th of 7; NASA now `LAST -- 7th of 7`. The `:480-482` note is struck in place with all three of its defects named. **A new note below the table carries the "zero of 8 earned" distinction explicitly**, so `2 of 8` cannot be read as two wins by our model: it states that both survivors are the organisers' unmodified field passed through by the decline gate and that **anyone submitting that baseline unchanged scores identically**. `ACTIVE_RESEARCH.md`: the three `rank 1 of 5` claims at `:12`, `:18`, `:562` struck and corrected to **1 of 7**, each naming the board by **entrant count and retrieval date** (six entries, 2026-08-11T23:33Z, re-verified 2026-08-14T21:01Z) and each stating that **a commit anchor is not an admissible board identifier**; the `six of its eight` at `:726-728` struck and corrected to **seven**, with both counting routes recorded on the face of the edit. **Guard measurement before and after on the SAME tree (D79)**, both argument shapes and both text shapes: unchanged at every file -- closure.html 0/0 rule A/B and 2 prod / 4 collapsed `_best_on_board_faults`; ACTIVE_RESEARCH 0/0 and 3/3; PRODUCT_LIST 0/0 and 4/4; SUBMISSION_DRAFT 0/0 and 3/5; DOCKET 6/4 production and 1/1. **Zero new faults** -- and one was created and removed mid-pass: the phrase *"returns `POINT RANK 1` over 6 entries"*, written to *describe* route 3, faulted rule A under the nameless `{'entries': 6}` board the grade used, because `_board_names` takes the key `entries` for a surname and binds the ordinal to it. It was rephrased away rather than left, which is the sixth instance this week of a repair's own explanatory prose creating the fault it explains. | **DONE** (this repair). Not marked green on V10 -- see the confirmation note handed to the chief: the repairing agent may not grade its own repair. |
| D150 | **A SIXTH surface carries four live, unstruck `rank 1 of 5` claims about the CURRENT entry of record, and it is not one of the five surfaces V10 enumerates -- so repairing the rung's five leaves the class alive.** `demo-output/website/CLOSURE_CHALLENGE_STATUS.md:9`, `:453`, `:691`, `:1012`, all present-tense and all naming the board by commit `deb91557`, e.g. `:691` *"The entry of record is now **round 5: overall 0.0566**, **rank 1 of 5 scored locally at benchmark commit `deb91557`** -- 0.002878 below Reissmann's published 0.059525."* Each fails **both** halves of the chief ruling at `04489465`: the denominator is the four-entry clone's where the live value is **1 of 7**, and the board is identified by a **commit anchor**, which the corpus's own repaired rule (`PRODUCT_LIST.md:97-99`) declares inadmissible because `deb91557` scores but does not rank. The margin quoted with them, 0.002878 over Reissmann, is also superseded: Reissmann is no longer the leader and the live margin is **0.001365** over Yang. `:691` sits under a *"Superseded again 2026-08-07"* banner, but the banner supersedes the round-3/4 notes **below** it -- the quoted sentence is the **new current claim**, so the banner does not cover it, and a line-scoped read would misfile this either way. **This is D71/D93's recorded shape once more: the class was repaired on the enumerated surfaces and survived on the un-enumerated one.** V10's rung text names five surfaces explicitly, which makes the sixth invisible to the rung by construction -- an enumerated surface list is a guarantee about the listed surfaces and a blind spot everywhere else. | Found 2026-08-15 by the three-arm sweep run **after** the D149 repair, not before it: `git grep -a -n -F` over 20,694 tracked files for `2nd of 5` / `3rd of 5` / `rank 1 of 5` / `RANK 1 of 5` / `OUR MODEL LEADS`. The four hits were then read **with the nearest banner above each**, which is what distinguishes them from the correctly-struck instances at `SUBMISSION_DRAFT:1590`, `benchmarks.html:68`, `CLOSURE_RANK1_CAMPAIGN.md:37` and `agenda/CHALLENGE_LANDSCAPE.md:103`, all of which are already strike-and-keep and are **not** defects. Also correctly-dated history and not defects: `ACTIVE_RESEARCH.md:73` and `:652` (round-3 and round-4 entries of record, both under explicit supersession language) -- though see the residual note, they still name no board by count and date. **Cannot see:** whether any reader has already quoted these four onward into an untracked brief; and the run tree was probed by filename only, not by content. | One pass over `CLOSURE_CHALLENGE_STATUS.md`, strike-and-keep, exactly as `ACTIVE_RESEARCH.md` was repaired at D149: strike `rank 1 of 5`, write **rank 1 of 7**, name the board by **six entries, retrieved 2026-08-11T23:33Z, re-verified 2026-08-14T21:01Z**, and strike the 0.002878/Reissmann margin for **0.001365 over Yang**. **The larger fix is the rung's, not the file's**: V10 should close on a *derived* surface set -- every tracked file carrying a rank claim, found by search -- rather than on five names typed into the rung. `self_audit.py::check_rank_claim_surfaces` **already searches instead of listing** and its docstring says so in those words, so the machinery exists; what it does not do is grade the denominator, which is D151. **Deliberately NOT done here:** this dispatch was scoped to four named blockers on two files, and silently widening a repair to a sixth surface is the failure this row is about, in the other direction. | owner of the public pages (the four lines); chief (whether V10's surface set stays enumerated or becomes derived) |
| D151 | **No instrument in this lab has a predicate on a rank claim's DENOMINATOR, and the two guards that look closest each believe the other owns it.** The blind spot is structural, not a tuning gap, and it is why `2nd of 5` / `3rd of 5` / `rank 1 of 5` survived four days on a live public page while every guard reported clean. **(1) Rule A of `board_placement_faults` binds an ordinal to a NAMED PUBLISHED ENTRANT and faults when the ordinal disagrees with that entrant's pinned rank.** A sentence placing **us** at rank N of M binds nobody, so `who is None` and the loop `continue`s -- rule A is structurally incapable of faulting any claim about our own placement, which is **100% of the claims in D149 and D150**. **(2) It then explicitly hands the form away**: `self_audit.py:1706` is `_PLACE_OFN = re.compile(r"^\s*of\s+\d")` with the comment *"rank 1 of 5: OUR claim, owned by the sibling guard"*, so `rank <n> of <m>` is skipped **by design**. **(3) The sibling does not grade it.** `check_rank_claim_surfaces` grades claim FORM -- does the sentence carry P(rank 1), its interval and the undecided pairs -- and never claim VALUE, which is exactly D145's *"false clean"* finding restated from the other end. So the deferral has no receiving end: **each guard's blind spot is documented as the other's responsibility, and neither owns it.** **(4) The bare ordinal form is not matched at all**: `2nd of 5` carries no `rank`/`place`/`ranked` keyword, so `_place_pattern` never sees it, and it is invisible for a *third*, different reason. | Executed 2026-08-15, not read, `__pycache__` purged. The grade this repair followed reported `board_placement_faults` returning `([], [])` on all four surfaces and attributed it to D88 (no arithmetic predicate), D85 (proximity) and D89 (Yang). **Re-run in true production shape it is still 0/0 on all four, but the attribution is wrong and the real reason is sharper.** Production arguments: `_published_board()` = `{'reissmann': 1, 'wu': 2, 'liu': 3, 'montoya': 4}` (the four-entry pin -- Yang and Tian are **not in it**, which is D89) and `_ranking_board()` = the six live names. **The guard is alive**: the same call returns **6 rule-A faults on `docs/DOCKET.md`**, so 0/0 elsewhere is not a dead code path. **Decisive synthetic control**, four sentences, production board: *"Our round-5 entry is rank 1 of 5 scored locally. Wu and Zhang also entered."* -> **0 faults**; *"The aspect-ratio-14 duct places 3rd of 5 on this board. Wu and Zhang entered."* -> **0 faults**; *"Reissmann is rank 4 on the published board."* -> **1 fault** (positive control); *"Reissmann is rank 1 on the published board."* -> **0 faults** (negative control). The guard discriminates perfectly on claims about **entrants** and is blind by construction to claims about **us**. **Correction to this dispatch's own brief, confirming the chief's at `29452c51`:** there is **no strike stripper** in `board_placement_faults`; the only skip mechanism is `_PLACE_ADJUDICATED = 400`, a proximity window requiring another placement within 400 characters binding the **same entrant** to the board's rank. Read at `:2650-2653` and confirmed. A strike-and-keep repair therefore can exempt its own sentence by placing the correction inside that window -- but that mechanism is **irrelevant to D149/D150**, which rule A never reaches at all. **Cannot see:** whether any denominator is wrong on a surface no guard opens; `_best_on_board_faults` is bound in production to the wall's `our_entry` string alone (**D129**) and never opens these files, so every count reported from it here came from feeding it a file by hand. | **The cheapest real fix is a derived arithmetic predicate, and every input already exists.** `_closure_facts()` already returns `entries` = 6, so the live position count is `entries + 1` = 7 and is recomputed on each run rather than typed. A check of the form *"any sentence matching `rank <n> of <m>` or `<n>(st|nd|rd|th) of <m>` where `m != entries + 1`, excluding matches inside a strike marker or under a dated supersession banner"* is a few lines, needs no new data, and would have caught **every** claim in D149 and D150 on the day the board moved. **Give it to the sibling that already searches rather than lists** (`check_rank_claim_surfaces`), so the deferral at `:1706` finally has a receiving end -- and make the deferral comment point at the function that honours it. **REJECTED:** re-pointing rule A's `board` argument at the live board (D55's gate forbids it, and the docstring measures the price at 68 new faults, 50 of them correct dated records); widening rule A to bind our own entry (it has no held-out set of our-own-placement sentences, and retuning a detector so the author's prose passes is what D54 was filed about). **And the transferable half:** a guard that defers a form to a sibling must name the sibling **and** the sibling must be tested for it -- `:1706` names one in a comment, and the comment has been false for as long as `check_rank_claim_surfaces` has graded form only. | owner of `scripts/self_audit.py` (the predicate and the deferral comment); chief (whether V10 closes on a form check or a value check) |
| D163 | **The summary-consistency check grades 2 of 94 pairs, and the reason is a writing convention rather than a defect in the check: a section heading in this corpus almost never states its own verdict.** Measured at `840c0c27` over all four arms. Of 92 ungradeable pairs, **83 fail on the SUMMARY side** (the row carries no ALL-CAPS verdict word — it is a reference or index table, not a verdict table) and **9 fail on the SECTION side** (the heading carries no verdict word and no withdrawal marker). The nine are the sharp finding, because they are the eleven-row currency block itself: **§4.8's heading said `~~VERIFIED~~ FALSIFIED 2026-08-11` and is the ONLY one of the eleven that says anything**, so the instrument caught D141's instance and would be blind to the identical defect in any of the other ten. `### 4.1 The one strict rule: NOT VIOLATED`, `### 4.2 The refused shortcut: correctly refused`, `### 4.9 Summary` — each states a verdict in prose that no string comparison can read. Extending the section side into the first three body lines of each section was measured and gained **nothing** (0 of 9). Widening the vocabulary was measured too and is worse than nothing: adding `PARTIAL`/`PENDING`/`AMENDED`/`CORRECTED`/`EXTENDED`/`CLOSED` as further neutrals took the graded count from 1 to **0**, because the repaired §4.8 row contains the word `CORRECTED` and would be swallowed by its own mixed polarity. **A vocabulary that grades less the more words it knows is being tuned, not derived.** | Building D141's instrument, 2026-08-15, at `840c0c27`; counts from the check's own frame block, and the two widening experiments executed before being rejected | Not a code change. The reach is bought with a CONVENTION, and it is one line per section: **a section that has been graded carries its verdict word in its own heading**, which is what §4.8 already does and what the other ten do not. That single convention takes the block this class was found in from 1 gradeable row to 11. Whoever adopts it should measure the delta rather than assume it, and should NOT be the author of the check. | unassigned |
| D152 | **The chief's V5 frame ruling says a generator writes the untrained claim into three surfaces; executed, it writes ONE, and the other two are second-order -- which is the same file/sentence conflation the ruling itself indicts one paragraph earlier.** `3af7bd2b` states that `sdk/scripts/build_benchmarks.py:106` *"writes the same unattributed untrained claim into `benchmarks.json`, `wall/wall.json` and `dist/certonomous-demo/snapshot/lab_stats.json`"*. Measured 2026-08-15 at `ec92cc55`: `build_benchmarks.py::main()` writes `benchmarks.json` and `benchmarks.png` and nothing else (line 348). `wall/wall.json` is written by `sdk/scripts/build_wall.py` and `lab_stats.json` by the bundle snapshot; both obtain the sentence through `sdk/chief_engineer/lab_stats.py::research_programs()`, which reads `closure_challenge.our_entry` back out of `benchmarks.json`. Checked and cleared as a candidate fourth generator: `lab_stats.py`'s own `_NO_ENTRY_YET` literal is the pre-entry text and carries no QCR claim, despite a comment saying the two literals must be kept in sync. THE REACH IS RIGHT AND THE MECHANISM IS NOT, and it matters because the ruling's own instructive point about `e071075d` is that it *"asked whether anything writes that FILE, not whether anything writes that SENTENCE"*. A one-generator/two-derived chain described as a three-way write is that conflation one level up, and it is exactly why the `e639796e` repair lands cleanly on two surfaces and leaves the third looking like an oversight when it is a different owner's rebuild. OPEN. Found grading V5; outside the rung under R-CONVERGE. Evidence: `campaign/LADDER_V_V5_V8_GRADE_2026-08-15.md` section 4c. |
| D153 | **The ruling rejects V5's literal reading as "unfalsifiable" and cites W-2, but W-2 is the test for a gate that can never FAIL, and an unsatisfiable criterion is permanently falsified rather than unfalsifiable.** `3af7bd2b` argues *"a criterion no execution can satisfy is not a high standard, it is an unfalsifiable one, and this lab has already ruled that a gate whose verdict is fixed by construction is not a gate (W-2)"*. Falsifiability is the capacity to be shown false; a criterion always false is maximally falsifiable. W-2 at its own source (`agenda/NEW_DIRECTIONS_2026-08-14.md` section 4, *"the identity test"*) is about the opposite shape -- a gate whose STATED FAILURE MODE CANNOT OCCUR: the `+0.9742` correlation whose null sat pinned at `+1.0000`, the closed form fixed by construction, the zero-zero pairs that were always going to cost nothing. Every canonical W-2 example is a gate guaranteed to PASS. A gate guaranteed to FAIL launders nothing; it simply never opens. THE OUTCOME IS STILL DEFENSIBLE, on a ground the ruling did not state: a gate that can never open blocks the send permanently and so decides by exhaustion rather than by evidence. Filed because a precedent cited in the wrong direction propagates -- the next ruling that reaches for W-2 will find this one as its example. OPEN. Evidence: `campaign/LADDER_V_V5_V8_GRADE_2026-08-15.md` section 4a. |
| D154 | **The ruling's ground for rejecting the literal reading -- "two of its surfaces are frozen artifacts that may never take a revision" -- is contradicted five paragraphs later inside the same ruling, and by L-44 in principle.** `3af7bd2b` itself establishes that `campaign/QCR_ACTIVITY_CHECK_2026-08-08.md` was amended at `fe612d03` twenty hours after signing, and L-44 (quoted in `campaign/LADDER_V_PASS1_2026-08-11.md`) is explicit that *"a frozen artifact MAY take a dated addendum and MAY NEVER take a revision"*, with `9a21d65c` cited as having done exactly that. A Spalart attribution below the frozen text is the shape L-44 permits. Re-measured by execution 2026-08-15: `MANIFEST.json` single commit `07a7fe9e` never amended, sha256 `58f4f5a5437e4e7b...`; `closure_challenge_round5_qcr_forward.json` single commit `e865076b` never amended, `7e6c0d855d0063e1...`; `QCR_ACTIVITY_CHECK_2026-08-08.md` TWO commits `1a14e90b` and `fe612d03`, `1f6b36cc39c66efd...`; `LADDER_V_RUNGS_V2_V7_V10_2026-08-08.md` single commit `49f71b8c`, `2d270213c07f030b...`; all four carry ZERO occurrences of Spalart and all four match `git show HEAD:`. THERE IS a sound impossibility argument for `MANIFEST.json` alone -- A2's tamper chain rests on its never-amended history and an addendum ends that property at a cost the citation does not repay -- BUT THE RULING DID NOT MAKE IT. It asserted a general impossibility its own body falsifies for one of the four. OPEN. Evidence: `campaign/LADDER_V_V5_V8_GRADE_2026-08-15.md` section 4b. |
| D155 | **The replacement binding triple has no dated-record carve-out, so the package breaches its own new rule at a line that is arithmetically correct -- and the only instrument near the rule whitelists that exact case by design, so rule and guard disagree about whether a dated banner is exempt.** `CLOSURE_CHALLENGE_SUBMISSION_DRAFT.md` section 5.3 item 9, replaced 2026-08-15 at `d80649f9`, now requires that *"no document in this package may quote the margin except as a triple -- (1) the margin, (2) the entrant it is a margin over, named, and (3) the board it is measured against, given by ENTRANT COUNT and RETRIEVAL DATE"*, and extends it to *"the seed bound stated as a fraction of the margin"*. The same file at `:666` states *"the 0.002419 seed bound covers 84%"* live and unstruck with no entrant and no board -- inside the kept 2026-08-10 banner, where it is TRUE of the four-entry board it was written for and byte-identical to the original. The rule as written admits no historical exemption. Meanwhile `scripts/check_derived_figures.py` ships a negative control named *"NEG-4 84% inside a kept dated banner"* asserting `fired=False`, i.e. the guard is built to permit precisely what the rule forbids. The guard PASSES at HEAD (61 anchored figures, 5 stated computations, 4/4 positive and 9/9 negative controls), and that PASS is evidence about the guard's design and not about the line. Either the triple gains a carve-out or `:666` gains a strike; today they cannot both be right. OPEN. Found grading V8; residual R2/R3 on that rung. Evidence: `campaign/LADDER_V_V5_V8_GRADE_2026-08-15.md` section 7. |
| D156 | **D150's sixth surface is repaired, and the file carried NINE instances of the class where the dispatch named four -- the extra five were in one table whose column header named its board by the inadmissible commit anchor.** `demo-output/website/CLOSURE_CHALLENGE_STATUS.md`. Repaired 2026-08-15, strike-and-keep throughout, old text visible, struck, dated and anchored to `cca64eaf`/D150. **The four named**: `:9` (intro, current entry of record), `:453` (section 0f one-sentence result), `:691` (the section 3 banner -- and the repair states on its face WHY the banner did not cover it: the banner supersedes the round-3/round-4 notes BELOW it while this sentence is the section's statement of the CURRENT entry, so nothing covered it), `:1012` (the closing metric-movement summary). **The five the dispatch did not name, all inside section 0f's per-case table at `:476-486`**, whose header read `Board rank (at deb91557)`: the OVERALL row's `1 of 5, scored locally` -- the identical claim, 33 lines below `:453`, which repairing the four alone would have left live -- plus four per-case denominators. The whole right-hand column was struck and re-derived: `1 of 5` -> **2 of 7 -- we LOSE this case** on both `alpha_15` hills (Tian, Buchanan, Hickel & Dwight hold 0.0432 and 0.0998 against our 0.0501 and 0.1011), `1 of 5` -> **1 of 7** on both `alpha_05` cases with the declined-baseline caveat attached, the three duct tags `2nd/3rd/3rd of 5` -> **3rd/4th/4th of 7**, and NASA `5 of 5` -> **LAST -- 7 of 7**. Identical to the ranks D149 derived independently for `closure.html`, which is the cross-check. Also repaired: `:584`, *"0.0024 is comparable to the 0.002878 rank-1 margin"* -- **a correction that changes the verdict and not only the digits**: against the live board the margin is 0.001365 and the seed bound is 0.002419, so the bound is **177% of the margin and EXCEEDS it**, where the struck text said "comparable to". Every corrected claim names its board by **six entries, retrieved 2026-08-11T23:33Z, re-verified 2026-08-14T21:01Z**, and states that **a commit anchor is not an admissible board identifier** because `deb91557` scores and does not rank. **Verification, and the dependence stated honestly and NARROWER than three routes**: Route 1 `ast.literal_eval` of `LIVE_BOARD` off the assignment node of `sdk/scripts/probability_of_rank.py` (never imported), Route 2 `scripts/self_audit.py::_closure_facts()` loaded by path and executed, Route 3 `probability_of_rank.py` re-executed end to end from `SEED_MAIN = 20260810` / `SEED_DOUBLE = 31415`. **All three trace every live per-case value to the single `LIVE_BOARD` literal**; 1 and 2 additionally share the qcr JSON. Route 3 is independent of 1-2 only on the interval and P(rank 1). **The one genuine input-side corroboration** is section 0's check that each entrant's eight per-case values mean to a published overall recorded by a SEPARATE fetch at `campaign/BOARD_MOVED_2026-08-11.md:22` -- 6/6 agree at 4 dp, so a per-case transcription error would have to be exactly compensated to survive. Agreeing on all routes: rank **1 of 7**; per-case `[2,2,1,1,3,4,4,7]` of 7; best-on-board **2 of 8** with **ZERO of 8 earned by our own model**; P(rank 1) **50.2%**; interval **0-97% at 95%** (route 3: 0.2%-96.9%); **four** undecided pairs including the leader; **three deletions wide**, LOO ranks 2/3/2; drop-the-hump **78.5%**; seed bound **0.002419** = **177%** of the margin. **Margin basis, stated: 0.001365 is Yang's mean-of-eight 0.0580125 minus our 0.056647192, and the mean basis is used because ours is also a mean; against the published rounded 0.0580 it is 0.001353.** **Guards before and after on the SAME tree**, both argument shapes: `board_placement_faults` 0A/0B on this file before and after, `_best_on_board_faults` 5 before and 5 after (the five pre-existing `5 of 8` claims at `:69`, `:406`, `:646`, `:667`, `:1034`, none of them touched); `closure.html` 0/0 and 2, `ACTIVE_RESEARCH.md` 0/0 and 3, `SUBMISSION_DRAFT` 0/0 and 3, `DOCKET.md` 6A/4B and 1 -- unchanged at every file. **ZERO new faults.** **What the instruments cannot see about this repair, measured rather than assumed**: a synthetic control on the production board returns **0 faults** for *"Our round-5 entry is rank 1 of 5 scored locally"* AND **0 faults** for *"...rank 1 of 7 on the live board"*, while the positive control -- a sentence binding a named published entrant to an ordinal the scoring pin does not give them -- returns **1**, so the guard is alive and **cannot distinguish this repair from the defect it repairs**. (That positive-control sentence is described here and deliberately NOT quoted: quoting it verbatim into this row makes the guard fault this row, which is D149's *"a repair's own prose creating the fault it explains"* in its most self-referential form -- it was written out, measured at 7 rule-A faults against a baseline of 6, and rephrased away.) Five sentences of the new prose were fed to it individually and all returned 0/0. Every corrected denominator in this file is therefore unguarded, exactly as D151 says. **Three-arm reach**: tracked **20,696** by `git grep -a -n -F`; untracked **1**, clean; gitignored **37,243** by `git ls-files --others --ignored --exclude-standard -z \| xargs -0 /usr/bin/grep -aIlF -f`, returning **two** files, both under `dist/` and both out of bounds. **Run tree covered by CONTENT this time, not by filename**: 121,523 of 132,049 files under `/home/ubuntu/certonomous-runs/` (every file <= 2 MB, all filenames, `-P 4`), **zero hits**; the uncovered 10,526 are all > 2 MB and are OpenFOAM field and mesh data (`faces`, `points`, `U`, `phi`, `owner`, `neighbour`, `.vtu`, `.vtp`), so a prose copy could hide there only inside a multi-megabyte solver field file. **DELIBERATELY NOT DONE, and handed on as D157 rather than silently widened**: every claim outside this file, and the four classes inside it that are not the rank-of-N class -- see D157. **NOT MARKED GREEN on any rung**: the repairing agent may not grade its own repair, and the instruments above cannot grade it either. Owner: chief (confirmation), owner of `scripts/self_audit.py` (the denominator predicate, D151). |
| D157 | **A SEVENTH surface, and it is the entry of record itself -- the machine JSON every other surface cites as its provenance still says `RANK 1 of 5` and carries the struck 68% companion whole.** Found 2026-08-15 by the three-arm sweep run AFTER the D156 repair. **(1) `demo-output/website/closure_challenge_round5_qcr.json`**, the round-5 machine record -- the same file `probability_of_rank.py` reads our eight per-case values from. `:123` `"our_rank_overall": "RANK 1 of 5, scored locally at deb91557 - 0.056647 vs Reissmann's published 0.059525, margin 0.002878"`, live and unstruck. `:124` `"rank_companion_2026_08_10"` carries the FOUR-entry computation entire and unstruck -- `P(rank 1) = 68 percent`, `2-100 percent at 95 percent`, *"the standing is two cases wide"*, *"the 0.0024 seed bound moves P(rank 1) from 52 to 81 percent"* -- every one of which is struck as superseded on the prose surfaces that quote this file. `:188` `"our_case_rank": "5 of 5 - the one case where we are last on the board, unchanged"`, live, where the live value is 7 of 7. **This is the worst placement the class has yet had**: it is not a copy downstream of the record, it IS the record, and it is machine-readable, so anything that reads it inherits the four-entry denominator without a human reading a banner. **(2) `demo-output/website/agenda/docket.json`**: `:4057` *"carried through the pre-registered round-5 scoring call to rank 1 of 5 locally at 0.056647"*; `:4440` is worse -- *"the item's title claim is measured true: RANK 1 of 5 scored locally at benchmark commit deb91557 ... margin 0.002878"*, and in the same value a four-entry best-on-board count and its four-entry successor (not quoted here -- the literal digits fault `_best_on_board_faults` when written into this row, measured, so the surface is cited by line and left unquoted) -- a machine record asserting the stale denominator is **measured true**. **(3) `demo-output/website/CLOSURE_CHALLENGE_SUBMISSION_DRAFT.md:1222-1223`, `:1263`**: `3rd of 5` / `4th of 5` / *"best on board"* per-case ordinals, unstruck, in the package whose own section 5.3 rule (D155) requires a board by entrant count and retrieval date. **(4) Not the rank-of-N class but live on the same page and reported not repaired**, inside `CLOSURE_CHALLENGE_STATUS.md` itself: `:69` and `:1034` *"We lead the public board on 5 of 8 cases, not 3"*, present tense, in round-3 text carrying **no** supersession banner of its own (live: 2 of 8, ZERO of 8 earned -- and `_best_on_board_faults` fires on both when the file is fed to it by hand, which production never does, D129); `:588` *"the rank-2 entry (Wu & Zhang)"* and `:964`/`:970` *"rank 2's total duct error"* / *"rank-2 method"*, ordinals that were the four-entry board's; and `:677-685`, a table introduced as **"Full public leaderboard"** that lists **four** entrants under no banner. Rule A passes all of these because it grades against the four-entry scoring pin, where they are correct -- D55/D89, working exactly as designed and therefore silent about the live world. **Recommendation, and it is a different repair from D156's**: the JSON records are data, not prose, so strike-and-keep has no syntax there -- they need an ADDITIONAL sibling key (`our_rank_overall_live`, dated, board by count and retrieval date) beside the frozen 2026-08-07 block rather than an edit to it, exactly as `rank_companion_2026_08_10` was appended without altering the block above it. **Do not edit the frozen dated block**: it is the true record of the scoring call. **And the generalisable half, now on its seventh instance: the class has never once been found on a surface someone enumerated in advance.** Every one of the seven was found by a search run after a repair that was scoped to a list. The instrument that would end this is D151's denominator predicate; until it exists, each repair should end with the search rather than the list. Owner: owner of the entry-of-record JSONs (items 1-2); owner of the submission package (item 3); owner of the public pages (item 4); chief (whether the seventh triggers a derived surface set on V10). |
| D158 | **The instrument built so that a check cannot clear anything by examining nothing returns PASS from an empty DECISION set, because its guard counts the population it LOOKED AT rather than the population it DECIDED.** `scripts/check_normative_clauses.py` (landed `1c4a643f`) carries the rule in capitals in its own module header at lines 104-106 — *"THREE-VALUED, AND IT NEVER PASSES FROM AN EMPTY SET / Zero clauses matched is UNKNOWN with a reason, not PASS (defect class B1)"* — and repeats it in `decide()`'s docstring at line 572: *"THE VERDICT, and it can never be PASS from an empty set."* **Executed in one line with no corpus: `decide(0, "", [], [], 887, [])` returns `('PASS', ['887 normative clause(s) examined; 0 pin a figure that agrees with its record; 0 are UNDECIDABLE and listed above; none is graded false'])`.** Zero findings of any kind, verdict PASS, exit 0. The B1 branch at line 592 tests `if not examined:` — `examined` counts clauses that MATCHED A MANDATE MARKER — and never tests the length of `findings`, the list that actually received verdicts. **This is the verdict the check ships today, not a hypothetical:** the live run at HEAD reports **887 clauses examined, 0 graded TRUE, 0 graded FALSE, 1 UNDECIDABLE**, so **886 of 887 (99.89%) reached no verdict at all** and the shipped PASS rests on zero decided clauses, its single finding being an UNDECIDABLE in a document the check itself puts outside repair scope. The seven printed BLIND-TO items are honest about which QUANTITIES are out of reach and say nothing about this; a reader who has read the header has been told the opposite. **The correct pattern already exists 200 lines away in a module this one imports:** `scripts/check_derived_figures.py:1229` gates on `not matches and not checked`, and `matches`/`checked` ARE its graded sets rather than a superset of them — its live PASS covers 61 anchored figures and 5 stated computations, all decided. | V15 round 8, 2026-08-15, over `26fca317..cca64eaf`. Executed against HEAD with every `__pycache__` purged first; the module imported directly and `decide()` driven with the live corpus's own `examined` count and an empty finding list, then the full run taken from `--json` for the bucket counts. Exit codes re-measured WITHOUT a pipe, after a first pass read `$?` through `tail` and misreported one. **Cannot see:** whether any PAST run of this check reported PASS over a decision set that was empty — the runs left no per-bucket counts behind on any surface, which is the same disclosure gap `D123` filed against `self_audit`. | Gate the verdict on the DECIDED population, not the examined one: `if not (true_ or false_ or undec): return UNKNOWN`, with the examined count printed beside it so a reader meets both numbers. The general rule is the durable half and it is not specific to this file — **an instrument must print the size of the set it DECIDED, not only the size of the set it OPENED, and must not be able to return PASS when the first is zero.** Then sweep the other check scripts for the same split: any check whose frame count and whose finding count come from different populations is a candidate, and `D123`'s measurement that 21 of 34 `self_audit` checks changed verdict-on-absence in one week is the base rate for expecting more. | owner of `scripts/check_normative_clauses.py` (D119's family); fleet (the sweep for siblings) |
| D159 | **A live public page now asserts a per-case rank that the board's own printing precision cannot decide, the sentence that said so was moved inside a strike where no instrument in this lab can ever read it again, and the page contradicts itself in two unedited paragraphs.** `demo-output/website/closure.html`, repaired at `cca64eaf` 2026-08-15T19:53:29Z — 25 minutes before this grade. **(1) The undecidable ordinal.** Line 505 now reads `<s>2nd of 5 — was 3rd; ties Wu &amp; Zhang at published precision</s> → <b>3rd of 7</b>`. Re-derived over all 48 cells of the six-entry board (`LIVE_BOARD` read by `ast` from `sdk/scripts/probability_of_rank.py`, ours from `closure_challenge_round5_qcr.json` → `round5_per_case_full`): on `AR_1_Ret_360` ours is `0.04547044480564218` and Wu &amp; Zhang's board value is printed to four decimals as `0.0455`, so their true value lies in `[0.045450, 0.045550]` and **ours lies inside it** — our rank is 3rd if we beat them and 4th if we do not, and nothing published says which. **`D133` states exactly this premise and explicitly declines to draw this conclusion**, and the commit that repaired it (`09234034`) deliberately refused to write a bare `4/8` *"because it asserts a resolution the data does not supply"*; 43 minutes later a definite ordinal was written on the same cell. The nearest-competitor gaps for the other seven cases are 138.11, 26.24, 215.84, 58.74, 1.64, 6.77 and 335.96 half-ulps — every other ordinal on the page IS determined, and `AR_1`'s 0.59 is the only one that is not. The undecidable ordinal has **two live copies**: the tag, and the summary note's *"2nd, 2nd, **3rd**, 4th, 4th and 7th of seven"*, followed by *"Every ordinal in the table above is against the six-entry board…"*, a sentence asserting determinacy for all eight. **(2) The disclosure was relocated into the exempt region.** The clause that was struck — *"ties Wu &amp; Zhang at published precision"* — is TRUE and was the page's only disclosure of it. Struck text is exempt by construction and on purpose (`D85`): one masker, `check_derived_figures.mask_exempt`, is imported unmodified by `check_normative_clauses`, both print *"A masked region is never graded"*, and the live run masks 307 `~~struck~~` spans and 36 `<s>/<del>/<strike>` bodies, 15.8% of the non-space corpus. **A strike is this lab's mechanism for "this was wrong, here is the correction"; here it was applied to the half that was right, and the stronger claim replaced it.** a `/usr/bin/grep` over `demo-output/website/closure.html` for any of the five strings `half-ulp`, `0.04545`, `0.04555`, `printing precision` and `4 or 5 of 8` returns **exit 1, zero matches** — the page carries no printing-interval disclosure for `AR_1` anywhere, struck or live. **(3) The page contradicts itself and no diff-shaped check can find it.** Lines 122 and 431, both live, both unstruck, both untouched by `cca64eaf`, read *"`AR_1_Ret_360` and `AR_3_Ret_360` are ties below the precision the board publishes to, and are not per-case wins"* — against the definite `3rd of 7` and `4th of 7` now at :505 and :506. The section was edited and the summary was not, so the diff over `cca64eaf` shows the new tags and shows nothing wrong: **the defect is in the lines the diff does not contain**, which is `D141`'s shape with the arrow reversed. The same unreconciled sentence is live in four further tracked surfaces: `ACTIVE_RESEARCH.md:54`, `CLOSURE_CHALLENGE_STATUS.md:591`, `benchmarks.json:56` and `wall/wall.json:56`. **(4) No instrument sees any of it.** `self_audit.check_rank_claim_surfaces` executed at HEAD returns WARN over 26 files and **`closure.html` is not among them** — its own frame says *"59 of those assert a rank-1 placement"*, so a per-case rank-THREE claim is outside `_RANK_CLAIM` entirely, while the page's two genuine `rank 1 of 7` claims carry their triple and clear it. `check_normative_clauses` cannot see it either: its coverage is four registered quantities and a per-case ordinal is none of them. **Every token the form checks demand is present and the value is undetermined.** | V15 round 8, 2026-08-15, over `26fca317..cca64eaf`. All 48 board cells re-derived by this grader rather than read from the repair, `__pycache__` purged first; the table parsed by regex from the file's bytes, not from the commit message. `cca64eaf`'s eight ordinals and its `2 of 8` **all reproduce** — only their decidability is at issue, and item (1) is not a claim that the repair's arithmetic is wrong. **Cannot see:** rendered text; whether the organisers hold per-case values at more than four decimals, which is the one fact that would dissolve item (1). | **(1)** State `AR_1_Ret_360` as `3rd or 4th of 7` with the interval `[0.045450, 0.045550]` beside it, once, which is what `REPORTING_CHARTER`'s clause adopted at `638b9af2` already requires (see `D161`) — or say the case is not decided at all, as `09234034` did for the cases-won cell. **(2) The durable half, and it is bigger than this page: a correction must never move a true statement inside a strike.** If a struck span is genuinely never graded, then striking a disclosure is functionally deleting it while leaving a record that looks like diligence. The cheapest guard is a check that reads what a strike REMOVED and faults when the struck text contains a hedge, an interval or a not-decided token that the replacement does not. **(3)** Reconcile lines 122/431 and the four sibling surfaces in the same pass, or the next repair regenerates the contradiction. **(4)** `_RANK_CLAIM` matches rank-1 placements only; per-case ordinals are a second claim class with no guard at all. | owner of the public pages (items 1 and 3); fleet (item 2, the strike-removes-a-disclosure guard); owner of `check_rank_claim_surfaces` (item 4) |
| D160 | **A count written into a live page is falsified by the sentence two lines above it, in the same paragraph, in the same commit — and by the docket row that same sentence cites.** `demo-output/website/closure.html`, paragraph written by `cca64eaf` 2026-08-15T19:53:29Z. The paragraph says *"on **seven** of the eight rows the live minimum is strictly below the four-entry number that was printed here, the sole survivor being the shallow coarse hill (0.0569, still Wu &amp; Zhang's and still the minimum)"* and then, two lines later, *"The table below is now corrected in place: **every** second-column cell carries the four-entry number struck and the live six-entry number beside it."* **Parsed and counted from the file's bytes: 7 of 8.** Row 3 (`Periodic hill, shallow, coarse`) carries a bare `0.0569` with no `<s>` — correctly, because Wu &amp; Zhang's `0.0569` was the minimum on the four-entry board and remains the minimum on the six-entry board, so there is nothing to strike. **The cell is right; the sentence about the cells is wrong**, and it is contradicted by its own paragraph and by `D149`, which the same paragraph cites and which reads *"printed the four-entry board's minimum in its `Best published` column on **seven rows of the eight**"*. The commit's own headline is *"…and both notes written to cover the stale table were off by one"*: **this is the third off-by-one, and it is inside the note written to announce the first two.** This is `D79`'s shape at zero commits' remove and tighter than `D79` — the falsifier is not the world moving, it is adjacent prose. | V15 round 8, 2026-08-15, over `26fca317..cca64eaf`. The eight `<tr>` rows extracted by regex from `demo-output/website/closure.html` and each second-column `<td>` tested for `<s>`; result 7/8, printed row by row. Not inferred from the commit message, which claims *"All eight second-column cells carry the four-entry number struck beside the live one"* and is wrong in the same way. **Cannot see:** whether the other counts in that paragraph hold — only the `every`/`seven` pair was re-derived here. | Change `every` to `seven of the eight` and name the survivor, which the paragraph already names two lines up. **The durable half is that a universal quantifier is a measurement and this lab does not treat it as one:** `every`, `all eight`, `no surface` and `none` are the highest-yield defect sites in this corpus (`D117` was a sweep frame overstating what it searched; `D108`'s follow-up was two unbacked absolutes in a comment about `D108`; `D160` is the third this week), and none of them is graded by any instrument. A check that extracts universal quantifiers whose scope is a countable set in the same document, and re-counts that set, would have caught all three. | owner of the public pages (the sentence); fleet (the universal-quantifier check) |
| D161 | **A charter clause adopted by the chief at 19:10:39Z was violated by the very next durable write at 19:53:29Z, because the clause shipped with no instrument.** `docs/charters/REPORTING_CHARTER.md`, adopted at `638b9af2`: *"A figure is printed at the precision of the derivation that produced it, and the uncertainty of its inputs is stated as an interval where the quantity is defined — never by deleting digits… Nothing already written is rewritten under this clause and it is not authority for a sweep. **It governs what is written next**… Where a live claim quotes a figure whose inputs carry a printing interval, **the interval goes beside it, once, at the point where the quantity is defined**."* The clause is well made — it fixes a FORM, names no figure and no sentence, prohibits designating a reference wording, and cites why two prior digit-count rulings (`D127` and its successor guess) were both refuted in opposite directions. **What it lacks is any way to be checked.** `scripts/check_normative_clauses.py` grades whether a MANDATE, obeyed, would write a true sentence about four registered quantities; **nothing in this lab grades whether a NEWLY WRITTEN FIGURE obeys a mandate.** Forty-three minutes and one commit later, `cca64eaf` wrote `3rd of 7` on `AR_1_Ret_360` — a live claim whose input carries the printing interval `[0.045450, 0.045550]` that `D133` exists to establish — with no interval beside it, and struck the previous disclosure (`D159`). **The adoption-to-first-violation interval is under an hour, the rule was obeyed by nobody, and enforced by nothing.** | V15 round 8, 2026-08-15, over `26fca317..cca64eaf`. The clause read from `638b9af2`'s diff; the violation re-derived independently from the board data rather than taken from any record; the absence of an enforcing instrument established by running all four new checks in the range and reading their printed coverage statements, each of which names the quantity set it can reach. **Cannot see:** whether any surface written between 19:10Z and 19:53Z other than `cca64eaf` also violates it — only the next durable write was checked, and 78 commits landed on 2026-08-15. | **The asymmetry is the finding and it generalises past this clause:** this lab grades DIRECTIVES (`D119`, `check_normative_clauses`) and it grades ASSERTIONS (`check_derived_figures`), and it has nothing that grades **an assertion against a directive that governs its form**. A rule adopted as FORM with no form-checker is a rule that exists only for as long as its author is in the room. Minimum: enumerate the surfaces written after `638b9af2` and re-check them against the clause, since the clause explicitly does not authorise a retrospective sweep and therefore leaves everything before it alone by design — the population needing a check is exactly the one that keeps growing. Then decide whether the clause gets an instrument or gets an owner who reads every new figure; the third option, which is what happened, is that it gets neither. | chief (the clause is a chief adoption); fleet (whether a form-directive can be instrumented at all) |
| D162 | **A register's stated reason for existing is an absolute that this lab has disproved twice and acted against once, it is still shipped at HEAD, and the repair it denies was applied to the correcting agent's own file rather than to the one the red test names.** `sdk/chief_engineer/exec_bits.py:300-301`, landed `e933e31b`, alive at HEAD: *"Measured 2026-08-15 in a scratch repository across four variants; **only an index commit with no pathspec preserves 100755. So this register is the only repair a protocol-conforming agent can perform**, and `D134` records that."* **Falsified three independent ways.** **(a) By execution here:** a fresh `git init` with `core.filemode false`, one shebang-bearing file at HEAD mode `100644`, plain `git add` leaving the index at `100644`, then `git -c core.fileMode=true commit -q -m v147 -- s.py` — a **PATHSPEC** commit, the only form this lab's protocol permits — records **`100755`**; and the control reproduces D147's asymmetry, the same flag on the `git add` alone yielding `100644` despite the index visibly reading `100755`. **(b) By `D147`** (`f732ebaf`, 19:44:12Z), which records the identical measurement and says in its own title that it *"reopens `D134`'s choice"*. **(c) By commit `7370b3d3`** (20:01:20Z), which actually performed one: `scripts/check_summary_consistency.py`, same blob `1c11ae63`, `100644` at `840c0c27` → `100755` at HEAD. The comment was never corrected, and `exec_bits.py`'s module docstring at line 36 still says the register *"makes the gap countable and stops it growing"*, which `D134` had already recorded as false in the other direction. **The consequence is a red suite.** `sdk/tests/test_exec_bits.py::ThisRepositoryTests::test_no_shebang_script_is_both_unexecutable_and_unregistered` **FAILS at HEAD** on `scripts/check_normative_clauses.py` (executed with `__pycache__` purged: `1 failed, 14 passed in 1.62s`). Between `f732ebaf` establishing the working repair and HEAD, that repair reached `check_summary_consistency.py` — the file belonging to the agent that wrote it — and **not** `check_normative_clauses.py`, the file the red test actually names, which belongs to another family and is still `100644`. **`D145`'s ownership boundary: the correction reached the copy inside the correcting agent's own zone and stopped at the edge of it.** | V15 round 8, 2026-08-15, over `26fca317..cca64eaf`. The four-variant filemode experiment re-run from scratch by this grader in a private scratch repository, both arms, rather than read from `D134` or `D147`; the test run in isolation with every `__pycache__` purged first; the mode history read with `git ls-tree -r <rev>` and never with `git ls-files -s`, which reads the index and passed on a staged-never-committed mode once already (`0462b45b`). **Cannot see:** whether `git -c core.fileMode=true` is protocol-conforming, which is a chief question and is OPEN — see the settlement column. | **This row has a genuine fork and both branches leave something wrong, which is why it is filed rather than fixed.** If the chief rules `git -c core.fileMode=true commit -F <msg> -- <paths>` **conforming**, then the `exec_bits.py:300-301` absolute is false and must be replaced with what is true, `D134`'s three options collapse to a fourth that changes nothing durable, and `check_normative_clauses.py` gets its bit and the suite goes green. If the chief rules it **non-conforming** under `ESCALATION_CHARTER` §9.6, then the absolute is defensible as written — and `7370b3d3` becomes a protocol violation that landed a mode change by a means the protocol forbids, which is a different finding and a worse one. **Either way something at HEAD is wrong and the red test is the visible end of it.** Independent of the ruling: the module docstring's *"stops it growing"* describes an object the register is not, and `test_exec_bits.py`'s failure message should stop leading with `git update-index --chmod=+x`, which `D147` measured as **inert in both directions** under a pathspec commit — following the message produces a green index and a red test. | chief (the §9.6 ruling, which is the blocking question); the infra family (`exec_bits.py`'s comment, docstring and failure message); owner of `scripts/check_normative_clauses.py` (the bit itself, once the ruling lands) |
| D164 | **This lab has no instrument whose empty-set behaviour is both correct AND verified, and it acquired both halves of that on 2026-08-15.** Two sibling checks split the failure. **(a) The tested guard is wrong** — `scripts/check_normative_clauses.py` factored its verdict into `decide()` expressly, in its own docstring, *"so a test can drive it to that state without a corpus"*; the test exists, `test_normative_clause_check.py`'s two equivalent mutants are both KILLED, and the guard they protect tests the wrong population (`D158`). **(b) The correct guard has no test at all** — `scripts/check_derived_figures.py`'s verdict block gates on `not matches and not checked`, which ARE its graded sets, and **`cdf.main` is never called from any test file in this repository**. Three separate mutations of that block, each run against `sdk/tests/test_derived_figure_check.py` AND `sdk/tests/test_normative_clause_check.py` together, with every `__pycache__` purged immediately before each `pytest` invocation: `elif not matches and not checked: verdict = UNKNOWN` → `elif False:` **SURVIVED, exit 0**; `if problems: verdict = UNKNOWN` → `if False:` **SURVIVED, exit 0**; `elif bad_controls: verdict = UNKNOWN` → `elif False:` **SURVIVED, exit 0**. The test class covering this is literally named **`ItCannotPassFromAnEmptySet`**, docstring *"Defect class B1, the silent-zero sweep (D62)"*, and its four tests exercise `_scan`, `read_sources`, `build_registry` and `dig` — never the function that produces a verdict. `TheExitContract::test_the_module_publishes_the_three_valued_contract` pins the `EXIT` dict and nothing that reaches it. **Two further B1 arms in the same range are untested and both return PASS over nothing when deleted.** `scripts/check_rung_attribution.py:439`'s `if counts[OK] == 0:` → `if False:` leaves all of `test_rung_attribution.py` green; probed on a throwaway repo of an anchor plus two trailer-less commits, **clean gives `exit 3 / VERDICT: UNKNOWN` and the mutant gives `exit 0 / VERDICT: PASS`, both printing `ADOPTION: 0 of 3 commits carry an identity`** — and the uncovered arm is `integrity()`, **the mode `scripts/lab_check.py` actually schedules**, while the covered sibling in `attribution()` is killed. `self_audit.check_bundle_drift`'s `if not pairs:` → `if False:` leaves 10 passed, exit 0; probed with an archive that has members and no tracked sources, the mutant returns **PASS: "all 0 file(s) the builder copies verbatim out of tracked sources match the shipped zip byte for byte (2 of 2 shipped members ungraded)"** — the arm is named in the function's own docstring (*"an empty copy list"*) and `AnAbsentOrUnreadableArchiveIsUNKNOWN` covers the empty-MEMBER case and not the empty-PAIR case. **Four empty-set-agreement instances in four modules, three weeks after the class was named and one day after 21 of 34 `self_audit` checks were repaired for it. The class is not closing.** | V15 round 8 mutation cell, 2026-08-15, over the 15 test files added or modified in `26fca317..cca64eaf`. ~45 mutations across 12 production files, each through a harness that asserts the site occurs exactly once, **purges `__pycache__` with `/usr/bin/find … -prune -exec rm -rf {} +` immediately before every `pytest`** (stale bytecode has INVERTED mutation results in this tree and `PYTHONDONTWRITEBYTECODE=1` does not fix it, `D1`), then hash-checks and restores byte-for-byte. Baseline and final full runs identical: **346 passed, 51 subtests**. Survivors were probed DIRECTLY as well as through the suite, so each row above has a printed clean-versus-mutant pair and not only a green suite. **Cannot see:** whether any of these arms is unreachable in production — `_since_anchor`'s `if not shas:` and `integrity()`'s `if not commits:` also survive and were **rejected** as findings for exactly that reason, so the four above are the ones judged reachable. **RISK CARRIED:** `scripts/self_audit.py` was dirty with ~900 uncommitted lines from another agent throughout, and if that agent wrote inside one of the ~5-20 s mutate-run-restore windows, that write was overwritten. | **Do not fix these one at a time.** The pattern across all four is identical and so is the repair: the B1 arm must be reachable from a test WITHOUT a corpus, which means the verdict decision has to be a function that takes counts and returns a verdict — `check_normative_clauses.decide()` is the right SHAPE with the wrong ARGUMENT, and `check_derived_figures` needs the shape at all. Then one test per instrument that calls that function with zeros. **The general rule this lab keeps rediscovering and has not yet written down as a rule:** a guard is not evidence until a test has watched it fail, and a test class named for an arm is not a test of that arm. `D123` measured 8 of 14 guard sites surviving deletion with the suite green in one file; this row measures 4 more in four other files; nothing enumerates guard sites and checks each has a killing test. That enumeration is the durable fix and it is cheap — the mutation harness used here is ~40 lines. | fleet (the four repairs are one repair); owner of `check_derived_figures.py` (the missing `main()` coverage, which is the largest single hole); chief (whether "every guard site has a killing test" becomes a standing requirement, since it is the third week running that it would have caught the week's findings) |
| D165 | **A test can be incapable of failing for any reason connected to the module it names, while carrying that module's name — and one of them is labelled "MUTATION PROOF".** This is `D123`'s `test_an_empty_directory_is_UNKNOWN` finding taken one step further: not an arm left unasserted, but **an assertion aimed at nothing**. **(1)** `sdk/tests/test_bundle_drift_gate.py:230`, `test_UNKNOWN_exits_non_zero_so_an_off_detector_reddens_the_runner`. Its entire body is `self.assertEqual(sa.UNKNOWN, "UNKNOWN")`, `self.assertIn(off.status, (sa.UNKNOWN,))` and `self.assertNotIn(sa.UNKNOWN, (sa.PASS, sa.WARN, sa.INFO))` — three statements about string constants defined in the module under test, where `off` is a `Result` the test itself just built. **`main()` is never called.** Mutating `scripts/self_audit.py`'s `return 3 if any(r.status == UNKNOWN …) else 0` → `return 0` leaves `test_bundle_drift_gate.py` at **10 passed, exit 0**. The property IS guarded — by `sdk/tests/test_empty_set_is_not_agreement.py::TestUnknownRedddensTheRunner::test_an_UNKNOWN_with_no_FAIL_exits_3`, which kills it — so this is dead weight rather than an open hole, and that is the danger: **a reader auditing coverage by test name concludes the exit contract is guarded here, and moves on.** **(2)** `sdk/tests/test_certificate.py:279`, `test_the_reproducibility_guard_catches_the_footer_that_defeated_it`, labelled **MUTATION PROOF** in its own name and comment, asserts only `in`/`not in` over two string literals defined inside the test body. It cannot fail for any reason connected to `sdk/chief_engineer/certificate.py`. **(3) And the guard it claims to prove has a real hole.** `certificate.py:815`: reinstating a `"Reproducible from the sealed evidence bundle; …"` footer in `build_certificate` (v1) leaves `test_certificate.py` + `test_bundle_certificate_pairing.py` at **62 passed, exit 0**. `test_banned_language_never_renders`'s widened `assertNotIn("reproduc", text.lower())` runs only through `GeometryStudyCertificateTests._render`, which calls `build_certificate_v2`; planting the same sentence in the **v2** footer IS caught (1 failed), so the guard bites — it never reaches v1, which `PdfTests:69` and `RedesignV2Tests:124` render with no language assertion at all. **Severity stated rather than inflated:** v1 is called only by `sdk/scripts/build_certificate_compare.py` and every shipping workflow uses v2, so this is a latent hole and not a live wrong claim. **(4) A fourth, different family, same shape.** `sdk/tests/test_refused_inbox_lifecycle.py:146`, `test_a_root_that_does_not_exist_empties_the_ledger`, whose docstring says *"The early return had no ledger write at all, so a read of a missing directory left the previous directory's refusals standing."* Deleting `_publish_refused_inbox(root, refused)` from the absent-root early return at `sdk/chief_engineer/agenda.py:1126` leaves **11 passed, exit 0**, because the body asserts only `refused_inbox() == set()` — which the ROOT FILTER at `agenda.py:1110` already satisfies without any emptying. Probed directly: clean gives `_REFUSED_INBOX = []` with `root = <new dir>/proposals`; the mutant gives `_REFUSED_INBOX = ['present.json']` with `root = <OLD dir>/proposals`, so the ledger still names a file from a directory the read never opened. The two sibling mutations were both killed, so only this arm is uncovered. | V15 round 8 mutation cell, 2026-08-15, over `26fca317..cca64eaf`; same harness and same `__pycache__` discipline as `D164`; baseline and final runs both **346 passed, 51 subtests**. Each item above has a printed clean-versus-mutant pair. **Cannot see:** how many tests elsewhere in the 78-file suite have bodies of this kind — this cell read 15 files, the ones in the declared range, and did not audit the rest. | **The detection is mechanical and nobody has run it:** a test whose body contains no call into the package under test, or whose every assertion argument is a literal or a name bound inside the test, is a candidate. That is a static check over `sdk/tests/`, and it would have flagged (1) and (2) without running anything. **The second half is a naming rule:** a test may not be named for a property, or labelled "MUTATION PROOF", unless a mutation of the code implementing that property has been observed to turn it red — and the observation belongs in the test's docstring, which is where `test_empty_set_is_not_agreement.py` already puts it and why that file scored 5 of 5. Then fix (3) by routing v1 through the same language assertion, and (4) by asserting the ledger contents rather than the filtered accessor. | fleet (the static candidate check and the naming rule); owner of `sdk/chief_engineer/certificate.py` (item 3); owner of `sdk/chief_engineer/agenda.py` (item 4) |
| D166 | **A control that guards the generator and not the artifact it generates, on a claim that has already been wrong once.** `sdk/tests/test_untrained_qcr_attribution.py` carries `test_the_control_is_live`, which guards `_closure_literal()` against deletion of its subject — the file's own stated failure mode being *"a rule survives the deletion of its subject and goes on reporting clean"*. Its sibling `test_the_shipped_json_surfaces_carry_the_attribution` has **no equivalent control**. Measured, five mutations: `"untrained QCR2000 forward solve"` → `"untrained closure forward solve"` in `sdk/scripts/build_benchmarks.py` **KILLED**; the same string in `demo-output/website/benchmarks.json` **SURVIVED**; the same string in `demo-output/website/wall/wall.json` **SURVIVED**; `"is Spalart (2000)'s "` → `"is the "` in the generator **KILLED**; the same in `benchmarks.json` **KILLED**. So the attribution phrase is guarded on the generator and on one of the two shipped surfaces for one of the two strings, and unguarded for the other three cells. **It is not vacuous today** — each JSON currently carries exactly one QCR string, so the assertion passes on real content — but the test cannot distinguish that from a surface that has lost the phrase, which is precisely the state `e639796e` was written to prevent when the untrained-QCR claim was made to name Spalart (2000) and its two JSON surfaces were regenerated. **The generator and the artifact are two objects and this lab has already been bitten by treating them as one:** `D118` was a drift detector that never opened the artifact that ships, and `a0f89c4b` (in this same range) records *"the generator had already stopped saying it; the guard and three descriptions had not"*. This is the mirror image — the guard watches the generator while the artifact goes ungraded. | V15 round 8 mutation cell, 2026-08-15, over `26fca317..cca64eaf`; same harness, `__pycache__` purged before every `pytest`, every mutated file hash-checked and restored byte-for-byte, `git diff` empty on all of them afterwards. Baseline and final runs both **346 passed, 51 subtests**. **Cannot see:** whether the two shipped JSON surfaces are the only ones carrying the phrase — `benchmarks.json` and `wall/wall.json` were the two the test names, and no independent sweep for other carriers was run in this cell. | Give the shipped-surface test the control its sibling has: delete the phrase from each JSON in turn and require the test to go red, for **both** strings and **both** files — four cells, four controls, and the file's own docstring already argues for exactly this. The wider question, which is a chief question because it is a convention and not a bug: **when a claim exists in a generator and in N generated artifacts, which one is the subject of the test?** The answer that survives is *the artifact, because the artifact is what travels* (`e933e31b` adopted that reasoning for authorship on the same day), and the generator gets a separate test that it still emits it. Right now this file does the reverse. | owner of `sdk/tests/test_untrained_qcr_attribution.py`; chief (the generator-versus-artifact convention, which recurs in D118, a0f89c4b and here) |
| D167 | **"Left to its owner" is a routing rule, and V5 spent four days treating it as a frame exclusion — on two surfaces, not one.** `LADDER_V_PASS1_2026-08-11.md:466-469` refused `campaign/QCR_ACTIVITY_CHECK_2026-08-08.md` because it is *"another verification agent's signed report, left to its owner"*, and did so *"exactly as the 08-08 pass left the V2/V7/V10 record to its owner"* — so `campaign/LADDER_V_RUNGS_V2_V7_V10_2026-08-08.md` rests on the identical sentence (`LADDER_V_RUNGS_V1_V3_V4_V5_2026-08-08.md:258-260`). The chief ruling `3af7bd2b` and its amendment `25777f57` found the ground stale for the first file only. **It fails for both, and the reason is that ownership answers *who edits*, never *whether the surface is in frame*.** A refusal that names an owner has not excluded anything; it has assigned something. | Executed 2026-08-15T20:11Z. `git log --format='%H %cI'` per path: `QCR_ACTIVITY_CHECK_2026-08-08.md` has **two** commits — `1a14e90b` @ 2026-08-08T02:18:29Z signed it, `fe612d03` @ 2026-08-08T22:25:35Z amended it **20 h 07 m later and three days before the refusal was written**. Read at the diff, not at the commit message: `fe612d03` appends a section headed *"## 2026-08-08 addendum — the hills SST control leg's citation gets its log line"*, wholly below the signed text, closing *"nothing else in this record changes"* — precisely the dated additive note L-44 permits and precisely the shape a Spalart attribution would take. `LADDER_V_RUNGS_V2_V7_V10_2026-08-08.md` has **one** commit, `49f71b8c` @ 2026-08-08T02:08:13Z, never amended — so it has no counterexample of its own, which is exactly why the ground had to be tested rather than inherited. Both files: sha256 of worktree identical to `git show HEAD:`, **zero** occurrences of "Spalart". | **Both exclusions withdrawn on the rung's face** (`LADDER_V_TRIPLE_VERIFICATION.md`, "V5 FRAME ENUMERATION"), which takes V5 from four exclusions to two exclusions plus **two open unmet in-frame sites** — the repair made the rung's state worse and that is the honest direction. The durable half: **a refusal must name a property of the surface, not a person.** "Frozen", "no addendum region", "held-out test corpus" are properties and survive re-execution; "his", "hers", "its owner's" are assignments and cannot exclude anything from a frame. Every existing refusal in this lab phrased as ownership should be re-read against that test. | chief (V5's frame is his ruling); owners of the two reports (`fe612d03`'s author for the QCR check, `49f71b8c`'s for the V2/V7/V10 record) — each repairable by a dated addendum below the signed text |
| D168 | **The impossibility ground that actually survives for a frozen artifact is a property of its CONTAINER, not of freezing — and both the ruling and its withdrawal missed that, in opposite directions.** `3af7bd2b` argued frozen artifacts *"may never take a revision"* as a general premise; `25777f57` withdrew it and conceded *"a sound impossibility argument exists for `MANIFEST.json` alone"*. Neither is right. L-44 permits a **dated addendum**, and an addendum needs somewhere below the freeze line to sit. **A Markdown record has a below; a JSON object does not.** Any citation added to `closure_challenge_submission_round5/MANIFEST.json` or `closure_challenge_round5_qcr_forward.json` is a byte inserted *inside* the frozen structure — a revision, which L-44 forbids — so the ground covers **both** JSONs, and it covers **neither** of the two Markdown reports the same refusal sentence was applied to (D167). | Executed 2026-08-15. Both JSONs: single commit each (`07a7fe9e` @ 2026-08-07T20:51:51Z, `e865076b` @ 2026-08-07T20:38:52Z), never amended, worktree sha256 `58f4f5a5…7092` and `7e6c0d85…5d1a` byte-identical to `git show HEAD:`. Neither file's sha256 is asserted in any tracked tamper record — searched — so A2's chain rests on **commit history**, which is why an addendum to `MANIFEST.json` carries a second and independent cost: it would end the single-commit never-amended property A2 is asserted on. Also measured, so the refusal is not costless: the citation is **not** missing from the round-5 package — the sibling `closure_challenge_round5_qcr.json` carries the full journal reference in `model.provenance`. | Enumerated on V5's face with both hashes and this ground. **These two exclusions are permanent, and they are the correct form of the argument `3af7bd2b` made badly:** V5 can never reach a plain PASS under the literal *"wherever QCR is named"* reading, because two of its surfaces are containers with no region an addendum can occupy. The durable half: **before refusing a frozen artifact, ask what shape an addendum to it would take.** If the answer is "there is nowhere to put one", the refusal is structural and permanent; if the answer is "below the freeze line, dated", the refusal is a scheduling decision and expires. | chief (V5's frame); the round-5 package's owner for the standing question of whether a package whose manifest cannot cite is a package that should ship uncited |
| D169 | **`docs/PRODUCT_LIST.md` was not missed by V5's sweeps — it was excluded once, in writing, on 2026-08-08, with no ground and no hash, and the exclusion then became an invisible frame boundary for seven days.** The 2026-08-15 grade reports it as *"outside every V5 sweep frame ever executed"*, which is true and understates it. `LADDER_V_RUNGS_V1_V3_V4_V5_2026-08-08.md:263` says *"The `.tex` and `PRODUCT_LIST` were not touched per the rung's scope"*, and `:282` lists the reason in the same breath as the others: *"the `.tex` (its Opus writer's), `PRODUCT_LIST` (chief's)"*. **That is the D167 defect — ownership used as an exclusion — committed seven days earlier on the very file that later failed the rung**, and no subsequent pass re-derived the boundary because no subsequent pass could see it: leg 3's declared frame was 920 files under `demo-output/website/`, and `docs/` was never in it. | Executed 2026-08-15. Four arms. **Untracked** (`git ls-files --others --exclude-standard`): 2 files, **0** hits. **Gitignored** (`git ls-files --others --ignored --exclude-standard -z | xargs -0 /usr/bin/grep -aIlF -f <patterns>`): **37,254** files in 64 s, **3** hits, all under `dist/` — `site/benchmarks.html` (3 Spalart), `site/closure.html` (3), `snapshot/lab_stats.json` (**0**, the fourth face of the generator string, gitignored with a designated owner). **Run tree** (`/usr/bin/find` — `find` here is `bfs` — over `/home/ubuntu/certonomous-runs/`, **132,050** files, fixed-string prefilter): completed at 2026-08-15T20:24Z, exit 0, **1** hit — the seeded positive control `/home/ubuntu/certonomous-runs/__probe_v5_repair.txt`, since removed (`ls` confirms absence). The control fired, so the **0** real hits is a measurement and not a blind spot; nothing here was recorded UNMEASURED. **Tracked** (`git grep -a`, not `-I`, not the shell `grep`): 20,698 files; the claim pattern `untrained[- ]QCR` matched **46** files at 20:14Z and **47** at 20:22Z (a concurrent agent committed `campaign/LADDER_V_V15_ROUND8.md` between the two reads — the corpus moves under measurement, and the grade's "47" reproduces at the later clock), of which **15** carried zero "Spalart" at both reads. Every one opened and classified. `docs/PRODUCT_LIST.md` carried the untrained claim about **our own** model at `:63`, `:136`, `:608`, `:655`; `demo-output/website/CLOSURE_EVALUATION_PROTOCOL.md:391` carried it too and had been classified a passing mention by an earlier pass. Both repaired this pass to name Spalart (2000) with the year in the same sentence; measured with the guard's own regexes and sentence splitter, `PRODUCT_LIST.md` **17 → 13** unattributed QCR sentences and the protocol **1 → 0**; `sdk/tests/test_untrained_qcr_attribution.py` 4 passed, `__pycache__` purged first. | **The frame is the deliverable, not the repairs.** Of the 15 zero-Spalart files, five are in frame — `PRODUCT_LIST.md` and `CLOSURE_EVALUATION_PROTOCOL.md` (repaired), and the third generator `sdk/scripts/closure_eval_battery/build_master_table.py:246,:371` with its two tracked outputs `closure_eval/closure_eval_master_table.md:20` and `.json:84`, **still unrepaired** because they need the eval battery re-run (D128) — and ten are out: two frozen JSONs and two withdrawn-ownership reports (D167/D168), two files describing **Wu & Zhang's** SST-QCRC rather than ours (`CLOSURE_METHODS_COMPARISON.md:216`, `closure_challenge_C2_error_decomposition.md:255`), two held-out guard corpora where the uncited sentence **is** the test datum (`campaign/V16_GRADE_HELDOUT_SETS.py:70`, `sdk/tests/test_rank_claim_surfaces.py:584`), and two dated round reports. The durable half: **a sweep frame inherits every exclusion its predecessors made silently.** A frame that names its own boundary in one report and is copied without it in the next is indistinguishable, three passes later, from a frame that was never drawn. | chief (the 08-08 scope exclusion was made on his file and in his name); D128's owner for the third generator and its two outputs |
| D167-D169 note | *[W-4 duplicate-ID check, executed 2026-08-15 while filing D167-D169. A numeric detector over row-start IDs reports collisions at **D1, D8, D19 and D119**, at HEAD and at `HEAD~1`. Opened and read: there are **none**. The four are suffixed sub-rows and an annotation — `D1a` (a chief correction of D1), `D8b` (already annotated *"ID CORRECTED 2026-08-11 by the chief; this row was filed as a second D8"*), the `D19-D20 note` renumbering record, and `D119a` (its author's own disclosure). Every one strips to the same integer under `[^0-9]` deletion, which is what the detector does and why it is wrong. **Recorded so the next pass does not re-file the same false positive, and so nobody renumbers a correctly-suffixed row to satisfy an instrument.** No ID was reused or renumbered by this pass; D167, D168 and D169 were each asserted free by execution immediately before writing.]* | | | |
| D170 | **Three remedies printed as done-and-costed in one document, executed for the first time here, and none of them does what it says: one produces no PDF at all, one exits 1 and silently drops 15 of 90 shipping members, and one returns 33 files where it claims three.** `demo-output/website/campaign/OWNERSHIP_BOUNDARY_SWEEP_2026-08-15.md` (`a3342d9a`). **W1, `:295` and `:175`** — *"`cd demo-output/website/latex && pdflatex closure_challenge_report.tex && pdflatex closure_challenge_report.tex` … **No source edit is needed.** `pdflatex` is installed; `latexmk` is not"*, cost *"two commands, no compute"*, and *"The whole zone reduces to a two-command rebuild."* Executed verbatim on a scratch copy of the real directory: **``! LaTeX Error: File `ulem.sty' not found. / ! Emergency stop. / ! ==> Fatal error occurred, no output PDF file produced!``, rc=1 on both passes.** `kpsewhich ulem.sty` exits 1 and `/usr/bin/find /usr/share/texlive -name ulem.sty` is empty; the `.tex` requires it at line 19 (`\usepackage[normalem]{ulem}`) and uses `\sout` **30 times**, so **the missing package is precisely the strike macro the repaired `.tex` depends on**. Control: the sibling `dafoam_defect_report.tex` builds `rc=0` from the same directory, so the toolchain works and the failure is specific to this document. **The sweep verified that `pdflatex` is INSTALLED and never that the document BUILDS**, and that distinction falsifies its own thesis sentence at `:310-311`: *"Twenty-six remain, and every single one is blocked on a name -- not on a sweep, not on compute, not on a measurement."* Fifteen of the twenty-six are additionally blocked on a missing TeX package. **W2, `:296`** — *"re-run `python3 scripts/build_laptop_bundle.py` and commit the rebuilt `.zip`. It is a `shutil.copy2` of the already-repaired sources -- **no content edit is needed**"*, cost *"one command"*. Executed twice into scratch: **`rc=1` both times**, with 16 `SKIPPED … no certificate.ready event to pair` lines and `WARNING: no complete recording could be PAIRED with the certificate on disk for: aircraft-optimization`. Member diff against the committed zip: **90 -> 75, fifteen members lost and none added** -- the entire `aircraft-optimization` act (`certificate.pdf`, nine `.stl`, three `.png`, two mission-state files), 1.48 MB -> 1.02 MB. The gate that causes it landed at `67e472c6`, and `git merge-base --is-ancestor 67e472c6 a3342d9a` confirms it **predates the sweep's own commit**, so W2 was already broken when it was printed. *Fair to the document:* the rebuilt `site/closure.html` does clear the live wrong copies, the survivors being inside `<s>` tombstones; the defect is the cost claim and the exit code, not the repair intent. **The demonstration, `:350-352`** — *"Run at `1360a5a9`, `sweep.py -F \"rank 1 of 5\" --frame everything --files-only` returns [three named files] **without any change whatsoever**."* Executed verbatim: **`MATCHES: 33 files / 90 lines`**. At the document's own stated frame, `git grep -a -l "rank 1 of 5" 1360a5a9` gives **26 tracked files** plus two gitignored `dist/` files; the three named are about a **10% subset**, and this sentence is the sole evidence for the document's proposal (b) (*"nearly free, because the machinery already exists"*). | V15 round 8 document-verification cell, 2026-08-15, over `26fca317..cca64eaf`. All three run in a scratch copy, never against the live tree; nothing in the repository was written. `pdflatex` and `kpsewhich` re-confirmed independently by the round's grader after the cell reported. **Cannot see:** whether `ulem.sty` is installable on this box (no network use was made), and whether the 15 dropped members are recoverable by supplying the missing pairing event rather than by reverting the gate. | **The durable half is not "fix these three", it is a rule this lab already applies to claims and does not apply to instructions: a remedy is a claim about the future and must be EXECUTED before it is costed.** `0ec04c6e` established this on 2026-08-15 for the handbook -- four of its remedies did not run -- and the same pass did not extend to any other document that prints commands. This row is the second instance in one day, so the class is not specific to the handbook. Minimum: every document that prints a shell recipe as a residual or a cost gets its recipes run once, and the exit code recorded beside the recipe. **W1 specifically needs a decision and not a rerun:** either `ulem.sty` is installed on this box, or the `.tex` stops using `\sout` (30 sites), or the `latex/` zone's 15 residuals are recorded as blocked on a package and not on an owner -- which changes the sweep's headline claim. **W2 needs the pairing gate looked at before the zip is rebuilt**, because rebuilding it as printed ships a bundle missing an entire act. | fleet (the execute-before-costing rule); `latex/*` owner (W1's package decision); the infra family (`build_laptop_bundle.py`'s pairing gate and its exit code) |
| D171 | **A sweep whose forensics are exact and whose citations are not: six count-and-citation defects in one document, including a quotation of a sentence that did not exist at the frame the document declares, dated "for three days" when it was three minutes old.** `demo-output/website/campaign/OWNERSHIP_BOUNDARY_SWEEP_2026-08-15.md` (`a3342d9a`), which declares at `:3` *"Swept 2026-08-15 at HEAD `1360a5a9`."* **(1) THE WORST ONE.** `:63` quotes `self_audit.py:5948` as printing *"nothing in this file opens a PDF, so a claim that exists only in a compiled report is invisible while its `.tex` source is not"*, and `:368-373` (§5.1(d)) says *"**That sentence described `latex/closure_challenge_report.pdf` exactly, for three days, and no one acted**"*. Executed: `git show 1360a5a9:scripts/self_audit.py \| grep -c "nothing in this file opens a PDF"` -> **0**; `grep -c "_blind_corpus"` -> **0** in a 6,006-line file; `sed -n '5946,5950p'` -> unrelated print-formatting code. Bisected across all 20 in-range commits: the sentence and `_blind_corpus` **first appear at `9af7e2d3`, 19:31:04Z**, and the sweep committed at 19:34:20Z. **The quoted sentence was three minutes and sixteen seconds old, and absent from the declared frame; "for three days" is wrong by a factor of about 1,440.** At HEAD it lives at `:6847` and has never been at `:5948`. §5.1(d) is one of the document's four proposed durable fixes and this quotation is its entire evidential support. **(2)** `:62`, the `dist/` row: *"Called \"the designated owner\" six times across six rows and never once given a name."* Executed on `docs/DOCKET.md`: **4 occurrences in 4 rows at the declared frame `1360a5a9`** (D71, D115, D118, D136); **6 across 5 rows** at its own commit and since -- and it reaches six only because **the D145 row this same document filed says "designated owner" twice**. It is never six rows. Of the four pre-existing rows, D71 and D115 are `latex/` rows, which the document's own `latex/` register cell cites as such, so only three belong to `dist/`, against a "Where it is declared" cell listing seven. **(3)** `:106-107`: *"**THE FINDING. A check crosses this boundary, reads exactly these bytes, and returns a PASS.**"* and `:62` *"(currently a **false PASS**, §2.1)"* -- against the document's OWN quote two lines below at `:110`: *"> `WARN -- every travelling surface complies; 26 lab record(s) claim rank 1 without what V8 requires`"*. Executed at HEAD: `check_rank_claim_surfaces()` returns **`status='WARN'`** (27 records now). The document's quote was faithful to the instrument; **the mismatch is internal, headline against evidence, with neither edited** -- `D141`'s shape at fifteen minutes' remove. **(4)** `:228` heading *"The other **seventeen** `LADDER_V_*` documents"* against `:230` body *"All **eighteen** … were swept"*: 19 such files at `1360a5a9` (18 others), 20 at its own commit and at HEAD (19 others). **The body is right at the stated frame and wrong at its own commit; the heading is wrong at both; neither was edited.** **(5)** `:148`: *"All **fifteen** table entries below are LIVE AND WRONG"* over a table with **13 data rows** covering **16** distinct PDF line locations. Fifteen is reachable only as 16 minus row 260 -- and **row 260's own cell reads "the \"68% level\" wording IS CORRECT"**, contradicting the headline above it. The fifteen propagates into W1 and into the **D145** row. **(6)** `:307-309`, copied verbatim into **D145**: *"27 live wrong passages, in **7** files."* The document's own enumeration gives **5 or 6** depending on whether the shipped zip counts as one file or two. The passage arithmetic 15+4+4+3+1 = 27 is internally consistent; the file count is not. **AND THE POINT OF THE ROW IS THE CONTRAST.** This same document's measurement work is **exact**, independently re-derived: 14 `68%` strings in the PDF at lines 19/34/228/254/265/305/319/700/739/757/808/836 plus 260/2371 as the 1-sigma convention; `rank 1 of 5` exactly twice; `Yang` and `Tian` zero times; tracked **20,688** at `1360a5a9`; `git grep -I` skipping **1,476** of which **50** are PDFs; 50 tracked / 18 gitignored PDFs, 454 `.gz`, 1 `.zip`; **90 zip members with 0 md5 mismatches** against the unpacked mirror; `_rank_claim_lines` on the shipped `closure.html` byte-for-byte the document's list; the four `LAPTOP_SHOOT.md` lines verbatim; and the tracked-PDF sweep re-run finding exactly one PDF with a withdrawn figure. **A document can be meticulous in what it measures and wrong in almost every sentence that cites what it measured.** | V15 round 8 document-verification cell, 2026-08-15, over `26fca317..cca64eaf`, every figure re-derived rather than read; item (1) re-verified independently by the round's grader with `git show` and `git log -S` before filing. Counts given at the document's own declared frame `1360a5a9` AND at HEAD wherever they are time-sensitive, so no item rests on drift the cell caused. **Cannot see:** `:62`'s `self_audit.py:1060-1079` locator, which matches no revision -- at `1360a5a9` those lines are a comment block and the archive-opening calls are at `:848`/`:932` -- but that file was dirty and grew 1,005 lines in-range, so a worktree state the cell never saw cannot be excluded. Also not closed: `:273`'s *"four published certificate PDFs"* against five tracked PDFs matching "certificate", where "published" may denote a set the document never defines. | **Item (1) first, because it is the only one that changes what a reader should believe:** §5.1(d)'s proposal must either find real evidence or be withdrawn, and the "for three days" clause must go. Items (2)-(6) are arithmetic and cheap. **The durable half is a frame rule this lab states and does not enforce: a document that declares a frame must quote only from that frame, and a quotation carrying a line number is a claim that must resolve AT THE DECLARED COMMIT.** Every element of item (1) is mechanically checkable -- `git show <declared-frame>:<file>` and a substring test -- and no instrument here does it, which is why a three-minute-old sentence could be described as three days old inside a document whose PDF forensics are exact to the line. That check is the row's real ask; the six repairs are the occasion for it. | owner of the sweep (the six repairs); fleet (the declared-frame quotation check, which is the durable half) |
| D172 | **Four more citations contradicted by the artifacts they name, filed together because they are one defect wearing four costumes -- and one of them has already been transcribed into a document that ships.** **(1) An adoption ratio no run has ever produced, quoted by two surfaces that disagree with each other.** `docs/AGENT_ATTRIBUTION.md:214`: *"The adoption ratio is printed in the frame on every run, loudly. While it reads `2 of 40`, the honest summary of this repository is still 'authorship is unknown for 38 of the last 40 commits'."* The module it documents, `scripts/check_rung_attribution.py:156-157`, says *"`1 of 40` … unknown for 39 of the last 40 commits"*. The instrument, executed: `frame: git rev-list e933e31b..HEAD, plus the anchor itself`, `examined: 44`, `ADOPTION: 4 of 44`. **The frame is ANCHORED, not a forty-commit window**, and it grows with every commit: 3 at `44ac957a` (the document's own commit, where the instrument printed `2 of 3`), 34 at `cca64eaf`, 44 when this round ran. **There is no tree at which the denominator is 40.** `grep -n "40" scripts/check_rung_attribution.py` shows the only other `40` is `graded[:40]`, a display truncation -- which is the likely origin and makes it a misread of the instrument's own output. **(2) A correction anchored to a commit that never touched the file.** `docs/HANDSHAKE.md:270`: *"**[CORRECTED 2026-08-15 at `29beb7cf`. …]**"*. Executed: `git show 29beb7cf:docs/HANDSHAKE.md \| grep -c "CORRECTED 2026-08-15"` -> **0**; `git show 0b0041b1:docs/HANDSHAKE.md` -> **1**; `git show --stat 29beb7cf` -> `scripts/check_derived_figures.py`, one file changed. `29beb7cf` is 01:01:21Z; the block landed at `0b0041b1`, 01:40:43Z, **39 minutes later**. This is the exact class the lab filed at `3314d746` (*"the one citation that was contradicted by the artifact it named"*) **recurring inside the same commit range that filed it**. **(3) A document filing its finding under an ID the same commit gave to a different row.** `demo-output/website/campaign/MARGIN_PRECISION_INTERVAL_2026-08-15.md:208`: *"It is recorded here, filed as **D132**, and **not repaired**"*; its own Related section at `:312` says *"**D133** (C10, filed by this document)"*. At HEAD, **D132** is *"D124's option (a) is built and anchored: commit authorship now travels in the artifact…"* -- the attribution mechanism -- and **D133** is the `AR_1_Ret_360` cases-won row. `git log -S'\| D132 \|'` and `-S'\| D133 \|'` both return **`eadcd112`**, the very commit that wrote the sentence, **so the citation was false the moment it landed** and a reader following §3.1 to D132 arrives at an unrelated row. Zero commits' remove. **(4) A row count that is wrong at every tree, carrying a sentence that narrows a FAIL, plus a locator off by 44 lines that has been transcribed into a shipping document.** `demo-output/website/campaign/LADDER_V_V6_V10_GRADE_2026-08-15.md:133-134` says *"Twelve rows (the nine findings plus the two sub-rows and the citations row the block adds)"*, and `:223-224` narrows a FAIL with *"**eleven of twelve verdict rows** … are MEASURED-PASS"*. Programmatic extraction from the `\| § \| the finding as written \|` header with a sha256 per row, five trees: `472f9f92` **11**, `99c66254` (the grade's own anchor) **11**, `377d6afb` **11**, `87324012`/`bd8280de`/`be0a0c5d`/HEAD **11**. **Eleven at every tree; never twelve.** And `:193`'s *"`:519+`, 212 lines below in the same file, §4.8 is now headed…"*: at `99c66254`, line **519 is §4.7's heading**, §4.8's is at 563, the row is at 307, so the distance is **256**, not 212 -- and both errors were **transcribed forward into `demo-output/website/CLOSURE_CHALLENGE_SUBMISSION_DRAFT.md:321`**, where `grep -c "212 lines below"` returns 1 at `87324012` and 0 at `472f9f92`. | V15 round 8 document-verification cell, 2026-08-15, over `26fca317..cca64eaf`; every claim re-derived by execution, item (1) re-run independently by the round's grader after the cell reported. **Cannot see:** whether other shipping surfaces carry the transcribed `212 lines` or `twelve rows` figures -- only `CLOSURE_CHALLENGE_SUBMISSION_DRAFT.md` was checked. Two related items are recorded as SUSPECTED and not filed here because they could not be closed: `LADDER_V_V5_GRADE:89-90`'s `118 / 64 / 29`, which no `git grep` arm reproduces (`-a` 108, `-I` 100, `-a -i` 154), and `HANDSHAKE:282`'s *"SUPPLIED SEVEN TIMES … through grade round 10"* against four tracked V16 grade documents carrying six grades at the most generous count. | **These four are one defect and want one fix.** `scripts/docket_citation_guard.py` already proves the mechanism is cheap: it resolves every `D<n>` citation against the docket and returns PASS over 303 of them. **What it checks is EXISTENCE. What all four items above need is IDENTITY** -- does the cited row, line, commit or count say what the citing sentence says it says? Item (3) would have been caught by nothing weaker, because D132 exists. **The generalisation, and it is the same one D168 asks for from the other direction: a citation that carries a locator (a line number, a commit SHA, a docket ID, a row count) is a claim, and this lab grades no claim of that shape.** A first cut is mechanical for two of the four: a commit SHA cited beside a file must have touched that file (`git show --stat`), and a `D<n>` cited beside a quoted phrase must contain that phrase. Item (4) additionally needs its transcribed copy in the submission draft corrected, because that one travels. | fleet (the citation-identity check); owner of `docs/AGENT_ATTRIBUTION.md` and of `check_rung_attribution.py`'s docstring (item 1, and they must agree); owner of `docs/HANDSHAKE.md` (item 2); owner of `MARGIN_PRECISION_INTERVAL` (item 3); owner of the V6/V10 grade and of the submission draft (item 4) |
| D173 | **The lab's independence instrument has emitted exactly ONE identity across one hundred percent of its deployed life, that identity is this session's, and run on its own adopting commits it returns AUTHOR.** `scripts/check_rung_attribution.py`, adopted at `e933e31b` 2026-08-15T03:02:18Z. Executed at HEAD: `git log --format='%H%n%b' --all \| grep -oE 'Lab-Agent: .*' \| sort \| uniq -c` returns **four real trailers and one distinct value** -- `ip-172-31-43-247/64b13819-ff95-4d4d-a50f-3720bab19084/-` on `e933e31b`, `44ac957a`, `638b9af2` and `09234034` -- plus one occurrence of the docstring's own `<host>/<session-uuid>/<tag>` template. `python3 scripts/check_rung_attribution.py --emit-trailer` **emits that same string for this grader**. And `--closing 44ac957a --graded e933e31b` returns **`VERDICT: AUTHOR / BECAUSE: 1 of 1 graded commits carry the closing commit's own identity … The measurer is an author`**. **So every non-author claim made anywhere in this commit range would grade AUTHOR under the lab's own instrument** -- including `LADDER_V_V6_V10_REGRADE_2026-08-15.md` §0, which names this same `64b13819-…` transcript as its independence evidence, and including the V15 round 8 grade that filed this row. **WHY THIS IS NOT `D124`/`D130` RESTATED.** `D130` records that the evidence does not travel. `docs/AGENT_ATTRIBUTION.md:98` predicts the collision in the abstract: *"TWO AGENTS DISPATCHED BY THE SAME CHIEF SESSION READ AS THE SAME AGENT"*, and the script repeats it at its own line 92. **What neither records is that this has already bound every commit that has ever adopted the mechanism, without exception.** An instrument whose discriminating field has never varied has not been shown to discriminate: its four runs are **one constant observed four times**, not four confirmations. That is a property of the DEPLOYMENT rather than of the design, it is measurable in one command, and it is the number nobody has printed. **What it does NOT undermine:** an independence argument resting on a TIMESTAMP -- an agent transcript that opens after the last commit it grades -- is untouched by this, because it does not use the trailer. What it removes is the other leg: **a green `check_rung_attribution` run must not be cited as independence evidence.** | V15 round 8, 2026-08-15. Found by the document-verification cell while re-deriving `docs/AGENT_ATTRIBUTION.md`'s measurement table (which is otherwise EXACT at its stated frame `e639796e`: 1,849/4, 1,217/1, 36/1, five distinct `Co-Authored-By` values, and every printed command behaving as documented including empty-set -> UNKNOWN exit 3). Re-verified independently by the round's grader with `git log --all`, `--emit-trailer` and the `--closing/--graded` pair before filing. **Cannot see:** whether any agent has ever passed `--tag`; all four trailers end in `-`, the no-tag marker, so the one field that could have discriminated within a session has never been used. **Also unverifiable from the repository:** `docs/AGENT_ATTRIBUTION.md:243-246`'s *"Every test is mutation-proved: 12 mutants, all killed"* -- `grep -ci mutant sdk/tests/test_rung_attribution.py` returns **0** and no mutation harness or record for this module is tracked, which is the same evidence-does-not-travel defect the document is about. | **Print the number.** The instrument's frame block should carry `distinct identities observed: N` beside its adoption ratio, because `N = 1` is the single fact that tells a reader what a green run is worth, and it costs one `git log`. **Then decide what the mechanism is FOR, because right now it is doing the opposite of its name:** its only live effect is to convert same-session pairs into `AUTHOR`, which is correct and useful as a NEGATIVE result -- it catches an agent grading its own work -- and useless as a positive one, since `NON-AUTHOR` has never been returned by any real pair. Naming it after the negative it can actually deliver would stop it being cited as independence evidence. **Third, and this is the chief's:** if per-agent identity is wanted rather than per-session, the `<tag>` field already exists, has never been used, and is free -- but the script itself says a same-session pair is `AUTHOR` whatever the tags say, so making tags load-bearing is a design change and not a convention. **Standing rule in the meantime, which this round followed:** independence claims rest on the dispatch record's timestamps, are stated with their limits, and are never supported by citing this check. | fleet (the distinct-identity count in the frame); chief (whether the mechanism is renamed to what it delivers, and whether `<tag>` becomes load-bearing); owner of `docs/AGENT_ATTRIBUTION.md` (the unverifiable mutation claim) |
| D174 | **The false clean is a CLASS, not an instance: of the 34 checks in `self_audit.py`, 15 grade FORM where the claim's failure mode is VALUE, and the repair of the one that shipped a false PASS leaves 14 standing.** D145 named `check_rank_claim_surfaces` as the worst finding in the ownership sweep -- it opened `dist/certonomous-demo.zip`, read line 502 of `site/closure.html` inside it, recognised the rank claim and reported *"every travelling surface complies"* over `rank 1 of 5` and a bound *"comparable to its margin"* that is 177% of it. **That is now repaired** (`847b4492`: a separate `check_rank_claim_values`, three derived value rules, both digits of `rank N of M` graded). **The sweep for the same SHAPE is the larger finding.** Classified by predicate: **17 value-grading (A), 15 form-grading-where-value-is-the-failure-mode (B), 2 form-is-genuinely-the-thing (C)**. The B set, with a well-formed falsehood that passes each, live where stated: **`check_rung_estimates_state_their_iterations`** matches `\biterat` in `cost_basis`, and two live docket items clear it with *"**the iteration count is NOT priced here**"* -- the regex reads the disclaimer as the disclosure (19 of 69 rung-shaped proposals clear this way); **`check_fd_grades_current_standard`** recomputes the grade WORD from the PRINTED percentage and never the percentage, and across 323 surfaces its entire positive evidence is one `1.10% -> PASS` row appearing three times; **`check_statistical_labels`** tests that the identifier `_looks_like_interval` is TEXTUALLY PRESENT and that a `±` glyph is on the line -- a hand-typed `± 0.004` passes, and the caption occurs in **zero** of the 17 transcripts it reports clean; **`check_nonconclusive_band_readers`** clears any function whose body contains the substring `conclusive`, including in a comment saying the flag is ignored; **`check_channel_totals_use_one_rule`** clears on `"combine_expanded" in text`, a docstring mention sufficing; **`check_record_writers_name_their_drops`** clears on the bare token `EXCLUDED_KEYS`, so `EXCLUDED_KEYS = ()` -- an empty, false declaration -- satisfies it; **`check_evidence_paths_exist`** and **`check_campaign_json_citations`** test `.exists()` only, so a citation pointing at a real file that does not hold the claimed evidence passes; **`check_studies_carry_what_the_fit_records`** compares KEY SETS only and says so in its own BASIS; **`check_declared_fleet_vs_work`**, **`check_withdrawn_numbers`**, **`check_ungated_completed_runs`**; and two meta-checks, **`check_every_check_states_its_basis`**, which tests that a BASIS string is non-empty and never that it is TRUE -- `check_studies_carry_what_the_fit_records` declares `EVIDENCE` (*"re-derives a published number ... by arithmetic this file performs itself"*) while its own `blind_to` two lines later says *"every VALUE. It compares key sets only"*, **a declaration that contradicts itself inside one dict entry and passes** -- and **`check_every_finding_prices_its_remedy`**, which tests name-membership in `REMEDIES` and never the price. **Seven more outside `self_audit.py`**, ranked: **`check_absolutes.py::classify_unit`** grades an absolute BACKED if the prose unit contains **any runnable command** -- executed over three of the lab's own guards it returns **17/17, 16/16 and 19/19 BACKED**, laundering sentences like *"THIS FILE NEVER WRITES"* on the strength of a nearby USAGE line; **`gate_table.py`** takes the nine-act gate table's verdicts from each act's **self-written** Deviation string and reprints four acts' own `Verdict:` sentences, and it launders `check_gate_table_vs_transcripts`, which re-derives from that same generator, so a wrong self-report agrees on both sides; **`docket_citation_guard.py::unresolved`** tests set membership on a `D<n>` -- **4 live wrong-but-resolving citations** (`V16_GRADE_ROUND5.md:142,143,149` cite D5/D6, which `DOCKET.md:236` says were renumbered to D19/D20 and which today are unrelated `unlink`-swallow rows; `S1_WITH_PRIORS_PREREGISTRATION.md:16,571` cite D8); **`cost_calibration.py`** decides measurement-vs-forecast on `^\s*measured\b` and three live items type "measured" for costs never measured, one with `measured_core_min: 0.0`, a zero that can never over-run and silently strengthens *"a measured basis never over-runs"*; **`withdrawal_sweep.py`** reads OWNED off a role WORD naming who made the past ruling (6 live blocks; `DOCKET.md:115` C4 records 11 of 12 passing on that basis); **`lab_check.py`** P2 admits a module as a gate if any syntactic non-zero exit exists anywhere; **`morning_report.py`** RULE 7 checks `.exists()` and never that the printed figure came from the cited file. **AND A SECOND, NARROWER RESIDUAL. The B1 sweep of 2026-08-15 caught SOURCE ABSENCE and not EMPTY SELECTION.** Thirteen `_no_evidence` sites plus `check_bundle_drift`'s five UNKNOWNs cover a missing file; **nine checks still return PASS when their source is present and their selector matches nothing** -- `checked == 0`, `branches == 0`, `total == 0`, `claiming == 0`, `not rung`, `not groups`. Two confirmed by execution: `check_rung_estimates_state_their_iterations` returns `PASS  no rung-shaped compute proposal is on the docket` against `{"proposals": []}`, and **`check_closure_entry_of_record` returns `PASS  the wall quotes the current entry of record` with `wall.json` REPOINTED AT A NONEXISTENT PATH** -- every guard skipped by `or {}` chaining -- which is the one check that owns the credentials wall's closure claims going green on the wall's absence, while its sibling `check_wall_counters_vs_ledger` FAILs on the same absence. Only `check_board_placement_words` and `check_bundle_drift` treat an empty SELECTION as non-agreement. | Swept 2026-08-15 at HEAD `82fb3d46`+`847b4492` by a delegated read-only cell whose brief was the classification, not the repair; the 34-check count is `len(self_audit.CHECKS)` by import and not by grep, and every B claim above was executed rather than read -- the empty-set PASSes by driving the checks with emptied inputs, `check_absolutes` by running `classify_unit` over three real files, `gate_table` by recomputing all five SIMPLE_ACTS transcripts (every stated deviation is currently correct **by luck**; nothing would notice otherwise), `docket_citation_guard` by resolving the four citations against the rows they land on. The repair half was verified separately: `check_rank_claim_values` FAILs on the shipped member, 27 controls green, 8 mutants each reddening their aimed test in one invocation with `__pycache__` purged per cell. **Cannot see:** whether any of the 15 B checks has a live falsehood passing it TODAY beyond the eight where one is named above; the classification is of the PREDICATE, and a predicate that could pass a well-formed falsehood is a finding whether or not the corpus currently contains one. Also not measured: the same classification over `sdk/` test guards and `sdk/chief_engineer/`. | **Do not repair fifteen checks. The two meta-checks are the leverage, and they are the two that are themselves in the B set.** `check_every_check_states_its_basis` already forces every check to declare `catches` and `blind_to`; **add a third declared field -- does this check grade FORM or VALUE -- and make a check that declares VALUE prove it by naming the source record it re-derives from.** That converts fourteen silent instances into fourteen printed ones at the cost of one dict column, and it is the same move `_no_evidence` made for B1 on 2026-08-15. `BASIS_MEANING` already has the vocabulary: `PROPERTY` is *"asserts a property of a record ... no number is confirmed"* and is the honest label for most of the B set, but **`check_rank_claim_surfaces` was declared `PROPERTY` and still got quoted as reassurance**, so the label is necessary and not sufficient -- the verdict LINE must carry it too, which is why the repaired sibling now says *"complies ON FORM"* in its summary and not only in its BASIS. **Second, and cheaper: extend the B1 rule from source-absence to empty-selection.** Nine `if not <selection>: return PASS` branches become `_no_evidence`; the helper exists, the call sites are enumerated above, and `check_closure_entry_of_record`'s absent-wall PASS should be a FAIL and not an UNKNOWN, because the wall is that check's SUBJECT and not its EVIDENCE -- the distinction `self_audit.py:169-176` already draws. **REJECTED:** rewriting the fifteen as value checks (most have no derivable value -- there is no source record that says whether a `cost_basis` sentence is honest, which is why they are form checks in the first place); and deleting them (a form check that SAYS it is a form check is useful; the defect is a form verdict read as a value verdict). No solver ran; no scoring call, ledger stands at 6; nothing sent, uploaded, filed or registered. | fleet (the FORM/VALUE declared field and the nine empty-selection branches); owner of `check_absolutes.py`, `gate_table.py`, `cost_calibration.py`, `withdrawal_sweep.py` (the four outside guards with live instances); owner of `V16_GRADE_ROUND5.md` and `S1_WITH_PRIORS_PREREGISTRATION.md` (the four wrong-but-resolving D-citations) |
| D175 | **D157's seventh surface is repaired, and the entry-of-record JSON needed the SIBLING treatment for a reason the file already states about itself in another key -- but the consumer question had to be settled first, and it changed what could be written.** `demo-output/website/closure_challenge_round5_qcr.json`, `demo-output/website/agenda/docket.json`, plus the D157 item-4 residue in `demo-output/website/CLOSURE_CHALLENGE_STATUS.md`. Repaired 2026-08-15. **WHAT CONSUMES EACH KEY, ESTABLISHED BY EXECUTION AND NOT BY READING.** `git grep -a` for the filename returned 15 code files; each was opened at its read site. **Nothing in the tree reads `leaderboard_comparison_dated_2026_08_07`, `our_rank_overall` or `our_case_rank` by key** -- `git grep -a -n -F` for all three over `*.py`/`*.js`/`*.html` returns ZERO code hits, prose only. `sdk/scripts/probability_of_rank.py:119` reads `["official_test_harness_result"]`, a DIFFERENT top-level block, so the per-case values it consumes are not the ones carrying the ordinals; `scripts/check_derived_figures.py` reads `official_test_harness_result.round5_overall_full`; `scripts/self_audit.py::_closure_facts()` reads `round5_per_case_full`, `round5_per_case` and `rans_identity_floor_per_case`. **ONE REAL CONSUMER EXISTS AND THE DISPATCH DID NOT NAME IT**: `sdk/tests/test_rank_claim_surfaces.py:2528-2555` locates `rank_companion_2026_08_10` by a recursive `holder()` walk, POPS it, and asserts `_states_the_interval(popped)` is **False** while `_states_the_interval(json.dumps(rest))` is **True**. So that key is load-bearing in the negative direction: **writing the live interval into the 2026-08-10 companion would have turned a green test red**, which is precisely the "changing the string changes a computation" case -- and it is an argument FOR the sibling and AGAINST the edit that is independent of the dated-record argument. **SIBLING, NOT EDIT, AND THE DEFENCE.** The `:123` block is dated in its own key name (`..._dated_2026_08_07`) and its `caveat` says ranks are as-of the frozen pin, so `RANK 1 of 5` was TRUE of the board the block is dated to; overwriting it would make a dated record false of its own date. That is not my reasoning imported from `qcr_vs_rank2_duct_consistency` -- **the file states it about itself**, in `qcr_vs_rank2_duct_consistency_2026_08_11_six_entry_board`, in those words. **The block does NOT present itself as current**, so the worse case the dispatch warned about does not obtain. **Three live siblings were already there** (`qcr_vs_rank2_duct_consistency_*`, `rank_companion_2026_08_11_six_entry_board`, `best_on_board_count_2026_08_11_six_entry_board`), so D157's report that `:124` carries the struck companion "with no forward pointer" is half right: `:194` already supersedes it explicitly and by name. What had NO live sibling was the OVERALL ordinal and all EIGHT per-case ordinals. **TEN keys added, all placed ABOVE what they supersede** per the file's own stated rule that nothing walking the block in order should meet the dated ordinal first: `our_rank_overall_2026_08_11_six_entry_board`, `per_case_our_rank_2026_08_11_six_entry_board`, and eight `our_case_rank_2026_08_11_six_entry_board`. **Repairing only `:188`, the one D157 enumerated, would have left seven identical stale ordinals live** -- the D156 shape once more, so all eight were done. **PROOF THE RECORD IS INTACT**: full flat-key structural diff against `git show HEAD:`, walking every leaf -- **150 leaves before, 160 after, 10 added, 0 removed, 0 CHANGED**. The frozen block is byte-identical. **PROOF IT STILL WORKS**: the file parses (`json.load`), `probability_of_rank.py` re-executed end to end from `SEED_MAIN = 20260810` / `SEED_DOUBLE = 31415` and its 65-line output is **BYTE-IDENTICAL before and after** (`diff` clean), and the one consuming test passes. **`docket.json` SETTLED, and it is a claim about evidence, not a copy of a figure.** Two dated closure records took a dated sibling field each, again byte-identical below (structural diff: 2 leaves added, 0 removed, 0 changed). The `:4440` sibling makes the sharp point rather than restating digits: **the 2026-08-07 call measured SCORES, and how many entries a public leaderboard carries is not something a local scoring call can measure**, so the denominator was never part of what was "measured true" -- it was read off the frozen four-entry clone and then carried inside the same sentence as the measurement. What WAS measured stands (0.0654 -> 0.056647191704213645, ACCEPT under the pre-registered `< 0.065438`); what was not is the denominator, the entrant the margin is over, and the margin. **RESIDUE, and D157's line numbers were STALE by ~40 lines after `fb1833d2`, so every item was re-located by CONTENT**: the two present-tense unbannered lead-claims (D157's `:69`/`:1034`, actually `:77`/`:1092`) struck with the live count **2 of 8** and the model's own count **zero of 8**, with the distinction that the two surviving rows are the organisers' own unmodified RANS field carried into the correction; the three stale entrant ordinals (D157's `:588`/`:964`/`:970`, actually `:627`/`:1015`/`:1021`) struck and paired with their live values; and the four-entrant table titled *"Full public leaderboard"* struck whole and replaced by the six-entry table, on the ground that **the word "full" is what makes it a defect rather than a stale figure** -- a partial table labelled partial misinforms nobody. **FOUR MORE OF THE SAME CLASS THAT D157 DID NOT ENUMERATE, found by the search that followed the repair**: a third present-tense lead-claim, the section-0e standings sentence, a per-case movement table whose rank column named NO board at all, and a docket-recorded target ordinal. All repaired. Re-derived for those: round 4 stands **5 of 7** and round 3 **5 of 7**; and the movement table's correction changes a story rather than digits -- on the four-entry clone round 4 read as holding `AR_14_Ret_180` outright, and against the live population **it was never held**, so "margin collapsed" describes a lead that did not exist. **VALUES, RE-DERIVED, AND THE DEPENDENCE STATED NARROWER THAN THREE ROUTES.** Route 1 `ast.literal_eval` of `LIVE_BOARD` off its assignment node (never imported), route 2 `_closure_facts()` loaded by path and executed, route 3 the script re-executed from seeds. **All three trace every live per-case value to the SAME `LIVE_BOARD` literal**, and routes 1-2 additionally share this JSON; route 3 is independent of 1-2 **only on the interval and P(rank 1)**. **The single genuine input-side corroboration** is the separate fetch at `campaign/BOARD_MOVED_2026-08-11.md:22`: each of the six entrants' eight per-case values means to a published overall at **4 dp, 6/6**, executed here, so a per-case transcription error would have to be exactly compensated to survive. Agreeing: per-case `[2,2,1,1,3,4,4,7]` of 7; best-on-board **2 of 8** with **zero of 8** earned by our own model; P(rank 1) **50.2%**; interval **0-97% at 95%** (route 3: 0.2%-96.9%, 1.55% of outer resamples exactly zero); **four** undecided pairs including the leader's; **three deletions wide**, LOO point ranks 2/3/2; drop-the-hump **78.5%**; seed bound **0.002419** = **177%** of the margin, so it EXCEEDS the lead and its adverse leg puts the overall above the leader. **Margin basis stated**: 0.001365 is the mean-of-eight basis, used because ours is also a mean of the same eight cases; 0.001353 against the published rounded. **One correction to my own first pass, caught by execution**: judging the decline gate with a `1e-12` tolerance against `rans_identity_floor_per_case` returned "2 of 8 EARNED", because the floor is stored to four places while our score is stored full-precision. `_closure_facts()` judges the identity at four places and returns **earned = []**. The dispatch's zero is right and a tolerance chosen without reading the storage precision inverts the most important number on the page. **WHAT THE INSTRUMENTS CANNOT SEE ABOUT THIS TEXT, MEASURED.** Re-derived rather than inherited: rule A returns **0 faults** for a synthetic sentence placing US at a wrong denominator AND **0** for the same sentence corrected, and **0/0** for the per-case ordinal form in both directions, while a positive control binding a NAMED PUBLISHED ENTRANT to an ordinal the pin does not give them returns **1** -- the guard is alive and cannot distinguish this repair from the defect it repairs, because `who is None` for every claim about our own placement. That positive-control sentence is described and **deliberately not quoted**: quoting it makes this row fault, which is D149's shape in its most self-referential form. `self_audit.py:1706` still defers `rank <n> of <m>` to a sibling that grades FORM, so **no instrument in this lab has a denominator predicate** and every corrected ordinal here is unguarded -- D151, unchanged, though `sdk/tests/test_rank_claim_values.py` and `scripts/mutation_harness_rank_values.py` appeared in the tree during this repair and may be it arriving. **GUARDS BEFORE AND AFTER ON THE SAME TREE, IN TRUE PRODUCTION SHAPE** -- `board_placement_faults(text, _published_board(), _ranking_board())`, one call, not the two-call shape a first harness here used and got wrong: `closure_challenge_round5_qcr.json` 0A/0B and 1 unchanged; `docket.json` 0A/0B, **2 -> 1**; `CLOSURE_CHALLENGE_STATUS.md` 0A/0B, **5 -> 4**; `closure.html`, `ACTIVE_RESEARCH.md`, `SUBMISSION_DRAFT`, the `.tex` and `DOCKET.md` all unchanged. **ZERO new faults; two files improved.** The four best-on-board counts still reported on the status page are all on text that is struck or inside a dated banner and are the no-strike-stripper limitation, not live claims. **THREE-ARM REACH**: tracked **20,700** by `git grep -a -n -F`; untracked **1** (`.autostop-hold`), clean; gitignored **37,243** by `git ls-files --others --ignored --exclude-standard -z \| xargs -0 -P 4 /usr/bin/grep -aIlF -f`, returning **two** files, both under `dist/` and both out of bounds -- the same two the previous pass found. **Run tree INHERITED, not extended, and the inheritance is stated because the tree has moved**: the D156 pass covered 121,523 files <= 2 MB by content with zero hits; re-counting today `/usr/bin/find /home/ubuntu/certonomous-runs -type f -size +2M` returns **6,108**, not the 10,526 that pass left uncovered, so the uncovered set SHRANK between passes and no claim is made about the difference. The uncovered files remain OpenFOAM field and mesh data by extension census (`faces` 1075, `points` 607, `U` 594, `phi` 533, `owner` 315, `neighbour` 314, `.vtp`/`.vtu` 528). **NOT MARKED GREEN ON ANY RUNG**: the repairing agent may not grade its own repair, and the instruments above cannot grade it either. Owner: chief (confirmation); owner of the submission package (D157 item 3, `CLOSURE_CHALLENGE_SUBMISSION_DRAFT.md`, NOT touched here -- different owner, and the file is unchanged at 0A/0B/3); owner of `scripts/self_audit.py` (D151's denominator predicate). |
| D176 | **Rule A does not merely fail to see this defect class -- it FAULTS THE REPAIR, and a hard regression test pins the repaired surface at zero, so the corpus's own rule and its own instrument were briefly unsatisfiable together.** Measured 2026-08-15 while repairing D157, `__pycache__` purged. Writing the LIVE entrant ordinals that the chief ruling at `04489465` requires -- a board named by entrant count and retrieval date -- into `CLOSURE_CHALLENGE_STATUS.md` took rule A from **0 to 14 faults** in true production shape, every one of them a CORRECT live statement faulted against the four-entry scoring pin, which is D55/D89 working exactly as designed and therefore hostile to the repair the ruling mandates. **This is not a guard-count observation. `sdk/tests/test_rank_claim_surfaces.py:2250` (`test_the_three_surfaces_fixed_on_2026_08_11_stay_fixed`) asserts `([], [])` on this exact file**, so the compliant repair turned a green suite red -- caught here only because the suite was run mid-repair rather than after it. **THE RESOLUTION IS THE GUARD'S OWN DESIGNED PATH AND IT IMPROVED THE PROSE**: `_PLACE_ADJUDICATED = 400` clears a fault when a placement binding the SAME entrant to the pin's rank sits within 400 characters, and the code comment says why -- *"a passage that ALSO states the entrant's real rank is adjudicating a wrong ordinal, not asserting one"*. Strike-and-keep already requires exactly that: the old ordinal visible, struck, beside the new one. Rewriting each correction block to carry BOTH boards' ordinals for each entrant side by side returned the file to **0A/0B** with no correct claim removed. **The distinction that matters, because the opposite move is what D54 was filed about**: this is not retuning prose until a detector goes quiet -- the added text is the struck predecessor the doctrine demanded and the first draft had merely omitted; had the resolution required DELETING a live-correct ordinal, the right outcome would have been a red test and a filed conflict. **AND A SEPARATE MIS-ATTRIBUTION FOUND IN THE SAME 14, which is a defect in the fault MESSAGE and not in the verdict**: three of the fourteen reported an ordinal bound to an entrant the sentence never placed. The pin carries four names and the live board six, so when a sentence places one of the TWO entrants absent from the pin, `_place_subject_np` finds no pin-known head at that ordinal and walks back to the nearest name that IS pinned -- reporting a complaint about an entrant whose ordinal the sentence never stated. A reader repairing from that message repairs the wrong sentence. This is downstream of D89 (the two live entrants are not in the pin) but is not the same finding: D89 is about coverage, this is about **a fault that names the wrong subject**, and it is invisible in any count-only measurement. **The transferable half**: a regression test that pins a surface to zero faults against a referent the surface is REQUIRED to contradict is a test that will fail on the day the surface is repaired correctly, and the failure will look like the repair's fault. Either the pinned surfaces are exempted from rule A's identity binding, or the doctrine's both-boards form becomes explicit in the repair rule rather than discovered by a red test. OPEN. Owner: owner of `scripts/self_audit.py` (the subject-walk mis-attribution and the fault message); chief (whether the both-boards pairing becomes the stated form of a strike-and-keep ordinal repair). |
| D177 | **A remedy printed to the lab owner as available, costed, and never once executed -- twice in one report, and both commands failed the first time anyone ran them.** The chief told the owner that *two commands clear nineteen of twenty-seven stale claims*. Executed here for the first time: **W1 produced no PDF at all** and **W2 exited 1 and shipped a bundle missing an entire filmed act**. Both are now repaired and both repairs were verified on the ARTIFACT, not on the exit code. **W1 -- `ulem.sty` was genuinely absent from the box, not merely off the build's path.** `/usr/bin/find / -name ulem.sty` over the whole filesystem returned nothing; `dpkg -S ulem.sty` found only `lwarp-ulem.sty`, a different file. Ubuntu ships `ulem.sty` in **`texlive-plain-generic`**, which was not installed, while `texlive-base`, `-latex-base`, `-latex-recommended` and `-fonts-recommended` were -- so the failure was an environment gap, not a source defect, and the alternative repair (dropping `\sout`) would have changed how all 35 strike-and-keep corrections render. `sudo apt-get install -y texlive-plain-generic` (rc=0) -> `kpsewhich ulem.sty` = `/usr/share/texlive/texmf-dist/tex/generic/ulem/ulem.sty`. `closure_challenge_report.tex` now builds **rc=0 on both passes, 42 pages, 1.62 MB**, built with `-output-directory` into scratch so nothing was written into `latex/`. **The output was checked, not the exit code:** `pdftotext` on the rebuilt PDF against the committed stale one gives `Yang` **15 vs 0**, `50%` **15 vs 1**, `rank 1 of 7` **2 vs 0**, `six-entry` **19 vs 0** -- the stale four-entry PDF has no Yang row and no corrected posterior. The withdrawn figures are still *present* in the text layer of the correct build, because `\sout` is strike-and-KEEP; page 1 was therefore **rendered to PNG and read visually** to confirm the rules actually draw. **A text-only check would have graded a struck `68%` as a live one** -- `pdftotext` cannot see a strike, and that is the trap in verifying any corrected PDF in this lab. **W2 -- 15 of 90 members were lost because refusing the PAIR was implemented as refusing the ACT.** Root cause, executed: a recording is frozen and `mission-output/<dir>/certificate.pdf` is a single mutable path every re-run overwrites. **97 recordings announce a certificate in `aircraft-optimization/`; not one announces the page now at that path.** That page is stamped `Issued 2026-08-15T19:47:41Z` / C-2026-3060 and has **no recording in either state directory** -- it was written by a run that recorded nothing, and the plots beside it were rewritten again at 20:44Z mid-session, so the build was pairing against a moving target. Twelve of the fifteen lost members were on disk, intact and referenced; only the certificate disagreed. The fix (`pick_unpairable` + `withheld_directories` in `scripts/build_laptop_bundle.py`) keeps the strict rule untouched, then asks a second question for acts it rejected: ship the act, withhold the contradicting certificate BY NAME, write `CERTIFICATE_WITHDRAWN.txt` into the bundle beside where it would have been, and do not exit 0. Proof, scratch builds only: **75 -> 90 members**; against the committed zip **exactly one member removed** (`aircraft-optimization/certificate.pdf`, which is itself the D136 contradiction -- the committed zip pairs a recording announcing `d889461d...`/C-2026-0918 with a page printing `aeccc6bf...`/C-2026-5686) and **exactly one added** (the withdrawal note). `check_bundle_pairing` still passes, on truth rather than on tolerance. A full sha256 manifest diff -- not just the members predicted -- also found **7 members whose bytes moved** for unrelated reasons (`agenda.py`, `certificate.py`, `exec_bits.py`, `benchmarks.html`, `closure.html`, `wall.html`, `lab_stats.json`), which is the D115 shape: a clearance verified against an enumeration cannot see the item beside the ones it lists. Second half of the fix: `--zip` now **refuses to write an archive at all** when a filmed act is absent (rc=3), because the original defect was not the exit code, it was that a shippable `.zip` got written anyway. **W3 (the same report's evidence sentence) -- `sweep.py -F "rank 1 of 5" --frame everything --files-only` returns 34 files / 111 lines** at `def3ac00`, 20:50Z, not the three named; the round-8 cell got 33/90 hours earlier. The three are a ~10% subset; the *reach* they demonstrate is real, the closed count was not. Corrected in place to a command the reader runs, per the handbook's own convention, rather than a number that rots. | Executing the two owner-facing rebuild commands and the evidence sweep, 2026-08-15, at `def3ac00`. W1 is `demo-output/website/campaign/OWNERSHIP_BOUNDARY_SWEEP_2026-08-15.md:295` and `:175`; W2 is `:296`; the sweep demonstration is `:349-352`. Prior measurement in D170 / `LADDER_V_V15_ROUND8.md` P2-P4, whose open question *"whether `ulem.sty` is installable on this box"* is now answered yes, and whose other open question *"whether the 15 dropped members are recoverable by supplying the missing pairing event rather than by reverting the gate"* is answered **neither**: no pairing event can be supplied because the certificate that would match any surviving recording no longer exists anywhere on disk (every `.pdf` in the repo was decoded and searched for seal `d889461d...` -- one hit, and it is the mismatched copy inside `dist/`). **Cannot see:** which process rewrote the served airliner certificate at 19:47:41Z, and whether shipping Act 1 with a `CERTIFICATE_WITHDRAWN.txt` instead of a sealed page is acceptable on camera -- the console still replays the recording's `certificate.ready`, so it announces a seal whose link now reads *"no certificate for this mission"* rather than opening a page that contradicts it. That is an owner ruling, and nothing was committed into `dist/` or `latex/`. | **The durable half is the reporting rule, not the three repairs: a remedy is a claim about the future, and this lab reported two of them to its OWNER as done-and-costed before either had been run once.** D170 named the class and scoped it to documents; this row is the instance where it reached the person the lab reports to, which is the expensive end. `0ec04c6e` established execute-before-costing for the handbook, D170 for one campaign document, and neither pass reached chief-to-owner reporting. Minimum: **no command may be quoted to Katie with a cost until its exit code and its OUTPUT have been recorded beside it** -- output, because W1's repair now exits 0 while still being one text-layer check away from grading a struck figure as a live one. Second, smaller, and mechanical: `mission-output/<dir>/certificate.pdf` is a single mutable path that any unrecorded run can advance past every recording that describes it, and there is no archive keyed to a certificate number, so a demo artifact is **unrecoverable by design** the moment it is overwritten. A certificate archive is the fix; withholding is only the safe failure. | fleet (the report-to-owner execution rule); the infra family (`build_laptop_bundle.py`, and a certificate archive so this is recoverable next time); `latex/*` owner (a rebuilt PDF is now producible and is NOT committed here) |
| D178 | **The V12 repair was scoped to one section, and the rest of the same document still hands the steward four-entry figures -- including the rung's only actionable recommendation.** The 2026-08-15 repair of `campaign/LADDER_V_PASS3_COLD_2026-08-11.md` fixed §W2 and the ledger row says it fixed *"every figure it carries."* It did not. **(a) `:525-527` is unstruck and recommends publishing the struck figure externally**: *"put the number in the external text. 'Rank 1 on the point estimate; **P(rank 1) ~ 0.67** by a case-level bootstrap over eight cases'…"* -- while `:491` of the same file strikes `0.674` as the four-entry board's and gives **50.2%**. That is **17 points optimistic in the flattering direction**, it is the single sentence in the rung that proposes text for a travelling surface, and the send gate routes this document to Sanaa (*"she… reads V12"*). `:518` carries the same figure as its premise. **(b) §W3 `:559-562` reads *"the supplied baseline beats **all four published entries** (0.046108 against a best published 0.0569; 0.071863 against a **best published 0.0760**)."*** Re-derived against the six-entry board: the **claim's direction survives** -- the baseline still beats all six on both cases -- but the count is four, and on `alpha_05_4071_2024` the board's best is **Yang's 0.0748**, not Reissmann's 0.0760. **The durable point is that a repair scoped to the section where a defect was NOTICED leaves the same defect in the sections where it was not**, and the ledger row then reports the repair with a word (*"every"*) that no one measured. | Independent grade of V12/V13/V14, 2026-08-15, `campaign/LADDER_V_V12_V13_V14_GRADE_2026-08-15.md` §2.3 R1/R2. All figures re-derived from `closure_challenge_round5_qcr.json` and `LIVE_BOARD` read by `ast` (never imported), with my own 400,000-draw bootstrap; **every figure the V12 ledger row asserts reproduced exactly** (margin `0.001365`, seed bound 177%, adverse rank 2 of 7, P(rank 1) `0.5023` interval `[0.007, 0.976]`, three of eight LOO deletions losing point rank 1, four pairwise undecided, best-on-board 2 of 8 with 0 earned). Only these two passages did not. | The rung's product is Sanaa's briefing, so this is a defect in the rung's instrument by the repair block's own standard. Minimum: strike or date `:517-527` and `:559-562` to the six-entry board, and **replace the ledger row's *"every figure it carries"* with the sections actually swept**. Broader remedy, and the transferable half: **a repair that reports coverage must state the frame it swept**, because "every figure" is a claim about a document and the repair only ever read a section. | V12's repair pass (an author, so not its own grader); the ledger's chief |
| D179 | **The V12 repair block quotes two different seed bounds in adjacent bullets and derives its headline from one and its digits from the other.** `LADDER_V_PASS3_COLD_2026-08-11.md:399-403` asserts the bound is `spreads.overall_equivalent_S_bound` = **`0.002419121853891026`** and derives **177% of the margin** from it. The very next bullet, `:404-407`, prints adverse overall **`0.059047`** and favourable **`0.054247`**. Executed: `0.056647191704213645 + 0.002419121853891026 = 0.059066` and `- = 0.054228`, **not** the printed pair; but `0.056647191704213645 +/- 0.0024` gives **exactly `0.059047` / `0.054247`**. `0.0024` is `SEED_BOUND_ON_OVERALL` at `sdk/scripts/probability_of_rank.py:64` -- **the rounded script constant**. So one bullet quotes the JSON to eighteen digits and the next silently uses the script's two-digit rounding. **The conclusion is unaffected** (the adverse overall ranks 2nd of 7 behind Yang under either bound, so the point lead is gone either way), which is why this is a defect of derivation and not of candour -- and it is why it survived: nothing downstream moved. | Independent grade of V12/V13/V14, 2026-08-15, §2.3 R3, arithmetic executed rather than read. | Pick one bound and print both figures from it, or say in the bullet that the adverse pair uses the script's rounded constant. **The durable half is D179's shape, which this lab has now recorded three times in one function family: a figure copied from a script and a figure copied from a JSON, presented as one measurement.** The V16 line already names the cause -- *"storing a measurement whose inputs are not in the repository"* -- and here both inputs ARE in the repository and simply disagree. A single derived constant, or a test that the two agree, closes the class. | V12's repair pass; the UQ family (whether `SEED_BOUND_ON_OVERALL = 0.0024` should be the full value) |
| D180 | **The tracked shipping archive carries EIGHT live four-entry claims; the sweep of record named ONE -- and the miss is in the rung's CRITERION, not its execution.** `dist/certonomous-demo.zip` is six files stale, so `site/closure.html` and `site/benchmarks.html` ship pre-repair. Measured by decoding the archive with `zipfile` (never byte-grepped) and applying a 400-character strike window: **15 of 15 occurrences of this class are STRUCK on the tracked source pages; 9 of 15 are LIVE in the shipped zip** (the ninth is a `lab_stats.py` docstring warning against exactly this fraction -- NOT-A-CLAIM). The eight: `site/closure.html:341` *"the case falls to **3rd of 5**"*; `:342` *"best-on-board count drops **5 of 8 -> 4 of 8**"* (live value **2 of 8, 0 earned**); `:411` *"**comparable to the 0.0029 margin over the published leader**"* (margin is `0.001365` over Yang, bound is **177%** of it); `:434` *"**2nd of 5**"*; `:435` and `:436` *"**3rd of 5**"*; `:502-503` *"**rank 1 of 5**… bound **comparable to its margin**"* (the one D112 named -- and `:139` of the same shipped file says *rank 1 of 7*); `site/benchmarks.html:148-149` *"the **0.0029 margin**… bound of **comparable size (0.0024)**"*, struck on its source at `:156` on 2026-08-14 and still shipping. **WHY THE SWEEP SAW ONE, AND THIS IS THE DURABLE PART.** Its Frame C searched its own derived literal set, which is Rules S/B/R = **score literals**. `2nd of 5`, `3rd of 5`, `5 of 8 -> 4 of 8` are **ordinals and counts about ourselves** and appear in **no rule V14's criterion names**: *"every score literal … plus every prior-art sentence fragment"* enumerates two families and omits the class D151 records as having **no denominator predicate anywhere in this lab**. The search was correct and complete for the families it derived; **the families are the gap**. And the shape recurred against the instrument that named it: the re-run's own transferable lesson is *"a clearance verified against a list cannot see a fourth defect standing beside the three the list names"* -- **its report then became a list of one, and seven stood beside it.** Also measured: the drift is now **50/56, not 51/56** -- `sdk/chief_engineer/certificate.py` is a sixth stale member since V13 §8's discharge. **Reported as a fact about the guard, not cited as evidence:** `check_rank_claim_surfaces` **does open the archive** (its frame line reads *"20700 tracked path(s) plus the members of 1 shipping archive(s)"*), returns WARN, and names **none of the eight**; `check_bundle_drift` returns FAIL and names the same six members I derived without it. | Independent grade of V12/V13/V14, 2026-08-15, §4.3 / §3.2. Four arms stated: tracked **20,698** via `git grep -a` (`-a` reaches **1,476** files `-I` skips, **11 of them holding no NUL byte at all**); untracked **3**; gitignored **37,244** (3 hits, all the extracted `dist/` tree plus one `__pycache__`); run tree **132,049 files / 66 GB covered in full** in three size bands summing exactly to the total, **zero claim-shaped hits**, positive control firing in each. Containers decoded: zip 90 members, **68** PDFs (0 extraction failures), **529** `.gz` (0 hits, control 15,522 lines). | **Two separable remedies and they have different owners.** (i) `dist/` -- rebuild the bundle so the shipped pages match their repaired sources; the source pages are already clean, so this is a build, not an edit, and it is `dist/`'s owner's call (see D177's `--zip` refusal gate). (ii) **Katie's, and it is the one that closes the class:** V14's criterion must name the ordinal-and-count family, or say that it delegates it -- because a rung whose method is fully faithful to its criterion and still misses seven of eight defects has **a criterion that is a list of families where it demands a search**, which is the failure the rung's own last sentence names. **This cannot be closed by re-running the sweep.** | `dist/` owner (the rebuild); Katie (the criterion); the guard family (a denominator predicate, D151) |
| D181 | **Three Ladder V records anchor findings to `latex/…`, and `latex/` does not exist at the repo root.** The 2026-08-15 V14 re-run's Frame D attributes its four-entry PDF hits to **`latex/closure_challenge_report.pdf`**; V16's criterion at `LADDER_V_TRIPLE_VERIFICATION.md:236` names **`latex/closure_challenge_report.tex`** as *"the real instance"* that justifies whole-text matching -- the load-bearing citation of that whole clause; and `LADDER_V_PASS2_2026-08-11.md:245` cites `latex/closure_challenge_report.tex:864`, `:825`, `:905`. Executed: `ls -d latex` fails, and `git ls-files` returns only **`demo-output/website/latex/closure_challenge_report.{tex,pdf}`**. Every one of those anchors is unresolvable as written. Not cosmetic: V16's whole-text-matching justification rests on where a line break falls in a specific file, and a reader cannot open the file to check. **The same defect is in the dispatch layer** -- the do-not-touch pathspec `latex/*` given to graders matches nothing, so a rule intended to protect a document protects no file. | Independent grade of V12/V13/V14, 2026-08-15, §4.4, while sweeping 68 PDFs with `pdftotext` (0 extraction failures) and finding the hits under the `demo-output/website/latex/` path instead. | Repoint the three citations to `demo-output/website/latex/…`, and fix the `latex/*` pathspec in the standing do-not-touch list to the real prefix. This is V15 round 7 §3.2's *"repo-rooted citation resolution"* class with a live instance; the mechanical form of the remedy is a check that every `` `path/…` `` citation in a campaign record resolves against `git ls-files`, which would have caught all four at write time. | the V14 re-run's author and V16's author (their own records); whoever maintains the do-not-touch list |
| D182 | **F1 and M1 of D164 are repaired, and the repair required a defect nobody had named: the check was throwing away its own positive evidence.** **(a) `check_normative_clauses.decide()` reproduced exactly as reported** — `decide(0, "", [], [], 887, [])` returned `('PASS', ['887 normative clause(s) examined; 0 pin a figure that agrees with its record; 0 are UNDECIDABLE and listed above; none is graded false'])` at `def3ac00`. It now gates on the DECIDED population and returns `UNKNOWN` naming which of three arms it took: **EMPTY-1** no clause matched, **EMPTY-2** clauses matched and none reached a verdict (the F1 state), **EMPTY-3** a source could not be read so the emptiness is unattributable. `empty_arm()` and `empty_reason()` are one definition with two callers, so the new `DECISION SET` frame block and the verdict line cannot name different arms — a mutant that made `main` stop passing `unreadable` to `decide` SURVIVED the first cut of the test because the test read the frame instead of the verdict, and the test now reads only the text below `VERDICT:`. **(b) The undiscovered half.** Gating on findings alone made a genuinely CLEAN corpus return UNKNOWN: `scan()` dropped a compliant clause with a bare `continue`, so a corpus in which every mandate is obeyed produced an EMPTY graded set — which is *why* the original author had to gate on `examined`. The check's own control NEG-6 caught the naive repair (recording compliance as a finding) and forced the right one: compliant clauses go to a `cleared` list, silent in the report as NEG-6 requires, counted in the decision set. Without this the instrument would have PASSed only while defects remained. **(c) `check_derived_figures.main()` reproduced with no test at all** — all three UNKNOWN guards survived `if False:` with both test files at 78 passed, exit 0. It now has seven tests that DRIVE `main()` by patching its source of records (`tracked_prose`, `read_sources`, `run_controls`) and asserting on the code `main` RETURNS, never recomputing the invariant beside it. **20 of 20 mutants killed**, each with `__pycache__` purged by `/usr/bin/find … -prune -exec rm -rf {} +` immediately before every `pytest`, control and mutant in one invocation, file restored and SHA-256-verified. **Both must-not-match controls hold (L-84):** a fully compliant corpus PASSes exit 0 and a planted violation FAILs exit 1 in BOTH checks, so this is not an alarm that only ever returns UNKNOWN. **And the guard enumeration that the repair turned up, which is the part worth keeping.** Rather than asking which guards the tests cover, every branch in both modules that sets a verdict, returns an exit code, changes a finding, or changes a count printed in the frame was ENUMERATED (12 in `check_derived_figures`, 16 in `check_normative_clauses`) and each was neutered. **Eight more had no killing test.** Two in `check_derived_figures` were real: `parse_written`'s `except InvalidOperation: return None` (mutated to return an exact zero with zero tolerance, the suite stayed green, and every unparseable figure would then be graded as a precise 0) and the interval guard on a divisor whose interval spans zero (deleted, the next such sentence in the corpus raises ZeroDivisionError). Five in `check_normative_clauses` -- W1's cross-document-pointer discriminator, W3's deferral to W2, the generated-HTML frame exclusion, the oversize frame exclusion, and `has_triple` -- **and not one of them is a false green: every one changes a REASON or a printed FRAME COUNT and not a verdict, which is exactly why nothing noticed.** A check whose verdict is right and whose stated reason is wrong reads as working, and the reason is what an operator acts on. All eight are now pinned. One survivor is LEFT surviving and stated: `parse_written`'s `if not isinstance(exp, int)` is reachable only for a Decimal NaN or Infinity, which the regexes that feed it cannot produce, so a test would pin its own fixture. **37 mutants killed, 1 stated survivor.** **AND ONE MUTATION RESULT THAT WAS WRONG, recorded because a mutation cell that cannot be audited is not evidence:** mutating `unreadable.append(...)` to `opened.append(rel) or unreadable.append(...)` reported a SURVIVOR and is not one -- `list.append` returns None, so the `or` still evaluated the original call and the mutant was a no-op. Deleting the line outright kills two tests. **A harness that asserts its mutation site occurs exactly once cannot tell a mutation from a no-op; only a KILLED result can, so a SURVIVED result is never self-certifying.** | LADDER_V_V15_ROUND8 F1 and M1, re-executed rather than taken on the record, 2026-08-15. Live verdicts unchanged either side of the repair: `check_normative_clauses` PASS exit 0 (895 examined, 2 decided, **0 cleared**), `check_derived_figures` PASS exit 0 (64 anchored + 5 computations). | **The residual this repair does NOT close, stated because it is the more interesting number: `cleared` is 0 on the live tree.** Not one normative clause in this corpus pins a correct literal WITH its board triple, so the shipped PASS still rests on 2 UNDECIDABLE findings out of 895 examined clauses and on no positive evidence at all. That is inside the contract — an UNDECIDABLE is a verdict and the decision set is not empty — but a PASS whose entire decided set is UNDECIDABLE confirms nothing, and the `TRUE` section of the report has printed `[0]` on every run in this check's life. Settling it needs a ruling on whether an all-UNDECIDABLE decision set should itself be UNKNOWN; that would turn the live run red, which is why it is filed rather than done. | chief (the all-UNDECIDABLE ruling); fleet (the same `cleared` shape in any other check whose compliant case is silent) |
| D183 | **D165's shape enumerated across all 1,934 tests in `sdk/tests/`, and the answer is EIGHT tests that cannot fail — but the more useful result is that the detector for the LAST one has no usable precision, so this census cannot claim completeness and says so.** An AST pass flagged every test whose body reaches NO production symbol at all — resolving `self._helper()` methods, pytest fixture parameters, `setUp`-bound `self` attributes, module constants built by reading a tracked file, and locals aliased from production (`for check in (sa.a, sa.b)`), each refinement added because the previous cut produced a false positive that was checked by hand. **11 candidates. Every one was then MUTATED IN PRODUCTION and re-run: 7 confirmed unable to fail, 4 killed on the first mutation.** The 7: `test_certificate.py:279` (labelled MUTATION PROOF, restates that `"reproduc"` is a substring of `"Reproducible…"` over two of its own locals — the same footer planted in `build_certificate_v2` was caught by a SIBLING and not by it); `test_pope_basis.py:124` (recomputes both sides of an algebraic cancellation from its own random matrix, never imports `pope`; `a = rs/k - (2/3)I` → `(1/3)` in `sdk/scripts/pope_1975_basis_check.py` left it green); `test_uq.py:531` (reads two class constants declared six lines above it; forcing every ladder non-monotone in `uq.eca_hoekstra_band` left it green); and four honest positive controls of checkers that live **inside the test files** — `test_register.py:491` and `:526`, `test_sweep_frames.py:389`, `test_untrained_qcr_attribution.py:140`. Each of those four DOES die when its own checker is stubbed, but each stayed green while the exact production regression it is named after was re-introduced (an em dash planted in `valve_study.py`'s emitted `geometry.ready.label`; an unbacked absolute planted in `scripts/sweep.py`; the Spalart (2000) attribution stripped from `build_benchmarks.py:116`), and in every case a sibling test was what actually caught it. **The names are the hazard**: `…fails_loudly`, `test_the_mutant_is_caught`, `MUTATION PROOF` all read as end-to-end proof that a production surface is guarded, when the guarding is done elsewhere. **The eighth is D165's own instance and this census MISSED it**, which is the finding worth keeping: `test_bundle_drift_gate.py:230` CALLS production (`sa.Result(...)`) and then asserts only `sa.UNKNOWN == "UNKNOWN"` and two membership facts, so a detector asking "does the body reach production?" says yes. Re-verified independently: `self_audit.py:7745`'s `return 3 if any(r.status == UNKNOWN …) else 0` → `return 0` leaves **all 16 tests in that file green, exit 0**. **The detector that would catch that shape was built and REJECTED by measurement**: "no operand of any assertion is the result of a call" flags 845 tests, 203 of them behaviour-named, and **26 of the 26 mutation-killed tests written in this same commit appear in it** — a precision of zero on the only sample whose answer was known. Two weaker cuts were rejected the same way: "constant-pinning only" flagged 114 and 4 of 6 sampled by hand were false positives. **So the honest statement of coverage is: 8 tests in this repo are known to be unable to fail, the detector that found 7 of them has 64% precision and unmeasured recall, and no detector was found that separates a tautology from a legitimate two-line compute-then-assert.** | Full AST census over 1,934 test functions in 79 files, `sdk/tests/`, 2026-08-15, executed while repairing D164. Every candidate verified by mutating the production file it names and re-running, `__pycache__` purged before every `pytest`, all 12 production files restored and SHA-256-verified. | **Do not delete the four test-file-checker controls.** Three of them guard checkers that genuine sibling tests use, so removing them removes a real control. **Rename them** — a name is read as a coverage statement, and the repair is to make each name say which artifact it grades. The four verified GENUINE (`test_geometry_study.py:925`, `test_race_study.py:243`, `test_sweep_frames.py:399`, `:406`) are untouched. `test_bundle_drift_gate.py:230` is the one to fix outright: the property is guarded by `test_empty_set_is_not_agreement.py`, so it is dead weight occupying the slot where its absence would be noticed. **The durable fix is not a better detector — the measurement above says one may not exist. It is that a test asserting a property must be shown to die without it, once, at the time it is written.** | fleet (the four renames and the one deletion); chief (whether "every new test ships with the mutation that kills it" becomes standing, which is the fourth week this would have caught the week's findings) |
| D184 | **Rule W-3's legality test is structurally blind to the whole class of gates this lab actually scores, and S6 is the specimen its own record hands over.** W-3: a pre-registration may be **amended** only while no compute has run, *checked against run directories*; after first compute, only dated addenda that cannot alter gates. Checked 2026-08-15 for `campaign/S6_WIRING_PREREGISTRATION.md` against `/home/ubuntu/certonomous-runs/`, which is **outside the repo and invisible to every git-based search and to this shell's `grep`** (it execs `ugrep --ignore-files`): **0** paths matching *s6* / *residual_gate* / *monitor_rule*, against an **independently obtained total of 132,049 files** and 446 top-level run directories; **0** `ledger.csv` naming s6; **0** files written since 2026-08-10 22:00 (the archive's newest file is dated 2026-08-10). **By the literal test an amendment is legal. It is not.** S6's gates were **scored twice** -- the RESULT section at `1471b8f3` and the re-score at `b4144116` -- both by **replay over an already-archived corpus, at zero core-minutes**. The answer exists, is written in the file, and the property the L-44 freeze protects (a gate could not have been tuned to an answer) **is already spent**. **A replay consumes no run directory**, so the run-directory test cannot see it. The contradiction is three screens apart inside one document: it reports *"Zero core-minutes"* in the same section that reports eight scored rows. | Settling the S6 retrospective-row item, 2026-08-15. The marking was consequently written as a **dated addendum, not an amendment** -- the conservative branch -- and states the check in its own text, as W-3 requires. | **W-3 needs a second limb: frozen on compute OR on scoring, whichever comes first**, because *"has this been graded yet?"* is the question the freeze is actually asking and run directories cannot answer it. Cheap instrument: a pre-registration freezes the moment any document, **including itself**, reports a number against one of its bars. **The population is unchecked** -- `ladder-b/S1_PRIORS_PREREGISTRATION.md` §4a uses the identical seven-item run-directory form; its verdict happens to be right because that item had been neither run nor scored, so the form has never yet been tested against the case it fails on. Every replay-scored pre-registration in the lab is in this class. **[CHIEF RECOMMENDATION 2026-08-15, at repo `b241951f`, for the owner — W-3 is her rule and only she can amend it.]** **The defect is that W-3 tests for the WRONG EVENT.** It asks whether a run directory exists; what it means to ask is **whether the gate has been answered**. Those coincide only when answering costs compute. A **replay over an already-archived corpus scores a gate at zero core-minutes and creates no run directory**, so the literal test returns "no compute has run — amend freely" at the exact moment amending is least legitimate: the answer is already known, and the freeze L-44 exists to protect is spent. S6's gates were scored **twice** this way (`1471b8f3`, `b4144116`). **The recommended repair is one clause, and it makes the rule cheaper rather than stricter:** an amendment is legal only while the gate is **unanswered by any means** — run, replay, or analysis — and the amendment must state **how it was checked**, naming the replay corpus as well as the run directories. That is the same shape as the rule's existing requirement to state the run-directory check, so it adds a line rather than a procedure. **What I am NOT recommending, and why:** banning amendment after any analysis at all would freeze a pre-registration the moment anyone looked at adjacent data, which would push work into unrecorded channels — the failure W-3 exists to prevent, arriving by the other door. **The population is unmeasured and that is the owner's call to prioritise, not mine to assume:** `S1_PRIORS_PREREGISTRATION.md` §4a uses the identical seven-item run-directory form and has simply never met the failing case, so its verdict is right by luck rather than by construction. **Note the asymmetry that makes this worth her time:** the rule is currently permissive where it should be strict, so nothing it has ever cleared was necessarily checked — the direction of the error is toward more amendment, not less. | chief (W-3 is a rule); fleet (the population) |
| D185 | **S8 is wired to nothing at HEAD, the validation ladder still grades it `validated`, and the audit row that filed this got its own sharpest detail wrong.** Verified 2026-08-15 **by execution, not by reading**. The only production `LogMonitor` construction is `head_engineer.py:1030` -- `LogMonitor(novel=novel, on_anomaly=...)`, no `courant_limit` -- and **no arming method exists**; S8's twin S6 got `arm_residual_gate`, called from `stage_case`, and S8 was left behind. `courant_limit` appears in exactly three production places (`:223` accept, `:230` store, `:619` gate) plus two test lines. **Mutation reproduced independently**, `__pycache__` purged, two arms and one lever, 60 fixed time steps climbing monotonically to a peak Courant of **49.52 against a case limit of 1.5** (33x): the production shape raises **0** anomalies; `courant_limit=1.5` raises **2** on byte-identical input. **The probe's first form raised 0 in BOTH arms** -- the `COURANT` regex at `:205-206` requires `mean: ... max:` *with colons* and the probe omitted them, so a false clean was one punctuation mark away and was caught only because the positive control failed to fire. That is L-84 and never-PASS-from-an-empty-set working exactly as intended, and it is recorded because the near miss is the transferable part. **WHICH SENTENCE IS TRUE:** `MONITOR_STANDARD.md:1168-1169` -- *"S8 is unchanged: still no replay, still needs one... No replay exists at all"* -- is **TRUE**, and `:694`'s bare **`validated`** is **false as a ladder grade**. Corroborated independently: `sdk/scripts/replay_monitor_rules.py` covers **S1-S6 only** (`RULE_KINDS`, `:122-129`), which is consistent with `:1169` and not with `:694`. `:694`'s cell is a **tolerance calibration over 4 transient logs** (largest healthy overshoot 0.403% against a 2% tolerance, longest healthy monotone run 17 steps against a window of 20) -- a **constant fitted**, graded as a **rule validated** -- against the **21** transient logs `:1169` names as the corpus a real replay needs. **AND THE CORRECTION TO D60, which strengthens its point and falsifies its evidence:** Round 5 states S8 *"is the only row in a 12-row table with no date."* Measured off the table: it has **13** data rows and **five** carry no date -- S6, S8, S9, S10, S11 -- and **three of those read `validated`**: S8, S9 and S10. **The undated grade is a class, not an instance.** | Verifying the S6/S8 monitor-rule line by execution, 2026-08-15, at `9393e267`. Frame: `git grep -a` over the tracked arm for `courant_limit` and `LogMonitor(`; the ladder table parsed cell-by-cell rather than read. **Cannot see:** whether S9's and S10's undated `validated` are also unearned -- only S8's was adjudicated here, and the other two are named so the next reader does not have to rediscover the class. | **Do not fix the ladder cell by adding a date -- that would launder a calibration into a validation.** `:694` should read what it is (tolerance calibrated over 4 logs, no replay, rule UNREACHABLE in production) or say `not validated`, and it must carry the reach qualifier `:1028` already carries. Then S9 and S10 get the same read. **Separately and more cheaply: wire S8.** `MONITOR_STANDARD.md:1181` already prices it -- *a Courant replay over the 21 transient logs, then `maxCo` from each case's `controlDict` as the per-case default* -- at **~1 pass, 0 core-min**, and the S6 wiring is the worked template for exactly that shape. `MONITOR_STANDARD.md` is a standing record (H6) and the ladder edit needs its owner. | chief (the standard is a chief record); infra family (the wiring) |
| D186 | **A defect record that names the wrong symbol stays unfalsifiable-looking-live indefinitely; this one survived four days and two settlement attempts.** `PRODUCT_LIST.md` filed, 2026-08-11: *"NEW CODE DEFECT, needs a rung: `_solver_tolerances` cannot resolve OpenFOAM regex-group field keys... because matching is string equality."* **True when written, and repaired the next morning at `b4144116` -- but not in `_solver_tolerances`.** Resolution moved **one level down** into `_dict_lookup` / `_field_reachability`, per FIELD, following OpenFOAM's own `dictionarySearch.C::csearch` order (literal keys before pattern keys; among patterns the last-declared match wins; `fullmatch` because `regExp::match` is `std::regex_match`; an uncompilable key left UNRESOLVED rather than guessed). **`_solver_tolerances` and `_residual_controls` still exist, still key by declared key, and have no production caller** -- four-arm sweep 2026-08-15: **tracked** (`git grep -a`), **untracked**, **gitignored** (`git ls-files --others --ignored --exclude-standard`, the only reach to that arm), and the **out-of-repo run archive**. The single gitignored hit is `dist/certonomous-demo/sdk/chief_engineer/head_engineer.py`, verified **byte-identical to the tracked file** and therefore shipping the repair, not the defect. So a reader following the record to the named function finds code that still looks defective, guarded by a docstring warning against a use **nothing makes**: the record reads live, the defect is dead, and nothing in between says so. **Settled by mutation rather than by reading the repair** -- every `__pycache__` purged before each cell, control and both mutants in ONE invocation: control **9/9 green**; the original defect reintroduced (pattern walk deleted, i.e. string equality) kills **6 of 9**; and the **must-not-match control (L-84)**, `fullmatch` weakened to `search` so the lookup begins resolving keys it should not, kills **2 of 9**. Reverted, green at both ends, working tree clean. | Settling the `_solver_tolerances` standing item, 2026-08-15. The brief that dispatched it warned that long-open items here have been *"repaired differently from what their record said"*; this is that, measured. | Two things. **(1)** Delete the two dead helpers, or mark them dead in one line -- a function whose own docstring says it *"must not be indexed by field name"* and which nothing calls is a trap set for the next reader, and the next reader is the one who wires S8. **No live caller depended on the loose behaviour**: the four-arm sweep is the evidence that none exists, and the must-not-match mutant is the evidence that the suite would catch one if it were added. **(2)** The durable half: **a defect record must be anchored to the symbol that is REPAIRED, not the symbol where it was FOUND** -- otherwise the closing commit and the record never meet, and the record ages into a false live defect. The instrument is cheap: a settlement note carrying the repairing commit anchor, written where the finding was filed, which is what closed this one. | fleet |
| D189 | **The placement guard's RECOGNISER is loaded from the frozen scoring pin while its ORACLE is loaded from the live record, so rule A is structurally blind to the two live entrants the pin does not carry -- one of whom is the leader.** Executed at `9dca4738` in a detached worktree, `__pycache__` purged. `_board_names(_published_board())` compiles `\b(liu\|montoya\|reissmann\|wu)\b` -- **four** names, from `deb91557`. The live board has **six**; `_ranking_board()` returns `{yang 1, reissmann 2, wu 3, tian 4, liu 5, montoya 6}` and `_live_ranks()`, in the same module, returns **seven** keys including `Yang` and `Tian`. Rule A therefore **cannot fault any ordinal pinned on Yang or Tian, true or false**: `"Yang is rank 6 on the live board"` (false), `"Yang holds rank 3 on the published board"` (false), `"Tian is rank 1 on the live board"` (false) and `"The rank-2 entry, Tian's model, leads the field"` (false) each return **0 faults**; substitute a pinned surname into the identical sentence and it returns 1. **THIS IS NOT D48 AND NOT BLIND TO ITEM 12.** Item 12 is about the pin's AGE -- for the four entrants the pin does carry, the ordinal is judged against a stale board and the error runs both ways, but the sentence IS examined. This is about the pin's MEMBERSHIP: for Yang and Tian no sentence is examined at all, in either direction. The check's own fourteen-class "WHAT IT CANNOT SEE" list contains its nearest neighbour (*"any entrant referred to by a co-author rather than the first author on their board row"*) and does not contain this. **IT IS ALSO THE CAUSE OF D176's WRONG-ENTRANT FAULTS, which D176 filed as a symptom**: executed, `"Wu & Zhang were passed; Tian is rank 4 on the live board."` returns *"'rank 4' is bound to **Wu**"* -- the subject walk does not recognise `Tian` and keeps walking to the nearest name it does. **WHY IT IS THE WORST OMISSION AVAILABLE TO THIS RUNG:** V16 exists on the finding that a wrong ordinal about a competitor is a rank claim about ourselves wearing a competitor's name, wrong in the direction that flatters us. The competitor a lab most benefits from misplacing is the one AHEAD of it. That is Yang, and rule A cannot see Yang. **THE DURABLE SHAPE -- split-referent vocabulary:** when a check's RECOGNISER and its ORACLE have different provenance, the difference is a silent MISS rather than a loud error, and **no precision or reach measurement can ever see it**, because both are computed over sentences the recogniser has already accepted. That is why six graded rounds of precision and reach work did not surface this. | Ladder V, V16 grade round 11, 2026-08-15, frame `9dca4738`, non-author (dispatch record `a00b47de0f60cc014`, first action 20:56Z, after every commit graded). Tracked arm only -- this is a property of the guard, not of the corpus. Checked against `git show HEAD:docs/DOCKET.md` before filing: D48, D55, D89, D151 and D176 each name an adjacent defect and none names this one. | **Do NOT move the scoring pin** -- it is correct and V1 needs it frozen, and `BOARD_MOVED` sec.4 says so. The recogniser's vocabulary is a DIFFERENT list from the oracle's ranks, and the fix is to build `_board_names` from the union of the pin and the ranking record while leaving every rank lookup on the pin. That makes a claim about Yang or Tian *visible*, which is a strict improvement even before the referent question of `04489465` is settled, and it costs nothing that a rank lookup depends on. Add the class to the check's BLIND TO list either way, **generated from the two boards rather than typed** (L-79), so it cannot go stale the way D190's figures did. Cheap: both boards are already read in the same function. | V16's owner / fleet |
| D190 | **Chief ruling `04489465` refused to re-point the guard 'until the dated-context discriminator exists'; the discriminator was built eighteen hours later in the same file, nothing recorded its arrival, and re-measured today the ruling's quantitative ground does not hold.** **(a) THE PREREQUISITE WAS MET AND NOBODY WROTE IT DOWN.** `04489465` (2026-08-15T02:59:48Z) declines to re-point on the ground *"50 of the 68 new faults being correct dated records -- so until the dated-context discriminator exists, criteria are graded by a reader."* `847b4492` (2026-08-15T20:26:59Z) built exactly that: *"THE DATED-CONTEXT DISCRIMINATOR ... a section banner carrying a withdrawal verb AND an ISO date exempts the section BELOW it. The date is required."* It is `self_audit.py::_live_claim_text` at `:1529`, it is in production use by `check_rank_claim_values`, and mutant M3 of `mutation_harness_rank_values.py` reddens its aimed test, so it is pinned and not merely present. `board_placement_faults` does not call it and `check_board_placement_words` passes raw decoded text straight in (`:3820`). **The ruling still reads as live on a condition that expired the same day.** **(b) THE FRAME'S PUBLISHED COST IS STALE BY A FACTOR OF EIGHT.** The verdict line generated today says *"swapping it to the live board ... takes rule-A faults **from 2 to 68** ... 50 of them dated records."* Re-measured at `9dca4738` over **18,974 opened tracked surfaces** (of 20,701; 1,727 skipped as absent, a directory, or over 4 MB), `__pycache__` purged, three configurations in one pass: **PIN 16 faults / 6 files; LIVE 79 / 33; LIVE + the discriminator 40 / 24.** The typed pair `2 -> 68` describes a measurement of 2026-08-14 and is generated into the verdict beside figures the same function deliberately generates rather than types (the pin's date, its entrant count) -- L-79 applied to one half of the sentence and not the other. **(c) AND THE GROUND ITSELF FAILS.** *"50 of the 68"* implies a dated-context discriminator recovers ~74% of the re-point's cost. Measured, `_live_claim_text` -- which masks MORE than dated history, also clearing strikes, quotations and code -- clears **39 of 79, i.e. 49%**, leaving **40 faults standing**, not the ~18 the ruling's arithmetic implies. Two caveats stated rather than buried: the corpus moved between the measurements (68 -> 79), so this is not like-for-like, and per D191 the post-mask set is **not a subset** of the pre-mask one. Neither caveat closes the gap between 74% and 49%, and forty is not eighteen. **THE DURABLE SHAPE: a ruling that gates itself on a prerequisite, with no instrument watching for the prerequisite's arrival.** Second instance in this rung's own records -- D48's addendum (*"THE LATENCY PREMISE HAS EXPIRED, AND WITH IT THIS ROW'S REASON FOR BEING FILED RATHER THAN FIXED"*) is the first, and it too was caught by a human reading the row. | Ladder V, V16 grade round 11, 2026-08-15, frame `9dca4738`, non-author. The three-configuration sweep is one pass over `_tracked_files()` calling `board_placement_faults(text, pin, live)`, `(text, live, live)` and `(_live_claim_text(text, cdf)[0], live, live)` per surface; run in a detached worktree so the concurrent uncommitted edits to `scripts/self_audit.py` could not contaminate it. **Cannot see:** the untracked, gitignored and run-tree arms, which this round did not measure and does not quote. | **Three separable asks.** (1) Record that `04489465`'s prerequisite is met -- one dated line on the ruling, and it is the cheapest item here. (2) Replace the typed `2 -> 68 / 50 of them` in the frame with a figure the check computes, or with a dated measurement that says its own date; a cost figure inside a verdict is a claim and this one is 8x wrong. (3) **The decision itself is the chief's and is NOT proposed here**: the new input is that re-pointing today costs **40** faults after the discriminator, not ~18, so the ruling's cost-benefit should be re-run rather than assumed. **The durable half is (4): an instrument that watches for a gating prerequisite.** Every ruling of the form *'not until X exists'* should name X as a grep-able symbol and be re-checked when it appears; two instances in one rung with no mechanism after either is the argument for it. | chief (the ruling and the decision); V16's owner (the frame figures); fleet (the prerequisite watcher) |
| D191 | **A masker built to REMOVE claims MANUFACTURES them, because it preserves offsets and destroys separation while the rule that consumes it binds by character distance.** `_live_claim_text` blanks exempt regions **to spaces, preserving offsets** -- which is correct for a line-number-reporting value grader. Rule A binds by **character distance**: `_PLACE_BIND = 40` for the subject walk and `_PLACE_ADJUDICATED = 400` for the adjudication window. Blanking preserves offsets and destroys separation, so prose that held a name 200 characters from an ordinal becomes 200 spaces and the two become adjacent. Measured over the tracked corpus at `9dca4738`, rule A at the LIVE board with and without the masker: `campaign/LADDER_V_PASS3_COLD_2026-08-11.md` **0 -> 1**; `CLOSURE_EVALUATION_PROTOCOL.md` **3 -> 4, and all four are NEW while all three originals cleared**; `campaign/LADDER_V_TRIPLE_VERIFICATION.md` **2 -> 3**. The mechanism is legible in the fault messages themselves: *"...against Yang's (   sec.3.4): point rank 2..."* -> `'rank 2' is bound to Yang` (blanking pulled the name into the 40-character window); *"\| the paper linked from leaderboard rank 4 \| \| \| 6 \| Reissmann,"* -> `'rank 4' is bound to Reissmann` (**blanked TABLE CELLS collapsed two rows into one another's window**); *"...are none of those: *  * -- Wu & Zhang being **rank 2**..."* -> `'rank 2' is bound to Wu` (the blanked quotation was the thing that made the passage a quotation). **SO THE AGGREGATE IS NOT A SUBSET:** re-point + mask gives 40 faults where re-point alone gives 79, and the 40 are not 40 of the 79. **WHY THIS MATTERS MORE THAN THE INSTANCE.** The guard fails the lab's own strike-and-keep house rule -- executed, rule A faults a `\sout{}` claim, a `~~...~~` claim, a `<del>` claim and a claim under a dated `## Superseded 2026-08-12` banner, **all four**, while `_live_claim_text` clears all four -- and *'pipe rule A through the masker'* is the one-line remedy that finding invites. **Measured, that one-liner invents at least eight faults across three files, three of them binding an ordinal to an entrant the sentence never mentions.** The fix has to be a SEPARATION-PRESERVING mask (delete-and-close, or a token barrier) and that is a different change. **BOUNDED, NOT WIDENED:** probed on a struck third-party sentence and on one of ours with a competitor's name behind a struck clause, `check_rank_claim_values` returned 0 faults on each -- `_VALUE_OTHERS_SUBJECT` requires the name adjacent to a copula and then `\s*$` immediately before the ordinal, so a run of blanks defeats the bind rather than forging it. The hazard is real for a **distance** bind and **not demonstrated** for an **adjacency** bind. | Ladder V, V16 grade round 11, 2026-08-15, frame `9dca4738`, non-author, in a detached worktree with `__pycache__` purged before every cell. The three surfaces were re-executed individually after the corpus sweep and the created faults set-differenced against the unmasked set, so 'created' means `set(masked) - set(unmasked)` and not a count delta. | **Do not pipe rule A through `_live_claim_text` as it stands.** Either (a) give the masker a separation-preserving mode -- replace an exempt span with a single sentinel character rather than N spaces, and let the offset map be carried separately, which is what a line-reporting consumer actually needs; or (b) leave rule A unmasked and give it its own strike/banner test that runs BEFORE the distance walk rather than by blanking the text under it. (b) is cheaper and is the honest reading of chief ruling `29452c51` (*there is no strike stripper; the only mechanism is a 400-character proximity window*) -- the ruling is right that there is no stripper, and this row is the measurement of why adding the obvious one is not safe. **The transferable rule: an offset-preserving mask and a distance-based matcher must never be composed without measuring the composition**, and the measurement is cheap -- one sweep, set-differenced. | V16's owner / whoever implements the strike-and-keep repair for rule A |
| D192 | **Every one of the placement guard's 29 live faults is a record OF the guard; its sibling already excludes exactly those surfaces by name; and the guard's own frame states the principle about a hypothetical while breaking it in the shipped configuration.** `check_board_placement_words()` executed at `9dca4738` over 20,701 tracked paths (1,504 opened, 167 naming an entrant, 983 placement expressions found) returns **WARN -- *every travelling surface agrees with the board; 29 lab record placement(s) do not***. Tabulated: `docs/DOCKET.md` **10** (rows recording this guard's defects), `sdk/tests/test_fault_message_matches_rule.py` **8** (its own test file), `campaign/LADDER_V_V15_ROUND7.md` **3** (a round grading it), `sdk/tests/test_two_board_referents.py` **3** (its own test file), `docs/INSTRUMENT_INTEGRITY_LEDGER.md` **2**, **`scripts/self_audit.py` 2 (its own source comments)**, `campaign/MARGIN_PRECISION_INTERVAL_2026-08-15.md` **1**. 15 rule A, 14 rule B. **Not one is a claim this lab is making about the board. The correct count of misplaced entrants is ZERO and the reported count is 29.** **THE SIBLING ALREADY SOLVED IT, BY NAME:** `check_rank_claim_values` carries `_VALUE_NOT_A_SURFACE = ("sdk/tests/",)` with the rationale in the module -- *"A LABELLED TEST CORPUS IS NOT A CLAIM SURFACE, and this is a definition rather than a tuning ... Faulting them is faulting the control"* -- and prints the excluded count in its frame. `check_board_placement_words` has no such exclusion and is reporting **11 faults inside two of its own test files and 2 inside its own source**. **AND THE GUARD HAS THE CONCEPT, APPLIED ONLY TO A FUTURE IT DECLINED:** its generated frame says, of the re-point it did NOT make, *"18 of them inside its own held-out sets and source, **where moving the binding moves the ruler with the sample**"* -- exactly right, written about a hypothetical, while 29 of 29 such faults are reported from the shipped configuration unremarked. **WHAT IT DOES TO V16's PASS CONDITION.** V16 fails *"if the check does not state its false-positive rate against a measured corpus."* The check states one and it is honestly measured -- **on held-out SENTENCES**. Its entire live cost is a property of **SURFACE CLASSES**, and a sentence-level precision figure is structurally incapable of expressing it. The disclosure is true and the reader is still misled. **A SECOND, SEPARABLE HALF:** V16's face makes the precision and reach disclosures a pass requirement of *the check it names*. Since `847b4492` the rung's criterion is served by TWO checks, and `check_rank_claim_values` -- which now owns the entire denominator predicate D151 found missing -- states **no false-positive rate and names no held-out reach set** in its verdict, its BASIS or its build commit. **The criterion was written when one check did the work, the work was split in two, and the requirement stayed with the name.** | Ladder V, V16 grade round 11, 2026-08-15, frame `9dca4738`, non-author. The verdict was RUN (171s and 258s in two independent invocations, same 29) and the per-file tally taken from `Result.detail`, not from a grep. The `check_rank_claim_values` frame string was read in full at `scripts/self_audit.py:2050-2073`. | **Two asks, different sizes.** (1) Cheap: give rule A a surface-class exclusion of the same shape the sibling already carries -- at minimum `sdk/tests/`, and the question of whether a check may fault its OWN source is worth one sentence of policy rather than a regex. Print the excluded count, as the sibling does. **This is not tuning: the sibling's own comment says it is a definition, and the two halves of one rung currently hold different definitions.** (2) Structural: a precision figure measured over sentences cannot price a cost that lives in surface classes, so V16's pass requirement should read *'states its false-positive rate AND the composition of its live faults'* -- the second is free, it is a `Counter` over the detail list the check already builds. **And the rung's requirement should attach to the FUNCTION, not to the function's name at the time of writing**, or the next split walks out of it the same way. | V16's owner (both); chief (the requirement's wording, since it is the rung's face) |
| D193 | **A dated settlement claim on V16's own face took its date from the commit it GRADED rather than from the act of settling, and it is wrong by a day on two surfaces.** `demo-output/website/campaign/LADDER_V_TRIPLE_VERIFICATION.md:690` and the D48 row at `docs/DOCKET.md:241` both read **"[SETTLED 2026-08-11 by V16 round 10, subject `067caac0`"**. Executed with `git log -S` over `scripts/self_audit.py`, every artefact of that settlement first appears at **`0c7b968d`, 2026-08-12T16:13:12Z**: `_board_pin_date` (earliest `0c7b968d`), the BLIND TO item text `WHETHER ITS OWN BOARD IS STILL CURRENT` (only `0c7b968d`), and `test_the_verdict_admits_it_cannot_tell_its_board_is_stale` (only `0c7b968d`). **Nothing of it exists at `067caac0`** (2026-08-11T22:50:51Z). The ledger row at `:816` is internally consistent -- it names `067caac0` as round 9 and `0c7b968d` as round 10 -- so it dates the settlement correctly by implication while `:690` and D48 do not. **THE DEFECT IS NOT STALENESS AND THAT IS WHY IT IS A ROW.** The date is not old; it is **borrowed**. Same family as D171's *"for three days"* (wrong by a factor of about 1,440) -- a date correctly copied off the wrong event -- and the lab has now produced it twice inside one rung's records. **Consequence:** `:816` uses the settlement to argue *"'self-graded' has been false since 2026-08-11"*, which on the corrected date is off by one day **in the lab's own favour**. | Ladder V, V16 grade round 11, 2026-08-15, frame `9dca4738`, non-author; three independent `git log -S` probes, each naming the symbol it searched for. | Repoint both surfaces to **2026-08-12** (strike-and-keep, since the wrong date is on the record). **The durable half extends D171's ask by one step:** D171 asks that a quotation carrying a commit resolve AT that commit; add that **a claim carrying both a date and a commit must have the date agree with the commit's own timestamp**. Both halves are `git show`-checkable, both are one regex over campaign records plus a `git log -1 --format=%cI`, and between them they would have caught D171's item (1), this row, and the `:690`/`:816` disagreement. | V16's owner (the two surfaces); fleet (the date-agrees-with-commit check, which is the durable half) |
| D194 | **D174's own denominator counted 34 checks where the tree holds 35, and the one it omitted is the check D174's cited repair added -- so the census that graded the form/value class measured the tree BEFORE its own repair.** `len(self_audit.CHECKS)` by import is **34** at `82fb3d46` and **35** at `847b4492`, `337dd62d`, `f2fa9cee` and `e98e40fe`. The single-line diff between the two `CHECKS` tuples is **`check_rank_claim_values`** -- a VALUE grader, and precisely the repair D174 cites in its own headline (*"repairing the one that shipped leaves 14"*). D174 declares its tree as `82fb3d46`+`847b4492` and reports 34, which is the count at the first of those and not the second. **The B-count of 15 reproduces independently** and its fourteen named members hold on reading; membership takes two swaps (`check_fd_grades_current_standard` OUT -- it recomputes a grade word from a rule-derived ground truth, which is a value comparison, though the counter-argument that its input is the claim itself is legitimate and this is the one genuinely contestable cell; `check_declined_ladders_name_their_guard` IN -- its first arm is bare presence and its own BASIS says it *"asks only that the record agrees with itself; it never refits"*). **A SECOND, INDEPENDENT RESULT ON THE SAME ROW:** D174 files **nine** checks that PASS on an empty SELECTION. Re-derived by execution in throwaway repositories where the source exists, is non-empty, and the selector matches zero items, the answer is **eighteen**, every one recorded with the verdict string it printed; six are genuinely guarded (`ledger_integrity`, `ledger_stalls`, `bundle_drift`, `statistical_labels`, `fd_grades_current_standard`, and `withdrawn_numbers` by construction). The likely cause of nine is collapsing: D174 lists the selector VARIABLES (`checked == 0`, `branches == 0`, `total == 0`, `not rung`, `not groups`) rather than the checks, and `checked == 0` alone covers seven. **This corroborates, by a different route and a different agent, the eighteen that the in-flight `_no_selection` work states in its own D175 comment** -- two agents arriving at the same 18 from opposite directions. **And one live item:** `check_cost_predictions` returns `INFO` unconditionally and can never fault, so counting it among the value graders overstates the graded set by one. | Ladder V, V16 grade round 11 supporting cell, 2026-08-15; enumeration by import at four revisions, classification by reading all 35 predicates, and the empty-selection result by EXECUTION with `__pycache__` purged before every import. **Filed and not appended to V16** -- it is outside the rung's declared scope (`0c7b968d..9dca4738`, the placement guard and its surfaces) under R-CONVERGE. **Cannot see:** `sdk/`, which D174's own 'cannot see' also concedes was never measured; and the three rank checks' empty-selection guards rest on reading rather than a clean probe, because `self_audit.py` sits inside its own corpus and contaminated the sandbox. | Correct D174's denominator to 35 and name the omitted check, since the omission is exactly the repair the row is about. Correct nine to eighteen, or say which nine were meant. **The durable half is a rule about census frames: a census that cites a repair must be measured at or after that repair's commit**, which is mechanically checkable -- the row declares a frame and the cited commit must be an ancestor of it. `git merge-base --is-ancestor` is the whole test, and it would have caught this at write time. | D174's author |
| D187 | **S6's reach is ONE template, not the six paths the standard claims, and the reason is that the template three of them stage declares no `residualControl` at all.** `MONITOR_STANDARD.md:1028` reads *"fires on the six paths that call `stage_case`"*. Enumerated and then **executed** 2026-08-15: the six sites are `head_engineer.py:1792`, `sdk/scripts/run_uq_studies.py:192`, `sdk/workflows/geometry_study.py:1819`, `sdk/workflows/nasa_hump.py:575`, `sdk/workflows/crm_wingbody.py:188` and `sdk/workflows/onera_m6.py:425`. **Two of the six are not HeadEngineer at all** -- crm_wingbody and onera_m6 build `DockerDAFoamEngineer` (`docker_dafoam.py:92`, a standalone class with its own `stage_case()` at `:133`), which contains **zero** `LogMonitor` references and therefore has no S6, no S8 and no monitor of any kind. Of the four that remain, **three stage the stock OpenFOAM `motorBike` tutorial**, and that template **declares no `residualControl` block** -- measured against the archived copy at `certonomous-runs/validation-scratch/motorBike/system/fvSolution`: `grep -c residualControl` returns **0**, its `SIMPLE` block holds only `nNonOrthogonalCorrectors` and `consistent`, and the production gate run over that exact text returns `_residual_controls` **`{}`** and reachability **`{reachable:{}, excluded:{}, unresolved:{}}`**, i.e. **S6 cannot arm**. The one path that does arm it is `nasa_hump.py:575` on `dafoam/f6a_nasa_hump/case_template`, which resolves **four fields** -- `U`, `p`, `k` at 5e-07 and `omega` at 1e-10 -- **through the regex key `"(U|p|k)"`**, so the `b4144116` repair has exactly one live production consumer and that consumer would have been silently disarmed without it. **The six-path sentence is true about how many places CALL the arming method and false about where the gate ARMS**, which is the same call-site-versus-effect gap that let a false S6 coverage sentence stand for six hours in this very document. | Verifying the S6/S8 monitor line by execution rather than by task list, 2026-08-15, anchored at `39c0e2bd`. The gate was run over the real staged templates, not reasoned about. **Cannot see:** whether any workflow rewrites `system/fvSolution` after staging and before solving -- staging-time text is what was measured, and a post-staging injection would change the answer. | **Correct `:1028` to say what arms rather than what calls**, and record that two of the six are a different class with no monitor at all -- a reader budgeting monitor coverage across the acts is currently told six and has one. **Then the actionable half: give the three motorBike paths a `residualControl` block, or accept that the geometry studies and the UQ studies -- the bulk of what HeadEngineer runs -- have no S6.** This is also the reason S6's PRIMARY pre-registered prediction cannot be scored as written (see the compute note filed with this row): the post-wiring fire rate on HeadEngineer runs is not 48%, it is **0/0 undefined on three of four paths and single-template on the fourth**, so the number the pre-registration fixed as its primary bar has no sample that could produce it. `MONITOR_STANDARD.md` is a standing record (H6) and the edit needs its owner. | chief (the standard); infra family (the templates) |
| D195 | **The declared FORM-or-VALUE column is in, and a VALUE declaration now proves itself by execution: 18 of 36 checks grade whether a claim is true, 16 grade shape over a claim whose failure mode is value, and 21 of 21 named records MOVE the verdict when taken away.** D174 proposed one dict column. It is `GRADES`, name -> (kind, evidence), enforced by a new check `check_every_value_claim_names_its_source`, and the enforcement is the point: D174's own finding is that `check_every_check_states_its_basis` tests a BASIS string is NON-EMPTY and never that it is TRUE, so a column nothing verifies would have BEEN the defect. Four rules, of which the last is executed rather than read. NAMED: a VALUE claim names the machine record it grades against, and a FORM-OVER-VALUE claim names the well-formed falsehood that passes it -- the obligation is symmetric so the form column is not the cheap place to sit. CONSISTENT: a VALUE claim whose own `blind_to` disclaims value fails, and so does a check declaring basis EVIDENCE while grading form. PRESENT: a named record absent from this box is UNPROVEN, not false. **CONSTRAINS -- THE PROBE, and it is what typing VALUE costs.** Each named record is made unreadable IN PROCESS by `_Blindfold` and the check is RUN AGAIN; its verdict must move. A verdict identical with its evidence taken away was not derived from it. **THE POSITIVE CONTROL IS THE LIVE INSTANCE D174 FILED**, caught in both directions on its own text: `check_studies_carry_what_the_fit_records` declared `EVIDENCE` -- *re-derives a published number from a primary artifact* -- two lines above a `blind_to` reading *every VALUE. It compares key sets only*. Corrected to `PROPERTY`, and **the correction makes the report WORSE**: the EVIDENCE tally on the verdict line drops from 14 to 13, one fewer check in this lab that re-derives a published number than the report has been claiming. **THE PROBE REJECTED THIS TABLE'S OWN FIRST DRAFT** -- `check_board_placement_words` was declared against the in-repo submission README and blinding it moved nothing, because `_parse_published_board` reads the BENCHMARK CLONE. **AND D176 GETS ITS OWN COLUMN rather than being folded in**: `VALUE-ON-A-PIN`, for a check grading truth against a deliberately FROZEN referent that is not what the claims are about, where the error runs both ways and 14 correct statements were faulted with three naming the WRONG ENTRANT. The grade rides on the VERDICT LINE and not only in the dict, because `check_rank_claim_surfaces` was declared `PROPERTY` on the day it cleared a false rank claim on a shipped page and the label stopped nothing. | Census re-derived INDEPENDENTLY, by reading all 35 check bodies at HEAD rather than by trusting D174: 18 A, 15 B, 2 C at HEAD -- which is D174's 17/15/2 at `82fb3d46` plus `check_rank_claim_values`, the one `847b4492` added, and `len(CHECKS)` is 34 at `82fb3d46` and 35 at `847b4492` by import. With this row's own check that is 36, declared 17 VALUE + 1 VALUE-ON-A-PIN + 16 FORM-OVER-VALUE + 2 FORM. The probe is executed every run and reports `21 of 21 (check, record) pair(s) moved their verdict`; total audit runtime went 2m48s to 2m53s, because a compliant check SHORT-CIRCUITS the moment its record is gone and the unblinded half is this process's own already-computed result. **TWO PROBE GAPS FOUND BY THE PROBE FAILING HONESTLY AND BOTH CLOSED**: Python's import machinery reads through `_io.open_code`, not `builtins.open` or `Path.read_text`, so five module-valued declarations read as unconstrained by records they cannot run without; and evicting `sys.modules` is not enough, because a submodule stays BOUND on its package and `from chief_engineer import uq` then imports nothing at all. The blindfold also hides `__pycache__/<stem>.*`, since `SourceFileLoader` stats the source through `os.stat` -- which no hook touches -- and reads the bytecode, which does not live under the source path. Mutation-proved in `scripts/mutation_harness_form_or_value.py`: control green and **all 28 mutants reddened their aimed test**, control and mutant in ONE invocation, `__pycache__` purged before every cell, each mutant run against a MIRROR of symlinks so the shared tree is never edited. 39 tests and 63 subtests in `sdk/tests/test_form_or_value_and_empty_selection.py`. **CANNOT SEE:** whether reading the named record ANSWERS the claim -- the probe proves the record is read and that the verdict depends on it, never that the check asks it the right question; a record present and WRONG, since blinding a corrupt source moves a verdict exactly as blinding a good one does; and the four kinds themselves, the census being a human classification of what each predicate establishes. | **Reclassification was not taken as the repair and the counts print on a PASS.** Relabelling the sixteen form-graders would have made the census line shorter and changed nothing about what they see, so the verdict line carries `18 of 36 grade whether a claim is TRUE ... 16 grade SHAPE over a claim whose failure mode is VALUE ... 0 claim VALUE without naming a source record` on every run including a green one. **The new check does not exempt itself**: it declares FORM-OVER-VALUE and names what gets through it -- a check that names a real record, is genuinely constrained by it, and grades the WRONG PROPERTY of it. **OPEN, and owned elsewhere:** the seven form-graders outside `self_audit.py` that D174 ranked (`check_absolutes.py::classify_unit` at 17/17, 16/16 and 19/19 BACKED; `gate_table.py` taking each act's verdict from that act's self-written deviation string, which launders `check_gate_table_vs_transcripts` -- that check's VALUE declaration is PROVED here, since blinding `gate_table.py` moves its verdict, and that is precisely the limit the probe declares: the record is read and the record is a self-report). Also not measured: the same classification over `sdk/` test guards and `sdk/chief_engineer/`. No solver ran; no scoring call, ledger stands at 6; nothing sent, uploaded, filed or registered. | fleet (done: `scripts/self_audit.py`, `scripts/mutation_harness_form_or_value.py`, `sdk/tests/test_form_or_value_and_empty_selection.py`); owner of `check_absolutes.py`, `gate_table.py`, `cost_calibration.py`, `withdrawal_sweep.py` for the outside guards |
| D196 | **The second residual is EIGHTEEN checks, not nine, and all eighteen are repaired.** D174 filed nine checks returning PASS when the source is present and the selector matches nothing, naming six selector spellings. Measured by execution with a REAL, NON-EMPTY source in every case -- one real `.py` that no source-shape selector matches, one real stored study that every scope rule declines, one real record citing nothing, one real docket holding a non-rung proposal -- the answer is **18 distinct checks**. The eleven D174 did not name are the five sharing `_py_corpus` and the six sharing `_stored_studies`: those helpers guard an EMPTY CORPUS, which is a different fact from a corpus that is present and fails to select, and all eleven said *all 0 ...* and PASSed. **THE TWO CASES NOW SAY DIFFERENT THINGS, because they have different remedies.** `_no_evidence` names its sources as `not read:` and closes with *an empty sweep is not agreement*; the new `_no_selection` names them as `read, and not empty:`, prints the selector that matched nothing, and closes with *the source was FINE and this check's own selector picked nothing out of it -- read the SELECTOR*. Collapsing them into one sentence hands the reader the wrong repair half the time: an absent source is fixed by putting the source back, a present corpus that yields nothing is either a corpus with no instance in it or a pattern that has stopped matching, and the verdict cannot tell those apart. **The wall is a FAIL and not an UNKNOWN**, per D174's own ruling and the SUBJECT versus EVIDENCE rule this file already draws: `wall.json` repointed at a nonexistent path now FAILs, matching its sibling `check_wall_counters_vs_ledger`, while the wall present with an empty closure block is the UNKNOWN, and the round-5 entry of record -- which is EVIDENCE -- moves from WARN to UNKNOWN. **`check_ungated_completed_runs` had no denominator at all**: it grepped one literal and, not finding it, said *no completed run is missing its gate verdict* having identified no run; it now counts the completed rungs its record names. | Measured 2026-08-15 by execution, twice: a first pass that emptied the shared corpora outright, and then the honest pass reported here, which hands every check a real non-empty source that simply does not select -- so the 18 is not inflated by re-testing source absence. **MUST-NOT-MATCH (L-84), and it is the load-bearing control**: on the live tree not one of the eighteen returns UNKNOWN, every one reaches the verdict it reached before, and a per-check diff of the full audit before and after shows **no check changed status**. Status tally before: PASS 18, WARN 10, FAIL 4, INFO 3, UNKNOWN 0 over 35 checks. After: PASS 19, WARN 10, FAIL 4, INFO 3, UNKNOWN 0 over 36. **The aggregate did not move toward green**: FAIL, WARN and INFO are unchanged and the single extra PASS is the new meta-check itself; no FAIL became a PASS on this corpus. The other failure mode is equally excluded -- zero UNKNOWNs before and zero after, so false greens were not replaced by a useless instrument. Mutation-proved: 18 mutants restore the exact empty-selection PASS sentence each check used to print and a nineteenth drops the wall's SUBJECT guard so the absent wall chains through `or {}` to the PASS D174 measured; every one reddened its aimed test, control green, one invocation, `__pycache__` purged per cell. Also kept, not undone: an unopenable archive is still a named blind spot and not a surface whose text is the empty string, and `_travelling_names` still does not downgrade a shipped FAIL to a lab-record WARN. | **Not every zero is an empty selection and the repair is scoped.** A check that enumerated a population and found no fault in it still PASSes: `check_ledger_stalls` reading rows and finding no stall, and `check_restated_thresholds` seeing five governed defaults all pinned, are genuine negatives and are asserted as such in the suite. What changed is only the branch where the DENOMINATOR is zero. Two denominators had to be invented rather than exposed, and that is stated rather than hidden: `check_restated_thresholds` counted only its compliant half, so its zero read as compliance and it now counts every governed parameter it saw; `check_ungated_completed_runs` had no population at all. **A test that accepted either verdict was settled, not relaxed**: `test_git_unavailable_is_reported_as_a_detector_that_is_off` was red at HEAD after `847b4492` moved git-unavailable from WARN to UNKNOWN. The expectation was stale, not the change -- WARN is not blocking, and `lab_check.py:229` records that self_audit *exits non-zero only on FAIL, so an OFF detector reddens nothing here* (D78). It now requires UNKNOWN and asserts the intent on the real text rather than on the word OFF. No solver ran; no scoring call, ledger stands at 6; nothing sent, uploaded, filed or registered. | fleet (done: `scripts/self_audit.py`, `sdk/tests/test_form_or_value_and_empty_selection.py`, `sdk/tests/test_rank_claim_surfaces.py`) |
| D197 | **V12's three residuals are repaired in the skeptic's briefing itself, and R1 was a live instruction to publish a withdrawn figure to a steward.** The V12/V13/V14 grade at `9c2734f8` raised R1/R2/R3 against `campaign/LADDER_V_PASS3_COLD_2026-08-11.md`, the document the send gate hands to Sanaa. **R1** at `:525-527` recommended putting *"P(rank 1) ~ 0.67"* into external text -- the FOUR-entry board's figure, struck 34 lines earlier at `:491`, and **17 points optimistic in the direction that flatters us**. Struck and kept; the restated recommendation names the live figure **with its interval and its board**: `50.2%`, `0-97% at 95%`, six entries retrieved **2026-08-11T23:33Z**, re-verified **2026-08-14T21:01Z**, and states in terms that **a commit anchor is not an admissible board identifier** -- `deb91557` scores and does not rank, and the board moved under it without the pin changing a character. The premise sentence at `:518` (*"I got 0.674 from the shipped CSVs"*) is struck with it. **R2** at `:559-562` said the declined-gate baseline beats *"all four published entries (... a best published 0.0760)"*; the direction survives and strengthens -- it beats **all six** -- but the second value is **Yang's 0.0748**, `0.0760` being Reissmann's, best on the four-entry board and second on the six. **R3**: two adjacent bullets stated one bound two ways. `:399-403` quotes `spreads.overall_equivalent_S_bound` = `0.002419121853891026` and derives 177%; `:404-407` printed adverse `0.059047` / favourable `0.054247`, which are `0.056647191704213645 +/- 0.0024`, the SCRIPT constant `SEED_BOUND_ON_OVERALL` at `sdk/scripts/probability_of_rank.py:64`. Corrected to **`0.059066` / `0.054228`**, struck pair kept, with the four-line arithmetic table showing which constant produces which figure. | **Every figure re-derived by execution, nothing copied from the grade or from the ledger row.** `LIVE_BOARD` read by `ast.literal_eval` from `sdk/scripts/probability_of_rank.py`, **never imported**: 6 entrants, `fetched` `2026-08-11T23:33Z`. Ours from `closure_challenge_round5_qcr.json` -> `round5_per_case_full`; overall `0.056647191704213645`, equal to the mean of the eight per-case values. Yang = mean of his eight published values = `0.0580125`; margin `0.001365308295786356` -> **`0.001365`**. Seed bound from `closure_challenge_seed_sensitivity.json` = `0.002419121853891026`; **177.185%** of the margin. `0.056647191704213645 + 0.002419121853891026 = 0.059066313558104669` and `- = 0.054228069850322622`; the script constant gives `0.059047` / `0.054247` **exactly**, which is how the struck pair was identified rather than guessed. Adverse sorts **2nd of 7** under BOTH figures, so the conclusion does not move. P(rank 1) by a 400,000-draw case-level bootstrap written for this repair with its own seed: **200,924 / 400,000 = 0.5023**; double bootstrap 2,000 outer x 4,000 inner: **[0.007, 0.979]**. Best published on `alpha_05_4071_4048` = `0.0569` (Wu & Zhang) and on `alpha_05_4071_2024` = **`0.0748` (Yang)**; ours `0.046108` and `0.071863` beat all six on both. `__pycache__` purged before every cell. | **ROUTE DEPENDENCE, stated because it is the weakness of the whole re-derivation.** Every route -- this repair's, the repair block's, the ledger row's, the grade's -- reads the board from **one `LIVE_BOARD` literal in one tracked file**. Their agreement is agreement about a transcription, **not corroboration of the board**. The only genuine input-side corroboration on the record is the separate second fetch of the rendered project page at `campaign/BOARD_MOVED_2026-08-11.md:22`, which independently returns the same six-row table. **No fetch was made for this repair** and nothing here is more current than that record. This pass is now an author of the repaired text and **may not grade it** under R-ISOLATE part 3. No solver ran; no scoring call, ledger stands at 6; nothing sent, uploaded, filed or registered; the pin `deb91557` was not moved. | fleet (done: `campaign/LADDER_V_PASS3_COLD_2026-08-11.md`); a non-author owes it a V15 claims-table pass |
| D198 | **V12's criterion contains no accuracy clause and no currency clause at all, so the standard its repairs were graded against was DERIVED and never quoted.** Read verbatim at `campaign/LADDER_V_TRIPLE_VERIFICATION.md:360-362`: *"V12. The cold agent writes the skeptic's report: the three weakest points of the entry as an outside reviewer would state them, each with the record's best answer beside it. This becomes Sanaa's briefing for any follow-up questions from the steward."* That is the whole criterion. It requires three weakest points with answers beside them. **It says nothing about whether the figures must be true, or true NOW.** The standard the 2026-08-15 repair applies -- *"a red-team brief that hands the skeptic a weaker version of the true case is a defect in the rung's own instrument"* -- and the currency standard the V12/V13/V14 grade adopts are both derivations from *"as an outside reviewer would state them"* being present tense. They are derivable and this pass adopts them too. **But a grader reading only the criterion could pass V12 on a briefing whose every number was four days stale, and R1 shows what that costs**: the rung's one actionable recommendation instructed a steward-facing surface to publish a 17-point-optimistic withdrawn figure, and nothing in the criterion as written forbids it. | Criterion extracted mechanically from the three source lines and tested for the vocabulary: `true`, `accur*`, `current`, `now`, `live`, `board`, `stale`, `up to date`, `correct`, `fact`, `date` -- **all eleven ABSENT**. The V12/V13/V14 grade records the same ambiguity at its section 2.1 and says it grades against *"the criterion as written PLUS the derived currency standard"*; two other graders hit criterion ambiguity on their own rungs the same week. | **NO CLAUSE IS PROPOSED HERE AND NONE IS INVENTED.** A rung criterion is Katie's. This row records only that the standard in force is unwritten, so that whoever rules knows they are choosing text rather than restating it. What would settle it: Katie either writes an accuracy-and-currency clause into V12, or rules that V12 is a structural rung and that accuracy belongs to V15's claims table -- in which case the R1 class must be routed there explicitly rather than left to derivation. No solver ran; no scoring call, ledger stands at 6. | Katie (the criterion); chief (routing, if it goes to V15) |
| D199 | **Rule O measured, not installed: a denominator rule catches 5 of V14's 8 shipped claims plus a ninth nobody had, at 14.3% false positives on travelling surfaces and at least 92.5% on the lab record.** V14's Rules S, B and R derive **score literals**; ordinals and counts about ourselves (`2nd of 5`, `3rd of 5`, `5 of 8 -> 4 of 8`) belong to no family the criterion names -- D151's gap at criterion level. A candidate **Rule O** was BUILT AS A MEASUREMENT (`campaign/RULE_O_DENOMINATOR_MEASUREMENT_2026-08-15.py`, not wired into `self_audit.py`, no caller) and run over V14's declared frame: **O1** ordinal placements `<n>[st|nd|rd|th] of <m>` with the word `rank` OPTIONAL, **O2** counts out of eight, both graded against values derived at run time from the board's length and the per-case values. **THE SPLIT IS THE FINDING.** Shipping archive: 31 hits, **24 admitted, 7 faulted, 6 TRUE** -- **FP 14.3%**. Tracked PDFs: **12 faults, 5 true, all 5 in `latex/closure_challenge_report.pdf`** -- FP 58.3%. Tracked lab record: **393 faults**, and in a random 40 read by hand **ZERO are a live wrong claim** (4 correct dated history, 36 misreads) -- **FP >= 92.5% at 95% by rule-of-three**. Gitignored, naive configuration: **7,685 faults, 99.9% misread.** Run tree: **0 hits and 0 faults over all 132,049 files** in the fair configuration (the naive one returns exactly one, `115 of 120`, a finite-difference gradient index) -- V14's arm 4 is clean under Rule O as well, and extending the criterion would not change it. Untracked: **42 faults, 0 true, all of them this measurement's own two files quoting the defect in order to report it**. **What it catches:** `closure.html` `:341` `3rd of 5`, `:434` `2nd of 5`, `:435` `3rd of 5`, `:436` `3rd of 5`, `:502` `rank 1 of 5` -- **four of those five have never been named by any sweep**. **What it still misses:** `:342` `5 of 8 -> 4 of 8`, and the miss is STRUCTURAL -- best-on-board derives to 2, model-earned to 0, and pairwise cases-won to (1, 3, 4, 5, 7), so the admissible union is seven of the nine integers a count-of-eight can take; `4 of 8` is simultaneously the WITHDRAWN best-on-board count and the CORRECT cases-won count against Yang, and **no arithmetic over the board separates them**. O2's contribution to the catch on this archive is **zero**; the entire catch comes from O1. `:411` and `benchmarks.html:148-149` are score literals already inside Rule R -- their survival is an EXECUTION gap no criterion change repairs. | Frame re-measured today: tracked **20,702** (18,975 decoded), untracked **1**, gitignored **37,242** (35,723 decoded), run tree **132,049** by a SINGLE `/usr/bin/find -print0` enumeration counted by NUL bytes -- **not banded**, so the `-size -2M`/`+2M` non-complementarity that hid 4,418 files cannot apply, and the total equals the grade's independently obtained 132,049. Archive `zipfile`-decoded, 90 members / 78 decoded. `LIVE_BOARD` by `ast`, never imported. Derived: 6 entrants, our rank 1 of 7, per-case ranks of 7 = 2,2,1,1,3,4,4,7, best-on-board 2 of 8, model-earned 0 of 8. `/usr/bin/grep` GNU 3.11 and `/usr/bin/find` GNU 4.9.0 (both shell names are functions wrapping `ugrep`/`bfs`). **Every LIVE/HISTORY/MISREAD label is a HAND reading**: the script's own classifier disagreed with the hand reading on 5 of 7 archive faults and is used for no number. **TWO DEFECTS FOUND IN RULE O BY MEASUREMENT**: `duct` written without a word boundary matched inside `product`, faulting `43 of 60 sampled diagonal indices` in DAFoam adjoint work -- the V14 grade's own `68%`-matches-`1.68%` class, reproduced inside the instrument built to measure it; and O1 swallowed O2's spans, reporting the CORRECT `2 of 8` as a wrong denominator. Both fixed and recorded. **A THIRD IS NOT FIXED**: the frozen-pin decline makes Rule O MISS the `rank 1 of 5` on page 1 of the report PDF, because `deb91557` sits eleven characters away -- a false negative that appears in no false-positive rate. | **THIS ROW DOES NOT RULE AND CHANGES NO CRITERION.** What the measurement supports: a denominator predicate scoped to surfaces that TRAVEL. What it does not support: a repo-wide one -- 393 faults finding nothing is worse than the 74%-FP checker this lab already carries, it would fault the labelled test corpus that exists to hold withdrawn text and the guards' own explanatory comments, and an ignored gate is a fail-open gate with a green light on it. The strike mask was measured and moves the tracked arm 616 -> 393, which is not the difference between unusable and usable. **The tension the ruling has to resolve** is that V14's criterion demands repo-wide reach and the measurement says repo-wide reach is where the rule stops working. **THE EXISTING INSTRUMENT MOSTLY SUFFICES AND WAS NOT REBUILT**: `check_rank_claim_values` (`847b4492`) already carries the right arithmetic, a D151 third-party arm, a three-valued verdict and it opens the archive. It misses this class for one small reason -- `_VALUE_BOARD_SIZE` requires the literal token `rank`, so `2nd of 5` is outside its REGEX and not its arithmetic. **A one-line widening of one pattern is the highest-value change the measurement identifies**; a second checker is not. Its 4 MB / UTF-8-only frame also excludes the compiled PDFs where 5 live claims sit, which its own docstring already declares. No solver ran; no scoring call, ledger stands at 6; `dist/` and `demo-output/website/latex/` read and never written. | chief (the ruling); Katie (V14's criterion); owner of `scripts/self_audit.py` (the one-line regex widening, if ruled) |
| D200 | **A NINTH live four-entry claim in the tracked shipping archive, found by Rule O and not on the V14 grade's list of eight -- and it is wrong by one in the direction that understates our own correction.** `dist/certonomous-demo.zip!certonomous-demo/site/closure.html:426` reads *"the table below compares against the FOUR-entry board and has not been re-derived against the six; against the six, the 'best published' column would be lower on **six of the eight rows**"*. Re-derived cell by cell -- four-entry best (Reissmann, Wu & Zhang, Liu, Montoya) against six-entry best on all eight cases -- the answer is **SEVEN of the eight rows**; only `alpha_05_4071_4048` is unchanged, its `0.0569` still Wu & Zhang's. **The tracked source has already been corrected** and says so in terms at `demo-output/website/closure.html:482-488` (*"its count was wrong: it is seven of the eight rows, not six"*), so this is the same staleness as V14-1: **the zip is six files behind its sources and is the only copy that leaves the box.** V14-1's finding is therefore **nine, not eight**, and the ninth is again a count-of-N -- outside every literal family V14's criterion names, and outside the ordinal arm that caught the other four. | Per-case comparison, `LIVE_BOARD` by `ast` and never imported: `alpha_15_13929_4048` 0.0592 -> 0.0432 lower; `alpha_15_13929_2024` 0.1195 -> 0.0998 lower; `alpha_05_4071_4048` 0.0569 -> 0.0569 **unchanged**; `alpha_05_4071_2024` 0.0760 -> 0.0748 lower; `AR_1_Ret_360` 0.0387 -> 0.0291 lower; `AR_3_Ret_360` 0.0341 -> 0.0311 lower; `AR_14_Ret_180` 0.0325 -> 0.0250 lower; `NASA_2DWMH` 0.0364 -> 0.0294 lower. **7 of 8.** The four-entry membership is read from `campaign/BOARD_MOVED_2026-08-11.md`, which records that Yang and Tian/Buchanan/Hickel/Dwight are the two that appeared on 2026-08-11. Archive opened with `zipfile`, never byte-grepped. | **REPORTED, NOT REPAIRED.** `dist/` has a designated owner and this pass measures rather than ships. What would settle it: rebuild the bundle from current sources, which closes this and the other eight together -- `check_bundle_drift` already returns FAIL naming six stale members. **Not cited as evidence:** `check_rank_claim_surfaces` does open the archive, returns WARN, and names none of the nine; this row's nine were derived without it. No solver ran; no scoring call, ledger stands at 6; nothing sent, uploaded, filed or registered. | owner of `dist/certonomous-demo.zip` and the public pages |
| D206 | **The chief's one-line widening is landed and measured -- and its own premise, that `_VALUE_BOARD_SIZE` misses this class "for exactly one reason", is REFUTED BY EXECUTION: there is a second gate, and it blocks three of the four claims the widening was bought to catch.** The ruling at `9b9951a1` names one cause (the pattern requires the literal token `rank`) and it is real: `_VALUE_BOARD_SIZE` in `scripts/self_audit.py` now admits `<ordinal> of <N>` and the shipped `3rd of 5` at `dist/certonomous-demo.zip!certonomous-demo/site/closure.html:341` FAULTS for the first time. **But `:434`, `:435` and `:436` -- the other three bare ordinals in the same member, all confirmed-true -- still do not fault, and the pattern is not why.** They are dropped by `_in_board_context`, the gate `check_rank_claim_values` SHARES with the form guard, which requires a `_RANK_BOARD` word within `_RANK_WINDOW = 300` characters. **Measured on the member: the nearest board word is 1,224, 1,448 and 1,319 characters away.** Those three sit in an HTML table where one `<tr>` is ~220 bytes of markup around ~60 bytes of prose, and the window was calibrated on markdown. **NOT WIDENED HERE, deliberately:** `_in_board_context` feeds `check_rank_claim_surfaces` as well, a 5x window expansion to make a control pass is the tuning this ruling forbids, and the same gate is what silently drops `closure.html:411` (see D208). **Cost of the widening, both arms, measured on the same tree before and after:** value faults **24 -> 26**; travelling (GATED) **2 -> 3**; non-travelling (REPORTED, not gated -- `check_rank_claim_values` already returns FAIL only on travelling surfaces and WARN on lab records, so the ruling's scoping needed no code change) **22 -> 23**; passages matched and NOT graded, each named with its reason, **95 -> 117**. **BOTH NEW FAULTS ARE TRUE. False-positive rate of the widening: 0 of 2.** The measurement that seeded the ruling predicted >=92.5% on the tracked arm for a repo-wide denominator rule; this one adds ONE tracked fault and it is a defect -- `demo-output/website/CLOSURE_CHALLENGE_SUBMISSION_DRAFT.md:1263` *"at round 3 it was last on `AR_1_Ret_360` only, **4th of 5** on `AR_3_Ret_360`"*, an ordinal out of five dated to one of OUR rounds and not to a board by entrant count and retrieval date, which the chief ruling at `377d6afb` makes a defect rather than history. **AND A LATENT DEFECT IN THE SAME PATTERN WAS FIXED IN THE SAME CHANGE, BECAUSE THE WIDENING WOULD HAVE MADE IT LIVE.** V16 round 11 (`94419cc8`, D189-D194) found `_VALUE_BOARD_SIZE` matching **`rank 2 of 0`** out of `demo-output/website/latex/closure_challenge_report.tex:1890` -- *"a gap to rank 2 of 0.0030"* -- because the word boundary after `\d{1,3}` falls BETWEEN the `0` and the decimal point: **the pattern read a MARGIN as a BOARD SIZE.** Reproduced here by execution before touching it, on the real file, and confirmed **one gate deep** -- the only thing holding it is `_in_board_context` returning False, and NOT the strike masker, which leaves that passage byte-identical. The word "board" anywhere in that window, entirely natural in a document about a leaderboard, and a correct sentence on a TRAVELLING document faults. **The bare-ordinal branch makes that surface bigger**, measured: before the fix the widened pattern also matched `2nd of 0.0030` and `3rd of 0.0592`, and bare ordinals are commoner than `rank N of M` in exactly the prose where margins are discussed. Fixed by `(?![.,]\d)` -- a denominator may end a sentence (`of 7.`) or a clause (`of 7,`) but may not be the integer part of a decimal -- and controlled on that exact `.tex` sentence in both branches, including the version with `board` added to the window. **Not fixed and reported instead:** `_BEST_COUNT` carries the same `\b`-after-digits shape on `(?:eight|8)\b`, so `of 8.5` would match; no instance exists in this corpus and it was left rather than changed, because it is a different rule and this pass was scoped to one. **The reason the noise did not arrive is a design choice that is itself the answer to the ruling's stated reversal condition: ONLY THE DENOMINATOR IS GRADED ON THE BARE BRANCH.** `rank N of M` names an overall placement, so both digits are graded; a bare `<n>th of <m>` does not say WHICH placement it is -- `4th of 7` is the CORRECT live per-case ordinal for `AR_3_Ret_360` and the wrong overall one -- so grading its numerator against our overall sort would fault a correct travelling claim, which is exactly what the ruling says would reverse it. 22 such correct live ordinals (`1st`, `2nd`, `3rd`, `4th`, `5th`, `7th` of 7) were reached and every one was admitted with its numerator returned ungraded and its reason printed. **Zero faulted.** | **Executed, not reasoned, and every cell purged of `__pycache__` first.** Developed and measured in a `git worktree` at `9b9951a1` because `scripts/self_audit.py` was under a concurrent mutation census (see the note in this row's remedy). Baseline and widened runs are the SAME corpus: frame unchanged at 20,708 tracked paths considered, 20,521 opened (90 archive members), 1,422 decoded as UTF-8 mentioning `rank` or `overall`, 498 rank claims. **MUTATION-PROVED, control and both mutants in ONE invocation with `__pycache__` purged before each cell and a sha256 restore assertion:** control GREEN (17 tests, 28 subtests); **M1** `_VALUE_BOARD_SIZE` reverted to its HEAD text -> **RED, 19 failures**; **M2** `_VALUE_TRIGGER` reverted -> **RED, exactly one failure**, the test asserting the cheap prefilter stays a superset of the rule; **M3** the `(?![.,]\d)` decimal lookahead removed -> **RED**, the margin-is-not-a-denominator control; restored -> GREEN at sha256 `8a094349...`, byte-identical to the control. **M2 is not decoration:** `_VALUE_TRIGGER` runs FIRST and a surface it skips is never masked and never graded, so a widened rule behind an un-widened trigger is inert on exactly the files it was widened for -- and the shipping archive would NOT have shown it, because that member reaches the gate on its `rank 1 of 5` at line 502 anyway. **BLINDFOLD (the rule shipped at `fb30e00f`), and it caught a defect in this widening before it landed:** repointing `_PROB_SCRIPT` at a non-existent path and clearing every `lru_cache` in the chain, the new `3rd of 5` fault **SURVIVED** -- with the board unreadable `facts["entries"]` is 0, `admissible_m` becomes {0, 1}, and the arm manufactured faults out of the ABSENCE of its own evidence. The `rank` branch never had the hole because it grades its numerator through `_live_ranks()`, which returns {} and declines; the bare branch reads only the board's length and needed its own decline. Added, and the blindfold now passes: faults gone, whole check UNKNOWN, board readable again on the way out. **WORD BOUNDARIES, PROVED NOT READ** -- the measurement behind the ruling found `duct` matching inside `product` in its own instrument, the `68%`-inside-`1.68%` class: `\b(?P<o>\d{1,3})(?:st|nd|rd|th)\b` takes `21st` whole out of `21st of 50` (asserted `group("o") == "21"`, not `"1"`) and does not fire in `v3rd`, `x21st`, `3rdx`, `1sts` or `rev2nd`. **AND IT CANNOT SWALLOW RULE V2's SPANS** -- the same measurement found its ordinal family eating its count family and reporting the CORRECT `2 of 8` as a wrong denominator; the ordinal suffix is what keeps them disjoint here, asserted on `4 of 8`, `2 of 8`, `5 of 8` and `6 of the eight rows` in one direction and on `_BEST_COUNT` still holding `4 of 8` in the other. **A THIRD CHANGE THE CONTROL RUN FORCED, found by execution and not by review:** `_VALUE_TO_NEW` required the right operand of a `from X to Y` correction to be a bare cardinal, so `\d{1,3}\b` could not end inside `3rd` and *"the duct falls from 2nd of 5 to 3rd of 7"* was not recognised as a correction at all -- the sentence ANNOUNCING a repair was faulted for announcing it, which is the exact defect that constant exists to prevent and the form this corpus's own repair notes are written in. Suffix admitted. **Ledger 6, no solver run, no scoring call, pin `deb91557` not moved.** | **LANDED** (this row) as one pattern widening plus the two changes its own controls forced, with `sdk/tests/test_rank_claim_values.py` carrying 14 new tests including both L-84 halves, the word-boundary proof, the span-disjointness proof, the blindfold and the trigger-superset assertion. **WHAT IS NOT LANDED AND WHY IT IS A GAP RATHER THAN A TODO:** `_in_board_context` at `_RANK_WINDOW = 300` is now the binding constraint on this class, not the regex. Widening it is a decision for the owner of BOTH rank checks, because the constant is shared with `check_rank_claim_surfaces`, and it must be bought with a measured false-positive count on the tracked arm -- the number this pass did not spend. **Transferable form, and it is the one worth keeping: a ruling that names ONE cause for a miss has usually measured the cause it could see. Re- measure the miss after the fix, on the same corpus, and count what still misses** -- this widening was ruled as catching four claims and catches one, and only executing it says so. | owner of both rank checks (the shared 300-character board-context window); chief (whether the ruling's "exactly one reason" is amended) |
| D207 | **The ninth claim's repair target does not exist on the surface the dispatch names: the tracked page was ALREADY repaired, and the only live copy of the wrong count is the one in `dist/` that nobody in this pass may touch.** The brief and D200 both point at *"`demo-output/website/closure.html:426` states the six-entry board lowers the best-published column on **six of the eight rows**"*. **Read at HEAD, `demo-output/website/closure.html:426` says nothing of the kind** -- it is a sentence about the margin over Yang and the per-case spread. The claim lives at **`:482`**, and it is *already struck, already dated and already corrected*: the sentence sits inside an HTML strike element and is followed by `-- struck 2026-08-15` (**the strike tags are described here and not written: this file already carries nine opening strike tags against three closing ones, so emitting one more closer blanks every claim between the nearest dangling opener and this row -- see D209, which this row's own first draft caused and the post-write sweep caught**), followed at `:488` by *"Its count was wrong: it is **seven** of the eight rows, not six"* and at `:490` by the board named the admissible way, **six entries retrieved 2026-08-11T23:33Z, re-verified 2026-08-14T21:01Z**. `426` is the line number **inside the archive member** `dist/certonomous-demo.zip!certonomous-demo/site/closure.html`, confirmed by opening the zip with `zipfile`. **So there is nothing to repair in a writable file, and the correct action was to verify the count rather than to write the repair a second time.** A tracked sweep for the live sentence returns it on no other surface: the seven other tracked hits are `campaign/` grades and docket rows QUOTING it in order to report it, plus `LADDER_V_V15_ROUND8.md` and `LADDER_V_V6_V10_REGRADE_2026-08-15.md` already saying **seven**. | **Re-derived cell by cell, twice, by two routes that share no rule**, because an off-by-one on exactly this class has now occurred three times on three different surfaces (D149, D160, D200). `LIVE_BOARD` read by `ast.literal_eval` off the assignment node of `sdk/scripts/probability_of_rank.py`, **never imported**; `fetched 2026-08-11T23:33Z`; six entrants. Case order from `closure_challenge_round5_qcr.json` -> `official_test_harness_result` -> `round5_per_case_full`. **ROUTE A -- numeric, and it never looks at a name:** the eight `Best published` cells parsed out of the STALE SHIPPED MEMBER itself (`0.0592, 0.1195, 0.0569, 0.0760, 0.0387, 0.0341, 0.0325, 0.0364`) against the live six-entry minimum per case (`0.0432, 0.0998, 0.0569, 0.0748, 0.0291, 0.0311, 0.0250, 0.0294`); strictly lower on **7**. **ROUTE B -- identity, and it never compares two boards' numbers:** who HOLDS the live minimum on each case (`Tian, Tian, Wu & Zhang, Yang, Yang, Yang, Yang, Tian`) and whether that holder is absent from the four-entry board -- absent on **7**. The four-entry membership for route B is taken from the qcr JSON's own dated `leaderboard_comparison_dated_2026_08_07.overall` keys (Reissmann, Wu & Zhang, Liu, Montoya), a different input from the `campaign/BOARD_MOVED_2026-08-11.md` that D200 used. **Both routes: 7. The sole survivor is `alpha_05_4071_4048`, 0.0569, still Wu & Zhang's on both boards.** The shipped sentence says six. **AND `:426` IS STILL MISSED BY THE INSTRUMENT AFTER THE WIDENING, for two reasons and neither is the `rank` token:** `_BEST_COUNT` requires `\bbest\b` within 80 characters **on the same line** (`[^.\n]{0,80}`) and the count sits on the next line; and the only nearby `best` is inside ``Best published` written with HTML left/right double-quote entities`, which the strike masker blanks as a quotation before any rule sees it. Confirmed by executing `_BEST_COUNT` over the masked member. | **VERIFIED, NOT REPAIRED, AND THE DISTINCTION IS THE POINT.** The count is seven and the tracked page already says seven; `dist/` has a designated owner and was read and never written. What settles the live copy is the bundle rebuild that closes this and the eight beside it in one move -- `check_bundle_drift` already returns FAIL naming the stale members. **Transferable form: a dispatch that carries a file:line for a repair is carrying a coordinate, and a coordinate from an archive member does not address the tracked source even when the file name is identical. Open the named line before writing the repair** -- this pass was one step from re-striking an already-struck sentence and dating the second strike today, which would have read as two independent corrections of one defect. No solver ran, no scoring call, ledger 6, nothing sent or registered. | owner of `dist/certonomous-demo.zip` (the rebuild); fleet (a dispatch coordinate that names the archive it came from) |
| D208 | **The two "execution gaps" are HALF confirmed and half refuted, and the half nobody named is a rule that ran, matched, decided nothing, and said nothing about it.** V14's Rule R is claimed to already derive the score literals at `closure.html:411` and `benchmarks.html:148-149`, making their survival an execution failure rather than a missing rule. **CONFIRMED for `0.0029`:** re-derived here from Rules S and B -- our `round5_overall_full` 0.056647191704213645 against `LIVE_BOARD` read by `ast` -- the margin over Reissmann, Fang & Sandberg is **0.0028778083**, which is `0.0029` at 4 dp, exactly as V14's Rule R bullet states; and the literal `0.0029` sits on both lines in the shipping archive. Rule R's own search set reaches them, so the sweep of record's silence is an execution gap. **REFUTED for `0.0024`:** it is `overall_equivalent_S_bound = 0.002419121853891026` in `demo-output/website/closure_challenge_seed_sensitivity.json`, and **no V14 rule names it.** Rule S is scoped to *"every numeric leaf under `official_test_harness_result`"* and that file has no such block; Rule R as V14 writes it names two margins plus `P(rank 1)`, its interval and the entrant count, and no bound. The measurement's own gloss of Rule R as *"the four derived margins **and bounds**"* is wider than the criterion it is glossing. **So each of those two sentences is half inside a declared family and half outside every one of them, and calling either sentence simply "already covered" overstates the criterion.** **AND THE THIRD THING, which is neither:** `check_rank_claim_values` RULE V3 DID run on both lines and DID match. Executed on the archive member: at `closure.html:411` `_VALUE_COMPARABLE` matched, `margin` and `bound` were both in the +/-160 window, and the passage was dropped by `_in_board_context` -- `_RANK_BOARD` has no token for *"the published leader"* and no board word sits within 300 characters. At `benchmarks.html:149` it is dropped twice over, by board context AND by `_is_rejection`. **None of those drops is reported.** The `continue` on the board-context gate produces neither a fault nor an `ungraded` entry, so a check whose docstring promises that *"every passage a rule matched and could not decide is returned with its reason"* violates that promise on exactly this class -- silently, in the register a reader would consult to find it. | Rule R re-derived by execution, `LIVE_BOARD` by `ast.literal_eval` and never imported, our overall from `closure_challenge_round5_qcr.json`; all six margins printed before the verdict (Yang 0.0013653083, Reissmann 0.0028778083, Wu & Zhang 0.0057653083, Tian 0.0074903083, Liu 0.0170403083, Montoya 0.0212153083). Archive opened with `zipfile`, never byte-grepped. Every gate of RULE V3 evaluated separately per occurrence on the MASKED text, so the answer is which gate declined and not that the rule was absent. **`__pycache__` purged before each cell.** **A FOURTH ITEM, stated because the brief asks for it plainly rather than for a rule tuned until it seems to cover it:** `closure.html:342`'s `5 of 8 -> 4 of 8` remains **structurally unreachable by any board arithmetic**, and this pass did not widen anything toward it. Derived from the board here, and **D199's own figure for this does not reproduce**: executed against `LIVE_BOARD` and `round5_per_case_full`, best-on-board is **2** (`alpha_05_4071_4048`, `alpha_05_4071_2024`, both organisers'-baseline passthroughs, so belonging to our model **0**) and the pairwise cases-won counts are **Yang 4, Reissmann 4, Wu & Zhang 5, Tian 5, Liu 7, Montoya 7 -- the set {4, 5, 7}**, NOT the **{1, 3, 4, 5, 7}** D199 records. `{1, 3}` are the cases-LOST counts (8-7 and 8-5), so that row's set appears to be won and lost unioned together. **The admissible union is therefore {0, 2, 4, 5, 7} -- FIVE of the nine integers a count-of-eight can take, not seven -- so the rule is tighter than the measurement claimed and THE CONCLUSION SURVIVES ANYWAY:** `4` is simultaneously the WITHDRAWN best-on-board count and the CORRECT cases-won count against Yang, the board leader, and against Reissmann; `5` is the correct cases-won count against Wu & Zhang and against Tian. Independent corroboration on the same page: `closure.html:424` already says *"we win four of the eight cases and lose four"* of Yang. **No rule separates them, because the surface does not say which quantity it means.** The widening landed in D206 cannot reach it either and was not stretched to: the ordinal branch requires a `st|nd|rd|th` suffix precisely so it stays disjoint from this family. **This one needs a human reading of the sentence.** | **REPORTED. Nothing repaired in `dist/`, which has a designated owner.** Three separable items, three different owners. (1) The V14 sweep's Rule R execution gap on `0.0029` -- the sweep's owner re-runs arm C over Rule R's literals and names every line, or records that it did not. (2) `0.0024` is outside every declared family; Katie owns whether V14's criterion gains a bound family or admits the gap. (3) **The silent drop is the cheap one and the one this lab has paid for before:** a rule that declines without saying so is the same instrument as the one that swallows what it cannot decide, which is the defect `check_rank_claim_values` was built to remove and which its own frame line advertises it does not have. One `ungraded.append` on the `_in_board_context` decline would put the count on the verdict; it should be bought with the measured volume, because that gate declines far more often than it admits. **Transferable form: an "execution gap" is a claim about a rule that ran. Before filing one, execute the rule on the line and record WHICH gate returned -- this pass found a gate that returns silently, and a silent gate is indistinguishable from a missing rule from the outside, which is how the same two lines were filed as both.** | the V14 sweep's owner (the Rule R re-run); Katie (whether `0.0024` gains a family); owner of `scripts/self_audit.py` (the silent board-context decline); a human reader (`:342`, which no board arithmetic decides) |
| D209 | **`docs/DOCKET.md` is a booby trap for every future writer: it carries NINE opening HTML strike tags against THREE closing ones, so any agent who writes a single closing strike tag into it silently BLANKS every claim between the nearest dangling opener and their own prose -- in every check that uses the strike masker.** Found by re-running the sweep AFTER writing, which is the only way it could have been found: this pass appended three rows containing one balanced opener/closer strike pair to illustrate a repair, and the masked text then showed **row D200 reduced to an empty line** and `_BEST_COUNT`'s pre-existing fault on it -- *"best on all eight cases -- the answer is SEVEN of the eight"* -- **GONE**. The append had not created a fault; it had DELETED one, which is strictly worse, because a new fault is loud and a suppressed fault reads as a clean file. The masker (`scripts/check_derived_figures.py::mask_exempt`, imported by `check_rank_claim_values` and by the word-form guard) blanks character spans between an opener and the next closer; it has no parser and no balance check, so an unclosed opener is inert until somebody downstream supplies a closer, and then it reaches backwards across everything in between. **The six unclosed openers are already in HEAD** -- this pass did not create them and did not repair them, because they are inside other agents' dated rows and rewriting a dated record to fix markup is the worse defect. What this pass did was stop emitting the closer: the tags are now described in words instead of written, which is the discipline `LADDER_V_TRIPLE_VERIFICATION.md` already records for rule B's use-vs-mention trap. | Counted with `/usr/bin/grep -o` on the committed file and on the working file: **HEAD 9 openers / 3 closers; after the append 10 / 4**. Verified by executing the masker both ways with `__pycache__` purged first: on `HEAD:docs/DOCKET.md` the masked text has 571 lines, live L570 is the D200 row and `_BEST_COUNT` returns two matches (L569 `best-on-board 2 of 8`, L570 `SEVEN of the eight`); on the appended file live L570 is the empty string and `_BEST_COUNT` returns **zero**. The whole-file verdict moved from **1 fault** to **0 faults** on a file that had gained 17 KB of new text and lost nothing. Restoring the prose without the closing tag restores the fault. **This is the same span class the Rule O measurement recorded against itself** (*"O1 swallowed O2's spans"*), arriving from the other direction: there a rule ate another rule's match, here a document's markup ate a rule's whole subject. **AND THIS ROW SPRANG THE TRAP WHILE DESCRIBING IT.** Its first draft wrote the two tags inside backticks in order to name them; a backtick is not a parser and the masker read the closer, so the row reporting the suppression suppressed the same fault again. Caught by the same post-write sweep, on the second pass -- the fourth instance this week of a repair's own explanatory prose creating the defect it explains, and the reason the remedy is a check and not a rule about how to write. | **REPORTED AND WORKED AROUND, NOT REPAIRED.** The workaround is a discipline and disciplines do not hold across ten agents: **the durable fix is a check, and it is one line over a file this lab already parses** -- count opening and closing strike tags in every tracked prose surface and FAIL on an imbalance, because an unclosed opener is either a rendering defect on a page that ships or a masking hazard on a record that is graded, and it is never correct. It would have caught this without anyone looking, and it is cheaper than the class it prevents: a suppressed fault leaves no trace in any count, so nothing downstream can notice it. **Not proposed: mass-closing the six openers.** They sit in other agents' dated rows; closing them changes what those rows mask and therefore what they assert, and that is their owners' call. **Transferable form: markup that masks is markup that must balance, and "I only added text at the end" is not a reason a file's earlier verdicts cannot move.** | fleet (the tag-balance check); the owners of the six rows carrying unclosed openers |
| D210 | **A mutation harness that edits the real file is unsafe under a fleet, and the failure is INVISIBLE AFTERWARDS because the restore succeeds. Measured: 3 of the 4 harnesses live in this session had the defect.** The mechanism, not the two mutants, is the finding. A cell that writes a tracked path, runs `pytest`, and restores in a `finally` leaves a 5-20 s window in which the working tree holds a mutant. Any concurrent session that commits that path captures it. **Commit `fb30e00f` (2026-08-15 21:49:10Z) did exactly that**, taking two live mutants of `scripts/self_audit.py` into history: `check_rank_claim_surfaces:1207` `if not lines:` had become `if False:`, disabling the skip guard so every surface counted as claiming -- a wrong denominator, with `_rank_companions_missing` running on surfaces that make no rank claim -- and `:1280`'s empty-set branch returned **PASS instead of UNKNOWN**, defect class B1 re-introduced by accident into the shipped audit on the same day twenty-one checks were repaired for it. **Live in HEAD for 65 minutes**; restored by the chief at `0a3e82d7`. **The reason nobody saw it is the restore.** By the time anyone looked, the harness's `finally` had put the file back, so `git status` read CLEAN while `git show HEAD:scripts/self_audit.py` was WRONG. A successful restore does not prevent the capture; it destroys the evidence of it. This is `git status reads stale under concurrency` with the arrow reversed -- there the tool under-reported dirt, here it under-reports a defect that is no longer in the worktree at all. **Census of the four `mutate.py` harnesses under the shared scratchpad, read rather than assumed:** `scratchpad/mutate.py` writes `scratchpad/mut/scripts/self_audit.py`, **a mirror -- SAFE**, and is the pattern that should have been copied; `censusA/mutate.py` takes a path argument and was pointed at **10 real tracked files** including `scripts/self_audit.py`, `sdk/chief_engineer/certificate.py` and `sdk/workflows/race_study.py` (its own `.sha` guard files name every one) -- **UNSAFE**; `censusB/mutate.py` resolves `REPO / m["file"]` -- **UNSAFE**; `emptyset/mutate.py` resolves `REPO / relpath` -- **UNSAFE, and it is mine.** **3 of 4.** Two of the three were unsafe because I instructed subagents to copy my own harness, so one bad pattern propagated to three cells in one session. **Audited, by `git show <sha>:<path>` and not by `git diff` or `git status`:** every commit between `def3ac00` and HEAD that touches any of the 19 tracked files mutated by my cell or my two subagents' cells -- the answer is that **only my own two commits touched any of them**, `31e0aa26` and `9393e267`, and each committed blob is byte-identical to HEAD, so **no mutant of mine or my agents' was captured. NONE.** Every one of the 19 targets matches HEAD in the worktree now. **The repair is `mutate_safe.py`**: one `git worktree --detach` per cell inside the scratchpad, every mutation, pytest run and restore inside it, with `assert WT.resolve() != SHARED.resolve()` as an executed precondition rather than a convention. Replayed two mutants through it -- both KILLED, same as before -- with the shared tree's SHA-256 taken before and after and unchanged. The old harness now `raise SystemExit`s with the reason. **It also closes a second defect found in the same cell:** the safe harness asserts `mutated != text`, because a mutation whose bytes do not change reports **SURVIVED** and is indistinguishable from an uncovered guard -- measured on `opened.append(rel) or unreadable.append(x)`, where `list.append` returns `None` so the original call still ran and the "survivor" was a no-op. | Chief's finding, relayed 2026-08-15, verified independently here by `git show` on every commit in the window and by reading all four harnesses. | **This is a fleet-wide rule, not a note for one agent, and it is cheap: `git worktree add --detach <scratch> HEAD` is one line.** No cell that mutates a tracked path in the shared tree should be admitted, however careful its restore, because correctness of the restore is not the property that matters -- absence of a window is. Two enforcement options, both mechanical: a pre-commit hook that refuses a commit whose diff touches a path currently held open by a mutation cell, or -- cheaper and with no coordination -- a lint that fails any harness whose write target resolves under the repo root. **The second is the one to take**, because the first still relies on every agent registering its window. **What this lab cannot currently do and should note: nothing detects a mutant that was committed and then restored.** It was found because the chief ran `git show HEAD:<path>`; no instrument does. | fleet (every agent running a mutation cell -- the pattern to copy is `scratchpad/mutate.py`'s mirror, or `emptyset/mutate_safe.py`'s worktree); chief (whether the write-target lint becomes a standing gate) |
| D211 | **D210's repair was not enough on its own, and building it correctly exposed two further harness defects that silently INVERT a mutation cell's results. Both measured.** Moving mutation into a `git worktree` removes the capture window, but it introduces a second failure and does not touch a third. **(1) A `git worktree` contains only TRACKED files, so any check that reads a gitignored artifact degrades to UNKNOWN and its test reddens.** Measured: `self_audit.check_statistical_labels` rglobs for `certificate.py` and globs `MISSION.glob("*/transcript.*")`; a fresh worktree has neither the ignored `dist/certonomous-demo/` copy (`.gitignore:72`) nor `mission-output/` at all (`.gitignore:13`), so it returned UNKNOWN and reddened `test_the_live_tree_still_reaches_a_verdict_in_every_one_of_them`. Control in the shared tree: **38 passed, 34 subtests**. Control in the bare worktree: **1 failed, 38 passed.** **(2) And that is not a cosmetic difference, because `killed = returncode != 0` is WRONG whenever the control is already red.** With a red control the mutant is red too, and the exit-code predicate reports **every mutation as KILLED, including genuine survivors.** Measured on the real instance: neutering `self_audit.main`'s `if any(r.status == FAIL for r in results): return 1` to `return 0` was reported **KILLED** by exit code and is in fact a **SURVIVOR** -- failure count 1 on both sides. Re-run against a green control it is unambiguous: **0 -> 0 failures, SURVIVED**, while the sibling `return 3 if any(UNKNOWN)` arm goes **0 -> 1, KILLED**. So this defect does not merely add noise; **it converts an uncovered guard into a clean bill of health**, which is the same shape as the empty-set class the lab has spent the week removing -- a verdict reached from a population that was never actually measured. **THE SUBSTANTIVE FINDING IT WAS HIDING:** nothing in this repo asserts that a FAIL exits 1. `test_an_UNKNOWN_with_no_FAIL_exits_3` pins only the UNKNOWN arm, and the FAIL arm is the one that reddens `scripts/lab_check.py` and blocks the push. Independently confirmed here, with a green control, at HEAD. **A third, unrelated hazard measured in the same cell:** `dist/` is only PARTLY ignored, so a mirror step guarded by `if not (WT/rel).exists()` copies **nothing** while appearing to succeed -- the checkout had already created `dist/` from its tracked members. The control stayed red and the mirror looked done. Merge with `dirs_exist_ok=True`; do not skip-if-exists. | Found while repairing D210, 2026-08-15. `emptyset/mutate_safe.py`; every measurement re-run in a worktree with the shared tree's SHA-256 taken before and after, unchanged. | **A mutation cell must assert its control is GREEN before it reports anything, and judge KILLED by the CHANGE IN FAILURE COUNT rather than by exit code.** `mutate_safe.py` now does both, announces a red control in its output instead of absorbing it, and mirrors the ignored inputs -- `dist/` copied, `mission-output/` symlinked because it is 574 MB and copying it per cell is not affordable. With those three, the worktree control reproduces the shared tree exactly: 38 passed, 34 subtests. **The general rule, which is bigger than mutation testing: any instrument that reports a verdict by comparing two runs must first establish that the baseline run is clean, or its comparison is between two failures and means nothing.** Two of this session's cells reported results against a red control before this was found; both were re-run. **What is still unfixed and is somebody's to take: the guard-census cell measured 59 guards with no covering test across 4 scripts, and the two highest-severity -- `self_audit.main`'s FAIL->1 and `check_summary_consistency`'s `if bad_controls: verdict = UNKNOWN` -- are both confirmed SURVIVORS at HEAD with a green control. The second is the arm that module's own docstring sells.** | fleet (the green-control and failure-count rules, in every harness); owner of `scripts/self_audit.py` (the FAIL->exit-1 hole); owner of `scripts/check_summary_consistency.py` (ten control-misfire guards and the verdict arm they feed, none covered) |
| D212 | **THE RESTORE CAPTURED A MUTANT. The commit that repaired `fb30e00f`'s captured mutation captured a DIFFERENT one from the same harness in the same way, and it shipped a fail-CLOSED regression that BLOCKS `lab_check` on the everything-complies path.** `0a3e82d7` was made from the working tree to repair `fb30e00f`. It fixed one thing correctly -- `if False:` back to `if not lines:` in `check_rank_claim_surfaces` -- and in the same diff changed the compliant fall-through from **`PASS` to `UNKNOWN`**, which is not a restore: it is census mutant **RC10** applied. Executed across the history: `847b4492` (20:26), `fb30e00f` (21:49) and `9b9951a1` (22:05) all carry **PASS**; `UNKNOWN` first appears at `0a3e82d7` (22:54). The census's own `spec_rc.json` records RC10 as `old` = `PASS`, `new` = `UNKNOWN`, so the harness author's record says `PASS` is the original and the restore installed the mutant. **The whole function is byte-identical to `9b9951a1` apart from those two lines**, so the diagnosis is a diff and not an inference from behaviour. **THE CONSEQUENCE IS NOT COSMETIC:** `scripts/lab_check.py`'s `EXIT_CONTRACT` is `{0: PASS, 1: FAIL, 2: FAIL, 3: UNKNOWN}` and treats UNKNOWN as **blocking**, so the mutant turns the branch whose entire meaning is *nothing is wrong* into a hard stop. The defect being repaired was fail-OPEN; the one introduced is fail-CLOSED; both came out of the same 5-20 second window. **Restored to `PASS` here.** The independent witness is a test in a file this pass never touched: `test_rank_claim_surfaces.py::test_a_compliant_unlisted_surface_passes` asserts `PASS`, names the defect in its own title, and had been red since 22:54. | **AND THE GUARD WRITTEN TO PREVENT THIS CLASS ASSERTED THE MUTANT, GREENLY.** This pass added `TheAuditMustNotSHIPAMutantTests` on the chief's instruction, and its first draft contained `test_the_form_guard_has_no_PASS_path_at_all` -- an assertion that `check_rank_claim_surfaces` never returns PASS -- written by reading the already-mutated HEAD and reasoning backwards to a plausible design (*"a FORM verdict is not a statement that a page is right"*). The reasoning was tidy and the invariant was the mutant. **It passed, AND it was mutation-proved**: an `M5` cell re-introduced the opposite edit and the test went red exactly as designed. **That is a proof the test detects a change; it is not a proof it detects the RIGHT change, and this is the cleanest example of the difference this lab has produced.** Caught only by re-running the FULL suite on the live tree afterwards, where one test in an untouched file went red -- **1 failure in 2,011**. Corrected to `test_the_compliant_fall_through_is_PASS_and_not_UNKNOWN`; M5 reversed and the whole cell re-run with `__pycache__` purged before each cell and a sha256 restore assertion: control GREEN (20 tests, 33 subtests), M1-M5 each RED on their aimed test, restored GREEN at the control's sha256 `1a237b0e`. **Evidence order, which is the transferable part: a harness spec plus three commits beat a design argument reconstructed from the current file.** | **REPAIRED (this row). The class needs a mechanism, not more care.** This is the FOURTH instance in one session of a working-tree read taken as ground truth while a harness held the file: `fb30e00f` captured two mutants; the census's exit `finally` re-applied RC10 over the chief's restore minutes later, which this pass caught by scanning the file it was about to commit; this pass's own docket commit captured another agent's row; and `0a3e82d7` captured RC10. **Not one of them was careless -- every author checked `git status` or `git diff`, and every one was clean at the instant it was checked.** What would settle it: (1) the harness stops writing tracked paths, already instructed under D210/D211; (2) **a pre-commit assertion that every hunk about to be committed is one the committer can name** -- the only check that catches all four, because each was a diff its author did not intend and could not see; (3) **treat a restore as a CHANGE and run the aimed suite on it** -- the sibling test named this defect in its title and was red for eighteen minutes. **Transferable form: a repair made from a working tree under concurrency is a new commit with a new blast radius, not a return to a known-good state, and it earns the same controls as the change it undoes.** Note on numbering: this row was drafted as D211 and the free-ID assertion refused it mid-write, because D211 had been taken between drafting and writing. The gap is not reused. **[CHIEF ACCEPTANCE 2026-08-15, at repo `49b36e57`. THE RESTORE WAS MINE AND THE ROW IS RIGHT. I INTRODUCED A MUTANT WHILE ANNOUNCING THAT I WAS REMOVING ONE.]** Verified by execution before accepting: `git show 9b9951a1:scripts/self_audit.py` carries **`PASS`**; `git show 0a3e82d7:` carries **`UNKNOWN`**; HEAD now carries `PASS` again. So of the two hunks in `0a3e82d7`, **only one was a restore.** `if False:` → `if not lines:` was correct. `PASS` → `UNKNOWN` was **census mutant RC10**, which I swept in from the working tree and then described in my own commit message as *"the empty-set branch returned PASS… defect class B1, the exact defect this lab spent today removing, re-introduced by accident."* **That sentence is false, and it is false in the most self-flattering available direction: I named the thing I was adding as the thing I was fixing.** I also reported it to the owner in those terms. **THE HARM IS THE OPPOSITE SIGN TO THE ONE I CLAIMED.** `lab_check`'s `EXIT_CONTRACT` treats UNKNOWN as **blocking**, so the branch meaning *nothing is wrong* became a hard stop. The defect I repaired was **fail-open**; the one I introduced was **fail-closed** — and a fail-closed audit is how a gate gets switched off by whoever it next inconveniences. **WHAT WOULD HAVE CAUGHT IT, in order of cost.** (1) Running the suite: `test_a_compliant_unlisted_surface_passes` asserts `PASS`, **names the defect in its own title**, and was red from 22:54 — I committed a two-line change to an audited file without running its tests, on the reasoning that a restore cannot break anything. **A restore that is not byte-checked against a known-good commit is an edit.** (2) The check this row proposes: count the hunks you expect and stop if the number differs. My diff had two hunks and I expected one; I read the second as corroboration rather than as an anomaly. (3) `git show <known-good>:<path>` — four keystrokes, and it is the exact command I had used ten minutes earlier to find the original mutant. **I had the instrument in my hand and did not point it at my own change.** **THE TRANSFERABLE PART IS NOT MINE, IT IS THE GUARD'S.** The agent's test `test_the_form_guard_has_no_PASS_path_at_all` **asserted the mutant** — derived by reading the already-mutated HEAD and reasoning backwards to a plausible design. It passed, and it was **mutation-proved**, because re-introducing the opposite edit reddened it exactly as designed. **A mutation proof shows a test detects *a* change, not the *right* change.** Every mutation-proved test in this lab written against a corpus rather than against a specification carries that exposure, and this is the first measured instance. | chief (the restore, and whether a restore gets a test run); fleet (the name-every-hunk pre-commit check) |
| D213 | **A 150 core-min proposal is live, its named release trigger has FIRED, and its dispatch surface carries no record of the two documents that killed it -- and the reason those documents give is itself REFUTED by execution.** `demo-output/website/agenda/proposals/s1-cbfs-w1-only-arm.json` reads `"status": "proposed"`, `"est_core_min": 150.0`, last touched `b4fd62e7` **2026-08-10 15:43:18**, and its entire standing decision is a `decision_note` reading *"NO-GO FOR NOW ... approved in principle with a NAMED TRIGGER -- the S1-with-priors design phase ... Do not claim, price, or launch **before that phase opens**"*. **The trigger fired and the item that fired it says so in its own words**: `ladder-b/S1_PRIORS_PREREGISTRATION.md:7-9` opens by naming itself *"the named TRIGGER that unblocks `s1-cbfs-w1-only-arm`"*, and `:208-214` **decides loss support without it** (equal-weight, all cells, argued before any compute) and **releases the arm back to the chief** as a separate go/no-go. So the arm is released and dispatchable in principle behind an eleven-day-old surface that says NO-GO. **KILLED HERE ON TWO LEGS, NEITHER OF WHICH IS THE REASON ON THE RECORD.** (1) Its first deliverable is a G2 verdict (`expected_knowledge_gain`: *"Whether the window-only bar is passable"*; `gate`: *"G2 unchanged"*), and G2 is invalidated six independent ways -- a PASS on an invalid gate establishes nothing and so does a FAIL. Its co-deliverable G1 is a bar on the training objective the optimizer directly minimises with 21,000 free parameters, which the record itself calls a capability verdict and not a correctness verdict. (2) Its second deliverable, *"the loss-support design datum the S1-with-priors phase needs either way"*, was declined by the only consumer named for it -- that phase decided loss support without it and recorded that the W1 question is **not consumed**. Both deliverables dead, so **every outcome leaves belief unmoved.** **AND THE PUBLISHED REASON IS WRONG.** `campaign/CLOSURE_STAGE1_AND_C2_STATUS.md` S5 request 2 declines the arm because *"a window-only loss makes the deviating set ~ the window ~ 8.44% < a decile ... **It would score 8.4% and FAIL by construction**"*, and `dafoam/S1_ZEROCOMPUTE_TRIAGE_2026-08-14.md:89` carries it forward as *"arithmetic, not judgement"*. The premise is that a restricted loss support confines the deviating beta set to that support. **The one executed instance of that class on disk refutes it**: `S1-cbfs-weighted-arm/cbfs_inv/beta_final.npy`, whose loss is supported on **2,970 of 21,000 cells = 14.14%**, deviates from 1 in **19,842 cells = 94.49%** (>1e-12; 10,877 at >1e-6), with a 90th percentile of |beta-1| of **2.555e-02**. The `np.quantile(dev,0.9)` degeneracy the reason invokes (`S1-cbfs-reinversion/audit_final.py:56-57`) needs the deviating set **below 10%**. **Positive control fires**: `beta_masked.npy`, the field that WAS built by confining deviations, returns a confined set (2,875 cells = 13.69%), so the measurement can see confinement where it exists. **The status document contradicts itself and its S1.5 caveat is the half the data supports** -- *"this degeneracy is not what produced 26.9%. Both real beta fields deviate in essentially every cell"* -- while S5 extrapolates S1.5's SYNTHETIC table row to what a real inversion would produce. Consequential sibling: `S1_PRIORS_PREREGISTRATION.md:211`'s *"the weighted arm already demonstrated that localization follows the loss support **cell-for-cell**"* is too strong -- top-decile MEMBERSHIP follows the support (42.7% in-window vs 79.7% in-support, that file's `:80-82`); the DEVIATING SET does not. (Frame: three `.npy` arrays under `/home/ubuntu/certonomous-runs/`, outside the repo and invisible to every git route; 21,000 cells each; DV order throughout, no permutation applied. Zero solver compute, zero scoring calls, ledger stands at 6.) | Closure three-strand re-establishment, 2026-08-15, `campaign/CLOSURE_THREE_STRANDS_2026-08-15.md` S1, repo anchor `54a562a3` | **Two separable actions, and the second is the one that generalises.** (i) Write the kill onto the DISPATCH SURFACE, not into a third narrative document -- a proposal JSON is what an agent acts from, and both documents that killed this arm wrote only prose. (ii) **Do not recite "FAIL by construction"**; it is refuted, and the kill that survives is the deliverable-level one, which is stronger because it does not depend on any prediction about what the arm's beta field would look like. Also owed, and outside this row: `S1_PRIORS_PREREGISTRATION.md:211`'s cell-for-cell sentence, which belongs to that file's owner. **This is D41's class on real money** -- the t_preregistration > t_proposal detector would not have caught it, because here the proposal did not go stale against an amendment: it went stale against its own trigger FIRING. A release event is a second detector nobody has, and it is the one that leaves a priced item dispatchable with a dead justification. | chief (the go/no-go this arm was released back to); owner of `CLOSURE_STAGE1_AND_C2_STATUS.md` and `S1_ZEROCOMPUTE_TRIAGE_2026-08-14.md` (the refuted reason); owner of `S1_PRIORS_PREREGISTRATION.md` (the cell-for-cell sentence) |
| D214 | **One third of the frozen round-5 gate grades two fields the entry is not scored on, and it read PASS on an arm whose graded fields never converged.** `campaign/R5_PREREGISTRATION.md` S1 freezes three clauses on the validation duct `AR_7_Ret_180`: V1 QCR scaled MAE <= 0.70 x SST's; V2 in-plane Pearson r >= 0.85; **V3 both arms converge on their own `residualControl` within the cap**. V1 and V2 grade the **in-plane secondary-flow velocity field**. **Executed on the archived cases** (`/home/ubuntu/certonomous-runs/w3-qcr-rank1/`, outside the repo): `system/fvSolution:49-53` is byte-identical in **all five arms** and reads `residualControl { k 5e-6; omega 1e-10; }`. **Entries for `p`, `U`, `Ux`, `Uy` or `Uz`: ZERO, in 5 of 5 arms. Positive control on the identical extractor: 2 of 2 (`k` and `omega`) in 5 of 5**, so the zeros are real absences and not a broken range. **Consequence:** V3 is decided entirely by two turbulence residuals; it cannot fail for any amount of drift in the quantity V1 and V2 measure; and the only way it can fail at all is the iteration cap binding, so it carries the same one bit as *iters < cap*, which the record already prints. It is not an independent third clause. **WHAT THE RESIDUAL HISTORIES SHOW, over every iteration and not just the last.** `AR_7_Ret_180_sst` -- **the validation control, whose in-plane field is V1's denominator and one side of V2's correlation** -- terminates with `SIMPLE solution converged in 4652 iterations` while `Uy` has median **0.597** and minimum **0.110**, `Uz` median 0.516 minimum 0.158, and `p` median 0.360 minimum **0.280**, across all **4,652** iterations. **Positive control**: the QCR arm drives the same three to **2.08e-05 / 1.99e-05 / 6.79e-06** over 4,660 iterations, so the log reader finds convergence where it exists and the SST arm's O(1) plateau is a property of that arm. **Stated no stronger than the evidence carries: V3 is NOT an identity** -- it can fail if the cap binds -- **it is a NON-DISCRIMINATING clause.** And whether the O(1) plateau is genuine non-convergence or the residual-normalisation degeneracy of a field that is structurally machine-zero (`R5_PREREGISTRATION.md` S1 records SST in-plane RMS at **1.2e-15 of bulk**, the linear-eddy-viscosity duct zero) **is not decidable from the logs -- and that ambiguity IS the finding**: V3 was reported as a passed clause with no evidence either way about the graded quantity. | Closure three-strand re-establishment, 2026-08-15, `campaign/CLOSURE_THREE_STRANDS_2026-08-15.md` S3.1, repo anchor `54a562a3` | **Report, do not repair: R5 is frozen and S2b forbids repairing a recorded gate**, and the round-5 entry of record is not in question -- V1 passed at 0.4770 against a 0.70 bar and V2 at 0.9284 against 0.85, both on quantities that were measured. What is owed is that V3 stop being cited as a third independent clause. **THE OBVIOUS FOLLOW-UP EXPERIMENT MUST NOT BE BOUGHT**, and the arithmetic is here so nobody re-derives it: re-solving the SST control with `residualControl` extended to `p` and `U` (~10-20 core-min) cannot move V1, because SST's in-plane RMS is 1.2e-15 of bulk -- the structural duct zero of the linear eddy-viscosity class -- so its MAE against the LES is bounded below by essentially the whole LES secondary-flow magnitude and cannot fall the 32% a verdict change would need, however long it runs. **Every outcome leaves belief unmoved.** What IS worth doing costs nothing and is already filed and unranked: `agenda/proposals/does-the-residual-threshold-that-declares-convergence-bound-the-graded-quantitys-remaining-drift.json` (0 core-min). **This row is its first executed instance, and it fired on the entry of record's own validation control.** Sweep every archived case's `residualControl` against the fields its record grades: if a majority of graded runs declare convergence on fields they do not grade, the lab's convergence claims need re-stating class-wide and a standing rule follows; if it is only these five arms, this row is the whole of it. Belief moves either way at zero cost. | owner of the R5 pre-registration and freeze (the citation); chief (whether the 0 core-min residual-threshold sweep is ranked) |
| D215 | **C2's central statistic could not have returned a value inconsistent with its own hypothesis, its headline is dead at round 5, and its targeting instruction is now exactly backwards -- while the audit that cleared it verified the arithmetic instead.** **(1) THE STATISTIC.** `closure_challenge_C2_error_decomposition.md:63-89` offers *"Pearson r between floor and delta is **-0.9418**"* with the caveat *"n = 5 ... suggestive, not established"*. **The caveat names the wrong problem**: the statistic correlates `floor` against `delta = entry - floor`, so the floor is on both sides and the correlation is coupled by construction. Executed on C2's own five published pairs: the **exact permutation null over all 120 assignments** of the same five entry values to the same five floors runs **[-0.9940, -0.9418] with mean -0.9583**; the observed value is **the least negative value the null can take**, 118 of 120 permutations are at least as negative, one-sided **p = 0.983**, and **120 of 120 = 100% of permutations return r <= -0.90**. The **analytic independence null**, `-sd(F)/sqrt(sd(F)^2+sd(E)^2)`, is **-0.9479** -- MORE negative than the value offered as evidence. The **un-confounded** statistic is `r(floor, entry)` = **+0.3259**, Spearman **+0.3000**, two-sided exact permutation **p = 0.625**: nothing. Per the heavy-tailed rule, Spearman on the coupled statistic is **-0.9000**, LESS extreme than Pearson, which is the tell that Pearson is driven by the coupling and by the 0.2049 leverage point rather than by rank structure. C2's companion claim *"clean separation with no overlap"* has a **base rate of 102 of 120 = 85%** under the same null, and 3 of the 5 case signs are forced by the value ranges alone. **The mechanism C2 proposes may well be true and round 5's decline gate is built on it and works -- C2 supplies no evidence for it.** **(2) THE AUDIT.** `campaign/CLOSURE_STAGE1_AND_C2_STATUS.md:386` records this as `| Pearson r(floor, delta) = -0.9418 on n=5 | **-0.9418** | check |`. It verified the ARITHMETIC and never asked whether the statistic could discriminate. **(3) THE HEADLINE IS DEAD.** C2 decomposes the ROUND-2 entry and has never been recomputed on round 4 or round 5. Recomputed here from `closure_challenge_round5_qcr.json` (`round5_per_case_full` and `rans_identity_floor_per_case` sit side by side in that one file): *"on 3 of 8 cases our correction is worse than doing nothing"* is **1 of 8** at round 5 on the published four-decimal values, both `alpha_05` cases now sitting EXACTLY on the floor because the decline gate ships the organisers' own unmodified RANS field there; and the *"1.7x our entire margin"* recoverable term of 0.0066 is **0.000138** on the 8-case mean, a **48x reduction**. **(4) THE TARGETING IS BACKWARDS.** C2:43-44 instructs *"Effort should target the alpha_05 periodic-hill regime **and nothing else**. Chasing the NASA hump would recover **at most 2%** of the available term."* Recomputing the live successor quantity -- per-case headroom to the six-entrant column minima, `LIVE_BOARD` read by `ast.literal_eval` off its assignment node and never imported -- gives **`NASA_2DWMH` 0.033798 = 43.6%**, `AR_1_Ret_360` 21.1%, `AR_14_Ret_180` 13.3%, `AR_3_Ret_360` 11.4%, `alpha_15_..._4048` 8.9%, `alpha_15_..._2024` 1.7%, both `alpha_05` cases **0**, total 0.077607. **The hump is the single largest remaining term, larger than AR_1 + AR_3 combined at 32.5%, and it is our worst row on the whole board.** The derivation independently reproduces **best-on-board = 2 of 8** (the two zero-headroom rows), which is the cross-check that it is sound. **No surface anywhere states this.** **(5) THE SANITY CHECK IS AN IDENTITY, CONFIRMED, with two corrections to its adjudication.** The scorer is `return np.mean(list(scores.values()))` at line 17 of **`/home/ubuntu/closure-challenge-pkg/src/closure_challenge/eval.py`** -- so the overall is an unweighted mean of the per-case values by construction and recomputing it from them is an identity under W-2. But S3.2 of the audit attributes that line to *"the benchmark's own scorer"* and cites the benchmark clone two paragraphs later, and `/home/ubuntu/closure-challenge-benchmark/closure_challenge/eval.py` **does not exist** -- the line number is right and the repository is not. And C2:7's word *"exactly"* is **false at full precision**: round-2 entry mean 0.0740625 against the stated 0.0741 and floor mean 0.1036375 against 0.1036, **|diff| 3.750e-05** in both, so the identity's one real failure channel is only detectable above ~4e-4 on a single case. **(6) "C2's RECOMMENDATION WAS EXECUTED" IS TRUE OF THE FALLBACK ONLY.** The audit's S3.5 cites the trust/decline gate, which does verify. C2 has a second recommendation, and it is the one C2 TITLES *"Recommended next action"* and calls *"strictly better than gating"*: train a second alpha_05-regime model. **It ran.** `demo-output/website/closure_challenge_alpha05_regime_model.json`, purpose *"per C2's recommended next action"*, records `go_no_go: {..., "rule_iii_hurt_cap": false, "decision": "NO-GO"}` -- it failed its own pre-registered hurt cap and never reached an entry. **(7) NINE STALE BOARD CLAIMS, REPORTED NOT REPAIRED**, all in C2 and against the live seven-position board on which **Wu and Zhang is position 4 of 7, not rank 2**: `:36` a present-tense *"current margin over the rank-4 target (0.0779)"* where 0.0779 is Montoya at **7 of 7**; `:131-137` a **five-position** board table, undated and unlabelled; `:139` *"between rank 3 and rank 4"*; `:156, :175, :234, :253, :258, :264` six *"rank 2"* identifiers; `:158-167` a *"best on board"* column that is the minimum over **four** rows; `:169` *"**We hold the best score on the entire leaderboard on three of eight cases**"*, which against the live board is **0 of 8** for round 2; and `:281` a commit anchor that is legitimate as HARNESS provenance but is the only board identifier the document carries while the table at `:131-137` is drawn from that same clone and the document never says so. **And the audit carries it onward in its own voice**: `CLOSURE_STAGE1_AND_C2_STATUS.md:430-434` *"confirmed the claim that the lab holds the board best on **three of eight** cases ... a genuine external referent"*, whose staleness caveat at `:442-447` names the six-row board and the new rank 1 and **never restates the count**. **C2's own supersession note at `:142-154` is explicit about its reach** -- *"the label is the only edit made to it"* -- so it covers the `ours` row of one table and reaches none of `:12`, `:30-44`, `:169` or `:173-187`, which is where the conclusions actually live. | Closure three-strand re-establishment, 2026-08-15, `campaign/CLOSURE_THREE_STRANDS_2026-08-15.md` S4, repo anchor `54a562a3` | **Nothing here is repaired and the physics is not overturned** -- C2's mechanism is untested, not refuted. Four separable owings. (i) The check at `CLOSURE_STAGE1_AND_C2_STATUS.md:386` should read *arithmetically reproduced, inferentially void* rather than verified; a reproduction is not an adjudication, and this is the second time on this line that a ratio with a pinned null was recorded as evidence. (ii) C2 needs one banner where its conclusions live, not one on the row it was pointed at. (iii) **The NASA hump is the one experiment on this line whose outcome can move the standing, and nothing on disk prices it** -- a sweep of the tracked closure JSONs and `closure_challenge_C6_hump_decision.md` finds no priced hump request, so the cost is **UNKNOWN with the reason, not cheap**. The acceptance threshold is arithmetic and is stated now so it cannot be chosen later: the overall is an unweighted mean of eight, so closing **57.3%** of the 0.033798 headroom moves the overall by **0.002419**, exactly the seed bound, which is the only term currently able to reverse the lead; closing **32.3%** moves it by the Yang margin 0.001365 and leaves the claim inside the seed bound; **anything below 0.0109 on the case is inside the noise the standing is already measured against and decides nothing.** A proposal that cannot pre-state which side of 0.0109 it expects should not be bought. (iv) **No guard opens C2**: `scripts/self_audit.py`'s `_best_on_board_faults` derives exactly the right numbers (best 2, earned 0) and its only caller is fed the credentials-wall string, so this is D151's gap on a surface no denominator predicate would have caught anyway -- C2 states its counts in WORDS (*"three of eight"*), not in the `n of 8` form. **Not proposed, and named so nobody proposes it**: re-running C2's floor decomposition on round 5 as an investigation. It is done above, the recoverable term is 0.000138 on the mean -- ~10% of the margin, ~6% of the seed bound -- so every outcome is below the noise. It is a supersession note, not a queue item. **And C2's cost line `| Compute cost | none -- 0 core-min |` at `:283` is honest about C2 and misleading about C2's consequences**: the duct work its targeting pointed at billed **37.0 measured core-min** (`closure_challenge_round5_qcr.json`, `cost.solver_core_min_total_measured`; AR_14 alone 24.8) plus **2.1** for the regime model that scored NO-GO, and `CLOSURE_STAGE1_AND_C2_STATUS.md` S5, the one section built to price this family, has **no C2 row at all**. | owner of `closure_challenge_C2_error_decomposition.md` (the banner and the stale ordinals); owner of `CLOSURE_STAGE1_AND_C2_STATUS.md` (the check mark, the eval.py attribution, the unrestated count, the missing price row); chief (whether the hump is priced) |
| D216 | **The count that gates a first send has never been enumerated anywhere, in any arm: D31 says FOUR of the five open policy questions bind a first send, lists FIVE, and names none of the four.** Executed across all four arms. **Tracked**: `git grep -a -n -i -E 'bind (a|the) first send|binds a first send|binding a first send'` returns **exactly 2 hits** -- `docs/DOCKET.md:224`, D31's own headline, which then enumerates **(a) through (e), five of them**, and `demo-output/website/CLOSURE_SUBMISSION_REQUIREMENTS.md:123-124`, *"Five policy questions remain open (S3), **four of which D31 marks as binding a first send**"*, which CITES D31 rather than enumerating. **Untracked + gitignored + whole working tree**: `/usr/bin/grep -rl --binary-files=text` over the repo excluding `.git` returns **one file, `docs/DOCKET.md`** -- the same row. So the one document that cites the number points at the one document that asserts it, which lists a different number. **The four have no referent, and a count that is never enumerated cannot be checked and cannot be discharged one question at a time.** **THE FIVE THEMSELVES ARE RE-VERIFIED TODAY and stand**, by D31's own method re-run over the pinned clone `/home/ubuntu/closure-challenge-benchmark/README.md` (116 lines): four positive controls fire -- `strictly forbidden` 1, `anytime` 1, `authors` 2, `rmcconke` 7 -- while eight absence terms return zero on the same lines with the same instrument: `registration`, `deadline`, `resubmi`, `licen`, `per team`, `entries`, `hyperparameter`, `copyright`. **The zeros are real absences.** Additionally `/usr/bin/find` for `license*` or `copying*` in that clone returns **no file**, corroborating D31(d)'s `license: null` locally with no network read. **THE FOUR, DERIVED HERE UNDER A STATED CRITERION** -- a question binds a first send iff its answer changes either the CONTENT of the package or the LEGALITY/REVERSIBILITY of the act of sending; a question that only bounds what may happen after the first send does not bind it. **(e) company vs named individuals on the Authors row -- BINDS**: step 3 requires a list of all authors and the field is literally `[KATIE TO FILL]`, which `CLOSURE_SUBMISSION_REQUIREMENTS.md` S4.2 already calls blocking by construction. **(c) hyperparameter disclosure -- BINDS**: it fixes what the description document must contain at the moment it is sent, and it is upstream of the single biggest identified risk, the steward's reading of the disclosed adaptive-selection history. **(d) licence -- BINDS**: accepted submissions are committed into a public repository with no licence, so sending disposes of rights under terms nobody has established and it cannot be undone. **(b) corrected resubmission after scoring -- BINDS**: it decides whether the first send is one-shot, which decides whether to send now at 0.056647 or after the post-send month's work. **(a) entries per team -- DOES NOT BIND**: a first entry is entry number one and no limit permitting any entry at all can bar it; (a) bounds FUTURE sends and is (b) seen from the other side. **CAVEAT STATED RATHER THAN SMOOTHED OVER**: under a stricter reading -- must be answered before the send can be physically executed -- only (c), (d) and (e) bind and the count is **THREE**, because (b) also concerns a later send. The four survives only under the reversibility limb. **PARKED throughout: no registration, no email, no issue, no upload, no contact of any kind. Only files already fetched and committed were read.** | Closure three-strand re-establishment, 2026-08-15, `campaign/CLOSURE_THREE_STRANDS_2026-08-15.md` S2, repo anchor `54a562a3` | **Write the four down, whichever reading is taken, and say which reading it is.** This row does not authorise asking the steward anything and does not close any of the five: they remain unanswerable from documents and asking is an external interaction reserved to Katie in two charters. What it establishes is narrower and is a defect in the lab's own record -- **an unbacked count on the one row whose owner is Katie**, carried forward by a second document that cites it in good faith. The generalisable half: D31 was audited twice (2026-08-11, and again 2026-08-12 against Katie's live-README paste) and **both audits re-checked the FIVE and neither noticed that the FOUR beside it has no members** -- a count in a headline is not covered by an audit of the list under it. | Katie (D31 is hers, and so is the reading of which questions bind) |
| D217 | **The two highest-severity uncovered guards from the 127-guard census are closed, and re-running the cell that missed them found a THIRD survivor nobody had named: a check that RAISES was recorded PASS.** D211 left two confirmed survivors at HEAD; both are now killed, and the re-run cost one extra measurement and returned one extra finding. **(1) NOTHING ASSERTED THAT A FAIL EXITS 1.** `self_audit.main`'s `if any(r.status == FAIL for r in results): return 1` had no covering test at all -- its sibling `test_an_UNKNOWN_with_no_FAIL_exits_3` pins only the UNKNOWN arm, and exit 1 is the code `scripts/lab_check.py`'s `EXIT_CONTRACT` reads as FAIL, so it is the arm that reddens the runner and would block a push. A pre-push hook whose blocking condition rests on an untested exit path is worth very little, which is why this one was worth buying before the hook. **(2) A TEST FILE THAT RE-IMPLEMENTED WHAT IT SHOULD DRIVE.** `sdk/tests/test_summary_consistency.py` never called `run_controls()` once. Its `currency_block` helper is a HAND COPY of `run_controls.block`, and all five controls were re-implemented in the test file's own helpers and asserted on there. Measured in an isolated worktree with a green control: **all TEN misfire guards SURVIVED**, so did `main`'s `if bad_controls: verdict = UNKNOWN` -- the arm that module's own docstring sells -- **and so did `run_controls()` neutered to return an empty misfire list before doing anything at all. Thirteen mutations, thirteen survivors, suite green throughout.** That is the recompute-instead-of-drive failure in a third instrument this week. **(3) THE THIRD SURVIVOR, and it is the worst of the three:** neutering `self_audit.main`'s `except` arm so a check that RAISES is recorded PASS instead of FAIL was reported KILLED by the old harness and is a SURVIVOR. A crashed check then reports as a passing check and the audit exits 0 on a detector that never ran -- the empty-set shape reached through the runner instead of through a check. It survived for the same reason the FAIL arm did: `killed = returncode != 0` against an already-red control calls every mutation KILLED, survivors included. | **Every cell in a `git worktree` in the scratchpad with the ignored inputs mirrored (`dist/` copied, `mission-output/` symlinked), a GREEN control ASSERTED before anything was reported, the verdict taken from the CHANGE IN FAILURE COUNT and not the exit code, `__pycache__` purged before each run, control and mutant in one invocation, and the shared tree's SHA-256 taken before and after and unchanged.** THE THREE EXIT-MAP MUTATIONS RE-RUN, census id and old verdict against new: **SAM1** `if any(FAIL): return 1` never taken -- was KILLED by exit code, **is SURVIVED, 0 to 0 failures: INVERTED** (this is D211's own re-run, confirmed here independently); **SAM2** `return 3 if any(UNKNOWN) else 0` collapsed to `return 0` -- KILLED then, KILLED now, 0 to 1: **holds, and it is the control that says this cell was not simply reporting everything as survived**; **SAM3** the raising-check arm recording PASS -- was KILLED by exit code, **is SURVIVED, 0 to 0: INVERTED, and it was not in D211**. **TWO OF THE THREE INVERT.** After the repair, six mutations on that exit map all KILLED (0 to 6, 0 to 6, 0 to 2, 0 to 2, 0 to 2, 0 to 2), including two must-not-fire cells -- the FAIL arm returning 0, and the clean arm returning 1 -- so the class cannot be satisfied by an exit map that reddens unconditionally. On the control block: **BEFORE 0 of 13 killed, AFTER 13 of 13**, each guard driven by handing the SHIPPED `run_controls()` a corpus in which one named control genuinely misfires, through the module-level `_blob` it reads its two immutable git blobs with. **L-84 executed on the shipped CLI, not argued:** a synthetic clean document returns VERDICT PASS exit 0, a genuine stale summary returns VERDICT FAIL exit 1, and the unsubstituted real corpus reports zero misfires. **AGGREGATE UNMOVED:** `self_audit.py` PASS 19 WARN 10 FAIL 4 INFO 3, exit 1, before AND after, every per-check status byte-identical; the only textual change is a denominator, `cited evidence paths` 1374 to 1386 repo-rooted citations with the twelve new ones all resolving and still 2 unresolved and still FAIL. `check_summary_consistency.py` PASS exit 0 both. **No production file was touched: the commit moves two test files and nothing else, verified by `git show HEAD:` on all four paths afterwards.** | **BOTH CLOSED. What remains of the census is 48 of the original 59 survivors, and the priority order is not by count.** ELEVEN census guards were bought here (the ten control-misfire guards plus the verdict arm they feed), so 59 minus 11 is 48; the two `self_audit` exit-map guards closed alongside them were NEVER IN THE 59, because the broken predicate had recorded both as KILLED. **That is the second-order consequence of the red control and it should be stated plainly: the census's survivor count is a FLOOR, not a figure.** Every mutation it judged against a red control was recorded killed, so the true original population is at least 61, of which 13 are now closed -- 48 either way, and the same number by luck rather than by construction. **What is left, and how it should be ranked: (a) the remaining `self_audit.py` guards inside individual checks -- these are the cheapest per unit and the least valuable each, because a single check going quiet is visible in the printed tally; (b) `check_rung_attribution.py` at 23 survivors of 41, which is the largest single block and grades AUTHORSHIP, where a guard that stops firing produces a false NON-AUTHOR clearance nobody can see from the outside; (c) `lab_check.py`'s 14, which should arguably outrank (b) because lab_check is the gate every other instrument reports THROUGH -- a survivor there silences whatever it wraps. Rank by WHAT A SILENT GUARD WOULD CLEAR, not by how many there are in a file.** **And one thing the census cannot rank, which is the transferable finding: the recompute-instead-of-drive shape does not show up as a low guard count.** `test_summary_consistency.py` had 34 green tests over a function that could have returned an empty misfire list unconditionally. The tell is not coverage; it is **a production entry point that no test names**, and it is worth one grep over every test file in the lab: for each script gate, does any test call its `main()` or its named control function, or only its leaf helpers. **`scripts/check_absolutes.py` was OUTSIDE the audited scope and is UNAUDITED -- it is not in the 127 and nothing here says anything about it.** | owner of `scripts/self_audit.py` and `scripts/check_summary_consistency.py` (done, this row); whoever takes the remaining 46 (the ranking above is a recommendation, not a claim about their contents); fleet (the entry-point grep, which is one pass over `sdk/tests/` and would have found this in minutes) |
| D218 | **The two surfaces that both call themselves the docket disagree about whether work has been done on 45.3% of the items they share, and the disagreement is on the one field that decides whether an agent starts a run. 463 core-min of FINISHED work is advertised as available and 7,650 core-min of APPROVED work is advertised as merely proposed.** Found while auditing a single stale closure proposal (D213) and measured because two more turned up on the same line within minutes. **METHOD:** join `demo-output/website/agenda/proposals/*.json` to `demo-output/website/agenda/docket.json` on `id` and compare `status`. Both surfaces are tracked; no other arm is involved. **COUNTS, printed before the verdict:** docket.json **264** records; proposal files **131**; ids present in BOTH **75**; proposals with NO docket record **56**; docket records with NO proposal file **189**; **statuses that DISAGREE: 34 of 75 = 45.3%**. **THE TWO DIRECTIONS THAT COST SOMETHING, separated because they cost different things.** (i) **`proposed` on the surface, `done` in the docket -- 15 items, 463 core-min of already-finished work still advertised for dispatch**: `fiml-adjoint-conditioning-unblock` **240**, `w3-qcr-forward-on-the-ducts-is-the-rank-1-route` **95**, `closure-baseline-error-estimator-gate` **45**, `r1-multifidelity-propagation` 20, `double-mach-reflection-woodward-colella` 20, `naca4412-independent-reference-anchor` 15, `b52-replicate-meshes-at-rung-6` 14, `f6a-hump-qcr-arm-on-the-challenge-run` 8, `r1-sobol-sensitivity-mission` 6, and six 0-core-min items. (ii) **`proposed` on the surface, `approved` in the docket -- 10 items, 7,650 core-min that are dispatchable and whose surface does not say so**: `hlpw6-testcase1-coarse-grid-entry` **6,390**, `closure-duct-field-inversion` **420**, `dpw8-v1-oat15a-committee-grid-primal` 240, `naca4412-converge-lift-coefficient` 220, `naca0012-derive-reference-then-relayer` 200, plus five smaller. **THE SHARPEST SINGLE INSTANCE, because it is the entry of record's own producer:** `agenda/proposals/w3-qcr-forward-on-the-ducts-is-the-rank-1-route.json` reads `"status": "proposed"` at **95 core-min**, created 2026-08-05T17:35:00Z -- and `demo-output/website/closure_challenge_round5_qcr.json` names that id as its own `"item"`. **It ran on 2026-08-05/07, it produced the round-5 entry of record at 0.056647, and `docket.json` carries it `"status": "done", "closed_at": "2026-08-07T20:45:02+00:00"`.** **AND A SECOND, WORSE IN KIND BECAUSE THE SURFACE IS SILENT RATHER THAN STALE:** `agenda/proposals/s1-whitened-reinversion-does-the-correction-move-when-sensitivity-is-divided-out.json`, **340 core-min**, `"status": "proposed"`, carries **no `decision_note` field at all** and `/usr/bin/grep -c 'D45\|D68\|enabling premise'` over it returns **0** -- while **D45 is a docket row filed about that exact item**, headed *"A filed 340 core-min item's enabling premise is measured false"*, and `dafoam/S1_ZEROCOMPUTE_TRIAGE_2026-08-14.md:86` puts it in class (c). An agent opening it meets a clean, priced, gated item with a full launch prompt and nothing saying its premise is dead. **NOBODY HAS MEASURED THIS AND NOTHING CHECKS IT.** `git grep` for any prior statement of a proposal/docket status divergence returns **one** hit, `campaign/COLD_START_TEST_2026-08-11.md:36`, which lists both surfaces as things a cold reader should open -- a checklist, not a measurement. No script joins them: `scripts/calibration_scorecard.py` and `scripts/add_proposals_supervisor_review_2026_08_07.py` read the inbox and never open `docket.json`. **Closure/S1-line subset of the two costly directions: 6 items, 868 core-min.** Zero compute; both surfaces are tracked JSON. | Closure three-strand re-establishment, 2026-08-15, `campaign/CLOSURE_THREE_STRANDS_2026-08-15.md` S1b, repo anchor `54a562a3`; generalised from D213 after two further instances appeared on the same line | **This row does not rule which surface is authoritative -- that is D38 item (5), and it is unresolved.** What it supplies is the measurement that row has never had: **the collision is not latent, it is 45.3% wrong on the field that dispatches work.** Three separable actions, in increasing order of what they settle. (1) **A join check, which is cheap and catches the whole class**: any id present in both surfaces whose `status` differs is a fault, and no such check exists -- the two scripts that read the inbox never open `docket.json`. Note the honest limit: a checker cannot repair a disagreement it cannot adjudicate, so it must report, not reconcile. (2) **The 189 docket records with no proposal file and the 56 proposals with no docket record are NOT necessarily faults** and this row does not claim they are -- they are unmeasured, and whoever rules on (1) should say which of the two surfaces is allowed to hold an item alone. (3) **The 15 `proposed`/`done` items should be closed on the inbox first**, because that is the direction that wastes a whole dispatch: an agent picks up finished work, and the cheapest instance of that already happened -- `duplicate-check-at-intake-against-open-and-closed-items` records that 13 of 114 closed items scored bottom-of-scale and were *overwhelmingly one shape, work that was ALREADY DONE when the item was created*. That row diagnosed the symptom at intake and could not see the cause, because the cause is that the inbox does not know what the docket closed. **The transferable form, and it is why D213 should not be repaired one file at a time: a proposal JSON is a dispatch surface with no owner for its status, and every narrative document that kills an item writes prose instead of writing the surface.** | chief (which surface is authoritative -- D38 item (5)); fleet (the join check); owner of the proposal inbox (the 15 finished items still advertised) |
| D219 | **D218's 45.3% is re-derived and confirmed exactly, its ruling is made, and its two costed directions cover only 25 of the 34 disagreements it counts.** Re-executed independently at `87c19742`, joining `demo-output/website/agenda/proposals/*.json` to `demo-output/website/agenda/docket.json` on `id`, reading both surfaces RAW (never through `agenda.read_inbox`, which silently coerces an unknown status to `proposed` and would have hidden two of the faults). **FRAME:** inbox **131** files, 131 unique ids, zero parse failures, zero filename/id mismatches; docket **264** records, 264 unique ids, zero duplicates; **shared 75**; **agreeing 41**; **disagreeing 34 = 45.3%**; **present on ONE surface only: 245** (inbox-only 56, docket-only 189). Every D218 number reproduces to the digit. **THE THIRD CLASS, which the headline does not cover and which this row measures for the first time:** the 56 inbox-only ids carry 49 `proposed`, 5 `done` and 2 `approved` -- so **five items are recorded finished on a surface the docket has never heard of** -- and the 189 docket-only records carry 84 `done`, 57 `proposed`, 35 `approved`, 13 `dismissed`. Three inbox files are refused outright by `read_inbox` and can never reach the docket (`dafoam-restore-ksp-options-escape-hatch`, `f5c-unsteady-probe-run`, `test-the-solver-default-relaxation-across-the-family`), and exactly one more is blocked by an objective collision (`tmr-naca0012-verification` against `agp-4369c99cdd7f`). **D218's TWO DIRECTIONS ARE 25 OF 34; THE OTHER NINE ARE 1,381 CORE-MIN AND ARE NOT IN ITS TEXT:** `approved`->`done` 4 items 64 core-min, `done`->`approved` 2 items 475, `done`->`proposed` 1 item 35, `queued`->`approved` 1 item 480, `queued`->`done` 1 item 327. **A SECOND FAULT CHANNEL NOBODY HAD NAMED: `queued` is not a status.** `agenda.STATUSES` is the closed set (`proposed`, `approved`, `approved-queued`, `dismissed`, `done`); two inbox files carried `queued`, which `read_inbox` coerces to `proposed`, making it invisible to every consumer that goes through the module and visible only to an agent opening the raw file -- which is what an agent does. **D218'S OWN CLAIM THAT NOBODY HAD MEASURED THIS IS FALSE, and the prior measurement is better than the one that superseded it.** `docs/MEMORY_ARCHITECTURE.md:636`, written **2026-08-10 at `975d4098`, five days earlier**, carries defect **D-1** with the identical arithmetic (*'YES, 45%. 34 of 75 shared ids disagree ... 189 docket records have no file'*), names the MECHANISM D218 does not (*'refresh_docket() at agenda.py:1197 skips any id already on the docket, so a file is never re-read after first merge'*), enumerates the three file-ahead records by id, and rules on them. Row 594 of the same file answers the authority question in terms: *'Neither is currently authoritative ... an agent that confidently picks one has failed.'* D218's `git grep` returned one hit because it searched for a statement of divergence, not for the row that had already measured it. **THE SHARPEST SINGLE INSTANCE D218 NAMES IS NOT A DISAGREEMENT AT ALL:** `s1-whitened-reinversion-does-the-correction-move-when-sensitivity-is-divided-out` (340 core-min, the D45 item) **has no docket record**. It is inbox-only, so it is in the third class, and **no join check can ever see it** -- the surface is silent rather than contradictory, which is the harder defect and a different one. **AND A CLASS BELOW BOTH: BOTH SURFACES WRONG TOGETHER.** `r2-closure-coefficient-uncertainty` reads `proposed` on the inbox and `approved` on the docket while `demo-output/website/r2-closure-coefficient-uncertainty/report.json` names that proposal file as its own `agenda_entry` and records the work **finished**: stage one 10 of 10 samples at **26.91 of 60 core-min**, band 0.02040 Cd = 4.90% of baseline and MATERIAL against the 0.00053 numerical channel; stage two 20 Latin hypercube samples, `ALL_DONE` 2026-07-26T08:33Z. A join measures agreement, not truth, and two surfaces agreeing are still capable of agreeing on something false. | Re-derivation and reconciliation, 2026-08-16, repo anchor `87c19742`; check and controls at `e0b24366`, reconciliation at `73a42b03`. Generalised from D218 and D38 item (5) after the join was executed rather than read | **THE RULING, which is what D38 item (5) has been missing and which a one-off reconciliation would not have supplied: `docket.json` IS AUTHORITATIVE FOR `status` ON ANY ID IT HOLDS, and a proposal file's `status` is INTAKE-ONLY -- load-bearing before first merge, non-authoritative after it.** Four legs, every one read out of code rather than out of preference. (1) `agenda.set_status` is the only decision-recording function in this tree and it writes `docket.json` alone; every control-room action routes through it (`server.py:554,566,575,604`). (2) **No code path anywhere writes a proposal file's `status`** -- every inbox status ever changed was changed by hand. (3) `refresh_docket` already declares the winner in its own docstring, *'existing entries always win the merge: a decision is never overwritten by a re-draft'*, and skips any id already on the docket, which IS the drift mechanism. (4) `docket_view`, what the control room serves, is the docket; the inbox is served nowhere. **AUTHORITY IS NOT EVIDENCE, and that is a limb of the rule rather than a counter-example to it:** where the FILE carries an `outcome` the docket lacks, the file holds a record the docket lost, and the repair is to move it INTO the docket, never to promote the file to a dispatch surface. This overrules `MEMORY_ARCHITECTURE.md:594`'s *'neither is currently authoritative'* by supplying what that row asked for, a ruling, and it is the chief's to make under D218; Katie can overrule it, and until she does the surfaces have a precedence order for the first time. **RECONCILED, 34 of 34, each against a bar stated before the write and none skipped.** Class A, **20 items**: docket `done` with a non-empty `outcome`, which is `set_status`'s own precondition (*'a proposal marked done must carry an outcome'*); the file takes the docket's status and that outcome. This is the 463 core-min that was advertised for dispatch, including `w3-qcr-forward-on-the-ducts` at 95 core-min, which produced the round-5 entry of record at 0.056647 and had read `proposed` for nine days. Class B, **11 items**: docket `approved` carrying `decided_at` and `decision_note`; all eleven are one batch, 2026-07-31T22:18:09Z under Katie's standing authorization, so the 7,650 core-min was authorised by a single recorded act. Class C, **3 items**: file `done` with an `outcome` and `measured_core_min` that the docket lacks -- `s1-cbfs-objective-repair-and-reinversion` (424.80 of 450), `f6b-model-form-matrix-on-the-hills` (29.18 of 35), `kfamily-fpe-shared-diagnosis-bump-and-hills` (3.54 of 25) -- each citing a pre-registration commit that exists (`223c3b84`, `28e14422`, `0be92103`, `d05b83c3`) and a record file on disk; their closures move into `docket.json`. **NOTHING WAS MARKED `done` THAT COULD NOT BE SHOWN FINISHED**, and the two `queued` values went with the rest. **TWO ITEMS RECONCILED ON THE JOIN AND LEFT OPEN FOR KATIE, marked in the files themselves with an `owner_decision_pending` field rather than only in prose.** (i) `r2-closure-coefficient-uncertainty`: both surfaces are stale against a completed report; closing it needs an `outcome` sentence, which is a claim about what the work found and is hers, not a reconciliation's. (ii) `tmr-naca0012-complete-ladders`, **480 core-min**: the join is settled (an illegal `queued` against a recorded approval) but `demo-output/website/tmr/C4_naca0012_closure.json` records that the medium and fine rungs' wake-driven oscillation **cannot be reached inside that 480-core-minute budget** and that the honest next step is a budget or method reset -- reprice, retire, or leave approved is a decision, not bookkeeping. **THE CHECK: `scripts/check_docket_surface_agreement.py`, read-only, three-valued, admitted by `lab_check.py`'s AST predicates with no wiring because enumeration there is derived.** Zero shared ids is UNKNOWN with a reason and never PASS (class B1); so is an unreadable docket, a missing inbox directory, and one unparseable proposal file -- the last for the WHOLE RUN rather than as a skipped row, because a join that drops what it cannot read reports agreement over a smaller set than it claims. The frame prints every run: ids per surface, shared, agreeing, disagreeing, present-in-one-only, and the disagreements priced and split by direction, never summed. **The 245 one-sided ids are counted and NOT judged**, because D218 item (2) says no rule exists on which surface may hold an item alone and a check that invented one would be legislating. **Both controls (L-84):** a planted disagreement fires; a legitimately one-sided id whose statuses WOULD disagree if joined does not fire and is still printed. **Six mutations, all killed, each by named probes, judged by FAILURE-COUNT DELTA against a control asserted green first -- never by `returncode != 0`, which scores every survivor a kill once the control is red.** Mutants are substituted COPIES in a temporary tree; no tracked file is ever held mutated. **The mutation proof caught a defect in my own probe and that is the reportable part:** inverting `!=` to `==` in the comparison SURVIVED, because the probe asserted a FAIL verdict and a disagreement count of one, and the inverted check reports the AGREEING id as the disagreement, which keeps both. The probe now names the id. A mutation proof shows a test detects *a* change, not the right one. **MEASURED BOTH WAYS: 34 of 75 (45.3%, exit 1) before, 0 of 75 (0.0%, exit 0) after, with the frame otherwise unchanged; `read_inbox` refuses exactly the same 3 files with exactly the same violations after the write as before, which is the control that no reconciliation broke a style rail.** **THE INSTRUMENT'S LIMIT, written into its own docstring rather than left for a reader to discover: it measures agreement between two surfaces, not the truth of either.** The `r2` class -- both surfaces wrong together -- and the `s1-whitened-reinversion` class -- one surface silent -- are both structurally invisible to it, and both are worse than what it catches. **WHAT IS NOT DONE:** nothing enforces the ruling in code. `refresh_docket` still skips a merged id forever, so an agent hand-editing an inbox `status` tomorrow re-opens this at the same rate; the ruling is a rule with a sweep, which `SUPERVISION_CHARTER.md:136` says is the minimum, but not yet a rule with an enforcement. The 245 one-sided ids remain unruled. And `docket.json:generated_at` still reads 2026-08-08 against a file written today, which is D-1's other half and was deliberately not touched here. | Katie (the two `owner_decision_pending` items, and whether to overrule the authority ruling); chief (whether `refresh_docket` should become a two-way merge, and the rule for one-sided ids); fleet (nothing outstanding -- the check is committed and admitted) |
| D220 | **Six commit captures in one evening, re-counted by execution: FIVE are commit captures, one is a worktree re-application that never reached a commit, and one of the six SHAs is an amended duplicate of another. And the check that would have caught them is `scripts/hunk_check.py`, shipped here at `8ba32c27`.** **THE RE-COUNT, because the tally itself had drifted.** `fb30e00f` (21:49, two harness mutants into `scripts/self_audit.py`, live in HEAD 65 min), `0a3e82d7` (22:54, census mutant RC10 into the commit repairing `fb30e00f`), `1aa90081` (23:01, a fifth docket row in a four-row commit), `49b36e57` (23:43, D213-D216 written by other agents), `d6ff862d` (23:44, the chief's CHIEF ACCEPTANCE block into an agent's commit about a docket join) -- **five**. The sixth named in the records, the census `finally` re-applying RC10 over the chief's restore, was caught by scanning the file before the `git add` and **never became a commit**; and `c75f6198`, counted separately in two records, has the **same tree `d39087fb` and the same parent `c699b8cd`** as `49b36e57` -- it is an amend, not a second capture. **TWO GIT FACTS, MEASURED IN A SCRATCH REPOSITORY BEFORE A LINE OF THE CHECK WAS WRITTEN, and the first one decides the whole design.** (1) **`git commit -F msg -- <paths>` takes those paths FROM THE WORKING TREE AND IGNORES THE INDEX.** Staged 1+/1-, a concurrent agent appended one line, committed with the pathspec: `git diff --cached` still read **1+/1-** and the commit landed **2+/1-**. So `git add` is decorative under this lab's mandated commit form, and a check reading the index would be **defeated by exactly the failure it prevents**. (2) Under that same pathspec commit git builds a temporary index holding precisely what it is about to write, exports `GIT_INDEX_FILE` at it and runs `pre-commit` against it -- the hook saw `2 1 f.txt` and `2 1 f.txt` landed -- and does **not** re-read the worktree afterwards. **Installed as a pre-commit hook the window is ZERO; run by hand it is NOT zero, and the module says so.** | built and measured 2026-08-16, this pass, at repo `8ba32c27` | **THE CHECK: the author declares, in one token per path, what they are committing, and the tool refuses when what is there does not match.** `python3 scripts/hunk_check.py docs/DOCKET.md:1` -- **46 keystrokes**, counted with `wc -c` rather than by eye. Declaration, not heuristic: a tool guessing which hunks *look like yours* would grade FORM where the failure mode is AUTHORSHIP. **LINES, NOT HUNKS, and that is the one choice that was measured rather than assumed** -- five docket rows appended on five adjacent lines are **ONE** `@@` hunk at any `-U`, so a hunk count misses three of the five real captures and a line count catches four. **THREE-VALUED AND IT CANNOT PASS FROM AN EMPTY SET (B1):** no declaration, nothing changed at the declared paths, or a `?` on any number, are each **UNKNOWN with a reason**; PASS needs a non-empty declaration, a non-empty actual, and every number graded and equal. Exit contract `0/1/3`, `lab_check`'s. **MEASURED AGAINST THE FIVE REAL CAPTURES, each with a declaration reconstructed from its own author's account: CAUGHT 4 of 5** -- `0a3e82d7` (declared `self_audit.py:1-1`, actual 2+/2-; the chief's acceptance says *"my diff had two hunks and I expected one"*), `1aa90081` (declared 4, actual 5), `49b36e57` (declared 1, actual 5), `d6ff862d` (declared 1+/0-, actual 2+/1-). **MISSED 1**, and the miss is the more valuable half: `fb30e00f` was a **1055+/17-, 43-hunk** write, and no author of a 43-hunk edit can name their own line counts, so the honest declaration is `?` and the verdict is **UNKNOWN, not PASS**. The same boundary hides the worktree re-application at `f4c531cb` (136+/5-). **THE REACH BOUNDARY IS ONE SHAPE: a small foreign edit inside a large intended edit to the same file.** Counting does not reach it and neither does the private index (D221); only editing outside the shared worktree does. **CONTROLS BOTH WAYS (L-84), 20 tests on real `git init` repositories, every one driving the shipped `main()` rather than its helpers.** FIRES on: an extra hunk in a declared file; an extra FILE; five contiguous rows against one declared; a path another agent **STAGED into the shared `.git/index`**; the index-is-not-the-truth case; and both private-index failure modes. DOES NOT FIRE on: a clean single-author commit; two files legitimately intended and declared; a declared removal; a correct private-index commit with two foreign rows beside it; **an unrelated dirty worktree**, because a check that reddens on another agent's unstaged file is a check nobody can pass. **MUTATION CELL ON A COPY, never on the tracked file** -- that trap cost this lab a 65-minute mutant in HEAD. Control **GREEN 20 passed / 0 failed** at sha256 `1470346d`; **M1-M9 judged by FAILURE-COUNT DELTA against the green control and by the NAME of the test that reddened**, never by `returncode != 0`; **all nine KILLED on their aimed test**; tracked file sha256 `1470346d` **UNCHANGED**; control re-run 20/0; `__pycache__` purged before every cell. **THE RUNNER SKIPS IT ON PURPOSE AND THAT IS THE HONEST OUTCOME.** It declares a required positional, so `lab_check.py` classifies it `requires-arguments` -- a reason **not** in `HIDING_PREFIXES`, so it does not downgrade the aggregate. There is no declaration on a schedule; an admitted check that always returned UNKNOWN would exit the runner **4**, which `pre-push` BLOCKS on, making the lab's one gate permanently fail-closed. Measured: `lab_check.py --only hunk_check` -> 1 skipped `requires-arguments`, **0 of them could be hiding a verdict**, test file ADMITTED, **VERDICT PASS exit 0**. **NOT FOR `scripts/installed/pre-push`:** a push carries N commits and no declarations, so there is nothing for the hook to compare against; the only viable hook is **pre-commit**, where the window is measured at zero -- and installing that blocks every commit in a two-agent tree on an untested path, so it is **recommended NOT installed** until the standalone form has mileage. **DOGFOODED, AND THE FIRST USE FOUND A DEFECT IN THE CHECK ITSELF:** run on its own two files it printed *actual nothing* for both, because **`git diff HEAD -- <path>` is BLIND to an untracked path**. It failed SAFE (empty actual -> UNKNOWN) but for the wrong reason, and a check whose UNKNOWNs carry wrong reasons stops being read. Repaired, with the count asserted against **git's own** by committing the file and comparing `--numstat`, no-trailing-newline case included; M8/M9 pin both. The second dogfooding result is a limit, not a defect: **this instrument grades a DELTA, so on a pure new-file commit the additions are not independently knowable and PASS is unavailable** -- what stays load-bearing there is the path SET and the zero removals. | fleet (use it); chief (whether pre-commit is installed) |
| D221 | **The private-index commit technique works and has three failure modes, one of which is STRICTLY WORSE THAN THE CAPTURE IT PREVENTS: it can silently DELETE another agent's committed work. Measured; and the two-token repair is `git update-ref`.** The technique, from `c699b8cd`: build the blob you intend (HEAD's version of the file plus your own row), point `GIT_INDEX_FILE` at a private index, `git read-tree HEAD`, `git update-index --add --cacheinfo`, commit from it. Reproduced in a scratch repository: **two foreign rows sat in the worktree beside the author's and neither was captured** -- the commit is 1+/0- and the worktree keeps all three. It is a **structural fix**: it removes the window instead of noticing afterwards that something crossed it, and it is the right primary. **FM1, THE SEVERE ONE.** If HEAD moves between `git read-tree HEAD` and the commit, the private index is a snapshot of the OLD tree and the commit **reverts everything the intervening commit did**. Executed: a concurrent commit added `n.txt` and a line to `o.txt`; the private-index commit that followed landed `1 0 d.md`, **`0 1 n.txt`, `0 1 o.txt`** -- `n.txt` gone from the tree entirely. A capture ADDS someone else's work to your commit and loses nothing; **this DELETES it**. **THE REPAIR IS ATOMIC AND COSTS TWO TOKENS:** `git write-tree` + `git commit-tree -p $H` + **`git update-ref refs/heads/main $C $H`**, whose third argument is a **compare-and-swap**. Executed under the same race: `fatal: cannot lock ref 'refs/heads/main': is at 9846c995 but expected 84622855`, **HEAD unmoved, nothing lost**. A `[ "$(git rev-parse HEAD)" = "$H" ] && git commit` guard is NOT equivalent -- it reopens the same check-then-act window one level up. **FM2: it silently drops the author's OWN second edit.** The recipe is *HEAD's version plus my row*, so any other change the same author made to that file is discarded. Executed: an author who fixed a typo AND appended a row committed **1+/0-**; the typo fix stayed uncommitted while the worktree looked correct, and under a fleet where the docket is permanently dirty `git status` does not surface it. **FM3 fails SAFE:** a path absent from HEAD errors loudly -- *`cannot add to the index - missing --add option?`* -- rather than dropping the file. | evaluated by execution 2026-08-16 against `c699b8cd`, in scratch repositories under this session's scratchpad | **PRIMARY AND BACKSTOP, and they are not rivals.** The private index **prevents** capture; `hunk_check` (D220) **detects** any mismatch, including this technique's own FM1 and FM2 -- both are asserted as tests in `sdk/tests/test_hunk_check.py` and both **FAIL** under it, FM1 by naming the two reverted paths as *changed and NOT DECLARED*. **So the backstop's most valuable catch is the primary's worst failure**, which is the argument for keeping both. **REACH, on the same five real captures (D220's re-count), scored the same way:** the private index **prevents 4 of 5** -- `1aa90081`, `49b36e57`, `d6ff862d` (docket appends, where *HEAD + my row* is exactly right) and `0a3e82d7` (a 1-line restore reconstructible against `9b9951a1`) -- and **misses `fb30e00f`**, a 1055+/17- 43-hunk write whose intended blob only exists in the shared worktree. **Both mechanisms miss the same one, and it is the same shape both times**: a small foreign edit inside a large intended edit to the same file. Neither counting nor blob construction reaches that; **only editing outside the shared worktree does**, which is D210/D211's `mutate_safe.py` direction generalised from harnesses to authors. **COST, on the standard D212 set:** the declaration check is **46 keystrokes** on one line and pastes into a dispatch brief unchanged; the private index is **five commands and roughly 250 keystrokes**, needs a shell variable and a temporary blob file, and in its SAFE form needs `commit-tree` and `update-ref` as well -- it is a **wrapper script, not a one-liner**, and shipping it as a one-liner is how FM1 gets into the fleet. **NOT written here:** that wrapper is the inventing agent's to write, with the CAS in it, and this row is the failure-mode measurement it should be built against. **The 59-survivor census figure is not quoted anywhere in this pass; it is a floor of >=61, not a figure.** | the agent who built `c699b8cd` (the CAS wrapper); chief (which of the two is mandated in dispatch briefs) |
| D222 | **V5 re-graded FAIL by a non-author, and the block is the THIRD generator: the frame ruling's own load-bearing surface class, unrepaired.** The ruling `3af7bd2b` kept V5 open on generator 1 with the words *"a generator is the most load-bearing surface there is -- it manufactures new copies after every repair"*. Generator 1 (`sdk/scripts/build_benchmarks.py:115-117`) is now repaired and names Spalart (2000) beside `Ccr1 = 0.3`. **`sdk/scripts/closure_eval_battery/build_master_table.py:246,:371` is not**, and it emits the untrained claim about OUR OWN model into two tracked surfaces (`closure_eval/closure_eval_master_table.md:20` and `.json:84`) with **zero** occurrences of Spalart in either. Fourth in-frame unmet site: `campaign/LADDER_V_RUNGS_V2_V7_V10_2026-08-08.md:121`, *"duct gains from the untrained QCR2000 term (nothing fitted)"*, a site under BOTH the literal and the narrowed predicate, returned to frame by the enumeration's own withdrawal and unrepaired since. **AND A FRAME HOLE NO ARM OF THE SWEEP CAN SEE:** `demo-output/website/closure_challenge_round5_qcr_forward.json` makes the same claim as JSON structure -- `"Ccr1": 0.3`, `"trained": false` -- carries zero occurrences of the string *untrained*, and matches NEITHER the sweep pattern `untrained[- ]QCR` NOR the attribution guard's own regex-plus-sentence-splitter predicate. Its exclusion is unaffected (the container ground holds) but the rung's face states *"the two frozen JSONs"* are among the 15 zero-Spalart claim files and **only `MANIFEST.json` is**. A claim expressed as structure rather than prose is invisible to the whole instrument. | Four arms re-derived by this grader, `__pycache__` purged before each cell, 2026-08-15T23:53Z-2026-08-16T00:04Z: **tracked** 20,709 files by `git grep -a` (not `-I`, which skips ~1,476 files marked binary), 47 claim files, **13** zero-Spalart -- reconciles exactly with the enumeration's 15 taken at 20:14Z/20:22Z, since the two files repaired at `f956e348` (20:23:09Z) now carry Spalart: 15 minus 2. **Untracked** 1 file, 0 hits. **Gitignored** 37,254 files, 3 hits, all under `dist/`. **Run tree** enumerated ONCE by `/usr/bin/find -print0` with NUL bytes counted (`-size -2M`/`+2M` are not complementary), 132,050 files / 66 GB, 9-minute grep, **1 hit and it was the seeded positive control**, since removed and confirmed absent -- so the zero is a measurement. All four refusal sha256s on the rung's face recomputed on the worktree and against `git show HEAD:` and **all four match byte for byte**; commit histories reproduce. Repairs verified with the guard's OWN regexes and sentence splitter applied file-wide: PRODUCT_LIST 17 QCR-naming sentences and 13 unattributed (reproduces 17 to 13), CLOSURE_EVALUATION_PROTOCOL 1 to 0 (reproduces); all 13 survivors opened and none carries an untrained claim about our model. | Name Spalart (2000) with the year in the same sentence at `build_master_table.py:246` and `:371`; re-run the eval battery so both tracked outputs carry it; add an L-44 dated addendum to `LADDER_V_RUNGS_V2_V7_V10_2026-08-08.md` by its owner. **Then re-run the four arms with a seeded positive control: if the tracked arm returns zero in-frame zero-Spalart surfaces carrying the untrained claim about our own model, this FAIL is wrong.** Separately, widen the claim pattern to reach structured expressions (a `trained` key with a false value beside a QCR coefficient) or record on the rung's face that the sweep cannot see them. | D128's owner for the generator; the owner of `LADDER_V_RUNGS_V2_V7_V10` for its addendum; chief for whether a structured claim is in the sweep's frame |
| D223 | **V14's "needs a human reading" concession does not survive execution: the obstruction is a NEWLINE IN A CHARACTER CLASS, not board arithmetic, and the same rule already grades the same quantity at eleven tracked sites.** The ruling `9b9951a1` records `:342`'s correction of the best-on-board count as *"structurally unreachable by any board arithmetic"* because the digit is ambiguous between the withdrawn count and the cases-won count against the leader, and asks the rung to say so. **Three findings against it.** (i) `_BEST_COUNT` faults that exact quantity carrying that exact wrong value at eleven tracked sites, including `campaign/LADDER_V_RUNGS_V2_V7_V10_2026-08-08.md:120` whose sentence is the identical claim in Markdown; if it needed a human reading none of the eleven could have been decided by a rule, and all eleven were. (ii) The pattern is `\bbest\b[^.\n]{0,80}?...` and in the archive member the sentence WRAPS: the noun phrase ends one line and the count begins the next, a gap of **25 characters -- well inside the 80-character budget** -- containing a newline the class excludes. Executed: no match on the shipped fragment, match on the identical text with the newline replaced by a space. (iii) The ambiguity argument treats the claim as a bare ordinal; the sentence NAMES its own quantity and the rule binds on that noun phrase -- which is exactly the *grade only what the sentence names* design the same commit used to justify grading the bare branch's denominator and not its numerator. **An instrument defect was written into a ruling as a property of the world.** It is the same failure as the 300-character board-context window: a constant calibrated on flowing Markdown, applied to wrapped HTML. **Mitigating: the claim it excuses is already struck, dated and corrected at its tracked source**, so the concession cost the corpus nothing -- it is wrong as reasoning and should not stand on the rung's face. | Executed by a non-author 2026-08-16 against HEAD. `_BEST_COUNT` run directly on the archive member's bytes and on the same fragment with the newline substituted; eleven faulting sites enumerated from a live `check_rank_claim_values` run (28 value faults, 118 matched-and-not-graded). **Refutation 1 independently confirmed** with two of its three numbers corrected: board-context distances measured here are 1,219 / 1,443 / 1,319 characters against D206's 1,224 / 1,448 / 1,319, a measure-to-start versus measure-to-nearest-edge convention on a 5-character word; substance and conclusion untouched, and the refusal to widen the shared 300-character constant was correct. **Refutation 3 re-derived from primary sources** -- the live board read by AST and the per-case block read from the entry of record -- giving cases-won {4,4,5,5,7,7}, best-on-board two and earned zero, so the admissible union is {0,2,4,5,7}: **five of the nine integers, not seven**. D199's union does not reproduce. | Strike the *structurally unreachable / needs a human reading* sentence from V14's face and replace it with the measured cause. Then decide the instrument question separately and on its own merits: whether `_BEST_COUNT`'s gap class should tolerate a single line break (`[^.]` with a bounded newline allowance) or whether wrapped HTML should be line-joined before value rules run. **Do not widen it to make this one control pass** -- measure the cost on both arms first, exactly as the widening pass did for the 300-character window and declined. | the owner of `check_rank_claim_values` and `check_rank_claim_surfaces`, who owns both shared constants; chief for the ruling's face |
| D224 | **V14 graded PASS WITH RESIDUALS: every claim the ruling said was live is repaired at its tracked source, and all three faults on the gating arm are a shipping bundle built BEFORE the corrections landed.** The denominator ruling's reach/gating split is re-measured and holds -- travelling faults three with **zero false positives**, against 25 lab-record faults which are overwhelmingly correct dated records of the four-entry board, so repo-wide gating really would be unusable rather than stricter. The ruling's own falsifier (*a travelling surface whose correct claim this predicate faults*) is **not triggered**. But the three travelling faults all sit in `dist/certonomous-demo.zip`, built **2026-08-14T21:16:50Z**, while its tracked source `demo-output/website/closure.html` was last committed **2026-08-15T19:53:29Z** and carries every one of those claims struck, dated 2026-08-15 and corrected -- the ordinal to fourth of seven, the count to two, the placement to rank one of seven entrants, and the seed bound to 177% of the margin rather than comparable to it. `benchmarks.html:148` likewise now reads *larger than the margin, not comparable to it*. **The residual is a rebuild, not a claim repair, and `dist/` is the owner's.** | `check_rank_claim_values` re-run by a non-author 2026-08-16T00:08Z: 28 value faults (widening pass reported 26), travelling **3** (reported 3 -- reproduces exactly), lab records 25 (reported 23), matched-and-not-graded 118 (reported 117), 503 rank claims, 20,713 tracked paths considered and 20,526 opened including 90 archive members. The plus-two on the lab-record arm is the D199 row and its own write-up, both committed after the widening was measured -- **the instrument faulted the record of its own measurement**, which is correct behaviour and is why the gate is travelling-scoped. **THE INSTRUMENTS CANNOT CONFIRM THE RUNG AND THIS IS REPORTED AS A FACT ABOUT THEM:** `board_placement_faults` returns **0 rule-A and 0 rule-B faults** on the shipping member -- it finds four placements, three with no named published entrant (skipped at `who is None`) and the fourth agreeing with the pin, confirming D151 by execution; it has no arithmetic predicate (D88), its 400-character skip additionally requires the same entrant be named (D85), and it masks nothing, since raw archive text was passed and the strikes were never stripped (there is no strike stripper, `29452c51`). `check_rank_claim_surfaces` returns **WARN**, its frame line confirms it opens the archive's 90 members, and **not one of its 33 detail items names a `dist/` archive path**. Every conclusion above rests on re-derivation instead. | **Rebuild `dist/certonomous-demo.zip` from the current tracked site and re-run the value check. If the travelling arm does not go to zero, or if any TRACKED travelling surface faults, this PASS is wrong and V14 is FAIL.** Five further residuals stand and are named on the grade's face: the 300-character window, the `_BEST_COUNT` newline (D223), the same rule's unbounded digit boundary before *eight* which would admit a decimal, D208's confirmed execution gap on the four-decimal margin at `closure.html:411`, and the **entirely unmeasured PDF arm** -- no PDF claim is graded in this document, because a text extract shows strike-and-keep markup as live text and rendering to PNG was outside this pass's time. | `dist/`'s owner for the rebuild; chief for whether a stale bundle blocks a rung whose criterion names `dist/` as search frame; a later pass for the PDF arm |
| D225 | **The ruling gets an enforcement, and the 245 one-sided ids get the rule nobody had written. Of the 245, 235 are LEGITIMATELY one-sided by construction and 10 are faults -- and the sharpest instance D218 named is in neither class, which is the finding.** **THE RULE, and every clause of it is READ OUT OF `chief_engineer.agenda` rather than invented, because D218 item (2) is right that a check which legislates is not measuring: EVERY RECORD OF A DECISION MUST REACH `docket.json`. A surface other than the docket may hold an item alone only while that item carries NO decision; the moment any non-docket surface records one -- a status past `proposed`, or an artifact naming the item -- the docket must carry it too, and the repair is always to move the record IN.** **Its four corollaries and the code that makes each true.** (1) **DOCKET-ONLY IS LEGITIMATE BY CONSTRUCTION**, because `draft_all()` has five sources and FOUR are code drafters that write no file at all. Measured, and this is the number that decides the whole rule: **189 of 189 docket-only records carry a drafter `source_kind`** (report 47, capability 34, measurement 31, gate 29, ledger 25, challenge 11, reading 10) **or predate the field (2); ZERO carry `source_kind: inbox`.** A rule that called one-sided ids faults would have manufactured 189 of them. (2) **INBOX-ONLY AND STILL `proposed` IS PENDING and is not a fault**: `refresh_docket` merges any inbox id the docket does not hold, so it is one refresh away and nothing is lost meanwhile -- **46 items**. (3) **INBOX-ONLY WITH A DECIDED STATUS IS LOST**, because `set_status` is the only decision-recording function in this tree and it writes the docket alone, so the decision exists nowhere any consumer can act on -- **7 items, D219's five `done` and two `approved` exactly**, worst `s1-cbfs-weighted-reinversion-arm` at **267.1 measured core-min**. (4) **REFUSED OR OBJECTIVE-COLLIDED IS BLOCKED/SHADOWED, a different fault: not lost yet, UNABLE EVER TO ARRIVE** -- 2 refused by the style rails and 1 shadowed (`tmr-naca0012-verification`, **604 core-min**, whose objective `agp-4369c99cdd7f` already holds, so `refresh_docket` skips it forever while both look live). **NOTHING IN THE RULE DELETES A RECORD**: every clause is a classification and a named repair, which is the constraint that made it hard. **AND THE INSTANCE THAT BREAKS THE HEADLINE, reported because it is the honest result: `s1-whitened-reinversion-...` (340 core-min, the D45 item), which D218 called its sharpest and D219 called inbox-only, classifies PENDING -- LEGITIMATE.** It is `proposed` on a surface that has not merged yet, and that is all the surface question can say about it. **Its actual defect -- D45 measured its enabling premise false and the file says nothing -- is not a surface-coverage defect at all, and no join and no coverage check will ever see it.** That is a third instrument, not a wider one. | built and measured 2026-08-16 00:19-01:0x UTC at repo anchor `b2668906`, re-deriving D218/D219 by execution before changing anything: 131 inbox ids, 264 docket, 75 shared, **0 disagreeing (was 34)**, 245 one-sided -- every D219 figure reproduces | **THE ENFORCEMENT, three changes in `sdk/chief_engineer/agenda.py`, because D219's own last paragraph says the ruling had a sweep and no enforcement and an agent hand-editing an inbox status tomorrow re-opened it at the SAME RATE.** **E1, `queued` SETTLED AS REFUSED:** `read_inbox`'s `status if status in STATUSES else "proposed"` is gone and `status_violations` refuses the value loudly into `refused_inbox()`, naming the near-miss (`queued` -> `approved-queued`). Refused rather than admitted because `approved-queued` already carries the meaning and is already graded by `OPEN_STATUSES`, and because **`set_status` raises on anything outside the vocabulary, so the value was ALREADY unactionable -- coercion added only the pretence that it was actionable as something else.** Measured: **zero of the 131 live files are refused by the new rail**, so nothing standing broke. **E2, THE MERGE IS TWO-WAY FOR EVIDENCE AND ONE-WAY FOR AUTHORITY:** `refresh_docket` no longer `continue`s the instant an id is known -- that skip IS the drift mechanism D-1 named. It now carries `outcome` and `measured_core_min` from the file INTO the docket, **additively, never overwriting, and `status` is absent from `_EVIDENCE_FIELDS` so the ruling holds by CONSTRUCTION rather than by a conditional somebody can edit.** Two live shared ids gain a measured cost the docket lost (`s1-cbfs-field-inversion-run` 335.98, `n-a10-third-member-...` 1.07). **E3:** `measured_core_min` was DROPPED at intake while 37 docket records carry it, so E2 would have had nothing to carry. **A DEFECT FOUND IN MY OWN ENFORCEMENT AND IT IS THE REPORTABLE PART: `_carry_evidence` mutates the very dicts `load_docket` returned, so `result != existing` compares them against themselves and reads EQUAL.** The carry would have happened in memory, been reported by `carried_evidence()`, and NEVER REACHED THE FILE -- a repair that says it ran. Fixed with `or carried`; pinned by M3, which asserts against the file on disk rather than the return value. **WHAT I REJECTED, and why each is worse than what shipped.** (i) **Refusing any hand-edited inbox `status` at intake** -- rejected because refusal removes the file from every consumer, converting a LOUD disagreement into SILENCE, and silence is the harder class this same pass exists to attack. The narrow form survives only where the value can never become a decision anyway (E1). (ii) **Writing the docket's status back into the file on every refresh** -- rejected because it destroys the evidence that a divergence happened, and D219 class C shows the file is sometimes AHEAD: a write-back would have silently erased three genuine closures before anyone read them. (iii) **Auto-promoting a file's terminal status into the docket** -- rejected because D219's three file-ahead closures each needed a named pre-registration commit checked to exist first, and automating it closes items on the filer's say-so and defeats `set_status`'s own outcome precondition. **Carrying the outcome forward is what MAKES a later close possible; it is not the close, and M4 is the mutation that proves the difference is one word in a tuple.** (iv) **Scheduling the existing sweep more often, or installing it pre-commit** -- rejected on the brief's own ground and on D220's: a rate-unchanged defect detected sooner is still rate-unchanged, and a pre-commit hook blocks every commit in a two-agent tree on an untested path. **THE CHECK: `scripts/check_proposal_surface_coverage.py`, read-only, three-valued, admitted by `lab_check.py` with no wiring, exit contract 0/1/3.** Live: **FAIL, 10 one-sided faults and 2 unabsorbed**, against 235 legitimately one-sided. **IT SHIPS RED ON PURPOSE**: every one of the 12 repairs is a claim about what work found, which is the owner's or the item's author's and not a reconciliation's -- and the pre-push hook is not installed, so nothing is blocked today. **IT REACHES THE BOTH-SURFACES-WRONG-TOGETHER CLASS, AND THE REACH IS MEASURED RATHER THAN CLAIMED.** Some artifacts name their own agenda item in a machine-readable field (`item`, `agenda_entry`), which is a THIRD surface and the only one that is evidence work HAPPENED rather than a record of what somebody believed. **Scanned 506 JSON artifacts under `demo-output/`: exactly FOUR distinct agenda ids are named by one, 1.5% of the docket** -- and over those four it separates cleanly, the two closed items carrying an `outcome` that quotes their artifact's own finding and the two open ones carrying nothing. **The two it catches are `r2-closure-coefficient-uncertainty`, which is the exact case D219 named as structurally invisible, and `hlpw6-testcase1-coarse-grid-entry`, whose feasibility probe spent compute and corrected the filed estimate while the docket record carries no trace of it.** **THE LIMIT, printed on every run rather than left to a reader: 502 artifacts carry NO back-reference and are invisible here, and completion is never READ out of an artifact** -- the five back-referencing files share no completion field (`verdict` in three of five; the one recording `ALL_DONE` records it nested inside `stage_two_lhs`) -- **so the check says the docket has no trace of work an artifact proves happened, and never that the item is finished.** An empty artifact scan prints `NO STATEMENT AVAILABLE`, which is class B1 applied sectionally. **CONTROLS BOTH WAYS (L-84) AND 15 MUTATIONS, ALL KILLED, judged by FAILURE-COUNT DELTA against a control asserted GREEN first -- never by `returncode != 0`, which scores every survivor a kill once the control is red.** 33 tests on the coverage check (M1 swaps LOST/PENDING, M2 swaps ORPHANED/DRAFTED and would turn all 189 legitimate records into faults, M3-M6 defang each class guard, M7 the empty-set guard, M8 the population, M9 the exit contract, M10 an unreadable surface reading PASS); 23 tests and 5 subtests on the enforcement (M1 restores the coercion, M2 restores the unconditional skip, M3 the aliasing bug above, M4 widens `_EVIDENCE_FIELDS` to `status`, M5 makes the carry overwrite). **Mutants are substituted COPIES -- the coverage check into a temp `scripts/`, the whole `chief_engineer` package into a temp tree -- and no tracked file was ever held mutated.** **EVERY PROBE NAMES ITS ID AND ITS CLASS, not a fault count**, because half these mutations SWAP two classes and a count survives every one of them: that is D219's own probe defect generalised. **Expectations are derived from the specification and from `agenda`'s clauses, never from the file under test** -- and `TestTheRuleIsReadOutOfTheModuleItGoverns` makes that falsifiable by asserting the normalizer, the vocabulary and `set_status`'s write target against `agenda` itself, so the rule reddens here if the code it was read out of moves. **AND THE CORRECTION CARRIED, because it is the same shape as the defect: `docs/MEMORY_ARCHITECTURE.md` is amended to v1.1 and its D-1 row is CLOSED.** D218's *"nobody has measured this"* is false -- D-1 was written five days earlier at `975d4098`, carried the identical arithmetic, and named the mechanism D218 does not. **SIX places in that file carried the superseded *"neither is authoritative"*, not the two the brief names**: the two custody rows in section 1, cold-start question 8, **its calibration note (which still told the next runner to EXPECT question 8 to fail -- a question with a right answer, marked in advance as one with none)**, the D-1 row, and defect 1 of section 9. **D-1's own *"which is right: the file"* is NOT overturned; it is promoted to a limb of the rule** -- authority is not evidence -- **and D-1's other half stays OPEN and is said so: `docket.json:generated_at` still reads 2026-08-08 against a file written today.** A version bump and an amendment log went in with it, because D-2 of that document's own register is a header that stayed stale through six amending commits. | Katie (the 7 LOST closures and the 2 UNABSORBED items -- each needs an outcome sentence, which is a claim about what the work found; `r2` and `tmr-naca0012-complete-ladders` still carry `owner_decision_pending`); the filers of `dafoam-restore-ksp-options-escape-hatch`, `f5c-unsteady-probe-run` and `test-the-solver-default-relaxation-across-the-family` (repair or withdraw -- they can never reach the docket); chief (whether `tmr-naca0012-verification` merges with `agp-4369c99cdd7f` or is reworded, and whether `generated_at` is now worth fixing); fleet (nothing outstanding -- both checks are committed and admitted) |
| D226 | **The ladder's ledger disagrees with the grade of record on NINE of sixteen rungs, and all five rungs that moved on 2026-08-15/16 are among them.** Consolidated by reading each rung's grade document and then its row in `campaign/LADDER_V_TRIPLE_VERIFICATION.md` at HEAD. **V5** `:884` says PASS WITH EXCEPTIONS against **FAIL** in three independent grades (`7ea96c0f` 02:23Z, `2a686b0a` 20:07Z, `6d95f812` 00:20Z) -- 22 hours. **V6** `:885` says FAIL at `377d6afb` against **PASS WITH RESIDUALS** at `60073572`. **V8** `:887` and `:924` say FAIL at `f8c889cc` and *"OPEN -- and it has now failed three times"* against **PASS WITH RESIDUALS** at `2a686b0a`. **V10** `:889` says FAIL on three of five surfaces at `377d6afb` against **FAIL on four named blockers** at `60073572`. **V12** `:892` and **V13** `:893` both say DELIVERED against **PASS WITH RESIDUALS** at `9c2734f8`. **V14** `:894` says PASS as executed against **PASS WITH RESIDUALS** at `6d95f812`. **V15** `:895` stops at round 7 / 25 findings against **round 8 / 30 findings** at `c1a13015`. **V16** `:896` enumerates through round 10 against **round 11** at `a5bbdeca`. **V9** `:888`'s verdict is right and its confirmation column has been wrong for five days** -- it still reads *"the struck sentence was then found still in the shipping archive"*, and the sentence was removed by the 2026-08-10 rebuild. And the GREEN REQUIRES table `:922-931` is stale in **five of its eight rows**. **Eleven of the twelve are one defect: a summary row stale against the section it summarises, with neither edited -- D141, whose base rate this lab measured at 1 in 11 and which is 9 in 16 here.** | Consolidated state of record, `campaign/LADDER_V_STATE_2026-08-16.md`, 2026-08-16T00:29-00:5xZ, by a pass that graded no rung and repaired nothing. Verdicts read from the grade documents; ledger rows read at HEAD; V9's row re-measured by execution against `dist/certonomous-demo.zip` (sha256 `34b8feed...`, 90 file members): both discriminating fragments of the struck prior-art sentence return **0** hits whitespace-normalised, positive control `Closure` firing in **7** members. No solver run, no scoring call, ledger stands at 6, nothing sent. | **Do not repair the ledger row-by-row in the same pass that measures it** -- that is how a consolidation becomes an author of what it reports. A non-author must rewrite the ledger from the grade documents in one pass, and the durable half is structural: **the ledger is edited by hand by whoever is holding it, five rungs moved in six hours by five graders, and no instrument compares a ledger row to the grade it cites.** D141's instrument, built for summary-versus-section inside one document, does not reach a row that cites a document it does not contain. | fleet (the rewrite, by a non-author of the 08-15/16 grades); **chief** for whether the ledger should carry verdicts at all rather than pointers to the grade of record |
| D227 | **PASS WITH RESIDUALS is carried by five rungs and the only entry condition ever written for it has been met ZERO times, on any rung, in the ladder's whole record.** `LADDER_V_TRIPLE_VERIFICATION.md:551-554` is the sole definition: *"When **two consecutive rounds return only findings that would not change an external reader's belief**, the rung closes as **PASS WITH RESIDUALS**."* **Established from the round documents rather than from anyone's assertion: the consecutive-neutral count is ZERO for V15 and ZERO for V16, and no round anywhere has ever declared itself belief-neutral.** V15 rounds 1, 2, 4, 6, 7, 8 each self-declare NOT neutral (round 5 is silent and contributes nothing either way); V16 rounds 5, 6, 7, 11 each self-declare NOT neutral. **V16 rounds 8, 9 and 10 have no documents at all** -- their whole record is commit messages `5e4a0cc5`, `067caac0`, `0c7b968d`, and **none contains the string R-VALUE, belief-neutral, or any neutrality verdict**; the sole claim that round 10 was *scored neutral* is an assertion made ABOUT it by round 11 (`LADDER_V_V16_ROUND11.md:712-713`) and echoed by V15 round 8 (`:619-620`), and both are warnings AGAINST scoring neutrality early rather than records of a neutral round. **Yet V6, V8, V12, V13 and V14 each carry PASS WITH RESIDUALS issued by a SINGLE grade.** This may be exactly what the lab intends, but it is not what is written and nobody has ruled on it. | R-VALUE audit over every V15 and V16 round document at HEAD, 2026-08-16, counting each round on its own words and refusing to count a round that states no verdict. Rule text quoted from `LADDER_V_TRIPLE_VERIFICATION.md:551-554`; per-round anchors in `campaign/LADDER_V_STATE_2026-08-16.md` §5. | **Rule, do not repair.** Either PASS WITH RESIDUALS may be issued by a single grade -- in which case R-VALUE's two-round mechanism is not its definition and the rule must say what it IS -- or it may not, in which case five rungs carry a verdict they have not earned and the send gate is counting them. **This compounds D38(1), which already records PASS WITH RESIDUALS as a sixth gate verdict against `VERIFICATION_CHARTER.md:95` (*"The verdict vocabulary is fixed"*), invisible by construction to the negative-verdict-review sweep of section 16 -- so a verdict nothing authorises is also a verdict nothing reviews.** The cheap partial fix is to require every PASS WITH RESIDUALS to name the two rounds it closed on, which makes the gap self-reporting. | **chief** (the ruling); Katie for the charter half, as D38(1) already is |
| D228 | **A ruling's own count of the arguments it has withdrawn went stale inside four hours, in the same document, with neither line edited -- and it is a ruling about frames, written by the author who has now committed this shape twice inside it.** `LADDER_V_TRIPLE_VERIFICATION.md:150-152` states *"**Four** of my arguments about this rung have now been refuted by execution -- three in the amendment above, and the `MANIFEST.json` concession here"*, written at `78eb5530` (2026-08-15T20:26:20Z) and true then. At `b2668906` (2026-08-16T00:22:34Z) the same author accepts a **fifth**, 298 lines below, at `:449-451`: *"the textual argument **fails** -- the 'untrained is load-bearing' parenthetical attaches to the zero-fitted-parameters clause, separated from the citation clause by **a semicolon**, so the rung's own sentence does not say what I said it says"* -- plus a sixth accepted defect of provenance at `:452-455`, that the ruling issued **3 minutes 11 seconds** after a pass which had measured thirteen unattributed sentences, declined to repair them, and then made exactly those thirteen not-sites. **Counting the same way `:150-152` counts, the number at HEAD is five.** The line says four. **This is exactly the defect the amendment at `:87-92` indicts the ruling for -- *"a claim stale against another region of its own document -- committed by me inside a ruling about frames"* -- committed a second time, in the same document, by the same hand, four hours later.** The stakes are not cosmetic: a reader who inherits *four* will believe the semicolon argument still stands and will grade V5's predicate on a textual ground its author has abandoned. | Rulings audit for the consolidated state of record, `campaign/LADDER_V_STATE_2026-08-16.md` section 8, 2026-08-16. Both passages read at HEAD; commit times from `git log --date=format-local`. All four originally-counted withdrawals re-read and quoted at `:81-85` (W-2 cited backwards, D153), `:87-92` (the frozen-artifact impossibility premise, D154), `:94-99` (generator reach-versus-mechanism, D152) and `:120-128` (the `MANIFEST.json`-alone concession, D167/D168/D169). | Correct `:150-152` to the count at HEAD, or -- better, because the number will go stale again the next time a ruling is graded -- **replace the count with a pointer**: *"the withdrawals are enumerated below and the list is the count."* **The durable half is that this lab now has three recorded instances of a stale-summary defect inside a RULING rather than a report, and a ruling is the one artifact readers are most likely to inherit without re-deriving.** The rung outcomes are unaffected: V5's frame ruling survives on a ground its author did not state, and the predicate narrowing survives because it discriminates -- four in-frame sites survive it and V5 still FAILS. | **chief** (it is his own ruling text; no other hand should edit a ruling's face) |
| D229 | **V10's verdict of record is a FAIL of a tree that no longer exists: its four named blockers were repaired NINETEEN MINUTES after the grade that named them, and no non-author has graded the repair in the five hours since.** The regrade `60073572` (2026-08-15T19:34:11Z) failed V10 on B1 (`closure.html`'s per-case table, two gold OUR MODEL LEADS badges on cases we lose), B2 (`closure.html:480-482`, the disclaimer naming the wrong column and undercounting by one), B3 (`ACTIVE_RESEARCH.md` three live `rank 1 of 5`) and B4 (`ACTIVE_RESEARCH.md` six-where-the-truth-is-seven). The repair `cca64eaf` landed at **19:53:29Z** and its own message says *"V10 is NOT marked green here: the repairing agent may not grade its own repair."* **Verified at HEAD by execution, not by reading that message: all four are struck and REPLACED** -- `closure.html:501-502` now read `2nd of 7 -- WE LOSE` with the holder and both numbers; `:486-490` name the second column and *seven of the eight rows*; `ACTIVE_RESEARCH.md:12`, `:25`, `:580` read **rank 1 of 7** naming the board by entrant count and retrieval date; `:764` reads seven. **D150's sixth surface, outside V10's enumerated five, is repaired too** -- `CLOSURE_CHALLENGE_STATUS.md:9`, `:528`, `:873`, `:1226`. **So what blocks V10 is not a repair. It is a grade, and the ledger says the opposite of the truth in the direction that makes us look worse.** | Consolidated state of record, `campaign/LADDER_V_STATE_2026-08-16.md` section 4, 2026-08-16. Blocker states re-derived at HEAD by `git show HEAD:<path>` rather than from the working tree, since ten agents are committing concurrently and `git status` reads stale under concurrency. Commit times from `git log --date=format-local`; no document later than `LADDER_V_V6_V10_REGRADE_2026-08-15.md` (mtime 19:32:01Z) grades V10, and no commit after `cca64eaf` re-grades it. | **Dispatch a non-author grader against HEAD who wrote neither `cca64eaf` nor `60073572`, and have it run the regrade's OWN four falsifiers** (`LADDER_V_V6_V10_REGRADE_2026-08-15.md:415-420`): re-render `closure.html`'s per-case column 2 from `LIVE_BOARD` and `round5_per_case_full`; show `Uncorrected` and not `Best published` to be the column carrying four-entry values, and seven of eight rather than six; show any of `ACTIVE_RESEARCH.md:12/:25/:580` struck or naming the six-entry board by count and date; show `0.0364` not to be the live `NASA_2DWMH` minimum. **All four fall implies V10 is PASS.** **Brief the grader that the guards will report clean and that this means nothing** -- `board_placement_faults` returns `([], [])` on all four surfaces while `closure.html` carries live placements in the same text (D88, D85, D89, D151), and `_best_on_board_faults` never opens any of these files in production (D129). **And note the shape worth more than the instance: the same off-by-one, six where the truth is seven, appeared in two independent places from no shared source, one four days old and one written the same day.** | fleet (the grade) |
| D230 | **The lab's only independence instrument returned AUTHOR for every pairing it could ever be asked, and the repair is executable: a per-agent handle exists, it is on the filesystem rather than in the environment, and it is read rather than typed.** REPRODUCED FIRST, at HEAD, by an agent that wrote none of the commits in question: `python3 scripts/check_rung_attribution.py --emit-trailer` emitted `Lab-Agent: ip-172-31-43-247/64b13819-ff95-4d4d-a50f-3720bab19084/-` -- **byte-identical to the trailer on `e933e31b`, which this agent did not write** -- and `--closing 44ac957a --graded e933e31b` returned **`VERDICT: AUTHOR`, exit 1**, for two commits made by two different agents. FOUR OPTIONS COSTED, NOT PICKED. **(a) Keep the session id:** free, zero adoption cost, and structurally incapable of ever returning NON-AUTHOR inside one chief session, which is this box's normal mode at three to five concurrent agents. **(b) A self-generated per-agent nonce written into the trailer:** free, and REJECTED ON SOUNDNESS rather than cost -- the same agent generating a fresh nonce per commit reads as many different agents, so its failure mode manufactures NON-AUTHOR, the one direction that must never fail open. **(c) A tracked mapping artifact the agent writes:** same defect as (b), the value is typed, and it puts the deciding field in a file a rebase can drift from the commit. **(d) A handle DERIVED from the harness, taken:** measured by execution on Claude Code 2.1.232, the environment carries no per-subagent handle at all -- `CLAUDE_CODE_SESSION_ID`, `CLAUDE_PID` and `CLAUDE_CODE_MESSAGING_SOCKET` are all session-scoped and `CLAUDE_CODE_CHILD_SESSION=1` says an agent IS a subagent, not WHICH -- but the harness keeps one transcript per dispatched subagent at `~/.claude/projects/<slug>/<session>/subagents/agent-<hex>.jsonl` (**716 for this session** -- ~~a fact~~ **[RESTATED at `d91b101a`; it is a live counter, see the staleness sweep at the end of this row]**, each with a `.meta.json` naming the spawning `toolUseId`). An agent finds its own by typing a fresh token into the tool call that runs the emitter and having the emitter search those transcripts for it. **Verified both directions: a typed token resolved to exactly one file, `agent-afa60c3f2045c7ce3`, including within the same tool call that carried it -- the harness flushes the call's record before the command runs -- and a token produced by `$(...)`, which the shell expands before the harness records the call, resolved to none and the emitter refused, exit 3.** ~~The rule is the syntax.~~ **[NARROWED at `d91b101a`: the refusal was measured to turn on the token's VALUE never reaching the transcript, not on the presence of `$(...)` -- see the staleness sweep at the end of this row.]** WHAT THE CHECK NOW CLAIMS: different sessions -> NON-AUTHOR as before; same session, BOTH sides carrying a well-formed `agent-<hex>` and DIFFERING -> NON-AUTHOR at agent granularity, the case that had never once fired; everything else -> AUTHOR exactly as before. The granularity that decided each verdict is printed on every run. WHAT IT REFUSES TO CLAIM, printed in every mode including `--json`: that a green run is independence; anything whatever about pre-anchor commits, since **BACKFILL IS IMPOSSIBLE for all 1,849+ of them and every independence claim about that work rests on records outside this repository and always will**; and that any identity it reads names an agent that existed. D173's number is now in the frame -- `distinct identities observed` -- and while it reads `1 session, 0 per-agent` the output says in words that the instrument HAS NOT BEEN SHOWN TO DISCRIMINATE and must not be cited as independence evidence. | Executed 2026-08-16 against D124/D130/D132/D173 by a non-author of every commit named. Frame: HEAD at `a8b3bce6`; `git log --format='%H%n%b' --all \| grep -oE 'Lab-Agent: .*' \| sort \| uniq -c` still returns **four real trailers and one distinct value** plus the docstring template, confirming D173 on a moved denominator. **[OVERTAKEN at `d91b101a`: the same command returned SIX real trailers and TWO distinct values -- sweep below.]** Before this repair the live integrity run read `1 session, 0 per-agent` and printed the never-varied wording; at `62d5757e` it reads `1 session, 1 per-agent`, and the wording correctly stops, because the field has now varied once. **[NARROWED at `d91b101a`: the never-varied sentence was measured to stop in the `DISCRIMINATION:` note only. The `distinct identities observed` frame row appends its `-- 1 session and 0 agents means the discriminating field has never varied` gloss as a constant string at `scripts/check_rung_attribution.py:861-863`, so a reader of the frame row saw the never-varied words beside a `1 per-agent` reading -- sweep below.]** **Cannot see:** whether any `agent-<hex>` names an agent that existed -- the transcripts are per-machine and no clone contains them, so the mechanism makes DIFFERENCE checkable and leaves EXISTENCE unverifiable, unchanged from D132. **Named residual risk:** if one agent could obtain two handles across its life this would manufacture a false NON-AUTHOR; the transcript file is stable for an agent's lifetime and a resumed agent appends to the same file, so the known routes are a harness change or a deleted transcript, neither silent. **And per-agent difference is NOT contextual independence:** a `fork` subagent inherits its parent's entire conversation and is a different agent by this measure while having read everything the parent read. | **Put the probe in the dispatch brief, or nothing changes.** `python3 scripts/check_rung_attribution.py --emit-trailer --probe <fresh-token> >> <msgfile>`, with the token TYPED and never `$(...)`. ~~Until briefs carry it the adoption count stays at zero~~ **[CORRECTED at `d91b101a`: the published brief line was fixed by `9416db99` and the count was never zero -- 6 of 105 post-anchor commits carried an identity and 2 carried a per-agent handle. What was zero, and stayed zero until this row's own repair commit, is adoption by any agent OTHER than this row's author. Sweep item (2)/(3) below.]** and every same-session pair keeps grading AUTHOR -- measured live: a query from this agent's own probed commit `62d5757e` against `e933e31b` STILL returns AUTHOR at SESSION granularity, exit 1, because the graded side carries a three-field trailer and a handle on one side is not evidence about the other. **Then rule on R-ISOLATE.** The mechanism now supports either ruling; what it no longer supports is the status quo in which a green run reads as independence. **Untouched and still owed:** D173's observation that the mechanism is named after the positive it cannot deliver rather than the negative it can. **[STALENESS SWEEP 2026-08-16T16:44Z-17:05Z, frame HEAD `d91b101a`, executed by an agent that wrote none of the commits this row names and whose own derived handle `agent-a51eee0d63411897c` differs from this row's author `agent-afa60c3f2045c7ce3`. Eleven checkable claims re-run; nothing above was deleted, and each correction carries the anchor that overtook it.]** **(1) Trailer census.** `git log --format='%H%n%b' --all \| grep -oE 'Lab-Agent: .*' \| sort \| uniq -c`: ~~four real trailers, one distinct value~~ -> **six real trailers, two distinct values** plus the docstring template -- four of the session form `.../64b13819-.../-` and two of the agent form `.../-/agent-afa60c3f2045c7ce3`, the latter on `62d5757e` and `f6bf8c39` and nowhere else. **(2) Adoption.** `python3 scripts/check_rung_attribution.py` with no arguments: ~~zero~~ -> **`ADOPTION: 6 of 105 commits since the anchor carry an identity; 99 carry none`**, of which **two carried a per-agent handle and both were written by this row's own author**. So adoption by any OTHER agent was zero at `d91b101a`; the sentence was right about what mattered and wrong about the number it used to say it, which is the exact shape this row was filed to remove. **(3) The published brief line.** ~~Until briefs carry it~~ -> **OVERTAKEN by `9416db99`**: `git show 9416db99 -- docs/AGENT_ATTRIBUTION.md` shows the one-line adoption instruction corrected from the SESSION form to `--emit-trailer --probe <a-unique-token-you-type> >> <msgfile>`, so the document that publishes the line had shipped the very defect this row reproduced. Publication is done. Whether any dispatch brief QUOTES it stayed unmeasured and is unmeasurable here, because briefs are untracked (D130) and no command in this repository can read one. **(4) Integrity reading.** `1 session, 1 per-agent` at `62d5757e` -> **unchanged, `1 session, 1 per-agent`** at `d91b101a`, over 105 examined commits, 0 malformed, 0 duplicate, 0 pre-anchor claims, `VERDICT: PASS`, exit 0. **(5) The never-varied wording.** Half held. `_discrimination_note()` branches on `sessions <= 1 and agents == 0` at `scripts/check_rung_attribution.py:765-767` and the note did stop; the `distinct identities observed` frame row at `:859-863` appends `-- 1 session and 0 agents means the discriminating field has never varied` as a CONSTANT, so that reading was printed beside `1 per-agent` on the same line. It is a legend rather than an assertion and no verdict turns on it, so it is filed as a legibility defect, not a false claim. **(6) Transcript count.** `ls -1 ~/.claude/projects/-home-ubuntu-Certonomous/64b13819-.../subagents/agent-*.jsonl \| wc -l`: ~~716~~ -> **361, then 362, then 363 on three calls minutes apart**, each with a `.meta.json` beside it (726 files) and the oldest mtime 2026-08-04. The number moved WHILE IT WAS BEING MEASURED, which is the whole case against an unanchored count in a durable record. Its monotone growth also settles one leg of this row's own named residual risk by execution rather than by argument: no transcript went missing. **(7) Typed probe.** `--emit-trailer --probe D230STALEREPAIR16AUG7T4Q`: **HELD**, exit 0, resolved inside the same tool call that carried the token, and resolved to a DIFFERENT file than this row's -- `agent-a51eee0d63411897c`. **(8) Substituted probe -- NARROWED, and this is the one finding worth more than its row.** `--emit-trailer --probe "D230CTL$(od -An -N8 -tx1 /dev/urandom \| tr -d ' ')"`: **HELD**, exit 3, refusing with the reason. But the control for the control did not: `--emit-trailer --probe "$(echo D230DOLLARCONTROL16AUGZZ9)"` returned **exit 0 and a resolved handle**, because the literal token sat inside the substitution and therefore reached the transcript -- `fgrep -o D230DOLLARCONTROL16AUGZZ9 agent-a51eee0d63411897c.jsonl \| wc -l` counted 2. **The deciding condition was measured to be that the token's VALUE is absent from the recorded call, not that the command contains no `$(...)`.** The published instruction -- type it, never substitute it -- is unaffected and stays correct; it is now known to be a SUFFICIENT rule rather than a statement of the mechanism, and a row claiming it verified BOTH DIRECTIONS was claiming more than it had run. **(9) Reproduction.** `--closing 44ac957a --graded e933e31b`: **HELD**, `VERDICT: AUTHOR`, exit 1, `1 session, 0 per-agent`. **(10) Same-session pair.** `--closing 62d5757e --graded e933e31b`: **HELD**, AUTHOR at SESSION granularity, exit 1. And `--closing f6bf8c39 --graded 62d5757e`, which this row never ran: AUTHOR at **AGENT** granularity, exit 1, both sides carrying the same handle -- the sharper question was asked and correctly answered AUTHOR. **(11) Frame.** HEAD `a8b3bce6` -> `d91b101a`; 105 commits examined since the anchor. **(12) THE CASE THAT HAD NEVER ONCE FIRED, FIRED -- and the sweep is what fired it.** This row's column 1 named `same session, BOTH sides carrying a well-formed agent-<hex> and DIFFERING -> NON-AUTHOR` as the case that had never happened, and it could not happen while one handle existed. Committing this sweep under a probed trailer made a second: `python3 scripts/check_rung_attribution.py --closing 01e94313 --graded 62d5757e` returned **`VERDICT: NON-AUTHOR`, `GRANULARITY: agent`, exit 0**, `1 session, 2 per-agent`, naming both handles -- **the first NON-AUTHOR verdict this instrument has ever returned inside one chief session**, and it was returned about the agent who wrote the row being graded. The census moved again with it: `4 + 2 + 1` trailers, `ADOPTION: 7 of 106`, `1 session, 2 per-agent`, integrity `PASS` at `01e94313`. Two limits, unchanged and stated because the verdict is worth exactly this much: NON-AUTHOR means *no evidence of the same agent*, never *provably independent*; and it answers only the first of R-ISOLATE's two conditions, since no attribution mechanism can see whether the grader EXECUTED what it graded. | chief (whether briefs carry the probe line, and what R-ISOLATE now says); Katie (whether rung independence must be machine-checkable) |
| D231 | **The largest uncovered block in the lab is closed, and the proof is a tracked file rather than a sentence: 26 survivors killed, 87 mutants, all 87 reddened a test they aimed at.** Measured before touching anything: 58 mutants over `scripts/check_rung_attribution.py` against its 24 tests, **32 killed and 26 SURVIVED**. Ranked by what a silent guard would clear, not by count, because this check grades AUTHORSHIP and a guard that stops firing produces a false NON-AUTHOR clearance invisible from outside. **The tier that could fabricate independence -- every one of these silently SHRINKS the graded set, and a set that lost the author's commit returns NON-AUTHOR:** a `--graded A..B` range collapsing to its tip; a graded commit that failed to read being dropped; a selector matching nothing contributing nothing; a `--closing` spec resolving to many and the first being taken; the closing commit filtered out of its own graded set. **The tier that could fabricate a green:** zero adoption anywhere returning PASS; an anchor on another branch accepted; a duplicate trailer counted as an adoption gap rather than a broken emitter; the adoption ratio printed as N of N. **Two live fail-opens found while closing them, and repaired:** `_pre_anchor_claims` returned `[]` on a non-zero git exit, so the check's ONE FAIL condition -- a rewritten history or a fabricated trailer -- was switched off by an exit code nobody read; and `_has_parent` reported an anchor that is NOT IN THIS REPOSITORY as a root commit with nothing behind it, collapsing 'nothing was found' into 'nothing was looked at'. A third: the record split had no `maxsplit`, so a commit body containing the unit separator lost its trailer -- an identity turned into an absence by a control character. **An equivalent mutant recorded as a finding rather than a survivor:** `rev-list --no-walk A..B` returns the WHOLE range, measured, so swapping the two arms of the selector is inert and a harness that did not check would have filed a false survivor. | 2026-08-16. `scripts/mutation_harness_rung_attribution.py`, tracked and runnable, `--list` prints the table. **Control asserted GREEN over 68 aimed tests before any mutant ran, and a mutant is scored killed only when a test IT NAMED is in the new-failure set** -- `killed = returncode != 0` scores every survivor a kill the moment the control is red. Mutants applied to a copy inside a symlink mirror, never to the working tree; the test file is COPIED and not symlinked because `Path(__file__).resolve()` follows a symlink back to the real checker and would prove nothing while looking green. `__pycache__` purged before every cell in both trees. A mutant that does not PARSE is reported as a defect in the table -- caught live, where one such mutant broke the import and reddened nothing, scoring as a survivor. Suite: **24 tests -> 76**, all green. **Both L-84 halves, including the sharp one:** a genuine non-author verifies (two sessions -> NON-AUTHOR, exit 0; and same session, two handles -> NON-AUTHOR at agent granularity, exit 0, the pairing that had never fired), and **a genuine author is caught** (same session -> AUTHOR exit 1; and same session, SAME handle -> AUTHOR exit 1, which is the half the repair could have turned into a rubber stamp). **Cannot see:** three guards remain unreachable from the shipped entry point and are proved only in pairs or at the function boundary -- the graded-set size check (an equivalent mutant alone, killed only when paired with the drop it exists to catch), the failed-sweep arm of `_pre_anchor_claims`, and `run_integrity`'s empty-commit arm. Each is named in the table with why. | Run the harness whenever this module changes; `scripts/lab_check.py` already admits it as a gate. **The pattern generalises and is the reason the count was 26:** every survivor here was a guard whose failure mode is a SMALLER input rather than a wrong answer, and no test in the suite named an input set. Any check that takes a selector should be asked which of its tests names the commits it graded rather than counting them. | fleet (the same sweep on the other selector-taking checks) |
| D232 | **The D227 ruling's own count of which V15 rounds self-declare NOT neutral is wrong, in exactly the D141 shape the ruling elsewhere indicts -- and it overstates how much of the record has ever addressed neutrality at all.** `campaign/LADDER_V_TRIPLE_VERIFICATION.md:577-579` states *"V15 rounds 1, 2, 4, 6, 7, 8 each self-declare NOT neutral."* **Measured at HEAD by sweeping each round document with `/usr/bin/grep -ain -E` over the three alternatives `R-VALUE`, `belief-neutral` and `belief neutral`: rounds 1 (`LADDER_V_V15_LADDER_TEXT_CLAIMS.md`), 2 (`LADDER_V_V15_ROUND2.md`) and 4 (`LADDER_V_V15_ROUND4.md`) return ZERO hits -- no occurrence of R-VALUE, of belief-neutral, or of any neutrality verdict.** Their `# NO.` answers the **termination rule's fixed-point** question (*"is the ladder at the fixed point?"*), which has a different subject and a different rule behind it. Round 6 names R-VALUE **only to file a defect in the rule** (`:297-298`), not to score itself. **Three V15 rounds self-declare NOT neutral, not six: rounds 7 and 8; and on V16, rounds 5, 6, 7 and 11.** The ruling's CONCLUSION is unaffected and is in fact strengthened -- more rounds are silent than it said, so **fewer** rounds have ever addressed belief-neutrality and R-VALUE's count of zero is even less contestable. The defect is that a reader inherits *"six rounds applied the test"* when three did. | Independent re-execution of the D227 premise before grading entitlement, 2026-08-16, by a non-author of the ruling. Every V15 and V16 round document swept at HEAD; V16 rounds 8, 9 and 10 have no documents, so their three commit messages (`5e4a0cc5`, `067caac0`, `0c7b968d`) were read with `git log -1 --format=%B` and each returns **0** hits for R-VALUE or neutral. A tracked sweep with `git grep -a` for any POSITIVE neutrality declaration returns three lines, all of which are the statement that the count is zero. | Correct `:577-579` to name rounds 7 and 8, or -- better, since a count of rounds goes stale every time a round lands -- **replace the enumeration with the predicate**: *"every round that states a neutrality verdict states NOT neutral; the rest state none."* That form cannot go stale. **The durable half: this is the third recorded instance of a stale-summary defect inside a RULING rather than a report (see D228), and a ruling is the artifact readers are most likely to inherit without re-deriving.** | **chief** (it is his own ruling's face; no other hand should edit a ruling) |
| D233 | **TWO OF THE FIVE RUNGS CARRYING PASS WITH RESIDUALS WERE NOT ENTITLED TO IT, AND THE OTHER THREE WERE NOT ENTITLED TO THAT LABEL.** D227's test applied per rung by a non-author of every grade under measurement. **V6 -- ENTITLED.** All three findings sit outside *"every finding gets a round-5 verdict, and QCR gets its own compliance line"*: R1/R3 are a bookkeeping count in the block's preamble (re-derived here: 11 rows, **9** byte-identical, sections 4.7 and 4.8 differing, against the stated ten), R2 a wrong line-distance (`577-321 = 256` against the stated 212) beside a verdict that is correct. **V8 -- ENTITLED.** All three sit outside the two artifacts the criterion names, are not rank claims, and are not the P(rank 1) figure the three protocol edits govern; the grade's own falsifier was executed and does not fire. **V12 -- NOT ENTITLED; the honest verdict is FAIL.** The criterion says *"each with the record's BEST answer beside it"* and *"this BECOMES Sanaa's briefing"*; residual R1 is an unstruck instruction, inside weakest point W2's own answer block, to publish `P(rank 1) = 0.67` while a bullet **34 lines above in the same answer** strikes it for 50.2%. **A better answer sat in the same document, so "the record's best answer" fails even with no accuracy clause imported.** R2 is inside a second time (W3's answer: *"all four published entries … best published 0.0760"* against six entries and Yang's 0.0748). The grade's own sentence -- *"R1 must close before this document is read as a briefing"* -- concedes the second clause. **V13 -- ENTITLED**, and the one clause no grade of record had ever executed is executed here and MET: section 9 collects all four pass owners' signatures quoted from each owner's own report at the commit carrying it. Three of its four findings are decay that occurred AFTER the close-out was signed, by other agents' commits, and **a closing condition cannot be failed by the world moving after delivery.** **V14 -- NOT ENTITLED; the honest verdict is FAIL.** The grade read the criterion as a **method** test (*"fails if its method is a list rather than a search"*) when its **first** clause is a **coverage** requirement -- *"across tracked files, built artifacts, and shipping archives **including `dist/`**"*. **Residual 1 is inside and is the paradigm case the rung was created for**: the "PASS 4" preamble says V14 exists because the first run found *"a tracked shipping archive carrying round-3 numbers with zero caveats"* that was *"nobody's rung."* Verified by opening the container read-only: member `certonomous-demo/site/closure.html`, dated **2026-08-12T16:31:22**, carries live and unstruck `3rd of 5` at `:341`, `rank 1 of 5` at `:502` and `comparable to its margin` at `:503`, all three true faults, while the tracked source has them struck at `:590-591` and carries `of 7` **eleven** times against **once** in the member. Residual 6 (the unmeasured PDF arm) is inside independently. **AND THE RULING ITSELF: its test is sound, decidable, and its stated falsifier is NOT triggered -- the chief let none of the five stand but marked them provisional and dispatched this measurement. What does not survive is the LABEL. R-CONVERGE's own words are that an out-of-scope finding *"is filed as a separate docket item, NOT APPENDED TO THE LIVE RUNG"*, so on the R-CONVERGE route the findings are not the rung's residuals at all and the rung's verdict is PLAIN PASS. Borrowing R-VALUE's label for that state is the relabelling the ruling forbids, running the other way -- and it has teeth, because D38(1) already records PASS WITH RESIDUALS as a sixth gate verdict against `VERIFICATION_CHARTER.md:95` and INVISIBLE BY CONSTRUCTION to the negative-verdict-review sweep. A verdict nothing authorises is also a verdict nothing re-examines, which is how five rungs carried it for a day.** | Entitlement measurement `campaign/LADDER_V_D227_ENTITLEMENT_2026-08-16.md`, 2026-08-16T00:47Z onward, by an agent whose first dispatch record (`subagents/agent-ad1da5054d70fe96f.meta.json`, *"Measure PASS WITH RESIDUALS entitlement"*) opens at **00:47:09.339024368Z**, after the committer timestamp of every artifact graded -- the tightest margin being **36 seconds**, against `20966952`, the ruling that created the task. Independence is established from that untracked, per-machine record and **from nothing else**; `scripts/check_rung_attribution.py` was not run and must not be cited (D173). **No reader of this repository can re-derive it (D130).** Every load-bearing figure re-executed rather than read: the row extraction and per-row sha256 at two trees, the 4.8 line distance, `overall_equivalent_S_bound` divided by `0.001365` giving `1.77225`, and the archive opened member by member. Guard output is reported as a fact about the guard; no verdict rests on any guard's silence. No PDF was rendered, so no PDF claim is graded. | **Amend the ruling so the R-CONVERGE route issues `PASS` with its findings filed as docket rows, and `PASS WITH RESIDUALS` stays reachable only through R-VALUE's two-round bar** -- under which **no rung in Ladder V carries `PASS WITH RESIDUALS`**, which is precisely the outcome the ruling said it was protecting. Then: **V12 needs a fresh non-author grade against HEAD**, because all three findings were repaired at `6dbb3be6` **34 minutes after** the grade and that repair is ungraded -- it could well return PASS. **V14 needs the owner's bundle rebuild** (`scripts/build_laptop_bundle.py`, one command, clears V5-5 in the same act) and then a re-grade running the grade's own falsifier. **Falsifier for the label argument:** produce a reading on which R-CONVERGE's *"not appended to the live rung"* is compatible with those findings being the rung's own named residuals, or find `PASS WITH RESIDUALS` defined anywhere outside `LADDER_V_TRIPLE_VERIFICATION.md:561-565`. A tracked sweep found no second definition. | **chief** (the label amendment and the two demotions); **fleet** (V12's re-grade, V14's re-grade); **OWNER** (the `dist/` rebuild, which no rung may perform) |
| D234 | **The GREEN REQUIRES table is now stale in two further rows, and it sits outside the editing licence of every recent pass that could have fixed it.** `campaign/LADDER_V_TRIPLE_VERIFICATION.md:966-977` was itself written on 2026-08-15 to repair a paragraph that had gone stale in three of six rows. It has gone stale again: its **V8** row reads *"OPEN -- and it has now failed three times"* against V8's verdict of record of **PASS** at `2a686b0a`, now measured as entitled to the pass; and its **V14** row reads **OPEN** against a rung this measurement settles as **FAIL** on residual 1. The consolidation at `04144e7c` recorded five of its eight rows stale (D226(k)); two of the eight now disagree in the opposite direction from that record. **The structural half is the point: three consecutive passes have been licensed to touch the LEDGER ROWS and not this table, so the one summary nobody may edit is the one that goes stale fastest -- and it is the summary that states whether the send gate is shut.** | Entitlement measurement `campaign/LADDER_V_D227_ENTITLEMENT_2026-08-16.md` section 8, 2026-08-16. Both rows read at HEAD and compared against the grade of record for each rung. **Deliberately NOT edited**: this pass's dispatch permits `LADDER_V_TRIPLE_VERIFICATION.md`'s **ledger rows only**, and the GREEN REQUIRES table is not a ledger row. Naming it rather than editing it is the whole content of this row. | Either **widen one pass's licence to cover the table**, or -- better, because it removes the class rather than the instance -- **delete the table's verdict cells and replace each with a pointer to the ledger row for that rung**, so a rung's state is written in exactly one place and the summary cannot disagree with what it summarises. That is the same remedy D226 proposes for the ledger one level up, and the two should be done together or the defect simply moves. | **chief** (the ruling-adjacent half of his own document); Katie on whether the gate summary should carry verdicts at all |
| D235 | **An unhandled exception inside `scripts/lab_check.py` ITSELF exited 1 -- which is `EXIT_FAIL` -- so a broken instrument was reported in the exact vocabulary of a finding about the lab, and every line the run had already produced went with it.** Found 2026-08-16 while measuring D148's three interrupt paths, not by reading the source: a temp copy of the runner was given a `run_script` that raises, and the process exited **1 with 523 bytes and no FRAME block** -- Python's default for an uncaught exception. 1 is the code `scripts/installed/pre-push` maps to *"FAIL -- push blocked. Read the frame above: it names which check failed and what it examined"*, over a runner that never got as far as launching a check and printed no frame to read. This is the D101/F0 class -- the instrument confused with the lab -- one level out from where D101 repaired it: D101 fixed a CHECK's crash being read as a finding, and left the RUNNER's own crash being read as one. It blocked either way, so nothing was waved through; what was wrong is what it said. | Found 2026-08-16T00:45Z by the D148 repair pass, measured on substituted copies in a temp tree (the worktree was never mutated), before/after in the same harness. **Cannot see:** whether this has ever fired in anger -- no run of this runner has ever been scheduled, which is D64 and the reason the question is empty rather than answerable. | REPAIRED IN THE SAME PASS, `d6e9e82b`: the runner catches it, prints `RUNNER TRACEBACK -- this is the instrument, not the lab:` with the traceback, and exits `EXIT_UNSOUND` (4) above an INCOMPLETE terminator, frame intact -- measured after: rc 4, 3,005 bytes, frame present. Pinned by `test_an_unhandled_exception_inside_the_runner_is_UNKNOWN_not_FAIL`, which patches the imported module object rather than the file. Nothing further is owed. | the infra family (`lab_check.py`) -- filed for the record, already executed |
| D236 | **The V14 concession's obstruction was a newline in a character class, and removing it is measured to cost the gating arm NOTHING and to catch the claim the ruling excused.** `_BEST_COUNT`'s gap was `[^.\n]{0,80}`. In the shipping member `dist/certonomous-demo.zip!certonomous-demo/site/closure.html` the sentence wraps -- *"and our best-on-board count"* ends L341, *"drops `<b>`5 of 8 -> 4 of 8"* begins L342 -- so the gap is **25 characters, well inside the 80-character budget**, and the only thing in it the class excluded is the line break. Executed both ways: **no match on the shipped bytes, match on the identical text with the newline replaced by a space.** **REPAIRED, AND THE BOUNDARY WAS REPLACED RATHER THAN DELETED.** Deleting it outright (`[^.]`) was measured and **breaks 7 of the 11 must-not-match controls** -- it binds a subject to a count across a blank line, a table row boundary, a heading, a bullet, a blockquote and an HTML block tag, which manufactures a fault out of two unrelated sentences and is strictly worse than the miss it repairs. **[CORRECTED 2026-08-16T17:0xZ at `d91b101a` by adversarial re-measurement: this row was first written as 9 of 11 and 9 did not reproduce -- seven broke, not nine. The other four hazards (the paragraph break, the ordered-list item, the sentence boundary, the 80-character budget) held under `[^.]` as well, because a `.` in the text or the length budget already stopped them, so they never discriminated on the newline at all and could not have been broken by deleting it.]** What ships allows a **SOFT WRAP** only: a single newline whose next line does not OPEN A NEW BLOCK. **THIS IS NOT THE TUNING THE RULING FORBIDS AND THE DISTINCTION IS THE POINT** -- the forbidden move was widening `_RANK_WINDOW` 5x so a control would pass, which changes WHAT COUNTS AS EVIDENCE; this changes only WHAT COUNTS AS ONE SENTENCE, and the predicate is already proven right by the sites it decides today. **The digit boundary went with it:** `\b` succeeds inside `8.5`, so `of 8.5` read as a board of eight; closed with `(?![.,]\d)`, the same guard `_VALUE_BOARD_SIZE` took at `f4c531cb`. The NUMERATOR needs no guard and that was measured rather than assumed -- `0.5 of 8` cannot match, because the gap class still excludes the `.`. | **Measured on both arms by execution, 2026-08-16T00:3xZ-01:0xZ, and RE-MEASURED ADVERSARIALLY BY A SECOND AGENT at 2026-08-16T16:4xZ-17:0xZ against `d91b101a` before this row was allowed to ship, `__pycache__` purged before every cell and the tracked file never mutated in place** (sha256 asserted unchanged across every measurement; candidates substituted on the module global in memory). **TRAVELLING, the gating arm: 3 -> 4 faults on BOTH runs, and the one gained was TRUE on both, so false positives stayed 0 of 4 (0%) and the ruling's stated reversal condition was NOT triggered.** The re-run adjudicated all four against the board retrieved 2026-08-11T23:33Z and re-verified 2026-08-14T21:01Z (seven positions, best-on-board 2 of 8, 0 of 8 earned by our model, seed bound 177% of the margin): `closure.html:341` *`3rd of 5`* (no placement out of five exists), `:341` *`4 of 8`* best-on-board, `:502` *`rank 1 of 5`*, `:503` *comparable*. Four true faults, no false one. The gained fault is the withdrawn count itself -- member `closure.html:341`, *"our best-on-board count drops 5 of 8 -> 4 of 8"*, **live and unstruck** in a bundle built 2026-08-14T21:16Z, against a live board where the count is **2 of 8** and the count belonging to our model is **0 of 8**; its tracked source carries the correction at `demo-output/website/closure.html:358` (*"the best-on-board count is 2 of 8"*). All four travelling faults are true of the live board. **TRACKED, which reports and does not gate: re-measured 25 -> 26 at `d91b101a`, NOT the 26 -> 27 this row first carried.** **A tracked absolute should not have been written down as if it were a constant, and that is the correction worth more than the digit:** the swept corpus contains `docs/DOCKET.md` itself, and two peer agents appended D238 and D239 to it between the first measurement and the re-run, so the arm moves whenever anybody files anything. What DID reproduce across both runs was the DELTA -- exactly ONE tracked site newly reached, `campaign/LADDER_V_PASS2_2026-08-11.md:325` (*"best-on-board / **4 of 8**"*, an item in a numbers-checked list inside a dated 2026-08-11 pass, so a lab record and not a live claim). The two other apparent gains were the SAME claims already faulted under the old pattern, reported one line earlier because the match now begins on the line the subject sits on. **The false-positive rate stated here (24/26 -> 25/27) was NOT re-adjudicated and its denominators no longer exist; it should be read as withdrawn rather than as measured.** Only **two** tracked faults are live claims needing repair: `ACTIVE_RESEARCH.md:632` (*comparable*, the bound is 177% of the margin) and `closure_challenge_C2_error_decomposition.md:169` (*"best score on the entire leaderboard on three of eight cases"*, unstruck and undated). **CORPUS-WIDE, the widened class admitted 18 newline-crossing matches on the raw text and 10 after the strike-and-history masking the check actually grades (`d91b101a`), and NONE crossed more than one newline**; the **nine** first written here reproduced under neither frame, while the load-bearing half -- that no match spans two line breaks -- reproduced exactly; every one is a single claim that wraps, and none joins two blocks. **19 controls, must-match and must-not-match, in `sdk/tests/test_rank_claim_values.py`.** | **The two live claims above are the repair this now reaches and neither is mine to write.** Separately: the grade's figure of **eleven** tracked sites carrying the wrong value `4` **does not reproduce** -- running HEAD's own pattern I measure **ten** today and **nine** at the time it was graded, the tenth being the grade's own document; there are **15** best-on-board faults in total, and `sdk/tests` contributes none **[re-measured at `d91b101a`: 14 under HEAD's own pattern, and this figure drifts for the reason the middle column now gives -- the swept corpus contains this file]**. The finding is unaffected -- the decisive Markdown twin `campaign/LADDER_V_RUNGS_V2_V7_V10_2026-08-08.md:120` reproduces exactly -- but a number on a rung's face should be reproducible or explained. | the owners of `ACTIVE_RESEARCH.md` and `closure_challenge_C2_error_decomposition.md` for the two live claims; `dist/`'s owner for the rebuild that clears the fourth travelling fault |
| D237 | **A correct pattern behind a caller that never runs it is not a repair, and the declared blind spot had been confessing the flattering limit.** `_best_on_board_faults` is reached from exactly ONE place, `check_closure_entry_of_record`, which hands it `counters.research.closure.our_entry` out of `wall.json` -- measured today at **3,172 characters on ONE line, whitespace-collapsed** (D129). **It opens no SURFACE at all** -- and this row first said *"no file"*, which was FALSE and was caught by adversarial re-measurement at `d91b101a`. Driven with the real `_closure_facts` on a cold `lru_cache`, `_best_on_board_faults` opened FOUR files through `Path.read_text` (`probability_of_rank.py` twice by AST, the entry of record, the probability record) and zero on a warm one. Those are the BOARD it grades AGAINST. The set that is empty is the set of surfaces that could carry a claim, and that -- not "no file" -- is the sentence the finding rests on. So D236's repair, which is real, changes **nothing whatsoever** about what THIS check reads: a one-line input has no line break to cross. The reach that made the repair worth making belongs to the sibling `check_rank_claim_values`, which shares the same compiled pattern and sweeps **20,721 tracked paths and 90 archive members** at `d91b101a` (20,714 when this row was first written; the tracked figure moves with every commit and only the 90 is stable) -- that is the caller through which the shipped claim is now graded, and it is the only one. **THE PART WORTH KEEPING IS WHAT THE BLIND-SPOT LINE SAID BEFORE.** `_blind_best_on_board` declared, as this check's measured limit, that `_BEST_COUNT` *"CANNOT CROSS A LINE BREAK"*. True at the time, and **the comfortable one to confess**: it names a defect whose repair costs this check nothing, while the limit that actually decides what the check can ever grade -- its caller reads one string -- went unnamed. That is the same shape as the ruling D236 withdrew: **an honest-looking limit standing in front of the real one.** | Established by execution 2026-08-16: the wall's `our_entry` read and measured; `_best_on_board_faults` driven with `Path.read_bytes` spied on and **zero paths opened -- A PROBE THAT COULD NOT HAVE FIRED**, because `_load_json` and `_module_literal` both read TEXT and the test class's own `setUp` had already replaced `_closure_facts` with a lambda, so two independent reasons guaranteed the empty answer; re-run at `d91b101a` with `read_bytes`, `read_text` and `Path.open` all spied and the real board reader restored on a cold cache, it opened four paths and every one was a board record**; the two call sites of `_BEST_COUNT` read at HEAD (`scripts/self_audit.py`, `_best_on_board_faults` and RULE V2 inside `_rank_value_faults`). | **REPAIRED HERE, not filed:** the blind-spot line now names the CALLER as the frame, states that the rule opens no other file, and sends the reader to the sibling that does sweep the corpus. Pinned by a test that drives the rule with its REAL board reader on a cold cache, spies on `read_bytes`, `read_text` and `Path.open` alike, asserts the probe FIRED AT ALL, and asserts that everything opened was a board record -- the three properties the first version of that test was missing, each of which was on its own enough to make it vacuous. **What is left for an owner:** whether `check_closure_entry_of_record` should read the wall's SOURCE surfaces rather than one collapsed string -- that is a reach decision about the check, not a pattern question, and it is the one D129 has been waiting on. | the owner of `check_closure_entry_of_record` for the reach; chief for whether a check whose frame is one JSON string should keep its own verdict |
| D238 | **A frame warning binding "every ordinal on this page" was contradicted three times by the page's own round-4 history, and the class had been recorded as a residual for a day without ever becoming a row.** `demo-output/website/ACTIVE_RESEARCH.md:73` (*"round 4 (2026-07-31) is the entry of record: overall 0.0654, **rank 3 of 5**"*), `:652` (the Ladder C round-4 headline, *"rank 3 of 5, gap to rank 2 cut 0.0052 -> 0.0030"*) and `:668` (*"Best-on-board **5 of 8**, unchanged"*). All three are live and unstruck; all three name their board by neither entrant count nor retrieval date; and `:700-710`, added by `cca64eaf`, declares *"The board of record for **every ordinal on this page** is the SIX-entry board retrieved 2026-08-11T23:33Z."* Two of the three sit under a heading explicitly reading *"(superseded as entry of record by round 5 above, 2026-08-07)"*, which is why they are **outside V10's declared scope** -- V10's claim class is round-5 numbers -- and why they are filed rather than appended to the rung. `60073572` section 3.6 recorded exactly this pair as a residual and asked for *"one ruling, not three"*; no docket row had ever carried it, so the ask had nowhere to land. | Found 2026-08-16 while grading V10 at HEAD `d91b101a` by a non-author, `campaign/LADDER_V_V10_GRADE_2026-08-16.md` section 4.3. Established by a whole-file strike-stripping sweep with a planted positive control (4/4 hits) and a struck negative control (0 hits), not by reading the prior grade's list. **Cannot decide:** whether a kept-history ordinal owes a board identifier at all -- the chief ruling at `04489465` fixes the referent by the claim (dated claims grade against their own board, *"and must name it by entrant count and retrieval date"*) but does not say whether a superseded-round paragraph inherits its page's frame warning or overrides it. That is the ruling being asked for. | **One ruling covering the class**, then one edit: either the kept-history ordinals gain their board by count and date (four entrants plus us, the `deb91557` clone, dated to the round-4 scoring day), or the frame warning at `:700-710` is narrowed so it does not claim ordinals it does not govern. The second is cheaper and removes the contradiction rather than papering it; the first is the one that survives a reader who arrives at `:652` without reading `:700`. | **chief** for the ruling; the owner of `ACTIVE_RESEARCH.md` for the edit |
| D239 | **Every V10 sweep on record enumerated line ranges by hand, and that shape is why two live four-entry-board surfaces survived five days on the two most-read pages in the lab.** V10 is *"cross-surface number sweep"* and its grades have been region reads: `60073572` section 3.8 certified the wall `PASS` after checking `wall/wall.json:56`, `benchmarks.json:56`, `benchmarks.html:92-94` and `build_benchmarks.py:126,138` -- four hand-picked ranges -- while `benchmarks.html:109-121`, **fifteen lines below the last range read**, carried a table headed *"Cases where we lead the entire public leaderboard"* over four rows, all four tagged gold `#1` with green `win` margins, **two of them on cases lost to Tian, Buchanan, Hickel & Dwight**. The same shape missed `closure.html:558-561` across four consecutive V10 grades. **This is the B1 class inside a grading record**: a sweep that searched four line ranges reported a clean surface, and the report is indistinguishable from a sweep that searched the file. The mechanical alternative is cheap and was run in one pass here -- blank `<s>...</s>`, `<del>...</del>` and `~~...~~` spans in place (newline offsets preserved, so line numbers survive), then search what remains for the four-entry literals `0.0592 0.1195 0.0760 0.0387 0.0341 0.0325 0.0364 0.0350` and the ordinal forms `rank 1 of 5`, the ordinal-suffix form `Nth of 5`, `4 of 8`, `5 of 8`, `OUR MODEL LEADS`. Across the five surfaces plus the wall's HTML member it returned 30 raw hits, of which 28 resolved on inspection to corrections quoting what they strike, our own round-4 scores, or correct live values -- a signal-to-noise ratio a grader can actually work through. | Found 2026-08-16 grading V10 at HEAD `d91b101a` by a non-author of any V10 grade or repair, `campaign/LADDER_V_V10_GRADE_2026-08-16.md` sections 3 and 4.2. **The instrument was controlled before its output was believed, and the control is why this row exists**: the first positive-control plant silently failed (its anchor string was absent from the target) and the sweep returned 0 on a file that was supposed to contain four planted hits. Confirmed present by readback, re-run, 4/4. A struck negative control returned 0. `<s>` balance audited separately -- `closure.html` 25/25, `benchmarks.html` 4/4, depth never outside [0,1] -- because an unbalanced strike tag makes struck and live text indistinguishable in the markup. **Cannot see:** anything outside the five enumerated surfaces plus `benchmarks.html`, and any surface whose stale value is not one of the eight literals; the sweep is a corpus-shaped instrument, not a semantic one. | **Give V10 a standing mechanical sweep with its own controls, and make the controls part of the grade rather than part of the grader's discipline** -- a grade that does not print a fired positive control has not established that its sweep searched anything. Cheapest form: a script beside `sdk/scripts/probability_of_rank.py` that derives the live per-case minima from `LIVE_BOARD` rather than hard-coding literals, so it does not itself go stale when the board next moves, and fails loudly if its planted control does not fire. Until it exists, a V10 grade that reports line ranges should be read as a partial sweep and said to be one. | the ladder's owner for whether V10's criterion should require a controlled sweep; the infra family for the script |
| D240 | **Chief ruling, 2026-08-16: `scripts/check_rung_attribution.py` returned its first negative, and a GREEN run of it is still not admissible as independence evidence -- no ladder rung may cite one as its independence warrant.** The two halves are not in tension, and the reason is that the instrument's value is ASYMMETRIC. **(a) It was shown to discriminate.** Re-executed at HEAD `4a923413` by an agent that wrote neither commit, `--closing 01e94313 --graded 62d5757e` returned **`VERDICT: NON-AUTHOR`, `GRANULARITY: agent`, exit 0**, over a frame reading `distinct identities observed : 1 session, 2 per-agent, over 2 commits` -- the two sides carrying `agent-a51eee0d63411897c` and `agent-afa60c3f2045c7ce3` inside ONE chief session. That closes D173's objection that the mechanism was structurally incapable of coming out any way but AUTHOR, and it closes it by execution rather than by argument. **(b) A green run remains inadmissible, and R-ISOLATE compliance may not be claimed from one.** A `NON-AUTHOR` verdict is informative; an `AUTHOR` verdict is the default that almost every pair returns for reasons that have nothing to do with who did the work. Measured at `4a923413`: **6 of the 108 commits since anchor `e933e31b` carry a per-agent handle**, so exactly **15 of the 5,778 commit pairs (0.26%)** can be asked the agent-granularity question at all; the remaining 99.74% answer AUTHOR, or answer at session granularity, out of ABSENCE OF DATA. The check says so itself in every mode including `--json`. **(c) Rung-level independence therefore continued to rest on untracked dispatch records**, exactly as before this ruling and exactly as D130 recorded, and the ruling changes nothing about how a rung earns an independence claim. **(d) What would lift (b): adoption reaching the point where a representative sample of grader/graded pairs carries a probed four-field trailer on BOTH sides.** The distance, at `4a923413`: 9 of 108 commits since the anchor carry any identity, 6 of them a per-agent handle, across three distinct handles (`agent-a51eee0d63411897c`, `agent-a9f0446ec1fa5cc99`, `agent-afa60c3f2045c7ce3`). **A premise of this ruling was refuted while being verified, and the refutation is recorded rather than absorbed:** the brief carried *"6 of 105 commits since the anchor carrying any identity, and only 2 carrying a per-agent handle, both written by one author."* The `6 of 105` reproduces EXACTLY, but only at frame `01e94313`, where per-agent was **3 across 2 handles**; the `2 per-agent, one author` reproduces only at the earlier frame `d91b101a` (`5 of 104`). The pair of figures came from two different frames of a moving corpus -- the D230 shape, an unanchored count in a durable claim -- and no single frame returns both. The correction STRENGTHENS (a) and shortens (d)'s distance; it leaves (b) and (c) untouched, which is why the ruling was recorded rather than withdrawn. | Verified by execution at HEAD `4a923413` by an agent that authored none of `01e94313`, `62d5757e`, `f6bf8c39` or the instrument, on 2026-08-16T17:00Z; every exit code captured as `cmd > out 2>&1; rc=$?`, never through a pipe. **Existence stays unverifiable, confirmed by clone rather than asserted:** the handles resolve from harness transcripts under `~/.claude/projects/<slug>/<session>/subagents/agent-<hex>.jsonl`, whose root `/home/ubuntu/.claude/projects` is not inside `/home/ubuntu/Certonomous`; a fresh `git clone` of this repository at `4a923413` contained **no `subagents` directory and no `agent-*.jsonl`**, and `--emit-trailer --probe <typed token> --transcripts <clone>` exited **3** with *"appears in no subagent transcript"* rather than falling back to the weaker session line. The verdict itself still reproduced from inside that clone, because a verdict reads COMMIT TRAILERS: the mechanism makes DIFFERENCE checkable and leaves EXISTENCE unverifiable, permanently. **Backfill stays impossible:** 1,858 commits up to and including the anchor, exactly ONE of which carries a trailer (`e933e31b`, the anchor itself), so 1,857 pre-anchor commits are unattributable and every independence claim about that work rests on records outside this repository and always will. **Per-agent difference is not contextual independence**, and the repo supports the claim: `docs/AGENT_ATTRIBUTION.md:198-201` states a `fork` subagent *"inherits its parent's whole conversation: a different agent by this measure, having read everything the parent read"*, and the check prints the same caveat in its FORGEABILITY line on every run. **Cannot see:** whether any of the six handled commits was written by a fork -- nothing in the trailer, the transcripts or the tree records dispatch KIND; and whether a grader EXECUTED what it graded, which is R-ISOLATE's other half and which no attribution mechanism can reach, since a grader that read a summary and a grader that ran the code commit identical bytes. | **Owner work implied by this ruling and deliberately NOT done here.** (1) Five campaign faces carry the pre-repair blanket line that the emitter *"emits the same value for every agent in this session"* or that the check has never discriminated -- `LADDER_V_V12_V13_V14_GRADE_2026-08-15.md:48`, `LADDER_V_V15_ROUND8.md:36`, `LADDER_V_V16_ROUND11.md:21`, `LADDER_V_D227_ENTITLEMENT_2026-08-16.md:35`, `LADDER_V_STATE_2026-08-16.md:30`. All five were true when written and are now HALF overtaken by `62d5757e`: the session field is still one constant, the fourth field is not. Their standing instruction -- *do not cite it* -- survives this ruling unchanged, so none is urgent and none is a false clearance; the owners hold the wording. (2) A sweep of `demo-output/website/campaign` and `docs/charters` found **no rung face citing a green attribution run as its independence warrant**, so this ruling required no rung-face correction to be safe. `LADDER_V_TRIPLE_VERIFICATION.md` was held by another agent and was not read into or edited. (3) `docs/USING_THIS_LAB.md` contains **no occurrence of the word `fork`**, so the lab's own dispatch guide never warns a dispatcher that a fork reads as independent to the instrument while having read everything its parent read; that warning lives only in `AGENT_ATTRIBUTION.md`, which a dispatcher has no reason to open. | **chief** (ruling recorded here, nothing further owed); the owners of the five campaign faces for the wording; the owner of `docs/USING_THIS_LAB.md` for the fork warning |
| D241 | **The lab's own integrity instrument asserted, in a CONSTANT STRING, that its discriminating field had never varied -- while printing `3 per-agent` on the same line. Defect class B4 inside the instrument the lab points at B4. Repaired by derivation at `42f3e7b4`.** `scripts/check_rung_attribution.py`'s `distinct identities observed` frame row appended `-- 1 session and 0 agents means the discriminating field has never varied and a green run proves nothing about discrimination` unconditioned on either of the two numbers it glossed (`:871-875` at `1a08e75d`; `:859-863` when D230's staleness sweep item (5) recorded it at `d91b101a`). **What had changed since D230 filed it is the reading, and that is what turned a misplaced sentence into a false one:** at `d91b101a` the row read `1 per-agent`, and at `1a08e75d` it read **`1 session, 3 per-agent`**, so the instrument was denying, in words, a variation it was reporting in figures one column to its left. **THE TOOL HAD CARRIED A CORRECT MECHANISM AND A CONSTANT ONE PRINTING SIDE BY SIDE:** `_discrimination_note()` branched on `sessions <= 1 and agents == 0` at `:765-767` and correctly stopped saying it, while the frame row a hundred lines below did not branch at all. **The repair was derivation, not deletion:** `_field_has_not_varied()` took the predicate once, `_identities_row()` chose the gloss from the same two numbers it printed, and the note was rewired onto that same predicate, so the two surfaces can no longer disagree about one reading. Measured at `42f3e7b4`: a `1 session, 0 per-agent` reading carried the never-varied wording, and `1 session, 1 per-agent`, `1 session, 2 per-agent`, `2 session, 0 per-agent` and `3 session, 5 per-agent` each carried instead a gloss naming its own counts. **Filed alongside it:** `sdk/tests/test_exec_bits.py` had been red at `1a08e75d` on four tracked shebang-bearing files with no exec bit and no register entry -- `hunk_check.py`, `check_docket_surface_agreement.py`, `check_proposal_surface_coverage.py`, `mutation_harness_rung_attribution.py` -- closed at `e4c1319e` by registration, on D134's authority that the other repair the test offers is one no protocol-conforming agent can perform. | Executed 2026-08-16 by an agent whose derived handle `agent-a7906acd0a1e114c3` differs from every handle on the commits it graded: `--closing 42f3e7b4 --graded 62d5757e` returned **NON-AUTHOR at AGENT granularity**, `1 session, 2 per-agent`. `sdk/tests/test_rung_attribution.py`: **76 passed at `1a08e75d`, 79 at `42f3e7b4`**, rc 0 both times, captured by `cmd > out 2>&1; rc=$?` rather than through a pipe, with `__pycache__` cleared before each measurement. `sdk/tests/test_exec_bits.py`: **14 passed 1 failed, rc 1 before; 15 passed, rc 0 at `e4c1319e`**. **The three added tests assert the IDENTITY of the wording emitted for a given reading and never a length or a count**, because a probe that asserts a COUNT survives an inverted comparison and three instances of that have been caught here; the phrase was TYPED in the test file and imported from nowhere, since reading `ra._NEVER_VARIED` would move both sides of every assertion together. **Mutation proof, three mutants, all killed, and the killing ARM recorded rather than the total, because a mutation proof shows a test detects A change and not the RIGHT one:** (A) the constant restored, the exact defect re-introduced -- killed by both must-not-say arms; (B) the predicate inverted -- killed in **BOTH** directions across four tests, which is precisely the arm a count-based probe would have survived; (C) the caveat deleted outright -- killed by the direct-drive test while the end-to-end test correctly stayed green, because the `DISCRIMINATION:` note still carried the phrase into stdout. **Cannot see:** whether any reader was ever misled by the gloss. No verdict has ever turned on it, so what was repaired was legibility and **no verdict moved**; D230's sweep said as much when it filed the observation. For the four scripts, a bare-path call site was searched for across the tree, the docs and the tests and none existed -- every invocation was `python3 <path>` -- so none belonged in `REQUIRED_EXECUTABLE`. | **Nothing further owed on the gloss**; the ratchet on `exec_bits` was left strictly intact and verified rather than assumed, both positive controls green at `e4c1319e`, so a NEW unregistered script is still refused and any of the four is reported stale the moment it gains its bit. **Still owed, and deliberately not taken here: D134's option (c).** `sdk/chief_engineer/exec_bits.py`'s docstring recorded that the register "stops it growing", while D134 measured that under this lab's mandated pathspec commit form the register **can only ever grow**; `e4c1319e` was a fifth instance and changed the docstring not at all, that file's prose belonging to the family that owns it. **And one mechanical hazard measured in passing, which is worth more than the row it is attached to:** at `1a08e75d` the SHARED INDEX carried stale `git add` entries for five paths -- `docs/AGENT_ATTRIBUTION.md`, `docs/DOCKET.md`, `docs/USING_THIS_LAB.md`, `scripts/check_rung_attribution.py`, `sdk/chief_engineer/exec_bits.py` -- whose blobs were verified to match ancestor `d91b101a` rather than HEAD. The mandated pathspec commit ignored them exactly as designed; **a BARE index commit by any agent would have silently REVERTED all five to their `d91b101a` state**, which is the same shape as the capture the pathspec rule exists to prevent, running in the opposite direction. | **chief** (the stale-shared-index hazard, and D134's protocol ruling that leaves the waiver register the only exit); **Infrastructure and Standards** (the `exec_bits.py` docstring sentence D134 already flagged) |
| D242 | **D239's sweep-shape finding was measured across all twenty-eight ladder grading records, and the exposure divides on one line: a certification that names its read method can have its unread remainder computed, and a certification that names none cannot.** The eight class-(b) certifications whose named lines could be counted against their surfaces read **61 lines of 8,366 — 0.73%, leaving 8,305 lines certified but unread**: `60073572` section 3.8 read `wall/wall.json:56` (1 of 416), `benchmarks.json:56` (1 of 88), `benchmarks.html:92-94` (3 of 275) and `build_benchmarks.py:126,138` (2 of 356) — 7 lines of 1,135 — and the same section's PRODUCT_LIST tick read 14 of 3,963; `49f71b8c` rung V10 certified `closure.html` CONSISTENT on 18 of 456 and `latex/closure_challenge_report.tex` CONSISTENT on 8 of 2,258. **For the class-(d) certifications no such number exists at all, and that is the finding rather than a gap in it** — `campaign/LADDER_V_V6_V10_V14_CLOSURE.md:355` certified *"Everything in the working tree is consistent"* under a criterion reading *"one inconsistent surface fails the rung"*, from an eleven-item list and two guard passes; `campaign/LADDER_V_PASS2_2026-08-11.md:334-338` certified five surfaces PASS, `latex/closure_challenge_report.tex` among them, naming the numbers checked and no read method; `campaign/LADDER_V_V15_ROUND7.md:662-669` certified seven further surfaces compliant, four of them travelling, with no frame, pattern, count, line list or control; `campaign/LADDER_V_V13_CLOSEOUT.md:85-99` carried fifteen rung verdicts *"read from the rung's own record"* with the extent never stated. **None of those was shown false at its own commit** — `171b1241` (2026-08-11T00:23:36Z) preceded the board move by 23 hours, so the four gold badges it passed over were still true — **and none can be bounded by a later reader either, which is the whole of the difference.** Of the 8,305 unread lines, 6,055 across seven surfaces were swept whole-file at `1a08e75d` (2026-08-16) and again in this pass; `latex/closure_challenge_report.tex`'s 2,250 were read by nothing until this pass. The corpus already carried one realised instance diagnosed in its own words: `campaign/LADDER_V_V15_LADDER_TEXT_CLAIMS.md:196` certified a struck sentence *"gone"* with no method, and round 4 later measured the line-bounded instrument used in all three prior passes returning ZERO on a buffer that demonstrably contained it. | Measured 2026-08-16 at HEAD `5a0127d3` by a non-author of any ladder grading record, against ground truth re-derived here rather than inherited (`LIVE_BOARD` in `sdk/scripts/probability_of_rank.py` joined to `demo-output/website/closure_challenge_round5_qcr.json`: seven positions, per-case ranks 2,2,1,1,3,4,4,7 of 7, best-on-board **2 of 8** with **0 of 8** earned by our own model, both survivors being cases unchanged from round 4, overall rank 1 of 7, margin 0.001365, seed bound 176% of it). **Corpus enumerated across all four arms with a fired control**: `git ls-files 'demo-output/website/campaign/LADDER_V*'` returned **28 records, 15,902 lines**, every one read in full; the gitignored arm (`git ls-files --others --ignored --exclude-standard`) and the untracked arm returned **0**; the run tree at `/home/ubuntu/certonomous-runs/` (**132,049 files**) returned 0, and a planted `LADDER_V__probe_D241.md` **was** returned by the identical command and then removed, so that zero was a measurement and not a silence. **Spot sweeps carried both controls, confirmed by readback before any result was believed**: a plant exercising five claim classes was inserted **by line index, never by anchor string** — the failure mode that returned a silent 0 for the V10 grader — read back present at its expected line, and fired 5 of 5 classes; a struck copy of the same plant returned 0 with the rest of the file's hits unchanged. A strike-blanker control matrix (plain, `\sout` flat, `\sout{\textbf{}}` nested, deep-nested, `<s>`, `~~`) caught a real defect in this pass's own instrument: a `\sout\{[^{}]*\}` regex silently failed on `\sout{\textbf{4 of 8}}` and left struck text live, which was repaired and re-controlled before any `.tex` result was used. Whole-file and strike-blanked: `CLOSURE_CHALLENGE_SUBMISSION_DRAFT.md` 34 hits, the `.tex` 42, `closure.html` 17, `ACTIVE_RESEARCH.md` 28, `docs/PRODUCT_LIST.md` 13, `wall/wall.json` 3, `benchmarks.json` 3, `wall/wall.html` **0** — every one resolving to a correction quoting what it strikes, a head-to-head count against a named entrant (re-derived here: we beat Yang on exactly 4 of 8 cases), or round-3 and round-4 history under a dated heading. **`docs/DOCKET.md`'s own strike balance measured 11 `<s>` openers against 4 closers, final depth 7**, so a strike-blanked read of that file is unsound and none was relied on. **Cannot see:** any claim whose stale value is not one of the swept literals; text rendered into PNG; and whether a record's arithmetic is right, as opposed to how far it read. | **The rule question, stated for the chief and Katie and deliberately not ruled on here: should a certification that names no read method be admissible at all?** The case for inadmissibility is that class (d) is not a weaker class (b) but a different kind of object — this audit computed an unread remainder for every class-(b) certification and for **no** class-(d) one, so a later reader can re-sweep what (b) missed and can never bound what (d) covered; the lab already applies exactly this principle one level down, `campaign/LADDER_V_V5_V8_GRADE_2026-08-15.md:416-421` recording an incomplete sweep **as UNMEASURED rather than as zero**, and extending it from sweeps to certifications changes no doctrine. The costs, so the alternatives are priced rather than assumed: **(A) retroactive void** — every class-(d) certification lapses to UNGRADED, reopening rungs whose only evidence is one, the most expensive option and the only one that clears the present exposure outright; **(B) prospective only** — new grades must name a read method, existing ones stand labelled INHERITED-UNSTATED, which costs almost nothing and leaves the present exposure exactly where it is; **(C) downgrade rather than void** — a class-(d) certification stays readable as *asserted* but may neither close a rung nor be cited as evidence by a later grade, forcing re-sweep only where a rung actually leans on one; **(D) require the printed control** — a grade that does not print a fired positive control is a partial sweep by definition whatever its prose says, this being the half that cannot be satisfied by a formula, since a phrase can be written and a fired control cannot be faked. (C) and (D) together are the only combination that would have caught both `benchmarks.html:109-121` and the stale artifact in D243; that is an argument, not a ruling. | the ladder's owner and Katie for the admissibility rule; the infra family for (D)'s standing control |
| D243 | **The shipped `demo-output/website/latex/closure_challenge_report.pdf` was rendered to PNG and read visually for the first time, and it carried live, unstruck, four-entry-board claims on its own front page while its LaTeX source sat fully repaired beside it.** Tracked, 40 pages, sha256-stable at HEAD `5a0127d3`, PDF `CreationDate` **2026-08-11T03:26:36Z** — twenty hours **before** the board moved at 2026-08-11T23:33Z, and one day before the source was repaired. Page 1 read visually at 130 dpi: *"the best overall number on the board as published at benchmark commit `deb91557` — **rank 1 of 5, scored locally**, 0.002878 below Reissmann"*, and *"**P(rank 1) = 68%**, and eight cases cannot pin that tighter than **2–100% at 95%**"*, and *"two of the **four** leaderboard-best rows we now hold"*. Page 14 read visually: Table 4 tagging both `alpha_15` rows **"our model leads"** — the two cases lost to Tian, Buchanan, Hickel and Dwight — plus `2nd of 5`, `3rd of 5`, `3rd of 5` and an overall row reading `rank 1 of 5, scored locally; P 68% (2--100% at 95%)`; and section 3.5 *"we hold the best score on the public board on **four of eight** cases"* over a Table 5 attributing a **"clear lead"** to our own model on exactly those two lost cases. Live truth, re-derived: rank **1 of 7**, margin **0.001365** over Yang, P(rank 1) **50.2%** at **0–97%**, best-on-board **2 of 8** with **0 of 8** our model's, and `deb91557` scores but does not rank. **Nothing on either page is struck.** | Found 2026-08-16 auditing certification reach at HEAD `5a0127d3`. **The `\sout{}` trap was avoided by a one-sided discriminator rather than by trusting the text layer**: a repaired document carries **both** the struck original and its correction, so the *absence* of every post-repair token is decisive where their presence would not have been. `pdftotext -layout` over all 40 pages returned **0** occurrences of each of `six-entry`, `Tian`, `two of eight`, `rank 1 of 7`, `0.0432`, `0.0998`, `struck 2026-08-12` and `zero of`, against **17** four-entry-class occurrences (`rank 1 of 5` 2, `Nth of 5` 3, `four of eight` 1, `our model leads` 2, `0.0592` 3, `0.1195` 1, `0.0387` 2, `0.0341` 2, `0.0364` 1) and **14** of the withdrawn `68%`; pages 1 and 14 were then rendered with `pdftoppm` and read, confirming no strike rendering anywhere. **The source is not at fault and must not be edited to match**: a whole-file strike-blanked sweep of `latex/closure_challenge_report.tex` (2,509 lines, brace-balanced `\sout` handling, positive control 5 of 5 classes at a plant confirmed by readback, struck negative control 0) returned 42 hits, **every one struck-and-kept with its six-entry correction inline**, and the table caption at `:897-905` frame-declares the stale column correctly — the disclosure `closure.html:480-482` was faulted for getting wrong. The gap is that the artifact was never rebuilt after `2cec44ee` (2026-08-12) repaired the source; the PDF's last build is `98a39662` (2026-08-11T03:29:15Z). **How it survived five days:** `campaign/LADDER_V_V14_SURFACE_DISCOVERY.md:150-154` certified it *"**CURRENT** … In sync."* on 2026-08-10, true when written and stale two days later; `campaign/LADDER_V_V15_ROUND7.md:662-669` certified the source compliant with no read method and said nothing of the artifact; and every record since declined the artifact by policy — `campaign/LADDER_V_STATE_2026-08-16.md:550-552` *"No PDF was rendered, so no PDF claim is graded"*, `campaign/LADDER_V_D227_ENTITLEMENT_2026-08-16.md:76-79` *"I rendered no PDF, so I grade no PDF claim"*, `campaign/LADDER_V_V5_V14_REGRADE_2026-08-16.md:409-417` *"rendering to PNG was outside the time this grade had. That is a stated blind spot, not a clean result."* Both standing instruments are blind to it by construction: `scripts/check_derived_figures.py:251` and `scripts/check_normative_clauses.py:153` both set `PROSE_GLOBS = ("*.md", "*.html")`, so `*.tex` and `*.pdf` are outside every frame, and D115 excludes `latex/*` PDFs explicitly. **Not repaired here:** `demo-output/website/latex/` is on this dispatch's do-not-touch list, and the fix is a rebuild rather than an edit. **Cannot see:** the other 67 tracked PDFs, which were not rendered. | **Rebuild the PDF from the repaired source and commit the artifact, then state the rebuild in the same commit as the source repair from now on** — the defect is not a wrong sentence but a build product that outlived its input, and no wording change fixes it. Two durable halves worth more than the rebuild: **(i)** a check that fails when a tracked build product is older than the source it names, which is cheap and would have fired on 2026-08-12; **(ii)** a ruling on whether `*.tex` and `*.pdf` belong in `PROSE_GLOBS` — they were excluded as unreadable prose, and the consequence measured here is that the lab's single largest concentration of withdrawn claims sits in the one arm no instrument opens. Until one exists, a grade that declines the PDF arm should say the shipped artifact is **UNGRADED**, not that the surface is clean. | the report's designated writer for the rebuild; the infra family for the staleness check; the ladder's owner for the `PROSE_GLOBS` ruling |
| D244 | **The instrument that measured D242 carried the defect D242 was about, and re-measurement with it repaired changes no finding but forces one correction and splits class (c) in two.** The sweep behind D242 and D243 matched `lead the entire public leaderboard` with **literal spaces**, and the heading that was V10's C2 blocker **wraps between "entire" and "public"**. A/B on the exact blocker file `git show fe54ec0a^:demo-output/website/benchmarks.html`: the literal-space pattern returned **0**, the `\s+` pattern returned **1**, matching `'lead the entire\npublic leaderboard'`, and the whole-file hit count moved **6 to 7**. This is the third independent arrival at one defect — `_BEST_COUNT`'s `[^.\n]` bound repaired at `567c7aa6`, the V10 repairer's own sweep going 7 hits to 8, and this one — in three unrelated tools, and it is the sharpest available statement of D239: **a defect is not at a line, it is at a wrap.** **Re-run wrap-safe, nine surfaces returned byte-identical hit counts** (`CLOSURE_CHALLENGE_SUBMISSION_DRAFT.md` 34, `latex/closure_challenge_report.tex` 42, `closure.html` 17, `ACTIVE_RESEARCH.md` 28, `docs/PRODUCT_LIST.md` 13, `wall/wall.json` 3, `benchmarks.json` 3, `wall/wall.html` 0, `benchmarks.html` 4), **so every conclusion in D242 and D243 stands unchanged**; the blindness had bitten only on a heading already removed at `fe54ec0a`. **One correction, and it runs against this pass's own interest:** D243 stated 17 four-entry-class occurrences in the shipped PDF; re-counted wrap-safe over the same member set it is **19**, the extra pair being `four of eight` occurrences broken across a line by `pdftotext -layout`. **The undercount was in the direction that made the finding look smaller, and the finding is worse than filed.** All eight post-repair tokens re-confirmed at **0**. | Measured 2026-08-16 at HEAD `6097856c` by the author of D242 and D243, correcting its own work; prompted by a chief data point reporting the identical defect in the V10 repairer's instrument, then reproduced here from scratch rather than accepted. **The classification of class (c) is revised, and the revision is the durable half:** a pattern sweep over a whole file was scored in D242 as covering that file, and **it cannot be, unless its pattern crosses line breaks** — a whole-file sweep with a whitespace-literal pattern reads exactly as rigorous in the record as a wrap-safe one and is as blind as a hand-picked range. Measured across the corpus: **15 of the 28 grading records mention whitespace, newlines or wrapping at all, and NOT ONE declares the property of its own sweep pattern.** In the two most sweep-heavy records the mentions are about the lab's *own guard* — `campaign/LADDER_V_V6_V10_REGRADE_2026-08-15.md:433,473` on `_BEST_COUNT` collapsed versus production shape — never about the record's own instrument. The exemplary case proves the point rather than escaping it: `campaign/LADDER_V_V10_GRADE_2026-08-16.md:86-89` states its pattern set as the wrap-immune numeric literals `0.0592 0.1195 0.0760 0.0387 0.0341 0.0325 0.0364 0.0350` plus `rank 1 of 5`, the ordinal-suffix form `Nth of 5`, `rank 3 of 5`, `4 of 8`, `5 of 8`, `OUR MODEL LEADS` and `best score on the entire leaderboard` — **five of the seven classes written with literal spaces** — and its single newline mention is about preserving offsets while blanking strikes, not about matching across them. **That grade found C2 through the numeric literals, which have no spaces to wrap; had the table carried only its heading, the same sweep would have returned clean.** So class (c) is not auditable from the record either: the one property that decides whether a pattern sweep saw the file is the one property no record states, which collapses (c) toward (d) on exactly the axis that matters and means D242's (b)-versus-(d) split understated how much of the corpus cannot be bounded by a reader. **A fourth class is also now on record and is not (a)-(d):** a certification whose instrument was **incapable of returning the negative** — a pinning test asserting a rule opened no file passed while the rule opened four, unfailable for two independent reasons at once (a spy on `read_bytes` against code reading text, and a `setUp` that had already replaced the function under test with a lambda) — the same shape as an attribution instrument that could never emit NON-AUTHOR. A sweep that searched nothing and a test that could not fail are one class seen from two sides, and neither is visible in a record that reports only its verdict. **Controls, re-run and strengthened:** plants inserted by line index and read back before any result was believed; positive 5 of 5 classes; **a new WRAP control whose every claim is broken by a newline at the point real prose wraps** fired 3 of 3 wrap-sensitive classes on both HTML surfaces; struck negative 0 with the rest of each file unchanged; a strike-blanker matrix (plain, `\sout` flat, `\sout{\textbf{}}` nested, deep-nested, `<s>`, `~~`) still green after the earlier nested-brace repair. **Cannot see:** a claim split by an intervening HTML tag rather than by whitespace — `\s+` does not cross `</b>`, and that gap is stated rather than closed. | **Make wrap-safety a declared property of any sweep a grade relies on, on the same footing as the fired positive control D239 asked for**, and require both to be printed: a grade that shows neither has not established that its sweep searched anything, whatever its prose says. Cheapest sufficient form, and it is one line in a control harness rather than a new instrument: **plant a control whose claim strings are broken by newlines at the wrap points and require it to fire**, which cannot be satisfied by writing a phrase and would have caught all three of the tools that failed this way. This strengthens rather than replaces the rule question D242 put to the chief and Katie: class (c) can no longer be treated as a safe class on the record alone, so option (C) — a certification that cannot be bounded may be read as *asserted* but may neither close a rung nor be cited by a later grade — now has a wider population than D242 scored it against. **Still not ruled on here.** | the ladder's owner and Katie for the admissibility rule as widened; the infra family for the wrap control beside D239's positive control |
| D245 | **THE D238 RULING, COSTED AND NOT RULED ON — and the measurement says the cheap rule is ambiguous, the strict rule is expensive in the wrong currency, and the status quo has already cost four live falsehoods in one week.** This row decides nothing; it prices the four candidates the chief named and hands the choice to the chief and to Katie. **THE CORPUS.** 474 tracked prose surfaces (`.md`/`.html`) at pinned commit `bded9bee`, carrying **1,884 board-claim sites** — ordinals, superlatives and margin claims about the public leaderboard. **THE CLASSIFICATION**, by how each site identifies the board it is about: **(a) carries its own retrieval timestamp 320; (b) entrant count only 290; (c) neither 696; (d) relies on a dated SECTION heading rather than dating the claim 94; (e) struck or withdrawn 484.** Of the 1,884, **383 are ASSERTIONS** and 1,501 are meta — a grade quoting `rank 1 of 5` in order to strike it is not making the claim, and a rule that required a retrieval timestamp on a quotation would be absurd. **42 assertions sit on shipping surfaces.** **THE COSTS**, in sites needing repair, split substantive (the block carries a superseded four-entry figure, so the repair means re-deriving and restating) versus clerical (the claim is true and lacks only an identifier): **R1 — timestamp on every claim: 371 assertions (99 substantive / 272 clerical), of which 38 shipping (10/28); 1,080 sites counting meta. R2 — timestamp except under a dated heading: 358 assertions (93/265), 29 shipping (8/21); 986 counting meta. R3 — count PLUS timestamp on every claim: 372 assertions (99/273), 39 shipping (10/29); 1,115 counting meta. R4 — status quo: 0.** **THE PIVOTAL MODELLING CHOICE, STATED BECAUSE IT IS THE RULING AND NOT MINE:** whether the ordinal's own denominator identifies a board. `rank 3 of 5` encodes a count but names no board; counting it as identification moves **217 sites** from (c) to (b). This measurement does NOT count it, on the ground the lab already established — `deb91557` "scores but does not rank" was rejected as an identifier, `rank 1 of 5` was struck corpus-wide for exactly this reason, and D238's own remedy concedes it by proposing the count be stated separately. **R2's EXEMPTION IS NOT ONE CLAUSE BUT THREE, AND THEY DISAGREE.** Over the **1,080** live, unstruck, self-untimestamped sites the clause has to decide, "inside a dated-history section" reads three ways: **nearest heading dated exempts 94; any ancestor heading dated exempts 305; the document H1 dated exempts 189. The three readings disagree on 295 sites — 27% of the exemption surface.** Two shipping sites are the paradigm: `CLOSURE_CHALLENGE_SUBMISSION_DRAFT.md:1420` sits under an undated `### 8.2` but a dated `## 8. ADDENDUM 2026-08-01`, and `CLOSURE_CHALLENGE_STATUS.md:76` under an undated `## 0.` but a dated document H1. **AND THE TWO CONVENTIONS ALREADY IN FORCE CONTRADICT EACH OTHER AT 44 SITES:** eight pages carry a page-level frame warning binding *"every ordinal on this page"* to the live six-entry board (`ACTIVE_RESEARCH.md:700-710` is the one D238 cites), while dated headings inside those same pages say *"judge this by its own date"*. R2 exempts those 44; the frame warning convicts them. Both are live today. **THE FAILURE MODES, INCLUDING THE ONE NOBODY WANTS TO SAY.** **R1** prices keeping honest history at the same rate as making a claim, and **272 of its 371 assertion repairs are pure timestamp insertion** — a rule whose main product is 272 pasted timestamps trains the paste and not the re-derivation, which is B4, the quoted-generated-number class that produced V10's C1 and C2. **R2** is defeated by a section heading nobody updates, and its exemption is undecidable at 27% of its own surface without a fourth ruling saying which heading counts. **R3** buys almost nothing over R1 — **372 assertions against 371, one extra site** — because a retrieval timestamp already picks out a unique board; the count is redundancy for a human reader, not identification, and it should be argued for on that ground or not at all. **R4** costs zero today and has already produced, in one week: V10-C1 (`closure.html` naming Reissmann as the duct leader), V10-C2 (four gold `#1` badges over two lost cases), `CLOSURE_CHALLENGE_PRIOR_ART.md:327` (*"the entry's five best-on-board cases"*, live and unstruck for sixteen days), and D238's own three sites — **four live falsehoods, three of which needed a dispatched grader to find.** | **Measured 2026-08-16T17:0x-17:2xZ against pinned commit `bded9bee`, never the moving worktree** — HEAD advanced four times during this pass (`567c7aa6` -> `0035ef96`) and a costing whose denominator shifts mid-run is not a costing. Enumeration by `git grep -aInE` over the tracked corpus (1,843 raw lines / 235 files, the headline command) cross-checked against an independent `git ls-tree` walk in the classifier; the two agree on the exemption surface at **1,080 sites** by separate code paths. **Every pattern is whitespace-flexible (`\s+`, never a literal space)** — claims wrap, and the literal space is D236's defect, which this agent hit in its OWN V10 sweep earlier tonight and reported against itself. **CONTROLS, run before any number was believed:** a planted positive with one site of each of the five classes, confirmed present by readback, all five classified correctly; a **wrap control** of six claims each split across a line break at a different point, all six found; a struck negative, 20 sites, **zero leaking as unstruck**. **The controls twice caught real defects in this instrument before it was trusted** — a +/-500-character proximity window credited a claim with a timestamp belonging to a different paragraph (fixed to the claim's own block, which is the C1 defect in miniature), and the entrant-count pattern read the ordinal's own denominator as a board identification (split into strict and loose and reported both ways above). **VALIDATION BY HAND:** 30 sites drawn by seeded random sample from the live set and adjudicated by opening them — **2 of 30 (6.7%) were claim-pattern false positives** (`RESULT_PRIORITY_CHARTER.md:98`, a research-priority table header; `LADDER_V_V6_V10_V14_CLOSURE.md:71`, a ranking of FILES not entrants) and **3 of 30 (10%) were meta-heuristic misclassifications**. **Every count above therefore carries roughly +/-10%, and is stated as a scale rather than a tally.** **CANNOT SEE:** anything outside `.md`/`.html` — `.json`, `.py` and the shipping archives were excluded by scope, and `dist/` was not opened; whether a site's claim is TRUE, only whether it identifies its board; and the meta/assertion split at better than the sampled 10%. **The live falsehood this sweep turned up was repaired and is not part of the costing:** `CLOSURE_CHALLENGE_PRIOR_ART.md:327` at `bded9bee`. | **THE DECISION IS NOT TAKEN HERE AND MUST NOT BE READ AS TAKEN.** What the measurement supports handing over: (1) **the pivotal question is not R1-vs-R2-vs-R3 but whether the ordinal denominator identifies a board** — it moves 217 sites and every rule inherits the answer, so it should be ruled on FIRST and separately; (2) **R2 cannot be adopted as worded** — "inside a dated-history section" is undecidable at 27% of its own surface and contradicts the frame-warning convention at 44 live sites, so adopting it needs a companion ruling naming WHICH heading dates a claim, and a decision on which of the two conventions yields; (3) **R3 should be argued on reader-redundancy or dropped** — it is one site more expensive than R1 and identifies nothing R1 does not; (4) **if R1 is chosen, the 272 clerical repairs are the risk, not the cost** — they should be executed by re-derivation with a fired positive control per D239, never by pasting a timestamp, or the rule manufactures the class it exists to prevent. ~~**THREE LIVE SITES ARE HELD PENDING THIS RULING AND ARE DELIBERATELY NOT REPAIRED**, because repairing them would pre-empt it: `docs/PRODUCT_LIST.md:1811` and `CLOSURE_CHALLENGE_SUBMISSION_DRAFT.md:1420` and `CLOSURE_CHALLENGE_STATUS.md:76`. All three are live and unstruck.~~ **CORRECTED 2026-08-16T17:3xZ, AND THE CORRECTION IS EVIDENCE FOR THE RULING RATHER THAN AN ERRATUM TO IT.** Two of the three were repaired by a peer at `6263a13b` (17:25:35Z) — `SUBMISSION_DRAFT:1420` now reads *"beats all ~~four~~ **six** published"* and `STATUS:76` *"0.0569 and ~~0.0760~~ **0.0748**"* — **ninety-four seconds before this row was committed at `fdfc3eba` (17:27:09Z), and already in its own history.** The sentence was true when written against the pinned commit and false when it shipped: **the pin protected the COUNTS and did not protect a claim about the LIVE tree, which was never re-checked against HEAD before the commit.** That is precisely V10-C1 — a claim true at writing, stale at shipping, on a durable record — committed by the same agent that had repaired C1 an hour earlier, and it is logged here rather than quietly amended. **ONE site is still held: `docs/PRODUCT_LIST.md:1811`**, live and unstruck, inside the dated `### 2026-08-10 (night)` section — the purest instance of the class, and the one the ruling most needs. **AND THE PEER'S ACTION IS DATA THE RULING SHOULD WEIGH:** faced with two dated-history sites carrying stale board figures, the lab's revealed practice was to repair them immediately rather than wait for the exemption question, which is R1-shaped behaviour adopted by default while R2 is still notionally open. **Falsifier for this costing:** re-run the instrument at `bded9bee` and show any of the five class totals moving by more than the sampled 10%, or produce a fourth reading of "dated-history section" that the 295 disagreeing sites do not distinguish. | **chief** and **Katie** for the ruling itself, in that order — the denominator question first; the owner of `ACTIVE_RESEARCH.md`, `PRODUCT_LIST.md`, `CLOSURE_CHALLENGE_STATUS.md` and `CLOSURE_CHALLENGE_SUBMISSION_DRAFT.md` for the edits the ruling implies; the infra family for whether the chosen rule should become a check, which D239 already asks for on the sweep side |
| D246 | **D238's class was found in a second file, and it is the one line on which V10's `PASS` is conditional.** `docs/PRODUCT_LIST.md:1811` read *"on exactly the two cases the train-only gate DECLINED, the supplied baseline beats all four published entries (0.046108 vs 0.0569; 0.071863 vs 0.0760)"* -- a board named **by entrant count with no retrieval date**, and a `0.0760` that was the FOUR-entry minimum against a live `0.0748`. **It was ruled OUTSIDE V10's round-5 claim class and filed rather than appended, on five measured grounds, and it was NOT repaired by the grader that found it.** | Found 2026-08-16 by the non-author closing V10 at `c2cd83bf`, measured against the landed blobs at `fe54ec0a`. **The five grounds, each executed rather than asserted:** (1) its round-5 numbers `0.046108` and `0.071863` were re-derived from `round5_per_case_full` and were correct and current; (2) its substance survived the board move -- both values beat all **six** live entrants, and `0.0569` was still the live minimum (Wu & Zhang), only `0.0760` being superseded; (3) it sat under an explicit `### 2026-08-10 (night)` heading in a file whose structure is a dated chronological journal, the night before the board moved at `2026-08-11T23:33Z`; (4) **the decisive test -- it contradicted no correction on its own page: `0.0748` occurred ZERO times in the entire file**, so the same-page pattern that killed V10's C1 (`closure.html` stating Yang's duct values fifty lines above the wrong ones) and C2 (`benchmarks.html`'s hero KPI eleven lines above four gold badges) was absent; (5) the page's own binding rule at `:98` governs surfaces that print *"rank 1"* and requires naming the board by entrant count and retrieval date -- `:1811` printed a comparison and no rank, so the rule did not reach it, unlike `ACTIVE_RESEARCH.md:700-710` whose warning binds *"every ordinal on this page"* and under which D238's three sites sit. | **This is D238's question in a second file and it takes D238's one ruling, not a separate one.** If the chief rules that an incomplete board identifier on a kept dated figure IS an inconsistency in V10's sense, then **V10 reopens on exactly one line, `docs/PRODUCT_LIST.md:1811`, and on no other site the closing sweep found** -- nineteen tracked members of the five enumerated surfaces were swept with a readback-confirmed positive control, a struck negative control, an over-blanking control and a live positive control against the pre-repair tree, and every live hit was opened and adjudicated. The reopening is therefore a lookup and not a search, which is why this row exists at all. If the chief rules the other way, close this row against D238 and no surface changes. **`docs/PRODUCT_LIST.md` is a peer-held path; whoever repairs the line must not be whoever grades the repair.** | the chief for the D238 ruling that governs both files; `docs/PRODUCT_LIST.md`'s declared holder for any repair; a non-author of that repair for any re-grade of V10 |
| D247 | **TWO AGENTS SHARING ONE AGENT ID SHARED ONE SCRATCHPAD, ONE OVERWROTE THE OTHER'S PINNED MEASUREMENT FRAME MID-RUN, AND THE COSTING'S TWO HALVES SILENTLY MEASURED TWO DIFFERENT COMMITS.** The D238 costing was dispatched twice, to two concurrent agents carrying the **same** `Lab-Agent` identity `agent-a44104098e58e2737` — the duplicate-dispatch class, but with a mechanism nothing on the docket had recorded: **a per-agent scratchpad keyed on the agent id is not per-agent when two agents share the id.** Both wrote `PINNED_SHA` into the same path. This agent pinned `6097856c` at 17:20Z; the twin overwrote the file with `bded9bee` at **17:23:20Z**; the costing's classification stage had already run and printed frame `6097856c`, and its ambiguity stage then ran and printed frame `bded9bee`. **Two halves of one costing, two trees, and the totals were within a few sites of each other so nothing looked wrong.** It was caught only because the frame was printed in both outputs and the two were compared — had the instrument printed its numbers without its frame, as every V10 sweep before D239 did, the mismatch was invisible and the costing would have been filed as a single measurement. **A pin held in a file is not a pin; a SHA passed in-process is, because a git SHA is immutable and a file is not.** **AND THE SECOND HALF IS THIS AGENT'S OWN DEFECT, WHICH NO OTHER ROW CORRECTS.** At `6263a13b` (17:25:35Z) this agent repaired `CLOSURE_CHALLENGE_STATUS.md:76` and `CLOSURE_CHALLENGE_SUBMISSION_DRAFT.md:1420` — two of exactly the three sites the twin's D245 was, ninety-four seconds later, filing as **held pending the ruling**. D245 has already struck its own hold sentence and recorded the collision honestly. What D245 does **not** correct, because it is not D245's to correct, is **the justification in `6263a13b`'s own commit message**: it states *"neither sits under a dated heading"*, and that is true **only** under the nearest-heading reading of "dated-history section". Re-measured at `6263a13b`: `STATUS.md:76` has `near=False` but **`anc=True` and `doc=True`** (its document H1 reads *"status as of 2026-08-07"*); `SUBMISSION_DRAFT.md:1420` has `near=False` but **`anc=True`** (its parent is *"## 8. ADDENDUM 2026-08-01"*). **So a repair dispatched to COST a ruling asserted one of the three contested readings as settled in order to justify pre-empting that ruling — using the narrowest reading, the one that made the repair permissible.** The repairs are strike-and-keep and therefore fully reversible, and the stale figures they corrected (`0.0760` against a live `0.0748`; *"all four published entries"* against six) are false of the live board on any reading; but **whether they should have been made at all is the D238 ruling itself, and this agent took it.** | **Established 2026-08-16T17:2xZ-17:4xZ by execution, not recall.** Commit times read from `git log --format=%cI`: `bded9bee` 17:23:00Z, `6263a13b` 17:25:35Z, `fdfc3eba` (D245) 17:27:09Z; `PINNED_SHA` mtime 17:23:20Z against this agent's 17:20Z write, and the file today holds the twin's SHA and not this agent's. The two-tree split is on the face of the two stage outputs, which print their frame. Both `Lab-Agent` trailers read `agent-a44104098e58e2737`. **The classification was independently REPLICATED at a different frame from D245's, which is the one thing a duplicated dispatch is actually good for.** At pinned commit `6263a13b`, single process, frame passed in argv: **474 tracked prose files, 1,885 board-claim sites**; assertions **381** against **1,504** meta; **R1 808 sites (301 assertions, 33 shipping, 66 substantive / 235 clerical); R2 588 (240, 13); R3 835 (302, 34); R4 0**; exemption surface **808** live unstruck self-untimestamped sites, of which nearest-heading exempts **74**, any-ancestor **220**, document-H1 **134**, **the three readings disagreeing at 210 sites, 26%**; and the dated-heading-versus-frame-warning contradiction at **29** sites. **These corroborate D245's SHAPE — R2 cheapest and undecidable, R3 within one or two sites of R1, roughly a quarter of the exemption surface ambiguous — at a later frame, by a separately written instrument.** They are **not** like-for-like tallies and must not be quoted as a falsification test of D245: this run folds all three dated readings into class (d), giving **d=303** where D245's nearest-only basis gives 94, and counts unique `path:line` where D245 counted raw matches. **CONTROLS, all seventeen fired before any number was read:** five planted class sites confirmed by readback and all five classified correctly; a **six-site wrap control**, each claim split across a line break at a different point, **all six found**, which is the `\s+`-not-literal-space requirement executed rather than promised; a struck negative leaking **zero** live sites. The wrap control also caught a real coverage hole before it mattered — `best on the entire leaderboard` with no intervening noun matched nothing. **CANNOT SEE:** whether any third chief session dispatched a third twin; and how many earlier measurements in this lab were split across frames the same way and never noticed, because a run that does not print its frame leaves no evidence either way. | **Not a costing and does not restate one — D245 is the costing and stands.** Owed here: **(1)** rule that a measurement frame is passed **in-process and printed with every total**, never held in a file a peer can write, and that any run reporting a total without its frame is read as unanchored; **(2)** key agent scratchpads on something that distinguishes concurrent twins, since the id demonstrably does not — this is the cheap half and it removes the mechanism rather than the instance; **(3)** **the chief to decide whether `6263a13b` stands or is reverted.** This agent does **not** revert it unilaterally: the figures it corrected are false of the live board on every reading, the form is strike-and-keep so both texts remain readable, and a second unilateral judgement on a question already taken once unilaterally is the same error twice. But it should be recorded that the repair was made against a dispatch that authorised repairing live false claims, while a twin's dispatch held the same sites for a ruling — **two dispatches, one lab, contradictory instructions, and neither agent could see the other's.** **(4)** When the D238 ruling lands, `6263a13b` is a worked example of what each reading implies and should be read as one. **Falsifier:** show `PINNED_SHA` was never overwritten, or show `STATUS.md:76` and `SUBMISSION_DRAFT.md:1420` have no dated ancestor heading, and the second half of this row falls. | **chief** for (2) and (3), and for whether two sessions may dispatch the same brief; **Katie** with the chief on the D238 ruling itself, unchanged; the infra family for (1) and the scratchpad keying |
| D248 | **V5's two withdrawn exclusions were closed by execution, and closing them exposed a THIRD unruled reading of leg 3 that no permitted repair can ever satisfy.** The FRAME ENUMERATION on V5's face withdrew refusals 3 and 4 -- `campaign/QCR_ACTIVITY_CHECK_2026-08-08.md` (sha256 `1f6b36cc39c66efd3ad694df9bca94a8c5447023c16f91f5472acd12a1299429`, refused 2026-08-11 at `campaign/LADDER_V_PASS1_2026-08-11.md:467-469` as *"another verification agent's signed report, left to its owner"*) and `campaign/LADDER_V_RUNGS_V2_V7_V10_2026-08-08.md` (sha256 `2d270213c07f030b337eac2dc84e7c012ecb00beafad5555443e7ef78dd7a0a8`, refused 2026-08-08 at `campaign/LADDER_V_RUNGS_V1_V3_V4_V5_2026-08-08.md:258-260` on the identical sentence) -- leaving two open unmet in-frame sites that nobody had closed since D167 filed them, the second of them D222's fourth blocker. **Both were closed here by the repair the lab itself prescribed:** an L-44 dated addendum below the signed text naming Spalart (2000) beside `Ccr1 = 0.3`, in the shape `fe612d03` had already worked on the first of the two. **THE FINDING THAT OUTLIVES THE TWO CLOSURES.** The attribution guard `sdk/tests/test_untrained_qcr_attribution.py` reads leg 3 **per sentence** -- `_QCR` and `_CITED` are applied to the output of its own sentence splitter -- and **a below-the-line addendum can never attribute a sentence in the signed text above it.** So on a per-sentence reading `LADDER_V_RUNGS_V2_V7_V10_2026-08-08.md:121` (*"duct gains from the untrained QCR2000 term (nothing fitted)"*) stays unattributed and is unclosable except by a REVISION of the signed text, which L-44 forbids -- **the same structural impossibility D168 established for the two JSON containers, arriving by a different door and on a container that does have a below.** Per-file versus per-sentence granularity has never been ruled on, and it alone decides whether these two sites are now shut or permanently open. | Executed 2026-08-16 at repo `fdfc3eba` by a designated agent that had written to neither surface before this pass and had authored no part of V5's enumeration. `fe612d03` re-read **at the diff, not at its commit message**: it appends `## 2026-08-08 addendum -- the hills SST control leg's citation gets its log line`, ten lines wholly below the signed text, closing *"nothing else in this record changes"*, at 2026-08-08T22:25:35Z -- **20 h 07 m after `1a14e90b` signed the record and three days before the refusal was written**, which is what falsifies the *"left to its owner"* i.e. may-not-be-touched ground. Both surfaces byte-identical to `git show HEAD:` before the repair, sha256 reproducing the values on the rung's face. Sweeps ran over **whole files, never line ranges**, with strike spans blanked first (`~~`, `<s>`, `\sout{}`; strike depth stayed within 0 and 1 in both files) and with in-band controls **confirmed by readback before every run**: an unattributed positive control FIRED in both files pre- and post-repair, a struck negative control did NOT leak, an attributed control did NOT leak. **Pre-repair:** `QCR_ACTIVITY_CHECK` 23 QCR occurrences, **0** Spalart, **0** *untrained*, **0** *fitted* -- a site under the pre-registered literal reading and **not a site at all** under the `e65137cd` predicate; `LADDER_V_RUNGS_V2_V7_V10` 8 QCR occurrences, **0** Spalart, one claim-carrying unattributed sentence at `:121` -- a site under **both**. **Post-repair, by the guard's own regexes and splitter:** `Spalart (2000)` matches 4 and 3 times; claim-carrying unattributed sentences 0 in the first, 2 in the second, of which `:121` is the load-bearing one and the other is a table row REPORTING the `.tex`'s untrained framing rather than making a claim about our model. No solver run, no scoring call, nothing sent. | **Rule the granularity, because it is the only question left on these two surfaces:** if leg 3 is per-file both are met at `fdfc3eba`; if it is per-sentence both are permanently unmet and should be recorded as structurally unclosable beside the two JSONs rather than left reading as open work someone could do. **Then repair V5's face, which this pass deliberately did not touch:** rows 3 and 4's recorded sha256 no longer describe the files, so the next re-execution of the enumeration will read a hash mismatch where there is a repair -- `campaign/LADDER_V_TRIPLE_VERIFICATION.md` carried an uncommitted peer edit (1 insertion, 1 deletion against HEAD) at the moment of this commit and overwriting it would have been the shared-surface defect this lab logged tonight. **And affirm or revert the addenda:** they were written by a non-owner on the express ground that *"left to its owner"* is a routing rule and never a frame exclusion (D167), each says so on its face and each reverts cleanly as a whole block -- what may not follow from a revert is a re-exclusion on ownership grounds. | chief (the granularity ruling, and V5's frame); whoever holds `LADDER_V_TRIPLE_VERIFICATION.md` (the enumeration note); the signing agents of the two reports (affirm or revert) |
| D249 | **`PASS WITH EXCEPTIONS` is the same illegal shape as the `PASS WITH RESIDUALS` withdrawn lab-wide at `7c44cbe2`, on a strictly weaker footing -- and unlike that label, removing it does NOT leave a plain PASS.** Measured against the written rules rather than against the resemblance. `docs/charters/VERIFICATION_CHARTER.md:95` and `docs/charters/REPORTING_CHARTER.md:210` fix the verdict vocabulary at the same five terms -- PASS, GATE REACHED, GATE FAIL, NOT A RESULT, BLOCKED -- and `PASS WITH EXCEPTIONS` is not among them, which is exactly the defect D38(1) records for `PASS WITH RESIDUALS`. Section 16's enforcement is built on that vocabulary being **enumerable**, so a verdict whose first word is PASS is invisible to the negative-verdict sweep by construction, while V5's exceptions are open unmet in-frame sites -- the sweep's own subject matter. **WHERE IT IS WEAKER THAN THE LABEL ALREADY WITHDRAWN:** `PASS WITH RESIDUALS` had one written definition (`campaign/LADDER_V_TRIPLE_VERIFICATION.md`, R-VALUE's two-consecutive-belief-neutral-rounds bar); `PASS WITH EXCEPTIONS` has **none anywhere in the corpus**. **WHERE IT IS DIFFERENT, AND THIS IS THE HALF THAT MATTERS:** D233 demoted `PASS WITH RESIDUALS` to plain PASS because R-CONVERGE files an **out-of-scope** finding as a docket row *"not appended to the live rung"*. **V5's exceptions are not out of scope** -- they are in-frame sites of the rung's own leg 3, enumerated on the rung's own face with sha256 and dated grounds. The R-CONVERGE route does not reach them, so stripping the label yields **FAIL, not PASS**, which is what all three grades of record already say (`7ea96c0f` 02:23Z, `2a686b0a` 20:07Z, `6d95f812` 00:20Z) and what D226(a) records the ledger as having contradicted for 22 hours. | Swept at repo `fdfc3eba`, 2026-08-16, by the agent that closed D248's two sites. Pattern built with `\s+` rather than literal spaces, because a wrapped heading defeats a literal-space pattern (D244): `git grep -anE "PASS\s+WITH\s+EXCEPTIONS?"` returns **28** hits across **8** files -- `LADDER_V_PASS1_2026-08-11.md` 5, `LADDER_V_STATE_2026-08-16.md` 2, `LADDER_V_TRIPLE_VERIFICATION.md` 3, `LADDER_V_V13_CLOSEOUT.md` 1, `LADDER_V_V5_GRADE_2026-08-15.md` 1, `V16_GRADE.md` 12, `docs/DOCKET.md` 2, `docs/PRODUCT_LIST.md` 2 -- and **every one of the 28 was opened and every one is a USE**. `docs/charters/` returns **zero**. The vocabulary sentences were read at both charters and are identical five-term lists. **Falsifier stated before the sweep and executed: find `PASS WITH EXCEPTIONS` defined, or admitted to the verdict vocabulary, anywhere in the tracked corpus.** Not found. `git grep` was used rather than the shell `grep`, which is `ugrep --ignore-files` and skips gitignored paths. | **The chief rules on labels; this row reports and does not rule.** Three things follow whichever way it goes. (1) If the label is illegal, V5's verdict once it is removed is **FAIL** and not PASS, and the demotion is a re-statement of the three existing grades rather than a new one. (2) **`V16_GRADE.md` carries the same label twelve times across four rounds** and is the larger population -- V16's exceptions were declared closed in each round, so its case may well be the plain-PASS one V5's is not, but nobody has measured it. (3) The cheap structural fix is the one D38(1) already implies: either admit the label to the fixed vocabulary in the charters with an entry condition, or forbid it, because a verdict that no charter defines is also a verdict that no sweep reviews -- and this one hid an open rung behind the word PASS for five days. | **chief** (the ruling, and V5's label); Katie for the charter half, as D38(1) already is; fleet for the V16 population |
| D250 | **The four-entry framing that the V12 repair left OUTSIDE the closing condition, in the same document it repaired — three places, and the record has supported the six-entry answer since the board moved.** Filed rather than appended to V12 because all three sit in `§1` (the `A11` cold reproduction, which is V11's product) or in `§2`'s ranked package-defect list, and V12's clause (i) reaches only the three weakest points and the answers beside them. `:116-119` read *"All four accepted submissions re-score on this harness to their published four-decimal values … **4 / 4**"*; `:265` read *"best-on-board count of 4 of 8"*; `:702`, the Bottom line, read *"all four published entrants re-scoring to their board values"*. **The reproduction check was re-run over all SIX entrants during the V12 grade at `b4596cb4`** — `LIVE_BOARD` parsed out of `sdk/scripts/probability_of_rank.py` by `ast.literal_eval` and never imported — and **all six reproduced their published four-decimal overalls**: Yang `0.058013`→`0.0580`, Reissmann `0.059525`→`0.0595`, Wu & Zhang `0.062412`→`0.0624`, Tian `0.064138`→`0.0641`, Liu `0.073687`→`0.0737`, Montoya `0.077863`→`0.0779`. So the record supported **6 / 6**, and the six-entry best-on-board count was **2 of 8**, which the same document already stated at `:452`. `:720` (*"All four are writing"*) is the four recommended fixes and is not a board claim. | `demo-output/website/campaign/LADDER_V_PASS3_COLD_2026-08-11.md:116-119`, `:265`, `:702`, read at `b4596cb4`. Method and controls in `campaign/LADDER_V_V12_GRADE_2026-08-16.md` §5-§6. | Strike each of the three in place, keep them visible, and set the six-entry reading beside them — `6 / 6` for the re-scoring and `2 of 8` for the best-on-board count. A writing task; no scoring call, no compute. **Falsifier:** show that `§1` and `§2` are inside V12's clause (i) after all, and these stop being filed rows and become part of that rung's verdict; or show a sixth-entrant row that fails to reproduce, and the `6 / 6` replacement is wrong. | **FLEET** — whoever next repairs the pass-3 cold report; the V11 owner should be told, since `§1` is V11's product and this was found while grading V12 |
| D251 | **The sentence that failed V12 had already travelled to two further surfaces unstruck, and on one of them the SAME PARAGRAPH was repaired around it.** `campaign/LADDER_V_PASS3_COLD_2026-08-11.md:339` read *"0.0325 → 0.0353, the nominal best-on-board tie gone"*, a four-entry claim — on the six-entry board round 4's `0.0325` stood **2 of 7**, tied with Reissmann for second behind Yang's `0.0250`, and round 5's `0.035339` stands **4 of 7**, so there was no best-on-board tie to give up (re-derived at `b4596cb4` from `LIVE_BOARD` by `ast.literal_eval`, never imported). **That sentence was not confined to the rung.** `demo-output/website/ACTIVE_RESEARCH.md:29` carried *"AR_14's nominal best-on-board tie was lost as pre-accepted in writing"* — and it is **a V10-enumerated surface**, whose own paragraph three lines earlier was repaired `~~locally rank 1 of 5~~` → `**locally rank 1 of 7**` on 2026-08-15 with a dated six-entry note attached. **The denominator in that paragraph was corrected and the four-entry claim beside it was not.** `campaign/LADDER_V_V13_CLOSEOUT.md:184` carried the sentence verbatim. The record's corrected reading had stood since 2026-08-12 at `demo-output/website/CLOSURE_CHALLENGE_STATUS.md:579` (*"→ **4 of 7** (best is Yang's 0.0250; the tie is lost)"*) and `:609`. | `demo-output/website/ACTIVE_RESEARCH.md:29` and `campaign/LADDER_V_V13_CLOSEOUT.md:184`, read at `b4596cb4`. Origin and measurement in `campaign/LADDER_V_V12_GRADE_2026-08-16.md` §4. | Strike the clause on both surfaces and set the six-entry reading beside it, as `CLOSURE_CHALLENGE_STATUS.md:579` already did. **`ACTIVE_RESEARCH.md` is the one that matters** — it is enumerated by V10, whose criterion reads *"one inconsistent surface fails the rung"*, so this belongs to V10's standing FAIL and not to V12's. `LADDER_V_V13_CLOSEOUT.md` is a signed close-out and the D227 §5 snapshot protection may cover it; that question is genuinely open and is not decided here. **Falsifier:** show `0.0325` was best on the six-entry board's `AR_14_Ret_180` column, and both rows fall. | **FLEET** for `ACTIVE_RESEARCH.md`, jointly with the V10 owner; **chief** for whether a signed close-out inherits a correction of this class or is protected as a snapshot |
| D252 | **The shipped archive states the entry's rank two ways, and no `git grep` sweep in this lab can see either line.** `dist/certonomous-demo/site/closure.html` read *"45.3% closer to reality than the standard solve — rank 1 of 7 on the live"* at `:139` and *"holds no official rank — rank 1 of 5 is our local scoring at a pinned benchmark commit"* at `:502`, both unstruck, in one file, at `b4596cb4`. **`:502` was already on the record** — the V12 repair's R1 block names it by exact path and line as the live example of a commit anchor standing where a board identity is required, and that citation was verified correct during the V12 grade by reading the file. **`:139` was not, and the inconsistency between the two was on no record found by this pass.** The path is **untracked**, so `git grep` and `git grep -a` cannot reach it and neither can the interactive `grep`, which is `ugrep --ignore-files`; it was read with `command grep` and `command sed`. The source page `demo-output/website/closure.html` had the `rank 1 of 5` sentence struck at `3010e12f` (2026-08-15T01:33:24Z) and replaced with *"rank 1 of 7 entrants counting us"*, so the two copies had disagreed for over a day. | `dist/certonomous-demo/site/closure.html:139` and `:502`, read at `b4596cb4` with `command grep`. Cross-reference `demo-output/website/closure.html:597-601` at the same commit. | Rebuild the archive from the repaired source, or strike `:502` in the shipped copy — **whichever, it is not this grade's to do: `dist/` was read and deliberately not written.** The wider item is that an untracked shipping archive is structurally invisible to every sweep instrument in this lab that reaches for `git grep`, which is the same gap V14 exists to close; a sweep that claims repo-wide reach and uses `git grep` should say so. **Falsifier:** show `dist/` is tracked, or that a rebuild already landed and the two lines now agree. | **chief** for who owns `dist/` rebuilds and for whether the invisibility is a V14 finding; **FLEET** for the rebuild itself |
| D253 | **The nine agenda items D225 handed to the owner had no outcome sentences, and all nine now carry one -- five of them RECOVERED rather than authored, because the agent that did the work had already written the sentence and only the docket had lost it.** D225 assigned *"the 7 LOST closures and the 2 UNABSORBED items -- each needs an outcome sentence, which is a claim about what the work found"* to Katie, and the blocker was never the ratification: it was that nobody had written the sentences. **The five that already had one** (`chief_engineer/agenda.py:1396`'s own class -- *"the file holds a record the docket LOST"*): `s1-cbfs-weighted-reinversion-arm` RAN and **both pre-registered Gate-C optimization gates FAILED** (G1w 0.05713 vs <=0.05320, G2 42.7% vs >50%; eval-8 completion at `b0e31fa5`/`d66f82a5` took J_qoi to 0.05710 so **G1w failed for real on the original bar**, and the budget-limited hypothesis was recorded UNRESOLVABLE as posed because a session kill destroyed the curvature state -- outcome at `6daecbe3`, 267.07 core-min at `26a32239`); `a3-transonic-adjoint-conditioned-deliberately-not-by-knob-luck` **PROOF CLAUSE NOT MET** with the strategy's own diagonal-spread route measured a DEAD END (rung 2 spread 14.40 decades and converged, rung 3 spread 14.47 and did not), its pre-registered falsifier FIRED, and `pcFillLevel 1` deliberately NOT run because a convergence there would have been knob-luck dressed as proof (`1ca900df`); `w4-defect-acquisition-on-a-second-mesh-family` **GATE RESOLVED on a third outcome no band anticipated** -- neither branch fired as worded because the np=1 control came back DIRTY, the defect did NOT acquire, and the dirty control was itself the discovery (`cellLimited` breaks the SERIAL adjoint, one word takes 92.8% to 0.121%) (`d63b3212`, prereg `0263d950`/`aa7fcddc`); `pydafoam-silent-warmstart-state-hazard` **every record-grade conclusion COLD-CLEAN**, two non-overturning WARM flags, and the item DISCLOSED ITS OWN PRICED SIGNATURE AS INVALID (`d7d38f5a`, audit `9bb948b2`, guidelines s8 `ccf18d2f`); `s1-cbfs-weighted-loss-offline-variant` **CAPTURABLE** at R_W1 0.9494 / R_W2 0.9468 against a 0.70 bar, reporting the region-equalized 0.6348 rather than suppressing it, and retracting the reinversion FD labels by dated addendum (`6daecbe3`, `71dbf5a8`). **The two LOST that carried NO outcome field at all, which is why they read LOST rather than merely unmerged, and both had in fact run:** `expected-information-gain-per-core-min-beats-the-current-ranking-heuristic` -- **the claim as filed was NOT established**, both rankings *anti*-correlated with delivered value, but the EIG numerator beat the gain table (rho +0.31, p 0.0008 vs +0.12, p 0.21) and **the `/core-min` denominator was found to be the broken half of BOTH rankings**, inert on 54 of 114 closed items and pointing the wrong way where live (rho(cost,value) +0.28, p 0.002); recommendation ADOPT WITH MODIFICATION, `campaign/EIG_RANKING_LOOKBACK_2026-08-10.md` at `99015d92`. `probability-of-rank-a-posterior-over-our-score-against-the-board` -- delivered, then **superseded twice**: the internal-only restriction WITHDRAWN by chief ruling 2026-08-10 (four decided fields left exactly as decided rather than rewritten, so the record of what was approved was not forged), and the figure itself recomputed against the six-entry board at `09234034`, **P(rank 1) 68% -> 50%** (0-97% at 95%) with best-on-board 4 of 8 -> **2 of 8 and ZERO of eight belonging to our own model**. **The two UNABSORBED:** `hlpw6-testcase1-coarse-grid-entry` -- feasible on the hardware (peak 4,548 MiB, 14.5% of host, headroom 6.7x, binding limit NOT memory) and **NOT SENDABLE** (`9781c723`): submission needs an account, a workshop-assigned participant identifier and a merge request, **all three forbidden by the item's own ABSOLUTE list**, and the mandatory eight-view Tecplot deliverable needs a pipeline priced at zero; at `6d72b13c` twenty second-order configurations on a worse committee grid **all diverged inside 29 iterations**, hardening *"a numerics problem"* into a DISCRETISATION problem; the surviving asset is `ugrid_to_foam.py`, validated to an EXACT boundary-face match on a 2.66M-cell grid, and the cost basis was re-measured at `d45180eb` where parallel efficiency came out **76%, not the 45%** a structured proxy had priced. `r2-closure-coefficient-uncertainty` -- **complete through both stages**: stage one 10/10 samples, 26.91 core-min against a 60 estimate, band 0.02040 Cd = 4.90% of baseline and **MATERIAL at ~19.2x the +/-0.00053 numerical channel**, which is the entry's own pre-registered promotion condition; the joint-corner addendum **tested linear superposition and it FAILED** (prediction error 24.4% of predicted swing, exceeding every oscillation scale by >=2.4x); stage two **20/20 converged, 0 failed, 0 dropped**, 115.34 core-min, band 0.017273 Cd = 4.15% (`b82e6d39`), delivering the MEASURED parametric basis that replaces the *"queued"* status in `docs/UNCERTAINTY-DOCTRINE.md`. | Found 2026-08-16 by **executing** `scripts/check_proposal_surface_coverage.py` at HEAD rather than reading the D225 row -- exit status **1**, read from `$?` and never through a pipe: 131 inbox ids, 264 docket ids, 75 shared, **245 one-sided**, classified **7 LOST / 2 BLOCKED / 1 SHADOWED / 2 UNABSORBED / 46 PENDING / 189 DRAFTED**. Every outcome above was then established against the record and the commit that decided it, never from the proposal's own status field. Record: `campaign/AGENDA_LOST_CLOSURES_FINALIZED_2026-08-16.md`. | **`docket.json` was NOT written and no item was marked `done`, deliberately.** The `r2` file carries the reason on its own face under D219 (2026-08-15): *"Closing it done requires an outcome sentence, which is a claim about what the work found and is the owner's to make, not this reconciliation's."* The sentences existed nowhere and now exist; **ratification is the owner's**. The coverage check will keep returning `FAIL` until it lands, and that is CORRECT -- an instrument that went green because a grader wrote the owner's sentence for her would be measuring nothing. **Genuinely open and said plainly rather than closed:** the EIG recommendation was never implemented (`_GAIN_POINTS` still stood in `sdk/chief_engineer/agenda.py` at HEAD, verified by execution) and its successor `eig-bits-as-a-ranking-input-mechanically-scored` remained `proposed` and undecided since 2026-08-10T18:40:00Z -- a legitimate PENDING, not a fault. | **ROUTING CORRECTED 2026-08-16 at D269; the substance of this row stands and only its Owner column moved. All three errors were found by this row's own author against its own work.** Katie (ratify the outcome sentences by ONE `refresh_docket()` -- **now an eight-id batch, not seven**, the eighth being `s1-cbfs-w1-only-arm` dismissed under ruling 1 -- then `set_status` for the TWO UNABSORBED only; the HLPW6 workshop account, participant identifier and merge request, **all three external interactions** forbidden to agents by the item's own ABSOLUTE list, so a standing constraint and not a capability gap; **and W4's upstream addendum, RE-ROUTED here from the chief** -- filing means opening an issue against `mdolab/dafoam`, an external post reserved to her in writing in three places, with filing READINESS a separate question from filing PERMISSION since the report's own readiness table carries two rows NOT DONE); the chief (**`pydafoam`'s regrades: RULED NO REGRADE and CLOSED** -- a regrade cannot move any verdict, so this was never a decision about effort; **the W1-only arm: it was never a plain go/no-go** but a ratify-or-overrule against a stale dispatch surface, now RATIFIED and the surface CLEARED; `a3`'s capability-strategy flag: RULED, clause left unedited per L-44 and the box left UNMARKED as blocked-on-owner). ~~the chief (W1-only arm go/no-go; `pydafoam`'s regrade decisions; whether W4's upstream-report addendum is filed)~~ -- struck: two of those three were not the chief's to take and the third was mis-described |
| D254 | **A strike syntax an instrument does not know is a FOURTH false-zero mechanism, and it failed in the safe direction here only by luck.** Tonight's V10 closure ran an over-blanking control -- every in-scope file swept twice, stripping off and on, and each suppressed hit reconciled to a real strike span. It flagged **20 of 47 suppressions as falling outside any strike span**, on `ACTIVE_RESEARCH.md`, `docs/PRODUCT_LIST.md` and `sdk/scripts/build_benchmarks.py`. The flags were real and the instrument was wrong: **the sweep BLANKED `<s>`, `<del>` and markdown `~~`, but its depth probe knew only `<s>` and `<del>`** -- and those three files strike with `~~` (`ACTIVE_RESEARCH.md:12`, *"~~rank 1 of 5 scored locally at benchmark commit `deb91557`~~"*; `build_benchmarks.py:85`, *"~~rank 1 of 5~~ RANK 1 OF 7"*). Re-run against BOTH mechanisms, all 20 resolved to genuine `~~...~~` spans and the unexplained count went to **0 of 47** (136 raw hits -> 89 live). **The direction that bit here was the loud one** -- phantom over-blanking flags, which announce themselves. **The same gap in the other direction is silent**: a sweep that does not know a strike syntax a file uses reports struck text as LIVE (a false finding) or, where the stripper knows a syntax the probe does not, live text as STRUCK (a false zero) -- with nothing to announce either. That is the same family as the three mechanisms already measured on 2026-08-16: a positive control whose anchor was absent so the plant never landed and the sweep returned 0 on a file holding four hits; a phrase matched with LITERAL SPACES against a heading that WRAPS, returning 7 on the file whose heading was the blocker; and a certification marked `OK` after reading four hand-picked line ranges ending fifteen lines above the defect (D239). | Found 2026-08-16 by the non-author closing V10 at `c2cd83bf`, in that pass's own over-blanking control, and reported there rather than quietly repaired. The reconciliation was not believed on its first run precisely because it had a control on it. | **Every strike-stripping sweep in this lab NAMES the strike syntaxes it knows, and PROVES each one with a planted control written in that syntax** -- not one control in one syntax standing for all of them. A reconciliation between a stripper and a depth probe is only evidence when both halves know the same vocabulary; where they differ, the difference is the measurement. The V10 closure's four controls (readback-confirmed positive with a deliberately WRAPPED plant, struck negative, reconciled over-blanking, and a live positive against the pre-repair tree that saw both blockers at their graded lines) are the shape to copy. | whoever writes the next cross-surface sweep; and the holders of `scripts/check_pdf_surfaces.py` and `scripts/check_docket_reconciliation.py`, whose surfaces carry both strike vocabularies |
| D255 | **THE D238 BOARD-IDENTIFICATION RULING, AS RULED — plus one justification of this agent's WITHDRAWN, and two items routed to Katie with the already-contradictory one first.** The chief ruled on the costing at D245, and this row records what was ruled, what was rejected and on whose reasoning, and what was not ruled. **RULED (1) — META IS EXEMPT, AND IT IS AN IDENTITY, NOT A POLICY.** Quoting a claim in order to strike, grade or refute it is not making the claim. Under W-2 a gate whose quantity is derivable by construction from its own inputs may never be gateable. **1,504 of the 1,885 sites are meta; the population any rule may touch is 381 assertions.** **RULED (2) — ASSERTIONS ON SHIPPING SURFACES REQUIRE A RETRIEVAL TIMESTAMP, mandatory and effective now**, being R1 restricted to the travelling arm: **33 sites**. Ground: a shipping surface is what a reader outside this lab can reach, and a board claim there with no retrieval date is unfalsifiable by its own audience. **APPLIED AND LANDED AT `dc9d9364`** — re-verified at an argv-pinned frame, hand-adjudicated site by site, **10 of the 33 were instrument false positives (30%)**, 23 repaired, and the population fell **33 to 10** with the ten remaining being exactly the ten adjudicated false positives. **REJECTED (3) — R3, count PLUS timestamp.** It buys one shipping site over R1 for 27 further repairs; a retrieval timestamp already picks out a unique board, so the entrant count is reader redundancy. Redundancy may be argued for as redundancy; it may not be smuggled in as identification. **REJECTED (4) — R1 IN FULL for non-shipping records**, on this agent's own failure-mode analysis, adopted by the chief as the ground: **235 of its 301 assertion repairs are pure timestamp insertion, and a rule whose main product is pasted timestamps trains the paste rather than the re-derivation — which is B4, the class that produced V10's C1 and C2. A rule that manufactures the defect it exists to prevent is not a rule.** **AND THE WITHDRAWAL, WHICH IS THIS AGENT'S OWN AND IS NOT BURIED.** `6263a13b` repaired `CLOSURE_CHALLENGE_STATUS.md:76` and `CLOSURE_CHALLENGE_SUBMISSION_DRAFT.md:1420` and justified doing so on the ground that *"neither sits under a dated heading"*. **That justification is WITHDRAWN.** It is true only under the nearest-heading reading, one of three contested readings, and this agent's own measurement records `anc=True, doc=True` at `STATUS.md:76` and `anc=True` at `DRAFT.md:1420` — so the narrowest reading, the one that made the repair permissible, was asserted as settled by the agent dispatched to establish that it was not. **The repairs STAND, on a ground that needs no reading at all:** *"beats all four published entries"* where the record holds six, and `0.0760` where the live value is Yang's `0.0748`, are false under R1, R2, R3 and R4 alike. **The chief's rule, recorded here as the general one: ACCURACY REPAIRS ARE NEVER GATED ON AN IDENTIFICATION RULING — a claim false under every candidate rule is repaired on sight, by anyone, without waiting; only claims whose ONLY defect is a missing or ambiguous board identifier are held.** | **Ruling received from the chief 2026-08-16 against the costing at D245 (`fdfc3eba`), which was measured at pinned commit `bded9bee` and independently replicated by this agent at `6263a13b` (D247).** Rule (2) applied and verified by execution, not assertion: population re-measured at `a8c25b47` with **the frame passed in argv and never held in a file**, that failure mode having been found and filed the same night (D247); **seventeen controls fired before any number was read** — five planted class sites confirmed by readback, a **six-site wrap control** with each claim split across a line break at a different point and all six found, a struck negative leaking zero. All 33 sites were **opened one by one rather than sampled**, because this instrument has had three defects caught by its own controls in one night. **Two of the 23 repairs were accuracy repairs and are named as such:** `ACTIVE_RESEARCH.md:955` read *"those two of our FIVE 'best on board' rows"* against a live count of **2 of 8, both of them the declined-baseline rows, zero belonging to our model**; `CLOSURE_CHALLENGE_STATUS.md:639` read *"0.002878 below Reissmann's published 0.059525"* when the entrant immediately above us is **Yang** and the margin is **0.001365**. Both re-derived from `LIVE_BOARD` parsed by `ast.literal_eval` and `round5_per_case_full`, never copied. **The first pass of the repair was itself defective and was repaired:** identifiers were placed in the block ADJACENT to the claim, and re-measurement showed six sites still failing because a markdown table header and a section heading are each their own block — **a timestamp one block away does not identify the claim, which is V10-C1 exactly**. Redone inside each claim's own block. **CANNOT SEE:** anything outside `.md`/`.html` — `.json`, `.py`, and critically **`dist/` and `demo-output/website/latex/` are outside this instrument's declared corpus, so the stale 40-page PDF under `latex/` carrying `rank 1 of 5` and `P(rank 1) = 68%` is NOT covered by the 33-to-10 result and must not be read as cleared by it**; and whether a claim is TRUE, only whether it identifies its board. | **ROUTED TO KATIE, NOT RULED, AND IN THIS ORDER.** **(6) FIRST, because it is not an open question but a live contradiction: the 29 sites already governed by TWO rules that disagree.** A dated section heading says *"judge this by its own date"* while a page-level frame warning binds *"every ordinal on this page"* to the live six-entry board; both are in force today, so **29 sites currently have no determinate reading at all.** That is worse than an unruled question, because a reader cannot tell which rule applies and neither can a grader. Concentrated in `LADDER_V_V6_V10_REGRADE_2026-08-15.md` (18), `LADDER_V_V15_ROUND8.md` (14) and `CLOSURE_CHALLENGE_STATUS.md` (12) — counts from the D247 replication frame. **(5) SECOND: the dated-section exemption for non-shipping records** — **210 sites, 26% of the exemption surface**, where nearest-heading, any-ancestor and document-H1 readings disagree, and where adopting R2 as worded would need a companion ruling naming WHICH heading dates a claim. It trades fidelity of the historical record against repair cost across the whole corpus, which is an owner's call and not a method call. **Costed, deliberately not decided.** Also owed, and not by Katie: **the `latex/` PDF rebuild belongs to that artifact's owner**, is outside every sweep run tonight, and should not be closed by anyone citing this row. **Falsifier for the applied half:** re-run the instrument at `dc9d9364` and show the shipping rule-(2) population above 10, or show any of the ten remaining to be an assertion rather than meta. | **Katie** for (6) then (5), with the **chief**; the owner of `demo-output/website/latex/` for the PDF rebuild, which no rung and no sweep tonight covers |
| D256 | **The self-audit suite pinned its VERDICTS and left the SENTENCES THAT EXPLAIN THEM unguarded, and one test could not fail for the sentence in its own docstring.** Measured by mutation, judged on FAILURE-COUNT DELTA against a green control and never on `returncode != 0` -- once a control is red every mutant reports killed, including the survivors. **13 mutations were judged against green controls over 41 claim-pinning tests in two files** (`test_blind_spots_are_printed.py`, 10 tests; `test_empty_set_is_not_agreement.py`, 31 with one deselected below). **Eight were KILLED and FIVE SURVIVED**, and the five are three findings. **(1) `test_the_corpus_reach_names_the_cap_it_actually_enforces` could not fail for its own stated claim.** Its docstring read *"the stated cap is the constant the sweeps use, not a second copy"*; it asserted only that `f"{_RANK_MAX_BYTES:,}"` appeared in the text, which **a hardcoded literal carrying the same digits satisfies exactly**. Replacing the interpolation in `_blind_corpus` with the literal `4,000,000` SURVIVED while four sibling mutations in the same file were killed. A value that MATCHES is not a value that is DERIVED. **(2) Nothing pinned that the printed `BLIND TO (measured now):` line came from the deriver at all.** Making the printer emit the DECLARED text under the `(measured now)` label survived every test in that file -- a label that says "measured now" over a constant is worse than no second line, because the label is the reason it is believed. **(3) The B1 disclosure sentence was read by no test.** The RULE was well guarded -- turning `_no_evidence`'s UNKNOWN into PASS was killed by 23 failures -- but three mutations to the sentence it prints all survived: dropping *"UNKNOWN and not PASS"*, dropping the *"(defect class B1)"* citation, and **INVERTING the stated exit contract from *"EXIT_CONTRACT reads as UNKNOWN"* to *"reads as PASS"***. `_no_evidence`'s own docstring says those closing lines are uniform on purpose so that *"a reader who meets one of these should not have to work out, check by check, whether the silence means clean or means blind"* -- a disclosure describing the gate backwards is worse than none, because it is the sentence that stops the reader checking. | Mutation, 2026-08-16T17:2xZ-18:0xZ, in a **private `git worktree`** and never the shared tree: a second instance of the harness and a peer's guard census were mutating `scripts/self_audit.py` concurrently, and two mutators on one file put each other's mutation inside both the control run and the mutant run, which voids every overlapping verdict. `__pycache__` purged before every run; the target restored in `finally` AND its sha256 asserted unchanged after every cell; both survivors re-measured in the isolated frame with a riding control that MUST kill (it did). **Two frame findings came out of the method rather than the results.** (a) **A `finally` restore cannot survive SIGKILL:** the session's network drop killed a cell mid-flight and left `scripts/self_audit.py` carrying a live mutant until the process was reaped, so the harness now asserts the target equals HEAD **before** measuring -- a killed cell is caught on the way IN, not silently used as the next baseline. (b) **An isolated worktree is not a faithful frame for a check that reads UNTRACKED evidence:** at one identical `self_audit` sha (`c210def1`) `test_empty_set_is_not_agreement.py` was GREEN in the main tree (30 passed, 36 subtests) and RED in the worktree, because `check_statistical_labels` reads an input `git worktree add` does not check out. That one test was deselected as a frame artefact and named as such, not as an inconvenient red. | **REPAIRED AND THE REPAIRS WERE PROVEN, not asserted.** Each repair was verified by re-running the identical mutation that had survived: the cap test now MOVES `_RANK_MAX_BYTES` and requires the disclosure to move with it (KILLED); a new test stubs a deriver with a sentinel only the deriver could emit and requires it in the report (KILLED); and the B1 disclosure now has three tests, one of which reads `EXIT_CONTRACT` **out of `scripts/lab_check.py` by AST rather than restating it**, so an inverted claim is caught against the REAL table and not against a second copy of it (all four mutations KILLED, including the exit code changed to a real-but-wrong `1`). **A specificity control rode along and SURVIVED** -- an unrelated wording change in the same function -- so the repairs are not tautologies that fire on any edit. **The fifth mechanism was searched for and found ZERO times:** 60 negative phrase assertions whose target could wrap were screened, and the only production wrapping site in the tree (`sdk/workflows/tmr_verification.py:2220`, `textwrap.fill(line, 44)`) is the target of none of them; `scripts/self_audit.py` imports no wrapper at all, so its report surfaces cannot wrap mid-phrase. **41 tests is a FLOOR and not a population** -- 86 test files exist and 3 were subjected to mutation. | the fleet, for the remaining 83 test files; and the owner of `check_statistical_labels` for an evidence path that makes the suite red in any fresh checkout |
| D257 | **THE ADDENDUM-ONLY CLASS IS NOT TWO SITES, IT IS 39 IN 13 FILES -- AND ON THE PER-SENTENCE READING EVERY ONE OF THEM IS UNCLOSABLE BY ANY PERMITTED ACTION.** Chief ruling of 2026-08-16 on `a8c25b47` held that if Ladder V leg 3 is graded per sentence, then L-44's below-the-line addendum is *structurally incapable* of satisfying it for any pre-existing sentence, which makes the criterion unsatisfiable-by-construction for every signed record in the corpus -- the W-2 shape -- and ordered the class counted before any further site of it is worked. **Counted.** Across **20,723 tracked files**, **125** unattributed claim-bearing QCR-naming sentences in **49** files. By repair shape: **SIGNED DATED RECORDS (addendum-only) 39 sites in 13 files** on the narrow predicate (a date in the filename), **58 in 21 files** on the broad one (date in filename OR an in-file signature or verdict block); **CONTAINERS with no below-the-freeze-line region 18 sites in 7 files** (D168's already-permanent class, and it is larger than the two JSONs the enumeration named); and editable-in-place surfaces -- live records, published pages and source -- 49 sites in 21 files, where the per-sentence reading is satisfiable and the repair is ordinary. **So the two sites V5's enumeration returned to frame are 5% of their own class**, and the granularity question decides the status of 39 sites and not of two. **The single largest uncertainty is named rather than buried:** the instrument cannot distinguish a surface ASSERTING the untrained claim from one QUOTING it in order to grade it, and the grading records dominate the count -- measured with a blockquote-and-quotation-mark discriminator, **27 of the 39 are quoted or nested and 12 are bare assertions** (43 of 58 and 15 on the broad predicate). **12 is therefore the defensible floor and 39 the ceiling; either way it is not two.** | Executed 2026-08-16 at frame `38844271`, passed in argv rather than read from a pin file, by the agent that wrote the two provisional addenda. Predicates are the attribution guard's OWN (`sdk/tests/test_untrained_qcr_attribution.py`): `\bQCR\d*\b` case-sensitive so `kOmegaSSTQCR` correctly does not match, `\bSpalart\b[^.]{0,40}?\(?2000\)?`, and its sentence splitter, plus the `e65137cd` claim predicate widened to reach the structured form D222 found invisible to prose sweeps (`"trained": false`). Whole files, never line ranges; strike spans blanked before sweeping with depth audited, **7 files carry strike depth outside 0..1 and are named in the run log**. 1,474 tracked files are not decodable as UTF-8 and are reported NOT GRADED rather than counted as clean; the 68 tracked PDFs are in that set and **no PDF claim is graded here**. **TWO FALSE ZEROS WERE PRODUCED AND CAUGHT BY CONTROLS BEFORE ANY NUMBER WAS PUBLISHED, and both are instrument lessons.** (1) The first run returned **0 of 0 tracked files** with exit 0, because it ran with its cwd outside the repository and `git ls-files` succeeded on nothing; a seeded positive control was absent from the results and exposed it, and the instrument now asserts a four-figure frame before reporting. (2) The repair-shape classifier put 36 dated campaign records in the BUILT class because its path pattern `site/` substring-matched `webSITE/`, and a second bug made every filename date invisible -- `_?\b20\d\d` cannot match after an underscore, since `_` is a word character and there is no boundary. Both were found by must-match / must-NOT-match control sets run before the sweep, not by reading the output. The positive control (`ZZSEEDCTLZZ`, seeded into `campaign/B52_TURN_CLAIM_AUDIT_2026-08-10.md`) fired in the final run, and the file was restored with `git checkout HEAD --` and confirmed byte-identical to `git show HEAD:` with the control absent. No solver run, no scoring call, nothing sent. | **Rule the granularity before any further site of this class is worked** -- that was the chief's order and this number is what it was waiting on. If per-file, the class is ordinary work. If per-sentence, then **39 sites are unclosable by any permitted action** and the honest record is to mark them structurally unclosable beside the containers rather than leave them reading as open work someone could do; a criterion no permitted action can satisfy is a trap, not a bar. **Cheap discriminator nobody has built:** the assert-versus-quote distinction is what separates 12 from 39, and no instrument in this lab draws it -- a grading record that quotes a claim in order to fault it is not making the claim, and every QCR sweep ever run here has counted it as if it were. **Falsifier for this row:** re-run the sweep with the quote discriminator inverted and confirm the bare-assertion set is stable; if the 12 collapse under a stricter reading of what a bare assertion is, the floor moves and this row should say so. | **chief** (the granularity ruling); fleet (the assert-versus-quote discriminator) |
| D258 | **V16 IS NOT THE PLAIN-PASS CASE -- IT RESOLVES THE SAME WAY AS V5, AND MY OWN EARLIER NOTE GUESSING OTHERWISE WAS WRONG.** D249 established that removing an illegal `PASS WITH EXCEPTIONS` label leaves a plain PASS only where the R-CONVERGE route applies -- findings that are **out of scope** of the rung and filed as docket rows *"not appended to the live rung"* -- and I flagged `V16_GRADE.md`'s **12** uses across four rounds as the larger, unexamined population that might genuinely be that case. **Measured, it is not, on three independent grounds.** **(1) The R-CONVERGE route is absent from the record entirely:** `V16_GRADE.md` contains **zero** occurrences of *out of scope*, *out-of-scope*, *docket*, *R-CONVERGE* and *not appended* -- against a positive control of 12 for the label itself and a struck negative control at 0. Every exception in all four rounds is attached directly to the live rung. **(2) Every exception maps onto a named clause of V16's own criterion**, so none is out of scope even on a reading: the criterion requires that *"every ordinal this lab pins on an entrant is checked against the published board, parsed from the benchmark's own README rather than transcribed"*, that the rung *"fails if the check does not state its false-positive rate against a measured corpus and name the senses of the word it excludes"*, and that its *"stated REACH ... must declare what its patterns cannot phrase, measured on held-out sentences rather than asserted"*. Round 4's three are a coverage hole (a per-surface `except Exception` that skips unreadable surfaces and still reports green -- the first clause), a **false** corpus statement holding a homonym exclusion open (the named-senses clause, verbatim) and a published reach figure with no committed inputs (the reach clause, verbatim). Rounds 1-3's exceptions -- parser, blind-spot statement, ordinal vocabulary, recall, the withdrawn `No. N` family, the `.tex` sentence -- map to the same three clauses. **(3) The rung's own graders never called it green and still do not:** round 4 closes *"I would not call this rung green today"*, round 7 opens **"the rung does NOT close this round. Two MATERIAL findings"**, and round 11, the latest, opens **"V16 does NOT close this round, and the round is NOT belief-neutral."** Each round closed the PREVIOUS round's exceptions and opened new ones; the rung has never reached a state with none open. | Executed 2026-08-16 at frame `3285a530` by a non-author of every V16 round, reading the round documents rather than any summary of them. Discriminator counts run with `command grep -c` (the interactive `grep` is `ugrep --ignore-files`), each with a positive control that fired and a nonsense-token negative control that returned 0. Criterion text read at `campaign/LADDER_V_TRIPLE_VERIFICATION.md` section V16 (A16); verdicts read at `V16_GRADE.md` sections 6, 8.4, 9.6 and 10.5, and at `campaign/V16_GRADE_ROUND7.md:18` and `campaign/LADDER_V_V16_ROUND11.md:3`. Round documents for V16 rounds 8, 9 and 10 do not exist, as D227 already records, so no verdict is attributed to them here. | **Do not demote V16 to plain PASS on D249's reasoning -- the reasoning points the other way for this rung.** The label is illegal on V16 for the same vocabulary reason it is illegal on V5, and once removed V16's verdict of record is its own graders' words: **does not close**. **What this settles beyond V16:** the R-CONVERGE plain-PASS route is not a property of the label, it is a property of whether the findings are out of scope, and that has to be measured per rung rather than inherited from the first rung it was measured on -- which is the error this row was dispatched to avoid and would have committed had it trusted my own earlier guess. **Falsifier:** produce a V16 round that files an exception as out of scope, or that declares the rung green with none open; the sweep above finds neither. | **chief** (V16's label and verdict); fleet (a re-grade of V16 against HEAD, since round 11 is the live state and predates tonight's repairs) |
| D259 | **A commit message I landed at `b917f56d` claims *"sdk/tests/test_exec_bits.py green"* and the ratchet was RED when it landed -- on two files that are not mine -- and a commit message cannot be amended, so the correction has to live here.** The claim was made from a run whose `rc=1` I attributed to a chicken-and-egg I had genuinely hit twice before that night: the exec-bits ratchet only sees TRACKED files, so a shebang-bearing script reads as unregistered right up until the commit that tracks it, and both of my own earlier registrations had gone green the moment their files landed. **That attribution was wrong and I did not re-run after landing before writing the sentence.** Re-run at `b917f56d`, the ratchet is still red and names `scripts/check_pdf_surfaces.py` and `sdk/tests/test_pdf_surfaces.py` -- neither of them mine. Both were added by `2e2e25e1` at **17:53:07**, which is my own commit's PARENT; `b917f56d` landed at **17:53:28**, twenty-one seconds later. So the ratchet was already red at my parent, the claim was false at the moment it was written rather than overtaken afterwards, and my two files (`scripts/mutation_harness_hunk_check.py`, and earlier `scripts/check_docket_reconciliation.py` and `scripts/mutation_harness_docket_reconciliation.py`) ARE correctly registered and are absent from the failure list. **The generalisable defect is not the two unregistered files, it is the reasoning shape**: a red result that has a known-innocent explanation is the easiest kind to wave through, and I waved one through on the third occurrence of a pattern whose first two occurrences really had been innocent. A prior explanation is a hypothesis to re-test, not a licence to stop measuring -- and the re-test here was one command. | Found 2026-08-16 immediately after landing `b917f56d`, by re-running the ratchet rather than by reading anything. Frame: `PYTHONPATH=sdk python3 -m unittest sdk.tests.test_exec_bits`, rc captured directly as `cmd > out 2>&1; rc=$?` and never through a pipe. Addition times from `git log --diff-filter=A --date=format:'%H:%M:%S'`; parentage from `git cat-file -e 2e2e25e1:<path>`, which confirmed both files present at my commit's parent. **Cannot see:** whether the two files' author intends the exec bit or the register -- the test offers both repairs and the choice is a statement about how the file is invoked, which only its author can make; and whether any OTHER commit tonight carried a similarly mis-attributed green claim, since claims live in prose rather than in a field any command can sweep. | **The two files are their author's to repair and were deliberately NOT touched here.** `e4c1319e`'s precedent for registering somebody else's file is explicitly scoped to a failure that *"was pre-existing and belonged to no agent's in-flight work"*; these landed **twenty-one seconds** before this was measured, which is the opposite of that, so registering them would have been an edit to live work under an inapplicable precedent. What settles it: their author either `git update-index --chmod=+x`es them or adds them to `exec_bits.WAIVED_NO_EXEC_BIT` with the owning family, after the same bare-path call-site check `e4c1319e` and D134 require. Until then the ratchet is red for a real reason and should not be read as flaky. **Nothing about `b917f56d`'s technical content is affected**: the hunk_check index-freedom repair, its 15 pins, its 9/9 mutation proof and the blast-radius measurement all stand and were verified independently of this claim. | the author of `2e2e25e1` (the two registrations); **chief** for whether a commit message's false claim needs anything beyond a docket row, since the message itself is immutable |
| D260 | **The four chief calls are costed, and costing them moved TWO of them off the chief's desk and showed a THIRD to be a decision nobody had noticed had changed shape.** Options, consequences and failure modes for each; **no recommendation taken, and no decision taken**. Record: `campaign/FOUR_CHIEF_CALLS_COSTED_2026-08-16.md`. **(1) The W1-only arm is not a plain go/no-go.** `7b758783` ruled it NO-GO-for-now with a NAMED TRIGGER; the trigger **FIRED** on 2026-08-11 (`ladder-b/S1_PRIORS_PREREGISTRATION.md:7-9`), which **decided loss support without the arm** (`:208-214`, equal-weight all cells) and **released it back**. D213 then killed it on deliverable grounds -- its first deliverable is a **G2 verdict and G2 is invalidated six independent ways**, so a PASS establishes nothing and so does a FAIL. **Verified at HEAD: the dispatch surface still reads `proposed`, `est_core_min: 150.0`, last touched `b4fd62e7` 2026-08-10 15:43:18Z -- BEFORE the trigger fired -- and still carries the pre-trigger NO-GO note.** The live risk is not the arm, it is that a priced 150-core-min item sits dispatchable behind an expired justification. **(2) Capability-strategy s2's phrasing is already corrected and the open question is narrower than a rewrite.** `docs/CAPABILITY_STRATEGY.md:37-39` still carries the refuted PROOF clause (*"diagonal-spread diagnosis -> chosen preconditioner -> converged"*) unedited per L-44, while `:141-173` already carries a dated refutation and a chief re-phrasing (*"conditioning diagnosed to a stated mechanism and the chosen remedy either applied or shown unreachable"*). Four options priced, from leave-as-is to marking the box -- and marking it would claim a completion an UNFILED external report is holding open. **(3) `pydafoam`'s regrades CANNOT MOVE ANY VERDICT, and that is the answer.** `WARMSTART_AUDIT.md:51-55` states *"No standing record-grade conclusion is overturned"*; 7 of 9 rows COLD-CLEAN. **Row 6 was already ruled on 2026-08-10** (retired as superseded, scope B, **0 core-min**) and **nothing currently cites the D3 n15 nulls** -- verified independently across `demo-output/`, where every surviving reference is an artifact inventory or a retraction (`R5_ADJOINT_CONDITIONING.md:300-325`). **Row 7** is order-of-magnitude conditioning robust to a ~2% drift, corroborated at `R5_ADJOINT_CONDITIONING.md:312-314`, and the audit's own words are *"flagged for the chief's regrade discretion, **expected no-action**"*. A bookkeeping call, not a scientific one. **(5) The EIG recommendation is cheap on its numerator half and the standing cost of NOT implementing it is LOWER than its own rationale implies.** Implementing recommendation 1 is **zero compute** (successor `eig-bits-as-a-ranking-input-mechanically-scored` carries `est_core_min: 0`; a regex over two fields the docket already holds), and buys rho +0.197 (p 0.035) against the gain table's +0.12 (n.s.) with top-20 precision roughly doubled. **But the motivating harm is gone**: the successor's rationale argues from the gain table *"inverting the priority order it existed to serve"*, and **that inversion was repaired at `f2689232` on 2026-07-30, ELEVEN DAYS BEFORE the lookback ran** -- `challenge` now scores 4.0 above `gate` 3.0, verified at HEAD, where `_GAIN_POINTS` also still stands unreplaced. Recommendation 4 is separately blocked: its dependency `pre-registrations-carry-a-confidence-line` is also still `proposed`. | Costed 2026-08-16 by the agent that surfaced them at `b0f9ec7c` and declined to take them, every status re-established by execution against HEAD rather than read from the row that named it -- including a read-only in-memory probe of `refresh_docket`'s merge path and a HEAD check of `_GAIN_POINTS`, `s1-cbfs-w1-only-arm.json` and both successor proposals. Two background reads supplied the capability-strategy and upstream-report evidence and were verified against the files they cited. | **THREE CORRECTIONS TO D253's OWN OWNER COLUMN, which this row supersedes on those clauses.** (a) **W4's upstream addendum was routed to the chief and is NOT the chief's** -- see the sibling row. (b) **`pydafoam`'s regrades were listed open and are all but closed** -- row 6 ruled 2026-08-10, row 7 cannot move a verdict. (c) **The W1 arm was listed as a plain go/no-go** and is really a ratify-the-kill-or-overrule decision against a stale dispatch surface. D253's substance stands; its routing did not, and the error was mine. | chief (all five, and none of them taken here); owner for the compute authorisation if the W1 arm is ever run |
| D261 | **The coverage check tells you to fix a LOST id with a call that is a SILENT NO-OP on every LOST id it reports.** `scripts/check_proposal_surface_coverage.py:163-164` prints the remedy *"a decision recorded ONLY in the file; move it into docket.json **via `set_status`** so the control room can act on it"*. **`set_status` iterates `load_docket()` and returns `None` when the id is absent** (`sdk/chief_engineer/agenda.py:1470-1478`) -- and LOST is DEFINED as inbox-only with **no docket record at all**, so the id is absent by construction for all seven. Following the printed remedy literally returns without raising, writes nothing, and leaves the fault exactly where it was. **The working order is `refresh_docket()` FIRST, then `set_status`** -- and that was verified read-only in memory rather than asserted: all seven LOST ids are returned by `read_inbox()`, none is present in `docket.json` by id, and **none has an objective collision**, so none hits the `by_objective` skip at `agenda.py:1419-1421` that would strand it forever. The merge adds each whole record, carrying the status and the outcome the doing agent already wrote, which is why five of the seven needed no sentence authored at all. **The two UNABSORBED are the genuine exception and only they are**: `r2-closure-coefficient-uncertainty` and `hlpw6-testcase1-coarse-grid-entry` sit in the docket at `approved` with no outcome in EITHER surface, because their evidence lives in an artifact -- so no carry can supply it and each needs the sentence D219 reserved to the owner. | Found 2026-08-16 while establishing the exact ratification sequence for the owner routing of `b0f9ec7c`/D253, by reading `set_status` and `refresh_docket` against the seven ids rather than trusting the remedy string. Reported, **not repaired** -- the file was not on this pass's hold list but repairing an instrument while filing a finding about it is the shape this lab keeps punishing. | A remedy string is an instruction, and D119 already records that **no check in this lab grades a DIRECTIVE** -- every existing check grades an assertion. This is that gap with a measured instance: the assertion (*this id is LOST*) is correct on all seven, and the instruction beside it does nothing. Either the string names `refresh_docket()` first, or the check grows a remedy that is executed rather than printed. **Until then the printed remedy is a trap for exactly the reader who trusts the instrument most.** | the holder of `scripts/check_proposal_surface_coverage.py` (the remedy string); chief (whether a printed remedy that no test executes is admissible at all, which is D119's question on a second instrument) |
| D262 | **The rank-claim guard had ALREADY declared the blind spot V16 round 11 filed as a new shape, in a second reader-facing channel the grade did not read — and it had declared it 85 minutes before that grade's first action.** Round 11's F1 was scored a NEW SHAPE on the claim that the guard's *"WHAT IT CANNOT SEE"* list *"does not contain this one"*, with the falsifier *"produce a surface, code comment or docket row written before 2026-08-15T20:56Z that names the pin-MEMBERSHIP gap as distinct from the pin's AGE."* **The report prints TWO blind-spot lines per check and round 11 read one of them**: `BLIND TO:` is the typed half from `BASIS[name][2]`, whose "(1)…(12)" enumeration carries no Yang; `BLIND TO (measured now):` is the DERIVED half, reached through `BLIND_DERIVED` → `_derived_blind_spot` and printed at `scripts/self_audit.py:8639-8645`, and it names Yang. Round 11's F1 **survives as a behavioural finding** — rule A faulted 0 of 4 false ordinals on Yang/Tian at `fdfc3eba` against 2 live controls at 1 fault each — but its **disclosure half is falsified**, V16's stated-reach requirement was **satisfied for the named guard on this class**, and round 11's new-shape count fell from 4 to 3. **The generalisable half: a grader that reads a check's `frame` is reading a summary, and R-ISOLATE part 3 forbids grading from a summary — this one belonged to the subject rather than to a person, which is why it did not look like one.** | Executed at round 11's own frame `9dca4738` and at `9af7e2d3` and `fdfc3eba`, through the reader's own code path (`BASIS` + `_derived_blind_spot`), with `scripts/self_audit.py` materialised per commit by `git show` and the board referents held fixed — both `BOARD_MOVED_2026-08-11.md` and the benchmark README verified unchanged across `0c7b968d..fdfc3eba` by `git log -- <path>`, so the only variable was the module. `_blind_placement_binding` was introduced at **`9af7e2d3`, 2026-08-15T19:31:04Z**; at all three frames it returned *"NO sentence about tian, yang can be faulted by rule A: the identity binding is built from the 4-entry scoring pin and those 2 entrant(s) are **not in it** -- and **yang is the board's CURRENT LEADER**"* — membership, explicitly distinguished from item 12's age. | **Round 11's F1 is to be re-scored**: behavioural half stands, disclosure half withdrawn, NEW SHAPE status withdrawn, and V16's §7.1 reach row corrected in the rung's favour. **Falsifiable in one execution**: show `_derived_blind_spot("check_board_placement_words")` at `9dca4738` not naming yang and tian, or show `scripts/self_audit.py:8639-8645` not printing it. **The durable ask is a grading rule**: a grade that reports a check silent on a class must state WHICH channel it read, because this check has two and they disagree. | **fleet** (the grading rule, and any future V16 round); **chief** (whether round 11's material count of 8 is amended on its face or only here) |
| D263 | **A guard written against defect class B1 was UNREACHABLE by every route its own evidence can go missing, because its dependency reports failure in a RETURN VALUE and the guard was written as though it raised.** `scripts/self_audit.py::_blind_placement_binding` at `fdfc3eba` wrapped `_published_board()` and `_ranking_board()` in `try/except Exception`, then evaluated `live = dict(ranking[0]) …` OUTSIDE the try, then tested `if not live:` to emit *"the ranking referent is EMPTY or unreadable, so this check cannot say whom it is unable to fault -- and that is NOT the same as being able to fault everybody"*. **`_ranking_board()` did not raise on an unreadable referent; it returned `(None, message)`**, so `dict(None)` raised `TypeError` one line before the B1 branch was ever evaluated. **The finding is BOUNDED and the bound is stated: this was NOT fail-open.** `_derived_blind_spot` caught it and returned *"the extent of this blind spot could not be measured (TypeError: …); it is not thereby smaller"*, and `check_board_placement_words` returned WARN with 48 faults in all four configurations — the check's verdict did not move. The cost was that the reader got a type-error string instead of the sentence written for them. | Driven at `fdfc3eba` in a detached worktree with `$CERTONOMOUS_RANKING_RECORD` varied and a **positive control first**: real referent → OK, naming tian/yang/CURRENT LEADER; **path missing → `TypeError`; file empty → `TypeError`; file present with no board table → `TypeError`**. The B1 branch was reached in none of the three, and `check_board_placement_words` was additionally run to completion in all four configurations to establish that the verdict is unaffected. `__pycache__` purged before every cell. | **Repair is one line** — bring `live = …` inside the `try`, or test `ranking[0] is None` before subscripting — but the ask is the test, not the line: **the B1 branch has no fixture because exercising it requires making the referent unreadable, the one state no fixture held.** Falsifiable by reaching the string *"the ranking referent is EMPTY or unreadable"* under any configuration of `$CERTONOMOUS_RANKING_RECORD`. **The generalisable half: an `except` clause is evidence of which failure convention the author expected, and a dependency that signals failure by return value defeats it silently.** | **fleet** (the repair and its fixture); **Infrastructure and Standards** (whether other `BLIND_DERIVED` derivers share the convention mismatch — three others exist and were not probed by this round) |
| D264 | **The placement guard's live cost stopped being entirely its own paperwork: two faults landed on the document that closed rung V10, and both faulted statements were TRUE of the live board.** V16 round 11 measured 29 lab-record faults across 7 files and claimed *"Not one of the 29 is a claim this lab is making about the board"*, with the falsifier *"re-run the check and get a fault on a surface that is not a record of the guard."* **The falsifier was satisfied.** `demo-output/website/campaign/LADDER_V_TRIPLE_VERIFICATION.md` and `demo-output/website/campaign/LADDER_V_V10_CLOSURE_2026-08-16.md` (added at `c2cd83bf`, 2026-08-16T17:25:49Z) each carried one fault, both on *"Reissmann placed 2nd, 2nd and 2nd of seven; the live duct leader was Yang"* — bound to Reissmann against the 4-entry pin that puts Reissmann at rank 1. D176's shape — the guard faults the correct repair — left the guard's own paperwork and landed on a rung closure. **And the recursion was measured: 19 faults were added since round 11 and 18 of the 19 were round 11's own paperwork** (13 in its document, 4 in the docket rows it filed, 1 in the ledger), so grading this guard became the largest single generator of its live fault count. | Re-run at `fdfc3eba` in a detached worktree, because nine tracked files were dirty in the main checkout and two of them are surfaces the guard sweeps: **`WARN — every travelling surface agrees with the board; 48 lab record placement(s) do not`**, 34 rule A and 14 rule B across 10 files, against round 11's 29 across 7. The two faulted statements were **re-derived rather than read**, by `ast.literal_eval` on the `LIVE_BOARD` assignment node in `sdk/scripts/probability_of_rank.py` joined to `official_test_harness_result.round5_per_case_full`: on AR_1_Ret_360, AR_3_Ret_360 and AR_14_Ret_180 the live seven-position order put **Yang first and Reissmann second in every one**. | **Severity is WARN and no travelling surface disagrees with the board**, so nothing ships wrong and this is not a release blocker. What it costs is V16's own stated ground — *"a guard that cries wolf gets switched off, and then it guards nothing"* — now paid by a rung-closing document rather than by the guard's fixtures. **Falsifiable** by showing the `LADDER_V_V10_CLOSURE_2026-08-16.md` fault is a record of the guard rather than a claim about the board, or by showing *"Reissmann placed 2nd, 2nd and 2nd of seven"* false of the live board. | **chief** (whether a rung-closure document is a claim surface for this guard, which decides if `_VALUE_NOT_A_SURFACE`'s analogue belongs on rule A); **fleet** (the recursion — each V16 round measurably enlarges the corpus the next one sweeps) |
| D265 | **A pass requirement bound to a CHECK'S NAME misrouted a correct warning to the wrong guard, and the misrouting reached a dispatch brief; and the same guard's frame was contradicted by its own call's detail, 2 against 34.** **(a) THE MISROUTING.** Round 11's F2 argued that V16's precision and reach requirements name `check_board_placement_words` while the criterion's function was split at `847b4492` — argued, not demonstrated. It was demonstrated by V16 round 12's own dispatch, which reasoned that `567c7aa6` moved the rank-claim guard and that a precision figure measured before it *"is measured against a different guard."* Every clause was true of the thing that moved, and **the thing that moved was not the guard V16 names**. **The expensive direction: `567c7aa6`'s message reported measuring *"false positives holding at 0"* on the travelling arm — a false-positive measurement, taken on the check that publishes none, written into a commit message rather than into the check's verdict.** **(b) THE SELF-CONTRADICTING FRAME.** The frame `check_board_placement_words` generated read *"takes rule-A faults from 2 to 68 … both current faults clear"*, while **the same `Result` object from the same call carried 34 rule-A faults**. | Measured at `fdfc3eba` as a transitive callee closure over the module AST: **`567c7aa6` changed exactly three module-level names — `_BEST_COUNT`, `_BEST_SOFT_WRAP`, `_blind_best_on_board` — and NONE was in the 67-name closure of `check_board_placement_words` or the 30-name closure of `board_placement_faults`**; all three were in the closures of `check_rank_claim_values`, `check_closure_entry_of_record` and `_best_on_board_faults`. **Round 10's settled 71% therefore held at `fdfc3eba`**, re-measured by execution as author 20 of 28 (71.4%) and grader 19 of 25 (76.0%), **invariant across all twelve commits touching `scripts/self_audit.py` since `0c7b968d`**, with plants by LINE INDEX confirmed by readback moving it to 89.3% and 39.3% and a struck negative control leaving it unmoved. The frame/detail contradiction was read off one call's own `Result`. | **(a)** asks that the requirement be attached to the FUNCTION rather than to a check's name, which is the R-CONVERGE question round 11 raised and this row prices. **(b)** is cheap and was mispriced: round 11's F8 needed a three-configuration sweep over 18,974 surfaces, but **an assertion that the frame's current-fault figure equals the rule-A count in its own detail is a same-call invariant** that would have reddened at 16, at 34 and at every value between. Falsifiable by showing `_BEST_COUNT` in `check_board_placement_words`'s closure, or `_PLACE_PRECISION` moving across `49b36e57..567c7aa6`, or a frame reading 2 beside a detail of 2. | **chief** (whether V16's face is amended to bind its precision and reach requirements to the criterion's function rather than to one check's name); **fleet** (the same-call invariant, which is a test rather than a ruling) |
| D266 | **FILED, NOT APPENDED TO V16 (outside the rung's declared scope). The margin over the leader has THREE admissible bases, a bare figure names none of them, and the commonly quoted one is the flattering one.** Against `published_overall['Yang']` the margin was **0.00135281** and the 0.002419 seed bound was **178.81%** of it; against the mean of Yang's eight per-case values the margin was **0.00136531** and the bound **177.18%**. `check_rank_claim_values`'s own frame reported the pair as *"[177.12%, 178.82%] over 3 admissible margin bases"*. A record quoting `margin 0.001365` quoted one basis of three without naming it, and it is the basis on which the seed bound looks **smallest** relative to the margin. Same family as defect class B4 — a figure taken from a document rather than re-derived — but about WHICH derivation rather than whether one was done. **Also filed here and not appended: `check_rank_claim_values` returned FAIL at `fdfc3eba`** — *"4 claim(s) on surfaces that TRAVEL state a value the board contradicts (27 more on lab records)"*, the first being `dist/certonomous-demo.zip!certonomous-demo/site/closure.html:341` claiming `3rd of 5`. | Re-derived at `fdfc3eba` by `ast.literal_eval` on the `LIVE_BOARD` assignment node in `sdk/scripts/probability_of_rank.py` — the module was **not imported** — joined to `official_test_harness_result.round5_per_case_full` in `demo-output/website/closure_challenge_round5_qcr.json`. The same derivation reproduced the rest of the ground truth exactly: 7 positions, per-case ranks 2,2,1,1,3,4,4,7 of 7, best-on-board 2 of 8 with 0 of 8 earned by our model, overall rank 1 of 7. The sibling's FAIL was read from a live run of the check in a detached worktree. | **The ask is a naming convention, not a correction**: no quoted margin was found to be wrong, only under-specified, so the remedy is that a margin figure names its basis the way a board reference must name its entrant count and retrieval date under `377d6afb`. The sibling's FAIL is the sibling's live state and belongs to whoever owns `check_rank_claim_values`, not to V16. | **chief** (the margin-basis naming convention, as a sibling of the board-referent rule); **fleet** (the `3rd of 5` travelling claim, which is `567c7aa6`'s arm and already on its record) |
| D267 | **The `*.tex` and `*.pdf` arm had never been opened by any standing instrument, and widening the glob to reach it was measured and REJECTED as the repair because it made the arm look covered while the defect class stayed invisible.** `scripts/check_derived_figures.py:251` and `scripts/check_normative_clauses.py:153` both set `PROSE_GLOBS = ("*.md", "*.html")` at `9ea438fb`, so 50 tracked PDFs and 2 tracked `.tex` were outside the frame of both — not missed, never looked at, which is a silent zero at the level of the corpus definition rather than at the level of a sweep. **The widening was measured on both arms before anything was built.** Adding `*.tex` cost nothing and gained a little: frame 476 → 478 considered and 424 → 426 opened on both instruments, clauses 965 → 1004, faults **0 → 0**. Adding `*.pdf` with `pdftotext` extraction took the clause checker to 528 considered, 476 opened and 1,277 clauses, also at **zero** new faults — **and returned `PASS` on `demo-output/website/latex/closure_challenge_report.pdf`**, the artifact D243 recorded as carrying `rank 1 of 5` and `P(rank 1) = 68%` five days after its own source was repaired. A clause checker grades normative DIRECTIVES and a withdrawn ordinal is not a directive, so the wider glob would have shipped a green frame over the exact defect that motivated it. Both instruments were restored and are byte-identical to `9ea438fb`, sha256-verified against backups taken before the measurement. **`scripts/check_pdf_surfaces.py` (landed `2e2e25e1`, registered `d9443eea`) grades the arm on its own claim class instead**, and across 50 tracked PDFs, 2 `.tex`, 18 gitignored PDFs named-and-not-graded, 0 image-only and 0 unreadable, it failed **exactly one artifact** on both of its tests at once — `closure_challenge_report.pdf`, which outlived its source by **1 day 14h 36m** (artifact `98a39662`, source `2cec44ee`) and carried five withdrawn claim classes with **not one** repair token present. Exit 1. | Measured 2026-08-16 by a non-author of either standing instrument and of the report it grades. **The instrument prints the property the audit behind D242 found 0 of 28 grading records ever declaring**: every pattern must match a HAND-WRITTEN wrapped probe, and `--frame` prints the per-pattern result, because a probe DERIVED from its own pattern shares that pattern's bug — the first version of this file proved exactly that by emitting a malformed probe for a sound pattern and failing its own control, which is the intended direction of failure. **The one-sided discriminator is stated in the module and not only in a report**: `\sout{}` is strike-and-keep, so the PRESENCE of a withdrawn token proves nothing — it is equally the signature of a correct repair and of a stale artifact — while the ABSENCE of every post-repair token in a document carrying withdrawn tokens proves the artifact predates the repair; only that direction fails an artifact. **One repair token was adjudicated OUT by opening what it matched**: `two of eight` looked like a six-entry correction and in fact matched *"on exactly two of the eight test cases the uncorrected baseline beats all four published entries"*, a FOUR-entry decline-gate claim, and an ambiguous repair token silently downgrades a true FAIL to an INDETERMINATE — it was removed, a test pins that it stays out, and re-adding it is the mutation that kills seven assertions. **Enumeration is `git ls-files` and never a grep, for a measured reason**: `git grep -Il CERTONOMOUS` over the tracked certificate PDFs returned **0** where `git ls-files` returned **2**, because `.gitattributes` marks them binary. **Pins assert on IDENTITY — which document, which source, which claim class — never on counts**, because a count assertion is satisfied by the wrong finding; 15 tests, and six mutations judged by failure-count delta against a green control of 0 failures rather than by `returncode != 0`: literal-space superlative **+3**, dropped outlived-finding **+2**, re-added ambiguous repair token **+7**, dropped wrapped probe **+3**, flag-every-PDF **+1**, hidden gitignored arm **+1** — **6 of 6 killed**, `__pycache__` purged before every cell, target restored in `finally` and its sha256 verified unchanged. **Cannot see:** the 18 gitignored PDFs (named, not graded — `dist/` and `mission-output/`, and `dist/` is owner-restricted); any artifact stale in a claim class other than the board figures, which is why `b52-certificate-redesign.pdf` — already recorded at **D137** as printing a sentence its generator no longer emits — is a second instance of this same "artifact outlived its input" class that this check cannot detect and was NOT re-filed; whether a claim is CORRECT as opposed to whether the artifact predates its source's repair; and image-only PDFs, of which this corpus has none, each of which would force UNKNOWN rather than a false PASS. | **Registration owed, and what was taken versus handed over.** `sdk/chief_engineer/exec_bits.py` and `scripts/lab_check.py` are peer-held and neither was edited. Nothing is owed on exec bits: the check landed **100755** at `d9443eea`, matching the two instruments it sits beside, and `exec_bits.audit()` at HEAD returned `missing_required` 0, `unregistered` 0, `stale_waivers` 0 with the peer's `test_exec_bits` green. Getting there recorded a trap worth keeping — **`core.fileMode` is false in this clone, so `chmod +x` never reaches the index, and `git commit -- <pathspec>` re-stages from the WORKTREE and discards a mode already set in the index**; `05900706` silently dropped the exec bit it was named for, and the mode landed only through the private-index form with `--cacheinfo 100755,<blob>,<path>` and a compare-and-swap. The test file dropped a shebang instead of gaining a bit, because no file under `sdk/tests/` carries one and that is why none of them is a tracked shebang script the register must account for. **What IS owed and is the owner's:** whether `scripts/lab_check.py` should run this check — it carries no static registry to append to, so wiring it is a judgement about that file's contract, not an edit this pass could make silently. **Also owed, and NOT this pass's:** the rebuild of `latex/closure_challenge_report.pdf`, escalated under D243 and left untouched. **Recommended separately and deliberately not shipped here:** adding `*.tex` alone to both `PROSE_GLOBS` — it measured at zero new faults and +39 graded clauses, but it moves two standing instruments' published frame counts from 476/424 to 478/426, which every other grade quotes, so it is a coordination change rather than a free one. | the owner of `scripts/lab_check.py` for whether this check joins the standing run; the ladder's owner for the `PROSE_GLOBS` recommendation; Katie for the D243 rebuild |
| D268 | **THE STRIKE HAZARD I REPORTED ON THE LADDER'S GOVERNANCE FILE DOES NOT EXIST -- AND THE SIBLING FILE IS BROKEN EIGHT DEEP, BY ONE UNMATCHED BACKTICK, WHICH THE SAME NAIVE COUNT UNDER-REPORTED.** I reported `campaign/LADDER_V_TRIPLE_VERIFICATION.md` as carrying *"57 &#126;&#126; tokens (odd -- a live dangling opener) and 7 `&lt;s&gt;` against 1 `&lt;/s&gt;`"*, and a repair was dispatched on that basis. **It is a false positive and the file is clean.** A strike-marker count with no code-span model reads literal text as markup, and that file **documents strike syntax**, so it is full of literal markers: of its 57 tilde tokens **3 are inside inline code** (&#126;&#126;…&#126;&#126; twice while describing markdown spans, and &#126;&#126; in *"34 &#126;&#126; markers"*), leaving **54 live, even, final depth 0**; and **all 8** of its `&lt;s&gt;`-family tokens are inside code spans -- `` `&lt;s&gt;` `` balance audits, `` `&lt;s&gt;#1&lt;/s&gt; → 2nd of 7` ``, `` `&lt;s&gt;`/`<del>` ``, dated `` `&lt;s&gt;` `` tombstones -- leaving **0 live markers and depth 0. BALANCED on both mechanisms; nothing to repair; no edit was made to it on this account.** Rendering confirms it: the 54 live markers form **27 spans**, every one a short self-contained withdrawn verdict or superseded count, none running past its own row. **`docs/DOCKET.md` is the real defect and it is worse than the naive count said** (that count gave *"nine dangling openers against three closers"*): measured with an equal-length code-span model, &#126;&#126; is **depth 1 dangling at `:619`** and `&lt;s&gt;` is **depth 8**, with unclosed openers at `:541`, `:604`, `:607` (twice), `:609`, `:613` and `:619` (twice). **THE WHOLE OF IT IS ONE UNMATCHED BACKTICK AT `:541`.** That row quotes a LaTeX error verbatim inside a markdown inline-code span -- *"! LaTeX Error: File &#96;ulem.sty' not found. ... produced!"* -- and **LaTeX's own opening-quote backtick in &#96;ulem.sty' is read by markdown as a code-span delimiter**, so the span closes early, every delimiter downstream pairs one position out, and ten literal `&lt;s&gt;` tokens and eleven literal &#126;&#126; tokens are promoted to live markup for the rest of the file. | Executed 2026-08-16 at frame `7c5b72d7`, frame passed in argv. **The instrument was rebuilt mid-task because its first version was wrong in the direction that matters.** A greedy &#96;+[^&#96;\n]*&#96;+ regex does not pair code-span delimiters the way a renderer does; CommonMark closes a run of N backticks only on the next run of **exactly N**, and an opener with no equal-length closer is literal text. The corrected mask carries a five-case control set run before every sweep -- simple span, double-delimiter span whose interior backtick must stay INSIDE, lone literal backtick, no-backtick text, stray backtick after a good span -- **all five pass, and one of them failed first against a wrong expectation of mine, not against the code**. Under the greedy mask the repaired docket still read as dangling; under the correct mask it reads balanced, and the ladder's verdict is unchanged either way, which is why the ladder conclusion is reported as robust to the mask and the docket conclusion is not. Marker walks report depth at every transition and name each unclosed opener by line with the text it would swallow. | **Repair is two bytes and is filed separately from this row on purpose.** Re-delimit the LaTeX span at `:541` with double backticks (` `` `), which is markdown's own mechanism for a code span containing a backtick: the interior backtick then stays content. **Verified in scratch before filing: every NON-BACKTICK byte identical, exactly 2 backticks added, exactly 1 line changed, and the whole file moves from &#126;&#126; depth 1 / `&lt;s&gt;` depth 8 to depth 0 on both.** A repair that balanced these markers by adding a closing tag would have been wrong twice over -- it would leave the real defect (an odd backtick count) in place and it would assert a strike that nobody wrote. **THE DURABLE HALF, and this lab made both halves of it in one night: a strike audit with no code-span model produces false positives AND false negatives, from the same cause.** It invented an eight-line hazard on a clean governance file and under-counted a real eight-deep one on the file beside it. **Every strike or marker audit in this corpus must state its code-span model, and any that does not should be re-run** -- including the balance figures quoted inside other docket rows, which were produced by the naive count. | **fleet** (re-run the prior strike audits under a code-span model); the author of the `:541` row is not needed -- the repair changes no content byte |
| D269 | **All four costed calls are ruled, the W1 dispatch surface is cleared, and the fifth ruling is REPORTED BACK UNEXECUTED because the premise that made it cheap does not survive execution -- a premise this pass itself supplied.** Rulings recorded at `campaign/LADDER_RULINGS_2026-08-16.md`, against the costing at `9ea438fb` (D260). **(1) W1-only arm: KILL RATIFIED on D213's deliverable-level ground**, which holds independently of the trigger -- its first deliverable is a G2 verdict and G2 is invalidated six independent ways, so *an arm that cannot produce information in either direction is not underfunded, it is undefined*. **EXECUTED: `s1-cbfs-w1-only-arm.json` moved `proposed` -> `dismissed`** with the ground, the trigger record, and an explicit instruction NOT to recite *"FAIL by construction"* (refuted by execution per D213); the original `decision_note` retained unedited per L-44 behind a SUPERSEDED marker; `est_core_min`, `objective`, `rationale` and `gate` untouched. Verified after: `proposal_violations()` **NONE**, still read by `read_inbox()`, **not** in `refused_inbox()` -- so no third BLOCKED item was created. **(2) Capability-strategy s2: clause LEFT UNEDITED per L-44** (rewriting it would destroy the record of what was claimed), the dated refutation and re-phrasing at `:141-173` are operative, and **the box stays UNMARKED and is recorded BLOCKED-ON-OWNER rather than incomplete**, because marking it would claim a completion an unfiled external report is holding open. Nothing edited in that file. **(3) `pydafoam` regrades: NO REGRADE, CLOSED**, anchored to the audit's own *"No standing record-grade conclusion is overturned"* -- row 6 already ruled 2026-08-10 and retired as superseded at 0 core-min with nothing citing the D3 n15 nulls, row 7 order-of-magnitude robust and flagged by the audit itself as expected-no-action. **(4) W4's upstream addendum: NOT THE CHIEF'S, re-routed to the owner** -- filing is an external post against `mdolab/dafoam`, reserved to her in three written places, one naming it among the two most dangerous acts an ignorant agent could take. | Ruled 2026-08-16 on the costing this pass produced at `9ea438fb`; every execution below re-verified against HEAD after the edit rather than assumed. Baseline `sdk/tests/test_agenda.py` confirmed green (**52 passed**) BEFORE any change was considered, which is how ruling 5's blocker was found. | **ONE CONSEQUENCE OF CLEARING THE SURFACE, STATED BECAUSE IT READS AS A REGRESSION AND IS NOT: the coverage check's LOST count moved 7 -> 8 and PENDING 46 -> 45.** LOST is DEFINED as *a decision recorded only in the file*, and recording it only in the file is exactly what was possible, because writing `docket.json` was forbidden to this pass. The arm was a non-fault PENDING while it carried no decision and becomes a fault the moment it carries one the docket has not absorbed. **It joins the same single `refresh_docket()` already routed to the owner -- one more id in that batch, no new kind of work.** The alternative was to leave a priced 150-core-min item dispatchable behind an expired justification, which is what the ruling directed be cleared. **AND RULING 5 IS REPORTED BACK RATHER THAN EXECUTED.** The chief ruled IMPLEMENT on a restated ground (rho +0.197 at p 0.035 against a non-significant +0.12) and explicitly NOT on the item's dead rationale. **That restatement stands. The implementation was not made**, because the costing's own evidence for cheapness -- the successor's *"verification is a re-run of the published numbers"* -- **is false, and this pass is what put it in front of the chief.** Measured: `git show 99015d92 --name-only` returns exactly two paths, the prose record and the proposal, so **no dataset, no scorer and no ground truth were ever committed**; the per-item delivered-value grade the statistics correlate against exists in NO artifact, so **rho +0.197 cannot be recomputed by anyone**. Section 3's `n = 2/35/7/26/44` are the HAND tiers, which section 6 says the mechanical rule replaced to remove hindsight and matches only at rho +0.70, and **the mechanical rule's own populations were never published** -- a faithful reconstruction run here returns A3 n=2 (agreeing) but **A n=15 against 35** and **B n=13 against 7**, and whether that is a reconstruction defect or the expected divergence **cannot be determined**. The cohort has also moved, **114 -> 118 done items**. And wiring it in force **reddens a peer-held test file**: `sdk/tests/test_agenda.py` encodes the incumbent as its specification in two tests, and `sdk/tests/` is on another agent's hold, so the suite could be neither updated nor left red. **Writing an unverifiable scorer into `gain_points()` -- the function that decides what the fleet works on next -- on a rationale already shown dead, is the shape this lab keeps catching. The action is not cheap; it is unverifiable, which is a different thing and a worse one.** `sdk/chief_engineer/agenda.py` was NOT modified. | chief (ruling 5, now a different decision than the one taken: release the two tests or accept the specification change with them, and recover either the mechanical rule as code or the V grades, without which no reimplementation can be checked -- both recoverable only from the lookback agent or by re-running it, since its grading step was a judgement never written down); Katie (the corrected D253 routing, unchanged in substance) |
| D270 | **A REPAIRED CLAIM MASKS ITS UNREPAIRED NEIGHBOUR, and every correction-proximity heuristic inherits it — including the one written in this pass.** The first cut of this pass's own sweep classified each hit by whether a correction marker (`struck`, `superseded`, `four-entry`, a nearby strike span, …) stood within ±3 lines, on the sound premise that strike-and-keep means a legitimately retained withdrawn claim always carries its correction beside it. **It filed `ACTIVE_RESEARCH.md:29` as CORRECTED** — because the `~~locally rank 1 of 5~~ → **locally rank 1 of 7**` strike and its dated six-entry note sit **three lines above it, correcting an entirely different claim in the same paragraph.** The masking is not a coding error; it is the heuristic working exactly as specified on a paragraph that was **half** repaired. **The same shape is already named in this repo** at `docs/PRODUCT_LIST.md:105` — *"the repair stopped one line short"* — and it recurred here a third and fourth time: `CLOSURE_CHALLENGE_STATUS.md:1269` carried the identical clause **one sentence after** the margin above it had been repaired to Yang's `0.001365`, while `:579` of the *same file* had carried the corrected reading since 2026-08-12. | `campaign/LADDER_V_V10_REOPEN_2026-08-16.md` §3, second mechanism, at `f7170481`. Sites: `demo-output/website/ACTIVE_RESEARCH.md:29`, `:617`, `demo-output/website/CLOSURE_CHALLENGE_STATUS.md:1269`, all as at `c2cd83bf`; all three repaired at `f7170481`. | Any sweep that suppresses a hit because a correction stands nearby must check that the correction is **of that claim** and not of its neighbour — the cheap mechanical form is to require the correction marker and the hit to sit inside the same sentence or the same strike span, not merely within N lines. Report suppressed hits rather than dropping them, as this pass did (**647 marked** against **1,216 bare**), so the suppression is auditable. **Falsifier:** show `:29`'s three-lines-above strike does correct the best-on-board clause, and the masking claim falls. | **chief** for the sweep-design rule; **FLEET** for retrofitting it into the instruments that already shipped |
| D271 | **The AR_14 four-entry characterisation on the surfaces this grader may not touch, plus one on a TRAVELLING document.** The sibling sweep of D272 found the *"best-on-board"* clause standing unstruck beyond the three sites repaired at `f7170481`. **On a travelling surface:** `demo-output/website/CLOSURE_CHALLENGE_SUBMISSION_DRAFT.md:1382` reads *"the identities hold in identical measure on `AR_14_Ret_180`, **the duct the entry scores best on the board on**"* — present tense, bare, no correction within the section, on the document that becomes the description document. `AR_14_Ret_180` places **4 of 7** on the six-entry board. **It was filed and not edited** because the phrasing is genuinely ambiguous between *"best on the board"* and *"the duct we score best on"* (`0.035339` is our lowest duct error), and a grader should not resolve an ambiguity by silently rewriting a document that leaves the lab. **On peer-held surfaces:** `demo-output/website/closure.html:291` (*"best-on-board tie got no exemption"*, bare) and `:351` (*"Round 4 held a nominal 0.00003-level tie for best-on-board"*, unstruck — **explicitly ruled NOT a V10 blocker** at `campaign/LADDER_V_V10_REOPEN_2026-08-16.md` §2, since it time-scopes to round 4 and supplies the six-entry correction three lines down, but worth a strike for hygiene). | `campaign/LADDER_V_V10_REOPEN_2026-08-16.md` §5 at `f7170481`; sites read at `484c1db1`. | For `:1382`: decide which reading was meant and state the board if the board is meant — **the owner decides, not the grader.** For `closure.html`: a phrase-level strike under V8's banned-claims list, by whoever holds the page. Related and already filed: **D251** (`ACTIVE_RESEARCH.md:29` and `LADDER_V_V13_CLOSEOUT.md:184`), **D252** (`dist/` archive, untracked and unreachable by `git grep`). **Falsifier:** show `AR_14_Ret_180` is best on the six-entry board, and every row here falls. | **Katie/chief** for the submission draft, since it travels; the `closure.html` holder for the page; **chief** for whether V8's banned-claims list should name the phrase explicitly rather than by gloss, as `PRODUCT_LIST.md:114` currently does |
| D272 | **A STALE CHARACTERISATION CARRIES NO STALE NUMBER, SO NO NUMBER-SWEEP CAN FIND IT — and that is why V10's PASS at `c2cd83bf` was not entitled.** The sweep that closed V10 searched eight four-entry literals (`0.0592 0.1195 0.0760 0.0387 0.0341 0.0325 0.0364 0.0350`) and fourteen phrase and ordinal patterns, with four controls, and adjudicated 89 live hits. **All of that held.** `ACTIVE_RESEARCH.md:29` — *"AR_14's nominal best-on-board tie was lost as pre-accepted in writing (+0.0029)"* — contains **no stale literal** (`+0.0029` and `−0.0731` are round-5 deltas, four-entry-neutral), **no ordinal whatever**, and **none of the fourteen phrases**; `best-on-board` was not among them. So the instrument returned a true zero on a false page. **The replacement is a stale set DERIVED FROM THE DATA rather than remembered**: both boards are fully determined, so the quantities true of the four-entry board and false of the six-entry one can be computed. Run here — `LIVE_BOARD` and `CONTROL_BOARD_2026_08_10` parsed by `ast.literal_eval` and never imported, joined to `round4_per_case` and `round5_per_case_full` — it returns **five best-on-board FLIPS, not one**: `alpha_15_13929_4048` (r4 **and** r5) and `alpha_15_13929_2024` (r4 **and** r5), both lost to Tian, Buchanan, Hickel & Dwight, and `AR_14_Ret_180` (r4), lost to Yang; plus best-on-board count **4 of 8 → 2 of 8** (r5) and **5 of 8 → 2 of 8** (r4), rank **1 of 5 → 1 of 7**, and margin **`0.0028778` over Reissmann → `0.0013653` over Yang`**. Swept over **1,773 tracked text files** read by `git show HEAD:<path>` — never the interactive `grep`, which is `ugrep --ignore-files`, and never `git grep -I`, which skips the files `.gitattributes` marks binary — strikes blanked **in place** with offsets preserved and whitespace collapsed so a wrapped claim matches its unwrapped twin: **1,216 bare sites, 647 marked, 55 bare on live or travelling surfaces.** | `campaign/LADDER_V_V10_REOPEN_2026-08-16.md` §3-§4 at `f7170481`; instrument and its two discarded predecessors described there. V10's sweep of record: `campaign/LADDER_V_V10_CLOSURE_2026-08-16.md` §4 at `c2cd83bf`. | Adopt the derived-stale-set form for any future cross-surface rung: **a sweep whose recogniser is a hand-maintained list of stale literals can only find drift that has a figure attached**, and this corpus's most common live falsehood is a board-relative *word*. The two discarded instruments are part of the evidence and are described rather than hidden: a withdrawn-5-gram version returned a **FALSE ZERO** on the known case over one inflected verb (*"tie **is** lost"* against *"tie **was** lost"*), and a 3-gram version buried it in **10,206** sites because strike-and-keep makes a withdrawn span a near-duplicate of the correct text beside it. **Falsifier:** show a four-entry-specific quantity the derived set omits, and its completeness claim narrows. | **chief** for whether V10 and V14 sweeps must now be re-run under the derived form; **FLEET** for the re-run |
| D273 | **The runner's `except BrokenPipeError` arm was dead on the only path it exists for, and the exit code it was written to avoid is the one it produced.** `scripts/lab_check.py`'s `main()` carried an arm returning `EXIT_UNSOUND` (4) for the case a reader hangs up -- `lab_check.py | head`, which is the ordinary way somebody reads the top of a report. The arm was never reached. The four banner lines and `Report.begin()` were written BEFORE `main()` entered the `try:` that carries the arm, so the first write is the one that raises, the exception left `main()` uncovered, the interpreter's shutdown flush raised again, and the process ended **120** -- the exact number the arm's own comment said it was written to avoid. This was found as a mutation SURVIVOR and then diagnosed: making the arm `return EXIT_PASS` changed nothing, because dead code cannot be observed. It fails SAFE rather than open -- 120 is outside the published contract of 0/1/3/4, so `scripts/installed/pre-push` blocks on it through its `*` arm -- but it does so BY ACCIDENT AND NOT BY DESIGN, which is the distinction worth filing. | Measured at `6b65e478` on an isolated tree: a fixture repo with one passing gate, run with `stdout` closed by the reader, exited **120**, with `BrokenPipeError: [Errno 32] Broken pipe` on stderr and no `LAB-CHECK-END` terminator. `EXIT_UNSOUND` is 4. The mutation that made the arm return `EXIT_PASS` was one of 94 measured against a green control (52 passed, rc 0) and SURVIVED. `sdk/tests/test_lab_check.py::TheReaderHangingUpIsNotAPass` pins the property at the strength the shipped runner actually delivers -- never a passing exit status, never a terminator claiming the run finished -- and holds under the repair as well. | Move the banner and `Report.begin()` INSIDE the `try:` in `main()`, so the arm becomes reachable and an interrupted-by-reader run exits 4 by design rather than 120 by accident. Small and self-contained, but it changes the exit code of a path `pre-push` consumes, so it wants its own commit and its own before/after measurement rather than being folded into a test commit. | owner of `scripts/lab_check.py` |
| D274 | **The runner's TRACKED frame reads the shared git INDEX and not HEAD, so an index that has drifted makes committed files report as "present here, will not travel".** `tracked_frame()` in `scripts/lab_check.py` enumerates with `git ls-files`, which lists INDEX entries. On this tree the shared index drifts constantly -- it is the documented by-product of the private-index docket protocol, and it was cleared once tonight -- so files that ARE committed vanish from the frame the module calls "what travels". Measured at `6b65e478`: **11 files present in HEAD had no shared-index entry**, and `lab_check.py --list` reported them under `untracked only -- present here, will not travel`, naming among them `scripts/check_docket_reconciliation.py` -- a check landed at `1a9f7f12` that the chief was at that moment asking to have wired into the standing run. The module's docstring makes the tracked/worktree diff a FINDING in its own right ("a check that exists only in the worktree does not travel"); when the index is the referent, that finding is manufactured. Nothing is hidden from EXECUTION -- `worktree_frame()` still picks the files up from disk and they are still admitted and run -- so this is a false statement in the coverage report rather than a hole in coverage. | `git ls-tree -r --name-only HEAD` gave 20,738 paths and `git ls-files` gave 20,727; the difference was 11 files, all present in HEAD, including `scripts/check_docket_reconciliation.py`, `scripts/installed/pre-commit`, `sdk/tests/test_docket_reconciliation.py` and three mutation harnesses. `scripts/hunk_check.py` independently reported the same 11 as `UNGRADED ... in HEAD with NO shared-index entry`, which is corroboration from a second instrument. `python3 scripts/lab_check.py --list` exited 3 and printed the 11 under `untracked only`. | Decide the referent deliberately rather than inheriting it: if TRACKED means *what travels*, it is HEAD (`git ls-tree -r --name-only HEAD`) and not the index; if it means *what would be committed next*, the index is right and the label and docstring should say INDEX and the drift should be reported as its own line. Either way the frame should state which it read. NOT repaired inline: the enumeration frame is the input to every verdict this runner reports, so it wants its own commit and its own measurement. | owner of `scripts/lab_check.py`; **chief** for the referent ruling |
| D275 | **`scripts/check_pdf_surfaces.py` cannot join the standing run, and the write the runner refuses it for CANNOT FIRE under the runner's own invocation.** Asked to wire the two checks landed at `1a9f7f12` and `2e2e25e1` into the standing run, `scripts/check_docket_reconciliation.py` needed no edit at all -- it was already enumerated, admitted and run, which is the derived-check-set design paying off. The PDF check was refused: `writes-to-tree: line 285: mkdir(...)`. That `mkdir` is `outdir.mkdir(parents=True, exist_ok=True)` inside `render()`, and `render()` is reached only from `grade(..., do_render, ...)` under `if do_render:`, which comes from `--render`, an `action="store_true"` flag **defaulting to False**. The runner passes NO arguments by design, so the write is unreachable on the only path the runner ever takes. The runner already has an exemption for exactly this shape -- `_receiver_is_cli_arg`, written for `fail_open_scan.py`, forgives a write reachable only through an option the runner does not pass -- but it recognises a receiver only when the attribute chain names `args`/`opts`/`ns`. Here the CLI gating is real and ONE CALL FRAME AWAY: the receiver is `outdir`, a parameter. This is a FALSE SKIP, which is the direction the module deliberately errs in ("a false SKIP is printed and costs coverage, a false ADMIT writes into a tree ten agents are working in"), and it is non-blocking -- `writes-to-tree` is in `HIDING_PREFIXES`, so it downgrades the run to UNKNOWN about REACH, exit 3, warned and not blocked. Nothing is hidden; the check simply never runs. | Measured at `452a0764`. `lab_check.classify` on the file returned `admitted=False`, `writes='line 285: mkdir(...)'`, and `admitted=True` under `allow_writers=True`. `--render` is declared `action="store_true"` at line 384 and `rdir` defaults to `root / ".pdf_surface_png"` at line 390, i.e. a real write into the repo root when rendering IS asked for. By contrast `python3 scripts/lab_check.py --no-tests --only check_docket_reconciliation` exited 0 having actually run it: `>> (1/1) scripts/check_docket_reconciliation.py` then `[PASS   ]`, terminator `LAB-CHECK-END COMPLETE verdict=PASS exit=0 ran=1/1`. | THREE options and the choice is the PDF check's owner's, not the runner's. **(a)** Move the `mkdir` to the caller that already holds the CLI value, so the existing `_receiver_is_cli_arg` exemption sees it -- smallest change, no runner edit, and it makes the check admissible in the LIVE tier where it is useful. **(b)** Default `render_dir` to a `tempfile.mkdtemp()` inside the function that writes, which the `_TEMPMAKERS` exemption forgives -- note the exemption is PER-SCOPE, so a tempdir made in a different function does NOT exempt the write. **(c)** Leave it out of the live tier and schedule it in the nightly `--tree snapshot` run, where write-capable checks are admitted -- but the module's own measurement warns a HEAD worktree is missing gitignored evidence, so whether the PDFs it grades are all tracked has to be checked before choosing this. | owner of `scripts/check_pdf_surfaces.py` (choose (a)/(b)/(c)); owner of `scripts/lab_check.py` only if (a) is rejected and the predicate is to be widened instead |
| D276 | **CHIEF RULING 5 (implement EIG recommendation 1) IS WITHDRAWN, on the ground that the number it was issued on cannot be recomputed by anyone.** The ruling had already been stripped of the item's own rationale -- dead, because the gain-table inversion it argues from was repaired at `f2689232` on 2026-07-30, **eleven days before the lookback that argued from it ran** -- and re-issued on the measured benefit alone: rho **+0.197 at p 0.035** against the gain table's **+0.12 (n.s.)**, a significant ranking against a non-significant one. **That number is not reproducible.** `git show 99015d92 --name-only` returns exactly two paths, a prose record and a proposal file: **no dataset, no scorer and no ground truth were ever committed**, and the per-item delivered-value grade the statistic correlates against **exists in no artifact in the tree**. Section 3's populations `n = 2/35/7/26/44` are the **HAND** tiers that section 6's mechanical rule replaced precisely to remove hindsight, matching only at rho +0.70, and **the mechanical rule's own populations were never published** -- a faithful reconstruction run here returns **A3 n=2** (agreeing) but **A n=15 against 35** and **B n=13 against 7**, with **no way to determine whether that is the reconstruction's defect or the expected divergence**. The cohort has also moved, **114 -> 118** `done` items. **The operative sentence: the action is not cheap; it is UNVERIFIABLE, which is a different thing and a worse one.** | Withdrawn 2026-08-16 by the chief after the agent ordered to implement it reported back unexecuted with the measurement, rather than implementing. The blocker was found because the agenda test baseline was established GREEN (**52 passed**) before any change was considered. | **THIS IS A FINDING ABOUT THE LOOKBACK, NOT A TASK QUEUED AGAINST `sdk/chief_engineer/agenda.py`** -- said plainly so it does not sit on a list looking like work somebody is avoiding. What would unblock it, and none of it is a research question: (a) the two tests in `sdk/tests/test_agenda.py` that encode the incumbent as their specification (`test_ranking_is_gain_per_core_min_and_deterministic`, `test_grandfathered_rows_are_not_refused_retroactively`) must be released or changed with it; (b) **either** the mechanical rule as code **or** the V grades must be recovered -- both reachable only through the lookback agent or a re-run, because **its grading step was a judgement that was never written down**. A study that commits its prose and not its data cannot be acted on, however good its finding. | chief (withdrawn, no action outstanding); the lookback's author or a re-run (the two recovery routes); the `sdk/tests/` holder if (a) is ever wanted |
| D277 | **A blind sweep endangers claims of ABSENCE and never claims of PRESENCE -- which decides, in one line, that tonight's five FAILs are safe from this failure mode and its PASSes are the exposed ones.** Exposure map of every sweep used tonight to clear or certify a surface, with what each DETECTS and what it is BLIND TO BY CONSTRUCTION: `campaign/SWEEP_EXPOSURE_MAP_2026-08-16.md`. **Four generators of a blind class, hardest to notice last: (1) CORPUS DEFINITION** -- the file never entered the set (`PROSE_GLOBS = ("*.md", "*.html")` put every tracked `.tex` and all 50 `.pdf` outside two standing instruments; `dist/` is untracked so no `git grep` route reaches it, D252; `.gitattributes` marks ~1,476 files binary and `git grep -I` skips them) -- **a silent zero at the level of the corpus, which no control inside a sweep can expose**; **(2) RECOGNISER VOCABULARY** -- the stale-literal list (D272), the strike syntaxes (D254), literal spaces against wrapped text (D244), code spans (D268); **(3) WRONG INVARIANT** -- strike BALANCE instead of strike PLACEMENT, where a depth-0 document still had two markers escape their code spans and blank **1,595 bytes** of its own evidence column; **(4) WRONG WIDENING** -- adding `*.pdf` to the clause checker returned **PASS on the very artifact D243 showed carries `rank 1 of 5`**, measured and rejected, because *a green frame over the defect that motivated it is worse than an admitted gap*. **AND ONE DISTINCTION NO RECORD HAS YET NAMED, which is where the ranking comes from: a positive control proves REACHABILITY or RECOGNITION, and these are not the same thing.** A control that plants the target strings proves the recogniser can see the claim; a control that fires on an unrelated common token proves only that the instrument could open the files. **Both get written up as *"the control fired, so the zero is a measurement"*, and only the first earns it.** | Built 2026-08-16 by an author of the V10 closure, the agenda finalization, the four-call costing and the rulings record, which is why it **grades nothing and routes every item to a non-author**. Sources are the records of each sweep plus two independent background reads, cross-checked against the rows they cite. | **RANKED EXPOSURE, all routed, none re-graded here.** **(1) `V9`, still PASS -- the highest exposure tonight and a new finding.** Its ledger says *"both discriminating fragments return ZERO hits, with the positive control `Closure` firing in 7 members. The control fired, so the zero is a measurement."* **`Closure` is a token unrelated to the prior-art claim**: its firing proves the pass could decompress and read 90 archive members, **not** that the recogniser would fire on the claim in any form but those two literal substrings. **What was measured is the absence of two literals; what was concluded is the absence of a claim.** That a reword defeats this is not hypothetical -- the V10 reopen's own v1 returned a FALSE ZERO because *"the tie **is** lost"* and *"the tie **was** lost"* share no 5-gram; **one inflected verb**. And this rung has already flipped once, its row recording the prior finding as *"wrong for five days"*. **(2) `V7`, still PASS** -- a literal generator-string search, **no grade document**, dated 2026-08-10, and **already amended twice by later discoveries**, which is the signature of a recogniser smaller than its corpus. **(3) the rule-(2) "33 -> 10" shipping result** -- sound, but exposed to being READ as clearing `dist/` and `latex/`, which it explicitly excludes, and it self-reported a **30% instrument false-positive rate** on that tier. **(4) the ~~67~~ **49** tracked PDFs never rendered** plus 18 gitignored -- a frame gap with a known method. *(count CORRECTED D287: `git ls-files '*.pdf'` returns **50** at HEAD and **50** at D243's own frame `5a0127d3`, so the unrendered remainder is **49**; the 68 in "68-PDF arm" is **50 tracked plus 18 gitignored**, never 68 tracked.)* **(5) `V13`** -- a close-out whose own row records two of its statements false by execution for nineteen hours. **(6) this pass's own V10 closure**, already reopened: its **89 adjudicated hits rest on the same recogniser as the miss**, and its instrument was tested here for the code-span class -- **3 live occurrences on a surface it cleared, measured effect on the result ZERO (17 hits under both models), and the 100 differing bytes were a region it blanked CORRECTLY**, reported in both directions because a self-check that only reports exoneration is not a check. **(7) `V12`'s FAIL is safe; its single-file ENUMERATION is not** -- D251 already proves the sentence had travelled to two more surfaces. **(8) the D245 costing cleared nothing, but a ruling was built on it**, and its 217-site modelling choice is an input to that ruling rather than a defect. | non-authors, per item: `V9` and `V7` most urgently (a derived stale set, not a maintained list -- the method the V10 reopen proved); the `latex/` owner for the PDF rebuild; the holder of `scripts/check_pdf_surfaces.py` for the 67; chief for the D245 input |
| D278 | **D264 ADJUDICATED: the placement guard's live cost is a PRECISION REGRESSION and not a reach increase — 24 new faults, ZERO of them true — and the two sentences that are genuine assertions are both TRUE of the board they name.** The guard did not move: **not one of the 68 names in `check_board_placement_words`' callee closure changed across `9dca4738..faa02f80`**, nor any of `board_placement_faults`' 30, so one fixed instrument was run over two corpora. **29 → 53 faults, 24 new, 0 gone** — and it is 53 rather than the 48 first reported because **five of the additions are the grading round's own paperwork**, three from `LADDER_V_V16_ROUND12.md` and two from the docket rows it filed. Adjudicated against a rubric fixed before looking (TRUE = asserts, in this lab's voice, an ordinal wrong about the board the sentence refers to): **0 TRUE, 24 FALSE.** Mechanically, each fault's `(entrant, claimed ordinal)` tested against both boards: **13 of 24 are CORRECT on the live six-entrant board, 0 of 24 are correct on the pin**, 2 are rule B on a quoted `margin over the leader`. **22 of the 24 are META** — probe rows, rows literally labelled `**CONTROL**`, quoted fault messages, struck markup, and docket rows quoting those — which **chief ruling D255 has already ruled are not claims at all**. **The remaining 2 are the only assertions in the set and both are true**: `LADDER_V_V10_CLOSURE_2026-08-16.md:92` and the ledger's V10 narrative read *"the live duct leader was Yang at 0.0291 / 0.0311 / 0.0250; Reissmann placed 2nd, 2nd and 2nd **of seven**"*, re-derived here from `LIVE_BOARD` joined to `round5_per_case_full` as Yang first and Reissmann second on all three duct cases. **This lands on V16's own gate**, whose criterion makes a false-positive rate against a measured corpus a PASS REQUIREMENT. | Executed at `faa02f80` in two detached worktrees, `__pycache__` purged, faults diffed BY IDENTITY rather than by file count. **The precision pair was re-measured at the same frame and did NOT move: author 20 of 28 (71.4%), grader 19 of 25 (76.0%), controls 0 of 5 and 0 of 4 missed** — reported as a pair with its frame because a single figure hides both the spread and the two builders. **That non-movement is the finding, not a null result:** the guard's PUBLISHED false-positive rate did not shift by a tenth of a point while its LIVE false-fault count grew by 24, because the published figure is measured over held-out SENTENCES and the cost is a property of SURFACE CLASSES. Round 11's F5 argued this; it now carries a number on both sides — `71.4% / 76.0%` unchanged against `29 → 53`. | **A disclosure that cannot move when the cost nearly doubles is not a disclosure of that cost.** The ask is NOT a tuning: see the sibling row for the two discriminators this needs. **Falsifiable** by producing, from the 24, one fault that asserts in this lab's voice an ordinal wrong about the board its sentence names; or by showing any of the 13 wrong on the live board; or by showing a closure name that changed across the range. **What must NOT be done, and V16's own history forbids it:** widening a window to make the 24 disappear would change what counts as evidence and would leave every one of them exactly as true or false as it was. | **chief** (whether a corpus-class false-fault count belongs beside the sentence-level rate on V16's face, which is the gate this moves); **fleet** (nobody may tune the guard to this number) |
| D279 | **THE GUARD NEEDS TWO DISCRIMINATORS, NOT ONE, AND ONLY THE FIRST IS THE GAP A PEER ALREADY NAMED — measured, both are TOTAL.** Driven one sentence at a time at `faa02f80`. **GAP-Q, use versus mention:** a bare assertion, the same sentence inside double quotes, introduced as a quotation, labelled a PROBE, labelled a CONTROL, negated, and struck with markdown **all return exactly 1 fault**; only an explicitly corrected one clears, and that is `_PLACE_ADJUDICATED`, a 400-character proximity window rather than grammar. **GAP-B, the board identifier the sentence carries:** `Reissmann placed 2nd`, `... of seven` (TRUE), `... of five`, `... of four`, `Reissmann is rank 2 on the live board` and `... on the published board` **all return exactly 1 fault** — the denominator and the board name are read by nothing. **GAP-Q IS the same gap named at D257** (*"the instrument cannot distinguish a surface ASSERTING the untrained claim from one QUOTING it in order to grade it"*, 27 of 39 quoted) **and measured at scale at D245** (1,501 of 1,884 board-claim sites meta, 383 assertions) **and already RULED at D255** (*"Quoting a claim in order to strike, grade or refute it is not making the claim"* — an identity, not a policy). **So the discriminator is not merely worth building: the ruling requiring it exists, the population it governs is measured, and V16's rule A implements none of it.** One discriminator closes 22 of this round's 24 faults, D257's 39-site uncertainty and D245's 1,501 meta sites — three problems, not two. **GAP-B is a DIFFERENT gap and a meta discriminator will not touch it**: the two true assertions are not quotations, they are the lab stating a derived fact correctly about a board they name by entrant count, so D255's identity does not exempt them because they ARE claims. | Measured by driving `board_placement_faults` directly at `faa02f80` on single sentences, with ordinal words assembled at runtime so this row does not itself become a corpus hit (D4). The convergence with D245/D255/D257 was established by reading those rows at `git show HEAD:docs/DOCKET.md`, not from memory. | **GAP-B's remedy is already written down elsewhere and is not a new invention**: the submission draft's binding triple (margin, entrant by name, board by count and date) and chief ruling `377d6afb` (*the referent is fixed by the CLAIM, not by the rung*) both say a claim naming its board is graded against that board. **Reading `of seven` is executing that ruling; re-pointing `_published_board` is not, and remains declined** — ruling `04489465` declined it and round 12 measured the cost at 40 faults after the dated-context mask rather than the ~18 its arithmetic implied. **Neither discriminator was built by this round and neither should be built to hit a number.** Falsifiable in one execution: show rule A returning a different verdict for `of seven` than for `of four`. | **chief** (GAP-B is a ruling already made and not implemented — whether V16 may be graded against a guard that does not execute `377d6afb`); **fleet / whoever owns D257** (GAP-Q is one build serving three registers, and that is the argument for its priority) |
| D280 | **D263 REPAIRED, and the FIRST proof of its neutrality was worthless in a way that would have passed for a proof.** The one-token repair at `scripts/self_audit.py:7445` — `dict(ranking[0])` → `dict(ranking[0] or {})` — makes the B1 branch reachable, because `_ranking_board()` reports failure by RETURNING `(None, message)` rather than raising, so the `except` above never fired and `dict(None)` raised `TypeError` one line above the branch written for exactly that case. **Verdict-neutrality measured in a REAL GIT TREE: status WARN → WARN, summary identical, and the FULL 53-FAULT SET identical, with the author precision row unmoved at 20 of 28 and the real-referent positive control unchanged; the B1 branch is reached in all three failure routes where it previously raised.** **THE DISCARDED FIRST PROOF IS THE REASON THIS ROW EXISTS.** It ran in a temp directory holding only the module, where `check_board_placement_words` degraded to *"could not enumerate tracked files (git unavailable)"* — so it compared **two broken verdicts**, reported `check verdict+summary identical in all 4 configs : True`, and that TRUE was worth nothing. A neutrality proof whose subject is disabled in both arms will always return neutral. It was caught by reading the summary string the harness printed rather than the boolean it computed. | Repaired 2026-08-16 at `faa02f80` under the chief's explicit conditional authorisation (*"if the fix is cheap and provably does not change any verdict, take it"*), after verifying `scripts/self_audit.py` was byte-clean against HEAD, unstaged, and held by no peer — the shared-index entries suggesting otherwise were confirmed STALE, the index blob for `test_blind_spots_are_printed.py` (`741de9d6`) predating HEAD's (`f6a9c9ad`) while the worktree matched HEAD exactly. Plant by LINE INDEX confirmed by readback; the throwaway worktree was restored and byte-verified afterwards, and an earlier run of the same harness that was killed mid-measurement left that worktree patched until it was restored and re-verified — recorded because a `finally` does not run when the process is killed. | **Nothing further owed on D263.** The generalisable half is the discarded proof: **a neutrality or regression harness must assert that its subject is LIVE in both arms before it compares them** — the same "a zero without a live control is not evidence" rule, applied to a comparison rather than to a sweep, where the failure mode is that BOTH arms are dead and the diff is therefore empty and reassuring. | **fleet** (the live-in-both-arms assertion, which belongs in any mutation or neutrality harness) |
| D281 | **THE V5 LEG-3 GRANULARITY RULING, REISSUED AFTER ITS OWN MEASUREMENT REFUTED THE DRAFT — AND IT REPAIRS 36 SITES, NOT 118.** The chief drafted a ruling that V5's leg 3 (*"Spalart (2000) cited wherever QCR is named"*) be graded per SENTENCE where revision is permitted and per FILE on records L-44 freezes, dispatched it to be refuted or confirmed by measurement before issuing, and the measurement killed three of its four premises. It is reissued here changed by them. **(1) THE GROUND STANDS, UNCHANGED: a criterion may not require more than the maximum permitted action on the surface it grades.** Where L-44 forbids revision, a dated addendum below the freeze line **IS** that maximum, so a per-sentence reading is **VOID there, not strict** — a rule satisfiable only by breaking another rule is not a strict rule. **Proved by execution on a real frozen pre-registration, `campaign/F6b_QCR_PREREGISTRATION.md`: per-sentence faults 7 before a below-the-line addendum and 7 after — UNCHANGED, which is the trap — against per-file 1 before and 0 after.** **(2) GRANULARITY FOLLOWS WHAT REVISION PERMITS.** Per **sentence** on live, published and source surfaces, where inline attribution is possible and therefore required; per **file** on signed or frozen records, where the dated addendum satisfies it. **(3) IT REPAIRS 36 SITES, NOT 118, AND THIS ROW IS WHAT REFUTES ANYONE WHO LATER CITES IT OTHERWISE.** Of the 118 claim-bearing sites inside the ruling's own scope, **82 — 69.5% — already attribute Spalart (2000) elsewhere in the same file and flip FAIL to PASS with ZERO WORK DONE the moment this issues. Those 82 are a RE-GRADING, NOT A REPAIR.** Only **36 sites across 22 files** get an addendum actually written. Seven in ten closing for free is the form-over-value defect this line of work exists to remove, and the draft would have read as closing 118. **(4) IT DOES NOT REACH TWO CLASSES, BOTH NEEDING A SEPARATE ANSWER.** **(4a)** **12 sites in 5 files** are structured containers with no addendum region **and** no attribution anywhere, unclosable under either reading: `agenda/proposals/f6b-qcr2000-on-the-hills.json` (4), `agenda/proposals/w3-qcr-forward-on-the-ducts-is-the-rank-1-route.json` (4), `sdk/tests/test_rank_claim_surfaces.py` (2), `closure_challenge_submission_round5/MANIFEST.json` (1), `sdk/tests/fixtures/absolute_claims_labelled_second_instance.json` (1). **(4b) HELD-OUT TEST FIXTURES, roughly 6% of the population, and the sharper of the two because neither the chief nor the measuring agent had listed it: the unattributed sentence IS THE TEST DATUM, and editing it destroys the control.** Any rule that reaches those is a rule that degrades the lab's own instruments; it is named here as its own class rather than folded into 4a. **(5) THE TRAP DOES NOT DISSOLVE INTO QUOTING.** Hand-adjudicated over 36 sites in two disjoint seeded samples: **quote or grading-meta 36.1% (95% CI 20-52%), genuine assertions 52.8%**, held-out fixture 5.6%, claim-filter false positive 5.6%. **The majority is the lab asserting in its own voice, so the trap is real and not an artifact of grading records quoting it.** Recorded because the discrepancy is the point: the loose auto-classifier said **73.8%** and was **inflated roughly 2x by counting BACKTICKS as quotation**; tightened to real quote marks and blockquotes it said **57.9%**; hand adjudication said **36.1%**. **A use-versus-mention discriminator that cannot say "CANNOT TELL" will guess, and three independent confirmations of that need landed tonight.** **(6) UNDER THE CHIEF'S OWN NARROWED PREDICATE (`78eb5530`) the criterion is about THE UNTRAINED CLAIM ABOUT OUR OWN MODEL**, so the ~6% of assertions concerning **Wu & Zhang's SST-QCRC** — another entrant's model — are **OUT OF FRAME ENTIRELY**, not merely lower priority, and the accounting above absorbs them rather than leaving them as an aside. | **Measured 2026-08-16T17:5xZ-18:2xZ at argv-pinned frame `ae09cb3c`, the SHA passed in-process and never held in a file (D247's class).** Predicates **copied verbatim** from `sdk/tests/test_untrained_qcr_attribution.py` so this graded THAT criterion and not a paraphrase of it. **Four-figure frame assertion executed before any number was reported: 20,734 tracked entries.** **CONTROLS: positive plant BY LINE INDEX with readback** (fired at the planted line), **attributed twin** (0 hits, correct), **struck negative** (0 leaking live). **NOT-GRADED ACCOUNTING, reported rather than counted clean: 1,476 non-UTF-8 binary-marked files INCLUDING ALL 50 TRACKED PDFs; 26 files unreachable by `git grep` in any mode (17 symlinks at mode 120000, 9 empty blobs); `git grep -a` reaches 20,708 of 20,734.** **INDEPENDENT REPRODUCTION, which is what makes the numbers usable: a separately written claim-bearing predicate returned 126 sites in 48 files against the peer's 125 in 49.** **AND THE FINDING THAT RECLASSIFIES THE WHOLE QUESTION: no executable check fails any of these sites.** The guard's entire frame is **three artifacts** — `build_benchmarks.py`'s `_CLOSURE` literal, `demo-output/website/benchmarks.json`, `demo-output/website/wall/wall.json` — it **walks no corpus**, it **passes 4 of 4**, and **ZERO of the 48 claim-bearing files are in its frame**. The two corpus-walking guards that mention QCR, `scripts/self_audit.py` and `sdk/tests/test_rank_claim_surfaces.py`, contain **zero occurrences of "Spalart"**. **So this is a HUMAN-GRADED LADDER CRITERION, not a guard trap.** That does not make it less real — a criterion no permitted action can satisfy poisons every grade of leg 3 — but the ruling's effect is on how a grader reads the criterion and **on no CI verdict whatsoever**, and any statement otherwise would be a false claim about this lab's own machinery. **CANNOT SEE:** whether the 36.1% quote rate holds outside its 95% interval of 20-52% at n=36; anything in `dist/` or `demo-output/website/latex/`, which were outside the declared corpus; and whether any surface outside the tracked tree carries the claim. | **NOTHING WAS REPAIRED IN THIS PASS AND THAT WAS THE INSTRUCTION** — the ruling had to land before the addenda, so that no agent pre-empts the ruling it was dispatched to test. What is now owed, in order: **(i)** the **36 addenda across 22 files**, each a dated note below the freeze line, written by re-derivation and never by pasting a citation, since a rule whose main product is pasted attributions trains the paste and is the B4 class; **(ii)** a separate answer for the **12 container sites in 5 files** of clause 4a, which no reading of this criterion reaches — the honest options are to declare structured containers out of leg 3's frame, or to give them a sibling file that carries the attribution, and neither is this ruling's to choose; **(iii)** a separate and more careful answer for the **held-out fixtures** of clause 4b, where the default must be DO NOT EDIT and the burden is on anyone proposing to touch a control; **(iv)** the **use-versus-mention discriminator** of clause 5, which must be able to return "cannot tell" and must be controlled, since two classifiers over the same population disagreed by 22 points. **This row must not be cited as having closed 118 sites.** **Falsifier:** re-run the instrument at `ae09cb3c` and show the already-attributing share of the 118 to be other than 82, or show a below-the-line addendum changing the per-sentence fault count on `F6b_QCR_PREREGISTRATION.md`. | **chief** for clause 4a's frame decision and for whether leg 3's criterion text should be amended to state its own granularity; **Katie** with the chief on 4b, since editing a held-out control is a decision about the lab's instruments and not about a citation; the infra family for the clause-5 discriminator; the owner of the 22 frozen records for the 36 addenda |
| D282 | **THE USE-VERSUS-MENTION DISCRIMINATOR IS BUILT AND MEASURED, AND IT NARROWS THE ADDENDUM-ONLY CLASS FROM "12 TO 39" TO "2 TO 17" -- WITH 15 SITES IT REFUSES TO CALL.** Three measurements tonight hit one instrument gap: D257's unattributed-QCR class, where the whole distance between the floor of 12 and the ceiling of 39 is whether a grading record QUOTES a claim or MAKES it; D264, where 18 of the 19 newly added rank-claim faults were the previous round's own paperwork, on a rung whose criterion makes false-positive rate a PASS requirement; and the strike false positive corrected at faa02f80, which is the same shape one level down -- an instrument that could not tell a USED delimiter from a MENTIONED one, over-reporting on exactly the documents that discuss delimiters, which are the governance files. **Built as scripts/use_mention_discriminator.py, three-valued, with the CANNOT_TELL bucket reported and never folded into either side.** Applied to the 39-site narrow class: **2 ASSERT, 22 MENTION, 15 CANNOT_TELL**; on the 58-site broad class, 4 / 34 / 20. **So the class that looked like 39 unclosable sites is very probably 2 -- and one of those 2 is a known error shape (a grading record saying "our untrained QCR2000" while adjudicating it), so the true count may be 1.** The 15 undecided are the honest remainder and are why this row does not claim the class is empty. **THE DESIGN DECISION THAT MATTERS, and it was forced by measurement rather than chosen: the API REQUIRES the caller to pass the claim's character span, and has no overload that guesses it.** The first version took the sentence alone, scored four lexical signals equally, and measured MENTION precision at **58%** on held-out sentences. Every failure was one shape: a lab record CITING ITS OWN EVIDENCE carries quote marks, commit hashes and file paths exactly as a record quoting someone else does. What decides the question is whether THE CLAIM ITSELF is enclosed -- in quotation marks, a code span, a strike span or a blockquote -- and that cannot be asked without offsets. | Measured 2026-08-16 at frame e1ede44a. **Precision is measured on held-out sentences rather than asserted, which is the clause V16's criterion holds its own subject to.** A DESIGN set of 48 real sentences was drawn first and the rules were revised against it; a FRESH held-out set of 78 sentences was then drawn from sentences never seen during design and scored **EXACTLY ONCE**, with no rule changed afterwards: **ASSERT precision 7/10 = 70%, MENTION precision 23/30 = 77%, decided 40/78 = 51%, CANNOT_TELL 38/78 = 49%.** Both sets ship as scripts/use_mention_control_set.json so the figures are reproducible. **Three error classes are named in the shipped control set rather than fixed**: a JSON string value is read as a quotation, because the enclosure test cannot tell a quoted claim from a JSON string delimiter; a grading record written in the first person trips the ASSERT signal while adjudicating; and adjudication vocabulary in assertive prose pulls an assertion to MENTION. The positional test carries a six-case control set including the discriminating case -- a quotation mark present in the sentence but NOT around the claim, which must NOT count -- and all six pass. **A fourth false zero was produced and caught during this work**: the class sweep was re-run as a background job that exited **0** having written **nothing**, leaving a stale results file that still contained the seeded positive control from an earlier run; the seed is what exposed it, for the third time tonight, and the sweep now takes an explicit destination and prints a completion marker. **STATED LIMITATION: the gold labels are single-annotator and the annotator is the module's author.** Nobody has re-labelled them independently, and that is the first thing to attack if these figures are relied on. | **Do not gate on it. Use it to narrow a range and to route attention.** At 70% and 77% precision with half the population undecided it is a triage instrument, not a verdict instrument, and any sweep quoting it must carry the CANNOT_TELL count beside the other two or it has converted an honest range into a false point estimate -- the failure this module exists to prevent. **What it settles for D257:** the granularity ruling still decides the class, but the class is now measured at 2 probable assertions plus 15 undecided rather than 39, which is a materially smaller thing to rule about. **What it offers D264:** the same question, one level up -- whether a rung-closure document is a claim surface is exactly a use-versus-mention judgement, and this module composes with self_audit.py's `_VALUE_NOT_A_SURFACE` rather than competing with it, because that rule is about WHERE a sentence lives and this one about WHAT IT DOES. **Both admission predicates are stated on the module's face on purpose (D49), so a second instrument answering this question can be compared to this one instead of merely disagreeing with it.** **Falsifier: have a second annotator re-label the 78 held-out sentences.** If inter-annotator agreement is below the measured precision, the precision figure is measuring the annotator and not the instrument. | **fleet** (the second annotator, which is the cheapest way to falsify this row); D264's owner (whether this composes into the rank-claim guard); **chief** (whether a triage instrument may be cited in a rung grade at all) |
| D283 | **The runner's write predicate could be switched off for a whole file by one `tempfile.mkdtemp()` anywhere in it, and that had put SEVEN MUTATION HARNESSES into the standing pre-push run.** `_write_primitive` in `scripts/lab_check.py` collected `ast.Module` as a scope and marked a scope temp-scoped if ANY call inside it made a temporary directory. `ast.Module` contains every node in the file, so a single `mkdtemp` in an unrelated helper marked the module temp-scoped, and the containment exemption then forgave EVERY write in that module. **The direction is what ranks this:** the module's own comment states the asymmetry it must preserve -- "a false SKIP is printed and costs coverage, a false ADMIT writes into a tree ten agents are working in" -- and this was a false ADMIT. Measured before the repair: **8 of 25 admitted script gates were admitted ONLY by that blanket, and SEVEN of the 8 were mutation harnesses**, programs that hold a tracked file mutated BY CONSTRUCTION. `scripts/installed/pre-push` runs every admitted gate on every push, so the fast tier was reachable to programs whose entire purpose is to leave the tracked tree mutated -- docket D212's capture window, unattended, on a hook. Found while repairing D274, not predicted, and not previously filed by anyone. | Demonstrated with a control at `9def09f5`: a module whose `main()` does `Path("/home/ubuntu/Certonomous/EVIDENCE.txt").write_text(...)` -- an ABSOLUTE path inside this repository, so no reading of it makes running the file safe -- classified `admitted=True, writes=False` while an unrelated function called `tempfile.mkdtemp()`, and `admitted=False, writes='line 8: write_text(...)'` with that one line removed. The 8 were `check_pdf_surfaces.py` and `mutation_harness_{control_kind, coverage_remedy, docket_reconciliation, form_or_value, hunk_check, rank_values, rung_attribution}.py`; `mutation_harness_hunk_check.py` writes to `REPO / "scripts/hunk_check.py"`. After the repair, admitted script gates went **25 -> 17** and all seven harnesses are skipped `writes-to-tree`. Pinned by `sdk/tests/test_lab_check.py::TheTempScopeExemptionIsPerFunctionNotPerModule` with both halves, and the pin was PROVED by reverting the repair in a scratch copy: the positive case failed, both controls passed. | **CLOSED at `9def09f5`** -- the exemption is per-function; `ast.Module` no longer grants it. Two residuals for others, neither owned here. **(a)** The seven harnesses are now correctly OUT of the live tier and are not run anywhere else either; if they are meant to run on a schedule they belong in `--tree snapshot`, and somebody should decide that rather than inherit it. **(b)** The exemption remains a lexical guess in both directions -- it cannot tell `mkdtemp` used as the write target from `mkdtemp` used incidentally in the same function -- so a writer that makes a temp dir AND writes to the repo in one function is still admitted. Narrower than the module-wide blanket by a lot, and still a guess. | owner of `scripts/lab_check.py` (done, this row); **chief** for whether the mutation harnesses should be scheduled under `--tree snapshot` |
| D284 | **V7 gave two sentences of the same shape, sitting in the same table of its own pass record, opposite treatments -- and the one it left is a present-tense claim naming the wrong ordinal, unchanged since.** V7's claim class is standing claims in `sdk/` about the cumulative count of official scoring calls; the true count has been **6** since 2026-08-07. Its pass record `campaign/LADDER_V_RUNGS_V2_V7_V10_2026-08-08.md:74-83` enumerates eight sites in one table. Two of them have the same shape -- a count stated without period scoping -- and got opposite verdicts. `sdk/scripts/closure_round4_manifest.py:135` (*"five distinct prediction sets scored, **ever**"*) was marked **"false as a standing claim"** and **FIXED**, and now reads *"five distinct prediction sets scored **as of this round-4 manifest**; six after round 5's 2026-08-07 call"*. `sdk/scripts/closure_criterion_on_test_features.py:10-11` (*"Whether to spend a **5th** official scoring call on anything this table shows **is** explicitly the coordinator's decision"*) was marked *"historical tense, dated to the round-3 era, makes no claim about the current count"* and **left**. Measured against the file rather than the reconciliation: the sentence is **present tense** (*"is explicitly the coordinator's decision"*), contains **no year** and **no round or era scoping of its own** (`re.search(r'20\d\d')` and a round/as-of/era scan both return nothing over lines 7-11), and the era attribution belongs to the SCRIPT, not to the sentence. The next call today would be the **7th**. The file's last commit is `36b7e178`, **2026-08-02**, six days before V7 -- so the line is byte-unchanged since V7 examined it, and this is **not a miss by the sweep**: it was enumerated, reasoned about, and deliberately left. **The finding is the inconsistency, not the line.** Whether a period-correct ordinal inside a present-tense sentence needs scoping is exactly the question V7 answered YES to in one row and NO in another, four rows apart. | Found 2026-08-16 at HEAD `de3bc725` by an agent that authored none of V7, its record, or any of the eight enumerated sites, while measuring V7's exposure under the reachability/recognition distinction (`scripts/control_kind.py`, `90cd7f15`). **The sweep carried a RECOGNITION control and it fired on its own author first:** six variants planted in V7's own claim vocabulary -- numeral, spelled word, ordinal, `#N`, *"the ledger stood at N"*, *"N distinct prediction sets scored"* -- of which *"the sixth pre-registered scoring call"* was **NOT FOUND**, because the pattern's `\w+` filler could not cross the hyphen in *"pre-registered"*. The control returned **BROKEN** and the sweep's count was refused until the pattern was repaired to `[\w-]+`, after which all six fired, one negative form was rejected, and the classification reached RECOGNITION. **The repaired sweep found 31 hits across 15 files and no OTHER live standing count-claim defect**: every remaining in-class hit is period-scoped, dated, or states the correct 6. **Cannot see:** whether V7's *"full grep of `sdk/`"* would have reached a file it did not list -- the one in-class site outside its eight-row table, `sdk/scripts/closure_eval_battery/build_master_table.py:247`, carries the CORRECT count and was added by `dc13f1cc` on **2026-08-10**, two days after V7, so no reach gap is demonstrated and none is claimed; and anything outside `sdk/`, which V7's own scope excludes. | **A NON-AUTHOR GRADER, and this row deliberately states no verdict.** V7 is still marked PASS and nothing here changes that: the question is whether one enumerated site was correctly dispositioned, which is a grading call and belongs to someone who wrote neither V7 nor this row. What would settle it, either way and cheaply: rule on whether a present-tense sentence naming an ordinal count needs its own period scoping when the enclosing script is era-dated. If YES, `closure_criterion_on_test_features.py:10` takes the same one-clause repair `closure_round4_manifest.py:137` already carries and V7's row is amended to say four sites were corrected rather than three. If NO, then `closure_round4_manifest.py`'s fix was unnecessary and V7's own stated reason for it -- *"false as a standing claim"* -- is the sentence that needs narrowing. **The wider exposure, measured and also not ruled on:** the ladder row for V7 records *"two more stale generator strings found later"*, and those two (`1449557a`, `e639796e`) are stale generator literals about **best-on-board counts and QCR attribution**, not about scoring-call counts at all. V7's searched class was narrower than the defect SHAPE that kept recurring -- a literal frozen in a generator whose output has moved past it -- which is V10/D239 territory rather than V7's, and is named here so the grader is not left inferring that V7's class and that shape are the same thing. | **chief** to route to a non-author grader (the ruling above); the ladder's owner if V7's face needs amending afterwards |
| D285 | **The prior-art firewall's headline figure rests on a write time no tracked artifact carries, and the margin the record can demonstrate is 2 minutes 19 seconds rather than 16 hours.** `closure_challenge_submission_round5/DESCRIPTION_DOCUMENT.md` states, as the chronological proof that the entry is not indirectly trained on a scored test case: *"our `NASA_2DWMH` prediction is byte-identical across rounds 3, 4 and 5 and was written on 2026-07-31, **16 hours before** that paper is first mentioned anywhere in our repository."* Measured at `27295b9b`: the 2025 paper is first mentioned at `92840d8c`, **2026-07-31T23:13:49Z** (probes `Lăcătuş` and `Buchanan, L` agree; the earlier `Buchanan` at `08007253` is the **benchmark** paper's author list and was opened rather than assumed). The prediction's file first lands at `4c2f3562`, **2026-07-31T23:11:30Z** — **2m19s** earlier. **The two statements are not contradictory** — the claim is about when the prediction was *written*, and git records when it was *committed* — **but no tracked artifact carries that write time**, so the 16 hours is not checkable by the reviewer it is written for, and the direction it errs in flatters us. Separately, *"rounds 3, 4 and 5"* is verifiable for **two** of the three: `NASA_2DWMH.csv` is byte-identical between the round-4 and round-5 submission directories (sha256 prefix `cf8e023c7b8fcb05`), and **no round-3 submission directory is tracked at that path**. | `demo-output/website/closure_challenge_submission_round5/DESCRIPTION_DOCUMENT.md`, the compliance-fact paragraph; commits `92840d8c`, `4c2f3562`, `08007253` read at `27295b9b`. `campaign/LADDER_V_V9_REGRADE_2026-08-16.md` §4. | Either cite a tracked artifact that carries the write time — a run-tree timestamp, a solver log, a mission record — or restate the proof on the margin the record can actually demonstrate, which is still the right *ordering* and is what the claim needs. Fix *"rounds 3, 4 and 5"* to name the directories that exist. **This did NOT fail V9**: the clause asks for a check, the check exists and its substantive core holds — no coefficient or artifact of that model in any `*.py`/`*.sh`/`*.json`/`*.yaml`, and byte-identity where it can be measured. **It is a travelling document**, which is why it is filed at all. **Falsifier:** produce the tracked artifact carrying the 2026-07-31 morning write time, and the row closes. | **Katie/chief**, because the description document travels with the submission; **FLEET** for locating a write-time artifact if one exists |
| D286 | **THE SENTENCE RUNG V9 EXISTS TO ELIMINATE IS ALIVE AND UNSTRUCK IN A TRACKED FILE, ONE REWORDED PHRASE PAST BOTH CERTIFIED LITERALS.** `demo-output/website/agenda/docket.json:1594` reads *"a classifier reading only the uncorrected solve and **controlling where a data-driven correction may act** is published prior art in **Ling and Templeton 2015, Wu, Wang, Xiao and Ling 2017**, Steiner, Dwight and Vire 2022, and Buchanan, Lacatus, West and Dwight 2025"* — all four papers rolled into one mechanism, **crediting the two identify-papers with a control mechanism neither reported**, which is exactly what the 2026-08-05 correction `d84b649f` struck and what V9 was created to prevent. **It reads `may act` where the certified fragment reads `is allowed to act`**, so both literal sweeps of record pass straight over it. It was found by a recogniser built for the **claim class** and validated on eight paraphrases, and it is the strongest validation that instrument has: **an unplanted, real-world instance, not one the grader wrote.** **Deliberately NOT repaired** — its grader may not repair what he grades, and `docket.json` is an agenda record with an owner. | `demo-output/website/agenda/docket.json:1594`, read at `27295b9b`. Method, controls and readbacks in `campaign/LADDER_V_V9_REGRADE_2026-08-16.md` §2 and §5. | Restate the sentence in the split form the description and `closure.html` already carry — **identify** (Ling & Templeton 2015, which controls nothing; Wu, Wang, Xiao & Ling 2017) and **control** (Steiner, Dwight & Viré 2022; Buchanan, Lăcătuş, West & Dwight 2025) — or strike it in place with the split beside it. **Ruled OUT of V9's scope and filed rather than appended:** V9 names *the description*, and `docket.json` is neither the description document nor an archive member — the archive's JSON members are mission and snapshot files only, and `docket.json` does not ship. **The wider point outranks the row:** an outcome record that states the prior-art finding *incorrectly* is a surface no prior-art sweep had ever enumerated. **USE VERSUS MENTION IS THE WHOLE DIFFICULTY HERE, AND IT SHOULD NOT BE SOLVED TWICE.** The class recogniser returned ~14 tree-wide hits and **all but this one are MENTIONS** — `campaign/LADDER_V_V14_SURFACE_DISCOVERY.md`, `LADDER_V_V15_ROUND4.md`, `LADDER_V_V13_PENDING2_MEASURED_2026-08-15.md` and `LADDER_V_TRIPLE_VERIFICATION.md` all carry the fragments **because they are records certifying the sentence's absence**, and `closure.html` and the description carry the *corrected* split whose two halves sit in adjacent sentences. `docket.json:1594` is the only **ASSERTION**, stated in the lab's own voice as the review's verdict. They were separated here by **opening each one and reading it**, which does not scale. The lab already has the instrument: the use-versus-mention discriminator at `79e52dfa`, held-out precision **ASSERT 70% / MENTION 77%**, deciding **51%** and returning CANNOT_TELL rather than guessing, whose API **requires the claim's character span** precisely because a lab record citing its own evidence carries quote marks, commit hashes and file paths exactly as a record quoting someone else does. **Any future prior-art sweep should call it rather than grow a second admission predicate — that is D49's lesson.** **Falsifier:** show `may act` is inside a strike span, or that Ling & Templeton 2015 does report a control mechanism, and this falls. | **FLEET**, jointly with whoever owns `agenda/docket.json`; **chief** for whether outcome records fall inside prior-art rungs at all, and for wiring `79e52dfa` into the prior-art class sweep |
| D287 | **I repeated a figure I had not checked, and it reached the owner-facing document -- the exact defect class this pass spent the night mapping, committed by the pass that mapped it.** D243 recorded its own blind spot as *"the other **67 tracked PDFs**, which were not rendered"*, implying 68 tracked. **`git ls-files '*.pdf'` returns 50 -- at HEAD, and at D243's own frame `5a0127d3`, so the figure was wrong when it was written and wrong at every tree since.** The **68** is **50 tracked plus 18 gitignored**; the unrendered tracked remainder is **49**. **I inherited it without re-deriving it and propagated it to three further surfaces**: `campaign/SWEEP_EXPOSURE_MAP_2026-08-16.md` (twice), row **D277**, and -- worst -- `campaign/LAB_STATE_FOR_OWNER_2026-08-16.md`, **the single document written to be read by the owner in one sitting**. All four corrected here; D243's own wording is left standing and marked rather than rewritten, because it is another agent's record. **A SECOND CORRECTION IN THE SAME DOCUMENT, and it is the same shape.** The owner document stated that the camera-facing *"Act 1 versus sealed certificate"* decision **"comes first"** and had to be taken before the `dist/` rebuild. **No record sequences it that way.** The decision is real, open and the owner's (D177; control-room surface D136), but every record that prescribes the rebuild -- D233, D224 -- calls for *"a plain re-run … with no hand-editing"* and names no precondition. **I asserted a dependency between two owner decisions that the record does not carry**, which would have made a free decision look blocked. Corrected to state both as open and neither as gating the other. | Found 2026-08-16 by a verification pass this agent had itself dispatched to re-derive every owner-queue figure BEFORE quoting it -- which returned **after** the document had already been committed at `de3bc725`. The count was then re-derived here directly rather than taken from that pass either. | **The rule this violates is one this pass wrote down four rows earlier**: *a positive control proves reachability or recognition, and these are not the same thing* -- and its sibling, that a figure inherited from a record is not a measurement. **D243's 67 was never re-derived by anyone between its filing and its arrival in front of the owner**, across four documents and roughly one hour. The cheap general form: **a count quoted from another record carries that record's frame, and a count in an owner-facing document must be re-derived at the tree it ships from, not inherited.** The verification that caught this was dispatched correctly and simply lost the race with the commit -- **the process defect is that the document was committed before its own verification returned**, not that the verification was missing. | this agent (both corrections landed here); the holder of D243 for its own wording, which is left marked and not rewritten |
| D288 | **The two surfaces V10 was graded FAIL on are still live and unstruck inside the archive that travels, and the class already filed for that archive does not cover them.** `dist/certonomous-demo.zip`, built 2026-08-14T21:16:50Z, 103 members, opened with `zipfile` and never byte-grepped, carries: `certonomous-demo/site/closure.html:430` and `:431`, **two gold `OUR MODEL LEADS` badges** on `Periodic hill, steep, coarse` (0.0592 / 0.0501) and `Periodic hill, steep, fine` (0.1195 / 0.1011) -- **0 of the 2 sit inside a strike span**, and against the live six-entry board those two cases are **2nd of 7 and 2nd of 7**, both lost to Tian, Buchanan, Hickel & Dwight at 0.0432 and 0.0998; and `certonomous-demo/site/benchmarks.html`, the heading *"Cases where we lead the entire public leaderboard"* -- **soft-wrapped across a line break, so a literal-space pattern misses it and `\s+` finds it** -- over a four-row wall with **four gold `#1` tags**, the four-entry `Best published` column 0.0592 / 0.1195 / 0.0569 / 0.0760 and green `win` margins -15.4% / -15.4% / -19.0% / -5.4%, of which **two rows are losses and both margins flip sign, to +16.0% and +1.3%**. Plus one live `rank 1 of 5`. **Both tracked copies were repaired inside V15 round 9's declared range** -- `closure.html` at `cca64eaf`, `benchmarks.html` at `fe54ec0a`, the latter forty minutes before that scope closed -- and the archive predates both by two days. | Found 2026-08-16 by **V15 round 9**, out of that rung's declared scope (`dist/` is gitignored and is not among the 88 files of `cca64eaf..fe54ec0a`), so **filed and not appended**. Instrument: a four-arm sweep, whole-file, strike-blanked with non-greedy `<s>(.*?)</s>` pairing, patterns built with `\s+` and never a literal space. Controls: ten positive plants read back off disk before the run (2 anchors each) firing 30/30, four struck negatives returning 0/4; the zip arm's **first** plant targeted a member that does not exist, the readback printed *"NO POSITIVE PLANT LANDED -- control is dead"* and that run was discarded before it was believed. Arms enumerated, not assumed: tracked 20,704 at `fe54ec0a`, untracked 1, gitignored 37,245, run tree 132,049 with `FILES SKIPPED: 0`. | **What is already filed and does not need re-filing:** D56, D112, D180 (which enumerates *eight* live four-entry claims in this same archive), D200 (a ninth), D207, D224 (*"all three faults on the gating arm are a shipping bundle built BEFORE the corrections landed"*) and D252 (the two rank statements in the unpacked mirror). **Neither the gold `OUR MODEL LEADS` badges nor the wall heading is among any of them** -- D180's `benchmarks.html` entry is `:148-149` and is about the margin, and D149 covers the badges only on the tracked page. That delta is what this row adds. **Settled by** a rebuild of the archive from the repaired sources, or by striking the four sites in the shipped copy. **Falsifier:** show that either site is struck in the archive, or that a rebuild has landed and the members now agree with their tracked sources. **Limit of this row's own instrument, stated:** its controls plant the exact strings searched for, so they prove REACHABILITY into the archive's members and encodings, **not RECOGNITION** -- a paraphrase of the same claim sharing no token with the probe would pass through, and the four sites above are therefore a **floor**, not the population. | **chief** for who owns `dist/` rebuilds (D71/D115/D118/D136/D145 record that zone as UNOWNED and never once name an owner); **FLEET** for the rebuild itself |
| D289 | ~~**DUPLICATE — this row is a verbatim re-append of D278 and carries no finding of its own.**~~ **Struck in place rather than deleted or renumbered, the register being append-only. Cause and remedy: see the triplication row appended below.**  **D264 ADJUDICATED: the placement guard's live cost is a PRECISION REGRESSION and not a reach increase — 24 new faults, ZERO of them true — and the two sentences that are genuine assertions are both TRUE of the board they name.** The guard did not move: **not one of the 68 names in `check_board_placement_words`' callee closure changed across `9dca4738..faa02f80`**, nor any of `board_placement_faults`' 30, so one fixed instrument was run over two corpora. **29 → 53 faults, 24 new, 0 gone** — and it is 53 rather than the 48 first reported because **five of the additions are the grading round's own paperwork**, three from `LADDER_V_V16_ROUND12.md` and two from the docket rows it filed. Adjudicated against a rubric fixed before looking (TRUE = asserts, in this lab's voice, an ordinal wrong about the board the sentence refers to): **0 TRUE, 24 FALSE.** Mechanically, each fault's `(entrant, claimed ordinal)` tested against both boards: **13 of 24 are CORRECT on the live six-entrant board, 0 of 24 are correct on the pin**, 2 are rule B on a quoted `margin over the leader`. **22 of the 24 are META** — probe rows, rows literally labelled `**CONTROL**`, quoted fault messages, struck markup, and docket rows quoting those — which **chief ruling D255 has already ruled are not claims at all**. **The remaining 2 are the only assertions in the set and both are true**: `LADDER_V_V10_CLOSURE_2026-08-16.md:92` and the ledger's V10 narrative read *"the live duct leader was Yang at 0.0291 / 0.0311 / 0.0250; Reissmann placed 2nd, 2nd and 2nd **of seven**"*, re-derived here from `LIVE_BOARD` joined to `round5_per_case_full` as Yang first and Reissmann second on all three duct cases. **This lands on V16's own gate**, whose criterion makes a false-positive rate against a measured corpus a PASS REQUIREMENT. | Executed at `faa02f80` in two detached worktrees, `__pycache__` purged, faults diffed BY IDENTITY rather than by file count. **The precision pair was re-measured at the same frame and did NOT move: author 20 of 28 (71.4%), grader 19 of 25 (76.0%), controls 0 of 5 and 0 of 4 missed** — reported as a pair with its frame because a single figure hides both the spread and the two builders. **That non-movement is the finding, not a null result:** the guard's PUBLISHED false-positive rate did not shift by a tenth of a point while its LIVE false-fault count grew by 24, because the published figure is measured over held-out SENTENCES and the cost is a property of SURFACE CLASSES. Round 11's F5 argued this; it now carries a number on both sides — `71.4% / 76.0%` unchanged against `29 → 53`. | **A disclosure that cannot move when the cost nearly doubles is not a disclosure of that cost.** The ask is NOT a tuning: see the sibling row for the two discriminators this needs. **Falsifiable** by producing, from the 24, one fault that asserts in this lab's voice an ordinal wrong about the board its sentence names; or by showing any of the 13 wrong on the live board; or by showing a closure name that changed across the range. **What must NOT be done, and V16's own history forbids it:** widening a window to make the 24 disappear would change what counts as evidence and would leave every one of them exactly as true or false as it was. | **chief** (whether a corpus-class false-fault count belongs beside the sentence-level rate on V16's face, which is the gate this moves); **fleet** (nobody may tune the guard to this number) |
| D290 | ~~**DUPLICATE — this row is a verbatim re-append of D279 and carries no finding of its own.**~~ **Struck in place rather than deleted or renumbered, the register being append-only. Cause and remedy: see the triplication row appended below.**  **THE GUARD NEEDS TWO DISCRIMINATORS, NOT ONE, AND ONLY THE FIRST IS THE GAP A PEER ALREADY NAMED — measured, both are TOTAL.** Driven one sentence at a time at `faa02f80`. **GAP-Q, use versus mention:** a bare assertion, the same sentence inside double quotes, introduced as a quotation, labelled a PROBE, labelled a CONTROL, negated, and struck with markdown **all return exactly 1 fault**; only an explicitly corrected one clears, and that is `_PLACE_ADJUDICATED`, a 400-character proximity window rather than grammar. **GAP-B, the board identifier the sentence carries:** `Reissmann placed 2nd`, `... of seven` (TRUE), `... of five`, `... of four`, `Reissmann is rank 2 on the live board` and `... on the published board` **all return exactly 1 fault** — the denominator and the board name are read by nothing. **GAP-Q IS the same gap named at D257** (*"the instrument cannot distinguish a surface ASSERTING the untrained claim from one QUOTING it in order to grade it"*, 27 of 39 quoted) **and measured at scale at D245** (1,501 of 1,884 board-claim sites meta, 383 assertions) **and already RULED at D255** (*"Quoting a claim in order to strike, grade or refute it is not making the claim"* — an identity, not a policy). **So the discriminator is not merely worth building: the ruling requiring it exists, the population it governs is measured, and V16's rule A implements none of it.** One discriminator closes 22 of this round's 24 faults, D257's 39-site uncertainty and D245's 1,501 meta sites — three problems, not two. **GAP-B is a DIFFERENT gap and a meta discriminator will not touch it**: the two true assertions are not quotations, they are the lab stating a derived fact correctly about a board they name by entrant count, so D255's identity does not exempt them because they ARE claims. | Measured by driving `board_placement_faults` directly at `faa02f80` on single sentences, with ordinal words assembled at runtime so this row does not itself become a corpus hit (D4). The convergence with D245/D255/D257 was established by reading those rows at `git show HEAD:docs/DOCKET.md`, not from memory. | **GAP-B's remedy is already written down elsewhere and is not a new invention**: the submission draft's binding triple (margin, entrant by name, board by count and date) and chief ruling `377d6afb` (*the referent is fixed by the CLAIM, not by the rung*) both say a claim naming its board is graded against that board. **Reading `of seven` is executing that ruling; re-pointing `_published_board` is not, and remains declined** — ruling `04489465` declined it and round 12 measured the cost at 40 faults after the dated-context mask rather than the ~18 its arithmetic implied. **Neither discriminator was built by this round and neither should be built to hit a number.** Falsifiable in one execution: show rule A returning a different verdict for `of seven` than for `of four`. | **chief** (GAP-B is a ruling already made and not implemented — whether V16 may be graded against a guard that does not execute `377d6afb`); **fleet / whoever owns D257** (GAP-Q is one build serving three registers, and that is the argument for its priority) |
| D291 | ~~**DUPLICATE — this row is a verbatim re-append of D280 and carries no finding of its own.**~~ **Struck in place rather than deleted or renumbered, the register being append-only. Cause and remedy: see the triplication row appended below.**  **D263 REPAIRED, and the FIRST proof of its neutrality was worthless in a way that would have passed for a proof.** The one-token repair at `scripts/self_audit.py:7445` — `dict(ranking[0])` → `dict(ranking[0] or {})` — makes the B1 branch reachable, because `_ranking_board()` reports failure by RETURNING `(None, message)` rather than raising, so the `except` above never fired and `dict(None)` raised `TypeError` one line above the branch written for exactly that case. **Verdict-neutrality measured in a REAL GIT TREE: status WARN → WARN, summary identical, and the FULL 53-FAULT SET identical, with the author precision row unmoved at 20 of 28 and the real-referent positive control unchanged; the B1 branch is reached in all three failure routes where it previously raised.** **THE DISCARDED FIRST PROOF IS THE REASON THIS ROW EXISTS.** It ran in a temp directory holding only the module, where `check_board_placement_words` degraded to *"could not enumerate tracked files (git unavailable)"* — so it compared **two broken verdicts**, reported `check verdict+summary identical in all 4 configs : True`, and that TRUE was worth nothing. A neutrality proof whose subject is disabled in both arms will always return neutral. It was caught by reading the summary string the harness printed rather than the boolean it computed. | Repaired 2026-08-16 at `faa02f80` under the chief's explicit conditional authorisation (*"if the fix is cheap and provably does not change any verdict, take it"*), after verifying `scripts/self_audit.py` was byte-clean against HEAD, unstaged, and held by no peer — the shared-index entries suggesting otherwise were confirmed STALE, the index blob for `test_blind_spots_are_printed.py` (`741de9d6`) predating HEAD's (`f6a9c9ad`) while the worktree matched HEAD exactly. Plant by LINE INDEX confirmed by readback; the throwaway worktree was restored and byte-verified afterwards, and an earlier run of the same harness that was killed mid-measurement left that worktree patched until it was restored and re-verified — recorded because a `finally` does not run when the process is killed. | **Nothing further owed on D263.** The generalisable half is the discarded proof: **a neutrality or regression harness must assert that its subject is LIVE in both arms before it compares them** — the same "a zero without a live control is not evidence" rule, applied to a comparison rather than to a sweep, where the failure mode is that BOTH arms are dead and the diff is therefore empty and reassuring. | **fleet** (the live-in-both-arms assertion, which belongs in any mutation or neutrality harness) |
| D292 | **V15 round 9's F4 is UPHELD as a criticism and its worry is REFUTED as a fact: re-run with a recognition control, no round has ever declared itself belief-neutral, so `R-VALUE = 0` survives — but the sweep that establishes it answers a NARROWER question than R-VALUE asks, and that gap is not closable by any declaration sweep.** F4 was right that the lab's headline rested on searches for two token families (`R-VALUE`, `belief-neutral`) whose positive controls planted the tokens they searched for, proving REACHABILITY and not RECOGNITION. Re-measured with `scripts/check_belief_neutrality.py`, which carries a **12-form recognition control frozen (sha256 `e7119fb4…`) BEFORE its patterns were written** and classifies itself through `scripts/control_kind.py`: **117 hits over two corpus arms — 36 tracked round/grade documents by `git ls-files`, and every commit message by `git log --all` — classified ASSERT 0, NEGATED 62, MENTION 20, LOCAL_CONCLUSION 15, CANNOT_TELL 20.** The five places a neutral declaration could have hidden — V16 rounds 8, 9, 10, whose entire record is the commit messages `5e4a0cc5`, `067caac0`, `0c7b968d`; V15 round 5's document; and `V16_GRADE.md` covering rounds 1–4 which predate the rule — return **zero recogniser hits each**, so their silence is measured rather than assumed. **THE BOUND, which is the part that matters and is not a hedge:** R-VALUE closes on rounds that *"return only findings that would not change an external reader's belief"* — a condition on what a round's FINDINGS DO, not on what the round SAYS. A round whose findings were in fact belief-neutral and which never discussed neutrality satisfies R-VALUE and is **invisible to any declaration sweep, however good its recognition control**. Twenty hits sit in CANNOT_TELL and seven rounds carry no neutrality language at all; **none of them may be read as neutral, and none as not-neutral.** So the defensible statement is: *no round ASSERTED neutrality, at recognition strength* — strictly stronger than the token-family floor Cell B stated, strictly weaker than *"R-VALUE's condition has never been met"*, which no text instrument can establish. | Measured 2026-08-16 at HEAD `22e32c03` by an agent that authored no round document and no earlier R-VALUE probe. **The instrument broke four times before it was believed, and every break is recorded in its docstring rather than erased.** (1) The recogniser found **11 of 12** frozen forms; the miss, *"No external belief moves on any of this"*, was NEG…BELIEF…MOVE where every pattern required NEG…MOVE…BELIEF — the V7 hyphen in a new shape. (2) A negated declaration was wrongly placed in the NEGATIVE control set, which correctly returned **BROKEN** until corrected: the recogniser does not discriminate polarity by design, the classifier does. (3) `classify()` could not return ASSERT at all in the first version, so the headline zero was **true by construction** — the vacuous-assertion defect, caught by adding a reachability control on the ASSERT branch that the run now prints (**11 of 12** frozen forms classify ASSERT standing alone). (4) Sharpest: with MENTION tested before scope, the sentence *"this round returned only findings that would not change an external reader's belief"* — a round applying R-VALUE's own words to ITSELF, the strongest possible assertion — classified **MENTION**, the use/mention error running the direction that would hide exactly the round being hunted. Scope now beats rule-quotation. **Cannot see:** any form outside the 12-paraphrase control; the scope-less form 12 classifies CANNOT_TELL standing alone and is reported, not patched around; the sweep is LINE-WISE, so a declaration split across a line wrap can be missed — one was, and only the fix for it revealed the wrap sensitivity. | **Nothing here changes a rung and this row rules on nothing.** What it settles: the wording of the lab's own headline. `LADDER_V_D227_ENTITLEMENT_2026-08-16.md` §1 and `LADDER_V_STATE_2026-08-16.md` §5 should say **"no round has ever ASSERTED belief-neutrality, measured at recognition strength over declarations"** rather than *"R-VALUE's condition has never been met"* — the first is now measured, the second is not establishable by any instrument of this kind and never was. **What a grader must decide, and it is not mine:** whether any of the seven CANNOT_TELL rounds in fact returned only belief-neutral findings. That is a re-grade of those rounds' findings against an external reader, not a text search, and it is the only route by which `R-VALUE = 0` could still be wrong. V16 rounds 9 and 10 are the natural first candidates because the "round 10 was neutral" claim has circulated twice; a reader of their commit messages will note round 10 moved a published figure from 49% to 71%, which bears on the question, but calling that belief-changing is the grading judgement this row declines to make. | **chief** to route the seven CANNOT_TELL rounds to a non-author grader; the owners of `LADDER_V_STATE_2026-08-16.md` and `LADDER_V_D227_ENTITLEMENT_2026-08-16.md` for the wording correction |
| D293 | **THE V12 RESIDUAL WAS ONE OF EIGHT LIVE INSTANCES OF ITS CLASS, AND THE PAGE THAT ALREADY "FIXED" IT STRUCK THE ORDINAL SENTENCE AND LEFT THE TIE SENTENCE THREE TIMES OVER.** V12 was graded FAIL on campaign/LADDER_V_PASS3_COLD_2026-08-11.md:339, unstruck inside W1's answer: *"0.0325 to 0.0353, the nominal best-on-board tie gone."* **Derived rather than remembered, by parsing LIVE_BOARD out of sdk/scripts/probability_of_rank.py:** round 4's 0.0325 on AR_14_Ret_180 was TIED BEST on the four-entry pin and stands **2 of 7** on the six-entry live board, behind Yang's 0.0250 -- **so there was no best-on-board tie to give up**; round 5's 0.0353 stands 4 of 7. The same derivation yields the whole stale class rather than the one site: best-on-board **round 4 pin 5 of 8 goes to live 2 of 8** (three cases flip: both alpha_15 and AR_14) and **round 5 pin 4 of 8 goes to live 2 of 8** (two flip), while overall round 4 moves from 3 of 5 to 5 of 7 and round 5 holds rank 1 on both. **THE DIAGNOSIS THE GRADE MADE IS CONFIRMED AND IS WORSE THAN IT LOOKED.** The 2026-08-15 repair on demo-output/website/closure.html struck exactly one sentence -- the ordinal and the count -- and left the *tie* claim live and unstruck in **three separate places** on the same public page: the round-5 ledger row, the rule-freeze section, and the warning note whose very next clause carries the strike. A repair scoped to a LIST fixed the sentence it was handed and left the class standing on the surface that travels. | Swept 2026-08-16 at frame 22e32c03 over **20,748 tracked files**, whole files never ranges, strike spans blanked first, patterns built with \s+ so a wrapped claim cannot hide. **The control is RECOGNITION and was scored by scripts/control_kind.py rather than self-declared**: five mutually independent paraphrases in the corpus's own vocabulary, planted BY LINE INDEX into campaign/BOARD_RESCORE_2026-08-14.md, **read back before the run** and all five found -- including the inflected pair the module exists for, *"the tie for best on the board is lost"* against *"the nominal best-on-board tie gone"*, which share no 5-gram. Two negative forms, both correctly rejected. The seed file was restored inside a finally block and is byte-identical to HEAD with every planted form absent. **139 hits, then triaged by stance with scripts/use_mention_discriminator.py so a grading record QUOTING the stale claim is not counted as making it: 7 ASSERT, 76 MENTION, 56 CANNOT_TELL -- and CANNOT_TELL was NOT read as ASSERT.** All 7 ASSERTs were opened by hand and **none is itself a stale live claim**: five are correct live figures or grading records, one is a planted control literal inside another grade, and one is a sweep record quoting a site. The live sites were found by reading the travelling surfaces directly, which is why the stance triage is reported as a filter on volume and not as the finding. | **REPAIRED, four sites, additive dated board identifiers in the form the corpus already uses, no strike marker added anywhere** (a balanced pair added to a document carrying a dangling opener blanks unrelated text, which cost a governance file earlier tonight; render audited before and after on all three files, unchanged and balanced): campaign/LADDER_V_PASS3_COLD_2026-08-11.md **:339** (the residual of record) and **:265** (which asserted the 4-of-8 count *"verified directly against the published board, and it is correct"* -- correct against the pin, 2 of 8 live, and zero of 8 belonging to our own model); demo-output/website/ACTIVE_RESEARCH.md **:680**, *"Best-on-board 5 of 8, unchanged"* with no board named, in a file whose top already carried the correction -- the same list-not-class shape; and closure_challenge_submission_round5/DESCRIPTION_DOCUMENT.md **:164**, which sits **one section above its own repaired sibling at :183**, the exact pattern the dispatch predicted. **DELIBERATELY LEFT, so the grader knows the boundary. Held or forbidden, and each is a LIVE fault that still needs an owner:** demo-output/website/closure.html **:190, :290, :350** -- three live unstruck instances of the tie claim on the public page, peer-held; demo-output/website/latex/closure_challenge_report.tex **:866, :2207** -- pin counts, never-touch; docs/PRODUCT_LIST.md -- peer-held. **Left because already correct:** CLOSURE_CHALLENGE_STATUS.md, which carries the strike and the dated six-entry reading at :587, :613 and :774. **Left because they are mentions, not claims:** every grading record, docket row, test fixture and control literal among the 139. **I MAY NOT GRADE V12 AND HAVE NOT.** | **fleet, a non-author** (V12's re-grade); **closure.html's owner** (three live travelling faults, the largest remaining exposure of this class); the latex owner; PRODUCT_LIST's owner |
| D294 | **A CORRECTION THAT REPAIRS THE ORDINAL AND THE COUNT BUT NOT THE CHARACTERISATION LEAVES A FALSE SENTENCE STANDING, and the adjacency test that clears it cannot see that.** Found while re-grading V10 at `f4f05b0f`. `demo-output/website/closure.html:350-351` says *"Round 4 held a nominal 0.00003-level tie for best-on-board on the aspect-ratio-14 duct"*. Five lines below, inside the same block, the page strikes the sentence that followed it -- *"the case falls to 3rd of 5, and our best-on-board count drops 5 of 8 to 4 of 8"* -- and supplies the six-entry board by entrant count and retrieval date with **4th of 7**, **2 of 8** and **zero of 8 ours**. So the block PASSES the test V10's criterion actually applies (*"the same caveats"*, i.e. does the claim travel with its board identifier) and it was ruled NOT a blocker on that ground. **But the characterisation itself is still false and still unstruck:** on the six-entry board round 4's `0.0325` stood **2 of 7**, tied with Reissmann for SECOND behind Yang's `0.0250`, so it was never a best-on-board tie, and nothing in the correction says so -- the correction fixes the ORDINAL and the COUNT, which are the things that carry numbers. **This is the third appearance of one shape in one day:** `c2cd83bf`'s sweep missed the clause because a stale characterisation carries no stale number (`f7170481`); a peer found the same class live in eight places at `1fc1f639`; and here the repair itself stopped at the numeric half of its own sentence. | Re-derived at `f4f05b0f`, `ast.literal_eval` on `LIVE_BOARD` (parsed, never imported) joined to `round5_per_case_full`: AR_14 round 4 `0.0325` = Reissmann's `0.0325`, board minimum Yang `0.0250`, so **2 of 7 and not best**; round 5 `0.035339` is **4 of 7**. A whole-file recognition sweep over the five V10 surfaces, every pattern joined on `\s+`, returns **six live instances** of the tie characterisation: four carry a six-entry board identifier within twelve lines (`closure.html:351`, `ACTIVE_RESEARCH.md:37`, `DESCRIPTION_DOCUMENT.md:166` and `:185`) and two carry none (`closure.html:191`, `:290` -- the rung's blockers, graded FAIL). Controls were RECOGNITION by `scripts/control_kind.py`'s own classification, 6 independent forms planted by line index and read back, 2 struck negatives rejected; over-blanking reconciled at 188 raw / 143 live / 45 suppressed / **0 unexplained**. | Repair `closure.html:351`'s tie sentence in the same pass as the two blockers -- strike `best-on-board` or say plainly that the tie was for second -- and do NOT treat the adjacency of a board identifier as evidence the sentence above it is true. **The transferable half, which is why this is filed rather than fixed inline:** a caveat-adjacency test grades whether a claim TRAVELS WITH its board, and is silent on whether the claim is TRUE of that board. Both are needed, and only the first is mechanical today. Worth one pass over every surface where a numeric correction was applied to a sentence that also characterises a standing in words. | owner of `demo-output/website/closure.html` (with the two blockers); **chief** for whether the caveat-adjacency test should be paired with a truth check before any future rung leans on it |
| D295 | **The five highest-value mutations on the board-claim rules were all KILLED, so the tests-that-cannot-fail sweep closes at its measured floor with no finding on the rules -- and the frame it took three attempts to build is worth more than the negative result.** The rules that decide board claims were mutated one token at a time and every one was caught: **best-on-board rule disabled (KILLED, 3 arms), `comparable` bound-vs-margin threshold INVERTED (KILLED, 3), board denominator losing its `+1` for us (KILLED, 15), our-placement `or` weakened to `and` (KILLED, 11), wall best-on-board disclosure disabled (KILLED, 1)**. **START CONTROL 205 passed / 0 failed and END CONTROL 205 passed / 0 failed**, so the suite did not drift under the run and the cells stand. **Every killing arm was recorded and checked against the test files present at HEAD; none was foreign.** This confirms the regularity D256 measured rather than weakening it: **this suite pins what its checks DECIDE and not what they SAY about it** -- all three D256 findings sat in a declared sentence (a blind-spot line, a disclosure, a stated exit contract) and every mutation to a verdict or a rule has now been killed, one of them by 23 failures and one by 15. | Mutation, 2026-08-16T18:3xZ-20:0xZ, anchored at `e1ede44a`, seven full-suite runs over `sdk/tests/test_rank_claim_values.py` + `test_rank_claim_surfaces.py` (~13.3 min each). **AN EARLIER RUN OF THESE SAME FIVE CELLS WAS DISCARDED, NOT REPORTED**, and the reason is the method: its frame was a `git worktree`, which is **ENUMERABLE** -- it appears in the shared `git worktree list`, a peer iterating worktrees had written into another agent's frame mid-sweep, and a point-in-time isolation check CANNOT prove absence across a run. Contamination ADDS tests, so it biases toward FALSE KILLS as much as false survivors, and that run's harness recorded totals rather than killing arms and so could not have detected either. **The replacement frame took three attempts and each failure is a fact about this tree.** (1) `git archive` alone drops `.git`, so `git ls-files` fails, every corpus-sweeping check returns UNKNOWN, and the control came back **193 passed / 12 failed**. (2) `git init && git add -A` re-applies `.gitignore` to files git actually TRACKS, giving **13,802 tracked against the real 20,727** -- an unfaithful corpus and therefore unfaithful fault counts. (3) Only force-adding the real tracked set (`git ls-files -z` from the live repo, `xargs -0 git add -f`) reproduced **20,727 = 20,727**, with `dist/` matching and the shipping archive present on disk but untracked, exactly as in the live tree. That frame is invisible to `git worktree list`. Its control came back **205 passed / 0 failed, identical to the worktree control**, which is what licenses comparing the two runs at all. | **NO REPAIR IS OWED HERE and that is the finding**; the sweep is CLOSED at its floor. **The floor is 3 of 86 test files and 18 mutations, and 86 is a file count that licenses nothing** -- the guard census before this one was misread as a population once and the correction cost real time. **What it would take to raise it, measured rather than estimated:** the six remaining claim-pinning files (`test_absolute_claims.py`, `test_docket_surface_agreement.py`, `test_fault_message_matches_rule.py`, `test_form_or_value_and_empty_selection.py`, `test_two_board_referents.py`, `test_summary_consistency.py`) are ~30 cells on cheap suites, **1.5-3 h**, and at the measured rate of 3 findings per 13 mutations would be expected to yield ~5-7. The other ~77 files are ~300+ cells at **20+ h** for a mechanism that needs a declared SENTENCE to hide in and would be expected to yield far less. **Two standing hazards were re-confirmed by execution during this round rather than by argument:** a `finally` does not survive a kill -- a session drop left a live mutant in `scripts/self_audit.py` until the process was reaped -- so the harness now asserts its subject equals HEAD BEFORE each cell; and a second agent's abandoned mutant (`if traces:` -> `if True:` in `scripts/check_proposal_surface_coverage.py`) was found in an idle worktree with no process, 57 minutes stale. | the fleet, for the six remaining claim-pinning files if anyone wants the floor raised |
| D296 | **THE EIGHT SILENT ROUNDS WERE RE-GRADED ON THEIR FINDINGS AND NOT ONE WAS BELIEF-NEUTRAL, SO `R-VALUE = 0` NOW RESTS ON A SECOND LEG THAT IS NOT A TEXT SEARCH — AND THE `"ROUND 10 WAS SCORED NEUTRAL"` PREMISE THAT HAS CIRCULATED TWICE HAS NO SOURCE ANYWHERE IN THE CORPUS.** D292 established at recognition strength that no round ever ASSERTED neutrality, and stated correctly that this answers a narrower question than R-VALUE asks, since R-VALUE's condition is on what a round's FINDINGS DO. The residue it routed — eight rounds in five places carrying no neutrality language: `V16_GRADE.md` §§1–7/8/9/10 for V16 rounds 1–4, the document-less V16 rounds 8, 9 and 10 (`5e4a0cc5`, `067caac0`, `0c7b968d`), and V15 round 5 (`1b0433eb`) — was re-graded from each round's own commits and artifacts rather than from any later summary. **Every one of the eight returned at least one belief-changing finding, so no consecutive pair exists and NO RUNG CLOSES.** **THE HARD CASE IS RULED RATHER THAN ROUTED AGAIN: V16 round 10's 49% → 71% move is NOT belief-neutral.** Re-derived by `ast.literal_eval` over `_PLACE_PRECISION` and `_PLACE_ADMISSION` in `scripts/self_audit.py` at `9cdb1851` and by an exact hypergeometric test written for the grade, reproducing D49's three p-values to the digit: the move is **20 of 41 (48.78%) → 20 of 28 (71.43%) = 22.65 points**, numerator unmoved at 20, and the published spread went from **27.2 points at p = 0.040** to **4.6 points at p = 0.763**. It is the rung's OWN CLOSING FIGURE — V16 closes on the guard stating what it costs — it moved in the direction that makes the instrument look worse, and it travelled with three further belief-changing changes: a fifth absolute struck out of the reader-facing verdict, the builder effect declared CONFOUNDED with the admission rule across a 5-to-24 point range, and both set sizes reaching a reader for the first time. **THE COUNTER-ARGUMENT IS STATED AT ITS STRONGEST AND REJECTED:** the numerator did not move and all thirteen dropped sentences were true negatives, so the guard's behaviour was byte-identical across the round — but R-VALUE asks what a READER would believe, not what the CODE would do, and the round's own record calls its choice *"the less flattering"* one, which is a statement that a belief was expected to get worse. **The three other rounds a closure could have hidden in are equally clear:** round 9's entire tracked footprint is one docket row (D49) and that row falsified an absolute sitting in the generated verdict AND showed the p = 0.040 a chief ruling at `02e941c4` cited to be the artefact of the one pairing of four that is not internally consistent; round 8 withdrew the rung's fourth absolute and gave two false-FAULT shapes (`other-named-board`, `negated-or-questioned`, 2 of 2 apiece) their first count and name; V16 rounds 1–4 returned respectively an invisible wrapped placement in the report's `.tex`, a published recall improvement of `11% → 70%` measured as `11% → 47%` on one held set, a documented-as-`NEVER raises` function that raises, and a guard defect that returns PASS while reporting *"all 0 placement expression(s) agree with the published board"*. | Re-graded 2026-08-16 at HEAD `9cdb1851` by an agent that wrote no round document, no rung, no grade and no earlier R-VALUE probe; recorded in `demo-output/website/campaign/LADDER_V_RVALUE_SILENT_ROUNDS_REGRADE_2026-08-16.md`. **THE `"ROUND 10 WAS SCORED NEUTRAL"` PREMISE, MEASURED.** Both circulating sites (`LADDER_V_V16_ROUND11.md:712`, `LADDER_V_V15_ROUND8.md:619`) are the second clause of a sentence beginning *"Do not score neutrality early"*, and both classify **CANNOT_TELL** under `scripts/use_mention_discriminator.py` with the claim span passed explicitly; the chief's quotation of the claim classifies **MENTION** on the positional test. **None is ASSERT, and CANNOT_TELL was not read as ASSERT.** A third-person sweep — does any record say OF a round, by number, that it was neutral or found nothing — was then run over **2,120 units**: every tracked ladder/grade/docket/lessons/product document read from `git show HEAD:` blobs rather than a possibly-dirty worktree, plus every commit message from `git log --all`. **The control is RECOGNITION and was classified as such by `scripts/control_kind.py` rather than self-declared:** six mutually independent paraphrases in a lab record's own vocabulary, frozen and hashed (sha256 `c8243490…`) before the corpus was swept, planted BY LINE INDEX into the real HEAD bytes of `LADDER_V_V16_ROUND11.md` and all six read back through the sweep's own reader, with two negative forms correctly rejected. **Reported honestly as NOT a zero:** the sweep returned 2 provisional hits, so `control_kind.verdict_for(2)` returned `NOT_A_ZERO` and no zero-strength claim is drawn from it; both hits were opened by hand and are false positives — one the chief's quotation of the claim in order to refute it, one a commit subject about ledger bookkeeping. **No record anywhere scores round 10, or any round, neutral.** **Cannot see:** any paraphrase outside the six-form control; the sweep is sentence-wise over a splitter that keeps a warning clause attached to its factual clause, which is why both warning sites were also read by hand. | **NOTHING HERE CLOSES, PROMOTES OR RE-OPENS A RUNG, and the grade says so on its face.** What it settles is one sentence of the lab's own headline: `R-VALUE = 0` may now be stated on two independent legs — *no round ASSERTED neutrality* (D292, over declarations) and *no silent round WAS neutral* (this row, over findings, decided at every one of the eight rounds individually rather than only at the pairing). **What must NOT be inferred from it:** that a grading judgement is a measurement. §1.2 and §5 of the grade name their grounds so they can be attacked, and the falsifiers are printed there — the sharpest being that **§5 falls if the chief rules that R-VALUE's *"external reader"* means a reader outside the LAB rather than outside the ROUND**, a reading under which V15 round 5's *"Nothing that travels is affected"* would make it neutral. That reading is rejected in the grade as self-defeating: taken narrowly, nearly every finding in this ladder's history lands on a lab record, and R-VALUE would have closed every rung at round two — and V16 rounds 11 and 12 both self-declared NOT neutral on findings that were entirely lab records, which is the ladder's own practice contradicting the narrow reading. **The correction owed elsewhere:** the *"round 10 was scored neutral"* clause should not be repeated as a fact in any future record; the advice it introduces is sound and the premise is unsourced. | **chief** (whether *"external reader"* means outside the round or outside the lab — the one ruling that could overturn §5, and it is larger than this rung); the owners of `LADDER_V_STATE_2026-08-16.md` and `LADDER_V_D227_ENTITLEMENT_2026-08-16.md` (D292's wording correction, now with a second leg to cite) |
| D297 | **FILED, NOT APPENDED TO V16 (outside the rung's declared closing condition). `LESSONS.md` L-82 CARRIES THREE STATEMENTS THAT V16 ROUNDS 9 AND 10 FALSIFIED, IN THE PRESENT TENSE, WITH NO SUPERSESSION MARKER — AND THE LESSON THAT WENT STALE IS THE LESSON ABOUT FIGURES GOING STALE.** Found while re-grading the eight silent R-VALUE rounds. L-82 (*"A held-out set built by the party being measured understates the cost"*) states as current: (1) *"the guard **falsely faults 20 of 41 (49%)**"*, restated a second time in the same lesson as *"The author's 49% is not a property of the guard"*; (2) *"measured **19 of 25 (76%)** under the author's own admission rule, **unchanged**"*; and (3) *"Fisher exact two-sided **p = 0.040**"*. **All three were superseded on 2026-08-11–12.** Re-derived at `9cdb1851` by `ast.literal_eval` over the `_PLACE_PRECISION` and `_PLACE_ADMISSION` assignment nodes in `scripts/self_audit.py` — parsed, never imported — and by an exact hypergeometric two-sided test written for the re-grade: the guard publishes **20 of 28 (71.4%)** for that row, not 20 of 41 (48.8%); the `unchanged` clause of (2) is precisely what D49 falsified and what `0c7b968d` struck-and-kept inside `demo-output/website/campaign/V16_PRECISION_SET.py` (the grader's set admitted on `_placements`, the author's on a raw rule-A match); and (3)'s `p = 0.040` belongs to the ONE pairing of four that is not internally consistent — under the rule in use at `9cdb1851` the same counts give **4.6 points at p = 0.763**, and under the consistent looser rule **24.3 points at p = 0.075**. **WHY THIS IS MORE THAN STALENESS.** L-82's rule — *any figure a lab publishes about its own cost carries the provenance of the sample it was measured on* — is DERIVED from the 27-point spread between the two rows, and that spread is 4.6 points under the admission rule the lab now uses. The conclusion survives on other grounds (76% still exceeds the author's rate under every common rule, which is what keeps the *"worst case on hard sentences"* strike standing); the arithmetic under it does not. **This is L-82's own lesson happening to L-82.** | Measured 2026-08-16 at HEAD `9cdb1851`, non-author, during the D292 re-grade recorded in `demo-output/website/campaign/LADDER_V_RVALUE_SILENT_ROUNDS_REGRADE_2026-08-16.md`. **Checked against the docket by ID and by phrase before filing, which is the discipline that keeps a re-finding from being counted as a finding:** D27 quotes `20 of 41 (49%)` and `p = 0.040` too, but D27 is a DATED FINDING RECORD of V16 grade round 7 and carries its own `EXECUTED` marker, so it is history that recomputes correctly for its own date and is NOT this defect; D49 records the falsification itself and carries `EXECUTED AND CLOSED … by V16 round 10`. **Nothing on record covers `LESSONS.md`.** The shape is the one V15 round 5 (`1b0433eb`) filed nine times and named exactly: *"the correction propagated backwards — the prose was right when written and the code moved under it … this is not a copy-paste failure; it is that the copies had no way to learn."* | **Owner's call, and the cheap settlement is additive rather than destructive**, in the treatment the corpus already uses elsewhere: leave L-82's narrative bytes intact and append a dated supersession note giving the three live values with their commit anchor, rather than overwriting figures inside a lesson whose subject is what happens when figures are overwritten. **Do NOT strike the lesson**: its conclusion is not falsified, only its arithmetic is, and L-76 strikes falsified claims rather than superseded ones. **The transferable half, and it is why this is filed rather than fixed inline:** `LESSONS.md` is a durable derivation surface, and no instrument in this lab watches for a figure QUOTED INSIDE A DERIVATION going stale against the generator it was quoted from. V15 round 5's proposed remedy — a check that a prose surface contains the string the generator currently prints — reaches the rung page and the product record and does not reach a lesson that reasons from the number rather than reporting it. | owner of `LESSONS.md` (the supersession note); **fleet** (the generator-versus-derivation staleness check, which is the general form and is nobody's today) |
| D298 | ~~**DUPLICATE — this row is a verbatim re-append of D278 and carries no finding of its own.**~~ **Struck in place rather than deleted or renumbered, the register being append-only. Cause and remedy: see the triplication row appended below.**  **D264 ADJUDICATED: the placement guard's live cost is a PRECISION REGRESSION and not a reach increase — 24 new faults, ZERO of them true — and the two sentences that are genuine assertions are both TRUE of the board they name.** The guard did not move: **not one of the 68 names in `check_board_placement_words`' callee closure changed across `9dca4738..faa02f80`**, nor any of `board_placement_faults`' 30, so one fixed instrument was run over two corpora. **29 → 53 faults, 24 new, 0 gone** — and it is 53 rather than the 48 first reported because **five of the additions are the grading round's own paperwork**, three from `LADDER_V_V16_ROUND12.md` and two from the docket rows it filed. Adjudicated against a rubric fixed before looking (TRUE = asserts, in this lab's voice, an ordinal wrong about the board the sentence refers to): **0 TRUE, 24 FALSE.** Mechanically, each fault's `(entrant, claimed ordinal)` tested against both boards: **13 of 24 are CORRECT on the live six-entrant board, 0 of 24 are correct on the pin**, 2 are rule B on a quoted `margin over the leader`. **22 of the 24 are META** — probe rows, rows literally labelled `**CONTROL**`, quoted fault messages, struck markup, and docket rows quoting those — which **chief ruling D255 has already ruled are not claims at all**. **The remaining 2 are the only assertions in the set and both are true**: `LADDER_V_V10_CLOSURE_2026-08-16.md:92` and the ledger's V10 narrative read *"the live duct leader was Yang at 0.0291 / 0.0311 / 0.0250; Reissmann placed 2nd, 2nd and 2nd **of seven**"*, re-derived here from `LIVE_BOARD` joined to `round5_per_case_full` as Yang first and Reissmann second on all three duct cases. **This lands on V16's own gate**, whose criterion makes a false-positive rate against a measured corpus a PASS REQUIREMENT. | Executed at `faa02f80` in two detached worktrees, `__pycache__` purged, faults diffed BY IDENTITY rather than by file count. **The precision pair was re-measured at the same frame and did NOT move: author 20 of 28 (71.4%), grader 19 of 25 (76.0%), controls 0 of 5 and 0 of 4 missed** — reported as a pair with its frame because a single figure hides both the spread and the two builders. **That non-movement is the finding, not a null result:** the guard's PUBLISHED false-positive rate did not shift by a tenth of a point while its LIVE false-fault count grew by 24, because the published figure is measured over held-out SENTENCES and the cost is a property of SURFACE CLASSES. Round 11's F5 argued this; it now carries a number on both sides — `71.4% / 76.0%` unchanged against `29 → 53`. | **A disclosure that cannot move when the cost nearly doubles is not a disclosure of that cost.** The ask is NOT a tuning: see the sibling row for the two discriminators this needs. **Falsifiable** by producing, from the 24, one fault that asserts in this lab's voice an ordinal wrong about the board its sentence names; or by showing any of the 13 wrong on the live board; or by showing a closure name that changed across the range. **What must NOT be done, and V16's own history forbids it:** widening a window to make the 24 disappear would change what counts as evidence and would leave every one of them exactly as true or false as it was. | **chief** (whether a corpus-class false-fault count belongs beside the sentence-level rate on V16's face, which is the gate this moves); **fleet** (nobody may tune the guard to this number) |
| D299 | ~~**DUPLICATE — this row is a verbatim re-append of D279 and carries no finding of its own.**~~ **Struck in place rather than deleted or renumbered, the register being append-only. Cause and remedy: see the triplication row appended below.**  **THE GUARD NEEDS TWO DISCRIMINATORS, NOT ONE, AND ONLY THE FIRST IS THE GAP A PEER ALREADY NAMED — measured, both are TOTAL.** Driven one sentence at a time at `faa02f80`. **GAP-Q, use versus mention:** a bare assertion, the same sentence inside double quotes, introduced as a quotation, labelled a PROBE, labelled a CONTROL, negated, and struck with markdown **all return exactly 1 fault**; only an explicitly corrected one clears, and that is `_PLACE_ADJUDICATED`, a 400-character proximity window rather than grammar. **GAP-B, the board identifier the sentence carries:** `Reissmann placed 2nd`, `... of seven` (TRUE), `... of five`, `... of four`, `Reissmann is rank 2 on the live board` and `... on the published board` **all return exactly 1 fault** — the denominator and the board name are read by nothing. **GAP-Q IS the same gap named at D257** (*"the instrument cannot distinguish a surface ASSERTING the untrained claim from one QUOTING it in order to grade it"*, 27 of 39 quoted) **and measured at scale at D245** (1,501 of 1,884 board-claim sites meta, 383 assertions) **and already RULED at D255** (*"Quoting a claim in order to strike, grade or refute it is not making the claim"* — an identity, not a policy). **So the discriminator is not merely worth building: the ruling requiring it exists, the population it governs is measured, and V16's rule A implements none of it.** One discriminator closes 22 of this round's 24 faults, D257's 39-site uncertainty and D245's 1,501 meta sites — three problems, not two. **GAP-B is a DIFFERENT gap and a meta discriminator will not touch it**: the two true assertions are not quotations, they are the lab stating a derived fact correctly about a board they name by entrant count, so D255's identity does not exempt them because they ARE claims. | Measured by driving `board_placement_faults` directly at `faa02f80` on single sentences, with ordinal words assembled at runtime so this row does not itself become a corpus hit (D4). The convergence with D245/D255/D257 was established by reading those rows at `git show HEAD:docs/DOCKET.md`, not from memory. | **GAP-B's remedy is already written down elsewhere and is not a new invention**: the submission draft's binding triple (margin, entrant by name, board by count and date) and chief ruling `377d6afb` (*the referent is fixed by the CLAIM, not by the rung*) both say a claim naming its board is graded against that board. **Reading `of seven` is executing that ruling; re-pointing `_published_board` is not, and remains declined** — ruling `04489465` declined it and round 12 measured the cost at 40 faults after the dated-context mask rather than the ~18 its arithmetic implied. **Neither discriminator was built by this round and neither should be built to hit a number.** Falsifiable in one execution: show rule A returning a different verdict for `of seven` than for `of four`. | **chief** (GAP-B is a ruling already made and not implemented — whether V16 may be graded against a guard that does not execute `377d6afb`); **fleet / whoever owns D257** (GAP-Q is one build serving three registers, and that is the argument for its priority) |
| D300 | ~~**DUPLICATE — this row is a verbatim re-append of D280 and carries no finding of its own.**~~ **Struck in place rather than deleted or renumbered, the register being append-only. Cause and remedy: see the triplication row appended below.**  **D263 REPAIRED, and the FIRST proof of its neutrality was worthless in a way that would have passed for a proof.** The one-token repair at `scripts/self_audit.py:7445` — `dict(ranking[0])` → `dict(ranking[0] or {})` — makes the B1 branch reachable, because `_ranking_board()` reports failure by RETURNING `(None, message)` rather than raising, so the `except` above never fired and `dict(None)` raised `TypeError` one line above the branch written for exactly that case. **Verdict-neutrality measured in a REAL GIT TREE: status WARN → WARN, summary identical, and the FULL 53-FAULT SET identical, with the author precision row unmoved at 20 of 28 and the real-referent positive control unchanged; the B1 branch is reached in all three failure routes where it previously raised.** **THE DISCARDED FIRST PROOF IS THE REASON THIS ROW EXISTS.** It ran in a temp directory holding only the module, where `check_board_placement_words` degraded to *"could not enumerate tracked files (git unavailable)"* — so it compared **two broken verdicts**, reported `check verdict+summary identical in all 4 configs : True`, and that TRUE was worth nothing. A neutrality proof whose subject is disabled in both arms will always return neutral. It was caught by reading the summary string the harness printed rather than the boolean it computed. | Repaired 2026-08-16 at `faa02f80` under the chief's explicit conditional authorisation (*"if the fix is cheap and provably does not change any verdict, take it"*), after verifying `scripts/self_audit.py` was byte-clean against HEAD, unstaged, and held by no peer — the shared-index entries suggesting otherwise were confirmed STALE, the index blob for `test_blind_spots_are_printed.py` (`741de9d6`) predating HEAD's (`f6a9c9ad`) while the worktree matched HEAD exactly. Plant by LINE INDEX confirmed by readback; the throwaway worktree was restored and byte-verified afterwards, and an earlier run of the same harness that was killed mid-measurement left that worktree patched until it was restored and re-verified — recorded because a `finally` does not run when the process is killed. | **Nothing further owed on D263.** The generalisable half is the discarded proof: **a neutrality or regression harness must assert that its subject is LIVE in both arms before it compares them** — the same "a zero without a live control is not evidence" rule, applied to a comparison rather than to a sweep, where the failure mode is that BOTH arms are dead and the diff is therefore empty and reassuring. | **fleet** (the live-in-both-arms assertion, which belongs in any mutation or neutrality harness) |
| D301 | **L-82's REPAIR IS ROUTED AND THE CALL IS MADE PLAINLY: RESTATE IT, DO NOT STRIKE IT AND DO NOT WITHDRAW IT — AND THE RESTATEMENT OWES A SECOND VARIABLE THE LESSON NEVER NAMED, WHICH ITS OWN WORKED EXAMPLE DEMONSTRATES.** D297 recorded that `LESSONS.md` L-82 carries three statements V16 rounds 9 and 10 falsified, in the present tense with no supersession marker. **THE ROUTING CALL, since a row that names a defect and not its disposition leaves the owner to re-derive the judgement.** **(1) NOT WITHDRAWN.** L-82's conclusion — *a held-out set built by the party being measured understates the cost, and the remedy is a second set with a different author, reported as its own row* — is **not falsified**. Re-derived at `678cb7e8` by `ast.literal_eval` over `_PLACE_ADMISSION`: the grader's rate exceeds the author's under **BOTH** admission rules in the table (71.4% vs 76.0% under `_placements`; 48.8% vs 73.1% under raw rule-A match), which is exactly the property `test_the_precision_spread_is_measured_under_both_rules` was built to pin and the reason the *"worst case on hard sentences"* strike still stands. A lesson whose conclusion survives its arithmetic is not withdrawn. **(2) NOT STRUCK.** L-76 strikes claims execution has **falsified**; these three are **superseded**, which is a lesser and different defect — the same distinction D58 drew for an unsourced date, where striking would have asserted false something merely unevidenced. Striking L-82 would assert its conclusion false, and its conclusion is true. **(3) RESTATED, and the restatement is bigger than three numbers.** L-82's rule reads *"any figure a lab publishes about its own cost carries the provenance of the SAMPLE it was measured on — who built it, and whether they could see the thing being measured."* **D49 established a SECOND variable of comparable size that the lesson does not name: the ADMISSION PREDICATE.** The builder effect and the scoring rule are **confounded**, and the admission rule alone moves the spread across a **5-to-24 point range** — so the lesson's promise that *"the distrust is quantifiable for the price of one more sample"* is **incomplete as written**: a second sample scored under a different predicate does not quantify the builder effect, it mixes it with a second one. **L-82's own worked example is the counter-example to L-82's own remedy**, and that is the part worth writing down rather than the corrected digits. | Measured 2026-08-16 at `678cb7e8` by the non-author grader of the eight silent R-VALUE rounds, recorded in `demo-output/website/campaign/LADDER_V_RVALUE_SILENT_ROUNDS_REGRADE_2026-08-16.md` §7. **The three superseded statements, each re-derived rather than quoted:** *"falsely faults 20 of 41 (49%)"* against `_PLACE_PRECISION`'s **20 of 28 (71.4%)** at `678cb7e8`, restated a second time in the same lesson as *"The author's 49% is not a property of the guard"*; *"19 of 25 (76%) under the author's own admission rule, **unchanged**"*, whose `unchanged` clause is precisely what D49 falsified and what `0c7b968d` struck-and-kept inside `campaign/V16_PRECISION_SET.py`; and *"Fisher exact two-sided p = 0.040"*, reproduced here by an exact hypergeometric two-sided test written for the grade, which returns **p = 0.040 only for the one pairing of four that is not internally consistent** and **p = 0.763** under the rule in use at `678cb7e8`. **Checked against the docket by ID and by phrase before filing, which is what keeps a re-finding from being counted as a finding:** D27 quotes `20 of 41 (49%)` and `p = 0.040` too, and D49 quotes both sides, but **both are DATED FINDING RECORDS that recompute correctly for their own dates and carry their own `EXECUTED` markers** — they are history, not this defect. Nothing on record covers `LESSONS.md`. | **Additive, not destructive, and the treatment is the one the corpus already uses.** Leave L-82's narrative bytes intact and append a dated supersession note carrying the three live values with their commit anchor, plus one sentence naming the admission predicate as a second confound — **overwriting figures inside a lesson whose subject is what happens when figures are overwritten would be the defect committing itself.** **THE TRANSFERABLE HALF, and it is why this is routed rather than fixed inline: no instrument in this lab watches a figure QUOTED INSIDE A DERIVATION for staleness against the generator it came from.** V15 round 5's proposed remedy — a check that a prose surface contains the string the generator currently prints — reaches the rung page and the product record, and does **not** reach a lesson that REASONS FROM a number rather than reporting it, because the reasoning paraphrases the figure instead of restating it. That gap is general, it is nobody's today, and `LESSONS.md` is the highest-value surface sitting in it: a durable derivation surface every future round is told to read. | **owner of `LESSONS.md`** (the supersession note and the second-variable sentence — the call is made above, so what is owed is execution and not a judgement); **fleet** (the generator-versus-derivation staleness check, which is the general form and has no owner) |
| D302 | **I APPENDED THE SAME THREE ROWS TO THE REGISTER THREE TIMES AND THEN CITED THE COPIES AS A DIFFERENT ROUND'S FINDINGS — the register carries D278-D280, D289-D291 and D298-D300 as three identical copies, and V16 round 13's and V12's real rows never landed at all.** **The mechanism.** The allocator `renumber.py` **reads and writes one hard-coded path**, `rows.md`. For V16 round 13 and again for the V12 grade I wrote the new rows to `rows2.md` and then ran the allocator, which silently re-read the STALE `rows.md` — still holding the round-12 addendum rows — renumbered those to the next free block and appended them. It printed `D278 -> D289`, which is exactly what a correct run looks like, and the compare-and-swap's ID-freedom assertion passed **because the IDs really were free**: the assertion checks that an ID is unused and never that the CONTENT is new. **THE CONSEQUENCE IS A FALSE CITATION IN A COMMITTED RECORD.** `campaign/LADDER_V_V16_ROUND13.md` §7.3 cites **D289-D291** as its three findings; those IDs hold round-12 content, so **round 13's document names three rows that are not the three that exist** — and the commit message for `b6974959` says the same. **That is the exact defect of `38844271`, which I QUOTED IN MY OWN ROUND 12 §0.4** as the failure my re-read discipline avoided. I avoided it in the direction I was watching (IDs taken by a peer) and walked into it from the direction I was not (my own stale input file). | Found 2026-08-16 at `7239eced` by the agent that caused it, `ae0ce79e959ec8ea0`, immediately after the third append, by reading back what had actually landed instead of trusting the allocator's printed mapping. Duplication measured over the whole register by grouping every `^\| D\d+ \|` row on the first 200 characters of its text: **3 duplicate groups, each of size 3** — `[D278, D289, D298]`, `[D279, D290, D299]`, `[D280, D291, D300]` — and no other duplicate group anywhere in the register. | **The six copies are NOT deleted and NOT renumbered**, the register being append-only; they are struck in place and each points at the original. The real rows for V16 round 13 and V12 are appended below with fresh IDs, and `LADDER_V_V16_ROUND13.md` §7.3 is corrected to cite them. **`renumber.py` is hardened**: it now takes its source file as an explicit argument and **aborts if any row's text already exists in HEAD's register**, which is the check that was missing — ID-freedom is not content-freedom. **The durable half: an append-only register's compare-and-swap can assert that an ID is FREE and still let the same row land three times, because the two properties are unrelated. Any allocator that renumbers must diff the CONTENT against HEAD, not just the keys.** |  **[SECOND SLIP INSIDE THE SAME REPAIR, 2026-08-16: the six rows below landed carrying literal `DAAA`..`DGGG` in the ID column, because the substitution pattern was `D[A-Z]{4}` and the placeholders were `D` plus THREE letters. `re.sub` does not report a miss -- it returns its input -- so the CAS reported success truthfully while every assertion it made held: the IDs it COMPUTED were free, the cells were 5, the content was new. It never checked that the IDs it computed were the IDs that reached the rows. Same shape as the row it sits in: an assertion that is true and does not cover the thing that went wrong. IDs assigned in a follow-up CAS.]** **fleet** (the content-diff check belongs in every docket allocator, not only mine); **chief** (whether the six struck copies should be left in place as history or handled some other way — I have left them) |
| D303 | **V16 ROUND 13 — V16's CENTRAL CLAUSE IS SATISFIED OR VOID, AND IT CANNOT FAIL THE RUNG EITHER WAY. (Round 13's real row; the copy at D289 is not this.)** C1 reads *"every ordinal this lab pins on an entrant is checked against **the published board, which is parsed from the benchmark's own README table**"*, and everything turns on *"an entrant"*. **Measured at `22e32c03`:** the published board is `{reissmann 1, wu 2, liu 3, montoya 4}` parsed from clone HEAD `deb91557`, verified equal, and **yang and tian are not on it**. Under ruling `bcad2bbc`: **reading (a)** *entrant = entrant ON the board C1 names* → Yang and Tian fall outside C1's scope, the four it covers are checked and both controls fault → **C1 SATISFIED**; **reading (b)** *entrant = any challenge entrant* → C1 requires checking an ordinal against a board **holding no rank for that entrant**, satisfiable only by re-pointing, which `04489465` DECLINED and V1 forbids → **the W-2 shape, VOID**. **Rounds 11 and 12 both scored C1 `FAILS`**, round 12 by this same agent. | Executed at `22e32c03` in a detached worktree with the subject asserted byte-equal to HEAD before every cell. **(i)** `_board_names(board)` builds from `sorted(board)` and `_placements` receives the same dict — **the recogniser vocabulary IS the oracle's key set**, so round 11's F1 ask to separate them is a code change that does not exist. **(ii)** `board.get('yang')` is `None`; ranks available are `[1,2,3,4]`. **(iii) THE PROOF, by extension with controls:** pin as shipped → Yang TRUE 0 / FALSE 0 / control 1; **+ yang at his LIVE rank → TRUE 0 / FALSE 1** (correct, but the rank came from the live board); **+ yang at an INVENTED rank → TRUE 1 / FALSE 1**, the guard faulting a **true** sentence. The only value that works is the forbidden one. | **A RULING QUESTION, HANDED OVER AND NOT TAKEN** — which reading governs is the chief's, as `377d6afb` fixed the referent question; this round patched nothing. **Falsifiable by ONE artefact:** a rank for Yang **derived from the README at `deb91557`**, which would make reading (b) satisfiable without re-pointing. **R-ISOLATE NOT SATISFIED:** a grader reversing its own prior verdict TOWARD a pass. | **chief** (the ruling on *"an entrant"*); **a NON-AUTHOR** (re-execution) |
| D304 | **V16 ROUND 13 — THE PRECISION AND REACH CLAUSES ARE DISCLOSURE REQUIREMENTS, THEY ARE SATISFIED, AND BOTH MISSING DISCRIMINATORS ARE OFF THE RUNG'S CRITICAL PATH. (Round 13's real row; the copy at D290 is not this.)** C4 reads *"the rung **fails if the check does not state** its false-positive rate against a measured corpus **and name the senses of the word it excludes**"* — it does **not** require the rate to be low. C5 requires the reach **declared** and **measured on held-out sentences**. **Both SATISFIED at `22e32c03`**, measured by calling `_place_precision_sentence()` and `_place_reach_sentence()` directly: the rate is stated as **two rows, `20 of 28 (71%)` and `19 of 25 (76%)`**; the corpus is named three times; the excluded sense is named verbatim (*"a **linear-algebra** subject whose head noun is not in `_PLACE_LINALG_NEAR` -- a kernel, a Gramian, a Laplacian, an array"*); the reach carries **four measured rows** each naming its builder and blindness. **CONSEQUENCE: GAP-Q and GAP-B bear on neither clause, so neither blocks V16.** **What DOES block it is one repairable falsehood inside the disclosure C4 requires:** `scripts/self_audit.py:4042` still emits *"takes rule-A faults **from 2 to 68** … **both current faults clear**"* while the same call's detail carried **34** (D265(b)). **That is NOT W-2** — deriving the figure is an ordinary permitted action. | Generators called directly at `22e32c03`, `__pycache__` purged, subject asserted byte-equal to HEAD; the `:4042` line read from `git show HEAD:` rather than the worktree. | **V16 CAN CLOSE**: it needs one figure derived rather than typed, and one ruling on C1. **D265(b) deliberately NOT repaired by that round** — the agent that filed it must not close it in the round where closing it clears the rung. | **a NON-AUTHOR of D265** (derive the figure); **chief** (whether a live typed falsehood inside a required disclosure is a C4 failure or its own) |
| D305 | **V16 ROUND 13 — GAP-B IS NOT CLOSABLE BY THE RULING EXPECTED TO CLOSE IT, AND THE COSTING SAYS DO NOT BUILD. (Round 13's real row; the copy at D291 is not this.)** D255(2) — *assertions on shipping surfaces require a retrieval timestamp* — is **R1 restricted to the travelling arm, 33 sites**, applied at `dc9d9364`. The placement guard's live verdict at `faa02f80` was *"**every travelling surface agrees with the board**; 53 lab record placement(s) do not"*, and **both** GAP-B faults are lab records. **The intersection of the ruling's scope with GAP-B's faults is ZERO.** The ruling would guarantee an identifier on shipping assertions, but the guard has **0 faults there**, so a predicate built to read it would have nothing to read. **GAP-B needs a PREDICATE as well as an identifier, and the identifier requirement stops at the arm where the guard is already clean.** | Read from the landed sweep at `faa02f80` (53 faults, 0 travelling) and from D255's own text at `git show HEAD:docs/DOCKET.md`; both GAP-B faults were adjudicated in D278 and re-derived TRUE of the live board. | **DO NOT BUILD**, which is the condition the chief set. **Return: 2 faults**, both already hand-adjudicated false positives, neither reachable by a reader outside this lab. **Cost:** separating recogniser from oracle (structurally absent), a predicate parsing an entrant count or timestamp, **plus its own held-out set and false-positive measurement, because C4 would then bind it too**. Falsifiable by producing one GAP-B fault on a **travelling** surface. | **chief** (whether the timestamp requirement should extend past the travelling arm); **fleet** (nobody builds GAP-B on this return) |
| D306 | **V12 GRADED `PASS` AT HEAD BY A NON-AUTHOR — and the `closure.html` instances defeat NEITHER clause, on three grounds that are a scope RULING rather than an inherited routing.** V12 asks **(i)** the three weakest points each with **the record's BEST answer** beside it — a quality clause, not met if a better answer sits in the same document (D227) — and **(ii)** that the product **becomes** Sanaa's standing briefing. **Both MET at `678cb7e8`.** The fourth residual the prior FAIL turned on, `LADDER_V_PASS3_COLD_2026-08-11.md:339`, is repaired and passes **both halves of D294's test**: it **travels with** its board (four-entry `deb91557` named on its face, retrieval `2026-08-11T23:33Z`, re-verification `2026-08-14T21:01Z`) and it is **true of** it — **re-derived here: on the four-entry board the minimum on `AR_14_Ret_180` was `0.0325` held by US AND Reissmann, a real best-on-board tie; on the six-entry board round 4's `0.0325` stood 2 of 7 behind Yang's `0.0250`, round 5's `0.0353` stands 4 of 7.** The sibling at `:265` re-derived as **4 of 8 four-entry / 2 of 8 six-entry**. **THE RULING: NEITHER CLAUSE. (1)** Clause (i)'s frame is the answer blocks; that page holds no weakest point and no answer, and D227 located R1/R2 INSIDE the answer blocks. **(2)** Clause (ii) is the product's own readiness; reading it to reach every claim-bearing surface makes V12 unclosable by any action available to its owner, who cannot edit that page — **a criterion may not require more than the maximum permitted action on the surface it grades (`bcad2bbc`), so the wide reading is VOID.** **(3)** R-CONVERGE forbids double-counting: those sites are in **V10's** scope and V10 was re-graded **FAIL** on them at `f4f05b0f`, so grading them here lets **one repair close two rungs**. | Graded at `678cb7e8` by `ae0ce79e959ec8ea0`, author of none of it — not the product, not the repair `1fc1f639` (`a3ca1abf`), not the prior FAIL `b4596cb4` (`aaf894dc`), not the V10 re-grade `f4f05b0f` (`adccb1ac`). Board re-derived by `ast.literal_eval` on `LIVE_BOARD`, never imported and never quoted. Whole-file sweep read from `HEAD:`, every pattern `\s+`, strike spans blanked in place. **CONTROLS FIRST: a RECOGNITION control of five MUTUALLY INDEPENDENT paraphrases planted BY LINE INDEX and read back fired 5 of 5; struck negative control gained 0; strike balance 34 markers, even.** All ten remaining live hits opened by hand: the skeptic's own quoted heading, corrections naming the old value to reject it, head-to-head counts against a named entrant (re-derived correct), and soft-wrap spans. Use-versus-mention returned MENTION 3 / no-span 9 and is reported **as a volume filter only**; **`CANNOT_TELL` is never `ASSERT`**. | **Nothing owed. The verdict is a plain `PASS`**, no residual label attached. **Falsifiable, cheapest first:** one live, unstruck, board-**unidentified** placement claim inside `:317-361`, `:362-620` or `:621-727`; or a demonstration that the four-entry board carried no tie at `0.0325`, which collapses the grade; or a better answer to W1/W2/W3 elsewhere in the document. **The scope ruling is a RULING and is the chief's to overturn.** | **chief** (only to overturn the scope ruling); otherwise **nobody — V12 is closed** |
| D307 | **A HOUSE-RULE RESIDUAL ONE STRIKE WIDE, FILED RATHER THAN APPENDED BECAUSE IT DEFEATS NEITHER V12 CLAUSE: the superseded reading is KEPT BUT NOT STRUCK, so the assertion slot holds the stale board and the current reading sits in a parenthesis.** `LADDER_V_PASS3_COLD_2026-08-11.md:339` reads *"0.0325 → 0.0353, the nominal best-on-board tie gone"* in running prose, **unstruck**, with the six-entry correction following in parentheses; `:265` reads *"best-on-board count of 4 of 8"* the same way. **Both are TRUE of the four-entry board they name and both carry entrant count and retrieval date, so V12's clauses are met** — the best answer *is* beside them. **What is not met is the house rule**, which is **strike-and-keep**. **The ledger's own stated closing action for `:339` was two things — *"strike `:339` in place, state the six-entry reading beside it"* — and the repair performed the second and not the first.** | Found while grading V12 at `678cb7e8` by a non-author of the repair, in the sweep whose recognition control fired 5/5 and whose struck negative gained 0 — **the sites are LIVE precisely because the sweep blanks struck spans before matching, so an unstruck claim is what survives.** Both sites' arithmetic re-derived on both boards rather than accepted. | **One strike per site closes it**; a writing task needing no scoring call. **Explicitly NOT a V12 blocker and must not be cited as one.** Falsifiable by showing strike-and-keep is satisfied by an adjacent identifier, in which case this closes with no work. | **fleet** (the two strikes); **chief** (only if an adjacent identifier is held to satisfy strike-and-keep) |
| D308 | **D265(b) CLOSED BY DERIVATION, AND THE FIGURE HAD MOVED TWICE MORE WHILE IT SAT OPEN — WHICH IS THE ARGUMENT AGAINST REPAIRING IT WITH A NUMBER.** scripts/self_audit.py emitted *"takes rule-A faults from 2 to 68, both current faults clear"* inside check_board_placement_words' frame line. The literal **2** was true of the tree on 2026-08-14; it read **34** in the same call's detail when D265(b) was filed; and **derived by execution at HEAD 9cdb1851 it is 53**, with 15 rule-B faults beside it and 68 fault lines in total. So a repair that replaced 2 with 34 would have shipped a third stale literal, and one that replaced it with 53 would have shipped a fourth. **The two counters are now accumulated from the sweep's own verdicts and interpolated, so the sentence cannot go stale again.** The historical pair is kept, because the counterfactual it records is real and only a re-run of the swap can move it, but it is now labelled a DATED MEASUREMENT of that tree with its date and its commit rather than presented as a live figure — the check does not re-run the swap and now says so. | Derived by execution, not read off the detail line. Command: python3 with sys.path set to the repo's scripts directory and sys.dont_write_bytecode, importing self_audit and calling check_board_placement_words() directly, __pycache__ purged before every cell. It returns WARN, *"every travelling surface agrees with the board; 68 lab record placement(s) do not"*, with 69 detail entries: 68 faults plus the frame. Classified independently of the emitted figure by their message shape: **53 rule A** (a placement bound to a named entrant the board puts elsewhere) and **15 rule B** (the margin-over-the-leader family, which names nobody). Emitted and independent counts agree at 53 and 15. **AND MATCHING IS NOT DERIVED, so the claim was tested by mutation rather than by agreement.** A rule-A fault was planted by line index into campaign/BOARD_MOVED_2026-08-11.md (*"Reissmann placed 4th on the published board"* — the pin puts Reissmann at rank 1), **read back before the run**: the emitted figure moved **53 to 54** and the independent count moved with it, and the planted sentence appeared as a fault line. **The discriminating half was then run: the interpolation was replaced by a hardcoded literal of the SAME digits and the identical mutation re-applied — emitted stayed 53 against a true 54, so the mutation KILLS the literal and the test is not satisfied by a value that merely matches.** Both the seed file and the instrument were restored and verified byte-identical to HEAD by content comparison, never by git status; self_audit.py was confirmed byte-clean against HEAD before the work began, sha256 b8d83db742870fa9c4a26732087eaba110604d3e6e592991d078b77721ad21c9. | **The figure is DERIVED, so no pin is owed and none was added** — the chief's alternative branch (keep it literal and pin it with a test) did not arise. **D265(b) PROPOSED A SAME-CALL INVARIANT — *"an assertion that the frame's current-fault figure equals the rule-A count in its own detail ... would have reddened at 16, at 34 and at every value between"* — AND THAT INVARIANT NOW HOLDS BY CONSTRUCTION RATHER THAN BY ASSERTION**, because the emitted figure IS the count in the detail, computed once and interpolated. An assertion would still be worth adding as a regression guard against someone re-typing it, but it lives in sdk/tests/, which is peer-held, so it is routed rather than written here. **NOTHING WAS TUNED**: no window, pattern, admission rule, board, severity or admission predicate changed. The diff is 22 added and 5 removed lines by content comparison — two counters, their two increments, a comment recording why they exist, and the rewritten sentence. The check's verdict, its fault set and its frame's other figures are untouched, which is what makes this a reporting repair rather than evidence. **AND THE SAME STALE PAIR HAS A SIBLING I MAY NOT TOUCH:** sdk/tests/test_two_board_referents.py's MODULE DOCSTRING carries the identical sentence in the present tense -- *"swapping it to the live board takes rule-A faults from 2 to 68 over disjoint sets, both current faults CLEAR"* -- with the same 1455-opened-surface frame, now 1554. It is a docstring and not an assertion, which is why nothing failed and why it went unnoticed: the file's only frame-string assertions are on TWO REFERENTS and on the ranking record's path, both preserved by this repair, so the whole suite stays green over a stale docstring. sdk/tests/ is peer-held and was read and run but NOT edited. **What I did NOT do and may not: grade V16.** Round 13 established that C4 and C5 are disclosure requirements, satisfied, so the two discriminator gaps are off the rung's critical path; this was the remaining named blocker and a non-author is owed the re-execution of round 13's four items including this repair. **The durable half, and it is why the chief's instruction not to type a second number was the whole job:** a reporting figure sitting beside the call that computes it will go stale at the rate the corpus grows, and this one moved 2 to 34 to 53 in two days while a docket row about it was open. The repair for a typed number is never a better number. | **fleet, a non-author** (V16 round 13 re-execution, this repair included) |
| D309 | **V15 round 10 returned ONE finding and it is against the round's own instrument: the lab's standing sweep rule `\s+`-never-literal-spaces is INSUFFICIENT for blockquoted prose, and it produced a false V8 violation against a compliant sentence.** Frame pinned in argv (`fe54ec0a..fac54ab4`, 83 commits, 21 ladder-written files, whole files never ranges, strike spans blanked with offsets preserved). At `LADDER_V_PASS3_COLD_2026-08-11.md:465` the required V8 companion wraps as *"are `not / > statistically decided`;"* — between `not` and `statistically` sit a newline **and a blockquote `>`**. A literal space misses it; **`\s+` misses it too, because `>` is not whitespace.** The cell therefore reported the not-decided-pairs companion ABSENT at a site that states it plainly, and would have filed a violation against a correct sentence. **This is the V10 grader's wrapped-heading defect one level deeper, and the rule written to prevent that one does not reach this one.** Repaired by stripping line-continuation markers (blockquote and table prefixes) before companion matching — for detection only, never for offsets — after which the count moved **18 -> 17** and the false site disappeared, which is the repair's own control. **Every V10- and V15-class sweep that greps prose inside `>` blocks shares the exposure, and this lab's records are largely blockquoted rulings.** | Executed 2026-08-16 at pin `fac54ab4`, board re-derived by `ast.literal_eval` on `LIVE_BOARD` joined to the round-5 harness record (6 entrants, 7 positions, rank 1 of 7, best-on-board 2 of 8, 0 earned, overall 0.056647191704213645). **Three cells, each control kind DERIVED by `scripts/control_kind.py` rather than declared, every plant by LINE INDEX and read back at that index before any zero was trusted.** Cell 1 (claims contradicting the board): **RECOGNITION**, 3 mutually independent forms, readback 3/3, struck negative gained 0; 47 live hits, 4 ASSERT / 31 MENTION / 12 CANNOT_TELL by `scripts/use_mention_discriminator.py`, **CANNOT_TELL never promoted to ASSERT**, every non-MENTION opened and hand-adjudicated -> **0 findings** (correction left-operands, quotations of `closure.html:502`, a blocker named in backticks, and untouched dated history in a file that took 41 insertions and 0 deletions). Cell 2 (V8's companion rule): **RECOGNITION**, 3 forms, **two** negatives both held at 0 DELTA over the unplanted baseline — a struck claim and a *compliant* claim carrying all three companions, the second being what stops the cell flagging every rank claim; 17 hits, **0 ASSERT** -> 0 findings. Cell 3 (banned-claims list): 5 hits, 0 findings, both non-MENTION hits being the prohibition itself being stated. | **NOT BELIEF-NEUTRAL, declared explicitly, and R-VALUE's consecutive count stays at ZERO.** Graded against the ruling that *"external reader"* means external to the ROUND and against its paired limit that a finding is belief-changing only if a reader would believe something different: a reader of this record learns a standing control rule has a measured gap, demonstrated by an executed counter-example, and so believes something different about every `\s+`-based zero in the record. **The zero on the TEXT is a FLOOR over four of V8's rules, not a population statement about V8** — *"every quantitative sentence maps to a named artifact"* and the soft-adaptive-leakage disclosure were NOT run, and a grader calling V15 clean on this evidence would be committing round 9's F2 shape one rung along. **Two things were CLEARED rather than accused:** round 9's apparently contradictory **20,704** and **20,721** are two populations both correctly measured (at `fe54ec0a`, 20,721 paths minus 17 symlinks = 20,704 files, re-derived here); and the brief's margin `0.001365` against this round's `0.0013528` are both admissible bases, the seed bound `0.002419` being 177.12% of one and 178.82% of the other. Record: `campaign/LADDER_V_V15_ROUND10.md`. | the fleet, for the two V8 rules this round did not run; and whoever owns the standing sweep rule, which needs the blockquote case written into it |
| D310 | **D284 RULED: the two sentences are the same shape, and the lab applied the fix to their SAME-ERA SIBLING.** V7's second conjunct reads *"a grep for any other count claims about scoring calls, **all** reconciled against actual call sites"* — no tense clause, no era clause, no historical-prose exemption. `sdk/scripts/closure_criterion_on_test_features.py` carries **two** unreconciled count claims, not the one `D284` names: `:10` *("Whether to spend a **5th** official scoring call … **is** explicitly the coordinator's decision")* and `:248` *("Zero cases warrant a **5th** official scoring call")*, the second inside an emitted JSON verdict string. Against a ledger of **6** the next call would be the **7th** — off by two. Measured against the file: present tense; **no year anywhere in the docstring** (`grep -cE "20[0-9]{2}"` over lines 1–12 returns **0**); the round-3 anchor sits in paragraph 1 scoping what the TABLE describes while the claim sits in paragraph 2; and the script **makes no scoring call at all** by its own docstring, so its ordinal cannot be a record of its own action. **THE DECISIVE MEASUREMENT IS NOT TENSE.** `sdk/scripts/export_closure_submission_csvs.py:31` is **also a round-3-era script**, names an even older count, and reads *"the ledger (**"four official scoring calls, ever" as this round-3-era script was written; the cumulative count is six after round 5's 2026-08-07 call**)"* — the identical treatment `closure_round4_manifest.py:135` received at `49f71b8c`. So *"round-3-era prose is historical"* is **not a rule this lab follows**; it was applied to one sentence and to nothing else of its kind. The rule the corpus does follow is **authorship of the call**: scripts describing the call **they themselves make** keep a bare ordinal (`apply_closure_ph_gate.py` *"4th"*, `closure_round4_duct_rescale.py` *"#5"*); scripts stating **where the ledger stands** get era-scope plus the live count (the manifest, `export_closure_submission_csvs.py`, and `closure_round5_qcr_forward.py`, which says *"Scoped to its own run because…"*). **Both were false at one instant**: the 6th call landed `e865076b` 2026-08-07T20:38:52Z, the pass fixing one landed `49f71b8c` 2026-08-08T02:08:13Z — **5h29m later**, with the other already wrong for the same 5h29m, so this is not the world moving after delivery. | `sdk/scripts/closure_criterion_on_test_features.py:10` and `:248`; `sdk/scripts/closure_round4_manifest.py:135`; `sdk/scripts/export_closure_submission_csvs.py:31`; all read at `41ec5a8b`. Full ruling, instrument and controls: `campaign/LADDER_V_V7_RULING_2026-08-16.md`. Ledger truth from `closure_challenge_round5_qcr.json` → `scoring_calls`. | **V7 is `FAIL`** and the ledger row is struck accordingly. What closes it: give `:10` and `:248` the treatment `export_closure_submission_csvs.py:31` already carries — era-scope inside the sentence plus the live count. A writing task; no scoring call, no compute. **Not repaired by this grader — a grader may not repair what he grades.** Instrument, for reuse: control kind **`RECOGNITION`** derived by `scripts/control_kind.py` and never declared (six mutually independent forms including one line-wrapped and one hyphenated, all found; two negative forms rejected), every gap bounded by characters because **`\w` cannot cross a hyphen**; pre-filter **provably a superset** — every `CALLNP` alternative contains `scoring`, `prediction set`, or `call`, so no matchable text can be dropped — keeping **937** paths read through **one** `git cat-file --batch`; **132 files, 694 count claims**. **CORRECTED 2026-08-16 at `c93d72aa`, and the correction is mine:** the first filter used `prediction[ -]sets?`, a **two-word phrase** a blockquote wrap can split, so it kept only 568 paths and was **not** the superset this row first claimed; single tokens `scoring|prediction|calls?` cannot be split by a wrap. Also **`\s+` does not reach a wrap across a `>` prefix, because `>` is not whitespace** — line-continuation prefixes are now stripped for detection only, controlled by planting `>` and `> >` wraps that `\s+` alone MISSED. Corpus count moved 693 → 694, zero files gained, and the one revealed claim is a **mention** inside V7's own verification record. **The ruling's evidence did not move under either normaliser** (criterion file 4/4, manifest 3/3, export 11/11), so the `FAIL` stands. Also recorded and NOT carrying the ruling: the row's vindication, *"two more stale generator strings found later"*, concerned **best-on-board counts and QCR attribution, not scoring-call counts** — a different class from the one V7 declares. **Falsifier:** show the ledger stood at 4 when `49f71b8c` ran, or that `export_closure_submission_csvs.py` is not round-3-era, and the ruling falls. | **FLEET** for the two-sentence repair; **chief** for whether a rung's vindication may be drawn from a class the rung does not declare |
| D311 | **V10's third-failure blocker repaired as a CLASS across the whole round-5 submission package, and the sweep that found it caught the same wrap defect in ITS OWN pattern first.** The blocker at `closure_challenge_submission_round5/DESCRIPTION_DOCUMENT.md:112-113` read *"`AR_14_Ret_180` was best on board in round 4 by 0.00003 and **is not any more** (see disclosure 4)"* -- live (the `~~` span closes two words before it), **false of the live board**, and carrying **no board identifier in its own block**, `(see disclosure 4)` pointing 53 lines away which D294 does not admit. **Derived from the data, not from any brief** (`ast.literal_eval` on the `LIVE_BOARD` assignment node, never imported, joined to the round-5 harness record): on `AR_14_Ret_180` the live best is **Yang at 0.0250**; our round-4 **0.0325 stood 2 of 7, tied with Reissmann**, and round-5 **0.035339 stands 4 of 7**. **There was never a best-on-board tie on this case to lose -- what was lost was a tie for SECOND**, and the best-on-board reading was true only of the **four**-entry `deb91557` clone in which Yang does not appear. Repaired in place with the identifier INSIDE the block, on the pattern the same document already used correctly at its own `:166` and `:184-187` -- **the repair that stopped one site short, which is the fifth time this rung has fallen to that shape.** | **Every one of the 11 package members swept, enumerated by `git ls-tree` and never through a pipe** (a peer's hit-dump through `head -220` silently truncated a listing at 8 of 31 and hid a site). 3 text members + 8 prediction CSVs; CSVs returned 0 as expected and were swept rather than argued about. **CONTROL A reproduced the false zero before fixing it:** a naive literal-class pattern MISSES `best-on-\n> board` split across a blockquote wrap and finds it once continuation prefixes are stripped. **CONTROL B, planted BY LINE INDEX and read back at that index before any zero was trusted, returned 2 of 3 and was therefore BROKEN** -- the third planted form was the hyphen-split-across-wrap one, **so this sweep's own claim pattern carried the exact defect its author had filed against V15 round 10 one task earlier**. Repaired with an OFFSET-PRESERVING deprefix (marker characters overwritten with spaces in place, newlines untouched, quoting and offsets always taken from the unmodified text), after which the control read **3/3 with readback OK on all three text members**. **Strike parity before and after: 40 `~~` markers, even, final depth 0, zero `<s>`/`<del>`** -- unchanged by the repair; file 564 -> 571 lines. | **One site repaired, one deliberately left, and the rest already compliant.** LEFT WITH GROUNDS: `:97`, the section heading *"### 2. Best-on-board count, stated with its own qualification"* -- a heading is its own block under D294, but this one names the disclosure section and **asserts no value and no board frame**, so neither half of D294 has anything to bind to. ALREADY COMPLIANT and re-verified in-block, not inherited: `:166`/`:185` (now `:173`/`:192`) and the placement claims at `:105`, `:195`, `:286`, each carrying an entrant count or a retrieval timestamp inside its own block. A second pass over the placement class (`rank N of M`, `Nth of M`, *lowest/best number on the board*) returned **3 sites, all with in-block identifiers**. **I MAY NOT GRADE THIS: a non-author of this repair owes V10 its fourth grade.** | a non-author of this repair, for the V10 fourth grade |
| D312 | **V10 FAILS A FOURTH TIME, AND FOR THE FOURTH TIME THE REPAIR FIXED THE SITE IT WAS HANDED AND LEFT THE CLASS — THIS TIME INSIDE A FILE IT HAD ALREADY SWEPT. THE TRAVELLING DOCUMENT NOW ASSERTS BOTH THAT THE `AR_14_Ret_180` BEST-ON-BOARD TIE WAS LOST AND THAT IT NEVER EXISTED.** `demo-output/website/closure_challenge_submission_round5/DESCRIPTION_DOCUMENT.md` at `38cd036f`: **§2 `:118-119`** — *"**`AR_14_Ret_180` was never best on board**, and there was no best-on-board tie on this case to lose; what was lost was a tie for second"*; **§4 `:192`** — *"its 0.00003 **best-on-board tie is lost**"*; **§3b `:173`** — *"**the best-on-board tie gone**"*. Same document, same case, same day, opposite assertions, all three confirmed present by byte test. **Measured with `scripts/use_mention_discriminator.py`, claim span passed explicitly as its API requires: `:192` classifies ASSERT** (*own voice, claim not set off or attributed*); **`:119` classifies CANNOT_TELL and was NOT promoted to ASSERT** — it is hand-adjudicated as an assertion on written grounds, being bolded, in the document's own voice, and the sentence the repair itself added in order to replace a claim. **THE CRITERION DECIDES IT WITHOUT A JUDGEMENT CALL**, quoted from `campaign/LADDER_V_TRIPLE_VERIFICATION.md:386-389` (drifted from the `:359-362` older records cite): the five surfaces *"must all carry round-5 numbers with the same caveats — **one inconsistent surface fails the rung**"*. **Every other criterion surface strikes the characterisation and says the tie was never a live one** — `closure.html:192`, `:197`, `:302`, `:373`, `:403`; `ACTIVE_RESEARCH.md:29-31`; `benchmarks.html:128-130`; `wall/wall.json:56`. **The submission package alone leaves it asserted and unstruck, and it is the surface that leaves the lab.** **THE SHAPE IS D294'S, APPLIED HALF-WAY:** the repair OPENED `:173` and `:192`, cleared them because they carry in-block board identifiers, and left them — half (a) without half (b), which D294's own row warns of in those words (*"a caveat-adjacency test grades whether a claim TRAVELS WITH its board, and is silent on whether the claim is TRUE of that board"*). **A per-site check cannot see a defect that lives in the agreement between sites**, which is D49's shape one rung over, where a per-row check could not see a defect living in a pairing. | Graded 2026-08-16 at the **pinned** frame `38cd036f` by an agent that wrote no part of V10 — no grade, no repair, no reopen, no closure record; recorded in `campaign/LADDER_V_V10_GRADE_PASS4_2026-08-16.md`, landed `9e3d686b`. **Arithmetic re-derived, never quoted:** `ast.literal_eval` on the `LIVE_BOARD` assignment node, parsed and never imported, joined to `round5_per_case_full` and `round4_per_case` — on `AR_14_Ret_180` the live seven-position column puts **Yang first at 0.0250**, round 4's `0.0325` stands **2 of 7** (tie membership derived by equality test, returning Reissmann) and round 5's `0.035339` stands **4 of 7**. **DETECTION RULE AS IT STANDS TODAY:** line-continuation prefixes stripped **with offsets preserved**, byte-length asserted per line and whole-text — and because a **hyphen-split** wrap defeats `\s+` *even after* that repair, every token joiner is `[-\s]{1,20}`. **Controls planted BY LINE INDEX into scratchpad copies and read back AT that index** (no tracked path written): plain, wrap, blockquote-wrap and an inflected sibling — **4 of 4 found**; a negative form correctly rejected; a **struck negative** returning **0 live** on its planted line. `scripts/control_kind.py` classified the ledger **RECOGNITION** from the evidence rather than a typed label. **The blockquote-wrap control reproduced the false zero before it fixed it: NOT FOUND without the prefix repair, FOUND with it.** **THE FRAME WAS PINNED AND THE PIN WAS TESTED** — HEAD advanced eight commits mid-grade, from `51d8eb43` to `38cd036f`, and `38cd036f` **is** the repair under grade, so a moving frame could have had this grade reading the pre-repair file; `51d8eb43` was confirmed an **ancestor** first, then the whole sweep was re-run pinned and the two runs compared **byte for byte: identical, zero differing lines**. **No listing that was read was piped**, because the third grade recorded losing a live blocker to `head -220`. | **CLEARS IN ONE OF TWO WAYS, stated so the repair is not a guess:** bring `:173` and `:192` into the treatment every other criterion surface already uses — **strike the characterisation** rather than appending a board identifier to it, as `closure.html:302` and `ACTIVE_RESEARCH.md:29-31` both do — **or** narrow §2's `:118-119` so its denial is explicitly scoped to the six-entry board rather than absolute. Either resolves it; doing neither leaves the package asserting P and ¬P. **WHAT MUST NOT BE DONE: repairing only `:192` and leaving `:173`.** That is the fifth repetition of the shape this rung has now failed on four times, and `:173` is the harder one to see precisely because its correction is long and reads as thorough. **RULED EXPLICITLY, AND BOTH AGREE WITH THE THIRD GRADE ON GROUNDS NOT INHERITED FROM IT:** `docs/PRODUCT_LIST.md:1810-1812` is **outside the claim class** — my recogniser matched 11 family-A and 39 family-B hits in that file and matched those lines **not at all**, `0.0748` occurs **ZERO** times in its 3,996 lines re-verified, and the sentence names its board **by entrant count** (*"all four published entries"*) about the **organisers' baseline** rather than our standing; and the four surviving `closure.html` matches are **clean** — my wider family found **ten** live hits there rather than four, and all ten carry a refuting or dating operator plus an in-block identifier, so the extra six **strengthen** the third grade rather than qualifying it. **`:97` was correctly left**: *"Best-on-board count"* is a noun phrase naming a metric, not a proposition, so neither half of D294 has a truth-bearer to bind to, and the strongest objection — that a reader infers a nonzero count — fails on the text directly below it, which states the count belonging to our model is **ZERO of 8**. **Nothing was repaired by this grade.** | **owner of the submission package** (the two sites, and both of them in one pass); **chief** (whether cross-section agreement inside one surface is inside V10 — the falsifier this grade names against itself, and a ruling that it is not would also reverse the third grade) |
| D313 | **The package asserted both that the AR_14 best-on-board tie was LOST and that it NEVER EXISTED, and the repair that made it consistent is a check over the SET rather than over each site.** V10's fourth FAIL was not a bad site: `:118-119` (*"was never best on board, and there was no best-on-board tie on this case to lose"*), `:173` (*"the best-on-board tie gone"*) and `:192` (*"its 0.00003 best-on-board tie is lost"*) each passed D294 **individually** -- every one carried an in-block board identifier -- and the document still contradicted itself. **A PER-SITE CHECK CANNOT SEE A DEFECT THAT LIVES IN THE AGREEMENT BETWEEN SITES**, which is D294 half (a) applied without half (b): I cleared `:173` and `:192` at D311 for carrying identifiers and never re-checked whether they were TRUE against the correction I had just written into section 2. | **ROUTE 1 OF THE TWO THE GRADE ALLOWED: strike the characterisation, on the pattern `closure.html:302` already uses** (`<s>a best-on-board tie</s> <b>a tie for second</b>`), applied at BOTH `:173` and `:192` rather than at one -- the grade's explicit warning being that repairing `:192` and leaving `:173` fails a fifth time. Route 2 (scoping section 2's denial to the six-entry board) was rejected on a stated ground: it would leave the characterisation ASSERTED on the surface that leaves the lab, while **every other criterion surface strikes it** (`closure.html:192/:197/:302/:373/:403`, `ACTIVE_RESEARCH.md:29-31`, `benchmarks.html:128-130`, `wall.json:56`), so route 1 makes the package agree with the lab as well as with itself. Re-derived rather than inherited (`ast.literal_eval` on the `LIVE_BOARD` assignment node, never imported, joined to `round5_per_case_full` and `round4_per_case`, tie membership by equality test): live best on `AR_14_Ret_180` is **Yang 0.0250**; round 4's **0.0325 stood 2 of 7, tied with Reissmann**; round 5's **0.035339 stands 4 of 7**; the best-on-board reading was true only of the **four**-entry `deb91557` clone in which Yang does not appear. | **THE AGREEMENT CHECK IS THE DELIVERABLE, and it was proved to have power before it was believed:** it extracts every live mention of the characterisation, classifies each as ASSERTED or DENIED by scope, and fails if the document does both. Against the **pre-repair blob it returns CONTRADICTION and names exactly `:173` and `:192`** -- the two sites the grader named, found independently; against the repaired text it returns **AGREES: 0 live unnegated assertions, 4 denials or scoped corrections, 3 struck occurrences**. **The wrap defect bit a THIRD time and was caught by that control:** the scope detector split sentences on a bare newline, so *"there was **no** best-on-/ board tie"* had its negator severed and a DENIAL was reported as an ASSERTION -- fixed by bounding on sentence punctuation and paragraph breaks, never on `\n`. Detection throughout used the standing rule as it now reads: continuation prefixes overwritten with spaces IN PLACE (same length, newlines untouched, offsets preserved) **and** `[-\s]{1,20}` joiners, because a hyphen-split wrap defeats `\s+` even after the prefix repair -- CONTROL A reproduced that false zero before fixing it, and CONTROL B planted three mutually independent forms by LINE INDEX, read back 3/3 by slicing at the planted index, struck negative gaining 0, kind **RECOGNITION**. Strike parity 40 -> 44 `~~`, even both, zero `<s>`/`<del>`, file 571 -> 573 lines. The family is confined to this member: `README.md` and `MANIFEST.json` carry **0** occurrences, and the eight prediction CSVs carry none. **I MAY NOT GRADE THIS: a fifth grade is owed to a non-author of this repair.** | a non-author of this repair, for V10's fifth grade |
| D314 | **V16 ROUND 14 — ROUND 13'S REVERSAL SURVIVES A NON-AUTHOR'S EXECUTION, AND V16 WAS PASSABLE AT `e71faf8b` WITH NO MEASURED OBSTACLE LEFT.** All four of round 13's substantive claims were re-executed by an agent that authored no V16 round and no part of the guard. **C4/C5 are DISCLOSURE clauses and were SATISFIED**: `_place_precision_sentence()` called directly returned `20 of 28 (71%)` and `19 of 25 (76%)`, the corpus named 3 times, the excluded sense named verbatim (*kernel, Gramian, Laplacian, array*); `_place_reach_sentence()` returned 7 rate-shaped figures over 3 rule-A sets plus a rule-B row, with 4 `invented` clauses each naming builder and blindness. **C1 was SATISFIED-OR-VOID under all three constructible readings** — and a THIRD reading round 13 did not run lands on VOID too (see method). **Both discriminator gaps are not merely off the critical path: they are DISCLOSED BY NAME AND COUNT inside C4's own sentence** — GAP-Q as *"quotation (3 author / 3 grader … neither reads intent)"* and GAP-B as *"other-named-board (0 author / 2 grader … this check reads WHETHER an ordinal is claimed, never WHICH board")*. **A gap that is named, counted and published is what the disclosure clauses exist to require, not what they fail on.** | Executed at frames `2daebd70`, `38cd036f` and `e71faf8b`, ancestry asserted with `git merge-base --is-ancestor` for every subject commit before any cell; `__pycache__` purged per cell; `scripts/self_audit.py` asserted byte-clean against HEAD by CONTENT COMPARISON before and after every cell — **`git status` reported it `MM` at a moment `cmp` against `git show HEAD:` returned byte-identical, so the status output was discarded**. Board re-derived: `ast.literal_eval` on the `LIVE_BOARD` assignment node in `sdk/scripts/probability_of_rank.py`, module NEVER imported (live ranks yang 1, reissmann 2, wu 3, tian 4, liu 5, montoya 6), joined to `_published_board()` -> `{reissmann 1, wu 2, liu 3, montoya 4}` at clone HEAD `deb91557184a`, pin dated 2026-05-04. **The extension experiment run EXHAUSTIVELY rather than at two points:** of every rank the board can hold for Yang, **exactly one behaves correctly and it is his LIVE rank** — the pin leaves him invisible both ways (0/0), ranks 2-5 fault a sentence TRUE of the live board, and rank 6 inverts the guard entirely; control (Wu, wrong) faulted 1 in all seven configurations and a nonsense-entrant negative control returned 0 in all seven. **THE THIRD READING:** under chief ruling `04489465` (*"the referent is fixed by the claim … undated present-tense claims grade against the live board"*), the guard was driven on the FOUR entrants C1 indisputably covers and was **wrong in both directions for all four** — it faulted every sentence TRUE of the live board and cleared every sentence FALSE of it. So *"the published board"* read as the board as published today makes C1 fail for six entrants rather than two, satisfiable only by re-pointing, which `04489465` DECLINED in its own words (*"explicitly does NOT authorise re-pointing the guard"*) and which the guard's own frame forbids (*"the pin … must not be moved (rung V1 needs the case scores to recompute identically)"*) — **VOID under `bcad2bbc`**. | **THE RUNG IS PASSABLE AND THIS ROUND DID NOT CLOSE IT, and the reason is an adjudication rather than a defect.** Two questions on V16's face are RULINGS: **(1)** which reading of *"an entrant"* and of *"the published board"* governs C1; **(2)** whether C4's operative condition is its disclosure clause or its precision preamble. **ROUND 13 TRUNCATED ITS C4 QUOTATION IN FRONT OF THE COUNTER-ARGUMENT** — it dropped *"Precision is a requirement of this rung, not a nicety — a guard that cries wolf gets switched off"* and quoted only what follows. **Answered here rather than dropped again:** the two clauses are joined by **"so"**, making the precision statement the RATIONALE and the disclosure the OPERATIVE CONDITION, and a rationale does not create a failure condition the sentence declines to type. **The reversal survives — but the whole rung turns on it, because the stated rates are 71% and 76%: under the performance reading V16 fails hard, under the disclosure reading it passes.** **Falsifiable by ONE artefact:** a rank for Yang derived from the README at `deb91557`. | **chief** (both rulings — nothing else was measured to be outstanding); grade document `campaign/LADDER_V_V16_ROUND14_NONAUTHOR.md` |
| D315 | **D308's REPAIR HOLDS UNDER AN INDEPENDENTLY REBUILT MUTATION PROOF, AND THE FIGURE MOVED AGAIN — 53 TO 56 — UNDER THE GRADER WHO CAME TO CHECK IT, WITH NOBODY EDITING THE SENTENCE.** D308 replaced a typed `2` in `check_board_placement_words`' frame with two counters accumulated from the sweep's own verdicts, and proved it derived rather than merely matching by mutation. **Re-executed here by a non-author of the repair, with the harness rebuilt from scratch.** Control asserted GREEN FIRST, green defined as *the emitted figure agreeing with a count classified independently by message shape* and never as `returncode == 0`: **emitted 56 / independent 56** at `2daebd70`. Two rule-A faults planted BY LINE INDEX and read back from disk: **emitted 58 / independent 58**. Then the discriminating half — the interpolation replaced by a **hardcoded literal of the same digits** and the identical plants re-applied: **emitted 56 against a true 58**. **The mutation kills the literal.** Rule-B stayed 15 in all three cells and the struck negative control contributed nothing, so the delta was exactly the two live plants. **D308 derived 53 at `9cdb1851`; this round derived 56 at `2daebd70`; a literal would have been stale for the fourth time in three days.** D308's own declarations checked: 22 added / 5 removed by content comparison (matches), nothing tuned (the entire diff is a comment block, two counter initialisations, two `+=` accumulations and the rewritten f-string), the historical pair kept but relabelled *"AS THAT TREE THEN STOOD"* and *"A DATED MEASUREMENT AND NOT A LIVE FIGURE"*. Three consuming modules: **172 passed, 36 subtests passed, 0 failures**, 804s. | **A SAFETY IMPROVEMENT OVER THE ORIGINAL METHOD, STATED BECAUSE IT MATTERS:** the author wrote the mutant into `scripts/self_audit.py` and restored it; this round **never wrote to that file at all** — the mutated source was compiled from a string with `compile(src, "<the real path>", "exec")` so `REPO = Path(__file__).resolve().parents[1]` resolved identically, and the file on disk was asserted byte-identical to HEAD before and after. **The control was RECOGNITION-grade, not reachability-only, and the plants are SEMANTIC rather than literal — no string the guard searches for was planted, only a wrong ordinal it had to recognise.** Four syntactically distinct forms were validated in isolation first and all four faulted, none a substring of another: *"The rank-4 entry, Wu & Zhang's SST-QCRC, …"*, *"Liu, Wang, Zhao & Xiao took second place …"*, *"Montoya, Oulghelou & Cinnella ranked first …"*, *"Reissmann, Fang & Sandberg is the third-place entry"*; a TRUE placement (*"Wu & Zhang's SST-QCRC is the rank-2 entry"*) and a nonsense-entrant placement both stayed silent. **A TRAP REPRODUCED LIVE:** this round's first mutation attempt was killed by a 120s foreground timeout, **its `finally:` did not run**, and the seed file was left carrying the plant — detected by content comparison against `git show HEAD:<path>`, restored, and re-run under a longer budget. That is not a maxim here; it happened, on the first try. | **D265(b) IS CLOSED AND THE CLOSURE IS NOW NON-AUTHOR-VERIFIED.** **BUT THE REPAIR LEFT THE SAME STALE FIGURE IN THE SAME FILE, ~270 LINES AWAY:** `scripts/self_audit.py`'s `board_placement_faults` docstring reads *"…at a price of clearing **two live faults** -- which is not a repair."* — present tense, **no anchor**, against 56 derived at `2daebd70`. Its neighbour *"rule-A faults go from 2 to 68 over the same 1455 opened surfaces"* IS anchored (`2026-08-14`, `48d3f05a`) and is a dated measurement; **the "two live faults" clause is not, and is the finding.** `use_mention_discriminator.classify` returned **CANNOT_TELL** (*"both stances present"*) and was **NOT promoted** — hand-adjudicated **ASSERT** on the written ground that the clause is in the file's own voice, is not enclosed, and the discriminator's MENTION evidence comes from an attribution verb governing a DIFFERENT span. **Not repaired here: a grader does not repair what it grades.** | **a NON-AUTHOR of this row** (the unanchored docstring clause); D308's author is not eligible |
| D316 | **V16'S LEDGER ROW STILL CITES THE PHANTOM ROWS D289 AND D291 UNSTRUCK, AND STILL RECORDS ITS BLOCKER AS OPEN — D302's STRIKE-IN-PLACE REPAIR REACHED THE ROUND DOCUMENT AND NOT THE LEDGER.** At `e71faf8b`, `campaign/LADDER_V_TRIPLE_VERIFICATION.md`'s V16 verdict row cites **`(D289)`** for the C1 finding and **`(D291)`** for the GAP-B costing, neither struck. D302 records that those IDs hold ROUND-12 content and that round 13's real rows landed as D303-D305; the strike was applied in `campaign/LADDER_V_V16_ROUND13.md` §7.3 and nowhere else. **The round-13 block's frame anchor does not save this one: an ID citation is not a dated claim — D289 held round-12 content at `22e32c03` too.** The same row also states *"`self_audit.py:4042` still emits … (D265(b), unrepaired…)"*, closed at `29ad0364`/D308 — that clause sits inside a block opening with its frame, so it is **anchored and is NOT a W-5 violation; it is a ledger that has not been advanced**, erring in the direction the ladder's own 2026-08-15 records-repair note calls *"least likely to be checked"*. **RULED OUT OF V16'S SCOPE AND FILED RATHER THAN APPENDED:** `sdk/tests/test_two_board_referents.py`'s module docstring carries the identical sentence in the present tense — *"(1455 opened surfaces), swapping it to the live board takes rule-A faults from 2 to 68 … both current faults CLEAR"* against **1558** surfaces and **56** rule-A faults at `2daebd70`. **The scope ruling rests on the lab's own standing predicate, not on this round's judgement:** `scripts/self_audit.py:1749` defines `_VALUE_NOT_A_SURFACE = ("sdk/tests/",)` — *a labelled test corpus is not a claim surface* — and V16's face names `sdk/tests/test_rank_claim_surfaces.py` and no other test module. It is emitted by nothing and asserted by no test, **which is exactly why the suite stayed green over it**. | **AN INSTRUMENT CORRECTION THAT CHANGED A RESULT, AND IT IS THE REASON THESE SITES WERE FOUND AT ALL.** A first pass built with literal spaces reported two of these sentences **ABSENT**, because they wrap in source. Rebuilt with `\s+` **and** with leading blockquote prefixes stripped **for detection only** — never altering the text quoted or counted against — both appeared. **Demonstrated on a control rather than asserted:** the form `"> takes rule-A faults from\n> 2 to 68 over disjoint sets"` is **MISSED by `\s+` alone** and **FOUND** once `>` prefixes are stripped, because **`>` is not whitespace**. Whole files swept, never enumerated ranges: 18,972 tracked files scanned, 1,782 skipped as binary or oversize and the count stated. Controls were **RECOGNITION**-grade by `control_kind.py`'s definition — six distinct forms, none a substring of another, all six fired — with a **struck negative control** returning nothing after strike-blanking. 18 sites carry the `2 to 68` counterfactual; the great majority are correctly dated grading records and are not findings. | **DO NOT REPAIR AS A SITE LIST — the three carriers above are three different classes and only one is a stale ASSERTION.** (a) The `(D289)`/`(D291)` citations are **false regardless of frame** and are the repair D302 owes. (b) The `D265(b), unrepaired` clause is **anchored and merely unadvanced** — it needs a ledger entry, which is a verdict action and was declined by this round. (c) The `sdk/tests/` docstring is **out of scope by standing rule** and needs no V16 action at all. **Falsifier for (a):** show D289 holding round-13 content at any frame. | **the author of D302's repair** (a); **chief** (b, advancing V16's verdict row); **unowned/low priority** (c) |
| D317 | **V10 PASSES AT ITS FIFTH GRADE, AND THE DEFECT THAT ALMOST HID FROM THIS ONE WAS THE WRAP TRAP IN A THIRD DELIMITER.** Graded at the pinned frame `e71faf8b` by an agent that authored no V10 grade, repair, reopen or closure record (D130). **The agreement check was REIMPLEMENTED, not re-executed**, sharing no code with the repair's: against the pre-repair blob `deb57c99` it returned **CONTRADICTION** and named `:192` independently; against the landed blob and again at `04b9cc37` it returned **AGREES -- 0 live unnegated assertions, 4 denials or scoped corrections, 2 struck**, all seven raw occurrences reconciled hit by hit, **0 unexplained**. It scored pre-repair `:173` a DENIAL where the author's scored it an ASSERTION, so it was **weaker** there and still failed the blob. **Both repaired sites held on BOTH halves of D294**, blocks computed rather than assumed (`:173` the list item `170-173`, `:192` the paragraph `191-198`): **seven of seven identifiers in-block at each**, and the replacement *"tie for second"* re-derived TRUE -- round 4's `0.0325` stood **2 of 7 tied with Reissmann**, round 5's `0.035339` stood **4 of 7**, and the best-on-board reading held **only** on the four-entry `deb91557` clone where round 4 was **1 of 5 tied for best** and Yang was absent. | **A JOIN FAULT CAUGHT BY ASSERTION BEFORE IT WAS BELIEVED, and it would have inverted the whole grade:** `LIVE_BOARD["entrants"]` holds **positional lists, not per-case dicts**, and the first derivation silently produced ranks `1,1,1,1,1,1,1,1` and **best-on-board 8 of 8**. The corrected pass parsed the `CASES` assignment node as well and asserted `list(round5_per_case_full) == CASES` and per-entrant vector length **before forming any figure**. Derived, never quoted: ranks **2,2,1,1,3,4,4,7 of 7**; best-on-board **2 of 8**, **0 of 8** ours, both survivors confirmed equal to `rans_identity_floor_per_case` **and** present in `unchanged_cases_sha256`; overall `0.056647191704213645`, **1 of 7**; margin **0.0013653** mean-of-eight, **0.0013528** against the published rounded `0.0580`. **SIX CONTROLS planted BY LINE INDEX and read back by SLICING AT THAT INDEX with byte-equality asserted**, `control_kind.py` returning **RECOGNITION** from measured booleans and never a typed label: plain, wrap, **blockquote-wrap reproducing the false zero before the fix**, hyphen-split, a **struck negative gaining ZERO**, a negative form correctly rejected. **THE FINDING WORTH MORE THAN THE VERDICT:** the first board-size sweep returned a **FALSE ZERO** on the submission package, because `beats **all four** published entries` puts `**` between the tokens and **`**` is neither whitespace nor a hyphen, so the `[-\s]{1,20}` rule as it stood this morning CANNOT CROSS IT. Fixed by an **offset-preserving emphasis blank** (`**`, `*`, `_`, backticks, `<b>`/`<em>` overwritten with spaces in place, same length, newlines untouched) applied **AFTER** strike detection because `~~` is a strike marker; the sweep then returned 3. **That is the same defect as the blockquote wrap and the hyphen split, in a THIRD delimiter, found by a grader who had read the warning about it the same hour -- the fourth agent in three days to be bitten by a class it had just been briefed on. Only the readback caught it.** A second self-defect was caught the same way: `^\s*#{1,6}\s` under `re.M` lets `\s*` consume the **preceding newline** and reported headings one line early; bounded to `^[ \t]*#{1,6}[ \t]`. | **VERDICT `PASS`, plain -- no residual and no exception label.** Every mechanical claim of the repair re-measured and held: **571 -> 573** lines, `~~` **40 -> 44** even on both blobs, final depth **0**, zero `<s>`/`<del>`, `README.md`/`MANIFEST.json` at **0**, eight CSVs at **0**. The repair's *"3 struck occurrences"* measured **2** here -- a population difference between recognisers over SUPPRESSED items (D49); the verdict turns on live unnegated assertions, **0** under both. **THREE STANDING ITEMS RULED, all AGREED on re-executed grounds:** `PRODUCT_LIST.md:1810-1812` outside the class (five grounds re-measured, `0.0748` **ZERO** times in 3,996 lines); `DESCRIPTION_DOCUMENT.md:97` correctly left, the discriminator returning **`CANNOT_TELL`** and **NOT promoted**, hand-adjudicated because a heading is a noun phrase with no predicate and neither half of D294 has a truth-bearer to bind to; the `closure.html` survivors clean, **seven** on this family, each opened and each carrying a refuting or dating operator plus an in-block identifier. **FILED, NEVER APPENDED -- F1, `DESCRIPTION_DOCUMENT.md:92`, *"beats **all four** published entries"*, ruled NOT a V10 blocker and the grounds given because the ruling is close:** both halves of D294 hold -- the board is named by **entrant count**, which the FOURTH grade ruled *"the lab's own admissible identifier form"*, and the claim is **true by execution of BOTH boards** (`0.0461` beats the clone's four and the live six; `0.0719` likewise). **Every V10 blocker in this rung's history was FALSE of the live board; this one is true of both.** And **no criterion surface states it with "six"** -- `PRODUCT_LIST.md:1787`/`:1811`/`:2511` and this line all say *"four"* -- so there is no INCONSISTENT surface, which is the criterion's actual test. What is wrong with it is **D238's class**: a board named by count with no retrieval date. **Failing V10 here would reverse D246 and reopen `:1811` by the same stroke** -- the boundary the V10 closure parked and the reopen deliberately landed away from. **The case AGAINST this ruling is recorded rather than omitted:** `:92` sits in an **undated, live, forward-facing disclosure section** rather than a dated journal, and its own page states a six-entry board eight lines below at `:99-104` and again at `:449`, so D246's third and fourth grounds do **NOT** transfer to it. **F2** -- `CLOSURE_CHALLENGE_STATUS.md:454`/`:781`, out of scope by surface (the criterion names five and that document is none of them); `:781` MENTION by blockquote, `:454` CANNOT_TELL, neither promoted. **Nothing was repaired by this grader.** Grade: `campaign/LADDER_V_V10_GRADE_PASS5_2026-08-16.md`. | **chief** (F1's D238 boundary: whether a board named by entrant count with **no retrieval date** is a V10 inconsistency -- a ruling that would also reverse D246 and reopen `PRODUCT_LIST.md:1811`); `CLOSURE_CHALLENGE_STATUS.md`'s owner (F2) |
| D318 | **CHIEF RULES F1: `DESCRIPTION_DOCUMENT.md:92` IS NOT A V10 BLOCKER -- AND THE RULING IS RECORDED WITH ITS LIMITATION ON ITS FACE, RESTING ON ONE GROUND RATHER THAN FOUR.** Raised by V10's fifth grade at `700d2f08` and ruled by the chief in the same exchange. **THE DECISIVE GROUND IS THE ONLY LOAD-BEARING ONE: a claim TRUE OF EVERY CANDIDATE BOARD cannot be an inconsistency BETWEEN boards.** The board-identification requirement (D255, D238) exists so a reader can determine **WHICH** board a claim is about; **it is not violated where the answer does not change the claim's truth**, so the missing retrieval date on `:92` was held **not load-bearing here**. **Three supporting grounds were recorded and are expressly NOT load-bearing:** (2) every prior V10 blocker was **FALSE** of the live board and this one is true of both, which makes it a different object, V10's criterion being about surfaces carrying INCONSISTENT numbers rather than about identifier form in the abstract; (3) **no criterion surface states the claim with "six"** -- `PRODUCT_LIST.md:1787`/`:1811`/`:2511` and `:92` all say *"four"* -- so there is no INCONSISTENT surface, which is the criterion's actual test; (4) failing here would **reverse D246 and reopen `PRODUCT_LIST.md:1811`**, and a rung must not be closed or opened by a side effect on a neighbouring ruling. | **THE CHIEF RECORDED THE CASE AGAINST THE CHIEF'S OWN RULING, FROM THE FLANK THE GRADE SUPPLIED, AND DID NOT DISPOSE OF IT.** `:92` sits in an **undated, live, forward-facing disclosure section** rather than a dated reverse-chronological journal, and **its own page states a six-entry board eight lines below** at `:99-104` and again at `:449` -- so **D246's THIRD and FOURTH grounds (dated journal heading; no same-page correction) do NOT transfer to it.** That was accepted as a **genuine weakness**. It was held not to overturn ground 1, because truth-on-both-boards survives whatever the section's dating -- **but the ruling therefore rests on ground 1 ALONE and is to be read that way, not as a clean call.** **THE RULING CARRIES ITS OWN FALSIFIER ON ITS FACE: if a future measurement shows `:92` FALSE on EITHER board, the ruling falls immediately.** The measurements it stands on, executed at `e71faf8b` and not quoted: the supplied baseline `0.0461` on `alpha_05_4071_4048` against the four-entry `deb91557` clone's `0.0606 / 0.0569 / 0.0613 / 0.0591` and against the live six's `0.0645 / 0.0606 / 0.0569 / 0.0744 / 0.0613 / 0.0591`; and `0.0719` on `alpha_05_4071_2024` against `0.0760 / 0.0848 / 0.0769 / 0.0882` and the live `0.0748 / 0.0760 / 0.0848 / 0.0960 / 0.0769 / 0.0882`. **A board move putting any entrant below either baseline value on either case falsifies the ruling and reopens the site.** | **RECORDED, NOT REPAIRED -- `:92` was left exactly as it stood, and a grader does not repair what it graded.** **F2 (`CLOSURE_CHALLENGE_STATUS.md:454`/`:781`) was ACCEPTED AS OUT OF SCOPE BY SURFACE**, the criterion naming five surfaces and that document being none of them, and remains filed for that file's owner rather than appended to any rung. **V10 IS CLOSED `PASS` at `700d2f08` and MUST NOT be re-graded by its author.** **A STANDING LAB RULE WAS SET IN THE SAME EXCHANGE and belongs to every sweep, not to this rung:** the detection rule is **NOT** *"use `\s+`"* -- it is **BLANK EVERY DELIMITER CLASS WITH OFFSETS PRESERVED**, in a load-bearing ORDER: (1) strike spans (`~~`, `<s>`, `<del>`) blanked FIRST and tracked; (2) line-continuation prefixes (`>`, `> >`, list markers); (3) **markdown/HTML emphasis (`**`, `*`, `_`, backticks, `<b>`, `<strong>`, `<em>`) blanked AFTER strikes**, or `~~` is eaten as emphasis first; (4) joiners built as `[-\s]{1,20}`, never `\s+` and never a literal space. **`**` is the FOURTH delimiter class to defeat a sweep in a single day** -- after the literal space, the blockquote `>`, and the hyphen split -- and it produced a **FALSE ZERO** in the fifth grade's own board-size sweep before the emphasis blank took it to 3. **The grader had read the warning about the class the same hour and was bitten anyway: knowing the trap does not protect you, the readback does.** | **fleet** (the four-class delimiter rule, broadcast); `CLOSURE_CHALLENGE_STATUS.md`'s owner (F2); **nobody** for `:92`, which is ruled and closed unless its falsifier fires |
| D319 | **CHIEF RULING 1 RECORDED AND TESTED: V16's C4 IS A DISCLOSURE CLAUSE, AND THE COUNTER-ARGUMENT IS ANSWERED RATHER THAN TRUNCATED IN FRONT OF.** The clause quoted WHOLE, which round 13 did not do: *"Precision is a requirement of this rung, not a nicety -- a guard that cries wolf gets switched off, and then it guards nothing -- so the rung fails if the check does not state its false-positive rate against a measured corpus and name the senses of the word it excludes."* **THE COUNTER-ARGUMENT, at its strongest:** a rung declaring precision *"a requirement, not a nicety"* cannot be satisfied by a guard that falsely faults ~3 sentences in 4; on the disclosure reading the emphasised half does no work and a check stating *"my rate is 100%"* would pass. **ANSWERED ON FOUR GROUNDS.** (i) The connector is **so**: the only predicate the clause types as capable of failing is **does not state** -- no threshold, ceiling or comparator appears -- and **C5 is built identically with its rationale on `because`**, so V16's face marks rationale by connective TWICE and the grammar argument is structural to the criterion rather than convenient to the verdict. (ii) **No threshold was ever pre-registered**; any bar would be chosen knowing 71% and 76%, which is the `_RANK_WINDOW` move this rung already refused, in the other direction. (iii) The rationale's harm is **invisibility** -- *gets switched off, and then it guards nothing* -- so its remedy is publication, and the check publishes two rates from two independently built sets with builder and blindness named, the corpus named 3x, and **ten** accepted false-positive shapes counted per sample. (iv) **DECISIVE: under a performance reading the cheapest way to pass V16 would be to SHRINK THE DENOMINATOR**, and the check measures that against itself -- the admission rule moves the spread across a **5-to-24 point range** and moved the author's own row **49% -> 71% with the numerator unmoved at 20**. A pass condition the gradee can move by choosing an admission rule is not a criterion; the disclosure reading is invariant under exactly that move. **RULING STANDS.** **THE COST, RECORDED AND NOT BURIED: the disclosure reading makes C4 satisfiable at ANY rate, 100% included.** That is a defect **of the criterion as written**. **REQUEST TO THE CHIEF: amend V16's face FORWARD with a pre-registered false-positive bar for future grades.** It must not be read in retroactively -- a criterion is amended forward and graded as written. | V16 round 15, frame `04b9cc37`, HEAD `8c01202f` (frame confirmed an ancestor; `scripts/self_audit.py` byte-identical at both by content comparison). Non-author of every V16 round, of the guard, of the D308 repair and of the ledger row repaired. **Forward amendment is the chief's.** |
| D320 | **CHIEF RULING 2 RECORDED AND VERIFIED BY EXECUTION: V16's C1 IS *VOID* UNDER `bcad2bbc`, NOT SATISFIED -- AND **V16 CLOSES WITH A PLAIN `PASS`** CARRYING A MEASURED FALSE-POSITIVE RATE OF `20 of 28 (71%)` and `19 of 25 (76%)` ON ITS FACE.** Board re-derived by `ast.literal_eval` on the `LIVE_BOARD` **assignment node** in `sdk/scripts/probability_of_rank.py`, the module **never imported**: the frozen pin `deb91557` (2026-05-04) holds `{reissmann 1, wu 2, liu 3, montoya 4}` and **Yang and Tian are absent**. **THE EXTENSION EXPERIMENT DRIVEN EXHAUSTIVELY** over every rank the vocabulary admits, with a wrong-vs-pin recognition control that fired in **all seven** configurations and a nonsense-entrant negative silent in **all seven**: **exactly one value behaves correctly and it is Yang's LIVE rank** -- ranks 2-5 fault a sentence TRUE of the live board, and **rank 6 inverts the guard**. So the correct behaviour is **uniquely determined by the import `04489465` forbids** (*"does NOT authorise re-pointing the guard -- that trades a silent instrument for a loud wrong one"*, verified at source), and under `bcad2bbc` (*"a criterion may not require more than the maximum permitted action ... void there rather than strict"*, verified at source) **C1 is VOID**. On the widest reading the guard is wrong in **both directions for all four entrants C1 covers, 8 of 8**, with negative control 0 and recognition control 1. **VOID IS NOT SATISFIED, and the distinction is recorded on the rung's face: C1 cannot block V16 AND MAY NOT BE CITED AS EVIDENCE THE PLACEMENT PREDICATE IS CORRECT.** **THE RUNG THEREFORE CLOSES.** C2 held; **C3 held -- 172 passed, 36 subtests, 0 failures** across the three consuming modules; **C4 and C5 SATISFIED**, re-measured by direct call, with a rate-shaped negative control (`77 of 99 (78%)`) correctly not found. **`PASS`, plain** -- *"with residuals"* and *"with exceptions"* are illegal labels and neither is issued. **WHAT THE PASS DOES NOT SAY, on its face wherever it is cited: the guard is not precise (it falsely faults `20 of 28 (71%)` and `19 of 25 (76%)`) and the placement predicate does not work.** It says the guard DISCLOSES its cost and its reach, measured, corpus and excluded senses named. | V16 round 15, frame `04b9cc37`, HEAD `8c01202f` (frame confirmed an ancestor; `scripts/self_audit.py` byte-identical at both by content comparison). Non-author of every V16 round, of the guard, of the D308 repair and of the ledger row repaired. Full record `campaign/LADDER_V_V16_ROUND15_CLOSE.md`; ledger row and verdict cell advanced in the same commit |
| D321 | **D308 RE-PROVED AND THE FIGURE MOVED A FIFTH TIME; D316 REPAIRED; AND MY OWN SWEEP CAUGHT ITSELF TWICE BEFORE ANY FIGURE FROM IT WAS BELIEVED.** **(a) D308.** Mutation rebuilt independently; **`scripts/self_audit.py` was NEVER WRITTEN TO** -- the mutant compiled from a string via `compile(src, "<the real path>", "exec")` so `REPO = Path(__file__)...` resolved identically, byte-clean asserted by **content comparison** before and after every cell (`git status` again reported `MM` against a byte-identical file and was **discarded**). Control asserted **GREEN FIRST**, green defined as the emitted figure agreeing with a count classified independently by message shape, never `returncode == 0`. **Derived / pristine: 61 = 61. Derived / +2 plants (by line index, read back by slicing at that index): 63 = 63. HARDCODED LITERAL of the same digits, identical plants: 61 against a true 63 -- THE MUTATION KILLS THE LITERAL.** Rule-B invariant at 15; struck negative at zero gain. RECOGNITION control validated in isolation first: four syntactically distinct placement forms, none a substring of another, all four fired; a TRUE placement and a nonsense entrant both silent; **plants semantic, not literal**. **The figure has now moved 2 -> 34 -> 53 -> 56 -> 61 in four days, TWICE under a grader sent to check it with nobody editing the sentence.** **(b) D316 REPAIRED.** `(D289)` and `(D291)` struck **in place** on V16's ledger row and pointed at their real rows **D303** and **D305**; **D265(b) advanced from open to CLOSED** at `29ad0364`/D308. **The phantom mechanism was reproduced rather than inherited:** hashing the first 400 characters of every docket row's content returns exactly **three** content-duplicate pairs under distinct IDs -- **D289/D298, D290/D299, D291/D300** -- which is the CAS's blind spot measured directly: **it asserts an ID is FREE, never that the CONTENT is new.** **(c) TWO INSTRUMENT FAILURES IN MY OWN SWEEP, both caught by controls and not by inspection.** The needle `rule-A faults from 2 to 68` **cannot span an intervening verb**, so it silently missed `rule-A faults **go** from 2 to 68` at `self_audit.py:3777` -- the is/was failure `control_kind.py` exists for, live; sites 9 -> 13. Repairing for **markdown emphasis as a third delimiter** (`**` is neither whitespace nor a hyphen) took sites **13 -> 18** and recovered `sdk/tests/test_two_board_referents.py:25` itself -- **but blanking `~` alongside the other markers eats `~~` before strikes are classified, and a first strike pattern `~~.+?~~` under `re.S` paired across paragraphs and reported `(D289)` STRUCK WHEN IT IS LIVE**, a false negative that would have silently cancelled this round's own repair. Caught by a strike-classifier control run **both ways on the real row** (as shipped D289 live=1; with `(~~D289~~)` planted, live=0). **Order is now strikes -> prefixes -> emphasis, offset-preserving, `^[ \t]*` never `^\s*`.** Final ledger by `scripts/control_kind.py` **from evidence**: **RECOGNITION** -- 7 vocabulary forms + 4 wrap forms (plain, wrap, blockquote-wrap, hyphen-split) all found, 2 negatives rejected, struck negative at zero gain. **(d) ROUND 14's SCOPE RULING ON `test_two_board_referents.py`: CONCLUSION SUSTAINED, GROUND OVERTURNED.** Leg 1 is **refuted by execution** -- `_VALUE_NOT_A_SURFACE` belongs to a DIFFERENT check, is referenced by neither `check_board_placement_words` nor `board_placement_faults`, and **V16's guard does sweep `sdk/tests/`**: **3 of the 61 live rule-A faults are on `test_two_board_referents.py` itself**. Leg 2 stands alone and suffices: V16's face names `test_rank_claim_surfaces.py` and the string `test_two_board_referents` occurs **nowhere** in `LADDER_V_TRIPLE_VERIFICATION.md`. **Out of scope because the criterion does not reach it, NOT because a test file cannot carry a claim.** **(e) D315 ROUTED, NOT REPAIRED:** `self_audit.py:3796` *"at a price of clearing **two live faults**"*, present tense, unanchored, against a live 61; its anchored neighbour at `:3777` is a dated measurement and is not the finding. Round 14's discriminator returned **CANNOT_TELL** and was **not promoted**, hand-adjudicated ASSERT with written grounds; that adjudication stands. | V16 round 15, frame `04b9cc37`, HEAD `8c01202f` (frame confirmed an ancestor; `scripts/self_audit.py` byte-identical at both by content comparison). Non-author of every V16 round, of the guard, of the D308 repair and of the ledger row repaired. **D315 owner: the `scripts/self_audit.py` owner -- not the agent closing the rung, and not its grader** |
| D322 | **THE V10 BLOCKER'S CLASS HAS FOUR MORE LIVE INSTANCES, ON CLAIM SURFACES THE REPAIR NEVER REACHED — AND FINDING THEM REQUIRED A CHECK THAT RUNS OVER THE SET, BECAUSE A PER-SITE SWEEP IS STRUCTURALLY BLIND TO A DEFECT THAT LIVES IN THE AGREEMENT BETWEEN SITES.** Nine characterisation classes were enumerated **as classes rather than as sites** — a characterisation being a claim about what HAPPENED rather than a number, which is precisely why it carries no stale literal and why `ACTIVE_RESEARCH.md:29` survived a number sweep and reopened V10. Every live instance of each class was collected across the corpus, each instance's stance classified ASSERTED/DENIED × SCOPED/UNSCOPED or STRUCK, and the **class** failed where the corpus does both unscoped. **EIGHT CLASSES AGREE. ONE DISAGREES:** `AR14_BEST_ON_BOARD_TIE` — the same characterisation V10's repair struck in `DESCRIPTION_DOCUMENT.md` and at five sites on `closure.html` — **is still ASSERTED and unstruck on four other claim surfaces**: `demo-output/website/CLOSURE_CHALLENGE_STATUS.md:617-621` (*"the pre-registration said … round 4's 0.00003-level **best-on-board tie** … was put at risk"*, reported speech with no marker that the characterisation is now known false); `demo-output/website/CLOSURE_CHALLENGE_SUBMISSION_DRAFT.md:1239` (*"**The `AR_14_Ret_180` best-on-board margin collapsed from 0.0022 to 0.00003**"* — and it **carries a repair parenthetical on the very next line**, so the board identifier reached it and the characterisation did not, which is D294's shape exactly) and `:1273` (*"at round 3 it was … and **best on board** on `AR_14_Ret_180`"*); and `demo-output/website/latex/closure_challenge_report.tex:860-862`. **THE REPAIRING AGENT'S OWN GENERALISATION IS THEREFORE CONFIRMED BY EXECUTION.** **THREE CLASSES ARE NOTABLE FOR AGREEING and show what the failing class is missing:** `MODEL_IS_BEST_ON_SOME_CASE` and `LEAD_IS_DECIDED` return **ZERO** unscoped assertions anywhere in 1,883 blobs against 40 and 87 denials; `WORST_CASE_ON_HARD_SENTENCES` and `TWO_ROWS_DIFFER_ONLY_IN_BUILDER` survive **only inside grading records**, both withdrawn absolutes struck at their sources — L-76 working. | Swept 2026-08-16 at pinned frame `8c01202f` by an agent that wrote none of the four surfaces named, recorded in `campaign/CROSS_SITE_AGREEMENT_SWEEP_2026-08-16.md`, landed `1c0f92d3`. **THE FOURTH DELIMITER AND ITS ORDERING CONSTRAINT:** markdown **emphasis** is blanked offset-preservingly and **ONLY AFTER strike detection** — reversing those two steps eats the `~~` and turns STRUCK text into LIVE text, the direction that manufactures assertions; `~~` is never blanked and `_` is never blanked because this corpus's identifiers are full of it. **CONTROLS, planted BY LINE INDEX into scratchpad copies and read back by SLICING at that index:** plain, wrap, blockquote-wrap, hyphen-split, **emphasis-split** and **emphasis-mid** — **6 of 6 FOUND**; a negative correctly rejected; a **struck negative** returning STRUCK with **zero gain** to the asserted population; and an **ORDERING control — emphasis INSIDE a strike — returning STRUCK**, which is what proves the two steps are in the right order. `scripts/control_kind.py` returned **RECOGNITION** from the evidence rather than a typed label. **Both wrap false zeros were reproduced before they were fixed**, and **rule 3's false contradiction was reproduced deliberately**: on *"…so there was no\nbest-on-board tie…"* a punctuation scope returns DENIED and a **bare-newline scope returns ASSERTED**. **KNOWN-CASE VALIDATION on real production data, sharing no code with the V10 fifth grader: CONTRADICTION on the pre-repair blob naming `:173` and `:192` against `:118-119`, AGREES on the repaired one** — a third recogniser over a third population, which is D49 and not corroboration by construction. **ARM AND FLOOR:** tracked blobs at `8c01202f` by `git ls-tree -r`, read in one `git cat-file --batch` — **1,883 prose-bearing blobs OPENED AND SCANNED (the floor)**, 17 symlinks, 5 binary/empty/undecodable, and **18,854 non-prose-extension blobs EXCLUDED AND COUNTED** because a characterisation is prose and cannot live in a mesh file or a prediction CSV. Board arithmetic re-derived by `ast.literal_eval` on the `LIVE_BOARD` assignment node, parsed and never imported: **Yang first at 0.0250** on `AR_14_Ret_180`, round 4's `0.0325` at **2 of 7** tied with Reissmann, round 5's `0.035339` at **4 of 7**. | **NOTHING IS RULED ON HERE AND NO RUNG IS GRADED.** Three of the four sites are reported speech or dated history, and **whether reported speech must carry the correction is a RULING and not a measurement.** `docs/PRODUCT_LIST.md` and the package sit inside **V10's** declared criterion and the `.tex` inside another rung's — and is a path this pass may not write — so R-CONVERGE forbids one pass closing two rungs: **the class is filed, the rungs are named, and their graders decide.** **I REPORT MY OWN FALSE POSITIVES RATHER THAN HIDING THEM, because a sweep that conceals them has converted a range into a point estimate.** My instrument first flagged FIVE classes; **three are its own artifacts**: `RANK_1_IS_OFFICIAL` is a **recogniser design error of mine** — its pattern matches the CAVEAT phrases (`scored locally`, `unsubmitted`), so a surface CARRYING the disclosure scores as asserting its opposite; `AR14_TIE_WAS_FOR_SECOND` is **rule 3's failure one level down** — the negator in *"a tie for second, **not** for best"* belongs to a neighbouring clause, and **sentence scope is not clause scope**, which is the next delimiter this class of instrument will have to learn; `NOTHING_TRAVELS_AFFECTED` is two true statements about two moments. **FALSIFIERS:** the class verdict falls if the four assertions are shown adequately scoped by their own sentences (the scoping words are absent, so it is checkable in one pass); the floor falls if a characterisation form exists outside the nine recognisers; and **the whole per-class method falls if D255's identity is narrowed**, since 21 of the 26 raw assertions in the failing class are grading records excluded under it. | **a non-author of `CLOSURE_CHALLENGE_STATUS.md` and `CLOSURE_CHALLENGE_SUBMISSION_DRAFT.md`** (the two sites outside every rung's criterion, and the cheapest to settle); **the `.tex` owner**; **chief** (whether reported speech must carry the correction — the ruling all four sites turn on, and the one this row explicitly declines to make) |
| D323 | **PROPOSAL FILED, NOT APPLIED: AMEND V16's C4 FORWARD, BINDING THE BAR AND THE ADMISSION PREDICATE TOGETHER -- WITH THE BAR ABSTAINED ON RATHER THAN INVENTED.** Chief ruled 2026-08-16 that the amendment should be made and that a bar on the rate ALONE reproduces the defect one level up. **MEASURED HERE RATHER THAN ACCEPTED**, both precision sets driven through their own differing `measure()` APIs at frame `b2534057`, each with a live planted control required to move the count by exactly one and restore exactly (author `(20,28) -> (21,29) -> (20,28)`; grader `(19,25) -> (20,25) -> (19,25)`; **both FIRED**). **THE SAME NUMERATOR READS TWO DIFFERENT RATES:** the author set's **same 20 errors** are **20 of 28 = 71%** under admission `_placements` and **20 of 41 = 49%** under a raw pattern match -- **a 23-point swing with the numerator untouched** -- and the admission predicate ALONE flips that row at every candidate bar in **50, 55, 60, 65, 70, 71%**. A threshold on a gradee-chosen denominator is not a threshold. **HALF ONE PROPOSED FOR ADOPTION: the rate must be measured under a NAMED, COMMITTED admission predicate stated beside it, and a round changing the predicate must re-publish every historical row under it in the same commit or the change is void.** It invents nothing -- the predicate is already in force and already recomputed from committed sentences. **FAIRNESS POINT RECORDED: nobody gamed this denominator.** Round 10 / D49 moved the figure to the LESS flattering predicate, taking the author's row **49% -> 71%** against the lab's own interest; the amendment locks a choice already made honestly. **HALF TWO ABSTAINED, AND THE ABSTENTION IS THE ANSWER RATHER THAN A DEFERRAL: no number is proposed, because no evidence supports one.** Both existing measurements are adversarial and **explicitly not representative by the check's own words**, so neither bounds anything; the two blind sets disagree by **5 points under one predicate and 24 under the other**; and **the live corpus rate has never been adjudicated -- 61 rule-A faults at `04b9cc37`, not one classified true or false.** Proposing a figure would be inventing a threshold to fit a verdict, which is the `_RANK_WINDOW` move this rung already refused. **A PROCEDURE IS PROPOSED IN PLACE OF A NUMBER:** sample the LIVE corpus rather than either adversarial set, adjudicate blind by a non-author of the guard and of both sets, publish that rate with its predicate and sampling frame, and only then set the bar. **WHAT V16 WOULD HAVE GRADED UNDER A PERFORMANCE READING: `FAIL`** -- at every bar below 71%, and at every bar below 76% once the grader row is included, so the lowest bar V16 would have met is one **permitting the guard to be wrong more often than right**. **THE RATES TRAVEL WITH THE PROPOSAL** as required: `20 of 28 (71%)` and `19 of 25 (76%)`, so no future bar is read as describing today's guard. **FORWARD ONLY: V16's PASS at `b2534057` stands on the criterion as it read when graded; amending a rung's face and applying it backwards is the retroactive move W-3 forbids.** Proposal at `campaign/V16_C4_AMENDMENT_PROPOSAL.md`. **NOT APPLIED, NOT IN FORCE.** | **OWNER: KATIE** -- the amendment changes a closed rung's face and the chief ruled it should be made, not that it is made. Drafted at frame `b2534057` by the non-author who closed the rung. **Durable record of the moving figure filed separately as LESSONS.md L-85.** |
| D324 | **THE REPORTED-SPEECH RULING IS RECORDED — AND MEASURING THE PREDICATE IT TURNS ON REVERSED THREE OF ITS FOUR ROUTINGS, THEN SHOWED THE PREDICATE ITSELF CANNOT SEE THE TWO SURFACES THAT MOST NEED IT.** **THE RULING (chief, 2026-08-16), filed so the next sweep of this class does not re-derive it:** reported speech is **not** an assertion — *"on 2026-08-07 we claimed a best-on-board tie"* is a true statement about what was claimed and under D238/D255 does not assert the tie — so **as a matter of TRUTH it needs no correction**. **But V10's criterion is *"the same caveats"*, not *"the same truth values"*, and that clause decides it:** on a **TRAVELLING** surface reported speech **MUST** carry the correction, because a reader outside the lab meeting it with no withdrawal beside it takes the tie as fact whatever the sentence's grammatical mood — **a CAVEATS failure, not a truth failure**; on a **lab record** reported speech **stands**, because forcing a correction into every historical mention would make the record unable to describe its own history, which is unsatisfiable by construction. **I MEASURED THE PREDICATE RATHER THAN INHERITING IT.** The travelling set was derived by the same two derivations `self_audit._travelling_names` uses — shipping-archive members plus submission-package directories — reimplemented rather than imported: **one archive, `dist/certonomous-demo.zip`, 103 members, 111 travelling basenames**, controlled both ways (`DESCRIPTION_DOCUMENT.md` **True**, `LESSONS.md` **False**, `closure.html` **True**). **MEASURED: NONE of the four D322 sites travel.** `CLOSURE_CHALLENGE_STATUS.md`, `CLOSURE_CHALLENGE_SUBMISSION_DRAFT.md` and `latex/closure_challenge_report.tex` are **all lab records by the predicate**, so **under the ruling exactly as written reported speech stands at all four and no correction is required anywhere** — the opposite of the routing anticipated for three of them. **AND THE PREDICATE IS WRONG FOR TWO OF THEM, WHICH IS THE FINDING THIS ROW EXISTS FOR.** `_travelling_names()` is a **snapshot of what is CURRENTLY PACKED**, so it cannot see a document whose purpose is to **BECOME** the thing that is sent — and the lab says both of these in its own words: `closure_challenge_round5_qcr.json:213` calls `CLOSURE_CHALLENGE_SUBMISSION_DRAFT.md` **"Katie's send-package"** and records that *"Items 6-7 (author names, Katie's proofread and approval) remain the only things between the package and the steward"*, while the file's own opening calls itself *"draft package … This document exists to be proofread"*; and **D243 calls `latex/closure_challenge_report.pdf` SHIPPED** while the predicate marks it a lab record. **A draft is PRE-TRAVELLING and the binary has no third state. Directionally this is the worst way for it to be wrong: the two surfaces whose content is destined for an outside reader are the two the predicate marks safest**, so a rule keying correction-required off this predicate exempts exactly the sites that most need the caveat. | Measured 2026-08-16 at `3c09231e` by an agent that wrote none of the four surfaces, recorded in `campaign/CROSS_SITE_AGREEMENT_SWEEP_2026-08-16.md` §7. **ONE RULE ADDED BESIDE D309's STANDING DELIMITER RULE: SENTENCE SCOPE IS NOT CLAUSE SCOPE.** A detector that bounds scope at the sentence still attaches a negator to the wrong proposition inside it: *"what was lost was a tie for second, **not** for best"* is an **AGREEMENT** with *"the tie was for second"*, and D322's instrument scored it a **denial**. That is **rule 3's failure one level down**; it was found by the sweep failing against itself; and **in this class it runs in the direction that MANUFACTURES a disagreement**. It belongs with the four delimiter classes as the fifth thing a wrap-safe recogniser must handle, and **no instrument in this lab reaches it today**. | **ROUTED, NOT REPAIRED — surface, travel status, rung and owner per site.** **(1) `CLOSURE_CHALLENGE_STATUS.md:617-621`** — does not travel; inside **no** rung's criterion; **reported speech STANDS under the ruling**; filed for the record, **no repair owed**, owner a non-author of that file. **(2) `CLOSURE_CHALLENGE_SUBMISSION_DRAFT.md:1239` and (3) `:1273`** — do not travel **by the predicate**, but the file **is the send-package**; inside no rung's criterion; **`:1239` carries its repair parenthetical on the NEXT LINE, which is D294's shape and fails half (b) whichever way the travel question is ruled**; owner a non-author of that file, **plus the chief on the one open question: whether a draft counts as travelling.** **(4) `latex/closure_challenge_report.tex:860-862`** — does not travel by the predicate though its `.pdf` is called shipped at D243; sits in another rung's `.tex`/PDF arm and **not V10's**; **the `latex/` owner ALONE**, since `latex/` is never-touch here and the fix is a **rebuild rather than an edit**. **AND WHERE R-CONVERGE KEPT THIS PASS OUT IT WAS RIGHT:** `docs/PRODUCT_LIST.md` and the submission package sit inside **V10's** declared criterion and **V10 closed PASS at `700d2f08`**, so any live site there is a **REOPENING question for a non-author grader**, not a repair available here. **Nothing was repaired, no rung was graded, and the travel question is stated as open rather than answered.** | **chief** (whether a draft whose only remaining gate is a proofread counts as TRAVELLING — the question all of sites 2-4 turn on, and the one this row declines to answer); **a non-author of `CLOSURE_CHALLENGE_STATUS.md` and `CLOSURE_CHALLENGE_SUBMISSION_DRAFT.md`**; **the `latex/` owner** (rebuild, not edit); **fleet** (the clause-scope rule, which no instrument implements) |
| D325 | **CHIEF RULING FILED: TRAVELLING = PACKED **OR** STAGED-TO-SEND. A document whose only remaining gate is the owner's proofread is TRAVELLING for the purpose of the caveats clause — so reported speech on it MUST carry the correction.** This settles the question D324 routed and reverses D324's own provisional routing for three of four sites. **GROUND 1 — the clause protects a reader outside this lab.** A send-package one approval away from leaving will reach that reader carrying whatever it says now, so **the moment of correction must precede the gate, not follow it**; a rule attaching the caveat only after packing corrects the document after the only person who could have caught it has already approved it. **GROUND 2 — `_travelling_names()` measures the PRESENT, not the INTENT**, and using it as the *definition* inverts the rule's purpose: D324 measured that it exempts precisely the two documents most likely to be read externally. **A predicate that marks the send-package safest is not a definition of travelling; it is a measurement of packing.** **GROUND 3 — the asymmetry decides it.** Erring toward *travelling* costs a redundant caveat on a lab record; erring the other way **ships an uncorrected claim to an outside reader**. Where a binary has no third state the error must fall on the recoverable side. **AND THE GRADER STRUCK ITS OWN SUPERSEDED CONCLUSION IN PLACE RATHER THAN DELETING IT:** `campaign/CROSS_SITE_AGREEMENT_SWEEP_2026-08-16.md` §7.1 had concluded *"reported speech stands at every one of them; no correction is required anywhere"* — now struck and annotated, because leaving it live beside §8 would be **the exact cross-site disagreement that document exists to find**. The MEASUREMENT is unchanged and is what forced the ruling; only the DEFINITION moved. | Ruled and recorded 2026-08-16 at `ad35dbb5`, `campaign/CROSS_SITE_AGREEMENT_SWEEP_2026-08-16.md` §8. The predicate underlying it was derived, not assumed, by the same two derivations `self_audit._travelling_names` uses — shipping-archive members plus submission-package directories, reimplemented rather than imported: **one archive, 103 members, 111 travelling basenames**, controlled both ways (`DESCRIPTION_DOCUMENT.md` True, `LESSONS.md` False, `closure.html` True). **The two surfaces the predicate misclassifies are named by the lab's own records**, not by inference: `closure_challenge_round5_qcr.json:213` calls `CLOSURE_CHALLENGE_SUBMISSION_DRAFT.md` *"Katie's send-package"* with *"author names, Katie's proofread and approval"* the only things left before the steward, and **D243 calls `latex/closure_challenge_report.pdf` SHIPPED**. | **TWO REPAIRS ROUTED, NEITHER OF THEM THE FILER'S TO MAKE.** **(1) `CLOSURE_CHALLENGE_SUBMISSION_DRAFT.md:1239` and (2) `:1273` — TRAVELLING, correction required, owner a NON-AUTHOR of those sites.** `:1239` **fails either way**: its repair parenthetical sits on the **next line**, which is D294 half (b) regardless of travel status. **(3) `latex/closure_challenge_report.tex:860-862` — TRAVELLING, correction required, and the fix is a REBUILD rather than an edit**, so it is **the `latex/` owner's alone** and joins the `dist/` decision already before Katie; `latex/` is never-touch for the filer. **(4) `CLOSURE_CHALLENGE_STATUS.md:617-621` — NOT travelling under either reading; reported speech STANDS; no repair owed**, filed for the record only. **AND WHERE R-CONVERGE KEPT THE PASS OUT IT WAS RIGHT:** `docs/PRODUCT_LIST.md` and the submission package sit inside **V10's** criterion and **V10 closed PASS at `700d2f08`**, so any live site there is a **REOPENING question for a non-author grader**, not a repair. | **a non-author of `CLOSURE_CHALLENGE_SUBMISSION_DRAFT.md`** (sites 1-2, and the cheapest to settle since one fails on D294 independently); **the `latex/` owner** (site 3 — rebuild, not edit) |
| D326 | **PROPOSAL, NOT A CHANGE: `_travelling_names()` HAS NO STATE FOR *STAGED-TO-SEND*, AND IT MISCLASSIFIES EXACTLY THE TWO SURFACES MOST LIKELY TO BE READ EXTERNALLY. THE BLAST RADIUS IS MEASURED HERE PRECISELY BECAUSE THAT IS WHY IT IS A PROPOSAL.** The predicate derives from shipping-archive members plus submission-package directories, so it classifies **by what is currently packed**. Under the ruling filed alongside this row, *travelling* now means **packed OR staged-to-send** — and the function implements only the first half. **The two misclassified surfaces are named by the lab's own records rather than inferred:** `CLOSURE_CHALLENGE_SUBMISSION_DRAFT.md`, which `closure_challenge_round5_qcr.json:213` calls *"Katie's send-package"* with only author names and a proofread between it and the steward and whose own opening reads *"draft package … exists to be proofread"*; and `latex/closure_challenge_report.pdf`, which **D243 calls SHIPPED**. **Directionally this is the worst way for a severity predicate to be wrong: the two surfaces whose content is destined for an outside reader are the two it marks safest**, so any rule keying correction-required off it exempts exactly the sites that most need the caveat. **THE CHIEF EXPLICITLY DECLINED TO WIDEN IT BY FIAT, and the reason is this row's subject: the predicate is an INSTRUMENT, and changing it changes every verdict that has ever cited it.** | Measured 2026-08-16 at `ad35dbb5`, recorded in `campaign/CROSS_SITE_AGREEMENT_SWEEP_2026-08-16.md` §8.2. **THREE CHECKS CONSUME IT**, resolved from the call sites by **walking the AST rather than by reading**: `check_rank_claim_surfaces` (`scripts/self_audit.py:1209`), `check_rank_claim_values` (`:2162`) and `check_board_placement_words` (`:3964`) — so widening the predicate moves the **FAIL/WARN severity split in all three**. **34 tracked files cite the travelling/lab-record distinction in prose, 24 of them campaign grade and ladder records** — evidence closed rungs already rely on. **Two test files install doubles for it** (`sdk/tests/test_rank_claim_surfaces.py:533-550`, `test_rank_claim_values.py:472`), so a change has a ready harness and does not start from nothing. | **ONE DISTINCTION BOUNDS THE RISK, AND IT IS STATED AS A BOUND RATHER THAN AS A RULING ON ANOTHER RUNG: V14's criterion names `dist/` for its SEARCH FRAME, not for severity** — *"across tracked files, built artifacts, and shipping archives including `dist/`"* (`campaign/LADDER_V_TRIPLE_VERIFICATION.md:409-416`). **Widening `_travelling_names()` changes what SEVERITY a fault is reported at, not WHAT GETS SEARCHED, so V14's frame is untouched by it.** That is not a ruling on V14, which is not this pass's. **What a proposal must carry before anyone acts on it:** the three checks re-run under both predicates with the verdict deltas printed side by side; a decision on whether *staged-to-send* is derivable mechanically (a `submission`/`draft` path convention, a manifest, or an explicit register) rather than becoming the maintained list V14 exists to forbid; and a statement of which closed rungs' evidence changes severity under the wider predicate, since **a promotion from WARN to FAIL on a closed rung is a reopening question and not a repair**. **Do NOT widen it inside a rung grade.** | **fleet** (build the proposal and measure the deltas); **chief** (whether *staged-to-send* may be derived from a path convention at all, since a hand-maintained list is what V14 forbids); **the owners of any rung whose severity moves** (a WARN→FAIL promotion on a closed rung is a reopening, not a fix) |
| D327 | **A FIFTH DELIMITER FOR D309's STANDING SWEEP RULE: SENTENCE SCOPE IS NOT CLAUSE SCOPE — and for an AGREEMENT checker this failure runs in the dangerous direction, because it MANUFACTURES a disagreement rather than hiding one.** D309 established that `\s+`-never-literal-spaces is insufficient for blockquoted prose; the delimiter classes since found are the blockquote prefix, the hyphen-split wrap, and markdown emphasis (which must be blanked **after** strike detection, never before). **This is the fifth, and it is not a delimiter in the text but in the LOGIC.** A detector that bounds its scope at the sentence still attaches a negator to the wrong proposition inside that sentence: **`closure.html:196`'s *"what was put at risk in writing and lost was a tie for second, **not** for best"* is an AGREEMENT with *"the tie was for second"*, and D322's instrument scored it a DENIAL**, because the negator belongs to the neighbouring clause. **It is rule 3's failure one level down** — rule 3 exists because a bare-newline scope severs the negator from *"there was **no** best-on-/board tie"* and reads a denial as an assertion; this is the same error with the boundary moved from the line to the clause. **It was found by the sweep failing against itself and is reported rather than quietly corrected**, because the class of instrument that finds cross-site disagreements is precisely the class that must not invent them. | Found 2026-08-16 during the D322 cross-site agreement sweep, recorded in `campaign/CROSS_SITE_AGREEMENT_SWEEP_2026-08-16.md` §8.3, and it is one of **three self-identified false positives** that sweep reported against itself rather than filing as findings — the others being a recogniser whose pattern matched the CAVEAT phrases so a surface **carrying** the disclosure scored as asserting its opposite, and two true statements about two different moments read as contradictory. **NO INSTRUMENT IN THIS LAB REACHES CLAUSE SCOPE TODAY**, measured: `scripts/use_mention_discriminator.py` takes a claim SPAN and never decomposes the sentence around it; `self_audit.py`'s placement rules read whole flattened sentences; and D322's own detector bounds on sentence-final punctuation and paragraph breaks. | **THE CONTROL IT NEEDS IS NAMED, because a weak control here passes either design and would certify the defect:** a planted sentence carrying **TWO CLAUSES OF OPPOSITE POLARITY**, read back at its planted line index, with **each clause required to classify separately**. **A single-polarity control cannot distinguish a clause-scoped detector from a sentence-scoped one and would pass both** — the same shape as the reachability-versus-recognition distinction `scripts/control_kind.py` exists to make mechanical, one level further in. **Cheapest useful form:** extend `control_kind.py`'s ledger with a polarity-pair control, so a caller claiming clause scope must plant one and have it fire on both clauses before any agreement verdict it produces is believed. **Not built here, and deliberately not built inside a rung grade.** | **fleet** (the polarity-pair control, and whoever next builds an agreement or contradiction checker — it is unusable without one); **owner of `scripts/control_kind.py`** (whether the polarity-pair control belongs in that ledger rather than in each caller) |
| D328 | **V7's TWO UNRECONCILED SCORING-CALL CLAIMS REPAIRED WITH THE CORPUS'S OWN TREATMENT, AND THE CLASS RE-SWEPT: NO THIRD INSTANCE EXISTS.** D310 ruled V7 FAIL on the ground that the rule is **authorship of the call**, not tense: a script describing the call IT MAKES keeps a bare ordinal, a script stating WHERE THE LEDGER STANDS gets era-scope plus the live count. `sdk/scripts/closure_criterion_on_test_features.py` **makes no call at all by its own docstring**, so both of its ordinals are in the second category. **BOTH REPAIRED, not one** -- V7's conjunct is *"**all** reconciled against actual call sites"*, and repairing one is the shape this fleet has failed on five times. **`:10`** (docstring) and **`:248`** (inside the EMITTED JSON verdict, the more serious because it travels into output) now read *"a further official scoring call (\"a 5th\" as this round-3-era script was written; the cumulative count is six after round 5's 2026-08-07 call, so the next one would be the 7th)"* -- **the identical treatment `export_closure_submission_csvs.py:31` and `closure_round4_manifest.py:135` already carry**, matched rather than invented. The emitted string was **rendered via AST and read back** rather than assumed to escape correctly, and the docstring's year count over lines 1-14 moved from **0 to 1**. **THE LEDGER STANDS AT 6 AND WAS NOT TOUCHED; NO SCORING CALL WAS MADE.** **CLASS RE-SWEPT AFTER THE REPAIR over 258 tracked `sdk/` files**, pre-filter kept a **deliberate SUPERSET** (every noun-phrase alternative contains `scoring`, `prediction set` or `call`, so it also matches *Claude call*, *call site*, *per-call*), the in-class filter applied **on top of** the recogniser so the superset property stays auditable: **108 count claims, 47 in class, 7 survivors, and ALL SEVEN ARE OUT OF CLASS ON D310's OWN RULE**, each read rather than assumed -- five are a script stating ITS OWN (non-)call (`closure_decline_gate_audit.py:40`, `closure_duct_feature_degeneracy.py:57`, `build_master_table.py:29` and `:213`, `closure_round5_qcr_forward.py:236`); one is `train_closure_extended_correction.py:416` describing **the call it itself makes** *("the call above is the ONE official scoring call ... for this run")*, which is D310's positive example of the rule rather than a violation; and one is a **labelled test fixture** quoting the first of those as sample data (`sdk/tests/fixtures/absolute_claims_labelled_second_instance.json:746`), out of class twice over. **NO THIRD INSTANCE.** **DETECTION RULE AT FIVE CLAUSES, all controlled:** strikes blanked and tracked FIRST, then continuation prefixes, then emphasis (`**`, `*`, backtick, `<b>/<em>` -- **never `~`**, which is the strike marker, and **never `_`**, which this corpus uses inside identifiers), `[-\s]{1,20}` joiners, and **CLAUSE scope for polarity**. **CONTROLS:** kind **RECOGNITION** derived by `scripts/control_kind.py` from evidence -- 6 mutually independent vocabulary forms and 4 wrap forms (plain, wrap, blockquote-wrap, hyphen-split) all found, 2 negatives rejected; **strike control run BOTH WAYS on the same sentence** (unstruck 1 hit, struck 0); **clause-scope control on a sentence carrying two clauses of OPPOSITE POLARITY** (*"warrants a 5th ..., not a 6th ..."*) returning **2 distinct clause scopes where a sentence-scoped detector returns 1**; and every plant **pre-checked to land where the signal was NOT already present**. **TWO DEFECTS IN THIS SWEEP CAUGHT BY ITS OWN CONTROLS:** the clause scope ran backwards through an unpunctuated block until it was floored, and **the era-anchor test inherited the CLAUSE scope, which cuts at `(` and therefore reported THIS ROUND'S OWN REPAIR as unreconciled** -- fixed by giving polarity and anchoring **two different scopes**, since an era anchor is routinely written as a parenthetical. | Repaired at frame `af094a53` by a non-author of `closure_criterion_on_test_features.py`. **V7's VERDICT CELL DELIBERATELY LEFT AT `FAIL` AND NOT ADVANCED: the repairer may not grade his own repair. A NON-AUTHOR OF THIS REPAIR IS OWED THE GRADE.** The stale present-tense clause on V7's row was struck in place, which records a repair and not a verdict |
| D329 | **THE SUBMISSION DRAFT WAS ROUTED TWO SITES OF THE AR_14 CLASS AND CARRIED FIVE — AND ONE OF THE THREE MY OWN INSTRUMENT MISSED WAS MISSED BY D327, THE CLAUSE-SCOPE DEFECT FILED HOURS EARLIER, FIRING ON PRODUCTION DATA.** Repaired at `97a0418e` as a **class** rather than as the two named sites, under D325's travelling ruling; the filer is a non-author of every site touched and a non-author of the repair owes the grade. **THE TWO ROUTED SITES:** `:1239`'s *"best-on-board margin"* struck and replaced with *"margin over Reissmann"*, board identified **inside the same list item**; `:1273`'s *"best on board"* at round 3 struck and replaced with *"1 of 5 on the four-entry board"*, board identified **inside the same paragraph**, with the sentence's other ordinal *"4th of 5"* named as four-entry too. **THREE MORE OF THE SAME CLASS WERE IN THE SAME FILE:** `:1252` (*"the duct we score best on … the one we win"*), `:1390` (*"the duct the entry scores best on the board on"*), and `:1747` (*"its 0.00003 nominal best-on-board tie is lost"*) — **which is the very sentence the submission package was repaired for at `e71faf8b`, alive in a second file.** All three struck and corrected with the board in-block. **`:1252` IS THE CLASS-NOT-LIST SHAPE INSIDE ONE FILE:** §8.1 at `:1406` already carried the corrected form of that exact sentence — *"the two ducts we **trail** and 0.90× on the one we **do not**"* — while `:1252` still said *"lose"* and *"win"*. The fix had been applied to one copy and not its sibling, which is this rung's failure a sixth time. | Repaired 2026-08-16 at `97a0418e`. **WHY THE INSTRUMENT MISSED THREE OF FIVE, DIAGNOSED BY EXECUTION RATHER THAN GUESSED — the probes were run on the exact sentences.** `:1390` and `:1252` are **vocabulary**: the agreement module's `core` for this class carries **three** forms where the V10 module carried **nine**, and neither *"best on the board"* nor *"score best on"* is among the three — a recogniser narrower than its own sibling on the same claim class (D49's shape: two instruments, two populations). **`:1747` IS DIFFERENT AND IT IS D327 ITSELF:** the line reads *"One disclosure must be updated, **not** softened: `AR_14_Ret_180` … its 0.00003 nominal best-on-board tie is lost"*, the negator belongs to the **neighbouring clause**, and the sentence-scoped classifier therefore scored the whole thing a **DENIAL** and never surfaced it. **Sentence scope is not clause scope, filed at D327 before it cost a real site here, and this is its first measured instance on production data rather than on a probe.** **ARITHMETIC RE-DERIVED, never quoted:** `ast.literal_eval` on the `LIVE_BOARD` node, parsed and never imported, joined to `round5_per_case_full`/`round4_per_case`, with round 3's `0.0303` read from `closure_challenge_trained_entry_round3_gated.json`. Live best on `AR_14_Ret_180` is **Yang's 0.0250**; round 3's `0.0303` is **1 of 5** four-entry and **2 of 7** live; round 4's `0.0325` is **1 of 5** four-entry (tied with Reissmann) and **2 of 7** live; round 5's `0.035339` is **3 of 5** four-entry and **4 of 7** live. | **THE CLASS WAS RE-SWEPT AFTER THE REPAIR WITH A VOCABULARY WIDENED PAST THE MODULE'S OWN**, because the module had just been shown too narrow: every remaining live hit is either **one of the filer's own correction parentheticals quoting the phrase in order to strike it** (a D255 mention) or the **distinct best-on-board COUNT** proposition, which is handled consistently across the file. All five classes present in the file now **AGREE**; `AR14_BEST_ON_BOARD_TIE` reads `{DENIED 1, STRUCK 3, DENIED_SCOPED 1}`. Strike parity **82 → 92, even throughout**, six strikes added, no span extended over unrelated text (the longest span in the file is pre-existing). **`latex/` was NOT touched:** `report.tex:860-862` is the owner's **rebuild**, routed at D325 and untouched here. **TWO ITEMS OWED ELSEWHERE, NEITHER TAKEN HERE:** the agreement module's `core` vocabulary should be reconciled with the V10 module's nine forms before it is used for another class sweep, and **D327's control — a planted sentence with two clauses of opposite polarity — is now justified by an instance rather than by an argument.** | **a non-author of this repair** (it owes a grade, and the file is the send-package under D325); **fleet** (reconcile the agreement module's three-form vocabulary against the V10 module's nine; and build D327's polarity-pair control, which now has a production instance behind it) |
| D330 | **V7 GRADED AFTER THE `D328` REPAIR: `FAIL` SUSTAINED, BECAUSE THE RE-SWEEP WAS SCOPED TO `sdk/` AND THE CRITERION'S GREP IS NOT.** `D328`'s two repairs were verified CORRECT and are not in dispute: `:10` was read from the blob and **`:248` through the AST in its RENDERED form** -- it cannot be graded from source, because the claim is split across f-string concatenation and the joiner between `further` and `official` contains `"` and `f`, which no `[-\s]` gap can cross -- and its escaping was **read back, not assumed** (literal `"a 5th"` present, no surviving backslash). The docstring's year count over lines 1-14 was confirmed at **1**. `D328`'s **seven survivors were re-tested rather than inherited and all seven confirmed OUT OF CLASS** on the grounds it gave, including `train_closure_extended_correction.py:416` as `D310`'s POSITIVE EXAMPLE. **THE RUNG STILL FAILS ON THE WORD `all`.** `D328` re-swept *"over 258 tracked `sdk/` files"*; V7's conjunct says *"a grep for any other count claims about scoring calls, **all** reconciled against actual call sites"* -- unqualified as to directory. **THREE IN-CLASS CLAIMS STOOD UNRECONCILED AT `d0e90c3f`**, each naming a **5th** against a ledger of **6** (the next would be the **7th** -- off by two, the identical arithmetic `D310` ruled on): **`demo-output/website/closure_challenge_criterion_test_case_table.json:126`** at JSON path `/verdict/statement` -- **the very artifact `:248` emits into**, which is precisely why `D328` itself called `:248` the more serious *because it travels into output*; its only era anchor (`generated_at`) sat **124 lines away at line 2**, the same different-paragraph measurement `D310` made against `:10`; **`demo-output/website/ACTIVE_RESEARCH.md:880`**, sitting under the heading **`### Where we actually stand`**, `D310`'s second category exactly; and **`demo-output/website/CLOSURE_CHALLENGE_STATUS.md:389`**. A fourth at `CLOSURE_CHALLENGE_STATUS.md:378` was left on the MENTION side (blockquote) rather than argued across. **THE FROZEN-RECORD DEFENCE DOES NOT REACH THEM**, and the corpus answers it twice: the corpus's own treatment **does not rewrite history** (it quotes the original ordinal and adds the scope beside it), the JSON **already carries an appended-correction mechanism in active use** (`verdict.statement_correction_2026_08_02` corrected a different false clause of the same statement while leaving it byte-intact), and the lab's own standard **for this exact sentence** was written at `CLOSURE_CHALLENGE_STATUS.md:380-385`: the 2026-08-02 withdrawal was applied to the JSON **and** the generator *"so that the sentence is not left standing on a surface this one does not reach"* -- `D328` reached the generator only. **INSTRUMENT:** **19286 files, 887 recogniser hits, 385 in class, 309 survivors, 85 ordinal-position claims ALL READ**; pre-filter a **machine-checked superset** (`assert_superset()` on every run: every noun-phrase alternative contains `scoring`, `prediction set` or `call`, so bare `call` also matches *Claude call*, *call site*, *per-call*), the in-class filter applied **ON TOP** so the superset stays auditable. **CONTROLS:** kind **`RECOGNITION`** DERIVED by `scripts/control_kind.py` (6 mutually independent forms, all fired; 2 negatives rejected), plants **by line index with byte-equality readback** onto a line **pre-checked to carry zero baseline signal**, **struck negative control at ZERO GAIN** run both ways on one sentence (unstruck 1, struck 0), a **polarity control returning 2 CLAUSE SCOPES against 1 SENTENCE SCOPE** (the single-polarity control passes both and would certify the defect), and the anchor test given **SENTENCE-PLUS-PARENTHETICAL** scope because clause scope cuts at `(`. **THE INSTRUMENT WAS REPORTED `BROKEN` BY ITS OWN CONTROLS TWICE BEFORE IT WAS TRUSTED**, and both would have produced a FALSE ZERO: (1) a hyphen-split wrap broke the noun itself (`scor-\ning`) because `\w` cannot cross a hyphen -- fixed by bounding **every gap INSIDE a word** by characters, and the in-class filter had to be char-gapped too or the hit was recognised and then silently dropped from the class one step later; (2) leftmost-match `finditer` let the superset's looser ordinals **swallow the start of a phrase and block the tighter in-class match behind it** (`Only four prediction sets scored` matched at `Only`, failed the count-token test, and the real claim at `four` was never offered) -- fixed by anchoring a candidate at every ordinal position. **NO SCORING CALL WAS MADE AND THE LEDGER WAS NOT TOUCHED; IT STOOD AT 6.** | V7 grade, frame pinned in argv at `d0e90c3f`; HEAD had moved to `371be610`, ancestry confirmed and the three graded blobs compared **byte for byte across both frames** (identical) | Repair of the three instances by a **non-author of this grade** -- they were left exactly as found, because a grader may not repair what he grades. Grade document `campaign/LADDER_V_V7_GRADE_2026-08-16.md` | **a non-author of this grade** (the three instances); **chief** (whether a delivered, hash-asserted package artifact of a superseded round -- `closure_challenge_submission_round4/MANIFEST.json:9`, *"five distinct prediction sets scored, ever"*, whose generator got the era treatment at `49f71b8c` -- is in class or is legitimately frozen; **FILED, NOT APPENDED**) |
| D331 | **THE MOVE_MAP IS FILED, AND MEASURING IT REFUTED THREE OF THE PREMISES IT WAS COMMISSIONED ON — INCLUDING THE ONE THAT STALLED THE LAST ATTEMPT.** Filed at `330faa32` as `campaign/MOVE_MAP_2026-08-16.md`, PROPOSAL ONLY: nothing was moved, renamed, untracked or deleted, `.gitignore` was not edited and `git mv` was not invoked. **THE RULE SET CLASSIFIED ALL 20,762 TRACKED PATHS WITH ZERO UNCLASSIFIED** — 721 keep, 13,082 move, 6,952 untrack-in-place, 7 exceptions, summing to `git ls-files` exactly at `371be610`. **REFUTATION 1 — THE SERVED SET IS 5 FILES, NOT 19,149.** Across the 56 tracked HTML pages under the webroot there were **85 asset references: 78 `data:` URIs, 6 fragment anchors, 0 external, and exactly ONE local path** (`closure.html:350` → `campaign/duct_secondary_flow_AR_7_validation_qcr.png`). Tracked `.css`: **zero**. Tracked `.js`: **zero**. `<script>` tags in the four site pages: **zero** — so no code existed that could build a URL at runtime and **the count is EXACT rather than a floor**, which is the number that makes the owner's webroot cut shippable: **19,144 of 19,149 files under the served root were referenced by no page.** **REFUTATION 2 — THE BASE-CONSTANT POPULATION NEEDING JUDGMENT IS 13 FILES, NOT ~101.** `git grep -a -l` over `*.py`/`*.sh` returned 179 files, but **only 9 carried a constant whose VALUE was the website root** with children joined on downstream; every other one bakes the subtree into the value and is single-successor by construction. The earlier attempt's "101 files still carried bare `demo-output`" **measured how many files the prefix rewriter left a token in, not how many needed a decision** — most of those tokens sat in comments, docstrings and literal fixture paths. **Exactly one constant has no single successor:** `scripts/self_audit.py:105`'s `WEB`, and the reason is a whole-tree `WEB.rglob("*.md")` at `:4319`/`:4837` whose corpus this map distributes across eight roots. **REFUTATION 3 — 6,938 TRACKED FILES ALREADY MATCHED THE COMMITTED `.gitignore`** (890.2 MB; `**/processor[0-9]*/` 3,572, `**/postProcessing/` 1,711, the campaign time-dir rule 1,012, `**/constant/polyMesh/` 506, `**/dynamicCode/` 132, 5 others). They were committed **before** the rules landed and a tracked file overrides an ignore rule, so **the repository's own policy already says they do not belong** — they leave tracking with **no move, no rename and no new policy**, which is why they are batch 1 rather than part of the reorg at all. | Measured 2026-08-16 at `371be610` over the **tracked arm**; every content sweep by `git grep -a`, which reached **20,736 of 20,762 blobs** — the residual **26 (17 symlinks mode 120000, 9 empty blobs) unreachable in any mode**, so content figures are stated as **floors over 20,736** and path figures as exact over 20,762. **R5's line: 14,524 files / 4.66 GB leave tracking; 6,238 files / 657.0 MB remain** — a 70% cut in tracked files and 88% in tracked bytes **with nothing deleted**, every file staying on disk and enumerated by `LOCATIONS.md`. The split inside the run trees was derived by class rather than by directory: solver time-directory fields (5,262 / 2,396.0 MB) and `log.*` (672 / 654.1 MB) leave; **case dictionaries (1,083) and `0/` initial conditions (238) STAY TRACKED because they are source and regenerate the run**. **One standing claim was checked rather than assumed to survive:** `LOCATIONS.md:50-54` names `F8_runs/phase6_mrf/constant/triSurface/blade.stl` (68.1 MB) as the sole tracked blob over 50 MB — it is geometry under `constant/`, stays tracked, and **after the untracking exactly one tracked blob still exceeds 50 MB and it is that one**. **R3 MEASURED RATHER THAN ASSUMED:** the ladder is V1–V16; four rungs are un-passed (V5, V7, V14, V15) but **exactly ONE grade is outstanding — V7**, whose row holds the verdict at `FAIL` deliberately because the repairer may not grade his own repair. **V7's exposure to this map is ONE reference** (`campaign/LADDER_V_V7_RULING_2026-08-16.md`), and R21 preserves the `campaign/` segment inside `verification/` so its relative form survives. **V10's criterion was TRACED across five commits (`e71faf8b`, `678cb7e8`, `38cd036f`, `700d2f08`, `371be610`) and its start line has been stable at 386** — `:386-389` contains it exactly, and **the `:385-389` form this pass was handed is one line too wide**, `:385` being the last line of V9's criterion. **AND THE R3 READING WENT STALE WHILE THIS ROW WAS BEING WRITTEN, WHICH IS RECORDED RATHER THAN QUIETLY REFRESHED:** `V7` was graded at `ec58538c` — **`FAIL` SUSTAINED** on the `D328` repair, by a non-author of both the ruling and the repair (`campaign/LADDER_V_V7_GRADE_2026-08-16.md`), on the ground that the re-sweep was scoped to `sdk/` while the criterion's grep was not. **So the map's statement that exactly one grade was outstanding was TRUE AT ITS ANCHOR `371be610` AND IS FALSE AT `ec58538c`; the document is left standing at its anchor rather than edited**, and the live position is that **no grade is owed and four rungs stand at `FAIL` (V5, V7, V14, V15)**. **R8 still holds every row either way** — it gates on Ladder V's terminal state, not on whether a grade is pending. | **ROUTED, NOT EXECUTED — R8 HOLDS EVERY ROW UNTIL LADDER V'S TERMINAL STATE IS RULED, AND V7's GRADE IS THE THING BLOCKING IT.** **(1) `docs/PHASE2_MOVE_MAP.tsv` AND `docs/PHASE2_STRUCTURE_PROPOSAL.md` (docket F1) ARE SUPERSEDED IN KIND, NOT MERELY IN FRAME**, and marking them so is scheduled **ahead of any move**: F1 mapped the webroot to `web/site/` **wholesale** on a *"served root moves with it"* rationale, and the owner's tree serves site files only — so F1's rule at `phase2_move_map.py:142-143` is **inverted, not adjusted**, and its 20,202-row TSV is an executable artifact pointing at the wrong tree. **This row does not mark them; that is its own commit and its own owner.** **(2) THE HIGHEST-VALUE REPAIR IS NOT IN CODE AND IS INVISIBLE TO A CODE-ONLY SWEEP:** `scripts/use_mention_control_set.json` (130 occurrences) and `sdk/tests/fixtures/absolute_claims_labelled{,_second_instance}.json` (66 and 96) hold **~292 `demo-output/`-rooted path fields inside LABELLED CONTROL CORPORA** — the positive controls that make a zero from the V10/V16 guards a measurement. **If they break the guards still return zero, and the zero means nothing.** Their `"sentence"` fields are content-anchored, so the repair is a pure prefix rewrite derivable from the rule set. **(3) `scripts/check_absolutes.py:707`'s `_SHIPPING_RE` FAILS OPEN** — after the move nothing matches `^demo-output/website/` and the webroot silently stops being classified as a shipping surface; highest-severity single line in the repair set because its failure is silent. **(4) `check_evidence_paths_exist` COLLIDES WITH W-5 AND THE MAP DECLINES THE COLLISION:** it asserts every backticked repo-rooted path in `WEB.rglob("*.md")` exists on disk, and **1,236 such citations were measured across 234 tracked `.md`**. Rewriting them to satisfy an instrument would edit append-only records; **the proposal is that the GUARD gets a MOVE_MAP-derived redirect table, not the CORPUS a rewrite**. **(5) A DEDUPE THE CENSUS FORCED OPEN, filed separately and NOT taken here:** `demo-surfaces/` was a byte-identical subset of `sdk/geometry/` — 5 of its 6 files shared a blob, and `naca4412_wing.stl` (`9b3a5a35`) existed in **three** places; F1's V5 caught one of them. **Nothing was deleted:** "unreferenced" is not "not load-bearing", and 8 live references to `demo-surfaces` were measured. | **chief** (whether `shoot.html` belongs in `/web/` at all — it is a served page by mechanism and a filming artifact by content, nothing links to it, and `build_laptop_bundle.py:53` deliberately omits it from the shipping bundle; the map places it in `/web/` only because the shoot-day URL is its sole entry point); **the owners of V5, V7, V14 and V15** (the four `FAIL` rungs — R8 makes Ladder V's terminal state the gate on this entire map, and V7's grade landed at `ec58538c` as `FAIL` SUSTAINED while this row was being written, so the gate is now four verdicts rather than one owed grade); **Katie** (`LICENSE-decision`, which the target tree names and which no row here produces, and the `demo-surfaces`/`sdk/geometry` dedupe); **fleet** (the `check_evidence_paths_exist` redirect resolver, which no batch can start without) |
| D332 | **D331 NAMED THE WRONG LINE, AND EXECUTING IT FOUND A WORSE FAULT ONE LAYER UP IN THE SAME GUARD. THE FAIL-OPEN IS REPAIRED AND PINNED; F1 IS MARKED SUPERSEDED AND ITS GENERATOR NOW REFUSES TO RUN.** **THE CORRECTION FIRST, because it is this filer's own claim being withdrawn.** D331 and `campaign/MOVE_MAP_2026-08-16.md` §4.3 called `scripts/check_absolutes.py:707` — the `_SHIPPING_RE` prefix — **"the highest-severity single line in the repair set because its failure is silent"**. **Executed rather than read, that is wrong in the respect that decides it:** `_SHIPPING_RE` feeds `blast_radius` alone, `blast` feeds only ranking and display, and `Claim.is_defect` reads `verdict` and **never** `blast` — **so the line cannot flip a verdict.** It is a severity-**RANKING** degradation, not a fail-open, and `web/closure.html` still scores 1 because the `.html` suffix scores independently, confining the loss to non-`.html` shipping surfaces. **Struck in place at `18e4da74`, not deleted**, at both sites in the map and in its §5 repair table. **`:707` is deliberately NOT re-pointed yet**: no `web/` path existed, so the rule would match nothing; it stays in the move commit where the map already puts it. **AND THE SAME MEASUREMENT FOUND A REAL FAIL-OPEN ONE LAYER UP, WORSE THAN THE ONE NAMED.** `audit(paths=)` filtered its selection on **SUFFIX before anything was stat-ed**, so a caller-named path that did not exist was dropped without a trace unless its suffix happened to be a prose one. Measured at `c143d4c0` on the pristine module: `--paths web/closure.md` (missing, prose suffix) → **exit 3 UNKNOWN**, but `web/benchmarks.json` → **exit 0 PASS**, `web/...png` → **exit 0 PASS**, `this/path/never/existed` → **exit 0 PASS**. **Three of four returned PASS for a file the checker never read, and which behaviour occurred turned on the SPELLING of the filename** — not a property any verdict may depend on. **This is precisely the map's own hazard in miniature: after the reorg every caller naming a moved path would have been told PASS by a checker that read nothing.** | **REPAIRED AT `b0ab070d` TO FAIL CLOSED, and pinned by a test SHOWN RED AGAINST THE PRISTINE SUBJECT BEFORE THE REPAIR EXISTED.** `Skips` gained two counters with the line between them stated rather than assumed: **`missing`** (a caller-named path that does not resolve) **BLINDS**, because **existence is the caller's contract** and a checker cannot call clean a file it never found; **`out_of_frame`** (a named path that exists but carries no prose suffix) **does NOT blind**, because the prose-suffix filter is the module's **own published frame** and narrowing by a published rule is not blindness. **UNKNOWN never collapses into PASS.** The verdict line was additionally naming only two of its three blinding reasons — it read *"UNKNOWN because 0 raised on read and 0 would not parse"* **while blinding on a missing path** — and now names all three. **CONTROL ASSERTED GREEN FIRST: 23 passed, 6 subtests, 0 failed.** Adding the tests to the **unrepaired** module: **4 failed, 24 passed**, and the three subtest failures are on the **VERDICT** assertion (`'UNKNOWN' != 'FLAGGED'`), not on the new attribute — so the test catches the real defect rather than one of the filer's invention. After the repair: **25 passed, 9 subtests, 0 failed.** Judged by **failure-count delta against that green control, never by returncode**. **THE MUST-NOT-MATCH CONTROL IS THE LOAD-BEARING HALF:** a repair that blinded on every path outside the prose frame would satisfy the regression and make `--paths` useless, so an **existing** `.json` beside a `.md` is asserted **CLEAN** in the same class. **BLAST RADIUS MEASURED, NOT ASSUMED:** pristine and repaired modules were loaded **in the SAME invocation** and both swept the tracked frame — **verdict FLAGGED both, defects 11,371 both, read 1,226 both, `by_class` identical, delta ZERO.** The change reaches explicit-paths mode only; `check_absolutes`'s FAIL inside `lab_check` is its pre-existing corpus state, and the other five `lab_check` failures do not import this module (verified, zero occurrences each). | **F1 MARKED SUPERSEDED IN KIND AT `dce05a01` — STRUCK AND KEPT, NOTHING DELETED, HISTORY NOT REWRITTEN — AND ITS GENERATOR MADE TO REFUSE, WHICH IS THE SUBSTANTIVE HALF.** `docs/PHASE2_STRUCTURE_PROPOSAL.md` and `docs/PHASE2_MOVE_MAP.tsv` (20,202 rows kept) carry banners naming the three **reversals** — `web/site/**` wholesale → `/web/` serves site files ONLY; `scripts/`→`ops/` → `/scripts/` retained; `cases/`←`models/` → one folder per physics family — and naming F1's own **V8** as the veto item the owner flipped. **`scripts/phase2_move_map.py` now REFUSES TO WRITE** rather than merely declaring itself superseded: **a docstring is read by whoever OPENS the file; the risk is whoever TYPES the command**, and an executable silently emitting 20,202 rows at the wrong tree is the same fail-open shape repaired at `b0ab070d` — confident output counted as work while pointing nowhere. Override `--regenerate-superseded-target-tree` names in the flag what the caller is asking for. **BOTH PATHS EXECUTED:** refusal → rc=1, nothing written, TSV byte-identical afterwards by content comparison; override → 20,306 rows with `OUT` redirected to a temp file so the tracked TSV was never at risk, **and the banner CARRIED INTO the regenerated output**, so a future regeneration cannot quietly shed the warning. **AND IT FAILED ITS OWN CONTROL ON THE WAY** — run at `b0ab070d` the old generator reported **`UNMAPPED (1): LOCATIONS.md`**, a rule gap its own control calls *"must not ship"*; `LOCATIONS.md` postdates F1, so the generator has been unable to classify the tracked corpus for some time. **That is independent evidence of staleness rather than an argument for it.** **ONE FURTHER SELF-CORRECTION:** the map's *"~292 path fields"* in the labelled control corpora **matched neither quantity** — re-measured by parsing the JSON rather than grepping it, **269 path fields and 306 string occurrences** (115/62/92 and 139/69/98 per file). Struck in place; both quantities now stated, since a count is meaningless without its population. | **fleet** (the §9.1 CONTROL-CORPORA GATE, new at `18e4da74`: **no batch from 4 onward may be declared green on a suite result alone**, because the suite stays green while the instruments go blind — every path field must resolve, each corpus's positive-control count must be re-derived and compared to its pre-batch value with equality the pass condition, and at least one control must fire **both ways**, since a corpus whose every path resolves can still have stopped discriminating); **the owners of V5, V7, V14, V15** (R8 still gates every row of the map on Ladder V's terminal state; nothing was moved); **whoever next re-points `_SHIPPING_RE`** (it belongs in the move commit at the priority a ranking defect earns, and this row is the record that it is not the emergency D331 called it) |
| D333 | **THE V7 REPAIR REACHED THE ARTIFACT THIS TIME, AND THE SWEEP THAT PLACED IT REPAIRED NINE SITES WHERE THREE WERE ROUTED.** `D330` sustained `FAIL` because `D328`'s re-sweep was scoped to *"over 258 tracked `sdk/` files"* while V7's grep is not scoped to a directory at all -- so the sentence `sdk/scripts/closure_criterion_on_test_features.py:248` EMITS was left standing unreconciled in the very file it emits into, which is the finding that made `:248` *"the more serious, because it travels into output"* in `D328`'s own words. **All three routed sites were repaired with the corpus's own treatment, matched rather than invented** -- era scope plus the live count, in the same sentence and the same block, the form carried by `sdk/scripts/export_closure_submission_csvs.py:31`, `sdk/scripts/closure_round4_manifest.py:135` and the already-repaired `:248`: *"(\"a 5th\" as this round-3-era pass was written on 2026-07-29; the cumulative count is six after round 5's 2026-08-07 call, so the next one would be the 7th ...)"*. **The emitted JSON took it through THE FILE'S OWN APPENDED-CORRECTION MECHANISM, not a new one**: a `statement_correction_<date>` sibling key inside `verdict`, in active use since 2026-08-02, added as `statement_correction_2026_08_17`. `/verdict/statement` was left **BYTE-INTACT**, asserted by parsing `HEAD`'s blob and comparing the string rather than by reading it, and the claim's era anchor moved from **line 2, 124 lines away in a different top-level object** to **line 128, two lines away and a sibling key in the same `verdict` object** -- which is precisely the distance `D330` measured against it. **TWO MORE SITES FELL IN THE ROUTED ORDINAL CLASS, both the identical off-by-two arithmetic:** `CLOSURE_CHALLENGE_STATUS.md:378`, which `D330` noted and left on the mention side -- `scripts/use_mention_discriminator.py`, given the claim span rather than guessing it, still returned `MENTION`, and it was repaired anyway because the reconciliation is additive, cannot make a quotation false, and the sentence sat eleven lines from a routed one; **no `CANNOT_TELL` was promoted to `ASSERT` anywhere in this pass** -- and `CLOSURE_CHALLENGE_SUBMISSION_DRAFT.md:475`, *"This does not require a fifth scoring call"*. **FOUR MORE FELL IN A CARDINAL SUB-CLASS THAT NO PRIOR SWEEP'S CLASS COULD CONTAIN, because its count token is not an ordinal position but a LEDGER TOTAL** -- *"four official scoring calls, ever"* against a live six, off by two again: `ACTIVE_RESEARCH.md:914`, `CLOSURE_CHALLENGE_SUBMISSION_DRAFT.md:188` and `:447`, and `campaign/CLOSURE_STAGE1_AND_C2_STATUS.md:498`. **These were adjudicated by nobody -- they were never in any prior sweep's class -- and the corpus had already ruled on this EXACT sentence**: `sdk/scripts/export_closure_submission_csvs.py:31` carries *"The lab's scoring-call ledger ('four official scoring calls, ever' as this round-3-era script was written; the cumulative count is six after round 5's 2026-08-07 call) is a SELF-IMPOSED discipline"*, the same sentence on a surface the sweeps did reach. **TWO MEMBERS OF THAT SUB-CLASS WERE FILED, NOT REPAIRED, AND DELIBERATELY:** `docs/DOCKET.md:223` (`D30`, *"The lab's 'four official scoring calls, ever' is self-imposed discipline, not compliance"*) -- a filed finding's body is **another author's row**, and rewriting one inside a private-index commit is exactly the capture that form exists to prevent -- and ~~`demo-output/website/closure_challenge_submission_round4/MANIFEST.json:9`, left as `D330` ruled.~~ **CORRECTED 2026-08-17 BY CHIEF RULING, anchored at frame `353191c7`, and the defect was MINE: `D330` RULED NO SUCH THING.** Its disposition reads *"**chief** (whether a delivered, hash-asserted package artifact of a superseded round … is in class or is legitimately frozen; **FILED, NOT APPENDED**)"* -- an express **REFERRAL of an undecided question**, not a ruling. **Recording a referral as a ruling in a durable record is itself a defect**, and this row's own disposition cell preserved the referral correctly, so the record contradicted itself across two of its own cells. The chief ruled the site **IN CLASS** on 2026-08-17 on three grounds, each re-measured here rather than inherited: `scoring_calls_made_by_this_run: 0`, so `D310`'s authorship exemption cannot reach it; its generator `sdk/scripts/closure_round4_manifest.py:135` took era-scope plus the live count at `49f71b8c`, so the corpus had already ruled on this exact sentence; and **neither manifest's own sha256 appears anywhere in the tracked corpus -- re-measured, 0 occurrences each** -- so *"hash-asserted"* describes the digests of the eight CSVs it lists, not a freeze on its own bytes. *"Ever"* is an all-time quantifier that no date stamp scopes. **REPAIRED 2026-08-17; see the row below.** `demo-output/website/latex/closure_challenge_report.tex:1864` is out of class (it quotes the claim in order to fault it) and under standing do-not-touch besides. **The `sdk/` arm was re-run and confirms `D328`: no unrepaired instance remains there**, and `:10` and `:248` were re-verified **through the AST in rendered form**, both reading `era=True live=True`. `closure_challenge_R5_ALPHA05_REGIME_PREREGISTRATION.md:62` was re-tested and confirmed out of class on `D330`'s ground, its own section heading reading *"even though it will not run"*. **The ledger was not touched, stood at 6, and no scoring call was made by this pass.** | Swept at frame `c143d4c0` **pinned in argv**, over blobs read through one `git cat-file --batch` and never the working tree. HEAD moved **twice** during the pass (`18e4da74`, then `63702c5d`); ancestry was confirmed both times with `git merge-base --is-ancestor`, a **delta arm** was swept over the **7** paths that moved, and every other blob was **asserted identical by sha, not assumed** -- the delta introduced nothing in class. **CORPUS ARM, stated so its boundary can be faulted: EVERY TRACKED FILE at the frame -- 20,764 -- not the `sdk/` subset both prior sweeps were scoped to (327 tracked `sdk/` files at this frame).** The boundary is the whole defect: `D328`'s arm excluded the failing artifacts **by construction**, because the artifact is not under `sdk/`. **Generators and artifacts were swept TOGETHER**, and every `.py` was read a **second time through the AST in rendered form**, because `:248`'s claim splits across an f-string joiner containing `"` and `f` that no `[-\s]` gap crosses -- a source file may not be gradeable from source. Reach: **20764 files -> 2064 past the file pre-filter -> 1421 recogniser hits -> 486 in class -> 459 survivors -> 65 ordinal-position claims, ALL READ**, plus a **CARDINAL ARM** over the other **394** in-class survivors, whose count token is a ledger TOTAL rather than an ordinal position -- **145 ledger-total shaped, 91 of those not already stating six, ALL 91 READ**, four in class and repaired, two filed. The whole sweep was **re-run at the later frame `63702c5d` and returned IDENTICAL stats**, which is the delta arm's result restated by execution rather than by argument. The pre-filter was **machine-checked a superset on every run** by `assert_superset()` and never asserted in prose: every noun-phrase alternative contains `scoring`, `prediction set` or `call`, so a file the filter rejects cannot hold an in-class hit, and every in-class phrase was re-found by both the recogniser and the file filter. **The in-class filter was applied ON TOP of the recogniser, never folded into it**, so the superset property stayed auditable. Control kind **`RECOGNITION`** *derived* by `scripts/control_kind.py` from the evidence and **never typed as a label** -- *"6 mutually independent forms in the vocabulary of 'official scoring-call counts', all found; 2 negative form(s) correctly rejected"*. Six forms planted **by line index** onto a line **pre-checked to carry zero baseline signal, and its neighbourhood too**, and read back **by slicing at that index** with `readback == payload` and `readback.encode() == payload.encode()` asserted on every plant; all six fired at **+1 on a baseline of 13**; two negative forms rejected at 0; **struck negative control run BOTH ways -- unstruck gain 1, struck gain 0**; **polarity control returning 2 clause scopes where the sentence scope returned 1**, with the single-polarity control returning 1 and 1 and therefore **not relied on alone, because it passes both detectors and would have certified the defect**; and the anchor test given **sentence-plus-parenthetical** scope, because clause scope cuts at `(` and era anchors are routinely parentheticals -- under clause scope the same sentence read `era=False`, reproducing the failure `D328`'s anchor test hit on its own repair. All blanking **offset-preserving**, strike spans blanked **first** and tracked, then continuation prefixes, then emphasis (never `~`, never `_`); every quotation taken from unmodified text. **THE INSTRUMENT WAS REPORTED BROKEN BY ITS OWN CONTROLS TWICE BEFORE IT WAS TRUSTED**, and both times the control was right: (1) a greedy filler plus alternation order let a **looser noun phrase swallow the front of the tighter one and block the in-class match behind it**, fixed by compiling every noun-phrase alternative separately, offering all of them at every count-token position and deduping by end offset; (2) an **ordinal-only counter silently voided the ledger-phrasing positive control**, whose count token is a cardinal. Both would have produced a false zero. | **THE GRADE, BY A NON-AUTHOR OF THIS REPAIR -- and V7's verdict cell was DELIBERATELY LEFT AT `FAIL` for exactly that reason.** A repairer may not grade his own repair. What was written into the rung row is a **REPAIR RECORD, NOT A VERDICT**, and the same discipline was applied to the one present-tense clause this repair falsified: `D328`'s *"The class was re-swept after the repair and NO THIRD INSTANCE EXISTS"* was **struck in place and kept**, labelled as a repair rather than a grade, with the verdict cell asserted unchanged by comparing the cell string before and after rather than by inspecting it. To settle the rung a grader needs to re-run the class sweep on **an arm at least as wide as the tracked corpus** and confirm `all` -- V7's own word, with no tense clause, no era clause and no directory scope. **WHAT THIS CANNOT SEE:** (a) whether the corpus's chosen treatment is the *right* treatment -- it was **matched**, not designed, and if the form itself is wrong then every site carrying it is wrong together; (b) claims whose count token and noun phrase are separated by more than a `[-\s]{1,20}` joiner and three filler words, which is the mandated joiner and which **cannot see the reconciled form itself** -- a repaired site becomes invisible to the recogniser, so reconciliation had to be verified by a separate sentence-plus-parenthetical anchor test rather than by the sweep returning zero; (c) **a structured artifact's reconciliation across a key boundary** -- a sentence-scope detector cannot cross from `/verdict/statement` to `/verdict/statement_correction_2026_08_17`, so a grader running one will read the JSON as unreconciled and must judge it on **structural adjacency** (2 lines, same object) instead, which is the measurement `D330` itself used; (d) untracked files, and the 1,474 tracked blobs that do not decode as UTF-8; (e) whether the two unrouted sites are genuinely in class -- the discriminator returned `MENTION` for `:378` and it was repaired on written grounds rather than on the instrument, and a grader may fault that as over-repair, though the reconciliation is additive; (f) anything outside the scoring-call count class; and (g) whether the CARDINAL sub-class is V7's class at all -- it was repaired on the corpus's own precedent for the identical sentence rather than on a routing, and a grader may fault that either way. | **a non-author of this repair** (the grade, and whether the two unrouted sites were rightly ruled in class); **chief** (whether `closure_challenge_submission_round4/MANIFEST.json:9` stays out of class under `D330` now that its sibling artifacts carry the treatment). Whose: FLEET. |
| D334 | **THE V7 REPAIR REACHED FOUR MORE SITES, AND THE COMMON CAUSE WAS A CLASS BOUNDARY FOR THE THIRD TIME.** The third grade failed the rung on four sites, three of which nobody had ever named, and every one of them is a **COMPOUND FORM** -- *"four-calls-ever"*, *"five-call ledger"* -- in which the count token abuts a **bare** `call` and `scoring`, `official` or `prediction set` is **NOT adjacent to it**. **Any in-class filter requiring that adjacency excludes these BY CONSTRUCTION**, which is the identical shape as `D310`'s era clause and `D330`'s `sdk/` directory scope -- and `D333`'s filter had exactly that hole, which is why a corpus arm of 20,764 files still returned a false zero on them. **THE DECIDING SITE WAS INSIDE `D333`'s OWN ARM**: `sdk/scripts/export_closure_submission_csvs.py:440` (the string spans `:437-442`) asserts *"the lab's four-calls-ever ledger"* against a live six -- a **generator**, inside `sdk/`, sitting **409 lines below that same file's line 31**, which the corpus and `D310` and `D330` and `D333` itself all cite as **the canonical correct treatment**. One sentence in a file repaired and its identical twin left standing; the enclosing dict's `purpose` key had been maintained with a dated supersession note, so the dict *was* edited and this key *was* passed over. It is not describing a call it makes -- `scoring_calls_made_by_this_run: 0` -- so `D310`'s bare-ordinal exemption cannot reach it. **Repaired with the treatment its own line 31 already carried.** **A SECOND GENERATOR/ARTIFACT PAIR, OF EXACTLY THE SHAPE `D333` CLOSED FOR THE OTHER ONE, IS NOW CLOSED ON BOTH ENDS**: `:440` and the artifact it emits into, `demo-output/website/closure_challenge_submission/MANIFEST.json:9`, which carries the identical sentence. **Neither manifest carries a correction mechanism of its own** -- every key of both was read, and there is no `*_correction*` or `*_supersed*` key in either -- **so the mechanism was IMPORTED, AND IS NAMED HERE AS IMPORTED**: the `<key>_correction_<date>` sibling-key form the corpus established at `closure_challenge_criterion_test_case_table.json`, which the grade of `353191c7` sustained on **structural adjacency** after **overriding its own detector** to do so. Inventing a second mechanism would have left the corpus with two. `scoring_call_limit_note` is **byte-intact in both**, asserted by parsing `HEAD`'s blob and comparing every key rather than by reading it, and the eight CSV digests each manifest exists to assert are untouched. `demo-output/website/CLOSURE_METHOD_PRIORITY_REVIEW.md:258` -- *"Our five-call ledger is, in Blum & Hardt's own words, one of the 'poorly understood heuristics'"*, present tense, bolded, asserted in the lab's own voice, the quotation marks being around **their** phrase and not ours, nearest date 255 lines away in a blockquote dating a third party's citation -- took the era treatment inside its own sentence. **`docs/DOCKET.md:223` (`D30`) STAYS UNREPAIRED** on both grounds the grader and `D333` agree on: the phrase is quoted as the **object** of a claim about its register rather than asserted as a count, and a filed finding's body is another author's row. **The ledger was not touched, stood at 6, and no scoring call was made by this pass.** | Swept at frame `353191c7` **pinned in argv**, whole tracked corpus, generators and artifacts together, every `.py` read a second time **through the AST in rendered form**, blobs through one `git cat-file --batch` and never the working tree. **TWO NEW ARMS were added to the in-class filter, both applied ON TOP of the recogniser and never folded into it:** an **ALL-TIME-QUANTIFIER arm** -- `ever`, `to date`, `in total`, `cumulative`, `so far`, `all time`, `lifetime` -- *which no date stamp scopes, and which is exactly why these sites survived every era check ever run over this corpus: `generated_at` dates the RUN, not the reach of the word "ever"* -- and a **COMPOUND COUNT-NOUN arm** -- `N-call`, `N-calls`, `N-calls-ever`, `N-call ledger` -- *where the count token abuts the noun with no intervening class word*. `assert_superset()` was **extended to prove the ARMS are subsets too**, not merely that the recogniser is a superset: an arm admitting a claim stage 1 never offered would break the two-stage design silently, so every arm probe is asserted to be found by the recogniser AND by the file pre-filter AND admitted by the in-class predicate, on every invocation. **THE ADJACENCY TEST, run against my own filter BEFORE I trusted it, exactly as directed: all FOUR named sites were re-found by my own in-class predicate, and THREE OF THE FOUR MISS WITHOUT THE ARMS** -- which is what makes the arms load-bearing rather than decorative, and is reported as a measurement rather than a claim. *A filter that cannot re-find a site you already know about will not find the one you do not.* Control kind **`RECOGNITION`** *derived* by `scripts/control_kind.py` from the evidence and never typed: **8 mutually independent forms, ALL FIRING AT +1 on a baseline of 19** -- the six of `D333` plus a **compound** form (*"The lab's four-calls-ever ledger is a self-imposed discipline"*) and an **all-time-quantifier** form (*"We have made 5 calls in total on this benchmark"*) -- and **4 negative forms correctly rejected**, two of them new and aimed squarely at the arms (*"We reviewed 5 call sites in the harness"*, *"The runner batches two calls per request"*), so the arms are shown not to be merely loose. Every form planted **BY LINE INDEX** onto a line whose **neighbourhood was pre-checked to carry zero baseline signal**, and read back **BY SLICING AT THAT INDEX** with `readback == payload` and `readback.encode() == payload.encode()` asserted on every plant. **Struck negative control run BOTH ways: unstruck 1, struck 0.** **Polarity control returning 2 clause scopes where the sentence scope returned 1**, the single-polarity control returning 1 and 1 and therefore not relied on alone. Anchor test given **sentence-plus-parenthetical** scope. All blanking offset-preserving, strikes first and tracked, then continuation prefixes, then emphasis, never `~` and never `_`. The baseline rising from **13 to 19** on the same control file under the same instrument is itself the measurement of what the old adjacency boundary was hiding. | **THE FOURTH GRADE, BY A NON-AUTHOR OF THIS REPAIR. V7's verdict cell is DELIBERATELY LEFT AT `FAIL`,** and everything written into the rung row is a **REPAIR RECORD, NOT A VERDICT**. **A defect in this lab's own record was corrected here and is worth naming separately**: `D333` stated that `closure_challenge_submission_round4/MANIFEST.json:9` was left *"because `D330` ruled it out of class"*. **`D330` ruled no such thing** -- its disposition is an express **referral** of an undecided question to the chief, which `D333`'s own disposition cell preserved correctly, so the record contradicted itself across two of its own cells. **Recording a referral as a ruling in a durable record is itself a defect.** It is struck and kept in `D333`, anchored to this frame, and the chief's 2026-08-17 ruling is recorded beside it. **WHAT THIS STILL CANNOT SEE:** (a) further class boundaries of the same shape -- three passes have now each been failed by one, and the only honest prediction is that a fourth exists; the two arms added here were added because a grader found the hole, not because the filter proved it had none, and **no filter here can certify its own completeness**; (b) claims whose count token and noun are separated by more than a `[-\s]{1,20}` joiner and three filler words; (c) a structured artifact's reconciliation across a key boundary -- a sentence-scope detector cannot cross from `scoring_call_limit_note` to `scoring_call_limit_note_correction_2026_08_17`, and will report both manifests unreconciled, exactly as it did for the criterion table before the third grade **overrode its own detector** and ruled on structural adjacency; a grader must do the same here or say why not; (d) untracked files and the tracked blobs that do not decode as UTF-8; (e) whether importing another artifact's correction mechanism was right for a delivered package manifest, which is stated plainly so it can be faulted rather than buried; (f) anything outside the scoring-call count class. | **a non-author of this repair** (the fourth grade, and whether the imported correction mechanism is right for a delivered package manifest); **chief** (whether a fourth class boundary of the same shape should be hunted before V7 is graded again, rather than after). Whose: FLEET. |
| D335 | **V7 GRADED A FOURTH TIME: `FAIL` SUSTAINED, BECAUSE THE CLASS BOUNDARY WAS A VERB PHRASE AND THE SAME SESSION'S THIRD SURFACE HAD ALREADY BEEN FIXED UNDER THIS RUNG.** `D334`'s four repairs were verified CORRECT and are not in dispute: `export_closure_submission_csvs.py:440` was graded **THROUGH THE AST in rendered form** -- era-scope True, live count True, next-call arithmetic True, the escaping READ BACK rather than assumed and NO SURVIVING BACKSLASH -- `CLOSURE_METHOD_PRIORITY_REVIEW.md:258` carried the treatment inside its own sentence, and both manifests were verified key-by-key against the prior frame's parsed blob. **THE RUNG STILL FAILED ON THE WORD `all`. THREE IN-CLASS CLAIMS STOOD UNRECONCILED AT `8cefb4e9`**, each naming a **5** against a ledger of **6** -- the next would have been the 7th: **`demo-output/website/campaign/R5_PREREGISTRATION.md:7`** (*"The scoring-call ledger stands at 5, where round 4 left it"*, present tense, in the document's opening block; **205 lines carrying ZERO occurrences of `cumulative`, of `six`, or of the 2026-08-07 call**), **`demo-output/website/campaign/R5_RULE_FREEZE.md:122`** (*"the scoring-call ledger stays at 5 in this session"*; **124 lines, ZERO occurrences of `cumulative` or `six`**), and **`demo-output/website/CLOSURE_METHODS_COMPARISON.md:325`** (*"Scoring-call ledger \| yes, 5 calls, self-imposed"*, the lab's own column; stated at its weakest, because the column header **"Us (round 4)"** IS a structural era scope, but the corpus treatment is era scope PLUS the live count and no `six` appeared in the sentence, the row or the file -- and this site was **reachable by `D334`'s own compound arm** and is not named in its record). **THE BOUNDARY, FOR THE FOURTH CONSECUTIVE TIME, AND MACHINE-CHECKED:** every prior in-class filter, `D334`'s two arms included, joined the count token to a class noun with `[-\s]{1,20}` -- **whitespace and hyphens only. A VERB PHRASE is neither.** An adjacency-joiner predicate of that shape **MISSES** both R5 sites and **FINDS** the sites `D334` repaired, so the disagreement was a property of these sentences and not of a broken control. `D334` named this hole in its own blind-spot list and did not close it. **THE DECISIVE MEASUREMENT WAS NOT TENSE AND NOT NOVELTY: THE LAB HAD ALREADY APPLIED THE FIX TO THIS SESSION'S THIRD SURFACE, UNDER THIS RUNG.** `sdk/scripts/closure_round5_qcr_forward.py:26-33` -- the script of the SAME round-5 session, governed by the very rule freeze at `R5_RULE_FREEZE.md` -- carried the same claim and reads *"as of THIS SCRIPT'S RUN (2026-08-07, before the call) the ledger stood at 5 ... Scoped to its own run because the standing count has since moved ... the cumulative ledger is now SIX ... only the sentence describing the ledger needed a date on it. (Ladder V rung V7, re-run under Pass 2, 2026-08-10.)"* -- **it names rung V7 in its own text.** The script was reached; the two prose documents of the same session were not (`R5_PREREGISTRATION.md` last touched at `9a21d65c`, `R5_RULE_FREEZE.md` at `0bade54a`). That is verbatim the standard `CLOSURE_CHALLENGE_STATUS.md:380-385` set and `D330` quoted: applied to the artifact AND the generator *"so that the sentence is not left standing on a surface this one does not reach"*. **EVERY EXEMPTION WAS TESTED AND NONE REACHED THEM:** authorship -- both documents made **zero** scoring calls and said so, which is the chief's own `scoring_calls_made_by_this_run` test falling the other way from the round-4 manifest; era -- a document-level date was rejected as reconciliation for `export_closure_submission_csvs.py` and for `CLOSURE_METHOD_PRIORITY_REVIEW.md:258` in the very repair graded here; freeze -- **neither file's own sha256 appeared anywhere in the tracked corpus, 0 occurrences each**, and both were cited by COMMIT anchor, which a later edit does not disturb; `§2b` at `CLOSURE_THREE_STRANDS_2026-08-15.md:356` forbids repairing **a recorded gate**, which a prose ledger sentence is not; and session scope was already overruled by Pass 2 on this exact verb, which had been passed as *"none -- correct"* on 2026-08-08 and was repaired anyway on 2026-08-10 as *"TRUE of its own run, FALSE as standing text"*. **TESTED AND RULED NOT DEFECTS:** the two frozen entry-of-record JSONs and their generators (`this_run: 1` and a list item reading *"this run"* -- each recorded the cumulative count as of and INCLUDING its own call, by the run that made it, so `D310`'s authorship exemption reached them where the manifests' `0` had denied it); `CLOSURE_CHALLENGE_STATUS.md:1139` (*"total: 4"* followed in the same bullet by two dated supersessions ending at **6**, the file's own convention, already marked CLOSED by `LADDER_V_V8_REVERIFICATION`); and the per-round records naming one past call's ordinal position, which were true and remained true. | Graded 2026-08-17 at frame `8cefb4e9` **pinned in argv**, by a non-author of the ruling, of every repair and of all three prior grades. **HEAD moved to `52cdf6dd` during the pass; ancestry was confirmed, the grade stands at the pinned frame, and a DELTA ARM over the 9 modified paths returned 43 in-class positions, ALL READ, NOTHING NEW IN CLASS.** Every blob this grade rests on -- the three findings, the four repaired sites, `closure_round5_qcr_forward.py`, the docket and the ladder document -- was asserted **IDENTICAL across both frames**. **A RECOGNISER BUILT FROM THE CLAIM CLASS AND NOT FROM THEIRS:** the three defeated filters shared one property -- the in-class test demanded a RELATIONSHIP between count token and class word -- so this instrument tested **no relationship at stage 1**: it anchored at EVERY count token and took a raw **CHARACTER WINDOW** (-90/+160), which has no grammar and so has no joiner boundary. `ledger` was carried as a class root, **wider than `D334`'s vocabulary**. **Sweep: 20,764 files -> 2,184 past the file pre-filter -> 53,268 recogniser hits -> 8,641 in class -> 3,823 distinct reading cores, ALL READ**; 35 tracked blobs did not decode as UTF-8 and were named as unread. Every `.py` was read a second time **through the AST in rendered form** and, going beyond the repair, **every `.json` through the parser**, so escapes were resolved rather than matched around. **Two flats, unioned** (one dehyphenating soft wraps, one collapsing whitespace only). Pre-filter **machine-checked a superset on every run** by `assert_superset()`, applied **ON TOP** of the recogniser and never folded in, with every control probe asserted admitted by the file filter AND the recogniser AND the in-class predicate. **ADJACENCY TEST, run against MY OWN filter BEFORE it was trusted, on the PRE-REPAIR blobs at `353191c7`: all 4 known sites re-found, 3 of 4 MISS without the arms.** **Its first version passed 4 of 4 and was WRONG** -- it matched by line proximity, and two of the four were hits on a date token `07` and a CSV count `eight` seven lines from the claim; a control satisfiable by an accident is not a control, and this one had been satisfied by one. Control kind **`RECOGNITION`** *derived* by `scripts/control_kind.py` from the evidence and never typed: **8 mutually independent forms, ALL FIRED**, including a compound, an all-time-quantifier, a hyphen-wrapped and a bare-ledger form, and **4 negative forms correctly rejected**, two aimed at the arms. Planted **BY LINE INDEX** onto a line whose neighbourhood was **pre-checked to carry zero baseline signal** and read back **BY SLICING AT THAT INDEX** with byte-equality asserted -- the readback **caught the multi-line wrapped plant on its first attempt**. **Struck negative both ways: unstruck 1, struck 0.** **Polarity 2 clause scopes against 1 sentence scope**, single-polarity 1 and 1 and therefore not relied on alone. Anchor test **sentence-plus-parenthetical**, measured both ways: the same repaired sentence read reconciled under it and UNRECONCILED under clause scope. **The instrument was reported BROKEN by its own controls twice before it was trusted**, and both times the control was right: the all-time form was rejected by a stage-2 clause that carried **the exact `D334` hole**, and the polarity control was measured after the value test, which discards the live-count clause by construction. | **THE THREE EXPLICIT RULINGS.** (1) **STRUCTURAL adjacency SUSTAINED for both manifests, agreeing with the third grade on a RE-TESTED ground, and my own detector was OVERRIDDEN IN WRITING rather than left to decide silently** -- it reported both unreconciled, exactly as `D334` predicted. Re-serializing the identical parsed document: at `indent=2/4/8` the correction sat **1** line away against `generated_at` at **7**; at `indent=None` **both distances collapsed to 0**. A metric that changes under a pretty-printer setting is not a measurement of the document, while *same object, immediately following sibling key* returned **True at every serialization in both files**. The correction wins under either reading. (2) **THE IMPORTED MECHANISM IS CORRECT for these manifests.** Measured by parsing the prior frame's blob: `scoring_call_limit_note` **BYTE-INTACT** in both, **the eight CSV digests untouched** in both, **zero other keys changed**, exactly one key added, `scoring_calls_made_by_this_run: 0`, and **neither manifest's own sha256 anywhere in the corpus (0 occurrences each)**. Inventing a second form would have left the corpus with two mechanisms for one job; the import was **named as imported** in the key's own body. **One residual FILED and NOT a ground of this verdict:** the generator now emits a note whose text differs from the artifact's byte-intact note and emits no correction key, so regenerating that package would drop the correction -- the identical shape already sustained for the criterion-table pair, and not a count-claim defect. (3) **THE RECORD DEFECT IS CORRECTLY REPAIRED**: `D330`'s disposition was re-read and is an express **referral**; the chief's three grounds were re-measured rather than inherited; and the correction was confirmed **struck and kept in BOTH places**, `D333` and the V7 rung row, anchored to `353191c7`. `docs/DOCKET.md:223` (`D30`) stayed unrepaired and this grader agreed on both grounds. **WHAT THIS GRADE COULD NOT SEE:** a fifth class boundary -- four passes had each been failed by one, and the honest prediction was that a fifth existed; a character window closes the joiner family but not **cross-sentence** claims, where the unit is established in one sentence and the count asserted in another beyond the window; claims carrying **no count token at all** (*"the ledger has not moved since round 4"*); the 35 undecodable blobs and untracked files; and whether the three sites exhaust the joiner family or only the part a 200-character window reached. **No filter here could certify its own completeness.** **THE THREE SITES WERE LEFT EXACTLY AS FOUND: a grader may not repair what he grades.** No scoring call was made, the ledger was not touched and stood at 6, and nothing was moved. Grade document `campaign/LADDER_V_V7_GRADE_2026-08-17.md`. | **Repair of the three sites by a non-author of this grade**; **chief** (whether the generator/artifact regeneration residual in ruling 2 is owed a mechanism rather than a filing, and whether a fifth class boundary should be hunted before V7 is graded a fifth time). Whose: FLEET. |
| D336 | **THE FROZEN-EXHIBIT RULING ON V7 WAS REFUTED BY EXECUTION ON EVERY GROUND IT STOOD ON, AND THE ACTION IT CALLED FORBIDDEN HAD ALREADY BEEN PERFORMED ON THE VERY FILE.** The chief ruled that `campaign/R5_PREREGISTRATION.md:7` and `campaign/R5_RULE_FREEZE.md:122` were frozen attestations outside `V7`'s class, on three grounds: **(a)** their evidentiary value was that their bytes had not changed; **(b)** `W-3` forbade amending a pre-registration once compute had run, so by `bcad2bbc` the criterion was **void** on those surfaces rather than strict; **(c)** the correction already existed where a reader met it. **ALL THREE FAILED, AND (b) FAILED HARDEST.** **`bcad2bbc` REACHED THIS CASE AND DECIDED IT THE OTHER WAY:** it held that *"a criterion may not require more than the maximum permitted action on the surface it grades. Where `L-44` forbids revision, **a dated addendum below the freeze line is that maximum**, so a **per-sentence** reading is void there rather than strict"* -- and it proved that **by execution on a real frozen PRE-REGISTRATION**, `campaign/F6b_QCR_PREREGISTRATION.md`, per-sentence faults **7 before and 7 after** an addendum against **per-file 1 before and 0 after**. Ground (b) required the maximum permitted action to be **NOTHING**; the precedent it cited held that action to be an **ADDENDUM**, so the criterion was never voided -- it was graded **PER FILE**, and the sites stood **in class and simply unrepaired**. **`L-44` SETTLED IT IN THE LAB'S OWN WORDS AND NAMED BOTH FILES** (`LESSONS.md` §L-44, which arose when an agent refused to edit these exact documents): *"New knowledge attaches to a **DATED ADDENDUM**, a superseding record, or the live surfaces; never to the frozen document's body ... Pre-registrations, rule freezes, dated reports, gate records and audit reports all answer yes. **They take addenda, never revisions.**"* **THE DECISIVE MEASUREMENT, AND IT WAS ARITHMETIC RATHER THAN ARGUMENT: `R5_PREREGISTRATION.md` HAD ALREADY BEEN AMENDED BY DATED ADDENDUM, AFTER ITS FREEZE AND AFTER COMPUTE HAD RUN.** The lab's bytes hashed `2bb14468`; the frozen text of `e865076b` hashed `eaf42fe1`; the difference was **six appended lines at `:200-205`** reading *"**Dated citation note, 2026-08-08 (Ladder V rung V5; additive only, no frozen clause touched)**"*. Same file, **same ladder**, a different rung, landed after the 2026-08-07 solve and after the round-5 scoring call. The action ground (b) called forbidden **was the file's own authorised state**. **`W-3` DID NOT SAY WHAT THE RULING SAID IT SAID:** its text (`dafoam/S1_GP4_REPLACEMENT_2026-08-14.md:92`) governed **amendment of gates**, checked against run directories; the closure package's own verifier read it identically -- *"An amendment that only appends is **legal** under the lab's standing order `W-3` read together with the lab's own practice; an amendment that **rewrites** a frozen clause is not."* Ground **(a)** failed on `L-44`'s own generalisation, that a frozen artifact's value was *"its **TIMESTAMP RELATIVE TO WHAT IT JUDGES**"* and not its byte-constancy -- which is why an append below the line leaves the property intact and why the corpus permits one. Ground **(c)** failed in part, and the part where it failed was new. | Tested 2026-08-17 at frame `3e04991c` **pinned in argv**, by a non-author of the ruling, of the fourth grade and of the repairs; the closure repository read **STRICTLY READ-ONLY** and not written. **WHAT THE FOURTH GRADE ACTUALLY TESTED ABOUT THE FREEZE -- it answered BOTH questions, not the weak one.** (i) Whether any freeze was **asserted on the bytes**: *"neither file's own sha256 appeared anywhere in the tracked corpus, 0 occurrences each"* and both were cited **by COMMIT anchor, which a later edit does not disturb** (`LADDER_V_V7_GRADE_2026-08-17.md:311-317`). **RE-MEASURED HERE AND CONFIRMED: 0 hits in the lab for all three of `2bb14468`, `7e08d6ec` and `eaf42fe1`**; every citation found was a commit anchor (`@ e865076b`, `@ 0bade54a`) across `CLOSURE_CHALLENGE_STATUS.md`, `ACTIVE_RESEARCH.md`, both round-5 JSONs, the round-5 `MANIFEST.json` and the submission `README`/`DESCRIPTION_DOCUMENT`, **so nothing in this repository pins their bytes**. (ii) Whether the freeze **RULE** forbade the repair action, read against `§2b` at `CLOSURE_THREE_STRANDS_2026-08-15.md:356`: `§2b` governed **recorded gates and verdicts**, which a prose ledger sentence was not, *"and the corpus treatment **appends beside** rather than rewriting"* -- the grade had therefore already named the addendum form as the permitted one. **The grade tested `§2b` and did NOT test `W-3` by name; that gap was the ruling's only unexamined ground, and closing it closed it AGAINST the ruling.** **THE CLOSURE-REPOSITORY DIVERGENCE ANSWER: AN ADDENDUM IN THE LAB BREAKS NEITHER THE CLOSURE PIN NOR THE CLOSURE REPOSITORY'S VERIFICATION.** `scripts/verify_freeze.py` resolved every path against **its own `ROOT`** and compared to **hardcoded sha256 constants**; it never read the lab, so no lab-side edit can fail it. `evidence/R5_PREREGISTRATION.md` sat in its **`ADDITIVE`** class, whose authorised sha `2bb14468` **was already the frozen-plus-addendum state** -- the package already shipped an addendum and passed on it -- while `evidence/R5_RULE_FREEZE.md` sat in the **`EXACT`** class at `7e08d6ec`, *"never amended"*. Measured: lab and closure copies were **BYTE-IDENTICAL at this frame for both files**. A lab-side addendum therefore **diverges the copies without failing anything**, and it is **not** the defect repaired on 2026-08-16, which ran the **opposite direction** -- the closure copies had been **EDITED IN BODY** while still citing the freeze commits, so the package asserted a freeze it did not have; a lab-side append leaves the package's freeze assertion **true**. Keeping the two in step would require updating the `ADDITIVE` constant for the pre-registration and **moving `R5_RULE_FREEZE.md` from `EXACT` to `ADDITIVE`** -- writes to a repository this agent did not own and did not make. **THE SAME-SESSION SCRIPT WAS NOT FROZEN, so the chief's own decisive test was met:** `sdk/scripts/closure_round5_qcr_forward.py` was **born at `e865076b`, the very commit cited as the pre-registration's freeze anchor**, yet its sha256 `0348ca21` appeared **nowhere** in the corpus, it was named in **no** freeze verifier, it **did not travel** in the closure package, and it was edited **twice** after `e865076b` (`92562841`, which added the correction under Pass 2, and `9477a2ed`) with nothing asserting otherwise. Ground (a)'s factual distinction therefore **survived as a fact while failing as a ground**. | **VERDICT: THE RULING IS REFUTED, AND `V7` REMAINS `FAIL` ON THREE SITES RATHER THAN ONE.** Grounds (a) and (b) failed outright; (c) failed in part. Because the criterion was **not** voided, the question of whether a criterion voided on two surfaces would have been the same as satisfied on them **did not arise** -- and the answer would have been no in any case. **THE PERMITTED REPAIR, NAMED:** a **dated addendum below the freeze line** on each of the two documents, carrying **era scope PLUS the live count** -- the treatment the corpus already applied to this session's third surface at `closure_round5_qcr_forward.py:26-33`, which names rung `V7` in its own text -- touching **no frozen clause** and **removing nothing**, exactly the form `R5_PREREGISTRATION.md:200-205` already carries and the form `verify_freeze.py` blesses in as many words. It can be made **without breaking the closure repository's pin**, because that pin reads only that repository's own files. **A FOURTH UNREPAIRED SITE WAS FOUND THAT THE FOURTH GRADE COULD NOT SEE, BECAUSE IT IS OUTSIDE THIS REPOSITORY:** `Certonomous_closure_challenge/evidence/CLOSURE_METHODS_COMPARISON.md:335` -- *"Scoring-call ledger \| **yes, 5 calls, self-imposed**"* -- the package's own copy of site 3 **at a different line number**, which repairing the lab's `:325` **will not reach**. The package did carry the live count in two places (`records/MANIFEST.json:8`, *"the lab's **6th** cumulative distinct prediction set scored"*, and `description/CLOSURE_SUBMISSION_REQUIREMENTS.md:127`, *"the ledger stands at **6**"*), so ground (c) held for those two surfaces and failed for `:335` and for both `evidence/` R5 copies. **Nothing was repaired in this pass and nothing was moved: this was a ruling test, and the repair belongs to a non-author of it.** No scoring call was made and the ledger stood at 6, untouched. **[WITHDRAWN AND REPAIRED 2026-08-17, at `6aedfcc9`.]** The chief **withdrew the ruling in full** on this row's grounds. The repair was then made by this agent under express authorisation, and **repairing is not grading**: no grade was issued and `V7`'s verdict cell was left at **`FAIL`**, a fifth non-author grade still owed. **Two frozen surfaces took a DATED ADDENDUM BELOW THE FREEZE LINE**, matched to the pre-registration's own precedent at `:200-205` rather than to a second invented shape, carrying **era scope PLUS the live count**; `R5_RULE_FREEZE.md` had carried none before and this was its first. **Append-only PROVED rather than asserted: the frozen bytes of `e865076b` and `0bade54a` are an exact PREFIX of each repaired file**, so nothing was removed or altered and the property `verify_freeze.py` tests is intact. `CLOSURE_METHODS_COMPARISON.md:325` took the ordinary in-cell repair. **TWO INDEPENDENT READERS ARGUED THE ERA WORDING ALREADY SUFFICED, AND THE LAB HAD ALREADY OVERRULED THAT EXACT READING ON THIS EXACT SENTENCE.** `R5_RULE_FREEZE.md:122` read *"the round-5 pre-registration goes to the supervisor; the scoring-call ledger stays at 5 in this session"*; the same session's `closure_round5_qcr_forward.py` read *"The round-5 scoring decision belongs to the supervisor; the ledger stays at 5 distinct prediction sets scored"* -- **same clause structure, same verb, same session scope, and a stronger authorship disclaimer beside it**. That sentence was passed on 2026-08-08 as *"none -- correct"*, which IS the era-suffices reading, and **Pass 2 overruled it on 2026-08-10** as *"TRUE of its own run, FALSE as standing text"*, repairing it at `92562841`. The reading offered for the frozen pair was **not a new reading; it was the overruled one**. **`D310`'s authorship rule points the same way:** its first category is a surface recording the call it ITSELF makes, whose exemplars MADE calls; both R5 documents made **zero** and say so, so neither records its own action, and they fall in the second category -- *stating where the ledger stands* -- which takes era scope plus the live count. `D310` names **this session's own script** as an exemplar of that second category. **AND THE TRAP `bcad2bbc` NAMED WAS RE-MEASURED HERE AND HELD:** an addendum below the freeze line **cannot** reconcile the sentence under any block-local reading -- the prereg's claim sits at line **7** and its addendum at line **212**, ~200 lines apart, outside any window -- so **PER-SENTENCE 1 before and 1 after, unchanged**, against **PER-FILE 1 before and 0 after** (live-count occurrences now 1, 1 and 4 across the three files). That is not a defect in the repair; it is why `bcad2bbc` ruled the per-sentence reading **VOID** on frozen surfaces and graded them per file. **PROPAGATION TO THE SEND PACKAGE WAS MEASURED AND DELIBERATELY NOT DECIDED** -- it is the package owner's, and that repository was not written. The package's twin of site 3, `evidence/CLOSURE_METHODS_COMPARISON.md:335`, **was repaired by its own owner** in the canonical in-cell form. | **Chief**, to withdraw or reissue the ruling; then **repair of all three lab sites plus the closure package's `:335` by a non-author of this test**, the two frozen documents by dated addendum below the freeze line and never by revision; and the **owner of `Certonomous_closure_challenge`**, for whether the package's `evidence/` copies take the same addendum and whether `verify_freeze.py`'s `EXACT`/`ADDITIVE` classes move with them. Whose: CHIEF.
| D337 | **V7 GRADED A FIFTH TIME: `PASS`. THE CLASS WAS SWEPT REPO-WIDE WITH A GRAMMAR-FREE RECOGNISER, AND THE TWO FROZEN SURFACES WERE RULED PER FILE BECAUSE AN ADDENDUM BELOW A FREEZE LINE CANNOT RECONCILE PER SENTENCE.** Graded at frame `678a69ab` pinned in argv by a non-author of the ruling, of every repair and of all four prior grades; record at `campaign/LADDER_V_V7_GRADE_5_2026-08-17.md`. **THE RECOGNISER WAS BUILT FROM THE CLAIM CLASS, NOT FROM ANY PRIOR GRADE**, because each of the four prior failures was a class boundary that excluded the defect by construction -- an era clause, a `sdk/` directory scope, word adjacency, and a `[-\s]{1,20}` joiner that a verb phrase could not satisfy. Stage 1 tested NO relationship: struck spans blanked offset-preserving, an anchor on EVERY count token, a RAW CHARACTER WINDOW of 240, and a class root required only inside that window -- a window has no grammar, so it has no joiner, no adjacency, no clause scope and no line boundary. The in-class filter was kept ON TOP of it and never folded in. **THE SUPERSET CLAIM WAS MACHINE-CHECKED**: all four prior recogniser shapes were re-implemented and run over the corpus -- 322, 218, 159 and 276 matches, and **0** of them fell outside the stage-1 hit set. **KNOWN-SITE RECALL 21/21** before the instrument was trusted on anything unknown. **CORPUS ARM: the whole repository, 13,815 tracked paths, NO directory scope**, since the criterion's grep carried none; 1,107 binary skips and 10,529 root-free prunes were each machine-proven not to hide a site (pruned files carrying a class root = **0**), and the 15 root-bearing binaries were opened individually and carried **zero** scoring-call phrases -- `dist/certonomous-demo.zip` was unpacked and its three scoring-call entries all stated the LIVE count. **THE EQUIVALENCE CHECK EARNED ITS PLACE**: its first run reported 2 divergent files, caused by padding the scan regions on one side only, and an unchecked optimisation would have silently narrowed the arm; after correction, divergences = **0**. **CONTROLS WERE PAIRED WITH DARK TWINS**, 72/72 positives fired and 6/6 dark twins stayed silent, and the host probe had to be MADE the dark twin itself -- an earlier rootless probe with its count token at a different relative offset passed a host on which the dark twin then FIRED, which is proximity rather than recognition. Triage: 78,615 anchors to 2,267 blocks to 104 read individually, resolving to event records keeping a bare ordinal on `D310`'s authorship ground, use/mention inside repair and grade records, true per-run counts, non-class counts, and polysemy. **THE TWO FROZEN SURFACES WERE RULED PER FILE PER `bcad2bbc`**, and this pass reproduced the trap on its own instrument: per block **19/21**, per file **21/21**, and the ONLY two per-block misses were exactly those two files. Append-only was verified rather than accepted: the frozen bytes of `e865076b` and `0bade54a` were an EXACT BYTE PREFIX of each repaired file with **0** lines removed or altered. Manifests were compared key-by-key against the prior frame's parsed blob: `scoring_call_limit_note` byte-intact by sha256, the eight CSV digests untouched, key order preserved, exactly **1** sibling key added. **JSON ADJACENCY WAS RULED, NOT LEFT TO A TOOL**: re-serialised at indent 2/4/8/None the textual line gap collapsed to **0** at `None` and even the character gap moved with the flag, so both textual measures are functions of a pretty-printer setting, whereas same-object-sibling-key was invariant. The verdict cell was confirmed byte-identical across every repair since the fourth grade. `docs/DOCKET.md:223` (`D30`) stayed out of scope and NOTHING was appended to it. A peer's staged reversion of the `6aedfcc9` repairs was live in the shared index throughout and was left exactly as found, which is why this grade was built through a private index. | fleet |
| D338 | **A STATUS TABLE WENT STALE AGAINST ITS OWN RUNG RECORDS, AND TWO OF ITS CELLS CARRIED LABELS THAT ARE NOT VERDICTS AT ALL.** The chief-maintained status ledger in `campaign/LADDER_V_TRIPLE_VERIFICATION.md` held **V9** at `FAIL → FIXED` and **V12** at `DELIVERED` while both rungs' own records had closed at a non-author `PASS`: V9 at `27295b9b` (`campaign/LADDER_V_V9_REGRADE_2026-08-16.md`, all three declared clauses verified by execution) and V12 at `dbb6564a` (`campaign/LADDER_V_V12_GRADE_2026-08-16_NONAUTHOR.md`, frame `678cb7e8`, a plain `PASS` with no residual label). **THE GENERAL DEFECT IS THE TABLE, NOT THE TWO CELLS.** A summary maintained by hand rots against the records it summarises the moment a rung moves, and it rots **silently**, because nothing in this lab reads a verdict cell back against the record it names. This is the **third** instance on this one table: V13 found six rungs described as awaiting unpark after they had executed and no rows at all for V14/V15, which is why the table was rewritten on 2026-08-11; V15 round 9's **F2** found the V15 cell stopped at round 7 while two further rounds had run, with **ten edits landing after round 8** without absorbing it. **V12's case is sharper than staleness:** `ad4d2315`, whose subject line reads *"The V12 ledger row records PASS, closed by a non-author"*, wrote that grade into the confirmation column and left the verdict cell at `DELIVERED` in the same commit — so the row contradicted **itself, across its own two cells**. **THE SECOND HALF IS A VOCABULARY FAILURE WITH A KNOWN CONSEQUENCE.** Neither `FIXED` nor `DELIVERED` is among the five terms fixed at `docs/charters/VERIFICATION_CHARTER.md:95` and `docs/charters/REPORTING_CHARTER.md:210` — PASS, GATE REACHED, GATE FAIL, NOT A RESULT, BLOCKED — and both return **zero** occurrences anywhere under `docs/charters/`. Section 16's enforcement (`docs/charters/VERIFICATION_CHARTER.md:1348`, clause at `:1390-1393`) is built on that set being **enumerable**, so a cell outside it is invisible to the negative-verdict sweep **by construction** — the identical structural ground on which `PASS WITH EXCEPTIONS` was withdrawn at `D249` and `PASS WITH RESIDUALS` at `7c44cbe2`. These two differ from those only in not beginning with the word PASS; the invisibility is the same. **The V13 cell had already been repaired off this very label and V12's was not carried with it**, which is the strongest evidence available that the repair route here is per-cell, manual, and will diverge again. | The ledger's own status table, `campaign/LADDER_V_TRIPLE_VERIFICATION.md`, section *"Status ledger (chief-maintained)"*, rows V1 through V16. Both charter lines were opened and read at frame `f7a14346` rather than inherited from `D249`, and `FIXED` and `DELIVERED` were searched across `docs/charters/` for **zero** hits. Both grade records were opened in full and each verdict read off its own line 3, together with each grader's non-authorship declaration; each grade's landing commit was confirmed by `git log` on its own path, and each was checked for a later overturn, amendment or strike. **The only later commit on either is `66bd94ea`**, which reassigned two docket IDs inside the V9 record after a race (`D272`/`D273` became `D285`/`D286`) and left the verdict untouched. No superseding V9 or V12 grade record exists on any branch. | **REPAIRED HERE, APPEND-ONLY UNDER W-4: nothing deleted, nothing renumbered.** Each superseded label is struck in place and the current verdict appended beside it with its record filename, its landing commit and its date, in the form the V5, V7, V13 and V15 cells already use; every pre-existing anchor in both cells is preserved, no line but those two moved, and strike depth stays in the range 0 to 1. **WHAT THIS DOES NOT FIX, AND IT IS THE ROW'S REAL SUBJECT:** the table has now been repaired against its records three times by hand, and the only honest prediction is that a fourth divergence is already forming. **What would settle it is a checker, not a fourth repair** — read each rung's verdict cell, require every verdict token in it either to be a member of the five-term vocabulary or to sit inside a strike span, and require the newest unstruck verdict to match the verdict on line 3 of the newest grade record naming that rung. Both halves are mechanical, and section 16's enumerability is precisely what makes the first half possible. **Falsifier:** show that `FIXED` or `DELIVERED` is defined as a verdict anywhere under `docs/charters/`, or that either grade was overturned, and this repair falls with it. | **chief** (the ledger is chief-maintained, and whether a verdict-cell checker belongs in `sdk/tests/` or in the ladder's own harness); **FLEET** (building it). Whose: FLEET. |
| D339 | **A BYTE-IDENTITY CERTIFICATION SURVIVED THREE INDEPENDENT REPRODUCTIONS OF ITS OWN FALSIFICATION, AND WHAT KEPT IT ALIVE WAS THAT REPRODUCING IT WAS CHEAP WHILE REPAIRING IT REQUIRED CHOOSING BETWEEN TWO NON-EQUIVALENT REPAIRS.** `demo-output/website/CLOSURE_CHALLENGE_SUBMISSION_DRAFT.md` §5 certified that the preserved round-4 banner's *"thirty body lines are byte-identical — every row of the ten-row defect table"*. Re-measured at frame `101079fd` as an aligned 32-line byte comparison against `e87650db^` (`514876b0`), each side anchored on its own heading line rather than on a line number: **3 of the 32 block lines differed — indices 0, 21 and 31 — and index 21 was the seventh of the defect table's ten data rows** (header at index 13, separator at 14, rows at 15–24). Indices 0 and 31 were declared in the sentence; index 21 was not. Of the thirty body lines the sentence spoke about, **29 matched and one did not**, and **9 of the 10 table rows matched, not 10**. **REPAIRED BY CORRECTING THE CERTIFICATION, NOT BY RESTORING THE ROW, AND THE EDIT ITSELF DECIDED WHICH.** `2cec44ee` (2026-08-12 18:05:51Z) was the only commit between `e87650db^` and `101079fd` to move any block line other than 0 and 31, and its edit was a **pure insertion of 186 bytes** (579 → 765) that deleted and reworded nothing: a dated scope note recording that the row's own mandate *"must now carry P(rank 1) = 68% with its 2–100% interval"* held the FOUR-entry board's figures, the live ones being 50% and 0–97% on the six-entry board fetched 2026-08-11T23:33Z (`campaign/BOARD_MOVED_2026-08-11.md`). That row mandated rather than narrated, so obeying it as it stood would have written a superseded probability into §5.2 and §5.4 — the claim-generator class `scripts/check_normative_clauses.py` was built against (`D119`), whose own BLIND-TO census at `101079fd` named a four-entry `P(rank 1)` mandate as its live uncaught instance. Restoring index 21 would have reinstated that mandate un-scoped inside a package held open for proofreading and would have deleted a dated repair, against `W-4`. The certification was the false thing, not the edit. **WHAT MADE IT SURVIVABLE, WHICH IS THE PART WORTH KEEPING AND NOT THE FACT THAT IT WAS FIXED.** **(a) The edit and the claim about it sat in different registers, four lines apart.** `2cec44ee`'s commit message declared its §10 edits and two closure-table rows *below* the banner and named no edit to the banner itself, while the sentence it falsified sat *above* it inside a paragraph whose announced subject was the §5.1–§5.4 rewrite. Nothing on the page joined the edit to the claim, and nothing in the message did either. **(b) The claim was about BYTES, and at `101079fd` no instrument in this lab took a byte-identity ASSERTION as its unit.** Every check here grades a figure, a wording, a citation, a count, an ordinal or a verdict cell; `scripts/check_summary_consistency.py`, the one check built around a byte-identical block, says in its own header that byte-identity decides nothing in either direction and that it deliberately never looks at a diff. So a sentence certifying byte-identity against a named ancestor was re-derived only by whoever chose to re-derive it, and the three reproductions the record names (2026-08-14 as F4-prime, 2026-08-15 as R1, 2026-08-17) each re-measured the same bytes by hand and left no executable behind that would re-measure them for the next reader. **(c) The two declared exceptions made the sentence read as already audited.** A claim that names its own exceptions looks reconciled, and the undeclared third line hid behind the two declared ones — the same shape as a green frame over a gap. | `demo-output/website/CLOSURE_CHALLENGE_SUBMISSION_DRAFT.md`, the §5 DISCHARGED banner's opening paragraph, at frame `101079fd`; the finding is recorded in the non-author re-verification `campaign/LADDER_V_V8_REVERIFICATION_2026-08-17.md` (`101079fd`), which reports it as the third independent reproduction. The 32-line block was located in each side by its own heading line and compared index for index on raw bytes; the commit that moved index 21 was found by replaying every commit that touched the file across `e87650db^..101079fd` rather than by blame. | **Corrected here, append-only under `W-4`: the false clause is struck where it stood and the true line-by-line certification appended beneath it, with its frame, its ancestor and its indices, so it can be re-run.** What would CLOSE this row rather than repair this instance is an instrument that grades byte-identity certifications the way `scripts/check_summary_consistency.py` grades a summary row against its own section — derived from document structure, never from a list: a recogniser for sentences of the shape *N lines are byte-identical to <anchor>*, which resolves the anchor, re-runs the aligned comparison and fails on disagreement. Cost estimate: a few hours — the recogniser is the work, the comparison is fifteen lines, and the corpus is the tracked `*.md` surfaces `check_normative_clauses.py` already enumerates. Until one exists, this class is re-checked only by hand and this row is the standing notice of that. | **chief** (whether a byte-certification recogniser belongs in `scripts/` beside the other claim-class checks or in the ladder's own harness); **FLEET** (building it). Whose: FLEET. |
| D340 | **THE FOUR RANK CLAIMS THAT TRAVEL WERE NEVER WRONG AT THE SOURCE — THEY WERE A STALE COPY OF A REPAIRED PAGE INSIDE THE TRACKED BUNDLE, AND THE 35 THAT REMAIN ARE A DIFFERENT REPAIR, NOT A SMALLER ONE.** `scripts/self_audit.py`'s `rank claims carry the right values` returned 4 faults on surfaces that TRAVEL and 35 on lab records. All four travelling faults were the same surface — `dist/certonomous-demo.zip!certonomous-demo/site/closure.html` — and diffing it against its tracked source `demo-output/website/closure.html` shows the source carries none of them: the page was repaired and the bundle was never rebuilt from it, which is **D112(i)** stated in its own words (*"rebuild the bundle from it; verify by reading the archive member, not the build input"*), and which `closure.html` itself already disclosed as out of that pass's scope. Cleared here by replacing that ONE member and carrying the other 102 across byte-for-byte with their original metadata, verified by reading the member back out of the archive. Re-derived board, computed from `sdk/scripts/probability_of_rank.py`'s `LIVE_BOARD` by AST and the entry of record, never transcribed: six published entrants, so **rank 1 of 7** counting us on the board fetched **2026-08-11T23:33Z**, overall `0.056647191704213645`, **P(rank 1) = 50.2%**, interval **0–97% at 95%**, and **four pairs not decided** (Yang, Reissmann, Wu & Zhang, Tian); per-case 2,2,1,1,3,4,4 and 7 of 7; best-on-board **2 of 8** with **0 of 8** earned by our model, both survivors being rows the decline gate passed through as the organisers' own unmodified RANS field; and the truth-free seed bound `0.002419` is **177% of** the `0.001365` margin over Yang — it exceeds that margin. **THE 35 ARE FILED, NOT SWEPT, AND THEY ARE NOT ONE POPULATION.** They fall in five classes whose repair calculus differs, which is why batching them would be wrong: **(a) 21 dated rung and campaign records** (`campaign/LADDER_V_*`, `CLOSURE_THREE_STRANDS_2026-08-15.md`, `RULE_O_DENOMINATOR_MEASUREMENT_2026-08-15.md`) whose figures were true of the four-entry board on the date each was written — re-pointing a historical record at a live board would stop it being a record of the call it exists to record, the precedent `closure.html` already sets in its own gold box and which any repair here must argue against rather than assume; **(b) `scripts/use_mention_control_set.json` (2)** — a LABELLED CONTROL SET whose whole function is to carry the withdrawn wording, so repairing it is faulting the control and would blind the discriminator that reads it; **(c) `scripts/self_audit.py:1358` (1)** — the checker's own explanatory prose about the defect, a MENTION and not a use, and the file that would have to grade its own repair; **(d) four machine records** (`closure_challenge_round5_qcr.json`, `agenda/docket.json` ×2, one agenda proposal) whose narrative keys are provenance of a scoring call already made — the class D157/D175 settled once and whose consumer question is open; **(e) six live prose surfaces** (`CLOSURE_CHALLENGE_SUBMISSION_DRAFT.md` ×3, `ACTIVE_RESEARCH.md`, `CLOSURE_CHALLENGE_STATUS.md`, `closure_challenge_C2_error_decomposition.md`) plus **`latex/closure_challenge_report.tex` (1)**, which is the only one of the 35 with a COMPILED SURFACE the checker cannot read at all — `closure_challenge_report.pdf` is not UTF-8, and `\sout{}` is strike-and-keep, so its text layer carries a withdrawn figure identically whether or not the `.tex` is ever repaired. Only (e) is a plain repair; (a) needs a ruling, (b) must not be touched, (c) is self-referential, and (d) is a settled class. | Lab check `scripts/lab_check.py --no-tests`, verdict FAIL, 2026-08-17, measured in a clean detached worktree at `7939324d` (HEAD is the referent; the shared index carries phantom entries). Travelling corpus swept independently of `self_audit` — whole files, offset-preserving masking of strike spans then continuation prefixes then emphasis, joiners `[-\s]{1,20}` — with a RECOGNITION control of 12 plants in the claim's vocabulary and its variants (word- number, hyphen, newline, emphasis, code-span, continuation) and 4 struck NEGATIVE plants that stayed silent. The four travelling `certificate.pdf` members were RENDERED to image and read, not grepped; they carry no board claim, only the validation-tier homonym. Cannot see: the 8 travelling PNGs, and any render-time text. | **(i) DONE** — bundle member refreshed; `rank claims carry the right values` goes FAIL → WARN with the travelling count at **0** and the lab-record count unchanged at 35, and no other gate changes verdict (`bundle drift vs shipped zip` stays FAIL, its count moving 6 → 5 behind, since the other five drifted members are that gate's defect and not this one's). **(ii)** Katie or chief rules on class (a): repair to the live board, or add a dated banner that exempts them by construction as `CLOSURE_CHALLENGE_STATUS.md` already does. **(iii)** Class (e) is a fleet repair, six sentences, and each must carry P(rank 1), its interval, its board and the undecided pairs or it is still a defect. **(iv)** `closure_challenge_report.pdf` must be recompiled and READ AS RENDERED whenever its `.tex` is repaired, or the repair is invisible where it counts. | fleet (i, iii); chief (iv); Katie (ii) |
| D341 | **CLAUSE (d) STILL MANDATES A FALSIFIED PROBABILITY ONTO EVERY SURFACE, AND ITS MANDATE SENTENCE NAMES NO BOARD — IT REACHES ONE ONLY BY POINTER, WHICH THE SUBMISSION'S OWN RULING SAYS IS NOT AN IDENTIFIER.** `CLOSURE_FAMILY_SUPERVISION_GUIDELINES.md:195-201`, inside clause (d) which runs `:172-209`, still directs that **every surface, internal or public — including anything shipping in `dist/` and the entry's own cover material — carries `P(rank 1) = 68%` with its interval `2-100% at 95%`** plus the two-pair not-decided list (Reissmann and Wu & Zhang not decided; Liu and Montoya decided). Opened and read at frame `a5080f6e`, not inherited from the handing-back agent. **THE FIGURE IS FALSIFIED, AND BY THIS LAB'S OWN MEASUREMENT.** The live figure is **`P(rank 1) = 50.2%` with a `0-97%` band at 95%**, on the **six**-entry board fetched `2026-08-11T23:33Z` and re-verified unchanged `2026-08-14T21:01Z` — `campaign/BOARD_RESCORE_2026-08-14.md` §3.2 `:129`, precisely 200,609 of 400,000 draws, derived by `sdk/scripts/probability_of_rank.py`. The 68% is the **four**-entry board's, and `adff0697` has already had to rule on exactly this clause once, because the §5 banner row it under-declared *was this directive*. **AND THE STRUCTURAL DEFECT IS THE ONE CLAUSE (d) ITSELF LEGISLATES AGAINST.** The mandate sentence at `:195-199` states the figure and its interval and names **no board at all** — no entrant count, no retrieval date, no commit. Its dated note at `:181-194` is INSIDE the clause and immediately above the mandate, and it does inline the live 50% figure, but it reaches the BOARD only by pointer: `Sources: campaign/PROBABILITY_OF_RANK_2026-08-10.md` at `:207` and *"see (e), which is where the live figures and the board that dates them live"* at `:185-186`. The note itself says at `:187` that it is *"added on the mandate rather than beside it"*, so adjacency is not even claimed. Clause (d)'s own third prohibition at `:235-237` reads **no surface may state the figure without its BOARD — how many entries, fetched when** — so the clause fails its own rule inside its own mandate sentence, and the rule was bought by an incident. `DESCRIPTION_DOCUMENT.md:289-291` rules the general case in as many words: *"a cross-reference is not an identifier, which is the defect this section exists to prevent."* **TWO PATHS IN THE HANDED-BACK CLAIM WERE WRONG AND ARE CORRECTED HERE RATHER THAN COPIED FORWARD:** the guidelines file is `demo-output/website/CLOSURE_FAMILY_SUPERVISION_GUIDELINES.md`, **not** under `docs/`, and `DESCRIPTION_DOCUMENT.md` sits at `demo-output/website/closure_challenge_submission_round5/`, **not** at the repo root. Both line numbers were right, and the interval is written with an en dash rather than the word "to". | `demo-output/website/CLOSURE_FAMILY_SUPERVISION_GUIDELINES.md:195-201` — clause (d) `:172-209`, its dated note `:181-194`, its third prohibition `:235-237`; ruled against `demo-output/website/closure_challenge_submission_round5/DESCRIPTION_DOCUMENT.md:290`; live figure `demo-output/website/campaign/BOARD_RESCORE_2026-08-14.md:129`, origin `campaign/PROBABILITY_OF_RANK_SIX_ENTRY_2026-08-11.md`. Measured 2026-08-17 at frame `a5080f6e`. | **NOT REPAIRED HERE, AND DELIBERATELY NOT.** This is supervision text whose own section rule is *dated superseded notes, never rewrites*, so striking the 68% is the clause owner's act and not a filing agent's — and a filing agent who rewrote a mandate to match a note would be doing to a charter what `D338` was opened for on a ledger. What SETTLES it is one sentence, not a rewrite: put the board **inside the mandate sentence** — six entries, fetched `2026-08-11T23:33Z` — so that obeying clause (d) literally can no longer write a boardless figure onto a surface. Until that lands, every surface that obeys clause (d) as written ships a falsified probability with a four-entry interval, and the note that would correct it is reachable only by following a pointer the submission's own §5 ruling has already declared insufficient. | **chief** (the clause is supervision text and the figure is a board claim); the **Closure/UQ family supervisor** as clause owner. Whose: the clause owner. |
| D342 | **THE "120 LIVE OCCURRENCES" WAS A LINE COUNT, NOT AN OCCURRENCE COUNT, AND USE WAS NEVER SEPARATED FROM MENTION. MEASURED, WITH THE SPLIT DONE BEFORE THE NUMBER WAS FILED: 153 OCCURRENCES ON 121 LINES IN 24 FILES — 49 USES ACROSS 12 FILES, 104 MENTIONS ACROSS 17.** Regex, named rather than described: `PASS\s+WITH\s+(?:RESIDUALS?\|EXCEPTIONS?)` with `re.I`, run against **whole-file contents and not line by line**, because a wrapped heading defeats a literal-space line-scoped pattern (D244). Exclusions, named: `.git/` only, plus binaries (NUL in the first 8 KiB) and files over 20 MB; tracked, untracked, gitignored, `dist/`, `scripts/` and `sdk/tests/fixtures/` are all IN. **WHY 153 AND NOT 120, AND IT IS NOT AN EXCLUSION DIFFERENCE:** the 153 occurrences sit on only **121 distinct lines** — 22 lines carry two or more and `docs/DOCKET.md:614` alone carries eight — so the prior figure was `git grep -n \| wc -l`, a line count, one line off this frame because HEAD has moved. Sub-counts that a line count also loses: **7 occurrences are newline-wrapped** and invisible to any literal-space pattern, 1 is lowercase, and 35 sit inside document-level strike spans. **THE CONTROL SET WAS USED AND IT IS OUT OF DOMAIN — THAT IS THE FINDING, NOT AN EXCUSE.** `scripts/use_mention_control_set.json` is 126 rows (53 ASSERT / 66 MENTION / 7 AMBIGUOUS; 48 DESIGN / 78 FRESH_HELDOUT) over the **untrained-QCR attribution** family; only **3 of its 126 rows contain the string "PASS WITH" at all**. Its shipped classifier `scripts/use_mention_discriminator.py` was run and **reproduces its published held-out precision exactly** — ASSERT 7/10, MENTION 23/30, 41 of 78 decided — so the instrument is sound where it was validated. Transferred to this family it returns **0 USE, 120 MENTION, 33 CANNOT_TELL**, i.e. **USE recall 0 of 49**, while its MENTION precision holds at 96/120 = 80%, indistinguishable from its in-domain 77%. The cause is named in its own error class 3: `_ADJUDICATION` matches `verdict\|graded\|withdrawn\|struck\|criterion\|defect\|superseded\|stale`, and a withdrawn VERDICT LABEL occurs only ever in grading prose, so the MENTION signal fires on essentially every sentence in the population; a second, circular defect is that `exception` is itself in `_ADJUDICATION`, so `PASS WITH EXCEPTIONS` supplies its own mention evidence (masking the claim span fixes that and changes nothing, because the first cause dominates). **SO THE 49 USES ARE A HAND ADJUDICATION OF ALL 153, DECLARED AS SUCH, NOT AN INSTRUMENT'S OUTPUT** — and the instrument's own D249 precedent agrees, having hand-read 28 of these at `docs/DOCKET.md:614` and called every one a use. Concentration: 22 of the 49 uses are in two files, `campaign/LADDER_V_STATE_2026-08-16.md` (11) and `campaign/V16_GRADE.md` (11), then `campaign/LADDER_V_PASS1_2026-08-11.md` (5), `campaign/LADDER_V_TRIPLE_VERIFICATION.md` (4), `campaign/LADDER_V_V12_V13_V14_GRADE_2026-08-15.md` (4), `campaign/LADDER_V_V6_V10_REGRADE_2026-08-15.md` (3), `docs/PRODUCT_LIST.md` (3), `campaign/LADDER_V_V5_GRADE_2026-08-15.md` (2), `campaign/LADDER_V_V5_V14_REGRADE_2026-08-16.md` (2), `campaign/LADDER_V_V5_V8_GRADE_2026-08-15.md` (2), `docs/DOCKET.md` (1), `campaign/LADDER_V_V13_CLOSEOUT.md` (1). The mention mass is where it SHOULD be and is correctly there: `docs/DOCKET.md` 31 mentions to 1 use, `campaign/LADDER_V_D227_ENTITLEMENT_2026-08-16.md` 20 to 0, `scripts/check_verdict_cells.py` 6 to 0. **`docs/PRODUCT_LIST.md:3950` IS CONFIRMED A USE**, in the `2026-08-11 — Katie's directives land` block `:3946-3951`: *"two consecutive rounds returning only findings that would not move an external reader's belief closes the rung **PASS WITH RESIDUALS**"* — unquoted, unstruck, not blockquoted, carrying no withdrawal frame, and stating in the product record's own voice what R-VALUE does. Its source rule at `campaign/LADDER_V_TRIPLE_VERIFICATION.md:590-591` is the same content and is also a use. Five further calls are flagged rather than buried: `campaign/LADDER_V_STATE_2026-08-16.md:126,127,130,131,132`, the *"Grade of record says"* column, counted USE because it commits that document to the rung carrying the label, while the paired *"Ledger says"* cells at `:92` and `:125` are counted MENTION because that column exists to quote the cell being faulted. | Whole tree at frame `a5080f6e`, 2026-08-17, excluding `.git/` only; control set `scripts/use_mention_control_set.json` and its classifier `scripts/use_mention_discriminator.py` both run rather than read. | **THE NUMBER TO ACT ON IS 49 IN 12 FILES, NOT 120 IN 25**, and filing the unsplit figure would have sent a repair at 104 correct mentions — including the docket rows that WITHDREW the labels and the checker's own forbidden-token literals, which a naive sweep would delete and thereby destroy the record of the withdrawal. **NOT SWEPT HERE:** repairing 49 verdict labels across 12 grade records is a grader's act, because in every one of them the demotion question is *entitled to plain `PASS`, or `FAIL`* — the question `D227` had to measure rung by rung — and it is not answerable by substitution. **WHAT WOULD CLOSE THIS ROW** is a use/mention discriminator re-validated ON THIS FAMILY: the existing one is honest, three-valued and correct where it was measured, and its 0-of-49 recall here is the standing demonstration that an instrument validated on one claim family and transferred to another without re-measurement will report a defect class fully closed. | **chief** (whether a withdrawn label sitting unamended in a landed grade record is repaired in place or ruled superseded); **FLEET** (re-validating the discriminator on this family). Whose: chief first, then FLEET. |
| D343 | **THE `2 DECIDED` IS RIGHT AND THE REPORT AROUND IT IS THE DEFECT: `check_normative_clauses.py` PRINTS `1 TRUE` ON ITS DECISION LINE AND A `TRUE ... [0]` BUCKET SIX LINES LOWER, WITH NOTHING JOINING THEM, AND ITS JSON CANNOT RECONSTRUCT ITS OWN TOTAL.** The handed-back claim was that *2 decided* contradicts *FALSE 0 + UNDECIDABLE 1 + TRUE 0 = 1*. Run at frame `a5080f6e`: exit 0, `VERDICT: PASS`, `1078 normative clause(s) examined and 2 decided`. **THE ARITHMETIC HOLDS AND THE CLAIM DOES NOT.** `decided = len(findings) + len(cleared)` (`:756`, `:863`) = 1 + 1 = 2, and the decision line at `:863-865` is internally consistent — `0 FALSE + (0 true_ + 1 cleared) TRUE + 1 UNDECIDABLE = 2`. The three bucket sections below print `len(false_)`, `len(undec)` and **bare `len(true_)`** (`:936-937`), i.e. FINDINGS ONLY, because a clause that obeys its rule WITH its board triple is deliberately suppressed from the findings list at `:504-516` — *"not a FINDING — NEG-6 is the control that says so — but it IS a DECISION"*. So there is no double count, no unprinted bucket and no miscount; the two numbers count two different REPORTING populations on purpose, and the purpose is defect class B1 at `:106-121`, where this check once shipped `PASS` over *"887 examined and 0 decided"*. **WHAT IS REAL IS THE PRESENTATION, AND IT IS WHAT PRODUCED THE FALSE REPORT:** the one `cleared` clause is a genuine corpus clause and not a control fixture — `demo-output/website/CLOSURE_CHALLENGE_STATUS.md:906`, quantity `seed_bound`, written `0.002419`, agreeing with `closure_challenge_seed_sensitivity.json` → `spreads.overall_equivalent_S_bound` and carrying its board triple — and it appears in **no** printed bucket, under a caption that says `(none matched)`. The same split leaks into `--json`: `frame.clauses_decided = 2` and `frame.clauses_cleared = 1`, but `counts = {"FALSE": 0, "UNDECIDABLE": 1, "TRUE": 0}` (`:990-991`), so a consumer reading `counts` alone **cannot reconstruct 2 and will conclude exactly what was handed back here**. A report whose own reader derives a contradiction from it has a defect even when its total is correct. `--help` offers only `--root`, `--verbose` and `--json`; there is no selftest flag, and the 12 in-memory controls (5 POS / 7 NEG) all reported `[ok ]` on this run. | `scripts/check_normative_clauses.py` — `:504-516` (the `cleared` suppression and its stated reason), `:738-740` (buckets), `:755-757` (the summary total), `:863-865` (the decision line), `:936-940` (the TRUE bucket header), `:990-991` (the JSON `counts`). Executed 2026-08-17 at frame `a5080f6e`. | **NOT FIXED HERE.** The number needs no change; the fix is one line and it belongs to whoever owns the check, because choosing between the two forms is a reporting decision: either the TRUE bucket header carries `len(true_) + len(cleared)` with the cleared clause's path and line named beside it, or the decision line reads `{len(true_)} TRUE + {len(cleared)} CLEARED`. Cheapest lossless form is the second. `counts` should additionally gain `"CLEARED": len(cleared)` so the JSON total is reconstructible by a consumer that never reads the prose. **The general lesson is the one this row was mis-filed under:** a suppressed-but-counted population must be visible in every surface that publishes the total, or the next reader files the arithmetic as a bug — which is precisely what happened. | **FLEET** (the check's owner; one-line fix, and the choice between the two forms). Whose: FLEET. |
| D344 | **D339's REMEDY IS OPEN, MEASURED RATHER THAN ASSUMED: NO EXECUTABLE IN THIS REPOSITORY GRADES A BYTE-IDENTITY ASSERTION, AND THAT ABSENCE IS WHAT LET ONE CERTIFICATION SURVIVE THREE INDEPENDENT REPRODUCTIONS OF ITS OWN FALSIFICATION.** D339 (`docs/DOCKET.md:704`) repaired an instance and said so; its remedy column names what would CLOSE it — *a recogniser for sentences of the shape "N lines are byte-identical to \<anchor\>", which resolves the anchor, re-runs the aligned comparison and fails on disagreement* — and adds *"until one exists, this class is re-checked only by hand and this row is the standing notice of that."* **THE PRECONDITION IS UNMET AND THE SEARCH IS DECLARED.** Every check under `scripts/` was opened: `check_summary_consistency.py` is the only one built around a byte-identical block and it **explicitly refuses the unit** at `:810-811` and `:890-891` — *"byte-identity decides nothing, so no diff is consulted anywhere"* — grading a summary row against its own section instead; `check_normative_clauses.py` grades directives that pin a figure; `check_derived_figures.py` grades a figure against a machine record; `check_absolutes.py` grades absolute claims in prose; `check_verdict_cells.py` grades a verdict cell against a grade record; `docket_citation_guard.py` resolves citations; `check_docket_reconciliation.py` grades row presence; `hunk_check.py` grades an author's PRE-DECLARED line counts for a commit and never rebuilds a named ancestor's block; `detect_overwrite_signature.py` reads mtime ordering; `self_audit.py`, `check_rung_attribution.py`, `installed_registry.py`, `phase2_move_map.py` and the `sdk/scripts/closure_round*` family mention byte-identity only in docstrings or compute sha256 over CSVs. **None takes a SENTENCE CLAIMING byte-identity as its unit.** No path in the tree matches `*byte*`, and `git log --diff-filter=A` over `scripts/` and `sdk/scripts/` since 2026-08-16 adds no such recogniser. **THE COST OF THE ABSENCE, NAMED:** the §5 DISCHARGED banner of `demo-output/website/CLOSURE_CHALLENGE_SUBMISSION_DRAFT.md` certified *thirty byte-identical body lines and every row of the ten-row defect table*; the truth is **29 of 30 body lines and 9 of 10 table rows**, block index 21 — row 7 of the ten — having been moved by the pure 186-byte insertion at `2cec44ee` (2026-08-12 18:05:51Z) after the certification was written at `b65bdf01`. It was reproduced **three times by hand**: 2026-08-14 as F4' (HIGH) in `campaign/LADDER_V_V8_REVERIFICATION_2026-08-14.md`, 2026-08-15 as R1 (*"F4 is NOT repaired"*) in `campaign/LADDER_V_V5_V8_GRADE_2026-08-15.md`, and 2026-08-17 at frame `101079fd` in `campaign/LADDER_V_V8_REVERIFICATION_2026-08-17.md`, which states at `:197` *"bytes and got the same answer. This is the third independent reproduction."* **EACH REPRODUCTION LEFT NO EXECUTABLE BEHIND, WHICH IS THE WHOLE MECHANISM:** reproducing was cheap and repairing required choosing between two non-equivalent repairs, so the cheap half ran three times and the expensive half ran once, at `adff0697`. **AND A FOURTH INSTANCE OF THE SAME ABSENCE LANDED TODAY:** the non-author grade of the `adff0697` repair rebuilt the bytes to test its pure-insertion claim, found the original certification true when written and falsified the following day — and **filed no record and committed nothing**, so that grade cannot be cited and V8's cell in `campaign/LADDER_V_TRIPLE_VERIFICATION.md` states the `GATE FAIL` at `101079fd` instead. A hand check that leaves no artifact is indistinguishable, one day later, from a hand check that never ran. | `docs/DOCKET.md:704` (D339 and its remedy column); the certified sentence at `demo-output/website/CLOSURE_CHALLENGE_SUBMISSION_DRAFT.md` §5; the three reproductions at `campaign/LADDER_V_V8_REVERIFICATION_2026-08-14.md`, `campaign/LADDER_V_V5_V8_GRADE_2026-08-15.md` and `campaign/LADDER_V_V8_REVERIFICATION_2026-08-17.md:195-197`; the repair at `adff0697`. Script sweep executed over `scripts/` and `sdk/scripts/` 2026-08-17 at frame `a5080f6e`. | **STILL OPEN, AND THIS ROW EXISTS TO STOP D339 BEING READ AS CLOSED BY ITS OWN REPAIR** — D339 corrected an instance append-only and said in its own remedy column that the class stays hand-checked until an instrument lands; nothing has landed. What settles it is the recogniser D339 specifies, built to the same contract as its neighbours: three-valued, `lab_check`'s `0/1/3` exit, planted-error controls in the defect's own vocabulary and struck negatives that must stay silent — the shape `check_verdict_cells.py` shipped at `12f8a83e` and hardened at `a5080f6e`. **Two design points this instance pays for in advance:** the anchor must be resolved and the bytes REBUILT, never compared to a stored count, because a stored count is what failed; and each side must be aligned on its **own heading line rather than on a line number**, which is how `adff0697` reproduced 3-of-32 across two trees whose line numbering had diverged. Cost estimate stands at a few hours. Until it exists, **any grade that turns on a byte-identity claim must file its rebuild as an artifact**, because today's fourth instance shows the hand check is lost the moment its author stops. | **chief** (whether the recogniser belongs in `scripts/` beside the other claim-class checks or in the ladder's own harness — the same open question D339 left); **FLEET** (building it). Whose: FLEET. |
| D345 | **A GRADE WAS REACHED AND NEVER FILED, SO THE RUNG SILENTLY REVERTED TO A STALE VERDICT — AND THE GRADER WHO DID IT WAS APPLYING THE RIGHT STANDARD TO EVERYONE BUT HIMSELF.** A non-author graded the `adff0697` repair of V8's blocking finding, re-derived the aligned diff, tested the pure-insertion claim by rebuilding the bytes, and reached **`PASS`** — then committed nothing and filed no record, because the instruction to commit was phrased conditionally and the shared index was known to hold phantom entries. **The reasonable-sounding half is the trap: a conditional instruction to commit is not a licence to leave a verdict unrecorded.** The consequence was mechanical and correct — `4a207196` searched at frame `a5080f6e`, found that no file in the tree mentioned `adff0697`, that no untracked `.md` existed under `campaign/`, and that `git log --all --diff-filter=A` over the campaign V8 records returned four and no fifth, and therefore restated V8 as the `GATE FAIL` at `101079fd`, the last verdict with an artifact behind it. **That was the correct call, and it is the same standard this lab applies to a gate measured against a reference that was never obtained.** The generalisable shape: **a verdict exists in the tree or it does not exist at all**, and the grader is the last person able to notice the difference, because to everyone downstream an unfiled `PASS` and an ungraded rung are the same observation. | The V8 cell at `campaign/LADDER_V_TRIPLE_VERIFICATION.md`, rewritten by `4a207196` with its own search transcript; the grade itself, re-executed and filed at `campaign/LADDER_V_V8_REPAIR_GRADE_2026-08-17_NONAUTHOR.md`, every measurement re-run at frame `32d4ae0d` rather than transcribed from the unrecorded round | **THE INSTANCE IS CLOSED — the record is filed and the cell now cites it.** What would close the CLASS is a check that is already most of the way built: `check_verdict_cells.py` grades a cell against a named grade record and `4a207196` used it to find eleven cells naming none, so the missing half is the reverse direction — a rung whose ledger cell cites a record that no commit ever added. Cheaper still, and worth more: **no agent should be asked to commit conditionally.** A grading task either produces a record or produces `BLOCKED` with a reason, and "I measured it but did not write it down" should not be a reachable state | **chief** (whether conditional-commit phrasing is ever legitimate in a grading brief); **FLEET** (the reverse-direction check) |
| D346 | **STRIKEOUT IS INVISIBLE TO TEXT EXTRACTION, SO EVERY PDF-SURFACE AUDIT IN THIS LAB IS STRIKE-BLIND IN BOTH DIRECTIONS — AND THE FIRST EVIDENCE OFFERED FOR THIS ROW WAS ITSELF WRONG, WHICH IS THE HALF WORTH KEEPING.** `pdftotext` renders `\sout{}` as a glyph overlay rather than as characters. Demonstrated by controlled build rather than inferred: a two-line document whose source reads `Live claim: P(rank 1) = 50\%.` followed by `\sout{Struck claim: P(rank 1) = 68\%.}` extracts as `Live claim: P(rank 1) = 50%. Struck claim: P(rank 1) = 68%.` — **the struck sentence carries no marker of any kind.** Consequence: a recogniser that masks struck text, which is how every claims-language instrument here stays silent on repaired prose, loses that entire class the moment its corpus is a rendered page. It will read repaired text as live, and it cannot distinguish a genuine live violation from a struck one sitting beside it. **THE CORRECTION, AND WHY IT MATTERS MORE THAN THE ROW.** This row was first asserted on the evidence that a V8 recogniser flagged three `P(rank 1) = 68%` sites in `demo-output/website/latex/closure_challenge_report.pdf` while the `.tex` carries six `\sout{}` 68% sites and five live 50% sites — reported as "three false positives from strike-blindness". **That was wrong: the mechanism was inferred and never tested against the artifact.** Re-measured on the PDF itself, it contains **zero** occurrences of `P(rank 1) = 50`, **zero** of `six-entry` and **zero** of `Yang`; it was last committed at `98a39662` (2026-08-11 03:29Z) and the `.tex` was repaired at `2cec44ee` (2026-08-12 18:05Z). **The PDF predates the strikes entirely, so its figures were never struck and the three flags were TRUE POSITIVES restating `D115`.** The blind class is real; that artifact was simply not an instance of it | Controlled `pdflatex` + `pdftotext -layout` build, 2026-08-17; and the re-measurement of `closure_challenge_report.pdf` against its own `.tex` and both of their landing commits, at frame `32d4ae0d` | **NOT FIXED.** Two separable halves. (a) **Do not grade claims on rendered PDFs at all where a source exists** — grade the `.tex` and check separately that the PDF is a current build of it, which is `D115`'s standing finding and is the cheaper ordering. (b) Where no source exists, a strike-aware reader must recover the overlay: `pdftotext -bbox-layout` plus the page's line-drawing operators can in principle re-associate a strike rule with the glyphs it crosses, but this is real work and should not be started before (a) is ruled on, since (a) may remove the need. **Until either lands, any figure read out of a PDF in this repository must be reported as strike-unknown rather than as live** | **chief** (ruling on (a)); **FLEET** (only if (a) is refused) |
| D347 | **A RECOGNISER RETURNED ZERO ACROSS 2,014 FILES AND THE ZERO WAS THE INSTRUMENT, NOT THE CORPUS: `[-\s]{1,20}?` IS A LAZY QUANTIFIER, NOT AN OPTIONAL ONE.** Building a V8 rank-claim recogniser under this lab's blank-delimiter discipline, the author wrote joiners as `J = r"[-\s]{1,20}"` and then formed supposedly-optional joiners as `J + "?"`. In Python's `re`, `X{1,20}?` is the **lazy** form of `X{1,20}` and still demands at least one character; the optional form is `(?:X{1,20})?`. Every optional joiner therefore became mandatory, so `P(rank 1)` — which has nothing between `P` and `(` — could never match, and a whole-corpus sweep of 2,014 text files returned **0 sites**. **THE DANGER IS THE SHAPE OF THE FAILURE, NOT THE TYPO.** A zero from a claims sweep is exactly what a clean corpus looks like. Had it been reported, it would have been a green result on the rung then under audit, produced by an instrument that could not have found a violation if the document had consisted of nothing else. It was caught only because the lab's standing rule — **a zero requires a recognition control, never a reachability one** — was applied before the zero was written down: the first plant failed to fire and the instrument was opened. The same build carried a second defect of the same family, caught the same way: matching only the strikes-blanked buffer made the STRUCK branch dead code, while matching only the strikes-preserved buffer read `P(rank 1) is ~~91%~~ **78.5%**` as a live 91% claim, label live and value struck. **This is a hazard of the delimiter discipline itself** — `[-\s]{1,20}` is mandated in place of `\s+` precisely so joiners span hyphens and line wraps, and the mandated form is one `?` away from silently requiring what it was meant to make optional | The recogniser's own recognition control, 2026-08-17: 14 of 14 positive plants and 4 of 4 negative controls after repair, 0 of 14 before it. Instrument and controls at `campaign/LADDER_V_V8_REPAIR_GRADE_2026-08-17_NONAUTHOR.md` §5.2 | **NOT FIXED, and a fix is cheap.** The discipline that mandates `[-\s]{1,20}` should mandate its optional form in the same breath — write `JO = r"(?:[-\s]{1,20})?"` beside `J` wherever the rule is stated, so no author has to derive it — and should state the rule that produced the catch: **a claims sweep returning zero is not a result until a plant in the claim's own vocabulary has been shown to fire against the same corpus.** Neither costs more than a sentence, and the second is already this lab's practice in prose without being written where recogniser authors read it | **chief** (where the delimiter rule is stated, so the optional form travels with it) |
| D348 | **A CHECK THAT DECIDES WHETHER A CITED TEST EXISTS WAS READING THE SHARED INDEX, SO THIS LAB'S OWN COMMIT PROTOCOL MADE ITS NEWEST TESTS UNCITABLE.** `scripts/check_absolutes.py:known_test_names()` enumerated the corpus with `git ls-files`, which lists INDEX entries; an absolute naming a test it cannot find is condemned `CITES_MISSING_CHECK`, the severe class. The private-index commit form this lab commits by (`GIT_INDEX_FILE=<tmp> git add` -> `write-tree` -> `commit-tree` -> `update-ref`) leaves every committed file with NO shared-index entry, so the invisible tests were disproportionately the NEWEST -- the freshest evidence was the likeliest to be called missing, and the gap grows on its own with every such commit. Measured in the live checkout: 2,597 names off the index against 2,658 off HEAD (61 real test functions unable to back a claim); 477 paths present in HEAD and absent from the index and ZERO the other way, which names them as landed work rather than deletions; 100 prose citation sites across 5 files move out of the condemned class. | Handed over as a standing finding; re-derived by execution at `3af826ed` after a machine poweroff destroyed the first measurement | Repaired at the FRAME rather than the symptom: enumerate `git ls-tree -r HEAD`, the referent `scripts/lab_check.py:tracked_frame` already carries under D274, with a fallback to the committed blob for a path in HEAD but not on disk. Controlled by `scripts/mutation_harness_known_test_names.py`, 14/14. Its positive control is a file landed by the private-index form, because a NORMALLY STAGED file is green before AND after the repair and measures nothing; its negative control plants an absolute citing a test that exists nowhere and asserts it is STILL condemned end to end, so the frame narrowed and the class did not blind. |
| D349 | **A CITATION GUARD READ TWO NON-CITATIONS AS CITATIONS: A NUMBER NAMED AS A GAP, AND A CUE GLUED INSIDE AN IDENTIFIER.** `scripts/docket_citation_guard.py` returned FAIL on two numbers that have no row -- a gap (D188), and an unallocated (D901) fixture constant -- and neither available repair was legitimate: W-4 forbids renumbering and the standing rule forbids creating a row to satisfy a citation, while editing either text would have deleted a TRUE statement to appease a checker. (a) The gap (D188) is named only by `campaign/LADDER_V_V16_ROUND12.md:61`, in the phrase `one gap (D188)` -- a record REPORTING that the number has no row. It never had one: a `git log --all -S` pickaxe over the docket file is empty across every ref, and the ID sequence at HEAD has exactly that one hole, left deliberately by an ID block asserted and aborted under concurrency. A number said to be ABSENT is mentioned, not cited; the sentence is the inverse of the defect the guard exists to catch. (b) The unallocated (D901) constant is a synthetic out-of-range fixture value inside a `TempDocketRepo(rows(...))` call, which supplied its own `docket` cue from the class NAME, making the guard's behaviour arbitrary rather than merely strict. **This row is written under the convention it establishes**, which is not decoration: as first committed at `c83c9de0` it named both numbers beside a cue and outside any absence language, and the guard read the row documenting the use-mention distinction as two fresh unresolved citations -- D3's shape exactly. Every number named here now sits inside the absence language that is true of it, and the fixture's literal argument list is described rather than reproduced. | `scripts/lab_check.py --no-tests`, verdict FAIL, at `32d4ae0d` and again at `3af826ed`; the self-citation caught by re-running the repaired guard against the tree that carried the new row | Both repaired at the PREDICATE with paired controls (11/11): a left word-boundary on the cue (NEG7) and a tight absence-language exclusion (NEG6), each carrying a POSITIVE twin (POS6, POS7) asserting the same number still flags once the excluding condition is removed, so each narrowing is demonstrated not to be a blinding. `ABSENCE_WINDOW` was MEASURED, not picked: at 40 characters it suppressed a genuine citation to the allocated row D35 because "the smallest gap in the data" sat 21 characters away, and it matched a section name of the form `GAP-B`; at 12 it fires only on the census sentence and the places that quote it. Guard PASSES with every citation resolving. |
| D350 | **THE F14 LAMINAR RUNG'S ENERGY-BALANCE CONTROL DOES NOT MEET THE CONVERGENCE CRITERION THAT RUNG ITSELF ESTABLISHED, AND ITS OWN PUBLISHED TABLE SAYS SO WITHOUT ACTING ON IT.** K0c gates the graded quantity on a peak-to-peak spread of 0.02 % over a fixed 400-iteration window. Re-measured at K1a by execution, from the committed logs alone, with the criterion now standing in `scripts/check_convergence.py --monitor-regex`: ten of the eleven committed K0c cases pass at 0.000219 % to 0.008403 %, and `C3_Ra1e5_m64_source` reads **0.123720 %** — a factor of six over the criterion, and the same figure K0c_RESULTS.md prints in its own convergence table. K0c graded eight cases and C3 is not among them: it is the planted-source control for the energy row, and its Nusselt convergence was never load-bearing there. So nothing published is wrong. But the criterion is standing now, it fires on this case, and a reader running the standing check over that tree gets one FAIL with no note explaining it. | F14 rung K1a, `scripts/check_convergence.py` signature 5 replayed over `docs/campaigns/F14-cooling-ladder/K0c_runs/*/log.buoyantBoussinesqSimpleFoam*` in a clean detached worktree at `3af826ed` | Either continue `C3_Ra1e5_m64_source` until it meets the criterion and re-audit its energy row (about 20 core-seconds at the stage-2 relaxation factors, on a 64x64 mesh — K1c's equivalent controls cost 17.1 to 18.9 s each to 4000 iterations), or record in `K0c_RESULTS.md` that the case is deliberately ungated on that criterion and why. Not settled here: K1's declared scope is making the checks standing and verifying them, not re-opening a closed rung's cases. | fleet |
| D351 | **A PLANTED DEFECT MOVES THE REGIME NUMBER THE AUDITOR REPORTS, SO A CONTROL AND ITS PARENT CASE PRINT DIFFERENT RAYLEIGH NUMBERS FOR THE SAME DESIGN.** `scripts/heat_balance.py` builds Ra from the temperature range of the solved field (`max(dT_field, dT_wall)`), which is the right quantity for the realised flow and the wrong one for identifying the case. Measured at K1c: the unplanted control `KC0_nosource` reports **Ra = 1.000000e+05**, and its twin `KC1_src_5mW` — same mesh, same properties, same wall temperatures, one 5 mW volumetric source added — reports **Ra = 1.799639e+05**, because the source raises the field's own range from 1.088162 K to 1.958300 K. Both numbers are correct and they answer different questions. A reader comparing a control row against its parent row sees the regime move and has nothing in the report telling them the design Ra did not. | F14 rung K1c, audit JSONs of the six control cases, clean detached worktree at `3af826ed` | Report both: the design Ra from the case's own boundary condition dT alongside the realised Ra from the solved field, with the second labelled as such. Script work in `scripts/heat_balance.py`, no compute. Not settled here: it changes a reported figure on every existing thermal audit, and K1c's whole no-regression proof rests on no existing figure moving. | fleet |
| D352 | **THE INSTALLED CRONTAB AND ITS TRACKED COPY DISAGREE, AND THE DISAGREEMENT IS THE REMEDIATION FOR THIS MORNING'S POWEROFF.** `scripts/installed_registry.py` moved from PASS to FAIL with `DRIFT ubuntu crontab (scripts/installed/crontab.ubuntu -> $ crontab -l)`. Diffed rather than guessed: the live crontab carries five lines the tracked copy does not -- a comment block dated 2026-08-17 explaining a deliberate keep-alive, `0 */6 * * * touch /home/ubuntu/Certonomous/.autostop-hold`, and `@reboot sleep 20 && touch ...` -- installed after a stale `auto-stop.sh` read an active session as an idle box and powered the machine off at 17:45:14Z, killing five agents mid-work. The registry is behaving exactly as designed: it noticed within the hour that the box's state and the repository's record of that state had parted company. FILED RATHER THAN REPAIRED, and deliberately: this is a change to the BOX, made by whoever owns the box, and committing the tracked copy forward would be this agent recording a decision it did not make and cannot ratify -- the same reasoning D219 applies to agenda ratification. Note also that the hold is self-expiring by its own comment, so the tracked copy must not be updated to match a state that is designed to lapse. | `scripts/lab_check.py --no-tests` at `a871c520`; `diff <(crontab -l) scripts/installed/crontab.ubuntu` | The box's owner either ratifies the keep-alive into `scripts/installed/crontab.ubuntu`, or removes it from the crontab when the hold is no longer wanted. Either action clears the DRIFT; a third party doing it for them does not. |
| D353 | **R-CONVERGE'S FILING WAS NEVER PERFORMED FOR V6, SO THREE OUT-OF-SCOPE FINDINGS HAVE BEEN CARRIED BY A LEDGER PARENTHETICAL RATHER THAN BY THE QUEUE THAT EXISTS FOR THEM.** V6 closed a plain `PASS` on the R-CONVERGE route, whose whole condition is that an out-of-scope finding is *"filed as a docket item, not appended to the live rung"*. The grade of record appended all three to the live rung and filed none, and D233 named them only inside its own narrative, so at HEAD `3f46e4e9` no row in this file carried V6-R1, V6-R2 or V6-R3. Filed here at last, re-measured rather than inherited, in a clean detached worktree and again in the dirty checkout with four recognition controls planted by line index. **V6-R1 and V6-R3** -- `demo-output/website/CLOSURE_CHALLENGE_SUBMISSION_DRAFT.md:301-303` states *"Ten of the eleven are byte-identical to what `472f9f92` wrote"* with *"the eleventh being 4.7"*; the measurement is **nine of eleven, 4.7 AND 4.8 differing**, and the sentence is self-contradictory on its own face because it names 4.8 as *"repaired above"* two lines earlier. **V6-R2** -- `:323`'s evidence column states the section sits *"212 lines below in this same file"*; the true distance is **584 - 323 = 261** at this HEAD, which is neither the stated 212 nor the **256** recorded on 2026-08-16, so the locator has drifted a further five lines since it was last measured. All three are live and unrepaired. | Adjudicating the V6 verdict cell against its grade of record under D338; the R-CONVERGE filing gap found by looking for the rows the plain PASS depends on and not finding them | A repairing pass with an editing licence for the submission draft corrects the byte-identity sentence to nine and the locator to a relative reference that cannot drift, then strikes these three here. None of the three moves the V6 verdict, which is a plain `PASS` on both limbs of the scope test | Unassigned; not this pass's, which is a grader and may not repair its own subject |
| D354 | **A GRADE RECORDED A DOCKET FILING THAT NEVER LANDED, AND THE THREE RESIDUALS IT DID NOT CLAIM TO FILE WERE APPENDED TO THE LIVE RUNG INSTEAD.** `campaign/LADDER_V_V12_V13_V14_GRADE_2026-08-15.md:515` reads *"Filed to `docs/DOCKET.md`, not appended here: the residuals R1, R2, R3, V13-a and the 4.4 citation defect."* At HEAD `3f46e4e9` **no row in this file carries V13-a**, by label or by content, and V13-b, V13-c and V13-d were named in the verdict line rather than filed anywhere. V13's plain `PASS` runs through R-CONVERGE, which requires the filing, so the rung's verdict has rested on a step that was reported as done and was not. Filed here, all four, re-measured at this HEAD. **V13-a** -- the shipped-zip drift figure in close-out 8 is `51/56` and measures `50/56` since `sdk/chief_engineer/certificate.py` became a sixth stale member. **V13-b** -- the corrected V15 ledger row names rounds 5 to 7 and round 8 landed `05354615`. **V13-c** -- `campaign/LADDER_V_V13_CLOSEOUT.md:94` still reads *"NO -- SELF-GRADED at the last step"* while `:597` of the same file reads *"moves off CLOSED-BY-ITS-AUTHOR"*, neither struck against the other; both mitigations hold, `:597` self-qualifying *"but only as a statement about `2b251689`"* and `:94` closing *"See 8 PENDING-2"*. **V13-d** -- the *"not edited"* and *"still empty as slots"* pair has **moved to `campaign/LADDER_V_TRIPLE_VERIFICATION.md:1332-1334`** from the `:1193-1194` recorded on 2026-08-16, and is still false. | Adjudicating the V13 verdict cell against its grade of record under D338 | Each of the four is a staleness repair owned by whoever holds the surface. V13-c and V13-d are the two that a reader meets as flat contradictions and are worth doing first. None of the four moves the V13 verdict, which is a plain `PASS` on both limbs of the scope test | Unassigned; not this pass's |
| D355 | **THE SAME GRADE FILE STILL CARRIES THE WITHDRAWN LABEL FOR V12, AND THIS PASS DELIBERATELY DID NOT TOUCH IT.** `campaign/LADDER_V_V12_V13_V14_GRADE_2026-08-15.md` states `PASS WITH RESIDUALS` for **V12** at `:170` and `:507` -- illegal on exactly the ground that withdrew it lab-wide at `7c44cbe2` under D249, absent from the five-term vocabulary at `docs/charters/VERIFICATION_CHARTER.md:95-96` and `docs/charters/REPORTING_CHARTER.md:210-211`, and invisible to section 16's negative-verdict sweep by construction. This pass amended the V13 label in that same file and left V12's standing, because **V12's verdict of record is not this document**: the ledger's V12 cell cites the later `campaign/LADDER_V_V12_GRADE_2026-08-16_NONAUTHOR.md`, and the chief ruled V12 NOT ENTITLED and `FAIL` at `campaign/LADDER_V_TRIPLE_VERIFICATION.md:719-724`. Amending it here would be grading V12 on the way past, which is the failure R-CONVERGE exists to prevent -- so it is filed rather than acted on, in the same act that shows the shape is worth checking for. **The general form is what earns the row:** a verdict cell can be corrected while the grade it cites keeps the illegal label, and nothing reads a GRADE RECORD's label against the vocabulary -- `scripts/check_verdict_cells.py` reads the record only to compare it with the CELL, so a record whose label is illegal but whose cell agrees with it is silent in both directions. | Amending the V6 and V13 grade records under D338 and finding a third live label in a file being edited for a different rung | A non-author of the V12 grades amends `:170` and `:507` to the verdict of record, append-only. Separately, an instrument that reads every grade record's stated label against the five-term vocabulary directly, rather than only against its cell, would close the class rather than this instance | Unassigned; explicitly not this pass's, which is a non-author of V6 and V13 only |
| D356 | **EVERY K1 THERMAL INSTRUMENT INVOKED FROM THIS CHECKOUT RUNS THE PRE-K1 CODE, AND ONE OF THE TWO FAILS QUIET.** The K1 rung landed the standing thermal checks by private-index commit (`4afefe54`) and, per the documented steady state of that commit form, never wrote back to the worktree — so the live checkout's copies of all four governing surfaces are stale in the reverting direction: `scripts/heat_balance.py` 541 lines against HEAD's 901, `scripts/check_convergence.py` 472 against 687 with **no `classify_monitor` and no `--monitor-regex` mode at all**, `docs/physics_rules.yaml` 99 against 257 with **no `thermal` block**, `docs/standards/MONITOR_STANDARD.md` v1.8 against HEAD's v1.9 (no S13–S15). Measured, not inferred: each worktree copy hashes byte-identical to its blob at `3af826ed`, the pre-K1 commit — pure staleness, zero unlanded edits, so a worktree sync of exactly these four paths loses nothing. The failure asymmetry is the L-93 shape: `check_convergence.py --monitor-regex` from this checkout dies loud on an unknown argument, but `heat_balance.py` **runs to completion and silently omits** the S14 identity-class stamp, the S15 fvOptions witness, and the physics-rules governance — an audit that looks complete and is missing exactly the parts K1 exists to add. Found while writing the F14 K2a spec against the HEAD `thermal` block and discovering the worktree file does not contain it. | F14 rung K2a, 2026-08-17; line counts and blob hashes measured in the live checkout against HEAD `3f46e4e9` | Copy the four paths from HEAD into the worktree (safe by the blob measurement above; no `git checkout --` on anything else, and the shared index — which separately holds staged deletions of tracked files including the F14 campaign README — is not touched, per USING_THIS_LAB §9 item 4: inspected, never reverted, chief's call). Class-level: the write-back step USING_THIS_LAB §9 item 8 mandates for the docket has no analogue for ordinary files landed by private index; either the committer writes back, or every wired instrument's runner re-derives its subject from HEAD | fleet |
| D357 | **AN INSTRUMENT DATED A GRADE BY ITS LAST EDIT, SO AMENDING A RECORD RE-DATED IT AS THOUGH IT HAD BEEN GRADED THAT AFTERNOON -- AND IT PUNISHED THE REPAIR IT HAD ITSELF ASKED FOR.** `scripts/check_verdict_cells.py`'s `_landed()` read `git log -1 -- <path>`, which returns the most recent commit TOUCHING a path. That equals the landing date only until somebody amends a grade record, and amending one is precisely what this instrument's `CELL-VS-RECORD` finding demands. Demonstrated by a commit rather than argued: amending the withdrawn `PASS WITH RESIDUALS` label in `campaign/LADDER_V_V6_V10_REGRADE_2026-08-15.md` and `campaign/LADDER_V_V12_V13_V14_GRADE_2026-08-15.md` moved both from their true landings of **2026-08-15 19:34:11Z** and **21:00:12Z** to **2026-08-17 19:34:20Z**, a jump of 172809s and 167648s. Three rungs the amendment never touched were immediately misreported: **V10** and **V12** as `UNCITED-NEWER` against 2026-08-15 grades that had not moved, and **V14** as `CELL-VS-RECORD` against a record that AGREES with its cell. Worse, each mis-report quoted the wrong rung's verdict, because a record grading three rungs returns one verdict to all of them -- so the finding would have sent three repair passes to three wrong places. **The corpus already used the word correctly and that is what made it decidable**: the ledger cells read *"landed `60073572`"* and `60073572` is the ADD commit at 19:34:11Z, not the amendment two days later. Fixed to the earliest `--diff-filter=A` commit, with a fallback to last-touch for a path with no add in history rather than a silent 0, which would have made every record look newer than every other. A **non-vacuous** recognition control is planted in `--selftest`: it asserts add != last on a genuinely amended record before comparing, prints VACUOUS rather than passing when it cannot discriminate, and reports the delta the old reader would have added. Selftest 12/12. | Amending the V6 and V13 grade records under D338: the amendment was correct and the instrument reported three new failures because of it | Done for `_landed`. **The second half is OPEN and is the larger one:** `record_verdict()` returns ONE verdict for a record that grades three rungs, so `LADDER_V_V12_V13_V14_GRADE_2026-08-15.md` reports V13's verdict to V12 and V14 as well. Today that is harmless only because the add-date fix stops those rows selecting it. A rung-aware reader -- take the verdict from the anchor whose window names the rung -- would close it, and would also need `blank_strikes` applied to records, which it currently is not, so a struck label in a record still reads as its verdict | Unassigned; the `_landed` half was repaired by the pass that exposed it, which is a non-author of the instrument |
| D358 | **THE MOVE MAP ROUTED A 315-FILE OPENFOAM RUN ARCHIVE INTO `/docs/` BECAUSE OF WHERE ITS AUTHOR PUT IT, AND ITS BATCH-2 GATE WAS SATISFIED BY THE RISKIER OF THE TWO OPTIONS AND FAILED BY THE SAFER ONE. THREE RULINGS AND ONE INSTRUMENT REPAIR, NOTHING MOVED.** Re-derived over `git ls-tree -r 4d7c195a` — never `git ls-files`, which is 479 paths behind HEAD and behind by construction on the newest. **(1) R25, ruled: a run tree is classified by what it is, not by where it sits.** `docs/campaigns/<campaign>/*_{runs,sensitivity}/**` follows R20's destination, `verification/runs/`, not R7's *"`docs/**` unchanged"*. Composition verified before the rule was written: of 346 tracked files under `docs/campaigns/F14-cooling-ladder/`, **315 are run trees** (`K0c_runs` 274, `K0b_mesh_sensitivity` 41 — leaf directories `system/`, `constant/`, `0.orig/` and `log.*`, the identical class `campaign/*_runs/` holds), 23 published reference data, **7 documents and 1 script — and it was 5 documents at `7554e5d3` four commits earlier**, before the K2a spec and the K2c search landed at `e893f57c`; `315+23+7+1=346`, and the two figures the rule turns on, 315 and 23, are identical at both anchors, so the drift lands wholly on the class R7 keeps. The only other prose in the tree is `reference-data/MANIFEST.md`, inside the 23. **R7 net = 186**, was 501 gross, quoted as 137. R25 preserves the campaign segment because `verification/runs/` is flat and R20 already puts 27 tree names into it — that sub-decision is the owner's to ratify and is one line in `lab_paths._MOVES`. `lab_paths` does **not** bind R25 yet; binding it was measured to move 617 dark trees / 5,447 files / 835,459,647 gitignored bytes from STAYS PUT to RIDES ALONG and to leave the hand-carry set **exactly unchanged**, so G1's baseline is untouched. **(2) `AWS_TREE_PLAN.md` joins R1**, a third root record of the class R1 already routes to `docs/`; R1 = 3, and the HEAD-frame reconciliation then closes at **780 keep + 13,511 move + 7 exceptions + 0 UNCLASSIFIED = 14,298**, with no other path failing to classify. **(3) Batch 2 takes option A** — exclude `MESH_AUDIT_runs`'s 78 `*.log.checkMesh` (78 tracked at HEAD, 78 on disk, 380,082 B both) from the untracking until batch 7 has moved the tree — **and the plan's own verification line defeated that choice, so it was amended in the same change.** `hand_carry.batch2_survivors()` classified by FILE CLASS, so *"the goes-dark section must read 0"* read 0 trivially under B and **1 under A**: satisfied by the option that grows a 1.5 GB carry set, failed by the option that keeps the move a `git mv`. It now tests membership of batch 2's own list; fired both ways on the live tree it reads 0 under A (list of 7,599) and 1 under B (7,677), difference exactly 78, carry set byte-identical. **(4) REPAIR: `hand_carry.py` could report success while data went missing.** `measure()` and `maximal_dark_trees()` walked with no `onerror` and swallowed per-file `OSError`, so an unreadable directory left the count, the byte total and the path digest short **together** — and `carry()` compares two readings by that same blinded instrument, so the comparison agreed with itself. Controlled by `chmod 000` on one directory in a scratch copy, HEAD's blob beside the repaired module on the same bytes: **pre-repair returned 7f/2,180,257B of a 14f/4,360,514B tree, exit 0, stderr empty**; repaired refuses, names the path on stderr, exits 3 UNKNOWN. Four mutants, six new tests (24 → 35 in `test_hand_carry.py`), readable-tree control green under all. **Carried forward: R8's "15 named launchers" are in no committed artefact and the reconstruction that reproduces both totals is a set that fits, not the author's list (`L-94`); and batch 6's root-ownership precondition re-measures to `! -user ubuntu` = 0 repo-wide, stale in the safe direction, so batch 6 re-runs the `find`.** Batch 0's other half was found **already done**: both PHASE2 artefacts were marked superseded at `dce05a01` on 2026-08-16 and the marks are in HEAD; an append-only note naming the four-document successor chain was added to each, editing no existing line. | MOVE_MAP batch 0 rulings, 2026-08-17, over `git ls-tree -r 4d7c195a`; no figure here is ordered or selected by a commit date, so D357's re-dating defect cannot reach any of them; `campaign/MOVE_MAP_BATCH0_RULINGS_2026-08-17.md` | Nothing outstanding on the rulings themselves — they are recorded in `campaign/MOVE_MAP_2026-08-16.md` §2.6 and §9.0 and the reconciliation closes at zero. **Three things are owed to a later batch and none is this pass's:** bind R25 in `scripts/lab_paths.py` (batch 3b), with the campaign-segment sub-decision ratified or flattened first; write batch 2's actual untracking list to a file and hand it to `hand_carry.py derive --batch2-list` rather than letting the gate reconstruct it; and re-run batch 6's `find . ! -user ubuntu` at the batch rather than reading any recorded line. | fleet |
| D359 | **THE ONE EXPERIMENTAL PRIMARY THIS CAMPAIGN HOLDS IS MISSING FIVE OF ITS TWENTY SENSOR VALUES WITHOUT SAYING SO, AND ONE OF ITS PLOTTED CFD BARS CONTRADICTS ITS OWN EQ. (9) BY 9.1 K. BOTH WERE INVISIBLE UNTIL THE FIGURES WERE DIGITIZED, AND ONE OF THEM SHRINKS A GATE ROW FROM TEN RACKS TO EIGHT.** Wibron, Ljung and Lundström 2018, *Energies* 11(3):644, CC-BY, the primary K2c-A grades against, read at `fc1e3bac` from `docs/papers/wibron_ljung_lundstrom_2018_en11030644.pdf` (SHA-256 `4de4798e...`). **(1) Five missing experimental values.** Figure 6a plots an experimental bar of height EXACTLY ZERO with a ZERO-LENGTH error bar for R5 and R6; Figure 6b does the same for R1, R5 and R6. Read as vector `re` heights, not as pixels: the bar rectangles are `2695.4 415 99.2031 0 re` and `3129.4 415 99.2031 0 re` in Fig 6a's content stream. The paper's text never mentions it. Its own sentences are consistent with the absence once found -- "all the values predicted by the CFD model are within the experimental error bars" is 8 of 8, not 10 of 10, and "all values except for two" on the back side is 2 of 7 -- and the digitized values reproduce both counts exactly (5 of 7 inside, R2 out by 1.625 K and R3 by 2.683 K), which is what makes the reading trustworthy rather than the absence suspicious. **Consequence, already applied:** `K2c_DIGITIZATION_ADDENDUM.md` §5.1 arms the rack-front temperature row for **8 racks, not the 10 that `K2c_RACK_ROW_VALIDATION_SEARCH.md` §3.1 says "per rack"**, and marks R5 and R6 NOT ARMABLE rather than REPORT-ONLY, because the distinction is absence of a reference, not a judgement about one. **(2) R6's back-side CFD bar fails the paper's own Eq. (9) by 9.1 K.** Eq. (9) is dT = q/(ṁ·c_p) with q and the face velocity both tabulated in the paper's Table 2, which forces (dT)·v/q to one constant across racks. Fitted over the eight full racks it is 1.147581e-03 K·m/(W·s) with maximum deviation **0.202 %**, i.e. 0.029 K on a 13 K rise -- the tightest control this digitisation has. Applied as a PREDICTION to the two partial racks: R5 (q = 0 W) predicts dT = 0.000 K against 0.152 K read, which the paper explains itself (§3.3 builds the back temperature on the AVERAGE front-face temperature while Fig 6a plots the SENSOR-POINT value, so the difference carries that offset); **R6 (q = 1762 W, v = 0.41 m/s) predicts dT = 4.932 K against 14.071 K read, residual +9.139 K.** Read backwards, the plotted rise corresponds to a load near 5030 W at R6's tabulated flow, close to a FULL rack rather than the six servers Table 2 records. **What this docket entry does not claim:** which of the three possibilities is true (the simulation applied a different load than Table 2 records; Table 2's R6 entry is a misprint; the Fig 6b bar is misplotted). It cannot: the underlying data is not published and the CC-BY licence does not supply it. | `docs/campaigns/F14-cooling-ladder/K2c_DIGITIZATION_ADDENDUM.md` §4.1 and §6, at `fc1e3bac`; every figure reproducible by `python3 docs/campaigns/F14-cooling-ladder/digitize_wibron2018.py`, which re-verifies the source SHA-256 before reading a path and exits 2 on a mismatch | **Nothing is owed by this lab for (1)** -- it is recorded, applied to the gate, and the machine copy marks the cells `NODATUM`. **(2) is open and is the one that could bite.** A later K2b solve that instantiates Table 2 faithfully will disagree with Figure 6b at R6 by about 9 K, and the risk is that the disagreement is read as the solve's failure. R6's rack-back row was already REPORT-ONLY and R6 has no experimental value on either face, so nothing downstream is graded on it today; the settlement is (a) that this row exists so the next grader finds it, and (b) one email to the corresponding author (emelie.wibron@ltu.se, printed in the paper) asking whether R6's simulated load was 1762 W. **No compute needed for either half.** | fleet; the author enquiry is the chief's to send or to decline |
| D360 | **`--allow-advective` COMPUTES NO ADVECTIVE TERM, STAMPS NOTHING, AND THEN THE PRINTED REPORT POSITIVELY VOUCHES FOR THE NUMBER AS "A GENUINE CONSTRAINT RATHER THAN A RESTATEMENT OF THE DISCRETISATION". THE FLAG'S ONLY EFFECT AT HEAD IS TO SILENCE THE REFUSAL THAT WAS PROTECTING THE READER.** `scripts/heat_balance.py`, worktree byte-identical to the HEAD blob at `fc1e3bac` (verified, not assumed: `git show HEAD:scripts/heat_balance.py \| diff - scripts/heat_balance.py` is empty). K2a §8 states that *"`--allow-advective` adds the term but the report is then stamped UNVALIDATED"* and that *"every advective closure number is reported UNVALIDATED in the script's own words"*. **Neither half holds in the code.** `a.allow_advective` is read at exactly ONE place, line 551, the guard on the exit-2 refusal. The only per-patch heat computed anywhere is `Q = kcond * G` at line 633, pure conduction; `rho·cp·∫T(U·n)dA` appears nowhere outside prose. The flag sets no key in the `res` dict (lines 728-748) and prints no line in `emit()`, so a run with it produces a report and a JSON with **no UNVALIDATED marking at all**. Worse, `sealed = not nonwall` at line 708 is then False, so `closure_is_identity_class` is False and `emit()` prints *"NOT of the identity class, so the balance is a genuine constraint here rather than a restatement of the discretisation"* over a ledger whose advective term was never computed -- the script's most emphatic endorsement, spent on its least trustworthy output. The docstring's UNVALIDATED stamp (lines 143, 149) and the refusal itself (551) are accurate and were introduced at `183c91c0`; `git log -S UNVALIDATED -- scripts/heat_balance.py` and `git log -S allow_advective -- scripts/heat_balance.py` each return that commit alone, so nothing has touched the advective path since. **KV1 is therefore not merely unattempted, it is not yet runnable:** it would validate a quantity the script does not compute. Repo-wide, `KV1` occurs six times, all forward-looking spec text; no KV1 case, run tree, JSON or docket row exists; no test in `scripts/` or `sdk/tests/` references `heat_balance` at all; the file's only self-calibration, `--selftest-conduction`, is explicitly the laminar wall path. | Prerequisite check run while writing `docs/campaigns/F14-cooling-ladder/K2c_DIGITIZATION_ADDENDUM.md` §7, at `fc1e3bac`; K2a §8 lines 373-382; `docs/NUMERICS_KNOWLEDGE.md:899-901` and `docs/standards/MONITOR_STANDARD.md:761-762` already scope themselves to the sealed case, so nothing downstream is quoting an advective number today | **Three steps, in order, and the first two are zero-compute.** (1) Fix the stamp before the physics: set `res["advective_unvalidated"]` whenever `nonwall` is non-empty, print it in `emit()`, and suppress the "genuine constraint here" sentence while the term is unvalidated. That alone removes the false endorsement. (2) Implement `rho·cp·∫T(U·n)dA` per non-wall patch, in the same postProcess pass as `grad(T)`, for the same reason the conduction path must be (docstring lines 33-45). (3) THEN run KV1: a straight duct, one inlet, one outlet, adiabatic walls, imposed ṁ and inlet T, plus a planted volumetric source of known watts through `fvOptions`, audited net to recover the plant within `heat_balance_source_recovery_tol_pct` = 0.1 % (already in `docs/physics_rules.yaml:243`, and the same constant K1c's SEALED planted-source control used). It must be an OPEN duct: on a sealed case the balance is an identity by construction and W-2 forbids gating on it, so the through-flow is the entire point. **Compute: one coarse laminar duct, the KV1 line item already in K2a §10. Not requested here and no solver was launched.** | fleet for (1) and (2); the owner for (3)'s authorisation |
| D361 | **A PEER COMMIT CAPTURED THIS SESSION'S `LESSONS.md` APPEND, SO LESSON L-102 IS LANDED UNDER A MESSAGE ABOUT AN UNRELATED FILE AND `git log` TELLS THE WRONG STORY ABOUT WHERE IT CAME FROM.** Measured, not inferred: at 20:5x on 2026-08-17 this session appended L-102 to the worktree `LESSONS.md`; `git diff HEAD --numstat -- LESSONS.md` read `83 0` against `fc1e3bac`. Minutes later the same command read `0 0` because HEAD had moved to `676e2bb8`, whose `git show --numstat` is exactly `82 LESSONS.md` + `129/1 LOCATIONS.md` and whose subject is *"The machine is filed, and the plan's entire deletion budget turned out to be a deployment check whose passing state is the matching hash"*. `git show HEAD:LESSONS.md \| grep -c '^## L-102'` returns 1. **Nothing was lost and no ID collided** -- L-102 is intact, it was the next free number in both frames, and `676e2bb8` allocated no lesson of its own -- so this is the benign half of D221's FM-family, capture rather than the reverting variant. **What is durable is the attribution.** A reader running `git log -S 'L-102'` or `git log -1 -- LESSONS.md` is told the lesson arrived with a commit about `LOCATIONS.md`, and the lesson's own text names the rung that produced it (F14 K2c-A) while the commit that carries it does not. This is the exact cost the private-index protocol of `docs/USING_THIS_LAB.md` §9 exists to avoid, and it was paid here by the OTHER agent's pathspec, not by this session's: nothing this session could have done to its own commit would have prevented it, which is the part worth recording. | This session's own commit run, 2026-08-17; `676e2bb8` against `fc1e3bac`; `LESSONS.md` L-102 | **Do not repair it in place.** Re-landing L-102 would duplicate the block, and amending `676e2bb8`'s message would rewrite a peer's commit; both are worse than the wrong pointer. The settlement is this row, which is the only durable place the true provenance can live, plus the standing question it raises: **`docs/USING_THIS_LAB.md` §9 protects the committer from capturing others, and has no clause protecting the committer from BEING captured.** A concrete guard exists and is one line -- land an append the moment it is written, rather than batching the record files to the end of a session -- and it is worth writing into §9 next to the protocol it complements. **No compute needed.** | fleet; the §9 amendment is the chief's |
| D362 | **BATCH 2'S UNTRACKING RULE REACHED 109 TRACKED FILES OF A RUN ARCHIVE ANOTHER RULE OWNS, AND ITS OWN GATE READS 0 WHETHER THE BATCH TAKES THEM OR SPARES THEM — PLUS 122 INITIAL-CONDITION FILES NO GATE COVERS AT ALL. EXECUTED, WITH BOTH POPULATIONS EXCLUDED AND ONE THIRD CLASS DEFERRED.** Measured over `git ls-tree -r fc1e3bac` (14,299 paths, 14,299 unique, stderr empty); never `git ls-files`, 485 behind and behind on the newest. **(1) THE R25 INTERACTION.** `hand_carry.batch2_class_candidates` reaches 7,677 tracked paths, **109 of them inside `docs/campaigns/F14-cooling-ladder/K0c_runs/`** — 44 `0.orig` initial conditions and 65 solver logs — which RULING 1 of 2026-08-17 counts among the **315** it sends to `verification/runs/` under batch 7, and which a lane is writing right now behind an ignore block it wrote the same day. **The plan's stated batch-2 verification cannot see it.** `project_after_untracking` reports trees that GO DARK; `K0c_runs` holds 274 tracked files, 109 reachable, **165 left**, so `git mv` never aborts. Fired four ways — the ruled option, the riskier option, the ruled option with the batch taking all 109, and that again with the absent rule binding supplied by a probe — it read **0, 1, 0, 0**: the two firings that differ by the whole finding are indistinguishable, so a green batch-2 gate is not evidence the batch left that archive alone. Handled by the same ruling as the first interaction: `docs/campaigns` added to `BATCH2_EXCLUSIONS`, the new `.gitignore` block scoped to `demo-output/website/{campaign,dafoam}` and naming `docs/` nowhere, and batch 7 untracks those trees at their new path. A repo-wide rule would not have untracked the 65 committed logs — a tracked file overrides an ignore rule — but would have silently ignored every solver log that lane writes NEXT. **(2) `0.orig` IS CASE INPUT AND THE CLASS RULE CALLED IT OUTPUT.** `src_seg` was `{system, constant, 0}` and the OpenFOAM initial-condition directory is spelled `0.orig`, so the rule reached **122 of the 160 tracked `0.orig` files** (F7_runs 45, F14 K0c_runs 44, THERMAL_K0_runs 20, dafoam 13) — **64 of them tracked ONLY because two negation blocks re-include them, both written 2026-08-17 by the owning lanes**, one of whose comments records that a `[0-9]*` loop had already deleted every initial-condition directory in one tree once. Repaired at the classifier, not deferred, because a `0.orig` file is source and the rule should never have reached it. **(3) EXECUTED.** New `.gitignore` block; **7,403 files untracked, 3,631,998,729 bytes, all still on disk** — the list is not a classifier's output but exactly what `git check-ignore --no-index` newly matches over all 14,299 tracked paths in two scratch repositories, so list and rule cannot drift. 6,331 solver time-directory fields + 1,072 solver logs; tracked corpus 14,299 → 6,896 files and 4,475,301,712 → 843,302,891 bytes. Committed by the index-isolated form because the mandated pathspec form takes content from the WORKING TREE and re-adds every untracked file silently; the tree-diff assertion **counts the D entries and compares the D set against the list in both directions** rather than trusting the command. `.gitignore` rebuilt from the parent's BLOB, never the shared index, whose copy is **25 lines behind HEAD and lacks both `0.orig` protection blocks**. Every path `lstat`ed before and after and compared PATH BY PATH, not by total: 7,403/7,403 present, 0 bytes changed. Gates unchanged both sides — `lab_check --no-tests` FAIL with the same seven checks, G1 carry set byte-identical at 2 trees / 307 files / 1,510,309,145 B, G2 223/0 with both controls fired, G3 roots OK. | MOVE_MAP batch 2, 2026-08-17, over `git ls-tree -r fc1e3bac`; no figure ordered or selected by a commit date; `campaign/MOVE_MAP_BATCH2_EXECUTION_2026-08-17.md` | **Three owed, none of them this pass's.** (a) **The shared index still holds 7,386 of the 7,403 removed entries.** This pass was directed not to touch it and did not; until somebody runs `docs/USING_THIS_LAB.md` §8.5's surgical `git update-index --force-remove --stdin` over ONLY those paths, a `git commit -- <one of them>` by any agent re-adds it to the tree silently. (b) **`LOCATIONS.md` is not updated.** §7.3 makes enumerating untracked evidence part of the deal; its one load-bearing claim was re-checked and holds — exactly one tracked blob still exceeds 50 MB and it is `blade.stl` at 68,053,713 B under `constant/`, which is source — but the enumeration is a separate obligation. (c) **§7.2's third untrack class, 198 files, is DEFERRED and needs a membership list rather than a shape.** Reconstructed by the same classifier that had just been shown wrong about `0.orig`, it contains a custom OpenFOAM solver's C++ source (8 files plus 2 in its `Make/`) and 26 case dictionaries (`controlDict`, `blockMeshDict`, `turbulenceProperties`, `caseDef`, `fieldDef`) that merely do not sit under a `system/` or `constant/` segment. Untracking them on that verdict would repeat the error the same pass had just caught. Also carried: batch 7 must remove the `MESH_AUDIT_runs` negation and re-point this block's two roots, which are path-anchored, alongside the two re-points already recorded for `:122-123`. | fleet |
| D363 | **KV1 IS CLOSED AND D360 IS SETTLED: all five of its statements confirmed at `267a4021`, a sixth found, the term implemented and validated at +2.40e-08 % on a planted source.** D360's five statements were re-derived at HEAD rather than taken second-hand and **all five hold**: `a.allow_advective` read at exactly one place (line 551, the exit-2 guard); the only per-patch heat `Q = kcond * G` at line 633; no advective key in `res` and no advective line in `emit()`; `sealed = not nonwall` at line 708; and — confirmed by *readback* rather than by tracing, on a synthetic open twin of `C3_Ra1e5_m64_source` audited under the HEAD auditor — the printed report does say *"NOT of the identity class, so the balance is a genuine constraint here rather than a restatement of the discretisation"* over a conduction-only ledger. **The sixth, not in D360:** the docstring's claim that the flag "adds the term but the report is then stamped UNVALIDATED" is false in BOTH halves — `UNVALIDATED` appears at HEAD only in the docstring and in two stderr messages, never in `res` and never in `emit()`, so there was no stamp to be had. **Repaired in D360's own three steps, in order.** (1) Stamp first, `965777d2`, computing nothing new: `advective.state` / `.ledger_complete` / `.note`, an incomplete ledger that cannot PASS, and `closure_is_identity_class` degraded to `null` rather than `false`. (2) The term, `b8c398a4`: `-rho.cp.integral (T - datum)(U.n) dA` against the solver's own `phi` via `weightedSum`, summed into the same ledger, keys in `res` and a block in `emit()`. (3) KV1 run: `KV1a_duct_source` recovers a planted 5.000e-03 W at **-4.999999998798e-03 W, error +2.403e-08 %** against the governed 0.1 % — three orders of magnitude better than K0c C3's -7.08e-05 % — with the plant witnessed in the solver log (`Selecting finite volume options` / `Source: heatPlant`) and in the thermal field the log prints (`outletT` 305.1830 against an inlet at 305.0000). **Able to fail, which is the part that matters:** on the heated twin, sign flipped 45.6605 %, scaled by two 17.4433 %, one open patch dropped 100.0000 %, T weight silently dropped REFUSED by the guard; and the same case reads 20.881 % at iteration 20 falling to 0.000000 % at 201, tracking the T residual across nine decades, so the open-case closure is a measurement and not a second identity. **Not more permissive:** over all 18 committed thermal field sets, zero exit-code changes, zero pre-existing JSON keys moved, three added. | This session, rung KV1 of F14, 2026-08-17. Full record `docs/campaigns/F14-cooling-ladder/KV1_RESULTS.md`; controls pre-registered and hashed in `KV1_runs/CONTROL_PREDICTIONS.txt` before any solver ran; commits `965777d2` and `b8c398a4` | **Nothing outstanding on the advective path itself.** What is NOT settled and must not be read as settled: (a) the TURBULENT path, `--allow-turbulent`, remains uncalibrated and its refusal stands; (b) CIRCULATION, on any geometry — KV1 is a straight duct with one inlet and one outlet and has no aisle flow to get wrong, and `MONITOR_STANDARD.md` **S16** was adopted at this rung precisely so no K2b sentence can cite an open-case closure as evidence about a flow field; (c) any OPEN case with buoyancy — KV1 runs at g = 0 deliberately, and a buoyant open case integrates the advective term against a `phi` the temperature helped set. K2a §8 carries an addendum recording (a)-(c). **Compute spent: ~1.48 core-minutes, 0.0406 of it solver time and ~0.55 of it the reproduction check that found D364.** The first cost table stopped at 0.93 by omitting the reproduction check's own compute; corrected in the same pass. K2b, the rack-row module and every turbulent case remain unrun and unauthorized | closed by fleet; (a) and (c) are the owner's to authorise |
| D364 | **A MUTATION HARNESS REPORTED `sealed_identical_to_unmutated: True` FOR EVERY MUTATION WHILE COMPARING AN EMPTY CORPUS, AND IT WAS INVISIBLE UNTIL THE REPRODUCTION RECIPE WAS RUN IN A FRESH CLONE.** `KV1_runs/mutate_advective.py` selects its sealed half by globbing K0c cases that have a `constant/polyMesh/boundary`. `constant/polyMesh/` is **not tracked** (`.gitignore:60`; it is rebuilt from the dictionaries by design), so in a fresh clone that glob returns **zero cases** — and the verdict was `all(out[name]["sealed"][k] == ref[k] for k in ref)` over an empty dict, which is `True`. The harness printed five confident `True`s having compared nothing at all. This is L-98's vacuous pass — a check whose input is missing must FAIL — reappearing inside a control written *at* a rung about instruments that vouch for themselves. **A second defect was found in the same fifteen minutes and by the same means:** `run_kv1.sh` used `for c in "${@:-A B C}"`, which collapses the default case list into ONE word, so `cd` failed on a directory literally named `A B C`. Every earlier invocation had passed an explicit case name, so the default branch — the one the published recipe uses — had never once been taken. **Neither defect is visible by reading the code and both were found by executing it.** | This session, KV1, 2026-08-17. Found by cloning the committed state at `b8c398a4` into a fresh temporary directory and following `KV1_RESULTS.md` §9 literally, as this campaign's twice-demonstrated documentation hazard requires | **Both fixed in the same pass and the fixes are of different kinds, which is the point.** The shell bug is fixed with an array and a comment naming the failure so it is not "simplified" back. The vacuous pass is fixed by making the corpus size a DECLARED CONSTANT — `SEALED_EXPECTED = 11` — with a refusal and a rebuild recipe when the count does not match, and the found/required counts printed on every run; the recipe in §9 gained the mesh-rebuild step. **The generalisable remedy, offered and not executed here:** every control in this repository that selects its corpus by globbing should declare the count it expects, because a glob that returns nothing is indistinguishable from a glob that returns everything-passing, and `all([])` is `True` in Python as `[ -z "" ]` is true in shell. `scripts/fail_open_scan.py` is the natural home for a sweep that finds them. **No compute.** | fleet |
| D365 | **`lab_paths` IMPLEMENTED NO RULE FOR R16 OR R17, SO 40 TRACKED FILES ROUTED TO A ROOT THE MAP NEVER SENDS THEM TO, AND THE TEST THAT ASSERTS RULE COVERAGE COULD NOT SEE IT.** Measured at `267a4021` over `git ls-tree -r HEAD`: 19 loose `*.md` and 21 loose `*.json` sitting directly under `demo-output/website/` were falling through the `WEB` catch-all row to `web/<name>`, where the map's R16/R17 send them to `research/closure/md/` and `research/closure/data/`. R13 says the webroot is **five** files; `redirect()` would have put **45** there. `test_every_move_rule_of_section_2_2_is_represented` was green throughout because it enumerated `{m.rule for m in MOVES}` — the prefix TABLE — and R12, R16, R17, R20, R21 and R25 are regex rules in `_special`, so they can never appear in it however correctly they are implemented. `test_the_webroot_is_fully_classified` was green too: every webroot file did reach a rule, the wrong one. **REPAIRED IN THIS COMMIT**, because batch 3b could not be correct without it — 21 of the ~40 `sdk/scripts/closure_*.py` generators write a `closure_challenge_*.json` into the webroot and would have been bound to `web/`. R16/R17 are now a loose-file rule in `_special` excluding any path with its own table row (`benchmarks.json` stays R14's, `benchmarks.png` stays R15's — same destination for the first, **different** for the second), with the matching inverse. `lab_paths.RULES` now covers table and regex rules alike, the coverage test reads it, and `test_the_rules_set_is_not_a_wish_list` requires every claimed rule to FIRE on a witness path so the declaration cannot be padded. `test_the_webroot_keeps_exactly_the_five_served_files` runs over the tracked corpus and is the assertion that would have caught this on the day. Lesson L-108. Evidence: `test_r16_and_r17_take_the_loose_webroot_files_out_of_the_webroot`, `test_the_must_not_match_control_for_r16_r17`, `test_the_webroot_keeps_exactly_the_five_served_files`, `test_the_rules_set_is_not_a_wish_list`. **CLOSED.** |
| D366 | **R25 = 315 WAS A FRAME AND IT MOVED TO 490 WITHIN THE DAY, WHICH THE RULE SURVIVED AND EVERY TOTAL DERIVED FROM IT DID NOT.** RULING 1 of 2026-08-17 ratified R25 with a bolded **315 files** (`K0c_runs` 274, `K0b_mesh_sensitivity` 41), confirmed identical at `7554e5d3` and `4d7c195a`. At `b8c398a4` it is **490**: the thermal lane landed `KV1_runs`, 175 tracked files, under the same campaign directory. Nothing needs re-ruling — the ruling states a PATTERN (`*_runs` or `*_sensitivity`), so `redirect()` routed the new tree to `verification/runs/F14-cooling-ladder/KV1_runs/**` with no edit — but every figure derived from 315 is stale in the direction of being too small: batch 7's **8,643** and the move subtotal **13,511** are both `4d7c195a` readings and are re-derived by whoever runs batch 7, not carried. **OWED:** nobody has re-derived §2.5's reconciliation at a current frame; it closed at 780 + 13,511 + 7 + 0 = 14,298 over 14,298 tracked paths, and the corpus is 7,077 after batches 1 and 2. Lesson L-109. Evidence: the paired `hand_carry.derive` measurement in `demo-output/website/campaign/MOVE_MAP_BATCH3_EXECUTION_2026-08-17.md` §1.2, and `test_r25_sends_a_docs_campaign_run_tree_to_verification_runs`. **OPEN — the re-derivation is owed to batch 7.** |
| D367 | **A CONSTANT SPLIT INTO SIX NAMED SOURCES LEFT AN f-STRING 280 LINES BELOW STILL READING THE DELETED NAME, AND THE SUITE COULD NOT HAVE CAUGHT IT BECAUSE THE MODULE IS CLASSIFIED `writes-to-tree` AND IS NEVER RUN.** Batch 3b split `scripts/morning_report.py`'s `WEB = "demo-output/website"` into six separately-named sources, because `mega-batch/`, `campaign/`, `agenda/` and `ACTIVE_RESEARCH.md` go to four different roots. `morning_report.py:552` still had `f"{WEB}/campaign/AHMED_BODY_RECONCILIATION.md"`. The module compiles; `lab_check.py` skips it with *"writes-to-tree: line 785: mkdir(...)"*; the failure is a `NameError` on a code path nothing in the suite exercises. It was found by an instrument built for a different purpose — a static path-set comparator that reported the file as having *lost* a path — and not by review or by the suite. **REPAIRED IN THIS COMMIT** (`SRC_AHMED`), and generalised: every one of the 98 rewritten files now has its free names compared against the same answer taken from HEAD's blob, so a name this batch un-bound is caught and a name that was already undefined is not blamed on it. Result: **0 of 98**. **The transferable half is the skip reason.** `writes-to-tree` and `requires-arguments` are the two classes `lab_check.py` will not execute, and any defect confined to such a module is invisible to a green suite by construction. There are **15 such modules at `b8c398a4`**, named in every run's own UNKNOWN block. **OPEN — nobody owns a static check over that set.** Evidence: `demo-output/website/campaign/MOVE_MAP_BATCH3_EXECUTION_2026-08-17.md` §4, instrument C. |
| D368 | **BATCH 3b's FIRST CUT DISABLED THE ONLY PROOF THAT THIRTEEN CHECKS FAIL CLOSED ON AN EMPTY CORPUS, AND THREE PURPOSE-BUILT INSTRUMENTS ALL PASSED IT.** `sdk/tests/test_empty_set_is_not_agreement.py` blinds a check by rebinding `self_audit`'s module-level `WEB`, `REPO`, `ACTIVE`, `WALL`, `CAMPAIGN` or `MISSION` to an empty directory and asserting UNKNOWN rather than a clean sweep of nothing (defect class B1, L-45); `_ABSENT_SOURCE_CASES` covers **thirteen** checks that way, each with a live-arm control. Converting `self_audit.py` to import `lab_paths` made five of those checks read **past** the rebindable handles — `ladder = WEB / "campaign" / "F5a_….md"` became `lab_paths.CAMPAIGN / …`, `docket_path = REPO / "demo-output" / …` became `lab_paths.AGENDA / …`, and both whole-tree rglobs became `RECORD_ROOTS()`. Each is MORE correct across the move and none could be blinded any more: **8 tests failed against a baseline of 0** (`check_evidence_paths_exist`, `check_ungated_completed_runs`, `check_fd_grades_current_standard`, `check_rung_estimates_state_their_iterations`, `check_closure_entry_of_record`). The guards were still correct; what was destroyed was the evidence that they are, which is the silent-zero failure one level up. **The transferable half:** all three instruments this batch built to prove behaviour-identity — import-and-dump, static path-set, free-name sweep — passed it, because every one of them asks *does this constant name the same path* and the answer was yes. **A path shim moves WHERE a name is resolved, and a test that blinds a check by rebinding a name is testing exactly that.** Whoever routes a module through an indirection must ask which names other code REBINDS, not only which paths it reads. **REPAIRED IN THIS COMMIT** by `self_audit._at()`: every call-time path is re-rooted on this module's `REPO`, and on this module's `WEB` when `WEB` has actually been rebound — the second condition being what stops the two re-rootings fighting while the webroot is still inside the repository — and a region that has genuinely moved out of both passes through unchanged, which is what batches 4-8 need. All 13 absent-source cases and both live arms pass. Evidence: `test_an_absent_source_is_UNKNOWN_in_every_repaired_check`, `test_the_live_tree_still_reaches_a_verdict_in_every_one_of_them`. **CLOSED.** |
| D369 | **THE MANDATED WRITE-BACK OVERWRITES THE WORKTREE COPY OF AN APPEND-ONLY FILE, AND THIS PASS USED IT ON A `LESSONS.md` THAT WAS LONGER THAN HEAD'S BLOB.** §8.5's private-index form rebuilds an append-only file from HEAD's blob plus the author's own rows and then, as *"part of the protocol, not an afterthought"*, writes that result **into the worktree**. That step is a plain overwrite. At `12b7ed42` it was applied to a `LESSONS.md` whose worktree copy was **longer than `65684e7c:LESSONS.md`** — `cmp` reported *"EOF on - after byte 313171, line 5058"*, so the first 313,171 bytes were byte-identical to HEAD's blob and an unknown number of further bytes followed. **Those bytes were destroyed and are not recoverable**: no dangling blob in this repository holds a `LESSONS.md`, the shared index's staged copy is three lessons OLDER (304,841 bytes, no L-105/106/107), and the peer scratch directory `/tmp/tmp6jkrvzl7/` holds a copy identical to HEAD's blob. **What is known about the loss.** No lesson heading beyond `L-107` existed in HEAD, and **no citation anywhere in the repository names a lesson that is now missing** — every `L-<n>` in `docs/DOCKET.md` resolves to a section in `LESSONS.md`. The most likely content is a trailing newline left by the preceding peer's own write-back of `0ab6c39a` (three lessons, 142 lines), but that is an INFERENCE and the bytes were not captured before the overwrite, so it is recorded as an unknown rather than as nothing. **The protocol defect, which is general.** §9 item 8's reconciliation guidance reads the two sides by ROW ID and says *"in the worktree, not in HEAD"* means unlanded work that must be landed, never discarded — and the very step it mandates two paragraphs earlier discards it, by construction, for any content the ID regex does not see: a partial row, a row under a heading shape the regex misses, an in-progress paragraph. **The write-back must be a MERGE, not an overwrite**: rebuild HEAD's blob plus your rows, then re-apply whatever the worktree held beyond HEAD's blob, and refuse if the two disagree anywhere inside HEAD's own bytes. This pass's `cmp` was run BEFORE the batch and again immediately before the commit; the second one is what caught it, and it caught it a few seconds too late to matter, which is the difference between a check and a guard. **OPEN — the merge form is owed to §8.5 and to `scripts/check_docket_reconciliation.py`'s sibling for `LESSONS.md`, which does not exist.** |
| D370 | **R8's "15 NAMED LAUNCHERS" ARE NAMED NOWHERE, AND THE BATCH-4 HAZARD THE PLAN CALLS "THE ONE WHOSE INCOMPLETENESS IS NOT VISIBLE FROM INSIDE THE REPOSITORY" IS INSIDE THAT UNNAMED SET.** `MOVE_MAP_2026-08-16.md` §2.2 prices R8 at **26 files** — `scripts/{installed,laptop_bundle}/**` *"+ 15 named launchers"*. Measured at `5d1b41a0` over `git ls-tree -r HEAD`: the two directories hold **11** tracked files, so the arithmetic closes at 11 + 15 = 26 and batch 4's 872 closes at 857 + 15. **No list of the 15 exists** — not in the map, not in `MOVE_MAP_EXECUTION_2026-08-17.md`, not in `scripts/lab_paths.py` (`redirect("scripts/demo_servers.sh")` and `redirect("scripts/auto-stop.sh")` both return `None`; R8 is implemented as two directory rows), and not in the superseded `docs/PHASE2_MOVE_MAP.tsv`, which moved all of `scripts/` and was superseded for it. `scripts/` holds 12 top-level `*.sh`; at most seven read as launcher/keepalive/preflight, so eight of the fifteen would have to be `*.py` and nothing says which. **CONSEQUENCE FOR BATCH 4, STATED RATHER THAN ABSORBED.** `scripts/demo_servers.sh` and `scripts/auto-stop.sh` are 2 of the 15 — the map names those two in prose at §5.2 — so **batch 4 as executed did NOT move them, the `@reboot` crontab line does NOT dangle, and the crontab hazard the execution plan puts in batch 4 is DEFERRED WITH THE LAUNCHER SET, not discharged.** All three live crontab lines were nonetheless verified by executing what each invokes, before and after (`MOVE_MAP_BATCH4_EXECUTION_2026-08-18.md` §3). The plan's other batch-4 out-of-tree item — re-installing `/usr/local/bin/auto-stop.sh` — was **not done and is not the fleet's to do**: `scripts/installed_registry.py:245-266` registers it `PendingInstall`, owner *"Katie / Sanaa (docket A4)"*, and the fleet installing the box's power control would cross the boundary that register exists to hold. **OPEN — the 15 need a membership list before any batch can move them, and the crontab re-point plus the `demo_servers.sh --directory` change of §5 Class 1 travel with them.** Evidence: `demo-output/website/campaign/MOVE_MAP_BATCH4_EXECUTION_2026-08-18.md` §2; `docs/LESSONS.md` L-111. |
| D371 | **A CLASS-1 PATH SCAN WITH A SUFFIX WHITELIST MISSED THE ONE `.js` FILE IN THE REPOSITORY THAT NAMES A REPO PATH, AND THE SUITE WENT FROM 6 FAILED TO 24.** Batch 4's same-commit re-point set was derived by scanning every tracked `*.py *.sh *.ps1 *.cron *.json *.yaml *.yml` for a literal naming a moved source. Three live sites were outside that reading and `f69a3ed5` landed without them: `sdk/tests/control_room_ramp_harness.js` **(a Node harness — the suffix list had no `.js`)**, which `open()`s two `demo-output/plots/pressure_slices/...` files; `sdk/tests/test_lab_check.py:57`'s `HOOK = REPO / "scripts" / "installed" / "pre-push"`, **in a file the scan DID report and whose ten other hits are prose about the hook, so the one live constant read as more prose — a hit count is not a reading**; and `scripts/build_laptop_bundle.py:535`, which `lab_check.py` classifies `writes-to-tree` and never runs, so no gate could see it and only the end-to-end test could. **A suffix whitelist is an enumeration and this is L-108's shape** — the members implemented in a form the enumeration does not cover are not failing, they are invisible. **REPAIRED IN THE FOLLOWING COMMIT**, after re-running the scan over **every tracked non-`.md` file with no suffix filter at all**, which is what produced all three; all three now name their paths through `lab_paths`, except the `.js` harness, which cannot import a Python module and spells the prefix once. Suite back to **6 failed / 2,443**, the standing baseline. **The rollback rule was not followed and that is stated rather than absorbed**: `MOVE_MAP_EXECUTION_2026-08-17.md` §4 says *"the response is rollback, not repair-forward"*, and the reason it was repaired forward is that the MOVE is provably correct — 855 of 857 moved files have a byte-identical `(mode, blob)` at the new path and the two that differ are exactly the two the same commit also edited — so a rollback would have re-landed an identical tree plus these three edits. What failed was the MEMBERSHIP of the same-commit set, not the mechanics. **CLOSED**, with the transferable half open: a path scan for any later batch must have no suffix filter. Evidence: `demo-output/website/campaign/MOVE_MAP_BATCH4_EXECUTION_2026-08-18.md` §7.1a; `docs/LESSONS.md` L-108. |
| D372 | **A PATH SHIM RESOLVING AGAINST THE FILESYSTEM WAS FEEDING FOUR `git show` LOOKUPS INTO IMMUTABLE COMMITS, AND ONE OF THEM WAS AT MODULE SCOPE, SO MOVE_MAP BATCH 5 TOOK 46 TESTS OUT OF THE SUITE BEHIND A SINGLE `skipped` LINE.** `lab_paths.web_file()` answers *where a file is now* — correct for every live read and wrong for a lookup into history, where the file sits at whatever path that commit used. R16 moved `CLOSURE_CHALLENGE_SUBMISSION_DRAFT.md` and `CLOSURE_FAMILY_SUPERVISION_GUIDELINES.md` to `research/closure/md/`, and three callers broke at once: `scripts/check_summary_consistency.py:226` (`CONTROL_PATH`, feeding `git show 87324012~1:` and `87324012:`) went **PASS → UNKNOWN exit 3** when its own control-misfire rule fired as designed; `sdk/tests/test_normative_clause_check.py:118` failed its positive control; and `sdk/tests/test_summary_consistency.py:196` raised `unittest.SkipTest` **at import**, taking the whole file out of the run. **The suite's verdict and failing-module list did not show the third one.** Collected fell **2,443 → 2,398** and `skipped` read **1**; the 46 lost tests were named nowhere. It was found by comparing the collected TOTAL across the same instrument in the same frame, which is why a batch reports finding sets and not verdicts. **REPAIRED IN THIS COMMIT** by `check_summary_consistency._in_commit()` and a sibling in the normative test: the spelling is probed inside the named commit with `git cat-file -e` across the literal path, `lab_paths.unredirect()` of it and `lab_paths.redirect()` of it, so it is a measurement of that commit rather than a hard-coded legacy string, which would rot the other way the moment a control commit is written after a move. The module-scope `SkipTest` now names the path it tried and the `CONTROL_PATH` it came from. Collection back to **2,444** (2,443 plus this batch's new R23 ratification test); all five of `check_summary_consistency`'s controls fire (P1 1 hit, N1 0, N2 0, P2 1 hit, N3 0 + 1 exempt). Lessons L-114 (both halves). Evidence: `demo-output/website/campaign/MOVE_MAP_BATCH5_EXECUTION_2026-08-18.md` §7.3. **CLOSED, with one transferable item open: nobody has swept for other module-scope `SkipTest`/`raise` sites, and each is a hole the size of its file.** |
| D373 | **D368's DEFECT RECURRED AT THE BATCH THAT MADE IT UNAVOIDABLE: THE HANDLE THE BLINDING TESTS REBIND STOPPED COVERING SIX OF THE SEVEN ROOTS IT USED TO COVER, AND THREE GUARDS LOST THEIR ONLY PROOF.** `sdk/tests/test_empty_set_is_not_agreement.py`'s `_ABSENT_SOURCE_CASES` and `sdk/tests/test_form_or_value_and_empty_selection.py`'s `_Swap` blind a check by rebinding `self_audit`'s `WEB`, `REPO`, `ACTIVE`, `WALL`, `CAMPAIGN` or `MISSION` and assert UNKNOWN rather than a clean sweep of nothing (L-45 class B1). Until MOVE_MAP batch 5 every root `lab_paths.RECORD_ROOTS()` returns was under the webroot, so `WEB` blinded all of them. R16 and R23 moved records to `research/closure/md/`, `research/agenda/`, `research/race/` and the four `closure_*` trees, and `WEB` began blinding **one root of seven**: `check_evidence_paths_exist` returned FAIL, `check_fd_grades_current_standard` returned PASS and `check_closure_entry_of_record` returned PASS where all three must return UNKNOWN — 5 failures across the two files. **`_at()` was still correct; the handle had stopped covering the region.** **REPAIRED IN THIS COMMIT**: `REPO` — which `_at()` re-roots on unconditionally and which covers all seven roots — is now named in both case lists, and `test_rung_estimates`'s fixture derives its layout from the shim (`root / lab_paths.AGENDA.relative_to(lab_paths.REPO)`) instead of spelling `demo-output/website/agenda/`, which had quietly turned its UNKNOWN from the empty-selection reason into the absence reason — a different finding that still counted as green. All 13 absent-source cases and both live-arm controls pass. Lesson L-115. Evidence: `demo-output/website/campaign/MOVE_MAP_BATCH5_EXECUTION_2026-08-18.md` §7.3; `test_an_absent_source_is_UNKNOWN_in_every_repaired_check` and its live-arm control `test_the_live_tree_still_reaches_a_verdict_in_every_one_of_them`. **CLOSED — but the question generalises and is OPEN for batches 6 to 8: before each one, which handle covers the region afterwards?** |
| D374 | **`check_summary_consistency.py` ENUMERATES ITS CORPUS FROM `git ls-files`, THE FIFTH NAMED INSTANCE OF D274's FRAME DEFECT, AND BATCH 5 COST IT 30 DOCUMENTS IN THAT FRAME AND NONE IN HEAD's.** `scripts/check_summary_consistency.py:732` calls `git ls-files -z -- '*.md' '*.html' '*.tex'`. This lab's mandated commit form never writes the shared index, so that frame is whatever the last `git add` left — the pre-batch-2 layout. Measured at `7a98c33d`: 532 prose paths, **18 absent from disk before batch 5 and 48 after**, the 30 added being R16's 19 loose `*.md` plus 11 `*.md`/`*.html` under R23's directories. **The same enumeration over `git ls-tree -r HEAD` returns the same 532 paths and is repaired by the batch-5 commit itself**, because HEAD then names the files where they are — so this is the INDEX frame going stale, not the corpus shrinking. The check names all 48 on stdout rather than swallowing them, and its verdict is PASS on the same graded pair either way. **NOT REPAIRED HERE**, on the precedent `MOVE_MAP_EXECUTION_2026-08-17.md` §2.4 set for `check_absolutes.py:520-547`'s `known_test_names()`: a shared instrument mid-flight, and batch 5 is not that batch. The five instances are `lab_check.py:486` (repaired, D274), `hand_carry.tracked_paths` (repaired), `check_absolutes.known_test_names` (open), `MOVE_MAP` §2's own totals (open) and this one. **OPEN — four unrepaired instances of one defect are being carried as four separate items and should be one sweep for `git ls-files` read as "the tracked set".** Evidence: `demo-output/website/campaign/MOVE_MAP_BATCH5_EXECUTION_2026-08-18.md` §7.2. |
| D375 | **`scripts/heat_balance.py` DELETES THE CASE'S `postProcessing/` DIRECTORY, WHICH IS THE EVIDENCE THIS CAMPAIGN'S OWN CONVERGENCE GATE READS, AND IT DOES SO SILENTLY AND AFTER THE FACT.** `scripts/heat_balance.py:770` calls `shutil.rmtree(os.path.join(case, "postProcessing"), ignore_errors=True)` unconditionally before its own postProcess pass. That directory holds the in-pass function-object history that `docs/physics_rules.yaml` `thermal.monitor_peak_to_peak_max_pct` / `monitor_window_iterations` and `scripts/check_convergence.py --monitor-regex` are built on — the criterion that rung K1a wrote *because* residuals are not admissible. **Measured at F14 K2e on 2026-08-18: twelve Boussinesq cases lost `hotFlux`, `coldFlux`, `Umax` and `Tcentre` the moment they were audited**, with no error, no warning and no exit code. `docs/campaigns/F14-cooling-ladder/K0c_runs/*/postProcessing/` shows the same hole already committed — only `hbAudit_*` survives there — and no K0c document says why. **What it cannot see:** whether any published K0c or KV1 number was graded from a history that had already been deleted and silently regenerated; that needs the run order of each rung reconstructed from timestamps, which was not done here. **NOT REPAIRED — the shared instrument is mid-flight across three rungs tonight and K2e is not the batch to change it.** K2e's own workaround is local: `analyse_k2e.py` audits a temporary COPY, and the archived convergence evidence is a filtered solver LOG (`log.<app>.monitor`), which nothing deletes and which `check_convergence.py` reads directly. **What settles it:** either the `rmtree` becomes conditional on a directory the auditor itself created, or it moves the existing tree aside and restores it; plus a test that runs `heat_balance.py` over a case with a populated `postProcessing/` and requires the tree to survive. Owner: fleet. Evidence: `docs/campaigns/F14-cooling-ladder/K2e_RESULTS.md` §8.1, `K2e_runs/archive_cases.sh` docstring. Lesson L-118. |
| D376 | **THE LAB'S HEAT-BALANCE INSTRUMENT HAS NO PATH FOR A COMPRESSIBLE CASE, SO IT CANNOT AUDIT THE EXACT SOLVER CLASS THE BOUSSINESQ LIMIT TELLS A RUNG TO MOVE TO.** `scripts/heat_balance.py` reads `nu` and `Pr` from `constant/transportProperties` (line 437's `read_dict` call path) and refuses when the file is absent. A `rhoThermo` case — `buoyantSimpleFoam`, `buoyantPimpleFoam`, `chtMultiRegionFoam` — carries `constant/thermophysicalProperties` instead and has no `transportProperties`. **Measured at F14 K2e: 15 of 15 variable-density cases refused**, with `REFUSE: <case>/constant/transportProperties not found and no default for 'nu'`. `docs/physics_rules.yaml` `thermal` §2 says in terms that a run violating `boussinesq_beta_dT_max` "must either justify that explicitly or **move to a compressible thermo solver**" — and the standing check that every solve is specified to pass cannot audit the destination. **What it cannot see:** nothing about whether the compressible cases balanced; K2e recomputed the sealed closure from the two wall integrals the running solver printed (≤ 2.0e-05 % on all 30) but that is a different, weaker quantity and it is reported as such. **What settles it:** a `rhoThermo` branch that reads `mu`, `Pr` and `Cp` out of `thermophysicalProperties` and forms `k = mu.Cp/Pr` directly, rather than the kinematic `alpha` the Boussinesq path uses — plus a case in the test set that exercises it. **OPEN.** Owner: fleet. Evidence: `docs/campaigns/F14-cooling-ladder/K2e_RESULTS.md` §6 and `K2e_runs/GATE_TABLE.md`, heat-balance table. |
| D377 | **SIX REFUSAL PATHS IN `scripts/heat_balance.py` EXIT 1, WHICH ITS OWN DOCSTRING RESERVES FOR "THE BALANCE DID NOT CLOSE", SO A CALLER CANNOT TELL A REFUSAL FROM A FINDING.** The file's contract at lines 7-8 reads "Exit 0 = balance closed within --tol percent. Exit 1 = it did not. Exit 2 = the auditor REFUSED to produce a number". Its main refusal sites do return 2, but six others — lines 437, 444, 576, 586, 619 and 692 — are `raise SystemExit(f"REFUSE: …")`, and `SystemExit` with a string argument exits **1**. **Measured 2026-08-18: `heat_balance.py /nonexistent-case` exits 1, and an empty case directory containing only `constant/polyMesh/` exits 1** — indistinguishable, to any caller reading the status, from a case whose physics failed to balance. This is the L-113 class in the file that documents itself most carefully: a sentence stating what an instrument does, true of part of the file, false of the rest, and decaying silently. **What it cannot see:** how many recorded exit-1 results in this repository's run archives were refusals rather than failures; a sweep of the collectors for stored `heat_balance` exit codes would settle it and was not done here. **What settles it:** convert the six sites to `sys.stderr.write(...)` plus `return 2` on the `main()` path, and add a control that asserts a refusal and a failed balance produce DIFFERENT codes — the check that would have caught it. **OPEN, and it is the cheapest of the three.** Owner: fleet. Evidence: `docs/campaigns/F14-cooling-ladder/K2e_RESULTS.md` §8.3. Lesson L-118. |
| D378 | **EVERY K2b CLOSURE NUMBER IS PRODUCED THROUGH AN UNCALIBRATED CODE PATH, BECAUSE THE INSTRUMENT PREREQUISITE K2a NAMED WAS A LAMINAR RUNG AND EVERY CASE IN THIS MODULE IS TURBULENT.** `K2a_RACK_ROW_MODULE_SPEC.md` section 8 makes KV1 the prerequisite for quoting any closure figure, and KV1 closed — a planted 5.000e-03 W recovered at +2.40e-08 %. KV1 is **laminar** (`KV1_RESULTS.md` section 10 says so in its own words: "the turbulent path — `--allow-turbulent` remains uncalibrated and its refusal remains in place"). Every K2b case is turbulent by K2a section 5's own regime numbers, so `alphat` is non-zero (measured max 1.047795e-02 m²/s), `alphaEff` varies over every patch, and `scripts/heat_balance.py` **REFUSES outright, exit 2**, unless `--allow-turbulent` — whose report is stamped UNVALIDATED. So the spec's prerequisite did not cover the rung it was written for. **BOUNDED, NOT WAVED THROUGH, AND THE BOUND IS MEASURED:** the uncalibrated path affects only the CONDUCTIVE rows; every wall in this module is adiabatic and all five wall rows read exactly 0 at every audit, leaving total conduction at **5.5e-04 %** of the ledger on `K2bP_under` and **6.6e-05 %** on `K2bP_coarse`. **What the bound does not cover** is any variant with a non-adiabatic envelope, which K2a section 3.1 already flags as a modelling choice, or any use of `--allow-turbulent` where wall conduction is not near-zero. **To settle:** calibrate the turbulent conduction path the way K1c calibrated the laminar one — a turbulent case with a known wall heat flux — before the first non-adiabatic variant of this module runs. Found at F14 rung K2b-pilot. Owner: fleet. |
| D379 | **y+ ON THE K2b PILOT MESHES IS BELOW THE WALL-FUNCTION BAND THE SPEC NAMES, ON EVERY WALL, AND IT IS ONLY VISIBLE BECAUSE THE SPEC REQUIRED IT MEASURED.** `K2a_RACK_ROW_MODULE_SPEC.md` section 6 sets the target at "y+ in the log-law band (30–300, RECALLED ... to be *measured* by the mesh-report function object and reported per case, not assumed)". Measured in-log on `K2bP_coarse` at 9,000 iterations: per-patch averages **2.6 to 9.1**, maxima 8.8 to 26.9, minima as low as 0.217 — one to two orders under the band on average, and no patch's maximum reaches it. At a 12.5 mm cell the near-wall cell is far too fine for standard wall functions. OpenFOAM's `nutkWallFunction` and `omegaWallFunction` blend continuously, so nothing failed and the solves are stable, but **the wall treatment the mesh delivers is not the one the spec specifies**, and no claim in `K2b_PILOT_RESULTS.md` rests on a wall heat flux (every wall is adiabatic and reads exactly 0 W). **AND REFINING MAKES IT WORSE, NOT BETTER:** on the 1.5x-refined mesh the per-patch averages fall further, to 3.3-5.4, so this is a wall-treatment decision and not something more cells will fix. **To settle:** decide, before the 3D module is built, whether this module class is low-Re-resolved or wall-functioned, and set the near-wall cell size from that decision rather than from the base cell. The 3D coarse mesh at a 60 mm base cell will sit somewhere else again and must be measured, not inherited. Found at F14 rung K2b-pilot. Owner: fleet. |
| D380 | **K2a SECTION 3.3 DESCRIBES THE FALLBACK BRANCH OF `outletMappedUniformInlet` AS ITS NORMAL BEHAVIOUR, AND THE BRANCH IT DESCRIBES IS THE ONE THAT SILENTLY DROPS THE RACK'S ENTIRE HEAT INPUT.** The spec's load-bearing row states the BC's face-averaging is "area-weighted (`gWeightedAverage` over `magSf` in the source)" and offers, as a remedy if the two averages disagree, to "switch the grading to the BC's own area average". Read this session in `outletMappedUniformInletFvPatchField.txx`, `updateCoeffs()`, v2606: there are **two** branches, selected on `gSum(phi) > SMALL` over the mapped outlet patch, and the area-weighted one is the **`else`**. In normal operation the BC is **mass-flux**-weighted — the same weighting as the graded quantity — so the dispute the spec set out to close does not arise, and its stated remedy points the wrong way: taking it would move the graded rack-inlet temperature **0.403 K, 3.4 % of the rack rise**, away from what the BC computes (measured on `K2bP_under`; the same gap is −0.73 % on the contained case and **−16.27 %** on the g = 0 twin). Worse, **the `else` branch appends no `offset`**, so a rack front face that ever loses net outflow hands the rear face the front face's own temperature and a rack adding no heat reads exactly like a converged rack — a hazard the spec does not name. K2b adopted `T̄(rack_out) − T̄_ṁ(rack_in)` as the in-log reachability control against it (measured +1.75e-05 % on the converged case; reads 0 K on the fallback branch). **To settle:** amend K2a section 3.3, or record the correction against it, before any 3D build reads that paragraph as instructions. Found at F14 rung K2b-pilot. Owner: fleet. |
| D381 | **THE K2b PLANTED-SOURCE CONTROL (C3) IS CONVERGENCE-LIMITED AND ITS RECOVERY WAS NOT LANDED INSIDE THE PILOT'S AUTHORISATION — REPORTED AS A TRAJECTORY, NOT AS A PASS.** K2a section 8 pre-registers "C3 planted volumetric source (KV1's plant in the room)". KV1's 5.000e-03 W plant is **1.0e-06 of this module's 4.914e+03 W ledger** and could not have failed, so K2b re-sized it to **500.000 W** (≈10 % of the rack load, so the governed 0.1 % recovery tolerance is 0.5 W). Run as a restart twin against its own negative twin `K2bP_C3b_noplant` — byte-identical but with no `fvOptions` — the recovered plant, taken as the difference of the two ledgers at matched state, reads **155.55 W at 400 iterations and 315.57 W at 800** against the planted 500.000 W: errors of −68.89 % and −36.89 %, moving in the right direction at roughly the case's own closure rate. **The control is not degenerate — its readback doubled between two audits — and it is not established either.** Reaching the 0.1 % band needs an estimated ~5,000 further iterations on both twins, ≈16 core-minutes, which is outside the 60 core-minute pilot authorisation and is priced into the 3D proposal instead. **S15 is separately satisfied and discriminates:** `heat_balance.py` reads the SOLVER LOG and reports `constructed ['roomPlant']` on C3 against `none` on C3b. **To settle:** run both twins to closure when the next authorisation covers this module, or plant into a case whose ledger is small enough that the recovery lands at affordable convergence. Found at F14 rung K2b-pilot. Owner: fleet. |
| D382 | **THE LAB'S `NOT OBTAINED` CONVENTION IS ONE CAMPAIGN'S LOCAL DIALECT, SO THE CORPUS RECORDS THE SAME STATE IN AT LEAST FIVE VOCABULARIES AND NO SWEEP CAN COUNT IT.** Measured at `fa2c8bb0` over `git ls-tree -r HEAD`: the token `NOT OBTAINED` occurs 15 times in `docs/campaigns/F14-cooling-ladder/K2c_RACK_ROW_VALIDATION_SEARCH.md`, 4 in that campaign's `README.md`, 3 each in `K2c_DIGITIZATION_ADDENDUM.md`, `K0d_TURBULENT_MIXED_CONVECTION_GATE.md` and `K0c_RESULTS.md`, 1 each in `K2a_RACK_ROW_MODULE_SPEC.md` and `K0c_runs/GATE_TABLE.md`, **and twice in the entire rest of the corpus** (`docs/NUMERICS_KNOWLEDGE.md`, `campaign/W2_DOW_STRUCTURAL_UQ_READING.md`). The same state is recorded elsewhere as *"a literature-recalled x/c ~ 0.60 stated to two significant figures with no retained citation"* (`campaign/F2_transonic_naca0012.md`), as a fallback to a secondary reproduced in a 2014 thesis because *"no paper was found that reports a point value"* (`campaign/F5a_cylinder_reynolds_ladder.md`, Re 2000), as *"no tabulated numeric data could be located"* followed by a 600 dpi digitisation of a 2021 figure (`campaign/F7_marine_free_surface.md`), as *"the exact form given in this task's gate"* (F5a batch), and at the nine-act gate table as **nothing at all**. Act 1's reference cell names Roshko and Williamson over constants the repo traces to a prior lab gate and a task prompt (D12). **Consequence: the lab cannot count its own missing references, which is why `docs/VALIDATION_INVENTORY.md` §6 had to assemble that list by hand from two different kinds of record and says so per row.** What settles it: adopt F14's `NOT OBTAINED` token plus its four fields (what is missing, which rung it blocks, why, acquisition path) as the corpus-wide form, back-fill the seven named instances above, and add a `self_audit` check that a gate row naming a reference either resolves to a retained artifact or carries the token. Owner: fleet. **MAPPING LANDED 2026-08-18; THE TOKEN IS ADOPTED AND NOTHING WAS RENAMED.** `docs/charters/VERIFICATION_CHARTER.md` section 6b now carries the clause: a gate row whose reference was never obtained says `NOT OBTAINED` in those two words and carries four fields (what is missing, which rung or row it blocks, why with the availability check named and dated, and the acquisition path with its price), and the obligation runs **forward only**. The mapping table for the five dialects plus the sixth silent state was **re-derived at HEAD on 2026-08-18 rather than copied from this row, because the counts moved**: `docs/VALIDATION_INVENTORY.md` alone added 14 occurrences of the token after this row was filed, and the corpus at HEAD reads 15 in `K2c_RACK_ROW_VALIDATION_SEARCH.md`, 14 in the inventory, 5 in F14's `README.md`, 5 in `K0cT_runs/analyse_k0ct.py`, 4 in `K0c_runs/gate_k0c.json`, and eleven further files at one to three each. **No frozen artifact was edited and no back-fill was performed**, which this row asked for: rewriting a published record's own words to match a token adopted afterwards is the edit the charter forbids everywhere else, so the mapping is the instrument instead. The sixth state, the nine-act gate table saying nothing at all, was repaired on 2026-08-18 by the referent and band columns. **Still owed by this row:** the `self_audit` check it names, that a gate row naming a reference either resolves to a retained artifact or carries the token. |
| D383 | **TWELVE GATE ROWS COMPARE AGAINST A REFERENCE THAT WAS OBTAINED AND CARRY NO PRE-REGISTERED TOLERANCE, SO "CLOSE" IS ASSIGNED AFTER THE NUMBER IS READ, AND THE CLASS IS INVISIBLE BECAUSE EVERY ROW INDIVIDUALLY LOOKS LIKE A PASS.** Found while building `docs/VALIDATION_INVENTORY.md` at `fa2c8bb0`. `VERIFICATION_CHARTER.md` §2a requires a gate to answer *"what result would make this gate FAIL"* **at creation**. These rows have a genuine external referent and no answer to that question: F1's Cp-RMS and shock-location rows (`dafoam/ladder-a/A3_onera_m6.md`, `ba88dff4`) where **no declared RMS tolerance exists anywhere in the record** and GATE REACHED was asserted after the deviations were read; F3's five exact-theory rows (`d8a81e55`); F4 Gates 1 and 2 (`d8a81e55`), where the record itself notes the coarse-mesh agreement "is largely coincidental" and convergence is non-monotonic; F5a's batch Strouhal rows (`515d9093`); and F6a's separation and reattachment rows (`f4bf2c89`), where **reattachment misses by +13.9 percent and the row reads GATE REACHED**, carried onto the nine-act table as `VALIDATED` (`b14356b0`). The +13.9 percent instance is the proof the class is not cosmetic: there is no miss large enough to have failed it. **Distinct from D12 and D14**, which are about a reference that is wrong or a chip that loses its qualifier; this is about a reference that is right and a threshold that was never written. What settles it: for each of the twelve, either write the band the rung should have carried and re-read the existing numbers against it, at zero compute since every number is already recorded, or restate the row as a measurement rather than a gate, per §2's rule that a gate not reached is stated as not reached. Owner: fleet. |
| D384 | **`scripts/heat_balance.py --allow-turbulent` DOES NEITHER OF THE TWO THINGS ITS OWN DOCSTRING SAYS IT DOES, and the check that would have caught it is reading the docstring beside the code.** The docstring, lines 263-268: alphat non-zero anywhere means "`alphaEff` varies over the patch and `alphaEff . integral(n.grad T) dA` is NOT `integral(alphaEff n.grad T) dA`. **`--allow-turbulent` uses a weighted integral instead and again stamps the report UNVALIDATED.**" **Read in the installed source: the flag only skips the refusal at line 780.** There is no weighted integral anywhere in the module — the LAMINAR uniform coefficient is still used — and `UNVALIDATED` appears in the file only inside the REFUSAL message the flag suppresses, so no report can ever carry it. **Measured on nine turbulent cases at F14 rung K0c-T: `"UNVALIDATED" in stdout` is `false` on every one, exit 0 on eight.** The same docstring already records ONE previous instance of exactly this failure at its own line 92 ("did not *add an UNVALIDATED term*; it added NOTHING, and the docstring's claim … was false in both halves"), so this is the second occurrence in one file, in the file that documents itself most carefully. **THE NUMBERS IT PRODUCED AT K0c-T ARE NEVERTHELESS CORRECT, AND THE REASON IS NOT IN THE CHECK.** Those cases use `nutLowReWallFunction`, so nut is identically **zero on every wall patch** (measured per case as `nut_wall_max = 0.000e+00`) and alphaEff on every patch really is uniform at nu/Pr — which is why the independent raw-cell wall-flux path and this module's postProcess snGrad path agree to **8.8e-08 to 1.8e-06** relative across all nine. **It would NOT agree on a wall-function case**, where nut at the wall is non-zero, and nothing in the module tells a caller which case they are in. The refusal is gated on the DOMAIN max of alphat; the quantity that actually decides the answer is the PATCH value, and the module never looks at it. | F14 rung K0c-T, 2026-08-18, at repo `e4a977ef`; measured on the nine cases of `docs/campaigns/F14-cooling-ladder/K0cT_runs/`, recorded in `gate_k0ct.json` as `report_stamped_UNVALIDATED` per case | **Three parts, and the first is free.** (1) **Correct the docstring**, which is the load-bearing half: a caller choosing to pass the flag is choosing on the strength of a sentence that is false. (2) **Gate the refusal on the PATCH maximum of alphat, not the domain maximum** — a wall-resolved case has alphat = 0 on every patch and needs no refusal at all, while a wall-function case needs one whatever its domain maximum is. That turns a flag most turbulent cases must pass into one they mostly do not need. (3) **If the weighted integral is wanted, it has to be written**; the honest interim is a report that says which regime it is in. The K0c-T cross-check (raw cells against postProcess snGrad, agreeing to 1e-06 with nut_wall = 0) is the first calibration datum this path has ever had, and it calibrates only the uniform branch. | the module's owner (docstring and the patch-vs-domain gate); a later turbulent rung with a wall-function case (the weighted branch) |
| D385 | **A REPOSITORY ROOT DERIVED BY COUNTING SEGMENTS UP FROM A MOVED PATH IS A PATH LITERAL NO SCAN CAN SEE, AND ONE OF THE TWO TESTS IT BROKE WENT ON PASSING WHILE COMPARING NOTHING.** `sdk/tests/test_a2_shape.py:28` read `_SDK = _a2_shape._LADDER.parents[3] / "sdk"`, and `_LADDER` is `lab_paths.DAFOAM / "ladder-a"`. R22 moves `DAFOAM` from `demo-output/website/dafoam` to `cases/dafoam` — **two segments shallower** — so `parents[3]` moved from the repository root to `/home/ubuntu` and `_SDK` began naming `/home/ubuntu/sdk`, which does not exist. **The shim worked exactly as designed; what did not follow the move is the ARITHMETIC performed on the constant.** D371's own remedy cannot reach it: the Class-1 scan run for this batch read **every tracked non-`.md` file with no suffix filter** (6,606 files, 1,024 binaries skipped on a NUL probe) and this line contains no path literal to match. **The cost is asymmetric and the quiet half is the finding.** `test_control_room_keeps_the_old_defaults_verbatim` raised `FileNotFoundError` — loud. `test_hint_free_payloads_are_untouched_by_the_hint` globbed the absent directory, got nothing, and asserted its projection identity over the **ten synthetic bodies it builds itself**: every assertion passed while it compared **no real geometry at all**. The only thing that caught it was that test's own floor, `assert len(bodies) > 20`, which read **10**. **AND THE CLASS IS LIVE FOR BATCH 7, MEASURED RATHER THAN PREDICTED:** seven tracked modules under `demo-output/website/campaign/` resolve the repository root by segment counting — `F5_runs/{cylinder_ladder,cylinder_ladder_3d,run_rung}.py` at `_HERE.parents[4]` and `F9_work/{analyse_f9,f9_criteria,setup_f9_round3}.py` at `HERE.parents[3]` — and **all six land on `/home/ubuntu` after R20/R21**, while `F9_work/finalize_f9.py:12`'s `HERE.parents[1] / "solve_registry"` moves from the webroot to `verification/` and its target is 7H's `evidence/`. Every one of the seven resolves correctly today, so nothing reddens until batch 7 runs. | MOVE_MAP batch 6, 2026-08-18, over `git ls-tree -r 0869284e`; the seven batch-7 modules measured by resolving `parents[N]` at the current and post-R20 spellings in one invocation, stderr empty; `campaign/MOVE_MAP_BATCH6_EXECUTION_2026-08-18.md` section 5 | **REPAIRED HERE FOR THE ONE THIS BATCH BROKE, DEFERRED FOR THE SEVEN IT DID NOT.** `_SDK` is now `_a2_shape.lab_paths.SDK` — a bound non-moving name, correct at every depth, needing no edit at any future batch — and the `> 20` floor that caught it is left exactly as it is, because that floor is the whole reason this is a finding rather than a silent green. `sdk/tests/test_a2_shape.py` reads **9 passed** afterwards. **The seven campaign modules are NOT touched**: they are batch 7's files, they move in batch 7's commit, and repairing them now would put a path change in a batch that is not moving them — the same reason `_SHIPPING_RE` waits for batch 8. **The standing instruction this row exists to add:** when a path constant moves, scan for **arithmetic on it** — `.parents[N]`, `.relative_to()`, `str().split("/")`, any index into a path — not only for its spelling, because a depth assumption is invisible to every prefix search this plan runs. | **whoever runs batch 7** (the seven modules, in the commit that moves them); fleet (the scan-for-arithmetic rule) |
| D386 | **A PATH REGISTER MATCHED BY STRING EQUALITY REPORTS A MOVED FILE TWICE — ONCE AS A STALE ENTRY AND ONCE AS AN UNREGISTERED ONE — AND THE TWO CANCEL IN EVERY AGGREGATE.** `sdk/chief_engineer/exec_bits.py`'s `WAIVED_NO_EXEC_BIT` is the dated register of tracked shebang-bearing files with no exec bit; **137 of its 255 entries sit under a batch-6 source** (dafoam 111, committee-grids 20, hlpw6 6). Matched by string equality against `git ls-tree -r HEAD`, R22 would have moved all 137 out of `modes` — reporting them as `stale_waivers`, *"a waived file … that no longer exists"* — while their successors under `cases/` entered `unregistered` as new shebang scripts with no bit and no waiver. **137 leave one list and 137 enter the other: the register length does not move, the shebang-script total does not move, and a reader comparing sizes sees nothing.** Two currently-PASSING tests would have flipped — `test_the_waiver_register_still_describes_the_tree` and, via a missing `cases/` row in `OWNERS` sending all 137 to `UNASSIGNED`, `test_the_register_is_split_across_families_and_says_so`, whose message reads *"a waived path with no owning family cannot be routed"*. The `OWNERS` gap is **literally the row batch 4 had to add for `ops/`**, and the comment batch 4 left beside it is what made this one findable. | MOVE_MAP batch 6, 2026-08-18; `exec_bits.audit()` executed at `0869284e` before and after the move; `campaign/MOVE_MAP_BATCH6_EXECUTION_2026-08-18.md` section 4.2 | **REPAIRED BY TEACHING THE MATCH THE MAP, NOT BY RE-WRITING THE REGISTER.** `exec_bits._spellings()` returns the literal, `lab_paths.redirect()` of it and `lab_paths.unredirect()` of it; `audit()` matches each waived entry against HEAD under all three, and `tracked_shebang_scripts` probes the shebang under the same set so the window between a `git mv` and the commit recording it does not blank the register either. **The register keeps the spelling it was written with** — `WAIVER_REGISTER_DATED = "2026-08-14"` makes it a dated enumeration and re-spelling history is not what makes it true. Two `OWNERS` rows added (`cases/dafoam/` → DAFoam, `cases/` → Demo and website) and **every file keeps the family it already had**; re-assigning a family across a family boundary is the supervision charter's, not a mover's. Executed either side of the move: `missing_required 0, unregistered 16, stale_waivers 0` **identically**, and afterwards `unregistered` reads 24 only because a peer commit (`e4a977ef`) landed eight new F14 shebang scripts, none of them under a batch-6 source. Pinned by three tests, of which two are the load-bearing controls: `test_a_waived_path_is_still_matched_after_its_tree_moves`, `test_a_waived_path_that_is_nowhere_under_either_spelling_is_still_stale` (without which a matcher answering *covered* to everything passes the first), and `test_a_moved_waiver_that_gained_its_bit_is_still_stale` (without which following the map swallows the finding the register exists to make). **Owed to batch 7:** 61 further waived paths sit under `demo-output/website/campaign/`, and they now need no edit — the matcher follows R20/R21 for free. | fleet |
| D387 | **BATCH 2'S OWN `.gitignore` COMMENT ROUTES ITS RE-POINT OBLIGATION TO BATCH 7, AND HALF OF IT BELONGED TO BATCH 6.** `.gitignore:140-158` ends *"Those trees are excluded exactly as `MESH_AUDIT_runs` is, and batch 7 untracks them at their new path. **Batch 7 must re-point these two roots when it moves them**"*. The two roots are `demo-output/website/campaign/` and `demo-output/website/dafoam/`. `campaign/` is R20/R21 and is batch 7; **`dafoam/` is R22 and is batch 6**. An obligation deferred to a batch that runs AFTER the move leaves a window in which the rules name a path `git mv` has vacated, and an ignore rule naming a vacated path stops ignoring anything: the next `git add` sweeps in what it used to hold. **Twenty rules across five blocks named a batch-6 source** — four mega-batch live/transient, six mega-batch runner-state, five F6d random-matrix ensembles, two dafoam solver-time-directory patterns and three dafoam solver-log patterns. | MOVE_MAP batch 6, 2026-08-18, over `git ls-tree -r 0869284e`; `campaign/MOVE_MAP_BATCH6_EXECUTION_2026-08-18.md` section 4.1 | **ALL TWENTY RE-POINTED IN THE COMMIT THAT MOVED THE TREE, AND THE COMMENT AMENDED IN PLACE** so the next reader meets the correction where the rules are rather than in a record. **The re-point untracks nothing and that was measured before it was made:** 1,833 tracked dafoam files, **0** of which match `log.*`, `*.log`, `*.log.*`, a `[1-9]*/` segment or a `0.[0-9]*/` segment at either spelling. What the rules hold is the untracked side — the five F6d trees alone are 16,146 files and 4,924,886,566 bytes, and the dafoam rename carries 238 dark trees / 22,324 files / 6,807,297,223 bytes. All **2,118** destination paths were passed through `git check-ignore --no-index --stdin` afterwards: **0 ignored**, stderr empty. `demo-output/website/solve_registry/` on the same line as the runner block was deliberately **left alone** — it is 7H's hand-carry, it has not moved, and re-pointing an ignore rule ahead of its tree is the mirror image of this defect. **Still owed by batch 7:** the `campaign/` half of the same rules, plus `.gitignore:122-123`'s two `THERMAL_K0_runs` re-inclusions, without which that case's 97 tracked initial-condition files stop being tracked at the next index refresh. | **whoever runs batch 7**; fleet |
| D388 | **SETTLES D381.** **THE PLANTED-SOURCE RECOVERY CONTROL CANNOT BE LANDED TO ITS GOVERNED TOLERANCE ON AN UNSTEADY OPEN CASE, AND THE OBSTRUCTION IS THE CASE AND NOT THE INSTRUMENT.** K2a section 8 pre-registers "C3 planted volumetric source (KV1's plant in the room)". KV1's 5.000e-03 W plant is 1.0e-06 of this module's 4.914e+03 W ledger and could not have failed, so K2b re-sized it to **500.000 W** (~10 % of the rack load, making the governed 0.1 % recovery tolerance 0.500 W). Run to 5,000 iterations against its byte-identical negative twin `K2bP_C3b_noplant`, the recovery reads **353.337, 406.290, 460.392, 536.469, 466.300 W** at iterations 1,000-5,000: it **reaches the plant and then oscillates about it**, crossing 500 W between 3,000 and 4,000 and overshooting to +7.29 % before falling back to -6.74 %. **MORE ITERATIONS WILL NOT FIX IT AND THE REASON IS MEASURED:** the no-plant twin's own ledger net wanders **48.313 W peak-to-peak** over iterations 2,000-5,000, a floor **97x** the 0.500 W tolerance. The parent case fails S13 at 0.34553 %, so its ledger oscillates by tens of watts, and the matched-twin difference does not cancel it because the plant perturbs the flow enough that the twins leave the same phase. **What it DOES establish:** the ledger recovers a planted source on this geometry **to about 7 %**, from -68.89 % at 400 iterations, monotonically across four decades of the run - and the control is emphatically not degenerate, its readback having travelled 155.55 -> 536.47 W. On a STEADY case the same control is excellent (KV1a, +2.40e-08 %). **To settle:** on the 3D module, run this control only against a case that meets S13, or against a time-averaged ledger, or replace it with one that does not depend on closure. Spend: 13.51 of 20 core-minutes authorised. D381 recorded this as "convergence-limited, needs ~16 more core-minutes"; that was the right filing on the evidence then and it is now superseded -- the obstruction is not iteration count. D381 is left standing rather than rewritten, per this file's append-only rule. Found at F14 rung K2b-pilot. Owner: fleet. |
| D389 | **S13 NORMALISES THE PEAK-TO-PEAK SPREAD BY THE MEAN, WHICH ON AN ABSOLUTE TEMPERATURE IS 24x LOOSER THAN IT READS.** Found while repairing S13's null-variation defect at K2b and deliberately NOT repaired in the same pass. `thermal.monitor_peak_to_peak_max_pct` (0.02 %) is applied to `spread / |last value|`. For K0c's Nusselt number - an O(1) dimensionless group - the mean IS the scale and that is right. For a rack-inlet temperature the mean is ~289-300 K while the physically meaningful scale is the rack rise dT = 12 K, so **0.02 % of the mean is 0.058 K where 0.02 % of the signal is 0.0024 K**. Measured consequence on this rung: `K2bP_coarse` passes at a spread of 2.929e-03 K against the mean-normalised band and would **FAIL** against a dT-normalised one - which would agree with its heat balance, still 0.7857 % out at 9,000 iterations, and with S13 read on its free boundary, 0.03973 %. So on this case all three instruments agree the run is not converged and only the mean-normalised S13 says otherwise. **Not repaired here on purpose:** changing the gate's normalisation moves verdicts across the whole thermal corpus (K0c's eleven, K2e's thirty, KV1's three) and is not a decision one rung should take unilaterally. **To settle:** decide whether S13 normalises by the mean, by a declared signal scale, or by the quantity's own dynamic range over the run, and re-grade the corpus once under whichever is chosen. Found at F14 rung K2b-pilot. Owner: chief. |
| D390 | **A COST RECORD THAT OUTLIVES THE RUN IT DESCRIBES IS WORSE THAN NO COST RECORD, AND ONE ALMOST TRAVELLED.** `K2b_runs/run_k2b.sh` wrote `COST.txt` only after the solver exited and never removed the previous one first. When `K2bP_C3b_noplant` was re-run from 800 to 5,000 iterations, the stale file - `iterations 800`, `core_minutes 1.2083` - sat beside a log that had already reached 3,460 iterations, and a `until [ -f COST.txt ]` wait armed against it returned **instantly**, reporting a live run as finished at the wrong price. Repaired: `rm -f COST.txt` now runs first and unconditionally, with the incident in the script as a comment. **The same class bit twice in one session:** `heat_balance.py` audits invoked against time directories that did not exist yet left `HEATBALANCE_<t>.txt` reports carrying no ledger, which were cleared and re-run rather than committed. **The transferable rule:** any artefact written at the END of a step must be deleted at its START, or a killed step leaves a confident-looking record of a run that never happened. Found at F14 rung K2b-pilot. Owner: fleet. |
| D391 | **THE `outletMappedUniformInlet` FALLBACK BRANCH IS UNTESTED ON THIS MODULE AND THE CONTROL AGAINST IT HAS NEVER BEEN SEEN TO FIRE.** D380 records that the BC's `else` branch appends no `offset`, so a rack front face that loses net outflow silently stops adding heat, and K2b adopted `T(rack_out) - T_mdot(rack_in)` as the in-log reachability control against it - measured at +1.75e-05 % on the converged case. **But that control has only ever been observed PASSING.** No K2b case ever drove `gSum(phi)` on a rack front face to or below `SMALL`, so the branch that would make it read 0.000 K was never entered, and "it can fail" is an argument from reading the source rather than a demonstration. That is the same standard KV1b was held to and failed. **To settle, and it is cheap:** build a one-off case with a rack face whose imposed flow rate is zero or sign-reversed, confirm the readback reads 0.000 K rather than dT, and record it. Until then the control is REACHABILITY-BY-CONSTRUCTION, not reachability-by-demonstration, and K2b's write-up says so. Found at F14 rung K2b-pilot. Owner: fleet. |
| D392 | **SETTLES D386. K2a SECTION 6's WALL-TREATMENT SPECIFICATION IS INAPPLICABLE TO THIS FLOW: THERE IS NO LOG LAYER TO PUT A CELL CENTRE IN, AND THE MESH WAS NEVER THE PROBLEM.** D386 recorded y+ of 2.9-9.1 against K2a section 6's stated 30-300 band, getting worse under refinement (3.3-5.4 on the fine mesh), and asked whether this module class is low-Re-resolved or wall-functioned. **Measured, not argued.** Friction velocity from the committed y+ data is 0.0074-0.023 m/s, so the viscous length nu/u_tau is 0.69-2.14 mm and **y+ = 300 -- the TOP of the specified band -- sits 0.21 to 0.64 m from the wall. On `wall_cold` that is beyond the cold aisle's own mid-plane (0.60 m).** Taking the largest length the boundary layer could possibly occupy (the distance to the nearest opposing boundary) as an upper bound, **delta+ is 560 on the cold-aisle wall and 916 on the rack top**, where a distinct log region needs delta+ well above ~1000 before it exists at all. **The band the spec asks for does not fit inside the flow it is meant to describe**, and reaching y+ >= 30 would need a 41-128 mm first cell -- 3 to 10 % of the aisle width in a single cell. The 30-300 figure is marked RECALLED in K2a section 6 and is generic wall-function practice, not something derived from this flow. **THE FIX IS THE MODEL, NOT THE MESH, AND IT COSTS NOTHING.** `nutkWallFunction` assumes a log law that is not present; `nutUSpaldingWallFunction` is continuous through the viscous and buffer layers and is valid at exactly the y+ this mesh already produces. Three cases byte-identical except `0.orig/nut` -- verified file by file, in place and in a fresh tree -- give window-mean theta of 0.4246 (nutk), 0.4113 (Spalding) and 0.4569 (low-Re). **THE DIFFERENCES ARE NOT RESOLVED:** Spalding-baseline is -0.1587 K against a 0.7363 K noise band (0.22x) and low-Re-baseline +0.3879 K against 1.0497 K (0.37x). The spread across all three is 0.5466 K on the window mean against a largest single-case swing of 1.0832 K -- **the whole wall-treatment question moves T_in by less than one case's own wobble.** What is established is a BOUND: the choice among applicable treatments moves theta by ~11 %, not distinguishable from this configuration's unsteadiness. **And a single-snapshot difference would have been badly wrong**: the same Spalding-baseline comparison reads +20.6 % on theta at iteration 2,000 and -3.1 % on the window mean, because these cases do not meet S13 and a snapshot measures the phase of an oscillation. **This is the same wall C3 hit** -- there the twin's ledger wanders 48.3 W against a 0.5 W tolerance; here the twins' inlet temperature wanders a kelvin. On this configuration the UNSTEADINESS is the binding limit on every differential measurement, not the instrument. **Correcting the treatment requires no extra cells and therefore no extra 3D compute.** **To settle:** amend K2a section 6 to specify a continuous/low-Re wall treatment with a y+ target of <~5 rather than 30-300, stating that no log layer exists at this Ra and these velocities. Found at F14 rung K2b. Owner: fleet. |
| D393 | **SETTLES D389, AND REPAIRS A FALSE POSITIVE THIS AGENT SHIPPED HOURS EARLIER IN THE SAME CRITERION.** D389 recorded that S13 normalised the peak-to-peak spread by the quantity's ABSOLUTE MEAN, which on a rack-inlet temperature (mean 289 K, range 0.086 K) is a factor of 3,343 looser than it reads. **Repaired: the spread is now referred to the RANGE THE QUANTITY SPANNED OVER THE RUN** (`docs/physics_rules.yaml` `heat_monitor_normaliser: range_spanned_over_run`, MONITOR_STANDARD v1.12, `scripts/check_convergence.py`). The threshold NUMBER is unchanged at 0.02 % because its original derivation -- "one fiftieth of the tightest pass band K0c gated on" -- was always a fraction of the thing being resolved and the mean was only a proxy. **THE SAME REPAIR FIXES A FAULT OF MINE.** The v1.11 null-variation clause tested the WINDOW SPREAD against print resolution, which cannot distinguish "never moved" from "converged to the last bit" -- both give a spread of zero -- and it **refused four committed K2e cases** (`m96_dT10/20/30/90_bou`) that print SIXTEEN significant figures bit-identically across the window after travelling 0.53. It now tests **the range over the whole run**, the same quantity D389 requires: one repair, both faults. **RE-GRADE OVER ALL 49 COMMITTED CASES** (K0c 11, K2e 30, K2b 8), against the ORIGINAL criterion: **exactly TWO verdicts change, both K2b's** -- `K2bP_fine` CONVERGED -> CANNOT_TELL (never resolvably moved) and `K2bP_coarse` CONVERGED -> NOT_CONVERGED at 3.3876 % of its range, **which agrees with the two independent instruments on that same run** (heat balance 0.7857 % out, S13 on the free boundary 0.03973 %). All 11 K0c and all 30 K2e verdicts unchanged. **Both constants shown non-load-bearing by sweep:** the threshold gives an identical verdict set anywhere in (0.0027, 0.0642] %, a 24x span with 0.02 inside it; the ulp floor anywhere from 2 to 1e4, 3.7 orders. **AND THE FIRST RE-GRADE WAS UNDER-SCOPED** -- it swept 14 cases and omitted K2e's thirty, which had been graded under the same old reading and were committed in the same tree; widening the corpus is what exposed the false positive. Found at F14 rung K2b. Owner: fleet. |
| D394 | **`TREND-ONLY` IS RETIRED IN CODE WHILE THE REGISTERS USE IT AS A LIVE HONESTY CAP, SO READING THE CODE ALONE UPGRADES THE RUNG AND DELETES THE REASON.** `sdk/chief_engineer/lab.py:194` maps `LEGACY_CHIPS = {"TREND ONLY": SOLVER_BACKED, ...}` under the comment *"Retired labels, kept only so historical records still map to a chip"*, and `:198` binds `TREND_ONLY = SOLVER_BACKED` outright under *"Backwards-compatible aliases for older call sites/tests"*. **The label is not historical.** `docs/NUMERICS_KNOWLEDGE.md:486` states it as a live rule: *"No trust tier above TREND ONLY without an experimental comparison (Salehi and Nilsson, V and V 20). Mesh quality and convergence justify a trend; only data justifies a magnitude."* `docs/campaigns/F14-cooling-ladder/K0c_DIFFERENTIALLY_HEATED_CAVITY_GATE.md:20` and `:218` apply it as a cap this session, and fifteen tracked markdown files use it live. **The two are not the same statement.** SOLVER-BACKED means *a real solve produced it; no experimental comparison, or the comparison is not like-for-like* (`lab.py:183-184`). TREND ONLY means *no experimental comparison exists at all, and therefore no magnitude claim is admissible.* The mapping keeps the chip and **erases the second clause**, which is the entire honesty content. An agent that reads the code alone sees a rung capped at TREND ONLY and reports it SOLVER-BACKED, with nothing left on the surface to say why it was capped. **The erasure is demonstrated inside the lab's own tests, not argued:** `sdk/tests/test_curriculum.py:118`, `:130` and `:178` assert `verdict["tier"] == TREND_ONLY` in tests named `test_out_of_tolerance_but_converged_is_trend_only` and `test_bad_mesh_caps_at_trend_even_when_the_number_agrees`. Because `TREND_ONLY is SOLVER_BACKED`, **all three assertions are satisfied by a SOLVER-BACKED verdict and cannot fail if the cap is lost.** Three tests are named for a distinction they cannot make. **Found at:** Reference-acquisition session, 2026-08-18, while repairing the nine-act table's referent column. **What settles it:** **NO CHIP WAS CHANGED ON ANY RECORD, deliberately: re-tiering a rung is a verdict change and it is the owner's.** What settles it, in the order the work would run: (1) give TREND ONLY its own constant with its own reason string rather than an alias, so `trust()` and `validate_against_reference()` can return *no experimental comparison exists for this quantity* as a distinct fact; (2) fix the three `test_curriculum.py` assertions to compare against that constant, which makes them able to fail; (3) leave `LEGACY_CHIPS` alone, since a historical record genuinely does need to map, and separate the two concerns the one dict now conflates. **Until (1) lands, no sweep over code can count the lab's TREND-ONLY rungs**, and the registers are the only surface that knows they exist. Owner: fleet. |
| D395 | **ACT 1 OF THE NINE-ACT TABLE PRINTS PASS WHILE MISSING THE BAND THE ACT DECLARED FOR ITSELF, BECAUSE THE GENERATOR SUPPLIED ITS OWN.** `mission-output/cylinder-vortex-shedding/transcript.md:5` states the act's gate in the act's own words: *"Gate: Strouhal number against the Roshko and Williamson correlation, **within 0.70%**."* The deviation the same transcript prints at line 19 is **0.77 percent**. The published row reads **PASS**. The act's transcript carries no `Verdict:` line, so `scripts/gate_table.py` fell through to `_tolerance_verdict(dev, limit=5.0)` and decided the row against **its own 5 percent default**, which was never printed. Four other rows (wedge, cone, diamond airfoil, hypersonic cylinder) declared no band at all and were decided by the same default, and no reader of the table could tell those four from the one that declared 0.70 and missed it. **Partly repaired 2026-08-18:** the table now carries a `band, as the act declared it` column, parsed back out of each act's own transcript, and names the generator's screen by file and function wherever it decided a row. **The verdict cell was NOT changed.** **Found at:** Reference-acquisition session, 2026-08-18, found by the band column the same session added. **What settles it:** The cell reads PASS against a screen the act did not set and FAIL against the one it did, and choosing between them is a verdict change. **Owner's ruling required**, and it is one of two: re-grade act 1 against its declared 0.70 percent and publish the result whatever it is, or amend the act's declared gate with a stated reason and re-run the surface. A third option exists and should be named to be refused: leaving the row PASS while the band column states 0.70 is honest today and decays the moment somebody reads only the verdict column. Related: D12, whose reading establishes that the 0.1590 the 0.77 percent is measured against has no cited source at all. Owner: fleet, then Katie. |
| D396 | **THE CRM ACT'S `VALIDATED` CHIP IS ASSIGNED INSIDE THE ACT, BYPASSING THE ONLY FUNCTION THAT IS ALLOWED TO GRANT IT.** `sdk/chief_engineer/lab.py:182` defines the chip as *"graded against a published experiment and inside its band"*, and `:224-225` states *"VALIDATED is earned only against a published experiment, that path is `validate_against_reference()`"*. Act 9 never calls it: `sdk/workflows/crm_wingbody.py:328-333` computes `gate_ok = abs(deviation) <= GATE_TOLERANCE` against `PUBLISHED_CD = 0.02090` and assigns `verdict = {"tier": VALIDATED, ...}` directly, where the reference is DAFoam's own `CRM_Wing` tutorial documentation, the same code, the same downloaded mesh and the same unmodified `daOptions`. **The tier rule is not being violated by a bad judgement inside the tier path; it is being routed around.** The same shape should be swept for: any module that imports the `VALIDATED` constant and builds a verdict dict itself has the same freedom, and the rule lives only in the one function's branches. **Surface repaired 2026-08-18** without touching the chip: the nine-act table now states row 9's referent class as ANOTHER SOLVER, CODE-TO-CODE and names both `lab.py:224-225` and the assignment site. **Found at:** Reference-acquisition session, 2026-08-18, while making the nine-act table carry its referents. **What settles it:** **The chip was left in place because removing it is a verdict change and it is the owner's.** What settles the machinery half regardless of that ruling: (1) `git grep -n 'VALIDATED' sdk/workflows/` and count the modules that assign the constant directly rather than calling `validate_against_reference()`, which this row did not do and is one command; (2) either route those call sites through the graded path or make the constant private to `lab` and expose only the graded function, so the rule at `:224-225` is enforced by construction rather than by every act author remembering it. A rule stated in a docstring beside a constant anyone can import is a comment, not a gate. Owner: fleet. |
| D397 | **THE F14 RACK-ROW MODULE AT 70 % PROVISIONING IS PHYSICALLY UNSTEADY. THE STEADY FORMULATION IS THE WRONG TOOL AND THE 374-697 CORE-MINUTE 3D ESTIMATE IS VOID.** Pre-registered in `K2b_UNSTEADINESS_PREREGISTRATION.md`, hashed `932451ad...` and timestamped 2026-08-18T05:20:05Z **before any diagnostic solver ran**; three amendments, each written before the data it could have affected existed, and **no threshold ever moved**. **TEST A, under-relaxation halved on an otherwise byte-identical case:** the T_in oscillation grew from 1.0162 to **1.8396 K, a factor of 1.81** (1.91 against a development-matched baseline window). Under-relaxation is the direct remedy for an unstable outer iteration and has no counterpart in physics -- it cannot damp a real vortex, only the solver's pursuit of one. **TEST B, a 40 s transient (`buoyantBoussinesqPimpleFoam`) started from the oscillating state, BCs byte-identical:** amplitude **1.1088 K over 30-40 s against 1.1293 K over 20-30 s, ratio 0.982 -- no measurable decay across 40 s of physical time.** Graded on BOTH pre-registered window pairs; both say PHYSICAL. **TEST C:** dominant period **6.000 s**, autocorrelation +0.96 at one period and +0.94 at two -- a coherent limit cycle -- matching the **rack-face flow-through time H_r/U_face = 6.856 s to 12 %**, and four of five physical timescales within a factor of three. The pre-registered Strouhal band 0.05-0.5 FAILED at St = 1.14 and is reported as it fell: the mechanism is an oscillating recirculation cell, not bluff-body shedding. **ONE MECHANISM EXPLAINS THREE SEPARATE FAILURES ON THIS RUNG**: C3's plant recovery could not reach 0.500 W because the twin's ledger wanders 48.313 W; the wall-treatment comparison could not resolve 0.5466 K against a ~1.1 K swing; `K2bP_under` never met S13 and `K2bP_coarse`'s balance never closed. **Every steady-solver number for `K2bP_under` and its twins is a mean over a limit cycle, not a converged value**, and is relabelled as such. **COST: 36.75 core-minutes to invalidate a 374-697 core-minute plan BEFORE it was paid for.** **What it cannot reach:** this is the 2D slice, which suppresses spanwise instability and can also manufacture oscillations a 3D flow would damp -- so O1 is a warning about the 3D module, not a measurement of it. **To settle:** authorise the ~11 core-minute 3D coarse TRANSIENT named in `K2b_PILOT_RESULTS.md` section 14.6 before any graded pair, and re-specify K2a section 9 for a transient-with-averaging formulation at this provisioning. Found at F14 rung K2b-U. Owner: chief. |
| D398 | **NO REPLACEMENT COST NUMBER IS OFFERED FOR THE 3D MODULE, AND THAT IS DELIBERATE.** With the steady estimate void (D397), the obvious next move is to multiply it by K2a section 5's 5-10x unsteady factor and hand over a new range. **That would repeat exactly the error the diagnosis just caught: pricing a formulation before knowing it is the right one.** The 5-10x is K2a's planning figure and has never been measured on this module; the transient measured here cost **29.67 core-minutes for 40 s of physical time at 46,400 cells**, which is a rate for a 2D slice and says nothing about how much averaging a 3D graded quantity would need. **What IS carried over:** the measured throughput of **3.968e5 cell.iter/(core.s)**, which is a property of the machine and the mesh rather than of the formulation. **To settle:** measure the averaging requirement -- how long a transient must run before the mean of the graded quantity stops moving -- on the cheapest case that has a limit cycle, and only then price the 3D module. Found at F14 rung K2b-U. Owner: fleet. |
| D399 | **THE SPECIFIED 3D MESH PAIR CANNOT SEE THE PHENOMENON THAT DECIDES ITS OWN FORMULATION: THE LIMIT CYCLE EXISTS ONLY AT 12.5 mm AND K2a SPECIFIES 60 mm AND 40 mm.** Pre-registered in `K2b_3D_UNSTEADINESS_PREREGISTRATION.md` sha256 `91a26fc9...`, timestamped 2026-08-18T06:02:35Z before either case was built, with three outcomes fixed including the one that would have vindicated the plan. **THE GATE FAILED:** the 2D slice re-run at the 3D test's own 100 mm cell shows the 6.000 s cycle DAMPING (final-window p2p 0.7792 K against a preceding 1.6156 K, **ratio 0.482** against a 0.5 threshold), so a 3D run at 100 mm could not separate three-dimensionality from mesh resolution. Test D was **not run** and no 3D verdict is claimed -- outcome **P3**. The aliasing guard passed on every rung (34.4 steps and 29.9 samples per period against floors of 20 and 10) and was never the reason for a refusal. **P3 OWES A MEASURED PRICE, SO THE RESOLUTION LADDER WAS RUN:** 2D transients at 100, 50, 25 and 12.5 mm give ratios 0.482, 0.318, 0.377 and **0.982** -- **the cycle exists at 12.5 mm and at no coarser mesh tested.** **TWO CONSEQUENCES.** (a) The TIME-MEAN is nearly mesh-converged -- 293.9410, 293.9351, 294.0662 K across the three finest, a spread of **0.1311 K** -- while the UNSTEADINESS is not, so a coarse mesh gets the mean roughly right and misses the oscillation entirely. (b) **K2a section 6 specifies a 60 mm coarse and 40 mm fine 3D pair, BOTH on the damping side of a threshold measured between 25 and 12.5 mm, so a graded pair built exactly to specification would return a converged steady-looking answer that is an artefact of under-resolution** -- a confident wrong answer rather than a refused one. **THE UN-CONFOUNDED EXPERIMENT COSTS ~22,064 core-minutes (368 core-hours)** at the measured 4.261e4 cell.steps/(core.s): a 14.7 M-cell 3D transient at 12.5 mm, **32-59x the entire 374-697 core-minute graded pair it would be checking.** **To settle:** do NOT run the specified 60/40 mm pair to decide steadiness; section 15.6 names the cheaper prior experiment. Found at F14 rung K2b-U3. Owner: chief. |
| D400 | **O1 IS NOT ESTABLISHED AS MESH-CONVERGED, AND THIS ROW IS FILED AGAINST THIS AGENT'S OWN PRIOR FINDING.** `K2b_PILOT_RESULTS.md` section 14 concluded O1 -- the module is physically unsteady -- on three legs measured at 12.5 mm: the oscillation grew **1.81x** when under-relaxation was halved, it did not decay across **40 s** of physical time (ratio 0.982), and its **6.000 s** period matched the rack-face flow-through time to 12 %. Those measurements stand. **What the resolution ladder (D399) adds is that the limit cycle exists at exactly ONE resolution on the ladder and has never been shown to persist under FURTHER refinement.** A physical instability should converge under refinement; one point on the sustaining side is one point. **The argument that favours O1** -- under-resolution adds numerical diffusion, which damps instabilities rather than creating them, so coarse-damps/fine-sustains is the expected direction, and Test A's growth under reduced solver damping points the same way -- **is an argument, not a measurement.** **The measurement that would settle it is a 6.25 mm 2D rung, ~460 core-minutes**, which was outside this authorisation and was not run. **Consequence for the wording of every K2b claim:** every sentence in section 14 that reads as a statement about *the module* is a statement about *the module at 12.5 mm* until that rung exists, and section 15.5 says so in the document itself rather than only here. **To settle:** run the 6.25 mm 2D rung before any 3D unsteadiness work is authorised; it is 48x cheaper than the 3D experiment it would qualify. Found at F14 rung K2b-U3. Owner: fleet. |
| D401 | **MOVE_MAP R25 IS DEFERRED BECAUSE ITS SOURCE TREES WERE UNDER A LIVE SOLVER, AND LIVENESS IS A PRECONDITION NO REPOSITORY SCAN REPORTS.** `MOVE_MAP_EXECUTION_2026-08-17.md` section 4 prices batch 7 as R20, R21, R24 **and R25**. R25's sources are `docs/campaigns/F14-cooling-ladder/{K0c_runs, K0b_mesh_sensitivity, K2b_runs, K0cT_runs}`. Measured immediately before the move: **`buoyantBoussinesqPimpleFoam` PID 1809944 was writing `K2b_runs/K2bU_trans/log.buoyantBoussinesqPimpleFoam` in the second the measurement was taken**, three peer shell loops held that directory as their working directory, and the owning lane had landed **ten commits in 75 minutes**, three of them inside this pass. A `git mv` there would have renamed a directory out from under an open solver whose writes would have continued into the moved inode, invisible at the path its own lane reads, and left three polling loops testing a path that no longer exists. R20/R21/R24 landed (2,367 files); **R25's 1,554 tracked files did not**, and the figure was 315 at the ruling, ~487 at batch 5's frame, 1,514 at `aebbcac8` and 1,554 at `fccedd3a` — it grew by 40 in the 45 minutes this pass took. **To settle:** run R25 when the F14 lane is quiescent, re-pointing `.gitignore:144-149`, `:161-163` and `:260-264` in the same commit, and DECIDING rather than inheriting the `exec_bits.OWNERS` family — those files are "Infrastructure and Standards" under `docs/` and would become "Cases" under `verification/runs/`; 0 waived paths are under `docs/campaigns/` today, so nothing forces the answer. `lab_paths` needs no edit: `F14_K0C_RUNS` and `F14_K0B_MESH_SENSITIVITY` are bound to pairs and report `state() == "legacy"`. Found executing MOVE_MAP batch 7. Owner: fleet. **CLOSED 2026-08-18 by the R25 commit.** The quiet window was re-verified immediately before the move and all three conditions had cleared: **0 solver processes of any name**, **0 processes holding the tree as a working directory and 0 open file descriptors into it**, and a newest write **99.0 minutes old**. The population was 1,713 tracked files at `a1fbe127`, re-derived over `git ls-tree -r HEAD` and unchanged from the last recorded figure. The split was re-measured by composition rather than carried: **1,671 move, 42 stay** across **six** run trees (`K2e_runs` 577, `K2b_runs` 374, `K0c_runs` 274, `K0cT_runs` 230, `KV1_runs` 175, `K0b_mesh_sensitivity` 41) — two more trees than this row names, both landed after it was written. **Fourteen `.gitignore` rules** were re-pointed, re-derived by prefix at `:144-149`, `:161-163` and **`:284-288`** rather than at the `:260-264` this row cites, the file having grown 24 lines since. `exec_bits.OWNERS` was **DECIDED and not inherited: "Cases"**, by letting the existing `verification/runs/` row answer and adding no row — see the comment at that row for why the batch-7 preservation precedent does not transfer when 0 register entries move. `lab_paths` needed no edit, **confirmed by executing `redirect()` and `resolve()` over all 1,671 paths** rather than assumed. |
| D402 | **TWO GENERATORS HAVE BEEN WRITING INTO NOTHING SINCE BATCH 6, AND BATCH 7 FOUND THEM ONLY BECAUSE THEIR OUTPUT LANDS IN ITS OWN DESTINATIONS.** `cases/dafoam/f6d_random_matrix_uq/make_campaign_json.py:7` read `CAMP = HERE.parents[1] / "campaign"` and its last line is `(CAMP / "F6d_random_matrix_uq.json").write_text(...)`. R22 moved that file from `demo-output/website/dafoam/f6d_random_matrix_uq/` to `cases/dafoam/f6d_random_matrix_uq/` in batch 6, at which point `parents[1] / "campaign"` became `cases/campaign` — a directory that does not exist. It is L-127's class exactly, one batch late: **no path literal, so batch 6's Class-1 no-suffix-filter scan could not match it, and batch 6's own frame did not include it because the search was scoped to depth derivations under the batch's sources rather than to depth derivations ON them.** `sdk/scripts/model_form_batch.py:1115` and `:1144` are the same story with literals rather than arithmetic — two `sys.path.insert(0, str(REPO_ROOT / "demo-output" / "website" / "dafoam" / ...))` calls in a file batch 3b DID convert, whose `OUT_ROOT`, `BAND_JSON` and `BAND_MD` three lines above are already `lab_paths` names. **A census of converted FILES is not a census of converted SITES.** All three repaired to `lab_paths.CAMPAIGN` / `lab_paths.DAFOAM` in the batch-7 commit and proved by execution. **To settle:** re-run the repo-wide depth-derivation scan (`.parents[N]`, `.parent.parent`, `../..`, `dirname(dirname`) with the receiver checked against `lab_paths` names rather than against `__file__`, at every future move batch, and scope it to the whole tree rather than to the batch's own sources. Found executing MOVE_MAP batch 7. Owner: fleet. **CLOSED by the batch-7 commit for these three sites; the standing scan is the open half.** |
| D403 | **THE K0b MESH-SENSITIVITY RUNG HAS BEEN UNRUNNABLE SINCE BATCH 7, AND R25 FOUND IT WHILE MOVING THE TREE IT LIVES IN WITHOUT BEING ABLE TO FIX IT.** `K0b_mesh_sensitivity/build_and_run.sh:29` reads `SRC=$(cd "$HERE/../../../.." && pwd)/demo-output/website/campaign/THERMAL_K0_runs/K0b_cavity_Ra1e5` and `analyse_k0b_mesh.py:49` builds the same path as `K0B_64`. **R20 moved `THERMAL_K0_runs` to `verification/runs/THERMAL_K0_runs` in batch 7**, so both have been naming a directory that does not exist since that commit. Proved by execution rather than by reading, at the pre-R25 tree: the shell guard `[ -d "$SRC/0.orig" ]` is FALSE and the script exits 2 with *"REFUSE: K0b source case not found"*, and `os.path.isdir(K0B_64)` is `False` while the successor `verification/runs/THERMAL_K0_runs/K0b_cavity_Ra1e5` exists and holds 12 tracked files. The consequence is not cosmetic: `K0B_64` is the **64x64 rung of a three-rung mesh ladder** (`32x32`, `64x64`, `128x128`), so the rung whose whole purpose is to say whether K0b's published numbers move with mesh cannot assemble two of its three points. **Neither scan R25 ran would have been required to find this** — it is an ordinary literal and batch 7's own literal scan should have caught it; what hid it is that batch 7 scoped the scan to depth derivations *under* its sources and this file was not a batch-7 source, it was a batch-7 *destination reference* from a tree R25 owns. That is D402's sentence — *"a census of converted FILES is not a census of converted SITES"* — with the scope error on the other axis. **NOT REPAIRED BY R25, deliberately:** R25's mandate is one rule, the defect is batch 7's and predates this move, and batch 7's own precedent for fixing a foreign residue in flight (§6.2) turned on the repair being unprovable otherwise, which does not hold here — R25 changes nothing in either file and both were proved to fail identically before and after. **To settle:** repoint both to `lab_paths.run_archive("THERMAL_K0_runs")`, which resolves today, and then RE-RUN the rung, because the ladder has not been assembled since the move and its published numbers have not been checked against a rebuilt 64x64. Found executing MOVE_MAP R25. Owner: fleet. **CLOSED IN PART 2026-08-18 by the batch-8 commit, and the open half is named.** Both sites now resolve through `lab_paths.run_archive("THERMAL_K0_runs")` and both FIND the repository root by walking up for `scripts/lab_paths.py` rather than counting `..` segments. Proved by EXECUTION on both sides rather than by reading the diff: the shell guard went from *"REFUSE: K0b source case not found"* / exit 2 to **exit 0**, building both new meshes and passing **all 22 byte-for-byte `cmp` checks** (11 files x 2 meshes) against the real archive; `K0B_64` went from a directory with `isdir() == False` to `verification/runs/THERMAL_K0_runs/K0b_cavity_Ra1e5`; and the three-mesh triple went from **2 of 3 legs present to 3 of 3**. The guard was run from a scratch working directory so no tracked case file was touched. The must-not-match control holds: both files copied to a directory with no repository above them REFUSE and exit non-zero rather than returning a path. **THE OPEN HALF, and it is the half this row's own "to settle" names second: the ladder has NOT been re-run.** K0b's published mesh-sensitivity numbers still have not been checked against a rebuilt 64x64 leg, that is compute batch 8 was not authorised to spend, and a rung that CAN run is not a rung that HAS run. **STILL OPEN on the re-run.** **CLOSED 2026-08-18 BY THE RE-RUN, and the half this row left open is the half that closed.** The ladder was RE-RUN under Sanaa's standing authorisation of any run under $25 by the running agent's own estimate, pre-registered at `a157ccba` BEFORE the first solve -- estimate 20.0 core-minutes, hard stop 60, thresholds and convergence criterion fixed there -- and into a DISTINCT tree `verification/runs/F14-cooling-ladder/K0b_D403_rerun/`, because `build_and_run.sh` opens each leg with `rm -rf "$dst"` and the rung it grades holds **41 tracked files** including both `COST.txt`, every solver log and `system/controlDict.4000`; re-running in place would have destroyed the record the run existed to grade. **THE 64x64 LEG WAS REBUILT** from the archive's own tracked dictionaries -- **12 of 12 byte-identical**, the script's `sed` on `(64 64 1)` being a no-op at n=64 -- **and it reproduced the committed case BIT-FOR-BIT: 6 of 6 field files at t=4000, 5 of 5 at the intermediate written times, and the four final initial residuals to every digit the log prints.** Graded against the published numbers through the byte-identical `measure()`: **13 of 13 quantities EXACT on 32x32, 13 of 13 on the fresh 64x64, 13 of 13 on 128x128 continued to 16000** -- zero deviation, not a tolerance met; and the 32x32 leg reproduced its stopping integer, `SIMPLE solution converged in 1386 iterations`. The committed `analyse_k0b_mesh.py` was then run UNMODIFIED end-to-end, exit 0, and its JSON differed from the published one in **11 leaves of 674: 8 wall-clock and 3 `case` path strings.** **ZERO PHYSICS LEAVES DIFFERED** -- not one entry of the 406-, 138- and 6-entry Nusselt histories, no observed order, no GCI, no extrapolate. The three path leaves are THIS ROW'S OWN DEFECT visible in the artifact: the published record names `demo-output/website/campaign/THERMAL_K0_runs/K0b_cavity_Ra1e5` and the re-run resolved `verification/runs/THERMAL_K0_runs/K0b_cavity_Ra1e5` through `lab_paths.run_archive`, which is `726f477e` working on live data rather than on a resolution probe. **P2's answer stands unchanged:** Nu_avg moves **0.552 %** from 64x64 to 128x128, observed order **1.94**, fine-pair GCI **0.243 %**, triple monotone on all six graded quantities. **Cost: 13.974 core-minutes / $0.0119 actual against a pre-registered 20.0 / $0.0171** at the measured $0.0513 per core-hour -- **30.1 % under its own estimate and 0.048 % of the authorisation**; both a wall-clock and a solver-CPU basis are recorded because another lane put eleven solvers on the box mid-run and wall clock stops being a cost instrument on a contended machine (the two bases differ by 1.5 %). The committed archive case was NOT written to: 88 of its 89 files md5-identical after the end-to-end run, the 89th an untracked OpenFOAM log the analysis regenerates by design, and `git status` on that tree returned empty. Record: `docs/campaigns/F14-cooling-ladder/K0b_D403_RERUN_RESULTS.md`; pre-registration: `docs/campaigns/F14-cooling-ladder/K0b_D403_RERUN_PREREGISTRATION.md`. **Two findings surfaced in the doing and did NOT close here, deliberately: D406** (the script does not encode the continuation the published fine leg required) **and D407** (that fine leg's neighbour, the 64x64, has one residual of four quoted as its convergence evidence). **CLOSED.** |
| D404 | **EIGHT PATH CONSTANTS ASSEMBLED FROM SEGMENTS ARE STILL DANGLING FROM BATCHES 6 AND 7, AND THE QUOTED-TOKEN SCAN IS THE ONLY ONE OF THE THREE THAT SEES THEM.** Batch 8 ran all three scan shapes (literal, depth idiom, quoted token) repo-wide rather than over its own sources, per D402's standing item. Its own twelve surfaces were clean. The token scan found **eight sites outside batch 8's mandate that name a directory which does not exist**, every one built as `REPO / "demo-output" / "website" / ...` or `os.path.join(REPO, "demo-output", "website", ...)` and therefore invisible to a literal scan for `demo-output/website/...` and to a depth scan for `parents[N]`: `verification/runs/B52_RUNG6_REPLICATE_runs/run_rung6_replicates.py:39`, `verification/runs/F5c_runs/run_stage_a.py:40`, `verification/runs/FPE_DIAG_runs/run_fpe_diag.py:27`, `verification/runs/GEN_ALT_runs/run_gen_alt.py:27` (all four naming their OWN tree at its pre-R20 spelling, i.e. each module names the directory it now lives in and cannot find it); `verification/runs/4G_runs/bump_iteration_matched/drag_split.py:28` and `ladder.py:23` (R22's `tmr/runs`, batch 6); `sdk/chief_engineer/log_signatures.py:100-103` (`DEFAULT_LEDGER_PATH`, R22's `mega-batch/ledger.jsonl`, batch 6 — a **default** that points nowhere, which is the fail-open shape); `sdk/workflows/rae2822_case9.py:98` and `sdk/workflows/nasa_hump.py:60` (R20's `campaign/F12_runs` and R22's `dafoam`, and these two are worse in kind because they are already CONVERTED to `lab_paths.WEB` and then append the unconverted tail — D402's *"a census of converted FILES is not a census of converted SITES"* verbatim, one root deeper). **All eight have a live successor**: `lab_paths.resolve()` returns one for every one of them, so none is genuinely lost and all eight are one-line repairs of the L-137 form (ask by name, not by spelling). **Not repaired here:** batch 8's mandate is R13 plus the single named D403 repair, and eight foreign repairs inside a commit that must also take a live surface down is how a batch stops being characterisable. **To settle:** repair all eight through `lab_paths` in one commit that is not a move batch, with each proved by execution at the new path, and add the bound-root evaluation of L-138 to the standing scan so the two `lab_paths.WEB`-consuming sites cannot recur. Found executing MOVE_MAP batch 8. Owner: fleet. **OPEN.** |
| D405 | **SEVEN OF THE EIGHT FREEZE-CLAIM CONTROL SHAPES PLANT AT ONE PARAGRAPH, SO THE CONTROL MEASURES RE-WORDINGS AND NOT COVERAGE ACROSS SITES.** Repository: `Certonomous_closure_challenge`, at `6873e7f`. That commit widened `FREEZE_CLAIM`, narrowed `FREEZE_NEGATOR` to the run-up and gave `CLAUSE_END` line-shaped bounds, on five re-wordings and two clause defects measured escaping the scan on 2026-08-18, and its run reported `PASS: 20 checks, the coverage control and the 34 planted shapes` at exit 0 with 34 of 34 caught. **What that PASS does not establish is that the scan finds this claim wherever it stands.** Read off the run output rather than off the source: shapes **AD, AE, AG, AH, AI, AJ and AK all plant at the same site** -- *"position 23 of the 47 para positions in `description/METHOD.md`"* -- and the eighth, **AC**, plants at *"position 17 of the 35 row positions"* of that same file. Eight shapes, one document, and seven of them one paragraph. **A scan that had become position-sensitive at that paragraph, or a `description/METHOD.md` that had drifted, would satisfy all seven and the control would still read 34 of 34.** Seven re-wordings of one plant is evidence about the REGEX; it is not evidence about the CORPUS, and the coverage control does not currently distinguish those two things. Related but distinct in mechanism, and left standing: **AB** plants a citation defect rather than a wording defect, and **AF** is not a freeze shape at all. **To settle:** widen the control SITES rather than the control shapes -- plant the same claim in at least two further documents that carry it and at more than one position in each, and add a must-not-match control at a position where the claim does not stand, so that a scan which had learned one paragraph fails. Found landing the closure tail in F11 thermal Phase A1; the residual was reported in that commit message rather than left silent. Owner: Phase A5. **CLOSED 2026-08-18 in `Certonomous_closure_challenge` at `33a5f23` on `closure-send-package-prep`. The defect was real, the control was widened, and nothing was traded away to make it green.** **The population was measured before anything was changed, and it was not one site.** `_freeze_matches` was run over every shipped text file and the enumeration was written to a file and counted rather than piped through `head` or `tail`: the pre-solve freeze claim stood at **9 sites in 5 files** — `description/CLOSURE_SUBMISSION_REQUIREMENTS.md:267`, `description/DESCRIPTION_DOCUMENT.md:237`, `description/METHOD.md:115`, the frozen `evidence/frozen/R5_RULE_FREEZE.md:1`, and five sites in `scripts/check_consistency.py` itself (`:3588`, `:3615`, `:3617`, `:3675`, `:4571`). 0 of the 9 stood. Four documents, not one, so the control was disproportionate and the answer was not NOT A DEFECT. **This row's own count was re-derived off the run output rather than inherited, and this row was right where the reporting agent's recollection of six was wrong:** seven shapes printed *"position 23 of the 47 para positions in `description/METHOD.md`"* — AD, AE, AG, AH, AI, AJ, AK — and AC printed *"position 17 of the 35 row positions"* of that same file. **What was done.** Each entry of `FREEZE_SHAPES` now names the document it plants in and where in it, read by a new `_freeze_site`. All eight grammatical variants were kept and none was weakened; they were moved apart. The ten shapes plant in **nine documents across five directories**: AC `description/DESCRIPTION_DOCUMENT.md`, AD and AM `description/METHOD.md`, AE `description/CLOSURE_SUBMISSION_REQUIREMENTS.md`, AG `evidence/README.md`, AH `README.md`, AI `evidence/audits/CLOSURE_METHODS_COMPARISON.md`, AJ `evidence/reproduction/REPRODUCE.md`, AK `evidence/audits/closure_challenge_C2_error_decomposition.md`, AL `evidence/reproduction/SCORING_PROCEDURE.md`. The citation each plant carries is built per target by `_ref_from`, so a plant spread across four directory depths does not also break a path and get caught for the wrong reason. The needle the control matches on became `{file}:{line}: offers the` rather than the bare string `R5_PREREGISTRATION.md`, so a run reporting a standing claim somewhere else is no longer counted as catching this one. **The four shapes added, and the failure each saw that the old set could not.** **AL** planted the plainest wording in a document carrying no freeze claim of its own, in a different top-level directory — the shape a scan walking `description/` alone fails. **AM** planted the plainest wording at the last para position of `description/METHOD.md` that stands AFTER that file's own first freeze claim, which was left intact and left clear — the shape a scan stopping at the first match in a file fails; AM raises `PlantError` rather than planting if that first claim is ever missing, later than the plant, or itself standing, so it cannot decay into a test of something else. **AN and AO are the must-not-match half this row's own "to settle" asked for**, and they plant outside `description/METHOD.md` on purpose: AN is the package's own correction sentence, which states the claim in order to DENY it while citing the pre-registration, and AO is the TRUE claim citing the rule freeze alone. Both plant text the claim regex MATCHES and the verdict then CLEARS, and both `_need` exactly that, so a must-not-match control planting text the regex never saw is refused as decoration. **Proved by execution, exit codes captured directly and never through a pipe, and the control was shown to FIRE rather than assumed to.** Before: exit **0**, `PASS: 20 checks, the coverage control and the 34 planted shapes`. After: exit **0**, `PASS: 20 checks, the coverage control and the 36 planted shapes`, 36 of 36 caught and 2 pieces of correct prose left alone; `--self-test` exit 0; `verify_freeze.py`, `verify_manifest.py`, `assert_no_leakage.py` and `reproduce_score.py` each exit 0. **Three defects were then injected into `freeze_claims` in throwaway copies, and each was run against BOTH the widened checker and the pre-D405 one at `6873e7f`:** (i) *stop at the first match per file* — widened **exit 1**, AM and AC MISSED; pre-D405 **exit 0**, `PASS ... the 34 planted shapes`. (ii) *walk `description/` only* — widened **exit 1**, **AG, AH, AI, AJ, AK and AL** MISSED; pre-D405 **exit 0**. (iii) *treat any claim under `evidence/audits/` as standing*, which the clean tree cannot reveal because it carries no claim there — widened **exit 1**, AN and AO reported as CONTROL FAILED; pre-D405 **exit 0**. **This row asserted that a scan failing to reach a second document, or stopping at the first match, would pass the old control unchanged. That is now a measurement and not an argument.** **Corrected 2026-08-18, before this row was landed: the commit message at `33a5f23` and the first draft of this row both listed FIVE shapes under (ii) and dropped AK**, which plants in `evidence/audits/closure_challenge_C2_error_decomposition.md` and is missed by that break exactly as the other five are. The list was re-derived by re-running the injection rather than re-read off a scrolled terminal; it is six. The commit message is landed and still carries the short list, which is why this is stated here rather than only fixed. **Disclosed rather than papered over.** No shape plants in `evidence/frozen/`, which carries the fourth real site at `R5_RULE_FREEZE.md:1`: under the 2026-08-17 ruling the freeze is kept and departures are disclosed, not edited away. The scan's reach into the three frozen documents was measured a different way, by `freeze_scan_positions`, which planted every one of the ten shapes at every position of all **17** shipped markdown documents, the frozen ones included, in memory and against the text alone — **0 escaped of 531 row positions and 0 of 765 para positions, per shape**. Only `scripts/check_consistency.py` was edited; no shipped document moved a byte. Submissions remain **PARKED**: nothing was sent, uploaded, filed or registered, and the closure repository's public/private status was neither decided nor assumed. **This row was written into this file twice.** The first append was made at 17:33 and was gone by 17:40, overwritten by a concurrent writer that rewrote `docs/DOCKET.md` whole; the re-application above was a MERGE onto the file as it then stood, and it is landed rather than left in the worktree for the same reason. |
| D406 | **`build_and_run.sh` DOES NOT REPRODUCE ITS OWN RUNG'S PUBLISHED NUMBERS, AND THE STEP IT IS MISSING EXISTS ONLY AS PROSE IN A README.** `verification/runs/F14-cooling-ladder/K0b_mesh_sensitivity/build_and_run.sh` runs every leg to the source case's `endTime 4000` and contains **no continuation step**, while the published 128x128 leg was continued BY HAND to 16000 iterations. The rung's README describes that continuation in prose and `K0b_m128/system/controlDict.4000` is the surviving fingerprint of the hand edit, but nothing executable performs it -- and worse, the copy at `build_and_run.sh:69` overwrites `system/controlDict` from the source case on every invocation, so the hand edit cannot survive a re-run of the very script that is supposed to have produced the result. **PREDICTED BEFORE THE SOLVE AND THEN MEASURED.** It was registered as prediction P-D in `K0b_D403_RERUN_PREREGISTRATION.md` section 4, committed at `a157ccba`, from READING the script rather than running it, at *"approximately 4.3255, i.e. approximately 4.5 % below"*. Measured: **4.3254648850 against the published 4.5288167412, a deviation of 4.490 %.** The whole ladder goes with it -- run the two commands the README documents, in the order it documents them, and the table reads observed order **-1.25** on `Nu_avg_hot` against the published **+1.94**, five of six orders negative, the sixth (`V_star_max`) non-monotone so `richardson()` correctly refuses to extrapolate, and per-quantity deviations of **-5.811 %** on `Nu_max_hot`, **+11.040 %** on `Nu_min_hot` and **-11.691 %** on the stratification slope. **A reader of that table would conclude the case DIVERGES under refinement.** It does not; the leg was unconverged, at initial residuals 8.71e-05 / 9.39e-05 / 5.28e-05 / 6.71e-05 against 2.36e-08 / 2.77e-08 / 2.51e-08 / 3.44e-08 after the continuation. **The rung's NUMBERS were never in doubt** -- applying the recorded edit (`startFrom latestTime`, `endTime 16000`, `writeInterval 2000`, both `controlDict` versions `cmp`-proved byte-identical to the published ones before launch) brought them back exactly. **What is in doubt is the rung's executability without an undocumented human step**, which is what a run tree exists to remove. Proved by execution 2026-08-18 during the D403 re-run, at `verification/runs/F14-cooling-ladder/K0b_D403_rerun/K0b_m128`. **NOT REPAIRED HERE, deliberately: a re-run that edits the instrument it is grading has graded the edit.** To settle: encode the continuation in `build_and_run.sh` -- a residual-based loop or an explicit second stage with its own `controlDict` -- so that the script alone reproduces the published leg, and re-run the rung once against the amended script to prove it. Owner: fleet. **OPEN.** **CLOSED 2026-08-18 BY THE REPAIR THIS ROW ASKED FOR, AND THE PROOF IS ONE RE-RUN OF THE TWO DOCUMENTED COMMANDS FROM A CLEAN START.** Pre-registered at `211d401e` BEFORE the first solve (`K0b_D406_REPAIR_PREREGISTRATION.md`): the change declared file by file, the predictions fixed, the thresholds fixed, and the cost estimate written down because the authorisation clause -- *"less than $25 in your estimated computations"* -- makes the estimate an instrument. **THE CONTINUATION IS NOW A STEP OF THE SCRIPT.** `build_and_run.sh` gained an explicit second stage entered ONLY by a leg that did not stop itself on `residualControl`, and the stopping reason is READ from the solver's own `SIMPLE solution converged in N iterations` line rather than inferred from the time directories, which cannot distinguish the two cases, or from a single residual, which is D407's shape. **THE CLOBBER IS REMOVED AT ITS CAUSE RATHER THAN WORKED AROUND:** the stage-2 dictionary is DERIVED by the script from the source case's own `controlDict` on every invocation -- `startFrom latestTime`, `endTime 16000`, `writeInterval 2000` and a guard that the two dictionaries with those three key lines removed are identical -- so nothing hand-edited has to survive anything and there is nothing left for the copy step at `:69` to destroy. `system/controlDict.4000` is still written and is now an OUTPUT of the script rather than an input to it, preserved immediately before the rewrite and `cmp`-proved against the source case's own dictionary at that moment. The script also ended in a bare `wait`, which returns 0 however its jobs ended; it now waits per PID and checks each status, because a continuation that never ran would otherwise have left the script exiting 0 with a half-built rung behind it -- the same class of silence this row is about. **PROVED BY ONE RE-RUN OF `bash build_and_run.sh` THEN `python3 analyse_k0b_mesh.py`, UNMODIFIED, FROM A CLEAN START**, into a DISTINCT tree `verification/runs/F14-cooling-ladder/K0b_D406_repair/` because the rung holds 36 tracked files and the script opens each leg with `rm -rf`. Both copies were proved byte-identical to the committed files by `cmp` exit 0 AND by hash -- `build_and_run.sh` `8f98d8ab`, `analyse_k0b_mesh.py` `3876b8a4`, the latter being HEAD's own blob for that path, which was not edited at all. **THE SCRIPT'S OWN STDOUT IS THE ANSWER TO THIS ROW:** *"K0b_m32 stopped itself on residualControl; NOT continued"* and *"K0b_m128 continued to t = 16000, wall 564.77 s"*. **EVERY DEVIATION IS ZERO. 39 of 39 graded quantities BIT-IDENTICAL across three legs** (13 each), **551 of 551 `Nu_history_by_time` values bit-identical**, **48 of 48 Richardson leaves bit-identical** -- `Nu_avg_hot` 4.528816741169209 against the published 4.528816741169209 where the UNREPAIRED script produced 4.3254648850, observed order 1.9404946505323346 against the published +1.94 where the unrepaired script produced -1.25. There is no percentage to quote because there is no difference to express as one. Three reproductions that are integers and cannot be hit by accident: the 32x32 leg's stopping iteration **1386**, the fine leg's analysed time **16000**, and the written-time counts **139 / 6 / 406** -- the 406 being 400 writes at `writeInterval 10` plus SIX at `writeInterval 2000`, so a leg that had not been continued would carry 400 and one continued at the wrong interval would not carry six. **THE DERIVED CONTINUATION DICTIONARY IS BYTE-IDENTICAL TO THE ONE A HUMAN WROTE:** `cmp` exit 0 against the published `K0b_m128/system/controlDict`, and the preserved `controlDict.4000` `cmp` exit 0 against BOTH the published fingerprint and the archive case's own `system/controlDict`. That was also proved OFFLINE BEFORE THE FIRST SOLVE and recorded as such in the pre-registration, because it is a static check and pretending otherwise would have been dishonest. 20 artifact checks in `check_artifacts.sh`, exit codes captured directly and never through a pipe, all 20 as predicted. The committed `analyse_k0b_mesh.py` run end to end UNMODIFIED, exit 0, differed from the published JSON in **11 leaves of 674: 8 wall-clock and 3 `case` path strings, ZERO PHYSICS LEAVES** -- the same count and the same two classes the D403 re-run reported on a different tree. **Cost: 12.701 core-minutes / $0.0109 actual against a pre-registered 17.0 / $0.0145** at the measured $0.0513 per core-hour -- **25.3 % under its own estimate and 0.043 % of the authorisation**, hard stop 45.0 core-minutes never approached. Both a wall-clock and a solver-CPU basis are recorded because two foreign `buoyantBoussinesqSimpleFoam` processes ran at 99.5 % throughout (pids 2396479 and 2396481, resolved by `readlink /proc/<pid>/exe` and separated from this run's pid 2517713 by **cwd**, which a peer cannot share; nothing was killed); the two bases differ by **0.30 %** here against the 1.5 % the D403 lane measured under a load average of 12.83. **The committed archive case was NOT written to: 88 of its 89 files md5-identical before and after, the 89th `log.cellCentres`, an untracked OpenFOAM log the analysis regenerates by design, and `git status` on that tree returned 0 lines.** Record: `docs/campaigns/F14-cooling-ladder/K0b_D406_REPAIR_RESULTS.md`. **D407 IS UNCHANGED BY THIS REPAIR AND STAYS OPEN**, which was registered as prediction P-F before the solve so it could not be claimed either way afterwards: across the ladder exactly one leg still met its own `residualControl`, the 32x32; the continued fine leg goes from 8714x above the U target to 2.4x above it and meets only `p_rgh`. **One new row leaves this one rather than being absorbed into it: D410**, the `rm -rf` that deletes 36 tracked files when the documented command is run where the README documents it. **CLOSED.** |
| D407 | **THE K0b 64x64 LEG'S CONVERGENCE EVIDENCE IS ONE RESIDUAL OF FOUR, IT IS THE SMALLEST OF THE FOUR, AND IT IS QUOTED AGAINST NO TARGET.** `analyse_k0b_mesh.py:264-265` gives that leg's convergence warrant as *"the final initial residual of 9.6e-08 in its own log"*, offered in place of a drift statement because the committed case wrote at t=500 and then not again until t=4000. **9.6e-08 is the T residual. The case's own `residualControl` asks 1e-08 of T, so the quoted number stands 9.6x ABOVE the target it is offered as evidence of meeting**, and the other three are worse or unmentioned: Ux 1.510e-07 and Uy 1.657e-07 against a 1e-08 U target, **16x above**, and p_rgh 1.477e-07 against 1e-07. **The leg did not terminate by `residualControl`; it ran out at `endTime 4000`.** The same holds one rung up: the 128x128 leg at t=16000 reads 2.36e-08 / 2.77e-08 / 2.51e-08 / 3.44e-08 and also stopped at `endTime`, meeting only the p_rgh target. Across the whole three-mesh ladder **exactly one leg met its own case's `residualControl` -- the 32x32, which stopped itself at 1386 iterations.** **THIS IS RECORDED, NOT GATED, AND THE DISTINCTION IS THE POINT:** the residual shortfall is small against the signal the legs were used to measure -- the 64->128 change is 0.552 % while the 64x64 leg's last 3500 iterations moved `Nu_avg` by 14.35 % -- so the mesh-sensitivity result is undisturbed and the re-run graded it PASS. What is disturbed is a REPORTING habit: quoting the best of four residuals, without its target, as a convergence claim is the shape that reads as evidence and is not one, and it is one line of code away from quoting all four. **Registered BEFORE the re-run**, in `K0b_D403_RERUN_PREREGISTRATION.md` section 5 at `a157ccba`, from a reading of the committed log, precisely so it could not afterwards be presented as something the re-run discovered. To settle: report all four final initial residuals beside their `residualControl` targets, and state whether the run terminated by `residualControl` or by `endTime` -- both are already in the log the module reads. Owner: fleet. **OPEN.** |
| D408 | **THE PAPER-LIBRARY MOVE RE-POINTED NOTHING, AND THE THREE DEAD PAPER PATHS IN THIS FILE ARE DISCLOSED HERE RATHER THAN EDITED ABOVE.** `5c0d2483` (2026-08-18 17:14:23) refiled 87 paths under `docs/papers/` into topic subdirectories and renamed almost all of them, and changed **0 files outside `docs/papers/`** (re-derived with `git show --name-only --format='' 5c0d2483 | grep -v '^docs/papers/'`). **26 distinct dead paths were left standing in 40 tracked files, 74 occurrences.** Repaired 2026-08-18: 17 live pointers repointed in place -- 6 Python files, `research/agenda/docket.json`, `verification/campaign/F6d_random_matrix_uq.json`, and three routing sentences in maintained reference documents -- and **57 historical statements left byte-identical** under dated forwarding notes appended below them, because each records where a file was fetched to on a stated date, with its hash, and rewriting one would have made the record resolve and lie. **Three of the 57 are rows in THIS file and W-4 forbids editing them**: `D12` cites `docs/papers/roshko_1954_naca_tr_1191.txt`, now `docs/papers/turbulence_models/roshko_1954_naca_tr_1191.txt`; `D139` cites `docs/papers/Paper3.pdf`, now `docs/papers/verification_validation/oberkampf_roy_2011_verification_validation.pdf`; `D359` cites `docs/papers/wibron_ljung_lundstrom_2018_en11030644.pdf`, now `docs/papers/data_center_indoor_airflow/wibron_ljung_lundstrom_2018_en11030644.pdf`. **Those three rows were not touched. This row is the forwarding.** Note that `Paper3.pdf` was RENAMED as well as moved, so no name-based map would have found it -- the whole pairing was derived by **git blob identity**, matching each old path's blob hash to the path carrying the identical hash, and every destination was then confirmed on the filesystem at `4323d7e3` rather than taken from `5c0d2483`'s destination, because `4323d7e3` lowercased two of the new names afterwards. **Two frozen labelled samples could not take a forwarding note in prose and took a machine-readable one**: `sdk/tests/fixtures/absolute_claims_labelled{,_second_instance}.json` carry `path` and `lineno` inside records drawn at a stated commit, and `path` is passed into `classify_unit()` as classification context, so editing a record would have moved the F1 measurement those files exist to hold; a top-level `paper_path_forwarding` key was added and no record was touched. **One executable defect was real and one was not**, and the difference is the finding: `cases/dafoam/f6d_random_matrix_uq/{make_campaign_json,rmt_sampler}.py` name the dead path only in a docstring and in a JSON value they emit -- **neither opens it, and neither run was ever going to fail** -- while `sdk/tests/test_pdf_surfaces.py:37` listed it in `CLEAN_PDFS`, which is consumed by `assertNotIn(rel, flagged)`: a dead path is **never** in the findings set, so that assertion had silently become vacuous and the test went on passing while checking nothing. **A broken string in a docstring is loud and harmless; a broken string in a negative assertion is silent and disarms the test.** **Found at:** citation-repair pass, 2026-08-18, after the K2c lane's extraction command died with `FileNotFoundError` on one of the 26 (`K2c_DIGITIZATION_ADDENDUM.md` section 11.6). **What settles the open half:** `scripts/check_paper_citations.py` was added and re-derives the pairing from git on every run, but it covers `docs/papers/` only; the same sweep has never been run for any other move batch's destinations, and D404's eight assembled-path residues say the population is not empty. Owner: fleet. **The repair is CLOSED; the generalisation to other move batches is OPEN.** |
| D409 | **THE REPAIR THAT TAUGHT `check_docket_reconciliation.py` TO SEE A DUPLICATE ID SILENTLY RETARGETED ONE OF ITS OWN MUTATIONS ONTO THE BRANCH IT ADDED, SO MUTATION COVERAGE FELL FROM 9 TO 8 UNDER AN UNCHANGED EXIT CODE.** **The defect that prompted it, and it is closed.** `reconcile()` verdicted on `set(committed_ids)` against `set(worktree_ids)`, and a set cannot hold a duplicate, so a worktree row reusing a committed ID added no member and the symmetric difference stayed empty. **A lone duplicate was sufficient; nothing had to cancel it** -- the row counts were computed, printed, and annotated *"counts are diagnostic only -- the verdict is over ID SETS"*, so the instrument held the disagreement and declined to act on it. Measured before the repair by calling `reconcile()` directly on a three-row committed text against a four-row worktree text whose fourth row repeated `D406`: `parse_ids` returned `['D405','D406','D407','D406']`, `head_only: []`, `worktree_only: []`, counts `3` and `4`, **`verdict: PASS`, `exit_code: 0`**. **CLOSED at `97060a0e` by a peer lane, verified here by execution and not from its report:** the same plant now returns `verdict: FAIL`, `exit_code: 2`, `worktree_dupes: ['D406']`. **THE RESIDUAL THAT REPAIR CREATED, measured on both sides rather than inferred.** `scripts/mutation_harness_docket_reconciliation.py` scored **9 killed, 1 survived** against the pre-repair subject (`c9577519`) and scores **8 killed, 2 survived** against the repaired one. **Both runs exit 1**, because `M8-pattern-not-published` was already surviving with a missing anchor, so nothing in the exit status moved and the loss is invisible to any caller that reads only the code. **Mechanism, pinned by locating the anchor on both sides.** `M2-unlanded-downgraded` substitutes the literal `verdict, code = "FAIL", EXIT_FAIL_UNLANDED` for its `EXIT_FAIL_WRITEBACK_OWED` form. At `c9577519` that literal occurred **once**, at line 234, guarded by `elif worktree_only:` -- the unlanded branch the mutation is named for -- and three tests killed it. At `97060a0e` it occurs **twice**, at lines 253 and 255, because the new duplicate branch was written with the identical body; the substitution takes the **first**, now guarded by `elif committed_dupes or worktree_dupes:`, and leaves the unlanded branch untouched. **No test plants a duplicate, so nothing observes the mutation and M2 survives while still applying cleanly.** **The general shape, and it is worth more than this instance: a mutation anchored to a source literal RETARGETS SILENTLY when a copy of that literal is inserted above it.** 'The mutation applied' and 'the mutation applied where it was aimed' are different facts, and this harness reports only the first -- it distinguishes `ANCHOR NOT FOUND` from applied, and has no concept of an anchor that matched the wrong site. A survivor then reads as a missing test when the missing thing is a correct target. **A second gap, in the module's own controls.** `run_controls()` at `:181-208` remains a RECOGNITION control over the ID PATTERN: five planted decoration forms and one negative, **and no planted duplicate**. The four scenario controls the repair reports were run in a scratch repository at commit time and are not in the module's ledger, so the new arm carries no standing control of its own. **A control over the parser is not a control over the verdict.** **Found at:** citation-repair pass, 2026-08-18, after this pass's own row was written as `D406` while the K0b lane's `D406` and `D407` had already landed at `349f065a`. The collision was found by reading `git show HEAD:docs/DOCKET.md` by hand. The K0b lane preserved this pass's text byte-for-byte rather than renumbering it, which is W-4 correct, and this pass moved its own row to `D408`. **What settles it, three things and none of them is the reconciliation fix, which is done:** (1) anchor `M2` to a line that is unique in the subject -- the `elif worktree_only:` guard together with its body -- and make the harness REFUSE a mutation whose anchor matches more than once, since an ambiguous anchor is a silent retarget waiting to happen; (2) add a mutation for the duplicate branch, so the arm added at `97060a0e` is covered rather than merely present; (3) plant a duplicate in `run_controls()` so the module's own ledger exercises the verdict and not only the pattern. **The count is the thing to watch, not the exit code**: this harness's exit status was already 1 and stayed 1 while its coverage fell, which is the same fail-quiet shape one level up. Owner: fleet. **The reconciliation defect is CLOSED; all three residuals are OPEN.** |
| D410 | **`build_and_run.sh` DELETES 36 TRACKED FILES WHEN IT IS RUN WHERE ITS OWN README DOCUMENTS IT, AND THE ONLY THING STANDING BETWEEN A READER AND THAT IS A PARAGRAPH OF PROSE -- WHICH IS THE SHAPE D406 WAS ABOUT.** `verification/runs/F14-cooling-ladder/K0b_mesh_sensitivity/build_and_run.sh` opens each leg with `rm -rf "$dst"`. The rung's two legs hold **36 tracked files** (`git ls-files` over `K0b_m32` and `K0b_m128`: 17 and 19) -- both `COST.txt`, `log.blockMesh`, `log.checkMesh`, `log.buoyantBoussinesqSimpleFoam`, `log.buoyantBoussinesqSimpleFoam.continue`, `log.cellCentres`, `system/controlDict.4000` and every dictionary of both legs. **The two commands the README documents, run in the directory the README is in, therefore destroy the published record of the rung before rebuilding half of it.** This is not a prediction: it is the reason the D403 re-run declined to re-run in place (`K0b_D403_RERUN_RESULTS.md` section 3) and the reason the D406 repair run used a distinct tree as well, and the D406 pre-registration fixed that distinct tree in advance at section 4. **The count is measured, not estimated, and it is measured at HEAD**; nothing was executed against the rung to establish it, because executing it is the defect. **What makes this a row rather than a footnote is what the D406 repair did about it: it added a README paragraph saying "Run it somewhere else, not here."** D406's whole finding was that a required step existed only as prose in a README; answering an adjacent hazard in the same file with more prose is the same shape one hazard along, and saying so here is cheaper than discovering it again. **NOT REPAIRED, and the reason is the instrument rather than the difficulty.** The D406 repair's change set was declared file by file in section 3 of its pre-registration, committed at `211d401e` before the first solve; a refusal guard is not in it. Adding an undeclared behaviour change after the fact would spend the pre-registration this task exists to honour, and the proof run could not have exercised the guard anyway -- it deliberately ran nowhere near the tracked tree. **To settle, and the choice is a workflow decision rather than a bug fix, which is the second reason it was not taken unilaterally:** either (a) make the script REFUSE when its destination holds tracked files, naming the scratch-tree procedure in the refusal, which makes the documented in-place command fail by design; or (b) move the published legs out of the script's destination path so that `rm -rf` cannot reach them; or (c) decide that a run tree is regenerable and that deleting it is correct, and say so in the README instead of warning against it. **(a) and (c) are opposites and the repository should not hold both**, which is exactly why this is filed for a decision rather than patched. Whichever is chosen, it should be pre-registered and proved by a run that shows the guard FIRING, not by a run that avoids it. Found repairing D406, 2026-08-18. Owner: fleet. **OPEN.** |
