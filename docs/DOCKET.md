# The docket — findings that are real but are not this rung's

Created 2026-08-11 by the chief supervisor, as the queue **R-CONVERGE** requires.

A rung that absorbs every new finding never closes. A rung that DROPS them is worse.
This file is the third option: a finding made outside a rung's declared scope is written
down here, with the rung it surfaced in and what would settle it, and the rung closes.

**This is a queue, not an archive.** An item leaves it by being executed or by Katie
ruling it out — never by ageing.

Rules that govern entries here:

- Every item names **where it was found**, **what it would take to settle**, and **who
  owns it** (fleet / chief / Katie).
- Items filed under **R-DEPTH** (depth-3 meta-work) stay filed unless a depth-2 finding
  falsified something published. They are marked `[R-DEPTH — filed, not executed]`.
- Items filed under **R-VALUE** are the residuals of a rung that closed as PASS WITH
  RESIDUALS. They are marked with the rung.
- No item here is a claim about the world. Where an item asserts something, it carries
  its evidence or it says it has none yet.
- **IDs ARE APPEND-ONLY AND STABLE. NOTHING IS EVER RENUMBERED.** (Katie, W-4,
  2026-08-11.) A new entry takes a **fresh** ID — check the highest in use first. If two
  writers collide on a number, **both keep their text and the later one takes the next free
  ID**; the earlier one is not moved.

  **Renumbering is the defect, not the fix, and I learned that by doing it twice.** Three
  collisions happened in this file on 2026-08-11 — all from concurrent sessions that could
  not see each other — and I resolved two of them by renumbering: a second `D8` became
  `D8b`, and a grader's `D5`/`D6` became `D19`/`D20`. **Every reference written before that
  moment then pointed at the wrong row.** One was found the same day, in a document that had
  correctly refuted a claim and then rotted while sitting there being right. **An ID is a
  reference target; moving it breaks every citation you cannot see.** The collision costs one
  duplicate number. The renumbering costs every pointer in the corpus.

  **Superseded IDs are struck in place and kept, never reused** — a retired number stays
  retired so an old citation resolves to a withdrawal rather than to somebody else's finding.

  **ID map for the two renumberings already committed**, so references written before
  2026-08-11 resolve rather than rot. This map is permanent; it is not a cleanup step.

  | as originally cited | now | what it is |
  |---|---|---|
  | `D8` (the plateau-balance correction) | **`D8b`** | WITHDRAWN — refuted by D9 |
  | `D8` (B2's seven candidates) | `D8` — unmoved | the original holder of the number |
  | `D5` (worktree staleness, from grade round 5) | **`D19`** | filed by the V16 grader |
  | `D6` (no precision figure, from grade round 5) | **`D20`** | filed by the V16 grader |

---

## A. Katie's — decisions the fleet cannot make

| # | Item | Where found | What settles it |
|---|------|-------------|-----------------|
| A1 | **8 transcript hits before filming.** Three acts narrate the cache, one names the solver binary, four carry internal doc paths. | Transcript sweep, 2026-08-11 | Katie's call on each: cut, reword, or accept on camera. |
| A2 | **40 core-min hump authorisation** — M1 offline + M2 negative control. Drawn from a 365 core-min list I recommend against buying as a block. | C1 lever audit | Katie authorises compute, or does not. Nothing runs meanwhile. |
| A3 | **`LAPTOP_SHOOT.md` still carries the withdrawn board claim.** It is Katie's file; the fleet does not edit it. | V15 round 5 reconciliation | Katie edits, or rules the claim stands. |
| A4 | **The auto-stop causal question.** A real code defect plus a real symptom is not a cause. Katie/Sanaa own the box's power control. | `auto-stop-ignores-control-room` | The fleet exhibits wiring, or the claim stays a candidate. |

## B. Machinery — each defect class becomes an executable check

A lesson without a check is a lesson that will recur. Owner: fleet.

> **CLAIM AN ITEM BEFORE YOU DISPATCH IT — write your session into the Status column first.**
> Added 2026-08-11 after two chief sessions independently dispatched the *same four briefs*
> off the same directive block. The B1 collision is the proof: two agents ran the identical
> sweep brief, and one's `Write` landed on the other's already-committed
> `docs/SWEEP_REFRAME_AUDIT.md`, replacing a 335-line document with a 281-line one and
> dropping two sections. It survived only because that agent went looking, compared both,
> judged the other strictly more complete, and restored it with `git checkout --`.
>
> **That is L-77 with commits instead of a scratchpad, and the failure is the dispatcher's,
> not the agent's.** This file is the one surface both sessions already read. One edit before
> dispatch makes the collision visible while it is still cheap — the agents do not exist yet.

| # | Item | Lesson | Status / owning session |
|---|------|--------|--------|
| B1 | **Sanctioned `sweep()` helper** — names its frame, filter and commit in its own output; cannot silently exclude; test asserts it sees a planted file inside an ignored path. Plus: re-run every standing sweep whose conclusion mattered; mark any that cannot be re-derived as **UNFRAMED**. | L-75 | **LANDED** — `85cc1078` + `fc8e1812` (helper, 4 named frames, 3 verdicts, `FrameMismatch` on cross-frame comparison; 24 tests). Part (c) at `8ebe2b8e`/`8623b8ca`: **16 published sweeps re-run, 4 moved, 3 UNFRAMED.** *Ran twice — see the claim rule above.* |
| B2 | **Fail-false gates.** Every gate and parser answers "did the check run?" before "what did it find?", with a third verdict for unknown. Sweep all gates. | The V16 skip-is-not-agreement defect | `be42e5d4` landed; **re-running its CLEARED sites** under the cache guard — injection is a mutation plus a re-import, so a stale cache silently skips the injection and the site reads as safe (see D1a). Reporting three groups, not two. |
| B3 | **Absolutes.** No absolute claim ("never", "always", "cannot") ships in a docstring, comment, report or camera surface without an executed test named beside it. Surface list derived mechanically, never enumerated. | L-76 | **MEASURED, AND NOT USABLE AS A GATE YET — see B3a/B3b.** The FP rate was published as required, and it is the reason this row does not close. |
| B3a | **74%.** A checker was built (`scripts/check_absolutes.py`, `8a8b5392`) and its author measured its own false-positive rate against a 75-record hand-labelled sample: **37 of 50 flagged records were legitimate**, false-negative rate 8%. Not a usable gate — and publishing the number rather than a clean-looking output is the deliverable working as designed. Sweep as it stands: **7,040 unbacked absolutes across 685 files**, from 18,145 absolutes in 44,206 prose units, 1,077 of 1,078 in-frame files read (1 binary skipped, 0 unreadable). Frame: `git ls-files` filtered to `.py .md .html .tex .sh .js .txt`, 4 MB cap. The seven false-positive classes are labelled per-record in the committed fixture: past-tense reports with evidence cited, counterfactual premises, bounded quantifiers enumerated a few words past the gate's window, table rows and headings, stated blind spots, claims inside a `test_*` docstring where the enclosing test **is** the check, and third-party text. | B3, 2026-08-11 | Round-2 gates are drafted and were **deliberately not shipped unmeasured**. Each must be checked against a positive-control set of the six real specimens pulled from git history (`c05a03e0`, `db096bb7`, `fd855017`, `V15_ROUND5_NUMBER_RECONCILIATION.md:96/168`) before it is trusted — otherwise gating tunes to a pretty number. | fleet |
| B3b | **The keyword premise has a measured floor, and it is this rule's problem rather than the checker's.** **Two of the six real specimens carry no keyword from B3's list at all** — *"stops being able to go stale"* and *"the three `BaseException`s propagate exactly as documented"* contain no never/always/cannot/every/all/none. So **any keyword-family checker has a ~33% structural miss on the known specimen set**, before tuning. Related: `must` must NOT be treated as normative and exempted — a deontic modal on an *inanimate* subject (*"one surface must never be able to end the audit"*, repaired at `fd855017`) is a claim wearing a modal; exempt only on an imperative opening, an agentive subject, or a rules surface. | B3, 2026-08-11 | Either the rule widens past a word list, or the audit document **states the ~33% miss as a declared blind spot**. A checker that silently misses a third of its own founding examples while reporting 7,040 hits is the fail-open shape in a new costume. | fleet |
| B3c | **A committed checker cites tests that do not exist.** `scripts/check_absolutes.py` shipped at `8a8b5392` without `sdk/tests/test_absolute_claims.py`, and its own docstring names those tests. **By the checker's own rule that is `CITES_MISSING_CHECK`** — its author's stated reason being that a name resolving to nothing reads as backing to every human who sees it. The instrument fails itself on its first surface. | B3 stand-down, 2026-08-11 | Write the tests or strike the citations. Do not leave it citing air. | fleet |
| B4 | **Quoted generated numbers.** A generated figure quoted into prose carries its commit and the moment it was taken, or it is regenerated at read time. Prefer regeneration; forbid bare copies. | L-79 | Open |
| B5 | **Frames.** Every count carries frame + filter + commit. A number whose frame nobody can state is worse than no number. | L-75 | Open |
| B6 | **Shipped-helper blindness.** A check written with the same helpers as the thing it checks proves only transcription fidelity. Any load-bearing verification names its EXTERNAL referent, or declares it has none. | L-74 | **EXECUTED 2026-08-11 by session `64b13819` — `docs/EXTERNAL_REFERENT_AUDIT.md`.** Classification only; nothing repaired. Both controls run first and reported first, in both directions. Population derived mechanically: 1,549 verification sections over 297 of 367 tracked markdown files. **The finding is that the corpus is in better shape than L-74 implies** — every clean instance of the failure mode in the gradient work had already been found and retracted by this lab — and that the live defect class is **transmission loss**, not self-referential checking: of seven findings, four have a correct referent declaration in the canon that does not travel with the number (a fifth arguably), and only two are gaps where no correct declaration exists anywhere. Residuals D12-D18; D17 is the one that would close the other five and is Katie's/chief's ruling. The last untouched item in this block. Tonight's audits keep landing on its shape from the side — the sweep helper measured its own frames, the absolutes checker measured its own false-positive rate, the dead-lever audit caught its own regex — and each time the *external* referent was what made the result credible. This item asks the question deliberately instead of by luck. |
| B7 | **Lever activity.** Configured is not active. Log-verified or it is not evidence. | L-40 class | **DELIVERED 2026-08-11 by session `64b13819` — `docs/DEAD_LEVER_AUDIT.md` (`81b40251`).** **The hump is CLEAN: zero dead levers, primal or adjoint, 17 lever/conclusion pairs proven active from logs**, reproducing C1-A's verdict from a vocabulary derived from the configuration grammar rather than from the prose. One INACTIVE specimen found off the hump (D10), one headline ACTIVE-UNPROVEN (`jacMatReOrdering` at `rcm`: 257 requests, 0 readbacks — D10), one records defect (S1 rung 4 published as a one-change arm, its own echo shows three levers moved), and one correction to `INSTRUMENT_INTEGRITY_2026-08-11.md` §1.1 whose headline verdict is unaffected. Two arms priced at 8 and 40 core-min, neither authorised, zero compute spent. (Katie's §4 item 4, the C1 dead-lever audit incl. every hump-adjoint conclusion.) L-40 is the lab's most-cited lesson — **104 citations** across the corpus, measured, well clear of the next at 58 — so this is the discipline the most conclusions lean on and the least mechanised. The naval line already produced one instance at capability scale: **zero tracked files reference any wave machinery**, against 1,825 mentioning `alpha.water` by the identical pipeline. Configured, shipped, linked into three solvers — and never once run. |

## C. Memory as an audited system

| # | Item | Owner |
|---|------|-------|
| C1 | **Cold-start test, monthly.** A fresh agent with no context reaches correct current state from the durable files alone. Everything it gets wrong is a memory defect fixed **in the files**, never by explaining. **First run IN FLIGHT 2026-08-11** — dispatched with exactly the one instruction §6.3 specifies. | fleet |
| C2 | ~~`docs/MEMORY_ARCHITECTURE.md`~~ — **ALREADY EXISTS**, v1.0 dated 2026-08-10, and it carries all four required parts plus the test spec in its §6. *I filed this as open without opening the docs directory; the check took one `ls`. Left visible rather than deleted, because a docket whose first entry was a thing already done is exactly the defect the docket exists to catch.* | — |
| C2a | **The §6.4 answer key is itself a set of quoted generated numbers** (B4's defect class): it fixes the lesson corpus at L-53 when `LESSONS.md` now runs to L-79, and the fleet-death count at eight. Scoring a fresh agent against a stale key marks correct answers wrong. The key needs regeneration-at-read-time or a stamp on every row. | chief |
| C3 | **One home per fact.** Cross-reference; never copy. Copies drift (L-79). Supersession in place, dated — never a silent edit. | standing |

## D. Rung residuals

Filled as rungs close under R-VALUE. A rung closing here does not mean these are small;
it means they are not what that rung declared it would settle.

| # | Item | Where found | What settles it | Owner |
|---|------|-------------|-----------------|-------|
| D1 | **A mutation test can report the exact opposite of the truth, via stale bytecode.** Verifying V16's E3 recompute, an equal-length source mutation (`(5, 5,` → `(4, 5,`) plus a restore left a stale `scripts/__pycache__/self_audit.cpython-312.pyc`: Python's source-based invalidation compares `(mtime, size)`, and `cp` back gave the same size. For three consecutive runs `sa._PLACE_REACH_B` was `(4, 5)` at run time while the file on disk read `(5, 5)`. The clean control **failed** and the mutated case **passed** — a perfectly inverted mutation matrix, which a less suspicious reading would have written up as "the recompute is dead". Re-run with the cache cleared before every step, the matrix is correct in all four cells. | V16 grade-four close-out, 2026-08-11 | A mutation-test harness that **clears `__pycache__` between every cell** — **NOT `PYTHONDONTWRITEBYTECODE=1`, see the correction below** — and that asserts the clean control and the mutated case in the *same* run so an inversion cannot look like a pass. Until it exists, every mutation result in this lab is only as good as whether its author happened to clear the cache. |
| **D1a** | **[CORRECTION 2026-08-11, chief — the fix D1 first recommended DOES NOT WORK, and I propagated it to three agents before testing it.]** `PYTHONDONTWRITEBYTECODE=1` stops Python **writing** a `.pyc`; it does nothing about **reading** a stale one that already exists — and one always exists, because the module was imported before the mutation. Reproduced minimally: with a stale pyc present and an equal-length mutation, the mutated cell returns the **clean** value while the file on disk holds the mutation. **A wrong fix is worse than no fix: it converts an unverified result into a falsely verified one.** Executed mitigation table — deleting `__pycache__` before every cell: **WORKS**; `PYTHONPYCACHEPREFIX` to a fresh dir per cell: **WORKS**; a mutation that changes byte length: works, but that is a property of the edit, not a guard; `sleep` **between the prime and the write**: works; `sleep` between the write and the run: **FAILS**, the mtime was already stamped; `PYTHONDONTWRITEBYTECODE=1`: **FAILS**. **Mechanism:** Python's timestamp invalidation compares `int(st_mtime)` — *whole seconds* — plus size, so a prime-mutate-run-restore-run cycle completing inside one second with a length-preserving edit matches both halves of the key. This is why the defect surfaces for scripted agents and essentially never for someone editing by hand. | Chief, on D1, 2026-08-11 | Nothing further — executed. The harness in D1 must be built against cache **clearing**; a harness built on the flag would certify the same false passes. | fleet | fleet |
| D2 | **Two published surfaces carry an underived count: "four" digit-form linear-algebra ranks.** `campaign/V16_GRADE.md` says *"swept over every tracked UTF-8 surface … four genuine linear-algebra ranks written as digits"*; `docs/PRODUCT_LIST.md:3765` repeats *"It is not — four are digits"*. Re-run mechanically (`git ls-files` piped to `/usr/bin/grep`, **not** this shell's `grep`, which is `ugrep --ignore-files`) the number is not four — it moves with whether the guard's own source, its tests, and the audit records that quote them are inside the filter. Neither surface states a filter or a commit, so neither can be reproduced. The count is not load-bearing — the exclusion was rewritten on the *shape* of the two senses, not on how many instances exist — which is why V16 dropped the figure from `self_audit.py` rather than restating it. The two published copies still carry it. | V16 grade-four close-out, 2026-08-11 | Either re-derive both figures with frame + filter + commit (B5/L-75/L-72), or replace them with the named paths, which do not drift. | fleet |
| D3 | **`docs/INSTRUMENT_INTEGRITY_LEDGER.md` holds the guard's two standing faults, and one of them is structurally unclearable.** Every run of `check_board_placement_words` reports WARN with exactly these two: a rule-A ordinal-three expression bound to the entrant the published board places second (a *quotation* of defect A, which the adjudication clause does not clear because the correcting sentence is too far away) and a rule-B margin-over-a-position-word comparison naming no holder (a quotation of defect C — and **rule B has no adjudication clause and cannot be given one**, since there is no correct form of a position word to sit beside a wrong one). *[DEQUOTED 2026-08-11 by the chief. Both faults were originally written here **verbatim**, and the V16 author round then found that two of the guard's four remaining live faults were **in this entry** — the docket row documenting the unclearable-quotation problem had become an instance of it. Asked directly, the guard bound the quoted ordinal to the entrant and flagged the quoted margin sentence, exactly as it should. The remedy is D3's own: state the shape, do not reproduce the string. This is the markdown analogue of the split-token convention `test_rank_claim_surfaces.py` already uses, and it is the third time tonight a record has been caught demonstrating the defect it describes.]* This is the check's permanent non-green floor: no future round can reach PASS while these stand, so a reader cannot tell this WARN from a new one. | V16 rounds 1–4; measured again at close-out, 2026-08-11 | Assemble the ledger's two quotations from split tokens the way `sdk/tests/test_rank_claim_surfaces.py` and `V16_AUTHOR_HELDOUT_SET.py` already assemble theirs — the convention exists and this surface predates it — **or** record an explicit dated accepted-exception so the WARN is legible as "the two known quotations, nothing new". | fleet |
| D4 | **The fixture-assembly convention is enforced by review alone, and review failed twice in one night.** `test_rank_claim_surfaces.py` states in its own header that a tracked file must not spell a wrong placement in full. The killed agent's inherited diff broke it, and so did my first draft of the repair: between us we put **eight** new real rule-A faults about a live entrant into the guard's own test file (measured: WARN 2 → WARN 10, all eight new ones in that one file). The live guard *did* catch it — but only because I ran the audit by hand mid-round. | V16 grade-four close-out, 2026-08-11 | `check_board_placement_words` (or a thin wrapper) run as part of `sdk/tests/`, so adding a literal fault to a tracked file reddens the suite rather than waiting for someone to run the audit. **[R-DEPTH — borderline: this is the guard checking the guard's own test file. Filed, not executed.]** | fleet |
| D5 | **`sdk/workflows/adjoint_optimization.py` swallows the same certificate `unlink` B2 fixed in ten places, but publishes no withdrawal claim** — so by B2's own test (does the swallow reach a *published* verdict?) it is out of that rung, and it was left alone rather than swept in. The stale-page risk is nonetheless real: if the unlink fails and the build then fails, an earlier mission's certificate stays on disk and the act says nothing about it either way. | B2 fail-open sweep, 2026-08-11 | Either route it through `workflows.withdraw_certificate` like its nine siblings, or record the deliberate decision that an act which makes no claim needs none. One line of code; the reason it is filed rather than done is R-CONVERGE, not difficulty. | fleet |
| D6 | **Five acts take the OPPOSITE failure on the same line, and it contradicts a rule those same files state.** `cylinder_vortex_shedding.py`, `diamond_airfoil.py`, `hypersonic_cylinder.py`, `supersonic_cone.py` and `supersonic_wedge.py` withdraw the previous certificate with a bare `if cert_path.exists(): cert_path.unlink()` **outside** the try that wraps certificate building. A `PermissionError` there raises straight out of `main()` — taking down a mission whose result was already computed, in files that state "a certificate must never take down a good mission". Not fail-open, so not B2's defect class; it is the same line failing the other way. These five also still carry the unconditional withdrawal sentence, and they are **the same five** — the set that takes the bare `unlink` is exactly the set that still states the claim. (Frame: `git ls-files 'sdk/workflows/*.py'`, matched on the two source substrings, at `36c533f3`.) | B2 fail-open sweep, 2026-08-11 | Route them through `workflows.withdraw_certificate` too — it returns rather than raising and states which of three outcomes happened, which fixes both directions at once. Then assert in `sdk/tests/` that no act in the package calls `unlink` on a certificate outside that helper. | fleet |
| D7 | **An absolute with no executed test beside it, and this rung produced the counterexample.** **Two** acts — `sdk/workflows/aircraft_optimization.py:2671` and `sdk/workflows/geometry_study.py:2914` — carry the comment *"the previous run's page is withdrawn FIRST and the new page lands by atomic replacement, so a page from an earlier mission **can never** be served after this one completes"*. (Frame: `git ls-files '*.py'` piped to `/usr/bin/grep -ln "from an earlier mission"`, at `36c533f3`, excluding this rung's own test file which quotes the phrase to describe it. My first draft of this entry said **six**, from reading rather than counting, and the count was wrong — recorded here because an underived number in a docket entry about underived numbers is worth leaving visible.) B2's injection exhibits exactly that: refuse the unlink, fail the build, and the earlier mission's page is still there. The code is now fixed to *say so*, but the comment still states the absolute. This is B3's class (no absolute ships without an executed test named beside it), found in B2. | B2 fail-open sweep, 2026-08-11 | Reword to what the code can support — the page is withdrawn *unless the withdrawal is reported as failed* — or name `sdk/tests/test_certificate_withdrawal.py::TheActPublishesWhatHappenedTests` beside it, which is the executed test that bounds the claim. Mechanical sweep, not an enumerated list: B3 owns the derivation. | fleet |
| D8 | **B2's seven CANDIDATES — flagged, plausible, and NOT made to fire.** Each is a swallowing `except` inside a function that publishes something, where the swallow cannot move the status; none was shown to produce a wrong published verdict, so none is a defect and none was fixed. (1) `chief_engineer/head_engineer.py:1084`, an all-failing `line_hook` empties a published live chart with no marker — the step verdict comes from the process exit status, which is why it is only a candidate. (2) `workflows/aircraft_optimization.py:2034`, a malformed finalist result silently leaves a published table with no expected-count beside it. (3–5) `workflows/ahmed_body.py:697`, `geometry_study.py:2262`, `nasa_hump.py:833` — the painted-field copy is swallowed and `announce_field` then publishes `/api/field/<act>/<name>` while the file stayed in the case directory, a URL the acts' own comments say cannot resolve. (6) `workflows/backstep_case.py:925`, an all-unparsed pressure profile publishes as `[]`, indistinguishable from a file of comments; weak, a `len(parts) < 4` guard absorbs realistic truncation. (7) `workflows/tmr_verification.py:438`, `parse_yplus_dat` keeps the last **parseable** row — reproduced: a garbled final row returns the 200-iteration row, published downstream as `max y+`, "the converged state" per its own docstring, with nothing saying a row was dropped. | B2 fail-open sweep, 2026-08-11 | 1–5 need a live solve or a live hook to inject into, which B2 may not launch; 7 needs an owner's ruling on whether the last *complete* row is the right answer, which is a semantics call and not a bug an agent may assert. Full reasoning and injections in `docs/FAIL_OPEN_GATE_AUDIT.md` section 4. | fleet |
| D8b | *[ID CORRECTED 2026-08-11 by the chief. This row was filed as a second **D8** by a concurrent session while B2's candidates already held that number — two writers, one ID, neither aware. Renumbered rather than merged, and the collision is recorded rather than tidied away: it is the L-43/L-63 duplicate-number shape one level up, in the very file built to stop findings being lost. **D9 below refutes this row**, and cites it as "the D8 row above" — read that as D8b.]* **A published plateau-balance triple does not reconcile with the field it describes, by 1.684x.** `dafoam/ladder-b/S1_CBFS_INVERSION_RESULT.md` §4(2) reports the corrupted run's stationary point as `\|g_penalty\|/\|g_QoI\| = 0.998` with `cos(g_QoI, −g_penalty) = 0.9995`, and its own trajectory table gives `‖g‖₂ = 9.011e-06` at eval 16 — which the driver writes as `np.linalg.norm(g)` of the **total** gradient (`S1-cbfs-inversion/invert_lbfgsb.py:117`, read). Those three over-determine `\|g_penalty\|` and imply **2.841e-04**. Computed exactly from the archived field, `\|g_penalty\| = 2·λ_L2·‖β−1‖₂ = 2·1e-4·2.392652 =` **4.785304e-04**. Solving back, `‖g‖₂ = 9.011e-06` at ratio 0.998 needs `cos = 0.999826`, which does not round to 0.9995. **The finding is unaffected** — the plateau *is* a penalty/likelihood cancellation, confirmed independently by the 53x cancellation itself and by rms\|β−1\| = 0.016511 reproducing the published 0.0165 exactly. What is in doubt is the published cosine, and it matters because `s1-regularization-chosen-by-prior-theory-with-a-posterior-on-beta` named that balance as the control a posterior must reproduce. (Frame: `S1-cbfs-inversion/fields_beta.npy`, 21,000 cells, λ_L2 = 1e-4 read from that run's own driver line 25; host-side arithmetic, zero solver compute; repo commit `7d9e4c51`.) | S1-with-priors pre-registration, 2026-08-11 | Either re-derive the cosine and the ratio with their frame stated, or replace both with the exact analytic `\|g_penalty\|`, which is reproducible from a file on disk. The dependent gate has **already been re-based** onto the analytic quantity (`S1_PRIORS_PREREGISTRATION.md` §4 G-P4, §8), so nothing is blocked on this — it is the published sentence that still carries the unreconciled pair. Belongs to that document's owner, per C3. | fleet |
| D9 | **The plateau-balance triple reconciles exactly, to three published figures; the D8 row above mixes two evaluations.** The second D8 row reports the corrupted S1 run's published `\|g_penalty\|/\|g_QoI\| = 0.998` and `cos(g_QoI, −g_penalty) = 0.9995` as unreconcilable with the archived field by **1.684x**, and `S1_PRIORS_PREREGISTRATION.md` §8 re-bases that document's control gate G-P4 onto `\|g_penalty\|` alone on the strength of it. **Executed check: all three published numbers reproduce from disk.** That run archived a *matched* (β, ∇) pair at the plateau — `beta_eval010.npy` with `grad_eval010.npy`, both DV order, written by the same `run_eval` call (`invert_lbfgsb.py:56,64`) — and evals 11–17 agree in J to seven digits, so eval 10 **is** the plateau. Composing the driver's own convention `g_total = λ_QoI·g_raw + 2·λ_L2·(β−1)` (line 114) gives ratio **0.998441**, cos **0.999542**, rms\|β−1\| **0.016502** against the published 0.998 / 0.9995 / 0.0165 — three for three. The identity `‖g_total‖ = \|g_QoI\|·√(1+r²−2rc)` closes to **six digits** (1.450369e-05 measured directly and via the identity), so the triple is internally self-consistent. **The 1.684 factor is produced by combining the plateau-state ratio and cosine with `‖g‖₂ = 9.011e-06`, which the trajectory table reports at eval 16 — a different state.** Near-cancellation is what makes the mix bite: `‖g_total‖` moves 1.45e-05 → 9.01e-06 (1.6x) between eval 10 and eval 16 while r and c move only in the fourth decimal. No gradient was ever needed to be re-derived — one was on disk. (Frame: two `.npy` files under `/home/ubuntu/certonomous-runs/S1-cbfs-inversion/cbfs_inv/`, **outside the repo, invisible to any repo-scoped grep**; 21,000 cells, DV order both, λ_QoI/λ_L2 from that run's pre-registration §2; host-side arithmetic, zero solver compute.) | `S1_WITH_PRIORS_PREREGISTRATION.md` §3 and its dated reconciliation section, 2026-08-11 | The plateau-balance D8 row and `S1_PRIORS_PREREGISTRATION.md` §8 want withdrawing by their author, and G-P4 restoring to the ratio-and-cosine form. `\|g_penalty\| = 2·λ_L2·‖β−1‖` alone is an **identity** that any treatment knowing λ_L2 and β reproduces exactly, so re-basing onto it removes the only part of the control that tests the *balance* — the cosine is the part that carries information about whether the prior pull actually opposed the likelihood gradient. Not edited in place: the entry belongs to its author, per C3. **[CHIEF, 2026-08-11 — the author session has since ended, so "belongs to its author" no longer assigns this to anyone. C3's supersession-by-the-author rule has no owner to name here, and an unowned withdrawal is how a refuted claim stays published. Reassigned: whoever next picks up the S1 line withdraws D8b and `S1_PRIORS_PREREGISTRATION.md` §8, and restores G-P4 to the ratio-and-cosine form. Until that happens, a refuted correction and its dependent gate re-basing are both live in the record — which is the state this docket exists to prevent.]** | fleet — **unowned, see the chief's note** |
| D10 | **DAFoam's `printInfo` prints ASM/ILU levers regardless of which preconditioner is actually built — a second, independent reason for the effective-value echo already recommended.** `A3-stage2-gamg/stage2_gamg.log:864,867,868` print `ASM Overlap: 1`, `Mat ReOrdering: natural` and `ILU PC Fill Level: 0` while that same run's `-ksp_view` at `:884` shows `type: gamg` and the whole file contains **0** `type: asm` and **0** `type: ilu` objects (positive control: `A3-gateB-restoration/gateB.log` returns 1 of each). No conclusion cites those three for the GAMG arm, so nothing reopens. **The filing is the generalisation:** `DEFECT_CANDIDATE_ksp_options_override.md:160-163` recommends an effective-value echo on **override** grounds; this is the same recommendation on **PC-type** grounds, and it fires with no override present. Compounding it, `jacMatReOrdering` has activity proof nowhere in the archive when set to `rcm` — frame: 1,635 files named `*.log` under `certonomous-runs` + `Certonomous`, `/usr/bin/grep -lI`, 2026-08-11 17:24 UTC, repo `11b44cab`: **386** logs print the requested `Mat ReOrdering:`, **257** request `rcm`, **8** carry a `matrix ordering:` readback, and the intersection of the last two is **0**. | B7 dead-lever audit, 2026-08-11 (`docs/DEAD_LEVER_AUDIT.md` §4 I-1, U-1) | An effective-value echo in the DAFoam wrapper, or `-ksp_view` on by default for adjoint runs. Until then the cheapest partial close is priced there as **M-A, ~8 core-min** — two arms of the existing rung-4 hump configuration with `-ksp_view` on, which settles the ordering question for all 257 archived runs at once, not just the hump. **Not authorised; not run.** | fleet |
| D11 | **The hump adjoint family predates the launcher lever echo, so its scheme and relaxation levers are structurally unprovable — with a remedy that already exists and is proven.** Stock `simpleFoam` and DAFoam echo neither `fvSchemes` nor relaxation factors, and every hump run predates the lever echo adopted at `199e9d17` (2026-08-10). No hump conclusion currently cites a scheme, so this is a **latent exposure, not a live gap** — filed rather than actioned because the remedy is known to work (`F5C_STAGE_A_RESULTS.md:84-88` reads `consistent yes;` out of a gzipped archived log, with the echo's `fvSolution` sha256 matching the archived dictionary) and applying it retroactively would need reruns nobody has priced. | B7 dead-lever audit, 2026-08-11 (`docs/DEAD_LEVER_AUDIT.md` §6 item 2) | Nothing, until a hump conclusion cites a scheme or a relaxation factor. At that moment the citing arm needs a re-run under the echo, or the caveat on its face. Recorded so the next hump author finds it before publishing rather than after. | fleet |
| D12 | **A filmed gate names two published authors over a constant the repo attributes to itself.** `NINE_ACT_GATE_TABLE.md:3` grades act 1 `PASS` at 0.77% against *"Strouhal vs Roshko-Williamson correlation"*, reference 0.1590. That number comes from `sdk/workflows/_exact_theory.py:253-260`, `St = 0.198(1 - 19.7/Re)`, whose docstring warrants the constants as *"the exact form this lab's Re=100-180 cylinder family already gated against"*, and `F5a_cylinder_reynolds_ladder.md:73-75` attributes the form to *"this task's gate"* — i.e. to the prompt. **The one form the repo cites to a paper disagrees**: `cylinder_vortex_shedding.py:22-24` gives Roshko (1954), NACA Report 1191, as `St ~ 0.212(1 - 21.2/Re)`. Executed arithmetic at Re=100: used form **0.158994** (measured 0.1578 → **0.75%, PASS**); cited form **0.167056** (→ **5.54%**). Both forms are quoted in the cylinder literature for different Reynolds ranges, so which governs Re=100 is a source question, not a classification one. (Frame: `git ls-files '*.md'` + `git ls-files '*.py'`, host-side arithmetic, zero compute, repo `cf7995ff`.) | B6 external-referent audit, 2026-08-11 (`docs/EXTERNAL_REFERENT_AUDIT.md` §4.1) | Read NACA Report 1191 and record which form it supports at Re=100, then either re-attribute the constants or re-grade the act. Until then the gate is passing against a constant with no cited source. Literature task, zero compute. | fleet |
| D13 | **The Strouhal correlation is duplicated inline, in the file whose sibling forbids exactly that.** `sdk/workflows/mega_batch.py:366` carries `st_ref = 0.198 * (1.0 - 19.7 / reynolds)` while `sdk/workflows/cylinder_vortex_shedding.py:58-62` states the correlation is *"deliberately not duplicated here: two copies of one correlation is how a gate silently drifts"*. Two copies now exist. | B6 external-referent audit, 2026-08-11 (§9) | Route `mega_batch.py` through `_exact_theory.roshko_strouhal`, or record why a second copy is wanted. Blocked behind D12 — re-attributing the constants would otherwise have to be done twice. | fleet |
| D14 | **A `VALIDATED` chip earned against another solver's tutorial documentation loses that qualifier on the camera surface.** `NINE_ACT_GATE_TABLE.md:11` grades CRM wing-body `VALIDATED` at +0.007% against *"DAFoam CRM_Wing tutorial, Cd 0.02090"* — a code-to-code reproduction, not a physical experiment. `A6_crm_wingbody.md:154-159` says so explicitly and refuses the AIAA DPW-VI band by name. But `FILMING_COMMANDS.md:108-117` restates the nine results as a flat list in which **row 8 drops "DAFoam tutorial" entirely**, sitting beside rows 6 and 7 whose referents are experimental — so the tightest number on the page reads as the lab's best agreement with a published measurement. Row 1 loses "Roshko-Williamson"; row 7 loses "NASA". The two morning reports and `PRODUCT_LIST.md:191` **do** preserve the wording; the loss is specific to the hand-written filming list. | B6 external-referent audit, 2026-08-11 (§5.1) | Restore the referent to each line of the filming list, or regenerate that list from `scripts/gate_table.py` as the surrounding runbook already does at `:259`. Zero compute. Note `INSTRUMENT_INTEGRITY_2026-08-11.md:429-438` separately disputes this row's `VALIDATED` tier on convergence grounds — the two findings are independent and both live. | fleet |
| D15 | **Two of three `models/curriculum/*/reference.yaml` files are known to carry hand-set numbers; the third is behind a filmed act and has never been checked.** `naca4412_wing/reference.yaml:29-31` records its own former value as *"a hand-set estimate entered directly in this file with no computation and no supporting script behind it"*; `NOT_PASSING_REGISTER.md` Group 4 records `naca0012_wing`'s `cd: 0.009` as *"hand-set in the initial commit and has never been touched"*. `ahmed_25/reference.yaml:4` holds `cd: 0.285` sourced to *"Ahmed, Ramm & Faltin 1984, SAE 840300"* — a real, uniquely identified paper — with **no record of which table, figure or page it came from** (executed: `git log --follow` returns only the import commit `5336dd57`; a grep for table/figure/page/extracted/digitised/hand-set over that file returns no provenance line). It is the reference behind act 6. Compounding it, `F10_3D_VISCOUS_FAMILY.md` writes the gate as *"the citable reference — Ahmed, Ramm & Faltin 1984, SAE 840300, Cd 0.285 ± 15%"*, where the **±15% is the lab's own `tolerance: 0.15`**, not the paper's scatter — and the graded 0.32284 sits outside the same yaml's own `cd_range: [0.27, 0.30]`. | B6 external-referent audit, 2026-08-11 (§4.2, §4.5) | Record the extraction route for 0.285 from SAE 840300, or mark its provenance unknown the way the two siblings are marked. Separately, rewrite the F10 sentence so the band is not attributed to the citation. Zero compute. | fleet |
| D16 | **The FD-vs-adjoint shared-primal limitation is stated sharply in the diagnostic documents and absent from the gate lines.** An FD gradient and an adjoint gradient from the same solver on the same primal agree wherever the primal is wrong together — which the lab knows and uses decisively: `PROOF.md:917-940` (*"FD and the adjoint are, provably, differentiating the exact same frozen-`yWall` discrete function"*, the move that refutes a hypothesis), `W5_GRADIENT_REGRADE.md:622-631` (*"the FD cannot change between the runs"*, retracting a control that could not have failed), `VERIFICATION_A4_mechanism_supervisor_sweep.md:121-126` (turning it into a positive argument). It is **not** stated at the gates themselves — `PROOF.md` §8/§8.2, the A5 FD-verification section, or `DAFOAM_CASE_STATUS.md`'s per-case summaries — where the FD is called "the true derivative". | B6 external-referent audit, 2026-08-11 (§5.2, §5.3) | One clause at each gate naming what the FD shares with the adjoint. The wording already exists in this lab, four times over; it has not reached the verdict lines. Zero compute. | fleet |
| D17 | **The referent declaration does not travel with the number, and the lab has already built the fix once without generalising it.** Four of B6's seven live findings are the same shape — a correct referent declaration exists in the canonical record and is absent where a reader meets the figure — and a fifth arguably is. The closure score's canon declares *"a local scoring, not an official leaderboard placement"* and pins the scorer to commit `1c4e22c8`; the restatements in `LADDER_V` tables, `ACTIVE_RESEARCH.md`'s body and `PRODUCT_LIST.md` carry the figure alone. F6b Gate V's distributor is named one document away from the verdict (executed: a grep for `McConkey|rmcconke|closure-challenge-benchmark` over `F6b_ERCOFTAC_RESULTS.md` returns **0**), and the travelling phrasings compress to *"reproduces the known answer"* — which is another RANS code's answer. **The chief has already made the rank-1 companion clauses mandatory on every surface; no equivalent travelling rule exists for the referent.** | B6 external-referent audit, 2026-08-11 (§4.3, §4.4, §10) | Generalise the rank-1 companion-clause rule: a figure that carries a gate verdict carries its referent, or a pointer to the document that states it. This is the single finding of B6 that would close the other four. Zero compute; needs a ruling, not a run. | Katie / chief |
| D18 | **A lexical screen does not reliably classify referent declaration in this corpus — measured, not asserted.** B6 built one, froze it, and measured its error rather than shipping the number: it reported **62.3% UNDECLARED** over 1,549 verification sections, and adjudicating a random sample of 14 found **7 were not load-bearing verifications at all** and **3 carried referents the screen missed** (e.g. `LADDER_V_PASS3_COLD_2026-08-11.md:86`, *"at commit `1c4e22c8`, working tree clean, unmodified"*). The cause is that this lab declares provenance in a rich non-formulaic idiom — *"the benchmark's own unmodified scorer"*, *"our figure, not one the benchmark publishes"*, *"(own run)"*, *"fetched live 2026-07-28, on disk"* — which is itself evidence of a corpus that does the right thing in prose. Only **12 tables** in the tracked markdown corpus carry the Verification Charter §2 `reference` column header (frame: `git ls-files '*.md'`, 367 files). | B6 external-referent audit, 2026-08-11 (§3, §9) | If referent declaration is ever to be machine-checkable it needs a **field**, not prose — the charter's `reference` column made mandatory for gate rows. Filed as a design question, not a defect: the prose is currently better than the field would be. | fleet |

### D-follow-on — the two published results D1 puts in doubt

Filed by the chief, 2026-08-11, on the back of D1. **D1 says the instrument can lie. This
entry asks whether it already did, in something we published.**

A sweep of every commit in this tree since 00:00 today for mutation-proved claims returns
three hits, one spurious:

| Commit | Time | Claim | Why it carries D1's signature |
|---|---|---|---|
| `717d7e7a` | 06:19 | *"mutating `_PLACE_REACH` from 24 to 99 moves…"* | `24` → `99` is an **equal-length** edit — the size half of `(mtime, size)` is preserved by construction. |
| `6b37866a` | 07:14 | *"the recompute moves when I mutate my own sentences"* (V16 fourth grade) | Its own body states the file was **"restored byte-for-byte"**, which reproduces both halves of the invalidation key. |
| `d4509368` | 01:36 | *"the solves did not mutate their own inputs"* | **Not a mutation test** — a different sense of the word. Discarded, and recorded so the next sweep does not re-flag it. |

**Both live ones are POSITIVE results, and that is the dangerous polarity.** Each concluded
that a recompute is alive. Under the inversion D1 demonstrated, the module can hold the
mutated value at run time while the file on disk reads the original — so a test that appeared
to redden on mutation may have been reading stale mutated bytecode throughout, and the
recompute it certified as live may not be. That is a **false pass in a published claim**, not
merely an unverified one.

**What settles it:** re-run those two with `__pycache__` **cleared between every cell**
(NOT `PYTHONDONTWRITEBYTECODE=1` — see D1a; the flag does not stop a stale pyc being read),
asserting all cells **in the same run** so an inversion cannot present as a pass. Routed to
the V16 agent with an explicit instruction not to re-open the rung for it.

**SETTLED 2026-08-11, V16 close-out — both hold, neither was a false pass.** Re-run in a
detached worktree at each claim's own commit, with `__pycache__` deleted before every cell,
each cell a fresh subprocess, and every cell reporting the value seen AT RUNTIME beside the
value ON DISK so an inversion would be visible rather than inferred. Both mutations were kept
**equal-length**, so both sat squarely in the vulnerable class rather than dodging it.

`717d7e7a`, `_PLACE_REACH` 24 → 99 (equal length), harness at `scripts/self_audit.py`:

| cell | expect | runtime | disk | verdict |
|---|---|---|---|---|
| A clean | 24 | 24 | 24 | OK |
| B mutated 24→99 | 99 | 99 | 99 | OK |
| C restored byte-for-byte | 24 | 24 | 24 | OK |

The generated verdict line carried `24 of 45` in A and C and `99 of 45` in B. Claim holds.

`6b37866a`, one sentence in `V16_AUTHOR_HELDOUT_SET.py` mutated `Wu and Zhang` →
`Xu and Zhung` (equal length, one occurrence), test
`test_every_published_reach_figure_recomputes_from_its_sentences`:

| cell | expect | got | runtime has mutant | disk has mutant | verdict |
|---|---|---|---|---|---|
| A clean control | GREEN | GREEN | False | False | OK |
| B one sentence mutated | RED | RED | True | True | OK |
| C restored byte-for-byte | GREEN | GREEN | False | False | OK |

Byte-for-byte restore verified by SHA-256. Runtime matched disk in every cell of both
matrices — no inversion. **D1 says the instrument can lie; these two say it did not lie
here.** D1 itself stays open: the finding is real and the harness it asks for is still not
part of `sdk/tests/`.

**The cheap answer that would settle it for free:** if either mutation was reverted with
`git checkout` rather than a copy, the mtime changes and the stale cache is defeated. One line
of evidence closes it.

**The general form, worth more than the two instances:** every mutation-proved result this lab
has produced is only as good as whether its author happened to clear the cache — and mutation
testing is the main way both live sessions have been proving that recomputes are live. Until
the harness in D1 exists, treat any mutation result produced without the guard as **unproven
rather than merely unverified.**
| D19-D20 note | *[RENUMBERED 2026-08-11 by the chief while landing the grade commit. These two were filed as **D5** and **D6** on a worktree branch cut when the docket ended at D4; `main` meanwhile grew D5-D18, so both numbers were taken. Kept and renumbered rather than merged or dropped -- **the third ID collision in this file today**, all three from concurrent writers who could not see each other. The docket needs allocated IDs, not sequential ones; filed as its own item.]* | | | |
| D19 | **A worktree can isolate a grader from the commit it was sent to grade, and R-ISOLATE's stated caveat does not cover it.** Grade round 5 was dispatched into a worktree whose branch was cut **18 commits behind** the subject: `HEAD` was `2c2c6a9a` (V15 round 5), and the V16 close-out it was sent to execute — `c05a03e0`, `236465a5`, `9c5b7b9d` — was not an ancestor of it (`git merge-base --is-ancestor 9c5b7b9d HEAD` → 1). The V16 work was simply absent from the checkout. R-ISOLATE part 1 already carries a caveat and it is about **gitignored** files; this is a second, independent way the same mechanism fails open, and it produces the identical shape — an honest, confident report about a tree that does not contain the subject. A grader that had gone straight to the files would have returned five UNEXECUTABLEs, or worse, five confident verdicts on the wrong revision. Detected only because `git log --oneline -3` disagreed with the dispatch brief's own quoted commit subjects; recovered with `git merge --ff-only 1393b8b4` inside the worktree. | V16 grade round 5, 2026-08-11 | Have the dispatcher **state the subject SHA in the brief**, and have the agent assert `git merge-base --is-ancestor <subject> HEAD` before it executes anything — the same "print what you can see before you report what you found in it" discipline the gitignore caveat already prescribes, applied to revision rather than to file count. | fleet |
| D20 | **The placement guard publishes four recall figures and no precision figure, and both of grade round 5's findings are precision defects.** `_PLACE_REACH` (three rows) and `_PLACE_REACH_B` all measure MISSES — how much placement language the patterns fail to reach — and the verdict line spends a long paragraph on them. Nothing measures the opposite direction: how often the guard binds an ordinal to an entrant in a sentence that is not a placement at all. The frame's blind-spot list names one over-binding mechanism (item 6, a surname that is also an ordinary English word) and it is not the mechanism either E2 or E4 fails by. A reader is given a well-quantified account of what the guard misses and no account of what it invents — and the guard's own comment argues the invented one is the worse of the two, "because the false positive is loud and the false negative is silent". | V16 grade round 5, 2026-08-11 | A held-out set of sentences that are NOT placements — linear-algebra ranks, dated history, quotations, cross-sentence adjacencies — measured the way the three recall sets are, recomputed from committed inputs, and interpolated into the verdict beside them. `campaign/V16_GRADE_ROUND5_PROBES.py` is a 20-sentence starting point with its controls already separated. | fleet |
| D21 | **The staleness assertion is unsatisfiable for any subject landed by cherry-pick, and it failed on the first attempt to clear it.** D19's remedy — assert `git merge-base --is-ancestor <subject> HEAD` before executing — was adopted, and the very next use of it was a false STALE. The V16 round-2 author asserted against subject `0a51269d`, got STALE, and stopped; the chief landed the commit as cherry-pick `df53488e` and dispatched "re-run the assertion; it will pass now". **It did not pass and it never will.** A cherry-pick replays the diff under a new SHA (`0a51269d`'s parent is `1393b8b4`; `df53488e`'s is `0049463f`), so the original commit is not an ancestor of `main` and cannot become one. The content is present and byte-identical — every file the two commits touch, plus the two files under repair, matched by blob hash, and the committed probes reproduced the recorded 12-disagree/8-control split with zero drift — while the ancestry proxy says absent. This is the same defect as the E2 finding one rung down: a proxy standing in for the property actually wanted, confidently wrong in both directions. Note the standing cost: **every future agent dispatched to subject `0a51269d` will halt**, because the identifier in the brief now names a commit no ancestry query on `main` can reach. | V16 fix round 2, 2026-08-11 | Two parts. (1) The assertion must be **content-addressed, not commit-addressed**: pair or replace ancestry with `git rev-parse HEAD:<path>` blob identity on the files under repair, plus execution of the subject's committed probes with a drift check — that is what established the position here when ancestry could not. (2) A dispatcher landing a grader's branch should **merge rather than cherry-pick** when the SHA has already been quoted in a brief, or re-quote the new SHA in the dispatch; otherwise the brief's own identifier is stale on arrival. | fleet |
| D22 | **A repair's comment states a cause that executing the build it describes contradicts, and the correction it makes to a previous grader is the wrong way round.** V16's round-6 fallback is justified by a comment saying the two false negatives round 5 filed *"stay silent with the linear-algebra discriminator answering correctly"* and that *"distance was not what muted them"* — a correction of round 5's stated cause. Executed against `1393b8b4`, the build round 5 actually graded, `_place_linalg_subject` returns **True** for both shapes, which is the `continue` that discards the placement before the bind loop is ever reached. The discriminator was what muted them; round 5 was right. The correction is true only of the repaired build, where the discriminator now answers correctly and the distance test takes over — which is a statement about the fix, presented as a statement about the defect. The repair itself is needed either way, confirmed by the same execution, and the two figures quoted in the correction (69 and 73) reproduce exactly under the frame that governs, name-start to ordinal. The pattern is the one round 4 already met: a comment explaining why the code is correct, where the explanation is what is wrong. | V16 grade round 6, 2026-08-11 | A convention that any comment asserting **why a previous verdict was wrong** cites the SHA it was executed against and the value it observed, in the same sentence — the numbers here carried a frame and the causal claim carried none. Cheap to satisfy: both facts above came from one script that imports the old and new module side by side and prints the intermediate predicate, not just the verdict. | fleet |
| D23 | **Whitespace collapse makes markdown structure invisible to every clause-boundary rule in the placement guard, so "cannot cross a sentence" means only "cannot cross `[.!?;:]`".** `_placements` flattens the surface with `re.sub(r"\s+", " ", text)` before any boundary test runs, and every boundary rule downstream — `_PLACE_CLAUSE` for the subject-NP cut, `_PLACE_SENTENCE` for the bind probe — is a punctuation rule. A markdown heading, a list item and a table cell all end a unit of meaning **without terminal punctuation**, so after flattening they are not boundaries at all and the guard reads across them as if the text were one sentence. This corpus is predominantly markdown, which makes the unpunctuated boundary the ordinary case here rather than the exotic one. Executed at `72609a04`: three shapes bind an ordinal to an entrant named in the previous structural unit at gaps of 65, 69 and 54 characters against a `_PLACE_BIND` of 40, and the identical text with a full stop inserted is correctly silent. | V16 grade round 6, 2026-08-11 | Either flatten to a **boundary-preserving** representation — substitute a sentinel for a blank line, a heading, a list marker and a cell separator before collapsing the rest, and add that sentinel to `_PLACE_CLAUSE` and `_PLACE_SENTENCE` — or state in the frame that the guard's unit is the punctuation-delimited clause and not the sentence, so the written claim matches what runs. The first is a few lines and testable; the second is free and honest. | fleet |
| D24 | **The bind's boundary probe cannot tell an abbreviation-final period from a sentence-final one, and both the old and the new form get one of the two cases wrong.** `_PLACE_SENTENCE` fires on `[.!?]` + optional closers + whitespace + an uppercase-or-opening character. Until `72609a04` the probe appended the real next character, so a period followed by a digit or a lowercase token was not treated as a boundary — the defect grade round 5 filed, six shapes binding straight across a full stop. The repair appends a constant that is unconditionally uppercase, which fixes all six and, by the same stroke, makes **every** period-plus-space a boundary, including the one closing an abbreviation. Executed old-vs-new in one invocation with `__pycache__` cleared first: an abbreviation followed directly by an ordinal, and by an ordinal-plus-noun, faulted at `1393b8b4` and are silent at `72609a04`. Both are real wrong placements, now missed. The direction is the cheap one, but the comment making the trade says the appended character's identity *"was never doing any work"*, and on this class it was doing exactly one job. | V16 grade round 6, 2026-08-11 | A boundary test with a short abbreviation exception — the closed set that actually occurs in this corpus (`et al.`, `Fig.`, `Eq.`, `Sec.`, `No.`, `cf.`, `e.g.`, `i.e.`, `vs.`) checked against the token immediately before the period — plus a two-direction control set beside the existing over-fix controls: shapes that must still bind after an abbreviation, and shapes that must stop binding after a sentence. The current controls only test one of the two directions, which is why the trade landed unnoticed. | fleet |
| D25 | **Two boundary rules run inside one function and they disagree about what a boundary is, so a semicolon stops the fallback and does not stop the ordinary bind.** `_place_subject_np` cuts the subject phrase at `_PLACE_CLAUSE`, which after this round is `[.!?;:\|\n]`. The `_PLACE_BIND` loop twenty lines above it tests `_PLACE_SENTENCE`, which reads only `[.!?]`. So the SAME surface, with the SAME two words at the SAME distance, is a boundary for one rule and not the other, purely by which of the two paths the ordinal happens to take — and which path it takes is decided by whether the entrant sits inside 40 characters. Found by the precision set built for this round, not by a probe aimed at it: an ordinal with a position word, an entrant named before a semicolon and inside the bind, binds across the semicolon and faults. It is one of the twenty false FAULTs the published figure now counts, in class `cross-sentence`. **Not fixed here, and the reason is scope, not merit** — R-CONVERGE bounded this round to four deliverables and widening the bind's boundary test is a fifth behaviour change, which is precisely the "one more discriminator" move the ruling that governs this round exists to stop. | V16 round-6 close-out, 2026-08-11 | Give both rules the same boundary predicate — most cheaply by having the bind loop consult `_PLACE_CLAUSE` for the clause-level marks and keep `_PLACE_SENTENCE` for the sentence-level ones — and measure the result on the precision set AND all four recall sets in the same invocation, since a wider boundary buys precision with recall and this rung has already traded one for the other twice without measuring both ends. | fleet |
| D26 | **A test whose whole subject is "the set must be derived, not listed" enumerated its own evidence files by hand, and the sixth file added was the one that needed it.** `test_the_evidence_files_are_not_themselves_corpora_of_faults` guards the committed held-out sets against becoming corpora of the very defects they describe (docket D4). It named five files in a literal tuple. The file added this round was written with one position word unsplit, and the guard reported that source as carrying a real fault — caught, but caught by the pre-commit sweep the brief mandates rather than by the test, and a seventh file added by an agent who does not edit the tuple would be unguarded with the suite still green. This is the identical shape as the defect the whole test file was created for, quoted from its own docstring: *the list was the defect.* Fixed in the same commit by globbing the evidence directory with a floor on the count, so a set that vanishes fails rather than passes vacuously. Filed anyway, because the finding is that **a lab can restate a principle in a docstring and violate it eleven hundred lines below**, and the general form is worth more than the instance. | V16 round-6 close-out, 2026-08-11 | Executed for this instance. The general form is a sweep for *literal collections of paths inside tests whose stated property is derivation* — there is no reason to think this file is the only one, and the two places already found (the original `check_rank_claim_surfaces` surface list, and this) were both found by accident rather than by looking. | fleet |
| D27 | **The precision denominator that closes V16 is single-sourced, and its "WORST CASE" framing is falsified by the first independent set built against it.** The rung's closing condition is a published precision figure; the figure is 20 of 41 (49%) on a non-placement set built by the guard's own author *with the pattern list in hand*, and the generated verdict paragraph calls it "a WORST CASE on hard sentences". Grade round 7 built a disjoint 43-sentence set blind -- author's set and `_place*` patterns both unread until after the sentences were written -- and applied the author's own admission rule unchanged (only sentences a rule-A pattern actually matches enter the denominator; 18 of 43 excluded). Result: **19 of 25 (76%)**, Fisher exact two-sided **p = 0.040**, Wilson 95% intervals 57-89% against 34-64% -- the independent LOWER bound sits above the author's point estimate. Split by provenance: the four classes the ruling itself names went 9 of 15 (60%); the seven classes the grader invented went **10 of 10 (100%)**. So "worst case" is not a bound, it is an author's-set case, 27 points optimistic -- a false written claim of exactly the kind L-76 exists for, and the fourth such absolute this rung has produced. The lab already solved this one screen higher: `_PLACE_REACH` publishes THREE reach rows, each naming who built it and whether they were blind, under a note that "two honest sets disagree by twenty points". `_PLACE_PRECISION` publishes one row from the least independent possible source. | V16 grade round 7, 2026-08-11, subject `64281b6b` | Make `_PLACE_PRECISION` a multi-row table shaped like `_PLACE_REACH`, carrying the grader's row with `blind=True` beside the author's `blind=False`; delete "a WORST CASE" and let two disagreeing rows say what one row cannot. The set is committed at `campaign/V16_GRADE_ROUND7_PRECISION_SET.py` and recomputes, so it cannot go stale (L-79). **General form, worth more than the instance:** any figure this lab publishes about its own cost should carry the provenance of the sample it was measured on, because the sample's author is a variable in the result. **EXECUTED 2026-08-11 by V16 round 8, `5e4a0cc5`.** `_PLACE_PRECISION` is a two-row table shaped like `_PLACE_REACH`; both rows name their builder and their blindness and both recompute from committed sentences; the verdict computes the 27-point spread and which row is higher rather than asserting either; and "a WORST CASE on hard sentences" is withdrawn from both places it stood, quoted beside its falsification (L-76) and added to `_FALSIFIED` so reinstating it as a claim reddens the suite. | fleet |
| D28 | **The test that enforces the "knowingly accepted" proviso is scoped to the author's own set, so it cannot see a shape the author never thought of.** The V16 ruling permits a false-FAULT shape to stand unfixed PROVIDED it is counted in the precision figure and named in the blind-spot enumeration, and `test_every_false_fault_class_is_counted_and_named` enforces that well: dict equality in both directions plus a sum, so a shape cannot be counted in the figure and left out of the list. Verified binding. But both sides of that equality are computed over `campaign/V16_PRECISION_SET.py`, so the proviso is self-certifying one level up -- it is the same L-74 circularity as the figure itself, moved from the number to the test that guards the number. Executed: an independent set found two rule-A false-FAULT shapes outside the nine-class enumeration -- **negation/interrogative** (an ordinal denied or questioned, never asserted) and **a present-tense placement on a different, explicitly named ranking** (which is not `dated-history`; nothing about it is dated, and it disclaims the closure board in the same sentence). Neither is counted; neither is named; the suite is green. A rule-A fault on a travelling surface is FAIL severity, and a citation ranking is a thing a travelling submission document plausibly contains. | V16 grade round 7, 2026-08-11, subject `64281b6b` | Either enumerate the two shapes with their counts, or state explicitly that the enumeration is scoped to the author's set rather than to the guard. **General form:** a proviso of the form "accepted IF counted and named" is only as wide as the corpus the counting runs over, and that corpus should be named wherever the proviso is claimed. **EXECUTED 2026-08-11 by V16 round 8, `5e4a0cc5`** — and the remedy taken was the first option, not the second: the test now binds against the grader's blind set as well as the author's, so a false FAULT in a shape nobody enumerated reddens it. Mutation-proved with the cache cleared and control and mutants in one invocation. Two classes (`other-named-board`, `negated-or-questioned`) entered the enumeration because of that binding and are filed as D46. **The residual circularity — the label→class assignment is still the author's — is filed separately as D47 rather than treated as closed.** | fleet |
| D29 | **A gate can be non-monotone in the property it names, and G2 is: it awards its worst possible score to a perfectly localised correction.** `np.quantile(dev, 0.9)` returns **0.0** when fewer than 10% of cells deviate, so `dev >= thr` selects the whole domain and the top-decile share collapses to the window's base rate. Executed on the real mesh (21,000 cells, window 1,773 = 8.44%): pure noise confined **entirely inside the window** scores **8.4% and FAILS**, while the same noise smeared over the 14.14% W2 support scores **59.5% and PASSES**. Negative control passed -- noise over the whole domain returns 8.3%, so the instrument can return the null. **This did not cause the recorded 26.9%** (both real beta fields deviate in nearly every cell and sit far from the collapse); it is independent proof that the metric is invalid, not the explanation of the value. **General form:** any gate of the shape "fraction of the top q-quantile falling inside region R" silently inverts when the support of the deviation is smaller than the quantile, which is exactly the regime a *localisation* gate is supposed to reward. | Closure task 2, 2026-08-11, subject `64281b6b`, evidence `demo-output/website/campaign/CLOSURE_STAGE1_AND_C2_STATUS.md` §1.5 at `e43084c8` | A quantile-based spatial gate declares its behaviour when the deviating set is smaller than the quantile, or uses a base-rate-free enrichment statistic instead. The S1-with-priors pre-registration is the next place this choice gets made, and it must be made **before** that line's first compute (§2b). | fleet |
| D30 | **The lab's "four official scoring calls, ever" is self-imposed discipline, not compliance -- the benchmark ships the test ground truth and instructs previewing against it.** The eval package contains `src/closure_challenge/data/ground_truth_test.npz` and the benchmark README tells submitters "You can preview what your score will be using the benchmark dataset's python package." There is **no limit on scoring calls and there cannot be one**; the only published rule is the strict prohibition on *training or validating* on test cases. `CLOSURE_CHALLENGE_SUBMISSION_DRAFT.md:169-172` already states this correctly, but the constraint is recited elsewhere in a register that reads as an organiser requirement. **Naming it as a choice is worth more than obeying it silently:** a stricter-than-required protocol is a credibility asset only if the record says it was elective. | Closure task 2 submission-policy research, 2026-08-11, subject `64281b6b` | Every surface that recites the scoring-call count labels it as lab discipline rather than benchmark rule, per the §6a principle that a verdict carries what it was checked against. | fleet |
| D31 | **Five of the eight submission-policy questions have no answer in any open source, and four of them are the ones that bind a first send.** Verified from fetched primaries with positive controls (the same grep found `strictly forbidden`, `anytime` and `authors` in the same documents, so the zeros are real absences): **no published rule exists** on (a) entries per team -- no limit stated *and* no statement that multiple are allowed, (b) whether a corrected resubmission is permitted after scoring, (c) whether hyperparameter tuning must be disclosed, (d) what licence submitted predictions and code fall under -- the GitHub API returns `license: null` for both repos that would host them, (e) whether a company rather than named individuals may occupy the "Authors" row. Practice evidence exists for (b) -- an open PR was scored, and the steward re-scored all entries in place at `44540c8f` -- but practice is not a rule. **All five are steward questions, not document questions**, and the README invites them. **Addendum 2026-08-12:** audited against the challenge README as pasted by Katie from the live page on 2026-08-12 (not fetched by the fleet; operative wording identical to the pinned clone at `deb91557`, README lines 92–100, so the procedure did not change when upstream HEAD moved to `d572d40c`): the paste re-confirms the three already-answered questions — registration (none), attribution (test subdirectory + authors + references to rmcconke@mit.edu), deadline (none, "Submissions are accepted anytime!") — and the strict rule verbatim, and **answers NONE of (a)–(e); all five remain open and remain Katie's steward questions.** The relaxed-rules sentence ("free to use your own input features, base turbulence model, data assimilation technique") broadens what is permitted and says nothing about disclosure, so (c) does not close. Full audit with the measured readiness checklist and per-file conformance verification: `demo-output/website/CLOSURE_SUBMISSION_REQUIREMENTS.md`, written 2026-08-12 at `5c9c63fb`. | Closure task 2 submission-policy research, 2026-08-11, subject `64281b6b`; addendum 2026-08-12, Katie's paste of the live README | **Katie's, and only Katie's.** Contacting the steward is an external interaction reserved to her in two charters. This entry records that the questions exist and are unanswerable from documents -- it does not authorise asking them. | Katie |
| D32 | **The L-76 enforcer cites thirteen tests, none of which exists, and its own missing-check class is structurally unable to fire on it.** `scripts/check_absolutes.py` backticks 13 distinct `test_*` names under the literal word *"Evidence:"*. **0 of the 13 resolve to a `def` in any `.py` on disk** (frame: `find` under the repo root, `.git` and `.claude/worktrees/` excluded; 1,459 real test defs found in the same sweep, so the finder works). Worse, the module's stated rule -- *"a cited test that does not exist is reported as its own class, `CITES_MISSING_CHECK`, and is counted as unbacked"* -- **does not hold in its own code**: `classify_unit` tests `elif resolved:` **before** `elif dangling:`, so a single runnable command anywhere in the same prose unit launders every dangling name into `BACKED`. Executed on the module itself: **BACKED 15, CITES_MISSING_CHECK 0**. Injection control, both directions: the unit *"This never fails. Evidence: `test_does_not_exist_anywhere_at_all`."* classifies `CITES_MISSING_CHECK`; the same unit with `python3 scripts/check_absolutes.py --frame tracked` appended classifies `BACKED`. The module's `resolved` list for its own docstring is `['<command>', 'path.py:function']` -- and `path.py:function` is the metasyntactic placeholder from its own prose. | V15 round 6, 2026-08-11, subject `2266c4e3` | Either write the 13 tests or delete the citations; and reorder the verdict cascade so `dangling` is consulted before `resolved`, with a test that plants a dangling name beside a runnable command. Until then every `BACKED` this checker reports is unsafe, and B3a's published 74% false-positive rate was measured on `UNBACKED` only -- `CITES_MISSING_CHECK` has no measured rate at all. **EXECUTED AND CLOSED 2026-08-11, commit `89c2a467`, subject `f6b4eec0`.** Both halves done, and the second half was done per citation rather than wholesale. **(1) The cascade is reordered** -- `dangling` is asked before `resolved` -- and the mixed unit is DECIDED rather than left implicit: **any dangling name makes the unit `CITES_MISSING_CHECK` even when a sibling citation resolves.** The argument is written at the branch and it is about the reader, not the arithmetic: someone who spot-checks one of five citations and finds it good does not check the other four, so one real name beside four fictional ones misleads him more than no citation at all. The rejected alternative (report the dangling ones as a note) is named there too, with its reason -- a note is not a verdict and nothing downstream counts it. The cost is stated: this is strictly the more severe rule, so the reason string now says `mixed unit: N other citation(s) here do resolve, and that does not rescue it`. **(2) Of the 13 citations, 11 were WRITTEN and 2 STRUCK.** Written into `sdk/tests/test_absolute_claims.py` (23 tests, 8 classes) because the claim beside each is load-bearing: the frame is derived from `git ls-files` rather than enumerated, the verdict is three-valued with skips counted per reason, backing is unit-level, the four gates each reject with a positive control surviving, the measured rate is recomputed from the fixture at call time and replays through the same `classify_unit` the corpus sweep uses. Struck where the name was aspirational, with the reason left in place at each site: the one asserting the audit's published rate matched the recomputed one (it does not -- **D50**), and the one asserting the sample was drawn from the checker's own output (a past sampling act at `8ebe2b8e` that no execution today repeats). **Neither struck name is repeated in the module even as a corpse**, since by its own rule a name resolving to nothing reads as backing. Also fixed: `FILE_FUNC_RE` was reading the metasyntactic placeholder `path.py:function` out of the module's own prose as a citation -- now written `<path>.py:<function>` (see **D51**). **NONE OF THE 13 WAS A DELETED TEST.** `git log --all -S"def <name>" -- '*.py'` returns zero commits for all thirteen and `sdk/tests/test_absolute_claims.py` had never existed in history, so they were fictional rather than decayed -- a different finding from a citation that rotted. **SELF-CLASSIFICATION, `--paths scripts/check_absolutes.py`, corpus as it stood at each cell:** BEFORE (code and prose as committed at `8a8b5392`) **BACKED 15, CITES_MISSING_CHECK 0, UNBACKED 2**, with **13 distinct dangling names silently discarded**; **ORDERING FIX ALONE**, same prose, same corpus: **BACKED 0, CITES_MISSING_CHECK 15, UNBACKED 2** -- the instrument indicting itself, which is the proof the fix reaches; AFTER (fix + citations settled) **BACKED 15, CITES_MISSING_CHECK 0, UNBACKED 2, 0 dangling**. **MUST-NOT-MATCH CONTROLS (L-84) AT TWO SCALES.** Unit: a unit whose every citation resolves is `BACKED`, and a unit citing only a runnable command is `BACKED` -- so the fix is not "flag everything". File: `UNBACKED` stays 2 and the verdict on itself stays `FLAGGED`, asserted by a test -- so the fix is not "switch it off". The two survivors are real and left standing (*"Every word the docket names has a family here"*, *"never enumerated by path"*). **AND THE CONTROL THAT DECIDES WHAT THE EVIDENCE IS.** Once the citations are real, the OLD order gives the SAME file-level counts as the new one, so **the file count no longer discriminates between fixed and unfixed** and cannot be the proof. The discriminating evidence is the unit-level mutation replay -- control and mutant loaded in ONE invocation with `__pycache__` purged -- where **exactly 2 of the 23 tests fail on the pre-fix order and 0 on the fix**. The two are the ones planting a dangling name beside a runnable command and beside a resolving test; the other 21 pass on both, which is what a test that pins prose rather than mechanism should do. **Not touched, on purpose:** no matching retuned, no gate added, no corpus sweep re-run, and the 74% figure not chased -- that is B3a/B3b, and two things found in passing are filed to D50 and D51 rather than fixed here. | fleet |
| D33 | **"Not one log file is cited by path anywhere in the committed markdown corpus" is false, and the recommendation built on it would break a published citation.** `docs/TRACKED_ARTIFACT_SCOPING.md:69` publishes **0** for logs cited by exact path, and `§5` recommends *"Move solver **logs** first -- 778 files, no path citations"*. Re-executed with a broader citation vocabulary -- every path-like token in all 372 tracked `.md` files rather than only `*_runs/`-shaped tokens -- finds **1**: `demo-output/website/tmr/runs/naca-a10-medium/log.checkMesh`, tracked, cited by exact backticked path at `demo-output/website/tmr/C4_naca0012_closure.md:80`. The published harvester misses it because the path is under `tmr/runs/`, which carries no underscore and so matches no `*_runs/` token. **The sibling claim holds**: 0 of the 8,582 solver-output files are cited by exact path under the broad vocabulary too, and `scripts/corpus_figures.py` reproduces 8,582 exactly at `2266c4e3`. The defect is the restricted vocabulary, which the frame does not state. | V15 round 6, 2026-08-11, subject `2266c4e3` | Re-run the citation harvest with no `*_runs/` restriction, publish the corrected log figure, and state the vocabulary in the frame. `§4` lists three ways the measurement could be wrong and this is not one of them. | fleet |
| D34 | **[WITHDRAWN 2026-08-11 by its own author, subject `0be3f1d0`. E1 was right; this entry was wrong in all three of its parts, and the defect was in MY frame.** The disagreement is not a moving tree: both frames run over the identical 34-check population (`set(check_* attrs) - set(CHECKS)` is empty), and the replay at `e551f68f` gives the same answer as HEAD. **The whole difference is one check.** I widened the blind-spot keyword to `blind|cannot see|does not see|unable to see|not detected|out of reach|no visibility`; the chief matched `blind` alone. My extra hit is `check_record_writers_name_their_drops`, and its `cannot see` is the check **reporting a defect it found** -- *"`mesh_validity()` builds a record from `wall` ... so **this function** cannot see what its caller put in it"* -- where the subject is the audited function, not the instrument. E1's question is what the checks say **they** cannot see, so my match is a false positive and the count is **3 of 34, 31 silent**, exactly as published. **(2)** Not "wrong when written" -- nothing was wrong. **(3)** No contradiction with the ledger: `INSTRUMENT_INTEGRITY_LEDGER.md:219` counts checks whose `BLIND TO:` line the terminal printer emits at `self_audit.py:3713` (gated on `GENERATOR`/`TRANSCRIBED`/shared-symbol), which is a different measurement from what a `Result` says; **0 of 34 checks reprint their own declared BASIS text**, so 3 and 4 were never the same quantity. **The lesson, and it is D20's defect committed by the agent auditing D20:** I broadened a keyword list for recall and ran no precision control over the broadening, so I bought one extra hit and it was noise. A paraphrase list needs a set of sentences that must NOT match, measured, before the widened figure is published -- which is exactly what B3b already records about keyword-family checkers. The one part worth keeping is minor and stands: **E1 carries no commit anchor (W-5).** The original entry follows unedited.]** ~~Docket E1's headline correction is itself the error: it is 30 of 34, not 31.~~ E1 reads *"MEASURED 2026-08-11: it is 31 of 34, not 30 ... Three state one; thirty-one state none"*. Executed all 34 callables in `scripts.self_audit.CHECKS`, searching `summary` and `detail` for a blind-spot marker: **4 state one, 30 state none, 0 raised** -- `check_rank_claim_surfaces`, `check_board_placement_words`, `check_record_writers_name_their_drops`, `check_every_check_states_its_basis`. This is not rot: `scripts/self_audit.py` **as of `e551f68f`, the commit that filed E1**, replayed against the current tree gives the same 4 / 30, and all four emitters already existed there. So E1 corrected a right number to a wrong one, in the direction that makes its own finding sound worse, and it disagrees with `docs/INSTRUMENT_INTEGRITY_LEDGER.md:25` and `docs/LEDGER_HEADLINE_AUDIT.md:437` -- both of which say 30 / 4 and both of which are right. E1 also carries no commit anchor, which is W-5. | V15 round 6, 2026-08-11, subject `2266c4e3` | Correct E1 to 4 / 30 with its anchor, and note that its own positive-control paragraph (*"my first run returned 0 of 34"*) did not catch the residual off-by-one. | fleet |
| D35 | **`docs/MISSION_TOKEN_COSTS.md` declares two mission classes at a token boundary that is the eighth-largest of eleven gaps in its own data.** The document states *"There are two mission classes here, not a spectrum"* and gives them as **88k-160k** and **164k-309k**. Sorted, the twelve token totals are 88, 103, 157, 160, 164, 168, 185, 196, 197, 228, 249, 309. The claimed split is the 160 -> 164 gap: **4k, which is 1.8% of the 221k range and ranks 8th of the 11 gaps** (the largest is 249 -> 309 at 60k, inside the class the document calls homogeneous). The token distribution is continuous across the declared boundary. **The split is real in the other two columns** and the document half-notices it -- tool calls jump 5 -> 33, a 28-call gap that is the largest of 11 and 40% of that range. So the finding is sound and the axis is wrong: the discriminator is tool calls, not tokens, and the class ranges are stated in tokens first. Secondarily, class 2 is described as *"15 to 22 minutes"* while containing its own 27-minute and 62-minute rows. | V15 round 6, 2026-08-11, subject `2266c4e3` | Restate the classes on the tool-call axis with the token range as a consequence rather than a definition, and either drop the wall-clock range or widen it to cover the rows it contains. | fleet |
| D36 | **R-ISOLATE's own leftover worktree silently doubles two of the four frames in `scripts/sweep.py`, the instrument built the same evening to stop exactly this.** `.claude/worktrees/agent-ad85b7e8a54a4a57b/` is a full second copy of the repo -- **20,579 files**, checked out at `0a51269d`, ~3.5 hours behind `main` -- and it is excluded via `.git/info/exclude`, not `.gitignore`. Consequence, measured: `everything` selects **80,493** files against the module docstring's 59,560, and `worktree` **78,414** against 58,083, both ~35% inflated, entirely by that one directory. A sweep for `R-ISOLATE` over `*.md` returns **8 files under `worktree` and 4 under `tracked`** -- exactly 2x, because it is the same four files twice. `tracked` and `ignore-honouring` are unaffected. The `worktree` frame's own words are *"drops .bzr/.git/.hg/.jj/.sl/.svn directories"*: it prunes `.git` **directories**, and a nested worktree's metadata is a `.git` **file**, so the working copy walks straight in. The docstring's four numbers are anchored to `83ed6dab` and so are visibly dated rather than rotted -- but any `everything` or `worktree` sweep run today is silently double-counting, which is L-75 reintroduced by the rule written to prevent the class. | V15 round 6, 2026-08-11, subject `2266c4e3` | Add a fifth frame, or prune nested worktrees from `worktree`/`everything` and name the prune in the frame's own words; plus a test that plants a nested worktree and requires the frames to disagree. Separately: R-ISOLATE's dispatcher obligation should include tearing the worktree down, since the ladder doc already makes merging the output branch the dispatcher's job. | fleet |
| D37 | **`docs/MEMORY_ARCHITECTURE.md:852` misquotes the verification charter's version line by two bumps, inside the lab's own register of stale date literals.** It reads `` `VERIFICATION_CHARTER.md:3` (*"Version 1.6, dated 2026-08-11"*) ``; the actual line 3 reads *"Version 1.8, dated 2026-08-11."* (`2d0e5260` took it to 1.7, `be1b955c` to 1.8, both today). The register rotted in precisely the manner it documents. Adjacent, same class: `VERIFICATION_CHARTER.md`'s own version-history block covers 1.1-1.6 only -- **sections 2a, 2b and 6a appear in no changelog entry**, breaking a convention every prior bump kept; and three surfaces still cite `SUPERVISION_CHARTER.md` as **v1.0** after today's 1.1 bump (`docs/charters/PROPOSALS_OPEN.md:37`, `docs/standards/INFRA_FAMILY_SUPERVISION_GUIDELINES.md:113`, `docs/PRODUCT_LIST.md:512`). | V15 round 6, 2026-08-11, subject `2266c4e3` | One sweep over every tracked file quoting a charter version literal, then the changelog entries for 1.7 and 1.8. This is the cross-file propagation gap `MEMORY_ARCHITECTURE.md:861` already names, recurring on the file that names it. | fleet |
| D38 | **The four standing rules written on the evening of 2026-08-11 contradict the charters they sit beside, in six places, and none is enforced by any code.** Frame: `git ls-files` + `/usr/bin/grep` for `R-ISOLATE|R-CONVERGE|R-DEPTH|R-VALUE` -- **15 files match, all markdown, two test fixtures and one docstring mention; no script, hook, test or dispatch template enforces any part.** Against `SUPERVISION_CHARTER.md:136`, written the same evening -- *"A rule without a sweep is a preference"* -- all four are currently preferences, and `LADDER_V_TRIPLE_VERIFICATION.md:213`'s *"R-ISOLATE, four parts, all mechanical"* is an absolute with no executed test beside it (L-76). The six conflicts: **(1)** R-VALUE's `PASS WITH RESIDUALS` is a sixth gate verdict against `VERIFICATION_CHARTER.md:95` *"The verdict vocabulary is fixed"*, and §16's negative-verdict-review sweep enumerates the fixed vocabulary, so residuals are invisible to it by construction. **(2)** R-VALUE's closing condition -- two rounds returning only findings that would not change an external reader's belief -- is satisfied by R-ISOLATE's own documented fail-open shape, *"an honest, confident, empty result"*; two rules in one document, neither citing the other. **(3)** R-CONVERGE requires closure declared *before* the first grade; the E2 ruling in the same file sets E2's closing condition after six rounds, which `VERIFICATION_CHARTER.md:156-164` (§2b) forbids explicitly. **(4)** R-DEPTH caps meta-work at depth 2 with no exemption, while `SUPERVISION_CHARTER.md` §3 checks 1 and 3 mandate depth-3 supervisor work unconditionally, and A15 mandates it too -- V16 rounds 5-7 ran it. **(5)** Two live documents are both *"the docket"*, definite article: `LADDER_V:189` says `docs/DOCKET.md`, `ESCALATION_CHARTER.md:85` says `demo-output/website/agenda/docket.json`, they overlap on substance, and neither names the collision, against `MEMORY_ARCHITECTURE.md:613` *"A fact has one home"*. **(6)** None of the four carries the archive replay and fire rate `VERIFICATION_CHARTER.md:622` and §16 rule 2 require before a rule is adopted. | V15 round 6, 2026-08-11, subject `2266c4e3` | Katie's on (1) and (3) -- they are charter amendments, not fleet edits. (2), (4) and (5) are drafting fixes the chief can make. (6) is a measurement nobody has run. **The rung is not green while a rule set written in one evening conflicts with the charters in six places.** | Katie |
| D39 | **37 archived case directories carry a finished result and no log of any kind, so nothing about them can be verified — including whether they ran.** `act9-crm_wingbody-*` under `/home/ubuntu/certonomous-runs/` (37 directories, frame: `find /home/ubuntu/certonomous-runs -maxdepth 1 -type d -name 'act9-crm_wingbody-*'`) each hold a complete `1000/` field set with `1000/uniform/time` reading `value 1000; index 1000;`, and a `runScript.py` **byte-identical** to A6's (`diff` → empty). A sweep of all 37 for `*.log`, `*.txt` and `*stdout*` returns **zero files**. So there is no way to check convergence, CD/CL, exit status, or whether any of them crashed the way A6's own `run_model_run2` did. They can neither corroborate nor contradict the A6 CRM findings, and were excluded from them for that reason. This is the same family as the four zero-byte completion records already filed (E2) — **absence reads as nothing happened**, except here the fields prove something did. | A1/A5/A6 diagnosis, 2026-08-11, commit anchor `1393b8b4` | Either locate the driver that produced them and confirm whether it wrote logs elsewhere, or mark the 37 directories as unevidenced in whatever index cites them, so a later sweep does not read 37 finished-looking cases as 37 results. Not a compute request. | fleet |
| D40 | **A published root-cause document answers a standing question with a check that a dead lever would answer the same way, and reports it as passed.** `demo-output/website/dafoam/ROOTCAUSE_getRotationMatrix3d.md` §4.7 tests pre-registered prediction P6 — *"adjoint matrix reordering (`rcm` vs `natural`) must not move the result at all"* — reports 207.04% vs 207.05% at idx8, and concludes *"Reordering is not a factor, exactly as a purely geometric defect requires."* The only activity evidence in those runs is `W5-rotation-branch/D5a_pl_real_rotON_RCM.log:825` `Mat ReOrdering: rcm`, which `docs/DEAD_LEVER_AUDIT.md` §4 U-1 identifies as the **requested-value** print, not a readback. Checked: `/usr/bin/grep -c "matrix ordering"` returns **0** on `D5a_pl_real_rotON_RCM.log`, `D5b_pl_real_rotOFF_RCM.log` and `D1a_pl_real_rot_ON.log`, and neither D5 log holds a `KSP Object` block. **Positive control:** the identical grep on `W4-adjoint-pc-unblock/control_rcm.log` returns `12:    matrix ordering: rcm`, so the method finds a readback when one exists. P6's null is therefore exactly what an inert `rcm` would produce and the check does not discriminate. **This does not threaten the root cause** — it is established upstream of any linear solver by the `\|\|dXs\|\|` fingerprint being bit-identical rotations-on/off at the baseline and differing once pre-deformed — but P6 is one of seven pre-registered predictions in a document headed for upstream filing, and it should be relabelled as unresolved rather than cited as confirmed. | A1/A5/A6 diagnosis, 2026-08-11, commit anchor `1393b8b4` | `DEAD_LEVER_AUDIT.md`'s M-A (two arms with `-ksp_view`, ~8 core-min) settles it at the class level and 257 archived `rcm` runs inherit the answer, this one among them. Until then: strike P6's verdict in place, per the family's quote-and-strike rule, and do not carry it into any upstream draft. | fleet |
| D41 | **A live, actionable proposal still gates on a control that was withdrawn three and a half hours ago precisely because it could not fail.** `demo-output/website/agenda/proposals/s1-regularization-chosen-by-prior-theory-with-a-posterior-on-beta.json:21` carries the gate definition verbatim: G-P4 requires the posterior to reproduce the plateau balance *"re-based onto the exact analytic \|g_penalty\| = 4.785304e-04 computed from the archived beta field rather than onto the published ratio-and-cosine pair"*. That re-basing was **withdrawn at `57fd0d83` (20:30:59)**, whose own title is *"Withdraw the plateau-balance correction and restore G-P4 to ratio-and-cosine: the control it replaced could not fail"*, and `S1_PRIORS_PREREGISTRATION.md:379` now reads *"G-P4 is restored to the ratio-and-cosine form in §4."* The proposal was last touched at `b63b568e` (17:08:07) and nothing swept it. **An agent that picks this proposal up executes against a control that cannot fail** — the exact defect the withdrawal exists to remove — and a proposal JSON is a dispatch surface, not a narrative record. Doubly rotted: the same line cites `docket D8`, which now resolves to B2's seven CANDIDATES. | V15 round 6 rot hunt, 2026-08-11, subject `cb7122ba` | Sweep the proposal corpus for every gate definition whose source preregistration has been amended since the proposal was last touched -- `t_preregistration > t_proposal` is the two-timestamp detector `MEMORY_ARCHITECTURE.md` §8.4 already specifies and nobody has run over `agenda/proposals/`. Highest priority of the seven rot findings because it is the only one on a surface an agent acts from. | fleet |
| D42 | **D17 asks Katie for a ruling that the charter made 41 minutes after D17 was written, and the stale ask is repeated on four surfaces.** D17 (`docs/DOCKET.md:123`, written `0049463f` 18:17:54) says *"The chief has already made the rank-1 companion clauses mandatory on every surface; no equivalent travelling rule exists for the referent"*, owner **Katie / chief**, settled by *"a ruling, not a run"*. `docs/charters/VERIFICATION_CHARTER.md:772` -- **§6a, "The referent travels with the verdict (added 2026-08-11)"**, landed at `2d0e5260` (18:58:54) -- is exactly that generalised rule, and its own text cites this audit's figures as its justification. Three further copies are live: `docs/EXTERNAL_REFERENT_AUDIT.md:368`, `:744` (*"A parallel travelling rule for the referent does not exist"*) and `:811`. `docs/DOCKET.md:86` compounds it -- *"D17 is the one that would close the other five and is Katie's/chief's ruling"* -- so five docket items are parked behind a question already answered. The docket header says an item leaves *"by being executed or by Katie ruling it out -- never by ageing"*; this one was executed and stayed. | V15 round 6 rot hunt, 2026-08-11, subject `cb7122ba` | Close D17 against `2d0e5260`, unblock the five items behind it, and strike the three sentences in `EXTERNAL_REFERENT_AUDIT.md` in place. **This is chief work, not Katie's** -- the ask should never reach her. | chief |
| D43 | **Two docket pointers in a grade record no longer dangle -- they resolve, silently, to unrelated live defects.** `demo-output/website/campaign/V16_GRADE_ROUND5.md:142-143` files two findings as *"**docket D5**"* (a worktree cut behind the subject commit) and *"**docket D6**"* (the guard publishes four recall figures and no precision figure), repeated at `:149`. Those rows are now **D19** and **D20** -- `docs/DOCKET.md:46-47` says so in its own ID map. D5 and D6 today are the `adjoint_optimization.py` certificate-`unlink` swallow and the five acts taking the opposite failure, **both still live and unfixed** (verified: `unlink` at `adjoint_optimization.py:1427`; the five acts still carry `if cert_path.exists(): cert_path.unlink()` outside the try). `df53488e` (18:20:45) landed the round-5 file and renumbered the rows **in the same commit**, without rewriting the file's own prose; rounds 6 and 7 cite D20 correctly, round 5 was never swept. Same class, same evening: `S1_WITH_PRIORS_PREREGISTRATION.md:16` and `:571` still say **D8** where `ad02a331` -- the commit titled *"The refutation's own pointer had rotted twice while it sat there being right"* -- corrected only the occurrence at `:547`. | V15 round 6 rot hunt, 2026-08-11, subject `cb7122ba` | A resolving-but-wrong pointer is worse than a dangling one and no checker looks for it. `docs/DOCKET.md:32` already names the hazard -- *"An ID is a reference target; moving it breaks every citation you cannot see"* -- so the sweep is owed: every `\bD[0-9]+\b` in the corpus, resolved against the ID map, with the renumber commits `df53488e`/`ad02a331` as the positive-control set. **This is the strongest argument yet for W-4** and should be cited in it. | fleet |
| D44 | **D20 is E2's declared closing condition, it was met two hours forty-five minutes after it was filed, and the queue still shows it open.** D20 (`docs/DOCKET.md:194`, filed `df53488e` 18:20:45) states in the present tense that *"The placement guard publishes four recall figures and no precision figure … Nothing measures the opposite direction"*. `scripts/self_audit.py` now defines `_PLACE_PRECISION` and generates it into the verdict paragraph; `git log -S'_PLACE_PRECISION'` returns a single hit, **`64281b6b` (21:05:17)**. `LADDER_V_TRIPLE_VERIFICATION.md:325` says *"This is docket D20, and it is now E2's closing condition."* The row carries no closure marker while its siblings do (D1a *"Nothing further — executed"*, D26 *"Executed for this instance"*), so a reader working the queue rebuilds a figure that already exists. **Related and unswept:** `LADDER_V_TRIPLE_VERIFICATION.md:364-365` still tells a reader *"20 of 41 is a worst case on hard sentences"*; grade round 7 falsified that at `db18553b` with an independent 19-of-25 (76%) against the published 49%, filed as D27 -- but round 7's "WHAT WAS FILED" list names neither the ladder record nor this human copy, so **the ladder's own closing narrative still carries the refuted bound**. | V15 round 6 rot hunt, 2026-08-11, subject `cb7122ba` | Mark D20 executed against `64281b6b`; strike the worst-case sentence in the ladder record against `db18553b`. Structurally: a docket item promoted to a rung's closing condition needs a back-pointer from the commit that closes it, or the queue and the rung disagree about whether the rung can close. | fleet |
| D45 | **A filed 340 core-min item's enabling premise is measured false, and the gate it is filed to cross has a bar above its own ceiling.** R1 (`RESEARCH_DIRECTIONS_2026-08.md`, `6773c36f`) was filed as R5's prerequisite because R5's §2a says *"whitening ... pushes correction away from high-sensitivity cells; **if those cells happen to sit outside the window**, G2 rises for a reason with nothing to do with turbulence."* R1 executed at zero compute (`S1_SENSITIVITY_VS_ERROR.md`): the top decile of `\|g(β=1)\|` from `S1-cbfs-reinversion/cbfs_inv/grad_eval001.npy` is **35.4% in-window against an 8.44% base rate — a ×4.19 enrichment, the largest of any region**, and the bottom decile of `\|g\|` is **90.0% above the shear layer, 1.8% in-window**. R5's "if" is false in the direction that reverses its prediction: whitening moves G2 down from 26.86%, not up toward 50%. **Independently of any whitening argument, the >50% bar cannot be met by any loss-following field**: the top decile of the *per-cell baseline loss* (`0/U` vs `0/UData`, the best case a physical correction could produce) lies **32.71%** in the window, so 26.86% is 82% of the achievable ceiling. This sharpens `CLOSURE_STAGE1_AND_C2_STATUS.md` §1.2, which put the ceiling at the window's 41.2% total loss share; the operative ceiling is the loss's own top-decile geography, **32.71%**. Same arithmetic closes R8's condition (a): the correction's geography is reproduced by a field containing no inversion result, so there is no physical geography to build a transferable prior on. **Reported with its own null stated:** the headline correlation (Spearman +0.9742) does **not** clear its null (+1.0000, an arithmetic identity), the TOO-CLOSE abort fires, and none of the numbers in this row depend on it. | R1 execution, 2026-08-11, `S1_SENSITIVITY_VS_ERROR.md` | **Do not buy R5 at 340 core-min in its current form**, and do not promote R8 into the ambition slot; both are premised on an answer now measured the other way. R5's G-W1 reproduction control is already discharged — both archived G2 values (29.0476%, 26.8571%) reproduce at zero compute. The one thing worth buying is PR-1, a second-QoI sensitivity map at β=1, **25 core-min** on the measured `anchor8` basis (599 s wall × 2 cores = 19.97 core-min): it is the only measurement in reach that separates "the adjoint is loud there" from "the closure is wrong there". | Katie / chief |
| D46 | **The placement guard reads WHETHER an ordinal is claimed and never WHICH board it is claimed on, and it reads no polarity and no mood.** Two false-FAULT shapes, both found by a held-out set built *outside* the lab (grade round 7, `campaign/V16_GRADE_ROUND7_PRECISION_SET.py`), both rule-A faults, both **FAIL severity on a travelling surface**. (a) *Other named board:* a genuine placement on a different and explicitly named ranking — another benchmark, a citation ranking, an internal timing table — is indistinguishable from a placement on this one, and naming the other ranking in the same sentence does not clear it. A submission document is a likely place to meet one. **2 of the 2 admitted sentences of this shape fault.** (b) *Negation, interrogative, counterfactual:* rule A tests neither polarity nor mood, so a sentence saying the *opposite* of a placement reads as the placement. **2 of 2 admitted fault.** Both are now counted in `_PLACE_FALSE_FAULT` with their per-sample counts and named as BLIND TO items 10 and 11, so they are **knowingly accepted** under the V16 ruling rather than unnamed — which is the whole difference between a residual and a defect. | V16 round 8, 2026-08-11, subject `db18553b`, landed `5e4a0cc5` | Neither closes with a word list; both need to read structure a regex does not carry (which ranking a sentence is about; whether an ordinal is asserted). **Nothing here is worth a fifth discriminator** — the V16 ruling says so explicitly, and four discriminators over six rounds each broke on a sentence their author had not tried. This is filed so the cost stays visible, not so it gets paid. It becomes worth paying if a travelling submission document is ever found to contain either shape — which is checkable today with the live sweep and has not been checked. | fleet |
| D47 | **The proviso test's independence is now exactly one author-written map deep, and that map is the remaining circularity.** Round 8 fixed D28: `test_every_false_fault_class_is_counted_and_named` no longer computes both sides over the author's own set, and it reddens when the outside set faults in a shape nobody enumerated (mutation-proved). But the assignment of each of that set's false FAULTs to an enumerated class — `_PLACE_FF_OUTSIDE` in `scripts/self_audit.py` — **is still written by the party being measured.** So the test can see a shape the author never *thought of*; it cannot see a shape the author **mis-filed into an existing class**, because nothing external checks the assignment. The assignments made in round 8 were checked by execution rather than read off the grader's own labels (two `LINALG` faults have different mechanisms — one is silenced by restoring the deleted relativizer, one is not — and they were filed to different classes accordingly), so the instance is sound; the *class* of defect is not closed. **This is L-74 one level further up again, and the pattern is worth more than this instance: each fix moves the circularity up a level rather than removing it, because the last step of any self-measurement is written by the self.** | V16 round 8, 2026-08-11, `5e4a0cc5` | A grader re-deriving the label→class assignment independently, from the sentences alone, and the two derivations compared — cheap, since the set and its labels are committed and recompute. Or a third precision set from a third builder, which would test the enumeration's coverage again without testing the assignment. **The general form for the chief:** where a self-measurement's last step is an author's judgment, name that step in the record so a grader knows where to aim, rather than reporting the measurement as fully external. **CONFIRMED AND SHARPENED BY EXECUTION — V16 grade round 9, 2026-08-11, subject `e16d7329`** (`__pycache__` purged before every cell; control and mutants in one invocation): a **single** mis-file reddens the test, because the per-class totals move — so the row's own wording is stronger than the defect. What is invisible is a **count-preserving swap**: exchanging the class of one `other-named-board` label with one `quotation` label leaves both class totals unchanged and the suite **green**. That is narrower than "cannot see a mis-file" and exactly as bad, because a totals-preserving swap is the mis-file an author would actually make. **The remedy this row proposes was also executed and the instance is discharged:** all nineteen assignments were re-derived from the sentences alone, using restoration of the deleted relativizer as the discriminator between `reduced-relative` and `linalg-head-unlisted` (a head on `_PLACE_LINALG_NEAR` goes silent when the relativizer is restored; one off the list faults either way), and **all nineteen agree with `_PLACE_FF_OUTSIDE`.** The *class* of defect stays open; this instance does not. | fleet |
| D48 | **The placement guard's board is a pinned snapshot, the world has moved under it, and the guard's own eleven-item BLIND TO list does not say so.** `scripts/self_audit.py` reads its board from the frozen benchmark clone's README at `deb9155` (dated 2026-05-04) and validates every placement sentence against the four entrants that table lists. `demo-output/website/campaign/BOARD_MOVED_2026-08-11.md` (`9cb2a20a`, fetched and verified twice by two routes) records the live board as **six** entrants with a new leader. **Executed at `be36e22f`:** six probe sentences that are TRUE of the live board all FAULT, and the two that are true only of the pin stay silent — so the guard is presently able to fault a correct sentence at FAIL severity on a travelling surface, and to clear a false one. **Latent, not live:** the corpus sweep is WARN on the same two lab-record quotations of D3, no tracked surface currently states a live-board placement, and submission is PARKED. **The pin itself is correct and must not be moved** — V1 needs a frozen scoring reference and `BOARD_MOVED` §4 says so explicitly. What is missing is disclosure: the verdict names its board by commit, annotated *"at the commit this lab's claims are dated to"*, which reads as alignment rather than as age; and none of BLIND TO items 1–11 names the referent's currency, though item 9 names the symmetric hazard one level down, for the SENTENCE (*"whether a placement is dated history rather than a live claim"*). | V16 grade round 9, 2026-08-11, subject `e16d7329` (ancestry OK; four-path content diff against HEAD `be36e22f` empty) | **Ruled OUTSIDE V16 and filed rather than fixed.** E2's closing condition governs how the guard READS a sentence, measured over held-out sets that are themselves defined against whatever board `_published_board()` returns; board currency is orthogonal to that frame, and no written claim the guard makes is falsified by it — it says "the board", defines it by commit in the same verdict, and never asserts currency. Cheapest settlement: a twelfth BLIND TO item naming the pin's age and the direction of the error, **generated from the pin rather than typed**, so it cannot go stale the way the figures did (L-79). The separate and larger decision — whether a rank check should fetch the live board rather than the scoring pin — is `BOARD_MOVED` §4's and is not this row's. **DISCLOSURE SETTLED 2026-08-11 by V16 round 10, subject `067caac0`, carried alongside D49 because it is one line.** BLIND TO item **(12) WHETHER ITS OWN BOARD IS STILL CURRENT** now stands in the frame, naming itself as item 9's hazard one level up — for the REFERENT rather than the sentence — and stating that if the board has moved the error runs **both** ways: a sentence true of the live board FAULTS, one true only of the pin stays SILENT. **Generated from the pin, not typed:** the entrant count comes from the parsed board, the commit from `_published_board`, and the date from a new `_board_pin_date()` that reads the clone's own HEAD (an unreadable clone yields "date unreadable" rather than a guess). `test_the_verdict_admits_it_cannot_tell_its_board_is_stale` asserts the item carries what those functions actually return, so moving the clone moves the item or reddens the suite — the one thing an item about staleness must not do is go stale. The item says explicitly that the pin is deliberate and **not** a defect to repair there, so it cannot be misread as licence to move the scoring pin. A prose parallel was added to `BASIS` carrying no pin values. **The row stays open**: `BOARD_MOVED` §4's larger question is untouched, and the nine surfaces carrying the stale `P(rank 1) = 68%` are not this row's either. | fleet |
| D49 | **The two precision rows are built by two different admission rules, and the 27-point spread the verdict publishes is mostly that difference rather than the builder's.** `_place_precision_sentence` asserts, in the generated verdict and in BASIS, that *"a set's builder is a variable in the result, **the two here differ in that variable and in nothing else**"* and that *"**the same admission rule applies to both**, so neither denominator can be padded with sentences the guard never looks at."* Two different predicates implement that one rule. The author's set admits on a **raw pattern match** (`_place_pattern(upto).search(_place_flatten(s))`, asserted at import for all 41). The grader's set admits on **`len(_placements(...)) > 0`**, which applies `_RANK_HOMONYM`, `_PLACE_PROB`, `_PLACE_OFN` and the linear-algebra subject-head discriminator *after* the match — strictly narrower. **Executed at `be36e22f`:** the author's 41 drop to **28** under the grader's predicate, losing 13 sentences the guard examines and correctly clears (8 `linalg-head-listed`, 1 `coordination`, 4 `homonym`); the grader's 26 raw matches drop to **25** under it. All 13 are true negatives, so the author's numerator stays 20. **Score both samples under ONE rule and the headline moves: raw-match everywhere gives 49% vs 73%, a 24-point spread (Fisher two-sided p = 0.075); `_placements` everywhere gives 71% vs 76%, a 5-point spread (p = 0.763).** The published pairing — the *looser* rule on the author's set, the *stricter* one on the grader's — is the only one of the four combinations that is not internally consistent, and it is the one that maximises the gap and yields the p = 0.040 the ruling at `02e941c4` cited. **What this does NOT touch:** both rows recompute correctly from their own sentences; all four recall figures are unchanged; the false-FAULT enumeration and its per-class counts are unaffected; and the struck *"worst case on hard sentences"* stays struck, because 76% exceeds the author's rate under **every** common rule. What is falsified is the comparison narrative and the absolute carrying it. | V16 grade round 9, 2026-08-11, subject `e16d7329` (ancestry OK; four-path content diff against HEAD `be36e22f` empty); executed at `be36e22f`, suite 111 passed | **Material — this is the rung's fifth L-76 absolute and it sits in the generated verdict, the surface an outside reader reads.** Settlement is narrow and needs no fifth discriminator: pick ONE admission predicate, apply it to both samples, recompute both rows from it, and **replace "differ in that variable and in nothing else" with the measured sensitivity** — the spread is 5 to 24 points depending on the rule, and that range is itself the honest statement. If the raw-match rule is kept, say why a sentence the discriminator correctly clears belongs in a precision denominator; if `_placements` is kept, say why a correct silencing should not be counted as a precision success. Either is defensible; asserting the choice does not exist is not. **EXECUTED AND CLOSED 2026-08-11 by V16 round 10, subject `067caac0` (ancestry OK; four-path content diff against HEAD empty).** `_placements` was chosen and applied to both rows, and the reason is structural rather than a preference: `board_placement_faults`, whose output **is** the numerator, is built on `_placements`, so under the raw rule the numerator and denominator came from two different functions and a sentence the discriminator correctly cleared sat in a denominator it could never enter. **It is the less flattering choice and the row it worsens is the author's own — 49% → 71%, numerator unmoved at 20** — and the code says so. Published now: author **20 of 28** of 41 held; grader **19 of 25** of 43 held; spread **5 points**, and both set sizes reach the reader for the first time. The absolute is struck and kept: the verdict states the admission rule moves the spread across a **5-to-24 point range** and that the builder effect is **confounded** with it, all recomputed from a new `_PLACE_ADMISSION` table rather than typed (L-79), with both falsified clauses added to `_FALSIFIED`. The strike on "worst case" survives and no longer rests on prose: `test_the_precision_spread_is_measured_under_both_rules` asserts the grader's rate exceeds the author's under **every** rule in the table, and pins that the numerator does not move with the rule (all 13 dropped sentences being true negatives). Recall figures, class enumeration and per-class counts unchanged; no fifth discriminator; no parsing touched. Mutation-proved with `__pycache__` purged before every cell and control and mutants in one invocation. **The transferable shape, and the reason three rounds missed it: every row recomputed correctly from its own sentences, and a per-row check cannot see a defect that lives in a PAIRING.** The new test asserts the property that was absent — that both rows were admitted by one rule — rather than the property each row already satisfied. | fleet |
| D50 | **The false-positive rate the docket publishes for the L-76 checker and the rate the checker recomputes are measured on two different samples, and neither reproduces the other.** `scripts/check_absolutes.py` states in its own docstring that *"the rate is recomputed from that file by `measure_against_sample()`"*, where *that file* is `SAMPLE_PATH` = `sdk/tests/fixtures/absolute_claims_labelled.json` -- **100 records, `drawn_at_commit` `8ebe2b8e`**. B3a's published **74% (37 of 50 flagged)** was measured on the *other* fixture, `absolute_claims_labelled_second_instance.json` -- **75 records, `drawn_at_commit` `ecbdc288`, `checker_commit` `8a8b5392`**, a different labeller with a different `label_question`. **Executed at `f6b4eec0`, both files through the same `measure_against_sample()`:** SAMPLE_PATH gives **fp 42 of 70 flagged = 60.0%**, fn 5 of 33 = 15.2%; the second-instance file gives **fp 41 of 54 flagged = 75.9%**, fn 2 of 15 = 13.3%. So the second-instance file does not reproduce 37-of-50 either -- the flagged count has moved from 50 to 54 because the checker has changed since `8a8b5392`. Three numbers, one docket row, and the module's docstring points at the sample that is NOT the published one. | found while settling B3c/D32, 2026-08-11, executed at `f6b4eec0` | **B3a's territory, not this rung's -- filed rather than fixed.** Settlement is a choice, not a merge: name which sample is the lab's rate and say so in B3a with its record count and `drawn_at_commit`, or publish both with their two `label_question`s side by side, since a rate is a rate over a labelling rule as much as over a checker. Either way the module's docstring and B3a must name the SAME file. A citation asserting the two matched was struck from the module rather than written, because it would have asserted something false. | fleet |
| D51 | **The L-76 checker's `<command>` citation -- the exact token that laundered thirteen fictional test names -- can be conjured by ordinary English.** `COMMAND_RE` includes the alternative `make\s+\w+`, so the sentence *"Skips that make the verdict UNKNOWN rather than merely narrower"* (the `Skips.blinding` docstring, in the checker's own source) resolves to a runnable command and backs every absolute in its unit. **Executed at `f6b4eec0`** over `scripts/check_absolutes.py`: that unit reports `resolved=['<command>']` with no command in it. The unit happens to contain no absolute, so nothing is mis-verdicted **in this file today** -- the finding is that the token is reachable from prose, and D32's laundering mechanism needed exactly one such token. Related and unmeasured: `FILE_FUNC_RE` matched the metasyntactic placeholder `path.py:function` out of the module's own prose (fixed in this rung by writing it `<path>.py:<function>`), which is the same defect in the other direction -- a citation regex reading illustrative prose as a citation. | found while mapping the checker's own citations, 2026-08-11, executed at `f6b4eec0` | **B3a/B3b's territory (matching precision) -- filed rather than fixed, because retuning `COMMAND_RE` without replaying the labelled sample is how a checker gets tuned to a pretty number.** Candidate narrowing: require a command to sit inside backticks, a fenced block, or at the start of a line. **It must be measured against the hand-labelled sample in BOTH directions before it ships** -- the rate over `<command>`-backed units has never been measured at all, which is the same gap D32 records for `CITES_MISSING_CHECK`. | fleet |

## E. Standing task-list items not yet executed

| # | Item |
|---|------|
| E1 | **MEASURED 2026-08-11: it is 31 of 34, not 30.** Executed every `check_*` in `scripts/self_audit.py` and searched both `summary` and `detail` for a stated blind spot. **Three state one; thirty-one state none; zero raised.** So the audit's own checks are almost entirely silent about what they cannot see — and a green check that does not say what it is blind to reads as coverage. The three that do are the recently-built ones, which suggests the practice is arriving rather than eroding. *Positive control, and it was needed: my **first** run of this measurement returned **0 of 34** — a clean, confident zero produced by searching only `summary`, when the blind-spot text lives in `detail`. `check_board_placement_words` is the known-positive that a working method must find, and the first method did not. The corrected method finds it. This is the third instrument tonight whose first answer was a false zero.* | Print them. The measurement is the easy half; the thirty-one texts are the work, and each is a claim about a check's reach that its author has to be able to defend. |
| E2 | **DIAGNOSED 2026-08-11. Four of 146 `.done` records are zero bytes**, and all four carry the *same* timestamp — `f6b2_{coarse,medium,fine,veryfine}_20260805T171046Z` — so one event killed four collectors at once. **Mechanism:** `scripts/launch_solve.sh:427` ends its collector with `} > "$REC"`, and the shell **creates and truncates `$REC` at redirection time, before the block runs**. If the disowned subshell dies first — box power-off, session kill — the file survives at zero bytes. **Why it matters:** a zero-byte `.done` is indistinguishable from a completed record to anything testing existence rather than content, which is the fail-open shape; and **146 `.done` records are load-bearing evidence** cited across `INSTRUMENT_INTEGRITY_LEDGER.md`, `LEDGER_HEADLINE_AUDIT.md` and `PRODUCT_LIST.md`, with proposal P6 naming a 146-record corpus replay as a prerequisite. Four of the records that replay would read are empty. The class is already known — `campaign/DPW8_V2_joukowski.md:214` says *"Check the `.done` body, not merely its existence"* — but was never made structural. | **Write the record atomically**: build it at `$REC.partial`, then `mv` into place, so a `.done` either exists complete or does not exist. A rename within one directory is atomic; a redirection is not. Then the existence test becomes true again and every consumer is correct without changing. The four existing empties are evidence of a real interruption and should be marked, not deleted. *(Registry is gitignored, so this is invisible to any repo-scoped sweep — L-75.)* |

## F. Repo professionalization — Katie's §7, 2026-08-11. **BLOCKED BY DESIGN.**

**The block is Katie's own sequencing and it is not a delay to be worked around:** *"after
Ladder V converges — not during"*, because path moves under active worktrees create merge
chaos and moves during an open verification poison the ladder's paths. It sits here, in the
queue, rather than in flight.

**Release conditions, all four:**

1. Ladder V converged (the fixed point, not a clean sweep — see the ladder's termination rule).
2. Every family drained or checkpointed. No agent holding open work in the tree.
3. One dedicated agent, one branch `chore/repo-structure`, one quiet window.
4. A written `MOVE_MAP` (old → new, **every** file) committed **first**, before any move.

**Mechanics that bind that agent:** `git mv` only, history preserved; imports, launchers,
cron and systemd paths updated **in the same commits** as the moves; full test suite green
after **every batch**; the sanctioned `sweep()` helper re-pointed (it is B1's deliverable, so
B1 lands first); no history rewrite; nothing load-bearing deleted without a docket entry.

**Pointers that will dangle when `scripts/` moves under `ops/`** — a running list, because
each is a live cross-file dependency and G3 requires them updated *in the same commit as the
move*, not afterwards. Add to it as they are created; this is cheaper to maintain now than to
discover during the window:

- `scripts/corpus_figures.py` — `docs/MEMORY_ARCHITECTURE.md` §5 points at it by path, having
  replaced its hardcoded corpus counts with a call to it. If it moves and the pointer does
  not, the map's central section instructs the reader to run a command that does not exist.
  *(Raised by peer session `certonomous-3b`, whose agent holds the other half of that repair.)*
- `scripts/sweep.py` — B1's sanctioned helper, named by G3 explicitly.
- `scripts/self_audit.py` — invoked by name from the test suite and from several campaign
  records.
- `scripts/fail_open_scan.py` and `sdk/tests/test_fail_open_scan.py` — the scanner hardcodes a
  path to `scripts/self_audit.py` **and pins a historical commit for its positive control**. So
  a move breaks **the control rather than the scan**, which is the quieter and worse failure: a
  positive control that silently stops working is how a scan starts reporting confident
  negatives. *(Raised by the peer session; reasoning kept as they put it, because the reason is
  sharper than the fact.)*
- `demo-output/website/campaign/F7_runs/old_spec_readings.py` — reads tracked field files by
  relative path.

**Target shape** (proposed, then executed): product source under `certonomous/` or a retained
`sdk/`, with `workflows/`, `agents/`, `kernel/` beneath it; `ops/` for launchers, auto-stop and
preflight; `docs/` as the **only** documentation home, with `charters/`, `standards/`,
`research/`, `architecture/`; `cases/` for configs and birth certificates, **never results**;
`evidence/` gitignored with a README explaining what lives there and why it is untracked;
`web/` for site-bound artifacts. Root reduced to README, the LICENSE decision, top-level
configs, and directories. **Nothing loose at root.**

**READMEs:** root README = what Certonomous is in three sentences, a 30-second architecture
sketch, how to run one mission, a repo map at one line per directory, and a pointer to
`docs/`. Every top-level directory gets its own short README naming purpose and entry points.
The qg-closure Q2 standard applies — goals and structure, professional register, no stale
claims — and **every number quoted obeys L-79: regenerate it, or carry its provenance.**

**Acceptance is a test, not an opinion — the cold-visitor test.** An agent with no context
clones the branch and must answer, within one screen per level: what is this, how do I run a
mission, where are the charters, where is the evidence for claim X. **Every question it has to
dig for is a defect.** Then Sanaa walks the tree herself before merge. That second gate is a
**taste gate and taste is hers** — it is not delegable to a checklist.

**One finding already, made while filing this and worth landing before the move window opens:**
G5 asks that tracked generated artifacts move to a gitignored `evidence/` or leave tracking.
They exist in quantity **today** — `demo-output/website/campaign/F7_runs/F7a_R1/` alone tracks
OpenFOAM time directories (`0.025/U`, `alpha.water`, `phi`, `uniform/functionObjects/…`) as
version-controlled files. That is solver output in git. **Scoping it is zero-compute and can
be done now**; the moves themselves wait for the window with everything else.

## G. Naval campaign — Katie's §8, 2026-08-11. Executing the zero-compute half now.

| # | Item | Compute? | State |
|---|------|----------|-------|
| G1 | **H1 F7a re-gate.** Pin the measurement definition **contractually** in the gate spec — probe row, front criterion, time origin: the exact ambiguity that produced the false FAIL. Then re-run and take the verdict either way. | Spec: **no**. Re-run: **yes** | **DONE, and the re-run turned out not to be needed for the verdict.** `campaign/F7a_REGATE_SPEC.md` §2 pins all four definitions; §1 exhibits the old ambiguity as measured fact (+13.4% vs −16.1% on the same solve); §3 takes the verdict **FAIL, max +11.03%**, from already-tracked fields at **zero compute**. Compute request re-scoped and its expensive half recommended against — see G1a |
| G2 | **H2 Wigley hull.** Wave-making resistance vs published data at 2–3 Froude numbers; free-surface mesh discipline written into a new marine section of `MESH_STANDARD`; birth certificates as everywhere. | Yes | Mesh section and pre-registration can precede it |
| G3 | **H3 DTMB 5415.** Resistance vs open workshop data — the credibility case naval people recognise on sight. Staged per doctrine: feasibility → physics (wave pattern qualitatively right) → gate rung. | Yes, staged | Staging plan is zero-compute |
| G4 | **H4 extensions, filed as COSTED PROPOSALS and not auto-run:** KCS container ship; propeller open-water curve vs workshop data ~~(reuses F8's MRF machinery — the naval turbine)~~ **— see the amendment below, the reuse premise does not survive**; seakeeping / added resistance, which needs wave BCs we may not have, so **the deliverable is the capability-gap map, honest about what is missing.** | Filing: **no** | Filing now |
| G5 | **H5** Every naval gate lands on the credentials wall with regime metadata like everything else, and **Naval becomes a wall category.** Katie's GTM list already touches marine engineers, so these wall entries are sales artifacts the day they exist. | No | Follows each gate |

**State after the 2026-08-11 zero-compute pass.** G1 done (above). G2 mesh
section done — `docs/standards/MESH_STANDARD.md` §7, with §7.0 stating why it
landed there rather than in `docs/MESH_STANDARD.md`, and the Wigley comparison
stations pinned in `campaign/NAVAL_CAPABILITY_GAP_MAP.md` §6.2a. G3 staging plan
done — `campaign/F7c_DTMB5415_STAGING_PLAN.md`, now carrying a **Stage 0** it was
missing (see G3a). G4 done — three proposals filed, and the gap map delivered.

| # | Amendment | Owner |
|---|---|---|
| **G1a** | **The F7a compute request, re-scoped.** The re-gate needs no compute to produce a verdict, so the request is now: **buy the zero-core-min comparison-basis item (R0)**; **do not buy** the a/512 rung (R1a, **760–1,520 core-min**, 2–4× the entire R1 campaign) to settle the shape of a convergence sequence. Reason, measured: `res16_papermodel` — comparator physics, mesh and domain all matched — sits **+23.3% ahead of the comparator's own published curve**, an offset larger than the +8.2% residual the campaign has been chasing. Refining the mesh explores a direction already shown to be small. Priced in `campaign/F7a_REGATE_PREREGISTRATION.md` §5. | Katie |
| **G1b** | **The compressed version of the D-12 story is wrong, and D-12 names six files that carry it.** "The F7a FAIL was a measurement artifact" is false: the ambiguity is worth ~20 points of the *originally reported* number and **essentially none of the current one** — at dy ≤ a/32 every reading of even the old definition lands +7.8% to +11.9%, all failing 5%. The six files are not individually wrong; the summary that circulates is. Not edited here: six near-verbatim copies is exactly the amendment hazard D-12 exists to flag, and C3 gives the case record the home. | fleet |
| **G3a** | **DTMB 5415 has no verified open geometry route** — ITTC-cited navy host **DNS-dead**, `simman2014` **registration-walled**, `simman2008` **TLS cert mismatch**. Its reference *data* is the best of the four (ITTC 27th Resistance Committee: CT, sinkage, trim at Fr 0.1/0.28/0.41 across **eleven** tanks), which is what makes it look fundable until someone tries to mesh it. **Registration is an external interaction — PARKED and reserved to Katie.** Filed as Stage 0 of the staging plan. | Katie |
| **G3b** | **The naval ordering docket §8 implies is inverted by the evidence.** By blockedness: propeller open-water (**not blocked by the ladder rule at all**, 900 core-min) → wave-capability step (135) → KCS (3,000; data open and tabulated, geometry verified) → **DTMB 5415, the most blocked of the four**. Recommendation only; §8's sequencing was written before this evidence existed. | Katie |
| **G4a** | **`grade_f7a.py` averages spurious post-wall crossings into its reported mean** — it prints "mean −0.8%" for `res16_base` and "mean −6.7%" for `res16_papermodel`, both arithmetic over ≈ −87% artifacts produced when the front passes the far wall and the furthest-crossing search picks up a crossing back in the collapsing column. Fixed **by contract** in `F7a_REGATE_SPEC.md` §2.2 (monotonicity guard); **not fixed in the script**. B2's class. Any number taken from that script's unrestricted mean line is unframed until re-derived. | fleet |

**The honest note about H4's third item:** "needs wave BCs we may not have" is a claim about
our capability, and the deliverable Katie asked for is the **gap map**, not a workaround. An
agent that discovers we lack the boundary conditions and quietly substitutes something else
has destroyed the deliverable. The map is the product.

> **[AMENDED 2026-08-11 — G4's propeller premise did not survive checking. Original text
> retained above rather than edited, per the supersession rule; the claim is Katie's and she
> is entitled to see it challenged rather than quietly reinterpreted.]**
>
> G4 describes the propeller open-water case as *"reuses F8's MRF machinery — the naval
> turbine"*. That phrasing is Katie's, from §8 H4, and I transcribed it here. **An evidence
> audit of the F8 case files contradicts it on four points**, and pricing the proposal on the
> reuse premise would understate it badly:
>
> - **F8 is a WIND turbine in AIR** — `simpleFoam`, steady, incompressible (`nu 1.4805e-05`,
>   kinematic `p`, no thermophysical model), single-phase, Spalart-Allmaras.
> - Its MRF **is genuine** (a real `constant/MRFProperties`, `omega 7.5398`, zone actually
>   built by `topoSetDict` — not SRF, not AMI, not `rotatingWallVelocity`) — **but the rotating
>   zone is the entire domain**, which is the one thing a propeller case must not copy.
> - **F8's steady-MRF branch is closed on evidence three ways, with ZERO gated CFD numbers.**
>   Reuse would inherit a branch that never produced a gated result.
> - The geometry is a fixed 68 MB / 330,950-triangle STL with **no generator anywhere in the
>   repo**, so a propeller blade is **100% new work**, not a parameter change. And **the sign
>   convention is inverted** — F8 extracts power, a propeller absorbs torque — which is
>   precisely the error class that cost F8 three riders.
>
> **Status: routed to the naval agent to confirm or contradict against the case files
> itself**, because the audit reached me second-hand and this lab's rule is that a grader
> executes rather than relays. Not treated as settled here.
>
> **Calibration datum from the same audit, and it is load-bearing for every H4 price:** F8's
> own proposal carried `est_core_min: 6` against **47.7 core-min actual — an 8× overrun**,
> at a measured 0.54–0.60 core-s/iteration for 230k cells. That is the house's demonstrated
> optimism factor on a rotating case. *(A "0.145 core-s/iteration" figure in F8 §5 contradicts
> every other number in its own record and is not to be used.)*

> **[AMENDED 2026-08-11 — the F8 reuse premise in G4 does not survive checking, and the
> premise was mine to check.]** Katie's §8 describes the propeller item as reusing F8's MRF
> machinery, *"the naval version of the turbine"*. I transcribed that into G4 without opening
> F8. An agent sent to price the reuse found **both halves wrong**:
>
> - **F8 is a WIND turbine in AIR** — `simpleFoam`, steady, incompressible, single-phase,
>   Spalart–Allmaras — not a naval one. And its steady-MRF branch is **closed on evidence
>   three ways** with **zero gated CFD numbers**, so there is no validated result to inherit.
> - **The geometry does not transfer at all.** `blade.stl` is a fixed 68 MB, 330,950-triangle
>   S809 wind blade with **no generator anywhere in the repo**. A propeller blade is **100%
>   new work**.
> - **The sign convention is INVERTED** for a propeller, which absorbs torque rather than
>   producing it — and that is precisely the error class that cost F8 three riders.
> - What genuinely transfers is **dictionaries and process discipline**, which is real but is
>   not what "reuses the MRF machinery" prices at.
>
> One more thing found while pricing it, and it is a calibration fact about the whole proposal
> system rather than about F8: **F8 cost 47.7 core-min against an `est_core_min` of 6 in its
> own proposal — an 8× overrun.** Any naval estimate built by analogy to F8's *estimate*
> inherits that error; build them against its *measured* 0.54–0.60 core-s/iteration at 230k
> cells instead. (F8's own record also carries a "0.145 core-s/iteration" figure in its §5
> that is inconsistent with every other number in the same document; do not use it.)
>
> **And the thing not to copy:** F8's MRF cellZone is **the entire domain** — 230,135 of
> 230,135 cells. It ran, and it is not what an MRF zone is for.
>
> This is Katie's text, so the correction goes to her rather than being applied silently. The
> proposal is not withdrawn — a propeller open-water gate against PPTC/VP1304 is still worth
> filing — it is **repriced from scratch rather than by analogy**, and the report to Katie
> says the analogy was the thing that failed.
