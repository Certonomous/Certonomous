# dafoam — CAPABILITY GRID (Sanaa's taxonomy, `068c2bf0`)

**Written 2026-08-26 by dafoam lane Q2 for dafoam-supervisor, on the chief's dispatch under Sanaa's directive at `068c2bf0` (*"THIS IS THE ULTIMATE GOAL FOR US"*). Zero compute. The verification supervisor owns `docs/CAPABILITY_GRID.md` and audits every citation below; this file is the dafoam table it assembles from.**

**Cells** = dimension (2D / axisym / 3D) × time (steady / unsteady) × regime (incompressible / subsonic-compressible / transonic / supersonic / hypersonic / multiphase-free-surface) = **36 cells**. **Two verdict columns**, in Sanaa's words: *"gradients computed + FD-verified"* and *"optimization converged"*. **Every cell carries exactly one verdict** from `CAN DO` / `CAN DO, CAVEATS` / `CAN NOT DO`; an unattempted cell reads `CAN NOT DO — not attempted`. **Every verdict is entered from the record at HEAD, never from a board summary**; each citation is a repository path plus the abbreviated sha of the commit that last touched that record (every one resolves under `git cat-file -e`; the footer lists them all with their results). The lab's verdict vocabulary (`PASS` / `GATE REACHED` / `GATE FAIL` / `NOT A RESULT` / `BLOCKED` / `PENDING`) is quoted inside the cells exactly as the records use it — Sanaa's three-value word is the cell's summary, never a replacement for the record's token.

**Regime per case, derived from the case's registered flow conditions by path:**

| case family | solver / conditions (path) | cell |
|---|---|---|
| A1 NACA0012 (D1, D1-C′, D2, D13, A_stepsize) | `DASimpleFoam`, `U0 = 10.0`, 4,032 cells — `cases/dafoam/ladder-a/A1/curriculum_D1/PREREGISTRATION.md:21-43` ("this 2D case") | **2D · steady · incompressible** |
| B3 / S1 CBFS field inversion | `DASimpleFoam`, 21,000 cells, 2D duct — `cases/dafoam/ladder-b/B3/decomposition_np4/RESULTS.md` | **2D · steady · incompressible** |
| A4 Ahmed body (D3, first/shipped optimisation) | `DASimpleFoam` — `cases/dafoam/ladder-a/A4/first_optimisation_np1/RESULTS.md:37` | **3D · steady · incompressible** |
| A5 U-bend (D9, D10) | `DASimpleFoam`, `U0 = 8.4 m/s` — `cases/dafoam/ladder-a/A5/curriculum_D9/PREREGISTRATION.md:19` | **3D · steady · incompressible** |
| D11 MRF rotor probes | `DASimpleFoam` + `MRFProperties` — `cases/dafoam/probes/curriculum_D11_mrf_probe/PREREGISTRATION.md:15-16` | **3D · steady · incompressible** |
| A2 MACH wing (D4, D4-SHIPPED, D14-M) | `DARhoSimpleFoam`, `U0 = 100 m/s`, `T0 = 300 K` → M ≈ 0.29 — `cases/dafoam/ladder-a/A2/curriculum_D4/PREREGISTRATION.md:24`; `/home/ubuntu/certonomous-runs/A2-mach-wing/runScript.py:28-39` | **3D · steady · subsonic-compressible** |
| A3 ONERA M6 (D7, D7R, D7FR, sweep rungs) | `U0 = 291.6 m/s (M 0.84)` — `cases/dafoam/ladder-a/A3/curriculum_D7R/PREREGISTRATION.md:138` | **3D · steady · transonic** |
| A6 CRM wing-alone (D8, N=16 rungs) | `DARhoSimpleCFoam`, transonic, `U0 = 295 m/s` — `cases/dafoam/ladder-a/A6/curriculum_D8/PREREGISTRATION.md:44-45` | **3D · steady · transonic** |
| D12 / D12R / D12R2 / W2 / W2R cylinder | `DAPimpleFoam`, time-accurate unsteady adjoint, 2D, `Re_D = 1.0e6` — `cases/dafoam/curriculum_D12R2/PREREGISTRATION.md:1,313,331` | **2D · unsteady · incompressible** |

---

## The table

| cell | gradients computed + FD-verified | optimization converged |
|---|---|---|
| **2D · steady · incompressible** | **CAN DO — 6 cases** (D1, D1-C′, D2, D13, A1 re-verification, A_stepsize); **strongest case `cases/dafoam/ladder-a/A1/curriculum_D13/RESULTS.md` @ `15767999`**; checked: FD-vs-adjoint relative error per DV at ~~three~~ **five** independently converged endpoints, **4/4 components each, worst ~~0.2568 / 0.2540 / 0.2557 %~~ 0.2568 / 0.2540 / 0.2557 / 0.2591 / 0.2582 % — family-worst 0.2591 % (s4; `RESULTS.md:29`, `:234` "worst relative error 0.2591 %")** *(corrected 2026-08-26, Correction 1 below)*, zero sign flips, on the patched IDWarp image; the SHIPPED row is **design-point dependent** — `GATE FAIL` **11.4274 %, one sign flip** at the baseline (`ladder-a/A1/reverify_patched_idwarp_np1/RESULTS.md` @ `be35dcad`) and **PASS** at the converged optimum (`ladder-a/A1/curriculum_D1_Cprime/RESULTS.md` @ `cffd90e7`); step-plateau sweep (`ladder-a/A_stepsize_study.md`); np-invariance measured on B3 CBFS: serial vs scotch vs simple decomposition, **G4 `GATE FAIL` as written** — ~~gradients differ by decomposition~~ **G4 is the OBJECTIVE bit-identity gate: objectives spread 1.9e-07, not bit-identical (`:25`); the gradients agree with the serial reference to 1.1–1.7e-4 (`:18`)** *(corrected 2026-08-26, Correction 1 below)* (`ladder-b/B3/decomposition_np4/RESULTS.md` @ `bb5088c4`); B3 shipped adjoint `BLOCKED` (PETSc `-9`), passes only on the sub-LU patch (`ladder-b/B3/adjoint_unblock_reproduce/RESULTS.md` @ `3f8c6b13`); dot-product/duality and complex-step **not checked** anywhere in this family. | **CAN DO — 4 cases** (D13 ~~three~~ **five** restarts *(corrected 2026-08-26, Correction 1 below)*, D2 two optimisers, D1); **strongest case `ladder-a/A1/curriculum_D13/RESULTS.md` @ `15767999`**: ~~three~~ **five** starts each reach `EXIT: Optimal Solution Found.` in 9–11 majors at `CD = 0.017527…` (spread ~~≤ 2e-7~~ **2.036213e-07**, `:123`), each endpoint FD-verified 4/4 ≤ 0.26 % (worst 0.2591 %) *(corrected 2026-08-26, Correction 1 below)* — the item's cross-start BASIN verdict is `GATE FAIL`, 15 of 15 pairs `DIFFERENT` on the design vector while 0 of 15 differ on drag (`:34-36`): a flat valley floor, not a point; reported here because it is the record's own headline, and it is not a caveat on optimiser convergence; D2 IPOPT **and** SLSQP both terminate `Inform = 0` with endpoint FD PASS 0.2553 / 0.2485 % (`ladder-a/A1/curriculum_D2/RESULTS.md` @ `b840fcd5`); D1 arm O PATCHED `PASS`, arm C shipped endpoint `BLOCKED` then `PASS` under D1-C′ (`ladder-a/A1/curriculum_D1/RESULTS.md` @ `b10260a0`). Checked: optimiser convergence line + endpoint FD, both rows. |
| **2D · steady · subsonic-compressible** | ~~CAN NOT DO — not attempted; **`PENDING: cases/dafoam/ladder-a/A1/curriculum_D15/PREREGISTRATION.md`** @ `8fc2bdeb` (D15, `DARhoSimpleFoam` M 0.288, two-row X + FD chain frozen and on the drop path as `verification/queue/dafoam/D15_chain.json`; a queue state, no verdict — *Correction 1a*)~~ **CAN DO, CAVEATS — 1 case (D15)** *(corrected 2026-08-31, Correction 4 below — the `PENDING` note was a queue state that outlived its run: D15 launched 2026-08-27T11:32:30Z and graded the same day)*; **strongest (and only) case `cases/dafoam/ladder-a/A1/curriculum_D15/RESULTS.md` @ `aa1ac179`**, on the registration frozen at `…/curriculum_D15/PREREGISTRATION.md` @ `8fc2bdeb`: `DARhoSimpleFoam` at M 0.288 on the A1 NACA0012, **4,032 cells**, two hashed toolchain rows (SHIPPED `.so` md5 `f0fcb488…` / PATCHED `85f59e87…`, `G9 PASS`), adjoint-vs-central-FD per DV on `CD` and on `CL`, band D 5.0 % per component and band E 5.0 % aggregate, three registered steps {1e-2, 1e-3, 1e-4}, grading path re-hashed 8 of 8 MATCH against the freeze blob. **Item verdict `GATE FAIL`.** **SHIPPED row `GATE FAIL`** — worst **44.8738 %** on `CD` `shape[6]` against the 5.0 % band, 3 of 5 components outside it, zero sign flips, `CD` aggregate 34.6807 %; its cause class is **`PHYSICS-FAIL` — against the SHIPPED TOOLCHAIN** (`docs/dafoam/GRADING_CHAIN.md:46`, re-checked and standing at `:81`). **PATCHED row `PASS`** — 5 of 5 on both functions, **`CD` aggregate 0.047405 %**, **`CL` aggregate 0.0099 %**, zero sign flips. **CAVEATS, all carried WITH the verdict:** (a) **the item is a `GATE FAIL`** and this cell's `CAN DO` rests on the PATCHED row alone — the SHIPPED toolchain cannot do this cell; (b) **single mesh, 4,032 cells, no grid family, NO GCI is quoted** (standing rule 5 has no row; the grader says so in its own `no_gci` field); (c) **`G6` dot-product/duality `NOT MEASURED`** — the tutorial exposes no such test, named and never composed; (d) **the PATCHED row's `CD` `shape[7]` plateau is ONE-SIDED** — `plateau_neighbour_pct = [1.1559 %, 21.6299 %]` against the registered 10.0 % tolerance — disclosed in that record's dated 2026-08-31 amendment (@ `aa1ac179`): the frozen rule tests `min()`, so it was entitled to conclude the graded step is *not isolated* and **never** that it sits in a *plateau*, and the fine-end break is **NOT diagnosed** (cancellation is a hypothesis with a ~50× gap). That qualifies the PATCHED row's **precision** on one component and nothing larger; the SHIPPED failures are unaffected — `shape[6]` (44.8738 %) and `shape[0]` (12.2745 %) sit on fully two-sided plateaus and either alone carries the row. | CAN NOT DO — not attempted (D15 runs no optimiser) |
| **2D · steady · transonic** | ~~CAN NOT DO — not attempted; **`PENDING: cases/dafoam/ladder-a/A1/curriculum_D16/PREREGISTRATION.md`** @ `3ccb0c81` (D16, `DARhoSimpleCFoam` M 0.685, frozen, `verification/queue/dafoam/D16_chain.json`; a queue state, no verdict — *Correction 1a*)~~ **CAN DO, CAVEATS — 1 case (D16)** *(corrected 2026-08-31, Correction 4 below — the same outlived queue state: D16 launched 2026-08-27T11:33:35Z and graded the same day)*; **strongest (and only) case `cases/dafoam/ladder-a/A1/curriculum_D16/RESULTS.md` @ `aa1ac179`**, on the registration frozen at `…/curriculum_D16/PREREGISTRATION.md` @ `3ccb0c81`: `DARhoSimpleCFoam` at M 0.685 (`transonicPCOption 1`) on the SAME A1 NACA0012 mesh, **4,032 cells**, the same two hashed toolchain rows, the same bands and the same three steps; grading path re-hashed 8 of 8 MATCH. **Item verdict `GATE FAIL`.** **SHIPPED row `GATE FAIL`** on two components — `CD` `shape[0]` at **5.1511 %** against the 5.0 % band (a marginal crossing; `CD` aggregate 1.9648 %, band E `PASS`) and `CL` `shape[6]` at **41.8022 %** (`CL` aggregate 4.5797 %) — zero sign flips; cause class **`PHYSICS-FAIL` — against the SHIPPED TOOLCHAIN** (`docs/dafoam/GRADING_CHAIN.md:46`). **PATCHED row `PASS`** — 5 of 5 on both functions, **`CD` aggregate 0.146003 %**, **`CL` aggregate 0.1758 %**. **CAVEATS, all carried WITH the verdict:** (a) **the item is a `GATE FAIL`** and this cell's `CAN DO` rests on the PATCHED row alone; (b) **the registered prediction P5 scored `MISS`** — it named SHIPPED `shape[6]` on `CD`, and that component read 1.95 %, inside the band, `PASS`: **the component the defect lands on MOVED** between M 0.288 and M 0.685 (`shape[6]` → `shape[0]`), so the row failed while the falsifier built to detect it missed (the supervisor's 2026-08-27 check-3 sweep inside the same record; the lesson is `L-352`); (c) **single mesh, 4,032 cells, no grid family, NO GCI is quoted**; (d) **`G6` dot-product/duality `NOT MEASURED`**; (e) **the PATCHED row's `CL` `shape[6]` plateau is ONE-SIDED at the COARSE end** — `plateau_neighbour_pct = [14.0978 %, 1.1268 %]` against the registered 10.0 % — disclosed in that record's dated 2026-08-31 amendment (@ `aa1ac179`). Unlike D15's, this break **IS diagnosed**: truncation / shock motion under a 1e-2 shape perturbation at S/N 901,738, with the plateau at 1e-3 and finer. The exposure is on the **constraint** Jacobian, not the objective — D16's PATCHED `CD` is the only one of the four PATCHED functions across D15/D16 two-sided on all five components — and a lift-constrained drag minimisation follows both. | CAN NOT DO — not attempted (D16 runs no optimiser) |
| **2D · steady · supersonic** | CAN NOT DO — not attempted (~~the image ships `Cone_Supersonic`; never staged~~ **the tutorial checkout carries `Cone_Supersonic`** — neither image ships the DAFoam tutorials; the checkout is on the host at `/home/ubuntu/dafoam-tutorials` @ `d3b7e38b` (UPDATE N); never staged; **D17 on that case: pending freeze, lane N2** — no `curriculum_D17` directory or commit exists at this write, so no `PENDING: <path>` can be cited yet — *Correction 1a*) | CAN NOT DO — not attempted |
| **2D · steady · hypersonic** | ~~CAN NOT DO — not attempted~~ **CAN DO, CAVEATS — 1 case (D18)**; **strongest (and only) case `cases/dafoam/curriculum_D18_cone_hypersonic/RESULTS.md`** *(no sha — the record is created by the commit that makes this edit; disclosed, Correction 3)*: `DAHisaFoam` on the `Cone_Supersonic` tutorial re-staged as a planar 2-D wedge (half-angle 11.3099°, 40,000 cells) at **M 5.0002** (gate `G-MACH` `PASS`, `U0 = 1736.0`, `a = 347.1887`); item **`PASS`**, **both rows `PASS`** on two hashed toolchains (SHIPPED `sha256:9d45679d…` / `.so` `f0fcb488…`; PATCHED `sha256:2927768a…` / `.so` `85f59e87…`, `G9 PASS`). Checked: adjoint-vs-central-FD per DV on `CD`, band D 5.0 % per component, band E 5.0 % aggregate, three registered steps {1e-2, 1e-3, 1e-4} with a 10 % plateau rule — **4 of 5 registered components graded, 4 of 4 `PASS`, zero sign flips, aggregates 0.1305 % (SHIPPED) / 0.1459 % (PATCHED)**; worst single component 3.2410 % (PATCHED `shape[0]`). Planted controls seen on both readers, worst residual **4.2718e-17**. **CAVEATS, all four load-bearing:** (a) **"hypersonic" here names a MACH NUMBER, not a modelled regime** — `perfectGas` + `hConst` `Cp 1005` constant + `mu 0` + `RASModel dummy` + slip wall, so **no real-gas effect, no vibrational excitation, no chemistry, no viscous or radiative heating is modelled** (`RESULTS.md` §0; registered at `PREREGISTRATION.md:13`, `:103`); (b) **`shape[3]` is `NOT A RESULT` (`NO_PLATEAU`) in BOTH rows and was never graded** — the row grades on the registered minimum of ≥ 3 (`curriculum_D17_cone_supersonic/PREREGISTRATION.md:50` clause (c)); (c) **single mesh, no grid family, NO GCI is quoted** (standing rule 5 has no row); (d) **no dot-product/duality check** — `G6` `NOT MEASURED`, the tutorial exposes none. **A caveat on the item's own P7 discrimination reading, carried so the cell cannot imply it:** P7 scored `MISS` (S/N 19.8691, i.e. "discriminating"), but its signal is the 64.3953 % SHIPPED-vs-PATCHED divergence at **`shape[3]` — the `NOT A RESULT` component** — while over the four graded components the same formula gives 0.1383; **the cell claims no shipped-vs-patched discrimination** (`RESULTS.md` §7a). | CAN NOT DO — not attempted **(unchanged — D18 runs no optimiser; `PREREGISTRATION.md:103`)** |
| **2D · steady · multiphase-free-surface** | CAN NOT DO — not attempted | CAN NOT DO — not attempted |
| **2D · unsteady · incompressible** | **CAN DO, CAVEATS** — the time-accurate unsteady adjoint **reaches** (`DAPimpleFoam`, `GATE REACHED`, `cases/dafoam/probes/curriculum_D12_unsteady_probe/RESULTS.md` @ `4d9d902b`), but **no admissible FD step**: D12R2 phase 1 `G12R-4` = `NOT A RESULT` — *"`h_min = 1.742838e-01` EXCEEDS the registered `h_max = 5.000e-02`; no admissible FD step exists at `W = 300`"* (`cases/dafoam/curriculum_D12R2/RESULTS.md` @ `65882eb3`); W2 `BLOCKED` (aggregate guard); ~~W2R at `W = 900` running with phases 2–4 and their plan steps queued agent-independently~~ **W2R at `W = 900`: phase 1 complete on disk (ledger `PHASE1_COMPLETE spent=105.2334 core-min`), the frozen comparator's plan step wrote `step_plan.json` `admissible: false`, `h_min = 1.575533e-01` vs `h_max = 5.000e-02`, and phase 2 launched nothing by its registered branch — its `RESULTS_W2R.md` is NOT yet at HEAD (owed by the W2R scoring lane), so no verdict from it enters this cell** *(corrected 2026-08-26, Correction 1 below)* (`curriculum_D12R2/W2R_PREREGISTRATION.md` @ `5d1f89cd`); its primary prediction P3 is again *no admissible step* (`W2R_PREREGISTRATION.md:96`). Caveats: no admissible FD step at W = 300; single mesh (2,450 cells); `St ≈ 0.53` is a resolution artifact never quoted as a Strouhal number. | **CAN NOT DO** — attempted: D12 → D12R → D12R2 phases 2–4 all gated on `G12R-11` ~~(`curriculum_D12R2/RESULTS.md` @ `65882eb3`)~~ **— defined at `cases/dafoam/curriculum_D12R2/PREREGISTRATION.md:277` @ `e6580910` ("`G12R-11` (optimisation)"), the token does not occur in the RESULTS record** *(corrected 2026-08-26, Correction 1 below)* — which authorises S8 only after an FD-verified gradient; the FD line stops at `G12R-4`, so the optimiser was never authorised (`curriculum_D12R2/RESULTS.md:12` @ `65882eb3`, and `:165` "PHASE 2 WAS NOT" launched). What would fix it: an admissible step at a longer averaging window (~~W2R, `W = 900`, running; phases queued~~ **W2R at `W = 900` returned `admissible: false` on disk; the next window is the W3 draft, `curriculum_D12R2/W3_PREREGISTRATION_DRAFT.md`, NOT frozen** *(corrected 2026-08-26, Correction 1 below)*) or an objective whose window derivative `δ_window` cancels by construction. |
| **2D · unsteady · subsonic-compressible** | CAN NOT DO — not attempted | CAN NOT DO — not attempted |
| **2D · unsteady · transonic** | CAN NOT DO — not attempted | CAN NOT DO — not attempted |
| **2D · unsteady · supersonic** | CAN NOT DO — not attempted | CAN NOT DO — not attempted |
| **2D · unsteady · hypersonic** | CAN NOT DO — not attempted | CAN NOT DO — not attempted |
| **2D · unsteady · multiphase-free-surface** | CAN NOT DO — not attempted | CAN NOT DO — not attempted |
| **axisym · steady · incompressible** | CAN NOT DO — not attempted | CAN NOT DO — not attempted |
| **axisym · steady · subsonic-compressible** | CAN NOT DO — not attempted | CAN NOT DO — not attempted |
| **axisym · steady · transonic** | CAN NOT DO — not attempted | CAN NOT DO — not attempted |
| **axisym · steady · supersonic** | CAN NOT DO — not attempted | CAN NOT DO — not attempted |
| **axisym · steady · hypersonic** | CAN NOT DO — not attempted | CAN NOT DO — not attempted |
| **axisym · steady · multiphase-free-surface** | CAN NOT DO — not attempted | CAN NOT DO — not attempted |
| **axisym · unsteady · incompressible** | CAN NOT DO — not attempted | CAN NOT DO — not attempted |
| **axisym · unsteady · subsonic-compressible** | CAN NOT DO — not attempted | CAN NOT DO — not attempted |
| **axisym · unsteady · transonic** | CAN NOT DO — not attempted | CAN NOT DO — not attempted |
| **axisym · unsteady · supersonic** | CAN NOT DO — not attempted | CAN NOT DO — not attempted |
| **axisym · unsteady · hypersonic** | CAN NOT DO — not attempted | CAN NOT DO — not attempted |
| **axisym · unsteady · multiphase-free-surface** | CAN NOT DO — not attempted | CAN NOT DO — not attempted |
| **3D · steady · incompressible** | **CAN DO, CAVEATS — 4 cases** (A4 two optimisations, A5 re-verification, D11 MRF probe); **strongest case `cases/dafoam/ladder-a/A4/first_optimisation_np1/RESULTS.md` @ `5d1718df`**: endpoint FD at the converged design **`PASS` 0.4936 %**, zero flips, patched image; A4 shipped row PASS 1.10 % at the baseline (`docs/dafoam/README.md:92` @ ~~`ddb99eca`~~ **`2a93ff27`** — the README did not exist at `ddb99eca` *(corrected 2026-08-26, Correction 1 below)*, record `ladder-a/A4_ahmed_body.md:195`). Caveats: A5 U-bend SHIPPED row `GATE FAIL` **46.84 %, two sign flips** (IDWarp rotation defect), PATCHED `PASS` on the aggregate band with **5 of 27 components outside** it (`ladder-a/A5/reverify_patched_idwarp_np1/RESULTS.md` @ `35171866`); D11 MRF adjoint `GATE REACHED` — agrees with a central FD (`probes/curriculum_D11_mrf_probe_Fprime/RESULTS.md` @ `52a213ad`) — but the base probe `NOT A RESULT` (all stages died); ~~D10 thermal objective reaches the adjoint (`GATE REACHED`) while its FD table is `NOT A RESULT` (`ladder-a/A5/curriculum_D10_probe_Fprime/RESULTS.md` @ `4d9d902b`)~~ **D10-P′ thermal objective (`DAFunctionWallHeatFlux`) reaches the adjoint — `GATE REACHED` (`ladder-a/A5/curriculum_D10_probe_Pprime/RESULTS.md:5` @ `4d9d902b`) — and its FD pair D10-F′ is `PASS`: `d(HFX)/d(patchV[0])` agrees with a central FD at a proved plateau step to 4.401007e-08 relative against a 5.0e-2 band, one component, np=1 (`ladder-a/A5/curriculum_D10_probe_Fprime/RESULTS.md:9-13` @ `4d9d902b`); the base D10 probe is `NOT A RESULT` — grader refused, exit 2, on its own planted control (`ladder-a/A5/curriculum_D10_probe/RESULTS.md:5` @ `52a213ad`)** *(corrected 2026-08-26, Correction 1 below)*; D9 U-bend `NOT A RESULT` (`ladder-a/A5/curriculum_D9/RESULTS.md` @ `f8916f36`). | **CAN DO, CAVEATS — 2 converged, 2 not**; **strongest case `ladder-a/A4/first_optimisation_np1/RESULTS.md` @ `5d1718df`**: `EXIT: Optimal Solution Found.` in 9 majors, NLP error 6.28e-07, **CD −7.478 %**, endpoint FD `PASS`; the shipped row also converges (`Optimal Solution Found.`, NLP error 6.9114e-08, ~~fewer majors~~ **6 majors (`:13-14`)** *(corrected 2026-08-26, Correction 1 below)* — `ladder-a/A4/shipped_optimisation_np1/RESULTS.md` @ `f9a59d47`). Caveats: **unconstrained A4 only** — the constrained Ahmed item D3 is `BLOCKED` on an instrument (`nom_addThicknessConstraints2D` surface name) across two attempts (`ladder-a/A4/curriculum_D3_attempt2/RESULTS.md` @ `6b8d6355`); D9 U-bend SLSQP driver failed (`G9-3 GATE FAIL`, probe `NOT A RESULT` @ `f8916f36`); single mesh per case. |
| **3D · steady · subsonic-compressible** | **CAN DO — 3 cases** (A2 grading confirmation, D4, D4-SHIPPED grader path); **strongest case `cases/dafoam/ladder-a/A2/curriculum_D4/RESULTS.md` @ `1697ea49` §11**: endpoint FD at the corrected optimum, `G5 PASS` **5 of 5 registered components, aggregate 0.1634 % against 5 %, zero sign flips, zero without a plateau**, np=4 `scotch`, patched image (`ARMF3_d4_grade_verdict.json` @ `4eae12f4`); the SHIPPED row at the baseline `PASS` six rows, `CD/shape` **1.714 %** (`ladder-a/A2/grading_confirmation/RESULTS.md` @ `35171866`). Checked: FD-vs-adjoint per DV with plateau, planted zero, decomposition determinism (G8), toolchain identity by `.so` md5 (G9). Noted, not a caveat on the gradient: the D4-SHIPPED item closed `NOT A RESULT` on its grader path before its F3 table was graded (`ladder-a/A2/curriculum_D4_SHIPPED/RESULTS.md` @ `b26b875c`); **D14-M shows the mesh is regenerable bit-for-bit** (`ladder-a/A2/curriculum_D14/RESULTS.md` @ `60cfd4c8`). Supporting mesh-reproduction citation, added 2026-08-26 (Correction 1): **D14-M item `PASS` — the pyHyp toolchain reproduces the 38,304-cell mesh with `points.gz` sha256 equal, P1–P6 6/6 HIT, 0.1833 core-min (C-135)** (`ladder-a/A2/curriculum_D14/RESULTS.md:1,11,18` @ `60cfd4c8`). | **CAN DO, CAVEATS — 1 converged (patched), 1 cap-stopped (shipped)**; **strongest case `ladder-a/A2/curriculum_D4/RESULTS.md` @ `1697ea49`**: PATCHED arm O `EXIT: Optimal Solution Found.` at 80 majors, **28.6758 % drag reduction at `CL = 0.5`**, rung `GATE REACHED` (G2 band A `GATE FAIL` on CL feasibility reported, endpoint FD `PASS`). Caveats: SHIPPED arm O stopped at `max_iter` 100 (`EXIT: Maximum Number of Iterations Exceeded`) and the item is `NOT A RESULT` on its grader path, 731.667 core-min named waste (`ladder-a/A2/curriculum_D4_SHIPPED/RESULTS.md` @ `b26b875c`); single mesh (38,304 cells); the number is a patched-IDWarp statement, not toolchain-independent. |
| **3D · steady · transonic** | **CAN DO — 4 cases** (D7FR two rows, A3 sweep rung 2, A6 N=16 fixed reference, A3 grading confirmation); **strongest case `cases/dafoam/ladder-a/A3/curriculum_D7FR/RESULTS.md` @ `2a93ff27`**: ONERA M6 at M 0.84, 42,120 cells, np=4 — **SHIPPED row `PASS` 5 of 5 (worst 1.959 %) and PATCHED row `PASS` 5 of 5, divergence 0.000 % on every component**, **item verdict `PASS` (`curriculum_D7FR/RESULTS.md:1` @ `2a93ff27` — "ITEM VERDICT: `PASS`"; added explicitly 2026-08-26, Correction 1)**, two distinct IDWarp `.so` md5s asserted; A3 rung 2 patched np=4 `PASS` (`ladder-a/A3/rung2_patched_idwarp_np4/RESULTS.md` @ `0f56460d`); A6 CRM N=16 with the FD reference fixed: `PASS` ≤ 5 % among graded components (`ladder-a/A6/rung_n16_fixed_reference/RESULTS.md` @ `66f42398`). Checked: FD-vs-adjoint per DV, plateau, np=4 decomposition disclosed. Scale limit, recorded: A3 at 399,360 cells and A6 at 579,072 cells are `BLOCKED` on memory/conditioning (`ladder-a/A6/adjoint_feasibility/RESULTS.md` @ `be35dcad`; `ladder-a/A3/grading_confirmation/RESULTS.md` @ `35171866`). | **CAN DO, CAVEATS — ~~0 converged to tolerance, 2 cap-stopped~~ 2 converged to tolerance, 2 cap-stopped** *(Correction 2)*; **strongest case `cases/dafoam/ladder-a/A6/curriculum_D8R/RESULTS.md`** — sha not citable: that record is introduced by the same commit that makes this edit, so no earlier commit touched it; disclosed, never approximated (*Correction 2*) — **CRM N=16 twist-only run TO CONVERGENCE on BOTH toolchain rows; item verdict `PASS`, rows SHIPPED `PASS` / PATCHED `PASS`**: both printed **`EXIT: Optimal Solution Found.`** (SHIPPED **12** majors, PATCHED **8**, `max_iter` 30), and **both endpoints are FD-verified 10 of 10 components each** (5 on `CD` + 5 on `CL` per row — 20 of 20 `PASS`, **zero sign flips**; aggregates SHIPPED 0.6458 % `CD` / 0.0884 % `CL`, PATCHED 0.9015 % / 0.1068 %; worst single component 3.9170 % against band D 5.0 %), on two distinct IDWarp `.so` md5s (`f0fcb488…` shipped, `85f59e87…` patched). **The improvement itself is a marginal `PASS` and is recorded as one:** 0.3025 % (SHIPPED) and 0.3040 % (PATCHED) drag reduction against a band whose lower edge, 0.29 %, is D8's own measured 3-major drop — 4–5 % above that edge, not a large gain. **THE 2 CAP-STOPPED REMAIN ON RECORD AND ARE NOT DELETED** — ~~strongest case~~ `ladder-a/A6/curriculum_D8/RESULTS.md` @ `9c241fe2`: CRM N=16 twist-only, `GATE REACHED` — stopped on its registered 3-major cap (`EXIT: Maximum Number of Iterations Exceeded.`), never `PASS` by `DAFOAM_CHARTER.md` §9. Caveats: D7R arm O (M6, 30 majors at `max_iter`) `NOT A RESULT` — the grader refused and the 30.40 % reduction lies outside the frozen band (`ladder-a/A3/curriculum_D7R/RESULTS.md` @ `2b50394a`); ~~no `Optimal Solution Found.` exists on record in this cell~~ **`Optimal Solution Found.` now exists on record in this cell, on BOTH rows (D8R, graded 2026-08-28)** *(Correction 2)*; single mesh per case; **no common-point SHIPPED-vs-PATCHED comparison exists** — D8R's two rows converged to different endpoints (5 components, divergence 0.0008–0.0545 %, recorded by its own grader as *"different endpoints; a reading"*, no gate attached). |
| **3D · steady · supersonic** | CAN NOT DO — not attempted | CAN NOT DO — not attempted |
| **3D · steady · hypersonic** | CAN NOT DO — not attempted | CAN NOT DO — not attempted |
| **3D · steady · multiphase-free-surface** | CAN NOT DO — not attempted (~~the image ships `JBC_Hull`; never staged~~ **the tutorial checkout carries `JBC_Hull`** — `/home/ubuntu/dafoam-tutorials` @ `d3b7e38b`, not in either image (UPDATE N); never staged — *Correction 1a*) | CAN NOT DO — not attempted |
| **3D · unsteady · incompressible** | CAN NOT DO — not attempted | CAN NOT DO — not attempted |
| **3D · unsteady · subsonic-compressible** | CAN NOT DO — not attempted | CAN NOT DO — not attempted |
| **3D · unsteady · transonic** | CAN NOT DO — not attempted | CAN NOT DO — not attempted |
| **3D · unsteady · supersonic** | CAN NOT DO — not attempted | CAN NOT DO — not attempted |
| **3D · unsteady · hypersonic** | CAN NOT DO — not attempted | CAN NOT DO — not attempted |
| **3D · unsteady · multiphase-free-surface** | CAN NOT DO — not attempted | CAN NOT DO — not attempted |

**Census:** ~~5~~ ~~6~~ **8** of 36 cells carry evidence (2D·steady·incompressible, 2D·unsteady·incompressible, **2D·steady·hypersonic**, **2D·steady·subsonic-compressible**, **2D·steady·transonic**, 3D·steady·incompressible, 3D·steady·subsonic-compressible, 3D·steady·transonic); ~~**31 not attempted**~~ ~~**30 not attempted**~~ **28 not attempted**. Gradient column: CAN DO 3, CAN DO CAVEATS ~~2~~ ~~3~~ **5**. *(census moved by Correction 3, 2026-08-28 — D18; the optimisation column is unchanged)* *(census moved again by Correction 4, 2026-08-31 — D15 and D16; the optimisation column is again unchanged, both items run no optimiser)* Optimisation column: CAN DO 1, CAN DO CAVEATS 3, CAN NOT DO (attempted) 1. **Checks never performed anywhere in the family, stated so the grid cannot imply them: dot-product/duality test, complex-step** (Sanaa's §6 list); **no grid family exists on any DAFoam case, so no GCI is quoted anywhere above** (standing rule 5). **Correction 1 (2026-08-26) moved no verdict: the census above is unchanged.**

---

## Correction 1 — 2026-08-26, dafoam lane G, on the verification supervisor's audit (`docs/capability/dafoam_GRID_AUDIT.md` @ `4228f216`)

**Method.** Every number, token and path cited in the five evidenced cells was re-read at `git show <sha>:<path>` (the script and its output are in the lane's report, not in this file); each line below is `VERIFIED` (the record says what the cell says) or `CORRECTED` (the cell now carries the record's own words, the original struck in the cell, never rewritten). **No verdict moved.** Zero compute.

| # | cell / citation | reading | source line(s) at the cited blob |
|---|---|---|---|
| 1 | 2D·steady·incompressible — D13 `curriculum_D13/RESULTS.md` @ `15767999`, start count and worst FD | **CORRECTED** — five starts, not three; family-worst **0.2591 %** (s4), s5 0.2582 %; CD spread **2.036213e-07**, not "≤ 2e-7"; the item's basin verdict `GATE FAIL` (15/15 `DIFFERENT` on the design vector) now stated | `:19` "five seeded perturbed starts"; `:26-30` per-start rows; `:234` "worst relative error 0.2591 %"; `:123` spread; `:34-36` basin verdict |
| 2 | same cell — A1 SHIPPED baseline `reverify_patched_idwarp_np1/RESULTS.md` @ `be35dcad`, 11.4274 %, one sign flip, `GATE FAIL` | **VERIFIED** | `:38` "**11.4274%** … **one flip, idx6, 640.3696%** … **GATE FAIL**" |
| 3 | same cell — D1-C′ endpoint `PASS` @ `cffd90e7`; D1 arm O PATCHED `PASS`, arm C SHIPPED `BLOCKED` @ `b10260a0` | **VERIFIED** | D1-C′ `:29`; D1 `:26-27` (PATCHED **PASS** / SHIPPED **BLOCKED**) |
| 4 | same cell — B3 decomposition `ladder-b/B3/decomposition_np4/RESULTS.md` @ `bb5088c4` "G4 `GATE FAIL` — gradients differ by decomposition" | **CORRECTED** — G4 is the objective bit-identity gate (spread 1.9e-07); the gradients **agree** with the serial reference to 1.1–1.7e-4 | `:25` G4 row; `:18` "their gradients agree with the **serial** reference to **1.1–1.7 x 10⁻⁴**" |
| 5 | same cell — B3 shipped adjoint `BLOCKED` (PETSc −9) @ `3f8c6b13`; D2 IPOPT + SLSQP `Inform = 0`, FD 0.2553 / 0.2485 % @ `b840fcd5` | **VERIFIED** | `3f8c6b13:33-37`; `b840fcd5:27` (0.2553 %), `:30` (`Inform = 0`), `:31` (0.2485 %) |
| 6 | 2D·unsteady·incompressible — D12 probe `GATE REACHED` @ `4d9d902b`; D12R2 `h_min = 1.742838e-01` vs `h_max = 5.000e-02`, `NOT A RESULT` @ `65882eb3` | **VERIFIED** | D12 `:5`; D12R2 `:12-13`, `:51`, `:54` (`h_min = 100·δ_eff/|g|`) |
| 7 | same cell — `G12R-11` cited to `curriculum_D12R2/RESULTS.md` @ `65882eb3` | **CORRECTED** — the token lives at `curriculum_D12R2/PREREGISTRATION.md:277` @ `e6580910`; the RESULTS record carries the substance at `:165` | audit §2 row 3; `PREREGISTRATION.md:277` "`G12R-11` (optimisation)" |
| 8 | same cell — W2R "running, phases queued" @ `5d1f89cd` | **CORRECTED** (status, from disk, no verdict) — phase 1 complete, `step_plan.json` `admissible: false`, `h_min 1.575533e-01`; `RESULTS_W2R.md` not at HEAD; P3 wording verified | run-root `ledger.txt` `PHASE1_COMPLETE spent=105.2334 core-min`; `step_plan.json`; `W2R_PREREGISTRATION.md:96` "P3 … STILL NO ADMISSIBLE FD STEP" |
| 9 | 3D·steady·incompressible — A4 `first_optimisation_np1/RESULTS.md` @ `5d1718df` 0.4936 %, 9 majors, 6.28e-07, −7.478 % | **VERIFIED** | `:15`, `:75`, `:13`, `:37` (`DASimpleFoam`) |
| 10 | same cell — A4 shipped row 1.10 % cited to `docs/dafoam/README.md` §3 @ `ddb99eca` | **CORRECTED** — `git show ddb99eca:docs/dafoam/README.md` does not resolve (the file did not exist at that commit); the record is `README.md:92` @ `2a93ff27` and `A4_ahmed_body.md:195` | `2a93ff27:docs/dafoam/README.md:92` "**PASS** … **1.10 %** at np=1" |
| 11 | same cell — A5 `reverify_patched_idwarp_np1/RESULTS.md` @ `35171866`: SHIPPED 46.84 %, two flips, `GATE FAIL`; PATCHED `PASS` on the aggregate band, 5 of 27 outside | **VERIFIED** (record words "2 flips, idx8 … idx17"; PATCHED "22/27 within ±12%" = 5 outside) | `:34`, `:35` |
| 12 | same cell — D11 MRF: F′ `GATE REACHED`, agrees with a central FD @ `52a213ad`; base probe `NOT A RESULT` | **VERIFIED** | `probes/curriculum_D11_mrf_probe_Fprime/RESULTS.md:5-7`; `probes/curriculum_D11_mrf_probe/RESULTS.md:5` |
| 13 | same cell — "D10 … FD table `NOT A RESULT`" cited to `curriculum_D10_probe_Fprime/RESULTS.md` @ `4d9d902b` | **CORRECTED** — D10-F′ is `PASS` (4.401007e-08 relative, plateau proved); `GATE REACHED` is D10-P′'s verdict; `NOT A RESULT` is the base D10 probe's (@ `52a213ad`) | `Fprime/RESULTS.md:9-13`; `Pprime/RESULTS.md:5`; `curriculum_D10_probe/RESULTS.md:5` |
| 14 | same cell — D9 `NOT A RESULT`, `G9-3 GATE FAIL` SLSQP driver @ `f8916f36`; A4 shipped optimisation 6.9114e-08 @ `f9a59d47`; D3 `BLOCKED` on `nom_addThicknessConstraints2D` @ `6b8d6355` | **VERIFIED** ("fewer majors" made exact: 6) | `f8916f36:12,30`; `f9a59d47:13-14`; `6b8d6355:3,149` |
| 15 | 3D·steady·subsonic-compressible — D4 @ `1697ea49` G5 `PASS` 5/5, 0.1634 %, 80 majors, 28.6758 %, `GATE REACHED`, G2 band A `GATE FAIL`, G8 decomposition, `scotch` | **VERIFIED** | `:530-533`, `:576`, `:127-128`, `:637`, `:133` (G8 PASS) |
| 16 | same cell — `ARMF3_d4_grade_verdict.json` @ `4eae12f4`; grading confirmation 1.714 % @ `35171866`; D4-SHIPPED `NOT A RESULT`, 731.667 core-min, `max_iter` 100 @ `b26b875c`; D14-M @ `60cfd4c8` | **VERIFIED** (D14-M citation made explicit with its item `PASS`) | `4eae12f4` stat lists the JSON; `35171866:31`; `b26b875c:1,55-56,15`; `60cfd4c8:1,11,18` |
| 17 | 3D·steady·transonic — D7FR @ `2a93ff27` both rows `PASS` 5/5, worst 1.959 %, divergence 0.000 %, two `.so` md5s, item `PASS` | **VERIFIED** (item `PASS` made explicit) | `:1`, `:73`, `:58-60` |
| 18 | same cell — A3 rung 2 `PASS` np=4 @ `0f56460d`; A6 N=16 fixed reference `PASS` @ `66f42398`; A6 579,072 / A3 399,360 cells `BLOCKED` @ `be35dcad` / `35171866`; D8 `GATE REACHED` on a 3-major cap @ `9c241fe2`; D7R arm O `NOT A RESULT`, 30.40 % @ `2b50394a` | **VERIFIED** | `0f56460d:8`; `66f42398:24`; `be35dcad:12`; `35171866:14-15`; `9c241fe2:20`; `2b50394a:18-20` |
| 19 | regime table — A1 4,032 cells `:21`; A5 `U0 = 8.4` `:19`; D11 `MRFProperties` `:16`; D4 `DARhoSimpleFoam` `:24`; A3 `291.6 m/s (M 0.84)` `:138`; A6 `DARhoSimpleCFoam`, 295 m/s `:44-45`; D12R2 2,450 cells / `Re_D = 1.0e6` `:313,331` | **VERIFIED**; one imprecision left standing and disclosed: `DAPimpleFoam` is not named in `D12R2/PREREGISTRATION.md` (0 hits) — the solver name is carried by the D12 probe record @ `4d9d902b:5`; D4's `T0 = 300 K` is not on `:24` (the line names the solver; the conditions are in the runScript cited beside it) | as listed |

**Disclosed for the W2R scoring lane, not acted on here (its file):** the W2R comparator's replan output records `G12R-3` at `"W": 300` — the launcher's manifest carries no `W` key, so ~~`d12y_grade.py:2511` (`man.get("W", W_PRIMARY)`) evaluated `δ_window` at the primary window~~ **`d12y_grade.py:2011` in `plan()` — the `--plan` path that wrote W2R's `step_plan.json` — passes the literal `W_PRIMARY` (`g3_delta_window(retained, W_PRIMARY, period_steps=…)`); `:2511` `man.get("W", W_PRIMARY)` is the legacy `grade_from_manifest` path, not the one that ran, so a manifest `W` key alone could not have fixed it (*Correction 1a*, read at `git show HEAD:cases/dafoam/curriculum_D12R2/d12y_grade.py`, md5 `02a9ab62…`)** — the comparator evaluated `δ_window` at the primary window, and the recorded `h_min = 0.15755` is `δ_window(300) / (0.01·|g(900)|)`. The S2b series is byte-identical to D12R2's (P1), and from it `δ_window(900) = 9.879556e-04` — the value W2R's own P1 predicted — which gives `h_min(900) = 0.0867`: still above `h_max`, so P3's reading does not move; P4's does.

---

## Footer — the planted control: every cited sha, resolved

Run from the repository root; every line must read `ok`, and the count must equal the number of distinct shas cited above (26 — Correction 1 removed `ddb99eca` and added `e6580910`; **28 after Correction 1a**, which added `8fc2bdeb` and `3ccb0c81`; **29 after Correction 4, 2026-08-31**, which added `aa1ac179` — the commit that last touched BOTH `curriculum_D15/RESULTS.md` and `curriculum_D16/RESULTS.md`; `8fc2bdeb` and `3ccb0c81` STAY in the loop, no longer as `PENDING` queue notes but as the two items' frozen-registration citations; the tutorial checkout's `d3b7e38b` is a sha of a DIFFERENT repository and is deliberately NOT in this loop):

```
for s in 15767999 be35dcad cffd90e7 bb5088c4 3f8c6b13 b840fcd5 b10260a0 4d9d902b 65882eb3 5d1f89cd 5d1718df e6580910 35171866 52a213ad f8916f36 f9a59d47 6b8d6355 1697ea49 4eae12f4 b26b875c 60cfd4c8 2a93ff27 0f56460d 66f42398 9c241fe2 2b50394a 8fc2bdeb 3ccb0c81 aa1ac179; do printf '%s ' "$s"; git cat-file -e "$s^{commit}" 2>/dev/null && echo ok || echo MISSING; done
```

(29 shas listed; 29 distinct commits — several records share a commit. A `MISSING` line is a defect in this file, not in the record.) Reading at write time, 2026-08-26: 26 ok, 0 MISSING, 26 distinct shas. Re-read after Correction 1, 2026-08-26: 26 ok, 0 MISSING, 26 distinct shas. Re-read after Correction 1a, 2026-08-26: 28 ok, 0 MISSING, 28 distinct shas. **Corrections 2 and 3 each added a citation WITHOUT a sha (self-referential commits) and did not re-drive the loop.** **Re-driven by Correction 4, 2026-08-31, on the 29-sha list above: 29 ok, 0 MISSING, 29 distinct shas** — the two citations this correction adds both carry a resolving sha, so Correction 4 leaves no citation without one.

---

## Correction 1a — 2026-08-26, dafoam lane G (re-spawned), appended after the footer so that no line above this section changes number

**Scope.** Zero compute. No verdict moved; the census line is unchanged (5 of 36 cells with evidence, 31 not attempted). Correction 1 was landed by the supervisor at `72e08fe8` from this lane's on-disk record; this section carries what UPDATE N reported for the grid and one mechanism correction to Correction 1's own disclosure paragraph. Every edit above is marked *Correction 1a* in the cell; originals are struck, never rewritten.

| # | what | where |
|---|---|---|
| 1 | **Neither image ships the DAFoam tutorials** (`ls /home/dafoamuser/dafoam/tutorials` fails on both digests, UPDATE N; `curriculum_D15/PREREGISTRATION.md` §0 @ `8fc2bdeb`). The checkout is on the host at `/home/ubuntu/dafoam-tutorials` @ `d3b7e38b` (a sha of that repository, not of this one). The two "the image ships …" parentheticals now read "the tutorial checkout carries …". | 2D·steady·supersonic; 3D·steady·multiphase-free-surface |
| 2 | **Three registered-but-unrun cells on the drop path, as `PENDING: <path>` notes** (rule 1: `PENDING` is a queue state, never a verdict): D15 `cases/dafoam/ladder-a/A1/curriculum_D15/PREREGISTRATION.md` @ `8fc2bdeb` (`verification/queue/dafoam/D15_chain.json`, 28.3 core-min point, 165.0 ceiling); D16 `…/curriculum_D16/PREREGISTRATION.md` @ `3ccb0c81` (`D16_chain.json`, 40.3 point, 245.0 ceiling); **D17 (supersonic cone, lane N2): pending freeze** — `git log --all -- '*curriculum_D17*'` is empty and no directory exists at this write, so no path is cited. Both filed entries were HELD at the runner's 85 % busy ceiling at this write (`runner.log` 22:08Z: box 94.8 % busy, 27 entries queued lab-wide); no `LAUNCHED` line exists for either and none is claimed. | 2D·steady·subsonic-compressible; 2D·steady·transonic; 2D·steady·supersonic |
| 3 | **Mechanism correction to Correction 1's W2R disclosure.** The `--plan` path that produced W2R's `step_plan.json` is `plan()` at `d12y_grade.py:2011`, which passes the literal `W_PRIMARY` (300) to `g3_delta_window`; the `man.get("W", W_PRIMARY)` cited at `:2511` belongs to the legacy `grade_from_manifest` path and did not run. Consequence for the W3 registration: writing a `W` key into the launcher's manifest cannot by itself make the frozen comparator grade the registered window — a successor comparator is required and is disclosed as such in `curriculum_D12R2/W3_PREREGISTRATION.md`. The recorded numbers (`h_min = 0.15755 = δ_window(300)/(0.01·|g(900)|)`, `δ_window(900) = 9.879556e-04`, `h_min(900) = 0.0867`) are unchanged and were re-derived by this lane with the frozen comparator's own `read_series`/`block_averages` on the W2R S2b log (2,400 samples, identical sample-for-sample to D12R2's). | Correction 1 disclosure paragraph |

**For the verification supervisor's assembler (`scripts/assemble_capability_grid.py`, `docs/CAPABILITY_GRID.md`):** the dafoam table changed in four cells and one disclosure paragraph; the census line did not change; the footer loop now lists 28 shas. This lane does not edit `docs/CAPABILITY_GRID.md`.

| what this section did | figure |
|---|---|
| verdicts moved | 0 |
| cells edited (text appended or struck in place, same line) | 4 |
| lines whose number changed above this section | 0 (the section is appended after the footer; the footer's own lines carry their Correction 1a readings in place) |

---

## Correction 2 — 2026-08-28, dafoam lane R, on the D8R grading (`cases/dafoam/ladder-a/A6/curriculum_D8R/RESULTS.md`), appended after Correction 1a so that no line above this section changes number

**Scope.** Zero compute in this section; the compute it reports was bought by D8R and is costed at `docs/COST_CALIBRATION.md` row `C-191`. **One cell touched, both of its columns' worth of text confined to the optimisation column: `3D · steady · transonic`. NO VERDICT MOVED** — the cell's three-value summary was `CAN DO, CAVEATS` before this section and is `CAN DO, CAVEATS` after it, so **the census line is unchanged** (5 of 36 cells with evidence, 31 not attempted; optimisation column CAN DO 1, CAN DO CAVEATS 3, CAN NOT DO (attempted) 1). Every edit is marked *Correction 2* in the cell; originals are struck, never rewritten.

| # | what changed in the cell | reading, and where it is read from |
|---|---|---|
| 1 | **The count.** ~~0 converged to tolerance, 2 cap-stopped~~ → **2 converged to tolerance, 2 cap-stopped.** | D8R's two O arms both printed **`EXIT: Optimal Solution Found.`** — SHIPPED (`O-S`) at **12** majors, PATCHED (`O-P`) at **8**, against `max_iter` 30. Read twice: from the frozen grader's output `D8R_grade_20260828T021329Z.json` (`gates.SHIPPED.G-O_terminus` / `gates.PATCHED.G-O_terminus`) and directly from the two named arm logs `O-S_20260828T000818Z_1676866.log` and `O-P_20260827T223101Z_1595223.log`, each carrying the `EXIT` line and the driver's `D8R_IPOPT_TERMINUS … n_iter=` stamp. **The 2 cap-stopped are D8 and D7R arm O and both remain on record in the cell, undeleted.** |
| 2 | **The endpoint FD evidence added beside the count.** | **10 of 10 graded components `PASS` per row** — 5 on `CD` and 5 on `CL`, so **20 of 20 across the cell's two new rows, zero sign flips.** Aggregates (band E 5.0 %): SHIPPED 0.6458 % `CD` / 0.0884 % `CL`; PATCHED 0.9015 % / 0.1068 %. Worst single component **3.9170 %** (PATCHED `CD` `twist` idx5) against band D 5.0 %. Two distinct IDWarp `.so` md5s asserted equal to the printed value on all four arms (`gates.G9_toolchain`, `PASS`). |
| 3 | **A statement in the cell that this item made FALSE, struck.** ~~no `Optimal Solution Found.` exists on record in this cell~~ → it now exists, on both rows. | The clause was true when Correction 1 was written and is not true after D8R. It is struck in place rather than deleted so the cell shows what changed. |
| 4 | **Two caveats ADDED rather than removed, so the count does not read as unqualified.** | (a) The drag reduction is a **marginal `PASS`**: 0.3025 % / 0.3040 % against a band whose lower edge 0.29 % is D8's own measured 3-major drop — 4–5 % above the edge, and roughly three-tenths of the registration's own 1.0 % point. (b) **No common-point SHIPPED-vs-PATCHED comparison exists**: the two rows converged to different endpoints, and their endpoint divergence (0.0008–0.0545 % over 5 components) is recorded by D8R's grader as *"different endpoints; a reading"*, with no gate attached. |

**The citation defect this section knowingly leaves open, named rather than papered over.** This file's header rule is that *"each citation is a repository path plus the abbreviated sha of the commit that last touched that record"*. The new strongest-case citation, `cases/dafoam/ladder-a/A6/curriculum_D8R/RESULTS.md`, **carries no sha**, because that record is created by the very commit that makes this edit: no earlier commit touched it, and a commit cannot contain its own sha. The alternatives were to split the work into two commits or to write a sha that does not resolve; the second is forbidden and the first was outside this lane's instruction, so **the gap is disclosed in the cell and here, and is owed to a one-line successor edit that fills the sha in.** **The footer's planted-control loop is therefore UNCHANGED at 28 shas** — this section adds no new sha to it, and the loop's reading is not restated here because this lane did not re-run it.

**For the verification supervisor's assembler (`scripts/assemble_capability_grid.py`, `docs/CAPABILITY_GRID.md`):** the dafoam table changed in **one cell, optimisation column only**; the census line did not change; the footer loop still lists 28 shas. This lane does not edit `docs/CAPABILITY_GRID.md`.

| what this section did | figure |
|---|---|
| verdicts moved | 0 |
| cells edited (text struck or appended in place, same line) | 1 |
| new shas added to the footer loop | 0 |
| citations left without a sha, disclosed | 1 (`curriculum_D8R/RESULTS.md`, self-referential commit) |
| lines whose number changed above this section | 0 (the section is appended after Correction 1a, at the foot of the file) |

## Correction 3 — 2026-08-28, dafoam lane W, on the D18 grading (`cases/dafoam/curriculum_D18_cone_hypersonic/RESULTS.md`), appended after Correction 2 so that no line above this section changes number

**Scope.** Zero compute in this section; the compute it reports was bought by D18 (129.016 core-min, five arms) and is costed at `docs/COST_CALIBRATION.md` row `C-198`. **One cell touched, gradient column only: `2D · steady · hypersonic`. A VERDICT MOVED** — `CAN NOT DO — not attempted` → **`CAN DO, CAVEATS`** — **so the census line at the foot of the table IS updated in place** (5 → 6 cells with evidence, 31 → 30 not attempted, gradient column CAN DO CAVEATS 2 → 3); the optimisation column of every cell, and the optimisation half of the census, are unchanged. Originals are struck, never rewritten. **Lines whose number changed above this section: 0.**

| # | what changed in the cell | reading, and where it is read from |
|---|---|---|
| 1 | **The gradient verdict.** ~~CAN NOT DO — not attempted~~ → **CAN DO, CAVEATS — 1 case (D18)** | Item `PASS`, both rows `PASS`, graded 2026-08-28T03:28:52Z by the frozen comparator (`d18_grade.py`, md5 `e4ade11ed9e3db18d2c4988b30e929b4`, verified **disk == HEAD blob** at this write; frozen at `PREREGISTRATION.md:120` @ `dae3dc9d`). Read from `/home/ubuntu/certonomous-runs/CURRICULUM-D18-cone-hypersonic/D18_grade_20260828T032852Z.json` (`verdict`, `rows`) and corroborated by `STATUS.chain` / `CHAIN_DONE` (five arms `rc=0`, `chain=COMPLETE`, `grader_rc=0`). |
| 2 | **The FD evidence entered beside the verdict.** | **4 of 5 registered components graded per row, 4 of 4 `PASS`, zero sign flips, both rows.** Band E aggregates 0.13046764846984363 % (SHIPPED) and 0.1458900868751844 % (PATCHED) against 5.0 %; worst single component 3.2409827588515716 % (PATCHED `shape[0]`) against band D 5.0 %. `CD_baseline` 0.07854521739673961. |
| 3 | **Two hashed toolchain identities entered, not version strings** (`DAFOAM_CHARTER.md` §6). | SHIPPED `sha256:9d45679d55fd47f5ca7afd99cabb86c7c2729cf2acf34c438eb33af5290f07fc` with idwarp `.so` md5 `f0fcb488e0e98156575cd19548e91663`; PATCHED `sha256:2927768a16acdea0330180fff95c8879c1dda9efcf6028728523b7dee30f6d35` with `.so` md5 `85f59e87253e0a71a813f64ca6e4c425`. `G9_toolchain` `PASS`, printed value == artefact value on all four arms that carry an artefact; **MESH's `artefact_so_md5` is `null`** (that arm writes only `checkMesh.log`) and is named in the record rather than left to be inferred. |
| 4 | **Four caveats entered WITH the verdict, so the cell cannot be quoted unqualified.** | (a) **The cell is hypersonic in the Mach number only.** Read from the staged case, not assumed: `equationOfState perfectGas`, `thermo hConst` with **`Cp 1005` constant**, `transport const` with **`mu 0`**, `RASModel dummy`, `cone` wall `slip`. **No real-gas equation of state, no vibrational excitation, no dissociation, no chemistry, no viscous or radiative heating is modelled.** Registered before compute at `PREREGISTRATION.md:13` and `:103`, and carried by the grader itself in its `real_gas_caveat` field. (b) **`shape[3]` is `NOT A RESULT` (`NO_PLATEAU`) in BOTH rows** — neighbour disagreements 1572.6916 % / 195.6583 % against a 10 % plateau rule — and the row stands on the **registered minimum of ≥ 3 graded components** (`cases/dafoam/ladder-a/A1/curriculum_D17_cone_supersonic/PREREGISTRATION.md:50` clause (c), adopted verbatim by `curriculum_D18_cone_hypersonic/PREREGISTRATION.md:48`; implemented as `MIN_GRADED = 3` at `d18_grade.py:86`). **8 of 10 component-slots across the two rows were graded; 2 were not.** (c) **One mesh, 40,000 cells; no grid family; NO GCI is quoted** — standing rule 5 has no row, and the grader says so in its `no_gci` field. (d) **`G6` dot-product/duality `NOT MEASURED`**, registered as such: the tutorial exposes no such test. |
| 5 | **A discrimination claim the cell does NOT make, entered so the cell cannot imply it.** | The item's P7 prediction (*"the two rows are NOT discriminating, S/N ≤ 1.0"*) scored **`MISS`** at S/N **19.8691**, whose registered reading (`PREREGISTRATION.md:95`) is that D18 **is** discriminating. **The signal in that ratio is the 64.3953 % SHIPPED-vs-PATCHED divergence at `shape[3]` — the component both rows graded `NOT A RESULT`** — because `d18_grade.py:585-589` takes the numerator over all five registered components with no verdict filter while the denominator excludes ungraded ones. Over the four **graded** components the worst divergence is 0.4482626682416995 % and the same formula gives **0.1383**. The other discrimination prediction, **P5**, which reads only a graded component, **also `MISS`ed — in the non-discriminating direction** (SHIPPED `shape[0]` inside band D at 2.9501 %, no flip). **Nothing about shipped-versus-patched discrimination enters this cell**, and the arithmetic above is this lane's, on the grader's published numbers, not a re-scoring: **P7 is `MISS` and stays `MISS`; the frozen comparator is untouched** (rules 2 and 6). A grader defect note on P7's composition is owed to a successor item. |

**The census DID move, and here is exactly how, because Correction 2's precedent was a section that moved none.** Before: *"5 of 36 cells carry evidence … 31 not attempted. Gradient column: CAN DO 3, CAN DO CAVEATS 2."* After: **6 of 36, 30 not attempted, gradient column CAN DO 3, CAN DO CAVEATS 3.** The optimisation column's counts (CAN DO 1, CAN DO CAVEATS 3, CAN NOT DO (attempted) 1) are **unchanged** — D18 runs no optimiser at all (`PREREGISTRATION.md:103`: *"nothing about the constraints (no optimiser; the `optimization converged` column does not move)"*), so the `2D · steady · hypersonic` optimisation cell stays `CAN NOT DO — not attempted`. The census sentence *"no grid family exists on any DAFoam case, so no GCI is quoted anywhere above"* remains **true after D18** — D18 ran one mesh — and is left unedited.

**The citation defect this section knowingly leaves open, named rather than papered over.** The header rule requires *"a repository path plus the abbreviated sha of the commit that last touched that record"*. The new citation `cases/dafoam/curriculum_D18_cone_hypersonic/RESULTS.md` **carries no sha**, because that record is created by the very commit making this edit and a commit cannot contain its own sha — the identical, disclosed gap Correction 2 recorded for `curriculum_D8R/RESULTS.md`. It is owed to a one-line successor edit. **The footer's planted-control loop is therefore UNCHANGED at 28 shas**; this section adds no new sha to it and this lane did not re-run the loop.

**For the verification supervisor's assembler (`scripts/assemble_capability_grid.py`, `docs/CAPABILITY_GRID.md`):** the dafoam table changed in **one cell, gradient column only**, **the census line DID change** (the first Correction in this file to move it), and the footer loop still lists 28 shas. This lane does not edit `docs/CAPABILITY_GRID.md`.

| what this section did | figure |
|---|---|
| verdicts moved | **1** (`2D · steady · hypersonic`, gradient column: `CAN NOT DO — not attempted` → `CAN DO, CAVEATS`) |
| cells edited (text struck or appended in place, same line) | 1 |
| census lines edited in place | 1 |
| new shas added to the footer loop | 0 |
| citations left without a sha, disclosed | 1 (`curriculum_D18_cone_hypersonic/RESULTS.md`, self-referential commit) |
| lines whose number changed above this section | 0 (the section is appended after Correction 2, at the foot of the file) |

---

## Correction 4 — 2026-08-31, dafoam lane, on the D15 and D16 gradings (`curriculum_D15/RESULTS.md`, `curriculum_D16/RESULTS.md` @ `aa1ac179`), appended after Correction 3 so that no line above this section changes number

**Scope.** Zero compute in this section; the compute it reports was bought by D15 (10.100 core-min, `docs/COST_CALIBRATION.md` row `C-154`) and D16 (15.935 core-min, row `C-156`) on 2026-08-27 and is already costed there. **Two cells touched, GRADIENT COLUMN ONLY: `2D · steady · subsonic-compressible` and `2D · steady · transonic`. TWO VERDICTS MOVED** — each `CAN NOT DO — not attempted` → **`CAN DO, CAVEATS`** — **so the census line at the foot of the table IS updated in place** (6 → 8 cells with evidence, 30 → 28 not attempted, gradient column CAN DO CAVEATS 3 → 5). **The optimisation column of both cells, and the whole optimisation half of the census, are UNCHANGED** — see the last block of this section, which is the point of it. Originals are struck, never rewritten. **Lines whose number changed above this section: 0** — proved mechanically, not asserted: the pre-edit file is git blob **`feb9bf8a4abc061b7e617b13bbe4f8d1a034ade0`**, **178 lines**; the six edits (lines 28, 29, 64, 100, 103, 106) are each same-line-count substitutions, and `diff` against the pre-edit blob reports exactly `28,29c28,29`, `64c64`, `100c100`, `103c103`, `106c106` — no insertion or deletion hunk anywhere above this section.

**What was actually wrong, stated plainly.** Correction 1a entered D15 and D16 as **`PENDING: <path>`** queue notes on 2026-08-26, correctly: at that write both entries were HELD at the runner's 85 % busy ceiling and neither had a `LAUNCHED` line. **Both ran the next day** — the queue-runner daemon launched D15 at `2026-08-27T11:32:30Z` and D16 at `2026-08-27T11:33:35Z` (`verification/queue/LAUNCH_LOG.tsv`, quoted in each record's header) with no agent alive — and both were graded the same morning. **The `PENDING` notes then outlived their runs by four days.** That is the failure mode this correction records: a queue state is a display state, and nothing in this file made it expire when the queue moved.

| # | what changed in the cell | reading, and where it was re-read from by this lane |
|---|---|---|
| 1 | **`2D · steady · subsonic-compressible`, gradient verdict.** ~~CAN NOT DO — not attempted; `PENDING: …D15/PREREGISTRATION.md` @ `8fc2bdeb`~~ → **CAN DO, CAVEATS — 1 case (D15)** | Item `GATE FAIL`; SHIPPED `GATE FAIL`, PATCHED `PASS`. Read from the frozen graded JSON `/home/ubuntu/certonomous-runs/CURRICULUM-D15-a1-naca0012-subsonic/D15_grade_20260827T114315Z.json` (`verdict`, `rows`, `gates.G5_{SHIPPED,PATCHED}.{G5_CD,G5c_CL}`) **and independently against `cases/dafoam/ladder-a/A1/curriculum_D15/RESULTS.md` §1, §3, §4 @ `aa1ac179`** — the two agree on every figure entered. |
| 2 | **The D15 numbers entered beside the verdict.** | SHIPPED `CD`: 2 `PASS` / 3 `GATE FAIL`, 0 sign flips, worst **44.873808 %** (`shape[6]`), aggregate **34.6807 %**, band D and band E both `GATE FAIL`. SHIPPED `CL`: worst 30.0724 % (`shape[6]`), aggregate 4.2394 %, band D `GATE FAIL`. PATCHED `CD`: 5/5 `PASS`, aggregate **0.04740516 %** (quoted 0.047405 %). PATCHED `CL`: 5/5 `PASS`, aggregate **0.009916 %** (quoted 0.0099 %). Mesh **4,032 cells** (`G-M2 PASS`, both rows). |
| 3 | **`2D · steady · transonic`, gradient verdict.** ~~CAN NOT DO — not attempted; `PENDING: …D16/PREREGISTRATION.md` @ `3ccb0c81`~~ → **CAN DO, CAVEATS — 1 case (D16)** | Item `GATE FAIL`; SHIPPED `GATE FAIL`, PATCHED `PASS`. Read from `/home/ubuntu/certonomous-runs/CURRICULUM-D16-a1-naca0012-transonic/D16_grade_20260827T114714Z.json` and independently against `…/curriculum_D16/RESULTS.md` §1, §3, §4 @ `aa1ac179`. |
| 4 | **The D16 numbers entered beside the verdict.** | SHIPPED `CD`: 4 `PASS` / 1 `GATE FAIL` on `shape[0]` at **5.1511 %** against a 5.0 % band — a marginal crossing, aggregate 1.9648 %, band E `PASS`. SHIPPED `CL`: `GATE FAIL` on `shape[6]` at **41.802189 %**, aggregate 4.5797 %. PATCHED `CD`: 5/5, aggregate **0.14600276 %** (quoted 0.146003 %). PATCHED `CL`: 5/5, aggregate **0.175768 %** (quoted 0.1758 %). Same mesh, 4,032 cells. |
| 5 | **The cause class, carried and not softened.** | Both SHIPPED rows are **`PHYSICS-FAIL` — against the SHIPPED TOOLCHAIN**, assigned by the grading record and cited at `docs/dafoam/GRADING_CHAIN.md:46`, re-checked and explicitly standing at `:81` ("their FD arms *did* run … the shipped adjoint genuinely disagrees with a measured FD referee"). **This section does not edit that file.** Per that file's own §50 note the class is a finding about DAFoam, not about this lab's ability to do physics — which is exactly why the cells read `CAN DO, CAVEATS` on the strength of the PATCHED row and say so, rather than reading `CAN DO` unqualified. |
| 6 | **D16's `P5 MISS` entered as a caveat, not omitted.** | P5 was scored **`MISS`** by the frozen comparator (`predictions.P5_shipped_shape6_outside_band_D_or_flipped`) while the row still `GATE FAIL`ed: the registered falsifier named `shape[6]` on `CD` and that component read 1.95 %, inside the band. **The component the defect lands on MOVED** (`shape[6]` at M 0.288 → `shape[0]` at M 0.685). This is the case that earned **`L-352`** — *a registered falsifier that names a COMPONENT can read MISS while the effect it was built to detect is still there*. **P5 is `MISS` and stays `MISS`; nothing is re-scored here** (rules 2 and 6). |
| 7 | **The two 2026-08-31 FD-plateau disclosures entered as caveats, because a cell that omitted them would overstate the PATCHED rows.** | D15 PATCHED `CD` `shape[7]`: `plateau_neighbour_pct = [1.1558573362510012, 21.629874553458254]` — **one-sided at the FINE end**, against a registered 10.0 % tolerance, and **NOT diagnosed** (the record's own arithmetic leaves a ~50× gap between the S/N bound and the observed break). D16 PATCHED `CL` `shape[6]`: `[14.097783376052847, 1.126810776327276]` — **one-sided at the COARSE end**, and **diagnosed** as truncation / shock motion at S/N 901,738. Both read from the same `plateau_neighbour_pct` fields this lane pulled out of the two graded JSONs, matching the disclosures verbatim. The frozen rule tests `min()`, so both rows passed the rule that graded them; **neither disclosure moves a gate, and neither is a regrade.** |
| 8 | **`G6` and GCI, entered as absences so the cells cannot imply them.** | `gates.G6_dot_product_duality` in both JSONs reads literally *"NOT MEASURED -- the tutorial exposes no dot-product/duality test; named, never composed"*. Both JSONs carry `no_gci` = *"no grid family; standing rule 5 has no row; NO GCI IS QUOTED"*. **One mesh per item, 4,032 cells, no grid family, no GCI** — and the census sentence *"no grid family exists on any DAFoam case, so no GCI is quoted anywhere above"* remains **true after D15 and D16** and is left unedited. |

**THE OPTIMISATION COLUMN OF BOTH CELLS IS UNCHANGED, AND THAT IS DELIBERATE.** Both cells keep `CAN NOT DO — not attempted (D15/D16 runs no optimiser)` exactly as written. This is not an oversight and it is not conservatism: it is what the two frozen registrations say before any compute — D15 `PREREGISTRATION.md:12` (*"This item moves ONLY the gradient column of that cell — it runs no optimiser; the 'optimization converged' column stays `CAN NOT DO — not attempted`"*) and `:109` (*"Nothing about an optimum (no optimiser runs …)"*); D16 `PREREGISTRATION.md:11` (*"Only the gradient column moves; no optimiser runs"*). **This is the family's genuine remaining gap in 2-D compressible: gradients are verified on the patched toolchain and no optimiser has ever been driven there.** The rung that addresses it is **D19**, and this section deliberately does not pre-empt its result: no optimisation cell is softened, no anticipation of D19 is written into any cell, and the optimisation half of the census (CAN DO 1, CAN DO CAVEATS 3, CAN NOT DO (attempted) 1) is byte-unchanged.

**The footer's planted control WAS re-driven by this section — unlike Corrections 2 and 3, which each left a citation without a sha and did not re-run the loop.** Both citations this correction adds resolve: `aa1ac179` is the commit that last touched **both** RESULTS records (`git log -1 -- <path>` on each returns it, and each file on disk is byte-identical to `git show HEAD:<path>`), and `8fc2bdeb` / `3ccb0c81` remain in the loop as the two items' frozen-registration citations rather than as retired queue notes. **The loop is now 29 shas; re-driven at this write: 29 ok, 0 MISSING, 29 distinct.** The reader was shown able to report a non-zero before its zero was accepted (rule 3): a deliberately malformed token, `deadbee1`, was put through the same `git cat-file -e "$s^{commit}"` test in the same shell and read `MISSING`. **The two sha-less citations Corrections 2 and 3 disclosed (`curriculum_D8R/RESULTS.md`, `curriculum_D18_cone_hypersonic/RESULTS.md`) are still owed a one-line successor edit; this section does not fill them, and names them so the debt is not lost.**

**A governance gap found while doing this and NOT fixed here, because it is not this family's to fix.** `docs/charters/FILING_CHARTER.md` and `docs/LOCATIONS.md` contain **zero** occurrences of the string `capability` in any case — measured, not assumed. So `docs/capability/` appears in no charter's filing rules and in no team's chartered territory, and this file's authority is de facto only (its own header line 3; every commit that has ever touched it is a dafoam commit; `curriculum_D19/PREREGISTRATION.md` §4 concludes *"Editing the grid is the dafoam-supervisor's call"*). **That is for the chief to route, not for a lane to legislate.**

**For the verification supervisor's assembler (`scripts/assemble_capability_grid.py`, `docs/CAPABILITY_GRID.md`):** the dafoam table changed in **two cells, gradient column only**, **the census line DID change**, and the footer loop now lists **29** shas. This lane does not edit `docs/CAPABILITY_GRID.md`. Two things that supervisor should know before re-running: (i) the assembled grid at HEAD was built from HEAD `5e5cd10d` on 2026-08-27 and is therefore behind by **Corrections 2, 3 and 4** — neither `D8R` nor `D18` occurs anywhere in it, and its dafoam `2D · steady · hypersonic` cell still reads `CAN NOT DO — not attempted`; (ii) because this correction moves the per-family census, the assembler will **REFUSE** at its own guard (`--allow-census-change` absent) — that refusal is working as designed and the decision to pass the flag is that supervisor's, not this lane's.

| what this section did | figure |
|---|---|
| verdicts moved | **2** (`2D · steady · subsonic-compressible` and `2D · steady · transonic`, gradient column: `CAN NOT DO — not attempted` → `CAN DO, CAVEATS`) |
| cells edited (text struck or appended in place, same line) | 2 |
| optimisation cells edited | **0** |
| census lines edited in place | 1 |
| new shas added to the footer loop | **1** (`aa1ac179`) |
| footer loop re-driven | **yes** — 29 ok, 0 MISSING, 29 distinct, with a planted `MISSING` control |
| citations left without a sha by THIS section | **0** |
| citations left without a sha by earlier sections, still owed | 2 (`curriculum_D8R/RESULTS.md`, `curriculum_D18_cone_hypersonic/RESULTS.md`) |
| lines whose number changed above this section | 0 (six same-line-count substitutions; `diff` hunks are all `NcN`) |
