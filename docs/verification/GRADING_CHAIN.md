# VERIFICATION — GRADING CHAIN

**Authority:** Sanaa's GRADING TRANSPARENCY ORDER, 2026-08-31, bullet 1, captured verbatim at `4116024a` (`etc/sessions/2026-08-31T2055Z_sanaa_grading_transparency_order.md`). Read at source before this file was written (standing rule 9). Her order passes her own 14-day rule freeze by its own terms.

**Ten bullets. Her instruction is explicit that a link which cannot be named in one bullet is a finding, not a formatting problem — bullet 8 is that finding, and it is this team's own.**

---

- **1 — WHAT THIS TEAM ACTUALLY GRADES, SAID HONESTLY UP FRONT.** Verification runs **no solves**; its product is **rulings, audits and instruments**, so "artifact → reader → gate → control → verdict" does not instantiate once here, it instantiates **twice**, and **only one of the two chains has a planted control.** Naming that split is the whole value of this document; collapsing it into one tidy chain would be the dishonest version.

- **2 — CHAIN A, ARTIFACT (the controlled chain).** The graded artifacts are the **four append-only lab registers** — `docs/DOCKET.md`, `docs/LESSONS.md`, `docs/NUMERICS_KNOWLEDGE.md`, `docs/COST_CALIBRATION.md` — plus the **git worktree/HEAD pair** for each. These are the shared records every team's verdict eventually lands in, which is why this team owns their guards.

- **3 — CHAIN A, READER.** `scripts/append_record.py`, specifically `parse_record_ids(text, path)` — the **single** reader that both reconcilers (`scripts/check_record_reconciliation.py`, `scripts/check_docket_reconciliation.py`) import rather than re-implement. A writer whose reader cannot see what it writes is a dead lever the day it ships, so writer and readers **land in one commit** by construction (`96f4b572`).

- **4 — CHAIN A, FROZEN GATE.** The registered patterns and shapes in `append_record.py` — `RECORDS`, `CANDIDATE_SHAPES`, `KNOWN_EXCLUDED`, `ANCHORED_TOOL_ID` — each **measured against the real files** and carrying its measured count in-line. Enforced fail-closed by **nine exit codes** (2, 3, 4, 5, 6, 7, 8, 9), plus **four load-time refusals** that make the module refuse to import if its tables disagree with each other.

- **5 — CHAIN A, WHO PROVED THE READER CAN SEE (the planted-control citation).** `run_controls()` and `run_allocation_disk_control()` in `scripts/append_record.py`. The allocation control is the strongest link in this team's chain and satisfies `VERIFICATION_CHARTER` §2j's birth question — **who wrote the bytes the control reads?** — because the bytes are written by **`main()` itself**, the same entry point the lab invokes, and read back **off disk**. Every refusal limb is paired with a **driven mutation** that removes the clause it guards and must flip it. **Driven by me personally, 2026-08-31, `__pycache__` cleared first: rc 0, `VERDICT: PASS`, 0 control failures.**

- **6 — CHAIN A, WHERE THE VERDICT LANDS.** The tool's own stdout verdict plus its **`CANNOT SEE` lines**, each carrying an owner and a re-read trigger (L-401); then `docs/LAB_STATE.md` and the commit message. **Declared blindness is not discharged blindness** — the trigger, not the date, is the guard.

- **7 — CHAIN B, ARTIFACT AND READER (the uncontrolled chain).** The artifacts are **other teams' grading records, pre-registrations, comparators and charter clauses**; the reader is **me, personally**. `SUPERVISION_CHARTER` §3's four checks are **non-delegable**, so the reader is a person and a relayed check is a summary, not a check. Today's specimen: `cases/F28_DUCTED_ACTUATOR_DISK/analyse_f28.py`, 763 lines, identity proved on **both sides** by md5 before a line was read.

- **8 — ⚠ CHAIN B, THE MISSING LINK, AND IT IS THIS TEAM'S OWN FINDING.** **There is no planted control on a ruling, and there cannot be one in the sense rule 3 means.** A charter ruling is not a measurement: nothing can be planted into a clause and read back to prove the reader would have seen a wrong one. **So this team's principal product is, by construction, the least-controlled thing it ships** — and it is the team that refuses other teams' gates for exactly that shortfall. **Stated because the order asks for it, not because it is comfortable.** The substitute is **adversarial falsification, not self-certification**: a ruling is published with its ground cited so another team can attack it. **It works, and the specimen is one day old** — `ansys-verification` was told by a peer that Sanaa had ruled, refused the peer's word under rule 9, read `27a49bda` at source, and found that **my own §2h.6 named two rules at once**; repaired at `84af66d5` as §2h.8. **A finding against this team, produced by the only control this chain has.**

- **9 — CHAIN B, FROZEN GATE.** `VERIFICATION_CHARTER.md` — §2 (the fixed verdict vocabulary), §2a (identity), §2b (amendment legality), §2c (discrimination), §2d/§2d.1 (comparator freeze and the repair exception), §2j (birth), §2f.3/§2h.8 (referent tiers); plus standing rules 3, 4 and 5. **The charter is the gate, and it is frozen by rule 6** — never edited, only appended as dated amendments asserting `lines whose number changed above this section: 0`, which is itself verified in the same shell invocation as the write.

- **10 — CHAIN B, WHERE THE VERDICT LANDS.** `VERIFICATION_CHARTER.md`'s amendment record (versioned, dated, with a change-count table naming gates/bands/caps/labels moved — usually **0**); the six standing audits `docs/*_AUDIT.md`; `verification/credibility/` for instrument verdicts; and `docs/LAB_STATE.md`, which is the only handoff channel between sessions. **A verdict uses only the fixed vocabulary — `PASS` / `GATE REACHED` / `GATE FAIL` / `NOT A RESULT` / `BLOCKED` / `PENDING` — and from 2026-08-31 every non-`PASS` also carries its CAUSE CLASS, assigned by the grading record and cited.**
