# R12 SUPERSESSION NOTE — APPEND DRAFT, PREPARED BY VERIFICATION, **TO BE LANDED BY THE CHIEF**

> **STATUS: DRAFT. NOT ADOPTED. NOTHING HERE IS IN FORCE.** The target is
> `docs/charters/SUPERVISOR_RULINGS.md`, the global supervisor's ledger — **which
> is nobody's team territory**, and therefore not verification's to append to.
>
> **WHY IT IS STILL NEEDED, stated plainly.** Sanaa's ruling of 2026-09-03 says
> *"this resolves the R12 question"*, and `VERIFICATION_CHARTER` v1.43 §2r carries
> that — **but only inside her quoted text.** At `SUPERVISOR_RULINGS.md`, **R12
> still reads as governing law with no forward pointer.** A reader arriving at R12
> cold has no way to learn it has been superseded in scope. **This note is the only
> artifact that closes that loop.**
>
> **R12 is NOT retired and its text is NOT edited** — retiring a ruling is reserved
> to Sanaa and she did not retire it. R12 continues to govern model-form banding on
> a reference community's own canonical verification grid. What changes is that
> **citing R12 in a validation-against-workshop-data context is non-conforming from
> today**; that context cites the two-tier standard and §2r.
>
> **⚠ RE-DERIVE THE TARGET'S LINE COUNT AND DIGEST AT LANDING.** The figures in the
> draft header are from an earlier HEAD.
>
> **Filed here rather than left in the scratchpad because `L-186` forbids the
> scratchpad as a handoff channel.**

---

DRAFT — NOT COMMITTED. Append VERBATIM at the foot of
/home/ubuntu/Certonomous/docs/charters/SUPERVISOR_RULINGS.md, after its current
line 240. Everything below the marker line is the append text.

Derived at HEAD 6319743d48f3c8830bde6cb37fe8dfc66344ae92:
  file lines before append = 240
  md5 of HEAD blob         = 7f7ddb0b499c7f2fbbbe1435a8dc59db
  md5 of worktree copy     = 7f7ddb0b499c7f2fbbbe1435a8dc59db  (EQUAL — clean at HEAD)

WHY THIS FILE, AND WHY THIS SHAPE. R12 lives here at :220-240. Standing rule 6
forbids editing it. This file already carries its OWN precedent for exactly
this move: "### R7 correction, 2026-08-05" (:208) and "### R10 correction, same
day" (:153) are both appended AFTER later rulings rather than edited into the
originals. This note follows that pattern. R12's text is NOT touched and no
line above moves.

⚠ SCOPE FLAG FOR THE SUPERVISOR: docs/charters/SUPERVISOR_RULINGS.md is not
named in CLAUDE.md's roster as any team's territory. It is the GLOBAL
supervisor's ruling ledger, and the ruling being carried is Sanaa's. Whether
the verification team appends here, or hands this block to the chief, is the
supervisor's call and I have not made it.
========================= APPEND BEGINS ON NEXT LINE =========================

---

## R12 SUPERSESSION, 2026-09-03 — [SANAA-RULED] the two-tier mesh standard replaces R12 as the governing instrument for committee grids, and R12 is not retired

**Nothing above this line is edited.** R12's text at lines 220–240 stands exactly as
ruled on 2026-08-07, its three mandatory conditions unchanged. **Lines whose number
changed above this section: 0.**

**WHAT HAPPENED.** Sanaa ruled a two-tier mesh standard on 2026-09-03 ~17:30Z
(`etc/sessions/2026-09-03T1730Z_sanaa_mesh_standard_and_freeze_enforcement.md:5-20`),
opening with, verbatim: *"Two-tier mesh standard — **this resolves the R12 question**."*
The certificate-issuance half is **COMMITTED at `fb2d0d39`** as
`docs/charters/VERIFICATION_CHARTER.md` **§2r (v1.43, 2026-09-03)**, which carries her words
verbatim and adds no ruling of its own beyond the mechanics of checking them. **The
mesh-side mechanics are DRAFTED AS `MESH_STANDARD` §15 (v1.10) AND NOT YET LANDED** —
`docs/standards/` is outside the verification team's folder scope and the block awaits cfd
or Sanaa. **This note states that plainly rather than citing a section that does not yet
exist**; if §15 lands under a different ordinal or version, this sentence is the one to
correct.

**THE QUESTION SHE RESOLVED — as actually put, quoted, so the supersession attaches to the
real text.** It was put by the cfd-supervisor in board 47 of `docs/LAB_STATE.md`, heading
at **:21961**:

> **`### 🔴 R12 DOES NOT REACH A CERTIFICATE, AND M6I DOES NOT EVEN QUALIFY FOR R12`**

and framed, verbatim, at **`docs/LAB_STATE.md:21963-21966`**:

> - `MESH_STANDARD.md:73-82` and ruling R12: *"physics gates and credential verdicts still
>   require compliant meshes -- this exemption never travels to them."* Rung 1's deliverable
>   is pressures against tunnel data = a physics gate. **R12 buys a MODEL-FORM BAND ONLY.
>   Sanaa's "full certificate" is NOT reachable on an R12-exempted grid.**
> - **M6I does not qualify:** R12 covers *"the reference community's OWN CANONICAL
>   verification grid"*. M6I was produced by invoking the public generator ONCE with a
>   namelist **this lab modified** (`target_y_plus` 1.0 -> 0.25, three counts doubled).
>   Reaching R12 needs an actually-distributed canonical family — **a NEW import, gated by
>   Rung 0.**
> - **A band costs MORE, not less, and inverts the shape** [...] **Three models on one
>   grid, not one model on three grids.**
> - **This is verification's call to confirm or overturn, not cfd's.**

Its measured ground is the same board's exhibit at **`docs/LAB_STATE.md:21918-21932`** —
*"FIVE OF FIVE COMMITTEE GRIDS EVER IMPORTED ON THIS BOX BREACH SECTION 3.1 BY 17-20
DEGREES"* — and it stood on Sanaa's desk as item 3, **`docs/LAB_STATE.md:22006`**: *"**The
committee-grid admissibility wall** — governs Rungs 1, 2 and 3 together."*

**SHE DID NOT ANSWER THE QUESTION. SHE REPLACED THE INSTRUMENT — and that distinction is
the whole content of this note.** R12 is a *narrow exemption to a gate*. §15 is a *two-tier
standard* in which the 70° gate is the **generation** standard and a workshop committee grid
is **a different object the gate never governed**. cfd's reading of R12 was correct; it is
superseded, not overturned.

| the question as put | disposition |
|---|---|
| *"R12 buys a MODEL-FORM BAND ONLY"* | **CORRECT ABOUT R12.** R12 is simply no longer the governing instrument for this class. cfd read R12 right. |
| *"a full certificate is NOT reachable on an R12-exempted grid"* | **True of an R12 exemption; false of a §15 Tier-2 grid.** Her words: *"A full certificate IS reachable this way — a certificate's honesty is disclosure and verification, not our internal birth standard."* |
| *"M6I does not qualify"* | **UPHELD, on stronger ground.** §15.3(0)(2) requires the grid byte-unmodified from what the committee distributed; M6I's namelist was modified by this lab. M6I is **Tier 1**, and its L1/L2/L3 readings **87.7462° / 86.4646° / 87.6620°** (`verification/runs/M6I_runs/{L1,L2,L3}/log.checkMesh:91`) are §3.1 breaches, not disclosures. |
| *"a band costs MORE ... three models on one grid, not one model on three grids"* | **MOOT and inverted.** §15.3(c) requires the numerical band from **the grid family** — three levels of one committee family under CLAUDE.md rule 5 in full. The band-of-models shape was R12's, not §15's. |
| *"This is verification's call to confirm or overturn, not cfd's"* | **Neither team's — Sanaa ruled it.** The referral named the wrong ruler, and that is recorded rather than quietly corrected. |

**WHAT R12 STILL GOVERNS.** Everything it always did: **model-form banding on a reference
community's own canonical verification grid**, under its three mandatory conditions
(stated on the band artifact with the failing number beside it; scoped to banding only;
grid provenance named). **R12 is NOT retired.** Retiring a ruling is reserved to Sanaa
(CLAUDE.md, FIRST-ACTION RULE) and she did not retire it.

**WHAT CHANGES FOR A READER, FROM TODAY.**

1. **Citing R12 in a validation-against-workshop-data context is non-conforming lab-wide.**
   That context cites `MESH_STANDARD` §15 and `VERIFICATION_CHARTER` §2r.
2. **`MESH_STANDARD.md:73-82` — R12's pointer inside §3.1 — is deliberately NOT edited**,
   because editing it would move every line number below it and three documents assert that
   none moved. **A reader arriving at §3.1:73-82 must read §15 with it.** This note is the
   bridge; the pointer is stale by design and disclosed here rather than silently repaired.
3. **`docs/CAPABILITY_GRID.md:26`** records the F6b MODEL_FORM band as carrying an *"R12
   mesh exemption, 85.70°"*. **That row is untouched and remains correct** — it is a
   model-form band, which is exactly what R12 still governs.

**⚠ ONE THING THIS NOTE DOES NOT DECIDE.** A distributed canonical committee grid used for
**model-form banding** satisfies R12; the same grid used for
**validation-against-workshop-data** satisfies §15. **Which instrument governs a case that
is both is unruled**, and is flagged identically in `MESH_STANDARD` §15.9 item 1 and
`VERIFICATION_CHARTER` §2r (committed at `fb2d0d39`). **Flagged in both; decided in neither.**

| assertion | value |
|---|---|
| authority | **[SANAA-RULED]** 2026-09-03 ~17:30Z, quoted verbatim above and in full at `MESH_STANDARD` §15.1 |
| R12's text | **NOT edited, NOT retired** |
| rulings retired by this note | **0** |
| results re-graded | **0** |
| gates · thresholds · caps · labels moved | **0 · 0 · 0 · 0** |
| solver compute | **0 core-min, $0.00** |
| **lines whose number changed above this section** | **0** |
| md5 of this file's HEAD blob before the append | `7f7ddb0b499c7f2fbbbe1435a8dc59db` |
| md5 of this file's first 240 lines after the append | *(RE-DERIVE AT COMMIT)* |
| the two digests | *(assert EQUAL — MEASURED, in the commit's own shell invocation)* |
