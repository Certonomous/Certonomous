# K0c turbulent-thermal MODEL-FORM GATE FAILs — closure-ladder routing successor (2026-09-07)

**Dated 2026-09-07. Written by a heat-transfer lane on the supervisor's dispatch;
routing/lineage record only, `[lab-attributed]`. ZERO COMPUTE. Nothing here has
been sent, filed, submitted, uploaded, registered or posted outside this box
(`CLAUDE.md` rule 7).**

## What this record is, and what it is NOT

This is a **routing / lineage pointer**, created under
`VERIFICATION_CHARTER.md` §2ay.2(b) to record the **dated fix PATH** for three
turbulent-thermal **model-form GATE FAILs** on the K0c natural-convection rungs.

- It creates **NO gate, NO threshold, NO band, NO cap and NO label.**
- It moves **NO verdict**: K0cS, K0cT and K0cX keep their **GATE FAIL** exactly as
  graded. Nothing below re-grades, widens, narrows or reinterprets any of them.
- It records only the **path** by which each fail is now being worked — the
  four-rung closure ladder — so that each is in an ACTIVE, DATED fix state (b)
  rather than a standing violation.

## The routing law this record applies

Per Sanaa's ruling of 2026-09-04, ~15:10Z
(`etc/sessions/2026-09-04T1510Z_sanaa_model_form_closure_ladder.md`), a **measured
model-form GATE FAIL is a routing signal, not a terminus.** Such a case escalates
into the four-rung closure ladder, each rung gated against the **same reference**
through the standard registered path:

1. exhaust all stock RANS closures (register if none land);
2. rule out numerical cause per §0 (the five process classes);
3. apply literature closure corrections (analytical, functional/nonlinear,
   algebraic-stress, curvature/rotation, separation-specific), each applied and
   **gated individually** with its paper provenance and known-good flow class;
4. the lab's own research (GP-based) closures as the top rung.

Consequences fixed by that ruling and carried here:

- These three fails are therefore **NOT terminal**, and they do **NOT** go to
  Sanaa's desk case-by-case as capability-gap filings — the ladder is the
  active successor.
- Execution is **sequenced behind the benchmark ladder's first rungs (M6 / CRM)**,
  with the closure-correction library built as the **F6 separated-flow cases
  (hump, periodic hills, duct)** come online — the flows where stock closures do
  fail and published corrections exist.
- **The closure team owns ladder execution and the closure-correction library.**
- The ladder is **MODEL-FORM ONLY**; process-class failures still halt and triage
  as before.

## Predecessors routed into the closure ladder

Predecessor: K0cS — model-form GATE FAIL (turbulent square cavity, Ampofo Ra 1.58e9; K0cS_RESULTS.md); routed into the closure ladder (Sanaa 2026-09-04), rung sequence: exhaust stock RANS / rule out numerics §0 / literature corrections / GP closures.
Predecessor: K0cT — model-form GATE FAIL (turbulent tall cavity, Betts & Bokhari; K0cT_RESULTS.md); routed into the closure ladder (Sanaa 2026-09-04).
Predecessor: K0cX — model-form GATE FAIL (3-model tall-cavity sweep; via K0cX_RESULTS.md); routed into the closure ladder (Sanaa 2026-09-04).

(Each Predecessor line names its id FIRST so the §2ay reader keys the lineage edge
to the K0c rung, not to the M6 / CRM / F6 sequencing tokens named above.)

## How the ladder preserves the gate

Each closure rung is **registered and gated individually against the SAME
reference** used by the original K0c GATE FAIL: the gate and the reference **never
move**. A rung either lands a PASS inside its own pre-registered band or it is a
GATE FAIL in turn, and the ladder advances to the next rung. The
**exhausted-ladder outcome is itself a certified product** — "all known closures
fail this flow at these numbers", shipped with the full ladder record (every
closure and correction tried, each deviation) — a rare and valuable certified
result, per the 2026-09-04 ruling.

## Scope guard (repeated because it is the whole point)

This record is lineage only. It records the dated fix path for K0cS / K0cT / K0cX
per §2ay.2(b). It does not move their GATE FAIL verdicts, arms no gate, and edits
no frozen file. **The K2bU3 unsteadiness rung is deliberately NOT covered by this
record** — it is a cost-bounded unsteadiness NOT A RESULT (undecidable at this
price), not a model-form failure, and does not route into the closure ladder. Its
id is written here without a bounded case token on purpose, so this record does
not accidentally register itself as that rung's successor.
