# LANE_REPORT — ansys-verification opus-4.8 lane, report of record

**NOT FILED ANYWHERE. Nothing here leaves this box** (CLAUDE.md rules 7, 8).

**Why this file exists.** A lane's report of record lives on disk, committed, in team
territory — a message that fails to address evaporates (`L-306`). This is that durable
channel; a `SendMessage` to the supervisor is a notification, not the artifact.
Append-only, newest section at the FOOT; each section carries its own disposition
(`L-305`).

---

## 2026-08-25 — records-integrity task (FAMILY INDEX + two lessons + board strings) — DELIVERED

**Disposition: DELIVERED. All commits at HEAD; per-item; each verified to touch exactly
one file; built from HEAD blobs (worktree copies of the shared ledgers are truncated);
CAS-clean. Zero compute.**

**ITEM 1 — FAMILY INDEX appended at the foot of `docs/NUMERICS_KNOWLEDGE.md`, commit
`dcf7b9be`.** Chronological append kept (no family grouping) per the supervisor's
ruling — 22 line-number citations, one inside a frozen prereg
(`A4/curriculum_D3/PREREGISTRATION.md:69` → `:3250-3257`), would break on any reorder.
The index carries NO line numbers, is found by grepping an id, notes both heading
styles (`## ` and `**`), and is regenerated by appending a superseding block (lowest
block authoritative), asserting lines-above-changed = 0. Id lists generated
programmatically from the same HEAD blob: 7 families, 113 entries; the N-B21 gap is
visible by design.

**ITEM 2 — two lessons landed** (ids re-derived max+1 from the HEAD blob at each
commit; appended at the foot; LESSONS.md worktree was truncated 655485 vs 768432 B):
- **`L-305`** (commit `ddcc7806`) — a drafts/staging file must carry its own DISPOSITION
  per item; a landed and an unlanded draft are indistinguishable from outside and git
  status distinguishes neither. Numbers: L-300/L-301 already at HEAD while the board
  said otherwise; N-AV7/N-AV8 forward-cited from register row #3 while NUMERICS stopped
  at N-AV6. Twin of L-186; forward-citation half is L-292.
- **`L-306`** (commit `109b132b`) — a lane's report of record lives on disk in its case
  directory, committed; a message that fails to address evaporates; the scratchpad is
  not a fallback (rule 13 / L-186). Checked HEAD first: no existing lesson in this class
  (the "watchers die with the agent" note is a monitor, not a report), so a new entry,
  not an addendum.

**ITEM 3 — strings for the board, verbatim from HEAD:**
- `N-AV7` — A small GCI is a statement about grid convergence ONLY — it does not license the claim that a residual deviation from a reference is numerical; VMFL005 is CONVERGING at 0.05 % GCI yet extrapolates 0.54 % AWAY from the exact value
- `N-AV8` — A gate-EXCLUDED null channel is excluded at the FREEZE and still PRINTED, never silently dropped — VMFL005's wedge `Uz` is the out-of-plane direction the wedge constrains rather than solves
- `N-AV9` — An OpenFOAM axisymmetric WEDGE under-represents the true circular cross-section by sin(t)/t (0.127 % at t = 5°), a MODELLING bias grid refinement does not remove — carry it in the error budget of every axisymmetric case run against an exact target
- Docket id opened for VMFL005's open mechanism: **`D512`** (commit `20afae1b`).

**Prior task commits, for the record:** COVERAGE_ROWS.md `ea10aa07`; CASE_MAP.md
GAP-CLOSING `c774cecd`; RECORDS_DRAFTS.md `a89da095` + SUPERSEDED `991b12b1`; and the
five repair commits — DOCKET D512 `20afae1b`, NUMERICS N-AV7/8/9 `7e2cb666`, register
foot-note `23fed33c`, RESULTS foot-note `4bb0f6a1`.

**Not done, flagged (chief-dispatch, outside our territory):** the machine docket
`demo-output/website/agenda/docket.json` may need D512 mirrored; `docs/LAB_STATE.md` is
the supervisor's to correct (L-300/L-301 already landed). I did not touch
`cases/ansys_verification/VMFL051/`.
