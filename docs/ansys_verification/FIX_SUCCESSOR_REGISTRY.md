# FIX / SUCCESSOR REGISTRY — Ansys VM landed fails under §2ay

**STATUS: DRAFT — for the ansys-verification supervisor's §2ay.7 diff-read and commit.**
Drafted by `ansys-lane-opus` (running as **claude-opus-4-8**), **2026-09-06**. **Zero
compute** — no solver was started; this is a reading of the register, the adopted
`RECOVERABILITY_SWEEP.md`, the supervisor's dated rulings of 2026-09-06, and the frozen
per-case pre-registrations. Nothing here is filed, sent or contacted upstream (rule 7).

**Law this registry answers to.** `VERIFICATION_CHARTER §2ay` (commit `75463642`) makes
Sanaa's "fix until it runs" executable. Its instrument `scripts/check_completion_enforcement.py`
flags every landed `GATE FAIL` / `NOT A RESULT` that is not in one of two acceptable
states: **(a)** a MEASURED, on-Sanaa's-desk capability-gap filing with all six of
bug / solver-selection / preconditioner / numerics-scheme / config / model-form ruled out
at source; or **(b)** an ACTIVE, DATED fix-successor — a registered next attempt that
**changes what failed and re-runs**, or its discharge by a landed passing successor.

**THE HEADLINE — read this before the table.** **NONE of the 18 cases below is a proven
OpenFOAM capability gap (state a).** Every one carries a live fix path and is therefore
**state (b)**. This is the answer the adopted `RECOVERABILITY_SWEEP.md` reached at source
("zero proven capability gaps; every fail carries a live fix path") and it is not changed
here. **⚠ §2ay.3 honesty bar:** each entry below is a *genuine registered next attempt that
changes the failing lever and re-runs* — NOT a bare mechanism diagnosis. A diagnosis alone
does not clear a flag; a dated successor with a concrete lever does. **No gate, threshold,
band, cap, tier or verdict is touched by this file.** It only records the next attempt each
landed fail is being carried forward by.

**Status vocabulary.** **LIVE** = a successor is frozen and queued/running now. **REGISTERED**
= a successor pre-registration directory already exists on disk (a later attempt already
run, or its prereg frozen and ready). **OWED-DATED-PLAN** = the next attempt is registered
*here*, dated, with its concrete lever named; its case directory is to be created and frozen
before that compute, per rule 2. All three are state (b) under §2ay.1.

**Verdicts were read from the register**
(`verification/credentials/ansys/ANSYS_VALIDATION_REGISTER.md`), the same artifact the
instrument enumerates from; the same fail rows also appear in `docs/capability/ansys_ROWS.md`
and `docs/CAPABILITY_GRID.md`. Case-id tokens below are written exactly as the instrument
extracts them, so each clears its every duplicate row.

---

## SUMMARY

- **Landed fails carried here:** 19. **Capability gaps (state a):** **0**. **State (b):** **19**.
- **LIVE:** 0 — VMFL046-R5 GRADED 2026-09-07 `NOT A RESULT` (register row #62): the shock
  **washed out** because `waveTransmissive lInf 2.0` un-anchored the back-pressure (outlet p
  collapsed to ~10% of the 176 325 Pa target). NOT a hunt. The R4 outlet-reflection question is
  therefore left OPEN, and the successor for the whole VMFL046 lineage is now **VMFL046-R6 with a
  back-pressure-anchoring outlet** (the pre-committed `rhoCentralFoam` R6 recipe carries the same
  defective `lInf 2.0` outlet and MUST fix it before freeze). State (b), a config/BC lever, not a
  capability gap.
- **REGISTERED (successor dir exists):** 8 — VMFL007-R2 → `VMFL007-R3`; VMFL011-R2 → `VMFL011-R3`;
  **VMFL054-R2 → `VMFL054-R3` (prereg + comparator FROZEN 2026-09-07, freeze `addd8240`; --selftest 16/16; LAUNCH HELD on Sanaa's desk)**;
  **VMFL034-R2 → `VMFL034-R3` (FROZEN-FLOW re-scope; prereg + comparator + frozen-flow solver source FROZEN 2026-09-07; --selftest 16/16; frozen-flow solver compiled + answer-blind smoke rc 0, no SIGFPE; LAUNCH HELD on Sanaa's desk)**;
  **VMFL010 → `VMFL010-R2` (SINGLE-LEVER, supervisor §3 ruling 2026-09-07: KEEP the base self-similar r=2 triple N=20/40/80 UNCHANGED — the finding that the base was ALREADY self-similar per birth certs stands, but the earlier "reposition to finest N=40" DROPPED N=80, the failing level, which has the shape of anti-circularity; the single lever is residualControl 1e-7→1e-9, directly testing the iterative-noise diagnosis on the SAME triple that failed. Prereg re-frozen 2026-09-07, freeze `02fb0e99` — 934605f3's prereg STRUCK not rewritten via dated amendment; --selftest 10/10; answer-blind N=80@1e-9 smoke rc 0 (3.533 core-min); gate 0.887 ±3% BYTE-IDENTICAL, L-487; LAUNCH HELD on Sanaa's desk)**;
  **VMFL051 → `VMFL051-R2` (SINGLE measurement-lever, L-500: the base OSCILLATORY triple is a temporally-persistent, mesh-convergent numerical oscillation — an answer-blind L1 smoke FALSIFIED both the reflex "more endTime" lever (ptp flat to 16 flow-throughs) and the dated plan's downstream-LINE lever (inner-zone ptp identical to gate); the lever is a TIME-MEAN of volAverage(Ma) over the settled window, KEEPING all three levels, the spatial zone, endTime 7e-3 s and the gate 3.2370 ±0.5% BYTE-IDENTICAL, L-487; prereg + comparator + driver FROZEN 2026-09-07, comparator blob `582dd1c8`, driver blob `0e1b3d4b`; --selftest 46/46; solve byte-identical to run 1; LAUNCH HELD on Sanaa's desk)**;
  **VMFL022 → `VMFL022-R2` (regime-consistent cavitating triple N2R 24/48/96 — the single-phase base level EXCLUDED by a gate-blind regime-consistency precondition, NOT dropped for its value; base's own uniform meshing, no grading DOF; L-500 tolerances tightened both ends; FROZEN 2026-09-07; --selftest 35/35; gate 0.780 ±5% BYTE-IDENTICAL, L-487; LAUNCH HELD on Sanaa's desk)**;
  **VMFL063 → `VMFL063-R2` (RESOLUTION-ONLY corrected-lever freeze 2026-09-07, freeze `a457a661`; base dated 'linearUpwind→linear' clause FALSIFIED — base fvSchemes was ALREADY `bounded Gauss linear` 2nd-order central, blob 0b7e1837, zero linearUpwind in history — STRUCK per supervisor §3 ruling; finer r=2 triple 23040/92160/368640, base grading inherited verbatim (DOF-free), schemes UNCHANGED, R2 case inputs byte-identical to VMFL063's; gate LR/(2t)=4.0 ±10% BYTE-IDENTICAL, L-487; --selftest 68/68; answer-blind smoke L1 1944 iters / L3 368640 mesh OK; LAUNCH HELD on Sanaa's desk)**.
- **OWED-DATED-PLAN (next attempt registered here):** 11 — the four VMFL046 lineage rows
  (VMFL046-INVISCID, VMFL046-R2, VMFL046-R3, **VMFL046-R5**, all → the outlet-anchored
  **VMFL046-R6**, dated 2026-09-07) plus the eight dated 2026-09-06: VMFL003-M2,
  VMFL011-R3, VMFL017-R2, VMFL072-R2,
  VMFLGPU002, VMFLGPU003, VMFLGPU005.

**GPU note (Task 1 finding).** VMFLGPU002, VMFLGPU003 and VMFLGPU005 are **LANDED, GRADED
verdicts, not drafts** — the GPU solver ran, the frozen comparator graded it (rc/exit 0 in
each, NOT an instrument refusal), and the verdict is recorded in the register with preserved
run artifacts under `verification/runs/ansys_verification/VMFLGPU00{2,3,5}/`. They are NOT
instrument false-positives; they genuinely need a dated successor like the CPU cases. GPU
quota is granted (us-east-2); `BLOCKED-GPU` is retired (CLAUDE.md rule 12), so a re-run is a
legitimate lever.

---

## REGISTRY TABLE

| Case (exact token) | Verdict | §2an.5 process class | Dated active successor — WHAT IT CHANGES, and it RE-RUNS | Status |
|---|---|---|---|---|
| **VMFL003-M2** | `NOT A RESULT` | #2 convergence (ε residual floor / wall-treatment) | **VMFL003-M3** — the M2 arms varied only `endTime`/grid; M3 changes the **near-wall treatment**: kOmegaSST or low-Re kEpsilon wall-resolved y+≈1 (or standard kEpsilon with the y+ 30–300 band held across all three levels) and a relaxed ε linear solver / residualControl, then re-runs the frozen triple. | OWED-DATED-PLAN |
| **VMFL007-R2** | `NOT A RESULT` | #4 instrument (planted-zero control refused, exit 2; no physics graded) | **VMFL007-R3** — repairs the planted-zero control so the reader is shown able to see a non-zero (sibling repair pattern in `VMFL011-R3/`), then re-registers and re-runs the pipe profile. Directory `cases/ansys_verification/VMFL007-R3/` exists. | REGISTERED |
| **VMFL010** | `NOT A RESULT` | #2 grid (triple OSCILLATORY) | **VMFL010-R2** — the base was ALREADY a self-similar structured-hex r=2 triple (birth certs: 3600/14400/57600 hex, non-orth 0, skew ~1e-13, junction included), so the "not cleanly refined" hypothesis is FALSIFIED by inspection; the split was textbook 2nd-order in magnitude (\|d32/d21\|=0.198, implied p=2.34) but SIGN-FLIPPED at a finest level (N=80) in a ~1e-4 iterative noise floor. R2 is SINGLE-LEVER (supervisor §3 ruling 2026-09-07): KEEP the base triple N=20/40/80 UNCHANGED (dropping N=80 — the failing level — as an earlier draft did has the shape of anti-circularity), change ONLY residualControl 1e-7→1e-9 (linear tol 1e-11), directly testing the iterative-noise diagnosis on the SAME triple that graded OSCILLATORY; gate 0.887 ±3% BYTE-IDENTICAL (L-487). Directory `cases/ansys_verification/VMFL010-R2/` exists; prereg re-frozen at **`02fb0e99`** (934605f3 STRUCK not rewritten via dated amendment; comparator blob `235fd332`, driver blob `fae72487`; --selftest 10/10; answer-blind smokes N=40 0.667 + N=80 3.533 core-min to residualControl 1e-9). If it still oscillates at 1e-9 → NOT A RESULT, honest finding of genuine mesh-sensitivity (a future separate R3 may then justify N≤40 with that documented data). Re-runs. **LAUNCH HELD** (Sanaa's desk, rule 9). | REGISTERED |
| **VMFL011-R2** | `NOT A RESULT` | #4 instrument (earlier planted-control refusal) | **VMFL011-R3** — the instrument refusal that failed R2 is fixed (rc measured, clean End, planted control fires); R3 re-ran the case and landed a graded value. Directory `cases/ansys_verification/VMFL011-R3/` exists. | REGISTERED |
| **VMFL011-R3** | `GATE FAIL` | #2 grid `[inferred]` (rms still falling with refinement) | **VMFL011-R4** — adds an **L4 finer level** and a higher-order convection scheme (linearUpwind→linear), and **bounds the Jyotsna & Vanka digitisation error** (§2al/§2am) before reading the band; re-runs the extended ladder against the frozen band ≤0.030. | OWED-DATED-PLAN |
| **VMFL017-R2** | `NOT A RESULT` | #1 completion (per-level cap rc=124 + comparator refused; no gradeable value) | **VMFL017-R3** — a **converging transonic rhoSimpleFoam setup** (the base VMFL017 divergence is process class #3, a finding until triaged): relaxation/under-relaxation and a stable transonic initialisation so the solve completes to `endTime`, then re-runs. | OWED-DATED-PLAN |
| **VMFL022** | `NOT A RESULT` | #2 grid (triple OSCILLATORY — a PHYSICAL REGIME CHANGE: base N2R=12 ran single-phase, N2R=24/48 cavitated) | **VMFL022-R2** — FROZEN 2026-09-07 (`cases/ansys_verification/VMFL022-R2/`). Supervisor §3 ruling "lever B": the base OSCILLATORY (R=−0.105) was a REGIME CHANGE, NOT L-500 noise (window CoVs 1e-4…2e-3) — measured min alpha.water 1.000000 / 0.263568 / 0.023003 across N2R 12/24/48. A single-phase orifice solve is a different PDE branch, not a coarse-grid estimate of the cavitating-orifice Cd, so the base coarse level is EXCLUDED a-priori by a GATE-BLIND regime-consistency precondition (every level min alpha.water ≤ 0.90), NOT by any Cd comparison — categorically distinct from the VMFL010 drop-the-failing-level trap. Triple SHIFTED to the base's OWN uniform meshing **N2R 24/48/96** (r=2, NO grading DOF; the in-place edge-grading "lever A", answer-blind-confirmed feasible at min alpha.water 0.773, was REJECTED for its continuous grading knob). L-500 applied at BOTH ends (linear-solver tolerances tightened ~2 orders, all levels). Gate **Cd 0.780 ±5% BYTE-IDENTICAL** (L-487); tier ceiling GATE REACHED. Prereg blob `fe5b1a65662e0827221085a70d0b58e897ef927c`, comparator `grade_vmfl022_r2.py` blob `ea29630c797e85a1d43c65c0a19036d70d315eb2` (--selftest 35/35, L-487 proper-subset plant + inert arm + gate-blind regime precondition), driver blob `35d936541cb8c5c567dc958143af9bc1c14a5d5e` (freeze-pin disk==HEAD). Answer-blind (Cd NEVER read; onset is a mesh-independent t≈0.03 event): N2R=24/48 MEASURED CAVITATING at endTime (min alpha.water 0.2649 / 0.0238 « 0.90); N2R=96 (finer than the cavitating 48) cavitation a sound inference from the monotone 12/24/48 trend (1.000/0.265/0.024), direct measurement past onset in progress (endTime 0.04); the comparator regime precondition ENFORCES all-cavitating at grade time. Cost (reported-by-owner): 0.88 / 8.18 / ~65-70 core-min (N2R=96 partial to t=0.015 = 17.08 core-min), total ~79, CAP 300. Re-runs. **LAUNCH HELD** (Sanaa's desk, rule 9). | REGISTERED |
| **VMFL034-R2** | `NOT A RESULT` | config / numerics (R2 SIGFPE rc 136 in `SchillerNaumann::CdRe`, live two-phase drag) | **VMFL034-R3** — FROZEN 2026-09-07 (`cases/ansys_verification/VMFL034-R3/`). Adopts the supervisor-led **frozen-flow re-scope** = the manual's OWN regime (p.121: "Moments are solved on a frozen flow field"): a two-stage run — Stage 1 solves the k-ε carrier once (`simpleFoam`), Stage 2 transports the constant-kernel population balance on the FROZEN carrier via a frozen-flow build of `reactingTwoPhaseEulerFoam` (`solver/reactingTwoPhaseEulerFoamFrozen/`) whose PIMPLE loop OMITS `pU/UEqns.H`+`EEqns.H`+`pU/pEqn.H` — the ONLY site invoking `fluid.Kd()`→`SchillerNaumann::CdRe`. R2's SIGFPE mechanism removed at its ROOT (structurally, not bounded). Gate/band/triple/kernel UNCHANGED from R2 (L-487 anti-circularity preserved). Comparator `analyse_vmfl034_r3.py` (blob `5fc867d9`, --selftest 16/16) adds a gate-blind physical-range refusal + the L-487 plant-design guard. Fix-until-runs (§2ay): frozen-flow solver **compiled** + **answer-blind smoke rc 0, reached End, 0 momentum/drag solves, no SIGFPE**. Re-runs. LAUNCH HELD on Sanaa's desk. | REGISTERED |
| **VMFL046-INVISCID** | `NOT A RESULT` | config / numerics (outlet reflection at the boundary) | **VMFL046-R6 (outlet-anchored)** — R5 (register #62) tried the single-variable `fixedValue`→`waveTransmissive lInf 2.0` outlet and graded `NOT A RESULT`: it did NOT hunt, the shock **washed out** because `lInf 2.0` un-anchored the back-pressure. R6 = the pre-committed `rhoCentralFoam`/Kurganov recipe (`VMFL046-R5/PREREGISTRATION.md §6`) **with its outlet BC fixed to anchor `p≈176 325 Pa` while damping reflections** (gate-blind choice; the `lInf 2.0` carry-forward is now known-defective). Re-runs. | OWED-DATED-PLAN |
| **VMFL046-R2** | `NOT A RESULT` | config / numerics (outlet reflection at the boundary) | **VMFL046-R6 (outlet-anchored)** — same lineage successor; R5's non-reflecting outlet washed the shock out (#62), so R6 carries `rhoCentralFoam`/Kurganov with a back-pressure-anchoring outlet, not `lInf 2.0`. Re-runs. | OWED-DATED-PLAN |
| **VMFL046-R3** | `NOT A RESULT` | config / numerics (outlet reflection at the boundary) | **VMFL046-R6 (outlet-anchored)** — same lineage successor; R6 = `rhoCentralFoam`/Kurganov with a back-pressure-anchoring outlet (R5's `lInf 2.0` washed out, #62). Re-runs. | OWED-DATED-PLAN |
| **VMFL046-R5** | `NOT A RESULT` | config / numerics (outlet un-anchored the back-pressure — the shock washed out; register #62, diagnosed 2026-09-07) | **VMFL046-R6 (outlet-anchored)** — the `waveTransmissive lInf 2.0` outlet relaxed the exit pressure to ~10% of target and the shock washed out of the domain (not a hunt, uniform at L1/L2/L3). R6 keeps the density-based `rhoCentralFoam`/Kurganov shock-capturing path but **must replace the outlet with a partially-reflecting subsonic outlet that anchors `p≈176 325 Pa`** (candidates to research/pin gate-blind: a smaller geometric `lInf`, an alternative anchoring BC, or the Fluent pressure-outlet equivalent) — a density-based solver does not restore a lost back-pressure. Re-runs. | OWED-DATED-PLAN |
| **VMFL051** | `NOT A RESULT` | #2 grid (triple OSCILLATORY) | **VMFL051-R2** — FROZEN 2026-09-07 (`cases/ansys_verification/VMFL051-R2/`). SINGLE measurement-lever (L-500): the base OSCILLATORY triple is NOT mesh non-similarity (mesh self-similar by construction) and NOT temporal under-convergence — an **answer-blind L1 smoke** (value-blind: ptp + mean-differences only, graded Mach never read) FALSIFIED the reflex "more endTime" lever (L1 ptp flat at ~6.6e-3 out to 16 flow-throughs) AND the dated plan's downstream-LINE lever (inner-zone ptp 6.689e-3 identical to gate 6.687e-3 ⇒ the noise is TEMPORAL, a line would be noisier). The base graded a single-endTime SNAPSHOT of a temporally-persistent, mesh-convergent numerical oscillation (corner-seeded Kurganov noise; L1 6.6e-3, L2 3.5e-3, L3 0.85e-3) at random phase. The lever = **TIME-MEAN of volAverage(Ma) over the settled window**, KEEPING all three levels, the spatial gate zone, endTime 7e-3 s and the gate 3.2370 ±0.5% BYTE-IDENTICAL (L-487); the snapshot-plateau clause is re-expressed as window-length insensitivity (the settledness of a time-mean). Comparator `grade_vmfl051_r2.py` (blob `582dd1c8`, --selftest 46/46) adds a gate-blind physical-range refusal + L-487 proper-subset plants; driver blob `0e1b3d4b` (freeze-pin disk==HEAD). Solve byte-identical to run 1 (all case blobs match). Re-runs. **LAUNCH HELD** (Sanaa's desk, rule 9). | REGISTERED |
| **VMFL054-R2** | `GATE FAIL` | #2 grid (order not asymptotic — observed p=3.438 ∉ [1,3]) | **VMFL054-R3** — adds a **4th finer level L4 = 320×320 (r=2)** so the order is graded on the FIXED finest triple L2/L3/L4 (nearest the asymptotic range), with a 4-point settling diagnostic; band [1,3] AND GCI ≤ 5 % frozen **BYTE-IDENTICAL** to R2 and **NOT widened** (L-487 anti-circularity). Directory `cases/ansys_verification/VMFL054-R3/` exists; prereg + comparator FROZEN at **`addd8240`** (comparator blob `fd959928`, driver blob `f451c24c`; `--selftest` 16/16; L4 mesh checkMesh OK, L4 solve de-risked converging within cap). Re-runs the extended ladder. **LAUNCH HELD** (Sanaa's desk, rule 9). | REGISTERED |
| **VMFL063** | `GATE FAIL` | #2 grid (GCI_fine 120.6% ≫ the 40% deviation) | **VMFL063-R2** — FROZEN 2026-09-07 (`cases/ansys_verification/VMFL063-R2/`), freeze `a457a661`. RESOLUTION-ONLY (supervisor §3 check-2 ruling 2026-09-07). **The dated plan's "higher-order convection (linearUpwind→linear)" clause is STRUCK** (corrected 2026-09-07): the base fvSchemes was ALREADY `bounded Gauss linear` (2nd-order central, least-diffusive), blob `0b7e1837`, disk==HEAD, zero linearUpwind in its full history — the clause was a no-op built on a false premise. Diagnosis (answer-blind): base p=0.143 / GCI 120% on an already-central scheme, with LR/(2t) monotone-decreasing-toward-4.0 (6.79→6.16→5.60), = UNDER-RESOLUTION (leading-edge corner singularity caps the observed order; separated shear layer under-resolved at 92160 cells), NOT a scheme defect; setup audited against the manual (Re_2t=260, 2t=0.090, U, ν, geometry, symmetry, BCs) — no bug; 2.5% blockage ≪ the 40% deviation. Lever = finer r=2 triple **23040 / 92160 / 368640** (the base's OWN self-similar grading inherited VERBATIM, family shifted up one octave; NO continuous grading DOF, L-501-safe), schemes UNCHANGED; R2 case inputs are BYTE-IDENTICAL to VMFL063's (all 9 blobs). Gate **LR/(2t)=4.0 ±10% BYTE-IDENTICAL** (L-487); Limb B (SAME-DISCRETE-PROBLEM IDENTITY) retained; comparator adds a **gate-blind physical-range refusal** (references neither 4.0 nor the band) + an explicit **L-487 inert arm**. Prereg blob `ec857514`, comparator `grade_vmfl063_r2.py` blob `5d94fecb` (--selftest 68/68 under python3 and python3 -O), driver blob `8eccf253` (freeze-pin disk==HEAD). Answer-blind smoke (scratch, LR NEVER read): L3 368640 `Mesh OK` (non-ortho 0), L1 converged 1944 iters / 1.007 core-min. Cost estimate 125 core-min, CAP 320 (running total); $0.107 / $0.274 derived @ $0.0513/core-h reported-by-owner. FROZEN caveats: the corner singularity may cap p<2 → if the finest triple is not asymptotic/CONVERGING it is NOT A RESULT, NOT rescued by dropping the failing level (L-500); if a resolved 2nd-order triple is still ~40% off, the GATE FAIL STANDS (no gate-fitting, rule 2). Re-runs. **LAUNCH HELD** (Sanaa's desk, rule 9). | REGISTERED |
| **VMFL072-R2** | `NOT A RESULT` | numerics / model (film dewetting), per supervisor routing 2026-09-06 | **VMFL072-R3** — dewetting remedy: an alternate film model `kinematicSingleLayer`, **or** precursor-film regularization, **or** a VOF re-formulation; the anti-circularity property (L-487) is kept; re-runs. | OWED-DATED-PLAN |
| **VMFLGPU002** | `NOT A RESULT` | #2 grid (GPU triple OSCILLATORY, R=−0.197; limb B GPU=CPU at 4.07e-10) | **VMFLGPU002-R2** — a **monotone grid triple** for the flow-split functional (structured, r=2 cleanly refined near the split), re-run **on the GPU path** (petsc4Foam) so the split converges monotonically; the verified GPU=CPU agreement is unaffected. Re-runs. | OWED-DATED-PLAN |
| **VMFLGPU003** | `GATE FAIL` | #2 grid / referent (limb C rms 0.0341 vs 0.030 band, CONVERGING triple; digitised benchmark) | **VMFLGPU003-R2** — GPU sibling of VMFL011-R3: adds an **L4 finer level** + higher-order convection and **bounds the Jyotsna & Vanka digitisation error** (§2al/§2am), re-run on the GPU path; limbs A/B already hold, so the physics limb is the lever. Re-runs. | OWED-DATED-PLAN |
| **VMFLGPU005** | `NOT A RESULT` | #2 grid (rule 5 disqualified channels C1/C2 non-CONVERGING; limb B PASS at 2.957e-10) | **VMFLGPU005-R2** — rebuilds **monotone/CONVERGING grid triples for the C1/C2 physics channels** (finer, cleanly refined), re-run on the GPU path; the verified GPU path (limb B) is unaffected. Re-runs. | OWED-DATED-PLAN |

---

## FILING NOTE

This is a durable, checkable state-(b) registry (the recognized `*SUCCESSOR*`-named shape the
§2ay instrument reads), so a repository document under the team's owned
`docs/ansys_verification/` is the correct form (FILING_CHARTER). It carries no gate,
threshold, band or verdict change. To be reviewed under the supervisor's §2ay.7 diff-read
and committed under the CLAUDE.md rule-10 private-index protocol. Each OWED-DATED-PLAN
successor's case directory and frozen pre-registration is created **before** its compute
(rule 2); this file registers the dated intent and the concrete lever, not a completed run.
