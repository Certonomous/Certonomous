# DOCKET_DRAFT — R4 SpaRTA-class build lane

**FILED — DO NOT APPEND AGAIN.** This row was committed to `docs/DOCKET.md` as
**D461** at `7965d08b` (2026-08-22), the number taken from the tail (D460) at
commit time. This file is the draft of record, not a pending item.

One row. The number is assigned by the supervisor at commit, **from the tail of
`docs/DOCKET.md` — the maximum existing number, never a count** — re-derived in
the same shell invocation as `commit-tree`, because peers commit constantly.
At the time of writing the tail was **D457**, and a peer reported it already
moved past that at `6a9b8c41`. Run
`scripts/check_docket_reconciliation.py` before editing `docs/DOCKET.md`.

---

| D&lt;n&gt; | **THE SPARTA-CLASS MODEL DISCOVERED ON TWELVE TRAINING CASES MISSES THE A-PRIORI BAR AND DIVERGES ON EVERY ONE OF THEM — AND FOR THE FIRST TIME IN THIS PROGRAMME THE CEILING THAT WOULD HAVE EXCUSED IT WORKS.** R4 build ladder under the frozen preregistration of D443/D444 (`cases/RANS_LES_closure_models/R4_sparta_build/PREREGISTRATION.md`, sha256 `05844430…cbbe8`, verified at lane start and at every commit, never edited). **Targets**: 27 `kOmegaSSTFrozen` extractions built from the benchmark's own fields; `PHLL10595` and `CBFS13700` come back **byte-identical to the W2 record** — inputs and outputs, at its own settle iterations 1492 and 354. **12 of 27 COMPLETE** under the strict completion rule (6 hills, 4 ducts, PHLL10595, CBFS13700; 172,171 cells; all four families). The 15 missing hills are diagnosed, not dropped: the frozen `omega` source carries `gamma(Pk+R)/max(nu_t,1e-12)` and the shipped hills carry `nu_t` down to **5.55e-12**, so `omega` is driven negative and clipped **every iteration** — 13 sit in a limit cycle whose residual is identical to **fifteen significant figures at iteration 5000 and at 20000**, and **2 reported CONVERGED at iteration 51 with a drift of exactly 0.0 because the field had been clipped flat**, which is why the completion rule gained a sixth condition. Four repairs tested, four failed. **Model** (`MODEL.md`, frozen before propagation, identical at seeds 0/1/2, IC1 solver-vs-Python **3.97e-12**): `R = 2k[1.261646 − 42.82548 I1 − 31.54762 I2 + 14.28260 I2²](T1:A)` — containing exactly the three candidates Schmelzer names for `R`, `T1` coefficient inside the paper's published 0.39–1.39 span — and `b^Δ = −7.550380 T2 − 16.07578 I2 T2 + 5.039083 T3`. **Sec. 1's T1–T3 solver constraint costs nothing, measured**: the constrained model generalises **better** cross-family than the unconstrained T1–T4 one (0.0156199 vs 0.0158092), so no static-injection *weaker check* was needed. **The three FS3 methods DISAGREE** (permutation-vs-MI Spearman 0.192, −0.283, 0.114, −0.119), which §3 registers in advance as a finding. **Planted-zero control PASS** on both propagated sets, **GATE FAIL** on `R`/T1–T4 where a selected term scores **−322.2** on held-out permutation importance, worse than a planted zero. **G1 a-priori GATE FAIL, 2 of 4 families** beat the train-mean tensor against a bar of 3; it beats the linear EVM on three and loses to a single **constant tensor** on the ducts (0.4839 vs 0.3687) and `CBFS13700` (0.3763 vs 0.3277). **G2 a-posteriori GATE FAIL: all 12 propagations DIVERGE**, in 5–18 iterations, FPE inside `updateCorrections`, `Ubar` reaching 5.7e+69 — because the model's total anisotropy is unrealisable on **19.1 % of `CBFS13700`** cells and 4.4–6.8 % of every duct, with `‖b^Δ‖_F` to **7.56** against a realisable bound near 0.82. **THE NOT A RESULT BRANCH DID NOT FIRE**: the per-case ceiling beats NULL by **60.2 %** on `CBFS13700` and **99.6 %** on `PHLL10595`, lands `CBFS13700` at **0.3975** against W2's independently obtained **0.39753**, recovers the duct secondary vortex to **0.4–0.6 %** of DNS where the linear EVM gives **exactly zero**, and recovers reattachment to **0.9–3.4 %** of the LES where the baseline over-predicts by 39–100 %. So the failure is the model's, not the harness's. **Two ungraded diagnostic arms locate it**: the paper's own `xi = 0.1` remedy converges on 3 of 4 ducts and **still diverges on every separated flow** — the paper's "sufficient to achieve convergence" does not transfer — and **`R` alone diverges on all six hills and `CBFS13700`**, which bounds what D443's `TRUTH+R` 98.3 % duct-error cut licenses for a *discovered* `R`. Continuity: NULL 12/12 and CEILING 10/11 inside the registered `1e-4`; `AR_1_Ret_180` CEILING at **1.0628e-04** is NOT CONVERGED as written, with the note that the threshold is dimensional and the ducts run at 37.5 m/s on a 1 mm half-height. **Also measured, and it corrects the preregistration's own premise**: the exact duct degeneracy §2.1 registers (`T4=−T3` to 2.74e-17, rank exactly 3.000) reproduces on the **baseline RANS** field and is **absent on the frozen field the regression fits** — rank **3.965**, `‖T3+T4‖/‖T3‖` 1.28e-02 — because a linear EVM duct has no secondary flow and the DNS mean does. The registered exclusion is unaffected (it is scoped to ducts-only fits, which this lane never ran) but its justification is not true of frozen fields. **Disclosed**: one `b^Δ` fit and 24 propagation arms **WITHDRAWN** before any a-posteriori number was read, and 8 in-flight runs stopped, after the a-priori gate returned a model *worse than predicting zero on its own training target* — arithmetically impossible for least squares, and a scrambled tensor design matrix; `R` was unaffected and reproduces bit for bit. The planted-zero control's pass rule was revised once after first run (D-5) and the reader control's tolerance made float-aware (D-9); both disclosed. **CANNOT SEE**: anything about generalisation — every number is a training-family number and no TEST case was opened, asserted in code at five entry points; whether the 15 missing hills would move the coefficients; whether a positivity-preserving discretisation of the frozen `omega` source would converge; and the model's realisability cannot fail a gate because **§6 registers no threshold on it**, the same omission Charter §4 was written to stop. **9.203 core-h, $0.472**, against a 12–20 core-h estimate and a 40 core-h cap. `cases/RANS_LES_closure_models/R4_sparta_build/RESULTS.md`. |
