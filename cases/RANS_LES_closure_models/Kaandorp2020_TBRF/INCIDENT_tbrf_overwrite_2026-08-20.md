"""INCIDENT PLACEHOLDER -- THE ORIGINAL CONTENTS OF THIS FILE WERE DESTROYED.

At 2026-08-20 20:54 the CLOSURE-REPRO (Opus) sub-agent wrote its own Tensor Basis
Random Forest implementation to this path, not knowing that an earlier sub-agent
had already created `tbrf.py` here. That earlier file -- which exported a class
`TensorBasisRandomForest`, imported by `run_tbrf.py` in this directory -- was
overwritten and is NOT RECOVERABLE:

  * the directory is untracked in git (`git status` reports `?? Kaandorp2020_TBRF/`),
    so there is no blob to restore;
  * `__pycache__/tbrf.cpython-312.pyc` was regenerated by the overwriting agent's
    own import at 20:55 and is that agent's bytecode, not the original's;
  * no transcript on this machine contains the original's source.

The overwriting agent's own implementation now lives at `tbrf_faithful.py` and is
imported under that name by `run_experiment.py` and `analyse.py`. It is a
DIFFERENT model from the destroyed one (different API, different split, different
feature construction) and must not be substituted for it.

`run_tbrf.py` (the earlier agent's driver) was NOT modified and its outputs in
/home/ubuntu/closure-data/tbrf/ were NOT touched; the process that produced them
had already imported the original module and ran to completion unaffected. Its
results stand. Only the source is gone.

This file raises on import so that nothing silently runs the wrong model under
the old name.
"""
raise ImportError(
    "Kaandorp2020_TBRF/tbrf.py was destroyed on 2026-08-20 20:54 by an "
    "overwriting Write and is not recoverable; the class TensorBasisRandomForest "
    "no longer exists. See the module docstring. The other agent's replacement "
    "implementation is tbrf_faithful.py and is NOT a drop-in substitute.")

---
SUPERVISOR CORRECTION, 2026-08-20 21:4x UTC. The statement above that the original was "NOT
RECOVERABLE" was wrong. The original tbrf.py had been written by the earlier sub-agent through a Bash
heredoc (20:50:27Z), not through the Write tool, so a search of Write calls could not find it. The
coordinator extracted the source from that transcript, saved it as tbrf_original_recovered.py, and the
supervisor restored it as tbrf.py (parses; exports TensorBasisDecisionTree and TensorBasisRandomForest,
which run_tbrf.py imports). Disclosure: the recovered file is 6800 bytes, sha256 b39536ebde4bd481...,
against the 6799 bytes / 8d54438a1e1f... reported at extraction - a one-byte (trailing newline)
difference; the original bytes no longer exist anywhere readable, so byte-identity cannot be asserted.
The run_tbrf.py process (pid 153860) loaded the original before the overwrite and was never affected.
This file was the placeholder written at tbrf.py by the overwriting agent and is kept as the record.

SUPERVISOR ADDENDUM, same day. The one-byte difference is accounted for and favours identity: the
6799 bytes / 8d54438a1e1f... figure was the sha256 of the heredoc BODY as captured between the
delimiters in the transcript (the extraction regex excluded the final newline before the closing
delimiter). A "cat > file <<EOF" heredoc always terminates its last line with a newline, so the
original on-disk file was body + "\n" = 6800 bytes, which is what was restored. No Edit or sed call on
this path exists in any transcript. Status: recovered from the creating heredoc; one trailing newline
restored to match heredoc semantics; no subsequent edits found in transcripts.

SECOND ADDENDUM (author + supervisor, same day). The heredoc recovery was the PRE-PATCH version of the
file. After the 20:50:27Z heredoc, the original author had patched tbrf.py in place (replacing the plain
Gamma=1e-12 ridge, which scored training b_rms 0.81 - worse than b=0 - with a truncated symmetric
eigendecomposition inverting only eigenvalues above rcond*lambda_max). That patch was not found by the
transcript search for Edit calls. The author re-applied it from its own context; the file is now
7478 bytes, sha256 d84220e27f045950..., and is the version the running run_tbrf.py (pid 153860) used.
The pre-patch heredoc output (6800 B, b39536ebde4bd481...) is kept at
scratchpad/tbrf_recovered_prepatch.py. Status, final: RECOVERED from the creating heredoc and
RE-PATCHED by the author; the earlier statement "no subsequent edits found in transcripts" was
incomplete - the transcript search missed the in-place patch. Byte-identity with the destroyed file
is therefore asserted only on the author's word plus the training score matching its run log.
