# M1 MULTI-MODEL SWEEP — GRADE ATTEMPT 2026-09-09 — VERDICT: BLOCKED

**VERDICT: BLOCKED.** The frozen comparator grade_m1.py (sha256 b3decc88aca147c9a975bfa21f9ce71ffeac2c34dd34a6943935e20114b78e6e, verified byte-identical to the freeze commit 7b00b3ec and to HEAD) REFUSED with exit 2 at its C1 planted-zero control, BEFORE computing any gate. Raw grader output: M1_GRADE_RESULT_2026-09-09.txt (same dir). G0-G4 are NOT COMPUTED. Grading cost 0.43 core-min [MEASURED] (wall 25.90 s x 1 rank / 60); 0 solver core-min (the 78 solves predate this).

**Why (verified, independently reproduced by the supervisor from the frozen source).** C1 plants PLANT=1.234e-3 onto the first internalField cell of the sort-first COMPLETE donor kOmega/AR_10_Ret_180/20000/U (first cell 47.0666702943016 m/s), re-reads from disk, and passes iff `seen >= PLANT - 1e-15` (grade_m1.py:215). seen = 0.0012339999999966267 — short of PLANT by 3.37e-15 (~half a ULP at magnitude 47), which exceeds the ABSOLUTE 1e-15 pass tolerance. The reader is NOT blind (it sees 1.234e-3); this is a FALSE-POSITIVE refusal that destroys a legitimate instrument — the opposite direction from DEAD_LEVER_AUDIT §16.1. It refuses on any O(1)+ velocity donor, so grade_m1.py cannot grade this sweep at all.

**The successor inherits it.** grade_m1b.py:357 copied the predicate verbatim, so the M1b successor would refuse identically. The M1b selftest passed only because its positive limb uses 1e-12 (grade_m1b.py:1397) and a small-magnitude fixture. Fix lands in the UNFROZEN M1b successor (a PLANT-relative tolerance + a real-magnitude selftest fixture); the frozen grade_m1.py is NOT edited (rule 6; §16.1 puts changes to this frozen grading path with verification).

**Independent supervisor completion census (corroborating, for the eventual regrade under fixed M1b):** 6 of 78 arms INCOMPLETE — rc=124 wall-timeout, final time dir absent (only 0/ and 0.orig/): kOmega/{AR_1_Ret_180, AR_7_Ret_180, AR_14_Ret_180, PH_Breuer}, kOmegaSST_null/{AR_1_Ret_180, PH_Breuer}. Once C1 is fixed, G0 will GATE FAIL on these 6; G2/G3/G4 compute on the 72 complete. WASTE (rule 12 §6): the 6 timeout arms' wall_s from their STATUS files are 213, 1489, 3063, 1502 (kOmega) and 213, 1502 (kOmegaSST_null), all at ranks=1 — sum 7982 wall-s = 133.03 core-min [MEASURED from the six STATUS wall_s], $0.114 DERIVED at $0.0513/core-h (owner-stated rate; the box cannot read its own billing, so the dollar figure is derived, not measured). All six are under the 3600-wall-s stall line.

**Independent G2/G3 spot-check (supervisor §3 check 3), for the regrade:** a fresh standalone reader (not grade_m1.py) on 5 computable sample cases gave null-vs-reference relL2 = AR_1_Ret_360 2.834e-4, AR_3_Ret_180 1.864e-3, CBFS 1.186e-3, alpha_075 8.9e-6, alpha_125 7.1e-6 (AR_3 and CBFS exceed the 1e-3 G2 band and, being non-hill 'unmatched' cases, would be the <=2 permitted outliers IF the comparator agrees — the comparator uses the SAME unweighted flattened relL2, so agreement is expected); arm-to-arm relL2 1.33e-2 to 7.15e-2 (clear separation, supports G3). These are NOT a verdict — G2/G3 are uncomputed until the successor grades.
