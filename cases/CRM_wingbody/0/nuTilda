FoamFile{version 2.0;format ascii;class volScalarField;location "0";object nuTilda;}
dimensions [0 2 -1 0 0 0 0];
internalField uniform 0.0012610370502;
boundaryField
{
    "(wing|body)" { type fixedValue; value uniform 0; }   // wall-resolved, no wall functions
    symmetry      { type symmetry; }
    farfield      { type freestream; freestreamValue uniform 0.0012610370502; }
}
