FoamFile{version 2.0;format ascii;class volScalarField;location "0";object T;}
dimensions [0 0 0 1 0 0 0];
internalField uniform 310.0;
boundaryField
{
    "(wing|body)" { type zeroGradient; }        // adiabatic walls, her section 6
    symmetry      { type symmetry; }
    farfield      { type freestream; freestreamValue uniform 310.0; }
}
