FoamFile { version 2.0; format ascii; class dictionary; object fvSchemes; }
// Exactly the shipped shockTube tutorial's schemes
// (/usr/lib/openfoam/openfoam2606/tutorials/compressible/rhoCentralFoam/shockTube/system/fvSchemes).
fluxScheme      Kurganov;
ddtSchemes      { default Euler; }                 // explicit first order in time
gradSchemes     { default Gauss linear; }
divSchemes      { default none; div(tauMC) Gauss linear; }
laplacianSchemes{ default Gauss linear corrected; }
interpolationSchemes
{
    default          linear;
    reconstruct(rho) vanLeer;
    reconstruct(U)   vanLeerV;
    reconstruct(T)   vanLeer;
}
snGradSchemes   { default corrected; }
