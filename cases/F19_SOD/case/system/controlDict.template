FoamFile { version 2.0; format ascii; class dictionary; object controlDict; }
application     rhoCentralFoam;
startFrom       startTime;
startTime       0;
stopAt          endTime;
endTime         0.2;           // t_end = 0.2: shock at 0.8504, rarefaction head at 0.2634
deltaT          __DT__;        // FIXED; refines by 2 with h (classical Co = 0.175, solver prints 0.088)
writeControl    runTime;
writeInterval   0.2;           // fields at endTime only: the gates read t = 0.2
purgeWrite      0;
writeFormat     ascii;
writePrecision  12;
writeCompression off;
timeFormat      general;
timePrecision   10;
runTimeModifiable false;
adjustTimeStep  no;            // dt is registered, never adapted
maxCo           0.2;
maxDeltaT       1;
