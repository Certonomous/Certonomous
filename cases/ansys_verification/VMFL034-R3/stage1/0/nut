FoamFile { version 2.0; format ascii; class volScalarField; object nut; }
dimensions      [0 2 -1 0 0 0 0];
internalField   uniform 1e-8;
boundaryField
{
    inlet       { type calculated; value uniform 1e-8; }
    outlet      { type calculated; value uniform 1e-8; }
    topWall     { type nutkWallFunction; value uniform 1e-8; }
    bottomWall  { type nutkWallFunction; value uniform 1e-8; }
    fixedWalls  { type nutkWallFunction; value uniform 1e-8; }
    frontAndBack{ type empty; }
}
