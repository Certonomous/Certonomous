FoamFile { version 2.0; format ascii; class volScalarField; object epsilon; }
dimensions      [0 2 -3 0 0 0 0];
internalField   uniform 1.5e-4;
boundaryField
{
    inlet       { type fixedValue; value uniform 1.5e-4; }
    outlet      { type inletOutlet; inletValue uniform 1.5e-4; value uniform 1.5e-4; }
    topWall     { type epsilonWallFunction; value uniform 1.5e-4; }
    bottomWall  { type epsilonWallFunction; value uniform 1.5e-4; }
    fixedWalls  { type epsilonWallFunction; value uniform 1.5e-4; }
    frontAndBack{ type empty; }
}
