# DRAFT CALIBRATION ROWS — VMFL046-R9-REGRADE

**DRAFT, NOT COMMITTED, NOT IN THE LEDGER.** Written by an `ansys-lane-opus` 2026-09-10.
Two rows for `docs/COST_CALIBRATION.md`, to be **appended at the foot of that file's table**
under its append rules (append-only; an existing row is never edited; a correction is a new
row naming the row it corrects) and the rule-10 private-index protocol. The supervisor
commits.

**Why two rows.** Rule 12 requires the estimate-versus-actual comparison **at process
completion**. Two distinct processes completed here: (i) the **grading act** registered by
`VMFL046-R9-REGRADE`, which had its own frozen estimate and cap; and (ii) the **VMFL046-R8
solver run**, whose rung only became *graded* now — it was previously ungradeable through an
instrument defect. **The 590.7413 core-min of (ii) is recorded at Row #71 and is NOT
re-charged to (i). One solve, two readings, one spend.**

**Row ids** are drafted below; if they collide at commit time, regenerate them in the same
shell invocation.

**Ledger column order** (from `docs/COST_CALIBRATION.md:77`):

    | id | date | team | process/rung | predicted (core-min or GPU-h) | actual gross | actual cleaned | ratio (cleaned/predicted) | gap attribution | record ref (commit/results file) |

---

## ROW 1 — the grading act

| field | value |
|---|---|
| **id** | `C-20260910T155654.923375Z-1abc39c1` |
| **date** | 2026-09-10 |
| **team** | ansys-verification |
| **process/rung** | **VMFL046-R9-REGRADE** (Ansys VM2026R1 p. 155, supersonic normal shock in a CD nozzle) — **GRADING ACT ONLY, ZERO SOLVER COMPUTE.** One invocation of a repaired reader, frozen **before it was ever run**, over artifacts already on disk from VMFL046-R8. Prereg FROZEN at **`faf4ccfd`** (blob `2634c72e…`), grading path pinned to blob **`660464f94a2afcbf73c5787e992887233b2b3419`**, `git hash-object` == pinned == committed, **re-verified in the same shell invocation that launched the grade** (rule 2). **Rung verdict `NOT A RESULT`** (`grade_rc = 1` with a printed limb verdict and no traceback, so **not** the pre-registered INSTRUMENT FAULT class): all three levels failed the pre-registered plateau at `endTime` 0.080 s (worst **975.5×** `DELTA_X` = 6.250e-04 m) — rule 5 step 1 — **and independently** the r = 2 triple on `x_shock` is **`OSCILLATORY`** (R = −2.364) — rule 5 step 2. `x_shock` = 1.260864 / 1.181162 / 1.369601 m against reference 1.250 m (+0.87 % / −5.51 % / +9.57 %). **No GCI and no observed order are quoted: the triple is non-monotone and rule 5 forbids it; `Fs` = 1.25 is registered and NOT applied.** All five planted controls (A/B/C1/C2/D) fired and passed at all three levels, and the comparator's refusal path is exercised green in its own selftest (90 ok / 0 FAILED, rc 0, 9 plant-refusal arms reporting `REFUSES(2)`). |
| **predicted** | **~3.0 core-min** point estimate; **cap 30 core-min** (10× the estimate, set deliberately wide because the box carried load average 29.6 at drafting). Registered at `PREREGISTRATION.md` §6. Derived $ **$0.0026** at $0.0513/core-h — **DERIVED, NOT MEASURED**. |
| **actual gross** | **0.4000 core-min MEASURED** (24 s wall × 1 rank ÷ 60; 2026-09-10T15:46:15Z → 15:46:39Z, from the wrapper's own clock) = 0.006667 core-h = **$0.000342 DERIVED, NOT MEASURED** at $0.0513/core-h, owner-stated / reported-by-owner (`COMPUTE_BUDGET_CHARTER` §5 — the box cannot read its own billing). **Solver compute: 0.0000 core-min. $0.00.** No mesh, no solver, no field written; the run root was verified unmodified (`find -newermt`, zero files) since the 04:25:31Z R8 grade, immediately before the invocation. |
| **actual cleaned** | **= gross (0.4000).** No stall: 24 s wall is three orders below the charter §2 3600-s figure. **No overrun** — 1.3 % of the 30 core-min cap consumed. |
| **ratio (cleaned/predicted)** | **0.133×** — an **over-estimate**, in the conservative direction. |
| **gap attribution** | **MISPREDICTION ONLY, IN THE CONSERVATIVE DIRECTION. NO WASTE (0.0000 core-min) AND NO CONTENTION COMPONENT.** Two named mechanisms, and the first is a caveat on the actual rather than on the estimate. **(a) WARM PAGE CACHE — the actual is NOT a cold-read basis and must not be carried forward as one.** The estimate was built on the *frozen* comparator's measured 10 s L1-only pass and extrapolated to 912.1 MB of solver logs across three levels (L1 127.7 + L2 265.3 + L3 519.1 MB, each read in one `open().read()` with three `re.findall` passes). The measured 24 s for all three implies the log parse was **not** the bottleneck assumed — the same logs had been read minutes earlier by this lane's independent rule-4 completion check, and before that by the R8 grading pass. **A cold read is NOT MEASURED and is left as a stated absence, not approximated.** **(b) THE CAP PAID FOR CONTENTION THAT DID NOT MATERIALISE:** the 10× cap was sized against load average **29.6** at drafting; at launch the box carried **13.14** on 16 vCPU. **CALIBRATION LESSON: a single-core reader's cost over a fixed artifact set is dominated by cache state, not by artifact size, so a grading-act estimate should register BOTH a warm and a cold figure — this row supplies the warm one (0.4000 core-min for 912.1 MB across three levels) and names the cold one as unmeasured.** **A SECOND, NON-COST FINDING RECORDED HERE BECAUSE IT BOUNDS WHAT RULE 2 RESTED ON:** `scripts/check_comparator_freeze.py` — rule 2's named enforcing instrument — was run and returned **rc 3, `VERDICT: FAIL`**, over 692 output lines and a population of **267 graders, 13 violating**. **This grading path IS in that population**, at output line 470, classified **`NO-MARKERS`** — *"no completion marker in this tree, out of evidence reach, reported so the gap is countable."* **78 output lines name the ansys tree and all 78 ansys graders are `NO-MARKERS`; NONE is in a violating class.** So the instrument neither faults nor blesses this path, and rule 2 was discharged by the direct `git hash-object` comparison, which is the check the charter specifies. **THE DOCKET-WORTHY GAP IS THAT NO ANSYS GRADER CAN EVER REACH `FROZEN` UNTIL THAT TREE CARRIES THE COMPLETION MARKERS THE SCRIPT READS — the fix is a marker convention in `cases/ansys_verification/`, not a change to the script.** **CORRECTION AGAINST THIS LANE, RECORDED NOT TIDIED:** an earlier reading here claimed "zero coverage" and that `ansys` appeared zero times — **FALSE and WITHDRAWN.** The first capture was `… \| tail -20`, so the grep saw **20 of 692 lines**, and the recorded exit status was **`tail`'s 0, not the script's rc 3**. **A zero asserted from a reader never shown able to see a non-zero is rule 3's own failure mode, applied to a grep.** Caught by the supervisor; re-measured here from a full capture with rc taken inside the invocation. |
| **record ref** | `cases/ansys_verification/VMFL046/RESULTS_VMFL046_R9_REGRADE.md` §4.1; verdict artifacts `verification/runs/ansys_verification/VMFL046-R8/GRADING_VMFL046_R9.log` (108 lines) and `.../GRADING_VMFL046_R9.json`; frozen prereg `cases/ansys_verification/VMFL046-R9-REGRADE/PREREGISTRATION.md` §6 at freeze commit `faf4ccfd`; grading path `cases/ansys_verification/VMFL046-R9-REGRADE/grade_vmfl046_r8_repaired.py` blob `660464f94a2afcbf73c5787e992887233b2b3419` (repair provenance, a DIFFERENT object: pre-fold blob `c7c00bbf1fa627ef4cbdda9c81724b0cd62008e5` at `e6ad3459`) |

---

## ROW 2 — the underlying VMFL046-R8 solver run, now that its rung is graded

| field | value |
|---|---|
| **id** | `C-20260910T155654.923375Z-ea6de6bd` |
| **date** | 2026-09-10 |
| **team** | ansys-verification |
| **process/rung** | **VMFL046-R8 SOLVER RUN** (`rhoPimpleFoam`, pressure-based transient PIMPLE, vanLeer TVD / Euler, `maxCo` 0.5, r = 2 triple L1 3 200 / L2 12 800 / L3 51 200 cells, **all three serial, 1 rank** — no `decomposeParDict`, no `processor*` dirs, verified on disk). Prereg frozen at **`a7a0e72b`**. **THIS ROW DOES NOT RE-CHARGE THE SPEND — it is recorded at register Row #71 and this row exists because rule 12 requires the estimate-versus-actual comparison AT PROCESS COMPLETION, and the rung only became GRADED on 2026-09-10T15:46:39Z, via the R9 re-grade above. ONE SOLVE, TWO READINGS, ONE SPEND.** Row #71 (`NOT A RESULT`, `grade_rc = 2`, frozen comparator blob `f89114bb…` REFUSED at L1) stands unamended; the R9 reading is a second register row, not a replacement. |
| **predicted** | **~398 core-min** total (per level: L1 ~7.7, L2 ~45, L3 ~345); **caps 24 / 140 / 1 040, running total 1 200 core-min**, at `cases/ansys_verification/VMFL046-R8/PREREGISTRATION.md:155-160`. Basis: L1 from a same-configuration answer-blind start-from-rest smoke (`SMOKE_rhoPimple_L1`, 7.68 core-min measured); **L2/L3 from a flat ~2.0× "start-from-rest factor" applied to R5's completed impulsive-start totals (22.6 and 171.583 core-min).** Derived $ **$0.3400** at $0.0513/core-h — **DERIVED, NOT MEASURED**. Under the $25 pre-authorisation. |
| **actual gross** | **590.7413 core-min MEASURED on the `ExecutionTime` (CPU-time) basis** — L1 439.77 s = 7.3295, L2 3 320.36 s = 55.3393, L3 31 684.35 s = 528.0725, each × 1 rank ÷ 60, read from the last `ExecutionTime` line of each `log.rhoPimpleFoam`. This is the basis the 590.7413 figure at Row #71 uses, and the two agree to the digit. **ClockTime (wall) gross is 593.1833 core-min** (444 / 3 348 / 31 799 s); the two bases are stated because they differ, and the difference — **2.4420 core-min, 0.412 %** — is I/O and OS overhead. `ExecutionTime` basis = 9.8457 core-h = **$0.5051 DERIVED, NOT MEASURED** at $0.0513/core-h, owner-stated / reported-by-owner (`COMPUTE_BUDGET_CHARTER` §5). **WITHIN every cap** (7.33/24, 55.34/140, 528.07/1 040, 590.74/1 200) — **no overrun, so no cap-stop.** |
| **actual cleaned** | **= gross (590.7413 on the `ExecutionTime` basis).** **NO STALL, and the test is measured not asserted:** only L3 exceeds the charter §2 3600-s wall figure (31 799 s), and it is a legitimately long single-core transient, not a stall — `ExecutionTime/ClockTime` = **0.99047 / 0.99174 / 0.99640**, i.e. the process held 99 %+ of one core throughout, and `Time` advanced **strictly monotonically** through all 125 473 steps to `endTime` 0.08 (independently verified by the lane's own reader, not relayed). A stalled or descheduled process does not hold CPU time at wall time. **WASTE NAMED SEPARATELY AND NOT ABSORBED INTO ANY RATIO (`COMPUTE_BUDGET_CHARTER` §6): the full 590.7413 core-min, as Row #71 recorded it — it yielded no gradeable answer.** |
| **ratio (cleaned/predicted)** | **1.484×** (590.7413 / ~398). Per level: **L1 0.952×**, **L2 1.230×**, **L3 1.531×** — **the miss grows monotonically with refinement, which is the signature that matters.** Every level is inside its own cap, so this is a cost-model miss caught by margin, not a budget failure. |
| **gap attribution** | **MISPREDICTION, LOCATED IN THE STEP COUNT AND NOT IN THE PER-CELL RATE. CONTENTION IS MEASURABLY RULED OUT, NOT ASSUMED AWAY.** **(1) CONTENTION ≈ 0, MEASURED:** `ExecutionTime/ClockTime` = 0.99047 / 0.99174 / 0.99640 bounds the descheduling channel at **0.412 %** of total spend — it cannot account for a 48 % miss, and contention is therefore **not named as a contributor**. (A shared-memory-bandwidth channel is not separable from these artifacts and no counter was read; it is carried inside the measured per-cell-step rate below rather than quoted as a number the box cannot measure.) **(2) THE MISPREDICTION, LOCATED:** the flat **~2.0× start-from-rest factor** applied to R5's impulsive-start totals is **not mesh-independent** — measured **2.449× at L2** and **3.078× at L3**. L1, estimated from a same-configuration smoke rather than from a factor, came in at **0.954×** of its basis: **the smoke basis was accurate and the factor was not.** **(3) THE MECHANISM, MEASURED, AND IT IS NOT THE SOLVER RATE:** per-cell-step cost is nearly **FLAT** across the triple at **4.4497e-06 / 4.0465e-06 / 4.9320e-06 s per cell-step** (spread 1.22×, non-monotone — a cache-residency wobble, not a modelling error). The entire miss is in the **cell-step count**: cells grow **exactly 4×** per level (2-D r = 2), steps grow **2.076×** then **1.957×** (≈2×, since `Δt ∝ Δx` under fixed `maxCo` with `adjustTimeStep yes`; 30 885 / 64 105 / 125 473), so **cell-steps grow ≈8× per level (measured 8.30× then 7.83×**; 9.883e7 / 8.205e8 / 6.424e9). **The estimate's implied per-level growth was only 5.84× and 7.67×, against measured cost growth of 7.55× and 9.54× — an under-prediction of 1.29× then 1.24×, and it COMPOUNDS into the 1.531× miss at L3.** **(4) THE TRANSFERABLE LESSON, AND IT IS RETRODICTIVELY CHECKED: price an adaptive-`Δt` transient by CELLS × STEPS at a measured per-cell-step rate, deriving the step count from `Δt ∝ Δx` under the registered `maxCo` — NEVER by applying a flat "start-from-rest factor" to a predecessor's TOTAL, which silently assumes mesh-independence of a quantity that is not mesh-independent.** A flat 4.5e-06 s/cell-step model over the measured cell-step counts retrodicts **7.41 / 61.5 / 481.8 = 550.8 core-min = 0.93× of actual — within 7 %**, against the registered model's 1.484×. **A method available before the run would have predicted it to 7 %.** **(5) THE WASTE IS RECLASSIFIED IN ITS CAUSE, NOT REDUCED IN ITS AMOUNT — and this is the finding worth carrying:** the 590.7413 core-min was named waste at Row #71 because an **instrument** refused (three `^`-anchored regexes could not read a legal brace-inline `constant/fvOptions`). **The R9 re-grade shows the run would have graded `NOT A RESULT` on PHYSICS anyway** — on the plateau limb at L1, before the triple was ever assembled, because the shock has not settled at `endTime` 0.080 s at any level (plateau missed by 68.8×–975.5× of `DELTA_X`). **So the instrument defect cost the lab a READING, not an ANSWER.** That neither excuses the defect (a frozen comparator that cannot parse its own frozen case is the `§38.1` class and the repair was owed) nor reduces the waste figure by one core-minute — the spend still bought no gradeable answer. **It does relocate the successor's problem from the regex to `endTime`, which is worth more than the re-grade cost (0.4000 core-min) to have learned.** **(6) THE SUCCESSOR'S BUDGET IS NOW ON A MEASURED BASIS, stated so it is not re-derived by spending:** at the same triple, a longer clock scales the step count linearly at fixed mesh, so `endTime` 0.24 s would cost **≈3× of 590.74 ≈ 1 772 core-min** — a figure for the supervisor to weigh **before** any successor registration, not an assumption to bury inside one. |
| **record ref** | `cases/ansys_verification/VMFL046/RESULTS_VMFL046_R9_REGRADE.md` §4.2; register Row #71 in `verification/credentials/ansys/ANSYS_VALIDATION_REGISTER.md` (the spend's home record, unamended); frozen prereg `cases/ansys_verification/VMFL046-R8/PREREGISTRATION.md:155-160` at `a7a0e72b`; per-level clocks and step counts from `verification/runs/ansys_verification/VMFL046-R8/{L1,L2,L3}/log.rhoPimpleFoam` (last `ExecutionTime`/`ClockTime` line, `Time =` line count) and `.../RUN_RC`; cell counts from `.../{L1,L2,L3}/log.blockMesh` (`nCells: 3200 / 12800 / 51200`); clocks from `.../{L1,L2,L3}/system/controlDict`; the grading act that completed the rung is row `C-20260910T155654.923375Z-1abc39c1` of this ledger |

---

## NOTE FOR THE COMMITTER

1. **Re-derive the row ids** in the same shell invocation as the commit if either collides
   with a peer's append (peers commit constantly).
2. **These are the only two calibration rows owed by this act.** Nothing else in this
   territory completed a process.
3. **Do not merge them into one row.** The ledger's ratio column is the lab's
   estimate-quality record, and a grading act at 0.133× and a solver run at 1.484× are two
   different estimate-quality facts. Merging them would produce a single ratio describing
   neither.
4. **Neither row's dollar figure is measured.** Every `$` above is derived at the recorded
   $0.0513/core-h and is labelled so, per append rule 3.
