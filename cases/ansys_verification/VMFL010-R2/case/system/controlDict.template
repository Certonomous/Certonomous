/*--------------------------------*- C++ -*----------------------------------*/
FoamFile { version 2.0; format ascii; class dictionary; object controlDict; }
application     simpleFoam;
startFrom       startTime;   startTime 0;
stopAt          endTime;     endTime   __ENDTIME__;   // SIMPLE iterations; residualControl 1e-9 stops earlier
deltaT          1;
writeControl    runTime;     writeInterval __ENDTIME__;
writePrecision  10;  writeCompression off;  timeFormat general;  runTimeModifiable false;
// Flow split read post-hoc by the comparator:
//   split = |phi(mainOutlet)| / |phi(inlet)|  -> flow split (target 0.887, manual p.39)
functionObjects
{
    // per-iteration solver residuals -> iterative-convergence evidence on disk (rule 5 step 1)
    resid { type solverInfo; libs (utilityFunctionObjects); fields (U p); }
}
// ************************************************************************* //
