VMFLGPU007-R2 -- CONSTANT-BY-CONSTANT DIFF AGAINST R1's FROZEN COMPARATOR
===============================================================================
written 2026-08-27T22:11:28Z by ansys-lane-opus (lane V), pre-freeze.

Generated by walking BOTH files' ASTs and comparing every module-level
UPPER-CASE assignment by literal value.  It is a MEASUREMENT of the two files,
not a hand-written table, so it cannot drift from the code it describes.

  R1  cases/ansys_verification/VMFLGPU007/grade_vmflgpu007.py
      blob:a4632b7ac2ef88fd98d14015b614ab4772ae71df
  R2  cases/ansys_verification/VMFLGPU007-R2/grade_vmflgpu007_r2.py  (UNCOMMITTED)
      sha256:920097dd877c48d78d7c546ad5a234370cf1f4639ffa200e76acc4e4f978423c

SUMMARY:  31 identical   1 moved   1 retired   14 new

-------------------------------------------------------------------------------
IDENTICAL -- CARRIED BYTE-IDENTICAL FROM R1 (31)
-------------------------------------------------------------------------------
  ARMS                       = ('gpu', 'cpu')
  BAND_B                     = 0.0001
  BAND_C1                    = 0.2
  BAND_C2                    = 1.5
  FIELDS_AT_ENDTIME          = ('T', 'U', 'p_rgh', 'alphat', 'nut', 'k', 'epsilon')
  FORBIDDEN                  = ('\\bGCI\\b', '\\bgrid convergence index\\b', '\\bobserved order\\b', '\\bp_obs\\b', '\\bapparent order\\b', '\\bRichardson\\b')
  GPU_PCTF_MIN               = 99.0
  H1_WALL                    = 0.07
  H_STEP                     = 1.0
  KAPPA                      = 1.408
  LEVELS                     = (('L1', 3648), ('L2', 7776), ('L3', 16128))
  LIMB_B_PASS_CAPABLE        = True
  PLANT                      = 0.001234
  PLANT_MIN_FRACTION         = 0.1
  PLATEAU_ALIVE_MIN          = 0.01
  PLATEAU_MIN_SAMPLES        = 200
  PLATEAU_PTP_TOL            = 0.0001
  PLATEAU_WINDOW             = 200
  Q_FLUX                     = 1000.0
  REF_PEAK_NU                = 64.853
  REF_PEAK_XH                = 5.8209
  RE_H_IF_BULK_WERE_USED     = 25709.0
  RE_H_REGISTERED            = 28000.0
  TIER_CEILING_C             = 'GATE REACHED'
  T_INLET                    = 300.0
  U_MAX                      = 2.8
  VOCAB                      = ('PASS', 'GATE REACHED', 'GATE FAIL', 'NOT A RESULT', 'BLOCKED', 'PENDING')
  YPLUS_GATE_PATCH           = 'heatedWall'
  YPLUS_MAX_ATTACHED         = 300.0
  YPLUS_MIN_ATTACHED         = 11.0
  _EMITTED                   = []

-------------------------------------------------------------------------------
MOVED (1)
-------------------------------------------------------------------------------
  VERSION                    R1 = 'VMFLGPU007-comparator-1.0'
                             R2 = 'VMFLGPU007-R2-comparator-1.0'
                             WHY: a LABEL, not a gate. R2 must not be mistaken for R1 in a record.

-------------------------------------------------------------------------------
RETIRED (1)
-------------------------------------------------------------------------------
  ARMS_TABLE                 R1 = []

-------------------------------------------------------------------------------
NEW (14)
-------------------------------------------------------------------------------
  COL_H2D                    = ('CpuToGpu', 'Count')
  COL_PCTF                   = ('GPU', '%F')
  GPU_PCTF_MAX               = 100.0
  LEVEL_ENDTIME              = {'L1': 3200, 'L2': 4400, 'L3': 6200}
  LIMBA_EVENTS               = ('MatMult', 'KSPSolve')
  LOGVIEW_LEGEND_TAIL        = (('Total Mflop/s', 'Mflop/s'), ('GPU Mflop/s', 'Mflop/s'), ('CpuToGpu Count', 'Count'), ('CpuToGpu Size', 'Size'), ('GpuToCpu Count', 'Count'), ('GpuToCpu Size', 'Size'), ('GPU %F', '%F'))
  LOGVIEW_PUNCT_RE           = '<non-literal>'
  LOGVIEW_STAGE_RE           = '<non-literal>'
  LOGVIEW_TOP_RE             = '<non-literal>'
  PLATEAU_MARGIN_TAU         = 3
  _LOGVIEW_HEADER_SUB        = '                   Max Ratio  Max     Ratio   Max  Ratio  Mess   AvgLen  Reduct  %T %F %M %L %R  %T %F %M %L %R Mflop/s Mflop/s Count   Size   Count   Size  %F'
  _LOGVIEW_HEADER_TOP        = 'Event                Count      Time (sec)     Flop                              --- Global ---  --- Stage ----  Total   GPU    - CpuToGpu -   - GpuToCpu - GPU'
  _LOGVIEW_LEGEND            = '   Total Mflop/s: 1e-6 * (sum of flop over all processes)/(max time over all processes)\n   GPU Mflop/s: 1e-6 * (sum of flop on GPU over all processes)/(max GPU time over all processes)\n   CpuToGpu Count: total number of CPU to GPU copies per processor\n   CpuToGpu Size (Mbytes): 1e-6 * (total size of CPU to GPU copies per processor)\n   GpuToCpu Count: total number of GPU to CPU copies per processor\n   GpuToCpu Size (Mbytes): 1e-6 * (total size of GPU to CPU copies per processor)\n   GPU %F: percent flops on GPU in this event\n'
  _NUM                       = '[-+]?(?:\\d+\\.?\\d*|\\.\\d+)(?:[eE][-+]?\\d+)?'

-------------------------------------------------------------------------------
WHY EACH NON-IDENTICAL ENTRY MOVED, one line each
-------------------------------------------------------------------------------
MOVED
  VERSION              A LABEL, NOT A GATE.  R2 must not be mistaken for R1 in a
                       record.  It changes no number and gates nothing.

RETIRED
  ARMS_TABLE = []      DEAD CODE IN R1: declared at module level and never read.
                       Measured: 1 occurrence in R1 (the declaration itself) and
                       0 reads.  Removing an unread empty list cannot move a
                       number; it is named here because a silent removal and an
                       overlooked one look the same in a diff.

NEW -- and the whole point is that NOT ONE OF THEM LOOSENS ANYTHING
  LEVEL_ENDTIME        THE ONE SUBSTANTIVE REGISTERED CHANGE.  L1 1200 -> 3200,
                       L2 1800 -> 4400, L3 3000 -> 6200, derived in
                       PLATEAU_EXTRAPOLATION_RECORD.txt from R1's own MEASURED
                       wallTmin decay rate.  endTime is a COST parameter.  It
                       also arms a NEW REFUSAL, clause E2: a run whose RUN_RC
                       declares a different endTime is refused.  R1 had no such
                       constant and took whatever RUN_RC said, so a run at ANY
                       endTime would have been graded -- a hole a registration
                       whose thesis is "the run was too short" cannot leave open.
  PLATEAU_MARGIN_TAU   = 3.  RECORDED FOR THE READER, not used at grade time; it
                       is how many time constants of headroom the endTimes carry.
                       Grades nothing and refuses nothing.
  GPU_PCTF_MAX = 100.0 A NEW REFUSAL.  GPU %F is a PERCENTAGE; above 100 the
                       reader has read the wrong column.  R1 cleared its 99.0
                       floor with 210.0 -- the CpuToGpu SIZE IN MBYTES.  Nothing
                       that refused in R1 passes here; this only ADDS a refusal.
  LIMBA_EVENTS         ('MatMult','KSPSolve') -- R1 selected exactly these rows
                       with an inline regex.  Made an explicit constant so the
                       selection is visible beside the floor it feeds.
  COL_PCTF, COL_H2D    The two columns limb A reads, NAMED as (top-line group,
                       sub-line token).  R1 named neither: it counted tokens from
                       the END OF A ROW, which is how it got a megabyte count.
  LOGVIEW_TOP_RE       The table's header shape.  A SHAPE, not a threshold.
  LOGVIEW_PUNCT_RE     Recognises the header's pure-punctuation rules ('-','---')
                       so they cannot own a data column.  A SHAPE.
  LOGVIEW_STAGE_RE     Recognises '--- Event Stage n: name'.  A SHAPE.
  _NUM                 The numeric-token pattern for a data row.  A SHAPE.
  LOGVIEW_LEGEND_TAIL  PETSc's own legend names the seven trailing columns in
                       order; this is that order, used as the SECOND, INDEPENDENT
                       column derivation.  It is used ONLY TO NAME COLUMNS and
                       NEVER TO READ A VALUE -- R1's h2d defect was a
                       value-reading regex whose only match in the whole file was
                       this legend.
  _LOGVIEW_LEGEND      THE FIXTURE, and it is THE REAL SHAPE.  These three are
  _LOGVIEW_HEADER_TOP  the VERBATIM BYTES of the table PETSc wrote in R1's own
  _LOGVIEW_HEADER_SUB  gpu/L1/log.buoyantSimpleFoam, verified byte-identical
                       against that file.  R1's fixture wrote a five-token
                       pseudo-table OpenFOAM+PETSc does not produce, which is
                       exactly why its selftest could not discover the defect.
                       Selftest arm `a_fixture_is_the_real_table_shape` parses
                       the fixture through the grade's own reader and requires
                       26 columns, GPU %F 100, CpuToGpu Count 7200/3601.

-------------------------------------------------------------------------------
THE ONE SENTENCE A READER SHOULD TAKE AWAY
-------------------------------------------------------------------------------
EVERY BAND, EVERY REFERENCE, EVERY FLOOR, EVERY CEILING, THE PLANT, THE PLATEAU
CLAUSE IN ALL FOUR OF ITS CONSTANTS, THE y+ BAND, THE MESH FAMILY AND THE CAPS
ARE BYTE-IDENTICAL TO R1.  The endTimes moved, one label moved, one piece of
dead code went, and everything else that is new is either a table SHAPE or a
NEW REFUSAL.  Nothing was loosened in the direction of the answer that refused.

  Asserted in code, not only here: selftest arms `plateau_clause_identical_to_r1`
  and `gate_constants_identical_to_r1` re-check these values at every run, so the
  claim cannot rot into prose.
