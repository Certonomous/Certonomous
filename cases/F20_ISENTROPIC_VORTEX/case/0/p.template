FoamFile { version 2.0; format ascii; class volScalarField; object p; }
// F20 isentropic vortex -- internalField = exact p = rho T at t = 0,
// at the built mesh's own cell centres (from 0/C), written by build_f20.py.
dimensions      [1 -1 -2 0 0 0 0];
internalField   nonuniform List<scalar> __INTERNALFIELD__;
boundaryField
{
    left   { type cyclic; }
    right  { type cyclic; }
    bottom { type cyclic; }
    top    { type cyclic; }
    frontAndBack { type empty; }
}
