FoamFile
{
    version 2.0;
    format ascii;
    class volScalarField;
    object p;
}
dimensions      [1 -1 -2 0 0 0 0];
internalField   uniform 101325;
boundaryField
{
    inlet       { type fixedValue;    value uniform 101325; }
    freestream  { type fixedValue;    value uniform 101325; }
    outlet      { type zeroGradient; }
    cone        { type zeroGradient; }
    axis        { type empty; }
    front       { type wedge; }
    back        { type wedge; }
}
