FoamFile { version 2.0; format ascii; class dictionary; object fvSolution; }
// Inviscid: rhoCentralFoam performs no implicit solves. The entries exist so the
// dictionary is complete if mu is ever made non-zero, which THIS registration
// forbids after freeze.
solvers { "(rho|rhoU|rhoE)" { solver diagonal; } }
