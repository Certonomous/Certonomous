# CURRICULUM D17 — `Cone_Supersonic`: a 2D WEDGE at M 1.96 (`DAHisaFoam`, inviscid): the BASELINE adjoint gradient of CD w.r.t. the tutorial's `shape` functions, FD-VERIFIED on TWO TOOLCHAIN ROWS — PRE-REGISTRATION

**Version 1.0. FROZEN.** Dated **2026-08-26**. Lane: dafoam `lab-lane` (N2). Supervisor: `dafoam-supervisor` (FIFTEENTH session, UPDATE S-2 dispatch: *"lane N2: D17 on the third reachable empty cell (`Cone_Supersonic`; the lane establishes whether it is 2D, axisymmetric or 3D before naming the cell), cpuset 12,15, same family, filed on the drop path"*). **This document is `curriculum_D16/PREREGISTRATION.md` with the registered deltas of §7 and nothing else inherited silently: where a number here disagrees with D16's text for a clause this document does not list as a delta, D16's text wins and this document is defective.** Permission: **`bc0e687e`**; queue-first `7def3c6b` / `73eccb1b` / `0b041d1a`; L-342 `d4d0c29d`; grid `068c2bf0`; `3c3ef86c`. `[lab-attributed]`. Nothing here is filed, sent or posted (rule 7; `DAFOAM_CHARTER.md` §10).

> **ID-NAMESPACE WARNING.** `cases/dafoam/EXPERTISE_CURRICULUM.md` carries no `D17` row (`grep -n 'D17'` → 0 hits at 2026-08-26T22:2xZ) and `docs/DOCKET.md` carries fleet-defect rows D5/D6/D14/D15 (different objects). **This item is the dafoam CURRICULUM CASE `D17`**, confirmed free by `grep -rln curriculum_D17 cases docs verification` (none) and assigned by the supervisor's dispatch. No document in this family cites a bare `D17` without saying which.

> **FILING.** This case lives at **`cases/dafoam/curriculum_D17_cone_supersonic/`**, not under `ladder-a/A1/`: A1 is the NACA0012 ladder (D1, D1-C′, D2, D13, D15, D16 all run A1's 4,032-cell airfoil mesh) and the wedge is not on any rung of A1–A6; creating a rung `A7` would change the charter's ladder (`CLAUDE.md` roster: *"A1–A6, B1–B3, S1, W4, W5"*), which is not a lane's call. The precedent for an off-ladder curriculum case at the top of `cases/dafoam/` is `cases/dafoam/curriculum_D12R2/`. `scripts/check_filing.py` reports the same 28 pre-existing violations (none under `cases/dafoam/`) before and after this directory was created — measured, §9.

---

## 0. SCOPE, THE CASE AS READ FROM ITS INPUTS, AND THE CAPABILITY-GRID CELL THIS ITEM CAN MOVE

**Cell (`068c2bf0`; `docs/capability/dafoam_GRID.md` @ HEAD, Correction 1 landed): `2D · steady · supersonic`, column "gradients computed + FD-verified" — today `CAN NOT DO — not attempted (the image ships `Cone_Supersonic`; never staged)`.** The grid's parenthetical is inexact (UPDATE N fact 1): **neither image ships the tutorials**; the checkout is on the host at `/home/ubuntu/dafoam-tutorials` (git `d3b7e38b`), and `Cone_Supersonic/` there holds `0.orig FFD constant system preProcessing.sh runScript.py Allclean.sh paraview.foam` (21 files). **Only the gradient column moves**; no optimiser runs.

**Dimension — established from `constant/polyMesh` (absent: the mesh is generated), `system/blockMeshDict`, `system/mirrorMeshDict` and `runScript.py`, before naming the cell:**

| reading | source | consequence |
|---|---|---|
| two hex blocks `(100 100 1)` each, z from 0 to 0.1, **one cell thick**; patch `syms` of type `symmetry` on both z-faces | `system/blockMeshDict` (md5 `4bf634ce…`) | planar, one cell in z |
| `mirrorMesh` across the plane `point (0 0 0) normal (0 1 0)` (y = 0); `renumberMesh -overwrite`; `cp -r 0.orig 0` | `preProcessing.sh` (md5 `124bd4bf…`) | the half-domain is mirrored to the full wedge; the y = 0 `syms` face of block 1 becomes internal |
| `meshOptions.symmetryPlanes` = z = 0, z = 0.1 and x = 2 planes; **no `wedge` patch type anywhere** | `runScript.py:76-83` | IDWarp treats it as a 2D slab; **not axisymmetric** |
| surface `cone` = the face `(1 8 11 5)` from (1, 0) to (2, 0.2): a straight ramp of half-angle **atan(0.2) = 11.31°**, base at the outlet x = 2 | `blockMeshDict` vertices 1, 8 | a **2D wedge (diamond half)**; the tutorial's name "cone" is a misnomer for this mesh and is not adopted by the cell |

**Therefore the cell is `2D · steady · supersonic`.** Not `axisym` (that would need a `wedge` patch pair or an axisymmetric solver setting; there is none) and not `3D` (one cell thick, symmetry planes on both z-faces).

**Solver and conditions** (`runScript.py:28-32,36`, `0.orig/include/freestreamConditions`): `"solverName": "DAHisaFoam"` — DAFoam's wrapper of the **HiSA** density-based compressible solver (`system/controlDict` `application hisa`; `fvSchemes` `fluxScheme JST`, `ddtSchemes bounded dualTime rPseudoDeltaT steadyState`; `fvSolution` `flowSolver GMRES` with `LUSGS` preconditioning, `pseudoTime … localTimestepping true`, `endTime 500` pseudo-steps); `U0 = 680.0` m/s, `p0 = 101325`, `T0 = 300.0` → `a = sqrt(1.4 · 287 · 300) = 347.2 m/s` → **M∞ = 1.958**; **aoa 0** (the freestream is `(680 0 0)` and there is no angle-of-attack variable); **inviscid**: `constant/thermophysicalProperties` `mu 0`, `turbulenceProperties` `RASModel dummy` — an Euler wedge, so CD is wave drag only; `adjEqnOption {hisaUsePCFlux 1, gmresRelTol 1e-5, pcFillLevel 1, jacMatReOrdering natural}`, `adjStateOrdering cell`, `normalizeStates {U, p, T}`, `checkMeshThreshold {maxNonOrth 70, maxSkewness 6, maxAspectRatio 5000}`.

**Cell count, DERIVED (never measured before this item): 2 blocks × 100 × 100 × 1 = 20,000 cells, ×2 by `mirrorMesh` = 40,000.** `mirrorMesh` duplicates every cell across the plane and merges the coincident points (`planeTolerance 1e-6`); the only y = 0 faces in the half-mesh are block 1's `syms` face (becomes internal) and block 2's `cone` wall (its mirror image is the lower wedge surface). G-M2 is registered at 40,000 with this derivation; a mismatch is `GATE FAIL` by the family rule ("a different mesh is a different item") **and would be a defect of this derivation, disclosed as such** — not of the toolchain.

**Design variables, objective, constraints** (`runScript.py:118-147`): `nom_addShapeFunctionDV(dvName="shape", …)` over the FFD `FFD/FFD.xyz` (**1 block, 7 × 3 × 2 points**, x from 0.99 to 2.01, y ±0.05 at the nose to ±0.25 at the base, `FFD/genFFD.py`); one shape function per station `i = 1 … 6` (`for i in range(1, pts.shape[0])` — the nose station i = 0 is NOT a DV), each moving the `j = 0` and `j = 2` rows in opposite `y` (a thickness function), so **`shape` has 6 components**, bounds ±0.1, `scaler 10`; **no `patchV`** (D15/D16 had one). Objective `cruise.aero_post.CD` (the scenario is named `cruise`, D16's was `scenario1`); constraints `thickcon` (2 × 10 stations, [0.2, 3.0]) and `volcon` (≥ 1.0) — **registered here for the record and NOT exercised** (no optimiser). `CL` is defined as a second `function` (direction `[0,1,0]`) and **is zero by construction**: the wedge is y-symmetric at 0° and every shape function is y-symmetric, so `CL` and `dCL/dshape` are a **symmetry reading, reported and never graded** (§3). The tutorial's own `check_totals` task uses `step=1e-2, form="central", step_calc="abs"` — the top of this item's registered step set.

**What `preProcessing.sh` does, and where:** `blockMesh` → `mirrorMesh -overwrite` → `renumberMesh -overwrite` → `cp -r 0.orig 0`, inside the SHIPPED image (the MESH arm, exactly as D15/D16's MESH arm regenerates A1's mesh). The tutorial's `system/decomposeParDict` says `numberOfSubdomains 8`; the registered overlay `d17_decomposeParDict` (`numberOfSubdomains 2`, `scotch`; byte-identical to D16's) replaces it at staging and the tutorial's own file is md5-asserted before being replaced.

**Both images can run it — established by two `ls`-class probes, billed in §4:** in `dafoam/opt-packages:latest` (`sha256:9d45679d…f07fc`) and `dafoam-idwarp-rot:v1` (`sha256:2927768a…6d35`) alike, `hisa` is at `/home/dafoamuser/dafoam/OpenFOAM/sharedBins/hisa`, `libHisaSolver.so` / `libHisaSolverADF.so` / `libHisaSolverADR.so` are in `sharedLibs/`, the DAFoam source tree carries `src/adjoint/DASolver/DAHisaFoam/DAHisaFoam.{H,C}`, `blockMesh`/`mirrorMesh`/`renumberMesh` are on the path, and `pygeo.mphys.OM_DVGEOCOMP` exposes `nom_addShapeFunctionDV`. **Two facts for the record:** the images carry **OpenFOAM-v2506** (`platforms/linux64GccDPInt32Opt`), not the v1812 the tutorial's file headers name (cosmetic); and `dafoam` has no `__version__` attribute (the digest is the identity, `DAFOAM_CHARTER.md` §11).

## 1. THE AMENDMENT CONDITION, AND HOW IT WAS CHECKED

> **The run root `/home/ubuntu/certonomous-runs/CURRICULUM-D17-cone-supersonic` DOES NOT EXIST.**

`test -e` → false at **2026-08-26T22:05:56Z** (first disk reading) and again as the last leg of `d17_groot5_selftest.sh` at **2026-08-26T22:26:27Z** (`d17_groot5_selftest_evidence.txt`, 18/18). **0 solver core-min; no arm container has started.** The only containers this lane has run are the two identity probes of §0/§4 (bash-only reads of the image filesystem; no mesh, no field, no solver) and the sacrificial `sleep` of the G-ROOT.5 selftest. After the first arm container, gates are **CLOSED**.

## 2. ARMS — as D16 §2, with these values

Chain `d17_chain_driver.sh MESH X-S F-S X-P F-P`; arms MESH (SCRIPT, SHIPPED, 1 rank), X-S / F-S (SOLVER, SHIPPED, 2 ranks), X-P / F-P (SOLVER, PATCHED, 2 ranks); ONE mesh generated once in the SHIPPED image; G-ROW from the arm suffix; **two rows or it is not a verdict**. The instrument `d17_xf.py` is `d16_xf.py` re-pointed at the wedge producer (`d17_runScript.py`, md5 `8621862522cf2af72f381f0d97660b8a`, scenario `cruise`) and reduced to the tutorial's ONE DV group: X = `compute_totals(of=[CD, CL], wrt=["shape"])` at the **baseline** (`shape = 0`); F = central FD on the **registered subset `shape[0]`, `shape[1]`, `shape[3]`, `shape[4]`, `shape[5]`** (i = 1, 2, 4, 5, 6: `shape[0]` is the first station aft of the nose — the **LE-class** function, this item's analogue of A1's idx6 / D15-D16's `shape[6]`; `shape[5]` is the base station) + the `CTRL` planted-zero control; steps `shape` **{1e-2, 1e-3, 1e-4}** (FFD y-displacement, metres; D16's `shape` set unchanged; the tutorial's own `check_totals` step 1e-2 is the top rung); **32 primals per F arm** (2 baselines for η + 5 × 3 × 2); middle-step reference; the CTRL row and its planted twin re-read from disk by the instrument and by the grader (D15 §2, verbatim). CL is written beside CD in every artefact as the symmetry reading.

## 3. GATES, THRESHOLDS AND LABELS — `d17_grade.py`

**Every gate, band, label and composition rule is D16 §3 (hence D15 §3), verbatim, with these registered changes and no other:** (i) **G-M2 `cells == 40,000`** (derived, §0); (ii) **G5 is graded on CD only; D16's G5c on CL is REPLACED by a symmetry reading** (`max |J_adj CL|`, `max |d_FD CL|`, `CL_baseline`) reported beside each row and never composed — D16's G5c would return `NEAR_ZERO → NOT A RESULT` on every component **by construction** for a symmetric wedge, which is not a finding about the gradient; (iii) G10 caps sum ≤ **785.0**; (iv) G12 `cpuset == 12,15`; (v) the registered components and step set of §2; (vi) the item string. In particular, unchanged: G1 arm-kind-aware completion with the age guard and the `.inspect.txt` kernel-record fallback; L-342 field classes; **G5 band D 5.0 % per component with the sign-flip rule, band E 5.0 % aggregate, plateau 10 %, ≥ 3 graded components** — `curriculum_D4/PREREGISTRATION.md:82`, `curriculum_D7FR/PREREGISTRATION.md:228-229`, cited not re-derived; **G6 dot-product/duality NOT MEASURED, named** (the tutorial exposes none); G9 two `.so` md5s (`f0fcb488…` / `85f59e87…`) and two digests per row, printed by the container and read in-process; G11 OOM hard (the kernel's `OOMKilled` refuses G1); G12 delivered cores ≥ 1.5 of 2 where measured; divergence shipped-vs-patched reported with its number; no GCI (no grid family; standing rule 5 has no row). **The grader's selftest is D16's with the D17 constants: 22/22 under `python3` and `python3 -O`** (`d17_grade_selftest_evidence.txt`; `ast.Assert` 0 in the grader, the instrument, the aggregate script and the new cpuset script; the counter shown to count a planted one).

**Registered exposures that would close the item early, named so a stop is read correctly:** (a) `DAHisaFoam` is a solver class this lab has never run; if its primal does not satisfy DAFoam's convergence guard (`primalMinResTol` default with `primalMinResTolDiff` default — the `DASolver::checkPrimalFailure()` mechanism that killed every A6 primal until D8 registered `primalMinResTolDiff 1e4`), X-S exits non-zero with `Primal solution failed`, the chain STOPS, the item is `NOT A RESULT` with ≤ one X cap spent, and the finding is *the tutorial as shipped does not clear its own primal guard on this box* — nothing is tuned here; (b) `checkMesh` on the mirrored 2D mesh must print `Mesh OK.` (G1 on MESH; the tutorial's `checkMeshThreshold` accepts non-orthogonality to 70° and aspect ratio to 5,000); (c) the three-step plateau at 1e-4 may be noise-limited for the weakest component (η is measured by the two-primal repeat; a component without a plateau is `NOT A RESULT` by name and the row still grades on ≥ 3).

## 4. COST — DERIVED FROM NAMED ANCHORS, NOT MEASURED

**Anchors:** `curriculum_D7FR/RESULTS.md:36` — **3.3 core-min per primal at 42,120 cells, np = 4**, the figure that absorbs 22 primals + colouring + one `compute_totals` in ~1,100 s per row (C-125); cell ratio **40,000 / 42,120 = 0.950** → **3.1 core-min per cold primal** (HiSA's implicit pseudo-time GMRES step is heavier than a SIMPLE iteration and there are 500 of them against ~1,000 — taken as a wash, stated as the assumption); **C-76** (D8, this family): warm restarts run **0.748×** a cold primal; **C-135** (D14-M): a pyHyp mesh at 0.1833 core-min, and `blockMesh` on 40,000 cells is cheaper; **C-31** (D1-C′): a cold adjoint is priced as mid-run + colouring; **D5 Addendum 3 A3.3 (`b5b428bc`)**: colouring alone reached **9.0–27.7 core-min** on a 38,304-cell 3D mesh at 4 ranks — the exposure the X caps carry (`D5-PREREG-DEF-1` / `D4S-LAUNCHER-DEF-2` class: never a cap the arm cannot finish colouring inside).

| arm | derivation | point (core-min) | cap (core-min) | in-container wall | mem |
|---|---|---|---|---|---|
| MESH | blockMesh + mirrorMesh + renumberMesh + checkMesh on 40,000 cells | **0.3** | 5.0 | 300 s (1 rank) | 4g |
| X-S, X-P | cold primal 3.1 + colouring ≈ 3 (structured 2D, 5 states) + two adjoints (CD, CL) ≈ 2 × 2.5 + start-up ≈ 1 | **12.0** each | 90.0 each (7.5×: a D5-class colouring of 27.7 + primal + two slow adjoints ≈ 52 still lands inside) | 2,700 s (2 ranks) | **8g** |
| F-S, F-P | 2 baselines × 3.1 + 30 perturbed × 3.1 × 0.748 + start-up ≈ 2 | **78.0** each | 300.0 each (3.8×: a HiSA primal three times the anchor still lands inside) | 9,000 s (2 ranks) | 4g |
| **total** | | **180.3** point, band **[60, 450]** | **ceiling 785.0 = Σ caps** | ≈ 1.6 h wall at the point (+ 5 H5 windows), ≈ 6.6 h at the caps | |

**`cost_basis`: c7a.4xlarge at $0.0513/core-h, REPORTED-BY-OWNER, NOT MEASURED** (`COMPUTE_BUDGET_CHARTER.md` §5). Dollars **DERIVED**: point **$0.1542**, ceiling **$0.6712**. Cap mode as D15 §4 (deadline inside the container; the queue entry carries `cap_core_min_registered 785.0`, so the runner's `CAP_OVERRUN.txt` fires at 785 × 60 / 2 = 23,550 s — report-only, and expected only if every arm runs to its cap). **Already spent and billed here, not hidden (the D8 §0 form): two image-identity probes, bash-only, no solver, no mesh — 180 s + 3 s of wall at `--cpus=1` = 3.05 core-min = $0.0026 DERIVED; one probe was cut by the harness timeout while reading the second image, so the wall is a ceiling on the reading and the cost is billed at that ceiling.** Calibration row owed at completion.

## 5. PLACEMENT, MEMORY AND THE DETACHED FORM

As D16 §5 with **cpuset `12,15`** (the supervisor's FIFTEENTH-session placement; disjoint from D15 `2,3`, D16 `4,14`, D5 `8,10,11,13`, D4-SHIPPED `5,6,7,9`, D6 `2,3,4,14`; not core 0). **Disclosed overlap: the same dispatch places D8R's four ranks on `0,1,12,15`.** A live 4-rank container on `12,15` would halve the cores delivered to this item's pinned 2-rank arms and fail G12's delivered-cores floor for a placement reason, so this family's driver gains **G-CPUSET** (`d17_cpuset_overlap.py`, new): before each arm, a LIVE container whose `HostConfig.CpusetCpus` shares a core with `12,15` → **wait-and-retry** in the aggregate guard's exact form (poll 30 s, bound 14,400 s, `rc=6 CPUSET_BLOCKED_AT_BOUND` at the bound, zero compute) — D8R's driver carries the same guard, so whichever item fires second waits for the other's arm to end. **Memory, a registered deviation from the dispatch's "4g":** the X arms carry the adjoint and are capped at **8g**; the F arms and MESH at 4g. Derivation: A6 N=16 (41,760 cells, 3D, 7 states, `transonicPCOption 1`) peaks at 9.787 GiB for one adjoint (`ADJOINT_MEMORY_ENVELOPE.md`, D8 §2); the wedge has 40,000 cells, **5 states** (ρ, ρU, ρE) and a 2D 5-point stencil against a 3D 7-point one, so the Jacobian storage scales by ≈ (5/7)² × (5/7) ≈ 0.36 → ≈ 3.6 GiB, plus HiSA's PC-flux matrix (`hisaUsePCFlux 1`) and ILU(1) fill — **4g is not defensible for an arm that carries the adjoint; 8g is**. H5 floors are per arm: **12.0 GiB** for X (8 + 4, the D4-SHIPPED 4.4 rule), 8.0 for MESH/F; aggregate wait-and-retry < 30.6 GiB with the arm's own cap; the detached driver with `STATUS.<arm>` / `STATUS.chain` / `d17_driver.pid`, no `--rm`, the `.inspect.txt` kernel record, **`CHAIN_DONE`** appended on every exit of a started chain (D5 Addendum 3 A3.5 form, so a later item can wait on this one with no agent alive), and G-ROOT.1–.5 from birth — the forbidden list names D16's, D8's and D8R's roots in addition to D15's. **G-ROOT.5 DEMONSTRATED 2026-08-26T22:26:24Z (`d17_groot5_selftest_evidence.txt`, 18/18):** sacrificial `sleep` container on cpuset 15 → `rc=3`; sacrificial live pid 518597 with cwd = run root → `rc=3`; stale pidfile ignored; clear passes to the L-251 check; D4/D5/D16/**D8** roots refuse at G-ROOT.1; G-ROW refuses `X-P` on shipped and `MESH` on patched, passes `X-S` on shipped which then refuses for want of `MESH/`; root empty and absent afterwards. Run-root staging as D16 (from the `Cone_Supersonic` checkout; **nineteen tutorial inputs md5-asserted** before copying — every file the arms consume, listed in `d17_chain_driver.sh::tut_md5_list`; the tutorial's `decomposeParDict` asserted then replaced by the overlay).

## 6. PREDICTIONS — scored HIT/MISS by the comparator, never adjusted

| # | prediction | value / band |
|---|---|---|
| **P1** | MESH reproduces the derived mesh | **cells == 40,000** |
| **P2** | baseline `CL` at 0° on the symmetric wedge (symmetry reading) | **abs(CL) ≤ 1.0e-3**, point 0 |
| **P3** | baseline `CD` — oblique-shock estimate for θ = 11.31° at M 1.958: β ≈ 41.7°, `Mn1` ≈ 1.30, `p2/p1` ≈ 1.82, `Cp` ≈ 0.30; the force function integrates **absolute** p over the two wedge faces (projected frontal area 2 × 0.2 × 0.1 = 0.04 m²) against `q · A0` with `A0 = 0.1` and `q = 27.2 kPa` → ≈ 0.27 (gauge reading ≈ 0.12) | **[0.08, 0.45]**, point 0.27 |
| **P4** | the PATCHED row G5 (CD) `PASS` with **≥ 4 of 5** in band D | point 5/5, worst ≤ 2 % (D7FR's worst 1.959 %; D8's 2.65 %) |
| **P5** | the SHIPPED row's `shape[0]` (the LE-class function, A1's idx6 class) **outside band D or sign-flipped** — the same geometric reading of the IDWarp rotation defect as D15 P5 / D16 P5, now on a supersonic Euler wedge with no aoa variable; hence SHIPPED `GATE FAIL`, item `GATE FAIL`, cell → `CAN DO, CAVEATS` | HIT / MISS / `NOT_MEASURED` as D15 |
| **P6** | total graded core-min in **[60, 450]** (point 180.3); MESH wall ≤ 120 s | both scored |

**Registered falsifier and the cross-item reading:** D15 P5, D16 P5 and D17 P5 are one claim at M 0.29, M 0.69 and M 1.96 (the third on a different geometry and solver class). **HIT on all three** = the rotation defect reaches the baseline gradient independent of regime and solver; **MISS on all three** = the shipped row passes at the baseline in every compressible regime, contradicting the incompressible A1 record at the same design point — a finding about the defect's reach, not a defect of the items; a **split** is reported as a split with every number, never averaged.

## 7. INSTRUMENTS, FROZEN BY MD5 AT THIS COMMIT — copy-and-delta from D16 (itself from D15 / D14 / D5)

| file | md5 | derivation, and the DELTAS file the supervisor reads |
|---|---|---|
| `d17_run_arm.sh` | `1de86c92e903a72dee10260b911b6ede` | `curriculum_D16/d16_run_arm.sh` (`bf29bd90…`) + **`d17_run_arm_DELTAS_from_d16.diff` (235 diff lines)**: item/root names; the `Cone_Supersonic` checkout path; cpuset `12,15` (+ the overlap comment); cap/memory table (X 90.0 / 8g, F 300.0 / 4g); producer md5 `86218625…`; D16's, D8's and D8R's roots forbidden; the MESH-arm cleanup names; header lineage |
| `d17_chain_driver.sh` | `a4bb75cfc8e1ed54e0b73a1c8d4133d5` | `curriculum_D16/d16_chain_driver.sh` (`3214efed…`) + **`d17_chain_driver_DELTAS_from_d16.diff` (257 lines)**: names/root; `TUT_SRC = …/Cone_Supersonic`; `tut_md5_list` (19 inputs) in place of six; per-arm `cap_mem_gib` / `h5_floor_gib`; **G-CPUSET wait-and-retry** (5b); `CHAIN_DONE` trap; launcher/grader/instrument md5s |
| `d17_xf.py` | `37b0861594065fd3a292224e1446d25a` | `curriculum_D16/d16_xf.py` (`c78eaaaa…`) + **`d17_xf_DELTAS_from_d16.diff` (255 lines)**: `ITEM`, `PRODUCER_MD5`, `SCENARIO = "cruise"`, the `shape`-only DV set (every `patchV` reference removed), `COMPONENTS` (0, 1, 3, 4, 5), `STEPS` (`shape` only), the CL symmetry note, artefact/marker names. CTRL control, emit/fsync, η, the plant read-back: unchanged |
| `d17_grade.py` | `a940b0ee51c9102bb27dd6aa4516f366` | `curriculum_D16/d16_grade.py` (`0b8338b3…`) + **`d17_grade_DELTAS_from_d16.diff` (338 lines)**: item/root/artefact names; `CAPS`, `ITEM_CEILING_CORE_MIN = 785.0`, `PREDICTED_CORE_MIN`; `CELLS_EXPECTED = 40000`; `COMPONENTS_REGISTERED`, `STEPS_REGISTERED`, `DVS = ("shape",)`; `CPUSET_REGISTERED = "12,15"`; `PRED` (P2 as an absolute bound, P3/P6 bands); **`symmetry_reading()` in place of `grade_components(…, "CL")` and `compose_row(g_cd)` on CD only**; P1/P2/P5 names; the cell string; the selftest fixture (6 shape values, CL 0) and the units that carry a constant (U4/U5/U6 component indices, U13 301 > 300, U14 cpuset `4,14`, U15 40001). Bands D/E, plateau, sign-flip rule, field classes, `EXPECTED_UNITS = 22`: unchanged. **22/22 plain and `-O`** |
| `d17_groot5_selftest.sh` | `37e06a38d786012e1d1382a58b43ab9a` | `curriculum_D16/d16_groot5_selftest.sh` + **`d17_groot5_selftest_DELTAS_from_d16.diff` (93 lines)**: names; sacrificial cpuset 15; D16's and D8's roots in (d) in place of D13's and D15's |
| `d17_cpuset_overlap.py` | `c976998d2ff5cd49d58374ee39ff5d57` | **NEW** (no DELTAS file): the aggregate-memory script's form; reads `docker ps` + `inspect HostConfig.CpusetCpus`, ignores this item's own prefix, prints one JSON line, `ok=false` on any overlap or exception |
| `d17_aggregate_memory.py` | `709ab0b98ef0302a3a3a318588f9493f` | byte-identical to D16's / D15's / D5's / D14-M's (`cmp` equal) |
| `d17_runScript.py` | `8621862522cf2af72f381f0d97660b8a` | **byte copy** of `/home/ubuntu/dafoam-tutorials/Cone_Supersonic/runScript.py` |
| `d17_decomposeParDict` | `68ecc827562886fb43c3aedb0627b344` | byte-identical to D16's / D15's (`numberOfSubdomains 2`, `scotch`); replaces the tutorial's `numberOfSubdomains 8` file (md5 `7840874d…`, asserted before replacement) |
| tutorial inputs, md5-asserted at staging (19) | `runScript.py` `86218625…`, `preProcessing.sh` `124bd4bf…`, `FFD/FFD.xyz` `99ec61c5…`, `FFD/genFFD.py` `4c5a9150…`, `system/blockMeshDict` `4bf634ce…`, `mirrorMeshDict` `e2db0b7f…`, `controlDict` `33fbb919…`, `fvSchemes` `5d01f551…`, `fvSolution` `a7762882…`, `decomposeParDict` `7840874d…`, `constant/thermophysicalProperties` `a62f7097…`, `turbulenceProperties` `5b8c8055…`, `0.orig/T` `c2f4edd5…`, `U` `3e7c37d4…`, `p` `9ffd0533…`, `nut` `12890bb5…`, `nuTilda` `63d469c0…`, `alphat` `8d593908…`, `include/freestreamConditions` `0241c099…` | the checkout is not frozen by this repo, so its bytes are |

No `assert` in any python file (AST count 0, counted by the grader and in the evidence file). Classifier denials while building: none.

## 8. WHAT THIS ITEM WILL NOT ESTABLISH

D16 §8 verbatim, plus: nothing about the shock's position or strength (no Cp is graded; CD is one number on a 40,000-cell mesh); nothing about viscous drag (the tutorial is inviscid); nothing about `CL` (a symmetry reading, never a gate); nothing axisymmetric — this is a planar wedge and the `axisym · steady · supersonic` cell stays `CAN NOT DO — not attempted`; nothing about the thickness/volume constraints (no optimiser; the `optimization converged` column does not move); nothing about `DAHisaFoam` beyond the tutorial's own options.

## 9. FREEZE AND QUEUE

**Committed BEFORE any container starts.** Grading path fixed at this commit: `d17_grade.py` md5 `a940b0ee51c9102bb27dd6aa4516f366`. **Queue entry `verification/queue/dafoam/D17_chain.json`:** team `dafoam`, `prereg_commit` = the sha of the commit introducing this file, `launch_cmd` = `["bash", "<abs>/d17_chain_driver.sh", "MESH", "X-S", "F-S", "X-P", "F-P"]`, `cwd` = this directory, `ranks 2`, `cost_core_min_estimate 180.3`, `cap_core_min_registered 785.0`, `memory_floor_gb 12.0`, `cost_basis` derived / not measured, `permission bc0e687e`. Enqueueing is not authorisation (check 4 is the supervisor's). **The runner is the launch path and is HELD at its 85 % busy ceiling by other teams' load at this write (`runner.log` 22:04:52Z `busy=94.6 %`); nothing is launched by hand.** Filing check: `python3 scripts/check_filing.py` → the same 28 pre-existing violations (papers' sidecars, none under `cases/dafoam/`) before and after this directory existed — this directory adds none. **Predicted outcome:** P1–P4, P6 HIT; P5 HIT → SHIPPED `GATE FAIL`, PATCHED `PASS`, item **`GATE FAIL`**; the `2D · steady · supersonic` gradient cell moves from `CAN NOT DO — not attempted` to `CAN DO, CAVEATS`.
