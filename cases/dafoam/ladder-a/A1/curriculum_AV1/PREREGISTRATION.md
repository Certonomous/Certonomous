# CURRICULUM AV1 — np-INVARIANCE of the A1 NACA0012 baseline adjoint gradient (`DASimpleFoam`, 4,032 cells) at np = 1 / 2 / 4 (`scotch`), ON TWO TOOLCHAIN ROWS — PRE-REGISTRATION

**Version 1.0. FROZEN.** Dated **2026-08-26**. Lane: dafoam `lab-lane` (G, re-spawned). Supervisor: `dafoam-supervisor` (lane G's brief, TASK 2: *"AV-1 — np-invariance on A1 NACA0012 (4,032 cells, `DASimpleFoam` … cheapest; np=1/2/4, both rows, CD and CL totals w.r.t. `shape`+`patchV`, band per (iii), CTRL planted-zero, G-ROOT.5 driven, cpuset 0,1 for np ≤ 2 and 0,1,12,15 for np=4"*). **This is the first measurement rung `docs/dafoam/ADJOINT_VERIFICATION_STANDARD.md` §3 calls for** (`aa5cd787`, amended `c3f08b6d`); Sanaa's §6 at `068c2bf0` names np-invariance as one of the five adjoint checks.
**Nothing in this item is filed, sent, uploaded, registered, posted or commented** (`CLAUDE.md` rule 7; `DAFOAM_CHARTER.md` §10). Permission for the detached launch: Sanaa's own words at **`bc0e687e`**; queue-first order `7def3c6b` / `73eccb1b` / `0b041d1a` / `3c3ef86c`; L-342 field classes `d4d0c29d`; the grid as the goal `068c2bf0`. Every decision here is `[lab-attributed]`.

---

## 0. SCOPE, AND WHAT THIS ITEM CAN AND CANNOT MOVE

**The check.** `ADJOINT_VERIFICATION_STANDARD.md` §3: the same gradient at the same design point on the same mesh and image, at np = 1 (the serial reference) and np = 2, 4 with `scotch`, and the objective beside it — `s_J(np) = |J(np) − J(1)| / |J(1)|`, `s_g(np) = ‖g(np) − g(1)‖₂ / ‖g(1)‖₂`, per-component sign agreement. **This item carries NO FD table**: it grades the adjoint against itself across rank counts, never against a finite difference, and therefore **moves no capability-grid verdict on its own** — it supplies the "np-invariance" entry of the 2D·steady·incompressible cell's *what was checked* (`docs/capability/dafoam_GRID.md`, which today cites only B3's un-banded measurement). The FD rows on this mesh are D13's (`15767999`) and D15's (`8fc2bdeb`).

**The case.** A1 NACA0012 on its own 4,032-cell mesh: the INCOMPRESSIBLE tutorial (`/home/ubuntu/dafoam-tutorials/NACA0012_Airfoil/incompressible`, checkout `d3b7e38b`; neither image ships the tutorials, D15 §0), `DASimpleFoam`, `U0 = 10.0`, `aoa0 = 5.139°`, SA, `primalMinResTol 1e-8`, `adjEqnOption.gmresRelTol 1e-6` (`av1_runScript.py:31-35, :43-44, :69`); 8 `shape` functions + `patchV` (`:143-171`). Regime: 2D · steady · incompressible (the grid's regime table).

**Why np = 4 lands on cores 0,1,12,15.** The supervisor's placement for this rung: `0,1` for every arm at np ≤ 2, `0,1,12,15` for the two np = 4 arms. **Disclosed:** `12,15` is lane N2's D17 placement; if D17 fires while an AV1 np = 4 arm is live the cpusets overlap — the aggregate guard (§5) absorbs the co-tenancy as a wait, and the cpuset is not re-pinned (placement is configuration). Core 0 is a departure from D15's "not core 0" note and is the supervisor's choice for this rung, recorded, not re-derived.

## 1. THE AMENDMENT CONDITION, AND HOW IT WAS CHECKED

> **The run root `/home/ubuntu/certonomous-runs/CURRICULUM-AV1-a1-naca0012-npinv` DOES NOT EXIST.**

`test -e` → false at **2026-08-26T22:33:23Z** (before the selftest) and again as the last leg of `av1_groot5_selftest.sh` at **22:33:25Z** (`av1_groot5_selftest_evidence.txt`: "run root ABSENT after the test"). The chain driver creates it on the first fire (§5). **This item has burned 0 core-min and started no arm container**; the only container this lane ran was the sacrificial `sleep` of the selftest (`av1_X1-S_selftest_…`, removed). After the first container, gates are **CLOSED**; changes land only as dated addenda that cannot alter a gate, threshold, cap or label.

## 2. ARMS — seven, in this order, one detached chain, TWO ROWS

| arm | kind (G1) | image (row) | ranks | cpuset | mem | task | work dir | artefact |
|---|---|---|---|---|---|---|---|---|
| **MESH** | SCRIPT | SHIPPED | 1 | 0,1 | 4g | the tutorial's own `preProcessing.sh` (pyHyp → `plot3dToFoam` → `autoPatch` → `createPatch` → `renumberMesh`, `cp -r 0.orig 0`) + `checkMesh` | `MESH/` (cold copy of `base/`) | `MESH/checkMesh.log` |
| **X1-S** | SOLVER | SHIPPED | 1 | 0,1 | 4g | `av1_x.py -mode X`: one primal, then `compute_totals(of=[CD, CL], wrt=[shape, patchV])` — **the serial reference** | `X1-S/` (cold copy of `MESH/`) | `X1-S/av1_X.json` |
| **X2-S** | SOLVER | SHIPPED | 2 | 0,1 | 4g | as X1-S at np = 2, `scotch` (`av1_decomposeParDict_np2` overlaid) | `X2-S/` | `X2-S/av1_X.json` |
| **X4-S** | SOLVER | SHIPPED | 4 | 0,1,12,15 | 6g | as X1-S at np = 4, `scotch` (`av1_decomposeParDict_np4` = the tutorial's own dict, overlaid) | `X4-S/` | `X4-S/av1_X.json` |
| **X1-P** | SOLVER | PATCHED | 1 | 0,1 | 4g | as X1-S | `X1-P/` | `X1-P/av1_X.json` |
| **X2-P** | SOLVER | PATCHED | 2 | 0,1 | 4g | as X2-S | `X2-P/` | `X2-P/av1_X.json` |
| **X4-P** | SOLVER | PATCHED | 4 | 0,1,12,15 | 6g | as X4-S | `X4-P/` | `X4-P/av1_X.json` |

Chain: `av1_chain_driver.sh MESH X1-S X2-S X4-S X1-P X2-P X4-P`, stops at the first non-zero rc, then runs the frozen grader on whatever exists (its rc is infrastructure). The two-row rule (`DAFOAM_CHARTER.md` §6; standard §5): every X arm is bought on BOTH images, on ONE mesh generated once in the SHIPPED image. G-ROW derives the row from the arm suffix and refuses an image of the other row (driven, §7). Arm command, verbatim from the launcher's `case`: `mpirun --allow-run-as-root -np <ranks> --bind-to core --report-bindings -x PYTHONPATH python av1_x.py -mode X`, written to `<arm>/av1_cmd.sh` and executed **inside the container under `timeout -k 60 <cap wall>`**. DAFoam decomposes the case itself at np > 1 (`pyDAFoam.py:1454 runDecomposePar`) from the overlaid, md5-asserted `system/decomposeParDict`; the instrument records the per-processor cell counts it finds afterwards (`av1_x.py: partition_record`) so every parallel gradient names its decomposition (`DAFOAM_CHARTER.md` §5).

**The planted control (rule 3), at the artefact level because this rung has no FD table to carry a CTRL component.** The instrument writes `av1_X.json`, then `av1_X_planted.json` = the same artefact with `PLANT = 1.234e-03` added to every total, re-reads both from disk through the same reader and exits 2 unless every value moved by exactly PLANT (`av1_x.py:150-166`), printing `AV1_PLANTED_CONTROL_SEEN` — a line the grader requires in every X log. The grader repeats the plant on the np = 2 SHIPPED artefact into `<root>/grader_controls/` and refuses unless it is seen, and additionally grades a **sign-flipped copy of the np = 4 SHIPPED artefact** through the same G-NP function, refusing unless it reads `GATE FAIL` with every component flipped (`av1_grade.py: grader_plant_control, sign_flipped_copy`). A spread reader not shown able to see a wrong gradient is not a reader.

## 3. GATES, THRESHOLDS AND LABELS — `av1_grade.py`

All bands are frozen now. Vocabulary: `PASS / GATE REACHED / GATE FAIL / NOT A RESULT / BLOCKED / PENDING` and nothing else.

* **G1 — completion, ARM-KIND AWARE** (D15's form, from birth): every arm kernel `rc == 0` from `docker inspect .State.ExitCode` (harness/kernel disagreement refuses), `OOMKilled false`, the **age guard** (artefact strictly newer than the arm's own `.av1_age_datum`; `0/U`, or `0.orig/U` for MESH, touched last at stage); SOLVER arms also need `AV1_X_WRITTEN` **and** `AV1_PLANTED_CONTROL_SEEN` in the arm log; MESH needs `Mesh OK.`. Any clause failing → **`NOT A RESULT`** (exit 2). An arm with no ledger row is read from the launcher's `<ARM>_<stamp>.inspect.txt` with the source named per field and every infrastructure field `NOT_MEASURED`.
* **L-342 field classes (`d4d0c29d`)** as D15 §3: absent infrastructure → `NOT_MEASURED` beside the verdict, never composed to PASS; present-but-garbage → REFUSE; absent physics → REFUSE.
* **G-M2 — mesh identity:** `cells == 4,032` → `PASS`, else `GATE FAIL`.
* **G-NP — the bright line, per ROW, with the np = 1 arm as the serial reference, for np ∈ {2, 4}:** (a) **objective spread `s_J ≤ 2.2e-5`** for CD and for CL — the lab's measured partition-reproducibility upper bound (`PARALLEL_GATE_DOCTRINE.md:38, :194-198` @ `fd9bf1b6`; B3 measured 2.9e-7, `bb5088c4:25`); (b) **gradient spread `s_g ≤ 1.0e-3`** against the serial reference for the CD gradient (`shape` + `patchV` concatenated, 10 components) and for the CL gradient — 6× the family's measured floor of 1.1–1.7e-4 (B3 `bb5088c4:16-24`) and 50× below band D, derived in the standard §3 from the adjoint solve at `gmresRelTol 1e-6` on two different preconditioned partitions; (c) **0 sign flips** per component on components with `|g_1,i| ≥ 1e-14` (smaller ones named and skipped; `VERIFICATION_CHARTER.md:845`). Any of (a)/(b)/(c) outside → the row is **`GATE FAIL`** with the np and the quantity named. The np = 2 / np = 4 partition record must carry exactly np entries summing to the mesh's cell count, else **`NOT A RESULT`** (a parallel gradient that cannot name its decomposition is not a result, `DAFOAM_CHARTER.md` §5); an artefact whose `nprocs` differs from the arm's registered ranks refuses. **This gate fails if** the decomposition changes the converged gradient by more than 0.1 % or flips a component, or the objective by more than 2.2e-5; **a wrong treatment could still pass it** only by producing the same wrong gradient at every np — which is exactly why this rung is NOT an FD verdict and moves no grid cell on its own (§0; the FD rows carry that). Identity test (`VERIFICATION_CHARTER.md` §2a) answered.
* **G9 — toolchain per row:** ledger `DIGEST`, the container's `D4S_IDWARP_SO_MD5:` print and the artefact's in-process `libidwarp.so` md5 must all name the row's toolchain — SHIPPED `f0fcb488e0e98156575cd19548e91663` @ `sha256:9d45679d…f07fc`, PATCHED `85f59e87253e0a71a813f64ca6e4c425` @ `sha256:2927768a…30f6d35`. Mismatch → `GATE FAIL`.
* **G10 — caps (§4):** every arm `core_min ≤ cap`, sum ≤ **95.0**; a crossing is `GATE FAIL` as the frozen text says (report mode; the in-container deadline is the stop).
* **G11 — OOM hard:** `--memory --memory-swap --oom-score-adj=500` kills; `OOMKilled true` is a G1 refusal, never a re-fire.
* **G12 — placement:** `cpuset` equals the registered one PER ARM (`0,1` at np ≤ 2; `0,1,12,15` at np = 4); delivered cores ≥ 0.75 × ranks where measured (cgroup sampler), `NOT_MEASURED` disclosed otherwise.
* **DIVERGENCE** shipped-vs-patched on the np = 1 CD gradient is **reported per component with its number**, never gated.
* **Item verdict (composition registered here):** any refusal → **`NOT A RESULT`**; else any of G-M2/G9/G10/G12 or any row `GATE FAIL` → **`GATE FAIL`**; else **`PASS`**. No grid family exists, so standing rule 5 has no row and **NO GCI IS QUOTED**. No FD table exists in this item, so **no FD verdict is quoted**.

## 4. COST — DERIVED FROM NAMED ANCHORS, NOT MEASURED

Anchors (`docs/COST_CALIBRATION.md`): **C-31** — D1-C′, one cold primal + one adjoint on this very mesh at np = 1 in the SHIPPED image: **1.483 core-min measured** (89 s); **C-71** — D13, per start 5.15–6.38 core-min for 9–11 IPOPT majors plus an FD suite at np = 1; **C-135** — D14-M mesh regeneration 0.1833 core-min for 38,304 cells at 1 rank; the D13 lane's measurement that concurrent containers on one core collapse throughput (launcher header) — hence the pinning. The X arm here is C-31's primal + adjoint **plus a second adjoint (CL)** ≈ 1.483 + 0.7 ≈ 2.2 → **2.5 at np = 1**; at np = 2 and 4 the wall falls sub-linearly on 4,032 cells (2,016 and 1,008 cells per rank, communication-bound) so the core-minutes rise: **3.0 at np = 2, 4.0 at np = 4** (parallel efficiency 0.8 and 0.6 assumed; P4 grades the assumption).

| arm | derivation | point (core-min) | cap (core-min) | in-container wall | mem |
|---|---|---|---|---|---|
| MESH | C-135 scaled to 4,032 cells + container start | **0.3** | 5.0 | 300 s (1 rank) | 4g |
| X1-S, X1-P | C-31 + a CL adjoint | **2.5** each | 10.0 each | 600 s (1 rank) | 4g |
| X2-S, X2-P | X1 at efficiency 0.8 | **3.0** each | 15.0 each | 450 s (2 ranks) | 4g |
| X4-S, X4-P | X1 at efficiency 0.6 | **4.0** each | 20.0 each | 300 s (4 ranks) | 6g |
| grader | zero compute | 0 | — | — | — |
| **total** | | **19.3** point, band **[10, 50]** | **ceiling 95.0 = Σ caps** | ≈ 15 min wall at the point, ≈ 45 min at the caps (+ seven 60-s H5 windows) | |

**`cost_basis`: c7a.4xlarge at $0.0513/core-h, REPORTED-BY-OWNER, NOT MEASURED** — the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` §5). Dollars **DERIVED**: point **$0.0165**, ceiling **$0.0812**. **Cap mode:** each arm's wall deadline is **inside the container** (`timeout -k 60` at cap × 60 / ranks s; the launcher asserts the enforced wall equals the registered cap to 0.02 core-min and aborts otherwise); the host poller reports a crossing and hard-stops at 4× cap while it lives. The queue entry carries `cap_core_min_registered = 95.0`, so the runner's `CAP_OVERRUN.txt` watch fires at 1.00 × 95.0 × 60 / 1 = 5,700 s after launch (report-only, infrastructure). A calibration row is owed at completion (rule 12).

## 5. PLACEMENT, MEMORY AND THE DETACHED FORM

* **cpuset per arm** (`--cpuset-cpus`, `--cpus=<ranks>`): `0,1` for MESH/X1/X2, `0,1,12,15` for X4 — §0's disclosure of the D17 overlap on 12,15 stands.
* **Memory:** `--memory=4g --memory-swap=4g` (6g at np = 4; D13 measured peak RSS 1.70 GiB on this mesh at np = 1; PETSc's per-rank footprint at np = 4 is bounded well inside 6g by the same measurement); **H5 windowed gate 45 samples / 60 s, refuse on ANY sample < 8.0 GiB**; **aggregate** (live caps + this cap + host non-container RSS) **< 30.6 GiB** in the wait-and-retry form (poll 30 s, bound 4 h, every wait a line in `STATUS.<arm>`, refuse-and-BLOCK at the bound) — UPDATE F's ruling, from birth.
* **The detached form.** The queue runner launches `bash av1_chain_driver.sh MESH X1-S X2-S X4-S X1-P X2-P X4-P` under its own `setsid nohup`; the driver writes its pid to `<run root>/av1_driver.pid` (G-ROOT.5 b), opens `STATUS.<arm>` at preflight and appends every wait and the final `rc=<n> … source=launcher_exit=docker_inspect_ExitCode` line inside the detached session — never the `$?` of a `setsid`/`timeout` line. `STATUS.chain` carries the chain state and, at the end, `grader_rc=<n> … note=comparator-exit-status-NOT-the-verdict`. Container `rc` is read from `docker inspect` before `docker rm`; **no `--rm`**; the launcher writes `<ARM>_<stamp>.inspect.txt` as the surviving kernel record.
* **G-ROOT.1–.5 from birth.** `BASE` must equal this item's root through `realpath -m` (G-ROOT.1); the forbidden list names D4's, D4-SHIPPED's, D4-SHIPPED-R's, D5's, D6's, D14's, D14M's, D7R's, D7FR's, D12R's, D12R2's, D12R2W2R's, D13's, **D15's, D16's, AV2's**, A2-mach-wing's roots, the tutorial checkout, `certonomous-runs` and the repository (G-ROOT.2); the ledger refuses a foreign `ITEM=` (G-ROOT.3); an `rc=0` row refuses a re-fire (G-ROOT.4 `ALREADY_BOUGHT`, driver); **G-ROOT.5** refuses a RUNNING container carrying `av1_<ARM>_` or a driver pidfile naming a live pid that is not an ancestor or whose cwd is the run root — **DEMONSTRATED 2026-08-26T22:33:23–25Z (`av1_groot5_selftest_evidence.txt`, 19/19):** a sacrificial `sleep` container → `rc=3`; a sacrificial live pid (532613) with cwd = run root → `rc=3`; a stale pidfile does not block; clear passes and the launcher stops at the L-251 mode check before any staging; D4's, D5's, D13's, D15's and D16's roots refuse at G-ROOT.1; G-ROW refuses `X1-P` on the shipped image and `MESH` on the patched image (rc=4) and passes `X4-S` on the shipped image, which then refuses (rc=5) for want of `MESH/` — nothing staged, no arm directory created; zero backticks on executable lines; the temporary root empty and **absent afterwards**.
* **Run root staging** (driver): the first fire creates the root (mode 777, L-251), copies the tutorial's `0.orig FFD constant system profiles genAirFoilMesh.py preProcessing.sh` into `base/` (no `polyMesh`), asserts the tutorial's `system/decomposeParDict` IS the registered np4 dict, copies the instruments and both decomposition dicts, writes `ITEM=AV1` alone on ledger line 1 and a `STAGED …` line, and md5-asserts every staged file — **including the six tutorial inputs** — before any container starts. A second fire stages nothing. The launcher overlays the np2 / np4 dict into `<arm>/system/decomposeParDict` for the X2 / X4 arms only, md5-asserted after the copy, and prints `D4S_DECOMP_DICT arm=… md5=…`.

## 6. PREDICTIONS — scored HIT/MISS by the comparator, never adjusted

| # | prediction | value / band | falsifier |
|---|---|---|---|
| **P1** | the PATCHED row is np-invariant: G-NP `PASS` at np = 2 and np = 4 inside the band | `s_g ≤ 1e-3`, `s_J ≤ 2.2e-5`, 0 flips; point `s_g ≈ 1–2e-4` (B3's floor) | MISS = a decomposition-dependent gradient on the patched toolchain on this mesh — the A4 finding reaching A1 |
| **P2** | the SHIPPED row is np-invariant the same way | as P1 | MISS = the same on the shipped toolchain; a P1 HIT / P2 MISS pair would locate the dependence in the shipped IDWarp |
| **P3** | the objective is **not bit-identical** across np — as B3 G4 found (spread 1.9e-07) — but inside the 2.2e-5 band | `0 < max s_J ≤ 2.2e-5`, point ~1e-7 | MISS if every arm reproduces the serial CD and CL to the bit (spread exactly 0), or if any spread exceeds the band |
| **P4** | cost ratio: the np = 4 SHIPPED arm's core-minutes are **1.0–3.0×** the np = 1 arm's (parallel efficiency 33–100 % at 1,008 cells per rank) | ratio in `[1.0, 3.0]`, point 1.6 | MISS below 1.0 (super-linear, implausible) or above 3.0 (efficiency < 33 %) |
| **P5** | total graded core-min in **[10, 50]** (point 19.3) | scored on the ledger | |
| **P6** | MESH wall ≤ 120 s | scored | |
| **P7** | the MESH arm reproduces A1's mesh | `cells == 4,032` | |

**The falsifier registered up front:** a P1 or P2 MISS is the finding this rung exists to be able to make — the family's only np measurements on a gradient are B3's (clean) and A4's (8.95 % on `scotch`), and no case has been graded against a frozen np-invariance band. Either outcome enters the standard's §3 status line with its number.

## 7. INSTRUMENTS, FROZEN BY MD5 AT THIS COMMIT — copy-and-delta from D15 (`8fc2bdeb`)

| file | md5 | derivation, and the DELTAS file the supervisor reads |
|---|---|---|
| `av1_run_arm.sh` | `9996cfddf5c1f8637a498027cd48385d` | `curriculum_D15/d15_run_arm.sh` (`796a2de5…`) + **`av1_run_arm_DELTAS_from_d15.diff` (300 diff lines)**: item/root; forbidden roots (+ D15, D16, AV2; the incompressible tutorial); seven arms; `ranks_of` 1/2/4; **`cpuset_of` per arm**; cap and memory tables; the two decomposition dicts asserted and **overlaid per np at staging**; the X-only command; G-COLD list; `D4S_*` strings inherited unchanged |
| `av1_chain_driver.sh` | `b070e40957b88e5d8b0ebba10075d500` | `curriculum_D15/d15_chain_driver.sh` (`4656f1a0…`) + **`av1_chain_driver_DELTAS_from_d15.diff` (173 lines)**: names/root/md5s; the INCOMPRESSIBLE tutorial source with its own `preProcessing.sh` md5 (`4a939545…`, comments-only different from the subsonic one's); the tutorial dict asserted as the np4 dict; both dicts copied; `cap_mem_gib` 4/6 per arm; H5 floor 8.0 |
| `av1_x.py` | `74011a9c5e6c760b8aec1c78e928b4d2` | `curriculum_D15/d15_xf.py` (`8a664a97…`) + **`av1_x_DELTAS_from_d15_xf.diff` (391 lines)**: producer = the incompressible `runScript.py` (`0557da51…`); X mode only; the partition record; the artefact-level planted control with disk read-back |
| `av1_grade.py` | `87f15e05130cfdb3cbf195d1daba6154` | **the grading path.** `curriculum_D15/d15_grade.py` (`b429ec89…`) + **`av1_grade_DELTAS_from_d15.diff` (796 lines; the G5 FD gate replaced by G-NP, read as a file)**. **24/24 under `python3` AND `python3 -O`** (`av1_grade_selftest_evidence.txt`): clean → PASS with P1–P7 HIT; planted 2e-3 spread at np = 4 → row GATE FAIL, np named; 9e-4 → PASS; planted 5e-5 objective spread → GATE FAIL; planted sign flip → counted; bit-identical objectives → P3 MISS; rc=1 / OOM / missing marker / missing control line / stale artefact / 3-entry partition / 4033-cell partition / wrong nprocs / harness-kernel disagreement → REFUSAL; wrong `.so` → G9; cap crossing → G10; wrong cpuset → G12; cells 4033 → G-M2; absent infrastructure → `NOT_MEASURED` beside an unchanged PASS; absent row + one inspect record → read from it; `ast.Assert` = 0 in grader and instrument, the counter sees a planted assert |
| `av1_groot5_selftest.sh` | `dbb22dae4266f026d40fb10d386760f3` | `curriculum_D15/d15_groot5_selftest.sh` (`8a2a7df6…`) + **`av1_groot5_selftest_DELTAS_from_d15.diff` (98 lines)**: names; sacrificial cpuset 1; D15's root added to (d); the G-ROW legs on `X1-P` / `MESH` / `X4-S` |
| `av1_aggregate_memory.py` | `709ab0b98ef0302a3a3a318588f9493f` | **byte-identical** to `d15_aggregate_memory.py` (`cmp` silent) |
| `av1_runScript.py` | `0557da51f6f179f6de865144343c499f` | **byte copy** of `/home/ubuntu/dafoam-tutorials/NACA0012_Airfoil/incompressible/runScript.py` (`d3b7e38b`) — the producer whose header the instrument execs; never modified |
| `av1_decomposeParDict_np2` | `68ecc827562886fb43c3aedb0627b344` | byte copy of `curriculum_D15/d15_decomposeParDict` (the tutorial's dict with `numberOfSubdomains 4 → 2`, `scotch`) |
| `av1_decomposeParDict_np4` | `816f5ba44075fde47fa5db4269877bc8` | byte copy of the tutorial's own `system/decomposeParDict` (`numberOfSubdomains 4`, `scotch`) |
| tutorial inputs, md5-asserted at staging | `genAirFoilMesh.py` `681f10659eb90457fca13fc933008b93`; `preProcessing.sh` `4a9395452540705686acf94898aa33af`; `NACA0012PS.profile` `51dfed28e1bdb4cd33e0d8d7dabd586a`; `NACA0012SS.profile` `4a6b8ef4501494c7693b71e88a2eabbf`; `FFD/wingFFD.xyz` `6ddf378b028d03d8a18270488bee1759`; `runScript.py` `0557da51…` | the mesh generator, profiles and FFD are byte-identical to the subsonic/transonic tutorials' (D15 §0), so the MESH arm regenerates the same 4,032-cell mesh |

**No `assert` carries a guard** in any python file here (AST count 0, counted by the grader; L-332). **Classifier denials in this lane while building AV1: none.**

## 8. WHAT THIS ITEM WILL NOT ESTABLISH

Nothing about FD agreement (no F arm — D13/D15 carry that); nothing about the `simple` or `hierarchical` decompositions (scotch only); nothing about partition determinism across two runs (one run per np; the partition sizes are recorded so a re-fire can grade it, D4 G8's form); nothing at np = 3 (A4's 6.05 % point is not re-measured); nothing about an optimum; no dot-product test (AV2), no complex step; no grid family, no GCI; nothing about a mesh other than A1's 4,032 cells. A P1/P2 HIT does not license carrying any other case's np = 1 verdict to np = 4 (`DAFOAM_CHARTER.md` §5).

## 9. FREEZE AND QUEUE

**Committed BEFORE any container starts** (rule 2). The grading path is fixed at this commit: `av1_grade.py` md5 `87f15e05130cfdb3cbf195d1daba6154`, asserted by the chain driver before staging and again before the grade. **Queue entry `verification/queue/dafoam/AV1_chain.json`:** team `dafoam`, `prereg_commit` = the sha of the commit introducing this file (full 40-hex), `launch_cmd` = `["bash", "<abs>/av1_chain_driver.sh", "MESH", "X1-S", "X2-S", "X4-S", "X1-P", "X2-P", "X4-P"]`, `cwd` = this directory, `ranks 4` (the widest arm; the runner's core-fraction check is taken on it), `cost_core_min_estimate 19.3`, `cap_core_min_registered 95.0`, `memory_floor_gb 8.0`, `cost_basis` derived / not measured, `permission bc0e687e`. Enqueueing is not authorisation: `SUPERVISION_CHARTER.md` §3 check 4 is the supervisor's own, discharged on the sha. **Predicted outcome, so it cannot be written afterwards:** P1–P7 HIT; both rows `PASS`; item **`PASS`**; the standard's §3 status line gains its first banded measurement; no grid cell moves.
