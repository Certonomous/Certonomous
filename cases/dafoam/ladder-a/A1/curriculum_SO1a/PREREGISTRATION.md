# CURRICULUM SO-1a — NACA0012 INCOMPRESSIBLE DRAG-MIN AT FIXED LIFT: the FD-VERIFIED GRADIENT RUNG of the shape-optimisation ladder SO-1, OBJECTIVE **and** CONSTRAINT, on TWO TOOLCHAIN ROWS — PRE-REGISTRATION

**Version 1.0. FROZEN.** Dated **2026-08-27**. Lane: dafoam `lab-lane` (D). Supervisor: `dafoam-supervisor`.
**Nothing in this item is filed, sent, uploaded, registered, posted or commented** (`CLAUDE.md` rule 7; `DAFOAM_CHARTER.md` §10). SUBMISSIONS PARKED.

**The order this item answers, verbatim from Sanaa's standing directives of 2026-08-27T16:54Z** (`git show HEAD:etc/sessions/2026-08-27T1654Z_sanaa_standing_directives.md`, §4):

> ### dafoam — SHAPE-OPTIMIZATION LADDER (pull in order when queue drains)
> Pattern per case: FD-verified gradient rung -> optimization rung ->
> post-optimum verification (re-solve at optimum, constraints checked,
> np-invariance spot row) -> D7R attribution rule: no improvement % quoted
> before its mechanism is decomposed (shape vs AoA vs operating point).
> After you are done with all the current optimization cases:
> - SO-1 NACA0012 subsonic drag-min at fixed lift; then RAE2822 transonic.

and §2:

> FREEZE-AHEAD >= 3: every team keeps at least three frozen, queue-ready registrations at all times. A starved queue is a planning defect.

**This item is rung 1 of 4 for SO-1 and nothing more.** Sanaa's pattern names four rungs per case; they are four separate registrations and this one freezes only the first. `SO-1b` (the optimisation rung), `SO-1c` (post-optimum verification) and the D7R attribution step are NOT frozen here, and `SO-1b`'s gates are deliberately not written until SO-1a has a result — writing them now would be fitting them to an answer this item has not yet produced.

Every decision here is `[lab-attributed]`. Permission for the detached launch: Sanaa's own words boarded at **`bc0e687e`**; queue-first order `7def3c6b` / `73eccb1b` / `0b041d1a`; L-342 field classes `d4d0c29d`; R-RC approved in the directives file above (§0); the grid as the goal `068c2bf0`.

> **ID-NAMESPACE CHECK.** `SO-1a` was confirmed free at 2026-08-27T17:0xZ: `grep -rn 'SO-1\|SO1_\|curriculum_SO1'` over `cases/`, `docs/`, `verification/` and `scripts/` returns exactly ONE hit, `docs/LAB_STATE.md:1274`, which is the chief's board entry naming the ladder Sanaa ordered — no directory, no queue entry, no record. `ls cases/dafoam/ladder-a/A1/` carried no `curriculum_SO1a`. The item is filed under `ladder-a/A1/` because it IS A1's NACA0012 mesh, alongside `curriculum_D1`, `_D13`, `_D15`, `_D16`, `_AV1`, `_AV2`; physical locality beats ladder taxonomy and the SO ladder's later 3-D and internal cases will file under their own geometries.

---

## 0. SCOPE, AND WHAT IS ACTUALLY NEW HERE

**Capability-grid cell (`docs/capability/dafoam_GRID.md`): `2D · steady · incompressible`, column "gradients computed + FD-verified".** Regime derived from the case's own registered conditions, not guessed: `so1a_runScript.py:34-40` (`U0 = 10.0`, `p0 = 0.0`, `nuTilda0 = 4.5e-5`, `CL_target = 0.5`, `aoa0 = 5.13918623195176`), `solverName = "DASimpleFoam"`, Spalart–Allmaras, 2D by the `symmetryPlanes` at z = 0 and z = 0.1. **M ≈ 0.03 — incompressible, which is the low-speed end of "subsonic" in Sanaa's phrasing and is the tutorial DAFoam ships as the drag-min-at-fixed-lift problem itself.** The transonic half of her SO-1 sentence ("then RAE2822 transonic") is a later item.

**This item runs NO optimiser.** The "optimization converged" column is untouched.

**The A1 incompressible cell already carries a CD-gradient reading, so state precisely what is new.** `ladder-a/A1/reverify_patched_idwarp_np1/RESULTS.md` (@ `be35dcad`) carries the SHIPPED row at **11.43 %** with `idx6` sign-flipped, and D13 (@ `15767999`) the PATCHED row PASS. Those are readings of **`dCD/dx` alone**. **SO-1 is drag-min at FIXED LIFT: `CL` is an EQUALITY constraint (`so1a_runScript.py:174`, `add_constraint("scenario1.aero_post.CL", equals=CL_target)`), so `dCL/dx` is what carries the constraint into the optimiser, and NO A1 record has ever FD-verified it.** A gradient rung that verifies only the objective cannot support a constrained optimisation. Three things are therefore new in this item and are the reason it exists:

1. **`dCL/dx` is graded as a first-class gate (`G5c`)**, on the same registered components and the same bands as `dCD/dx`, on both rows. It costs nothing extra: both come from the same primals.
2. **The `DAFOAM_CHARTER.md` §4 trivial baseline is BOUGHT AND GRADED (`G-TB`)** — the same probe at a deliberately wrong step. D15, D16 and D17 named no trivial baseline at all. §4's rule is explicit: *"If the wrong step also passes, the gate is not measuring what it claims and the verdict it produced is withdrawn."*
3. **np = 1 on every arm** (`DAFOAM_CHARTER.md` §5, serial before parallel), which removes A4's measured 16,600× decomposition effect from the chain rather than assuming it absent — and which is exactly the configuration in which `writeCompression on` produced the defect that killed AV-1 and AV-2 today.

**The mesh is A1's, 4,032 cells.** `genAirFoilMesh.py`, `profiles/NACA0012{PS,SS}.profile` and `FFD/wingFFD.xyz` are **byte-identical** between the `incompressible` and `subsonic` tutorials (`cmp` silent, 2026-08-27, four of four), so the MESH arm regenerates the mesh D15/D16/D13/AV-1 ran. `preProcessing.sh` and `runScript.py` are NOT identical and their own md5s are frozen in §7. The FFD box is 5×2×2 → **8 `shape` functions + `patchV` [|U|, aoa]** (`so1a_runScript.py:139-148`).

## 1. THE AMENDMENT CONDITION, AND HOW IT WAS CHECKED

> **The run root `/home/ubuntu/certonomous-runs/CURRICULUM-SO1a-a1-naca0012-dragmin-gradient` DOES NOT EXIST.**

`test -e` → **false** at **2026-08-27T17:03:56Z** (this lane, before any file in this directory was written), again as a leg of `so1a_groot5_selftest.sh` at **17:19:26Z** (*"run root ABSENT before the test"*), again at **17:19:28Z** after the selftest (*"run root ABSENT after the test (freeze condition)"*, `so1a_groot5_selftest_evidence.txt`), and again at **17:20:52Z**. The chain driver creates it on the first fire (§5).

**This item has burned 0 core-min of solver compute and started no arm container.** The only containers this lane started were the selftest's sacrificial `sleep`, pinned to `--cpus=0.1 --cpuset-cpus=9 --memory=64m`, removed with `docker rm -f` in the same script; no solver, no mesh. **After the first arm container, gates are CLOSED**; changes land only as dated addenda that cannot alter a gate, threshold, cap or label (`VERIFICATION_CHARTER.md` §2b).

## 2. ARMS — five, in this order, one detached chain, TWO ROWS, np = 1 THROUGHOUT

| arm | kind (G1) | image (row) | ranks | task | work dir | artefact |
|---|---|---|---|---|---|---|
| **MESH** | SCRIPT | SHIPPED | 1 | the tutorial's own `preProcessing.sh` (pyHyp `genAirFoilMesh.py` → `plot3dToFoam` → `autoPatch 30` → `createPatch` → `renumberMesh`, then `cp -r 0.orig 0`) + `checkMesh` | `MESH/` (cold copy of `base/`) | `MESH/checkMesh.log` |
| **X-S** | SOLVER | SHIPPED | 1 | `so1a_xf.py -mode X`: one primal, then `compute_totals(of=[CD, CL], wrt=[shape, patchV])` at the tutorial's baseline design | `X-S/` (cold copy of `MESH/`) | `X-S/so1a_X.json` |
| **F-S** | SOLVER | SHIPPED | 1 | `so1a_xf.py -mode F`: two baseline primals (η), central FD of CD **and** CL at the registered steps, the **trivial-baseline probe at the wrong step**, and the CTRL control row | `F-S/` | `F-S/so1a_F.json` (+ `.jsonl`) |
| **X-P** | SOLVER | PATCHED | 1 | as X-S | `X-P/` | `X-P/so1a_X.json` |
| **F-P** | SOLVER | PATCHED | 1 | as F-S | `F-P/` | `F-P/so1a_F.json` |

Chain: `so1a_chain_driver.sh MESH X-S F-S X-P F-P`, stops at the first non-zero rc, then runs the frozen grader on whatever exists (its rc is INFRASTRUCTURE, never the verdict). **The two-row rule (`DAFOAM_CHARTER.md` §1, §6, §10; D7FR §8): the DAFoam verdict is two rows or it is not a verdict about DAFoam** — X and F are bought on BOTH images, on ONE mesh generated once in the SHIPPED image (mesh generation does not touch IDWarp; D14-M @ `60cfd4c8`). The launcher's **G-ROW** derives the row from the arm suffix and refuses an image of the other row (DRIVEN, §7). Arm commands, verbatim from the launcher's `case`: `mpirun --allow-run-as-root -np 1 --bind-to core --report-bindings -x PYTHONPATH python so1a_xf.py -mode X|F`; each is written to `<arm>/so1a_cmd.sh` and executed **inside the container under `timeout -k 60 <cap wall>`**.

**The FD design, registered.** Components **`shape[0]`, `shape[3]`, `shape[6]`, `shape[7]`, `patchV[1]`** — the same five D15/D16/D17 registered, so the readings are directly comparable across the four regimes; `shape[6]` is the LE function, the A1 `idx6`-class component the shipped row sign-flipped at 11.43 %; `patchV[1]` is the angle of attack, which is the DV the constraint actually trades against. Plus the planted-zero control component **`CTRL`**. Step set **`shape`: {1e-2, 1e-3, 1e-4}**, **`patchV[1]`: {1e-1, 1e-2, 1e-3}** degrees; central differences, both signs → 2 baseline + 5 × 3 × 2 = **32 primals**, plus the trivial baseline below → **42 primals** per F arm. The FD reference is the **middle step**; the plateau proof is that it agrees with at least one neighbour to 10 % (§3).

**Why this step set and not another, by citation rather than by re-derivation.** `cases/dafoam/ladder-a/A_stepsize_study.md` measured twelve invocations on THIS case (A1, 4,032 cells, one step varying and nothing else) and found the curve **dead flat at 2.5–3.0 % from 1e-4 to 3e-2, cosine 0.99998**, once the three unstable components are excluded — and the primal **FAILS** at 5e-2 and 1e-1. The registered triple {1e-2, 1e-3, 1e-4} lies wholly inside that measured plateau and wholly below the primal-failure region. `DAFOAM_CHARTER.md` §3 additionally requires the sweep at the tolerance the graded run uses: `primalMinResTol 1.0e-8` (`so1a_runScript.py:45`) is the tutorial's own and is what both the X and the F arms run at, so the S1-CBFS failure mode (a sweep at a loose primal tolerance defending nothing) is excluded by construction.

**The planted-zero control component (rule 3), in the instrument AND the grader.** `CTRL` is a synthetic row with `CD_plus == CD_minus == CD_baseline` (derivative **exactly 0.0**, no solve) and a PLANTED row with `CD_plus = CD_baseline + 1.234e-03` (derivative **exactly PLANT/(2·1e-3) = 0.617**); the instrument writes both to the JSONL, **re-reads them from disk**, and exits 2 if the read-back does not see them. The grader re-checks the same two values and additionally writes a copy of each F table with PLANT added to every physical derivative into `<root>/grader_controls/`, re-reads it through the same reader, and refuses unless every value moved by exactly PLANT. **A zero from a reader not shown able to see a non-zero is not evidence.**

## 3. GATES, THRESHOLDS AND LABELS — `so1a_grade.py`

All bands are frozen now. Vocabulary: `PASS / GATE REACHED / GATE FAIL / NOT A RESULT / BLOCKED / PENDING` and nothing else.

* **G1 — completion, ARM-KIND AWARE and R-RC AWARE. THE FIVE RULE-4 CLAUSES ARE PRINTED INDIVIDUALLY, PER ARM.**
  * **C1 rc VALUE** — physics. The kernel's exit status from `docker inspect .State.ExitCode`; a harness/kernel disagreement REFUSES; non-zero REFUSES.
  * **C2 terminal marker** — this item's `End` line: `SO1A_X_WRITTEN` / `SO1A_F_WRITTEN` in the arm log for SOLVER arms, `Mesh OK.` in `checkMesh.log` for the SCRIPT arm.
  * **C3 artefact present** — this item's *fields present*: the arm's registered artefact exists.
  * **C4 age guard** — the artefact strictly newer than the arm's own `.so1a_age_datum`.
  * **C5 no fatal token** — `FOAM FATAL ERROR`, `FOAM FATAL IO ERROR`, `Segmentation fault`, `SIGSEGV`, `SIGKILL`, `MPI_ABORT`, `signal 9`, `Signal 11`, `Floating point exception`. **REFUSES REGARDLESS OF rc.**
  * **R-RC, as Sanaa approved it today (directives §0): the rc VALUE is physics, the rc RECORD is infrastructure.** When the rc RECORD is absent from BOTH channels (the ledger row's `inspect(exit,oomkilled)` field and the surviving `<ARM>_<stamp>.inspect.txt`), C1 reads **`NOT MEASURED` — and only when C2, C3, C4 and C5 all hold**, each shown individually in the artefact. The implied `rc = 0` is written as an explicit **INFERENCE** with its basis named (`rc_inferences` in the JSON) and is **never graded as a measurement**. If any of C2–C5 fails, the arm REFUSES. A rc RECORD that is absent while the harness `rc` field reads NON-ZERO also REFUSES: **R-RC relaxes a missing record, never a positive reading of failure.**
  * Any clause failing on any arm → **`NOT A RESULT`** (comparator exit 2).
* **THE AGE-GUARD DATUM IS RESOLVED BY EXISTENCE, NEVER BY NAME — the AV-1/AV-2 lesson, applied rather than renamed.** AV-1 (`0b3ebaa4`) and AV-2 (`3e2cbf74`) both returned `NOT A RESULT` on 2026-08-27 with **every physics artefact intact and every arm rc = 0**, because their frozen graders pinned `DATUM_REF = "0/U"` while this tutorial's `writeCompression on` (`system/controlDict:27`, md5 `46bb883cfc235d12df020bafbe3a9e78`) rewrites that file as `0/U.gz` on a **serial** arm. AV-1's own measurement: X1-S datum `1787838035`, `0/U.gz` mtime `1787838086` — **the guard's substance was satisfied, 51 s newer; only the reference PATH had vanished.** So here: both names are candidates (`0/U`, `0/U.gz`; `0.orig/U`, `0.orig/U.gz` for MESH), the one FOUND is **recorded**, `writeCompression` is **read from the arm's own `system/controlDict`** and recorded beside it, and the mtime rule follows the file found — the uncompressed name must still carry the recorded datum exactly (the launcher touched it), the compressed twin may only be NEWER (the solver wrote it after the datum was taken). **REFUSAL only when NEITHER name exists**, and that refusal is **DRIVEN** (grader `U24`; launcher legs `f1`/`f2`/`f3`). The launcher performs the same resolution and writes the name it used to `.so1a_age_datum_ref`, read back here as an informational cross-check.
* **L-342 field classes (`d4d0c29d`), as amended by R-RC.** `FIELDS_PHYSICS` = rc_value, oomkilled, terminal_statement, age_guard, no_fatal_token, core_min, cap_core_min, DIGEST, cpuset. `FIELDS_INFRASTRUCTURE` = **rc_record**, memavail_pre/post, delivered, siblings_pre/post, log, wall_s. **Absent infrastructure → `NOT_MEASURED`, named beside the verdict, never composed to PASS. Present-but-garbage → REFUSE. Absent physics → REFUSE.**
* **G-M2 — mesh identity:** `cells == 4,032` → `PASS`, else `GATE FAIL` (a different mesh is a different item; P1).
* **G5 — the bright line, per ROW, on `CD` (the objective); G5c the SAME on `CL` (the equality constraint).** Bands **by citation, not re-derived**: **band D = per-component `|d_FD − J_adj| / |d_FD| ≤ 5.0 %` with the same sign** and **band E = aggregate vector-relative error over graded components ≤ 5.0 %** — `cases/dafoam/ladder-a/A2/curriculum_D4/PREREGISTRATION.md:82` (*"endpoint FD agreement, per graded component and in aggregate — ≤ 5.0 %, and zero sign flips among graded components"*) and `cases/dafoam/ladder-a/A3/curriculum_D7FR/PREREGISTRATION.md:228-229` (*"band D, 5.0 % per component and 5.0 % aggregate, inherited unchanged and not re-derived by a lane that has seen an answer"*). **The sign-flip rule:** a component whose FD reference and adjoint differ in sign is **`GATE FAIL` whatever its magnitude**. **Plateau:** the middle step must agree with at least one neighbour to **10 %**, else the component is `NOT A RESULT`; `|d_mid| < 1e-14` is `NEAR_ZERO` → `NOT A RESULT`; a failed FD evaluation → `NOT A RESULT`. **Fewer than 3 graded components → the row is `NOT A RESULT`.** **Row verdict: `PASS` iff G5 and G5c are BOTH `PASS`.**
* **G-TB — THE `DAFOAM_CHARTER.md` §4 TRIVIAL BASELINE, REGISTERED BEFORE ITS OWN RUN.** The same probe at a **deliberately wrong step**: **h = 1.0e-8 for `shape`, 1.0e-6 for `patchV`** — five orders below the registered middle step, in the subtractive-cancellation regime where the FD numerator on a derivative of order 1e-2 is ~1e-10, **at or below the primal repeatability η** (D15 measured `eta_F` = 1.30e-10 on this mesh, `curriculum_D15/RESULTS.md` §4). **G-TB `PASS` iff AT MOST 1 of the 5 registered components passes band D at the wrong step. If 2 or more pass, the gate cannot fail, it is not evidence, and that row's G5 verdict is WITHDRAWN to `NOT A RESULT`** — the standing-rule-5 direction: a gate may turn a PASS or a GATE FAIL INTO `NOT A RESULT`, never the reverse. A probe that ERRORED counts as FAILING the baseline; an unevaluable estimate is not agreement. **The opposite-direction baseline (a step an order ABOVE the largest registered one, h = 5e-2 or 1e-1) is deliberately NOT bought:** `A_stepsize_study.md` measured the primal FAILING at both, so that probe would return an exception rather than a reading, and an errored probe is a weaker baseline than a noise-limited one.
* **G6 — dot-product / duality test: NOT MEASURED.** The tutorial and `dafoam.mphys` expose no dot-product test; nothing here computes one, and the grader prints that sentence beside the verdict rather than a value. Complex step likewise not performed. **`DAFOAM_CHARTER.md` §2 second clause requires this to be SAID:** a forward-AD build (`libDASolverADF.so`) ships in these images and this record states that it did not reach for it — because AV-2 measured, today, that seeding forward mode makes the primal FAIL on this exact case on BOTH images (`curriculum_AV2/RESULTS.md` §2: 5/5 rows `AnalysisError(... Primal solution failed!)`, `control_fail false`). That is the reason, and it is a measurement, not a preference.
* **G9 — toolchain per row:** the ledger `DIGEST`, the container's `D4S_IDWARP_SO_MD5:` print and the artefact's in-process `libidwarp.so` md5 must all name the row's registered toolchain — SHIPPED `f0fcb488e0e98156575cd19548e91663` @ `sha256:9d45679d…f07fc`, PATCHED `85f59e87253e0a71a813f64ca6e4c425` @ `sha256:2927768a…30f6d35`. Mismatch → `GATE FAIL`.
* **G10 — caps (§4):** every row `core_min ≤ cap`, sum ≤ **75.0**; a crossing is `GATE FAIL` exactly as this text says (report mode; the in-container deadline is the stop).
* **G11 — OOM hard:** the container cgroup (`--memory=4g --memory-swap=4g --oom-score-adj=500`) kills; `OOMKilled true` is a G1 refusal, never a re-fire.
* **G12 — placement:** `cpuset == 9` on every arm. **At np = 1 the delivered-cores floor does not apply** and is not composed (`so1a_grade.py: g_placement`, `dl_ok` is `True` when `ARM_RANKS[arm] == 1`); the sampler's reading is reported as a number and `NOT_MEASURED` where absent.
* **DIVERGENCE** shipped-vs-patched on the adjoint, per component, is **reported with its number**, never gated.
* **Item verdict (composition registered here):** any refusal or any row `NOT A RESULT` (including a G-TB withdrawal) → **`NOT A RESULT`**; else any of G-M2 / G9 / G10 / G12 or any row `GATE FAIL` → **`GATE FAIL`**; else **`PASS`**. No grid family exists, so standing rule 5 has no row and **NO GCI IS QUOTED**. `NOT_MEASURED` fields are printed beside the verdict, never inside it.

**§2a identity test, answered for the two gates that carry this item.**
*G5c (`dCL/dx`): what would make it FAIL?* A registered component whose FD estimate of `dCL/dx` differs from the adjoint by more than 5 % or in sign. *Could a wrong treatment still PASS it?* Yes, in one way we can name: a defect that scales BOTH the adjoint and the FD estimate identically — e.g. a wrong `scale` in the `CL` function definition — cancels in the ratio. That is why `CL_baseline` is separately banded (P2) against thin-airfoil theory and against the A1 record's measured 0.499; a mis-scaled `CL` moves the baseline out of band even when the ratio agrees.
*G-TB: what would make it FAIL?* Two or more components agreeing to 5 % at h = 1e-8. *Could a wrong treatment still pass it?* Only one we can name: an instrument that never actually perturbs at the TB step and returns the registered-step value instead. The instrument writes `tb` from its own `set_perturbed` calls in the same loop as `fd` and both are re-read from the JSONL; the emitted `tb_step` records carry the step, so a substituted value would show the wrong step on disk.

## 4. COST — DERIVED FROM **MEASURED** ANCHORS ON THIS EXACT CASE

**The family has been systematically OVER-pricing small 2-rank cases by roughly 2.3×** and this item prices from the ACTUALS, not from the sibling estimates. The three ratios measured today: D15 **0.357** (28.3 predicted, 10.100 measured, `docs/COST_CALIBRATION.md` C-154), D16 **0.395** (40.3 → 15.935, C-156), AV-1 **0.472** (19.3 → 9.102, C-158). Every anchor below is a MEASURED core-minute figure from a ledger, not an estimate.

| anchor | what it measures | value |
|---|---|---|
| **C-154 / C-156 / C-158** | the MESH arm on THIS 4,032-cell pyHyp mesh at 1 rank — **three independent items agree exactly** | **0.167 core-min MEASURED** |
| **C-158**, AV-1 `X1-S` | **the SAME arm this item's X arms run**: the incompressible A1 tutorial, np = 1, `compute_totals(of=[CD, CL], wrt=["shape","patchV"])`, SHIPPED image (`curriculum_AV1/av1_x.py:161`) | **1.017 core-min MEASURED** |
| **C-154**, D15 `F-S` / `X-S` | 32 COMPRESSIBLE primals + setup vs 1 primal + 2 adjoints + colouring + setup, np = 2 | **3.433** and **1.367 core-min MEASURED** |

**The F-arm derivation, stated so it can be attacked.** D15's `F-S − X-S = 3.433 − 1.367 = 2.066` core-min = 62 wall s at np = 2 buys **at least** the 31 primals by which F exceeds X, so a warm **compressible** primal on this mesh costs **≤ 2.0 wall s at np = 2**. Two adjustments, both cited: incompressible over compressible, **×0.5** (D15 §4 adopted ×2 in the other direction for `DARhoSimpleFoam`'s energy equation and density-coupled pressure, and the measurement was consistent with it); np = 1 over np = 2 on 4,032 cells, **×1.5** in wall (AV-1 measured 61 vs 41 wall s for the same X work). Net **≈ 1.5 wall s per warm incompressible primal at np = 1**. **42 primals ≈ 63 wall s**, plus setup bounded above by AV-1's whole X arm (61 wall s), ≈ **124 wall s = 2.07 core-min**. Point **2.5**, carrying margin.

| arm | derivation | point (core-min) | cap (core-min) | in-container wall | mem |
|---|---|---|---|---|---|
| MESH | C-154/C-156/C-158, all three read 0.167 | **0.167** | 5.0 | 300 s (1 rank) | 4g |
| X-S, X-P | C-158 AV-1 `X1-S` **1.017 measured, like for like** + margin | **1.1** each | 10.0 each | 600 s (1 rank) | 4g |
| F-S, F-P | derived above from C-154 and C-158 | **2.5** each | 25.0 each | 1500 s (1 rank) | 4g |
| grader | zero compute | 0 | — | — | — |
| **total** | | **7.4** point, band **[4.0, 22.0]** | **ceiling 75.0 = Σ caps** | ≈ 8 min wall at the point, ≈ 75 min at the caps (+ five 60-s H5 windows) | |

**`cost_basis`: c7a.4xlarge at $0.0513/core-h, REPORTED-BY-OWNER, NOT MEASURED** — the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` §5). Dollars **DERIVED, not measured**: point **$0.00633**, ceiling **$0.0641**. **Cap mode:** each arm's wall deadline is **inside the container** (`timeout -k 60` at cap × 60 / ranks s; the launcher asserts the enforced wall equals the registered cap to 0.02 core-min and aborts otherwise); the host poller reports a crossing and hard-stops at 4× cap while it lives. **An overrun stops the run; it does not get a new budget** (rule 12). A calibration row in `docs/COST_CALIBRATION.md` is owed at completion (rule 12, Sanaa's estimate-versus-actual directive).

**REGISTERED COST EXPOSURE, disclosed rather than absorbed.** cpuset **9** overlaps D4-SHIPPED's registered `5,6,7,9`. At np = 1 that cannot fail G12 (the delivered-cores floor does not apply) but it CAN inflate wall time and therefore core-minutes. If D4-SHIPPED fires concurrently the arms will land above the point estimate; the band `[4.0, 22.0]` is set wide enough to absorb it and P8 grades the band, not the point. Contention is named separately in the calibration row and never folded into the misprediction ratio (`COMPUTE_BUDGET_CHARTER.md` §6).

## 5. PLACEMENT, MEMORY AND THE DETACHED FORM

* **cpuset `9`** (`--cpuset-cpus=9`, `--cpus=1`), ONE core because every arm is np = 1. **Disclosed overlap:** 9 sits inside D4-SHIPPED's registered `5,6,7,9`. Reported here as a scheduling fact, not silently re-pinned (UPDATE 7: placement is configuration). Not core 0. At the time of freeze the only live container on this box was `d6_O_mp_…` on cpuset `2,3,4,14` — disjoint.
* **Memory:** `--memory=4g --memory-swap=4g` every arm (D13 measured peak RSS 1.70 GiB on this mesh at np = 1; 4g is the family's cap); **H5 windowed gate 45 samples / 60 s, refuse on ANY sample < 8.0 GiB** (4g + 4 GiB headroom); **aggregate** (live caps + 4 + host non-container RSS) **< 30.6 GiB** in the **wait-and-retry** form (poll 30 s, bound 4 h, every wait a line in `STATUS.<arm>`, refuse-and-BLOCK at the bound with the series named).
* **The detached form.** The queue runner launches `bash so1a_chain_driver.sh MESH X-S F-S X-P F-P` under its own `setsid nohup`; the driver writes its pid to `<run root>/so1a_driver.pid`, opens `STATUS.<arm>` at preflight and appends every wait and the final `rc=<n> … source=launcher_exit=docker_inspect_ExitCode` line inside the detached session — **never the `$?` of a `setsid`/`timeout` line**. `STATUS.chain` carries the chain state and, at the end, `grader_rc=<n> … note=comparator-exit-status-NOT-the-verdict`. Container `rc` is read from `docker inspect` before `docker rm`; **no `--rm`**; the launcher also writes `<ARM>_<stamp>.inspect.txt` as the surviving kernel record.
* **G-ROOT.1–.5 from birth, DRIVEN.** `BASE` must equal this item's root through `realpath -m` (G-ROOT.1); the forbidden list names D4's, D4-SHIPPED's, D4-SHIPPED-R's, D5's, D6's, D14's, D14M's, D7R's, D7FR's, D12R's, D12R2's, D12R2W2R's, D13's, **D15's, D16's, D17's, AV-1's, AV-2's**, A2-mach-wing's roots, the tutorial checkout, `certonomous-runs` and the repository (G-ROOT.2); the ledger refuses a foreign `ITEM=` (G-ROOT.3 — a `ROW=SHIPPED` row is NOT foreign here, this item buys both rows); an `rc=0` row refuses a re-fire (G-ROOT.4 `ALREADY_BOUGHT`); **G-ROOT.5** refuses a RUNNING container carrying `so1a_<ARM>_` or a driver pidfile naming a live pid that is not an ancestor or whose cwd is the run root. **DEMONSTRATED 2026-08-27T17:19:26–28Z, 25 of 25** (`so1a_groot5_selftest_evidence.txt`): sacrificial container `so1a_X-S_selftest_20260827T171926Z` → `rc=3`; sacrificial live pid `1060497` with cwd = run root → `rc=3`; stale pidfile does not block; clear passes and the launcher stops at the L-251 mode check before any staging; six forbidden roots refuse at G-ROOT.1; G-ROW refuses `X-P` on the shipped image and `MESH` on the patched image (`rc=4`) and passes `X-S`, which then refuses (`rc=5`) for want of `MESH/` — nothing staged, no arm directory created; **the NEW leg (f) drives the age-datum-by-existence resolution in all three states including the genuinely-absent one**; the launcher's own by-existence branch and its `.so1a_age_datum_ref` write are both asserted present; zero backticks on executable lines; the temporary root empty and **absent afterwards**.
* **Run root staging** (first fire only): creates the root (mode 777, L-251), copies the INCOMPRESSIBLE tutorial's `0.orig FFD constant system profiles genAirFoilMesh.py preProcessing.sh` into `base/` (never the checkout's `polyMesh`, none exists), overlays the registered `so1a_decomposeParDict` (`numberOfSubdomains 1`, scotch — the only edit to a tutorial file), copies the two instruments, writes `ITEM=SO1a` alone on ledger line 1 and a `STAGED …` line, and md5-asserts every staged file — **including the six tutorial inputs** — before any container starts. A second fire stages nothing.

## 6. PREDICTIONS — scored HIT/MISS by the comparator, never adjusted

| # | prediction | value / band | falsifier |
|---|---|---|---|
| **P1** | the MESH arm reproduces A1's mesh | **cells == 4,032** exactly | any other count → MISS and G-M2 `GATE FAIL`; a different mesh is a different item |
| **P2** | baseline `CL` at the tutorial's `aoa0 = 5.139°` | **[0.45, 0.55]**, point 0.50 (the tutorial's own `CL_target`; the A1 record reads 0.499) | outside the band → the `CL` function is not the one the tutorial registered |
| **P3** | baseline `CD` | **[0.015, 0.028]**, point 0.0209 (A1's measured coarse-mesh baseline at CL 0.499) | outside → the primal is not the A1 primal |
| **P4** | PATCHED row G5 (`CD`) is `PASS` with **5 of 5** components inside band D | worst ≤ 1 % (D13's 0.26 % on this twin) | fewer than 5 → the patched gradient is worse on this case than on its own prior reading |
| **P5** | the SHIPPED row's `shape[6]` (the LE function, the A1 `idx6` class) is **outside band D or sign-flipped** on `CD`, hence SHIPPED row `GATE FAIL` | HIT if `GATE FAIL`; `NOT_MEASURED` if `NOT A RESULT` | a MISS contradicts the A1 record's own 11.43 % reading on this very case, and THAT is the finding |
| **P6** | **THE POINT OF THIS RUNG.** the PATCHED row's CONSTRAINT gradient `dCL/dx` is `PASS` with **5 of 5** inside band D **and aggregate ≤ 1.0 %** — as good as the objective's | scored from the aggregate NUMBER, not from the component verdicts | **a MISS means SO-1b cannot be registered as a constrained optimisation on this gradient and the ladder stops at SO-1a. That is the finding, not a defect of this item.** |
| **P7** | the trivial baseline at h = 1e-8 **FAILS band D on ≥ 4 of 5** components on the PATCHED row | G-TB `PASS` | a MISS means the FD gate is not measuring the step, G-TB `GATE FAIL`, and the PATCHED row's G5 verdict is **WITHDRAWN to `NOT A RESULT`** |
| **P8** | total graded core-min in **[4.0, 22.0]** (point 7.4) | scored | a MISS is a cost miss, carried into the calibration row; the band is not adjusted |
| **P8b** | MESH wall ≤ 120 s | scored | — |
| **P9** | the age-guard datum resolves to the **COMPRESSED twin `0/U.gz`** on EVERY solver arm — the AV-1/AV-2 defect reproduced at np = 1 with `writeCompression on`, and survived | HIT if all four solver arms | a MISS means AV-1's diagnosis was incomplete: `writeCompression on` did NOT rewrite time-0 here, and the two items died for some other reason. That would be a finding worth more than this rung. |

**The registered outcome, written before any container starts:** P1, P2, P3, P4, P6, P7, P8, P8b, P9 **HIT**; P5 **HIT** → SHIPPED row **`GATE FAIL`**, PATCHED row **`PASS`**, item **`GATE FAIL`**, and the `2D · steady · incompressible` gradient column gains the CONSTRAINT gradient it did not have.

## 7. INSTRUMENTS, FROZEN BY MD5 AT THIS COMMIT — copy-and-delta from D15

| file | md5 | derivation, and the DELTAS file the supervisor reads |
|---|---|---|
| `so1a_run_arm.sh` | `dbe6e2b6ffd2f6630b7b40d91bfff61e` | `curriculum_D15/d15_run_arm.sh` (`796a2de5…`) + **`so1a_run_arm_DELTAS_from_d15.diff` (260 diff lines)**: item/root; the incompressible tutorial; five more forbidden roots; RANKS 1 on every arm; cpuset 9; the cap table; staged instrument md5s; **the age-guard datum resolved by EXISTENCE and its name written to `.so1a_age_datum_ref`**; the arm commands. `D4S_*` container-printed strings inherited UNCHANGED so the grader greps what the launcher writes |
| `so1a_chain_driver.sh` | `48d358fae15b37031ef1e69ab92afbe4` | `curriculum_D15/d15_chain_driver.sh` (`4656f1a0…`) + **`so1a_chain_driver_DELTAS_from_d15.diff` (154 lines)**: names/root/md5s; `TUT_SRC` = the incompressible tutorial with its six input md5s re-frozen |
| `so1a_xf.py` | `f34bd5bf550786ff926aed81b342cced` | `curriculum_D15/d15_xf.py` (`8a664a97…`) + **`so1a_xf_DELTAS_from_d15_xf.diff` (273 lines)**: producer/md5; **the trivial-baseline probe `TB_STEPS` written under `tb` beside `fd`**; **`writeCompression` read from the case's own controlDict into the artefact**; `CL_target` in the identity record |
| `so1a_grade.py` | `6966d19eeccd275b45fe9a5492eef6d2` | **the grading path.** `curriculum_D15/d15_grade.py` (`b429ec89…`) + **`so1a_grade_DELTAS_from_d15_grade.diff` (977 lines; read as a file)**. **35/35 under `python3` AND `python3 -O`** (`so1a_grade_selftest_evidence.txt`), including the five NEW driven controls: **U24** datum absent in BOTH names → REFUSAL; **U26** rc RECORD absent while C2–C5 hold → `NOT MEASURED` + inference printed, verdict unchanged; **U27** rc RECORD absent AND C2 fails → REFUSAL; **U28** rc RECORD absent but harness rc non-zero → REFUSAL; **U29/U30** fatal and signal tokens at rc = 0 → REFUSAL; **U31** the wrong step agrees 5/5 → G-TB `GATE FAIL`, row WITHDRAWN to `NOT A RESULT`; **U33** the CONSTRAINT gradient fails alone while the objective is clean; **U23/U25** the datum resolves to the compressed twin / to the plain name with P9 scored either way; **U35** the five rule-4 clauses present per arm. `ast.Assert` = 0 in grader and instrument, with the counter shown counting a planted one |
| `so1a_groot5_selftest.sh` | `b8bee4d073983090052ec39b49adfe37` | `curriculum_D15/d15_groot5_selftest.sh` (`8a2a7df6…`) + **`so1a_groot5_selftest_DELTAS_from_d15.diff` (107 lines)**: names; sacrificial cpuset 9; six forbidden roots; **the NEW leg (f), five checks, driving the datum-by-existence resolution including the absent case** |
| `so1a_aggregate_memory.py` | `709ab0b98ef0302a3a3a318588f9493f` | **byte-identical** to `d15_aggregate_memory.py` (`cmp` silent) |
| `so1a_runScript.py` | `0557da51f6f179f6de865144343c499f` | **byte copy** of `/home/ubuntu/dafoam-tutorials/NACA0012_Airfoil/incompressible/runScript.py` — the producer whose header the instrument execs; never modified |
| `so1a_decomposeParDict` | `e6f1b0060944bc86d6dff56480ad2bd4` | the tutorial's `system/decomposeParDict` (`816f5ba44075fde47fa5db4269877bc8`) with `numberOfSubdomains 4 → 1`, ONE line |
| tutorial inputs, md5-asserted at staging | `genAirFoilMesh.py` `681f10659eb90457fca13fc933008b93`; `preProcessing.sh` `4a9395452540705686acf94898aa33af`; `NACA0012PS.profile` `51dfed28e1bdb4cd33e0d8d7dabd586a`; `NACA0012SS.profile` `4a6b8ef4501494c7693b71e88a2eabbf`; `FFD/wingFFD.xyz` `6ddf378b028d03d8a18270488bee1759`; `system/controlDict` `46bb883cfc235d12df020bafbe3a9e78` | checkout `d3b7e38b058aba2a98a74092e15c41ec455c570d`, which is not frozen by this repository, so its bytes are frozen here. Four of them (`genAirFoilMesh.py`, both profiles, the FFD box) are byte-identical to the subsonic tutorial's — `cmp` silent, 2026-08-27 — which is why this item runs A1's mesh; `preProcessing.sh` and `runScript.py` are NOT, and their own md5s are above |

**The md5 agreement control ran and is part of the freeze:** the chain driver's five frozen values and the launcher's three were each compared against the file on disk at 17:21Z — **eight of eight AGREE**. **No `assert` carries a guard** in any python file here (AST count 0 on both, counted by the grader, with a planted assert shown counted; L-332). **Classifier denials in this lane while building SO-1a: none.**

## 8. WHAT THIS ITEM WILL NOT ESTABLISH

Nothing about an optimum — no optimiser runs, and the `2D · steady · incompressible` "optimization converged" column is untouched by this item. **Nothing at np ≠ 1**: `DAFOAM_CHARTER.md` §5 is explicit that *"a gradient verified at one np is a statement about that np and is never carried to another"*, so **SO-1b must either run at np = 1 or buy its own gradient verification at its np** — that consequence is registered here and is not a later discovery. Nothing about the other four `shape` functions or `patchV[0]`. No dot-product test and no complex step (§3 G6, with AV-2's measurement as the reason). No grid family, no GCI. Nothing about a mesh other than A1's 4,032 cells. Nothing about the thickness, volume or LE-radius constraints the tutorial also declares — those are geometric constraints evaluated by pyGeo without a flow solve, they belong to SO-2 ("constraint families"), and this rung's `compute_totals` does not request them. A shipped-vs-patched divergence of 0.000 % on some component would be reported with its number and would NOT be read as "the defect is absent"; P5 names the component where it is expected.

## 9. FREEZE AND QUEUE

**Committed BEFORE any container starts** (rule 2). The grading path is fixed at this commit: `so1a_grade.py` md5 `6966d19eeccd275b45fe9a5492eef6d2`, asserted by the chain driver before staging and again before the grade. **Queue entry `verification/queue/dafoam/SO1a_chain.json`:** team `dafoam`, `prereg_commit` = the sha of the commit introducing this file (full 40-hex), `launch_cmd` = `["bash", "<abs>/so1a_chain_driver.sh", "MESH", "X-S", "F-S", "X-P", "F-P"]`, `cwd` = this directory, `ranks 1`, `cost_core_min_estimate 7.4`, `cap_core_min_registered 75.0`, `memory_floor_gb 8.0`, `cost_basis` derived / not measured, `permission bc0e687e`. **Enqueueing is not authorisation**: `SUPERVISION_CHARTER.md` §3 check 4 is the supervisor's own, discharged on the sha. **Predicted outcome, so it cannot be written afterwards:** P1–P4 and P6–P9 HIT; P5 HIT → SHIPPED row `GATE FAIL`, PATCHED row `PASS`, item **`GATE FAIL`**, with the constraint gradient `dCL/dx` FD-verified for the first time on this case and SO-1b's registration unblocked at np = 1.
