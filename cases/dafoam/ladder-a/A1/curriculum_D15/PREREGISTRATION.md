# CURRICULUM D15 — NACA0012 at SUBSONIC-COMPRESSIBLE conditions (`DARhoSimpleFoam`, M ≈ 0.29): the BASELINE adjoint gradient of CD (and the CL constraint gradient) FD-VERIFIED on TWO TOOLCHAIN ROWS — PRE-REGISTRATION

**Version 1.0. FROZEN.** Dated **2026-08-26**. Lane: dafoam `lab-lane` (N). Supervisor: `dafoam-supervisor` (FOURTEENTH session §3, lane N: *"D15 (2D · steady · subsonic-compressible) and D16 (2D · steady · transonic) NACA0012 frozen on the D14/D5 launcher family — two-row X arms + FD table, G-ROOT.1–.5 driven, 4g/2 ranks on 2,3 and 4,14"*).
**Nothing in this item is filed, sent, uploaded, registered, posted or commented** (`CLAUDE.md` rule 7; `DAFOAM_CHARTER.md` §10). Permission for the detached launch: Sanaa's own words at **`bc0e687e`**; queue-first order `7def3c6b` / `73eccb1b` / `0b041d1a`; L-342 field classes `d4d0c29d`; the grid as the goal `068c2bf0`; no idle instance while a case is unrun `3c3ef86c`. Every decision here is `[lab-attributed]`.

> **ID-NAMESPACE WARNING (carried from `curriculum_D5/PREREGISTRATION.md`).** `cases/dafoam/EXPERTISE_CURRICULUM.md:126` carries a row numbered **D15** (*"Verification-at-scale institutionalization"*, a gate template that *"folds into each prereg"*, ~0 standalone core-min — it never has a directory), and `docs/DOCKET.md:139` a fleet-docs defect **D15**. **This item is the dafoam CURRICULUM CASE `D15`** — the number was confirmed free by `ls cases/dafoam/ladder-a/A1/` (no `curriculum_D15`), `cases/dafoam/INDEX.md` (no D15) and `grep -rn curriculum_D15` over the repository (none) at 2026-08-26T20:5xZ, and assigned by the supervisor's dispatch above. No document in this family cites a bare `D15` without saying which.

---

## 0. SCOPE, AND THE CAPABILITY-GRID CELL THIS ITEM CAN MOVE

**Capability-grid cell (Sanaa's taxonomy, `068c2bf0`; `docs/capability/dafoam_GRID.md` @ `6fcfe713`): `2D · steady · subsonic-compressible`, column "gradients computed + FD-verified" — today `CAN NOT DO — not attempted`.** Regime derived from the case's registered flow conditions, not guessed: `d15_runScript.py:31-36,44` (`U0 = 100.0`, `p0 = 101325`, `T0 = 300.0`, `aoa0 = 4.0`, `solverName = "DARhoSimpleFoam"`); `a = √(1.4·287·300) = 347.2 m/s` → **M∞ = 0.288**; Spalart–Allmaras (`constant/turbulenceProperties`); 2D by the `symmetryPlanes` at z = 0 and z = 0.1 (`d15_runScript.py:88-90`). **This item moves ONLY the gradient column of that cell** — it runs no optimiser; the "optimization converged" column stays `CAN NOT DO — not attempted`. What it can put in the cell: a two-row FD-vs-adjoint verdict at the tutorial's **baseline** design, the same design point at which the incompressible A1 rows split (SHIPPED `GATE FAIL` 11.43 %, idx6 sign-flipped, `ladder-a/A1/reverify_patched_idwarp_np1/RESULTS.md` @ `be35dcad`; PATCHED `PASS`, D13 @ `15767999`).

**The tutorial is NOT inside the images.** Probed at zero solver compute 2026-08-26T20:5xZ: `sudo -n docker run --rm dafoam/opt-packages:latest ls /home/dafoamuser/dafoam/tutorials` → *No such file or directory* on BOTH `dafoam/opt-packages:latest` (`sha256:9d45679d…f07fc`) and `dafoam-idwarp-rot:v1` (`sha256:2927768a…30f6d35`); the only `tutorials` directory in the image is OpenFOAM's own. The DAFoam tutorials live on the host at **`/home/ubuntu/dafoam-tutorials`** (git `d3b7e38b058aba2a98a74092e15c41ec455c570d`, 2026-05-16), which carries `NACA0012_Airfoil/{incompressible,subsonic,transonic,…}` and `Cone_Supersonic`; the grid's parenthetical *"the image ships `Cone_Supersonic`"* is therefore inexact and is reported to the supervisor, not edited here. **Inputs are staged by copy from that checkout with every file md5-asserted** (§7), because the checkout is not frozen by this repository.

**The mesh is A1's.** `genAirFoilMesh.py`, `profiles/NACA0012{PS,SS}.profile` and `FFD/wingFFD.xyz` are **byte-identical** between the `incompressible`, `subsonic` and `transonic` tutorials (`diff` silent, 2026-08-26), so the MESH arm regenerates the same **4,032-cell** mesh A1/D13 ran (`work/NACA0012_Airfoil_Incompressible/constant/polyMesh/owner` note `nCells:4032`), and the FFD box is the same 5×2×2 → **8 `shape` functions + `patchV`** (`d15_runScript.py:143-153`). Only the physics differs: compressible fields (`T`, absolute `p`), `DARhoSimpleFoam`, `U0 = 100` at `aoa0 = 4°`.

## 1. THE AMENDMENT CONDITION, AND HOW IT WAS CHECKED

> **The run root `/home/ubuntu/certonomous-runs/CURRICULUM-D15-a1-naca0012-subsonic` DOES NOT EXIST.**

`test -e` → false at **2026-08-26T20:56:01Z** (lane, before any file was written) and again as the last leg of `d15_groot5_selftest.sh` at **2026-08-26T21:06:57Z** (`d15_groot5_selftest_evidence.txt`, "run root ABSENT after the test"). The chain driver creates it on the first fire (§5). **This item has burned 0 core-min and started no arm container**; the only containers this lane ran were two `ls` probes (`--rm`, no solver) and the sacrificial `sleep` of the selftest. After the first container, gates are **CLOSED**; changes land only as dated addenda that cannot alter a gate, threshold, cap or label.

## 2. ARMS — five, in this order, one detached chain, TWO ROWS

| arm | kind (G1) | image (row) | ranks | task | work dir | artefact |
|---|---|---|---|---|---|---|
| **MESH** | SCRIPT | SHIPPED | 1 | the tutorial's own `preProcessing.sh` (pyHyp `genAirFoilMesh.py` → `plot3dToFoam` → `autoPatch 30` → `createPatch` → `renumberMesh`, then `cp -r 0.orig 0`) + `checkMesh` | `MESH/` (cold copy of `base/`) | `MESH/checkMesh.log` |
| **X-S** | SOLVER | SHIPPED | 2 | `d15_xf.py -mode X`: one primal, then `compute_totals(of=[CD, CL], wrt=[shape, patchV])` at the baseline | `X-S/` (cold copy of `MESH/`) | `X-S/d15_X.json` |
| **F-S** | SOLVER | SHIPPED | 2 | `d15_xf.py -mode F`: two baseline primals (η), then central FD of CD and CL for the registered subset at the registered steps; the CTRL control row | `F-S/` | `F-S/d15_F.json` (+ `.jsonl`) |
| **X-P** | SOLVER | PATCHED | 2 | as X-S | `X-P/` | `X-P/d15_X.json` |
| **F-P** | SOLVER | PATCHED | 2 | as F-S | `F-P/` | `F-P/d15_F.json` |

Chain: `d15_chain_driver.sh MESH X-S F-S X-P F-P`, stops at the first non-zero rc, then runs the frozen grader on whatever exists (its rc is infrastructure). **The two-row rule (`DAFOAM_CHARTER.md` §11; D7FR §8): the DAFoam verdict is two rows or it is not a verdict** — X and F are bought on BOTH images, on ONE mesh generated once in the SHIPPED image (mesh generation does not touch IDWarp; D14-M @ `60cfd4c8` showed this toolchain regenerates a pyHyp mesh bit-for-bit). The launcher's **G-ROW** derives the row from the arm suffix and refuses an image of the other row (driven, §7). Arm commands, verbatim from the launcher's `case`: `mpirun --allow-run-as-root -np 2 --bind-to core --report-bindings -x PYTHONPATH python d15_xf.py -mode X|F`; each is written to `<arm>/d15_cmd.sh` and executed **inside the container under `timeout -k 60 <cap wall>`**.

**The FD design, registered:** components **`shape[0]`, `shape[3]`, `shape[6]`, `shape[7]`, `patchV[1]`** — five physical components (two interior shape functions, the LE function `shape[6]` = the A1 idx6-class component, the TE function `shape[7]`, and the angle of attack) — **plus the planted-zero control component `CTRL`** (six rows; ≥ 5 incl. one control, as ordered). Step set **`shape`: {1e-2, 1e-3, 1e-4}** (FFD y-displacement, DV scaler 10), **`patchV[1]`: {1e-1, 1e-2, 1e-3}** degrees; central differences, both signs, so **32 primals** per F arm (2 baseline + 5 × 3 × 2). The FD reference is the **middle step**; the plateau proof is that it agrees with at least one neighbour to 10 % (§3). No optimiser endpoint is read (D4's `d4_endpoint_dvs.json` clause is removed): the design point is the producer's own default `shape = 0`, `patchV = [100, 4]`, read back from the model and written into the artefact.

**The planted-zero control component (rule 3), in the instrument AND the grader.** `CTRL` is a synthetic row with `CD_plus == CD_minus == CD_baseline` (derivative **exactly 0.0**, no solve) and a PLANTED row with `CD_plus = CD_baseline + 1.234e-03` (derivative **exactly PLANT/(2·1e-3) = 0.617**); the instrument writes both to the JSONL, **re-reads them from disk**, and exits 2 if the read-back does not see them (`d15_xf.py:239-252`). The grader re-checks the same two values and additionally writes a copy of each F table with PLANT added to every physical derivative into `<root>/grader_controls/`, re-reads it through the same reader, and refuses unless every value moved by exactly PLANT (`d15_grade.py: ctrl_control, grader_plant_control`). A zero from a reader not shown able to see a non-zero is not evidence.

## 3. GATES, THRESHOLDS AND LABELS — `d15_grade.py`

All bands are frozen now. Vocabulary: `PASS / GATE REACHED / GATE FAIL / NOT A RESULT / BLOCKED / PENDING` and nothing else.

* **G1 — completion, ARM-KIND AWARE (the D4-SHIPPED Addendum-2b form, from birth).** Every arm: kernel `rc == 0` read from `docker inspect .State.ExitCode` (the ledger's `inspect(exit,oomkilled)`, refusing on a harness/kernel disagreement), `OOMKilled false`, and the **age guard**: the registered artefact strictly newer than the arm's own `.d15_age_datum` (`0/U`, or `0.orig/U` for MESH, touched last at stage). SOLVER arms additionally need the instrument's terminal marker (`D15_X_WRITTEN` / `D15_F_WRITTEN`) in the arm log; the SCRIPT arm MESH needs `Mesh OK.` in `checkMesh.log`. Any clause failing on any arm → **`NOT A RESULT`** (comparator exit 2). An arm with no ledger row is read from the launcher's surviving `<ARM>_<stamp>.inspect.txt` (the kernel record: exit, OOMKilled, StartedAt/FinishedAt, cpuset, memory, digest) with the source named per field and every infrastructure field `NOT_MEASURED`.
* **L-342 field classes (`d4d0c29d`).** `FIELDS_PHYSICS` = rc, inspect_exit, oomkilled, terminal_statement, age_guard, core_min, cap_core_min, DIGEST, cpuset. `FIELDS_INFRASTRUCTURE` = memavail_pre/post, delivered, siblings_pre/post, log, wall_s. **Absent infrastructure → `NOT_MEASURED`, named beside the verdict, never composed to PASS. Present-but-garbage → REFUSE. Absent physics → REFUSE.**
* **G-M2 — mesh identity:** `cells == 4,032` → `PASS`, else `GATE FAIL` (a different mesh is a different item; P1).
* **G5 — the bright line, per ROW, on CD (objective); G5c the same on CL (constraint).** Bands **by citation, not re-derived**: **band D = per-component |d_FD − J_adj| / |d_FD| ≤ 5.0 % with the same sign** and **band E = aggregate vector-relative error over graded components ≤ 5.0 %** — `curriculum_D4/PREREGISTRATION.md:82` (*"endpoint FD agreement, per graded component and in aggregate — ≤ 5.0 %, and zero sign flips among graded components"*) and `curriculum_D7FR/PREREGISTRATION.md:228-229` (*"band D, 5.0 % per component and 5.0 % aggregate, inherited unchanged and not re-derived by a lane that has seen an answer"*). **The sign-flip rule:** a component whose FD reference and adjoint differ in sign is **`GATE FAIL` whatever its magnitude** (D4 :82 "zero sign flips"; D5 §3 G5d "no sign flip"). **Plateau:** the middle step must agree with at least one neighbour to **10 %** (`|d(s_nb) − d(s_mid)| / |d(s_mid)|`, D4/D5's `PLATEAU_TOL`), else the component is `NOT A RESULT`; `|d_mid| < 1e-14` is `NEAR_ZERO` → `NOT A RESULT`; a failed FD evaluation → `NOT A RESULT`. **Fewer than 3 graded components → the row is `NOT A RESULT`.** Row verdict: `PASS` iff G5 and G5c both `PASS`; `NOT A RESULT` if either is; else `GATE FAIL`.
* **G6 — dot-product / duality test: NOT MEASURED.** The tutorial and `dafoam.mphys` expose no dot-product test; nothing here computes one, and the grader prints that sentence beside the verdict rather than a value. Complex-step likewise not performed (the grid's §6 list stays true).
* **G9 — toolchain per row:** the ledger `DIGEST`, the container's `D4S_IDWARP_SO_MD5:` print and the artefact's in-process `libidwarp.so` md5 (`d15_xf.py: idwarp_identity`) must all name the row's registered toolchain — SHIPPED `f0fcb488e0e98156575cd19548e91663` @ `sha256:9d45679d…f07fc`, PATCHED `85f59e87253e0a71a813f64ca6e4c425` @ `sha256:2927768a…30f6d35` (D7FR RESULTS §G9). Mismatch → `GATE FAIL`.
* **G10 — caps (§4):** every row `core_min ≤ cap`, sum ≤ **165.0**; a crossing is `GATE FAIL` exactly as the frozen text says (report mode; the in-container deadline is the stop).
* **G11 — OOM hard:** the container cgroup (`--memory=4g --memory-swap=4g --oom-score-adj=500`) kills; `OOMKilled true` is a G1 refusal, never a re-fire.
* **G12 — placement:** `cpuset == 2,3` on every row; delivered cores ≥ 1.5 of 2 where measured (cgroup sampler), `NOT_MEASURED` disclosed otherwise.
* **DIVERGENCE** shipped-vs-patched on the adjoint, per component, is **reported with its number** (D7FR §5's form), never gated.
* **Item verdict (composition registered here):** any refusal or any row `NOT A RESULT` → **`NOT A RESULT`**; else any of G-M2/G9/G10/G12 or any row `GATE FAIL` → **`GATE FAIL`**; else **`PASS`**. No grid family exists, so standing rule 5 has no row and **NO GCI IS QUOTED**. `NOT_MEASURED` fields are printed beside the verdict, never inside it.

## 4. COST — DERIVED FROM NAMED ANCHORS, NOT MEASURED

Anchors (`docs/COST_CALIBRATION.md`): **C-31** — D1-C′, the SHIPPED-image endpoint gradient on this very mesh (A1, 4,032 cells, `DASimpleFoam`, np = 1): one cold primal + one adjoint = **1.483 core-min measured** (89 s); **C-71** — D13, per start **5.15–6.38 core-min** for 9–11 IPOPT majors *plus* a 4-component endpoint FD suite (≈ 18 warm primals) at np = 1, so a warm perturbed incompressible primal is ≈ (5.6 − 1.5)/29 ≈ **0.14 core-min**; **C-24** — D1, 7.000 core-min for 11 majors + the FD suite (the same figure from the other side); **C-135** — D14-M mesh regeneration **0.1833 core-min** for 38,304 cells at 1 rank; `curriculum_D7FR/RESULTS.md:36` — transonic `DARhoSimpleCFoam` on 42,120 cells np = 4, ≈ **3.3 core-min per primal**, ≈ 45 per adjoint, ≈ 24 colouring, i.e. 7.8e-5 core-min per cell per primal → **0.32 core-min per primal at 4,032 cells**. Compressible-vs-incompressible factor for `DARhoSimpleFoam` (energy equation, density-coupled pressure): **×2 adopted**, conservative against the D7FR cell-scaled figure (0.32 vs 0.14 × 2 = 0.28).

| arm | derivation | point (core-min) | cap (core-min) | in-container wall | mem |
|---|---|---|---|---|---|
| MESH | C-135 scaled to 4,032 cells + container start | **0.3** | 5.0 | 300 s (1 rank) | 4g |
| X-S, X-P | C-31 1.483 × 2 (compressible) + a second adjoint (CL) ≈ 0.7 + colouring | **4.0** each | 20.0 each | 600 s (2 ranks) | 4g |
| F-S, F-P | 32 primals × 0.28 + setup ≈ 1 | **10.0** each | 60.0 each | 1,800 s (2 ranks) | 4g |
| grader | zero compute | 0 | — | — | — |
| **total** | | **28.3** point, band **[12, 80]** | **ceiling 165.0 = Σ caps** | ≈ 20 min wall at the point, ≈ 85 min at the caps (+ five 60-s H5 windows) | |

**`cost_basis`: c7a.4xlarge at $0.0513/core-h, REPORTED-BY-OWNER, NOT MEASURED** — the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` §5). Dollars **DERIVED**: point **$0.0242**, ceiling **$0.1411**. **Cap mode:** each arm's wall deadline is **inside the container** (`timeout -k 60` at cap × 60 / ranks s; the launcher asserts the enforced wall equals the registered cap to 0.02 core-min and aborts otherwise); the host poller reports a crossing and hard-stops at 4× cap while it lives. The runner's own `CAP_OVERRUN.txt` watch (1.10 × 28.3 × 60 / 2 = 934 s after launch) will fire before the chain ends — it is report-only and is an INFRASTRUCTURE record, expected and disclosed here. A calibration row is owed at completion (rule 12).

## 5. PLACEMENT, MEMORY AND THE DETACHED FORM

* **cpuset `2,3`** (`--cpuset-cpus`, `--cpus=2`; MESH `--cpus=1` on the same set), the supervisor's FOURTEENTH-session placement: disjoint from D5 `8,10,11,13`, D4-SHIPPED `5,6,7,9`, W2R `12`, D16 `4,14`; not core 0. **Disclosed collision:** D6 (held, runs alone after D5 by the supervisor's plan) is registered on `2,3,4,14`; if D6 fires while D15/D16 are live the cpusets overlap — reported to the supervisor as a scheduling fact, not silently re-pinned (UPDATE 7: placement is configuration).
* **Memory:** `--memory=4g --memory-swap=4g` every arm (D13 measured peak RSS 1.70 GiB on this mesh at np = 1; 4g is the D14-M cap); **H5 windowed gate 45 samples / 60 s, refuse on ANY sample < 8.0 GiB** (4g + 4 GiB headroom, D4-SHIPPED §4.4's rule at this cap); **aggregate** (live caps + 4 + host non-container RSS) **< 30.6 GiB** in the **wait-and-retry** form (poll 30 s, bound 4 h, every wait a line in `STATUS.<arm>`, refuse-and-BLOCK at the bound with the series named) — UPDATE F's ruling, from birth.
* **The detached form.** The queue runner launches `bash d15_chain_driver.sh MESH X-S F-S X-P F-P` under its own `setsid nohup`; the driver writes its pid to `<run root>/d15_driver.pid` (G-ROOT.5 b), opens `STATUS.<arm>` at preflight and appends every wait and the final `rc=<n> … source=launcher_exit=docker_inspect_ExitCode` line inside the detached session — never the `$?` of a `setsid`/`timeout` line. `STATUS.chain` carries the chain state and, at the end, `grader_rc=<n> … note=comparator-exit-status-NOT-the-verdict`. Container `rc` is read from `docker inspect` before `docker rm`; **no `--rm`**; the launcher also writes `<ARM>_<stamp>.inspect.txt` as the surviving kernel record.
* **G-ROOT.1–.5 from birth.** `BASE` must equal this item's root through `realpath -m` (G-ROOT.1); the forbidden list names D4's, D4-SHIPPED's, D4-SHIPPED-R's, D5's, D6's, D14's, D14M's, D7R's, D7FR's, D12R's, D12R2's, D12R2W2R's, D13's, **D16's**, A2-mach-wing's roots, the tutorial checkout, `certonomous-runs` and the repository (G-ROOT.2); the ledger refuses a foreign `ITEM=` (G-ROOT.3 — a `ROW=SHIPPED` row is NOT foreign here, this item buys both rows); an `rc=0` row refuses a re-fire (G-ROOT.4 `ALREADY_BOUGHT`, driver); **G-ROOT.5** refuses a RUNNING container carrying `d15_<ARM>_` or a driver pidfile naming a live pid that is not an ancestor or whose cwd is the run root — **DEMONSTRATED 2026-08-26T21:06:53Z (`d15_groot5_selftest_evidence.txt`, 18/18):** a sacrificial `sleep` container → `rc=3`; a sacrificial live pid (444282) with cwd = run root → `rc=3`; a stale pidfile does not block; clear passes and the launcher stops at the L-251 mode check before any staging; D4's, D5's, D13's and D16's roots refuse at G-ROOT.1; **G-ROW** refuses `X-P` on the shipped image and `MESH` on the patched image (rc=4) and passes `X-S` on the shipped image, which then refuses (rc=5) for want of `MESH/` — nothing staged, no arm directory created; zero backticks on executable lines; the temporary root empty and **absent afterwards**.
* **Run root staging** (driver delta 4): the first fire creates the root (mode 777, L-251), copies the tutorial's `0.orig FFD constant system profiles genAirFoilMesh.py preProcessing.sh` into `base/` (never the checkout's `polyMesh`, none exists), overlays the registered `d15_decomposeParDict` (`numberOfSubdomains 2`, scotch — the only edit to a tutorial file, md5 `68ecc827…`), copies the two instruments, writes `ITEM=D15` alone on ledger line 1 and a `STAGED …` line (the D5-DRIVER-DEF-1 corrected form), and md5-asserts every staged file — **including the six tutorial inputs** — before any container starts. A second fire stages nothing.

## 6. PREDICTIONS — scored HIT/MISS by the comparator, never adjusted

| # | prediction | value / band |
|---|---|---|
| **P1** | the MESH arm reproduces A1's mesh | **cells == 4,032** exactly |
| **P2** | baseline `CL` at `aoa = 4°`, M 0.29 (thin-airfoil 0.11/deg × 4 × Prandtl–Glauert 1.045, on a coarse mesh) | **[0.35, 0.55]**, point 0.45 |
| **P3** | baseline `CD` (A1's incompressible coarse-mesh baseline read 0.0209 at CL 0.499) | **[0.012, 0.030]**, point 0.020 |
| **P4** | the PATCHED row G5 (CD) is `PASS` with **≥ 4 of 5** physical components inside band D | point 5/5, worst ≤ 1 % (D13's 0.26 % on the incompressible twin) |
| **P5** | the SHIPPED row's `shape[6]` (the LE function, A1's idx6 class) is **outside band D or sign-flipped** at the baseline — the IDWarp rotation defect is geometric, so it should reach this mesh regardless of regime; hence **SHIPPED row `GATE FAIL`, item `GATE FAIL`, and the cell reads `CAN DO, CAVEATS` (patched PASS / shipped design-point dependent), mirroring 2D·steady·incompressible** | HIT if `shape[6]` on SHIPPED is `GATE FAIL`; `NOT_MEASURED` if it is `NOT A RESULT` |
| **P6** | total graded core-min in **[12, 80]** (point 28.3); MESH wall ≤ 120 s | both scored |

**The falsifier registered up front:** if P5 MISSES (the shipped row PASSES at the baseline in the compressible regime while the incompressible twin failed there), the "geometric, regime-independent" reading of the rotation defect is contradicted by measurement and that is the finding, not a defect of this item.

## 7. INSTRUMENTS, FROZEN BY MD5 AT THIS COMMIT — copy-and-delta from D14/D5

| file | md5 | derivation, and the DELTAS file the supervisor reads |
|---|---|---|
| `d15_run_arm.sh` | `796a2de5b894e8fcdbfab99af6fbf09d` | `curriculum_D5/d5_run_arm.sh` (Addendum 1, `50a97678…`) + **`d15_run_arm_DELTAS_from_d5.diff` (408 diff lines)**: item/root; forbidden roots; G-ROOT.3 accepts both rows; arms MESH/X-S/F-S/X-P/F-P; `ranks_of`; cpuset `2,3`; cap table; G-ROW from the arm suffix; staged-instrument md5s; MESH stages from `base/`, solver arms from `MESH/`; G-COLD lists; arm commands; the `.inspect.txt` kernel record. Container-printed strings `D4S_*` inherited unchanged so the grader greps what the launcher writes (the `D4S-GRADER-DEF-1` class excluded by not renaming) |
| `d15_chain_driver.sh` | `4656f1a0e6381b9c03923cd2470d2814` | `curriculum_D5/d5_chain_driver.sh` (Addendum 2, `89c9b7e7…`) + **`d15_chain_driver_DELTAS_from_d5.diff` (242 lines)**: names/root/md5s; image per arm; tutorial staging with six input md5s; H5 floor 8.0; cap 4 GiB; the grader run at chain end |
| `d15_xf.py` | `8a664a976de77c5fe7c3fed5746be4de` | `curriculum_D5/d5_fd_endpoint.py` (`91b9f352…`) + **`d15_xf_DELTAS_from_d5_fd_endpoint.diff` (432 lines)**: producer/md5; modes X and F; registered step set replaces the clearance ladder; baseline design instead of an endpoint file; the CTRL control with disk read-back; idwarp identity in the artefact; CD **and** CL |
| `d15_grade.py` | `b429ec89e7a738647081783b8b755711` | **the grading path.** `curriculum_D14/d14m_grade.py` shape (built-in `--selftest` against a frozen `EXPECTED_UNITS = 22`) + D5's ledger regex / field classes / arm kinds — **`d15_grade_DELTAS_from_d14m.diff` (891 lines; effectively new, read as a file)**. **22/22 under `python3` AND `python3 -O`** (`d15_grade_selftest_evidence.txt`): clean → PASS; planted 7 % error → GATE FAIL; planted sign flip → flip counted + P5 HIT; no-plateau → component NOT A RESULT; three no-plateau → row NOT A RESULT; rc=1 / OOM / missing marker / stale artefact / broken CTRL / harness-kernel disagreement / absent row → REFUSAL; wrong `.so` md5 → G9; cap crossing → G10; wrong cpuset → G12; cells 4033 → G-M2 + P1 MISS; absent infrastructure → NOT_MEASURED named beside an unchanged PASS; absent row with one inspect record → read from it, source named; `ast.Assert` = 0 in grader and instrument; the counter sees a planted assert |
| `d15_groot5_selftest.sh` | `8a2a7df64b2db87615a20fab2d9208f8` | `curriculum_D5/d5_groot5_selftest.sh` + **`d15_groot5_selftest_DELTAS_from_d5.diff` (103 lines)**: names; sacrificial cpuset 3; D5/D13/D16 roots in (d); the G-ROW legs (e1–e3) |
| `d15_aggregate_memory.py` | `709ab0b98ef0302a3a3a318588f9493f` | **byte-identical** to `d5_aggregate_memory.py` / `d14m_aggregate_memory.py` (`cmp` silent) |
| `d15_runScript.py` | `6537fa7641c4ccb20056f60f96f63b11` | **byte copy** of `/home/ubuntu/dafoam-tutorials/NACA0012_Airfoil/subsonic/runScript.py` — the producer whose header the instrument execs; never modified |
| `d15_decomposeParDict` | `68ecc827562886fb43c3aedb0627b344` | the tutorial's `system/decomposeParDict` (`816f5ba4…`) with `numberOfSubdomains 4 → 2`, one line |
| tutorial inputs, md5-asserted at staging | `genAirFoilMesh.py` `681f10659eb90457fca13fc933008b93`; `preProcessing.sh` `e25c8f8c32886112e3f9591196c695ad`; `NACA0012PS.profile` `51dfed28e1bdb4cd33e0d8d7dabd586a`; `NACA0012SS.profile` `4a6b8ef4501494c7693b71e88a2eabbf`; `FFD/wingFFD.xyz` `6ddf378b028d03d8a18270488bee1759` | identical bytes to the incompressible tutorial's (A1's mesh and box) |

**No `assert` carries a guard** in any python file here (AST count 0 on both, counted by the grader; L-332). **Classifier denials in this lane while building D15/D16: none** — every file was authored with the harness Write tool; the two selftests and the md5 fills ran as plain bash.

## 8. WHAT THIS ITEM WILL NOT ESTABLISH

Nothing about an optimum (no optimiser runs; the "optimization converged" column stays not attempted); nothing at np ≠ 2 or another decomposition (no G8 arm); nothing about the other three `shape` functions or `patchV[0]`; no dot-product test, no complex step; no grid family, no GCI; nothing about a mesh other than A1's 4,032 cells; nothing about M > 0.3 (that is D16). A shipped-vs-patched divergence of 0.000 % on these five components (D7FR's finding on the M6 endpoint) would be reported with its number and would NOT be read as "the defect is absent" — P5 names the component where it is expected.

## 9. FREEZE AND QUEUE

**Committed BEFORE any container starts** (rule 2). The grading path is fixed at this commit: `d15_grade.py` md5 `b429ec89e7a738647081783b8b755711`, asserted by the chain driver before staging and again before the grade. **Queue entry `verification/queue/dafoam/D15_chain.json`:** team `dafoam`, `prereg_commit` = the sha of the commit introducing this file (full 40-hex), `launch_cmd` = `["bash", "<abs>/d15_chain_driver.sh", "MESH", "X-S", "F-S", "X-P", "F-P"]`, `cwd` = this directory, `ranks 2`, `cost_core_min_estimate 28.3`, `memory_floor_gb 8.0`, `cost_basis` derived / not measured, `permission bc0e687e`. Enqueueing is not authorisation: `SUPERVISION_CHARTER.md` §3 check 4 is the supervisor's own, discharged on the sha. **Predicted outcome, so it cannot be written afterwards:** P1–P4, P6 HIT; P5 HIT → SHIPPED row `GATE FAIL`, PATCHED row `PASS`, item **`GATE FAIL`**; the cell moves from `CAN NOT DO — not attempted` to `CAN DO, CAVEATS` on the gradient column, with the record's tokens beside it.
