# CURRICULUM SO-3a — NACA0012 ALPHA-MULTIPOINT WEIGHTED OBJECTIVE, INCOMPRESSIBLE: the FD-VERIFIED MULTIPOINT GRADIENT RUNG. PRE-REGISTRATION, STAGE 1 (FROZEN)

**Version 1.0. FROZEN.** Dated **2026-08-31**. Lane: dafoam `lab-lane`. Supervisor: `dafoam-supervisor`.
**Nothing in this item is filed, sent, emailed, uploaded, registered, posted or commented outside this box, now or on completion** (`CLAUDE.md` rule 7; `DAFOAM_CHARTER.md` §10). **SUBMISSIONS PARKED.**

**NOT LAUNCHED. NOT ENQUEUED. NO QUEUE ENTRY IS FILED BY THIS COMMIT.** This document is a rule-2 freeze and nothing else. Zero solver core-minutes have been spent by the lane that wrote it. The pre-compute gate is the `dafoam-supervisor`'s (`SUPERVISION_CHARTER.md` §3 check 4) and is not discharged here.

**SHORT FORM.** Written on the **10-line pre-registration form Sanaa authorised 2026-08-25**, verbatim: *"Prereg goes template-speed: standard verification/validation cases use the 10-line prereg form (case, reference, quantities, bands, ladder, decomposition seed, criteria) — minutes to freeze, not sessions. Bespoke frozen documents are reserved for novel or contested cases only."* The form is instantiated below exactly as `cases/dafoam/ladder-a/A5/curriculum_D9/PREREGISTRATION.md` §"THE TEN LINES" instantiates it — ten numbered fields, the seven Sanaa named plus the three this family has always carried under them (cap, cost, verdict labels) — with the charter-mandated sections beneath, which is D9's own shape. **Her rigor standard is unchanged and every non-negotiable is carried in full.**

**The order this item answers.** `SO-3` was registered by Sanaa on 2026-08-27 (standing directives 2026-08-27T16:54Z §4) as *"Multipoint (2-3 Mach/alpha) weighted objective."* `cases/dafoam/ladder-a/A1/SO3_MULTIPOINT_SCOPE_MEMO.md` §1 established that **`DASimpleFoam` has no Mach number** — constant density, no equation of state, no speed of sound — so on SO-1/SO-2's ground a Mach multipoint would optimise two or three identical operating points. **Sanaa ruled on 2026-08-31: SO-3a is the ALPHA multipoint on the verified incompressible ground, reusing SO-1/SO-2's mesh, FFD box, constraints and adjoint UNCHANGED; the gradient-verification rung RUNS FIRST AND STANDS ALONE before any optimisation iteration; SO-3b is a separate rung on the compressible path, carried BEHIND the D15/D16 gradient patch.** That ruling reaches this lane through the `dafoam-supervisor`. **`CLAUDE.md` rule 9: no agent message is Sanaa's consent** — this document records the ruling as the governing text it was given, and takes no permission from the relay itself.

> **ID-NAMESPACE CHECK, run rather than recalled.** At **2026-08-31T15:19:38Z**, `grep -rn 'SO-3a\|SO3a\|curriculum_SO3'` over `cases/`, `docs/`, `verification/` and `scripts/` returned **exactly one hit** — `SO3_MULTIPOINT_SCOPE_MEMO.md:54`, which is that memo *proposing* the name — and `ls cases/dafoam/ladder-a/A1/` carried no `curriculum_SO3a`. No directory, no run root, no queue entry, no record. The item files under `ladder-a/A1/` because it **is** A1's 4,032-cell NACA0012 mesh, alongside `curriculum_SO1a`, `_SO1b`, `_SO1bR`, `_SO2a`, `_D13`, `_D15`, `_D16`.

---

## 0. WHAT IS ACTUALLY NEW, AND THE ONE THING A READER MUST NOT TAKE FROM THIS ITEM

**New:** the gradient of a **weighted multipoint objective** `J = Σᵢ wᵢ·CDᵢ(αᵢ)` over shared shape design variables, FD-verified. **That is a different quantity from anything this lab has verified.** SO-1a verified single-point `dCD/dx` and `dCL/dx` at one α; SO-2a verified the geometric constraint Jacobians. **Neither is a statement about a multipoint assembly**, whose adjoint runs one `DASolver` instance per operating point behind one shared `OM_DVGEOCOMP` and sums through an `om.ExecComp`. The assembly is exactly where a multipoint gradient can be wrong while every single-point gradient is right — a mis-wired scenario connection, a weight applied on one side only, a geometry component shared where it should be, or not shared where it should be.

**NOT new, and reused UNCHANGED by Sanaa's ruling — these fields are QUOTATIONS, not fresh choices:**

| reused thing | value | quoted from |
|---|---|---|
| mesh | A1's pyHyp NACA0012, **4,032 cells**, `genAirFoilMesh.py` md5 `681f10659eb90457fca13fc933008b93`, profiles `51dfed28e1bdb4cd33e0d8d7dabd586a` / `4a6b8ef4501494c7693b71e88a2eabbf` | `curriculum_SO1a/PREREGISTRATION.md:40`, `:158` |
| FFD box | `FFD/wingFFD.xyz` md5 `6ddf378b028d03d8a18270488bee1759`, 5×2×2 → **8 `shape` functions** | `curriculum_SO1a/PREREGISTRATION.md:40`, `:158` |
| geometric constraints | `thickcon` (nSpan 2, nChord 10), `volcon`, `rcon` — pyGeo, no flow solve | `curriculum_SO2a/so2a_runScript.py:151-155` |
| adjoint | `DASimpleFoam` discrete adjoint, `primalMinResTol 1.0e-8`, Spalart–Allmaras with wall functions, `U0 = 10.0`, `p0 = 0.0`, `nuTilda0 = 4.5e-5`, `rho0 = 1.0`, `A0 = 0.1` | `curriculum_SO2a/so2a_runScript.py:31-45` |
| toolchain rows | SHIPPED `sha256:9d45679d…f07fc`, `libidwarp.so` md5 `f0fcb488e0e98156575cd19548e91663`; PATCHED `sha256:2927768a…30f6d35`, md5 `85f59e87253e0a71a813f64ca6e4c425` | `curriculum_SO1a/PREREGISTRATION.md:86` |
| FD step set | `shape`: {1e-2, 1e-3, 1e-4}, inside the measured plateau, below the primal-failure region | `curriculum_SO1a/PREREGISTRATION.md:62,64`; `cases/dafoam/ladder-a/A_stepsize_study.md` |

**⚠ THE ONE THING A READER MUST NOT TAKE FROM THIS ITEM'S GROUND.** The ruling calls the incompressible ground *verified*. **The precise state of the record is narrower than that word, and it travels with every claim SO-3a makes.** SO-1a's **item verdict is `GATE FAIL`**: the **PATCHED row `PASS`**, the **SHIPPED row `GATE FAIL`** `[MEASURED, cases/dafoam/ladder-a/A1/curriculum_SO1a/RESULTS.md §1]`. Building on the PATCHED row is legitimate — the two-row structure exists so the patched row can carry work the shipped row cannot — **but the SHIPPED `GATE FAIL` must travel with every downstream claim**, and it is enforced in code here exactly as `curriculum_SO1bR/so1br_precondition.py` enforces it (`require_travelling_provenance()`), not in prose. **G-PROV below.**

---

## THE TEN LINES

| # | field | value |
|---|---|---|
| **1** | **Case** | NACA0012, **4,032 cells**, `DASimpleFoam` (INCOMPRESSIBLE — there is no Mach number on this ground and none is claimed), Spalart–Allmaras with wall functions, `U0 = 10.0 m/s`, M ≈ 0.03. **THREE operating points differing ONLY in angle of attack** (line 4). Objective **`J = Σᵢ wᵢ·CDᵢ`**, one shared `shape` design-variable vector of 8 components across all three scenarios, one shared geometry component. Task: **`compute_totals` of `J` and of each `CLᵢ` wrt `shape`, and a CENTRAL-FD table beside them at a step PROVED to lie in the plateau PER PAIR.** **NO OPTIMISER RUNS. NO OPTIMISATION ITERATION IS TAKEN.** `patchV` is **NOT a design variable in this item** (line 4). |
| **2** | **Reference** | **No external reference exists** for a multipoint weighted-objective gradient on this case, so **the gate is the lab's own FD table and nothing else**, per `DAFOAM_CHARTER.md` §1: *a DAFoam gradient is not a result until a finite-difference table stands beside it at a step proved to lie in the plateau.* The FD side is **central differences at three registered steps**, the reference is the **middle step**, and the plateau is proved **PER PAIR** (line 7, G5J). Cross-reference, reported and never gated: SO-1a's single-point PATCHED `dCD/dx` aggregate **0.0474 %** `[MEASURED, curriculum_D15/RESULTS.md:57 — the same band and instrument shape]`. |
| **3** | **Quantities** | `CDᵢ`, `CLᵢ` for i = 1,2,3 at each α; `J`; `J_adj[J, shape[k]]` and `J_adj[CLᵢ, shape[k]]` for **k ∈ {0, 3, 6, 7}**; `d_fd[k, h]` at **h ∈ {1e-2, 1e-3, 1e-4}**, both signs, per pair; `d_ref` = the middle step; `plateau_neighbour_pct` (two values per pair); `rel_err_pct`; `sign_flip`; the per-scenario decomposition `Σᵢ wᵢ·J_adj[CDᵢ, shape[k]]` for **G-MP-STRUCT**; the primal repeatability **η** from two baseline evaluations, measured **before any FD step is sized**; the trivial-baseline table at **h = 1e-8**; the `CTRL` planted row; **DECLARED and EXECUTED stage counts**; `evaluations_declared` and `evaluations_failed` per arm. |
| **4** | **Multipoint angles and weights** | **THREE α: `{3.13918623195176, 5.13918623195176, 7.13918623195176}` degrees**, i.e. **α₀ − 2°, α₀, α₀ + 2°**. **α₀ = 5.13918623195176 is a QUOTATION** `[REGISTERED, curriculum_SO2a/so2a_runScript.py:35 — the tutorial's own trimmed angle at `CL_target = 0.5`]`, and it is the design point at which the family's plateau study, SO-1a and SO-2a were all measured. **The ±2° bracket is this lane's choice** `[REGISTERED, lane-chosen]` on three stated grounds: (a) all three lie strictly inside the tutorial's own registered aoa bound `[0.0, 10.0]` `[REGISTERED, so2a_runScript.py:170]`; (b) at Re ≈ 6.7×10⁵ (U0 = 10, c = 1, ν = 1.5e-5) NACA0012 is attached throughout 3–7°, so no scenario is placed near stall, where the primal's own repeatability would dominate the FD numerator; (c) 2° is wide enough that the three scenarios are genuinely different operating points and narrow enough that the plateau measured at α₀ is a *plausible* — **not a proved** — starting point at the wings, which is why the plateau is re-proved per pair rather than inherited. **WEIGHTS: `w = {1/3, 1/3, 1/3}`, equal** `[REGISTERED, lane-chosen]`. Equal weighting makes `J` the arithmetic mean drag over the bracket; it is a choice and not a default, exactly as `SO3_MULTIPOINT_SCOPE_MEMO.md` §2 requires it be named. **What a different weighting would change, stated before the run:** the *value* of `J` and of `dJ/dx`, and therefore the optimum a later rung would find — **but NOT this item's verdict**, and §"G-WT" below establishes why, and why the shuffled-weight control the scope memo proposed is NOT registered. **`patchV` is not a design variable here**: α is the *operating point* that distinguishes the scenarios, so it cannot simultaneously be a design variable trimmed to a lift target. This removes SO-1a's `patchV[1]` from the graded set and the removal is registered, not silent. |
| **5** | **Ladder** | **A1**, the NACA0012 rung, ladder **SO-3**. **SO-3a is the gradient rung and it is the WHOLE of this registration.** SO-3's optimisation rung is a SEPARATE, LATER, UNWRITTEN item; its gates are deliberately not written here, because writing them now would be fitting them to an answer this item has not produced. Predecessors: SO-1a (single-point objective + constraint gradient, `GATE FAIL` / PATCHED `PASS`), SO-2a (constraint Jacobians, **`PASS`** `[MEASURED, /home/ubuntu/certonomous-runs/CURRICULUM-SO2a-a1-naca0012-geometric-constraint-gradient/SO2a_grade_20260831T002529Z.json, key `rows.PATCHED`]`). |
| **6** | **Decomposition method AND seed** | **`np = 1` on every arm** (`DAFOAM_CHARTER.md` §5, serial before parallel), which removes A4's measured 16,600× decomposition effect from the chain rather than assuming it absent. `system/decomposeParDict` is overlaid with `numberOfSubdomains 1`, scotch — **NOT EXERCISED**: at np = 1 `decomposePar` is never invoked, the effective decomposition is the trivial single domain, and scotch's internal randomness is never reached. **Seed:** `PYTHONHASHSEED=0` pinned into every container. **No stochastic component exists in this chain** — pyGeo, IDWarp and SIMPLE are deterministic, and no optimiser runs — so there is no RNG seed to pin and none is claimed. **The consequence is registered here and is not a later discovery** (`DAFOAM_CHARTER.md` §5: *a gradient verified at one np is a statement about that np and is never carried to another*): **SO-3's optimisation rung must either run at np = 1 or buy its own multipoint gradient verification at its np.** |
| **7** | **Criteria** | The gate table in §3 below. **Item verdict = the composition registered in §3 and nowhere else.** Bands, all frozen now: **band D = per-pair `|d_ref − J_adj| / |d_ref| ≤ 5.0 %` with the same sign**; **band E = aggregate vector-relative error over graded pairs ≤ 5.0 %**; **plateau tolerance 10.0 %, proved PER PAIR** (the middle step must agree with **at least one** neighbour to 10 %); `NEAR_ZERO` at `|d_ref| < 1e-14`; **fewer than 3 graded pairs on a row → that row is `NOT A RESULT`**; **G-MP-STRUCT tolerance 1e-10 relative**; **G-TB `PASS` iff AT MOST 1 of the 4 registered components passes band D at h = 1e-8**. Bands D and E are **by citation, not re-derived by a lane that has seen an answer**: `curriculum_D4/PREREGISTRATION.md:82` and `curriculum_D7FR/PREREGISTRATION.md:228-229`. |
| **8** | **Cap** | **REGISTERED CAP: 115.0 core-min**, cumulative over every container, = Σ of the per-arm caps in §4 and asserted as that sum in the comparator's `main()`. **An overrun stops the run; it does not get a new budget** (`CLAUDE.md` rule 12). Per-arm caps: MESH 5.0; X-S 15.0; X-P 15.0; F-S 40.0; F-P 40.0. **Memory cap 12 GiB per arm** (raised from the family's 4 g convention; §5 states why and shows the arithmetic). |
| **9** | **Cost** | **Point 22.0 core-min, band [14.0, 60.0], ceiling 115.0.** Every anchor is a MEASURED core-minute figure and **every anchor names the program it prices and asserts the match** (`DAFOAM_CHARTER.md` §18.1) — §4. Dollars **DERIVED, not measured**: point **$0.01881**, ceiling **$0.09832**. **`cost_basis`: c7a.4xlarge at $0.0513/core-h, REPORTED-BY-OWNER, NOT MEASURED** — the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` §5). **GPU: 0 GPU-h; this item launches no GPU instance.** Both figures are far inside the $25 pre-authorisation, **and a blanket is not a per-item read** (rule 9) — the item is costed here regardless. A calibration row in `docs/COST_CALIBRATION.md` is **owed at completion** (rule 12, Sanaa's estimate-versus-actual directive), stating the ratio actual/predicted, attributing the gap, and naming contention and waste **separately**. |
| **10** | **Verdict labels** | `PASS` / `GATE REACHED` / `GATE FAIL` / `NOT A RESULT` / `BLOCKED` / `PENDING`, and no others. No synonyms, no hedging prose. **No grid family exists here, so standing rule 5 has no row and NO GCI IS QUOTED.** `NOT_MEASURED` and `NOT EXERCISED` fields are printed **beside** the verdict, never inside it. |

---

## 1. THE RULE-2 CONDITION, AND HOW IT WAS CHECKED — WITH A PLANTED CONTROL

> **The run root `/home/ubuntu/certonomous-runs/CURRICULUM-SO3a-a1-naca0012-alpha-multipoint-gradient` DOES NOT EXIST at this commit.**

Checked at **2026-08-31T15:19:51Z** by **running the commands, not by recalling them**, and **the reader was shown able to see a non-zero in the same invocation** (`CLAUDE.md` rule 3 applied to the freeze condition itself):

| probe | reader | pattern | reading |
|---|---|---|---|
| target | `test -e` | the run root above | **FALSE** — absent |
| **PLANTED CONTROL, same reader** | `test -e` | `/home/ubuntu/certonomous-runs/CURRICULUM-SO1a-a1-naca0012-dragmin-gradient` | **TRUE** — present |
| target | `find /home/ubuntu/certonomous-runs -maxdepth 2 -iname '*SO3*'` | — | **0 hits** |
| **PLANTED CONTROL, same reader** | `find /home/ubuntu/certonomous-runs -maxdepth 2 -iname '*SO2a*'` | — | **5 hits** |

**Both readers are demonstrably able to report a non-zero, and both report zero for SO-3a. The absence is real, not a blind reader.**

**This item has burned 0 core-min of solver compute and started no container of any kind** — no solver, no mesh, no sacrificial container. **Before first compute, amendments are legal and must state the condition and how it was checked** (`VERIFICATION_CHARTER.md` §2b); **after the first arm container, gates are CLOSED** and changes land only as dated addenda that cannot alter a gate, threshold, cap or label.

---

## 2. THE GRADIENT RUNG RUNS FIRST AND STANDS ALONE — AND WHAT ENFORCES IT

Sanaa's ruling: *the gradient-verification rung runs FIRST AND STANDS ALONE before any optimization iteration.* Two limbs, and they have **different** enforcement strengths. Both are stated at their true strength.

**LIMB 1 — WITHIN SO-3a: ENFORCED BY CONSTRUCTION, and this is the strong limb.** SO-3a's registered arm set is **`MESH, X-S, F-S, X-P, F-P`** and **contains no optimiser arm**. There is no `run_driver`, no `pyOptSparse` invocation, no `max_iter` and no majors anywhere in the registered program. **A frozen chain that cannot express an optimisation iteration cannot take one out of order**, and the comparator refuses any artefact carrying an optimiser history (`G-NOOPT`, §3). This is not a promise about behaviour; it is the absence of the capability, checkable by a reviewer against the arm table today.

**LIMB 2 — ACROSS ITEMS, so that SO-3's optimisation rung cannot start before SO-3a has a verdict: ⚠ NOTHING ENFORCES THIS.** In those words, per the precedent `DAFOAM_CHARTER.md` §18.4 sets. The successor does not exist, so no file gates it; the queue runner has no dependency field this lane could find; and **a requirement in prose has no call sites**. What EXISTS is the mechanism this family has already built and already had fire for real, and it is named here so the successor inherits it rather than reinventing it:

> `curriculum_SO1b/so1b_chain_driver.sh` evaluated a `G-SO1A` precondition against SO-1a's grade artefact **before staging anything**, and **it fired**: on 2026-08-28T02:31:50Z it closed the item at **rc = 7** with the reading `REFUSE no_gates.G5_PATCHED in=SO1a_grade_20260828T023132Z.json` `[MEASURED, cases/dafoam/ladder-a/A1/curriculum_SO1b/G_SO1A.20260828T023149Z.txt]`.

**And it carries two hard lessons the successor must take with it, both already paid for by this family:** (i) SO-1b pinned its dependency **by GLOB** (`SO1a_grade_*.json`), which could not match the successor artefact `SO1aR_grade_*.json` and fired into a guaranteed outcome — so **SO-3's precondition pins BY ABSOLUTE PATH AND BY MD5, and a moved md5 REFUSES**; (ii) SO-1b evaluated it inside an **inline heredoc**, which is **untestable by construction** — so the successor's precondition is a **file** the selftest can drive, of the `curriculum_SO1bR/so1br_precondition.py` form.

**REGISTERED HERE, as this item's own duty to its successor:** SO-3a's completion writes a **stop marker** naming its item verdict and its `G5J` reading into its run root on **every** exit path, so the successor's precondition has a fixed address to read and never a glob. **That marker's writer is a Stage-2 file and does not exist at this commit** (§7).

**G-PROV — THE TRAVELLING SHIPPED `GATE FAIL`, ENFORCED IN CODE.** SO-3a rests on SO-1a's **PATCHED** row while SO-1a's **item** verdict is `GATE FAIL`. The comparator **refuses to emit any verdict** unless: (1) a structured `upstream_provenance` block is present; (2) its SHIPPED row status reads **exactly `GATE FAIL`**; (3) it carries the SHIPPED per-gate detail, not only the row word; (4) a `verdict_line` is present containing the literal bytes `GATE FAIL`; and (5) `verdict_line` is **byte-identical** to the bytes the module composed for it — clause (5) because this lab lost the words `GATE FAIL` from `docs/LAB_STATE.md` to an unquoted heredoc on 2026-08-30 (L-405). **`verdict` itself stays exactly one of the six tokens and is never decorated** (rule 1); the provenance travels in a separate mandatory field.

---

## 3. GATES, THRESHOLDS AND LABELS — frozen now

Vocabulary: `PASS / GATE REACHED / GATE FAIL / NOT A RESULT / BLOCKED / PENDING` and nothing else.

* **G1 — completion, arm-kind aware, the five rule-4 clauses PRINTED INDIVIDUALLY PER ARM.** C1 rc VALUE (physics, from `docker inspect .State.ExitCode`; harness/kernel disagreement REFUSES); C2 terminal marker (`SO3A_X_WRITTEN` / `SO3A_F_WRITTEN` for SOLVER arms, `Mesh OK.` in `checkMesh.log` for MESH); C3 artefact present; C4 **age guard**, artefact strictly newer than the arm's own datum; C5 no fatal token (`FOAM FATAL ERROR`, `FOAM FATAL IO ERROR`, `Segmentation fault`, `SIGSEGV`, `SIGKILL`, `MPI_ABORT`, `signal 9`, `Signal 11`, `Floating point exception`) — **REFUSES REGARDLESS OF rc**. Any clause failing on any arm → **`NOT A RESULT`**. **R-RC** as Sanaa approved it: the rc VALUE is physics, the rc RECORD is infrastructure; a record absent from BOTH channels reads `NOT MEASURED` **only when C2–C5 all hold**, the implied `rc = 0` is written as an explicit INFERENCE with its basis named, and **R-RC relaxes a missing record, never a positive reading of failure**.
* **THE AGE-GUARD DATUM IS RESOLVED BY EXISTENCE, NEVER BY NAME.** `writeCompression on` (`system/controlDict` md5 `46bb883cfc235d12df020bafbe3a9e78`) rewrites `0/U` as `0/U.gz` on a serial arm; AV-1 and AV-2 both returned `NOT A RESULT` on 2026-08-27 with every physics artefact intact **only because their frozen graders pinned a vanished path**. Both names are candidates (`0/U`, `0/U.gz`; `0.orig/U`, `0.orig/U.gz` for MESH), the one FOUND is recorded, and **refusal only when NEITHER name exists**.
* **G-M2 — mesh identity:** `cells == 4,032` → `PASS`, else **`GATE FAIL`**. A different mesh is a different item.
* **G-ALPHA — the operating points are the registered ones.** Each scenario's α, read back from the artefact's own identity record, must equal the line-4 value to **1e-12** absolute. Mismatch → **REFUSE (exit 2)**, the read values printed. **A multipoint verdict at unregistered angles is not this item's verdict.**
* **G5J — THE BRIGHT LINE, on the MULTIPOINT OBJECTIVE `J`, per ROW.** For each of the four registered `shape` components: build `d_fd` at all three registered steps, take `d_ref` = the middle step, compute `plateau_neighbour_pct` as the two neighbour disagreements, and **grade the pair ONLY IF `min(plateau_neighbour_pct) ≤ 10.0 %`** — **the plateau is proved PER PAIR and is never asserted once for the item**. A pair failing that → `NO_PLATEAU`, ungraded. A failed FD evaluation → `FD_STEP_FAILED_OR_ABSENT`, ungraded. `|d_ref| < 1e-14` → `NEAR_ZERO`, ungraded. Then band D per pair and a **sign flip is `GATE FAIL` whatever the magnitude**; band E in aggregate over graded pairs. **Fewer than 3 graded pairs → the row is `NOT A RESULT`.** *This is `curriculum_SO2a/so2a_grade.py:889-912`'s reading, adopted as the reference implementation and not re-derived* — the record it produced carries `steps`, `d_fd`, `d_ref`, `plateau_neighbour_pct` and `rel_err_pct` on every pair.
* **G5C — the SAME test on each scenario's `dCLᵢ/dx`**, all three scenarios, same bands, same per-pair plateau proof. The optimisation rung will carry lift constraints; a multipoint objective gradient verified without them cannot support a constrained multipoint optimisation, which is SO-1a's own finding one level up.
* **G-MP-STRUCT — the assembly identity, and it is the gate that only a MULTIPOINT rung can buy.** For each registered component k: `|J_adj[J, shape[k]] − Σᵢ wᵢ·J_adj[CDᵢ, shape[k]]| / |J_adj[J, shape[k]]| ≤ 1e-10` → `PASS`, else **`GATE FAIL`**. **§2a identity test — what would make it FAIL?** A weight applied on one side only, a scenario connected to the wrong geometry output, a scenario silently omitted from the sum, or an `ExecComp` partial that does not match its own expression. **Could a wrong treatment still PASS it?** Yes, in one way we can name: a defect that corrupts every scenario **identically** cancels in the identity. That is precisely why G5J and G5C are bought **beside** it against an independent FD side — the identity tests the *assembly*, the FD table tests the *physics*, and neither substitutes for the other.
* **G-TB — the `DAFOAM_CHARTER.md` §4 TRIVIAL BASELINE, registered before its own run.** The same probe at a **deliberately wrong step, h = 1.0e-8** — five orders below the registered middle step, in the subtractive-cancellation regime where the FD numerator on a derivative of order 1e-2 is ~1e-10, at or below the primal repeatability η (D15 measured `eta_F` = 1.30e-10 on this mesh). **`PASS` iff AT MOST 1 of the 4 components passes band D at the wrong step. If 2 or more pass, the gate cannot fail, it is not evidence, and that row's G5J verdict is WITHDRAWN to `NOT A RESULT`** — the standing-rule-5 direction: a gate may turn a `PASS` or a `GATE FAIL` **into** `NOT A RESULT`, never the reverse. A probe that ERRORED counts as FAILING the baseline; an unevaluable estimate is not agreement. **The opposite-direction baseline (a step ABOVE the largest registered) is deliberately NOT bought**: `A_stepsize_study.md` measured the primal FAILING at 5e-2 and 1e-1, so that probe returns an exception rather than a reading, and an errored probe is a weaker baseline than a noise-limited one.
* **G-WT — THE SHUFFLED-WEIGHT CONTROL IS *NOT* REGISTERED, AND THE REASON IS THE POINT.** `SO3_MULTIPOINT_SCOPE_MEMO.md` §5 proposed a shuffled-weight trivial baseline — score the multipoint gradient against the wrong weights — as *"the natural candidate"* for a multipoint objective. **This lane rejects it and registers the rejection rather than dropping it silently.** `J = Σᵢ wᵢ·CDᵢ` is **linear in w on BOTH sides**: the adjoint gives `Σᵢ wᵢ·J_adj[CDᵢ]` and the FD side gives `Σᵢ wᵢ·d_fd[CDᵢ]`, so if every per-scenario pair agrees to ε, **any weighting agrees to ε up to a cancellation amplification factor `(Σ wᵢ|d_fdᵢ|)/|Σ wᵢ d_fdᵢ|` that measures the cancellation in the denominator and nothing about the adjoint.** A shuffled-weight probe therefore returns either agreement (whatever the adjoint does) or a cancellation artefact — **it carries no independent evidence about the gradient, and a control that cannot fail on the thing it claims to test is not evidence** (`DAFOAM_CHARTER.md` §4; `CLAUDE.md` rule 3's own logic). **What replaces it is G-MP-STRUCT**, which tests the assembly directly and *can* fail. **The scope memo's suggestion is superseded, on the record, by this paragraph.**
* **G-NOOPT — this item ran no optimiser, asserted rather than assumed.** The comparator REFUSES (exit 2) if any arm artefact carries an optimiser history key, a `major` count, an IPOPT/SLSQP/SNOPT exit line, or a `run_driver` marker. **Sanaa's "stands alone" is checked against the artefacts, not against the arm names.**
* **G-STAGES — DECLARED versus EXECUTED, and the shortfall is READ BY A GATE** (`DAFOAM_CHARTER.md` §18.7 Requirement 4). **DECLARED = 5 arms.** The completion line carries **both** counts. `executed == declared` → the gate is silent; `executed < declared` → the item verdict is **`NOT A RESULT`** or **`BLOCKED`** per the composition below, **never a token whose plain reading is success**. The gate takes the shortfall as an **input**; it is not merely recorded. W3 recorded its truncation perfectly in two independent artefacts and **no gate read it** — 20 of 33 declared stages discarded under a `PHASE1_COMPLETE` token and `exit 0` `[MEASURED, /home/ubuntu/certonomous-runs/CURRICULUM-D12R2W3-cylinder-unsteady/ledger.txt]`.
* **G-EVALFAIL — AN EVALUATION FAILURE IS A GRADABLE STATE, AND THIS IS THE `D6-GRADER-DEF-1` REPAIR.** §6 registers *why* one is expected. The gate: `evaluations_declared` and `evaluations_failed` are reported per arm; a failed evaluation marks its pairs `FD_STEP_FAILED_OR_ABSENT` and **the row is graded on the pairs that survive**, subject to the ≥ 3 graded-pairs floor. **A chain STOP is likewise a GRADABLE state: the comparator grades whatever arms exist and prints, per arm, `RAN` or `NOT RUN`, and REFUSES ONLY on a malformed artefact — NEVER on an ABSENT one.** D6 was ruled `NOT A RESULT` because its grader **refused, exit 2, with ZERO gate readings** on precisely this state — its registration registered a chain stop as a finding and its instrument could not grade that state (`D6-GRADER-DEF-1`), and the run's real measurements were lost behind it. **SO-3a's comparator must produce gate readings for every arm that ran, on every path, including the path where nothing after MESH ran.**
* **G9 — toolchain per row:** the ledger `DIGEST`, the container's printed `libidwarp.so` md5 and the artefact's in-process md5 must all name the row's registered toolchain (§0). Mismatch → **`GATE FAIL`**.
* **G10 — caps:** every arm `core_min ≤ cap`, sum ≤ **115.0**; a crossing is **`GATE FAIL`** exactly as this text says (report mode; the in-container deadline is the stop).
* **G11 — OOM hard:** the cgroup kills; `OOMKilled true` is a G1 refusal, never a re-fire.
* **G12 — placement:** `cpuset` as registered on every arm. **At np = 1 the delivered-cores floor does not apply** and is not composed; the sampler's reading is reported as a number and `NOT_MEASURED` where absent.
* **Rule-3 planted-zero control, in the instrument AND the comparator.** `CTRL` is a synthetic row with identical DVs on both sides (every derivative exactly `0.0`, no solve) plus a PLANTED row moving the plus side by `PLANT = 1.234e-03` so that entry is exactly `PLANT/(2·h) `. The instrument writes both, **re-reads them from disk**, and **exits 2 if the read-back cannot see the plant**. The comparator re-checks both and additionally writes a copy of each FD table with PLANT added to every physical derivative, re-reads it **through the same reader**, and refuses unless every value moved by exactly PLANT. **A zero from a reader not shown able to see a non-zero is not evidence.** **Every planted control reports `EXERCISED-PASS`, `EXERCISED-FAIL` or `NOT EXERCISED`, printed beside the verdict** — `NOT EXERCISED` is never counted as a pass and never inferred from the absence of a failure. *(`DAFOAM_CHARTER.md` §18.5 records this as a PROPOSAL, NOT ENACTED, surfaced to verification and reserved to Sanaa. **This item adopts it VOLUNTARILY, for itself alone; nothing here enacts it for any other item or any other family**, and the adoption is a tightening, never a relaxation.)*
* **DIVERGENCE** shipped-vs-patched on the adjoint, per component, is **reported with its number**, never gated.
* **ITEM VERDICT — the composition, registered here and nowhere else.** Any comparator refusal, any row `NOT A RESULT`, any `G-TB` withdrawal, or **`executed < declared` where the shortfall is not a registered resource block** → **`NOT A RESULT`**. Else `executed < declared` from a registered resource guard reaching its bound → **`BLOCKED`**, naming the guard and its series. Else any of `G-M2` / `G-ALPHA` / `G-MP-STRUCT` / `G9` / `G10` / `G12`, or any row `GATE FAIL` → **`GATE FAIL`**. Else **`PASS`**. `NOT_MEASURED` and `NOT EXERCISED` fields are printed beside the verdict, never inside it.

---

## 4. COST — EVERY ANCHOR NAMES THE PROGRAM IT PRICES AND ASSERTS THE MATCH (`DAFOAM_CHARTER.md` §18.1)

**Anchors. All four terms stated for each — ranks; adjoint yes/no; colouring yes/no; tree cold or already-decomposed — and the match asserted or the difference quantified.** This is the clause D5 and D6R both failed; D6R's anchor cell read a bare `9.0 × 1.6308` with **no row and no program behind the 9.0**, and its arm needed **3.75× its cap** and could not have finished. **Nothing below is a bare multiplier.**

| # | anchor | value | **program it prices** (ranks / adjoint / colouring / tree) | match against SO-3a's program |
|---|---|---|---|---|
| **A1** | SO-1a `MESH`, read from that item's own ledger | **0.183 core-min MEASURED** (11 wall s) `[MEASURED, curriculum_SO2a/PREREGISTRATION.md:131, read from /home/ubuntu/certonomous-runs/CURRICULUM-SO1a-a1-naca0012-dragmin-gradient/ledger.txt]` | **1 rank; NO adjoint; NO colouring; tree COLD** (`preProcessing.sh` from a fresh `base/`) | **MATCHES on all four.** SO-3a's MESH arm is byte-identically the same program on the same tutorial inputs and the same 4,032-cell mesh. **No adjustment.** |
| **A2** | SO-1a `X-S` / `X-P` | **1.017 / 1.200 core-min MEASURED** (61 / 72 wall s) `[MEASURED, same ledger, via curriculum_SO2a/PREREGISTRATION.md:132]` | **1 rank; ADJOINT YES (2 reverse sweeps: CD, CL); COLOURING YES (1); tree COLD (cold copy of MESH/)** | **DIFFERS on scenario count: 1 against SO-3a's 3.** SO-3a's X arm runs **3 primals, 3 colourings and 3×2 reverse sweeps**. **Adjustment quantified, not assumed: ×3**, giving **3.05 / 3.60**. |
| **A3** | SO-1a per-primal upper bound, derived as `(F-S − X-S)/41` | **0.062585 core-min/primal MEASURED-derived** (3.755 wall s) `[DERIVED from two MEASURED figures, curriculum_SO2a/PREREGISTRATION.md:135]` | **1 rank; NO adjoint; NO colouring; tree WARM** (the F arm's primals run in a directory the arm has already solved in) | **MATCHES on all four** for SO-3a's F arms, which are also warm serial primals with no adjoint and no colouring on the same mesh. **It is an UPPER bound** — the subtraction charges the F arm for X's adjoint and colouring work at zero — which is the safe direction for a cap. **No adjustment.** |
| **A4** | C-188, D6, 3-scenario multipoint | **31.258 core-min/major MEASURED against 19.167 registered = ×1.6308**; *"the ×3 model is short by 63 %"* `[MEASURED, docs/COST_CALIBRATION.md C-188]` | **4 ranks; ADJOINT YES; COLOURING YES; tree COLD; unit = an OPTIMISER MAJOR (primal + adjoint + line search) on a 3-D WING** | **DOES NOT MATCH — on ranks (4 vs 1), on geometry (3-D wing vs 2-D 4,032-cell airfoil), and on unit (per-major incl. line-search cutbacks vs per-evaluation; SO-3a has no majors).** **It is therefore NOT used in the point estimate.** It is used ONLY to size the X-arm **cap**, as an `[EXTRAPOLATED]` contingency, and the extrapolation is named: `3.05 × 1.6308 ≈ 4.97` core-min, covered ~3× over by the 15.0 cap. **A cap under one multipoint colouring's wall is a cap that will fire, and that is C-139's own lesson.** |

**The evaluation count, shown so it can be attacked.** One *evaluation* at multipoint = **3 primals**, one per scenario. Registered components **4** (`shape[0], shape[3], shape[6], shape[7]`); steps **3**; signs **2** → 24 perturbed evaluations. Plus **2** baseline evaluations for η. Plus the trivial baseline at h = 1e-8: 4 × 1 × 2 = **8** evaluations. **Total 34 evaluations × 3 scenarios = 102 primals per F arm.** At A3's upper bound: `102 × 0.062585 = 6.384` core-min, plus setup bounded above by a whole X arm (≤ 1.2) → **point 7.5** per F arm, carrying margin.

| arm | derivation | point (core-min) | cap (core-min) | in-container wall (np = 1) | mem |
|---|---|---|---|---|---|
| MESH | A1, matched on four terms | **0.19** | 5.0 | 300 s | 12g |
| X-S | A2 × 3 scenarios | **3.1** | 15.0 | 900 s | 12g |
| X-P | A2 × 3 scenarios | **3.7** | 15.0 | 900 s | 12g |
| F-S | A3 × 102 primals + setup | **7.5** | 40.0 | 2400 s | 12g |
| F-P | A3 × 102 primals + setup | **7.5** | 40.0 | 2400 s | 12g |
| comparator | zero solver compute | 0 | — | — | — |
| **total** | | **22.0** point, band **[14.0, 60.0]** | **ceiling 115.0 = Σ caps**, asserted in `main()` | ≈ 24 min wall at the point | |

**§18.2 CACHE-STATE TERM.** SO-3a is **not** an instrument-only item — it registers solver compute on five arms — so §18.2's mandatory term does not bind it. **It is nonetheless stated, because the omission is what the clause punishes:** the comparator's own dominant term is I/O, and its estimate assumes a **WARM** page cache over **one run root** (≈ 1 copy, ≈ 400–900 files expected at ≈ 0.15 s/1,000 files warm, `C-214`'s carry-forward, which is **this family's own two-root measurement on this box and is NOT a lab-wide constant**). **COLD** on that root would be ≈ 1.0 s/1,000 files (`C-212`, measured after a reboot), a spread this family has measured at 20.1× on byte-identical work. Either way the comparator's compute is **0.000 solver core-min** and it is not in the cap.

**Cap mode.** Each arm's wall deadline is **inside the container** (`timeout -k 60` at `cap × 60 / ranks` s; the launcher asserts the enforced wall equals the registered cap to 0.02 core-min and aborts otherwise); the host poller reports a crossing and hard-stops at 4× cap while it lives. **An overrun stops the run; it does not get a new budget** (rule 12).

**REGISTERED COST EXPOSURE, disclosed rather than absorbed.** The X arms' scenario scaling assumes OpenMDAO's relevance reduction does not re-drive shared work per scenario. **If the three colourings are not independent and the assembly re-colours per reverse sweep, the X arms scale toward A4's territory.** The band `[14.0, 60.0]` is set wide enough to absorb it, the cap covers ~3× the extrapolated contingency, and **P-COST grades the band, not the point.** Contention is named separately in the calibration row and **never folded into the misprediction ratio** (`COMPUTE_BUDGET_CHARTER.md` §6).

---

## 5. MEMORY, PLACEMENT, AND THE RESOURCE GUARDS IN THE **WAITING** FORM (`DAFOAM_CHARTER.md` §18.7)

**§18.7 landed at `cbc9d634` on 2026-08-31 and this is its first live application. The guards below are registered in the WAITING form, and that is a deliberate DIVERGENCE from SO-1a's and SO-2a's registered H5 form, disclosed here rather than discovered later.**

**Memory cap 12 GiB per arm, raised from the family's 4 g convention, with the arithmetic shown.** D13 measured peak RSS **1.70 GiB** for this 4,032-cell 2-D case at np = 1 `[MEASURED, via SO3_MULTIPOINT_SCOPE_MEMO.md §3]`. Three `DASolver` instances in one process is bounded crudely above by `3 × 1.70 ≈ 5.1 GiB` plus one shared mesh/FFD/IDWarp footprint. **That is an `[EXTRAPOLATED]` bound and not a measurement** — no multipoint arm has ever run on this case — so 12 GiB carries better than 2× headroom over it. The item's own first arm **measures** it and the calibration row is owed. **L-262's lesson, applied rather than renamed:** *a registration whose gate cannot satisfy its own floor at the registered peaks is refused at review, not discovered at kill time.*

**THE QUANTITY-TO-FORM MATCH, which is §18.7's actual thesis.** W3 put the **WAIT** on the **STABLE** quantity (a sum of registered caps, which changes only when a container starts or stops) and a **ONE-SHOT BLOCK** on the **TRANSIENT** one (live `MemAvailable`, which changes second by second) — and lost **20 of 33 declared stages, 60.6 % of its declared program** `[DERIVED, 20/33; MEASURED, W3 ledger.txt]`. **SO-3a matches the form to the quantity:**

| guard | quantity | its behaviour | form |
|---|---|---|---|
| **H5 — windowed `MemAvailable` floor, 16.0 GiB** (12 g cap + 4 GiB headroom), 45 samples over 60 s | **TRANSIENT** — a peer's working set grows and shrinks second by second | **WAIT AND RE-POLL**: poll 30 s, bounded deadline **`H5_BOUND_S = 3600`**, every wait a line in `STATUS.<arm>`; **at the bound the chain STOPS with `rc = 7`, `chain=BLOCKED_H5`, the series named** | bounded wait, **expiry TERMINATES with a non-zero rc** |
| **AGGREGATE — live sibling caps + this arm's cap + host NON-CONTAINER RSS < 30.6 GiB ceiling** | **STABLE** — changes only when a container starts or stops | **WAIT AND RE-POLL**: poll 30 s, bound **`AGG_BOUND_S = 14400`**; at the bound `rc = 6`, `chain=BLOCKED_AGGREGATE`, series named | bounded wait, expiry TERMINATES |
| **CAP / in-container deadline** | not a resource condition — a registered budget | **STOP.** `timeout -k 60`; an overrun stops the run and does not get a new budget | terminating, one-shot, correctly so |

**The aggregate reading is the reference implementation named in §18.7 and is adopted unchanged in substance:** `live container caps + this arm's cap + host NON-CONTAINER RSS` against a registered ceiling, one JSON line, **failing closed** — any exception yields `ok: false`. The third term is the one W3's arithmetic lacked. `cases/dafoam/ladder-a/A1/curriculum_SO1bR/so1br_aggregate_memory.py` is the file; SO-3a's copy is a Stage-2 file (§7).

**⚠ WHY H5 CHANGES FORM, STATED AGAINST THIS FAMILY'S OWN PRECEDENT.** SO-1a `:124` and SO-2a `§5` both register H5 as *"refuse on ANY sample < floor"* — a **one-shot** guard on a **transient** quantity. It terminates rather than block-and-continues, so it never had W3's silent-truncation failure mode; **but it is the wrong form for the quantity** and it converts a condition that clears on its own into an immediate stop. §18.7 Requirement 3 is explicit: *a guard for a TRANSIENT resource condition WAITS AND RE-POLLS under a bounded, registered deadline whose expiry TERMINATES the program with a non-zero rc.* SO-3a takes the wait form. **Evidence that the wait form is the one that preserves programs, from this family's own night:** SO-2a's aggregate gate **waited 540 s on `MESH`, re-polled, got `ok: true`, launched, and lost nothing** — all five arms `rc = 0`, `chain=COMPLETE` `[MEASURED, /home/ubuntu/certonomous-runs/CURRICULUM-SO2a-a1-naca0012-geometric-constraint-gradient/STATUS.chain; waits at curriculum_SO2a/launcher.queue.out:15]`. W3, on the same host condition against the same 20 GiB sibling, blocked-and-continued and lost 60.6 %.

**§18.7 REQUIREMENT 1 — WHAT A BLOCK DISCARDS, STATED WHERE THE NUMBERS ARE REGISTERED.**

> **SO-3a REGISTERS NO BLOCK-AND-CONTINUE ANYWHERE. Every guard above STOPS THE CHAIN with a non-zero rc.** There is no path on which SO-3a discards one unit of work and proceeds to the next.

The discard fractions are nonetheless stated, because they are what makes the choice legible before the launch. **DECLARED PROGRAM = 5 arms.** A stop at MESH discards **5/5 = 100 %**; at X-S **4/5 = 80 %**; at F-S **3/5 = 60 %**; at X-P **2/5 = 40 %**; at F-P **1/5 = 20 %**. **A sustained condition discards the same fraction as a single stop** — that is the whole difference from block-and-continue, where a sustained condition compounds one stage's fraction across every stage that meets it, which is how W3 reached 60.6 % from a 3.03 %-per-stage guard. **Because SO-3a stops, no fraction compounds.** A stop that leaves ≥ 1 arm graded is `BLOCKED` with the guard and series named; the arms that ran are still graded (G-EVALFAIL), so a stop is never a total loss of measurement.

**§18.7 REQUIREMENT 4 — NO SUCCESS-READING TOKEN OVER A TRUNCATED PROGRAM.** The chain's completion line carries **`DECLARED=5 EXECUTED=<n>`**, and where `n < 5` the token is one of rule 1's six — **`NOT A RESULT`** or **`BLOCKED`** — **never a word whose plain reading is success**, and never `COMPLETE`. `G-STAGES` reads the shortfall as a gate input. W3's `PHASE1_COMPLETE spent=63.2332 core-min` printed unconditionally after 20 of 33 stages were blocked, on a line outside every conditional, followed by `exit 0` `[MEASURED, d12y_w3_stage_and_run.sh:1011; W3 ledger.txt:108]`. **The lesson taken is not "log the blocks" — W3 logged them perfectly, in the ledger AND in the manifest, 33 rows with exactly 20 `"blocked": true`, in agreement — it is that NOTHING READ THEM.**

**Placement.** np = 1 on every arm, one core, `--cpuset-cpus` fixed at Stage 2 and disclosed there against every live sibling's registered set; **not core 0**. **At np = 1 the delivered-cores floor does not apply** and is not composed into any verdict.

---

## 6. EXPECT AN EVALUATION FAILURE — Sanaa's explicit instruction, registered as a PREDICTION and as a GRADABLE STATE

**Her instruction: the gradient rung is sized to EXPECT AN EVALUATION FAILURE, given D6/D6R's history.** The history, verified against the artefacts:

* **D6** — 3-scenario multipoint wing optimisation — died on IPOPT `EXIT: Invalid number in NLP function or derivative detected.` **D6's own cutback count is `545` in `C-188`; the widely circulated `548` is on record NOWHERE, and `8.56` is `548/64`.** `545/64 = 8.5156`. **This document therefore quotes NO D6 per-major cutback rate as measured, and says why** — a figure that has been wrong in two different ways, with each investigation moving the error upstream, is the signature of a number nobody ever measured `[per docs/LAB_STATE.md:4849]`.
* **D6R** — the successor built to fix D6's chain stop — **reproduced and WORSENED the pathology.** IPOPT ran **73 majors of a registered 80** and died on the identical `EXIT: Invalid number in NLP function or derivative detected.` after **673 alpha cutbacks = 9.219178082191782 per major** and 7 restoration majors `[MEASURED, cases/dafoam/ladder-a/A2/curriculum_D6RG/RESULTS.md:101,104]`.

**WHAT THAT HISTORY DOES AND DOES NOT PREDICT FOR SO-3a, stated precisely.** **SO-3a runs no optimiser, so there is no IPOPT to die and no alpha cutback to count.** D6/D6R's *proximate* failure mode is structurally unreachable here, and this document does not pretend otherwise. **What IS reachable, and is what Sanaa's instruction correctly anticipates, is the failure ONE LEVEL DOWN — the one IPOPT was reporting: an EVALUATION that returns a non-finite number.** `Invalid number in NLP function or derivative detected` is IPOPT saying *the model handed me a NaN*, and a multipoint model hands back a NaN when **any one** of its scenarios fails to solve. That mechanism is fully present in SO-3a, at 306 primals across two F arms, and this family has already measured it on this exact case:

* the primal **FAILS** at FD steps 5e-2 and 1e-1 `[MEASURED, cases/dafoam/ladder-a/A_stepsize_study.md]`;
* AV-2 measured **5/5 rows `AnalysisError(... Primal solution failed!)`** on this exact case on BOTH images when forward mode was seeded `[MEASURED, curriculum_AV2/RESULTS.md §2]`.

**And multipoint AMPLIFIES it by exactly the scenario count:** an evaluation survives only if **all three** primals converge, so a per-primal failure probability `p` becomes `1 − (1−p)³ ≈ 3p` per evaluation. **That is the honest reason to expect one here, and it is arithmetic about this item rather than an inheritance from D6's optimiser.**

**REGISTERED PREDICTION P-EVAL:** **at least one of the 34 declared evaluations per F arm fails or returns a non-finite value**, most probably at the widest α (7.139°) at the largest step (1e-2), where the perturbed geometry is furthest from the design point at which the plateau was measured. **A MISS is a genuinely useful reading** — it would say the α-bracket is comfortably inside the primal's robust envelope, which is exactly what a later optimisation rung needs to know before it runs 20–80 majors through it.

**AND THE PART THAT IS A GATE, NOT A PREDICTION — `D6-GRADER-DEF-1` MUST NOT REPEAT.** D6's registration registered a chain stop at `O_mp` as **a meaningful outcome**, in its own words *"a finding about multipoint feasibility at np=4 on this box, not a wasted run"*, and listed `STOPPED_AT_FIRST_NONZERO` among its registered chain outcomes — **but its grader required every arm to carry a ledger row and refused when one did not.** Those two registered behaviours cannot both be satisfied. **There was no path through the frozen instrument that yielded a gate verdict for that run**, and 2,257.933 core-min of real optimisation sat behind an instrument that could not read it. That is **L-322** — an instrument that cannot represent an outcome its own registration contemplates.

> **REGISTERED HERE, BEFORE ANY RUN: a chain stop and a failed evaluation are BOTH GRADABLE STATES OF SO-3a.** The comparator's arm loop is registered as a **census, not a requirement**: it prints `RAN` / `NOT RUN` per arm, grades every arm that ran, and **REFUSES ONLY on a MALFORMED artefact, NEVER on an ABSENT one**. A failed evaluation marks its own pairs ungraded and the row is graded on the survivors, subject to the ≥ 3 graded-pairs floor. **A registration that registers a stop as a finding and an instrument that cannot grade a stop is a contradiction, and it is refusable at review — this section is where a reviewer checks that SO-3a does not carry it.**

---

## 7. INSTRUMENTS — §18.3: EXISTENCE ASSERTED **BEFORE** ANY MD5, AND **NO MD5 IS CLAIMED FOR A FILE THAT DOES NOT EXIST**

**STAGE 2 IS NOT FROZEN HERE.** This is the section to read before believing SO-3a is ready to run. **Every file below is ABSENT at this commit, existence was CHECKED rather than assumed, and NOT ONE md5 is asserted** — because §18.3's whole subject is an md5-agreement control that read `eight of eight AGREE` while a file the driver executed was absent from the freeze tree (`SO2a-DRIVER-DEF-1`). **An md5 over a subset can read agreement on every pin it holds while a dependency the frozen code executes is missing; existence is a different question and cannot be inferred from any level of md5 agreement — not at 8 of 8, not at 800 of 800.**

| # | file the launcher / driver / comparator will EXECUTE or IMPORT | derived from | exists at this commit |
|---|---|---|---|
| 1 | `so3a_chain_driver.sh` | `curriculum_SO2a/so2a_chain_driver.sh` | **ABSENT** |
| 2 | `so3a_run_arm.sh` | `curriculum_SO2a/so2a_run_arm.sh` | **ABSENT** |
| 3 | `so3a_xf.py` (the instrument: modes X and F) | `curriculum_SO2a/so2a_xg.py` | **ABSENT** |
| 4 | `so3a_grade.py` (**the grading path**) | `curriculum_SO2a/so2a_grade.py` — **G5J adopts its `:889-912` per-pair plateau reading** | **ABSENT** |
| 5 | `so3a_runScript.py` (**3-scenario mphys `Multipoint`**) | `curriculum_SO2a/so2a_runScript.py` + D6's multipoint assembly | **ABSENT** |
| 6 | `so3a_aggregate_memory.py` | `curriculum_SO1bR/so1br_aggregate_memory.py` — **the §18.7 reference reading** | **ABSENT** |
| 7 | `so3a_groot5_selftest.sh` | `curriculum_SO2a/so2a_groot5_selftest.sh` | **ABSENT** |
| 8 | `so3a_decomposeParDict` (`numberOfSubdomains 1`) | the tutorial's, ONE line changed | **ABSENT** |
| 9 | `so3a_stop_marker.sh` — the SO-3a **stop marker writer** (§2, limb 2), the fixed address the successor's precondition reads | **new — NO PARENT** | **ABSENT** |

**Checked by `test -e`, file by file, at 2026-08-31T15:25:21Z: `present=0 of 9`.** **PLANTED CONTROL, same reader, same invocation:** the eight parent files named in column 3 read **`present=8 of 8`** — row 9 has no parent because it is new, and that is stated rather than padded. **The reader is demonstrably able to report a non-zero and reports zero here, so the absence is real and not a blind reader** (`CLAUDE.md` rule 3, applied to the freeze's own existence check as §18.3 requires).

**WHY THEY ARE NOT IN THIS FREEZE, and it is the `curriculum_SO1bR/PREREGISTRATION.md` §12 precedent taken deliberately.** Roughly 2,000 lines of derived shell and python **cannot be executed here** — this lane spends zero solver compute and the chain needs containers — and a §7 table full of md5s for code no one has driven is exactly the unverified surface §18.3 punishes. **The honest object is a Stage-2 registration whose instruments can be DRIVEN before they are FROZEN.**

**WHAT STAGE 1 BUYS, AND IT IS NOT NOTHING.** Every gate, every threshold, every band, every angle, every weight, every step, every cap and every guard form is **frozen at this commit and cannot be chosen to fit an answer**. Stage 2 may add md5s and code; **it may not alter a gate, a threshold, a cap or a label**, and any departure lands as a dated addendum at the foot with the originals struck, never rewritten (`CLAUDE.md` rule 6; `VERIFICATION_CHARTER.md` §2b).

**WHAT STAGE 2 OWES, registered so it cannot be quietly skipped:** (a) existence of all nine asserted **first and separately**, then md5s; (b) **the mechanical §18.3 extraction** — from every frozen script, every `$HERE/`, `$BASE/`, `$LAUNCHER`-style path reference and every local import resolved against this directory and asserted present **in the freeze commit's tree**; (c) the comparator driven end to end on real artefacts under **both `python3` and `python3 -O`**, with **`ast.Assert` = 0** in every python file and the counter shown counting a planted assert (L-332); (d) **G-ROOT.1–.5 demonstrated in both directions** on the real path, including the must-flag direction; (e) the rule-3 planted control shown **refusing** when the plant is invisible; (f) the rule-2 condition **re-checked** at the Stage-2 amendment, with its own planted control.

---

## 8. PREDICTIONS — scored HIT/MISS by the comparator, never adjusted

| # | prediction | value / band | falsifier |
|---|---|---|---|
| **P1** | MESH reproduces A1's mesh | **cells == 4,032** exactly | any other count → MISS and `G-M2` **`GATE FAIL`**; a different mesh is a different item |
| **P2** | baseline `CL` at α₀ = 5.139° | **[0.45, 0.55]**, point 0.50 | outside → the `CL` function is not the one the tutorial registered |
| **P3** | `CD` is **monotone increasing** across the three registered α | `CD(3.139°) < CD(5.139°) < CD(7.139°)` | a MISS at Re ≈ 6.7e5 on an attached NACA0012 means a scenario is not solving the case it is registered to solve — **which would be a finding about the multipoint assembly, and is the cheapest one this item buys** |
| **P4** | **G-MP-STRUCT `PASS`** on all 4 components | ≤ 1e-10 relative | a MISS means the multipoint assembly does not sum what it claims to sum, **and that is the single most valuable thing this rung can find** |
| **P5** | PATCHED row `G5J` **`PASS`**, 4 of 4 pairs inside band D | aggregate ≤ 1.0 % | fewer than 4 → the patched multipoint gradient is worse than its single-point ancestor's 0.0474 % |
| **P6** | **SHIPPED row `G5J` `GATE FAIL`** on at least one component | HIT if `GATE FAIL`; `NOT_MEASURED` if `NOT A RESULT` | a MISS contradicts SO-1a's own SHIPPED reading on this very case at a single point |
| **P7** | `G-TB` **`PASS`** — the wrong step at h = 1e-8 fails band D on ≥ 3 of 4 | scored | a MISS means the FD gate is not measuring the step, `G-TB` **`GATE FAIL`**, and that row's `G5J` verdict is **WITHDRAWN to `NOT A RESULT`** |
| **P8** | **P-EVAL** (§6): ≥ 1 of 34 declared evaluations per F arm fails or returns non-finite | scored per arm | a MISS says the α-bracket is inside the primal's robust envelope — **useful either way**, and it is what a later optimisation rung must know |
| **P9** | the plateau proof **holds per pair at the wing angles** as it does at α₀ | `min(plateau_neighbour_pct) ≤ 10 %` on ≥ 3 of 4 pairs at 3.139° AND at 7.139° | a MISS means the plateau measured at the design point does **not** extend across the bracket, and **the honest consequence is registered now: those pairs are `NO_PLATEAU` and ungraded, and the row may fall below the 3-graded-pair floor to `NOT A RESULT`.** It would **not** license widening the step set after the fact |
| **P-COST** | total graded core-min in **[14.0, 60.0]** (point 22.0) | scored | a MISS is a cost miss, carried into the calibration row; **the band is not adjusted** |

**THE REGISTERED OUTCOME, written before any container starts, so it cannot be written afterwards:** P1, P2, P3, P4, P5, P7, P9 and P-COST **HIT**; P6 **HIT**; P8 **HIT** → **SHIPPED row `GATE FAIL`, PATCHED row `PASS`, item `GATE FAIL`**, and the `2D · steady · incompressible` capability cell gains an **FD-verified multipoint objective gradient** it does not have.

---

## 9. WHAT THIS ITEM WILL NOT ESTABLISH

Nothing about an **optimum** — no optimiser runs, and the "optimization converged" column is untouched. **Nothing at np ≠ 1**: a gradient verified at one np is a statement about that np and is never carried to another, so SO-3's optimisation rung must run at np = 1 or buy its own multipoint verification at its np. **Nothing about Mach**, at all: `DASimpleFoam` has no equation of state and no speed of sound, and **no number from this item may be quoted as a compressibility result** — that is SO-3b's ground and SO-3b is gated behind D15/D16 (`SO3b_STUB.md`). Nothing about α outside `[3.139°, 7.139°]`, and nothing about stall. Nothing about the four unregistered `shape` components or about `patchV`. Nothing about the **weights** beyond the equal set registered: a different weighting is a different objective and would need its own row (§"G-WT" states why the shuffled-weight probe is not evidence). No dot-product test and no complex step — AV-2 measured that seeding forward mode makes the primal FAIL on this exact case on **both** images, and that is a measurement, not a preference. **No grid family, no GCI.** A shipped-vs-patched divergence of 0.000 % on some component would be **reported with its number** and would never be read as "the defect is absent".

---

## 10. FREEZE

**Committed BEFORE any container starts** (rule 2). **The grading path is fixed at this commit** as `cases/dafoam/ladder-a/A1/curriculum_SO3a/so3a_grade.py`, **and its md5 is NOT yet assertable because the file does not exist** (§7) — the md5 is fixed at the Stage-2 amendment, which is itself before first compute, and the amendment must state its own rule-2 condition and how it was checked.

**NOT ENQUEUED. NO QUEUE ENTRY IS FILED BY THIS COMMIT, AND ENQUEUEING IS NOT AUTHORISATION** in any case: `SUPERVISION_CHARTER.md` §3 check 4 is the supervisor's own and is discharged on the sha, not on this sentence. **SUBMISSIONS PARKED** — nothing in this item or its artefacts is filed, sent, uploaded, registered, posted or commented outside this box, now or on completion, and **sending is Sanaa's decision alone** (`CLAUDE.md` rule 7; `DAFOAM_CHARTER.md` §10).

---

## AMENDMENT S2-1 — STAGE 2: THE NINE INSTRUMENTS EXIST, THE md5 PINS ARE SET, AND ONE UNREPAIRED CALL SITE WAS FOUND AND REPAIRED

**Version 1.1.** Dated **2026-08-31**, appended at the foot. Lane: dafoam `lab-lane`.
**Lines whose number changed above this section: 0.** This amendment is APPENDED ONLY; §0–§10 and the TEN LINES are untouched, and every citation into them by line number still resolves.

**⚠ THIS AMENDMENT MOVES NO GATE, NO THRESHOLD, NO CAP AND NO LABEL.** §7 permits Stage 2 to add md5s and code and forbids it to alter a gate, a threshold, a cap or a label. Driven rather than asserted: every registered constant was mutated ONE AT A TIME in a byte-identical copy and the comparator's suite went non-zero on **17 of 17** — band D 5.0, band E 5.0, plateau 10.0, `NEAR_ZERO` 1e-14, `MIN_GRADED_PAIRS` 3, `MP_STRUCT_TOL` 1e-10, `ALPHA_TOL_ABS` 1e-12, `TB_MAX_PASSING` 1, `CELLS_EXPECTED` 4032, ceiling 115.0, the `F-S` cap, `PLANT`, `CTRL_STEP`, the weights, the step set, the TB step and the component set. **The pins are BEHAVIOURAL, not documentary** — widening band D to 7.0 does not trip a text comparison, it makes unit U61 (a 6 % error must be `GATE FAIL`) fail. That is the stronger form, and its limitation is stated: a change to a band AND to the unit that pins it would pass, and only `(f3)`'s ceiling grep reads this document's own bytes.

### 1. THE RULE-2 CONDITION, RE-CHECKED AT THIS AMENDMENT, WITH ITS OWN PLANTED CONTROL

> **The run root `/home/ubuntu/certonomous-runs/CURRICULUM-SO3a-a1-naca0012-alpha-multipoint-gradient` STILL DOES NOT EXIST**, at **2026-08-31T18:32:25Z**, checked by running the commands.

| probe | reader | reading |
|---|---|---|
| target | `test -e` on the run root | **FALSE** — absent |
| **PLANTED CONTROL, same reader, same invocation** | `test -e` on `CURRICULUM-SO1a-a1-naca0012-dragmin-gradient` | **TRUE** — present |
| target | `find -maxdepth 2 -iname '*SO3a-a1*'` | **0 hits** |
| **PLANTED CONTROL, same reader** | `find -maxdepth 2 -iname '*SO2a*'` | **5 hits** |

**Both readers are shown able to report a non-zero and both report zero for SO-3a.** This amendment is therefore **BEFORE FIRST COMPUTE**, where §2b makes it legal. **Zero solver core-minutes have been spent.** The only compute this amendment burned is the G-ROOT.5 suite's sacrificial 64 MB container, which is the selftest §7.3 registers as a precondition of enqueueing; it is not a solver arm and buys no measurement.

### 2. §18.3 STEP 1 — EXISTENCE OF ALL NINE, ASSERTED FIRST AND SEPARATELY, BEFORE ANY MD5

Checked by `test -f`, file by file, **before a single md5 was taken**: **`present = 9 of 9`.** §7's table read `present = 0 of 9` at the freeze; the condition it names — *"the amendment is OWED when rows 2, 3, 4, 5 and 7 exist, and not before"* — is discharged. Every file is tracked in git and byte-identical to its committed blob at the commit this amendment lands in.

### 3. §18.3 STEP 2 — THE MD5 TABLE, CLAIMED ONLY NOW THAT EVERY FILE EXISTS

| # | file | md5 | parent (§7 col. 3) |
|---|---|---|---|
| 1 | `so3a_chain_driver.sh` | `4f5148b741059dd5abd92d5cb9cc4757` | `so2a_chain_driver.sh` |
| 2 | `so3a_run_arm.sh` | `a5948480eadc57de00c27f84c057a66c` | `so2a_run_arm.sh` |
| 3 | `so3a_xf.py` | `6b0736be079b3f510f7bd59ae2114ff1` | `so2a_xg.py` |
| 4 | `so3a_grade.py` — **THE GRADING PATH (§10)** | `c5ccf28138caccd6eac1ad02cb13fa9a` | `so2a_grade.py` |
| 5 | `so3a_runScript.py` | `c0821199159026ec597549ee034b73ac` | `so2a_runScript.py` + D6's assembly |
| 6 | `so3a_aggregate_memory.py` | `709ab0b98ef0302a3a3a318588f9493f` | `so1br_aggregate_memory.py` |
| 7 | `so3a_groot5_selftest.sh` | `5cd8bc4edcc8bbc5b319da8db417b26e` | `so2a_groot5_selftest.sh` |
| 8 | `so3a_decomposeParDict` | `e6f1b0060944bc86d6dff56480ad2bd4` | the tutorial's, ONE line changed |
| 9 | `so3a_stop_marker.sh` | `d44e05f9d6097821502570fd3c70ebe7` | **new — NO PARENT** |

**§10 is hereby satisfied:** the grading path `so3a_grade.py` is fixed at `c5ccf28138caccd6eac1ad02cb13fa9a`, before first compute.

### 4. PINS DRIVEN VERSUS PINS DECLARED — THE NUMBER, NOT A LABEL

`so3a_chain_driver.sh` **DECLARES 13** `MD5_*` pins and **`(x12)`+`(x13)` DRIVE 13**, each compared against the file it pins; `so3a_run_arm.sh` declares and drives **3** more at every launch. **13 of 13 and 3 of 3 EQUAL the files they pin.**

**Why this is counted and not claimed.** SO-1c's leg **drove 4 pins of 12** under a printed label reading *"EVERY md5 PIN IN THE DRIVER"*, and **no leg could see the gap** — the label was the only thing asserting completeness, and a label is not a count. A stale pin aborts this chain at **rc = 4 before any container starts**, which is the W3 death mode: a whole run lost to a string. `(x14)` therefore re-counts the declared side **out of the driver's own bytes** and compares it to the driven count, and `(x14b)` proves it can move: a pin appended to a planted copy takes the declared count 13 → 14 and turns `(x14)` red. `(x12b)` plants a stale `MD5_GRADER` and the comparison goes red, so the thirteen matches are readings of files rather than of a pattern.

**The fail-closed sentinel `MD5_UNSET` is DELETED from both files, not left defined.** Its value was 32 zeros — a *well-formed* md5 — so a dead sentinel would have been counted as a real pin by `(x14)` and would have sat in the tree as a fail-open one edit from re-use.

**Registered consequence, stated so it is not discovered later:** `MD5_LAUNCHER` and `MD5_GRADER` now pin the FINAL bytes of the launcher and the grading path. **Any further edit to either file breaks the chain at rc = 4 and requires a re-pinning addendum.** That is the intended cost of freezing.

### 5. THE RULE-14 SWEEP — A FOURTH CONSUMER WAS FOUND, AND IT WAS FOUND BY LOOKING FOR IT

`CLAUDE.md` rule 14: *a lesson is not applied until EVERY call site asserts it.* The arm→row mapping had **four** consumers in this item. Three were registered tables — `so3a_run_arm.sh:row_of()`, `so3a_grade.py:ARM_ROW`, cross-checked by `(x4)`. **The fourth was `so3a_chain_driver.sh:img_of()`, and it still derived the row from an ARM-NAME SUFFIX GLOB** — `case "$1" in MESH|*-S) ... *-P)` — the exact derivation the launcher's OWN repair comment names as forbidden, two files away from the comment forbidding it.

**Why eight files of sweeping never saw it, which is the real finding.** `row_label_call_sites()` carried a suffix rule in its **PYTHON** rule set (`.endswith("-S")`) and **none in its SHELL rule set**. The single unrepaired site was in shell. **The sweep swept it and returned zero — a zero that was a statement about the rule set, not about the code.** This is the SO-1c shape one layer up: there, the fixture concealed the break; here, the sweep's own asymmetry did.

**Measured, not asserted:** `img_of Q-S` returned the **SHIPPED** image, and the driver takes its arms from the command line (`ARMS="$*"`), so an undeclared arm reaches it. **It could not reach a wrong verdict**: the launcher independently re-derives `ROW` from the **image digest** and then refuses because `row_of Q-S` is empty (exit 64). **Defence in depth held, and the honest size of the finding is that this was a latent second line, not a live hole** — recorded at its true size rather than the larger false one.

**Repaired and instrumented:**
1. `img_of` now reads a registered full-name table `row_of_arm`, label sets **disjoint**, an undeclared arm falling through to the empty case and refusing.
2. The **shell** rule set gained the suffix-glob rule, and its **known positive is the real file** — the rule flags `chain_driver:133` and flags **zero** of the legitimate full-name lists (`X-S|X-P)` etc.), which is stronger evidence than a plant.
3. **U101b** proves the new rule fires on a suffix glob **and stays silent on the registered full-name table** — a rule that flagged both would forbid the repair it demands. U101's existing shell plant could not speak for it: it is caught by the two older shell rules and would stay green with the new rule deleted.
4. **`(x11)`** asserts all **three** call sites agree on all 5 declared arms, `(x11b)` that the driver too refuses `Q-S`, `(x11c)` proves the comparison red on a planted copy. `(x4)` compared two agreeing sites and was green throughout; **two agreeing call sites say nothing about a third.**
5. `EXPECTED_UNITS` **91 → 92**, bumped deliberately with its reason recorded at the constant, which is what that guard exists to force.

### 6. WHAT WAS DRIVEN — §7's STAGE-2 OBLIGATIONS (a)–(f), EACH WITH ITS READING

| owed | discharged |
|---|---|
| (a) existence first, then md5s | `present = 9 of 9` by `test -f` before any md5; §2, §3 above |
| (b) mechanical §18.3 extraction | **20** `$HERE/`-style path references and local imports extracted from all nine and resolved against this directory: **0 absent** (`..` and `reference` are directories and both exist) |
| (c) comparator end to end under `python3` AND `python3 -O`; `ast.Assert` = 0 | grade **92 units / 0 failures** in BOTH modes; xf **33 / 0** in BOTH; `ast.Assert` = **0** in all five python files, and the **same counter reads 1 on a planted assert** (L-332) |
| (d) G-ROOT.1–.5 both directions incl. must-flag | `so3a_groot5_selftest.sh` **71 legs, 0 fail**: live container → exit 3, live pid cwd → exit 3, stale pidfile → NOT blocked, must-proceed, `ITEM=D4` ledger → exit 3 with the same ledger on this item ACCEPTED |
| (e) rule-3 planted control shown REFUSING | xf `E1`: an instrument whose planted `J` no longer moves by `PLANT` is REFUSED by the real reader, `reason=plant_not_seen`; `E2` proves the instrument byte-identical afterwards |
| (f) rule-2 re-checked with its own planted control | §1 above |

**TOTAL: 250 python-side legs + 71 G-ROOT.5 legs, 0 failures, 0 `NOT EXERCISED`.**

**THE AMENDMENT'S OWN EVIDENCE, registered in the selftest before it was taken.** `(e1)`, `(e2)` and `(e3)` were written to read the **PRE-AMENDMENT** fail-closed refusal and to name what they become. On this drive all three **FLIPPED to POST-AMENDMENT**: the pins verify and G-ROW refuses on its own terms. `(e5)` — the SHIPPED digest, the one leg that stood **NOT EXERCISED** because the launcher refused before resolving an image — is now **EXERCISED and PASSING**. **A leg that passed identically before and after would not have been testing the pin.**

### 7. NOT ENQUEUED, NOT FILED, NOT ARMED

**NOTHING IS FILED BY THIS AMENDMENT.** The queue entry stays in this case directory and is validated **OUT OF PLACE, WITHOUT `--require-binding`**. **TEAM-BINDING IS `NOT CHECKED`** — an unchecked condition, never a passing one; `--require-binding` out of place returns rc = 2 **by design**, and its refusal is the ABSENCE of a check, not a failed one. **In-place validation is ARMING and arming is the supervisor's**, not this lane's. `SUPERVISION_CHARTER.md` §3 check 4 is discharged on the sha, not on this sentence. **SUBMISSIONS PARKED** (`CLAUDE.md` rule 7). A `docs/COST_CALIBRATION.md` row is **OWED AND HELD**, not filed, pending the committed-state repair of `scripts/append_record.py`.
