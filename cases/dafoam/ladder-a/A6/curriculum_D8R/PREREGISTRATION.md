# CURRICULUM D8R — A6 CRM wing-alone, N=16, twist-only constrained drag minimisation **TO CONVERGENCE**, on **TWO TOOLCHAIN ROWS**, each with an **ENDPOINT FD table** beside the endpoint adjoint — PRE-REGISTRATION

**Version 1.0. FROZEN.** Dated **2026-08-26**. Lane: dafoam `lab-lane` (N2). Supervisor: `dafoam-supervisor` (FIFTEENTH session, TASK 2 of the N2 dispatch). **This document is the D8 item (`curriculum_D8/PREREGISTRATION.md`, frozen `70d1a3d4`; graded `GATE REACHED` at `9c241fe2`) re-registered to run to the optimiser's own convergence statement on BOTH rows, on the D16 launcher family (`curriculum_D16/PREREGISTRATION.md`, `3ccb0c81`): where a gate, band or label here is not listed as a delta from D8 §4/§7 or from D16 §3, that text wins and this document is defective.** Permission: **`bc0e687e`**; queue-first `7def3c6b` / `73eccb1b` / `0b041d1a`; L-342 `d4d0c29d`; grid `068c2bf0`; `3c3ef86c`. `[lab-attributed]`. Nothing here is filed, sent or posted (rule 7; `DAFOAM_CHARTER.md` §10).

> **ID-NAMESPACE WARNING.** `D8R` is this family's name for *"D8, re-run to convergence"*; `EXPERTISE_CURRICULUM.md` has no `D8R` row, `docs/DOCKET.md`'s D-rows are fleet defects, and `grep -rln curriculum_D8R cases docs verification` found nothing before this directory existed. It lives beside D8 at `cases/dafoam/ladder-a/A6/curriculum_D8R/` (A6 is the CRM ladder).

---

## 0. WHAT D8 ESTABLISHED, AND WHAT THIS ITEM BUYS — read from the record, not from the board

| fact | value | source |
|---|---|---|
| D8's verdict | **`GATE REACHED`** — every physics gate PASS, G1 stopped at the registered **3-major** IPOPT cap: `EXIT: Maximum Number of Iterations Exceeded.`; *"a stop is not a measurement"* | `curriculum_D8/RESULTS.md:1-30` @ `9c241fe2`; `docs/capability/dafoam_GRID.md` row `3D · steady · transonic`, optimisation column: **"0 converged to tolerance, 2 cap-stopped"** |
| D8's endpoint FD (patched row) | aggregate **0.6852 %**, worst 2.6497 %, 0 sign flips, 8 of 9 (twist idx6 `NOT A RESULT` by name); plateau worst 6.78 % against 10 %; min clearance 16.09× | `RESULTS.md` T1/T2 |
| the shipped row | **`PENDING` — NOT BOUGHT**, priced ~147 core-min; *"this item CANNOT claim a toolchain-independent result"* | `RESULTS.md` §6 |
| **seconds per major, MEASURED, np = 1** | the three majors carrying two flow adjoints each: **880, 909, 874 s** (mean 887.7); the major-0→1 gap **1,479 s** carries the one-off `dRdWColoring_1.bin` (≈ 591 s of colouring); a primal **61–85 s cold, 63–65 s warm**; **≈ 410 s per flow adjoint**; arm `opt` **5,251 wall s = 87.517 core-min** for 3 majors incl. trim and the endpoint CD adjoint; arm `fd` **2,602 s = 43.367 core-min**, 33 primals at **78.50 s** warm | `curriculum_D8/LANE_REPORT.md` §8 (`aaa06b73`); `RESULTS.md` §5, §10; `docs/COST_CALIBRATION.md` **C-76** |
| D8's trajectory | trimmed start CD `0.03877565033718443` → major 3 `0.03866430994135252` (**−0.2871 %**, `+1.113404e-04` = 10.2 η); `inf_du` 1.58e-3 → 8.61e-4 over 3 majors; every major a full step (`alpha_pr` 0.99–1.00, `ls 1`) | `RESULTS.md` §3; `LANE_REPORT.md` §7; `opt/opt_IPOPT.txt` |
| memory | adjoint peak RSS **9.970 GiB** at np = 1 under a 12 GiB cap, 327 samples, uncensored; fd arm 0.658 GiB | `RESULTS.md` §0 G6, §5 |
| the mesh | 41,760 cells, `constant/polyMesh/points.gz` md5 **`11b84f0de5fdf2d3e947fee8cea412a9`**, five independent cold-CD reproductions of **`0.03506349413916734`** at np = 1 | `curriculum_D8/PREREGISTRATION.md` §2, §9; `rung_n16_fixed_reference/RESULTS.md` @ `66f42398` |

**What this item buys, and nothing else:** the same problem (D8's producer, byte-frozen: `d8_gen_arm.py` applied to the A6 tutorial — `transonicPCOption 1`, `primalMinResTolDiff 1e4`, `primalMinIters 1000`, twist-only, LE/TE constraints removed), run under IPOPT with a **registered major-iteration budget sized from D8's measured seconds-per-major** instead of a 3-major cap, **on both rows, PATCHED first**, each followed by an **endpoint FD arm** on a registered twist subset at a plateau-proved step — *"the two-row endpoint FD is what turns a descent into a result"* (`DAFOAM_CHARTER.md` §9). The cell that can move: **`3D · steady · transonic`, column "optimization converged"** (from "0 converged to tolerance, 2 cap-stopped"); the gradient column is re-evidenced at two new endpoints.

**One departure from D8, registered here with its consequence: np = 4, not np = 1.** The dispatch places this item at 4 ranks on cpuset `0,1,12,15` (D8 ran `--cpus=1`). Consequences, all registered: (i) D8's **17-digit** cold-CD equality is an np = 1 statement; under `scotch` decomposition the cold CD is registered to land within **10 η = 1.091e-4** of `0.03506349413916734` (**G-BASE**, a gate; a miss says the decomposition moved the primal by more than the case's own noise floor and is `GATE FAIL`, never absorbed); (ii) `nProcs : 1` becomes **`nprocs == 4`**, asserted from the artefact; (iii) the decomposition question D8 §5 could set aside is live here and is **disclosed, not tested** — D7FR (same solver, 42,120 cells, np = 4, scotch) and D4 (np = 4) passed their FD tables, and D-B (the decomposition adjoint defect) is on record for other cases; the endpoint FD table is the check this item carries.

## 1. THE AMENDMENT CONDITION, AND HOW IT WAS CHECKED

> **The run root `/home/ubuntu/certonomous-runs/CURRICULUM-D8R-a6-twist-opt-conv` DOES NOT EXIST.**

`test -e` → false at **2026-08-26T22:05:56Z** (first disk reading, `ls` of `CURRICULUM-D8*` showed only D8's root) and again as the last leg of `d8r_groot5_selftest.sh` at **2026-08-26T22:41:42Z** (`d8r_groot5_selftest_evidence.txt`, 18/18). **0 solver core-min; no arm container has started; D8's run root is read-only to this item and G-ROOT.2-forbidden for writing (driven: refuses at G-ROOT.1).** After the first arm container, gates are **CLOSED**.

## 2. ARMS — the D16 chain form, four SOLVER arms, PATCHED first

Chain `d8r_chain_driver.sh O-P F-P O-S F-S`, every arm **4 ranks**; ONE mesh — D8's — staged read-only into `base/` on the first fire (23 inputs md5-asserted, `d8r_chain_driver.sh::tut_md5_list`); every arm stages a **cold** copy of `base/` (the launcher's G-COLD: `0/` present, no time directory, no `processor*`, no `reports/`, no `OptView.hst`/`opt_IPOPT.txt`/colouring file, age datum = `0/U` touched last); G-ROW from the arm suffix; **two rows or it is not a verdict**.

**Arm O-P / O-S** (`d8r_of.py -mode O`): the frozen producer's header (everything above `# OpenMDAO setup` of D8's `opt/runScript.py`, md5 `28c7819487a025a5f6554d38062a2b66`; the post-anchor D8 task branches with their `max_iter 3` are never reached), then IPOPT with **D8's option block verbatim except `max_iter = 30`** (`tol 1e-5`, `constr_viol_tol 1e-5`, `mu_strategy adaptive`, `limited_memory_max_history 10`, `nlp_scaling_method none`, `alpha_for_y full`, `recalc_y yes`, `print_level 5`, `output_file opt_IPOPT.txt`, `hist_file OptView.hst`): cold primal → **the CL trim, D8's exact call** (`findFeasibleDesign(["scenario1.aero_post.CL"], ["patchV"], targets=[0.5], designVarsComp=[1], epsFD=[1e-1], tol=1e-3, maxIter=4)`) → start primal → `run_driver` → endpoint re-evaluation → `compute_totals(of=[CD, CL], wrt=[twist, patchV])` → the IPOPT terminus read from `opt_IPOPT.txt` (`EXIT:` line, `Number of Iterations`) → `d8r_O.json`, marker `D8R_O_WRITTEN`.

**Arm F-P / F-S** (`d8r_of.py -mode F`): the **same row's** `d8r_O.json` copied in by the launcher as `d8r_O_endpoint.json` (refuses without it; the grader refuses unless its md5 is that row's O artefact), the endpoint design set, η by two primals, then central FD on the **registered subset `twist[0]`, `twist[1]`, `twist[3]`, `twist[4]`, `twist[5]`** (the three D8 components with the largest endpoint error — 2.650 / 2.171 / 2.070 % — and two of the best; `twist` idx6 is `NOT A RESULT` by name, D8 §6, untouched; `patchV` not graded at the endpoint — D8 measured it at 0.56–0.82 % and it does not cross the warp) + the **`CTRL` planted-zero component**; steps **{3e-2, 1e-1, 3e-1} degrees** — D8's measured graded rungs (T1: `s_lo` 0.03–0.1, `s_hi` 0.1–0.2, clearance ≥ 16×; `VERIFICATION_CHARTER.md` §7's nominal 1e-3…1e-2 is unusable in degrees, D8 §9 item 3); the **middle step 1e-1 is the reference** with the 10 % plateau proof; **32 primals** per F arm; `d8r_F.json`, marker `D8R_F_WRITTEN`.

## 3. GATES, THRESHOLDS AND LABELS — `d8r_grade.py`

**From D16 §3, verbatim:** G1 arm-kind-aware completion (all SOLVER) with the age guard, the `.inspect.txt` kernel-record fallback and the L-342 field classes; **G5 band D 5.0 % per component with the sign-flip rule, band E 5.0 % aggregate, plateau 10 %, ≥ 3 graded components** — `curriculum_D4/PREREGISTRATION.md:82`, `curriculum_D7FR/PREREGISTRATION.md:228-229`, cited not re-derived — on CD (G5) and CL (G5c); **G6 dot-product/duality NOT MEASURED, named**; G9 two `.so` md5s (`f0fcb488…` / `85f59e87…`) and two digests per row, printed by the container and read in-process; G10 caps; G11 OOM hard; G12 cpuset `0,1,12,15` and delivered cores ≥ 3.0 of 4 where measured; divergence reported (the two endpoints differ — a reading); no GCI.

**From D8 §4, carried by citation:** **G2** `|CL_final − 0.5| ≤ 5.0e-3`; the intermediate threshold **CD_start − CD_final ≥ 10 η = 1.0910e-04** (D8's G3), here the condition for `GATE REACHED`; η = 1.0910e-05 (N-D13, reproduced at ratio 0.989 across a container boundary, D8 §4).

**New gates, registered here:**
* **G-M2** mesh identity by hash: md5 of every arm's `constant/polyMesh/points.gz` == `11b84f0de5fdf2d3e947fee8cea412a9` → PASS else `GATE FAIL` (the launcher also asserts it at staging).
* **G-BASE** per O arm: `|CD_cold − 0.03506349413916734| ≤ 10 η` → PASS else `GATE FAIL` (§0).
* **G-O** per O arm, the optimiser's OWN terminus (`DAFOAM_CHARTER.md` §9): `EXIT: Optimal Solution Found.` → **PASS**; `EXIT: Maximum Number of Iterations Exceeded.` with the intermediate threshold met → **GATE REACHED**, not met → **NOT A RESULT**; any other `EXIT` (restoration failure, infeasible, an adjoint `AnalysisError` that kills the run) or none → **NOT A RESULT**. `Number of Iterations` > 30 → the grader **refuses** (an instrument defect). **Report-then-stop at the ceiling:** the budget is enforced by IPOPT's own `max_iter` (the optimiser stops itself and prints its statement); the in-container deadline is the cap wall (§4) and is sized to contain 30 majors, so a `rc=124` is an over-cap event on the per-major rate, `NOT A RESULT` on that arm, never re-budgeted.
* **G3** per O arm: the drag reduction `(CD_start − CD_final)/CD_start` lies inside the **registered band [0.29 %, 5.0 %]** (P5) → PASS, else **`GATE FAIL` — outside the band is a GATE FAIL, never a silent widening**. The band's derivation from D8's trajectory: the lower edge is D8's own measured 3-major drop (0.2871 %; an optimiser continuing from D8's trajectory cannot close *above* it without a restoration step, which IPOPT reports); the upper edge is the induced-drag bound of a twist-only reload at fixed CL — CD_i ≈ CL²/(π·AR·e) ≈ 0.25/(π·9·0.85) ≈ 0.0104 of a 0.0388 total (≈ 27 %), an Oswald gain from 0.85 to 0.95 gives ≈ 2.8 % of total, so 5 % is conservative; twist cannot change the sections' pressure drag.
* **Row** = `NOT A RESULT` if any of G-O/G2/G3/G5/G5c is; else `GATE FAIL` if any is; else `GATE REACHED` if G-O is; else `PASS`. **Item** = `NOT A RESULT` if any row is; else `GATE FAIL` if any row or G-M2/G-BASE/G9/G10/G12 is; else `GATE REACHED` if any row is; else `PASS`. The PATCHED row is the result-bearing row for the cell; a SHIPPED `GATE FAIL` makes the item `GATE FAIL` exactly as in D15/D16 (the cell then reads `CAN DO, CAVEATS`).

**The grader's selftest: 31/31 under `python3` and `python3 -O`** (`d8r_grade_selftest_evidence.txt`; D16's 22 units re-pointed + 9 new for G-O/G2/G3/G-BASE/`n_iter`/endpoint-identity/`nprocs`; `ast.Assert` 0 in the grader, the instrument, the aggregate and cpuset scripts; the counter shown to count a planted one).

**Registered exposures:** (a) the deadline class — if the np = 4 per-major rate is worse than half of D8's np = 1 rate, the cap wall cuts an O arm before `max_iter` (`rc=124`, `NOT A RESULT` on that arm and the chain stops: D5-PREREG-DEF-1's shape, named in advance rather than met); (b) IPOPT may terminate on a restoration failure near a constrained optimum with `nlp_scaling_method none` — `NOT A RESULT`, reported with its `EXIT` line; (c) the SHIPPED row's optimisation runs on stock IDWarp with twist DVs that cross the warp: its terminus and endpoint FD are the falsifier (P2/P4), not a defect of the item; (d) the endpoint FD steps are D8's endpoint rungs; at a converged optimum the reduced gradient is small but the raw `dCD/dtwist` components are not (they balance the CL multiplier) — a component without a plateau is `NOT A RESULT` by name and the row still grades on ≥ 3.

## 4. COST — DERIVED FROM D8's OWN MEASURED ANCHORS, NOT MEASURED; the ratio to D8 stated

**Per-major model at np = 1, from `LANE_REPORT.md` §8:** `wall(N) ≈ 231 (container, imports, mesh) + 409 (cold primal + 5 trim primals) + 591 (colouring) + 820 (N + 1) (two flow adjoints per gradient evaluation, N + 1 evaluations) + 65 N (warm primals) + 60 (endpoint primal) + 816 (two endpoint adjoints, CD and CL) = 2,927 + 885 N` seconds; the model reproduces D8's measured 5,251 s at N = 3 to **1.5 %** (5,174 s, one endpoint adjoint). **Predicted majors: 20** (point; band [8, 30]): D1/D13's IPOPT with the same option block on 9 DVs converged in **9–11** majors (C-71), A4 in 9, D4's 96-DV wing in 80; D8's 9-DV problem is the D1 class doubled for the transonic CL-coupled trim. **Budget `max_iter = 30 = 1.5 × 20`**, IPOPT's own stop, reported-then-stopped.

**Parallel scaling, an ASSUMPTION stated as such:** no np = 4 anchor exists for this case (D8 ran np = 1 by registration). The point takes **75 %** efficiency (D7FR delivered 3.99 of 4 cores on the same solver and cell count; C-125), the cap **50 %**.

| arm | derivation | point (core-min) | cap (core-min) | in-container wall | mem |
|---|---|---|---|---|---|
| O-P, O-S | `wall(20) = 20,627 s` at np = 1 = 343.8 core-min, ÷ 0.75 | **458.0** each | **1,000.0** each = `wall(30)` 29,477 s = 491.3 core-min ÷ 0.50 = 982.6, rounded up — **the deadline (15,000 s at 4 ranks) contains 30 majors at half of D8's rate** | 15,000 s (4 ranks) | **14g** |
| F-P, F-S | 32 primals × 78.50 s (D8 warm, C-76) + 231 s = 2,743 s = 45.7 core-min, ÷ 0.75 | **61.0** each | **120.0** each (÷ 0.50 = 91.4, rounded up) | 1,800 s (4 ranks) | 6g |
| **total** | | **1,038.0** point, band **[500, 1,800]** | **ceiling 2,240.0 = Σ caps** | ≈ 4.3 h wall at the point (+ 4 H5 windows), ≈ 9.3 h at the caps | |

**Ratio to D8's measured (C-76): O arm point 458.0 / 87.517 = 5.2× per row; item point 1,038.0 / 131.101 = 7.9×; ceiling 17.1×.** **`cost_basis`: c7a.4xlarge at $0.0513/core-h, REPORTED-BY-OWNER, NOT MEASURED** (`COMPUTE_BUDGET_CHARTER.md` §5). Dollars **DERIVED**: point **$0.8875**, ceiling **$1.9152**. The queue entry carries `cap_core_min_registered 2240.0` (the runner's `CAP_OVERRUN.txt` at 2,240 × 60 / 4 = 33,600 s, report-only). Which way this errs: D8's arm `opt` landed at 0.980 of its prediction and its `fd` at 0.750 (warm primals) — the primal basis here is already the warm figure; the major count is the open variable and the band carries it. Calibration row owed at completion, per row.

## 5. PLACEMENT, MEMORY AND THE DETACHED FORM — the aggregate arithmetic stated, not assumed

As D16 §5 with **cpuset `0,1,12,15`** (the supervisor's placement, verbatim *"choose 4 ranks on 0,1,12,15 and disclose"*): **disclosed — it includes core 0** (the family's earlier placements avoided it) **and overlaps D17's `12,15`**; both drivers carry **G-CPUSET** (`d8r_cpuset_overlap.py` = D17's): a live container sharing a core → wait-and-retry (poll 30 s, bound 14,400 s, `rc=6 CPUSET_BLOCKED_AT_BOUND`, zero compute), so whichever of D17/D8R fires second waits for the other's arm to end and G12's delivered-cores floor is never failed by the overlap.

**Memory — the arm that IS feasible, with the numbers:** the O arms carry the adjoint: D8 measured **9.970 GiB at np = 1** (cap 12g); at np = 4 the distributed Jacobian is the same total, plus per-rank Python/OpenMDAO/OpenFOAM overhead of order 4 × 0.6 GiB (D8's primal-only arm: 0.658 GiB) → ≈ **12.4 GiB expected — 12g is NOT defensible; the O arms are capped at 14g** (1.6 GiB headroom; G11 is hard: an `OOMKilled` O arm is `NOT A RESULT` on that arm and the chain stops — D8's censoring rule, *"a cap-kill is a measurement of the cap"*, applies to the reading and the registration stands). F arms (primals only) **6g**. H5 floors per arm (cap + 4, the D4-SHIPPED 4.4 rule): **O 18.0 GiB, F 10.0 GiB**; the queue entry's `memory_floor_gb` is 18.0.

**The aggregate rule (Σ live caps + this cap + host non-container RSS < 30.6 GiB), with the numbers at this write:** host non-container RSS measured **6.52 GiB** by `d17_aggregate_memory.py` at 22:23Z (other teams' non-container solvers — heat-transfer's 8-rank `T3_R_ff`, cfd's F18b — count as host RSS; the board's ≈ 3.7 was yesterday's reading). Cases: (i) **beside D5 (12g, 8,10,11,13): 12 + 14 + 6.5 = 32.5 > 30.6 → the O arm WAITS** (bounded 14,400 s; D5 r3's remaining chain is ≈ 813 core-min ≈ 3.4 h at 4 ranks plus its own windows, so the bound may be met — `rc=6 AGGREGATE_BLOCKED_AT_BOUND`, zero compute, the entry re-filed after D5; a registered outcome, not a defect); (ii) **beside D15/D16/D17 only (4–8g): 8 + 14 + 6.5 = 28.5 → runs**; (iii) **beside D6 (20g, after D5's `CHAIN_DONE`): 20 + 14 + 6.5 = 40.5 → whichever fires second waits, bounded 4 h, and D6's own wait-wrapper/aggregate bounds are its registered behaviour**; (iv) alone: 14 + 6.5 = 20.5. **On the runner's first-fit order** (oldest first within dafoam: `D5_chain_r3` 21:03Z, `D6_chain_wait`, `D15`, `D16`, `D17`, then this entry) D5 fires before D8R, so case (i) is the expected one; **the ordering is on the supervisor's desk (§9): as filed, D8R waits on its own guard; alternatively the supervisor re-files it as a `dafoam_wait_then_launch.sh` entry behind D6's chain end.** The runner's own gate (`MemAvailable ≥ 18.0`, busy < 85 %, busy-cores + 4 ≤ 14.4) is upstream of all of this.

The detached driver with `STATUS.<arm>` / `STATUS.chain` / `d8r_driver.pid`, no `--rm`, the `.inspect.txt` kernel record, **`CHAIN_DONE`** appended on every exit of a started chain, `rc` from `docker inspect` inside the driver, and G-ROOT.1–.5 from birth (forbidden: D4, D4-SHIPPED(-R), D5, D6, D14(M), D7R, D7FR, D12R*, D13, D15, D16, **D17, D8, P2-a6-n16, P3-a6-n16-ref, P3-a6-n16-rem**, A2-mach-wing, the tutorial checkout, the repo). **G-ROOT.5 DEMONSTRATED 2026-08-26T22:41:39Z (`d8r_groot5_selftest_evidence.txt`, 18/18):** sacrificial `sleep` container on cpuset 1 → `rc=3`; sacrificial live pid 558946 with cwd = run root → `rc=3`; stale pidfile ignored; clear passes to the L-251 check; D4/D5/**D8**/**D17** roots refuse at G-ROOT.1; G-ROW refuses `O-P` on shipped and `F-S` on patched, passes `O-S` on shipped which then refuses for want of D8's staged mesh; root empty and absent afterwards.

## 6. PREDICTIONS — scored HIT/MISS by the comparator, never adjusted

| # | prediction | value / band |
|---|---|---|
| **P1** | the PATCHED row prints **`EXIT: Optimal Solution Found.`** within the budget (G-O PASS) | HIT/MISS; majors point 20 |
| **P2** | the SHIPPED row's terminus is **`EXIT: Maximum Number of Iterations Exceeded.`** at 30 majors (the D4-SHIPPED precedent: 100 majors at `max_iter` against the patched row's 80 `Optimal`) — hence SHIPPED `GATE REACHED` at best | HIT/MISS |
| **P3** | the PATCHED endpoint FD **`PASS` 5 of 5** in band D (D8's endpoint worst 2.65 %; D7FR's 1.96 %) | point 5/5, worst ≤ 3 % |
| **P4** | the SHIPPED endpoint FD **`GATE FAIL`** — ≥ 1 of 5 outside band D or sign-flipped (A6 N=16's shipped gradient row is `GATE FAIL` on record; twist DVs cross the warp) | HIT / MISS / `NOT_MEASURED` |
| **P5** | the PATCHED drag reduction from the trimmed start lies in **[0.29 %, 5.0 %]** (G3); point **1.0 %** | HIT/MISS |
| **P6** | total graded core-min in **[500, 1,800]** (point 1,038) | scored |
| **P7** | PATCHED majors in **[8, 30]** (point 20) | scored |

**Predicted outcome:** P1, P3, P5, P6, P7 HIT; P2, P4 HIT → PATCHED `PASS`, SHIPPED `GATE REACHED` on G-O and `GATE FAIL` on G5 → **item `GATE FAIL`** with the PATCHED row's `PASS` carried into the grid's optimisation column as *"1 converged to tolerance (patched), endpoint FD-verified; shipped cap-stopped"* → `CAN DO, CAVEATS`. **MISS on P2 and P4** (the shipped row converges and its endpoint FD passes) would be the finding that the rotation defect does not reach this twist-only problem — reported as such, never averaged.

## 7. INSTRUMENTS, FROZEN BY MD5 AT THIS COMMIT — copy-and-delta from D16 (and D17 for the driver's two additions)

| file | md5 | derivation, and the DELTAS file the supervisor reads |
|---|---|---|
| `d8r_run_arm.sh` | `95beeed3a5dad0d715f3f1b2836ab973` | `curriculum_D16/d16_run_arm.sh` (`bf29bd90…`) + **`d8r_run_arm_DELTAS_from_d16.diff` (296 diff lines)**: item/root names; forbidden roots (+D16, D17, D8, P2/P3-a6); cpuset `0,1,12,15`; arms O-P F-P O-S F-S at **4 ranks**; cap/memory table (O 1000.0 / 14g, F 120.0 / 6g); producer md5 `28c78194…`, decomposeParDict md5 `1dbd9ead…`, `MD5_POINTS`; G-ROW without MESH; **the staging block** (every arm cold from `base/`, points.gz asserted, the F arm's endpoint copy + `rc=0` ledger check for its O arm, the G-COLD list); the arm commands. Container-printed strings (`D4S_*`), the cap assertion, the in-container deadline, the sampler, the inspect record: unchanged |
| `d8r_chain_driver.sh` | `34982b002393c5cd76a76bbef9d84748` | `curriculum_D16/d16_chain_driver.sh` (`3214efed…`) + **`d8r_chain_driver_DELTAS_from_d16.diff` (256 lines)**: names/root; `D8_BASE` (read-only source) in place of `TUT_SRC`, `tut_md5_list` of **23** D8 inputs; staging copies `0 FFD constant system runScript.py`; per-arm `cap_mem_gib` (14/6) and `h5_floor_gib` (18/10); no MESH in `img_of`; **G-CPUSET wait-and-retry and the `CHAIN_DONE` trap exactly as D17's driver** |
| `d8r_of.py` | `5a399b427331ab8eab1fcdc1a243b3aa` | `curriculum_D16/d16_xf.py` (`c78eaaaa…`) + **`d8r_of_DELTAS_from_d16_xf.diff` (453 lines)**: mode O (the optimisation, D8's trim call, IPOPT options with `MAX_ITER = 30`, the terminus reader) in place of mode X; mode F reads the endpoint file and sets the endpoint design; `COMPONENTS` twist {0,1,3,4,5}, `STEPS` twist {3e-2, 1e-1, 3e-1}, `CTRL_STEP 1e-1`; artefact/marker names. CTRL control, emit/fsync, η, the plant read-back, the in-process `.so` md5: unchanged |
| `d8r_grade.py` | `3f6eafac2ad4897417521d00c1cc5f3e` | `curriculum_D16/d16_grade.py` (`0b8338b3…`) + **`d8r_grade_DELTAS_from_d16.diff` (764 lines — read as a file)**: constants (arms, caps, ceiling 2,240.0, predictions, `POINTS_MD5`, `NPROCS_REGISTERED 4`, `MAX_ITER`, `G2_CL_TOL`, `ETA_D8`, `CD_COLD_REGISTERED`, `DROP_BAND_PCT`, cpuset, delivered floor 3.0); `read_O` in place of `read_X`; **G-O / G2 / G3 / G-BASE and the row/item composition**; G-M2 by points.gz md5 on every arm; the endpoint-identity and `nprocs` refusals; P1–P7; the fixture and 9 new units (`EXPECTED_UNITS = 31`). Ledger regex, L-342 classes, inspect fallback, `grade_components` (bands D/E, plateau, sign flip), the planted controls, G9/G10/G12: unchanged. **31/31 plain and `-O`** |
| `d8r_groot5_selftest.sh` | `570e737578eefd63e6b71b4a94a748ee` | `curriculum_D16/d16_groot5_selftest.sh` + **`d8r_groot5_selftest_DELTAS_from_d16.diff` (108 lines)**: names; arm `O-S`; sacrificial cpuset 1; D8's and D17's roots in (d); (e) on O-P/F-S/O-S with the "no staged mesh" refusal |
| `d8r_cpuset_overlap.py` | `c57598a84b46e82c05be2ecb483fa301` | `curriculum_D17_cone_supersonic/d17_cpuset_overlap.py` renamed (prefix `d8r_`; one docstring clause) |
| `d8r_aggregate_memory.py` | `709ab0b98ef0302a3a3a318588f9493f` | byte-identical to D16's / D17's / D5's (`cmp` equal) |
| `d8r_runScript.py` | `28c7819487a025a5f6554d38062a2b66` | **byte copy** of `/home/ubuntu/certonomous-runs/CURRICULUM-D8-a6-twist-opt/opt/runScript.py` — D8's frozen `d8_gen_arm.py` (`0b8a8b34…`) output from `base/runScript.py` (`0de915d2…`), byte identity proved in D8 `LANE_REPORT.md` §9; its `max_iter 3` sits below the anchor and is never executed |
| `d8r_decomposeParDict` | `1dbd9ead3f40a29f483444dc5fa1288b` | byte copy of D8 `base/system/decomposeParDict` (`numberOfSubdomains 4`, `scotch`) — overlaid on itself so the family form holds |
| D8 base inputs, md5-asserted at staging (23) | `runScript.py` `0de915d2…`; `0/{T,U,alphat,nuTilda,nut,p}` `8f100131… d298286a… d381ad50… 55ef53cb… 16be134b… 96fabd08…`; `system/{controlDict,createPatchDict,decomposeParDict,fvSchemes,fvSolution}` `527a589e… 5ef8b5ec… 1dbd9ead… bfe390ae… 36a8ad5c…`; `constant/polyMesh/{boundary,cellZones,faceZones,faces,neighbour,owner,pointZones,points}.gz` `7d5ff100… 01003fa5… c9c0f2a9… 67dfa0da… a52672f6… ba0ca1bc… dedc6402… 11b84f0d…`; `constant/{thermophysicalProperties,turbulenceProperties}` `2f5e9885… 03f244f2…`; `FFD/wingFFD.xyz` `905ade2c…` | D8's run root is outside git, so its bytes are frozen here |

No `assert` in any python file (AST count 0, in the evidence file). Classifier denials while building: none.

## 8. WHAT THIS ITEM WILL NOT ESTABLISH

D8 §12 verbatim (no A6 N=29, no 399,360-cell A3, no D16a; `twist` idx6 not repaired; `shape` not graded and `PENDING` for the family; A6 overall `BLOCKED` unchanged), plus: nothing toolchain-independent unless **both** rows PASS; nothing about the optimum's uniqueness (one start per row; D13's basin finding stands); nothing about `patchV`'s endpoint gradient (not in the registered subset); no GCI; nothing about the np = 1 ↔ np = 4 primal agreement beyond G-BASE's 10 η band.

## 9. FREEZE, QUEUE, AND WHAT IS ON THE SUPERVISOR'S DESK

**Committed BEFORE any container starts.** Grading path fixed at this commit: `d8r_grade.py` md5 `3f6eafac2ad4897417521d00c1cc5f3e`. **Queue entry `verification/queue/dafoam/D8R_chain.json`:** team `dafoam`, `prereg_commit` = the sha of the commit introducing this file, `launch_cmd` = `["bash", "<abs>/d8r_chain_driver.sh", "O-P", "F-P", "O-S", "F-S"]`, `cwd` = this directory, `ranks 4`, `cost_core_min_estimate 1038.0`, `cap_core_min_registered 2240.0`, `memory_floor_gb 18.0`, `cost_basis` derived / not measured, `permission bc0e687e`. Enqueueing is not authorisation (check 4 is the supervisor's). **The runner is the launch path and is HELD at its 85 % busy ceiling by other teams' load; nothing is launched by hand.** Filing check: `python3 scripts/check_filing.py` adds no violation under `cases/dafoam/` for this directory (§9 of D17 records the same reading). **On the supervisor's desk, a placement decision, not a physics one:** whether D8R stays a first-fit entry (it waits on its own aggregate guard beside D5 and may `BLOCK` at the 4 h bound, zero compute, to be re-filed) or is re-filed as a wait-wrapper behind D6's chain end (§5). **Predicted outcome:** PATCHED `PASS`, SHIPPED `GATE REACHED` / `GATE FAIL`, item **`GATE FAIL`**; the `3D · steady · transonic` optimisation cell moves from "0 converged, 2 cap-stopped" to `CAN DO, CAVEATS` with one converged, FD-verified endpoint on record.
