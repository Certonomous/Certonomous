# A3GC L2 convergence prediction — PROVENANCE OF ITS PRIORITY

The prediction beside this file
(`A3GC_L2_CONVERGENCE_PREDICTION_REGISTERED_BEFORE_RUN.txt`) is a **byte-identical
copy** of the file written into the run area at
`/home/ubuntu/certonomous-runs/A3GC_L2_CONVERGENCE_PREDICTION_REGISTERED_BEFORE_RUN.txt`.
It is copied into git by the `dafoam-supervisor` on 2026-09-12 because **a
prediction registered before a run that lives only outside git has no durable proof
of its priority** — the run area is not version-controlled and a cleanup would
destroy exactly the evidence that makes the prediction worth anything.

**IT IS NOT A GATE.** It registers nothing, moves no band and authorises no compute.
`PREREGISTRATION.md` §3.6's accept floor and §4.2's Roache limbs stand exactly as
frozen.

## The priority claim, and what actually supports it

| fact | value |
|---|---|
| prediction file mtime | **2026-09-11T23:51:04Z** |
| prediction file sha256 | `3af45c6c39efbb001ab627c612298f82583b53490a380d8c9560fef7fd5eb74d` |
| A3GC L2 first solver artifact (`/home/ubuntu/certonomous-runs/A3GC-L2/primal.log`) | **2026-09-12T00:38:51Z** |
| margin | **47 min 47 s** |

**The priority rests on the mtime and on the solver's own first artifact, NOT on this
commit** — this file is committed after the launch and says so rather than letting a
git timestamp imply something it cannot support. The sha256 above is what makes the
copy checkable: it is recorded so that a later reader can prove the committed text is
the text that predated the run, rather than a tidied version of it.

## The supervisor's standing position on what this prediction can and cannot do

Under `PREREGISTRATION.md` §4.2 clause 1, §4.4 and AMENDMENT 3(e)'s worst-first
composition, **the A3GC triple's verdict is already fixed at `NOT A RESULT`** on L3's
evidence alone (p 2.290019532e-05 and nuTilda 6.812089e-06, both above §3.6 limb 2's
1e-06). **L2 cannot move that**, and nothing in this prediction may be read as
suggesting it can. L2 is run to answer the one question a successor registration
depends on — whether the L3 plateau is a coarse-resolution artefact — and it is
**diagnostic within a frozen item, not a rescue**.

**SUBMISSIONS PARKED.**
