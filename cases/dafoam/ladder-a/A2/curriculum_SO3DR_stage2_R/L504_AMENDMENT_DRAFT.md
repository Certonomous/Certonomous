DRAFT — L-504 amendment addendum, authored by lab-lane (dafoam) for the dafoam-supervisor/chief to LAND into `docs/LESSONS.md` at commit. NOT LANDED. Re-derive the lesson number at commit per rule 11 (L-504 already exists — this is a dated addendum appended at the foot of the existing L-504 block, `lines whose number changed above this section: 0`, per rule 6). Every cited sha must be re-verified landed on disk before this is appended.

---

**AMENDMENT — 2026-09-07 (dafoam-supervisor, authored by lab-lane). lines whose number changed above this section: 0.** The original L-504 block is unrewritten; this dated addendum records the ENFORCING INSTRUMENT that turns clause-(c)'s producer-side "guard every expected-refusal path" from a repeatedly-paid lesson into a static, pre-freeze invariant.

*Refinement 3 — THE WRITE-BEFORE-RUN INVARIANT IS NOW STATICALLY ENFORCED (operationalises clause (c), producer side, per rule 14).* Clause (c) as written requires every expected-refusal path to be guarded producer-side so a product/echo survives DAFoam's EXPECTED `AnalysisError("Primal solution failed!")` raise. This session paid the **same write-after-run class a FOURTH time**: an INPUT echo-sidecar (`injected_dv.json`) written AFTER `prob.run_model()` is never reached on a primal-raised leg, the sidecar is absent, and the grader's F4 echo-check hard-REFUSES (exit 2) on exactly the legs the experiment counts. Occurrences: **D6RF5** (present-but-empty product; L-504 layer 2), **D6RF6** (file-absent vs present-but-empty control gap; layer 3), **D9successor** (FD-record path/derivation), and **SO3DR-F4** (the echo-sidecar written at frozen rig line 328, after `run_model` at line 312 — the 32 primal-raised legs never reached it; verification ruled a §2d.1 value-invariant repair unavailable and mandated a fresh successor, `curriculum_SO3DR_stage2_R`).

EXECUTABLE REMEDY (record it, per rule 14): **`scripts/check_sidecar_before_run.py`** — a STATIC, AST-based pre-freeze check that REFUSES (exit 3) if a sidecar-echo write (an `open(path, "w"…)` whose path carries an echo token — `injected_dv` / `sidecar` / `_echo` — resolved through one level of name binding) has a source line AFTER a run-trigger call (`run_model` / `run` / `run_driver`). The single conservative rule: REFUSE iff `max(write_lines) > min(run_lines)` — an input echo must precede EVERY run trigger. It carries a planted-control `--selftest` (rule 3 family): it must be shown to FIRE on a synthetic write-after-run fixture AND PASS on a write-before-run fixture, so a green result is evidence the checker has teeth. Demonstrated both directions on the REAL files: it REFUSES the frozen Stage-2 parent rig (write 328 after run 312 — the defect itself) and PASSES the ordering-fixed successor rig (write 352 ≤ run 359).

HONEST LIMITS (stated in the instrument's own docstring): it is a NECESSARY not sufficient guard — it enforces ORDERING only, not that the echoed fields are pre-run knowable (a human field-by-field review, done for SO3DR-F4); it does not follow the call graph (a write inside a helper called after the run is out of scope — keep the echo write inline in `main()`); and a path built with NO token-bearing string literal (pure variable / argv / f-string) is not resolved. It is added to the L-504 freeze pre-conditions: a rename-derived or successor freeze runs this check on the rig and requires PASS as part of clause (c).

*Related (this amendment).* Grounded files: `scripts/check_sidecar_before_run.py`; `cases/dafoam/ladder-a/A2/curriculum_SO3DR_stage2_R/` (successor rig, re-frozen grader, DRAFT prereg); the frozen parent `cases/dafoam/ladder-a/A2/curriculum_SO3DR_stage2/so3dr_stage2_standalone_runScript.py` (md5 `dc67cced46235f897f6257353402ea57`) is the write-after-run exemplar. Verification ruling V-127 (relayed by the chief) is the authority for the fresh-successor vehicle over an in-place §2d.1 repair.
