D6RF4 P_conv WATCHER -- READERS DRIVEN IN BOTH DIRECTIONS
  planted p initRes    = 1.234003e-03  (appears nowhere in this file's logic)
  planted nuTilda      = 5.678009e-04
  planted Time         = 747
  OK    direction 1  PLANTED LOG     state=READ Time=747 p=1.234003e-03 nuTilda=5.678009e-04 all6_fields=True -- the reader SEES a non-zero it was never told
  OK    direction 2  ABSENT LOG      state=SOURCE_ABSENT blocks=0 -- absence reports its own token, NOT an empty residual set that would read as convergence
  OK    direction 3  EMPTY LOG       state=SOURCE_EMPTY -- an empty file is NOT the same token as a missing one
  OK    direction 4  NO TIME BLOCKS  state=NO_TIME_BLOCKS -- a started-but-uniterated solver is its own state
  OK    direction 5  MISSING FIELD   absent=U0,U1,U2,he,nuTilda -- a field the log does not carry is REPORTED ABSENT, never defaulted to a passing value (nuTilda is the SECOND binding field)
  OK    direction 6  ROOT ABSENT rc=None  state=ROOT_ABSENT_AWAITING_DAEMON -- a sustained ABSENT is legible as its CAUSE, not as one undifferentiated fault
  OK    direction 6  ROOT ABSENT rc=4     state=ROOT_ABSENT_LAUNCHER_ABORTED -- a sustained ABSENT is legible as its CAUSE, not as one undifferentiated fault
  OK    direction 6  ROOT ABSENT rc=0     state=ROOT_ABSENT_UNEXPLAINED -- a sustained ABSENT is legible as its CAUSE, not as one undifferentiated fault
  OK    direction 6  ROOT PRESENT       state=ROOT_PRESENT mode=775 -- and the mode is REPORTED because L-251 gates on it
  OK    direction 7  FLOOR CONTROL   state=CONTROL_RAN rc=2 -- the watcher CALLS the frozen instrument; it does not re-implement the floor reader, so there is only ever ONE answer
  OK    direction 8  SELF-TERMINATE  8/8 input combos as specified; terminal ONLY on (no container, rc present, log settled); a live container or a still-growing log KEEPS the watch -- so it cannot become a corpse watching a corpse, and cannot exit on a run still spinning up

RESULT ALL DIRECTIONS AS REGISTERED
driven 2026-09-06T18:11:11Z
