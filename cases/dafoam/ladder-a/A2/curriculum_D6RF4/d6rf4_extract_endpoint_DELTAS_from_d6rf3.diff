--- /home/ubuntu/Certonomous/cases/dafoam/ladder-a/A2/curriculum_D6RF3/d6rf3_extract_endpoint.py	2026-09-05 21:25:57.529759274 +0000
+++ d6rf4_extract_endpoint.py	2026-09-06 01:57:43.657261709 +0000
@@ -1,5 +1,5 @@
 #!/usr/bin/env python
-"""Curriculum D6RF3 -- extract the optimiser's FINAL design point and the
+"""Curriculum D6RF4 -- extract the optimiser's FINAL design point and the
 per-major COMPOSITE OBJECTIVE from the pyOptSparse history FILE (`OptView.hst`),
 never from stdout.  IT READS DESIGN VARIABLES AND `J` ONLY.
 
@@ -28,7 +28,7 @@
   * `J` IS KEPT.  `R-RED` (REPORTED, NOT GATED) reads it, and section 3c
     requires that row to disclose non-finiteness rather than print a number
     derived from a `NaN`.
-  * Product names are this item's own (`d6rf3_*`), not `D6R`'s.  A product
+  * Product names are this item's own (`d6rf4_*`), not `D6R`'s.  A product
     registered under one name and written under another is `D6RF3-DEF-6`,
     measured in `D6RF2` and named in `PREREGISTRATION.md` section 1f.
 
@@ -55,13 +55,13 @@
 import sys
 
 HST = "OptView.hst"
-OUT = "d6rf3_endpoint_dvs.json"
-HIST_OUT = "d6rf3_major_history.json"
+OUT = "d6rf4_endpoint_dvs.json"
+HIST_OUT = "d6rf4_major_history.json"
 KEYS = ("twist", "shape", "patchV_cl04", "patchV_cl05", "patchV_cl06")
 
 
 def refuse(msg):
-    sys.stderr.write("D6RF3_EXTRACT REFUSE %s\n" % msg)
+    sys.stderr.write("D6RF4_EXTRACT REFUSE %s\n" % msg)
     sys.exit(2)
 
 
@@ -106,11 +106,11 @@
                "registered OptView.hst found NO CD in any structure of any of "
                "1,007 records. CD_i(mp) for this item comes from "
                "PREREGISTRATION.md section 2a -- the FD baseline primal, "
-               "d6rf3_fd_endpoint.json points.<pt>.CD -- and NOT from here.")
+               "d6rf4_fd_endpoint.json points.<pt>.CD -- and NOT from here.")
     out["_cd_block_dropped"] = dropped
     hist["_cd_block_dropped"] = dropped
-    out["_extractor"] = "d6rf3_extract_endpoint.py"
-    hist["_extractor"] = "d6rf3_extract_endpoint.py"
+    out["_extractor"] = "d6rf4_extract_endpoint.py"
+    hist["_extractor"] = "d6rf4_extract_endpoint.py"
     with open(HIST_OUT, "w") as fh:
         json.dump(hist, fh, indent=1, sort_keys=True)
         fh.flush()
@@ -119,7 +119,7 @@
         json.dump(out, fh, indent=1, sort_keys=True)
         fh.flush()
         os.fsync(fh.fileno())
-    sys.stdout.write("D6RF3_ENDPOINT_DVS_WRITTEN %s n_major_rows=%d n_shape=%d "
+    sys.stdout.write("D6RF4_ENDPOINT_DVS_WRITTEN %s n_major_rows=%d n_shape=%d "
                      "history=%s\n"
                      % (OUT, n_major, len(out["shape"]), HIST_OUT))
 
