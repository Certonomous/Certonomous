# Curriculum D5 — FFD parametrisation study on D4's problem: three FFD densities, same everything else

**DRAFT v0.1, 2026-08-26. NOT FROZEN. NOT A PRE-REGISTRATION UNTIL RE-COMMITTED AS `PREREGISTRATION.md` WITH A FREEZE SECTION.**
Drafted by dafoam `lab-lane` at a teammate's request for `dafoam-supervisor`'s freeze decision. Nothing here is fired, filed or sent (rule 7).
Short form: everything not stated here is **inherited verbatim from D4-SHIPPED-R's family** (`curriculum_D4_SHIPPED/d4s_run_arm.sh`, ruling R4 amended at `6109aa45`) and from `curriculum_D4/PREREGISTRATION.md` §1, §5, §6, §7, §9, §10. Curriculum row: `cases/dafoam/EXPERTISE_CURRICULUM.md:95`.

## 1. What changes, and only this

| | D4 (PATCHED row, `GATE REACHED`, C-97) | D5 |
|---|---|---|
| case, mesh, np, decomposition | A2 wing, 38,304 cells, np=4 `scotch` | **identical** |
| FFD box | `FFD/wingFFD.xyz` 6×2×8 → 96 local `shape` DVs | **three densities: 4×2×6 (48 DVs), 6×2×8 (96 — D4's own), 8×2×12 (192 DVs)**, same bounding box, same `wingAxis` at `xFraction=0.25`, same twist (7) and `patchV` DVs |
| objective / constraints / optimizer | CD at CL=0.5, IPOPT `max_iter` 100 | **identical**, `d4_opt_runScript.py` with ONE line changed per density (the FFD path) — the diff is registered as a diff, read by the supervisor |
| launcher family | `d4s_run_arm.sh` | derived copy `d5_run_arm.sh`: G-ROOT.1–3 from birth naming BOTH D4 roots and D4-SHIPPED(-R) roots as forbidden; explicit `--cpuset-cpus` chosen by measurement at freeze (draft proposes `8,10,11,13`; siblings hold `2,3,4,6` / `5,6,7,9` / `12` today); H5 `MemAvailable ≥ 14.0 GiB` window before each arm; rc from `docker inspect .State.ExitCode/.OOMKilled`, **no `--rm`**; the deadline runs **inside** the container command (`timeout` inside `bash -lc`, D4-SHIPPED-R's closure of the cap-dies-with-shell exposure); post-container bookkeeping in a `setsid nohup` driver, rc captured inside it into `STATUS.<arm>`; **no bare `assert`** (L-332): every guard is an `if … exit`; launch record cites `bc0e687e` |

**The 6×2×8 density is NOT re-bought.** D4's PATCHED `O/` artefacts (`GATE REACHED`, 80 majors, `EXIT: Optimal Solution Found.`, `CD_f = 2.1125978108239574e-02`) are **consumed as density 2**, the same way D4-SHIPPED-R consumes its preserved `O/`. They are **recorded, not imported**: the cross-density comparator re-reads them from disk and re-derives CD_f itself.

## 2. Arms

| arm | density | task | new compute |
|---|---|---|---|
| O48 | 4×2×6 | IPOPT run_driver, `max_iter` 100 | yes |
| O96 | 6×2×8 | — (D4 PATCHED `O/` consumed) | **no** |
| O192 | 8×2×12 | IPOPT run_driver, `max_iter` 100 | yes |
| ACC48, ACC192 | as above | cold primal at the endpoint, D4-DEF-4 acceptance shape | yes |
| F48, F192 | as above | endpoint FD table, the bright line, **five registered components per density** (the idx46-class near-zero components named in advance from the endpoint locus, D4 §6 procedure) | yes |

The FFD boxes are generated by one script `d5_gen_ffd.py` (pygeo-style Plot3D writer) whose three outputs are md5-registered at freeze; the 6×2×8 output must be **byte-identical** to D4's `base/FFD/wingFFD.xyz` — a planted control that the generator reproduces the parametrisation it claims to refine.

## 3. Gates (per density as D4 G1–G13; plus the cross-density gate)

* **G-D5-1 cross-density metric, registered here:** `ΔCD(d) = CD_f(d) − CD_f(96)` for d ∈ {48, 192}, read from each density's `opt_IPOPT.txt` final objective by the comparator, never from the runScript's own print. Band: **|ΔCD(192)| ≤ 3.0e-4** (1.4 % of CD_f(96); ≈ 9× D4's measured F3 aggregate FD error 0.1634 % so the band is above the verifiable resolution, per the curriculum's own warning) → `PASS`; else `GATE FAIL`. **ΔCD(48) carries NO band** — it is the diagnostic that the coarser box is worse, scored by sign only in P2.
* **The "optimizer exploits the parametrisation" pathology, named in advance:** a density whose F table has ≥ 2 sign flips among the five registered components is `NOT A RESULT` for that density, and its CD_f is quoted only beside that label. Rule 5 has no row (no grid family): **no GCI is quoted anywhere in D5.**

## 4. Cost — measured-derived from D4's own rows, never from the curriculum's range

| arm | anchor | prediction (core-min) | cap |
|---|---|---|---|
| O48 | 6.389 core-min/major (D4 PATCHED, 511.133/80, `RESULTS.md` §cost) × 100 majors | **638.9** | 800.0 |
| O192 | same anchor; the adjoint solve is DV-independent, the `dRdX`/FFD products are not — **exposure stated: the anchor may under-price 192 DVs** | **638.9** | 800.0 |
| ACC48 + ACC192 | 3.0 each (C-94) | **6.0** | 10.0 each |
| F48 + F192 | 47.267 each (C-96; five components regardless of density) | **94.5** | 120.0 each |
| comparator | zero compute | 0 | — |
| **total** | | **1,378.3 core-min = $1.18 DERIVED, NOT MEASURED** (c7a.4xlarge $0.0513/core-h, reported-by-owner) | ceiling **1,860.0** (sum of caps) |

**Below the curriculum's 1,500–2,250 because density 2 is not re-bought**, not because anything is cheaper. Per-arm cap asserted inside the launcher against the table above (D4-SHIPPED family); crossing is reported and the run continues to a 4× ceiling only by the supervisor's decision.

## 5. Predictions, scored HIT/MISS afterwards, never adjusted

| # | prediction | number |
|---|---|---|
| P1 | both new densities print `EXIT: Optimal Solution Found.` within `max_iter` 100 | majors in **[60, 100]** each |
| P2 | the coarser box is worse: `ΔCD(48) > 0` | sign only |
| P3 | the finer box gains little: `ΔCD(192) ∈ [−3.0e-4, 0]`, i.e. CD_f(192) ≤ CD_f(96) and within the G-D5-1 band | **point −1.0e-4** |
| P4 | O48 costs **[450, 700]** core-min, point 638.9; O192 **[550, 850]**, point 638.9 — the asymmetric band states the 192-DV exposure | |
| P5 | F48 and F192 each ≤ 1 sign flip among the five registered components (aggregate vector-relative error < 5 %, D4's band) | |

## 6. What this item does NOT claim
Nothing about the SHIPPED row (unbought here; the two-row rule names it unbought in every record). Nothing at np≠4 or another decomposition. No Strouhal, no grid family, no GCI. The 6×2×8 numbers are D4's and are cited, never re-derived into a new verdict.

## 7. Freeze — NOT YET
Freeze requires: the launcher diff read by the supervisor as a diff; `d5_gen_ffd.py` + three `.xyz` md5s; the cross-density comparator with its planted control; `test -e` on the run root `/home/ubuntu/certonomous-runs/CURRICULUM-D5-a2-wing-ffd-density` returning false, stamped; then a commit whose sha this document names.
