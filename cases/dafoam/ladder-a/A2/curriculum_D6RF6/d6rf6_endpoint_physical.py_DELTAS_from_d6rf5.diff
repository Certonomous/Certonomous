--- curriculum_D6RF5/d6rf5_endpoint_physical.py	2026-09-07 15:17:17.962232577 +0000
+++ curriculum_D6RF6/d6rf6_endpoint_physical.py	2026-09-07 15:58:30.730077308 +0000
@@ -1,20 +1,20 @@
 #!/usr/bin/env python
-"""Curriculum D6RF5 -- the MULTIPOINT endpoint repair, as a WRAPPER that edits
+"""Curriculum D6RF6 -- the MULTIPOINT endpoint repair, as a WRAPPER that edits
 zero bytes of any frozen file.
 
 DERIVED FROM `curriculum_D6RF2/d6rf2_endpoint_physical.py`
 (md5 `6e5fa9f9c2048b0665593282f62ba3dc`, verified on disk before this file was
-written); the deltas are in `d6rf5_endpoint_physical_DELTAS_from_d6rf2.diff`
+written); the deltas are in `d6rf6_endpoint_physical_DELTAS_from_d6rf5.diff`
 beside this file.  THREE OF THEM ARE COUPLED CONSEQUENCES OF ANOTHER RULING and
 are recorded here so a reader does not have to reconstruct why they moved:
 
-  D1  `EXTRACTOR` / `MD5_EXTRACT` are RE-PINNED to `d6rf5_extract_endpoint.py`.
+  D1  `EXTRACTOR` / `MD5_EXTRACT` are RE-PINNED to `d6rf6_extract_endpoint.py`.
       This file INVOKES the extractor (C3), so the supervisor's POSITION 1
       ruling on `PREREGISTRATION.md` section 9 -- a successor extractor reading
       DVs and `J` only -- does not land in the extractor alone.  Had this pin
       not moved, C1 would have refused on the first fire.
-  D2  Every product name is this item's own (`d6rf5_*`).
-  D3  ⚠ `PHYSICAL` IS `d6rf5_endpoint_dvs_PHYSICAL.json`, WHICH REPAIRS
+  D2  Every product name is this item's own (`d6rf6_*`).
+  D3  ⚠ `PHYSICAL` IS `d6rf6_endpoint_dvs_PHYSICAL.json`, WHICH REPAIRS
       `D6RF3-DEF-6`.  MEASURED IN THE FROZEN `D6RF2` SET: `d6rf2_grade.py:94`,
       `:104` and `:458` and `d6rf2_run_arm.sh:546` all register
       `d6rf2_endpoint_dvs_PHYSICAL.json`, while `d6rf2_endpoint_physical.py:75`
@@ -24,7 +24,7 @@
       producing arm ran, so `D6RF2` would have refused at grading even had every
       arm run clean.  This is `DAFOAM_CHARTER.md` section 18.3's failure mode
       exactly: a name in the instrument/product table that no instrument writes.
-      `d6rf5_grade.py` additionally carries an executable `PRODUCT_WRITER` check
+      `d6rf6_grade.py` additionally carries an executable `PRODUCT_WRITER` check
       so the class cannot recur silently.
 
 DERIVED FROM `cases/dafoam/ladder-a/A2/curriculum_D4/d4_endpoint_physical.py`
@@ -87,22 +87,22 @@
 
 HERE = os.path.dirname(os.path.abspath(__file__))
 sys.path.insert(0, HERE)
-import d6rf5_endpoint_locus as locus                                # noqa: E402
+import d6rf6_endpoint_locus as locus                                # noqa: E402
 
-EXTRACTOR = "d6rf5_extract_endpoint.py"
-RUNSCRIPT = "d6rf5_opt_runScript.py"
+EXTRACTOR = "d6rf6_extract_endpoint.py"
+RUNSCRIPT = "d6rf6_opt_runScript.py"
 # PREREGISTRATION.md section 7.  These are the FREEZE, not a preference.
-MD5_EXTRACT = "95630a223c638095cdfb4de5727d7a88"  # 2026-09-07 rule-2 pre-compute: was c5aace65 (the PARENT d6rf4_extract_endpoint.py md5, a rename-omission that made C1 refuse and abort the container exit 2); d6rf5_extract_endpoint.py on disk is 95630a22
+MD5_EXTRACT = "c93adc8b4d20c6097bb35e82592e6dae"  # 2026-09-07 rule-2 pre-compute: was c5aace65 (the PARENT d6rf4_extract_endpoint.py md5, a rename-omission that made C1 refuse and abort the container exit 2); d6rf6_extract_endpoint.py on disk is 95630a22
 MD5_RUNSCRIPT = "137539e0a99be27f27fdb69e063b2a87"
 
-SCALED_OUT = "d6rf5_endpoint_dvs.json"                 # the frozen extractor writes this
-PRESERVED = "d6rf5_endpoint_dvs_DRIVERSCALED.json"     # the pre-repair pre-image
-PHYSICAL = "d6rf5_endpoint_dvs_PHYSICAL.json"         # the corrected artefact
-HISTORY = "d6rf5_major_history.json"
+SCALED_OUT = "d6rf6_endpoint_dvs.json"                 # the frozen extractor writes this
+PRESERVED = "d6rf6_endpoint_dvs_DRIVERSCALED.json"     # the pre-repair pre-image
+PHYSICAL = "d6rf6_endpoint_dvs_PHYSICAL.json"         # the corrected artefact
+HISTORY = "d6rf6_major_history.json"
 
 
 def refuse(msg):
-    sys.stderr.write("D6RF5_ENDPOINT_PHYSICAL REFUSE %s\n" % msg)
+    sys.stderr.write("D6RF6_ENDPOINT_PHYSICAL REFUSE %s\n" % msg)
     sys.exit(2)
 
 
@@ -124,7 +124,7 @@
                "distinguish this arm's artefact from a stale one")
     # CALL SITE 1 OF 2 OF THE UN-TRUNCATED DATUM (CLAUDE.md rule 14: a lesson is
     # not applied until EVERY call site asserts it; the other is
-    # `d6rf5_grade.py`'s age guard).  The launcher produces this value with
+    # `d6rf6_grade.py`'s age guard).  The launcher produces this value with
     # `stat -c '%.9Y'`, which ALWAYS emits a decimal point, so a datum arriving
     # WITHOUT one has been floored to the whole second somewhere between there
     # and here.  That floor moves the datum EARLIER and C3 accepts on
@@ -151,10 +151,10 @@
     for stale in (SCALED_OUT, PRESERVED, PHYSICAL, HISTORY):
         if os.path.exists(stale):
             os.remove(stale)
-    spec = importlib.util.spec_from_file_location("d6rf5_extract_frozen",
+    spec = importlib.util.spec_from_file_location("d6rf6_extract_frozen",
                                                   os.path.abspath(EXTRACTOR))
     mod = importlib.util.module_from_spec(spec)
-    sys.modules["d6rf5_extract_frozen"] = mod
+    sys.modules["d6rf6_extract_frozen"] = mod
     spec.loader.exec_module(mod)      # __name__ != "__main__": no entrypoint fires
     mod.main()                        # the frozen bytes do the extraction
 
@@ -264,7 +264,7 @@
           "d6r_fd_endpoint.py; pre-image preserved at %s md5 %s"
           % (SCALED_OUT, PRESERVED, m_src))
 
-    sys.stdout.write("D6RF5_ENDPOINT_PHYSICAL_WRITTEN %s scalers=%s "
+    sys.stdout.write("D6RF6_ENDPOINT_PHYSICAL_WRITTEN %s scalers=%s "
                      "pinned_witnesses=%d bounds_checked=%d bounds_violations=%d\n"
                      % (PHYSICAL, json.dumps(scalers_used, sort_keys=True),
                         cp["n_pinned_found"], cb["n_checked"], cb["n_violations"]))
