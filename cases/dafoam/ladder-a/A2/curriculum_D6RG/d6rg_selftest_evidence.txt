D6RG SELFTEST EVIDENCE -- recorded execution, gates CLOSED at freeze commit 5563f78533391e8ec5842db915ff70ebd7663685
utc_start=2026-08-30T23:08:14Z
d6rg_grade.py md5 = 14356162837faece73acf40b323927c9   (registered 14356162837faece73acf40b323927c9)
d6rg_reader.py md5 = 61bb9dbe1adab6e9b2cdb3a13f53cff1  (registered 61bb9dbe1adab6e9b2cdb3a13f53cff1)
--- RUN 1: python3, __pycache__ cleared ---
  [OK ] U1 THE DEFECT, reproduced through the FROZEN code: `read_ipopt` on D6R's preserved opt_IPOPT.txt REFUSES on `no_final_objective_or_exit` with n_obj=0 and n_exit=1 -- the EXIT line IS there and only the summary Objective line is missing
  [OK ] U2 PLANT THE ZERO -- the CONTROL that makes U1's absence evidence: the SAME frozen reader on D4's real complete log RETURNS objective 2.1125978108239574e-02, exit 'EXIT: Optimal Solution Found.', n_iter 80.  A zero from a reader not shown able to see a non-zero is not evidence (rule 3)
  [OK ] U3 `nlp_scaling_method` is READ FROM BOTH LOGS as 'none' / 'none', so the iteration table's single objective column equals the summary's UNSCALED column BY THE SETTING IN THE FILE and not by assumption
  [OK ] U4 MUST-NOT-FLAG: the REPAIRED reader on D4's real complete log takes the ORIGINAL path -- every key the frozen module consumes is EQUAL to the frozen reader's own return and the provenance is SUMMARY_LINE.  The repair is unreachable when the summary line is present
  [OK ] U5 MUST-NOT-FLAG: the REPAIRED reader on D6R's real log recovers objective '2.2238800e-02' from major 73 with provenance ITERATION_TABLE_ROW, summary_line_present False, optimal False, n_iter 73 and 8 printed significant figures -- NOT the summary's 17
  [OK ] U6 THE READER READS THE FILE, NOT A CONSTANT: an INDEPENDENT path -- scanning the preserved log's own lines for major 73 -- yields the same objective token '2.2238800e-02'
  [OK ] U7 THE REPAIRED READER PASSES THE FROZEN PLANTED-ZERO CONTROL on D4's reference: a planted 1.234e-03 is SEEN (delta 1.234e-03) and the unperturbed negative control does not move (delta 0.0e+00).  Note the FROZEN grader never drove this control on D6R's data -- `g_price` short-circuits when F_mp did not run -- so rule 3 is driven HERE
  [OK ] U8 THE FROZEN `planted_zero_control`'s DEFAULT reader is NOT changed by the rebind -- it is bound in `__defaults__` at def time, so D4's reference control still travels the ORIGINAL frozen code AFTER `read_ipopt` has been rebound.  Registered, and measured rather than assumed
  [OK ] U9 BIRTH must-FLAG: EVERY major row removed, tail intact -> still REFUSED on `repair_declined_no_iteration_table`.  There is no objective in that file and the reader does not produce one (clause 'G-D6R-OPT:note,number_of_iterations_reported,repair_declined_no_iteration_table')
  [OK ] U10 BIRTH must-FLAG: the file TRUNCATED after major 40 the way a torn write cuts it -- table stops, whole tail gone -> REFUSED on `repair_declined_no_exit_line`.  An absent record is not a recognised non-finite exit (clause 'G-D6R-OPT:note,repair_declined_no_exit_line')
  [OK ] U11 BIRTH must-FLAG, THE DANGEROUS SHAPE: majors 68-73 deleted but the tail STILL reports 73 iterations -> REFUSED on `repair_declined_iteration_table_torn`.  A reader that trusted the last readable row would have published major 67's intermediate objective as the endpoint (clause 'G-D6R-OPT:last_table_major,note,number_of_iterations_reported,repair_declined_iteration_table_torn,rows_matched')
  [OK ] U12 BIRTH must-FLAG: the last objective rewritten to `nan` -> REFUSED, never coerced. HONEST CLAUSE NAMING: the FROZEN `MAJOR_ROW_RE` objective class `[-+0-9.eE]+` CANNOT EXPRESS `nan`, so the row stops matching and the refusal lands on the TORN clause, not on the non-finite one.  The requirement holds; the clause is named, not assumed (clause 'G-D6R-OPT:last_table_major,note,number_of_iterations_reported,repair_declined_iteration_table_torn,rows_matched')
  [OK ] U13 BIRTH must-FLAG, THE LIMB THAT PROVES NO SILENT COERCION: the last objective rewritten to `1e999` -- expressible in the frozen regex's own class, floats to inf -> REFUSED on `repair_declined_recovered_objective_not_finite`.  The failure under repair IS a non-finite evaluation, so returning one would be exactly the laundering this reader exists to prevent (clause 'G-D6R-OPT:float_value,note,path,repair_declined_recovered_objective_not_finite')
  [OK ] U14 BIRTH must-FLAG: `nlp_scaling_method` flipped none -> gradient-based -> REFUSED on `repair_declined_nlp_scaling_method`.  With scaling on, the table's single column is the SCALED objective and the unscaled number the frozen reader returns IS NOT IN THE FILE (clause 'G-D6R-OPT:note,path,repair_declined_nlp_scaling_method,required')
  [OK ] U15 BIRTH must-FLAG: the EXIT line replaced with an UNREGISTERED non-optimal exit -> REFUSED on `repair_declined_exit_not_registered`.  The repair covers exactly the registered Eval_Error exit; widening it is a registration change, not a reader change (clause 'G-D6R-OPT:note,path,registered,repair_declined_exit_not_registered')
  [OK ] U16 BIRTH must-FLAG: the EXIT line surgically deleted with the table intact -> REFUSED on `repair_declined_no_exit_line` (the same clause U10 reaches by truncation, from a different shape) (clause 'G-D6R-OPT:note,repair_declined_no_exit_line')
  [OK ] U17 BIRTH must-FLAG: `Number of Iterations` deleted -> REFUSED on `repair_declined_number_of_iterations_absent`.  Without the tail's own count there is nothing to corroborate the table against, so a truncated table cannot be distinguished from a complete one (clause 'G-D6R-OPT:note,repair_declined_number_of_iterations_absent')
  [OK ] U18 BIRTH must-FLAG: a HOLE punched at majors 30-35 with both ends intact -> REFUSED on `repair_declined_iteration_table_not_contiguous`.  A last row that agrees with the tail is not by itself proof the table is whole (clause 'G-D6R-OPT:first_major,gaps,note,repair_declined_iteration_table_not_contiguous')
  [OK ] U19 MUST-NOT-FLAG, THE WHOLE POINT: the unplanted copy runs the FROZEN `grade()` to a verdict and the original `G-D6R-OPT no_final_objective_or_exit` REFUSAL IS GONE -- the frozen instrument produced ELEVEN gate readings where it previously produced none
  [OK ] U20 REBIND AUDIT: exactly ['read_ipopt'] rebound and nothing else.  Every band, threshold, cap, label, composition rule and prediction that decides the verdict is the frozen code
  [OK ] U21 CARRIED FORWARD 1 -- G-D6R-OPT is UNCLASSIFIED, not STALLED: S1 TRUE (673 cutbacks over 73 majors = 9.2192/major against a registered 1.0) but S2 FALSE (dual infeasibility FELL 9.23e-04 at major 66 -> 9.00e-04 at 73).  The registered ladder needs BOTH
  [OK ] U22 CARRIED FORWARD 2 -- P1 scores MISS on POINT and BAND (registered set {ITERATION_CAP, STALLED}, point STALLED; observed UNCLASSIFIED) and P8 (`the chain COMPLETES -- all four arms run`) is a clean MISS.  Both are the FROZEN scorer's own
  [OK ] U23 CARRIED FORWARD 3 -- G10 GATE FAIL, and the failing limb is O_mp's FRAME GAP: host 33869 s minus container 33794 s = 75 s against an allowance of FRAME_ALLOWANCE_S 90 minus KILL_GRACE_S 60 = 30 s.  The arm was INSIDE its cap at 2257.933 of 2900.0 core-min; the frame allowance is under-registered for a 12.7 MB log
  [OK ] U24 THE REPAIR DID NOT LAUNDER A REFUSAL INTO A PASS: the item verdict is 'NOT A RESULT', and it is neither PASS nor GATE REACHED
  [OK ] U25 every one of the 11 gate values the frozen composer returned is in the fixed vocabulary
  [OK ] U26 L-332: both successor files carry ZERO `assert` statements ([0, 0]), and the counter is shown able to see a planted one (1) -- so `python3 -O` cannot strip a check
  [OK ] U27 PRESERVED ROOTS BYTE-IDENTICAL after the whole execution: 14546 files under the D6R root and 5085 under D4's O/, md5 manifest before == after.  READ FROM THE DISK, not from git -- a run root is not in git
D6RG SELFTEST units=27 expected=27 failures=0 python_O=False wall_s=9.7 core_min=0.162
D6RG SELFTEST PASS 27/27 (counted against the frozen EXPECTED_UNITS)
--- RUN 2: python3 -O, __pycache__ cleared ---
  [OK ] U1 THE DEFECT, reproduced through the FROZEN code: `read_ipopt` on D6R's preserved opt_IPOPT.txt REFUSES on `no_final_objective_or_exit` with n_obj=0 and n_exit=1 -- the EXIT line IS there and only the summary Objective line is missing
  [OK ] U2 PLANT THE ZERO -- the CONTROL that makes U1's absence evidence: the SAME frozen reader on D4's real complete log RETURNS objective 2.1125978108239574e-02, exit 'EXIT: Optimal Solution Found.', n_iter 80.  A zero from a reader not shown able to see a non-zero is not evidence (rule 3)
  [OK ] U3 `nlp_scaling_method` is READ FROM BOTH LOGS as 'none' / 'none', so the iteration table's single objective column equals the summary's UNSCALED column BY THE SETTING IN THE FILE and not by assumption
  [OK ] U4 MUST-NOT-FLAG: the REPAIRED reader on D4's real complete log takes the ORIGINAL path -- every key the frozen module consumes is EQUAL to the frozen reader's own return and the provenance is SUMMARY_LINE.  The repair is unreachable when the summary line is present
  [OK ] U5 MUST-NOT-FLAG: the REPAIRED reader on D6R's real log recovers objective '2.2238800e-02' from major 73 with provenance ITERATION_TABLE_ROW, summary_line_present False, optimal False, n_iter 73 and 8 printed significant figures -- NOT the summary's 17
  [OK ] U6 THE READER READS THE FILE, NOT A CONSTANT: an INDEPENDENT path -- scanning the preserved log's own lines for major 73 -- yields the same objective token '2.2238800e-02'
  [OK ] U7 THE REPAIRED READER PASSES THE FROZEN PLANTED-ZERO CONTROL on D4's reference: a planted 1.234e-03 is SEEN (delta 1.234e-03) and the unperturbed negative control does not move (delta 0.0e+00).  Note the FROZEN grader never drove this control on D6R's data -- `g_price` short-circuits when F_mp did not run -- so rule 3 is driven HERE
  [OK ] U8 THE FROZEN `planted_zero_control`'s DEFAULT reader is NOT changed by the rebind -- it is bound in `__defaults__` at def time, so D4's reference control still travels the ORIGINAL frozen code AFTER `read_ipopt` has been rebound.  Registered, and measured rather than assumed
  [OK ] U9 BIRTH must-FLAG: EVERY major row removed, tail intact -> still REFUSED on `repair_declined_no_iteration_table`.  There is no objective in that file and the reader does not produce one (clause 'G-D6R-OPT:note,number_of_iterations_reported,repair_declined_no_iteration_table')
  [OK ] U10 BIRTH must-FLAG: the file TRUNCATED after major 40 the way a torn write cuts it -- table stops, whole tail gone -> REFUSED on `repair_declined_no_exit_line`.  An absent record is not a recognised non-finite exit (clause 'G-D6R-OPT:note,repair_declined_no_exit_line')
  [OK ] U11 BIRTH must-FLAG, THE DANGEROUS SHAPE: majors 68-73 deleted but the tail STILL reports 73 iterations -> REFUSED on `repair_declined_iteration_table_torn`.  A reader that trusted the last readable row would have published major 67's intermediate objective as the endpoint (clause 'G-D6R-OPT:last_table_major,note,number_of_iterations_reported,repair_declined_iteration_table_torn,rows_matched')
  [OK ] U12 BIRTH must-FLAG: the last objective rewritten to `nan` -> REFUSED, never coerced. HONEST CLAUSE NAMING: the FROZEN `MAJOR_ROW_RE` objective class `[-+0-9.eE]+` CANNOT EXPRESS `nan`, so the row stops matching and the refusal lands on the TORN clause, not on the non-finite one.  The requirement holds; the clause is named, not assumed (clause 'G-D6R-OPT:last_table_major,note,number_of_iterations_reported,repair_declined_iteration_table_torn,rows_matched')
  [OK ] U13 BIRTH must-FLAG, THE LIMB THAT PROVES NO SILENT COERCION: the last objective rewritten to `1e999` -- expressible in the frozen regex's own class, floats to inf -> REFUSED on `repair_declined_recovered_objective_not_finite`.  The failure under repair IS a non-finite evaluation, so returning one would be exactly the laundering this reader exists to prevent (clause 'G-D6R-OPT:float_value,note,path,repair_declined_recovered_objective_not_finite')
  [OK ] U14 BIRTH must-FLAG: `nlp_scaling_method` flipped none -> gradient-based -> REFUSED on `repair_declined_nlp_scaling_method`.  With scaling on, the table's single column is the SCALED objective and the unscaled number the frozen reader returns IS NOT IN THE FILE (clause 'G-D6R-OPT:note,path,repair_declined_nlp_scaling_method,required')
  [OK ] U15 BIRTH must-FLAG: the EXIT line replaced with an UNREGISTERED non-optimal exit -> REFUSED on `repair_declined_exit_not_registered`.  The repair covers exactly the registered Eval_Error exit; widening it is a registration change, not a reader change (clause 'G-D6R-OPT:note,path,registered,repair_declined_exit_not_registered')
  [OK ] U16 BIRTH must-FLAG: the EXIT line surgically deleted with the table intact -> REFUSED on `repair_declined_no_exit_line` (the same clause U10 reaches by truncation, from a different shape) (clause 'G-D6R-OPT:note,repair_declined_no_exit_line')
  [OK ] U17 BIRTH must-FLAG: `Number of Iterations` deleted -> REFUSED on `repair_declined_number_of_iterations_absent`.  Without the tail's own count there is nothing to corroborate the table against, so a truncated table cannot be distinguished from a complete one (clause 'G-D6R-OPT:note,repair_declined_number_of_iterations_absent')
  [OK ] U18 BIRTH must-FLAG: a HOLE punched at majors 30-35 with both ends intact -> REFUSED on `repair_declined_iteration_table_not_contiguous`.  A last row that agrees with the tail is not by itself proof the table is whole (clause 'G-D6R-OPT:first_major,gaps,note,repair_declined_iteration_table_not_contiguous')
  [OK ] U19 MUST-NOT-FLAG, THE WHOLE POINT: the unplanted copy runs the FROZEN `grade()` to a verdict and the original `G-D6R-OPT no_final_objective_or_exit` REFUSAL IS GONE -- the frozen instrument produced ELEVEN gate readings where it previously produced none
  [OK ] U20 REBIND AUDIT: exactly ['read_ipopt'] rebound and nothing else.  Every band, threshold, cap, label, composition rule and prediction that decides the verdict is the frozen code
  [OK ] U21 CARRIED FORWARD 1 -- G-D6R-OPT is UNCLASSIFIED, not STALLED: S1 TRUE (673 cutbacks over 73 majors = 9.2192/major against a registered 1.0) but S2 FALSE (dual infeasibility FELL 9.23e-04 at major 66 -> 9.00e-04 at 73).  The registered ladder needs BOTH
  [OK ] U22 CARRIED FORWARD 2 -- P1 scores MISS on POINT and BAND (registered set {ITERATION_CAP, STALLED}, point STALLED; observed UNCLASSIFIED) and P8 (`the chain COMPLETES -- all four arms run`) is a clean MISS.  Both are the FROZEN scorer's own
  [OK ] U23 CARRIED FORWARD 3 -- G10 GATE FAIL, and the failing limb is O_mp's FRAME GAP: host 33869 s minus container 33794 s = 75 s against an allowance of FRAME_ALLOWANCE_S 90 minus KILL_GRACE_S 60 = 30 s.  The arm was INSIDE its cap at 2257.933 of 2900.0 core-min; the frame allowance is under-registered for a 12.7 MB log
  [OK ] U24 THE REPAIR DID NOT LAUNDER A REFUSAL INTO A PASS: the item verdict is 'NOT A RESULT', and it is neither PASS nor GATE REACHED
  [OK ] U25 every one of the 11 gate values the frozen composer returned is in the fixed vocabulary
  [OK ] U26 L-332: both successor files carry ZERO `assert` statements ([0, 0]), and the counter is shown able to see a planted one (1) -- so `python3 -O` cannot strip a check
  [OK ] U27 PRESERVED ROOTS BYTE-IDENTICAL after the whole execution: 14546 files under the D6R root and 5085 under D4's O/, md5 manifest before == after.  READ FROM THE DISK, not from git -- a run root is not in git
D6RG SELFTEST units=27 expected=27 failures=0 python_O=True wall_s=9.5 core_min=0.158
D6RG SELFTEST PASS 27/27 (counted against the frozen EXPECTED_UNITS)
utc_end=2026-08-30T23:08:33Z  rc_python3=0  rc_python3_O=0
