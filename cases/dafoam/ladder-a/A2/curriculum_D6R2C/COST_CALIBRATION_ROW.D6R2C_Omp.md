| {{ALLOCATE_ID}} | 2026-09-13 | dafoam | **D6R2C arm `O_mp` — the production multipoint optimisation GRADED against the frozen section 4 gates `G1`–`G5`.** 25 IPOPT majors from iteration 0, `DARhoSimpleFoam` primals and their adjoints under mphys/OpenMDAO, 4 ranks, 9,504 cells, three lift conditions (`cl04`/`cl05`/`cl06`), 103 design variables, container `d6r2c_O_mp_20260913T013230Z_226722` as uid 1000. **`rc = 0`, `Number of Iterations....: 25`, one `EXIT:` line and it is the registered one, time `1000` present in all twelve `mp0{4,5,6}/processor{0..3}`, 7,291 entries scanned with ZERO owned by uid 0 or gid 0.** Verdict **`GATE FAIL`**: `G1`∧`G4`∧`G5` hold, `G2` PASSES at a **24.732 %** weighted-drag reduction (`J0` 0.03064163 → `Jf` 0.02306326, ratio 0.752677) against the registered 10 % bar, and **`G3` MISSES** — `cl05` by 1.210e-03 and `cl06` by 2.787e-03 against 1.0e-3, corroborated to seventeen digits by IPOPT's own `Constraint violation....: 2.7869956827836218e-03`. 🔴 **THIS ROW IS THE RULE-12 COMPARISON AND SETTLES NO GATE.** 🔴 **AND IT CARRIES A DISCLOSURE THAT TRAVELS WITH THE VERDICT: the production grading instrument `d6r2c_grade.py` NAMED BY SECTION 4 DID NOT EXIST AT THE FREEZE** — absent from git history, from section 11's frozen-instrument table and from A1.6's updated table — **so the grading path for `G1`–`G5` was NOT fixed at the pre-registration commit** (rule 2), unlike the kill-and-resume and arm-0 paths which were. It was written 2026-09-13 after this run produced data, every literal copied verbatim from the frozen section 4, 43 planted controls including a real uid-0 file, supervisor's diff read before belief. Disclosed at `PREREGISTRATION.md` ADDENDUM 3 §A3.1–A3.2, not repaired retroactively. | **786.5 core-min**, frozen in `PREREGISTRATION.md` §8 at commit `7f685867d` **before any container started**, from the MEASURED anchor **31.258 core-min per IPOPT major at 4 ranks** (C-188, `8262f123`): 25 × 31.258 = 781.5 + 5 preamble. Registered **cap 2359.5 (3.00×)**. = **$0.672 DERIVED, NEVER MEASURED** at $0.0513/core-h, c7a.4xlarge, reported-by-owner — the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` §5). | **672.933 core-min MEASURED** = wall 10,094 s × 4 ranks ÷ 60, read from the `ARM=O_mp` row the launcher wrote at container exit in `ledger.txt` (`d6r2c_run_arm.sh:372`), not from memory. = **$0.575 DERIVED.** Against the cap: **not crossed, margin −1686.567**, and no `D6R2C_CAP_CROSSED arm=O_mp` row exists in the ledger. | **672.933 core-min — cleaned == gross, and the reason is stated rather than assumed.** The run is ONE ledger row of 10,094 wall s, which **does exceed the charter §2 3600-s threshold**; that threshold identifies a *stalled* row, and this row is not one. Evidence it was making progress throughout: 88 function and 26 gradient evaluations recorded in `d6r2c_evals.jsonl` with the last at `wall_since_start_s = 9799.6`, checkpoint-rotator rows in the ledger at the registered 1800-s cadence through 04:02:32Z, and IPOPT reaching its registered iteration 25. **No core-minute is deducted on that basis.** | **0.856** | **MISPREDICTION, IN THE CONSERVATIVE DIRECTION — the estimate was 16.9 % high and the majors came in cheaper than the anchor that set it.** Measured per-major cost **26.917 core-min** (672.933 ÷ 25) against the registered C-188 anchor of **31.258**, a ratio of **0.861**; the per-major figure includes line-search evaluations, the run having spent 88 function and 26 gradient evaluations across its 25 majors. **CONTENTION: NOT MEASURED FOR THIS ARM, and that is stated as absent rather than approximated** (append rule 2) — the launcher's delivered-core sampler wrote no `delivered_cores_mean` into the ledger or the log for `O_mp`; the only contention datum on record is `load1_pre = 27.90` on a 96-core host at launch, which is a pre-launch reading and not a delivered-core measurement. 🔴 **WASTE, NAMED SEPARATELY AND NEVER LAUNDERED INTO EITHER COLUMN OR INTO THE RATIO (charter §6): 51.955 core-min** on the **52 of 87** `kind == "F"` evaluations that returned `fail = 1`, summed from their own `eval_wall_s` fields × 4 ranks ÷ 60 — **7.7 % of the run produced no usable function value.** Honest caveat, so the figure is not over-read: these are not waste in the optimiser's sense, because IPOPT consumed each failure to cut its step; they are named because charter §6 requires waste to be named rather than absorbed, and because the next estimate for this family must know that fraction. The failures are primals that missed the `primalMinResTol × primalMinResTolDiff = 1.0e-5` bar by rounding (minimum failed residual **1.000006e-05**, maximum **7.312316e-05**, 48 blocks in the log, all failures), **not the NaN/divergence signature section 6 was written against** — see ADDENDUM 3 §A3.6. **ACCOUNTING RESIDUE STATED, NOT APPROXIMATED:** evaluations account for 469.465 of the 672.933 core-min; the remaining **203.468 core-min** is container preamble, mesh warping, IPOPT internals, checkpoint rotation and the final field write, **which this run did not instrument separately**, so no attribution is offered for it. | `cases/dafoam/ladder-a/A2/curriculum_D6R2C/PREREGISTRATION.md` §8 frozen at `7f685867d` (estimate + cap) and ADDENDUM 3 (verdict, grading-path defect, failure character); run root `/home/ubuntu/certonomous-runs/CURRICULUM-D6R2C-a2-wing-multipoint-transonic-restartable/` — `ledger.txt` (`ARM=O_mp` row, stamp `20260913T013230Z_226722`), `O_mp_GRADE.json`, `O_mp_20260913T013230Z_226722.log`, `O_mp/d6r2c_evals.jsonl`, `O_mp/opt_IPOPT.txt`; grader `cases/dafoam/ladder-a/A2/curriculum_D6R2C/d6r2c_grade.py` md5 `ca159f6cee00c3e571195b44d2967659`, **NOT in the freeze — disclosed, not pinned** |
