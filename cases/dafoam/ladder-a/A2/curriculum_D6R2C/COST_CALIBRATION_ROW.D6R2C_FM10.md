| {{ALLOCATE_ID}} | 2026-09-13 | dafoam | **`D6R2C-FM9` arm `FM10` — the re-run after `FM9`'s cpuset collision, GRADED `NOT A RESULT`.** `DARhoSimpleFoam` under mphys/OpenMDAO, 4 ranks, 38,304 cells, three lift conditions (`cl04`/`cl05`/`cl06`), four phases (deform → mesh → stage → solve), container `d6r2c_fm9_FM10_20260913T163543Z_1546115`, uid 1000, `rc = 0`. Verdict `NOT A RESULT` on `H3` — `J_fresh = 0.036964341844` against the inherited `Jf = 0.023063259529`, outside the registered band — and `FM9` before it was `NOT A RESULT` on its cap crossing (63.333 core-min against 34.200) and is never re-seeded. 🔴 **THIS ROW IS THE RULE-12 COMPARISON AND SETTLES NO GATE.** | **11.400 core-min**, frozen in `PREREGISTRATION_FRESHMESH_ARRIVES.md` §8 (`171.0 s × 4 ranks ÷ 60`), built from `FM5`'s MEASURED 114 s 1000-iteration primal plus mesh and deform; **registered cap 34.200 (3.00×)**, carried unchanged to `FM10` by ADDENDUM 5 §A5.5. = **$0.0097 DERIVED, NEVER MEASURED** at $0.0513/core-h, c7a.4xlarge, reported-by-owner — the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` §5). | **10.667 core-min MEASURED** = wall 160 s × 4 ranks ÷ 60, read from the `arm=FM10` row the launcher wrote at container exit in `ledger.txt` and independently from `FM10_GRADE.json` `cost.core_min`, which agree to all printed digits. = **$0.0091 DERIVED.** Against the cap: **not crossed, margin −23.533**, and no `D6R2C_FM9_CAP_CROSSED arm=FM10` row exists in the ledger. 🔴 **A TRANSCRIPTION DIFFERENCE IS NAMED RATHER THAN SMOOTHED:** `PREREGISTRATION_FM11_MATCHED_LIFT.md` lines 362 and 675 state this actual as **10.677** and the ratio as **0.937**. The instrument-written figures are **10.667** and **0.9357**; `160 × 4 ÷ 60 = 10.6667`. **This row states the instrument's figures**, and the prose difference is a third-decimal transcription slip in a sentence that itself says the row is still owed — it moves nothing. | **10.667 core-min — cleaned == gross.** The arm is ONE ledger row of 160 s wall, so charter §2's 3600-s stall rule matches no row and there is nothing to clean. **No core-minute is deducted.** | **0.9357** | **MISPREDICTION, IN THE CONSERVATIVE DIRECTION — the estimate was 6.9 % high, and it is the smallest miss this item has recorded.** The §8 prediction was built from a MEASURED predecessor (`FM5`'s 114 s primal) rather than from an anchor, and the measured end-to-end came in 11 s under its 171 s basis. **CONTENTION: NOT MEASURED FOR THIS ARM, and that is stated as absent rather than approximated** (append rule 2) — `FM10` was launched from its own wrapper and no `G_BOX` line for it survives in `FM9_launcher.out`, whose only reading (`load1 = 41.34`, `nproc = 96`, `avail_gb = 635`) belongs to `FM9`'s 10:16Z launch, not to `FM10`'s 16:35Z one. 🔴 **WASTE, NAMED SEPARATELY AND NEVER LAUNDERED INTO EITHER COLUMN OR INTO THE RATIO (charter §6): 63.333 core-min**, the whole of `FM9`, spent on a cpuset the arm shared with another job — disclosed at ADDENDUM 5 §A5.1, attributable to the launch and not to the prediction. It is **not** in `FM10`'s ratio and must not be read into it. | `cases/dafoam/ladder-a/A2/curriculum_D6R2C/PREREGISTRATION_FRESHMESH_ARRIVES.md` §8 (estimate + cap) and ADDENDUM 5 (`FM10`'s re-registration); run root `/home/ubuntu/certonomous-runs/CURRICULUM-D6R2C-FM9-a2-wing-freshmesh-arrives/` — `ledger.txt` (`arm=FM10` row, stamp `20260913T163543Z_1546115`), `FM10_GRADE.json`, `FM10_20260913T163543Z_1546115.log` |
