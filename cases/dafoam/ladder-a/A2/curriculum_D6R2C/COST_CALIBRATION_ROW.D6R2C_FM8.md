| {{ALLOCATE_ID}} | 2026-09-13 | dafoam | **`D6R2C-AFTER9-R2` arm `FM8` — the fresh-mesh confirmation of Sanaa's after-item 9, graded `PASS`.** THE FIRST COMPLETED ARM ON THIS ITEM after six `NOT A RESULT` rows. `DARhoSimpleFoam` under mphys, 38,304 cells, 4 ranks, three lift conditions, four phases (deform → mesh → **init** → solve). The ONE REGISTERED CHANGE: the fresh-mesh primal initialised from `O_mp`'s converged fields at the SAME design point, transferred cell-for-cell by index (no interpolation — the base, fresh and warped meshes are byte-identical in `faces`/`owner`/`neighbour`/`boundary`). rc=0, 144 s wall, **zero primal failures**, where `FM5` and `FM7` each stalled at `1.278377566e-05`. `H3`: `J_fresh 0.023063278222500323` vs `Jf 0.023063259528677764`, an absolute difference of `1.869382e-08` against a band of `3.064163e-04` — 16,391x inside. | **15.737 core-min** registered in `PREREGISTRATION_AFTER_ITEM9_R2.md` §8 (cap 47.211 = 3.00x) | **9.600 core-min** (144 s wall x 4 ranks / 60), from the launcher's own ledger row | **9.600 core-min — gross and cleaned are the SAME figure here**: the arm ran 144 s wall, so charter §2's 3600-s stall rule matches no row and there is nothing to clean. **$0.0082 DERIVED, NOT MEASURED** at $0.0513/core-h (c7a.4xlarge, reported-by-owner; the box cannot read its own billing, `COMPUTE_BUDGET_CHARTER.md` §5) | **0.610** | **MISPREDICTION, in the conservative direction, and it is one number.** The `init` phase was budgeted at **90 s as an explicitly labelled ESTIMATE** — §8 named it "the number this estimate is weakest on" because the instrument had never run — against a measured ~0 s contribution; the remainder of the prediction (`FM5`'s measured 114 s end-to-end plus 2 x 16.027 s for the two conditions `FM5` never reached) held. **No contention component**: `G-BOX` recorded `load1=37.36` of `nproc=96` at launch and the arm held its own cpuset. **WASTE IS NAMED SEPARATELY AND IS NOT IN ANY COLUMN ABOVE**: **9.066 core-min defect-attributable** across `FM6` (1.533, a launcher that staged three files where four were needed) and `FM7` (7.533, a transfer written where the solver does not read). Neither is in the class of `DEC3`'s 63.000 or `FM5`'s 7.600, which bought measurements. | `verification` record `/home/ubuntu/certonomous-runs/CURRICULUM-D6R2C-AFTER9R2-a2-wing-freshmesh-warmstart/AFTER_ITEM9R2_FM8_GRADE.json`; registration frozen at `2587bff12eda6c585ecb36c09ab24b9cbbb63bc0` |
