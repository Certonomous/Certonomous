| {{ALLOCATE_ID}} | 2026-09-13 | dafoam | **`D6R2C-FM11` arm `FM11` — the comparison at matched lift, graded `BLOCKED`.** The arm was frozen at `6190e070c6f53fe07f058dfcfa610ba7fb246596`, launched on 4 ranks (cores 4-7, wrapper pid 1594844) and **refused at its FIRST staging step, on all four ranks, after 22 s wall**, before a single primal. The refusal is `REFUSE_FRESH_IS_BASE` from the INHERITED `d6r2c_fm9_stage.py:95-100`: the guard fires when the generated mesh's `points.gz` md5 equals the base mesh's, and `FM11` introduced the state `Zb` FOR WHICH THAT IDENTITY IS THE CORRECT AND EXPECTED OUTCOME — a mesh generated at zero shape from the base surface *should* be the base mesh. **A REGISTRATION DEFECT, NOT PHYSICS, AND L-593 EXACTLY: a pin proves what a file IS, not what it NEEDS.** The arm nevertheless left a measured result on disk on its way to being blocked: `genWingMesh.py` plus the family sequence reproduced the base volume mesh **BIT FOR BIT — max point difference 0.0 m exactly, 40209/40209 points identical, md5 identical on files whose gzip header MTIME field reads 0 on both — 47.719 days after the original build** (2026-07-28T00:18:12Z → 2026-09-13T17:33:34Z). That is registered as gate `R1` in the successor arm `FM12`. | **222.853 core-min** registered in `PREREGISTRATION_FM11_MATCHED_LIFT.md` §8 (cap 668.559 = 3.00x), derived from DEC7 `STATE O/B/S` and `O_mp`'s 26 `G` records | **1.467 core-min** (22 s wall x 4 ranks / 60), from the launcher's own ledger row at `/home/ubuntu/certonomous-runs/CURRICULUM-D6R2C-FM11-a2-wing-matched-lift/ledger.txt` | **1.467 core-min — gross and cleaned are the SAME figure**: the arm ran 22 s wall, so `COMPUTE_BUDGET_CHARTER.md` §2's 3600-s stall rule matches no row and there is nothing to clean. **$0.0013 DERIVED, NOT MEASURED** at $0.0513/core-h (c7a.4xlarge, reported-by-owner; the box cannot read its own billing, `COMPUTE_BUDGET_CHARTER.md` §5) | **0.006583** | **THE `BLOCKED` REFUSAL — NOT MISPREDICTION, AND THIS RATIO CARRIES NO INFORMATION ABOUT THE ESTIMATE.** The arm refused at its first staging step and **never reached a single solve**, so the 222.853 figure was never exercised in either direction: nothing about it is confirmed and nothing about it is falsified by 1.467. It is carried into `FM12` UNCHANGED for exactly that reason, and re-derived from the same anchors in `d6r2c_fm12_prefreeze.sh` rather than copied. **NO contention component**: `G-BOX` recorded `load1=45.88` of `nproc=96` at launch, `G-CPUSET` found 48 of 96 cores busy and none of them 4-7, and the arm held its own cpuset for its whole 22 s. **WASTE IS NAMED SEPARATELY AND IS NOT IN ANY COLUMN ABOVE**: the 221.386 core-min not spent is **NOT waste** — it is compute the arm CORRECTLY DECLINED TO START, which is the guard doing its job on the wrong sub-arm rather than compute thrown away. The defect-attributable spend of this arm is the **1.467 core-min actually burned**, and it bought two things the successor uses: the `R1` measurement above, and the live failing controls for `M0b` and `M0o` that `FM12` drives against these very artifacts. | `BLOCKED` recorded in `PREREGISTRATION_FM11_MATCHED_LIFT.md` ADDENDUM 2 (committed `3656f5117`); arm log `/home/ubuntu/certonomous-runs/CURRICULUM-D6R2C-FM11-a2-wing-matched-lift/FM11_20260913T173321Z_1594848.log`; the `R1` measurement reproducible by `python3 cases/dafoam/ladder-a/A2/curriculum_D6R2C/d6r2c_fm12_stage.py --live-controls` |
