| {{ALLOCATE_ID}} | 2026-09-13 | dafoam | **`D6R2C-FM11` arm `FM11` — the matched-lift arm, `BLOCKED` at its first staging step, on the exact condition its own `M0` registered.** Two sub-arms (`Zb`, `Zo`) and four states (`Zb`, `Zo`, `Ez`, `Do`) were registered; **none of them solved.** Container `d6r2c_fm11_FM11_20260913T173321Z_1594848`, `rc = 1`, wall **22 s**. The `Zb` sub-arm's `--phase mesh` SUCCEEDED — `genWingMesh.py` ran, `plot3dToFoam` and `autoPatch` ran, `checkMesh` printed `Mesh OK` — and staging then refused: `d6r2c_fm9_stage.Refusal: REFUSE_FRESH_IS_BASE the generated mesh's points.gz md5 equals the BASE mesh's (0fb1935a9b8781b73ac4ccb136e3ec68)` (`d6r2c_fm9_stage.py:96`). For `Zb`, a mesh generated at ZERO shape, that identity is the EXPECTED outcome; the guard was written for `FM9`/`FM10`, where "fresh" always meant the OPTIMUM's mesh. 🔴 **THIS ROW IS THE RULE-12 COMPARISON AND SETTLES NO GATE.** Verdict **`BLOCKED`** — not `GATE FAIL`, because no gate was reached. | **222.853 core-min**, frozen in `PREREGISTRATION_FM11_MATCHED_LIFT.md` §8 (`3342.8 s × 4 ranks ÷ 60`); **registered cap 668.559 (3.00×)**, the figure `d6r2c_fm11_grade.py --print-cap` printed at launch. = **$0.1905 DERIVED, NEVER MEASURED** at $0.0513/core-h, c7a.4xlarge, reported-by-owner — the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` §5). | **1.467 core-min MEASURED** = wall 22 s × 4 ranks ÷ 60, read from the `arm=FM11` row the launcher wrote at container exit in `ledger.txt`, not from memory. = **$0.0013 DERIVED.** Against the cap: **not crossed, margin −667.092.** | **1.467 core-min — cleaned == gross.** One ledger row of 22 s wall; charter §2's 3600-s stall rule matches no row and there is nothing to clean. | **0.00658** | 🔴 **NOT MISPREDICTION, AND THE RATIO MUST NOT BE READ AS ESTIMATE QUALITY. THE PREDICTION WAS NEVER TESTED.** The arm was stopped by a **registered refusal** — `REFUSE_FRESH_IS_BASE`, which §3's `M0` named in advance and whose firing §3 registered in advance as **`BLOCKED`** — **22 s in, before the OpenMDAO model was built, before one primal iteration ran, and before the `Zo` sub-arm started at all.** Essentially none of the predicted work was performed: the 222.853 core-min budgeted 24 trimmed primals across four states, of which **zero** executed. The estimate is therefore **neither confirmed nor falsified by this row**, and the 0.00658 is a statement about how early the arm stopped, not about how well it was costed. **CONTENTION: measured and not a factor** — `G_BOX` recorded `load1 = 45.88` on `nproc = 96` with `avail_gb = 646`, and `G_CPUSET` gave the arm its own four cores (`cpuset=4,5,6,7`). **WASTE (charter §6): NONE ATTRIBUTED, and the reason is stated rather than assumed.** The 1.467 core-min bought a measurement — the guard-scope finding that became the lesson landed the same day, plus the two falsified claims disclosed in ADDENDUM 2 — so it is not in the class of `FM6`'s 1.533 or `FM7`'s 7.533. It is named here so the next estimate for this family knows the arm cost 1.467 core-min and returned no objective. | `cases/dafoam/ladder-a/A2/curriculum_D6R2C/PREREGISTRATION_FM11_MATCHED_LIFT.md` §8 (estimate + cap) and ADDENDUM 2 (the `BLOCKED` verdict and the two falsified claims); run root `/home/ubuntu/certonomous-runs/CURRICULUM-D6R2C-FM11-a2-wing-matched-lift/` — `ledger.txt` (`arm=FM11` row, stamp `20260913T173321Z_1594848`), `FM11_20260913T173321Z_1594848.log`, `FM11_launcher.out`, `FM11/Zb/mesh_generation.log` |
