| {{ALLOCATE_ID}} | 2026-09-13 | dafoam | **CORRECTION ROW** — corrects:[C-20260913T043733.251556Z-17681dfe] **on ONE descriptive figure and on NOTHING ELSE: the mesh size.** That row's process cell reads *"4 ranks, **9,504 cells**, three lift conditions"*. **`9,504` is PROCESSOR 0's SHARE under the 4-way decomposition, not the grid.** The mesh is **38,304 cells**. **Two independent sources, both on disk and both cited rather than recalled** (append rule 2): (1) the run's own decomposition table in `ARM0_4R_20260913T000043Z_152777.log` lines 94-132 — `Processor 0 … 9504`, `Processor 1 … 9600`, `Processor 2 … 9608`, `Processor 3 … 9592`, **sum 38,304**; (2) the mesh's own FoamFile header, written by the mesher and independent of any run — `base/constant/polyMesh/owner.gz` `note "nPoints:40209  nCells:38304  nFaces:116756  nInternalFaces:113068"`. The two agree exactly. The corrected row is low by **`38304/9504 = 4.0303`** — not exactly 4, since `38304/4 = 9576` and processor 0 holds 72 cells fewer than an even quarter. **Provenance of the error, stated so it is not hunted twice:** the figure was inherited from `PREREGISTRATION.md` section 1 line 72 (the section 1 table row whose three cells read `mesh` / `9,504 cells, single grid` / `base/constant/polyMesh`), which carries the same mistake; that line is **struck, never rewritten**, at `PREREGISTRATION.md` **ADDENDUM 4 §A4.2**. 🔴 **NO GATE READS CELL COUNT AND NO VERDICT MOVES.** `O_mp` remains **`GATE FAIL`** and `O_mp_GRADE.json` is not edited; `G1`–`G5`, `KR-G1`–`KR-G4`, section 7's tolerance and every section 8 cap contain no cell-count term. 🔴 **THE CORRECTED ROW IS NOT EDITED** (this file's append rule 1: *"An existing row is never edited; a correction is a new row naming the row it corrects"*). | **UNCHANGED — 786.5 core-min**, frozen in `PREREGISTRATION.md` §8 at commit `7f685867d` before any container started, registered cap 2359.5 (3.00×), = $0.672 **derived, never measured** at $0.0513/core-h, c7a.4xlarge, reported-by-owner (`COMPUTE_BUDGET_CHARTER.md` §5). **This column is untouched by the correction:** the estimate was built from the measured per-major anchor 31.258 core-min (C-188, `8262f123`) and never contained a cell-count term. | **UNCHANGED — 672.933 core-min MEASURED** (wall 10,094 s × 4 ranks ÷ 60, from the `ARM=O_mp` row in `ledger.txt`), = $0.575 derived, cap not crossed, margin −1686.567. **Untouched:** core-minutes are wall × ranks ÷ 60 and are independent of cell count. | **UNCHANGED — 672.933 core-min, cleaned == gross**, for the reason the corrected row gives and this row does not re-argue. **Untouched.** | **UNCHANGED — 0.856.** **Untouched:** the ratio is cleaned/predicted and neither term carries a cell count. | **NO GAP ATTRIBUTION IS RESTATED AND NONE IS REVISED.** The corrected row's attribution — misprediction in the conservative direction, per-major 26.917 vs the 31.258 anchor (0.861); contention **NOT MEASURED** for this arm and stated as absent; **waste named separately at 51.955 core-min** on the 52 of 87 failed `kind == "F"` evaluations; accounting residue 203.468 core-min unattributed — **stands unaltered.** 🔴 **WHAT THIS CORRECTION DOES CHANGE IS EVERY PER-CELL FIGURE A READER WOULD DERIVE FROM THAT ROW, AND EACH IS TOO LARGE BY 4.0303×.** For the reader who needs one: the whole 25-major arm costs **`672.933 / 38,304 = 1.757e-02` core-min per cell** (derived from the row's own measured core-minutes and this row's corrected cell count), where the corrected row as landed yields `7.080e-02`. **No new measurement is introduced by this row** — every core-minute figure above is the corrected row's own, quoted unchanged. | `cases/dafoam/ladder-a/A2/curriculum_D6R2C/PREREGISTRATION.md` **ADDENDUM 4 §A4.2** (the correction, with both sources); the corrected row the row named in the `corrects:` field of this row and its own refs, unchanged; mesh sources under `/home/ubuntu/certonomous-runs/CURRICULUM-D6R2C-a2-wing-multipoint-transonic-restartable/` — `base/constant/polyMesh/owner.gz` (FoamFile `note`) and `ARM0_4R_20260913T000043Z_152777.log` (decomposition table, lines 94-132) |
