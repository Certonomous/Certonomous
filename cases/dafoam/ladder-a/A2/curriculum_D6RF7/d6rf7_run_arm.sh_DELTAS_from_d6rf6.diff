--- a/(rename-only-from-d6rf6) d6rf7_run_arm.sh	2026-09-07 16:39:32.654219547 +0000
+++ b/(D6RF7-fixed) d6rf7_run_arm.sh	2026-09-07 16:56:12.246879452 +0000
@@ -94,7 +94,7 @@
 # assignment wins, so the stale one would have won and aborted staging at rc 4.
 # A field a human edits by searching for its name is exactly where that
 # recurs, so the count is PINNED here rather than the appearance trusted.
-PERMISSION=ff009251  # FROZEN 2026-09-07 by dafoam-supervisor: freeze sha ff009251 (D6RF7 dir commit, tree carries PREREGISTRATION.md, rule 2). Successor to D6RF5 (BLOCKED harness defect). Supervisor check-1 CLEAN: the guard on the P_conv baseline primal mirrors the F5 path and STILL writes the product (fixes the D6RF5 absent-product G1 refusal); pin fixpoint ALL_PINS_MATCH spot-verified (fd 6b753ab1==MD5_FD, physical f61a9665==MD5_PHYS6, extract c93adc8b==MD5_EXTRACT==MD5_EXTRACT6); real-base stager dry-run exit 0; completion clauses (rc/oom/terminal/age) do NOT key on the primal banner so the guarded exit-0 gives a CLEAN completion -> gate_conv grades L1 -> expected G-CONV GATE FAIL (binding p_first 1.626e-05 > 1.0e-05 floor; Fix#1+#2 insufficient, prediction confirmed). Gate NOT widened (T25). est 62.0 / cap 186.0 core-min (expected realised ~7.6).
+PERMISSION=NOT_FROZEN  # D6RF7 DRAFT 2026-09-07 by dafoam lab-lane. NOT FROZEN, NOT LAUNCHED. Successor to D6RF6 (graded BLOCKED: planted-CD control REFUSED on the present-but-empty product because the P_conv baseline primal RAISED BY DESIGN and the guard wrote points={}; grading never reached G-CONV). D6RF7's single class of DELTA is grading FAILURE-PATH handling so the whole path SURVIVES the primal-raised present-but-empty product and produces the graded G-CONV verdict. Pin fixpoint ALL_PINS_MATCH TRUE (fd 6f4c8bcd==MD5_FD, physical 625bacf5==MD5_PHYS6, extract baedb673==MD5_EXTRACT==MD5_EXTRACT6, units 34f477f9, anchor_gate c549ec2e; runscript/locus/fvSchemes/fvSol carried BYTE-IDENTICAL). FIXTURE-VALIDATED off the D6RF6 run root (no re-solve): grade rc 0, cd/fd controls NOT EXERCISED (no refuse), G-OFF/G-PRICE/G-FD NOT A RESULT under ARM_RAN_PRIMAL_RAISED_POINTS_EMPTY, G-CONV GATE FAIL on L1 (binding p_first_uncorrected 1.6256e-05 = 1.626x the 1.0e-05 floor; nuTilda 1.408e-05 also over floor), F5 AS PREDICTED (L3 GATE FAIL), overall NOT A RESULT per the registered ladder rung 6 (want-of-input on the unbought FD/CD/off/price gates -- rule 5, NOT widened). No gate/threshold/band/cap/scheme/mesh changed (N-D43, T25). est 62.0 / cap 186.0 core-min; a deterministic re-run of the same scheme realises ~12-16 core-min (D6RF6 measured 12.133). Supervisor freezes after non-delegable check-1 of the grade.py/control DELTAS.
 PERM_ASSIGNMENTS=$(grep -cE '^PERMISSION=' "${BASH_SOURCE[0]}" || true)
 case "$PERM_ASSIGNMENTS" in ''|*[!0-9]*) PERM_ASSIGNMENTS=UNMEASURED ;; esac
 if [ "$PERM_ASSIGNMENTS" != "1" ]; then
@@ -327,8 +327,8 @@
 
 # ---- FROZEN INSTRUMENT HASHES (PREREGISTRATION.md sections 7a and 7b) -----
 MD5_RUNSCRIPT6=137539e0a99be27f27fdb69e063b2a87
-MD5_FD=6b753ab17c7a598106ede906123d4103
-MD5_EXTRACT6=c93adc8b4d20c6097bb35e82592e6dae
+MD5_FD=6f4c8bcd91010141813e6b5a6736fa9c
+MD5_EXTRACT6=baedb673e9c88291f6724794681bc9a7
 MD5_LOCUS6=341189ca866f302a7e1bba8eefad3a57
 # 2026-09-06 PRE-FIRST-COMPUTE: was ca75db3e036b9f4e7cfca6462037c021.  Repinned
 # because `d6rf7_endpoint_physical.py`'s C3 gained the rule-14 assertion that its
@@ -338,9 +338,9 @@
 # because d6rf7_endpoint_physical.py:95 MD5_EXTRACT was corrected from the stale
 # PARENT d6rf4 value (c5aace65) to the real d6rf7 extractor (95630a22); that edit
 # shifts d6rf7_endpoint_physical.py's own md5.  Fixpoint pair with that fix.
-MD5_PHYS6=f61a96653a0d4d71931f4f553bef911f
-MD5_UNITS=f4a1524836cefe9fd5df66242f970ae0
-MD5_ANCHOR_GATE=21bd642c136df97fd6d5d883ecd3ceef
+MD5_PHYS6=625bacf5b1489989f9a2638dd99e2e52
+MD5_UNITS=34f477f92b23e27896b458475eef0f78
+MD5_ANCHOR_GATE=c549ec2ee18d7c2bf3ae4244bb12bc78
 # THE D6RF7 DISCRETISATION FILES, AND THE WHOLE ITEM TURNS ON THEM.
 # d6rf7_fvSchemes_LIMITED carries `Gauss linear limited corrected 0.333` /
 # `limited corrected 0.333` (Fix #1); d6rf7_fvSchemes_D6RF4_ORIGINAL carries
