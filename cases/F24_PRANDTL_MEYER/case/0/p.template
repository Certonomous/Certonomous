FoamFile { version 2.0; format ascii; class volScalarField; object p; }
// F24 -- state 1 (p1 = 1) everywhere at t = 0, at the mesh's own cell centres.

dimensions      [1 -1 -2 0 0 0 0];
internalField   nonuniform List<scalar> __INTERNALFIELD__;

boundaryField
{
    inlet    { type fixedValue; value uniform __P1__; }
    topA     { type fixedValue; value uniform __P1__; }
    topB     { type zeroGradient; }
    outlet   { type zeroGradient; }
    wallUp   { type zeroGradient; }
    wallDown { type zeroGradient; }
    frontAndBack { type empty; }
}
