*** THIS IS NOT THE ORIGINAL ARTIFACT. THE ORIGINAL IS LOST. ***
*** IT IS A RECORD OF A DELETION, WRITTEN BY THE LANE THAT DID THE DELETING. ***

WHAT WAS LOST
-------------
  verification/runs/AUTOSTOP_LIVENESS/clause6_planted_zero_2026-09-03T1813Z.txt

It was the FIRST attempt at the clause-(6) zero, taken 2026-09-03 ~18:13Z. It carried a
FALSE ZERO: it reported no fresh writes under /home/ubuntu/certonomous-runs when the tree
was in fact being written continuously by the live dafoam W3 container.

WHY IT WAS FALSE -- the finding the specimen carried
----------------------------------------------------
`find` on this box's interactive PATH is **bfs 4.1.1**, which REJECTS a relative
`-newermt "-30 minutes"` timestamp. The probe ran that reader under `2>/dev/null`. The
redirect swallowed bfs's parse error and the empty result set read exactly like a measured
absence. The zero was an error, not a reading.

Corrected reading, with both limbs of the planted control, is at:
  verification/runs/AUTOSTOP_LIVENESS/clause6_planted_zero_2026-09-03T1815Z.txt
It uses /usr/bin/find (GNU findutils 4.9.0) with a `touch -d` reference file and `-newer`
-- which is the construction clause (6) of scripts/auto_stop_patched.sh already uses, and
this is the defect that construction exists to avoid. It found
  /home/ubuntu/certonomous-runs/CURRICULUM-D12R2W3-cylinder-unsteady/
    S3b_c0_ap_20260903T172242Z_179692.log.ok.20260903T172242Z_179692
i.e. the tree was hot the whole time.

HOW IT WAS LOST, stated plainly because the deletion was mine
--------------------------------------------------------------
At 18:15Z this lane ran `rm -f` on the file in the same invocation that wrote the
corrected one, reasoning that a false reading should not be left on disk to be cited
later. That reasoning was WRONG and was overruled by the cfd supervisor the same session.
The lab's practice is the opposite and is on the board: RUNG 0's attempt 2 produced a
SPURIOUS GATE FAIL and was PRESERVED as RESULTS.ATTEMPT2_SPURIOUS_GATE_FAIL.json --
superseded and marked, never deleted. MOVE, NEVER DELETE is a standing rule in this family
precisely because a lane once deleted attempt 1's STATUS file to clear a path. A false
reading is a SPECIMEN: it is the evidence that the defect was real and the evidence of how
the defect PRESENTED. Deleting it destroys both.

RECOVERABILITY -- checked, not assumed
---------------------------------------
  * never committed: `git log --all -- <path>` returns nothing, so git holds no copy;
  * not on disk: `ls` reports No such file; a `find` over /home/ubuntu and the session
    scratchpad for the basename returns no path;
  * no backup, snapshot or tmp copy exists.
THE FILE IS UNRECOVERABLE. This is recorded as a LOSS, not repaired.

The paragraphs above describe what the original contained; they are written from this
lane's own running transcript. THEY ARE NOT THE FILE AND MUST NEVER BE CITED AS IT.
No reconstruction is offered in its place, because a reconstruction presented as an
original is a forged artifact, and this file exists to record a loss honestly rather than
to make the loss look repaired.

Lesson filed: docs/LESSONS.md L-466.
Written 2026-09-03 by the cfd lane that caused the loss.
