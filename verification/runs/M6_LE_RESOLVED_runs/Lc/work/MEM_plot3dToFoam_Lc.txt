# MEASURED peak resident set of SINGLE-PROCESS plot3dToFoam, Lc.
# Instrument: GNU /usr/bin/time is ABSENT from dafoam-idwarp-rot:v1 (measured: rc 127), so the
# peak is taken two independent ways -- a 0.2 s poller on the process's own /proc/<pid>/status
# VmHWM (the kernel's high-water mark) and the container cgroup-v2 memory.peak (which also
# counts page cache, hence the larger figure).  Driver: rerun_patch_tail.sh.
# This converts a previously INFERRED feasibility claim into a MEASUREMENT.
VmHWM_peak_kB=781832
cgroup_peak_bytes=782483456
cells=936000
bytes_per_cell_VmHWM=855.3
VmHWM_peak_GiB=0.746
