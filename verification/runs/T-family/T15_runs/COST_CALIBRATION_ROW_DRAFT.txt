DRAFT COST_CALIBRATION.md ROW -- NOT COMMITTED, NOT APPENDED.
Id PROVISIONAL: re-derive the tail id at commit time in the same shell
invocation (CLAUDE.md rule 11; COST_CALIBRATION append rule 5).
Highest id read at 2026-08-28 by this lane: C-187.

NOTE ON THE VERDICT COLUMN: this row records COST ONLY. The T15 rung carries
NO row verdict -- the frozen comparator REFUSED (exit 2) at analyse_t15.py:501
before any gate was applied. The compute was spent and is costed; the science
is ungraded and must not be reported as graded.

| C-189 | 2026-08-28 | heat-transfer | T15 (`T15_UP_f`) -- UNSTEADY axisymmetric turbulent plume, `buoyantBoussinesqPimpleFoam` RAS kEpsilon URANS on T8's fine mesh (102,400 cells), 1 rank, 24,000 fixed timesteps to endTime 240 s. Run COMPLETE by all six clauses of standing rule 4 (`mark_done_t15.py` rc 0, `DONE.T15_UP_f` written). **NO ROW VERDICT: the frozen comparator `analyse_t15.py` (sha256 `4ff5a01f…55031`, disk == its committed blob at the freeze `de0683c7`) REFUSED at exit 2** in the S1 planted-zero negative arm. | **1307 core-min** registered (`T15_registered.json` `cost.point_core_min`; cap 2600, timeout 156,000 s); **$1.1175 DERIVED** | **1195.817 core-min MEASURED** gross, wall basis (`STATUS.T15_UP_f`: `wall_s=71749`, `ranks=1`, 71749 x 1 / 60) = **$1.0224 DERIVED**. Solver-only basis from the log's own last `ExecutionTime = 70751.29 s` = **1179.188 core-min**. | **NO CLEANED FIGURE IS PUBLISHED, and the reason is named rather than a judgement.** The stall rule (`COMPUTE_BUDGET_CHARTER` §2, the single rule in `scripts/self_audit.py`: a ledger row over 3600 wall s is an infrastructure stall) would, read literally against this one-row STATUS, clean a 71,749 s REGISTERED transient — capped at 156,000 s before compute — down to zero. Its operand is a per-arm run-root ledger row, not a single registered long transient. **Referred to the supervisor and to verification as a rule-scope question; this lane does not reinterpret a standard.** Gross is therefore the published figure. | **0.915x** (1195.817 / 1307) on the registered wall basis; **0.902x** on the solver-only ExecutionTime basis. **46.0%** of the 2600 core-min cap; **46.0%** of the 156,000 s timeout. No overrun. | **MISPREDICTION IN THE CONSERVATIVE DIRECTION, TRACED TO THE PROBE'S SAMPLING WINDOW. NO CONTENTION PENALTY IDENTIFIED, NO WASTE.** The point came from a scratch probe measuring **3.267 core-s per step over steps 6-20** (`cost.rate_scratch_source`); 24,000 x 3.267 / 60 = 1306.8, which is the registered 1307. Measured over the whole run: **2.9895 core-s per step** wall basis, **2.9480** solver basis — the probe over-priced by **9.8%**. **The attribution is that steps 6-20 are start-up**: the registration's own P5 records the scratch Courant at 1.053 at start-up decaying through 0.72 by t = 0.12 s, so the probe sampled precisely the regime where PIMPLE does the most corrector work, and the settled regime is cheaper per step. **Transferable calibration finding: a per-step rate for a fixed-step transient must be probed AFTER the start-up transient has decayed, or it will over-price the run by roughly 10%; a 20-step probe beginning at step 6 cannot see the settled regime at all.** Second transferable figure: measured **2.9195e-05 core-s per cell-step** against T8's MEASURED SIMPLE rate of 6.397e-06 core-s per cell-iteration gives a **PIMPLE/SIMPLE ratio of 4.564**, against the registered `cost.pimple_over_simple_ratio_measured` = **4.99** — the registered ratio was 9.3% high, consistent with the same start-up bias and confirming a single cause rather than two. **NAMED SEPARATELY, NOT LAUNDERED INTO THE RATIO: 16.629 core-min (1.39% of gross) is wall-minus-ExecutionTime** — mesh and field I/O, `writeCellCentres`/`writeCellVolumes`, solver start-up. This is NOT waste (it bought the `0/Cx`, `0/Cz`, `0/V` the C_GEOM control reads and the five written time directories) and NOT stall; it is the wall-basis/solver-basis gap and is stated so a reader can choose a basis. **Zero waste: no failed run, no re-run, no capped arm.** | run artifacts: `verification/runs/T-family/T15_runs/STATUS.T15_UP_f`, `verification/runs/T-family/T15_runs/T15_UP_f/log.solve`; grader refusal captured at `verification/runs/T-family/T15_runs/T15_GRADE_OUTPUT.txt`; registration `T15_registered.json`, comparator frozen at `de0683c7`. **`DONE.T15_UP_f` and `T15_GRADE_OUTPUT.txt` are UNCOMMITTED at the time of drafting.** |
