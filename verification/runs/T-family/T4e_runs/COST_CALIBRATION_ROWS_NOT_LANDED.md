# T4e coarse + medium — `docs/COST_CALIBRATION.md` rows, **NOT LANDED**

**STATUS: DRAFTED AND BLOCKED. These two rows are NOT in the ledger.** They are parked here, under
the case directory they belong to and never in the scratchpad (L-186), so that whoever unblocks the
ledger can land them unchanged.

**Why they are not landed.** `scripts/append_record.py` refuses at **exit 7** on HEAD's own blob of
`docs/COST_CALIBRATION.md`, naming line **522** — the cfd **MRF_R1** row of 2026-09-10, id
`C-20260910T230023.521144Z-mrfr1a1`, whose body is 7 characters and non-hex and so fails
`TOOL_ID_BODY`. Clause 1b audits HEAD's **whole** blob, so **one malformed row blocks every team's
append**. This is the **ninth** instance; the register in `scripts/append_record.py` records the
previous eight and calls the condition *"a lab-wide rule-12 block"* in its own words.

**The ruled repair is a REGISTER EDIT in `scripts/append_record.py` — exclude by EXACT id in
`KNOWN_EXCLUDED`, never an edit to the record, and never a widening of `TOOL_ID_BODY`** (which would
legitimise hand-typed ids and hollow the smuggle guard). Every prior instance was ruled by the
**verification** team (2026-09-07, 2026-09-09).

**This lane did not make that edit, deliberately.** `scripts/append_record.py` is shared lab plumbing
under Sanaa's 2026-08-31 PLUMBING FREEZE, the defective row belongs to another team, and a
heat-transfer lane editing shared plumbing to unblock its own append is the shape the freeze exists
to prevent. **Nor was the tool bypassed** by hand-landing the rows through the private-index
protocol — that would make this lane the tenth offender and land rows the tool never validated. A
refusing instrument is evidence, not an obstacle.

**To land them:** once `KNOWN_EXCLUDED` carries the exact id, run

    python3 scripts/append_record.py --path docs/COST_CALIBRATION.md --rows <this file's row block> --allocate-id

The rows below carry the `{{ALLOCATE_ID}}` placeholder and **must** be minted by `--allocate-id`;
they are deliberately NOT hand-numbered.

**Figures, so a reader need not re-derive them:** coarse **24.483 core-min** actual against **21.667**
registered = ratio **1.130**; medium **218.500** against **206.1** = **1.060**. Gap attributed to
**contention**, corroborated independently by the like-for-like `ExecutionTime` comparison against
T4d at the same iteration (**1.126** / **1.057**, agreeing with the core-min ratios to **0.3 %**).
**Waste separately named and zero** on both legs. Dollars **derived, not measured**. The whole-rung
row is deliberately **deferred until `T4e_IJ_f` completes**.

---

## The two rows, verbatim, for append

| {{ALLOCATE_ID}} | 2026-09-11 | heat-transfer | **T4e coarse leg `T4e_IJ_c`** (impinging round jet H/D=2 Re_D=23 000, axisymmetric 2.5° wedge, 8 640 cells, `buoyantBoussinesqSimpleFoam` serial ranks=1, `endTime` 30000 at `deltaT` 1). Prereg **FROZEN v1.0 at `17fb5109c`**, no addenda; grading path re-hashed at this reading and **all nine pinned blobs match the worktree** (`mark_done_t4e.py` `e4e44396`, `analyse_t4e.py` `740575a7`, `launch_t4e.sh` `441ccdfb`, `build_t4e.py` `6e77d62f`, `T4e_registered.json` `a11380c6`, `trajectory_t4e.py` `512b3691`, frozen imports `analyse_t4.py` `6f362447`, `roache_triple.py` `78e56a3b`, `build_t4.py` `ff032f3e`). **Leg verdict: rule-4 `PASS`** — all seven conjuncts evidenced separately (rc 0; exactly one `End`; last time 30000 == `controlDict` `endTime` 30000; 8/8 fields `T U p_rgh alphat nut k omega phi`; `ExecutionTime` count 30000 == round(30000/`deltaT` 1); age guard margin **+1469.0 s** oldest-field-minus-`0/T`). **This row is the rule-12 comparison and settles NO gate: the RUNG is `PENDING`** — the fine leg `T4e_IJ_f` is still iterating, so there is no triple, no observed order and no GCI. | **21.667 core-min registered** — `docs/campaigns/T-family/T4e_PREREGISTRATION.md` §9 per-level POINT, at the corrected basis (T4d's OWN clean measured leg on the **identical** mesh at the identical `endTime`; the T4c per-cell rate is SIGTERM-truncated ~4.5× and is explicitly NOT used). Registered `ceiling_core_min` 32.5, `cap_core_min` 45, `timeout_s` 2700. = **$0.0185 DERIVED, NOT MEASURED** at $0.0513/core-h, owner-stated (`COMPUTE_BUDGET_CHARTER.md` §5 — the box cannot read its own billing). Under the $25 per-run pre-authorisation. | **24.483 core-min MEASURED** = `wall_s` 1469 × ranks 1 ÷ 60, read from the run's own `verification/runs/T-family/T4e_runs/STATUS.T4e_IJ_c` (`rc=0`, `capped=no`, `checkmesh_rc=0`, `note=clean`, `started_utc` 2026-09-10T15:44:03Z, `ended_utc` 16:08:32Z). = **$0.0209 DERIVED, NOT MEASURED** at $0.0513/core-h. **34.4 % of the registered ceiling, 54.4 % of the cap** — no overrun, no cap-stop. | **= gross, 24.483 core-min. NO STALL:** 1469 wall s is well below the charter §2 3600-s figure. **WASTE 0.000 core-min, named separately per charter §6 and never folded into the ratio or its explanation:** rc 0, exactly one `End`, `capped=no`, no relaunch, no discarded compute — every core-minute bought the completed field the rule-4 `PASS` certifies. | **1.130** (24.483 / 21.667). Against the ceiling 32.5: **0.753**. Against the cap 45: **0.544**. | **CONTENTION, AND THE ATTRIBUTION IS CORROBORATED BY AN INDEPENDENT INSTRUMENT RATHER THAN ARGUED FROM LOAD.** The decisive evidence separates *slower per iteration* from *more iterations*: `verification/runs/T-family/T4e_runs/T4E_RATE_PROJECTION.json` carries a **like-for-like** diagnostic comparing this leg's `ExecutionTime` against its T4d predecessor's **at the same iteration**, which factors the per-mesh transient out of both sides — it reads **1.1262** at iteration 30000 against this row's core-min ratio **1.130**, agreeing to **0.3 %**. The iteration count could not move (`endTime` pinned at 30000, `deltaT` 1, 30000 `ExecutionTime` lines measured) and did not; what moved is cost per iteration. **THE CAUSE IS NAMED AND IS SCHEDULING, NOT PHYSICS:** all three T4e legs launched at the same instant, `started_utc` 2026-09-10T15:44:03Z on every one of `STATUS.T4e_IJ_c`, `STATUS.T4e_IJ_m` and the running fine leg, so this 8 640-cell leg shared a 16-vCPU box with the 34 560-cell medium leg and the 138 240-cell fine leg for its whole 24.5-minute life, alongside other teams' solvers. **THE FINE LEG'S OWN LOG CORROBORATES THE MECHANISM INDEPENDENTLY AND IN THE RIGHT DIRECTION:** measured from `T4e_IJ_f/log.solve`'s `ExecutionTime` stamps in 4000-iteration windows, the fine leg ran at 2.52 → 1.63–2.22 s/iteration while the coarse and medium legs were alive and **recovered to 0.7917–0.7952 s/iteration after they retired** — a 2.0× rate recovery on an unchanged decomposition, which is the same contention seen from the other side. **CALIBRATION LESSON, and it is the MRF_R1 lesson arriving a second time in one week: the per-level POINT estimates were sound for an idle box — the T4d-measured basis is the right basis and no better unit cost is needed — what the lab lacks is a CONCURRENCY FACTOR.** A self-concurrent rung that launches its own three levels simultaneously should carry a +6–13 % band on each level rather than a point estimate; measured here, the smallest leg on the most contended mesh pays the largest premium (coarse 1.130 against medium 1.060), because a short leg spends all of its life inside the contended window while a long one outlives it. **NO LOG-MTIME EVIDENCE IS CITED IN EITHER DIRECTION:** a continuously appended log carries only the mtime of its LAST write, so an mtime sweep cannot establish occupancy of a window; the contention here is read from the like-for-like `ExecutionTime` comparison and from the simultaneous `started_utc` values. | frozen prereg `docs/campaigns/T-family/T4e_PREREGISTRATION.md` §9 (cost table) and §12 (grading-path sha freeze), freeze commit **`17fb5109c`**; registered cost block `verification/runs/T-family/T4e_runs/T4e_registered.json` key `cost` and `cases.T4e_IJ_c`; primary cost artifact `verification/runs/T-family/T4e_runs/STATUS.T4e_IJ_c`; completion evidence `verification/runs/T-family/T4e_runs/T4e_IJ_c/{log.solve,system/controlDict,0/T,30000/}` and the marker `verification/runs/T-family/T4e_runs/DONE.T4e_IJ_c` written by `mark_done_t4e.py` blob `e4e44396`; like-for-like corroboration `verification/runs/T-family/T4e_runs/T4E_RATE_PROJECTION.json` (`legs[0].like_for_like_ratio`); contention corroboration `verification/runs/T-family/T4e_runs/T4e_IJ_f/log.solve` `ExecutionTime` windows (read-only, solver running) |
| {{ALLOCATE_ID}} | 2026-09-11 | heat-transfer | **T4e medium leg `T4e_IJ_m`** (same case and solver as the coarse row, 34 560 cells, serial ranks=1, `endTime` 60000 at `deltaT` 1). Same frozen prereg **`17fb5109c`** and the same nine verified grading-path blobs. **Leg verdict: rule-4 `PASS`** — rc 0; exactly one `End`; last time 60000 == `endTime` 60000; 8/8 fields; `ExecutionTime` count 60000 == round(60000/1); age-guard margin **+13109.1 s**. **RUNG `PENDING`** — no triple until `T4e_IJ_f` lands; this row settles no gate. | **206.1 core-min registered** — `T4e_PREREGISTRATION.md` §9 per-level POINT at the same T4d-measured basis (identical mesh, identical `endTime`, so T4d's measured leg applies directly with no scaling). Registered `ceiling_core_min` 309.2, `cap_core_min` 412, `timeout_s` 24720. = **$0.1762 DERIVED, NOT MEASURED** at $0.0513/core-h, owner-stated (`COMPUTE_BUDGET_CHARTER.md` §5). Under the $25 per-run pre-authorisation. | **218.500 core-min MEASURED** = `wall_s` 13110 × ranks 1 ÷ 60, from `verification/runs/T-family/T4e_runs/STATUS.T4e_IJ_m` (`rc=0`, `capped=no`, `checkmesh_rc=0`, `note=clean`, `started_utc` 2026-09-10T15:44:03Z, `ended_utc` 19:22:33Z). = **$0.1868 DERIVED, NOT MEASURED** at $0.0513/core-h. **70.7 % of the registered ceiling, 53.0 % of the cap** — no overrun, no cap-stop. | **TWO FIGURES, AND THE BRIGHT-LINE QUESTION IS REFERRED, NOT RESOLVED — the same open question the cfd team's SUBOFF R1b ledger row of 2026-09-10 put to the owner of the `COMPUTE_BUDGET_CHARTER.md` §2 stall clause and, for any change to it, to Sanaa.** **(1) Cleaned = gross = 218.500 core-min**, the figure that holds if the stall clause does not reach a run that is long by its own registration. **(2) Cleaned = 0.000 core-min** under a LITERAL reading of `CLAUDE.md` rule 12's *"a row over 3600 wall s is a stall"* — this leg's 13110 wall s exceeds it, and because the row IS the whole leg the literal reading strips **all** of it, yielding a ratio of 0.000. **That the literal reading returns a cleaned zero for a leg that provably completed is stated as evidence about the CLAUSE, not about this run**, and it sharpens the referral: the SUBOFF row's literal reading stripped one level of three; applied per-leg it strips everything. **WHY A READER SHOULD DOUBT THE LITERAL READING HERE, measured and not relayed:** this leg ran **60000 of 60000** iterations to rc 0 with exactly one `End`, its p_rgh residual fell monotonically to a flat floor (C6.1 `floor_max` **4.35e-09** against the registered 1e-06 threshold) and its field change plateaued (C6.3 **3.390e-06** against tol 2e-4) — progress was continuous and nothing hung — and 13110 wall s sits at **53.0 %** of its own registered `timeout_s` of 24720. **NARROWING OR RETIRING A THRESHOLD IS RESERVED AND IS NEITHER A LANE'S CALL NOR A SUPERVISOR'S** (`CLAUDE.md` FIRST-ACTION reserved list). Until it is ruled both figures stand side by side and neither is presented as the answer. **WASTE 0.000 core-min, named separately per charter §6 and never folded into either figure or into the ratio:** rc 0, no relaunch, no cap, no discarded compute. | **1.060** (218.500 / 206.1) on cleaned = gross. Under the literal stall reading: **0.000** — reported for completeness and not offered as a calibration statement. Against the ceiling 309.2: **0.707**. Against the cap 412: **0.530**. | **CONTENTION, same mechanism as the coarse row and corroborated the same independent way — and the PAIR of legs is what makes the attribution quantitative rather than asserted.** The like-for-like `ExecutionTime` comparison against T4d **at iteration 60000** reads **1.0575** (`T4E_RATE_PROJECTION.json` `legs[1].like_for_like_ratio`) against this row's core-min ratio **1.060** — agreeing to **0.3 %**, the same agreement the coarse leg shows, from an instrument that grades nothing and moves no marker. Iteration count was pinned and immovable (60000 `ExecutionTime` lines measured against `endTime` 60000 at `deltaT` 1), so the entire gap is cost per iteration. **THE DOSE-RESPONSE IS THE FINDING: the two legs launched at the same instant into the same contention and paid 1.130 and 1.060 — the SHORTER leg paid MORE.** That is the signature of contention rather than of a bad unit cost: the coarse leg lived 24.5 minutes, entirely inside the window when all three legs were alive, while this leg ran 3.64 hours and outlived the coarse leg after 24.5 minutes, diluting its own premium over a window 8.9× longer. A misprediction of the per-cell rate would scale with the mesh, not inversely with the leg's duration. **CALIBRATION LESSON, and it is directly actionable: the T4d-measured basis is VINDICATED** — both legs land within 13 % on a basis built from the predecessor's own clean measured legs on identical meshes, which is the "measured anchor beats a reasoned factor" result the VMFL051-R3 row records from another family. **What the estimate lacks is not accuracy but a CONCURRENCY TERM**, and this rung sizes it: **+6 % for a leg that outlives its siblings, +13 % for one that does not**, on a 16-vCPU box carrying other teams' work. A successor that launches three levels simultaneously should carry that band explicitly rather than a point estimate per level. **NO LOG-MTIME EVIDENCE IS CITED IN EITHER DIRECTION** (a continuously appended log carries only its last write's mtime); contention is read from the like-for-like comparison, the identical `started_utc` across all three `STATUS` files, and the fine leg's measured 2.0× rate recovery after these two legs retired. | frozen prereg `docs/campaigns/T-family/T4e_PREREGISTRATION.md` §9 and §12, freeze commit **`17fb5109c`**; registered cost block `verification/runs/T-family/T4e_runs/T4e_registered.json` key `cost` and `cases.T4e_IJ_m`; primary cost artifact `verification/runs/T-family/T4e_runs/STATUS.T4e_IJ_m`; completion evidence `verification/runs/T-family/T4e_runs/T4e_IJ_m/{log.solve,system/controlDict,0/T,60000/}` and marker `verification/runs/T-family/T4e_runs/DONE.T4e_IJ_m`; convergence readers `verification/runs/T-family/T4e_runs/analyse_t4e.py` blob `740575a7` (`residual_history`/`residual_tests`) and the frozen `verification/runs/T-family/T4_runs/analyse_t4.py` blob `6f362447` (`sample_profile`/`peak_of`); like-for-like corroboration `verification/runs/T-family/T4e_runs/T4E_RATE_PROJECTION.json` (`legs[1]`); the referred stall-clause question, raised by the cfd team's SUBOFF R1b row in this ledger dated 2026-09-10 and by `verification/campaign/SUBOFF_R1b_RESULTS.md`; clause text `CLAUDE.md` rule 12 and `docs/charters/COMPUTE_BUDGET_CHARTER.md` §2 |
