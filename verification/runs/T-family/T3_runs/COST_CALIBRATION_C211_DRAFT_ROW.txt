| C-211 | 2026-08-30 | heat-transfer | **T3 fourth mesh level `R_ff` (602,128 cells, `buoyantBoussinesqSimpleFoam`, `endTime` 118,000, 8 ranks, `simple n (8 1 1)`) — SOLVE COMPLETE and marked DONE under the strict rule; THE TRIPLE `(R_m, R_f, R_ff)` IS NOT GRADED.** All six rule-4 clauses met and asserted by the frozen `mark_done_t3_rff.py` (blob `9436399f`, `--selftest` 8/8 PASS immediately before the real run): `rc=0` from the in-wrapper `STATUS.R_ff`, one `End` line, last time 118,000 == `controlDict` `endTime`, all nine reconstructed fields present, `ExecutionTime` count exactly 118,000, age guard clear against the case's own `0/T`, plus `reconstructpar_rc=0`. `DONE.R_ff` written 2026-08-30T22:42:41Z, exit 0. **The grading then REFUSED (`exit 2`): `analyse_t3_rff.py` stopped at the `CLAUDE.md` rule-3 planted-zero control on the medium level `R_f`, BEFORE any graded quantity was measured, so `gate_t3_rff.json` was never written and NO VERDICT ON THIS RUNG EXISTS. This row therefore carries no verdict of any kind** — it is filed because the LEVEL completed and rule 12 is owed at every process completion, and it is filed with the refusal named rather than held back until a verdict exists. | **POINT 18,218.2 core-min** (303.6 core-h; **USD 15.58 derived, not measured**), **CEILING / registered stop 27,400 core-min** (456.7 core-h; **USD 23.43 derived**), timeout 205,500 s at 8 ranks, 10x runaway guard 182,182 core-min. Basis: **6.50e4 cell-iterations per core-second**, itself an extrapolation — the measured `R_m` 1.12e5 to `R_f` 8.53e4 degradation (0.762x per 2.56x cells) applied ONCE MORE, with no fixed-cost term (the startup/IO term is under 0.2 % of the wall at this length). `docs/campaigns/T-family/T3_R_FF_PREREGISTRATION.md` section 6, lines 170-195. **Frozen before first compute and verified so**: the document's only three commits (`18a28aab`, `32980e09`, `ba023a53`) all predate the 2026-08-26T20:51:28Z launch, and neither amendment moves a cost, cap, gate, threshold, band or label. | **26,757.067 core-min, basis gross, MEASURED** as `wall_s` 200,678 x 8 ranks / 60, read from `STATUS.R_ff` (`rc=0`, `capped=no`, `cap_core_min=27400`, `reconstructpar_rc=0`, `checkmesh_rc=0`, `decomposepar_rc=0`, `started_utc=2026-08-26T20:51:28Z`, `ended_utc=2026-08-29T04:36:19Z`) = **445.9511 core-h** = **USD 22.8773 DERIVED, NOT MEASURED** at the owner-stated c7a.4xlarge $0.0513/core-h (`COMPUTE_BUDGET_CHARTER.md` section 5 — the box cannot read its own billing). **Solver-CPU basis stated beside the wall basis rather than replacing it**: the last `ExecutionTime` line of `R_ff/log.solve` reads 199,377.06 s against `ClockTime` 200,670 s, i.e. the master rank consumed CPU for **99.356 %** of its own wall. Wall 55.74 h. | **26,757.067 core-min — gross == cleaned.** The 3,600-second stall rule matches the 200,678 s wall on its face and is **ruled NOT a stall**, on the C-209 / C-210 evidence pattern and with the evidence in hand: one continuous 118,000-iteration solve, exactly 118,000 `ExecutionTime` lines and exactly one `End` (both asserted by the frozen `mark_done_t3.check` clauses 2 and 5, not by eye), `rc=0`, `capped=no`, and 99.356 % CPU delivery — a stalled row does not spend its wall on CPU. Nothing is removed. **WASTE: 0.000 core-min in the charter section 6 sense**, named separately and folded into neither column nor the ratio — no cap was hit, nothing was killed, truncated, re-run or discarded, and the AMENDMENT 2 launcher refusal of 2026-08-26T17:43:48Z (the G2 cwd-holder guard refusing its own ancestor shell) was a **ZERO-COMPUTE** refusal that started no solver. **Grading-pass cost, reported beside the solve and NOT folded into it**: 1.94 s wall at 1 rank = **0.032 core-min** (`mark_done_t3_rff.py` 1.33 s, MaxRSS 236,956 kB — the 114 MB `log.solve` read whole into memory, as predicted; `analyse_t3_rff.py` 0.61 s to the refusal, MaxRSS 60,748 kB). | **1.4687x actual/predicted against the POINT**; **0.9765 of the CEILING (97.65 % of the registered stop)**; **97.65 % of the 205,500 s timeout, with 4,822 s = 1.34 h of wall headroom (643 core-min) unused.** No overrun, no cap raised, no gate moved: rule 12's overrun clause binds on the CEILING and the CEILING was never reached. | **THE ENTIRE MISS IS IN THE RATE, AND THE RECORD CANNOT DECOMPOSE IT — SO IT IS NOT DECOMPOSED HERE.** Achieved **4.4257e4 cell-iterations per core-second** (602,128 cells from `R_ff/log.checkMesh` x 118,000 iterations = 7.10511e10, over 1,605,424 core-s) against the registered 6.50e4 = **0.6809 of it**, and 1/0.6809 = 1.4687 — **the ratio IS the rate miss, exactly, with no residual for anything else.** Two candidates, and they are NOT separable from this record: (a) the section 6 basis extrapolated a SERIAL single-core rate by applying the measured `R_m`-to-`R_f` degradation 0.762x one more time, an extrapolation section 6 makes explicit; (b) parallel efficiency at 8 ranks, which section 6 assumed at 0.85 for its wall estimate. **If the serial rate were exactly 6.50e4 the implied efficiency is 0.681; if the efficiency were 0.85 the implied serial rate is 5.21e4, i.e. a degradation factor of 0.611 rather than 0.762.** No serial `R_ff` and no other-rank `R_ff` exists, so **the split is UNATTRIBUTED and is stated as unattributed, not guessed.** **CONTENTION IS BOUNDED AND PARTLY EXCLUDED, NOT WAVED AT:** the master rank delivered CPU for 99.356 % of its wall, so **at most 0.644 % of the wall (1,292.94 s = 172 core-min, 0.64 of the 46.87 percentage points) can be master descheduling or non-solver wall of any kind** — descheduling of rank 0 is excluded as the mechanism. Memory-bandwidth contention, which slows CPU-consuming work WITHOUT descheduling, and the scheduling of the other seven ranks, whose `ExecutionTime` is not recorded, are **not measured and remain unattributed.** **AND THE LOAD RECORD DOES NOT EXIST, WITH THE ABSENCE MEASURED RATHER THAN ASSUMED:** the C-209 search pattern (`*load*`, `*box_*`, `*uptime*`, depth 3) returns **ZERO** under `verification/runs/T-family/T3_runs` and, run identically as a planted control, **FIVE** under `verification/runs/F23_HP_WEDGE_runs` — the reader that returned the zero was shown able to see a non-zero, so this is a measurement. **This is the FOURTH filing of the C-4 / B3 / C-209 lesson (record the load average beside any per-cell-iteration cost basis, and state the load the basis is re-applied at) and the field has still not been added; that repetition is itself the finding.** **WHAT THE CEILING GOT RIGHT AND THE POINT GOT WRONG:** the 1.504x CEILING was constructed to cover efficiency down to 0.85 PLUS a further ~20 % rate degradation, and the actual landed at 1.4687x — inside that combined allowance, consuming 97.65 % of it. **The cap construction was sound; the point estimate was not, and an extrapolated rate applied a second time is the named reason.** **THE SHARPEST FINDING IS THE MARGIN, AND IT IS A NEAR MISS RATHER THAN A COMFORT:** the run finished with **2.35 % of its timeout unused**. Had the achieved rate been 2.4 % lower the `timeout` would have killed the solve mid-run, the strict completion rule would have failed on every clause that depends on reaching `endTime`, and **the whole 26,757 core-min would have become waste.** A 1.5x cap over a point estimate that missed by 1.47x left 1.34 h of headroom on a 55.74 h run — the cap did its job by a margin of about eighty minutes. **COST-LINE CROSSING, DISCLOSED RATHER THAN NETTED:** T3's cumulative derived spend moves from USD 6.171 to **USD 29.048**, above the USD 25 line named in section 6 — a line that AMENDMENT 1's cost ruling (lab-attributed, the heat-transfer supervisor 2026-08-26) declared superseded for CPU spend under Sanaa's 2026-08-25 directive, caps being runaway guards rather than budget gates. **The registered CEILING in core-minutes, which is the actual stop, was NOT exceeded.** | Actuals: `verification/runs/T-family/T3_runs/STATUS.R_ff` (all fields quoted above), `R_ff/log.solve` (114,795,360 bytes; final `ExecutionTime = 199377.06 s  ClockTime = 200670 s`), `R_ff/log.checkMesh` (cells 602,128, the birth certificate). Completion: `verification/runs/T-family/T3_runs/DONE.R_ff` (written 2026-08-30T22:42:41Z) and its stdout `verification/runs/T-family/T3_runs/log.mark_done_rff.20260830T224240Z.txt`, produced by the frozen `mark_done_t3_rff.py` (blob `9436399f`). Refusal artefact: `verification/runs/T-family/T3_runs/T3_R_FF_GRADE_OUTPUT_20260830T224307Z.txt` (`analyse_t3_rff.py` blob `44e3e2b8`, exit 2 at the planted-zero control). Predicted figures and the cost ruling: `docs/campaigns/T-family/T3_R_FF_PREREGISTRATION.md` section 6 lines 170-195 and AMENDMENT 1 lines 382-388, frozen at `18a28aab` / `32980e09` / `ba023a53`, all pre-first-compute. Planted control for the load-record zero: `verification/runs/F23_HP_WEDGE_runs/{coarse,medium,fine}/box_before.txt` and `{coarse,medium}/box_after.txt`. Cross-rung comparison: C-4, C-209 and the B3 row (the load-average lesson, filed here for the fourth time). **NO FROZEN FILE WAS EDITED to produce this row and no gate, band, threshold, cap or label was touched**; every figure was re-derived by this lane from the artefacts named. |
