# T10aR2 LAUNCH RECORD

Written 2026-08-26T17:28:27Z by the T5 lane on the heat-transfer supervisor's extended dispatch (verify each launched case under its own registered marker). Supervisor decisions `[lab-attributed]`. Registration `docs/campaigns/T-family/T10aR2_PREREGISTRATION.md` at `fb4bf7e2` (blob `1c28eec7`, worktree identical); frozen `analyse_t10aR2.py` `a252463b` and `mark_done_t10aR2.py` `4fbff6a7` both byte-identical on disk at this write.

## Runner launches 2026-08-26 — ALL THREE LEVELS REFUSED BY THE LAUNCHER AT ZERO COMPUTE

| case | runner line (`verification/queue/runner.log`) | launcher output (`<case>/launcher.queue.out`, verbatim) | runner `<case>/STATUS.<id>` (INFRA only, L-342) |
|---|---|---|---|
| `R2_c` | `17:24:17Z LAUNCHED team=heat-transfer case=T10aR2_R2_c pid=280743 sid=280743 ranks=1 est=0.03 core-min prereg=fb4bf7e2` | `REFUSE: pid 280744 is already running in R2_c` | `launcher_rc=2 end=2026-08-26T17:24:18Z` |
| `R2_m` | `17:25:22Z LAUNCHED team=heat-transfer case=T10aR2_R2_m pid=284859 sid=284859 ranks=1 est=0.22 core-min prereg=fb4bf7e2` | `REFUSE: pid 284860 is already running in R2_m` | `launcher_rc=2 end=2026-08-26T17:25:23Z` |
| `R2_f` | `17:26:27Z LAUNCHED team=heat-transfer case=T10aR2_R2_f pid=286905 sid=286905 ranks=1 est=11.07 core-min prereg=fb4bf7e2` | `REFUSE: pid 286906 is already running in R2_f` | `launcher_rc=2 end=2026-08-26T17:26:29Z` |

**Finding F1 — the launcher's cwd-holder guard refuses the queue runner's own wrapper.** `launch_t10aR2.sh` lines 133–135 scan `/proc/*/cwd` for the case directory and exclude only `$$`. The runner's fixed launch form is `setsid nohup bash -c 'cd <cwd> && <argv> …'` (`docs/standards/QUEUE_RUNNER.md` §4): that `bash` — pid = launch pid + 1 in every row above — holds the case directory as cwd and is the launcher's own parent, so the guard fires on every runner launch by construction. No solver started; no `0/` was armed (each case still holds only `0.orig BUILD.txt CASE.txt constant log.* system`); no `log.solve`; no in-wrapper `T10aR2_runs/STATUS.R2_*` exists. **Zero core-minutes spent on the rung. The three entries are consumed into `verification/queue/heat-transfer/launched/` and would have to be re-dropped after the guard is amended.** `launch_t4b.sh` lines 132–135 carry the identical guard; the three `T4b_IJ_*` entries that were queued behind these were withdrawn from the drop path before the runner reached them (17:27Z; not by this lane). The same shape was ruled for T5 in its AMENDMENT 6(b) (`launch_t5.sh` G2 superseded for runner launches). Remedy (for the supervisor, pre-first-compute on each rung): exclude the launcher's own ancestry from the scan (e.g. `$PPID` and every pid in the launcher's session, `awk '{print $6}' /proc/<pid>/stat`), keeping the refusal for any foreign process.

**Registered marker applied:** `mark_done_t10aR2.py` (invoked once, without arguments — it executes its check on the registered case list rather than printing usage; disclosed) → `REFUSE: no STATUS.R2_c -- the solver's exit status was never recorded. An absent STATUS is not inferred from an End line (K0d L1); it is refused.` rc 2. That is the correct reading: nothing ran. **Verdict for the rung at this write: `PENDING` (not run); no level is DONE; no CAP_OVERRUN.txt was written (the runner saw a STATUS within 1 s).**

**Cost:** predicted POINT 11.32 core-min (§4: 0.03 + 0.22 + 11.07), caps 10 / 30 / 120; actual 0.000 core-min; waste 0.000 core-min (nothing armed, nothing to clean); no calibration row is owed until a level runs.
