T19 INSTRUMENT DIFFS -- for the supervisor's SUPERVISION_CHARTER section 3 check 1
(measurement-script diffs read AS DIFFS, personally, never delegated).

Generated 2026-08-27T19:33:34Z by the heat-transfer lane that wrote
docs/campaigns/T-family/T19_PREREGISTRATION.md.  Every parent below was confirmed
IDENTICAL ON DISK AND AT HEAD before the diff was taken (git diff HEAD -- <path>
empty on all of them), so these diffs are against the committed artifact and not
against a stale worktree copy.

STATUS: THESE INSTRUMENTS ARE PROPOSED.  Their output has not been believed by
anyone and no solver has run.  No launch may occur before this file is read.

================================================================================

--- DIFF: T14_runs/mark_done_t14.py  ->  T19_runs/mark_done_t19.py
================================================================================
--- T14_runs/mark_done_t14.py	2026-08-26 21:00:26.817398042 +0000
+++ T19_runs/mark_done_t19.py	2026-08-27 19:25:22.601625889 +0000
@@ -1,12 +1,15 @@
 #!/usr/bin/env python3
-"""Turn T14 STATUS files into DONE markers under the STRICT COMPLETION RULE
+"""Turn T19 STATUS files into DONE markers under the STRICT COMPLETION RULE
 (CLAUDE.md rule 4), all-or-nothing:
 
   1. STATUS.<case> EXISTS in the run root and carries the solver's own rc,
-     written INSIDE the launcher wrapper (run_one_t14.sh), and rc == 0
+     written INSIDE the launcher wrapper (run_one_t19.sh), and rc == 0
   2. log.solve carries an `End` line
   3. the last written time == endTime from the case's own controlDict
-  4. the registered field T is present at that time (pure conduction)
+  4. the registered fields T, U, p_rgh and alphat are present at that time.
+     THE TUPLE IS THE LAMINAR ONE: `simulationType laminar` writes NO nut, k,
+     omega or epsilon, so requiring T1b's thermal-family tuple here would make
+     completion IMPOSSIBLE (the K0d defect, 829 core-minutes).
   5. ExecutionTime line count == endTime/deltaT (one line per timestep --
      the steady rungs' `count == endTime` expressed for deltaT != 1, T11 form)
   6. THE AGE GUARD: T at endTime NEWER than the case's own 0/T
@@ -22,7 +25,7 @@
 LINE (K0d L1).  NO `assert` (L-332).  --selftest forges cases in scratch and
 drives every clause under python3 and python3 -O.
 
-Usage: python3 mark_done_t14.py [CASE ...] [--root DIR] | --selftest
+Usage: python3 mark_done_t19.py [CASE ...] [--root DIR] | --selftest
 Exit: 0 all DONE, 1 at least one NOT DONE, 2 REFUSAL.
 """
 import os
@@ -30,8 +33,8 @@
 import sys
 
 HERE = os.path.dirname(os.path.abspath(__file__))
-CASES = ("T14_SQ_c", "T14_SQ_m", "T14_SQ_f", "T14_SQ_f_CT")
-NEEDED = ("T",)
+CASES = ("P_q_c", "P_q_m", "P_q_f", "P_Ts_c", "P_Ts_m", "P_Ts_f")
+NEEDED = ("T", "U", "p_rgh", "alphat")
 INFRA = ("wall_s", "timeout_s", "ranks", "core_min", "capped", "checkmesh_rc",
          "solver", "solver_path", "note", "started_utc", "ended_utc")
 EXIT_OK, EXIT_NOTDONE, EXIT_REFUSE = 0, 1, 2
@@ -120,7 +123,7 @@
     rc = EXIT_OK
     for case in want:
         if case not in CASES:
-            refuse("%r is not a registered T14 case: %s" % (case, " ".join(CASES)))
+            refuse("%r is not a registered T19 case: %s" % (case, " ".join(CASES)))
         fails, notes = check(root, case)
         for n in notes:
             print("NOTE      %-12s - %s" % (case, n))
@@ -135,30 +138,32 @@
     return rc
 
 
-def _forge(root, case, rc="0", end=True, n_exec=None, last="2", field=True, stale=False,
-           status=True, infra=True, et=2.0, dt=1e-4):
+def _forge(root, case, rc="0", end=True, n_exec=None, last="30000", field=True, stale=False,
+           status=True, infra=True, et=30000.0, dt=1.0):
     import time
     d = os.path.join(root, case)
     os.makedirs(os.path.join(d, "system"), exist_ok=True)
     os.makedirs(os.path.join(d, "0"), exist_ok=True)
     open(os.path.join(d, "system", "controlDict"), "w").write("endTime %g;\ndeltaT %g;\n" % (et, dt))
-    open(os.path.join(d, "0", "T"), "w").write("x")
+    for nm in NEEDED:
+        open(os.path.join(d, "0", nm), "w").write("x")
     t0 = os.path.getmtime(os.path.join(d, "0", "T"))
     n = int(round(et / dt)) if n_exec is None else n_exec
     open(os.path.join(d, "log.solve"), "w").write("ExecutionTime = 1 s\n" * n + ("End\n" if end else ""))
     tdir = os.path.join(d, last)
     os.makedirs(tdir, exist_ok=True)
     if field:
-        p = os.path.join(tdir, "T")
-        open(p, "w").write("y")
-        if stale:
-            os.utime(p, (t0 - 100, t0 - 100))
-        else:
-            os.utime(p, (t0 + 5, t0 + 5))
+        for nm in NEEDED:
+            q = os.path.join(tdir, nm)
+            open(q, "w").write("y")
+            if stale:
+                os.utime(q, (t0 - 100, t0 - 100))
+            else:
+                os.utime(q, (t0 + 5, t0 + 5))
     if status:
         s = "case=%s\nrc=%s\n" % (case, rc)
         if infra:
-            s += "wall_s=1\nranks=1\ncore_min=0.017\ntimeout_s=120\ncapped=%s\ncheckmesh_rc=0\nsolver=laplacianFoam\nsolver_path=/x\nnote=clean\nstarted_utc=t\nended_utc=t\n" % ("yes" if rc == "124" else "no")
+            s += "wall_s=1\nranks=1\ncore_min=0.017\ntimeout_s=120\ncapped=%s\ncheckmesh_rc=0\nsolver=buoyantBoussinesqSimpleFoam\nsolver_path=/x\nnote=clean\nstarted_utc=t\nended_utc=t\n" % ("yes" if rc == "124" else "no")
         open(os.path.join(root, "STATUS.%s" % case), "w").write(s)
 
 
@@ -167,15 +172,15 @@
     fails = []
 
     def drive(label, want_rc, want_marker, **kw):
-        tmp = tempfile.mkdtemp(prefix="t14md_")
+        tmp = tempfile.mkdtemp(prefix="t19md_")
         try:
-            _forge(tmp, "T14_SQ_c", **kw)
+            _forge(tmp, "P_q_c", **kw)
             code = None
             try:
-                code = run(tmp, ["T14_SQ_c"])
+                code = run(tmp, ["P_q_c"])
             except SystemExit as e:
                 code = e.code
-            marker = os.path.exists(os.path.join(tmp, "DONE.T14_SQ_c"))
+            marker = os.path.exists(os.path.join(tmp, "DONE.P_q_c"))
             ok = (code == want_rc and marker == want_marker)
             print("  [%s] %-58s -> exit %s, DONE marker %s" % ("ok " if ok else "FAIL", label, code, marker))
             if not ok:
@@ -188,9 +193,9 @@
     drive("rc=1 below cap -> NOT DONE (CRASH)", EXIT_NOTDONE, False, rc="1")
     drive("rc=124 capped=yes -> NOT DONE (CAPPED, no new budget)", EXIT_NOTDONE, False, rc="124")
     drive("no End line -> NOT DONE", EXIT_NOTDONE, False, end=False)
-    drive("last time 1.5 != endTime 2 -> NOT DONE", EXIT_NOTDONE, False, last="1.5")
-    drive("field T missing at endTime -> NOT DONE", EXIT_NOTDONE, False, field=False)
-    drive("ExecutionTime count 19999 != 20000 -> NOT DONE", EXIT_NOTDONE, False, n_exec=19999)
+    drive("last time 28000 != endTime 30000 -> NOT DONE", EXIT_NOTDONE, False, last="28000")
+    drive("registered fields missing at endTime -> NOT DONE", EXIT_NOTDONE, False, field=False)
+    drive("ExecutionTime count 29999 != 30000 -> NOT DONE", EXIT_NOTDONE, False, n_exec=29999)
     drive("T at endTime OLDER than 0/T -> NOT DONE (age guard)", EXIT_NOTDONE, False, stale=True)
     drive("all INFRASTRUCTURE fields absent -> DONE with NOTE (L-342)", EXIT_OK, True, infra=False)
     src = open(__file__).read()

--- DIFF: T14_runs/run_one_t14.sh  ->  T19_runs/run_one_t19.sh
================================================================================
--- T14_runs/run_one_t14.sh	2026-08-26 21:00:37.055375781 +0000
+++ T19_runs/run_one_t19.sh	2026-08-27 19:25:10.817053277 +0000
@@ -1,6 +1,6 @@
 #!/usr/bin/env bash
 # ==========================================================================
-# T14 CASE LAUNCHER (2-D transient conduction, laplacianFoam) -- run ONE registered level, capture the SOLVER's rc in
+# T19 CASE LAUNCHER (2-D laminar forced convection, buoyantBoussinesqSimpleFoam) -- run ONE registered level, capture the SOLVER's rc in
 # this wrapper, write STATUS.<case>.  Modelled line-for-line on
 # run_one_t11.sh's rc-in-wrapper pattern and launch_t4b.sh's queue interface:
 #
@@ -9,14 +9,14 @@
 #     box: `setsid timeout ... bash -c 'exit 7'` returns 0, `setsid --wait`
 #     returns 7 -- K0f header);
 #   * rc is captured INSIDE and written to STATUS.<case> in the run root (the
-#     file mark_done_t14.py reads);
+#     file mark_done_t19.py reads);
 #   * `capped` = (wall_s >= timeout_s) is the INDEPENDENT expiry witness -- rc
 #     124 collides with a solver that itself exits 124, and 137 with an OOM
 #     kill (measured, GNU coreutils 9.4); it is an INFRASTRUCTURE field
 #     (L-342): it labels a non-zero rc, it never voids a run;
 #   * a PRE-FLIGHT REFUSAL writes NO STATUS: nothing ran, so there is no rc,
 #     and inventing one is the back-dating this file exists to prevent;
-#   * the registered cap is READ from T14_registered.json and a --timeout that
+#   * the registered cap is READ from T19_registered.json and a --timeout that
 #     is not EQUAL to it is REFUSED: a caller can neither widen a cap that rule
 #     12 says stops the run nor narrow one into a manufactured cap-stop;
 #     --ranks must equal the registered 1;
@@ -27,8 +27,8 @@
 #   * `exit "$RC"` is the LAST line: the wrapper reports the solver's own
 #     status to its caller, never 0.
 #
-# usage: run_one_t14.sh --case-dir DIR --timeout SECONDS [--ranks 1]
-#                       [--solver laplacianFoam] [--foam-bashrc F]
+# usage: run_one_t19.sh --case-dir DIR --timeout SECONDS [--ranks 1]
+#                       [--solver buoyantBoussinesqSimpleFoam] [--foam-bashrc F]
 #                       [--no-detach]
 #   --timeout   registered per-case cap in wall seconds = cap_core_min*60/ranks
 #   --no-detach run in the foreground (the queue runner already detaches; the
@@ -36,12 +36,12 @@
 # ==========================================================================
 set -u
 SELF="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
-REGJSON="$SELF/T14_registered.json"
+REGJSON="$SELF/T19_registered.json"
 
 usage() { sed -n '/^# usage:/,/^# ====/p' "$0" >&2; exit 2; }
 
 CASE_DIR=""; TIMEOUT_S=""; RANKS=1
-SOLVER="laplacianFoam"; DETACH=1
+SOLVER="buoyantBoussinesqSimpleFoam"; DETACH=1
 FOAM_BASHRC="/usr/lib/openfoam/openfoam2606/etc/bashrc"
 while [ $# -gt 0 ]; do
     case "$1" in
@@ -58,7 +58,7 @@
 [ -d "$CASE_DIR" ] || { echo "REFUSE: no case directory $CASE_DIR" >&2; exit 2; }
 case "$RANKS" in ''|*[!0-9]*) echo "REFUSE: --ranks must be an integer" >&2; exit 2;; esac
 case "$TIMEOUT_S" in ''|*[!0-9]*) echo "REFUSE: --timeout must be integer seconds" >&2; exit 2;; esac
-[ "$RANKS" = "1" ] || { echo "REFUSE: T14 is registered SERIAL (ranks 1 on every level); --ranks=$RANKS" >&2; exit 2; }
+[ "$RANKS" = "1" ] || { echo "REFUSE: T19 is registered SERIAL (ranks 1 on every level); --ranks=$RANKS" >&2; exit 2; }
 
 CASE_DIR="$(cd "$CASE_DIR" && pwd)"
 ROOT="$(dirname "$CASE_DIR")"
@@ -69,7 +69,7 @@
 if [ -f "$REGJSON" ]; then
     REG_T=$(python3 -c "import json,sys; c=json.load(open(sys.argv[1]))['cases'].get(sys.argv[2]); print(c['timeout_s'] if c else 'UNREGISTERED')" "$REGJSON" "$CASE" 2>/dev/null || echo UNREADABLE)
     case "$REG_T" in
-        UNREGISTERED) echo "REFUSE: $CASE is not a registered T14 case in $REGJSON" >&2; exit 2 ;;
+        UNREGISTERED) echo "REFUSE: $CASE is not a registered T19 case in $REGJSON" >&2; exit 2 ;;
         UNREADABLE)   echo "REFUSE: cannot read the registered cap from $REGJSON" >&2; exit 2 ;;
     esac
     [ "$TIMEOUT_S" -eq "$REG_T" ] || { echo "REFUSE: --timeout $TIMEOUT_S is not the registered timeout_s=$REG_T for $CASE (rule 12: a cap is neither widened nor narrowed at launch -- a narrower one manufactures a cap-stop)" >&2; exit 2; }
@@ -81,8 +81,8 @@
 [ -e "$STATUS" ] && { echo "REFUSE: $STATUS exists -- a completed run's record is never overwritten; nothing ran, nothing written" >&2; exit 2; }
 
 # --- detach ONCE by re-executing this file in a new session ------------------
-if [ "$DETACH" = "1" ] && [ "${T14_DETACHED:-}" != "1" ]; then
-    T14_DETACHED=1 exec setsid "$0" --case-dir "$CASE_DIR" --timeout "$TIMEOUT_S" \
+if [ "$DETACH" = "1" ] && [ "${T19_DETACHED:-}" != "1" ]; then
+    T19_DETACHED=1 exec setsid "$0" --case-dir "$CASE_DIR" --timeout "$TIMEOUT_S" \
         --ranks "$RANKS" --solver "$SOLVER" --foam-bashrc "$FOAM_BASHRC" \
         --no-detach </dev/null >>"$CASE_DIR/log.launch" 2>&1 &
     echo "launched $CASE detached; STATUS will appear at $STATUS"
@@ -128,8 +128,8 @@
     [ -n "$d" ] && { echo "REFUSE: $CASE already has time directory $(basename "$d")" >&2; exit 2; }
 done < <(find "$CASE_DIR" -maxdepth 1 -mindepth 1 -type d -regextype posix-extended -regex '.*/[0-9]+(\.[0-9]+)?' 2>/dev/null)
 [ -d "$CASE_DIR/0.orig" ] || { echo "REFUSE: $CASE has no 0.orig to arm from" >&2; exit 2; }
-[ -f "$CASE_DIR/constant/polyMesh/owner" ] || { echo "REFUSE: $CASE has no mesh (constant/polyMesh/owner) -- run build_t14.py" >&2; exit 2; }
-for f in T; do
+[ -f "$CASE_DIR/constant/polyMesh/owner" ] || { echo "REFUSE: $CASE has no mesh (constant/polyMesh/owner) -- run build_t19.py" >&2; exit 2; }
+for f in T U p_rgh alphat; do
     [ -f "$CASE_DIR/0.orig/$f" ] || { echo "REFUSE: 0.orig/$f missing -- the registered field set is incomplete" >&2; exit 2; }
 done
 # LINEAGE-AWARE foreign-process guard (T10aR2 AMENDMENT 1, 9fa66065): under the
@@ -172,7 +172,7 @@
 # --- 3. arm 0/ from 0.orig; 0/T touched LAST (the age-guard datum) ----------
 cp -r "$CASE_DIR/0.orig" "$CASE_DIR/0" || { echo "REFUSE: could not create 0/ from 0.orig" >&2; exit 2; }
 sleep 1
-touch "$CASE_DIR/0/T" || { echo "REFUSE: could not touch 0/T" >&2; exit 2; }
+touch "$CASE_DIR/0/U" "$CASE_DIR/0/p_rgh" "$CASE_DIR/0/alphat" && touch "$CASE_DIR/0/T" || { echo "REFUSE: could not touch 0/T" >&2; exit 2; }
 
 # --- 4. the solver, in the FOREGROUND, rc captured from it ------------------
 T0=$(date +%s)

--- T19_runs/exact_t19.py : NEW FILE.  Its structural parent is T14_runs/exact_t14.py but the
    physics, the referent and the readers are different, so a line diff would be
    499 lines of noise and is NOT the right reading.  Read it whole.
================================================================================

--- T19_runs/build_t19.py : NEW FILE.  Its structural parent is T14_runs/build_t14.py but the
    physics, the referent and the readers are different, so a line diff would be
    453 lines of noise and is NOT the right reading.  Read it whole.
================================================================================

--- T19_runs/analyse_t19.py : NEW FILE.  Its structural parent is T14_runs/analyse_t14.py but the
    physics, the referent and the readers are different, so a line diff would be
    903 lines of noise and is NOT the right reading.  Read it whole.
================================================================================

--- T19_runs/T19_registered.json : NEW FILE.  Its structural parent is T14_runs/T14_registered.json but the
    physics, the referent and the readers are different, so a line diff would be
    283 lines of noise and is NOT the right reading.  Read it whole.
================================================================================
