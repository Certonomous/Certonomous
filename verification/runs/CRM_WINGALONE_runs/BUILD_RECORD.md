# CRM WING-ALONE MESH LADDER — BUILD RECORD

Registration: `verification/campaign/CRM_WINGALONE_PREREGISTRATION.md`, **frozen at `d2629d326`**.
Before any compute this lane hashed the worktree file against the committed blob:
**`1fa5fb725e6298d99dc4ab2207f55f11e575b285`, identical** — the frozen file IS the file that ran.

Scope, per §2: **mesh ladder only — extrusion + convert + `checkMesh` on three levels.
NO SOLVER RAN UNDER THIS DOCUMENT.**

Grading path, per §12: **`checkMesh` from `openfoam2606`, read by its PRINTED VERDICT LINES, never
by its rc.** Dimensionality is matched on **`geometric`** specifically; the `solution (non-empty)
directions` line is printed alongside it in every grade so the two cannot be confused.

## The family as built — every count matches §4 exactly

| level | surface cells | `N` | cell layers | **volume cells (checkMesh)** | §4 registered | match |
|---|---|---|---|---|---|---|
| L1 | 2,784 | 53 | 52 | **144,768** | 144,768 | ✅ |
| L2 | 11,136 | 105 | 104 | **1,158,144** | 1,158,144 | ✅ |
| L3 | 44,544 | 209 | 208 | **9,265,152** | 9,265,152 | ✅ |

Volume-cell ratios **8.000000** twice; linear **r = 2.000000** twice. `plot3dToFoam` read
**177,974 / 1,287,090 / 9,773,258** points = surface points x `N` exactly at every level.

## Gate results

*(G-M1..G-M3 from `checkMesh`; G-M4 from the pyHyp marching columns; G-M5 from the surface readers)*

See `GATE_TABLE.md` for the filled table with artifact paths.

## Free space, RE-READ IMMEDIATELY BEFORE EACH LEVEL (§6.1)

A reading taken before L1 is not evidence about the disk at L3, so each was taken fresh:

| moment | free | gate |
|---|---|---|
| before L1 extrude | **28.294 GiB** @ 21:34:32Z | — |
| before L1 convert | 28.083 GiB @ 21:39:06Z | — |
| before L2 extrude | **27.806 GiB** @ 21:41:54Z | — |
| before L1/L2 rebuild | 27.204 / 27.272 GiB @ 21:48Z | — |
| **before L3 extrude** | **27.544 GiB** @ 21:42:59Z | **>= 8 GiB — OPEN** |
| **before L3 convert** | **26.339 GiB** @ 21:52:59Z | **>= 8 GiB — OPEN** |

**The >= 8 GB gate was OPEN at every L3 checkpoint; no level was disk-`BLOCKED`.**

## Build parameters — and the two that are lane decisions, both disclosed

- `marchDist` = 25 x 3.758151 = **93.9537750 mesh-units, IDENTICAL at all three levels** (a family
  must occupy one domain).
- **`s0` halves with each level** (4.0e-4 / 2.0e-4 / 1.0e-4) so that §4's *"the extrusion refines
  normal to the wall"* is true. **Not a registered quantity** — see `BUILD_PARAMETERS.md`.
- **`pGridRatio` = 1.03 at all three levels**, forced by a bound pyHyp printed itself. **The probe's
  1.1 is INADMISSIBLE at L3.** See `PGRIDRATIO_FINDING.md`. L1 and L2 were **rebuilt** at 1.03 so the
  family is generated by one option set; the 1.1 logs are preserved under `VARIANT_pGridRatio_1.1/`.

## §8 rule-5 scaling assertion — DISCHARGED BY MEASUREMENT

`scaling_assert.py` reproduces **1 mesh-unit = 6.976368 m to a residual of 1.692e-07 m** from this
lane's own census tip-`y` of **3.7666681523** (independently identical across all three surfaces).
Cross-check **274.6602 in against `Cref` 275.80 in, 0.413 %**. The farfield reaches **20.2 x the
semispan**, printed so the domain box is not mistaken for the aircraft.
**No scaling was applied at conversion**: §5.3 records G-M1..G-M3 as scale-invariant, so scaling
could not change a graded number, and leaving coordinates untouched preserves the exact nesting.

## What this lane did NOT verify

- **No solver ran, no flow quantity exists.** §10A's successor owns all of that.
- **`checkMesh`'s two unregistered failures** — `***Error in face tets` and `***Cells with small
  determinant` — are **reported, not graded**: neither is a registered gate in §10. They are quoted
  because the face-tet count **corroborates G-M4 independently** at L1.
- **The `s0` anchor is not validated against any `y+` target**, because no flow condition is
  registered. It carries no boundary-layer claim.
- **I did not search option space for a configuration that turns L1's layer 3 positive.** Two
  parameters were changed, both disclosed and neither chosen after seeing a gate outcome. Further
  search would be fitting the build to a frozen gate.
