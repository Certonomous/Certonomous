# PROVENANCE AND STATUS OF THIS DIRECTORY — READ BEFORE CITING ANYTHING IN IT

**Written 2026-08-25 by the cfd lane that recovered it, under the cfd supervisor's
Ruling 2 of the same date. It is not the work of this lane.**

## The four things a reader must know, in the opening lines

1. **This directory is a PRIOR LANE'S WORK, recovered UNTRACKED.** Every file in it
   was found on disk with no commit — `git ls-files` returned nothing for the path
   and `git log` showed no history. It is committed now to **preserve** it, not to
   endorse it.
2. **Its `PROBE_PREREGISTRATION.md` was NEVER COMMITTED AND THEREFORE NEVER FROZEN
   BY SHA. Standing rule 2 is NOT SATISFIED for this run.**
3. **This run is `NOT A RESULT`, and that verdict is permanent and unrepairable.**
   `VERIFICATION_CHARTER.md` §2d closes gates at first compute, and §2d.1's
   four-condition repair exception **does not apply**, because there is no frozen
   original to repair. **No number in this directory may be cited as a graded
   value**, by any record, at any level.
4. **The mtimes CANNOT establish that registration preceded compute, and no such
   claim is made here.** The run's own contention stamp reads `2026-08-25T18:20:07Z`;
   `PROBE_PREREGISTRATION.md` and every evidence file share an mtime of
   `18:28:52`, which is the archival copy-in, eight minutes after the run. That
   ordering is consistent with a registration written before the run *and* with one
   written after it. **The lane recovering it is not asserting that it came first.**

## What it may be used for

Its diagnostic content is a **LEAD, not a verdict** — the same standard the cfd
supervisor applied to `check_grader_self_blindness.py` on this date: an instrument's
finding is a lead until something frozen confirms it.

**That confirmation now exists and is separate from this directory.** The frozen
replication at
`verification/runs/F12_runs/field_localisation_replication_2026-08-25/`, registered
at `verification/campaign/F12_FIELD_LOCALISATION_PREREGISTRATION.md`
(commit `56d72ac3`, blob sha256
`bca4074a7de26f478115a1efc1706c9cba6daedab8174cf72c202566d534b667`, frozen **before**
its compute), reproduces this arm's central observations under criteria fixed in
advance. **Cite the replication. Do not cite this directory for any number.**

## What was NOT done to it

`PROBE_PREREGISTRATION.md` is preserved **exactly as found and was not edited**.
No file in this directory was altered, renamed or removed. Nothing here was
back-dated, and no freeze was written over it after the fact.
