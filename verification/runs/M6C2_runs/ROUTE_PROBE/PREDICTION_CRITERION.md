# PREDICTION CRITERION — FIXED BEFORE THE PROBE RUNS

**The prediction** (registered in M6C1 Addendum 1 §A1.3, outcomes fixed in Addendum 2
§A2.3): *if the outward/inward normal asymmetry is the governing mechanism — outward
marching diverges and is stable, inward marching converges and folds — then a
hyperbolic extrusion outward from a valid capped surface clears both hard gates.*

**This probe reaches the condition that was never met before: extrusion from a VALID
CAPPED MULTI-PATCH SURFACE.** So it is testable. The criterion, fixed now:

| probe outcome | reading |
|---|---|
| extrusion completes and the volume clears **both** hard gates (max non-orthogonality ≤ 70, max skewness ≤ 4) **and** boundary closure, openness and face pyramids are clean | **SUPPORTED at one level** — see the asymmetry note below |
| extrusion completes and the volume **fails either** hard gate, or shows open cells / boundary openness / misoriented face pyramids | **FALSIFIED** |
| extrusion does not complete | **STILL UNTESTED** — a third failure-to-test, banked as nothing |

**🔴 THE EVIDENTIARY WEIGHT IS DELIBERATELY ASYMMETRIC, AND THIS IS NOT A HEDGE.**
Addendum 2's prediction says *"clears both hard gates AT EVERY LEVEL"*. **This probe is
ONE level on a BORROWED body.** A failure here contradicts the prediction outright — the
mechanism claims the outward march is stable regardless of whose wing it is, so one
counter-example falsifies it. **A success here cannot discharge "at every level" and
does not.** It supports the mechanism at one level and licenses the route; it does not
convert the prediction to CONFIRMED.

**Whichever row obtains is the row that gets reported.**
