<!-- INPUT FILE for scripts/append_record.py --path docs/COST_CALIBRATION.md --allocate-id.
     NOT a report. This is the rule-12 estimate-versus-actual row for this case, drafted
     2026-09-11 and NOT YET LANDED in docs/COST_CALIBRATION.md.

     WHY IT IS NOT LANDED -- BLOCKED, and not by anything in this row.
     scripts/append_record.py REFUSES (exit 8, D549 clause 1a) on the whole record because one
     PRE-EXISTING committed row carries an id that imitates the tool-allocated form without being
     one: HEAD:docs/COST_CALIBRATION.md line 522, id C-20260910T230023.521144Z-mrfr1a1 (the MRF_R1
     row). A tool-allocated id is <PREFIX>-<YYYYMMDDThhmmss.ffffffZ>-<8 hex>; the suffix mrfr1a1 is
     seven characters and contains a non-hex character, so the id pattern parses no id from a line
     that matches the id-bearing candidate shape. That is a refusal condition, not an absence.
     The SHAPE AUDIT names exactly one such line, so this is the only blocker.

     THE FIX IS NOT AN EDIT TO THIS RECORD and was not attempted here. The module states the two
     permitted repairs itself, both REGISTER EDITS INSIDE scripts/append_record.py: widen that
     records entry in RECORDS if the line carries a real id in a form the pattern cannot see, or
     add its form to KNOWN_EXCLUDED if it deliberately is not an id. That register guards four
     lab-wide records and its declared owner is the verification supervisor, so the choice is not
     this lanes and not one team alone. Measure which repair applies on the real bytes first.

     UNTIL THEN the rule-12 comparison for this case is BLOCKED, and it is recorded here rather
     than in a scratch path (CLAUDE.md rule 13: the scratchpad is never a handoff channel).
     Land with:
       python3 scripts/append_record.py --path docs/COST_CALIBRATION.md --rows <this file> --allocate-id
-->

| {{ALLOCATE_ID}} | 2026-09-11 | cfd | **MRF_R2 three-level triple** (Rushton-turbine power number `Np` via whole-tank MRF, navier_class; prereg FROZEN `558a430a` blob `dba6e43a`, check 4 undelegated; grading path `grade_mrf_np.py` sha256 `2a443bfe731ea43e` and `measure_states_mrf.py` sha256 `3c7bcfabca1fda81`, both verified byte-identical to the HEAD blob at grade time) — **TRIPLE AT `endTime` 4000 GRADES `NOT A RESULT`** (comparator exit 3). All three levels hold every rule-4 limb (rc=0 from both `rc` and `RC.txt`, one `End` each, 4,000 `Time` and 4,000 `ExecutionTime` lines, last time 4000 == `endTime`, `U p phi k omega nut` present at 4000, age guard clear on the solver-written `processor*/4000/` fields as well as the reconstructed `4000/`, with `decomposePar` carrying zero `-fields` occurrences and preceding the solver, and `reconstructPar` following it). `Np` 4.206205499 / 4.221156836 / 4.439168331 at 154,715 / 601,696 / 2,418,780 cells; delivered r21 **1.5900**, r32 **1.5726**, ratio gap 0.017446, `form=unequal` (Celik). Triple **DIVERGENT**, observed order **p = -5.7784**, **NO GCI quoted**. **Band verdict computed first and unconditionally: PASS (4.43917 in [4.0, 6.0]) — turned to `NOT A RESULT` by the one-way gate.** Planted-zero control PASSED on all three levels (planted 1.234e-03, reader `read_axial_torque` saw 1.234e-03). **The frozen §9 drift contingency FIRED**: fine's S12 relative drift is **-4.2576e-03**, 4.26x the registered 1e-3, so the registered graded fine is an `endTime` 8000 re-run that does not exist and the rung's registered display state is `PENDING: verification/runs/navier_class/MRF/R2/`. | **1,438 core-min** total (prereg §11 corrected table: coarse ~69, medium ~264, fine ~1,080, meshing ~25 — each already carrying the x1.19 concurrency factor measured on R1). `budget_gate: NONE — Sanaa 2026-09-10` (3-D cap-stop exemption), so the estimate is **calibration data, not a gate**. Predicted dollars 1,438/60 x $0.0513 = **$1.23 DERIVED, NOT MEASURED**. | **3,557.34 core-min MEASURED** from the logs (`core_min = wall_s x ranks / 60`): coarse 5,726 s x 2 = 190.87; medium 19,082 s x 2 = 636.07; fine 27,304 s x 6 = 2,730.40. Meshing a further **14.67 core-min** serial (53 + 190 + 637 s). CPU-held component **2,713.81 core-min** (ExecutionTime 2,805.12 / 11,997.59 / 22,203.9 s against the same rank counts). Solve + meshing = 3,572.01 core-min = 59.534 core-h -> **$3.05 DERIVED, NOT MEASURED** at $0.0513/core-h, reported-by-owner. 0 GPU-h. | **= gross (3,557.34 solve, 3,572.01 with meshing).** No level stalled. **WASTE 64.13 core-min, NAMED SEPARATELY AND NOT IN THE RATIO**: the fine level's 2-rank attempt (1,924 s x 2 ranks, rc=1, 161 of 4,000 iterations, SIGTERM by supervisor decision) at `verification/runs/navier_class/MRF/R2/fine/STOPPED_2RANK_ATTEMPT/`, inspected and retained, unrecoverable. Derived **$0.055**. | **2.518** solve-only (3,557.34 / 1,413); **2.484** including meshing (3,572.01 / 1,438). Per level: coarse **2.766**, medium **2.409**, fine **2.528**. | **SPLIT TWO WAYS, AND WASTE IS A THIRD THING KEPT OUT OF BOTH.** (1) **CONTENTION 843.53 core-min, 23.71 % of gross** (coarse 97.37 = 51.0 %, medium 236.15 = 37.1 %, fine 510.01 = 18.7 %). The x1.19 factor budgeted 19 % on top of the anchor; measured contention was **31.1 %** on top of CPU-held, so the factor was right in kind and about a third too small. (2) **MISPREDICTION OF THE UNIT COST, which is the larger half.** With contention removed, CPU-held 2,713.81 against the un-inflated anchor projection 1,187.39 is still **2.286x**. Measured unit cost rises with cells per rank: **9.066e-06 / 9.968e-06 / 1.3770e-05 core-s per cell-iteration** at 77,358 / 300,848 / 403,130 cells per rank, against the R1-coarse anchor **5.630e-06** measured at 38,679 cells per rank — 1.61x / 1.77x / 2.45x. **The lab's anchor is measured at the smallest cells-per-rank point in its own family and is therefore optimistic everywhere else; a cells-per-rank correction is the fix, not a bigger concurrency factor.** (3) **A DECISION RECORDED AGAINST ITS OWN METRIC:** the 2-rank attempt was stopped because it projected ~1,683 core-min against a registered ~1,080, and the 6-rank relaunch then spent **2,730.40 core-min, 1.62x the projection it was stopped to avoid**. It bought 1.85x in WALL time (6.826 s/iter against 12.626), a parallel efficiency of 0.617 from 2 to 6 ranks. The decision was right in wall-clock and wrong in core-minutes, which is the unit the case is costed in; the 2-rank counterfactual was never run and is not a measurement. | `verification/runs/navier_class/MRF/R2/MRF_R2_TRIPLE_AT_4000.json`; per-level `CORE_MINUTES.txt`, `WALL_SECONDS_SOLVE.txt`, `RANKS.txt`, `log.simpleFoam`; `verification/runs/navier_class/MRF/R2/fine/STOPPED_2RANK_ATTEMPT/README_STOPPED.txt`; prereg `verification/campaign/MRF_R2_PREREGISTRATION.md` at `558a430a` blob `dba6e43ac046ae642eae259efad524cb801a6004`. |
