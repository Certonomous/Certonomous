# ANSYS VALIDATION REGISTER — every VM2026R1 case the lab has run

**Owner:** `ansys-verification-supervisor`. **Charter:**
`docs/charters/ANSYS_VERIFICATION_CHARTER.md` §6. **Created:** 2026-08-24 by
the harness-build lane, empty. **Append-only** under the private-index
protocol (CLAUDE.md rule 10): a row is never edited after it lands; a
correction or a re-run is a new row citing the old one.

**Reading rule.** Every case run is a row, whatever its verdict. **Only `PASS`
rows are credentials** — the lab's credential count from this suite is the
number of `PASS` rows and nothing else. `GATE FAIL` and `NOT A RESULT` rows
stay here honestly, with their numbers; they are findings, not deletions.
Verdict vocabulary only: `PASS` / `GATE REACHED` / `GATE FAIL` / `NOT A RESULT`
/ `BLOCKED` / `PENDING`. Dates are UTC from a `date -u` read in the writing
invocation. Dollars are **derived** at the owner-stated $0.0513/core-h, never
measured (the box cannot read its own billing).

Reference result and tolerance are the **frozen** values from the case's
pre-registration, whose sha is the row's `prereg sha`; the comparator sha is the
committed grading script that produced the number.

| # | Case | Date (UTC) | Verdict | Lab value | Reference (source, manual p.) | Tolerance (frozen) | Artifact path | Prereg sha | Comparator sha | Cost, core-min (measured) | $ (derived) | RESULTS path |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| **1** | **VMFL001** — Flow Between Rotating and Stationary Concentric Cylinders (VM2026R1, pp. 15–16) | 2026-08-24 | **`NOT A RESULT`** | **none — comparator refused (exit 2) + L3 not converged.** The frozen comparator could not find its sampled file (v2606 writes `gateAxis_p_U.xy`, header-less; it was frozen to expect `U_gateAxis.*` with a header) and refused rather than guess a column; independently, L3 fails the registered iterative-convergence clause (final-iteration initial residuals Ux/Uy 1.19876e-06, p 2.89185e-06 vs frozen < 1e-6; plateau ptp 2.77178e-05 m/s vs frozen < 1e-6 m/s) while L1/L2 pass (4.69e-14 / 1.49e-12; ptp 1.00e-14 / 1.27e-11), which under CLAUDE.md rule 5 step 1 makes the rung `NOT A RESULT` before the triple is classified. No planted-zero firing, no triple, no GCI | v_θ at r = 20/25/30/35 mm = **0.0151 / 0.0105 / 0.0072 / 0.0046 m/s** — analytical, F. M. White, *Viscous Fluid Flow* §3-2.3, as printed in the manual's "Target, m/s" column, **manual pp. 15–16**. Context only, never the gate: Fluent 0.0151/0.0105/0.0072/0.0045, CFX 0.0150/0.0105/0.0071/0.0045 | **2 % relative at all four radii, at the finest level (L3, 64 × 256)** — frozen in `PREREGISTRATION.md` §3, justified from the manual's own printed-target rounding (worth 1.148 % at 35 mm) and tighter than the manual's own 3 % goal. Never reached | `verification/runs/ansys_verification/VMFL001/` (committed `ae30f914`) | **`d6ea5de9286ad5c8699b6e709e105c2cd484e8c1`** (as amended and as it ran; original freeze **`e0afc25936277798d3054137734fd9775311fbbb`**, commit `ffeed580`) | **`8cb29610e5d6f6fa4291df503a98bc99d0ff660f`** | **1.9833** (119 wall s, serial; cap 10, never approached) | **$0.0017** (derived at $0.0513/core-h, not measured) | `cases/ansys_verification/VMFL001/RESULTS.md` |
| **2** | **VMFL001-R2** — Flow Between Rotating and Stationary Concentric Cylinders (VM2026R1, pp. 15–16). **Re-run of row #1 after that row's `NOT A RESULT`**, per `VERIFICATION_CHARTER.md` §6: a new row citing the old one, which is not removed, re-labelled or softened. Two things changed and both repair a named run-1 mechanism — the comparator's reader matched to what OpenFOAM v2606 actually writes (header-less `gateAxis_p_U.xy`), and L3's `endTime` raised 3000 → 6000. **The gate, tolerance, diagnostic, mesh levels, solver settings and Roache quantity are UNCHANGED from row #1** | 2026-08-24 | **`PASS`** | **v_θ at r = 20/25/30/35 mm = `0.0151121317` / `0.0105287949` / `0.0071835454` / `0.0045457781` m/s** at L3 (64 × 256, 16384 cells), from `L3_64x256/postProcessing/radialProbes/6000/gateAxis_p_U.xy`. **Deviations vs the manual's printed targets: 0.0803 % / 0.2742 % / 0.2285 % / 1.1787 % — all four inside the frozen 2 %, so the gate is MET.** Deviations vs the exact White §3-2.3 formula: **0.0529 % / 0.0456 % / 0.0420 % / 0.0447 %** — the frozen 0.5 % diagnostic is met at all four radii too (the diagnostic is not the gate). The 35 mm row's 1.179 % is dominated by the manual's own rounding of its printed target (0.0046 vs exact 0.00454781 = 1.148 %), exactly as `PREREGISTRATION.md` §2 justified before any number was seen. **Roache triple on v_θ(35 mm), ratio 2.0, Fs 1.25: coarse `0.0045145840`, medium `0.0045395745`, fine `0.0045457781` m/s; R = `0.2482366`; state `CONVERGING`; observed order p = `2.0102`; GCI_fine = `5.6328e-04` = 0.0563 %; Richardson extrapolated `0.0045478265` m/s, which lands on the analytic value to 0.000374 %.** Monotone, so the GCI is quotable. **Planted-zero control PASSED: planted 1.234e-03, read_back_delta 1.234e-03, reader_delta 1.234e-03, agreeing to 1e-15 m/s, no other radius moved.** **Iterative convergence, all three levels ✅:** final Ux/Uy/p initial residuals L1 4.69e-14 / 4.57e-14 / 6.51e-11, L2 1.49e-12 / 1.49e-12 / 4.85e-11, **L3 1.61027e-09 / 1.61026e-09 / 8.43e-11** — L3 is 620× inside the 1e-6 clause it failed in row #1 (1.19876e-06 there); plateau ptp 1.00e-14 / 1.27e-11 / **1.772e-07 m/s**, all inside 1e-6. Strict completion holds at all three levels (rc 0; one `End`; last time == that level's endTime 3000/3000/**6000**; `U`,`p` present; `ExecutionTime` count == endTime; age guard ✅). Levers verified from the run tree: ν = 2.0e-4, ω = 1.0, `laminar`, cells 1024/4096/16384, `Mesh OK` ×3. Azimuthal spread over 8 rays ≤ **3.10e-13 m/s** anywhere — axisymmetric to round-off | v_θ at r = 20/25/30/35 mm = **0.0151 / 0.0105 / 0.0072 / 0.0046 m/s** — analytical, F. M. White, *Viscous Fluid Flow* §3-2.3, as printed in the manual's "Target, m/s" column, **manual pp. 15–16**. Exact formula values 0.0151201 / 0.0105336 / 0.00718656 / 0.00454781. Context only, never the gate: Fluent 0.0151/0.0105/0.0072/0.0045, CFX 0.0150/0.0105/0.0071/0.0045. **This box has no Fluent and no CFX; nothing here is a statement about Ansys** | **2 % relative at all four radii, at the finest level (L3, 64 × 256)** — frozen in `R2/PREREGISTRATION.md` §2 (unchanged from row #1's freeze), justified from the manual's own printed-target rounding (worth 1.148 % at 35 mm) and deliberately tighter than the manual's own 3 % goal. **Met at all four.** Secondary diagnostic, printed beside the gate and never the gate: 0.5 % vs the exact formula — also met at all four | `verification/runs/ansys_verification/VMFL001/R2/` (committed `fd2321ef`; grading JSON `GRADING_VMFL001_R2.json`, stdout `GRADING_VMFL001_R2.stdout.txt`, cost `COST.txt`) | **`c6b4a7c4fd09d2286a30440f5090116a7dae0eea`** (commit `4507fc66`; frozen before compute, no amendment, and re-read by the run script at launch — every `RUN_RC.txt` carries this blob) | **`64b02be807a8adb6c2638fe739c55f735c8259ca`** (run script `116c7c2d5fc5ebe1ac742f69478d66ed37dbfc17`; HEAD at run `1fb3bf74`) | **3.2833** (197 wall s, serial; cap 10 core-min, 32.8 % used, never approached; predicted 3.7167, ratio **0.8834×**) | **$0.002807** (derived at $0.0513/core-h, not measured) | `cases/ansys_verification/VMFL001/R2/RESULTS.md` |
| **3** | **VMFL005** — Poiseuille Flow in a Pipe (VM2026R1, p. 25). Fully developed laminar flow in a circular tube at Re = 500; only the axisymmetric half-domain is modelled. Reproduced in OpenFOAM v2606 `simpleFoam` on a one-cell-thick 5° **wedge** (half-angle 2.5°, wall vertices at **exact** radius R = 0.00125 m), steady, `laminar`, ν = 1e-5 m²/s, ρ = 1 kg/m³ — so the solved kinematic pressure is numerically equal to Pa. Inlet is `codedFixedValue` `poiseuilleInlet`, u_x(r) = 2·V_avg·(1 − (r/R)²) from each patch face's own centre radius; outlet `fixedValue` p = 0. **Independent of rows #1 and #2 and re-grades neither** | 2026-08-24 | **`PASS`** | **dP = `10.2909853852` Pa** at L3 (400 × 40, 16 000 cells), from the last rows (iteration 6000) of `L3_400x40/postProcessing/{pInletMonitor,pOutletMonitor}/0/surfaceFieldValue.dat`; `p_outlet = 0` **exactly** at all three levels (`fixedValue` outlet — measured, not assumed, and believed only because the planted-zero control below fired on this exact file). **Deviation vs the manual's printed target 10.24 Pa: 0.4979 % — inside the frozen 2 % gate by a factor of 4.0.** Deviation vs the exact Hagen–Poiseuille closed form: **the same 0.4979 %** to eight significant figures — unlike VMFL001 (`N-AV3`), none of this rung's deviation is the manual's printed rounding. **Per level: `10.23475566` / `10.27932262` / `10.29098539` Pa** (1 000 / 4 000 / 16 000 cells; endTime 2000 / 3000 / 6000). **Roache triple on dP, ratio 2.0, Fs 1.25: R = `0.261691`; state `CONVERGING`; observed order p = `1.9341`; GCI_fine = `5.0212e-04` = 0.0502 %; Richardson extrapolated `10.295119` Pa.** Monotone, so the GCI is quotable and is quoted. **THE FINDING, recorded because the `PASS` does not convey it (`N-AV7`, `RESULTS.md` §5): the deviation from the analytic reference (0.4979 %) is 9.92× the fine-grid discretisation uncertainty (GCI_fine 0.0502 %), and Richardson extrapolation moves the answer FURTHER from the analytic value — the extrapolated 10.295119 Pa is 0.5383 % above the exact 10.24 Pa against the fine level's 0.4979 %. The triple is genuinely `CONVERGING` and the gate is genuinely met, and grid refinement does NOT close the gap: roughly 90 % of the residual deviation is a modelling/setup signature, not discretisation error. The mechanism is UNRESOLVED and is not claimed — candidates and what is and is not measurable are in `RESULTS.md` §5.3, and the open question is docket `D510`.** **Planted-zero control PASSED:** planted 1.234 Pa into a **temp copy** of L3's `surfaceFieldValue.dat` (the run tree was never modified), read_back_delta 1.234, reader_dp_delta 1.234; the control fires before the verdict and the comparator refuses on its failure. **Iterative convergence, all three levels ✅:** final Ux/Uy/p initial residuals L1 4.14e-12 / 5.40e-08 / 7.10e-10, L2 1.36e-12 / 9.49e-08 / 7.37e-10, L3 1.73e-13 / 2.00e-08 / 8.39e-10 — worst gated channel 9.49e-08, 10.5× inside the frozen 1e-6; inlet-pressure plateau ptp 4.10e-10 / 3.20e-10 / 6.00e-11 over the last 400/600/1200 iterations, all inside the frozen 1e-4. `Uz` (1.85e-02 / 8.82e-03 / 2.54e-03) is the wedge's out-of-plane null channel, excluded from the gate by the freeze before any number was seen and printed rather than dropped (`N-AV8`). Strict completion holds at all three levels (rc 0; one `End`; last time == that level's endTime; `U`,`p` present; `ExecutionTime` count == endTime; age guard vs that level's own `0/U`). Levers read from the run tree: ν = 1e-5, `laminar` active-in-log, cells 1 000 / 4 000 / 16 000, `Mesh OK` ×3, **Re = 500.0000** (manual: 500). Wedge verified by the lane from `constant/polyMesh/boundary` (`wedge1`/`wedge2` both `type wedge`) and from the monitor header's patch area 6.809042402188e-08 m² = ½R² sin 5° to twelve significant figures — the comparator's `wedge_in_log` lever reads `false` because v2606 does not print patch types in the solver log, which is a null and not a failure | **dP = 10.24 Pa** — analytical, Hagen–Poiseuille, F. M. White, *Fluid Mechanics*, 3rd ed., McGraw-Hill, New York, 1994, as printed in the manual's "Target" column of Table .05.1, **manual p. 25**. Exact closed form 8·μ·L·V_avg/R² = 10.240000 Pa (μ=1e-5, L=0.1, V_avg=2.0, R=0.00125), cross-checked via 8·μ·L·Q/(πR⁴) — the two forms agree, so the reference is not a transcription. Context only, never the gate: Fluent 10.22 Pa (ratio 0.998), CFX 10.49 Pa (ratio 1.024); this lab's 10.2910 Pa is ratio 1.0050. **This box has no Fluent and no CFX; nothing here is a statement about Ansys** (`ANSYS_VERIFICATION_CHARTER.md` §2) | **2 % relative to the manual's printed target, at the finest level (L3, 400 × 40)** — frozen in `PREREGISTRATION.md` before any solver started, at blob `43aaf6bf` / commit `2d54a629` / 2026-08-24T18:42:07Z, with the first run artifact at 18:45:32Z. No amendment, no addendum. **Met with a factor of 4.0 of margin.** Secondary diagnostic, printed beside the gate and never the gate: **1 %** vs the exact Hagen–Poiseuille closed form — also met (0.4979 %) | `verification/runs/ansys_verification/VMFL005/` (committed `90ee8d80`, 54 files; grading JSON `GRADING_VMFL005.json`, stdout `GRADING_VMFL005.stdout.txt`, cost `COST.txt`). **The six gate-source `surfaceFieldValue.dat` files match `.gitignore:67` and are invisible to `git add`; they were filed by explicit path via `update-index --add`, which does not consult the ignore rules (L-300)** | **`43aaf6bf5c5f1189495e1460e5de415e56860447`** (commit `2d54a629`; frozen before compute, no amendment, and re-read by the run script at launch — all three `RUN_RC.txt` carry this blob) | **`8e7410cc356c3e175fe0d993efa366d51085ba81`** (run script `06056e18a9fee92e2e8765b279ae31f5373c62c5`; HEAD at run **derived** as `22abb11d` from the artifact timestamps against `git log`, and HEAD moved during the run — the exact binding is the per-level `prereg_blob` stamp, not a single HEAD) | **4.0000** (240 wall s, serial; L1 11 s / L2 29 s / L3 200 s; cap 13 core-min, 30.8 % used, never approached, no `CAP_EXCEEDED.txt`; predicted 4.9, ratio **0.8163×**; waste **0.000**, named separately; calibration row **C-47**) | **$0.003420** (derived at $0.0513/core-h, c7a.4xlarge, reported-by-owner — **not measured**, the box cannot read its own billing) | `cases/ansys_verification/VMFL005/RESULTS.md` |
| **4** | **VMFL051** — Isentropic Expansion of Supersonic Flow Over a Convex Corner (VM2026R1, pp. 165–166). Inviscid, compressible, ideal-gas supersonic flow turning 15° around a convex corner through a **centred Prandtl-Meyer expansion fan**; the manual's target is the post-expansion Mach number. **The lab's FIRST compressible/supersonic case from this manual** — rows #1–#3 are all incompressible `simpleFoam`. Reproduced in OpenFOAM v2606 **`rhoCentralFoam`**, a density-based shock-capturing solver, transient with `adjustTimeStep yes`, on a three-level refinement-2 family (6 240 / 24 960 / 99 840 cells) at the **same physical `endTime` = 7e−3 s at every level**. Inlet M 2.5, static T 300 K, p 202 636.9 Pa; adiabatic slip walls. **Independent of rows #1, #2 and #3 and re-grades none of them** | 2026-08-25 | **`NOT A RESULT`** | **Gate value (finest level L3, 480 × 208, 99 840 cells, 1 816 zone cells): `Ma = 3.2294355513`**, `volAverage(Ma)` over the frozen `gateZone` cellZone at `endTime`, from `L3_480x208/postProcessing/gateMach/0/volFieldValue.dat`. Deviation vs the manual's printed target 3.2370: **−0.233687 %**, which is INSIDE the frozen ± 0.5000 % band by a factor of 2.14 — **and the row is `NOT A RESULT` regardless, because rule 5 gates the gate.** **TWO INDEPENDENT CLAUSES OF `CLAUDE.md` RULE 5 EACH PRODUCE THIS VERDICT ON ITS OWN. (1) Rule 5 step 1, the frozen plateau clause: L1 AND L2 BOTH FAIL.** Peak-to-peak of the `volAverage(Ma)` gate series over each level's last 20 % of rows is **L1 6.240e−03** (over its last 339 of 1 693 rows) and **L2 3.535e−03** (last 673 of 3 365) against a frozen tolerance of **1.000e−03** — 6.24× and 3.54×. Only **L3** plateaus, at **8.549e−04** (last 1 343 of 6 714), 0.85× the tolerance. **(2) Rule 5 step 2, the Roache triple: `OSCILLATORY`.** Coarse **3.2278606097**, medium **3.2233427020**, fine **3.2294355513**; **d32 = +4.517908e−03**, **d21 = −6.092849e−03**, **R = −1.348600** — the increments change sign. **No observed order exists and NO GCI IS QUOTED**, correctly: the three values are not monotone. `triple.f_extrapolated` and `triple.gci_fine` are both `null`, so the ρ and dev_extrap readings that Amendment 1 registered in advance are **not computable**; this is that amendment's reading **(d)**, and none of readings (a) the VMFL001-R2 pattern, (b) the VMFL005 pattern or (c) indeterminate is available or claimed. **Per-level deviations vs the manual target: −0.282341 % / −0.421912 % / −0.233687 %**; vs the closed-form exact value: **−0.237380 % / −0.377014 % / −0.188704 %**. **DIAGNOSTIC ONLY AND NEVER THE GATE: the closed-form exact `M₂ = 3.2355411372251854`**, Prandtl-Meyer at **γ = 1.3990093734749485** derived from the manual's OWN Cp = 1006.43 J/kg-K and MW = 28.966 — **not** the textbook 1.4, at which the exact value is 3.2368431056638847. L3's deviation from it is **−0.188704 %** against a frozen ± 0.25 % diagnostic band: inside. **ALL THREE PLANTED-ZERO CONTROLS FIRED** (`CLAUDE.md` rule 3), every plant into a temporary copy and the run tree never modified: **PZ-1** gated `.dat` reader, planted 1.234e−03 Mach, read back 1.2339999999997353e−03; **PZ-2** the `Ma` FIELD reader on the real 99 840-cell L3 field, planted 7.77e−02, read back 7.770000000000010e−02, with the field measured **non-uniform** (min 2.49999805, max 3.396358576, mean 2.7006549026427984) — a uniform Mach field after an expansion fan is not a solution; **PZ-3** the reference computation itself, +5° of turn moving M₂ by 0.30002701584152813, with the identity ν(M₂) − ν(M₁) returning **14.999999999999972°** against the required 15°, so the root finder solves rather than returning a constant. **Declared inner-zone clause PASSED** and is recorded because its failure would have been a third independent route to the same verdict: at L3, the gate-zone and inner-zone averages differ by **2.284871e−04 = 0.0228 %** against the frozen 1e−2, inside by 43.8×. **Strict completion HELD at all three levels** (rc = 0 ×3; exactly one `End` line each; fields `T U p rho Ma` present; `ExecutionTime` count == `Time` count, 1 693 / 3 365 / 6 714 steps; age guard dated from the latest mtime anywhere in each level's own `0/`, STRICTER than the rule) — **with the two departures from rule 4's literal text declared on the face of the freeze (§8) before the answer was known, both tighter-or-equal rather than looser**: DEPARTURE 1 replaces `last time == endTime` with agreement to within `maxDeltaT` = 1e−5 s (`adjustTimeStep yes`; last times 0.0070015301 / 0.0069997882 / 0.0069999107 against 7e−3), and DEPARTURE 2 replaces the steady-iteration `ExecutionTime` count clause — which a transient adaptive-step solver can never satisfy — with a direct check of the invariant it protects, that the log is not truncated mid-step. **MECHANISM: A LEADING CANDIDATE WITH ARITHMETIC BEHIND IT, EXPLICITLY NOT ESTABLISHED** (`RESULTS.md` §3). The two coarse levels have not reached a steady plateau in the sampling zone at `endTime`, and an unplateaued coarse and medium value is the most likely origin of the non-monotone triple: the level-to-level differences are **the same order as the coarse levels' own residual unsteadiness** — |d32| = 4.518e−03 is **0.72×** L1's peak-to-peak and **1.28×** L2's, and |d21| = 6.093e−03 is **1.72×** L2's — so the triple is plausibly measuring transient noise rather than discretisation error. **Four reasons it is NOT claimed as established are given rather than glossed:** the arithmetic is a consistency argument and not a demonstration; **L2, not L1, is the outlier** (3.2279 / 3.2233 / 3.2294 — the medium level dips below both neighbours) and nothing here explains why the middle level specifically; **no time-refinement study was run and none was pre-registered**, so whether a longer `endTime` would restore monotonicity is untested and is not asserted; and L3 being both the plateaued level and the one closest to exact is suggestive, not evidence. **No re-run is proposed by this row and no `endTime` is recommended.** **THE DISCIPLINE WORKING AS DESIGNED, and worth stating plainly** (`RESULTS.md` §4): the gate value sits inside the frozen ± 0.5 % gate band, inside the tighter ± 0.25 % diagnostic band, and inside the manual's own 3 % accuracy goal (§1.3, p. 5) by **12.8×** — and rule 5 makes it `NOT A RESULT` anyway, because two of three levels never settled and the lab therefore cannot show the number is a converged property of the discretisation rather than a snapshot of a still-moving solution. Had the order been read gate-first, this row would today be a `PASS` and it would be unsupported. **The rule's one-way property held**: the gate turned a would-be `PASS` INTO `NOT A RESULT`, never the reverse. **MANUAL DEFECT, recorded on the frozen pre-registration §1a BEFORE any compute so it cannot be presented as a discovery that followed a number:** VMFL051's *"Analysis Assumptions and Modeling Notes"* (p. 165) calls the flow *"steady, inviscid, and **incompressible**"* while its own *"Physics/Models"* line **on the same page** says *"**Compressible**, inviscid flow"*, its Material Properties line says *"Density: Ideal Gas law"*, and the case is an M 2.5 → 3.24 isentropic expansion. **Quantified: an incompressible treatment produces no Mach change at all** — M stays 2.5 against a target of 3.2370, **−22.77 %**, failing the frozen gate by a factor of **45**; the comparator's `--selftest` fires exactly this arm. Two further manual defects are on the freeze §1a and are not re-derived here: the printed target 3.2370 is not the exact Prandtl-Meyer value at any γ consistent with the case (≈ 0.005 % of two-decimal ν table-rounding), and Table .51.2's value column is headed *"Ansys Fluent"* under a *"Results Comparison for Ansys CFX"* section. **All three are `NOT FILED`** — contacting Ansys is Sanaa's decision alone (`CLAUDE.md` rule 7). **No `N-AV` or docket id is cited by this row, because none has been appended for this case**: an id written into prose before its append is a prediction and not an identifier (`L-292`), which is the defect this register already carries once in row #3 and corrected in the dated note below | **Mach number after expansion = 3.2370** — the manual's printed "Target" column, **Table .51.1, p. 166**, an analytic Prandtl-Meyer value attributed to John Anderson, *Modern Compressible Flow: With Historical Perspective*, McGraw-Hill, 2002. Table .51.2 prints **3.237** for the same quantity. **Closed-form exact for the gas the manual actually specifies: 3.2355411372251854** at γ = 1.3990093734749485 — computed in the freeze §2 to full double precision rather than taken from the printed four decimals, and used ONLY as a declared diagnostic. Context only, never the gate: **Fluent 3.2316** (ratio 0.9980), **CFX 3.2354** (ratio 0.9995); this lab's 3.2294355513 is ratio 0.99766 against the printed target. **This box has no Fluent and no CFX; nothing in this row is a statement about Ansys** (`ANSYS_VERIFICATION_CHARTER.md` §2) — `VMFL051_FLUENT.cas` and `VMFL051_CFX.def` were never opened or run, and no VM2026R1 archive was touched | **± 0.5000 % relative to the manual's printed target, at the finest level (L3, 480 × 208)** — frozen in `PREREGISTRATION.md` §3.1 with its derivation in §3.3, and deliberately tighter than the manual's own 3 % goal by 6×. **The band was met (−0.233687 %) and the band is not what decides this row**: rule 5 steps 1 and 2 both fire first. Secondary diagnostic, printed beside the gate and never the gate: **± 0.25 %** vs the closed-form exact value — also met (−0.188704 %). **Plateau clause, frozen in §6: peak-to-peak of the gate series over each level's last 20 % ≤ 1.000e−03 in Mach** (= 3.1e−4 relative, 16× tighter than the gate) — **FAILED at L1 and L2**. Inner-zone clause, frozen in §6: gate-vs-inner disagreement ≤ 1e−2 at L3 — met at 2.285e−04 | `verification/runs/ansys_verification/VMFL051/` (committed **`fc46ad2d`**, 25 files: `GRADING_VMFL051.json`, `GRADING_VMFL051.stdout.txt`, `COST.txt`, `CONTENTION.txt`, and per level `RUN_RC.txt`, `log.rhoCentralFoam`, `log.blockMesh`, `log.checkMesh`, `log.topoSet` and both `postProcessing` `volFieldValue.dat` series). **The six gate-source `volFieldValue.dat` files match `.gitignore` and are invisible to `git add`; they were filed by explicit path via `git update-index --add`, which does not consult the ignore rules — `L-300`, met again on this case.** **DISCLOSED, not glossed: the OpenFOAM field data (~51 MB of per-time-directory volFields) is on disk and deliberately NOT committed.** Every graded number above is read from the committed `.dat` series and logs, with one exception — **PZ-2 read the L3 `Ma` field directly**, so if that run root is cleared PZ-2 becomes unreproducible from the repository alone, though its firing is recorded in the committed grading JSON | **`7dad56168d7ad7d599f92e05aa249a3014d0dc63`** (commit **`22249c82`**, 2026-08-25T00:20:33Z; **before-first-compute Amendment 1** at **`54d34542`**, 00:25:34Z, legal under `CLAUDE.md` rule 2 and altering **no** gate, threshold, cap or label; **first mesh written 00:25:59Z**, so zero compute preceded the freeze). **Verified in this lane by hashing rather than taken on report**: the worktree file, the HEAD blob and **all three** per-level `RUN_RC.txt` stamps carry this identical sha, so the binding is per level and not a single HEAD read. The freeze's own §2b.1 condition was checked and not asserted — at 00:15:28Z the run directory did not exist and `find` beneath it returned 0 files | **`acad1aff71da4a960045484f9e6f8470f8beccb7`** (`grade_vmfl051.py`; run script `run_vmfl051.sh`). Identical across the worktree file, the HEAD blob and all three `RUN_RC.txt` stamps. The launcher's own guards — refuse to start into any pre-existing level directory (rule 4's guard), refuse unless the pre-registration is committed at HEAD, refuse if `topoSet` left either sampling zone empty, and enforce the cap with `timeout` — none fired as a refusal | **23.3167** (1 399 wall s × 1 rank ÷ 60, serial, from `COST.txt` and corroborated level by level against the three `RUN_RC.txt`: L1 9 s / 0.1500, L2 207 s / 3.4500, L3 1 183 s / 19.7167). **Cap 28 core-min, 83.27 % used, never reached, no `CAP_EXCEEDED.txt`.** Predicted point estimate 11.3, **ratio 2.0634×**. **Waste 0.000, named separately and netted off nothing** — no level failed or restarted, no re-run, comparator exit 0 first invocation. **CONTENTION 14.408 core-min, MEASURED and named separately from the ratio** (`COMPUTE_BUDGET_CHARTER.md` §6): solver CPU is **534.52 s** (final `ExecutionTime` 7.71 + 59.31 + 467.50) against 1 399 s of wall, on a 16-core box carrying loadavg **68.38 → 75.84** of other teams' jobs. **The frozen solver estimate was GOOD — 534.52 s measured against a 637 s solver-plus-function-object allowance, 0.839× and conservative — and essentially the whole 2.06× overrun is contention.** Calibration row **`C-50`** | **$0.019936** (derived at $0.0513/core-h, c7a.4xlarge, **reported-by-owner — NOT measured**, the box cannot read its own billing, `COMPUTE_BUDGET_CHARTER.md` §5) | `cases/ansys_verification/VMFL051/RESULTS.md` |
| **5** | **VMFL045** run 1 — Oblique Shock Over an Inclined Ramp (VM2026R1, pp. 153–154). The team's **first shock-capturing case** (rows #1–#3 incompressible `simpleFoam`; #4 VMFL051 a smooth expansion). Reproduced in OpenFOAM v2606 **`rhoCentralFoam`**, transient with `adjustTimeStep yes`, on a three-level refinement-2 family (6 840 / 27 360 / 109 440 cells) at the same physical `endTime` = 7e−3 s. **Independent of rows #1–#4 and re-grades none of them.** Re-run pending as **VMFL045-R2** (`cases/ansys_verification/VMFL045/R2/`), a new rung citing this row, which is NOT removed, re-labelled or softened | 2026-08-25 | **`NOT A RESULT`** (tier: **`NOT HELD`** — the supervisor's ruling; this row carries its tier inline per Sanaa's directive that every future row does so at grading time). **`rhoCentralFoam` CRASHED on its first timestep, `rc=1`, `wall=0 s`**, before any real solve: `FOAM FATAL IO ERROR: Entry 'e' not found in dictionary system/fvSolution/solvers`. **Nothing gradeable was produced**, so — as with row #1 — there is nothing to hold; `GATE REACHED` was not available because there is no believable measurement missing one column, there is no measurement at all | **none — the solver crashed before any solve.** Root cause (reached independently by two lanes AND the supervisor): frozen `thermophysicalProperties` sets `energy sensibleInternalEnergy` (energy field `e`) but the frozen `fvSolution`, cloned from the **inviscid** VMFL051, provides a solver named `h` and no `e`. VMFL045 is **viscous** (μ = 1e-8, the manual's own value, p. 153), so `rhoCentralFoam` entered the implicit viscous energy corrector — a path VMFL051's μ = 0 never touched — and aborted for want of an `e` entry. **Proof:** `smoothSolver: Solving for Ux` count — VMFL051's entire run 0, VMFL045 before death 1. What DID succeed on L1: `blockMesh` OK, `checkMesh` `Mesh OK`, `topoSet` filled both frozen zones (gateZone 252, gateZoneInner 148). A setup defect in the frozen inputs, not a numerical failure. **A comparator `--selftest` (45/45) could not have caught it — it proves the grader, not the case**; the R2 launcher adds a pre-flight solver smoke test as the generalisable fix | **Mach number after the oblique shock = 1.874** — the manual's printed target, Tables .45.1/.45.2, p. 154 (analytic oblique-shock, White 1994). Diagnostic (never the gate): as-modelled exact M₂ = 1.8749769576810524. Context only, never the gate: **Fluent 1.902** (would FAIL our 1 % gate at +1.494 %), **CFX 1.871** (would pass at −0.160 %). **This box has no Fluent and no CFX; nothing here is a statement about Ansys** | **1.0 % relative to the manual's target 1.874, at L3_360x304** — frozen in `PREREGISTRATION.md` §3.1 before any compute, derived from the 0.079 % systematic floor and tighter than the manual's 3 % goal. **Never reached** (the solver never advanced a real step) | `verification/runs/ansys_verification/VMFL045/L1_90x76/` (crashed run 1: `log.blockMesh`, `log.checkMesh`, `log.topoSet`, `log.rhoCentralFoam` the fatal, `RUN_RC.txt` rc=1; run-root `CONTENTION.txt`, `launch.stdout.txt`). **Filed by explicit path via `update-index --add` because `log.*` matches `.gitignore` (L-300).** No time directories, no fields; `COST.txt` never written (launcher refused before its summary). Tree LEFT IN PLACE as the evidentiary core of this `NOT A RESULT`; R2 runs in its own fresh directory | **`7a7f9d52fe8666a5ef3dd72c6dd4262e93b75b74`** (freeze commit `6a9701e1`; inputs `2198f9b2`; freeze verified intact before launch — all three named blobs and all ten §10 input blobs match HEAD, worktree byte-identical to HEAD) | **`0c83eeef1199e1376d7ef400d9e4fb5f3d30e526`** (`grade_vmfl045.py`; run script `db393261`; `--selftest` 45/0, `--verify-frozen` passes) | **0.0000** (crash at wall 0 s before any real solve; `RUN_RC.txt` `core_min=0.0000`). **Ratio actual/predicted UNDEFINED — the run never executed** (not 0.0×: an interruption is not a calibration, L-265); predicted 20.4, cap 48, never approached. Calibration row in `docs/COST_CALIBRATION.md` | **$0.00** (derived at $0.0513/core-h, c7a.4xlarge, reported-by-owner — NOT measured) | `cases/ansys_verification/VMFL045/RESULTS.md` |
| **6** | **VMFL003** — Pressure Drop in Turbulent Flow Through a Pipe (VM2026R1, p. 19). The team's **first turbulent case** — rows #1–#3 laminar/incompressible, #4–#5 inviscid compressible. Fully developed turbulent flow in a smooth pipe at **Re = 13 691.740248127866** (ρ = 1.225, μ = 1.7894e−5, U = 50 m/s, D = 0.004 m, L/D = 500), reproduced in OpenFOAM v2606 `simpleFoam` + **`kEpsilon`** with **`nutkWallFunction`** on a one-cell-thick 5° wedge (half-angle 2.5°, wall vertices at **exact** radius R), k-ε coefficients written out explicitly rather than defaulted. **N_r held FIXED at 5 across the Roache triple and refinement done AXIALLY** — a ratio-2 *radial* triple is impossible here because R⁺ = 408.35 puts the wall-function window at a factor of 2.72 < 4 (`N-AV10`) | 2026-08-25 | **`NOT A RESULT`** — **tier `NOT HELD`** *(drafted by the lane; tiers are the supervisor's ruling and overrulable)*. **V is present and G is actively negative, as in row #4.** The comparator refused at §6 step 1: **all three levels failed the frozen iterative-convergence residual leg** (final initial residuals of p, Ux, k, ε each < 1.0e−8); L3 missed on **ε alone, 2.523e−08, by 2.5×**, L1 and L2 on all four. Pre-registered branch — §4.5 declared the iteration counts estimates and §11 item 5 registered this exact outcome before compute. **`GATE REACHED` was refused deliberately:** the gate was not reached, it was missed by 3.6× the band | **Δp = 20 800.824487444752 Pa** (L3_1000x5, converged to **7.8 ppm** between the two finest grids). `gate_verdict_before_rule5` = **`GATE FAIL`** at **−4.337635727351215 %**; rule 5 then turned it to `NOT A RESULT` — the permitted direction only. **Colebrook diagnostic (2.0 %) also MISSED at −4.552199389548252 %**, reported here with the same prominence as the gate, not in a footnote. **f_dev = 0.027147309070475995, −4.626379490974265 % vs Colebrook** — the clean model statement. **Roache `CONVERGING`, R = 0.0804, observed order 3.6364, GCI `null`** (never quoted on a `NOT A RESULT` row); the order is **not trusted** — d21 = 0.161 Pa is 7.8 ppm, too small to extract an order from. **y⁺ 37.60014411059 mean at L3, inside the declared [25, 65] one-way band; min 23.812, below the floor, disclosed** | **21 744 Pa** — Moody-chart smooth-pipe branch as the manual prints it (VM2026R1 p. 19, Tables .03.1/.03.2); exact Colebrook 21 792.879830032474 Pa held as a **diagnostic, never the gate**. Ansys context only: Fluent 21 480, CFX 21 740 | **2.5 % relative**, frozen at `9195d25e` before any compute. Declared in advance to pass **both** Ansys solvers, because the manual's own two 'standard k-ε' codes differ by **1.210428 %** on this case and a tighter band would gate their disagreement, not the lab. **The lab missed it by 3.6×** | `verification/runs/ansys_verification/VMFL003/` | **`9195d25e`** (freeze); prereg blob `e969e654…` at freeze → **`778c3a3c…`** after **ADDENDUM 1**, which repaired the **launcher** before any graded compute and **moved no gate, threshold, band, cap, label or verdict** | **`15b14d40f166cc31770ead452c27905332670c97`** — **byte-identical and untouched**, `--verify-frozen HEAD` rc=0, `--selftest` 60 checks / 0 failures re-run as a gate on this grading | **13.5 core-min** (measured; 810 s wall × 1 rank), **56 % of the 24 core-min cap; not exceeded** | **$0.011543 DERIVED** at $0.0513/core-h (owner-stated; the box cannot read its own billing) | `cases/ansys_verification/VMFL003/RESULTS.md` |
| **7** | **VMFL045-R2** — Oblique Shock Over an Inclined Ramp (VM2026R1, pp. 153–154). **Re-run of run 1 (row #5)** on the single input fix `h` → `"(h\|e)"` in `fvSolution` (run 1 died on its first timestep for want of an `e` energy-solver entry, inherited from the inviscid VMFL051; VMFL045 is viscous, μ = 1e−8). Reproduced in OpenFOAM v2606 **`rhoCentralFoam`**, transient (`adjustTimeStep yes`), three-level refinement-2 family (6 840 / 27 360 / 109 440 cells) at physical `endTime` = 7e−3 s. **Independent of rows #1–#4 and #6 and re-grades none of them. Cites row #5's `NOT A RESULT`, which is NOT removed, re-labelled or softened** | 2026-08-25 | **`PASS`** (tier: **`GATE REACHED`** — the `ansys-verification-supervisor`'s ruling; this row carries its tier inline per Sanaa's directive). **The G column is the limb held back, and this row names it:** observed order **p = 3.3862** is **measured but NOT trusted** — it is **above the shock-capturing scheme's formal order**, so it is not an asymptotic-range measurement; **d21 = −2.447e−4 is only ~3× L3's plateau floor 7.77e−5** (the same diagnostic that condemned VMFL051, there ~1, here ~3); so **GCI_fine 0.0017 % is NOT a discretisation-uncertainty statement** (`N-AV7` second form: a small GCI licenses nothing). **V is strong** (exact reference matched to 0.0106 %). **`HOLDS` was refused** — P is not clean. The verdict was **not** softened and the order **not** re-run toward a better value | **M₂ = 1.874779041082** at the finest level L3_360x304; per level **1.8719757370 / 1.8745343357 / 1.8747790411** (zone 252 / 1 005 / 4 025 cells). **Roache triple** (r = 2, Fs = 1.25): R = 0.095640, state **`CONVERGING`**, observed order **p = 3.3862** (measured, not trusted), GCI_fine 0.0017 %, Richardson **1.8748049198**. Plateau ptp **3.84e−4 / 6.14e−5 / 7.77e−5** over 3127 / 6305 / 12661 rows, all under the 1.0e−3 clause — **§7's plateau mitigation worked; the VMFL051 failure mode did not recur.** **Planted-zero PZ-1/2/3 all FIRED.** Diagnostics (never the gate): exact as-modelled M₂ 1.8749769577 (dev −0.0106 %); T 382.1123 K (vs exact 382.1101, vs manual 382, +0.0294 %); ρ 2.278042 (vs exact 2.277884, vs manual 2.277, +0.0458 %) | **Mach 1.874** — manual printed target, Tables .45.1/.45.2, **p. 154** (analytic oblique-shock, White 1994). **Exact closed form derived here: 1.874976957681054** (M1 = 2.5018146, β_weak 36.923178°, θ 15.000000°). Context only, never the gate: **Fluent 1.902** (would **FAIL** our 1 % gate at +1.494 %), **CFX 1.871** (passes at −0.160 %). **We landed at 1.8748, nearest the EXACT — not nearest either code; this is NOT agreement with Ansys.** This box has no Fluent and no CFX | **1.0 % relative to the manual target 1.874, at L3_360x304** — frozen in `PREREGISTRATION.md` §3.1 before any compute (blob `592e872b` after the dated pre-compute AMENDMENT 1; freeze commit `4b3f512e`; comparator freeze `946bbf24`). **Met with 24× margin** (+0.041571 %). Secondary diagnostic band ±0.5 % vs the exact form, also met (−0.0106 %) | `verification/runs/ansys_verification/VMFL045/R2/` — evidence committed `c80c7188` (27 files by explicit path: `GRADING_VMFL045_R2.json` + `.stdout.txt`, `COST.txt`, `CONTENTION.txt`, `SMOKE.txt`, `launch.stdout.txt`, per-level `RUN_RC.txt` + logs + gate-source `volFieldValue.dat`). Both frozen zones non-empty at every level. **Strict completion holds** (rc=0, an `End` line, fields Ma T U p rho, age guard PASS; `adjustTimeStep` last time ~0.0069998 s accepted by the frozen comparator) | **`592e872b5738f33cbbbb5eac6440e7174cbed95e`** (`PREREGISTRATION.md` after the dated pre-compute AMENDMENT 1; freeze commit `4b3f512e`; the run stamped this blob in every `RUN_RC.txt`, binding the grading path to the freeze) | **`382ff4975801c5911277fb463076d1a14dd813c6`** (`grade_vmfl045_r2.py`; freeze commit `946bbf24`; `--verify-frozen HEAD` exit 0 before grading; `--selftest` 45/0) | **26.6667 core-min measured** (L1 0.3667 + L2 2.6667 + L3 23.6333; total wall 1600 s, serial RANKS=1; `COST.txt`). Predicted 20.4 → **ratio 1.307×**; cap 48, 55.6 % used, no `CAP_EXCEEDED`. **Measured contention: ClockTime/ExecutionTime = 1.021 / 1.004 / 1.000 per level** (L3 idle-wait 0.0 %) — peer VMFL003 co-resident the whole solve, but a serial job was not core-starved, so contention is negligible and **not charged**; **waste 0**. Calibration **C-58** | **$0.02280** (derived at $0.0513/core-h, c7a.4xlarge, reported-by-owner — **not measured**) | `cases/ansys_verification/VMFL045/R2/RESULTS.md` |

| **8** | **VMFL007** run 1 — Non-Newtonian Flow in a Pipe (VM2026R1, p. 29). The team's **first non-Newtonian case** — rows #1–#3 laminar Newtonian incompressible, #4–#5 inviscid compressible, #6 turbulent. Fully developed laminar flow of a **power-law** fluid (k = 10 Pa·sⁿ dynamic, n = 0.4, ρ = 1000 kg/m³, D = 0.0025 m, L = 0.1 m, V = 2 m/s, generalised Re = 84.597), reproduced in OpenFOAM v2606 `simpleFoam` + `viscosityModels::powerLaw` (k = 0.01 **kinematic**, the mandatory ÷ρ conversion) + `laminar { model Stokes; }` on a one-cell-thick **1° wedge** — 1° and not the team's usual 5° so the `sec(t/2)` pressure-drop bias (+0.0038 %) sits below the reference's own four-figure rounding (±0.0083 %). Planned r = 2 triple 625 / 2 500 / 10 000 cells at endTime 10 000 SIMPLE iterations. | 2026-08-25 | **`NOT A RESULT`** | **none — NO NUMBER WAS PRODUCED. The case DIVERGED.** `L1_25x25` completed all 10 000 iterations with rc = 0 and an `End` line — **and `rc = 0` plus an `End` line is not convergence**: its areaAverage(p)_inlet grew from 3.022488374470e+04 to **9.449536950130e+144 m²/s²** against a physical 60.522, and its continuity error from 0.0832213752667 at iteration 1 to **1.04772404566e+72**, the divergence starting at **iteration 2**. From **iteration 904** every cell in the domain sat on the `nuMin` floor of 1e−08, **4 298× below** the physical wall viscosity 4.298435325556426e−05 — a shear-thinning positive feedback that removed the only damping that could have arrested it. `L2_50x50` died at iteration 9 065 with **SIGFPE (rc 136) inside `GAMGSolver::scale`**, which is an **arithmetic overflow symptom** (its pressure had reached 6.034959177466e+211 and a squared inner product overflows a double above 1.341e+154), **not a linear-solver defect**. `L3_100x100` never launched. **The normalised residual channel was BLIND throughout** — p held in ≈[0.15, 0.46] and Ux in ≈[0.042, 0.072] for all 10 000 iterations, because OpenFOAM's residual normalisation is scale-invariant — and **zero** `nan`, `inf` or bounding messages appear in 90 076 + 81 671 log lines. **The `nuMax = 1.0` ceiling never bound: ZERO hits on either level, measured.** | **60.52 kPa** = 60521.96938383448 Pa closed form (Rabinowitsch–Mooney); W.F. Hughes and J.A. Brighton, *Schaum's Outline of Theory and Problems of Fluid Dynamics*, McGraw-Hill 1991; manual p. 29 Table .07.1. Ansys Fluent 60.41 kPa, Ansys CFX 61.52 kPa — context only. **Reference kind: closed-form/exact — buys V, never P.** | **0.5 %**, band [60217.40, 60822.60] Pa, frozen at 48f7a9bf before compute; the frozen gate was **never applied** because rule 5 step 1 fires first — no level was iteratively converged | `verification/runs/ansys_verification/VMFL007/` (`L1_25x25/`, `L2_50x50/`, `CONTENTION.txt`) | `48f7a9bf930fbd06bf23ba4b6469754f879cb74e` | `9a727216a73eedcdfbeff2b40c0f5a8f65db65e6` (frozen at `23091132`; **never run to grade — it did not need to be, and three of its own clauses each independently refuse this run**) | **6.2666** of a frozen **60** cap (L1 3.8833 + L2 2.3833); cap never bit, spend 10.4 % of it; **2.3833 core-min named as WASTE**, not absorbed | **$0.00536** derived at $0.0513/core-h, **derived not measured** | `cases/ansys_verification/VMFL007/RESULTS.md` | **TIER: `NOT HELD`** — the supervisor's ruling on its own crash triage. **Nothing was measured**, so there is nothing to hold: no triple, no observed order, no GCI, no plateau statistic, no quotable Δp. V was available (the reference is closed-form) and is unearned for want of any number to compare. Same shape as rows #1 and #5. Re-run registered as **VMFL007-R2**, a six-arm linear-solver/preconditioner slate under Sanaa's rule 2, machinery at `1387b89f`; **run 1's tree, pre-registration and comparator are PRESERVED — not edited, not cleared, not re-labelled** (VERIFICATION_CHARTER §6: a re-run is a new row citing the old one). |

| **9** | **VMFL003-M2 arm A** — `kEpsilon` + `nutkWallFunction`, the "try other models" baseline/control (VM2026R1 p. 19; new case id `VMFL003_M2`, cites run-1 row #6, re-grades nothing). Pressure drop, fully developed turbulent pipe flow, Re = 13 691.74; `simpleFoam` serial, six meshes — a three-level axial Roache triple at fixed N_r = 5 (`L1_250x5`/`L2_500x5`/`L3_1000x5`, ratio 2, the freeze's NAMED gate triple, prereg §9) plus the wall-treatment ladder `D_500x3/4/6` (L2 reused). The ONLY change vs run 1 is the endTime bump 6000/8000/12000 → 15000/18000/22000 to clear run-1 Defect B (iterative convergence). | 2026-08-25 | **`NOT A RESULT`** — the comparator refused at rule 5 step 1: **all three Roache levels failed the frozen iterative-convergence residual leg** (final initial residuals of p, Ux, k, ε each &lt; 1.0e−8). **L3 missed on ε alone, 2.49442e−08, by 2.5×; L1 and L2 missed on all four.** `gate_verdict_before_rule5` = **`GATE FAIL`**; rule 5 turned it to `NOT A RESULT` — the permitted direction only. **KEY FINDING: the endTime bump did NOT fix Defect B** — L3's ε floor is still missed (run 1 2.523e−08, here 2.494e−08) and L1/L2 sit at ~1e−6 on Ux/k/ε, while Δp is fully plateaued at every level (the normalised residual never reaches 1e−8). **`GATE REACHED` refused: the gate was missed by 3.6× the band, not reached.** | **Δp = 20 800.824488339003 Pa** (L3_1000x5). Dev vs target 21 744 = **−4.337635723 %**; dev vs Colebrook 21 792.88 = **−4.552199385 %**. **f_dev = 0.027147309, −4.626 % vs Colebrook 0.028464** — the clean model statement; standard k-ε under-predicts developed pipe friction here. **Roache CONVERGING, monotone** (coarse 20 802.99 / med 20 800.99 / fine 20 800.82), R = 0.0804, observed order **p = 3.6364 NOT TRUSTED** (d21 = 0.161 Pa ≈ 7.8 ppm, below the noise floor), **GCI `null` — not quoted on a NOT A RESULT row.** y⁺ L3 mean 37.600 in [25,65], min 23.812 ≥ 11.06 floor. Ladder spread 1.7356 % &lt; 5 %. **All three planted-zero controls FIRED** (Δp+ρ seen 1.51165 Pa = expected ρ-live; y⁺ 7.77; f_dev 9.38e−6) — the reader is not self-blind. **Strict completion rule 4 HOLDS at all six levels: rc=0 (`RUN_RC.txt`), an End line, last==endTime, fields U p k epsilon nut, ExecutionTime count==endTime, age guard.** | **21 744 Pa** — Moody-chart smooth-pipe branch as the manual prints it (VM2026R1 p. 19, Tables .03.1/.03.2); exact Colebrook 21 792.879830 Pa is a diagnostic only. Reference kind **category 2 (correlation) → V, ruled by Sanaa 2026-08-25**; P not attainable. Ansys context only: Fluent 21 480, CFX 21 740. | **2.5 % relative to 21 744, at L3_1000x5**, frozen in `PREREGISTRATION.md` §5 (blob `cdbf2659`, freeze `c5fdcad4`) before any compute; unchanged from run 1. **Missed by 1.735×.** | `verification/runs/ansys_verification/VMFL003_M2/A_kEpsilon/` (six level dirs, `RUN_RC.txt` rc=0 each, `GRADING_A_kEpsilon.json`; independent regrade reproduced the committed json byte-for-byte) | **`cdbf2659b6eec2599fc3eda7a149aaca391461b0`** (freeze commit `c5fdcad4`; launcher freeze-check at launch_head `117d8adc` logged `PREREGISTRATION.md == HEAD (cdbf2659)`) | **`6dcc99940154ea204a598ba2118042bf972a786d`** (`grade_vmfl003_m2.py`; `--verify-frozen HEAD` rc=0, `--selftest` clean, `check_grader_self_blindness.py` clean re-run as a gate on this grading) | **29.962 core-min** (measured; ExecutionTime-derived, 1797.71 s wall × 1 rank; 74.9 % of the 40 per-arm cap, not exceeded) | **$0.025617 DERIVED** at $0.0513/core-h (owner-stated; the box cannot read its own billing) | `cases/ansys_verification/VMFL003_M2/GRADING_LANE_REPORT.md` (RESULTS.md is the supervisor's to finalise) | **TIER: `NOT HELD`** *(lane-drafted, supervisor-overrulable)* — **V present, G actively negative.** The reference is a category-2 correlation so V is available but unearned for want of an in-band number; the gate was missed by 3.6×, so `GATE REACHED` is refused exactly as run-1 row #6 refused it. Same shape as rows #1, #5, #6, #8. |

| **10** | **VMFL003-M2 arm B** — `realizableKE` + `nutkWallFunction` (VM2026R1 p. 19; case id `VMFL003_M2`, cites run-1 row #6 and arm A row #9, re-grades nothing). The ONLY change vs arm A is the `RASModel` keyword `kEpsilon` → `realizableKE`; same meshes, same wall function, same endTimes, same gate triple. | 2026-08-25 | **`NOT A RESULT`** — same frozen residual leg as arm A: **all three Roache levels failed** (final initial residuals of p, Ux, k, ε each &lt; 1.0e−8); **L3 missed on k = 1.377e−07 and ε = 7.490e−08; L1/L2 broadly.** `gate_verdict_before_rule5` = **`GATE FAIL`**; rule 5 → `NOT A RESULT` (permitted direction). Δp fully plateaued at every level while the residual floor is unmet. **`GATE REACHED` refused: gate missed by 2.7× the band.** | **Δp = 20 278.128649 Pa** (L3_1000x5). Dev vs target = **−6.741498 %**; dev vs Colebrook = **−6.950670 %**. **f_dev = 0.026462670, −7.03 % vs Colebrook.** **realizableKE under-predicts pipe friction MORE than standard k-ε (arm A −4.34 % vs arm B −6.74 %).** **Roache CONVERGING, monotone** (coarse 20 280.51 / med 20 278.47 / fine 20 278.13), R = 0.1657, observed order **p = 2.5930 NOT TRUSTED** (near-noise-floor differences), **GCI `null`.** y⁺ L3 mean 37.491 in [25,65], min 23.784 ≥ 11.06. Ladder spread 0.6746 % &lt; 5 %. **All three planted-zero controls FIRED** (Δp+ρ 1.51165 Pa, y⁺ 7.77, f_dev 9.38e−6). **Strict completion rule 4 HOLDS at all six levels** (rc=0 each, End, last==endTime, fields U p k epsilon nut, ExecutionTime count, age guard). **Mechanism diagnostic (secondary, NOT a credential): f_dev moved 2.52 % from arm A — this EXCEEDS the prereg Arm-2 &gt;1 % "materially moved friction" threshold and FALSIFIES Arm-4's prediction that ε-family variants on a shared log-law wall function stay within ~1 % of A; caveat — both arms are NOT A RESULT on the residual leg, so this rests on plateaued-but-not-residual-converged Δp, and Arm-5's four-model falsification is NOT evaluable — only two of four arms are gradeable, and both non-gradeable arms are `NOT A RESULT` on incompleteness (arm C ruled at `aba61e53`; arm D stopped at its frozen 40 core-min per-arm cap, 27.0 % of endTime, ruled by the supervisor 2026-08-25).** | **21 744 Pa** — manual p. 19, Tables .03.1/.03.2; Colebrook 21 792.879830 Pa diagnostic only. Reference kind **category 2 (correlation) → V**; P not attainable. Fluent 21 480 / CFX 21 740 context only. | **2.5 % relative to 21 744, at L3_1000x5**, same frozen band (blob `cdbf2659`, freeze `c5fdcad4`). **Missed by 2.70×.** | `verification/runs/ansys_verification/VMFL003_M2/B_realizableKE/` (six level dirs, `RUN_RC.txt` rc=0 each, `GRADING_B_realizableKE.json`; independent regrade reproduced the committed json byte-for-byte) | **`cdbf2659b6eec2599fc3eda7a149aaca391461b0`** (freeze `c5fdcad4`; launcher freeze-check logged `== HEAD (cdbf2659)`) | **`6dcc99940154ea204a598ba2118042bf972a786d`** (`grade_vmfl003_m2.py`; verify-frozen + self-blindness clean) | **31.152 core-min** (measured; ExecutionTime-derived, 1869.11 s × 1 rank; 77.9 % of the 40 per-arm cap) | **$0.026635 DERIVED** at $0.0513/core-h (owner-stated; not measured) | `cases/ansys_verification/VMFL003_M2/GRADING_LANE_REPORT.md` | **TIER: `NOT HELD`** *(lane-drafted, supervisor-overrulable)* — V present (category-2 correlation), G actively negative (gate missed by 2.7×); `GATE REACHED` refused. Same shape as rows #1, #5, #6, #8, #9. |
| **11** | **VMFL003-M2 arm C** — `RNGkEpsilon` + `nutkWallFunction` (VM2026R1 p. 19; case id `VMFL003_M2`, cites run-1 row #6 and arm A row #9, re-grades nothing). The ONLY change vs arm A is the `RASModel` keyword `kEpsilon` → `RNGkEpsilon`; same meshes, same wall function, same endTimes, same gate triple. | 2026-08-25 | **`NOT A RESULT`** — **LADDER INCOMPLETE.** Ruled by the supervisor at commit `aba61e53`. Level `D_500x3` stopped at **`Time = 4085` of `endTime 18000` — 22.7 %**, no `End` line. **This was NOT a crash and NOT a divergence:** the frozen `PER_ARM_CAP = 40` core-min fired as registered. The arm consumed **39.93 of 40 core-min (99.8 %)**. Rule 4 (strict completion) fails on the `End`, `last time == endTime` and field-presence conjuncts; the row therefore carries **no lab value at all** and no gate was ever evaluated. **NO FRESH CAP was granted** — CLAUDE.md rule 12 as it stood at the run. | **none — the arm never reached `endTime` at every level, so no Δp was graded.** Reporting a partial-time Δp as the lab value would be reporting an unconverged transient as a steady answer. **Diagnostic only, NOT a credential:** on the identical mesh `L2_500x5` at identical `endTime`, `RNGkEpsilon` cost **4.23× the arm-A baseline** — and arm C's `L3` is **CHEAPER than its own `L2` at twice the cells**, which is the inverse of what cell count predicts. Reads as a badly-converging inner linear solve, **configuration-specific, not model-specific**. **CONTENTION IS FALSIFIED AS THE CAUSE:** `ExecutionTime` (CPU) tracks `ClockTime` (wall) to within **0.3 %** on every run in the rung (arm C `L2` 1413.1 s / 1416 s), so these processes were never starved and no contention story belongs in this row's calibration. | **21 744 Pa** — manual p. 19, Tables .03.1/.03.2. Reference kind **category 2 (correlation) → V**; P not attainable. **Never compared** — see verdict. | **2.5 % relative to 21 744, at L3_1000x5**, same frozen band (blob `cdbf2659`, freeze `c5fdcad4`). **Never evaluated.** | `verification/runs/ansys_verification/VMFL003_M2/C_RNGkEpsilon/` — **and note the instrument gap: NO `RC.txt` or `record.json` exists anywhere under `VMFL003_M2`**, because the launcher's `rc=` printf runs only on the completing path. **Rule 4's `rc = 0` conjunct is therefore UNEVALUABLE FROM DISK for this entire rung.** Rule 4 is a conjunction, and an unevaluable conjunct is one nobody is applying. Recorded as a finding, not hidden. | **`cdbf2659b6eec2599fc3eda7a149aaca391461b0`** (freeze `c5fdcad4`) | **`6dcc99940154ea204a598ba2118042bf972a786d`** (`grade_vmfl003_m2.py`) — **never ran on this arm** | **39.93 core-min** (measured; ExecutionTime-derived; **99.8 % of the 40 per-arm cap**) | **$0.034140 DERIVED** at $0.0513/core-h (owner-stated; not measured) | `cases/ansys_verification/VMFL003_M2/TRIAGE_ARM_D_BUDGET_STOP.md` (covers arms C and D; arm C's ruling is `aba61e53`) | **TIER: `NOT HELD`** — not an honest FAIL but a blocker: the arm produced no number. |
| **12** | **VMFL003-M2 arm D** — `kOmegaSST` + `nutkWallFunction` (VM2026R1 p. 19; case id `VMFL003_M2`, cites run-1 row #6 and arm A row #9, re-grades nothing). The ONLY change vs arm A is the `RASModel` keyword `kEpsilon` → `kOmegaSST` (with the ω-family field set); same meshes, same wall function, same endTimes, same gate triple. | 2026-08-25 | **`NOT A RESULT`** — **LADDER INCOMPLETE.** Ruled by the supervisor at commit `2dcea996`. Level `L3_1000x5` stopped at **`Time = 5949` of `endTime 22000` — 27.0 %**, no `End` line, `ExecutionTime 1662.74 s` against a wrapper `timeout 1663`. **The cap fired TO THE SECOND, exactly as registered** — the launcher's own abort text says `rc=124 means the BUDGET fired`. The arm consumed **39.99 of 40 core-min (100.0 %)**. **Not a crash, not a divergence, not a solver defect.** Rule 4 fails on `End`, `last time == endTime` and field presence. **NO FRESH CAP** — ruled on precisely the ground arm C was ruled on. | **none — no Δp was graded.** **Diagnostic only, NOT a credential:** on the identical mesh `L2_500x5` at identical `endTime`, `kOmegaSST` cost only **1.06× the arm-A baseline** — yet its `L3` ran **7× over its own mesh scaling**. Cheap at L2 and catastrophic at L3 is not a model-cost signature. **CONTENTION IS FALSIFIED:** arm D `L3` `ExecutionTime` 1662.74 s against `ClockTime` 1662 s — the process was never starved. **This is exactly what Sanaa's *"try different pre conditioners, see if that's a resolution"* instruction anticipates**, and `VMFL007_R2` is the instrument already built for it. **NOT run here:** each remedy changes the experiment and needs its OWN frozen registration — a stopped arm is never quietly re-run until it fits. | **21 744 Pa** — manual p. 19, Tables .03.1/.03.2. Reference kind **category 2 (correlation) → V**; P not attainable. **Never compared.** | **2.5 % relative to 21 744, at L3_1000x5**, same frozen band (blob `cdbf2659`, freeze `c5fdcad4`). **Never evaluated.** | `verification/runs/ansys_verification/VMFL003_M2/D_kOmegaSST/` — same missing-`RC.txt` instrument gap as row #11. | **`cdbf2659b6eec2599fc3eda7a149aaca391461b0`** (freeze `c5fdcad4`) | **`6dcc99940154ea204a598ba2118042bf972a786d`** (`grade_vmfl003_m2.py`) — **never ran on this arm** | **39.99 core-min** (measured; ExecutionTime-derived; **100.0 % of the 40 per-arm cap**) | **$0.034192 DERIVED** at $0.0513/core-h (owner-stated; not measured) | `cases/ansys_verification/VMFL003_M2/TRIAGE_ARM_D_BUDGET_STOP.md` | **TIER: `NOT HELD`** — a blocker, not an honest FAIL. |
| **13** | **VMFL019** — Transient Flow Near a Wall Set in Motion (VM2026R1, p. 77). Stokes' first problem: an infinite flat wall impulsively set in motion under a fluid at rest. Reproduced in OpenFOAM v2606 `icoFoam` (transient, incompressible, laminar), nu = 1e-3 m2/s, U_wall = 0.01 m/s, domain 0.75 m (x, cyclic — the flow is homogeneous in x for an infinite plate) x 0.3 m (y), one cell in z. **The manual prints FIGURES ONLY for this case** (.19.2/.19.3) and NO Target/Ansys/Ratio table, so there is no number in the manual to gate against. The frozen pre-registration therefore declared the **analytic Schlichting gate IN ADVANCE** — §2 of the freeze names both probe values before any solver ran. **That is what makes this row a credential rather than a digitisation of a printed curve**; a gate read off a plot after the fact would be neither. Independent of every prior row and re-grades none of them. | 2026-08-25 | **`PASS`** — both gate probes inside the frozen band; both Roache triples `CONVERGING`; every control fired. | **u_x(y=0.05 m, t=5 s) = 6.16765356837e-3 m/s** (dev **0.0502 %**) and **u_x(y=0.10 m, t=5 s) = 3.17007704028e-3 m/s** (dev **0.0954 %**), both at L3_120. **Roache `CONVERGING` and monotone at both probes**, with space AND time refined together at ratio 2 (Ny 30/60/120, dt 0.05/0.025/0.0125 s): observed order **p = 1.0933** and **0.9881**, **GCI_fine (Fs = 1.25) = 0.0579 %** and **0.1208 %**. The orders sit at the formal first order of the Euler time integration refined in lockstep with the mesh — **trustworthy, NOT a noise floor**, so the prereg §9 fallback to `GATE REACHED` does **not** fire and the ceiling `HOLDS`. **Planted-zero control FIRED**: 1.234e-3 m/s planted into u_x of probe 0 at L3_120, read back off disk as 1.2339999999999999e-3. **Strict completion rule 4 HOLDS at all three levels** — `rc = 0` evidenced on disk in each `RUN_RC.txt`, one `End` line in each `log.icoFoam` (matched by EXACT name, never a `log*` glob, which matches `log.blockMesh` first), last `Time = 5` == `endTime 5`, `U` and `p` present at `endTime`, **`ExecutionTime` count == 5/dt EXACTLY (100 / 200 / 400)**, and the age guard passed at every level. Comparator `--selftest` exit 0, including both gate arms (+2 % refused, +0.5 % admitted) and the `DIVERGENT`/`OSCILLATORY`/`EXACT` branches. | **u_x = U*erfc(y/(2*sqrt(nu*t)))** — analytic: 6.170750774519737e-3 m/s and 3.1731050786291404e-3 m/s at the two probes. H. Schlichting & K. Gersten, *Boundary Layer Theory*, 8th ed., pp. 126–127, 2000; manual p. 77. Reference kind **EXACT closed form → category V**. Ansys Fluent/CFX publish figure-only agreement — **no scalar value exists to quote, and none is used as the gate**. | **1 % relative, at L3_120, BOTH probes** (frozen prereg blob `e303c4dea3d82644e487a11ba173306cabf8d0ec`, §5). | `verification/runs/ansys_verification/VMFL019/` (levels `L1_30`, `L2_60`, `L3_120`; `RUN_RC.txt` rc=0 each — and each additionally carries `prereg_blob=e303c4de…`, so the RUN itself records the identity of the registration it ran under; `GRADING_VMFL019.json`) | **`e303c4dea3d82644e487a11ba173306cabf8d0ec`** (verified equal to `HEAD:…/PREREGISTRATION.md` at grading time) | **`c20d72fc4e03a431f0d874b943420ca2de1c6a8c`** (`grade_vmfl019.py`; verified equal to HEAD at grading time) | **0.0167 core-min** (measured from `RUN_RC.txt`; **0.19 % of the frozen 9.00 cap**) | **$0.000014 DERIVED** at $0.0513/core-h (owner-stated; **not measured** — the box cannot read its own billing) | `cases/ansys_verification/VMFL019/RESULTS.md` | **TIER: `HOLDS`** *(lane-drafted, supervisor-overrulable)* — V present and exact, G positive and inside band, triples CONVERGING with trustworthy orders. |
| **14** | **VMFL010** — Laminar Flow in a 90° Tee-Junction (VM2026R1, p. 39). Steady laminar flow entering a planar T-junction and dividing between two outlets held at equal static pressure; the graded quantity is the fraction of the inlet mass flow leaving the main branch. Reproduced in OpenFOAM v2606 `simpleFoam` (steady, laminar, incompressible), nu = 0.003333 m2/s, parabolic `codedFixedValue` inlet at Uc = 1.0 m/s, on a three-level refinement-2 family. Independent of every prior row and re-grades none of them. | 2026-08-25 | **`NOT A RESULT`** — CLAUDE.md **rule 5 step 2**: the grid triple is **`OSCILLATORY`**, and a triple that is not `CONVERGING` is `NOT A RESULT` **whatever the value**. **NOT SOFTENED, and the near-agreement does NOT rescue it: the L3 value is 0.26 % from the reference 0.887 — well inside the frozen 3 % band — so a value-only reading calls this `GATE REACHED`. It is not one.** The gate can only turn a result INTO `NOT A RESULT`, never the reverse, and this row is **not a credential**. | **flow split = 0.8847487181565803** (L3, 57 600 cells). Triple **`OSCILLATORY`**: **0.8859493355955057 / 0.8844529270402999 / 0.8847487181565803** (L1/L2/L3) — it falls then rises, d(L1→L2) = **−1.4964e-3**, d(L2→L3) = **+2.958e-4**. **NO GCI is quoted, because a GCI is never quoted when the three values are not monotone**, and no observed order is fitted. **Rule 5 step 1 was checked FIRST and PASSED**, so the oscillation is genuine grid response and not unconverged levels: every level reached `SIMPLE solution converged` on its own, with final initial residuals Ux ≈ 9.9e-8 (9.925464525e-08 / 9.822268888e-08 / 9.946326653e-08) and p between 3.3e-10 and 8.8e-10. **Mechanism, recorded as a finding and NOT acted on:** the gate quantity is an *integral ratio* of two patch mass flows whose discretisation error is already down at 1e-3–1e-4 relative, and the L2→L3 change is **5.06× smaller in magnitude than L1→L2 but carries the opposite sign** — the signature of a quantity whose leading-order truncation term no longer dominates its own grid response. Richardson extrapolation has no meaning there, which is exactly why rule 5 refuses the row rather than printing a fitted order. **Refining further will not fix this**; the honest repairs are a gate quantity with a cleaner grid response, or a coarser triple with a larger ratio keeping L1 inside the asymptotic range — **each needs its OWN frozen registration**, and changing a frozen gate quantity after compute is not a lane's to do. **Instrument defect, disclosed:** `grade_vmfl010.py` **writes no verdict artifact** — it prints to stdout and exits 0, so on disk `rc = 0` on a `NOT A RESULT` is indistinguishable from `rc = 0` on a pass. **The frozen comparator was NOT edited** (rule 2 fixes the grading path at the pre-registration commit); its stdout was captured verbatim beside the run with blob hashes, and the repair belongs in the launcher. | **flow split = 0.887** — R.E. Hayes, K. Nandkumar, H. Nasr-El-Din, *Steady Laminar Flow in a 90 Degree Planar Branch*, Computers & Fluids 17:537–553 (1989); manual p. 39, Tables .10.1/.10.2/.10.3. Reference kind **code-to-code / published numerical benchmark → buys NEITHER V NOR P**, declared in the freeze. Ansys Fluent 0.884 and CFX 0.8837 are **context only** and are not the gate. | **3 % relative to 0.887, at L3** (frozen prereg blob `4d30fb4c80e6dc9c4cdedf893020d3092b632baa`, §6 — the manual's own stated 3 % accuracy goal, §1.3). **Met on value, but never reached as a verdict.** | `verification/runs/ansys_verification/VMFL010/` (levels `L1`, `L2`, `L3`; `RUN_RC.txt` rc=0 each; `COST.txt`; captured verdict at `GRADING_VMFL010.stdout.txt` with grader and prereg blob hashes) | **`4d30fb4c80e6dc9c4cdedf893020d3092b632baa`** (verified equal to `HEAD:…/PREREGISTRATION.md` at grading time) | **`8bb2b2640428f6856c3e33432ca37f9d60dcd585`** (`grade_vmfl010.py`; verified equal to HEAD at grading time; `--selftest` exit 0, 4/4) | **3.5833 core-min** (measured from `RUN_RC.txt`: 0.2167 + 0.5167 + 2.8500; **9.29 % of the frozen 38.57 cap**) | **$0.003064 DERIVED** at $0.0513/core-h (owner-stated; **not measured**) | `cases/ansys_verification/VMFL010/RESULTS.md` | **TIER: `NOT HELD`** *(lane-drafted, supervisor-overrulable)* — the registered ceiling was **`GATE REACHED`, NEVER `PASS`** (code-to-code reference, declared in the freeze at §4), and the row did not reach even that. |
| **15** | **VMFL050** — Transient Heat Conduction in a Semi-Infinite Slab (VM2026R1, p. 163). Unsteady conduction in a thick copper plate, initially uniform at 293 K, suddenly exposed to a constant 3e5 W/m2 heat flux on one face; the temperature field after 120 s is graded at two probes. Reproduced in OpenFOAM v2606 `laplacianFoam` on a three-level family with space and time refined **together** at ratio 2. **Ruled personally by the `ansys-verification-supervisor`, 2026-08-25** (`SUPERVISION_CHARTER` §3 check 3), on a run that was already on disk — zero fresh compute. **This row is landed late: the case was carried in `CASE_MAP.md` as `NEVER RUN` when it had in fact run, been graded and been ruled** — a graded result that never reached the register is compute this lab paid for and did not bank. | 2026-08-25 | **`PASS`** — both probes inside the frozen band, both triples `CONVERGING` and monotone, every control satisfied. **Tier capped at `GATE REACHED` by a clause frozen BEFORE the run**, not after. | **wall T = 392.9773033924 K** (dev on the rise **2.2697e-4**, i.e. 0.0227 %) and **T at 150 mm = 318.401437549 K** (dev on the rise **5.6596e-5**, i.e. 0.0057 %), both at L3_300. Against the **analytic** value rather than the manual's rounded print the agreement is better still: 393.02659961 K exact vs 392.97730339 lab, and 318.40595772 exact vs 318.40143755 lab. Triples: **wallT** 392.8632 → 392.9336 → 392.9773, monotone increasing, `CONVERGING`, **p = 0.6883**, GCI_fine 2.2724e-4; **p150** 318.4169 → 318.4018 → 318.4014, monotone decreasing, `CONVERGING`, **p = 5.5982**, GCI_fine 2.5860e-08. **The p150 order is SUSPICIOUSLY HIGH — a warning, not a win**: space and time are refined together at ratio 2 under an implicit Euler integration, so the combined formal order is ≈ 1 and cannot exceed 2; 5.6× the formal order is a **noise floor**, with d21 = 3.125e-4 on a value of 318.4 (relative separation 9.8e-7 — fine and medium agree to seven significant figures, so the ratio producing p is computed at the level of probe interpolation and write precision). **Planted-zero control FIRED**: 1.234 K planted into both gate quantities, read back as 1.2339999999999804 off disk. **Strict completion rule 4 HOLDS at all three levels** — `rc = 0` evidenced on disk in `RUN_RC.txt`, `n_exec == expect_steps` (60/60, 120/120, 240/240), `endTime` 120.0 reached, age guard passed. **Ansys's own Fluent numbers (392.95 K, 318.41 K) are CONTEXT ONLY and are not the gate.** | **393 K and 318.4 K** — F.P. Incropera, D.P. Dewitt, T.L. Bergman, A.S. Lavine, *Introduction to Heat Transfer*, 5th ed., Wiley & Sons, p. 287, 2007; manual p. 164, Table .50.1. Reference kind **closed-form analytic → category V**. | **1 % relative, on the temperature RISE (T − 293 K), at the finest level, BOTH probes** (frozen prereg blob `c816fc5506ee6b3b20412688b89c878eb16a6291`, §5). **Met by roughly 44× and 177× of margin respectively.** | `verification/runs/ansys_verification/VMFL050/` (levels `L1_75`, `L2_150`, `L3_300`; `DONE.flag` and `RUN_RC.txt` rc=0 each, each carrying `prereg_blob=c816fc55…`; `GRADING_VMFL050.json`) | **`c816fc5506ee6b3b20412688b89c878eb16a6291`** (verified equal to `HEAD:…/PREREGISTRATION.md`) | **`22e7c758d117fe23c4b549bbc18512c73d6a2023`** (`grade_vmfl050.py`; the blob the freeze names as the grading path; rule 5 read AS CODE by the supervisor at lines 336–339, not as output) | **≤ 0.05 core-min** (measured: `RUN_RC.txt` records `wall_s=0` at all three levels against a frozen **8** core-min cap — **the case is faster than the launcher's 1-second wall-clock resolution**, so the honest statement is a bound, not a point value) | **≤ $0.000043 DERIVED** at $0.0513/core-h (owner-stated; **not measured**) | `cases/ansys_verification/VMFL050/SUPERVISOR_RULING_VERDICT.md` | **TIER: `GATE REACHED`** — declared as the prereg §9 fallback **before any number existed**: *"if the triple is `CONVERGING` but the observed order is untrustworthy (noise-floor), the ceiling drops to `GATE REACHED`"*. That fallback fires on the p150 triple. `HOLDS` refused. |
| **16** | **VMFL059** — Conduction in a Composite Solid Block (VM2026R1, p. 185). Steady conduction through a two-material plane wall, one material carrying a uniform volumetric heat source, the other exposed to convective cooling (h = 1000 W/m2K, T_inf = 303 K); the two graded quantities are the temperatures of the cooled wall and of the far adiabatic wall. Reproduced in OpenFOAM v2606 `laplacianFoam`. **Ruled personally by the `ansys-verification-supervisor`, 2026-08-25**; HEAD `94510794` is that ruling's commit. **Landed late — the case was carried as `NEVER RUN` when it had already run, graded and been ruled.** | 2026-08-25 | **`NOT A RESULT`** — CLAUDE.md **rule 5 step 2**: the `rightWall` triple is **`EXACT`**, and an `EXACT` triple is `NOT A RESULT` whatever the value. **The cause is a MIS-SPECIFIED GATE QUANTITY, not a failed solve: there is nothing wrong with this solve.** All three levels: `rc = 0`, `End` present, last `Time` == `endTime 0.05`, age guard passed, planted-zero passed on both patches. **NOT SOFTENED.** | **cooled (right) wall = 378.0 K** and **adiabatic (left) wall = 412.9479167 K**, at L3. **rightWall triple: 378.0 / 378.0 / 378.0 — state `EXACT`, observed order undefined, GCI undefined.** It lands on **378.0000 K against a reference of 378.0 K — a deviation of exactly zero — and that is precisely why the row cannot be a credential.** **leftWall triple: 412.7916667 / 412.8958333 / 412.9479167, `CONVERGING`, p = 1.00, GCI_fine 0.01576 %**; its 412.9479 K against 413.0 K is 0.0126 %, inside the frozen 1 % band — **but it does not rescue the row, because the pre-registration requires BOTH walls.** **A hypothesis formed and KILLED before it was written down:** three identical values across three refinements is the signature of *grading your own boundary condition* — a gate quantity that is an input rather than an output, which would have been a serious self-blind-gate finding. **It is false.** The supervisor read the field: `rightWall` is a **`codedMixed` Robin condition**, not a `fixedValue` — it sets `refValue = 303 K`, `refGrad = 0` and `valueFraction = h/(h + DT*deltaCoeffs)` with h = 1000 W/m2K, looking `DT` up from the patch so it stays correct if the conductivity is edited. **The wall temperature is genuinely solved for, not imposed.** | **378 K (cooled wall) and 413 K (adiabatic wall)** — F.P. Incropera, D.P. Dewitt, *Fundamentals of Heat and Mass Transfer*, 5th ed., p. 117, 2006; manual p. 186, Table .59.1. Reference kind **closed-form analytic → category V**. Ansys Fluent 378.14 K and 413.17 K are **context only**. | **1 % relative, BOTH walls** (frozen prereg blob `f853d8cad11b3347d409b4d54760cf6e31263983`). Both walls are inside the band on value; the row still fails on rule 5. | `verification/runs/ansys_verification/VMFL059/` (levels `L1`, `L2`, `L3`; `RUN_RC.txt` rc=0 each; `COST.txt`) | **`f853d8cad11b3347d409b4d54760cf6e31263983`** (verified equal to `HEAD:…/PREREGISTRATION.md`) | **`f700c48d56735ec244d75b7bec19feac01bb2240`** (`grade_vmfl059.py`) | **0.4167 core-min** (measured from `COST.txt`, 25 wall-s at ranks=1; **2.78 % of the frozen 15 core-min cap**) | **$0.000356 DERIVED** at $0.0513/core-h (owner-stated; **not measured**) | `cases/ansys_verification/VMFL059/SUPERVISOR_RULING_VERDICT.md` | **TIER: `NOT HELD`** — the row produced no gradeable result on its own gate. |
| **17** | **VMFL022** — Cavitation Over a Sharp-Edged Orifice, Case B: Low Inlet Pressure (VM2026R1, p. 87). Nurick sharp-edged orifice, weak-cavitation operating point, axisymmetric 5° wedge, OpenFOAM v2606 **`interPhaseChangeFoam`** with the **SchnerrSauer** phase-change model — **Ansys's Zwart-Gerber-Belamri is absent from OpenFOAM v2606, so SchnerrSauer was substituted and the substitution was priced into the gate BEFORE the freeze, not discovered after.** **THE COLLISION IS DISCLOSED, NOT HIDDEN:** a second lane independently built and ran these same two cases because **the supervisor wrongly inferred from an absent process that the incumbent lane was dead and dispatched a rival** — the supervisor's error, ruled at `docs/ansys_verification/VMFL021_022_COLLISION_RULING.md`. This row grades **only** the committed build `605d5593`, whose freeze the supervisor verified preceded its compute by 60 s (freeze 21:31:35Z, first run artifact 21:32:35Z) and whose on-disk blobs still hash equal to HEAD. | 2026-08-25 | **`NOT A RESULT`** | **L3 Cd = 0.7591820** (L1 0.7444189, L2 0.7609118). **Roache triple `OSCILLATORY`: d21 = +0.0017297, d32 = −0.0164929, R = −0.104878**; `p` and `GCI_fine` both reported **`None`** — the comparator refused to quote a GCI on a non-monotone triple, as rule 5 requires. **L3 is 2.669 % from the reference and INSIDE the frozen 5 % band, and STILL CANNOT CERTIFY** — rule 5's gate turns a PASS into `NOT A RESULT`, never the reverse. **ROOT CAUSE, and it is a finding about the METHOD rather than a run defect: cavitation onset is MESH-DEPENDENT. L1 never reached vapour pressure and stayed SINGLE-PHASE (Min alpha = 1) while L2 and L3 cavitated, so the three levels are NOT SOLVING THE SAME PROBLEM.** A Roache triple spanning a physical regime change is not a grid-convergence study: Richardson extrapolation assumes one smooth error expansion in h, and a regime change is a discontinuity in that expansion, not a term in it. Repaired by starting the family ABOVE the onset threshold and registering a regime check as a PRECONDITION of the triple — not by refining further. | **0.780**, W.H. Nurick, *Orifice Cavitation and Its Effects on Spray Mixing*, Journal of Fluids Engineering **98**, 681–687, 1976 (manual p. 87). **Reference kind: measured / experimental → CAN buy P.** Consistency check PASSED before the freeze: Nurick's own Cd = Cc·sqrt(K) with Cc = 0.62 and K = 1.59006 gives **0.7818 against the target 0.780, 0.23 %** — unlike VMFL036, this target is internally consistent. | **5 %** relative at the finest level, frozen before compute | `verification/runs/ansys_verification/VMFL022/GRADING_VMFL022.json` | `bdd565b0c909` | `1c750397a84b` | **8.43 core-min MEASURED** | **$0.007208 DERIVED, NOT MEASURED** — $0.0513/core-h, c7a.4xlarge, **reported-by-owner**; the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` §5) | `verification/runs/ansys_verification/VMFL022/LANE_REPORT_opus48_collision.md` (commit `2e83a89b`) |
| **18** | **VMFL021** attempt 1 — Cavitation Over a Sharp-Edged Orifice, Case A: High Inlet Pressure (VM2026R1, p. 85). Same frozen build as row #17 (`605d5593`), deep-cavitation operating point. | 2026-08-25 | **`NOT A RESULT`** | **none — the run cannot be certified, and the physics was NOT the problem.** The solve was going right: strongly cavitating (Min alpha 0.064 → 0.0022), Cd converging 0.663 → 0.642 toward Cc = 0.620, and **stable at P1 = 2.5e8 Pa**. It fails on TWO INDEPENDENT INSTRUMENT DEFECTS, both confirmed by the supervisor from disk. **(i) `controlDict` carried `writeInterval 0.01` against `endTime 0.003` under `adjustable` write control, so NO `endTime` FIELD DIRECTORY WAS EVER WRITTEN at any level** — L1 (`rc=0`, 23 wall-s) and L2 (`rc=0`, 169 wall-s) hold only a `0` time directory, so rule 4's strict completion cannot certify them however good the numbers are. **(ii) L3 has NO `RUN_RC.txt` at all** — it was killed mid-run when **the supervisor wrongly inferred an absent process meant a dead lane and dispatched a rival onto the same case**; that is the supervisor's error, ruled at `docs/ansys_verification/VMFL021_022_COLLISION_RULING.md`, and it is disclosed here rather than absorbed. **INSTRUMENT GAP THIS EXPOSES, not covered by `PREREG_TEMPLATE.md` Amendment 3's six required launcher artifacts: a launcher can pass every existing check and still write NO GRADEABLE OUTPUT, because nothing validates `writeInterval` against `endTime`.** A smoke test proves the launcher RUNS; it does not prove it WRITES A FIELD DIRECTORY AT `endTime`. Now a required check for this team. | **0.620**, W.H. Nurick, *Orifice Cavitation and Its Effects on Spray Mixing*, Journal of Fluids Engineering **98**, 681–687, 1976 (manual p. 85). **Measured/experimental → CAN buy P.** Consistency check PASSED before the freeze: Nurick's Cd = Cc·sqrt(K), Cc = 0.62, K = 1.00037 → **0.6201 against 0.620, 0.02 %**. **`P1 = 2.5e8 Pa` is LOAD-BEARING, NOT A TYPO** — only P1 >> P2 drives K → 1 so Cd → Cc; at 250 kPa the same law gives 0.782, i.e. Case B. | **5 %** relative at the finest level, frozen before compute | `verification/runs/ansys_verification/VMFL021/L{1,2,3}/` — L1/L2 `RUN_RC.txt` present with `rc=0`, `remaining_core_min` and `prereg_blob`; **L3 `RUN_RC.txt` ABSENT** | `46b52092b3d2` — **verified: the `prereg_blob` recorded in L1/L2's own `RUN_RC.txt` matches this HEAD blob EXACTLY, so the run was governed by the committed freeze** | `7e783fe4aea2` | **3.20 core-min MEASURED** (L1 0.3833 + L2 2.8167; L3 unrecorded) | **$0.002736 DERIVED, NOT MEASURED** — $0.0513/core-h, c7a.4xlarge, **reported-by-owner** (`COMPUTE_BUDGET_CHARTER.md` §5) | **Re-run registered as `VMFL021-R2`, a NEW row citing this one under `VERIFICATION_CHARTER.md` §6. This row is NOT removed, NOT edited and NOT re-labelled.** |
| **19** | **VMFL017** — Transonic Flow over an RAE 2822 Airfoil (VM2026R1, p. 69). Frozen at `d1de064b`, `RESULTS.md` at `f746233a`. | 2026-08-25 | **`PENDING`** | **none — no gradeable run was produced, and this is a queue state, NOT a softened `GATE FAIL`** (`CLAUDE.md` rule 1; `VERIFICATION_CHARTER.md` §9). The registered instrument **`rhoSimpleFoam` DIVERGES**: `FOAM FATAL ERROR: Negative initial temperature T0` in the thermo energy→temperature inversion **at shock formation**, ~150–540 iterations, at every attempt. Two principled stabilisation attempts — temperature limiting, and first-order upwind with tight relaxation — **both diverged**. **This is the PRINCIPAL RISK the pre-registration NAMED BEFORE COMPUTE**, not a surprise, and it is neither a solver-availability problem nor a mesh defect. **SUPERVISOR'S LADDER RULING: re-register as `VMFL017-R2` on `rhoCentralFoam`.** Grounds: this team has already carried a shock-capturing case on `rhoCentralFoam` successfully — **VMFL045, rows #5 and #7** — and a density-based shock-capturing scheme is the right instrument for a transonic case with an embedded shock, where a pressure-based steady solver inverting energy to temperature across a forming shock is not. **This `PENDING` stays exactly as it is: an honest record that the registered instrument did not converge.** | **Drag 0.0168, Lift 0.803**, P.H. Cook, M.A. McDonald, M.C.P. Firmin, *AEROFOIL RAE 2822 — Pressure Distributions, and Boundary Layer and Wake Measurements*, AGARD Advisory Report No. 138, 1979 (manual p. 69). **Measured/experimental → CAN buy P.** Consistency check PASSED: the manual's own inputs (p = 43765 Pa, T = 300 K, M = 0.73, air) give U = 253.47 m/s, rho = 0.5082 kg/m3 and **Re = 6.496e6 against the manual's stated 6.5e6 — 0.06 %.** **Note VMFL017 and VMFL041 are the SAME RAE 2822 case from the SAME source**, one gated on integrated coefficients and one on surface Cp: **a single converged solve can serve both.** | frozen before compute; not reached | `cases/ansys_verification/VMFL017/RESULTS.md` (commit `f746233a`) | `8a8db0eb0da1` | `cases/ansys_verification/VMFL017/grade_vmfl017.py` — `--selftest` green | **0 graded core-min** — scratch smoke tests only; the ~800 core-min family estimate stands **UNCONSUMED** | **$0.00 — nothing graded was run** | `cases/ansys_verification/VMFL017/RESULTS.md` |

~~*Credential count: 3 PASS of 10 run.*~~ **STRUCK, NOT REWRITTEN** — superseded by the append of rows #11 and #12 on 2026-08-25. ~~*Credential count: 3 PASS of 12 run.*~~ **STRUCK, NOT REWRITTEN** — superseded by the append of rows #13–#16 on 2026-08-25. ~~*Credential count: 5 PASS of 16 run.*~~ **STRUCK, NOT REWRITTEN** — it was stale in **both** numbers, and by more than the append it was written for: at the time it was struck the register already carried **29** rows and **6** `PASS` rows, not 16 and 5. **Re-derived by a lane from the rows in the HEAD blob, one row at a time, after row #30 was appended — 30 rows, and the four classes sum to 30:** **`PASS` 6** (rows #2, #3, #7, #13, #15, #28) · **`GATE REACHED` 5** (rows #20, #22, #23, #24, #30) · **`NOT A RESULT` 18** (rows #1, #4, #5, #6, #8, #9, #10, #11, #12, #14, #16, #17, #18, #21, #25, #26, #27, #29) · **`PENDING` 1** (row #19). **Credential count: 6 PASS of 30 run.** **Only `PASS` rows are credentials**, so the five `GATE REACHED` rows — row #30 (VMFL064-R2) among them — add **nothing** to the numerator; they are honest tiers against references that cannot buy P, and counting them would be exactly the inflation this register exists to prevent. **The two rows that moved the count since it was last written are #28 (VMFL004-R2, `PASS`) and #7 (VMFL045-R2, `PASS`, present all along and never counted).** The denominator moved by 14 while the numerator moved by 1, and the honest reading of that is the standing one: **the register was behind the solver, and a case that ran and produced nothing still counts as run.** **Two new credentials: row #13 (VMFL019) and row #15 (VMFL050), both `PASS` against category-V exact references inside bands frozen before compute.** Rows #14 (VMFL010) and #16 (VMFL059) add NO credentials — both are `NOT A RESULT` on rule 5, and **both are inside their frozen bands on value**, which is exactly why they are recorded unsoftened: a value-only reading would have banked two credentials this lab has not earned. **Rows #15 and #16 were graded and ruled BEFORE this append and were simply never entered** — the denominator moved by 4 while the run count moved by 4, and the honest reading of that is that the register, not the solver, was behind. Rows #11 and #12 add NO credentials: both are `NOT A RESULT` on ladder incompleteness and neither produced a lab value. The count moved only in its denominator, which is the honest direction — a case that ran and produced nothing still counts as run.

---

## Dated correction — 2026-08-25 — the `D510` docket citation in row #3 is wrong; the open question is `D512`

**Append-only correction under CLAUDE.md rule 6 and this register's own reading rule
(a row is never edited after it lands). Row #3 above is NOT edited; this note corrects
a citation it carries.**

Row #3 (VMFL005), in its "Lab value" cell, states: *"…the open question is docket
`D510`."* **`D510` is wrong.** `docs/DOCKET.md` `D510` is the **closure team's R3
SpaRTA ratification**, an unrelated item (a docket-id collision — the id was written
into prose before it was appended, the failure `L-292` names). The VMFL005
open-mechanism question — the ≈ 90 % non-discretisation deviation from Hagen–Poiseuille,
the `CONVERGING` triple that extrapolates *away* from the exact value (`N-AV7`), with
`RESULTS.md` §5.3's candidates and the `N-AV9` wedge-geometry arithmetic attached — was
opened under a genuinely free id, **`D512`**, at commit `20afae1b` (2026-08-25),
`docs/DOCKET.md` `D512`.

- Struck, verbatim: ~~"the open question is docket `D510`"~~
- Corrected: the open question is docket **`D512`**.

Read `D512`, not `D510`, wherever row #3 says `D510`. The same correction is recorded at
the foot of `cases/ansys_verification/VMFL005/RESULTS.md`.

**Lines whose number changed above this section: 0.**

---

## Dated note — 2026-08-25 — row #4 landed; what this append moved, and what it did not

**Recorded because the section immediately above asserts `Lines whose number changed
above this section: 0`, and this append shifted that section down.** That assertion was
true **of its own landing** and is not retracted; this note states what the *next*
append did, so a reader diffing the file is never left to infer it.

**Row #4 (VMFL051) was appended to the table**, between row #3 and the credential-count
line. **No existing row was edited** — the register's append-only reading rule holds,
and rows #1, #2 and #3 are byte-identical to their committed form.

**One derived line was updated**, and it is a tally rather than a row: the
credential-count line moved from *"2 PASS of 3 run"* to *"2 PASS of 4 run"*. **The
credential count itself did NOT change** — row #4's verdict is `NOT A RESULT`, and only
`PASS` rows are credentials. Leaving the denominator at 3 would have been false.

**Lines whose number changed above the table: 0.** The header, the reading rule and the
table's own header and separator are untouched. The `D510`→`D512` correction section
above shifted down by **two lines** and its text is unaltered.

**Row #4 cites no `N-AV` and no docket id**, deliberately. None has been appended for
VMFL051, and an id written into prose before its append is a prediction and not an
identifier (`L-292`) — the defect this register already carries once and corrects above.
The manual defect and the instrument defect that VMFL051 found are recorded in
`cases/ansys_verification/VMFL051/RESULTS.md` §5 and §7.1 and are **referred to the
supervisor** for an `N-AV` / `LESSONS` append under a genuinely free id; this lane
appended neither and cited neither.

**Lines whose number changed above this section: 1** — the credential-count tally
described above, and nothing else.

---

## Dated correction — 2026-08-25 — row #4's `C-50` calibration citation is wrong; VMFL051's calibration row is `C-51`

**Append-only correction under `CLAUDE.md` rule 6 and this register's own reading rule
(a row is never edited after it lands). Row #4 above is NOT edited; this note corrects
a citation it carries.** This is the same treatment row #3's `D510` mis-citation got.

Row #4 (VMFL051), in its "Cost, core-min (measured)" cell, states: *"Calibration row
`C-50`"*. **`C-50` is wrong.** `docs/COST_CALIBRATION.md` `C-50` is the **cfd team's
F12 rung 1** — RAE 2822, AGARD AR-138 Case 9, the coarse workshop rung — an unrelated
item belonging to another team.

- Struck, verbatim: ~~"Calibration row `C-50`"~~
- Corrected: VMFL051's calibration row is **`C-51`**, landed at commit `0c3f3054`.

**THE CAUSE, STATED PLAINLY AND NOT SOFTENED — and the two timestamps are the proof.**
cfd's `C-50` landed at commit `cd1ac21a`, **2026-08-25T01:09:21Z**. Register row #4
landed at **2026-08-25T01:14:13Z** — **four minutes and fifty-two seconds later**. The
id was therefore **already taken and already visible at HEAD** when row #4 was written.
Nothing raced: **the id was derived BEFORE the commit and was not re-derived inside the
committing shell invocation**, which `CLAUDE.md` rule 11 requires in terms — *"Peers
commit constantly: re-derive at commit time, in the same shell invocation."*

**A contributing factor, recorded as a factor and NOT as an excuse:** the worktree copy
of `docs/COST_CALIBRATION.md` is **~19.8 kB shorter than the HEAD blob** (168 635 B
against 188 448 B). An id derived from that copy is derived from a truncated file and
is **wrong before any race begins** — it races nothing and is still incorrect.

**THIS IS THE TEAM'S SECOND INSTANCE OF `L-292`** — *an id in prose before its append is
a prediction, not an identifier* — **and row #4 itself cites `L-292` while committing
it.** The first instance is row #3's `D510`, corrected in the note above only hours
earlier. That the defect recurred inside the very row that named it is the part worth
keeping: **naming a failure mode in prose does not implement it; only the committing
invocation does.**

**`C-51` was landed with its id re-derived from the HEAD blob's own tail inside the
shell invocation that wrote its tree**, with a re-derivation on any CAS retry, and its
building script now refuses to commit at all unless the tree actually changed.

**Every other identifier cited by row #4 was re-checked against HEAD at the time of
this correction and every one exists:** `L-292`, `L-300`, `N-AV7`, `N-AV8`, `N-AV9` and
`D512`. Row #4 deliberately cites **no** `N-AV` and **no** docket id of its own, because
none has been appended for VMFL051. **`C-50` was the single failing citation and it is
corrected here.**

**Lines whose number changed above this section: 0** — verified by hashing, not
asserted: the file's entire content up to and including the previous correction section
is byte-identical to its committed form, and this note is appended below it.

---

## Dated addendum — 2026-08-25 — TIER back-filled onto rows #1–#4, on Sanaa's directive

**Append-only addendum. Rows #1–#4 above are NOT edited** — the same treatment the
`D510` and `C-50` corrections got. This addendum adds a fact the rows were written
before the directive existed; it corrects nothing in them and moves no number.

**Sanaa's directive, 2026-08-25, quoted byte-exact with typos preserved and NOT
normalised:**

> *"i just meant for now cfd, ansys verification and heat transfer teams work on
> completeing all the tasks/ running all the cases and recording per our conventions,
> and record whether the case is hold, gate reached or surveyed or not held. Once that
> is done we will go back to the Matrix config. But for now these three teams work on
> that"*

**EVERY FUTURE ROW CARRIES ITS TIER IN THE ROW ITSELF, WRITTEN AT GRADING TIME.** This
addendum exists only because rows #1–#4 predate the directive. It is a back-fill and
must not become a pattern: a tier appended afterwards is weaker evidence than one
written when the number was first graded, for exactly the reason a pre-registration is
frozen before a run.

**The tier and the rule-1 verdict are DIFFERENT VOCABULARIES and are recorded side by
side. The tier NEVER flatters the verdict.** They overlap at one word only,
`GATE REACHED`. The verdict vocabulary — `PASS` / `GATE REACHED` / `GATE FAIL` /
`NOT A RESULT` / `BLOCKED` / `PENDING` — is unchanged and is not replaced by this
column. Tier values are `HOLDS` / `GATE REACHED` / `SURVEYED` / `NOT HELD` /
`NEVER RUN`.

**All four tiers below are the `ansys-verification-supervisor`'s rulings, recorded as
such and overrulable. They are not this lane's calls.**

| row | case | verdict (rule 1, unchanged) | **tier** | the reason, as ruled |
|---|---|---|---|---|
| **#1** | VMFL001 run 1 | `NOT A RESULT` | **`NOT HELD`** | The comparator refused (exit 2) on the v2606 sampled-file naming and never produced a number, and independently L3 failed the registered iterative-convergence clause. **Nothing was measured**, so there is nothing to hold |
| **#2** | VMFL001-R2 | `PASS` | **`HOLDS`** — **a CANDIDATE, stated plainly as one and NOT as settled** | V and G are both present and strong: triple `CONVERGING`, observed order **2.0102**, GCI_fine **0.0563 %**, Richardson extrapolate landing on the analytic value to **3.7 ppm**. **It is a candidate rather than a hold because the P limb is UNRESOLVED**, and neither this register nor this addendum resolves it. Calling it a settled hold on the strength of V and G alone is exactly the flattery this column is built to prevent |
| **#3** | VMFL005 | `PASS` | **`GATE REACHED`** | The gate is genuinely met (0.4979 % inside a frozen 2 %) and the triple is genuinely `CONVERGING` (observed order 1.9341, GCI_fine 0.0502 %). **P is the missing limb, and it is named rather than left implicit:** the deviation from the analytic reference is **9.92× the fine-grid GCI**, and Richardson extrapolation moves the answer **FURTHER from** the exact value — so roughly **90 % of the residual deviation is a modelling/setup signature, not discretisation error** (`N-AV7`). The mechanism is **unresolved and not claimed**, open at docket **`D512`** |
| **#4** | VMFL051 | `NOT A RESULT` | **`NOT HELD`** | **V present, G ABSENT, and G decides it.** The reference is the exact Prandtl-Meyer solution derived here to full double precision — **public classical gas dynamics** (Anderson; NACA 1135), **not vendor documentation** — so V is not the problem. But the triple is `OSCILLATORY` at **R = −1.348600** with **no observed order** and **no quotable GCI**, and **two of three levels fail the frozen plateau clause**. P is open. **The softer `GATE REACHED` was refused deliberately:** that word fits a case that produced a **believable measurement** and lacks one column, as row #3 does. **VMFL051 produced no usable measurement at all — its G column is not missing, it is actively negative.** Precedent is this team's own row #1 |

**The credential count is unchanged: `2 PASS of 4 run`.** A tier is not a credential and
does not create one. Only `PASS` rows are credentials, and no row's verdict moved.

**The suite-wide picture, derivable rather than recalled**, lives in
`docs/ansys_verification/CASE_MAP.md`, where all **95** case rows now carry this same
tier column and the counts are printed with the grep rule that derives each:
**3 of 95 run, 92 never run**; **12** cases have no lab solver on this box and **10**
are the `VMFLGPU` family, with **zero overlap**, giving **73 runnable distinct-physics
cases — 3 run, 70 never run**. **Whether the GPU family and the 12 count toward
"completed" is scope, and scope is Sanaa's**; that document prints both readings and
chooses neither.

**Lines whose number changed above this section: 0** — verified by hashing the prior
file as an exact prefix of this one, not merely asserted.

---

## Dated note — 2026-08-25T02:44:28Z — row #6 landed, and VMFL005's pre-registration blob has ADVANCED

**Row #6 (VMFL003) is `NOT A RESULT`, so the credential count does NOT change.** The
count line moved from *"2 PASS of 5 run"* to **"2 PASS of 6 run"** — the **denominator**
only. Only `PASS` rows are credentials; rows #2 and #3 remain the two.

**VMFL005's pre-registration blob has advanced, and BOTH shas are quoted rather than one
replaced.** Row **#3** cites `cases/ansys_verification/VMFL005/PREREGISTRATION.md` as the
freeze proof of a `PASS` credential.

| | blob |
|---|---|
| at the freeze, as row #3 cites it | **`43aaf6bf5c5f1189495e1460e5de415e56860447`** |
| after **AMENDMENT 1** (v1.1), 2026-08-25T02:44:28Z | **`7a7c4435f372a636f3a982a3cfaa121746a577a5`** |

**Why it moved.** §9's three per-cell-iteration rates were printed with exponents **10³
too small** (`6.51e-10`/`1.06e-9`/`2.12e-9` against the correct
`6.510416666666667e-07`/`1.0579427083333333e-06`/`2.1158854166666665e-06`). **Every
product, the 217 s total, the 4.9 core-min estimate, the 13 core-min cap and both dollar
figures are UNAFFECTED** — the arithmetic used the correct rates; only the printed labels
were wrong. Repaired by a **dated foot amendment**, the frozen text **not** edited in
place, with lines 1–330 proven byte-identical by prefix sha256
`2d18e67d01f0f9bfbcd26bcc258f63d995e25cb5b9773e7f15c02d8b16fdb999`.

**NO GATE, THRESHOLD, BAND, CAP, LABEL, VERDICT OR CREDENTIAL MOVES, and VMFL005's
`PASS` stands.** **Row #3 itself is append-only and is NOT edited** — this note is the
record that its cited blob now resolves to a superseded version, and the amendment names
both. Recorded as **`L-317`** in `docs/LESSONS.md` — a cost table's RATE cell is never
checked by anything, because only the PRODUCT feeds the cap.

**Lines whose number changed above this section:** row #6 was inserted into the main
table at the established insertion point (immediately before the credential-count line),
exactly as row #5 was, so lines below that point shifted by **1**. This is the register's
own append convention for the main table and is stated rather than glossed.

## Dated note — 2026-08-25 — row #7 contains a LITERAL PIPE and breaks pipe-splitting readers; the row is NOT edited

**Found while building `verification/runs/ansys_verification/check_case_map_glance.py`, not by
reading.** Row **#7** (`VMFL045-R2`) quotes the repaired `fvSolution` key — the regex
`"(h|e)"` — inside its description. **That is a LITERAL `|` inside a Markdown table cell.** It
shifts every field after it, so **any reader that splits the row on `|` mis-parses row #7**, and
a renderer may show an extra column. Measured consequence: a naive extractor read row #7's
verdict as `NOT A RESULT` instead of **`PASS`**, which would under-count the credentials.

**THE ROW IS NOT EDITED.** It is append-only, it landed correctly, and its *content* is right —
the verdict is **`PASS`**, the tier **`GATE REACHED`**, and the credential tally **3 PASS of 7
run** is correct as written. The defect is *encoding*, not fact.

**What a reader must do instead:** anchor on the **ISO date cell** and take the next cell as the
verdict. The date moves with the shift; a fixed field index does not. `check_case_map_glance.py`
does exactly this and carries row #7 as its regression case.

**A second trap in the same file, recorded together:** several rows mention a verdict *in prose*
before their own verdict cell — row #2 reads *"Re-run of row #1 after that row's `NOT A
RESULT`"* before its `PASS`. **So "the first backticked verdict in the row" is also wrong.** Both
traps are now selftest fixtures.

**For future rows:** escape a literal pipe as `\|` inside a table cell, or name the key without
the pipe. This note does not require any existing row to change.

---

## Dated note — 2026-08-25T17:40:54Z — row #8 landed (VMFL007 run 1), and what this append moved

**Row #8 is `NOT A RESULT`, so the credential count does NOT change.** The count
line moved from *"3 PASS of 7 run"* to **"3 PASS of 8 run"** — **the denominator
only.** Only `PASS` rows are credentials; rows #2, #3 and #7 remain the three.

**Rows #1–#7 are NOT edited.** Row #8 was inserted at this table's established
insertion point, immediately before the credential-count line, exactly as rows #5
through #7 were, so lines below that point shifted by **2** (the row and its
blank separator). That is this register's own append convention for the main
table and is stated rather than glossed.

**The tier is IN the row, written at grading time**, per the 2026-08-25 tier
addendum's requirement that every future row carry its tier inline rather than by
back-fill.

**The three parents moved in one invocation** (register row, credential tally,
`docs/ansys_verification/CASE_MAP.md`), together with the calibration row
`C-72` in `docs/COST_CALIBRATION.md` and the record
`cases/ansys_verification/VMFL007/RESULTS.md`.

**Lines whose number changed above this section:** the main table gained 2 lines
at its insertion point; nothing above that point moved.

## Dated note — 2026-08-25T19:20:02Z — rows #9 and #10 landed (VMFL003-M2 arms A and B), and what this append moved

**Rows #9 (VMFL003-M2 arm A, `kEpsilon`) and #10 (arm B, `realizableKE`) are both `NOT A RESULT`, so the credential count does NOT change.** The count line moved from *"3 PASS of 8 run"* to **"3 PASS of 10 run"** — **the denominator only** (8 → 10, +2 for the two rows). Only `PASS` rows are credentials; rows #2, #3 and #7 remain the three.

**Lines shifted:** the two rows were inserted immediately before the credential-count line, in the established insertion zone. **Every line from the credential-count line to the end of the file shifted down by 4** — row #9, a blank, row #10, a blank. No existing row, dated note, gate, band, cap, label, verdict, tier or credential was edited; this is a pure append plus the denominator bump.

**Both verdicts are the frozen machinery's, not a choice.** The gate triple `L1_250x5`/`L2_500x5`/`L3_1000x5` was NAMED in the freeze (prereg §9, blob `cdbf2659`, freeze `c5fdcad4`), so no triple was selected after seeing numbers. Both arms failed rule 5 step 1 — the frozen iterative-convergence residual leg — before the gate could apply; the gate would independently have been `GATE FAIL` for both (arm A −4.34 %, arm B −6.74 % vs the manual's 21 744 Pa, both outside the frozen 2.5 % band).

**The slate's other two arms are not graded here and both are `NOT A RESULT` on incompleteness.** Arm C (`RNGkEpsilon`) was ruled `NOT A RESULT` on ladder incompleteness at `aba61e53`. Arm D (`kOmegaSST`) stopped at its frozen 40 core-min per-arm cap (`simpleFoam` reached `Time = 5949` of `endTime 22000` = 27.0 %, `ExecutionTime = 1662.74 s` under `timeout 1663`, no `End` line; `ExecutionTime`/`ClockTime` = 1662.74/1662 = 1.0004, so the spend was real CPU, not contention). The supervisor did that crash triage personally (SUPERVISION_CHARTER §3 check 2) and ruled arm D `NOT A RESULT` with **no fresh cap** (CLAUDE.md rule 12 — an overrun stops the run). It is recorded as a stop, never as `PENDING`. A register row for arms C and D is the supervisor's to append.

**Correction to a premise carried into this grading, recorded so it is not repeated:** the grading brief stated no `RC.txt`/`record.json` exists so the `rc = 0` clause could not be evaluated from disk. That is **not so for arms A and B** — every one of their twelve level directories carries a `RUN_RC.txt` reading `rc=0`, and the frozen comparator's completion clause C1 reads exactly that file and passed (exit 0) at all six levels of each arm. The `rc = 0` leg of rule 4 is therefore **satisfied, not unevaluable and not substituted**, for both rows above.

---

## Dated note — 2026-08-25T20:30Z — the cap regime under which rows #11 and #12 were stopped HAS SINCE BEEN CHANGED BY SANAA, and what that does and does not license

**Appended by `ansys-verification-supervisor` personally. This note adds a row to nothing
and alters no verdict.** Rows #11 and #12 stand exactly as written.

**What happened.** Arms C and D were stopped by a frozen `PER_ARM_CAP = 40` core-min acting
as a **budget gate**, under CLAUDE.md rule 12 as it stood at the run (*"an overrun stops the
run; it does not get a new budget"*). Neither arm crashed, diverged or hit a solver defect.
They were killed for being expensive.

**Sanaa's directive of 2026-08-25 lifts exactly that ground.** Her words, verbatim and
unnormalised:

> I want the three teams to forget about cost constraints for now… So no team stops anything
> in the name of saving compute. The lab having experience running hard and diverse cases
> primes over saving money. Besides, we have two instances running rn. So ample ressources.

**Caps are now RUNAWAY GUARDS, not budget gates.** A run crossing its cap is reported to the
supervisor, who extends it by dated amendment if the work is sound, or stops it if it is
genuinely stuck, diverging or looping. **Costing and calibration continue — she lifted
constraints, not measurement.**

### THE RULING, AND WHY IT DOES NOT RESURRECT ROWS #11 AND #12

**It is tempting, and it is wrong, to now extend arm C's and arm D's cap and let them
finish.** CLAUDE.md rule 2 is explicit: **after first compute, gates are closed, and changes
land only as dated addenda that CANNOT alter a gate, threshold, cap or label.** A cap is
named in that list. Arms C and D have both had first compute. **Their caps cannot be
amended, and no directive of Sanaa's that lifts a budget constraint converts into permission
to edit a frozen document after the fact.** The freeze is the entire evidentiary content of
a pre-registration: it proves the cap could not have been chosen to fit the answer. Loosening
it retroactively, in the direction that rescues two dead arms, is precisely the move the
freeze exists to prevent.

**Therefore:**

1. **Rows #11 and #12 remain `NOT A RESULT`, permanently.** They are not upgraded, not
   re-graded and not deleted. They are findings.
2. **The lawful route to the missing `RNGkEpsilon` and `kOmegaSST` numbers is a NEW
   pre-registration with a NEW case id and its own frozen cap**, citing rows #11 and #12 —
   the same route rows #2 (`VMFL001-R2`) and #7 (`VMFL045-R2`) already took. Its cap is
   sized from the measured diagnostic in those rows, not guessed: arm C ran **4.23×** the
   arm-A baseline on the shared `L2_500x5` mesh, and arm D's `L3` ran **7×** over its own
   mesh scaling.
3. **A re-run at a larger cap alone would be the weaker experiment.** Both rows record a
   badly-converging inner linear solve as the live hypothesis, **configuration-specific and
   not model-specific**, with contention explicitly falsified (`ExecutionTime` tracks
   `ClockTime` to within 0.3 % throughout). Simply buying more core-minutes would purchase
   the same bad convergence for longer. The registered variable should be the **linear
   solver / preconditioner**, which is what `VMFL007_R2` was built to test and what Sanaa's
   *"try different pre conditioners, see if that's a resolution"* anticipated.

**Recorded so the reasoning survives:** a directive that removes a constraint is not
retroactive permission to unfreeze what that constraint already decided.

---

## Dated correction — 2026-08-25T20:28Z — **the `RC.txt` claim carried by rows #11 and #12 is WRONG, and it was the supervisor's own**

**Append-only correction under CLAUDE.md rule 6 and this register's reading rule (a row is
never edited after it lands). Rows #11 and #12 are NOT edited. Their VERDICTS are unaffected —
both remain `NOT A RESULT`, and the correction makes the evidence for them STRONGER.**

### What the rows claim

Rows #11 and #12 each state, in their "Artifact path" cell, that **no `RC.txt` or
`record.json` exists anywhere under `VMFL003_M2`**, that the launcher's `rc=` printf runs
only on the completing path, and that **rule 4's `rc = 0` conjunct is therefore UNEVALUABLE
FROM DISK for the entire rung** — recorded there as an instrument finding of charter grade.

### That is false. Measured 2026-08-25T20:27Z.

**The file exists at every level of every arm. It is named `RUN_RC.txt`.** The supervisor
searched for `RC.txt` and `record.json`, did not search for `RUN_RC.txt`, and reported the
resulting absence as a finding. **A grep for the wrong filename is not an enumeration
instrument** — this is L-312 firing again, on the supervisor's own hand, one day after the
same class of error was recorded against a document grep.

**The register itself already contained the refutation.** Row #10, written by the grading
lane, cites *"six level dirs, `RUN_RC.txt` rc=0 each"* — in this same file, two rows above
the claim that no such file exists. The contradiction was committed unnoticed.

### The measured contents, and why they strengthen both verdicts

| level | `rc=` |
|---|---|
| `C_RNGkEpsilon/L1_250x5`, `L2_500x5`, `L3_1000x5` | **0** |
| **`C_RNGkEpsilon/D_500x3`** | **124** |
| `D_kOmegaSST/L1_250x5`, `L2_500x5` | **0** |
| **`D_kOmegaSST/L3_1000x5`** | **124** |

Each file also carries `arm=`, `level=` and `nx=`.

**`rc=124` is `timeout`'s exit code** — the budget-fired signature. It appears on **exactly
the two levels that stopped**, and nowhere else. So:

1. **Rule 4's `rc = 0` conjunct is FULLY EVALUABLE from disk for this rung**, and it is
   **met** at every completing level and **violated** at exactly the two stopped ones.
2. **Both `NOT A RESULT` rulings are now independently confirmed without reading a solver
   log at all** — the machine-readable rc alone distinguishes a budget stop from a crash.
3. **The claim of a charter-grade instrument gap is WITHDRAWN.** There is no gap here. The
   convention was already correct and better than the supervisor credited: the launcher
   writes its rc on the ABORTING path too, which is the property that made this evidence
   exist.

### What is corrected downstream

The same false claim was carried in this team's `docs/LAB_STATE.md` section as instrument
finding #1 and was briefed to the batch lane as a standing instruction. **The lane has been
corrected in flight** and instructed to match `RUN_RC.txt` rather than introduce a second
convention. **A second rc filename would have been the real instrument gap** — created by the
correction, not by the original.

**Recorded rather than quietly fixed:** the supervisor's own big-claim check
(`SUPERVISION_CHARTER.md` §3, item 3 — *assume it is wrong until it has been defended against
its own evidence*) was NOT applied to this claim before it was committed. It was applied
afterwards, by an independent disk census, and it caught it. The check works; it was run late.

| **20** | **VMFL036** — Laminar Flow Past Sphere (VM2026R1, pp. 125–126). Steady laminar flow past a sphere, reproduced in OpenFOAM v2606 `simpleFoam` on a **two-block axisymmetric polar wedge** (5° full angle, r from D/2 to the manual's 50 D, collapsed axis in an `empty` patch per the OpenFOAM convention). **TWO ARMS, both frozen before any compute**, on the supervisor's committed pre-freeze finding (`3fa6058d`) that **the manual's page is internally inconsistent**: its stated μ = 0.02 gives **Re = 50**, but its target Cd = 1.0895 is the **Re = 100** literature value. **ARM A (this row, the gate)** ran the repaired μ = 0.01, Re = 100. **ARM B (`NOT A RESULT` by registration, scores nothing)** ran the manual's stated μ = 0.02 exactly as printed and returned **Cd = 1.577375** — **2.55% from the Schiller–Naumann Re = 50 prediction of 1.5381 that was registered BEFORE the run** (band 10% → AGREES) and **44.78% from the manual's own target** (discriminator ≥ 25% → DISCRIMINATES). **Arm B is evidence about the MANUAL, not a gate on this solver: it establishes that μ = 0.02 on that page is a transcription error for μ = 0.01.** Had the gate been frozen the obvious way it would have been a **guaranteed `GATE FAIL` measuring a typo** — the VMFL059 class, caught BEFORE the freeze this time. Roache triple r = 2 **by construction** (a fixed K = 400 radial stretching map with the expansion ratio recomputed per level, so h halves exactly): 3 072 / 12 288 / 49 152 cells, Cd = 1.091486 / 1.089233 / **1.088834**, **CONVERGING**, **GCI_fine = 0.0099% at Fs = 1.25**, Richardson f_ex = 1.088748. **p_obs = 2.4963 is ABOVE the formal p_f = 2 and is reported as the WARNING pre-registration line 10 declared it in advance, not as a win.** **N-AV9 azimuthal bias CARRIED, not assumed away:** the predecessor lane's frozen Aref = D²sin(a)/4 was **wrong by 0.095%** — the frontal PROJECTED area carries an extra cos(a) that the WETTED area does not; corrected to D²sin(a)cos(a)/4 = 1.08944678435e-02, which matches the value read off the real mesh **to 1 part in 1e8**, and the residual pressure-vs-viscous bracket (**0.0952% of Cd**; Cd = 1.087798 on the other normalisation) is printed beside the verdict rather than dropped. Comparator **repaired under `VERIFICATION_CHARTER.md` §2d.1** (see `PREREG_ADDENDUM_02.md`): it could not parse its own launcher's `rc = 0` and **refused every level**, caught by the rule-4 guard **before** grading; **the defect emitted no number at all, so the repair could not have been selected to move a verdict.** | 2026-08-25 | **`GATE REACHED`** | **Cd = 1.088834** (finest level, L3 128×192 = 49 152 cells) | **1.0895** — Mittal, R. (1999) *A Fourier-Chebyshev spectral collocation method for simulating flow past spheres and spheroids*, IJNMF 30(7); Tabata, M. & Itakura, K. (1998) *A precise computation of drag coefficients of a sphere*, IJCFD 9(3–4). VM2026R1 Table .36.1, p. 126. **REFERENCE KIND: CODE-TO-CODE** — both are COMPUTED spectral solutions, not experiment, so this reference buys **NEITHER V NOR P** and the **TIER CEILING is `GATE REACHED`**, frozen as the ceiling on pre-registration lines 3–4 and hard-coded in the comparator. **NOT A CREDENTIAL — this row is not a `PASS` and must not be counted as one.** (Context only, never a gate: Ansys Fluent 1.0875.) | **\|Cd − 1.0895\|/1.0895 ≤ 0.03** at the finest level — frozen at `ff9e28da` from the manual's own 3% acceptance practice, the ~1.4% published reference spread at Re = 100, and target rounding; **never from a first run**. Achieved **0.0611%**, inside the band by a factor of 49. | `verification/runs/ansys_verification/VMFL036/A/L3_128x192/` (and `/A/L1_32x48/`, `/A/L2_64x96/`; Arm B under `/B/`) | `ff9e28da` | `8a1aea3bd6f0dcd1d720c54a92f76b780924d028` | **82.40 core-min measured** across BOTH arms (Arm A 41.43, Arm B 40.97), from each level's own `RUN_RC.txt`; `ranks = 1` so core-min = wall-min. Predicted 70.0 → **ratio 1.18x**. Cap 120 core-min **per arm**, separate budgets so the diagnostic arm could not starve the gate arm; no overrun, zero waste. | **$0.0705 DERIVED** at $0.0513/core-h (owner-stated, c7a.4xlarge) — **derived, not measured** | `cases/ansys_verification/VMFL036/RESULTS.md` |

---

### CORRECTION TO ROW #19, 2026-08-25 — the "single solve serves both" note is WITHDRAWN

**Row #19 (VMFL017, `PENDING`) is NOT edited and its verdict does NOT change.** This note
corrects one claim inside it.

**Struck:** ~~"Note VMFL017 and VMFL041 are the SAME RAE 2822 case from the SAME source, one
gated on integrated coefficients and one on surface Cp: **a single converged solve can serve
both**."~~

**Measured from the manual sidecar by the supervisor:** VMFL017 (p.69) runs at **AoA 2.79°**,
viscosity 1.983e-5, M = 0.73, Re = 6.5e6, static pressure 43765 Pa. VMFL041 (p.141) runs at
**AoA 3.19°**, viscosity **1.831e-5**, driven by an **inlet average velocity of 218 m/s**.
**Same airfoil and same experiment; different solves.** The pair is RAE 2822 Case 9's
**corrected-versus-geometric** angle of attack, and 0.4° on a transonic aerofoil moves the
shock.

**The error was the supervisor's**: a shared *citation* was read as a shared *configuration*.
**No economy exists — VMFL041 requires its own solve.** `VMFL017-R2` is registered for
VMFL017's conditions only, and the pre-registration records this so the false economy
propagates no further. Everything else in row #19 stands: the `PENDING`, the divergence
finding, the reference kind (measured/experimental → can buy P) and the Re consistency check
(6.496e6 against 6.5e6, 0.06 %).

| **21** | **VMFL033** — Viscous Heating in an Annulus (VM2026R1, p.119). Steady laminar flow in a 2-D annulus driven by the rotating OUTER wall, with VISCOUS HEATING, reproduced in OpenFOAM v2606 `buoyantSimpleFoam` with `rhoConst` (density EXACTLY the manual's 1 kg/m3, never a perfect-gas function of T) plus OpenFOAM's OWN SHIPPED `viscousDissipation` fvOption, which builds its source from the NUMERICALLY SOLVED U via grad(U) and never from the closed form — no lab-written solver and no coded source. Mesh: annular sector with ROTATIONAL CYCLIC sides, r = 2 RADIAL triple 256/512/1024 cells, theta held FIXED at 8 because the exact solution is theta-invariant. **VERDICT `NOT A RESULT`, in rule 5's own order: (1) two of three levels are NOT PLATEAUED (ptp/rise 6.5e-05 and 7.8e-03 against a frozen 1e-06 floor), which alone forces it whatever the values; (2) the Roache triple is `OSCILLATORY`, R = −398.5, so NO GCI IS QUOTED; (3) the band is only then consulted.** **THE PRE-REGISTRATION CALLED THIS FAILURE BY NAME BEFORE COMPUTE** — line 9 registered *"THE SETTLING CLAUSE REFUSES… PREDICTED OUTCOME IF IT BITES: NOT A RESULT on step (1) of rule 5"* — and it bit in the predicted mechanism at the predicted step; **a correctly predicted failure is still a failure and the verdict is unchanged by having been foreseen.** Diagnostically, and WITHOUT softening the verdict: L1 and L2 are settled to 1e-13/1e-10 and sit 6.06e-03 and 2.79e-03 of the 17.33 K viscous-heating rise from the exact curve, while L3's residual is 2.14e-08 and its volume-average sits 2.75 K above L2's — **the finest mesh ran out of iterations; the discretisation did not degrade.** That is a hint the case would meet its band on a longer run and **NOT evidence that it does**; `endTime` is fixed by the freeze, so a longer run is a NEW pre-registration, not an extension. The secondary declared risk did NOT bite: max\|v_r\| ≤ 5.3e-07 against a 1 m/s wall speed, so the rotational cyclic reproduces a purely tangential field to seven digits. Comparator REPAIRED under `VERIFICATION_CHARTER.md` §2d.1 before any level was graded (`PREREG_ADDENDUM_01.md`): its selftest printed `SELFTEST GREEN` while a control that must fail had failed — **cause was a MISSING completion control, narrower than the diagnosis, plus a substring-swallow hazard fixed structurally** — and the repair is verified by **8 mutants × 2 interpreters, every one rc=2 with no green line**. | 2026-08-25 | **`NOT A RESULT`** | T_avg(finest) = 286.895530 K; max\|T−T_ex\| = 2.824e-01 of the rise, max\|v−v_ex\| = 5.120e-02 of the wall speed (**values printed beside the verdict as rule 5 requires, and scoring nothing**) | **CLOSED FORM** derived from R.B. Bird, W.E. Stewart & E.N. Lightfoot, *Transport Phenomena* (Wiley, 1960) — the reference the manual's own p.119 names when it calls the curve *"the analytical solution provided by Bird et al (1960)"*. T(r) = −133.333333/r² − 142.826809 ln r + 406.333333; interior peak **290.331779 K at r = 1.366405, 16.33 K ABOVE the hotter wall**. Confirmed by an INDEPENDENT finite-difference BVP inside the comparator's selftest that never uses the closed form (error falls 4.00× per doubling). **REFERENCE KIND: closed-form/exact → buys V, NEVER P; TIER CEILING `GATE REACHED`, frozen as the ceiling on lines 3–4.** The manual prints NO numeric table for this case, only two figures, which were **NOT digitised**. | **G1 max\|v−v_ex\|/(Ω2r2) ≤ 0.01 AND G2 max\|T−T_ex\|/(T_peak−T1) ≤ 0.01** at the finest level, frozen at `9b0b573c` from the manual's own 3% practice tightened because the reference carries no uncertainty; normalised by the 17.33 K VISCOUS-HEATING RISE, never by the 1 K wall difference which would have flattered it. **Not reached — the verdict is NOT A RESULT on settling and triple, prior to the band.** | `verification/runs/ansys_verification/VMFL033/{L1_nr32,L2_nr64,L3_nr128}/` | `9b0b573c` | `0ac96be5abafb9257737672e10bc2c484381587b` | **2.0667 core-min measured** (0.4167 + 0.6333 + 1.0167) from each level's own `RUN_RC.txt`; `ranks = 1` so core-min = wall-min. Predicted 8.4 → **ratio 0.246×**. Caps PER LEVEL 5/8/15, none crossed. **WASTE 1.0334 core-min named separately** (abandoned attempt 1, preserved, never absorbed into the ratio). | **$0.00265 DERIVED** at $0.0513/core-h (owner-stated, c7a.4xlarge) — **derived, not measured** | `cases/ansys_verification/VMFL033/RESULTS.md` |
| **22** | **VMFL023** — Oscillating Laminar Flow Around a Circular Cylinder (VM2026R1, p. 89) | 2026-08-26 | **`GATE REACHED`** | **St = 0.165993** at L3 (384 × 128), Strouhal from lift-force zero-crossings, St = D/(T·U). Roache triple **CONVERGING** (d32 = 5.252e-03, d21 = 1.394e-03, R = 0.265359), observed order **p = 1.9140** against a formal 2, **GCI(Fs = 1.25) = 0.3791 %**, Richardson f_ex = 0.165490. Deviation from reference **0.6019 %**. Stationarity: first-half ptp 6.62990e-01 vs second-half 6.63099e-01 (**0.02 %** against a frozen 5 %), max Courant **0.5239** < 1 | **St = 0.165** — experimental St–Re correlation, F. M. White *Viscous Fluid Flow* (1994) / Kim & Lee (2002), as printed in the manual, **manual p. 89** | **3 % relative at the finest level**, frozen in `PREREGISTRATION.md` before compute | `verification/runs/ansys_verification/VMFL023/` | **`131469a793b2dd9cb2e198947cebc359ea9f22a0`** | **`267d2b1091efea91b77b4cba9f97126d51504b22`** (blob-verified identical to the on-disk file that graded) | **228.667** (L1 8.117 + L2 32.417 + L3 188.133; ranks = 1 serial; est 210, cap 350) | **$0.195** (derived at $0.0513/core-h, not measured) | `cases/ansys_verification/VMFL023/RESULTS.md` |
| **23** | **VMFL021-R2** — Cavitation Over a Sharp-Edged Orifice, case A (VM2026R1, p. 85) | 2026-08-26 | **`GATE REACHED`** | **Cd = 0.634868** at L3, Cd = \|Q_inlet\|/(A2·V_theo) read at the pure-liquid inlet. Roache triple **CONVERGING** (d32 = 0.02195, d21 = 0.006570, R = 0.29926), observed order **p = 1.7405** against a formal 1, **GCI(Fs = 1.25) = 0.5524 %**, Cd_ex = 0.63206. Deviation from reference **2.398 %**. All three levels **PLATEAU** (ptp/range 0.104 / 2.434 / 0.002 %) and all three **CAVITATING** (alpha_min 0.0638 / 0.0031 / 0.0001, regime precondition met, so the gate is evaluated on the physics it was registered for) | **Cd = 0.620** — Nurick (1976), experimental, **manual p. 85** | **5 % relative at the finest converged level**, frozen in `R2/PREREGISTRATION.md` before compute | `verification/runs/ansys_verification/VMFL021/R2/` | **`610c42c41bce3865b4c07b62e48ed625f15c96ab`** | **`87ce3fa6a8181aae3071691694db26e89804f00b`** (blob-verified identical to the on-disk file that graded) | **32.933** (L1 0.550 + L2 3.433 + L3 28.950; ranks = 1 serial; est ~40, cap 180) | **$0.028** (derived at $0.0513/core-h, not measured) | `cases/ansys_verification/VMFL021/R2/RESULTS.md` |
| **24** | **VMFL002** — Laminar Flow Through a Pipe with Uniform Heat Flux (VM2026R1, p. 17). Laminar mercury flow in an axisymmetric 5° wedge pipe under uniform wall heat flux, reproduced in OpenFOAM v2606 `simpleFoam` + `scalarTransport(T)` (D = α = k/(ρcp) = 4.5315176e-06 m²/s), wall `fixedGradient` q″/k = 585.480 K/m. **The inlet velocity is NOT printed in the manual; it is archive-sourced SETUP data (supervisor Ruling 1), disclosed in the freeze** — `VMFL002_WB.wbpz → …/Fluent/VMFL002_laminar-pipe-hotflow.set.prof`, 20 radial points, least-squares fit umax = 1.0231091246e-02 m/s. The gate is the manual's PRINTED Target, never the archive. Independent of every prior row and re-grades none. | 2026-08-26 | **`GATE REACHED`** — dP and the outlet-T rise both inside 2 % at L3, both triples CONVERGING | dP = **1.000 Pa** and centreline outlet **T = 341.00 K** (manual p.17 Table .02.1 "Target"; White 1994 / Incropera 1981). Context only, never the gate: Fluent 0.999 Pa / 340.50 K, CFX 1.019 Pa / 340.8 K | **2 % relative on dP and on the 41 K rise, at L3** (frozen `PREREGISTRATION.md` §6; summed error budget ~0.5 %, band 4×) | `verification/runs/ansys_verification/VMFL002/` (`GRADING_VMFL002.json`) | **`bda4c9a083184335afa2b921fa6311cdf5d8ff4b`** | **`027bcbf72eba2177f8b76eec0136f13b6bb75db8`** | **5.2** (312 wall s, serial; cap 40; 0.46× the 11.4 predicted) | **$0.0044** derived, $0.0513/core-h reported-by-owner | `cases/ansys_verification/VMFL002/RESULTS.md` |
| **25** | **VMFL004** — Plain Couette Flow with Pressure Gradient (VM2026R1, p. 21). Plane Couette–Poiseuille flow (top wall U = 3 m/s, dp/dx = −12 Pa/m, ρ = 1, μ = 1), reproduced in OpenFOAM v2606 `simpleFoam` with the pressure gradient as a `vectorSemiImplicitSource` body force (+12 m/s²) and streamwise cyclic. **The manual prints Figure .04.2 only (no target table); the gate is the lab-evaluated EXACT section mean ⟨u⟩ = ∫₀¹(9y−6y²)dy = 2.5 m/s**, so the ceiling is PASS/HOLDS (supervisor Ruling 2; VMFL019 precedent verified). Gate on `volAverage(U)_x` to dodge the EXACT-collapse (central differencing is exact for the quadratic nodal field). **0/ patch names reconciled to the mesh before the run** (dead-lane fields used inlet/outlet/topWall/bottomWall, absent from the mesh). Independent of every prior row. | 2026-08-26 | **`NOT A RESULT`** — a FROZEN-INSTRUMENT block, NOT a physics failure: the volAverage gate is a textbook PASS (2.50125 / 2.5003125 / 2.5000781, triple CONVERGING p = 1.99999997, extrapolated 2.4999999993, rel_dev 3.1e-5 ≪ band, planted-zero fired, EXACT-collapse did not fire), but the inherited iterative-convergence check requires Uy_initial/p_initial < 1e-7, which in this 1-D fully-developed flow are normalization NOISE (Ux = 3.2e-13 converged; Uy/p bounce at 0.05/0.09). Per rule 2 the frozen comparator is not edited; a corrected re-run gating on the driven Ux is a NEW row (charter §6). | exact section mean **⟨u⟩ = 2.5 m/s** (closed form u = 9y−6y², Munson/Okiishi/Huebsch 5e; manual p.21 Reference, figure-only) | **0.1 % relative on `volAverage(U)_x` at L3** (frozen `PREREGISTRATION.md` §5) | `verification/runs/ansys_verification/VMFL004/` (`GRADING_VMFL004.json`) | **`0e61889534d0e7180a105190f7ead06e3c00b432`** | **`ddea9d473b6d6092427d29c5997c25af956a7fb6`** | **11.4** (685 wall s, serial; cap 30; no overrun) | **$0.0097** derived, $0.0513/core-h reported-by-owner | `cases/ansys_verification/VMFL004/RESULTS.md` |
| **26** | **VMFL011** — Laminar Flow in a Triangular Cavity (VM2026R1, p. 41). Top-wall-driven triangular cavity, Re = 400 (ρ=1, μ=0.01, base 2 m, height 4 m, top wall U=2 m/s), reproduced in OpenFOAM v2606 `simpleFoam` on a collapsed-hex triangular block (apex vertices genuinely merged — a prior dead lane's separate-coincident-vertices version SIGFPE'd on zero-area faces; the current mesh is the fix). Gate `rms_vs_benchmark ≤ 3 %` against the digitised Jyotsna-Vanka 1995 numerical benchmark; Roache triple on `u_min` (self-converging functional) to dodge the digitisation noise floor. Launched with the setsid detachment fix (verified live). Independent of every prior row. | 2026-08-26 | **`NOT A RESULT`** — the frozen comparator **REFUSED (exit 2)** on its planted-zero control for `rms_vs_benchmark`: a 1.234e-3 plant into one of 401 bisector points moved the RMS by only 3.68e-7 (< the 0.1·plant threshold). **Diagnosis: a mis-calibrated control, not a blind reader** — the reader is responsive (Δ≠0) but an RMS over 401 points dilutes a single-point plant by ~1/√N, so the `0.1·plant` threshold (right for the point-reader `u_min`) is unreachable for an averaging reader. Per rule 2 the frozen comparator is not edited; a corrected re-run (plant sized to the averaging reader) is a NEW row. **Physics beside it:** rms 0.0403/0.0348/**0.0341** all exceed the 3 % band (so even absent the refusal, L3 → GATE FAIL), and the `u_min` triple is CONVERGING (p=1.60, extrapolated −0.3465) but sits **8.9 % from the digitised benchmark −0.318** — a real discrepancy, OPEN. | normalized x-velocity on the base-bisector, `u_min/U_wall = −0.318062` (digitised from **Figure .11.2**; Jyotsna & Vanka, J. Comp. Phys. 122, 107-117, 1995). Figure-only, no manual target table. Context: Fluent/CFX curves overlaid, figure-only. | **3 % on `rms_vs_benchmark` at L3**, with the `u_min` triple required CONVERGING (frozen `PREREGISTRATION.md` §5) | `verification/runs/ansys_verification/VMFL011/` | **`4bd8c4285e379e93e1ad4e6c2b9967604d042523`** | **`e369496bf2e28ccb7145756e1c2442eb11e8e3f7`** | **8.2** (492 wall s, serial; cap 50; ran uncontended, ET/CT≈1.00) | **$0.0070** derived, $0.0513/core-h reported-by-owner | `cases/ansys_verification/VMFL011/RESULTS.md` |
| **27** | **VMFL076** — Laminar Flow Over a Flat Plate with Low-Prandtl Heat Transfer (VM2026R1, printed p. 219 = PDF p. 233, offset 14). Mercury, Pr = 0.003, reproduced in OpenFOAM v2606 `simpleFoam` + scalar energy transport against a **similarity solution the lab DERIVED AND EVALUATED ITSELF**, not a digitised figure. | 2026-08-26 | **`NOT A RESULT`** | **BOTH GATES WERE MET AND RULE 5 TOOK THE ROW ANYWAY.** Gate A `|I_lab-I_ref|/I_ref` = **0.900557 %** against a frozen **3.00 %**; Gate B `max|dTheta|` = **5.396919e-03** against a frozen **1.00e-02**. **Rule 5 step 2: the grid triple is `OSCILLATORY`** — e21 = **-5.007e-05**, e32 = **+3.512e-06**, R = **-0.0701**. **NO GCI AND NO OBSERVED ORDER ARE QUOTED, because neither is ever quoted off a non-monotone triple.** Diagnosis, and it is NOT a solver defect: the family was refined **past its own asymptotic range** — L2->L3 moves the scalar by **5.78e-05** relative, and the -0.90 % offset **does not move under 4x the cells**, so it is modelling error inside the **~1.8 %** budget the pre-registration totalled **before any run**. A **COARSER** triple is the fix, **as a new freeze, never an amendment**. | **I_ref = 6.131186321895e-02 m** — lab-derived similarity solution, `theta'(0) = 0.029370785745`, Blasius `f''(0) = 0.332057336215`, `beta = 1.7207876576`, **reproduced by THREE independent integrators** (brute to eta = 200; scipy `solve_ivp` rtol 1e-12 with an analytic `erfc` tail; stdlib RK4 + `math.erfc`). **The obvious low-Pr slug shortcut `sqrt(Pr/pi)` = 0.030901936162 is 4.954869 % high (slug-normalised) / 5.213175 % high (exact-normalised, the gate's own normalisation) and MUST NOT be the reference** — a control refuses (exit 2) unless that gap reproduces to +/-0.01. Manual **p. 219** | Gate A **3.00 %**, Gate B **1.00e-02**, frozen at `ab168c0e` before any solver ran (run root proved absent by `test -d` at 03:22:43Z) | `verification/runs/ansys_verification/VMFL076/{L1,L2,L3}/` | **`a0d5cb5b`** | **`42c89455`** | **48.900** (predicted 19.82, **2.467x**; cap 190 total 10/30/150, never crossed; waste 0.000) | **$0.0418** (derived at $0.0513/core-h, not measured) | `cases/ansys_verification/VMFL076/RESULTS.md`; calibration **C-113** |
| **28** | **VMFL004-R2** — Plane Couette Flow with Pressure Gradient, re-registration of row #25 (VM2026R1, p. 21). Same case, same mesh family, **case files a verified byte-identical copy**; `simpleFoam`, pressure gradient as a `vectorSemiImplicitSource`, streamwise cyclic. | 2026-08-26 | **`PASS`** | **volAverage(U)_x = 2.50007812 m/s** at L3. Triple **CONVERGING** on errors **5.00e-4 / 1.25e-4 / 3.13e-5** — each 4x the next, the signature of second order on an r=2 family. Observed order **p = 1.99999760**, **GCI_fine(Fs=1.25) = 3.906e-5**, Richardson **2.499999999**. Relative deviation **3.13e-5**, inside the band by **~32x**. Driven residual Ux_initial **3.22e-13**; transverse means |<U>_y| 4.3e-17 / 6.6e-17 / 2.5e-17. **All four planted controls fired at delta = 0.001234 exactly, no dilution**, including a NEW plant on the `Ux_initial` reader closing a latent rule-3 gap in row #25's grader. | **<u> = 2.5 m/s EXACT**, the closed form the lab EVALUATES ITSELF: integral of (9y - 6y^2) over [0,1] = 4.5 - 2 = 2.5. The manual prints **Figure .04.2 only**, no target table — so this is the VMFL019 route (declare the analytic gate in advance; never digitise a plot), which is why the ceiling is **`PASS`** and not `GATE REACHED`. Manual **p. 21** | **0.1 % relative (1.0e-3)** at the finest level — **CARRIED OVER BYTE-IDENTICAL from row #25**, together with the gate quantity, the reference and the ceiling. **Only the iterative-convergence CHANNEL changed** (driven `Ux_initial < 1e-7`), argued from the degeneracy of a 1-D fully-developed flow **independently of the known answer**; the transverse channel is **NOT deleted** but replaced with STRICTER detectors — |<U>_y|,|<U>_z| <= 1e-6 m/s (a larger transverse mean means broken cyclic or spurious cross-flow -> NOT A RESULT) and transverse residual < 1.0 (divergence). **A gate that is not touched cannot have been shaped by the answer.** | `verification/runs/ansys_verification/VMFL004-R2/{L1,L2,L3}/` | **`3c78d0f52a86c99e42f3c1cbd3285550160f38ef`** (frozen `bde20ff5` at 04:29:49Z; first run artifact 04:30:27Z) | **`417b4bbe60f49e67d0a33c2379fce20e38e72ee2`** (blob-verified == HEAD at grading) | **13.033** (L1 0.467 + L2 2.133 + L3 10.433; ranks = 1; cap 30, never approached) | **$0.01114** (derived at $0.0513/core-h, not measured) | `cases/ansys_verification/VMFL004-R2/RESULTS.md` |
| **29** | **VMFL064** — Low Reynolds Number Flow in a Channel with Sudden Asymmetric Expansion (VM2026R1, **p. 195**/196). Backward-facing step, laminar `simpleFoam`, Re_D = 200, three-level r = 2 ladder **3 072 / 12 288 / 49 152** cells, RANKS = 1. | 2026-08-26 | **`NOT A RESULT`** | **NO GRADEABLE VALUE EXISTS: the frozen comparator REFUSED (exit 2) at L3 and never reached a verdict, a triple or a GCI.** The refusal, verbatim from `verification/runs/ansys_verification/VMFL064/GRADING_ATTEMPT_REFUSED.txt`: **`REFUSED (exit 2): L3: wall shear never changes sign -- no reattachment found`**. **This is not a solver failure — all three levels ran clean and strict completion (rule 4) passed at ALL THREE**: `rc = 0`, an `End` line, `SIMPLE solution converged` at 424 / 907 / 2 152 iterations against an `endTime` of 20 000 (the solver stopped on its own `residualControl`, not on the clock), fields present, `ExecutionTime` count equal to the iteration count, and the age guard met. The two values that DO exist, and are NOT a verdict: **L1 LR/s = 4.714416** (LR = 0.023101 m) and **L2 LR/s = 4.800738** (LR = 0.023524 m). **The cross-instrument control PASSED and is the load-bearing good news**: the independent reader (sign change of near-wall `u_x`) agreed with the wall-shear reader to **2.42e-07 m** and **5.77e-08 m** against a refusal threshold of three cell widths (4.7e-03 / 2.3e-03 m) — four to five orders of margin, so the pre-registration's one named principal risk (an inverted `-(nHat & devTau)` sign convention, `ORIENT = -1`) is **confirmed correct on real data, not assumed**. **Diagnosis of the refusal** (`b8b5e2bf`): the finest mesh resolves a **secondary corner vortex** the coarser two do not, so the wall-shear profile no longer **starts** negative — and the frozen reader requires it to. **The reader is not repaired here.** Gates close after first compute (rule 2); a reader that does not assume a monotone start is a **NEW registration**, never an amendment. Both planted controls fired clean on the real run, including the p-floor control (`P_MIN = 0.05`: `(1.0,1.1,1.2) -> NOT A RESULT, no GCI`) printed before any level was read. | **LR/s = 5** — **EXPERIMENTAL**: Armaly, Durst, Pereira & Schoenung, *J. Fluid Mech.* **127**:473, 1983 (also Freitas, *J. Fluids Eng.* **117**, p. 208, 1995). Ansys Fluent 4.91 is **CONTEXT ONLY, never the gate**. Manual **p. 195** | **`abs(LR/s − 5.0) / 5.0 ≤ 0.10`** (10 %) at the finest level — the frozen band is written with `abs(...)` here in place of the two pipe characters that would split this table cell; the frozen text itself is unchanged **AND** a `CONVERGING` triple (rule 5); tier ceiling declared in advance **`GATE REACHED`**; cap **90 core-min** total across the three levels — all frozen at `1dc0e4d5` before any solver ran, and **untouched by this row** | `verification/runs/ansys_verification/VMFL064/GRADING_ATTEMPT_REFUSED.txt` (with `{L1,L2,L3}/`, `COST.txt`, `LAUNCH_RECORD.txt`, `CONTENTION.txt`) | **`1dc0e4d5`** (freeze) **+ `a7c42398`** (PRE-COMPUTE AMENDMENT 1, the `P_MIN = 0.05` observed-order floor with a planted control that DRIVES it); prereg blob **`7b9fd0dcaa141cacf4e0f3dee5a6ceab0b9f48b5`** | **`0be9126cd6a3e3c860b98854a4e21ba1102edfdc`** — blob-verified equal to the on-disk file **before** it graded | **5.383** (L1 0.0333 + L2 0.300 + L3 5.05; total wall 323 s × RANKS 1 ÷ 60; `COST.txt`) — **6.0 % of the 90 core-min cap, never approached** | **$0.0046** (derived at $0.0513/core-h, not measured — the box cannot read its own billing, `COMPUTE_BUDGET_CHARTER.md` §5) | `cases/ansys_verification/VMFL064/RESULTS.md`; graded at `b8b5e2bf`; calibration **C-131** |


## Dated note — 2026-08-26T03:1xZ — rows #22 and #23 landed, and BOTH were graded by comparators that OUTLIVED the fleet that launched them

**Appended by `ansys-verification-supervisor` personally.**

Both solves were **live when a session limit killed the fleet at ~23:00Z on 2026-08-25**, and
**both ran to completion without an agent watching** — VMFL021-R2's L3 finished at **23:02Z**
and VMFL023's L3 at **00:57Z**, after every agent that knew about them was dead. That is what
`setsid` buys, and it is the whole argument for detached launching.

**TWO RATIONALISATIONS WERE REFUSED BEFORE THESE ROWS WERE WRITTEN.** A triage lane reported
VMFL023's L3 last time as `299.9999999998` against an `endTime` of `300` and annotated it
*"(rounding: within machine epsilon)"*. The frozen pre-registration registers that check
**literally**, in its own words *"NO DEPARTURE IS DECLARED"* — and `heat-transfer` ruled two
K0d cases **NOT DONE** on this exact clause hours earlier. **The supervisor did not settle it
by argument.** The **frozen comparator** settled it: `grade_vmfl023.py` implements the clause
as `abs(t_last - ENDTIME) > 1e-6 -> refuse("C3")`, the drift is **2e-10**, and the instrument
**accepts**. The tolerance was frozen before compute and could not have been chosen to fit.

Likewise VMFL021-R2's `ExecutionTime` count (12 288 lines against ~3 000 000 nominal steps)
was **not** an omitted conjunct: line 114 of its frozen pre-registration registers *"strict
completion (rule 4, **adaptive-dt form**)"* **by name**, with Amendment 3's endTime/writeInterval
assertion as the substitute, and the freeze at **22:28:38Z** precedes the first run artifact at
**22:29Z**. A declared departure is a verdict; a silent one would have been `NOT A RESULT`.

**FORWARD NOTE, not a change to either frozen document:** *"rule 4, adaptive-dt form"* is a
form cited **by name**. A future registration writes the substitute conjunct out in full.

**Both planted-zero controls fired, and VMFL023's is controlled in BOTH directions** — a
sinusoid planted at St = 0.165000 was recovered as **0.165000**, and a **flat (dead) signal was
REFUSED**. A reader shown able to see a known frequency *and* to reject a dead one is stronger
evidence than a one-sided plant. VMFL021-R2 planted **1.234e-06 m³/s** and read back
**1.2340000000000766e-06**.

**Both comparators were blob-verified against HEAD before they graded**, and `__debug__` was
**True** in the grading interpreter — so the verdict-vocabulary guards were live, and these two
rows are **not** exposed to the lab-wide `python3 -O` hazard.

**CEILING, and why neither is a `PASS`.** Both references are **experimental/correlation** —
White/Kim & Lee for VMFL023, Nurick 1976 for VMFL021-R2. A measured external reference buys
`GATE REACHED`; `PASS` in this team is reserved for a closed form the lab **evaluates itself**.
Neither row is softened and neither is inflated.

| **30** | **VMFL064-R2** — Low Reynolds Number Flow in a Channel with Sudden Asymmetric Expansion (VM2026R1, **p. 195**/196). Backward-facing step, laminar `simpleFoam`, Re_D = 200, three-level r = 2 ladder **3 072 / 12 288 / 49 152** cells, RANKS = 1. **Re-registration of row #29 under `ANSYS_VERIFICATION_CHARTER` §6: a NEW row that CITES row #29 and does NOT overwrite it — row #29 stands as `NOT A RESULT` and is unchanged by this row.** The one thing the R2 changed is the reattachment reader: row #29's frozen comparator took the **FIRST** negative-to-positive crossing of the wall shear and refused whenever the profile did not **start** negative; the R2 takes the **LAST** crossing inside a registered window `0.0 m < x ≤ 0.05 m`. **Every gate-path constant is byte-identical to attempt 1** — reference, band, ceiling, mesh family, cap, `endTime` — and the launcher hashed all nine case inputs against their attempt-1 blobs before a core-minute was spent. **ROW #29'S DIAGNOSIS IS NOW CONFIRMED FROM THIS RUN'S OWN DATA, not argued:** `corner_vortex_resolved` is **false at L1 and L2 and true at L3**, and the crossing census reads `n→p = 1, p→n = 0` at both coarse levels and `n→p = 1, p→n = 1` at L3 with the profile **starting positive** — the secondary corner vortex appearing at the finest level and nowhere else, which is precisely what made the first-crossing reader lose its premise. **The R2 reader did not move the two values that already existed:** L1 `LR/s` = 4.714416 and L2 = 4.800738, identical to the pair row #29 published as the only ones it had. **Controls, all live at grade time:** `--selftest` 37/37 identical under `python3` and `python3 -O`, `ast.Assert` = 0, driven **by the launcher before the solver**; planted zero 0.001234 read back **0.0012340000000004199** (single-point plant into a single-point reader — no averaging dilution, L-340); the **corner-vortex planted control** recovered a constructed `LR/s = 5.999803` while **the attempt-1 logic exited 2 on the same bytes**, and single-bubble agreement between the two readers is exact (0.0294 m both); the **window is load-bearing, measured** — 0.029399 m with it, 0.065038 m without; **cross-instrument** wall-shear vs near-wall-`u_x` agreement **2.42e-07 / 5.77e-08 / 1.42e-08 m** against a three-cell-width threshold (4.69e-03 / 2.34e-03 / 1.17e-03 m); the `P_MIN = 0.05` floor driven **both** ways. **Strict completion (rule 4) HOLDS at all three levels** with L-342 field classes: `rc = 0` MEASURED at each, an `End` line, `SIMPLE solution converged` at 424 / 907 / 2 152 iterations against an `endTime` of 20 000 (the solver stopped on its own `residualControl`, not on the clock), `U` `p` `wallShearStress` `Cx` `Cy` present, `ExecutionTime` count equal to the iteration count, age guard met. **THREE INFRASTRUCTURE FACTS, RECORDED AND VOIDING NOTHING (L-342 — a bookkeeping failure invalidates the bookkeeping, never the physics artifacts):** (1) **`VMFL064R2-ENTRY-DEF-1`** — the first queue entry, fired 17:49:13Z, **omitted the launcher's required `<run_root>` argv** and the launcher refused **at zero compute** with `run_vmfl064_r2.sh: line 49: 1: usage: run_vmfl064_r2.sh <run_root> [levels...]` (`launcher.queue.out.attempt1`; `STATUS.VMFL064-R2.attempt1-refused-no-run_root`, `launcher_rc=1`). The guard worked as written; the entry was re-filed with the run root at 20:45Z. **Standing lesson: drive every entry's argv once against the launcher's usage guard before enqueueing.** (2) **`CAP_OVERRUN.txt` reports an overrun that did not happen** — stamped 20:45:38Z, **five seconds BEFORE the graded launch began at 20:45:43Z**, claiming *"elapsed 10585 s"*, which is the interval since the **attempt-1** launch epoch and includes three hours in which nothing ran. It says `REPORTED, NOT ENFORCED`, nothing was killed, and the run used **5.9 %** of its cap. A runner bookkeeping artefact, kept rather than deleted. (3) The launcher's **`DETACH WARN` at all three levels is the CHECK being wrong, not the detachment**: `setsid` is applied to `timeout` *inside* the waited-on subshell, so `SID == PID` can never hold in this form (wrapper pids 390529 / 390586 / 390833, all SID 390179; independently reproduced on the VMFL011-R2 launcher, which inherits the shape). It can raise a false alarm but never certify a false pass, and it gates nothing. | 2026-08-26 | **`GATE REACHED`** | **`LR/s` = 4.853056** at the finest level (L3, 49 152 cells; `LR` = 0.023779973479 m), against the experimental 5.0 — **deviation 2.9389 %**, inside a **10 %** band, consuming 29 % of the tolerance. **Triple `CONVERGING` and monotone** (4.714416 / 4.800738 / 4.853056; d21 = 0.086322377043, d32 = 0.052317864523, R = 0.606075), **`p_obs` = 0.722431**, **`GCI_fine` = 2.0733 %** at Fs = 1.25, Richardson `f_ex` = 4.933550. **`p_obs` is BELOW the formal `p_f` = 2 and is reported as that, not as a win** — the pre-registration flagged `p_obs > 2.3` as suspicious and the observed order sits at the other end, the ordinary signature of a wall-profile quantity interpolated between faces on a just-resolved bubble. It is above the registered floor `P_MIN` = 0.05, so a GCI is quoted. **The honest reading, stated beside the verdict: `GCI_fine` 2.0733 % is the same order as the 2.9389 % deviation itself, so the finest value agrees with the reference inside a band about three times the numerical uncertainty — the reattachment length is NOT pinned to three digits by this row.** | **`LR/s` = 5** — **EXPERIMENTAL**: Armaly, Durst, Pereira & Schoenung, *J. Fluid Mech.* **127**:473, 1983 (also Freitas, *J. Fluids Eng.* **117**, p. 208, 1995). **REFERENCE KIND: measured/experimental — CAN buy P**, and this row still does **not**, because the ceiling was lowered in advance (see the gate column). Ansys Fluent 4.91 is **CONTEXT ONLY, never the gate**. Manual **p. 195** | **`abs(LR/s − 5.0) / 5.0 ≤ 0.10`** (10 %) at the finest level — written with `abs(...)` here in place of the two pipe characters that would split this table cell; the frozen text itself is unchanged — **AND** a `CONVERGING` triple (rule 5). **BYTE-IDENTICAL to attempt 1's band and NOT re-justified from any number.** Tier ceiling **`GATE REACHED`** declared on pre-registration line 4 and **hard-coded in the comparator, which cannot print `PASS` or `HOLDS`**: although the reference is experimental, this team's product is reproducing the Ansys manual and Sanaa's ruling caps that at `GATE REACHED`. **NOT A CREDENTIAL — this row is not a `PASS` and must not be counted as one.** Cap **90 core-min** total across the three levels, byte-identical to attempt 1 | `verification/runs/ansys_verification/VMFL064-R2/GRADING_VMFL064_R2.json` (with `{L1,L2,L3}/`, `RUN_RC.{L1,L2,L3}`, `COST.txt`, `LAUNCH_RECORD.txt`, `CONTENTION.txt`, `CAP_OVERRUN.txt`, `STATUS.VMFL064-R2`, `STATUS.VMFL064-R2.attempt1-refused-no-run_root`, `launcher.queue.out`, `launcher.queue.out.attempt1`) | **`3e7c792cbde23a7cc105d63d01487dc5d01c8040`** (freeze — **the only commit that has ever touched that pre-registration**, and it landed before any R2 solver started); prereg blob **`abe17c3b990acd31bba12fde01aaf757c6ab398e`** | **`e04fdf937d557b4919928ab72b9fe5eb42f8b49a`** — the launcher printed `freeze OK` against this blob before every level; launcher blob **`c17ee2e2693b382fd043aadc326bdae8e51fd7b6`** | **5.3** core-min MEASURED (L1 0.0167 + L2 0.2833 + L3 5.0; total wall 318 s × RANKS 1 ÷ 60; `COST.txt` and each `RUN_RC.<level>`) against **5.4** predicted → **ratio 0.98**; **5.9 % of the 90 core-min cap**, never approached; **WASTE 0.000** — the 17:49Z refused launch spent nothing | **$0.0045** (derived at $0.0513/core-h, not measured — the box cannot read its own billing, `COMPUTE_BUDGET_CHARTER.md` §5) | `cases/ansys_verification/VMFL064-R2/RESULTS.md`; calibration **C-141** |

---

## Dated note — 2026-08-26 — row #30 lands, and the credential-count headline is struck as stale in BOTH numbers

**Appended by an `ansys-lane-opus` lane on the supervisor's dispatch. Nothing above is edited; the
headline strike at the top of this file follows the pattern already used twice on that line.**

**Row #30 is the team's first R2 to convert a refusal into a graded verdict.** Row #29 produced no
number at all — its frozen comparator refused at L3 — and row #30 returns `LR/s` = 4.853056 inside a
band that was never touched. **The credential count does not move**: `GATE REACHED` is a tier, not a
credential, and the ceiling was declared before compute.

**The headline was stale by more than one append.** It read *"5 PASS of 16 run"* against a register
that already held **29 rows and 6 `PASS` rows**. The count was re-derived here **from the rows in the
HEAD blob** — not from the worktree copy, which has repeatedly been behind (L-333) — one row at a
time, and the four classes are listed by row number at the top of the file so the next reader can
check the arithmetic instead of trusting it. Two rows account for the numerator gap: **#28
(VMFL004-R2)**, appended after the line was last written, and **#7 (VMFL045-R2)**, a `PASS` that had
been present all along and was never counted.

**Two rows resisted a naive parse and were read by hand rather than guessed:** rows **#7** and **#21**
carry literal `|` characters inside their description cells, so a column-split by pipe misreads their
verdict field. Both were resolved by reading the verdict token directly out of the row — #7 `PASS`,
#21 `NOT A RESULT`. **A count produced by a parser that silently dropped two rows would have been
wrong by exactly the amount that mattered**, and that is why the row numbers are published per class.

---

## Dated note — 2026-08-26 — row #31 lands: VMFL011-R2, a refusal that repaired one control and exposed a second

**Appended by an `ansys-lane-opus` lane on the supervisor's dispatch. Nothing above is edited.**
The register is append-only; row #26 (VMFL011) stands unchanged as `NOT A RESULT`, and this row
cites it rather than overwriting it (`ANSYS_VERIFICATION_CHARTER` §6).

| **31** | **VMFL011-R2** — Laminar Flow in a Triangular Cavity (VM2026R1, p. 41). Top-wall-driven triangular cavity, Re = 400 (rho = 1, mu = 0.01, base 2 m, height 4 m, moving base wall U = 2 m/s), OpenFOAM v2606 `simpleFoam`, steady laminar SIMPLEC on a collapsed-hex triangular block, three-level r = 2 ladder **800 / 3 200 / 12 800** cells, RANKS = 1, `endTime` 20 000 SIMPLE iterations with no `residualControl`. **Re-registration of row #26 under `ANSYS_VERIFICATION_CHARTER` §6: a NEW row that CITES row #26 and does NOT overwrite it — row #26 stands as `NOT A RESULT` and is unchanged by this row.** The one thing the R2 changed on the control path is the **plant SIZING on the `rms_vs_benchmark` channel** (L-340: an averaging reader dilutes a single-point plant by ~1/sqrt(N), so the plant is sized `K·U_wall·base` at `K = 4` and applied to every data row); the threshold rule `delta > 0.1·plant` is carried character for character, and **every gate-path constant — reference CSV blob `9f11191b…`, band, ceiling, mesh family, cap, `endTime`, gate quantity — is byte-identical to attempt 1**, hashed by the launcher against its attempt-1 blobs before a core-minute was spent. **THE REPAIR WORKED, AND IT IS MEASURED ON THE REAL ATTEMPT-1 BYTES, not argued:** the L-340 sizing control ran before any level was read and printed parent pair delta **3.677091e-07** < **1.234000e-04** (REFUSES, reproducing row #26's recorded `3.67709e-07`), all-row plant at the parent magnitude **1.063654e-04** < **1.234000e-04** (still REFUSES — the sizing is load-bearing, not the shape), and the sized plant **0.322114** moving the RMS by **1.185698e-01** > **3.221137e-02** (PASSES, inside the derived bounds **[8.052843e-02, 1.610569e-01]**). **AND THE RUN THEN REFUSED ON THE OTHER CHANNEL, WHICH HAD NEVER ONCE BEEN TESTED ON REAL BYTES.** `_perturb` plants into the **first data row** of `bisect_U.xy`; on the real bisector that row is the sample at **y = −4 m, the collapsed-hex apex, where the no-slip solution is `u ≡ 0` exactly**, so `−0.1234` there gives `−0.1234` — **above** the profile minimum `min(u) = −0.528988913215` m/s — and a `min()` reader returns the identical number, moving by **exactly 0**. **This is a SECOND, DISTINCT L-340 failure mode: plant LOCATION, not plant MAGNITUDE.** A correctly sized point plant landed on a row **outside the point reader's support**. **Why the freeze did not catch it, and it is checkable:** both comparators build their channels in the same dict order — `rms_vs_benchmark` first, `u_min_norm` second (attempt-1 blob `e369496b…` lines 262–265; R2 `controls()`) — and attempt 1 exited 2 inside the **first** entry, so the `u_min_norm` plant had **never run on a real VMFL011 bisector file**; the pre-registration's *"`u_min_norm` — a point reader — passed the identical control"* was true of the selftest profiles only. **A control standing behind another control's refusal is an untested control**, and this row is the measurement that says so. **The reader is NOT repaired here** — gates close at first compute (rule 2), and a plant placed at the reader's own argmin is a NEW registration and a NEW row, never an edit. **STRICT COMPLETION (rule 4) HOLDS AT ALL THREE LEVELS**, checked from `RUN_RC.<level>` and the logs independently of the comparator: `rc = 0` MEASURED at each, exactly one `End` line in `log.simpleFoam` matched **by exact name**, last `Time` 20 000 == `endTime` 20 000, `U` and `p` present at `endTime`, `ExecutionTime` count 20 000 == `endTime`, and the age guard met at every level (`0/U` mtime strictly older than `20000/U`). **The instrument refused; the solver did not fail.** **THE SUPERVISOR'S PRIOR PREDICTION WAS `GATE FAIL` AND IS RECORDED AS NOT WHAT HAPPENED:** the run never reached its band, so there is nothing to soften and nothing to claim — a predicted `GATE FAIL` that arrives as a refusal is still a refusal. **Freeze verified three ways before grading:** `git hash-object` of the comparator and the pre-registration on disk equal their blobs at the freeze commit `9f9d6925` **and** at HEAD, and `RUN_RC.L1/.L2/.L3` and `LAUNCH_RECORD.txt` independently record the same two blobs, written before any core-minute was spent. | 2026-08-26 | **`NOT A RESULT`** — the frozen comparator **REFUSED (exit 2)** on its planted-zero control (rule 3) for **`u_min_norm`**, verbatim from `verification/runs/ansys_verification/VMFL011-R2/GRADING.txt`: **`REFUSING (exit 2): planted-zero control FAILED for u_min_norm.`** / **`planted -0.1234 into …/L1/postProcessing/bisector/20000/bisect_U.xy, reader moved by only 0.`** / **`A reader not shown able to see a non-zero cannot certify a zero (CLAUDE.md rule 3).`** | **NONE — NO GRADEABLE VALUE EXISTS.** The comparator refused inside L1's controls, **before any channel value, triple class, observed order or GCI was computed**, so this row publishes no `rms_vs_benchmark`, no `u_min_norm`, no triple, no `p_obs` and no GCI. **Attempt 1's ungraded readings belong to row #26 and are NOT restated here as this row's numbers.** The one fact this run establishes about its own fields, and it is a reproduction check rather than a verdict: its L1 bisector minimum is `min(u)/U_wall = −0.264494`, matching row #26's L1 `u_min_norm` to six decimals. | normalised x-velocity on the base-bisector, **`u_min/U_wall = −0.318062` at y = −0.987229 m** — digitised from **Figure .11.2**; R. Jyotsna & S.P. Vanka, *J. Comp. Phys.* **122**:107–117 (1995), the manual's own cited Reference. **REFERENCE KIND: NUM, code-to-code, and doubly indirect (digitised from a figure) — buys NEITHER V NOR P**, so the **TIER CEILING is `GATE REACHED`**, hard-coded in the comparator, which cannot print `PASS`. **Never reached.** Manual p. 41; the manual p. 42 prints this **only as a FIGURE with NO discrete target table**. Ansys Fluent/CFX curves are overlaid on that figure and are **context only, never the gate**. | **`rms_vs_benchmark` ≤ 0.030** (3 %) at the finest level L3 — the RMS over the 46 benchmark abscissae of the normalised x-velocity error — **AND** a `CONVERGING` triple on `u_min_norm` (rule 5), with the observed-order floor `P_MIN = 0.05`. **BYTE-IDENTICAL to attempt 1's band and NOT re-justified from any number.** Cap **50 core-min** RUNNING TOTAL across the three levels, byte-identical to attempt 1. All frozen at `9f9d6925` before any R2 solver ran and **untouched by this row**. **Never evaluated — the verdict is a refusal, prior to the band.** | `verification/runs/ansys_verification/VMFL011-R2/GRADING.txt` (with `{L1,L2,L3}/`, `RUN_RC.{L1,L2,L3}`, `STATUS.VMFL011-R2`, `COST.txt`, `LAUNCH_RECORD.txt`, `CONTENTION.txt`, `launcher.queue.out`, and per level `log.blockMesh`, `log.checkMesh`, `postProcessing/bisector/20000/bisect_U.xy`, `postProcessing/resid/0/solverInfo.dat`). **No `GRADING_VMFL011_R2.json` exists — the comparator exited before writing one.** **DISCLOSED: each level's `log.simpleFoam` is 16.7–16.8 MB, over the 5 MB per-file staging refusal, so the three solver logs stay ON DISK ONLY**; the `End` line, last `Time` and `ExecutionTime` count read out of them are independently recorded in `RUN_RC.<level>` and `LAUNCH_RECORD.txt`, which are at HEAD. Bulk fields (`0/`, `20000/`), `constant/polyMesh` and `processor*` stay on disk by the same ruling. | **`9f9d69258dc7bb4ed981490cd90a85d7362ebed4`** (freeze — the only commit that has ever touched that pre-registration, and it landed before any R2 solver started); prereg blob **`a8c9b6f30e4383e23594b4203eea3bd28ded0a2a`**, verified equal on disk, at the freeze and at HEAD | **`45aa4613253d1d594b69b7f774e18cd7a312a20e`** — verified equal on disk, at `9f9d6925` and at HEAD before it graded; the launcher recorded the same blob in `LAUNCH_RECORD.txt` and in every `RUN_RC.<level>`; launcher blob **`b1d6a74a718e8e0dc8c5a76c0a07cf48006d2ab5`** | **9.0833** core-min MEASURED = (17 + 63 + 465) wall s × RANKS 1 ÷ 60 (`COST.txt` `total_core_min = 9.0833`; `RUN_RC.L1/.L2/.L3` `core_min` 0.2833 + 1.0500 + 7.7500) against **8.3** predicted → **ratio 1.094**; **18.2 % of the 50 core-min cap**, never approached; **WASTE 0.000** — every level reached `endTime` with `rc = 0`, and a comparator refusal costs no solver time and is not a re-run. Gross = cleaned (longest level 465 wall s, far below the 3600 s stall trigger). Gap attributed to **contention, not misprediction**: load average **14.79 on 16 cores** at launch (`CONTENTION.txt`, 21:11:27Z, with one `rhoCentralFoam`, one `icoFoam` and three `python3` live) against attempt 1's uncontended run | **$0.0078** (derived at $0.0513/core-h, **not measured** — the rate is reported-by-owner and the box cannot read its own billing, `COMPUTE_BUDGET_CHARTER.md` §5) | `cases/ansys_verification/VMFL011-R2/RESULTS.md`; calibration **C-144** |

**The credential-count headline is NOT struck, and that is the deliberate reading.** Row #31's
verdict is `NOT A RESULT`, so the `PASS` count does not move and stays at **6**. Under the
supervisor's standing instruction the headline is struck and re-issued **only when the `PASS`
count changes**; it does not here. **Stated so nobody has to infer it: the register now carries
31 rows, and the headline's denominator of 30 is therefore one behind by design, not by
oversight.** Recording that here is cheaper and safer than editing a line the append rules
protect.

**What this row buys, and what it does not.** It buys a **measured** statement that the L-340
repair works on the bytes that motivated it, and a **measured** statement that a second control
on the same file was blind for a reason nobody had tested. It buys **no physics**: no value, no
triple, no order, no GCI. VMFL011 remains, across rows #26 and #31, a case this lab has run
three times' worth of solver work on and has still never graded — and the honest reading is
that the instrument, not the solver, is what has failed twice.

---

## Dated note — 2026-08-26 — row #32 lands: VMFL017-R2, a cap that fired exactly where the freeze said it would

**Appended by an `ansys-lane-opus` lane on the supervisor's dispatch. Nothing above is edited.**
Row #19 (VMFL017 attempt 1, `PENDING`) stands unchanged; this row cites it and does not
overwrite it (`ANSYS_VERIFICATION_CHARTER` §6).

| **32** | **VMFL017-R2** — Transonic Flow over an RAE 2822 Airfoil (VM2026R1, **p. 69**). M = 0.73, Re = 6.5e6, alpha = 2.79 deg, p = 43 765 Pa, T = 300 K; OpenFOAM v2606 **`rhoCentralFoam`** (explicit, transient, shock-capturing, Kurganov flux + vanLeer) with `kOmegaSST` RAS and `hePsiThermo`/`perfectGas`, on a birth-certified ratio-2 2-D C-mesh family **23 040 / 92 160 / 368 640** cells, RANKS = 1, characteristic far-field, `endTime` **0.05 s of PHYSICAL time** with `adjustTimeStep` at `maxCo` 0.2. **NEW ROW citing row #19 (VMFL017 attempt 1, `PENDING`, `rhoSimpleFoam`, commit `d1de064b`) — row #19 is NOT removed and NOT re-labelled.** The supervisor's ladder ruling executed here was to switch the instrument from a pressure-based steady solver (which hit `FOAM FATAL ERROR: Negative initial temperature T0` inverting energy to temperature across a forming shock) to a density-based shock-capturing one. **THE PRE-REGISTRATION PREDICTED THIS ROW'S OUTCOME BY NAME, WITH NUMBERS, BEFORE COMPUTE, AND THE NUMBERS LANDED.** PRE-COMPUTE AMENDMENT 2 §E, frozen at `45328f8a`: *"L1 is expected to stop at its own cap with rc 124, the launcher then stops without launching L2 or L3, and the case is `NOT A RESULT`."* **A correctly predicted failure is still a failure and the verdict is unchanged by having been foreseen.** **WHAT THIS ROW BUYS IS A MEASUREMENT OF THE INSTRUMENT'S COST, converted from a scratch smoke projection into a graded-path number, and all three of §E's figures held:** physical time reached inside the 300 core-min cap **8.89114e-04 s (1.778 % of `endTime`)** against a projected ~9.0e-04 s (~1.8 %) — **ratio 0.988**; integration rate **27.04 steps per wall-second** (486 748 steps in 17 999 wall s) against a projected ~27.7 — **ratio 0.976**; core-minutes for L1 ALONE to reach `endTime` **16 871** (300.0 × 0.05 ÷ 8.89114e-04) against a projected ~16 700, about **56× L1's cap** — **ratio 1.010**. Realised adaptive step at the stop **Δt = 1.852520264e-09 s** (§E registered 1.806e-9 s at `maxCo` 0.2). **CONTENTION IS FALSIFIED AS THE CAUSE, AND IT IS MEASURED, NOT ARGUED:** `ExecutionTime` **17 761.7 s** against `ClockTime` **17 999 s** is **98.68 % CPU-bound** on a box carrying load average 13.73 of 16 cores at launch (`CONTENTION.txt`, 17:18:52Z) — **the process was computing, not starved.** L2 and L3 are worse by ~8× and ~64× (four/sixteen times the cells at half/quarter the step), so the registered family ceiling of 2 400 core-min is not within an order of magnitude of what this instrument needs here. **STRICT COMPLETION (rule 4) FAILS AT L1 ON THE REGISTERED CLAUSE, and nothing was bent to hide it:** `rc = 124` from `timeout` at `timeout_s = 18000` = cap 300 core-min × 60 ÷ RANKS 1, **zero `End` lines**, last `Time` 8.89114e-04 against `endTime` 0.05, **no time directory at `endTime`**, so the age guard has nothing to test. The launcher stopped at its own line 279 and `LAUNCH_RECORD.txt` records **`later levels are NOT launched`**; its stop text reads **`STOP L1: rc=124 after 18000s = 300.0 core-min (cap 300 core-min).`** and *"an overrun does NOT get a new budget and endTime is NOT shrunk to fit (rule 12)"*. **`endTime` WAS NEVER REDUCED TO FIT THE CAP.** **Freeze verified three ways before grading:** `git hash-object` of the comparator and the pre-registration on disk equal their blobs at the freeze commit `45328f8a` **and** at HEAD; `launcher.queue.out` printed `freeze OK` against both blobs before the solver started; and `RUN_RC.L1` and `LAUNCH_RECORD.txt` carry them independently. **Twelve further registered pre-flight assertions printed `registered OK`** in the same file — `endTime` 0.05, `deltaT` 1e-9, `maxCo` 0.2, `maxDeltaT` 1e-5, `writeControl adjustableRunTime` with `writeInterval` 0.05 and `adjustTimeStep yes`, `forceCoeffs1 executeInterval` 1e-4, the comparator's `ENDTIME_PHYS == controlDict endTime`, the 500-sample / 20-in-final-window plateau precondition, the mesh birth certificate `2a7e82c2…`, and the L1/L2/L3 `blockMeshDict` identity with the birth-certified attempt-1 family. **The `P_MIN = 0.05` planted control fired on the refusal path** and is printed in `GRADING.txt`; the `PLANT = 7.531e-3` planted zero was green in the launcher's pre-flight `--selftest` and was **not** reached on the graded path, because there was no settled window to read — stated rather than implied. | 2026-08-26 | **`NOT A RESULT`** — the registered per-level cap fired (`rc = 124`) and the frozen comparator **REFUSED (exit 2)** on strict completion (rule 4), verbatim from `verification/runs/ansys_verification/VMFL017/R2/GRADING.txt`: **`REFUSE (VMFL017-R2): no End line in solver log: …/VMFL017/R2/L1`**. **A MEASURED INSTRUMENT LIMIT, NOT A FAILED SOLVE AND NOT A FABRICATED NUMBER** — and not softened by having been predicted. | **NONE — NO GRADEABLE VALUE EXISTS.** No `Cd`, no `Cl`, no plateau window, no triple, no observed order, no GCI: L1 reached **1.778 %** of its registered `endTime` and the run never approached a settled state, so the comparator refused before any channel was read. **What DOES exist, and it is a cost measurement rather than a physics result:** L1 advanced **486 748 timesteps to t = 8.89114e-04 s in 300.0 core-min**, at **Δt = 1.852520264e-09 s** and **27.04 steps per wall-second**, which prices L1 alone at **16 871 core-min to `endTime`** — **about 56× its own cap.** | **`Cd` = 0.0168 and `Cl` = 0.803** — **EXPERIMENTAL**: P.H. Cook, M.A. McDonald & M.C.P. Firmin, *Aerofoil RAE 2822 — Pressure Distribution and Boundary Layer and Wake Measurements*, AGARD AR-138, 1979; manual p. 69. **REFERENCE KIND: measured/experimental — CAN buy P**, and this row does not, because the ceiling was lowered in advance and because no value was produced. Ansys Fluent (`Cd` 0.016, `Cl` 0.78) and CFX (`Cd` 0.0162, `Cl` 0.7981) are **CONTEXT ONLY, never the gate**. **Never compared.** | **`abs(Cd − 0.0168)/0.0168 ≤ 0.10` AND `abs(Cl − 0.803)/0.803 ≤ 0.05`** at the finest CONVERGING level — written with `abs(...)` here in place of the pipe characters that would split this table cell; the frozen text is unchanged — **AND** a `CONVERGING` triple on BOTH channels, else single-grid (rule 5), **AND** the Amendment-4 plateau clause (a non-settled window is REFUSED, never averaged through), with the observed-order floor `P_MIN = 0.05`. Tier ceiling **`GATE REACHED`** declared on pre-registration line 4. Caps **PER LEVEL, not a shared drawdown: L1 300 / L2 600 / L3 1 500 core-min**, family ceiling 2 400 if all three ran. All frozen at `45328f8a` before any solver ran and **untouched by this row**. **NEVER EVALUATED — the verdict is a cap and a refusal, prior to the bands.** | `verification/runs/ansys_verification/VMFL017/R2/GRADING.txt` (with `L1/`, `RUN_RC.L1`, `STATUS.VMFL017-R2`, `LAUNCH_RECORD.txt`, `CONTENTION.txt`, `launcher.queue.out`, and `L1/log.blockMesh`, `L1/log.checkMesh`, `L1/postProcessing/forceCoeffs1/0/coefficient.dat`). **No grading JSON exists — the comparator refused before writing one. There is NO `COST.txt` at this run root** — the launcher stopped on the cap before writing one; the core-minutes are taken from `RUN_RC.L1` (`core_min = 300.0`) and `LAUNCH_RECORD.txt` (`total_core_min = 300.0`), which agree. **DISCLOSED: `L1/log.rhoCentralFoam` is 528 629 457 bytes (504 MB), 486 748 timesteps at full print, far over the 5 MB per-file staging refusal, so it stays ON DISK ONLY**; the three clauses read out of it (zero `End` lines, last `Time` 8.89114e-04, `ExecutionTime` 17 761.7 s against `ClockTime` 17 999 s) are quoted in this register and the first two are independently recorded in `RUN_RC.L1` and `LAUNCH_RECORD.txt`, which are at HEAD. `0/` fields and `constant/polyMesh` stay on disk by the same ruling. | **`45328f8a4268e3d425a34be42f20ccc378c3e62f`** (freeze, before any R2 solver started); prereg blob **`9a58eed3413b7b54b92bbfbee527c7a913cbd8ec`**, verified equal on disk, at the freeze and at HEAD, and printed `freeze OK` by the launcher before compute | **`97c556f4a0d07f021480c75144d1b72a811fc391`** — verified equal on disk, at `45328f8a` and at HEAD; the launcher printed `freeze OK` against this blob before the solver started; mesh birth certificate **`2a7e82c280c2bad9c191996a26013ca4decf4e6c`** | **300.0** core-min MEASURED = 18 000 wall s × RANKS 1 ÷ 60 (`RUN_RC.L1` `core_min = 300.0`; `LAUNCH_RECORD.txt` `total_core_min = 300.0`) against **300.0** registered for this launch → **ratio 1.000**; **100.0 % of L1's per-level cap, which fired exactly, to the second**. L2 and L3 spent **0.000** — not launched. **WASTE 0.000** in the charter §6 sense: nothing abandoned, nothing re-run, the cap stopped the level as registered. **NAMED SEPARATELY AND NOT LAUNDERED INTO THE RATIO: 300.0 core-min bought no graded number** — the disclosed price of the measurement, paid on purpose | **$0.2565** (derived at $0.0513/core-h, **not measured** — the rate is reported-by-owner and the box cannot read its own billing, `COMPUTE_BUDGET_CHARTER.md` §5) | `cases/ansys_verification/VMFL017/R2/RESULTS.md`; calibration **C-149** |

**The credential-count headline is NOT struck.** Row #32's verdict is `NOT A RESULT`, so the
`PASS` count does not move and stays at **6**. Under the supervisor's standing instruction the
headline is struck and re-issued only when the `PASS` count changes. **Stated so nobody has to
infer it: the register now carries 32 rows, and the headline's denominator of 30 is therefore
two behind by design, not by oversight** — as is the denominator note appended with row #31.

**The reading this row and row #31 share, and it is not a coincidence.** Both are
`NOT A RESULT`; neither is a solver failure. Row #31's instrument refused because a control had
never been exercised on real bytes; row #32's instrument ran perfectly and simply cannot afford
the physics at the registered `endTime`. **Two different ways for a lab to spend compute and
buy no number — one a grading defect, one an honest cost measurement — and the register is
worth having precisely because it does not let either of them read as a result.**

---

## Dated note — 2026-08-27 — row #33 lands: VMFLGPU001, a refusal by the frozen plateau clause on a perfectly converged channel

**Appended by `ansys-lane-opus48` (lane H) on the supervisor's dispatch and ruling of 2026-08-27. Nothing above is edited.** This is the **first VMFLGPU (GPU-solver-path) row** in the register. VMFLGPU001 had NO verdict until today (its RESULTS/register/calibration were withheld at Amendment 4 pending a ruling on the I5 refusal); this row is that verdict.

| **33** | **VMFLGPU001** — GPU solver path on the concentric-cylinder (Taylor–Couette) Couette case (VM2026R1, **p. 225**, CPU parent VMFL001 / VMFL001-R2). Inner cylinder R_i = 0.0178 m rotating at omega = 1 rad/s, outer R_o = 0.04628 m fixed; OpenFOAM v2606 `simpleFoam` with the **petsc4Foam** GPU linear-algebra path (mat_type aijcusparse, vec_type cuda) on a **g6.xlarge** GPU instance, three-level r = 2 ladder 16x64 / 32x128 / 64x256, RANKS = 1, endTime 3000/3000/6000, run twice per level (a GPU arm and a forced-CPU control arm). The gate quantity is v_theta sampled at four radii (20/25/30/35 mm) against the **exact analytical** Taylor–Couette closed form. **Strict completion (rule 4) HOLDS at all six solves** (rc = 0 each, an End line, `Time =`-line count == endTime, fields at endTime, age guard met); the **ExecutionTime line count is endTime + 2 and is INFRASTRUCTURE** (L-342, petsc4Foam prints two init timing lines inside Time = 1 — reclassified at Amendment 4, blob f4b07b7f -> 21fa2387). Freeze proven at launch: LAUNCH_RECORD.txt records prereg and comparator shas equal on disk and at HEAD, written before any solver ran. Independent of every prior row. | 2026-08-27 | **`NOT A RESULT`** — the frozen comparator **REFUSED (exit 2)** at its **plateau clause I5**, verbatim from `GRADING_regrade.txt`: **`REFUSE (VMFLGPU001 I5): L1_16x64: the plateau window has NULL RANGE (peak-to-peak exactly 0 over 600 samples). A dead field and a perfectly converged one look identical to a tolerance (Amendment 4 item 4).`** **Diagnosis (measured by the lane on the preserved artifacts): the null range is a PHYSICS FACT, not a reader defect** — the L1 gate probe (v_theta at r = 35 mm) is a demonstrably LIVE channel: it rose from 1.86e-16 at iteration 1 to 4.639246e-03 (full-history range 4.639e-3) and then went **bit-identical for its final 1662 (GPU) / 1776 (CPU) iterations**, so the last-600 window has one distinct value and peak-to-peak exactly 0. It is a perfectly converged double-precision fixed point; L2 (ptp 1.093e-11) and L3 (3.527e-08) pass the plateau, only L1 hits I5. **SUPERVISOR RULING (2026-08-27, [lab-attributed]): the frozen clause STANDS and is NOT amended** — its liveness could be established only by reading the run's own values, so amending it now would be a change justified by the answer, moving the case toward GATE REACHED (the favourable direction), which is the bright line the lab will not cross for a good-looking case. The remedy is forward: the liveness plateau control goes into VMFLGPU001-R2 and VMFLGPU003+. | The gate quantity is v_theta on the radial line; the comparator refused before grading it, so the following is **CONTEXT computed independently by the lane, explicitly NOT this row's verdict**: v_theta at r = 35 mm (GPU arm) = 4.51458e-3 / 4.53957e-3 / 4.54578e-3 m/s, a **CONVERGING** triple (R = 0.24827, observed order p = 2.010), agreeing with the exact closed form to 0.75 % / 0.18 % / **0.045 %** at L1/L2/L3 (limb C 0.0446 % vs the 2 % band); the GPU and forced-CPU arms agree to the last digit (limb B held). **The physics looks strong — a clean second-order convergence to the exact Taylor–Couette solution — and the lab is deliberately NOT claiming it**, because the frozen instrument refused before grading it. | **EXACT analytical**: v_theta(r) = omega·R_i²(R_o²−r²)/(r(R_o²−R_i²)) (White, *Viscous Fluid Flow* §3-2.3). **REFERENCE KIND: closed-form/exact — buys V, NEVER P**; the **tier ceiling is `GATE REACHED`**, hard-coded in the comparator, which cannot print `PASS`. **Never reached.** Manual **p. 225**. | **abs(v_theta − v_exact)/abs(v_exact) ≤ 0.02** (2 %) at the finest level **AND** a `CONVERGING` triple (rule 5), plus a plateau clause (ptp < 1e-6 over a fixed 600-sample window, with the null-range refusal that fired here) and the P_MIN = 0.05 floor. Cap **2.0 GPU-h** (never fired) and 40 core-min on the CPU arm. All frozen at `0429091e` before any solver ran. **Never evaluated — the verdict is a refusal, prior to the band.** | `verification/runs/ansys_verification/VMFLGPU001/` (GRADING.txt, GRADING_regrade.txt, RUN_RC.{L1,L2,L3}.{cpu,gpu}, STATUS, COST.txt, LAUNCH_RECORD.txt, TOOLCHAIN_MANIFEST.txt, launcher.queue.out, per level log.simpleFoam / log.blockMesh / log.checkMesh / gpusample.txt / postProcessing) | **`0429091e`** (freeze — the FIRST commit that touched the pre-registration, zero compute; two pre-compute amendments `ce7f992d`/`9010f176` moved no gate/band/cap/label; Amendment 4 `59110074` reclassified C7 post-compute); prereg blob at launch **`b9bb3779`**, current **`ed697a13`** | launch **`f4b07b7f`**; re-grade **`21fa2387`** (Amendment 4, blob-verified == HEAD) | **0.441667 GPU-h** measured (total wall 1590 s; CPU arm **9.15 core-min**); **2.0 GPU-h cap, never fired**; WASTE 0.000 (a comparator refusal costs no solver time and is not a re-run) | **$0.3555** derived at the published-list **$0.8048/GPU-h** (g6.xlarge us-east-2), **not measured** — the box cannot read its own billing (`COMPUTE_BUDGET_CHARTER.md` §5); **the console figure is still owed and supersedes** | `cases/ansys_verification/VMFLGPU001/RESULTS.md`; calibration **C-165** |

**The credential-count headline is NOT struck.** Row #33's verdict is `NOT A RESULT`, so the `PASS` count does not move and stays at **6**. The register now carries **33 rows**.
