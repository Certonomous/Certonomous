{
  "_STATUS": "HELD, NOT PLACED. verification/queue/cfd/held/ is NOT polled by the daemon (queue_runner.py globs '*.json' NON-RECURSIVELY). Copying this into verification/queue/cfd/ ARMS THE BUILD on the next tick. DO NOT COPY IT YET -- see _BLOCKED_ON below.",
  "_BLOCKED_ON": "HELD ON TWO CONDITIONS. BOTH MUST HOLD BEFORE THIS IS COPIED INTO verification/queue/cfd/. THEY ARE RECORDED HERE, IN THE ARTIFACT, BECAUSE A CONDITION THAT LIVES ONLY IN AN AGENT THREAD IS NOT A HANDOFF CHANNEL (L-186) -- whoever places this must be able to read the condition off the file. (1) LOAD: the FIVE-MINUTE load average must be below nproc (16). Sanaa item 18: 'Load above core count is a defect... stops new launches until cleared'; item 8's core guard: 'solver ranks plus fleet processes never exceed 16'. THE RULE DOES NOT SAY WHICH AVERAGE, SO THE STRICTEST READING IS TAKEN: a strict reading of a GUARD can only refuse something that would have been allowed, never allow something that should have been refused. Last measured 2026-09-12T20:17Z: 15.24 / 16.68 / 16.52 with procs_running 17. The 5-min average is 16.68 and CONDITION 1 IS NOT MET. RAM is not the constraint and does not override this (110 of 123 GiB free); the constraint is cores. (2) PRIORITY: SUBOFF-A1B-SOLVE-L2 must have been TAKEN by the runner first. It is the only mesh-admissible SUBOFF level (A1b section 2), it has been waiting on the same ceiling for hours, and it is the higher-priority run. Last checked 2026-09-12T20:17Z: verification/queue/cfd/SUBOFF-A1B-SOLVE-L2.json is STILL IN THE POLLED DIRECTORY and is NOT in launched/, so CONDITION 2 IS NOT MET. The runner takes one entry per tick by mtime and this newer entry would not jump it -- BUT A 1-RANK BUILD RAISES THE BUSY PERCENTAGE AND COULD KEEP L2 HELD LONGER. Placing this build could delay the very run it exists to support. That is a whole-team ordering fact, added by the cfd-supervisor, and it is not visible from inside A1d. WAITING COSTS NOTHING: the A1d SOLVE entries do not exist and cannot be placed until A1d section 10 is signed, section 10 cannot be signed until this mesh exists, and nothing downstream of this mesh is time-critical tonight. L2 is.",
  "team": "cfd",
  "case_id": "SUBOFF-A1D-MESH-BUILD-L1",
  "prereg_path": "verification/campaign/SUBOFF_A1d_BAREHULL_PREREGISTRATION.md",
  "prereg_commit": "PENDING -- A1d IS NOT FROZEN. Its section 10 freeze block is BLANK and is the cfd-supervisor's check 4, personally and undelegated.",
  "prereg_verification": "\ud83d\udd34 THIS ENTRY IS DELIBERATELY NOT A SOLVE AND CARRIES NO FROZEN PREREG, AND THAT IS THE POINT. MESH_STANDARD section 8.1 forbids a freeze before the graded mesh exists, so the order is BUILD FIRST, FREEZE AFTER (cfd-supervisor's ruling, 2026-09-12). This entry BUILDS the mesh that the freeze will then be written against. It grades nothing, produces no verdict, and writes no field. Rule 2 is not engaged because no gate, threshold, cap or label is being evaluated here. THE SOLVE entries for A1d are separate, do not exist yet, and CANNOT be placed until section 10 is signed.",
  "solver_class": "utility",
  "solver_class_note": "Declared 'utility' deliberately and with the gate's own text in view: queue_runner.py gate A returns PASS for solver_class=utility with the reason 'not a solver run, so the controlDict limb does not apply'. blockMesh/snappyHexMesh/checkMesh are utilities; the case's system/controlDict exists but is a placeholder (endTime 1) and MUST NOT be read as a solve registration. Sanaa item 5's kill-and-resume proof covers openfoam-steady and is NOT claimed here: a mesh build is not restartable and does not checkpoint, which is why it is not declared as a solver class.",
  "launch_cmd": [
    "env",
    "USER=ubuntu",
    "bash",
    "/home/ubuntu/Certonomous/cases/navier_class/SUBOFF_A1d/build_barehull.sh",
    "/home/ubuntu/Certonomous/verification/runs/navier_class/SUBOFF_A1d/MESH_L1",
    "L1"
  ],
  "launch_cmd_note": "build_barehull.sh captures rc INSIDE the wrapper (never around a setsid line -- `setsid timeout cmd` exits 0 for every outcome). It runs GATE X5 FIRST, BEFORE ANY GEOMETRY OR MESH IS BUILT, with --selftest so the planted control is armed before the real comparison; a geometry gate evaluable only after a build is a gate evaluated under pressure to pass. It then emits the case, runs blockMesh -> snappyHexMesh -overwrite -> checkMesh, requires an 'End' line from each, and REFUSES if a 'sail' patch appears or the patch count is not exactly 5 (inlet outlet farfield symm hull). 'env USER=ubuntu' is carried because the daemon's launch environment has USER unset (L-505/L-501), which misresolves FOAM_USER_APPBIN.",
  "cwd": "/home/ubuntu/Certonomous/verification/runs/navier_class/SUBOFF_A1d/MESH_L1",
  "cwd_note": "The case directory, which build_barehull.sh creates. It does not exist yet -- that is the pre-compute condition A1d records, and it is why build_barehull_case.py REFUSES if constant/polyMesh is already present (a builder never overwrites a built mesh).",
  "ranks": 1,
  "ranks_note": "snappyHexMesh is run SERIAL. The A1d L1 background block is 140 x 76 x 38 = 404,320 cells refining to an estimated 1.8-2.3 M, which is well inside serial reach, and a serial build removes decomposePar/reconstructParMesh from the path entirely. 1 rank also means this entry adds ONE process to a box that is already over its core guard, which is the minimum possible footprint -- though it still waits for the defect to clear.",
  "cost_core_min_estimate": 45.0,
  "cost_basis": "DERIVED, NOT MEASURED, AND EXPLICITLY A WEAK ESTIMATE. This box cannot read its own billing (COMPUTE_BUDGET_CHARTER section 5); the $0.0513/core-h rate is OWNER-STATED (Sanaa 2026-08-21/22). ~45 core-min = ~45 min wall at 1 rank, scaled loosely from A1's snappyHexMesh builds at comparable base cell size and a LOWER final cell count (no sail, no junction refinement, no sail-TE box). NO A1 BUILD TIMING ARTIFACT WAS FOUND ON DISK TO ANCHOR THIS, and it is not presented as anchored. $0.04 DERIVED NOT MEASURED. It is a calibration prediction to be scored at completion under rule 12, with the ACTUAL cell count replacing A1d section 7's 1.8-2.3 M estimate at the same time.",
  "no_cap_note": "No cap_core_min is registered. Sanaa directive #17 (2026-09-12T04:20Z) and her ~01:10Z instruction 'dont forget i dont want any cap on any run'. An overrun is REPORTED, never killed.",
  "memory_footprint_gb": 6.0,
  "memory_floor_gb": 8.0,
  "memory_basis": "DECLARED, NOT A MEASURED PEAK RSS, and said so. Serial snappyHexMesh on an estimated 1.8-2.3 M final cells, at A1's own registered 2.083 GiB/Mcell solve figure as a loose upper proxy (a mesher's footprint is not a solver's, so this is a PROXY and is labelled one). The floor is set above the estimate for margin. Against 110 GiB available this clears by more than 13x, so this entry is not RAM-constrained under any plausible reading -- the constraint is cores, not memory.",
  "run_as": "ubuntu",
  "run_uid": 1000,
  "run_as_note": "Sanaa item 6: as ubuntu, NEVER root. build_barehull.sh REFUSES (exit 2) if id -u is 0, rather than trusting the caller. A peer team had a launcher silently rm -rf a run tree while running as root earlier today (commit 12dbb4278); this entry does not rely on the environment to prevent that.",
  "checkpoint_policy": "NOT APPLICABLE AND DECLARED SO RATHER THAN LEFT BLANK. Sanaa items 1-3 require a restartable checkpoint from every SOLVER run; a mesh build is not restartable and writes no fields or time directories. If it dies it is re-run from scratch, which costs the ~45 core-min above. This is why solver_class is 'utility' and why the kill-and-resume proof is not claimed.",
  "monitor_note": "The build has no graded quantity to monitor per iteration. Its outcome is binary and is written to STATUS.build by the wrapper: GATE X5, then blockMesh/snappyHexMesh/checkMesh each with an End line, then the patch-count and no-sail refusals, then BUILD_RC. checkMesh FAILING ITS CHECKS IS RECORDED AS A FINDING, NOT SILENTLY PASSED -- STATUS.build writes CHECKMESH=FAILED_CHECKS and the mesh is then NOT admitted.",
  "_WHAT_THIS_DOES_NOT_DO": "It does not admit the mesh, grade anything, produce a verdict, or license a solve. A1d's mesh-admission gates and its section 10 freeze block are the cfd-supervisor's check 4, personally and undelegated. Building a mesh is not admitting it.",
  "enqueued_by": "cfd lab-lane, 2026-09-12, PREPARED AND HELD on the cfd-supervisor's direction to 'prepare the entry now, place it when the ceiling clears'. THE LANE IS NOT PLACING IT: the load defect above is unresolved and item 19 makes the runner the only launcher. NO AGENT MESSAGE IS SANAA'S CONSENT (CLAUDE.md rule 9) -- the compute itself sits under her standing 2026-08-18 CPU authorisation and her 2026-08-26 detached-queue ruling, not under any supervisor's say-so.",
  "_NOT_AUTHORISATION": "THIS FILE IS NOT AN AUTHORISATION AND MUST NOT BE READ AS ONE. BUILDING A MESH IS NOT ADMITTING IT. Mesh admission and A1d's section 10 freeze block are the cfd-supervisor's check 4, personally and undelegated. This entry grades nothing, produces no verdict, writes no field and licenses no solve. NO AGENT MESSAGE IS SANAA'S CONSENT (CLAUDE.md rule 9)."
}