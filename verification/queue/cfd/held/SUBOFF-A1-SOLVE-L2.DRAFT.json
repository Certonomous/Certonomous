{
  "_STATUS": "DRAFT -- NOT PLACEABLE AS IT STANDS. It lives in verification/queue/cfd/held/, which the daemon does NOT poll (queue_runner.py globs d.glob('*.json') NON-RECURSIVELY at :292, :440, :700 -- subdirectories are invisible to it). Moving this file into verification/queue/cfd/ ARMS A LAUNCH on the next ~65 s tick. Do not move it until every item in _PRECONDITIONS is discharged.",
  "team": "cfd",
  "case_id": "SUBOFF-A1-SOLVE-L2",
  "prereg_path": "verification/campaign/SUBOFF_A1_PREREGISTRATION.md",
  "prereg_commit": "PENDING_CFD_SUPERVISOR_FREEZE_SEE_PREREG_SECTION_10",
  "_prereg_commit_NOTE": "DELIBERATELY NOT A SHA, AND DELIBERATELY INVALID. SUBOFF_A1_PREREGISTRATION.md is UNFROZEN at HEAD: its section 10 FREEZE BLOCK reads 'FROZEN AT COMMIT: ....................' (HEAD line 608, read with git show, not from the worktree) and every checkbox in it is empty; the opening DRAFT/UNFROZEN blockquote still stands. Section 13.3 conditions BOTH solve launches on that freeze in its own words: 'If the supervisor confirms the freeze sha, the narrow reading is adopted on the supervisor's authority ... If not, both solves are BLOCKED on Gate M and this lane reports them so.' A lane cannot supply this value: SUPERVISION_CHARTER section 3 check-4 is personal and undelegated (CLAUDE.md rule 9). The placeholder is left non-hex ON PURPOSE so that if this file is moved into the live drop path before the freeze, queue_entry_check's COMMIT-EXISTS clause REFUSES it into refused/ rather than launching it.",
  "grading_freeze": [
    "verification/runs/navier_class/SUBOFF_A1/GRADER_PINNED_8efe38e8f.py"
  ],
  "launch_cmd": [
    "env",
    "USER=ubuntu",
    "bash",
    "/home/ubuntu/Certonomous/cases/navier_class/SUBOFF_A1/solve_level.sh",
    "/home/ubuntu/Certonomous/verification/runs/navier_class/SUBOFF_A1/SOLVE_L2",
    "4",
    "19"
  ],
  "_launch_cmd_NOTE": "solve_level.sh takes exactly three positional arguments: CASE RANKS MIN_AVAIL_GIB. It captures rc INSIDE the wrapper (a setsid/timeout parent exits 0 for every outcome), runs its own memory gate and its own monitor-stop S5 guard before touching anything, and touches 0/U 0/p 0/k 0/omega 0/nut LAST before the solver so the rule-4 age guard dates the run. 'env USER=ubuntu' is carried because the daemon's launch environment has USER UNSET (L-501), which makes OpenFOAM's etc/bashrc resolve FOAM_USER_APPBIN to a user-v2606 path; SUBOFF uses STOCK simpleFoam so this is belt-and-braces here, not a dependency.",
  "cwd": "/home/ubuntu/Certonomous/verification/runs/navier_class/SUBOFF_A1",
  "_cwd_NOTE": "The PARENT of the case, not the case. queue_entry_check's AGE-GUARD refuses a cwd containing 0/ or any numeric time directory, and SOLVE_L2/ contains 0/. The parent exists, holds no 0/ and no numeric child (L0c, L1, L1_DECOMP4, L1_SHIFT, L2, SOLVE_L1, SOLVE_L2, geometry, SERIAL_DECOMP_PROBE_*), so EXEC and AGE-GUARD both pass. The driver cds into the absolute case path given in argv regardless.",
  "ranks": 4,
  "_ranks_NOTE": "4 is the case's OWN registered decomposition -- SOLVE_L2/system/decomposeParDict numberOfSubdomains 4, method scotch -- and is NOT changed here. Changing it would require re-running decomposePar against a different partition and would change the registered setup after first compute. Sanaa's item 8 core guard (ranks + fleet <= 16) is satisfied with 12 cores left for the rest of the fleet; L1's entry also declares 4, so both SUBOFF levels together occupy 8 of 16.",
  "cost_core_min_estimate": 7936.0,
  "cap_core_min_registered": 10600.0,
  "cost_basis": "DERIVED, NOT MEASURED. The box cannot read its own billing (COMPUTE_BUDGET_CHARTER.md section 5, CLAUDE.md rule 12); the $0.0513/core-h rate is OWNER-STATED (Sanaa 2026-08-21/22). THE RATE ANCHOR IS MEASURED AND IT IS THIS CASE'S OWN SIBLING: verification/runs/navier_class/SUBOFF_A1/SOLVE_L1/log.simpleFoam, ExecutionTime 5568.40 s at iteration 355 and 5639.51 s at iteration 360 => 14.22 s/iteration at 4 ranks on 3,268,613 cells, taken from a CONTENTION-FREE stretch of that log (the same log's iterations 367-368 cost 2283 s and 6259 s of ExecutionTime under starvation and are excluded, named, not averaged in). Scaled by cell count at equal ranks: 14.22 x (9,121,237 / 3,268,613) = 39.68 s/iteration => 2.645 core-min/iteration => 7,936 core-min for 3000 iterations (wall ~33.1 h at 4 ranks). Pre-registration section 13.9 registered 7,309 core-min expected and a 10,600 sub-cap; this measured-rate estimate is 1.086x the registered expectation and 0.749x the sub-cap. DERIVED USD: 7936/60 x $0.0513 = $6.78 (at the sub-cap, $9.06). Under the $25 CPU pre-authorisation, and still costed here because a blanket is not a per-item read (rule 9).",
  "memory_floor_gb": 19.0,
  "_memory_NOTE": "19 GiB is the case's OWN registered gate figure, unchanged from gated_launch_l2.sh (need_available_GiB=19) -- 2.083 GiB per Mcell x 9.121237 Mcell. IT IS A DECLARED FIGURE, NOT A MEASURED PEAK RSS: no /usr/bin/time -v artifact exists for any SUBOFF solve, because the per-phase peak-RSS instrumentation was added to solve_level.sh AFTER the live SOLVE_L1 launched (that run's STATUS.solve carries no simpleFoam_peak_rss_kB). THE RELAUNCH MEASURES IT. INDEPENDENT UPPER BOUND that does not use the 2.083 figure: SOLVE_L1/STATUS.solve records available_GiB_at_launch=11 and the L1 solver then ran 369 iterations without OOM on 3,268,613 cells, so L1's true footprint is <= 11 GiB, i.e. <= 3.366 GiB/Mcell, i.e. L2 <= 30.7 GiB worst case. AGAINST 116 GiB FREE / 120 GiB AVAILABLE (docs/RESIZE_CENSUS_2026-09-12.md, free -g at 17:44:39Z): the point figure clears by 6.1x and even the upper bound clears by 3.8x. The 19 GiB gate that held this case BLOCKED from 01:55Z to 17:25Z -- 251 consecutive GATE CLOSED readings, the last 41 of them at available=0 GiB -- opens on the first reading now.",
  "_PRECONDITIONS": [
    "1. FREEZE. cfd-supervisor performs SUPERVISION_CHARTER section 3 check-4 personally, fills section 10 of SUBOFF_A1_PREREGISTRATION.md, strikes the DRAFT blockquote in one cut as that banner instructs, commits, and writes the 40-hex sha into prereg_commit above. Section 13.3's collision (Gate M is GATE FAIL at L1 on M-d, and section 5.1 says 'no solver is launched') is decided BY that freeze and by nothing else.",
    "2. CHECKPOINT POLICY. SOLVE_L2/system/controlDict currently reads writeInterval 3000, purgeWrite 0 -- ONE write, at endTime. It must read writeInterval 25, purgeWrite 2. 25 iterations x 39.68 s = 992 s = 16.5 min per checkpoint (Sanaa item 1 ceiling: 30 min), 25 divides 3000 exactly (120 writes, so the section 13.7 reason for writeInterval==endTime -- 'a writeInterval that does not divide endTime writes NO fields at all' -- is still satisfied), and the margin tolerates the box running 1.8x slower than the anchor before the 30-min ceiling is touched. purgeWrite 2 keeps the last two, so 2975/ and 3000/ survive to endTime and Gate S-C's 'fields present in 3000/' clause is untouched. NOTE: this ALSO arms Gate S-W earlier -- the yPlus functionObject is executeControl writeTime, so y+ per patch currently cannot be written before iteration 3000, and postProcessing/yPlus/0/yPlus.dat in SOLVE_L1 is a TWO-LINE HEADER WITH NO DATA. Under writeInterval 25 the first y+ per patch lands at iteration 25.",
    "3. THE GENERATOR CANNOT PRODUCE ITEM 2 YET. cases/navier_class/SUBOFF_A1/setup_solve.py hard-codes 'writeInterval {a.end_time}' (:230) and 'purgeWrite 0' (:231) with no CLI option for either. Either add --write-interval/--purge-write to it, or edit the two lines of SOLVE_L2/system/controlDict in place. Both are changes to registered setup AFTER first compute and need a dated addendum -- neither alters a gate, a threshold, a cap or a label, so rule 2 permits the addendum form.",
    "4. RUNNER GUARDS. Sanaa item 4: the launcher refuses any case whose controlDict does not satisfy the checkpoint rules. The runner-guard lane's checkpoint/memory/core/no-root gates must be live in scripts/queue_runner.py before this is placed, or the guard she asked for is not the thing that let it through.",
    "5. RUNS AS ubuntu, NEVER root. The daemon is pid 1713, uid 1000 (census). solve_level.sh starts no container and does no privilege change. verification/runs/ carries ZERO root-owned files written today (census section a). Nothing here needs root and nothing here asks for it."
  ],
  "_WHY_L2_AND_NOT_L1_OR_L0c": "L2 is the ONLY mesh-admissible level in this family. Pre-registration 13.2: L2 passes all four Gate M limbs (nonOrtho 64.906 <= 70, skew 3.126 <= 4, min cell determinant 1.5198839e-03 >= 1.0e-03, 3 geometric directions). L1 is GATE FAIL on M-d at 8.6227045e-04, one cell in 3,268,613. L0c (14.2) is GATE FAIL on M-b-1 at 6 cells across the truncated TE base against a floor of 8, and closes its build with 123 illegal faces where L1 and L2 close clean. Its mesh is already built, so there is no build cost and no build risk.",
  "_OWED_AT_COMPLETION": "Gate S-C (eight clauses, each boolean printed), Gate S-W (y+ min/max/average PER PATCH from yPlus.dat AND independently from the solver log; PASS iff max y+ < 300), Gate S-CT (pre-committed NOT A RESULT -- no CONVERGING triple exists; CT reported beside the label with CT_ref 3.691617e-03 and the frozen +/-15% band [3.137874e-03, 4.245359e-03]), monitor stops S1/S3, and a rule-12 COST_CALIBRATION.md row with CONTENTION ON ITS OWN LINE. Nothing sent, filed or uploaded (rule 7)."
}
