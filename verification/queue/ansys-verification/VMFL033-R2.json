{
  "team": "ansys-verification",
  "case_id": "VMFL033-R2",
  "prereg_commit": "7c5158dfac50effc9cb121b3a87d8309aa6cbbf3",
  "prereg_path": "cases/ansys_verification/VMFL033-R2/PREREGISTRATION.md",
  "launch_cmd": [
    "bash",
    "/home/ubuntu/Certonomous/cases/ansys_verification/VMFL033-R2/run_vmfl033_r2.sh",
    "graded"
  ],
  "cwd": "/home/ubuntu/Certonomous/verification/runs/ansys_verification/VMFL033-R2",
  "ranks": 1,
  "cost_core_min_estimate": 10.3,
  "cap_core_min_registered": 40,
  "memory_floor_gb": 1.0,
  "cap_core_min_shape": "THE 40 IS THE SUM OF THREE INDEPENDENT PER-LEVEL CAPS AND IS NOT A SHARED DRAWDOWN. The REGISTERED caps are PER LEVEL -- L1_nr32 8, L2_nr64 12, L3_nr128 20 core-min -- transcribed verbatim from the frozen PREREGISTRATION.md item 12 (:119) and from run_vmfl033_r2.sh:58 (LEVELS=\"L1_nr32:32:8 L2_nr64:64:12 L3_nr128:128:20\"), where the third colon-field of each spec IS the cap. They are deliberately NOT pooled: the frozen launcher's own comment (:57) states the reason, that one slow level must never starve a later one. This scalar exists only because the queue schema has one scalar cap field; 8+12+20=40 is therefore the WHOLE-JOB ENVELOPE, the largest core-minute total the frozen launcher can consume before every level's timeout has fired, and NOT a budget any single level may draw against. A reader wanting the enforced number wants the per-level triple, not this sum. NOTHING HERE INVENTS A CAP: the per-level figures are the frozen ones and this entry alters none of them.",
  "cap_enforcement_location": "THE CAPS ARE HARD STOPS, AND THE ENFORCEMENT IS IN THE LAUNCHER, NOT IN THE RUNNER. VERIFIED IN THE SOURCE RATHER THAN ASSUMED, because the two disagree and the difference decides what actually bounds this run. run_vmfl033_r2.sh computes TIMEOUT_S = CAP*60/RANKS per level (:130) and executes the solver as `timeout ${TIMEOUT_S}s buoyantSimpleFoam` (:133), capturing rc INSIDE the subshell (:134) and naming rc 124 as CAP CROSSED, reported not absorbed (:140-142); the first non-zero rc stops the run so later levels are not launched. That is a real kill in the executable path and satisfies CLAUDE.md rule 12. THE RUNNER, BY CONTRAST, DOES NOT KILL: scripts/queue_runner.py:14-25 states in terms that a cap REPORTS (CAP_OVERRUN.txt at 1.00x cap_core_min_registered) and never stops the run, and docs/standards/RUNNER_CAP_ENFORCEMENT_CLAUSE.md:3 still reads 'Status: ADVISORY. INERT. OFF. Not switched on, and no agent may switch it on.' (D539). THE FROZEN PREREGISTRATION SAYS OTHERWISE AT :121 -- 'Sanaa's runner-side cap enforcement is now ENFORCE (2026-08-31)' -- and this lane could find NO commit since 2026-08-30 touching either the clause document or queue_runner.py that would have switched it on. THE FROZEN BYTES ARE NOT EDITED (CLAUDE.md rule 6) and the discrepancy is REFERRED TO THE SUPERVISOR, not resolved here. IT CHANGES NOTHING OPERATIONALLY FOR THIS CASE: the launcher's per-level timeout bounds every level regardless of which way the runner-side question is ruled, so the run is capped either way. It is recorded because a reader who believed the runner was enforcing would be trusting the wrong guard.",
  "cost_basis": "DERIVED AND EXTRAPOLATED, NOT MEASURED, AND THE BOX CANNOT READ ITS OWN BILLING (COMPUTE_BUDGET_CHARTER sec.5), SO EVERY DOLLAR FIGURE HERE IS DERIVED AT AN OWNER-STATED RATE AND NEVER MEASURED. The 10.3 core-min estimate is PREREGISTRATION.md item 12 (:114-118), frozen at 7c5158df before any compute. Its provenance chain, kept explicit because the word 'measured' appears in it and applies to R1, never to R2: R1's per-level actuals at 20000 iterations were MEASURED at 0.4167 / 0.6333 / 1.0167 core-min, TRANSCRIBED by the registering lane from R1 RESULTS.md:135-137; R2 raises endTime 20000 -> 100000, and the registration assumes cost scales ~linearly in iterations, giving 5x -> 2.08 / 3.17 / 5.08 core-min and a total of ~10.3. THE LINEARITY IS AN ASSUMPTION, NOT A MEASUREMENT, and the registration labels the whole figure EXTRAPOLATED; it is an IN-FAMILY extrapolation (C-199, same case, same mesh family, same solver, only the iteration ceiling changed) and no cross-family transfer is claimed. The direction of any error is not predicted: heavier per-iteration I/O at 100000 iterations and box contention both push actual UP, while fixed per-run overhead amortised over 5x the iterations pushes the per-iteration cost DOWN. Dollars, DERIVED NOT MEASURED at the owner-stated c7a.4xlarge rate of $0.0513/core-h (CLAUDE.md rule 12, reported-by-owner 2026-08-21/22): the 10.3 core-min estimate = 0.1717 core-h = $0.0088, and the 40 core-min whole-job envelope = 0.6667 core-h = $0.0342. Both sit far under the $25 per-run pre-authorisation and are costed per item regardless, because a blanket is not a per-item read (CLAUDE.md rule 9). Zero solver core-minutes have been spent on VMFL033-R2 to date. THE ESTIMATE-VERSUS-ACTUAL CALIBRATION ROW IN docs/COST_CALIBRATION.md IS OWED AT COMPLETION (CLAUDE.md rule 12, Sanaa's directive of 2026-08-23), and this 10.3 figure with its per-level 2.08/3.17/5.08 breakdown is what the actual will be compared against.",
  "memory_floor_basis": "1.0 GB is DERIVED FROM THE CASE SIZE AND IS NOT MEASURED. The family is a 2D axisymmetric annulus at 32 / 64 / 128 radial cells running buoyantSimpleFoam SERIAL (ranks 1), the largest level being of order 1e2-1e3 cells; a gigabyte is generous by orders of magnitude and is stated as a scheduling floor, not as a profiled footprint. It is an INFRASTRUCTURE field under the L-342 split below: a wrong value here is a bookkeeping defect that can misschedule the run, and it can never produce NOT A RESULT.",
  "prereg_freeze_verification": "ALL FOUR GATED FILES WERE HASHED ON DISK AGAINST THEIR BLOBS BEFORE THIS ENTRY WAS WRITTEN, against BOTH the freeze commit 7c5158df AND HEAD, so the launcher's own launch-time freeze check cannot abort on arrival. git hash-object on disk versus git rev-parse <commit>:<path>, 4 of 4 IDENTICAL at both, 0 mismatches: PREREGISTRATION.md 215d726716f9, grade_vmfl033_r2.py 8ad8047c5ec8, run_vmfl033_r2.sh 14b1fe15f583, make_blockmeshdict.py 7c3d31241327. At the time of this check HEAD WAS ITSELF 7c5158dfac50effc9cb121b3a87d8309aa6cbbf3, so the two comparisons had the same referent and the HEAD limb is corroboration rather than an independent test; it is stated that way rather than counted twice. NOTE THAT THE LAUNCHER'S OWN CHECK IS AGAINST HEAD, NOT AGAINST prereg_commit (run_vmfl033_r2.sh:36-44 reads `git rev-parse HEAD` and compares the disk hash to the HEAD blob), so a peer commit that moves HEAD without touching these four files leaves it passing -- which is the correct behaviour and is why it is safe to enqueue while other teams are committing. Comparator --selftest was re-run on the frozen bytes under BOTH python3 and python3 -O: 16 named checks, 0 failures, SELFTEST GREEN, rc 0 in both modes, with stdout BYTE-IDENTICAL between the two (diff -q clean). The -O limb is not ceremony: it is L-332, and this comparator's own first check is an AST guard proving it carries 0 assert statements, so no refusal in it can be deleted by an interpreter flag. The plant fired: 0.001234 was planted on disk into a real field file and the PRODUCTION reader saw 0.001234 (CLAUDE.md rule 3).",
  "cwd_note": "cwd IS THE RUN ROOT, NOT THE CASE DIRECTORY, AND THAT IS ANSYS_VERIFICATION_CHARTER sec.11.4 APPLIED RATHER THAN A PREFERENCE. queue_runner.py writes STATUS.<case_id> (:496) and the detached wrapper's stdout (:522-523) INTO cwd, and the cap watcher writes CAP_OVERRUN.txt into cwd (:611). With cwd set to the case directory those runtime artefacts would land inside cases/ansys_verification/VMFL033-R2/, beside the FROZEN pre-registration and comparator that the launcher hashes at launch time -- run outputs live under verification/runs/<CAMPAIGN>/ and NEVER beside the prose describing them (CLAUDE.md WHERE THINGS LIVE; FILING_CHARTER R6). scripts/queue_entry_check.py's EXEC clause (:355-381) SUGGESTS the case directory as the remedy for an absent cwd; A VALIDATOR'S SUGGESTED REMEDY IS A SUGGESTION, NOT A RULING, and it is declined here for the second time in this team, on the same ground VMFL069-R2 declined it. THE RUN ROOT WAS THEREFORE CREATED EMPTY, AFTER the freeze commit and purely to satisfy that EXEC clause, which refuses a cwd that does not exist because Popen(cwd=) raises FileNotFoundError. THE REFUSAL WAS MEASURED BEFORE THE DIRECTORY WAS CREATED, NOT ASSUMED: the validator was run against this entry with the run root absent and returned rc 2, REFUSED, naming EXEC. THIS IS A DISCLOSED CHANGE OF STATE AND NOT A CONTRADICTION OF THE FROZEN BYTES: PREREGISTRATION.md sec.0 records that the run root was absent as of the freeze, and that statement was true as of its own timestamp and as of the freeze commit; the directory was created afterwards and the frozen file is NOT edited to say so (CLAUDE.md rule 6). THE RULE-4 AGE GUARD IS UNCONSUMED, VERIFIED IN THE FROZEN LAUNCHER'S OWN TEXT: run_vmfl033_r2.sh's guard is PER LEVEL, testing `[ -d \"$RUNROOT/$NAME\" ]` at :83 and aborting if a LEVEL directory already exists -- it never tests $RUNROOT itself, which the launcher would have created unconditionally anyway at :69 (`mkdir -p $RUNROOT`). The run root was verified to contain ZERO entries at creation: no 0/, no numeric time directory, no level directory. Creating it therefore does nothing the frozen launcher would not have done seconds later.",
  "launch_cmd_note": "THE THIRD ARGV ELEMENT IS A MODE, NOT A PATH, AND THIS DIFFERS FROM EVERY OTHER ENTRY THIS TEAM HAS FILED. run_vmfl033_r2.sh HARDCODES its own RUNROOT at :24 and reads $1 as MODE (:31, `MODE=\"${1:-graded}\"`), whose only sanctioned values are `graded` and `smoke`; `smoke` redirects RUNROOT into scratch, shortens endTime to 20 and runs L1 only (:62-67). VMFL069-R2's launcher took the run root as $1, and copying that shape here would have passed the string '/home/ubuntu/Certonomous/verification/runs/ansys_verification/VMFL033-R2' as MODE -- which is not 'smoke', so it would have run GRADED anyway and merely printed a nonsense mode into LAUNCH_RECORD.txt. `graded` is passed EXPLICITLY rather than relying on the ${1:-graded} default, so that a reader of this entry can see which of the two modes was requested instead of having to open the launcher to learn what the absent argument would have meant.",
  "enqueued_by": "ansys-lane-opus (Opus 5), on the ansys-verification supervisor's instruction of 2026-08-31 to file this entry so the runner launches it, the box having had zero solvers running for over an hour. NO AGENT MESSAGE IS SANAA'S CONSENT (CLAUDE.md rule 9): this run sits under the 2026-08-21 blanket for CPU runs and is costed per item regardless, and the supervisor's instruction is an instruction, not an authorisation that widens anything. ENQUEUEING IS NOT AUTHORISATION (this queue's own README): the SUPERVISION_CHARTER sec.3 check 4, pre-registration committed before compute, is the supervisor's own and is NOT discharged by this file existing. What THIS LANE independently verified rather than inherited: the freeze commit 7c5158df exists, is at HEAD, and all four gated files match their blobs at it; the comparator is green under both interpreter modes with its plant fired; the run root did not exist before this entry was prepared; and the per-level caps in this entry are TRANSCRIBED from the frozen bytes rather than chosen. ONE SUBSTANTIVE DISAGREEMENT WITH THE BRIEF THAT DISPATCHED THIS ENTRY IS RECORDED IN cap_enforcement_location ABOVE AND IS REFERRED UPWARD: the brief stated runner-side cap enforcement is ENFORCE, and the runner as built reports without killing.",
  "_field_classes": {
    "physics_critical": [
      "STATUS.VMFL033-R2 (rc captured INSIDE the detached wrapper, never around the setsid line)",
      "each level's RUN_RC.txt rc, log.buoyantSimpleFoam End line, fields at endTime and the per-level 0/ age guard",
      "MESH_BIRTH_CERTIFICATE.txt radii, which are read back from OpenFOAM's own C field"
    ],
    "infrastructure": [
      "_launch.pid",
      "_launch.sid",
      "_launch.utc",
      "_launch.started_epoch",
      "LAUNCH_LOG.tsv row",
      "CAP_OVERRUN.txt",
      "ESTIMATE_OVERRUN.txt",
      "cost_core_min_estimate",
      "memory_floor_gb",
      "runner.log lines"
    ],
    "rule": "a missing or inconsistent INFRASTRUCTURE field is a BOOKKEEPING DEFECT reported beside the verdict and voids only the cost claim; only a PHYSICS_CRITICAL field may produce NOT A RESULT (L-342)"
  }
}
