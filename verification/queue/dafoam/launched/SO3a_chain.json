{
  "_NOT_ENQUEUED": "THIS FILE IS A DRAFT AND IS NOT IN THE DAEMON'S DROP PATH. It lives beside the case (CLAUDE.md rule 13: a draft another agent must read lives under the case directory it belongs to, never in scratch). It is copied to verification/queue/dafoam/SO3a_chain.json ONLY by the supervisor, and ONLY after SUPERVISION_CHARTER.md section 3 check 4 -- the pre-registration committed before compute -- is discharged PERSONALLY on the sha. THE RUNNER POLLS THE DROP DIRECTORY EVERY 60 s, SO PLACING THIS FILE THERE IS ARMING, AND ARMING IS THE SUPERVISOR'S ACT, NOT THE LANE'S. Consequently TEAM-BINDING is NOT CHECKED by this lane: --require-binding can only be satisfied by the queued copy in place, and running it in place would arm the entry. It returns rc=2 OUT OF PLACE BY DESIGN, and that refusal is the ABSENCE of a check, not a failed one. AN UNCHECKED CONDITION IS REPORTED AS UNCHECKED, NEVER AS PASSING.",
  "team": "dafoam",
  "case_id": "SO3a_chain",
  "prereg_commit": "1a06a7d6c5c750d7071a8400b54aaa1d6a0966ab",
  "prereg_path": "cases/dafoam/ladder-a/A1/curriculum_SO3a/PREREGISTRATION.md",
  "launch_cmd": [
    "bash",
    "/home/ubuntu/Certonomous/cases/dafoam/ladder-a/A1/curriculum_SO3a/so3a_chain_driver.sh",
    "MESH",
    "X-S",
    "F-S",
    "X-P",
    "F-P"
  ],
  "cwd": "/home/ubuntu/Certonomous/cases/dafoam/ladder-a/A1/curriculum_SO3a",
  "ranks": 1,
  "cost_core_min_estimate": 22.0,
  "cap_core_min_registered": 115.0,
  "cost_basis": "c7a.4xlarge at $0.0513/core-h, REPORTED-BY-OWNER, NOT MEASURED -- the box cannot read its own billing (COMPUTE_BUDGET_CHARTER.md section 5). Point 22.0 core-min, band [14.0, 60.0], ceiling 115.0 = the SUM of the five per-arm caps, asserted as that sum in the comparator's main(). Dollars DERIVED, not measured: point $0.01881, ceiling $0.09832. GPU: 0 GPU-h; this item launches no GPU instance. Both figures are far inside the $25 pre-authorisation, AND A BLANKET IS NOT A PER-ITEM READ (rule 9) -- the item is costed in its pre-registration regardless. A docs/COST_CALIBRATION.md row is OWED at completion and is presently HELD, not filed, pending the committed-state repair of scripts/append_record.py (verification's BUILD-1: the working-tree reader is 761 lines ahead of its HEAD blob, so HEAD's reconciler is blind to tool-allocated ids).",
  "memory_floor_gb": 16.0,
  "enqueued_by": "PREPARED BY the dafoam lab-lane that completed SO-3a Stage 2; NOT ENQUEUED. The freeze sha was DERIVED, not recalled: `git log --format=%H --diff-filter=A -- cases/dafoam/ladder-a/A1/curriculum_SO3a/PREREGISTRATION.md`.",
  "_schema_note": "Fields above are the validator's REQUIRED_FIELDS plus the optional cap_core_min_registered the runner's cap watch reads. Fields below are dafoam annotations the validator ignores and a human reader needs.",
  "ruling": "SANAA, 2026-08-31, verbatim at etc/sessions/2026-08-31T1513Z_sanaa_freeze_clock_and_so3_ruling.md: 'alpha multipoint (2-3 angles, weighted objective) on the verified incompressible ground, reusing SO-1/SO-2's mesh, FFD box, constraints, and adjoint unchanged; the gradient-verification rung runs first and stands alone before any optimization iteration, sized to expect an evaluation failure given D6/D6R's history.' THIS ENTRY IS THAT GRADIENT RUNG AND NOTHING ELSE. CLAUDE.md rule 9: the ruling reaches this lane through the supervisor and no agent message is Sanaa's consent; the entry records the governing text and takes no permission from the relay.",
  "stands_alone_enforced_by_construction": "The registered arm set is MESH X-S F-S X-P F-P and CONTAINS NO OPTIMISER ARM. There is no run_driver, no pyOptSparse, no IPOPT/SLSQP/SNOPT block, no max_iter and no major count anywhere in the registered program -- the parent's driver block is REMOVED from so3a_runScript.py, not merely left unreached, and the selftest's (x8) leg proves it BY AST rather than by text scan. G-NOOPT additionally REFUSES (exit 2) on any artefact carrying an optimiser history key. Section 2 limb 1: this is not a promise about behaviour, it is the ABSENCE OF THE CAPABILITY. Limb 2 (nothing stops a LATER optimisation rung starting before SO-3a has a verdict) is stated at its true strength in the frozen document: NOTHING ENFORCES IT.",
  "no_precondition": "SO-3a has NO precondition artefact and waits on nothing -- it is the rung that runs FIRST. That is why launch_cmd calls the chain driver directly rather than dafoam_wait_then_launch.sh. The absence is registered here so a reader does not look for a wait that was never owed.",
  "regime": "2D steady INCOMPRESSIBLE: DASimpleFoam, 4,032-cell NACA0012, U0 = 10.0 m/s (M ~ 0.03), nuTilda0 = 4.5e-5, rho0 = 1.0, A0 = 0.1, Spalart-Allmaras with wall functions, primalMinResTol 1.0e-8. THERE IS NO MACH NUMBER ON THIS GROUND AND NONE IS CLAIMED -- DASimpleFoam has constant density, no equation of state and no speed of sound, which is why Sanaa ruled SO-3a to ALPHA multipoint and carried Mach multipoint to SO-3b behind the D15/D16 gradient patch.",
  "multipoint": "THREE operating points differing ONLY in angle of attack: alpha = {3.13918623195176, 5.13918623195176, 7.13918623195176} degrees. The CENTRE is a QUOTATION (so2a_runScript.py:35, the tutorial's own trimmed angle at CL_target = 0.5); the +/-2 degree bracket is LANE-CHOSEN and registered as lane-chosen. WEIGHTS {1/3, 1/3, 1/3}, EQUAL -- a choice, not a default. Objective J = SUM_i w_i * CD_i over ONE shared 8-component `shape` vector behind ONE shared geometry. patchV is NOT a design variable here and the removal is registered, not silent.",
  "feasibility_evidence": "THE BRACKET IS MEASURED FEASIBLE, not assumed. feasibility_SO3a_alpha measured all three alphas CONVERGED -- minimal residuals 9.671227e-09 / 9.822716e-09 / 9.907086e-09 against tol 1e-08, stopping at 443/435/424 iterations, all below the 1000 cap -- CD monotone 0.01723938 < 0.02091051 < 0.02726805, and CL at the centre angle 0.4987652641542319 against target 0.5. Nothing is near stall. BUT the top angle sits on a measurably flatter part of the lift curve (slope 0.093435 -> 0.082606 /deg, ratio 0.8841), which is the MEASURED reason G5J proves the FD plateau PER PAIR at the bracket ends rather than inheriting it from alpha0.",
  "convergence_reader_discriminator": "ANY CONVERGENCE LIMB ON THIS FAMILY NEEDS A DISCRIMINATOR AND THIS ONE HAS ONE. OpenFOAM prints 'SIMPLE: no convergence criteria found. Calculations will run for 1000 steps.' -- MEASURED 4x in reference/REAL_SO1a_X-S_arm.log ON A RUN THAT CONVERGED, because DAFoam applies its own primalMinResTol and stops the solve. A reader keyed on that banner reports every converged solve as unconverged, which is exactly how SO-3aF recorded converged_alphas=0 when all three had converged. so3a_grade.py:read_primal_convergence counts DAFoam's REAL statement ('Minimal residual <r> satisfied the prescribed tolerance <tol>') and counts the banner SEPARATELY under the key `banner_not_evidence`. IT IS REPORTED, NEVER GATED. Likewise C5 scans LINE BY LINE with three named benign exclusions -- the trapFpe ENABLEMENT banner (a SAFETY NOTICE; the crash discriminator is the verb, and Foam::sigFpe::sigHandler is in FATAL_TOKENS), the SIMPLE banner, and 'Time step continuity errors' (MEASURED 5x per converged log; the word `errors` there names a RESIDUAL). All three counts were VERIFIED against the real log, not against a docstring.",
  "gates": "G1 (five rule-4 clauses printed individually per arm, arm-kind aware, R-RC aware, C5 refusing regardless of rc, the age datum resolved BY EXISTENCE over 0/U and 0/U.gz), G-M2 cells == 4032, G-ALPHA (each scenario's alpha read back from the artefact's own identity record, equal to the registered value to 1e-12 absolute, else REFUSE), G5J (the bright line, on J, per row, plateau proved PER PAIR at 10.0 %, band D 5.0 % per pair, sign flip is GATE FAIL whatever the magnitude, band E 5.0 % aggregate, fewer than 3 graded pairs -> NOT A RESULT), G5C (the same on each scenario's dCL_i/dx), G-MP-STRUCT (the assembly identity |J_adj[J,k] - SUM_i w_i J_adj[CD_i,k]| / |J_adj[J,k]| <= 1e-10 -- the gate only a MULTIPOINT rung can buy), G-TB (the trivial baseline at the deliberately wrong h = 1e-8; PASS iff AT MOST 1 of 4 passes band D there, else that row's G5J verdict is WITHDRAWN to NOT A RESULT), G-NOOPT, G-STAGES (DECLARED=5 vs EXECUTED, the shortfall read AS A GATE INPUT), G-EVALFAIL (a failed evaluation and a chain STOP are BOTH GRADABLE STATES; the arm loop is a CENSUS and REFUSES ONLY on a MALFORMED artefact, NEVER on an ABSENT one -- the D6-GRADER-DEF-1 repair), G-PROV, G9, G10, G11, G12.",
  "travelling_gate_fail": "G-PROV, ENFORCED IN CODE, NOT PROSE. SO-3a rests on SO-1a's PATCHED row while SO-1a's ITEM verdict is GATE FAIL. The comparator REFUSES to emit any verdict unless a structured upstream_provenance block is present, its SHIPPED row reads exactly `GATE FAIL`, it carries the SHIPPED per-gate detail, a verdict_line contains the literal bytes `GATE FAIL`, and that line is BYTE-IDENTICAL to what the module composes (clause 5 exists because this lab lost the words GATE FAIL to an unquoted heredoc on 2026-08-30, L-405). `verdict` itself stays one of the six tokens and is never decorated.",
  "predicted_outcome_registered_before_any_container": "P1-P5, P7, P9, P-COST HIT; P6 HIT; P8 HIT -> SHIPPED row GATE FAIL, PATCHED row PASS, ITEM VERDICT GATE FAIL. A PREDICTED GATE FAIL IS REGISTERED, NOT AVOIDED. P-EVAL (section 6) predicts at least one of the 34 declared evaluations per F arm fails or returns non-finite, because a multipoint evaluation survives only if ALL THREE primals converge, so a per-primal failure probability p becomes ~3p per evaluation. A MISS is a genuinely useful reading -- it would say the alpha bracket is inside the primal's robust envelope, which is what a later optimisation rung must know.",
  "controls_driven": "250 python-side legs and 71 G-ROOT.5 legs, 0 failures, 0 NOT EXERCISED, all rc captured DIRECTLY and never through a pipe. so3a_grade.py --selftest: 92 units / 0 failures under python3 AND python3 -O with __pycache__ cleared before each run. so3a_xf_selftest.py: 33 checks / 0 fail under both. so3a_groot5_selftest.sh: 71 legs / 0 fail, including the container and live-pid MUST-REFUSE directions, the stale-pidfile MUST-NOT-BLOCK discrimination, and the cross-file agreement legs (x1)-(x14) each PROVED ABLE TO FAIL on a planted copy. ast.Assert = 0 in all five python files, with the SAME counter shown reading 1 on a planted assert (L-332). Every registered constant was mutated ONE AT A TIME in a byte-identical copy: 17 of 17 CAUGHT.",
  "md5_pins": "13 of 13 pins DECLARED in so3a_chain_driver.sh are DRIVEN and EQUAL the files they pin ((x12)+(x13)); (x14) re-counts the declared side out of the driver's own bytes so a pin added and not driven turns it RED, and (x14b) proves that count can move. so3a_run_arm.sh declares and drives 3 more at every launch. The fail-closed sentinel MD5_UNSET is DELETED from both files -- its value was a well-formed md5 and would itself have been counted as a pin. SO-1c's leg drove 4 of 12 under a label reading 'EVERY md5 PIN IN THE DRIVER'; a label is not a count, and a stale pin aborts this chain rc=4 BEFORE any container.",
  "rule_14_repair": "A FOURTH CONSUMER of the arm->row mapping was found and repaired BEFORE the pins were set: so3a_chain_driver.sh:img_of derived the row from an ARM-NAME SUFFIX GLOB (`MESH|*-S`), the exact derivation the launcher's own repair comment names as forbidden. It survived because the comparator's row-label sweep carried a suffix rule in its PYTHON rule set and NONE in its SHELL rule set, so eight files were swept and the one shell instance was structurally invisible. MEASURED: img_of Q-S returned the SHIPPED image, and the driver takes its arms from the command line. It could NOT reach a wrong verdict -- the launcher independently re-derives ROW from the IMAGE DIGEST and then refuses because row_of Q-S is empty (exit 64) -- so this was a latent second line, not a live hole, and it is recorded at that size. Repaired to a registered full-name table with DISJOINT label sets; the shell rule added (its known positive is the REAL file); U101b proves the rule fires on a glob and stays SILENT on the registered full-name table; (x11)/(x11b)/(x11c) assert all THREE call sites agree, that the driver too refuses an undeclared arm, and that the comparison goes red on a plant.",
  "guards_before_launch": "G-ROOT.1 (BASE must normalise to THIS item's registered root), G-ROOT.2 (a FORBIDDEN_ROOTS list read AGAINST THE DISK rather than inherited -- two entries in the parent's list name roots that do not exist while four that do, including SO-2a's and SO-1c's live roots, were unprotected; all four were driven and each returned rc=3 at G-ROOT.1, so the repair does not close a live hole and says so), G-ROOT.3 (a foreign ITEM= line in this root's ledger REFUSES, with the same ledger carrying ITEM=SO3a ACCEPTED as the driven control), G-ROOT.5 (A LIVE ARM IS NEVER RE-STAGED: a running so3a_ container or a live driver pid with cwd in the run root -> exit 3; a STALE pidfile does NOT block). Every guard sits ABOVE the single `sudo -n rm -rf \"$WORK\"` line, and the selftest never drives arm MESH on the SHIPPED image because MESH is the one arm whose branch reaches it.",
  "resource_guards": "SECTION 5, THE QUANTITY-TO-FORM MATCH. H5 (windowed MemAvailable floor 16.0 GiB = 12 g cap + 4 GiB headroom, 45 samples over 60 s) is a TRANSIENT quantity and takes the WAIT-AND-RE-POLL form, poll 30 s, bounded H5_BOUND_S = 3600, and AT THE BOUND THE CHAIN STOPS with rc=7 / chain=BLOCKED_H5. AGGREGATE (live sibling caps + this arm's cap + host NON-CONTAINER RSS < 30.6 GiB) is STABLE, same wait form, bound 14400, expiry rc=6. SO-3a REGISTERS NO BLOCK-AND-CONTINUE ANYWHERE: every guard STOPS the chain with a non-zero rc, so no discard fraction compounds. W3 put the WAIT on the STABLE quantity and a ONE-SHOT BLOCK on the TRANSIENT one and lost 20 of 33 declared stages.",
  "cpuset": "14, ONE core, every arm at np = 1, NOT core 0. DISCLOSED against every live sibling's registered set, read rather than recalled: SO-1c holds 10 and 10-13; SO-2a held 9. 14 is disjoint from both. At np = 1 the delivered-cores floor does not apply and is NOT composed into any verdict; the sampler's reading is reported as a number. G12 compares this against so3a_grade.py's CPUSET_REGISTERED so the two cannot drift silently, and (x1) drives that comparison with a known positive.",
  "deadline_in_container_s": "MESH 300 s; X-S and X-P 900 s; F-S and F-P 2400 s -- each equal to cap x 60 / ranks, asserted by the launcher against the registered cap to 0.02 core-min, aborting otherwise. The deadline is INSIDE the container under `timeout -k 60`, so it survives every host shell; the host poller reports a crossing and hard-stops at 4x cap while it lives. AN OVERRUN STOPS THE RUN; IT DOES NOT GET A NEW BUDGET.",
  "age_guard": "THE AGE-CHECKED LIST CONTAINS ONLY WHAT THIS RUN PRODUCES: so3a_X.json / so3a_F.json (and their .jsonl twins), plus checkMesh.log on MESH. THE DATUM IS RESOLVED BY EXISTENCE, NEVER BY NAME: `writeCompression on` rewrites 0/U as 0/U.gz on a serial arm, and AV-1 and AV-2 both returned NOT A RESULT on 2026-08-27 with every physics artefact intact ONLY because their frozen graders pinned a vanished path. Both names are candidates, the one FOUND is recorded in .so3a_age_datum_ref, and refusal comes only when NEITHER exists.",
  "two_row_rule": "DAFOAM_CHARTER.md section 6: shipped and patched are always TWO ROWS, and toolchain identity is an image ID and a library hash, never a version string. SHIPPED dafoam/opt-packages:latest sha256:9d45679d...f07fc, libidwarp.so md5 f0fcb488e0e98156575cd19548e91663. PATCHED dafoam-idwarp-rot:v1 sha256:2927768a...30f6d35, md5 85f59e87253e0a71a813f64ca6e4c425. G9 requires the ledger DIGEST, the container's printed md5 and the artefact's in-process md5 all to name the row's registered toolchain.",
  "np_is_a_statement_about_np": "DAFOAM_CHARTER.md section 5: a gradient verified at one np is a statement about THAT np and is never carried to another. np = 1 on every arm here, so SO-3's optimisation rung must either run at np = 1 or buy its own multipoint gradient verification at its np. Registered now, not discovered later.",
  "what_it_will_not_establish": "Nothing about an OPTIMUM -- no optimiser runs and the 'optimization converged' column is untouched. Nothing at np != 1. Nothing about MACH at all: no number from this item may be quoted as a compressibility result; that is SO-3b's ground and SO-3b is gated behind the D15/D16 gradient patch. Nothing about alpha outside [3.139, 7.139] and nothing about stall. Nothing about the four unregistered shape components or about patchV. Nothing about weights beyond the equal set registered -- a different weighting is a DIFFERENT OBJECTIVE and needs its own row, and G-WT registers why the shuffled-weight probe is NOT bought (J is linear in w on BOTH sides, so it returns either agreement whatever the adjoint does, or a cancellation artefact). No dot-product test and no complex step -- AV-2 MEASURED that seeding forward mode makes the primal FAIL on this exact case on BOTH images. NO GRID FAMILY, NO GCI.",
  "capability_grid_cell": "2D . steady . incompressible -- the 'gradients FD-verified' column gains an FD-VERIFIED MULTIPOINT OBJECTIVE GRADIENT it does not have. WHAT IS NOT NEW, SAID FIRST: SO-1a verified single-point dCD/dx and dCL/dx at one alpha, and SO-2a verified the geometric constraint Jacobians. NEITHER IS A STATEMENT ABOUT A MULTIPOINT ASSEMBLY, which is exactly where a multipoint gradient can be wrong while every single-point gradient is right.",
  "permission_record": "Sanaa's verbatim permission for detached launches boarded at bc0e687e. NOTHING in this item is filed, sent, emailed, uploaded, registered, posted or commented outside this box, now or on completion; SENDING IS SANAA'S DECISION ALONE (CLAUDE.md rule 7; DAFOAM_CHARTER.md section 10). SUBMISSIONS PARKED.",
  "verdict_state": "PENDING",
  "status_files": [
    "/home/ubuntu/certonomous-runs/CURRICULUM-SO3a-a1-naca0012-alpha-multipoint-gradient/STATUS.chain"
  ],
  "run_root": "/home/ubuntu/certonomous-runs/CURRICULUM-SO3a-a1-naca0012-alpha-multipoint-gradient -- asserted ABSENT by `test -e` at 2026-08-31T18:32:25Z, WITH A PLANTED CONTROL in the same invocation (SO-1a's root read TRUE, and `find -iname '*SO2a*'` returned 5 hits against 0 for SO3a), so the absence is a reading and not a blind reader.",
  "_launch": {
    "utc": "2026-08-31T18:40:53Z",
    "pid": 275297,
    "sid": 275297,
    "status_file": "/home/ubuntu/Certonomous/cases/dafoam/ladder-a/A1/curriculum_SO3a/STATUS.SO3a_chain",
    "wrapper_out": "/home/ubuntu/Certonomous/cases/dafoam/ladder-a/A1/curriculum_SO3a/launcher.queue.out",
    "started_epoch": 1788201653.577907
  },
  "_field_classes": {
    "physics_critical": [
      "STATUS.SO3a_chain (rc inside the detached wrapper)",
      "the case's own log End line, endTime fields and 0/ age guard"
    ],
    "infrastructure": [
      "_launch.pid",
      "_launch.sid",
      "_launch.utc",
      "_launch.started_epoch",
      "LAUNCH_LOG.tsv row",
      "CAP_OVERRUN.txt",
      "ESTIMATE_OVERRUN.txt",
      "cost_core_min_estimate",
      "memory_floor_gb",
      "runner.log lines"
    ],
    "rule": "a missing or inconsistent INFRASTRUCTURE field is a BOOKKEEPING DEFECT reported beside the verdict and voids only the cost claim; only a PHYSICS_CRITICAL field may produce NOT A RESULT (L-342)"
  }
}
