# held/ — entries the runner MUST NOT see

`scripts/queue_runner.py` enumerates only top-level `*.json` in the team directory
(`list_entries`, `d.glob("*.json")`), so this subdirectory is outside its drop path.

**Supervisor ruling 2026-08-26 `[lab-attributed]`:** no dafoam entry goes to the drop path
until its launcher carries G-ROOT.5 (refuse if a running container carries the item
prefix+arm, or a live driver pid holds the run root) AND the entry names a precondition
artifact that must exist on disk. Three entries were launched on drop by the daemon
before that ruling (runner.log 16:10:17Z, 16:11:22Z, 16:17:27Z; each aborted rc=1 on its
absent `step_plan*.json`, zero stages) and are held here with the runner's own
`_launch` record intact: `D12R_phase3.json`, `D12R_phase4.json`, `W2R_phase2.json`.

Held is not cancelled. An entry leaves this directory only by a supervisor's dispatch,
after the precondition artifact exists.

**Move rule (supervisor, 2026-08-26):** every entry here names a `precondition_artifact`; it moves to the drop path the moment the runner evaluates preconditions (cfd's repair of `scripts/queue_runner.py`), not before.

**SUPERSEDED (lane Q2, 2026-08-26 ~17:15Z, `[lab-attributed]`, permission `bc0e687e`):** the runner still evaluates no precondition, so the route taken was a launch argv that evaluates its own — `cases/dafoam/_common/dafoam_wait_then_launch.sh` (W2R `ADDENDUM 2`, commit `331d1a2d`). `W2R_phase2.json` here is SUPERSEDED by the drop-path entry `W2R_phase2_wait.json` (launched by the runner 17:15:37Z, now in `launched/`), with `W2R_phase3_wait.json` and `W2R_phase4_wait.json` beside it and the comparator plan steps `W2R_plan*_wait.json` (`ADDENDUM 3`). The three files here stay as the historical record of the 16:10–16:17Z premature fires (their `_launch` blocks are the runner's own). **`D12R_phase3.json` and `D12R_phase4.json` were NOT re-filed:** `cases/dafoam/curriculum_D12R` (`f9c8b9c8`) is the item the supervisor's own ruling re-registered as D12R2 (`e6580910`; `docs/COST_CALIBRATION.md` C-107 — phase 1 `NOT A RESULT`, 63.95 core-min booked as waste, its frozen comparator `d12x_grade.py` refuses that run at stage 1 of 32), so its `step_plan*.json` can never be written by the registered grading path and the entries could only ever BLOCK at their bound. The live D12 line's later phases are W2R's, and those are what was filed. Reported to the supervisor for a ruling; held is not cancelled.

**SUPERSEDED (lane Q-A, 2026-08-26 ~20:59Z, `[lab-attributed]`, permission `bc0e687e`):** `D6_chain.json` here (prereg `a1283284`, 1,694.7 core-min, 20g per arm, floor 24.0, withdrawn 17:45:47Z on D5's G-ROOT.3 refusal) is SUPERSEDED by the drop-path entry **`D6_chain_wait.json`** — the same `d6_chain_driver.sh O_mp ACC_mp F_mp REF_off` argv behind `cases/dafoam/_common/dafoam_wait_then_launch.sh`, precondition `<D5 run root>/CHAIN_DONE` (D5 Addendum 3), bound 86,400 s, prefix `d6_` — registered by D6 ADDENDUM 2, because 20g cannot co-run with D5 (12g) + D4-SHIPPED (12g) under the 30.6 GiB aggregate rule and a bare entry would BLOCK at its 4 h bound before D5 ends. This file stays as the record; held is not cancelled.

**SUPERSEDED BY THE W3 PATH (lane V2, 2026-08-26 ~22:20Z, `[lab-attributed]`, permission `bc0e687e`; record `cases/dafoam/curriculum_D12R2/RESULTS_W2R.md` §7):** W2R phase 1 is GRADED — `G12R-4` `NOT A RESULT`, `step_plan.json` `admissible: false` (`h_min 1.575533e-01` > `h_max 5.000e-02`), phase 2 closed on its registered no-launch branch 17:46:38Z (`W2R_phase2_wait` `rc=0`), phases 3/4 and plan2/plan3 registered to close `BLOCKED` (`rc=6`) at their bounds (2026-08-27T17:16:42Z / 2026-08-28T05:17:47Z / 2026-08-27T17:29:42Z / 2026-08-28T05:30:47Z), 0 core-min. Therefore `W2R_phase2.json`, `D12R_phase3.json` and `D12R_phase4.json` here are **SUPERSEDED BY THE W3 PATH**: the 2D·unsteady FD line continues, if at all, from `cases/dafoam/curriculum_D12R2/W3_PREREGISTRATION_DRAFT.md` (on disk, NOT FROZEN, not at HEAD — nothing may be launched from it) with its own entries. None of the three is a launch candidate; nothing is deleted; held is not cancelled.

**SUPERSEDED (lane N2, 2026-08-26 ~22:55Z, `[lab-attributed]`, permission `bc0e687e`):** (i) `D6_chain_wait.e43bdf61.json` here is lane Q-A's Addendum-2 entry, SUPERSEDED by the drop-path `D6_chain_wait.json` re-filed against **D6 ADDENDUM 3** (`0aa9a82a`): one EXIT-trap line in `d6_chain_driver.sh` (md5 `63458a97…` → `b4ddca65…`) appends `chain_done …` to `<D6 run root>/CHAIN_DONE` on every exit of a started chain — the same argv, precondition, bound, cost and floor. (ii) `D8R_chain.357a2648.json` here is the first-fit D8R entry, SUPERSEDED by the drop-path **`D8R_chain_wait.json`** (`dafoam_wait_then_launch.sh`, precondition `<D6 run root>/CHAIN_DONE`, bound 129,600 s, prefix `d8r_`, `prereg_commit` = D8R ADDENDUM 1 `c933740d`, which corrects `D8R-DRIVER-DEF-1` — the frozen driver copied `d8r_xf.py` for the instrument `d8r_of.py` and would have aborted at staging, exit 4, zero compute) under the supervisor's placement ruling: beside D5 (12g) a first-fit D8R O arm (14g) waits ≤ 4 h at its own aggregate guard and BLOCKs at zero compute, and D6 (20g) then excludes it again. Neither held file was ever launched (no `_launch` block); nothing is deleted; held is not cancelled.

**WITHDRAWN, NOT SUPERSEDED (lane B, 2026-08-27T16:46Z, `[lab-attributed]`, permission `bc0e687e`):** `W3_chain_r2.a6dcc964.json` is the W3 re-file against W3 AMENDMENT 1 (`a6dcc964`, which repairs `W3-LAUNCHER-DEF-1` and `W3-SELFTEST-DEF-1` and moves no gate, threshold, cap, band or label). It was committed to the drop path at `562e5146` and **withdrawn 3 minutes later, before the runner fired it**, on the supervisor's sequencing order: verification's L-342 audit (`docs/L342_GRADER_AUDIT.md`, `8fed44ed`) found the frozen comparator `d12y_grade_w3.py` conflates INFRASTRUCTURE and PHYSICS-CRITICAL fields in its refusals, contrary to Sanaa's universal rule L-342 (`d4d0c29d`). That comparator **has not fired**, so it is freely amendable pre-compute at zero cost — **and the window shuts the instant W3 launches.** Withdrawing costs nothing and keeps it open. **Never launched:** no `_launch` block, `verification/queue/runner.log` carries no `W3_chain_r2` line, `test -e /home/ubuntu/certonomous-runs/CURRICULUM-D12R2W3-cylinder-unsteady` is FALSE at 16:46:16Z, and no `d12y_w3_` container has ever existed. W3 has spent **0 core-min**. The successor is `W3_chain_r3.json`, filed against the comparator-amendment commit once the grader is repaired, `MD5_GRADER` re-pinned in `d12y_w3_chain_driver.sh:32` (asserted at `:38`; leaving it stale is the `D8R-DRIVER-DEF-1` death) and both selftests re-driven under `python3` and `python3 -O`. Held is not cancelled; this one is not superseded either — it is parked mid-flight.

**WITHDRAWAL RESOLVED (lane B, 2026-08-27T17:14Z, `[lab-attributed]`, permission `bc0e687e`):** `W3_chain_r2.a6dcc964.json` above is now SUPERSEDED by the drop-path entry **`W3_chain_r3.json`**, filed against **W3 AMENDMENT 2** (`af44d244`) — ruling `R-RC` applied IN FULL as **Sanaa approved it** on 2026-08-27 (her standing directives §0), plus the `L-342` field-class split. **The withdrawal did its job:** the comparator `d12y_grade_w3.py` was amended pre-compute at ZERO cost (`f3c1252c…` → `3b0a7507…`), which would have been impossible the moment W3 launched — after that only §2d.1's exception remains, and this family has refused that four times. Authority for the amendment is a **standing rule enforced**, never an exception granted. Every registered field is unchanged; only `prereg_commit` moves. Held is not cancelled.

**CAP FIELD TRANSCRIBED ONTO TWO FACES (lane K, 2026-08-27T22:2xZ, `[lab-attributed]`, permission `bc0e687e`):** under the chief's lab-wide cap-field order, `cap_core_min_registered: 2230.0` was ADDED to `D6_chain.json` and `D6_chain_wait.e43bdf61.json`, with `cap_core_min_registered_source` naming `cases/dafoam/ladder-a/A2/curriculum_D6/PREREGISTRATION.md:93-99` (§4 total row, `ceiling 2,230.0` = O_mp 2000.0 + ACC_mp 30.0 + F_mp 180.0 + REF_off 20.0; gate G10 at `:84` reads the same figure) and a `cap_annotation_note`. **The addendum trail was followed to the foot of that registration:** Addenda 1 (`:220`), 2 (`:243`) and 3 (`:293`) are all PRE-COMPUTE and none moves a cap — Addendum 3's instrument table at `:282` records `every gate, band, cap, label, cost, prediction … unchanged`. **This is a transcription of an already-registered figure onto an entry's face, not an amendment**: the frozen registration is untouched, no value was derived, rounded or corrected, and a before/after parse of each entry asserts the added keys are the ONLY difference. `D6_chain_wait.e43bdf61.json` was edited textually so its diff against HEAD is 3 insertions / 0 deletions. Both files are SUPERSEDED records outside `list_entries`' glob, so the edit is operationally inert; the CURRENT governing record `launched/D6_chain_wait.json` is **NOT back-filled** and still carries no cap field, on the chief's order that launched records are not touched.

**THREE ENTRIES HERE WERE DELIBERATELY LEFT UNCHANGED, AND WHY (lane K, same sweep):** `D12R_phase3.json` (600.0), `D12R_phase4.json` (350.0) and `W2R_phase2.json` (600.0) carry their cap under the NON-CANONICAL name `cost_core_min_cap_registered`. They are CAPPED — a census keyed only on `cap_core_min_registered` counts them as missing and is wrong. The canonical name was **NOT** added beside the variant, because it would not be a transcription: `scripts/queue_runner.py:607-618` consumes `cap_core_min_registered` as a PER-LAUNCH wall (`allowed_wall = cap x 60 / ranks`), while each of these three carries a `cost_core_min_cap_scope` field saying the figure is CUMULATIVE ACROSS ALL PHASES — `D12R_phase3.json`'s reads, verbatim, *"This is NOT a per-entry cap and must not be re-read as one."* Writing the number under the canonical name would re-read it as exactly that, and would loosen the runner's flag from `ESTIMATE_OVERRUN` at 1.10 x 13.0 core-min to `CAP_OVERRUN` at 600.0 — a ~46x weaker guard. That is a value-bearing change, so it is referred to the supervisor and nothing was written. `cost_core_min_cap_scope` is likewise NOT renamed: renaming is a value-bearing edit to an archived record.

**`held/D6_chain.json` IS NOT AT HEAD (lane K, measured 2026-08-27 by `git cat-file -e HEAD:<path>` and `git log --all --diff-filter=A`, both negative).** The supersession note above says this file "stays as the record", but it has never been tracked in any branch — the record exists only in the working tree. Lane K did not land it (landing a peer's never-committed file is not a lane's call) and refers it for dispatch.

**THE TWO DROP-PATH ENTRIES NAMED ABOVE ARE DELETED FROM HEAD — THE SUPERSESSION RECORD NOW RESOLVES AT `launched/`, AND THE RENAME THAT WOULD HAVE KEPT IT RESOLVING AT THE OLD PATH WAS TRIED AND IS NOT REPRESENTABLE (dafoam `lab-lane`, 2026-08-31, `[lab-attributed]`, permission `bc0e687e`).** This paragraph is APPENDED; **no line above it is rewritten** (`CLAUDE.md` rule 6) — the supersession sentences at `:21` and `:25` stand exactly as their authors wrote them, and this note records what changed underneath them.

**What was deleted, and under what authority.** On 2026-08-31 this team deleted four dafoam queue fossils from HEAD under the committed authorisation `verification/credibility/QUEUE_FOSSIL_DELETION_LIST.md` §2 rows 4–7 (landed at `88530a3b`, verified an ancestor of the deleting HEAD and byte-identical to its committed blob at deletion time), which is itself the confirmation Sanaa's ruling of 2026-08-31 required — her answer 6 that day, verbatim: *"Yes the 17 verified safe path onlys"* (`etc/sessions/2026-08-31T1615Z_sanaa_six_answers.md`). **Seventeen explicit paths, never the class:** the same fossil predicate returned **135** at the authorisation clock, and 117 of those have a `launched/` counterpart that has never been committed, so a class deletion would strand the last tracked trace of 117 launch records. The four deleted here were named by absolute path, never by `case_id` — and `case_id D6_chain_wait` is precisely the specimen that forces that rule, because it names three distinct objects, one of which is `held/D6_chain_wait.e43bdf61.json` **in this directory**, ruled FOSSIL-RETAIN and untouched by that commit (verified present on disk and at HEAD after it).

| deleted from HEAD | surviving record, at HEAD | non-runner-key differences |
|---|---|---|
| `verification/queue/dafoam/D6_chain_wait.json` (7,591 B) | `verification/queue/dafoam/launched/D6_chain_wait.json` (7,841 B) | **0** |
| `verification/queue/dafoam/D8R_chain_wait.json` (11,237 B) | `verification/queue/dafoam/launched/D8R_chain_wait.json` (11,136 B) | **0** |
| `verification/queue/dafoam/D17_chain.json` (7,153 B) — not cited by this file | `verification/queue/dafoam/launched/D17_chain.json` (7,186 B) | **0** |
| `verification/queue/dafoam/D4S_F3S_chain.json` (4,957 B) — not cited by this file | `verification/queue/dafoam/launched/D4S_F3S_chain.json` (5,000 B) | **0** |

Sizes are **HEAD-blob** sizes, not disk sizes, measured in the same invocation as the deletion decision. Differences are confined to the runner-written keys `_launch` and `_field_classes`; **every other key is identical across the pair, present-on-both or absent-on-both alike** — `prereg_commit`, `prereg_path`, `cost_core_min_estimate`, `ranks`, `permission`, the launch argv, the precondition and the bound are present and verbatim on all four. Two absences are stated rather than glossed, because a reader could otherwise take the table as a promise the records do not make: **the D6 pair carries NO `cap_core_min_registered` on either face** — the un-back-filled state `:31` above already records, and the D8R, D17 and D4S pairs each do carry one — and **the D8R, D17 and D4S pairs carry NO `grading_path` on either face**, where the D6 pair does. Absent-on-both is a preserved field, not a lost one, and it is not the same thing as present-and-equal. **Planted controls, both directions, in the same invocation as the measurement** (`CLAUDE.md` rule 3): the HEAD-blob reader returned the full contents of a known-tracked file and `None` for a fabricated path; the disk reader saw this README and did not see a fabricated sibling; the difference comparator was shown to report a planted non-runner difference and to report none for a planted runner-key-only difference. **So the four zeros above are plant-controlled zeros, not a blind reader.**

**THE RENAME WAS THE PREFERRED CLOSURE AND IT IS UNAVAILABLE — MEASURED, NOT ASSUMED.** `R-QCOMMIT.9` offers *"the launched record lands as a RENAME of the committed root entry — git's `R100`, same blob, new path"*, which would have kept `:21` and `:25` resolving at the old path through git's rename record. It cannot apply here, for two independent reasons, both measured: **(i)** the four `launched/` counterparts are **already at HEAD** and unchanged, so the deleting commit contains no added path for a deletion to pair with — a trial tree built in a private index and diffed with maximal detection (`-M5% -C5% --find-copies-harder`) reports **four pure deletions and zero renames**; and **(ii)** no pair is byte-identical (the runner wrote `_launch` into the `launched/` copy), so `R100` is unreachable even in principle. The fallback in the same section — a dated addendum — is therefore what is landed, here and in the frozen registration below.

**WHAT THIS DOES AND DOES NOT CHANGE.** The supersessions at `:21` and `:25` **happened** and their record stands: `D6_chain.json` and `D6_chain_wait.e43bdf61.json` were superseded by the D6 wait-wrapper entry, and `D8R_chain.357a2648.json` by the D8R one. Only the *address* of the superseding entry moves. **Read `:21` and `:25` as naming `verification/queue/dafoam/launched/D6_chain_wait.json` and `verification/queue/dafoam/launched/D8R_chain_wait.json`** — which is where `:19`, `:25`(i) and `:31` already point, and which is the CURRENT governing record those lines call it. Nothing else in this directory is touched: `D12R_phase3.json`, `D12R_phase4.json`, `W2R_phase2.json`, `D6_chain.json`, `D6_chain_wait.e43bdf61.json`, `D8R_chain.357a2648.json` and `W3_chain_r2.a6dcc964.json` all remain. **No gate, threshold, cap, band, label, cost or prediction moves; nothing is sent** (rule 7). Zero core-minutes: the deletion and its verification are read-only against HEAD blobs and the working tree.

**AND `:35` IS NOW STALE, IN THE GOOD DIRECTION — MEASURED, NOT REPEATED.** Line `:35` records lane K's 2026-08-27 finding that `held/D6_chain.json` *"IS NOT AT HEAD"* and had never been tracked on any branch. **Re-measured 2026-08-31: it IS at HEAD** (`git cat-file -e` positive, HEAD blob 5,987 B, byte-identical to the working-tree copy), landed since by `09fdff24` with the disclosure lane K's referral asked for. This lane drafted the opposite sentence from `:35` and it did not survive its own check; the check is why this paragraph says the reverse. `:35` is not rewritten — it was true when written, and this is its dated correction.

**THREE OF THE FOUR ENTRIES HERE ARE NOW RETIRED AS SPENT — AND `refused/` WAS DELIBERATELY NOT USED (dafoam `lab-lane`, 2026-09-10T05:22:41Z, `[lab-attributed]`, on the dafoam-supervisor's ruling, board `S-147l`).** This paragraph is APPENDED; **no line above it is rewritten** (`CLAUDE.md` rule 6) — every prior author's supersession sentence stands exactly as written, and this note records the terminal state added underneath them.

**What was added, and to what.** `D8R_chain.357a2648.json`, `D6_chain_wait.e43bdf61.json` and `D6_chain.json` each gained seven keys — `retired`, `retired_utc`, `retired_by`, `retirement_reason`, `retirement_evidence`, `retirement_mechanism_note`, `retirement_releasable` — inserted **textually, immediately before each row's own `"hold_reason"` line**, so each diff is **pure insertion with ZERO deletions** (12/0, 13/0 and 11/0 lines respectively) and **no pre-existing key changed by a single byte** (asserted twice: original lines are an in-order subsequence of the result, and a re-parse from disk compares every pre-existing key equal). `D12R_phase4.json` was **not** retired; it stays held, and its own note is corrected separately.

**WHY NOT `refused/`.** `scripts/queue_runner.py:73` reserves that directory for a specific thing: *"refused/ means the entry is INVALID, a permanent property of the file."* None of these three is invalid — `scripts/queue_entry_check.py` **accepts all four entries in this directory**, which is exactly why acceptance is a mechanical guard and never authorisation. They are **SPENT**: the work each names has already been bought. Filing a spent-but-valid entry as invalid would put a false permanent property on the record and would make `refused/` unreadable as evidence of anything. `held/` is already outside `list_entries`' glob (`:3-5` above), so what was missing was never containment — it was a **terminal label a future reader could not mistake for a hold awaiting release.**

**THE EVIDENCE, RE-VERIFIED FIRST-HAND AT PRIMARY ARTEFACTS, NOT TAKEN FROM THE BRIEF.**

| row | why it is spent | artefact |
|---|---|---|
| `D8R_chain.357a2648.json` | **already launched from this queue path and graded PASS** | `cases/dafoam/ladder-a/A6/curriculum_D8R/launcher.queue.out:1027` (`event=LAUNCH begin=2026-08-27T22:29:56Z`) and `:1028` (`rc=0 event=LAUNCHER_EXIT wall_s=13414`); run root `/home/ubuntu/certonomous-runs/CURRICULUM-D8R-a6-twist-opt-conv` with `CHAIN_DONE stamp=20260828T021330Z arms=[O-P F-P O-S F-S] grader_rc=0` and `D8R_grade_20260828T021329Z.json` reading `verdict PASS` on **both** rows |
| `D6_chain_wait.e43bdf61.json` | **its argv already fired and its `O_mp` arm died AT its cap** | `cases/dafoam/ladder-a/A2/curriculum_D6/launcher.queue.out:5-6` (LAUNCH `2026-08-27T13:49:03Z`, `LAUNCHER_EXIT rc=124 wall_s=31230`); `/home/ubuntu/certonomous-runs/CURRICULUM-D6-a2-wing-multipoint/ledger.txt` |
| `D6_chain.json` | **the earlier half of the same superseded pair; same spent chain** | same two artefacts, plus its own recorded supersession by the `e43bdf61` entry |

**THE ~2,000 CORE-MIN FIGURE IS NOW A LEDGER READ, NOT A TIMESTAMP DERIVATION — AND IT IS 2000.533, NOT ~2,000.6.** The order that dispatched this work carried the figure as **derived from launcher timestamps** and asked that it be corroborated or carried forward labelled. It is corroborated. `/home/ubuntu/certonomous-runs/CURRICULUM-D6-a2-wing-multipoint/ledger.txt` reads verbatim `ARM=O_mp ROW=PATCHED … rc=124 wall_s=30008 ranks=4 core_min=2000.533 cap_core_min=2000.0 enforced_wall_s=30000 enforced_core_min=2000.000000`. So the arm spent **2000.533 core-min against a registered per-arm cap of 2000.0** and was killed by its own deadline **at** that cap — `CLAUDE.md` rule 12, an overrun stops the run and does not get a new budget. `STATUS.chain` in the same root reads `chain=STOPPED_AT_FIRST_NONZERO arm=O_mp rc=124`. The derived figure and the ledger figure differ by 0.07 core-min; **the ledger row is the one to quote.**

**WHAT RETIREMENT DOES AND DOES NOT MEAN.** It means **not releasable, ever, under this registration** — and in this lab that matters at a 47-second timescale: **the drop IS the launch** (`../README.md`, AMENDMENT 2026-09-01T03:3xZ). It does **not** mean deleted: all three files, their pre-registration pins, their `hold_reason` text and their `_field_classes` stand untouched as the historical record, and nothing above this line is edited. **The genuinely unrun D6 work — `F_mp` and `REF_off` — has NO registration and is a pre-registration to write, not a row to release.** **No gate, threshold, cap, band, label, cost or prediction moves. Zero solver core-minutes: every reading here is a read of an existing artefact. Nothing is sent** (rule 7).

**`D12R_phase4.json` STAYS HELD, AND ITS HOLD NOTE UNDERSTATED ITS OWN BLOCKER — DATED CORRECTION (dafoam `lab-lane`, 2026-09-10T05:22:41Z, `[lab-attributed]`, on the dafoam-supervisor's ruling).** APPENDED; **no line above is rewritten** (`CLAUDE.md` rule 6). **This row is NOT retired and NOT released.**

**The stale sentence, struck as a statement of fact.** The row's `hold_reason` says phase 3 *"is being launched now and PRODUCES that file"*. That was true as an intention on 2026-09-03 and is false as a description of what happened. **Phase 3 was launched on 2026-09-03 and ABORTED.** `cases/dafoam/curriculum_D12R/STATUS.queue.D12R_phase3` reads `launcher_rc=1 end=2026-09-03T17:26:48Z`, and `cases/dafoam/curriculum_D12R/launcher.queue.out` gives the reason on its last line, verbatim: `ABORT: /home/ubuntu/certonomous-runs/CURRICULUM-D12R-cylinder-unsteady/step_plan2.json absent -- run the comparator in --plan2 mode first`. Phase 3 produced nothing — it never reached a solver, and it failed not on its own input but on the input of the step **before** it.

**PHASE 4 IS TWO SOLVER-GATED STEPS AWAY, NOT ONE, and the registered chain is in the launcher itself.** `cases/dafoam/curriculum_D12R/d12x_stage_and_run.sh:756-757` makes phase 3 refuse without `step_plan2.json`; `:786-787` makes phase 4 refuse without `step_plan3.json`. The order is **comparator `--plan2` → phase 3 → comparator `--plan3` → phase 4**. Neither `step_plan2.json` nor `step_plan3.json` exists — **both read ABSENT in the same invocation in which `step_plan.json` read PRESENT**, a planted positive control, because a zero from a reader not shown able to see a non-zero is not evidence (rule 3).

**THE TRUE NEXT STEP IS A COMPARATOR PASS, NOT A LAUNCH** — `--plan2` mode of the frozen `d12x_grade.py` against that run root, and even then `:760` closes a no-plateau result as *"Phase 3 is NOT LAUNCHED and the gradient is NOT A RESULT."* **Nothing here authorises any of it.** And `:19` above separately holds that this item's `step_plan*.json` **can never be written by its registered grading path** — a further question, unresolved, and a reason the corrected distance may still *understate* the real one.

**Form and scope.** The stale sentence is **not rewritten**; three keys — `hold_reason_correction_2026-09-10`, `hold_next_step_corrected`, `hold_reason_correction_recorded` — were inserted **textually before the row's own `"hold_reason"` line**, so a reader meets the correction first while the original stands. **This lane's edit contributed 7 insertions and 0 deletions**; the file's diff against `HEAD` also carries **2 deletions that are not this lane's** — a peer's uncommitted 2026-09-03 replacement of `hold_reason`/`hold_reason_recorded` that was already in the working tree, **inspected and landed, never reverted** (rule 10). **No gate, threshold, cap, band, label or cost moves. Zero solver core-minutes. Nothing moved to the drop path** — the drop is the launch.
