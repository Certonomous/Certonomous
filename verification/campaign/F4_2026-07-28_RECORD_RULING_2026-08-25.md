# F4 — RULING ON THE 2026-07-28 RECORD'S TWO `PASS` VERDICTS

**Written by the cfd supervisor personally, 2026-08-25.** `[lab-attributed]` under
Sanaa's desk-item disposal rule of this date; **overrulable**.

**This ruling REWRITES NOTHING.** `verification/campaign/F4_hypersonic_blunt_body.md`
is not edited. Rule 6: frozen files are never edited; **originals are struck,
never rewritten.** This is a dated ruling filed beside the record.

**It grades nothing and moves no gate.** The F4 conversion
(`3e82e989`, grading path pinned to blob `f5196143`) is **UNFIRED** at the time
of writing. **Nothing here prejudges its outcome.**

---

## 1. THE THREE DEFECTS, EACH VERIFIED BY ME FROM THE SOURCE

Found by the F4 lane; **re-derived by me rather than relayed.**

**Defect 1 — Gate 1 is graded `PASS` over a NON-MONOTONE triple, and the record
says so ON THE SAME PAGE.** The Summary table's Gate 1 Verdict cell reads
**`PASS`**. The paragraph **directly beneath that table** reads, in bold:

> *"**Standoff (Gate 1) does NOT converge monotonically with mesh refinement**
> (M=6: −0.33→−0.60→+2.06%; M=8: +6.62→−1.69→+0.70%, coarse→medium→fine) —
> stated plainly, not glossed over."*

**Standing rule 5, limb (2): a triple that is not `CONVERGING` is `NOT A RESULT`,
whatever its value.** The record disqualifies its own verdict three lines below
it and grades it anyway.

**This is not a hidden defect. It is a disclosed one that was annotated instead
of being acted on** — the failure mode where *a printed discrepancy labelled
"analysis" is worse than one never computed*, because the reader steps over it
on the author's authority.

**Defect 2 — Gate 2 is graded `PASS` against NO THRESHOLD AT ALL.** The Summary's
Gate 2 **Deviation cell is literally an em-dash (`—`)**. The verdict reads
*"**PASS**, converges cleanly (near-monotonically) with mesh refinement"*.

> **RMS 3.91 % of Cp_max is a measurement. `PASS` is a claim that a
> pre-registered threshold was met. Where no threshold exists, "PASS" is not a
> weak gate verdict — it is not a gate verdict at all.**

**Defect 3 — no return code was ever recorded, anywhere.** `find
verification/runs/F4_runs/cyl -name RC.txt` returns **zero files**. Completion
for all nine runs rests on the runner's own `solver_completed_to_endTime` field
inside `result.json`. **Standing rule 4's first limb — `rc = 0` — has no artifact
behind it for any of the nine.**

**Found only by running the repaired grader end-to-end against real data**, not
by reading it. That is the strongest argument in this lab for doing exactly that,
and it is the lane's catch, not mine.

## 2. THE RULING

**The chief's reading, offered and not imposed, is that the verdicts stand as
recorded until a gate moves them, because a verdict is moved by a gate and not by
an audit. I ADOPT THAT PRINCIPLE, and it needs a second half to be safe.**

> **1. The 2026-07-28 `PASS` cells STAND AS RECORDED.** I do not strike them, do
> not overwrite them, and do not rewrite the file. A verdict is a historical fact
> about what a campaign recorded on a date; an audit that finds its basis
> defective does not retroactively produce a different verdict. **A ledger a later
> reading can silently move is not a ledger.**
>
> **2. THEY ARE NOT EVIDENCE OF WHAT THEY APPEAR TO CLAIM, and from this date no
> lab record, credential, register row, matrix cell, report or upward summary may
> cite them as a passed gate.** They may be cited only as *"recorded 2026-07-28,
> unsupported on three grounds, see this ruling."* **The citable status of F4
> gates 1 and 2 is `PENDING` — pending the conversion, which exists precisely to
> produce the defensible verdict.**

**Why both halves are needed.** Half 1 alone leaves a `PASS` on the board that a
reader takes at face value — and F4 is a named HOLDS path, so that `PASS` would
travel. Half 2 alone would be an audit moving a verdict, which is the instability
the chief's principle correctly forbids. **Together: the record is honest about
what happened, and no downstream artifact inherits a claim the record cannot
support.**

## 3. A CONSTRAINT ON THE CONVERSION, REGISTERED **BEFORE** IT FIRES

Stated now, while the conversion is **UNFIRED**, so it cannot be read afterwards
as an explanation shaped to fit an answer:

> **Standing rule 5 is one-directional: the gate can turn a `PASS` or `GATE FAIL`
> INTO `NOT A RESULT`, never the reverse. So for Gate 1 the non-monotonicity can
> only make the outcome WORSE, never better.**
>
> **If the re-run triple is still non-monotone, Gate 1 is `NOT A RESULT` no matter
> how good the deviation looks — and +2.06 % against Billig will look good.**
> **That is the number most likely to be oversold, and this paragraph exists so it
> cannot be.**

**And the non-monotonicity is very likely to persist**, because the record's own
§4 attributes it to **a resolution-dependent bias in the peak-density-gradient
detector itself** — a property of the instrument, which the conversion does not
change. **A conversion that returns `NOT A RESULT` on Gate 1 would therefore be
the conversion working, not failing.**

**One fact in the conversion's favour, which I checked and the lane did not
claim.** `LADDER_RECIPE_CONSISTENCY_SWEEP_2026-08-10.md` classifies the F4
blunt-body ladder **single-recipe (CLEAN)** — one knob, `RES` = 50×20 / 100×40 /
200×80, `RADIAL_GRADING` held constant. **Unlike the Ahmed ladder this is a
genuinely similar family and its increments really are discretisation
increments.** The non-monotonicity is a property of the detector, **not a recipe
fork** — so the conversion has a real ladder to work with.

## 4. WHAT THIS RULING DOES NOT SAY

- **It does not say F4's physics is wrong.** +2.06 % against Billig and RMS
  3.91 % against modified Newtonian may both be perfectly good numbers. **They are
  ungated, not disproved.**
- **It does not touch Gate 3 (SWBLI)**, recorded *"Not run"*, and `BLOCKED` in the
  conversion's §14 on Sanaa's unmade event-1/event-2 ruling. **That ruling is hers
  and neither the lane nor I make it.**
- **It sets no threshold.** Choosing one now, having seen the measured values,
  is the fit rule 2 exists to prevent. **The thresholds live in the frozen
  conversion pre-registration, written before its solver starts.**

## 5. WHY F4 IS THE CLEAREST CASE IN THE LAB OF A `PASS` THAT PREDATES ITS GATE

Gate 2 has **no threshold at all** — not a loose one, not an implicit one, an
**em-dash**. **That is the whole conversion programme's thesis in one table
cell**, and it is why this family, not F3 and not F11, is the one that had to be
re-run.

## 6. COST

**ZERO COMPUTE.** No `docs/COST_CALIBRATION.md` row is owed. Every figure is read
from artifacts already committed.
