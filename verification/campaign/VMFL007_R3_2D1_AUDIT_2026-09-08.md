# VMFL007-R3 — §2d.1 CROSS-TEAM AUDIT: SIGNED OFF. The off-gate viscosity-reader repair is a value-invariant PURE INSERTION; the recovered PASS is a property of the RUN, reproduced byte-for-byte on an independent re-run. The lab's FIRST Ansys credential may land, on two landing conditions.

**Date:** 2026-09-08. **Author:** verification-supervisor (personal cross-team §2d.1 audit; the four
conditions ruled by me, the 67-line insertion diff-read by me, the value-invariance re-run verified by
me — SUPERVISION §3 checks 1 & 3, non-delegable).
**Provenance:** chief routing 2026-09-08 (internal relay, not Sanaa's words), first-credential audit.
**Cost:** 0 solver core-min, $0.00 (re-grade/read only; all output to scratch; no recorded file touched).

## What this is

VMFL007-R3 (non-Newtonian power-law pipe, VM2026R1 p.29) was register row #64 **BLOCKED** (`fa899b0a`)
because the frozen comparator exit-2'd on an OFF-GATE viscosity-clip reader BEFORE reaching the
pressure gate. A §2d.1 repair recovers the run as **PASS**. Ansys held the credential for BOTH its own
clean-room Δp re-derivation AND this independent audit. This audit is the final independent
confirmation.

## Facts verified at source (by the lane, checked by me)

- **Frozen comparator** `cases/ansys_verification/VMFL007-R3/grade_vmfl007_r3.py` — HEAD blob AND disk
  `git hash-object` both `03518d00` (matches the claimed pin; disk == HEAD, 904 lines). Freeze: prereg
  blob `3242a99f`, freeze commit `529f1665`. **The frozen file is UNTOUCHED.**
- **The repair is an untracked DRAFT** `grade_vmfl007_r3_R3AMEND_DRAFT.py` (blob `b0d6381d`, 971 lines).

### The insertion — a PURE INSERTION (my check-1 read)

- Diff of frozen `03518d00` (904) vs draft `b0d6381d` (971): **0 deletions, 0 modifications, +67
  insertions**, one hunk `@@ -896,6 +896,73 @@` — appended AFTER the frozen body, BEFORE `__main__`.
  904 + 67 = 971.
- It adds `_visc_monitor_path` and `_visc_read_scalar_series` and **re-binds `viscosity_class`**
  (a shadowing redefinition after the frozen def at L286 — the rule-6-compliant way to change frozen
  behavior: the frozen body is byte-preserved, the new behavior is APPENDED). The demonstrable error:
  the `fieldMinMax` monitors `nuMinAll`/`nuMaxAll` write `postProcessing/<name>/0/volFieldValue.dat`
  (a VOLUME min/max), but the frozen `_monitor_path` globs `surfaceFieldValue.dat` (a SURFACE
  integral) → 0 matches → `one_or_refuse` exit-2 inside `viscosity_class`, BEFORE the pressure gate
  and before the pInlet planted-zero.
- The re-bind reads the file OpenFOAM ACTUALLY wrote (`volFieldValue.dat`, or the selftest's
  `surfaceFieldValue.dat`) under the **same `one_or_refuse` "exactly one file, else REFUSE"
  discipline**, the **same parse** (last column), the **same frozen clip thresholds**
  (`0.999*NUMAX`, `1.001*NUMIN`), the same constants (`NUMIN 1e-8`, `NUMAX 1.0`), the same return
  keys. The guard STILL REFUSES if the clip binds — it is corrected, not skipped.

### Frozen-gate byte-identity

`head -896` of both files is byte-identical (both hash `e0495921…`). Unchanged in that region: REF_PA
`60520.0` (L82), TOL `0.005` (L83), BAND_LO `60217.40` (L84), BAND_HI `60822.60` (L85), NUMIN/NUMAX
(L106), PLANT `1.234e-03` (L109), the GATE reader `_monitor_path`→surfaceFieldValue for pInlet/pOutlet
(L152, unchanged), `roache` (L381), `verdict_for` (L409), the planted-zero control (L448), the p-floor
planted control (L486).

## The four §2d.1 conditions — ALL MET

1. **Real, confirmed grading-path defect, demonstrable by FILE EXISTENCE.** The frozen glob
   (`surfaceFieldValue.dat`) matches 0 paths; the monitors wrote `volFieldValue.dat`, which exists.
   Frozen comparator on the existing outputs: **rc=2**, "nuMinAll matched 0 paths … REFUSING", inside
   `viscosity_class` BEFORE the gate — the BLOCKED state reproduced. The defect is real and OFF-GATE.
2. **VALUE-INVARIANT, verified on EVIDENCE not assertion.** My independent re-run of the repaired
   comparator on the existing outputs (zero re-solve): **rc=0**, reaches and passes the pressure gate.
   Verdict **PASS**; triple L1 `60432.62813025` / L2 `60498.98212049` / L3 `60517.07126295` Pa;
   Roache **CONVERGING** from below; observed order **p = 1.8750592**; GCI_fine **0.014003%**; finest
   rel dev **0.0048%** (≈100× inside the 0.5% gate); IN-BAND. **Every value matches
   `verification/runs/ansys_verification/VMFL007-R3/GRADING_VMFL007_R3.json` byte-for-byte** (verdict,
   lab_value, rel_dev, triple state/p/GCI, all three Δp). The off-gate viscosity guard is applied with
   FROZEN thresholds to the correct data and is non-binding on all three levels — a property of the
   RUN, not manufactured (it would REFUSE had the clip bound).
3. **Frozen file edited ONLY via a dated addendum.** The frozen grade_vmfl007_r3.py is byte-UNTOUCHED
   (blob 03518d00); the repair is written AS a dated v1.0→v1.1 addendum (pure insertion, "lines whose
   number changed above this section: 0"). *(Landing condition 1 below governs how it lands.)*
4. **No gate/threshold/band/cap/label/reader touched.** Proven by the byte-identity of lines 1–896.
   The insertion re-binds ONLY the off-gate viscosity reader; it moves no gate quantity, threshold,
   label, or verdict-cascade node.

### Anti-gaming (§2av) and the planted control

- **Exactly one correct repair:** `fieldMinMax` writes `volFieldValue.dat` — reading the file
  OpenFOAM actually wrote is the one correct fix; `one_or_refuse` refuses if two ever coexist, so a
  favorable file cannot be silently chosen.
- **Planted-zero control FIRES** (shown able to see a non-zero): on the real L3 run, planted
  0.001234 kinematic → `reader_dp_shift_pa = 1.234` Pa == RHO·PLANT, `passed:true`; both selftest arms
  PASS incl. "planted-zero REFUSES (exit 2) a reader that cannot see the plant"; 32/32 under `python3`
  and `python3 -O`.
- **Credential-grade grid convergence:** a genuine CONVERGING Roache triple with an OBSERVED order and
  GCI (rule 5 / Coverage-Matrix G), finest ~100× inside the gate.

## VERDICT: SIGNED OFF — §2d.1 GRANTED. The PASS is sound and defensible; the repair is proven not to have manufactured it.

The recovered **PASS** is a property of the run, independently reproduced by me byte-for-byte and (per
the chief) by ansys's clean-room Δp re-derivation (pOutlet≡0 reader, triple to <4e-5 Pa, p=1.875,
R=0.273, clip non-binding). Both ansys §3 checks (check-1 value-invariant insertion; check-3
independent diagnostic) plus this independent audit confirm. **The lab's first Ansys credential
(register #67) may land.**

### Two landing conditions (ansys to execute)

1. **Land the repair as an IN-PLACE dated addendum to the FROZEN `grade_vmfl007_r3.py`** (append the
   67-line block, file → 971 lines, lines 1–896 byte-preserved), and **re-pin the freeze** so the
   REGISTERED and GRADED comparator IS the frozen-plus-addendum file — NOT a separate DRAFT that
   shadows the frozen file at runtime. The official graded JSON must be produced by the landed, pinned
   comparator and must match `GRADING_VMFL007_R3.json` (my re-run confirms it will).
2. **Add a selftest arm that exercises the new `volFieldValue.dat` PRODUCTION reader path**
   (RED-then-GREEN), because the current selftest fixtures write only `surfaceFieldValue.dat` and so
   never exercise the fixed production path (the frozen and repaired selftests are byte-identical — a
   §28.8/§2ay.5 coverage gap, same class as `docs/COST_CALIBRATION.md:475`). **This does NOT block the
   current verdict** — its value-invariance rests on my independent re-run over the real
   `volFieldValue` path, which is stronger than a selftest for this instance — but the instrument owes
   its own plant going forward, and the same landing edit is the natural place to add it.

## Caveats (disclosed)

- I did not re-derive Δp by hand from the raw fields (that was ansys's clean-room task, now in and
  concurring); I confirmed the grader reproduces the recorded triple exactly.
- I did not re-verify prior VMFL007 rulings (§12.2 ceiling, the manual reference value) — outside this
  audit's scope.
- HEAD moved during the audit (peers active); the frozen comparator blob is byte-identical
  disk == HEAD == `03518d00`, which is what the audit needs.
