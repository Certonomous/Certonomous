{
  "team": "cfd",
  "case_id": "R5_RUNNER_STATUS_COLLISION",
  "prereg_commit": "52632a2dd3cc78fccf0855684dffc386f79dc701",
  "prereg_path": "verification/campaign/R5_RUNNER_STATUS_COLLISION_PREREGISTRATION.md",
  "launch_cmd": [
    "bash",
    "/home/ubuntu/Certonomous/scripts/run_r5_status_collision_repair.sh",
    "--prereg-commit=52632a2dd3cc78fccf0855684dffc386f79dc701"
  ],
  "cwd": "/home/ubuntu/Certonomous",
  "ranks": 1,
  "cost_core_min_estimate": 0.4717,
  "cost_basis": "DERIVED from measurements taken by cfd lane R45 on 2026-08-28 at HEAD 1f74d6de, term by term, and NO DOLLAR FIGURE HERE IS MEASURED. Terms (1 rank, core-min = wall s x ranks / 60): (a) scripts/queue_runner.py --selftest x3 -- the pre-repair baseline for S9, the post-repair run, and the mutation run of S7 = 15.8 s, MEASURED: one run took 5.24 wall s at rc 0 with 41/41 checks and 0 asserts, rc read on the bare command with output redirected to a file, taken only after reading its isolation (tempfile.mkdtemp at :772, shutil.rmtree of that tree alone at :1376, no literal reference to the real queue root in :772-1376, SIGTERM only to a scratch-root daemon it spawns itself at :1083) and verifying the live daemon pid 1120800 alive before (elapsed 1-00:01:13) and after (1-00:01:47) -- THIS IS THE TERM R2 DECLINED TO MEASURE AND CARRIED AT 40.0 s FOR TWO RUNS; the truth for two is 10.5 s; (b) the new rows S1-S7, each driving a REAL tick()/launch() with a real detached wrapper in a production-shape scratch root, plus S5's double launch and S6's injected-clock cap_watch = 8.0 s ALLOWANCE, on the measured rate of the existing suite (41 controls including ~10 real ticks in 5.24 s, about 0.5 s per tick) -- THIS IS THE ONLY LARGE ALLOWANCE AND THE CALIBRATION MUST ATTRIBUTE ANY GAP TO IT EXPLICITLY; (c) the collision sweep re-driven over all 375 tracked *.sh and all 134 entries, mandatory at implementation because a tree state has a shelf life = 2.0 s ALLOWANCE, the same sweep having run in under 1 wall s in the registering invocation; (d) the sha256 invariance set and the record write = 2.5 s ALLOWANCE. TOTAL 28.3 wall s = 0.4717 core-min. Registered CAP 0.7000 core-min = 1.4840x the estimate, converted to a 42.0 s wall allowance handed to timeout AROUND terms (b), (c) and (d), the three allowance terms; term (a) is measured and is not the exposure. An overrun STOPS THE ITEM and grades NOT A RESULT rather than taking a new budget: the correct response to term (b) proving larger than 8 s is a NOT A RESULT and a re-registration with a measured basis. Dollars at the rate $0.0513/core-h, c7a.4xlarge, owner-stated 2026-08-21/22 and REPORTED-BY-OWNER: $0.000403 at the estimate and $0.000599 at the cap -- DERIVED, NOT MEASURED; the box cannot read its own billing (COMPUTE_BUDGET_CHARTER.md section 5).",
  "memory_floor_gb": 1.0,
  "enqueued_by": "HELD, NOT ENQUEUED. Drafted by cfd lane R45 on 2026-08-28 at the cfd-supervisor's written direction under Sanaa's 2026-08-28T17:01Z freeze-ahead amendment ('the finding-repairs are the queue: they're frozen, capped, schedulable work items like any case'). THIS LANE HAS COMMITTED NOTHING, ENQUEUED NOTHING AND LAUNCHED NOTHING: the freeze commit and the drop into verification/queue/cfd/ are the supervisor's own acts under SUPERVISION_CHARTER.md sec.3 check 4, which may never be delegated and which NEITHER this entry NOR the queue-entry validator discharges. ENQUEUEING IS NOT AUTHORISATION. NOTE FOR THE ENQUEUER: scripts/queue_entry_check.py does NOT check that the launch target exists on disk (that gap is R4's subject), so this entry -- whose target does not exist yet -- validates cleanly today and would die at launch behind a LAUNCHED record. The five enqueue preconditions in the registration's LAUNCH SHAPE section are human checks, and precondition 4 is a real one: if the QUEUE_RUNNER_RECORD_LOCATION relocation has landed, the correct action is BLOCKED, not a rewritten R5.",
  "_schema_note": "Fields above are the required set (scripts/queue_entry_check.py REQUIRED_FIELDS). Fields below are cfd annotations the validator ignores.",
  "_prereg_commit_placeholder": "PLACEHOLDER -- the git null sha, deliberately chosen because it is a valid 40-char lowercase hex string that PASSES the SCHEMA clause and FAILS COMMIT-EXISTS and PREREG-AT-COMMIT. An entry cannot carry the sha of the commit that contains it. THE CFD-SUPERVISOR REPLACES THIS AT THE FREEZE, in BOTH places it appears (prereg_commit and the --prereg-commit= argument in launch_cmd), with the sha of the adding commit of prereg_path, re-derived from `git log --diff-filter=A -- <prereg_path>` and NEVER from a subject line. Until then this entry is NOT ENQUEUEABLE and the validator says so.",
  "purpose": "Make scripts/queue_runner.py unable to occupy a STATUS path it did not name. THE REPAIR IS CANDIDATE (a): :496 writes cwd/STATUS.queue.<case_id> UNCONDITIONALLY, one line. NO SOLVER COMPUTE, no validator change, no other team's launcher touched.",
  "the_defect_verified": "queue_runner.py:496 computes status = cwd / f'STATUS.{case_id}' from a path THE ENTRY SUPPLIES and :501 truncates it with a shell '>' after the launch argv returns. cases/F27_WOMERSLEY_PIPE/run_f27.sh:57 sets STATUS=\"$ROOT/STATUS.F27_WOMERSLEY_PIPE\" with ROOT the case directory (:48) = the entry's cwd -- BYTE-IDENTICAL. run_f27.sh:87-92 writes from an EXIT trap, so the launcher writes first and the runner second, unconditionally, and :89-90 emits a RICHER record carrying cap_core_min= and spent_core_min=. MEASURED: the on-disk file is 93 bytes, mtime 2026-08-28 09:01, untracked at HEAD, and reads 'launcher_rc=0 end=2026-08-28T09:01:29Z note=exit-status-of-the-launch-argv-NOT-the-solver-rc' -- THE RUNNER'S FORMAT. F27's own record was overwritten. The launcher's own comment at :83-86 documents the ordering and calls the two records 'identical in meaning', which is wrong: two of the launcher's four fields have no counterpart.",
  "brief_falsified": "THE BRIEF SAYS THE DESTROYED RECORD IS 'NOT RECONSTRUCTABLE'. IT IS RECONSTRUCTABLE, and this lane checked before writing that sentence. run_f27.sh:336 says the same figures to STDOUT and the runner redirects the argv's stdout to cwd/launcher.queue.out (:497, :501). Read from that one named file: launcher.queue.out:29 'ALL THREE LEVELS COMPLETE. Cumulative spend: 265.46666666666664 core-min of 680.' and :3 'CAP AGREES between launcher and grader: 680 core-minutes.' All three destroyed fields survive verbatim six inches away: spent_core_min = 265.4666666666666, cap_core_min = 680, i.e. 39.04 percent of cap -- RECOVERED, NOT ESTIMATED. The loss is the loss of A RECORD, not of the information in it. THIS MAKES THE REPAIR MORE URGENT, NOT LESS: (1) the recovery is PERISHABLE -- launcher.queue.out is written into the same cwd by the same mechanism and is truncated on the next launch of F27, and nothing on disk records that the window is closing; (2) the redundancy is an accident of one launcher -- a launcher writing cap and spend ONLY to STATUS would lose them outright, and nothing stops the next one from doing exactly that. The guard is registered against the class, not against F27.",
  "why_candidate_a_and_not_b": "CANDIDATE (b), refuse-to-launch-if-the-target-exists, IS REFUSED ON A MEASURED FACT: over the 134 entries under verification/queue/*/ and */*/, 117 ALREADY HAVE A FILE AT THE RUNNER'S EXACT TARGET PATH. (b) would refuse the relaunch of 87 percent of the lab's queue on the day it lands, and the block would be PERMANENT because nothing removes that file: archive_previous_records() (:454-470) iterates launched_dir.glob('*.json') and os.replace's the matching JSON LAUNCH RECORD to <stem>.<utc>.json -- it is handed launched_dir, not cwd, and never touches STATUS.<case_id>. The disk already shows operators paying that cost by hand: STATUS.VMFLGPU001.attempt1/.attempt2, STATUS.F15.attempt1, STATUS.F16.attempt1. (b) would make that manual step mandatory lab-wide for five teams who did not write the defect, trading a bookkeeping defect for a physics-blocking one -- the wrong direction under L-342. CANDIDATE (a) removes the collision BY CONSTRUCTION where (b) detects it BY STATE, and a guard depending on tree state that nothing maintains fails open the first time somebody cleans up and fails closed 117 times before that. (a) blocks zero relaunches, preserves the write unconditionally, is right on the merits (the runner's own :498-500 and :526-535 class this file as INFRASTRUCTURE about the LAUNCH, not the case), breaks no reader (cap_watch resolves it through the absolute _launch.status_file stamped at :530 and never reconstructs it from cwd -- the same finding QUEUE_RUNNER_RECORD_LOCATION.md sec.3.2 reached independently with its own planted control), and announces itself for free in the existing LAUNCHED log line at :541-543. ACCEPTED COST OF (a), STATED: two STATUS.* files can now sit in one directory and a HUMAN reader must know which is which -- accepted because the machine reader is unambiguous, because the alternative to two records is one record and one destroyed record, and because the annotation specified in the registration tells the human reader which is which for the one case where the destruction already happened. A THIRD VARIANT, archive-then-write, was considered and DECLINED: it fixes (b)'s refusal problem but leaves the runner writing into a namespace it does not own -- the actual defect -- and puts the runner in the business of renaming files in other teams' case directories.",
  "subsumption_answered_honestly": "DOES R5 BECOME UNNECESSARY IF docs/standards/QUEUE_RUNNER_RECORD_LOCATION.md (frozen 47e86a46, verified an ancestor of HEAD) LANDS? YES. Under its relocation the runner writes nothing into cwd at all, so the collision cannot occur, and its destinations carry the case id so it also closes the cross-case flag collision. R5 IS A STRICT SUBSET OF IT AND SAYS SO RATHER THAN DEFENDING ITS OWN EXISTENCE. R5 is registered anyway as the NARROW, INDEPENDENTLY-SHIPPABLE guard on four grounds: (1) THE RELOCATION IS REFERRED AND NOT RULED -- its own head matter says 'the code does not land until routed', because it alters a location dafoam's dafoam_wait_then_launch.sh documents a measured dependency on; R5 needs no referral, no new directory, no .gitignore and no migration; (2) the relocation FALSIFIES a documented invariant in dafoam's file (its :36-39 records the runner overwriting cwd/STATUS.<case_id>) while R5 merely NARROWS it, and R5 REPORTS that rather than making the change on another team's behalf; (3) a cap of 0.7000 core-min should not wait on a cross-team blast-radius review while a live loss stays live; (4) THEY COMPOSE, AND THE RETIREMENT PATH IS REGISTERED NOW -- if the relocation lands AFTER R5, :496 is rewritten wholesale and R5's clause is RETIRED AS A DATED ADDENDUM, not left as dead code; if it lands BEFORE R5, R5's absence condition fails at launch and R5 is BLOCKED, its defect having been fixed by another item, stated in the fixed vocabulary rather than as prose.",
  "cap_watch_sweep_result": "SWEPT AS ORDERED, over all 375 tracked *.sh from `git ls-tree -r --name-only HEAD` (never git ls-files, which reads the poisoned shared index), each file opened and read individually rather than through a bare recursive grep (ugrep 7.8.4 races its multi-file output order). 97 scripts mention one of the four runner basenames. THE SWEEP IS SHOWN ABLE TO SEE A REAL COLLISION BEFORE REPORTING ANYTHING: it found the known-true positive run_f27.sh:57 and dafoam_wait_then_launch.sh:82; a sweep returning zero without first finding F27 would be measuring its own regex. RESULT 1 -- STATUS.<case_id>: THREE COLLIDING LAUNCHERS, NOT ONE. (i) cases/F27_WOMERSLEY_PIPE/run_f27.sh:57 -- byte-identical, cfd's own, and the one that already fired. (ii) cases/dafoam/_common/dafoam_wait_then_launch.sh:82, STATUS=\"$CWD/STATUS.$CASE_ID\" with CWD=$(pwd) = the cwd the runner Popens with -- byte-identical, ANOTHER TEAM'S, REPORTED NOT REPAIRED. (iii) cases/dafoam/curriculum_D12R2/d12y_w3_chain_driver.sh:41, $HERE/STATUS.W3_chain, the same basename the W3_chain entry's runner target uses -- ANOTHER TEAM'S, REPORTED NOT REPAIRED. NOT COLLIDING, checked per script: the 20 heat-transfer/F14 launchers write $ROOT/STATUS.$CASE with ROOT=dirname(CASE_DIR) and CASE=basename(CASE_DIR), i.e. into the PARENT of cwd; T1's run_one_dts_u.sh:70 writes STATUS_u.$CASE; the 7 ansys VMFLGPU scripts write $BUILD_ROOT/STATUS.smoke; run_m1.sh:52 writes $CASE_DIR/STATUS with no case-id suffix; d14m_driver.sh:115 writes $BASE/STATUS.MESH, which no entry's case id matches. QUEUE_RUNNER_RECORD_LOCATION.md sec.5.1's 'a sweep of all cases/F*/run_f*.sh found this case only' is CORRECT WITHIN ITS STATED SCOPE and is not the lab-wide answer; that spec names dafoam's wrapper separately in its own sec.5.2. Candidate (a) fixes all three at once by moving the runner off the contested name. RESULT 2 -- CAP_OVERRUN.txt (:611) and ESTIMATE_OVERRUN.txt (:627): A DIFFERENT COLLISION, AND NOBODY HAS NAMED IT. No launcher collides with these -- of the 375 scripts exactly ONE mentions either basename and it is a COMMENT (run_one_t5b.sh:16); zero launchers write them. BUT THEY COLLIDE WITH EACH OTHER ACROSS CASES, because neither name carries a case id: 8 cwd values are shared by more than one case_id -- /home/ubuntu/Certonomous by 4 verification VR entries, cases/dafoam/curriculum_D12R2 by 11 dafoam case ids, .../curriculum_D5 by 4, and curriculum_D12R, curriculum_D4_SHIPPED, curriculum_D6, curriculum_D7FR and curriculum_D8R by 2 each. In each of those directories there is ONE SLOT for CAP_OVERRUN.txt; the flag's text names its case (:613-615) so a reader who opens it is not deceived, but a second case's overrun overwrites the first's and the first then has no flag at all -- the same class as VMFL064-R2, the documented reason archive_previous_records() exists. LAB-WIDE TODAY: 8 CAP_OVERRUN.txt and 5 ESTIMATE_OVERRUN.txt on disk, NONE in a directory currently shared by two case ids, so THE CROSS-CASE FLAG COLLISION IS LATENT AND HAS NOT FIRED. (Reconciling with QUEUE_RUNNER_RECORD_LOCATION.md sec.2's '4 and 0', which was scoped to cases/: exactly 4 of my 8 and 0 of my 5 are under cases/. The counts agree.) REPORTED, NOT REPAIRED HERE: it is runner-versus-runner rather than runner-versus-launcher, its fix is a per-case prefix which the relocation spec already specifies, and folding it in would let this item's PASS rest on evidence about a defect it was not registered against.",
  "blast_radius_census": "For all 134 entries the runner's own target cwd/STATUS.<case_id> was computed and the first line of each read: 117 ALREADY EXIST, 17 absent; 116 of the 117 are in a runner format and exactly 1 is not (cases/dafoam/ladder-a/A2/curriculum_D6R/STATUS.D6R_chain_wait, dafoam's wrapper series). CORRECTION OF THIS LANE'S OWN FIRST READING, recorded because it is the instrument class: the first pass classified 6 as foreign on a predicate requiring the literal 'note=exit-status-of-the-launch-argv'. Five of the six read 'rc=0 end=...' and are AN OLDER RUNNER FORMAT, not a foreign one -- `git log -S'launcher_rc=$R' -- scripts/queue_runner.py` names commit ba365479, whose own subject says it introduced launcher_rc 'labelled as NOT the solver's rc (L-342; the false T5_X_2d rc=0)'. A CLASSIFIER KEYED ON THE CURRENT FORMAT REPORTS EVERY HISTORICAL RECORD AS FOREIGN. The corrected count of 1 agrees with QUEUE_RUNNER_RECORD_LOCATION.md sec.2's independent finding. AND THE CENSUS CANNOT SEE THE DAMAGE IT LOOKS FOR: a STATUS file already clobbered reads as a runner record -- F27's does -- so a completed collision is indistinguishable from no collision, this census is a LOWER BOUND, and the only instrument that can find the class is a read of the LAUNCHERS.",
  "observable": "CONTROL OUTCOME VECTOR (S1..S9), each FIRED/HELD/FLIPPED/NOT RUN, with S9's set-equality reading. NOT A RICHARDSON QUANTITY: no grid triple, no refinement ratio, no mesh, no field, NO OBSERVED ORDER IS COMPUTED, NO GCI IS COMPUTED OR QUOTED at Fs=1.25 or any factor, roache_triple.py is NOT CALLED, and standing rule 5 is INAPPLICABLE -- not waived, not satisfied -- because it grades a row with a grid triple and this row has none. In-family precedent: cases/F26_RINGLEB/PREREGISTRATION_F26D_2026-08-27.md sec.4. F27's recovered 265.4666666666666 core-min of a 680 cap is NOT an observable of this item: it is a figure recovered from an existing artefact while verifying the defect, and it is not graded here.",
  "birth_requirement": "REFUSAL CONDITION ON THE INSTRUMENT (Sanaa directive 1, 2026-08-28T17:01Z). REAL PRODUCER: scripts/queue_runner.py::launch() -- :496 the status path, :501-502 the detached wrapper string, :503-507 the Popen(['setsid','nohup','bash','-c',inner], cwd=str(cwd)) that actually writes it, :520 shutil.move, :530 the _launch.status_file stamp, :536 dst.write_text. THE BYTES IN THE STATUS FILE MUST BE WRITTEN BY THAT Popen, NOT BY THE CONTROL. REAL READER: queue_runner.tick() for the launch decision and cap_watch() resolving _launch.status_file for S6 -- not a re-implementation, not a stub. THE DRIVER EXITS 2 AND GRADES NOTHING unless, in the same invocation: (i) the scratch queue root has PRODUCTION'S SHAPE, <scratch>/.../verification/queue/<team>/, the shape QUEUE_ENTRY_TEAM_BINDING.md amendment 2 made mandatory after a control passed vacuously on a flat root; (ii) the planted cwd/STATUS.<case_id> is written BEFORE the tick, its sha256 recorded, and its content is A SENTINEL THE RUNNER'S FORMAT CANNOT PRODUCE -- carrying cap_core_min= and spent_core_min= in run_f27.sh:89's exact shape -- so 'was it destroyed' is answered by CONTENT and never by mtime; (iii) S1 FIRES FIRST: the destruction is observed on the UNREPAIRED runner, the post-tick file shown NOT to contain the sentinel, before the repaired run may report that it does not destroy it; (iv) the launch argv is a trivial REAL command and the driver waits for the wrapper to complete, asserting STATUS.queue.<case_id> exists AND is newer than the launch epoch before reading it, so no control reads a file the wrapper has not finished writing; (v) NO PATH UNDER THE REAL verification/queue/ IS OPENED FOR WRITING at any point, and the live daemon's runner.pid is read but never signalled. WHY A HAND-WRITTEN STATUS FILE IS REFUSED: a STATUS file written by the control is a schema the producer never emitted, and grading against it would certify only that the control can overwrite its own file. A sentinel the runner's own format could have produced would make S2 unfalsifiable -- the plant, not the assertion, is where blindness lives.",
  "negative_control": "MANDATORY AND SEPARATELY GATED. S4: with NO file at either path, the same real tick() launches normally -- entry moved to launched/, LAUNCH_LOG.tsv gains EXACTLY ONE row, STATUS.queue.<case_id> written. S3: THE RECORD IS STILL WRITTEN AT ALL -- after S2 the file exists, is newer than the launch, its first line matches ^launcher_rc=\\d+ end=\\S+ note=, and _launch.status_file in the moved launched/ JSON is byte-equal to that absolute path; a 'fix' that stops writing destroys the launch bookkeeping and would pass any naive collision check. S5: the same entry launched TWICE IN A ROW into the same cwd launches both times, the second overwriting STATUS.queue.<case_id>, with archive_previous_records() archiving the first JSON record -- this is the limb candidate (b) would have failed on 117 of 134 entries. S6: cap_watch still finds the file through _launch.status_file under an injected clock, flag written and status_seen_utc retirement still firing. S7 MUTATION: :496 reverted to f'STATUS.{case_id}' and NOTHING ELSE CHANGED flips S2 to destruction and S3 onto the old path -- a one-line guard whose reversion changes nothing was never load-bearing.",
  "f27_record_boarding": "BOARDED AS A LOSS OF A RECORD, NOT OF ITS INFORMATION, AND NOT REPAIRED. The file stands as it is: a run artifact, untracked at HEAD, holding the RUNNER'S record. What is owed is AN ANNOTATION, which is a record and not an edit: it must state that the surviving line is the runner's and not the case's, that run_f27.sh:88-91's richer record occupied that path first and was overwritten by queue_runner.py:501, and that the overwritten fields are RECOVERED -- cap_core_min = 680 and spent_core_min = 265.4666666666666 (39.04 percent of cap) from launcher.queue.out:29 and :3 -- WITH THE WARNING THAT launcher.queue.out IS ITSELF TRUNCATED BY THE RUNNER ON THE NEXT LAUNCH OF F27, so the recovery window is open only until then. WHERE IT BELONGS: beside the artifact it describes, in the F27 case directory, as a new STATUS_PROVENANCE.F27_WOMERSLEY_PIPE.txt -- NOT as an edit to the STATUS file (which would destroy the evidence a second time), NOT in docs/ (it is run-artifact provenance, not a standard), and NOT in any scratchpad (L-186). run_f27.sh is a frozen grading path and rule 6 forbids editing it. THIS LANE HAS NOT WRITTEN THAT ANNOTATION AND MUST NOT: it asserts a fact about a completed run's cost record and SUPERVISION_CHARTER.md sec.3's big-claim check is the supervisor's.",
  "must_not_regress": "MEASURED TODAY in the registering invocation, rc read on the bare command with output redirected to a file: `python3 scripts/queue_runner.py --selftest` -> rc 0, 'SELFTEST PASS: 41/41 checks, 0 asserts', 5.24 wall s. The brief's 41/41, rc 0, ~5 wall s is CONFIRMED. `python3 scripts/queue_entry_check.py --selftest` -> rc 0, 31 controls, 0.50 wall s. THE NEW BAR: 41/41 at rc 0 PLUS the rows added here and not one row fewer, with the check-name set differing from today's by only the added rows.",
  "permitted_edit": "ONE HUNK: scripts/queue_runner.py:496, plus the control rows added to selftest(). ANY THIRD HUNK IS GATE FAIL under G-R5-9. The diff is read AS A DIFF by the cfd-supervisor personally -- SUPERVISION_CHARTER.md sec.3 check 1, which may never be delegated and which no control result discharges. scripts/queue_entry_check.py is NOT touched at all. cases/dafoam/_common/dafoam_wait_then_launch.sh and every other team's launcher are in the invariance set so that 'reported, not repaired' is mechanical rather than promised. cases/F27_WOMERSLEY_PIPE/ IN FULL is in the invariance set, because the recovery window depends on launcher.queue.out surviving untouched. A landed edit to :496 changes the running daemon's behaviour only when it is restarted; whether and when to restart is the supervisor's call and is deliberately left unanswered here. The live daemon pid 1120800 must not be restarted, signalled or raced by this item.",
  "cap_core_min_registered": 0.7,
  "cap_over_estimate": 1.484,
  "run_root_absence": "/home/ubuntu/Certonomous/verification/runs/TOOLING_REPAIRS, its child R5_STATUS_COLLISION_2026-08-28, and /home/ubuntu/Certonomous/scripts/run_r5_status_collision_repair.sh -- ALL ABSENT at 2026-08-28T17:35:08Z, each checked FOUR ways (os.path.exists False, os.path.lexists False, os.path.isdir False, glob []), plus a fifth wider glob: verification/runs/TOOLING* -> [] and scripts/run_r[45]* -> []. A SIXTH CONDITION, load-bearing for the subsumption ruling: scripts/queue_runner.py at HEAD 1f74d6de hashes to blob 94fdabfcaf661d0d7cf3bdf1bbffae81e3845f03, IDENTICAL to the worktree copy by git rev-parse HEAD:<path> against git hash-object <path> -- the only index-immune comparison -- and its :496 still reads status = cwd / f'STATUS.{case_id}', so THE RELOCATION HAS NOT LANDED. If it has by launch time the verdict is BLOCKED. 0.000 core-min spent under this registration.",
  "verdict_state": "PENDING"
}
