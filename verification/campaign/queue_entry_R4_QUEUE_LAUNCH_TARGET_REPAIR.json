{
  "team": "cfd",
  "case_id": "R4_QUEUE_LAUNCH_TARGET_REPAIR",
  "prereg_commit": "52632a2dd3cc78fccf0855684dffc386f79dc701",
  "prereg_path": "verification/campaign/R4_QUEUE_LAUNCH_TARGET_REPAIR_PREREGISTRATION.md",
  "launch_cmd": [
    "bash",
    "/home/ubuntu/Certonomous/scripts/run_r4_launch_target_repair.sh",
    "--prereg-commit=52632a2dd3cc78fccf0855684dffc386f79dc701"
  ],
  "cwd": "/home/ubuntu/Certonomous",
  "ranks": 1,
  "cost_core_min_estimate": 0.325,
  "cost_basis": "DERIVED from measurements taken by cfd lane R45 on 2026-08-28 at HEAD 1f74d6de, term by term, and NO DOLLAR FIGURE HERE IS MEASURED. Terms (1 rank, core-min = wall s x ranks / 60): (a) scripts/queue_entry_check.py --selftest x3 -- the pre-repair baseline for L13, the post-repair run and the -O run at L12 = 1.5 s, MEASURED: one run took 0.50 wall s at rc 0 with 31 controls fired, in the registering invocation, rc read on the bare command with output redirected to a file; (b) the new rows L1-L9 and L12-L13 = 2.5 s ALLOWANCE on the measured per-control rate 0.50/31 = 0.016 s, carried generously because L1/L2/L9 each build a tempfile production-shape queue tree and L3/L4/L5 copy real entries off disk; (c) L10/L11's five real subprocess invocations of the real file (bare, --dir empty, one-valid, one-invalid, --dir not-a-directory) = 1.5 s ALLOWANCE on a measured ~0.15 s interpreter start; (d) scripts/queue_runner.py --selftest x2 for the G-R4-10 regression reading = 11.0 s, MEASURED AND NOT GUESSED: one run took 5.24 wall s at rc 0 with 41/41 checks, taken only after reading its tempfile.mkdtemp isolation (:772, :1376) and verifying the live daemon pid 1120800 alive before and after -- this is the term R2 carried at 40.0 s as a declared guess and the truth for two runs is 10.5 s; (e) the shape survey re-driven over every entry on disk, the sha256 invariance set and the record write = 3.0 s ALLOWANCE, the same survey having run over 133 entries in under 1 wall s in the registering invocation. TOTAL 19.5 wall s = 0.3250 core-min. Registered CAP 0.4800 core-min = 1.4769x the estimate, converted to a 28.8 s wall allowance handed to timeout AROUND terms (b), (c) and (e), the three allowance terms; an overrun STOPS THE ITEM and grades NOT A RESULT rather than taking a new budget. Dollars at the rate $0.0513/core-h, c7a.4xlarge, owner-stated 2026-08-21/22 and REPORTED-BY-OWNER: $0.000278 at the estimate and $0.000410 at the cap -- DERIVED, NOT MEASURED; the box cannot read its own billing (COMPUTE_BUDGET_CHARTER.md section 5).",
  "memory_floor_gb": 1.0,
  "enqueued_by": "HELD, NOT ENQUEUED. Drafted by cfd lane R45 on 2026-08-28 at the cfd-supervisor's written direction under Sanaa's 2026-08-28T17:01Z freeze-ahead amendment ('the finding-repairs are the queue: they're frozen, capped, schedulable work items like any case'). THIS LANE HAS COMMITTED NOTHING, ENQUEUED NOTHING AND LAUNCHED NOTHING: the freeze commit and the drop into verification/queue/cfd/ are the supervisor's own acts under SUPERVISION_CHARTER.md sec.3 check 4, which may never be delegated and which NEITHER this entry NOR the queue-entry validator discharges. ENQUEUEING IS NOT AUTHORISATION. NOTE FOR THE ENQUEUER, AND IT IS THIS ITEM'S OWN SUBJECT MATTER: scripts/queue_entry_check.py does NOT check that the launch target exists on disk, so this entry -- whose target does not exist yet -- validates cleanly today and would die at launch behind a LAUNCHED record. The five enqueue preconditions in the registration's LAUNCH SHAPE section are human checks and P1 (R2 must have landed) is a sequencing block, not a formality.",
  "_schema_note": "Fields above are the required set (scripts/queue_entry_check.py REQUIRED_FIELDS). Fields below are cfd annotations the validator ignores.",
  "_prereg_commit_placeholder": "PLACEHOLDER -- the git null sha, deliberately chosen because it is a valid 40-char lowercase hex string that PASSES the SCHEMA clause and FAILS COMMIT-EXISTS and PREREG-AT-COMMIT. An entry cannot carry the sha of the commit that contains it. THE CFD-SUPERVISOR REPLACES THIS AT THE FREEZE, in BOTH places it appears (prereg_commit and the --prereg-commit= argument in launch_cmd), with the sha of the adding commit of prereg_path, re-derived from `git log --diff-filter=A -- <prereg_path>` and NEVER from a subject line. Until then this entry is NOT ENQUEUEABLE and the validator says so.",
  "_launch_target_absent": "STATED PLAINLY, BECAUSE IT IS THE DEFECT THIS ITEM REPAIRS: /home/ubuntu/Certonomous/scripts/run_r4_launch_target_repair.sh DOES NOT EXIST at 2026-08-28T17:35:08Z, checked four ways (os.path.exists False, os.path.lexists False, os.path.isdir False, glob []). This entry is one of exactly THREE-now-FOUR in the lab whose launch target is absent; the other three are cfd's R1, R2 and R3. All 130 other entries on disk resolve to a present target.",
  "purpose": "Make scripts/queue_entry_check.py refuse an entry it cannot have validated. THREE CLAUSES, ONE FAILURE: (1) LAUNCH-TARGET existence -- the argv's resolved launch target must exist; (2) executability, asymmetric -- X_OK required only where the target is executed directly, NEVER where an interpreter reads it; (3) non-empty invocation -- main() must not return rc 0 having validated nothing. They are one registration because they are one proposition failing: THE VALIDATOR RETURNED SUCCESS WITHOUT HAVING VALIDATED ANYTHING. One selftest, one cap. NO SOLVER COMPUTE.",
  "resolution_rule": "THE CLAUSE DOES NOT INDEX launch_cmd[1]. MEASURED OVER ALL 133 ENTRIES ON DISK: [1] is the script in only 68; in 62 heat-transfer/F14 entries argv[0] IS the script and [1] is the literal flag '--case-dir'; in 3 dafoam entries argv is ['setsid','nohup','bash',<script>,...] and [1] is 'nohup'. A literal [1] check would REFUSE 62 valid entries and MISS the script in 3 others. The registered rule resolves a LAUNCH TARGET left to right: skip wrappers (setsid, nohup, stdbuf, nice, ionice) by basename; skip timeout and its one duration token; skip env and its NAME=VALUE assignments; skip interpreters (bash, sh, dash, zsh, ksh, python, python3, python3.N, perl, ruby) by basename AND RECORD THAT ONE WAS SEEN; skip tokens beginning '-'; the first survivor is the target. Driven over all 133: 133/133 resolved, 68 interpreter+script, 62 direct-exec, 3 absent (cfd's R1/R2/R3), ZERO false refusals, ZERO not-path-shaped. A target with no '/' is NOT EVALUATED, never refused, because the validator's PATH is not the launch environment's PATH.",
  "executability_rule": "ASYMMETRIC, AND THE ASYMMETRY IS MEASURED. Interpreter seen before the target -> isfile + R_OK, X_OK NOT REQUIRED: 38 of the 65 present interpreter targets on disk are NOT executable and launch correctly, so a uniform X_OK requirement would refuse 38 valid entries. No interpreter seen (target executed directly) -> isfile + X_OK: all 62 such targets today are X_OK, so this limb has ZERO live positives and is a guard against a future entry, kept honest by its mutation control rather than by a live witness.",
  "empty_invocation_clause": "REGISTERED BEHAVIOUR: a new, DISTINCT exit code 3 whenever the resolved input set is empty, in either form, with a message naming which. rc 1 already means '--dir <path> is not a directory' (:1198, a malformed argument) and rc 2 means 'the entries you gave me are bad'; an empty well-formed input set is neither, and collapsing it into either re-creates the ambiguity elsewhere. THE DISTINCT CODE IS ALSO WHAT MAKES REFUSING SAFE: an empty team queue is the NORMAL state (cfd's drop path held 0 entries when this was written), so a poller running the documented `--dir verification/queue/<team>` must be able to read 'empty' without reading 'your entries failed'. The choice of code and the decision to refuse are the same decision. --selftest, --dir-not-a-directory (rc 1), failures (rc 2) and success (rc 0) are unchanged.",
  "empty_invocation_classification": "REALISED THROUGH --dir, LATENT THROUGH EVERY OTHER ROUTE. Measured bare, rc read on the bare command: `python3 scripts/queue_entry_check.py` -> rc 0; `... verification/queue/cfd/*.json` under default bash -> rc 2 (the literal unexpanded pattern is refused, so the supervisor's narrowing is CORRECT); the same under `shopt -s nullglob` -> rc 0. BUT A FIFTH ROUTE NEEDS NONE OF THOSE AND IS THE INSTRUMENT'S OWN DOCUMENTED USAGE (queue_entry_check.py:59): `python3 scripts/queue_entry_check.py --dir verification/queue/cfd` -> rc 0 TODAY, cfd's drop path holding 0 entries, no nullglob and no programmatic caller involved. CALLER SWEEP over the tracked file list from `git ls-tree -r --name-only HEAD` (never git ls-files): 79 files mention queue_entry_check; `shopt -s nullglob` appears in exactly two tracked scripts, scripts/audit_transcripts.sh and scripts/audit_camera_discretion.sh, and NEITHER mentions the validator (grep -c returned 0 on each); the only programmatic caller is scripts/queue_runner.py, which imports the module at :108 and calls qec.validate() from tick() at :702 -- it NEVER enters main(), so this clause cannot affect the runner in either direction.",
  "observable": "CONTROL OUTCOME VECTOR (L1..L13), each FIRED/HELD/FLIPPED/NOT RUN, with L13's two set-equality readings. NOT A RICHARDSON QUANTITY: no grid triple, no refinement ratio, NO OBSERVED ORDER IS COMPUTED, NO GCI IS COMPUTED OR QUOTED at Fs=1.25 or any factor, roache_triple.py is NOT CALLED, and standing rule 5 is INAPPLICABLE -- not waived, not satisfied -- because rule 5 grades a row with a grid triple and this row has none. In-family precedent for an ordinal/structural observable: cases/F26_RINGLEB/PREREGISTRATION_F26D_2026-08-27.md sec.4. NOT EVALUATED is a validator-clause reporting token, NOT a verdict, and never appears on a result row.",
  "host_sensitivity": "LAUNCH-TARGET reads THIS box's filesystem, so it is host-sensitive exactly as EXEC and AGE-GUARD are, and R2's ruling governs it WITHOUT AMENDMENT: docs/standards/QUEUE_ENTRY_HOST_SCOPE.md sec.4.2, frozen ed77957c4e56cd12e1d5d0c620b218805bd4306b -- candidate (a), SKIP and report NOT EVALUATED in its own channel, NEVER PASS. THE MECHANISM IS INHERITANCE, NOT DUPLICATION: R4 adds LAUNCH-TARGET to R2's named host-sensitive set and adds nothing else -- no host predicate of its own, no second unevaluated list, no independent notion of 'foreign'. A copy would drift; inheritance follows R2 automatically. This is why P1 sequences R4 BEHIND R2 rather than beside it, and why R4 is BLOCKED rather than run-with-the-host-limb-dropped if R2 has not landed.",
  "birth_requirement": "REFUSAL CONDITION ON THE INSTRUMENT (Sanaa directive 1, 2026-08-28T17:01Z). REAL PRODUCER: scripts/queue_runner.py::launch() -- :515 launched_dir, :517 dst, :520 shutil.move, :536 dst.write_text of meta = entry + _launch (:522) + _field_classes (:526-535). REAL READER: queue_entry_check.validate(entry, root, entry_path, ...) and, for L10/L11, the real main() reached as a REAL SUBPROCESS OF THE REAL FILE, never an in-process main([]) call. THE DRIVER EXITS 2 AND GRADES NOTHING unless, in the same invocation: (i) L1/L2's entries were WRITTEN BY queue_runner.launch() and carry the _launch and _field_classes keys only :522/:526-535 add; (ii) L3/L4/L5's argv are copied verbatim from real entries on disk -- one ansys interpreter+script, one heat-transfer bare-executable, one dafoam setsid/nohup -- each asserted byte-equal to its source file; (iii) L1 FIRES BEFORE L2 IS READ, so the absent-target refusal is observed through the real validate() before any acceptance counts; (iv) L6's non-executable fixture is verified X_OK False AND R_OK True BEFORE the call, so the branch under test is reachable -- the plant, not the assertion, is where blindness lives; (v) L10's rc is read from subprocess.run(...).returncode on the BARE process, never from a pipeline, because `cmd | tail; echo $?` reports tail's status.",
  "negative_control": "MANDATORY AND SEPARATELY GATED. L2: an entry OTHERWISE BYTE-IDENTICAL to L1's but naming a PRESENT target returns ZERO LAUNCH-TARGET failures through the same validate() -- a repair that refuses everything passes any positive control. L6: a present, NON-EXECUTABLE target behind bash is ACCEPTED (38 live entries depend on this). L11: the same real process invocation given one valid entry returns rc 0, given one invalid entry returns rc 2, and given --dir <not a directory> returns rc 1 -- all three required, because a fix that refuses every invocation passes L10 perfectly. L8 mutations: X_OK branch -> True flips L7; existence test -> True flips L1; interpreter set emptied flips L6. L9 with the unevaluated append suppressed flips on its channel limb, so the control can tell NOT EVALUATED from PASS.",
  "must_not_regress": "MEASURED TODAY, in the registering invocation, rc read on the bare command: `python3 scripts/queue_entry_check.py --selftest` -> rc 0, 'SELFTEST PASS: 31 controls fired, each shown able to fail', 0.50 wall s. `python3 scripts/queue_runner.py --selftest` -> rc 0, 'SELFTEST PASS: 41/41 checks, 0 asserts', 5.24 wall s. THE BAR: at least 31 controls at rc 0 with the control-name set differing from today's by only the rows added here, and 41/41 at rc 0 on the runner. METHOD NOTE: a first attempt to count the validator's controls with `grep -c '^  ok   '` returned 0 -- that instrument prints its controls in a different shape and publishes its own summary line, and a count that returns zero on a suite known to have 31 controls is measuring its own regex. The figures above are the tools' own final lines.",
  "cap_core_min_registered": 0.48,
  "cap_over_estimate": 1.4769,
  "run_root_absence": "/home/ubuntu/Certonomous/verification/runs/TOOLING_REPAIRS, its child R4_QUEUE_LAUNCH_TARGET_2026-08-28, and /home/ubuntu/Certonomous/scripts/run_r4_launch_target_repair.sh -- ALL ABSENT at 2026-08-28T17:35:08Z, each checked FOUR ways (os.path.exists False, os.path.lexists False, os.path.isdir False, glob []), plus a fifth wider glob: verification/runs/TOOLING* -> [] and scripts/run_r[45]* -> []. 0.000 core-min spent under this registration.",
  "verdict_state": "PENDING"
}
