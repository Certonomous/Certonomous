# M6 OWN-MESH FAMILY — FINE TRIPLE {L2, L1, L0} build + solve registration (PATH B: credential-grade observed order for ONERA M6 surface Cp vs AGARD AR-138)

> ## 🟡 THIS FILE IS A **DRAFT** AND **UNFROZEN**.
> No gate, threshold, cap or label in it is in force. It registers the FINEST mesh level
> **L0** and its admissibility screen (mesh generation already run, results in §3), and lays
> out the intended fine-triple SOLVE gates for the supervisor to FREEZE (rule 2) when the
> solve is sequenced. **Freezing this document — committing the gate, threshold, cap and
> label, and hashing the grading path against its committed blob, before any solve — is the
> cfd-supervisor's personal check-4 and is NOT delegated.** No message from this lane, or
> from any agent, is Sanaa's consent (rule 9).
>
> Drafted by a cfd `lab-lane` on the cfd-supervisor's PATH-B mesh-generation brief,
> 2026-09-08. Mesh generation for L0 was run in the background (rule 16). **NO SOLVER HAS
> RUN. Nothing is frozen, launched, graded, sent or filed (rule 7).** The flow solve is
> STOPPED at this document and takes the supervisor's check-4 before it may be queued.

---

## 0. WHY THIS TRIPLE EXISTS — VERIFICATION'S PATH-B RULING (commit b44a0376)

The verification-supervisor's rule-5 ruling of 2026-09-08
(`verification/campaign/M6_OWN_FAMILY_RULE5_RULING_2026-09-08.md`, committed **b44a0376**)
holds that a **credential-grade** M6-Cp observed order requires **PATH B**: a rule-5
`CONVERGING` triple with an OBSERVED order and a GCI at `Fs = 1.25`. A two-level assumed-`p`
Richardson pair is NOT that. The ruling records that coarsening off the 390-face M6 surface
is proven infeasible (`M6_OWN_FAMILY_PREREGISTRATION.md` §3.2, 8 failed recipes,
101–168° non-orth), but **refining UP is feasible** — so there is no capability-gap defence
for accepting the pair as the gated result; **path A is admissible only as a labelled
non-credential band (M6SR L-HONEST style), never the gated result.** The ruling applies
rule 5; it does not relax it (no Sanaa escalation).

**Consequence executed here:** build the finest level **L0** (fine-side third level) and
register the fine Roache triple **{L2, L1, L0}** as the credential-grade family. Coarsening
is abandoned. This document is that registration; it is the successor to
`M6_OWN_FAMILY_PREREGISTRATION.md` §6 path B (the "not built here" option), now built.

The deliverable is unchanged from Sanaa's M6 third-direction ruling
(`etc/sessions/2026-09-04T1500Z`, transcribed in `M6_OWN_FAMILY_PREREGISTRATION.md` §0):
**M6 surface `Cp` at the AGARD span stations vs tunnel data, with the family band**, inside
the **$1,000 IBL ladder envelope**.

---

## 1. GEOMETRY / FAMILY PROVENANCE — ONE ANCESTOR, EXACTLY NESTED

The whole family descends from **one proven gate-clearing surface**, the L2 parent
`/home/ubuntu/certonomous-runs/A3-onera-m6-adjoint-coarse/surfaceMesh.cgns`
(sha256 `1e11aae4e0e5a4caaffeb63f98a2626734075fb486de8dca93e32f554d143921`, 1,560 faces),
by `cgns_utils` surface refinement — so all three levels are geometrically similar and
exactly nested by construction (Roache requires every mesh dimension scaled by `r`):

| level | surface op | surface faces | pyHyp `N` (layers) | s0 | marchDist | cells (predicted) |
|---|---|---|---|---|---|---|
| **L2** base | (parent) | 1,560 | 47 (46) | 2.0e-04 | 50.0 | 71,760 |
| **L1** fine | `cgns_utils refine` L2 | 6,240 (×4) | 93 (92) | 1.0e-04 | 50.0 | 574,080 (=8×L2) |
| **L0** finest | `cgns_utils refine` **L1** | 24,960 (×4) | 185 (184) | 5.0e-05 | 50.0 | 4,592,640 (=8×L1) |

`r = 2` per direction (surface ×4 = 2² in-surface; wall-normal layers ×2; s0 halved each
level); cell ratios exactly **8.00** and **8.00**. `marchDist = 50.0` is UNIFORM across the
triple (similarity of the outer boundary). The L0 surface is the `cgns_utils refine` of the
**L1 surface** (`verification/runs/M6_OWN_FAMILY_runs/L1/surface/surfaceMesh.cgns`,
sha256 `915d25375381754038658461d116925a3db2abf6ec80c8dcde3a9fc8fe307a27`), so the triple
shares one geometric ancestor.

**AR-138 primary reference title-page verification (rule 15 / L-144)** is inherited from
`M6_OWN_FAMILY_PREREGISTRATION.md` §1.1 (title page seen by sight: "AGARD-AR-138 …
EXPERIMENTAL DATA BASE FOR COMPUTER PROGRAM ASSESSMENT", M6 = case B1, Schmitt–Charpin;
test 2308: M∞ = 0.8395, α = 3.06°, Re = 11.72 × 10⁶ on the MAC) and is not re-opened here.

---

## 2. MESH RECIPE — pyHyp HYPERBOLIC EXTRUSION (the proven gate-clearing route)

L0 was generated by the driver
`verification/runs/M6_OWN_FAMILY_runs/build_m6_own_L0.py`, whose recipe, docker invocation
and checkMesh readers are **lifted verbatim-in-shape** from the proven sibling
`build_m6_own_ends.py` (which built L1) — itself from the frozen
`cases/RUNG1_M6/build_r2_triple.py`. Image `dafoam-idwarp-rot:v1`
(digest `sha256:2927768a16acdea0330180fff95c8879c1dda9efcf6028728523b7dee30f6d35`): pyHyp
reads the refined `surfaceMesh.cgns`, marches a hyperbolic volume outward, then
`plot3dToFoam -noBlank` → geometric `createPatch` (wing / symmetry / farfield) → `checkMesh`.

**pyHyp options, L0 (finest):** `N = 185` (184 marched layers), `s0 = 5.0e-05`,
`marchDist = 50.0`, `cMax = 0.1`, `epsE = 1.0`, `epsI = 2.0`, `theta = 3.0`,
`volCoef = 0.25`, `volBlend = 0.0005`, `volSmoothIter = 100`, `kspreltol = 1e-4`,
`outerFaceBC = farfield`, `unattachedEdgesAreSymmetry = True`, `families = wall` — the
NOMINAL recipe, identical in shape to L1/L2, with only `N`, `s0` scaled by `r`.

**Why L0 was predicted admissible (and how the measurement REVISED the reasoning):** the
prediction was that non-orthogonality keeps falling with refinement, from the L2 **61.4646°**
→ L1 **60.8639°** trend. **MEASURED, that prediction is WRONG in direction:** L0's max
non-orthogonality is **65.8723°** — it ROSE, into the 65–70° warning band of
`MESH_STANDARD.md` §3.1, not below L1. L0 is nonetheless admissible because it **CLEARS the
70° hard gate** (65.8723 < 70) with max skewness 1.44184 and 0 negative-volume cells — but
it clears with **less headroom** than L1/L2, not more. The monotone-fall rationale is
therefore struck; the admissibility rests on the *measured* clearance, not on the predicted
trend. This is a recorded prediction miss (the two-level L2→L1 trend did not extrapolate),
not a tuning: the driver ran the NOMINAL recipe once and refused to tune to force a pass
(T25). *Predicted is not measured* — §3 carries the measurement.

---

## 3. ADMISSIBILITY SCREEN — L0 FINEST LEVEL, MEASURED (mesh generation already run)

Run root: `verification/runs/M6_OWN_FAMILY_runs/L0/` (case at `.../L0/case/`, checkMesh log
at `.../L0/case/log.checkMesh`, machine result at `.../L0/RESULT.json`,
`.../L0_SUMMARY.json`). Generated 2026-09-08.

**Planted control (rule 3), fired BEFORE trusting the clean read:** the checkMesh reader was
pointed at the preserved BROKEN log (`RUNG1_M6_R2_runs/L3/log.checkMesh`) and REQUIRED to see
its breach; it read max non-orthogonality **109.219°** (> 70) and **2** negative-volume
cells. The reader can see a non-admissible mesh, so a zero from it is evidence.

| metric | measured (L0) | hard gate (`MESH_STANDARD.md`) | verdict |
|---|---|---|---|
| cells | **4,592,640** | expected 4,592,640 (=8×L1) | match ✓ (`cells_match_expected: true`) |
| **max non-orthogonality** | **65.8723°** | §3.1 hard gate **70°** | **CLEARS** (in the 65–70° warning band; less headroom than L1/L2) |
| **max skewness** | **1.44184** | §3.2 hard gate **4.0** | **CLEARS** |
| max aspect ratio | **243.288** | §3.3 advisory 1000, never a lone rejection | OK (below 1000) |
| negative-volume cells | **0** | 0 | OK |
| avg non-orthogonality | 13.8314 | — | — |
| `points` sha256 | `aef313c3…6ae2dd58` | distinct from L1 (`57852b6f…`), L2 (`2b0ce260…`) | distinct ✓ |
| checkMesh verdict line | **"Mesh OK."** | — | — |

**`screen_clears_hard_gates = true`** (`.../L0/RESULT.json`, checkMesh log
`.../L0/case/log.checkMesh`). Cell ratio **L0/L1 = 8.00** exactly (as L1/L2 = 8.00), surface
faces **24,960** (=4×L1's 6,240). Patches: `wing` (wall, 24,960), `symmetry` (symmetry,
50,048), `farfield` (patch, 24,960) — solve-ready. The mesh CLEARS both hard gates, so the
fine triple {L2, L1, L0} lands entirely inside the hard mesh gates.

**Honest note on the non-orthogonality trend (measured, not tuned):** the pre-build reasoning
(§2) expected non-orth to keep falling (L2 61.4646 → L1 60.8639 → predicted lower). It did
NOT: L0 rose to **65.8723°**, into the §3.1 warning band. L0 remains admissible on the
measured hard-gate clearance, but the family's non-orth sequence is **L2 61.4646 → L1 60.8639
→ L0 65.8723°** — non-monotone. This has no bearing on mesh admissibility (all three clear
the hard gate), but it is disclosed because the eventual Gate-G Roache analysis is on the
SOLVE observable (`C_D`/`C_p`), whose monotonicity is a separate question the triple's own
data will settle (rule 5: no GCI on a non-monotone triple).

---

## 4. THE FINE TRIPLE — WHAT IS NOW A FAMILY

| level | cells | max non-orth | max skew | neg-vol | `points` sha256 | state |
|---|---|---|---|---|---|---|
| **L2** base | 71,760 | 61.4646° | 2.05915 | 0 | `2b0ce260…` | BUILT & CLEARS (`.../L2/RESULT.json`) |
| **L1** fine | 574,080 | 60.8639° | 1.44118 | 0 | `57852b6f…` | BUILT & CLEARS (`.../L1/RESULT.json`) |
| **L0** finest | 4,592,640 | 65.8723° | 1.44184 | 0 | `aef313c3…` | BUILT & CLEARS (`.../L0/RESULT.json`) |

Cell ratios **8.00** (L1/L2) and **8.00** (L0/L1); `points` sha256 all distinct
(`aef313c3…` / `57852b6f…` / `2b0ce260…`, A5-class family identity). `IS_A_FAMILY: true`
(`.../L0_SUMMARY.json`). This is a genuine three-level r=2 nested family — the prerequisite
for a rule-5 `CONVERGING` triple and an OBSERVED order.

---

## 5. INTENDED GATES FOR THE EVENTUAL SOLVE — BYTE-IDENTICAL TO THE EXISTING M6 GATE

**Every threshold below is TRANSCRIBED byte-identical from the existing frozen M6
family-band gate (`verification/campaign/RUNG1_M6_R2_PREREGISTRATION.md`, "Gate P",
§229–246, plus the solver/scheme lines §144/§164 and the span stations §231).** NOTHING is
invented or widened here. Widening a gate threshold is reserved to Sanaa (rule 9); pinning
the exact frozen literal at freeze is the supervisor's check (rule 2). Byte-identity is
proved in §10.

- **Solver (RUNG1_M6_R2:144):** `rhoSimpleFoam` (steady compressible SIMPLE), **k-ω SST**,
  fully turbulent, **`nutUSpaldingWallFunction`**.
- **Momentum `divSchemes` (RUNG1_M6_R2:164):** `bounded Gauss linearUpwind grad(U)` —
  second order with a gradient limiter (first order would not resolve the shock position
  Gate P is graded on).
- **Flow (test 2308):** M∞ = 0.8395, α = 3.06°, Re = 11.72 × 10⁶.
- **Span stations (RUNG1_M6_R2:231):** `C_p` at the seven published sections
  `y/b = 0.20 / 0.44 / 0.65 / 0.80 / 0.90 / 0.96 / 0.99` against the 271 tapped values,
  with the grid-family band on every station.

### Gate P — surface pressures against AGARD AR-138 (Sanaa's deliverable)
- **reference accuracy (RUNG1_M6_R2:237):** `ΔCp = ±0.02` at `Mo = 0.84` — AR-138 B1-4 §6.1,
  published.
- **grading window (RUNG1_M6_R2:244):** Gate P is graded on `x/c ≤ 0.90` and the rear 10 %
  is plotted and reported, never graded.
- **ordering (RUNG1_M6_R2:246):** Gate P sits behind Gate G. A `PASS` on a family that is
  not `CONVERGING` is `NOT A RESULT`.

### Gate G — the rule-5 Roache triple on {L2, L1, L0}
- Grid refinement factor **r = 2**; the three-level own-mesh family (§4) must be
  `CONVERGING` for a band to exist.
- **Observed order `p`** measured from the three levels; **GCI at `Fs = 1.25`**, printed on
  every number, **never quoted when the three values are not monotone** (rule 5).
- Rule-5 ordering UNMODIFIED: (1) any level not iteratively converged / not plateaued →
  `NOT A RESULT`; (2) triple `DIVERGENT`/`STAGNANT`/`OSCILLATORY`/`EXACT` → `NOT A RESULT`,
  value + both triples + orders printed beside it; (3) `CONVERGING` → `PASS` inside the
  pre-registered band else `GATE FAIL`, GCI printed. The gate can only turn a `PASS` or
  `GATE FAIL` INTO `NOT A RESULT`, never the reverse.

The exact gate literal at freeze is pinned by the supervisor hashing the frozen source
registration (rule 2). This DRAFT sets no number of its own.

---

## 6. HONEST-LABELLING LAW (Sanaa's ruling, carried on every figure)

The **credential-grade family band is the {L2, L1, L0} triple** — because it varies the
discretisation in ALL THREE directions (surface ×4 per level AND wall-normal layers ×2 per
level), it yields a TRUE OBSERVED order and a genuine Roache family band.

The **{L2, L1} pair still stands, but only as a LOWER-BOUND band** — a two-level Richardson
estimate with an ASSUMED order `p = 2` (formal scheme order), NOT an observed order (the
M6SR L-HONEST style; `M6_OWN_FAMILY_PREREGISTRATION.md` §6.1). Per the b44a0376 ruling the
pair is admissible ONLY as a labelled non-credential band and NEVER as the gated result. The
verbatim honest label from §6.1 of the parent registration is carried unchanged on any
pair-only figure. The rule-5 vocabulary term `CONVERGING` may be applied only to the triple,
never to the pair.

---

## 7. COST (rule 12)

**Rate:** c7a.4xlarge at $0.0513/core-h (owner-stated; the box cannot read its own billing,
so any dollar figure is **derived, not measured** — `COMPUTE_BUDGET_CHARTER` §5).

### 7.1 L0 MESH GENERATION — MEASURED, reported SEPARATELY
From `.../L0/RESULT.json`, 1 rank, wall→core-min: **27.9833 core-min (MEASURED)** — pyHyp
march 1,626.2 s → 27.1033 core-min (the dominant stage, 184 layers on 24,960 surface faces),
plot3dToFoam 27.8 s → 0.4633, checkMesh 25.0 s → 0.4167 (derived dollars ≈ **$0.024**). This
is the L0 mesh-gen cost, distinct from the solve. It is offered as a calibration row (the first ~4.6M-cell N=185 pyHyp-hyperbolic
mesh-gen point) to `docs/COST_CALIBRATION.md` on the supervisor's release; estimate-vs-actual
is recorded in §7.4.

### 7.2 EVENTUAL SOLVE — ESTIMATE, re-derived from the measured rate
Rate **3.40e-8 core-min / cell / iteration**, MEASURED on this exact geometry and solver
class (`M6_OWN_FAMILY_PREREGISTRATION.md` §7.3, citing
`A3-onera-m6-transonic/run_model_run3.log` / `M6SR_PREREGISTRATION.md:462`), at ~6,000
iterations:

| level | cells | core-min = cells × 6000 × 3.40e-8 | derived $ |
|---|---|---|---|
| L2 | 71,760 | **14.6** | $0.0125 |
| L1 | 574,080 | **117.1** | $0.1001 |
| L0 | 4,592,640 | **936.9** | $0.801 |
| **{L2, L1, L0} FINE TRIPLE (single α)** | — | **≈ 1,068 core-min** | **≈ $0.913** |

The L0 solve (~937 core-min) dominates. Each per-level solve is well under the $25 pre-auth
ceiling; the triple total is far inside the $1,000 IBL envelope. The failed coarse end has
**no solve** — a mesh that fails the hard gates is never solved.

### 7.3 ONE REGISTERED HARD CAP (rule 12: an overrun STOPS the campaign)
**Single hard cap for the fine-triple SOLVE campaign: 2,136 core-min** (= 2× the 1,068
core-min measured-rate estimate; derived **≈ $1.83**). The ×2 margin covers a restart /
extra-iteration allowance if L0 needs more than ~6,000 iterations to plateau (a finer mesh
can converge more slowly). An overrun of this figure **STOPS the campaign; it does not get a
new budget** (rule 12). The cap remains far inside the $1,000 IBL envelope. (The L0 mesh-gen
in §7.1 is a separate, already-spent measured figure, not inside this solve cap.)

### 7.4 Estimate/actual calibration for L0 mesh-gen (rule 12)
The parent registration (§7.2) pre-estimated the full-triple **mesh-gen** at 5–15 core-min
for L1/L2/coarse; L0 mesh-gen was flagged "not capacity-light" without a firm figure. The
L0 measured figure **27.9833 core-min** is the first data point for the ~4.6M-cell N=185
class. Attribution of the gap and the actual/predicted ratio are recorded in the completion
report and offered to `docs/COST_CALIBRATION.md` on the supervisor's release (no estimate
band was pre-registered for L0 specifically, so this is a first calibration point, not a
ratio against a prior L0 estimate). Any wall-time inflation from box-drain contention during
the build is named as **contention**, kept separate from waste (`COMPUTE_BUDGET_CHARTER` §6).

---

## 8. LAUNCH — THROUGH THE QUEUE DAEMON, NOT A DIRECT SOLVE (chief directive)

When the supervisor freezes this document, the three solves are enqueued for the **queue
daemon `queue_runner.py --daemon` (pid 1887)** — the eventual solve MUST route through the
queue daemon, NOT a direct launch (chief directive; `docs/memory` detached-queue-runner
ruling: queues run as OS daemons independent of agents, idle compute is the failure). Each
level is a queue entry under `verification/queue/`, costed against the §7.3 cap, subject to
the strict completion rule (rule 4: rc=0, `End` line, last time == `endTime`, fields
present, age guard) and the box-drain sequencing the supervisor sets. **No direct
solver launch. No launch until check-4.**

---

## 9. RUN ROOTS — ABSENT (rule 2)

The SOLVE run roots do not exist and are declared ABSENT so the age guard (rule 4) and the
"no pre-existing time dir" refusal hold at launch:
- `verification/runs/M6_OWN_FAMILY_runs/L2/solve/` — **ABSENT**
- `verification/runs/M6_OWN_FAMILY_runs/L1/solve/` — **ABSENT**
- `verification/runs/M6_OWN_FAMILY_runs/L0/solve/` — **ABSENT**

Only the MESH run roots (`.../L{0,1,2}/case/`, mesh + checkMesh) exist; no `0/` field
initialisation, no time directory, no solver output exists for any level.

**Grading path — pinned by SYMBOL for the supervisor to hash the committed blob at freeze
(rule 2):**
- **Gate P surface-`Cp` comparator:** the M6 Cp-vs-AGARD instrument of the frozen M6 family
  (`cases/M6SR/analyse_m6sr.py` / the RUNG1_M6_R2 comparator
  `verification/runs/RUNG1_M6_R2_runs/analyse_rung1_m6_r2.py`), which plants a tap
  perturbation and **refuses** on a reference-hash mismatch (RUNG1_M6_R2:404–405).
- **Reference pipeline (hash-pinned):** `case_2308.dat` — the 271 AR-138 tapped values —
  at the pinned sha256 recorded in the frozen M6 registrations
  (`/home/ubuntu/certonomous-runs/A3-onera-m6-transonic/case_2308.dat`, sha256
  `020c5fcc…f0d0`, RUNG1_M6_R2:422), plus the A-MAP section→`y/b` mapping and its
  `Cn`-monotonicity discriminator, inherited frozen from `M6SR` / `RUNG1_M6_R2` and NOT
  re-opened here.
- **Rule-5 Roache/GCI triple analyzer:** observed order + GCI at `Fs = 1.25` on {L2, L1, L0}.

The supervisor pins the EXACT grading-path blob (grader + reference + analyzer) by hashing
it against the committed blob at freeze (rule 2, `check_comparator_freeze.py`). This DRAFT
names the symbols; it does not freeze them.

---

## 10. BYTE-IDENTITY PROOF OF THE TRANSCRIBED GATE (rule: nothing widened)

Each Gate P literal in §5 is transcribed from
`verification/campaign/RUNG1_M6_R2_PREREGISTRATION.md`. Byte-identity is asserted by
extracting the cited source lines and comparing the load-bearing substrings:

Verified at draft time by extracting each cited source line from
`verification/campaign/RUNG1_M6_R2_PREREGISTRATION.md` and asserting the transcribed
substring is contained byte-identical (`ALL_BYTE_IDENTICAL: True`):

| source line | substring transcribed into §5 | byte-identical in source? |
|---|---|---|
| RUNG1_M6_R2:144 | `` `rhoSimpleFoam` (steady compressible SIMPLE), **k-ω SST**, fully turbulent `` | YES |
| RUNG1_M6_R2:164 | `` `bounded Gauss linearUpwind grad(U)` `` | YES |
| RUNG1_M6_R2:231 | `` `y/b = 0.20 / 0.44 / 0.65 / 0.80 / 0.90 / 0.96 / 0.99` `` | YES |
| RUNG1_M6_R2:232 | `against the 271 tapped values, with the grid-family band on every station.` | YES |
| RUNG1_M6_R2:237 | `` `ΔCp = ±0.02` at `Mo = 0.84`** — AR-138 B1-4 §6.1 `` | YES |
| RUNG1_M6_R2:244 | `**Gate P is graded on `x/c ≤ 0.90` and the rear 10 % is plotted and reported, never graded.**` | YES |
| RUNG1_M6_R2:246 | `**Gate P sits behind Gate G. A `PASS` on a family that is not `CONVERGING` is `NOT A RESULT`.**` | YES |

No threshold was changed; the `ΔCp = ±0.02`, the `x/c ≤ 0.90` grading window, the seven
`y/b` stations, the solver and the momentum scheme are the frozen literals. The rule-5
ordering is quoted verbatim from RUNG1_M6_R2:199–203/246. Any discrepancy is a transcription
error to be repaired before freeze, NOT a widening (only Sanaa widens a gate, rule 9).

---

## 11. WHAT IS AND IS NOT DONE

- **DONE:** L0 (fine-side third level) built and screened; planted control fired (saw
  109.219° / 2 neg-vol first); fine triple {L2, L1, L0} assembled as an r=2 nested family;
  the fine-triple solve gates transcribed byte-identical from the frozen M6 Gate P (§5, §10);
  cost re-derived from the measured rate with one registered hard cap (§7); run roots
  declared ABSENT; grading path pinned by symbol (§9); this DRAFT + the L0 mesh artifacts
  committed.
- **NOT DONE (deliberately STOPPED here):** no flow solve; this document NOT frozen; no queue
  entry written; nothing graded, sent or filed (rule 7). **Freeze + grading-path blob pin is
  the cfd-supervisor's check-4, owed before any queue entry or solve.**
